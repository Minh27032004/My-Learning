window.LESSONS["04:sql:truy-van-con-subquery-tuong-quan-va-khong-tuong-quan"] = {

tom_tat: [
  "Subquery không tương quan được tính như một nguồn dữ liệu độc lập; PostgreSQL có thể biến nó thành `InitPlan`, hash hoặc phép nối tùy hình dạng truy vấn.",
  "Subquery tương quan tham chiếu cột của hàng phía ngoài nên mang nghĩa “tính theo từng hàng”, nhưng optimizer vẫn có thể khử tương quan; phải đọc `EXPLAIN`, không đoán số lần chạy từ cú pháp.",
  "`EXISTS` diễn đạt phép kiểm tra tồn tại, `IN` diễn đạt thuộc tập, còn scalar subquery bắt buộc trả nhiều nhất một hàng; ba dạng có quy tắc `NULL` và lỗi khác nhau.",
  "Trong hệ thống Sao Mai, cùng câu hỏi “khách nào có đơn lớn hơn mức bình thường của chính họ?” minh họa vì sao tương quan hữu ích và vì sao index `(customer_id, total_amount)` có thể quyết định chi phí.",
  "Subquery không mặc nhiên chậm hơn `JOIN`; lựa chọn đúng phụ thuộc ý nghĩa cần giữ, độ chọn lọc, khả năng nhân bản hàng và query plan thực tế."
],

can_biet_truoc: [
  "Biết `SELECT`, `WHERE`, `GROUP BY`, aggregate function và thứ tự xử lý logic của một truy vấn.",
  "Phân biệt `INNER JOIN` với `LEFT JOIN`, đặc biệt hiện tượng một hàng bên trái bị nhân lên khi khớp nhiều hàng bên phải.",
  "Hiểu `NULL` và logic ba giá trị `TRUE`, `FALSE`, `UNKNOWN` của SQL.",
  "Biết mục đích của index B-tree và cách đọc các nút cơ bản `Seq Scan`, `Index Scan`, `Nested Loop` trong `EXPLAIN`."
],

dinh_nghia: "**Subquery** (truy vấn con) là một câu `SELECT` nằm bên trong biểu thức hoặc mệnh đề của câu SQL khác. Kết quả của nó có thể là một giá trị duy nhất, một hàng, một cột nhiều hàng, hoặc một quan hệ dùng trong `FROM`.\n\nSubquery **không tương quan** không tham chiếu cột do truy vấn bên ngoài cung cấp, vì vậy về mặt ngữ nghĩa nó có thể được đánh giá độc lập. Subquery **tương quan** chứa một tham chiếu như `o.customer_id = c.customer_id`, trong đó `c` thuộc truy vấn ngoài; kết quả của phần trong vì thế phụ thuộc hàng ngoài đang xét. “Phụ thuộc về ngữ nghĩa” không đồng nghĩa PostgreSQL chắc chắn chạy lại nguyên khối cho từng hàng: optimizer có quyền viết lại thành semi join, anti join hoặc kế hoạch khác miễn kết quả không đổi.",

vi_sao_can: "Một câu hỏi nghiệp vụ thường lồng hai cấp phạm vi. “Liệt kê đơn cao hơn giá trị trung bình toàn cửa hàng” cần so mỗi đơn với một con số tổng quát. “Liệt kê đơn cao hơn giá trị trung bình của chính khách đó” lại cần một ngưỡng thay đổi theo khách. Nếu ép cả hai vào chuỗi `JOIN` và `GROUP BY`, ý định bị chìm trong kỹ thuật gom nhóm; tệ hơn, nối `orders` với `order_items` có thể nhân bản đơn rồi làm trung bình sai.\n\nSubquery cho phép đặt câu hỏi phụ ngay tại nơi dùng câu trả lời. Nó đặc biệt phù hợp với kiểm tra tồn tại: cửa hàng chỉ cần biết khách có đơn đã thanh toán hay không, chứ không cần lấy tất cả đơn rồi `DISTINCT` để chữa số hàng bị nhân. Tuy nhiên cú pháp súc tích có thể che một vòng lặp đắt: 100.000 khách, mỗi khách quét lại toàn bộ `orders`, sẽ tạo lượng công việc gần tích hai kích thước. Vì vậy học subquery phải gắn với cardinality, index và `EXPLAIN ANALYZE`, không dừng ở việc câu lệnh trả đúng dữ liệu.",

co_che: "### Một bộ dữ liệu, hai phạm vi so sánh\n\nHệ thống Sao Mai dùng `customers(customer_id, full_name)`, `orders(order_id, customer_id, ordered_at, status, total_amount)` và `order_items`. Với câu “đơn lớn hơn trung bình cửa hàng”, ngưỡng không phụ thuộc đơn hiện tại:\n\n```sql\nSELECT order_id, customer_id, total_amount\nFROM orders\nWHERE total_amount > (SELECT avg(total_amount) FROM orders);\n```\n\nSubquery trả đúng một ô. PostgreSQL có thể tính ô ấy một lần dưới dạng `InitPlan`, lưu tham số nội bộ rồi quét `orders` phía ngoài. Nếu thêm một đơn, ngưỡng thay đổi cho cả kết quả; nhưng trong một statement, mọi hàng nhìn cùng snapshot theo MVCC nên không có chuyện trung bình đổi giữa chừng.\n\nCâu “đơn vượt trung bình của chính khách” đổi phạm vi:\n\n```sql\nSELECT o.order_id, o.customer_id, o.total_amount\nFROM orders AS o\nWHERE o.total_amount > (\n  SELECT avg(i.total_amount)\n  FROM orders AS i\n  WHERE i.customer_id = o.customer_id\n);\n```\n\nAlias `i` và `o` không phải trang trí. Chúng chỉ rõ hai lần xuất hiện độc lập của cùng bảng và làm lộ liên kết tương quan.\n\n### Scalar subquery và hợp đồng cardinality\n\nMột subquery đứng sau toán tử `=` hoặc `>` là **scalar subquery**: nó phải trả tối đa một hàng, một cột. Không có hàng thì giá trị là `NULL`; hơn một hàng khiến PostgreSQL báo lỗi “more than one row returned by a subquery used as an expression”. `avg` luôn tạo một hàng, kể cả tập đầu vào rỗng, nên ví dụ trên không vi phạm cardinality.\n\n```sql\n-- Sai nếu một khách có nhiều đơn\nSELECT full_name,\n       (SELECT total_amount FROM orders o\n        WHERE o.customer_id = c.customer_id)\nFROM customers c;\n```\n\nThêm `LIMIT 1` chỉ dập lỗi, chưa định nghĩa đúng nghiệp vụ. Không có `ORDER BY`, “một hàng bất kỳ” không ổn định; nếu cần đơn mới nhất phải viết `ORDER BY ordered_at DESC, order_id DESC LIMIT 1` và cân nhắc index cùng thứ tự.\n\n### `EXISTS` là phép hỏi có hay không\n\n`EXISTS (subquery)` chỉ quan tâm ít nhất một hàng được sinh ra. Danh sách `SELECT` bên trong không mang dữ liệu ra ngoài, nên quy ước dễ đọc là `SELECT 1`:\n\n```sql\nSELECT c.customer_id, c.full_name\nFROM customers c\nWHERE EXISTS (\n  SELECT 1\n  FROM orders o\n  WHERE o.customer_id = c.customer_id\n    AND o.status = 'paid'\n);\n```\n\nExecutor có thể dừng tìm ở lần khớp đầu tiên. Về đại số quan hệ đây là semi join: mỗi khách xuất hiện tối đa một lần dù có mười đơn. Viết `JOIN orders ... SELECT DISTINCT` buộc hệ quản trị tạo các hàng trung gian rồi loại trùng, vừa che ý nghĩa vừa có thể tốn sort hoặc hash.\n\n### `NOT EXISTS` và chiếc bẫy `NOT IN`\n\nTìm khách chưa từng đặt hàng nên dùng anti join dưới dạng `NOT EXISTS`:\n\n```sql\nSELECT c.customer_id, c.full_name\nFROM customers c\nWHERE NOT EXISTS (\n  SELECT 1 FROM orders o\n  WHERE o.customer_id = c.customer_id\n);\n```\n\n`NOT IN (SELECT customer_id FROM orders)` trông tương đương nhưng khác khi tập con chứa `NULL`. Phép `7 <> NULL` cho `UNKNOWN`; chuỗi so sánh của `NOT IN` vì thế không thành `TRUE`, và có thể loại mọi khách. Khóa ngoại thường nên `NOT NULL`, song câu truy vấn bền vững không nên dựa vào một giả định chưa kiểm tra. `NOT EXISTS` so khớp bằng điều kiện rõ ràng và không bị một `NULL` không liên quan đầu độc toàn tập.\n\n### `IN`, equality và tập nhiều hàng\n\n`IN` thích hợp khi câu hỏi thật sự là thuộc một tập:\n\n```sql\nSELECT product_id, product_name\nFROM products\nWHERE category_id IN (\n  SELECT category_id FROM categories WHERE is_active\n);\n```\n\nPostgreSQL thường biến dạng này thành hash semi join. Nếu subquery trả giá trị trùng, kết quả ngoài vẫn không nhân hàng. Đây là khác biệt quan trọng với `JOIN`: phép nối có thể trả một sản phẩm nhiều lần nếu phía danh mục vô tình không duy nhất. Khi tập nhỏ là hằng số, `IN (1, 2, 3)` rõ hơn một bảng tạm; khi cần cột từ cả hai phía, `JOIN` mới đúng công cụ.\n\n### Subquery trong `FROM` và ranh giới quan hệ\n\nSubquery ở `FROM` tạo một quan hệ dẫn xuất. Nó hữu ích để gom hạt dữ liệu trước khi nối:\n\n```sql\nSELECT c.customer_id, c.full_name, s.paid_total\nFROM customers c\nJOIN (\n  SELECT customer_id, sum(total_amount) AS paid_total\n  FROM orders\n  WHERE status = 'paid'\n  GROUP BY customer_id\n) s USING (customer_id)\nWHERE s.paid_total >= 1000000;\n```\n\nKhối `s` có đúng một hàng mỗi khách, nên phép nối không làm sai hạt “khách hàng”. Trên PostgreSQL hiện đại, subquery đơn giản thường được kéo lên và tối ưu cùng truy vấn ngoài; dấu ngoặc không nhất thiết tạo bảng tạm hay hàng rào vật lý.\n\n### Tương quan không đồng nghĩa vòng lặp vật lý\n\nCú pháp tương quan gợi hình ảnh chạy phần trong N lần. Đó là mô hình ngữ nghĩa an toàn để hiểu kết quả, không phải lời tiên tri về plan. Với `EXISTS`, optimizer thường decorrelate thành `Hash Semi Join`. Với scalar aggregate phụ thuộc `customer_id`, plan có thể vẫn hiện `SubPlan` dưới một scan phía ngoài; dòng `loops=...` trong `EXPLAIN (ANALYZE, BUFFERS)` mới cho biết số lần thực tế.\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT o.order_id\nFROM orders o\nWHERE o.total_amount > (\n  SELECT avg(i.total_amount)\n  FROM orders i\n  WHERE i.customer_id = o.customer_id\n);\n```\n\nNếu `SubPlan` chạy 200.000 vòng và mỗi vòng `Seq Scan`, vấn đề không phải từ khóa “subquery” mà là cách truy cập dữ liệu theo khóa tương quan. Không được chép số thời gian từ máy khác: dữ liệu, cache, phiên bản và cấu hình đều ảnh hưởng phép đo.\n\n### Index phục vụ khóa tương quan\n\nIndex `orders(customer_id)` giúp tìm các đơn của một khách mà không quét cả bảng. Với phép tính cần luôn đọc `total_amount`, index `(customer_id, total_amount)` còn cho phép index-only scan khi visibility map phù hợp:\n\n```sql\nCREATE INDEX orders_customer_amount_idx\nON orders (customer_id, total_amount);\n```\n\nThứ tự cột quan trọng vì điều kiện đầu tiên là equality trên `customer_id`; các khóa của một khách nằm kề nhau. Đổi thành `(total_amount, customer_id)` thường không hỗ trợ tốt việc tìm toàn bộ giá trị theo một khách. Đánh đổi là mỗi `INSERT`, `UPDATE customer_id/total_amount` phải duy trì thêm cây B-tree, tốn dung lượng và có thể giảm tốc ghi.\n\n### Viết lại bằng aggregate rồi nối\n\nKhi scalar subquery thực sự bị lặp nhiều lần, có thể tính trung bình một lần mỗi khách rồi nối:\n\n```sql\nSELECT o.order_id, o.customer_id, o.total_amount\nFROM orders o\nJOIN (\n  SELECT customer_id, avg(total_amount) AS avg_amount\n  FROM orders\n  GROUP BY customer_id\n) a USING (customer_id)\nWHERE o.total_amount > a.avg_amount;\n```\n\nCách này thường quét bảng theo lô, phù hợp khi cần xét phần lớn đơn. Nhưng nếu truy vấn ngoài chỉ lấy vài khách rất chọn lọc, aggregate toàn bảng có thể làm thừa việc; nested loop cùng index lại thắng. Quy tắc đúng là so plan trên phân bố dữ liệu đại diện, không phong thánh một cách viết.\n\n### Ca biên cần quyết định trước\n\nKhách chưa có đơn làm `avg` trả `NULL`; điều kiện `amount > NULL` là `UNKNOWN`, nên không hàng nào lọt qua. Một khách chỉ có một đơn thì đơn đó bằng trung bình của chính nó và không thỏa dấu `>`. Các giá trị `total_amount` bằng nhau cũng không tạo “trên trung bình”. Nếu cột tiền cho phép `NULL`, `avg` bỏ qua chúng; khách chỉ có đơn giá trị `NULL` nhận ngưỡng `NULL`. Cuối cùng, trung bình tiền nên dùng `numeric`, tránh chuyển vội sang số dấu phẩy động rồi kỳ vọng so sánh thập phân tuyệt đối.\n\n### Khi nào không nên dùng subquery\n\nKhông dùng scalar subquery tương quan để lấy nhiều thuộc tính từ cùng một hàng nếu mỗi thuộc tính lặp lại một subquery giống nhau; `LATERAL` hoặc một phép nối định vị hàng sẽ rõ và ít công việc hơn. Không dùng subquery để che một phép nối nhiều-nhiều chưa hiểu cardinality. Cũng không tách truy vấn thành hai lần gọi ứng dụng chỉ để “dễ đọc”: giữa hai statement dữ liệu có thể đổi, phát sinh round trip mạng và mất cơ hội tối ưu toàn cục của PostgreSQL.",

vi_du: "### Kịch bản PostgreSQL tự chứa\n\nĐoạn sau chạy trong một schema thử nghiệm và không cần dữ liệu ngoài:\n\n```sql\nCREATE TEMP TABLE customers (\n  customer_id bigint PRIMARY KEY,\n  full_name text NOT NULL\n);\nCREATE TEMP TABLE orders (\n  order_id bigint PRIMARY KEY,\n  customer_id bigint REFERENCES customers,\n  ordered_at timestamptz NOT NULL,\n  status text NOT NULL,\n  total_amount numeric(12,2)\n);\n\nINSERT INTO customers VALUES\n  (1, 'An'), (2, 'Bình'), (3, 'Chi'), (4, 'Dũng');\nINSERT INTO orders VALUES\n  (101, 1, '2026-08-01 09:00+07', 'paid', 100000),\n  (102, 1, '2026-08-03 09:00+07', 'paid', 300000),\n  (103, 2, '2026-08-02 09:00+07', 'cancelled', 900000),\n  (104, 2, '2026-08-04 09:00+07', 'paid', 100000),\n  (105, 3, '2026-08-05 09:00+07', 'paid', NULL);\n\nSELECT o.order_id, o.customer_id, o.total_amount\nFROM orders o\nWHERE o.total_amount > (\n  SELECT avg(i.total_amount)\n  FROM orders i\n  WHERE i.customer_id = o.customer_id\n)\nORDER BY o.order_id;\n-- 102 | 1 | 300000.00\n-- 103 | 2 | 900000.00\n\nSELECT c.customer_id, c.full_name\nFROM customers c\nWHERE NOT EXISTS (\n  SELECT 1 FROM orders o WHERE o.customer_id = c.customer_id\n)\nORDER BY c.customer_id;\n-- 4 | Dũng\n```\n\nAn có trung bình 200.000 nên chỉ đơn 102 qua. Bình có trung bình 500.000 nên đơn 103 qua dù trạng thái bị hủy; nếu nghiệp vụ chỉ tính đơn thanh toán, điều kiện `status = 'paid'` phải xuất hiện nhất quán cả hàng ngoài lẫn tập tính trung bình. Chi có duy nhất giá trị `NULL`, vì vậy `avg` là `NULL`. Dũng chứng minh anti join trên tập rỗng.\n\n### Đối chiếu hai cách viết\n\n```sql\nWITH correlated AS (\n  SELECT o.order_id\n  FROM orders o\n  WHERE o.total_amount > (\n    SELECT avg(i.total_amount) FROM orders i\n    WHERE i.customer_id = o.customer_id\n  )\n), joined AS (\n  SELECT o.order_id\n  FROM orders o\n  JOIN (\n    SELECT customer_id, avg(total_amount) avg_amount\n    FROM orders GROUP BY customer_id\n  ) a USING (customer_id)\n  WHERE o.total_amount > a.avg_amount\n)\nSELECT 'only_correlated' AS side, order_id FROM correlated\nEXCEPT\nSELECT 'only_correlated', order_id FROM joined;\n-- không có hàng: hai tập bằng nhau ở chiều này; đổi thứ tự để kiểm chiều còn lại\n```\n\nTrong dữ liệu thật, chạy `EXPLAIN (ANALYZE, BUFFERS, TIMING OFF)` cho từng bản trong một môi trường thử nghiệm. `TIMING OFF` giảm nhiễu đo từng nút nhưng vẫn báo tổng thời gian; `BUFFERS` cho biết số block cache/đĩa đã chạm. Không chạy `EXPLAIN ANALYZE` tùy tiện trên statement sửa dữ liệu vì nó thực thi thật.",

so_sanh: "### Chọn cấu trúc theo ý nghĩa\n\n| Cấu trúc | Hạt kết quả | Điểm mạnh | Rủi ro chính |\n|---|---|---|---|\n| Scalar subquery | một giá trị cho mỗi hàng ngoài | diễn đạt ngưỡng phụ thuộc cục bộ | lỗi nếu trả hơn một hàng; có thể thành SubPlan nhiều vòng |\n| `EXISTS` / `NOT EXISTS` | giữ hoặc loại hàng ngoài | không nhân bản hàng; dừng khi đủ bằng chứng | dễ quên điều kiện liên kết và biến thành kiểm tra toàn cục |\n| `IN` | kiểm tra thuộc một cột nhiều hàng | sát nghĩa tập hợp, tự bỏ ảnh hưởng của giá trị trùng | `NOT IN` nguy hiểm khi có `NULL` |\n| `JOIN` | kết hợp thuộc tính hai phía | lấy được nhiều cột, optimizer có nhiều lựa chọn | cardinality tăng nếu phía nối không duy nhất |\n| Aggregate rồi `JOIN` | gom theo khóa trước khi kết hợp | tránh tính aggregate lặp trên tập lớn | có thể gom cả bảng dù phía ngoài rất chọn lọc |\n\nKhông có hàng nào trong bảng nói “luôn nhanh hơn”. Nếu hai câu SQL không cùng hạt và cùng quy tắc `NULL`, benchmark giữa chúng là so hai bài toán khác nhau.",

loi_thuong_gap: [
  "Dùng `=` với subquery có thể trả nhiều hàng; nhận biết bằng lỗi cardinality khi dữ liệu tăng, sửa bằng cách xác định đúng aggregate hoặc chuyển sang `IN`/`EXISTS`, không chữa mù bằng `LIMIT 1`.",
  "Viết `NOT IN` trên cột nullable rồi nhận kết quả rỗng khó hiểu; kiểm tra xem tập con có `NULL`, ưu tiên `NOT EXISTS` với điều kiện liên kết tường minh.",
  "Quên alias phía trong và vô tình so một cột với chính nó; dấu hiệu là điều kiện luôn đúng hoặc `EXISTS` đúng cho mọi hàng, cần qualify mọi cột bằng alias.",
  "Lấy nhiều thuộc tính bằng nhiều scalar subquery tương quan giống nhau; `EXPLAIN` hiện nhiều `SubPlan` có số `loops` lớn, nên gom thành một phép nối hoặc `LATERAL`.",
  "Thay `EXISTS` bằng `JOIN` rồi thêm `DISTINCT`; số hàng trung gian phình ra và plan có `Sort`/`HashAggregate`, trong khi semi join diễn đạt đúng việc chỉ hỏi tồn tại.",
  "Tạo index sai thứ tự `(total_amount, customer_id)` cho điều kiện equality theo khách; plan vẫn quét nhiều vùng index, cần đặt khóa liên kết làm đoạn đầu khi phù hợp workload.",
  "Tin rằng subquery luôn chạy một lần hoặc luôn chạy N lần; chỉ `EXPLAIN (ANALYZE, BUFFERS)` cùng trường `loops` mới cung cấp bằng chứng cho plan cụ thể."
],

tu_kiem_tra: [
  "Vì sao scalar subquery dùng `avg` không báo lỗi nhiều hàng, và nó trả gì khi không có đầu vào không `NULL`?",
  "Hãy dựng một tập con chứa `(2, NULL)` rồi giải thích bằng logic ba giá trị vì sao `1 NOT IN (...)` không phải `TRUE`.",
  "Khi nào `EXISTS` bảo toàn cardinality của `customers` tốt hơn `JOIN orders`, và optimizer có thể dùng phép nối logic nào?",
  "Nếu truy vấn ngoài chỉ chọn 10 khách trong 10 triệu đơn, vì sao aggregate toàn bộ rồi nối có thể thua scalar subquery có index?",
  "Đọc một plan có `SubPlan`, `loops=50000` và `Seq Scan on orders`: đề xuất index, rồi nêu chi phí ghi mà index ấy gây ra.",
  "Viết lại câu “đơn cao hơn trung bình khách” bằng window function; giải thích vì sao phải đặt phép lọc ở một tầng truy vấn ngoài.",
  "Một lập trình viên thêm `LIMIT 1` để hết lỗi cardinality nhưng không có `ORDER BY`. Hãy chỉ ra hai lỗi về tính đúng đắn và tính ổn định."
],

tai_nguyen: [
  "PostgreSQL Documentation, các phần Subquery Expressions, Table Expressions và Using EXPLAIN.",
  "“SQL Antipatterns” của Bill Karwin, các chương về `NULL`, truy vấn nhiều hàng và thiết kế index.",
  "“SQL Performance Explained” của Markus Winand, các phần về nested loop, correlated subquery và index nhiều cột.",
  "“PostgreSQL: Up and Running” của Regina Obe và Leo Hsu, các chương truy vấn và phân tích kế hoạch."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu qua B-tree: thứ tự cột trong index quyết định vùng khóa nào có thể được tìm liên tục thay vì quét toàn cây.",
  "Nối sang mảng Thuật toán qua độ phức tạp: correlated SubPlan dạng nested loop có thể tiến gần tích kích thước hai tập, còn hash semi join đổi lấy bộ nhớ để xử lý theo lô.",
  "Nối sang mảng Backend qua lỗi N+1: một statement có subquery vẫn khác việc ứng dụng gửi hàng nghìn câu SQL qua mạng; round trip và snapshot làm phương án ứng dụng thường tệ hơn.",
  "Nối sang mảng Kiểm thử qua dữ liệu ca biên: bắt buộc có khách không đơn, giá trị `NULL`, nhiều hàng và giá trị bằng nhau để phát hiện sai khác giữa `NOT IN`, `NOT EXISTS` và scalar subquery."
]

};
