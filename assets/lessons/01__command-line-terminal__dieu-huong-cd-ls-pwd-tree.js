window.LESSONS["01:command-line-terminal:dieu-huong-cd-ls-pwd-tree"] = {

tom_tat: [
  "Terminal luôn đứng ở **một thư mục** gọi là *working directory*. Mọi đường dẫn tương đối bạn gõ đều tính từ chỗ đó.",
  "`pwd` cho biết đang ở đâu · `ls` xem có gì · `cd` di chuyển. Ba lệnh này chiếm phần lớn thao tác điều hướng.",
  "**`cd` không phải chương trình mà là lệnh dựng sẵn của shell** — nó buộc phải vậy, vì một chương trình con không đổi được thư mục của process cha.",
  "`cd -` quay lại thư mục vừa rời, `pushd`/`popd` giữ cả một ngăn xếp thư mục. Hai mẹo này tiết kiệm rất nhiều thời gian.",
  "**Bấm Tab để tự động hoàn thành** là kỹ năng đáng giá nhất trong bài. Nó vừa nhanh hơn vừa chống gõ sai tên."
],

can_biet_truoc: [
  "Bài **Hệ thống file, đường dẫn, quyền truy cập** — cần hiểu cấu trúc cây thư mục và đường dẫn tuyệt đối/tương đối.",
  "Đã mở được terminal (Git Bash trên Windows, Terminal trên macOS/Linux)."
],

dinh_nghia: "Nói đời thường: terminal giống như bạn đang **đứng ở một căn phòng** trong toà nhà. Muốn xem phòng này có gì thì nhìn quanh, muốn sang phòng khác thì đi. Khác với giao diện đồ hoạ ở chỗ bạn không nhìn thấy toàn cảnh — bạn phải chủ động hỏi \"tôi đang ở đâu\" và \"chỗ này có gì\".\n\nNói chính xác:\n\n- **Working directory** (thư mục làm việc) là thư mục mà process shell đang \"đứng\". Nó là một thuộc tính của process, và **mỗi process có một cái riêng**.\n- **Điều hướng** là thay đổi working directory và quan sát nội dung hệ thống file bằng các lệnh dòng lệnh.\n- **Shell** là chương trình đọc lệnh bạn gõ, phân tích, rồi thực thi — `bash`, `zsh`, `fish` là các shell phổ biến.",

vi_sao_can: "Terminal không phải để trông ngầu. Nó là công cụ bắt buộc vì những lý do rất cụ thể:\n\n- **Server không có giao diện đồ hoạ.** Mọi máy chủ Linux bạn SSH vào đều chỉ có dòng lệnh. Không dùng được terminal là không triển khai được gì.\n- **Lặp lại và tự động hoá.** Một thao tác gõ được thì viết được thành script; thao tác chuột thì không.\n- **Nhanh hơn hẳn khi đã quen.** Mở terminal gõ `cd du-an && code .` nhanh hơn nhiều so với mở File Explorer bấm qua bốn tầng thư mục.\n- **Mọi công cụ dev đều là dòng lệnh.** `git`, `docker`, `npm`, `pip`, `pytest` — không có bản đồ hoạ tương đương đầy đủ.\n- **Thông báo lỗi đầy đủ.** Giao diện đồ hoạ thường nuốt lỗi và chỉ hiện \"đã xảy ra sự cố\"; terminal in ra nguyên văn.\n\nTình huống thật: bạn `cd` vào thư mục dự án, chạy `python main.py`, và nhận `FileNotFoundError: data.csv`. File rõ ràng có ở đó. Nguyên nhân: bạn đang đứng ở thư mục cha, và đường dẫn tương đối trong code tính từ **thư mục bạn đang đứng** chứ không phải từ chỗ file `.py` nằm. Đây là lỗi rất phổ biến, và hiểu working directory là hết.",

co_che: "### Working directory là thuộc tính của process\n\n```\nProcess shell (PID 1234)\n  ├─ working directory: /home/minh/du-an\n  ├─ biến môi trường\n  └─ bảng file đang mở\n```\n\nMỗi process giữ working directory riêng. Hệ quả rất quan trọng:\n\n- Mở hai terminal là có hai working directory độc lập\n- Chương trình bạn chạy **kế thừa** working directory từ shell tại thời điểm khởi động\n- Chương trình con `cd` đi đâu cũng **không ảnh hưởng** shell cha\n\nĐiều cuối giải thích một chuyện tưởng lạ: **vì sao `cd` phải là lệnh dựng sẵn của shell**.\n\nNếu `cd` là một chương trình riêng, shell sẽ tạo process con để chạy nó, process con đổi thư mục của chính nó, rồi kết thúc — và shell cha vẫn đứng nguyên chỗ cũ. Hoàn toàn vô dụng. Nên `cd` buộc phải do chính shell thực hiện.\n\nKiểm chứng:\n\n```bash\ntype cd      # cd is a shell builtin\ntype ls      # ls is /usr/bin/ls        ← đây mới là chương trình thật\nwhich cd     # không tìm thấy — vì nó không phải file\n```\n\nCùng lý do đó, `export`, `source`, `alias` cũng phải là builtin: chúng đều sửa trạng thái của chính shell.\n\n### Ba lệnh nền tảng\n\n```bash\npwd                    # in đường dẫn tuyệt đối của thư mục hiện tại\nls                     # liệt kê nội dung\ncd /duong/dan          # di chuyển\n```\n\nCác dạng `cd` đáng thuộc:\n\n```bash\ncd /home/minh/du-an    # đường dẫn tuyệt đối\ncd du-an               # tương đối — tính từ chỗ đang đứng\ncd ..                  # lên thư mục cha\ncd ../..               # lên hai cấp\ncd ~                   # về thư mục nhà\ncd                     # cũng về thư mục nhà (viết tắt của cd ~)\ncd -                   # ⭐ quay lại thư mục VỪA RỜI\n```\n\n`cd -` là mẹo tiết kiệm thời gian nhất trong nhóm này. Đang ở `/var/log`, nhảy sang `/etc/nginx` xem cấu hình, rồi `cd -` là về lại `/var/log` ngay. Nó hoạt động nhờ shell giữ đường dẫn cũ trong biến `$OLDPWD`.\n\n### Bốn ký hiệu đường dẫn\n\n| Ký hiệu | Nghĩa | Ai xử lý |\n|---|---|---|\n| `.` | thư mục hiện tại | hệ điều hành |\n| `..` | thư mục cha | hệ điều hành |\n| `~` | thư mục nhà của bạn | **shell bung ra** |\n| `/` | thư mục gốc | hệ điều hành |\n\nDòng thứ ba đáng chú ý: **`~` do shell bung ra thành đường dẫn thật trước khi lệnh chạy**. Chương trình không bao giờ nhìn thấy ký tự `~`. Đây là lý do `open(\"~/file.txt\")` trong Python thất bại — Python không phải shell, nó nhận `~` nguyên văn và đi tìm một thư mục tên là `~`.\n\n```python\nimport os\nos.path.expanduser(\"~/file.txt\")   # bung ~ thủ công\n```\n\n### Đọc kết quả `ls -l`\n\n```bash\nls -l\n```\n\n```\n-rw-r--r--  1 minh dev   4096 Aug 30 10:23 bao-cao.txt\ndrwxr-xr-x  3 minh dev   4096 Aug 29 15:10 du-lieu\nlrwxrwxrwx  1 minh dev      9 Aug 28 09:00 loi-tat -> bao-cao.txt\n│└───┬───┘  │  │    │     │        │             │\n│    │      │  │    │     │        │             └─ tên\n│    │      │  │    │     │        └─ thời gian sửa lần cuối\n│    │      │  │    │     └─ kích thước (byte)\n│    │      │  │    └─ nhóm\n│    │      │  └─ chủ sở hữu\n│    │      └─ số liên kết cứng\n│    └─ quyền (đã học ở bài hệ thống file)\n└─ loại: - file · d thư mục · l symlink\n```\n\nCột kích thước của **thư mục** luôn là 4096 hoặc bội số — đó là kích thước của chính bản ghi thư mục, **không phải tổng dung lượng nội dung**. Muốn biết thư mục nặng bao nhiêu phải dùng `du`.\n\n### Các cờ `ls` dùng hằng ngày\n\n```bash\nls -l          # dạng dài, đầy đủ thông tin\nls -a          # hiện cả file ẩn (tên bắt đầu bằng dấu chấm)\nls -h          # kích thước dễ đọc: 4.0K, 2.3M thay vì số byte\nls -t          # sắp theo thời gian sửa, mới nhất trước\nls -r          # đảo ngược thứ tự\nls -S          # sắp theo kích thước, lớn nhất trước\n\nls -lah        # ⭐ tổ hợp dùng nhiều nhất\nls -lt | head  # ⭐ 10 file sửa gần đây nhất — rất hay khi tìm log mới\n```\n\nCác cờ một chữ cái **gộp được**: `-lah` bằng `-l -a -h`.\n\n### File ẩn\n\nTrên Unix, file có tên bắt đầu bằng `.` được coi là ẩn và `ls` bỏ qua mặc định:\n\n```bash\nls        # main.py  README.md\nls -a     # .  ..  .git  .env  .gitignore  main.py  README.md\n```\n\nĐây là quy ước thuần tuý, không phải thuộc tính đặc biệt của file. Nó ra đời tình cờ: lệnh `ls` đời đầu muốn giấu `.` và `..`, và cách hiện thực đơn giản nhất là bỏ qua mọi tên bắt đầu bằng dấu chấm. Quy ước đó tồn tại tới nay.\n\nHệ quả thực tế: **luôn nhớ `ls -a` khi tìm file cấu hình**. `.env`, `.gitignore`, `.github/` đều ẩn.\n\n### Nhìn cấu trúc bằng `tree`\n\n```bash\ntree              # vẽ cả cây từ thư mục hiện tại\ntree -L 2         # ⭐ chỉ 2 tầng — không có cờ này thì output tràn màn hình\ntree -d           # chỉ thư mục\ntree -a           # kể cả file ẩn\ntree -L 2 -I 'node_modules|__pycache__|.git'    # loại thư mục rác\n```\n\n```\n.\n├── assets\n│   ├── ai\n│   └── lessons\n├── docs\n└── index.html\n```\n\n`tree` thường phải cài thêm (`apt install tree`, `brew install tree`). Không có nó thì thay bằng:\n\n```bash\nfind . -maxdepth 2 -type d\n```\n\n### Tab: kỹ năng đáng giá nhất\n\n```bash\ncd du<Tab>          → cd du-an/\ncat bao<Tab>        → cat bao-cao.txt\ncd du-an/da<Tab>    → cd du-an/data/\n```\n\nBấm **Tab một lần** để hoàn thành nếu chỉ có một khả năng; **Tab hai lần** để liệt kê mọi khả năng nếu có nhiều.\n\nHai lợi ích, và lợi ích thứ hai quan trọng hơn:\n\n1. Gõ nhanh hơn nhiều\n2. **Không bao giờ gõ sai tên** — Tab chỉ hoàn thành tên thật sự tồn tại. Nếu Tab không ăn, tức là file đó không có ở đây, và bạn phát hiện ngay thay vì sau khi lệnh chạy lỗi.\n\nNgười dùng terminal thành thạo bấm Tab gần như phản xạ, ở mọi đường dẫn.\n\n### Lịch sử lệnh\n\n```bash\n↑ ↓                  # duyệt lệnh đã gõ\nCtrl+R               # ⭐ tìm ngược trong lịch sử — gõ vài ký tự là ra\nhistory              # liệt kê toàn bộ\n!!                   # lặp lại lệnh vừa rồi\nsudo !!              # ⭐ chạy lại lệnh vừa rồi với quyền root\n!123                 # chạy lại lệnh số 123 trong history\n```\n\n`sudo !!` là mẹo kinh điển: chạy một lệnh, bị *permission denied*, gõ `sudo !!` là xong — không phải gõ lại cả dòng.\n\n`Ctrl+R` đáng học nhất: bấm rồi gõ `docker` là nó tìm ra lệnh docker dài ngoằng bạn đã gõ tuần trước.",

vi_du: "### Một phiên làm việc điển hình\n\n```bash\npwd\n# /home/minh\n\nls\n# Desktop  Documents  du-an\n\ncd du-an\nls -lah\n# total 24K\n# drwxr-xr-x  4 minh dev 4.0K Aug 30 10:00 .\n# drwxr-xr-x 12 minh dev 4.0K Aug 29 09:15 ..\n# drwxr-xr-x  8 minh dev 4.0K Aug 30 09:58 .git      ← ẩn, chỉ thấy nhờ -a\n# -rw-r--r--  1 minh dev  145 Aug 30 09:20 .gitignore\n# -rw-r--r--  1 minh dev 2.1K Aug 30 10:00 main.py\n\ncd /var/log        # nhảy đi chỗ khác\npwd\n# /var/log\n\ncd -               # quay lại ngay\npwd\n# /home/minh/du-an\n```\n\n### Bẫy working directory\n\n```bash\n# Cấu trúc:\n#   du-an/\n#     ├── main.py        ← code mở \"data.csv\"\n#     └── data.csv\n\ncd du-an\npython main.py     # ✓ chạy được — đang đứng trong du-an\n\ncd ..\npython du-an/main.py\n# ✗ FileNotFoundError: data.csv\n```\n\nLần thứ hai lỗi vì đường dẫn tương đối `\"data.csv\"` được tính từ **thư mục bạn đang đứng** (`/home/minh`), không phải từ chỗ file `main.py` nằm.\n\nCách viết code không phụ thuộc chỗ đứng:\n\n```python\nfrom pathlib import Path\n\n# __file__ là đường dẫn tới chính file .py này\nTHU_MUC = Path(__file__).parent\ndu_lieu = (THU_MUC / \"data.csv\").read_text()   # luôn đúng, chạy từ đâu cũng được\n```\n\nĐây là mẫu nên dùng mặc định trong mọi script đọc file kèm theo.\n\n### Tìm file mới sửa gần đây\n\n```bash\n# Đang gỡ lỗi, muốn biết log nào vừa được ghi\ncd /var/log\nls -lt | head -5\n# -rw-r----- 1 root adm  892K Aug 30 10:31 syslog        ← mới nhất\n# -rw-r--r-- 1 root root 145K Aug 30 10:28 nginx/access.log\n```\n\n`ls -lt | head` là một trong những tổ hợp hữu ích nhất khi chẩn đoán sự cố — nó cho biết ngay file nào vừa thay đổi.\n\n### Thư mục nặng bao nhiêu\n\n```bash\nls -lh du-lieu\n# drwxr-xr-x 3 minh dev 4.0K ...   ← 4.0K là SAI hiểu, đó không phải dung lượng\n\ndu -sh du-lieu\n# 2.3G   du-lieu                    ← đây mới là con số thật\n\ndu -sh * | sort -h | tail -5       # ⭐ 5 thứ nặng nhất trong thư mục hiện tại\n```\n\nTổ hợp cuối rất đáng nhớ: khi ổ đĩa đầy, nó chỉ ngay ra thủ phạm. `sort -h` hiểu được các hậu tố K, M, G nên sắp đúng thứ tự.\n\n### Nhìn cấu trúc dự án\n\n```bash\ntree -L 2 -I 'node_modules|__pycache__|.git|venv'\n```\n\n```\n.\n├── assets\n│   ├── ai\n│   ├── curriculum\n│   └── lessons\n├── docs\n├── tools\n└── index.html\n```\n\nCờ `-I` loại bỏ các thư mục rác. Không có nó thì `node_modules` một mình đã đủ làm output dài hàng nghìn dòng.\n\n### Ngăn xếp thư mục\n\n```bash\ncd /home/minh/du-an\npushd /var/log        # nhảy sang, ĐẨY chỗ cũ vào ngăn xếp\n# /var/log /home/minh/du-an\n\npushd /etc/nginx      # nhảy tiếp, đẩy tiếp\n# /etc/nginx /var/log /home/minh/du-an\n\npopd                  # quay về /var/log\npopd                  # quay về /home/minh/du-an\n```\n\nHữu ích khi phải đi qua ba bốn thư mục rồi lần lượt quay lại. Với chỉ hai chỗ thì `cd -` đơn giản hơn.",

so_sanh: "### Terminal và giao diện đồ hoạ\n\n| | Terminal | File Explorer |\n|---|---|---|\n| Thao tác lặp lại | **Viết script được** | Phải bấm lại từng lần |\n| Làm việc từ xa | **SSH là chuẩn** | Cần cài thêm, chậm |\n| Duyệt khám phá | Kém trực quan | **Trực quan hơn** |\n| Thao tác hàng loạt | **Rất mạnh** (glob, pipe) | Hạn chế |\n| Học ban đầu | Dốc | **Dễ** |\n| Thông báo lỗi | **Đầy đủ, nguyên văn** | Thường bị nuốt |\n\nKhông có bên nào thắng tuyệt đối. Người dùng thành thạo dùng **cả hai**: File Explorer để duyệt và xem trước, terminal để thao tác và tự động hoá.\n\nNhưng có một trường hợp terminal là **bắt buộc**: làm việc trên server. Và trường hợp đó là phần lớn công việc backend.\n\n### Vài shell phổ biến\n\n| | bash | zsh | fish | PowerShell |\n|---|---|---|---|---|\n| Mặc định trên | Linux, Git Bash | macOS | — | Windows |\n| Tương thích POSIX | ✓ | ✓ | **✗** | ✗ |\n| Gợi ý khi gõ | Cần cài thêm | Cần plugin | **Sẵn có** | ✓ |\n| Chạy script của người khác | **An toàn nhất** | Hầu hết được | Hay lỗi | Khác hẳn |\n\nLời khuyên thực dụng: **dùng zsh hoặc fish cho công việc hằng ngày** (tiện hơn nhiều), nhưng **viết script bằng bash** với dòng đầu `#!/usr/bin/env bash`. Lý do: script bash chạy được ở mọi nơi, còn cú pháp fish thì không tương thích POSIX.\n\n### Trên Windows nên dùng gì\n\nBa lựa chọn, và chúng khác nhau nhiều hơn vẻ ngoài:\n\n| | Git Bash | WSL2 | PowerShell |\n|---|---|---|---|\n| Bản chất | Bash mô phỏng trên Windows | **Linux thật trong máy ảo nhẹ** | Shell riêng của Windows |\n| Lệnh Unix | Phần lớn có | **Đầy đủ** | Khác cú pháp |\n| Docker, systemd | Không | **Có** | Không |\n| Cài đặt | Kèm Git | `wsl --install` | Có sẵn |\n\nVới người học backend hoặc AI/ML, **WSL2 là lựa chọn đúng**: nó là Linux thật, nên mọi hướng dẫn trên mạng chạy được y nguyên, và môi trường giống hệt server production. Git Bash đủ cho thao tác git và script đơn giản.",

loi_thuong_gap: [
  "**Quên mình đang ở đâu.** Gõ `pwd` khi thấy lệnh không như mong đợi. Đây là bước chẩn đoán đầu tiên, luôn luôn.",
  "**Chạy script từ thư mục khác rồi ngạc nhiên vì `FileNotFoundError`.** Đường dẫn tương đối tính từ chỗ bạn đứng, không phải chỗ file `.py` nằm. Dùng `Path(__file__).parent` trong code.",
  "**Không dùng Tab.** Gõ tay dễ sai tên và chậm hơn nhiều. Tab còn là cách kiểm tra file có tồn tại không.",
  "**Quên `ls -a` khi tìm file cấu hình.** `.env`, `.gitignore`, `.github/` đều ẩn.",
  "**Tưởng cột kích thước của thư mục trong `ls -l` là dung lượng nội dung.** Đó là kích thước bản ghi thư mục. Dùng `du -sh`.",
  "**Dùng `~` trong code.** Chỉ shell bung `~`; Python và các ngôn ngữ khác nhận nguyên văn. Dùng `os.path.expanduser`.",
  "**Chạy `tree` không giới hạn độ sâu.** Trong dự án có `node_modules` là output hàng chục nghìn dòng. Luôn `tree -L 2 -I 'node_modules|.git'`.",
  "**Gõ lại lệnh dài thay vì dùng `Ctrl+R`.** Tìm trong lịch sử nhanh hơn và không gõ sai."
],

tu_kiem_tra: [
  "Vì sao `cd` bắt buộc phải là lệnh dựng sẵn của shell chứ không thể là một chương trình riêng?",
  "Bạn chạy `python du-an/main.py` từ thư mục cha và gặp `FileNotFoundError` dù file có thật. Vì sao, và sửa trong code thế nào?",
  "`ls -l` báo thư mục `du-lieu` có kích thước 4096. Điều đó nghĩa là gì, và làm sao biết dung lượng thật?",
  "Vì sao `open(\"~/data.txt\")` trong Python thất bại trong khi `cat ~/data.txt` trong terminal thì được?",
  "Nêu hai lợi ích của việc bấm Tab, và giải thích vì sao lợi ích thứ hai quan trọng hơn.",
  "`cd -` làm gì, và nó dựa vào biến nào của shell?",
  "Ổ đĩa đầy. Viết một lệnh tìm ra 5 thứ nặng nhất trong thư mục hiện tại."
],

tai_nguyen: [
  "Khoá **The Missing Semester of Your CS Education** (MIT) — bài 1 và 2 về shell. Ngắn, thực dụng, đúng những gì trường không dạy.",
  "Sách **The Linux Command Line** — William Shotts. Đọc miễn phí, phần I về điều hướng rất phù hợp cho người mới.",
  "Tài liệu Linux: `man ls`, `man cd`, `man hier` — lệnh cuối mô tả ý nghĩa của từng thư mục chuẩn trong hệ thống.",
  "Trang **explainshell.com** — dán một lệnh dài vào, nó giải thích từng cờ. Rất hữu ích khi đọc lệnh trên mạng."
],

lien_he: [
  "**Hệ thống file, đường dẫn, quyền** ở module 2: bài này áp dụng trực tiếp kiến thức về inode, symlink và quyền `rwx` đã học ở đó.",
  "**Thao tác file** (bài kế tiếp): sau khi biết đi lại thì tới việc tạo, sao chép, xoá.",
  "**Process vs thread**: working directory là thuộc tính của process — đó là lý do hai terminal có hai chỗ đứng độc lập.",
  "**Git & GitHub** ở mảng 03: mọi lệnh git đều chạy trong terminal và phụ thuộc vào việc bạn đang đứng trong repo hay không.",
  "**Triển khai** ở mảng 05: SSH vào server chỉ có dòng lệnh, không có lựa chọn nào khác.",
  "**Công cụ của dev** ở module 9: `tree`, `du`, `ls -lt` là những lệnh bạn sẽ dùng hằng ngày khi chẩn đoán."
]

};
