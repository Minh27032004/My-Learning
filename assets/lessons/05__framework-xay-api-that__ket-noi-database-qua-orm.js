window.LESSONS["05:framework-xay-api-that:ket-noi-database-qua-orm"] = {

tom_tat: [
  "ORM ánh xạ object và quan hệ sang SQL, nhưng database vẫn thực thi query, transaction, constraint và locking; abstraction không xóa chi phí bên dưới.",
  "Một session thường đại diện unit of work: theo dõi entity, flush SQL, commit atomically hoặc rollback; vòng đời phải hữu hạn theo use case/request.",
  "Tạo đơn đúng cần transaction bao gồm order, order line và cập nhật tồn kho, đồng thời dùng constraint hay update có điều kiện để chống hai request bán cùng món cuối.",
  "Lazy loading tiện nhưng gây N+1 và I/O ẩn; eager loading cũng có thể nhân bản hàng dữ liệu, nên loading strategy phải theo access pattern.",
  "Bảo mật không dừng ở parameterized query: tài khoản database tối thiểu quyền, migration được kiểm soát, lỗi SQL không lộ và tenant filter không được bỏ quên."
],

can_biet_truoc: [
  "SQL cơ bản: SELECT, INSERT, UPDATE, JOIN, primary key, foreign key và index.",
  "Khái niệm transaction, commit, rollback và tính nguyên tử.",
  "Python class, context manager và `async`/`await` nếu dùng SQLAlchemy async.",
  "FastAPI dependency theo request và Pydantic model đầu vào/response."
],

dinh_nghia: "**ORM (Object–Relational Mapper)** là lớp ánh xạ bảng, cột và quan hệ của relational database sang class, thuộc tính và reference trong code; nó sinh SQL từ biểu thức và chuyển row kết quả thành object. **Session** là vùng làm việc theo dõi các object trong một unit of work, phối hợp flush và transaction trên connection.\n\nORM không phải database và không phải repository tự động đúng. Nó giảm code lặp, giữ parameter binding và mapping nhất quán, nhưng developer vẫn phải hiểu SQL được sinh, cardinality quan hệ, index, isolation và thời điểm I/O xảy ra.",

vi_sao_can: "API đơn hàng ghi ít nhất hai loại record: `orders` và `order_lines`; đồng thời phải giảm tồn kho. Nếu insert order thành công nhưng process chết trước khi insert line, dữ liệu nửa vời không còn ý nghĩa. Nếu hai request cùng thấy tồn kho bằng 1 rồi cùng trừ, cửa hàng bán quá số có thật. Dictionary trong RAM không giải quyết persistence, nhiều worker hay restart.\n\nViết SQL tay cho mọi endpoint cho quyền kiểm soát cao nhưng mapping, transaction boilerplate và thay đổi schema nhanh chóng lặp. ORM giúp biểu diễn quan hệ và gom unit of work. Cái giá là I/O có thể bị che sau truy cập thuộc tính, query sinh ra không tối ưu, session bị giữ quá lâu và developer tưởng `commit()` chỉ “lưu object” thay vì một ranh giới đồng thời quan trọng.\n\nAPI thật còn đối mặt pool cạn, deadlock, migration lệch phiên bản và thông tin nhạy cảm trong log SQL. Bài này dùng SQLAlchemy theo phong cách hiện đại và SQLite để ví dụ chạy gọn; khi triển khai PostgreSQL/MySQL, hành vi concurrency, kiểu dữ liệu và locking phải được kiểm thử trên đúng engine production.",

co_che: "### Mapping không làm relational model biến mất\n\nClass `Order` có collection `lines`, nhưng database lưu foreign key `order_lines.order_id`. Khi đọc collection, ORM phải phát SQL JOIN hoặc SELECT khác. Object graph khiến code dễ đọc; relational model quyết định số row, constraint và kế hoạch thực thi. Nếu không biết query thật, developer dễ coi truy cập thuộc tính là O(1) trong khi nó là network round-trip.\n\nTên bảng/cột, nullability và foreign key là contract dữ liệu. Type hint Python không thay `NOT NULL`; validator API không bảo vệ script hay service khác ghi trực tiếp. Invariant quan trọng nên có lớp phòng thủ database khi diễn đạt được.\n\n### Engine, connection và session là ba thứ khác nhau\n\nEngine giữ cấu hình dialect và connection pool. Connection là một phiên kết nối vật lý/logical tới database. Session là unit of work cấp ORM, mượn connection khi cần và có identity map. Tạo engine mỗi request phá pooling; giữ một session global tạo race và state cũ giữa người dùng.\n\nThông thường engine/session factory sống theo process, còn session sống theo request hoặc use case. “Một session mỗi request” là default tiện, không phải luật tuyệt đối: streaming dài hay background job cần ranh giới riêng.\n\n### Identity map và trạng thái entity\n\nTrong một session, tải cùng primary key thường trả cùng object Python. Identity map đảm bảo thay đổi nhất quán trong unit of work và tránh tạo hai đại diện xung đột. Nó không phải cache toàn hệ thống: session khác hoặc process khác không thấy object ấy, và dữ liệu có thể stale.\n\nEntity đi qua trạng thái transient, pending, persistent, detached. Truy cập lazy relationship trên detached object có thể lỗi vì session đã đóng. Response serialization không nên bất ngờ kích hoạt query sau cleanup; hãy tải đủ dữ liệu và map sang response DTO trước khi rời use case.\n\n### Flush khác commit\n\n`session.add(order)` chỉ đăng ký thay đổi. Flush phát SQL trong transaction, thường tự chạy trước query hoặc commit. Flush có thể cấp ID từ database và phát hiện constraint sớm, nhưng transaction chưa bền vững với bên ngoài. Commit kết thúc transaction thành công; rollback bỏ thay đổi chưa commit.\n\nĐừng trả 201 trước khi commit nếu contract hứa đơn đã được nhận bền vững. Ngược lại, commit giữa chừng order rồi commit line sau phá tính nguyên tử. Một use case nên sở hữu quyết định commit.\n\n### Transaction tạo đơn\n\nLuồng an toàn: bắt đầu transaction; kiểm/giảm tồn kho bằng thao tác chống cạnh tranh; insert order; insert lines; commit. Bất kỳ lỗi nào rollback toàn bộ. Gọi cổng thanh toán bên trong transaction database lâu dài lại nguy hiểm: network chậm giữ lock và connection. Thường cần outbox/saga hoặc tách trạng thái `pending_payment`.\n\nTransaction chỉ nguyên tử trong một database. Email, message broker và payment API không tự rollback theo SQL. Outbox ghi event cùng transaction rồi worker gửi sau giúp tránh “đã tạo đơn nhưng mất event”.\n\n### Chống overselling bằng thao tác nguyên tử\n\nCách ngây thơ `SELECT stock`, kiểm trong Python, rồi `UPDATE` có race. Hai transaction cùng đọc 1 trước khi transaction kia ghi. Một cách là update có điều kiện:\n\n```sql\nUPDATE products\nSET stock = stock - :quantity\nWHERE id = :id AND stock >= :quantity;\n```\n\nNếu affected rows bằng 0, sản phẩm không tồn tại hoặc không đủ tồn. Thao tác này để database phân xử atomically. Một lựa chọn khác là khóa row `SELECT ... FOR UPDATE` trên engine hỗ trợ, nhưng phải khóa theo thứ tự ổn định để giảm deadlock. Constraint `CHECK (stock >= 0)` là hàng rào cuối, song chỉ constraint không cho thông báo nghiệp vụ đẹp.\n\n### Isolation và retry có giới hạn\n\nIsolation level quyết định transaction nhìn thấy thay đổi cạnh tranh nào. Mức mạnh hơn giảm anomaly nhưng có thể tăng lock, abort và latency. Không chọn theo khẩu hiệu; xác định invariant rồi test lịch xen kẽ.\n\nDeadlock là hệ quả có thể xảy ra của locking, không nhất thiết là bug database. Database hủy một transaction; ứng dụng có thể retry toàn unit of work với backoff. Chỉ retry khi thao tác idempotent hoặc chưa phát side effect ngoài transaction. Retry vô hạn tạo bão tải.\n\n### N+1: vòng lặp Python, nhiều chuyến mạng\n\nTải 100 order bằng một SELECT rồi mỗi lần truy cập `order.lines` phát thêm SELECT tạo 101 query. Big-O của code nhìn như O(n), nhưng chi phí n round-trip thống trị. SQL logging hoặc query counter trong test phát hiện rõ.\n\n`selectinload` thường dùng một query cho parent và một query `WHERE order_id IN (...)` cho children. `joinedload` dùng JOIN một query, nhưng order có nhiều lines làm lặp cột parent và có thể tạo tích Descartes nếu eager nhiều collection. Không có eager strategy thắng mọi trường hợp.\n\n### Projection tốt hơn tải cả entity cho màn hình đọc\n\nEndpoint summary chỉ cần ID, status và total không nhất thiết tạo full entity, relationship và tracking. SELECT đúng cột hoặc DTO projection giảm byte, memory và nguy cơ lazy load. Entity hữu ích khi thực hiện behavior và thay đổi state; read model có thể tối ưu theo query.\n\nRepository không nên trả một “queryable” sống ra ngoài rồi để route tùy ý nối filter. Điều đó làm transaction/I/O rò qua boundary và test khó biết query nào chạy.\n\n### Connection pool là tài nguyên hữu hạn\n\nPool tái sử dụng connection đắt đỏ. Nếu mỗi request giữ connection trong lúc gọi payment API, chỉ vài request chậm có thể làm request khác timeout chờ pool. Dependency phải đóng session trong `finally`; transaction cần ngắn.\n\nPool size không nên tăng mù quáng. Database có giới hạn connection và mỗi connection tốn memory. Nhiều app replica nhân pool size lên; tổng mới là con số database thấy. Metric cần có thời gian checkout, số connection đang dùng và timeout.\n\n### Async ORM chỉ có ý nghĩa với driver async\n\nSQLAlchemy async dùng `AsyncSession` và driver bất đồng bộ. Nó cho event loop làm request khác khi chờ socket, không làm query SQL chạy nhanh hơn. Không dùng cùng một `AsyncSession` đồng thời cho nhiều task; session mang transaction state mutable.\n\nLazy loading ngầm trong async đặc biệt khó vì truy cập thuộc tính không có chỗ `await`. Tải quan hệ chủ động hoặc projection làm I/O hiển thị. Nếu stack hiện tại đồng bộ và tải vừa, sync ORM trong thread worker có thể đơn giản, đáng tin hơn migration async nửa vời.\n\n### Parameter binding và giới hạn bảo mật\n\nBiểu thức ORM bind value thành parameter, giảm SQL injection so với nối chuỗi. Nhưng tên cột/order-by động không phải value parameter; phải chọn từ allowlist, không ghép raw input. Raw SQL vẫn hợp lý cho query đặc biệt nếu bind parameter đúng.\n\nTài khoản runtime chỉ cần quyền SELECT/INSERT/UPDATE cần thiết, không nên là superuser hay có quyền migration. Secret database đến từ secret manager/biến môi trường, không hard-code và không hiện trong exception/log. Multi-tenant cần tenant predicate bắt buộc hoặc row-level security; một filter bị quên có thể thành data breach.\n\n### Migration là một phần deployment\n\nThay class ORM không tự sửa database production một cách an toàn. Migration có version tạo schema chuyển tiếp. Deploy zero-downtime thường theo expand–migrate–contract: thêm cấu trúc tương thích, deploy code đọc/ghi hai dạng nếu cần, backfill, rồi mới xóa cũ.\n\nMigration khóa bảng lớn có thể gây downtime dù code đúng. Hãy test trên dữ liệu đại diện, backup/restore và quan sát lock. Không chạy `create_all` như chiến lược migration production.\n\n### Khi nào không nên dùng ORM\n\nQuery phân tích phức tạp, bulk load cực lớn, tận dụng tính năng riêng engine hoặc service rất nhỏ với vài câu SQL có thể rõ hơn bằng SQL/Core. Không nên ép mọi query qua repository CRUD chung khiến SQL tệ. ORM đáng dùng khi mapping và unit of work đem lại giá trị; quyền xem SQL thật phải luôn còn.",

vi_du: "### Schema và transaction tạo đơn bằng SQLite\n\nVí dụ chạy độc lập sau khi cài SQLAlchemy. SQLite phù hợp minh họa mapping/rollback, không đại diện đầy đủ row locking production.\n\n```python\nfrom sqlalchemy import CheckConstraint, ForeignKey, String, create_engine, select, update\nfrom sqlalchemy.orm import DeclarativeBase, Mapped, Session, mapped_column, relationship\n\nclass Base(DeclarativeBase):\n    pass\n\nclass Product(Base):\n    __tablename__ = \"products\"\n    __table_args__ = (CheckConstraint(\"stock >= 0\", name=\"ck_stock_nonnegative\"),)\n    id: Mapped[int] = mapped_column(primary_key=True)\n    sku: Mapped[str] = mapped_column(String(32), unique=True)\n    stock: Mapped[int]\n\nclass Order(Base):\n    __tablename__ = \"orders\"\n    id: Mapped[int] = mapped_column(primary_key=True)\n    customer_id: Mapped[str] = mapped_column(String(64), index=True)\n    status: Mapped[str] = mapped_column(String(20), default=\"pending\")\n    lines: Mapped[list[\"OrderLine\"]] = relationship(back_populates=\"order\", cascade=\"all, delete-orphan\")\n\nclass OrderLine(Base):\n    __tablename__ = \"order_lines\"\n    id: Mapped[int] = mapped_column(primary_key=True)\n    order_id: Mapped[int] = mapped_column(ForeignKey(\"orders.id\"))\n    product_id: Mapped[int] = mapped_column(ForeignKey(\"products.id\"))\n    quantity: Mapped[int] = mapped_column(CheckConstraint(\"quantity > 0\"))\n    order: Mapped[Order] = relationship(back_populates=\"lines\")\n\nengine = create_engine(\"sqlite:///shop.db\")\nBase.metadata.create_all(engine)  # chỉ dành cho demo/test\n\ndef place_order(customer_id: str, product_id: int, quantity: int) -> int:\n    with Session(engine) as session, session.begin():\n        result = session.execute(\n            update(Product)\n            .where(Product.id == product_id, Product.stock >= quantity)\n            .values(stock=Product.stock - quantity)\n        )\n        if result.rowcount != 1:\n            raise ValueError(\"không đủ tồn kho hoặc sản phẩm không tồn tại\")\n        order = Order(customer_id=customer_id)\n        order.lines.append(OrderLine(product_id=product_id, quantity=quantity))\n        session.add(order)\n        session.flush()  # lấy ID nhưng vẫn trong transaction\n        order_id = order.id\n    return order_id  # context commit trước khi ra đây\n\nwith Session(engine) as session, session.begin():\n    if session.scalar(select(Product).where(Product.id == 1)) is None:\n        session.add(Product(id=1, sku=\"BOOK-01\", stock=2))\n\nprint(place_order(\"an\", 1, 1))\n```\n\nChạy lần đầu tạo file database và in ID. Gọi thêm với quantity vượt stock sẽ ném lỗi; context manager rollback nên không có order rỗng. Trong API thật, map lỗi thiếu tồn thành domain exception/409, không trả `ValueError` hay SQL cho client.\n\n### Dependency session cho FastAPI\n\n```python\nfrom collections.abc import Generator\nfrom fastapi import Depends\nfrom typing import Annotated\n\ndef get_session() -> Generator[Session, None, None]:\n    with Session(engine) as session:\n        yield session\n\nDbSession = Annotated[Session, Depends(get_session)]\n```\n\nDependency chịu trách nhiệm đóng session. Service/use case nên điều khiển `begin` và commit để test failure giữa các bước. Nếu chọn quy ước commit trong dependency, phải tài liệu hóa rõ và cẩn thận exception sau endpoint nhưng trước response.\n\n### Test rollback thay vì chỉ test happy path\n\nTạo product stock 1, gọi quantity 2, bắt lỗi rồi query số order và stock. Kỳ vọng số order không tăng, stock vẫn 1. Test này chứng minh transaction boundary; chỉ assert status 409 chưa chứng minh database không bị thay đổi nửa chừng.",

so_sanh: "### ORM, SQLAlchemy Core và SQL viết tay\n\n| Cách | Mạnh ở đâu | Rủi ro/chi phí |\n|---|---|---|\n| ORM entity | quan hệ, unit of work, business object | lazy I/O, N+1, tracking memory |\n| SQLAlchemy Core | query tường minh, bind parameter, composable | tự mapping nhiều hơn |\n| SQL viết tay | toàn quyền SQL/engine feature | boilerplate, portability và mapping |\n\nMột codebase có thể dùng cả ba. Command thay đổi aggregate dùng ORM; reporting projection dùng Core; query tối ưu đặc thù dùng SQL bind parameter. Tính nhất quán không có nghĩa ép mọi vấn đề qua một abstraction.\n\n### Loading strategy theo hình dạng kết quả\n\n| Chiến lược | Số query điển hình | Đánh đổi |\n|---|---:|---|\n| lazy cho từng parent | 1 + n | đơn giản nhưng N+1 qua mạng |\n| `selectinload` | thường 2 | câu IN lớn, phù hợp collection vừa |\n| `joinedload` | thường 1 | lặp cột parent, bùng row với nhiều collection |\n| projection | 1 | tối ưu đọc, không có full entity behavior |\n\nKhông chọn bằng số query một mình. Một JOIN trả triệu row có thể tệ hơn hai query nhỏ; đo row, byte, latency và query plan trên dữ liệu đại diện.",

loi_thuong_gap: [
  "Tạo engine hoặc pool mỗi request; log cho thấy liên tục mở connection mới và database chạm giới hạn dù traffic thấp.",
  "Dùng session global hoặc chia một `AsyncSession` cho task đồng thời; lỗi transaction state ngẫu nhiên và dữ liệu identity map lẫn request.",
  "Commit sau từng `add`; khi bước giữa lỗi, database còn order không line hoặc tồn kho đã trừ.",
  "Đọc tồn kho rồi trừ trong Python không locking/update điều kiện; test hai request đồng thời bán được cùng món cuối.",
  "Serialize relationship lazy sau khi session đóng; response ném detached-instance error hoặc bất ngờ phát query lúc render.",
  "N+1 query; bật SQL log thấy một SELECT orders theo sau bởi một SELECT lines cho từng order.",
  "Ghép input vào raw SQL hoặc ORDER BY; ký tự đặc biệt làm đổi câu query, trong khi value đáng lẽ phải bind và tên cột phải allowlist.",
  "Dùng `create_all` thay migration production; schema môi trường lệch nhau và không có lịch sử nâng cấp/rollback được kiểm soát."
],

tu_kiem_tra: [
  "Phân biệt engine, connection và session; vòng đời hợp lý của từng thứ trong FastAPI là gì?",
  "Vì sao `flush()` có thể cấp `order.id` nhưng chưa cho phép khẳng định transaction đã bền vững?",
  "Hai request cùng đọc stock bằng 1. Viết điều kiện UPDATE giúp chỉ một request thắng.",
  "Một endpoint tải 100 orders rồi đọc `lines` từng order. Làm sao chứng minh có N+1 và hai cách sửa khác nhau?",
  "Tại sao gọi payment API trong lúc giữ transaction database là nguy hiểm, và outbox giải quyết phần nào?",
  "Câu khó: tăng pool mỗi replica từ 10 lên 50 khi có 20 replica tác động database ra sao? Cần đo những metric nào trước quyết định?",
  "ORM parameter binding có loại bỏ mọi SQL injection không? Nêu trường hợp ORDER BY động còn cần allowlist."
],

tai_nguyen: [
  "SQLAlchemy Documentation — ORM Quick Start, Session Basics và Relationship Loading Techniques.",
  "Architecture Patterns with Python — Harry Percival và Bob Gregory.",
  "Designing Data-Intensive Applications — Martin Kleppmann.",
  "Database Internals — Alex Petrov."
],

lien_he: [
  "Nối sang mảng Cơ sở dữ liệu: index, isolation, execution plan và constraint giải thích hiệu năng/tính đúng mà ORM chỉ biểu diễn.",
  "Nối sang mảng Hệ phân tán: outbox, idempotency và saga cần thiết khi transaction đi qua payment hoặc message broker.",
  "Nối sang mảng Bảo mật: least privilege, secret rotation, tenant isolation và SQL injection tạo nhiều lớp bảo vệ quanh persistence.",
  "Nối sang mảng Testing: integration test phải dùng đúng database engine production cho locking, migration và concurrency; SQLite không đủ chứng minh các hành vi đó."
]

};
