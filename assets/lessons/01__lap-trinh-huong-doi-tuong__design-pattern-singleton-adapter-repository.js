window.LESSONS["01:lap-trinh-huong-doi-tuong:design-pattern-singleton-adapter-repository"] = {

tom_tat: [
  "**Trong Python, Singleton gần như luôn là câu trả lời sai** — module đã là singleton sẵn, và trạng thái toàn cục làm kiểm thử khổ sở. Biết nó chủ yếu để nhận ra và tránh.",
  "**Adapter dịch giao diện của bên ngoài sang giao diện của bạn** — nó là cách giữ tầng nghiệp vụ không bị nhiễm hình dạng của thư viện bên thứ ba.",
  "**Repository giấu hoàn toàn cách lưu trữ**, cho tầng nghiệp vụ làm việc với object thay vì với SQL. Đây là mẫu quan trọng nhất trong ba mẫu của bài.",
  "**Adapter và Repository đều là Dependency Inversion được đặt tên** — trừu tượng thuộc tầng nghiệp vụ, bản cài đặt thuộc tầng hạ tầng.",
  "**Bản cài đặt trong bộ nhớ của Repository là món quà lớn nhất**: nó biến kiểm thử từ hàng chục giây xuống mili giây, và kiểm được cả những điều database thật khó kiểm."
],

can_biet_truoc: [
  "Bài **Design pattern — Factory, Strategy, Observer** ngay trước — cùng cách tiếp cận: nhận ra vấn đề rồi mới gọi tên mẫu.",
  "Bài **SOLID — Dependency Inversion** — Adapter và Repository là DIP ở dạng cụ thể.",
  "Bài **SOLID — Liskov** — bộ kiểm thử giao ước là công cụ bắt buộc khi có nhiều bản cài đặt Repository.",
  "Bài **Module, package, import** ở module 5 — module chỉ chạy một lần, và đó là lý do Python không cần Singleton."
],

dinh_nghia: "Nói đời thường: **Adapter là cái đầu chuyển ổ cắm** khi bạn mang thiết bị ra nước ngoài — bạn không sửa thiết bị, không sửa ổ cắm, chỉ thêm một miếng ở giữa. **Repository là người thủ thư**: bạn nói \"cho tôi cuốn sách mã X\", không cần biết sách nằm kệ nào, tầng nào.\n\nNói chính xác:\n\n- **Singleton** — mẫu đảm bảo một class chỉ có **đúng một instance** trong toàn chương trình.\n- **Adapter** — mẫu **dịch** giao diện của một class sang giao diện mà code của bạn mong đợi.\n- **Repository** — mẫu cung cấp giao diện **giống một collection object** để truy cập dữ liệu, giấu hoàn toàn cách lưu trữ.\n- **Đối tượng nghiệp vụ** (*domain object*) — class biểu diễn khái niệm nghiệp vụ, **không biết gì** về database.\n- **Ánh xạ** (*mapping*) — chuyển đổi giữa dòng dữ liệu và đối tượng nghiệp vụ.\n\nĐiểm cần nói thẳng ngay: **Singleton nằm trong bài này chủ yếu để bạn biết mà tránh.** Nó là mẫu bị phê phán nhiều nhất trong sách gốc, và trong Python thì gần như luôn có cách tốt hơn. Hai mẫu còn lại thì ngược lại — chúng là công cụ bạn sẽ dùng thật.",

vi_sao_can: "Ba mẫu này gắn với việc code của bạn giao tiếp với thế giới bên ngoài:\n\n- **Repository quyết định tầng nghiệp vụ có kiểm thử được không.** Không có nó, mọi bài kiểm thử cần database; có nó, bản trong bộ nhớ chạy trong mili giây.\n- **Adapter giữ code của bạn không bị nhiễm thư viện ngoài.** Đổi nhà cung cấp thanh toán, đổi thư viện HTTP, đổi dịch vụ lưu trữ — nếu chúng nằm sau adapter thì chỉ sửa một file.\n- **Singleton là bẫy phổ biến.** Người mới thấy nó trong sách rồi dùng cho cấu hình, cho kết nối database — và sau đó không kiểm thử được gì vì trạng thái dính giữa các bài kiểm thử.\n- **Chúng là DIP ở dạng nhận ra được.** Khi ai đó nói \"tầng này cần một repository\", cả nhóm hiểu ngay chiều phụ thuộc phải như thế nào.\n- **Định hướng Backend và AI/ML.** Repository cho dữ liệu, adapter cho API bên ngoài và cho các thư viện mô hình — hai mẫu này xuất hiện trong mọi hệ thống thật.\n\nTình huống thật rất kinh điển: nhóm bạn dùng một thư viện gửi tin nhắn, gọi thẳng API của nó ở hai mươi chỗ trong code. Nhà cung cấp đổi API ở bản mới, hoặc công ty đổi sang nhà cung cấp khác — và bạn phải sửa hai mươi chỗ, mỗi chỗ một kiểu. Với một adapter, con số đó là **một file**. Phần cơ chế sẽ chỉ cách dựng adapter đúng và cách nó nối với Dependency Inversion.",

co_che: "### Singleton — và vì sao Python không cần\n\nSách gốc mô tả Singleton như sau:\n\n```python\n# Cách sách gốc — chống lại ngôn ngữ\nclass CauHinh:\n    _instance = None\n\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n\n    def __init__(self):\n        if hasattr(self, \"_da_khoi_tao\"):     # ⚠️ __init__ chạy MỖI lần gọi\n            return\n        self._da_khoi_tao = True\n        self.debug = False\n\na, b = CauHinh(), CauHinh()\na is b       # True\n```\n\nĐoạn này có một cạm bẫy ngay trong nó: **`__init__` vẫn chạy mỗi lần gọi `CauHinh()`**, kể cả khi `__new__` trả về instance cũ. Không có dòng `_da_khoi_tao` thì mọi lần gọi sẽ đặt lại `debug = False`, xoá mất giá trị người dùng vừa đặt.\n\nTrong Python, **module đã là singleton**:\n\n```python\n# cau_hinh.py\nDEBUG = False\n_cache: dict = {}\n\ndef dat_debug(gt: bool) -> None:\n    global DEBUG\n    DEBUG = gt\n```\n\n```python\n# file1.py\nimport cau_hinh\ncau_hinh.dat_debug(True)\n\n# file2.py\nimport cau_hinh\nprint(cau_hinh.DEBUG)      # True — cùng một object\n```\n\nLý do: module chỉ được thực thi **một lần** và cất vào `sys.modules` — đúng cơ chế ở bài import. Không cần class, không cần `__new__`, không cần khoá.\n\nBa cách khác, tuỳ nhu cầu:\n\n```python\n# ① Module — ⭐ mặc định đúng\nimport cau_hinh\n\n# ② Một instance tạo ở tầng module\nclass CauHinh:\n    def __init__(self, debug=False): self.debug = debug\n\ncau_hinh = CauHinh()          # một instance dùng chung, nhưng TẠO THÊM được\n\n# ③ Hàm có cache — khởi tạo lười\nfrom functools import lru_cache\n\n@lru_cache(maxsize=1)\ndef lay_ket_noi():\n    return tao_ket_noi_that()     # chỉ tạo ở lần gọi đầu\n```\n\nCách ③ đáng chú ý: `lru_cache(maxsize=1)` cho **khởi tạo lười** — kết nối chỉ được tạo khi thật sự cần, và chỉ một lần. Đây là mẫu thay thế Singleton gọn nhất trong Python.\n\n### Vì sao Singleton là vấn đề\n\nNói rõ ba lý do, vì đây là điểm chính của phần này:\n\n**① Trạng thái toàn cục ẩn — kiểm thử dính vào nhau:**\n\n```python\ndef test_a():\n    CauHinh().debug = True\n    ...\n\ndef test_b():\n    assert CauHinh().debug is False    # ⚠️ THẤT BẠI nếu test_a chạy trước\n```\n\nHai bài kiểm thử ảnh hưởng lẫn nhau qua một object mà không bài nào nhắc tới. Tệ hơn: kết quả phụ thuộc **thứ tự chạy**, nên lỗi lúc có lúc không.\n\n**② Phụ thuộc bị giấu:**\n\n```python\nclass DichVu:\n    def xu_ly(self):\n        if CauHinh().debug:        # ⚠️ phụ thuộc KHÔNG xuất hiện ở chữ ký\n            ...\n```\n\nĐọc `DichVu.__init__` không thấy nó cần cấu hình. Đây đúng là thứ Dependency Inversion cố loại bỏ.\n\n**③ Không thay thế được khi kiểm thử.** Không có chỗ nào để tiêm bản giả vào.\n\n```python\n# ✓ Tiêm phụ thuộc — phụ thuộc hiện rõ, thay được\nclass DichVu:\n    def __init__(self, cau_hinh: CauHinh):\n        self.cf = cau_hinh\n\ndv = DichVu(CauHinh(debug=True))       # kiểm thử dễ dàng\n```\n\nKhi nào một instance dùng chung là chấp nhận được:\n\n```\n✓ Hằng số cấu hình chỉ đọc\n✓ Pool kết nối (nhưng tiêm vào, đừng gọi toàn cục)\n✓ Logger (thư viện chuẩn đã làm sẵn)\n✗ Trạng thái thay đổi được mà nhiều nơi ghi vào\n```\n\n### Adapter — dịch giao diện\n\nVấn đề: thư viện bên ngoài có giao diện khác với thứ code bạn mong đợi.\n\n```python\n# Thư viện bên ngoài — bạn không sửa được\nclass ThuVienSMS:\n    def send_message(self, recipient: str, body: str,\n                     sender_id: str) -> dict:\n        return {\"status\": \"ok\", \"message_id\": \"abc\"}\n\nclass ThuVienEmail:\n    def deliver(self, to: list[str], subject: str, html: str) -> bool:\n        return True\n```\n\nHai thư viện, hai giao diện hoàn toàn khác nhau. Gọi thẳng thì code nghiệp vụ đầy `if`:\n\n```python\n# ❌ Nghiệp vụ biết chi tiết của cả hai\ndef thong_bao(kieu, den, tin):\n    if kieu == \"sms\":\n        ThuVienSMS().send_message(den, tin, sender_id=\"APP\")\n    elif kieu == \"email\":\n        ThuVienEmail().deliver([den], \"Thông báo\", f\"<p>{tin}</p>\")\n```\n\n```python\n# ✓ Adapter — giao diện CỦA BẠN ở tầng nghiệp vụ\nfrom typing import Protocol\n\nclass BoThongBao(Protocol):                    # ⭐ thuộc tầng NGHIỆP VỤ\n    def gui(self, den: str, tin: str) -> bool: ...\n\n\nclass AdapterSMS:                              # ⭐ thuộc tầng HẠ TẦNG\n    def __init__(self, tv: ThuVienSMS, ma_gui: str = \"APP\"):\n        self._tv, self._ma_gui = tv, ma_gui\n\n    def gui(self, den: str, tin: str) -> bool:\n        kq = self._tv.send_message(den, tin, sender_id=self._ma_gui)\n        return kq.get(\"status\") == \"ok\"        # ⭐ dịch kiểu trả về\n\n\nclass AdapterEmail:\n    def __init__(self, tv: ThuVienEmail, tieu_de: str = \"Thông báo\"):\n        self._tv, self._tieu_de = tv, tieu_de\n\n    def gui(self, den: str, tin: str) -> bool:\n        import html\n        an_toan = html.escape(tin)              # ⭐ adapter lo chi tiết định dạng\n        return self._tv.deliver([den], self._tieu_de, f\"<p>{an_toan}</p>\")\n\n\ndef thong_bao(bo: BoThongBao, den: str, tin: str) -> None:\n    if not bo.gui(den, tin):\n        print(\"gửi thất bại\")\n```\n\nBốn việc adapter làm, và ba việc sau hay bị bỏ qua:\n\n```\n① Đổi TÊN phương thức       send_message → gui\n② Đổi HÌNH DẠNG tham số     (to, subject, html) → (den, tin)\n③ Dịch KIỂU TRẢ VỀ          {\"status\": \"ok\"} → True\n④ Dịch NGOẠI LỆ             lỗi của thư viện → ngoại lệ của bạn\n```\n\nViệc ④ quan trọng và hay bị quên:\n\n```python\nclass ThongBaoLoi(Exception): ...\n\nclass AdapterSMS:\n    def gui(self, den, tin):\n        try:\n            kq = self._tv.send_message(den, tin, sender_id=self._ma_gui)\n        except SMSLibraryError as e:                  # ngoại lệ của thư viện\n            raise ThongBaoLoi(f\"gửi SMS tới {den} thất bại\") from e   # ⭐ dịch\n        return kq.get(\"status\") == \"ok\"\n```\n\nKhông dịch ngoại lệ thì tầng nghiệp vụ vẫn phải `import` và bắt ngoại lệ của thư viện — tức là nó **vẫn biết** tới thư viện đó, và adapter chưa hoàn thành nhiệm vụ.\n\n### Adapter và ranh giới hệ thống\n\n```\nNGHIỆP VỤ    định nghĩa BoThongBao (Protocol)\n                ▲\n                │ cài đặt\nHẠ TẦNG      AdapterSMS ──dùng──► ThuVienSMS (bên ngoài)\n```\n\nMũi tên từ hạ tầng đi **ngược lên** nghiệp vụ — đúng Dependency Inversion. Adapter là **nơi duy nhất** trong hệ thống biết tới `ThuVienSMS`.\n\nPhép kiểm: `grep -rn \"ThuVienSMS\" .` chỉ nên ra **hai** kết quả — file adapter và thành phần gốc.\n\n### Adapter cho dữ liệu, không chỉ cho hành vi\n\n```python\n# API bên ngoài trả về hình dạng của họ\n{\"user_id\": 1, \"full_name\": \"Minh\", \"email_address\": \"m@x.com\",\n \"created_at\": \"2026-01-15T10:00:00Z\"}\n\n# Đối tượng nghiệp vụ của bạn\n@dataclass(frozen=True)\nclass NguoiDung:\n    id: int\n    ten: str\n    email: str\n    tao_luc: datetime\n\n\nclass AdapterAPI:\n    def _sang_nguoi_dung(self, d: dict) -> NguoiDung:     # ⭐ ánh xạ\n        return NguoiDung(\n            id=d[\"user_id\"],\n            ten=d[\"full_name\"],\n            email=d[\"email_address\"],\n            tao_luc=datetime.fromisoformat(d[\"created_at\"].replace(\"Z\", \"+00:00\")),\n        )\n```\n\nNhờ lớp ánh xạ này, API đổi tên trường thì **chỉ sửa adapter** — không dòng nào của nghiệp vụ đụng tới. Không có nó, `d[\"full_name\"]` sẽ rải rác khắp code.\n\n### Repository — giấu cách lưu trữ\n\nVấn đề: tầng nghiệp vụ đầy SQL, và mọi thay đổi về cách lưu đều lan vào logic.\n\n```python\n# ❌ SQL trong nghiệp vụ\nclass DichVuDonHang:\n    def don_can_duyet(self, nguong: float) -> list[dict]:\n        cur = self.conn.cursor()\n        cur.execute(\"SELECT id, tong, email FROM don \"\n                    \"WHERE tong > %s AND trang_thai = 'cho' \"\n                    \"ORDER BY tao_luc\", (nguong,))\n        return [{\"ma\": r[0], \"tong\": r[1], \"email\": r[2]} for r in cur]\n```\n\n```python\n# ✓ Repository\nfrom typing import Protocol\nfrom dataclasses import dataclass\nfrom datetime import datetime\n\n@dataclass\nclass DonHang:\n    ma: int | None\n    tong: float\n    email: str\n    trang_thai: str = \"cho\"\n    tao_luc: datetime | None = None\n\n    def can_duyet(self, nguong: float) -> bool:      # ⭐ logic ở đối tượng\n        return self.tong > nguong and self.trang_thai == \"cho\"\n\n\nclass KhoDonHang(Protocol):                          # ⭐ tầng nghiệp vụ\n    def them(self, don: DonHang) -> int: ...\n    def lay(self, ma: int) -> DonHang | None: ...\n    def liet_ke_cho_duyet(self) -> list[DonHang]: ...\n    def cap_nhat(self, don: DonHang) -> None: ...\n\n\nclass DichVuDonHang:\n    def __init__(self, kho: KhoDonHang):\n        self.kho = kho\n\n    def don_can_duyet(self, nguong: float) -> list[DonHang]:\n        return [d for d in self.kho.liet_ke_cho_duyet()\n                if d.can_duyet(nguong)]              # ⭐ không SQL\n```\n\nBa điều Repository mang lại:\n\n```\n① Nghiệp vụ làm việc với OBJECT, không với dòng dữ liệu\n② Đổi cách lưu trữ ⇒ viết bản cài đặt mới, không sửa nghiệp vụ\n③ Bản trong BỘ NHỚ cho kiểm thử — không cần database\n```\n\n### Repository không phải DAO\n\nMột phân biệt đáng biết:\n\n```\nDAO         phản chiếu BẢNG — mỗi bảng một DAO, phương thức theo SQL\n            insert(), update(), select_by_id()\n\nRepository  phản chiếu NGHIỆP VỤ — phương thức theo NHU CẦU\n            liet_ke_cho_duyet(), tim_khach_hang_vip()\n```\n\nĐây là khác biệt về **hướng thiết kế**, và nó quan trọng: một Repository tốt có phương thức mang tên nghiệp vụ, không mang tên thao tác database.\n\n```python\n# ❌ Repository nghĩ theo database\nclass KhoDon(Protocol):\n    def select_where(self, dieu_kien: str, tham_so: tuple): ...   # ⚠️ SQL rò rỉ\n\n# ✓ Repository nghĩ theo nghiệp vụ\nclass KhoDon(Protocol):\n    def liet_ke_cho_duyet(self) -> list[DonHang]: ...\n    def tim_theo_khach(self, email: str) -> list[DonHang]: ...\n```\n\nBản đầu để `dieu_kien: str` rò rỉ SQL ra tầng nghiệp vụ — nó là adapter tồi và không đổi được sang kho không phải SQL.\n\n### Ba bản cài đặt Repository\n\n```python\n# ① Bộ nhớ — cho kiểm thử ⭐\nclass KhoBoNho:\n    def __init__(self, ban_dau=()):\n        self._d: dict[int, DonHang] = {}\n        self._id = 0\n        for d in ban_dau:\n            self.them(d)\n\n    def them(self, don):\n        self._id += 1\n        don.ma = self._id\n        self._d[self._id] = don\n        return self._id\n\n    def lay(self, ma):\n        return self._d.get(ma)\n\n    def liet_ke_cho_duyet(self):\n        return [d for d in self._d.values() if d.trang_thai == \"cho\"]\n\n    def cap_nhat(self, don):\n        if don.ma not in self._d:\n            raise KeyError(f\"không có đơn {don.ma}\")\n        self._d[don.ma] = don\n\n\n# ② SQL\nclass KhoSQLite:\n    def __init__(self, conn):\n        self.conn = conn\n\n    def _sang_object(self, r) -> DonHang:            # ⭐ ánh xạ một chỗ\n        return DonHang(ma=r[0], tong=r[1], email=r[2],\n                       trang_thai=r[3],\n                       tao_luc=datetime.fromisoformat(r[4]) if r[4] else None)\n\n    def lay(self, ma):\n        r = self.conn.execute(\n            \"SELECT ma, tong, email, trang_thai, tao_luc FROM don WHERE ma=?\",\n            (ma,)).fetchone()\n        return self._sang_object(r) if r else None\n\n    def liet_ke_cho_duyet(self):\n        return [self._sang_object(r) for r in self.conn.execute(\n            \"SELECT ma, tong, email, trang_thai, tao_luc FROM don \"\n            \"WHERE trang_thai='cho' ORDER BY tao_luc\")]\n    ...\n\n\n# ③ Có cache — bọc bản khác, CÙNG giao diện\nclass KhoCoCache:\n    def __init__(self, goc: KhoDonHang):\n        self._goc, self._cache = goc, {}\n\n    def lay(self, ma):\n        if ma not in self._cache:\n            self._cache[ma] = self._goc.lay(ma)\n        return self._cache[ma]\n\n    def cap_nhat(self, don):\n        self._goc.cap_nhat(don)\n        self._cache.pop(don.ma, None)                # ⭐ vô hiệu cache\n    ...\n```\n\nBản ③ là Decorator ở tầng object: cùng giao diện, bọc quanh một bản khác. Xếp chồng được nhiều lớp — cache, ghi log, đo thời gian.\n\n### Giữ giao ước giống nhau\n\nBa bản cài đặt phải hành xử **như nhau**, nếu không thì đổi bản là hỏng — đúng vấn đề Liskov. Công cụ là bộ kiểm thử giao ước:\n\n```python\nimport pytest, sqlite3\n\n@pytest.fixture(params=[\"bo_nho\", \"sqlite\"])\ndef kho(request):\n    if request.param == \"bo_nho\":\n        return KhoBoNho()\n    conn = sqlite3.connect(\":memory:\")\n    conn.execute(\"CREATE TABLE don (ma INTEGER PRIMARY KEY, tong REAL, \"\n                 \"email TEXT, trang_thai TEXT, tao_luc TEXT)\")\n    return KhoSQLite(conn)\n\n\nclass TestGiaoUocKho:\n    def test_them_roi_lay_lai(self, kho):\n        ma = kho.them(DonHang(None, 100, \"a@x.com\"))\n        d = kho.lay(ma)\n        assert d is not None and d.tong == 100\n\n    def test_lay_ma_khong_ton_tai_tra_None(self, kho):\n        assert kho.lay(9999) is None            # ⭐ cam kết: KHÔNG ném\n\n    def test_chi_liet_ke_don_dang_cho(self, kho):\n        kho.them(DonHang(None, 100, \"a@x.com\", trang_thai=\"cho\"))\n        kho.them(DonHang(None, 200, \"b@x.com\", trang_thai=\"xong\"))\n        assert len(kho.liet_ke_cho_duyet()) == 1\n```\n\nMột bộ kiểm thử, chạy cho mọi bản. Thêm `KhoPostgres` là thêm vào `params` — và nếu nó lệch giao ước, bài kiểm thử chỉ ra **chính xác điều khoản nào**.\n\n### Repository và giao dịch\n\nMột câu hỏi thật khi dùng Repository: nếu một thao tác cần sửa nhiều kho thì giao dịch nằm ở đâu?\n\n```python\nfrom typing import Protocol\n\nclass DonViCongViec(Protocol):\n    \"\"\"Gom nhiều kho vào một giao dịch.\"\"\"\n    don: KhoDonHang\n    kho_hang: KhoHang\n    def __enter__(self) -> \"DonViCongViec\": ...\n    def __exit__(self, *a) -> bool | None: ...\n    def commit(self) -> None: ...\n\n\nclass DonViCongViecSQL:\n    def __init__(self, conn):\n        self.conn = conn\n\n    def __enter__(self):\n        self.don = KhoSQLite(self.conn)\n        self.kho_hang = KhoHangSQLite(self.conn)\n        return self\n\n    def __exit__(self, loai, gt, tv):\n        if loai is None:\n            self.conn.commit()\n        else:\n            self.conn.rollback()\n        return False                        # ⭐ không nuốt ngoại lệ\n\n\n# Dùng\ndef dat_hang(dvcv: DonViCongViec, don: DonHang) -> int:\n    with dvcv:\n        ma = dvcv.don.them(don)\n        dvcv.kho_hang.giu(don.san_pham)\n        return ma\n    # lỗi giữa chừng ⇒ rollback cả hai kho\n```\n\nĐây là mẫu **Đơn vị công việc**, và nó là bài context manager ở module 5 áp dụng vào tầng dữ liệu. Không có nó, hai kho commit độc lập và dữ liệu có thể ở trạng thái nửa vời.\n\n### Khi nào KHÔNG cần Repository\n\nCân bằng lại:\n\n```\n✗ Script một lần đọc ghi vài dòng\n✗ Ứng dụng nhỏ, một database, không có kế hoạch đổi\n✗ Khi ORM đã đóng vai trò tương tự và bạn chấp nhận phụ thuộc vào nó\n✓ Cần kiểm thử nghiệp vụ mà không dựng database\n✓ Nhiều nguồn dữ liệu, hoặc dự định đổi\n✓ Logic nghiệp vụ phức tạp, đáng tách khỏi hạ tầng\n```\n\nHàng thứ ba đáng nói rõ: nhiều ORM **đã là** một dạng repository. Bọc thêm một lớp nữa chỉ đáng khi bạn thật sự cần đổi ORM, hoặc cần bản trong bộ nhớ cho kiểm thử.\n\nMột cái giá thật: Repository làm **truy vấn phức tạp trở nên khó**. Một câu SQL với ba phép nối và một hàm gộp không diễn đạt được qua giao diện object mà không thêm rất nhiều phương thức. Thực tế thường chấp nhận cho một số truy vấn báo cáo đi thẳng SQL, tách riêng khỏi repository nghiệp vụ.",

vi_du: "### Singleton — bốn cách, và cách nào đúng\n\n```python\n# ① Sách gốc — có cạm bẫy __init__\nclass CauHinhSingleton:\n    _instance = None\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n        return cls._instance\n    def __init__(self):\n        if getattr(self, \"_xong\", False):\n            return                          # ⚠️ BẮT BUỘC, nếu không sẽ reset\n        self._xong = True\n        self.debug = False\n\na = CauHinhSingleton(); a.debug = True\nb = CauHinhSingleton()\nprint(b.debug)        # True — nhờ dòng _xong\n\n\n# ② Module — ⭐ Pythonic nhất\n# cau_hinh.py\nDEBUG = False\nDATABASE_URL = \"sqlite:///app.db\"\n\n\n# ③ Instance ở tầng module — vẫn tạo thêm được khi cần\nfrom dataclasses import dataclass\n\n@dataclass\nclass CauHinh:\n    debug: bool = False\n    database_url: str = \"sqlite:///app.db\"\n\ncau_hinh_mac_dinh = CauHinh()            # dùng chung\ncau_hinh_test = CauHinh(debug=True)      # ⭐ tạo thêm cho kiểm thử\n\n\n# ④ Khởi tạo lười có cache\nfrom functools import lru_cache\n\n@lru_cache(maxsize=1)\ndef lay_ket_noi():\n    import sqlite3\n    print(\"tạo kết nối\")\n    return sqlite3.connect(\"app.db\")\n\nlay_ket_noi(); lay_ket_noi()       # \"tạo kết nối\" in MỘT lần\nlay_ket_noi.cache_clear()          # ⭐ đặt lại được khi kiểm thử\n```\n\nCách ④ đáng chú ý: nó cho khởi tạo lười **và** có `cache_clear()` để đặt lại giữa các bài kiểm thử — thứ mà Singleton cổ điển không có.\n\n### Vì sao Singleton phá kiểm thử\n\n```python\nclass CauHinhSingleton:\n    _instance = None\n    def __new__(cls):\n        if cls._instance is None:\n            cls._instance = super().__new__(cls)\n            cls._instance.debug = False\n        return cls._instance\n\n\ndef test_bat_debug():\n    cf = CauHinhSingleton()\n    cf.debug = True\n    assert cf.debug is True\n\ndef test_mac_dinh_la_tat():\n    cf = CauHinhSingleton()\n    assert cf.debug is False       # ⚠️ THẤT BẠI nếu test_bat_debug chạy trước\n```\n\nBài kiểm thử thứ hai **đúng về logic** nhưng thất bại vì bài trước để lại trạng thái. Và nó chỉ thất bại khi chạy theo thứ tự đó — nên lỗi lúc có lúc không, rất khó lần.\n\n```python\n# ✓ Tiêm phụ thuộc — mỗi bài kiểm thử có bản riêng\n@dataclass\nclass CauHinh:\n    debug: bool = False\n\nclass DichVu:\n    def __init__(self, cf: CauHinh):\n        self.cf = cf\n\ndef test_bat_debug():\n    dv = DichVu(CauHinh(debug=True))\n    assert dv.cf.debug is True\n\ndef test_mac_dinh_la_tat():\n    dv = DichVu(CauHinh())          # ⭐ hoàn toàn độc lập\n    assert dv.cf.debug is False\n```\n\n### Adapter — thống nhất hai thư viện khác nhau\n\n```python\nfrom typing import Protocol\n\n# ── Thư viện bên ngoài (không sửa được) ──\nclass SMSProvider:\n    def send_message(self, recipient, body, sender_id):\n        if not recipient.startswith(\"+\"):\n            raise SMSError(\"số điện thoại phải có mã quốc gia\")\n        return {\"status\": \"sent\", \"id\": \"sms-1\"}\n\nclass SMSError(Exception): ...\n\nclass EmailClient:\n    def deliver(self, to: list[str], subject: str, html: str) -> bool:\n        return True\n\n\n# ── Tầng nghiệp vụ: giao diện CỦA BẠN ──\nclass ThongBaoLoi(Exception): ...\n\nclass BoThongBao(Protocol):\n    ten_kenh: str\n    def gui(self, den: str, tin: str) -> None:\n        \"\"\"Gửi thông báo. Raises: ThongBaoLoi nếu thất bại.\"\"\"\n\n\n# ── Tầng hạ tầng: adapter ──\nclass AdapterSMS:\n    ten_kenh = \"SMS\"\n\n    def __init__(self, nha_cung_cap: SMSProvider, ma_gui: str = \"APP\"):\n        self._ncc, self._ma_gui = nha_cung_cap, ma_gui\n\n    def gui(self, den: str, tin: str) -> None:\n        so = den if den.startswith(\"+\") else \"+84\" + den.lstrip(\"0\")  # ⭐ chuẩn hoá\n        try:\n            kq = self._ncc.send_message(so, tin[:160], self._ma_gui)  # ⭐ cắt độ dài\n        except SMSError as e:\n            raise ThongBaoLoi(f\"SMS tới {den} thất bại\") from e       # ⭐ dịch lỗi\n        if kq.get(\"status\") != \"sent\":\n            raise ThongBaoLoi(f\"SMS tới {den}: {kq}\")                 # ⭐ dịch kết quả\n\n\nclass AdapterEmail:\n    ten_kenh = \"Email\"\n\n    def __init__(self, client: EmailClient, tieu_de: str = \"Thông báo\"):\n        self._client, self._tieu_de = client, tieu_de\n\n    def gui(self, den: str, tin: str) -> None:\n        import html\n        ok = self._client.deliver([den], self._tieu_de,\n                                  f\"<p>{html.escape(tin)}</p>\")\n        if not ok:\n            raise ThongBaoLoi(f\"email tới {den} thất bại\")\n\n\nclass AdapterGia:                       # ⭐ cho kiểm thử\n    ten_kenh = \"giả\"\n    def __init__(self): self.da_gui = []\n    def gui(self, den, tin): self.da_gui.append((den, tin))\n\n\n# ── Nghiệp vụ: không biết thư viện nào ──\ndef thong_bao_nhieu_kenh(kenh: list[BoThongBao], den: str, tin: str) -> int:\n    thanh_cong = 0\n    for k in kenh:\n        try:\n            k.gui(den, tin)\n            thanh_cong += 1\n        except ThongBaoLoi as e:\n            print(f\"kênh {k.ten_kenh} lỗi: {e}\")     # ⭐ chỉ bắt lỗi CỦA MÌNH\n    return thanh_cong\n\n\n# Kiểm thử — không mạng, không nhà cung cấp\ndef test_gui_moi_kenh():\n    a, b = AdapterGia(), AdapterGia()\n    assert thong_bao_nhieu_kenh([a, b], \"0901234567\", \"xin chào\") == 2\n    assert a.da_gui == [(\"0901234567\", \"xin chào\")]\n```\n\nBốn việc adapter làm, thấy rõ trong `AdapterSMS`:\n\n```\nchuẩn hoá số điện thoại  → chi tiết của kênh, không phải của nghiệp vụ\ncắt tin về 160 ký tự      → giới hạn của SMS, nghiệp vụ không cần biết\ndịch SMSError             → nghiệp vụ chỉ bắt ThongBaoLoi\ndịch {\"status\": ...}      → giao ước của bạn là \"ném lỗi hoặc thành công\"\n```\n\nHàm `thong_bao_nhieu_kenh` chỉ bắt `ThongBaoLoi` — nó **không import** gì từ thư viện SMS. Đó là dấu hiệu adapter làm đúng việc.\n\n### Adapter cho dữ liệu — API bên ngoài\n\n```python\nfrom dataclasses import dataclass\nfrom datetime import datetime\nfrom typing import Protocol\n\n@dataclass(frozen=True)\nclass NguoiDung:\n    id: int\n    ten: str\n    email: str\n    tao_luc: datetime\n\n\nclass NguonNguoiDung(Protocol):\n    def lay(self, uid: int) -> NguoiDung | None: ...\n    def tim_theo_email(self, email: str) -> NguoiDung | None: ...\n\n\nclass AdapterAPIv1:\n    \"\"\"API v1 trả: {\"user_id\", \"full_name\", \"email_address\", \"created_at\"}\"\"\"\n\n    def __init__(self, client): self._c = client\n\n    def _sang_object(self, d: dict) -> NguoiDung:\n        return NguoiDung(\n            id=int(d[\"user_id\"]),\n            ten=d[\"full_name\"].strip(),\n            email=d[\"email_address\"].lower(),\n            tao_luc=datetime.fromisoformat(d[\"created_at\"].replace(\"Z\", \"+00:00\")),\n        )\n\n    def lay(self, uid):\n        d = self._c.get(f\"/users/{uid}\")\n        return self._sang_object(d) if d else None\n\n    def tim_theo_email(self, email):\n        kq = self._c.get(\"/users\", params={\"email\": email})\n        return self._sang_object(kq[0]) if kq else None\n\n\nclass AdapterAPIv2:\n    \"\"\"API v2 đổi hình dạng: {\"id\", \"name\", \"contact\": {\"email\"}, \"meta\": {...}}\"\"\"\n\n    def __init__(self, client): self._c = client\n\n    def _sang_object(self, d: dict) -> NguoiDung:\n        return NguoiDung(\n            id=d[\"id\"],\n            ten=d[\"name\"],\n            email=d[\"contact\"][\"email\"].lower(),\n            tao_luc=datetime.fromtimestamp(d[\"meta\"][\"created_ts\"]),\n        )\n    ...\n```\n\nAPI đổi hoàn toàn hình dạng, và **tầng nghiệp vụ không đổi một dòng** — nó vẫn nhận `NguoiDung`. Đây là giá trị lớn nhất của adapter cho dữ liệu.\n\nKhông có lớp ánh xạ này, `d[\"email_address\"]` sẽ nằm rải rác khắp code, và đổi API là đi tìm từng chỗ.\n\n### Repository đầy đủ — ba bản cài đặt\n\n```python\nfrom dataclasses import dataclass, field\nfrom datetime import datetime\nfrom typing import Protocol\n\n@dataclass\nclass DonHang:\n    ma: int | None\n    email: str\n    tong: float\n    trang_thai: str = \"cho\"\n    tao_luc: datetime = field(default_factory=datetime.now)\n\n    def duyet(self) -> None:\n        if self.trang_thai != \"cho\":\n            raise ValueError(f\"đơn {self.ma} không ở trạng thái chờ\")\n        self.trang_thai = \"da_duyet\"\n\n\nclass KhoDonHang(Protocol):\n    def them(self, don: DonHang) -> int: ...\n    def lay(self, ma: int) -> DonHang | None: ...\n    def cap_nhat(self, don: DonHang) -> None: ...\n    def liet_ke_cho_duyet(self) -> list[DonHang]: ...\n    def tim_theo_khach(self, email: str) -> list[DonHang]: ...\n\n\n# ── ① Bộ nhớ ──\nclass KhoBoNho:\n    def __init__(self, ban_dau=()):\n        self._d: dict[int, DonHang] = {}\n        self._id = 0\n        for don in ban_dau:\n            self.them(don)\n\n    def them(self, don):\n        self._id += 1\n        don.ma = self._id\n        self._d[self._id] = don\n        return self._id\n\n    def lay(self, ma):\n        return self._d.get(ma)\n\n    def cap_nhat(self, don):\n        if don.ma not in self._d:\n            raise KeyError(f\"không có đơn {don.ma}\")\n        self._d[don.ma] = don\n\n    def liet_ke_cho_duyet(self):\n        return sorted((d for d in self._d.values() if d.trang_thai == \"cho\"),\n                      key=lambda d: d.tao_luc)\n\n    def tim_theo_khach(self, email):\n        return [d for d in self._d.values() if d.email == email.lower()]\n\n\n# ── ② SQLite ──\nimport sqlite3\n\nclass KhoSQLite:\n    def __init__(self, conn: sqlite3.Connection):\n        self.conn = conn\n        self.conn.execute(\n            \"CREATE TABLE IF NOT EXISTS don (\"\n            \"ma INTEGER PRIMARY KEY AUTOINCREMENT, email TEXT NOT NULL, \"\n            \"tong REAL NOT NULL, trang_thai TEXT NOT NULL, tao_luc TEXT NOT NULL)\")\n\n    def _sang_object(self, r) -> DonHang:\n        return DonHang(ma=r[0], email=r[1], tong=r[2],\n                       trang_thai=r[3], tao_luc=datetime.fromisoformat(r[4]))\n\n    def them(self, don):\n        cur = self.conn.execute(\n            \"INSERT INTO don (email, tong, trang_thai, tao_luc) VALUES (?,?,?,?)\",\n            (don.email.lower(), don.tong, don.trang_thai, don.tao_luc.isoformat()))\n        self.conn.commit()\n        don.ma = cur.lastrowid\n        return don.ma\n\n    def lay(self, ma):\n        r = self.conn.execute(\"SELECT * FROM don WHERE ma=?\", (ma,)).fetchone()\n        return self._sang_object(r) if r else None\n\n    def cap_nhat(self, don):\n        cur = self.conn.execute(\n            \"UPDATE don SET email=?, tong=?, trang_thai=? WHERE ma=?\",\n            (don.email.lower(), don.tong, don.trang_thai, don.ma))\n        if cur.rowcount == 0:\n            raise KeyError(f\"không có đơn {don.ma}\")    # ⭐ giữ CÙNG giao ước\n        self.conn.commit()\n\n    def liet_ke_cho_duyet(self):\n        return [self._sang_object(r) for r in self.conn.execute(\n            \"SELECT * FROM don WHERE trang_thai='cho' ORDER BY tao_luc\")]\n\n    def tim_theo_khach(self, email):\n        return [self._sang_object(r) for r in self.conn.execute(\n            \"SELECT * FROM don WHERE email=?\", (email.lower(),))]\n\n\n# ── ③ Có cache — bọc bản khác ──\nclass KhoCoCache:\n    def __init__(self, goc: KhoDonHang):\n        self._goc = goc\n        self._cache: dict[int, DonHang] = {}\n\n    def them(self, don):\n        ma = self._goc.them(don)\n        self._cache[ma] = don\n        return ma\n\n    def lay(self, ma):\n        if ma not in self._cache:\n            self._cache[ma] = self._goc.lay(ma)\n        return self._cache[ma]\n\n    def cap_nhat(self, don):\n        self._goc.cap_nhat(don)\n        self._cache.pop(don.ma, None)          # ⭐ quên dòng này = dữ liệu cũ\n\n    def liet_ke_cho_duyet(self):\n        return self._goc.liet_ke_cho_duyet()   # không cache truy vấn\n\n    def tim_theo_khach(self, email):\n        return self._goc.tim_theo_khach(email)\n```\n\n### Nghiệp vụ dùng Repository\n\n```python\nNGUONG_DUYET = 10_000_000\n\nclass DichVuDonHang:\n    def __init__(self, kho: KhoDonHang, thong_bao: BoThongBao):\n        self.kho, self.thong_bao = kho, thong_bao\n\n    def tao_don(self, email: str, tong: float) -> int:\n        if tong <= 0:\n            raise ValueError(f\"tổng phải dương: {tong}\")\n\n        don = DonHang(None, email.lower(), tong)\n        ma = self.kho.them(don)\n\n        if tong > NGUONG_DUYET:\n            self.thong_bao.gui(email, f\"Đơn {ma} đang chờ duyệt\")\n        else:\n            self.thong_bao.gui(email, f\"Đã nhận đơn {ma}\")\n        return ma\n\n    def duyet(self, ma: int) -> None:\n        don = self.kho.lay(ma)\n        if don is None:\n            raise KeyError(f\"không có đơn {ma}\")\n        don.duyet()                             # ⭐ logic ở đối tượng nghiệp vụ\n        self.kho.cap_nhat(don)\n        self.thong_bao.gui(don.email, f\"Đơn {ma} đã được duyệt\")\n\n    def tong_chi_tieu(self, email: str) -> float:\n        return sum(d.tong for d in self.kho.tim_theo_khach(email))\n```\n\nKhông một dòng SQL nào. `DichVuDonHang` chạy được với cả ba bản cài đặt.\n\n### Kiểm thử — lợi ích lớn nhất\n\n```python\nimport pytest\n\ndef tao_dich_vu(don_co_san=()):\n    kho = KhoBoNho(don_co_san)\n    tb = AdapterGia()\n    return DichVuDonHang(kho, tb), kho, tb\n\n\ndef test_don_nho_khong_can_duyet():\n    dv, kho, tb = tao_dich_vu()\n    ma = dv.tao_don(\"a@x.com\", 1_000_000)\n\n    assert kho.lay(ma).trang_thai == \"cho\"\n    assert \"Đã nhận\" in tb.da_gui[0][1]\n\n\ndef test_don_lon_thong_bao_cho_duyet():\n    dv, kho, tb = tao_dich_vu()\n    dv.tao_don(\"a@x.com\", 50_000_000)\n    assert \"chờ duyệt\" in tb.da_gui[0][1]\n\n\ndef test_tong_am_khong_luu_gi():\n    dv, kho, tb = tao_dich_vu()\n    with pytest.raises(ValueError):\n        dv.tao_don(\"a@x.com\", -100)\n\n    assert kho.liet_ke_cho_duyet() == []       # ⭐ kiểm được điều KHÔNG xảy ra\n    assert tb.da_gui == []\n\n\ndef test_khong_duyet_don_da_duyet():\n    dv, kho, _ = tao_dich_vu()\n    ma = dv.tao_don(\"a@x.com\", 1_000)\n    dv.duyet(ma)\n    with pytest.raises(ValueError, match=\"không ở trạng thái chờ\"):\n        dv.duyet(ma)\n```\n\nBốn bài kiểm thử, không database, chạy trong mili giây. Bài thứ ba kiểm **một điều không xảy ra** — với database thật thì phải truy vấn để xác nhận, còn ở đây là một dòng.\n\n### Bộ kiểm thử giao ước cho ba bản\n\n```python\n@pytest.fixture(params=[\"bo_nho\", \"sqlite\", \"cache\"])\ndef kho(request):\n    if request.param == \"bo_nho\":\n        return KhoBoNho()\n    if request.param == \"sqlite\":\n        return KhoSQLite(sqlite3.connect(\":memory:\"))\n    return KhoCoCache(KhoSQLite(sqlite3.connect(\":memory:\")))\n\n\nclass TestGiaoUocKhoDonHang:\n    def test_them_gan_ma_va_lay_lai_duoc(self, kho):\n        ma = kho.them(DonHang(None, \"a@x.com\", 100))\n        assert ma is not None\n        d = kho.lay(ma)\n        assert d is not None and d.tong == 100\n\n    def test_lay_ma_khong_ton_tai_tra_None(self, kho):\n        assert kho.lay(9999) is None           # KHÔNG ném\n\n    def test_cap_nhat_ma_khong_ton_tai_nem_KeyError(self, kho):\n        with pytest.raises(KeyError):\n            kho.cap_nhat(DonHang(9999, \"a@x.com\", 100))\n\n    def test_email_duoc_chuan_hoa_ve_chu_thuong(self, kho):\n        kho.them(DonHang(None, \"A@X.COM\", 100))\n        assert len(kho.tim_theo_khach(\"a@x.com\")) == 1\n\n    def test_cap_nhat_roi_lay_lai_thay_gia_tri_moi(self, kho):\n        ma = kho.them(DonHang(None, \"a@x.com\", 100))\n        don = kho.lay(ma)\n        don.trang_thai = \"da_duyet\"\n        kho.cap_nhat(don)\n        assert kho.lay(ma).trang_thai == \"da_duyet\"      # ⭐ bắt lỗi quên xoá cache\n```\n\nBài cuối là bài **bắt được lỗi cache**: nếu `KhoCoCache.cap_nhat` quên `self._cache.pop(...)`, `lay()` trả bản cũ và bài này thất bại. Không có bộ kiểm thử giao ước thì lỗi đó chỉ lộ ra trong sản phẩm.\n\n### Đổi bản cài đặt ở thành phần gốc\n\n```python\nimport os, sqlite3\n\ndef dung_ung_dung(che_do: str = \"that\"):\n    if che_do == \"kiem_thu\":\n        kho, thong_bao = KhoBoNho(), AdapterGia()\n    elif che_do == \"cuc_bo\":\n        kho = KhoSQLite(sqlite3.connect(\"app.db\"))\n        thong_bao = AdapterGia()\n    else:\n        kho = KhoCoCache(KhoSQLite(sqlite3.connect(os.environ[\"DB\"])))\n        thong_bao = AdapterSMS(SMSProvider())\n\n    return DichVuDonHang(kho, thong_bao)\n\n\ndv = dung_ung_dung(\"kiem_thu\")\ndv.tao_don(\"a@x.com\", 1_000_000)\n```\n\nBa chế độ chạy, **một dòng đổi**, và `DichVuDonHang` không biết gì.\n\n### Kiểm tra chiều phụ thuộc\n\n```bash\n# Nghiệp vụ không được biết tới thư viện hạ tầng\ngrep -rn \"sqlite3\\|psycopg2\\|SMSProvider\\|requests\" nghiep_vu/\n# (không có kết quả) ✓\n\n# Mỗi thư viện ngoài chỉ xuất hiện ở MỘT adapter\ngrep -rln \"SMSProvider\" .\n# ./ha_tang/adapter_sms.py\n# ./main.py                    ← chỉ hai chỗ ✓\n```\n\nHai lệnh này là phép kiểm nhanh cho cả Adapter lẫn Repository. Kết quả nhiều hơn hai file nghĩa là thư viện ngoài đang rò rỉ vào nơi khác.\n\n### Khi Repository là thừa\n\n```python\n# ❌ Script một lần — Repository chỉ thêm tầng\nclass KhoTam(Protocol):\n    def lay_tat_ca(self) -> list[dict]: ...\n\nclass KhoCSV:\n    def lay_tat_ca(self):\n        import csv\n        with open(\"a.csv\", newline=\"\", encoding=\"utf-8-sig\") as f:\n            return list(csv.DictReader(f))\n\nprint(len(KhoCSV().lay_tat_ca()))\n\n# ✓\nimport csv\nwith open(\"a.csv\", newline=\"\", encoding=\"utf-8-sig\") as f:\n    print(len(list(csv.DictReader(f))))\n```\n\nPhép kiểm: **có bản cài đặt thứ hai không, hoặc có cần kiểm thử mà không dựng hạ tầng không?** Không cả hai → Repository là tầng thừa.",

so_sanh: "### Ba mẫu — vai trò\n\n| | Singleton | Adapter | Repository |\n|---|---|---|---|\n| Vấn đề | chỉ một instance | giao diện không khớp | giấu cách lưu trữ |\n| Trong Python | **thường sai** | **hữu ích** | **rất hữu ích** |\n| Thay thế Pythonic | module, `lru_cache` | — | — |\n| SOLID liên quan | (thường vi phạm DIP) | DIP | DIP, SRP |\n| Ảnh hưởng kiểm thử | **xấu** | tốt | **rất tốt** |\n\nHàng cuối là lý do chính để tránh mẫu đầu và dùng hai mẫu sau.\n\n### Bốn cách thay Singleton\n\n| | Sách gốc | Module | Instance ở module | `lru_cache` |\n|---|---|---|---|---|\n| Pythonic | ✗ | **✓** | ✓ | ✓ |\n| Khởi tạo lười | ✓ | ✗ | ✗ | **✓** |\n| Tạo bản khác khi test | **✗** | ✗ | **✓** | ✓ `cache_clear()` |\n| Cạm bẫy `__init__` chạy lại | **✓** | — | — | — |\n| Phù hợp | hiếm | hằng số | cấu hình | kết nối, tài nguyên nặng |\n\nCột `lru_cache` là lựa chọn tốt nhất khi cần khởi tạo lười: nó có cả tính lười lẫn khả năng đặt lại.\n\n### Bốn việc của Adapter\n\n| Việc | Ví dụ | Hay bị quên |\n|---|---|---|\n| Đổi tên phương thức | `send_message` → `gui` | ✗ |\n| Đổi hình dạng tham số | `(to, subject, html)` → `(den, tin)` | ✗ |\n| Dịch kiểu trả về | `{\"status\": \"ok\"}` → `True` hoặc ném lỗi | **✓** |\n| **Dịch ngoại lệ** | `SMSError` → `ThongBaoLoi` | **✓ nhiều nhất** |\n\nQuên hai việc cuối thì tầng nghiệp vụ vẫn phải biết tới thư viện ngoài — adapter chưa hoàn thành nhiệm vụ.\n\n### Repository và DAO\n\n| | DAO | Repository |\n|---|---|---|\n| Phản chiếu | **bảng database** | **khái niệm nghiệp vụ** |\n| Tên phương thức | `insert`, `select_by_id` | `liet_ke_cho_duyet`, `tim_theo_khach` |\n| Trả về | dòng dữ liệu, `dict` | **đối tượng nghiệp vụ** |\n| Một cái cho | mỗi bảng | mỗi nhóm nghiệp vụ |\n| SQL rò rỉ ra ngoài | thường có | **không** |\n\nDấu hiệu Repository làm sai: có tham số kiểu `dieu_kien: str` — đó là SQL đội lốt, và nó khoá bạn vào database quan hệ.\n\n### Ba bản cài đặt Repository\n\n| | Bộ nhớ | SQL | Có cache |\n|---|---|---|---|\n| Dùng cho | **kiểm thử** | sản phẩm | sản phẩm, đọc nhiều |\n| Tốc độ test | **mili giây** | giây | — |\n| Cần hạ tầng | ✗ | ✓ | ✓ |\n| Cạm bẫy riêng | dữ liệu mất khi kết thúc | ánh xạ rải rác | **quên xoá cache khi ghi** |\n\nCạm bẫy của bản cache là lỗi im lặng: trả dữ liệu cũ mà không có lỗi nào. Bộ kiểm thử giao ước bắt được nó.\n\n### Khi nào dùng\n\n| Tình huống | Singleton | Adapter | Repository |\n|---|---|---|---|\n| Dùng thư viện bên thứ ba | — | **✓** | — |\n| Cần kiểm thử không dựng DB | ✗ | ✓ | **✓** |\n| Có thể đổi nhà cung cấp | — | **✓** | ✓ |\n| Script một lần | ✗ | ✗ | ✗ |\n| Cấu hình chỉ đọc | dùng module | — | — |\n| Truy vấn báo cáo phức tạp | — | — | **✗ đi thẳng SQL** |\n\nHàng cuối là giới hạn thật của Repository: truy vấn nhiều phép nối và hàm gộp không diễn đạt gọn qua giao diện object.\n\n### Cái giá\n\n| Mẫu | Cái giá |\n|---|---|\n| Adapter | thêm một lớp; phải bảo trì khi thư viện đổi |\n| Repository | nhiều file; **truy vấn phức tạp khó diễn đạt**; ánh xạ tốn công |\n| Singleton | **trạng thái toàn cục, phá kiểm thử, giấu phụ thuộc** |\n\nHai mẫu đầu có cái giá **trả bằng công sức**; mẫu cuối có cái giá **trả bằng chất lượng thiết kế** — đó là khác biệt quyết định.",

loi_thuong_gap: [
  "**Dùng Singleton cho cấu hình hoặc kết nối.** Trạng thái toàn cục làm kiểm thử dính vào nhau và phụ thuộc thứ tự chạy.",
  "**Cài Singleton mà quên chặn `__init__` chạy lại.** Mỗi lần gọi `Lop()` sẽ đặt lại mọi thuộc tính.",
  "**Adapter chỉ đổi tên phương thức mà không dịch ngoại lệ.** Nghiệp vụ vẫn phải import và bắt lỗi của thư viện ngoài.",
  "**Adapter không dịch kiểu trả về.** `{\"status\": \"ok\"}` rò rỉ vào nghiệp vụ.",
  "**Để hình dạng dữ liệu của API bên ngoài lan vào nghiệp vụ.** `d[\"full_name\"]` rải rác khắp code; đổi API là sửa mọi chỗ.",
  "**Repository có tham số kiểu `dieu_kien: str`.** SQL đội lốt — khoá bạn vào database quan hệ.",
  "**Đặt tên phương thức Repository theo thao tác database.** `select_by_id` thay vì `tim_theo_khach` là nghĩ theo DAO.",
  "**Quên vô hiệu cache khi ghi trong lớp Repository có cache.** Trả dữ liệu cũ, không có lỗi nào báo.",
  "**Các bản cài đặt Repository hành xử khác nhau.** Bản này ném `KeyError`, bản kia trả `None` — đổi bản là hỏng. Dùng bộ kiểm thử giao ước.",
  "**Dựng Repository cho script một lần.** Tầng thừa, không được gì.",
  "**Cố nhét mọi truy vấn báo cáo phức tạp vào Repository.** Chấp nhận cho một số truy vấn đi thẳng SQL, tách riêng.",
  "**Gọi thư viện ngoài ở nhiều chỗ thay vì qua một adapter.** `grep` tên thư viện ra hơn hai file là dấu hiệu.",
  "**Bọc thêm Repository quanh một ORM đã đóng vai trò tương tự.** Chỉ đáng khi cần bản trong bộ nhớ cho kiểm thử hoặc thật sự cần đổi ORM."
],

tu_kiem_tra: [
  "Vì sao Python không cần Singleton? Cơ chế nào của ngôn ngữ đã cho sẵn?",
  "Cài Singleton kiểu sách gốc có cạm bẫy gì với `__init__`?",
  "Nêu ba lý do Singleton phá kiểm thử. Lý do nào liên quan tới Dependency Inversion?",
  "Nêu bốn cách thay Singleton và trường hợp dùng của mỗi cách.",
  "`lru_cache(maxsize=1)` cho hai ưu điểm nào so với Singleton cổ điển?",
  "Nêu bốn việc Adapter làm. Hai việc nào hay bị quên nhất, và hậu quả?",
  "Phép kiểm nhanh để biết adapter làm đúng việc là gì?",
  "Adapter cho dữ liệu giải quyết vấn đề gì khi API bên ngoài đổi hình dạng?",
  "Repository khác DAO ở điểm nào? Dấu hiệu Repository làm sai?",
  "Nêu ba điều Repository mang lại cho tầng nghiệp vụ.",
  "Vì sao bản cài đặt trong bộ nhớ là \"món quà lớn nhất\" của Repository?",
  "Bộ kiểm thử giao ước bắt được lỗi nào của bản Repository có cache?",
  "Mẫu \"Đơn vị công việc\" giải quyết vấn đề gì? Nó dựa trên cơ chế nào của Python?",
  "Nêu hai giới hạn thật của Repository.",
  "Ba mẫu này khác nhau ra sao về ảnh hưởng tới kiểm thử?"
],

tai_nguyen: [
  "Sách **Patterns of Enterprise Application Architecture** — Martin Fowler. Nguồn gốc của Repository và Unit of Work, mô tả đầy đủ nhất.",
  "Sách **Architecture Patterns with Python** — Harry Percival & Bob Gregory. Áp dụng Repository, Unit of Work và Adapter vào Python thật, kèm kiểm thử.",
  "Sách **Design Patterns** — Gamma và cộng sự. Adapter và Singleton nằm ở đây; phần phê phán Singleton đáng đọc.",
  "Sách **Fluent Python** — Luciano Ramalho. Phần giải thích vì sao module là singleton và các cách thay thế trong Python.",
  "Tài liệu Python, module `functools` — `lru_cache` dùng cho khởi tạo lười, kèm `cache_clear` để đặt lại khi kiểm thử."
],

lien_he: [
  "**Design pattern — Factory, Strategy, Observer** ngay trước: cùng cách tiếp cận, và Factory thường dùng chung với Repository để tạo bản cài đặt.",
  "**SOLID — Dependency Inversion**: Adapter và Repository là DIP ở dạng cụ thể; trừu tượng thuộc tầng nghiệp vụ.",
  "**SOLID — Liskov**: bộ kiểm thử giao ước là công cụ bắt buộc khi Repository có nhiều bản cài đặt.",
  "**SOLID — Single Responsibility**: Repository tách trách nhiệm lưu trữ khỏi logic nghiệp vụ.",
  "**Composition over inheritance**: lớp Repository có cache là composition — bọc quanh một bản khác, cùng giao diện.",
  "**Trừu tượng: ABC, Protocol**: `Protocol` mô tả giao diện của cả Adapter lẫn Repository.",
  "**Module, package, import** ở module 5: module chỉ chạy một lần — đó là lý do Python không cần Singleton.",
  "**Decorator** ở module 5: `lru_cache` là cách thay Singleton, và lớp cache là mẫu Decorator ở tầng object.",
  "**Context manager và `with`** ở module 5: Đơn vị công việc dựng trên `__enter__`/`__exit__`.",
  "**Xử lý ngoại lệ** ở module 5: `raise ... from e` là cách adapter dịch ngoại lệ mà vẫn giữ nguyên nhân gốc.",
  "**Database** ở mảng 04: Repository là ranh giới giữa mã ứng dụng và SQL; giao dịch và cách ánh xạ được nói kỹ ở đó.",
  "**Backend & Web** ở mảng 05: Repository, Adapter và Unit of Work là ba mẫu nền của kiến trúc phân tầng.",
  "Đây là **bài cuối module Lập trình hướng đối tượng**: module 7 chuyển sang Bộ nhớ và cơ chế thực thi, nơi bạn nhìn xuống dưới xem Python chạy thế nào."
]

};
