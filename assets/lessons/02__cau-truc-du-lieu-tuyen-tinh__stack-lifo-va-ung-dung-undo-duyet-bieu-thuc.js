window.LESSONS["02:cau-truc-du-lieu-tuyen-tinh:stack-lifo-va-ung-dung-undo-duyet-bieu-thuc"] = {

tom_tat: [
  "**Stack là cấu trúc đơn giản nhất mà lại có nhiều ứng dụng nhất.** Chỉ ba thao tác — `push`, `pop`, `peek` — nhưng nó là nền của đệ quy, undo, phân tích cú pháp, và duyệt cây.",
  "**Dấu hiệu nhận ra bài toán cần stack: \"cái mở gần nhất phải khớp với cái đóng đầu tiên\".** Ngoặc, thẻ HTML, lời gọi hàm — cùng một mẫu lồng nhau.",
  "**Trong Python, dùng `list` làm stack là đúng** — `append` và `pop()` đều ở cuối nên đều O(1). Đây là trường hợp hiếm hoi mà cách đơn giản nhất cũng là cách tối ưu.",
  "**Mọi thuật toán đệ quy đều chuyển được thành vòng lặp với stack tường minh** — cách chữa `RecursionError` mà không cần nâng giới hạn nguy hiểm.",
  "**Undo/redo là hai stack đối xứng**, và một quy tắc quan trọng: thao tác mới xoá sạch stack redo."
],

can_biet_truoc: [
  "Bài **Array và dynamic array** trong module này — vì sao `append`/`pop()` ở cuối là O(1) còn ở đầu là O(n).",
  "Bài **Linked list** ngay trước — cài stack bằng danh sách đơn là ứng dụng tự nhiên nhất của nó.",
  "Bài **Độ phức tạp không gian** ở module 1 mảng này — ngăn xếp lời gọi, và vì sao đệ quy sâu gây `RecursionError`.",
  "Bài **Stack và heap** ở module 2 mảng 01 — ngăn xếp lời gọi của hệ điều hành chính là một stack."
],

dinh_nghia: "Nói đời thường: chồng đĩa trong bếp. Bạn đặt đĩa sạch lên trên cùng, và khi cần thì lấy cũng từ trên cùng. Không ai rút đĩa ở giữa chồng — và chính ràng buộc đó làm thao tác trở nên đơn giản và nhanh.\n\nCác khái niệm:\n\n- **Stack** (ngăn xếp) — cấu trúc mà việc thêm và lấy đều ở **cùng một đầu**, gọi là **đỉnh** (*top*).\n- **LIFO** (*Last In, First Out*) — vào sau ra trước. Đây là tính chất định nghĩa của stack.\n- **`push`** — thêm một phần tử lên đỉnh. **`pop`** — lấy và **bỏ** phần tử ở đỉnh. **`peek`** (hay `top`) — **xem** phần tử đỉnh mà không bỏ.\n- **Tràn ngăn xếp** (*stack overflow*) — đẩy vào quá sức chứa. Trong Python là `RecursionError` khi đệ quy quá sâu.\n- **Ngăn xếp lời gọi** (*call stack*) — stack mà chương trình dùng để nhớ đường về sau mỗi lời gọi hàm.\n- **Ký pháp hậu tố** (*postfix / Reverse Polish*) — viết toán tử **sau** toán hạng: `3 4 +` thay vì `3 + 4`. Tính được bằng stack mà không cần dấu ngoặc.\n\nĐiểm cần nhớ nhất về stack, và là lý do nó xuất hiện khắp nơi:\n\n```\nSTACK GIẢI ĐÚNG MỘT LOẠI BÀI TOÁN:\n\n  \"Cái MỞ GẦN NHẤT phải khớp với cái ĐÓNG ĐẦU TIÊN.\"\n\n  ( [ { } ] )        ngoặc lồng nhau\n  <div><p></p></div> thẻ HTML\n  f() gọi g() gọi h() lời gọi hàm — h xong trước, rồi g, rồi f\n\n⇒ Thấy cấu trúc LỒNG NHAU là nghĩ tới stack.\n```",

vi_sao_can: "Stack đơn giản tới mức dễ bị coi nhẹ, nhưng nó là nền của nhiều thứ bạn dùng hằng ngày:\n\n- **Nó giải thích cách chương trình của bạn chạy.** Ngăn xếp lời gọi là một stack; traceback bạn đọc khi có lỗi chính là nội dung stack đó in ra. Hiểu stack là hiểu vì sao traceback đọc từ dưới lên.\n- **Nó là cách chữa `RecursionError` đúng đắn.** Bài module 1 đã cảnh báo không được nâng `setrecursionlimit`; cách đúng là chuyển đệ quy thành vòng lặp với stack tường minh — và bài này chỉ ra quy trình chuyển đổi.\n- **Nó là công cụ phân tích cú pháp.** Trình biên dịch, trình đọc JSON, trình dựng HTML — tất cả dùng stack để xử lý cấu trúc lồng nhau. Đây là nối trực tiếp về bài *trình biên dịch làm gì* ở module 1 mảng 01.\n- **Nó là mẫu thiết kế cho undo/redo.** Mọi trình soạn thảo đều dùng hai stack; hiểu mẫu này là viết được chức năng undo cho ứng dụng của mình.\n- **Nó là bước đầu của duyệt cây và đồ thị.** DFS chính là duyệt bằng stack — module 4 và 5 sẽ dùng lại nguyên mẫu này.\n\nTình huống thật rất hay gặp: bạn viết hàm duyệt cây thư mục đệ quy, chạy tốt trên máy mình. Trên máy chủ có cây thư mục sâu 2.000 tầng (do một liên kết tượng trưng hoặc dữ liệu bất thường), chương trình chết với `RecursionError`. **Chuyển sang stack tường minh giải quyết dứt điểm** — vì stack nằm trên heap, không bị giới hạn 1.000 tầng của ngăn xếp lời gọi.\n\nMột lưu ý về mức độ khó: bài này dễ hơn hai bài trước về mặt cấu trúc, nhưng **các ứng dụng của nó thì không**. Phần tính biểu thức và chuyển đệ quy sang vòng lặp là hai kỹ thuật đáng đầu tư thời gian nhất.",

co_che: "### Ba thao tác, và vì sao chỉ có ba\n\n```\n        push(4)              pop() → 4\n          ↓                     ↑\n     ┌────────┐           ┌────────┐\n     │   4    │ ← đỉnh    │        │\n     ├────────┤           ├────────┤\n     │   3    │           │   3    │ ← đỉnh\n     ├────────┤           ├────────┤\n     │   1    │           │   1    │\n     └────────┘           └────────┘\n```\n\n```\npush(x)   thêm x lên đỉnh          O(1)\npop()     lấy và BỎ phần tử đỉnh   O(1)\npeek()    XEM đỉnh, không bỏ       O(1)\nis_empty()  có rỗng không           O(1)\n```\n\nĐiểm đáng chú ý: **stack cố tình KHÔNG cho truy cập phần tử ở giữa.** Đó không phải thiếu sót mà là **ràng buộc có chủ ý** — và ràng buộc đó chính là thứ làm mọi thao tác O(1) và làm code dùng nó dễ lập luận hơn.\n\n### Hai cách cài đặt\n\n```\nBẰNG MẢNG (list Python)\n\n    [1][3][4][ ][ ]\n              ↑ đỉnh = cuối mảng\n\n    push → append   O(1) khấu hao\n    pop  → pop()    O(1)\n\n⭐ ĐỈNH ĐẶT Ở CUỐI, không phải đầu.\n  Đặt ở đầu thì push/pop thành O(n) vì phải dịch (bài mảng động).\n```\n\n```\nBẰNG DANH SÁCH LIÊN KẾT ĐƠN\n\n    đỉnh → [4] → [3] → [1] → None\n\n    push → thêm ĐẦU   O(1)\n    pop  → xoá ĐẦU    O(1)\n\n⭐ ĐỈNH ĐẶT Ở ĐẦU — ngược với mảng.\n  Vì linked list thêm/xoá đầu là O(1), cuối là O(n) (nếu không giữ tail).\n```\n\nSo sánh hai cách:\n\n```\n                     Mảng (list)      Linked list\n                     ───────────      ───────────\npush / pop           O(1)             O(1)\nBộ nhớ mỗi phần tử   ⭐ ít            nhiều (một object mỗi nút)\nCache locality       ⭐ tốt           tệ\nCó giới hạn kích thước ✗ (tự nở)      ✗\nTrong Python         ⭐ NÊN DÙNG      chỉ để học\n```\n\nKết luận rất rõ ràng: **trong Python, dùng `list` làm stack là lựa chọn đúng.** Đây là trường hợp hiếm hoi mà cách đơn giản nhất cũng là cách tối ưu — khác với hàng đợi, nơi `list` là lựa chọn sai.\n\n⚠️ Nhưng có một lưu ý về **giao diện**: `list` cho phép `ds[0]`, `ds.insert(2, x)`, `ds.sort()`. Nếu bạn muốn ép ràng buộc LIFO (để người khác không dùng sai), hãy bọc nó trong một class chỉ phơi ba thao tác — đúng nguyên tắc *Interface Segregation* ở module 6 mảng 01.\n\n### Mẫu bài toán ①: cấu trúc lồng nhau\n\nĐây là ứng dụng phổ biến nhất, và mẫu rất dễ nhận:\n\n```\nQUY TẮC:\n  Gặp ký tự MỞ   → push vào stack\n  Gặp ký tự ĐÓNG → pop ra, kiểm tra có KHỚP không\n  Hết chuỗi      → stack phải RỖNG\n```\n\n```\nVí dụ: \"( [ ] )\"\n\n  '('  push      stack: [ '(' ]\n  '['  push      stack: [ '(', '[' ]\n  ']'  pop → '[' khớp ']'   ✓   stack: [ '(' ]\n  ')'  pop → '(' khớp ')'   ✓   stack: []\n  hết  stack rỗng           ✓   HỢP LỆ\n```\n\nBa kiểu sai và cách stack phát hiện:\n\n```\n\"( ]\"     pop ra '(' nhưng cần '['        → SAI KHỚP\n\"( ( )\"   hết chuỗi mà stack còn '('       → THIẾU ĐÓNG\n\") (\"     gặp ')' khi stack RỖNG           → THỪA ĐÓNG\n```\n\nBa trường hợp này là **ba ca kiểm thử bắt buộc** cho mọi bài toán dạng này.\n\n### Mẫu bài toán ②: tính biểu thức\n\nĐây là ứng dụng đẹp nhất của stack, và nó giải thích cách máy tính thật sự tính toán.\n\n**Ký pháp hậu tố** bỏ được dấu ngoặc hoàn toàn:\n\n```\nTRUNG TỐ (người viết)     HẬU TỐ (máy tính)\n─────────────────────     ─────────────────\n3 + 4                     3 4 +\n3 + 4 × 2                 3 4 2 × +\n(3 + 4) × 2               3 4 + 2 ×\n\n⭐ Hậu tố KHÔNG CẦN dấu ngoặc và KHÔNG CẦN quy tắc ưu tiên —\n  thứ tự viết đã quyết định thứ tự tính.\n```\n\nTính hậu tố bằng stack — thuật toán chỉ có hai quy tắc:\n\n```\nGặp SỐ        → push\nGặp TOÁN TỬ   → pop HAI số, tính, push kết quả trở lại\n\nVí dụ: \"3 4 2 × +\"\n\n  '3'  push        [3]\n  '4'  push        [3, 4]\n  '2'  push        [3, 4, 2]\n  '×'  pop 2, pop 4 → 4×2=8 → push   [3, 8]\n  '+'  pop 8, pop 3 → 3+8=11 → push  [11]\n\n  Kết quả: 11\n```\n\n⚠️ Chi tiết quan trọng với phép **không giao hoán**:\n\n```\n\"10 3 -\"  phải cho 7, không phải −7\n\n  pop lần 1 → 3   (đây là toán hạng PHẢI)\n  pop lần 2 → 10  (đây là toán hạng TRÁI)\n  ⇒ tính 10 − 3 = 7\n\n⭐ Pop ra theo thứ tự NGƯỢC. Nhầm chỗ này là lỗi phổ biến nhất.\n```\n\n**Chuyển trung tố sang hậu tố** — thuật toán sân ga (*shunting-yard*):\n\n```\nGặp SỐ         → xuất ra ngay\nGặp TOÁN TỬ    → pop mọi toán tử có ưu tiên ≥ nó, rồi push nó\nGặp '('        → push\nGặp ')'        → pop tới khi gặp '(' , bỏ '('\nHết chuỗi      → pop hết phần còn lại\n```\n\nÝ tưởng cốt lõi: **stack giữ các toán tử đang chờ**, và một toán tử chỉ được xuất ra khi gặp toán tử có ưu tiên thấp hơn hoặc bằng — vì lúc đó nó phải được tính trước.\n\n### Mẫu bài toán ③: undo / redo\n\n```\nHAI STACK ĐỐI XỨNG:\n\n  undo_stack: các thao tác ĐÃ LÀM\n  redo_stack: các thao tác ĐÃ HOÀN TÁC\n\n  Làm việc mới  → push vào undo,  ⚠️ XOÁ SẠCH redo\n  Undo          → pop từ undo → thực hiện ngược → push vào redo\n  Redo          → pop từ redo → thực hiện lại  → push vào undo\n```\n\nQuy tắc **xoá sạch redo khi có thao tác mới** là chi tiết quan trọng nhất, và lý do rất cụ thể:\n\n```\nBạn: viết A → viết B → undo (bỏ B) → viết C\n\nGiờ \"redo B\" có nghĩa gì không?\n  Lịch sử đã RẼ NHÁNH: B và C là hai tương lai khác nhau.\n  ⇒ Redo B sẽ tạo trạng thái không nhất quán.\n\n⇒ Phải XOÁ redo. Đây là hành vi của mọi trình soạn thảo.\n```\n\n### Mẫu bài toán ④: khử đệ quy\n\nĐây là kỹ thuật giá trị nhất của bài, và nó giải quyết trực tiếp vấn đề `RecursionError`.\n\n```\nMỌI hàm đệ quy đều chuyển được thành vòng lặp + stack tường minh.\n\nVÌ SAO: đệ quy vốn ĐANG dùng một stack — ngăn xếp lời gọi.\n        Ta chỉ thay stack của hệ điều hành bằng stack của mình.\n\nLỢI ÍCH: stack của mình nằm trên HEAP\n         ⇒ không bị giới hạn ~1000 tầng\n```\n\nQuy trình chuyển đổi:\n\n```\n① Xác định TRẠNG THÁI mà mỗi lời gọi cần nhớ\n② Đưa trạng thái đó vào stack thay vì vào tham số hàm\n③ Vòng lặp: pop trạng thái → xử lý → push các trạng thái con\n④ Chú ý THỨ TỰ push (stack là LIFO nên push ngược)\n```\n\nBước ④ là chỗ hay sai nhất: muốn xử lý con trái trước thì phải **push con phải trước**.\n\n### Ngăn xếp lời gọi — stack mà bạn đã dùng\n\nNối về bài stack/heap ở module 2 mảng 01:\n\n```python\ndef a(): b()\ndef b(): c()\ndef c(): raise ValueError(\"lỗi\")\n```\n\n```\nKhi c() ném lỗi, ngăn xếp lời gọi là:\n\n    ┌──────────┐\n    │   c()    │ ← đỉnh, nơi lỗi xảy ra\n    ├──────────┤\n    │   b()    │\n    ├──────────┤\n    │   a()    │\n    └──────────┘\n\nTraceback in ra từ ĐÁY lên ĐỈNH:\n  File ..., line ..., in a\n  File ..., line ..., in b\n  File ..., line ..., in c        ← dòng CUỐI là nơi lỗi thật\n```\n\nĐây là lý do quy tắc đọc traceback: **đọc từ dưới lên** — dòng cuối là đỉnh stack, tức là nơi lỗi xảy ra; các dòng trên cho biết đường đi tới đó.\n\n### Khi nào KHÔNG dùng stack\n\n```\nSTACK SAI KHI:\n· Cần lấy phần tử VÀO TRƯỚC     → queue (FIFO)\n· Cần truy cập giữa             → list hoặc mảng\n· Cần lấy phần tử NHỎ NHẤT      → heap\n· Cần cả hai đầu                → deque\n\nSTACK ĐÚNG KHI:\n· Cấu trúc LỒNG NHAU (ngoặc, thẻ, lời gọi)\n· Cần \"quay lui\" về trạng thái trước (undo, backtracking)\n· Khử đệ quy\n· DFS trên cây và đồ thị\n```\n\nMột nhận xét đáng nhớ: **stack và queue khác nhau đúng một chữ — lấy từ đầu nào.** Nhưng khác biệt nhỏ đó dẫn tới hai lớp bài toán hoàn toàn khác nhau, và bài queue tiếp theo sẽ cho thấy DFS với BFS khác nhau ra sao chỉ vì đổi stack thành queue.",

vi_du: "### Cài đặt stack — hai cách\n\n```python\nfrom typing import Any, Iterator\n\n\nclass StackMang:\n    \"\"\"Stack bằng list Python. Đỉnh ở CUỐI mảng ⇒ mọi thao tác O(1).\"\"\"\n\n    def __init__(self) -> None:\n        self._o: list[Any] = []\n\n    def push(self, x: Any) -> None:\n        self._o.append(x)                  # ⭐ O(1) khấu hao\n\n    def pop(self) -> Any:\n        \"\"\"Raises: IndexError nếu rỗng.\"\"\"\n        if not self._o:\n            raise IndexError(\"pop từ stack rỗng\")\n        return self._o.pop()               # ⭐ O(1) — lấy từ CUỐI\n\n    def peek(self) -> Any:\n        \"\"\"Raises: IndexError nếu rỗng.\"\"\"\n        if not self._o:\n            raise IndexError(\"peek stack rỗng\")\n        return self._o[-1]\n\n    def __len__(self) -> int:\n        return len(self._o)\n\n    def __bool__(self) -> bool:\n        return bool(self._o)               # ⭐ cho phép: while stack:\n\n    def __repr__(self) -> str:\n        return f\"StackMang({self._o}, đỉnh→)\"\n```\n\n```python\nclass NutStack:\n    __slots__ = (\"gia_tri\", \"duoi\")\n\n    def __init__(self, gia_tri: Any, duoi: \"NutStack | None\") -> None:\n        self.gia_tri = gia_tri\n        self.duoi = duoi\n\n\nclass StackLienKet:\n    \"\"\"Stack bằng danh sách liên kết đơn. Đỉnh ở ĐẦU ⇒ O(1).\"\"\"\n\n    def __init__(self) -> None:\n        self._dinh: NutStack | None = None\n        self._size = 0\n\n    def push(self, x: Any) -> None:\n        self._dinh = NutStack(x, self._dinh)   # ⭐ nút mới trỏ xuống đỉnh cũ\n        self._size += 1\n\n    def pop(self) -> Any:\n        if self._dinh is None:\n            raise IndexError(\"pop từ stack rỗng\")\n        nut = self._dinh\n        self._dinh = nut.duoi\n        nut.duoi = None                        # ⭐ cắt để GC thu hồi\n        self._size -= 1\n        return nut.gia_tri\n\n    def peek(self) -> Any:\n        if self._dinh is None:\n            raise IndexError(\"peek stack rỗng\")\n        return self._dinh.gia_tri\n\n    def __len__(self) -> int:\n        return self._size\n\n    def __bool__(self) -> bool:\n        return self._size > 0\n```\n\n```python\nfor Lop in (StackMang, StackLienKet):\n    s = Lop()\n    assert len(s) == 0 and not s\n\n    s.push(1); s.push(2); s.push(3)\n    assert len(s) == 3\n    assert s.peek() == 3                 # ⭐ xem không bỏ\n    assert len(s) == 3\n    assert s.pop() == 3                  # ⭐ LIFO: 3 vào cuối, ra đầu\n    assert s.pop() == 2\n    assert s.pop() == 1\n    assert not s\n\n    # ---- Ca biên ----\n    try:\n        s.pop()\n    except IndexError as e:\n        print(f\"✓ {Lop.__name__}: {e}\")\n    try:\n        s.peek()\n    except IndexError as e:\n        print(f\"✓ {Lop.__name__}: {e}\")\n\nprint(\"hai cách cài đặt: mọi ca kiểm thử đạt\")\n```\n\n⚠️ Vì sao đỉnh phải ở **cuối** mảng — chứng minh bằng phép đo:\n\n```python\nimport time\n\nn = 50_000\n\n# ✓ Đỉnh ở CUỐI — O(1)\nt0 = time.perf_counter()\ns = []\nfor i in range(n):\n    s.append(i)\nwhile s:\n    s.pop()\nt_cuoi = time.perf_counter() - t0\n\n# ❌ Đỉnh ở ĐẦU — O(n) mỗi thao tác\nt0 = time.perf_counter()\ns = []\nfor i in range(n):\n    s.insert(0, i)                       # ⚠️ dịch mọi phần tử\nwhile s:\n    s.pop(0)                             # ⚠️ dịch mọi phần tử\nt_dau = time.perf_counter() - t0\n\nprint(f\"đỉnh ở CUỐI: {t_cuoi:>7.4f}s\")\nprint(f\"đỉnh ở ĐẦU : {t_dau:>7.4f}s   chậm hơn {t_dau/t_cuoi:.0f} lần\")\n```\n\n```\nđỉnh ở CUỐI:  ~0.010s\nđỉnh ở ĐẦU :  ~1.500s   chậm hơn ~150 lần\n\n⇒ Cùng một cấu trúc `list`, chỉ khác CHỌN ĐẦU NÀO LÀM ĐỈNH:\n  O(1) so với O(n). (Bài mảng động giải thích vì sao.)\n```\n\n### Ứng dụng ①: kiểm tra ngoặc cân bằng\n\n```python\nCAP_NGOAC = {\")\": \"(\", \"]\": \"[\", \"}\": \"{\"}\nMO = set(CAP_NGOAC.values())\n\n\ndef ngoac_can_bang(s: str) -> bool:\n    \"\"\"Kiểm tra ngoặc lồng nhau đúng. O(n) thời gian, O(n) không gian.\"\"\"\n    stack: list[str] = []\n    for ky_tu in s:\n        if ky_tu in MO:\n            stack.append(ky_tu)\n        elif ky_tu in CAP_NGOAC:\n            if not stack:\n                return False                     # ⚠️ THỪA đóng\n            if stack.pop() != CAP_NGOAC[ky_tu]:\n                return False                     # ⚠️ SAI khớp\n    return not stack                             # ⚠️ còn dư = THIẾU đóng\n\n\n# Ba kiểu sai — ba ca kiểm thử bắt buộc\nassert ngoac_can_bang(\"()\") is True\nassert ngoac_can_bang(\"([{}])\") is True\nassert ngoac_can_bang(\"(]\") is False             # sai khớp\nassert ngoac_can_bang(\"((\") is False             # thiếu đóng\nassert ngoac_can_bang(\"))\") is False             # thừa đóng\nassert ngoac_can_bang(\"\") is True                # ⭐ biên: chuỗi rỗng\nassert ngoac_can_bang(\"abc\") is True             # ⭐ không có ngoặc nào\nassert ngoac_can_bang(\"a(b[c]d)e\") is True       # ⭐ lẫn ký tự khác\nassert ngoac_can_bang(\"([)]\") is False           # ⭐ lồng SAI THỨ TỰ\n```\n\nCa cuối đáng chú ý: `([)]` có đúng số ngoặc mở và đóng của mỗi loại, nhưng **lồng sai thứ tự**. Chỉ stack mới bắt được lỗi này — đếm số lượng thì không.\n\nPhiên bản báo lỗi **chi tiết** — nối về bài xử lý lỗi ở module 8 mảng 01:\n\n```python\nfrom dataclasses import dataclass\n\n\n@dataclass(frozen=True)\nclass LoiNgoac:\n    vi_tri: int\n    thong_bao: str\n\n\ndef kiem_ngoac(s: str) -> LoiNgoac | None:\n    \"\"\"Trả None nếu hợp lệ, hoặc lỗi ĐẦU TIÊN kèm vị trí.\"\"\"\n    stack: list[tuple[str, int]] = []      # ⭐ lưu cả VỊ TRÍ để báo lỗi\n    for i, ky_tu in enumerate(s):\n        if ky_tu in MO:\n            stack.append((ky_tu, i))\n        elif ky_tu in CAP_NGOAC:\n            if not stack:\n                return LoiNgoac(i, f\"thừa '{ky_tu}' — không có ngoặc mở\")\n            mo, vi_tri_mo = stack.pop()\n            if mo != CAP_NGOAC[ky_tu]:\n                return LoiNgoac(\n                    i, f\"'{ky_tu}' không khớp với '{mo}' mở ở vị trí {vi_tri_mo}\"\n                )\n    if stack:\n        mo, vi_tri_mo = stack[-1]\n        return LoiNgoac(vi_tri_mo, f\"'{mo}' chưa được đóng\")\n    return None\n\n\nprint(kiem_ngoac(\"([)]\"))\n# LoiNgoac(vi_tri=2, thong_bao=\"')' không khớp với '[' mở ở vị trí 1\")\n\nprint(kiem_ngoac(\"(a[b\"))\n# LoiNgoac(vi_tri=2, thong_bao=\"'[' chưa được đóng\")\n\nassert kiem_ngoac(\"([{}])\") is None\n```\n\nMẹo lưu **cả vị trí** vào stack là kỹ thuật chung cho mọi trình phân tích cú pháp: nó biến thông báo lỗi từ *\"cú pháp sai\"* thành *\"dòng 12 cột 5, ngoặc mở ở dòng 8 chưa được đóng\"*.\n\n### Ứng dụng ②: tính biểu thức hậu tố\n\n```python\nimport operator\n\nPHEP_TINH = {\n    \"+\": operator.add,\n    \"-\": operator.sub,\n    \"*\": operator.mul,\n    \"/\": operator.truediv,\n    \"^\": operator.pow,\n}\n\n\ndef tinh_hau_to(bieu_thuc: str) -> float:\n    \"\"\"Tính biểu thức hậu tố. O(n) thời gian, O(n) không gian.\n\n    Raises: ValueError nếu biểu thức sai cú pháp.\n            ZeroDivisionError nếu chia cho 0.\n    \"\"\"\n    stack: list[float] = []\n    for token in bieu_thuc.split():\n        if token in PHEP_TINH:\n            if len(stack) < 2:\n                raise ValueError(f\"thiếu toán hạng cho '{token}'\")\n            phai = stack.pop()             # ⭐ pop LẦN 1 là toán hạng PHẢI\n            trai = stack.pop()             # ⭐ pop LẦN 2 là toán hạng TRÁI\n            stack.append(PHEP_TINH[token](trai, phai))\n        else:\n            try:\n                stack.append(float(token))\n            except ValueError:\n                raise ValueError(f\"token không hợp lệ: {token!r}\") from None\n\n    if len(stack) != 1:\n        raise ValueError(f\"biểu thức sai: còn {len(stack)} giá trị trên stack\")\n    return stack[0]\n\n\nassert tinh_hau_to(\"3 4 +\") == 7\nassert tinh_hau_to(\"3 4 2 * +\") == 11              # 3 + (4×2)\nassert tinh_hau_to(\"3 4 + 2 *\") == 14             # (3+4) × 2\nassert tinh_hau_to(\"10 3 -\") == 7                 # ⭐ KHÔNG phải −7\nassert tinh_hau_to(\"2 3 ^\") == 8\nassert tinh_hau_to(\"5\") == 5                       # ⭐ biên: một số\n\n# ---- Ca biên và lỗi ----\nfor sai, mo_ta in ((\"3 +\", \"thiếu toán hạng\"),\n                   (\"3 4\", \"thừa toán hạng\"),\n                   (\"\", \"rỗng\"),\n                   (\"3 x +\", \"token lạ\")):\n    try:\n        tinh_hau_to(sai)\n        print(f\"⚠️ {mo_ta}: KHÔNG báo lỗi — sai!\")\n    except ValueError as e:\n        print(f\"✓ {mo_ta}: {e}\")\n\ntry:\n    tinh_hau_to(\"1 0 /\")\nexcept ZeroDivisionError:\n    print(\"✓ chia cho 0: để lỗi bay lên, không nuốt\")\n```\n\nChú ý thứ tự pop — đây là lỗi phổ biến nhất của bài toán này:\n\n```python\n# ❌ SAI — đảo ngược toán hạng\ntrai = stack.pop()      # thật ra đây là PHẢI\nphai = stack.pop()\n# \"10 3 -\" → 3 − 10 = −7   ⚠️ SAI\n\n# ✓ ĐÚNG\nphai = stack.pop()      # ⭐ ra trước là PHẢI\ntrai = stack.pop()\n# \"10 3 -\" → 10 − 3 = 7    ⭐ ĐÚNG\n```\n\n```\n⚠️ Với '+' và '*' thì đảo không sao (giao hoán) — nên bug này\n   ẨN cho tới khi gặp '-' hoặc '/'.\n\n⇒ Đây là lý do phải có ca kiểm thử với phép KHÔNG giao hoán.\n```\n\n### Ứng dụng ③: chuyển trung tố sang hậu tố\n\n```python\nUU_TIEN = {\"+\": 1, \"-\": 1, \"*\": 2, \"/\": 2, \"^\": 3}\nKET_HOP_PHAI = {\"^\"}                     # ⭐ 2^3^2 = 2^(3^2), không phải (2^3)^2\n\n\ndef sang_hau_to(bieu_thuc: str) -> str:\n    \"\"\"Thuật toán sân ga (shunting-yard). O(n) thời gian, O(n) không gian.\n\n    Raises: ValueError nếu ngoặc không cân bằng.\n    \"\"\"\n    ra: list[str] = []\n    toan_tu: list[str] = []\n\n    for token in bieu_thuc.split():\n        if token in UU_TIEN:\n            while (toan_tu and toan_tu[-1] in UU_TIEN\n                   and (UU_TIEN[toan_tu[-1]] > UU_TIEN[token]\n                        or (UU_TIEN[toan_tu[-1]] == UU_TIEN[token]\n                            and token not in KET_HOP_PHAI))):\n                ra.append(toan_tu.pop())          # ⭐ toán tử ưu tiên cao ra trước\n            toan_tu.append(token)\n        elif token == \"(\":\n            toan_tu.append(token)\n        elif token == \")\":\n            while toan_tu and toan_tu[-1] != \"(\":\n                ra.append(toan_tu.pop())\n            if not toan_tu:\n                raise ValueError(\"thừa dấu ')'\")\n            toan_tu.pop()                          # bỏ '('\n        else:\n            ra.append(token)                        # số → xuất ngay\n\n    while toan_tu:\n        if toan_tu[-1] == \"(\":\n            raise ValueError(\"thiếu dấu ')'\")\n        ra.append(toan_tu.pop())\n\n    return \" \".join(ra)\n\n\nassert sang_hau_to(\"3 + 4\") == \"3 4 +\"\nassert sang_hau_to(\"3 + 4 * 2\") == \"3 4 2 * +\"           # ⭐ × ưu tiên hơn +\nassert sang_hau_to(\"( 3 + 4 ) * 2\") == \"3 4 + 2 *\"       # ⭐ ngoặc đổi thứ tự\nassert sang_hau_to(\"2 ^ 3 ^ 2\") == \"2 3 2 ^ ^\"           # ⭐ kết hợp PHẢI\nassert sang_hau_to(\"5\") == \"5\"                            # biên\n\nfor sai in (\"( 3 + 4\", \"3 + 4 )\"):\n    try:\n        sang_hau_to(sai)\n        print(f\"⚠️ {sai!r}: không báo lỗi — sai!\")\n    except ValueError as e:\n        print(f\"✓ {sai!r}: {e}\")\n\n# Nối hai bước: trung tố → hậu tố → kết quả\nfor bt, mong_doi in ((\"3 + 4 * 2\", 11),\n                     (\"( 3 + 4 ) * 2\", 14),\n                     (\"2 ^ 3 ^ 2\", 512),\n                     (\"10 - 3 - 2\", 5)):        # ⭐ kết hợp TRÁI\n    kq = tinh_hau_to(sang_hau_to(bt))\n    assert kq == mong_doi, f\"{bt} → {kq}, mong đợi {mong_doi}\"\n    print(f\"{bt:>16}  →  {sang_hau_to(bt):>16}  =  {kq}\")\n```\n\n```\n       3 + 4 * 2  →        3 4 2 * +  =  11.0\n   ( 3 + 4 ) * 2  →        3 4 + 2 *  =  14.0\n       2 ^ 3 ^ 2  →        2 3 2 ^ ^  =  512.0\n      10 - 3 - 2  →        10 3 - 2 -  =  5.0\n```\n\nHai ca cuối kiểm đúng thứ tự kết hợp:\n\n```\n2 ^ 3 ^ 2  kết hợp PHẢI  → 2^(3^2) = 2^9 = 512   ⭐ không phải (2^3)^2 = 64\n10 - 3 - 2 kết hợp TRÁI  → (10−3)−2 = 5          ⭐ không phải 10−(3−2) = 9\n```\n\nĐây là **hai đoạn code thực sự nằm trong mọi trình biên dịch** — nối trực tiếp về bài *trình biên dịch làm gì* ở module 1 mảng 01.\n\n### Ứng dụng ④: undo / redo\n\n```python\nfrom dataclasses import dataclass\nfrom typing import Protocol\n\n\nclass ThaoTac(Protocol):\n    \"\"\"Mỗi thao tác phải biết cách LÀM và cách HOÀN TÁC chính nó.\"\"\"\n    def lam(self, van_ban: str) -> str: ...\n    def hoan_tac(self, van_ban: str) -> str: ...\n\n\n@dataclass(frozen=True)\nclass ChenChu:\n    vi_tri: int\n    noi_dung: str\n\n    def lam(self, van_ban: str) -> str:\n        return van_ban[:self.vi_tri] + self.noi_dung + van_ban[self.vi_tri:]\n\n    def hoan_tac(self, van_ban: str) -> str:\n        return (van_ban[:self.vi_tri]\n                + van_ban[self.vi_tri + len(self.noi_dung):])\n\n\n@dataclass(frozen=True)\nclass XoaChu:\n    vi_tri: int\n    noi_dung: str                          # ⭐ phải LƯU nội dung đã xoá\n\n    def lam(self, van_ban: str) -> str:\n        return (van_ban[:self.vi_tri]\n                + van_ban[self.vi_tri + len(self.noi_dung):])\n\n    def hoan_tac(self, van_ban: str) -> str:\n        return van_ban[:self.vi_tri] + self.noi_dung + van_ban[self.vi_tri:]\n\n\nclass SoanThao:\n    \"\"\"Trình soạn thảo với undo/redo bằng hai stack đối xứng.\"\"\"\n\n    def __init__(self) -> None:\n        self.van_ban = \"\"\n        self._undo: list[ThaoTac] = []\n        self._redo: list[ThaoTac] = []\n\n    def thuc_hien(self, tt: ThaoTac) -> None:\n        self.van_ban = tt.lam(self.van_ban)\n        self._undo.append(tt)\n        self._redo.clear()                 # ⭐⭐ THAO TÁC MỚI XOÁ SẠCH REDO\n\n    def undo(self) -> bool:\n        if not self._undo:\n            return False\n        tt = self._undo.pop()\n        self.van_ban = tt.hoan_tac(self.van_ban)\n        self._redo.append(tt)\n        return True\n\n    def redo(self) -> bool:\n        if not self._redo:\n            return False\n        tt = self._redo.pop()\n        self.van_ban = tt.lam(self.van_ban)\n        self._undo.append(tt)\n        return True\n```\n\n```python\ne = SoanThao()\ne.thuc_hien(ChenChu(0, \"Xin chào\"))\nassert e.van_ban == \"Xin chào\"\n\ne.thuc_hien(ChenChu(8, \" các bạn\"))\nassert e.van_ban == \"Xin chào các bạn\"\n\nassert e.undo() is True\nassert e.van_ban == \"Xin chào\"\n\nassert e.redo() is True\nassert e.van_ban == \"Xin chào các bạn\"\n\n# Xoá rồi hoàn tác\ne.thuc_hien(XoaChu(0, \"Xin \"))\nassert e.van_ban == \"chào các bạn\"\ne.undo()\nassert e.van_ban == \"Xin chào các bạn\"\n\n# ---- ⭐ Quy tắc quan trọng nhất: thao tác mới XOÁ redo ----\ne2 = SoanThao()\ne2.thuc_hien(ChenChu(0, \"A\"))\ne2.thuc_hien(ChenChu(1, \"B\"))\ne2.undo()                                  # bỏ B\nassert e2.van_ban == \"A\"\ne2.thuc_hien(ChenChu(1, \"C\"))              # ⭐ thao tác MỚI\nassert e2.van_ban == \"AC\"\nassert e2.redo() is False                  # ⭐ redo B ĐÃ BỊ XOÁ\n\n# ---- Ca biên ----\ne3 = SoanThao()\nassert e3.undo() is False                  # undo khi chưa có gì\nassert e3.redo() is False\n\nprint(\"undo/redo: mọi ca kiểm thử đạt\")\n```\n\n```\n⭐ VÌ SAO PHẢI XOÁ REDO:\n\n  Viết A → viết B → undo (bỏ B) → viết C\n\n  Lịch sử đã RẼ NHÁNH: B và C là hai tương lai KHÁC NHAU.\n  \"Redo B\" giờ sẽ chèn B vào một văn bản đã có C\n  ⇒ trạng thái không nhất quán.\n\n  Mọi trình soạn thảo đều làm đúng như vậy.\n```\n\nChú ý `XoaChu` phải **lưu nội dung đã xoá** — nếu không thì không hoàn tác được. Đây là nguyên tắc chung: **mỗi thao tác phải mang đủ thông tin để tự đảo ngược.**\n\n### Ứng dụng ⑤: khử đệ quy\n\n```python\nimport sys\n\nclass NutCay:\n    __slots__ = (\"gia_tri\", \"trai\", \"phai\")\n\n    def __init__(self, gia_tri, trai=None, phai=None):\n        self.gia_tri, self.trai, self.phai = gia_tri, trai, phai\n\n\n# ── ĐỆ QUY: ngắn gọn nhưng giới hạn ~1000 tầng ──\ndef duyet_de_quy(nut: NutCay | None) -> list:\n    if nut is None:\n        return []\n    return ([nut.gia_tri]\n            + duyet_de_quy(nut.trai)\n            + duyet_de_quy(nut.phai))\n\n\n# ── STACK TƯỜNG MINH: không giới hạn độ sâu ──\ndef duyet_stack(goc: NutCay | None) -> list:\n    \"\"\"Duyệt preorder bằng stack. O(n) thời gian, O(chiều cao) không gian.\"\"\"\n    if goc is None:\n        return []\n    kq: list = []\n    stack: list[NutCay] = [goc]\n    while stack:\n        nut = stack.pop()\n        kq.append(nut.gia_tri)\n        if nut.phai:                       # ⭐ PUSH PHẢI TRƯỚC\n            stack.append(nut.phai)\n        if nut.trai:                       # ⭐ để TRÁI được pop ra trước\n            stack.append(nut.trai)\n    return kq\n```\n\n```python\n#       1\n#      ╱ ╲\n#     2   3\n#    ╱ ╲\n#   4   5\ncay = NutCay(1, NutCay(2, NutCay(4), NutCay(5)), NutCay(3))\n\nassert duyet_de_quy(cay) == [1, 2, 4, 5, 3]\nassert duyet_stack(cay) == [1, 2, 4, 5, 3]      # ⭐ CÙNG kết quả\n\nassert duyet_stack(None) == []                   # biên: cây rỗng\nassert duyet_stack(NutCay(9)) == [9]             # biên: một nút\n```\n\nChứng minh lợi ích thật — cây suy biến sâu 5.000 tầng:\n\n```python\n# Dựng cây suy biến (mỗi nút chỉ có con trái) — như một danh sách\ngoc = NutCay(0)\nnut = goc\nfor i in range(1, 5_000):\n    nut.trai = NutCay(i)\n    nut = nut.trai\n\nprint(f\"giới hạn đệ quy: {sys.getrecursionlimit()}\")\n\ntry:\n    duyet_de_quy(goc)\n    print(\"đệ quy: chạy được\")\nexcept RecursionError:\n    print(\"⚠️ đệ quy: RecursionError\")\n\nkq = duyet_stack(goc)\nprint(f\"⭐ stack tường minh: chạy được, {len(kq):,} nút\")\n```\n\n```\ngiới hạn đệ quy: 1000\n⚠️ đệ quy: RecursionError\n⭐ stack tường minh: chạy được, 5,000 nút\n\n⇒ CÙNG thuật toán, CÙNG O(n). Khác nhau ở stack nằm ĐÂU:\n    đệ quy       → ngăn xếp lời gọi (giới hạn cứng ~1000)\n    stack riêng  → HEAP (chỉ giới hạn bởi RAM)\n\n⇒ Đây là cách chữa RecursionError ĐÚNG — không phải nâng\n  setrecursionlimit (có thể crash cứng, xem module 1).\n```\n\n⚠️ Chi tiết **thứ tự push** là chỗ sai phổ biến nhất:\n\n```python\n# ❌ Push TRÁI trước → pop ra PHẢI trước → sai thứ tự\nif nut.trai: stack.append(nut.trai)\nif nut.phai: stack.append(nut.phai)\n# → [1, 3, 2, 5, 4]   ⚠️ không phải preorder\n\n# ✓ Push PHẢI trước → pop ra TRÁI trước → đúng preorder\nif nut.phai: stack.append(nut.phai)\nif nut.trai: stack.append(nut.trai)\n# → [1, 2, 4, 5, 3]   ⭐ đúng\n```\n\nNguyên tắc chung khi khử đệ quy: **stack là LIFO, nên push theo thứ tự NGƯỢC với thứ tự muốn xử lý.**\n\n### Ứng dụng ⑥: quay lui (backtracking)\n\nStack tự nhiên xuất hiện khi cần \"thử rồi lùi lại\":\n\n```python\ndef tim_duong(me_cung: list[list[str]],\n              bat_dau: tuple[int, int],\n              dich: tuple[int, int]) -> list[tuple[int, int]] | None:\n    \"\"\"Tìm một đường đi trong mê cung bằng DFS với stack. O(số ô).\n\n    me_cung: '.' đi được, '#' tường.\n    \"\"\"\n    cao, rong = len(me_cung), len(me_cung[0])\n    da_tham = {bat_dau}\n    # stack chứa: (ô hiện tại, đường đi tới nó)\n    stack: list[tuple[tuple[int, int], list]] = [(bat_dau, [bat_dau])]\n\n    while stack:\n        (h, c), duong = stack.pop()\n        if (h, c) == dich:\n            return duong\n        for dh, dc in ((-1, 0), (1, 0), (0, -1), (0, 1)):\n            nh, nc = h + dh, c + dc\n            if (0 <= nh < cao and 0 <= nc < rong\n                    and me_cung[nh][nc] != \"#\"\n                    and (nh, nc) not in da_tham):\n                da_tham.add((nh, nc))       # ⭐ đánh dấu khi PUSH, không khi pop\n                stack.append(((nh, nc), duong + [(nh, nc)]))\n    return None\n\n\nme_cung = [\n    list(\".#...\"),\n    list(\".#.#.\"),\n    list(\"...#.\"),\n]\nduong = tim_duong(me_cung, (0, 0), (2, 4))\nprint(f\"tìm thấy đường dài {len(duong)} ô\" if duong else \"không có đường\")\n\n# Ca biên\nassert tim_duong([list(\".\")], (0, 0), (0, 0)) == [(0, 0)]      # trùng nhau\nassert tim_duong([list(\".#\"), list(\"#.\")], (0, 0), (1, 1)) is None  # bị chặn\n```\n\n```\n⭐ Đánh dấu đã thăm khi PUSH, không khi POP.\n  Nếu đánh dấu khi pop, cùng một ô có thể bị push nhiều lần\n  ⇒ stack phình to và có thể lặp.\n\n⭐ Đổi stack.pop() thành hàng đợi popleft() → thành BFS,\n  và BFS cho đường đi NGẮN NHẤT. Bài queue tiếp theo sẽ nói.\n```\n\nĐiểm cuối là mối nối quan trọng: **DFS và BFS khác nhau đúng một chi tiết — lấy từ đầu nào của cấu trúc.**\n\n### Ngăn xếp lời gọi — quan sát trực tiếp\n\n```python\nimport traceback\n\n\ndef a():\n    b()\n\ndef b():\n    c()\n\ndef c():\n    print(\"NGĂN XẾP LỜI GỌI hiện tại (từ đáy lên đỉnh):\")\n    for khung in traceback.extract_stack()[:-1]:\n        print(f\"   {khung.name}()  dòng {khung.lineno}\")\n    print(f\"\\nĐỘ SÂU: {len(traceback.extract_stack())}\")\n\n\na()\n```\n\n```\nNGĂN XẾP LỜI GỌI hiện tại (từ đáy lên đỉnh):\n   <module>()  dòng ...\n   a()  dòng ...\n   b()  dòng ...\n\nĐỘ SÂU: 4\n\n⭐ Đây chính là STACK — và traceback in nó ra từ ĐÁY lên ĐỈNH.\n  Vì vậy khi đọc traceback: DÒNG CUỐI là nơi lỗi thật sự xảy ra.\n```\n\n### Khi nào stack là lựa chọn SAI\n\n```python\nfrom collections import deque\n\n# ❌ Dùng stack cho hàng đợi công việc → xử lý SAI THỨ TỰ\ncong_viec_stack: list[str] = []\ncong_viec_stack.append(\"việc 1\")\ncong_viec_stack.append(\"việc 2\")\ncong_viec_stack.append(\"việc 3\")\nprint(\"stack (LIFO):\", [cong_viec_stack.pop() for _ in range(3)])\n# ['việc 3', 'việc 2', 'việc 1']   ⚠️ việc tới TRƯỚC lại làm SAU CÙNG\n\n# ✓ Hàng đợi cho đúng thứ tự đến trước làm trước\ncong_viec_queue: deque[str] = deque([\"việc 1\", \"việc 2\", \"việc 3\"])\nprint(\"queue (FIFO):\", [cong_viec_queue.popleft() for _ in range(3)])\n# ['việc 1', 'việc 2', 'việc 3']   ⭐ đúng thứ tự\n```\n\n```\nSTACK ĐÚNG                        STACK SAI\n─────────────────────────         ────────────────────────\ncấu trúc lồng nhau                hàng đợi công việc\nquay lui / backtracking           tìm đường NGẮN NHẤT (cần BFS)\nkhử đệ quy                        cần lấy phần tử nhỏ nhất (cần heap)\nundo / redo                       cần truy cập giữa (cần list)\nDFS                               xử lý theo thứ tự đến\n```",

so_sanh: "### Ba thao tác của stack\n\n| Thao tác | Việc | Bậc |\n|---|---|---|\n| `push(x)` | thêm lên đỉnh | **O(1)** |\n| `pop()` | lấy **và bỏ** đỉnh | **O(1)** |\n| `peek()` | **xem** đỉnh, không bỏ | O(1) |\n\nStack cố tình **không** cho truy cập giữa — ràng buộc đó chính là thứ giữ mọi thao tác O(1).\n\n### Hai cách cài đặt\n\n| | Mảng (`list`) | Danh sách liên kết |\n|---|---|---|\n| Đỉnh đặt ở | **cuối** | **đầu** |\n| `push`/`pop` | O(1) | O(1) |\n| Bộ nhớ | **⭐ ít** | nhiều (một object/nút) |\n| Cache locality | **⭐ tốt** | tệ |\n| Trong Python | **⭐ nên dùng** | chỉ để học |\n\n⚠️ Đặt đỉnh ở **đầu** mảng thì `push`/`pop` thành **O(n)** — đo được là chậm hơn khoảng 150 lần.\n\n### Bốn mẫu bài toán dùng stack\n\n| Mẫu | Dấu hiệu nhận ra | Ví dụ |\n|---|---|---|\n| **Cấu trúc lồng nhau** | \"mở gần nhất khớp đóng đầu tiên\" | ngoặc, thẻ HTML, JSON |\n| **Tính biểu thức** | cần nhớ toán hạng/toán tử đang chờ | hậu tố, shunting-yard |\n| **Undo/redo** | cần quay về trạng thái trước | trình soạn thảo |\n| **Khử đệ quy** | đệ quy quá sâu | duyệt cây, DFS |\n\n### Ba kiểu sai của bài toán ngoặc\n\n| Đầu vào | Lỗi | Stack phát hiện thế nào |\n|---|---|---|\n| `\"( ]\"` | sai khớp | pop ra `(` nhưng cần `[` |\n| `\"( ( )\"` | thiếu đóng | hết chuỗi mà stack **còn dư** |\n| `\") (\"` | thừa đóng | gặp `)` khi stack **rỗng** |\n| `\"([)]\"` | **lồng sai thứ tự** | ⭐ chỉ stack bắt được — đếm số lượng thì không |\n\n### Trung tố và hậu tố\n\n| | Trung tố | Hậu tố |\n|---|---|---|\n| Ví dụ | `(3 + 4) × 2` | `3 4 + 2 ×` |\n| Cần dấu ngoặc | **✓** | **✗** |\n| Cần quy tắc ưu tiên | **✓** | **✗** |\n| Người đọc | dễ | khó |\n| Máy tính | cần chuyển đổi | **⭐ tính thẳng bằng stack** |\n\n### Thứ tự pop — lỗi phổ biến nhất\n\n| | Pop lần 1 | Pop lần 2 |\n|---|---|---|\n| Là toán hạng | **PHẢI** | **TRÁI** |\n| `\"10 3 -\"` đúng | `3` | `10` → **10 − 3 = 7** |\n| Nếu đảo | | → 3 − 10 = **−7** ⚠️ |\n\n⚠️ Với `+` và `×` (giao hoán) thì đảo **không lộ ra** — phải có ca kiểm thử với `-` hoặc `/`.\n\n### Undo/redo — quy tắc then chốt\n\n| Hành động | undo stack | redo stack |\n|---|---|---|\n| Làm việc mới | push | **⭐ XOÁ SẠCH** |\n| Undo | pop | push |\n| Redo | push | pop |\n\nXoá redo là bắt buộc vì lịch sử đã **rẽ nhánh** — redo cái cũ sẽ tạo trạng thái không nhất quán.\n\n### Đệ quy và stack tường minh\n\n| | Đệ quy | Stack tường minh |\n|---|---|---|\n| Thời gian | O(n) | O(n) |\n| Không gian | O(độ sâu) | O(độ sâu) |\n| Stack nằm ở | **ngăn xếp lời gọi** | **heap** |\n| Giới hạn | **⚠️ ~1000 tầng** | chỉ bởi RAM |\n| Độ dài code | **⭐ ngắn** | dài hơn |\n| Cách chữa `RecursionError` | ✗ | **⭐ đúng cách** |\n\n### Thứ tự push khi khử đệ quy\n\n| Muốn xử lý trước | Phải push trước |\n|---|---|\n| con **trái** | con **phải** |\n| con **phải** | con **trái** |\n\nStack là LIFO ⇒ **push theo thứ tự NGƯỢC với thứ tự muốn xử lý.**\n\n### Stack đúng và sai\n\n| Stack đúng | Stack sai — dùng gì |\n|---|---|\n| cấu trúc lồng nhau | hàng đợi công việc → **queue** |\n| quay lui, backtracking | tìm đường **ngắn nhất** → **BFS/queue** |\n| khử đệ quy | cần phần tử **nhỏ nhất** → **heap** |\n| undo/redo | cần truy cập **giữa** → **list** |\n| **DFS** | xử lý theo **thứ tự đến** → queue |\n\nDFS và BFS khác nhau **đúng một chi tiết**: lấy từ đầu nào của cấu trúc.",

loi_thuong_gap: [
  "**Đặt đỉnh stack ở ĐẦU mảng.** `insert(0)`/`pop(0)` là O(n) — chậm hơn khoảng 150 lần so với dùng cuối mảng.",
  "**Đảo thứ tự pop khi tính biểu thức.** Pop lần 1 là toán hạng PHẢI; nhầm thì `10 3 -` ra −7.",
  "**Chỉ test phép giao hoán.** Lỗi đảo toán hạng ẩn hoàn toàn với `+` và `×`; phải test `-` và `/`.",
  "**Quên kiểm stack rỗng trước khi pop.** Gặp ngoặc đóng thừa hoặc thiếu toán hạng là `IndexError` thay vì thông báo rõ ràng.",
  "**Quên kiểm stack còn dư ở cuối.** `\"((\"` có mọi bước hợp lệ nhưng vẫn sai — phải kiểm `not stack` khi kết thúc.",
  "**Đếm số ngoặc thay vì dùng stack.** `\"([)]\"` có đúng số lượng mỗi loại nhưng lồng sai thứ tự.",
  "**Quên xoá redo stack khi có thao tác mới.** Lịch sử rẽ nhánh, redo cái cũ tạo trạng thái không nhất quán.",
  "**Thao tác undo không lưu đủ thông tin để đảo ngược.** `XoaChu` phải lưu **nội dung đã xoá**, không chỉ vị trí.",
  "**Push sai thứ tự khi khử đệ quy.** Stack là LIFO — muốn xử lý trái trước thì push phải trước.",
  "**Nâng `setrecursionlimit` để chữa `RecursionError`.** Có thể crash cứng; dùng stack tường minh.",
  "**Đánh dấu đã thăm khi POP thay vì khi PUSH** trong DFS. Cùng một ô bị push nhiều lần, stack phình to.",
  "**Dùng stack cho hàng đợi công việc.** LIFO làm việc đến trước bị xử lý sau cùng.",
  "**Dùng DFS để tìm đường ngắn nhất.** DFS tìm *một* đường, không phải đường ngắn nhất — cần BFS.",
  "**Phơi cả giao diện `list` khi cần ràng buộc LIFO.** Người dùng có thể `insert`, `sort`, truy cập giữa; bọc lại nếu cần ép ràng buộc.",
  "**Đọc traceback từ trên xuống.** Dòng **cuối** là đỉnh stack — nơi lỗi thật sự xảy ra."
],

tu_kiem_tra: [
  "LIFO nghĩa là gì? Nêu ba thao tác của stack và bậc của mỗi cái.",
  "Vì sao stack cố tình không cho truy cập phần tử giữa? Ràng buộc đó mang lại lợi ích gì?",
  "Trong mảng thì đỉnh đặt ở đâu, trong linked list thì ở đâu? Vì sao ngược nhau?",
  "Đặt đỉnh ở đầu mảng thì chuyện gì xảy ra? Giải thích bằng cấu trúc bộ nhớ của mảng.",
  "Nêu dấu hiệu nhận ra một bài toán cần stack. Cho ba ví dụ khác nhau.",
  "Nêu ba kiểu sai của bài toán ngoặc và cách stack phát hiện mỗi kiểu.",
  "Vì sao `\"([)]\"` không phát hiện được bằng cách đếm số ngoặc?",
  "Ký pháp hậu tố bỏ được hai thứ gì so với trung tố?",
  "Trình bày thuật toán tính biểu thức hậu tố bằng stack.",
  "Pop lần 1 và lần 2 tương ứng toán hạng nào? Vì sao lỗi này hay bị bỏ sót?",
  "Thuật toán sân ga hoạt động thế nào? Stack giữ cái gì trong đó?",
  "Kết hợp trái và kết hợp phải khác nhau thế nào? Cho ví dụ với `^` và `-`.",
  "Undo/redo dùng mấy stack? Nêu ba quy tắc chuyển đổi giữa chúng.",
  "Vì sao thao tác mới phải xoá sạch redo stack?",
  "Vì sao mỗi thao tác undo phải tự mang đủ thông tin để đảo ngược?",
  "Nêu bốn bước chuyển một hàm đệ quy thành vòng lặp với stack.",
  "Vì sao stack tường minh chữa được `RecursionError` mà đệ quy thì không?",
  "Quy tắc thứ tự push khi khử đệ quy là gì? Cho ví dụ với cây nhị phân.",
  "Trong DFS, vì sao phải đánh dấu đã thăm khi push chứ không khi pop?",
  "DFS và BFS khác nhau đúng ở chi tiết nào? Cái nào tìm được đường ngắn nhất?",
  "Vì sao traceback phải đọc từ dưới lên?"
],

tai_nguyen: [
  "Sách **Introduction to Algorithms** — CLRS. Chương 10 về stack và queue, trình bày ngắn gọn và chặt chẽ.",
  "Sách **Crafting Interpreters** — Robert Nystrom. Phần về máy ảo dựa trên stack; cho thấy stack thật sự là nền của việc thực thi chương trình.",
  "Sách **Algorithms** — Sedgewick & Wayne. Phần *Stacks and Queues* với hiện thực bằng cả mảng lẫn linked list, kèm phân tích.",
  "**Tài liệu Python — `traceback` và `sys.getrecursionlimit`** — công cụ quan sát ngăn xếp lời gọi dùng trong bài.",
  "**Tài liệu Python — `operator`** — các hàm `add`, `sub`, `mul` dùng trong bảng phép tính; sạch hơn viết `if/elif`."
],

lien_he: [
  "**Array và dynamic array** trong module này: vì sao đỉnh stack phải ở cuối mảng.",
  "**Linked list** ngay trước: cài stack bằng danh sách đơn là ứng dụng tự nhiên nhất của nó.",
  "**Queue FIFO** ngay sau: cấu trúc đối xứng với stack; đổi stack thành queue biến DFS thành BFS.",
  "**Độ phức tạp không gian** ở module 1 mảng này: ngăn xếp lời gọi là O(độ sâu); stack tường minh chuyển nó sang heap.",
  "**Stack và heap** ở module 2 mảng 01: ngăn xếp lời gọi của hệ điều hành chính là một stack.",
  "**Xử lý ngoại lệ** ở module 5 mảng 01: traceback là nội dung ngăn xếp lời gọi in ra.",
  "**Trình biên dịch làm gì** ở module 1 mảng 01: shunting-yard và tính hậu tố là hai đoạn code thật trong trình biên dịch.",
  "**Xử lý lỗi tường minh** ở module 8 mảng 01: lưu vị trí vào stack để báo lỗi cú pháp có ngữ cảnh.",
  "**Design pattern Command** ở module 6 mảng 01: mỗi thao tác undo là một Command tự biết cách đảo ngược.",
  "**Cây** ở module 4 mảng này: duyệt cây bằng stack, và ba thứ tự preorder/inorder/postorder.",
  "**Đồ thị** ở module 5 mảng này: DFS là stack, BFS là queue — cùng một khung, khác cấu trúc.",
  "**Kỹ thuật giải thuật cốt lõi** ở module 8 mảng này: backtracking là stack áp cho không gian lời giải."
]

};
