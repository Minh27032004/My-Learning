window.LESSONS["03:hoan-tac:git-restore-bo-sua-doi-bo-staging"] = {

tom_tat: [
  "`git restore` chép nội dung từ một nguồn Git sang working tree hoặc index; mặc định nó khôi phục working tree từ index, chứ không “quay thời gian” cho cả repository.",
  "`git restore src/cart.js` bỏ phần sửa chưa staging của file, còn `git restore --staged src/cart.js` gỡ file khỏi staging nhưng giữ nguyên nội dung đang sửa trên đĩa.",
  "Muốn bỏ cả phần đã staging lẫn phần sửa sau đó, phải xác định rõ nguồn và hai đích; cách dễ kiểm tra nhất là thao tác từng bước rồi đọc `git diff` và `git diff --staged`.",
  "Lệnh có thể làm mất thay đổi chưa commit. Trước khi chạy, cần phân biệt file đã được theo dõi với file chưa được theo dõi và lưu bản vá nếu nội dung còn giá trị.",
  "`restore` không di chuyển nhánh, không xóa commit và không thay thế `revert`; nó là dụng cụ sửa hai vùng cục bộ trong mô hình HEAD–index–working tree."
],

can_biet_truoc: [
  "Biết ba vùng của Git: commit mà `HEAD` đang trỏ tới, index (staging area) và working tree.",
  "Biết `git status`, `git diff` và `git diff --staged` trả lời ba câu hỏi khác nhau về trạng thái file.",
  "Biết tracked file là file đã nằm trong lịch sử hoặc index; untracked file chưa thuộc phạm vi quản lý nội dung của `git restore`.",
  "Biết pathspec là đối số chọn đường dẫn, chẳng hạn `src/cart.js` hoặc `:(top)src/*.js`."
],

dinh_nghia: "`git restore` là lệnh **chép phiên bản của một hoặc nhiều đường dẫn từ một cây nguồn sang working tree, index, hoặc cả hai**. Nguồn mặc định phụ thuộc vào đích: khi chỉ sửa working tree, nguồn mặc định là index; khi có `--staged`, nguồn mặc định là `HEAD`.\n\nVì vậy “restore là hoàn tác” chỉ là cách nói tắt. Cơ chế thật không ghi nhớ ý định của người dùng và cũng không tìm “bước trước”: Git lấy blob ở nguồn đã chọn rồi làm cho đích mang nội dung tương ứng. Commit và ref của nhánh không đổi.",

vi_sao_can: "Trong dự án Sao Mai, lập trình viên sửa `src/cart.js` để thêm mã giảm giá, chạy `git add src/cart.js`, rồi tiếp tục chèn các dòng `console.log`. Cùng lúc, `config/payment.json` bị đổi sang khóa sandbox. Có ba nhu cầu khác nhau: bỏ riêng log chưa staging; gỡ `cart.js` khỏi staging để chia commit; hoặc trả cấu hình về bản đã commit. Nếu chỉ nhớ một câu mơ hồ như “restore đưa file về cũ”, người dùng rất dễ xóa cả phần giảm giá muốn giữ.\n\nLệnh này tồn tại để diễn đạt rõ **đích cần sửa**. Working tree là nơi trình soạn thảo và chương trình đọc file; index là ảnh chụp sẽ trở thành commit kế tiếp. Hai nơi có thể chứa hai phiên bản khác nhau của cùng một đường dẫn. Một thao tác hoàn tác tốt phải nói rõ đang thay nơi nào và chép từ đâu.\n\n`git checkout -- file` trước đây gánh cả chuyển nhánh lẫn phục hồi file. Hai ý nghĩa không liên quan khiến một lệnh khó học và dễ đọc nhầm. `git restore` tách riêng thao tác phục hồi đường dẫn, còn `git switch` tách thao tác chuyển nhánh. Tuy nhiên tên lệnh dễ tạo cảm giác an toàn quá mức: thay đổi chưa commit bị ghi đè thường không có ref hay reflog để cứu.",

co_che: "### Bản đồ ba cây trước khi gõ lệnh\n\nHãy coi mỗi đường dẫn có thể có ba bản sao. `HEAD` chỉ commit hiện tại; index giữ nội dung dự kiến cho commit kế; working tree là file thật. Sau khi thêm giảm giá, staging, rồi thêm log, trạng thái `cart.js` là:\n\n| Vùng | Nội dung `src/cart.js` | Quan sát bằng |\n|---|---|---|\n| `HEAD` | giỏ hàng chưa có giảm giá | `git show HEAD:src/cart.js` |\n| index | giảm giá, chưa có log | `git show :src/cart.js` |\n| working tree | giảm giá và log | đọc file hoặc `git diff` |\n\n`git diff` so working tree với index nên chỉ hiện log. `git diff --staged` so index với `HEAD` nên chỉ hiện giảm giá. Đây không phải hai chế độ hiển thị tùy ý; chúng là hai cạnh của cùng tam giác trạng thái. Trước một lệnh có khả năng ghi đè, đọc cả hai diff cho biết phần nào sẽ biến mất và phần nào còn lại.\n\n### Trường hợp mặc định: index chép xuống working tree\n\nLệnh:\n\n```bash\ngit restore src/cart.js\n```\n\nkhông đụng đến index. Git giải pathspec, lấy blob của `cart.js` trong index, rồi ghi nội dung đó ra file. Trong ví dụ, các log biến mất nhưng phần giảm giá vẫn nằm cả trên đĩa lẫn trong staging. Sau lệnh, `git diff -- src/cart.js` rỗng; `git diff --staged -- src/cart.js` vẫn hiện thay đổi giảm giá.\n\nĐiểm quan trọng là nguồn mặc định **không nhất thiết là commit**. Nếu index đã chứa nội dung sai, restore mặc định sẽ chép đúng nội dung sai ấy xuống. Vì thế câu “khôi phục về commit gần nhất” không mô tả đúng lệnh này. Muốn chỉ định commit làm nguồn, dùng `--source`.\n\n### `--staged`: HEAD chép vào index\n\nLệnh:\n\n```bash\ngit restore --staged src/cart.js\n```\n\nđặt entry của đường dẫn trong index về phiên bản ở `HEAD`, nhưng không sửa file đang mở. Phần giảm giá vẫn nằm trong working tree và chuyển từ “changes to be committed” sang “changes not staged for commit”. Đây là thao tác phù hợp khi `git add` quá rộng hoặc muốn chia một thay đổi lớn thành nhiều commit.\n\nTại sao nguồn mặc định đổi sang `HEAD`? Index cần một mốc để gỡ staging. Chép `HEAD` vào index làm chênh lệch `HEAD → index` biến mất, trong khi chênh lệch `index → working tree` nhận toàn bộ sửa đổi. Lệnh cũ tương đương về ý định thường là `git reset HEAD -- src/cart.js`, nhưng `restore --staged` diễn đạt trực tiếp rằng ref không bị di chuyển.\n\n### Chỉ định nguồn bằng `--source`\n\nCó thể lấy nội dung từ commit bất kỳ:\n\n```bash\ngit restore --source=HEAD~1 -- src/cart.js\n```\n\nDấu `--` kết thúc phần tùy chọn và revision, giúp Git hiểu phần sau là đường dẫn. Nó đặc biệt đáng dùng khi tên file trông giống tùy chọn hoặc trùng tên nhánh. Lệnh trên chép bản `cart.js` từ commit cha vào working tree; index vẫn giữ nguyên. Kết quả là thay đổi mới xuất hiện trong `git diff`, sẵn sàng được xem xét và commit nếu đó là chủ ý.\n\n`--source` không làm nhánh quay về commit ấy. Nó chỉ dùng commit như kho chứa blob. Lịch sử vẫn giữ nguyên; nếu commit lại, bạn tạo một commit mới có nội dung file cũ. Đây là khác biệt bản chất với `git reset`, vốn có thể di chuyển ref hiện tại.\n\n### Sửa đồng thời index và working tree\n\nKhi chắc chắn muốn cho cả hai vùng giống một commit, viết đầy đủ:\n\n```bash\ngit restore --source=HEAD --staged --worktree -- src/cart.js\n```\n\nGit lấy blob ở `HEAD`, cập nhật entry trong index và ghi file. Cả thay đổi đã staging lẫn chưa staging của đường dẫn đều mất khỏi hai diff. Cú pháp dài là một hàng rào tốt: hành động mạnh phải lộ rõ cả nguồn lẫn hai đích.\n\nKhông nên biến lệnh này thành phản xạ dọn trạng thái. Nếu phần sửa có thể cần lại, hãy tạo bản vá trước: `git diff HEAD -- src/cart.js > cart-wip.patch` trong thư mục ngoài repository thực hành, hoặc tạo một commit WIP trên nhánh riêng. Stash cũng dùng được, nhưng phải hiểu nó tạo các commit nội bộ và có quy tắc riêng với file chưa được theo dõi.\n\n### Khôi phục từng đoạn bằng `--patch`\n\n`git restore --patch src/cart.js` cho phép duyệt từng hunk (cụm thay đổi liền nhau) thay vì cả file. Git dựng diff giữa nguồn và đích, rồi hỏi có áp dụng phần đảo ngược cho từng hunk hay không. Điều này hữu ích khi cùng file có log cần bỏ và sửa lỗi tính thuế cần giữ.\n\n```bash\ngit diff -- src/cart.js\ngit restore --patch -- src/cart.js\ngit diff -- src/cart.js\n```\n\nRanh giới hunk do ngữ cảnh diff quyết định, không theo ý nghĩa nghiệp vụ. Hai thay đổi đặt gần nhau có thể dính vào cùng một hunk. Trong giao diện tương tác có thể chia hunk khi Git cho phép; nếu vẫn không tách được, sửa tay rồi kiểm tra lại an toàn hơn việc chấp nhận mù quáng.\n\nVới `--staged --patch`, đích là index: bạn chọn từng hunk để gỡ khỏi staging nhưng working tree không đổi. Đây là công cụ tốt để tạo commit tập trung, miễn là sau đó chạy test trên đúng nội dung sẽ được commit; chương trình chạy từ working tree có thể bao gồm nhiều thay đổi hơn index.\n\n### Pathspec quyết định phạm vi thiệt hại\n\n`git restore .` áp dụng từ thư mục hiện tại trở xuống, không nhất thiết từ gốc repository. Chạy trong `src/` sẽ không sửa `config/payment.json`; chạy ở gốc sẽ bao phủ mọi tracked path khớp. Dấu chấm ngắn nhưng phạm vi rộng, vì thế cần xem `git status --short` và ưu tiên tên file cụ thể.\n\n```bash\ngit restore -- src/cart.js src/checkout.js\ngit restore -- ':(top)src/*.js'\n```\n\nNháy pathspec giúp shell không tự mở rộng ký tự `*` trước Git. Magic `:(top)` neo mẫu tại gốc repository. Với file đã đổi tên, restore không suy luận “ý định đổi tên”; Git nhìn entry cũ và mới theo pathspec. Hãy đọc `git status` để biết cần khôi phục đường dẫn nào.\n\n### File chưa được theo dõi và file bị bỏ qua\n\n`git restore` vận hành trên nội dung có trong nguồn Git. `debug.log` mới tạo, chưa `git add`, không có entry trong `HEAD` hay index nên lệnh không xóa nó. Đây là ca biên quan trọng: sau restore, `git status` vẫn báo `?? debug.log`.\n\nXử lý file chưa được theo dõi thuộc phạm vi `git clean`, bài sau của module. Không nên chuyển sang `git clean -fd` chỉ vì muốn màn hình status sạch; file untracked có thể là dữ liệu duy nhất chưa được Git bảo vệ. Luôn xem trước bằng chế độ dry-run. File bị `.gitignore` bỏ qua còn cần lựa chọn riêng và mức rủi ro cao hơn.\n\n### Xung đột và trạng thái chưa merge\n\nKhi merge tạo xung đột, index có thể giữ nhiều stage cho một đường dẫn: base, “ours” và “theirs”. `git restore --ours` hoặc `--theirs` có thể lấy một phía vào working tree trong một số trạng thái, nhưng tên phía dễ gây nhầm khi rebase vì vai trò được nhìn theo quá trình đang chạy.\n\nKhông dùng `restore` như cách kết thúc merge. Nó chỉ thay nội dung đường dẫn; trạng thái merge của repository và các file khác vẫn tồn tại. Sau khi chọn hoặc tự hòa giải nội dung, cần `git add` để đánh dấu đã giải quyết, rồi tiếp tục quy trình merge/rebase. Trước hết đọc `git status`, vì chính Git in ra lệnh tiếp tục hoặc hủy phù hợp với trạng thái hiện tại.\n\n### Khi nào không nên dùng\n\nKhông dùng `restore` để hủy commit đã chia sẻ: nó không tạo lịch sử giải thích việc đảo ngược và không tác động commit cũ. Trường hợp ấy cần `git revert`. Không dùng nó để di chuyển nhánh hay thay đổi tập commit: đó là phạm vi của `reset`, `switch` hoặc rebase tùy mục tiêu.\n\nCũng không dùng khi chưa xác định thay đổi có bản sao hay chưa. Nội dung working tree bị ghi đè có thể không xuất hiện trong reflog, bởi reflog ghi chuyển động của ref chứ không lưu mọi lần bấm Save. Nếu chỉ muốn xem bản cũ, dùng `git show HEAD:path` hoặc `git diff`; xem là thao tác đọc, restore là thao tác ghi.",

vi_du: "### Phòng thí nghiệm Sao Mai có thể chạy lại\n\nĐoạn shell sau tạo repository tạm, không phụ thuộc dự án hiện tại. Chạy trong Bash, Git Bash hoặc WSL:\n\n```bash\ntmp_dir=\"$(mktemp -d)\"\ncd \"$tmp_dir\"\ngit init -q\ngit config user.name \"Lesson User\"\ngit config user.email \"lesson@example.invalid\"\nmkdir -p src config\nprintf '%s\\n' 'export function total(x) { return x; }' > src/cart.js\nprintf '%s\\n' '{\"gateway\":\"production\"}' > config/payment.json\ngit add src/cart.js config/payment.json\ngit commit -qm \"Initialize Sao Mai cart\"\n\nprintf '%s\\n' 'export function total(x) { return x * 0.9; }' > src/cart.js\ngit add src/cart.js\nprintf '%s\\n' 'export function total(x) {' '  console.log(x);' '  return x * 0.9;' '}' > src/cart.js\nprintf '%s\\n' '{\"gateway\":\"sandbox\"}' > config/payment.json\nprintf '%s\\n' 'temporary trace' > debug.log\n\ngit status --short\ngit diff -- src/cart.js\ngit diff --staged -- src/cart.js\n```\n\nTrạng thái mong đợi gồm `MM src/cart.js`, ` M config/payment.json` và `?? debug.log`. Hai chữ `M` của `cart.js` thuộc hai cột: index khác `HEAD`, đồng thời working tree khác index.\n\n```bash\ngit restore -- src/cart.js\ngrep console src/cart.js || printf '%s\\n' 'log đã bị bỏ'\ngit diff --quiet -- src/cart.js\ngit diff --staged --quiet -- src/cart.js || printf '%s\\n' 'giảm giá vẫn staged'\n\ngit restore --staged -- src/cart.js\ngit diff --staged --quiet -- src/cart.js\ngit diff --quiet -- src/cart.js || printf '%s\\n' 'giảm giá vẫn ở working tree'\n```\n\nCác kiểm tra dùng exit code của `git diff --quiet`, không dựa vào lời kể. Cuối cùng, thử trả riêng cấu hình về `HEAD` và xác nhận file chưa được theo dõi còn nguyên:\n\n```bash\ngit restore --source=HEAD -- config/payment.json\ntest \"$(cat config/payment.json)\" = '{\"gateway\":\"production\"}'\ntest -f debug.log\ngit status --short\n```\n\nCa biên file rỗng vẫn hoạt động theo cùng cơ chế: nếu blob nguồn rỗng, đích trở thành file rỗng; restore không xóa đường dẫn chỉ vì kích thước bằng 0. Với symlink và executable bit, Git khôi phục những thuộc tính mà index theo dõi, nhưng Git không phải hệ thống sao lưu đầy đủ metadata của hệ điều hành.",

so_sanh: "### Chọn lệnh theo thứ cần thay đổi\n\n| Mục tiêu | Lệnh điển hình | Ref nhánh đổi? | Working tree đổi? | Tạo commit mới? |\n|---|---|---:|---:|---:|\n| Bỏ sửa chưa staging của file | `git restore -- file` | Không | Có | Không |\n| Gỡ file khỏi staging | `git restore --staged -- file` | Không | Không | Không |\n| Di chuyển nhánh và tùy chọn sửa index/cây làm việc | `git reset <mode> <commit>` | Có | Tùy mode | Không |\n| Đảo commit đã chia sẻ | `git revert <commit>` | Không | Có qua commit kết quả | Có |\n| Xem file ở commit cũ | `git show <commit>:<path>` | Không | Không | Không |\n\n### `restore` và `checkout -- path`\n\nHai lệnh có thể cho kết quả giống nhau trong trường hợp đơn giản, nhưng `restore` biểu đạt đích và nguồn rõ hơn. `checkout` còn mang nghĩa chuyển nhánh nên người đọc phải dựa vào cú pháp để đoán. Dù vậy, repository dùng Git cũ hoặc tài liệu lịch sử vẫn có thể dùng `checkout`; hiểu mô hình ba cây quan trọng hơn học thuộc tên mới.\n\n### Đánh đổi của thao tác theo đường dẫn\n\nƯu điểm của restore là phạm vi hẹp: sửa đúng file hoặc hunk, không viết lại lịch sử. Nhược điểm cũng từ phạm vi ấy: nó có thể tạo tổ hợp nội dung chưa từng được test, chẳng hạn lấy `cart.js` cũ nhưng giữ `checkout.js` mới. Khi các file phụ thuộc nhau, khôi phục một phần phải đi kèm test tích hợp, không chỉ thấy `git status` sạch.",

loi_thuong_gap: [
  "**Tưởng `git restore file` lấy từ `HEAD`.** Dấu hiệu là phần đã staging vẫn còn sau lệnh; nguyên nhân vì nguồn mặc định cho working tree là index. Dùng `git diff` và `git diff --staged` để nhận diện hai lớp thay đổi.",
  "**Gõ `git restore .` khi đang ở sai thư mục.** Dấu hiệu là nhiều file biến mất khỏi `git status` hơn dự kiến. Kiểm tra `git rev-parse --show-prefix` và liệt kê đường dẫn trước khi dùng pathspec rộng.",
  "**Dùng `--staged --worktree` như lệnh dọn rác.** Dấu hiệu là cả hai diff đều rỗng nhưng phần code có giá trị không còn trên đĩa. Tạo patch hoặc commit WIP trước nếu chưa chắc chắn.",
  "**Mong restore xóa `debug.log` untracked.** Dấu hiệu là `?? debug.log` vẫn xuất hiện. File không có trong nguồn Git nên phải được xử lý riêng; đừng vội clean mà chưa dry-run.",
  "**Nhầm `--source` với chuyển nhánh.** Dấu hiệu là `git branch --show-current` và `HEAD` không đổi. Tùy chọn chỉ chọn cây làm nguồn blob cho pathspec.",
  "**Bỏ qua dấu `--` trước đường dẫn.** Dấu hiệu là Git báo revision mơ hồ hoặc hiểu tên file thành tùy chọn. Dùng `git restore [options] -- path` thành thói quen.",
  "**Chạy chương trình để test nhưng commit một index khác working tree.** Dấu hiệu là `git diff` vẫn còn sau khi test. Kiểm thử lại nội dung staged hoặc bảo đảm diff ngoài staging không ảnh hưởng kết quả."
],

tu_kiem_tra: [
  "Sau `git add cart.js` rồi sửa thêm cùng file, `git restore cart.js` xóa lớp thay đổi nào, và vì sao phần staged không mất?",
  "Hãy dự đoán lần lượt kết quả của `git diff` và `git diff --staged` sau `git restore --staged cart.js` mà không chạy lệnh.",
  "Viết lệnh lấy `config/payment.json` từ `HEAD~2` vào working tree nhưng giữ nguyên index và không di chuyển nhánh.",
  "Vì sao reflog thường không cứu được nội dung vừa bị `git restore` ghi đè? Trả lời dựa trên đối tượng mà reflog theo dõi.",
  "Một file có sửa staged A và sửa chưa staged B. Hãy thiết kế quy trình bỏ A nhưng giữ B; nêu rủi ro nếu B được xây trên A.",
  "Trong merge conflict, vì sao chọn `--ours` chưa đủ để kết thúc merge, và bước nào khiến index ghi nhận xung đột đã được giải quyết?",
  "Khi nào `git show HEAD:path` tốt hơn `git restore --source=HEAD -- path`, dù cả hai đều truy cập cùng blob?"
],

tai_nguyen: [
  "Sách “Pro Git”, các chương Git Basics và Reset Demystified — nền tảng về index, working tree và phép biến đổi trạng thái.",
  "Tài liệu tham khảo chính thức của Git cho `git-restore` — cú pháp `--source`, `--staged`, `--worktree`, `--patch` và pathspec.",
  "Sách “Version Control with Git” — cách Git lưu object, tree, index và quy trình phục hồi thay đổi.",
  "Khóa học “Git Immersion” — bài tập thao tác staging và quan sát lịch sử trong repository thực hành."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: index hoạt động như một ánh xạ đường dẫn tới blob và metadata; hiểu ánh xạ giúp giải thích vì sao một file có bản staged khác bản trên đĩa.",
  "Nối sang mảng Hệ điều hành: working tree là file thật chịu quy tắc filesystem, còn Git object store không lưu mọi metadata hay mọi lần ghi file.",
  "Nối sang mảng Kiểm thử phần mềm: test working tree chưa chắc test đúng snapshot staged; bài học về tính lặp lại yêu cầu kiểm thử đúng nội dung sắp commit.",
  "Nối sang mảng An toàn dữ liệu: thao tác ghi đè cục bộ cần chiến lược sao lưu theo giá trị và khả năng phục hồi, thay vì tin rằng mọi thứ trong Git đều cứu được."
]

};
