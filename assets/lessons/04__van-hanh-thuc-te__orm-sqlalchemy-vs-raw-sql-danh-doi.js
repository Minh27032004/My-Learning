window.LESSONS["04:van-hanh-thuc-te:orm-sqlalchemy-vs-raw-sql-danh-doi"] = {

tom_tat: [
  "ORM như SQLAlchemy ánh xạ hàng trong bảng thành object và tạo SQL từ biểu thức Python; nó giảm mã lặp nhưng không xoá nhu cầu hiểu transaction, index hay query plan.",
  "Raw SQL cho phép biểu đạt trực tiếp đặc trưng PostgreSQL và nhìn rõ câu lệnh gửi đi; cái giá là phải tự quản lý ánh xạ kết quả, tính khả chuyển và ranh giới transaction.",
  "Lựa chọn tốt thường không phải “chọn một bỏ một”: cùng một ứng dụng có thể dùng ORM cho luồng CRUD, SQLAlchemy Core cho truy vấn tổng hợp và `text()` cho đoạn SQL chuyên biệt.",
  "Hiệu năng phải được chẩn đoán từ số lượt round trip, lượng hàng đọc, index và `EXPLAIN (ANALYZE, BUFFERS)`, không suy ra chỉ từ việc code dùng ORM hay raw SQL.",
  "Ví dụ Vé Mây dùng một truy vấn danh sách booking xuyên suốt để lộ ba lỗi thật: N+1, tải thừa cột và phân trang bằng `OFFSET` quá sâu."
],

can_biet_truoc: [
  "Khái niệm bảng, khóa chính, khóa ngoại và phép `JOIN` trong cơ sở dữ liệu quan hệ.",
  "Transaction, `commit`, `rollback` và tính nguyên tử của một lượt đặt vé.",
  "Python context manager và type hint; ví dụ dùng SQLAlchemy theo phong cách 2.x.",
  "Index B-tree nhiều cột và cách PostgreSQL đọc kế hoạch do `EXPLAIN` trả về."
],

dinh_nghia: "**ORM (Object–Relational Mapping, ánh xạ object–quan hệ)** là lớp phần mềm chuyển giữa hai mô hình: object có thuộc tính và quan hệ trong chương trình, còn PostgreSQL lưu tuple trong bảng và thao tác bằng SQL. Với SQLAlchemy ORM, biểu thức `select(Booking).where(Booking.user_id == uid)` được biên dịch thành SQL có placeholder; hàng kết quả được gắn vào các instance `Booking` trong một `Session`.\n\n**Raw SQL** là việc ứng dụng cung cấp trực tiếp văn bản SQL cho driver, nhưng giá trị động vẫn phải truyền riêng bằng tham số. “Raw” không đồng nghĩa nối chuỗi và cũng không đồng nghĩa bỏ transaction. Trong SQLAlchemy, `text(\"SELECT ... WHERE user_id = :uid\")` vẫn dùng pool, transaction và bind parameter của engine.\n\nĐiểm phân biệt hữu ích không phải “trừu tượng hay nhanh”, mà là **ai chịu trách nhiệm biểu đạt truy vấn và ánh xạ kết quả**. ORM nhận nhiều trách nhiệm hơn; raw SQL trao quyền đó lại cho người viết.",

vi_sao_can: "Vé Mây bắt đầu bằng màn hình “booking của tôi”. Khi chỉ có vài bảng, viết `INSERT`, `SELECT`, chuyển từng cột thành object và đồng bộ khóa ngoại bằng tay có vẻ đơn giản. Đến lúc một request vừa tạo booking, vừa thêm nhiều hạng vé, vừa ghi payment attempt và phải rollback toàn bộ nếu hết chỗ, phần mã lặp quanh SQL lấn át quy tắc nghiệp vụ. ORM giải quyết đúng ma sát đó: unit of work theo dõi object mới/bẩn, identity map giữ một bản object cho mỗi hàng trong Session, và relationship mô tả liên kết.\n\nNhưng abstraction bị hiểu nhầm thành giấy phép quên cơ sở dữ liệu. Một vòng lặp truy cập `booking.event` có thể âm thầm phát thêm một truy vấn cho mỗi booking. Một thuộc tính tiện lợi có thể tải cột JSON lớn không cần hiển thị. Một câu `.offset(100000)` vẫn bắt PostgreSQL đi qua rất nhiều tuple trước khi bỏ chúng. Những chi phí này nằm dưới cú pháp Python nên code review chỉ nhìn application code dễ bỏ sót.\n\nRaw SQL cần thiết khi truy vấn mang hình dạng SQL rõ hơn object: báo cáo `GROUP BY`, window function, `WITH ... RETURNING`, khóa `FOR UPDATE SKIP LOCKED`, bulk upsert hay toán tử PostgreSQL chuyên biệt. Đổi lại, người viết phải bảo vệ contract tên cột, kiểu dữ liệu và transaction. Bài này cung cấp tiêu chí chọn theo từng query, thay vì biến lựa chọn thư viện thành niềm tin tuyệt đối.",

co_che: "### Một truy vấn đi qua những lớp nào\n\nLệnh ORM không “đọc database bằng Python”. SQLAlchemy tạo cây biểu thức, chọn dialect PostgreSQL, sinh SQL cùng bind parameter rồi giao cho DBAPI driver. Driver gửi protocol tới PostgreSQL; parser, planner và executor của PostgreSQL vẫn làm công việc y hệt raw SQL. Sau đó SQLAlchemy mới chuyển hàng thành object và ghi chúng vào identity map.\n\nVì vậy, nếu ORM và raw SQL phát đúng cùng SQL, cùng tham số và cùng transaction, phần database thường nhận cùng plan. Chênh lệch còn lại nằm ở thời gian biên dịch biểu thức, tạo object, theo dõi trạng thái và lượng dữ liệu truyền về. Với query đọc nhiều triệu hàng để xuất file, chi phí tạo object có thể đáng kể; với request đợi mạng và ổ đĩa, một round trip thừa thường đắt hơn nhiều phép gán thuộc tính.\n\n```text\nPython expression → SQL compiler → driver → PostgreSQL planner → executor\n       ORM object ← row mapping  ← rows   ← index/heap pages\n```\n\n### Session không phải connection\n\n`Session` là ranh giới unit of work và identity map. Nó mượn connection từ pool khi cần, mở transaction ngầm ở lần thao tác đầu, `flush` thay đổi thành SQL, rồi trả connection khi transaction kết thúc và Session đóng. Giữ Session toàn cục khiến transaction kéo dài, object cũ sống dai và một request có thể làm bẩn request khác.\n\n`flush` chỉ đồng bộ trạng thái với database trong transaction hiện tại; nó chưa làm thay đổi bền vững cho session khác thấy. `commit` mới kết thúc transaction. PostgreSQL có thể từ chối ở lúc flush vì unique constraint, foreign key hoặc deadlock; code phải rollback trước khi tái sử dụng Session.\n\n```python\nwith Session(engine) as session:\n    try:\n        booking = Booking(user_id=user_id, event_id=event_id, status=\"pending\")\n        session.add(booking)\n        session.flush()       # có booking.id, nhưng chưa commit\n        session.add(BookingItem(booking_id=booking.id, ticket_type=\"standard\", quantity=2))\n        session.commit()\n    except Exception:\n        session.rollback()\n        raise\n```\n\n### Identity map giải quyết gì và che giấu gì\n\nTrong một Session, tải cùng `(class, primary_key)` hai lần thường trả cùng instance. Điều này tránh hai object đại diện cùng hàng nhưng có trạng thái mâu thuẫn. Nó không phải cache dùng chung giữa request, không tự biết transaction khác vừa sửa gì, và không thay thế HTTP cache.\n\nSau `commit`, SQLAlchemy mặc định hết hạn thuộc tính để lần truy cập sau có thể đọc trạng thái mới. Tắt `expire_on_commit` làm code thuận tiện hơn nhưng tăng nguy cơ dùng dữ liệu cũ. Với tác vụ nền xử lý booking, nên truyền ID qua queue rồi mở Session mới, thay vì truyền ORM object đã rời Session.\n\n### Lazy loading và N+1\n\nGiả sử query đầu lấy 50 booking. Khi template chạm `b.event.name`, relationship lazy có thể phát thêm query cho từng `event_id` chưa có trong identity map. Tổng là 1 + N round trip. Dấu hiệu không phải CPU Python cao mà là log SQL lặp cùng hình dạng với tham số khác nhau và latency tăng theo số hàng hiển thị.\n\n`selectinload` thường phát query thứ hai `WHERE events.id IN (...)`, phù hợp quan hệ một-nhiều hoặc nhiều-một khi tập ID vừa phải. `joinedload` dùng JOIN trong một query nhưng có thể nhân số hàng khi tải collection. Không có loader luôn thắng; phải xét lực lượng quan hệ và số byte kết quả.\n\n```python\nstmt = (\n    select(Booking)\n    .options(selectinload(Booking.event))\n    .where(Booking.user_id == bindparam(\"uid\"))\n    .order_by(Booking.created_at.desc(), Booking.id.desc())\n    .limit(50)\n)\nbookings = session.scalars(stmt, {\"uid\": user_id}).all()\n```\n\n### Cột chọn ra và chi phí object\n\nMàn hình chỉ cần `id`, tên sự kiện, trạng thái, tổng tiền và thời gian. Tải nguyên entity có thể kéo theo ghi chú nội bộ hoặc JSON payment metadata. Raw SQL hay SQLAlchemy Core cho projection rõ ràng; ORM cũng làm được bằng `load_only`, nhưng instance tải một phần có thể phát query bổ sung khi code chạm thuộc tính deferred.\n\nĐối với báo cáo chỉ đọc, trả mapping/tuple thường thành thật hơn giả vờ rằng mỗi hàng là aggregate có vòng đời. Đối với thao tác thay đổi booking kèm invariant, entity ORM lại giúp gom hành vi và flush theo transaction. Đây là ranh giới thiết kế, không chỉ mẹo tối ưu.\n\n### Index quyết định nhiều hơn API truy vấn\n\nTruy vấn danh sách có điều kiện `user_id = ?`, sắp `created_at DESC, id DESC` và lấy 50 hàng. Index `(user_id, created_at DESC, id DESC)` cho PostgreSQL đi thẳng vào đoạn của một user và đọc theo đúng thứ tự, tránh sort toàn bộ. Thêm `status` vào đầu index chỉ hợp nếu hầu hết request lọc status; nếu status có ít giá trị và request thường không lọc, index đó có thể kém linh hoạt.\n\n```sql\nCREATE INDEX ix_bookings_user_created_id\nON bookings (user_id, created_at DESC, id DESC);\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, event_id, status, total_cents, created_at\nFROM bookings\nWHERE user_id = 42\nORDER BY created_at DESC, id DESC\nLIMIT 50;\n```\n\nKhông được chép thời gian plan từ máy khác rồi gọi là benchmark. Hãy nhìn `Index Scan` hay `Bitmap Heap Scan`, số `actual rows`, `loops`, khối `shared hit/read`, và so estimated rows với actual rows. Nếu lệch lớn, thống kê có thể cũ hoặc các cột tương quan mạnh.\n\n### Keyset pagination thay OFFSET sâu\n\n`OFFSET 100000 LIMIT 50` không cho phép executor nhảy miễn phí tới hàng thứ 100001; nó vẫn phải xác định và bỏ các hàng trước. Keyset dùng khóa cuối trang trước làm mốc, khớp với index nhiều cột. Cần thêm `id` để phá hòa khi nhiều booking cùng timestamp.\n\n```sql\nSELECT id, event_id, status, total_cents, created_at\nFROM bookings\nWHERE user_id = :uid\n  AND (created_at, id) < (:cursor_created_at, :cursor_id)\nORDER BY created_at DESC, id DESC\nLIMIT :page_size;\n```\n\nKeyset không tiện nhảy thẳng tới “trang 317” và cursor phải được ký hoặc kiểm tra để client không sửa tùy ý. Nó phù hợp timeline cuộn tiếp; trang quản trị cần nhảy số trang có thể chấp nhận OFFSET hoặc dùng chiến lược khác.\n\n### Raw SQL vẫn nằm trong transaction và pool\n\nTrong SQLAlchemy, `session.execute(text(...), params)` dùng chính transaction của Session. Điều này hữu ích khi một phần nghiệp vụ dùng ORM còn câu lệnh khóa chỗ dùng SQL PostgreSQL rõ ràng. Không mở connection driver riêng giữa chừng, vì như vậy hai lệnh có thể thuộc hai transaction khác nhau.\n\n```python\nrow = session.execute(\n    text(\"\"\"\n      UPDATE events\n      SET seats_remaining = seats_remaining - :qty\n      WHERE id = :event_id AND seats_remaining >= :qty\n      RETURNING id, seats_remaining\n    \"\"\"),\n    {\"event_id\": event_id, \"qty\": quantity},\n).mappings().one_or_none()\nif row is None:\n    raise SoldOut()\n```\n\nCâu lệnh nguyên tử tránh kiểu “SELECT thấy còn, rồi UPDATE” bị race condition. ORM không cấm viết câu này; raw SQL không buộc bỏ unit of work. Điểm mạnh của SQLAlchemy chính là cho phép hạ mức abstraction tại đúng chỗ.\n\n### Bulk operation và RETURNING\n\nTạo hàng nghìn booking bằng `session.add()` từng object vừa tạo nhiều object vừa có thể phát nhiều batch tùy driver. SQLAlchemy Core `insert(Booking).values(rows).returning(...)` biểu đạt bulk insert tốt hơn. PostgreSQL `ON CONFLICT` hỗ trợ idempotency cho webhook thanh toán; ORM entity không phải hình dạng tự nhiên nhất của thao tác này.\n\nTuy nhiên bulk update trực tiếp có thể làm object đang nằm trong identity map trở nên cũ. Sau câu lệnh, cần expire/refresh hoặc thiết kế để Session đó không giữ entity liên quan. Tốc độ không đáng đổi lấy hai nguồn sự thật trong cùng unit of work.\n\n### Ranh giới repository và khả năng kiểm thử\n\nRepository có ích khi nó đặt tên cho truy vấn nghiệp vụ như `list_recent_bookings` và che chi tiết loader/index khỏi service. Repository vô nghĩa nếu chỉ bọc từng hàm SQLAlchemy thành `get`, `save` mà không thêm contract. Test nên có lớp integration chạy PostgreSQL thật cho transaction, constraint và SQL đặc thù; SQLite không tái hiện đầy đủ kiểu, lock hay planner PostgreSQL.\n\nMột test unit có thể kiểm quy tắc tính tổng tiền không cần database. Một test query phải seed vài booking có cùng timestamp, nhiều status và user khác nhau để bắt lỗi cursor. Phân tầng kiểm thử theo rủi ro giúp ORM không trở thành lý do mock toàn bộ database.\n\n### Khi nào không nên dùng ORM entity\n\nKhông nên ép ORM vào truy vấn phân tích rộng, ETL streaming, bulk sửa hàng triệu dòng hoặc câu lệnh dựa sâu vào CTE/window function nếu mapping object không mang giá trị nghiệp vụ. Đừng bỏ ORM chỉ vì một query chậm trước khi xem SQL và plan: nguyên nhân có thể là thiếu index, statistics sai hoặc N+1 có thể sửa bằng loader.\n\nCũng không nên dùng raw SQL rải khắp handler với tên cột lặp lại. Schema đổi sẽ tạo lỗi runtime ở nhiều nơi; quy tắc authorization dễ bị quên ở một câu. Hãy tập trung SQL chuyên biệt trong module truy cập dữ liệu, có test và review plan.",

vi_du: "### Schema PostgreSQL tối thiểu của Vé Mây\n\nĐoạn sau chạy được trên PostgreSQL và tạo đúng index phục vụ danh sách booking. Dùng `BIGINT GENERATED ALWAYS AS IDENTITY` tránh phụ thuộc extension; tiền lưu bằng số nguyên cent để không gặp sai số số thực.\n\n```sql\nDROP TABLE IF EXISTS booking_items, payment_attempts, bookings, events, users CASCADE;\nCREATE TABLE users (\n  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  email text NOT NULL UNIQUE\n);\nCREATE TABLE events (\n  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  name text NOT NULL,\n  starts_at timestamptz NOT NULL,\n  seats_remaining integer NOT NULL CHECK (seats_remaining >= 0)\n);\nCREATE TABLE bookings (\n  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  user_id bigint NOT NULL REFERENCES users(id),\n  event_id bigint NOT NULL REFERENCES events(id),\n  status text NOT NULL CHECK (status IN ('pending','paid','cancelled','expired')),\n  total_cents bigint NOT NULL CHECK (total_cents >= 0),\n  created_at timestamptz NOT NULL DEFAULT now()\n);\nCREATE TABLE booking_items (\n  booking_id bigint NOT NULL REFERENCES bookings(id) ON DELETE CASCADE,\n  line_no smallint NOT NULL,\n  ticket_type text NOT NULL,\n  quantity integer NOT NULL CHECK (quantity > 0),\n  unit_price_cents bigint NOT NULL CHECK (unit_price_cents >= 0),\n  PRIMARY KEY (booking_id, line_no)\n);\nCREATE TABLE payment_attempts (\n  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  booking_id bigint NOT NULL REFERENCES bookings(id),\n  provider_ref text NOT NULL UNIQUE,\n  state text NOT NULL,\n  payload jsonb NOT NULL DEFAULT '{}'::jsonb,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\nCREATE INDEX ix_bookings_user_created_id\n  ON bookings (user_id, created_at DESC, id DESC);\n```\n\n### Ba cách viết cùng truy vấn\n\n```python\nfrom sqlalchemy import BigInteger, ForeignKey, String, Text, bindparam, create_engine, select, text\nfrom sqlalchemy.orm import DeclarativeBase, Mapped, Session, mapped_column, relationship, selectinload\n\nclass Base(DeclarativeBase): pass\n\nclass Event(Base):\n    __tablename__ = \"events\"\n    id: Mapped[int] = mapped_column(BigInteger, primary_key=True)\n    name: Mapped[str] = mapped_column(Text)\n\nclass Booking(Base):\n    __tablename__ = \"bookings\"\n    id: Mapped[int] = mapped_column(BigInteger, primary_key=True)\n    user_id: Mapped[int] = mapped_column(ForeignKey(\"users.id\"))\n    event_id: Mapped[int] = mapped_column(ForeignKey(\"events.id\"))\n    status: Mapped[str] = mapped_column(String)\n    total_cents: Mapped[int] = mapped_column(BigInteger)\n    event: Mapped[Event] = relationship()\n\nengine = create_engine(\"postgresql+psycopg://app:app@localhost/vemay\")\n\ndef bang_orm(session: Session, uid: int):\n    stmt = (select(Booking).options(selectinload(Booking.event))\n            .where(Booking.user_id == bindparam(\"uid\"))\n            .order_by(Booking.id.desc()).limit(50))\n    return session.scalars(stmt, {\"uid\": uid}).all()\n\ndef bang_core(session: Session, uid: int):\n    stmt = (select(Booking.id, Booking.status, Booking.total_cents, Event.name)\n            .join(Event, Event.id == Booking.event_id)\n            .where(Booking.user_id == bindparam(\"uid\"))\n            .order_by(Booking.id.desc()).limit(50))\n    return session.execute(stmt, {\"uid\": uid}).mappings().all()\n\ndef bang_raw_sql(session: Session, uid: int):\n    stmt = text(\"\"\"\n      SELECT b.id, b.status, b.total_cents, e.name AS event_name\n      FROM bookings AS b JOIN events AS e ON e.id = b.event_id\n      WHERE b.user_id = :uid ORDER BY b.id DESC LIMIT 50\n    \"\"\")\n    return session.execute(stmt, {\"uid\": uid}).mappings().all()\n```\n\nBa hàm có contract kết quả khác: ORM trả entity dùng tiếp cho nghiệp vụ; Core/raw trả projection vừa màn hình. Đừng benchmark chúng khi một hàm tải nhiều cột hơn hàm kia. Muốn so công bằng, phải giữ SQL, dữ liệu, warm-up và số vòng lặp tương đương, đồng thời công bố môi trường.\n\n### Ca biên cần test\n\n```sql\n-- User chưa có booking: phải trả 0 hàng, không phải 404 ở tầng query.\nSELECT id FROM bookings WHERE user_id = -1 ORDER BY created_at DESC, id DESC LIMIT 50;\n\n-- Hai hàng trùng thời gian vẫn có thứ tự xác định nhờ id.\n-- Booking bị xóa event bị FK chặn; dữ liệu không thành orphan.\n-- total_cents lớn dùng bigint; ứng dụng phải tránh ép về integer 32 bit.\n-- quantity = 0 và seats_remaining âm bị CHECK từ chối ngay tại database.\n```\n\nĐối với timeout, đặt `statement_timeout` theo transaction hoặc role, bắt exception, rollback Session và trả lỗi có thể retry nếu nghiệp vụ cho phép. Không nuốt timeout rồi tiếp tục dùng transaction đã aborted.",

so_sanh: "### Ma trận chọn công cụ theo hình dạng công việc\n\n| Tiêu chí | SQLAlchemy ORM | SQLAlchemy Core | Raw SQL có tham số |\n|---|---|---|---|\n| CRUD gắn invariant object | Rất phù hợp | Dùng được | Nhiều mã ánh xạ |\n| Projection/báo cáo | Có thể tải thừa | Phù hợp | Phù hợp nhất khi SQL phức tạp |\n| PostgreSQL đặc thù | Có extension nhưng đôi lúc gượng | Khá mạnh | Biểu đạt trực tiếp |\n| Kiểm tra tên cột lúc viết | Qua model/type checker một phần | Qua column object | Chủ yếu integration test |\n| Identity map/unit of work | Có | Không | Không |\n| Nguy cơ N+1 | Cao nếu loader sai | Không có lazy relationship | Không có lazy relationship |\n| Khả chuyển database | Tương đối, không tuyệt đối | Tương đối | Thấp hơn |\n| Quyền kiểm soát SQL | Gián tiếp nhưng xem được | Rõ | Trực tiếp |\n\nNếu query là “lấy aggregate rồi sửa theo quy tắc”, ORM có lợi. Nếu query là “tổng doanh thu theo cửa sổ thời gian”, Core hoặc raw SQL thường rõ hơn. Nếu nhóm không có kỷ luật integration test, raw SQL phân tán làm schema migration nguy hiểm; nếu nhóm không biết đọc SQL, ORM lại che lỗi hiệu năng lâu hơn.",

loi_thuong_gap: [
  "Gọi relationship trong vòng lặp rồi chỉ nhìn một query ban đầu. Nhận biết bằng log có cùng `SELECT events ... WHERE id = ?` lặp theo số booking; sửa bằng loader phù hợp hoặc projection có JOIN.",
  "Giữ một Session toàn cục cho nhiều request. Dấu hiệu là transaction “idle in transaction”, object trả dữ liệu cũ và lỗi chéo request; dùng Session theo request/job.",
  "Cho rằng raw SQL tự động nhanh hơn. Nếu plan và số hàng giống nhau, khác biệt có thể không đáng kể; phải đo riêng thời gian database và serialization.",
  "Dùng `OFFSET` rất sâu cho timeline. `EXPLAIN ANALYZE` cho thấy executor xử lý nhiều hàng rồi bỏ; chuyển sang keyset khi UX cho phép.",
  "Bulk update trong cùng Session nhưng tiếp tục tin object đã tải. Nhận biết bằng giá trị Python khác database; expire/refresh hoặc tách unit of work.",
  "Bắt `IntegrityError` nhưng quên rollback. Mọi lệnh sau báo transaction đã lỗi; rollback trước khi tái sử dụng Session.",
  "Tạo index theo mọi cột xuất hiện trong model. Index làm chậm ghi và tốn chỗ; thiết kế từ predicate, thứ tự và workload thật."
],

tu_kiem_tra: [
  "Vì sao hai cách viết khác nhau nhưng phát đúng cùng SQL thường có cùng query plan PostgreSQL?",
  "Phân biệt `flush`, `commit` và đóng Session trong luồng tạo booking kèm booking item.",
  "Một trang tải 50 booking và 12 event khác nhau. Lazy loading có thể phát bao nhiêu query, và `selectinload` thay đổi hình dạng ra sao?",
  "Vì sao index `(user_id, created_at DESC, id DESC)` hỗ trợ keyset, còn chỉ index `created_at` chưa đủ tốt?",
  "Khi nào `joinedload` làm kết quả truyền qua mạng phình to hơn `selectinload`?",
  "Thiết kế ranh giới để một transaction vừa dùng ORM tạo booking vừa dùng raw SQL trừ ghế mà không vô tình dùng hai connection.",
  "Nêu một query của Vé Mây nên dùng entity ORM và một query nên dùng Core/raw SQL; giải thích theo hình dạng dữ liệu, không theo sở thích cú pháp."
],

tai_nguyen: [
  "Tài liệu chính thức SQLAlchemy, các phần ORM Session Basics, Relationship Loading Techniques và SQL Expression Language.",
  "“Architecture Patterns with Python” — Harry Percival và Bob Gregory, các chương Repository, Unit of Work và SQLAlchemy.",
  "“High Performance PostgreSQL for Rails” — Andrew Atkinson, hữu ích dù ví dụ framework khác vì trọng tâm là query plan và index PostgreSQL.",
  "“Designing Data-Intensive Applications” — Martin Kleppmann, các chương về mô hình dữ liệu, transaction và hệ thống lưu trữ."
],

lien_he: [
  "Nối sang mảng Bảo mật: raw SQL chỉ an toàn khi giá trị động đi qua bind parameter; bài SQL injection kế tiếp phân tích vì sao ranh giới dữ liệu–cú pháp quan trọng hơn ORM hay raw.",
  "Nối sang mảng Kiểm thử: repository query cần integration test trên PostgreSQL thật, còn quy tắc tính tiền có thể kiểm thử đơn vị không cần database.",
  "Nối sang mảng Hệ điều hành và concurrency: Session theo request và connection pool là quản lý tài nguyên hữu hạn; bài connection pooling sẽ lượng hóa hàng đợi và timeout.",
  "Nối sang mảng Thiết kế hệ thống: keyset pagination, idempotency webhook và atomic update là các quyết định API/đồng thời chứ không chỉ cú pháp SQL."
]

};
