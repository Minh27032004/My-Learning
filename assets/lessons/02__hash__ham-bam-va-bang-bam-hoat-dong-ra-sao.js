window.LESSONS["02:hash:ham-bam-va-bang-bam-hoat-dong-ra-sao"] = {

tom_tat: [
  "**Hai module trước đã dùng \"đổi list thành set là O(1)\" khoảng mười lần mà chưa giải thích. Bài này mở nắp:** bảng băm biến một khoá bất kỳ thành **chỉ số mảng** bằng một phép tính, rồi truy cập thẳng.",
  "**Toàn bộ phép màu nằm ở hai bước:** `hash(khoá)` cho một số nguyên, rồi `số % kích_thước` cho chỉ số. Sau đó nó chỉ là mảng — và mảng thì `ds[i]` là O(1).",
  "**Hàm băm tốt cần ba tính chất:** xác định (cùng đầu vào luôn cùng kết quả), phân bố đều, và nhanh. Thiếu tính chất thứ hai là bảng băm thoái hoá thành danh sách.",
  "**Hợp đồng `__hash__` và `__eq__` là ràng buộc bắt buộc:** hai object bằng nhau **phải** cùng hash. Vi phạm làm `dict` mất dữ liệu một cách im lặng.",
  "**Chỉ đối tượng BẤT BIẾN mới làm khoá được** — vì hash phải không đổi suốt đời object. Đây là lý do `list` không làm khoá `dict` được còn `tuple` thì được."
],

can_biet_truoc: [
  "Bài **Array và dynamic array** ở module 2 mảng này — bảng băm xây **trên** mảng; `ds[i]` là O(1) nhờ công thức địa chỉ.",
  "Bài **Phân tích khấu hao** ở module 1 mảng này — bảng băm cũng cấp phát lại theo tỉ lệ, cùng lập luận.",
  "Bài **Mutable vs immutable** ở module 7 mảng 01 — vì sao chỉ đối tượng bất biến mới làm khoá được.",
  "Bài **Magic method** ở module 6 mảng 01 — `__hash__` và `__eq__` là hai phương thức đặc biệt của giao thức băm."
],

dinh_nghia: "Nói đời thường: thư viện xếp sách theo mã số. Muốn tìm cuốn \"Giải thuật\", bạn không đi dò từng kệ — bạn tra bảng, thấy mã 512, rồi đi thẳng tới kệ 512. Bảng tra đó chính là **hàm băm**, và việc đi thẳng tới kệ chính là truy cập mảng O(1).\n\nCác khái niệm:\n\n- **Hàm băm** (*hash function*) — hàm biến dữ liệu **kích thước bất kỳ** thành một **số nguyên kích thước cố định**.\n- **Giá trị băm** (*hash value*) — kết quả của hàm băm. Trong Python là `hash(x)`.\n- **Bảng băm** (*hash table*) — mảng mà vị trí lưu một khoá được **tính ra** từ khoá đó, không phải tìm.\n- **Ô** (*bucket / slot*) — một vị trí trong mảng nền của bảng băm.\n- **Va chạm** (*collision*) — hai khoá khác nhau cho cùng một chỉ số. **Không tránh được**, và bài sau nói cách xử lý.\n- **Hệ số tải** (*load factor*) — tỉ lệ `số phần tử / số ô`. Quyết định khi nào phải cấp phát lại.\n- **Hashable** — đối tượng có `__hash__` và dùng làm khoá được. Trong Python, gần như đồng nghĩa với **bất biến**.\n\nHai bước biến khoá thành chỉ số — đây là toàn bộ ý tưởng:\n\n```\n  khoá \"minh\"\n      │\n      ↓  ① HÀM BĂM\n  hash(\"minh\") = 8374926183746   (một số nguyên lớn)\n      │\n      ↓  ② THU VỀ PHẠM VI MẢNG\n  8374926183746 % 8 = 2\n      │\n      ↓\n  o[2] ← đặt (khoá, giá trị) vào đây\n```\n\n```\n⭐ Sau bước ②, mọi thứ chỉ còn là MẢNG.\n  Và mảng cho ds[i] là O(1) nhờ công thức địa chỉ (module 2).\n\n⇒ \"O(1) trung bình\" của dict KHÔNG phải phép màu —\n  nó là O(1) của mảng, cộng chi phí tính hash.\n```",

vi_sao_can: "Đây là bài trả nợ cho một lời hứa đã dùng liên tục:\n\n- **Hai module trước dựa vào `set`/`dict` để sửa gần như mọi vấn đề hiệu năng.** \"Đổi list thành set\" xuất hiện ở bài vòng lặp lồng nhau, bài chi phí ẩn, bài thang bậc, bài queue. Không hiểu cơ chế thì bạn đang dùng một hộp đen.\n- **Nó giải thích các giới hạn mà bạn sẽ gặp.** Vì sao `list` không làm khoá được, vì sao `dict` tốn nhiều bộ nhớ, vì sao thứ tự lặp qua `set` trông ngẫu nhiên — cả ba đều suy ra từ cơ chế.\n- **Nó là điều kiện để tự viết class dùng làm khoá.** Rất nhiều bug đến từ việc định nghĩa `__eq__` mà quên `__hash__` — và Python im lặng cho phép, rồi `dict` cư xử kỳ lạ.\n- **Bảng băm là cấu trúc quan trọng nhất trong lập trình thực tế.** Nó nằm dưới `dict`, `set`, bộ nhớ đệm, chỉ mục database, bảng định tuyến, và cả cách Python tra thuộc tính object.\n- **Nó là nền cho bốn bài còn lại của module.** Va chạm, đánh đổi bộ nhớ, và câu hỏi \"vì sao chỉ là trung bình\" đều xây trên bài này.\n\nTình huống thật rất hay gặp: bạn viết một `dataclass` làm khoá cho `dict` đếm tần suất. Bạn định nghĩa `__eq__` để so sánh theo mã, và mọi thứ *trông* chạy được. Nhưng đếm ra kết quả sai — cùng một mã lại thành nhiều mục riêng. **Nguyên nhân: định nghĩa `__eq__` mà không định nghĩa `__hash__` thì Python đặt `__hash__ = None`**, và nếu bạn tự viết một `__hash__` không nhất quán với `__eq__` thì hai object bằng nhau lại rơi vào hai ô khác nhau. Hiểu hợp đồng băm là cách duy nhất tránh được loại lỗi này.\n\nMột lưu ý về phạm vi: bài này nói về **cơ chế cơ bản và giao thức của Python**. Cách xử lý va chạm là bài 2; đánh đổi bộ nhớ là bài 3; và câu hỏi \"vì sao xấu nhất là O(n)\" là bài 5.",

co_che: "### Bước ①: hàm băm\n\n```\nHÀM BĂM: dữ liệu bất kỳ  →  số nguyên cố định\n\n  \"minh\"        →  8374926183746\n  \"Minh\"        →  −2938471029384      ⭐ khác một chữ, hash khác hẳn\n  42            →  42                   (số nguyên nhỏ: hash = chính nó)\n  (1, 2)        →  −3550055125485641917\n```\n\nBa tính chất bắt buộc của một hàm băm tốt:\n\n```\n① XÁC ĐỊNH\n   cùng đầu vào → LUÔN cùng kết quả (trong một lần chạy)\n   ⇒ không có ngẫu nhiên, không phụ thuộc thời gian\n\n② PHÂN BỐ ĐỀU\n   các đầu vào khác nhau → rải ĐỀU trên phạm vi chỉ số\n   ⇒ đây là tính chất QUAN TRỌNG NHẤT\n\n③ NHANH\n   phải tính trong O(độ dài khoá), không đắt hơn việc so sánh trực tiếp\n```\n\nTính chất ② quyết định mọi thứ, và đây là lý do:\n\n```\nHÀM BĂM XẤU:  hash(x) = 0  cho MỌI x\n\n  → mọi khoá vào CÙNG một ô\n  → bảng băm thoái hoá thành DANH SÁCH\n  → tra cứu thành O(n)\n\n⇒ \"O(1) trung bình\" giả định hàm băm phân bố ĐỀU.\n  Giả định đó hỏng thì lời hứa hỏng theo. (Bài 5 nói kỹ.)\n```\n\n### Bước ②: từ hash sang chỉ số\n\n```\nhash(khoá) là số nguyên RẤT LỚN (64 bit), có thể ÂM.\nMảng chỉ có n ô.\n\n⇒ phải THU VỀ phạm vi 0..n−1\n```\n\nHai cách, và Python dùng cách thứ hai:\n\n```\nCÁCH 1 — CHIA LẤY DƯ\n  chi_so = hash(khoá) % n\n\n  ⭐ đơn giản, hoạt động với mọi n\n  ⚠️ phép chia chậm hơn phép bit\n  ⚠️ nên chọn n là SỐ NGUYÊN TỐ để phân bố đều hơn\n\nCÁCH 2 — MẶT NẠ BIT (khi n là luỹ thừa của 2)\n  chi_so = hash(khoá) & (n − 1)\n\n  ⭐ RẤT nhanh — chỉ một phép AND\n  ⚠️ chỉ dùng các bit THẤP của hash\n     ⇒ hàm băm phải trộn đều các bit\n\n  Ví dụ n = 8:  n − 1 = 7 = 0b111\n     hash & 0b111  ⇒ giữ 3 bit thấp ⇒ giá trị 0..7   ⭐ đúng phạm vi\n```\n\nPython dùng cách 2, và kích thước bảng luôn là **luỹ thừa của 2**.\n\n⚠️ Hệ quả đáng chú ý: vì chỉ dùng bit thấp, một hàm băm chỉ tốt ở bit cao sẽ cho phân bố tệ. Đây là lý do hàm băm thật phải **trộn** các bit chứ không chỉ cộng dồn.\n\n### Bước ③: xử lý va chạm\n\n```\nVA CHẠM LÀ KHÔNG TRÁNH ĐƯỢC:\n\n  Số khoá có thể có: VÔ HẠN (mọi chuỗi, mọi số)\n  Số ô trong mảng:   HỮU HẠN\n\n  ⇒ theo nguyên lý chuồng bồ câu, PHẢI có hai khoá cùng ô.\n```\n\nBài này chỉ nêu vấn đề; **bài 2 sẽ nói kỹ hai cách giải** (chaining và open addressing). Điều cần nhớ ở đây: bảng băm **luôn** phải lưu **cả khoá** trong ô, không chỉ giá trị.\n\n```\nVÌ SAO PHẢI LƯU CẢ KHOÁ:\n\n  Tra o[2] và thấy có dữ liệu — nhưng đó có phải khoá MÌNH CẦN không?\n  Có thể là một khoá khác cũng băm về ô 2.\n\n  ⇒ phải SO SÁNH khoá lưu trong ô với khoá đang tìm\n  ⇒ đó là lý do cần cả __hash__ VÀ __eq__\n```\n\nĐây là điểm nối quan trọng: **`__hash__` để tìm đúng ô, `__eq__` để xác nhận đúng khoá.**\n\n### Hệ số tải và cấp phát lại\n\n```\nhệ_số_tải = số_phần_tử / số_ô\n\n  tải thấp (0,2)  → ít va chạm, nhưng LÃNG PHÍ bộ nhớ\n  tải cao (0,9)   → tiết kiệm bộ nhớ, nhưng NHIỀU va chạm ⇒ chậm\n```\n\n```\nKHI hệ số tải VƯỢT NGƯỠNG → CẤP PHÁT LẠI:\n\n  ① tạo mảng MỚI lớn hơn (thường gấp đôi hoặc gấp bốn)\n  ② TÍNH LẠI chỉ số cho MỌI khoá cũ    ⚠️ vì n đổi nên hash % n đổi\n  ③ chuyển sang mảng mới\n\n  ⇒ O(n) cho MỘT lần cấp phát lại\n  ⇒ nhưng O(1) KHẤU HAO nếu tăng theo TỈ LỆ\n     (đúng lập luận ở bài khấu hao, module 1)\n```\n\nĐiểm ② đáng chú ý: **không thể chỉ copy sang mảng mới** như mảng động. Vì chỉ số phụ thuộc `n`, đổi `n` là mọi khoá phải tính lại vị trí — đây gọi là *rehashing*.\n\nNgưỡng của Python:\n\n```\nCPython cấp phát lại khi bảng đầy khoảng 2/3.\nKích thước mới thường gấp 3 lần số phần tử đang dùng\n(rồi làm tròn lên luỹ thừa của 2).\n\n⇒ Đánh đổi: chấp nhận lãng phí ~1/3 bộ nhớ để giữ ít va chạm.\n```\n\n### Giao thức băm của Python\n\n```python\nclass X:\n    def __hash__(self) -> int: ...      # cho ra chỉ số\n    def __eq__(self, other) -> bool: ... # xác nhận đúng khoá\n```\n\n**Hợp đồng bắt buộc — thuộc lòng dòng này:**\n\n```\nNẾU  a == b   THÌ  hash(a) == hash(b)\n\n⇒ Hai object BẰNG NHAU phải CÙNG hash.\n\n⚠️ CHIỀU NGƯỢC LẠI KHÔNG BẮT BUỘC:\n  hash(a) == hash(b)  KHÔNG suy ra  a == b\n  (đó chính là va chạm — chuyện bình thường)\n```\n\nVì sao hợp đồng này bắt buộc:\n\n```\nNếu a == b nhưng hash(a) ≠ hash(b):\n\n  d[a] = 1        → đặt vào ô tính từ hash(a)\n  d[b]            → tìm ở ô tính từ hash(b)  ⚠️ Ô KHÁC\n  ⇒ KeyError, dù a == b\n\n⇒ dict MẤT DỮ LIỆU một cách IM LẶNG.\n```\n\nQuy tắc thực dụng suy ra từ hợp đồng: **`__hash__` và `__eq__` phải dùng CÙNG BỘ THUỘC TÍNH.**\n\n```python\n# ✓ Nhất quán\ndef __eq__(self, other): return self.ma == other.ma\ndef __hash__(self):      return hash(self.ma)          # ⭐ cùng self.ma\n\n# ❌ Không nhất quán — nguồn bug im lặng\ndef __eq__(self, other): return self.ma == other.ma\ndef __hash__(self):      return hash((self.ma, self.ten))   # ⚠️ thêm ten\n```\n\n### Quy tắc mặc định của Python\n\n```\nĐỊNH NGHĨA          KẾT QUẢ\n────────────────    ────────────────────────────────────\nkhông định nghĩa gì  __hash__ dựa trên id(), __eq__ dựa trên is\n                     ⇒ mỗi instance là một khoá RIÊNG\n\nchỉ __eq__           ⚠️ Python đặt __hash__ = None\n                     ⇒ object thành KHÔNG hashable\n                     ⇒ TypeError khi làm khoá\n\ncả hai               ⭐ đúng cách\n\n@dataclass(frozen=True)  ⭐ Python tự sinh CẢ HAI, nhất quán\n```\n\nHàng thứ hai là một quyết định thiết kế **có chủ ý và rất tốt** của Python: thay vì để bạn có `__hash__` mặc định (theo `id`) không khớp với `__eq__` tự viết, nó **chủ động vô hiệu hoá** — biến một bug im lặng thành một lỗi rõ ràng.\n\n### Vì sao chỉ đối tượng bất biến làm khoá được\n\n```\nhash phải KHÔNG ĐỔI suốt thời gian object nằm trong dict.\n\nNếu đổi:\n  d[k] = 1        → đặt vào ô 2 (tính từ hash lúc đó)\n  k.thay_doi()    → hash(k) giờ khác\n  d[k]            → tìm ở ô 7  ⚠️ KHÔNG THẤY\n\n  ⇒ dữ liệu \"biến mất\" dù vẫn nằm trong bảng.\n```\n\n```\nHASHABLE (bất biến)              KHÔNG HASHABLE (khả biến)\n──────────────────────           ─────────────────────────\nint, float, str, bytes           list\ntuple (nếu phần tử hashable)     dict\nfrozenset                        set\nbool, None                       bytearray\n@dataclass(frozen=True)          @dataclass thường\nclass tự viết (mặc định)         class chỉ có __eq__\n```\n\n⚠️ Chi tiết tinh tế: `tuple` chỉ hashable **nếu mọi phần tử của nó hashable**.\n\n```python\nhash((1, 2))          # ✓ được\nhash((1, [2]))        # ⚠️ TypeError — list bên trong\n```\n\n### Bảng băm của Python — cấu trúc thật\n\nTừ Python 3.6, `dict` dùng thiết kế **hai tầng** (*compact dict*):\n\n```\nTẦNG 1 — mảng CHỈ SỐ (thưa, nhỏ)\n    [ −1 ][ 0 ][ −1 ][ 2 ][ 1 ][ −1 ][ −1 ][ −1 ]\n              ↓         ↓     ↓\n              │         │     └────────┐\n              │         └──────┐       │\nTẦNG 2 — mảng MỤC (đặc, theo thứ tự chèn)\n    [ (hash, \"a\", 1) ][ (hash, \"b\", 2) ][ (hash, \"c\", 3) ]\n           0                  1                 2\n```\n\nHai lợi ích của thiết kế này:\n\n```\n① TIẾT KIỆM BỘ NHỚ\n   mảng thưa chỉ chứa SỐ NGUYÊN nhỏ, không chứa cả bộ ba\n   ⇒ dict Python 3.6+ tốn ít hơn khoảng 20–25% so với trước\n\n② GIỮ THỨ TỰ CHÈN\n   ⭐ tầng 2 ghi theo thứ tự chèn ⇒ duyệt dict ra đúng thứ tự đó\n   ⇒ từ Python 3.7 đây là ĐẢM BẢO của ngôn ngữ, không phải chi tiết cài đặt\n```\n\n⚠️ Điểm ② đáng nhớ vì nó khác `set`:\n\n```\ndict  ⭐ GIỮ thứ tự chèn (đảm bảo từ 3.7)\nset   ⚠️ KHÔNG đảm bảo thứ tự — nó không có tầng 2 theo thứ tự chèn\n```\n\n### Ngẫu nhiên hoá hàm băm\n\n```\nTừ Python 3.3, hash của str và bytes được NGẪU NHIÊN HOÁ\nbằng một khoá sinh ngẫu nhiên lúc khởi động tiến trình.\n\n⇒ hash(\"abc\") KHÁC NHAU giữa hai lần chạy chương trình\n⇒ nhưng KHÔNG ĐỔI trong một lần chạy\n```\n\nLý do là **bảo mật**, không phải kỹ thuật:\n\n```\nKhông ngẫu nhiên hoá ⇒ kẻ tấn công tính trước được các khoá\n                       cùng băm về một ô\n                    ⇒ gửi hàng nghìn khoá va chạm\n                    ⇒ dict thoái hoá thành O(n)\n                    ⇒ TẤN CÔNG TỪ CHỐI DỊCH VỤ\n\n(Bài 5 nói kỹ về loại tấn công này.)\n```\n\n⚠️ Hệ quả thực tế phải biết:\n\n```\n· KHÔNG lưu hash(str) xuống file/database — lần chạy sau sẽ khác\n· Muốn hash ỔN ĐỊNH giữa các lần chạy → dùng hashlib (md5, sha256)\n· Thứ tự lặp qua set có thể khác giữa hai lần chạy\n· Đặt PYTHONHASHSEED=0 tắt được ngẫu nhiên hoá — chỉ dùng để gỡ rối\n```\n\n### Hai loại hàm băm — đừng nhầm\n\n```\n                  BĂM CHO BẢNG BĂM        BĂM MẬT MÃ\n                  ──────────────────      ──────────────────\nMục tiêu          nhanh, phân bố đều       chống giả mạo\nTốc độ            ⭐ RẤT nhanh             chậm (có chủ ý)\nChống va chạm     không cần               ⭐ bắt buộc\nỔn định giữa      ✗ (Python ngẫu nhiên hoá) ⭐ ✓\ncác lần chạy\nVí dụ             hash() của Python        sha256, bcrypt\nDùng cho          dict, set               chữ ký, mật khẩu\n```\n\nNhầm hai loại này gây hai lỗi thật:\n\n```\n❌ Dùng hash() để lưu mật khẩu   ⇒ cực kỳ không an toàn\n❌ Dùng sha256 làm khoá dict     ⇒ chậm không cần thiết\n```",

vi_du: "### Quan sát hàm băm của Python\n\n```python\n# Số nguyên nhỏ: hash = chính nó\nprint(f\"hash(0)   = {hash(0)}\")\nprint(f\"hash(42)  = {hash(42)}\")\nprint(f\"hash(-1)  = {hash(-1)}\")      # ⭐ chú ý: KHÔNG phải −1\n\n# Số thực\nprint(f\"hash(1.0) = {hash(1.0)}\")\nprint(f\"hash(1)   = {hash(1)}\")\nprint(f\"1 == 1.0  → {1 == 1.0}\")      # ⭐ bằng nhau ⇒ CÙNG hash\n```\n\n```\nhash(0)   = 0\nhash(42)  = 42\nhash(-1)  = -2          ⭐ CPython dùng −1 làm mã lỗi nội bộ\nhash(1.0) = 1\nhash(1)   = 1\n1 == 1.0  → True\n\n⭐ 1 == 1.0 nên hash phải BẰNG NHAU — đúng hợp đồng.\n  Hệ quả: d[1] và d[1.0] là CÙNG MỘT khoá!\n```\n\n```python\nd = {}\nd[1] = \"int\"\nd[1.0] = \"float\"                       # ⭐ GHI ĐÈ, không tạo khoá mới\nprint(d)                                # {1: 'float'}\nprint(len(d))                           # 1\n\nd[True] = \"bool\"                        # ⚠️ True == 1 !\nprint(d)                                # {1: 'bool'}\nprint(len(d))                           # 1  ⭐ vẫn MỘT khoá\n```\n\n```\n⚠️ BẪY THẬT: True == 1 == 1.0 nên chúng là CÙNG một khoá dict.\n\n  Gặp khi: đếm tần suất trên dữ liệu trộn kiểu\n  ⇒ {1: 5, True: 3} không tồn tại — chúng cộng vào nhau\n```\n\nNgẫu nhiên hoá hash của chuỗi:\n\n```python\n# Chạy đoạn này HAI LẦN ở hai tiến trình khác nhau\nprint(f\"hash('abc') = {hash('abc')}\")\n\n# Lần 1: hash('abc') = 8374926183746183\n# Lần 2: hash('abc') = -2938471029384756    ⭐ KHÁC HẲN\n\n# Nhưng trong CÙNG một lần chạy thì KHÔNG đổi:\nprint(hash(\"abc\") == hash(\"abc\"))       # True — luôn luôn\n```\n\n```bash\n# Tắt ngẫu nhiên hoá — CHỈ để gỡ rối, không dùng trong production\n$ PYTHONHASHSEED=0 python -c \"print(hash('abc'))\"\n$ PYTHONHASHSEED=0 python -c \"print(hash('abc'))\"\n# ⭐ hai lần cho cùng kết quả\n```\n\n```python\n# ⚠️ KHÔNG lưu hash(str) xuống database\n# ✓ Muốn hash ỔN ĐỊNH giữa các lần chạy: dùng hashlib\nimport hashlib\n\ndef hash_on_dinh(s: str) -> int:\n    \"\"\"Hash ổn định giữa các lần chạy và giữa các máy.\"\"\"\n    return int(hashlib.sha256(s.encode(\"utf-8\")).hexdigest()[:16], 16)\n\nprint(hash_on_dinh(\"abc\"))              # ⭐ LUÔN cùng giá trị\n```\n\n### Tự cài bảng băm — thấy rõ cơ chế\n\n```python\nfrom typing import Any, Iterator\n\n\nclass BangBamDonGian:\n    \"\"\"Bảng băm tối giản dùng chaining (danh sách trong mỗi ô).\n\n    Mục tiêu: THẤY RÕ hai bước hash → chỉ số.\n    Cách xử lý va chạm đầy đủ: xem bài tiếp theo.\n    \"\"\"\n\n    NGUONG_TAI = 0.75                  # ⭐ vượt là cấp phát lại\n\n    def __init__(self, so_o_ban_dau: int = 8) -> None:\n        self._o: list[list[tuple[Any, Any]]] = [[] for _ in range(so_o_ban_dau)]\n        self._size = 0\n        self.so_lan_cap_phat_lai = 0    # ⭐ để quan sát\n\n    def _chi_so(self, khoa: Any) -> int:\n        \"\"\"HAI BƯỚC: hash rồi thu về phạm vi mảng.\"\"\"\n        return hash(khoa) % len(self._o)       # ⭐ toàn bộ 'phép màu'\n\n    @property\n    def he_so_tai(self) -> float:\n        return self._size / len(self._o)\n\n    def __len__(self) -> int:\n        return self._size\n\n    def __setitem__(self, khoa: Any, gia_tri: Any) -> None:\n        i = self._chi_so(khoa)\n        o = self._o[i]\n        for vi_tri, (k, _) in enumerate(o):\n            if k == khoa:                       # ⭐ __eq__ xác nhận đúng khoá\n                o[vi_tri] = (khoa, gia_tri)     # cập nhật\n                return\n        o.append((khoa, gia_tri))\n        self._size += 1\n        if self.he_so_tai > self.NGUONG_TAI:\n            self._cap_phat_lai(len(self._o) * 2)\n\n    def __getitem__(self, khoa: Any) -> Any:\n        \"\"\"Raises: KeyError.\"\"\"\n        for k, v in self._o[self._chi_so(khoa)]:\n            if k == khoa:\n                return v\n        raise KeyError(khoa)\n\n    def __contains__(self, khoa: Any) -> bool:\n        return any(k == khoa for k, _ in self._o[self._chi_so(khoa)])\n\n    def __delitem__(self, khoa: Any) -> None:\n        \"\"\"Raises: KeyError.\"\"\"\n        o = self._o[self._chi_so(khoa)]\n        for vi_tri, (k, _) in enumerate(o):\n            if k == khoa:\n                o.pop(vi_tri)\n                self._size -= 1\n                return\n        raise KeyError(khoa)\n\n    def _cap_phat_lai(self, so_o_moi: int) -> None:\n        \"\"\"⚠️ Phải TÍNH LẠI chỉ số cho MỌI khoá — vì n đổi.\"\"\"\n        cu = self._o\n        self._o = [[] for _ in range(so_o_moi)]\n        self._size = 0\n        self.so_lan_cap_phat_lai += 1\n        for o in cu:\n            for k, v in o:\n                self[k] = v                     # ⭐ chèn lại, tính chỉ số MỚI\n\n    def __iter__(self) -> Iterator[Any]:\n        for o in self._o:\n            for k, _ in o:\n                yield k\n\n    def phan_bo(self) -> list[int]:\n        \"\"\"Số phần tử trong mỗi ô — để xem có phân bố đều không.\"\"\"\n        return [len(o) for o in self._o]\n```\n\n```python\nb = BangBamDonGian()\nb[\"minh\"] = 25\nb[\"lan\"] = 30\nb[\"cuong\"] = 28\n\nassert b[\"minh\"] == 25\nassert \"lan\" in b\nassert \"khong-co\" not in b\nassert len(b) == 3\n\nb[\"minh\"] = 26                          # cập nhật, không thêm mới\nassert b[\"minh\"] == 26 and len(b) == 3\n\ndel b[\"lan\"]\nassert \"lan\" not in b and len(b) == 2\n\n# ---- Ca biên ----\ntry:\n    b[\"khong-co\"]\nexcept KeyError as e:\n    print(f\"✓ KeyError: {e}\")\n\ntry:\n    del b[\"khong-co\"]\nexcept KeyError as e:\n    print(f\"✓ KeyError khi xoá: {e}\")\n\nrong = BangBamDonGian()\nassert len(rong) == 0 and list(rong) == []\nprint(\"✓ bảng băm tự cài: mọi ca kiểm thử đạt\")\n```\n\nQuan sát chỉ số được tính ra:\n\n```python\nb = BangBamDonGian(so_o_ban_dau=8)\nfor ten in (\"minh\", \"lan\", \"cuong\", \"an\", \"binh\"):\n    print(f\"{ten:>8}  hash % 8 = {hash(ten) % 8}\")\n```\n\n```\nKẾT QUẢ có dạng (khác nhau mỗi lần chạy vì ngẫu nhiên hoá):\n\n    minh  hash % 8 = 3\n     lan  hash % 8 = 7\n   cuong  hash % 8 = 1\n      an  hash % 8 = 3      ⚠️ VA CHẠM với \"minh\"\n    binh  hash % 8 = 5\n\n⭐ Với 5 khoá trong 8 ô, va chạm đã xuất hiện.\n  Đây là chuyện BÌNH THƯỜNG — bài sau nói cách xử lý.\n```\n\n### Hàm băm tốt và hàm băm xấu\n\n```python\nimport time\n\n\nclass BangBamHamXau(BangBamDonGian):\n    \"\"\"Hàm băm cho MỌI khoá về ô 0 — trường hợp xấu nhất.\"\"\"\n    def _chi_so(self, khoa: Any) -> int:\n        return 0                        # ⚠️ hàm băm tệ nhất có thể\n\n\nclass BangBamHamKem(BangBamDonGian):\n    \"\"\"Chỉ dùng ký tự đầu — phân bố kém với dữ liệu thật.\"\"\"\n    def _chi_so(self, khoa: Any) -> int:\n        s = str(khoa)\n        return (ord(s[0]) if s else 0) % len(self._o)\n\n\nn = 4_000\nkhoa = [f\"nguoi_dung_{i:05d}\" for i in range(n)]\n\nfor Lop, ten in ((BangBamDonGian, \"hash Python (tốt)\"),\n                 (BangBamHamKem, \"chỉ ký tự đầu (kém)\"),\n                 (BangBamHamXau, \"luôn trả 0 (tệ nhất)\")):\n    b = Lop(so_o_ban_dau=8192)         # cố định số ô để so công bằng\n    t0 = time.perf_counter()\n    for k in khoa:\n        b[k] = 1\n    t_chen = time.perf_counter() - t0\n\n    t0 = time.perf_counter()\n    for k in khoa:\n        _ = b[k]\n    t_tra = time.perf_counter() - t0\n\n    pb = b.phan_bo()\n    o_dung = sum(1 for x in pb if x > 0)\n    print(f\"{ten:>22}: chèn {t_chen:>7.4f}s  tra {t_tra:>7.4f}s  \"\n          f\"ô dùng {o_dung:>5}/{len(pb)}  ô đông nhất {max(pb)}\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\n     hash Python (tốt): chèn ~0.004s  tra ~0.003s  ô dùng ~3200/8192  đông nhất ~5\n   chỉ ký tự đầu (kém): chèn ~0.900s  tra ~0.850s  ô dùng    ~1/8192  đông nhất 4000\n    luôn trả 0 (tệ nhất): chèn ~1.100s  tra ~1.000s  ô dùng    1/8192  đông nhất 4000\n\n⭐ ĐỌC CỘT \"ô đông nhất\":\n  hàm băm tốt  → ~5 phần tử mỗi ô  ⇒ tra cứu gần O(1)\n  hàm băm tệ   → 4000 phần tử một ô ⇒ tra cứu thành O(n)\n\n⇒ Bảng băm THOÁI HOÁ thành danh sách khi hàm băm không phân bố đều.\n  \"O(1) trung bình\" phụ thuộc HOÀN TOÀN vào tính chất này.\n```\n\nChú ý `BangBamHamKem` không phải hàm băm cố tình sai — nó là **lỗi thật** mà người ta hay mắc: chỉ dùng một phần của khoá. Với `nguoi_dung_00001`, `nguoi_dung_00002`, … thì mọi khoá đều bắt đầu bằng `n`, nên tất cả rơi vào một ô.\n\n### Quan sát cấp phát lại\n\n```python\nb = BangBamDonGian(so_o_ban_dau=4)\nprint(f\"{'sau khi chèn':>14} {'số ô':>7} {'hệ số tải':>11}\")\nfor i in range(20):\n    truoc = len(b._o)\n    b[f\"khoa_{i}\"] = i\n    if len(b._o) != truoc:\n        print(f\"{i + 1:>14} {len(b._o):>7} {b.he_so_tai:>10.2f}  ⭐ cấp phát lại\")\nprint(f\"\\ntổng số lần cấp phát lại: {b.so_lan_cap_phat_lai}\")\n```\n\n```\n  sau khi chèn    số ô  hệ số tải\n             4       8       0.50  ⭐ cấp phát lại\n             7      16       0.44  ⭐ cấp phát lại\n            13      32       0.41  ⭐ cấp phát lại\n\ntổng số lần cấp phát lại: 3\n\n⭐ Cấp phát lại xảy ra khi tải vượt 0,75, và sau đó tải TỤT xuống ~0,4.\n  Khoảng cách giữa các lần cấp phát TĂNG DẦN (4, 7, 13, ...)\n  ⇒ đúng dấu hiệu tăng trưởng theo TỈ LỆ ⇒ O(1) khấu hao (module 1).\n```\n\n⚠️ Chi phí của rehashing:\n\n```python\nimport time\n\nb = BangBamDonGian(so_o_ban_dau=8)\nthoi_gian = []\nfor i in range(50_000):\n    t0 = time.perf_counter_ns()\n    b[f\"k{i}\"] = i\n    thoi_gian.append(time.perf_counter_ns() - t0)\n\ncham_nhat = sorted(range(len(thoi_gian)), key=lambda j: -thoi_gian[j])[:5]\nprint(\"5 lần chèn CHẬM NHẤT (đây là các lần cấp phát lại):\")\nfor j in sorted(cham_nhat):\n    print(f\"   lần {j:>6,}: {thoi_gian[j]:>12,} ns\")\n\ntrung_vi = sorted(thoi_gian)[len(thoi_gian) // 2]\nprint(f\"\\ntrung vị: {trung_vi:,} ns\")\nprint(f\"chậm nhất / trung vị = {max(thoi_gian) / trung_vi:,.0f} lần\")\n```\n\n```\n⭐ Các lần chậm đột biến rơi đúng vào lúc bảng nở.\n  Giống hệt hiện tượng ở bài khấu hao (module 1) với list.append.\n\n⇒ dict cũng có ĐỘ TRỄ ĐUÔI — cần p999 ổn định thì phải\n  cấp phát trước bằng cách nào đó, hoặc chấp nhận.\n```\n\n### Hợp đồng `__hash__` và `__eq__`\n\n```python\n# ── ❌ SAI: định nghĩa __eq__ mà quên __hash__ ──\nclass NguoiDungSai1:\n    def __init__(self, ma: str, ten: str) -> None:\n        self.ma, self.ten = ma, ten\n\n    def __eq__(self, other) -> bool:\n        return isinstance(other, NguoiDungSai1) and self.ma == other.ma\n\n\ntry:\n    d = {NguoiDungSai1(\"U1\", \"Minh\"): 1}\nexcept TypeError as e:\n    print(f\"✓ Python CHẶN: {e}\")\n```\n\n```\n✓ Python CHẶN: unhashable type: 'NguoiDungSai1'\n\n⭐ Đây là thiết kế TỐT của Python: định nghĩa __eq__ thì __hash__\n  bị đặt = None, biến một bug IM LẶNG thành LỖI RÕ RÀNG.\n```\n\n```python\n# ── ❌ SAI HƠN: __hash__ KHÔNG NHẤT QUÁN với __eq__ ──\nclass NguoiDungSai2:\n    def __init__(self, ma: str, ten: str) -> None:\n        self.ma, self.ten = ma, ten\n\n    def __eq__(self, other) -> bool:\n        return isinstance(other, NguoiDungSai2) and self.ma == other.ma\n\n    def __hash__(self) -> int:\n        return hash((self.ma, self.ten))    # ⚠️ dùng CẢ ten, còn __eq__ thì không\n\n\na = NguoiDungSai2(\"U1\", \"Minh\")\nb = NguoiDungSai2(\"U1\", \"Minh Nguyen\")     # cùng ma, KHÁC ten\n\nprint(f\"a == b        → {a == b}\")          # True  (chỉ so ma)\nprint(f\"hash(a)==hash(b) → {hash(a) == hash(b)}\")   # False ⚠️ VI PHẠM HỢP ĐỒNG\n\nd = {a: \"giá trị của a\"}\nprint(f\"b in d        → {b in d}\")          # ⚠️ False, dù a == b\nprint(f\"len(d)        → {len(d)}\")\nd[b] = \"giá trị của b\"\nprint(f\"sau khi thêm b, len(d) = {len(d)}\")  # ⚠️ 2 — hai mục cho hai object BẰNG NHAU\n```\n\n```\na == b        → True\nhash(a)==hash(b) → False    ⚠️ VI PHẠM\nb in d        → False\nsau khi thêm b, len(d) = 2  ⚠️ dict chứa hai khoá \"bằng nhau\"\n\n⇒ Đây là bug IM LẶNG — không exception, chỉ là kết quả sai.\n  Loại tệ nhất theo bài xử lý lỗi (module 8 mảng 01).\n```\n\n```python\n# ── ✓ ĐÚNG: nhất quán ──\nclass NguoiDungDung:\n    def __init__(self, ma: str, ten: str) -> None:\n        self.ma, self.ten = ma, ten\n\n    def __eq__(self, other) -> bool:\n        return isinstance(other, NguoiDungDung) and self.ma == other.ma\n\n    def __hash__(self) -> int:\n        return hash(self.ma)                 # ⭐ CÙNG thuộc tính với __eq__\n\n    def __repr__(self) -> str:\n        return f\"NguoiDungDung({self.ma!r}, {self.ten!r})\"\n\n\na = NguoiDungDung(\"U1\", \"Minh\")\nb = NguoiDungDung(\"U1\", \"Minh Nguyen\")\nassert a == b\nassert hash(a) == hash(b)                    # ⭐ hợp đồng thoả\n\nd = {a: \"giá trị\"}\nassert b in d                                 # ⭐ tìm thấy\nd[b] = \"cập nhật\"\nassert len(d) == 1                            # ⭐ MỘT khoá\n```\n\n```python\n# ── ⭐ ĐƠN GIẢN NHẤT: dataclass frozen ──\nfrom dataclasses import dataclass, field\n\n\n@dataclass(frozen=True)                       # ⭐ tự sinh CẢ __eq__ lẫn __hash__\nclass Diem:\n    x: int\n    y: int\n\n\np1, p2 = Diem(1, 2), Diem(1, 2)\nassert p1 == p2 and hash(p1) == hash(p2)      # ⭐ nhất quán tự động\nassert len({p1, p2}) == 1\n\n# Chọn trường nào tham gia hash/eq\n@dataclass(frozen=True)\nclass SanPham:\n    sku: str\n    ten: str = field(compare=False)           # ⭐ KHÔNG tham gia __eq__/__hash__\n\n\ns1 = SanPham(\"A1\", \"Bút bi\")\ns2 = SanPham(\"A1\", \"Bút bi xanh\")\nassert s1 == s2 and hash(s1) == hash(s2)      # ⭐ chỉ so sku, vẫn nhất quán\nassert len({s1, s2}) == 1\n```\n\n```\n⭐ @dataclass(frozen=True) là cách ĐÚNG và AN TOÀN nhất:\n   Python tự sinh __eq__ và __hash__ từ CÙNG bộ trường\n   ⇒ không thể vi phạm hợp đồng.\n```\n\n### Khả biến làm khoá — hậu quả\n\n```python\n# ── list KHÔNG hashable ──\ntry:\n    d = {[1, 2]: \"giá trị\"}\nexcept TypeError as e:\n    print(f\"✓ {e}\")\n\n# ── tuple thì được... nếu phần tử hashable ──\nd = {(1, 2): \"ok\"}\nassert d[(1, 2)] == \"ok\"\n\ntry:\n    d2 = {(1, [2]): \"x\"}                     # ⚠️ list bên trong tuple\nexcept TypeError as e:\n    print(f\"✓ {e}\")\n```\n\nChứng minh vì sao khả biến nguy hiểm — dùng dataclass **không** frozen nhưng tự viết `__hash__`:\n\n```python\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass GioHangNguyHiem:\n    ma: str\n    so_luong: int\n\n    def __hash__(self) -> int:                # ⚠️ hash dựa trên trường THAY ĐỔI ĐƯỢC\n        return hash((self.ma, self.so_luong))\n\n\ng = GioHangNguyHiem(\"A1\", 2)\nd = {g: \"đơn hàng 1\"}\nprint(f\"trước khi sửa: g in d → {g in d}\")    # True\n\ng.so_luong = 5                                # ⚠️ SỬA object đang làm khoá\nprint(f\"sau khi sửa  : g in d → {g in d}\")    # ⚠️ False!\nprint(f\"nhưng len(d) = {len(d)}\")             # 1 — dữ liệu VẪN NẰM ĐÓ\nprint(f\"list(d.keys()) = {list(d.keys())}\")   # thấy nó, nhưng không tra được\n```\n\n```\ntrước khi sửa: g in d → True\nsau khi sửa  : g in d → False    ⚠️\nnhưng len(d) = 1\nlist(d.keys()) = [GioHangNguyHiem(ma='A1', so_luong=5)]\n\n⚠️ Object VẪN nằm trong dict nhưng KHÔNG TRA ĐƯỢC.\n  Vì hash đổi ⇒ tính ra ô khác ⇒ tìm không thấy.\n\n⇒ Đây chính là lý do khoá phải BẤT BIẾN.\n  Và là lý do @dataclass thường KHÔNG sinh __hash__.\n```\n\n### Bẫy `True == 1` khi đếm tần suất\n\n```python\nfrom collections import Counter\n\ndu_lieu = [1, True, 1.0, 0, False, 0.0, \"1\"]\ndem = Counter(du_lieu)\nprint(dem)\n```\n\n```\nCounter({1: 3, 0: 3, '1': 1})\n\n⚠️ 1, True, 1.0 gộp thành MỘT khoá (đếm 3)\n⚠️ 0, False, 0.0 gộp thành MỘT khoá (đếm 3)\n⭐ \"1\" (chuỗi) là khoá RIÊNG\n\n⇒ Vì 1 == True == 1.0 nên chúng CÙNG hash và CÙNG khoá.\n```\n\n```python\n# ✓ Nếu cần phân biệt kiểu: đưa kiểu vào khoá\ndem_theo_kieu = Counter((type(x).__name__, x) for x in du_lieu)\nfor (kieu, gia_tri), so_lan in sorted(dem_theo_kieu.items(), key=str):\n    print(f\"{kieu:>5} {gia_tri!r:>6} → {so_lan}\")\n```\n\n```\n bool  False → 1\n bool   True → 1\nfloat    0.0 → 1\nfloat    1.0 → 1\n  int      0 → 1\n  int      1 → 1\n  str    '1' → 1\n\n⭐ Giờ mỗi kiểu là một khoá riêng.\n```\n\n### Thứ tự: `dict` giữ, `set` thì không\n\n```python\nd = {}\nfor ten in (\"cuong\", \"an\", \"minh\", \"binh\"):\n    d[ten] = 1\nprint(f\"dict: {list(d)}\")               # ⭐ ĐÚNG thứ tự chèn\n\ns = set()\nfor ten in (\"cuong\", \"an\", \"minh\", \"binh\"):\n    s.add(ten)\nprint(f\"set : {list(s)}\")               # ⚠️ thứ tự tuỳ hash, KHÔNG đảm bảo\n```\n\n```\ndict: ['cuong', 'an', 'minh', 'binh']    ⭐ đảm bảo từ Python 3.7\nset : ['minh', 'an', 'binh', 'cuong']    ⚠️ đổi giữa các lần chạy\n\n⇒ dict có tầng 2 ghi theo thứ tự chèn; set thì không.\n⇒ Cần thứ tự ổn định với set: dùng sorted(s) hoặc dict.fromkeys()\n```\n\n```python\n# ✓ Loại trùng mà GIỮ thứ tự — dùng dict thay set\nds = [3, 1, 3, 2, 1]\nassert list(dict.fromkeys(ds)) == [3, 1, 2]      # ⭐ giữ thứ tự\n# set(ds) → không đảm bảo thứ tự\n```\n\n### Bộ nhớ: `dict` compact so với thiết kế cũ\n\n```python\nimport sys\n\nfor n in (0, 1, 5, 10, 100, 1_000):\n    d = {i: i for i in range(n)}\n    s = {i for i in range(n)}\n    l = list(range(n))\n    print(f\"n={n:>5}  dict {sys.getsizeof(d):>8,}  \"\n          f\"set {sys.getsizeof(s):>8,}  list {sys.getsizeof(l):>8,} byte\")\n```\n\n```\nn=    0  dict       64  set      216  list       56 byte\nn=    1  dict      224  set      216  list       64 byte\nn=    5  dict      224  set      728  list      120 byte\nn=   10  dict      352  set      728  list      184 byte\nn=  100  dict    4,704  set    8,408  list      856 byte\nn= 1000  dict   36,960  set   32,984  list    8,856 byte\n\n⭐ dict và set tốn NHIỀU HƠN list vài lần — đó là cái giá của O(1) tra cứu.\n  (Bài 3 của module này sẽ phân tích kỹ đánh đổi này.)\n\n⚠️ getsizeof chỉ đo VỎ, không đo các object bên trong.\n```\n\n### Hai loại hàm băm — đừng nhầm\n\n```python\nimport hashlib\nimport time\n\ns = \"mật khẩu của tôi\"\nn = 100_000\n\n# ① hash() — cho bảng băm\nt0 = time.perf_counter()\nfor _ in range(n):\n    _ = hash(s)\nt_hash = time.perf_counter() - t0\n\n# ② sha256 — băm mật mã\nt0 = time.perf_counter()\nfor _ in range(n):\n    _ = hashlib.sha256(s.encode()).hexdigest()\nt_sha = time.perf_counter() - t0\n\nprint(f\"hash()  : {t_hash:>7.4f}s\")\nprint(f\"sha256(): {t_sha:>7.4f}s   chậm hơn {t_sha/t_hash:.0f} lần\")\n```\n\n```\nhash()  :  ~0.005s\nsha256(): ~0.080s   chậm hơn ~16 lần\n\n⭐ sha256 CHẬM HƠN CÓ CHỦ Ý — nó phải chống giả mạo.\n  hash() nhanh vì chỉ cần phân bố đều.\n```\n\n```python\n# ❌ TUYỆT ĐỐI KHÔNG dùng hash() để lưu mật khẩu\n# mat_khau_luu = hash(mat_khau)      ⚠️ không an toàn, và đổi mỗi lần chạy\n\n# ✓ Dùng hàm băm mật khẩu chuyên dụng (bcrypt, argon2, scrypt)\n#   — chúng còn CỐ Ý CHẬM để chống thử vét cạn.\n#   Chi tiết ở bài Auth & bảo mật, mảng 05.\n```\n\n### Tự viết hàm băm cho chuỗi\n\nĐể thấy vì sao \"trộn bit\" quan trọng:\n\n```python\ndef hash_cong_don(s: str) -> int:\n    \"\"\"❌ Chỉ CỘNG mã ký tự — phân bố rất tệ.\"\"\"\n    tong = 0\n    for c in s:\n        tong += ord(c)\n    return tong\n\n\ndef hash_djb2(s: str) -> int:\n    \"\"\"✓ Thuật toán djb2 — nhân rồi cộng, TRỘN các bit.\"\"\"\n    h = 5381\n    for c in s:\n        h = ((h * 33) + ord(c)) & 0xFFFFFFFF     # ⭐ nhân 33 rồi cộng\n    return h\n\n\ndef do_phan_bo(ham_bam, khoa: list[str], so_o: int) -> dict:\n    o = [0] * so_o\n    for k in khoa:\n        o[ham_bam(k) % so_o] += 1\n    dung = sum(1 for x in o if x > 0)\n    return {\n        \"ô dùng\": f\"{dung}/{so_o}\",\n        \"đông nhất\": max(o),\n        \"trung bình ô có dùng\": round(len(khoa) / max(1, dung), 1),\n    }\n\n\nkhoa = [f\"nguoi_dung_{i:04d}\" for i in range(2_000)]\nso_o = 1024\n\nfor ten, f in ((\"cộng dồn\", hash_cong_don),\n               (\"djb2\", hash_djb2),\n               (\"hash() Python\", hash)):\n    print(f\"{ten:>16}: {do_phan_bo(f, khoa, so_o)}\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\n        cộng dồn: {'ô dùng': '~40/1024', 'đông nhất': ~120, ...}   ⚠️ RẤT tệ\n            djb2: {'ô dùng': '~650/1024', 'đông nhất': ~8, ...}    ⭐ tốt\n   hash() Python: {'ô dùng': '~650/1024', 'đông nhất': ~8, ...}    ⭐ tốt\n\n⚠️ VÌ SAO \"cộng dồn\" tệ:\n   \"ab\" và \"ba\" cho CÙNG tổng ⇒ cùng ô\n   các khoá chỉ khác vài chữ số cuối ⇒ tổng gần nhau ⇒ dồn cụm\n\n⭐ djb2 NHÂN với 33 mỗi bước ⇒ vị trí ký tự ảnh hưởng kết quả\n   ⇒ \"ab\" và \"ba\" cho hash KHÁC NHAU ⇒ phân bố đều hơn hẳn\n```\n\n```python\n# Chứng minh trực tiếp\nassert hash_cong_don(\"ab\") == hash_cong_don(\"ba\")      # ⚠️ va chạm chắc chắn\nassert hash_djb2(\"ab\") != hash_djb2(\"ba\")              # ⭐ phân biệt được\nprint(f'cộng dồn: \"ab\"={hash_cong_don(\"ab\")}, \"ba\"={hash_cong_don(\"ba\")}')\nprint(f'djb2    : \"ab\"={hash_djb2(\"ab\")}, \"ba\"={hash_djb2(\"ba\")}')\n```\n\n### Bảng tra: cái gì hashable\n\n```python\nMAU = [\n    42, 3.14, \"chuỗi\", b\"bytes\", True, None,\n    (1, 2), frozenset([1, 2]),\n    [1, 2], {1: 2}, {1, 2}, bytearray(b\"x\"),\n    (1, [2]),                                # ⚠️ tuple chứa list\n]\n\nfor x in MAU:\n    try:\n        h = hash(x)\n        print(f\"✓ {type(x).__name__:>10} {str(x)[:18]:>20}  hashable\")\n    except TypeError:\n        print(f\"✗ {type(x).__name__:>10} {str(x)[:18]:>20}  KHÔNG hashable\")\n```\n\n```\n✓        int                   42  hashable\n✓      float                 3.14  hashable\n✓        str                chuỗi  hashable\n✓      bytes             b'bytes'  hashable\n✓       bool                 True  hashable\n✓ NoneType                  None  hashable\n✓      tuple               (1, 2)  hashable\n✓  frozenset    frozenset({1, 2})  hashable\n✗       list               [1, 2]  KHÔNG hashable\n✗       dict             {1: 2}    KHÔNG hashable\n✗        set               {1, 2}  KHÔNG hashable\n✗  bytearray      bytearray(b'x')  KHÔNG hashable\n✗      tuple            (1, [2])   KHÔNG hashable   ⭐ vì chứa list\n\n⇒ QUY TẮC: hashable ⟺ BẤT BIẾN (và mọi phần tử bên trong cũng bất biến)\n```",

so_sanh: "### Hai bước biến khoá thành chỉ số\n\n| Bước | Việc | Kết quả |\n|---|---|---|\n| ① `hash(khoá)` | dữ liệu bất kỳ → số nguyên | số 64 bit, có thể âm |\n| ② thu về phạm vi | `% n` hoặc `& (n−1)` | chỉ số `0..n−1` |\n| ③ | truy cập mảng | **O(1) — công thức địa chỉ** |\n\n**\"O(1) trung bình\" của `dict` chính là O(1) của mảng**, cộng chi phí tính hash.\n\n### Ba tính chất của hàm băm tốt\n\n| Tính chất | Nghĩa | Thiếu thì sao |\n|---|---|---|\n| **Xác định** | cùng đầu vào → cùng kết quả | không tra lại được |\n| **Phân bố đều** | rải đều trên các ô | **⚠️ bảng thoái hoá thành danh sách** |\n| **Nhanh** | O(độ dài khoá) | mất lợi thế so với so sánh trực tiếp |\n\nTính chất thứ hai là quan trọng nhất — nó là **giả định** của lời hứa O(1).\n\n### Hai cách thu về phạm vi\n\n| | `% n` (chia lấy dư) | `& (n−1)` (mặt nạ bit) |\n|---|---|---|\n| Tốc độ | chậm hơn | **⭐ rất nhanh** |\n| Yêu cầu về `n` | bất kỳ (nên là số nguyên tố) | **luỹ thừa của 2** |\n| Dùng bit nào của hash | toàn bộ | **chỉ bit thấp** ⚠️ |\n| Python dùng | ✗ | **✓** |\n\nVì chỉ dùng bit thấp, hàm băm phải **trộn** bit chứ không chỉ cộng dồn.\n\n### Hợp đồng `__hash__` / `__eq__`\n\n| Mệnh đề | Bắt buộc |\n|---|---|\n| `a == b` ⇒ `hash(a) == hash(b)` | **✓ BẮT BUỘC** |\n| `hash(a) == hash(b)` ⇒ `a == b` | ✗ không (đó là va chạm) |\n\n**Quy tắc:** `__hash__` và `__eq__` phải dùng **cùng bộ thuộc tính**.\n\n### Bốn cách định nghĩa và hậu quả\n\n| Định nghĩa | Kết quả |\n|---|---|\n| Không định nghĩa gì | hash theo `id()` — mỗi instance là khoá riêng |\n| **Chỉ `__eq__`** | **⭐ Python đặt `__hash__ = None`** ⇒ `TypeError` rõ ràng |\n| Cả hai, **không nhất quán** | **⚠️ bug IM LẶNG** — `dict` chứa hai khoá \"bằng nhau\" |\n| `@dataclass(frozen=True)` | **⭐ tự sinh cả hai, nhất quán** |\n\nHàng hai là thiết kế **tốt** của Python: biến bug im lặng thành lỗi rõ ràng.\n\n### Hashable ⟺ bất biến\n\n| Hashable | Không hashable |\n|---|---|\n| `int`, `float`, `str`, `bytes` | `list` |\n| `bool`, `None` | `dict` |\n| `tuple` (nếu **mọi phần tử** hashable) | `set` |\n| `frozenset` | `bytearray` |\n| `@dataclass(frozen=True)` | `@dataclass` thường |\n| class tự viết (mặc định) | class chỉ có `__eq__` |\n\n⚠️ `(1, [2])` **không** hashable — `tuple` chứa `list`.\n\n### Hệ số tải\n\n| Hệ số tải | Va chạm | Bộ nhớ |\n|---|---|---|\n| Thấp (0,2) | ít | **lãng phí** |\n| Vừa (0,5–0,7) | **⭐ cân bằng** | chấp nhận được |\n| Cao (0,9) | **nhiều ⇒ chậm** | tiết kiệm |\n\nCPython cấp phát lại khi bảng đầy khoảng **2/3**.\n\n### Cấp phát lại: mảng động và bảng băm\n\n| | Mảng động (`list`) | Bảng băm (`dict`) |\n|---|---|---|\n| Khi nào | hết chỗ | **vượt hệ số tải** |\n| Làm gì | copy sang mảng lớn hơn | **⚠️ TÍNH LẠI chỉ số mọi khoá** |\n| Vì sao khác | — | chỉ số phụ thuộc `n`, `n` đổi thì chỉ số đổi |\n| Bậc | O(n) một lần, O(1) khấu hao | như bên trái |\n| Độ trễ đuôi | có đỉnh nhọn | **có đỉnh nhọn** |\n\n### `dict` compact — hai tầng\n\n| Tầng | Chứa gì | Lợi ích |\n|---|---|---|\n| 1 — mảng chỉ số | số nguyên nhỏ, thưa | **tiết kiệm ~20–25% bộ nhớ** |\n| 2 — mảng mục | `(hash, khoá, giá trị)` theo **thứ tự chèn** | **⭐ `dict` giữ thứ tự chèn** |\n\n| | `dict` | `set` |\n|---|---|---|\n| Giữ thứ tự chèn | **⭐ ✓ (đảm bảo từ 3.7)** | **✗ không đảm bảo** |\n\nCần loại trùng mà giữ thứ tự ⇒ `dict.fromkeys(ds)`, không phải `set(ds)`.\n\n### Ngẫu nhiên hoá hash\n\n| | Hệ quả |\n|---|---|\n| `hash(\"abc\")` giữa hai lần chạy | **khác nhau** |\n| Trong một lần chạy | không đổi |\n| Lý do | **chống tấn công va chạm** (bài 5) |\n| ⚠️ Không được | lưu `hash(str)` xuống file/DB |\n| Cần hash ổn định | dùng `hashlib` |\n| Tắt để gỡ rối | `PYTHONHASHSEED=0` |\n\n### Hai loại hàm băm\n\n| | Băm cho bảng băm | Băm mật mã |\n|---|---|---|\n| Mục tiêu | **nhanh, phân bố đều** | **chống giả mạo** |\n| Tốc độ | rất nhanh | **chậm có chủ ý** |\n| Ổn định giữa các lần chạy | ✗ (Python) | **✓** |\n| Ví dụ | `hash()` | `sha256`, `bcrypt` |\n| ❌ Dùng sai | lưu mật khẩu bằng `hash()` | dùng `sha256` làm khoá `dict` |",

loi_thuong_gap: [
  "**Định nghĩa `__eq__` mà quên `__hash__`.** Python đặt `__hash__ = None` ⇒ `TypeError` khi làm khoá. Đây là thiết kế tốt, không phải lỗi.",
  "**`__hash__` và `__eq__` dùng khác bộ thuộc tính.** Bug im lặng: `dict` chứa hai khoá \"bằng nhau\", `in` trả `False` dù `==` là `True`.",
  "**Dùng object khả biến làm khoá rồi sửa nó.** Object vẫn nằm trong `dict` nhưng không tra được — dữ liệu \"biến mất\".",
  "**Tưởng `tuple` luôn hashable.** `(1, [2])` không hashable vì chứa `list`.",
  "**Quên `True == 1 == 1.0`.** Ba giá trị này là **cùng một khoá** `dict` — đếm tần suất trên dữ liệu trộn kiểu sẽ gộp chúng.",
  "**Lưu `hash(str)` xuống file hoặc database.** Python ngẫu nhiên hoá hash chuỗi; lần chạy sau ra giá trị khác. Dùng `hashlib`.",
  "**Tưởng `set` giữ thứ tự chèn như `dict`.** Chỉ `dict` có đảm bảo đó; loại trùng giữ thứ tự thì dùng `dict.fromkeys`.",
  "**Viết hàm băm chỉ cộng dồn mã ký tự.** `\"ab\"` và `\"ba\"` cùng hash ⇒ phân bố tệ; phải **trộn** bit như djb2.",
  "**Viết hàm băm chỉ dùng một phần khoá.** Với khoá dạng `nguoi_dung_00001` thì mọi khoá vào cùng một ô.",
  "**Dùng `hash()` để lưu mật khẩu.** Nó nhanh có chủ ý và không ổn định — phải dùng bcrypt/argon2.",
  "**Dùng `sha256` làm khoá `dict`.** Chậm hơn khoảng 16 lần mà không được lợi gì.",
  "**Quên rằng bảng băm phải lưu CẢ khoá.** Cần `__eq__` để xác nhận đúng khoá sau khi tìm đúng ô.",
  "**Tưởng cấp phát lại của `dict` chỉ là copy.** Phải **tính lại chỉ số** cho mọi khoá vì `n` đổi.",
  "**Bỏ qua độ trễ đuôi của `dict`.** Lúc bảng nở có lần chèn chậm đột biến, giống `list.append`."
],

tu_kiem_tra: [
  "Nêu hai bước biến một khoá thành chỉ số mảng. Bước nào là \"phép màu\"?",
  "Vì sao nói \"O(1) của `dict` chính là O(1) của mảng\"?",
  "Nêu ba tính chất của hàm băm tốt. Tính chất nào quan trọng nhất và vì sao?",
  "Chuyện gì xảy ra nếu hàm băm trả cùng một giá trị cho mọi khoá?",
  "So sánh hai cách thu hash về phạm vi mảng. Python dùng cách nào và hệ quả là gì?",
  "Vì sao va chạm là không tránh được? Liên hệ nguyên lý chuồng bồ câu.",
  "Vì sao bảng băm phải lưu cả khoá chứ không chỉ giá trị?",
  "Hệ số tải là gì? Cao và thấp đánh đổi gì?",
  "Cấp phát lại của bảng băm khác mảng động ở điểm nào? Vì sao?",
  "Phát biểu hợp đồng `__hash__`/`__eq__`. Chiều nào bắt buộc, chiều nào không?",
  "Chuyện gì xảy ra nếu `a == b` nhưng `hash(a) != hash(b)`?",
  "Vì sao Python đặt `__hash__ = None` khi bạn chỉ định nghĩa `__eq__`? Đó là lỗi hay tính năng?",
  "Vì sao chỉ đối tượng bất biến làm khoá được? Mô tả hậu quả nếu vi phạm.",
  "`tuple` luôn hashable phải không? Cho ví dụ ngược lại.",
  "Vì sao `d[1]`, `d[1.0]`, `d[True]` là cùng một khoá?",
  "Mô tả cấu trúc hai tầng của `dict` Python. Hai lợi ích của nó?",
  "Vì sao `dict` giữ thứ tự chèn còn `set` thì không?",
  "Vì sao Python ngẫu nhiên hoá hash của chuỗi? Nêu ba hệ quả thực tế.",
  "Phân biệt băm cho bảng băm và băm mật mã. Nêu hai lỗi khi nhầm lẫn.",
  "Vì sao hàm băm \"cộng dồn mã ký tự\" là tệ? djb2 khắc phục thế nào?"
],

tai_nguyen: [
  "Sách **Introduction to Algorithms** — CLRS. Chương 11 về bảng băm: hàm băm, phân tích, và chứng minh về hệ số tải.",
  "**Mã nguồn CPython — `Objects/dictobject.c`** — đọc phần đầu file, nơi có ghi chú dài giải thích thiết kế compact dict và chiến lược dò tìm.",
  "**Tài liệu Python — `object.__hash__` và `object.__eq__`** — phát biểu chính thức của hợp đồng băm và quy tắc `__hash__ = None`.",
  "**Tài liệu Python — `hashlib`** — các hàm băm mật mã, và ghi chú vì sao không dùng chúng cho mật khẩu (nên dùng `scrypt`/`bcrypt`).",
  "**PEP 456 — Secure and interchangeable hash algorithm** — lý do và cơ chế ngẫu nhiên hoá hash trong Python."
],

lien_he: [
  "**Array và dynamic array** ở module 2 mảng này: bảng băm xây **trên** mảng; O(1) của nó là O(1) của mảng.",
  "**Phân tích khấu hao** ở module 1 mảng này: cấp phát lại theo tỉ lệ cho O(1) khấu hao — cùng lập luận, thêm bước tính lại chỉ số.",
  "**Xử lý va chạm** ngay sau: chaining dùng linked list (module 2), open addressing dùng dò tìm trên mảng.",
  "**Hash map, hash set** trong module này: đánh đổi bộ nhớ lấy tốc độ, phân tích kỹ hơn.",
  "**Vì sao O(1) chỉ là trung bình** cuối module: điều gì phá vỡ giả định phân bố đều, và tấn công va chạm.",
  "**Mutable vs immutable** ở module 7 mảng 01: nền của quy tắc \"hashable ⟺ bất biến\".",
  "**Magic method** ở module 6 mảng 01: `__hash__` và `__eq__` là hai phương thức của giao thức băm.",
  "**Dataclass** ở module 6 mảng 01: `frozen=True` là cách an toàn nhất để có khoá đúng.",
  "**Xử lý lỗi tường minh** ở module 8 mảng 01: `__hash__` không nhất quán tạo bug im lặng — loại tệ nhất.",
  "**Vòng lặp lồng nhau** ở module 1 mảng này: mọi lần \"đổi list thành set\" đều dựa trên cơ chế của bài này.",
  "**Hiệu năng** ở mảng 04: chỉ mục băm của database dùng cùng nguyên lý, nhưng trên đĩa.",
  "**Auth & bảo mật** ở mảng 05: vì sao mật khẩu cần hàm băm cố ý chậm, không phải `hash()`.",
  "**Các khối xây dựng** ở mảng 06: consistent hashing để phân mảnh dữ liệu — mở rộng của ý tưởng này."
]

};
