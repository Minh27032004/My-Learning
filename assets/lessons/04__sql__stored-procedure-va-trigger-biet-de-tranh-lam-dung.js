window.LESSONS["04:sql:stored-procedure-va-trigger-biet-de-tranh-lam-dung"] = {

tom_tat: [
  "Function, procedure và trigger đều chạy code gần dữ liệu nhưng có hợp đồng khác nhau: function trả giá trị và dùng trong biểu thức; procedure được gọi bằng `CALL`; trigger tự chạy theo sự kiện trên bảng hoặc view.",
  "Stored procedure hữu ích khi một thao tác dữ liệu nhiều bước cần nằm sát database, song nó không tự nhanh hơn SQL thường và dễ tạo API ẩn nếu quyền, transaction, lỗi và version không được thiết kế.",
  "Trigger hợp với constraint liên bảng khó diễn tả, audit tối giản hoặc đồng bộ kỹ thuật bắt buộc; nó nguy hiểm khi chứa workflow, gọi mạng, truy vấn theo từng hàng hoặc hiệu ứng khó thấy.",
  "Trong PostgreSQL, row-level trigger chạy một lần cho mỗi hàng còn statement-level trigger chạy một lần cho câu lệnh; transition table giúp xử lý tập hàng thay vì N truy vấn lặp.",
  "Ưu tiên constraint khai báo cho bất biến đơn giản, câu SQL rõ cho biến đổi tập hợp, service cho điều phối bên ngoài; chỉ chọn procedure/trigger khi lợi ích sở hữu dữ liệu lớn hơn chi phí ẩn."
],

can_biet_truoc: [
  "Biết transaction, lock hàng, `COMMIT`, `ROLLBACK` và hiện tượng hai phiên ghi đồng thời.",
  "Biết `INSERT`, `UPDATE`, `DELETE`, constraint, khóa ngoại và `RETURNING` trong PostgreSQL.",
  "Biết function SQL ở mức khái niệm, biến, điều kiện và exception trong ngôn ngữ thủ tục.",
  "Đã theo dõi schema bán vé gồm sự kiện, khách hàng, vé và thanh toán từ hai bài trước."
],

dinh_nghia: "**Stored procedure** là chương trình được đặt tên và lưu trong database, được thực thi tường minh bằng `CALL`. Trong PostgreSQL, procedure khác function ở giao diện gọi và khả năng điều khiển transaction trong những bối cảnh hợp lệ; function được gọi trong biểu thức SQL và trả một giá trị hoặc tập hàng.\n\n**Trigger** là cơ chế đăng ký một trigger function để PostgreSQL tự gọi khi sự kiện như `INSERT`, `UPDATE`, `DELETE` hoặc `TRUNCATE` xảy ra. Trigger có thời điểm `BEFORE`, `AFTER`, `INSTEAD OF`, và mức `FOR EACH ROW` hoặc `FOR EACH STATEMENT`. Điểm quyết định là tính tự động: caller không cần và đôi khi không biết code phụ đang chạy.",

vi_sao_can: "Thanh toán vé gồm nhiều bước phụ thuộc: khóa vé, xác nhận số tiền, ghi thanh toán và chuyển trạng thái. Nếu từng client tự viết bốn câu lệnh, một client có thể quên lock hoặc xử lý retry khác. Một routine trong database có thể cung cấp một điểm vào, chạy gần dữ liệu và bảo vệ cùng quy tắc cho nhiều caller.\n\nMặt khác, tính ẩn của trigger từng làm nhiều hệ thống khó cứu: một `UPDATE` tưởng chỉ đổi trạng thái lại chèn audit, cập nhật bảng tổng hợp, gọi tiếp trigger khác và giữ lock lâu. Khi sự cố xảy ra, log ứng dụng chỉ thấy câu lệnh ban đầu. Vì vậy mục tiêu bài không phải khuyến khích nhét nghiệp vụ vào database, mà cung cấp tiêu chí nhận ra phần nhỏ nào thật sự đáng đặt ở đó và cách chứng minh nó đúng dưới concurrency, thao tác nhiều hàng và lỗi.",

co_che: "### Phân biệt function, procedure và trigger function\n\nPostgreSQL dùng `CREATE FUNCTION` cho hàm gọi trong `SELECT`, cho trigger function và nhiều mục đích mở rộng. Procedure được tạo bằng `CREATE PROCEDURE` rồi gọi bằng `CALL`. Trigger không chứa body trực tiếp; `CREATE TRIGGER` liên kết sự kiện với một function `RETURNS trigger`.\n\n| Đối tượng | Cách chạy | Giá trị trả về | Tính hiển thị với caller | Dùng phù hợp |\n|---|---|---|---|---|\n| Function | `SELECT f(...)` hoặc trong biểu thức | Có | Tường minh trong SQL | Tính toán, truy vấn đóng gói |\n| Procedure | `CALL p(...)` | Tham số `OUT` nếu thiết kế | Tường minh | Thao tác nhiều bước, công việc quản trị |\n| Trigger | Tự chạy khi DML xảy ra | Trigger function trả `NEW`, `OLD` hoặc `NULL` theo loại | Ẩn sau bảng | Bất biến/audit gần dữ liệu |\n\nKhông nên gọi mọi routine là “stored procedure” rồi bỏ qua khác biệt. Function được gọi từ một truy vấn không được tự ý commit transaction của truy vấn đó. Procedure có thể dùng transaction control chỉ khi được gọi trong ngữ cảnh cho phép; nếu `CALL` nằm trong transaction block do client mở hoặc call stack có câu lệnh xen vào, `COMMIT` bên trong có thể bị cấm. Thiết kế API cần chọn rõ ai sở hữu ranh giới transaction.\n\n### Một procedure xác nhận thanh toán\n\nTa muốn ghi nhận callback thanh toán theo `provider_ref` duy nhất và không bán lại vé đã thanh toán. Ví dụ tối giản:\n\n```sql\nCREATE OR REPLACE PROCEDURE confirm_ticket_payment(\n    p_ticket_id bigint,\n    p_provider_ref text,\n    p_amount_cents integer\n)\nLANGUAGE plpgsql\nSECURITY INVOKER\nAS $$\nDECLARE\n    v_ticket tickets%ROWTYPE;\nBEGIN\n    SELECT * INTO STRICT v_ticket\n    FROM tickets\n    WHERE ticket_id = p_ticket_id\n    FOR UPDATE;\n\n    IF v_ticket.status = 'paid' THEN\n        -- Retry cùng provider được unique constraint phân biệt bên dưới.\n        RAISE EXCEPTION 'ticket % is already paid', p_ticket_id;\n    END IF;\n\n    IF v_ticket.status <> 'held' THEN\n        RAISE EXCEPTION 'ticket % is not held', p_ticket_id;\n    END IF;\n\n    IF v_ticket.price_cents <> p_amount_cents THEN\n        RAISE EXCEPTION 'amount does not match ticket price';\n    END IF;\n\n    INSERT INTO ticket_payments\n      (ticket_id, provider_ref, amount_cents, payment_status, paid_at)\n    VALUES\n      (p_ticket_id, p_provider_ref, p_amount_cents, 'succeeded', clock_timestamp());\n\n    UPDATE tickets\n    SET status = 'paid', purchased_at = clock_timestamp()\n    WHERE ticket_id = p_ticket_id;\nEND;\n$$;\n```\n\n`FOR UPDATE` khóa hàng vé, khiến hai callback cho cùng vé không đồng thời vượt kiểm tra. Unique constraint trên `provider_ref` vẫn là lớp bảo vệ idempotency ở dữ liệu. Procedure không gọi `COMMIT`, nên caller có thể đặt `CALL` cùng việc khác trong một transaction và toàn bộ rollback khi exception. Đây thường là hợp đồng dễ suy luận hơn routine tự commit.\n\nVí dụ trên cố ý ném lỗi nếu vé đã paid; hệ thống thật có thể muốn retry cùng `provider_ref` trả thành công. Khi đó phải đọc payment hiện có và so mọi trường quan trọng. Không được coi mọi lỗi unique là retry hợp lệ, vì cùng mã nhà cung cấp nhưng khác vé hoặc số tiền là tín hiệu nguy hiểm.\n\n### Transaction control: quyền năng có điều kiện\n\nProcedure PostgreSQL có thể `COMMIT` hoặc `ROLLBACK` rồi tự bắt đầu transaction mới trong một call top-level phù hợp. Khả năng này hữu ích cho batch quản trị muốn commit từng lô để tránh transaction khổng lồ. Nhưng nó làm caller mất quyền nguyên tử hóa toàn bộ thao tác và có các hạn chế liên quan cursor, exception block, transaction block bên ngoài.\n\n```sql\nCREATE PROCEDURE archive_old_audit_rows(p_before timestamptz)\nLANGUAGE plpgsql\nAS $$\nDECLARE\n    v_deleted integer;\nBEGIN\n    LOOP\n        DELETE FROM ticket_status_audit\n        WHERE audit_id IN (\n            SELECT audit_id\n            FROM ticket_status_audit\n            WHERE changed_at < p_before\n            ORDER BY audit_id\n            LIMIT 10000\n        );\n        GET DIAGNOSTICS v_deleted = ROW_COUNT;\n        COMMIT;\n        EXIT WHEN v_deleted = 0;\n    END LOOP;\nEND;\n$$;\n```\n\nRoutine kiểu này phải được gọi như công việc quản trị độc lập, không nằm trong transaction do framework bao quanh. Nó cũng cần index hỗ trợ `(changed_at, audit_id)` hoặc điều kiện xóa có thể quét bảng mỗi vòng. Kích thước 10.000 chỉ là cấu hình ví dụ, không phải con số tối ưu đã benchmark; phải đo WAL, replication lag, lock và thời gian trên dữ liệu thật.\n\n### Kế hoạch thực thi bên trong routine\n\nPL/pgSQL không biến truy vấn chậm thành nhanh. Các câu SQL vẫn đi qua planner và cần index. Dùng `EXPLAIN (ANALYZE, BUFFERS)` cho chính câu `SELECT`, `UPDATE` hoặc `DELETE` với tham số đại diện. Nếu procedure gọi câu lệnh theo vòng lặp từng vé, hiệu ứng N+1 xảy ra ngay trong database: giảm round trip mạng nhưng vẫn lặp planner/executor và truy cập index N lần.\n\nƯu tiên thao tác tập hợp:\n\n```sql\nUPDATE tickets AS t\nSET status = 'cancelled'\nFROM events AS e\nWHERE e.event_id = t.event_id\n  AND e.starts_at < now()\n  AND t.status = 'held';\n```\n\nMột câu lệnh cho planner cơ hội chọn join và quét phù hợp, thường dễ quan sát hơn vòng `FOR ticket IN SELECT ... LOOP UPDATE`. Vòng lặp chỉ đáng dùng khi mỗi phần tử thật sự có logic không biểu diễn tốt theo tập hợp hoặc cần ranh giới commit riêng.\n\n### Trigger timing: `BEFORE`, `AFTER`, `INSTEAD OF`\n\n`BEFORE ROW` chạy trước khi ghi hàng và có thể sửa `NEW` hoặc trả `NULL` để bỏ qua hàng. Nó phù hợp chuẩn hóa kỹ thuật nhỏ, nhưng âm thầm bỏ hàng dễ gây bất ngờ. `AFTER ROW` thấy thay đổi đã được áp dụng và thường hợp audit; nếu nó ném exception, toàn câu lệnh vẫn rollback. `INSTEAD OF` dùng trên view để thay thế thao tác vốn không cập nhật trực tiếp được.\n\nStatement-level trigger chạy một lần cho câu lệnh, kể cả câu lệnh ảnh hưởng 0 hàng. Row-level trigger chạy theo mỗi hàng. Một update 500.000 hàng sẽ gọi row trigger 500.000 lần; nếu mỗi lần query bảng khác, chi phí và lock tăng mạnh.\n\nTrigger cùng nằm trong transaction của câu lệnh gây ra nó. Không có chuyện audit trigger commit độc lập rồi dữ liệu chính rollback; muốn log thất bại bất kể rollback cần cơ chế ngoài transaction, thường ở ứng dụng hoặc hệ thống log.\n\n### Audit thay đổi trạng thái bằng trigger tối giản\n\nTa chỉ ghi chuyển trạng thái, không sao chép toàn bộ hàng:\n\n```sql\nCREATE TABLE ticket_status_audit (\n    audit_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    ticket_id bigint NOT NULL,\n    old_status text NOT NULL,\n    new_status text NOT NULL,\n    changed_at timestamptz NOT NULL,\n    changed_by text NOT NULL\n);\n\nCREATE FUNCTION audit_ticket_status_change()\nRETURNS trigger\nLANGUAGE plpgsql\nAS $$\nBEGIN\n    INSERT INTO ticket_status_audit(\n        ticket_id, old_status, new_status, changed_at, changed_by\n    ) VALUES (\n        NEW.ticket_id, OLD.status, NEW.status,\n        clock_timestamp(), current_user\n    );\n    RETURN NEW;\nEND;\n$$;\n\nCREATE TRIGGER tickets_status_audit_trg\nAFTER UPDATE OF status ON tickets\nFOR EACH ROW\nWHEN (OLD.status IS DISTINCT FROM NEW.status)\nEXECUTE FUNCTION audit_ticket_status_change();\n```\n\n`IS DISTINCT FROM` xử lý cả `NULL`, dù status hiện `NOT NULL`; cách viết vẫn bền nếu schema thay đổi. `UPDATE OF status` chỉ xét câu lệnh có cột status trong `SET`, còn `WHEN` xác nhận giá trị thực sự đổi. Audit table cố ý không có khóa ngoại tới tickets, vì nếu xóa vé mà audit phải tồn tại, foreign key sẽ chặn hoặc cascade phá lịch sử. Đó là quyết định retention phải ghi rõ, không phải công thức chung.\n\nTrigger làm tăng một insert cho mỗi lần đổi trạng thái, sinh WAL và làm transaction người dùng chậm theo. Index trên audit nên phục vụ tra cứu thật nhưng càng nhiều index càng tăng chi phí ghi. Partition theo thời gian có thể cần khi retention lớn; không nên thêm trước khi biết tải.\n\n### Transition table: suy nghĩ theo tập hàng\n\nPostgreSQL cho `AFTER ... FOR EACH STATEMENT` trigger tham chiếu tập hàng cũ/mới qua transition relation. Điều này hữu ích để audit hoặc tính tác động của bulk update bằng một câu insert thay vì callback từng hàng:\n\n```sql\nCREATE TABLE ticket_bulk_change_log (\n    change_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    changed_at timestamptz NOT NULL DEFAULT clock_timestamp(),\n    changed_by text NOT NULL,\n    affected_rows bigint NOT NULL\n);\n\nCREATE FUNCTION log_ticket_bulk_update()\nRETURNS trigger LANGUAGE plpgsql AS $$\nBEGIN\n    INSERT INTO ticket_bulk_change_log(changed_by, affected_rows)\n    SELECT current_user, count(*) FROM new_rows;\n    RETURN NULL;\nEND;\n$$;\n\nCREATE TRIGGER tickets_bulk_log_trg\nAFTER UPDATE ON tickets\nREFERENCING NEW TABLE AS new_rows\nFOR EACH STATEMENT\nEXECUTE FUNCTION log_ticket_bulk_update();\n```\n\nTransition table có giới hạn về loại trigger và cú pháp; cần kiểm tài liệu cho trường hợp cụ thể. Nó cũng chứa toàn bộ tập chuyển tiếp trong phạm vi câu lệnh, nên bulk cực lớn vẫn tiêu thụ tài nguyên. Lợi ích chính là cho phép SQL tập hợp, không phải biến cập nhật khổng lồ thành miễn phí.\n\n### Thứ tự, recursion và hiệu ứng dây chuyền\n\nMột trigger cập nhật bảng khác có thể kích hoạt trigger của bảng đó; tệ hơn, trigger cập nhật lại chính bảng nguồn và đệ quy đến khi stack lỗi hoặc điều kiện dừng ngẫu nhiên. PostgreSQL có quy tắc thứ tự cho trigger cùng loại, nhưng dựa vào tên để điều phối workflow là thiết kế mong manh.\n\nĐiều kiện `WHEN`, so sánh `OLD`/`NEW` và thiết kế không cập nhật ngược giúp tránh recursion. Có thể xem độ sâu trigger bằng hàm hệ thống, nhưng dùng nó như miếng vá thường che vòng phụ thuộc. Sơ đồ hóa A cập nhật B, B cập nhật C là bắt buộc khi có nhiều trigger.\n\nKhông tạm `DISABLE TRIGGER ALL` trong migration nếu chưa hiểu rằng thao tác có thể tắt cả trigger bảo vệ referential integrity hoặc audit. Tài khoản và session replication role có quyền lực lớn; script bulk load phải validate lại dữ liệu sau đó.\n\n### Exception và thông báo lỗi\n\nPL/pgSQL cho `RAISE EXCEPTION` với SQLSTATE và chi tiết. Caller nên phân biệt lỗi nghiệp vụ có thể hiển thị, conflict cần retry và lỗi hệ thống. Bắt `WHEN OTHERS` rồi nuốt lỗi hoặc trả boolean false làm mất stack, khiến transaction có thể tiếp tục với trạng thái caller không hiểu.\n\nException block tạo subtransaction và có chi phí. Không dùng exception như nhánh điều khiển cho từng hàng nếu có thể kiểm bằng SQL. Đồng thời tránh mô hình “check rồi insert” không constraint: hai phiên có thể cùng thấy chưa tồn tại rồi cùng chèn. Unique constraint mới giải quyết race; exception chỉ chuyển vi phạm thành hợp đồng API có nghĩa.\n\n### `SECURITY DEFINER`, `search_path` và đặc quyền\n\nRoutine mặc định `SECURITY INVOKER`, chạy với quyền caller. `SECURITY DEFINER` chạy với quyền owner, hữu ích để cung cấp thao tác hẹp cho role không có quyền bảng trực tiếp, nhưng trở thành ranh giới bảo mật. Nếu tên đối tượng không schema-qualified và `search_path` cho phép schema do kẻ tấn công kiểm soát đứng trước, routine có thể gọi nhầm hàm hoặc bảng giả.\n\nRoutine đặc quyền nên đặt `search_path` an toàn, qualify đối tượng, thu hồi `EXECUTE` khỏi `PUBLIC` khi cần và chỉ grant cho role cụ thể. Owner không nên là superuser nếu không cần. Input vẫn phải validate; parameter hóa trong SQL tĩnh tránh injection, còn dynamic SQL phải dùng `format('%I', identifier)` cho identifier và `USING` cho giá trị, không nối chuỗi thô.\n\n### Trigger không được gọi mạng\n\nGửi email, gọi cổng thanh toán hoặc publish HTTP từ trigger gắn vòng đời hệ thống ngoài vào transaction database. Mạng chậm giữ lock lâu; timeout làm câu lệnh rollback nhưng bên ngoài có thể đã nhận yêu cầu; retry tạo hiệu ứng trùng. Database cũng khó quan sát và quản lý credential.\n\nMẫu tốt hơn là transactional outbox: trong cùng transaction, chèn một hàng `outbox_events` mô tả `ticket.paid`. Worker bên ngoài đọc, gửi và đánh dấu với idempotency key. Nếu transaction rollback, outbox cũng biến mất; nếu worker lỗi, hàng còn để retry. Có thể để procedure chèn outbox tường minh. Trigger chỉ nên chèn outbox khi mọi đường ghi đều cần sự kiện và đội ngũ chấp nhận tính ẩn đó.\n\n### Version, triển khai và khả năng quan sát\n\nRoutine và trigger là code production: phải lưu trong migration, review diff, test bằng role thật và có rollback tương thích. `CREATE OR REPLACE FUNCTION` không cho đổi tùy tiện signature hay return type; overload có thể khiến phiên bản cũ vẫn tồn tại. Khi xóa, dependency phải được khảo sát như DDL bài trước.\n\nTheo dõi thời gian câu lệnh gọi routine qua `pg_stat_statements`; truy vấn bên trong có thể cần cấu hình theo dõi function và log phù hợp. Một trigger chậm xuất hiện như latency của `UPDATE`, nên runbook phải biết liệt kê trigger bằng catalog và dùng `EXPLAIN (ANALYZE, BUFFERS)` trên câu lệnh thử trong môi trường an toàn.\n\n### Khi nào không nên dùng\n\nKhông dùng trigger để dựng workflow nhiều bước, gọi dịch vụ ngoài, gửi thông báo hoặc che hàng chục cập nhật liên bảng. Không dùng procedure chỉ để tránh viết SQL trong application nếu đổi lại mất type checking, test và observability của stack chính. Không chuyển luật thay đổi theo từng sản phẩm vào database nếu mỗi lần phát hành phải phối hợp migration khó rollback.\n\nConstraint khai báo thắng trigger khi có thể biểu diễn cùng luật: engine hiểu constraint, thông báo dependency rõ và tối ưu được. SQL set-based thắng loop thủ tục cho biến đổi hàng loạt. Service thắng ở điều phối network, timeout, circuit breaker và trải nghiệm lỗi. Procedure/trigger thắng khi quy tắc phải áp dụng cho mọi caller ngay sát dữ liệu, phạm vi nhỏ, deterministic, không I/O ngoài và được đo lường.",

vi_du: "### Kiểm thử đồng thời và ca biên\n\nSau khi tạo procedure, chuẩn bị một vé `held`, rồi mở hai phiên `psql`.\n\n```sql\n-- Phiên A\nBEGIN;\nCALL confirm_ticket_payment(1, 'provider-A', 250000);\n-- Chưa COMMIT để giữ lock hàng.\n```\n\n```sql\n-- Phiên B: câu này chờ lock, rồi sau khi A commit sẽ đọc trạng thái paid và lỗi.\nBEGIN;\nSET LOCAL lock_timeout = '3s';\nCALL confirm_ticket_payment(1, 'provider-B', 250000);\nROLLBACK;\n```\n\nTrong thử nghiệm, có thể để B timeout để chứng minh lock tồn tại; không ghi một con số thời gian chạy như benchmark vì nó phụ thuộc lúc bạn thao tác. Sau đó `COMMIT` A và xác minh chỉ một payment thành công.\n\nKiểm trigger audit bằng update nhiều hàng:\n\n```sql\nUPDATE tickets\nSET status = 'cancelled'\nWHERE status = 'held'\n  AND event_id IN (\n      SELECT event_id FROM events WHERE starts_at < now()\n  );\n\nSELECT ticket_id, old_status, new_status, changed_by\nFROM ticket_status_audit\nORDER BY audit_id DESC\nLIMIT 20;\n```\n\nCác ca biên bắt buộc: update `SET status = status` không tạo audit nhờ `WHEN`; update 0 hàng vẫn gọi statement trigger và ghi `affected_rows = 0` theo định nghĩa hiện tại; rollback update phải rollback audit; xóa vé không được xóa lịch sử nếu audit không có foreign key; bulk update lớn phải đo WAL và thời gian. Nếu không muốn log câu lệnh ảnh hưởng 0 hàng, thêm `HAVING count(*) > 0` vào `INSERT ... SELECT`, thể hiện rõ lựa chọn thay vì coi hành vi hiện tại là lỗi.",

so_sanh: "### Đặt logic ở đâu\n\n| Cơ chế | Hiển thị | An toàn concurrency | Quan sát và test | Phù hợp nhất | Không phù hợp |\n|---|---|---|---|---|---|\n| Constraint | Rõ trong schema | Engine cưỡng chế nguyên tử | Dễ tạo ca vi phạm | `UNIQUE`, `CHECK`, khóa ngoại | Workflow nhiều bước |\n| SQL trong service | Rõ trong code gọi | Cần transaction/constraint đúng | Tooling ứng dụng tốt | Điều phối nghiệp vụ và network | Nhiều caller dễ viết lệch luật |\n| Procedure | Caller gọi tường minh | Có thể khóa và nguyên tử hóa gần dữ liệu | Cần tooling database | API dữ liệu nhiều bước, batch quản trị | Chỉ bọc một câu SQL đơn giản |\n| Trigger | Tự động, dễ bị quên | Cùng transaction với DML | Khó truy dấu hơn | Audit nhỏ, invariant bắt buộc mọi caller | Email, HTTP, workflow dây chuyền |\n| Outbox + worker | Ghi tường minh, xử lý bất đồng bộ | Outbox nguyên tử với dữ liệu | Có retry/metric riêng | Hiệu ứng ra hệ thống ngoài | Luật phải hoàn tất trước commit |\n\nTiêu chí không phải số dòng code. Một constraint một dòng thường mạnh hơn trigger 30 dòng vì engine hiểu ý nghĩa. Procedure dài nhưng có một API sở hữu rõ có thể tốt hơn năm service sao chép transaction. Trigger ngắn vẫn nguy hiểm nếu nó gọi mạng hoặc kích hoạt vòng dây chuyền.",

loi_thuong_gap: [
  "Dùng trigger thay cho `UNIQUE` hoặc `CHECK`. Dấu hiệu là code “SELECT xem có chưa rồi RAISE” vẫn lọt race condition; chuyển bất biến sang constraint nguyên tử.",
  "Viết row trigger query bảng khác cho từng hàng. Dấu hiệu là bulk update tăng tuyến tính với N truy vấn phụ và nhiều buffer hit; dùng transition table hoặc một câu SQL tập hợp.",
  "Gọi HTTP/email trong trigger. Dấu hiệu là transaction giữ lock khi mạng chậm và retry tạo tác dụng kép; chèn transactional outbox rồi để worker xử lý.",
  "Bắt `WHEN OTHERS` và nuốt exception. Dấu hiệu là caller nhận thành công hoặc false chung chung trong khi dữ liệu chỉ làm nửa ý định; giữ SQLSTATE và rollback rõ.",
  "Tạo `SECURITY DEFINER` mà không khóa `search_path`. Dấu hiệu là tên hàm/bảng có thể resolve sang schema ngoài dự kiến; qualify tên, cấu hình path an toàn và thu hồi quyền rộng.",
  "Cho procedure tự `COMMIT` dù caller cần transaction lớn hơn. Dấu hiệu là không rollback được toàn bộ hoặc lỗi transaction control theo ngữ cảnh; xác định một chủ sở hữu transaction.",
  "Trigger tự cập nhật bảng nguồn gây recursion. Dấu hiệu là stack depth error, nhiều bản audit hoặc update lặp; thiết kế luồng một chiều và điều kiện `OLD`/`NEW` rõ.",
  "Tin rằng chạy trong database mặc nhiên nhanh. Dấu hiệu là plan vẫn sequential scan, loop thủ tục và WAL tăng; tách từng SQL để `EXPLAIN` và đo trên dữ liệu đại diện."
],

tu_kiem_tra: [
  "Vì sao `FOR UPDATE` trong procedure xác nhận thanh toán vẫn cần unique constraint trên `provider_ref`?",
  "Phân biệt row-level và statement-level trigger khi một `UPDATE` ảnh hưởng 0, 1 và 500.000 hàng.",
  "Audit trigger có ghi lại thay đổi nếu transaction chính rollback không? Vì sao câu trả lời đó quan trọng?",
  "Thiết kế retry idempotent cho callback cùng `provider_ref`; trường hợp nào trả thành công và trường hợp nào phải cảnh báo?",
  "Tại sao procedure có `COMMIT` không gọi được tùy ý từ mọi transaction của framework?",
  "Mô tả một cuộc tấn công qua `search_path` vào `SECURITY DEFINER` routine và các lớp phòng vệ.",
  "Khi bulk update chậm, bạn sẽ tách chi phí SQL gốc, row trigger, index audit và WAL để tìm nguyên nhân như thế nào?",
  "Hãy chuyển thiết kế trigger gửi email sang transactional outbox, nêu cách xử lý crash sau khi gửi nhưng trước khi đánh dấu hoàn tất."
],

tai_nguyen: [
  "Tài liệu chính thức PostgreSQL, các chương PL/pgSQL, `CREATE PROCEDURE`, Trigger Definition, Trigger Functions và Transaction Management.",
  "“PostgreSQL: Up and Running” — Regina Obe và Leo Hsu, phần functions, triggers và bảo mật routine.",
  "“Designing Data-Intensive Applications” — Martin Kleppmann, phần transaction, log, idempotency và xử lý dữ liệu dẫn xuất.",
  "“SQL Antipatterns” — Bill Karwin, các chương về thiết kế database và logic khó bảo trì."
],

lien_he: [
  "Nối sang mảng Hệ thống phân tán: idempotency key và transactional outbox giải quyết retry cùng hiệu ứng bên ngoài không nằm trong transaction database.",
  "Nối sang mảng Bảo mật: `SECURITY DEFINER`, `search_path` và `GRANT EXECUTE` là bài thực hành đặc quyền tối thiểu và trust boundary.",
  "Nối sang mảng Kiểm thử: procedure/trigger cần integration test nhiều phiên, rollback, bulk update và role quyền hạn thật, không chỉ unit test đường thuận.",
  "Nối sang mảng Quan sát hệ thống: latency ẩn của trigger phải được truy từ câu DML, statement statistics, lock wait, WAL và replication lag.",
  "Nối sang mảng Kiến trúc phần mềm: quyết định đặt logic ở constraint, database routine hay service là quyết định ownership và coupling, không đơn thuần là sở thích ngôn ngữ."
]

};
