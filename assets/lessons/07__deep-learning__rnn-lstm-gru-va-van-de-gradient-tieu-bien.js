window.LESSONS["07:deep-learning:rnn-lstm-gru-va-van-de-gradient-tieu-bien"] = {

tom_tat: [
  "RNN đọc chuỗi theo thứ tự thời gian và mang một trạng thái ẩn từ bước trước sang bước sau; vì vậy cùng một mẫu rung hiện tại có thể được hiểu khác nhau tùy lịch sử trước nó.",
  "Gradient tiêu biến hoặc bùng nổ xuất hiện khi đạo hàm phải nhân lặp qua nhiều bước thời gian. Trị riêng nhỏ hơn 1 làm tín hiệu học xa dần về 0; lớn hơn 1 có thể làm nó tăng mất kiểm soát.",
  "LSTM tạo đường truyền cell state có các cổng quên, ghi và đọc; GRU gộp bớt cổng để giảm tham số. Cả hai giúp giữ phụ thuộc dài tốt hơn RNN thường nhưng không xóa mọi khó khăn của chuỗi dài.",
  "Độ dài cửa sổ, padding, masking và cách lấy trạng thái cuối ảnh hưởng trực tiếp đến kết quả. Một tensor đúng kích thước vẫn có thể mang ngữ nghĩa sai nếu trộn chiều batch với chiều thời gian.",
  "Ví dụ xuyên suốt dùng 120 mẫu gia tốc của một máy công nghiệp để phân loại bình thường, lệch trục hoặc mòn ổ bi; cùng dữ liệu này sẽ được tái dùng trong ba bài kế tiếp."
],

can_biet_truoc: [
  "Tensor ba chiều và ý nghĩa của các chiều batch, time, feature; phép nhân ma trận và broadcasting.",
  "Hàm kích hoạt tanh, sigmoid và đạo hàm; hiểu vì sao sigmoid cho giá trị từ 0 đến 1.",
  "Lan truyền ngược, chain rule (quy tắc dây chuyền), hàm mất mát cross-entropy và optimizer.",
  "Khái niệm batch, epoch, train/validation split và overfitting."
],

dinh_nghia: "**Recurrent Neural Network (RNN, mạng nơ-ron hồi quy)** là họ mô hình xử lý một chuỗi bằng cách cập nhật trạng thái ẩn `hₜ` ở bước `t` từ đầu vào hiện tại `xₜ` và trạng thái trước `hₜ₋₁`. Cùng một bộ trọng số được dùng ở mọi thời điểm, nên mô hình nhận được chuỗi dài khác nhau mà không phải tạo một tầng riêng cho từng vị trí.\n\nRNN thường dùng `hₜ = tanh(Wₓxₜ + Wₕhₜ₋₁ + b)`. Ở đây `Wₓ` biến đổi đặc trưng hiện tại, `Wₕ` biến đổi ký ức cũ, còn `b` là độ lệch. **LSTM** và **GRU** là hai cell hồi quy có cổng: chúng học xem thông tin nào cần giữ, cập nhật hoặc bỏ, nhờ đó đường truyền gradient qua thời gian ít bị bóp nghẹt hơn.\n\n**Gradient tiêu biến** là hiện tượng gradient của các bước xa trở nên gần 0, khiến mô hình gần như không học được quan hệ dài hạn. **Gradient bùng nổ** là trường hợp đối nghịch: độ lớn gradient tăng quá mức, làm loss dao động, xuất hiện `inf` hoặc `nan`.",

vi_sao_can: "Một cửa sổ rung không phải túi các con số độc lập. Lệch trục thường tạo nhịp lặp có pha; mòn ổ bi có những xung ngắn xuất hiện theo chu kỳ; còn một va chạm đơn lẻ có thể rất mạnh nhưng không phải hỏng hóc. Nếu lấy trung bình 120 mẫu, ba tình huống có thể cho cùng năng lượng và mất hoàn toàn thứ tự. RNN tồn tại để giữ lại câu hỏi “điều gì vừa xảy ra trước mẫu này?”.\n\nTrong ngôn ngữ cũng vậy: từ “không” đổi nghĩa phần sau của câu; trong log hệ thống, chuỗi `retry → timeout → reconnect` khác ba sự kiện đứng riêng. Mô hình chỉ nhìn từng điểm không thể biểu diễn trực tiếp bối cảnh đó.\n\nTuy nhiên, nói “RNN có bộ nhớ” dễ gây hiểu nhầm. Bộ nhớ không phải kho lưu vô hạn; nó là vector hữu hạn liên tục bị ghi đè. Khi huấn luyện, tín hiệu lỗi từ cuối chuỗi phải đi ngược qua nhiều phép biến đổi. Nếu mỗi bước giữ lại 0,8 tín hiệu thì sau 30 bước chỉ còn 0,8³⁰ ≈ 0,0012. LSTM và GRU ra đời vì RNN thường có đường học quá mong manh cho phụ thuộc dài.",

co_che: "### Một cell được dùng lại theo thời gian\n\nHãy coi 120 mẫu rung là 120 khung hình của một cuốn phim rất hẹp. Cell không nhìn cả phim cùng lúc. Ở bước đầu nó nhận `x₀` và trạng thái 0; ở bước kế tiếp nó nhận `x₁` cùng kết quả tóm tắt của bước đầu. Việc dùng chung trọng số tạo hai lợi ích: số tham số không tăng theo độ dài chuỗi, và quy luật phát hiện ở vị trí 10 cũng dùng được tại vị trí 90.\n\nVới batch 32 cửa sổ, mỗi cửa sổ 120 thời điểm và mỗi thời điểm có 3 trục gia tốc, đầu vào khi `batch_first=True` có shape `[32, 120, 3]`. RNN trả `output` shape `[32, 120, H]`, chứa trạng thái ở mọi bước, và `h_n` chứa trạng thái kết thúc của từng tầng. `output[:, -1, :]` chỉ đúng là “thời điểm thật cuối” khi mọi chuỗi đều dài 120; nếu có padding thì vị trí cuối có thể chỉ là số 0 giả.\n\n### Vì sao thứ tự tạo ra năng lực mới\n\nGiả sử hai cửa sổ có cùng các giá trị `{0, 1, 2}` nhưng thứ tự lần lượt là `0→1→2` và `2→1→0`. Một mô hình lấy tổng cho cả hai kết quả 3. RNN cập nhật tuần tự nên trạng thái sau cùng khác nhau: phép biến đổi phi tuyến không có tính giao hoán. Đây vừa là sức mạnh vừa là hạn chế; khó song song hóa 120 bước vì `hₜ` phải chờ `hₜ₋₁`.\n\nTrạng thái ẩn không nhất thiết có một chiều ứng với “tần số mòn ổ bi”. Nó là biểu diễn phân tán: nhiều chiều cùng mã hóa pha, biên độ và lịch sử. Vì thế diễn giải một neuron riêng lẻ thường kém tin cậy hơn kiểm tra phản ứng của toàn mô hình khi che một đoạn chuỗi.\n\n### Backpropagation Through Time\n\nĐể học, đồ thị tính toán được mở ra theo thời gian; kỹ thuật này gọi là **Backpropagation Through Time (BPTT, lan truyền ngược qua thời gian)**. Loss ở cuối phụ thuộc `h₁₂₀`, trạng thái đó phụ thuộc `h₁₁₉`, và cứ thế. Chain rule buộc gradient nhân liên tiếp với các Jacobian, tức ma trận đạo hàm cục bộ.\n\nNếu giản lược còn một số vô hướng `hₜ = tanh(w hₜ₋₁ + u xₜ)`, ảnh hưởng của `h₀` lên `h_T` chứa tích `Πₜ w·tanh'(…)`. Đạo hàm `tanh'` không vượt quá 1 và thường nhỏ hơn nhiều khi đầu vào rơi vào vùng bão hòa. Với `|w|` nhỏ, tích nhanh chóng về 0. Với `|w|` và hướng khuếch đại đủ lớn, tích có thể bùng lên. Trong mạng thật, trị riêng và hướng của ma trận thay cho số `w`, nhưng trực giác nhân lặp vẫn giữ nguyên.\n\n### Nhận biết tiêu biến và bùng nổ\n\nGradient tiêu biến không nhất thiết tạo lỗi runtime. Loss vẫn giảm, nhưng mô hình chỉ khai thác vài bước gần cuối. Một phép thử thực dụng là giữ nguyên phần cuối và thay đổi phần đầu quan trọng; nếu dự đoán hầu như không đổi dù nghiệp vụ nói rằng phải đổi, mô hình có thể đã quên. Cũng có thể đo norm gradient theo tầng hoặc tính độ nhạy của logit với từng vị trí.\n\nGradient bùng nổ dễ thấy hơn: norm nhảy hàng nghìn lần, loss tăng đột ngột, trọng số thành `nan`. `torch.nn.utils.clip_grad_norm_` giới hạn norm sau `backward()` và trước `optimizer.step()`. Clipping là dây an toàn, không sửa kiến trúc hoặc learning rate sai; nếu nó kích hoạt ở gần mọi batch, cần điều tra thay vì coi đó là trạng thái bình thường.\n\n### LSTM tạo đường cao tốc cho ký ức\n\nLSTM duy trì `cₜ`, gọi là cell state, bên cạnh trạng thái xuất `hₜ`. Cổng quên `fₜ = sigmoid(…)` quyết định phần nào của `cₜ₋₁` còn lại. Cổng đầu vào `iₜ` điều khiển lượng ứng viên `gₜ` được ghi. Cập nhật cốt lõi là `cₜ = fₜ ⊙ cₜ₋₁ + iₜ ⊙ gₜ`, trong đó `⊙` là nhân từng phần tử. Cổng đầu ra chọn phần cell state đưa thành `hₜ`.\n\nĐiểm quan trọng nằm ở phép cộng. Gradient có thể đi qua chuỗi `cₜ` bằng một đường gần tuyến tính, thay vì bắt buộc xuyên qua `tanh` và ma trận tại mọi bước. Khi `fₜ` gần 1, ký ức được bảo toàn. Nhưng cổng được học từ dữ liệu, không phải lời hứa toán học; dữ liệu ít hoặc tối ưu kém vẫn khiến LSTM quên.\n\n### GRU gọn hơn bằng cách gộp trạng thái\n\nGRU không giữ riêng `cₜ`; nó có update gate để trộn trạng thái cũ với ứng viên mới và reset gate để quyết định mức lịch sử tham gia tạo ứng viên. Ít cổng hơn nghĩa là ít ma trận hơn, thường huấn luyện nhanh và dùng ít bộ nhớ hơn. Không có định luật rằng LSTM luôn chính xác hơn GRU. Với cửa sổ rung chỉ 120 bước và tập vừa phải, GRU là baseline hợp lý vì giảm nguy cơ overfitting.\n\nSự lựa chọn cần được đo trên cùng cách chia dữ liệu theo máy. Nếu chia ngẫu nhiên các cửa sổ chồng lấn, mẫu từ cùng một lần đo lọt sang cả train và validation, cả LSTM lẫn GRU sẽ có điểm đẹp giả.\n\n### Padding, packing và masking\n\nKhi chuỗi dài khác nhau, ta thường nối số 0 vào cuối để tạo batch hình chữ nhật. Số 0 đó không phải quan sát thật. PyTorch cung cấp `pack_padded_sequence` để cell bỏ qua phần đệm; cách khác là dùng mask khi tính loss và gom trạng thái tại chỉ số `length - 1`.\n\n`enforce_sorted=False` tiện cho batch chưa sắp xếp theo chiều dài, nhưng `lengths` thường phải ở CPU. Ca chuỗi dài 0 nên bị loại hoặc được định nghĩa riêng, vì không tồn tại “trạng thái cuối thật”. Chuỗi dài 1 chạy hợp lệ nhưng không kiểm tra được khả năng nhớ; nó gần như một mạng feed-forward dùng trọng số RNN.\n\n### Hai chiều và nhiều tầng không miễn phí\n\nBidirectional RNN đọc cả xuôi và ngược, hữu ích khi toàn bộ cửa sổ đã có sẵn. Nó không dùng được nguyên trạng cho cảnh báo trực tuyến tại thời điểm hiện tại, vì nhánh ngược cần dữ liệu tương lai. Mạng nhiều tầng tăng khả năng biểu diễn nhưng làm tăng latency, bộ nhớ activation và nguy cơ overfitting. Dropout giữa các tầng chỉ có tác dụng khi `num_layers > 1` trong các module RNN chuẩn của PyTorch.\n\n### Truncated BPTT và trạng thái giữa các đoạn\n\nVới luồng rất dài, giữ đồ thị cho hàng triệu bước là bất khả thi. Truncated BPTT cắt chuỗi thành đoạn, mang giá trị trạng thái sang đoạn sau nhưng gọi `detach()` để dừng gradient tại ranh giới. Nhờ vậy bộ nhớ bị chặn, đổi lại mô hình không nhận tín hiệu học xuyên qua ranh giới xa.\n\nQuên `detach()` khiến đồ thị tích lũy qua batch, bộ nhớ tăng cho đến khi hết. Detach quá sớm ở mọi bước lại biến mô hình thành gần như không học quan hệ thời gian. Ranh giới cắt là hyperparameter gắn với khoảng phụ thuộc mà bài toán cần.\n\n### Khi nào không nên chọn RNN\n\nNếu nhãn chỉ phụ thuộc thống kê tổng hợp như RMS của cả cửa sổ, một mô hình cây trên đặc trưng có thể rẻ và dễ giải thích hơn. Nếu chuỗi rất dài và cần huấn luyện song song, Transformer hoặc temporal convolution thường phù hợp hơn. Nếu cảnh báo phải chạy trên vi điều khiển với ngân sách vài kilobyte, GRU nhỏ hoặc thậm chí bộ lọc tín hiệu thủ công có thể thực tế hơn LSTM hai chiều.\n\nRNN cũng không tự giải quyết irregular sampling. Khoảng thời gian giữa hai mẫu thay đổi phải được đưa thành đặc trưng, nội suy có kiểm soát, hoặc dùng mô hình chuyên biệt; coi các bước cách đều sẽ gán sai ý nghĩa cho trạng thái.",

vi_du: "### Mô hình GRU chạy được trên dữ liệu rung tổng hợp\n\nĐoạn mã dưới đây tạo ba lớp có quy luật khác nhau để kiểm tra shape và vòng học. Nó không phải benchmark độ chính xác cho dữ liệu nhà máy thật; seed cố định chỉ giúp tái lập.\n\n```python\nimport math\nimport torch\nfrom torch import nn\n\ntorch.manual_seed(7)\nN, T, F, C = 192, 120, 3, 3\nt = torch.linspace(0, 1, T)\nxs, ys = [], []\nfor i in range(N):\n    y = i % C\n    freq = (6.0, 9.0, 13.0)[y]\n    base = torch.sin(2 * math.pi * freq * t)\n    if y == 2:\n        base[::15] += 1.5\n    x = torch.stack([base, 0.7 * base, -0.4 * base], dim=-1)\n    xs.append(x + 0.08 * torch.randn(T, F))\n    ys.append(y)\nX, y = torch.stack(xs), torch.tensor(ys)\n\nclass BoPhanLoaiGRU(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.gru = nn.GRU(input_size=F, hidden_size=24,\n                          batch_first=True)\n        self.head = nn.Linear(24, C)\n    def forward(self, x):\n        _, h_n = self.gru(x)\n        return self.head(h_n[-1])\n\nmodel = BoPhanLoaiGRU()\nopt = torch.optim.Adam(model.parameters(), lr=3e-3)\nloss_fn = nn.CrossEntropyLoss()\nfor epoch in range(20):\n    opt.zero_grad(set_to_none=True)\n    loss = loss_fn(model(X), y)\n    loss.backward()\n    grad_norm = nn.utils.clip_grad_norm_(model.parameters(), 1.0)\n    opt.step()\nprint(model(X).shape, float(loss), float(grad_norm))\nassert model(X).shape == (N, C)\n```\n\nKhông ghi sẵn con số loss vì nó phụ thuộc phiên bản thư viện và phần cứng; người học phải chạy để lấy số trên môi trường của mình. Điều chắc chắn được `assert` kiểm tra là mỗi cửa sổ sinh ba logit.\n\n### Thử ca biên và lỗi shape\n\n```python\nwith torch.no_grad():\n    assert model(torch.zeros(1, 1, 3)).shape == (1, 3)\ntry:\n    model(torch.zeros(2, 120, 4))\nexcept RuntimeError as e:\n    print(\"Sai số đặc trưng được phát hiện:\", str(e).splitlines()[0])\n```\n\nTensor `[2, 120, 4]` sai vì cell khai `input_size=3`. Nguy hiểm hơn là tensor `[120, 2, 3]`: nó vẫn có thể chạy nhưng bị hiểu 120 là batch nếu `batch_first=True`, tạo lỗi ngữ nghĩa chứ không nhất thiết ném exception.\n\n### Kiểm tra mô hình có dùng phần đầu hay không\n\nSau huấn luyện, thay 30 mẫu đầu bằng 0 rồi so logit. Đây không chứng minh mô hình “hiểu” lịch sử, nhưng phát hiện nhanh trường hợp đầu chuỗi hoàn toàn vô tác dụng.\n\n```python\nmodel.eval()\nwith torch.no_grad():\n    goc = model(X[:16])\n    che = X[:16].clone()\n    che[:, :30] = 0\n    do_lech = (goc - model(che)).abs().mean()\nprint(\"Độ nhạy với 30 bước đầu:\", float(do_lech))\n```\n\nĐộ lệch gần 0 cần được diễn giải cùng nghiệp vụ: nếu lỗi chỉ xuất hiện cuối cửa sổ thì đó có thể là hành vi đúng; nếu nhãn phụ thuộc nhịp xuyên suốt thì đó là dấu hiệu cần điều tra.",

so_sanh: "### RNN thường, LSTM, GRU và phương án không hồi quy\n\n| Phương án | Đường giữ lịch sử | Chi phí tương đối | Điểm mạnh | Khi không nên ưu tiên |\n|---|---|---:|---|---|\n| RNN thường | trạng thái ẩn qua `tanh` | thấp | baseline nhỏ, quan hệ ngắn | phụ thuộc dài, gradient thiếu ổn định |\n| LSTM | cell state cộng dồn và ba nhóm cổng | cao nhất trong ba cell | kiểm soát quên/ghi rõ, lịch sử dài hơn | dữ liệu ít, latency hoặc bộ nhớ chặt |\n| GRU | một trạng thái, reset/update gate | trung bình | gọn, thường là điểm xuất phát tốt | cần tách rõ cell state và đầu ra để nghiên cứu |\n| Temporal CNN | convolution theo thời gian | dễ song song | receptive field rõ, huấn luyện nhanh | quan hệ dài cần nhiều tầng/dilation |\n| Transformer | attention giữa vị trí | bộ nhớ tăng mạnh theo độ dài | tương tác xa và song song hóa tốt | chuỗi dài trên thiết bị nhỏ, dữ liệu hạn chế |\n\nSố tham số phụ thuộc hidden size và implementation, nên bảng không gán một tỷ lệ giả định. So sánh công bằng phải giữ ngân sách tham số hoặc latency gần nhau, dùng cùng split theo máy và báo cả độ nhạy lớp lỗi hiếm.\n\n### Trạng thái cuối so với pooling mọi bước\n\n| Cách gom chuỗi | Ưu điểm | Rủi ro |\n|---|---|---|\n| `h_n[-1]` | đơn giản, đúng với chuỗi không padding | dồn mọi thông tin vào một vector cuối |\n| mean pooling có mask | gradient tới nhiều vị trí, bền với nhiễu cục bộ | làm loãng xung hiếm nếu cửa sổ dài |\n| max pooling | giữ kích hoạt mạnh | nhạy với một spike nhiễu |\n| attention pooling | học vị trí đáng chú ý | thêm tham số, có thể overfit |",

loi_thuong_gap: [
  "Đảo chiều batch và time nhưng tensor vẫn chạy. Nhận biết bằng cách assert `x.shape == (batch, length, features)` ngay tại biên dữ liệu và thử batch có kích thước khác length.",
  "Lấy `output[:, -1]` trên chuỗi đã padding. Dấu hiệu là kết quả đổi mạnh khi chỉ tăng lượng đệm; sửa bằng packing hoặc lấy tại `length - 1`.",
  "Không clip gradient khi loss thỉnh thoảng thành `nan`. Ghi norm trước bước optimizer để phân biệt bùng nổ gradient với dữ liệu đầu vào đã có `nan`.",
  "Giữ hidden state qua các batch độc lập. Mô hình vô tình trộn máy này với máy khác; cần khởi tạo lại state hoặc chỉ mang state trong cùng một luồng có thứ tự.",
  "Quên `detach()` khi truncated BPTT. Bộ nhớ GPU tăng theo từng batch dù kích thước batch không đổi, vì đồ thị cũ vẫn được giữ.",
  "Dùng bidirectional model cho cảnh báo trực tuyến. Điểm validation tốt nhưng triển khai không thể cung cấp nhánh “tương lai”; hãy đánh giá bằng mô hình nhân quả.",
  "Chia ngẫu nhiên cửa sổ chồng lấn. Validation gần như chứa bản sao của train; dấu hiệu là điểm giảm mạnh khi chia theo mã máy hoặc theo phiên đo."
],

tu_kiem_tra: [
  "Vì sao việc dùng chung trọng số qua thời gian vừa giảm số tham số vừa khiến các bước khó song song hóa?",
  "Trong tích gradient qua 40 bước, yếu tố trung bình 0,9 cho trực giác gì? Vì sao không thể kết luận chỉ từ một số vô hướng trong mạng nhiều chiều?",
  "Giải thích bằng lời vai trò riêng của forget gate, input gate và output gate trong LSTM; cổng nào tạo đường giữ cell state?",
  "Một batch có lengths `[120, 80]` được padding đến 120. Vì sao lấy vị trí `-1` cho cả hai chuỗi là sai, và có hai cách sửa nào?",
  "Khi gradient clipping kích hoạt ở mọi batch, vì sao tăng ngưỡng clip chưa chắc là sửa đúng nguyên nhân?",
  "Thiết kế phép thử để phân biệt mô hình thực sự dùng 50 bước đầu với mô hình chỉ nhìn 10 bước cuối mà không dựa vào attention plot.",
  "Tại sao bidirectional GRU hợp lệ cho phân loại một tệp rung hoàn chỉnh nhưng gây rò rỉ tương lai trong cảnh báo streaming?"
],

tai_nguyen: [
  "Deep Learning — Ian Goodfellow, Yoshua Bengio, Aaron Courville; phần sequence modeling và khó khăn của phụ thuộc dài.",
  "Dive into Deep Learning — chương Recurrent Neural Networks và Modern Recurrent Neural Networks.",
  "PyTorch Tutorials — Sequence Models and Long Short-Term Memory Networks.",
  "Understanding Deep Learning — Simon J. D. Prince; các chương về mô hình chuỗi và tối ưu."
],

lien_he: [
  "Nối sang Xử lý tín hiệu: phổ tần, lấy mẫu và cửa sổ giải thích vì sao chuỗi rung phải được lọc và chuẩn hóa trước khi vào RNN; mô hình không sửa được aliasing đã xảy ra.",
  "Nối sang Cấu trúc dữ liệu: trạng thái ẩn là một dạng tóm tắt streaming có kích thước cố định, đối lập với việc lưu toàn bộ lịch sử trong hàng đợi.",
  "Nối sang Hệ thống phân tán: trạng thái mang qua đoạn dữ liệu đòi hỏi định tuyến đúng phiên máy; đảo thứ tự message làm sai lịch sử dù từng tensor hợp lệ.",
  "Nối sang Đánh giá mô hình: split theo thiết bị và theo thời gian ngăn rò rỉ từ các cửa sổ chồng lấn, quan trọng hơn việc chỉ báo accuracy ngẫu nhiên."
]

};
