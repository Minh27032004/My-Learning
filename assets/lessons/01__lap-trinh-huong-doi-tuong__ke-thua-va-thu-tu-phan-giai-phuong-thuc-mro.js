window.LESSONS["01:lap-trinh-huong-doi-tuong:ke-thua-va-thu-tu-phan-giai-phuong-thuc-mro"] = {

tom_tat: [
  "**Kế thừa mở rộng quy tắc tra cứu thuộc tính** đã học ở bài class: không thấy ở instance thì tra class, không thấy thì tra các class cha **theo đúng thứ tự MRO**.",
  "**`super()` không có nghĩa là \"class cha\"** — nó là \"cái tiếp theo trong MRO\". Với đa kế thừa, cái tiếp theo có thể là một class hoàn toàn không phải cha của class hiện tại.",
  "**MRO được tính bằng thuật toán C3**, đảm bảo mỗi class xuất hiện đúng một lần và lớp con luôn đứng trước lớp cha. Xem bằng `Lop.__mro__`.",
  "**Đa kế thừa chỉ an toàn khi mọi class trong chuỗi đều gọi `super()`.** Một mắt xích quên gọi là các class phía sau bị bỏ qua âm thầm.",
  "**Kế thừa là quan hệ ràng buộc chặt nhất giữa hai class.** Dùng nó khi có quan hệ \"là một\" thật; còn lại thì composition tốt hơn — bài riêng sẽ nói."
],

can_biet_truoc: [
  "Bài **Class, object, thuộc tính, phương thức** — thứ tự tra cứu instance → class là nền để mở rộng sang class cha.",
  "Bài **Đóng gói** ngay trước — `__x` tồn tại chính vì kế thừa; `_x` thì lớp con thấy và đè được.",
  "Bài **Module, package, import** ở module 5 — MRO cũng là một bài toán \"tìm theo thứ tự\", giống `sys.path`.",
  "Bài **Hàm, tham số mặc định** ở module 5 — `super().__init__(*args, **kwargs)` là chuyển tiếp tham số."
],

dinh_nghia: "Nói đời thường: kế thừa giống như **một bản thiết kế dựa trên bản thiết kế khác**: \"giống nhà mẫu, nhưng thêm gác lửng và đổi màu cửa\". Bạn không vẽ lại từ đầu — bạn nói rõ những gì khác đi.\n\nNói chính xác:\n\n- **Kế thừa** (*inheritance*) — class con nhận toàn bộ thuộc tính và phương thức của class cha, và có thể thêm hoặc thay đổi.\n- **Class cha** (*base*, *superclass*) và **class con** (*derived*, *subclass*).\n- **Ghi đè** (*override*) — định nghĩa lại một phương thức đã có ở class cha.\n- **MRO** (*Method Resolution Order*) — thứ tự Python tra cứu để tìm một thuộc tính qua chuỗi kế thừa.\n- **`super()`** — truy cập **class tiếp theo trong MRO**, không phải \"class cha\".\n- **Đa kế thừa** — kế thừa từ nhiều class cùng lúc. Python cho phép; nhiều ngôn ngữ thì không.\n- **Mixin** — class nhỏ chỉ cung cấp một nhóm hành vi, được thiết kế để trộn vào class khác, không dùng độc lập.\n- **Quan hệ \"là một\"** (*is-a*) — điều kiện để kế thừa hợp lý: mọi `Con` **phải là một** `Cha` theo đúng nghĩa.\n\nĐiểm cần đính chính ngay và là nguồn của phần lớn nhầm lẫn: **`super()` không phải \"gọi class cha\".** Trong đơn kế thừa thì hai cách hiểu trùng nhau nên không ai để ý; trong đa kế thừa thì chúng khác hẳn, và hiểu sai dẫn tới những lỗi rất khó lần.",

vi_sao_can: "Kế thừa là công cụ mạnh và cũng là công cụ bị lạm dụng nhiều nhất trong OOP:\n\n- **Bạn dùng nó mỗi ngày mà chưa gọi tên.** Mọi ngoại lệ tự định nghĩa đều kế thừa `Exception`; mọi class trong Python đều kế thừa `object`; các framework đòi bạn kế thừa class nền của chúng.\n- **Đọc mã nguồn thư viện đòi hỏi hiểu MRO.** Không biết `super()` gọi đi đâu là không lần được luồng thực thi trong code có nhiều tầng kế thừa.\n- **Đa kế thừa và mixin xuất hiện khắp nơi** trong các framework web và thư viện học sâu. Dùng sai thứ tự class là hành vi khác đi mà không có lỗi nào báo.\n- **Kế thừa sai là nợ kỹ thuật nặng nhất.** Nó tạo ràng buộc chặt nhất giữa hai class: đổi class cha là mọi class con chịu ảnh hưởng. Sửa lại một cây kế thừa sai thường phải viết lại cả nhánh.\n- **Nền cho Liskov và cho composition.** Hai bài quan trọng sau trong module này dựa hoàn toàn vào việc hiểu kế thừa hoạt động thế nào.\n\nTình huống thật rất hay gặp: bạn dùng đa kế thừa `class A(B, C)`, và một phương thức của `C` **không bao giờ chạy**. Không có lỗi, không có cảnh báo — chỉ là hành vi thiếu đi một phần. Nguyên nhân: `B.__init__` không gọi `super().__init__()`, nên chuỗi MRO đứt tại đó. Phần cơ chế sẽ chỉ rõ vì sao và cách phòng.",

co_che: "### Kế thừa mở rộng quy tắc tra cứu\n\n```python\nclass DongVat:\n    def __init__(self, ten: str):\n        self.ten = ten\n\n    def keu(self) -> str:\n        return \"...\"\n\n    def gioi_thieu(self) -> str:\n        return f\"{self.ten} kêu {self.keu()}\"      # ⭐ gọi keu() của ai?\n\nclass Cho(DongVat):\n    def keu(self) -> str:                          # GHI ĐÈ\n        return \"gâu gâu\"\n\nc = Cho(\"Vàng\")\nc.gioi_thieu()        # 'Vàng kêu gâu gâu'\n```\n\nDòng `gioi_thieu` định nghĩa ở `DongVat` nhưng gọi ra `Cho.keu` — vì `self` là một `Cho`, và việc tra cứu bắt đầu từ kiểu thật của object. Đây gọi là **liên kết động**, và nó là cơ chế nền của đa hình (bài sau).\n\nThứ tự tra cứu đầy đủ khi đọc `c.keu`:\n\n```\n① c.__dict__            không có\n② Cho.__dict__          CÓ → dùng\n③ DongVat.__dict__      (không tới đây)\n④ object.__dict__\n⑤ AttributeError\n```\n\nĐây chính là quy tắc ở bài class, chỉ thêm bước ③ và ④. Toàn bộ kế thừa nằm ở việc **kéo dài chuỗi tra cứu**.\n\n### `super()` — \"tiếp theo trong MRO\"\n\n```python\nclass DongVat:\n    def __init__(self, ten: str):\n        self.ten = ten\n\nclass Cho(DongVat):\n    def __init__(self, ten: str, giong: str):\n        super().__init__(ten)          # ⭐ để class cha lo phần của nó\n        self.giong = giong             # rồi thêm phần riêng\n```\n\nHai lỗi kinh điển của người mới:\n\n```python\n# ❌ Quên super().__init__() ⇒ thuộc tính của cha KHÔNG được tạo\nclass Cho(DongVat):\n    def __init__(self, ten, giong):\n        self.giong = giong\n\nCho(\"Vàng\", \"cỏ\").ten        # AttributeError: no attribute 'ten'\n\n# ❌ Gọi thẳng tên class cha ⇒ hỏng khi đa kế thừa\nclass Cho(DongVat):\n    def __init__(self, ten, giong):\n        DongVat.__init__(self, ten)    # chạy được, nhưng xem mục đa kế thừa\n        self.giong = giong\n```\n\nCách thứ hai \"chạy được\" trong đơn kế thừa và **phá vỡ chuỗi MRO** trong đa kế thừa. Quy tắc: **luôn dùng `super()`, không bao giờ gọi thẳng tên class cha.**\n\nMẫu mở rộng thay vì thay thế hoàn toàn:\n\n```python\nclass GhiLog:\n    def xu_ly(self, x):\n        return x * 2\n\nclass GhiLogCoDo(GhiLog):\n    def xu_ly(self, x):\n        import time\n        t = time.perf_counter()\n        kq = super().xu_ly(x)              # ⭐ dùng lại logic cha\n        print(f\"mất {time.perf_counter()-t:.6f}s\")\n        return kq\n```\n\n### MRO — thứ tự phân giải\n\n```python\nclass A: pass\nclass B(A): pass\nclass C(A): pass\nclass D(B, C): pass\n\nD.__mro__\n# (D, B, C, A, object)\n\n# Hoặc dễ đọc hơn\n[c.__name__ for c in D.mro()]     # ['D', 'B', 'C', 'A', 'object']\n```\n\nĐây là **bài toán kim cương** kinh điển:\n\n```\n      A\n     / \\\n    B   C\n     \\ /\n      D\n```\n\nCâu hỏi: `D` gọi một phương thức có ở cả `B`, `C`, `A` thì chạy cái nào? MRO trả lời: **`D` → `B` → `C` → `A` → `object`**, lấy cái đầu tiên tìm thấy.\n\nBa tính chất mà thuật toán C3 đảm bảo:\n\n```\n① Mỗi class xuất hiện ĐÚNG MỘT LẦN\n② Lớp con luôn đứng TRƯỚC lớp cha\n③ Thứ tự khai báo cha được TÔN TRỌNG (B trước C vì viết D(B, C))\n```\n\nTính chất ① rất quan trọng: nếu không, `A.__init__` sẽ chạy **hai lần** trong ví dụ trên — một lần qua `B`, một lần qua `C`.\n\nKhi không có MRO hợp lệ, Python **từ chối tạo class**:\n\n```python\nclass X: pass\nclass Y: pass\nclass A(X, Y): pass\nclass B(Y, X): pass\nclass C(A, B): pass\n# TypeError: Cannot create a consistent method resolution order (MRO)\n```\n\n`A` đòi `X` trước `Y`, `B` đòi ngược lại — mâu thuẫn không giải được. Báo lỗi **lúc định nghĩa class** là thiết kế tốt: nó bắt vấn đề ngay thay vì để hành vi kỳ lạ lúc chạy.\n\n### `super()` trong đa kế thừa — phần khó nhất\n\nĐây là chỗ hiểu \"`super()` = class cha\" sẽ sai:\n\n```python\nclass A:\n    def chao(self):\n        print(\"A\")\n\nclass B(A):\n    def chao(self):\n        print(\"B\")\n        super().chao()        # ⭐ \"cha của B\" là A... nhưng chưa chắc!\n\nclass C(A):\n    def chao(self):\n        print(\"C\")\n        super().chao()\n\nclass D(B, C):\n    def chao(self):\n        print(\"D\")\n        super().chao()\n\nD().chao()\n# D\n# B\n# C      ← ⭐ super() TRONG B gọi sang C, không phải A!\n# A\n```\n\n```\nMRO của D:  D → B → C → A → object\n\nsuper() trong B khi self là D  →  C   (cái TIẾP THEO sau B trong MRO của D)\nsuper() trong B khi self là B  →  A   (MRO của B là B → A → object)\n```\n\n**`super()` phụ thuộc vào MRO của kiểu thật của `self`, không phụ thuộc vào chỗ nó được viết.** Đây là điểm phải nắm; nó làm đa kế thừa hoạt động được nhưng cũng làm nó khó suy luận.\n\nNhờ cơ chế này mà **mỗi class trong chuỗi chạy đúng một lần**, theo đúng thứ tự MRO — gọi là *hợp tác đa kế thừa*.\n\n### Chuỗi đứt — lỗi im lặng nguy hiểm nhất\n\n```python\nclass B(A):\n    def chao(self):\n        print(\"B\")\n        # ❌ QUÊN super().chao()\n\nD().chao()\n# D\n# B\n#     ← C và A KHÔNG BAO GIỜ chạy, không có lỗi nào\n```\n\nMột mắt xích quên `super()` là **cắt đứt phần còn lại của chuỗi**. Không ngoại lệ, không cảnh báo — chỉ là hành vi thiếu đi.\n\nQuy tắc bắt buộc cho đa kế thừa:\n\n```\nMỌI class trong chuỗi phải gọi super() — kể cả class không cần làm gì thêm\n```\n\nVấn đề đi kèm: chữ ký phương thức phải tương thích qua cả chuỗi. Mẫu an toàn dùng `**kwargs`:\n\n```python\nclass Nen:\n    def __init__(self, **kwargs):\n        super().__init__(**kwargs)        # ⭐ chuyển tiếp phần còn lại\n\nclass CoTen(Nen):\n    def __init__(self, ten, **kwargs):\n        super().__init__(**kwargs)        # đưa phần không phải của mình đi tiếp\n        self.ten = ten\n\nclass CoTuoi(Nen):\n    def __init__(self, tuoi, **kwargs):\n        super().__init__(**kwargs)\n        self.tuoi = tuoi\n\nclass Nguoi(CoTen, CoTuoi):\n    pass\n\nn = Nguoi(ten=\"Minh\", tuoi=22)     # ⭐ mỗi class lấy phần của mình\nn.ten, n.tuoi                       # ('Minh', 22)\n```\n\nMẫu này là cách duy nhất làm đa kế thừa `__init__` chạy đúng. Chú ý phải gọi bằng **tham số tên** — với tham số vị trí thì không có cách nào phân phối đúng.\n\nCũng chú ý `object.__init__` không nhận tham số nào, nên class cuối chuỗi phải \"tiêu thụ\" hết mọi tham số còn lại.\n\n### Mixin\n\nMixin là cách dùng đa kế thừa an toàn nhất: **class nhỏ, một nhóm hành vi, không có trạng thái riêng, không dùng độc lập.**\n\n```python\nimport json\n\nclass JSONMixin:\n    \"\"\"Thêm khả năng xuất JSON. Không dùng độc lập.\"\"\"\n    def to_json(self) -> str:\n        return json.dumps(vars(self), ensure_ascii=False, default=str)\n\nclass SoSanhMixin:\n    \"\"\"Thêm so sánh dựa trên khoá_sap_xep(). Class dùng phải định nghĩa nó.\"\"\"\n    def khoa_sap_xep(self):\n        raise NotImplementedError\n\n    def __lt__(self, khac):\n        return self.khoa_sap_xep() < khac.khoa_sap_xep()\n\nclass SanPham(JSONMixin, SoSanhMixin):\n    def __init__(self, ten, gia):\n        self.ten, self.gia = ten, gia\n\n    def khoa_sap_xep(self):\n        return self.gia\n\ns = SanPham(\"sách\", 100_000)\ns.to_json()                              # '{\"ten\": \"sách\", \"gia\": 100000}'\nsorted([SanPham(\"a\", 3), SanPham(\"b\", 1)])[0].ten    # 'b'\n```\n\nBốn quy ước làm mixin an toàn:\n\n```\n① Đặt tên kết thúc bằng \"Mixin\" — nói rõ ý định\n② KHÔNG có __init__, hoặc có thì phải gọi super()\n③ KHÔNG có trạng thái riêng\n④ Đặt TRƯỚC class chính trong danh sách kế thừa\n```\n\nĐiểm ④ có lý do: MRO tra từ trái sang, nên mixin đứng trước thì phương thức của nó **thắng** phương thức của class nền. Đặt sau thì mixin không có tác dụng gì.\n\n### Kế thừa từ kiểu dựng sẵn\n\n```python\n# ⚠️ Cạm bẫy: phương thức C bên trong KHÔNG gọi phương thức bạn ghi đè\nclass DictGhiLog(dict):\n    def __setitem__(self, k, v):\n        print(f\"đặt {k}={v}\")\n        super().__setitem__(k, v)\n\nd = DictGhiLog()\nd[\"a\"] = 1            # đặt a=1 ✓\nd.update({\"b\": 2})    # ⚠️ KHÔNG in gì — update() không gọi __setitem__\n\n# ✓ Dùng UserDict — được thiết kế để kế thừa\nfrom collections import UserDict\n\nclass DictGhiLog(UserDict):\n    def __setitem__(self, k, v):\n        print(f\"đặt {k}={v}\")\n        super().__setitem__(k, v)\n\nd = DictGhiLog()\nd.update({\"b\": 2})    # đặt b=2 ✓\n```\n\nNguyên nhân: `dict` được cài đặt bằng C và các phương thức của nó gọi thẳng vào code C, không đi qua bảng phương thức Python. `UserDict`, `UserList`, `UserString` là bản viết bằng Python thuần, dành riêng cho việc kế thừa.\n\nĐây là ví dụ tốt cho bài học chung: **kế thừa từ class không được thiết kế để kế thừa là rủi ro.**\n\n### Kiểm tra quan hệ kế thừa\n\n```python\nisinstance(c, Cho)          # True — c là Cho hoặc lớp con của Cho\nisinstance(c, DongVat)      # True — ⭐ nhận cả lớp cha\ntype(c) is Cho              # True — chính xác kiểu này, KHÔNG nhận lớp con\n\nissubclass(Cho, DongVat)    # True\nCho.__bases__               # (DongVat,) — cha trực tiếp\nCho.__mro__                 # đầy đủ chuỗi\nDongVat.__subclasses__()    # [Cho, ...] — con trực tiếp\n```\n\nDùng `isinstance` chứ không `type(x) is` là quy tắc: `type(x) is Cho` từ chối mọi lớp con, và đó thường là vi phạm nguyên tắc Liskov (bài riêng sẽ nói).\n\n### Khi nào kế thừa là lựa chọn sai\n\nPhần này quan trọng ngang phần cơ chế, vì kế thừa bị lạm dụng nhiều nhất:\n\n```python\n# ❌ Kế thừa chỉ để DÙNG LẠI code, không có quan hệ \"là một\"\nclass DanhSachEmail(list):\n    def gui_tat_ca(self): ...\n\n# Vấn đề: DanhSachEmail giờ có sort(), reverse(), append() bất kỳ thứ gì\ndse = DanhSachEmail()\ndse.append(42)          # ✓ không lỗi — thêm số nguyên vào danh sách email\n\n# ✓ Composition — chỉ lộ ra thứ có nghĩa\nclass DanhSachEmail:\n    def __init__(self):\n        self._email: list[str] = []\n\n    def them(self, e: str) -> None:\n        if \"@\" not in e:\n            raise ValueError(f\"email không hợp lệ: {e!r}\")\n        self._email.append(e)\n\n    def gui_tat_ca(self): ...\n```\n\nPhép thử: **mọi `Con` có thật sự là một `Cha` không, ở mọi tình huống?**\n\n```\nCho là một DongVat            ✓ kế thừa hợp lý\nHinhVuong là một HinhChuNhat  ⚠️ về toán thì đúng, về CODE thì có bẫy\n                                 (bài Liskov sẽ phân tích kỹ)\nDanhSachEmail là một list     ✗ nó CHỨA một list, không PHẢI một list\nNhanVien là một Nguoi         ✓\nNhanVien là một CongTy        ✗ nó thuộc về một công ty\n```\n\nHai cái giá của kế thừa cần cân nhắc:\n\n```\n① Ràng buộc chặt: đổi class cha ảnh hưởng MỌI class con,\n   kể cả những class bạn không biết là ai viết\n② Thừa hưởng TOÀN BỘ, kể cả những gì không phù hợp\n   → không chọn lọc được\n```\n\nQuy tắc thực dụng của cộng đồng: **ưu tiên composition hơn kế thừa.** Bài riêng trong module này sẽ đi sâu; ở đây chỉ cần biết kế thừa không phải lựa chọn mặc định.\n\n### Kế thừa sâu — dấu hiệu cảnh báo\n\n```\nA → B → C → D → E → F\n```\n\nCây kế thừa sâu hơn ba tầng gần như luôn là dấu hiệu thiết kế có vấn đề:\n\n```\n· Muốn hiểu F phải đọc cả sáu class\n· Đổi A ảnh hưởng năm class, khó lường hết\n· Không rõ phương thức đến từ đâu — phải tra MRO\n· Kiểm thử khó: mỗi tầng thêm một tập hành vi\n```\n\nHướng sửa: gộp các tầng trung gian không cần thiết, hoặc chuyển sang composition. Cây kế thừa **rộng và nông** (nhiều lớp con của một lớp cha) lành mạnh hơn nhiều so với **hẹp và sâu**.",

vi_du: "### Kế thừa cơ bản với `super()`\n\n```python\nclass NhanVien:\n    def __init__(self, ten: str, luong: float):\n        self.ten = ten\n        self.luong = luong\n\n    def thu_nhap(self) -> float:\n        return self.luong\n\n    def __repr__(self) -> str:\n        return f\"{type(self).__name__}({self.ten!r}, {self.luong})\"\n#              ⭐ type(self) — lớp con tự có repr đúng tên\n\nclass NhanVienBanHang(NhanVien):\n    def __init__(self, ten: str, luong: float, hoa_hong: float = 0):\n        super().__init__(ten, luong)          # ⭐ cha lo phần của cha\n        self.hoa_hong = hoa_hong\n\n    def thu_nhap(self) -> float:\n        return super().thu_nhap() + self.hoa_hong    # ⭐ MỞ RỘNG, không thay thế\n\nclass QuanLy(NhanVien):\n    def __init__(self, ten, luong, so_nhan_vien=0):\n        super().__init__(ten, luong)\n        self.so_nhan_vien = so_nhan_vien\n\n    def thu_nhap(self) -> float:\n        return super().thu_nhap() * 1.2\n\nds = [NhanVien(\"An\", 10), NhanVienBanHang(\"Bình\", 8, 5), QuanLy(\"Chi\", 15, 3)]\nfor nv in ds:\n    print(f\"{nv!r} → {nv.thu_nhap()}\")\n# NhanVien('An', 10) → 10\n# NhanVienBanHang('Bình', 8) → 13\n# QuanLy('Chi', 15) → 18.0\n```\n\nHai kỹ thuật đáng học:\n\n```\ntype(self).__name__ trong __repr__  → lớp con tự có repr đúng tên, không phải viết lại\nsuper().thu_nhap() + ...             → MỞ RỘNG logic cha thay vì chép lại\n```\n\nKỹ thuật thứ hai quan trọng: nếu công thức lương cơ bản đổi, chỉ sửa ở `NhanVien` và mọi lớp con đúng theo.\n\n### Nhìn MRO hoạt động\n\n```python\nclass A:\n    def chao(self): print(\"A\"); \nclass B(A):\n    def chao(self): print(\"B\"); super().chao()\nclass C(A):\n    def chao(self): print(\"C\"); super().chao()\nclass D(B, C):\n    def chao(self): print(\"D\"); super().chao()\n\nprint([c.__name__ for c in D.mro()])\n# ['D', 'B', 'C', 'A', 'object']\n\nD().chao()\n# D → B → C → A          ⭐ mỗi class ĐÚNG MỘT LẦN\n```\n\nSo với việc gọi thẳng tên class cha:\n\n```python\nclass B(A):\n    def chao(self): print(\"B\"); A.chao(self)      # ❌ bỏ qua C\nclass C(A):\n    def chao(self): print(\"C\"); A.chao(self)\nclass D(B, C):\n    def chao(self): print(\"D\"); B.chao(self); C.chao(self)\n\nD().chao()\n# D → B → A → C → A      ⭐ A chạy HAI LẦN\n```\n\nĐây là lý do cụ thể cho quy tắc \"luôn dùng `super()`\": nó đảm bảo mỗi class chạy đúng một lần, còn gọi thẳng tên thì không.\n\nVới `__init__` thì chạy hai lần có hậu quả thật — khởi tạo lại thuộc tính, mở lại kết nối, cộng dồn bộ đếm.\n\n### Chuỗi đứt — chẩn đoán\n\n```python\nclass Nen:\n    def thiet_lap(self):\n        print(\"Nen\")\n\nclass A(Nen):\n    def thiet_lap(self):\n        print(\"A\")\n        super().thiet_lap()\n\nclass B(Nen):\n    def thiet_lap(self):\n        print(\"B\")\n        # ❌ QUÊN super()\n\nclass C(A, B):\n    def thiet_lap(self):\n        print(\"C\")\n        super().thiet_lap()\n\nC().thiet_lap()\n# C → A → B        ⭐ \"Nen\" KHÔNG chạy, không có lỗi nào\n\nprint([c.__name__ for c in C.mro()])\n# ['C', 'A', 'B', 'Nen', 'object']    ← Nen CÓ trong MRO nhưng không tới được\n```\n\nCách chẩn đoán khi nghi ngờ chuỗi đứt:\n\n```python\ndef kiem_chuoi(cls, ten_pt):\n    \"\"\"Class nào trong MRO định nghĩa phương thức mà KHÔNG gọi super().\"\"\"\n    import inspect\n    for c in cls.mro():\n        if ten_pt in c.__dict__:\n            try:\n                nguon = inspect.getsource(c.__dict__[ten_pt])\n                co_super = \"super()\" in nguon\n            except (OSError, TypeError):\n                co_super = None\n            print(f\"  {c.__name__:<10} super(): {co_super}\")\n\nkiem_chuoi(C, \"thiet_lap\")\n#   C          super(): True\n#   A          super(): True\n#   B          super(): False    ← ⭐ đứt ở đây\n#   Nen        super(): False    (cuối chuỗi, không cần)\n```\n\n### Mẫu `**kwargs` cho đa kế thừa\n\n```python\nclass Nen:\n    def __init__(self, **kwargs):\n        super().__init__(**kwargs)        # tới object — phải hết tham số\n\nclass CoTen(Nen):\n    def __init__(self, ten: str, **kwargs):\n        super().__init__(**kwargs)\n        self.ten = ten\n\nclass CoTuoi(Nen):\n    def __init__(self, tuoi: int, **kwargs):\n        super().__init__(**kwargs)\n        self.tuoi = tuoi\n\nclass CoEmail(Nen):\n    def __init__(self, email: str, **kwargs):\n        super().__init__(**kwargs)\n        if \"@\" not in email:\n            raise ValueError(f\"email không hợp lệ: {email!r}\")\n        self.email = email\n\nclass NguoiDung(CoTen, CoTuoi, CoEmail):\n    pass\n\nnd = NguoiDung(ten=\"Minh\", tuoi=22, email=\"m@x.com\")\nprint(vars(nd))     # {'email': 'm@x.com', 'tuoi': 22, 'ten': 'Minh'}\nprint([c.__name__ for c in NguoiDung.mro()])\n# ['NguoiDung', 'CoTen', 'CoTuoi', 'CoEmail', 'Nen', 'object']\n```\n\nChú ý thứ tự trong `vars`: `email` được gán **trước** vì `CoEmail.__init__` gọi `super()` **trước** rồi mới gán — nên nó chạy sau cùng trong chuỗi đi xuống, và gán đầu tiên trên đường quay lại.\n\nGọi bằng tham số vị trí thì hỏng:\n\n```python\nNguoiDung(\"Minh\", 22, \"m@x.com\")\n# TypeError — CoTen nhận \"Minh\" làm ten, rồi chuyển 22 và \"m@x.com\"\n#             xuống dưới dưới dạng vị trí, mà CoTuoi không nhận được\n```\n\nĐây là lý do đa kế thừa `__init__` **bắt buộc dùng tham số tên**.\n\n### Mixin trong thực tế\n\n```python\nimport json, time\nfrom datetime import datetime\n\nclass JSONMixin:\n    def to_json(self, **kw) -> str:\n        return json.dumps(\n            {k: v for k, v in vars(self).items() if not k.startswith(\"_\")},\n            ensure_ascii=False, default=str, **kw)\n\nclass GhiLogMixin:\n    def log(self, tin: str) -> None:\n        print(f\"[{datetime.now():%H:%M:%S}] {type(self).__name__}: {tin}\")\n\nclass DoThoiGianMixin:\n    def do(self, ten_pt: str, *a, **k):\n        t = time.perf_counter()\n        kq = getattr(self, ten_pt)(*a, **k)\n        print(f\"{ten_pt}: {time.perf_counter()-t:.4f}s\")\n        return kq\n\nclass DonHang(JSONMixin, GhiLogMixin, DoThoiGianMixin):\n    def __init__(self, ma: str, tong: float):\n        self.ma, self.tong = ma, tong\n        self._noi_bo = \"không xuất ra JSON\"\n\n    def xu_ly(self):\n        self.log(f\"đang xử lý {self.ma}\")\n        time.sleep(0.1)\n        return \"xong\"\n\nd = DonHang(\"DH001\", 500_000)\nprint(d.to_json())          # {\"ma\": \"DH001\", \"tong\": 500000}  ← bỏ _noi_bo\nd.do(\"xu_ly\")\n```\n\nBa mixin, ba nhóm hành vi độc lập, ghép vào một class mà không class nào biết về class kia. Chú ý `to_json` lọc bỏ thuộc tính bắt đầu bằng `_` — đây là bài đóng gói áp dụng vào mixin.\n\nSo với đa kế thừa \"thật\" (nhiều class đều là cha đầy đủ), mixin an toàn hơn nhiều vì **không có trạng thái riêng và không có `__init__`** — nên không có gì để xung đột.\n\n### Ngoại lệ tự định nghĩa — kế thừa dùng đúng chỗ\n\n```python\nclass UngDungLoi(Exception):\n    \"\"\"Gốc cho mọi lỗi của ứng dụng.\"\"\"\n\nclass CauHinhLoi(UngDungLoi):\n    \"\"\"Cấu hình sai hoặc thiếu.\"\"\"\n\nclass KetNoiLoi(UngDungLoi):\n    def __init__(self, host: str, cong: int, ly_do: str = \"\"):\n        self.host, self.cong = host, cong\n        super().__init__(f\"không nối được {host}:{cong}\" +\n                         (f\" — {ly_do}\" if ly_do else \"\"))\n\n# Bắt theo mức độ cụ thể\ntry:\n    ...\nexcept KetNoiLoi as e:\n    print(f\"thử lại {e.host}\")           # xử lý riêng\nexcept UngDungLoi as e:\n    print(f\"lỗi ứng dụng: {e}\")           # mọi lỗi khác của ứng dụng\n\nprint([c.__name__ for c in KetNoiLoi.mro()])\n# ['KetNoiLoi', 'UngDungLoi', 'Exception', 'BaseException', 'object']\n```\n\nĐây là chỗ kế thừa **đúng bản chất nhất**: `KetNoiLoi` thật sự **là một** `UngDungLoi`, và cây phân cấp cho phép bắt ở đúng mức cụ thể mình muốn — nối thẳng về bài ngoại lệ ở module 5.\n\n### Kế thừa từ `dict` — cạm bẫy và cách đúng\n\n```python\n# ❌ Kế thừa dict trực tiếp\nclass DemTruyCap(dict):\n    def __init__(self, *a, **k):\n        super().__init__(*a, **k)\n        self.so_lan = 0\n\n    def __getitem__(self, k):\n        self.so_lan += 1\n        return super().__getitem__(k)\n\nd = DemTruyCap(a=1, b=2)\nd[\"a\"]; d.so_lan        # 1 ✓\nd.get(\"a\"); d.so_lan    # 1 ⚠️ get() KHÔNG gọi __getitem__\n\n# ✓ UserDict — mọi phương thức đi qua __getitem__\nfrom collections import UserDict\n\nclass DemTruyCap(UserDict):\n    def __init__(self, *a, **k):\n        self.so_lan = 0\n        super().__init__(*a, **k)\n\n    def __getitem__(self, k):\n        self.so_lan += 1\n        return super().__getitem__(k)\n\nd = DemTruyCap(a=1, b=2)\nd[\"a\"]; d.get(\"a\"); d.so_lan     # 2 ✓\n```\n\nQuy tắc: **muốn kế thừa hành vi của kiểu dựng sẵn thì dùng `UserDict`, `UserList`, `UserString`.** Kế thừa thẳng chỉ an toàn khi bạn **thêm** phương thức mới, không ghi đè phương thức có sẵn.\n\n### Khi kế thừa là lựa chọn sai\n\n```python\n# ❌ Kế thừa để dùng lại code — thừa hưởng cả những gì không muốn\nclass NganXep(list):\n    def push(self, x): self.append(x)\n    def pop_dinh(self): return self.pop()\n\nnx = NganXep()\nnx.push(1)\nnx.insert(0, 99)      # ⚠️ chèn vào giữa — phá tính chất ngăn xếp\nnx.sort()             # ⚠️ sắp xếp một ngăn xếp?\n\n# ✓ Composition — chỉ lộ ra thao tác có nghĩa\nclass NganXep:\n    def __init__(self):\n        self._ds: list = []\n\n    def push(self, x) -> None:\n        self._ds.append(x)\n\n    def pop(self):\n        if not self._ds:\n            raise IndexError(\"ngăn xếp rỗng\")\n        return self._ds.pop()\n\n    def dinh(self):\n        if not self._ds:\n            raise IndexError(\"ngăn xếp rỗng\")\n        return self._ds[-1]\n\n    def __len__(self) -> int:\n        return len(self._ds)\n\n    def __bool__(self) -> bool:\n        return bool(self._ds)\n\nnx = NganXep()\nnx.push(1); nx.push(2)\nnx.pop()          # 2\nnx.insert         # AttributeError ✓ không có thao tác vô nghĩa\n```\n\nBản composition **chỉ lộ ra bốn thao tác đúng nghĩa ngăn xếp**, và đó là toàn bộ điểm mạnh của nó: không ai dùng sai được vì không có gì để dùng sai. Đây là bài đóng gói và bài kế thừa gặp nhau.",

so_sanh: "### `super()` và gọi thẳng tên class cha\n\n| | `super().f()` | `Cha.f(self)` |\n|---|---|---|\n| Đơn kế thừa | ✓ | ✓ giống nhau |\n| Đa kế thừa | **✓ đúng MRO** | **✗ bỏ qua nhánh khác** |\n| Class chạy nhiều lần | ✗ | **✓ có thể** |\n| Đổi class cha sau này | không phải sửa | **phải sửa** |\n| Khuyến nghị | **luôn dùng** | không |\n\nKhông có trường hợp nào gọi thẳng tên class cha tốt hơn. Nó chỉ \"chạy được\" trong đơn kế thừa và tiềm ẩn lỗi khi cây kế thừa đổi.\n\n### `isinstance` và `type() is`\n\n| | `isinstance(x, C)` | `type(x) is C` |\n|---|---|---|\n| Nhận lớp con | **✓** | ✗ |\n| Nhận nhiều kiểu | ✓ `(A, B)` | ✗ |\n| Hoạt động với `Protocol` | ✓ (nếu runtime-checkable) | ✗ |\n| Khuyến nghị | **✓** | chỉ khi thật sự cần kiểu chính xác |\n\n`type(x) is C` từ chối lớp con, và đó thường là vi phạm nguyên tắc Liskov — bài riêng sẽ nói rõ vì sao đó là mùi thiết kế.\n\n### Đơn kế thừa và đa kế thừa\n\n| | Đơn | Đa |\n|---|---|---|\n| Dễ suy luận | **✓** | khó hơn nhiều |\n| Bài toán kim cương | ✗ | **✓ cần MRO** |\n| Yêu cầu mọi class gọi `super()` | không bắt buộc | **bắt buộc** |\n| Chữ ký phương thức | tự do | phải tương thích (`**kwargs`) |\n| Dùng an toàn | luôn | **chỉ với mixin** |\n\nLời khuyên thực dụng: **đa kế thừa chỉ dùng cho mixin.** Nhiều class cha \"đầy đủ\" (có trạng thái, có `__init__` phức tạp) là công thức cho lỗi khó lần.\n\n### Kế thừa và composition\n\n| | Kế thừa | Composition |\n|---|---|---|\n| Quan hệ | **\"là một\"** | **\"có một\"** |\n| Ràng buộc | **chặt nhất** | lỏng |\n| Thừa hưởng | **toàn bộ, không chọn lọc** | chỉ cái bạn gọi |\n| Đổi lúc chạy | ✗ | **✓** |\n| Đổi class cha ảnh hưởng | mọi class con | chỉ nơi dùng |\n| Kiểm thử | phải dựng cả cây | dễ thay thế bằng bản giả |\n\nBảng này là tóm tắt của bài **Composition over inheritance** trong module. Ở đây chỉ cần rút ra: **kế thừa không phải lựa chọn mặc định.**\n\n### Kế thừa kiểu dựng sẵn\n\n| | Kế thừa `dict` | `UserDict` |\n|---|---|---|\n| Phương thức nội bộ gọi override của bạn | **✗** | **✓** |\n| Hiệu năng | nhanh hơn | chậm hơn chút |\n| Dữ liệu thật nằm ở | chính object | `self.data` |\n| An toàn khi ghi đè | ✗ | **✓** |\n| Dùng khi | chỉ **thêm** phương thức | **ghi đè** hành vi |\n\nNguyên nhân là `dict` cài đặt bằng C và gọi thẳng code C, bỏ qua bảng phương thức Python.\n\n### Dấu hiệu kế thừa sai\n\n| Dấu hiệu | Ý nghĩa |\n|---|---|\n| Lớp con **vô hiệu hoá** phương thức cha (`raise NotImplementedError`) | vi phạm Liskov |\n| Lớp con thừa hưởng nhiều thứ vô nghĩa | quan hệ \"có một\", không phải \"là một\" |\n| Cây sâu hơn 3 tầng | nên gộp hoặc chuyển sang composition |\n| Phải kiểm tra `type()` để xử lý khác nhau | thiết kế đa hình chưa đúng |\n| Kế thừa chỉ để dùng lại một phương thức | dùng composition hoặc hàm |\n\nHàng đầu là dấu hiệu nghiêm trọng nhất: nếu lớp con phải **từ chối** một phương thức của lớp cha thì nó không thật sự là một lớp cha — đó là chủ đề của bài Liskov.",

loi_thuong_gap: [
  "**Quên `super().__init__()` trong lớp con.** Thuộc tính của lớp cha không được tạo, và lỗi hiện ra muộn ở chỗ khác.",
  "**Gọi thẳng `Cha.__init__(self, ...)` thay vì `super()`.** Chạy được trong đơn kế thừa, phá vỡ MRO trong đa kế thừa.",
  "**Hiểu `super()` là \"class cha\".** Nó là \"cái tiếp theo trong MRO của kiểu thật của `self`\" — khác hẳn khi đa kế thừa.",
  "**Một class trong chuỗi đa kế thừa quên gọi `super()`.** Cắt đứt phần còn lại, im lặng, không lỗi.",
  "**Dùng tham số vị trí với `__init__` trong đa kế thừa.** Không phân phối đúng được. Bắt buộc dùng tham số tên và `**kwargs`.",
  "**Đặt mixin sau class nền trong danh sách kế thừa.** MRO tra từ trái sang, nên mixin không có tác dụng.",
  "**Kế thừa `dict`, `list` rồi ghi đè phương thức.** Các phương thức C bên trong không gọi override của bạn. Dùng `UserDict`, `UserList`.",
  "**Kế thừa chỉ để dùng lại code.** Bạn thừa hưởng toàn bộ, kể cả những gì vô nghĩa với class mới.",
  "**Cây kế thừa sâu hơn ba tầng.** Khó hiểu, khó đổi, khó kiểm thử. Gộp lại hoặc chuyển sang composition.",
  "**Dùng `type(x) is C` thay vì `isinstance`.** Từ chối lớp con, thường là vi phạm Liskov.",
  "**Lớp con `raise NotImplementedError` cho phương thức của cha.** Dấu hiệu rõ ràng của kế thừa sai bản chất.",
  "**Đa kế thừa với nhiều class cha \"đầy đủ\".** Chỉ dùng đa kế thừa cho mixin không trạng thái.",
  "**Ghi đè phương thức mà không gọi `super()` khi vẫn cần logic cha.** Sao chép lại code cha là nợ kỹ thuật ngay lập tức.",
  "**Hard-code tên class trong `__repr__`.** Dùng `type(self).__name__` để lớp con tự đúng."
],

tu_kiem_tra: [
  "Kế thừa mở rộng quy tắc tra cứu thuộc tính thế nào? Liệt kê đủ các bước khi đọc `c.x`.",
  "Vì sao `gioi_thieu()` định nghĩa ở lớp cha lại gọi ra `keu()` của lớp con?",
  "`super()` thật sự nghĩa là gì? Vì sao \"class cha\" là cách hiểu sai?",
  "MRO là gì? Nêu ba tính chất mà thuật toán C3 đảm bảo.",
  "Trong bài toán kim cương `D(B, C)`, `super()` trong `B` gọi tới đâu và vì sao?",
  "Chuyện gì xảy ra nếu một class trong chuỗi đa kế thừa quên gọi `super()`?",
  "Vì sao đa kế thừa `__init__` bắt buộc dùng tham số tên và `**kwargs`?",
  "Nêu bốn quy ước làm mixin an toàn. Vì sao mixin phải đặt trước class nền?",
  "Vì sao kế thừa `dict` rồi ghi đè `__setitem__` lại không có tác dụng với `update()`?",
  "Nêu phép thử để biết kế thừa có hợp lý không. Cho hai ví dụ đúng và hai ví dụ sai.",
  "Hai cái giá của kế thừa là gì?",
  "Vì sao cây kế thừa sâu hơn ba tầng là dấu hiệu cảnh báo?",
  "Vì sao dùng `type(self).__name__` trong `__repr__` thay vì viết thẳng tên class?",
  "Nêu năm dấu hiệu của kế thừa sai."
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Inheritance* và *Multiple Inheritance* trong tutorial; mục *super()* trong tham khảo hàm dựng sẵn.",
  "Tài liệu Python, bài *The Python 2.3 Method Resolution Order* — mô tả chính thức thuật toán C3, vẫn đúng cho Python 3.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về kế thừa nói kỹ về MRO, mixin, và cạm bẫy kế thừa kiểu dựng sẵn.",
  "Sách **Effective Python** — Brett Slatkin. Các mục về `super()`, về mixin, và về việc ưu tiên composition.",
  "Tài liệu Python, module `collections` — `UserDict`, `UserList`, `UserString` và lý do chúng tồn tại."
],

lien_he: [
  "**Đóng gói** ngay trước: `__x` tồn tại chính vì kế thừa — nó ngăn lớp con vô tình đè dữ liệu nội bộ của lớp cha.",
  "**Đa hình và duck typing** ngay sau: liên kết động ở bài này là cơ chế nền của đa hình.",
  "**Trừu tượng: abstract class, Protocol**: class trừu tượng là kế thừa dùng để ép giao ước, không để dùng lại code.",
  "**Composition over inheritance**: bài này nêu hai cái giá của kế thừa; bài đó nói khi nào nên chọn cách khác.",
  "**SOLID — Liskov Substitution**: \"lớp con phải thay được lớp cha\" là điều kiện làm kế thừa đúng đắn.",
  "**Class, object, thuộc tính**: MRO chỉ là phần mở rộng của quy tắc tra cứu đã học ở đó.",
  "**Xử lý ngoại lệ** ở module 5: cây phân cấp ngoại lệ là ví dụ kế thừa đúng bản chất nhất trong thư viện chuẩn.",
  "**Module, package, import** ở module 5: MRO và `sys.path` cùng là bài toán \"tìm theo thứ tự, lấy cái đầu tiên\".",
  "**Backend & Web** ở mảng 05: các framework dùng mixin rất nhiều cho view và cho model.",
  "**AI/ML** ở mảng 07: các lớp mạng nơ-ron đều kế thừa từ một class nền, và `super().__init__()` là dòng bắt buộc."
]

};
