window.LESSONS["05:framework-xay-api-that:node-js-express-tham-khao"] = {

tom_tat: [
  "Node.js chạy JavaScript trên event loop; Express cung cấp router và chuỗi middleware mỏng, không tự giải quyết schema, database transaction hay cấu trúc dự án.",
  "I/O bất đồng bộ cho phép một process phục vụ nhiều kết nối đang chờ, nhưng một đoạn tính toán CPU dài vẫn chặn mọi request trên cùng event loop.",
  "Thứ tự middleware là semantics: parser, request ID, authentication, route, 404 và error handler đặt sai vị trí sẽ tạo hành vi khác.",
  "API Bàn Trống cần validation runtime dù viết TypeScript, vì type bị xóa khi chạy và dữ liệu từ mạng không đáng tin.",
  "Triển khai thật phải đặt giới hạn body, timeout, proxy trust, shutdown có thoát tải và xử lý promise rejection; `app.listen()` chỉ là điểm bắt đầu."
],

can_biet_truoc: [
  "JavaScript object, function, Promise, `async`/`await` và module.",
  "HTTP request–response, status code, JSON và header.",
  "FastAPI routing, middleware, Pydantic schema và exception handler.",
  "Khái niệm event loop, process, connection pool và transaction ở mức cơ bản."
],

dinh_nghia: "**Node.js** là môi trường chạy JavaScript phía server. Nó dùng event loop để điều phối callback khi I/O hoàn tất, nhờ đó một thread JavaScript không phải đứng chờ từng socket. Một số công việc được hệ điều hành hoặc thread pool hỗ trợ, nhưng code JavaScript thông thường vẫn chạy lần lượt trên event loop của process.\n\n**Express** là web framework tối giản cho Node.js. Ứng dụng là một chuỗi hàm middleware nhận `req`, `res`, `next`; middleware có thể kết thúc response hoặc chuyển quyền cho bước sau. Express định tuyến và hỗ trợ HTTP, nhưng cố ý không áp ORM, validator, dependency injection hay layout dự án.\n\nTrong module này Express là ngôn ngữ đối chiếu với FastAPI: cùng hợp đồng đặt bàn được giữ nguyên, còn cách biểu diễn schema, vòng đời và lỗi cho thấy điều gì thuộc framework, điều gì thuộc bản chất backend.",

vi_sao_can: "JavaScript phổ biến ở frontend nên một đội có thể chia sẻ ngôn ngữ và package model giữa browser với server. Node phù hợp với API dành nhiều thời gian chờ database, cache hoặc dịch vụ khác; event loop giữ chi phí concurrency thấp mà không cần một thread ứng dụng cho mỗi connection.\n\nExpress có giá trị vì lõi nhỏ và dễ nhìn: route là function, middleware là function, response được viết trực tiếp. Nó hợp cho service có stack đã chuẩn hóa hoặc gateway cần kiểm soát chi tiết. Mặt trái chính là những thứ FastAPI tạo sẵn từ type hint—validation, OpenAPI, dependency graph—phải được chọn và nối vào.\n\nHọc Express còn sửa một ngộ nhận: “Node là async nên tự nhanh”. Nếu Bàn Trống băm mật khẩu bằng phép tính nặng, xử lý ảnh menu hoặc parse payload khổng lồ trên event loop, mọi request khác cùng process bị trì hoãn. Kiến trúc tốt phải phân loại I/O-bound và CPU-bound, đặt timeout và đẩy việc nặng sang worker phù hợp.",

co_che: "### Một lượt quay của event loop\n\nKhi route gọi database bằng API bất đồng bộ, JavaScript đăng ký công việc rồi trả quyền điều khiển. Event loop có thể bắt đầu request khác; khi socket báo kết quả, callback/promise tiếp tục được xếp lịch. Đây là concurrency, không nhất thiết là parallelism: hai đoạn JavaScript không cùng chạy trên một event loop.\n\n`await` không chặn cả process nếu promise đại diện I/O đúng cách; nó chỉ tạm dừng async function hiện tại. Ngược lại, vòng lặp tính toán 500 ms không có điểm nhường sẽ giữ thread 500 ms. Một endpoint `/health` rất nhẹ cũng phải chờ, khiến orchestrator tưởng replica chết.\n\n### Middleware là một pipeline có thứ tự\n\nExpress duyệt middleware theo thứ tự đăng ký. Mỗi bước có ba lựa chọn: ghi response, gọi `next()` hoặc chuyển lỗi. Nếu middleware gửi response rồi vẫn gọi `next`, bước sau có thể cố ghi header lần hai. Nếu quên cả hai, request treo đến timeout.\n\nMột thứ tự hợp lý cho Bàn Trống là request ID và log cơ bản; security headers; body parser có giới hạn; authentication; route; handler 404; cuối cùng error handler bốn tham số. Rate limit đặt trước route tốn kém, nhưng sau proxy normalization và nhận diện client đáng tin.\n\n```javascript\nimport express from \"express\";\nimport crypto from \"node:crypto\";\n\nconst app = express();\n\napp.use((req, res, next) => {\n  req.requestId = req.get(\"x-request-id\") || crypto.randomUUID();\n  res.set(\"x-request-id\", req.requestId);\n  next();\n});\napp.use(express.json({ limit: \"64kb\", strict: true }));\napp.use(\"/v1/bookings\", bookingRouter);\napp.use((req, res) => res.status(404).json({ error: \"not_found\" }));\napp.use(errorHandler);\n```\n\nKhông nên tin mọi `x-request-id`: giới hạn độ dài/ký tự hoặc tự sinh để tránh log injection. Body limit giảm nguy cơ cạn bộ nhớ; reverse proxy cũng cần limit vì request bị chặn càng sớm càng rẻ.\n\n### Router tách HTTP khỏi use case\n\nRouter chỉ nên chuyển dữ liệu đã kiểm tra thành command, gọi service rồi ánh xạ kết quả miền sang HTTP. Logic tìm bàn trống không nên phụ thuộc `req`/`res`, nhờ đó unit test không cần dựng server.\n\n```javascript\nimport { Router } from \"express\";\nimport { bookingSchema } from \"./schemas.js\";\nimport { createBooking } from \"./service.js\";\n\nexport const bookingRouter = Router();\n\nbookingRouter.post(\"/\", async (req, res, next) => {\n  try {\n    const parsed = bookingSchema.safeParse(req.body);\n    if (!parsed.success) {\n      return res.status(422).json({\n        error: \"invalid_booking\",\n        details: parsed.error.issues.map(({ path, code }) => ({ path, code })),\n      });\n    }\n    const result = await createBooking(parsed.data, {\n      idempotencyKey: req.get(\"idempotency-key\"),\n    });\n    return res.status(201).json(result);\n  } catch (error) {\n    return next(error);\n  }\n});\n```\n\n`return` trước `res.status` tránh code tiếp tục chạy. Không gửi nguyên validator error nếu nó chứa đầu vào nhạy cảm. Tùy phiên bản Express và convention dự án, promise rejection có thể được chuyển tự động hoặc cần wrapper; đội phải chuẩn hóa và kiểm thử thay vì giả định.\n\n### TypeScript không validate gói tin mạng\n\nInterface TypeScript biến mất khi transpile. Client vẫn có thể gửi `partySize: \"4\"`, thiếu email hoặc thêm `isAdmin: true`. Ép kiểu bằng `as BookingInput` chỉ làm compiler im lặng.\n\nSchema runtime như Zod kiểm tra giá trị thật và có thể suy ra type TypeScript, giảm hai nguồn mô tả. Cần chọn coercion có chủ ý: tự đổi chuỗi `\"4\"` thành số đôi khi tiện cho form, nhưng với JSON API nghiêm ngặt nó có thể che bug client. Các trường không biết nên bị loại hoặc từ chối để chống mass assignment.\n\n```javascript\nimport { z } from \"zod\";\n\nexport const bookingSchema = z.object({\n  guestName: z.string().trim().min(1).max(120),\n  guestEmail: z.string().email().transform(v => v.toLowerCase()),\n  partySize: z.number().int().min(1).max(20),\n  startsAt: z.string().datetime({ offset: true }),\n}).strict();\n```\n\nSchema HTTP không thay constraint database. Hai request hợp lệ riêng lẻ vẫn có thể tranh suất cuối. Service phải dùng transaction và constraint/lock như bản FastAPI/Django.\n\n### Lỗi vận hành khác lỗi nghiệp vụ\n\nĐịnh nghĩa class lỗi có `code`, `status` và metadata an toàn giúp error handler không đoán bằng chuỗi message. `BookingConflict` thành 409; lỗi validation thành 422; database timeout thường thành 503 nếu client có thể thử lại. Bug không biết vẫn là 500.\n\n```javascript\nfunction errorHandler(err, req, res, next) {\n  if (res.headersSent) return next(err);\n\n  const status = Number.isInteger(err.status) ? err.status : 500;\n  req.log?.error({ err, requestId: req.requestId }, \"request failed\");\n  return res.status(status).json({\n    error: status === 500 ? \"internal_error\" : err.code,\n    requestId: req.requestId,\n  });\n}\n```\n\nKhông trả `err.stack`, SQL hay message driver cho client. Logging object error nên dùng logger có serializer và redaction; `JSON.stringify(err)` thường mất stack. Sau khi headers đã gửi, handler không thể đổi status an toàn nên phải giao lại cho server đóng connection.\n\n### Promise, callback và lỗi bị thất lạc\n\n`try/catch` quanh lời gọi callback không bắt lỗi xảy ra ở lượt event loop sau. Với promise, phải `await` hoặc `return` promise; tạo promise rồi bỏ quên dẫn đến rejection không gắn request. Không nên cài global `unhandledRejection` để log rồi giả vờ process vẫn khỏe: trạng thái có thể không còn đáng tin.\n\nChiến lược an toàn là log sự kiện ở biên process, ngừng nhận request, drain kết nối và thoát để supervisor tạo process mới. Đây không thay việc bắt lỗi dự kiến tại route. Global handler là dây an toàn cuối, không phải luồng điều khiển thường ngày.\n\n### Database pool và transaction\n\nMột pool cho mỗi process thường được tạo lúc startup và đóng lúc shutdown. Nếu chạy tám process, mỗi pool tối đa 20 connection có thể tạo 160 connection; phải tính theo toàn deployment, không theo file cấu hình đơn lẻ.\n\nService tạo booking cần lấy client từ pool, `BEGIN`, kiểm tra/khóa inventory, insert booking và outbox, `COMMIT`; trong `finally` trả client. Khi có lỗi phải `ROLLBACK`. Không dùng `pool.query` xen giữa transaction trên client vì câu lệnh có thể đi qua connection khác.\n\nIdempotency key cần unique constraint gắn với caller và endpoint. Nếu client timeout sau commit rồi retry, server trả kết quả đã có thay vì insert lần hai. Chỉ cache response trong memory không đủ vì request có thể vào replica khác hoặc process restart.\n\n### Timeout, hủy và retry có ngân sách\n\nNode server có nhiều loại timeout: thời gian nhận header/body, thời gian giữ connection và deadline nghiệp vụ. Database driver và HTTP client cũng cần timeout riêng. Một reverse proxy timeout 30 giây nhưng database chờ 60 giây làm app tiếp tục tiêu tài nguyên sau khi client đã bỏ đi.\n\n`AbortController` có thể truyền tín hiệu hủy qua các thư viện hỗ trợ. Tuy nhiên client ngắt socket không đồng nghĩa transaction database tự rollback; code vẫn cần deadline và cleanup. Retry chỉ dành cho thao tác an toàn hoặc có idempotency, kèm backoff, jitter và giới hạn tổng thời gian. Retry mọi 500 có thể nhân tải đúng lúc dependency suy yếu.\n\n### CPU-bound và worker\n\nJSON nhỏ, routing và ánh xạ dữ liệu phù hợp event loop. Nén lớn, xử lý ảnh, PDF hoặc thuật toán tối ưu xếp bàn phức tạp có thể chặn nó. Chia nhỏ bằng `setImmediate` chỉ nhường lượt, không tạo thêm sức CPU.\n\nWorker threads phù hợp công việc CPU trong process khi có giao thức message rõ; queue và worker service phù hợp job lâu, cần retry và bền vững. Nhiều process tận dụng nhiều core nhưng mỗi process có memory/pool riêng. Đừng chuyển mọi việc sang worker: overhead sao chép dữ liệu và độ phức tạp có thể lớn hơn công việc nhỏ.\n\n### Proxy trust và kết nối bảo mật\n\nNếu Express chạy sau load balancer, `req.ip`, `req.protocol` và secure cookie phụ thuộc `trust proxy`. Đặt `true` vô điều kiện khi app còn truy cập trực tiếp cho phép client giả forwarded header. Nên khai topology hoặc hàm tin cậy dải proxy cụ thể, đồng thời firewall không cho bypass proxy.\n\nTLS thường kết thúc ở edge; kết nối nội bộ vẫn cần đánh giá threat model. Security header, CORS và rate limit là các lớp khác nhau. Rate limit theo IP không đủ chống tài khoản phân tán và có thể phạt nhiều người chung NAT.\n\n### Graceful shutdown không phải `process.exit()`\n\nKhi nhận `SIGTERM`, replica phải báo không sẵn sàng, ngừng nhận connection mới, chờ request đang chạy trong một deadline, đóng pool/telemetry rồi thoát. `process.exit()` cắt ngang `finally`, log buffer và transaction.\n\n```javascript\nconst server = app.listen(config.port);\n\nasync function shutdown(signal) {\n  logger.info({ signal }, \"draining\");\n  server.close(async (serverError) => {\n    try {\n      await dbPool.end();\n      await logger.flush?.();\n      process.exitCode = serverError ? 1 : 0;\n    } catch (error) {\n      logger.error({ error }, \"shutdown failed\");\n      process.exitCode = 1;\n    }\n  });\n}\n\nprocess.once(\"SIGTERM\", () => void shutdown(\"SIGTERM\"));\nprocess.once(\"SIGINT\", () => void shutdown(\"SIGINT\"));\n```\n\nCần watchdog ở ngoài để kill sau grace period nếu request treo. Readiness phải chuyển false trước khi đóng listener đủ lâu để load balancer cập nhật. WebSocket và keep-alive cần chính sách đóng riêng.\n\n### Quan sát mà không tiết lộ dữ liệu\n\nLog có cấu trúc nên chứa request ID, route pattern, status, duration và error code; không ghi nguyên body, authorization header hay email khách. Dùng route pattern `/bookings/:id` thay URL thật để metric không bùng cardinality.\n\nMetrics cần latency histogram, số request đang xử lý, tỷ lệ lỗi theo class và pool saturation. Trace truyền context qua promise; thư viện instrumentation giúp nhưng phải kiểm tra redaction. Event loop lag là tín hiệu đặc biệt của Node: tăng mạnh có thể chỉ ra CPU block hoặc garbage collection.\n\n### Ca biên của JavaScript\n\n`0`, chuỗi rỗng, `null` và `undefined` khác nhau; dùng `value || default` sẽ thay cả giá trị 0 hợp lệ, trong khi `value ?? default` chỉ thay nullish. `Number(\" \")` thành 0; `parseInt(\"4guests\")` thành 4, nên coercion tùy tiện làm dữ liệu bẩn được nhận.\n\nSố JavaScript là floating-point; ID database lớn có thể vượt vùng integer chính xác. Trả ID dạng chuỗi hoặc dùng driver mapping thích hợp. Date parsing và timezone cần schema rõ; không dựa múi giờ máy chủ. Object merge từ body có thể gây mass assignment hoặc prototype pollution nếu thư viện cũ/xử lý sai; allowlist field là phòng tuyến tốt.\n\n### Khi Express không phải lựa chọn tốt\n\nNếu đội muốn OpenAPI, validation và dependency injection thống nhất nhưng chưa có platform convention, Express thuần khiến mỗi service tự phát minh. Framework có opinion mạnh hơn trong hệ Node có thể phù hợp. Nếu workload chủ yếu CPU nặng, lợi thế event loop không giải quyết nút thắt; ngôn ngữ/runtime khác hoặc worker architecture nên được cân nhắc.\n\nNgược lại, không nên loại Node chỉ vì JavaScript “đơn luồng”. API I/O-bound được thiết kế đúng có thể vận hành hiệu quả. Quyết định phải dựa trên profiling, thư viện cần dùng, năng lực on-call và sự nhất quán của tổ chức.",

vi_du: "### Chạy một API Bàn Trống tối thiểu nhưng kiểm thử được\n\nTách `app` khỏi listener để test import không mở port:\n\n```javascript\n// app.js\nexport function buildApp({ bookingService, logger }) {\n  const app = express();\n  app.use(express.json({ limit: \"64kb\" }));\n  app.post(\"/v1/bookings\", makeCreateBooking({ bookingService }));\n  app.use((req, res) => res.status(404).json({ error: \"not_found\" }));\n  app.use(makeErrorHandler({ logger }));\n  return app;\n}\n\n// server.js\nconst app = buildApp({ bookingService, logger });\nconst server = app.listen(config.port);\n```\n\nTest inject service giả, không chạm database:\n\n```javascript\nimport request from \"supertest\";\n\ntest(\"từ chối partySize bằng 0\", async () => {\n  const bookingService = { create: jest.fn() };\n  const app = buildApp({ bookingService, logger: silentLogger });\n  const response = await request(app)\n    .post(\"/v1/bookings\")\n    .send({\n      guestName: \"An\",\n      guestEmail: \"an@example.com\",\n      partySize: 0,\n      startsAt: \"2030-06-15T19:00:00+07:00\",\n    });\n  expect(response.status).toBe(422);\n  expect(bookingService.create).not.toHaveBeenCalled();\n});\n```\n\nCa này chứng minh validation chặn trước service. Cần thêm integration test với PostgreSQL thật cho transaction/idempotency và test shutdown với request chậm. Không dùng database mock để tuyên bố constraint hoạt động.\n\n### Đối chiếu response với bản FastAPI\n\nCả hai implementation nên trả 201 cùng `{id, status}`, 422 cùng mã `invalid_booking`, 409 khi hết chỗ và request ID trong lỗi 500. OpenAPI có thể được sinh từ schema ở Node, nhưng phải kiểm tra schema dùng cho runtime và tài liệu không trôi lệch. Contract test dùng chung là bằng chứng mạnh hơn việc hai file YAML trông giống nhau.",

so_sanh: "### Express và FastAPI dưới cùng yêu cầu\n\n| Khía cạnh | Express | FastAPI |\n|---|---|---|\n| Runtime | Node.js event loop | Python trên ASGI server |\n| Route | callback/middleware | path operation function |\n| Validation | chọn và nối thư viện runtime | Pydantic tích hợp từ type hint |\n| OpenAPI | bổ sung/generate có chủ ý | sinh mặc định từ route/schema |\n| Dependency | closure, factory, container tùy đội | `Depends` có graph rõ |\n| Xử lý lỗi | `next(error)` và error middleware | exception handler |\n| Tự do cấu trúc | rất cao | có quy ước API rõ hơn |\n| Bẫy đặc trưng | block event loop, promise thất lạc | blocking call trong async route |\n\nHai bên giống nhau ở phần khó: transaction, idempotency, secret, timeout, proxy và observability. Chuyển ngôn ngữ không xóa các ràng buộc phân tán. Express đáng dùng khi hệ JavaScript mang lại lợi ích đội ngũ và tổ chức đã có paved road; FastAPI đáng dùng khi schema Python và API type-first giảm nhiều công tích hợp.",

loi_thuong_gap: [
  "Tin TypeScript đã validate body: request chứa kiểu sai vẫn lọt lúc chạy; nhận biết khi lỗi phát sinh sâu trong service, sửa bằng schema runtime ở biên HTTP.",
  "Quên `return` sau khi gửi response: code tiếp tục và ghi header lần hai; nhận biết qua lỗi “headers already sent”.",
  "Thực hiện CPU loop dài trong route: mọi endpoint cùng worker tăng latency; nhận biết bằng event loop lag và profile CPU, sửa bằng worker/queue.",
  "Đặt error handler trước routes hoặc khai ba tham số: Express không dùng nó như error middleware; nhận biết khi response mặc định lộ HTML/stack.",
  "Tin toàn bộ forwarded header với `trust proxy=true`: client giả IP/protocol; nhận biết bằng request bypass edge, sửa trust topology và firewall.",
  "Tạo pool cho mỗi module hoặc tính connection theo một process: deployment làm cạn database; nhận biết qua số connection tăng theo worker, sửa singleton theo process và budget toàn cụm.",
  "Gọi `process.exit()` ngay khi SIGTERM: request/transaction bị cắt và log mất; nhận biết qua lỗi tăng lúc rollout, sửa bằng drain có deadline.",
  "Retry POST sau timeout không có idempotency key: sinh hai booking; nhận biết bằng bản ghi trùng thời điểm, sửa unique key bền vững và replay response."
],

tu_kiem_tra: [
  "Vì sao `await` một database query không chặn event loop, còn vòng lặp JavaScript 500 ms lại chặn?",
  "Hãy sắp thứ tự request ID, JSON parser, auth, router, 404 và error handler; đổi hai bước nào sẽ gây lỗi quan sát được?",
  "TypeScript interface và Zod schema bảo vệ hai thời điểm khác nhau ra sao?",
  "Tính tổng connection database nếu có 6 replica, mỗi replica 4 process và pool 10; bạn sẽ giới hạn thế nào?",
  "Client ngắt kết nối giữa transaction có bảo đảm rollback không? Hãy mô tả deadline và cleanup cần có.",
  "Thiết kế shutdown cho WebSocket khác request HTTP ngắn ở điểm nào?",
  "Vì sao idempotency record phải ở storage dùng chung thay vì Map trong memory?",
  "Khi event loop lag thấp nhưng latency cao và pool chờ connection, nút thắt có khả năng nằm ở đâu?"
],

tai_nguyen: [
  "Node.js Documentation — Event Loop, Timers và `process`.",
  "Express Documentation — Using middleware và Error handling.",
  "Node.js Design Patterns.",
  "OWASP Node.js Security Cheat Sheet."
],

lien_he: [
  "Nối sang mảng Hệ điều hành: process, signal và thread pool giải thích concurrency cùng graceful shutdown của Node.",
  "Nối sang mảng Cơ sở dữ liệu: pool budget, transaction, unique constraint và idempotency mới bảo vệ đặt bàn khi có tranh chấp.",
  "Nối sang mảng Bảo mật web: validation runtime, proxy trust, body limit và redaction tạo nhiều lớp phòng thủ ở biên Internet.",
  "Nối sang mảng Hệ thống phân tán: timeout, retry, backoff và request ID quyết định cách service phản ứng khi dependency chập chờn."
]

};
