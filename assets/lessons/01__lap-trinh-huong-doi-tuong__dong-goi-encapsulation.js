window.LESSONS["01:lap-trinh-huong-doi-tuong:dong-goi-encapsulation"] = {

tom_tat: [
  "**Đóng gói không phải là giấu dữ liệu — nó là giấu QUYẾT ĐỊNH có thể thay đổi.** Mục đích là để bạn sửa bên trong mà không phá code người khác.",
  "**Python chọn quy ước thay vì ép buộc**: `_x` là lời hứa \"đừng dùng, tôi có quyền đổi\". Không có `private` thật, và đó là quyết định thiết kế có lý do.",
  "**Đừng viết getter/setter cho mọi thuộc tính.** Để thuộc tính công khai, và chỉ đổi sang `@property` **khi thật sự cần** — người dùng không phải sửa gì.",
  "**Bất biến (*invariant*) là thứ đóng gói bảo vệ**: quy tắc luôn đúng về trạng thái object, ví dụ \"số dư không âm\". Mọi đường vào phải đi qua chỗ kiểm tra.",
  "**Rò rỉ tham chiếu tới dữ liệu mutable phá vỡ đóng gói** dù thuộc tính có gạch dưới. Trả `list` nội bộ ra ngoài là cho người khác sửa ruột object của bạn."
],

can_biet_truoc: [
  "Bài **Class, object, thuộc tính, phương thức** ngay trước — `@property`, `_x`, `__x` đã giới thiệu ở đó.",
  "Bài **Cú pháp, kiểu dữ liệu** ở module 5 — \"biến là nhãn\" giải thích vì sao trả `list` ra ngoài là rò rỉ.",
  "Bài **Decorator** ở module 5 — `@property` và `@x.setter` là decorator.",
  "Bài **Xử lý ngoại lệ** ở module 5 — `raise ValueError` khi dữ liệu vi phạm ràng buộc là mẫu chuẩn ở đây."
],

dinh_nghia: "Nói đời thường: đóng gói giống như **mặt trước của một chiếc máy giặt**. Bạn có nút bấm và cửa; bạn không cần biết bên trong có bao nhiêu van, và hãng có thể đổi toàn bộ ruột máy ở đời sau mà bạn vẫn dùng y như cũ. Cái đáng giá không phải là \"giấu\" — mà là **hãng đổi được ruột mà không phải dạy lại người dùng**.\n\nNói chính xác:\n\n- **Đóng gói** (*encapsulation*) — gói dữ liệu cùng các thao tác lên dữ liệu đó, và **chỉ để lộ ra những gì người dùng cần**.\n- **Giao diện công khai** (*public interface*) — tập thuộc tính và phương thức mà người ngoài được phép dùng. Đây là **cam kết**: bạn hứa không phá nó.\n- **Chi tiết cài đặt** (*implementation detail*) — mọi thứ còn lại. Bạn có quyền đổi bất cứ lúc nào.\n- **Bất biến** (*invariant*) — điều kiện luôn phải đúng về trạng thái object. Ví dụ: \"số dư ≥ 0\", \"danh sách luôn được sắp xếp\".\n- **`_x`** — quy ước báo hiệu nội bộ. Không có ép buộc nào.\n- **`__x`** — bị đổi tên thành `_Lop__x`. Mục đích là **tránh xung đột khi kế thừa**, không phải bảo mật.\n\nĐiểm cần đính chính vì sách vở hay dạy sai: **đóng gói không phải \"làm mọi thứ private rồi viết getter/setter\".** Một class có mười thuộc tính private với mười cặp getter/setter thì **không đóng gói gì cả** — nó chỉ dài hơn, và người ngoài vẫn đọc ghi được mọi thứ. Đóng gói thật nằm ở việc **quyết định cái gì không cần lộ ra**.",

vi_sao_can: "Đóng gói là thứ quyết định code của bạn có sửa được hay không sau sáu tháng:\n\n- **Nó cho bạn quyền đổi ý.** Nếu người khác chỉ dùng ba phương thức công khai, bạn viết lại toàn bộ ruột cũng không ai bị ảnh hưởng. Nếu họ đụng thẳng vào thuộc tính, mỗi thay đổi nhỏ là một lần phá code người khác.\n- **Nó bảo vệ tính đúng đắn của dữ liệu.** Không có nó thì bất kỳ đoạn code nào cũng đặt được số dư âm, và bạn không bao giờ tìm ra chỗ nào làm việc đó.\n- **Nó thu hẹp phạm vi phải hiểu.** Dùng một class mà chỉ cần đọc năm phương thức công khai, thay vì đọc hết 500 dòng.\n- **Nó là nền của mọi nguyên tắc thiết kế sau.** SOLID, design pattern, kiến trúc phân tầng — tất cả dựa trên ý \"phần này không cần biết ruột của phần kia\".\n- **Rò rỉ đóng gói là lỗi âm thầm.** Code chạy đúng hôm nay, và hỏng khi ai đó sửa một dòng ở nơi tưởng như không liên quan.\n\nTình huống thật rất kinh điển: bạn viết class `GioHang` có `self.san_pham = []` và cho phép người dùng đọc `gio.san_pham`. Ba tháng sau bạn cần thêm quy tắc \"tối đa 50 món\" và \"tự tính lại tổng khi thay đổi\". Nhưng khắp dự án đã có mười chỗ viết `gio.san_pham.append(...)` — **bỏ qua hoàn toàn mọi quy tắc bạn định thêm**. Sửa đúng thì phải đi tìm và sửa cả mười chỗ. Phần cơ chế sẽ chỉ cách thiết kế để tình huống này không xảy ra ngay từ đầu.",

co_che: "### Đóng gói thật sự bảo vệ cái gì\n\nCâu hỏi đúng không phải \"cái gì nên private\" mà là: **cái gì tôi có thể muốn đổi sau này?**\n\n```python\nclass TaiKhoan:\n    def __init__(self, chu: str):\n        self.chu = chu               # ⭐ CÔNG KHAI — đơn giản, ổn định\n        self._so_du = 0.0            # ⭐ NỘI BỘ — có bất biến phải giữ\n        self._lich_su: list[dict] = []\n\n    def nap(self, so_tien: float) -> None:\n        if so_tien <= 0:\n            raise ValueError(f\"số tiền nạp phải dương: {so_tien}\")\n        self._so_du += so_tien\n        self._ghi(\"nạp\", so_tien)\n\n    def rut(self, so_tien: float) -> None:\n        if so_tien <= 0:\n            raise ValueError(f\"số tiền rút phải dương: {so_tien}\")\n        if so_tien > self._so_du:\n            raise ValueError(f\"không đủ số dư: cần {so_tien}, có {self._so_du}\")\n        self._so_du -= so_tien\n        self._ghi(\"rút\", so_tien)\n\n    @property\n    def so_du(self) -> float:         # ⭐ chỉ ĐỌC — không có setter\n        return self._so_du\n\n    def _ghi(self, loai: str, tien: float) -> None:     # nội bộ\n        self._lich_su.append({\"loai\": loai, \"tien\": tien})\n```\n\nHai quyết định đáng chú ý:\n\n```\nself.chu công khai      → chỉ là một cái tên, không có ràng buộc nào\nso_du chỉ đọc           → BẤT BIẾN \"số dư ≥ 0\" chỉ giữ được nếu MỌI thay đổi\n                          đi qua nap()/rut()\n```\n\nNếu `so_du` cho phép gán, ai đó viết `tk.so_du = -1000` là bất biến vỡ, và bạn không có cách nào biết chỗ nào làm việc đó.\n\n**Không phải mọi thứ đều nên nội bộ.** `self.chu` là chuỗi tên, không có quy tắc nào cần bảo vệ — bọc nó bằng getter/setter chỉ làm code dài hơn mà không được gì.\n\n### Bất biến — thứ đáng bảo vệ\n\n```python\nclass DanhSachSapXep:\n    \"\"\"Bất biến: _ds LUÔN được sắp xếp.\"\"\"\n\n    def __init__(self, ds=()):\n        self._ds = sorted(ds)              # ⭐ thiết lập bất biến ngay từ đầu\n\n    def them(self, x):\n        import bisect\n        bisect.insort(self._ds, x)          # ⭐ chèn mà VẪN giữ thứ tự\n\n    def tim(self, x) -> bool:\n        import bisect\n        i = bisect.bisect_left(self._ds, x)\n        return i < len(self._ds) and self._ds[i] == x    # O(log n) — nhờ bất biến\n```\n\nBất biến \"luôn sắp xếp\" cho phép `tim` chạy O(log n) thay vì O(n). Nhưng nó chỉ đúng **nếu không ai chèn bậy vào `_ds`**. Đóng gói ở đây không phải để giấu — nó là điều kiện để thuật toán đúng.\n\nQuy tắc: **mọi đường vào thay đổi trạng thái phải đi qua chỗ kiểm tra.** Một cửa sau là bất biến vỡ.\n\n### Ba mức truy cập trong Python\n\n```python\nclass A:\n    def __init__(self):\n        self.cong_khai = 1        # API — cam kết giữ ổn định\n        self._noi_bo = 2          # quy ước — có quyền đổi\n        self.__doi_ten = 3        # đổi thành _A__doi_ten\n\na = A()\na.cong_khai       # 1\na._noi_bo         # 2 — truy cập được, chỉ là bạn KHÔNG NÊN\na.__doi_ten       # AttributeError\na._A__doi_ten     # 3 — vẫn lấy được\n```\n\nCơ chế của hai gạch dưới gọi là **đổi tên** (*name mangling*): trong thân class `A`, mọi `self.__x` được trình biên dịch viết lại thành `self._A__x`.\n\nMục đích thật của nó — **tránh xung đột khi kế thừa**, không phải bảo mật:\n\n```python\nclass Cha:\n    def __init__(self):\n        self.__kho = \"của Cha\"        # → _Cha__kho\n    def doc_cha(self):\n        return self.__kho\n\nclass Con(Cha):\n    def __init__(self):\n        super().__init__()\n        self.__kho = \"của Con\"        # → _Con__kho, KHÔNG đè lên của Cha\n    def doc_con(self):\n        return self.__kho\n\nc = Con()\nc.doc_cha()      # 'của Cha' ✓ hai kho tách biệt\nc.doc_con()      # 'của Con'\nvars(c)          # {'_Cha__kho': 'của Cha', '_Con__kho': 'của Con'}\n```\n\nKhông có đổi tên thì lớp con vô tình ghi đè dữ liệu nội bộ của lớp cha — một lỗi rất khó lần.\n\nKhi nào dùng cái nào:\n\n```\n_x    → mặc định cho mọi thứ nội bộ. Đủ dùng trong 95% trường hợp.\n__x   → CHỈ khi class sẽ được kế thừa và bạn cần chắc lớp con không đụng vào.\n```\n\nDùng `__x` bừa bãi gây phiền: gỡ lỗi khó hơn, kế thừa khó hơn, và không đổi lại được lợi ích gì về an toàn.\n\n### Vì sao Python chọn quy ước thay vì ép buộc\n\nĐây là khác biệt triết lý rõ rệt với Java hay C++:\n\n```\nJava:   private → trình biên dịch CHẶN\nPython: _x     → quy ước, người dùng tự chịu trách nhiệm\n```\n\nLý do và đánh đổi:\n\n```\nLỢI\n· Gỡ lỗi và kiểm thử dễ hơn — chạm được vào ruột khi cần\n· Mở rộng thư viện bên thứ ba mà không phải fork\n· Không có rào cản nhân tạo khi tình huống thật khác dự đoán của tác giả\n\nGIÁ\n· Dựa vào kỷ luật — người dùng CÓ THỂ đụng vào và tự chuốc khổ\n· Không có bảo đảm nào của trình biên dịch\n```\n\nCâu tổng kết của cộng đồng Python: *\"chúng ta đều là người lớn\"*. Nó không có nghĩa \"đóng gói không quan trọng\" — nó có nghĩa **trách nhiệm nằm ở người dùng, còn bạn có trách nhiệm nói rõ đâu là API**.\n\nHệ quả thực dụng: nếu bạn dùng `_x` của thư viện khác, code của bạn **có thể vỡ ở bản cập nhật tiếp theo** mà tác giả không hề sai — họ đã cảnh báo bằng dấu gạch dưới.\n\n### Getter/setter — vì sao Python không cần\n\n```python\n# ❌ Thói quen từ Java — dài mà không được gì\nclass Nguoi:\n    def __init__(self, ten):\n        self._ten = ten\n    def get_ten(self):\n        return self._ten\n    def set_ten(self, gt):\n        self._ten = gt\n\nn.get_ten(); n.set_ten(\"An\")\n\n# ✓ Python — cứ để công khai\nclass Nguoi:\n    def __init__(self, ten):\n        self.ten = ten\n\nn.ten; n.ten = \"An\"\n```\n\nBản Java dài gấp ba mà **không bảo vệ gì**: setter nhận mọi giá trị, y hệt gán trực tiếp.\n\nLý do Python không cần getter/setter phòng xa: **`@property` cho phép thêm logic sau này mà không phá code người dùng.**\n\n```python\n# Sáu tháng sau, cần kiểm tra tên không rỗng\nclass Nguoi:\n    def __init__(self, ten):\n        self.ten = ten                 # đi qua setter mới\n\n    @property\n    def ten(self):\n        return self._ten\n\n    @ten.setter\n    def ten(self, gt):\n        if not gt or not gt.strip():\n            raise ValueError(\"tên không được rỗng\")\n        self._ten = gt.strip()\n```\n\nMọi code cũ viết `n.ten` và `n.ten = \"An\"` **vẫn chạy y nguyên**. Đây là lý do kỹ thuật cụ thể cho lời khuyên \"đừng viết getter/setter phòng xa\" trong Python — thứ **không** đúng với Java, nơi đổi từ trường sang phương thức là phá vỡ tương thích.\n\nKhi nào thì `property` đáng dùng:\n\n```\n✓ Cần kiểm tra dữ liệu khi gán\n✓ Giá trị tính từ thuộc tính khác\n✓ Cần chỉ đọc\n✓ Cần ghi log hoặc kích hoạt việc gì đó khi giá trị đổi\n✗ Chỉ để \"cho đúng nguyên tắc\" — không\n```\n\n### Rò rỉ đóng gói — lỗi tinh vi nhất\n\nĐây là phần quan trọng nhất và hay bị bỏ qua:\n\n```python\nclass GioHang:\n    def __init__(self):\n        self._san_pham = []\n\n    @property\n    def san_pham(self):\n        return self._san_pham          # ❌ RÒ RỈ tham chiếu\n\ngio = GioHang()\ngio.san_pham.append(\"hàng lậu\")        # ⭐ sửa được ruột, bỏ qua mọi kiểm tra\n```\n\nDấu gạch dưới không cứu được gì: bạn **trả ra chính object đó**, và người nhận sửa tại chỗ. Đây là \"biến là nhãn\" ở module 5 áp vào thiết kế class.\n\nBa cách chống, theo mức độ chặt:\n\n```python\n# ① Trả BẢN SAO — đơn giản, tốn bộ nhớ với dữ liệu lớn\n@property\ndef san_pham(self):\n    return list(self._san_pham)\n\n# ② Trả dạng KHÔNG SỬA ĐƯỢC — rẻ hơn\n@property\ndef san_pham(self):\n    return tuple(self._san_pham)\n\n# ③ Không lộ container, chỉ lộ THAO TÁC — chặt nhất ⭐\ndef them(self, sp):\n    if len(self._san_pham) >= 50:\n        raise ValueError(\"giỏ hàng đầy\")\n    self._san_pham.append(sp)\n\ndef __iter__(self):\n    return iter(self._san_pham)        # duyệt được mà không lộ list\n\ndef __len__(self):\n    return len(self._san_pham)\n```\n\nCách ③ đáng ưu tiên vì nó trả lời đúng câu hỏi thiết kế: **người dùng cần LÀM GÌ với dữ liệu này?** Nếu chỉ cần duyệt và đếm thì cho họ đúng hai thứ đó, không cần đưa cả `list`.\n\nCùng vấn đề ở chiều ngược lại — **nhận vào**:\n\n```python\nclass Bao:\n    def __init__(self, ds):\n        self._ds = ds                  # ❌ giữ tham chiếu của NGƯỜI GỌI\n\ngoc = [1, 2]\nb = Bao(goc)\ngoc.append(3)                          # sửa từ ngoài\nb._ds                                   # [1, 2, 3] ← ruột b bị đổi\n\n# ✓ Sao chép khi nhận vào\nclass Bao:\n    def __init__(self, ds):\n        self._ds = list(ds)            # ⭐ bản sao riêng\n```\n\n`list(ds)` còn có lợi ích thứ hai: nó **vật chất hoá generator**, nên class dùng được với mọi iterable — nối về bài generator ở module 5.\n\n### Ranh giới API và chi tiết cài đặt\n\nCách khai báo rõ ràng:\n\n```python\nclass XuLyVanBan:\n    \"\"\"Xử lý và chuẩn hoá văn bản.\n\n    API công khai: lam_sach(), tach_tu(), thong_ke\n    Mọi thứ bắt đầu bằng _ là nội bộ, có thể đổi bất cứ lúc nào.\n    \"\"\"\n\n    def lam_sach(self, s: str) -> str:          # công khai\n        return self._bo_dau_thua(self._chuan_hoa(s))\n\n    def tach_tu(self, s: str) -> list[str]:     # công khai\n        return self.lam_sach(s).split()\n\n    @property\n    def thong_ke(self) -> dict:                 # công khai, chỉ đọc\n        return dict(self._dem)                  # ⭐ bản sao, không rò rỉ\n\n    def _chuan_hoa(self, s): ...                # nội bộ\n    def _bo_dau_thua(self, s): ...              # nội bộ\n```\n\nỞ tầng module thì dùng `__all__`:\n\n```python\n__all__ = [\"XuLyVanBan\", \"lam_sach\"]     # chỉ hai tên này là công khai\n```\n\nCả hai đều là **giao ước, không phải ép buộc** — nhưng chúng nói rõ ý định, và đó là điều kiện để người dùng biết cái gì an toàn để dựa vào.\n\n### Đóng gói ở tầng cao hơn\n\nÝ tưởng này không dừng ở class:\n\n```\nHÀM     giấu thuật toán      người gọi chỉ cần biết vào/ra\nCLASS   giấu dữ liệu + cài đặt\nMODULE  giấu class phụ trợ   chỉ export cái cần\nGÓI     giấu module nội bộ   __init__.py quyết định API\nDỊCH VỤ giấu cả database     chỉ lộ API mạng\n```\n\nNguyên tắc chung: **mỗi tầng chỉ để lộ thứ tầng trên thật sự cần.** Đây là ý sẽ quay lại ở SOLID và ở System Design — cùng một tư duy, khác quy mô.\n\n### Khi nào đóng gói quá mức\n\nCân bằng lại, vì lạm dụng cũng có hại:\n\n```python\n# ❌ Đóng gói vô nghĩa — chỉ dài hơn\nclass Diem:\n    def __init__(self, x, y):\n        self._x, self._y = x, y\n    @property\n    def x(self): return self._x\n    @x.setter\n    def x(self, v): self._x = v          # không kiểm tra gì cả\n    # ...lặp lại cho y\n\n# ✓ Không có bất biến nào cần giữ ⇒ để công khai\nfrom dataclasses import dataclass\n\n@dataclass\nclass Diem:\n    x: float\n    y: float\n```\n\nDấu hiệu đóng gói thừa:\n\n```\n· Getter/setter không có logic nào\n· Mọi thuộc tính đều _ mà không có bất biến nào\n· Phải viết thêm phương thức chỉ để lấy dữ liệu người dùng rõ ràng cần\n· Class thành khó dùng hơn mà không an toàn hơn\n```\n\nCâu hỏi phân định: **có quy tắc nào về trạng thái mà tôi cần bảo vệ không?** Không có → để công khai. Có → đóng gói và bảo vệ nó cho chặt.",

vi_du: "### Bất biến được bảo vệ — trước và sau\n\n```python\n# ❌ Không đóng gói: bất biến \"số dư ≥ 0\" không ai giữ\nclass TaiKhoan:\n    def __init__(self, so_du=0):\n        self.so_du = so_du\n\ntk = TaiKhoan(100)\ntk.so_du = -500                # ✓ không lỗi — bất biến VỠ\ntk.so_du -= 1_000_000          # ✓ cũng không lỗi\n```\n\n```python\n# ✓ Có đóng gói\nclass TaiKhoan:\n    def __init__(self, chu: str, so_du: float = 0):\n        if so_du < 0:\n            raise ValueError(f\"số dư ban đầu không được âm: {so_du}\")\n        self.chu = chu\n        self._so_du = float(so_du)\n\n    @property\n    def so_du(self) -> float:\n        return self._so_du              # chỉ đọc\n\n    def nap(self, tien: float) -> float:\n        if tien <= 0:\n            raise ValueError(f\"số tiền nạp phải dương: {tien}\")\n        self._so_du += tien\n        return self._so_du\n\n    def rut(self, tien: float) -> float:\n        if tien <= 0:\n            raise ValueError(f\"số tiền rút phải dương: {tien}\")\n        if tien > self._so_du:\n            raise ValueError(f\"không đủ: cần {tien:,.0f}, có {self._so_du:,.0f}\")\n        self._so_du -= tien\n        return self._so_du\n\ntk = TaiKhoan(\"Minh\", 100_000)\ntk.so_du = -500        # AttributeError ✓ không có setter\ntk.rut(200_000)        # ValueError: không đủ ✓\ntk._so_du = -500       # vẫn làm được — nhưng bạn đã BIẾT mình đang phá\n```\n\nDòng cuối là điểm mấu chốt của triết lý Python: nó **không ngăn** bạn, nhưng bạn phải cố ý vượt qua một dấu hiệu rõ ràng. Với một lỗi vô tình thì `_so_du` là đủ để chặn.\n\n### Rò rỉ tham chiếu — tái hiện và ba cách sửa\n\n```python\n# ❌ Rò rỉ\nclass GioHang:\n    def __init__(self):\n        self._sp = []\n\n    def them(self, ten, gia):\n        if gia < 0:\n            raise ValueError(\"giá không được âm\")\n        self._sp.append({\"ten\": ten, \"gia\": gia})\n\n    @property\n    def san_pham(self):\n        return self._sp                 # ❌\n\ng = GioHang()\ng.them(\"sách\", 100)\ng.san_pham.append({\"ten\": \"lậu\", \"gia\": -999})    # ⭐ bỏ qua kiểm tra\nlen(g._sp)                                        # 2 — ruột đã bị sửa\n```\n\n```python\n# ✓ ① Bản sao\n@property\ndef san_pham(self):\n    return [dict(s) for s in self._sp]      # sao chép SÂU một tầng\n\n# ✓ ② Không sửa được\n@property\ndef san_pham(self):\n    return tuple(self._sp)\n\n# ✓ ③ Chỉ lộ thao tác cần thiết — ⭐ tốt nhất\nclass GioHang:\n    TOI_DA = 50\n\n    def __init__(self):\n        self._sp = []\n\n    def them(self, ten, gia):\n        if gia < 0:\n            raise ValueError(f\"giá không được âm: {gia}\")\n        if len(self._sp) >= self.TOI_DA:\n            raise ValueError(f\"giỏ hàng tối đa {self.TOI_DA} món\")\n        self._sp.append({\"ten\": ten, \"gia\": gia})\n\n    def bo(self, ten):\n        truoc = len(self._sp)\n        self._sp = [s for s in self._sp if s[\"ten\"] != ten]\n        return truoc - len(self._sp)\n\n    def __iter__(self):\n        return iter(tuple(self._sp))        # duyệt được, không lộ list gốc\n\n    def __len__(self):\n        return len(self._sp)\n\n    def __contains__(self, ten):\n        return any(s[\"ten\"] == ten for s in self._sp)\n\n    @property\n    def tong(self):\n        return sum(s[\"gia\"] for s in self._sp)\n\ng = GioHang()\ng.them(\"sách\", 100_000)\nlen(g)                     # 1\n\"sách\" in g                # True\nfor s in g: print(s)       # duyệt được\ng.tong                     # 100000\n```\n\nBản ③ cho người dùng **đúng những gì họ cần** — đếm, kiểm tra tồn tại, duyệt, tính tổng — mà không đưa `list` ra. Chú ý ba magic method `__iter__`, `__len__`, `__contains__` làm class hoà vào cú pháp Python; bài magic method sẽ nói kỹ.\n\n### Nhận dữ liệu vào — cũng phải sao chép\n\n```python\nclass BoLoc:\n    def __init__(self, tu_khoa):\n        self._tu_khoa = set(tu_khoa)        # ⭐ bản sao, và đổi sang set\n\n    def khop(self, s: str) -> bool:\n        return any(t in s.lower() for t in self._tu_khoa)\n\n    def them_tu(self, t: str) -> None:\n        self._tu_khoa.add(t.lower())\n\n# Người gọi sửa list của họ — không ảnh hưởng bộ lọc\ndanh_sach = [\"spam\", \"quảng cáo\"]\nbl = BoLoc(danh_sach)\ndanh_sach.append(\"bất kỳ\")\nbl.khop(\"đây là bất kỳ\")     # False ✓ bộ lọc không bị đổi\n```\n\nDòng `set(tu_khoa)` làm ba việc cùng lúc: sao chép, đổi sang cấu trúc tra O(1) (bài list/dict/set), và chấp nhận mọi iterable kể cả generator.\n\n### `_x` và `__x` khi kế thừa\n\n```python\nclass Cha:\n    def __init__(self):\n        self._chung = \"cha\"        # một gạch — lớp con THẤY và đè được\n        self.__rieng = \"cha\"       # hai gạch — thành _Cha__rieng\n\n    def doc(self):\n        return self._chung, self.__rieng\n\nclass Con(Cha):\n    def __init__(self):\n        super().__init__()\n        self._chung = \"con\"        # ⚠️ ĐÈ mất của cha\n        self.__rieng = \"con\"       # → _Con__rieng, KHÔNG đè\n\nc = Con()\nprint(c.doc())        # ('con', 'cha')\n#                        └─ đã bị đè   └─ vẫn nguyên ✓\nprint(vars(c))\n# {'_chung': 'con', '_Cha__rieng': 'cha', '_Con__rieng': 'con'}\n```\n\nĐây là minh hoạ chính xác mục đích của hai gạch dưới: nó **không chặn ai**, nó chỉ đảm bảo lớp cha và lớp con có kho riêng, không vô tình giẫm lên nhau.\n\nKhi nào cần: viết một class **nền** sẽ được nhiều người kế thừa, và bạn có dữ liệu nội bộ không muốn lớp con đụng vào. Ngoài trường hợp đó, `_x` đơn giản hơn và đủ dùng.\n\n### Đổi cài đặt mà không phá code người dùng\n\n```python\n# Phiên bản 1\nclass ThongKe:\n    def __init__(self):\n        self._so = []\n\n    def them(self, x):\n        self._so.append(x)\n\n    @property\n    def trung_binh(self):\n        return sum(self._so) / len(self._so) if self._so else 0\n```\n\n```python\n# Phiên bản 2 — không lưu cả dãy nữa, chỉ giữ tổng và số lượng\nclass ThongKe:\n    def __init__(self):\n        self._tong = 0.0            # ⭐ cài đặt đổi HOÀN TOÀN\n        self._n = 0\n\n    def them(self, x):\n        self._tong += x\n        self._n += 1\n\n    @property\n    def trung_binh(self):\n        return self._tong / self._n if self._n else 0\n```\n\n```python\n# Code người dùng — KHÔNG đổi một dòng nào\nt = ThongKe()\nfor x in du_lieu_10_trieu_so:\n    t.them(x)\nprint(t.trung_binh)\n```\n\nBộ nhớ giảm từ O(n) xuống O(1), và **không ai phải sửa gì**. Đây là lợi ích cụ thể nhất của đóng gói — nếu `_so` là công khai và có người viết `t._so[5]`, phiên bản 2 sẽ phá code của họ.\n\n### `property` với logic phụ\n\n```python\nimport logging\n\nclass CauHinh:\n    def __init__(self):\n        self._debug = False\n        self._log = logging.getLogger(__name__)\n\n    @property\n    def debug(self) -> bool:\n        return self._debug\n\n    @debug.setter\n    def debug(self, gt: bool) -> None:\n        if not isinstance(gt, bool):\n            raise TypeError(f\"debug phải là bool, nhận {type(gt).__name__}\")\n        cu = self._debug\n        self._debug = gt\n        if cu != gt:\n            self._log.info(\"debug: %s → %s\", cu, gt)      # ⭐ tác dụng phụ\n            logging.getLogger().setLevel(\n                logging.DEBUG if gt else logging.INFO)\n\ncf = CauHinh()\ncf.debug = True        # tự đổi mức log toàn hệ thống\ncf.debug = \"yes\"       # TypeError ✓\n```\n\nĐây là chỗ `property` thật sự đáng: một phép gán đơn giản kích hoạt cả kiểm tra lẫn hành động phụ, mà người dùng vẫn viết `cf.debug = True` như một thuộc tính thường.\n\nCảnh báo cân bằng: **tác dụng phụ trong setter dễ gây bất ngờ.** Người đọc `cf.debug = True` không nghĩ nó đổi cấu hình log toàn cục. Chỉ làm vậy khi hành động phụ là hệ quả **hiển nhiên** của việc gán; nếu không thì dùng một phương thức có tên rõ ràng như `bat_debug()`.\n\n### Kiểm tra đóng gói của một class\n\n```python\ndef soi_dong_goi(cls):\n    \"\"\"Liệt kê API công khai và phần nội bộ của một class.\"\"\"\n    cong_khai, noi_bo, dac_biet = [], [], []\n    for ten in dir(cls):\n        if ten.startswith(\"__\") and ten.endswith(\"__\"):\n            dac_biet.append(ten)\n        elif ten.startswith(\"_\"):\n            noi_bo.append(ten)\n        else:\n            cong_khai.append(ten)\n\n    print(f\"{cls.__name__}\")\n    print(f\"  công khai ({len(cong_khai)}): {cong_khai}\")\n    print(f\"  nội bộ    ({len(noi_bo)}): {noi_bo}\")\n\nsoi_dong_goi(GioHang)\n```\n\nHàm này hữu ích khi review thiết kế: **danh sách công khai dài bất thường là dấu hiệu class đang lộ ra quá nhiều** — và mỗi cái lộ ra là một cam kết bạn phải giữ.\n\nMột phép thử khác, đáng chạy trước khi phát hành:\n\n```python\n# Thử phá bất biến bằng API công khai — nếu làm được thì đóng gói chưa đủ\ntk = TaiKhoan(\"Minh\", 100)\ntry:\n    tk.so_du = -1\n    print(\"✗ bất biến vỡ được qua API công khai\")\nexcept AttributeError:\n    print(\"✓ so_du chỉ đọc\")\n\ng = GioHang()\ng.them(\"a\", 100)\nds = list(g)\nds.append(\"lậu\")\nprint(\"✓ sửa bản sao không ảnh hưởng gốc\" if len(g) == 1 else \"✗ rò rỉ\")\n```",

so_sanh: "### Ba mức truy cập\n\n| | `x` | `_x` | `__x` |\n|---|---|---|---|\n| Truy cập từ ngoài | ✓ | **✓ nhưng đừng** | qua `_Lop__x` |\n| Ép buộc bởi ngôn ngữ | ✗ | ✗ | **đổi tên, không khoá** |\n| Lớp con thấy | ✓ | ✓ | ✗ (tên khác) |\n| `import *` lấy | ✓ | ✗ | ✗ |\n| Ý nghĩa | **API, cam kết giữ** | nội bộ, có quyền đổi | tránh xung đột kế thừa |\n| Tần suất nên dùng | nhiều | **nhiều** | ít |\n\nHàng cuối là lời khuyên thực dụng: `_x` đủ cho gần như mọi trường hợp. `__x` chỉ đáng khi viết class nền cho người khác kế thừa.\n\n### Python và Java về đóng gói\n\n| | Python | Java |\n|---|---|---|\n| Cơ chế | **quy ước** | trình biên dịch ép |\n| Getter/setter phòng xa | **không cần** | thường phải có |\n| Đổi trường → phương thức | **✓ không phá gì** | ✗ phá tương thích |\n| Gỡ lỗi, kiểm thử | dễ chạm vào ruột | khó hơn |\n| Bảo đảm | không có | có |\n\nHàng thứ ba là lý do kỹ thuật cho khác biệt văn hoá: ở Java, đổi `obj.x` thành `obj.getX()` là phá vỡ mọi code đang dùng, nên người ta viết getter phòng xa. Ở Python, `@property` làm việc đó **trong suốt**, nên viết phòng xa là công vô ích.\n\n### Ba cách chống rò rỉ tham chiếu\n\n| | Trả bản sao | Trả `tuple` | Chỉ lộ thao tác |\n|---|---|---|---|\n| Người ngoài sửa được gốc | ✗ | ✗ | ✗ |\n| Chi phí | **O(n) mỗi lần đọc** | O(n) một lần | **không** |\n| Người dùng sửa bản mình nhận | ✓ | ✗ | — |\n| Kiểm soát được thao tác | ✗ | ✗ | **✓** |\n| Nên dùng | dữ liệu nhỏ | trả danh sách chỉ đọc | **mặc định** |\n\nCột cuối đáng ưu tiên vì nó trả lời đúng câu hỏi thiết kế: người dùng **cần làm gì**, chứ không phải \"làm sao đưa dữ liệu ra an toàn\".\n\n### Khi nào dùng `@property`\n\n| Tình huống | Dùng `property`? |\n|---|---|\n| Cần kiểm tra khi gán | **✓** |\n| Giá trị tính từ thuộc tính khác | **✓** |\n| Cần chỉ đọc | **✓** |\n| Cần ghi log / kích hoạt việc khi đổi | ✓ (cẩn thận bất ngờ) |\n| Chỉ trả về `self._x` không làm gì thêm | **✗ để công khai** |\n| Tính toán tốn kém | ✗ dùng phương thức có tên rõ |\n\nHàng áp chót là lỗi phổ biến nhất: một `property` chỉ `return self._x` **không bảo vệ gì** — nó chỉ dài hơn ba dòng so với để thuộc tính công khai.\n\n### Đóng gói đủ và đóng gói thừa\n\n| | Đủ | Thừa |\n|---|---|---|\n| Có bất biến cần giữ | **✓** | ✗ không có gì để giữ |\n| Getter/setter | chỉ khi có logic | mọi thuộc tính |\n| API công khai | nhỏ, rõ | quá nhỏ, khó dùng |\n| Người dùng | dùng được tự nhiên | phải viết vòng vo |\n| Kết quả | dễ đổi ruột | chỉ dài hơn |\n\nCâu hỏi phân định: **có quy tắc nào về trạng thái mà tôi cần bảo vệ không?** Không → công khai. Có → đóng gói chặt và bảo vệ mọi đường vào.\n\n### Đóng gói qua các tầng\n\n| Tầng | Giấu gì | Lộ gì |\n|---|---|---|\n| Hàm | thuật toán | tham số và giá trị trả về |\n| Class | dữ liệu, cài đặt | phương thức công khai |\n| Module | class phụ trợ | `__all__` |\n| Gói | module nội bộ | `__init__.py` |\n| Dịch vụ | database, kiến trúc | API mạng |\n\nCùng một tư duy ở năm quy mô: **mỗi tầng chỉ lộ ra thứ tầng trên thật sự cần**. Đây là ý sẽ quay lại ở SOLID và ở System Design.",

loi_thuong_gap: [
  "**Viết getter/setter cho mọi thuộc tính theo thói quen Java.** Dài gấp ba mà không bảo vệ gì. Để công khai, đổi sang `property` khi cần.",
  "**Trả thẳng `list` hoặc `dict` nội bộ qua `property`.** Người nhận sửa tại chỗ là ruột object bị đổi, bỏ qua mọi kiểm tra.",
  "**Nhận `list` từ người gọi rồi giữ nguyên tham chiếu.** Họ sửa list của họ là ruột object bạn đổi theo. Sao chép khi nhận: `list(ds)`.",
  "**Tưởng `__x` là private thật.** Nó chỉ đổi tên; mục đích là tránh xung đột khi kế thừa.",
  "**Dùng `__x` bừa bãi.** Gỡ lỗi khó hơn, kế thừa khó hơn, không đổi lại lợi ích an toàn nào. `_x` đủ cho 95% trường hợp.",
  "**Viết `property` chỉ để `return self._x`.** Không bảo vệ gì — để thuộc tính công khai.",
  "**Cho `so_du` (hoặc thuộc tính có bất biến) một setter.** Bất biến vỡ và không tìm ra chỗ nào làm việc đó.",
  "**Để một cửa sau bỏ qua kiểm tra.** Bất biến chỉ giữ được khi **mọi** đường vào đi qua chỗ kiểm tra.",
  "**Đặt tác dụng phụ nặng trong setter.** `cf.debug = True` đổi cấu hình toàn cục là bất ngờ cho người đọc.",
  "**Dùng `_x` của thư viện bên ngoài.** Tác giả có quyền đổi ở bản sau, và code bạn vỡ mà họ không sai.",
  "**Đóng gói khi không có bất biến nào.** Class chỉ chứa dữ liệu thì dùng `dataclass` với thuộc tính công khai.",
  "**API công khai quá rộng.** Mỗi thứ lộ ra là một cam kết phải giữ mãi. Lộ ít, thêm sau dễ hơn bớt đi.",
  "**Không ghi rõ đâu là API.** Người dùng không biết cái gì an toàn để dựa vào. Dùng `_`, `__all__`, và docstring."
],

tu_kiem_tra: [
  "Đóng gói bảo vệ cái gì? Vì sao \"private hết rồi viết getter/setter\" không phải đóng gói?",
  "Bất biến là gì? Nêu một ví dụ và giải thích vì sao mọi đường vào phải đi qua chỗ kiểm tra.",
  "`_x` và `__x` khác nhau thế nào? Mục đích thật của hai gạch dưới, và khi nào cần nó?",
  "Vì sao Python chọn quy ước thay vì ép buộc? Nêu một lợi ích và một cái giá.",
  "Vì sao Python không cần getter/setter phòng xa mà Java thì cần?",
  "\"Rò rỉ tham chiếu\" là gì? Cho một ví dụ và ba cách chống.",
  "Vì sao `self._ds = ds` trong `__init__` là rủi ro? Sửa thế nào và lợi ích phụ là gì?",
  "Ba cách chống rò rỉ khác nhau về chi phí và mức kiểm soát ra sao? Cái nào nên mặc định?",
  "Nêu bốn trường hợp `@property` đáng dùng và hai trường hợp không.",
  "Vì sao tác dụng phụ trong setter cần cẩn trọng?",
  "Cho ví dụ về việc đổi hoàn toàn cài đặt bên trong mà không phá code người dùng.",
  "Dùng `_x` của thư viện bên thứ ba có rủi ro gì, và ai chịu trách nhiệm khi code vỡ?",
  "Nêu dấu hiệu của đóng gói thừa. Câu hỏi nào giúp phân định đủ hay thừa?",
  "Ý tưởng đóng gói xuất hiện ở những tầng nào ngoài class?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Private Variables* trong tutorial — giải thích chính thức về quy ước gạch dưới và cơ chế đổi tên.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về quản lý thuộc tính và về `property`, gồm cả các bẫy rò rỉ tham chiếu.",
  "Sách **Effective Python** — Brett Slatkin. Các mục về việc dùng thuộc tính công khai thay vì getter/setter, và khi nào dùng `property`.",
  "Sách **Clean Code** — Robert C. Martin. Chương về object và cấu trúc dữ liệu bàn đúng ranh giới \"lộ dữ liệu hay lộ hành vi\".",
  "Sách **Practical Object-Oriented Design** — Sandi Metz. Viết cho Ruby nhưng phần về giao diện và về phụ thuộc áp dụng nguyên vẹn."
],

lien_he: [
  "**Class, object, thuộc tính, phương thức** ngay trước: `@property`, `_x`, `__x` đã giới thiệu ở đó; bài này nói vì sao và khi nào dùng.",
  "**Kế thừa và MRO** ngay sau: `__x` tồn tại chính vì kế thừa, và lớp con đè `_x` là chuyện thường gặp.",
  "**Trừu tượng: abstract class, interface, Protocol**: đóng gói quyết định cái gì lộ ra, trừu tượng quyết định cái gì bắt buộc phải có.",
  "**SOLID — Single Responsibility**: API công khai rộng thường là dấu hiệu class có quá nhiều trách nhiệm.",
  "**SOLID — Dependency Inversion**: phụ thuộc vào giao diện công khai chứ không vào chi tiết cài đặt là cùng một ý tưởng.",
  "**Cú pháp, kiểu dữ liệu** ở module 5: rò rỉ tham chiếu là \"biến là nhãn\" áp vào thiết kế class.",
  "**Module, package, import** ở module 5: `__all__` và quy ước `_` là đóng gói ở tầng module.",
  "**Xử lý ngoại lệ** ở module 5: `raise ValueError` khi vi phạm bất biến là mẫu bảo vệ chuẩn.",
  "**Clean Code** ở module 8 mảng 01: \"lộ hành vi, đừng lộ dữ liệu\" là một trong những nguyên tắc trung tâm.",
  "**System Design** ở mảng 06: ranh giới dịch vụ là đóng gói ở quy mô kiến trúc — mỗi dịch vụ giấu database của mình."
]

};
