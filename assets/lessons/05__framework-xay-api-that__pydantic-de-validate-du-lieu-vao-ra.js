window.LESSONS["05:framework-xay-api-that:pydantic-de-validate-du-lieu-vao-ra"] = {

tom_tat: [
  "Pydantic biến dữ liệu chưa đáng tin thành object đã qua kiểm tra theo schema; parse thành công không đồng nghĩa dữ liệu đúng mọi quy tắc nghiệp vụ.",
  "Model đầu vào, model nội bộ và model response nên tách khi quyền ghi/đọc khác nhau, nhờ đó client không thể mass assignment các trường như `status` hay `total`.",
  "Field constraint xử lý quy tắc cục bộ; model validator xử lý quan hệ giữa nhiều trường; kiểm tra cần database thuộc service chứ không nằm trong schema đồng bộ.",
  "Response model là hàng rào chống rò dữ liệu: nó tài liệu hóa contract và lọc những field nội bộ trước khi JSON rời server.",
  "Validation có chi phí và giới hạn tài nguyên; API thật vẫn cần giới hạn kích thước body, độ dài danh sách và độ sâu dữ liệu trước các payload ác ý."
],

can_biet_truoc: [
  "Python type hint, class, `datetime`, `Decimal` và sự khác nhau giữa mutable với immutable.",
  "JSON chỉ có số, chuỗi, boolean, null, object và array; không có kiểu UUID hay `Decimal` nguyên bản.",
  "HTTP request body, response body và status code 422 trong FastAPI.",
  "Khái niệm validation, serialization và business rule."
],

dinh_nghia: "**Pydantic model** là một schema có thể nhận dữ liệu Python chưa được tin cậy, kiểm tra kiểu và constraint, chuyển đổi có kiểm soát rồi tạo object đã biết hình dạng. Khi FastAPI dùng model cho request body, dữ liệu sai tạo lỗi validation trước endpoint. Khi dùng model cho response, dữ liệu trả ra được kiểm tra và tuần tự hóa theo contract công khai.\n\nValidation trả lời “giá trị này có đúng hình dạng và quy tắc cục bộ của contract không?”. Nó không tự trả lời “SKU còn tồn tại không?”, “khách này có quyền mua không?” hay “tồn kho còn đủ tại thời điểm commit không?”. Ba câu sau phụ thuộc trạng thái hệ thống và thuộc application/domain service.",

vi_sao_can: "JSON từ client là dữ liệu đối nghịch cho tới khi được chứng minh ngược lại. Client có thể gửi thiếu field, số âm, chuỗi cực dài, `null` bất ngờ, field thừa hoặc cố gán `status=\"paid\"`. Nếu endpoint thao tác trực tiếp dictionary, lỗi xuất hiện sâu trong code dưới dạng `KeyError`, phép tính sai hoặc nguy hiểm hơn là ghi dữ liệu mà client không được quyền quyết định.\n\nỞ chiều ra, ORM object thường chứa nhiều hơn contract public: giá vốn, ghi chú chống gian lận, token nội bộ hoặc hash mật khẩu. Trả thẳng `__dict__` khiến một field mới thêm vào database vô tình trở thành API. Response model đảo mặc định đó: chỉ field đã công khai mới đi qua.\n\nAPI đơn hàng cần giữ tiền chính xác, số lượng dương và danh sách không rỗng. Nhưng nó cũng phải chống payload khổng lồ làm CPU/memory cạn, phân biệt lỗi client với lỗi server, và không hứa rằng validation thay thế transaction tồn kho. Pydantic hữu ích vì gom contract gần type hint và OpenAPI; dùng sai sẽ tạo cảm giác an toàn giả.",

co_che: "### Biên tin cậy: từ JSON tới object\n\nServer JSON parser trước hết biến byte thành `dict`, `list`, `str`, `int`, `float`, `bool` và `None`. Pydantic sau đó đọc từng field, áp dụng alias, kiểm tra hoặc chuyển kiểu, chạy validator, rồi tạo model. Chỉ sau chuỗi này endpoint mới nhận `OrderCreate`. Nếu bất kỳ bước nào thất bại, FastAPI tạo response 422 chứa vị trí lỗi.\n\nRanh giới này quan trọng vì code phía sau có thể dựa vào invariant như `quantity >= 1`. Nhưng invariant chỉ mạnh bằng schema: nếu cho phép field thừa hoặc coercion quá rộng, dữ liệu “hợp lệ” vẫn có thể khác ý client và server.\n\n### Coercion tiện lợi nhưng có thể mơ hồ\n\nPydantic có thể chuyển một số biểu diễn tương thích thành kiểu đích. Điều đó tốt với query string vốn luôn là chuỗi, nhưng đôi khi che lỗi producer. Một hệ thống gửi `\"12\"` thay số 12 có thể vẫn pass và bug hợp đồng tồn tại nhiều tháng. Với biên nhạy cảm, strict type buộc producer sửa đúng.\n\nKhông có một chính sách strict tối ưu cho mọi field. ID và tiền thường nên chặt; form HTML có thể cần chuyển đổi. Quyết định phải xuất phát từ contract, không từ việc “Pydantic parse được”.\n\n### Constraint cục bộ đặt ngay tại field\n\nSố lượng ít nhất 1, mã SKU có độ dài hữu hạn, danh sách tối đa 50 dòng là quy tắc một field. Khai báo chúng trong schema giúp OpenAPI mô tả được và lỗi chỉ đúng vị trí. Giới hạn trên không chỉ là nghiệp vụ; nó chặn client buộc server validate hàng triệu phần tử.\n\n`Decimal` phù hợp hơn `float` cho tiền vì số thập phân nhị phân không biểu diễn chính xác nhiều giá trị như 0,1. Dù vậy client không được gửi `unit_price` để server tin theo; server phải tra giá hiện hành theo SKU. Schema request chỉ nên nhận thứ client được quyền chọn.\n\n### Validator nhiều field và thứ tự dữ liệu\n\nQuy tắc “đơn giao hàng phải có địa chỉ” liên quan `delivery_method` và `shipping_address`, nên thuộc model validator. Validator field riêng dễ phụ thuộc thứ tự khai báo và khó diễn đạt lỗi toàn model. Validator phải thuần, xác định và nhanh: cùng đầu vào cho cùng kết quả, không gọi network, không sửa database.\n\nNếu validator truy vấn “SKU có tồn tại không”, validation trở thành I/O ẩn, khó timeout và có race: SKU có thể bị xóa ngay sau khi kiểm tra. Service nên tải sản phẩm, kiểm policy rồi transaction hóa thay đổi.\n\n### Ba model cho ba quyền khác nhau\n\n`OrderCreate` mô tả client được gửi. `OrderRecord` hoặc ORM entity chứa state lưu trữ. `OrderPublic` mô tả client được thấy. Dùng một model duy nhất thường dẫn tới optional field tràn lan và mass assignment.\n\nVí dụ client không được gửi `id`, `customer_id`, `status`, `created_at` hay `total`. Chúng đến từ server, identity đã xác thực và phép tính giá. Nếu model input nhận các field ấy dù “để tiện”, attacker sẽ thử gán chúng.\n\n### Field thừa: bỏ qua hay cấm\n\nMặc định bỏ qua field lạ có lợi cho tương thích tiến hóa: client mới gửi thêm field tới server cũ không nhất thiết hỏng. Nhưng với command nhạy cảm, field thừa thường là typo hoặc nỗ lực mass assignment. Cấu hình `extra=\"forbid\"` làm lỗi xuất hiện sớm.\n\nĐánh đổi nằm ở rollout. Nếu nhiều phiên bản client chạy song song, cấm tuyệt đối có thể làm deployment khó hơn. Khi ấy version endpoint hoặc chiến lược additive contract rõ ràng tốt hơn việc âm thầm nhận mọi thứ.\n\n### Optional không giống nullable và không giống thiếu\n\nMột field có type `str | None` cho phép giá trị null, nhưng việc có bắt buộc xuất hiện hay không còn phụ thuộc default. Trong PATCH, “không gửi” thường nghĩa là giữ nguyên, còn `null` có thể nghĩa là xóa. Nếu gộp hai trạng thái, API không thể diễn đạt cập nhật chính xác.\n\nPydantic lưu tập field thực sự được client cung cấp. Khi tạo dictionary cập nhật, chỉ lấy các field đã được đặt; sau đó service vẫn phải áp policy field nào được phép đổi.\n\n### Response model là allowlist\n\nKhi endpoint trả ORM object hoặc dictionary nhiều field, response model chỉ serialize contract đã khai. Đây là allowlist, an toàn hơn blacklist vì field nội bộ mới không tự lộ. Nếu code trả thiếu hoặc sai kiểu so với response model, đó là lỗi server, không phải lỗi request; cần log nội bộ nhưng không gửi chi tiết nhạy cảm cho client.\n\n```python\nclass UserInternal(BaseModel):\n    id: int\n    email: str\n    password_hash: str\n\nclass UserPublic(BaseModel):\n    id: int\n    email: str\n\n# response_model=UserPublic loại password_hash khỏi JSON\n```\n\n### Serialization: JSON không giữ mọi kiểu Python\n\n`datetime`, UUID và `Decimal` phải đổi thành biểu diễn JSON. Contract cần quyết định timezone và dạng chuỗi, không dựa vào `str(value)` tùy tiện. Thời điểm nên có timezone; tiền thường trả chuỗi thập phân hoặc đơn vị nhỏ nhất dạng integer tùy quy ước toàn hệ thống.\n\nCustom serializer quá nhiều sẽ làm model khó đoán. Nếu cùng một khái niệm tiền xuất hiện nhiều nơi, nên định nghĩa một value object hoặc quy ước thống nhất thay vì mỗi endpoint format khác nhau.\n\n### Validation không thay giới hạn ở tầng vận chuyển\n\nGiới hạn `max_length=50` chỉ phát huy sau khi body đã được nhận và JSON đã parse. Payload hàng trăm megabyte vẫn có thể tiêu tốn băng thông và memory trước đó. Reverse proxy hoặc ASGI middleware cần giới hạn Content-Length/body, đồng thời đặt timeout đọc request. Không tin hoàn toàn `Content-Length`, vì truyền chunked có thể không khai báo trước.\n\nĐộ sâu JSON cũng là bề mặt tấn công. Schema phẳng và giới hạn danh sách giảm rủi ro; endpoint upload file nên stream thay vì nhét base64 vào JSON.\n\n### Schema evolution và khả năng tương thích\n\nThêm field response thường tương thích với client biết bỏ qua field lạ; xóa hoặc đổi kiểu thì không. Thêm field request bắt buộc phá client cũ. Vì vậy field mới thường bắt đầu optional/default, server hỗ trợ giai đoạn chuyển tiếp, rồi mới siết sau khi quan sát usage.\n\nSchema sinh tự động không thay consumer test. OpenAPI cho biết hình dạng hiện tại, còn test contract phát hiện một thay đổi vô ý trước deploy.\n\n### Khi nào Pydantic không phải nơi đặt quy tắc\n\nKhông đặt authorization, truy vấn database, gửi email, trừ tồn kho hoặc khóa phân tán trong validator. Cũng không cần dựng model cho object nội bộ rất nhỏ và đã nằm sau biên tin cậy nếu dataclass/type hint đủ rõ. Validation lặp ở mọi tầng làm tốn CPU và khiến nguồn sự thật mơ hồ; hãy validate mạnh tại boundary, giữ invariant trong domain, dùng constraint database cho tính đúng đắn cuối cùng.",

vi_du: "### Contract tạo đơn không cho client tự đặt giá\n\nVí dụ dùng cú pháp Pydantic hiện hành của FastAPI. Lưu thành `main.py` rồi chạy bằng Uvicorn. Giá nằm phía server; `customer_id` lấy từ identity giả lập để nhấn mạnh chống mass assignment.\n\n```python\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nfrom typing import Annotated, Literal\n\nfrom fastapi import FastAPI\nfrom pydantic import BaseModel, ConfigDict, Field, model_validator\n\nSku = Annotated[str, Field(min_length=3, max_length=32, pattern=r\"^[A-Z0-9-]+$\")]\nQuantity = Annotated[int, Field(strict=True, ge=1, le=20)]\n\nclass OrderLineCreate(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    sku: Sku\n    quantity: Quantity\n\nclass OrderCreate(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    delivery_method: Literal[\"pickup\", \"shipping\"]\n    shipping_address: str | None = Field(default=None, max_length=300)\n    lines: list[OrderLineCreate] = Field(min_length=1, max_length=50)\n\n    @model_validator(mode=\"after\")\n    def address_matches_delivery(self):\n        if self.delivery_method == \"shipping\" and not self.shipping_address:\n            raise ValueError(\"đơn giao hàng cần địa chỉ\")\n        if self.delivery_method == \"pickup\" and self.shipping_address is not None:\n            raise ValueError(\"đơn nhận tại quầy không dùng địa chỉ\")\n        return self\n\nclass OrderPublic(BaseModel):\n    id: int\n    status: Literal[\"pending\", \"confirmed\"]\n    total: Decimal\n    created_at: datetime\n\napp = FastAPI()\nPRICES = {\"BOOK-01\": Decimal(\"125000\"), \"PEN-02\": Decimal(\"15000\")}\n\n@app.post(\"/orders\", response_model=OrderPublic, status_code=201)\nasync def create_order(command: OrderCreate):\n    # Thực tế customer_id đến từ dependency xác thực và dữ liệu sẽ vào transaction.\n    unknown = [line.sku for line in command.lines if line.sku not in PRICES]\n    if unknown:\n        from fastapi import HTTPException\n        raise HTTPException(409, detail={\"unknown_skus\": unknown})\n    total = sum(PRICES[line.sku] * line.quantity for line in command.lines)\n    return {\n        \"id\": 101,\n        \"status\": \"pending\",\n        \"total\": total,\n        \"created_at\": datetime.now(timezone.utc),\n        \"internal_risk_score\": 0.73,  # response model không công khai field này\n    }\n```\n\nPayload hợp lệ:\n\n```json\n{\n  \"delivery_method\": \"shipping\",\n  \"shipping_address\": \"12 Nguyen Trai\",\n  \"lines\": [{\"sku\": \"BOOK-01\", \"quantity\": 2}]\n}\n```\n\nThử đổi `quantity` thành chuỗi `\"2\"`: strict integer khiến request bị từ chối, qua đó phát hiện producer sai contract. Thử thêm `status: \"confirmed\"`: `extra=\"forbid\"` chặn ý định gán state. Thử bỏ địa chỉ trong đơn shipping: model validator báo lỗi quan hệ. Thử SKU đúng hình dạng nhưng không có trong bảng giá: schema pass, service trả 409; đây chính là ranh giới giữa structural validation và stateful rule.\n\n### PATCH phải giữ được ba trạng thái\n\nĐoạn rút gọn sau phân biệt field vắng mặt và null:\n\n```python\nclass OrderPatch(BaseModel):\n    shipping_address: str | None = Field(default=None, max_length=300)\n\npatch = OrderPatch.model_validate({})\nassert patch.model_dump(exclude_unset=True) == {}\n\nclear = OrderPatch.model_validate({\"shipping_address\": None})\nassert clear.model_dump(exclude_unset=True) == {\"shipping_address\": None}\n```\n\nHai object có cùng giá trị thuộc tính nhưng khác ý định cập nhật. Service dùng dictionary `exclude_unset=True`, kiểm trạng thái đơn có còn cho sửa không, rồi mới ghi database.",

so_sanh: "### Chọn đúng lớp bảo vệ cho từng invariant\n\n| Quy tắc | Pydantic | Domain/service | Database |\n|---|---|---|---|\n| `quantity` là integer 1–20 | phù hợp nhất | có thể giả định sau boundary | CHECK có thể bảo vệ lần cuối |\n| đơn shipping cần địa chỉ | model validator phù hợp | kiểm lại nếu domain tạo từ nguồn khác | constraint thường khó diễn đạt |\n| SKU đang tồn tại | không nên gọi DB trong validator | tải và quyết định lỗi nghiệp vụ | foreign key chống tham chiếu rác |\n| không bán quá tồn kho | snapshot validation không đủ | transaction/locking hoặc update có điều kiện | nơi phân xử cạnh tranh cuối cùng |\n| client không thấy risk score | response model allowlist | mapper có thể hỗ trợ | quyền DB không thay response contract |\n\nMỗi lớp trả lời một loại câu hỏi. Lặp cùng một quy tắc ở vài lớp đôi khi là defense in depth, nhưng không được nhầm lần kiểm tra sớm với nguồn sự thật cuối cùng.\n\n### Model riêng so với một model dùng chung\n\n| Thiết kế | Lợi ích | Giá phải trả |\n|---|---|---|\n| một model CRUD | ít class lúc đầu | optional tràn lan, mass assignment, contract khó đọc |\n| input/output tách biệt | quyền rõ, chống rò field | có mapping và nhiều tên hơn |\n| dictionary tự do | nhanh cho prototype | lỗi muộn, OpenAPI nghèo, refactor nguy hiểm |\n\nVới API public hoặc dữ liệu nhạy cảm, model riêng gần như luôn đáng chi phí. Prototype một lần dùng có thể chọn dictionary, nhưng phải nhìn nhận đó là nợ kỹ thuật chứ không phải kiến trúc linh hoạt.",

loi_thuong_gap: [
  "Dùng cùng model cho create và response; nhận biết khi client có thể gửi `id`, `status`, `customer_id` hoặc field chỉ server được đặt.",
  "Cho phép coercion ngoài ý muốn; producer gửi số dưới dạng chuỗi vẫn pass nên lỗi contract chỉ lộ khi đổi sang hệ thống chặt hơn.",
  "Đặt truy vấn database trong validator; dấu hiệu là tạo model cần event loop/session và một request sinh hàng loạt query ẩn.",
  "Hiểu `str | None` là field không bắt buộc; schema vẫn có thể yêu cầu field nếu không có default, hãy kiểm OpenAPI và payload thiếu.",
  "Trả thẳng ORM entity không có response model; thêm cột nội bộ rồi JSON đột nhiên lộ nó.",
  "Chỉ giới hạn số phần tử trong schema mà không giới hạn body; tiến trình tăng memory mạnh trước khi response 422 xuất hiện.",
  "Dùng `float` cho tiền; cộng nhiều dòng thấy sai số thập phân hoặc tổng serialize thành giá trị không mong muốn.",
  "Biến mọi lỗi nghiệp vụ thành 422; SKU hết hàng hay xung đột trạng thái cần contract lỗi riêng, không phải lỗi hình dạng JSON."
],

tu_kiem_tra: [
  "Tại sao payload có `sku` đúng pattern vẫn có thể bị service từ chối, và vì sao không nên truy vấn SKU trong validator?",
  "Client gửi `status=\"paid\"` khi tạo đơn. Ba model tách biệt ngăn việc này ở đâu?",
  "Nêu sự khác nhau giữa field vắng mặt, field có `null` và field có chuỗi rỗng trong PATCH.",
  "Response model bảo vệ bí mật theo cơ chế allowlist ra sao khi ORM entity vừa được thêm một cột mới?",
  "Vì sao `max_length` trên list chưa đủ chống request body cực lớn?",
  "Câu khó: hai request đồng thời đều validate rằng tồn kho còn một sản phẩm. Vì sao cả hai có thể pass và constraint/transaction nào phải phân xử?",
  "Khi nào strict mode làm contract tốt hơn, và khi nào nó gây khó cho dữ liệu từ form/query string?"
],

tai_nguyen: [
  "Pydantic Documentation — Models, Validators, Strict Mode và Serialization.",
  "FastAPI Documentation — Request Body và Response Model.",
  "Architecture Patterns with Python — Harry Percival và Bob Gregory.",
  "Designing Data-Intensive Applications — Martin Kleppmann."
],

lien_he: [
  "Nối sang mảng Database: schema kiểm hình dạng trước, còn foreign key, CHECK và transaction giữ invariant khi nhiều request cạnh tranh.",
  "Nối sang mảng Bảo mật Web: input allowlist chống mass assignment, response allowlist giảm rò dữ liệu, nhưng vẫn cần authentication và authorization.",
  "Nối sang mảng API Design: optional, nullable, versioning và error contract quyết định khả năng tương thích client.",
  "Nối sang mảng Testing: property-based test có thể sinh ca biên cho quantity, danh sách rỗng, field thừa và tổ hợp delivery–address."
]

};
