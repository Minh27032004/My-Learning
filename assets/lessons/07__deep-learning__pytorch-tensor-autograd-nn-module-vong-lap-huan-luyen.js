window.LESSONS["07:deep-learning:pytorch-tensor-autograd-nn-module-vong-lap-huan-luyen"] = {

tom_tat: [
  "Tensor là dữ liệu nhiều chiều có dtype và device; shape đúng nhưng dtype, thiết bị hoặc ngữ nghĩa chiều sai đều có thể phá huấn luyện.",
  "Autograd ghi đồ thị các phép toán trên tensor cần gradient rồi áp dụng chain rule khi gọi `backward()`; gradient được cộng dồn chứ không tự xóa.",
  "`nn.Module` đăng ký tham số và module con, quản lý `state_dict`, chế độ train/eval và di chuyển thiết bị; gọi tầng được khai trong `__init__` khác hẳn tạo tầng trong `forward`.",
  "Một vòng lặp đúng có thứ tự lấy batch, forward, tính loss, xóa gradient, backward, optimizer step và ghi metric với mẫu số đúng.",
  "Ví dụ xây pipeline GRU phân loại 120 mẫu rung thành ba tình trạng, có Dataset, DataLoader, validation và kiểm tra ca biên."
],

can_biet_truoc: [
  "Mảng nhiều chiều, shape, indexing, broadcasting và phép nhân ma trận.",
  "Đạo hàm, chain rule, gradient descent, loss cross-entropy và batch.",
  "Python class, iterator, context manager và khái niệm mutable state.",
  "Train/validation split, overfitting và metric cho phân loại nhiều lớp."
],

dinh_nghia: "**PyTorch** là thư viện tính toán tensor và deep learning theo đồ thị động. `torch.Tensor` chứa dữ liệu cùng shape, dtype và device. **Autograd** theo dõi phép toán để tính đạo hàm tự động. `nn.Module` là đơn vị đóng gói tầng, tham số và hành vi forward. Optimizer đọc gradient trong trường `.grad` của tham số để cập nhật trọng số.\n\nBốn phần không độc lập: tensor đi vào `Module`, phép toán dựng đồ thị autograd, loss nối đầu ra với mục tiêu, `backward()` đi ngược đồ thị, còn optimizer thay đổi các tensor đã được đăng ký là `Parameter`. Hiểu đường đi này quan trọng hơn thuộc một API riêng lẻ.",

vi_sao_can: "Notebook mười dòng có thể fit dữ liệu, nhưng hệ thống thật cần phát hiện lỗi ngầm: batch cuối nhỏ hơn, validation vô tình còn Dropout, gradient tích lũy, scaler fit trên test, hoặc loss trung bình sai. PyTorch trao quyền điều khiển trực tiếp nên dễ nghiên cứu và debug, đổi lại người viết phải chịu trách nhiệm về trạng thái.\n\nVới dữ liệu rung, mỗi batch là tensor `[B,120,3]`. Chỉ một lần transpose nhầm có thể khiến 120 được hiểu là batch; đôi khi chương trình vẫn chạy và tạo mô hình học sai đối tượng. Bài này xây mental model để trả lời ở mọi dòng: tensor có nghĩa gì, đang ở thiết bị nào, đồ thị còn giữ không, tham số nào sẽ đổi, và metric đại diện bao nhiêu mẫu.",

co_che: "### Tensor có bốn thuộc tính phải đọc cùng nhau\n\nGiá trị không đủ mô tả tensor. Shape `[32,120,3]` cho biết số phần tử theo chiều nhưng tên chiều chỉ tồn tại trong quy ước của code. `dtype=torch.float32` phù hợp đầu vào mạng; nhãn cho `CrossEntropyLoss` phải là `torch.int64` chứa chỉ số lớp, không phải float. `device` cho biết CPU hay accelerator. Layout/stride quyết định cách ánh xạ chỉ số vào bộ nhớ.\n\n`view` đòi vùng nhớ tương thích; sau `transpose`, tensor thường non-contiguous nên `reshape` có thể tạo bản sao. Bản sao không sai, nhưng có chi phí. In `shape`, `dtype`, `device`, `is_contiguous()` tại ranh giới pipeline hữu ích hơn nhìn vài giá trị.\n\n### Broadcasting tiện nhưng có thể che lỗi\n\nPyTorch căn các chiều từ phải sang trái và mở rộng chiều kích thước 1. Cộng bias `[H]` vào activation `[B,T,H]` là đúng. Trừ dự đoán `[B,1]` với mục tiêu `[B]` lại tạo `[B,B]`, so mọi mẫu với mọi nhãn mà không báo lỗi. Hãy assert shape trước loss; đừng xem “không exception” là bằng chứng đúng.\n\nPhép in-place có hậu tố `_`, như `add_`. Nó tiết kiệm allocation nhưng có thể ghi đè giá trị autograd cần cho backward. Ưu tiên phép ngoài-place khi chưa đo được nghẽn và chưa hiểu version counter.\n\n### requires_grad và lá của đồ thị\n\nTensor tham số là leaf tensor có `requires_grad=True`. Mỗi phép toán tạo nút mới chứa cách tính gradient cục bộ. Đồ thị được dựng lúc chạy Python, nên nhánh `if` nào thực sự đi qua mới xuất hiện. Đây là “dynamic graph”: debug tự nhiên, nhưng mỗi forward tạo đồ thị mới.\n\n`loss.backward()` tính đạo hàm loss theo các leaf cần gradient và cộng vào `.grad`. Autograd không phải symbolic algebra; nó lưu activation hoặc metadata cần thiết. Sau backward, đồ thị thường được giải phóng. `retain_graph=True` chỉ dùng khi thật sự backward nhiều lần trên cùng đồ thị, nếu lạm dụng sẽ tăng bộ nhớ.\n\n### Gradient cộng dồn là thiết kế\n\nHai lần `backward()` cộng hai đóng góp, vì mini-batch accumulation là tính năng. Vòng lặp thông thường phải `optimizer.zero_grad(set_to_none=True)` mỗi bước. Đặt gradient thành `None` có thể giảm ghi bộ nhớ và giúp phân biệt “không nhận gradient” với gradient toàn 0.\n\nThứ tự xóa trước hay sau `step()` đều có thể đúng nếu nhất quán, nhưng xóa ngay trước forward/backward làm ranh giới iteration dễ đọc. Khi tích lũy K microbatch, chia loss cho K trước backward rồi chỉ step sau K lần; nếu batch cuối thiếu microbatch phải điều chỉnh hoặc chấp nhận trọng số khác.\n\n### Chain rule qua một ví dụ nhỏ\n\nVới `y=(wx+b)²`, autograd lưu phép nhân, cộng và bình phương. Đạo hàm theo `w` là `2(wx+b)x`. Ta có thể kiểm tra bằng số hữu hạn, nhưng finite difference nhạy với bước epsilon và dtype. `torch.autograd.gradcheck` dùng double precision cho hàm nhỏ là công cụ kiểm tra custom operation; không dùng nó để kiểm toàn mạng lớn mỗi batch.\n\nGọi `.item()` biến tensor vô hướng thành số Python và cắt đồ thị. Dùng để log là đúng; dùng giữa forward để tính nhánh differentiable có thể vô tình ngăn gradient.\n\n### nn.Module đăng ký cấu trúc\n\nGán `self.gru = nn.GRU(...)` và `self.head = nn.Linear(...)` trong `__init__` khiến Module đăng ký module con. `model.parameters()`, `.to(device)`, `state_dict()` từ đó tìm được tensor. Nếu tạo `nn.Linear` mới bên trong `forward`, mỗi batch có trọng số ngẫu nhiên mới, optimizer không biết nó và checkpoint không lưu nó.\n\nDanh sách tầng phải dùng `nn.ModuleList`, không phải list Python thường, nếu muốn đăng ký. Tensor học trực tiếp dùng `nn.Parameter`; tensor trạng thái không học nhưng cần theo checkpoint dùng `register_buffer`.\n\n### forward là hợp đồng hình dạng\n\nNên ghi rõ đầu vào và kết quả. Bộ phân loại rung nhận `[B,T,3]`, trả logits `[B,3]`; không gọi softmax trước `CrossEntropyLoss` vì loss đã kết hợp `log_softmax` ổn định số. Logit thô dùng được cho `argmax`; chỉ softmax khi cần xác suất trình bày hoặc calibration.\n\nKhông gọi `model.forward(x)` trực tiếp. `model(x)` đi qua cơ chế hook, wrapper và một số tích hợp của PyTorch. Forward không nên tự gọi optimizer hay sửa dữ liệu toàn cục, vì làm kiểm thử và tái sử dụng khó.\n\n### train và eval thay đổi hành vi\n\n`model.train()` bật Dropout và cập nhật BatchNorm; `model.eval()` tắt Dropout ngẫu nhiên và dùng running statistics. Hai lệnh không bật/tắt autograd. Validation cần cả `model.eval()` lẫn `torch.inference_mode()` hoặc `no_grad()`.\n\nNgược lại, `no_grad()` không chuyển model sang eval. Nhầm hai khái niệm tạo điểm validation nhiễu hoặc thống kê BatchNorm bị test làm đổi. Sau validation phải gọi `train()` trước epoch mới.\n\n### Dataset và DataLoader xác định biên dữ liệu\n\nDataset ánh xạ index sang một mẫu; DataLoader gom batch, shuffle và có thể dùng worker. Shuffle train thường đúng, validation không cần. Với chuỗi dài khác nhau cần `collate_fn` để padding và trả lengths/mask.\n\nKhông dùng `drop_last=True` cho validation nếu muốn metric toàn tập. Với BatchNorm train, batch cuối một mẫu có thể gây vấn đề; có thể drop ở train nhưng phải biết đã bỏ dữ liệu. Seed worker và generator cần thiết khi yêu cầu tái lập.\n\n### Loss trung bình và metric đúng mẫu số\n\nNếu loss mặc định là mean theo batch, cộng `loss.item()` rồi chia số batch sẽ cho mỗi batch trọng số bằng nhau; batch cuối nhỏ vẫn ngang batch đầy. Đúng hơn là cộng `loss.item()*batch_size` rồi chia tổng số mẫu. Accuracy cũng cộng số dự đoán đúng và số mẫu, không lấy trung bình phần trăm batch.\n\nKhi lớp mất cân bằng, accuracy có thể che việc không phát hiện mòn ổ bi. Giữ confusion matrix, recall từng lớp và macro-F1 ở tầng đánh giá; vòng huấn luyện vẫn cần loss ổn định.\n\n### Optimizer sở hữu state riêng\n\nAdam giữ moment cho mỗi tham số; lưu chỉ `model.state_dict()` không đủ resume chính xác. Checkpoint huấn luyện cần optimizer state, scheduler state, epoch và trạng thái random khi yêu cầu tiếp tục bit-for-bit. Để inference chỉ cần model cùng preprocessing và mapping lớp.\n\nWeight decay không đồng nghĩa hoàn toàn L2 trong mọi optimizer. AdamW tách decay khỏi cập nhật adaptive; đừng suy luận chỉ từ tên tham số `weight_decay` mà không biết optimizer.\n\n### Validation không được tác động mô hình\n\nValidation phải không gọi backward/step, không fit scaler và không chọn mẫu dựa trên kết quả test. Dùng validation chọn epoch; test giữ kín. Nếu nhiều cửa sổ từ cùng máy, split theo máy trước khi Dataset cắt cửa sổ để tránh rò rỉ.\n\nLưu “best model” cần copy state hoặc serialize ngay. Gán `best_state=model.state_dict()` có thể giữ tham chiếu tới tensor tiếp tục thay đổi tùy cách dùng; `copy.deepcopy` thể hiện ý định rõ.\n\n### Debug theo từng lớp bằng invariant\n\nTrước khi train dài, overfit một batch nhỏ. Nếu loss không giảm, lỗi có thể ở nhãn, loss, optimizer hoặc gradient. Assert tensor hữu hạn, label trong `[0,C)`, logits đúng shape. In tên tham số có gradient `None`; hook chỉ dùng tạm vì dễ tạo overhead.\n\nAnomaly detection tìm operation tạo `nan` trong backward nhưng chậm, phù hợp debug ngắn. Gradient clipping che triệu chứng nên log norm trước clip.\n\n### Khi nào PyTorch thuần chưa đủ\n\nVòng lặp tự viết tốt để học và nghiên cứu đặc biệt. Khi dự án cần distributed training, checkpoint chịu lỗi, logging chuẩn và nhiều callback, framework tầng cao giảm boilerplate. Đánh đổi là hành vi ẩn và debug khó hơn. Dù dùng framework nào, mental model tensor–graph–parameter–optimizer vẫn bắt buộc.",

vi_du: "### Dataset và model hoàn chỉnh\n\n```python\nimport copy\nimport torch\nfrom torch import nn\nfrom torch.utils.data import TensorDataset, DataLoader, random_split\n\ntorch.manual_seed(23)\nX = torch.randn(300, 120, 3)\ny = torch.randint(0, 3, (300,), dtype=torch.long)\ntrain_ds, val_ds = random_split(TensorDataset(X, y), [240, 60],\n                                generator=torch.Generator().manual_seed(23))\ntrain_loader = DataLoader(train_ds, batch_size=32, shuffle=True)\nval_loader = DataLoader(val_ds, batch_size=32)\n\nclass PhanLoaiRung(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.gru = nn.GRU(3, 24, batch_first=True)\n        self.head = nn.Linear(24, 3)\n    def forward(self, x):\n        assert x.ndim == 3 and x.shape[-1] == 3\n        _, h = self.gru(x)\n        return self.head(h[-1])\n\nmodel = PhanLoaiRung()\nloss_fn = nn.CrossEntropyLoss()\noptimizer = torch.optim.AdamW(model.parameters(), lr=1e-3)\n```\n\n### Một epoch train và validation\n\n```python\ndef run_epoch(model, loader, loss_fn, optimizer=None):\n    training = optimizer is not None\n    model.train(training)\n    total_loss = total_ok = total_n = 0\n    context = torch.enable_grad() if training else torch.inference_mode()\n    with context:\n        for xb, yb in loader:\n            assert yb.dtype == torch.long\n            if training:\n                optimizer.zero_grad(set_to_none=True)\n            logits = model(xb)\n            loss = loss_fn(logits, yb)\n            if training:\n                loss.backward()\n                optimizer.step()\n            n = yb.numel()\n            total_loss += loss.item() * n\n            total_ok += (logits.argmax(1) == yb).sum().item()\n            total_n += n\n    return total_loss / total_n, total_ok / total_n\n\nbest_loss, best_state = float(\"inf\"), None\nfor epoch in range(3):\n    train_m = run_epoch(model, train_loader, loss_fn, optimizer)\n    val_m = run_epoch(model, val_loader, loss_fn)\n    if val_m[0] < best_loss:\n        best_loss = val_m[0]\n        best_state = copy.deepcopy(model.state_dict())\nmodel.load_state_dict(best_state)\nprint(\"logits:\", model(X[:2]).shape)\n```\n\nDữ liệu ngẫu nhiên nên không kỳ vọng accuracy cao; ví dụ kiểm cơ chế và shape, không bịa benchmark.\n\n### Tự kiểm autograd bằng công thức\n\n```python\nx = torch.tensor(3.0)\nw = torch.tensor(2.0, requires_grad=True)\nb = torch.tensor(-1.0, requires_grad=True)\nloss = (w * x + b) ** 2\nloss.backward()\nassert w.grad.item() == 30.0  # 2*(2*3-1)*3\nassert b.grad.item() == 10.0  # 2*(2*3-1)\n```\n\n### Ca biên batch cuối\n\nDataset train có 240 mẫu và batch 32 nên batch cuối 16. Hàm dùng `yb.numel()` làm mẫu số nên xử lý đúng. Dataset rỗng sẽ gây chia 0; production code phải báo lỗi rõ trước vòng lặp thay vì trả `nan`. Một mẫu vẫn chạy với GRU, nhưng BatchNorm trong kiến trúc khác có thể không huấn luyện được.",

so_sanh: "### Tensor, ndarray và Parameter\n\n| Đối tượng | Gradient tự động | Device accelerator | Được Module đăng ký | Dùng điển hình |\n|---|---|---|---|---|\n| NumPy ndarray | không | CPU | không | tiền xử lý, khoa học dữ liệu CPU |\n| Tensor thường | tùy `requires_grad` | có | không tự động | batch, activation, hằng số |\n| nn.Parameter | mặc định có | theo Module | có khi gán thuộc tính | trọng số học được |\n| Buffer | không | theo Module | có trong state | running statistics, mask cố định |\n\n### `no_grad` và `inference_mode`\n\n| Chế độ | Xây đồ thị gradient | Overhead | Trường hợp |\n|---|---|---|---|\n| bình thường | có khi cần | cao hơn | train |\n| `no_grad()` | không ghi operation | thấp | validation hoặc cập nhật tensor có kiểm soát |\n| `inference_mode()` | tắt thêm tracking nội bộ | thấp hơn | inference thuần, tensor không quay lại đồ thị train |\n\n`eval()` là trục khác, điều khiển hành vi module chứ không điều khiển autograd.",

loi_thuong_gap: [
  "Quên xóa gradient. Norm tăng qua batch và cập nhật phụ thuộc lịch sử ngoài ý muốn; kiểm `.grad` trước backward của batch mới.",
  "Softmax trước `CrossEntropyLoss`. Loss nhận logits; đưa xác suất làm gradient kém và tính log-softmax hai lần về ý nghĩa.",
  "Validation chỉ dùng `no_grad` nhưng quên `eval`. Dropout vẫn ngẫu nhiên, BatchNorm vẫn đổi; điểm thay đổi giữa hai lần chạy cùng dữ liệu.",
  "Tạo tầng trong `forward`. Số tham số optimizer không chứa tầng và dự đoán đổi do khởi tạo mới; khai tầng trong `__init__`.",
  "Lấy trung bình loss theo số batch. Batch cuối được cân nặng quá mức; nhân loss mean với kích thước batch rồi chia tổng mẫu.",
  "Broadcasting biến `[B,1]` và `[B]` thành `[B,B]`. Assert logits/mục tiêu theo hợp đồng trước loss.",
  "Gọi `.item()` giữa phép tính cần đạo hàm. Đồ thị bị cắt tại số Python; chỉ dùng cho logging hoặc điều khiển không cần gradient.",
  "Lưu model nhưng không lưu optimizer khi muốn resume. Adam mất moment và đường huấn luyện không tiếp nối thực sự."
],

tu_kiem_tra: [
  "Giải thích vì sao gradient được cộng dồn và cách dùng đặc tính đó cho microbatch accumulation.",
  "`eval()` khác `inference_mode()` ở trạng thái nào? Vì sao validation thường cần cả hai?",
  "Tại sao tạo `Linear` trong forward khiến model “chạy” nhưng không học tầng đó đúng cách?",
  "Cho prediction `[8,1]`, target `[8]`: broadcasting tạo shape nào và vì sao nguy hiểm?",
  "Thiết kế checklist overfit một batch để khoanh vùng pipeline không giảm loss.",
  "Vì sao cộng loss mean rồi chia số batch sai khi batch cuối nhỏ? Viết công thức đúng.",
  "Phân biệt Parameter, buffer và tensor thường về di chuyển device và checkpoint."
],

tai_nguyen: [
  "PyTorch Tutorials — Learn the Basics và Recipes.",
  "Deep Learning with PyTorch — Eli Stevens, Luca Antiga, Thomas Viehmann.",
  "Dive into Deep Learning — phần Deep Learning Computation.",
  "Deep Learning — Ian Goodfellow, Yoshua Bengio, Aaron Courville; các chương numerical computation và optimization."
],

lien_he: [
  "Nối sang Lập trình hệ thống: stride, contiguous memory và bản sao khi reshape giải thích chi phí ẩn của bố trí bộ nhớ.",
  "Nối sang Kiểm thử phần mềm: assertion về shape/dtype là design by contract cho pipeline số, còn overfit một batch là integration test nhỏ.",
  "Nối sang Cơ sở dữ liệu: Dataset/DataLoader tách truy cập và batching giống tầng đọc dữ liệu; shuffle không sửa được leakage do split sai.",
  "Nối sang MLOps: state_dict, optimizer state, preprocessing và mapping lớp tạo một artifact có thể tái lập, không chỉ file trọng số."
]

};
