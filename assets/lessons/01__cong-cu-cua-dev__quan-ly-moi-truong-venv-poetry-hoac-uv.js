window.LESSONS["01:cong-cu-cua-dev:quan-ly-moi-truong-venv-poetry-hoac-uv"] = {

tom_tat: [
  "**Module 5 dạy `venv` và `pip` để chạy được dự án; bài này dạy TÁI LẬP ĐƯỢC BUILD.** Cùng một lệnh cài, hôm nay và sáu tháng sau, trên máy bạn và trên máy chủ, phải cho **cùng một** bộ phiên bản.",
  "**`requirements.txt` viết tay KHÔNG tái lập được.** Nó liệt kê thứ bạn *muốn*, không phải thứ thật sự được cài — và không có phụ thuộc gián tiếp.",
  "**Phân biệt hai loại file: khai báo (`pyproject.toml`) và khoá (`lockfile`).** Cái đầu nói *tôi cần gì*, cái sau nói *chính xác cái gì đã được cài*. Cả hai đều phải commit.",
  "**`uv` nhanh hơn `pip` hàng chục lần** vì viết bằng Rust, tải song song, và dùng hard link thay vì copy — đủ nhanh để dựng lại môi trường trong CI mỗi lần chạy.",
  "**Chọn công cụ theo loại dự án:** ứng dụng cần khoá phiên bản chặt; thư viện cần khoảng phiên bản rộng để không xung đột với dự án của người dùng."
],

can_biet_truoc: [
  "Bài **venv, pip, requirements.txt, pyproject.toml** ở module 5 — cách tạo môi trường ảo và cài gói. Bài này giả định bạn đã nắm.",
  "Bài **Linter và formatter** trong module này — ghim phiên bản công cụ để máy và CI cho cùng kết quả.",
  "Bài **Biến môi trường và PATH** ở module 3 — cơ chế mà việc kích hoạt venv dựa vào.",
  "Bài **Module, package, import** ở module 5 — `sys.path` và cách Python tìm gói."
],

dinh_nghia: "Nói đời thường: một công thức bánh ghi *\"bột mì, đường, trứng\"* thì mười người làm ra mười cái bánh khác nhau. Ghi *\"bột mì loại X 250g, đường Y 100g\"* thì ai làm cũng ra cùng một cái.\n\nCác khái niệm:\n\n- **Môi trường ảo** (*virtual environment*) — thư mục chứa một bản Python riêng cùng các gói riêng của một dự án, độc lập với hệ thống và với dự án khác.\n- **File khai báo** (*manifest*) — liệt kê những gì dự án **cần**, thường là khoảng phiên bản: `fastapi>=0.100`. Ngày nay là `pyproject.toml`.\n- **File khoá** (*lockfile*) — liệt kê **chính xác** phiên bản của **mọi** gói đã cài, kể cả phụ thuộc gián tiếp, thường kèm mã băm để kiểm tính toàn vẹn.\n- **Phụ thuộc trực tiếp** — gói bạn tự khai báo. **Phụ thuộc gián tiếp** (*transitive*) — gói mà phụ thuộc của bạn cần.\n- **Giải phụ thuộc** (*resolution*) — quá trình tìm một tập phiên bản thoả mãn mọi ràng buộc cùng lúc.\n- **Tái lập được** (*reproducible*) — cài lại cho ra **đúng** bộ phiên bản cũ, bất kể thời gian và máy.\n- **Ghim phiên bản** (*pinning*) — cố định một phiên bản chính xác: `ruff==0.6.9`.\n\nMột phân biệt là trục của cả bài:\n\n```\nFILE KHAI BÁO   \"tôi cần fastapi mới hơn 0.100\"\n                → viết tay, đọc được, khoảng rộng\n\nFILE KHOÁ       \"đã cài fastapi 0.115.2, starlette 0.38.6,\n                  anyio 4.6.0, sniffio 1.3.1, idna 3.10, ...\"\n                → sinh tự động, dài, chính xác tuyệt đối\n\nCẢ HAI đều phải commit. Thiếu file khoá ⇒ không tái lập được.\n```",

vi_sao_can: "Đây là bài giải quyết một loại lỗi cực khó chịu: code không đổi mà chương trình vỡ.\n\n- **\"Máy tôi chạy được\" phần lớn là vấn đề phiên bản.** Bạn cài `pandas` hôm qua được 2.2.0; đồng nghiệp cài hôm nay được 2.2.3 với một hành vi đổi. Cùng `requirements.txt`, hai môi trường khác nhau.\n- **Phụ thuộc gián tiếp là chỗ vỡ nhiều nhất.** Bạn khai `fastapi`, nó kéo theo `starlette`, `pydantic`, `anyio`. Bạn không kiểm soát phiên bản chúng, mà chúng mới là nơi phát sinh lỗi.\n- **Không tái lập được thì không debug được sự cố cũ.** Production lỗi ở bản triển khai hai tuần trước; bạn cần dựng lại **đúng** môi trường đó. Không có file khoá thì không dựng lại được.\n- **CI chậm vì cài lại từ đầu mỗi lần.** Với `pip` là vài phút mỗi lần chạy; với `uv` là vài giây. Nhân với số lần push mỗi ngày của cả đội.\n- **Cài gói vào Python hệ thống gây hỏng khó sửa.** Trên Linux, nhiều tiện ích hệ thống viết bằng Python; `sudo pip install` nâng một gói lên có thể làm chúng vỡ.\n\nTình huống thật rất kinh điển: dự án chạy tốt sáu tháng. Một hôm CI đỏ ở một test không ai chạm tới. Nguyên nhân: `requirements.txt` ghi `pydantic>=2.0`, và pydantic vừa phát hành bản mới đổi một hành vi xác thực. **Không ai sửa gì cả — thời gian trôi qua là đủ để làm vỡ build.** Đó chính là điều file khoá ngăn chặn: build của bạn không phụ thuộc vào việc hôm nay là ngày nào.\n\nMột lưu ý về phạm vi và đánh đổi, để không hiểu sai: **khoá phiên bản không có nghĩa là không bao giờ nâng.** Nó có nghĩa là nâng phải là một **hành động có chủ ý**, thành một commit riêng, được test — thay vì xảy ra ngẫu nhiên vào lúc bạn không ngờ.",

co_che: "### Vì sao `requirements.txt` viết tay không đủ\n\n```\n# requirements.txt viết tay\nfastapi\nsqlalchemy>=2.0\npandas\n```\n\nBa vấn đề, mỗi cái đủ để phá tính tái lập:\n\n```\n① KHÔNG có phiên bản chính xác\n   \"fastapi\" hôm nay ra 0.115.2, tháng sau ra 0.116.0\n\n② KHÔNG có phụ thuộc GIÁN TIẾP\n   fastapi kéo theo starlette, pydantic, anyio... không được ghi lại\n\n③ KHÔNG có mã băm\n   không phát hiện được nếu gói trên PyPI bị thay đổi\n```\n\nVấn đề ② là nghiêm trọng nhất và ít người để ý: **phần lớn gói trong môi trường của bạn không nằm trong `requirements.txt`.**\n\n```bash\n$ pip install fastapi\n$ pip list | wc -l\n12          # khai 1 gói, cài 12\n```\n\n### `pip freeze` — bước tiến, nhưng chưa đủ\n\n```bash\npip freeze > requirements.txt\n```\n\n```\nannotated-types==0.7.0\nanyio==4.6.0\nfastapi==0.115.2\nidna==3.10\npydantic==2.9.2\npydantic-core==2.23.4\nsniffio==1.3.1\nstarlette==0.38.6\ntyping-extensions==4.12.2\n```\n\nGiải quyết được ① và ②, nhưng còn ba vấn đề:\n\n```\n① Không phân biệt gói TRỰC TIẾP với GIÁN TIẾP\n   → gỡ fastapi rồi, các gói nó kéo theo vẫn ở lại mãi\n\n② Không có mã băm\n\n③ Phụ thuộc vào MÔI TRƯỜNG lúc freeze\n   → gói chỉ cài trên Windows sẽ lọt vào file, làm vỡ build trên Linux\n```\n\nVấn đề ① tạo ra hiện tượng \"file requirements phình dần\": sau hai năm nó chứa hàng chục gói không ai biết còn cần không, và **không ai dám xoá**.\n\nCách chữa thủ công — tách hai file:\n\n```\nrequirements.in     ← viết TAY, chỉ gói trực tiếp\nrequirements.txt    ← SINH RA, đầy đủ, có băm\n```\n\n```bash\npip install pip-tools\npip-compile requirements.in         # sinh requirements.txt có băm\npip-sync requirements.txt           # cài ĐÚNG bộ đó, gỡ gói lạ\n```\n\n`pip-sync` là điểm đáng chú ý: khác `pip install -r`, nó **gỡ** những gói không nằm trong file — nên môi trường của bạn khớp chính xác với file khoá, không có gói \"rơi rớt\" từ lần thử nghiệm trước.\n\n### Ba lớp công cụ hiện đại\n\n```\npip + venv          có sẵn, ai cũng có. Không có lockfile thật.\npip-tools           thêm lockfile cho pip. Nhẹ, dễ hiểu.\npoetry              quản lý trọn gói: phụ thuộc, lock, build, publish.\nuv                  thay pip/venv/pip-tools, RẤT nhanh. Viết bằng Rust.\n```\n\nCả `poetry` và `uv` đều dùng `pyproject.toml` làm file khai báo — đây là chuẩn chung, nên chuyển đổi giữa chúng không quá khó.\n\n### `pyproject.toml` — file khai báo chuẩn\n\n```toml\n[project]\nname = \"ban-hang\"\nversion = \"1.0.0\"\nrequires-python = \">=3.12\"\ndependencies = [\n    \"fastapi>=0.115,<0.116\",\n    \"sqlalchemy>=2.0,<3.0\",\n]\n\n[project.optional-dependencies]\ndev = [\n    \"pytest>=8.0\",\n    \"ruff==0.6.9\",        # ⭐ công cụ thì GHIM CHÍNH XÁC\n    \"mypy==1.11.2\",\n]\n```\n\nMột điểm thực dụng đáng nhớ: **thư viện thì khai khoảng, công cụ thì ghim chính xác.**\n\n```\nfastapi>=0.115,<0.116   thư viện — cho phép bản sửa lỗi\nruff==0.6.9              công cụ — phiên bản khác cho KẾT QUẢ khác\n```\n\nLý do: nâng `ruff` từ 0.6.9 lên 0.7.0 có thể thêm quy tắc mới và làm CI đỏ đột ngột — trong khi code không đổi. Đó là lý do bài trước nhấn mạnh ghim chính xác công cụ.\n\n### Quy ước phiên bản ngữ nghĩa\n\n```\n2 . 9 . 2\n│   │   └── PATCH  sửa lỗi, tương thích\n│   └────── MINOR  thêm tính năng, tương thích\n└────────── MAJOR  phá vỡ tương thích\n```\n\n```\n>=2.0,<3.0    cho phép mọi bản 2.x        ← phổ biến cho thư viện\n~=2.9.0       tương đương >=2.9.0,<2.10.0  ← chỉ nhận bản sửa lỗi\n==2.9.2       ghim chính xác                ← công cụ, hoặc trong lockfile\n>=2.0         KHÔNG chặn trên              ⚠️ 3.0 sẽ phá vỡ code của bạn\n```\n\nDòng cuối là lỗi phổ biến nhất. `>=2.0` nghĩa là *\"tôi chấp nhận cả phiên bản 5.0 chưa tồn tại\"* — một lời hứa bạn không thể giữ. Luôn có **chặn trên** cho phụ thuộc quan trọng.\n\n⚠️ Nhưng đây là chỗ có đánh đổi thật, và nó khác nhau giữa hai loại dự án:\n\n```\nỨNG DỤNG   chặn trên CHẶT — bạn kiểm soát môi trường triển khai\n           \"fastapi>=0.115,<0.116\"\n\nTHƯ VIỆN   chặn trên LỎNG — người dùng phải kết hợp thư viện của bạn\n           với hàng chục thư viện khác\n           \"fastapi>=0.100\"  hoặc  \">=0.100,<1.0\"\n```\n\nLý do: nếu thư viện A đòi `pydantic<2.5` và thư viện B đòi `pydantic>=2.6`, người dùng **không cài được cả hai**. Chặn quá chặt trong thư viện tạo ra địa ngục xung đột phiên bản cho người khác.\n\n### `uv` — vì sao nhanh hơn hàng chục lần\n\n```bash\n# Cài\ncurl -LsSf https://astral.sh/uv/install.sh | sh      # macOS/Linux\n# hoặc: pip install uv\n```\n\n```bash\nuv venv                          # tạo môi trường ảo (nhanh hơn python -m venv)\nuv pip install fastapi            # tương thích cú pháp pip\nuv pip sync requirements.txt      # cài ĐÚNG bộ đó\nuv add fastapi                    # thêm vào pyproject.toml + cập nhật lock\nuv sync                           # dựng môi trường khớp uv.lock\nuv run pytest                     # chạy lệnh trong môi trường, không cần activate\n```\n\nBốn lý do nó nhanh:\n\n```\n① Viết bằng Rust, giải phụ thuộc bằng thuật toán hiệu quả\n② Tải song song nhiều gói\n③ Cache toàn cục + HARD LINK thay vì copy\n   → gói đã tải một lần thì dùng cho mọi dự án, gần như không tốn thời gian\n④ Không khởi động Python để làm việc của mình\n```\n\nĐiểm ③ đáng hiểu rõ vì nó là nguồn của phần lớn tốc độ: `pip` **copy** file vào từng venv, `uv` tạo **hard link** trỏ tới cache — nên cài `pandas` (khoảng 60MB) lần thứ hai gần như tức thì và không tốn thêm dung lượng đĩa.\n\nHệ quả thực tế quan trọng: **đủ nhanh để CI dựng lại môi trường sạch mỗi lần chạy** thay vì phải cache phức tạp. Môi trường sạch mỗi lần loại bỏ hẳn một loại lỗi \"CI xanh vì cache cũ\".\n\n`uv run` cũng đáng chú ý: nó chạy lệnh trong môi trường của dự án **mà không cần kích hoạt** — nên script và Makefile không phải lo chuyện activate.\n\n### Poetry — cách tiếp cận trọn gói\n\n```bash\npoetry new du-an          # tạo dự án theo khuôn\npoetry add fastapi        # thêm phụ thuộc + cập nhật poetry.lock\npoetry add --group dev pytest\npoetry install            # dựng môi trường theo lock\npoetry run pytest         # chạy trong môi trường\npoetry lock               # cập nhật lock mà không cài\npoetry build              # đóng gói thành wheel\npoetry publish            # đẩy lên PyPI\n```\n\n```\nƯU: một công cụ cho cả vòng đời, kể cả build và publish\n    quản lý nhóm phụ thuộc rõ ràng\n    lockfile tốt, có băm\n\nNHƯỢC: chậm hơn uv đáng kể\n       tự quản môi trường ảo theo cách riêng (dễ gây bối rối lúc đầu)\n       trước đây dùng cú pháp riêng trong pyproject.toml\n```\n\nPoetry phù hợp nhất khi bạn **phát hành thư viện** — phần build và publish của nó rất tiện. Với ứng dụng web hay dịch vụ, `uv` thường đủ và nhanh hơn nhiều.\n\n### Nhóm phụ thuộc — vì sao cần tách\n\n```toml\n[project]\ndependencies = [\"fastapi\", \"sqlalchemy\"]          # CHẠY production\n\n[project.optional-dependencies]\ndev = [\"pytest\", \"ruff\", \"mypy\"]                  # phát triển\ndocs = [\"mkdocs\", \"mkdocs-material\"]              # tài liệu\n```\n\n```bash\npip install -e \".\"           # chỉ production\npip install -e \".[dev]\"      # thêm công cụ phát triển\npip install -e \".[dev,docs]\"\n```\n\nBa lý do tách, theo mức độ quan trọng:\n\n```\n① BẢO MẬT — image production không chứa công cụ phát triển\n   ít gói ⇒ ít lỗ hổng tiềm ẩn\n② KÍCH THƯỚC — image nhỏ hơn, triển khai nhanh hơn\n③ RÕ RÀNG — biết gói nào thật sự cần để chạy\n```\n\nĐiểm ① là lý do thật, không phải hình thức: `pytest` và `ruff` không có việc gì trên máy chủ production, và mỗi gói thừa là một bề mặt tấn công.\n\n### Cài dự án ở chế độ có thể sửa\n\n```bash\npip install -e .\n```\n\nDấu `-e` (*editable*) tạo một liên kết tới mã nguồn thay vì copy nó. Nghĩa là:\n\n```\n· Sửa code → có hiệu lực NGAY, không phải cài lại\n· import du_an hoạt động từ BẤT KỲ thư mục nào\n· Không cần thao tác với sys.path\n```\n\nĐây là cách giải quyết đúng cho vấn đề rất hay gặp ở module 5: `import du_an.mo_hinh` chạy được khi ở thư mục gốc nhưng lỗi khi chạy từ thư mục con. Nguyên nhân là `sys.path`; `pip install -e .` giải quyết dứt điểm.\n\n### File khoá — commit hay không\n\n```\nỨNG DỤNG / DỊCH VỤ    COMMIT lockfile\n                      → mọi người và production dùng CÙNG bộ phiên bản\n\nTHƯ VIỆN              KHÔNG commit lockfile\n                      → người dùng cần tự do kết hợp với thư viện khác\n                      → nhưng NÊN test với nhiều tổ hợp phiên bản trong CI\n```\n\nĐây là một trong ít quy tắc trong Python có câu trả lời khá dứt khoát, và lý do nằm ở **ai kiểm soát môi trường**: ứng dụng thì bạn kiểm soát, thư viện thì người dùng kiểm soát.\n\n### Nâng phiên bản có chủ ý\n\n```bash\n# ① Xem gói nào cũ\nuv pip list --outdated\n\n# ② Nâng MỘT gói, xem gì đổi\nuv add \"fastapi@latest\"\n\n# ③ Chạy đủ kiểm tra\nruff check . && mypy . && pytest -q\n\n# ④ Commit RIÊNG, ghi rõ vì sao\ngit commit -am \"chore: nâng fastapi 0.115.2 -> 0.116.0\n\nBản mới sửa lỗi rò rỉ bộ nhớ khi dùng WebSocket (issue #12345).\nĐã chạy đủ test, không có thay đổi phá vỡ tương thích.\"\n```\n\nBa nguyên tắc nâng phiên bản:\n\n```\n· Nâng TỪNG gói, không nâng hàng loạt — vỡ thì biết ngay do gói nào\n· Commit RIÊNG, không lẫn thay đổi logic\n· Đọc CHANGELOG trước khi nâng bản MAJOR\n```\n\nNguyên tắc đầu quan trọng nhất: nâng mười gói cùng lúc rồi test đỏ thì bạn phải thử từng cái để tìm nguyên nhân — mất nhiều thời gian hơn hẳn so với nâng lần lượt.\n\n### Tái lập được — kiểm chứng bằng cách nào\n\n```bash\n# Phép thử: dựng lại từ đầu, kết quả phải GIỐNG HỆT\nrm -rf .venv\nuv sync --frozen        # --frozen: KHÔNG cập nhật lock, dùng đúng như đã khoá\npytest -q\n```\n\nCờ `--frozen` (hoặc `poetry install` không kèm `lock`) là chi tiết quyết định: nó bảo công cụ **không giải phụ thuộc lại**, chỉ cài đúng những gì lockfile ghi. Trong CI luôn phải dùng cờ này — nếu không, CI có thể âm thầm cài phiên bản mới hơn.\n\n```yaml\n# CI — dựng môi trường sạch, khoá cứng\n- uses: astral-sh/setup-uv@v3\n- run: uv sync --frozen --all-extras\n- run: uv run pytest -q\n```\n\nMột phép kiểm khác đáng làm định kỳ: **xoá cache và dựng lại.** Nếu build chỉ thành công khi có cache, bạn đang phụ thuộc vào thứ không được ghi lại ở đâu.",

vi_du: "### Ba mức độ tái lập\n\n```\n# ❌ MỨC 0 — requirements.txt viết tay\nfastapi\nsqlalchemy>=2.0\npandas\n```\n\n```bash\n# Tháng 1\n$ pip install -r requirements.txt\n$ pip freeze | grep -E \"fastapi|pandas\"\nfastapi==0.109.0\npandas==2.1.4\n\n# Tháng 6 — CÙNG file, CÙNG lệnh\n$ pip install -r requirements.txt\n$ pip freeze | grep -E \"fastapi|pandas\"\nfastapi==0.115.2      # ⚠️ khác\npandas==2.2.3         # ⚠️ khác\n\n# → Không ai sửa gì. Thời gian trôi qua là đủ để đổi môi trường.\n```\n\n```\n# ⚠️ MỨC 1 — pip freeze\nannotated-types==0.7.0\nanyio==4.6.0\nfastapi==0.115.2\npandas==2.2.3\n...  (47 dòng)\n\n# Tái lập được, NHƯNG:\n# · không biết dòng nào là gói mình THẬT SỰ cần\n# · gỡ pandas rồi thì numpy, pytz vẫn ở lại mãi\n# · không có mã băm\n```\n\n```toml\n# ✓ MỨC 2 — khai báo + khoá, hai file riêng\n# pyproject.toml — VIẾT TAY, chỉ gói trực tiếp\n[project]\nname = \"ban-hang\"\nrequires-python = \">=3.12\"\ndependencies = [\n    \"fastapi>=0.115,<0.116\",\n    \"sqlalchemy>=2.0,<3.0\",\n    \"pandas>=2.2,<3.0\",\n]\n\n[project.optional-dependencies]\ndev = [\n    \"pytest>=8.0\",\n    \"ruff==0.6.9\",        # ⭐ công cụ: ghim CHÍNH XÁC\n    \"mypy==1.11.2\",\n]\n```\n\n```\n# uv.lock — SINH TỰ ĐỘNG, commit vào Git, không sửa tay\n[[package]]\nname = \"fastapi\"\nversion = \"0.115.2\"\nsource = { registry = \"https://pypi.org/simple\" }\ndependencies = [\n    { name = \"pydantic\" },\n    { name = \"starlette\" },\n]\nsdist = { hash = \"sha256:3e4bd...\" }      # ⭐ mã băm: phát hiện gói bị đổi\n```\n\n```bash\n# Kiểm chứng tính tái lập\n$ rm -rf .venv\n$ uv sync --frozen           # ⭐ --frozen: dùng ĐÚNG lock, không giải lại\n$ uv pip list | grep fastapi\nfastapi   0.115.2            # ⭐ luôn luôn, bất kể hôm nay là ngày nào\n```\n\n### Vòng đời một dự án với `uv`\n\n```bash\n# ===== KHỞI TẠO =====\n$ uv init ban-hang\n$ cd ban-hang\n\n$ uv add fastapi sqlalchemy\nResolved 12 packages in 89ms\nInstalled 12 packages in 43ms          # ⭐ so với pip: vài giây\n\n$ uv add --dev pytest ruff mypy\n\n# ===== HẰNG NGÀY =====\n$ uv run pytest                 # ⭐ không cần activate\n$ uv run ruff check .\n$ uv run python -m ban_hang\n\n# ===== NGƯỜI MỚI CLONE =====\n$ git clone ... && cd ban-hang\n$ uv sync                        # dựng môi trường khớp uv.lock\nResolved 24 packages in 12ms\nInstalled 24 packages in 156ms   # ⭐ toàn bộ môi trường trong 0.2 giây\n\n# ===== NÂNG PHIÊN BẢN CÓ CHỦ Ý =====\n$ uv pip list --outdated\nfastapi   0.115.2  →  0.116.0\npandas    2.2.3    →  2.3.0\n\n$ uv add \"fastapi@latest\"        # nâng MỘT gói\n$ uv run pytest -q\n1 failed                          # ⚠️ phát hiện ngay do gói nào\n```\n\nNếu nâng cả hai gói cùng lúc rồi test đỏ, bạn phải thử từng cái để tìm nguyên nhân. **Nâng lần lượt là cách rẻ nhất để biết ngay.**\n\n### So sánh tốc độ thật\n\n```bash\n# Dựng môi trường sạch cho một dự án 40 gói\n\n$ rm -rf .venv && time python -m venv .venv && time pip install -r req.txt\nreal    0m3.2s      # tạo venv\nreal    0m52.4s     # cài gói\n\n$ rm -rf .venv && time uv venv && time uv pip sync req.txt\nreal    0m0.09s     # tạo venv\nreal    0m1.8s      # cài gói (đã có cache)\n```\n\n```\n≈ 30 lần nhanh hơn ở bước cài.\n\nHỆ QUẢ THỰC TẾ:\n· CI dựng môi trường SẠCH mỗi lần chạy → không lo \"xanh vì cache cũ\"\n· Chuyển nhánh có phụ thuộc khác → uv sync trong 2 giây\n· Thử một thư viện mới không phải chờ\n```\n\nHệ quả đầu là thứ đáng giá nhất: cache trong CI là nguồn của loại lỗi khó chịu \"CI xanh nhưng triển khai vỡ\", vì cache giữ lại phiên bản cũ đã hoạt động.\n\n### Tách nhóm phụ thuộc — dùng trong Docker\n\n```toml\n[project]\ndependencies = [\"fastapi\", \"sqlalchemy\", \"psycopg[binary]\"]\n\n[project.optional-dependencies]\ndev = [\"pytest\", \"pytest-cov\", \"ruff\", \"mypy\", \"ipdb\"]\ndocs = [\"mkdocs\", \"mkdocs-material\"]\n```\n\n```dockerfile\n# Image production — CHỈ gói cần để chạy\nFROM python:3.12-slim\n\nCOPY pyproject.toml uv.lock ./\nRUN pip install uv && uv sync --frozen --no-dev    # ⭐ --no-dev\n\nCOPY . .\nCMD [\"uv\", \"run\", \"uvicorn\", \"ban_hang.main:app\", \"--host\", \"0.0.0.0\"]\n```\n\n```bash\n# Khác biệt đo được\nVới --no-dev:      142 MB, 24 gói\nKhông --no-dev:    398 MB, 67 gói\n\n→ 43 gói thừa trên production: pytest, ruff, mypy, ipdb...\n  Mỗi gói là một bề mặt tấn công tiềm ẩn, và không gói nào có việc ở đó.\n```\n\n### `pip install -e .` giải quyết vấn đề import\n\n```\ndu-an/\n├── pyproject.toml\n├── ban_hang/\n│   ├── __init__.py\n│   ├── mo_hinh.py\n│   └── dich_vu/\n│       └── don.py\n└── tests/\n    └── test_don.py\n```\n\n```bash\n# ❌ Không cài dự án\n$ cd du-an && python -c \"import ban_hang\"        # OK, vì cwd nằm trong sys.path\n$ cd tests && python -c \"import ban_hang\"\nModuleNotFoundError: No module named 'ban_hang'   # ⚠️\n\n$ pytest tests/\nImportError while importing test module...\n```\n\n```bash\n# ✓ Cài ở chế độ editable\n$ pip install -e .          # hoặc: uv pip install -e .\n\n$ cd /bat/ky/dau && python -c \"import ban_hang; print(ban_hang.__file__)\"\n/duong/dan/du-an/ban_hang/__init__.py     # ⭐ trỏ về mã nguồn THẬT\n\n$ cd tests && pytest .      # ⭐ chạy được từ mọi nơi\n```\n\nĐiểm cốt lõi: `-e` tạo **liên kết** tới mã nguồn, nên sửa code có hiệu lực ngay mà không phải cài lại. Đây là cách đúng thay cho các mẹo chỉnh `sys.path` hay `PYTHONPATH`.\n\n### Vì sao không dùng `sudo pip install`\n\n```bash\n# ❌ TUYỆT ĐỐI tránh trên Linux\n$ sudo pip install requests==2.25.0\n\n# Hậu quả có thật:\n#   nhiều tiện ích hệ thống viết bằng Python và dùng requests\n#   hạ phiên bản có thể làm chúng vỡ\n#   apt và pip cùng quản một gói ⇒ trạng thái không nhất quán\n```\n\nPython hiện đại chặn hẳn việc này:\n\n```bash\n$ pip install requests\nerror: externally-managed-environment\n\n× This environment is externally managed\n╰─> To install Python packages system-wide, try apt install ...\n    If you wish to install a non-Debian-packaged Python package,\n    create a virtual environment using python3 -m venv path/to/venv\n```\n\nĐây là **tính năng**, không phải lỗi. Ba cách xử lý đúng:\n\n```bash\n# ✓ Dự án → môi trường ảo\n$ uv venv && uv pip install requests\n\n# ✓ Công cụ CLI dùng toàn máy → pipx hoặc uv tool\n$ uv tool install ruff        # mỗi công cụ một môi trường riêng\n$ pipx install black\n\n# ✓ Thử nhanh một lệnh, không cài gì\n$ uvx ruff check .            # tải, chạy, không để lại gì\n```\n\n`uvx` (hay `pipx run`) là lựa chọn tiện nhất cho việc thử một công cụ: nó chạy công cụ trong môi trường tạm và không làm bẩn máy.\n\n### Nâng bản MAJOR — quy trình cẩn thận\n\n```bash\n# pydantic 1.x → 2.x là thay đổi PHÁ VỠ TƯƠNG THÍCH\n\n# ① ĐỌC changelog TRƯỚC\n#    → biết những gì đổi, có công cụ chuyển đổi tự động không\n\n# ② Nâng trên NHÁNH RIÊNG\n$ git switch -c nang-pydantic-2\n$ uv add \"pydantic>=2.0,<3.0\"\n\n# ③ Chạy test, sửa từng lỗi\n$ uv run pytest -q\n23 failed\n```\n\n```python\n# Ví dụ thay đổi phải sửa\n# ❌ pydantic 1.x\nclass Don(BaseModel):\n    ma: str\n\n    class Config:\n        orm_mode = True\n\n    @validator(\"ma\")\n    def kiem_ma(cls, v):\n        ...\n\n# ✓ pydantic 2.x\nclass Don(BaseModel):\n    model_config = ConfigDict(from_attributes=True)\n\n    ma: str\n\n    @field_validator(\"ma\")\n    @classmethod\n    def kiem_ma(cls, v):\n        ...\n```\n\n```bash\n# ④ Commit RIÊNG, mô tả rõ\n$ git commit -am \"chore!: nâng pydantic 1.10 -> 2.9\n\nThay đổi phá vỡ tương thích:\n- Config -> model_config = ConfigDict(...)\n- @validator -> @field_validator + @classmethod\n- orm_mode -> from_attributes\n\nĐã sửa 23 test. Xem hướng dẫn di trú trong tài liệu pydantic.\"\n```\n\nDấu `!` trong `chore!:` là quy ước Conventional Commits cho thay đổi phá vỡ tương thích — nối về hook kiểm commit message ở bài trước.\n\n### CI với môi trường tái lập\n\n```yaml\n# .github/workflows/kiem-tra.yml\nname: Kiểm tra\non: [push, pull_request]\n\njobs:\n  kiem-tra:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: astral-sh/setup-uv@v3\n        with:\n          enable-cache: true\n          cache-dependency-glob: \"uv.lock\"    # ⭐ cache theo lockfile\n\n      - run: uv python install 3.12\n\n      - run: uv sync --frozen --all-extras\n        # ⭐ --frozen: LỖI nếu uv.lock không khớp pyproject.toml\n        #    → phát hiện ngay khi ai đó sửa pyproject mà quên chạy uv lock\n\n      - run: uv run ruff check .\n      - run: uv run mypy .\n      - run: uv run pytest -q\n```\n\nCờ `--frozen` là bảo vệ quan trọng nhất trong workflow này:\n\n```\nKhông --frozen  → uv giải lại phụ thuộc, có thể cài bản MỚI HƠN\n                  ⇒ CI test một môi trường KHÁC với máy bạn\n\nCó --frozen     → dùng ĐÚNG uv.lock, và BÁO LỖI nếu lock lỗi thời\n                  ⇒ CI và máy bạn giống nhau tuyệt đối\n```\n\nTest với nhiều phiên bản Python — dành cho thư viện:\n\n```yaml\n    strategy:\n      matrix:\n        python: [\"3.10\", \"3.11\", \"3.12\", \"3.13\"]\n    steps:\n      - run: uv python install ${{ matrix.python }}\n      - run: uv sync --frozen\n      - run: uv run pytest -q\n```\n\nĐây là chỗ khác biệt ứng dụng và thư viện lộ rõ: ứng dụng chạy trên **một** phiên bản Python mà bạn chọn; thư viện phải chạy trên **mọi** phiên bản bạn tuyên bố hỗ trợ.\n\n### Ứng dụng và thư viện — cấu hình khác nhau\n\n```toml\n# ===== ỨNG DỤNG: chặt, có lockfile =====\n[project]\nname = \"ban-hang-api\"\nrequires-python = \">=3.12\"           # ⭐ MỘT phiên bản, bạn kiểm soát\ndependencies = [\n    \"fastapi>=0.115,<0.116\",          # ⭐ chặn trên CHẶT\n    \"sqlalchemy>=2.0.30,<2.1\",\n]\n# uv.lock ĐƯỢC COMMIT\n```\n\n```toml\n# ===== THƯ VIỆN: rộng, không lockfile =====\n[project]\nname = \"thu-vien-thue-vn\"\nrequires-python = \">=3.10\"           # ⭐ hỗ trợ NHIỀU phiên bản\ndependencies = [\n    \"pydantic>=2.0,<3.0\",             # ⭐ chặn trên LỎNG\n]\n# uv.lock KHÔNG commit — thêm vào .gitignore\n```\n\nLý do đằng sau, đáng nhớ:\n\n```\nNếu thư viện của bạn ghi \"pydantic>=2.9,<2.10\"\nvà thư viện khác ghi \"pydantic>=2.8,<2.9\"\n⇒ người dùng KHÔNG cài được cả hai.\n\nChặn quá chặt trong thư viện tạo địa ngục xung đột cho người khác.\nBạn không kiểm soát môi trường của họ — nên đừng cố.\n```\n\n### Gỡ rối môi trường\n\n```bash\n# \"Cài rồi mà import vẫn lỗi\"\n$ which python                     # đang dùng Python nào?\n$ python -c \"import sys; print(sys.executable)\"\n$ python -c \"import sys; print(sys.path)\"\n$ pip list | grep ten_goi          # gói có trong MÔI TRƯỜNG NÀY không?\n\n# → Gần như luôn là: terminal và trình soạn thảo dùng hai môi trường khác nhau\n#   (đúng vấn đề ở bài VS Code)\n\n# \"Xung đột phiên bản\"\n$ uv pip tree                      # ⭐ xem cây phụ thuộc, tìm ai đòi gì\nban-hang v1.0.0\n├── fastapi v0.115.2\n│   ├── pydantic v2.9.2\n│   └── starlette v0.38.6\n└── thu-vien-cu v0.3.0\n    └── pydantic v1.10.18          # ⚠️ XUNG ĐỘT\n\n# \"Môi trường hỏng không rõ lý do\"\n$ rm -rf .venv && uv sync --frozen  # ⭐ dựng lại sạch, 2 giây\n```\n\nLệnh `uv pip tree` (hoặc `pipdeptree`) là công cụ duy nhất trả lời được câu *\"vì sao gói này lại có trong môi trường của tôi\"* — thông tin cần thiết khi giải quyết xung đột.\n\nVà lệnh cuối đáng thành thói quen: với `uv`, dựng lại môi trường sạch nhanh tới mức nó **luôn** là bước gỡ rối đầu tiên đáng thử, thay vì mất thời gian điều tra một môi trường đã bị làm bẩn.",

so_sanh: "### Ba mức độ tái lập\n\n| | `requirements.txt` viết tay | `pip freeze` | Khai báo + lockfile |\n|---|---|---|---|\n| Phiên bản chính xác | **✗** | ✓ | ✓ |\n| Phụ thuộc gián tiếp | **✗** | ✓ | ✓ |\n| Mã băm | ✗ | ✗ | **✓** |\n| Phân biệt trực tiếp/gián tiếp | ✓ | **✗** | **✓** |\n| Gỡ gói sạch được | ✗ | **✗** | ✓ |\n| Tái lập được | **✗** | phần lớn | **✓** |\n\nCột giữa là chỗ nhiều dự án đang đứng, và vấn đề của nó là **file phình dần mà không ai dám xoá gì**.\n\n### Bốn công cụ\n\n| | `pip` + `venv` | `pip-tools` | `poetry` | `uv` |\n|---|---|---|---|---|\n| Có sẵn | **✓** | ✗ | ✗ | ✗ |\n| Lockfile thật | ✗ | ✓ | ✓ | ✓ |\n| Tốc độ | chậm | chậm | chậm | **rất nhanh** |\n| Build & publish | ✗ | ✗ | **✓** | một phần |\n| Quản phiên bản Python | ✗ | ✗ | ✗ | **✓** |\n| Dùng `pyproject.toml` | ✓ | một phần | ✓ | ✓ |\n| Phù hợp nhất | học, script nhỏ | thêm lock cho pip | **thư viện** | **ứng dụng** |\n\n### Khai báo và khoá\n\n| | File khai báo | File khoá |\n|---|---|---|\n| Tên | `pyproject.toml` | `uv.lock`, `poetry.lock` |\n| Viết bằng | **tay** | **sinh tự động** |\n| Nội dung | khoảng phiên bản | phiên bản **chính xác** + băm |\n| Gồm gián tiếp | ✗ | **✓** |\n| Sửa tay | ✓ | **✗ không bao giờ** |\n| Commit | ✓ | **✓ (ứng dụng)** / ✗ (thư viện) |\n\n### Cú pháp khoảng phiên bản\n\n| Cú pháp | Nghĩa | Dùng cho |\n|---|---|---|\n| `==2.9.2` | chính xác | **công cụ** (ruff, mypy) |\n| `~=2.9.0` | `>=2.9.0,<2.10.0` | chỉ nhận bản sửa lỗi |\n| `>=2.0,<3.0` | mọi bản 2.x | **thư viện** |\n| `>=0.115,<0.116` | chặt | **ứng dụng** |\n| `>=2.0` | không chặn trên | **⚠️ tránh** |\n\nHàng cuối là lỗi phổ biến nhất: nó chấp nhận cả phiên bản chưa tồn tại.\n\n### Ứng dụng và thư viện\n\n| | Ứng dụng / dịch vụ | Thư viện |\n|---|---|---|\n| Chặn trên phiên bản | **chặt** | **lỏng** |\n| Commit lockfile | **✓** | **✗** |\n| Số phiên bản Python hỗ trợ | một | **nhiều** |\n| Test matrix trong CI | không cần | **cần** |\n| Ai kiểm soát môi trường | **bạn** | **người dùng** |\n\nHàng cuối giải thích mọi hàng trên. Chặn chặt trong thư viện tạo xung đột cho người khác.\n\n### Vì sao `uv` nhanh\n\n| Yếu tố | `pip` | `uv` |\n|---|---|---|\n| Ngôn ngữ | Python | **Rust** |\n| Tải gói | tuần tự | **song song** |\n| Cài từ cache | **copy** | **hard link** |\n| Khởi động Python | ✓ | ✗ |\n\nHard link là nguồn của phần lớn tốc độ: cài `pandas` lần thứ hai gần như tức thì và **không tốn thêm đĩa**.\n\n### Nhóm phụ thuộc — vì sao tách\n\n| Lý do | Cụ thể |\n|---|---|\n| **Bảo mật** | production không chứa `pytest`, `ipdb` — ít bề mặt tấn công |\n| Kích thước | 142 MB thay vì 398 MB |\n| Rõ ràng | biết gói nào thật sự cần để chạy |\n\n### Cài công cụ dùng toàn máy\n\n| Cách | Kết quả |\n|---|---|\n| `sudo pip install` | **✗ hỏng Python hệ thống** |\n| `uv tool install` / `pipx install` | ✓ mỗi công cụ một môi trường riêng |\n| `uvx` / `pipx run` | ✓ chạy tạm, **không để lại gì** |\n\n`externally-managed-environment` là **tính năng** bảo vệ, không phải lỗi cần bypass.",

loi_thuong_gap: [
  "**Dùng `requirements.txt` viết tay cho dự án thật.** Không có phiên bản chính xác, không có phụ thuộc gián tiếp — không tái lập được.",
  "**Khai `>=2.0` không có chặn trên.** Đó là lời hứa chấp nhận cả phiên bản 5.0 chưa tồn tại.",
  "**Chặn phiên bản quá chặt trong THƯ VIỆN.** Tạo xung đột không giải được cho người dùng.",
  "**Không commit lockfile của ứng dụng.** Mỗi người và production dùng bộ phiên bản khác nhau.",
  "**Commit lockfile của thư viện.** Người dùng cần tự do kết hợp với thư viện khác.",
  "**Sửa lockfile bằng tay.** Nó được sinh tự động; sửa tay làm mất tính nhất quán và mã băm.",
  "**Quên `--frozen` trong CI.** Công cụ giải lại phụ thuộc và có thể cài bản mới hơn — CI test môi trường khác máy bạn.",
  "**`sudo pip install`.** Có thể làm vỡ tiện ích hệ thống; `externally-managed-environment` là bảo vệ, không phải lỗi.",
  "**Không tách nhóm dev.** Image production chứa `pytest`, `ruff`, `ipdb` — 43 gói thừa và bấy nhiêu bề mặt tấn công.",
  "**Không dùng `pip install -e .`.** Rồi chỉnh `sys.path` hoặc `PYTHONPATH` để chữa lỗi import.",
  "**Nâng nhiều gói cùng lúc.** Test đỏ thì không biết do gói nào; nâng lần lượt.",
  "**Nâng bản MAJOR mà không đọc changelog.** Thay đổi phá vỡ tương thích lộ ra dần trong lúc sửa.",
  "**Commit nâng phiên bản lẫn với thay đổi logic.** Vỡ thì không biết do nâng hay do code.",
  "**Không ghim chính xác phiên bản công cụ.** `ruff 0.7` thêm quy tắc mới làm CI đỏ khi code không đổi.",
  "**Tin vào cache CI.** \"CI xanh vì cache cũ\" là loại lỗi khó chịu; `uv` nhanh đủ để dựng sạch mỗi lần.",
  "**Không biết `uv pip tree` khi gặp xung đột.** Đó là công cụ duy nhất trả lời \"vì sao gói này có trong môi trường của tôi\"."
],

tu_kiem_tra: [
  "Nêu ba vấn đề của `requirements.txt` viết tay. Vấn đề nào nghiêm trọng nhất và vì sao?",
  "`pip freeze` giải quyết được gì, còn thiếu gì? Hiện tượng \"file phình dần\" từ đâu ra?",
  "Phân biệt file khai báo và file khoá. Cái nào viết tay, cái nào sinh tự động?",
  "Vì sao cả hai file đều phải commit (với ứng dụng)?",
  "`pip-sync` khác `pip install -r` thế nào? Lợi ích là gì?",
  "Nêu bốn lý do `uv` nhanh hơn `pip`. Yếu tố nào đóng góp nhiều nhất?",
  "Vì sao tốc độ của `uv` thay đổi được cách tổ chức CI?",
  "Giải thích ý nghĩa MAJOR.MINOR.PATCH. `~=2.9.0` tương đương gì?",
  "Vì sao `>=2.0` là lỗi phổ biến? Nêu cách viết đúng.",
  "Ứng dụng và thư viện khác nhau thế nào về chặn phiên bản và lockfile? Lý do gốc là gì?",
  "Vì sao khai `pydantic>=2.9,<2.10` trong một thư viện là có hại?",
  "Nêu ba lý do tách nhóm dev. Lý do nào là thật, không phải hình thức?",
  "`pip install -e .` làm gì? Nó giải quyết vấn đề import nào ở module 5?",
  "Vì sao không dùng `sudo pip install`? `externally-managed-environment` là gì?",
  "Nêu ba cách cài công cụ CLI dùng toàn máy mà không làm bẩn Python hệ thống.",
  "Cờ `--frozen` làm gì? Vì sao CI bắt buộc phải dùng?",
  "Nêu ba nguyên tắc nâng phiên bản. Vì sao nâng từng gói tốt hơn nâng hàng loạt?",
  "Làm sao kiểm chứng dự án của bạn thật sự tái lập được?",
  "`uv pip tree` dùng để làm gì? Khi nào cần nó?"
],

tai_nguyen: [
  "**Tài liệu chính thức của uv** (docs.astral.sh/uv) — hướng dẫn đầy đủ về `uv add`, `uv sync`, `uv lock`, quản lý phiên bản Python, và cách di trú từ pip hay poetry.",
  "**Tài liệu Poetry** — mục *Dependency specification* giải thích cú pháp khoảng phiên bản và quản lý nhóm phụ thuộc.",
  "**Tài liệu Python Packaging User Guide** (packaging.python.org) — nguồn chuẩn về `pyproject.toml`, và bài *Tool recommendations* so sánh các công cụ.",
  "**PEP 621** — chuẩn khai báo metadata dự án trong `pyproject.toml`; đọc để hiểu vì sao mọi công cụ đều dùng file này.",
  "**Semantic Versioning** (semver.org) — quy ước đánh số phiên bản mà mọi cú pháp khoảng phiên bản dựa vào."
],

lien_he: [
  "**venv, pip, requirements.txt** ở module 5: nền tảng; bài này bổ sung lockfile và tính tái lập.",
  "**Linter và formatter** trong module này: ghim chính xác phiên bản công cụ để máy và CI cho cùng kết quả.",
  "**Pre-commit hook** trong module này: `rev` trong cấu hình hook phải khớp phiên bản trong `pyproject.toml`.",
  "**VS Code** trong module này: chọn interpreter là điểm nối giữa môi trường ảo và trình soạn thảo.",
  "**Docker cơ bản** ngay sau: `--no-dev` và lockfile là hai thứ làm image nhỏ và build tái lập được.",
  "**Module, package, import** ở module 5: `pip install -e .` là cách đúng để giải quyết vấn đề `sys.path`.",
  "**Biến môi trường và PATH** ở module 3: cơ chế mà việc kích hoạt venv dựa vào; `uv run` bỏ qua bước này.",
  "**DRY, KISS, YAGNI** ở module 8: lockfile là một nguồn sự thật duy nhất cho phiên bản — DRY áp cho cấu hình.",
  "**Git & GitHub** ở mảng 03: commit nâng phiên bản nên tách riêng; quy ước `chore!:` cho thay đổi phá vỡ tương thích.",
  "**Triển khai** ở mảng 05: môi trường tái lập được là điều kiện tiên quyết của triển khai đáng tin cậy.",
  "**MLOps** ở mảng 07: tái lập được còn quan trọng hơn nữa với ML — cùng dữ liệu, cùng phiên bản thư viện mới cho cùng kết quả huấn luyện."
]

};
