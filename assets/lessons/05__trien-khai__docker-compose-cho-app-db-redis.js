window.LESSONS["05:trien-khai:docker-compose-cho-app-db-redis"] = {

tom_tat: [
  "Docker Compose mô tả một ứng dụng nhiều container thành đồ thị service, network, volume, healthcheck và cấu hình; nó không gộp app, database, Redis vào một container.",
  "Tên service là DNS nội bộ ổn định, còn `localhost` luôn trỏ về container hiện tại; hiểu ranh giới này giải quyết phần lớn lỗi kết nối ban đầu.",
  "`depends_on` chỉ diễn tả thứ tự và có thể chờ healthcheck khi cấu hình đúng, nhưng ứng dụng vẫn phải chịu được dependency mất kết nối sau lúc khởi động.",
  "PostgreSQL cần named volume và migration có kiểm soát; Redis cache/rate-limit có thể tái tạo nên phải có chính sách persistence khác dữ liệu giao dịch.",
  "Compose rất hợp cho development, integration test và một máy chủ nhỏ; nó không tự cung cấp scheduler nhiều máy, secret manager hay high availability."
],

can_biet_truoc: [
  "Đã hiểu image, container, port publish, biến môi trường và Dockerfile của bài đóng gói Sổ Chi Tiêu API.",
  "Biết PostgreSQL giữ dữ liệu quan hệ bằng transaction, còn Redis là kho key–value trong bộ nhớ thường dùng cho cache và bộ đếm.",
  "Biết URL kết nối gồm scheme, user, mật khẩu, host, port và database; biết `localhost` là loopback của một network namespace.",
  "Biết migration thay đổi schema và phải tương thích với code đang phục vụ trong lúc rollout."
],

dinh_nghia: "**Docker Compose** là cách khai báo và điều khiển một ứng dụng gồm nhiều container trên cùng Docker host. File Compose mô tả các service cùng image/build, command, environment, network, volume, port, healthcheck và quan hệ phụ thuộc. CLI dùng mô hình ấy để tạo tài nguyên rồi đưa các container về trạng thái mong muốn.\n\nTrong Sổ Chi Tiêu, ba service có trách nhiệm khác nhau: `api` xử lý HTTP nhưng không giữ dữ liệu cục bộ; `db` chạy PostgreSQL và sở hữu ledger bền vững; `redis` giữ cache cùng bộ đếm rate-limit có thể tái tạo. Tách trách nhiệm khiến mỗi thành phần có vòng đời, quyền và chính sách lưu trữ phù hợp thay vì biến một container thành máy chủ đa dịch vụ khó thay thế.",

vi_sao_can: "Chạy riêng `docker run` ba lần đòi người phát triển nhớ network, tên volume, port, biến môi trường và thứ tự lệnh. Chỉ cần một người dùng tên network khác, lỗi đã không tái hiện được. Compose đưa topology đó vào một file có thể review và chạy lại, đặc biệt hữu ích cho onboarding và integration test.\n\nVấn đề sâu hơn không phải “khởi động ba chương trình”, mà là quản lý các vòng đời không giống nhau. API có thể bị hủy bất kỳ lúc nào. PostgreSQL không được mất volume. Redis có thể khởi động trống nếu chỉ làm cache, nhưng không thể tùy tiện mất bộ đếm nếu rate-limit là kiểm soát bảo mật quan trọng. Compose buộc ta ghi rõ ranh giới, song không tự chọn chính sách đúng.\n\nProduction nhiều máy cần orchestration, backup, secret manager và failover mà Compose không cung cấp. Vì vậy bài này dùng Compose làm mô hình chạy local sát production và công cụ kiểm thử hệ thống, không tô vẽ nó thành Kubernetes thu nhỏ.",

co_che: "### Đồ thị ba service của Sổ Chi Tiêu\n\nRequest từ trình duyệt đi vào port được publish của `api`. API phân giải hostname `db` để mở kết nối PostgreSQL và hostname `redis` để đọc cache hoặc tăng bộ đếm. Database không cần publish ra host nếu chỉ API dùng; giảm cổng công khai cũng giảm bề mặt tấn công.\n\n```text\nhost:8000 -> api:8000 -> db:5432       (giao dịch bền vững)\n                     \\-> redis:6379    (cache, rate-limit)\ndb volume -----------> /var/lib/postgresql/data\n```\n\nCompose tạo default network cho project. Container tham gia network nhận DNS theo tên service. DNS này ổn định dù địa chỉ IP đổi khi container được tạo lại, nên cấu hình phải ghi `db`, không ghi IP.\n\n### Vì sao `localhost` thường sai\n\nMỗi container có network namespace riêng. Bên trong `api`, `localhost:5432` nghĩa là cổng 5432 của chính container API, nơi không có PostgreSQL. Địa chỉ đúng là `db:5432`. Tương tự, host có thể truy cập `localhost:8000` chỉ vì ta publish `8000:8000`; service khác trong network gọi thẳng `api:8000`, không đi vòng qua cổng host.\n\nPhân biệt `ports` và `expose`: `ports` tạo đường từ host vào container; `expose` chủ yếu ghi nhận cổng nội bộ, còn các container chung network vốn đã có thể kết nối nếu process lắng nghe đúng interface. Không publish PostgreSQL và Redis chỉ để GUI local tiện truy cập; nếu cần debug tạm thời, dùng profile hoặc file override không áp dụng ở production.\n\n### File Compose làm nền tảng\n\n```yaml\nservices:\n  api:\n    build: .\n    command: uvicorn app.main:app --host 0.0.0.0 --port 8000\n    environment:\n      DATABASE_URL: postgresql://expense:${POSTGRES_PASSWORD}@db:5432/expense\n      REDIS_URL: redis://redis:6379/0\n    ports:\n      - \"8000:8000\"\n    depends_on:\n      db:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n    restart: unless-stopped\n\n  db:\n    image: postgres:16\n    environment:\n      POSTGRES_DB: expense\n      POSTGRES_USER: expense\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}\n    volumes:\n      - postgres_data:/var/lib/postgresql/data\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U expense -d expense\"]\n      interval: 5s\n      timeout: 3s\n      retries: 10\n\n  redis:\n    image: redis:7-alpine\n    command: redis-server --save \"\" --appendonly no\n    healthcheck:\n      test: [\"CMD\", \"redis-cli\", \"ping\"]\n      interval: 5s\n      timeout: 3s\n      retries: 10\n\nvolumes:\n  postgres_data:\n```\n\nVersion tag ở đây minh họa major version chứ không bảo đảm tái lập tuyệt đối; dự án nghiêm ngặt có thể khóa digest và cập nhật có lịch. Mật khẩu được nội suy lúc chạy, không viết thẳng vào file. Tuy vậy environment vẫn có thể bị process có quyền hoặc công cụ inspect đọc; production nên dùng secret facility của nền tảng.\n\n### Startup order không phải runtime resilience\n\nHealth condition giúp API không chạy trước khi PostgreSQL nhận kết nối. Nó không bảo đảm migration xong, schema đúng, database sẽ sống mãi hoặc Redis không restart sau đó. Mạng phân tán luôn cho phép dependency biến mất giữa hai câu lệnh.\n\nAPI cần timeout hữu hạn, retry có backoff cho thao tác an toàn và response 503 khi dependency thiết yếu chưa sẵn sàng. Không retry vô hạn: hàng trăm request cùng retry có thể đè database vừa phục hồi. Với thao tác tạo giao dịch, retry ở client có thể tạo bản ghi trùng; cần idempotency key hoặc unique constraint thay vì hy vọng request chỉ đến một lần.\n\n### Healthcheck kiểm tra đúng lớp\n\nPostgreSQL `pg_isready` trả lời khi server nhận kết nối, không chứng minh user có quyền trên schema hay migration tương thích. Redis `PING` chứng minh event loop đáp ứng, không chứng minh memory chưa gần cạn. Healthcheck nên rẻ và trả lời một câu hỏi hẹp.\n\nHealth của API nên tách live và ready. Liveness không phụ thuộc database để tránh restart API chỉ vì DB chập chờn. Readiness có thể thử một truy vấn nhẹ với timeout và loại instance khỏi nhận tải. Nếu một probe thực hiện truy vấn nặng mỗi vài giây trên mọi replica, chính hệ thống giám sát trở thành tải gây sự cố.\n\n### Volume và quyền sở hữu dữ liệu\n\nNamed volume tồn tại độc lập với container. `docker compose down` giữ volume; `docker compose down -v` xóa nó và mất dữ liệu local. Đây là lệnh phá hủy, phải phân biệt rõ trong tài liệu nhóm. Bind mount tiện sửa source tức thời nhưng mang permission và đường dẫn đặc thù host; named volume phù hợp dữ liệu do database quản lý.\n\nVolume không phải backup. Nó vẫn nằm trên một host, có thể hỏng cùng ổ đĩa hoặc bị xóa nhầm. Backup phải được sao chép ra vị trí khác và quan trọng hơn là phải diễn tập restore. Một file dump chưa từng restore chỉ là niềm tin, chưa phải khả năng phục hồi.\n\n### Redis có semantics khác PostgreSQL\n\nỞ ví dụ, Redis tắt snapshot và append-only vì cache có thể tái tạo. Khi Redis restart, cache miss tăng và rate-limit counter mất; API phải chịu được cache trống mà không trả dữ liệu sai. Nếu rate-limit là lớp chống brute force duy nhất, mất counter tạo cửa sổ tấn công. Khi đó cần persistence, datastore bền hơn hoặc nhiều lớp kiểm soát ở gateway.\n\nKhông cache object người dùng mà thiếu user id trong key. `expense:{id}` có thể làm dữ liệu của người A được trả cho người B nếu authorization bị thực hiện sau cache lookup. Key nên bao gồm tenant/user khi phù hợp, và quyền vẫn phải được kiểm tra trước khi trả response. TTL tránh dữ liệu cũ sống vô hạn nhưng không thay thế invalidation đúng lúc.\n\n### Migration là một công việc riêng\n\nĐể mỗi API replica tự chạy migration lúc startup gây tranh chấp và làm khó rollback. Tốt hơn là một one-shot service hoặc bước release chạy `alembic upgrade head` một lần, sau backup và trước khi code mới phụ thuộc schema mới nhận traffic.\n\nMigration production nên theo expand–migrate–contract: thêm cột/bảng tương thích, deploy code đọc được cả dạng cũ và mới, chuyển dữ liệu, rồi mới xóa cấu trúc cũ ở release sau. `ALTER TABLE` lớn có thể khóa hoặc viết lại dữ liệu; Compose không làm nguy cơ này biến mất.\n\n```yaml\n  migrate:\n    build: .\n    command: alembic upgrade head\n    environment:\n      DATABASE_URL: postgresql://expense:${POSTGRES_PASSWORD}@db:5432/expense\n    depends_on:\n      db:\n        condition: service_healthy\n    profiles: [\"tools\"]\n```\n\nChạy có chủ đích bằng `docker compose --profile tools run --rm migrate`, ghi nhận revision đã áp dụng và dừng rollout nếu migration thất bại.\n\n### Pool kết nối và giới hạn tài nguyên\n\nBa container không có nghĩa tài nguyên vô hạn. Nếu API có bốn worker, mỗi worker pool mười kết nối, ta đã có thể dùng bốn mươi connection. Thêm migration, dashboard và job nền có thể vượt `max_connections`. Pool cần timeout để request thất bại có kiểm soát thay vì treo chờ mãi.\n\nĐặt memory limit hữu ích để lộ hành vi dưới quota giống production. Redis thường bị OOM khi không có `maxmemory` và eviction policy phù hợp; PostgreSQL bị chậm nếu ép memory quá thấp. Giới hạn là một phần của thiết kế capacity, không phải con số sao chép từ blog.\n\n### Biến môi trường và file `.env`\n\nCompose tự đọc `.env` để nội suy nhưng file đó không nên commit nếu chứa secret. Dùng `.env.example` chỉ liệt kê tên và giá trị giả an toàn. Phân biệt nội suy `${POSTGRES_PASSWORD}` của Compose với `env_file` đưa biến vào container; nhầm hai lớp khiến biến có ở shell nhưng không có trong process hoặc ngược lại.\n\nKhi thiếu biến bắt buộc, cú pháp `${NAME:?message}` làm cấu hình fail sớm. Fail sớm tốt hơn khởi động với mật khẩu mặc định. Có thể xem mô hình sau nội suy bằng `docker compose config`, nhưng không dán output vào ticket vì nó có thể hiện secret.\n\n### Log, quan sát và correlation id\n\nContainer nên ghi log cấu trúc ra stdout/stderr; Compose gom stream bằng `docker compose logs`. API cần correlation id đi qua request, truy vấn và thông báo lỗi để ghép sự kiện giữa service. Không log access token, password, toàn bộ connection URL hay payload tài chính.\n\nMột lỗi “API 500” cần được phân loại: DNS không phân giải, connection refused, authentication failed, pool timeout, migration thiếu hay câu SQL lỗi. Mỗi loại có dấu hiệu khác và hướng xử lý khác. Log stack trace ở server nhưng response cho client chỉ trả mã lỗi ổn định, tránh lộ schema.\n\n### Profiles, override và parity có giới hạn\n\nProfile có thể bật Adminer, migration hoặc công cụ debug mà không đưa chúng vào đường chạy mặc định. File override development gắn source code và bật reload; production không mount code. Sự giống nhau quan trọng nằm ở image, protocol và dependency major version, không phải ép laptop mô phỏng hoàn hảo cloud.\n\nCompose local không mô phỏng network latency, disk failure, managed database proxy hay nhiều availability zone. Vì thế integration test bằng Compose là một lớp kiểm chứng, không phải bằng chứng cuối cùng cho production.\n\n### Shutdown và thứ tự mất kết nối\n\nKhi `docker compose down`, API cần ngừng nhận request, trả connection về pool rồi thoát. PostgreSQL cần thời gian flush. Dựa hoàn toàn vào “thứ tự dừng” vẫn mong manh; process phải chịu được peer biến mất. Job đang ghi giao dịch phải nằm trong transaction để hoặc commit đầy đủ, hoặc rollback đầy đủ.\n\nRedis cache update sau database commit có thể thất bại, tạo cache cũ. Pattern an toàn đơn giản là invalidate cache sau commit; lần đọc tiếp theo nạp lại. Với yêu cầu mạnh hơn, outbox và worker giúp retry sự kiện, nhưng đổi lại tăng thành phần và độ trễ.\n\n### Khi nào không nên dùng Compose\n\nKhông dùng Compose làm lời giải mặc định cho cluster nhiều host, autoscaling, zero-downtime rollout hoặc database high availability. Nó cũng quá nặng cho unit test chỉ cần fake dependency. Ngược lại, chạy PostgreSQL/Redis managed cho mọi test local có thể chậm, tốn tiền và làm test phụ thuộc mạng. Hãy dùng Compose ở nơi topology thật mang giá trị: integration test, môi trường phát triển và deployment đơn host có rủi ro chấp nhận được.",

vi_du: "### Endpoint ghi giao dịch với dependency rõ ràng\n\nĐoạn minh họa dưới đây chạy được khi cài FastAPI, SQLAlchemy async, asyncpg và redis client; schema `expenses` phải được tạo bằng migration. Điểm chính là transaction commit trước khi xóa cache và lỗi Redis không được rollback giao dịch đã bền vững.\n\n```python\nimport os\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI, HTTPException\nfrom pydantic import BaseModel, Field\nfrom redis.asyncio import Redis\nfrom sqlalchemy import text\nfrom sqlalchemy.ext.asyncio import create_async_engine\n\nengine = create_async_engine(os.environ[\"DATABASE_URL\"], pool_size=5, pool_timeout=3)\nredis = Redis.from_url(os.environ[\"REDIS_URL\"], socket_timeout=1)\n\nclass ExpenseIn(BaseModel):\n    amount: int = Field(gt=0, le=1_000_000_000)\n    note: str = Field(min_length=1, max_length=200)\n\napp = FastAPI()\n\n@app.post(\"/users/{user_id}/expenses\", status_code=201)\nasync def create_expense(user_id: int, body: ExpenseIn):\n    if user_id <= 0:\n        raise HTTPException(422, \"user_id phải dương\")\n    async with engine.begin() as conn:\n        row = (await conn.execute(text(\n            \"INSERT INTO expenses(user_id, amount, note) \"\n            \"VALUES (:u, :a, :n) RETURNING id\"\n        ), {\"u\": user_id, \"a\": body.amount, \"n\": body.note})).one()\n    try:\n        await redis.delete(f\"expense-summary:{user_id}\")\n    except Exception:\n        pass  # log có lọc; giao dịch không được biến mất vì cache lỗi\n    return {\"id\": row.id, \"amount\": body.amount}\n```\n\nTrong code thật, `user_id` phải lấy từ principal đã xác thực thay vì tin path tùy ý. Việc nuốt mọi exception ở ví dụ chỉ chấp nhận nếu có metric/log và cache là tùy chọn; lỗi lập trình không nên bị che.\n\n### Kịch bản kiểm thử bằng Compose\n\n```bash\ndocker compose config --quiet\ndocker compose build\ndocker compose up -d db redis\ndocker compose --profile tools run --rm migrate\ndocker compose up -d api\ndocker compose ps\ncurl -X POST http://localhost:8000/users/1/expenses \\\n  -H \"content-type: application/json\" \\\n  -d '{\"amount\":125000,\"note\":\"Tiền điện\"}'\ndocker compose restart redis\ncurl http://localhost:8000/health/live\n```\n\nSau khi Redis restart trống, expense vẫn nằm trong PostgreSQL và API vẫn live. Sau `docker compose restart api`, dữ liệu cũng còn vì volume thuộc DB. Hãy thử payload `amount: 0`, note rỗng và số vượt giới hạn; chúng phải nhận 422 trước SQL. Sau cùng thử sai mật khẩu DB: log phải chỉ ra authentication failure nhưng không in mật khẩu.\n\n### Test mất dependency có chủ đích\n\nDừng `db` trong lúc API còn chạy. Request tạo expense cần kết thúc bằng lỗi hữu hạn, không treo vô tận. Khởi động DB lại và quan sát API phục hồi mà không cần rebuild image. Phép thử này phân biệt “startup order đúng” với “hệ thống chịu lỗi runtime”; chỉ cái sau mới phản ánh deployment thật.",

so_sanh: "### Compose đứng ở đâu trong các lựa chọn\n\n| Tiêu chí | Nhiều lệnh `docker run` | Docker Compose | Orchestrator/managed platform |\n|---|---|---|---|\n| Topology lưu thành code | Thấp, dễ quên cờ | Tốt trên một host | Tốt, thêm scheduler và controller |\n| Development local | Vụn và khó chia sẻ | Rất phù hợp | Thường quá nặng hoặc tốn phí |\n| Tự phục hồi một container | Có thể cấu hình riêng lẻ | Có restart policy | Có reconciliation và health routing |\n| Nhiều host, autoscaling | Không | Không phải mục tiêu | Có |\n| Secret và policy | Tự quản lý | Cơ bản | Thường tích hợp secret manager/IAM |\n| Độ phức tạp | Thấp cho một service | Vừa cho stack nhỏ | Cao hơn, đổi lại năng lực vận hành |\n\nSQLite có thể thay cả PostgreSQL trong demo đơn giản, nhưng semantics ghi đồng thời, migration và backup khác production. Test unit nên dùng dependency giả để chạy nhanh; integration test nên dùng PostgreSQL thật qua Compose khi cần chứng minh SQL và transaction. Redis cũng không bắt buộc nếu traffic nhỏ: cache thêm invalidation, stale data và một failure mode. Chỉ giữ Redis khi rate-limit/cache đã có lý do đo được hoặc khi mục tiêu học tập là luyện ranh giới phân tán.",

loi_thuong_gap: [
  "**Dùng `localhost` trong URL của API.** Log báo connection refused dù DB healthy; đổi host thành tên service `db` hoặc `redis`.",
  "**Tin rằng `depends_on` bảo đảm dependency sống mãi.** Stack khởi động được nhưng lỗi sau khi restart DB; thêm timeout, retry có giới hạn và test gián đoạn runtime.",
  "**Publish mọi port ra host.** `docker compose ps` cho thấy PostgreSQL/Redis công khai không cần thiết; bỏ `ports` và giữ chúng trong network nội bộ.",
  "**Xem named volume là backup.** Xóa host hoặc chạy `down -v` là mất dữ liệu; tạo backup ngoài host và diễn tập restore.",
  "**Cho mỗi replica tự chạy migration.** Log có lock/race lúc startup; tách migration thành one-shot job và dùng thay đổi schema tương thích.",
  "**Dùng cache trước authorization.** Người dùng có thể nhận object của người khác qua key chung; kiểm quyền và thiết kế key theo tenant/user.",
  "**Đưa secret thật vào Compose đã commit.** Secret xuất hiện trong history và bản sao repo; dùng nội suy/secret manager, sau đó xoay credential đã lộ.",
  "**Tạo pool theo từng worker mà quên tổng.** PostgreSQL báo quá nhiều connection; tính tổng replica × worker × pool và chừa headroom vận hành."
],

tu_kiem_tra: [
  "Vì sao `api` gọi `db:5432`, còn công cụ trên host chỉ gọi được `localhost:<published-port>`?",
  "Hãy phân biệt ba trạng thái: process PostgreSQL đã chạy, database nhận connection, và schema đã sẵn sàng cho phiên bản API mới.",
  "Nếu Redis mất toàn bộ key, điều gì trong Sổ Chi Tiêu được phép chậm hoặc lỏng hơn, và điều gì tuyệt đối không được sai?",
  "Thiết kế migration thêm cột bắt buộc sao cho code cũ và code mới cùng chạy trong một giai đoạn rollout.",
  "Vì sao readiness phụ thuộc DB còn liveness thường không nên phụ thuộc DB? Nếu làm ngược, vòng lặp restart nào có thể xảy ra?",
  "Với 3 API replica, 4 worker mỗi replica và pool 8, tổng connection cực đại là bao nhiêu trước migration và tác vụ quản trị?",
  "Compose phù hợp cho production đơn host ở điều kiện nào, và dấu hiệu nào cho thấy đã cần nền tảng orchestration hoặc dịch vụ managed?"
],

tai_nguyen: [
  "Docker Documentation — phần Compose, networking, volumes và healthcheck.",
  "Designing Data-Intensive Applications — Martin Kleppmann.",
  "Release It! — Michael T. Nygard.",
  "PostgreSQL Documentation — phần backup, connection và transaction.",
  "Redis Documentation — phần persistence, eviction và security."
],

lien_he: [
  "Nối sang mảng Cơ sở dữ liệu: volume, transaction, migration và pool biến schema PostgreSQL thành phần có vòng đời độc lập với API.",
  "Nối sang mảng Mạng máy tính: service discovery bằng DNS, network namespace và port publish giải thích vì sao cùng một URL không dùng được ở host lẫn container.",
  "Nối sang mảng Bảo mật web: authentication, rate-limit, secret và cache key theo tenant phải được giữ đúng qua ranh giới service.",
  "Nối sang mảng Kiểm thử: Compose tạo fixture tích hợp có PostgreSQL/Redis thật, còn fault injection kiểm chứng timeout và phục hồi thay vì chỉ happy path."
]

};
