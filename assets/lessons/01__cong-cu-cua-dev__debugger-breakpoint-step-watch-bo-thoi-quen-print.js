window.LESSONS["01:cong-cu-cua-dev:debugger-breakpoint-step-watch-bo-thoi-quen-print"] = {

tom_tat: [
  "**Module 8 dùng `pdb` như một cách ĐỌC code; bài này đi sâu vào công cụ để SỬA LỖI.** Breakpoint điều kiện, watch, post-mortem, debug test — những thứ `print` không làm được.",
  "**`print` không sai — nó chỉ kém hiệu quả cho phần lớn trường hợp.** Mỗi lần đoán sai chỗ là một vòng sửa-chạy-đọc; debugger cho bạn xem **mọi** biến ở **mọi** khung mà không phải đoán trước.",
  "**Breakpoint điều kiện là tính năng đáng giá nhất.** Bug xảy ra ở bản ghi thứ 4.782 — đặt điều kiện `ma == 'DH4782'` thay vì bấm continue bốn nghìn lần.",
  "**Post-mortem (`pdb.pm()`) cho phép khám nghiệm sau khi chương trình đã chết** — xem được toàn bộ biến tại thời điểm ném lỗi, không cần chạy lại.",
  "**Debugger không thay thế được logging.** Debugger cho lỗi tái hiện được trên máy bạn; logging cho lỗi xảy ra lúc 3 giờ sáng trên production."
],

can_biet_truoc: [
  "Bài **Đọc và review code người khác** ở module 8 — đã giới thiệu `breakpoint()` và lệnh `w` như công cụ đọc.",
  "Bài **VS Code** ngay trước — `launch.json` và tuỳ chọn `justMyCode`.",
  "Bài **Xử lý ngoại lệ** ở module 5 và **Xử lý lỗi tường minh** ở module 8 — traceback, cây exception.",
  "Bài **Stack và heap** ở module 2 — khung ngăn xếp (stack frame) là khái niệm trung tâm của debugger."
],

dinh_nghia: "Nói đời thường: xe hỏng, bạn có thể đoán rồi thay từng bộ phận — hoặc cắm máy chẩn đoán vào và nhìn thẳng vào thông số. Cách thứ hai không phải lúc nào cũng cần, nhưng khi cần thì nó tiết kiệm cả buổi.\n\nCác khái niệm:\n\n- **Debugger** — chương trình cho phép **dừng** một chương trình khác giữa chừng, **xem** trạng thái, và **chạy từng bước**.\n- **Breakpoint** — điểm dừng. Chương trình chạy bình thường tới đó rồi dừng lại, chờ bạn.\n- **Breakpoint điều kiện** — chỉ dừng khi một biểu thức đúng: `i > 1000`, `don.ma == \"DH4782\"`.\n- **Khung ngăn xếp** (*stack frame*) — một lần gọi hàm đang dở dang, kèm biến cục bộ của nó. Ngăn xếp là chồng các khung đó.\n- **Step over / step into / step out** — chạy hết một dòng · bước **vào trong** hàm được gọi · chạy nốt hàm hiện tại rồi dừng ở nơi gọi.\n- **Watch** — biểu thức được tính lại và hiển thị mỗi lần chương trình dừng.\n- **Post-mortem** — gắn debugger vào **sau khi** chương trình đã ném lỗi, để khám nghiệm trạng thái tại thời điểm chết.\n- **Debug từ xa** (*remote debugging*) — gắn debugger vào tiến trình đang chạy ở nơi khác: container, máy chủ.\n\nMột phân biệt định hình cả bài:\n\n```\n`print`     bạn phải ĐOÁN TRƯỚC cần xem biến nào, ở dòng nào\nDEBUGGER    bạn dừng lại rồi hỏi BẤT KỲ điều gì, ở BẤT KỲ khung nào\nLOGGING     ghi lại để đọc SAU, ở nơi bạn không có mặt\n```\n\nBa công cụ cho ba tình huống khác nhau — và phần cuối bài nói rõ khi nào dùng cái nào.",

vi_sao_can: "Sửa lỗi chiếm phần lớn thời gian của một lập trình viên, và cách bạn sửa lỗi quyết định phần lớn đó dài hay ngắn:\n\n- **`print` bắt bạn đoán trước.** Bạn thêm `print(x)`, chạy lại, thấy `x` đúng, rồi phải đoán tiếp biến nào. Mỗi lần đoán sai là một vòng sửa file — chạy lại — đọc kết quả. Với chương trình khởi động chậm, mỗi vòng là vài phút.\n- **`print` không thấy được ngăn xếp gọi.** Bạn biết giá trị sai ở đây, nhưng không biết hàm này được gọi từ đâu với tham số gì — thứ debugger hiện ngay.\n- **`print` sót lại trong code.** Ai cũng từng để lọt `print(\"aaa\")` lên production. Breakpoint không lọt vào Git được — trừ khi bạn gõ `breakpoint()` và quên xoá, và điều đó thì linter bắt được.\n- **Bug ở vòng lặp thứ N gần như không sửa được bằng `print`.** In 5.000 dòng ra terminal rồi cuộn tìm là cách làm tệ; breakpoint điều kiện dừng đúng chỗ.\n- **Debugger là công cụ đọc code mạnh nhất**, như bài module 8 đã nói — nó cho bạn thấy đường đi *thực tế*, không phải đường đi bạn *nghĩ*.\n\nTình huống thật rất phổ biến: một hàm xử lý 5.000 đơn hàng, đơn thứ 4.782 gây lỗi. Với `print`, bạn in mọi đơn rồi cuộn terminal tìm — mà terminal thường chỉ giữ 1.000 dòng cuối. Bạn thêm `if i > 4700: print(...)`, chạy lại, phát hiện cần xem thêm một biến nữa, sửa, chạy lại. **Ba mươi phút.** Với breakpoint điều kiện `don.ma == \"DH4782\"`: chạy một lần, dừng đúng chỗ, xem mọi biến trong mọi khung. **Hai phút.**\n\nPhản biện công bằng: `print` vẫn hợp lý trong một số trường hợp thật, và phần cuối bài liệt kê chúng. Vấn đề không phải `print` xấu — mà là dùng nó cho **mọi** thứ.",

co_che: "### Đặt điểm dừng — ba cách\n\n```python\n# ① breakpoint() — Python 3.7+, không cần import\ndef tinh_gia(don):\n    breakpoint()          # ⭐ dừng ở đây\n    tong = sum(m.gia for m in don.muc)\n    return tong\n```\n\n```python\n# ② pdb.set_trace() — cách cũ, tương đương\nimport pdb; pdb.set_trace()\n```\n\n```\n③ Bấm vào lề trái số dòng trong VS Code → chấm đỏ\n   ⭐ KHÔNG sửa file, nên không bao giờ lọt vào Git\n```\n\nCách ③ tốt nhất cho công việc hằng ngày. Cách ① hữu ích khi debug trong container, qua SSH, hoặc ở nơi không có giao diện.\n\nMột tính năng ít người biết của `breakpoint()`: nó tuân theo biến môi trường `PYTHONBREAKPOINT`, nên **tắt được toàn bộ mà không sửa code**:\n\n```bash\nPYTHONBREAKPOINT=0 python main.py      # bỏ qua mọi breakpoint()\nPYTHONBREAKPOINT=ipdb.set_trace python main.py   # dùng debugger khác\n```\n\n### Lệnh `pdb` — nhóm theo mục đích\n\n**Xem xung quanh:**\n\n```\nl (list)      xem code quanh chỗ dừng\nll            xem TOÀN BỘ hàm hiện tại\nw (where)     xem NGĂN XẾP GỌI — ai gọi tới đây\na (args)      tham số của hàm hiện tại\np bien        in một biến\npp bien       in ĐẸP (dict, list lớn)\n```\n\n**Di chuyển giữa các khung — nhóm bị bỏ qua nhiều nhất:**\n\n```\nu (up)        lên khung của HÀM GỌI\nd (down)      xuống khung của hàm được gọi\n```\n\nĐây là khả năng mà `print` **hoàn toàn không có**. Bạn đang ở trong một hàm sâu, giá trị sai, nhưng nguyên nhân nằm ở hàm gọi nó ba tầng phía trên. Gõ `u` ba lần là bạn đứng ở khung đó và xem được biến cục bộ của nó.\n\n**Chạy tiếp:**\n\n```\nn (next)      chạy hết dòng này, KHÔNG vào trong hàm\ns (step)      bước VÀO trong hàm được gọi\nr (return)    chạy nốt hàm hiện tại, dừng khi nó return\nc (continue)  chạy tiếp tới breakpoint sau\nuntil 45      chạy tới dòng 45\nq (quit)      thoát\n```\n\nPhân biệt `n` và `s` là điều đầu tiên phải nắm:\n\n```python\ntong = tinh_tong(don)     # n: chạy xong cả hàm, dừng ở dòng sau\n                           # s: nhảy VÀO trong tinh_tong()\n```\n\nQuy tắc thực dụng: **`n` cho tới khi thấy dòng nghi ngờ, rồi `s` vào đúng dòng đó.** Dùng `s` cho mọi dòng sẽ đưa bạn lạc vào hàm dựng của thư viện chuẩn rất nhanh.\n\n`r` là lệnh tiết kiệm thời gian nhất khi lỡ bước vào một hàm dài: nó chạy nốt hàm đó và dừng ngay khi trả về, cho bạn xem cả giá trị trả về.\n\n**Thay đổi và thử nghiệm:**\n\n```\n!bien = 10        gán lại giá trị (dấu ! để không nhầm với lệnh pdb)\ninteract          mở REPL Python đầy đủ với biến hiện tại\n```\n\n`interact` rất mạnh và ít người biết: nó cho bạn một shell Python bình thường **ngay trong ngữ cảnh của khung đang dừng** — viết vòng lặp, thử hàm, khám phá dữ liệu thoải mái. Gõ `Ctrl+D` để quay lại pdb.\n\n⚠️ Cạm bẫy tên biến: nếu biến của bạn tên `n`, `s`, `c`, `l`, `p`, `a`, `r`, `u`, `d`, `q` thì gõ tên nó sẽ chạy **lệnh pdb** chứ không in biến. Dùng `p n` hoặc `!n` để nói rõ ý.\n\n### Breakpoint điều kiện — tính năng đáng giá nhất\n\n```python\nfor i, don in enumerate(danh_sach):     # 5.000 đơn\n    xu_ly(don)                           # lỗi ở đơn thứ 4.782\n```\n\n```\n❌ Bấm continue 4.782 lần\n❌ print mọi đơn rồi cuộn terminal tìm\n\n✓ Breakpoint ĐIỀU KIỆN: don.ma == \"DH4782\"\n  → chạy một lần, dừng đúng chỗ\n```\n\nTrong VS Code: chuột phải vào chấm đỏ → *Edit Breakpoint* → nhập biểu thức.\n\nTrong `pdb`:\n\n```\n(Pdb) b xu_ly.py:45, don.ma == \"DH4782\"\n(Pdb) c\n```\n\nBa loại breakpoint nâng cao trong VS Code:\n\n```\nĐIỀU KIỆN (Expression)     dừng khi biểu thức đúng\nSỐ LẦN (Hit Count)          dừng ở lần chạm thứ N\nLOGPOINT                    KHÔNG dừng, chỉ in — \"print không sửa code\"\n```\n\n**Logpoint** đáng chú ý: nó cho bạn đúng cái lợi của `print` (thấy dòng chảy giá trị theo thời gian) mà **không sửa file**, nên không có gì để quên xoá.\n\n```\nLogpoint message:  Đơn {don.ma}: tổng {don.tong}\n```\n\n### Post-mortem — khám nghiệm sau khi chết\n\nTình huống: chương trình chạy 20 phút rồi ném lỗi. Chạy lại với breakpoint tốn thêm 20 phút nữa.\n\n```bash\n# Chạy dưới pdb: khi ném lỗi, TỰ ĐỘNG vào chế độ post-mortem\npython -m pdb -c continue main.py\n```\n\n```\nTraceback (most recent call last):\n  File \"main.py\", line 45, in xu_ly\n    return tong / so_luong\nZeroDivisionError: division by zero\n\n(Pdb) p so_luong          ⭐ xem được biến TẠI thời điểm chết\n0\n(Pdb) u                    ⭐ lên khung trên xem vì sao nó bằng 0\n(Pdb) p don.muc\n[]\n# → đơn không có mục hàng nào\n```\n\nTrong một phiên tương tác hoặc notebook:\n\n```python\nimport pdb\ntry:\n    ham_loi()\nexcept Exception:\n    pdb.post_mortem()       # khám nghiệm ngay tại chỗ ném lỗi\n\n# Hoặc sau khi đã thấy traceback trong REPL:\nimport pdb; pdb.pm()\n```\n\nGiá trị cốt lõi: **traceback cho bạn biết lỗi ở đâu; post-mortem cho bạn biết vì sao.** Traceback nói `so_luong` bằng 0; post-mortem cho bạn đi ngược lên và thấy đơn hàng rỗng.\n\nTrong VS Code, bật *Uncaught Exceptions* trong khung BREAKPOINTS làm điều tương tự tự động: chương trình dừng ngay tại dòng ném lỗi, mọi biến còn nguyên.\n\n### Debug test\n\n```bash\n# pytest tự động vào pdb khi test thất bại\npytest --pdb\n\n# Dừng ngay ở đầu mỗi test (ít dùng hơn)\npytest --trace\n\n# Chỉ chạy test thất bại lần trước, dừng ở lỗi đầu tiên\npytest --lf -x --pdb\n```\n\nLệnh cuối là công thức đáng nhớ khi sửa một bộ test đang đỏ:\n\n```\n--lf   (last failed)  chỉ chạy test đã hỏng lần trước\n-x     dừng ở lỗi ĐẦU TIÊN\n--pdb  vào debugger ngay tại đó\n```\n\n⚠️ Lưu ý quan trọng: **`pytest` bắt `stdout` theo mặc định**, nên `print` trong test không hiện ra khi test **đậu**. Đây là một lý do nữa để dùng debugger.\n\n```bash\npytest -s          # tắt bắt stdout, print mới hiện\n```\n\n### Debug trong container hoặc từ xa\n\n```python\n# Trong ứng dụng chạy ở container\nimport debugpy\ndebugpy.listen((\"0.0.0.0\", 5678))\nprint(\"Chờ debugger gắn vào...\")\ndebugpy.wait_for_client()        # dừng cho tới khi VS Code gắn vào\n```\n\n```json\n// .vscode/launch.json\n{\n  \"name\": \"Gắn vào container\",\n  \"type\": \"debugpy\",\n  \"request\": \"attach\",\n  \"connect\": { \"host\": \"localhost\", \"port\": 5678 },\n  \"pathMappings\": [\n    { \"localRoot\": \"${workspaceFolder}\", \"remoteRoot\": \"/app\" }\n  ]\n}\n```\n\n`pathMappings` là chỗ hay sai: debugger cần biết file `/app/main.py` trong container **là** file `./main.py` trên máy bạn. Thiếu nó thì breakpoint không bao giờ dừng.\n\n⚠️ **Không bao giờ để cổng debug mở trên production** — nó cho phép chạy code tuỳ ý trong tiến trình của bạn.\n\n### Khi nào `print` vẫn hợp lý\n\nPhản biện cần thiết — nguyên tắc \"bỏ thói quen print\" cũng bị áp máy móc:\n\n```\nprint VẪN HỢP LÝ khi:\n· Chỉ cần xem MỘT giá trị ở MỘT chỗ, biết chắc chỗ nào\n· Theo dõi DÒNG CHẢY giá trị qua nhiều vòng lặp (nhìn xu hướng)\n· Code chạy ở nơi không gắn debugger được\n· Debug bất đồng bộ / đa luồng — debugger làm đổi thời điểm, che lỗi\n· Cần bản ghi để SO SÁNH giữa hai lần chạy\n\nDEBUGGER tốt hơn hẳn khi:\n· Chưa biết vấn đề ở đâu\n· Cần xem NHIỀU biến, hoặc biến ở khung KHÁC\n· Bug ở vòng lặp thứ N\n· Chương trình khởi động chậm (mỗi vòng print tốn nhiều phút)\n· Muốn ĐỌC HIỂU code lạ\n```\n\nDòng \"debug bất đồng bộ / đa luồng\" là điểm quan trọng: **dừng một luồng làm thay đổi thời điểm tương tác giữa các luồng**, nên bug điều kiện tranh chấp có thể biến mất khi bạn đặt breakpoint — hiện tượng gọi là *heisenbug*. Với loại lỗi đó, logging là công cụ đúng.\n\n### Debugger, logging, và test — ba việc khác nhau\n\n```\nDEBUGGER   lỗi TÁI HIỆN ĐƯỢC, trên máy bạn, ngay bây giờ\nLOGGING    lỗi xảy ra lúc 3h sáng trên production, không tái hiện được\nTEST       ngăn lỗi QUAY LẠI sau khi đã sửa\n```\n\nQuy trình đầy đủ khi gặp bug — thứ tự này quan trọng:\n\n```\n① TÁI HIỆN được lỗi (nếu không thì đọc log để hiểu điều kiện)\n② Viết TEST tái hiện lỗi → test này phải ĐỎ\n③ Dùng DEBUGGER tìm nguyên nhân\n④ Sửa → test xanh\n⑤ Test ở lại vĩnh viễn, ngăn lỗi quay lại\n```\n\nBước ② trước bước ③ là điều nhiều người làm ngược. Viết test trước cho bạn ba thứ: một cách tái hiện lỗi **nhanh** (không phải bấm qua giao diện), bằng chứng bạn đã hiểu đúng lỗi, và lưới an toàn vĩnh viễn.\n\n### `ipdb` — bản pdb dễ dùng hơn\n\n```bash\npip install ipdb\n```\n\n```python\nimport ipdb; ipdb.set_trace()\n\n# Hoặc đặt mặc định cho breakpoint(), không sửa code:\n# PYTHONBREAKPOINT=ipdb.set_trace python main.py\n```\n\n```\nipdb thêm so với pdb:\n· Tô màu cú pháp\n· Tự hoàn thành bằng Tab\n· Lịch sử lệnh giữa các phiên\n· Hiện nhiều dòng ngữ cảnh hơn\n\nLệnh GIỐNG HỆT pdb — học pdb là dùng được cả hai.\n```\n\nMột chi tiết cần biết cho môi trường thật: **`pdb` có sẵn trong Python, `ipdb` phải cài.** Khi debug trong container tối giản hoặc trên máy chủ lạ, `pdb` là thứ chắc chắn có.",

vi_du: "### Cùng một bug — hai cách tiếp cận\n\n```python\n# Bug: hàm này thỉnh thoảng trả về sai tổng tiền\ndef tinh_tong_don(don_hang: list[DonHang]) -> float:\n    tong = 0\n    for don in don_hang:\n        gia = don.gia_goc\n        if don.khuyen_mai:\n            gia = gia * (1 - don.khuyen_mai.ti_le)\n        if don.khach.la_vip:\n            gia = gia * 0.9\n        tong += gia\n    return tong\n```\n\n**Cách `print` — bốn vòng sửa-chạy-đọc:**\n\n```python\n# Vòng 1\ndef tinh_tong_don(don_hang):\n    tong = 0\n    for don in don_hang:\n        gia = don.gia_goc\n        print(f\"gia goc: {gia}\")          # ⚠️ 5.000 dòng ra terminal\n        ...\n\n# Vòng 2: quá nhiều output, thêm điều kiện\n        if don.ma == \"DH4782\":\n            print(f\"gia goc: {gia}\")\n\n# Vòng 3: thấy giá gốc đúng, cần xem khuyến mãi\n        if don.ma == \"DH4782\":\n            print(f\"gia goc: {gia}, km: {don.khuyen_mai}\")\n\n# Vòng 4: cần xem cả ti_le, và vip\n        if don.ma == \"DH4782\":\n            print(f\"gia: {gia}, km: {don.khuyen_mai}, \"\n                  f\"ti_le: {don.khuyen_mai.ti_le if don.khuyen_mai else None}, \"\n                  f\"vip: {don.khach.la_vip}\")\n\n# → 4 lần sửa file, 4 lần chạy lại. Chương trình khởi động 40s\n#   ⇒ khoảng 15 phút, chưa kể thời gian suy nghĩ.\n```\n\n**Cách debugger — một lần chạy:**\n\n```\n① Đặt breakpoint ở dòng \"gia = don.gia_goc\"\n② Chuột phải → Edit Breakpoint → điều kiện:  don.ma == \"DH4782\"\n③ Chạy debug\n```\n\n```\n(Pdb) p don.gia_goc\n1000000\n(Pdb) p don.khuyen_mai\nKhuyenMai(ma='TET2026', ti_le=0.2)\n(Pdb) p don.khach.la_vip\nTrue\n(Pdb) n\n(Pdb) n\n(Pdb) p gia\n800000.0\n(Pdb) n\n(Pdb) p gia\n720000.0                    ⭐ áp CẢ HAI giảm giá — đúng nghiệp vụ chưa?\n\n(Pdb) u                     ⭐ lên khung gọi xem ngữ cảnh\n(Pdb) p len(don_hang)\n5000\n```\n\nMột lần chạy, xem được **mọi** thứ, kể cả ở khung hàm gọi. Và phát hiện ra vấn đề thật: giảm giá **chồng nhau** — câu hỏi nghiệp vụ mà `print` theo kịch bản định sẵn không đưa bạn tới.\n\n### Di chuyển giữa các khung — thứ `print` không làm được\n\n```python\ndef api_tao_don(yeu_cau: dict):              # khung 1\n    don = dung_don(yeu_cau)\n    return dich_vu_dat_hang(don)\n\ndef dich_vu_dat_hang(don: DonHang):          # khung 2\n    kiem_tra(don)\n    return tinh_va_luu(don)\n\ndef tinh_va_luu(don: DonHang):               # khung 3\n    tong = tinh_tong(don.muc)\n    return luu(don, tong)\n\ndef tinh_tong(muc: list[MucHang]):           # khung 4 — lỗi ở đây\n    breakpoint()\n    return sum(m.gia * m.so_luong for m in muc)\n```\n\n```\n(Pdb) w                          ⭐ toàn bộ đường đi tới đây\n  api/don.py(12)api_tao_don()\n-> return dich_vu_dat_hang(don)\n  dich_vu/dat_hang.py(23)dich_vu_dat_hang()\n-> return tinh_va_luu(don)\n  dich_vu/dat_hang.py(31)tinh_va_luu()\n-> tong = tinh_tong(don.muc)\n> dich_vu/tinh.py(8)tinh_tong()\n-> return sum(m.gia * m.so_luong for m in muc)\n\n(Pdb) p muc\n[]                               # rỗng — nhưng VÌ SAO?\n\n(Pdb) u                          ⭐ lên khung tinh_va_luu\n(Pdb) p don.muc\n[]                               # đơn đã rỗng từ đây\n\n(Pdb) u                          ⭐ lên nữa\n(Pdb) p don\nDonHang(ma='DH001', muc=[])\n\n(Pdb) u                          ⭐ lên tận khung API\n(Pdb) pp yeu_cau\n{'ma': 'DH001', 'items': [{'sku': 'A1', 'sl': 2}]}\n                    ↑ ⭐ TÌM RA: JSON dùng khoá \"items\",\n                      dung_don() đọc khoá \"muc\" → luôn rỗng\n```\n\nBốn lệnh `u`, không sửa một dòng code nào, và tìm ra nguyên nhân nằm cách chỗ lỗi **bốn tầng**. Với `print` bạn phải thêm lệnh in vào cả bốn hàm, chạy lại bốn lần.\n\n### Breakpoint điều kiện và logpoint\n\n```python\n# Xử lý 50.000 bản ghi, lỗi ở một số bản ghi không rõ\nfor i, ban_ghi in enumerate(du_lieu):\n    ket_qua = chuyen_doi(ban_ghi)\n    luu(ket_qua)\n```\n\n```\n① ĐIỀU KIỆN — dừng khi gặp dữ liệu bất thường\n   Breakpoint condition:  ban_ghi.get(\"so_tien\") is None\n\n② SỐ LẦN — chương trình chậm dần sau ~10.000 bản ghi\n   Hit count:  > 10000\n\n③ LOGPOINT — theo dõi mà KHÔNG dừng, KHÔNG sửa code\n   Log message:  [{i}] {ban_ghi.get('ma')} -> {ket_qua.trang_thai}\n```\n\nLogpoint là lựa chọn tốt nhất cho trường hợp ③ vì nó cho đúng cái lợi của `print` — nhìn dòng chảy giá trị theo thời gian — mà không để lại gì trong file.\n\nTương đương trong `pdb`:\n\n```\n(Pdb) b xu_ly.py:34, ban_ghi.get(\"so_tien\") is None\n(Pdb) c\n\n(Pdb) b                    xem danh sách breakpoint\nNum Type         Disp Enb   Where\n1   breakpoint   keep yes   at xu_ly.py:34\n        stop only if ban_ghi.get(\"so_tien\") is None\n        breakpoint already hit 1 time\n\n(Pdb) disable 1            tắt tạm\n(Pdb) cl 1                 xoá hẳn\n```\n\n### Post-mortem — chương trình chạy 20 phút rồi chết\n\n```bash\n$ python xu_ly_du_lieu.py\n... chạy 20 phút ...\nTraceback (most recent call last):\n  File \"xu_ly_du_lieu.py\", line 87, in <module>\n    main()\n  File \"xu_ly_du_lieu.py\", line 62, in tong_hop\n    ti_le = dat / tong_so\nZeroDivisionError: division by zero\n```\n\n```bash\n# ❌ Thêm breakpoint rồi chạy lại → mất thêm 20 phút\n\n# ✓ Chạy dưới pdb, tự dừng tại chỗ ném lỗi\n$ python -m pdb -c continue xu_ly_du_lieu.py\n```\n\n```\nZeroDivisionError: division by zero\n> xu_ly_du_lieu.py(62)tong_hop()\n-> ti_le = dat / tong_so\n\n(Pdb) p tong_so\n0\n(Pdb) p dat\n0\n(Pdb) u\n(Pdb) p len(ban_ghi)\n0                              ⭐ không có bản ghi nào lọt qua bộ lọc\n(Pdb) p dieu_kien_loc\n{'tu_ngay': '2026-13-01'}      ⭐ THÁNG 13 — dữ liệu đầu vào sai\n```\n\nTraceback chỉ nói \"chia cho 0\". Post-mortem cho bạn đi ngược lên và tìm ra nguyên nhân thật cách đó hai tầng: một ngày tháng không hợp lệ khiến bộ lọc không khớp bản ghi nào.\n\nTrong notebook hoặc REPL:\n\n```python\n>>> xu_ly(du_lieu)\nZeroDivisionError: division by zero\n\n>>> import pdb; pdb.pm()       # ⭐ khám nghiệm ngay, không chạy lại\n(Pdb) p tong_so\n0\n```\n\n### Debug một test đang đỏ\n\n```python\n# tests/test_don_hang.py\ndef test_tinh_tong_co_khuyen_mai():\n    don = DonHang(\n        muc=[MucHang(gia=100_000, so_luong=2)],\n        khuyen_mai=KhuyenMai(ti_le=0.2),\n    )\n    assert tinh_tong(don) == 160_000        # thực tế: 144_000\n```\n\n```bash\n$ pytest tests/test_don_hang.py --pdb\n```\n\n```\n>       assert tinh_tong(don) == 160_000\nE       assert 144000.0 == 160000\n\n>>> entering PDB >>>\n(Pdb) p don.muc[0].gia * don.muc[0].so_luong\n200000\n(Pdb) p don.khuyen_mai.ti_le\n0.2\n(Pdb) p 200000 * (1 - 0.2)\n160000.0                       ⭐ công thức đúng phải ra 160.000\n\n(Pdb) s                        bước VÀO tinh_tong xem nó làm gì\n(Pdb) ll\n 12  def tinh_tong(don):\n 13      tong = sum(m.gia * m.so_luong for m in don.muc)\n 14      if don.khuyen_mai:\n 15 ->        tong = tong * (1 - don.khuyen_mai.ti_le)\n 16      if don.khach and don.khach.la_vip:\n 17          tong = tong * 0.9\n 18      return tong\n\n(Pdb) p don.khach\nKhach(la_vip=True)             ⭐ TÌM RA: test quên rằng khách mặc định là VIP\n                                  → 160.000 × 0.9 = 144.000\n```\n\nKết luận thú vị: **code không sai, test sai.** Đây là loại phát hiện mà chỉ debugger mới cho nhanh — `print` trong test còn bị `pytest` nuốt mất nếu test đậu.\n\nCông thức khi sửa một bộ test đỏ:\n\n```bash\npytest --lf -x --pdb\n#      --lf  chỉ chạy test hỏng lần trước\n#      -x    dừng ở lỗi đầu tiên\n#      --pdb vào debugger ngay tại đó\n```\n\n### `interact` — khám phá dữ liệu tự do\n\n```\n(Pdb) p du_lieu\n<generator object doc_csv at 0x7f8b...>      # không xem được gì\n\n(Pdb) interact                                ⭐ REPL đầy đủ\n>>> mau = list(itertools.islice(du_lieu, 5))\n>>> for d in mau:\n...     print(d.keys())\ndict_keys(['ma', 'ten', 'so_tien'])\n>>> [d for d in mau if d['so_tien'] is None]\n[{'ma': 'A3', 'ten': 'X', 'so_tien': None}]   ⭐ tìm ra bản ghi hỏng\n>>> ^D\n(Pdb)\n```\n\nTrong `pdb` thuần bạn chỉ chạy được biểu thức đơn; `interact` cho bạn viết vòng lặp, import thêm module, thử nghiệm thoải mái — rồi quay lại phiên debug với trạng thái nguyên vẹn.\n\n### Bước vào code thư viện để ĐỌC\n\n```json\n// .vscode/launch.json\n{\n  \"name\": \"Debug cả thư viện\",\n  \"type\": \"debugpy\",\n  \"request\": \"launch\",\n  \"program\": \"${file}\",\n  \"justMyCode\": false        // ⭐ cho phép bước vào requests, pandas...\n}\n```\n\n```python\nimport requests\n\nbreakpoint()\nr = requests.get(\"https://example.com\")\n# (Pdb) s   → bước vào requests/api.py\n# (Pdb) s   → vào sessions.py, thấy cách nó dựng Request, áp adapter...\n```\n\nĐây chính là kỹ thuật *dùng debugger để đọc* ở bài module 8, giờ có cấu hình cụ thể: **đọc thư viện bằng cách đi theo dữ liệu thật xuyên qua nó**, thay vì đọc code tĩnh và đoán.\n\n⚠️ Đặt lại `justMyCode: true` cho công việc hằng ngày — nếu không bạn sẽ lạc vào hàng chục tầng framework mỗi lần bấm step.\n\n### Debug trong Docker\n\n```python\n# main.py\nimport os\n\nif os.environ.get(\"CHO_DEBUG\") == \"1\":\n    import debugpy\n    debugpy.listen((\"0.0.0.0\", 5678))\n    print(\"Chờ debugger gắn vào cổng 5678...\")\n    debugpy.wait_for_client()\n```\n\n```yaml\n# docker-compose.yml\nservices:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n      - \"5678:5678\"          # ⭐ mở cổng debug\n    environment:\n      CHO_DEBUG: \"1\"\n    volumes:\n      - .:/app               # ⭐ để sửa code không phải build lại\n```\n\n```json\n// .vscode/launch.json\n{\n  \"name\": \"Gắn vào container\",\n  \"type\": \"debugpy\",\n  \"request\": \"attach\",\n  \"connect\": { \"host\": \"localhost\", \"port\": 5678 },\n  \"pathMappings\": [\n    { \"localRoot\": \"${workspaceFolder}\", \"remoteRoot\": \"/app\" }\n  ]\n}\n```\n\n`pathMappings` là chỗ sai phổ biến nhất: nếu VS Code không biết `/app/main.py` trong container tương ứng với `./main.py` trên máy bạn, breakpoint sẽ **không bao giờ dừng** và không có thông báo lỗi nào.\n\n⚠️ Biến `CHO_DEBUG` phải mặc định tắt, và **không bao giờ bật trên production** — cổng debug cho phép chạy code tuỳ ý trong tiến trình.\n\n### Quy trình đầy đủ khi gặp bug\n\n```python\n# ① Báo cáo lỗi: \"đơn của khách VIP tính sai tiền\"\n\n# ② VIẾT TEST TÁI HIỆN TRƯỚC — test này phải ĐỎ\ndef test_don_vip_co_khuyen_mai_khong_cong_don_giam_gia():\n    \"\"\"Khách VIP + mã khuyến mãi: chỉ áp mức giảm CAO NHẤT.\n\n    Theo quy định của bộ phận kinh doanh 08/2026.\n    \"\"\"\n    don = DonHang(\n        muc=[MucHang(gia=1_000_000, so_luong=1)],\n        khuyen_mai=KhuyenMai(ti_le=0.2),\n        khach=Khach(la_vip=True),          # VIP giảm 10%\n    )\n    assert tinh_tong(don) == 800_000       # chỉ áp 20%, KHÔNG cộng dồn\n```\n\n```bash\n# ③ DEBUGGER tìm nguyên nhân\n$ pytest tests/test_don.py::test_don_vip_co_khuyen_mai --pdb\n```\n\n```\nE       assert 720000.0 == 800000\n(Pdb) ll\n 14      if don.khuyen_mai:\n 15          tong = tong * (1 - don.khuyen_mai.ti_le)     # 800.000\n 16      if don.khach and don.khach.la_vip:\n 17 ->        tong = tong * 0.9                            # 720.000 ⚠️ CỘNG DỒN\n```\n\n```python\n# ④ SỬA — áp mức giảm cao nhất, không cộng dồn\nTI_LE_GIAM_VIP = 0.10\n\n\ndef tinh_tong(don: DonHang) -> float:\n    tien_hang = sum(m.gia * m.so_luong for m in don.muc)\n    return tien_hang * (1 - _ti_le_giam_cao_nhat(don))\n\n\ndef _ti_le_giam_cao_nhat(don: DonHang) -> float:\n    \"\"\"Chỉ áp MỘT mức giảm — mức có lợi nhất cho khách.\n\n    Quy định kinh doanh 08/2026: các chương trình giảm giá KHÔNG cộng dồn.\n    \"\"\"\n    cac_muc = [0.0]\n    if don.khuyen_mai:\n        cac_muc.append(don.khuyen_mai.ti_le)\n    if don.khach and don.khach.la_vip:\n        cac_muc.append(TI_LE_GIAM_VIP)\n    return max(cac_muc)\n```\n\n```bash\n# ⑤ Test xanh, và ở lại vĩnh viễn\n$ pytest -q\n1 passed\n```\n\nChú ý bản sửa áp mấy nguyên tắc của module 8: hằng số có tên, hàm nhỏ tách riêng, và **comment giải thích *vì sao*** (quy định kinh doanh không cộng dồn) — thứ mà code không tự nói được và người sau rất dễ \"sửa cho nhất quán\" thành cộng dồn trở lại.",

so_sanh: "### `print`, debugger, logging\n\n| | `print` | Debugger | Logging |\n|---|---|---|---|\n| Phải đoán trước xem gì | **✓ có** | ✗ | ✓ |\n| Xem biến ở khung khác | **✗** | **✓ `u`/`d`** | ✗ |\n| Thấy ngăn xếp gọi | ✗ | **✓ `w`** | một phần |\n| Dừng có điều kiện | thủ công | **✓** | ✗ |\n| Thay đổi giá trị lúc chạy | ✗ | **✓** | ✗ |\n| Dùng được trên production | ✗ | **✗ nguy hiểm** | **✓** |\n| Lỗi không tái hiện được | ✗ | ✗ | **✓** |\n| Sót lại trong code | **✓ hay bị** | ✗ (dùng chấm đỏ) | — |\n\n### `n` và `s`\n\n| | `n` (next) | `s` (step) |\n|---|---|---|\n| Vào trong hàm được gọi | **✗** | **✓** |\n| Dùng khi | quét nhanh qua dòng | đã nghi ngờ đúng dòng đó |\n| Rủi ro | bỏ sót chỗ lỗi | **lạc vào thư viện** |\n\nQuy tắc: `n` cho tới khi thấy dòng nghi ngờ, rồi `s` vào đúng dòng đó. `r` để thoát nhanh khi lỡ vào hàm dài.\n\n### Ba loại breakpoint nâng cao\n\n| Loại | Dừng khi | Dùng cho |\n|---|---|---|\n| Điều kiện | biểu thức đúng | **bug ở bản ghi thứ N** |\n| Số lần (hit count) | chạm lần thứ N | chậm dần sau N vòng |\n| **Logpoint** | **không dừng**, chỉ in | theo dõi dòng chảy, **không sửa code** |\n\nLogpoint cho đúng cái lợi của `print` mà không để lại gì trong file.\n\n### Cách đặt điểm dừng\n\n| Cách | Sửa file | Dùng khi |\n|---|---|---|\n| Chấm đỏ trong VS Code | **✗** | **hằng ngày** |\n| `breakpoint()` | ✓ | container, SSH, không có GUI |\n| `pdb.set_trace()` | ✓ | tương đương, cách cũ |\n| `python -m pdb -c continue` | ✗ | **post-mortem** |\n| `pytest --pdb` | ✗ | debug test đỏ |\n\n### `pdb` và `ipdb`\n\n| | `pdb` | `ipdb` |\n|---|---|---|\n| Có sẵn trong Python | **✓** | ✗ phải cài |\n| Tô màu, hoàn thành Tab | ✗ | **✓** |\n| Lệnh | — | **giống hệt pdb** |\n| Dùng khi | container tối giản, máy chủ lạ | máy phát triển |\n\nHọc `pdb` là dùng được cả hai — và `pdb` là thứ chắc chắn có ở mọi nơi.\n\n### `justMyCode`\n\n| | `true` | `false` |\n|---|---|---|\n| Bước vào thư viện | ✗ | **✓** |\n| Lạc vào framework | ✗ | **✓ rủi ro** |\n| Dùng khi | **hằng ngày** | đọc thư viện, nghi ngờ nó |\n\n### Khi nào `print` vẫn đúng\n\n| Tình huống | Công cụ đúng | Vì sao |\n|---|---|---|\n| Xem một giá trị, biết chắc chỗ | `print` | nhanh hơn mở debugger |\n| Nhìn xu hướng qua nhiều vòng | `print` / logpoint | cần **dãy** giá trị |\n| **Đa luồng, bất đồng bộ** | **logging** | dừng luồng làm **đổi thời điểm**, che lỗi |\n| So sánh hai lần chạy | `print` ra file | cần bản ghi để diff |\n| Chưa biết lỗi ở đâu | **debugger** | không phải đoán trước |\n| Bug ở vòng lặp thứ N | **debugger** | breakpoint điều kiện |\n\nHàng đa luồng là điểm quan trọng: breakpoint có thể làm bug điều kiện tranh chấp **biến mất** (heisenbug).\n\n### Ba công cụ, ba việc\n\n| | Debugger | Logging | Test |\n|---|---|---|---|\n| Lỗi tái hiện được | **✓** | — | ✓ |\n| Lỗi trên production lúc 3h sáng | ✗ | **✓** | ✗ |\n| Ngăn lỗi quay lại | ✗ | ✗ | **✓** |\n| Thời điểm dùng | đang tìm nguyên nhân | sau khi đã xảy ra | trước và sau khi sửa |",

loi_thuong_gap: [
  "**Chỉ dùng `print` cho mọi thứ.** Mỗi lần đoán sai là một vòng sửa-chạy-đọc; với chương trình khởi động chậm thì rất tốn.",
  "**Không biết `u`/`d` để đi giữa các khung.** Đây là khả năng `print` hoàn toàn không có.",
  "**Dùng `s` cho mọi dòng.** Lạc vào hàm dựng của thư viện chuẩn rất nhanh; `n` trước, `s` đúng chỗ nghi ngờ.",
  "**Không biết `r` để thoát khỏi hàm dài đã lỡ bước vào.**",
  "**Không dùng breakpoint điều kiện.** Bấm continue hàng nghìn lần thay vì đặt một biểu thức.",
  "**Không biết logpoint.** Vẫn thêm `print` vào file để theo dõi dòng chảy giá trị.",
  "**Chạy lại chương trình 20 phút thay vì dùng post-mortem.** `python -m pdb -c continue` cho xem trạng thái tại thời điểm chết.",
  "**Đặt tên biến trùng lệnh pdb** (`n`, `s`, `c`, `l`, `p`, `a`, `r`). Gõ tên biến sẽ chạy lệnh; dùng `p n` hoặc `!n`.",
  "**Quên `pytest -s`** khi cần thấy `print` trong test — pytest bắt stdout theo mặc định.",
  "**Để `justMyCode: false` cho công việc hằng ngày.** Lạc vào hàng chục tầng framework mỗi lần step.",
  "**Quên `pathMappings` khi debug container.** Breakpoint không bao giờ dừng và không có thông báo lỗi nào.",
  "**Để cổng debug mở trên production.** Cho phép chạy code tuỳ ý trong tiến trình — lỗ hổng nghiêm trọng.",
  "**Dùng debugger cho bug đa luồng.** Dừng một luồng làm đổi thời điểm, bug có thể biến mất (heisenbug) — dùng logging.",
  "**Sửa bug mà không viết test tái hiện trước.** Mất cách tái hiện nhanh, mất bằng chứng đã hiểu đúng, và lỗi có thể quay lại.",
  "**Quên xoá `breakpoint()` trước khi commit.** Dùng chấm đỏ của trình soạn thảo thì không có gì để quên.",
  "**Tưởng debugger thay được logging.** Debugger cho lỗi tái hiện được trên máy bạn; logging cho lỗi lúc 3h sáng trên production."
],

tu_kiem_tra: [
  "Nêu ba cách đặt điểm dừng và ưu nhược của mỗi cách.",
  "`PYTHONBREAKPOINT` dùng để làm gì? Nêu hai giá trị hữu ích.",
  "Phân biệt `n`, `s`, `r`, `c`. Quy tắc thực dụng khi dùng `n` và `s`?",
  "`u` và `d` làm gì? Vì sao đây là khả năng `print` không có?",
  "`w` cho biết điều gì? Vì sao nó quan trọng khi đọc code lạ?",
  "`interact` khác việc gõ biểu thức trong pdb thế nào?",
  "Breakpoint điều kiện giải quyết vấn đề gì? Cho ví dụ cụ thể.",
  "Logpoint là gì? Nó tốt hơn `print` ở điểm nào?",
  "Post-mortem là gì? Nêu hai cách vào chế độ đó.",
  "Traceback và post-mortem khác nhau thế nào về thông tin cung cấp?",
  "Công thức `pytest --lf -x --pdb` làm gì? Từng cờ có tác dụng gì?",
  "Vì sao `print` trong test có thể không hiện ra? Cách khắc phục?",
  "`justMyCode` là gì? Khi nào đặt `false`, khi nào phải đặt lại `true`?",
  "Debug trong container cần hai cấu hình then chốt nào? Chỗ nào hay sai nhất?",
  "Nêu năm tình huống `print` vẫn hợp lý hơn debugger.",
  "Heisenbug là gì? Vì sao debugger không hợp với bug đa luồng?",
  "Debugger, logging, test khác nhau về mục đích thế nào?",
  "Nêu năm bước của quy trình sửa bug. Vì sao viết test TRƯỚC khi debug?"
],

tai_nguyen: [
  "**Tài liệu module `pdb` của Python** — danh sách đầy đủ các lệnh và cú pháp breakpoint điều kiện; đây là nguồn chính xác nhất.",
  "**Tài liệu `debugpy`** — cấu hình `listen`, `wait_for_client`, và debug từ xa; cần khi làm việc với container.",
  "**Tài liệu VS Code mục *Debugging*** — breakpoint điều kiện, logpoint, `launch.json`, và các thuộc tính cấu hình.",
  "**Tài liệu pytest** — mục về `--pdb`, `--trace`, `--lf`, và cơ chế bắt stdout.",
  "Sách **Debugging** — David J. Agans. Không nói về công cụ mà về *phương pháp* tìm lỗi; chín quy tắc trong đó áp dụng cho mọi ngôn ngữ."
],

lien_he: [
  "**Đọc và review code người khác** ở module 8: debugger là công cụ đọc mạnh nhất; bài này bổ sung phần sửa lỗi.",
  "**VS Code** ngay trước: `launch.json`, `justMyCode`, và cách đặt breakpoint bằng chấm đỏ.",
  "**Xử lý lỗi tường minh** ở module 8: thông báo lỗi tốt giảm hẳn nhu cầu mở debugger; post-mortem bổ sung cho traceback.",
  "**Stack và heap** ở module 2: khung ngăn xếp là khái niệm mà `w`, `u`, `d` thao tác trên đó.",
  "**Process và thread** ở module 2: vì sao debugger không hợp với bug đa luồng — dừng luồng làm đổi thời điểm.",
  "**Nhận diện code smell và refactor an toàn** ở module 8: quy trình \"viết test tái hiện trước, rồi mới debug\" là cùng nguyên tắc lưới an toàn.",
  "**Xử lý ngoại lệ** ở module 5: traceback và cây exception là thứ post-mortem khai thác.",
  "**Docker cơ bản** trong module này: `debugpy` và `pathMappings` để debug code chạy trong container.",
  "**Pre-commit hook** trong module này: hook bắt được `breakpoint()` quên xoá trước khi commit.",
  "**Testing** ở mảng 05: `pytest --pdb`, fixture, và quy trình test-đỏ-rồi-sửa.",
  "**Observability** ở mảng 06: logging có cấu trúc là công cụ cho lỗi trên production mà debugger không với tới."
]

};
