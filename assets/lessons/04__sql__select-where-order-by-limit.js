window.LESSONS["04:sql:select-where-order-by-limit"] = {

tom_tat: [
  "`SELECT` xác định các biểu thức tạo nên từng dòng kết quả; nó không mặc nhiên có nghĩa “lấy nguyên dòng trong bảng”.",
  "`WHERE` loại dòng trước khi sắp xếp và giới hạn, còn giá trị `NULL` làm điều kiện rơi vào logic ba trạng thái thay vì chỉ đúng/sai.",
  "`ORDER BY` là thứ duy nhất bảo đảm thứ tự kết quả; muốn phân trang ổn định phải thêm khóa phân xử duy nhất như `order_id`.",
  "`LIMIT` chỉ cắt số dòng trả về, không tự làm truy vấn rẻ; index phù hợp có thể giúp PostgreSQL dừng sớm thay vì lọc và sắp xếp toàn bảng.",
  "Thứ tự viết câu lệnh khác thứ tự xử lý logic, nên alias trong `SELECT` thường chưa dùng được ở `WHERE` nhưng dùng được ở `ORDER BY`."
],

can_biet_truoc: [
  "Khái niệm bảng, dòng, cột, khóa chính và kiểu dữ liệu trong mô hình quan hệ.",
  "Cú pháp literal của PostgreSQL: chuỗi đặt trong nháy đơn, ngày có thể viết `DATE '2026-09-01'`, số không đặt trong nháy.",
  "Ý nghĩa cơ bản của index B-tree và việc PostgreSQL đọc dữ liệu theo page, đủ để hiểu `Index Scan` và `Sort`.",
  "Giá trị `NULL` biểu diễn chưa biết hoặc không áp dụng; nó không phải số 0 hay chuỗi rỗng."
],

dinh_nghia: "Một truy vấn `SELECT` là biểu thức tạo **quan hệ kết quả** từ dữ liệu nguồn. Trong nhóm cú pháp của bài này, `FROM` chọn nguồn, `WHERE` giữ các dòng thỏa điều kiện, `SELECT` tính các cột cần trả, `ORDER BY` áp đặt thứ tự quan sát được, và `LIMIT` giữ một số dòng đầu của thứ tự đó.\n\n```sql\nSELECT order_id, customer_id, total_amount\nFROM orders\nWHERE status = 'paid'\nORDER BY created_at DESC, order_id DESC\nLIMIT 20;\n```\n\nCâu trên không có nghĩa PostgreSQL buộc phải chạy tuần tự từ dòng đầu xuống dòng cuối. SQL mô tả **kết quả cần có**; bộ tối ưu được quyền chọn quét tuần tự, dùng index, đổi cách lọc hoặc dừng sớm miễn kết quả tương đương. Đây là ranh giới quan trọng giữa ngôn ngữ khai báo và một chương trình chỉ dẫn từng bước.",

vi_sao_can: "Màn hình “đơn hàng mới nhất đã thanh toán”, API tìm đơn theo khách và tác vụ xuất báo cáo đều bắt đầu từ bốn thao tác này. Nếu viết sai, lỗi không chỉ là cú pháp. Thiếu `ORDER BY` khiến trang đầu thay đổi theo query plan; dùng `= NULL` làm mất âm thầm mọi dòng; chọn quá nhiều cột tăng lượng page phải đọc và byte phải truyền; đặt hàm lên cột được index có thể biến một lần tìm hẹp thành quét lớn.\n\nHệ thống bán vé xuyên module có nhiều đơn đồng thời. Hai đơn có thể cùng `created_at`, một khách có thể chưa có email xác minh, và trạng thái bị giới hạn bởi nghiệp vụ. Vì vậy ví dụ nhỏ đã chứa ba vấn đề thật: thứ tự hòa, dữ liệu chưa biết và điều kiện kết hợp. Học cú pháp mà bỏ qua chúng tạo truy vấn “đúng trên máy cá nhân” nhưng rung lắc khi dữ liệu tăng.\n\nHiệu năng cũng bắt đầu ở đây. Với index `(status, created_at DESC, order_id DESC)`, PostgreSQL có thể đi thẳng vào nhánh `paid`, đọc theo đúng thứ tự cần và dừng sau 20 dòng. Không có index đó, hệ quản trị có thể phải đọc mọi đơn đã thanh toán rồi sắp xếp chúng chỉ để giữ 20 dòng. Cùng một SQL, lượng công việc khác nhau vì đường truy cập khác nhau.",

co_che: "### Schema bán vé dùng xuyên suốt\n\nCác bài sau sẽ mở rộng schema, nhưng bài này chỉ cần khách hàng, sự kiện và đơn hàng. Đoạn SQL chạy được trên PostgreSQL trong một schema trống.\n\n```sql\nCREATE TABLE customers (\n  customer_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  full_name text NOT NULL,\n  email text UNIQUE,\n  city text\n);\n\nCREATE TABLE events (\n  event_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  event_name text NOT NULL,\n  starts_at timestamptz NOT NULL,\n  venue text NOT NULL,\n  base_price numeric(12,2) NOT NULL CHECK (base_price >= 0)\n);\n\nCREATE TABLE orders (\n  order_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  customer_id bigint REFERENCES customers(customer_id),\n  status text NOT NULL CHECK (status IN ('pending','paid','cancelled','refunded')),\n  created_at timestamptz NOT NULL,\n  total_amount numeric(12,2) NOT NULL CHECK (total_amount >= 0),\n  note text\n);\n```\n\n`numeric(12,2)` được chọn cho tiền vì biểu diễn thập phân chính xác; `double precision` có sai số nhị phân không thích hợp cho phép đối soát. `customer_id` cho phép `NULL` để giữ đơn mua tại quầy chưa nhận diện khách, một ca biên sẽ ảnh hưởng tới điều kiện lọc và JOIN.\n\n### Thứ tự xử lý logic\n\nMột mô hình hữu ích là: `FROM` → `WHERE` → `SELECT` → `ORDER BY` → `LIMIT`. Đây không phải cam kết về thao tác vật lý, mà giải thích phạm vi tên và ý nghĩa kết quả.\n\n```sql\nSELECT total_amount * 0.9 AS after_discount\nFROM orders\nWHERE total_amount >= 500000\nORDER BY after_discount DESC\nLIMIT 5;\n```\n\nAlias `after_discount` tồn tại sau bước chiếu của `SELECT`, nên `ORDER BY` thấy nó. `WHERE after_discount >= ...` không hợp lệ ở cùng cấp vì bộ lọc logic xảy ra trước. Khi thật sự cần lọc biểu thức đã đặt tên, dùng subquery hoặc CTE ở bài sau; đừng sao chép biểu thức dài mà không cân nhắc khả năng lệch khi sửa.\n\n### SELECT là phép chiếu và tính toán\n\n`SELECT *` tiện khi khám phá dữ liệu, nhưng là hợp đồng API kém: thêm cột có thể làm payload lớn hơn, thay thứ tự cột, hoặc vô tình lộ `note`. Liệt kê cột làm ý định rõ và có thể cho phép index-only scan nếu mọi cột cần lấy nằm trong index và visibility map phù hợp.\n\n```sql\nSELECT order_id,\n       total_amount,\n       total_amount * 0.08 AS estimated_fee,\n       created_at::date AS order_date\nFROM orders;\n```\n\nPhép ép `created_at::date` phụ thuộc múi giờ phiên vì nguồn là `timestamptz`. Báo cáo theo ngày kinh doanh ở Việt Nam nên chỉ rõ `created_at AT TIME ZONE 'Asia/Ho_Chi_Minh'` trước khi lấy ngày. Nếu không, một đơn sát nửa đêm có thể sang ngày khác trên server UTC.\n\n### WHERE và logic ba trạng thái\n\nTrong SQL, so sánh với `NULL` không cho `TRUE` hay `FALSE` mà cho `UNKNOWN`. `WHERE` chỉ giữ `TRUE`; cả `FALSE` lẫn `UNKNOWN` đều bị loại.\n\n| Biểu thức | Khi `city = NULL` | Có được giữ? |\n|---|---:|---:|\n| `city = 'Hà Nội'` | `UNKNOWN` | không |\n| `city <> 'Hà Nội'` | `UNKNOWN` | không |\n| `city IS NULL` | `TRUE` | có |\n| `city IS NOT NULL` | `FALSE` | không |\n\nDo đó `WHERE city <> 'Hà Nội'` không bao gồm khách chưa biết thành phố. Nếu nghiệp vụ muốn “khác Hà Nội hoặc chưa khai”, phải viết `city <> 'Hà Nội' OR city IS NULL` hay dùng `IS DISTINCT FROM`, toán tử PostgreSQL coi `NULL` là giá trị có thể so sánh an toàn.\n\n### AND, OR và dấu ngoặc\n\n`AND` có độ ưu tiên cao hơn `OR`. Điều kiện “đơn paid hoặc refunded, đồng thời từ 500.000 trở lên” phải nhóm trạng thái.\n\n```sql\nWHERE status IN ('paid', 'refunded')\n  AND total_amount >= 500000\n```\n\nViết `status = 'paid' OR status = 'refunded' AND total_amount >= 500000` sẽ nhận mọi đơn paid bất kể giá. Dấu ngoặc hoặc `IN` không chỉ để đẹp; nó đóng gói luật nghiệp vụ, giúp người review không phải nhớ bảng ưu tiên toán tử.\n\n### Điều kiện có thể tận dụng index\n\nIndex B-tree sắp khóa, nên điều kiện giữ nguyên cột thường cho phép tìm một khoảng liên tiếp.\n\n```sql\nCREATE INDEX orders_created_idx ON orders (created_at);\n\n-- Có thể dùng khoảng trên index\nWHERE created_at >= TIMESTAMPTZ '2026-09-01 00:00+07'\n  AND created_at <  TIMESTAMPTZ '2026-09-02 00:00+07'\n\n-- Thường không dùng trực tiếp index ở trên\nWHERE created_at::date = DATE '2026-09-01'\n```\n\nĐặt phép ép kiểu lên từng dòng biến khóa index thành một biểu thức khác. Có thể tạo expression index đúng biểu thức, nhưng khoảng nửa mở `[đầu ngày, đầu ngày kế)` thường linh hoạt hơn và xử lý chính xác phần giây. Quy tắc không tuyệt đối: bộ tối ưu vẫn có thể chọn sequential scan khi phần lớn bảng thỏa điều kiện, vì đọc liên tục rẻ hơn nhảy giữa index và heap.\n\n### ORDER BY và thứ tự toàn phần\n\nKhông có `ORDER BY`, chuẩn SQL không hứa thứ tự. Việc lần chạy đầu trông giống khóa chính chỉ là hệ quả tạm thời của cách quét. Ngay cả `ORDER BY created_at DESC` cũng chưa ổn định nếu hai đơn cùng thời điểm. Thêm khóa chính tạo thứ tự toàn phần:\n\n```sql\nORDER BY created_at DESC, order_id DESC\n```\n\nPostgreSQL mặc định đặt `NULL` cuối với `ASC` và đầu với `DESC`; có thể ghi `NULLS FIRST` hoặc `NULLS LAST` để hợp đồng không phụ thuộc trí nhớ. Collation cũng ảnh hưởng cách sắp text; thứ tự tên người dùng không nên được dùng như định danh phân trang.\n\n### LIMIT, OFFSET và dừng sớm\n\n`LIMIT 20` có ý nghĩa chỉ sau khi biết “20 dòng nào”. Không có thứ tự, đó là 20 dòng tùy plan. Với index khớp điều kiện và thứ tự, executor có thể dừng sau đủ số dòng. Tạo index phục vụ màn hình chính:\n\n```sql\nCREATE INDEX orders_paid_feed_idx\nON orders (created_at DESC, order_id DESC)\nWHERE status = 'paid';\n```\n\nĐây là partial index: nhỏ hơn vì chỉ chứa đơn paid. Nó không giúp truy vấn refunded và tăng chi phí ghi mỗi đơn paid. Đừng tạo chỉ vì thấy một câu `ORDER BY`; hãy dựa trên workload thật.\n\n`OFFSET 100000 LIMIT 20` vẫn phải đi qua 100.020 mục rồi bỏ 100.000 mục, và dữ liệu chèn mới có thể làm trùng hoặc hụt giữa hai trang. Keyset pagination dùng khóa cuối trang trước:\n\n```sql\nWHERE status = 'paid'\n  AND (created_at, order_id) < ($1, $2)\nORDER BY created_at DESC, order_id DESC\nLIMIT 20;\n```\n\nSo sánh tuple khớp thứ tự hai cột và tiếp tục đúng vị trí. Đổi lại, giao diện khó nhảy thẳng tới “trang 500” và client phải giữ cursor.\n\n### Đọc query plan đúng mức\n\n`EXPLAIN` cho biết kế hoạch ước lượng; `EXPLAIN (ANALYZE, BUFFERS)` thật sự chạy truy vấn và báo số dòng, thời gian, page hit/read. Trên dữ liệu ít, PostgreSQL thường chọn `Seq Scan` dù có index vì đọc vài page rẻ hơn khởi động index scan. Đó không phải bằng chứng index hỏng.\n\nCác dấu hiệu cần đọc: `Sort` cho biết có bước sắp xếp riêng; `Index Scan using orders_paid_feed_idx` cho thấy index cung cấp đường đi; `Rows Removed by Filter` lớn gợi ý đang đọc nhiều rồi mới loại; chênh lớn giữa `rows=` ước lượng và `actual rows=` báo statistics chưa mô tả tốt phân bố. Không chép thời gian plan từ máy khác thành benchmark: cache, phần cứng và dữ liệu làm con số đổi mạnh.\n\n### Khi nào không nên tối ưu bằng index\n\nIndex không miễn phí. Nó chiếm đĩa, làm `INSERT`, `UPDATE`, `DELETE` phải bảo trì thêm và có thể không được chọn khi điều kiện trả phần lớn bảng. Bảng vài nghìn dòng đọc hiếm, tác vụ xuất toàn bộ dữ liệu, hoặc cột trạng thái có rất ít giá trị thường không đáng một index đơn cột. Index ghép hoặc partial index chỉ đáng khi hình dạng truy vấn quan trọng lặp lại đủ nhiều. Ưu tiên tính đúng và đo bằng plan trên dữ liệu đại diện trước.",

vi_du: "### Dữ liệu nhỏ có chủ ý\n\n```sql\nINSERT INTO customers(full_name,email,city) VALUES\n('An','an@example.com','Hà Nội'),\n('Bình','binh@example.com','Đà Nẵng'),\n('Chi',NULL,NULL);\n\nINSERT INTO events(event_name,starts_at,venue,base_price) VALUES\n('Đêm Jazz', '2026-09-10 20:00+07','Nhà hát A',450000),\n('Tech Day', '2026-09-12 09:00+07','Trung tâm B',700000);\n\nINSERT INTO orders(customer_id,status,created_at,total_amount,note) VALUES\n(1,'paid',    '2026-09-01 09:00:00+07',900000,NULL),\n(2,'paid',    '2026-09-01 09:00:00+07',700000,'xuất hóa đơn'),\n(3,'pending', '2026-09-01 10:00:00+07',450000,NULL),\n(NULL,'paid', '2026-09-01 11:00:00+07',450000,'mua tại quầy'),\n(1,'refunded','2026-08-31 21:00:00+07',900000,NULL);\n```\n\nHai đơn paid cùng timestamp cố ý kiểm tra khóa phân xử. Truy vấn feed:\n\n```sql\nSELECT order_id, customer_id, total_amount, created_at\nFROM orders\nWHERE status = 'paid'\nORDER BY created_at DESC, order_id DESC\nLIMIT 3;\n```\n\nKết quả xác định là đơn mua tại quầy trước, rồi đơn của Bình trước đơn của An vì `order_id DESC`. Hãy chạy `EXPLAIN (ANALYZE, BUFFERS)` trước và sau khi tạo partial index; với chỉ năm dòng, khả năng cao vẫn thấy sequential scan. Điều đó minh họa quyết định theo chi phí, không phải ép PostgreSQL dùng cấu trúc vừa tạo.\n\n### Kiểm tra NULL và ca biên\n\n```sql\n-- Không trả dòng nào: phép so sánh đều UNKNOWN\nSELECT customer_id FROM customers WHERE email = NULL;\n\n-- Trả khách Chi\nSELECT customer_id FROM customers WHERE email IS NULL;\n\n-- LIMIT 0 hợp lệ, hữu ích khi chỉ muốn kiểm metadata ở một số client\nSELECT order_id FROM orders ORDER BY order_id LIMIT 0;\n\n-- Bảng rỗng trả tập rỗng, không phải một dòng NULL\nBEGIN;\nDELETE FROM orders;\nSELECT order_id FROM orders ORDER BY order_id LIMIT 10;\nROLLBACK;\n```\n\n### Cursor cho trang kế\n\nGiả sử dòng cuối trang trước có `created_at = '2026-09-01 09:00+07'` và `order_id = 2`:\n\n```sql\nSELECT order_id, created_at, total_amount\nFROM orders\nWHERE status = 'paid'\n  AND (created_at, order_id) <\n      (TIMESTAMPTZ '2026-09-01 09:00:00+07', 2)\nORDER BY created_at DESC, order_id DESC\nLIMIT 20;\n```\n\nĐơn `order_id = 1` xuất hiện tiếp theo, không bị mất dù timestamp trùng. Đây là lý do cursor phải chứa đủ hai cột của thứ tự, không chỉ thời gian.",

so_sanh: "### Các lựa chọn truy xuất danh sách\n\n| Lựa chọn | Điểm mạnh | Rủi ro / khi tránh |\n|---|---|---|\n| `SELECT *` | nhanh khi thăm dò thủ công | hợp đồng mong manh, đọc và truyền cột không cần |\n| Liệt kê cột | rõ dữ liệu công khai, có cơ hội index-only scan | phải cập nhật khi thật sự cần cột mới |\n| `OFFSET ... LIMIT` | đơn giản, nhảy tới số trang | càng sâu càng tốn, dễ trùng/hụt khi dữ liệu đổi |\n| Keyset pagination | thời gian ổn định theo độ sâu, chống dịch trang | không nhảy trang tùy ý, cursor phức hợp |\n| Sequential scan | hiệu quả khi đọc tỷ lệ lớn hoặc bảng nhỏ | tốn khi chỉ cần một lát rất hẹp của bảng lớn |\n| Index scan khớp thứ tự | tìm và dừng sớm | tốn dung lượng, tăng chi phí ghi |\n\n### WHERE hay lọc trong ứng dụng\n\n| Tiêu chí | Lọc tại PostgreSQL | Tải về rồi lọc |\n|---|---|---|\n| Byte qua mạng | chỉ dòng cần | toàn bộ tập ứng viên |\n| Tận dụng index | có | không |\n| Tính nhất quán snapshot | một câu truy vấn | dễ đọc các đợt khác thời điểm |\n| Logic đặc thù khó biểu diễn | SQL có thể dài | ngôn ngữ ứng dụng thuận tiện hơn |\n\nThông thường nên đẩy điều kiện chọn lọc xuống database. Nhưng không nên biến SQL thành nơi chứa thuật toán nghiệp vụ cực khó kiểm thử chỉ để tránh truyền vài dòng; ranh giới phụ thuộc lượng dữ liệu, khả năng biểu diễn và trách nhiệm của từng lớp.",

loi_thuong_gap: [
  "Dùng `= NULL` hoặc `<> NULL`; nhận biết bằng kết quả rỗng bất ngờ dù nhìn thấy dữ liệu thiếu. Sửa bằng `IS NULL`, `IS NOT NULL` hoặc `IS DISTINCT FROM`.",
  "Tin vào thứ tự tự nhiên khi thiếu `ORDER BY`; lỗi lộ ra sau `VACUUM`, thay index hoặc đổi plan. Luôn ghi thứ tự nếu giao diện phụ thuộc nó.",
  "Chỉ sắp theo cột có giá trị trùng; biểu hiện là phân trang lúc trùng lúc hụt. Thêm khóa duy nhất làm tiêu chí cuối.",
  "Đặt hàm hay phép ép lên cột được index trong `WHERE`; plan xuất hiện `Seq Scan` và `Rows Removed by Filter` lớn. Viết điều kiện khoảng hoặc tạo expression index có chủ đích.",
  "Dùng `OFFSET` rất sâu; API chậm dần theo số trang dù chỉ trả 20 dòng. Chuyển sang keyset pagination nếu không cần nhảy trang.",
  "Tạo index đơn cột cho mọi điều kiện; ghi chậm và planner vẫn bỏ qua. Thiết kế index theo tổ hợp lọc–sắp xếp của truy vấn quan trọng.",
  "Nhúng giá trị vào chuỗi SQL; dấu nháy gây lỗi và mở SQL injection. Dùng tham số `$1`, `$2` qua driver."
],

tu_kiem_tra: [
  "Vì sao alias ở `SELECT` dùng được trong `ORDER BY` nhưng thường không dùng được trong `WHERE` cùng cấp?",
  "`WHERE city <> 'Hà Nội'` có giữ khách `city IS NULL` không? Hãy giải thích bằng logic ba trạng thái.",
  "Vì sao `ORDER BY created_at DESC LIMIT 20` vẫn có thể phân trang không ổn định, và khóa nào khắc phục trong schema bài?",
  "Hãy viết điều kiện lấy trọn ngày 1/9 theo múi giờ Việt Nam mà không ép `created_at` sang `date` trên từng dòng.",
  "Tại sao PostgreSQL có thể chọn sequential scan trên bảng năm dòng dù index khớp hoàn hảo?",
  "So sánh lượng công việc của `OFFSET 100000 LIMIT 20` với keyset pagination; đánh đổi về trải nghiệm giao diện là gì?",
  "Một partial index chỉ chứa `status = 'paid'` có giúp truy vấn `status IN ('paid','refunded')` không? Đừng đoán: bạn sẽ dùng công cụ nào để xác minh?"
],

tai_nguyen: [
  "PostgreSQL Documentation — chương Queries, Indexes và Using EXPLAIN.",
  "“SQL Antipatterns” của Bill Karwin — các lỗi thiết kế và truy vấn thường gặp.",
  "“The Art of PostgreSQL” của Dimitri Fontaine — tư duy truy vấn và mô hình dữ liệu trên PostgreSQL.",
  "Khóa học Databases của Stanford Online — nền tảng đại số quan hệ và SQL."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: B-tree thấp và rộng vì database đọc theo page; hiểu cây giải thích vì sao một index có thể tìm khoảng và cung cấp thứ tự.",
  "Nối sang mảng Mạng máy tính: chọn đúng cột và lọc tại server giảm payload, số round trip và thời gian truyền cho API danh sách.",
  "Nối sang mảng Bảo mật: parameterized query tách mã SQL khỏi dữ liệu người dùng, là lớp phòng thủ căn bản trước SQL injection.",
  "Nối sang mảng Thiết kế API: keyset pagination biến cặp `(created_at, order_id)` thành cursor ổn định thay cho số trang dựa trên OFFSET."
]

};
