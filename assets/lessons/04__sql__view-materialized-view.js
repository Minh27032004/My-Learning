window.LESSONS["04:sql:view-materialized-view"] = {

tom_tat: [
  "View lưu định nghĩa truy vấn chứ không lưu riêng kết quả; mỗi lần đọc, PostgreSQL lập kế hoạch trên truy vấn đã mở rộng nên dữ liệu phản ánh các bảng gốc tại thời điểm câu lệnh chạy.",
  "Materialized view lưu kết quả vật lý và có thể được đánh index, đổi lại dữ liệu chỉ mới đến lần `REFRESH` gần nhất và refresh toàn bộ có chi phí đáng kể.",
  "View tạo một hợp đồng đọc ổn định, gom logic join/lọc và có thể giới hạn cột được nhìn thấy, nhưng không tự biến truy vấn chậm thành nhanh và không thay thế kiểm soát quyền cẩn thận.",
  "`REFRESH MATERIALIZED VIEW CONCURRENTLY` cho phép đọc trong lúc làm mới nhưng cần unique index phù hợp, không chạy đồng thời hai refresh trên cùng đối tượng và thường tốn thêm tài nguyên.",
  "Chọn giữa truy vấn trực tiếp, view, materialized view và bảng tổng hợp dựa trên độ mới chấp nhận được, chi phí tính toán, cách cập nhật và nhu cầu vận hành."
],

can_biet_truoc: [
  "Biết `SELECT`, `JOIN`, `GROUP BY`, `HAVING`, hàm tổng hợp và ý nghĩa của `NULL`.",
  "Biết bảng, khóa chính, khóa ngoại, index và cách đọc các nút cơ bản trong `EXPLAIN`.",
  "Biết transaction cung cấp một snapshot nhất quán cho câu lệnh đọc trong PostgreSQL.",
  "Đã xem schema bán vé gồm `events`, `customers`, `tickets` và `ticket_payments` ở bài DDL."
],

dinh_nghia: "**View** là một đối tượng database có tên chứa định nghĩa `SELECT`. View thường không giữ bản sao các hàng; khi được tham chiếu, PostgreSQL đưa định nghĩa đó vào truy vấn bao quanh, tối ưu kế hoạch rồi đọc dữ liệu từ các quan hệ gốc.\n\n**Materialized view** cũng được định nghĩa bằng một truy vấn nhưng giữ kết quả như dữ liệu vật lý. Đọc nó giống đọc một bảng đã chụp trước; `REFRESH MATERIALIZED VIEW` chạy lại truy vấn để thay ảnh chụp. Vì vậy khác biệt cốt lõi không phải “cú pháp ngắn hơn”, mà là lúc trả chi phí tính toán và mức độ mới của kết quả.",

vi_sao_can: "Báo cáo bán vé phải lặp lại một join giữa sự kiện, vé và thanh toán, đồng thời loại vé hủy. Nếu năm màn hình tự chép truy vấn, một nơi có thể đếm hàng thanh toán thay vì đếm vé, nơi khác quên trạng thái hoặc lộ email khách hàng. View đặt tên cho ngữ nghĩa “vé đã thanh toán” và cho các client dùng chung một giao diện đọc.\n\nKhi báo cáo doanh thu quét hàng triệu vé và nhóm theo ngày, tính lại cho mỗi lượt mở dashboard lãng phí CPU, I/O và làm tải phân tích tranh tài nguyên với luồng mua vé. Materialized view chuyển chi phí sang lịch refresh. Nhưng nó tạo lời hứa mới: con số có thể cũ. Nếu giao diện ghi “doanh thu trực tiếp” trong khi refresh mỗi giờ, vấn đề không còn là SQL mà là sai hợp đồng sản phẩm.",

co_che: "### View là phép đặt tên cho một quan hệ dẫn xuất\n\nTa tạo lớp đọc chi tiết cho vé đã thanh toán:\n\n```sql\nCREATE VIEW paid_ticket_details AS\nSELECT\n    t.ticket_id,\n    t.event_id,\n    e.event_code,\n    e.title AS event_title,\n    t.customer_id,\n    t.seat_no,\n    t.price_cents,\n    t.purchased_at\nFROM tickets AS t\nJOIN events AS e ON e.event_id = t.event_id\nWHERE t.status = 'paid';\n```\n\n`SELECT * FROM paid_ticket_details WHERE event_id = 42` không nhất thiết tạo toàn bộ view rồi mới lọc. PostgreSQL thường mở rộng view, đẩy điều kiện xuống và tối ưu join cùng truy vấn ngoài. Bởi vậy view bình thường không phải một cache. Nếu bảng gốc đổi sau transaction khác commit, lần đọc sau có thể thấy kết quả mới theo mức cô lập của transaction.\n\nView đem lại tên nghiệp vụ và giảm sao chép, nhưng mọi cột được chọn trở thành giao diện. Dùng `SELECT *` trong định nghĩa khiến thay đổi schema khó suy luận và vô tình công bố cột mới không tự xảy ra theo cách nhiều người tưởng; PostgreSQL lưu cấu trúc view, và thay đổi kiểu/tên cột gốc có dependency. Liệt kê cột rõ ràng làm review quyền và tương thích dễ hơn.\n\n### Query rewrite và predicate pushdown\n\nPlanner có thể đẩy `event_id = 42` xuống trước join, rồi dùng index `tickets(event_id, status)`. Kiểm bằng bằng chứng:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT ticket_id, seat_no, price_cents\nFROM paid_ticket_details\nWHERE event_id = 42;\n```\n\nNếu kế hoạch dùng `Bitmap Index Scan` hoặc `Index Scan` trên index phù hợp, điều kiện đã được tận dụng. Nếu `Seq Scan`, chưa chắc là lỗi: bảng nhỏ, tỷ lệ hàng thỏa cao hoặc thống kê cũ có thể khiến quét tuần tự rẻ hơn. Chạy `ANALYZE` sau khi nạp dữ liệu đại diện rồi so estimated rows với actual rows. View không tạo index riêng; index phải nằm trên bảng gốc theo phép lọc và join thực tế.\n\nKhông phải mọi ranh giới đều được đẩy qua tự do. Aggregate, `DISTINCT`, window function, `LIMIT`, hàm volatile hay hàng rào bảo mật có thể hạn chế biến đổi vì đẩy điều kiện sẽ đổi nghĩa. Do đó quy tắc “planner luôn tối ưu xuyên view” quá mạnh; phải xem plan của truy vấn thật.\n\n### View cập nhật được và giới hạn của nó\n\nMột view đơn giản trên một bảng, không aggregate, join hoặc các cấu trúc làm mất ánh xạ hàng, có thể tự động cập nhật được:\n\n```sql\nCREATE VIEW upcoming_events AS\nSELECT event_id, event_code, title, starts_at, capacity\nFROM events\nWHERE starts_at > now()\nWITH LOCAL CHECK OPTION;\n\nUPDATE upcoming_events\nSET capacity = capacity + 50\nWHERE event_code = 'SQL-DAY';\n```\n\n`CHECK OPTION` ngăn insert/update qua view làm hàng không còn thỏa điều kiện nhìn thấy. Tuy vậy `now()` khiến tập hàng thay đổi theo thời gian; dùng view này như hàng đợi nghiệp vụ cần cân nhắc. View có join như `paid_ticket_details` không tự biết phải phân phối một update sang bảng nào. Có thể dùng `INSTEAD OF` trigger hoặc rule, nhưng logic ghi ẩn khó theo dõi; API hay procedure rõ ràng thường dễ kiểm thử hơn.\n\nĐừng hứa rằng view là “bảng ảo có thể thao tác như bảng” trong mọi trường hợp. Khả năng cập nhật là thuộc tính của cấu trúc cụ thể.\n\n### View như hợp đồng tương thích schema\n\nKhi đổi tên cột ở tầng lưu trữ, view có thể giữ tên cũ cho client trong giai đoạn chuyển tiếp:\n\n```sql\nCREATE VIEW events_api_v1 AS\nSELECT event_id, event_code, title, starts_at AS start_time, capacity\nFROM events;\n```\n\nỨng dụng cũ đọc `start_time`; ứng dụng mới có thể đọc bảng hoặc view phiên bản mới. Đây là adapter ở tầng dữ liệu. Đánh đổi là dependency kéo dài: nếu không có ngày loại bỏ, view tương thích trở thành schema thứ hai phải duy trì vô hạn. Nên version, ghi chủ sở hữu và đo client còn dùng trước khi xóa.\n\n`CREATE OR REPLACE VIEW` chỉ cho phép một số thay đổi tương thích; không thể tùy tiện đổi kiểu, tên hay thứ tự các cột hiện có. Khi thay hợp đồng phá vỡ client, tạo `v2` thường trung thực hơn cố thay tại chỗ.\n\n### Quyền truy cập và `security_barrier`\n\nCó thể `GRANT SELECT` trên view báo cáo trong khi không cấp trực tiếp bảng chứa dữ liệu nhạy cảm. Ví dụ view cho nhân viên sự kiện thấy mã vé và ghế nhưng không chọn email. Tuy nhiên bảo mật bằng view cần hiểu owner, `security_invoker`, quyền hàm được gọi và nguy cơ suy luận dữ liệu; không chỉ bỏ cột khỏi `SELECT` là xong.\n\nMột view lọc theo tenant phải chống việc planner sắp xếp biểu thức theo cách khiến hàm do người dùng cung cấp quan sát hàng đáng lẽ bị ẩn. Tùy trường hợp có thể dùng tùy chọn `security_barrier`, Row-Level Security và hàm leakproof phù hợp. Đây là lĩnh vực phải dựa tài liệu PostgreSQL và threat model cụ thể. Với phân tách tenant, Row-Level Security thường là hàng rào cốt lõi; view là giao diện bổ sung, không phải lý do bỏ kiểm thử quyền.\n\n### Materialized view trả chi phí lúc refresh\n\nBảng điều khiển cần doanh thu theo sự kiện, nhưng mỗi lần join và aggregate tốn nhiều tài nguyên. Ta chụp kết quả:\n\n```sql\nCREATE MATERIALIZED VIEW event_sales_summary AS\nSELECT\n    e.event_id,\n    e.event_code,\n    e.title,\n    count(t.ticket_id) AS paid_ticket_count,\n    coalesce(sum(t.price_cents), 0)::bigint AS revenue_cents,\n    max(t.purchased_at) AS latest_purchase_at\nFROM events AS e\nLEFT JOIN tickets AS t\n  ON t.event_id = e.event_id\n AND t.status = 'paid'\nGROUP BY e.event_id, e.event_code, e.title\nWITH DATA;\n\nCREATE UNIQUE INDEX event_sales_summary_event_uq\n  ON event_sales_summary(event_id);\n```\n\nKết quả được lưu, nên có thể tạo index trên cột của materialized view. Dashboard lọc `event_id` hoặc sắp theo `revenue_cents` có thể cần các index khác, nhưng mỗi index lại tăng thời gian và I/O refresh. `WITH NO DATA` tạo cấu trúc ở trạng thái chưa populated; truy vấn nó sẽ lỗi cho tới refresh đầu tiên.\n\nAggregate dùng `LEFT JOIN` để sự kiện chưa bán vé vẫn có một hàng. `count(t.ticket_id)` cho 0 vì biểu thức không đếm `NULL`; `count(*)` sẽ cho 1 do hàng phía sự kiện vẫn tồn tại, một lỗi báo cáo rất phổ biến. `coalesce(sum(...), 0)` biến tổng rỗng từ `NULL` thành 0 theo đúng ngữ nghĩa doanh thu.\n\n### Độ cũ là một phần của mô hình dữ liệu\n\nSau khi một vé chuyển sang `paid`, view thường thấy thay đổi ở câu lệnh sau, còn materialized view vẫn giữ ảnh chụp cũ:\n\n```sql\nSELECT revenue_cents, latest_purchase_at\nFROM event_sales_summary\nWHERE event_id = 42;\n\nREFRESH MATERIALIZED VIEW event_sales_summary;\n```\n\nỨng dụng nên hiển thị `refreshed_at` hoặc lưu metadata refresh riêng để người đọc biết tuổi dữ liệu. Chỉ ghi lịch cron trong tài liệu vận hành là chưa đủ vì job có thể thất bại. Có thể dùng một bảng `report_refresh_log` được cập nhật sau refresh thành công; không nên đặt `now()` trực tiếp trong mỗi hàng aggregate rồi hiểu đó là bằng chứng job hoàn tất nếu transaction thất bại.\n\nYêu cầu đúng tuyệt đối cho kiểm tra tồn kho không phù hợp materialized view. Một ghế vừa bán nhưng ảnh chụp chưa refresh có thể bị bán lại nếu hệ thống dùng nó để quyết định ghi. Quyết định bảo toàn invariant phải đọc bảng nguồn và constraint trong transaction; ảnh chụp chỉ hợp với phân tích chấp nhận độ trễ.\n\n### Refresh thường và refresh concurrent\n\n`REFRESH MATERIALIZED VIEW` thay toàn bộ nội dung và khóa việc đọc materialized view trong quá trình đó. `CONCURRENTLY` cho phép các phiên tiếp tục đọc phiên bản cũ trong khi bản mới được xây dựng, rồi chuyển đổi khi hoàn tất:\n\n```sql\nREFRESH MATERIALIZED VIEW CONCURRENTLY event_sales_summary;\n```\n\nĐiều kiện đáng nhớ là phải có ít nhất một `UNIQUE` index chỉ dùng tên cột và bao phủ mọi hàng; partial index hoặc index dựa trên biểu thức không đáp ứng yêu cầu này. Materialized view cũng phải đã được populated. Chỉ một refresh được chạy tại một thời điểm cho cùng materialized view.\n\nConcurrent không có nghĩa miễn phí hay hoàn toàn không lock. Nó thường tốn thêm việc đối chiếu dữ liệu, kéo dài tải CPU/I/O và vẫn cần lock ngắn để chuyển trạng thái. Nếu refresh chạy lâu hơn chu kỳ lịch, các job chồng nhau có thể tạo tải liên tục. Scheduler nên có cơ chế không khởi động bản thứ hai và theo dõi thời gian, lỗi, tuổi snapshot.\n\n### Refresh toàn phần và bài toán incremental\n\nPostgreSQL core refresh materialized view bằng cách tính lại toàn bộ truy vấn, không tự biết chỉ có mười vé mới. Với dữ liệu lớn, điều này có thể quá đắt. Một bảng tổng hợp do ứng dụng hoặc job cập nhật theo watermark có thể xử lý gia tăng, nhưng phải giải quyết cập nhật muộn, hoàn tiền, xóa, chạy lại và tính idempotent.\n\nVí dụ chỉ cộng doanh thu cho `ticket_id > watermark` sẽ sai khi vé cũ bị refund. Muốn incremental đúng phải mô hình hóa event thay đổi hoặc tính lại partition bị ảnh hưởng. Đây là lý do “tự viết materialized view gia tăng” phức tạp hơn tên gọi. Nếu cửa sổ báo cáo chỉ cần 30 ngày, một truy vấn trực tiếp trên partition và index đúng đôi khi đơn giản hơn.\n\n### Dependency, thay đổi schema và refresh lỗi\n\nMaterialized view phụ thuộc cột và hàm nguồn. Đổi kiểu hoặc xóa cột có thể bị PostgreSQL chặn do dependency; dùng `CASCADE` có thể xóa cả lớp báo cáo. Quy trình thay đổi nên tạo phiên bản mới, refresh và so sánh, chuyển client, rồi xóa phiên bản cũ.\n\nRefresh chạy trong transaction: người đọc không thấy nửa kết quả mới, nhưng truy vấn nguồn có thể thất bại do dữ liệu, hết dung lượng tạm hoặc timeout. Khi lỗi, snapshot cũ thường vẫn là thứ cần giữ, song hệ thống giám sát phải báo rằng nó đang già đi. Một dashboard trả được dữ liệu cũ không có nghĩa pipeline khỏe.\n\n### Khi nào không nên dùng\n\nKhông dùng view chỉ để che một truy vấn không ai hiểu; abstraction tốt phải có ngữ nghĩa, owner và kiểm thử. Không xếp nhiều tầng view lồng nhau nếu `EXPLAIN` trở nên khó truy dấu và một thay đổi lan rộng không đo được. Không dùng materialized view cho quyết định cần trạng thái tức thời, tập dữ liệu nhỏ tính rất rẻ, hoặc nguồn thay đổi liên tục đến mức refresh ăn hết tài nguyên.\n\nKhi query plan đã tốt và client chỉ có một truy vấn, SQL trực tiếp có thể minh bạch hơn. Khi cần cập nhật gia tăng với SLA chặt, bảng tổng hợp hoặc pipeline phân tích chuyên dụng có thể phù hợp hơn. Sự tiện lợi của `SELECT` không xóa nghĩa vụ vận hành dữ liệu dẫn xuất.",

vi_du: "### Thí nghiệm nhìn thấy độ mới khác nhau\n\nChạy sau khi đã tạo `event_sales_summary`. Dùng một sự kiện và khách hàng thử nghiệm trong database riêng:\n\n```sql\nINSERT INTO events(event_code, title, starts_at, capacity)\nVALUES ('SQL-DAY', 'SQL Day', now() + interval '30 days', 100)\nRETURNING event_id;\n\nINSERT INTO customers(email) VALUES ('learner@example.test')\nRETURNING customer_id;\n\nINSERT INTO tickets(event_id, customer_id, seat_no, price_cents, status, purchased_at)\nSELECT e.event_id, c.customer_id, 'A-01', 250000, 'paid', now()\nFROM events e CROSS JOIN customers c\nWHERE e.event_code = 'SQL-DAY' AND c.email = 'learner@example.test';\n\n-- View đọc bảng gốc nên thấy vé ngay.\nSELECT count(*) FROM paid_ticket_details WHERE event_code = 'SQL-DAY';\n\n-- Materialized view chưa chắc có hàng hoặc doanh thu mới.\nSELECT * FROM event_sales_summary WHERE event_code = 'SQL-DAY';\n\nREFRESH MATERIALIZED VIEW event_sales_summary;\nSELECT * FROM event_sales_summary WHERE event_code = 'SQL-DAY';\n```\n\nThử thêm sự kiện chưa có vé để xác nhận `paid_ticket_count = 0`, không phải 1. Thử hủy vé rồi đọc lại trước và sau refresh. Cuối cùng chạy `EXPLAIN (ANALYZE, BUFFERS)` cho cả view chi tiết và materialized view; so sánh số bảng nguồn được chạm, nhưng chỉ kết luận từ dữ liệu đủ lớn. Ví dụ không đưa ra thời gian giả định vì kết quả phụ thuộc máy, cache và quy mô thực tế.",

so_sanh: "### Bốn cách cung cấp dữ liệu đọc\n\n| Phương án | Độ mới | Chi phí mỗi lần đọc | Chi phí cập nhật | Điểm mạnh | Khi không phù hợp |\n|---|---|---|---|---|---|\n| SQL trực tiếp | Theo snapshot hiện tại | Tính lại đầy đủ | Không có bản sao | Minh bạch, dễ thử nghiệm | Logic bị sao chép ở nhiều client |\n| View | Theo snapshot hiện tại | Vẫn tính truy vấn nguồn | Không refresh | Hợp đồng tên và quyền truy cập | Aggregate nặng được gọi liên tục |\n| Materialized view | Đến lần refresh cuối | Thường thấp, đánh index được | Refresh toàn phần | Dashboard chấp nhận độ trễ | Quyết định tồn kho tức thời |\n| Bảng tổng hợp tự quản | Tùy pipeline, có thể gần thời gian thực | Thấp | Logic incremental phức tạp | Kiểm soát cập nhật gia tăng | Nhóm nhỏ không đủ năng lực vận hành |\n\nView không phải bản “nhẹ” của materialized view; nó giải quyết tái sử dụng ngữ nghĩa. Materialized view không phải index; nó sao chép kết quả của cả truy vấn, rồi index chỉ là cấu trúc truy cập trên bản sao đó. Hai công cụ có thể cùng tồn tại: view chi tiết làm hợp đồng chuẩn, materialized view tổng hợp phục vụ dashboard.",

loi_thuong_gap: [
  "Tin rằng view lưu sẵn kết quả. Dấu hiệu là truy vấn view vẫn quét/join bảng lớn và chậm; kiểm `EXPLAIN` và dùng materialized view chỉ khi chấp nhận độ cũ.",
  "Đếm `count(*)` sau `LEFT JOIN` rồi báo một vé cho sự kiện rỗng. Nhận biết bằng ca kiểm thử sự kiện chưa có vé; đếm khóa phía bảng con không `NULL`.",
  "Refresh theo lịch nhưng không ghi tuổi snapshot. Dấu hiệu là dashboard có dữ liệu mà job đã hỏng nhiều giờ; lưu và hiển thị thời điểm refresh thành công.",
  "Dùng `REFRESH ... CONCURRENTLY` mà thiếu unique index hợp lệ hoặc view chưa populated. PostgreSQL báo lỗi ngay; tạo index bao phủ mọi hàng và thực hiện refresh đầu đúng trình tự.",
  "Cho materialized view quyết định còn ghế. Dấu hiệu là bán trùng trong khoảng giữa hai refresh; dùng transaction và unique constraint trên bảng nguồn.",
  "Xếp nhiều view lồng nhau rồi đoán planner sẽ tối ưu hết. Dấu hiệu là plan phình lớn, ước lượng sai và dependency khó đổi; kiểm plan tại truy vấn ngoài cùng và đơn giản hóa ranh giới.",
  "Cấp quyền view nhưng bỏ qua hàm, owner, Row-Level Security và khả năng suy luận. Dấu hiệu là role đọc được dữ liệu ngoài tenant; viết test quyền bằng chính role bị giới hạn."
],

tu_kiem_tra: [
  "Vì sao điều kiện `WHERE event_id = 42` ngoài view có thể dùng index bảng `tickets`, và trường hợp nào ngăn predicate pushdown?",
  "Giải thích vì sao `count(*)` và `count(t.ticket_id)` khác nhau trong aggregate dùng `LEFT JOIN`.",
  "Materialized view đã tạo unique index vẫn có thể chậm refresh vì những chi phí nào?",
  "Thiết kế cách người dùng nhận biết báo cáo đang cũ khi job refresh thất bại nhưng snapshot cũ vẫn đọc được.",
  "Vì sao incremental theo `ticket_id > watermark` cho doanh thu sẽ sai khi có refund trên vé cũ?",
  "Khi nào view là hợp đồng tương thích tốt, và khi nào nó chỉ kéo dài nợ schema?",
  "Hãy chọn công cụ cho ba nhu cầu: kiểm tra ghế trước khi bán, dashboard theo giờ, và API chi tiết vé; nêu đánh đổi của từng lựa chọn."
],

tai_nguyen: [
  "Tài liệu chính thức PostgreSQL, các mục `CREATE VIEW`, Rules and Privileges, `CREATE MATERIALIZED VIEW` và `REFRESH MATERIALIZED VIEW`.",
  "“PostgreSQL: Up and Running” — Regina Obe và Leo Hsu, phần view, materialized view và quản trị PostgreSQL.",
  "“Designing Data-Intensive Applications” — Martin Kleppmann, các chương về dữ liệu dẫn xuất, batch processing và stream processing.",
  "“SQL Performance Explained” — Markus Winand, các phần về index, predicate, join và đọc execution plan."
],

lien_he: [
  "Nối sang mảng Kiến trúc dữ liệu: materialized view là một dạng dữ liệu dẫn xuất, đặt ra SLA độ mới và chiến lược tái tạo.",
  "Nối sang mảng Bảo mật: view có thể thu hẹp bề mặt dữ liệu nhưng phải phối hợp role, Row-Level Security và kiểm thử phân quyền.",
  "Nối sang mảng Hệ thống phân tán: snapshot cũ minh họa đánh đổi giữa consistency, độ trễ và độ sẵn sàng của báo cáo.",
  "Nối sang mảng Quan sát hệ thống: lịch refresh cần metric thời lượng, lần thành công cuối, lỗi và cảnh báo tuổi dữ liệu."
]

};
