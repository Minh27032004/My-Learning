window.LESSONS["02:cau-truc-du-lieu-tuyen-tinh:queue-fifo-va-circular-queue"] = {

tom_tat: [
  "**Queue khác stack đúng một chi tiết — lấy từ đầu nào — nhưng chi tiết đó đổi hẳn lớp bài toán giải được.** Đổi stack thành queue biến DFS thành BFS, và BFS cho đường đi ngắn nhất.",
  "**`list` là lựa chọn SAI cho queue.** `pop(0)` là O(n) vì phải dịch mọi phần tử — đây là điểm khác biệt lớn nhất so với stack, nơi `list` lại là lựa chọn đúng.",
  "**Circular queue giải bài toán lãng phí chỗ bằng phép chia lấy dư.** Con trỏ chạy tới cuối mảng thì quay về đầu, nên mảng cố định dùng được vô hạn lần.",
  "**Vấn đề khó nhất của circular queue: phân biệt RỖNG với ĐẦY** — cả hai đều cho `dau == cuoi`. Ba cách giải, mỗi cách một đánh đổi.",
  "**Trong Python, dùng `collections.deque` cho queue** — O(1) hai đầu, viết bằng C. Tự cài circular queue chỉ khi cần giới hạn kích thước cứng hoặc dùng ngôn ngữ khác."
],

can_biet_truoc: [
  "Bài **Stack LIFO** ngay trước — cấu trúc đối xứng; bài này so sánh liên tục với nó.",
  "Bài **Array và dynamic array** trong module này — vì sao `pop(0)` là O(n).",
  "Bài **Linked list** trong module này — cài queue bằng danh sách liên kết cần giữ cả `head` lẫn `tail`.",
  "Bài **Process và thread** ở module 2 mảng 01 — hàng đợi là cơ chế nền của lập lịch và giao tiếp giữa tiến trình."
],

dinh_nghia: "Nói đời thường: xếp hàng mua vé. Người tới trước mua trước, người tới sau đứng cuối. Không ai chen ngang, và cũng không ai phục vụ người cuối hàng trước — đó là toàn bộ quy tắc.\n\nCác khái niệm:\n\n- **Queue** (hàng đợi) — cấu trúc mà việc **thêm** ở một đầu và **lấy** ở đầu kia.\n- **FIFO** (*First In, First Out*) — vào trước ra trước. Tính chất định nghĩa của queue.\n- **`enqueue`** — thêm vào **cuối** hàng. **`dequeue`** — lấy và bỏ từ **đầu** hàng. **`front`** — xem đầu hàng mà không bỏ.\n- **Circular queue** (hàng đợi vòng) — hàng đợi cài trên mảng cố định, dùng phép chia lấy dư để con trỏ quay vòng.\n- **Con trỏ `dau`** — vị trí phần tử sắp lấy ra. **Con trỏ `cuoi`** — vị trí sẽ ghi phần tử tiếp theo.\n- **Hàng đợi có chặn** (*bounded queue*) — có sức chứa tối đa; đầy thì từ chối hoặc chờ.\n- **Hàng đợi ưu tiên** (*priority queue*) — lấy ra phần tử có **độ ưu tiên cao nhất** thay vì phần tử vào trước. **Không phải** queue theo nghĩa của bài này; nó là heap, chủ đề của module 4.\n\nSo sánh với stack — điểm cần nắm chắc nhất:\n\n```\nSTACK (LIFO)                    QUEUE (FIFO)\n─────────────────               ─────────────────\nthêm và lấy CÙNG một đầu        thêm một đầu, lấy đầu KIA\n\n  push ↓  ↑ pop                   enqueue ↓        ↑ dequeue\n      ┌───┐                              ┌───┬───┬───┐\n      │ 3 │                              │ 1 │ 2 │ 3 │\n      │ 2 │                              └───┴───┴───┘\n      │ 1 │                              cuối      đầu\n      └───┘\n\nDùng cho: lồng nhau, quay lui   Dùng cho: xử lý theo thứ tự đến,\n          khử đệ quy, DFS                 BFS, hàng đợi công việc\n```",

vi_sao_can: "Queue là cấu trúc xuất hiện nhiều nhất trong hệ thống thật, dù ít được nhắc tới:\n\n- **Nó là cơ chế nền của mọi hệ thống xử lý bất đồng bộ.** Hàng đợi công việc, hàng đợi tin nhắn, bộ đệm mạng, hàng đợi in ấn — tất cả đều là queue. Mảng 06 sẽ nói về message queue ở quy mô hệ thống, và nền của nó chính là bài này.\n- **Nó cho BFS, và BFS cho đường đi ngắn nhất.** Đây là khác biệt thực chất giữa queue và stack: DFS tìm *một* đường, BFS tìm đường *ngắn nhất*. Cùng một khung code, chỉ đổi cấu trúc.\n- **Nó là chỗ `list` sai còn `deque` đúng.** Bài stack cho thấy `list` là lựa chọn đúng; bài này cho thấy cùng cấu trúc đó lại là lựa chọn sai. Hiểu vì sao là hiểu được cách chọn cấu trúc theo thao tác.\n- **Circular queue dạy một kỹ thuật tổng quát.** Phép chia lấy dư để quay vòng xuất hiện ở bộ đệm âm thanh, ring buffer của log, và cả bảng băm (module 3).\n- **Hàng đợi có chặn là nền của điều tiết tải.** Khi hệ thống quá tải, câu hỏi \"đầy thì làm gì\" quyết định hệ thống sập hay chỉ chậm lại — và bài này nêu bốn chiến lược.\n\nTình huống thật rất phổ biến: bạn viết trình thu thập dữ liệu, dùng `list` làm hàng đợi URL với `pop(0)` để lấy việc. Với vài trăm URL thì chạy tốt. Với 500.000 URL, chương trình chậm dần rồi gần như đứng — vì mỗi `pop(0)` phải dịch nửa triệu phần tử, và tổng cộng là O(n²). **Đổi một dòng sang `deque.popleft()` là xong** — cùng logic, khác bậc.\n\nMột lưu ý về ranh giới: **hàng đợi ưu tiên không phải queue.** Nó lấy ra phần tử \"quan trọng nhất\" chứ không phải \"vào trước nhất\", và cài bằng heap. Đừng nhầm hai thứ khi đọc tài liệu.",

co_che: "### Bốn thao tác\n\n```\n     enqueue(4)                      dequeue() → 1\n         ↓                                ↑\n   ┌───┬───┬───┬───┐            ┌───┬───┬───┐\n   │ 1 │ 2 │ 3 │ 4 │            │ 2 │ 3 │ 4 │\n   └───┴───┴───┴───┘            └───┴───┴───┘\n    đầu         cuối             đầu     cuối\n```\n\n```\nenqueue(x)   thêm vào CUỐI        O(1)\ndequeue()    lấy và bỏ từ ĐẦU     O(1)\nfront()      xem đầu, không bỏ    O(1)\nis_empty()   có rỗng không        O(1)\n```\n\n### Vì sao `list` là lựa chọn SAI\n\nĐây là điểm đối lập quan trọng nhất với bài stack:\n\n```\nSTACK trên list:  push=append (cuối), pop=pop() (cuối)\n                  ⇒ CẢ HAI ở cuối ⇒ CẢ HAI O(1)   ⭐\n\nQUEUE trên list:  enqueue=append (cuối), dequeue=pop(0) (đầu)\n                  ⇒ pop(0) phải DỊCH n−1 phần tử ⇒ O(n)   ⚠️\n```\n\n```\npop(0) trên mảng:\n\n    [1][2][3][4][5]\n     ↓  lấy 1, rồi DỊCH toàn bộ\n    [2][3][4][5]\n     ←  ←  ←  ←     n−1 phép dịch\n\n⇒ n lần dequeue = O(n²) tổng\n```\n\nCách khác cũng sai:\n\n```\nĐẢO NGƯỢC: enqueue=insert(0), dequeue=pop()\n           ⇒ insert(0) là O(n)  ⚠️ chỉ đổi chỗ vấn đề\n\n⇒ Với mảng thường, MỘT trong hai thao tác BUỘC phải là O(n).\n  Vì mảng chỉ rẻ ở một đầu.\n```\n\nBa cách giải đúng:\n\n```\n① DANH SÁCH LIÊN KẾT có cả head và tail   → O(1) hai đầu\n② CIRCULAR QUEUE trên mảng cố định        → O(1) hai đầu, không dịch\n③ deque (Python)                           → O(1) hai đầu, viết bằng C  ⭐\n```\n\n### Circular queue — ý tưởng\n\nVấn đề của queue trên mảng thường:\n\n```\nMảng 5 ô, enqueue 3 lần rồi dequeue 2 lần:\n\n    [ ][ ][3][ ][ ]\n           ↑     ↑\n          đầu   cuối\n\n⚠️ Hai ô đầu BỎ TRỐNG mà không dùng lại được.\n   Cứ thế thì mảng \"hết chỗ\" dù còn nhiều ô rỗng.\n```\n\nGiải pháp — cho con trỏ **quay vòng**:\n\n```\nKhi con trỏ chạy tới cuối mảng, nó QUAY VỀ đầu:\n\n    vị_trí_tiếp = (vị_trí + 1) % kích_thước\n\n         ┌───────────────┐\n         ↓               │\n    [ 6 ][ 7 ][ 3 ][ 4 ][ 5 ]\n              ↑         ↑\n             đầu       cuối (vừa quay vòng về ô 0, 1)\n```\n\nPhép `% kích_thước` là toàn bộ kỹ thuật: nó biến một mảng thẳng thành một vòng tròn về mặt logic.\n\n```\nenqueue(x):  o[cuoi] = x;  cuoi = (cuoi + 1) % n\ndequeue():   x = o[dau];   dau = (dau + 1) % n;  return x\n\n⇒ CẢ HAI đều O(1), KHÔNG dịch phần tử nào.\n```\n\n### ⚠️ Vấn đề khó nhất: rỗng hay đầy\n\nĐây là chỗ mọi hiện thực circular queue phải giải quyết:\n\n```\nRỖNG:  dau == cuoi\nĐẦY:   dau == cuoi\n\n⚠️ HAI TRẠNG THÁI KHÁC HẲN NHAU cho CÙNG một điều kiện!\n```\n\n```\nRỖNG                          ĐẦY\n[ ][ ][ ][ ][ ]               [a][b][c][d][e]\n ↑                             ↑\ndau=cuoi=0                    dau=cuoi=0  (cuoi vừa quay hết một vòng)\n```\n\nBa cách giải, mỗi cách một đánh đổi:\n\n```\n① BỎ TRỐNG MỘT Ô\n   Đầy khi:  (cuoi + 1) % n == dau\n   Rỗng khi: dau == cuoi\n   ⚠️ mảng n ô chỉ chứa được n−1 phần tử\n   ⭐ đơn giản nhất, không cần biến phụ\n\n② GIỮ BIẾN ĐẾM size\n   Đầy khi:  size == n\n   Rỗng khi: size == 0\n   ⭐ dùng hết n ô, và len() là O(1)\n   ⚠️ phải cập nhật size ở MỌI thao tác\n\n③ CỜ BOOLEAN \"vừa đầy\"\n   ⚠️ dễ sai nhất, ít dùng\n```\n\nKhuyến nghị thực dụng: **dùng cách ②.** Một biến `int` là cái giá rất nhỏ so với việc mất một ô và có `len()` miễn phí. Cách ① chỉ hợp khi bộ nhớ cực chật hoặc viết ở tầng rất thấp.\n\n### Hàng đợi có chặn — đầy thì làm gì\n\nCircular queue có sức chứa cố định, nên phải quyết định hành vi khi đầy. Bốn chiến lược:\n\n```\n① NÉM LỖI            raise QueueDay\n   → người gọi tự quyết. Rõ ràng nhất.\n\n② TỪ CHỐI IM LẶNG    return False\n   → hợp khi mất dữ liệu là chấp nhận được (log, metric)\n\n③ GHI ĐÈ CÁI CŨ NHẤT  ring buffer\n   → hợp cho \"N mục gần nhất\": log gần nhất, bộ đệm âm thanh\n   → deque(maxlen=k) làm đúng việc này\n\n④ CHỜ TỚI KHI CÓ CHỖ  queue.Queue chặn luồng\n   → hợp cho mô hình người sản xuất – người tiêu thụ\n   → tạo ÁP LỰC NGƯỢC (backpressure): người sản xuất bị làm chậm lại\n```\n\nChiến lược ④ đáng chú ý vì nó là cơ chế bảo vệ hệ thống: **khi người tiêu thụ chậm, hàng đợi đầy, và người sản xuất bị chặn lại** — thay vì hàng đợi phình vô hạn cho tới khi hết RAM. Đây là chủ đề của mảng 06.\n\n⚠️ Hàng đợi **không giới hạn** là một rủi ro vận hành thật: nếu tốc độ vào lớn hơn tốc độ ra, nó sẽ ngốn hết bộ nhớ. Đặt giới hạn là quyết định thiết kế, không phải chi tiết nhỏ.\n\n### BFS — vì sao queue cho đường ngắn nhất\n\nĐây là ứng dụng quan trọng nhất, và là khác biệt thực chất so với stack:\n\n```\nCÙNG MỘT KHUNG CODE:\n\n    kho = [điểm_bắt_đầu]\n    while kho:\n        v = LẤY_RA(kho)\n        xử lý v\n        THÊM các hàng xóm chưa thăm vào kho\n\n  LẤY_RA = pop()      → STACK → DFS → đi SÂU trước\n  LẤY_RA = popleft()  → QUEUE → BFS → đi RỘNG trước\n```\n\nVì sao BFS cho đường **ngắn nhất**:\n\n```\nQueue xử lý theo THỨ TỰ VÀO ⇒ các đỉnh cách gốc 1 bước được\nxử lý HẾT trước khi tới các đỉnh cách 2 bước.\n\n    tầng 0:  A\n    tầng 1:  B  C          ← xử lý hết tầng 1...\n    tầng 2:  D  E  F       ← ...rồi mới tới tầng 2\n\n⇒ Lần ĐẦU TIÊN gặp một đỉnh là qua đường NGẮN NHẤT.\n```\n\n```\n⚠️ Điều này CHỈ đúng khi mọi cạnh có TRỌNG SỐ NHƯ NHAU.\n   Cạnh có trọng số khác nhau ⇒ cần Dijkstra (hàng đợi ưu tiên),\n   chủ đề của module 5.\n```\n\nDFS thì không có tính chất đó:\n\n```\nStack đi sâu vào một nhánh tới cùng rồi mới quay lui\n⇒ đường tìm được có thể dài hơn nhiều so với đường ngắn nhất\n⇒ DFS tìm MỘT đường, không phải đường TỐT NHẤT\n```\n\n### Ba biến thể của queue\n\n```\nQUEUE THƯỜNG (FIFO)\n  lấy theo thứ tự vào\n  → hàng đợi công việc, BFS\n\nDEQUE (hai đầu)\n  thêm/lấy được ở CẢ HAI đầu\n  → dùng làm cả stack lẫn queue; bài tiếp theo\n\nHÀNG ĐỢI ƯU TIÊN\n  lấy phần tử ƯU TIÊN CAO NHẤT, không phải vào trước nhất\n  → cài bằng HEAP, không phải mảng vòng\n  → module 4; Dijkstra, lập lịch theo mức khẩn\n```\n\n⚠️ Hàng đợi ưu tiên hay bị gọi là \"queue\" nhưng **không** có tính chất FIFO. Đây là nhầm lẫn phổ biến khi đọc tài liệu.\n\n### Bốn lựa chọn trong Python\n\n```\nCÔNG CỤ                  DÙNG KHI                      AN TOÀN ĐA LUỒNG\n──────────────────       ─────────────────────         ────────────────\ncollections.deque        ⭐ mặc định, một luồng         ✓ append/popleft\nqueue.Queue              nhiều LUỒNG, cần chặn          ✓ có khoá\nmultiprocessing.Queue    nhiều TIẾN TRÌNH               ✓ qua ống dẫn\nasyncio.Queue            bất đồng bộ                    ✓ trong một luồng\n\nlist                     ❌ KHÔNG BAO GIỜ làm queue\n```\n\nĐiểm khác biệt then chốt giữa `deque` và `queue.Queue`:\n\n```\ndeque        NHANH, nhưng KHÔNG chặn khi rỗng/đầy\n             → phải tự xử lý \"hết việc thì làm gì\"\n\nqueue.Queue  CHẬM HƠN (có khoá), nhưng CHẶN được\n             → luồng tiêu thụ tự ngủ khi hàng rỗng\n             → có join()/task_done() để chờ xong hết việc\n```\n\nQuy tắc chọn: **một luồng thì `deque`; nhiều luồng thì `queue.Queue`.** Dùng `deque` cho nhiều luồng thì `append`/`popleft` vẫn an toàn (nhờ GIL), nhưng bạn phải tự viết cơ chế chờ — và đó là chỗ dễ sai.",

vi_du: "### Chứng minh `list` sai cho queue\n\n```python\nimport time\nfrom collections import deque\n\nprint(f\"{'n':>9} {'list.pop(0)':>16} {'deque.popleft()':>18} {'tỉ lệ':>9}\")\nfor n in (10_000, 20_000, 40_000):\n    # list — pop(0) là O(n)\n    ds = list(range(n))\n    t0 = time.perf_counter()\n    while ds:\n        ds.pop(0)                      # ⚠️ dịch mọi phần tử\n    t_list = time.perf_counter() - t0\n\n    # deque — popleft() là O(1)\n    dq = deque(range(n))\n    t0 = time.perf_counter()\n    while dq:\n        dq.popleft()                   # ⭐ O(1)\n    t_deque = time.perf_counter() - t0\n\n    print(f\"{n:>9,} {t_list:>15.4f}s {t_deque:>17.4f}s \"\n          f\"{t_list/t_deque:>8.0f}x\")\n```\n\n```\n        n      list.pop(0)    deque.popleft()     tỉ lệ\n   10,000          ~0.020s            ~0.001s       ~20x\n   20,000          ~0.080s            ~0.002s       ~40x\n   40,000          ~0.320s            ~0.004s       ~80x\n\n⭐ ĐỌC CỘT CUỐI: tỉ lệ GẤP ĐÔI khi n gấp đôi\n  ⇒ list là O(n²) tổng, deque là O(n) tổng\n```\n\nĐối lập với stack — cùng `list`, khác kết luận:\n\n```python\nn = 40_000\n\n# ⭐ STACK trên list — CẢ HAI thao tác ở CUỐI ⇒ O(1)\nt0 = time.perf_counter()\ns = []\nfor i in range(n):\n    s.append(i)\nwhile s:\n    s.pop()                            # ⭐ cuối — O(1)\nprint(f\"list làm STACK: {time.perf_counter()-t0:.4f}s   ⭐ đúng\")\n\n# ⚠️ QUEUE trên list — pop(0) ở ĐẦU ⇒ O(n)\nt0 = time.perf_counter()\nq = []\nfor i in range(n):\n    q.append(i)\nwhile q:\n    q.pop(0)                           # ⚠️ đầu — O(n)\nprint(f\"list làm QUEUE: {time.perf_counter()-t0:.4f}s   ⚠️ sai\")\n```\n\n```\nlist làm STACK: ~0.008s   ⭐ đúng\nlist làm QUEUE: ~0.320s   ⚠️ sai\n\n⇒ CÙNG một cấu trúc `list`.\n  Đúng cho stack, SAI cho queue — chỉ vì lấy từ đầu nào.\n```\n\n### Circular queue — hiện thực với biến đếm\n\n```python\nfrom typing import Any, Iterator\n\n\nclass QueueDay(Exception):\n    \"\"\"Hàng đợi đã đầy.\"\"\"\n\n\nclass QueueRong(Exception):\n    \"\"\"Hàng đợi rỗng.\"\"\"\n\n\nclass CircularQueue:\n    \"\"\"Hàng đợi vòng trên mảng cố định. Mọi thao tác O(1).\n\n    Dùng biến đếm _size để phân biệt rỗng và đầy\n    ⇒ dùng được HẾT n ô (khác cách 'bỏ trống một ô').\n    \"\"\"\n\n    def __init__(self, suc_chua: int) -> None:\n        if suc_chua < 1:\n            raise ValueError(\"sức chứa phải ≥ 1\")\n        self._o: list[Any] = [None] * suc_chua\n        self._dau = 0                      # vị trí sắp LẤY RA\n        self._cuoi = 0                     # vị trí sắp GHI VÀO\n        self._size = 0                     # ⭐ phân biệt rỗng/đầy\n\n    @property\n    def suc_chua(self) -> int:\n        return len(self._o)\n\n    def __len__(self) -> int:\n        return self._size\n\n    def __bool__(self) -> bool:\n        return self._size > 0\n\n    @property\n    def day(self) -> bool:\n        return self._size == len(self._o)\n\n    def enqueue(self, x: Any) -> None:\n        \"\"\"Thêm vào cuối. O(1). Raises: QueueDay.\"\"\"\n        if self.day:\n            raise QueueDay(\n                f\"hàng đợi đầy ({len(self._o)} phần tử)\"\n            )\n        self._o[self._cuoi] = x\n        self._cuoi = (self._cuoi + 1) % len(self._o)   # ⭐ QUAY VÒNG\n        self._size += 1\n\n    def dequeue(self) -> Any:\n        \"\"\"Lấy từ đầu. O(1). Raises: QueueRong.\"\"\"\n        if self._size == 0:\n            raise QueueRong(\"lấy từ hàng đợi rỗng\")\n        x = self._o[self._dau]\n        self._o[self._dau] = None          # ⭐ bỏ tham chiếu để GC thu hồi\n        self._dau = (self._dau + 1) % len(self._o)\n        self._size -= 1\n        return x\n\n    def front(self) -> Any:\n        \"\"\"Xem đầu, không bỏ. Raises: QueueRong.\"\"\"\n        if self._size == 0:\n            raise QueueRong(\"xem hàng đợi rỗng\")\n        return self._o[self._dau]\n\n    def __iter__(self) -> Iterator[Any]:\n        \"\"\"Duyệt từ đầu tới cuối theo thứ tự FIFO.\"\"\"\n        for i in range(self._size):\n            yield self._o[(self._dau + i) % len(self._o)]\n\n    def __repr__(self) -> str:\n        return f\"CircularQueue({list(self)}, sức chứa={len(self._o)})\"\n\n    def trang_thai_mang(self) -> str:\n        \"\"\"Hiện bố cục thật trong mảng — để thấy cơ chế quay vòng.\"\"\"\n        o = [\"·\" if v is None else str(v) for v in self._o]\n        danh_dau = []\n        for i, v in enumerate(o):\n            nhan = \"\"\n            if i == self._dau and self._size:\n                nhan += \"↑đ\"\n            if i == self._cuoi:\n                nhan += \"↑c\"\n            danh_dau.append(f\"{v}{nhan}\")\n        return \"[\" + \"][\".join(danh_dau) + \"]\"\n```\n\n```python\nq = CircularQueue(5)\nfor x in (1, 2, 3):\n    q.enqueue(x)\nprint(q.trang_thai_mang())        # [1↑đ][2][3][·↑c][·]\n\nq.dequeue(); q.dequeue()\nprint(q.trang_thai_mang())        # [·][·][3↑đ][·↑c][·]\n\n# ⭐ QUAY VÒNG: thêm tiếp, con trỏ cuối quay về đầu mảng\nfor x in (4, 5, 6):\n    q.enqueue(x)\nprint(q.trang_thai_mang())        # [5][6][3↑đ][4][·]  ← cuối đã quay vòng\nassert list(q) == [3, 4, 5, 6]    # ⭐ thứ tự FIFO vẫn đúng\n```\n\n```\n[1↑đ][2][3][·↑c][·]\n[·][·][3↑đ][·↑c][·]\n[5][6][3↑đ][4][·↑c]\n\n⭐ Hai ô đầu mảng ĐƯỢC DÙNG LẠI sau khi dequeue.\n  Không có phép dịch nào, và không lãng phí chỗ.\n```\n\nKiểm tra đầy đủ:\n\n```python\nq = CircularQueue(3)\nassert len(q) == 0 and not q and not q.day\n\nq.enqueue(\"a\"); q.enqueue(\"b\"); q.enqueue(\"c\")\nassert q.day and len(q) == 3\nassert q.front() == \"a\" and len(q) == 3       # front không bỏ\n\nassert q.dequeue() == \"a\"                      # ⭐ FIFO: vào trước ra trước\nassert q.dequeue() == \"b\"\nq.enqueue(\"d\")                                  # quay vòng\nassert list(q) == [\"c\", \"d\"]\n\n# ---- Ca biên ----\ntry:\n    day = CircularQueue(2)\n    day.enqueue(1); day.enqueue(2); day.enqueue(3)\nexcept QueueDay as e:\n    print(f\"✓ {e}\")\n\ntry:\n    CircularQueue(1).dequeue()\nexcept QueueRong as e:\n    print(f\"✓ {e}\")\n\ntry:\n    CircularQueue(0)\nexcept ValueError as e:\n    print(f\"✓ {e}\")\n\n# ⭐ Biên quan trọng: đầy rồi rỗng rồi đầy lại — con trỏ quay nhiều vòng\nq = CircularQueue(3)\nfor vong in range(5):                          # 5 vòng liên tiếp\n    for x in range(3):\n        q.enqueue(vong * 10 + x)\n    assert q.day\n    kq = [q.dequeue() for _ in range(3)]\n    assert kq == [vong * 10, vong * 10 + 1, vong * 10 + 2]\n    assert len(q) == 0\nprint(\"✓ quay vòng 5 lần: thứ tự luôn đúng\")\n\n# ⭐ Biên: sức chứa 1\nq1 = CircularQueue(1)\nq1.enqueue(\"x\")\nassert q1.day\nassert q1.dequeue() == \"x\"\nassert not q1\n\nprint(\"circular queue: mọi ca kiểm thử đạt\")\n```\n\nCa \"quay vòng 5 lần\" là ca biên quan trọng nhất: nó bắt được lỗi khi con trỏ quay nhiều vòng mà `dau`/`cuoi` không đồng bộ.\n\n### Ba cách phân biệt rỗng/đầy\n\n```python\nclass QueueBoTrongMotO:\n    \"\"\"Cách ①: bỏ trống một ô. Mảng n ô chỉ chứa n−1 phần tử.\n\n    Ưu: không cần biến phụ.\n    Nhược: mất một ô, và len() phải TÍNH chứ không lưu sẵn.\n    \"\"\"\n\n    def __init__(self, suc_chua: int) -> None:\n        self._o: list[Any] = [None] * (suc_chua + 1)   # ⭐ cấp THÊM một ô\n        self._dau = self._cuoi = 0\n\n    @property\n    def rong(self) -> bool:\n        return self._dau == self._cuoi\n\n    @property\n    def day(self) -> bool:\n        return (self._cuoi + 1) % len(self._o) == self._dau   # ⭐ khác biệt\n\n    def __len__(self) -> int:\n        return (self._cuoi - self._dau) % len(self._o)         # ⭐ phải TÍNH\n\n    def enqueue(self, x: Any) -> None:\n        if self.day:\n            raise QueueDay(\"đầy\")\n        self._o[self._cuoi] = x\n        self._cuoi = (self._cuoi + 1) % len(self._o)\n\n    def dequeue(self) -> Any:\n        if self.rong:\n            raise QueueRong(\"rỗng\")\n        x = self._o[self._dau]\n        self._o[self._dau] = None\n        self._dau = (self._dau + 1) % len(self._o)\n        return x\n\n\nq = QueueBoTrongMotO(3)          # xin 3, cấp phát 4 ô\nq.enqueue(1); q.enqueue(2); q.enqueue(3)\nassert q.day and len(q) == 3\ntry:\n    q.enqueue(4)\nexcept QueueDay:\n    print(\"✓ cách ①: đầy ở 3 phần tử dù mảng có 4 ô\")\n```\n\n```\nSO SÁNH BA CÁCH:\n\n              Bỏ trống một ô    Biến đếm size    Cờ boolean\n              ──────────────    ─────────────    ──────────\nDùng hết n ô  ✗ (n−1)           ⭐ ✓             ✓\nlen()         phải TÍNH         ⭐ O(1) lưu sẵn  phải tính\nBiến phụ      không             1 int            1 bool\nDễ sai        thấp              thấp             ⚠️ cao nhất\nKhuyến nghị   khi bộ nhớ chật   ⭐ MẶC ĐỊNH      tránh\n```\n\n### Ring buffer — ghi đè cái cũ nhất\n\nChiến lược ③ khi đầy, và nó rất hữu ích trong thực tế:\n\n```python\nclass RingBuffer:\n    \"\"\"Giữ N mục GẦN NHẤT. Đầy thì GHI ĐÈ mục cũ nhất.\n\n    Dùng cho: log gần nhất, bộ đệm âm thanh, cửa sổ trượt.\n    Bộ nhớ O(N) cố định, không bao giờ tăng.\n    \"\"\"\n\n    def __init__(self, suc_chua: int) -> None:\n        if suc_chua < 1:\n            raise ValueError(\"sức chứa phải ≥ 1\")\n        self._o: list[Any] = [None] * suc_chua\n        self._cuoi = 0\n        self._size = 0\n\n    def them(self, x: Any) -> Any | None:\n        \"\"\"Thêm x. Trả về mục bị GHI ĐÈ (nếu có). O(1).\"\"\"\n        bi_ghi_de = None\n        if self._size == len(self._o):\n            bi_ghi_de = self._o[self._cuoi]      # ⭐ mục cũ nhất\n        else:\n            self._size += 1\n        self._o[self._cuoi] = x\n        self._cuoi = (self._cuoi + 1) % len(self._o)\n        return bi_ghi_de\n\n    def __iter__(self) -> Iterator[Any]:\n        \"\"\"Từ CŨ NHẤT tới MỚI NHẤT.\"\"\"\n        bat_dau = (self._cuoi - self._size) % len(self._o)\n        for i in range(self._size):\n            yield self._o[(bat_dau + i) % len(self._o)]\n\n    def __len__(self) -> int:\n        return self._size\n\n\nlog = RingBuffer(3)\nfor dong in (\"A\", \"B\", \"C\"):\n    log.them(dong)\nassert list(log) == [\"A\", \"B\", \"C\"]\n\nbo = log.them(\"D\")                      # ⭐ đầy → ghi đè \"A\"\nassert bo == \"A\"\nassert list(log) == [\"B\", \"C\", \"D\"]\n\nlog.them(\"E\")\nassert list(log) == [\"C\", \"D\", \"E\"]     # ⭐ luôn giữ 3 mục GẦN NHẤT\n\n# Ca biên: sức chứa 1\nr1 = RingBuffer(1)\nr1.them(\"x\")\nassert r1.them(\"y\") == \"x\"\nassert list(r1) == [\"y\"]\n```\n\n```python\n# ⭐ Python có sẵn: deque(maxlen=k) làm đúng việc này\nfrom collections import deque\n\nlog = deque(maxlen=3)\nfor dong in \"ABCDE\":\n    log.append(dong)\nassert list(log) == [\"C\", \"D\", \"E\"]     # ⭐ tự bỏ mục cũ nhất\n\n# ⚠️ Khác biệt: deque KHÔNG cho biết mục nào bị bỏ.\n#    Nếu cần biết (để ghi log, giải phóng tài nguyên) thì tự cài.\n```\n\n### BFS — queue cho đường ngắn nhất\n\nĐây là ứng dụng quan trọng nhất, và so sánh trực tiếp với DFS:\n\n```python\nfrom collections import deque\n\n\ndef bfs_duong_ngan_nhat(do_thi: dict[str, list[str]],\n                        bat_dau: str, dich: str) -> list[str] | None:\n    \"\"\"Tìm đường NGẮN NHẤT (ít cạnh nhất). O(V + E).\n\n    Chỉ đúng khi mọi cạnh có trọng số NHƯ NHAU.\n    \"\"\"\n    if bat_dau == dich:\n        return [bat_dau]\n    da_tham = {bat_dau}\n    hang_doi: deque[tuple[str, list[str]]] = deque([(bat_dau, [bat_dau])])\n\n    while hang_doi:\n        dinh, duong = hang_doi.popleft()          # ⭐ FIFO ⇒ theo TẦNG\n        for ke in do_thi.get(dinh, []):\n            if ke == dich:\n                return duong + [ke]\n            if ke not in da_tham:\n                da_tham.add(ke)                    # ⭐ đánh dấu khi THÊM\n                hang_doi.append((ke, duong + [ke]))\n    return None\n\n\ndef dfs_mot_duong(do_thi: dict[str, list[str]],\n                  bat_dau: str, dich: str) -> list[str] | None:\n    \"\"\"Tìm MỘT đường bất kỳ. CÙNG khung code, chỉ đổi pop().\"\"\"\n    if bat_dau == dich:\n        return [bat_dau]\n    da_tham = {bat_dau}\n    kho: list[tuple[str, list[str]]] = [(bat_dau, [bat_dau])]\n\n    while kho:\n        dinh, duong = kho.pop()                    # ⭐ LIFO ⇒ đi SÂU\n        for ke in do_thi.get(dinh, []):\n            if ke == dich:\n                return duong + [ke]\n            if ke not in da_tham:\n                da_tham.add(ke)\n                kho.append((ke, duong + [ke]))\n    return None\n\n\n#   A ─── B ─── C ─── D\n#   │                 │\n#   └───── E ─────────┘\ndo_thi = {\n    \"A\": [\"B\", \"E\"],\n    \"B\": [\"A\", \"C\"],\n    \"C\": [\"B\", \"D\"],\n    \"D\": [\"C\", \"E\"],\n    \"E\": [\"A\", \"D\"],\n}\n\nbfs = bfs_duong_ngan_nhat(do_thi, \"A\", \"D\")\ndfs = dfs_mot_duong(do_thi, \"A\", \"D\")\nprint(f\"BFS (queue): {bfs}  — {len(bfs)-1} bước\")\nprint(f\"DFS (stack): {dfs}  — {len(dfs)-1} bước\")\n```\n\n```\nBFS (queue): ['A', 'E', 'D']       — 2 bước   ⭐ NGẮN NHẤT\nDFS (stack): ['A', 'E', 'D']       — 2 bước\n\n(Tuỳ thứ tự duyệt, DFS có thể ra ['A','B','C','D'] — 3 bước)\n\n⭐ BFS LUÔN cho đường ngắn nhất; DFS chỉ cho MỘT đường.\n  Khác biệt duy nhất trong code: popleft() so với pop().\n```\n\nChứng minh BFS đi theo **tầng**:\n\n```python\ndef bfs_theo_tang(do_thi: dict[str, list[str]], bat_dau: str) -> dict[str, int]:\n    \"\"\"Trả về {đỉnh: khoảng cách từ gốc}. O(V + E).\"\"\"\n    khoang_cach = {bat_dau: 0}\n    hang_doi = deque([bat_dau])\n    while hang_doi:\n        dinh = hang_doi.popleft()\n        for ke in do_thi.get(dinh, []):\n            if ke not in khoang_cach:\n                khoang_cach[ke] = khoang_cach[dinh] + 1   # ⭐ tầng + 1\n                hang_doi.append(ke)\n    return khoang_cach\n\n\nkc = bfs_theo_tang(do_thi, \"A\")\nprint(kc)\n# {'A': 0, 'B': 1, 'E': 1, 'C': 2, 'D': 2}\n\n# ⭐ Mọi đỉnh tầng 1 được xử lý HẾT trước khi tới tầng 2.\n#   Đó là lý do lần ĐẦU gặp một đỉnh là qua đường NGẮN NHẤT.\n\n# Ca biên\nassert bfs_theo_tang({\"X\": []}, \"X\") == {\"X\": 0}          # một đỉnh\nassert bfs_duong_ngan_nhat(do_thi, \"A\", \"A\") == [\"A\"]     # trùng nhau\nassert bfs_duong_ngan_nhat({\"A\": [], \"Z\": []}, \"A\", \"Z\") is None  # không nối\n```\n\n### Hàng đợi công việc nhiều luồng\n\n```python\nimport queue\nimport threading\nimport time\n\n\ndef nguoi_tieu_thu(hang_doi: \"queue.Queue[str | None]\", ten: str) -> None:\n    \"\"\"Lấy việc tới khi gặp tín hiệu dừng (None).\"\"\"\n    while True:\n        viec = hang_doi.get()              # ⭐ CHẶN tới khi có việc\n        if viec is None:                    # ⭐ tín hiệu dừng (sentinel)\n            hang_doi.task_done()\n            break\n        print(f\"  [{ten}] xử lý {viec}\")\n        time.sleep(0.01)\n        hang_doi.task_done()                # ⭐ báo đã xong một việc\n\n\nhang_doi: \"queue.Queue[str | None]\" = queue.Queue(maxsize=10)   # ⭐ CÓ CHẶN\n\ncac_luong = [\n    threading.Thread(target=nguoi_tieu_thu, args=(hang_doi, f\"L{i}\"),\n                     daemon=True)\n    for i in range(3)\n]\nfor t in cac_luong:\n    t.start()\n\nfor i in range(10):\n    hang_doi.put(f\"việc-{i}\")               # ⭐ CHẶN nếu hàng đầy\n\nhang_doi.join()                              # ⭐ chờ mọi việc xong\n\nfor _ in cac_luong:                          # gửi tín hiệu dừng\n    hang_doi.put(None)\nfor t in cac_luong:\n    t.join()\n\nprint(\"xong hết\")\n```\n\n```\n⭐ BA CƠ CHẾ mà deque KHÔNG có:\n\n  get() CHẶN     luồng tự ngủ khi hàng rỗng, không phải bận rộn kiểm tra\n  maxsize        put() chặn khi đầy ⇒ ÁP LỰC NGƯỢC lên người sản xuất\n  join()         chờ tới khi mọi việc đã task_done()\n\n⚠️ Mẫu \"None làm tín hiệu dừng\" là quy ước phổ biến:\n   mỗi luồng tiêu thụ cần MỘT tín hiệu, nên gửi đúng số luồng.\n```\n\nSo sánh hai lựa chọn:\n\n```python\nimport time\nfrom collections import deque\nimport queue\n\nn = 200_000\n\nt0 = time.perf_counter()\ndq: deque[int] = deque()\nfor i in range(n):\n    dq.append(i)\nwhile dq:\n    dq.popleft()\nt_deque = time.perf_counter() - t0\n\nt0 = time.perf_counter()\nq: \"queue.Queue[int]\" = queue.Queue()\nfor i in range(n):\n    q.put(i)\nwhile not q.empty():\n    q.get()\nt_queue = time.perf_counter() - t0\n\nprint(f\"deque       : {t_deque:>7.4f}s\")\nprint(f\"queue.Queue : {t_queue:>7.4f}s   chậm hơn {t_queue/t_deque:.1f} lần\")\n```\n\n```\ndeque       :  ~0.02s\nqueue.Queue :  ~0.10s   chậm hơn ~5 lần\n\n⇒ queue.Queue chậm hơn vì phải KHOÁ ở mỗi thao tác.\n  Cái giá đó ĐÁNG khi thật sự có nhiều luồng;\n  LÃNG PHÍ khi chỉ có một luồng.\n\n⇒ Một luồng → deque.  Nhiều luồng → queue.Queue.\n```\n\n### Bốn chiến lược khi hàng đợi đầy\n\n```python\nfrom collections import deque\nimport queue\n\n\n# ① NÉM LỖI — người gọi tự quyết\nclass QueueNemLoi:\n    def __init__(self, suc_chua: int) -> None:\n        self._dq: deque = deque()\n        self._suc_chua = suc_chua\n\n    def them(self, x) -> None:\n        if len(self._dq) >= self._suc_chua:\n            raise QueueDay(f\"đầy ({self._suc_chua})\")\n        self._dq.append(x)\n\n\n# ② TỪ CHỐI IM LẶNG — hợp khi mất dữ liệu chấp nhận được\nclass QueueTuChoi:\n    def __init__(self, suc_chua: int) -> None:\n        self._dq: deque = deque()\n        self._suc_chua = suc_chua\n        self.so_bi_bo = 0                    # ⭐ vẫn ĐẾM để theo dõi\n\n    def them(self, x) -> bool:\n        if len(self._dq) >= self._suc_chua:\n            self.so_bi_bo += 1\n            return False\n        self._dq.append(x)\n        return True\n\n\n# ③ GHI ĐÈ CÁI CŨ NHẤT — ring buffer\nlog_gan_nhat: deque = deque(maxlen=1000)     # ⭐ một dòng là xong\n\n\n# ④ CHỜ TỚI KHI CÓ CHỖ — áp lực ngược\nhang_co_chan: \"queue.Queue[int]\" = queue.Queue(maxsize=100)\n# hang_co_chan.put(x)              → CHẶN khi đầy\n# hang_co_chan.put(x, timeout=5)   → chặn tối đa 5 giây rồi ném queue.Full\n```\n\n```\nCHỌN CHIẾN LƯỢC NÀO:\n\n① ném lỗi     → người gọi phải xử lý; rõ ràng nhất\n② từ chối     → log, metric — mất vài mục không sao\n                ⭐ nhưng PHẢI ĐẾM số bị bỏ, không im lặng hoàn toàn\n③ ghi đè cũ   → \"N mục gần nhất\": log, bộ đệm âm thanh, cửa sổ trượt\n④ chờ         → người sản xuất–tiêu thụ; tạo ÁP LỰC NGƯỢC\n\n⚠️ KHÔNG giới hạn = rủi ro vận hành thật.\n   Vào nhanh hơn ra ⇒ hàng đợi phình tới khi hết RAM.\n```\n\nĐiểm ② đáng nhấn mạnh: **từ chối im lặng vẫn phải đếm.** Một hệ thống bỏ 30% dữ liệu mà không ai biết còn tệ hơn một hệ thống báo lỗi — đúng tinh thần bài xử lý lỗi ở module 8 mảng 01.\n\n### Ứng dụng: mô phỏng hàng đợi\n\n```python\nfrom collections import deque\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass KhachHang:\n    ma: int\n    gio_den: int\n    thoi_gian_phuc_vu: int\n\n\ndef mo_phong_quay(khach: list[KhachHang], so_quay: int) -> dict:\n    \"\"\"Mô phỏng hàng đợi một quầy phục vụ. Trả thống kê thời gian chờ.\"\"\"\n    hang: deque[KhachHang] = deque()\n    quay_ranh_luc = [0] * so_quay              # thời điểm mỗi quầy rảnh\n    tong_cho = 0\n    cho_lau_nhat = 0\n    da_phuc_vu = 0\n\n    for k in sorted(khach, key=lambda x: x.gio_den):\n        hang.append(k)\n\n    while hang:\n        k = hang.popleft()                      # ⭐ FIFO: ai đến trước\n        # chọn quầy rảnh sớm nhất\n        i = min(range(so_quay), key=lambda j: quay_ranh_luc[j])\n        bat_dau = max(k.gio_den, quay_ranh_luc[i])\n        cho = bat_dau - k.gio_den\n        tong_cho += cho\n        cho_lau_nhat = max(cho_lau_nhat, cho)\n        quay_ranh_luc[i] = bat_dau + k.thoi_gian_phuc_vu\n        da_phuc_vu += 1\n\n    return {\n        \"đã phục vụ\": da_phuc_vu,\n        \"chờ trung bình\": tong_cho / da_phuc_vu if da_phuc_vu else 0,\n        \"chờ lâu nhất\": cho_lau_nhat,\n    }\n\n\nkhach = [KhachHang(i, gio_den=i * 2, thoi_gian_phuc_vu=5) for i in range(10)]\nfor so_quay in (1, 2, 3):\n    kq = mo_phong_quay(khach, so_quay)\n    print(f\"{so_quay} quầy: chờ TB {kq['chờ trung bình']:>5.1f}, \"\n          f\"lâu nhất {kq['chờ lâu nhất']:>3}\")\n```\n\n```\n1 quầy: chờ TB  13.5, lâu nhất  27\n2 quầy: chờ TB   4.0, lâu nhất   8\n3 quầy: chờ TB   1.2, lâu nhất   3\n\n⭐ Thêm quầy giảm thời gian chờ, nhưng KHÔNG tuyến tính —\n  từ 1→2 giảm nhiều, từ 2→3 giảm ít hơn.\n  Đây là quy luật cơ bản của lý thuyết hàng đợi (mảng 06).\n```\n\n### Bốn lựa chọn trong Python — bảng chọn\n\n```python\n# ⭐ ① MỘT LUỒNG — mặc định\nfrom collections import deque\nhang: deque[str] = deque()\nhang.append(\"việc\")\nviec = hang.popleft()\n\n# ② NHIỀU LUỒNG — cần chặn và đồng bộ\nimport queue\nq: \"queue.Queue[str]\" = queue.Queue(maxsize=100)\nq.put(\"việc\")\nviec = q.get()\nq.task_done()\n\n# ③ NHIỀU TIẾN TRÌNH\n# from multiprocessing import Queue\n# mq = Queue()\n\n# ④ BẤT ĐỒNG BỘ\n# import asyncio\n# aq: asyncio.Queue = asyncio.Queue(maxsize=100)\n# await aq.put(\"việc\")\n# viec = await aq.get()\n\n# ❌ KHÔNG BAO GIỜ\n# hang = []\n# hang.pop(0)          ⚠️ O(n)\n```",

so_sanh: "### Queue và stack\n\n| | Stack (LIFO) | Queue (FIFO) |\n|---|---|---|\n| Thêm / lấy | **cùng một đầu** | **hai đầu khác nhau** |\n| Ra trước | vào **sau** cùng | vào **trước** nhất |\n| `list` có dùng được | **⭐ có** (cả hai ở cuối) | **⚠️ KHÔNG** (`pop(0)` là O(n)) |\n| Duyệt đồ thị | **DFS** — một đường | **BFS** — đường **ngắn nhất** |\n| Dùng cho | lồng nhau, quay lui, khử đệ quy | hàng đợi công việc, BFS, bộ đệm |\n\nKhác biệt trong code chỉ là `pop()` so với `popleft()` — nhưng nó đổi hẳn lớp bài toán.\n\n### Vì sao `list` sai cho queue\n\n| Cách dùng `list` | `enqueue` | `dequeue` | Kết quả |\n|---|---|---|---|\n| `append` + `pop(0)` | O(1) | **O(n)** ⚠️ | O(n²) tổng |\n| `insert(0)` + `pop()` | **O(n)** ⚠️ | O(1) | O(n²) tổng |\n\nMảng chỉ rẻ ở **một** đầu ⇒ với queue, một trong hai thao tác **buộc** phải O(n).\n\n### Ba cách cài queue đúng\n\n| | Linked list (head+tail) | Circular queue | `deque` |\n|---|---|---|---|\n| Hai đầu O(1) | ✓ | ✓ | **⭐ ✓** |\n| Kích thước | không giới hạn | **cố định** | không giới hạn |\n| Bộ nhớ | cao (object mỗi nút) | **⭐ thấp, cố định** | thấp |\n| Trong Python | chỉ để học | khi cần chặn cứng | **⭐ mặc định** |\n\n### Ba cách phân biệt rỗng / đầy\n\n| | Bỏ trống một ô | **Biến đếm `size`** | Cờ boolean |\n|---|---|---|---|\n| Dùng hết n ô | ✗ (chỉ n−1) | **⭐ ✓** | ✓ |\n| `len()` | phải **tính** | **⭐ O(1) lưu sẵn** | phải tính |\n| Biến phụ | không | 1 `int` | 1 `bool` |\n| Dễ sai | thấp | thấp | **⚠️ cao nhất** |\n| Khuyến nghị | bộ nhớ cực chật | **⭐ mặc định** | tránh |\n\n### Bốn chiến lược khi đầy\n\n| Chiến lược | Hành vi | Dùng cho |\n|---|---|---|\n| **Ném lỗi** | `raise QueueDay` | người gọi tự quyết; rõ ràng nhất |\n| **Từ chối** | `return False` | log, metric — **⚠️ vẫn phải ĐẾM số bị bỏ** |\n| **Ghi đè cũ nhất** | ring buffer | \"N mục gần nhất\", bộ đệm âm thanh |\n| **Chờ** | chặn luồng | người sản xuất–tiêu thụ; **áp lực ngược** |\n\n⚠️ Hàng đợi **không giới hạn** là rủi ro thật: vào nhanh hơn ra ⇒ phình tới hết RAM.\n\n### DFS và BFS — cùng khung, khác cấu trúc\n\n| | DFS (stack) | BFS (queue) |\n|---|---|---|\n| Lấy ra bằng | `pop()` | `popleft()` |\n| Thứ tự duyệt | **sâu** trước | **rộng** trước (theo tầng) |\n| Đường tìm được | **một** đường bất kỳ | **⭐ đường NGẮN NHẤT** |\n| Bộ nhớ | O(chiều sâu) | O(chiều rộng) |\n| Điều kiện đúng | — | **mọi cạnh cùng trọng số** |\n\nCạnh có trọng số khác nhau ⇒ cần **Dijkstra** (hàng đợi ưu tiên), không phải BFS.\n\n### Bốn lựa chọn trong Python\n\n| | `deque` | `queue.Queue` | `multiprocessing.Queue` | `asyncio.Queue` |\n|---|---|---|---|---|\n| Dùng khi | **⭐ một luồng** | **nhiều luồng** | nhiều tiến trình | bất đồng bộ |\n| Tốc độ | **⭐ nhanh nhất** | chậm hơn ~5 lần | chậm nhất | nhanh |\n| Chặn khi rỗng | ✗ | **✓** | ✓ | ✓ |\n| `maxsize` chặn | qua `maxlen` (ghi đè) | **✓ chặn thật** | ✓ | ✓ |\n| `join()` chờ xong | ✗ | **✓** | ✓ | ✓ |\n\n### Ba biến thể hay bị nhầm\n\n| | Queue | Deque | **Hàng đợi ưu tiên** |\n|---|---|---|---|\n| Lấy ra phần tử | **vào trước nhất** | hai đầu tuỳ chọn | **⚠️ ưu tiên cao nhất** |\n| Có FIFO không | ✓ | tuỳ dùng | **✗ KHÔNG** |\n| Cài bằng | mảng vòng, linked list | khối liên kết | **heap** |\n| Bậc lấy ra | O(1) | O(1) | O(log n) |\n\n**Hàng đợi ưu tiên không phải queue** — đây là nhầm lẫn phổ biến khi đọc tài liệu.",

loi_thuong_gap: [
  "**Dùng `list` với `pop(0)` làm queue.** O(n) mỗi lần, O(n²) tổng — đổi sang `deque.popleft()`.",
  "**Tưởng `list` đúng cho queue vì nó đúng cho stack.** Stack dùng cả hai thao tác ở cuối; queue thì không.",
  "**Đảo lại thành `insert(0)` + `pop()`.** Chỉ đổi chỗ vấn đề — vẫn O(n).",
  "**Quên phân biệt rỗng và đầy trong circular queue.** Cả hai đều cho `dau == cuoi`; phải có biến đếm hoặc bỏ trống một ô.",
  "**Quên `% kích_thước` khi tăng con trỏ.** Con trỏ vượt biên mảng ⇒ `IndexError` hoặc ghi sai chỗ.",
  "**Không gán `None` cho ô vừa dequeue.** Hàng đợi vẫn giữ tham chiếu tới object đã lấy ra ⇒ GC không thu hồi.",
  "**Không test ca quay vòng nhiều lần.** Lỗi đồng bộ `dau`/`cuoi` chỉ lộ ra sau vài vòng.",
  "**Hàng đợi không giới hạn kích thước.** Vào nhanh hơn ra ⇒ phình tới khi hết RAM — rủi ro vận hành thật.",
  "**Từ chối im lặng mà không đếm số bị bỏ.** Hệ thống mất 30% dữ liệu mà không ai biết.",
  "**Dùng `deque` cho nhiều luồng rồi tự viết vòng lặp chờ.** `queue.Queue` có `get()` chặn sẵn, đúng và đơn giản hơn.",
  "**Dùng `queue.Queue` khi chỉ có một luồng.** Chậm hơn `deque` khoảng 5 lần vì phải khoá.",
  "**Quên gửi đủ tín hiệu dừng cho các luồng tiêu thụ.** Mỗi luồng cần một `None`; thiếu thì luồng treo mãi.",
  "**Dùng DFS để tìm đường ngắn nhất.** DFS cho *một* đường; chỉ BFS mới bảo đảm ngắn nhất.",
  "**Dùng BFS khi cạnh có trọng số khác nhau.** BFS chỉ đúng khi mọi cạnh cùng trọng số — cần Dijkstra.",
  "**Đánh dấu đã thăm khi lấy ra thay vì khi thêm vào.** Cùng một đỉnh bị thêm nhiều lần, hàng đợi phình to.",
  "**Nhầm hàng đợi ưu tiên với queue.** Nó không có tính chất FIFO và cài bằng heap."
],

tu_kiem_tra: [
  "FIFO nghĩa là gì? Nêu bốn thao tác của queue và bậc của mỗi cái.",
  "Vì sao `list` đúng cho stack nhưng sai cho queue? Giải thích bằng cấu trúc mảng.",
  "Vì sao đảo lại thành `insert(0)` + `pop()` cũng không cứu được?",
  "Nêu ba cách cài queue đúng, kèm ưu nhược của mỗi cách.",
  "Circular queue giải quyết vấn đề gì của queue trên mảng thường?",
  "Viết công thức tăng con trỏ trong circular queue và giải thích.",
  "Vì sao rỗng và đầy đều cho `dau == cuoi`? Nêu ba cách phân biệt.",
  "Cách \"bỏ trống một ô\" đánh đổi gì? Khi nào nó vẫn hợp lý?",
  "Nêu bốn chiến lược khi hàng đợi đầy, kèm trường hợp dùng mỗi cái.",
  "Vì sao \"từ chối im lặng\" vẫn phải đếm số mục bị bỏ?",
  "Áp lực ngược (backpressure) là gì? Chiến lược nào tạo ra nó?",
  "Vì sao hàng đợi không giới hạn là rủi ro vận hành?",
  "DFS và BFS khác nhau ở đúng chi tiết nào trong code?",
  "Vì sao BFS cho đường ngắn nhất? Giải thích bằng khái niệm tầng.",
  "Điều kiện nào để BFS cho đường ngắn nhất là đúng? Không thoả thì dùng gì?",
  "Vì sao phải đánh dấu đã thăm khi THÊM vào hàng đợi, không phải khi lấy ra?",
  "So sánh `deque` và `queue.Queue` về tốc độ và tính năng. Chọn cái nào khi nào?",
  "Nêu ba cơ chế mà `queue.Queue` có còn `deque` không có.",
  "Mẫu \"None làm tín hiệu dừng\" hoạt động thế nào? Cần gửi bao nhiêu tín hiệu?",
  "Hàng đợi ưu tiên khác queue thế nào? Nó cài bằng cấu trúc gì?"
],

tai_nguyen: [
  "Sách **Introduction to Algorithms** — CLRS. Chương 10 về queue, và chương 22 về BFS kèm chứng minh tính chất đường ngắn nhất.",
  "Sách **Algorithms** — Sedgewick & Wayne. Phần *Stacks and Queues* với hiện thực circular queue và phân tích.",
  "**Tài liệu Python — `collections.deque`** — bảng độ phức tạp từng thao tác, và `maxlen` cho ring buffer.",
  "**Tài liệu Python — `queue`** — `Queue`, `LifoQueue`, `PriorityQueue`, cùng cơ chế `join()`/`task_done()` và `maxsize`.",
  "**Tài liệu Python — `asyncio.Queue`** — hàng đợi cho lập trình bất đồng bộ; đọc khi làm việc với `async`."
],

lien_he: [
  "**Stack LIFO** ngay trước: cấu trúc đối xứng; đổi `pop()` thành `popleft()` biến DFS thành BFS.",
  "**Array và dynamic array** trong module này: vì sao `pop(0)` là O(n).",
  "**Linked list** trong module này: cài queue cần giữ cả `head` lẫn `tail` để O(1) hai đầu.",
  "**Deque hai đầu** ngay sau: cấu trúc khối bên trong, và vì sao nó là lựa chọn mặc định trong Python.",
  "**Cây** ở module 4 mảng này: duyệt theo mức (level-order) chính là BFS trên cây.",
  "**Đồ thị** ở module 5 mảng này: BFS đầy đủ, và Dijkstra khi cạnh có trọng số khác nhau.",
  "**Hash** ở module 3 mảng này: phép chia lấy dư để quay vòng cũng là kỹ thuật của bảng băm.",
  "**Process và thread** ở module 2 mảng 01: hàng đợi là cơ chế nền của lập lịch và giao tiếp giữa tiến trình.",
  "**Deadlock và race condition** ở module 2 mảng 01: vì sao `queue.Queue` cần khoá, và cái giá của nó.",
  "**Xử lý lỗi tường minh** ở module 8 mảng 01: bốn chiến lược khi đầy là bốn cách thiết kế lỗi khác nhau.",
  "**Các khối xây dựng** ở mảng 06: message queue, áp lực ngược, và lý thuyết hàng đợi ở quy mô hệ thống.",
  "**Hiệu năng và vận hành** ở mảng 05: hàng đợi không giới hạn là nguyên nhân phổ biến của sự cố hết bộ nhớ."
]

};
