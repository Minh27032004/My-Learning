window.LESSONS["01:mang-may-tinh:http-method-header-body-status-code"] = {

tom_tat: [
  "**HTTP là giao thức văn bản, không trạng thái, theo cặp request–response.** Server không tự nhớ bạn là ai giữa hai request — mọi cơ chế \"đăng nhập\" đều là thứ dựng thêm bên trên.",
  "**Method mang ý nghĩa, không mang cơ chế.** `GET` và `DELETE` về mặt kỹ thuật làm được cùng một việc; khác biệt nằm ở **hợp đồng** mà cả hệ sinh thái (cache, proxy, crawler) dựa vào.",
  "**An toàn (*safe*) và bất biến (*idempotent*) là hai tính chất khác nhau.** `PUT` không an toàn nhưng bất biến; `POST` không có cả hai. Đây là cơ sở để biết request nào retry được.",
  "**Chữ số đầu của status code là thứ cần thuộc**: 2xx thành công · 3xx chuyển hướng · 4xx **bạn sai** · 5xx **server sai**. Trả sai lớp là làm client xử lý sai.",
  "**Header là nơi diễn ra gần như toàn bộ cơ chế web**: cache, nén, xác thực, CORS, content negotiation. Body chỉ chở dữ liệu."
],

can_biet_truoc: [
  "Bài **TCP vs UDP** ngay trước — HTTP chạy trên TCP, và keep-alive là cách tránh trả giá bắt tay lặp lại.",
  "Bài **IP, port, DNS** — trình duyệt phải phân giải tên miền và nối tới cổng 80/443 trước khi nói HTTP.",
  "Bài **Mô hình TCP/IP 4 tầng** — HTTP là tầng 7, và điều đó quyết định thiết bị nào đọc được nó.",
  "Bài **Unicode, UTF-8** ở module 1 — `Content-Type: text/html; charset=utf-8` là lý do trang web không bị lỗi font."
],

dinh_nghia: "Nói đời thường: HTTP giống như **điền một tờ đơn rồi nhận lại một tờ trả lời**. Tờ đơn ghi: muốn làm gì (method), với cái gì (đường dẫn), kèm các ghi chú bên lề (header), và nội dung đính kèm nếu có (body). Tờ trả lời ghi: kết quả thế nào (status code), ghi chú bên lề, và nội dung.\n\nNói chính xác:\n\n- **HTTP** (*HyperText Transfer Protocol*) — giao thức tầng Ứng dụng theo mô hình **request–response**, chạy trên TCP (cổng 80) hoặc trên TLS (cổng 443, gọi là HTTPS).\n- **Không trạng thái** (*stateless*) — mỗi request độc lập hoàn toàn; server **không** nhớ gì về request trước đó của cùng một client.\n- **Method** — động từ chỉ ý định: `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, `HEAD`, `OPTIONS`.\n- **Header** — các cặp `Tên: giá trị` chứa metadata: kiểu dữ liệu, thông tin cache, thông tin xác thực.\n- **Body** — dữ liệu thật. Request có thể không có body; response cũng vậy.\n- **Status code** — số ba chữ số chỉ kết quả. Chữ số đầu chia thành năm lớp.\n\nĐiểm cần nắm ngay và sẽ quay lại nhiều lần: **HTTP không trạng thái là một quyết định thiết kế, không phải hạn chế.** Nhờ nó, mọi request đều xử lý được bởi bất kỳ server nào trong cụm — nền tảng của khả năng mở rộng ngang. Cái giá là ứng dụng phải tự dựng cơ chế nhận diện người dùng, và đó là chủ đề của bài kế tiếp về cookie và session.",

vi_sao_can: "HTTP là giao thức mà một backend engineer đụng tới nhiều nhất — nhiều hơn cả SQL:\n\n- **Mọi API bạn viết đều là HTTP.** Chọn method nào, trả code nào, đặt header nào — đây là toàn bộ phần \"giao diện\" của API, và là thứ người dùng API cảm nhận đầu tiên.\n- **Gỡ lỗi tích hợp.** Khi hệ thống A gọi hệ thống B không được, câu hỏi đầu tiên luôn là \"request thật gửi đi là gì, response thật nhận về là gì\". Không đọc được HTTP thô thì chỉ có thể đoán.\n- **Hiệu năng web.** Cache HTTP là tối ưu rẻ nhất và hiệu quả nhất tồn tại: đặt đúng vài header là giảm được phần lớn lưu lượng, không phải sửa một dòng code xử lý.\n- **Bảo mật.** CORS, `SameSite`, `Authorization`, `Content-Security-Policy` — đều là header. Hiểu sai một cái là mở lỗ hổng thật.\n- **Đọc tài liệu API của người khác.** Mọi tài liệu API đều mô tả bằng từ vựng của bài này. Không có nó thì đọc tài liệu như đọc ngoại ngữ.\n\nTình huống thật rất hay gặp: API của bạn xoá một bản ghi rồi trả về `200 OK` kèm body `{\"error\": \"không tìm thấy\"}`. Client của bên đối tác kiểm tra `if (status === 200) coi_nhu_thanh_cong()` và im lặng ghi nhận sai. Lỗi nằm ở **trả sai lớp status code**, và nó gây thiệt hại thật vì cả hệ sinh thái công cụ đều dựa vào chữ số đầu tiên đó. Phần cơ chế sẽ nói rõ hợp đồng của mỗi lớp.",

co_che: "### Một request và response thô\n\nHTTP/1.1 là giao thức **văn bản thuần**, đọc được bằng mắt. Đây là toàn bộ cấu trúc:\n\n```http\nGET /api/users/42?fields=name HTTP/1.1     ← dòng đầu: method, đường dẫn, phiên bản\nHost: api.example.com                       ← header (bắt buộc từ HTTP/1.1)\nAccept: application/json\nAuthorization: Bearer eyJhbGci...\nUser-Agent: curl/8.4.0\n                                            ← DÒNG TRỐNG kết thúc phần header\n(không có body với GET)\n```\n\n```http\nHTTP/1.1 200 OK                            ← dòng đầu: phiên bản, code, mô tả\nContent-Type: application/json; charset=utf-8\nContent-Length: 47\nCache-Control: max-age=300\n                                            ← DÒNG TRỐNG\n{\"id\":42,\"name\":\"Minh\",\"email\":\"m@example.com\"}\n```\n\nBa chi tiết cấu trúc có hệ quả thực tế:\n\n1. **Dòng trống là ranh giới header–body.** Đây là lý do không được có ký tự lạ hay dòng trống sớm khi tự sinh HTTP — server sẽ hiểu phần header đã kết thúc và coi phần còn lại là body.\n2. **Header `Host` là bắt buộc.** Nhiều website chạy trên cùng một IP; server dựa vào `Host` để biết bạn muốn site nào. Bỏ nó là `400 Bad Request`.\n3. **`Content-Length` cho bên nhận biết body dài bao nhiêu** để biết khi nào đọc xong — vì kết nối TCP vẫn giữ mở cho request tiếp theo (keep-alive). Sai `Content-Length` là treo kết nối.\n\n### Method — hợp đồng, không phải cơ chế\n\nĐây là ý bị hiểu sai nhiều nhất. **Về mặt kỹ thuật, mọi method đều có thể làm mọi việc.** Không có gì trong TCP ngăn `GET /delete-user/42` xoá người dùng. Method là **lời hứa**, và cả hệ sinh thái dựa vào lời hứa đó:\n\n```\nTrình duyệt: tự động thử lại GET khi mạng lỗi, KHÔNG tự thử lại POST\nProxy/CDN:   được phép cache GET, KHÔNG cache POST\nCrawler:     tự do đi theo mọi link GET\nNút Back:    tải lại trang GET, hỏi lại người dùng với POST\n```\n\nVì vậy `GET /delete-user/42` là lỗi thiết kế nghiêm trọng: một crawler đi qua trang có link đó sẽ **xoá sạch dữ liệu của bạn** mà không làm gì sai — nó chỉ tin vào hợp đồng của `GET`.\n\nHai tính chất định nghĩa hợp đồng, và cần phân biệt rõ:\n\n```\nAn toàn (safe)        không làm thay đổi gì trên server\nBất biến (idempotent) gọi 1 lần hay 10 lần cho cùng KẾT QUẢ CUỐI\n```\n\n| Method | An toàn | Bất biến | Có body | Ý nghĩa |\n|---|:---:|:---:|:---:|---|\n| `GET` | ✓ | ✓ | không | đọc |\n| `HEAD` | ✓ | ✓ | không | đọc, chỉ lấy header |\n| `OPTIONS` | ✓ | ✓ | không | hỏi server cho phép gì |\n| `PUT` | ✗ | **✓** | có | thay thế toàn bộ |\n| `DELETE` | ✗ | **✓** | không | xoá |\n| `POST` | ✗ | ✗ | có | tạo mới, hoặc mọi thứ khác |\n| `PATCH` | ✗ | ✗ | có | sửa một phần |\n\nHai dòng đáng suy nghĩ nhất:\n\n**Vì sao `DELETE` bất biến dù nó thay đổi dữ liệu?** Vì kết quả cuối như nhau: xoá bản ghi 42 mười lần thì trạng thái cuối vẫn là \"bản ghi 42 không còn\". Lần đầu trả `204`, các lần sau trả `404` — status khác nhau, nhưng **trạng thái server giống nhau**. Bất biến nói về trạng thái, không nói về response.\n\n**Vì sao `PATCH` không bất biến?** Vì `PATCH` có thể chứa phép toán tương đối: `{\"tang_diem\": 5}` gọi hai lần thì cộng 10. Nếu `PATCH` của bạn chỉ gán giá trị tuyệt đối thì nó *thực tế* bất biến, nhưng chuẩn không đảm bảo điều đó nên client không được phép giả định.\n\nGiá trị thực dụng của bảng này: **chỉ retry được request bất biến một cách an toàn**. Đây là lý do mọi thư viện HTTP retry `GET` mặc định nhưng không retry `POST` — và là lý do API tạo đơn hàng bằng `POST` cần cơ chế *idempotency key* riêng nếu muốn retry an toàn.\n\n### Status code — năm lớp\n\n```\n1xx  thông tin       hiếm gặp trực tiếp (101 khi nâng cấp lên WebSocket)\n2xx  thành công      yêu cầu đã được nhận và xử lý\n3xx  chuyển hướng    tài nguyên ở chỗ khác, hoặc cache còn dùng được\n4xx  LỖI PHÍA CLIENT bạn gửi sai — gửi lại y như vậy cũng thế thôi\n5xx  LỖI PHÍA SERVER server hỏng — thử lại sau có thể được\n```\n\nRanh giới 4xx/5xx là ranh giới quan trọng nhất trong toàn bộ HTTP, vì nó quyết định **client có nên thử lại hay không**:\n\n```\n4xx → đừng retry, hãy sửa request\n5xx → retry có nghĩa, nên retry kèm exponential backoff\n```\n\nTrả `500` cho lỗi validate dữ liệu là làm client retry vô nghĩa và làm server bạn thêm tải. Trả `400` cho lỗi database là làm client bỏ luôn thay vì thử lại — mất dữ liệu đáng lẽ cứu được.\n\nCác code cần thuộc, kèm chỗ hay dùng sai:\n\n| Code | Nghĩa | Dùng khi | Hay sai ở đâu |\n|---|---|---|---|\n| `200` | OK | GET thành công | dùng cả cho lỗi — sai nghiêm trọng |\n| `201` | Created | POST tạo xong | nên kèm header `Location` |\n| `204` | No Content | DELETE, PUT xong, không có body | trả kèm body — sai chuẩn |\n| `301` | Moved Permanently | đổi URL vĩnh viễn | **trình duyệt cache rất dai** |\n| `302`/`307` | Found / Temporary | chuyển tạm | 302 có thể đổi method, 307 thì không |\n| `304` | Not Modified | cache còn dùng được | quên nó là mất tối ưu lớn |\n| `400` | Bad Request | request sai cú pháp | dùng cho mọi lỗi — quá chung |\n| `401` | Unauthorized | **chưa xác thực** | lẫn với 403 |\n| `403` | Forbidden | **đã biết bạn là ai, nhưng không cho** | lẫn với 401 |\n| `404` | Not Found | không có tài nguyên | dùng cả khi lỗi quyền (đôi khi có chủ đích) |\n| `409` | Conflict | xung đột trạng thái, ví dụ email trùng | ít ai dùng, thường trả 400 |\n| `422` | Unprocessable | cú pháp đúng, dữ liệu vô nghĩa | thay cho 400 khi validate |\n| `429` | Too Many Requests | vượt giới hạn tần suất | nên kèm `Retry-After` |\n| `500` | Internal Error | lỗi không lường trước | dùng cho lỗi client — sai |\n| `502` | Bad Gateway | proxy không nối được backend | |\n| `503` | Unavailable | đang quá tải hoặc bảo trì | nên kèm `Retry-After` |\n| `504` | Gateway Timeout | backend trả lời quá chậm | |\n\nCặp `401`/`403` đáng nhớ bằng một câu: **401 là \"bạn là ai?\", 403 là \"tôi biết bạn là ai, và bạn không được vào\"**. Trả `401` khi token hết hạn để client biết đi làm mới token; trả `403` khi tài khoản không có quyền để client biết đừng thử lại.\n\nCặp `502`/`504` rất hữu ích khi vận hành: cả hai đều do proxy sinh ra, nhưng `502` nghĩa là **backend không nối được** (chết, sai cổng), còn `504` nghĩa là **backend còn sống nhưng quá chậm**. Hai nguyên nhân, hai cách xử lý khác nhau hoàn toàn.\n\n### Header — nơi chứa gần như toàn bộ cơ chế\n\nHeader chia thành các nhóm theo việc chúng làm:\n\n```\nMÔ TẢ NỘI DUNG\n  Content-Type       kiểu dữ liệu của body — quyết định bên kia phân tích thế nào\n  Content-Length     độ dài body theo byte\n  Content-Encoding   nén bằng gì (gzip, br)\n\nCLIENT MONG MUỐN GÌ  (content negotiation)\n  Accept             tôi muốn nhận kiểu gì\n  Accept-Language    ngôn ngữ ưu tiên\n  Accept-Encoding    tôi giải nén được những gì\n\nCACHE\n  Cache-Control      luật cache — header quan trọng nhất về hiệu năng\n  ETag               \"vân tay\" của phiên bản tài nguyên\n  Last-Modified      lần sửa cuối\n  If-None-Match      client hỏi: bản của tôi còn dùng được không?\n\nXÁC THỰC & TRẠNG THÁI\n  Authorization      Bearer <token>, hoặc Basic <base64>\n  Cookie / Set-Cookie  chủ đề của bài kế tiếp\n\nĐI QUA PROXY\n  Host               site nào — bắt buộc\n  X-Forwarded-For    IP thật của client, do proxy thêm vào\n  X-Forwarded-Proto  client dùng http hay https\n```\n\n`X-Forwarded-For` đáng chú ý ngay từ bây giờ: khi ứng dụng nằm sau reverse proxy, `IP nguồn` mà ứng dụng thấy là **IP của proxy**, không phải của người dùng. Ghi log IP mà không đọc header này là ghi ra hàng triệu dòng cùng một địa chỉ. Nhưng cũng phải cẩn thận: header này **do client gửi được**, nên chỉ tin nó khi request đến từ proxy mà bạn kiểm soát.\n\n### Cache HTTP — tối ưu rẻ nhất tồn tại\n\nHai cơ chế, và hiểu sự khác nhau là hiểu toàn bộ cache HTTP:\n\n**① Cache tươi — không cần hỏi lại gì cả**\n\n```http\nCache-Control: max-age=3600\n```\n\nTrong 3600 giây, trình duyệt dùng bản trong máy, **không gửi request nào**. Nhanh nhất có thể, vì không có request nghĩa là không có RTT.\n\n**② Xác nhận lại — hỏi \"còn dùng được không\"**\n\n```http\n→ GET /style.css\n← 200 OK\n  ETag: \"abc123\"\n  Cache-Control: max-age=60\n\n... sau 60 giây ...\n\n→ GET /style.css\n  If-None-Match: \"abc123\"      ← \"tôi đang có bản abc123\"\n← 304 Not Modified              ← KHÔNG có body ⇒ tiết kiệm băng thông\n```\n\n`304` vẫn tốn một vòng đi-về, nhưng không tốn băng thông. Với file lớn, đây là khác biệt rất đáng kể.\n\nBa giá trị `Cache-Control` cần dùng đúng:\n\n| Giá trị | Nghĩa | Dùng cho |\n|---|---|---|\n| `max-age=31536000, immutable` | cache một năm, không hỏi lại | file có hash trong tên |\n| `no-cache` | **được lưu, nhưng phải xác nhận mỗi lần** | HTML |\n| `no-store` | **cấm lưu bất kỳ đâu** | dữ liệu nhạy cảm |\n\nCặp `no-cache` / `no-store` là cặp bị nhầm nhiều nhất, và nhầm ở đây có hậu quả bảo mật thật: `no-cache` **vẫn cho phép lưu** bản sao lên đĩa. Muốn thật sự không lưu thông tin ngân hàng vào cache trình duyệt thì phải dùng `no-store`.\n\nKỹ thuật thực dụng nhất — **cache busting bằng tên file**:\n\n```html\n<!-- ❌ đổi nội dung mà tên không đổi ⇒ người dùng vẫn dùng bản cũ -->\n<script src=\"/app.js\"></script>\n\n<!-- ✓ đổi nội dung ⇒ đổi hash ⇒ đổi URL ⇒ tải bản mới ngay -->\n<script src=\"/app.a3f9c1.js\"></script>\n```\n\nVới cách này bạn được cả hai: cache một năm cho tốc độ tối đa, mà vẫn cập nhật tức thì khi deploy. Đây là lý do mọi công cụ build frontend hiện đại đều thêm hash vào tên file.\n\n### Nội dung có nghĩa gì với body\n\n```http\nContent-Type: application/json                    ← API\nContent-Type: application/x-www-form-urlencoded   ← form HTML mặc định\nContent-Type: multipart/form-data; boundary=...   ← form có tải file\nContent-Type: text/html; charset=utf-8            ← trang web\n```\n\n`charset=utf-8` không phải chi tiết trang trí: thiếu nó, trình duyệt phải đoán bảng mã, và đoán sai là tiếng Việt biến thành ký tự lạ. Đây là nối trực tiếp với bài **Unicode, UTF-8** ở module 1 — vấn đề lỗi font trên web gần như luôn quy về một trong hai: thiếu khai báo charset, hoặc khai báo không khớp với bảng mã thật của file.\n\n### HTTP/1.1, HTTP/2, HTTP/3 — cái gì đổi\n\nĐiều cần nhớ: **ngữ nghĩa không đổi, chỉ cách truyền đổi.** Method, status code, header vẫn nguyên nghĩa qua cả ba phiên bản.\n\n| | HTTP/1.1 | HTTP/2 | HTTP/3 |\n|---|---|---|---|\n| Định dạng | văn bản | **nhị phân** | nhị phân |\n| Ghép nhiều request | ✗ nối tiếp | ✓ song song trên 1 kết nối | ✓ |\n| Nén header | ✗ | ✓ HPACK | ✓ QPACK |\n| Chạy trên | TCP | TCP | **UDP (QUIC)** |\n\nHTTP/2 chuyển sang nhị phân nên **không đọc được bằng `telnet`** nữa — đây là lý do các ví dụ dạy học vẫn dùng HTTP/1.1, và cũng là lý do `curl -v` hữu ích hơn hẳn: nó dịch lại thành dạng văn bản cho người đọc.",

vi_du: "### Xem HTTP thô bằng `curl -v`\n\n```bash\ncurl -v https://example.com\n\n# > GET / HTTP/2                    ← dấu > là những gì GỬI ĐI\n# > host: example.com\n# > accept: */*\n#\n# < HTTP/2 200                      ← dấu < là những gì NHẬN VỀ\n# < content-type: text/html; charset=UTF-8\n# < cache-control: max-age=604800\n```\n\nHọc thuộc quy ước `>` là gửi, `<` là nhận — đọc log `curl` sẽ nhanh hơn nhiều. Vài cờ dùng hằng ngày:\n\n```bash\ncurl -I https://example.com                   # chỉ header (dùng HEAD)\ncurl -X POST -d '{\"a\":1}' \\\n     -H 'Content-Type: application/json' \\\n     https://api.example.com/items            # gửi JSON\ncurl -L https://example.com                   # đi theo chuyển hướng 3xx\ncurl -s -o /dev/null -w '%{http_code}\\n' URL  # chỉ lấy status code\ncurl --compressed https://example.com         # xin nén gzip\n```\n\nCờ `-w '%{http_code}'` rất tiện trong script health check: nó in đúng một con số, dễ so sánh trong `if`.\n\n### Nói HTTP bằng tay\n\nCách tốt nhất để thấm rằng HTTP là văn bản thuần:\n\n```bash\nprintf 'GET / HTTP/1.1\\r\\nHost: example.com\\r\\nConnection: close\\r\\n\\r\\n' \\\n  | nc example.com 80\n```\n\nBốn chi tiết trong lệnh này đều quan trọng:\n\n```\n\\r\\n        HTTP yêu cầu kết thúc dòng bằng CRLF, không phải \\n\nHost:      bắt buộc từ HTTP/1.1, thiếu là 400\nConnection: close   yêu cầu server đóng sau khi trả lời,\n                    nếu không nc sẽ treo chờ request tiếp (keep-alive)\n\\r\\n\\r\\n    dòng trống cuối kết thúc phần header\n```\n\nThiếu dòng trống cuối là lỗi kinh điển: server sẽ chờ mãi vì tưởng bạn còn header chưa gửi hết. Đây là bài tập nên làm một lần — nó biến HTTP từ khái niệm thành thứ cụ thể.\n\n### Chọn method và status code cho một API\n\n```\nGET    /api/users           → 200 + danh sách\nGET    /api/users/42        → 200 + một user, hoặc 404\nPOST   /api/users           → 201 + Location: /api/users/43\nPUT    /api/users/42        → 200 (thay TOÀN BỘ, thiếu trường = xoá trường đó)\nPATCH  /api/users/42        → 200 (sửa MỘT PHẦN, chỉ trường có mặt)\nDELETE /api/users/42        → 204 lần đầu, 404 các lần sau\n```\n\nSự khác nhau giữa `PUT` và `PATCH` có hệ quả trực tiếp, và đây là chỗ gây lỗi dữ liệu thật:\n\n```bash\n# Dữ liệu hiện tại: {\"name\":\"Minh\", \"email\":\"m@x.com\", \"tuoi\":22}\n\n# PUT — thay toàn bộ\ncurl -X PUT -d '{\"name\":\"Minh Tran\"}' .../users/42\n# ⇒ {\"name\":\"Minh Tran\"}          ← email và tuoi BIẾN MẤT\n\n# PATCH — sửa một phần\ncurl -X PATCH -d '{\"name\":\"Minh Tran\"}' .../users/42\n# ⇒ {\"name\":\"Minh Tran\", \"email\":\"m@x.com\", \"tuoi\":22}\n```\n\nRất nhiều API tự nhận là `PUT` nhưng hành xử như `PATCH`. Nó \"chạy được\", nhưng phá hợp đồng: client cẩn thận sẽ gửi đủ mọi trường khi dùng `PUT`, và nếu server thực ra là `PATCH` thì hai bên vẫn hiểu nhau — nhưng khi client dựa vào `PUT` để **xoá một trường bằng cách bỏ nó ra**, hành vi sẽ khác hoàn toàn với mong đợi.\n\n### Trả status code đúng cho từng tình huống\n\n```python\n# ❌ Sai: mọi thứ 200, lỗi nhét trong body\n@app.post(\"/users\")\ndef create(data):\n    if not data.get(\"email\"):\n        return {\"ok\": False, \"error\": \"thiếu email\"}, 200   # client không biết đây là lỗi\n    ...\n\n# ✓ Đúng: status code nói kết quả, body nói chi tiết\n@app.post(\"/users\")\ndef create(data):\n    if not data.get(\"email\"):\n        return {\"error\": \"thiếu email\", \"field\": \"email\"}, 422\n    if db.exists(email=data[\"email\"]):\n        return {\"error\": \"email đã tồn tại\"}, 409\n    try:\n        user = db.create(data)\n    except DatabaseError:\n        return {\"error\": \"lỗi hệ thống\"}, 500      # ⇒ client BIẾT nên retry\n    return user, 201, {\"Location\": f\"/users/{user.id}\"}\n```\n\nBa lợi ích cụ thể của việc trả đúng code, không phải chuyện \"cho đúng chuẩn\":\n\n```\n① Thư viện HTTP tự retry 5xx và KHÔNG retry 4xx — miễn phí, không cần code\n② Hệ thống giám sát đếm tỉ lệ 5xx làm chỉ báo sức khoẻ — trả 200 là làm mù nó\n③ Người dùng API đọc code biết ngay lỗi của ai, không phải đọc body đoán\n```\n\nĐiểm ② đáng nhấn: nếu API trả `200` cho mọi lỗi, biểu đồ giám sát sẽ mãi mãi xanh trong khi hệ thống đang hỏng. Đây không phải lý thuyết — đó là cách một sự cố nghiêm trọng bị phát hiện muộn.\n\n### Chẩn đoán vấn đề cache\n\n```bash\n# Xem server nói gì về cache\ncurl -I https://example.com/style.css\n# cache-control: max-age=604800\n# etag: \"abc123\"\n\n# Mô phỏng lần truy cập thứ hai — mong đợi 304\ncurl -I -H 'If-None-Match: \"abc123\"' https://example.com/style.css\n# HTTP/2 304          ← đúng, cache đang hoạt động\n\n# Bỏ qua mọi cache để lấy bản mới nhất\ncurl -H 'Cache-Control: no-cache' https://example.com/\n```\n\nBảng chẩn đoán \"đã deploy mà người dùng vẫn thấy bản cũ\":\n\n| Hiện tượng | Nguyên nhân | Sửa |\n|---|---|---|\n| Ctrl+F5 thì mới | cache trình duyệt | thêm hash vào tên file |\n| Máy khác vẫn cũ | cache ở CDN | purge CDN, xem lại `max-age` |\n| Chỉ HTML cũ, asset mới | HTML bị cache | HTML dùng `no-cache` |\n\nQuy tắc rút ra rất đáng thuộc: **HTML thì `no-cache`, asset có hash thì `max-age` dài**. HTML là chỗ trỏ tới asset, nên nó phải luôn tươi; asset đã có hash trong tên thì không bao giờ cần hỏi lại.\n\n### Chuyển hướng và cạm bẫy của `301`\n\n```bash\ncurl -I http://example.com\n# HTTP/1.1 301 Moved Permanently\n# Location: https://example.com/\n\ncurl -L -v http://example.com 2>&1 | grep -E '^(< HTTP|> GET)'\n# > GET / HTTP/1.1\n# < HTTP/1.1 301\n# > GET / HTTP/2          ← curl tự đi theo Location\n# < HTTP/2 200\n```\n\nCảnh báo thực dụng: **`301` được trình duyệt cache rất dai, có khi vĩnh viễn.** Trả `301` sai một lần là người dùng bị chuyển sai mãi, và bạn không có cách nào gọi lại. Khi chưa chắc chắn, dùng `302` hoặc `307` — sửa lại được.\n\nKhác biệt `302` và `307` cũng đáng biết: `302` cho phép client đổi `POST` thành `GET` khi đi theo (nhiều client làm vậy vì lý do lịch sử), còn `307` **bắt buộc giữ nguyên method**. Với API nhận `POST`, chuyển hướng bằng `302` có thể làm mất body — dùng `307`.",

so_sanh: "### An toàn và bất biến\n\n| Method | An toàn | Bất biến | Cache được | Retry được |\n|---|:---:|:---:|:---:|:---:|\n| `GET` | ✓ | ✓ | ✓ | ✓ |\n| `HEAD` | ✓ | ✓ | ✓ | ✓ |\n| `PUT` | ✗ | ✓ | ✗ | ✓ |\n| `DELETE` | ✗ | ✓ | ✗ | ✓ |\n| `POST` | ✗ | ✗ | ✗ | **✗** |\n| `PATCH` | ✗ | ✗ | ✗ | ✗ |\n\nCột \"retry được\" là cột có giá trị vận hành cao nhất: nó cho biết thư viện HTTP có được phép tự thử lại hay không. `POST` không retry được là lý do API tạo đơn hàng cần *idempotency key* — client sinh một khoá duy nhất, server ghi nhớ, gặp lại khoá đó thì trả kết quả cũ thay vì tạo đơn thứ hai.\n\n### `PUT` và `PATCH`\n\n| | `PUT` | `PATCH` |\n|---|---|---|\n| Phạm vi | **toàn bộ** tài nguyên | chỉ trường được gửi |\n| Trường thiếu | bị **xoá** / về mặc định | giữ nguyên |\n| Bất biến | ✓ | ✗ (chuẩn không đảm bảo) |\n| Body | trạng thái đầy đủ mong muốn | phần thay đổi |\n\nChọn thế nào: **`PATCH` cho form sửa một phần** (đổi mật khẩu, đổi avatar), **`PUT` khi client thật sự nắm toàn bộ trạng thái** (đồng bộ từ hệ thống khác). Sai chỗ này gây mất dữ liệu âm thầm — đúng như ví dụ ở trên.\n\n### `401` và `403`\n\n| | `401 Unauthorized` | `403 Forbidden` |\n|---|---|---|\n| Nghĩa | **chưa biết bạn là ai** | biết rồi, nhưng không cho |\n| Nguyên nhân | thiếu/sai/hết hạn token | thiếu quyền |\n| Client nên làm gì | **đăng nhập lại, làm mới token** | dừng, hiện thông báo |\n| Header kèm | `WWW-Authenticate` | — |\n\nHệ quả thực dụng: client thường tự động làm mới token khi gặp `401`. Trả `403` cho token hết hạn là làm client không tự phục hồi được, và người dùng bị bắt đăng nhập lại thủ công. Ngược lại, trả `401` cho lỗi thiếu quyền làm client rơi vào vòng lặp làm mới token vô nghĩa.\n\n### `no-cache`, `no-store`, `max-age=0`\n\n| | Lưu lên đĩa | Phải xác nhận | Dùng cho |\n|---|:---:|:---:|---|\n| `max-age=0` | ✓ | ✓ | gần như tương đương `no-cache` |\n| `no-cache` | **✓** | ✓ | HTML |\n| `no-store` | **✗** | — | dữ liệu nhạy cảm |\n\nDòng khác biệt duy nhất mà cũng là dòng quan trọng nhất: `no-cache` **vẫn ghi bản sao xuống đĩa**. Trang xem số dư tài khoản dùng `no-cache` là để lại dữ liệu trong cache trình duyệt trên máy dùng chung. Đúng phải là `no-store`.\n\n### Trạng thái: HTTP so với các giao thức có trạng thái\n\n| | HTTP (không trạng thái) | Giao thức có trạng thái (ví dụ FTP, SSH) |\n|---|---|---|\n| Server nhớ client | không | có, suốt phiên |\n| Mở rộng ngang | **dễ — request nào cũng vào server nào cũng được** | khó, phải dính server cũ |\n| Chịu lỗi | server chết, request sau vào server khác | mất phiên, phải làm lại |\n| Chi phí | mỗi request phải gửi lại thông tin nhận dạng | gửi một lần |\n\nĐây là đánh đổi thiết kế cốt lõi của web, và nó đã thắng: khả năng đặt bất kỳ request lên bất kỳ server nào là **nền tảng của mọi kiến trúc mở rộng ngang** — load balancer, autoscaling, blue-green deployment đều dựa vào tính chất này. Cái giá là mỗi request phải mang theo thông tin nhận dạng, và cách mang thông tin đó chính là chủ đề bài kế tiếp.",

loi_thuong_gap: [
  "**Dùng `GET` cho hành động thay đổi dữ liệu.** Crawler và trình duyệt tự đi theo link `GET`; `GET /delete/42` là dữ liệu bị xoá bởi một con bot vô tội.",
  "**Trả `200` cho lỗi, nhét lỗi vào body.** Client không biết đó là lỗi, hệ thống giám sát mãi xanh trong khi hệ thống đang hỏng.",
  "**Trả `500` cho lỗi validate.** Client sẽ retry vô nghĩa và tăng tải. Lỗi dữ liệu là `400` hoặc `422`.",
  "**Trả `400` cho lỗi database.** Client sẽ bỏ luôn thay vì thử lại, làm mất dữ liệu đáng lẽ cứu được. Lỗi server là `5xx`.",
  "**Lẫn `401` với `403`.** Token hết hạn phải là `401` để client tự làm mới; thiếu quyền là `403` để client dừng.",
  "**Cài `PUT` mà hành xử như `PATCH`.** Client gửi thiếu trường mong đợi trường đó bị xoá, nhưng server giữ nguyên — sai lệch âm thầm.",
  "**Dùng `no-cache` cho dữ liệu nhạy cảm.** Nó vẫn lưu xuống đĩa. Cần `no-store`.",
  "**Đổi nội dung file mà không đổi tên.** Người dùng vẫn dùng bản cũ tới hết `max-age`. Thêm hash vào tên file.",
  "**Cache HTML với `max-age` dài.** HTML là chỗ trỏ tới asset, nên phải luôn tươi. HTML dùng `no-cache`.",
  "**Dùng `301` khi chưa chắc.** Trình duyệt cache rất dai, sửa lại rất khó. Dùng `302`/`307` cho tới khi chắc chắn.",
  "**Chuyển hướng `POST` bằng `302`.** Client có thể đổi thành `GET` và mất body. Dùng `307`.",
  "**Ghi log `IP nguồn` khi ứng dụng nằm sau proxy.** Sẽ thấy toàn IP của proxy. Đọc `X-Forwarded-For`, nhưng chỉ tin khi request đến từ proxy mình kiểm soát.",
  "**Quên `charset=utf-8` trong `Content-Type`.** Trình duyệt phải đoán bảng mã, và tiếng Việt sẽ lỗi."
],

tu_kiem_tra: [
  "HTTP không trạng thái nghĩa là gì? Nêu một lợi ích lớn và một cái giá phải trả.",
  "Phân biệt *an toàn* và *bất biến*. Vì sao `DELETE` bất biến dù nó thay đổi dữ liệu?",
  "Vì sao `GET /delete-user/42` là lỗi thiết kế nghiêm trọng, chứ không chỉ là \"không đẹp\"?",
  "Ranh giới `4xx`/`5xx` quyết định điều gì ở phía client? Nêu hậu quả của việc trả sai lớp.",
  "Khi nào trả `401`, khi nào trả `403`? Client xử lý hai code này khác nhau thế nào?",
  "`PUT` và `PATCH` khác nhau ra sao với các trường không được gửi lên?",
  "Phân biệt `no-cache` và `no-store`. Cái nào dùng cho trang xem số dư tài khoản, và vì sao?",
  "Vì sao \"thêm hash vào tên file\" cho phép cache một năm mà vẫn cập nhật tức thì khi deploy?",
  "`502` và `504` đều do proxy sinh ra — mỗi cái nói gì về tình trạng backend?",
  "Vì sao `POST` không retry được tự động, và cơ chế nào giúp `POST` retry an toàn?",
  "Header `Host` để làm gì, và vì sao HTTP/1.1 bắt buộc nó?"
],

tai_nguyen: [
  "Tài liệu **MDN Web Docs**, mục HTTP — tra cứu từng header và từng status code. Đây là nguồn tra cứu hằng ngày, không phải sách đọc một lượt.",
  "Sách **HTTP: The Definitive Guide** — David Gourley & Brian Totty. Cũ nhưng phần cache và proxy vẫn là giải thích tốt nhất tồn tại.",
  "Sách **High Performance Browser Networking** — Ilya Grigorik. Đọc miễn phí trực tuyến; chương về HTTP/2 và về cache rất thực dụng.",
  "Tài liệu `man curl` — dài nhưng đáng đọc mục về `-w` và các biến định dạng, dùng cho script chẩn đoán.",
  "Sách **RESTful Web APIs** — Leonard Richardson & Mike Amundsen. Về thiết kế API dựa trên đúng ngữ nghĩa HTTP."
],

lien_he: [
  "**Cookie, session và trạng thái trên web** ngay sau: giải quyết đúng cái giá của tính không trạng thái mà bài này nêu ra.",
  "**HTTPS, TLS handshake** ngay sau nữa: HTTPS là HTTP chạy trên TLS — ngữ nghĩa không đổi, chỉ thêm mã hoá.",
  "**TCP vs UDP** ngay trước: keep-alive tránh bắt tay lặp lại, và head-of-line blocking là lý do HTTP/2 vẫn chưa đủ.",
  "**WebSocket vs polling**: `101 Switching Protocols` là cách WebSocket khởi đầu từ một request HTTP thường.",
  "**Proxy, reverse proxy, CDN** ở cuối module: CDN hoạt động được nhờ đúng các header cache của bài này.",
  "**Unicode, UTF-8** ở module 1: `charset=utf-8` là mắt nối trực tiếp giữa hai bài.",
  "**Backend & Web** ở mảng 05: thiết kế REST API là bài này áp dụng có hệ thống — chọn method, chọn status code, đặt header.",
  "**Database** ở mảng 04: `409 Conflict` thường xuất phát từ ràng buộc unique của database.",
  "**System Design** ở mảng 06: tính không trạng thái của HTTP là nền tảng của mở rộng ngang và autoscaling."
]

};
