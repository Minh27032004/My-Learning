window.LESSONS["01:lap-trinh-huong-doi-tuong:solid-open-closed"] = {

tom_tat: [
  "**Mở cho mở rộng, đóng cho sửa đổi**: thêm hành vi mới bằng cách **viết thêm code**, không phải bằng cách sửa code đã chạy đúng.",
  "**Dấu hiệu vi phạm rõ nhất là chuỗi `if/elif` theo loại** — mỗi lần thêm loại là mở đúng file đó ra sửa, và bỏ sót một chỗ là lỗi im lặng.",
  "**Cơ chế đạt được OCP là điểm mở rộng (*extension point*)**: một giao diện cộng một chỗ đăng ký. Code cũ gọi qua giao diện, code mới cắm vào.",
  "**Không thể mở cho mọi hướng thay đổi.** Chọn một trục — loại phí, loại định dạng, loại thanh toán — và mở cho trục đó; các trục khác vẫn phải sửa.",
  "**Đừng dựng điểm mở rộng khi mới có một biến thể.** OCP đáng làm khi bạn đã sửa cùng một chỗ **lần thứ hai** vì cùng một lý do."
],

can_biet_truoc: [
  "Bài **SOLID — Single Responsibility** ngay trước — phải tách được trách nhiệm rồi mới mở rộng được từng phần.",
  "Bài **Đa hình và duck typing** — thay chuỗi `if/elif` bằng đa hình là cơ chế chính của bài này.",
  "Bài **Trừu tượng: ABC, Protocol** — giao diện là \"phần đóng\", các bản cài đặt là \"phần mở\".",
  "Bài **Composition over inheritance** — điểm mở rộng thường là một thành phần được truyền vào."
],

dinh_nghia: "Nói đời thường: một ổ cắm điện trên tường là **đóng** — bạn không đục tường mỗi lần mua thiết bị mới. Nhưng nó **mở**: cắm được quạt, đèn, máy tính, và cả những thiết bị chưa ra đời khi người ta xây nhà.\n\nNói chính xác:\n\n- **OCP** (*Open/Closed Principle*) — một đơn vị code nên **mở cho việc mở rộng** và **đóng với việc sửa đổi**.\n- **Mở cho mở rộng** — thêm hành vi mới được, bằng cách viết code mới.\n- **Đóng với sửa đổi** — code đã chạy đúng và đã kiểm thử thì không phải mở ra sửa.\n- **Điểm mở rộng** (*extension point*) — chỗ được thiết kế sẵn để cắm hành vi mới: một giao diện, một tham số nhận hàm, một bảng đăng ký.\n- **Trục thay đổi** (*axis of change*) — hướng mà bạn dự đoán hệ thống sẽ mở rộng. Mỗi thiết kế chỉ mở được vài trục.\n\nĐiểm cần nói rõ ngay: **\"đóng\" không có nghĩa là file không bao giờ được sửa.** Sửa lỗi thì vẫn sửa. Nó có nghĩa là **thêm một loại mới không đòi hỏi sửa code cũ** — và đó là khác biệt quan trọng, vì mỗi lần sửa code đang chạy đúng là một lần có nguy cơ làm hỏng nó.",

vi_sao_can: "OCP là nguyên tắc trả lời câu hỏi \"thêm tính năng mới tốn bao nhiêu\":\n\n- **Sửa code đang chạy là rủi ro.** Code đã qua kiểm thử, đã chạy trong sản phẩm nhiều tháng. Mỗi lần mở ra sửa là một lần có thể làm hỏng thứ vốn đang tốt.\n- **Chuỗi `if/elif` lan ra nhiều nơi.** Cùng một danh sách loại xuất hiện ở bốn hàm khác nhau, và thêm loại thứ năm nghĩa là tìm đủ bốn chỗ. Bỏ sót một chỗ là lỗi im lặng.\n- **Nó quyết định ai mở rộng được hệ thống.** Có điểm mở rộng thì người dùng thư viện của bạn thêm được hành vi riêng mà không cần sửa mã nguồn của bạn — và không cần chờ bạn phát hành bản mới.\n- **Kiểm thử không phải chạy lại toàn bộ.** Thêm một class mới thì chỉ cần kiểm thử class đó; sửa một hàm lớn thì phải kiểm thử lại mọi nhánh của nó.\n- **Nền cho kiến trúc plugin.** Mọi hệ thống cho phép cắm thêm module đều dựng trên nguyên tắc này.\n\nTình huống thật rất kinh điển: hàm `tinh_phi_van_chuyen` có `if phuong_thuc == \"thuong\": ... elif \"nhanh\": ... elif \"hoa_toc\": ...`. Marketing yêu cầu thêm \"giao trong ngày\". Bạn sửa hàm đó, và phát hiện cùng chuỗi `if` còn nằm ở `uoc_tinh_thoi_gian`, `kiem_tra_kha_dung`, và `hien_thi_tuy_chon`. Bốn chỗ, và bạn quên chỗ thứ tư — nên phương thức mới không hiện ra cho khách chọn, mà không có lỗi nào báo. Phần cơ chế sẽ chỉ cách thiết kế để thêm phương thức vận chuyển là **viết một class, sửa không dòng nào**.",

co_che: "### Vi phạm điển hình: chuỗi `if/elif`\n\n```python\n# ❌ Thêm loại mới ⇒ SỬA hàm này\ndef tinh_phi_van_chuyen(don, phuong_thuc: str) -> float:\n    if phuong_thuc == \"thuong\":\n        return 30_000\n    elif phuong_thuc == \"nhanh\":\n        return 50_000 + don.can_nang * 5_000\n    elif phuong_thuc == \"hoa_toc\":\n        return 100_000 + don.khoang_cach * 3_000\n    else:\n        raise ValueError(f\"không hỗ trợ: {phuong_thuc}\")\n```\n\nBa vấn đề, và vấn đề thứ ba nghiêm trọng nhất:\n\n```\n① Thêm loại ⇒ sửa hàm đã kiểm thử và đang chạy đúng\n② Hàm phình dần theo số loại\n③ CÙNG chuỗi if đó lặp ở nhiều hàm khác — và bỏ sót một chỗ\n   là lỗi IM LẶNG\n```\n\nĐiểm ③ là chỗ OCP tạo ra khác biệt lớn nhất trong thực tế:\n\n```python\ndef uoc_tinh_thoi_gian(phuong_thuc):\n    if phuong_thuc == \"thuong\": return 5\n    elif phuong_thuc == \"nhanh\": return 2\n    elif phuong_thuc == \"hoa_toc\": return 1\n\ndef kiem_tra_kha_dung(phuong_thuc, tinh):\n    if phuong_thuc == \"hoa_toc\":\n        return tinh in (\"HN\", \"HCM\")\n    return True\n\ndef hien_thi_tuy_chon():\n    return [\"thuong\", \"nhanh\", \"hoa_toc\"]      # ⚠️ danh sách thứ tư\n```\n\nBốn nơi cùng biết về danh sách loại. Đây là **kiến thức bị nhân bản**, và mỗi bản sao là một chỗ có thể quên cập nhật.\n\n### Cách đạt OCP: giao diện + đa hình\n\n```python\nfrom typing import Protocol\n\nclass PhuongThucVanChuyen(Protocol):\n    ma: str\n    ten_hien_thi: str\n    def tinh_phi(self, don) -> float: ...\n    def uoc_tinh_ngay(self) -> int: ...\n    def kha_dung(self, tinh: str) -> bool: ...\n\n\nclass GiaoThuong:\n    ma, ten_hien_thi = \"thuong\", \"Giao tiêu chuẩn\"\n    def tinh_phi(self, don): return 30_000\n    def uoc_tinh_ngay(self): return 5\n    def kha_dung(self, tinh): return True\n\nclass GiaoNhanh:\n    ma, ten_hien_thi = \"nhanh\", \"Giao nhanh\"\n    def tinh_phi(self, don): return 50_000 + don.can_nang * 5_000\n    def uoc_tinh_ngay(self): return 2\n    def kha_dung(self, tinh): return True\n\nclass GiaoHoaToc:\n    ma, ten_hien_thi = \"hoa_toc\", \"Hoả tốc\"\n    def tinh_phi(self, don): return 100_000 + don.khoang_cach * 3_000\n    def uoc_tinh_ngay(self): return 1\n    def kha_dung(self, tinh): return tinh in (\"HN\", \"HCM\")\n```\n\nBốn hàm cũ trở thành **bốn phương thức của cùng một class** — nên mọi kiến thức về một loại nằm gọn một chỗ:\n\n```python\ndef tinh_phi(don, pt: PhuongThucVanChuyen) -> float:\n    return pt.tinh_phi(don)                     # ⭐ không if nào\n```\n\nThêm loại mới:\n\n```python\nclass GiaoTrongNgay:\n    ma, ten_hien_thi = \"trong_ngay\", \"Giao trong ngày\"\n    def tinh_phi(self, don): return 70_000\n    def uoc_tinh_ngay(self): return 0\n    def kha_dung(self, tinh): return tinh == \"HCM\"\n```\n\n**Không sửa dòng nào của code cũ**, và **không thể quên** một khía cạnh nào — vì giao diện liệt kê đủ ba phương thức, thiếu cái nào thì `mypy` báo.\n\nĐây là lợi ích tinh tế nhưng quan trọng nhất: OCP không chỉ giảm số chỗ phải sửa, nó còn **biến \"quên cập nhật\" từ lỗi im lặng thành lỗi bắt được**.\n\n### Bảng đăng ký — điểm mở rộng thứ hai\n\nVẫn còn một chỗ phải sửa: nơi ánh xạ từ mã sang class.\n\n```python\n# Vẫn vi phạm nhẹ — thêm loại phải sửa dict này\nBANG = {\"thuong\": GiaoThuong, \"nhanh\": GiaoNhanh, \"hoa_toc\": GiaoHoaToc}\n\ndef tao(ma: str) -> PhuongThucVanChuyen:\n    if ma not in BANG:\n        raise ValueError(f\"không hỗ trợ {ma!r}, có: {sorted(BANG)}\")\n    return BANG[ma]()\n```\n\nSo với `if/elif` thì đây đã tốt hơn nhiều — chỉ **một** chỗ thay vì bốn. Nhưng vẫn mở được thêm bằng decorator đăng ký:\n\n```python\nBANG: dict[str, type] = {}\n\ndef dang_ky(cls):\n    \"\"\"Decorator tự đăng ký class vào bảng.\"\"\"\n    if cls.ma in BANG:\n        raise ValueError(f\"mã {cls.ma!r} đã được đăng ký\")\n    BANG[cls.ma] = cls\n    return cls                          # ⭐ trả nguyên class\n\n@dang_ky\nclass GiaoThuong:\n    ma = \"thuong\"\n    ...\n\n@dang_ky\nclass GiaoTrongNgay:                    # ⭐ thêm loại: không sửa gì cả\n    ma = \"trong_ngay\"\n    ...\n```\n\nGiờ **thêm một class là xong** — kể cả từ một file khác, kể cả từ một gói do người khác viết. Đây chính là cách các framework cho phép cắm plugin, và nó là bài decorator ở module 5 áp dụng vào thiết kế.\n\nCái giá phải nói rõ: bảng đăng ký chỉ được điền khi module chứa class **được import**. Nếu không ai import thì class không có trong bảng — một lỗi khó hiểu. Vì vậy hệ thống plugin thật thường có bước quét thư mục và import tự động.\n\n### Ba mức mở rộng\n\n```\n① if/elif                sửa 1 hàm cho mỗi loại mới, và lặp ở nhiều hàm\n② Giao diện + bảng tra   sửa 1 dòng (bảng) cho mỗi loại mới\n③ Giao diện + đăng ký    sửa 0 dòng — chỉ thêm file\n```\n\nMức ② đã đủ tốt cho phần lớn dự án và đơn giản hơn. Mức ③ đáng khi **người khác** cần mở rộng hệ thống của bạn mà không sửa mã nguồn.\n\n### Không thể mở cho mọi hướng\n\nĐây là giới hạn quan trọng nhất và hay bị bỏ qua:\n\n```python\nclass PhuongThucVanChuyen(Protocol):\n    def tinh_phi(self, don) -> float: ...\n```\n\nThiết kế này **mở** cho trục \"thêm phương thức vận chuyển\". Nhưng nếu ngày mai cần `tinh_phi(don, khuyen_mai)` thì **mọi bản cài đặt đều phải sửa** — trục đó không được mở.\n\n```\nMỞ cho:      thêm một LOẠI mới\nKHÔNG mở cho: đổi CHỮ KÝ của giao diện\n              thêm một PHƯƠNG THỨC vào giao diện\n              đổi cách các phần ghép với nhau\n```\n\nHệ quả thực dụng: **chọn trục để mở, và chọn đúng.** Chọn sai trục thì bạn có một tầng trừu tượng không giúp gì mà vẫn tốn công.\n\nCách chọn: nhìn lại lịch sử. **Thay đổi nào đã xảy ra nhiều lần?** Nếu trong sáu tháng qua bạn thêm ba phương thức vận chuyển và chưa bao giờ đổi chữ ký, thì trục \"thêm loại\" là trục đáng mở.\n\n### Đừng mở rộng sớm\n\n```python\n# ❌ Mới có MỘT loại, đã dựng cả bộ khung\nclass BoTinhPhi(Protocol):\n    def tinh(self, don) -> float: ...\n\nclass TinhPhiTieuChuan:\n    def tinh(self, don): return 30_000\n\nBANG = {\"tieu_chuan\": TinhPhiTieuChuan}\n\n# ...và sẽ mãi chỉ có một loại\n```\n\nBa tầng cho một hằng số. Tệ hơn: **trừu tượng dựng từ một ví dụ thường sai hình dạng**, đúng như đã nói ở bài trừu tượng — khi loại thứ hai xuất hiện, nó không vừa với giao diện bạn đoán.\n\nQuy tắc thực dụng:\n\n```\nLần 1: viết thẳng\nLần 2: sao chép, hoặc thêm một if — CHẤP NHẬN ĐƯỢC\nLần 3: giờ đã thấy đủ điểm chung ⇒ rút ra giao diện\n```\n\nDấu hiệu đã tới lúc: **bạn mở đúng file đó ra sửa lần thứ hai vì cùng một lý do.**\n\n### Ba cách tạo điểm mở rộng\n\n**① Giao diện + đa hình** — cho hành vi phức tạp, nhiều phương thức:\n\n```python\nclass ChinhSach(Protocol):\n    def ap_dung(self, x): ...\n    def mo_ta(self) -> str: ...\n```\n\n**② Truyền hàm vào** — cho hành vi đơn giản, một thao tác:\n\n```python\ndef xu_ly(ds, bien_doi=None, loc=None):\n    if loc: ds = [x for x in ds if loc(x)]\n    if bien_doi: ds = [bien_doi(x) for x in ds]\n    return ds\n\nsorted(ds, key=lambda x: x.gia)      # ⭐ key= chính là điểm mở rộng\n```\n\n**③ Decorator** — cho việc bọc quanh hành vi có sẵn:\n\n```python\n@ghi_log\n@thu_lai(3)\ndef goi_api(): ...\n```\n\nCách chọn:\n\n```\nMột thao tác đơn giản        → truyền hàm (nhẹ nhất)\nNhiều phương thức liên quan  → giao diện\nThêm hành vi quanh cái có sẵn → decorator\n```\n\nRất nhiều người dùng giao diện cho thứ chỉ cần một hàm. `sorted(ds, key=...)` là ví dụ đẹp: nó mở cho **mọi** cách sắp xếp mà không cần một `Protocol` nào.\n\n### OCP và kế thừa\n\nPhát biểu gốc của OCP dùng kế thừa: \"đóng\" là class cha, \"mở\" là các lớp con. Nhưng bài composition đã cho thấy kế thừa mang theo ràng buộc chặt.\n\n```python\n# Cách cũ — kế thừa\nclass BoTinhPhi:\n    def tinh(self, don): raise NotImplementedError\n\nclass TinhPhiNhanh(BoTinhPhi): ...\n\n# ⭐ Cách hiện đại — composition + giao diện cấu trúc\nclass BoTinhPhi(Protocol):\n    def tinh(self, don) -> float: ...\n\nclass TinhPhiNhanh:              # không kế thừa gì\n    def tinh(self, don): ...\n```\n\nCách thứ hai đạt cùng mục tiêu OCP mà **không ép người mở rộng phải kế thừa class của bạn** — nên class từ thư viện khác cũng cắm vào được.\n\n### Khi vi phạm OCP là chấp nhận được\n\nCân bằng lại:\n\n```\n✓ Hai nhánh cố định, chắc chắn không thêm  → một if là rõ nhất\n✓ Nguyên mẫu, script một lần               → không cần khung\n✓ Danh sách loại do NGÔN NGỮ quy định      → ví dụ xử lý các kiểu dựng sẵn\n✓ Trục thay đổi chưa rõ                    → đợi\n```\n\nMột `if/else` hai nhánh rõ ràng **tốt hơn** hai class và một giao diện. OCP là công cụ cho chỗ thật sự hay thay đổi, không phải luật áp cho mọi `if`.",

vi_du: "### Chiết khấu — từ `if` sang điểm mở rộng\n\n```python\n# ❌ TRƯỚC\ndef tinh_chiet_khau(don, loai_kh: str) -> float:\n    if loai_kh == \"thuong\":\n        return 0\n    elif loai_kh == \"bac\":\n        return don.tong * 0.05\n    elif loai_kh == \"vang\":\n        return don.tong * 0.1\n    elif loai_kh == \"kim_cuong\":\n        return don.tong * 0.15 if don.tong > 1_000_000 else don.tong * 0.12\n    raise ValueError(loai_kh)\n\n# Và cùng danh sách đó ở ba hàm khác:\ndef nguong_len_hang(loai_kh): ...\ndef quyen_loi(loai_kh): ...\ndef mau_the(loai_kh): ...\n```\n\n```python\n# ✓ SAU — mọi kiến thức về một hạng nằm trong một class\nfrom typing import Protocol\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass DonHang:\n    tong: float\n    so_mon: int = 1\n\n\nclass HangKhachHang(Protocol):\n    ma: str\n    ten: str\n    nguong_len_hang: float\n    def chiet_khau(self, don: DonHang) -> float: ...\n    def quyen_loi(self) -> list[str]: ...\n\n\nBANG: dict[str, type] = {}\n\ndef dang_ky(cls):\n    BANG[cls.ma] = cls\n    return cls\n\n\n@dang_ky\nclass Thuong:\n    ma, ten, nguong_len_hang = \"thuong\", \"Thường\", 5_000_000\n    def chiet_khau(self, don): return 0.0\n    def quyen_loi(self): return []\n\n@dang_ky\nclass Bac:\n    ma, ten, nguong_len_hang = \"bac\", \"Bạc\", 20_000_000\n    def chiet_khau(self, don): return don.tong * 0.05\n    def quyen_loi(self): return [\"Miễn phí giao hàng\"]\n\n@dang_ky\nclass Vang:\n    ma, ten, nguong_len_hang = \"vang\", \"Vàng\", 50_000_000\n    def chiet_khau(self, don): return don.tong * 0.10\n    def quyen_loi(self): return [\"Miễn phí giao hàng\", \"Đổi trả 30 ngày\"]\n\n@dang_ky\nclass KimCuong:\n    ma, ten, nguong_len_hang = \"kim_cuong\", \"Kim cương\", float(\"inf\")\n    def chiet_khau(self, don):\n        return don.tong * (0.15 if don.tong > 1_000_000 else 0.12)\n    def quyen_loi(self):\n        return [\"Miễn phí giao hàng\", \"Đổi trả 60 ngày\", \"Hỗ trợ riêng\"]\n\n\ndef tao_hang(ma: str) -> HangKhachHang:\n    if ma not in BANG:\n        raise ValueError(f\"không có hạng {ma!r}, có: {sorted(BANG)}\")\n    return BANG[ma]()\n\ndef tinh_thanh_tien(don: DonHang, hang: HangKhachHang) -> float:\n    return don.tong - hang.chiet_khau(don)\n```\n\nThêm hạng mới — **không sửa dòng nào**:\n\n```python\n@dang_ky\nclass BachKim:\n    ma, ten, nguong_len_hang = \"bach_kim\", \"Bạch kim\", 100_000_000\n    def chiet_khau(self, don):\n        return don.tong * 0.20 + (50_000 if don.so_mon > 5 else 0)\n    def quyen_loi(self):\n        return [\"Miễn phí giao hàng\", \"Đổi trả 90 ngày\", \"Quà sinh nhật\"]\n\n# Mọi hàm hiện có tự dùng được\nfor ma in sorted(BANG):\n    h = tao_hang(ma)\n    print(f\"{h.ten:<12} {h.chiet_khau(DonHang(1_000_000)):>12,.0f}đ  \"\n          f\"{len(h.quyen_loi())} quyền lợi\")\n```\n\nĐiểm quan trọng hơn việc bớt `if`: **bạn không thể quên** một khía cạnh nào. Nếu `BachKim` thiếu `quyen_loi`, `mypy` báo ngay — trong khi ở bản `if/elif`, quên thêm nhánh vào hàm `quyen_loi` là lỗi im lặng.\n\n### Điểm mở rộng bằng hàm — nhẹ hơn\n\n```python\n# ❌ Dùng giao diện cho thứ chỉ cần một hàm\nclass BoSapXep(Protocol):\n    def khoa(self, sp): ...\n\nclass SapTheoGia:\n    def khoa(self, sp): return sp.gia\n\nclass SapTheoTen:\n    def khoa(self, sp): return sp.ten\n\ndef sap_xep(ds, bo: BoSapXep):\n    return sorted(ds, key=bo.khoa)\n\n# ✓ Python đã có sẵn điểm mở rộng\nsorted(ds, key=lambda sp: sp.gia)\nsorted(ds, key=lambda sp: (-sp.gia, sp.ten))     # ⭐ tổ hợp tuỳ ý\n\nfrom operator import attrgetter\nsorted(ds, key=attrgetter(\"gia\"))\n```\n\n`key=` là điểm mở rộng **mở hơn** mọi giao diện bạn viết: nó nhận mọi cách sắp xếp, kể cả những cách bạn không nghĩ tới. Đây là lý do nên xem hàm-là-tham-số trước khi dựng `Protocol`.\n\nÁp dụng vào một quy trình xử lý:\n\n```python\nfrom typing import Callable\n\ndef xu_ly_ban_ghi(\n    ds: list[dict],\n    loc: Callable[[dict], bool] | None = None,\n    bien_doi: Callable[[dict], dict] | None = None,\n    khoa_sap_xep: Callable[[dict], object] | None = None,\n) -> list[dict]:\n    kq = ds\n    if loc is not None:\n        kq = [x for x in kq if loc(x)]\n    if bien_doi is not None:\n        kq = [bien_doi(x) for x in kq]\n    if khoa_sap_xep is not None:\n        kq = sorted(kq, key=khoa_sap_xep)\n    return kq\n\nxu_ly_ban_ghi(\n    du_lieu,\n    loc=lambda b: b[\"diem\"] >= 8,\n    bien_doi=lambda b: {**b, \"xep_loai\": \"giỏi\"},\n    khoa_sap_xep=lambda b: -b[\"diem\"],\n)\n```\n\nHàm này mở cho **mọi** cách lọc, biến đổi, sắp xếp — mà chỉ có ba tham số. Dùng `None` làm mặc định thay vì hàm rỗng là mẫu đã học ở module 5: nó phân biệt được \"không truyền\" với \"truyền một hàm luôn đúng\".\n\n### Cùng chuỗi `if` lặp nhiều nơi — lỗi im lặng\n\n```python\n# ❌ Thêm \"trong_ngay\" và quên một chỗ\ndef tinh_phi(pt):\n    if pt == \"thuong\": return 30_000\n    elif pt == \"nhanh\": return 50_000\n    elif pt == \"trong_ngay\": return 70_000      # ✓ nhớ sửa\n\ndef uoc_tinh_ngay(pt):\n    if pt == \"thuong\": return 5\n    elif pt == \"nhanh\": return 2\n    elif pt == \"trong_ngay\": return 0            # ✓ nhớ sửa\n\ndef hien_thi_tuy_chon():\n    return [\"thuong\", \"nhanh\"]                   # ⚠️ QUÊN — không có lỗi nào\n```\n\nHậu quả: phương thức mới tính phí đúng, ước tính đúng, nhưng **không bao giờ hiện ra cho khách chọn**. Không exception, không cảnh báo — chỉ là một tính năng lặng lẽ không tồn tại.\n\n```python\n# ✓ Với bảng đăng ký, danh sách tự đầy đủ\ndef hien_thi_tuy_chon() -> list[tuple[str, str]]:\n    return [(cls.ma, cls.ten_hien_thi) for cls in BANG.values()]\n\n# Thêm class ⇒ tự xuất hiện ở mọi nơi dùng BANG\n```\n\nĐây là lợi ích của việc **có một nguồn sự thật duy nhất** cho danh sách loại — và đó chính là điều OCP mang lại.\n\n### Xác thực dữ liệu — điểm mở rộng dạng danh sách\n\n```python\nfrom typing import Protocol\n\nclass QuyTac(Protocol):\n    def kiem(self, gt: str) -> str | None:\n        \"\"\"Trả None nếu hợp lệ, hoặc thông báo lỗi.\"\"\"\n\nclass KhongRong:\n    def kiem(self, gt):\n        return None if gt.strip() else \"không được rỗng\"\n\nclass DoDaiToiThieu:\n    def __init__(self, n: int): self.n = n\n    def kiem(self, gt):\n        return None if len(gt) >= self.n else f\"cần ít nhất {self.n} ký tự\"\n\nclass CoChuSo:\n    def kiem(self, gt):\n        return None if any(c.isdigit() for c in gt) else \"cần có chữ số\"\n\nclass KhongTrongDanhSachCam:\n    def __init__(self, cam: set[str]): self.cam = cam\n    def kiem(self, gt):\n        return None if gt.lower() not in self.cam else \"giá trị bị cấm\"\n\n\nclass BoXacThuc:\n    def __init__(self, *quy_tac: QuyTac):\n        self.quy_tac = list(quy_tac)\n\n    def them(self, qt: QuyTac) -> \"BoXacThuc\":     # ⭐ mở rộng lúc chạy\n        self.quy_tac.append(qt)\n        return self\n\n    def kiem(self, gt: str) -> list[str]:\n        return [loi for qt in self.quy_tac\n                if (loi := qt.kiem(gt)) is not None]     # ⭐ walrus\n\n\nmat_khau = BoXacThuc(KhongRong(), DoDaiToiThieu(8), CoChuSo())\nprint(mat_khau.kiem(\"abc\"))\n# ['cần ít nhất 8 ký tự', 'cần có chữ số']\n\n# Thêm quy tắc mới — không sửa BoXacThuc, không sửa quy tắc cũ\nclass CoKyTuDacBiet:\n    def kiem(self, gt):\n        return None if any(not c.isalnum() for c in gt) else \"cần ký tự đặc biệt\"\n\nmat_khau.them(CoKyTuDacBiet())\nprint(mat_khau.kiem(\"abcdefg1\"))     # ['cần ký tự đặc biệt']\n```\n\nMẫu \"danh sách quy tắc\" này là một trong những ứng dụng OCP hữu dụng nhất: nó mở cho **cả việc thêm loại quy tắc lẫn việc tổ hợp chúng khác nhau cho từng trường**.\n\nChú ý toán tử walrus trong comprehension — đúng mẫu đã học ở module 5: gọi `qt.kiem(gt)` một lần thay vì hai.\n\n### Xuất báo cáo — so sánh ba mức\n\n```python\n# Mức ① — if/elif\ndef xuat(du_lieu, dinh_dang):\n    if dinh_dang == \"json\": ...\n    elif dinh_dang == \"csv\": ...\n\n# Mức ② — giao diện + bảng tra (sửa 1 dòng cho mỗi loại)\nBANG = {\"json\": XuatJSON, \"csv\": XuatCSV}\n\n# Mức ③ — giao diện + đăng ký (sửa 0 dòng)\n@dang_ky\nclass XuatXML: ...\n```\n\nMức ③ đầy đủ, kèm cách nạp plugin:\n\n```python\nfrom typing import Protocol\nimport importlib, pkgutil\n\nclass BoXuat(Protocol):\n    ma: str\n    duoi: str\n    def xuat(self, du_lieu: list[dict]) -> str: ...\n\nBANG_XUAT: dict[str, type] = {}\n\ndef dang_ky_xuat(cls):\n    if cls.ma in BANG_XUAT:\n        raise ValueError(f\"mã {cls.ma!r} bị trùng với {BANG_XUAT[cls.ma].__name__}\")\n    BANG_XUAT[cls.ma] = cls\n    return cls\n\n@dang_ky_xuat\nclass XuatJSON:\n    ma, duoi = \"json\", \".json\"\n    def xuat(self, du_lieu):\n        import json\n        return json.dumps(du_lieu, ensure_ascii=False, indent=2)\n\n@dang_ky_xuat\nclass XuatCSV:\n    ma, duoi = \"csv\", \".csv\"\n    def xuat(self, du_lieu):\n        import csv, io\n        if not du_lieu: return \"\"\n        buf = io.StringIO()\n        w = csv.DictWriter(buf, fieldnames=list(du_lieu[0]))\n        w.writeheader(); w.writerows(du_lieu)\n        return buf.getvalue()\n\n\ndef nap_plugin(ten_goi: str) -> None:\n    \"\"\"Import mọi module trong gói để chúng tự đăng ký.\"\"\"\n    goi = importlib.import_module(ten_goi)\n    for _, ten, _ in pkgutil.iter_modules(goi.__path__):\n        importlib.import_module(f\"{ten_goi}.{ten}\")     # ⭐ import ⇒ @dang_ky chạy\n\n\ndef xuat(du_lieu: list[dict], ma: str) -> str:\n    if ma not in BANG_XUAT:\n        raise ValueError(f\"không hỗ trợ {ma!r}, có: {sorted(BANG_XUAT)}\")\n    return BANG_XUAT[ma]().xuat(du_lieu)\n```\n\nHàm `nap_plugin` giải quyết đúng cạm bẫy đã nêu: **class chỉ vào bảng khi module chứa nó được import.** Không có bước quét này, người dùng đặt plugin vào thư mục mà nó không bao giờ được đăng ký — và thông báo lỗi \"không hỗ trợ\" chẳng gợi ý gì về nguyên nhân.\n\nKiểm tra trùng mã trong `dang_ky_xuat` cũng quan trọng: hai plugin cùng mã thì cái sau ghi đè cái trước im lặng.\n\n### Decorator làm điểm mở rộng\n\n```python\nimport functools, time\n\ndef thu_lai(so_lan=3, cho=1.0):\n    def deco(ham):\n        @functools.wraps(ham)\n        def bao(*a, **k):\n            for lan in range(so_lan):\n                try:\n                    return ham(*a, **k)\n                except (ConnectionError, TimeoutError):\n                    if lan == so_lan - 1: raise\n                    time.sleep(cho * 2 ** lan)\n        return bao\n    return deco\n\ndef do_thoi_gian(ham):\n    @functools.wraps(ham)\n    def bao(*a, **k):\n        t = time.perf_counter()\n        try:\n            return ham(*a, **k)\n        finally:\n            print(f\"{ham.__name__}: {time.perf_counter()-t:.3f}s\")\n    return bao\n\n\n# Thêm hành vi mà KHÔNG sửa hàm gốc\n@do_thoi_gian\n@thu_lai(3)\ndef goi_api(url: str) -> dict:\n    ...\n```\n\nHàm `goi_api` **không biết** nó đang được đo thời gian và thử lại. Thêm một hành vi nữa (ghi log, giới hạn tần suất) là thêm một dòng decorator — không sửa thân hàm.\n\nĐây là dạng OCP nhẹ nhất và thường bị bỏ qua: không cần class, không cần giao diện.\n\n### Trục thay đổi — chọn đúng\n\n```python\n# Thiết kế MỞ cho trục \"thêm phương thức thanh toán\"\nclass CongThanhToan(Protocol):\n    def thanh_toan(self, so_tien: float) -> str: ...\n\n# ✓ Thêm loại — không sửa gì\nclass ViDienTu:\n    def thanh_toan(self, so_tien): ...\n\n# ✗ Nhưng nếu cần thêm tiền tệ vào chữ ký:\nclass CongThanhToan(Protocol):\n    def thanh_toan(self, so_tien: float, tien_te: str) -> str: ...\n#                                        ⚠️ MỌI bản cài đặt phải sửa\n```\n\nCách giảm rủi ro trục thứ hai: **gói tham số vào một object**.\n\n```python\n@dataclass(frozen=True)\nclass YeuCauThanhToan:\n    so_tien: float\n    tien_te: str = \"VND\"\n    ma_khuyen_mai: str = \"\"          # ⭐ thêm trường không phá chữ ký\n\nclass CongThanhToan(Protocol):\n    def thanh_toan(self, yc: YeuCauThanhToan) -> str: ...\n```\n\nThêm trường vào `YeuCauThanhToan` **không phá** bản cài đặt nào — chúng chỉ dùng những trường chúng cần. Đây là kỹ thuật đáng nhớ khi thiết kế giao diện sẽ có nhiều bản cài đặt bên ngoài.\n\n### Đo lợi ích\n\n```bash\n# Trước: đếm số chỗ phải sửa khi thêm một loại\ngrep -rn 'phuong_thuc ==' .\n#   giao_hang.py:12\n#   bao_cao.py:45\n#   api.py:88\n#   giao_dien.py:23\n# ⇒ 4 chỗ, và bỏ sót một chỗ là lỗi im lặng\n\n# Sau: 1 file mới, 0 dòng sửa\ngrep -rn 'phuong_thuc ==' .\n# (không có kết quả)\n```\n\nPhép đo thực dụng nhất cho OCP: **số chỗ phải sửa khi thêm một biến thể.** Lớn hơn một là còn chỗ cải thiện; bằng không là đã đạt.",

so_sanh: "### Ba mức mở rộng\n\n| | `if/elif` | Giao diện + bảng tra | Giao diện + đăng ký |\n|---|---|---|---|\n| Sửa khi thêm loại | **nhiều hàm** | 1 dòng | **0 dòng** |\n| Bỏ sót một khía cạnh | **✓ lỗi im lặng** | `mypy` bắt | `mypy` bắt |\n| Người ngoài mở rộng | ✗ | ✗ | **✓** |\n| Độ phức tạp | thấp nhất | vừa | cao nhất |\n| Cạm bẫy riêng | kiến thức nhân bản | — | **class phải được import** |\n| Phù hợp | 2–3 nhánh cố định | **đa số dự án** | hệ thống plugin |\n\nCột giữa là điểm cân bằng tốt cho phần lớn trường hợp: đủ mở, mà không có cạm bẫy \"module chưa được import\".\n\n### Ba cách tạo điểm mở rộng\n\n| | Truyền hàm | Giao diện | Decorator |\n|---|---|---|---|\n| Số phương thức | **1** | nhiều | bọc quanh |\n| Trạng thái | qua closure | **✓ tự nhiên** | qua closure |\n| Độ nhẹ | **nhẹ nhất** | nặng hơn | vừa |\n| Tổ hợp nhiều cái | qua `lambda` | qua danh sách | **✓ xếp chồng** |\n| Ví dụ | `sorted(key=...)` | `Protocol` | `@thu_lai` |\n\nRất nhiều người dùng giao diện cho thứ chỉ cần một hàm. Hỏi trước: **điểm mở rộng này có bao nhiêu thao tác?** Một → truyền hàm.\n\n### Mở cho trục nào\n\n| Thay đổi | Có mở không |\n|---|---|\n| Thêm một **loại** mới | **✓** |\n| Đổi cài đặt của một loại | ✓ (chỉ sửa class đó) |\n| Thêm **phương thức** vào giao diện | **✗ mọi bản cài đặt phải sửa** |\n| Đổi **chữ ký** phương thức | **✗** |\n| Đổi cách các phần ghép nhau | ✗ |\n\nBa hàng cuối là giới hạn thật của OCP. Kỹ thuật giảm rủi ro: **gói tham số vào một object** — thêm trường vào `dataclass` không phá bản cài đặt nào.\n\n### Khi nào áp dụng\n\n| Tình huống | Áp dụng OCP? |\n|---|---|\n| Đã sửa cùng chỗ **lần thứ hai** vì cùng lý do | **✓ đúng lúc** |\n| Đã có 3 biến thể trở lên | ✓ |\n| Người ngoài cần mở rộng | **✓ dùng đăng ký** |\n| Mới có 1 biến thể | **✗ đợi** |\n| Hai nhánh cố định, không thêm | ✗ một `if` rõ hơn |\n| Nguyên mẫu, script một lần | ✗ |\n\nNhịp áp dụng: **lần 1 viết thẳng, lần 2 chấp nhận một `if`, lần 3 rút giao diện.** Dựng khung từ một ví dụ thường cho giao diện sai hình dạng.\n\n### OCP qua kế thừa và qua composition\n\n| | Kế thừa (cách gốc) | `Protocol` + composition |\n|---|---|---|\n| Người mở rộng phải kế thừa | **✓** | ✗ |\n| Class thư viện khác dùng được | ✗ | **✓** |\n| Chia sẻ code chung | **✓** | qua hàm hoặc class nền tuỳ chọn |\n| Ràng buộc | chặt | **lỏng** |\n\nPhát biểu gốc của OCP dùng kế thừa, nhưng trong Python hiện đại thì `Protocol` đạt cùng mục tiêu với ràng buộc lỏng hơn.\n\n### Lợi ích ngoài việc bớt `if`\n\n| Lợi ích | Vì sao |\n|---|---|\n| Không thể **quên** một khía cạnh | giao diện liệt kê đủ, `mypy` bắt thiếu |\n| Một nguồn sự thật cho danh sách loại | bảng đăng ký thay cho nhiều `if` rời |\n| Kiểm thử chỉ class mới | không phải chạy lại mọi nhánh của hàm lớn |\n| Người ngoài mở rộng được | không cần sửa mã nguồn, không cần chờ phát hành |\n\nHàng đầu quan trọng hơn việc bớt `if`: nó biến **lỗi im lặng thành lỗi bắt được**.",

loi_thuong_gap: [
  "**Để cùng một chuỗi `if/elif` theo loại lặp ở nhiều hàm.** Thêm loại là phải tìm đủ mọi chỗ; bỏ sót một chỗ là lỗi im lặng.",
  "**Dựng giao diện khi mới có một biến thể.** Trừu tượng từ một ví dụ thường sai hình dạng; biến thể thứ hai không vừa.",
  "**Dùng `Protocol` cho điểm mở rộng chỉ cần một hàm.** `key=lambda ...` nhẹ hơn và mở hơn.",
  "**Quên rằng class chỉ vào bảng đăng ký khi module được import.** Plugin đặt đúng chỗ mà không bao giờ hoạt động.",
  "**Không kiểm tra trùng mã khi đăng ký.** Plugin sau ghi đè plugin trước im lặng.",
  "**Tưởng OCP mở cho mọi hướng.** Thêm phương thức vào giao diện vẫn buộc mọi bản cài đặt phải sửa.",
  "**Thêm tham số vào chữ ký giao diện đã có nhiều bản cài đặt.** Gói tham số vào một `dataclass` thay vì đổi chữ ký.",
  "**Áp OCP cho hai nhánh cố định.** Một `if/else` rõ ràng tốt hơn hai class và một giao diện.",
  "**Dùng kế thừa làm điểm mở rộng khi `Protocol` đủ.** Ép người mở rộng phải kế thừa class của bạn.",
  "**Để một khía cạnh nằm ngoài giao diện.** Ví dụ: `tinh_phi` trong class nhưng `hien_thi_tuy_chon` vẫn là danh sách cứng.",
  "**Dựng ba tầng trừu tượng cho một hằng số.** Phức tạp hơn mà không mở gì.",
  "**Chọn sai trục để mở.** Mở cho hướng chưa bao giờ thay đổi, còn hướng hay đổi thì vẫn phải sửa."
],

tu_kiem_tra: [
  "Phát biểu OCP là gì? \"Đóng với sửa đổi\" có nghĩa là file không bao giờ được sửa không?",
  "Vì sao chuỗi `if/elif` theo loại vi phạm OCP? Vấn đề nào của nó nghiêm trọng nhất?",
  "OCP biến loại lỗi nào từ \"im lặng\" thành \"bắt được\"? Bằng cơ chế gì?",
  "Nêu ba mức mở rộng và số chỗ phải sửa của mỗi mức khi thêm một loại.",
  "Cạm bẫy riêng của bảng đăng ký là gì, và cách xử lý?",
  "Vì sao phải kiểm tra trùng mã khi đăng ký?",
  "OCP mở cho những thay đổi nào và **không** mở cho những thay đổi nào?",
  "Kỹ thuật nào giảm rủi ro khi giao diện cần thêm tham số về sau?",
  "Nêu ba cách tạo điểm mở rộng. Chọn cái nào khi điểm mở rộng chỉ có một thao tác?",
  "`sorted(ds, key=...)` là điểm mở rộng kiểu gì? Vì sao nó \"mở hơn\" một `Protocol` tự viết?",
  "Nhịp áp dụng OCP là gì? Dấu hiệu nào cho biết đã tới lúc?",
  "Vì sao `Protocol` + composition tốt hơn kế thừa cho OCP trong Python hiện đại?",
  "Nêu ba trường hợp vi phạm OCP là chấp nhận được.",
  "Phép đo nào cho biết thiết kế đã đạt OCP?"
],

tai_nguyen: [
  "Sách **Clean Architecture** — Robert C. Martin. Chương về OCP giải thích ý \"trục thay đổi\" và giới hạn của nguyên tắc.",
  "Sách **Design Patterns** — Gamma và cộng sự. Strategy và Template Method là hai mẫu trực tiếp phục vụ OCP.",
  "Sách **Refactoring** — Martin Fowler. Phép \"Replace Conditional with Polymorphism\" là đúng quy trình chuyển từ `if/elif` sang đa hình.",
  "Sách **Practical Object-Oriented Design** — Sandi Metz. Chương về quản lý phụ thuộc và về thiết kế cho thay đổi.",
  "Tài liệu Python, mục *Entry points* trong Packaging User Guide — cơ chế chuẩn để plugin tự đăng ký mà không cần quét thư mục."
],

lien_he: [
  "**SOLID — Single Responsibility** ngay trước: phải tách được trách nhiệm rồi mới mở rộng từng phần độc lập được.",
  "**SOLID — Liskov Substitution** ngay sau: các bản cài đặt phải thay thế nhau được thì OCP mới thật sự hoạt động.",
  "**SOLID — Dependency Inversion**: code cũ gọi qua giao diện chứ không gọi thẳng bản cài đặt — điều kiện của OCP.",
  "**Đa hình và duck typing**: thay `if/elif` bằng đa hình là cơ chế chính của bài này.",
  "**Trừu tượng: ABC, Protocol**: giao diện là \"phần đóng\", và lời khuyên \"đợi tới bản thứ hai\" áp cho cả hai bài.",
  "**Design pattern — Strategy, Factory** ngay sau: hai mẫu này là OCP được đặt tên và chuẩn hoá.",
  "**Decorator** ở module 5: dạng điểm mở rộng nhẹ nhất, không cần class.",
  "**Hàm, tham số mặc định** ở module 5: truyền hàm vào là điểm mở rộng nhẹ nhất; mẫu `None` mặc định cũng từ đó.",
  "**Module, package, import** ở module 5: bảng đăng ký chỉ đầy khi module được import — cạm bẫy chính của mức ③.",
  "**Backend & Web** ở mảng 05: middleware và hệ thống plugin của framework đều dựng trên nguyên tắc này.",
  "**AI/ML** ở mảng 07: đổi giữa các thuật toán, các bộ tối ưu, các hàm mất mát — mỗi thứ là một điểm mở rộng."
]

};
