window.LESSONS["01:lap-trinh-huong-doi-tuong:solid-interface-segregation"] = {

tom_tat: [
  "**Không ép ai phụ thuộc vào thứ họ không dùng.** Giao diện lớn buộc người cài đặt phải làm cả những phương thức vô nghĩa với họ, và buộc người gọi phụ thuộc vào những thứ họ không cần.",
  "**Giao diện lớn là nguyên nhân, vi phạm Liskov là hậu quả.** Bắt `KhoChiDoc` cài `luu()` thì nó chỉ còn cách ném lỗi — hai bài nối thẳng vào nhau.",
  "**Chia giao diện theo NĂNG LỰC, không theo class.** Một class cài đặt nhiều giao diện nhỏ là bình thường và tốt.",
  "**Giao diện nhỏ làm chữ ký hàm tự tài liệu**: `def sao_luu(nguon: DocDuoc, dich: GhiDuoc)` nói rõ hàm cần gì mà không phải đọc thân hàm.",
  "**Chia quá nhỏ cũng là lỗi.** Mỗi giao diện một phương thức thì bạn có hàng chục `Protocol` mà không thêm thông tin gì."
],

can_biet_truoc: [
  "Bài **SOLID — Liskov Substitution** ngay trước — cách sửa vi phạm Liskov thường chính là tách giao diện.",
  "Bài **Trừu tượng: ABC, Protocol** — `Protocol` là công cụ chính để tạo giao diện nhỏ trong Python.",
  "Bài **SOLID — Single Responsibility** — giao diện lớn thường là dấu hiệu class phía sau có nhiều trách nhiệm.",
  "Bài **Đa hình và duck typing** — Python vốn không cần khai báo interface, nên ISP có sắc thái riêng ở đây."
],

dinh_nghia: "Nói đời thường: một cái điều khiển từ xa có bốn mươi nút, trong đó bạn dùng ba nút. Ba mươi bảy nút còn lại không giúp gì — chúng chỉ làm bạn khó tìm nút cần, và mỗi lần hãng đổi bố cục là bạn phải học lại.\n\nNói chính xác:\n\n- **ISP** (*Interface Segregation Principle*) — **không client nào bị ép phụ thuộc vào phương thức nó không dùng**.\n- **Client** — ở đây là **cả hai phía**: người **cài đặt** giao diện, và người **gọi** qua giao diện.\n- **Giao diện béo** (*fat interface*) — giao diện có nhiều phương thức, phục vụ nhiều nhóm nhu cầu khác nhau.\n- **Giao diện theo vai trò** (*role interface*) — giao diện nhỏ mô tả **một năng lực**: đọc được, ghi được, đóng được.\n- **Phụ thuộc không cần thiết** — module bị buộc phải biên dịch lại, phải sửa, hoặc phải hiểu một thứ nó không dùng.\n\nĐiểm cần nắm ngay và là sắc thái riêng của Python: **ở ngôn ngữ có `interface` bắt buộc, ISP nói về người cài đặt; ở Python với duck typing, nó nói nhiều hơn về người GỌI.** Một hàm nhận `KhoLuuTru` đầy đủ trong khi chỉ gọi `doc()` là đang đòi hỏi nhiều hơn nó cần — và điều đó thu hẹp vô cớ những gì có thể truyền vào.",

vi_sao_can: "ISP là nguyên tắc quyết định giao diện của bạn dễ dùng hay khó dùng:\n\n- **Giao diện lớn sinh ra vi phạm Liskov.** Bắt một bản cài đặt cung cấp phương thức nó không làm được thì nó chỉ còn cách ném lỗi hoặc để trống — cả hai đều phá giao ước.\n- **Nó thu hẹp vô cớ những gì truyền vào được.** Hàm chỉ đọc dữ liệu mà đòi cả giao diện ghi thì `tuple`, generator, kho chỉ đọc đều bị loại — dù chúng hoàn toàn dùng được.\n- **Nó làm kiểm thử nặng nề.** Class giả phải cài đặt đủ mười phương thức trong khi bài kiểm thử chỉ dùng hai.\n- **Nó lan thay đổi.** Thêm một phương thức vào giao diện lớn là **mọi** bản cài đặt phải sửa, kể cả những cái không liên quan.\n- **Chữ ký hàm mất tính tự tài liệu.** `def xu_ly(kho: Kho)` không nói gì; `def xu_ly(kho: DocDuoc)` nói rõ hàm chỉ đọc.\n\nTình huống thật rất kinh điển: bạn định nghĩa `MayIn` với `in_tai_lieu`, `quet`, `fax`, `gui_email`. Rồi công ty mua một máy in đơn giản chỉ biết in. Class `MayInDonGian` buộc phải cài đủ bốn phương thức, và ba trong số đó ném `NotImplementedError`. Mọi hàm nhận `MayIn` giờ có thể đổ tuỳ máy được truyền vào — dù chúng chỉ gọi `in_tai_lieu`. Phần cơ chế sẽ chỉ cách chia bốn năng lực đó và mỗi hàm chỉ đòi đúng thứ nó dùng.",

co_che: "### Giao diện béo và hậu quả\n\n```python\nfrom abc import ABC, abstractmethod\n\nclass MayIn(ABC):                       # ❌ giao diện béo\n    @abstractmethod\n    def in_tai_lieu(self, tl): ...\n    @abstractmethod\n    def quet(self, tl): ...\n    @abstractmethod\n    def fax(self, tl, so): ...\n    @abstractmethod\n    def gui_email(self, tl, dc): ...\n\n\nclass MayInDaNang(MayIn):\n    def in_tai_lieu(self, tl): ...\n    def quet(self, tl): ...\n    def fax(self, tl, so): ...\n    def gui_email(self, tl, dc): ...\n\nclass MayInDonGian(MayIn):\n    def in_tai_lieu(self, tl): ...\n    def quet(self, tl): raise NotImplementedError(\"máy này không quét\")   # ❌\n    def fax(self, tl, so): raise NotImplementedError                       # ❌\n    def gui_email(self, tl, dc): raise NotImplementedError                 # ❌\n```\n\nBa hậu quả, và chúng nối vào ba bài trước:\n\n```\n① Vi phạm LISKOV — MayInDonGian không thay được MayIn ở mọi chỗ\n② Người CÀI ĐẶT bị ép viết ba phương thức vô nghĩa\n③ Người GỌI phụ thuộc vào bốn năng lực dù chỉ dùng một\n```\n\nHậu quả ③ ít được nhắc nhưng quan trọng:\n\n```python\ndef in_bao_cao(may: MayIn, bc) -> None:\n    may.in_tai_lieu(bc)            # ⭐ chỉ dùng MỘT phương thức\n#                ↑ nhưng chữ ký đòi hỏi CẢ BỐN\n```\n\nHàm này **không thể** nhận một object chỉ biết in — dù đó là tất cả những gì nó cần.\n\n### Chia theo năng lực\n\n```python\nfrom typing import Protocol\n\nclass CoTheIn(Protocol):\n    def in_tai_lieu(self, tl) -> None: ...\n\nclass CoTheQuet(Protocol):\n    def quet(self, tl) -> bytes: ...\n\nclass CoTheFax(Protocol):\n    def fax(self, tl, so: str) -> None: ...\n\n\nclass MayInDonGian:                     # ⭐ chỉ cài cái nó làm được\n    def in_tai_lieu(self, tl): ...\n\nclass MayInDaNang:                      # ⭐ cài nhiều giao diện\n    def in_tai_lieu(self, tl): ...\n    def quet(self, tl): ...\n    def fax(self, tl, so): ...\n\n\ndef in_bao_cao(may: CoTheIn, bc) -> None:       # ⭐ đòi ĐÚNG thứ cần\n    may.in_tai_lieu(bc)\n\ndef sao_chep(may: CoTheIn & CoTheQuet, tl) -> None:   # cần cả hai\n    may.in_tai_lieu(may.quet(tl))\n```\n\nBốn cải thiện:\n\n```\n① MayInDonGian không hứa điều nó không làm được\n② in_bao_cao nhận được MỌI thứ biết in\n③ Chữ ký tự nói hàm cần gì\n④ mypy chặn lúc viết code nếu truyền sai\n```\n\nĐiểm ④ đáng nhấn: lỗi chuyển từ **lúc chạy** sang **lúc viết code**. Với giao diện béo, `may.quet(...)` trên một máy đơn giản chỉ đổ khi chạy tới dòng đó.\n\nCú pháp giao nhiều `Protocol`:\n\n```python\n# Cách 1 — kế thừa Protocol\nclass MayDaNang(CoTheIn, CoTheQuet, Protocol): ...\n\ndef sao_chep(may: MayDaNang, tl): ...\n\n# Cách 2 — Python 3.13 trở lên hỗ trợ toán tử giao kiểu\n# def sao_chep(may: CoTheIn & CoTheQuet, tl): ...\n```\n\nCách 1 chạy được ở mọi phiên bản và nên dùng cho tới khi cách 2 phổ biến.\n\n### ISP nhìn từ phía người gọi\n\nĐây là sắc thái riêng của Python và là phần thực dụng nhất:\n\n```python\n# ❌ Đòi nhiều hơn cần\ndef thong_ke(kho: KhoLuuTru) -> int:      # KhoLuuTru có doc, luu, xoa, liet_ke\n    return sum(len(kho.doc(k) or b\"\") for k in kho.liet_ke())\n#              ⭐ chỉ dùng doc và liet_ke\n\n# ✓ Đòi đúng\nclass KhoDocDuoc(Protocol):\n    def doc(self, k: str) -> bytes | None: ...\n    def liet_ke(self) -> list[str]: ...\n\ndef thong_ke(kho: KhoDocDuoc) -> int:\n    return sum(len(kho.doc(k) or b\"\") for k in kho.liet_ke())\n```\n\nBản thứ hai nhận được nhiều thứ hơn: kho chỉ đọc, kho cache, class giả trong kiểm thử — tất cả đều dùng được vì chúng có đúng hai phương thức đó.\n\nĐây là nguyên tắc **\"nhận rộng nhất có thể\"** đã gặp ở bài trừu tượng, áp cho giao diện tự viết. Cùng lý do vì sao nhận `Iterable` tốt hơn nhận `list`.\n\n### Cùng một class, nhiều vai trò\n\n```python\nclass KhoPostgres:\n    \"\"\"Cài đặt CẢ BỐN vai trò — nhưng mỗi hàm chỉ đòi vai trò nó cần.\"\"\"\n    def doc(self, k): ...\n    def liet_ke(self): ...\n    def luu(self, k, v): ...\n    def xoa(self, k): ...\n    def sao_luu(self, dd): ...\n    def khoi_phuc(self, dd): ...\n\n\nclass DocDuoc(Protocol):\n    def doc(self, k: str) -> bytes | None: ...\n\nclass GhiDuoc(Protocol):\n    def luu(self, k: str, v: bytes) -> None: ...\n    def xoa(self, k: str) -> bool: ...\n\nclass SaoLuuDuoc(Protocol):\n    def sao_luu(self, dd: str) -> None: ...\n    def khoi_phuc(self, dd: str) -> None: ...\n\n\nkho = KhoPostgres()\n\ndef bao_cao(k: DocDuoc): ...           # nhận kho ✓\ndef ghi_nhat_ky(k: GhiDuoc): ...       # nhận kho ✓\ndef sao_luu_dinh_ky(k: SaoLuuDuoc): ...# nhận kho ✓\n```\n\n**Một class cài đặt nhiều giao diện nhỏ là hoàn toàn bình thường và tốt.** ISP không nói \"class phải nhỏ\" — nó nói **giao diện phải nhỏ**. Hai điều khác nhau, và nhầm lẫn giữa chúng dẫn tới việc chia class không cần thiết.\n\n### ISP và SRP — hai bài khác nhau\n\n```\nSRP  nói về CLASS      — một lý do để thay đổi\nISP  nói về GIAO DIỆN  — client không phụ thuộc thứ không dùng\n```\n\nChúng liên quan nhưng không trùng:\n\n```python\n# Class có MỘT trách nhiệm (SRP ✓)\n# nhưng giao diện béo với một số client (ISP ✗)\nclass QuanLyFile:\n    \"\"\"Quản lý file — một trách nhiệm rõ ràng.\"\"\"\n    def doc(self, p): ...\n    def ghi(self, p, nd): ...\n    def xoa(self, p): ...\n    def nen(self, p): ...\n    def giai_nen(self, p): ...\n    def ma_hoa(self, p, khoa): ...\n\n# Một hàm chỉ đọc file vẫn phải phụ thuộc vào cả sáu phương thức\n```\n\nCách sửa ở đây **không phải** chia class — trách nhiệm của nó vẫn thống nhất. Cách sửa là **định nghĩa nhiều giao diện nhỏ** cho cùng class đó.\n\nNgược lại, giao diện béo **thường** là dấu hiệu class phía sau vi phạm SRP — nên gặp giao diện lớn thì đáng kiểm tra cả hai.\n\n### Chia quá nhỏ — sai lầm ngược lại\n\n```python\n# ❌ Chia vụn\nclass CoDoc(Protocol):\n    def doc(self, k): ...\n\nclass CoLietKe(Protocol):\n    def liet_ke(self): ...\n\nclass CoDem(Protocol):\n    def dem(self): ...\n\nclass CoKiemTraTonTai(Protocol):\n    def ton_tai(self, k): ...\n\ndef xu_ly(kho: CoDoc & CoLietKe & CoDem & CoKiemTraTonTai): ...\n#              ⚠️ bốn giao diện cho một khái niệm duy nhất\n```\n\nBốn phương thức này **luôn đi cùng nhau** — không ai cài `doc` mà không cài `ton_tai`. Chúng là **một vai trò**, không phải bốn:\n\n```python\n# ✓\nclass KhoDocDuoc(Protocol):\n    def doc(self, k: str) -> bytes | None: ...\n    def liet_ke(self) -> list[str]: ...\n    def dem(self) -> int: ...\n    def ton_tai(self, k: str) -> bool: ...\n```\n\nPhép kiểm: **các phương thức này có bao giờ được cài đặt riêng lẻ không?** Không bao giờ → chúng thuộc cùng một giao diện.\n\nDấu hiệu chia quá nhỏ:\n\n```\n· Mọi bản cài đặt đều cài đủ mọi giao diện nhỏ\n· Chữ ký hàm phải giao ba giao diện trở lên\n· Giao diện chỉ có một phương thức và luôn đi kèm giao diện khác\n· Phải mở năm file để biết một object làm được gì\n```\n\n### Khi giao diện một phương thức là đúng\n\nMột phương thức không phải luôn quá nhỏ:\n\n```python\nclass CoTheGoi(Protocol):\n    def __call__(self, x: int) -> str: ...\n\nclass BoXacThuc(Protocol):\n    def kiem(self, gt: str) -> str | None: ...\n\nclass BoChuyenDoi(Protocol):\n    def chuyen(self, x): ...\n```\n\nBa giao diện này đúng vì **năng lực của chúng thật sự chỉ có một thao tác** — không có thao tác nào khác luôn đi kèm.\n\nVà với giao diện một phương thức, thường **truyền thẳng hàm còn nhẹ hơn**:\n\n```python\n# Thay vì Protocol một phương thức\ndef xu_ly(ds, kiem: Callable[[str], str | None]): ...\n\nxu_ly(ds, lambda s: None if s else \"rỗng\")\n```\n\nĐây là ý đã gặp ở bài Open/Closed: hỏi trước xem điểm mở rộng có mấy thao tác.\n\n### Kích thước giao diện hợp lý\n\n```\n1 phương thức     ✓ nếu năng lực thật sự chỉ có một thao tác\n                    (nhưng cân nhắc truyền hàm thay vì Protocol)\n2–5 phương thức   ⭐ vùng thoải mái cho hầu hết vai trò\n6–10              ⚠️ xem lại — có phải hai vai trò bị ghép?\n>10               ✗ gần như chắc chắn nên tách\n```\n\nCon số không phải luật. Câu hỏi quyết định là: **có client nào chỉ dùng một phần không?** Có → tách phần đó ra.\n\n### ISP trong Python thuần duck typing\n\nPython không bắt khai báo interface, nên ISP vẫn áp dụng ngay cả khi bạn **không viết `Protocol` nào**:\n\n```python\n# ❌ Nhận cả object lớn chỉ để lấy một trường\ndef gui_thong_bao(nguoi_dung, tin: str) -> None:\n    gui_mail(nguoi_dung.email, tin)\n#            ⭐ chỉ cần email, nhưng đòi cả object NguoiDung\n\n# ✓ Nhận đúng thứ cần\ndef gui_thong_bao(email: str, tin: str) -> None:\n    gui_mail(email, tin)\n\ngui_thong_bao(nd.email, \"xin chào\")\n```\n\nBản thứ hai kiểm thử được mà không cần dựng một `NguoiDung` đầy đủ, và dùng được cho địa chỉ email không đến từ `NguoiDung` nào.\n\nĐây là ISP ở dạng đơn giản nhất: **hàm chỉ nên nhận thứ nó dùng.** Nguyên tắc này thường được gọi là *\"đừng đòi cả con bò khi chỉ cần ly sữa\"*.\n\nNhưng có ngoại lệ: nếu hàm cần **ba trường trở lên** của cùng một object, nhận cả object rõ hơn:\n\n```python\n# Nhận cả object khi cần nhiều trường liên quan\ndef xuat_hoa_don(don: DonHang) -> str:\n    return f\"{don.ma} — {don.ngay} — {don.tong} — {don.khach.ten}\"\n```\n\nBốn tham số rời rạc ở đây khó đọc hơn và dễ truyền nhầm thứ tự.\n\n### Cái giá của ISP\n\nNói thẳng nhược điểm:\n\n```\n· Nhiều Protocol hơn ⇒ nhiều tên phải nhớ\n· Chữ ký hàm cần giao nhiều giao diện thì rườm rà\n· Người mới khó nắm bức tranh \"object này làm được gì\"\n· Chia sai chỗ thì phải sửa lại cả chữ ký của nhiều hàm\n```\n\nVì vậy nhịp áp dụng cũng giống các nguyên tắc khác:\n\n```\nLần 1: một giao diện đủ dùng\nLần 2: xuất hiện một bản cài đặt không làm được vài phương thức\n       → ĐÂY là lúc tách\n```\n\nDấu hiệu rõ ràng nhất để tách: **một bản cài đặt phải ném `NotImplementedError`.** Đó là lúc giao diện đang đòi hỏi quá nhiều, không phải lúc trách bản cài đặt.",

vi_du: "### Máy in — từ giao diện béo sang vai trò\n\n```python\n# ❌ TRƯỚC\nfrom abc import ABC, abstractmethod\n\nclass ThietBiVanPhong(ABC):\n    @abstractmethod\n    def in_ra(self, tl: str) -> None: ...\n    @abstractmethod\n    def quet(self) -> str: ...\n    @abstractmethod\n    def fax(self, tl: str, so: str) -> None: ...\n    @abstractmethod\n    def dong_ghim(self, so_to: int) -> None: ...\n\n\nclass MayInLaser(ThietBiVanPhong):\n    def in_ra(self, tl): print(f\"in: {tl}\")\n    def quet(self): raise NotImplementedError(\"không quét được\")     # ❌\n    def fax(self, tl, so): raise NotImplementedError                 # ❌\n    def dong_ghim(self, so_to): raise NotImplementedError            # ❌\n\n\ndef in_bao_cao(tb: ThietBiVanPhong, bc: str) -> None:\n    tb.in_ra(bc)                          # chỉ dùng MỘT phương thức\n\ndef quy_trinh_van_thu(tb: ThietBiVanPhong, tl: str) -> None:\n    ban_sao = tb.quet()\n    tb.in_ra(ban_sao)\n    tb.dong_ghim(1)                       # ⚠️ đổ với MayInLaser\n```\n\n```python\n# ✓ SAU — bốn vai trò độc lập\nfrom typing import Protocol\n\nclass CoTheIn(Protocol):\n    def in_ra(self, tl: str) -> None: ...\n\nclass CoTheQuet(Protocol):\n    def quet(self) -> str: ...\n\nclass CoTheFax(Protocol):\n    def fax(self, tl: str, so: str) -> None: ...\n\nclass CoTheDongGhim(Protocol):\n    def dong_ghim(self, so_to: int) -> None: ...\n\n\nclass MayInLaser:                          # ⭐ chỉ cài một năng lực\n    def in_ra(self, tl): print(f\"in: {tl}\")\n\nclass MayQuet:\n    def quet(self): return \"nội dung đã quét\"\n\nclass MayDaNang:                            # ⭐ cài bốn năng lực\n    def in_ra(self, tl): print(f\"in: {tl}\")\n    def quet(self): return \"nội dung đã quét\"\n    def fax(self, tl, so): print(f\"fax tới {so}\")\n    def dong_ghim(self, so_to): print(f\"ghim {so_to} tờ\")\n\n\n# Mỗi hàm đòi ĐÚNG năng lực nó dùng\ndef in_bao_cao(tb: CoTheIn, bc: str) -> None:\n    tb.in_ra(bc)\n\nclass MayInVaQuet(CoTheIn, CoTheQuet, Protocol): ...\n\ndef sao_chep(tb: MayInVaQuet, so_ban: int = 1) -> None:\n    nd = tb.quet()\n    for _ in range(so_ban):\n        tb.in_ra(nd)\n\n\nin_bao_cao(MayInLaser(), \"báo cáo\")        # ✓\nin_bao_cao(MayDaNang(), \"báo cáo\")         # ✓\nsao_chep(MayDaNang(), 2)                   # ✓\nsao_chep(MayInLaser())                     # ❌ mypy CHẶN lúc viết code\n```\n\nDòng cuối là lợi ích cụ thể: lỗi được bắt **trước khi chạy**. Với giao diện béo, nó chỉ đổ khi chương trình chạy tới `tb.quet()` — có thể là trên môi trường thật.\n\n### Kho lưu trữ — vai trò theo năng lực\n\n```python\nfrom typing import Protocol\n\nclass DocDuoc(Protocol):\n    def doc(self, khoa: str) -> bytes | None: ...\n    def ton_tai(self, khoa: str) -> bool: ...\n\nclass GhiDuoc(Protocol):\n    def luu(self, khoa: str, gt: bytes) -> None: ...\n    def xoa(self, khoa: str) -> bool: ...\n\nclass LietKeDuoc(Protocol):\n    def liet_ke(self, tien_to: str = \"\") -> list[str]: ...\n\n\nclass KhoBoNho:                             # đủ ba vai trò\n    def __init__(self): self._d: dict[str, bytes] = {}\n    def doc(self, k): return self._d.get(k)\n    def ton_tai(self, k): return k in self._d\n    def luu(self, k, v): self._d[k] = v\n    def xoa(self, k): return self._d.pop(k, None) is not None\n    def liet_ke(self, tien_to=\"\"):\n        return [k for k in self._d if k.startswith(tien_to)]\n\n\nclass KhoNenChiDoc:                         # ⭐ CHỈ đọc — và nói đúng như vậy\n    def __init__(self, duong_dan: str):\n        self._dd = duong_dan\n    def doc(self, k):\n        import gzip, json\n        with gzip.open(self._dd, \"rt\", encoding=\"utf-8\") as f:\n            return json.load(f).get(k, \"\").encode() or None\n    def ton_tai(self, k):\n        return self.doc(k) is not None\n\n\n# Mỗi hàm nêu rõ năng lực cần\ndef tong_kich_thuoc(kho: DocDuoc, khoa: list[str]) -> int:\n    return sum(len(kho.doc(k) or b\"\") for k in khoa)\n\ndef don_dep(kho: GhiDuoc, khoa: list[str]) -> int:\n    return sum(1 for k in khoa if kho.xoa(k))\n\nclass KhoDayDu(DocDuoc, GhiDuoc, Protocol): ...\n\ndef nhan_ban(nguon: DocDuoc, dich: GhiDuoc, khoa: list[str]) -> int:\n    n = 0\n    for k in khoa:\n        v = nguon.doc(k)\n        if v is not None:\n            dich.luu(k, v)\n            n += 1\n    return n\n\n\nkho_nen = KhoNenChiDoc(\"backup.gz\")\nkho_bn = KhoBoNho()\n\ntong_kich_thuoc(kho_nen, [\"a\"])            # ✓ kho chỉ đọc dùng được\nnhan_ban(kho_nen, kho_bn, [\"a\", \"b\"])      # ✓ đọc từ nén, ghi vào bộ nhớ\nnhan_ban(kho_bn, kho_nen, [\"a\"])           # ❌ mypy: KhoNenChiDoc thiếu luu\n```\n\nChữ ký `nhan_ban(nguon: DocDuoc, dich: GhiDuoc)` **tự nói** hướng dữ liệu chảy. Với một giao diện `Kho` duy nhất, bạn phải đọc thân hàm mới biết cái nào là nguồn.\n\n### Kiểm thử nhẹ hơn hẳn\n\n```python\n# ❌ Với giao diện béo — class giả phải cài đủ mọi thứ\nclass KhoGiaBeo:\n    def doc(self, k): return b\"x\"\n    def ton_tai(self, k): return True\n    def luu(self, k, v): pass          # ⚠️ không dùng trong bài test này\n    def xoa(self, k): return True      # ⚠️\n    def liet_ke(self, tien_to=\"\"): return []   # ⚠️\n    def sao_luu(self, dd): pass        # ⚠️\n    def khoi_phuc(self, dd): pass      # ⚠️\n\n# ✓ Với giao diện nhỏ — chỉ cài cái bài test cần\ndef test_tong_kich_thuoc():\n    class KhoGia:\n        def doc(self, k): return b\"12345\" if k == \"a\" else None\n        def ton_tai(self, k): return k == \"a\"\n\n    assert tong_kich_thuoc(KhoGia(), [\"a\", \"b\"]) == 5\n\n\ndef test_nhan_ban_ghi_dung_du_lieu():\n    class NguonGia:\n        def doc(self, k): return {\"a\": b\"1\", \"b\": b\"22\"}.get(k)\n        def ton_tai(self, k): return k in (\"a\", \"b\")\n\n    class DichGia:\n        def __init__(self): self.da_ghi = {}\n        def luu(self, k, v): self.da_ghi[k] = v\n        def xoa(self, k): return self.da_ghi.pop(k, None) is not None\n\n    dich = DichGia()\n    assert nhan_ban(NguonGia(), dich, [\"a\", \"b\", \"c\"]) == 2\n    assert dich.da_ghi == {\"a\": b\"1\", \"b\": b\"22\"}      # ⭐ kiểm được tương tác\n```\n\nBảy phương thức giả xuống còn hai — và quan trọng hơn, **bài kiểm thử nói rõ nó phụ thuộc vào gì**. Nhìn `KhoGia` là biết `tong_kich_thuoc` chỉ dùng `doc`.\n\n### Hàm chỉ nhận thứ nó dùng\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass\nclass NguoiDung:\n    id: int\n    ten: str\n    email: str\n    dia_chi: str\n    lich_su_mua: list\n    the_tin_dung: str\n\n\n# ❌ Đòi cả object cho một trường\ndef gui_chao_mung(nd: NguoiDung) -> None:\n    gui_mail(nd.email, f\"Chào {nd.ten}\")\n\n# Kiểm thử phải dựng đủ sáu trường\ndef test_cu():\n    nd = NguoiDung(1, \"Minh\", \"m@x.com\", \"HN\", [], \"4111...\")\n    gui_chao_mung(nd)\n\n\n# ✓ Nhận đúng thứ cần\ndef gui_chao_mung(email: str, ten: str) -> None:\n    gui_mail(email, f\"Chào {ten}\")\n\ndef test_moi():\n    gui_chao_mung(\"m@x.com\", \"Minh\")      # ⭐ hai giá trị, xong\n\n# Và dùng được cho email không từ NguoiDung nào\ngui_chao_mung(\"khach@x.com\", \"Khách\")\n```\n\nNhưng đừng máy móc — khi cần nhiều trường liên quan thì nhận cả object rõ hơn:\n\n```python\n# ✓ Bốn trường của cùng một khái niệm ⇒ nhận object\ndef xuat_hoa_don(don: DonHang) -> str:\n    return (f\"Đơn {don.ma}\\nNgày: {don.ngay}\\n\"\n            f\"Khách: {don.khach_ten}\\nTổng: {don.tong:,.0f}đ\")\n\n# ✗ Bốn tham số rời rạc — dễ truyền nhầm thứ tự\ndef xuat_hoa_don(ma, ngay, khach_ten, tong): ...\n```\n\nRanh giới thực dụng: **một hoặc hai trường thì truyền thẳng; ba trường trở lên của cùng một khái niệm thì truyền object.**\n\nMột giải pháp trung gian khi cần vài trường từ một object lớn — dùng `Protocol` mô tả đúng phần cần:\n\n```python\nclass CoEmailVaTen(Protocol):\n    email: str\n    ten: str\n\ndef gui_chao_mung(nd: CoEmailVaTen) -> None:\n    gui_mail(nd.email, f\"Chào {nd.ten}\")\n\n# Nhận NguoiDung, và cả bất cứ thứ gì có hai thuộc tính đó\ngui_chao_mung(NguoiDung(1, \"Minh\", \"m@x.com\", \"HN\", [], \"\"))\n\n@dataclass\nclass LienHe:\n    ten: str\n    email: str\n\ngui_chao_mung(LienHe(\"An\", \"a@x.com\"))     # ✓ class hoàn toàn khác\n```\n\n### Chia quá nhỏ — ví dụ ngược\n\n```python\n# ❌ Bốn Protocol cho một khái niệm\nclass CoDoc(Protocol):\n    def doc(self, k): ...\nclass CoTonTai(Protocol):\n    def ton_tai(self, k): ...\nclass CoDem(Protocol):\n    def dem(self): ...\nclass CoLietKe(Protocol):\n    def liet_ke(self): ...\n\nclass TatCa(CoDoc, CoTonTai, CoDem, CoLietKe, Protocol): ...\n\ndef xu_ly(kho: TatCa): ...\n\n# ⚠️ Mọi bản cài đặt đều cài đủ bốn — chúng chưa bao giờ tách rời\n\n# ✓ Một giao diện\nclass KhoDocDuoc(Protocol):\n    def doc(self, k: str) -> bytes | None: ...\n    def ton_tai(self, k: str) -> bool: ...\n    def dem(self) -> int: ...\n    def liet_ke(self) -> list[str]: ...\n```\n\nPhép kiểm: **có bản cài đặt nào cài `doc` mà không cài `ton_tai` không?** Không → chúng thuộc cùng một vai trò.\n\n### Xử lý dữ liệu — vai trò rõ ràng\n\n```python\nfrom typing import Protocol, Iterator\n\nclass NguonDuLieu(Protocol):\n    def doc(self) -> Iterator[dict]: ...\n\nclass DichDuLieu(Protocol):\n    def ghi(self, bg: dict) -> None: ...\n\nclass CoTheDong(Protocol):\n    def dong(self) -> None: ...\n\n\nclass DocCSV:\n    def __init__(self, dd): self.dd = dd\n    def doc(self):\n        import csv\n        with open(self.dd, newline=\"\", encoding=\"utf-8-sig\") as f:\n            yield from csv.DictReader(f)\n\nclass GhiJSONL:\n    def __init__(self, dd):\n        self._f = open(dd, \"w\", encoding=\"utf-8\")\n    def ghi(self, bg):\n        import json\n        self._f.write(json.dumps(bg, ensure_ascii=False) + \"\\n\")\n    def dong(self):\n        self._f.close()\n\nclass GhiBoNho:                     # ⭐ không cần dong()\n    def __init__(self): self.ban_ghi = []\n    def ghi(self, bg): self.ban_ghi.append(bg)\n\n\ndef chuyen(nguon: NguonDuLieu, dich: DichDuLieu,\n           loc=None) -> int:\n    n = 0\n    for bg in nguon.doc():\n        if loc is None or loc(bg):\n            dich.ghi(bg)\n            n += 1\n    return n\n\n\n# Cả hai đích đều dùng được — GhiBoNho không bị ép cài dong()\nchuyen(DocCSV(\"a.csv\"), GhiBoNho())\n\nd = GhiJSONL(\"ra.jsonl\")\ntry:\n    chuyen(DocCSV(\"a.csv\"), d, loc=lambda b: b.get(\"trang_thai\") == \"ok\")\nfinally:\n    d.dong()\n```\n\n`GhiBoNho` không có `dong()` và **không cần có** — nó không giữ tài nguyên nào. Nếu `DichDuLieu` gộp cả `dong()`, class này buộc phải viết một phương thức rỗng, và người đọc không biết gọi nó có ý nghĩa gì.\n\nMuốn dọn dẹp tự động thì tách thành một hàm riêng:\n\n```python\nfrom contextlib import nullcontext\n\ndef chuyen_va_dong(nguon: NguonDuLieu, dich: DichDuLieu, **kw) -> int:\n    cm = dich if hasattr(dich, \"__enter__\") else nullcontext(dich)\n    with cm:\n        return chuyen(nguon, dich, **kw)\n```\n\n### So sánh số dòng phải viết\n\n```python\n# Giao diện béo: thêm một bản cài đặt = cài 8 phương thức\nclass KhoMoi(KhoDayDu):\n    def doc(self, k): ...          # cần\n    def ton_tai(self, k): ...      # cần\n    def luu(self, k, v): raise NotImplementedError      # không làm được\n    def xoa(self, k): raise NotImplementedError\n    def liet_ke(self): raise NotImplementedError\n    def dem(self): raise NotImplementedError\n    def sao_luu(self, dd): raise NotImplementedError\n    def khoi_phuc(self, dd): raise NotImplementedError\n\n# Giao diện theo vai trò: cài đúng cái làm được\nclass KhoMoi:\n    def doc(self, k): ...\n    def ton_tai(self, k): ...\n```\n\nSáu phương thức ném lỗi biến mất — và cùng với chúng là sáu chỗ có thể làm đổ chương trình của người dùng.\n\n### Bộ kiểm nhanh\n\n```python\ndef soi_isp(giao_dien, *ban_cai_dat):\n    \"\"\"Tìm bản cài đặt không dùng hết giao diện.\"\"\"\n    import inspect\n\n    pt = [t for t in dir(giao_dien)\n          if not t.startswith(\"_\") and callable(getattr(giao_dien, t, None))]\n    print(f\"{giao_dien.__name__}: {len(pt)} phương thức\")\n    if len(pt) > 5:\n        print(f\"  ⚠️ hơn 5 phương thức — xem có client nào chỉ dùng một phần\")\n\n    for cls in ban_cai_dat:\n        thieu, trong = [], []\n        for t in pt:\n            v = getattr(cls, t, None)\n            if v is None:\n                thieu.append(t); continue\n            try:\n                src = inspect.getsource(v)\n            except (OSError, TypeError):\n                continue\n            if \"NotImplementedError\" in src or src.strip().endswith(\"pass\"):\n                trong.append(t)\n        if trong:\n            print(f\"  ❌ {cls.__name__} bỏ trống: {trong}\")\n            print(f\"     ⇒ tách {trong} ra một giao diện riêng\")\n        if thieu:\n            print(f\"  ⚠️ {cls.__name__} thiếu: {thieu}\")\n```\n\nBộ kiểm này chỉ ra **chính xác nhóm phương thức nên tách**: những cái mà một bản cài đặt phải bỏ trống. Đó là ranh giới tự nhiên giữa các vai trò, và nó do dữ liệu thật chỉ ra chứ không do dự đoán.",

so_sanh: "### Giao diện béo và giao diện theo vai trò\n\n| | Giao diện béo | Theo vai trò |\n|---|---|---|\n| Bản cài đặt phải ném lỗi | **✓** | ✗ |\n| Vi phạm Liskov | **✓ kéo theo** | ✗ |\n| Chữ ký hàm tự tài liệu | ✗ | **✓** |\n| Nhận được nhiều loại object | ✗ | **✓** |\n| Class giả khi test | cài đủ mọi thứ | **cài cái cần** |\n| Thêm phương thức ảnh hưởng | **mọi bản cài đặt** | chỉ bản cài đặt vai trò đó |\n| Số tên phải nhớ | ít | nhiều hơn |\n\nHàng áp chót là cái giá thật của giao diện béo: một phương thức mới lan ra mọi nơi.\n\n### ISP và SRP\n\n| | SRP | ISP |\n|---|---|---|\n| Nói về | **class** | **giao diện** |\n| Tiêu chí | một lý do để thay đổi | client không phụ thuộc thứ không dùng |\n| Cách sửa | tách class | **tách giao diện** (class có thể giữ nguyên) |\n| Quan hệ | giao diện béo **thường** báo hiệu class vi phạm SRP | — |\n\nĐiểm quan trọng: **ISP không đòi chia class.** Một class cài đặt bốn giao diện nhỏ là hoàn toàn tốt.\n\n### ISP và Liskov\n\n| | Liskov | ISP |\n|---|---|---|\n| Phát biểu về | lớp con thay được lớp cha | client không phụ thuộc thừa |\n| Quan hệ nhân quả | **hậu quả** | **nguyên nhân** |\n| Biểu hiện chung | `raise NotImplementedError` | giao diện đòi hỏi quá nhiều |\n| Cách sửa chung | **tách giao diện** | tách giao diện |\n\nHai bài này gặp nhau ở cùng một triệu chứng và cùng một cách chữa. Nhìn thấy `NotImplementedError` trong một bản cài đặt: **đó là ISP bị vi phạm trước, Liskov vỡ sau.**\n\n### Kích thước giao diện\n\n| Số phương thức | Đánh giá |\n|---|---|\n| 1 | ✓ nếu năng lực thật sự đơn lẻ — nhưng cân nhắc truyền hàm |\n| **2–5** | **⭐ vùng thoải mái** |\n| 6–10 | ⚠️ xem có hai vai trò bị ghép không |\n| > 10 | ✗ gần như chắc chắn nên tách |\n\nCon số chỉ là tín hiệu. Câu hỏi quyết định: **có client nào chỉ dùng một phần không?**\n\n### Dấu hiệu chia đủ và chia quá nhỏ\n\n| | Chia đủ | Chia quá nhỏ |\n|---|---|---|\n| Có bản cài đặt chỉ cài một phần | **✓** | ✗ ai cũng cài hết |\n| Chữ ký hàm | 1–2 giao diện | **3+ giao diện giao nhau** |\n| Giao diện một phương thức | hiếm, có lý do | **nhiều, luôn đi kèm nhau** |\n| Hiểu object làm được gì | dễ | **phải mở nhiều file** |\n\nPhép kiểm ngược: **các phương thức này có bao giờ được cài riêng lẻ không?** Không bao giờ → chúng là một vai trò.\n\n### Hàm nhận gì\n\n| Số trường cần | Nên nhận |\n|---|---|\n| 1–2 | **giá trị thẳng** (`email`, `ten`) |\n| 3+ của cùng khái niệm | **object** |\n| Vài trường, muốn nhận nhiều loại | **`Protocol` mô tả đúng phần cần** |\n| Cần gọi phương thức | `Protocol` theo năng lực |\n\nHàng thứ ba là giải pháp trung gian hay bị bỏ qua: `Protocol` với vài thuộc tính cho phép nhận cả `NguoiDung` lẫn `LienHe` mà không cần quan hệ gì giữa chúng.\n\n### Khi nào tách\n\n| Tình huống | Tách? |\n|---|---|\n| Một bản cài đặt phải ném `NotImplementedError` | **✓ ngay** |\n| Có client chỉ dùng một phần giao diện | **✓** |\n| Class giả khi test phải cài phương thức thừa | ✓ |\n| Giao diện hơn 10 phương thức | ✓ xem lại |\n| Mọi bản cài đặt đều dùng hết | ✗ |\n| Chưa có bản cài đặt thứ hai | ✗ đợi |\n\nHàng đầu là dấu hiệu dứt khoát nhất: **`NotImplementedError` trong một bản cài đặt là lỗi của giao diện, không phải lỗi của bản cài đặt.**",

loi_thuong_gap: [
  "**Gộp mọi phương thức liên quan vào một giao diện lớn.** Bản cài đặt không làm được hết sẽ phải ném lỗi — và kéo theo vi phạm Liskov.",
  "**Trách bản cài đặt khi nó phải ném `NotImplementedError`.** Đó là lỗi của giao diện: nó đòi hỏi quá nhiều.",
  "**Chú thích tham số bằng giao diện lớn dù hàm chỉ dùng hai phương thức.** Thu hẹp vô cớ những gì truyền vào được.",
  "**Nhầm ISP với SRP.** ISP nói về giao diện; class vẫn có thể cài đặt nhiều giao diện nhỏ.",
  "**Chia thành nhiều giao diện một phương thức luôn đi kèm nhau.** Chúng là một vai trò — chia vụn không thêm thông tin gì.",
  "**Chữ ký hàm phải giao ba giao diện trở lên.** Dấu hiệu chia quá nhỏ.",
  "**Nhận cả object lớn để lấy một trường.** Kiểm thử phải dựng object đầy đủ, và hàm không dùng được cho dữ liệu khác nguồn.",
  "**Máy móc tách mọi tham số thành giá trị rời.** Bốn tham số của cùng một khái niệm khó đọc hơn một object và dễ truyền nhầm thứ tự.",
  "**Ép mọi bản cài đặt có `dong()` dù có cái không giữ tài nguyên.** Phương thức rỗng làm người đọc không biết gọi nó có ý nghĩa gì.",
  "**Thêm phương thức vào giao diện đã có nhiều bản cài đặt.** Mọi bản đều phải sửa. Tạo giao diện mới thay vì mở rộng cái cũ.",
  "**Tách giao diện theo dự đoán khi chưa có bản cài đặt thứ hai.** Đường cắt thường sai chỗ.",
  "**Quên rằng ISP áp dụng cả khi không viết `Protocol` nào.** \"Hàm chỉ nhận thứ nó dùng\" là ISP ở dạng đơn giản nhất."
],

tu_kiem_tra: [
  "Phát biểu ISP là gì? \"Client\" ở đây gồm những ai?",
  "Sắc thái riêng của ISP trong Python với duck typing là gì?",
  "Nêu ba hậu quả của giao diện béo. Hậu quả nào ít được nhắc nhất?",
  "Vì sao nói giao diện béo là **nguyên nhân** còn vi phạm Liskov là **hậu quả**?",
  "ISP khác SRP thế nào? ISP có đòi chia class không?",
  "Một class cài đặt bốn `Protocol` nhỏ — điều đó tốt hay xấu, vì sao?",
  "Nêu bốn dấu hiệu chia quá nhỏ. Phép kiểm ngược là gì?",
  "Khi nào giao diện một phương thức là hợp lý? Và lựa chọn nhẹ hơn là gì?",
  "Vì sao `nhan_ban(nguon: DocDuoc, dich: GhiDuoc)` tự tài liệu hơn `nhan_ban(a: Kho, b: Kho)`?",
  "ISP làm kiểm thử nhẹ hơn ở hai điểm nào?",
  "Hàm nên nhận giá trị thẳng hay nhận cả object? Ranh giới thực dụng?",
  "`Protocol` mô tả vài thuộc tính giải quyết vấn đề gì?",
  "Dấu hiệu dứt khoát nhất cho biết đã tới lúc tách giao diện là gì?",
  "Nêu bốn cái giá của việc áp dụng ISP."
],

tai_nguyen: [
  "Sách **Clean Architecture** — Robert C. Martin. Chương về ISP, gồm cả ví dụ về việc phụ thuộc thừa làm lan thay đổi.",
  "Sách **Agile Software Development** — Robert C. Martin. Ví dụ máy in đa năng gốc nằm ở đây, phân tích chi tiết hơn.",
  "Tài liệu Python, `typing.Protocol` — cú pháp kế thừa nhiều `Protocol` để tạo giao diện giao nhau.",
  "Sách **Practical Object-Oriented Design** — Sandi Metz. Chương về giao diện nói rõ khái niệm \"giao diện theo vai trò\".",
  "Tài liệu Python, `collections.abc` — ví dụ mẫu mực về chia giao diện theo năng lực: `Iterable`, `Sized`, `Container`, `Sequence`."
],

lien_he: [
  "**SOLID — Liskov Substitution** ngay trước: giao diện béo là nguyên nhân, `NotImplementedError` là hậu quả — hai bài chữa cùng một chỗ.",
  "**SOLID — Single Responsibility**: giao diện béo thường báo hiệu class phía sau có nhiều trách nhiệm, nhưng ISP sửa được mà không cần chia class.",
  "**SOLID — Dependency Inversion** ngay sau: phụ thuộc vào trừu tượng nhỏ và đúng là điều kiện để đảo phụ thuộc có ích.",
  "**Trừu tượng: ABC, Protocol**: `Protocol` là công cụ chính; nguyên tắc \"nhận rộng nhất có thể\" ở đó chính là ISP nhìn từ phía người gọi.",
  "**Đa hình và duck typing**: Python không bắt khai báo interface, nên ISP ở đây chủ yếu là kỷ luật khi viết chú thích kiểu.",
  "**Composition over inheritance**: tách giao diện làm composition dễ hơn — mỗi thành phần chỉ cần cài vai trò của nó.",
  "**Type hint và mypy** ở module 5: `mypy` biến việc truyền sai vai trò thành lỗi lúc viết code thay vì lúc chạy.",
  "**Đóng gói**: cả hai đều nói về việc lộ ra ít nhất có thể — một cái với dữ liệu, một cái với giao diện.",
  "**Testing** ở mảng 05: class giả nhỏ gọn là lợi ích trực tiếp và đo được của ISP.",
  "**Backend & Web** ở mảng 05: repository chia theo năng lực đọc/ghi là ứng dụng thẳng của bài này."
]

};
