window.LESSONS["01:mang-may-tinh:proxy-reverse-proxy-cdn-khai-quat"] = {

tom_tat: [
  "**Phân biệt bằng câu hỏi \"đứng trước ai\"**: forward proxy đứng trước **client** và client biết nó; reverse proxy đứng trước **server** và client không biết nó tồn tại.",
  "**Reverse proxy là thành phần hạ tầng bạn sẽ gặp ở mọi hệ thống thật** — nó gánh TLS, cân tải, cache, nén, giới hạn tần suất, để ứng dụng chỉ còn lo logic.",
  "**CDN là reverse proxy đặt rải rác về mặt địa lý.** Giá trị lớn nhất của nó không phải băng thông mà là **giảm RTT** — thứ mà không có tối ưu code nào thay thế được.",
  "**Mỗi lớp trung gian là một chỗ có thể hỏng.** Danh sách lỗi kinh điển: mất IP thật của client, cache sai phản hồi riêng tư, timeout tầng nào cắt trước, và `502` khác `504` ở đâu.",
  "**Cache là con dao hai lưỡi lớn nhất.** Cache sai một phản hồi có `Set-Cookie` là phát phiên đăng nhập của một người cho mọi người — sự cố bảo mật thật, không phải giả thuyết."
],

can_biet_truoc: [
  "Bài **HTTP: method, header, body, status code** — cache HTTP, `X-Forwarded-For`, và status `502`/`504` đều từ đó.",
  "Bài **HTTPS, TLS handshake** — kết thúc TLS ở đâu là quyết định về reverse proxy.",
  "Bài **IP, port, DNS** — CDN hoạt động bằng cách trả IP khác nhau tuỳ vị trí người hỏi.",
  "Bài **Mô hình TCP/IP 4 tầng** — phân biệt load balancer tầng 4 và tầng 7 là phân biệt theo tầng mà thiết bị đọc được.",
  "Bài **WebSocket vs polling** ngay trước — phần lớn lỗi realtime là lỗi cấu hình proxy."
],

dinh_nghia: "Nói đời thường: **forward proxy là người đi mua hộ bạn** — bạn nhờ họ, cửa hàng chỉ thấy họ. **Reverse proxy là lễ tân của một toà nhà** — khách chỉ nói với lễ tân, không biết bên trong có bao nhiêu phòng và ai đang làm việc.\n\nNói chính xác:\n\n- **Forward proxy** — trung gian đặt **phía client**. Client **cấu hình tường minh** để đi qua nó. Server đích chỉ thấy IP của proxy.\n- **Reverse proxy** — trung gian đặt **phía server**. Client **không biết** nó tồn tại; với client, nó *là* server.\n- **Load balancer** — reverse proxy chuyên phân phối tải sang nhiều server backend. Về bản chất là một trường hợp của reverse proxy.\n- **CDN** (*Content Delivery Network*) — mạng reverse proxy phân bố khắp thế giới, phục vụ nội dung từ điểm gần người dùng nhất.\n- **Điểm hiện diện** (*PoP*, *edge*) — một vị trí vật lý của CDN.\n- **Origin** — server gốc thật sự chứa nội dung, nằm sau CDN.\n\nMột câu để phân biệt và không bao giờ nhầm lại: **forward proxy bảo vệ và đại diện cho client; reverse proxy bảo vệ và đại diện cho server.** Cùng một phần mềm (nginx chẳng hạn) làm được cả hai vai — khác nhau ở cấu hình và ở chỗ nó đứng, không ở bản chất.",

vi_sao_can: "Đây là tầng hạ tầng mà mọi backend engineer đều phải hiểu, vì không có hệ thống thật nào thiếu nó:\n\n- **Mọi hệ thống thật đều có reverse proxy.** nginx trước ứng dụng, load balancer của cloud trước cụm server, CDN trước tất cả. Không hiểu nó là không hiểu request thật đi đường nào.\n- **Rất nhiều lỗi \"bí ẩn\" nằm ở tầng này.** Log toàn IP giống nhau, WebSocket đứt mỗi 60 giây, SSE tới cả cục, deploy rồi vẫn thấy bản cũ — không lỗi nào ở trong code ứng dụng.\n- **Đây là chỗ tối ưu hiệu năng rẻ nhất.** Bật nén, bật cache, bật HTTP/2 ở proxy là cải thiện đo được mà không sửa một dòng code.\n- **Đây cũng là chỗ gây sự cố bảo mật nghiêm trọng.** Cache sai một phản hồi riêng tư là phát dữ liệu người này cho người khác. Tin `X-Forwarded-For` sai chỗ là cho phép giả mạo IP.\n- **Nền cho System Design.** Cân tải, giảm tải TLS, giới hạn tần suất, chặn tấn công — mọi kiến trúc mở rộng đều bắt đầu từ lớp này.\n\nTình huống thật rất đáng nhớ: bạn thêm CDN trước website để tăng tốc. Vài giờ sau, người dùng bắt đầu báo \"tôi đăng nhập mà thấy tên người khác\". Nguyên nhân: CDN cache trang chủ, nhưng trang chủ có `Set-Cookie` chứa session ID của người truy cập đầu tiên. CDN phát lại **cả cookie đó** cho mọi người sau. Đây là sự cố có thật ở nhiều nơi, và nó tránh được bằng đúng một dòng cấu hình. Phần cơ chế sẽ nói rõ dòng đó là gì và vì sao.",

co_che: "### Đứng trước ai — sơ đồ phân biệt\n\n```\nFORWARD PROXY (client biết nó, server không biết client)\n\n  Client ──► Proxy ──► Internet ──► Server\n   │          │\n   │          └─ server CHỈ thấy IP của proxy\n   └─ client CẤU HÌNH tường minh để đi qua đây\n\n\nREVERSE PROXY (client không biết nó, server ở sau)\n\n  Client ──► Internet ──► Reverse Proxy ──┬──► Server A\n   │                          │           ├──► Server B\n   │                          │           └──► Server C\n   └─ client TƯỞNG đây là server\n                              └─ quyết định gửi tới backend nào\n```\n\nBảng đối chiếu để không nhầm lại:\n\n| | Forward proxy | Reverse proxy |\n|---|---|---|\n| Đặt gần | client | server |\n| Ai cấu hình | **client** | **chủ server** |\n| Client biết nó tồn tại | ✓ | ✗ |\n| Ai được ẩn | **client** | **server** |\n| Dùng để | lọc, cache phía client, vượt hạn chế mạng | TLS, cân tải, cache, bảo vệ |\n| Ví dụ | proxy công ty, proxy trường học | nginx, load balancer cloud, CDN |\n\n### Reverse proxy làm những gì\n\nĐây là danh sách việc mà đưa lên proxy thì ứng dụng không phải làm nữa:\n\n```\n① Kết thúc TLS       một chỗ giữ chứng chỉ, ứng dụng nói HTTP thường\n② Cân tải            phân phối sang nhiều backend, bỏ backend đã chết\n③ Cache              phục vụ nội dung tĩnh, không đánh thức ứng dụng\n④ Nén                gzip/brotli một chỗ\n⑤ Giới hạn tần suất  chặn lạm dụng TRƯỚC khi tới ứng dụng\n⑥ Định tuyến         /api → service A, /anh → service B\n⑦ Che giấu kiến trúc bên ngoài chỉ thấy một điểm vào\n⑧ Phục vụ file tĩnh  nginh đọc file nhanh hơn ứng dụng nhiều lần\n```\n\nNguyên tắc chung đằng sau danh sách này: **việc gì không cần biết logic nghiệp vụ thì đẩy ra khỏi ứng dụng.** Nén một phản hồi không cần biết người dùng là ai; chặn kẻ gửi 10.000 request/giây cũng vậy. Đẩy chúng ra proxy làm ứng dụng đơn giản hơn và nhanh hơn — và điểm ⑤ còn có ý nghĩa bảo vệ: chặn ở proxy nghĩa là request tệ **không bao giờ** tiêu tốn tài nguyên ứng dụng.\n\n### Cân tải — các chiến lược\n\n```nginx\nupstream ung_dung {\n    # ① Round robin — mặc định, lần lượt từng server\n    server 10.0.1.10:8000;\n    server 10.0.1.11:8000;\n\n    # ② Ít kết nối nhất — tốt khi thời gian xử lý CHÊNH LỆCH lớn\n    # least_conn;\n\n    # ③ Băm theo IP — cùng client luôn vào cùng server (sticky)\n    # ip_hash;\n\n    # ④ Trọng số — server mạnh nhận nhiều hơn\n    # server 10.0.1.12:8000 weight=3;\n}\n```\n\n| Chiến lược | Nguyên tắc | Dùng khi | Cạm bẫy |\n|---|---|---|---|\n| Round robin | lần lượt | request đồng đều | request nặng dồn vào một server |\n| Least connections | chọn server ít việc nhất | thời gian xử lý chênh lệch | cần theo dõi trạng thái |\n| IP hash | cùng IP → cùng server | **cần sticky session** | phân bố lệch; NAT làm cả công ty vào một server |\n| Trọng số | theo năng lực | server không đồng đều | phải cập nhật khi đổi hạ tầng |\n\nDòng `ip_hash` đáng bình luận: nó là cách làm sticky session, cần cho WebSocket hoặc cho ứng dụng lưu session trong bộ nhớ tiến trình. Nhưng nó có nhược điểm thật — **cả một công ty đi qua NAT sẽ có cùng một IP công khai**, nên toàn bộ nhân viên dồn vào một backend. Giải pháp tốt hơn gần như luôn là **bỏ hẳn nhu cầu sticky** bằng cách lưu session ở Redis, đúng như bài cookie/session đã nói.\n\n**Health check** là phần không thể thiếu và hay bị bỏ:\n\n```nginx\nupstream ung_dung {\n    server 10.0.1.10:8000 max_fails=3 fail_timeout=30s;\n    server 10.0.1.11:8000 max_fails=3 fail_timeout=30s;\n}\n```\n\nÝ nghĩa: hỏng 3 lần thì loại khỏi vòng phân phối 30 giây rồi thử lại. Không có nó, proxy vẫn gửi request tới server đã chết, và một phần người dùng nhận lỗi cho tới khi có người phát hiện.\n\nĐiểm đáng suy nghĩ: endpoint health check nên kiểm tra **cả các phụ thuộc** (database còn nối được không) hay chỉ trả `200` cho biết tiến trình còn sống? Cả hai đều có lý — kiểm tra sâu phát hiện được nhiều hơn, nhưng nếu database chập chờn thì **toàn bộ** backend bị loại cùng lúc, biến một sự cố nhỏ thành sập hoàn toàn. Thực hành phổ biến là tách hai loại: một endpoint \"tôi còn sống\" cho load balancer, một endpoint \"tôi sẵn sàng nhận việc\" cho việc triển khai.\n\n### Ba header `X-Forwarded-*` — và cạm bẫy tin sai\n\nKhi có proxy ở giữa, ứng dụng mất ba thông tin:\n\n```\nIP thật của client   → proxy phải thêm X-Forwarded-For\nGiao thức gốc        → proxy phải thêm X-Forwarded-Proto (http/https)\nTên miền gốc         → proxy phải thêm X-Forwarded-Host\n```\n\n```nginx\nlocation / {\n    proxy_pass http://ung_dung;\n    proxy_set_header Host              $host;\n    proxy_set_header X-Real-IP         $remote_addr;\n    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;\n    proxy_set_header X-Forwarded-Proto $scheme;\n}\n```\n\nHậu quả khi thiếu, rất cụ thể:\n\n| Thiếu | Hậu quả |\n|---|---|\n| `X-Forwarded-For` | log toàn IP của proxy; giới hạn tần suất theo IP vô dụng |\n| `X-Forwarded-Proto` | ứng dụng tưởng là HTTP, sinh link `http://`, có thể tạo **vòng lặp chuyển hướng** |\n| `Host` | ứng dụng nhận sai tên miền, sinh URL tuyệt đối sai |\n\nDòng giữa là lỗi rất hay gặp và có triệu chứng gây bối rối: ứng dụng thấy `http` nên chuyển hướng sang `https`, proxy nhận request https rồi lại chuyển tiếp thành http xuống ứng dụng, và vòng lặp không bao giờ dừng. Trình duyệt báo \"chuyển hướng quá nhiều lần\".\n\n**Cạm bẫy bảo mật quan trọng** — `X-Forwarded-For` là header, và **client gửi được header tuỳ ý**:\n\n```bash\ncurl -H 'X-Forwarded-For: 1.2.3.4' https://example.com/\n```\n\nNếu ứng dụng tin header này một cách vô điều kiện, kẻ tấn công **giả mạo được IP** — vượt qua giới hạn tần suất theo IP, vượt qua danh sách chặn, làm sai lệch log kiểm toán. Quy tắc đúng:\n\n```\n① CHỈ tin X-Forwarded-For khi request đến từ proxy MÀ BẠN KIỂM SOÁT\n② Header có thể chứa nhiều IP nối nhau: \"client, proxy1, proxy2\"\n   → IP thật là phần tử ĐẦU, nhưng chỉ đáng tin nếu đếm đúng số proxy của mình\n③ Framework thường có cấu hình \"số proxy tin cậy\" — dùng nó, đừng tự phân tích\n```\n\nĐiểm ③ đáng nhấn: đây là loại code dễ viết sai một cách âm thầm. Phần lớn framework web có sẵn cấu hình cho việc này (thường tên là *trusted proxies* hoặc tương tự), và dùng nó an toàn hơn tự tách chuỗi.\n\n### Timeout ở nhiều tầng — và tầng nào cắt trước\n\nĐây là nguồn của những lỗi khó chẩn đoán nhất khi có nhiều lớp trung gian:\n\n```\nTrình duyệt ──► CDN ──► Load Balancer ──► nginx ──► Ứng dụng ──► Database\n   30s?        30s        60s              60s        ?            30s\n```\n\n**Tầng nào timeout ngắn nhất sẽ cắt trước**, và client nhận lỗi từ tầng đó — chứ không phải từ tầng thật sự chậm. Ví dụ database chậm 90 giây thì client nhận `504` từ nginx sau 60 giây, và trong log nginx bạn chỉ thấy \"upstream timed out\" mà không biết vì sao.\n\nNguyên tắc cấu hình đúng: **timeout phải tăng dần từ ngoài vào trong**, hoặc ít nhất là biết rõ tầng nào cắt trước:\n\n```\nỨng dụng     25s   ← cắt trước, và ứng dụng biết trả lỗi CÓ Ý NGHĨA\nnginx        30s\nLoad Balancer 35s\nCDN          40s\n```\n\nLý do đặt ứng dụng ngắn nhất: chỉ ứng dụng biết chuyện gì đang xảy ra, nên để nó tự cắt và trả một thông báo lỗi cụ thể thì hữu ích hơn nhiều so với việc nhận một `504` chung chung từ proxy.\n\nBảng phân biệt ba mã lỗi của proxy — dùng được ngay khi vận hành:\n\n| Code | Ai sinh ra | Nghĩa | Kiểm tra gì |\n|---|---|---|---|\n| `502 Bad Gateway` | proxy | **không nối được** backend | backend còn chạy? đúng cổng? |\n| `503 Unavailable` | proxy hoặc app | không có backend nào khoẻ | health check, quá tải |\n| `504 Gateway Timeout` | proxy | backend **còn sống nhưng chậm** | query chậm, khoá database |\n\nPhân biệt `502` và `504` là chẩn đoán có giá trị nhất trong bảng: `502` là *backend không trả lời gì cả* (chết, sai cổng, bind sai địa chỉ — nối về bài IP/port), còn `504` là *backend đang xử lý mà chưa xong*. Hai nguyên nhân hoàn toàn khác nhau và hai cách sửa khác nhau.\n\n### CDN — vì sao nhanh hơn\n\n```\nKHÔNG có CDN\n  Người dùng ở Hà Nội ──── 250 ms ────► Server ở Mỹ\n  Mỗi file: 250 ms chỉ để đi và về\n\nCÓ CDN\n  Người dùng ở Hà Nội ──── 15 ms ────► PoP Singapore (đã cache)\n                                        │ chỉ khi thiếu:\n                                        └── 200 ms ──► Origin ở Mỹ\n```\n\nĐiều quan trọng phải hiểu đúng: **giá trị chính của CDN là giảm RTT, không phải tăng băng thông.** Nối lại với bài TCP: thông lượng ≈ cửa sổ / RTT, và mỗi kết nối mới tốn một RTT bắt tay TCP cộng một RTT TLS. Giảm RTT từ 250 ms xuống 15 ms cải thiện **mọi thứ** — bắt tay nhanh hơn, cửa sổ TCP mở rộng nhanh hơn, mọi request nhanh hơn. Không có tối ưu code nào thay thế được việc đặt server gần người dùng hơn.\n\nCDN tìm PoP gần nhất bằng **DNS**: cùng một tên miền, resolver ở Việt Nam nhận IP của PoP Singapore, resolver ở Đức nhận IP của PoP Frankfurt. Đây là ứng dụng trực tiếp của bài IP/port/DNS, và cũng là lý do `CNAME` trỏ về CDN tiện hơn `A` — nhà cung cấp đổi IP bao nhiêu lần cũng không cần bạn sửa.\n\n### Cache ở CDN — nơi dễ gây sự cố nhất\n\nQuy tắc phân loại phải rõ ràng:\n\n```\nCACHE ĐƯỢC (giống nhau với mọi người)\n  ảnh, CSS, JS, font, video\n  trang tĩnh không phụ thuộc người dùng\n\nKHÔNG BAO GIỜ CACHE (khác nhau theo từng người)\n  phản hồi có Set-Cookie\n  trang sau đăng nhập\n  API trả dữ liệu riêng của người dùng\n```\n\n```nginx\n# ⚠️ Nếu tự làm lớp cache, đây là các dòng BẮT BUỘC\nproxy_cache_bypass $cookie_session $http_authorization;\nproxy_no_cache     $cookie_session $http_authorization;\n\n# Không cache phản hồi có Set-Cookie\nproxy_hide_header Set-Cookie;      # chỉ trên đường dẫn CHẮC CHẮN tĩnh\n```\n\nĐây chính là sự cố đã nêu ở phần *vì sao cần*: nếu CDN cache một phản hồi có `Set-Cookie`, nó sẽ phát **cùng một session ID** cho mọi người truy cập sau. Người thứ hai vào trang sẽ đăng nhập thành người thứ nhất. Đây là lỗ hổng nghiêm trọng và nguyên nhân chỉ là một dòng cấu hình thiếu.\n\nHeader `Vary` là công cụ đúng khi phản hồi khác nhau theo điều kiện:\n\n```http\nVary: Accept-Encoding        ← cache riêng bản gzip và bản không nén\nVary: Accept-Language        ← cache riêng theo ngôn ngữ\nVary: Cookie                 ← ⚠️ gần như VÔ HIỆU HOÁ cache\n```\n\nDòng cuối đáng cảnh báo: `Vary: Cookie` nghĩa là mỗi tổ hợp cookie khác nhau là một bản cache riêng. Vì cookie gần như luôn khác nhau giữa người dùng, tỉ lệ cache hit tụt về gần 0. An toàn, nhưng cũng gần như không còn cache — nên với những đường dẫn thật sự riêng tư, nói thẳng `Cache-Control: private, no-store` là rõ ràng hơn.\n\n### Purge cache và vì sao đánh phiên bản tốt hơn\n\n```bash\n# Purge: xoá cache thủ công — mọi CDN đều có API cho việc này\n# Nhược điểm: mất thời gian lan ra mọi PoP, và dễ quên\n\n# Cách tốt hơn: đánh phiên bản vào tên file\n/static/app.a3f9c1.js      ← đổi nội dung ⇒ đổi hash ⇒ đổi URL\n                             ⇒ KHÔNG cần purge, cache một năm vẫn đúng\n```\n\nĐây là ý đã gặp ở bài HTTP, và ở đây nó quan trọng hơn nữa: purge trên CDN không tức thì và không bao giờ đáng tin tuyệt đối, còn URL mới thì **không cần purge chút nào**. Với chiến lược đánh phiên bản, bạn được cache một năm mà vẫn cập nhật tức thì khi deploy.\n\n### Xếp lớp trong hệ thống thật\n\n```\nNgười dùng\n    │\n    ▼\n┌─────────────┐  CDN          cache tĩnh, chặn tấn công, TLS ở biên\n└──────┬──────┘\n       ▼\n┌─────────────┐  Load Balancer  cân tải, health check, TLS\n└──────┬──────┘\n       ▼\n┌─────────────┐  nginx          file tĩnh, nén, giới hạn tần suất, định tuyến\n└──────┬──────┘\n       ▼\n┌─────────────┐  Ứng dụng       CHỈ còn logic nghiệp vụ\n└─────────────┘\n```\n\nHai nhận xét cân bằng về sơ đồ này:\n\n**Lợi:** mỗi lớp làm đúng một việc và làm rất tốt việc đó. Ứng dụng cuối cùng chỉ còn logic nghiệp vụ — dễ viết, dễ kiểm thử, dễ thay thế.\n\n**Giá:** mỗi lớp là một chỗ có thể hỏng, một chỗ có timeout riêng, một chỗ có cache riêng, một chỗ phải giám sát. Gỡ lỗi phải xác định được **lỗi phát sinh ở lớp nào** trước khi sửa gì. Và không phải hệ thống nào cũng cần đủ bốn lớp — một ứng dụng nhỏ với nginx trước nó là hoàn toàn hợp lý; thêm CDN và load balancer khi chưa có nhu cầu chỉ là thêm việc.\n\nCâu hỏi đúng để quyết định thêm một lớp: **lớp này giải quyết vấn đề nào tôi đang thật sự có?** Nếu không trả lời được cụ thể thì chưa cần nó.",

vi_du: "### Xác định request đi qua những lớp nào\n\n```bash\ncurl -I https://example.com/\n\n# server: cloudflare              ← có CDN\n# cf-cache-status: HIT            ← được phục vụ từ cache CDN\n# age: 3542                       ← đã ở trong cache 3542 giây\n# x-served-by: cache-sin-1        ← PoP Singapore\n# via: 1.1 varnish                ← còn một lớp cache nữa\n```\n\nCác header đáng đọc để biết mình đang nói với ai:\n\n| Header | Cho biết |\n|---|---|\n| `via` | đã đi qua proxy nào |\n| `age` | phản hồi đã nằm trong cache bao lâu |\n| `x-cache` / `cf-cache-status` | HIT (từ cache) hay MISS (đi tới origin) |\n| `server` | phần mềm ở lớp ngoài cùng |\n\n`age` rất hữu ích khi chẩn đoán \"đã deploy mà vẫn thấy bản cũ\": `age` lớn nghĩa là bạn đang xem một bản cache cũ, không phải bản mới chưa deploy xong.\n\n### Kiểm tra CDN có hoạt động không\n\n```bash\n# ① Cùng tên miền, hỏi từ các resolver khác nhau ⇒ IP khác nhau\nfor r in 8.8.8.8 1.1.1.1; do\n  echo -n \"$r → \"; dig +short @$r cdn.example.com | head -1\ndone\n\n# ② Gọi hai lần: lần đầu MISS, lần sau HIT\ncurl -sI https://example.com/static/app.js | grep -i cache-status\n# cf-cache-status: MISS\ncurl -sI https://example.com/static/app.js | grep -i cache-status\n# cf-cache-status: HIT       ← ✓ cache đang hoạt động\n\n# ③ So độ trễ qua CDN và trực tiếp tới origin\ncurl -so /dev/null -w 'CDN:    %{time_total}s\\n' https://example.com/static/app.js\ncurl -so /dev/null -w 'origin: %{time_total}s\\n' https://origin.example.com/static/app.js\n```\n\nPhép thử ② nên chạy sau mỗi lần đổi cấu hình cache: nếu lần thứ hai vẫn `MISS`, cache không hoạt động và bạn đang trả tiền cho CDN mà không được lợi ích chính của nó.\n\n### Cấu hình nginx làm reverse proxy — bản đầy đủ\n\n```nginx\nupstream ung_dung {\n    server 10.0.1.10:8000 max_fails=3 fail_timeout=30s;\n    server 10.0.1.11:8000 max_fails=3 fail_timeout=30s;\n    keepalive 32;                       # dùng lại kết nối tới backend\n}\n\nserver {\n    listen 443 ssl http2;\n    server_name example.com;\n\n    ssl_certificate     /etc/letsencrypt/live/example.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;\n\n    gzip on;\n    gzip_types text/css application/javascript application/json;\n    gzip_min_length 1024;               # file nhỏ hơn thì nén không đáng\n\n    # File tĩnh: nginx đọc trực tiếp, KHÔNG đánh thức ứng dụng\n    location /static/ {\n        alias /var/www/static/;\n        expires 1y;\n        add_header Cache-Control \"public, immutable\";\n    }\n\n    location / {\n        proxy_pass http://ung_dung;\n        proxy_http_version 1.1;\n        proxy_set_header Connection \"\";           # bật keepalive tới backend\n        proxy_set_header Host              $host;\n        proxy_set_header X-Real-IP         $remote_addr;\n        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n        proxy_connect_timeout 5s;\n        proxy_read_timeout   30s;\n    }\n}\n```\n\nBốn dòng đáng giải thích vì lý do đằng sau không hiển nhiên:\n\n```\nkeepalive 32                 dùng lại kết nối TCP tới backend\n                             ⇒ không bắt tay lại mỗi request (bài TCP)\nproxy_set_header Connection \"\"  BẮT BUỘC để keepalive hoạt động;\n                             không có nó nginx gửi \"Connection: close\"\ngzip_min_length 1024         file nhỏ nén xong có khi to hơn, và tốn CPU vô ích\nexpires 1y + immutable       chỉ đúng khi tên file CÓ HASH\n```\n\nDòng cuối là điều kiện quan trọng: `immutable` cho một file không có hash trong tên là tự bắn vào chân — đổi nội dung thì không ai nhận được bản mới trong một năm.\n\n### Giới hạn tần suất ở proxy\n\n```nginx\n# Định nghĩa vùng: theo IP, cho phép 10 request/giây\nlimit_req_zone $binary_remote_addr zone=chung:10m rate=10r/s;\nlimit_req_zone $binary_remote_addr zone=dangnhap:10m rate=5r/m;\n\nlocation /api/ {\n    limit_req zone=chung burst=20 nodelay;\n    proxy_pass http://ung_dung;\n}\n\nlocation /api/login {\n    limit_req zone=dangnhap burst=3;    # 5 lần/PHÚT — chống dò mật khẩu\n    proxy_pass http://ung_dung;\n}\n```\n\nBa điểm đáng học từ đoạn này:\n\n```\nĐặt ở PROXY, không ở ứng dụng ⇒ request bị chặn KHÔNG tiêu tốn tài nguyên app\nburst cho phép vượt ngắn hạn  ⇒ tránh chặn oan lúc tải trang có nhiều request\nĐăng nhập giới hạn RẤT chặt   ⇒ 5 lần/phút là đủ cho người thật,\n                                nhưng phá vỡ hoàn toàn việc dò mật khẩu tự động\n```\n\nMột lưu ý nối với phần header: nếu nginx nằm sau một CDN, thì `$binary_remote_addr` là **IP của CDN**, và giới hạn tần suất sẽ áp cho toàn bộ người dùng đi qua PoP đó — chặn oan hàng loạt. Trong trường hợp đó phải dùng biến chứa IP thật từ `X-Forwarded-For`, và chỉ khi CDN đó là đáng tin.\n\n### Chẩn đoán `502` và `504`\n\n```bash\n# 502: proxy không NỐI ĐƯỢC backend\ntail -f /var/log/nginx/error.log\n# connect() failed (111: Connection refused) while connecting to upstream\n#                        ↑ nối về bài IP/port: không ai nghe cổng đó\n\n# Kiểm tra theo thứ tự từ tầng dưới lên\nss -tlnp | grep 8000                    # ứng dụng có nghe không?\ncurl -I http://10.0.1.10:8000/          # gọi trực tiếp backend được không?\nsystemctl status ung-dung               # tiến trình còn sống?\n\n# 504: backend CÒN SỐNG nhưng chậm\n# upstream timed out (110: Connection timed out) while reading response\ncurl -o /dev/null -w '%{time_total}\\n' http://10.0.1.10:8000/api/cham\n# 45.231      ← chậm thật ⇒ vấn đề ở ứng dụng/database, không phải ở nginx\n```\n\nQuy trình chẩn đoán đúng thứ tự, và đây là lúc bài đầu module phát huy giá trị:\n\n```\n① Gọi TRỰC TIẾP backend, bỏ qua proxy\n   → cũng lỗi  ⇒ vấn đề ở ứng dụng\n   → không lỗi ⇒ vấn đề ở proxy hoặc ở mạng giữa proxy và backend\n② Xem error.log của proxy — nó nói rõ \"refused\" hay \"timed out\"\n③ Chỉ sau khi biết lớp nào hỏng thì mới sửa\n```\n\nBước ① tiết kiệm rất nhiều thời gian và là thứ nên làm trước mọi phỏng đoán khác.\n\n### Sự cố cache phát cookie của người khác\n\n```bash\n# ① Phát hiện: phản hồi cache được mà có Set-Cookie\ncurl -sI https://example.com/ | grep -iE 'set-cookie|cache-control|cf-cache-status'\n# set-cookie: session=a3f9c1b2; Path=/\n# cache-control: public, max-age=300      ← ⚠️ NGHIÊM TRỌNG\n# cf-cache-status: HIT                    ← ⚠️ đang phát lại cho mọi người\n\n# ② Kiểm chứng: hai lần gọi \"khác người\" mà nhận CÙNG session\ncurl -sI https://example.com/ | grep -i set-cookie\ncurl -sI https://example.com/ | grep -i set-cookie\n# hai lần GIỐNG NHAU ⇒ đang phát chung một phiên cho mọi người\n```\n\nCách sửa, theo thứ tự ưu tiên:\n\n```nginx\n# ① Đúng nhất: trang động KHÔNG cache được\nlocation / {\n    proxy_pass http://ung_dung;\n    proxy_no_cache     $cookie_session $http_authorization;\n    proxy_cache_bypass $cookie_session $http_authorization;\n}\n\n# ② Ở tầng ứng dụng: nói rõ đây là riêng tư\n# Cache-Control: private, no-store\n\n# ③ Chỉ cache những đường dẫn CHẮC CHẮN tĩnh, liệt kê tường minh\nlocation /static/ { proxy_cache tinh; }\n```\n\nCách ③ là nguyên tắc đáng ghi nhớ hơn cả: **whitelist thay vì blacklist.** Liệt kê những gì được cache thì an toàn hơn nhiều so với liệt kê những gì không được cache — vì cái sau chỉ cần bỏ sót một đường dẫn là thành sự cố.\n\n### Forward proxy — phía còn lại\n\n```bash\n# Dùng forward proxy cho một lệnh\nexport https_proxy=http://proxy.congty.com:3128\ncurl https://example.com          # đi qua proxy\n\n# Kiểm tra: IP mà server thấy là IP của proxy\ncurl -s ifconfig.me               # trả về IP của PROXY, không phải của bạn\n\n# Bỏ qua proxy cho mạng nội bộ\nexport no_proxy=localhost,127.0.0.1,10.0.0.0/8\n```\n\nBiến `no_proxy` là chi tiết dễ quên mà gây lỗi khó hiểu: nếu không đặt, các lệnh gọi tới `localhost` cũng đi qua proxy công ty và thất bại — trong khi mọi thứ trông như lỗi ứng dụng.\n\nĐây cũng là nơi thấy rõ sự khác biệt hai loại proxy: bạn **tự cấu hình** forward proxy trên máy mình; còn reverse proxy thì chủ website cấu hình, và bạn không biết nó có ở đó.",

so_sanh: "### Forward proxy và reverse proxy\n\n| | Forward proxy | Reverse proxy |\n|---|---|---|\n| Đặt gần | client | server |\n| Ai cấu hình | **client** | **chủ server** |\n| Client biết nó | ✓ | ✗ |\n| Ẩn ai | **client** | **server** |\n| Mục đích chính | lọc, kiểm soát truy cập của client | TLS, cân tải, cache, bảo vệ server |\n| Thấy ở đâu | mạng công ty, trường học | **mọi hệ thống web thật** |\n\nCâu chốt để không nhầm: **cùng một phần mềm, khác chỗ đứng.** nginx làm được cả hai vai; điều quyết định là nó đứng trước client hay trước server.\n\n### Load balancer tầng 4 và tầng 7\n\n| | L4 | L7 |\n|---|---|---|\n| Đọc được | IP, cổng | **toàn bộ HTTP** |\n| Định tuyến theo | địa chỉ | đường dẫn, `Host`, cookie, header |\n| Kết thúc TLS | ✗ | **✓** |\n| Tốc độ | **rất nhanh** | chậm hơn, tốn CPU |\n| Cache | ✗ | ✓ |\n| Sticky session | theo IP | **theo cookie — chính xác hơn** |\n| Dùng khi | cần thông lượng thô | cần định tuyến theo nội dung |\n\nDòng \"sticky session\" là ưu điểm cụ thể của L7 mà ít được nhắc: sticky theo cookie chính xác hơn theo IP rất nhiều, vì nó không bị ảnh hưởng bởi NAT — cả công ty dùng chung IP nhưng mỗi người có cookie riêng.\n\nDòng \"kết thúc TLS\" nối với bài HTTPS: L7 phải giải mã mới đọc được URL, nên phải giữ chứng chỉ; L4 chuyển tiếp byte đã mã hoá nguyên vẹn nên nhanh hơn và không cần biết gì.\n\n### CDN và reverse proxy tự dựng\n\n| | Reverse proxy tự dựng | CDN |\n|---|---|---|\n| Vị trí | một chỗ | **rải khắp thế giới** |\n| Giảm RTT | ✗ | **✓ giá trị lớn nhất** |\n| Chống tấn công quy mô lớn | hạn chế | **✓** |\n| Kiểm soát cấu hình | **toàn quyền** | theo giới hạn nhà cung cấp |\n| Chi phí | server của bạn | theo lưu lượng |\n| Gỡ lỗi | dễ, log tại chỗ | khó hơn, log ở nhà cung cấp |\n\nKhi nào cần CDN: **khi người dùng ở xa server**, hoặc khi có nhiều nội dung tĩnh, hoặc khi cần chịu tải đột biến. Nếu toàn bộ người dùng ở cùng thành phố với server thì CDN thêm một lớp phức tạp mà lợi ích rất nhỏ — đây là chỗ dễ thêm hạ tầng theo phong trào thay vì theo nhu cầu.\n\n### Cache ở các tầng\n\n| Tầng | Ai kiểm soát | Xoá được không | Phạm vi |\n|---|---|---|---|\n| Trình duyệt | người dùng | **✗ bạn không xoá được** | một người |\n| CDN | bạn, qua API purge | ✓ nhưng không tức thì | mọi người |\n| Reverse proxy | bạn, toàn quyền | **✓ tức thì** | mọi người |\n| Ứng dụng | bạn | ✓ | tuỳ thiết kế |\n\nHàng đầu là hàng quyết định chiến lược: **cache trình duyệt bạn không bao giờ xoá được**, nên `max-age` dài chỉ an toàn khi URL sẽ đổi lúc nội dung đổi. Đây là lý do chiến lược đánh phiên bản vào tên file quan trọng hơn cả việc chọn số `max-age`.\n\n### Ba mã lỗi của proxy\n\n| | `502` | `503` | `504` |\n|---|---|---|---|\n| Backend nối được | **✗** | không có cái nào khoẻ | ✓ |\n| Backend trả lời | ✗ | ✗ | **quá chậm** |\n| Nguyên nhân hay gặp | tiến trình chết, sai cổng, bind sai địa chỉ | quá tải, health check loại hết | query chậm, khoá database |\n| Retry có ích | có thể | ✓ có, kèm `Retry-After` | ít khi |\n| Kiểm tra đầu tiên | `ss -tlnp` trên backend | health check, tài nguyên | gọi trực tiếp backend, đo thời gian |\n\nHàng cuối là quy trình chẩn đoán gói lại thành một dòng cho mỗi mã. `502` nối trực tiếp về bài IP/port: nguyên nhân số một là ứng dụng bind vào `127.0.0.1` nên proxy trên máy khác không nối được.",

loi_thuong_gap: [
  "**Quên `X-Forwarded-For`.** Log toàn IP của proxy, giới hạn tần suất theo IP trở nên vô dụng.",
  "**Tin `X-Forwarded-For` vô điều kiện.** Client gửi được header tuỳ ý ⇒ giả mạo IP được. Chỉ tin khi request đến từ proxy mình kiểm soát, và dùng cấu hình *trusted proxies* của framework.",
  "**Quên `X-Forwarded-Proto`.** Ứng dụng tưởng là HTTP, sinh link sai hoặc tạo vòng lặp chuyển hướng vô tận.",
  "**Cache phản hồi có `Set-Cookie`.** Phát cùng một session cho mọi người — sự cố bảo mật thật. Dùng `proxy_no_cache` và whitelist đường dẫn tĩnh.",
  "**Blacklist thay vì whitelist khi cấu hình cache.** Bỏ sót một đường dẫn động là thành sự cố. Liệt kê những gì ĐƯỢC cache.",
  "**Timeout các tầng không nhất quán.** Client nhận lỗi từ tầng cắt trước, không phải tầng thật sự chậm. Nên để ứng dụng timeout ngắn nhất để nó trả lỗi có ý nghĩa.",
  "**Không có health check.** Proxy vẫn gửi request tới server đã chết cho tới khi có người phát hiện.",
  "**Health check kiểm tra cả database.** Database chập chờn là loại toàn bộ backend cùng lúc, biến sự cố nhỏ thành sập hoàn toàn. Tách endpoint \"còn sống\" và \"sẵn sàng\".",
  "**Dùng `ip_hash` cho sticky session.** Cả công ty qua NAT có cùng IP nên dồn vào một server. Tốt hơn là bỏ nhu cầu sticky bằng cách lưu session ở Redis.",
  "**Quên `proxy_set_header Connection \"\"` khi bật keepalive tới backend.** Keepalive không hoạt động, và mỗi request lại bắt tay TCP mới.",
  "**Đặt `immutable` cho file không có hash trong tên.** Đổi nội dung thì không ai nhận được bản mới trong suốt `max-age`.",
  "**Giới hạn tần suất theo `$remote_addr` khi nằm sau CDN.** Đó là IP của CDN ⇒ chặn oan toàn bộ người dùng qua PoP đó.",
  "**Dựa vào purge CDN thay vì đánh phiên bản URL.** Purge không tức thì và dễ quên; URL mới thì không cần purge.",
  "**Thêm CDN khi toàn bộ người dùng ở cùng thành phố với server.** Thêm một lớp phức tạp mà lợi ích rất nhỏ.",
  "**Quên `no_proxy` khi dùng forward proxy.** Lệnh gọi `localhost` cũng đi qua proxy công ty và thất bại, trông như lỗi ứng dụng."
],

tu_kiem_tra: [
  "Phân biệt forward proxy và reverse proxy bằng câu hỏi \"đứng trước ai\" và \"ai cấu hình\".",
  "Nêu năm việc mà reverse proxy gánh hộ ứng dụng, và nguyên tắc chung để quyết định đẩy việc gì ra proxy.",
  "Vì sao không được tin `X-Forwarded-For` vô điều kiện? Kẻ tấn công lợi dụng được gì?",
  "Thiếu `X-Forwarded-Proto` gây ra hiện tượng gì, và vì sao nó thành vòng lặp?",
  "Giá trị chính của CDN là gì — băng thông hay RTT? Liên hệ với công thức thông lượng của TCP.",
  "CDN tìm PoP gần nhất bằng cơ chế nào?",
  "Mô tả sự cố \"CDN phát session của người khác\": nguyên nhân, cách phát hiện, cách sửa.",
  "Vì sao whitelist an toàn hơn blacklist khi cấu hình cache?",
  "`Vary: Cookie` có tác dụng gì với tỉ lệ cache hit, và khi nào nên dùng cách khác?",
  "Phân biệt `502`, `503`, `504`: mỗi mã nói gì về backend và kiểm tra gì đầu tiên?",
  "Nên đặt timeout tăng dần từ ngoài vào trong hay ngược lại? Vì sao ứng dụng nên cắt trước?",
  "`ip_hash` có nhược điểm gì, và giải pháp tốt hơn cho sticky session là gì?",
  "Vì sao `immutable` chỉ an toàn khi tên file có hash?"
],

tai_nguyen: [
  "Tài liệu nginx, các mục *ngx_http_proxy_module*, *ngx_http_upstream_module*, *ngx_http_limit_req_module* — đây là tài liệu tra cứu chính khi cấu hình reverse proxy.",
  "Sách **HTTP: The Definitive Guide** — David Gourley & Brian Totty. Các chương về proxy, cache và gateway vẫn là giải thích đầy đủ nhất về mô hình này.",
  "Sách **High Performance Browser Networking** — Ilya Grigorik. Chương về CDN và về latency giải thích rõ vì sao giảm RTT quan trọng hơn tăng băng thông.",
  "Tài liệu **MDN Web Docs**, mục *HTTP caching* và *Vary* — tra cứu chính xác hành vi cache.",
  "Tài liệu của nhà cung cấp CDN bạn dùng, mục về quy tắc cache và purge — mỗi bên có mặc định và cú pháp khác nhau, và mặc định là chỗ hay gây bất ngờ."
],

lien_he: [
  "**WebSocket vs polling** ngay trước: phần lớn lỗi realtime là lỗi cấu hình proxy — `Upgrade`, `proxy_buffering`, timeout.",
  "**HTTP: method, header, status code**: cache HTTP, `Vary`, `X-Forwarded-For`, `502`/`504` đều bắt nguồn từ đó.",
  "**HTTPS, TLS handshake**: kết thúc TLS ở đâu chính là quyết định về reverse proxy, và `fullchain.pem` là cấu hình của lớp này.",
  "**Cookie, session và trạng thái**: sự cố cache `Set-Cookie` là điểm gặp nhau nguy hiểm nhất giữa hai bài.",
  "**IP, port, DNS**: CDN chọn PoP bằng DNS; `502` thường do backend bind vào `127.0.0.1`.",
  "**TCP vs UDP**: `keepalive` tới backend tránh bắt tay lặp lại; giảm RTT cải thiện cửa sổ TCP.",
  "**Mô hình TCP/IP 4 tầng**: L4 và L7 là cách phân loại thiết bị theo tầng nó đọc được — bài này là ứng dụng đầy đủ của ý đó.",
  "**Backend & Web** ở mảng 05: cấu hình nginx trước ứng dụng khi deploy là việc bắt buộc, và bài này là phần lý thuyết của nó.",
  "**System Design** ở mảng 06: cân tải, cache nhiều tầng, giảm tải TLS, chống tấn công — toàn bộ chương về scalability dựng trên bài này.",
  "Đây là **bài cuối module Mạng máy tính**: module 5 chuyển sang Python, nơi bạn sẽ tự viết client và server dùng đúng những giao thức vừa học."
]

};
