window.LESSONS["01:clean-code-thiet-ke:tranh-magic-number-va-co-boolean-trong-tham-so"] = {

tom_tat: [
  "**Magic number là một con số không tự giải thích được.** Vấn đề không phải nó là số, mà là nó mang **tri thức nghiệp vụ** nhưng không có tên.",
  "**Không phải mọi số đều là magic number.** `0`, `1`, `2` trong ngữ cảnh rõ ràng thì đặt tên vào còn tệ hơn — `MOT = 1` là nhiễu.",
  "**Cờ boolean trong tham số nói rằng hàm làm HAI việc.** `gui(don, True)` — người đọc nơi gọi không biết `True` là gì, và tên hàm nói dối.",
  "**Ba cách sửa cờ boolean, theo thứ tự ưu tiên:** tách thành hai hàm (tốt nhất) → dùng enum → cuối cùng mới là tham số chỉ-tên.",
  "**Enum mạnh hơn hằng số rời ở ba điểm đo được:** không tạo được giá trị sai, `mypy` bắt lỗi lúc kiểm chứ không lúc chạy, và có chỗ đặt hành vi kèm theo."
],

can_biet_truoc: [
  "Bài **Đặt tên có nghĩa** trong module này — hằng số có tên là ứng dụng trực tiếp nhất của nó.",
  "Bài **Hàm nhỏ làm đúng một việc** trong module này — cờ boolean là dấu hiệu hàm làm hai việc.",
  "Bài **Hàm, tham số mặc định** ở module 5 — cú pháp tham số chỉ-tên `*`.",
  "Bài **SOLID — Open/Closed** ở module 6 — thay `if` theo cờ bằng đa hình."
],

dinh_nghia: "Nói đời thường: một tờ giấy ghi *\"30\"* dán trên tường không nói gì. Ghi *\"Hạn trả sách: 30 ngày\"* thì ai đọc cũng hiểu và ai muốn sửa cũng biết sửa đâu.\n\nNói chính xác:\n\n- **Magic number** — một giá trị nghĩa đen (số, chuỗi, hoặc bất kỳ hằng nào) xuất hiện thẳng trong code mà **mang tri thức nghiệp vụ nhưng không có tên**. Người đọc phải suy ra nghĩa từ ngữ cảnh xung quanh.\n- **Magic string** — cùng vấn đề với chuỗi: `if trang_thai == \"da_duyet\"`. Nguy hiểm hơn số vì gõ sai không ai báo.\n- **Cờ boolean** (*boolean flag / control coupling*) — tham số kiểu `bool` dùng để **chọn nhánh xử lý** bên trong hàm, chứ không phải để mang dữ liệu.\n- **Hằng số có tên** — gán giá trị cho một tên viết hoa ở phạm vi module: `SO_NGAY_MUON_TOI_DA = 30`.\n- **Enum** — một kiểu liệt kê tập giá trị hợp lệ đóng. Python có `enum.Enum`, `enum.IntEnum`, `enum.StrEnum` (từ 3.11).\n\nMột phân biệt quan trọng mà nhiều người bỏ qua: **tham số boolean mang dữ liệu thì không sao; tham số boolean chọn nhánh mới là vấn đề.**\n\n```python\nnguoi_dung.dang_hoat_dong = True      # ✓ DỮ LIỆU — đây là trạng thái thật\nxuat_bao_cao(don, chi_tiet=True)      # ⚠️ ĐIỀU KHIỂN — chọn nhánh xử lý\n```\n\nDòng đầu mô tả sự thật về đối tượng. Dòng sau ra lệnh cho hàm chạy nhánh nào — và đó là dấu hiệu hàm có hai hành vi trộn vào nhau.",

vi_sao_can: "Hai vấn đề này nhỏ về hình thức nhưng gây hậu quả rất cụ thể:\n\n- **Magic number làm việc sửa trở nên nguy hiểm.** Số `0.1` xuất hiện năm chỗ: ba chỗ là thuế, hai chỗ là chiết khấu. Đổi thuế mà tìm-thay `0.1` thì hỏng luôn chiết khấu — và không ai phát hiện cho tới khi khách phàn nàn.\n- **Magic string sai chính tả không ai báo.** `if trang_thai == \"da_duyet\"` mà chỗ khác ghi `\"đã_duyệt\"` thì điều kiện luôn sai — chương trình vẫn chạy, chỉ là im lặng làm sai.\n- **Cờ boolean làm nơi gọi không đọc được.** `gui_mail(don, True, False, True)` — muốn hiểu phải mở định nghĩa hàm ra đọc. Nhân với số lần đọc code trong đời dự án.\n- **Cờ boolean làm số test tăng theo cấp số nhân.** Ba cờ là tám tổ hợp; thực tế test hai ba cái.\n- **Cả hai đều cản việc thay đổi.** Muốn thêm một trạng thái mới vào `bool` thì không có chỗ — phải đổi kiểu, và mọi nơi gọi đều phải sửa.\n\nTình huống thật rất hay gặp: bạn thấy `if len(mk) < 8` trong hàm đăng ký. Đội bảo mật yêu cầu nâng lên 12. Bạn `grep` tìm `< 8` và thấy bảy chỗ — nhưng chỉ ba chỗ là độ dài mật khẩu, bốn chỗ kia là số dòng tối đa, số lần thử, số cột. Không có cách nào phân biệt bằng máy; bạn phải đọc từng chỗ. Với một hằng số có tên `DO_DAI_MAT_KHAU_TOI_THIEU`, cùng việc đó là **sửa một dòng** — và trình soạn thảo tự tìm đúng mọi nơi dùng.",

co_che: "### Khi nào một con số là \"magic\"\n\nPhép kiểm ba câu:\n\n```\n① Người đọc có phải SUY LUẬN để biết số này nghĩa gì không?\n② Nếu đổi số này, tôi có phải hiểu NGHIỆP VỤ mới biết đổi thành bao nhiêu?\n③ Số này có xuất hiện ở NHIỀU chỗ với cùng ý nghĩa không?\n\nCó ít nhất một câu \"có\" → nên đặt tên.\n```\n\n```python\n# ❌ Magic number — cả ba câu đều \"có\"\nif len(mat_khau) < 8: ...\nif so_ngay_muon > 30: ...\ntien = gia * 1.1\nif lan_thu > 3: ...\n\n# ✓ Hằng số có tên\nDO_DAI_MAT_KHAU_TOI_THIEU = 8\nSO_NGAY_MUON_TOI_DA = 30\nTHUE_SUAT_GTGT = 0.10\nSO_LAN_THU_LAI_TOI_DA = 3\n\nif len(mat_khau) < DO_DAI_MAT_KHAU_TOI_THIEU: ...\nif so_ngay_muon > SO_NGAY_MUON_TOI_DA: ...\ntien = gia * (1 + THUE_SUAT_GTGT)\nif lan_thu > SO_LAN_THU_LAI_TOI_DA: ...\n```\n\nChú ý dòng thuế: bản gốc viết `gia * 1.1`, bản mới viết `gia * (1 + THUE_SUAT_GTGT)`. Con số `1.1` che giấu **hai** thông tin — thuế suất là 10%, và công thức là cộng thêm vào giá. Bản mới nói cả hai.\n\n### ⚠️ Không phải mọi số đều là magic number\n\nĐây là chỗ nguyên tắc hay bị áp máy móc thành có hại:\n\n```python\n# ✓ Hoàn toàn ổn — số tự giải thích trong ngữ cảnh\nif len(ds) == 0: ...              # 0 = rỗng, ai cũng hiểu\nfor i in range(1, n + 1): ...     # 1 = bắt đầu từ 1\nnua = tong / 2                     # 2 = chia đôi\nx, y = diem[0], diem[1]            # chỉ số toạ độ\n\n# ❌ Đặt tên vào còn TỆ HƠN — nhiễu thuần tuý\nKHONG = 0\nMOT = 1\nHAI = 2\nif len(ds) == KHONG: ...\n```\n\nRanh giới rõ ràng:\n\n```\nSỐ KỸ THUẬT      0, 1, 2 trong ngữ cảnh hiển nhiên  → để nguyên\nSỐ NGHIỆP VỤ     8, 30, 0.10, 3                      → đặt tên\n```\n\nPhép kiểm phân biệt: **nếu người làm nghiệp vụ có thể yêu cầu đổi con số này, nó cần có tên.** Kế toán có thể yêu cầu đổi thuế suất; không ai yêu cầu đổi \"chia đôi\" thành \"chia ba\".\n\nMột trường hợp ranh giới đáng bàn — số trong công thức toán:\n\n```python\n# ✓ Để nguyên: đây là công thức, không phải quy tắc nghiệp vụ\ndien_tich = math.pi * ban_kinh ** 2\nquang_duong = 0.5 * gia_toc * t ** 2\n\n# Đặt SO_HAI = 2 ở đây làm công thức khó nhận ra hơn.\n```\n\n### Magic string nguy hiểm hơn magic number\n\n```python\n# ❌ Gõ sai không ai báo\ndef xu_ly(don):\n    if don.trang_thai == \"cho_duyet\":\n        ...\n    elif don.trang_thai == \"da_duyet\":\n        ...\n\n# Ở file khác, ai đó viết:\ndon.trang_thai = \"daduyet\"        # ⚠️ thiếu dấu gạch\n# Không lỗi. Không cảnh báo. Đơn im lặng không bao giờ được xử lý.\n```\n\n```python\n# ✓ Enum — sai tên là lỗi NGAY LÚC CHẠY DÒNG ĐÓ, và mypy bắt trước cả thế\nfrom enum import Enum\n\nclass TrangThaiDon(Enum):\n    CHO_DUYET = \"cho_duyet\"\n    DA_DUYET = \"da_duyet\"\n    DA_HUY = \"da_huy\"\n\ndon.trang_thai = TrangThaiDon.DADUYET    # ⭐ AttributeError ngay lập tức\n```\n\nBa lợi ích của enum so với hằng số chuỗi rời:\n\n```\n① KHÔNG TẠO ĐƯỢC GIÁ TRỊ SAI\n   TrangThaiDon(\"linh tinh\") → ValueError ngay\n\n② mypy KIỂM ĐƯỢC\n   def xu_ly(tt: TrangThaiDon)  → truyền chuỗi là lỗi lúc kiểm, không phải lúc chạy\n\n③ CÓ CHỖ ĐẶT HÀNH VI KÈM THEO\n   Enum là class — thêm được property và method\n```\n\nĐiểm ③ là thứ hằng số rời không làm được:\n\n```python\nfrom enum import Enum\n\nclass TrangThaiDon(Enum):\n    CHO_DUYET = \"cho_duyet\"\n    DA_DUYET = \"da_duyet\"\n    DA_HUY = \"da_huy\"\n\n    @property\n    def cho_phep_sua(self) -> bool:\n        return self is TrangThaiDon.CHO_DUYET\n\n    @property\n    def nhan_hien_thi(self) -> str:\n        return {\n            TrangThaiDon.CHO_DUYET: \"Chờ duyệt\",\n            TrangThaiDon.DA_DUYET: \"Đã duyệt\",\n            TrangThaiDon.DA_HUY: \"Đã huỷ\",\n        }[self]\n\n# Tri thức \"trạng thái nào cho sửa\" nằm CÙNG CHỖ với định nghĩa trạng thái\nif don.trang_thai.cho_phep_sua:\n    ...\n```\n\nĐây là DRY áp đúng chỗ: thêm một trạng thái mới thì `nhan_hien_thi` báo thiếu ngay (`KeyError`), thay vì rải rác các `if` khắp nơi âm thầm bỏ sót.\n\n### Chọn loại Enum nào\n\n```\nEnum        mặc định. Giá trị là gì không quan trọng, chỉ cần phân biệt.\nIntEnum     khi cần so sánh được với int (mã lỗi, độ ưu tiên)\nStrEnum     (3.11+) khi cần dùng thẳng như chuỗi (khoá JSON, giá trị DB)\nFlag        khi các giá trị KẾT HỢP được (quyền: ĐỌC | GHI)\nauto()      khi giá trị cụ thể không quan trọng\n```\n\n```python\nfrom enum import Enum, IntEnum, Flag, auto\n\nclass MucDoLog(IntEnum):          # so sánh được: DEBUG < INFO < LOI\n    DEBUG = 10\n    INFO = 20\n    LOI = 40\n\nif muc >= MucDoLog.INFO:          # ⭐ nhờ IntEnum mới viết được thế này\n    ghi(...)\n\n\nclass Quyen(Flag):                # kết hợp được\n    DOC = auto()\n    GHI = auto()\n    XOA = auto()\n\nquyen_bien_tap = Quyen.DOC | Quyen.GHI\nif Quyen.GHI in quyen_bien_tap:\n    ...\n```\n\n⚠️ Cạm bẫy của `IntEnum` và `StrEnum`: chúng **là** `int`/`str`, nên `MucDoLog.INFO == 20` là `True`. Tiện, nhưng cũng nghĩa là chúng lọt qua những chỗ đáng lẽ phải báo lỗi kiểu. Dùng `Enum` thuần khi không thật sự cần so sánh.\n\n### Cờ boolean — vì sao là vấn đề\n\n```python\n# ❌ Đọc nơi gọi không hiểu gì\nxuat_bao_cao(don, True)\ngui_mail(nd, False, True)\ntinh_gia(sp, True, False, True)\n```\n\nBa vấn đề, theo mức độ nghiêm trọng:\n\n```\n① NƠI GỌI KHÔNG ĐỌC ĐƯỢC\n   Phải mở định nghĩa hàm mới biết True nghĩa là gì\n\n② HÀM LÀM HAI VIỆC\n   Cờ chọn nhánh ⇒ thân hàm có if ⇒ hai hành vi trong một tên\n   (vi phạm \"một việc\" ở bài trước)\n\n③ SỐ TỔ HỢP TEST TĂNG THEO 2ⁿ\n   3 cờ = 8 tổ hợp; thực tế test 2–3 cái\n```\n\nĐiểm ② là gốc rễ. Xem thân hàm điển hình:\n\n```python\ndef xuat_bao_cao(don, chi_tiet):\n    if chi_tiet:\n        # 30 dòng dựng báo cáo đầy đủ\n        ...\n    else:\n        # 10 dòng dựng báo cáo tóm tắt\n        ...\n```\n\nHai nhánh này **không chia sẻ gì cả** — chúng là hai hàm bị nhét chung một tên. Đó là lý do cách sửa tốt nhất là tách đôi.\n\n### Ba cách sửa cờ boolean, theo thứ tự ưu tiên\n\n```\n① TÁCH THÀNH HAI HÀM        ← tốt nhất khi hai nhánh khác nhau nhiều\n② DÙNG ENUM                  ← khi có hoặc sẽ có nhiều hơn hai lựa chọn\n③ THAM SỐ CHỈ-TÊN            ← khi cờ thật sự là DỮ LIỆU, không phải điều khiển\n```\n\n**Cách ① — tách thành hai hàm:**\n\n```python\n# ❌\ndef xuat_bao_cao(don, chi_tiet=False): ...\nxuat_bao_cao(don, True)\n\n# ✓\ndef xuat_bao_cao_tom_tat(don: list[DonHang]) -> str: ...\ndef xuat_bao_cao_chi_tiet(don: list[DonHang]) -> str: ...\n\nxuat_bao_cao_chi_tiet(don)        # ⭐ tên nói rõ, không cần đọc định nghĩa\n```\n\nKhi nào **không** dùng cách này: khi hai nhánh chia sẻ 90% logic và chỉ khác một dòng. Lúc đó tách đôi tạo trùng lặp bản chất — đúng cái mà bài DRY vừa cảnh báo.\n\n**Cách ② — enum:**\n\n```python\n# ❌ Cờ bool khoá bạn ở đúng HAI lựa chọn\ndef sap_xep(ds, tang_dan=True): ...\n\n# ✓ Enum mở đường cho lựa chọn thứ ba\nclass ThuTu(Enum):\n    TANG_DAN = \"tang_dan\"\n    GIAM_DAN = \"giam_dan\"\n    THEO_DO_LIEN_QUAN = \"lien_quan\"      # ⭐ thêm được, bool thì không\n\ndef sap_xep(ds: list[T], thu_tu: ThuTu = ThuTu.TANG_DAN) -> list[T]: ...\n\nsap_xep(ds, ThuTu.GIAM_DAN)\n```\n\nĐây là lợi ích quan trọng nhất của enum so với bool: **bool là một quyết định đóng.** Ngày cần lựa chọn thứ ba, mọi nơi gọi đều phải sửa. Enum thì thêm một thành viên.\n\n**Cách ③ — tham số chỉ-tên:**\n\n```python\n# ✓ Khi cờ THẬT SỰ là dữ liệu, không phải chọn nhánh\ndef tao_ket_noi(host: str, *, dung_tls: bool = True) -> KetNoi: ...\n\ntao_ket_noi(\"db.congty.vn\", dung_tls=False)    # ⭐ đọc được\n```\n\nDấu `*` **bắt buộc** nơi gọi phải nêu tên — không thể viết `tao_ket_noi(\"x\", False)`. Đây là kỹ thuật từ module 5, và ở đây nó là biện pháp tối thiểu, không phải giải pháp tốt nhất.\n\n### Khi nào cờ boolean là chấp nhận được\n\nPhản biện cần thiết — nguyên tắc này cũng bị áp máy móc:\n\n```\nCHẤP NHẬN ĐƯỢC khi:\n· Là DỮ LIỆU của đối tượng:  nd.dang_hoat_dong = True\n· Là tuỳ chọn của thư viện chuẩn:  sorted(ds, reverse=True)\n· Bật/tắt một BƯỚC PHỤ, không đổi hành vi chính:\n    luu(don, ghi_log=False)\n· Có tham số chỉ-tên và chỉ MỘT cờ\n\nKHÔNG chấp nhận khi:\n· Chọn giữa hai THUẬT TOÁN/hành vi khác nhau\n· Có từ hai cờ trở lên\n· Các cờ TƯƠNG TÁC với nhau (cờ này ảnh hưởng ý nghĩa cờ kia)\n```\n\nDấu hiệu cuối là nghiêm trọng nhất: khi `la_marketing=True` làm `dinh_kem` bị bỏ qua, thì hàm đó đã thành một mê cung không ai lập luận nổi.\n\n### Cờ boolean và Open/Closed\n\nĐiểm nối quan trọng với module 6 — cờ boolean chính là dạng thô sơ của phép chọn nhánh mà đa hình làm tốt hơn:\n\n```python\n# ❌ Thêm loại mới = SỬA hàm (vi phạm Open/Closed)\ndef tinh_phi(don, la_vip=False, la_nuoc_ngoai=False, la_hang_cong_kenh=False):\n    phi = don.tong * 0.05\n    if la_vip:\n        phi *= 0.5\n    if la_nuoc_ngoai:\n        phi += 200_000\n    if la_hang_cong_kenh:\n        phi *= 1.5\n    return phi\n```\n\n```python\n# ✓ Thêm loại mới = THÊM class\nclass ChinhSachPhi(Protocol):\n    def tinh(self, don: DonHang) -> float: ...\n\nclass PhiTieuChuan:\n    def tinh(self, don): return don.tong * 0.05\n\nclass PhiVip:\n    def tinh(self, don): return don.tong * 0.025\n\nclass PhiQuocTe:\n    def tinh(self, don): return don.tong * 0.05 + 200_000\n\ndef tinh_phi(don: DonHang, chinh_sach: ChinhSachPhi) -> float:\n    return chinh_sach.tinh(don)\n```\n\n⚠️ **Đánh đổi thật, phải nói rõ:** bản sau nhiều code hơn và thêm một tầng gián tiếp. Với ba chính sách ổn định và không có dấu hiệu thêm nữa, bản đầu **đơn giản hơn và đủ tốt** — đây là KISS thắng Open/Closed, đúng thứ tự ưu tiên ở bài trước. Chỉ chuyển sang đa hình khi số nhánh đã tăng thật hoặc bạn biết chắc sẽ tăng.\n\n### Đặt hằng số ở đâu\n\n```\nTRONG HÀM        chỉ hàm đó dùng, và là chi tiết cục bộ\nCẤP MODULE       nhiều hàm trong file dùng  ← phổ biến nhất\nTRONG CLASS      thuộc về khái niệm của class đó\nFILE CẤU HÌNH    khác nhau theo môi trường (dev/staging/prod)\nBIẾN MÔI TRƯỜNG  bí mật, hoặc phụ thuộc nơi triển khai\n```\n\n```python\n# ✓ Hằng số thuộc về khái niệm → đặt trong class\nclass MatKhau:\n    DO_DAI_TOI_THIEU = 8\n    SO_KY_TU_DAC_BIET_TOI_THIEU = 1\n\n    @classmethod\n    def kiem_tra(cls, mk: str) -> None:\n        if len(mk) < cls.DO_DAI_TOI_THIEU:\n            raise MatKhauYeu(f\"cần {cls.DO_DAI_TOI_THIEU} ký tự\")\n```\n\n⚠️ Cái cần tránh: **file `constants.py` chứa tất cả hằng số của cả dự án.** Nó vi phạm SRP — không có \"một lý do để thay đổi\" nào cho một cái túi, và tạo phụ thuộc từ mọi module vào một file. Đặt hằng số **cạnh nơi dùng nó**.\n\nMột lưu ý về Python: `SO_NGAY = 30` viết hoa chỉ là **quy ước**, Python không cấm gán lại. Muốn thật sự bất biến thì dùng `Enum` hoặc `@dataclass(frozen=True)` — nối về bài bất biến ở module 7.\n\n### Phát hiện tự động\n\n```bash\n# ruff — bắt magic number trong so sánh\n# pyproject.toml:\n#   [tool.ruff.lint]\n#   select = [\"PLR2004\", \"FBT\"]\n#     PLR2004 = magic-value-comparison\n#     FBT001/002/003 = boolean trap (tham số bool vị trí)\nruff check du_an/\n```\n\n```\nPLR2004  if x > 100          → \"Magic value used in comparison\"\nFBT001   def f(co_thue: bool) → \"Boolean-typed positional argument\"\nFBT003   f(don, True)         → \"Boolean positional value in function call\"\n```\n\nBa quy tắc này bắt phần lớn trường hợp. Chúng **không** bắt được magic number trong phép tính (`gia * 1.1`) — cái đó vẫn cần mắt người, và cần người review, nối sang bài đọc-review code cuối module.\n\nKhi áp vào dự án cũ: bật `FBT` trước (ít cảnh báo, sửa dễ), rồi mới bật `PLR2004` — cùng chiến lược siết dần đã dùng với `mypy` ở module 5.",

vi_du: "### Magic number trong một hàm thật\n\n```python\n# ❌ TRƯỚC — bảy con số, không cái nào tự giải thích\ndef tinh_tien_thue_sach(so_ngay, la_thanh_vien, so_sach):\n    tien = so_sach * 5000\n    if so_ngay > 14:\n        tien += (so_ngay - 14) * 2000\n    if la_thanh_vien:\n        tien *= 0.8\n    if tien > 500000:\n        tien = 500000\n    return tien * 1.1\n```\n\nMọi con số ở đây đều là **quy tắc nghiệp vụ** — thủ thư có thể yêu cầu đổi bất kỳ cái nào:\n\n```python\n# ✓ SAU\nGIA_THUE_MOI_CUON = 5_000\nSO_NGAY_MIEN_PHI_GIA_HAN = 14\nPHI_QUA_HAN_MOI_NGAY = 2_000\nTI_LE_GIAM_THANH_VIEN = 0.20\nTRAN_TIEN_THUE = 500_000\nTHUE_SUAT_GTGT = 0.10\n\n\ndef tinh_tien_thue_sach(so_ngay: int, la_thanh_vien: bool,\n                        so_sach: int) -> float:\n    tien = so_sach * GIA_THUE_MOI_CUON\n\n    ngay_qua_han = max(0, so_ngay - SO_NGAY_MIEN_PHI_GIA_HAN)\n    tien += ngay_qua_han * PHI_QUA_HAN_MOI_NGAY\n\n    if la_thanh_vien:\n        tien *= (1 - TI_LE_GIAM_THANH_VIEN)\n\n    tien = min(tien, TRAN_TIEN_THUE)\n    return tien * (1 + THUE_SUAT_GTGT)\n```\n\nBốn cải thiện, không cái nào chỉ là thẩm mỹ:\n\n```\n· 0.8  → (1 - TI_LE_GIAM_THANH_VIEN)   nói rõ GIẢM 20%, không phải \"nhân 0.8\"\n· 1.1  → (1 + THUE_SUAT_GTGT)           nói rõ CỘNG 10% thuế\n· if/gán → max() và min()               ngắn hơn và đúng ý hơn\n· Dấu _ trong 500_000                    đọc số lớn không đếm chữ số\n```\n\nDòng thứ nhất đáng chú ý nhất: `tien *= 0.8` và `tien *= (1 - 0.20)` cho cùng kết quả, nhưng bản sau nói **ý định** (giảm 20%) chứ không chỉ nói **phép tính**. Khi giảm giá đổi thành 25%, sửa `0.20` thành `0.25` là hiển nhiên; sửa `0.8` thành `0.75` thì phải tính nhẩm.\n\n### Magic string — lỗi im lặng\n\n```python\n# ❌ Lỗi thật, không có thông báo nào\ndef xu_ly_don(don):\n    if don.trang_thai == \"cho_duyet\":\n        duyet(don)\n    elif don.trang_thai == \"da_duyet\":\n        giao_hang(don)\n\n# api/nhap_don.py — người khác viết, ba tháng sau\ndon.trang_thai = \"chờ_duyệt\"      # ⚠️ tiếng Việt có dấu\n\n# api/nhap_hang_loat.py\ndon.trang_thai = \"choduyet\"        # ⚠️ thiếu gạch dưới\n\n# ⇒ Hai loại đơn này KHÔNG BAO GIỜ được duyệt.\n#   Không exception, không log. Chỉ có khách gọi điện hỏi.\n```\n\n```python\n# ✓ Enum — mọi cách gõ sai đều lộ ra ngay\nfrom enum import Enum\n\n\nclass TrangThaiDon(Enum):\n    CHO_DUYET = \"cho_duyet\"\n    DA_DUYET = \"da_duyet\"\n    DANG_GIAO = \"dang_giao\"\n    HOAN_THANH = \"hoan_thanh\"\n    DA_HUY = \"da_huy\"\n\n    @property\n    def cho_phep_sua(self) -> bool:\n        return self is TrangThaiDon.CHO_DUYET\n\n    @property\n    def la_trang_thai_cuoi(self) -> bool:\n        return self in (TrangThaiDon.HOAN_THANH, TrangThaiDon.DA_HUY)\n\n    @property\n    def nhan_hien_thi(self) -> str:\n        return {\n            TrangThaiDon.CHO_DUYET: \"Chờ duyệt\",\n            TrangThaiDon.DA_DUYET: \"Đã duyệt\",\n            TrangThaiDon.DANG_GIAO: \"Đang giao\",\n            TrangThaiDon.HOAN_THANH: \"Hoàn thành\",\n            TrangThaiDon.DA_HUY: \"Đã huỷ\",\n        }[self]\n\n\n# Ba cách sai đều bị chặn:\ndon.trang_thai = TrangThaiDon.CHODUYET     # AttributeError ngay\nTrangThaiDon(\"chờ_duyệt\")                   # ValueError ngay\n\ndef xu_ly(tt: TrangThaiDon) -> None: ...\nxu_ly(\"cho_duyet\")                          # mypy báo lỗi trước cả khi chạy\n```\n\nLợi ích lớn nhất nằm ở `nhan_hien_thi`: **thêm một trạng thái mới thì `KeyError` báo ngay** rằng bạn quên nhãn. Với các `if` rải rác khắp nơi, thiếu sót chỉ hiện ra khi người dùng thấy ô trống trên màn hình.\n\nĐọc/ghi database vẫn dùng chuỗi bình thường:\n\n```python\ndon.trang_thai = TrangThaiDon(hang_db[\"trang_thai\"])   # chuỗi → enum\ndb.luu({\"trang_thai\": don.trang_thai.value})            # enum → chuỗi\n```\n\nRanh giới rõ ràng: **trong code dùng enum, ra ngoài dùng `.value`.** Việc chuyển đổi ở biên là chỗ duy nhất chuỗi xuất hiện.\n\n### Cờ boolean — ba cách sửa\n\n```python\n# ❌ TRƯỚC — không ai đọc nổi nơi gọi\ndef xuat_bao_cao(don, chi_tiet=False, gom_thue=True, dinh_dang_pdf=False):\n    ...\n\nxuat_bao_cao(don, True, False, True)\n#                 ⚠️ ba giá trị, phải mở định nghĩa mới hiểu\n```\n\n**Cách ① — tách hai hàm** (dùng khi hai nhánh khác nhau nhiều):\n\n```python\ndef xuat_tom_tat(don: list[DonHang]) -> str:\n    \"\"\"Một dòng mỗi đơn: mã, ngày, tổng.\"\"\"\n    ...\n\ndef xuat_chi_tiet(don: list[DonHang]) -> str:\n    \"\"\"Đầy đủ từng mục hàng, thuế, phí ship.\"\"\"\n    ...\n\nxuat_chi_tiet(don)        # ⭐ đọc là hiểu, không cần mở định nghĩa\n```\n\n**Cách ② — enum** (dùng khi có hoặc sẽ có hơn hai lựa chọn):\n\n```python\nclass DinhDangXuat(Enum):\n    VAN_BAN = \"txt\"\n    PDF = \"pdf\"\n    CSV = \"csv\"          # ⭐ thêm được — với bool thì không\n    EXCEL = \"xlsx\"\n\ndef xuat_bao_cao(don: list[DonHang], dinh_dang: DinhDangXuat) -> bytes: ...\n\nxuat_bao_cao(don, DinhDangXuat.PDF)\n```\n\n**Cách ③ — tham số chỉ-tên** (khi cờ thật sự là dữ liệu):\n\n```python\ndef xuat_bao_cao(don: list[DonHang], *, gom_thue: bool = True) -> str: ...\n\nxuat_bao_cao(don, gom_thue=False)     # ⭐ * BẮT BUỘC phải nêu tên\nxuat_bao_cao(don, False)              # TypeError — không viết được kiểu này\n```\n\n**Kết hợp cả ba cho ví dụ gốc:**\n\n```python\ndef xuat_tom_tat(don: list[DonHang], dinh_dang: DinhDangXuat,\n                 *, gom_thue: bool = True) -> bytes: ...\n\ndef xuat_chi_tiet(don: list[DonHang], dinh_dang: DinhDangXuat,\n                  *, gom_thue: bool = True) -> bytes: ...\n\n\n# So sánh nơi gọi:\nxuat_bao_cao(don, True, False, True)                        # ❌\nxuat_chi_tiet(don, DinhDangXuat.PDF, gom_thue=False)        # ✓\n```\n\n```\nSố cờ bool:        3 → 1\nSố tổ hợp test:    2³ = 8 → 2\nĐọc nơi gọi:       phải mở định nghĩa → hiểu ngay\n```\n\n### Cờ tương tác nhau — trường hợp tệ nhất\n\n```python\n# ❌ la_marketing thay đổi Ý NGHĨA của hai cờ kia\ndef gui(nd, khan_cap=False, la_marketing=False, dinh_kem=None,\n        bo_qua_dang_ky=False):\n    if la_marketing:\n        if not bo_qua_dang_ky and not nd.da_dang_ky:\n            return\n        dinh_kem = None              # ⚠️ marketing KHÔNG được đính kèm\n        khan_cap = False             # ⚠️ và không được gửi khẩn\n    ...\n\n# Người gọi viết:\ngui(nd, khan_cap=True, la_marketing=True)\n# ⚠️ khan_cap bị âm thầm bỏ qua. Không cảnh báo. Người gọi tưởng đã gửi khẩn.\n```\n\nĐây là dấu hiệu nghiêm trọng nhất: **cờ tương tác nhau nghĩa là hàm không có một mô hình nhất quán.**\n\n```python\n# ✓ Mỗi loại thông báo là một class, không có tổ hợp cờ nào\nfrom dataclasses import dataclass\nfrom pathlib import Path\n\n\n@dataclass(frozen=True)\nclass ThongBao:\n    nguoi_nhan: NguoiDung\n    tieu_de: str\n    noi_dung: str\n\n\nclass GuiThuong:\n    def gui(self, tb: ThongBao) -> None:\n        mail.gui(tb.nguoi_nhan.email, tb.tieu_de, tb.noi_dung)\n\n\nclass GuiKhanCap:\n    \"\"\"Mail ưu tiên kèm SMS. Không hỗ trợ đính kèm.\"\"\"\n    def gui(self, tb: ThongBao) -> None:\n        mail.gui_uu_tien(tb.nguoi_nhan.email, tb.tieu_de, tb.noi_dung)\n        sms.gui(tb.nguoi_nhan.sdt, tb.tieu_de)\n\n\nclass GuiMarketing:\n    \"\"\"Chỉ gửi cho người đã đăng ký. Không đính kèm, không khẩn cấp.\"\"\"\n    def gui(self, tb: ThongBao) -> None:\n        if not tb.nguoi_nhan.da_dang_ky_nhan_tin:\n            return\n        mail.gui(tb.nguoi_nhan.email, tb.tieu_de, tb.noi_dung)\n\n\nclass GuiKemTepTin:\n    def __init__(self, tep: Path):\n        self.tep = tep\n\n    def gui(self, tb: ThongBao) -> None:\n        mail.gui_kem(tb.nguoi_nhan.email, tb.tieu_de, tb.noi_dung, self.tep)\n```\n\nSự khác biệt quyết định: ràng buộc *\"marketing không đính kèm\"* giờ được biểu diễn bằng **việc `GuiMarketing` đơn giản là không có tham số tệp** — không thể gọi sai. Bản cũ biểu diễn nó bằng một dòng gán ngầm mà người gọi không thấy.\n\n### Cờ boolean là chấp nhận được\n\n```python\n# ✓ DỮ LIỆU của đối tượng — hoàn toàn bình thường\n@dataclass\nclass NguoiDung:\n    ten: str\n    dang_hoat_dong: bool = True       # trạng thái thật, không chọn nhánh\n    da_xac_thuc_email: bool = False\n\n\n# ✓ Bật/tắt BƯỚC PHỤ, không đổi hành vi chính\ndef luu_don(don: DonHang, *, ghi_log: bool = True) -> str:\n    ma = db.luu(don)\n    if ghi_log:\n        log.info(f\"đã lưu {ma}\")      # ⭐ chỉ là bước phụ\n    return ma\n\n\n# ✓ Quy ước thư viện chuẩn — đi ngược lại còn khó đọc hơn\nsorted(ds, reverse=True)\nopen(\"f.txt\", buffering=False)\n```\n\nRanh giới: `ghi_log` **không** đổi kết quả trả về hay tác dụng chính của hàm. `chi_tiet` thì đổi hẳn thứ hàm trả ra. Đó là khác biệt giữa tuỳ chọn phụ và cờ điều khiển.\n\n### Enum có hành vi — thay chuỗi if\n\n```python\n# ❌ Tri thức về \"gói\" rải rác khắp nơi\ndef gia_goi(goi: str) -> int:\n    if goi == \"co_ban\": return 99_000\n    elif goi == \"tieu_chuan\": return 199_000\n    elif goi == \"cao_cap\": return 499_000\n    raise ValueError(goi)\n\ndef so_du_an_toi_da(goi: str) -> int:\n    if goi == \"co_ban\": return 3\n    elif goi == \"tieu_chuan\": return 10\n    elif goi == \"cao_cap\": return 999\n    raise ValueError(goi)\n\ndef co_ho_tro_uu_tien(goi: str) -> bool:\n    return goi == \"cao_cap\"\n\n# ⚠️ Thêm gói \"doanh_nghiep\" → phải sửa BA hàm, quên một cái là lỗi lúc chạy\n```\n\n```python\n# ✓ Một chỗ định nghĩa, thiếu sót lộ ra ngay lúc import\nfrom enum import Enum\n\n\nclass GoiDichVu(Enum):\n    CO_BAN = (\"co_ban\", 99_000, 3, False)\n    TIEU_CHUAN = (\"tieu_chuan\", 199_000, 10, False)\n    CAO_CAP = (\"cao_cap\", 499_000, 999, True)\n\n    def __init__(self, ma: str, gia: int, so_du_an: int, ho_tro_uu_tien: bool):\n        self.ma = ma\n        self.gia = gia\n        self.so_du_an_toi_da = so_du_an\n        self.co_ho_tro_uu_tien = ho_tro_uu_tien\n\n    @classmethod\n    def tu_ma(cls, ma: str) -> \"GoiDichVu\":\n        for goi in cls:\n            if goi.ma == ma:\n                return goi\n        raise ValueError(f\"gói không hợp lệ: {ma}\")\n\n\n# Dùng\ngoi = GoiDichVu.TIEU_CHUAN\nprint(goi.gia)                      # 199000\nprint(goi.so_du_an_toi_da)          # 10\nif goi.co_ho_tro_uu_tien: ...\n\n# ⭐ Thêm gói mới = thêm MỘT dòng, và Python bắt buộc phải điền đủ 4 giá trị\n#    DOANH_NGHIEP = (\"doanh_nghiep\", 1_999_000, 9999, True)\n```\n\nĐây là DRY áp đúng: tri thức về một gói dịch vụ nằm **trên một dòng**, thay vì rải qua ba hàm. Và cơ chế `__init__` của Enum buộc mọi thành viên phải khai đủ trường — quên là lỗi ngay lúc import module, không phải lúc người dùng bấm nút.\n\n### Cấu hình — khi hằng số cần đổi theo môi trường\n\n```python\n# ❌ Giá trị khác nhau giữa dev và production, nhưng viết cứng\nURL_API = \"https://api.congty.vn\"\nSO_LUONG_MOI_TRANG = 20\nTHOI_GIAN_CHO = 30\n```\n\n```python\n# ✓ Mặc định trong code, ghi đè bằng biến môi trường\nimport os\nfrom dataclasses import dataclass\n\n\n@dataclass(frozen=True)        # ⭐ frozen: không ai lỡ tay sửa lúc chạy\nclass CauHinh:\n    url_api: str = \"https://api.congty.vn\"\n    so_luong_moi_trang: int = 20\n    thoi_gian_cho_giay: int = 30\n\n    @classmethod\n    def tu_moi_truong(cls) -> \"CauHinh\":\n        return cls(\n            url_api=os.environ.get(\"URL_API\", cls.url_api),\n            so_luong_moi_trang=int(\n                os.environ.get(\"SO_LUONG_MOI_TRANG\", cls.so_luong_moi_trang)),\n            thoi_gian_cho_giay=int(\n                os.environ.get(\"THOI_GIAN_CHO\", cls.thoi_gian_cho_giay)),\n        )\n\n\ncau_hinh = CauHinh.tu_moi_truong()\n```\n\nBa lợi ích: giá trị mặc định vẫn nằm trong code (đọc là biết), đổi theo môi trường không cần build lại, và `frozen=True` bảo đảm không ai sửa cấu hình giữa lúc chạy — nối về bài bất biến ở module 7.\n\n⚠️ Bí mật (mật khẩu DB, khoá API) **chỉ** đọc từ biến môi trường, không bao giờ có giá trị mặc định trong code — mặc định trong code nghĩa là nó nằm trong lịch sử Git vĩnh viễn.\n\n### Bắt tự động bằng ruff\n\n```toml\n# pyproject.toml\n[tool.ruff.lint]\nselect = [\"E\", \"F\", \"PLR2004\", \"FBT\"]\n\n[tool.ruff.lint.flake8-boolean-trap]\n# Các tên được miễn — hợp lý cho tuỳ chọn phụ\nextend-allowed-calls = [\"dict.get\"]\n```\n\n```bash\n$ ruff check bao_cao.py\n\nbao_cao.py:12:8: PLR2004 Magic value used in comparison, consider\n                          replacing 8 with a constant variable\nbao_cao.py:23:22: FBT001 Boolean-typed positional argument in function\n                          definition\nbao_cao.py:45:18: FBT003 Boolean positional value in function call\n```\n\nBa quy tắc này bắt phần lớn trường hợp phổ biến. Chúng **không** bắt được:\n\n```\n· Magic number trong phép tính:  gia * 1.1        ← cần mắt người\n· Magic string:                   == \"da_duyet\"    ← cần mắt người\n· Cờ đã có tên nhưng vẫn là điều khiển: f(x, chi_tiet=True)\n```\n\nBa thứ này là việc của người review — và đó là lý do bài **đọc và review code người khác** ở cuối module tồn tại.",

so_sanh: "### Magic number — khi nào cần đặt tên\n\n| Giá trị | Ngữ cảnh | Đặt tên? |\n|---|---|---|\n| `0`, `1`, `2` | chỉ số, rỗng, chia đôi | **✗ để nguyên** |\n| `math.pi`, `0.5` trong công thức vật lý | công thức toán | ✗ để nguyên |\n| `8` | độ dài mật khẩu | **✓** |\n| `30` | số ngày hạn trả | **✓** |\n| `0.10` | thuế suất | **✓** |\n| `\"da_duyet\"` | trạng thái | **✓ dùng Enum** |\n\nPhép kiểm: **người làm nghiệp vụ có thể yêu cầu đổi con số này không?** Có → cần tên.\n\n### Hằng số rời và Enum\n\n| | Hằng số rời | Enum |\n|---|---|---|\n| Tạo được giá trị sai | **✓ được** (nguy hiểm) | ✗ chặn ngay |\n| `mypy` kiểm | ✗ (chỉ thấy `str`) | **✓** |\n| Liệt kê hết giá trị hợp lệ | ✗ | **✓ lặp qua được** |\n| Gắn hành vi kèm theo | ✗ | **✓ property, method** |\n| Thêm giá trị mới | sửa nhiều chỗ | **thêm một dòng** |\n| Chi phí | 1 dòng | vài dòng |\n\nHàng \"gắn hành vi\" là lợi ích lớn nhất mà ít người dùng tới: tri thức về một giá trị nằm cùng chỗ với định nghĩa của nó.\n\n### Các loại Enum\n\n| Loại | Dùng khi | Cạm bẫy |\n|---|---|---|\n| `Enum` | mặc định, chỉ cần phân biệt | — |\n| `IntEnum` | cần so sánh `<`, `>` | **là `int`** — lọt qua kiểm kiểu |\n| `StrEnum` (3.11+) | dùng thẳng như chuỗi | **là `str`** — như trên |\n| `Flag` | giá trị kết hợp được (quyền) | dễ nhầm `in` với `==` |\n| `auto()` | giá trị cụ thể không quan trọng | không lưu DB được ổn định |\n\nHàng `auto()` có cạm bẫy thật: giá trị sinh tự động đổi khi bạn chèn thành viên mới — không dùng cho thứ đã lưu xuống database.\n\n### Ba cách sửa cờ boolean\n\n| | Tách hai hàm | Enum | Tham số chỉ-tên |\n|---|---|---|---|\n| Ưu tiên | **①** | ② | ③ |\n| Nơi gọi đọc được | **rõ nhất** | rõ | rõ |\n| Mở rộng lựa chọn thứ ba | thêm hàm | **thêm 1 dòng** | ✗ bế tắc |\n| Số tổ hợp test | **giảm hẳn** | tuyến tính | vẫn 2ⁿ |\n| Rủi ro | trùng lặp nếu hai nhánh giống nhau | — | không giải quyết gốc |\n| Chi phí | vừa | vừa | **rất thấp** |\n\nCách ③ rẻ nhất nhưng chỉ chữa triệu chứng — dùng khi cờ thật sự là dữ liệu, không phải điều khiển.\n\n### Cờ boolean — chấp nhận hay không\n\n| Trường hợp | Chấp nhận? | Vì sao |\n|---|---|---|\n| `nd.dang_hoat_dong = True` | **✓** | dữ liệu, không chọn nhánh |\n| `sorted(ds, reverse=True)` | ✓ | quy ước thư viện chuẩn |\n| `luu(don, ghi_log=False)` | ✓ | bước phụ, không đổi hành vi chính |\n| `xuat(don, chi_tiet=True)` | **✗** | chọn giữa hai hành vi |\n| `f(x, True, False, True)` | **✗** | nhiều cờ, vị trí |\n| cờ này ảnh hưởng cờ kia | **✗✗** | không có mô hình nhất quán |\n\nHàng cuối là nghiêm trọng nhất: người gọi truyền một cờ rồi bị âm thầm bỏ qua.\n\n### Đặt hằng số ở đâu\n\n| Nơi | Khi nào | Lưu ý |\n|---|---|---|\n| Trong hàm | chỉ hàm đó dùng | — |\n| Cấp module | nhiều hàm trong file | **phổ biến nhất** |\n| Trong class | thuộc khái niệm của class | truy cập qua `cls.` |\n| File cấu hình | khác theo môi trường | `frozen=True` |\n| Biến môi trường | **bí mật** | không có mặc định trong code |\n| `constants.py` toàn dự án | — | **✗ tránh** — vi phạm SRP |\n\n### Cờ boolean và đa hình\n\n| | Cờ boolean | Đa hình (Strategy) |\n|---|---|---|\n| Thêm loại mới | **sửa** hàm | **thêm** class |\n| Open/Closed | vi phạm | tuân thủ |\n| Số dòng | ít hơn | nhiều hơn |\n| Tầng gián tiếp | không | có |\n| Đúng khi | **2–3 nhánh ổn định** | nhiều nhánh, còn tăng |\n\nĐây là chỗ **KISS thắng Open/Closed** với số nhánh nhỏ — đúng thứ tự ưu tiên ở bài DRY/KISS/YAGNI. Đừng dựng Strategy cho hai nhánh.",

loi_thuong_gap: [
  "**Đặt tên cho mọi con số, kể cả `0` và `1`.** `MOT = 1` là nhiễu; chỉ số nghiệp vụ mới cần tên.",
  "**Giữ `0.8` thay vì `1 - TI_LE_GIAM`.** Con số kết quả che giấu ý định; sửa 20% thành 25% phải tính nhẩm.",
  "**Dùng chuỗi cho trạng thái.** Gõ sai không ai báo, đơn im lặng không được xử lý.",
  "**Dùng `IntEnum`/`StrEnum` khi không cần.** Chúng *là* `int`/`str` nên lọt qua kiểm kiểu — dùng `Enum` thuần khi có thể.",
  "**Dùng `auto()` cho giá trị lưu xuống database.** Chèn thành viên mới làm đổi số của các thành viên sau.",
  "**Truyền cờ boolean ở vị trí.** `f(don, True, False)` — nơi gọi không đọc được.",
  "**Chỉ thêm `*` rồi coi như xong.** Tham số chỉ-tên chữa triệu chứng; gốc rễ là hàm làm hai việc.",
  "**Để nhiều cờ tương tác nhau.** Cờ này âm thầm vô hiệu hoá cờ kia là lỗi nghiêm trọng nhất.",
  "**Tách hai hàm khi hai nhánh chia sẻ 90% logic.** Tạo ra trùng lặp bản chất — đúng cái bài DRY cảnh báo.",
  "**Dựng Strategy pattern cho hai nhánh ổn định.** KISS thắng Open/Closed khi số nhánh nhỏ.",
  "**Tạo `constants.py` chứa hằng số toàn dự án.** Vi phạm SRP, tạo phụ thuộc từ mọi module vào một file.",
  "**Tưởng chữ hoa là bất biến.** Python không cấm gán lại `SO_NGAY = 30`; muốn thật bất biến dùng `Enum` hoặc `frozen=True`.",
  "**Để giá trị mặc định của bí mật trong code.** Nó nằm trong lịch sử Git vĩnh viễn, xoá file không xoá được.",
  "**Quên chuyển đổi enum ở biên.** Trong code dùng enum, ghi database dùng `.value` — lẫn lộn hai bên gây lỗi khó tìm.",
  "**Tin `ruff` bắt hết.** Nó không bắt magic number trong phép tính, không bắt magic string — cần người review."
],

tu_kiem_tra: [
  "Định nghĩa magic number. Vấn đề cốt lõi là gì — nó là số, hay là điều khác?",
  "Nêu ba câu hỏi kiểm tra xem một con số có cần đặt tên không.",
  "Cho ví dụ số KHÔNG nên đặt tên. Phép kiểm phân biệt số kỹ thuật và số nghiệp vụ?",
  "Vì sao `tien *= (1 - TI_LE_GIAM)` tốt hơn `tien *= 0.8` dù cùng kết quả?",
  "Vì sao magic string nguy hiểm hơn magic number?",
  "Nêu ba lợi ích của Enum so với hằng số chuỗi rời. Cái nào ít người dùng tới nhất?",
  "Khi nào dùng `IntEnum`, `StrEnum`, `Flag`? Cạm bẫy chung của hai loại đầu?",
  "Phân biệt tham số boolean mang **dữ liệu** và mang **điều khiển**. Cho ví dụ mỗi loại.",
  "Nêu ba vấn đề của cờ boolean, xếp theo mức nghiêm trọng. Cái nào là gốc rễ?",
  "Nêu ba cách sửa cờ boolean theo thứ tự ưu tiên và điều kiện dùng mỗi cách.",
  "Khi nào KHÔNG nên tách một hàm có cờ thành hai hàm?",
  "Vì sao enum mở đường tốt hơn bool cho lựa chọn thứ ba?",
  "Nêu ba trường hợp cờ boolean chấp nhận được và ranh giới phân biệt với trường hợp xấu.",
  "Vì sao \"cờ tương tác nhau\" là dấu hiệu nghiêm trọng nhất?",
  "Khi nào dùng đa hình thay cờ, khi nào giữ `if`? Nguyên tắc nào quyết định?",
  "Nêu năm nơi có thể đặt hằng số. Vì sao nên tránh `constants.py` toàn dự án?",
  "`SO_NGAY = 30` có thật sự bất biến trong Python không? Muốn bất biến thì làm sao?",
  "Ba quy tắc `ruff` nào bắt được vấn đề trong bài? Chúng KHÔNG bắt được gì?"
],

tai_nguyen: [
  "Sách **Clean Code** — Robert C. Martin. Chương 3 (tham số boolean) và chương 17 (magic number trong danh mục code smell).",
  "Sách **Refactoring** — Martin Fowler. Phép \"Replace Magic Literal with Symbolic Constant\" và \"Remove Flag Argument\", kèm quy trình từng bước.",
  "**Tài liệu chính thức module `enum` của Python** — phần so sánh `Enum`, `IntEnum`, `StrEnum`, `Flag` và các cạm bẫy của từng loại.",
  "**Tài liệu quy tắc của ruff** — mục `flake8-boolean-trap` (FBT) và `PLR2004`, giải thích vì sao mỗi quy tắc tồn tại.",
  "Sách **The Pragmatic Programmer** — Andy Hunt & Dave Thomas. Phần về cấu hình và tách giá trị thay đổi ra khỏi code."
],

lien_he: [
  "**Đặt tên có nghĩa** trong module này: hằng số có tên là ứng dụng trực tiếp nhất; tên hằng cũng phải nói ý định.",
  "**Hàm nhỏ làm đúng một việc** trong module này: cờ boolean là dấu hiệu đo được của hàm làm hai việc.",
  "**DRY, KISS, YAGNI** trong module này: hằng số có tên là DRY ít rủi ro nhất; nhưng đừng dựng Strategy cho hai nhánh — KISS thắng.",
  "**Hàm, tham số mặc định** ở module 5: dấu `*` buộc nơi gọi nêu tên tham số.",
  "**SOLID — Open/Closed** ở module 6: cờ boolean là dạng thô sơ của phép chọn nhánh mà đa hình làm tốt hơn.",
  "**Dataclass** ở module 6: `@dataclass(frozen=True)` cho object cấu hình bất biến.",
  "**Mutable vs immutable** ở module 7: chữ hoa chỉ là quy ước — bất biến thật cần `Enum` hoặc `frozen=True`.",
  "**Nhận diện code smell** cuối module: *magic number* và *boolean trap* là hai smell có công cụ bắt tự động.",
  "**Đọc và review code người khác** cuối module: magic string và cờ đã đặt tên vẫn cần mắt người.",
  "**Thiết kế CSDL** ở mảng 04: enum lưu xuống database dùng `.value`; tránh `auto()` cho dữ liệu bền vững.",
  "**Auth & bảo mật** ở mảng 05: bí mật chỉ đọc từ biến môi trường — mặc định trong code nằm trong lịch sử Git vĩnh viễn."
]

};
