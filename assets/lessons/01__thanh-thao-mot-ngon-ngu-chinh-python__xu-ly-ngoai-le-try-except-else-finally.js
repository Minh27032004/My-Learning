window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:xu-ly-ngoai-le-try-except-else-finally"] = {

tom_tat: [
  "**Python theo triết lý \"xin lỗi dễ hơn xin phép\"**: cứ thử làm rồi bắt lỗi, thay vì kiểm tra đủ điều kiện trước. Đây là khác biệt văn hoá thật so với nhiều ngôn ngữ khác.",
  "**`except:` trần và `except Exception:` là hai mức độ tệ khác nhau** — cái đầu nuốt cả `Ctrl+C`, cái sau che mọi lỗi lập trình. Luôn bắt loại ngoại lệ cụ thể.",
  "**Bốn khối có bốn vai trò riêng**: `try` phần có thể lỗi, `except` xử lý, `else` phần chạy **khi không lỗi**, `finally` **luôn** chạy để dọn dẹp.",
  "**Chỉ bắt ngoại lệ khi bạn làm được điều gì đó với nó.** Bắt rồi `pass` hoặc bắt rồi in ra và chạy tiếp là cách biến một lỗi rõ ràng thành một lỗi im lặng ở chỗ khác.",
  "**`raise ... from e` giữ lại nguyên nhân gốc.** Thiếu nó, truy vết mất đoạn quan trọng nhất và việc gỡ lỗi khó hơn nhiều."
],

can_biet_truoc: [
  "Bài **Module, package, import** ngay trước — `ImportError` và `ModuleNotFoundError` là ngoại lệ đầu tiên bạn gặp nhiều.",
  "Bài **Hàm, tham số mặc định** — kiểm tra đầu vào rồi `raise ValueError` là mẫu chuẩn ở đó.",
  "Bài **Tín hiệu, tiến trình nền, exit code** ở module 2 — `KeyboardInterrupt` là `Ctrl+C`, tức tín hiệu SIGINT.",
  "Bài **Cú pháp, kiểu dữ liệu** — `TypeError` sinh ra từ kiểu chặt, đúng như đã nói ở đó."
],

dinh_nghia: "Nói đời thường: có hai cách đi qua một cánh cửa lạ. Cách thứ nhất là **kiểm tra kỹ trước**: cửa có khoá không, có tay nắm không, có bậc thềm không. Cách thứ hai là **cứ đẩy thử, vấp thì đỡ**. Python thiên về cách thứ hai — và điều đó có lý do kỹ thuật, không phải sự cẩu thả.\n\nNói chính xác:\n\n- **Ngoại lệ** (*exception*) — một object báo hiệu \"có chuyện bất thường\", làm luồng thực thi nhảy ra khỏi chỗ hiện tại và đi tìm nơi xử lý.\n- **`raise`** — ném một ngoại lệ.\n- **`try` / `except`** — vùng theo dõi và nơi xử lý.\n- **`else`** — chạy **chỉ khi `try` không có ngoại lệ nào**.\n- **`finally`** — **luôn** chạy, dù có lỗi hay không, dù có `return` hay không.\n- **EAFP** (*Easier to Ask Forgiveness than Permission*) — cứ làm rồi bắt lỗi. Phong cách Python.\n- **LBYL** (*Look Before You Leap*) — kiểm tra trước khi làm. Phong cách của nhiều ngôn ngữ khác.\n- **Truy vết** (*traceback*) — chuỗi lời gọi hàm dẫn tới lỗi. Đọc **từ dưới lên** thì thấy chỗ lỗi trước.\n\nĐiểm cần nắm ngay: **ngoại lệ trong Python không chỉ dành cho lỗi.** `StopIteration` báo hiệu iterator đã cạn, và mọi vòng `for` đều dựa vào nó. Ngoại lệ là **một cơ chế điều khiển luồng**, và Python dùng nó rộng rãi hơn nhiều ngôn ngữ khác.",

vi_sao_can: "Xử lý ngoại lệ là ranh giới giữa một script chạy được trên máy bạn và một chương trình dùng được thật:\n\n- **Chương trình thật luôn gặp thứ ngoài dự tính.** File không tồn tại, mạng chập chờn, dữ liệu sai định dạng, người dùng gõ chữ vào ô số. Không xử lý là chương trình chết giữa chừng, có khi để lại dữ liệu dở dang.\n- **Bắt sai cách còn tệ hơn không bắt.** `except: pass` biến một lỗi rõ ràng thành một lỗi âm thầm ở chỗ khác, muộn hơn, khó lần hơn nhiều. Đây là một trong những phản mẫu tai hại nhất trong Python.\n- **Đọc truy vết là kỹ năng gỡ lỗi cơ bản nhất.** Người mới thường hoảng khi thấy mười dòng đỏ; thật ra chỉ cần đọc dòng cuối và dòng có tên file của mình.\n- **Dọn dẹp tài nguyên đúng cách.** File đang mở, kết nối database, khoá — thiếu `finally` (hoặc `with`) là rò rỉ dần cho tới khi hết tài nguyên.\n- **Nền cho `with` và context manager.** Bài đó trong module này chính là `try/finally` được đóng gói lại cho gọn.\n\nTình huống thật rất hay gặp: bạn viết vòng lặp xử lý 10.000 bản ghi, bọc `try/except Exception: continue` cho \"an toàn\". Chương trình chạy trơn tru, báo \"xong 10.000 bản ghi\". Thực tế 3.000 bản ghi lỗi đã bị bỏ qua im lặng, và không ai biết cho tới khi báo cáo cuối tháng lệch số. Bắt ngoại lệ **không phải để chương trình đừng dừng**, mà để **xử lý đúng tình huống đã lường trước**.",

co_che: "### EAFP và LBYL\n\n```python\n# LBYL — kiểm tra trước\nif os.path.exists(f) and os.access(f, os.R_OK):\n    with open(f) as fh:\n        du_lieu = fh.read()\n\n# EAFP — cứ làm rồi bắt lỗi  ⭐ phong cách Python\ntry:\n    with open(f) as fh:\n        du_lieu = fh.read()\nexcept FileNotFoundError:\n    du_lieu = \"\"\n```\n\nVì sao Python thiên về EAFP — ba lý do kỹ thuật, không phải sở thích:\n\n```\n① LBYL có KHE HỞ ĐUA TRANH (race condition):\n   giữa lúc kiểm tra và lúc mở, file có thể bị xoá bởi tiến trình khác\n   ⇒ đoạn LBYL ở trên VẪN có thể lỗi\n\n② LBYL phải liệt kê ĐỦ mọi điều kiện — tồn tại, quyền đọc, là file\n   không phải thư mục, không phải liên kết hỏng...\n   ⇒ luôn thiếu một trường hợp nào đó\n\n③ Trong Python, khối try KHÔNG tốn gì khi không có lỗi\n   ⇒ EAFP thường NHANH HƠN khi lỗi hiếm\n```\n\nĐiểm ③ đáng nhấn vì nó ngược trực giác của người từ ngôn ngữ khác sang: đặt một khối `try` gần như miễn phí; chi phí chỉ phát sinh **khi ngoại lệ thật sự được ném**.\n\n```python\n# Với dict — EAFP nhanh hơn khi khoá thường tồn tại\ntry:\n    v = d[k]\nexcept KeyError:\n    v = mac_dinh\n\n# LBYL phải băm khoá HAI lần\nif k in d:          # lần 1\n    v = d[k]        # lần 2\n\n# Nhưng với dict thì có cách gọn nhất, không cần cả hai:\nv = d.get(k, mac_dinh)\n```\n\nDòng cuối là bài học phụ đáng nhớ: **trước khi chọn EAFP hay LBYL, xem có sẵn một cách diễn đạt trực tiếp không.** `dict.get`, `getattr` với mặc định, `next(it, mac_dinh)` đều thuộc loại này.\n\nKhi nào LBYL vẫn đúng: khi việc kiểm tra rẻ và rõ ràng hơn, và khi \"sai\" là trường hợp **thường xuyên** chứ không hiếm. Ví dụ kiểm tra `if not ds: return` ở đầu hàm rõ hơn hẳn việc bắt `IndexError`.\n\n### Bốn khối và vai trò của từng khối\n\n```python\ntry:\n    f = open(\"du_lieu.txt\")          # ① chỉ đặt phần CÓ THỂ lỗi\nexcept FileNotFoundError as e:\n    print(f\"không thấy file: {e}\")   # ② xử lý loại lỗi cụ thể\nelse:\n    noi_dung = f.read()               # ③ chạy KHI KHÔNG lỗi\n    print(len(noi_dung))\nfinally:\n    print(\"luôn chạy\")                # ④ dọn dẹp, LUÔN chạy\n```\n\nThứ tự thực thi:\n\n```\nKhông lỗi:  try → else → finally\nCó lỗi:     try (dừng giữa chừng) → except → finally\nLỗi không bắt được: try → finally → rồi ngoại lệ bay tiếp lên trên\n```\n\n**Vì sao cần `else`?** Nó thu hẹp phạm vi `try` xuống đúng phần có thể lỗi:\n\n```python\n# ❌ try quá rộng — nếu xu_ly() cũng ném KeyError thì bị bắt nhầm\ntry:\n    v = d[\"khoa\"]\n    xu_ly(v)\nexcept KeyError:\n    print(\"thiếu khoá\")      # có thể là lỗi TRONG xu_ly, không phải thiếu khoá\n\n# ✓ else tách bạch rõ ràng\ntry:\n    v = d[\"khoa\"]\nexcept KeyError:\n    print(\"thiếu khoá\")\nelse:\n    xu_ly(v)                 # lỗi ở đây KHÔNG bị except phía trên bắt\n```\n\nĐây là công dụng thật của `else` và cũng là lý do nó tồn tại: **`try` chỉ nên chứa dòng có thể ném ngoại lệ mà bạn định bắt.**\n\n**`finally` luôn chạy — kể cả khi có `return`:**\n\n```python\ndef f():\n    try:\n        return \"từ try\"\n    finally:\n        print(\"finally vẫn chạy\")     # in ra TRƯỚC khi hàm trả về\n\nf()\n# finally vẫn chạy\n# → 'từ try'\n```\n\nCạm bẫy: `return` trong `finally` **nuốt cả ngoại lệ lẫn giá trị trả về của `try`**.\n\n```python\ndef f():\n    try:\n        raise ValueError(\"lỗi thật\")\n    finally:\n        return \"ổn\"        # ❌ ngoại lệ BIẾN MẤT hoàn toàn\n\nf()      # 'ổn' — không ai biết đã có lỗi\n```\n\nQuy tắc: **không bao giờ `return`, `break`, hay `continue` trong `finally`.**\n\n### Cây phân cấp ngoại lệ\n\n```\nBaseException\n├── SystemExit          sys.exit()\n├── KeyboardInterrupt   Ctrl+C\n├── GeneratorExit\n└── Exception           ← MỌI lỗi thông thường kế thừa từ đây\n    ├── ArithmeticError → ZeroDivisionError\n    ├── LookupError     → IndexError, KeyError\n    ├── OSError         → FileNotFoundError, PermissionError, TimeoutError\n    ├── ValueError      giá trị sai\n    ├── TypeError       kiểu sai\n    ├── AttributeError  không có thuộc tính\n    └── StopIteration   iterator cạn — dùng cho ĐIỀU KHIỂN LUỒNG\n```\n\nCây này giải thích trực tiếp vì sao `except:` trần là tệ nhất:\n\n```python\n# ❌ TỆ NHẤT — bắt cả BaseException\ntry:\n    xu_ly_lau()\nexcept:                  # nuốt cả Ctrl+C và sys.exit()\n    pass\n#  ⇒ chương trình KHÔNG DỪNG ĐƯỢC bằng Ctrl+C\n\n# ❌ Vẫn tệ — che mọi lỗi lập trình\ntry:\n    xu_ly()\nexcept Exception:\n    pass                 # lỗi gõ sai tên biến cũng bị nuốt\n\n# ✓ Bắt đúng cái mình lường trước\ntry:\n    xu_ly()\nexcept (FileNotFoundError, PermissionError) as e:\n    ghi_log(f\"không đọc được: {e}\")\n```\n\nMức độ nghiêm trọng theo thứ tự: `except:` trần → không tắt được chương trình; `except Exception: pass` → lỗi lập trình bị che; `except Exception:` có ghi log → chấp nhận được ở tầng ngoài cùng.\n\n**Thứ tự `except` phải từ hẹp tới rộng**, vì Python lấy khối khớp đầu tiên:\n\n```python\n# ❌ FileNotFoundError không bao giờ tới lượt\ntry: ...\nexcept OSError: ...\nexcept FileNotFoundError: ...     # chết ở trên rồi\n\n# ✓\ntry: ...\nexcept FileNotFoundError: ...     # con trước\nexcept OSError: ...               # cha sau\n```\n\n### Nơi duy nhất `except Exception` chính đáng\n\n```python\ndef main():\n    try:\n        chay_ung_dung()\n    except Exception:\n        logging.exception(\"lỗi không lường trước\")   # ⭐ ghi CẢ truy vết\n        return 1\n    return 0\n```\n\nHai điều kiện để nó chấp nhận được: **ở tầng ngoài cùng**, và **có ghi lại truy vết đầy đủ**. `logging.exception()` làm đúng việc đó — nó tự đính kèm truy vết, khác với `logging.error()`.\n\nMẫu này đúng cho vòng lặp xử lý hàng loạt, nhưng phải **đếm và báo cáo**:\n\n```python\nthanh_cong = that_bai = 0\nfor bg in ban_ghi:\n    try:\n        xu_ly(bg)\n        thanh_cong += 1\n    except Exception:\n        that_bai += 1\n        logging.exception(f\"lỗi ở bản ghi {bg.id}\")   # KHÔNG im lặng\n\nprint(f\"xong: {thanh_cong} thành công, {that_bai} thất bại\")\nif that_bai:\n    raise RuntimeError(f\"{that_bai} bản ghi lỗi\")     # ⭐ báo ra ngoài\n```\n\nĐây là bản sửa của tình huống ở phần *vì sao cần*: vẫn xử lý hết 10.000 bản ghi, nhưng **không ai có thể tưởng nhầm là tất cả đều thành công**.\n\n### `raise` và chuỗi nguyên nhân\n\n```python\nraise ValueError(\"tuổi phải dương\")\nraise ValueError(f\"tuổi không hợp lệ: {tuoi!r}\")     # ⭐ nêu giá trị thật\n\n# Ném lại nguyên vẹn sau khi ghi log\ntry:\n    xu_ly()\nexcept ValueError:\n    ghi_log(\"có lỗi\")\n    raise                    # raise TRẦN — giữ nguyên truy vết gốc\n```\n\nBa cách chuyển đổi ngoại lệ, khác nhau ở thông tin giữ lại:\n\n```python\n# ✓ from e — GIỮ nguyên nhân gốc\ntry:\n    d[\"host\"]\nexcept KeyError as e:\n    raise CauHinhLoi(\"thiếu cấu hình host\") from e\n# Truy vết: \"The above exception was the direct cause of...\"\n\n# ~ không from — Python vẫn nối tự động nhưng nói mập mờ hơn\n    raise CauHinhLoi(\"thiếu host\")\n# Truy vết: \"During handling of the above exception, another occurred\"\n\n# ✗ from None — XOÁ hẳn nguyên nhân gốc\n    raise CauHinhLoi(\"thiếu host\") from None\n```\n\nDùng `from None` chỉ khi nguyên nhân gốc thật sự là nhiễu và không giúp gì cho người đọc. Trong đa số trường hợp, giấu nguyên nhân là **lấy đi đúng thông tin người gỡ lỗi cần nhất**.\n\n### Ngoại lệ tự định nghĩa\n\n```python\nclass UngDungLoi(Exception):\n    \"\"\"Lớp gốc cho mọi lỗi của ứng dụng này.\"\"\"\n\nclass CauHinhLoi(UngDungLoi):\n    \"\"\"Cấu hình sai hoặc thiếu.\"\"\"\n\nclass XacThucLoi(UngDungLoi):\n    def __init__(self, nguoi_dung, ly_do):\n        self.nguoi_dung = nguoi_dung          # ⭐ mang theo dữ liệu\n        self.ly_do = ly_do\n        super().__init__(f\"xác thực thất bại cho {nguoi_dung}: {ly_do}\")\n```\n\nVì sao nên có một lớp gốc riêng cho ứng dụng: người dùng thư viện của bạn bắt được **mọi lỗi của bạn** mà không phải bắt luôn lỗi của thư viện khác:\n\n```python\ntry:\n    chay()\nexcept UngDungLoi as e:        # chỉ lỗi của ứng dụng này\n    xu_ly_nhe_nhang(e)\n```\n\nCho ngoại lệ mang theo dữ liệu (như `nguoi_dung`, `ly_do`) là kỹ thuật đáng dùng: nơi bắt được thông tin có cấu trúc thay vì phải phân tích chuỗi thông báo.\n\nKhi nào **không** cần lớp mới: khi ngoại lệ dựng sẵn đã diễn đạt đúng. `ValueError` cho giá trị sai, `TypeError` cho kiểu sai, `KeyError` cho khoá thiếu — dùng chúng thì người khác hiểu ngay mà không phải đọc tài liệu của bạn.\n\n### Đọc truy vết\n\n```\nTraceback (most recent call last):\n  File \"main.py\", line 12, in <module>\n    ket_qua = xu_ly(du_lieu)          ← ② gọi từ đây\n  File \"xu_ly.py\", line 45, in xu_ly\n    return tinh(d[\"gia\"])              ← ③ rồi tới đây\n  File \"xu_ly.py\", line 8, in tinh\n    return 100 / gia                   ← ④ LỖI Ở ĐÂY\nZeroDivisionError: division by zero    ← ① ĐỌC DÒNG NÀY TRƯỚC\n```\n\nQuy trình đọc, ba bước:\n\n```\n① Đọc DÒNG CUỐI — loại lỗi và thông báo\n② Đọc KHUNG CUỐI CÙNG — dòng code gây lỗi\n③ Đi ngược lên tìm khung ĐẦU TIÊN thuộc code CỦA BẠN\n   (thường lỗi thật nằm ở đó, dù nó nổ trong thư viện)\n```\n\nBước ③ quan trọng khi lỗi xảy ra sâu trong thư viện: dòng cuối cùng thuộc code của bạn thường là nơi bạn truyền vào dữ liệu sai.\n\n```python\nimport traceback\n\ntry:\n    xu_ly()\nexcept Exception:\n    traceback.print_exc()               # in truy vết đầy đủ\n    s = traceback.format_exc()          # lấy dạng chuỗi để ghi log\n```\n\n### Ngoại lệ như cơ chế điều khiển luồng\n\n```python\n# Vòng for THỰC CHẤT là:\nit = iter(ds)\nwhile True:\n    try:\n        x = next(it)\n    except StopIteration:      # ⭐ ngoại lệ báo \"hết\", không phải lỗi\n        break\n    xu_ly(x)\n```\n\nĐây là điểm khác biệt văn hoá rõ nhất với Java hay C++, nơi ngoại lệ được coi là đắt và chỉ dành cho lỗi thật. Trong Python, ngoại lệ **rẻ** và được dùng như một cơ chế báo hiệu bình thường.\n\nHệ quả thực tế cần biết khi viết generator: nếu bạn `return` bên trong một generator, Python biến nó thành `StopIteration` — và bắt nhầm ngoại lệ đó trong thân generator sẽ gây hành vi rất khó hiểu. Bài generator sẽ nói kỹ.\n\n### `finally` và `with`\n\n```python\n# try/finally — dọn dẹp thủ công\nf = open(\"a.txt\")\ntry:\n    du_lieu = f.read()\nfinally:\n    f.close()          # đảm bảo đóng dù có lỗi\n\n# with — cùng ý nghĩa, gọn hơn ⭐\nwith open(\"a.txt\") as f:\n    du_lieu = f.read()\n# tự đóng, kể cả khi có ngoại lệ\n```\n\n`with` **là** `try/finally` được đóng gói. Hiểu điều này rồi thì bài **Context manager và câu lệnh `with`** trong module này chỉ còn là chi tiết cài đặt.\n\nQuy tắc: **có `with` thì dùng `with`; chỉ viết `try/finally` khi không có sẵn context manager.**\n\n### `ExceptionGroup` — nhiều lỗi cùng lúc\n\nTừ Python 3.11, khi nhiều tác vụ chạy song song và nhiều tác vụ cùng lỗi, tất cả được gom lại:\n\n```python\ntry:\n    raise ExceptionGroup(\"nhiều lỗi\", [\n        ValueError(\"lỗi 1\"),\n        TypeError(\"lỗi 2\"),\n    ])\nexcept* ValueError as eg:        # ⭐ except* bắt theo NHÓM\n    print(\"có lỗi giá trị:\", eg.exceptions)\nexcept* TypeError as eg:\n    print(\"có lỗi kiểu:\", eg.exceptions)\n```\n\nHai khối `except*` **đều chạy**, khác hẳn `except` thường chỉ chạy khối khớp đầu tiên. Cú pháp này chủ yếu gặp trong code bất đồng bộ; biết để không bỡ ngỡ khi đọc thư viện hiện đại.",

vi_du: "### Bốn khối trong một ví dụ chạy được\n\n```python\ndef doc_cau_hinh(duong_dan):\n    f = None\n    try:\n        f = open(duong_dan, encoding=\"utf-8\")\n        noi_dung = f.read()\n    except FileNotFoundError:\n        print(f\"không thấy {duong_dan}, dùng mặc định\")\n        return {}\n    except PermissionError as e:\n        print(f\"không có quyền đọc: {e}\")\n        raise                                  # lỗi này KHÔNG nên nuốt\n    else:\n        print(f\"đọc thành công {len(noi_dung)} ký tự\")\n        return phan_tich(noi_dung)\n    finally:\n        if f is not None:\n            f.close()\n            print(\"đã đóng file\")\n```\n\nBa quyết định thiết kế trong hàm này đáng chú ý:\n\n```\nFileNotFoundError → xử lý được (dùng mặc định) ⇒ BẮT và tiếp tục\nPermissionError   → không xử lý được ⇒ ghi log rồi NÉM LẠI\nfinally           → đóng file dù đi theo nhánh nào\n```\n\nBản gọn hơn dùng `with` — không cần `finally` nữa:\n\n```python\ndef doc_cau_hinh(duong_dan):\n    try:\n        with open(duong_dan, encoding=\"utf-8\") as f:\n            noi_dung = f.read()\n    except FileNotFoundError:\n        return {}\n    return phan_tich(noi_dung)      # ngoài try — lỗi ở đây không bị bắt nhầm\n```\n\n### Bắt hẹp so với bắt rộng\n\n```python\n# ❌ Che cả lỗi gõ sai tên biến\ndef lay_tuoi(d):\n    try:\n        return int(d[\"tuoi\"])\n    except Exception:\n        return 0            # d[\"tuoiii\"] gõ sai cũng trả 0 — không ai biết\n\n# ✓ Bắt đúng hai tình huống đã lường\ndef lay_tuoi(d):\n    try:\n        return int(d[\"tuoi\"])\n    except KeyError:\n        return 0                                     # thiếu trường\n    except (ValueError, TypeError) as e:\n        raise ValueError(f\"tuổi không hợp lệ: {d.get('tuoi')!r}\") from e\n```\n\nBản thứ hai phân biệt được hai chuyện khác hẳn nhau: **thiếu dữ liệu** (chấp nhận được, dùng mặc định) và **dữ liệu sai định dạng** (phải báo, vì nó là lỗi dữ liệu).\n\n### Vòng lặp xử lý hàng loạt — không im lặng\n\n```python\nimport logging\nlogging.basicConfig(level=logging.INFO)\n\ndef xu_ly_tat_ca(ban_ghi):\n    ok, loi = [], []\n    for bg in ban_ghi:\n        try:\n            ok.append(xu_ly_mot(bg))\n        except (ValueError, KeyError) as e:\n            loi.append((bg.get(\"id\"), str(e)))\n            logging.warning(\"bỏ qua bản ghi %s: %s\", bg.get(\"id\"), e)\n        except Exception:\n            logging.exception(\"lỗi KHÔNG lường trước ở %s\", bg.get(\"id\"))\n            raise                                   # ⭐ lỗi lạ thì DỪNG\n\n    logging.info(\"xong: %d thành công, %d lỗi\", len(ok), len(loi))\n    return ok, loi\n```\n\nBa mức xử lý trong một hàm, và đây là mẫu đáng học:\n\n```\nLỗi ĐÃ LƯỜNG (ValueError, KeyError) → ghi lại, bỏ qua bản ghi đó, chạy tiếp\nLỗi KHÔNG lường                      → ghi truy vết đầy đủ rồi DỪNG\nCuối cùng                            → BÁO CÁO số liệu, không im lặng\n```\n\nViệc `raise` lại ở nhánh `except Exception` là điểm mấu chốt: nếu có một loại lỗi bạn chưa nghĩ tới, bạn muốn **biết ngay** chứ không muốn xử lý sai 7.000 bản ghi rồi mới phát hiện.\n\n### Thử lại với ngoại lệ\n\n```python\nimport time, random\n\ndef goi_api_co_thu_lai(url, so_lan=3, cho_ban_dau=1.0):\n    lan_cuoi = None\n    for lan in range(so_lan):\n        try:\n            return goi_api(url)\n        except (TimeoutError, ConnectionError) as e:\n            lan_cuoi = e\n            if lan == so_lan - 1:\n                break                      # hết lượt, thoát để ném ở dưới\n            cho = cho_ban_dau * (2 ** lan) + random.uniform(0, 0.5)\n            logging.warning(\"lần %d lỗi (%s), thử lại sau %.1fs\", lan + 1, e, cho)\n            time.sleep(cho)\n        except ValueError:\n            raise                          # ⭐ lỗi dữ liệu: thử lại vô nghĩa\n\n    raise ConnectionError(f\"thất bại sau {so_lan} lần\") from lan_cuoi\n```\n\nHai điểm thiết kế quan trọng:\n\n```\nCHỈ thử lại lỗi TẠM THỜI (timeout, mất kết nối)\nKHÔNG thử lại lỗi dữ liệu — gửi lại y nguyên thì vẫn lỗi y nguyên\n```\n\nĐiều này nối thẳng với bài **HTTP** ở module 4: `5xx` đáng retry, `4xx` thì không. Cùng một nguyên tắc, ở hai tầng khác nhau.\n\nDòng `from lan_cuoi` giữ lại nguyên nhân thật của lần thử cuối — thiếu nó, người gỡ lỗi chỉ thấy \"thất bại sau 3 lần\" mà không biết vì sao.\n\n### Ngoại lệ tự định nghĩa cho một thư viện nhỏ\n\n```python\nclass ViLoi(Exception):\n    \"\"\"Gốc của mọi lỗi trong module ví.\"\"\"\n\nclass KhongDuTien(ViLoi):\n    def __init__(self, can, co):\n        self.can, self.co = can, co\n        self.thieu = can - co\n        super().__init__(f\"cần {can:,}đ nhưng chỉ có {co:,}đ (thiếu {self.thieu:,}đ)\")\n\nclass ViBiKhoa(ViLoi):\n    pass\n\ndef rut(vi, so_tien):\n    if so_tien <= 0:\n        raise ValueError(f\"số tiền phải dương, nhận {so_tien}\")   # lỗi lập trình\n    if vi.bi_khoa:\n        raise ViBiKhoa(f\"ví {vi.id} đang bị khoá\")\n    if so_tien > vi.so_du:\n        raise KhongDuTien(so_tien, vi.so_du)\n    vi.so_du -= so_tien\n    return vi.so_du\n```\n\n```python\n# Nơi gọi — bắt được thông tin CÓ CẤU TRÚC\ntry:\n    rut(vi, 500_000)\nexcept KhongDuTien as e:\n    print(f\"Bạn cần nạp thêm {e.thieu:,}đ\")     # ⭐ dùng dữ liệu của ngoại lệ\nexcept ViBiKhoa:\n    print(\"Vui lòng liên hệ hỗ trợ\")\nexcept ViLoi as e:\n    print(f\"Lỗi ví: {e}\")                        # mọi lỗi ví khác\n```\n\nChú ý sự phân biệt: `ValueError` cho **lỗi lập trình** (gọi hàm sai), các lớp tự định nghĩa cho **tình huống nghiệp vụ** mà người gọi cần xử lý khác nhau. Đây là ranh giới thiết kế đáng giữ.\n\n### `raise ... from` — nhìn khác biệt trong truy vết\n\n```python\ndef doc_cau_hinh(d):\n    try:\n        return d[\"database\"][\"host\"]\n    except KeyError as e:\n        raise CauHinhLoi(\"thiếu database.host\") from e\n```\n\n```\nTraceback (most recent call last):\n  File \"...\", line 3, in doc_cau_hinh\n    return d[\"database\"][\"host\"]\nKeyError: 'host'                                    ← ⭐ nguyên nhân THẬT\n\nThe above exception was the direct cause of the following exception:\n\nTraceback (most recent call last):\n  ...\nCauHinhLoi: thiếu database.host\n```\n\nNgười gỡ lỗi thấy được cả **cái gì sai** (thiếu `host`) lẫn **ý nghĩa** (lỗi cấu hình). Với `from None`, dòng `KeyError: 'host'` biến mất và bạn mất đúng chi tiết cần nhất.\n\n### Kiểm tra đầu vào ở biên\n\n```python\ndef tao_nguoi_dung(ten, email, tuoi):\n    if not ten or not ten.strip():\n        raise ValueError(\"tên không được rỗng\")\n    if \"@\" not in email:\n        raise ValueError(f\"email không hợp lệ: {email!r}\")\n    if not isinstance(tuoi, int):\n        raise TypeError(f\"tuổi phải là int, nhận {type(tuoi).__name__}\")\n    if not 0 < tuoi < 150:\n        raise ValueError(f\"tuổi ngoài khoảng hợp lệ: {tuoi}\")\n    return {\"ten\": ten.strip(), \"email\": email, \"tuoi\": tuoi}\n```\n\nHai nguyên tắc thể hiện ở đây:\n\n```\n① Kiểm tra ở BIÊN (nơi nhận dữ liệu ngoài), không rải khắp nơi\n② Thông báo lỗi phải nêu GIÁ TRỊ THẬT bằng !r\n   \"tuổi ngoài khoảng hợp lệ: 200\"  ≫  \"dữ liệu không hợp lệ\"\n```\n\nDấu `!r` gọi `repr()` nên chuỗi hiện cả dấu nháy — phân biệt được `\"\"` với `\" \"`, thứ mà `str()` không cho thấy.\n\n### `assert` — và vì sao không dùng để kiểm tra đầu vào\n\n```python\n# ❌ assert BIẾN MẤT khi chạy với -O\ndef rut(vi, tien):\n    assert tien > 0, \"số tiền phải dương\"       # bị bỏ qua với python -O\n\n# ✓ dùng raise cho kiểm tra thật\ndef rut(vi, tien):\n    if tien <= 0:\n        raise ValueError(f\"số tiền phải dương, nhận {tien}\")\n```\n\n```bash\npython -O script.py       # ⇒ MỌI assert bị vô hiệu hoá\n```\n\nRanh giới đúng: **`assert` cho giả định nội bộ mà bạn tin chắc đúng** (\"tới đây thì `ds` phải đã sắp xếp\"), **`raise` cho mọi kiểm tra dữ liệu từ bên ngoài.** Dùng `assert` để kiểm tra đầu vào người dùng là một lỗ hổng bảo mật khi ai đó chạy với `-O`.\n\n### Ghi log ngoại lệ đúng cách\n\n```python\nimport logging\nlog = logging.getLogger(__name__)          # ⭐ __name__ — nối bài module\n\ntry:\n    xu_ly()\nexcept ValueError as e:\n    log.error(\"lỗi: %s\", e)                # ✗ MẤT truy vết\n    log.exception(\"lỗi khi xử lý\")         # ✓ tự đính kèm truy vết\n    log.error(\"lỗi\", exc_info=True)        # ✓ tương đương\n```\n\nDùng `%s` thay vì f-string trong lời gọi log là quy ước có lý do: chuỗi chỉ được ghép **khi mức log đó thật sự được ghi ra**. Với log mức `debug` trong vòng lặp nóng, khác biệt này đo được.\n\n`logging.getLogger(__name__)` là mẫu chuẩn: tên logger trùng tên module, nên bật/tắt log theo từng module được — một ứng dụng trực tiếp của bài trước.",

so_sanh: "### EAFP và LBYL\n\n| | EAFP (`try/except`) | LBYL (`if` kiểm tra) |\n|---|---|---|\n| Phong cách | **Python** | C, Java |\n| Khe hở đua tranh | **không** | **có** |\n| Khi lỗi hiếm | **nhanh hơn** | chậm hơn (kiểm tra thừa) |\n| Khi lỗi thường xuyên | chậm hơn | **nhanh hơn** |\n| Bao phủ đủ trường hợp | **✓ tự nhiên** | dễ sót điều kiện |\n| Đọc dễ | khi lỗi là ngoại lệ thật | khi điều kiện đơn giản |\n\nHàng \"khe hở đua tranh\" là lý do kỹ thuật mạnh nhất: giữa `if os.path.exists(f)` và `open(f)`, file có thể biến mất. EAFP không có khoảng trống đó.\n\nChọn thế nào: **EAFP là mặc định; LBYL khi điều kiện rẻ, rõ, và trường hợp \"sai\" xảy ra thường xuyên.** Và trước cả hai, xem có sẵn `dict.get`, `getattr`, `next(it, mac_dinh)` không.\n\n### Bốn khối\n\n| | Chạy khi nào | Dùng cho |\n|---|---|---|\n| `try` | luôn bắt đầu | **chỉ** phần có thể lỗi |\n| `except` | khi có ngoại lệ khớp | xử lý tình huống đã lường |\n| `else` | **khi KHÔNG có lỗi** | phần tiếp theo, để `try` khỏi quá rộng |\n| `finally` | **luôn**, kể cả có `return` | dọn dẹp tài nguyên |\n\n`else` là khối bị bỏ quên nhiều nhất, mà công dụng của nó rất thật: **thu hẹp `try` để không bắt nhầm ngoại lệ cùng loại phát sinh từ code phía sau.**\n\n### Các mức bắt ngoại lệ\n\n| | `except:` | `except Exception:` | Loại cụ thể |\n|---|---|---|---|\n| Bắt `Ctrl+C` | **✓ rất tệ** | ✗ | ✗ |\n| Bắt `sys.exit()` | **✓ rất tệ** | ✗ | ✗ |\n| Che lỗi lập trình | ✓ | **✓** | ✗ |\n| Chấp nhận được khi | **không bao giờ** | tầng ngoài cùng, **có ghi log** | **luôn** |\n\nCột giữa cần điều kiện kèm theo: `except Exception` **kèm `logging.exception`** ở tầng ngoài cùng là mẫu hợp lệ; `except Exception: pass` thì không bao giờ.\n\n### `raise`, `raise from e`, `raise from None`\n\n| | Truy vết gốc | Thông báo trong truy vết | Dùng khi |\n|---|---|---|---|\n| `raise` (trần) | **giữ nguyên** | — | ném lại sau khi ghi log |\n| `raise X from e` | **giữ** | \"direct cause\" | **chuyển đổi ngoại lệ** |\n| `raise X` | giữ (ngầm) | \"during handling\" | ít dùng |\n| `raise X from None` | **mất** | — | khi nguyên nhân gốc là nhiễu |\n\nMặc định nên dùng `raise ... from e` khi chuyển đổi. `from None` chỉ dùng có chủ đích, vì nó lấy đi thông tin của người gỡ lỗi.\n\n### `assert` và `raise`\n\n| | `assert` | `raise` |\n|---|---|---|\n| Còn hiệu lực với `python -O` | **✗ bị xoá** | ✓ |\n| Dùng cho | giả định nội bộ | **kiểm tra dữ liệu ngoài** |\n| Trong kiểm thử | ✓ đúng chỗ | — |\n| Kiểm tra đầu vào người dùng | **✗ nguy hiểm** | ✓ |\n\nHàng đầu là lý do quyết định: code chạy với `-O` sẽ **bỏ qua toàn bộ `assert`**, nên mọi kiểm tra an toàn viết bằng `assert` biến mất im lặng.\n\n### `try/finally` và `with`\n\n| | `try/finally` | `with` |\n|---|---|---|\n| Đảm bảo dọn dẹp | ✓ | ✓ |\n| Số dòng | nhiều hơn | **ít hơn** |\n| Quên dọn dẹp | **dễ quên** | không thể quên |\n| Nhiều tài nguyên | lồng nhiều tầng | **một dòng** |\n| Dùng khi | không có context manager | **mặc định** |\n\n`with` chính là `try/finally` được đóng gói, nên hiểu bài này là đã hiểu một nửa bài context manager.\n\n### Bắt ngoại lệ ở tầng nào\n\n| Tầng | Nên làm gì |\n|---|---|\n| Hàm tiện ích sâu bên trong | **ném lên**, đừng bắt |\n| Tầng nghiệp vụ | bắt loại cụ thể, chuyển đổi bằng `from e` |\n| Vòng lặp xử lý hàng loạt | bắt lỗi đã lường, **ghi log và đếm** |\n| Điểm vào chương trình | `except Exception` + `logging.exception` |\n\nNguyên tắc chung: **bắt ngoại lệ ở nơi có đủ thông tin để quyết định phải làm gì.** Hàm tính toán sâu bên trong không biết nên thử lại hay báo người dùng, nên nó không nên bắt.",

loi_thuong_gap: [
  "**`except: pass`.** Nuốt cả `Ctrl+C`, che mọi lỗi, và biến lỗi rõ ràng thành lỗi im lặng ở chỗ khác. Phản mẫu tai hại nhất trong bài.",
  "**`except Exception` mà không ghi log.** Lỗi gõ sai tên biến cũng bị che.",
  "**Bọc `try/except` quanh cả hàm.** `try` chỉ nên chứa dòng có thể ném ngoại lệ bạn định bắt; phần còn lại đưa vào `else`.",
  "**Đặt `except` cha trước `except` con.** `except OSError` trước `except FileNotFoundError` khiến khối sau không bao giờ chạy.",
  "**`return` trong `finally`.** Nuốt cả ngoại lệ lẫn giá trị trả về của `try`.",
  "**Dùng `assert` để kiểm tra đầu vào.** `python -O` xoá hết `assert`. Dùng `raise`.",
  "**`raise X` mà quên `from e` khi chuyển đổi ngoại lệ.** Truy vết mất đoạn nguyên nhân gốc.",
  "**Dùng `from None` theo thói quen.** Nó xoá đúng thông tin người gỡ lỗi cần nhất.",
  "**`logging.error` thay vì `logging.exception`.** Mất truy vết. Hoặc thêm `exc_info=True`.",
  "**Thông báo lỗi không nêu giá trị thật.** \"Dữ liệu không hợp lệ\" vô dụng; nêu `{gia_tri!r}`.",
  "**Thử lại mọi loại lỗi.** Lỗi dữ liệu gửi lại y nguyên vẫn lỗi y nguyên. Chỉ thử lại lỗi tạm thời.",
  "**Bắt ngoại lệ ở hàm sâu bên trong.** Nó không đủ thông tin để quyết định. Ném lên tầng biết phải làm gì.",
  "**Dùng `try/finally` khi đã có `with`.** Dài hơn và dễ quên hơn.",
  "**Xử lý hàng loạt mà không đếm và báo cáo số lỗi.** Chương trình báo \"xong\" trong khi một phần ba dữ liệu bị bỏ.",
  "**Tạo lớp ngoại lệ mới khi `ValueError` đã đủ.** Người khác phải đọc tài liệu của bạn mới hiểu."
],

tu_kiem_tra: [
  "EAFP là gì? Nêu ba lý do kỹ thuật khiến Python thiên về nó.",
  "Đoạn LBYL kiểm tra `os.path.exists` trước khi mở file vẫn có thể lỗi — vì sao?",
  "Vai trò của `else` trong `try/except/else/finally`? Nó giải quyết vấn đề gì?",
  "`finally` có chạy khi trong `try` có `return` không? Vì sao không được `return` trong `finally`?",
  "Vì sao `except:` trần tệ hơn `except Exception:`? Cây phân cấp giải thích thế nào?",
  "Trường hợp nào `except Exception` là chính đáng, và cần kèm điều kiện gì?",
  "Vì sao thứ tự các khối `except` phải từ hẹp tới rộng?",
  "`raise`, `raise X from e`, `raise X from None` khác nhau ở thông tin nào?",
  "Vì sao không dùng `assert` để kiểm tra đầu vào người dùng?",
  "Đọc truy vết theo thứ tự nào? Vì sao phải tìm khung cuối cùng thuộc code của mình?",
  "Vòng `for` dùng ngoại lệ nào để biết đã hết? Điều đó nói gì về vai trò của ngoại lệ trong Python?",
  "Trong hàm thử lại, vì sao chỉ thử lại lỗi tạm thời? Liên hệ với `4xx`/`5xx` của HTTP.",
  "Khi nào nên tạo lớp ngoại lệ riêng, và khi nào dùng ngoại lệ dựng sẵn?",
  "Vì sao dùng `log.error(\"lỗi: %s\", e)` thay vì f-string trong lời gọi log?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Errors and Exceptions* trong tutorial và *Built-in Exceptions* trong tham khảo — cây phân cấp đầy đủ nằm ở mục sau.",
  "Sách **Effective Python** — Brett Slatkin. Các mục về `try/except/else/finally` và về ngoại lệ tự định nghĩa cho API.",
  "Sách **Fluent Python** — Luciano Ramalho. Phần về EAFP và về vai trò của ngoại lệ trong giao thức iterator.",
  "Tài liệu Python, module `logging` — mục *Logging Cookbook*, phần về `exception()` và `exc_info`.",
  "Tài liệu Python, module `traceback` — khi cần lấy truy vết dạng chuỗi để gửi đi nơi khác."
],

lien_he: [
  "**Module, package, import** ngay trước: `logging.getLogger(__name__)` dùng đúng biến `__name__` của bài đó.",
  "**Context manager và `with`** trong module này: `with` chính là `try/finally` được đóng gói.",
  "**Generator, iterator và `yield`**: `StopIteration` là ngoại lệ điều khiển luồng của vòng `for`.",
  "**Đọc ghi file, JSON, CSV**: `FileNotFoundError`, `JSONDecodeError`, `UnicodeDecodeError` là ba ngoại lệ gặp nhiều nhất ở đó.",
  "**Hàm, tham số mặc định**: kiểm tra đầu vào rồi `raise ValueError` là mẫu chuẩn của hàm công khai.",
  "**Tín hiệu, tiến trình nền, exit code** ở module 2: `KeyboardInterrupt` là SIGINT, và `except:` trần chặn mất nó.",
  "**HTTP: method, header, status code** ở module 4: `4xx` không retry, `5xx` thì có — cùng nguyên tắc với việc chọn ngoại lệ nào đáng thử lại.",
  "**Backend & Web** ở mảng 05: ánh xạ ngoại lệ nghiệp vụ sang status code là công việc thiết kế API trực tiếp.",
  "**Clean Code** ở module 8 mảng 01: thông báo lỗi rõ ràng và xử lý lỗi ở đúng tầng là hai chủ đề trung tâm."
]

};
