window.LESSONS["03:thao-tac-hang-ngay:git-status-doc-duoc-moi-trang-thai-file"] = {

tom_tat: [
  "`git status` là bảng điều khiển cục bộ: nó đối chiếu `HEAD`, index và cây làm việc để cho biết điều gì đã stage, điều gì chưa stage, file nào chưa được theo dõi và nhánh đang liên hệ với upstream ra sao.",
  "Hai cột của `git status --short` không phải một mã duy nhất: cột X mô tả index so với `HEAD`, cột Y mô tả cây làm việc so với index; một file có thể đồng thời mang hai thay đổi khác nhau như `MM`.",
  "`status` không đọc suy nghĩ của remote theo thời gian thực; thông tin ahead/behind dựa trên remote-tracking refs của lần fetch gần nhất.",
  "Đổi tên được Git suy ra từ nội dung khi so sánh snapshot, không phải sự kiện filesystem được ghi vĩnh viễn; cùng một thay đổi có thể hiện xóa và thêm trước staging rồi hiện rename sau staging.",
  "Thói quen an toàn là đọc status trước và sau mỗi thao tác làm đổi index/cây làm việc, sau đó dùng `git diff` và `git diff --cached` để xem nội dung chứ không dừng ở tên trạng thái."
],

can_biet_truoc: [
  "Hiểu repository đã được `init` hoặc `clone`, có `HEAD` trỏ tới commit hiện tại và có thể có upstream như `origin/main`.",
  "Biết `.gitignore` chỉ lọc file untracked; file tracked vẫn được Git báo khi sửa, xóa hoặc đổi tên.",
  "Phân biệt cây làm việc là file đang mở trên ổ đĩa, index là đề xuất snapshot kế tiếp, và commit `HEAD` là mốc lịch sử hiện tại.",
  "Biết `git add` cập nhật index chứ chưa tạo commit; bài sau sẽ đào sâu cách chọn theo file và theo hunk."
],

dinh_nghia: "`git status` tính và trình bày trạng thái của repository tại thời điểm chạy. Nó so sánh `HEAD → index` để tìm thay đổi đã stage, so sánh `index → working tree` để tìm thay đổi chưa stage, xác định đường dẫn untracked/ignored theo yêu cầu hiển thị, rồi bổ sung ngữ cảnh branch, rebase/merge và upstream.\n\nLệnh mặc định viết lời giải thích dài, phù hợp lúc học. `git status --short` hoặc `--porcelain` tạo biểu diễn gọn, ổn định hơn cho mắt hoặc script. Status là quan sát, không sửa file, không stage, không fetch và không gửi dữ liệu lên GitHub.",

vi_sao_can: "Git cho phép chuẩn bị commit khác nội dung đang nằm trên ổ đĩa. Đây là sức mạnh giúp tạo lịch sử rõ, nhưng cũng tạo ra tình huống một file tồn tại ở ba phiên bản: bản trong `HEAD`, bản đã stage, và bản vừa sửa thêm chưa stage. Nếu chỉ hỏi “file đã sửa chưa?”, bạn mất hai phần ba thông tin và dễ commit thiếu hoặc thừa.\n\nTrong “Sổ việc”, ta có thể stage phần sửa lỗi parse ngày, sau đó tiếp tục viết tính năng gắn nhãn trong cùng `src/tasks.js`. Status phải cho biết cả hai lớp đang tồn tại. Nó cũng phải phân biệt file test mới chưa tracked, file dữ liệu ignored, file đã xóa và đổi tên. Không có bước đọc trạng thái, `git add` trở thành phỏng đoán.\n\nStatus còn đóng vai trò kiểm tra phạm vi: tên nhánh lạ, hàng trăm file untracked hoặc đường dẫn ngoài dự kiến thường báo bạn đứng nhầm repository. Vì lệnh chỉ đọc trạng thái và chạy cục bộ, đây là phản xạ rẻ trước những thao tác có hậu quả như reset, commit hoặc push.",

co_che: "### Tam giác `HEAD` — index — cây làm việc\n\nHãy hình dung ba snapshot, không phải hai trạng thái “đã lưu/chưa lưu”:\n\n```text\nHEAD  --(thay đổi đã stage)-->  INDEX  --(thay đổi chưa stage)-->  WORKTREE\n```\n\n`HEAD` là commit đang checkout. Index, còn gọi staging area, là bản mô tả chính xác commit kế nếu commit ngay. Worktree là file thật bạn chỉnh bằng editor. Status chạy hai phép so sánh chính. Khi ba bản giống nhau, cây sạch. Khi worktree khác index, có thay đổi chưa stage. Khi index khác `HEAD`, có thay đổi chờ commit. Hai phép so sánh có thể cùng khác trên một đường dẫn.\n\n### Đọc output dài trước khi học mã ngắn\n\n`git status` mặc định chia nội dung thành các nhóm có câu hướng dẫn. “Changes to be committed” là khác biệt index so với `HEAD`. “Changes not staged for commit” là khác biệt worktree so với index. “Untracked files” là đường dẫn có trên ổ đĩa nhưng index và `HEAD` chưa biết, sau khi áp dụng ignore.\n\nCác câu gợi ý như `git add` hay `git restore` phụ thuộc trạng thái cụ thể. Đọc chúng như chỉ dẫn ngữ cảnh, không biến thành công thức gõ vô điều kiện. Trước khi restore, phải biết đang bỏ thay đổi ở lớp nào vì khôi phục worktree và gỡ stage là hai ý định khác nhau.\n\n### Hai cột X và Y của dạng short\n\nDạng ngắn thường có cấu trúc `XY đường-dẫn`. X là trạng thái của index so với `HEAD`; Y là trạng thái worktree so với index. Khoảng trắng cũng có nghĩa: ` M file` là chỉ sửa chưa stage, `M  file` là sửa đã stage. Một số mã thường gặp:\n\n| XY | Diễn giải |\n|---|---|\n| `??` | untracked, chưa có trong index |\n| `!!` | ignored, chỉ hiện khi yêu cầu |\n| ` M` | worktree sửa so với index |\n| `M ` | index sửa so với `HEAD` |\n| `MM` | đã stage một bản, rồi worktree sửa thêm |\n| `A ` | file mới đã stage |\n| ` D` | tracked file bị xóa ở worktree, chưa stage |\n| `D ` | việc xóa đã stage |\n| `R ` | đổi tên được nhận diện trong phần staged |\n\nĐừng bỏ khoảng trắng khi ghi chú, vì `M ` và ` M` trả lời hai câu khác nhau. Với font khó nhìn, dùng output dài hoặc porcelain có delimiters phù hợp.\n\n### Vì sao xuất hiện `MM`\n\nGiả sử `src/tasks.js` ban đầu thuộc `HEAD`. Bạn sửa hàm parse rồi `git add` file; index nhận bản mới. Sau đó bạn thêm hàm label trên worktree nhưng chưa add lại. So sánh `HEAD → index` thấy modified, và `index → worktree` cũng thấy modified, nên status in `MM`.\n\nNếu commit ngay, chỉ bản trong index được commit; phần label vẫn ở worktree. Đây không phải lỗi hay race condition. Nó là bằng chứng staging là snapshot nội dung, không phải cờ “file này đã được chọn mãi mãi”. Mỗi lần sửa sau add tạo ra nội dung mới cần quyết định lại.\n\n### Untracked không đồng nghĩa ignored\n\nUntracked là file Git chưa quản lý và không bị pattern hiệu lực che. Ignored cũng chưa tracked, nhưng bị chính sách bỏ qua nên mặc định không hiện. Dùng:\n\n```sh\ngit status --short\ngit status --short --ignored\n```\n\nTrong “Sổ việc”, `test/tasks.test.js` mới có thể hiện `??`, còn `data/tasks.json` và `.env` hiện `!!` ở lệnh thứ hai. Nếu `.env` đã tracked từ trước, ignore không che nó; thay đổi vẫn mang mã `M`.\n\n### Xóa file: mất trên ổ đĩa và đề xuất xóa là hai bước\n\nXóa bằng Explorer hoặc `rm src/legacy.js` chỉ làm worktree khác index, thường ra ` D`. Chạy `git add src/legacy.js` hoặc `git rm` cập nhật index để đề xuất snapshot không còn file, thành `D `. Commit sau đó ghi nhận việc xóa.\n\nĐiều này cho phép phục hồi file chưa stage từ index, hoặc gỡ một việc xóa đã stage mà vẫn cân nhắc nội dung worktree. Nhưng các lệnh restore có thể ghi đè dữ liệu; status chỉ báo trạng thái, không phải backup cho file untracked.\n\n### Đổi tên là kết luận so sánh, không phải nhật ký sự kiện\n\nGit lưu snapshot, không lưu sự kiện “người dùng kéo file A sang B”. Khi so hai snapshot, nó có thể nhận ra một file bị xóa và file mới có nội dung tương tự rồi trình bày rename. Trước khi stage, status có thể hiển thị delete cộng untracked. Sau `git add -A`, phép so `HEAD → index` có đủ hai đầu và suy ra `R  old -> new`.\n\nNếu đồng thời viết lại gần hết nội dung, độ tương đồng thấp và Git có thể trình bày xóa/thêm. Lịch sử vẫn biểu diễn snapshot đúng; “rename” là cách hiển thị hữu ích, không phải object đặc biệt bắt buộc.\n\n### Nhánh, upstream và chữ ahead/behind\n\n`git status --branch --short` thêm dòng như `## main...origin/main [ahead 1, behind 2]`. Con số được tính giữa branch cục bộ và remote-tracking ref hiện có. Status không tự kết nối server. Nếu đồng đội vừa push nhưng bạn chưa fetch, số behind có thể cũ.\n\nMuốn cập nhật hiểu biết về remote, chạy `git fetch` rồi status lại. Fetch thay đổi refs cục bộ nhưng không trộn code vào branch đang làm. Không có upstream thì status không thể suy ra ahead/behind; điều đó không nhất thiết là lỗi với branch cục bộ thử nghiệm.\n\n### Trạng thái trong merge, rebase và conflict\n\nKhi merge hoặc rebase dừng vì conflict, status trở thành hướng dẫn quy trình: nó cho biết đường dẫn unmerged và thao tác tiếp tục/hủy phù hợp. Mã short có các cặp như `UU`, `AA`, `DD`, `AU`, phản ánh hai phía và loại xung đột, không còn chỉ là phép so đơn giản thông thường.\n\nGiải quyết conflict nghĩa là sửa file thành nội dung mong muốn rồi add để đánh dấu đã xử lý; chỉ xóa marker `<<<<<<<` chưa đủ nếu chưa cập nhật index. Trước khi chạy continue, status phải không còn unmerged path. Không dùng `git commit` hay `rebase --continue` theo trí nhớ khi status nói repository đang ở quy trình khác.\n\n### `--porcelain` dành cho tự động hóa\n\nOutput mặc định hướng đến con người và có thể thay đổi theo cấu hình màu, ngôn ngữ hay phiên bản. `--porcelain=v1` cam kết định dạng ổn định hơn. Với tên file có khoảng trắng, newline hoặc ký tự đặc biệt, parsing bằng `split(' ')` rất dễ sai. Tùy trường hợp dùng `-z` để phân cách bằng byte NUL:\n\n```sh\ngit status --porcelain=v1 -z\n```\n\nĐầu ra NUL không thân thiện khi nhìn trực tiếp nhưng phù hợp cho chương trình. Script cũng cần kiểm exit code và chạy ở repository root xác định, thay vì tin thư mục hiện tại.\n\n### Status không thay thế diff\n\nStatus nói “đường dẫn nào, ở lớp nào”, không nói từng dòng thay đổi. Hai lệnh bổ sung:\n\n```sh\ngit diff\ngit diff --cached\n```\n\nLệnh đầu xem worktree so với index; lệnh sau xem index so với `HEAD`. Với file `MM`, hai output khác nhau và cùng cần review. File untracked thường không xuất hiện trong `git diff` thông thường vì chưa có bản trong index để so; phải mở file hoặc stage có chọn lọc.\n\n### Refresh, timestamp và hiệu năng trên repository lớn\n\nGit thường dùng metadata filesystem để tránh đọc lại mọi byte, rồi kiểm nội dung khi cần. Trên cây rất lớn, status có thể tốn thời gian vì phải khảo sát nhiều đường dẫn. Các tối ưu như untracked cache hay filesystem monitor tồn tại, nhưng không nên bật ngẫu nhiên để che vấn đề repository chứa build output khổng lồ chưa ignore.\n\nTimestamp thay đổi không nhất thiết có nghĩa nội dung khác; Git xác minh để tránh báo modified giả. Ngược lại, công cụ format chạy nền có thể thật sự đổi newline hoặc quyền executable, khiến status báo thay đổi dù mắt thường khó thấy. `git diff --summary`, cấu hình line ending và diff nội dung giúp chẩn đoán.\n\n### Ca biên: tên file, quyền và line ending\n\nTên file có khoảng trắng được quote trong output tùy cấu hình. Script phải dùng porcelain `-z`. Trên hệ hỗ trợ executable bit, `chmod` có thể tạo thay đổi mode mà nội dung không đổi. Chuyển Windows/Linux có thể làm toàn file đổi do CRLF/LF nếu thuộc tính và cấu hình không thống nhất.\n\nKhi status báo hàng loạt file modified ngay sau clone, đừng add. Chọn một file, xem `git diff --ignore-space-at-eol` và `git diff --summary`, kiểm `.gitattributes` cùng `core.autocrlf`. Sửa nguyên nhân trước, nếu không commit sẽ chứa nhiễu nền tảng.\n\n### Quy trình đọc status có mục đích\n\nThứ tự thực tế: xác nhận branch/quy trình đang diễn ra; nhìn staged; nhìn unstaged; nhìn untracked; nếu có remote context thì đánh giá độ mới của fetch; sau đó mở diff. Chạy lại sau add/restore/rename để kiểm giả thuyết.\n\nMột status sạch chỉ có nghĩa `HEAD`, index và worktree thống nhất đối với nội dung Git theo dõi tại thời điểm đó. Nó không chứng minh test pass, remote mới nhất, file ignored đã backup, hay không có tiến trình khác sắp sửa file.\n\n### Khi nào không nên coi status là câu trả lời cuối\n\nKhông dùng status để khẳng định branch đã đồng bộ server khi chưa fetch. Không dùng tên `M` để review nội dung. Không coi ignored là an toàn. Không parse output dành cho người bằng regex mong manh. Không chạy lệnh sửa chữa phá hủy chỉ vì thấy working tree dirty; trạng thái bẩn thường chính là công việc chưa hoàn tất cần bảo toàn.",

vi_du: "### Tạo đủ trạng thái trong một repository cục bộ\n\n```sh\nmkdir status-lab && cd status-lab\ngit init --initial-branch=main\nmkdir src test data\nprintf '.env\\ndata/*.json\\n' > .gitignore\nprintf 'export function addTask(x) { return [x]; }\\n' > src/tasks.js\nprintf 'export const version = 1;\\n' > src/config.js\ngit add .gitignore src/tasks.js src/config.js\ngit -c user.name=\"Hoc Git\" -c user.email=\"hoc@example.invalid\" commit -m \"Tạo lõi Sổ việc\"\n\n# sửa rồi stage bản thứ nhất\nprintf 'export function addTask(x) { return [{...x, done:false}]; }\\n' > src/tasks.js\ngit add src/tasks.js\n# sửa tiếp sau khi stage\nprintf 'export function labels(task) { return task.labels ?? []; }\\n' >> src/tasks.js\n# file mới, xóa file cũ và dữ liệu ignored\nprintf 'import test from \"node:test\";\\n' > test/tasks.test.js\nrm src/config.js\nprintf '{\"tasks\":[]}\\n' > data/tasks.json\nprintf 'SO_VIEC_TOKEN=local\\n' > .env\n\ngit status --short\ngit status --short --ignored\n```\n\nCác dòng cốt lõi dự kiến: `MM src/tasks.js`, ` D src/config.js`, `?? test/`, và khi thêm `--ignored` có `!! .env`, `!! data/`. Thứ tự có thể phụ thuộc sắp xếp tên, nhưng ý nghĩa hai cột không đổi.\n\n### Chứng minh hai lớp diff của `MM`\n\n```sh\ngit diff --cached -- src/tasks.js\ngit diff -- src/tasks.js\n```\n\nDiff cached chỉ chứa thay đổi `addTask`; diff không đối số cached chỉ chứa hàm `labels` được thêm sau. Nếu commit ngay, labels không vào commit. Tiếp tục stage việc xóa và file test:\n\n```sh\ngit add src/config.js test/tasks.test.js\ngit status --short\ngit diff --cached --name-status\n```\n\n`src/config.js` chuyển từ ` D` thành `D `, test thành `A `, còn tasks vẫn `MM`. Lab này có thể xóa sau khi học vì toàn bộ lịch sử chỉ phục vụ quan sát.",

so_sanh: "### Mỗi lệnh trả lời một câu khác nhau\n\n| Lệnh | So sánh/nguồn | Trả lời tốt nhất |\n|---|---|---|\n| `git status` | tổng hợp nhiều lớp | file nào ở trạng thái nào, đang trong quy trình gì |\n| `git diff` | worktree ↔ index | chưa stage đã đổi những dòng nào |\n| `git diff --cached` | index ↔ `HEAD` | commit ngay sẽ chứa những dòng nào |\n| `git ls-files` | index | Git đang theo dõi đường dẫn nào |\n| `git log` | commit graph | lịch sử đã ghi nhận gì |\n| `git fetch` | trao đổi với remote | cập nhật remote-tracking refs để ahead/behind mới hơn |\n\n### Output cho người và output cho máy\n\n| Dạng | Ưu điểm | Bẫy | Nên dùng khi |\n|---|---|---|---|\n| mặc định dài | giải thích và gợi ý lệnh | khó parse, nhiều chữ | học và thao tác tay |\n| `--short` | quét nhanh, thấy XY | khoảng trắng dễ bị bỏ sót | kiểm tra hằng ngày |\n| `--porcelain=v1` | ổn định hơn | vẫn cần xử lý tên đặc biệt | script đơn giản |\n| `--porcelain=v1 -z` | tên file không nhập nhằng | khó đọc trực tiếp | tự động hóa chắc chắn |",

loi_thuong_gap: [
  "Đọc `M ` và ` M` như nhau; nhận biết bằng việc commit thử không chứa phần mong muốn. Luôn gắn cột X với index và cột Y với worktree, hoặc dùng output dài.",
  "Thấy `MM` rồi tưởng Git báo trùng; thực tế file đã stage một bản và được sửa tiếp. Xem riêng `git diff --cached` cùng `git diff` để thấy hai tập dòng.",
  "Tin ahead/behind là trạng thái server trực tiếp; dấu hiệu là đồng đội nói đã push nhưng status không đổi. Fetch trước khi dùng con số để quyết định tích hợp.",
  "Không thấy file ignored nên tưởng nó đã được lưu; `status --ignored` cho thấy `!!`, còn `ls-files` không có đường dẫn. Git không phải backup cho file ấy.",
  "Parse output mặc định bằng cách tách khoảng trắng; tên file có space hoặc newline làm script sai. Dùng porcelain với `-z` và thư viện xử lý byte phù hợp.",
  "Add hàng loạt khi hàng trăm file đổi newline sau clone; diff cho thấy mọi dòng thay dù logic không đổi. Dừng, chẩn đoán `.gitattributes` và cấu hình line ending trước.",
  "Hiểu rename như sự kiện Git đã ghi; khi nội dung đổi mạnh, status có thể hiện delete/add. Đánh giá snapshot và diff, không phụ thuộc nhãn rename tuyệt đối."
],

tu_kiem_tra: [
  "Giải thích `MM file.js` bằng ba phiên bản nội dung cụ thể. Nếu commit ngay, phiên bản nào đi vào commit và phiên bản nào còn lại?",
  "Vì sao status sạch không chứng minh branch đã cập nhật với server? Nêu vai trò của remote-tracking ref và `git fetch`.",
  "Một file đổi tên hiện thành ` D old` và `?? new`. Sau staging nó có thể thành `R old -> new` vì sao, dù Git không theo dõi sự kiện rename?",
  "Thiết kế script lấy danh sách file thay đổi có tên chứa khoảng trắng và newline. Vì sao porcelain thường vẫn chưa đủ nếu thiếu `-z`?",
  "Phân biệt ` D`, `D ` và `DD`. Trường hợp cuối xuất hiện trong loại quy trình nào và tại sao không thể diễn giải chỉ bằng hai snapshot thông thường?",
  "Status báo mọi file modified ngay sau clone trên hệ điều hành khác. Hãy nêu chuỗi kiểm tra giúp phân biệt nội dung thật, line ending và executable bit trước khi add."
],

tai_nguyen: [
  "Sách “Pro Git” của Scott Chacon và Ben Straub, phần Recording Changes và Git Branching.",
  "Tài liệu đi kèm Git: `git help status`, `git help diff`, `git help ls-files` và `git help fetch`.",
  "Sách “Version Control with Git” của Jon Loeliger và Matthew McCullough, các phần index và working tree.",
  "Sách “Git Pocket Guide” của Richard E. Silverman, dùng làm tài liệu tra nhanh mã trạng thái và quy trình hằng ngày."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: index là một cấu trúc ánh xạ đường dẫn tới object và metadata, còn commit graph cho phép tính quan hệ ahead/behind giữa hai refs.",
  "Nối sang mảng Hệ điều hành: timestamp, executable bit, case sensitivity và line ending là metadata/filesystem behavior có thể làm trạng thái khác giữa máy.",
  "Nối sang mảng Kiểm thử: status sạch chỉ là một invariant về ba snapshot, độc lập với invariant chương trình đúng; pipeline vẫn phải chạy test và lint.",
  "Nối sang mảng Tự động hóa: porcelain `-z` minh họa nguyên tắc dùng giao diện ổn định cho máy và delimiter không nhập nhằng thay vì scrape văn bản cho người."
]

};
