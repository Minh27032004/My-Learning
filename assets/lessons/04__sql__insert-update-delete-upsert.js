window.LESSONS["04:sql:insert-update-delete-upsert"] = {

tom_tat: [
  "`INSERT`, `UPDATE`, `DELETE` thay đổi tập hàng dưới sự bảo vệ của kiểu dữ liệu, constraint và transaction; số hàng tác động là một phần của hợp đồng nghiệp vụ.",
  "`RETURNING` lấy dữ liệu thực sự đã ghi trong cùng statement, tránh truy vấn lại và tránh đoán id/default do server sinh.",
  "UPSERT của PostgreSQL dùng `INSERT ... ON CONFLICT` với một unique constraint/index làm trọng tài; nó không đơn giản là “nếu có thì update” bằng hai câu lệnh tách rời.",
  "MVCC và khóa hàng làm cho DML đồng thời khác hẳn chạy tuần tự; câu lệnh phải có điều kiện bảo vệ để tránh lost update, bán âm tồn kho và retry gây nhân đôi.",
  "Hệ thống Sao Mai dùng nhập đơn, chuyển trạng thái, hủy mềm và đồng bộ tồn kho để nối tính đúng đắn với query plan của phần tìm hàng cần sửa."
],

can_biet_truoc: [
  "Biết khóa chính, khóa ngoại, `UNIQUE`, `NOT NULL`, `CHECK` và giá trị mặc định.",
  "Hiểu transaction, `COMMIT`, `ROLLBACK` và khái niệm nhiều session truy cập cùng hàng.",
  "Biết `WHERE`, phép nối, subquery và index B-tree cơ bản.",
  "Phân biệt lỗi cú pháp, vi phạm constraint và thay đổi đúng cú pháp nhưng sai nghiệp vụ."
],

dinh_nghia: "**DML** (Data Manipulation Language, ngôn ngữ thao tác dữ liệu) là nhóm câu lệnh đọc và thay đổi hàng. Trong phạm vi bài này, `INSERT` tạo hàng, `UPDATE` sửa cột của hàng phù hợp, `DELETE` loại hàng, còn **UPSERT** yêu cầu PostgreSQL chèn hàng mới hoặc xử lý xung đột duy nhất bằng `DO NOTHING`/`DO UPDATE` trong một statement nguyên tử.\n\nMột statement DML không chỉ là phép gán. PostgreSQL phải tìm hàng, kiểm tra constraint, duy trì mọi index liên quan, tạo phiên bản hàng theo MVCC, ghi WAL để phục hồi, giữ khóa cần thiết và trả lỗi nếu invariant bị phá. Transaction xác định khi thay đổi trở nên bền vững và được nhóm với các thay đổi khác.",

vi_sao_can: "Ứng dụng sống nhờ thay đổi state: khách đặt đơn, kho giảm, trạng thái chuyển từ `pending` sang `paid`, tác vụ đồng bộ nhận lại cùng sự kiện. Nếu chỉ biết cú pháp `UPDATE table SET ...`, lập trình viên dễ bỏ `WHERE`, ghi đè thay đổi của session khác hoặc tách “kiểm tra rồi chèn” thành hai statement có race condition.\n\nDML đúng phải biến quy tắc nghiệp vụ thành điều kiện mà database có thể cưỡng chế. “Không bán quá tồn” nên là một `UPDATE ... WHERE quantity >= requested`, rồi kiểm số hàng trả về; không phải đọc quantity, tính trong ứng dụng, chờ mạng, sau đó ghi. “Mã sự kiện chỉ xử lý một lần” nên có unique key và UPSERT/`DO NOTHING`. Cách này đưa quyết định đến nơi dữ liệu đang bị tranh chấp.",

co_che: "### `INSERT`: khai báo cột là một hàng rào an toàn\n\n```sql\nINSERT INTO customers (email, full_name)\nVALUES ('an@example.com', 'An');\n```\n\nLuôn nêu danh sách cột trong code ứng dụng. Nếu schema thêm cột có default hoặc đổi thứ tự vật lý, statement vẫn giữ nghĩa. PostgreSQL chuyển kiểu, áp default cho cột thiếu, kiểm `NOT NULL`, `CHECK`, khóa ngoại và unique; chỉ sau khi toàn statement hợp lệ mới thành công. Một `INSERT` nhiều hàng là nguyên tử: một hàng vi phạm có thể làm cả statement thất bại.\n\n### Nhiều hàng và `INSERT ... SELECT`\n\n`VALUES` phù hợp lô nhỏ đã có trong ứng dụng. Khi nguồn nằm trong database, `INSERT ... SELECT` tránh kéo dữ liệu ra qua mạng:\n\n```sql\nINSERT INTO order_archive (order_id, customer_id, total_amount)\nSELECT order_id, customer_id, total_amount\nFROM orders\nWHERE ordered_at < date '2025-01-01';\n```\n\nĐây chưa phải “move”: hàng nguồn vẫn còn. Muốn chuyển nguyên tử có thể dùng data-modifying CTE với `DELETE ... RETURNING`, nhưng archive quy mô lớn còn cần chiến lược batch, partition và dung lượng WAL.\n\n### `RETURNING`: kết quả server đã chốt\n\n```sql\nINSERT INTO orders (customer_id, status, total_amount)\nVALUES (42, 'pending', 350000)\nRETURNING order_id, created_at, status;\n```\n\n`order_id` từ identity/sequence và `created_at` từ default được lấy trong cùng round trip. Truy vấn `SELECT max(order_id)` là sai dưới concurrency vì có thể lấy id của session khác. `RETURNING` cũng dùng với update/delete và nhìn thấy giá trị hàng tương ứng với thao tác đó.\n\n### `UPDATE` là phép biến đổi tập hợp\n\n```sql\nUPDATE orders\nSET status = 'expired', updated_at = clock_timestamp()\nWHERE status = 'pending'\n  AND created_at < current_timestamp - interval '30 minutes'\nRETURNING order_id;\n```\n\nMệnh đề `WHERE` chọn một tập, không phải “hàng đang mở trên màn hình”. Nếu điều kiện khớp 10.000 hàng, cả 10.000 bị sửa. PostgreSQL tạo tuple version mới; index có cột thay đổi phải được cập nhật. Update cột không nằm trong index đôi khi đủ điều kiện HOT update, giảm churn index, nhưng còn phụ thuộc trang có chỗ trống.\n\n### Guarded update ngăn tồn kho âm\n\nThay vì đọc rồi ghi bằng hai round trip:\n\n```sql\nUPDATE inventory\nSET quantity = quantity - 3\nWHERE product_id = 10\n  AND quantity >= 3\nRETURNING quantity;\n```\n\nNếu trả một hàng, phép trừ thành công và giá trị mới có trong kết quả. Nếu không trả hàng, hoặc sản phẩm không tồn tại hoặc không đủ kho; ứng dụng có thể phân biệt bằng truy vấn bổ sung khi thật sự cần. Khóa hàng đảm bảo hai session cạnh tranh không cùng dựa trên giá trị cũ rồi bán vượt. Điều kiện được đánh giá lại khi cần sau khi chờ khóa theo semantics của mức cô lập phổ biến.\n\n### Lost update và optimistic concurrency\n\nForm chỉnh sửa đọc `version=7`. Trong lúc người dùng gõ, session khác cập nhật thành 8. Gửi `UPDATE ... WHERE order_id=101` sẽ ghi đè mà không biết. Thêm version vào điều kiện:\n\n```sql\nUPDATE orders\nSET shipping_address = 'Đà Nẵng', version = version + 1\nWHERE order_id = 101 AND version = 7\nRETURNING version;\n```\n\nKhông có hàng trả về nghĩa là stale write hoặc id không tồn tại. Đây là optimistic locking: không khóa trong thời gian người dùng suy nghĩ, nhưng phát hiện xung đột lúc ghi. Ứng dụng phải báo/merge/reload, không tự retry với dữ liệu cũ.\n\n### `UPDATE ... FROM` và tính duy nhất phía nguồn\n\nPostgreSQL cho phép nối nguồn:\n\n```sql\nUPDATE inventory i\nSET quantity = s.quantity, synced_at = now()\nFROM stock_feed s\nWHERE s.sku = i.sku;\n```\n\nNếu `stock_feed` có hai hàng cùng `sku`, một hàng đích khớp nhiều nguồn và giá trị được chọn không nên dựa vào. Cần unique constraint trên feed hoặc gom/khử trùng với quy tắc rõ. Đây là lỗi cardinality giống `JOIN`, nhưng hậu quả là dữ liệu bị ghi chứ không chỉ báo cáo sai.\n\n### `DELETE`: xóa tập hàng và quan hệ khóa ngoại\n\n```sql\nDELETE FROM orders\nWHERE status = 'draft'\n  AND created_at < current_timestamp - interval '90 days'\nRETURNING order_id;\n```\n\nMVCC thường đánh dấu phiên bản chết; dung lượng không lập tức trả cho hệ điều hành. Autovacuum dọn tuple chết để tái sử dụng và duy trì visibility. Xóa lượng lớn có thể tạo WAL, bloat, giữ khóa lâu và làm replica trễ. Với bảng phân vùng theo thời gian, detach/drop partition đúng phạm vi thường hiệu quả hơn hàng triệu row delete, nhưng đó là thay đổi DDL cần kế hoạch riêng.\n\nKhóa ngoại quyết định con: `RESTRICT/NO ACTION` chặn xóa cha còn tham chiếu; `CASCADE` lan xóa; `SET NULL` giữ con nhưng bỏ liên kết. `CASCADE` tiện nhưng phạm vi có thể lớn bất ngờ, nên phải kiểm đếm và test.\n\n### Soft delete không phải DELETE\n\n`UPDATE products SET deleted_at = now()` giữ lịch sử và khả năng khôi phục. Đổi lại, mọi truy vấn phải lọc hàng đã xóa, unique key có thể cần partial unique index, và bảng tiếp tục phình. Ví dụ cho phép tái dùng email sau khi soft-delete:\n\n```sql\nCREATE UNIQUE INDEX customers_active_email_uq\nON customers (lower(email))\nWHERE deleted_at IS NULL;\n```\n\nSoft delete phù hợp audit/khôi phục; không phù hợp khi luật bảo vệ dữ liệu yêu cầu xóa thật hoặc dữ liệu nhạy cảm không được giữ.\n\n### UPSERT cần một arbiter duy nhất\n\n```sql\nINSERT INTO inventory (sku, quantity, synced_at)\nVALUES ('LAP-14', 12, now())\nON CONFLICT (sku) DO UPDATE\nSET quantity = EXCLUDED.quantity,\n    synced_at = EXCLUDED.synced_at\nRETURNING product_id, quantity;\n```\n\n`EXCLUDED` là hàng đã định chèn nhưng xung đột. Unique index/constraint trên `sku` làm arbiter phát hiện race một cách nguyên tử. Mẫu “SELECT xem có → INSERT, ngược lại UPDATE” có khe thời gian: hai session cùng thấy chưa có rồi cùng insert; một session sẽ lỗi unique, hoặc sinh trùng nếu không có constraint.\n\n### `DO NOTHING` cho idempotency\n\nConsumer có thể nhận lại cùng event. Đặt `event_id` unique:\n\n```sql\nINSERT INTO processed_events(event_id, processed_at)\nVALUES ('evt-839', now())\nON CONFLICT (event_id) DO NOTHING\nRETURNING event_id;\n```\n\nCó hàng trả về nghĩa là lần đầu đã giành quyền xử lý; rỗng nghĩa event đã tồn tại. Tuy nhiên nếu ghi marker rồi gọi dịch vụ ngoài và transaction rollback/timeout, tính đúng đắn end-to-end còn cần outbox/inbox design. UPSERT chỉ bảo vệ state trong database, không biến HTTP bên ngoài thành transaction.\n\n### Conditional UPSERT tránh dữ liệu cũ ghi đè mới\n\nFeed kho có `source_updated_at`. Retry cũ đến sau không được hạ phiên bản:\n\n```sql\nINSERT INTO inventory_feed(sku, quantity, source_updated_at)\nVALUES ('LAP-14', 10, '2026-08-10 10:00+07')\nON CONFLICT (sku) DO UPDATE\nSET quantity = EXCLUDED.quantity,\n    source_updated_at = EXCLUDED.source_updated_at\nWHERE inventory_feed.source_updated_at < EXCLUDED.source_updated_at\nRETURNING sku, quantity;\n```\n\nNếu timestamp không mới hơn, nhánh update không chạy và `RETURNING` rỗng. Cần định nghĩa xử lý timestamp bằng nhau: cùng event, xung đột nguồn hay cho phép thay thế.\n\n### `ON CONFLICT` không phải `MERGE`\n\nUPSERT giải quyết xung đột duy nhất của một insert dự kiến. `MERGE` diễn đạt nhiều nhánh matched/not matched dựa trên nguồn và điều kiện, phù hợp đồng bộ phức tạp. `MERGE` không tự miễn race hoặc lỗi nguồn trùng; semantics và khóa vẫn phải phân tích. Với một natural key và hai hành vi insert/update, `ON CONFLICT` thường ngắn và trực diện hơn.\n\n### Transaction ghép đơn và item\n\nTạo order, item và trừ kho phải cùng transaction nếu nghiệp vụ yêu cầu tất cả hoặc không gì cả:\n\n```sql\nBEGIN;\n-- INSERT order ... RETURNING order_id\n-- INSERT order_items ...\n-- guarded UPDATE inventory ... RETURNING quantity\nCOMMIT;\n```\n\nNếu update kho trả rỗng, ứng dụng phải `ROLLBACK`; không được commit đơn thiếu kho ngoài ý muốn. Constraint nên bảo vệ invariant nội bộ từng bảng, còn transaction bảo vệ invariant xuyên nhiều statement. Thứ tự khóa nhất quán giữa các code path giúp giảm deadlock; deadlock vẫn có thể xảy ra và transaction phải được retry toàn bộ khi phù hợp.\n\n### Query plan của phần tìm hàng\n\nDML cũng có scan plan. `UPDATE orders WHERE status='pending' AND created_at < ...` có thể dùng partial index:\n\n```sql\nCREATE INDEX orders_pending_created_idx\nON orders(created_at)\nWHERE status = 'pending';\n```\n\nIndex nhỏ và đúng predicate giúp tìm hàng hết hạn. Nhưng mỗi lần status vào/ra `pending` phải cập nhật index; batch sửa rất nhiều hàng có thể chọn sequential scan hợp lý. Dùng `EXPLAIN (ANALYZE, BUFFERS)` với DML sẽ thực thi thay đổi thật, nên chỉ chạy trong transaction thử rồi `ROLLBACK`, hoặc dùng `EXPLAIN` không `ANALYZE` trên production.\n\n### Batch lớn, khóa và thời gian đáp ứng\n\nMột statement sửa triệu hàng là nguyên tử nhưng giữ transaction lâu, sinh nhiều WAL và tuple chết. Chia batch giảm thời gian khóa và dễ quan sát, song mất atomicity toàn bộ lô; cần cursor/key range ổn định và thiết kế retry. Không dùng `LIMIT` trực tiếp với `UPDATE` PostgreSQL như `SELECT`; có thể chọn khóa trong CTE rồi update, thường kết hợp `FOR UPDATE SKIP LOCKED` cho nhiều worker. `SKIP LOCKED` phù hợp hàng đợi, không phù hợp báo cáo cần nhìn đủ hàng.\n\n### Ca biên và khi không nên UPSERT\n\nInsert nhiều hàng trong cùng statement có thể tự xung đột cùng key; cần làm sạch nguồn. Update không khớp trả command count 0, không phải lỗi SQL. Gán cột `NULL` có thể vi phạm constraint; phép cộng với `NULL` trả `NULL`. Delete cha có thể cascade ngoài dự tính. Sequence có khoảng trống sau rollback là bình thường và không nên dùng id để đếm số bản ghi.\n\nKhông dùng UPSERT nếu xung đột cần người dùng quyết định merge hai phiên bản, nếu update phải kiểm tra version cũ, hoặc nếu “trùng” không có unique constraint xác định. Đừng dùng `DO UPDATE` vô điều kiện cho retry: nó có thể tạo phiên bản hàng mới, trigger/audit và WAL dù giá trị nghiệp vụ không đổi.",

vi_du: "### Luồng tồn kho chạy được trên PostgreSQL\n\n```sql\nCREATE TEMP TABLE inventory (\n  product_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  sku text NOT NULL UNIQUE,\n  quantity integer NOT NULL CHECK (quantity >= 0),\n  source_updated_at timestamptz NOT NULL\n);\n\nINSERT INTO inventory(sku, quantity, source_updated_at)\nVALUES ('LAP-14', 5, '2026-08-10 09:00+07')\nRETURNING product_id, sku, quantity;\n\nUPDATE inventory\nSET quantity = quantity - 3\nWHERE sku = 'LAP-14' AND quantity >= 3\nRETURNING quantity;\n-- 2\n\nUPDATE inventory\nSET quantity = quantity - 3\nWHERE sku = 'LAP-14' AND quantity >= 3\nRETURNING quantity;\n-- 0 hàng: không bán âm, CHECK không phải tuyến phòng thủ duy nhất\n\nINSERT INTO inventory(sku, quantity, source_updated_at)\nVALUES ('LAP-14', 8, '2026-08-10 10:00+07')\nON CONFLICT (sku) DO UPDATE\nSET quantity = EXCLUDED.quantity,\n    source_updated_at = EXCLUDED.source_updated_at\nWHERE inventory.source_updated_at < EXCLUDED.source_updated_at\nRETURNING sku, quantity;\n-- LAP-14 | 8\n```\n\nGuarded update từ chối lần bán thứ hai bằng 0 hàng thay vì để `CHECK` ném lỗi sau khi quantity thành -1. Cả hai đều bảo vệ invariant, nhưng command count/`RETURNING` cho ứng dụng một nhánh nghiệp vụ dự kiến.\n\n### Rollback thử nghiệm plan DML\n\n```sql\nBEGIN;\nEXPLAIN (ANALYZE, BUFFERS, TIMING OFF)\nUPDATE inventory\nSET quantity = quantity\nWHERE sku = 'LAP-14';\nROLLBACK;\n```\n\nStatement thật sự chạy và lấy khóa trước khi rollback. Không làm cách này trên production nóng chỉ vì “cuối cùng đã rollback”: nó vẫn có thể chặn session khác, chạy trigger và tạo công việc nội bộ. Môi trường staging với phân bố dữ liệu tương tự mới là nơi đo phù hợp.",

so_sanh: "### Bốn thao tác và tín hiệu thành công\n\n| Thao tác | Mục đích | Tín hiệu cần kiểm | Rủi ro nổi bật |\n|---|---|---|---|\n| `INSERT` | tạo hàng | hàng `RETURNING` hoặc lỗi constraint | cột ngầm theo thứ tự, duplicate key |\n| `UPDATE` | biến đổi hàng khớp | command count/`RETURNING` đúng kỳ vọng | thiếu `WHERE`, lost update, bloat |\n| `DELETE` | loại hàng | số hàng xóa và tác động khóa ngoại | cascade lớn, WAL, không giải phóng file ngay |\n| `ON CONFLICT` | chèn hoặc xử lý key trùng | nhánh có/không có hàng trả về | arbiter sai, ghi đè dữ liệu mới bằng retry cũ |\n\n### Read-modify-write và guarded update\n\n| Cách | Round trip | Cửa sổ race | Cách báo thiếu kho |\n|---|---:|---|---|\n| `SELECT quantity`, tính ở app, rồi `UPDATE` | ít nhất 2 | có nếu không khóa/kiểm version | logic ứng dụng dễ dựa trên giá trị cũ |\n| `SELECT ... FOR UPDATE` rồi `UPDATE` | ít nhất 2 | khóa hàng đóng cửa sổ | rõ nhưng giữ khóa qua thời gian xử lý |\n| `UPDATE ... WHERE quantity >= n RETURNING` | 1 | điều kiện và ghi trong một statement | 0 hàng, gọn cho phép biến đổi đơn giản |",

loi_thuong_gap: [
  "Chạy `UPDATE` hoặc `DELETE` thiếu `WHERE`; command count tăng bất thường, cách phòng vệ là transaction, review predicate và kiểm số hàng trước khi commit với thao tác thủ công.",
  "Dùng `SELECT max(id)` sau insert; concurrency có thể trả id hàng khác, phải dùng `RETURNING`.",
  "Thực hiện check-then-insert bằng hai statement; lỗi unique xuất hiện ngẫu nhiên dưới tải, cần unique constraint cộng `ON CONFLICT` hoặc xử lý lỗi có chủ đích.",
  "Không kiểm số hàng của optimistic update; request báo thành công dù version đã stale, cần coi 0 hàng là xung đột.",
  "`UPDATE ... FROM` gặp nhiều nguồn cho một đích; dữ liệu chọn không xác định theo ý nghiệp vụ, phải cưỡng chế/khử trùng nguồn.",
  "Xem soft delete là miễn phí; truy vấn quên `deleted_at IS NULL`, unique key và kích thước bảng đều gây lỗi lâu dài.",
  "Tin rằng rollback làm `EXPLAIN ANALYZE UPDATE` vô hại trên production; statement vẫn thực thi, khóa và có thể kích hoạt trigger trước rollback.",
  "UPSERT retry cũ ghi đè bản mới; cần version/timestamp trong `WHERE` của `DO UPDATE` hoặc một quy tắc giải quyết xung đột rõ."
],

tu_kiem_tra: [
  "Vì sao một `INSERT` nhiều hàng có một hàng vi phạm constraint thường không để lại các hàng trước nó?",
  "Thiết kế phép trừ tồn kho 3 sản phẩm sao cho hai session không bán âm; ứng dụng phải hiểu 0 hàng trả về thế nào?",
  "So sánh optimistic locking bằng `version` với `SELECT FOR UPDATE`: thời gian giữ khóa và trải nghiệm xung đột khác nhau ra sao?",
  "Tại sao unique constraint là thành phần bắt buộc của UPSERT an toàn chứ không chỉ là tối ưu index?",
  "Một feed gửi bản ghi timestamp cũ sau bản mới. Viết điều kiện `DO UPDATE WHERE` ngăn ghi đè và quyết định cách xử lý timestamp bằng nhau.",
  "Giải thích vì sao delete một triệu hàng không lập tức làm file bảng nhỏ lại, và autovacuum đóng vai trò gì.",
  "Khi partial index cho đơn `pending` giúp update hết hạn, và chi phí nào xuất hiện lúc status thay đổi?",
  "Nêu một tình huống `DO NOTHING` chưa đủ bảo đảm exactly-once vì quy trình còn gọi dịch vụ ngoài database."
],

tai_nguyen: [
  "PostgreSQL Documentation, các phần INSERT, UPDATE, DELETE, RETURNING và ON CONFLICT.",
  "PostgreSQL Documentation, các chương Concurrency Control, Explicit Locking và Routine Vacuuming.",
  "“Designing Data-Intensive Applications” của Martin Kleppmann, các phần transaction, concurrency và xử lý sự kiện idempotent.",
  "“PostgreSQL: Up and Running” của Regina Obe và Leo Hsu, các chương thao tác dữ liệu và quản trị bảng."
],

lien_he: [
  "Nối sang mảng Hệ điều hành qua WAL và fsync: `COMMIT` bền vững cần nhật ký được ghi theo chính sách durability, không chỉ sửa trang dữ liệu trong RAM.",
  "Nối sang mảng Hệ phân tán qua idempotency: unique event key chặn xử lý lặp trong một database, còn hiệu ứng qua HTTP cần outbox/inbox hoặc giao thức phối hợp.",
  "Nối sang mảng Backend qua optimistic concurrency và HTTP: version mismatch thường ánh xạ thành phản hồi xung đột thay vì âm thầm ghi đè resource.",
  "Nối sang mảng Bảo mật: mọi giá trị DML từ người dùng phải truyền bằng parameterized query; quyền database nên giới hạn bảng/cột/thao tác mà service thật sự cần.",
  "Nối sang mảng Kiểm thử qua concurrency: test tuần tự không phát hiện lost update, nên cần chạy hai transaction xen kẽ và xác minh invariant tồn kho sau cả hai."
]

};
