window.LESSONS["05:thiet-ke-api:so-sanh-rest-vs-graphql-vs-grpc-vs-websocket"] = {

tom_tat: [
  "REST, GraphQL, gRPC và WebSocket không phải bốn phiên bản nhanh/chậm của cùng một thứ: chúng tối ưu cho các hình dạng tương tác, contract và hạ tầng khác nhau.",
  "REST phù hợp tài nguyên và HTTP semantics; GraphQL phù hợp giao diện cần chọn dữ liệu linh hoạt; gRPC mạnh ở RPC nội bộ có schema chặt; WebSocket dành cho kênh hai chiều sống lâu và sự kiện cần đẩy tức thời.",
  "Một hệ thống có thể phối hợp cả bốn: Rạp Mây dùng REST để đặt vé, GraphQL cho màn hình khám phá, gRPC gọi thanh toán nội bộ và WebSocket phát thay đổi trạng thái ghế.",
  "Bảo mật phải bám operation và object, không dừng ở kết nối: GraphQL cần giới hạn độ phức tạp, gRPC cần danh tính service, WebSocket cần authorize từng subscription/message.",
  "Đừng chọn bằng benchmark payload nhỏ hoặc xu hướng; hãy đo round-trip, số byte, khả năng cache, backpressure, debugging, compatibility và chi phí vận hành trên workload thật."
],

can_biet_truoc: [
  "HTTP method, status code, header, caching và mô hình request-response.",
  "JSON schema/type, authentication, authorization và nguyên tắc không tin đầu vào client.",
  "Khái niệm RPC, serialization, connection sống lâu, event và backpressure.",
  "Ba bài trước về thiết kế tài nguyên, idempotency/retry và OpenAPI contract."
],

dinh_nghia: "**REST** là phong cách kiến trúc tổ chức API quanh tài nguyên và semantics chuẩn của HTTP. **GraphQL** là ngôn ngữ truy vấn cùng runtime để client yêu cầu chính xác các field trên một schema kiểu hóa. **gRPC** là framework RPC dùng service/method được định nghĩa bằng Protocol Buffers và thường truyền dữ liệu nhị phân qua HTTP/2. **WebSocket** là protocol nâng cấp từ HTTP để duy trì một kết nối song công, cho phép hai phía gửi frame bất cứ lúc nào.\n\nSo sánh đúng phải hỏi “tương tác nào?” thay vì “công nghệ nào thắng?”. Tạo booking có một command rõ và cần idempotency; màn hình lịch chiếu cần ghép dữ liệu linh hoạt; service booking gọi payment theo contract nội bộ; bản đồ ghế cần server đẩy thay đổi. Bốn bài toán có thể tồn tại trong cùng Rạp Mây mà không mâu thuẫn.",

vi_sao_can: "Chọn sai abstraction tạo chi phí kéo dài hơn vài mili giây. Dùng WebSocket cho CRUD khiến retry, cache và quan sát phải tự phát minh. Dùng REST cho màn hình cần mười tài nguyên nhỏ có thể tạo nhiều round-trip. Mở GraphQL không giới hạn cho Internet biến một request thành hàng nghìn truy vấn database. Ép browser gọi gRPC thuần gặp giới hạn tương thích và proxy.\n\nQuyết định còn ảnh hưởng tổ chức: ai sở hữu schema, cách rollout client cũ, công cụ debug, load balancer, rate limit và audit. Vì vậy bài này không trao cúp “nhanh nhất”. Nó dựng một bản đồ đánh đổi, dùng cùng hành trình đặt vé để thấy có lúc giải pháp tốt là một giao thức, có lúc là tổ hợp, và có lúc REST đơn giản vẫn tốt hơn thêm một platform mới.",

co_che: "### Bắt đầu từ hình dạng cuộc hội thoại\n\nTrước khi chọn công nghệ, viết sequence bằng lời: ai khởi tạo, mỗi bên gửi bao nhiêu thông điệp, kết nối sống bao lâu, client có cần chọn field, dữ liệu có cache được, và mất kết nối thì khôi phục từ đâu. Request-response ngắn khác streaming; truy vấn khác command; public browser khác service trong datacenter.\n\nRạp Mây có bốn luồng. `POST /bookings` là command ngắn, hậu quả quan trọng. Trang chủ cần phim, suất, rạp và poster theo nhiều bố cục. Booking service gọi Payment service với contract do hai đội kiểm soát. Sơ đồ ghế phải nhận sự kiện “B12 vừa được giữ” mà không poll mỗi giây. Phân rã này đã gợi ý giải pháp trước khi nói tên protocol.\n\n### REST tận dụng semantics của HTTP\n\nREST biểu diễn danh từ thành URL và dùng method để nói ý định: `GET /shows`, `POST /bookings`, `GET /bookings/{id}`. Status, header, conditional request và cache đã có ý nghĩa chung cho browser, CDN, proxy và công cụ vận hành. Một `GET` có ETag có thể được cache/revalidate mà application không dựng protocol riêng.\n\nĐiểm mạnh không phải JSON; REST có thể dùng media type khác. Giá trị nằm ở uniform interface: người vận hành nhìn method/path/status đã hiểu phần lớn cuộc gọi. Idempotency của `PUT`/`DELETE` và safe semantics của `GET` tạo nền cho retry, dù implementation vẫn phải đúng contract.\n\nREST yếu khi một màn hình cần graph dữ liệu có hình dạng thay đổi. Endpoint quá thô gây over-fetching; endpoint quá nhỏ gây nhiều round-trip; endpoint theo từng màn hình làm tài nguyên mất tính khái quát. Có thể giải bằng query parameter, compound document hoặc backend-for-frontend trước khi kết luận phải đổi công nghệ.\n\n### GraphQL chuyển quyền chọn field cho client\n\nGraphQL thường dùng một HTTP endpoint nhưng operation trong body nói field và quan hệ cần lấy. Client mobile có thể lấy tên phim cùng ba suất gần nhất; web desktop lấy thêm poster và đánh giá, đều trên một schema. Schema và introspection hỗ trợ tooling mạnh; deprecation ở cấp field giúp tiến hóa dần.\n\nSự linh hoạt chuyển chi phí sang server. Một query ngắn về chữ có thể rất đắt nếu lồng danh sách qua nhiều tầng. HTTP status thường vẫn 200 khi response có cả `data` và `errors`, nên monitoring chỉ đếm 5xx sẽ bỏ sót lỗi nghiệp vụ. Cache CDN theo URL khó hơn, dù persisted query và cache phía client có thể khắc phục một phần.\n\nGraphQL không tự giải N+1. Resolver `shows` lấy 20 suất rồi mỗi suất gọi database lấy rạp tạo 21 query. DataLoader hoặc batch query gom khóa trong một request, nhưng phải giữ đúng phạm vi để không cache dữ liệu giữa user. Authorization phải áp tại resolver/service theo object và field; kiểm token ở cửa vào chưa ngăn user đọc booking người khác qua quan hệ lồng.\n\n### gRPC coi lời gọi mạng như method có schema\n\nFile `.proto` định nghĩa service, method và message với field number ổn định. Code generator tạo client/server type-safe cho nhiều ngôn ngữ. Dữ liệu Protocol Buffers nhỏ gọn và parse hiệu quả; HTTP/2 hỗ trợ multiplexing cùng streaming. Trong mạng nội bộ có hạ tầng kiểm soát, đây là nền tốt cho Booking gọi Payment.\n\nNhưng lời gọi mạng không trở thành lời gọi hàm cục bộ. Nó vẫn timeout, mất kết nối, trả kết quả mơ hồ và cần idempotency. Deadline phải truyền qua chuỗi service; nếu upstream còn 100 ms mà downstream chạy 2 giây, công việc vô ích tiếp tục sau khi người dùng đã bỏ cuộc. Retry policy phải biết method nào an toàn.\n\nField number trong protobuf là danh tính trên wire: xóa field không được tái sử dụng số cho nghĩa mới. Thêm field thường tương thích vì reader cũ bỏ qua field lạ, nhưng đổi semantics vẫn nguy hiểm. Error status của gRPC cần mapping có kỷ luật; đừng nhét mọi lỗi nghiệp vụ vào `INTERNAL` hoặc chuỗi không cấu trúc.\n\n### WebSocket là kênh, không phải mô hình dữ liệu\n\nSau handshake HTTP, WebSocket giữ TCP connection và hai bên gửi frame độc lập. Server có thể đẩy `seat.held` ngay khi ghế thay đổi, giảm độ trễ và tránh polling liên tục. Protocol chỉ cho kênh; đội vẫn phải định nghĩa message type, schema, version, correlation ID, ordering và error.\n\nKết nối sống lâu tạo state vận hành: heartbeat để phát hiện peer chết, giới hạn số connection, buffer và backpressure, reconnect, resubscribe. Load balancer phải hỗ trợ upgrade và timeout phù hợp. Nếu server giữ subscription trong RAM, deploy/restart làm mất chúng; hệ thống nhiều instance cần pub/sub hoặc routing sự kiện tới instance có client.\n\nKhông đảm bảo mọi event đến đúng một lần. Mất mạng có thể làm client bỏ lỡ ghế B12 chuyển từ free sang held. Message nên có `show_id`, `seat_id`, state và sequence/version; reconnect gửi `last_seen_version` hoặc client tải snapshot REST rồi tiếp tục stream. Snapshot cộng delta dễ phục hồi hơn hy vọng connection không bao giờ đứt.\n\n### Cùng hệ thống, bốn contract khác nhau\n\nREST/OpenAPI mô tả resource operation. GraphQL SDL mô tả type, field, argument và operation. gRPC dùng `.proto`. WebSocket cần message schema cùng state machine riêng; OpenAPI không bao trọn cuộc hội thoại ấy. Dùng một business service bên dưới giúp invariant đặt vé không bị nhân bốn, nhưng adapter phải chuyển lỗi và danh tính chính xác.\n\nKhông nên mở bốn đường tạo booking độc lập. Rạp Mây chỉ cho command đặt vé qua REST public; GraphQL có thể gọi cùng application service nếu thật sự cần mutation; gRPC chỉ nội bộ; WebSocket chỉ thông báo, không quyết định quyền sở hữu ghế. Thu hẹp write path làm audit và idempotency dễ hơn.\n\n### Authentication và authorization theo từng mô hình\n\nREST thường xác thực bearer token mỗi request; stateless thuận revoke/scale nhưng tốn verification. GraphQL xác thực request rồi vẫn authorize từng field/object. gRPC dùng TLS/mTLS hoặc token metadata và cần phân biệt service identity với end-user identity được chuyển tiếp. WebSocket xác thực lúc handshake chưa đủ cho connection dài: token có thể hết hạn, subscription có thể đổi quyền và từng message cần validation.\n\nĐừng đặt access token trong query string WebSocket vì URL dễ vào log/history. Browser API hạn chế custom header khi tạo WebSocket, nên có thể dùng secure cookie với kiểm tra Origin/CSRF phù hợp, hoặc gửi token ngắn hạn trong message đầu qua TLS. Subprotocol không nên bị lạm dụng để nhét secret. Luôn giới hạn kích thước frame và tốc độ message trước khi parse sâu.\n\n### Kiểm soát tài nguyên và chống abuse\n\nREST rate limit theo principal, route và cost tương đối dễ. GraphQL không thể chỉ đếm request: cần giới hạn depth, số node, complexity hoặc persisted operation; pagination bắt buộc cho list. Alias có thể gọi cùng field nhiều lần, fragment có thể làm query nở, nên parser phải áp giới hạn trước resolver.\n\ngRPC cần giới hạn message size, concurrent stream và deadline. Streaming không có backpressure đúng sẽ đầy bộ nhớ khi producer nhanh hơn consumer. WebSocket cần quota connection/user, subscription, message/giây và outbound queue; client chậm phải bị ngắt hoặc giảm dữ liệu thay vì giữ buffer vô hạn.\n\n### Cache và hiệu năng không có người thắng tuyệt đối\n\nREST GET khớp CDN/cache HTTP tự nhiên. GraphQL query qua POST thường cần cache theo normalized query, variables và identity; persisted query giúp biến operation thành ID. gRPC binary tiết kiệm byte/CPU nhưng proxy quan sát khó hơn và latency mạng/database thường lớn hơn serialization. WebSocket tránh handshake lặp lại nhưng giữ tài nguyên connection và không làm truy vấn database tự nhanh.\n\nBenchmark “hello world” chủ yếu đo framework/serialization, không trả lời workload. Hãy đo end-to-end p50/p95/p99, số round-trip, byte vào/ra, query database, CPU parse, connection memory và recovery sau lỗi. Nếu GraphQL giảm sáu REST round-trip thành một nhưng resolver tạo 200 SQL query, tối ưu bề mặt đã làm backend tệ hơn.\n\n### Compatibility và rollout client cũ\n\nREST thường version URL/header hoặc tiến hóa additive. GraphQL tránh version toàn API bằng thêm field và deprecate, nhưng field cũ phải tồn tại đến khi consumer rời đi. gRPC giữ field number, thêm method/message tương thích và rollout server trước client khi cần. WebSocket message cần `type` cùng version hoặc envelope để hai phía thương lượng.\n\nCompatibility là hành vi, không chỉ schema. Đổi ordering, default, timezone hoặc ý nghĩa null có thể phá client mà type không đổi. Mobile tồn tại ngoài quyền kiểm soát nhiều tháng; service nội bộ có thể rollout nhanh hơn nhưng vẫn có phiên bản cũ trong lúc rolling deployment.\n\n### Error và observability\n\nREST có status chuẩn nhưng domain error vẫn cần body code ổn định. GraphQL có thể partial success; metric nên đếm error theo path/code, không chỉ HTTP. gRPC interceptor ghi status, deadline và method; protobuf payload không nên log toàn bộ vì dữ liệu nhạy cảm. WebSocket cần correlation ID và event lag; một connection đóng không nói message cuối đã được client xử lý.\n\nTrace xuyên protocol phải truyền trace context có kiểm soát. Cardinality của path GraphQL nếu dùng raw query có thể làm nổ metric; chuẩn hóa theo operation name/persisted ID. WebSocket span kéo dài hàng giờ ít hữu ích, nên tạo span cho handshake và message quan trọng, metric cho số connection/queue/lag.\n\n### Retry, delivery và idempotency\n\nREST command dùng idempotency key như bài trước. GraphQL mutation cũng cần cơ chế tương đương trong input/header; tên “mutation” không làm nó đúng một lần. gRPC client có thể retry transparent hoặc theo policy, nên method tạo charge phải có request ID deduplicate phía server. WebSocket reconnect có thể gửi lại message cuối; message command cần ID và ack.\n\nQuery/read có thể retry dễ hơn nhưng vẫn phải tôn trọng deadline và consistency. Subscription mất kết nối cần cursor/version. Không tầng transport nào cung cấp exactly-once effect xuyên database và payment provider; protocol tốt chỉ cho primitives để ứng dụng xây invariant.\n\n### Khi nào không nên dùng từng lựa chọn\n\nKhông dùng GraphQL chỉ vì frontend muốn “một endpoint” nếu dữ liệu đơn giản, cache CDN quan trọng và đội chưa sẵn sàng quản complexity/N+1. Không dùng gRPC public cho browser khi phải dựng translation layer mà lợi ích không bù vận hành. Không dùng WebSocket cho thông báo thưa, chấp nhận trễ; Server-Sent Events hoặc polling có thể đơn giản hơn. Không bẻ REST thành giáo điều nếu màn hình graph thay đổi thực sự gây round-trip lớn.\n\nCũng đừng triển khai cả bốn trong monolith nhỏ để chứng minh kiến trúc. Mỗi stack thêm auth adapter, schema registry, test, dashboard, thư viện client và kỹ năng on-call. Bắt đầu với giao thức đơn giản nhất đáp ứng contract; thêm lựa chọn khi số đo hoặc nhu cầu consumer chỉ ra khoảng trống cụ thể.\n\n### Quyết định cho Rạp Mây\n\nPublic command đặt vé dùng REST vì semantics/status/idempotency rõ và dễ audit. Trang khám phá chỉ chuyển sang GraphQL nếu nhiều client thật sự cần shape khác; nếu hai màn hình cố định, REST/BFF đủ. Booking-to-Payment dùng gRPC khi có nhiều lời gọi nội bộ, đa ngôn ngữ và hạ tầng HTTP/2/mTLS; giai đoạn đầu cùng process không cần network RPC. Ghế thời gian thực dùng WebSocket với snapshot REST, version và resubscribe.\n\nQuyết định này không tạo bốn nguồn sự thật. Inventory/booking database vẫn phân xử ghế; event sau commit phát qua outbox; WebSocket chỉ phản chiếu. OpenAPI, GraphQL schema, `.proto` và message schema được version/test riêng, còn invariant nghiệp vụ có một implementation dùng chung.",

vi_du: "### REST và WebSocket trong FastAPI\n\nỨng dụng sau chạy bằng Uvicorn, minh họa command REST và kênh thông báo. Kho nhớ không an toàn cho nhiều worker; production thay bằng database cùng pub/sub/outbox.\n\n```python\nfrom collections import defaultdict\nfrom fastapi import FastAPI, Header, HTTPException, WebSocket, WebSocketDisconnect\nfrom pydantic import BaseModel, Field\n\napp = FastAPI()\nbooked: dict[tuple[str, str], dict] = {}\nidem: dict[str, dict] = {}\nlisteners: dict[str, set[WebSocket]] = defaultdict(set)\n\nclass BookingIn(BaseModel):\n    show_id: str = Field(min_length=1)\n    seat_id: str = Field(pattern=r\"^[A-Z][0-9]{1,2}$\")\n\nasync def publish(show_id: str, event: dict):\n    dead = []\n    for ws in listeners[show_id].copy():\n        try:\n            await ws.send_json(event)\n        except Exception:\n            dead.append(ws)\n    for ws in dead:\n        listeners[show_id].discard(ws)\n\n@app.post(\"/bookings\", status_code=201)\nasync def create_booking(body: BookingIn,\n                         idempotency_key: str = Header(min_length=16)):\n    if idempotency_key in idem:\n        return idem[idempotency_key]\n    seat = (body.show_id, body.seat_id)\n    if seat in booked:\n        raise HTTPException(409, \"Ghế đã được đặt\")\n    result = {\"booking_id\": f\"bk-{len(booked)+1}\", **body.model_dump()}\n    booked[seat] = result\n    idem[idempotency_key] = result\n    await publish(body.show_id, {\n        \"type\": \"seat.changed\", \"version\": len(booked),\n        \"seat_id\": body.seat_id, \"state\": \"BOOKED\",\n    })\n    return result\n\n@app.websocket(\"/ws/shows/{show_id}\")\nasync def seat_stream(ws: WebSocket, show_id: str):\n    token = ws.query_params.get(\"ticket\")\n    if token != \"short-lived-demo-ticket\":\n        await ws.close(code=1008)\n        return\n    await ws.accept()\n    listeners[show_id].add(ws)\n    try:\n        await ws.send_json({\"type\": \"ready\", \"show_id\": show_id})\n        while True:\n            message = await ws.receive_json()\n            if message.get(\"type\") == \"ping\":\n                await ws.send_json({\"type\": \"pong\"})\n    except WebSocketDisconnect:\n        listeners[show_id].discard(ws)\n```\n\nMở WebSocket rồi tạo booking sẽ nhận `seat.changed`. Demo chỉ ra ranh giới rõ: REST quyết định và trả lỗi; WebSocket phát delta. Race condition vẫn tồn tại trong dictionary, version toàn cục không đúng cho từng show, query token có thể vào log và event có thể mất giữa ghi booking với publish. Production sửa bằng transaction/UNIQUE, outbox, version theo show, ticket một lần đã che khỏi log và snapshot khi reconnect.\n\n### GraphQL-style query với Strawberry và FastAPI\n\nĐây là ví dụ chạy được khi cài `strawberry-graphql[fastapi]`. Resolver batch đơn giản chưa cần DataLoader vì dữ liệu nhớ nhỏ.\n\n```python\nimport strawberry\nfrom strawberry.fastapi import GraphQLRouter\n\n@strawberry.type\nclass Show:\n    id: str\n    title: str\n    available_seats: int\n\nSHOWS = [\n    Show(id=\"s1\", title=\"Mưa trên phố\", available_seats=42),\n    Show(id=\"s2\", title=\"Dòng sông đêm\", available_seats=0),\n]\n\n@strawberry.type\nclass Query:\n    @strawberry.field\n    def shows(self, only_available: bool = False) -> list[Show]:\n        rows = SHOWS\n        return [s for s in rows if s.available_seats > 0] if only_available else rows\n\nschema = strawberry.Schema(query=Query)\napp.include_router(GraphQLRouter(schema), prefix=\"/graphql\")\n```\n\nClient có thể hỏi `{ shows(onlyAvailable: true) { title availableSeats } }` mà không nhận ID. Khi chuyển sang database, phải pagination, batch quan hệ và đặt complexity limit. Đừng đưa mutation đặt vé vào chỉ để “mọi thứ GraphQL”; nếu thêm, nó vẫn phải gọi cùng service idempotent và trả domain error có kiểu.\n\n### Contract gRPC cho thanh toán nội bộ\n\n```proto\nsyntax = \"proto3\";\npackage rapmay.payment.v1;\n\nservice PaymentService {\n  rpc Authorize(AuthorizeRequest) returns (AuthorizeResponse);\n}\n\nmessage AuthorizeRequest {\n  string request_id = 1;\n  string booking_id = 2;\n  int64 amount_minor = 3;\n  string currency = 4;\n}\n\nmessage AuthorizeResponse {\n  string payment_id = 1;\n  enum Status { STATUS_UNSPECIFIED = 0; AUTHORIZED = 1; DECLINED = 2; }\n  Status status = 2;\n}\n```\n\n`request_id` là khóa deduplicate, `amount_minor` tránh số thực cho tiền, enum có giá trị 0 không xác định để phát hiện thiếu dữ liệu. Server không tin booking service tuyệt đối chỉ vì dùng protobuf: mTLS xác minh service, authorization giới hạn method, deadline chặn công việc quá hạn và payment vẫn đối chiếu amount/currency theo contract.\n\n### Ca biên cần chạy\n\nThử danh sách show rỗng, query GraphQL yêu cầu list không giới hạn, protobuf nhận enum chưa biết, WebSocket client chậm không đọc, reconnect sau khi bỏ lỡ ba version, hai REST request tranh cùng ghế và token hết hạn giữa connection. Quan sát không chỉ response: đếm SQL query, outbound queue, connection, deadline exceeded, event lag và booking duplicate.",

so_sanh: "### Ma trận lựa chọn theo workload\n\n| Trục | REST | GraphQL | gRPC | WebSocket |\n|---|---|---|---|---|\n| Hình dạng chính | Request-response theo tài nguyên | Client chọn graph field | RPC/stream có schema | Kênh song công sống lâu |\n| Contract | OpenAPI thường dùng | GraphQL SDL | Protocol Buffers | Tự định nghĩa message/state machine |\n| Cache trung gian | Rất thuận với GET | Cần chiến lược riêng | Ít dựa CDN | Không theo cache HTTP |\n| Browser | Tự nhiên | Tự nhiên | Thường cần gRPC-Web/gateway | Tự nhiên nhưng vận hành connection |\n| Rủi ro bảo mật nổi bật | Broken object authorization | Query abuse, field authorization | Service identity, deadline | Connection/message abuse |\n| Điểm vận hành khó | Endpoint/version proliferation | N+1 và complexity | Proxy/debug binary | Reconnect, backpressure, fan-out |\n| Rạp Mây dùng cho | Đặt/tra booking | Khám phá tùy biến | Booking gọi Payment | Trạng thái ghế tức thời |\n\nNếu chỉ có CRUD public và một frontend, REST thắng nhờ ít thành phần chứ không phải luôn nhanh hơn. GraphQL đáng dùng khi nhiều client chứng minh nhu cầu shape khác và đội sở hữu platform. gRPC đáng dùng trong service mesh có schema đa ngôn ngữ, nhưng không nên tạo microservice chỉ để dùng nó. WebSocket đáng chi phí khi server push thường xuyên và độ trễ quan trọng; thông báo mỗi giờ có thể dùng polling/SSE đơn giản hơn.",

loi_thuong_gap: [
  "Chọn công nghệ theo kích thước payload hello-world; production lại bị database, round-trip và authorization chi phối.",
  "Cho GraphQL query lồng/list không giới hạn; CPU hoặc số SQL query tăng mạnh dù rate limiter chỉ thấy một request.",
  "Viết resolver từng hàng gây N+1; log SQL cho thấy một query danh sách kéo theo hàng chục query giống nhau.",
  "Coi gRPC như lời gọi local và không truyền deadline/idempotency key; request upstream đã timeout nhưng downstream vẫn thu tiền.",
  "Tái sử dụng field number protobuf đã xóa; client cũ diễn giải byte mới theo nghĩa dữ liệu cũ.",
  "Xác thực WebSocket một lần rồi tin mọi subscription; user đổi `show_id`/booking ID để đọc dữ liệu không có quyền.",
  "Không có snapshot/version khi reconnect; sơ đồ ghế hiển thị stale sau một đoạn mất mạng mà không tự nhận biết.",
  "Duy trì bốn write path độc lập; cùng một invariant ghế được triển khai khác nhau và audit không ghép được giao dịch."
],

tu_kiem_tra: [
  "Vì sao “gRPC dùng binary nên luôn nhanh nhất” là kết luận thiếu những biến số nào?",
  "Một GraphQL response HTTP 200 chứa `errors` ảnh hưởng monitoring và retry ra sao?",
  "Thiết kế cách ngăn N+1 và query abuse cho màn hình phim → suất → rạp → ghế.",
  "Khi WebSocket reconnect từ version 105 nhưng server chỉ giữ event từ 110, client phục hồi đúng bằng quy trình nào?",
  "Vì sao mutation GraphQL và RPC `Authorize` vẫn cần idempotency dù tên operation thể hiện rõ ý định?",
  "Hãy đề xuất rollout khi thêm enum protobuf và field GraphQL mà vẫn có client cũ không biết giá trị mới.",
  "Nếu Rạp Mây chỉ có một monolith và một web frontend, lựa chọn tối thiểu nào hợp lý và điều kiện đo được nào biện minh thêm giao thức thứ hai?"
],

tai_nguyen: [
  "RESTful Web APIs — Leonard Richardson, Mike Amundsen và Sam Ruby.",
  "Production Ready GraphQL — Marc-André Giroux.",
  "gRPC: Up and Running — Kasun Indrasiri và Danesh Kuruppu.",
  "Designing Data-Intensive Applications — Martin Kleppmann."
],

lien_he: [
  "Nối sang mảng Hệ thống phân tán qua timeout, deadline, delivery trùng và backpressure: đổi transport không xóa các failure mode của mạng.",
  "Nối sang mảng Bảo mật qua object-level authorization, mTLS và chống abuse: mỗi mô hình mở một bề mặt tấn công khác nhau.",
  "Nối sang mảng Cơ sở dữ liệu qua N+1, transaction và outbox: hiệu năng/gurantee cuối cùng vẫn phụ thuộc cách truy cập và commit dữ liệu.",
  "Nối sang mảng Kiến trúc phần mềm qua backend-for-frontend và microservices: chọn protocol phải theo ranh giới sở hữu, không phải lý do để chia service.",
  "Nối sang mảng DevOps qua proxy, tracing và capacity planning: connection dài, HTTP/2 và query động cần dashboard cùng giới hạn khác nhau."
]

};
