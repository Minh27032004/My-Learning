window.LESSONS["02:bai-toan-kinh-dien-nen-biet:kmp-rabin-karp-so-khop-chuoi"] = {

tom_tat: [
  "KMP tránh so lại ký tự đã biết bằng bảng LPS; Rabin–Karp dùng hash cửa sổ để loại nhanh phần lớn vị trí rồi mới xác nhận ký tự.",
  "KMP bảo đảm O(n + m) thời gian và O(m) bộ nhớ; Rabin–Karp trung bình O(n + m), nhưng có thể O(nm) nếu nhiều va chạm hash.",
  "Hai thuật toán trả vị trí bắt đầu của mọi lần xuất hiện, kể cả các lần chồng lấn; mẫu rỗng phải được định nghĩa trước.",
  "Trong pipeline sự kiện tìm kiếm, ta dò cụm chuẩn hóa như `iphone` trong nội dung truy vấn mà không cắt chuỗi thành vô số bản sao.",
  "KMP thích hợp cho một mẫu cần bảo đảm; Rabin–Karp hấp dẫn khi quét nhiều mẫu cùng độ dài hoặc xử lý cửa sổ liên tục."
],

can_biet_truoc: [
  "Chuỗi, chỉ số và lát cắt trong Python.",
  "Độ phức tạp trường hợp xấu nhất và chi phí O(m) của so sánh hai chuỗi dài m.",
  "Hash, phép modulo và khái niệm va chạm hash.",
  "Invariant vòng lặp và đoạn đầu–đoạn hậu của chuỗi."
],

dinh_nghia: "**So khớp chuỗi** tìm mọi chỉ số i sao cho `text[i:i+m] == pattern`, với n là độ dài văn bản và m là độ dài mẫu. KMP tiền xử lý cấu trúc tự lặp của mẫu thành bảng **LPS** (longest proper prefix which is also suffix: đoạn đầu thực sự dài nhất đồng thời là đoạn cuối), nhờ đó khi lệch nó dịch mẫu mà không lùi con trỏ văn bản. Rabin–Karp biểu diễn mỗi cửa sổ độ dài m bằng rolling hash (hash cập nhật trượt); hash khác thì chắc chắn không khớp, hash bằng vẫn phải so ký tự vì va chạm tồn tại.",

vi_sao_can: "Pipeline thương mại điện tử nhận các truy vấn như `ốp iphone 15`, `iphoneiphone` và `điện thoại`. Ta muốn gắn nhãn truy vấn chứa mẫu `iphone`, kể cả xuất hiện chồng lấn, trên luồng log rất dài. Cách thử mẫu tại mọi vị trí dễ viết nhưng dữ liệu lặp như `aaaaaaaaab` đối đầu mẫu `aaaab` buộc so lại cùng ký tự nhiều lần, đạt O(nm).\n\nKMP khai thác chính phần đã khớp để biết lần thử tiếp theo; Rabin–Karp biến so sánh dài thành so sánh hai số trong đa số cửa sổ. Chúng quan trọng không chỉ vì tốc độ: quá trình xây LPS dạy cách giữ thông tin đủ dùng sau thất bại, còn rolling hash dạy rằng hash là bộ lọc xác suất chứ không phải bằng chứng bằng nhau.",

co_che: "### Baseline và nơi công việc bị lặp\n\nNaive matching thử i từ 0 đến n−m và so mẫu từ trái sang phải. Nếu lệch ở ký tự cuối, lần thử kế tiếp lại so gần toàn bộ đoạn vừa biết. Trên văn bản gồm nhiều `a` và mẫu `aaaaab`, tổng số so sánh tiến gần (n−m+1)m. Bộ nhớ chỉ O(1), và với mẫu rất ngắn baseline có thể nhanh do code đơn giản; Big-O không tự động biến KMP thành lựa chọn nhanh nhất cho mọi đầu vào.\n\n### LPS mã hóa điều gì\n\n`lps[j]` là độ dài đoạn đầu thực sự dài nhất của `pattern[0:j+1]` cũng là đoạn cuối của chính đoạn ấy. “Thực sự” nghĩa là ngắn hơn cả chuỗi. Với mẫu `ababaca`, bảng là `[0, 0, 1, 2, 3, 0, 1]`. Giá trị 3 tại vị trí 4 nói rằng `aba` vừa mở đầu vừa kết thúc `ababa`.\n\n| j | đoạn mẫu | LPS | lý do |\n|---:|---|---:|---|\n| 0 | a | 0 | không có đoạn đầu thực sự |\n| 1 | ab | 0 | đầu `a` khác cuối `b` |\n| 2 | aba | 1 | `a` trùng `a` |\n| 3 | abab | 2 | `ab` trùng `ab` |\n| 4 | ababa | 3 | `aba` trùng `aba` |\n| 5 | ababac | 0 | ký tự c phá mọi biên |\n| 6 | ababaca | 1 | còn biên `a` |\n\n### Xây LPS mà không quay về đầu\n\nGiữ `length` là độ dài biên tốt nhất của đoạn kết thúc trước i. Nếu `pattern[i] == pattern[length]`, biên dài thêm một. Nếu lệch và length > 0, không đặt ngay về 0; thử biên ngắn hơn `lps[length-1]`. Biên của một biên vẫn là ứng viên hợp lệ. i chỉ tăng khi đã quyết định được `lps[i]`, còn length có thể lùi nhiều bước. Mỗi lần lùi loại bỏ một mức đã từng tăng, nên tổng thời gian O(m), không phải O(m²).\n\n### Vòng tìm kiếm của KMP\n\nGiữ i trên văn bản và j trên mẫu. Khi ký tự bằng nhau, cả hai tăng. Khi j đạt m, đã tìm thấy vị trí i−m; để cho phép kết quả chồng lấn, đặt `j = lps[j-1]` thay vì 0. Khi lệch và j > 0, đặt j về `lps[j-1]` nhưng giữ nguyên i: ký tự văn bản hiện tại chưa được so với ứng viên biên mới. Chỉ khi j = 0 và vẫn lệch mới tăng i.\n\nInvariant quan trọng là trước mỗi phép so, `pattern[:j] == text[i-j:i]`. Vì vậy phần đã khớp không cần đọc lại. i không giảm; j tăng tối đa theo số lần i tăng và mỗi lần fallback làm j giảm. Tổng thao tác O(n + m), không gian O(m).\n\n### Vì sao KMP tìm được lần xuất hiện chồng lấn\n\nVăn bản `aaaaa`, mẫu `aaa` có kết quả 0, 1, 2. Sau kết quả đầu, LPS cuối bằng 2: hai chữ `aa` cuối của lần khớp cũng có thể là hai chữ đầu của lần sau. Đặt j về 2 giữ lại thông tin đó. Đặt j về 0 sẽ bỏ lỡ vị trí 1. Đây là ca biên hay bắt lỗi hơn ví dụ các lần xuất hiện tách rời.\n\n### Rolling hash như một đa thức\n\nRabin–Karp coi chuỗi ký tự mã số c₀…cₘ₋₁ như đa thức `c₀·B^(m−1) + ... + cₘ₋₁`, lấy modulo Q để số không phình vô hạn. Khi cửa sổ trượt, trừ đóng góp ký tự trái, nhân phần còn lại với B rồi cộng ký tự phải. Nhờ đó cập nhật hash O(1), thay vì băm lại m ký tự.\n\nNếu `H` là hash hiện tại và `high = B^(m−1) mod Q`, công thức là `H' = ((H − left·high)·B + right) mod Q`. Python cho modulo không âm; ở ngôn ngữ mà phần dư có thể âm, phải cộng Q trước khi dùng.\n\n### Va chạm không phải chi tiết có thể bỏ\n\nHai chuỗi khác nhau có thể cùng hash vì miền chuỗi vô hạn còn chỉ có Q giá trị. Do đó điều kiện đúng là: hash khác ⇒ không khớp; hash bằng ⇒ ứng viên, phải so chuỗi thật. Nếu bỏ bước xác nhận, thuật toán có thể trả kết quả sai. Nếu kẻ tấn công kiểm soát đầu vào và biết B, Q, họ có thể tạo nhiều va chạm khiến mỗi cửa sổ phải so O(m), đẩy trường hợp xấu nhất lên O(nm).\n\nDùng hai modulo làm xác suất va chạm ngẫu nhiên nhỏ hơn nhưng vẫn không phải chứng minh bằng nhau. Với tìm kiếm chính xác, phép so cuối là hàng rào đúng đắn. Với dữ liệu không đối kháng, một modulo lớn thường đủ làm bộ lọc; không được mô tả nó là “không bao giờ va chạm”.\n\n### Nhiều mẫu và lợi thế của Rabin–Karp\n\nNếu có nhiều mẫu cùng độ dài, lưu hash của chúng trong hash map. Mỗi cửa sổ văn bản chỉ tính một hash rồi tra nhóm ứng viên tương ứng; sau đó xác nhận từng mẫu trong bucket. KMP truyền thống cần một automaton logic riêng cho mỗi mẫu. Nếu độ dài khác nhau nhiều, lợi thế giảm vì phải duy trì nhiều cửa sổ; Trie hoặc Aho–Corasick hợp hơn.\n\n### Unicode và đơn vị chỉ số\n\nPython đánh chỉ số theo code point, không phải byte UTF-8 và cũng không chắc là ký tự người dùng nhìn thấy. Chuỗi có dấu có thể tồn tại ở dạng ký tự dựng sẵn hoặc chữ cái cộng dấu kết hợp; hai dạng nhìn giống nhưng so sánh khác. Pipeline phải chuẩn hóa Unicode nhất quán trước khi tìm. Nếu trả offset cho file byte, chỉ số Python không thể dùng trực tiếp; cần tìm trên bytes hoặc duy trì ánh xạ.\n\n### Mẫu rỗng, mẫu dài và văn bản rỗng\n\nToán học thường coi mẫu rỗng xuất hiện tại n+1 ranh giới, còn nhiều API chỉ trả 0 hoặc từ chối. Bài này chọn trả mọi ranh giới `[0, ..., n]`; đặc tả phải nói rõ vì lựa chọn ảnh hưởng bộ nhớ. Nếu m > n, kết quả rỗng ngay. Văn bản rỗng và mẫu không rỗng cũng rỗng. Những nhánh này tránh truy cập `pattern[0]` và số mũ âm khi tạo rolling hash.\n\n### Khi nào không nên tự triển khai\n\nĐể tìm một literal đơn giản trong Python, `str.find`, toán tử `in` hoặc thư viện chuẩn thường nhanh và ít lỗi hơn vì chạy ở mã tối ưu. Regex phù hợp khi mẫu mô tả lớp ký tự hay lựa chọn, không phải một chuỗi cố định. KMP đáng dùng khi cần học, cần bảo đảm tuyến tính hoặc triển khai trên kiểu sequence riêng; Rabin–Karp đáng dùng cho nhiều mẫu đồng độ dài, phát hiện đoạn giống hoặc cửa sổ luồng. Không dùng rolling hash để thay cryptographic hash cho an ninh.",

vi_du: "### KMP hoàn chỉnh và kiểm ca chồng lấn\n\n```python\ndef build_lps(pattern: str) -> list[int]:\n    lps = [0] * len(pattern)\n    length = 0\n    i = 1\n    while i < len(pattern):\n        if pattern[i] == pattern[length]:\n            length += 1\n            lps[i] = length\n            i += 1\n        elif length:\n            length = lps[length - 1]\n        else:\n            i += 1\n    return lps\n\ndef kmp_all(text: str, pattern: str) -> list[int]:\n    if pattern == \"\":\n        return list(range(len(text) + 1))\n    lps = build_lps(pattern)\n    found = []\n    i = j = 0\n    while i < len(text):\n        if text[i] == pattern[j]:\n            i += 1; j += 1\n            if j == len(pattern):\n                found.append(i - j)\n                j = lps[j - 1]\n        elif j:\n            j = lps[j - 1]\n        else:\n            i += 1\n    return found\n\nassert build_lps(\"ababaca\") == [0, 0, 1, 2, 3, 0, 1]\nassert kmp_all(\"iphone iphoneiphone\", \"iphone\") == [0, 7, 13]\nassert kmp_all(\"aaaaa\", \"aaa\") == [0, 1, 2]\nassert kmp_all(\"abc\", \"\") == [0, 1, 2, 3]\n```\n\n### Rabin–Karp có bước xác nhận\n\n```python\ndef rabin_karp_all(text: str, pattern: str) -> list[int]:\n    n, m = len(text), len(pattern)\n    if m == 0:\n        return list(range(n + 1))\n    if m > n:\n        return []\n    base, mod = 257, 1_000_000_007\n    high = pow(base, m - 1, mod)\n    hp = hw = 0\n    for i in range(m):\n        hp = (hp * base + ord(pattern[i])) % mod\n        hw = (hw * base + ord(text[i])) % mod\n    found = []\n    for start in range(n - m + 1):\n        if hw == hp and text[start:start + m] == pattern:\n            found.append(start)\n        if start < n - m:\n            hw = (hw - ord(text[start]) * high) % mod\n            hw = (hw * base + ord(text[start + m])) % mod\n    return found\n\ncases = [(\"\", \"\"), (\"\", \"a\"), (\"aaaaa\", \"aaa\"),\n         (\"ốp iphone và iphone cũ\", \"iphone\"), (\"abc\", \"abcd\")]\nfor text, pattern in cases:\n    assert rabin_karp_all(text, pattern) == kmp_all(text, pattern)\nprint(\"hai thuật toán đồng thuận\")\n```\n\nLát cắt xác nhận tạo chuỗi tạm độ dài m khi hash trùng; đó là lý do trường hợp nhiều va chạm tốn O(nm) và bộ nhớ tạm có thể O(m). Có thể so từng ký tự để tránh cấp phát nhưng code dài hơn.",

so_sanh: "### Không có thuật toán thắng mọi tình huống\n\n| Cách | Tiền xử lý | Tìm kiếm | Bộ nhớ | Đặc tính |\n|---|---:|---:|---:|---|\n| Thử mọi vị trí | không | O(nm) xấu nhất | O(1) | ngắn, tốt cho mẫu rất nhỏ |\n| KMP | O(m) | O(n) bảo đảm | O(m) | một mẫu, không va chạm |\n| Rabin–Karp | O(m) | trung bình O(n), xấu O(nm) | O(1) ngoài kết quả | thuận lợi cho nhiều mẫu cùng độ dài |\n| `str.find` | phụ thuộc hiện thực | tối ưu trong thư viện | ẩn | lựa chọn mặc định trong ứng dụng Python |\n| Regex | biên dịch mẫu | phụ thuộc mẫu/engine | phụ thuộc | biểu thức mẫu, không chỉ literal |\n\nKMP trả sự chắc chắn bằng một bảng phụ và logic fallback khó viết đúng. Rabin–Karp có vòng lặp trực quan hơn, dễ mở rộng sang fingerprint của cửa sổ, nhưng phải quản lý modulo và xác nhận va chạm. Với pipeline truy vấn, nếu chỉ dò `iphone`, API chuẩn là thực dụng; nếu bài toán yêu cầu hàng nghìn mẫu từ khóa, hãy cân nhắc Aho–Corasick thay vì chạy KMP hàng nghìn lần.",

loi_thuong_gap: [
  "Sau khi khớp KMP, đặt j về 0 nên bỏ lỡ kết quả chồng lấn; ca `aaaaa` và `aaa` phải trả 0, 1, 2.",
  "Khi lệch với j > 0 lại tăng i; invariant bị phá và có thể bỏ vị trí đúng. Chỉ fallback j, giữ nguyên ký tự văn bản.",
  "Hiểu `lps[i]` là số lần ký tự lặp thay vì độ dài biên; bảng `ababaca` là test chẩn đoán tốt.",
  "Tin hash bằng là chuỗi bằng; lỗi hiếm trên dữ liệu thường nhưng sai về nguyên tắc. Luôn xác nhận nội dung.",
  "Tính lại hash cả cửa sổ bằng slicing; profiler cho thấy thuật toán quay về O(nm). Phải trừ trái, nhân base, cộng phải.",
  "Không định nghĩa mẫu rỗng, dẫn đến truy cập ngoài mảng hoặc số mũ −1. Xử lý trước khi tiền xử lý.",
  "Chuẩn hóa một phía của Unicode; chuỗi nhìn giống nhau không khớp. Chuẩn hóa cùng dạng ở biên nhập liệu."
],

tu_kiem_tra: [
  "Hãy tự xây LPS cho `aabaaab` và giải thích từng lần fallback.",
  "Invariant `pattern[:j] == text[i-j:i]` cho phép KMP không lùi i như thế nào?",
  "Vì sao tổng số lần fallback khi xây LPS vẫn O(m) dù một i có thể fallback nhiều lần?",
  "Tạo một ca cho thấy đặt j = 0 sau kết quả sẽ bỏ lỡ lần khớp chồng lấn.",
  "Tại sao hai modulo chỉ giảm xác suất va chạm mà không biến hash thành chứng minh?",
  "Nếu cần tìm 10.000 mẫu có độ dài khác nhau, vì sao cả KMP lặp và Rabin–Karp nhiều cửa sổ đều chưa lý tưởng?",
  "Offset theo code point khác offset byte UTF-8 gây lỗi gì khi tô sáng kết quả trong file gốc?"
],

tai_nguyen: [
  "Introduction to Algorithms — phần String Matching.",
  "Algorithms của Robert Sedgewick và Kevin Wayne — phần Substring Search.",
  "The Algorithm Design Manual — phần String Matching.",
  "Competitive Programming — phần String Processing."
],

lien_he: [
  "Mảng Hash: rolling hash minh họa va chạm và lý do hash bằng chưa đủ kết luận.",
  "Mảng Cây: Trie và Aho–Corasick mở rộng tìm kiếm từ một mẫu sang nhiều mẫu.",
  "Mảng Bảo mật: cryptographic hash có mục tiêu khác rolling hash và không thể tráo đổi.",
  "Mảng Unicode: chuẩn hóa văn bản trước khi so khớp để dạng mã hóa không làm sai kết quả."
]

};
