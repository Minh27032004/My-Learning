window.LESSONS["03:hoan-tac:git-clean-de-don-file-rac"] = {

tom_tat: [
  "`git clean` xóa file và thư mục chưa được theo dõi khỏi working tree; vì Git không có snapshot của chúng, thao tác thật thường không thể hoàn tác bằng Git.",
  "Luôn bắt đầu bằng dry-run `git clean -n`, rồi thu hẹp theo đường dẫn hoặc mẫu loại trừ; thêm `-d`, `-x`, `-X` làm phạm vi thay đổi đáng kể.",
  "`-x` gồm cả file bị ignore, còn `-X` chỉ nhắm file bị ignore. Hai cờ giống chữ nhưng giải quyết hai mục tiêu khác và có mức rủi ro khác nhau.",
  "Lệnh mặc định không xóa nested Git repository và không xóa file đã theo dõi; đây là hàng rào an toàn, không phải cam kết rằng mọi dữ liệu quan trọng đều được giữ.",
  "Trong repository `banhang-api`, cần giữ `notes/coupon-cases.md` chưa commit nhưng dọn `coverage/`, `.pytest_cache/` và bundle; phân loại trước quan trọng hơn thuộc cờ."
],

can_biet_truoc: [
  "Biết đọc `git status --short --ignored` và phân biệt tracked, untracked, ignored, staged.",
  "Hiểu `.gitignore` chỉ ngăn file chưa được theo dõi xuất hiện như ứng viên commit; nó không tự xóa file và không áp dụng ngược cho file đã tracked.",
  "Biết pathspec là cách Git giới hạn đường dẫn, và shell có thể mở rộng wildcard trước khi Git nhận đối số.",
  "Đã hiểu từ bài reflog rằng Git chỉ cứu được object từng được lưu; file untracked chưa có blob nên không nằm trong reflog."
],

dinh_nghia: "`git clean` là lệnh loại bỏ có chọn lọc các mục **untracked** (chưa được Git theo dõi) trong working tree. Ở chế độ mặc định, Git yêu cầu cờ ép `-f` trước khi xóa; `-n` hoặc `--dry-run` chỉ liệt kê mục sẽ bị tác động, còn `-i` mở phiên chọn tương tác.\n\nLệnh không đảo commit, không dọn staging area và không thay đổi nội dung tracked. Nó xử lý phần nằm ngoài lịch sử Git — chính vì thế dữ liệu bị xóa thường không thể lấy lại bằng `restore`, `reset` hay `reflog`.",

vi_sao_can: "Build và test sinh ra nhiều dấu vết: bundle, coverage report, cache, file tạm và thư mục dependency. Chúng có thể làm `git status` nhiễu, khiến phép thử “build từ checkout sạch” không còn đáng tin, hoặc che lỗi do artifact cũ. Xóa thủ công từng đường dẫn dễ bỏ sót; xóa cả thư mục bằng lệnh filesystem lại không hiểu quy tắc tracked/ignored của Git.\n\nSau khi cứu `feature/coupon` của `banhang-api` bằng reflog, nhóm thấy `coverage/`, `.pytest_cache/`, `dist/`, `debug.log` và `notes/coupon-cases.md`. Bốn mục đầu có thể tái tạo; file notes là công sức điều tra chưa commit. Một `git clean -fdx` mù quáng sẽ xóa tất cả. Bài toán thật vì vậy không phải “lệnh nào dọn sạch nhất”, mà là “làm sao chứng minh tập sẽ xóa chỉ gồm dữ liệu tái tạo được”.\n\n`git clean` cần khi kiểm tra clean-room build, loại artifact gây test sai, chuẩn bị đóng gói hoặc trả working tree về trạng thái có thể tái lập. Nó không nên là thói quen chữa mọi `git status` lộn xộn.",

co_che: "### Git phân loại working tree trước khi xóa\n\nGit so đường dẫn trên đĩa với index và quy tắc ignore. File có entry trong index là tracked, dù đang sửa hay không; `git clean` bỏ qua. File không có entry là untracked. Một phần untracked lại khớp `.gitignore`, `.git/info/exclude` hoặc global excludes nên được gọi là ignored.\n\nBa lớp này giải thích mọi cờ. `git clean -f` nhắm untracked không ignored ở thư mục hiện tại theo phạm vi. `-x` bỏ qua quy tắc ignore để lấy cả ignored. `-X` làm ngược trực giác: chỉ lấy ignored. Không cờ nào biến file tracked thành ứng viên.\n\n### Tại sao phải có `-f`\n\nDo xóa untracked thường không phục hồi qua Git, cấu hình `clean.requireForce` mặc định buộc người dùng biểu đạt ý định bằng `-f`, trừ dry-run và interactive mode. Hàng rào này ngăn một lần gõ thiếu suy nghĩ, nhưng không đánh giá giá trị file. Sau khi có `-f`, Git tin lựa chọn của người dùng.\n\n`git clean -n` an toàn về mặt thay đổi vì chỉ in kế hoạch. Nó vẫn cần được đọc kỹ: nếu terminal cuộn qua hàng nghìn dòng, một file quan trọng có thể lẫn trong đó. Thu hẹp phạm vi trước làm review có ý nghĩa hơn.\n\n### `-n`: biến xóa thành kế hoạch có thể kiểm tra\n\nTrong `banhang-api`, chạy:\n\n```bash\ngit status --short --ignored\ngit clean -nd\n```\n\n`status` cho biết loại trạng thái; `clean -nd` cho biết chính xác mục untracked không ignored sẽ bị xóa khi thêm `-f`. Cờ `-d` bao gồm thư mục untracked. Không có `-d`, Git thường không đi vào xóa cả thư mục untracked, nhằm tránh quét phạm vi lớn ngoài ý muốn.\n\nDry-run phải dùng cùng tập cờ và pathspec với lệnh thật. Xem `git clean -n` rồi chạy `git clean -fdx` là một lỗi logic: kế hoạch không bao gồm ignored và thư mục, nhưng thao tác thật lại có.\n\n### `-d`: ranh giới thư mục\n\nArtifact hiếm khi chỉ là file ở gốc; coverage và cache nằm trong cây thư mục. `-d` cho phép loại thư mục untracked. Nếu chỉ một thư mục cần dọn, pathspec làm ý định rõ hơn:\n\n```bash\ngit clean -nd -- coverage/\ngit clean -fd -- coverage/\n```\n\nDấu `--` kết thúc phần option, hữu ích nếu đường dẫn bắt đầu bằng dấu gạch ngang. Pathspec còn giảm nguy cơ cuốn file notes ở nơi khác. Khi biết chính xác target, xóa có phạm vi tốt hơn lệnh quét toàn repository.\n\n### `-x` và `-X` không phải hai cách viết giống nhau\n\n`git clean -fdx` xóa untracked bất kể có bị ignore hay không. Nó phù hợp để mô phỏng checkout hoàn toàn sạch: dependencies, build output, cache và cả file cấu hình local bị ignore đều là ứng viên. Đây là lý do cờ này nguy hiểm nhất; nhiều nhóm ignore `.env`, certificate thử nghiệm hoặc database local vì chúng không được commit, nhưng dữ liệu ấy không nhất thiết tái tạo được.\n\n`git clean -fdX` chỉ xóa mục ignored. Nó hữu ích khi `.gitignore` được duy trì tốt và mục tiêu là dọn artifact mà vẫn giữ notes untracked chưa ignore. Tuy nhiên nếu file quý cũng bị ignore, `-X` vẫn xóa. Ignore là chính sách version control, không phải nhãn “rác”.\n\n| Lệnh dry-run | Untracked thường | Ignored | Thư mục |\n|---|---:|---:|---:|\n| `git clean -n` | Có | Không | Không xóa cả cây |\n| `git clean -nd` | Có | Không | Có |\n| `git clean -ndx` | Có | Có | Có |\n| `git clean -ndX` | Không | Có | Có |\n\n### Loại trừ: chú ý tương tác giữa `-e` và `-X`\n\n`-e <pattern>` thêm mẫu ignore riêng cho lần clean. Với `-x`, mẫu bổ sung có thể bảo vệ ngoại lệ vì `-x` lấy mọi untracked trừ các mẫu `-e`. Ví dụ muốn xem toàn bộ untracked và ignored nhưng bảo vệ file môi trường:\n\n```bash\ngit clean -ndx -e .env -e 'notes/'\ngit clean -fdx -e .env -e 'notes/'\n```\n\nĐặt pattern trong nháy khi shell có thể mở rộng ký tự đặc biệt. Cần lặp đúng các cờ giữa preview và execute. Với dữ liệu quan trọng, chuyển file ra ngoài repository hoặc commit đúng chỗ thường dễ kiểm chứng hơn việc dựa vào chuỗi exclude phức tạp.\n\nKhông suy rộng cách này sang `-X`: cờ `-X` chỉ xóa mục được xem là ignored, còn `-e .env` lại bổ sung `.env` vào tập ignored, nên không bảo vệ nó. Khi cần loại một đường dẫn khỏi `-X`, dùng pathspec loại trừ và preview đúng nguyên văn:\n\n```bash\ngit clean -ndX -- . ':(exclude).env'\ngit clean -fdX -- . ':(exclude).env'\n```\n\n### Interactive mode khi danh sách hỗn hợp\n\n`git clean -di` hiển thị tập ứng viên và cho phép lọc theo pattern hoặc chọn theo số. Nó phù hợp khi có vài nhóm artifact xen kẽ dữ liệu cần giữ. Interactive mode giảm lỗi nhớ option, nhưng vẫn là xóa thật khi xác nhận và không tạo backup.\n\nTrong automation, không dùng `-i` vì cần input con người. Script CI nên chạy trong workspace dùng một lần hoặc xóa target build xác định, thay vì phụ thuộc menu tương tác.\n\n### Nested repository và hai lần `-f`\n\nMột thư mục untracked có `.git` bên trong có thể là repository độc lập chứa công việc riêng. Git bảo vệ biên này: ngay cả `-fd` thường không xóa nested repository; cần `-ff` để ép qua hàng rào. Cờ ép lặp đôi là tín hiệu dừng lại kiểm tra, không phải mẹo “lệnh chưa đủ mạnh”.\n\nSubmodule được khai báo và tracked là trường hợp khác; đừng dùng clean để giải quyết trạng thái submodule khi chưa hiểu cấu hình. Chạy `git status`, `git submodule status` và vào đúng repository con để xử lý.\n\n### Điều gì xảy ra bên dưới\n\nGit duyệt working tree trong phạm vi pathspec, tham chiếu index để bỏ tracked, áp dụng ignore theo cờ, rồi xóa mục phù hợp bằng thao tác filesystem. Không có commit mới, không có ref di chuyển và không có blob dự phòng được tạo. Reflog vì thế không ghi “nội dung file trước clean”; cùng lắm lịch sử shell cho biết bạn đã chạy lệnh gì.\n\nĐây là khác biệt bản chất với `reset`: reset thay ref/index và có thể để dấu trong reflog; clean tác động trực tiếp lên dữ liệu ngoài object database. Thành công của command chỉ chứng minh mục đã bị xóa, không chứng minh chúng là rác.\n\n### Quy trình dọn repository `banhang-api`\n\nTrước hết phân loại:\n\n```bash\ngit status --short --ignored\ngit ls-files --others --exclude-standard\ngit ls-files --others --ignored --exclude-standard\n```\n\nLệnh thứ hai liệt kê untracked không ignored; lệnh thứ ba liệt kê ignored. Ta thấy `notes/coupon-cases.md` ở nhóm đầu, còn cache, coverage và dist ở nhóm sau. Vì mục tiêu chỉ là artifact ignored, chọn:\n\n```bash\ngit clean -ndX\ngit clean -fdX\ngit status --short --ignored\n```\n\nFile notes còn nguyên. Nếu muốn dọn riêng coverage, pathspec còn chặt hơn: `git clean -fdX -- coverage/`. Sau đó chạy lại test/build để chứng minh artifact thực sự tái tạo được.\n\n### Ca biên cần thử trước khi đưa vào script\n\nRepository không có untracked: dry-run và clean không làm gì, exit thành công không có nghĩa script đã tìm đúng thư mục. File rỗng vẫn là dữ liệu và bị xóa như file khác. Symlink cần được hiểu theo hành vi filesystem và phạm vi Git; không nên suy đoán rằng clean sẽ “dọn target”. Tên file có khoảng trắng hoặc xuống dòng làm parsing output bằng vòng lặp shell dễ sai.\n\nNếu cần xử lý máy móc, ưu tiên `git clean` trực tiếp với pathspec đã xác định thay vì parse danh sách rồi gọi `rm`. Với pipeline dùng workspace dùng một lần, hủy toàn workspace thường có mô hình an toàn rõ hơn clean chọn lọc trên máy lập trình viên.\n\n### Khi nào không nên dùng\n\nKhông dùng `git clean` khi chưa phân loại file, khi repository chứa dữ liệu local không backup, hoặc khi mục tiêu chỉ là một target do build tool quản lý. `npm run clean`, `mvn clean`, `cargo clean` hay xóa thư mục build cụ thể thường diễn đạt domain tốt hơn và không đụng notes ngoài phạm vi.\n\nKhông dùng nó để che `.gitignore` tệ. Nếu artifact liên tục xuất hiện như untracked, sửa ignore hoặc cấu hình output đúng mới là giải pháp gốc. Không dùng `-fdx` như bước mở đầu khi debug; cache đôi khi là bằng chứng cần giữ để tái hiện lỗi.",

vi_du: "### Phòng lab chứng minh sự khác biệt `-x` và `-X`\n\nChạy bằng Bash/Git Bash trong thư mục mới:\n\n```bash\nmkdir banhang-api-clean-lab\ncd banhang-api-clean-lab\ngit init\ngit config user.name \"Clean Lab\"\ngit config user.email \"clean-lab@example.invalid\"\n\nprintf 'dist/\\ncoverage/\\n.pytest_cache/\\n.env\\n' > .gitignore\nprintf 'coupon api\\n' > app.txt\ngit add .gitignore app.txt\ngit commit -m \"initialize coupon api\"\n\nmkdir -p dist coverage .pytest_cache notes\nprintf 'bundle\\n' > dist/app.js\nprintf '82%%\\n' > coverage/summary.txt\nprintf 'cache\\n' > .pytest_cache/state\nprintf 'LOCAL_TOKEN=demo-only\\n' > .env\nprintf 'check expired coupon boundary\\n' > notes/coupon-cases.md\n\ngit status --short --ignored\ngit clean -nd\ngit clean -ndX\ngit clean -ndx\n```\n\nBa preview phải khác nhau. `-nd` thấy `notes/`; `-ndX` thấy bốn mục ignored nhưng không thấy notes; `-ndx` thấy cả hai nhóm. Để thực hiện lựa chọn an toàn cho tình huống này:\n\n```bash\ngit clean -ndX -- . ':(exclude).env'\ngit clean -fdX -- . ':(exclude).env'\ntest -f notes/coupon-cases.md && echo 'notes còn'\ntest -f .env && echo '.env còn'\ntest ! -e dist && echo 'dist đã dọn'\ngit status --short --ignored\n```\n\nTa cố tình giữ `.env` bằng pathspec loại trừ dù nó bị ignore, vì “ignored” không đồng nghĩa “có thể tái tạo”. Dấu nháy ngăn shell diễn giải cú pháp pathspec. Các lệnh `test` trả exit code khác 0 nếu giả định sai, nên ví dụ có thể dùng trong script kiểm chứng thay vì chỉ nhìn bằng mắt.\n\n### Dọn đúng một target\n\nNếu chỉ coverage gây sai kết quả, lệnh hẹp nhất là:\n\n```bash\ngit clean -ndX -- coverage/\ngit clean -fdX -- coverage/\n```\n\nNếu dry-run không in đúng target, dừng lại. Không mở rộng sang `-x` chỉ để “cho chạy được”; hãy kiểm xem coverage có thật sự ignored và current directory có đúng repository không.",

so_sanh: "### Dọn sạch không chỉ có một cách\n\n| Cách | Hiểu tracked/ignored | Khả năng phục hồi | Khi phù hợp |\n|---|---|---|---|\n| `git clean -fdX` | Có; chỉ ignored | Git không phục hồi untracked | Dọn artifact đã được ignore có chủ đích |\n| `git clean -fdx` | Có nhưng bỏ qua ignore | Rất thấp | Clean-room build sau preview và backup cấu hình local |\n| Build tool `clean` | Hiểu output của công cụ | Tùy công cụ | Target build xác định, ưu tiên trong công việc hằng ngày |\n| Xóa workspace CI dùng một lần | Không cần bảo tồn local | Tạo lại từ clone/cache | Runner tạm thời, yêu cầu tái lập cao |\n| `git reset --hard` | Chỉ tracked/index/ref | Có thể tìm commit qua reflog, nhưng mất sửa đổi chưa commit | Đồng bộ tracked với commit, không dọn untracked |\n| `git stash -u` | Lưu cả untracked vào stash | Có thể apply/pop | Cần cất tạm công việc chứ không muốn xóa |\n\n`stash -u` là lựa chọn đáng cân nhắc cho `notes/coupon-cases.md`: nó đưa dữ liệu vào một object có ref stash thay vì đánh cược với exclude. Đổi lại, stash dễ bị quên và không thay thế commit có tên rõ. Với artifact hàng gigabyte có thể tái tạo, stash lại lãng phí dung lượng; build clean hoặc workspace mới hợp lý hơn.",

loi_thuong_gap: [
  "**Preview bằng cờ hẹp, xóa bằng cờ rộng.** `git clean -n` không đại diện cho `git clean -fdx`; nhận biết bằng việc hai dòng lệnh khác `d`, `x`, `X`, pathspec hoặc exclude.",
  "**Đọc ignored như đồng nghĩa với rác.** `.env`, database local và certificate có thể bị ignore nhưng khó tái tạo; kiểm nguồn phục hồi trước khi dùng `-x` hoặc `-X`.",
  "**Quên `-d` rồi tăng thành `-fdx` vì thư mục chưa biến mất.** Vấn đề chỉ là biên thư mục; thêm cờ không liên quan mở rộng sang ignored ngoài ý muốn.",
  "**Tin reflog sẽ cứu file untracked.** Sau clean, `git reflog` không có snapshot file; nhận biết từ `git ls-files` trước đó không liệt kê file như tracked.",
  "**Chạy ở sai repository hoặc sai thư mục con.** Dry-run rỗng hoặc danh sách lạ là dấu hiệu; xác nhận `git rev-parse --show-toplevel` và pathspec.",
  "**Dùng `-ff` để vượt nested repository mà không kiểm tra.** Sự xuất hiện của `.git` bên trong cho thấy có lịch sử riêng; vào repository con hoặc backup trước.",
  "**Parse output có khoảng trắng bằng vòng lặp shell rồi gọi `rm`.** Tên file bị tách sai; để Git xử lý pathspec hoặc dùng giao thức phân cách NUL khi thật sự cần automation.",
  "**Dọn cache trước khi thu thập bằng chứng lỗi.** Test hết lỗi sau clean nhưng nguyên nhân không được hiểu; lưu log và bước tái hiện trước nếu đang điều tra incident."
],

tu_kiem_tra: [
  "Tại sao `git clean` không xóa file tracked đang sửa, và tại sao điều đó không làm lệnh an toàn với mọi dữ liệu quan trọng?",
  "Cho `dist/` và `.env` đều bị ignore, `notes.txt` untracked thường. Bốn tập ứng viên của `-n`, `-nd`, `-ndX`, `-ndx` khác nhau ra sao?",
  "Bạn đã preview bằng `git clean -ndX -- coverage/`. Lệnh thực thi nào tương ứng chính xác, và vì sao không nên bỏ pathspec?",
  "Giải thích vì sao reflog cứu được commit sau reset nhưng không cứu được `notes.txt` sau clean.",
  "Khi nào `git stash -u` tốt hơn clean, và nhược điểm của việc stash artifact lớn là gì?",
  "Ca khó: repository có thư mục untracked là một repository Git khác. Vì sao `-f` chưa đủ, và phản ứng đúng khi thấy cần `-ff` là gì?",
  "Thiết kế quy trình CI đảm bảo build sạch mà không chạy `git clean -fdx` trên workspace lâu dài của lập trình viên."
],

tai_nguyen: [
  "Pro Git — Scott Chacon và Ben Straub, các phần Recording Changes và Undoing Things.",
  "Git Reference Manual — mục `git-clean`, `gitignore`, `git-ls-files` và `git-stash`.",
  "Version Control with Git — Jon Loeliger và Matthew McCullough.",
  "The DevOps Handbook — Gene Kim, Jez Humble, Patrick Debois và John Willis, các ý về build có thể tái lập."
],

lien_he: [
  "Nối sang mảng Hệ điều hành: clean cuối cùng gọi thao tác xóa trên filesystem; quyền, symlink và khả năng khôi phục phụ thuộc hệ điều hành chứ không phải lịch sử Git.",
  "Nối sang mảng Công cụ của dev: build tool biết output theo domain nên target `clean` thường hẹp và dễ tái lập hơn quét toàn repository.",
  "Nối sang mảng CI/CD: runner dùng một lần biến “môi trường sạch” thành thuộc tính kiến trúc, giảm nhu cầu dọn phá hủy trên workspace dùng lâu dài.",
  "Nối sang mảng Bảo mật: secret bị ignore vẫn là secret; dry-run, secret manager và vòng đời credential quan trọng hơn giả định `.gitignore` bảo vệ dữ liệu."
]

};
