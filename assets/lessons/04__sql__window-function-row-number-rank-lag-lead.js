window.LESSONS["04:sql:window-function-row-number-rank-lag-lead"] = {

tom_tat: [
  "Window function tính giá trị từ một nhóm hàng liên quan nhưng vẫn giữ nguyên từng hàng đầu vào, khác với `GROUP BY` vốn thu gọn mỗi nhóm thành một hàng.",
  "Mỗi cửa sổ có ba quyết định độc lập: `PARTITION BY` chia nhóm, `ORDER BY` tạo thứ tự trong nhóm, và frame xác định lát hàng được aggregate nhìn thấy.",
  "`ROW_NUMBER`, `RANK`, `DENSE_RANK` xử lý hòa điểm khác nhau; muốn kết quả tái lập phải thêm khóa phá hòa duy nhất.",
  "`LAG` và `LEAD` truy cập hàng trước/sau trong thứ tự logic, thích hợp tính biến động đơn hàng nhưng không tự lấp các ngày bị thiếu.",
  "Window function được tính sau `WHERE` và không thể lọc trực tiếp trong cùng tầng; cần subquery hoặc CTE, đồng thời phải đọc nút `Sort`/`WindowAgg` để hiểu chi phí."
],

can_biet_truoc: [
  "Biết `SELECT`, `ORDER BY`, `GROUP BY`, aggregate function và alias bảng.",
  "Hiểu một quan hệ không có thứ tự mặc định; chỉ `ORDER BY` mới yêu cầu thứ tự cho kết quả.",
  "Phân biệt hạt dữ liệu “một hàng mỗi đơn” với “một hàng mỗi khách”.",
  "Biết CTE hoặc subquery trong `FROM` để tạo tầng truy vấn rồi lọc kết quả trung gian."
],

dinh_nghia: "**Window function** (hàm cửa sổ) tính một giá trị cho mỗi hàng dựa trên một “cửa sổ” các hàng có quan hệ với nó. Cửa sổ được mô tả bởi mệnh đề `OVER (...)`; kết quả không làm mất hàng như `GROUP BY`.\n\n`ROW_NUMBER()` đánh số duy nhất theo thứ tự; `RANK()` cho các hàng hòa cùng hạng và để khoảng trống sau nhóm hòa; `LAG(expr)` lấy biểu thức ở hàng trước, còn `LEAD(expr)` lấy ở hàng sau trong partition. Aggregate như `sum` cũng trở thành window function khi có `OVER`, lúc đó frame quyết định tập hàng góp vào giá trị của từng hàng.",

vi_sao_can: "Báo cáo vận hành thường cần vừa giữ chi tiết vừa có ngữ cảnh: mỗi đơn phải còn `order_id`, nhưng cần biết đó là đơn thứ mấy của khách, chênh bao nhiêu so với lần trước, hoặc thuộc top ba doanh thu tháng. `GROUP BY customer_id` chỉ trả một hàng mỗi khách và xóa danh tính từng đơn. Nối kết quả aggregate trở lại bảng có thể làm được, nhưng dài, dễ sai khi hòa điểm và thường phải quét/nối thêm.\n\nWindow function biểu diễn phép so sánh “hàng này với hàng lân cận hoặc nhóm của nó” trực tiếp. Đổi lại, hệ quản trị thường phải gom và sắp xếp theo partition/order. Một truy vấn top-N tưởng nhỏ có thể sort hàng triệu bản ghi, dùng file tạm nếu thiếu bộ nhớ, và cho kết quả không ổn định nếu thứ tự hòa chưa được định nghĩa. Vì vậy phải học cả semantics và plan.",

co_che: "### `OVER` mở cửa sổ nhưng không gom hàng\n\n```sql\nSELECT order_id, customer_id, total_amount,\n       avg(total_amount) OVER (PARTITION BY customer_id) AS customer_avg\nFROM orders;\n```\n\nNếu An có ba đơn, kết quả vẫn có ba hàng của An; `customer_avg` lặp lại trên ba hàng. `GROUP BY customer_id` chỉ còn một hàng. Đây là phẩm chất cốt lõi: window function bổ sung ngữ cảnh vào hạt hiện tại, không tự thay hạt.\n\n### `PARTITION BY`: biên giới không cho dữ liệu đi qua\n\n`PARTITION BY customer_id` khởi động lại phép tính cho từng khách. Bỏ nó biến toàn bảng thành một partition. Với `ROW_NUMBER`, số thứ tự sẽ chạy xuyên khách; với `LAG`, đơn đầu của Bình có thể nhìn thấy đơn cuối của An, một kết quả hợp lệ về cú pháp nhưng vô nghĩa nghiệp vụ.\n\nPartition không nhất thiết là một cột. Báo cáo top sản phẩm theo tháng có thể dùng `PARTITION BY date_trunc('month', ordered_at), category_id`. Giá trị `NULL` của khóa partition được gom cùng nhau như một nhóm; nếu đơn không khách là dữ liệu lỗi, window function không sửa lỗi ấy mà chỉ tạo partition `NULL`.\n\n### `ORDER BY` bên trong và bên ngoài làm hai việc\n\n```sql\nSELECT order_id, customer_id,\n       row_number() OVER (\n         PARTITION BY customer_id\n         ORDER BY ordered_at, order_id\n       ) AS visit_no\nFROM orders\nORDER BY customer_id, visit_no;\n```\n\n`ORDER BY` trong `OVER` xác định đơn nào trước để tính `visit_no`. `ORDER BY` cuối statement xác định thứ tự hiển thị. Có cái thứ nhất không đảm bảo kết quả được gửi ra theo thứ tự ấy; planner có thể tái sử dụng sort nhưng SQL chỉ cam kết khi có mệnh đề cuối.\n\n`order_id` là khóa phá hòa. Nếu hai đơn có cùng `ordered_at`, chỉ sắp theo thời gian khiến `ROW_NUMBER` có thể đổi giữa hai execution plan. Thêm khóa duy nhất biến thứ tự thành toàn phần và giúp phân trang/kết quả kiểm thử ổn định.\n\n### `ROW_NUMBER`: chọn đúng một đại diện\n\nĐơn mới nhất mỗi khách là mẫu phổ biến:\n\n```sql\nSELECT *\nFROM (\n  SELECT o.*,\n         row_number() OVER (\n           PARTITION BY customer_id\n           ORDER BY ordered_at DESC, order_id DESC\n         ) AS rn\n  FROM orders o\n) ranked\nWHERE rn = 1;\n```\n\n`ROW_NUMBER` luôn cấp số khác nhau, nên `rn=1` lấy đúng một hàng. Quy tắc phá hòa bằng `order_id DESC` nói rõ nếu thời gian bằng nhau thì mã lớn hơn thắng. PostgreSQL còn có `DISTINCT ON`, thường gọn cho top-1, nhưng window linh hoạt cho top-3 và mang nhiều thứ hạng cùng lúc.\n\n### `RANK` và lỗ hổng sau hòa điểm\n\nGiả sử doanh thu sản phẩm là 900, 900, 700. `ROW_NUMBER` cho 1,2,3 tùy khóa phá hòa. `RANK` cho 1,1,3: hai sản phẩm cùng đứng nhất nên vị trí kế tiếp là thứ ba. `DENSE_RANK` cho 1,1,2. Chọn hàm là quy tắc nghiệp vụ, không phải sở thích.\n\n```sql\nSELECT product_id, revenue,\n       row_number() OVER (ORDER BY revenue DESC, product_id) AS seq,\n       rank()       OVER (ORDER BY revenue DESC) AS competition_rank,\n       dense_rank() OVER (ORDER BY revenue DESC) AS dense_rank\nFROM product_revenue;\n```\n\nKhông thêm `product_id` vào cửa sổ của `RANK` nếu muốn giữ hòa doanh thu: khóa phá hòa sẽ làm hai hàng không còn peers (các hàng đồng hạng). Chỉ `ROW_NUMBER` cần nó để chọn thứ tự duy nhất.\n\n### Top-N và câu hỏi “có tính cả hòa không?”\n\nLọc `ROW_NUMBER() <= 3` trả đúng ba hàng mỗi partition. Lọc `RANK() <= 3` có thể trả hơn ba hàng nếu hạng ba hòa. Một giao diện có đúng ba ô nên dùng quy tắc phá hòa rõ; bảng thành tích công bằng có thể cần giữ tất cả người đồng hạng. Trước khi tối ưu SQL, phải chốt nghĩa của N.\n\n### `LAG`: hàng trước không phải ngày trước\n\n```sql\nSELECT customer_id, ordered_at, total_amount,\n       lag(total_amount) OVER (\n         PARTITION BY customer_id\n         ORDER BY ordered_at, order_id\n       ) AS previous_amount\nFROM orders;\n```\n\nHàng đầu partition không có tiền nhiệm nên nhận `NULL`, hoặc dùng đối số thứ ba `lag(total_amount, 1, 0)`. Mặc định 0 chỉ hợp lý nếu “không có đơn trước” thực sự tương đương số tiền 0; thường `NULL` truyền đạt thiếu tiền nhiệm tốt hơn.\n\nNếu khách đặt ngày 1 rồi ngày 10, `LAG` của ngày 10 là ngày 1, không phải ngày 9. Muốn chuỗi ngày liên tục phải tạo lịch bằng `generate_series`, nối dữ liệu vào, quyết định ngày thiếu là 0 hay `NULL`, rồi mới dùng `LAG`.\n\n### `LEAD`: nhìn phía sau mà không self-join\n\n`LEAD(ordered_at)` giúp tính khoảng thời gian đến đơn tiếp theo:\n\n```sql\nSELECT customer_id, order_id, ordered_at,\n       lead(ordered_at) OVER w - ordered_at AS until_next\nFROM orders\nWINDOW w AS (\n  PARTITION BY customer_id\n  ORDER BY ordered_at, order_id\n);\n```\n\nHàng cuối nhận `NULL`. Mệnh đề `WINDOW` đặt tên specification để nhiều hàm dùng chung, giảm nguy cơ một hàm quên khóa phá hòa. Nó không lưu kết quả như CTE; chỉ tái sử dụng mô tả cửa sổ.\n\n### Frame: lát dữ liệu của aggregate\n\n`ROW_NUMBER`, `RANK`, `LAG`, `LEAD` chủ yếu dựa vào partition/order; frame gây bất ngờ nhất với `sum`, `first_value`, `last_value`. Tổng lũy kế rõ nghĩa nên viết:\n\n```sql\nsum(total_amount) OVER (\n  PARTITION BY customer_id\n  ORDER BY ordered_at, order_id\n  ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n) AS running_total\n```\n\n`ROWS` đếm hàng vật lý trong thứ tự logic. Frame mặc định khi có `ORDER BY` thường kéo cả peer group của hàng hiện tại; với hai đơn cùng timestamp, tổng có thể nhảy cả hai cùng lúc nếu dùng semantics kiểu `RANGE`. Viết frame tường minh làm ý đồ kiểm toán được.\n\n### `ROWS`, `RANGE`, `GROUPS`\n\n`ROWS BETWEEN 2 PRECEDING AND CURRENT ROW` lấy tối đa ba hàng, bất kể khoảng thời gian. `RANGE BETWEEN INTERVAL '7 days' PRECEDING AND CURRENT ROW` lấy các hàng có giá trị thời gian trong bảy ngày, có thể rất nhiều. `GROUPS` di chuyển theo nhóm peer. Ba loại giải ba câu hỏi khác nhau; đổi qua lại chỉ để plan nhanh có thể đổi kết quả.\n\nVới `RANGE`, kiểu và biểu thức order phải hỗ trợ độ lệch tương ứng. Cửa sổ bảy ngày không tự thêm ngày thiếu, nhưng nó không cần lịch liên tục vì chọn theo khoảng giá trị thời gian.\n\n### `LAST_VALUE` và frame mặc định\n\nNgười mới thường mong `last_value(total_amount) OVER (PARTITION BY customer_id ORDER BY ordered_at)` trả đơn cuối toàn khách. Frame mặc định kết thúc ở hàng hiện tại cùng peers, nên “last” thường chỉ là giá trị hiện tại. Muốn cuối toàn partition:\n\n```sql\nlast_value(total_amount) OVER (\n  PARTITION BY customer_id\n  ORDER BY ordered_at, order_id\n  ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING\n)\n```\n\nHoặc đảo thứ tự và dùng `first_value`. Nêu frame vẫn là cách rõ nhất.\n\n### Thứ tự xử lý và vì sao cần thêm một tầng\n\nWindow function được đánh giá sau `WHERE`, `GROUP BY`, `HAVING`, nhưng trước `ORDER BY` cuối. Vì vậy PostgreSQL không cho dùng `rn` trong `WHERE` cùng tầng: lúc lọc, `rn` chưa tồn tại. Subquery/CTE tạo một tầng hoàn tất `WindowAgg`, tầng ngoài mới lọc. Đây không phải hạn chế tùy tiện mà là hệ quả thứ tự logic.\n\nAggregate có thể xảy ra trước window trong cùng statement. Ta có thể `GROUP BY product_id` tạo doanh thu rồi `rank() OVER (ORDER BY sum(...) DESC)`, bởi window nhìn thấy các hàng đã gom.\n\n### Query plan: sort, incremental sort và `WindowAgg`\n\nPlan thường có `Sort` cung cấp thứ tự `(customer_id, ordered_at, order_id)` rồi `WindowAgg`. Sort toàn bộ có chi phí gần O(n log n) và cần bộ nhớ; khi vượt `work_mem`, `EXPLAIN ANALYZE` có thể báo sort method dùng disk. Index `(customer_id, ordered_at, order_id)` có khả năng cung cấp thứ tự, nhưng planner vẫn cân nhắc đọc heap, độ chọn lọc và việc cần hầu hết hàng.\n\nNếu đầu vào đã sắp theo đoạn đầu, PostgreSQL có thể dùng incremental sort để chỉ sắp phần còn lại theo từng nhóm. Không nên tạo index cho mọi báo cáo: index làm chậm ghi và chiếm cache. Hãy dùng `EXPLAIN (ANALYZE, BUFFERS)` trên dữ liệu đại diện.\n\n### Nhiều cửa sổ và khả năng dùng lại sort\n\nHai hàm có cùng partition/order có thể dùng một thứ tự. Hai hướng `ASC` và `DESC`, hoặc khóa khác nhau, có thể cần sort bổ sung. Đặt tên `WINDOW w AS (...)` giúp cú pháp nhất quán, nhưng optimizer quyết định tái sử dụng vật lý. Khi thấy nhiều nút sort, xem có thể biến đổi hàm hay không mà vẫn giữ nghĩa; không hợp nhất cửa sổ chỉ để bớt sort nếu quy tắc nghiệp vụ khác.\n\n### Ca biên và khi không nên dùng\n\nPartition rỗng không tạo hàng; partition một hàng cho `row_number=1`, `rank=1`, `lag/lead=NULL`. Giá trị sort `NULL` cần quy định `NULLS FIRST/LAST` để tránh ngạc nhiên. Top-N trên dữ liệu hòa phải quyết định giữ hòa. Tổng lũy kế với tiền `NULL` bỏ qua giá trị ấy, còn toàn frame `NULL` cho `sum=NULL`.\n\nKhông dùng window nếu chỉ cần một hàng tổng hợp mỗi khách: `GROUP BY` đơn giản và có thể giảm dữ liệu sớm hơn. Không dùng `LAG` thay temporal join khi cần “giá có hiệu lực gần nhất” từ bảng khác với quy tắc thời gian phức tạp. Không chạy một cửa sổ toàn lịch sử cho dashboard mỗi giây nếu có thể tiền tổng hợp hoặc giới hạn thời gian hợp lệ.",

vi_du: "### Báo cáo chuỗi đơn hàng hoàn chỉnh\n\n```sql\nCREATE TEMP TABLE orders (\n  order_id bigint PRIMARY KEY,\n  customer_id bigint NOT NULL,\n  ordered_at timestamptz NOT NULL,\n  status text NOT NULL,\n  total_amount numeric(12,2)\n);\nINSERT INTO orders VALUES\n (101,1,'2026-08-01 09:00+07','paid',100000),\n (102,1,'2026-08-03 09:00+07','paid',300000),\n (106,1,'2026-08-03 09:00+07','paid',300000),\n (103,2,'2026-08-02 09:00+07','cancelled',900000),\n (104,2,'2026-08-04 09:00+07','paid',100000),\n (105,3,'2026-08-05 09:00+07','paid',NULL);\n\nSELECT customer_id, order_id, ordered_at::date AS day, total_amount,\n       row_number() OVER w AS order_no,\n       lag(total_amount) OVER w AS previous_amount,\n       total_amount - lag(total_amount) OVER w AS change,\n       sum(total_amount) OVER (\n         PARTITION BY customer_id\n         ORDER BY ordered_at, order_id\n         ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW\n       ) AS running_total\nFROM orders\nWINDOW w AS (\n  PARTITION BY customer_id ORDER BY ordered_at, order_id\n)\nORDER BY customer_id, order_no;\n```\n\nVới An, thứ tự là 101, 102, 106 dù hai đơn cuối cùng giờ; `order_id` phá hòa. Biến động lần lượt là `NULL`, 200.000, 0 và tổng lũy kế 100.000, 400.000, 700.000. Với Chi, cả tiền và tổng là `NULL`, nhắc rằng thiếu dữ liệu không đồng nghĩa 0.\n\n### Xếp hạng doanh thu đã gom\n\n```sql\nWITH product_revenue(product_id, revenue) AS (\n  VALUES (10, 900000::numeric),\n         (11, 900000::numeric),\n         (12, 700000::numeric),\n         (13, 500000::numeric)\n)\nSELECT product_id, revenue,\n       row_number() OVER (ORDER BY revenue DESC, product_id) AS slot,\n       rank() OVER (ORDER BY revenue DESC) AS rank,\n       dense_rank() OVER (ORDER BY revenue DESC) AS dense_rank\nFROM product_revenue\nORDER BY slot;\n-- slot:       1,2,3,4\n-- rank:       1,1,3,4\n-- dense_rank: 1,1,2,3\n```\n\nCác dãy trên suy ra trực tiếp từ bốn hàng `VALUES`, không phải benchmark. Nếu lọc `rank <= 1`, hai sản phẩm doanh thu 900.000 cùng được giữ; `row_number <= 1` chỉ giữ sản phẩm 10 theo khóa phá hòa.",

so_sanh: "### Các hàm xếp hạng\n\n| Hàm | Hòa điểm | Sau hai hàng đồng hạng 1 | Dùng khi |\n|---|---|---|---|\n| `ROW_NUMBER` | vẫn cấp số riêng | hạng kế là 3 vì dãy 1,2,3 | cần đúng N hàng hoặc một đại diện xác định |\n| `RANK` | cùng hạng | hạng kế là 3, dãy 1,1,3 | thứ hạng thi đấu có khoảng trống |\n| `DENSE_RANK` | cùng hạng | hạng kế là 2, dãy 1,1,2 | phân tầng giá trị khác nhau liên tiếp |\n\n### Window, aggregate và self-join\n\n| Kỹ thuật | Giữ từng hàng? | Truy cập hàng trước | Chi phí/độ phức tạp nhận thức |\n|---|---|---|---|\n| Window function | có | trực tiếp bằng `LAG` | thường cần sort, cú pháp sát bài toán |\n| `GROUP BY` | không | không | tốt nhất khi chỉ cần một hàng mỗi nhóm |\n| Aggregate rồi nối lại | có sau phép nối | không tự nhiên | có thể cần scan/join thêm, hữu ích khi tái dùng aggregate |\n| Self-join theo thời gian | có thể | có nếu định nghĩa predecessor | điều kiện chống nhiều hàng phức tạp, đôi khi hợp temporal join |",

loi_thuong_gap: [
  "Không thêm khóa phá hòa cho `ROW_NUMBER`; test thỉnh thoảng đổi “đơn mới nhất” khi timestamp bằng nhau, cần order thêm khóa duy nhất.",
  "Nhét `RANK()` vào `WHERE` cùng tầng; PostgreSQL báo window function không được phép tại đó, phải tính trong subquery/CTE rồi lọc ngoài.",
  "Nhầm `LAG` là ngày trước; dữ liệu thiếu ngày làm khoảng cách nhảy, cần dựng calendar nếu câu hỏi theo ngày liên tục.",
  "Dùng `last_value` với frame mặc định và nhận chính hàng hiện tại; khai báo frame tới `UNBOUNDED FOLLOWING` khi muốn cuối partition.",
  "Thêm khóa duy nhất vào `ORDER BY` của `RANK` rồi vô tình phá nhóm hòa; giữ cửa sổ xếp hạng chỉ theo tiêu chí hòa, dùng khóa ngoài để hiển thị.",
  "Bỏ `PARTITION BY`; số thứ tự và hàng trước chạy xuyên khách, nhận biết ở ranh giới nơi khách mới không bắt đầu lại.",
  "Tạo index theo một chiều nhưng cửa sổ cần nhiều thứ tự khác nhau; plan vẫn có nhiều `Sort`, phải xem từng specification thay vì kỳ vọng một index cứu mọi báo cáo.",
  "Dùng window cho toàn lịch sử khi chỉ cần một tổng mỗi khách; lượng hàng không giảm và sort thừa, `GROUP BY` phù hợp hơn."
],

tu_kiem_tra: [
  "Giải thích tại sao `ORDER BY` trong `OVER` không cam kết thứ tự gửi kết quả về client.",
  "Với doanh thu 900, 900, 700, 500, hãy viết ba dãy do `ROW_NUMBER`, `RANK`, `DENSE_RANK` sinh ra.",
  "Thiết kế top ba sản phẩm: khi nào kết quả có thể hơn ba hàng và đó là bug hay yêu cầu đúng?",
  "Vì sao `LAG(amount, 1, 0)` có thể làm sai ý nghĩa dữ liệu ở hàng đầu partition?",
  "So sánh tổng ba hàng gần nhất bằng `ROWS` với tổng bảy ngày bằng `RANGE`; tạo một lịch đặt hàng khiến hai kết quả khác nhau.",
  "Giải thích frame mặc định khiến `last_value` gây bất ngờ và viết specification sửa nó.",
  "Nếu plan báo external merge sort, hãy nêu ba hướng đánh giá: giới hạn dữ liệu, index phù hợp, và bộ nhớ; vì sao không nên tăng `work_mem` toàn hệ thống ngay?"
],

tai_nguyen: [
  "PostgreSQL Documentation, phần Window Functions và Window Function Calls.",
  "“SQL for Data Analysis” của Cathy Tanimura, các chương phân tích chuỗi thời gian và window function.",
  "“SQL Cookbook” của Anthony Molinaro và Robert de Graaf, các công thức xếp hạng và running total.",
  "“SQL Performance Explained” của Markus Winand, phần sắp xếp, index và truy vấn top-N."
],

lien_he: [
  "Nối sang mảng Thuật toán qua sorting: nhiều window cần thứ tự toàn phần, nên chi phí sort và tính ổn định của khóa phá hòa trở thành vấn đề thuật toán thực tế.",
  "Nối sang mảng Phân tích dữ liệu: `LAG`, moving window và ranking là nền tảng cho retention, tăng trưởng kỳ trước và cohort report.",
  "Nối sang mảng Backend qua phân trang: `ROW_NUMBER` có thể đánh số kết quả, nhưng keyset pagination thường tránh phải xếp hạng và bỏ qua đoạn đầu lớn ở mỗi request.",
  "Nối sang mảng Kiểm thử: dữ liệu hòa điểm, timestamp trùng, partition một hàng và giá trị `NULL` là bộ ca bắt buộc để chứng minh semantics cửa sổ."
]

};
