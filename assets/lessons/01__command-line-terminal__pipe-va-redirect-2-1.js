window.LESSONS["01:command-line-terminal:pipe-va-redirect-2-1"] = {

tom_tat: [
  "Mỗi process sinh ra với **ba luồng** mở sẵn: **stdin (0)** nhận vào, **stdout (1)** trả kết quả, **stderr (2)** báo lỗi.",
  "Tách stdout khỏi stderr là quyết định thiết kế then chốt: nhờ đó bạn **lọc được kết quả mà vẫn thấy lỗi**.",
  "**Pipe `|` nối stdout của lệnh này vào stdin của lệnh kia**, và các lệnh chạy **đồng thời** chứ không lần lượt.",
  "`>` ghi đè, `>>` ghi nối. `2>&1` chuyển stderr về cùng chỗ với stdout — **thứ tự viết rất quan trọng**.",
  "Triết lý Unix: mỗi công cụ làm một việc và đọc/ghi văn bản, nên chúng **ghép được với nhau vô hạn**."
],

can_biet_truoc: [
  "Bài **Điều hướng** và **Thao tác file** — cần quen gõ lệnh.",
  "Bài **Process vs thread** — cần biết mỗi process có bảng file đang mở riêng.",
  "Bài **Tín hiệu và exit code** — cần biết exit code để hiểu `pipefail`."
],

dinh_nghia: "Nói đời thường: hình dung mỗi chương trình là một **cái máy có ba ống**. Ống vào để nhận nguyên liệu, ống ra để đẩy thành phẩm, và một ống riêng để xả khói báo sự cố. **Pipe** là nối ống ra của máy này vào ống vào của máy kia — thành một dây chuyền.\n\nNói chính xác:\n\n- **File descriptor** là số nguyên mà process dùng để chỉ một file đang mở. Ba số đầu được dành sẵn: `0` = stdin, `1` = stdout, `2` = stderr.\n- **Redirect** là đổi hướng một luồng từ terminal sang file hoặc luồng khác.\n- **Pipe** là một kênh trong bộ nhớ nối stdout của process này với stdin của process kia. Hai process chạy **đồng thời**.",

vi_sao_can: "Đây là chỗ terminal trở nên mạnh hơn hẳn giao diện đồ hoạ:\n\n- **Ghép công cụ thành thứ mới.** Không có công cụ nào tên là \"tìm 10 IP truy cập nhiều nhất\", nhưng ghép `grep | awk | sort | uniq | head` là có ngay.\n- **Đọc và lọc log.** File log 2 GB, mở bằng editor là treo máy. `grep ERROR app.log | tail -50` cho kết quả trong một giây.\n- **Ghi log cho dịch vụ.** Mọi script chạy nền đều cần `>> log 2>&1`. Viết sai là mất hết thông báo lỗi.\n- **Viết CI/CD đúng.** `set -o pipefail` quyết định pipeline có phát hiện được lỗi giữa chừng hay không.\n- **Tách kết quả khỏi thông báo.** Script của bạn phải ghi lỗi ra stderr, nếu không người dùng không lọc được đầu ra.\n\nTình huống thật: bạn chạy `python train.py > ket-qua.log &` rồi đi ngủ. Sáng ra file log **rỗng** dù chương trình đã chạy và báo lỗi. Nguyên nhân: lỗi đi ra stderr, mà bạn chỉ chuyển hướng stdout. Phần cơ chế cho biết viết đúng thế nào.",

co_che: "### Ba luồng chuẩn\n\nKhi hệ điều hành tạo một process, nó mở sẵn ba file descriptor:\n\n```\n           ┌─────────────────┐\n  stdin ──►│                 │──► stdout   (1) kết quả\n    (0)    │    Chương       │\n           │     trình       │──► stderr   (2) thông báo lỗi\n           └─────────────────┘\n```\n\nMặc định cả ba đều nối với terminal: bạn gõ vào stdin, và cả stdout lẫn stderr đều hiện lên màn hình. Vì cùng hiện ra nên **nhìn qua tưởng chúng là một** — nhưng chúng hoàn toàn tách biệt.\n\n### Vì sao phải tách stdout và stderr\n\nĐây là một trong những quyết định thiết kế đắt giá nhất của Unix. Lợi ích lộ ra ngay khi bạn chuyển hướng:\n\n```bash\ntim_kiem_du_lieu > ket-qua.txt\n# Kết quả vào file, còn LỖI vẫn hiện trên màn hình → bạn biết ngay có sự cố\n```\n\nNếu hai luồng gộp làm một thì thông báo lỗi sẽ **lẫn vào file kết quả**, làm hỏng dữ liệu và bạn không nhìn thấy lỗi.\n\nQuy tắc cho code bạn viết:\n\n```python\nimport sys\nprint(\"kết quả tính toán\")                    # ✓ stdout — dữ liệu\nprint(\"Cảnh báo: thiếu cột\", file=sys.stderr) # ✓ stderr — thông báo\n```\n\nMọi thứ **không phải dữ liệu đầu ra** đều nên đi ra stderr: tiến độ, cảnh báo, lỗi, log. Nhờ vậy `chuong_trinh > out.txt` cho file sạch, và `chuong_trinh 2> loi.txt` tách riêng được thông báo.\n\n### Chuyển hướng cơ bản\n\n```bash\nlenh > file          # stdout → file, GHI ĐÈ (xoá sạch nội dung cũ)\nlenh >> file         # stdout → file, GHI NỐI vào cuối\nlenh < file          # file → stdin\nlenh 2> file         # stderr → file\nlenh 2>> file        # stderr → file, ghi nối\n\nlenh > /dev/null     # vứt stdout đi\nlenh 2> /dev/null    # vứt stderr đi — ⚠️ giấu luôn lỗi thật\n```\n\n`>` và `>>` khác nhau ở một chỗ rất quan trọng khi ghi log: `>` **xoá sạch file rồi mới ghi**. Dùng nhầm là mất toàn bộ log cũ.\n\n`/dev/null` là một file đặc biệt: mọi thứ ghi vào đều biến mất. Nó là \"thùng rác\" của Unix.\n\n### `2>&1` — cú pháp gây bối rối nhất\n\nĐọc là: *\"chuyển file descriptor 2 tới cùng chỗ mà file descriptor 1 đang trỏ\"*. Dấu `&` báo cho shell biết `1` là **số hiệu luồng**, không phải tên file. Không có `&` thì `2>1` sẽ tạo ra một file tên là `1`.\n\n**Thứ tự viết quyết định kết quả** — đây là chỗ sai nhiều nhất:\n\n```bash\n# ✓ ĐÚNG — cả hai vào file\nlenh > log.txt 2>&1\n#      ①          ②\n# ① stdout → log.txt\n# ② stderr → \"chỗ mà stdout đang trỏ\" = log.txt ✓\n\n# ✗ SAI — stderr vẫn ra màn hình\nlenh 2>&1 > log.txt\n#      ①      ②\n# ① stderr → \"chỗ mà stdout đang trỏ\" = MÀN HÌNH (chưa chuyển hướng)\n# ② stdout → log.txt\n# → stderr đã bị gán vào màn hình từ trước, đổi stdout sau không ảnh hưởng nó\n```\n\nMẹo nhớ: `2>&1` **sao chép đích hiện tại của luồng 1**, nên phải đặt nó **sau** khi đã chuyển hướng luồng 1.\n\nBash có cách viết gọn cho trường hợp phổ biến:\n\n```bash\nlenh &> log.txt       # tương đương: lenh > log.txt 2>&1\nlenh &>> log.txt      # ghi nối cả hai\n```\n\nDạng gọn này chỉ có trong bash và zsh, không có trong `sh` thuần — nên script cần chạy ở mọi nơi thì dùng dạng đầy đủ.\n\n### Pipe: nối các lệnh\n\n```bash\nlenh_a | lenh_b       # stdout của a → stdin của b\n```\n\nHai điều quan trọng mà nhiều người hiểu sai:\n\n**① Các lệnh chạy ĐỒNG THỜI, không lần lượt.**\n\n```bash\ncat file-10GB.txt | grep loi\n```\n\n`grep` **không chờ** `cat` đọc xong. Cả hai chạy cùng lúc: `cat` đọc và đẩy vào pipe, `grep` lấy ra và xử lý ngay. Nhờ vậy pipeline xử lý được file lớn hơn RAM.\n\nKhi pipe đầy (bộ đệm khoảng 64 KB), `cat` bị chặn cho tới khi `grep` tiêu thụ bớt. Đây là cơ chế **áp lực ngược** tự nhiên, hoạt động mà không cần ai cấu hình gì.\n\n**② Pipe chỉ chuyển stdout, KHÔNG chuyển stderr.**\n\n```bash\nlenh_loi | grep gi_do\n# Thông báo lỗi của lenh_loi vẫn hiện thẳng ra màn hình,\n# không đi qua grep\n```\n\nMuốn lọc cả stderr thì phải gộp trước:\n\n```bash\nlenh 2>&1 | grep loi\n```\n\n### Exit code của pipeline\n\nMặc định, exit code của cả pipeline là exit code của **lệnh cuối cùng**:\n\n```bash\nlenh_that_bai | grep gi_do\necho $?        # 0 hoặc 1 tuỳ grep — LỖI của lệnh đầu bị nuốt mất\n```\n\nĐây là cái bẫy nghiêm trọng trong CI/CD: bước build thất bại nhưng pipeline vẫn báo thành công.\n\n```bash\nset -o pipefail\nlenh_that_bai | grep gi_do\necho $?        # ← giờ trả về mã lỗi của lệnh ĐẦU TIÊN thất bại\n```\n\nĐây là lý do dòng `set -euo pipefail` xuất hiện ở đầu mọi script nghiêm túc. Chữ `pipefail` chính là để xử lý chuyện này.\n\nMuốn xem mã của từng lệnh trong pipeline:\n\n```bash\nlenh_a | lenh_b | lenh_c\necho \"${PIPESTATUS[@]}\"    # ví dụ: 1 0 0 → lệnh đầu thất bại\n```\n\n### `tee` — vừa xem vừa lưu\n\nPipe có một hạn chế: dữ liệu đi qua thì không thấy được. `tee` giải quyết:\n\n```bash\nlenh | tee log.txt              # ghi vào file VÀ vẫn hiện ra màn hình\nlenh | tee -a log.txt           # ghi nối thay vì ghi đè\nlenh | tee log.txt | grep loi   # ghi bản đầy đủ, màn hình chỉ hiện dòng lỗi\n```\n\nTên gọi đến từ hình chữ T trong ống nước: dòng chảy tách làm hai.\n\nMột công dụng rất hay dùng:\n\n```bash\necho \"noi dung\" | sudo tee /etc/cau-hinh > /dev/null\n```\n\nVì sao không viết `sudo echo \"...\" > /etc/cau-hinh`? Vì **chuyển hướng do shell thực hiện, không phải do `sudo`** — shell chạy với quyền người dùng thường nên không mở được file trong `/etc`. `tee` thì chạy dưới `sudo` nên ghi được.\n\n### `xargs` — biến đầu vào thành đối số\n\nMột số lệnh không đọc stdin mà chỉ nhận đối số. `xargs` làm cầu nối:\n\n```bash\n# ✗ rm không đọc stdin\nfind . -name '*.tmp' | rm\n\n# ✓ xargs biến từng dòng thành đối số cho rm\nfind . -name '*.tmp' | xargs rm\n\n# ⭐ AN TOÀN với tên file có dấu cách\nfind . -name '*.tmp' -print0 | xargs -0 rm\n```\n\nCặp `-print0` và `-0` dùng ký tự null làm dấu ngăn cách thay vì xuống dòng. Bắt buộc phải dùng nếu tên file có thể chứa dấu cách hoặc ký tự lạ — nếu không, `file cua toi.tmp` bị hiểu thành ba file riêng.\n\n`xargs` còn chạy song song được:\n\n```bash\nls *.png | xargs -P 4 -I {} convert {} {}.jpg\n#          │       │  └─ {} là chỗ thay thế cho mỗi dòng đầu vào\n#          │       └──── chạy 4 tiến trình song song\n```",

vi_du: "### Chứng minh stdout và stderr tách biệt\n\n```bash\n# ls thư mục có thật và thư mục không có\nls /tmp /khong-ton-tai\n# ls: cannot access '/khong-ton-tai': No such file...   ← stderr\n# /tmp:                                                  ← stdout\n# ...\n\nls /tmp /khong-ton-tai > ket-qua.txt\n# Chỉ còn dòng lỗi hiện trên màn hình → stderr KHÔNG vào file ✓\n\nls /tmp /khong-ton-tai 2> loi.txt\n# Chỉ còn kết quả hiện ra → lỗi đã vào file riêng ✓\n\nls /tmp /khong-ton-tai > ket-qua.txt 2> loi.txt\n# Màn hình sạch — hai luồng vào hai file khác nhau\n```\n\n### Cái bẫy log rỗng\n\n```bash\n# ❌ SAI — chỉ chuyển stdout, mọi lỗi bay mất\npython train.py > train.log &\n\n# ✓ ĐÚNG — cả hai vào file\npython train.py > train.log 2>&1 &\n\n# ✓ Tốt hơn nữa — vừa lưu vừa xem được realtime\npython train.py 2>&1 | tee train.log\n```\n\nDạng cuối là mẫu nên dùng khi chạy tác vụ dài: bạn theo dõi được tiến độ mà vẫn có bản ghi đầy đủ.\n\nMột lưu ý khi dùng `tee` với Python: Python đệm stdout khi ghi vào pipe, nên output có thể hiện chậm. Thêm `-u` để tắt đệm:\n\n```bash\npython -u train.py 2>&1 | tee train.log\n```\n\n### Phân tích log bằng pipeline\n\n```bash\n# Tìm 10 IP truy cập nhiều nhất trong access log\ncat access.log \\\n  | awk '{print $1}' \\\n  | sort \\\n  | uniq -c \\\n  | sort -rn \\\n  | head -10\n```\n\nTừng bước:\n\n```\ncat access.log     đọc toàn bộ file\n  ↓\nawk '{print $1}'   lấy cột 1 = địa chỉ IP\n  ↓\nsort               sắp xếp — BẮT BUỘC, vì uniq chỉ gộp các dòng LIỀN KỀ\n  ↓\nuniq -c            gộp dòng trùng, thêm số đếm ở đầu\n  ↓\nsort -rn           sắp theo SỐ (-n), giảm dần (-r)\n  ↓\nhead -10           lấy 10 dòng đầu\n```\n\nKết quả:\n\n```\n   1523 203.0.113.42\n    891 198.51.100.7\n    445 192.0.2.15\n```\n\nBước `sort` trước `uniq` là chỗ hay quên nhất. `uniq` chỉ gộp các dòng **liền kề nhau**, nên không sắp trước thì kết quả sai hoàn toàn.\n\nKhông có công cụ nào tên là \"đếm IP\" — bạn vừa tạo ra nó bằng cách ghép năm công cụ nhỏ. Đó là triết lý Unix.\n\n### Đếm lỗi theo giờ\n\n```bash\ngrep ERROR app.log \\\n  | awk '{print $2}' \\\n  | cut -d: -f1 \\\n  | sort \\\n  | uniq -c\n```\n\n```\n     12 09\n    847 10       ← 10 giờ có sự cố\n     23 11\n```\n\nPipeline này cho thấy ngay khoảng thời gian bất thường — nhanh hơn nhiều so với cuộn tay qua file log.\n\n### Xử lý file lớn hơn RAM\n\n```bash\n# File 50 GB, máy chỉ 16 GB RAM\nzcat log-khong-lo.gz | grep 'user_id=12345' | head -100\n```\n\nChạy được vì pipeline **truyền theo dòng chảy**: `zcat` giải nén từng phần, `grep` lọc ngay, `head` lấy đủ 100 dòng rồi **đóng pipe**, khiến các lệnh trước nhận SIGPIPE và dừng. Không bao giờ có quá 64 KB nằm trong bộ nhớ cùng lúc.\n\nĐây là ưu điểm lớn của pipe so với việc nạp cả file vào biến.\n\n### Bẫy pipefail trong CI\n\n```bash\n#!/usr/bin/env bash\nset -e                        # chỉ set -e là CHƯA ĐỦ\n\nchay_test | tee ket-qua.txt   # test thất bại nhưng tee thành công\necho \"Build thành công\"       # ← vẫn in ra! CI báo xanh dù test đỏ\n```\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail             # ✓ giờ pipeline thất bại nếu BẤT KỲ lệnh nào lỗi\n\nchay_test | tee ket-qua.txt\necho \"Build thành công\"       # không chạy tới đây nữa\n```\n\nĐây là một trong những bug CI khó chịu nhất: test đỏ mà pipeline xanh, và không ai để ý cho tới khi lỗi lên production.\n\n### `xargs` với tên file có dấu cách\n\n```bash\ntouch \"file co dau cach.tmp\"\n\n# ❌ SAI — bị tách thành 4 file riêng\nfind . -name '*.tmp' | xargs rm\n# rm: cannot remove './file': No such file or directory\n# rm: cannot remove 'co': No such file or directory\n\n# ✓ ĐÚNG — dùng ký tự null làm dấu ngăn\nfind . -name '*.tmp' -print0 | xargs -0 rm\n\n# ✓ Hoặc để find tự xoá, không cần xargs\nfind . -name '*.tmp' -delete\n```\n\n### Here-document: truyền nhiều dòng vào stdin\n\n```bash\ncat << 'EOF' > cau-hinh.yml\nserver:\n  port: 8080\n  host: localhost\nEOF\n```\n\nDấu nháy quanh `'EOF'` rất quan trọng: **có nháy thì nội dung giữ nguyên văn**, không có nháy thì shell bung biến `$BIEN` và lệnh `` `cmd` `` bên trong. Với file cấu hình chứa `$` thì phải có nháy.\n\n```bash\npsql << 'EOF'\nSELECT count(*) FROM nguoi_dung;\nEOF\n```",

so_sanh: "### Các dạng chuyển hướng\n\n| Cú pháp | Nghĩa |\n|---|---|\n| `> f` | stdout → f, ghi đè |\n| `>> f` | stdout → f, ghi nối |\n| `2> f` | stderr → f |\n| `&> f` | cả hai → f (bash/zsh) |\n| `> f 2>&1` | cả hai → f (mọi shell) |\n| `2>&1 > f` | **stdout → f, stderr ra màn hình** — thường là nhầm |\n| `< f` | f → stdin |\n| `<<EOF` | here-document → stdin |\n| `> /dev/null` | vứt bỏ |\n\n### Pipe và file tạm\n\n| | Pipe | Ghi ra file tạm |\n|---|---|---|\n| Bộ nhớ | **~64 KB** bất kể dữ liệu lớn cỡ nào | Bằng kích thước dữ liệu |\n| Tốc độ | **Nhanh, chạy đồng thời** | Chậm hơn, phải chờ ghi xong |\n| Xử lý được file > RAM | **✓** | Cần đủ dung lượng đĩa |\n| Xem lại kết quả trung gian | ✗ | **✓** |\n| Dọn dẹp | Tự động | Phải tự xoá |\n\nDùng file tạm khi cần **kiểm tra kết quả trung gian** lúc gỡ lỗi, hoặc khi một bước cần đọc dữ liệu nhiều lần. Còn lại thì pipe tốt hơn ở mọi mặt.\n\n### Triết lý Unix\n\nDoug McIlroy, người phát minh ra pipe, tóm tắt:\n\n> *Viết chương trình làm một việc và làm tốt. Viết chương trình phối hợp được với nhau. Viết chương trình xử lý luồng văn bản, vì đó là giao diện phổ quát.*\n\nBa câu này giải thích vì sao Unix vẫn sống sau năm mươi năm. Mỗi công cụ nhỏ và ngu ngốc, nhưng **văn bản là giao diện chung** nên chúng ghép được với nhau theo vô số cách mà người tạo ra chúng chưa từng nghĩ tới.\n\nSo sánh: một ứng dụng đồ hoạ có 100 tính năng thì bạn dùng được đúng 100 tính năng đó. Mười công cụ dòng lệnh ghép lại cho bạn hàng nghìn tổ hợp.\n\nĐánh đổi thật của triết lý này: **văn bản không có cấu trúc**, nên phải dùng `awk` và `cut` để tách cột — dễ vỡ khi định dạng đổi. PowerShell đi hướng khác, truyền **object** qua pipe nên không cần phân tích văn bản. Cách đó chặt chẽ hơn nhưng chỉ hoạt động trong hệ sinh thái riêng của nó.\n\nHướng dung hoà đang phổ biến: dùng **JSON** làm định dạng trung gian và `jq` để xử lý.\n\n```bash\ncurl -s api.example.com/users | jq '.[] | select(.active) | .email'\n```\n\nGiữ được tính ghép nối của Unix mà có thêm cấu trúc.",

loi_thuong_gap: [
  "**Viết `2>&1` trước khi chuyển hướng stdout.** `lenh 2>&1 > log` không gộp được. Thứ tự đúng: `lenh > log 2>&1`.",
  "**Chỉ chuyển stdout khi chạy nền.** `lenh > log &` làm mất hết thông báo lỗi. Phải `> log 2>&1`.",
  "**Dùng `>` khi định ghi nối.** Nó xoá sạch file cũ. Ghi log thì luôn dùng `>>`.",
  "**Quên `sort` trước `uniq`.** `uniq` chỉ gộp dòng liền kề, không sắp trước thì kết quả sai.",
  "**Quên `set -o pipefail`.** Lỗi ở giữa pipeline bị nuốt, CI báo xanh dù test đỏ.",
  "**Dùng `xargs` với tên file có dấu cách.** Phải dùng `-print0` và `-0`, hoặc để `find` tự làm bằng `-delete`.",
  "**`sudo lenh > /etc/file`.** Chuyển hướng do shell làm với quyền thường nên vẫn bị từ chối. Dùng `| sudo tee`.",
  "**Vứt stderr bằng `2>/dev/null` để cho đỡ ồn.** Nó giấu luôn lỗi thật. Chỉ dùng khi biết chắc lỗi đó vô hại.",
  "**Tưởng pipe chạy lần lượt.** Các lệnh chạy đồng thời — đó là lý do pipeline xử lý được file lớn hơn RAM."
],

tu_kiem_tra: [
  "Vì sao Unix tách stdout và stderr thành hai luồng? Nêu một lợi ích cụ thể.",
  "`lenh > log 2>&1` và `lenh 2>&1 > log` khác nhau thế nào? Giải thích theo thứ tự shell xử lý.",
  "`cat file-10GB.txt | grep loi` — `grep` có phải chờ `cat` đọc xong không? Điều đó dẫn tới ưu điểm gì?",
  "Vì sao phải `sort` trước `uniq -c`?",
  "Test thất bại nhưng CI báo xanh với `chay_test | tee log`. Nguyên nhân là gì và sửa thế nào?",
  "Vì sao `sudo echo \"x\" > /etc/file` bị từ chối, còn `echo \"x\" | sudo tee /etc/file` thì được?",
  "Viết pipeline tìm 5 mã lỗi HTTP xuất hiện nhiều nhất trong access log (mã ở cột 9)."
],

tai_nguyen: [
  "Khoá **The Missing Semester of Your CS Education** (MIT), bài 2 *Shell Tools and Scripting*.",
  "Sách **The Linux Command Line** — William Shotts, chương 6 *Redirection*. Đọc miễn phí.",
  "Tài liệu bash, mục *Redirections* — mô tả chính xác thứ tự xử lý các toán tử chuyển hướng.",
  "Tài liệu Linux: `man 7 pipe`, `man xargs`, `man tee`.",
  "Trang **explainshell.com** — dán pipeline dài vào để xem giải thích từng phần."
],

lien_he: [
  "**Process vs thread**: ba luồng chuẩn chính là ba file descriptor đầu tiên trong bảng file đang mở của process.",
  "**Tín hiệu và exit code**: `pipefail` và `PIPESTATUS` đều xoay quanh exit code; `head` đóng pipe gây SIGPIPE cho lệnh phía trước.",
  "**grep, find, sed, awk** (bài kế tiếp): bốn công cụ này chỉ phát huy hết sức mạnh khi ghép qua pipe.",
  "**Viết shell script** (bài cuối module): `set -euo pipefail` là dòng đầu tiên của mọi script nghiêm túc.",
  "**Docker** ở mảng 05: log container chính là stdout và stderr của PID 1 — đó là lý do ứng dụng trong container nên ghi log ra stdout thay vì ra file.",
  "**Testing & CI/CD** ở mảng 05: thiếu `pipefail` là nguyên nhân kinh điển của pipeline báo xanh sai."
]

};
