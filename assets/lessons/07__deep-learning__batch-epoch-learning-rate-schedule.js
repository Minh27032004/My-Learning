window.LESSONS["07:deep-learning:batch-epoch-learning-rate-schedule"] = {

tom_tat: [
  "Batch là nhóm mẫu dùng để ước lượng gradient; batch nhỏ tạo nhiễu nhưng cập nhật thường xuyên, batch lớn ổn định hơn nhưng tốn bộ nhớ và có thể tổng quát hóa kém.",
  "Một epoch là một lượt mô hình đã có cơ hội nhìn toàn bộ tập huấn luyện; số epoch không cho biết số lần cập nhật nếu chưa biết kích thước batch.",
  "Learning rate quyết định độ dài mỗi bước của optimizer, còn schedule thay đổi độ dài ấy theo tiến độ để vừa đi nhanh lúc đầu vừa tinh chỉnh ở cuối.",
  "Ba núm chỉnh liên kết với nhau qua số bước cập nhật, độ nhiễu gradient và ngân sách tính toán; không nên chọn từng núm độc lập.",
  "Ví dụ xuyên module dùng ảnh lá cây 32×32 bốn lớp và ghi lịch sử theo từng bước để phân biệt lỗi do dữ liệu, lịch học hay mô hình."
],

can_biet_truoc: [
  "Gradient và lan truyền ngược: đạo hàm cho biết hướng thay đổi tham số làm loss tăng nhanh nhất.",
  "Optimizer SGD, momentum và Adam; đặc biệt là ý nghĩa của `optimizer.step()` và `optimizer.zero_grad()`.",
  "Tập huấn luyện, validation và test; validation dùng để chọn cấu hình chứ không cập nhật trọng số.",
  "Tensor, `Dataset` và `DataLoader` cơ bản trong PyTorch."
],

dinh_nghia: "**Batch** là một nhóm mẫu được đưa qua mạng trước khi gom loss và tính một lần cập nhật tham số. **Batch size**, ký hiệu B, là số mẫu trong nhóm. **Epoch** là một lượt qua toàn bộ N mẫu huấn luyện; nếu không bỏ phần dư thì mỗi epoch có `ceil(N/B)` bước. **Learning rate**, ký hiệu η, là hệ số nhân với hướng cập nhật. Với SGD tối giản, θ ← θ − ηg, trong đó θ là toàn bộ tham số và g là gradient ước lượng từ batch hiện tại.\n\n**Learning rate schedule** là quy tắc thay η theo số bước hoặc epoch. Nó không thay mục tiêu học; nó thay cách ta di chuyển trên bề mặt loss. Trực giác: đầu hành trình cần bước dài để rời vùng khởi tạo nhanh, gần vùng tốt cần bước ngắn để không nhảy qua lại quanh đáy.",

vi_sao_can: "Tập ảnh lá cây có thể chứa hàng chục nghìn ảnh, nên tính gradient trên toàn bộ tập rồi mới cập nhật vừa chậm vừa không vừa bộ nhớ. Ngược lại, cập nhật sau từng ảnh làm hướng đi rung mạnh theo nhiễu riêng của ảnh đó. Batch giải quyết bài toán thực dụng: lấy một mẫu đủ nhỏ để chạy được, nhưng đủ lớn để gradient phản ánh nhiều lá.\n\nNgay cả khi kiến trúc và optimizer đúng, một η quá lớn làm loss nổ hoặc dao động; η quá nhỏ cho đường loss đẹp nhưng mô hình chưa học xong khi ngân sách hết. Giữ η cố định lại buộc ta chọn giữa tốc độ đầu kỳ và độ tinh ở cuối kỳ. Schedule ra đời để tách hai nhu cầu ấy theo thời gian.\n\nEpoch là đơn vị báo cáo thuận tiện nhưng dễ đánh lừa. Với N = 2.400 ảnh, B = 24 tạo 100 cập nhật mỗi epoch, còn B = 96 chỉ tạo 25. Nói “huấn luyện 30 epoch” mà thiếu B, η và cách schedule bước là thiếu phần quyết định động lực học. Vì vậy bài này coi **số optimizer step** là đồng hồ vật lý, còn epoch là mốc tổ chức dữ liệu.",

co_che: "### Một batch tạo ra một hướng đi như thế nào\n\nGiả sử loss của mẫu i là ℓᵢ(θ). Gradient toàn tập là trung bình của N gradient, nhưng ta chỉ lấy B mẫu trong batch: g_B = (1/B)Σ∇ℓᵢ(θ). Mỗi ký hiệu có nghĩa cụ thể: θ là trọng số hiện tại; ∇ℓᵢ là độ dốc do một ảnh gây ra; Σ cộng các độ dốc; chia B lấy trung bình. Nếu batch được lấy ngẫu nhiên, g_B là một ước lượng của gradient toàn tập.\n\nƯớc lượng không hoàn hảo chính là điểm hay. Hai batch khác nhau kéo mô hình hơi khác hướng; nhiễu này có thể giúp thoát vùng phẳng hoặc vùng chỉ khớp dữ liệu huấn luyện. Nhưng nhiễu quá lớn khiến đường đi lảo đảo. B là núm điều khiển mức trung bình hóa và đồng thời quyết định RAM/VRAM.\n\n### Từ kích thước tập tới số lần cập nhật\n\nVới N mẫu và batch B, số batch mỗi epoch là ceil(N/B) khi giữ batch cuối. PyTorch mặc định `drop_last=False`, vì vậy batch cuối có thể nhỏ hơn. Nếu `drop_last=True`, số bước là floor(N/B) và một số mẫu không được dùng trong epoch đó. Đây không chỉ là chi tiết đếm: batch cuối quá nhỏ có thể làm thống kê Batch Normalization nhiễu, chủ đề bài 7.\n\nSau E epoch, số cập nhật gần bằng E·ceil(N/B). Đổi B từ 32 lên 128 mà giữ E sẽ chia số cập nhật khoảng bốn lần. Nếu giữ nguyên schedule tính theo epoch, learning rate giảm sau số cập nhật ít hơn nhiều so với thí nghiệm cũ. So sánh công bằng thường phải ghi cả số mẫu đã xử lý và số bước.\n\n### Shuffle ngăn batch mang cấu trúc giả\n\nNếu file ảnh được xếp theo thư mục lớp rồi `shuffle=False`, các batch đầu toàn lá khỏe, tiếp theo toàn bệnh gỉ. Gradient liên tiếp có thiên lệch, mô hình cứ học lớp mới rồi phá lớp cũ. Xáo trộn mỗi epoch làm mỗi batch gần hơn với phân phối chung.\n\nXáo trộn không sửa được mất cân bằng lớp. Nếu 80% ảnh là healthy, batch ngẫu nhiên vẫn thường nghiêng về healthy. Khi đó cần sampler theo trọng số, loss có trọng số, hoặc thu thập dữ liệu; không nên kỳ vọng tăng epoch tự sinh ra thông tin của lớp hiếm.\n\n### Batch nhỏ và batch lớn đổi động lực học\n\nBatch nhỏ có ba đặc điểm: dùng ít bộ nhớ, có nhiều optimizer step trên cùng một epoch, và gradient nhiễu hơn. Batch lớn tận dụng phần cứng song song tốt hơn tới một ngưỡng, nhưng mỗi epoch cập nhật ít lần và có thể hội tụ vào nghiệm sắc hơn. “Lớn nhanh hơn” chỉ đúng về throughput, không tự động đúng về thời gian đạt chất lượng validation.\n\nKhi tăng B, tổng gradient trung bình thường ít nhiễu và có độ lớn tương tự vì loss mặc định lấy mean. Một quy tắc thử nghiệm là tăng η gần tỷ lệ với B, gọi là linear scaling, nhưng đó không phải định luật. Adam, warmup, mất cân bằng và kiến trúc làm quy tắc lệch. Phải kiểm tra loss vài trăm bước đầu.\n\n### Learning rate là độ dài bước, không phải tốc độ đồng hồ\n\nTrong θ ← θ − ηg, η lớn phóng đại mọi thành phần gradient. Nếu bề mặt loss cong mạnh, bước lớn có thể vượt qua thung lũng sang phía đối diện, tạo dao động. Nếu η còn lớn hơn, mỗi lần vượt làm tham số xa hơn và loss thành `nan`. η nhỏ làm chuyển động ổn định nhưng có thể gần như đứng yên ở lớp sâu có gradient nhỏ.\n\nVới momentum hoặc Adam, hướng thực tế còn chứa lịch sử. Vì vậy giảm η không xóa ngay vận tốc tích lũy. Khi debug một cú nhảy loss sau mốc schedule, cần xem cả optimizer state thay vì chỉ nhìn giá trị η.\n\n### Warmup bảo vệ giai đoạn đầu dễ vỡ\n\nỞ vài bước đầu, trọng số ngẫu nhiên và thống kê kích hoạt chưa ổn định; gradient có thể lớn và không đại diện. Warmup bắt đầu bằng η thấp rồi tăng dần tới η mục tiêu. Nó đặc biệt hữu ích với batch lớn hoặc mô hình sâu, nơi một bước đầu quá mạnh có thể đưa tham số vào vùng khó phục hồi.\n\nWarmup không chữa η cực đại chọn sai. Nếu sau warmup loss vẫn dao động, cần giảm đỉnh η. Dùng warmup dài quá mức lại lãng phí phần đầu ngân sách bằng những bước quá nhỏ.\n\n### Step decay, cosine và OneCycle kể ba câu chuyện khác nhau\n\nStep decay giảm η tại vài mốc định trước. Nó dễ giải thích và tái lập, nhưng mốc 30/60 epoch chỉ hợp khi ngân sách cố định. Cosine annealing giảm trơn từ η_max xuống η_min theo hình nửa cos; không có cú đổi đột ngột và hợp khi biết trước tổng số bước. OneCycle tăng η sau warmup rồi giảm sâu ở cuối, đồng thời thường điều chỉnh momentum ngược chiều; nó ép quá trình khám phá rồi tinh chỉnh trong một chu kỳ.\n\n`ReduceLROnPlateau` khác hẳn: nó quan sát metric validation và giảm khi metric không cải thiện. Vì nhận metric, nó gọi sau validation; các scheduler theo bước không cần metric. Nhầm vị trí `scheduler.step()` là lỗi API có ảnh hưởng thật tới lịch.\n\n### Đồng hồ của scheduler: batch hay epoch\n\nMột scheduler có thể bước sau mỗi optimizer step hoặc mỗi epoch. Cosine với `T_max` tính theo epoch mà lại gọi mỗi batch sẽ hoàn thành chu kỳ sớm gấp số batch trong epoch. Ngược lại, OneCycle được cấu hình theo tổng step nhưng chỉ gọi cuối epoch gần như không thay η.\n\nHãy ghi log `optimizer.param_groups[0][\"lr\"]` theo global step. Biểu đồ η thực tế là bằng chứng mạnh hơn việc đọc cấu hình, vì nó lộ lỗi gọi thừa, gọi thiếu và resume checkpoint sai vị trí.\n\n### Gradient accumulation tạo batch hiệu dụng\n\nKhi VRAM chỉ chứa 16 ảnh nhưng muốn gradient trung bình của 64 ảnh, có thể chạy bốn micro-batch, chia loss cho 4, gọi `backward()` bốn lần rồi mới `step()`. Batch hiệu dụng là 64, nhưng Batch Normalization vẫn chỉ thấy từng micro-batch 16; thời gian cũng không giống một batch 64 chạy song song.\n\nPhải gọi `zero_grad()` sau lần cập nhật, không phải sau mỗi micro-batch. Nếu quên chia loss, gradient lớn gấp bốn; đôi khi có thể bù bằng η nhưng làm ý nghĩa cấu hình khó kiểm soát.\n\n### Khi nào không nên kéo dài thêm epoch\n\nNếu training loss còn giảm nhưng validation loss tăng, thêm epoch đang làm học thuộc; bài regularization sẽ xử lý. Nếu cả hai đứng yên và η đã rất nhỏ, schedule có thể đã “đóng băng” quá sớm. Nếu cả hai cao ngay từ đầu, lỗi có thể nằm ở dữ liệu, nhãn, kiến trúc hoặc η, không phải thiếu epoch.\n\nDừng theo ngân sách epoch chỉ hợp cho baseline tái lập. Trong sản phẩm, nên theo dõi metric validation, thời gian, năng lượng và checkpoint tốt nhất. Một epoch trên tập dữ liệu tăng gấp đôi không còn là cùng lượng công việc.\n\n### Ca biên cần định nghĩa trước\n\nTập rỗng phải báo lỗi trước khi tạo lịch; `ceil(0/B)=0` khiến vòng lặp im lặng và metric chia cho 0. B lớn hơn N tạo đúng một batch, nhưng Batch Normalization có thể lỗi nếu batch chỉ một mẫu. Batch cuối một mẫu cần `drop_last=True`, đổi normalization, hoặc gom dữ liệu khác. Resume giữa epoch cần lưu global step và trạng thái scheduler; chỉ lưu model khiến η quay về đầu lịch dù trọng số đã ở cuối hành trình.",

vi_du: "### Thí nghiệm PyTorch tái lập trên ảnh lá tổng hợp\n\nĐoạn code sau chạy độc lập, tạo ảnh 32×32 với bốn mẫu hình đại diện cho bốn lớp. Đây là dữ liệu kiểm tra pipeline, không phải tuyên bố benchmark ngoài đời.\n\n```python\nimport math, random, torch\nfrom torch import nn\nfrom torch.utils.data import DataLoader, TensorDataset, random_split\n\ntorch.manual_seed(7); random.seed(7)\ndef make_data(n=2400):\n    x = torch.randn(n, 3, 32, 32) * 0.12\n    y = torch.arange(n) % 4\n    for i, c in enumerate(y.tolist()):\n        if c == 0: x[i, 1] += 0.35\n        elif c == 1: x[i, 0, :, ::4] += 0.8\n        elif c == 2: x[i, :, 8:24, 8:24] += 0.35\n        else: x[i, 2, ::2, ::2] += 0.55\n    return TensorDataset(x, y)\n\nfull = make_data()\ntrain, valid = random_split(full, [1920, 480], generator=torch.Generator().manual_seed(7))\nmodel = nn.Sequential(nn.Flatten(), nn.Linear(3072, 64), nn.ReLU(), nn.Linear(64, 4))\nloader = DataLoader(train, batch_size=48, shuffle=True)\nvalid_loader = DataLoader(valid, batch_size=96)\nopt = torch.optim.SGD(model.parameters(), lr=0.08, momentum=0.9)\ntotal_steps = 12 * len(loader)\nsched = torch.optim.lr_scheduler.CosineAnnealingLR(opt, T_max=total_steps, eta_min=0.001)\n\ndef accuracy(dl):\n    model.eval(); correct = total = 0\n    with torch.no_grad():\n        for xb, yb in dl:\n            correct += (model(xb).argmax(1) == yb).sum().item(); total += len(yb)\n    return correct / total\n\nglobal_step = 0\nfor epoch in range(12):\n    model.train()\n    for xb, yb in loader:\n        opt.zero_grad()\n        loss = nn.functional.cross_entropy(model(xb), yb)\n        loss.backward(); opt.step(); sched.step()\n        global_step += 1\n    print(epoch + 1, global_step, f\"lr={opt.param_groups[0]['lr']:.5f}\", f\"val={accuracy(valid_loader):.3f}\")\n```\n\nĐiểm cần kiểm không phải một con số cố định giữa mọi máy, mà là các bất biến: mỗi epoch có 40 bước vì 1920/48; sau 12 epoch có 480 bước; η giảm theo từng batch và không âm; validation được tính trong `eval()` và không có gradient.\n\n### Kiểm tra lịch trước khi đốt thời gian huấn luyện\n\n```python\nprobe = nn.Linear(1, 1)\nprobe_opt = torch.optim.SGD(probe.parameters(), lr=0.1)\nprobe_sched = torch.optim.lr_scheduler.CosineAnnealingLR(probe_opt, T_max=10, eta_min=0.01)\nvalues = []\nfor step in range(10):\n    probe_opt.step(); probe_sched.step()\n    values.append(round(probe_opt.param_groups[0][\"lr\"], 5))\nassert len(values) == 10 and values[-1] == 0.01\nprint(values)\n```\n\nKiểm thử nhỏ này phát hiện ngay sai đơn vị `T_max`. Trong dự án thật, lưu dãy η cùng checkpoint để việc resume có thể được so sánh từng bước.\n\n### Gradient accumulation đúng vị trí\n\n```python\naccum = 4\nopt.zero_grad()\nfor step, (xb, yb) in enumerate(DataLoader(train, batch_size=12, shuffle=True)):\n    (nn.functional.cross_entropy(model(xb), yb) / accum).backward()\n    if (step + 1) % accum == 0:\n        nn.utils.clip_grad_norm_(model.parameters(), 5.0)\n        opt.step(); opt.zero_grad()\n```\n\nCa còn dư khi số micro-batch không chia hết cho 4 cần một lần `step()` cuối vòng; code minh họa cố ý ngắn nên người dùng phải thêm nhánh đó nếu dataset thay đổi. Đây là ví dụ vì sao batch cuối là điều kiện logic, không phải chuyện trang trí.",

so_sanh: "### Chọn lịch theo điều mình biết trước\n\n| Phương án | Cần biết trước | Điểm mạnh | Khi không nên dùng |\n|---|---|---|---|\n| η cố định | một η hợp lý | baseline dễ hiểu | ngân sách dài, cần tinh chỉnh cuối kỳ |\n| Step decay | các mốc giảm | đơn giản, dễ tái lập | chưa biết mốc hội tụ |\n| Cosine | tổng số bước | giảm trơn, ít siêu tham số | luồng huấn luyện không có điểm kết thúc rõ |\n| OneCycle | tổng số bước và η đỉnh | khám phá nhanh rồi hạ sâu | khó resume nếu sai global step |\n| Plateau | metric validation | phản ứng theo tiến triển | metric nhiễu hoặc validation quá thưa |\n\n### Batch thật và batch hiệu dụng\n\n| Cách | VRAM | Số lần forward | Thống kê BatchNorm | Thông lượng |\n|---|---:|---:|---|---|\n| batch 64 trực tiếp | cao | 1 | 64 mẫu | thường tốt hơn |\n| 4 micro-batch 16 | thấp | 4 | từng nhóm 16 | thường chậm hơn |\n| batch 16, cập nhật ngay | thấp | 4 và 4 lần step | từng nhóm 16 | động lực học khác |\n\nKhông có lựa chọn thắng tuyệt đối. Batch lớn đáng dùng khi phần cứng còn nhàn và metric giữ được; accumulation đáng dùng khi mô hình không vừa bộ nhớ; batch nhỏ đáng dùng khi cần nhiễu hoặc phản hồi cập nhật thường xuyên.",

loi_thuong_gap: [
  "Gọi scheduler sai nhịp: cấu hình theo step nhưng gọi theo epoch; nhận biết bằng biểu đồ η hoàn thành lịch quá chậm hoặc quá nhanh.",
  "Đổi batch size nhưng giữ nguyên mọi thứ rồi gọi là so sánh công bằng; số optimizer step và nhiễu gradient đã đổi.",
  "Quên `shuffle=True` khi dữ liệu nằm theo lớp; loss có dạng răng cưa theo từng cụm lớp và accuracy lớp cũ tụt.",
  "Gọi `zero_grad()` trong từng micro-batch khi muốn accumulation; gradient không tích lũy nên batch hiệu dụng vẫn nhỏ.",
  "Quên xử lý phần micro-batch còn dư; một số mẫu cuối epoch không bao giờ tạo cập nhật.",
  "Đánh giá chỉ bằng training loss; η nhỏ có thể làm loss đẹp trong khi validation không cải thiện.",
  "Resume model nhưng không resume optimizer và scheduler; η và momentum quay lại đầu, loss thường nhảy ngay sau checkpoint."
],

tu_kiem_tra: [
  "Tập có 10.003 mẫu, B=64 và `drop_last=False`: một epoch có bao nhiêu step, batch cuối có bao nhiêu mẫu?",
  "Vì sao tăng batch bốn lần nhưng giữ số epoch làm thay cả nhiễu gradient lẫn số cập nhật?",
  "Phân biệt warmup với cosine decay bằng mục tiêu của từng giai đoạn.",
  "Tại sao gradient accumulation 4×16 không hoàn toàn tương đương batch thật 64 khi mạng có Batch Normalization?",
  "Bạn sẽ ghi log tối thiểu những gì để chứng minh scheduler chạy đúng sau khi resume?",
  "Training loss đứng yên: nêu ba dấu hiệu giúp phân biệt η quá nhỏ, η quá lớn và dữ liệu gắn nhãn sai.",
  "Thiết kế một so sánh batch 32 với 128 mà giữ ngân sách số mẫu và lịch η hợp lý; biến nào phải được kiểm soát?"
],

tai_nguyen: [
  "Sách Deep Learning — Ian Goodfellow, Yoshua Bengio, Aaron Courville, phần tối ưu hóa mô hình sâu.",
  "Khóa Practical Deep Learning for Coders — fast.ai, các bài về vòng lặp huấn luyện và learning rate.",
  "Tài liệu chính thức PyTorch về `DataLoader` và Learning Rate Scheduler.",
  "Sách Dive into Deep Learning, chương Optimization Algorithms."
],

lien_he: [
  "Mảng Hệ điều hành: `DataLoader` nhiều worker nối trực tiếp tới process, bộ nhớ ghim và nghẽn đầu vào; GPU nhanh vẫn có thể phải chờ CPU.",
  "Mảng Phân tích độ phức tạp: epoch là O(N) lượt mẫu nhưng thời gian còn phụ thuộc kích thước tensor, kiến trúc và khả năng song song của phần cứng.",
  "Mảng Kiểm thử: kiểm lịch η bằng bất biến và seed cố định giống kiểm một state machine, thay vì nhìn vài dòng log rồi đoán.",
  "Mảng Triển khai: checkpoint phải đóng gói model, optimizer, scheduler và global step để resume có cùng ngữ nghĩa."
]

};
