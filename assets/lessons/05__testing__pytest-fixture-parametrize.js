window.LESSONS["05:testing:pytest-fixture-parametrize"] = {

tom_tat: [
  "Fixture của pytest là cơ chế cung cấp tài nguyên theo quan hệ phụ thuộc; nó gom bước chuẩn bị và thu dọn vào một nơi nhưng vẫn để từng test nói rõ mình cần gì.",
  "Phạm vi function, module và session quyết định thời điểm tạo lại fixture; chọn phạm vi rộng chỉ an toàn khi đối tượng không rò rỉ trạng thái giữa các test.",
  "`@pytest.mark.parametrize` biến một quy tắc nghiệp vụ thành ma trận ca kiểm thử có tên, nhờ vậy bổ sung ca biên mà không sao chép thân test.",
  "Trong FastAPI, `app.dependency_overrides` phải được đặt và xóa bằng fixture có `yield`; quên thu dọn có thể khiến test sau dùng nhầm database hoặc danh tính người dùng.",
  "Fixture không phải chỗ giấu toàn bộ kịch bản. Dữ liệu quan trọng với ý nghĩa của test nên xuất hiện ngay trong test hoặc qua factory có tham số rõ ràng."
],

can_biet_truoc: [
  "Luồng request–response của FastAPI, status code HTTP và cách dùng `TestClient`.",
  "Hàm Python, decorator, generator có `yield`, context manager và type hint cơ bản.",
  "Khái niệm dependency injection của FastAPI qua `Depends`.",
  "Ba trạng thái nghiệp vụ của ví dụ: ghế còn trống, đang được giữ và đã xác nhận."
],

dinh_nghia: "**Fixture** trong pytest là một hàm được pytest gọi để tạo giá trị hoặc trạng thái mà test yêu cầu qua tên tham số. Pytest dựng đồ thị phụ thuộc giữa các fixture, thực thi phần chuẩn bị theo thứ tự từ phụ thuộc đến người dùng, rồi thu dọn theo chiều ngược lại. Fixture có thể trả giá trị trực tiếp hoặc `yield` một lần để tách phần chuẩn bị khỏi phần thu dọn.\n\n**Parametrization** là việc chạy cùng một hàm test nhiều lần với các bộ đầu vào và kết quả mong đợi khác nhau. `@pytest.mark.parametrize` khai báo ma trận đó tại điểm đọc test; mỗi hàng trở thành một test case độc lập, có trạng thái pass/fail và định danh riêng.\n\nHai cơ chế giải hai vấn đề khác nhau. Fixture quản lý *vòng đời và dependency*; parametrize quản lý *biến thể dữ liệu*. Ghép chúng đúng cách giúp bộ test API đặt chỗ workshop vừa dễ đọc vừa cô lập.",

vi_sao_can: "API đặt chỗ cần nhiều thứ trước khi một assertion có ý nghĩa: ứng dụng FastAPI, kho ghế sạch, đồng hồ cố định, người dùng đã xác thực và một workshop có sức chứa. Nếu mỗi test tự dựng tất cả, code chuẩn bị lấn át quy tắc đang kiểm tra. Khi schema đổi, hàng chục đoạn sao chép phải sửa đồng loạt; chỉ một đoạn còn thiếu trường mới cũng tạo lỗi giả.\n\nChiều ngược lại cũng nguy hiểm: một biến toàn cục dùng chung giúp test ngắn nhưng làm kết quả phụ thuộc thứ tự. Test giữ ghế chạy trước có thể khiến test “ghế trống” chạy sau nhận 409. Chạy riêng từng test đều xanh nhưng chạy cả suite lại đỏ là dấu hiệu trạng thái bị rò.\n\nParametrize cần thiết vì quy tắc giữ ghế không chỉ có một đầu vào đẹp. Số lượng 0, âm, đúng số ghế còn lại và vượt sức chứa phải cho kết quả khác nhau. Viết bốn hàm gần giống nhau che khuất sự thật rằng chúng cùng kiểm một bảng quyết định. Một ma trận khiến người review thấy ngay miền dữ liệu nào còn thiếu.\n\nTrong triển khai thật, fixture còn là hàng rào bảo mật. Test xác thực thường thay dependency lấy người dùng hiện tại. Nếu override không được xóa, một test endpoint quản trị có thể vô tình chạy với admin do test trước để lại; suite xanh nhưng không còn chứng minh phân quyền. Vì vậy vòng đời fixture là một phần của độ tin cậy, không chỉ là tiện ích cú pháp.",

co_che: "### Pytest phân giải fixture như một đồ thị\n\nKhi thấy `def test_hold(client, workshop):`, pytest không truyền hai biến ngẫu nhiên. Nó tìm fixture tên `client` và `workshop`. Nếu `client` cần `app`, còn `app` cần `settings`, pytest đi từ các lá của đồ thị lên: tạo `settings`, tạo `app`, tạo `client`, sau đó mới gọi test. Cách phân giải theo tên khiến dependency hiện ngay ở chữ ký hàm; người đọc không phải lần theo `setUp()` của lớp cha.\n\nMỗi fixture chỉ được tạo một lần trong phạm vi đang hoạt động, kể cả khi hai fixture con cùng yêu cầu nó. Cơ chế cache này tránh mở hai connection ngoài ý muốn. Tuy nhiên cache cũng có nghĩa mọi người dùng trong cùng phạm vi nhìn thấy cùng đối tượng mutable. Nếu fixture session trả một `dict` rồi test sửa nó, thay đổi tồn tại đến hết phiên.\n\n```python\nimport pytest\n\n@pytest.fixture\ndef settings():\n    return {\"hold_ttl_seconds\": 600}\n\n@pytest.fixture\ndef clock():\n    return {\"now\": 1_700_000_000}\n\n@pytest.fixture\ndef hold_service(settings, clock):\n    return HoldService(ttl=settings[\"hold_ttl_seconds\"], clock=clock)\n```\n\nỞ đây chữ ký `hold_service(settings, clock)` chính là sơ đồ lắp ráp. Test đơn vị có thể xin `hold_service` mà không biết cách cấu hình TTL. Nếu thời gian là chi tiết quyết định kết quả, test vẫn nên sửa `clock[\"now\"]` một cách tường minh trước hành động để người đọc thấy mốc thời gian.\n\n### `yield` bảo đảm thu dọn kể cả khi assertion thất bại\n\nCode trước `yield` là setup; giá trị được yield đi vào test; code sau đó là teardown. Pytest đăng ký phần teardown trước khi trao quyền cho test, nên nó vẫn chạy nếu assertion ném lỗi. Với tài nguyên có context manager, `with` bên trong fixture là cách ngắn và an toàn.\n\n```python\n@pytest.fixture\ndef client(app, fake_repository):\n    app.dependency_overrides[get_repository] = lambda: fake_repository\n    with TestClient(app) as http:\n        yield http\n    app.dependency_overrides.clear()\n```\n\nThứ tự thu dọn ngược với thứ tự tạo. Nếu fixture database mở transaction rồi fixture client dùng session trong transaction ấy, client đóng trước, transaction rollback sau. Điều này quan trọng vì đóng database quá sớm có thể làm sự kiện shutdown của FastAPI lỗi và che mất assertion ban đầu.\n\n`return TestClient(app)` không tương đương hoàn toàn với `with TestClient(app)`. Context manager chạy lifespan của ứng dụng, gồm startup và shutdown. Endpoint phụ thuộc cache hoặc connection pool khởi tạo trong lifespan cần đường đi đó để test giống tiến trình thật.\n\n### Scope là quyết định về mức cô lập\n\nPytest hỗ trợ các scope thường dùng: `function`, `class`, `module`, `package`, `session`. Mặc định `function` tạo mới cho từng test, dễ suy luận nhất. Scope rộng giảm chi phí dựng tài nguyên nhưng tăng diện tích rò trạng thái.\n\n| Scope | Tạo lại khi nào | Hợp với | Rủi ro chính |\n|---|---|---|---|\n| `function` | mỗi test case | repository mutable, override auth | chậm nếu setup rất nặng |\n| `module` | mỗi file test | app cấu hình bất biến | test trong file ảnh hưởng nhau |\n| `session` | một lần cả suite | engine, container database, metadata bất biến | connection hỏng hoặc dữ liệu bẩn lan rộng |\n\nKhông nên nâng scope chỉ vì suite chậm. Trước hết tách phần đắt nhưng bất biến, chẳng hạn engine, khỏi phần mutable, chẳng hạn transaction hoặc session. Engine có thể session-scoped; mỗi test vẫn lấy connection riêng và rollback. Đó là tối ưu có ranh giới rõ thay vì chia sẻ cả database state.\n\n### Parametrize tạo các test độc lập\n\nDecorator nhận chuỗi tên tham số và danh sách hàng. Pytest thu thập mỗi hàng thành một item riêng. Nếu hàng “negative” thất bại, các hàng khác vẫn chạy và báo cáo riêng. `ids` biến định danh khó đọc thành ngôn ngữ nghiệp vụ.\n\n```python\n@pytest.mark.parametrize(\n    (\"quantity\", \"available\", \"status\"),\n    [\n        pytest.param(1, 4, 201, id=\"one-seat\"),\n        pytest.param(4, 4, 201, id=\"take-last-seats\"),\n        pytest.param(5, 4, 409, id=\"over-capacity\"),\n        pytest.param(0, 4, 422, id=\"zero-is-invalid\"),\n        pytest.param(-1, 4, 422, id=\"negative-is-invalid\"),\n    ],\n)\ndef test_hold_quantity(client, seeded_workshop, quantity, available, status):\n    seeded_workshop.available = available\n    response = client.post(\"/holds\", json={\n        \"workshop_id\": seeded_workshop.id,\n        \"quantity\": quantity,\n    })\n    assert response.status_code == status\n```\n\nMỗi hàng phải đại diện một lý do thất bại hoặc một biên đáng kể, không phải liệt kê số ngẫu nhiên. `1` và `2` không tạo thêm hiểu biết nếu cùng đi qua một nhánh. `available` và `available + 1` có giá trị vì chúng nằm hai phía ranh giới sức chứa.\n\n### Kết hợp fixture với indirect parametrization\n\nĐôi khi tham số không phải giá trị đưa thẳng vào test mà là cấu hình để dựng fixture. `indirect=True` bảo pytest gửi giá trị vào `request.param` của fixture. Cơ chế này hữu ích cho nhiều trạng thái workshop, nhưng dễ làm test khó đọc nếu lạm dụng.\n\n```python\n@pytest.fixture\ndef workshop(request, repository):\n    state = request.param\n    return repository.add_workshop(capacity=state[\"capacity\"],\n                                   confirmed=state[\"confirmed\"])\n\n@pytest.mark.parametrize(\n    \"workshop\",\n    [{\"capacity\": 2, \"confirmed\": 0},\n     {\"capacity\": 2, \"confirmed\": 2}],\n    indirect=True,\n    ids=[\"has-room\", \"sold-out\"],\n)\ndef test_availability(client, workshop):\n    response = client.get(f\"/workshops/{workshop.id}\")\n    assert response.json()[\"available\"] in (0, 2)\n```\n\nNếu assertion cần biết expected, nên parametrize cả expected thay vì dùng `in (0, 2)`. Ví dụ trên minh họa đường truyền; bản kiểm thử tốt sẽ có `(workshop, expected_available)` để một trạng thái không thể vô tình khớp kết quả của trạng thái kia.\n\n### Factory fixture giữ dữ liệu tại nơi sử dụng\n\nMột fixture cố định như `workshop` nhanh chóng sinh nhiều anh em: `full_workshop`, `expired_workshop`, `private_workshop`. Factory fixture trả về hàm tạo với mặc định hợp lý và cho test ghi đè điểm quan trọng. Nó giảm số tên toàn cục mà vẫn giữ ý nghĩa ca test.\n\n```python\n@pytest.fixture\ndef make_workshop(repository):\n    created = []\n    def make(*, capacity=10, confirmed=0, owner_id=\"teacher-1\"):\n        item = repository.add_workshop(\n            capacity=capacity, confirmed=confirmed, owner_id=owner_id\n        )\n        created.append(item)\n        return item\n    return make\n\ndef test_owner_cannot_reduce_capacity_below_confirmed(client, make_workshop):\n    workshop = make_workshop(capacity=10, confirmed=6)\n    response = client.patch(f\"/workshops/{workshop.id}\",\n                            json={\"capacity\": 5})\n    assert response.status_code == 409\n```\n\n`confirmed=6` và `capacity=5` nằm ngay trong test vì chúng giải thích xung đột. Nếu giấu hai số ấy trong fixture `invalid_workshop`, người đọc phải nhảy file mới hiểu assertion.\n\n### Autouse chỉ dành cho bất biến toàn cục thật sự\n\nFixture `autouse=True` chạy mà test không nêu tên. Nó phù hợp để cấm mọi request mạng trong test đơn vị hoặc reset một registry toàn cục. Nó không phù hợp để tự đăng nhập admin, bởi quyền admin là điều kiện bảo mật quan trọng phải lộ ra.\n\nMột autouse monkeypatch chặn socket khiến test mới thất bại ngay khi vô tình gọi Internet. Lợi ích là suite không gửi email hay thanh toán thật. Nhưng integration test chủ đích dùng container cục bộ cần marker hoặc thư mục riêng để bỏ chặn có kiểm soát.\n\n### Kiểm soát FastAPI dependency override\n\nOverride là dictionary toàn cục gắn trên app. Khóa phải là đúng object hàm đã truyền vào `Depends`, không phải một hàm trùng tên được import từ đường khác. Fixture nên lưu trạng thái cũ nếu suite có nhiều lớp override, rồi khôi phục chính xác thay vì luôn `clear()` làm mất override của fixture cha.\n\n```python\n@pytest.fixture\ndef as_user(app):\n    def activate(user):\n        old = app.dependency_overrides.get(current_user)\n        app.dependency_overrides[current_user] = lambda: user\n        try:\n            yield\n        finally:\n            if old is None:\n                app.dependency_overrides.pop(current_user, None)\n            else:\n                app.dependency_overrides[current_user] = old\n    return activate\n```\n\nVới factory context như trên, test có thể dùng `with contextmanager(as_user(...))` sau khi hoàn thiện helper. Điểm cốt lõi là quyền phải kết thúc đúng phạm vi; tuyệt đối không để admin giả tồn tại qua test khác.\n\n### Khi nào fixture và parametrize trở thành gánh nặng\n\nKhông nên biến mọi literal thành fixture. `quantity=0` là dữ liệu, không có vòng đời; fixture `zero_quantity` chỉ làm tăng indirection. Cũng không nên tạo tích Descartes của mười tham số: 4 × 5 × 3 đã thành 60 test nhưng có thể vẫn không diễn đạt quy tắc. Hãy dùng bảng quyết định để chọn tổ hợp đại diện, rồi dành property-based testing cho miền rộng nếu cần.\n\nFixture nhiều tầng cũng có thể che kiến trúc xấu. Nếu để gọi endpoint cần chuỗi 14 fixture, ứng dụng có thể đang phụ thuộc quá nhiều singleton hoặc test đang dựng quá rộng. Lúc ấy tách service thuần để unit test thường tốt hơn tiếp tục trang trí fixture.",

vi_du: "### Một file test FastAPI tối thiểu nhưng chạy được\n\nVí dụ sau tự chứa app, fake repository và test. Lưu thành `test_holds.py`, cài `fastapi`, `httpx`, `pytest`, rồi chạy `pytest -q`.\n\n```python\nfrom dataclasses import dataclass\nfrom fastapi import Depends, FastAPI, HTTPException\nfrom fastapi.testclient import TestClient\nfrom pydantic import BaseModel, Field\nimport pytest\n\n@dataclass\nclass Workshop:\n    id: int\n    capacity: int\n    held: int = 0\n\nclass MemoryRepo:\n    def __init__(self):\n        self.items = {1: Workshop(id=1, capacity=4)}\n    def hold(self, workshop_id: int, quantity: int) -> int:\n        item = self.items[workshop_id]\n        if item.held + quantity > item.capacity:\n            raise ValueError(\"sold out\")\n        item.held += quantity\n        return item.capacity - item.held\n\nclass HoldIn(BaseModel):\n    workshop_id: int\n    quantity: int = Field(gt=0)\n\ndef create_app(repo: MemoryRepo) -> FastAPI:\n    app = FastAPI()\n    def get_repo():\n        return repo\n    @app.post(\"/holds\", status_code=201)\n    def create_hold(body: HoldIn, store=Depends(get_repo)):\n        try:\n            return {\"remaining\": store.hold(body.workshop_id, body.quantity)}\n        except ValueError as error:\n            raise HTTPException(409, str(error)) from error\n    return app\n\n@pytest.fixture\ndef repo():\n    return MemoryRepo()\n\n@pytest.fixture\ndef client(repo):\n    with TestClient(create_app(repo)) as value:\n        yield value\n\n@pytest.mark.parametrize(\n    (\"quantity\", \"expected_status\", \"expected_remaining\"),\n    [(1, 201, 3), (4, 201, 0), (5, 409, None),\n     (0, 422, None), (-1, 422, None)],\n    ids=[\"one\", \"last-four\", \"too-many\", \"zero\", \"negative\"],\n)\ndef test_create_hold(client, quantity, expected_status, expected_remaining):\n    response = client.post(\"/holds\", json={\"workshop_id\": 1,\n                                            \"quantity\": quantity})\n    assert response.status_code == expected_status\n    if expected_remaining is not None:\n        assert response.json()[\"remaining\"] == expected_remaining\n```\n\nFixture `repo` có scope function nên năm case nhận năm kho sạch; hàng `last-four` không làm hàng `one` hết ghế. Hai ca 0 và âm dừng ở validation, chứng minh repository không cần tự diễn giải dữ liệu sai định dạng. Trong ứng dụng thật repository vẫn nên bảo vệ invariant, vì nó có thể được gọi từ worker không đi qua HTTP.\n\n### Ca bảo mật không nên bị autouse che khuất\n\nKhi endpoint cần user, tạo fixture `user_token` và `admin_token` riêng rồi truyền header rõ tại test. Parametrize vai trò cùng status mong đợi cho endpoint sửa workshop: owner nhận 200, user khác nhận 403, không token nhận 401. Không dùng fixture tự động gắn admin cho mọi request; cách đó làm toàn suite bỏ qua đường xác thực công khai.",

so_sanh: "### Fixture, helper, hook và parametrize giải các lớp khác nhau\n\n| Công cụ | Dùng khi | Điểm mạnh | Khi không nên dùng |\n|---|---|---|---|\n| Fixture | tài nguyên có dependency hoặc vòng đời | teardown tin cậy, composition theo tên | literal đơn giản, logic assertion |\n| Helper thường | thao tác lặp lại không cần pytest quản lý | gọi tường minh, dễ debug | cần teardown kể cả khi test lỗi |\n| Factory fixture | nhiều biến thể của một entity | mặc định chung nhưng ca test giữ tham số quan trọng | chỉ có một cấu hình duy nhất |\n| Parametrize | cùng quy tắc với nhiều hàng dữ liệu | báo cáo từng case, tránh sao chép | các case có hành vi và assertion hoàn toàn khác |\n| Hook pytest | thay đổi thu thập hoặc chính sách toàn suite | áp quy ước tập trung | setup nghiệp vụ của một nhóm test |\n\n`unittest.TestCase.setUp` tạo trạng thái theo lớp và thường chạy cho mọi method dù method không dùng hết. Fixture cho test yêu cầu đúng dependency cần thiết và ghép các mảnh nhỏ. Đổi lại, phân giải theo tên có thể gây bất ngờ nếu dự án đặt tên mơ hồ; type hint và tên theo vai trò giúp giảm rủi ro.\n\nParametrize khác vòng `for` bên trong test ở khả năng cô lập báo cáo. Với vòng `for`, assertion đầu tiên thất bại dừng các hàng sau và CI chỉ thấy một test đỏ. Với parametrize, pytest tạo năm test; người sửa biết biên nào hỏng. Vòng lặp vẫn hợp khi cả chuỗi hành động là một kịch bản duy nhất, chẳng hạn giữ rồi xác nhận theo thứ tự.",

loi_thuong_gap: [
  "Dùng fixture `session` cho repository mutable: nhận biết qua việc test pass khi chạy riêng nhưng fail khi chạy cả suite hoặc với thứ tự ngẫu nhiên; hạ scope hoặc rollback từng test.",
  "Quên xóa `app.dependency_overrides`: endpoint phân quyền vẫn nhận admin từ test trước; bọc override bằng fixture `yield` và khôi phục trạng thái cũ.",
  "Nhét expected vào fixture: test chỉ còn assertion chung chung và người đọc không thấy quy tắc; đưa giá trị quyết định trở lại hàng parametrize.",
  "Parametrize quá nhiều tổ hợp không có ý nghĩa: thời gian suite tăng nhưng các case đi cùng nhánh; chọn theo biên và bảng quyết định.",
  "Fixture mở connection nhưng không đóng khi assertion lỗi: pool cạn dần trong CI; dùng context manager hoặc teardown sau `yield`.",
  "Override sai object dependency do import vòng: request vẫn gọi database thật; assertion thêm vào fake để xác nhận nó đã nhận lệnh và dùng đúng hàm gốc làm khóa.",
  "Đặt ID mặc định khó đọc: báo cáo hiện `case3` hoặc repr dài; dùng `pytest.param(..., id=\"over-capacity\")` theo ý nghĩa nghiệp vụ."
],

tu_kiem_tra: [
  "Vì sao fixture session-scoped trả một engine có thể an toàn, còn trả một ORM session dùng chung thường không an toàn?",
  "Trong thứ tự teardown, vì sao client FastAPI nên đóng trước transaction database mà nó đang dùng?",
  "Hãy chọn các hàng tối thiểu để kiểm quy tắc `1 ≤ quantity ≤ available`; mỗi hàng bảo vệ ranh giới nào?",
  "Khi nào factory fixture làm test dễ đọc hơn ba fixture `empty_workshop`, `full_workshop`, `almost_full_workshop`?",
  "Một test phân quyền pass sau test admin nhưng fail khi chạy riêng hé lộ lỗi vòng đời nào?",
  "Vì sao vòng `for` qua năm đầu vào không tương đương năm case do parametrize thu thập?",
  "Nếu hai fixture cùng phụ thuộc một dict mutable function-scoped, chúng nhận cùng hay khác object trong một test, và hệ quả là gì?"
],

tai_nguyen: [
  "pytest Documentation — Fixtures và Parametrizing tests.",
  "FastAPI Documentation — Testing và Testing Dependencies with Overrides.",
  "Python Testing with pytest — Brian Okken.",
  "Architecture Patterns with Python — Harry Percival và Bob Gregory."
],

lien_he: [
  "Nối sang mảng Backend & Web ở dependency injection: fixture thay implementation của `Depends` mà không đổi endpoint, qua đó làm rõ Dependency Inversion trong kiến trúc ứng dụng.",
  "Nối sang mảng Database qua transaction, connection pool và ràng buộc unique: scope của fixture phải phản ánh vòng đời thật của các đối tượng này.",
  "Nối sang mảng Security qua test 401/403 và việc không để admin override rò giữa các case.",
  "Nối sang mảng CI/CD vì ID parametrize và test độc lập giúp log CI chỉ đúng biên nghiệp vụ bị hồi quy."
]

};
