window.LESSONS["05:web-hoat-dong-the-nao:cors-vi-sao-trinh-duyet-chan-request-cua-ban"] = {

tom_tat: [
  "CORS là cơ chế để server cho browser nới lỏng same-origin policy đối với JavaScript; nó không phải cơ chế xác thực hay firewall cho API.",
  "Origin là bộ ba scheme, host và port. Khác bất kỳ thành phần nào là cross-origin, kể cả hai subdomain cùng thuộc một tổ chức.",
  "Một số cross-origin request được gửi thẳng rồi browser quyết định có cho script đọc response; request khác phải qua preflight `OPTIONS` trước.",
  "Cookie hoặc credential làm quy tắc chặt hơn: client cần opt-in, server phải cho phép origin cụ thể và không thể dùng wildcard cho mọi thứ.",
  "Ứng dụng Bàn học dùng frontend `app.banhoc.test` gọi FastAPI `api.banhoc.test`, nhờ đó thấy rõ CORS nằm ở browser chứ không nằm ở curl hay giao tiếp server-to-server."
],

can_biet_truoc: [
  "URL gồm scheme, host, port và path; DNS không quyết định hai URL có cùng origin hay không.",
  "HTTP method, request header, response header và status code; `OPTIONS` cũng là một method thực.",
  "Cookie/session và header `Authorization` là credential, nhưng browser gửi hai loại theo quy tắc khác nhau.",
  "Khái niệm reverse proxy, cache và môi trường development/production có origin khác nhau."
],

dinh_nghia: "**CORS (Cross-Origin Resource Sharing, chia sẻ tài nguyên khác origin)** là giao thức dựa trên HTTP header, qua đó server công bố origin nào được phép để JavaScript trong browser đọc một cross-origin response và, với request cần preflight, method/header nào được phép gửi. CORS hoạt động cùng **same-origin policy**—chính sách cô lập origin của browser—chứ không thay thế nó.\n\nOrigin được xác định bởi `scheme + host + port`. `https://app.banhoc.test` và `https://api.banhoc.test` khác host nên khác origin; `http://app...` và `https://app...` khác scheme; `https://app...:8443` khác port mặc định. Path `/a` hay `/b` không tạo origin mới. Server không “bị CORS chặn”: browser của người dùng mới là bên thực thi quyết định cho script truy cập kết quả.",

vi_sao_can: "Nếu mọi script được đọc tùy ý tài nguyên từ mọi website, trang độc hại có thể mở trong tab của user, lợi dụng cookie đang đăng nhập ở ngân hàng hoặc Bàn học rồi đọc dữ liệu riêng tư. Same-origin policy dựng ranh giới mặc định. Nhưng ứng dụng hiện đại thường tách frontend, API, CDN hoặc đối tác sang origin khác; CORS là cách origin đích cấp ngoại lệ có kiểm soát.\n\nVì lỗi xảy ra ở ranh giới browser, thông báo thường gây nhầm: Network tab có thể thấy response 200 nhưng JavaScript chỉ nhận `TypeError`; curl gọi cùng API lại thành công; backend log không có request thực nếu preflight bị từ chối. Chữa bằng `allow_origins=[\"*\"]` có thể che sai kiến trúc hoặc mở dữ liệu công khai ngoài ý muốn. Muốn sửa đúng phải biết request thuộc nhánh simple hay preflight, có credential không, proxy có giữ header không và origin thực tế chính xác là gì.",

co_che: "### Same-origin policy bảo vệ ai\n\nChính sách chủ yếu bảo vệ dữ liệu của user đang dùng browser. Khi user đăng nhập `api.banhoc.test`, browser có credential và quyền mà website độc hại không nên hưởng. Trang `evil.test` vẫn có thể tạo một số request cross-origin—web từ lâu đã cần ảnh, link và form—nhưng script thường không được đọc response nếu origin đích không đồng ý. Phân biệt “gửi được” và “đọc được” là chìa khóa.\n\nCORS không bảo vệ API khỏi Postman, bot, mobile app hay server khác vì các client đó không buộc phải thi hành same-origin policy. Nếu endpoint `/notes/n1` chỉ dựa vào CORS mà không xác thực/phân quyền, attacker gọi trực tiếp vẫn lấy được dữ liệu. Ngược lại, API xác thực đúng vẫn có thể cần CORS để frontend hợp lệ đọc response.\n\n### Tính origin bằng ba thành phần\n\nBrowser so sánh scheme, host và port đã chuẩn hóa. Bảng nhỏ sau tránh nhầm “cùng domain công ty” với “cùng origin”:\n\n| URL so với `https://app.banhoc.test` | Kết quả | Lý do |\n|---|---|---|\n| `https://app.banhoc.test/notes` | cùng origin | chỉ đổi path |\n| `https://api.banhoc.test/notes` | khác origin | host khác |\n| `http://app.banhoc.test/notes` | khác origin | scheme khác |\n| `https://app.banhoc.test:8443/notes` | khác origin | port khác |\n\nDNS có thể cho hai host trỏ cùng IP nhưng chúng vẫn khác origin. Hai server khác IP vẫn có thể đứng sau cùng origin nhờ reverse proxy. Đây là lý do topology URL người dùng thấy quan trọng hơn sơ đồ container nội bộ.\n\n### Origin header là lời khai của browser, không phải danh tính user\n\nVới CORS request, browser gửi `Origin: https://app.banhoc.test`. Server so giá trị với allowlist và phản hồi `Access-Control-Allow-Origin` nếu chấp nhận. Origin giúp quyết định website nào được đọc response trong browser; nó không xác thực người dùng và client ngoài browser có thể tự tạo header này. Không dùng `Origin` thay API key, session hay mTLS.\n\nAllowlist nên so origin đã parse/canonicalize hoặc dùng cấu hình framework đáng tin, không kiểm kiểu `endswith(\"banhoc.test\")` vì `evilbanhoc.test` có thể lọt. Regex rộng cũng dễ nhận `https://banhoc.test.attacker.example`. Danh sách explicit cho production thường dễ audit hơn. Origin `null` xuất hiện trong một số sandbox/file context; không nên cho phép rộng chỉ để sửa demo local.\n\n### Nhánh không preflight\n\nMột nhóm “simple request” đáp ứng giới hạn về method, header và Content-Type được gửi ngay. Ví dụ GET thông thường hoặc POST form cổ điển. Browser thêm `Origin`, server xử lý request, rồi browser kiểm `Access-Control-Allow-Origin` trên response. Nếu thiếu/sai, script không được đọc response—nhưng side effect của request đã có thể xảy ra.\n\nĐây là lý do CORS không phải CSRF defense. Form từ trang độc có thể gửi POST loại đơn giản kèm cookie trong một số điều kiện; dù attacker không đọc kết quả, thao tác đổi email hay xóa ghi chú đã hoàn thành nếu server thiếu CSRF protection. Method có tác dụng phụ phải được bảo vệ độc lập bằng SameSite, CSRF token, kiểm Origin phù hợp và semantics đúng.\n\n### Khi nào browser preflight\n\nRequest dùng `PUT`, `PATCH`, `DELETE`, custom header, `Authorization`, hoặc Content-Type như `application/json` thường không nằm trong nhóm safelisted và browser gửi `OPTIONS` thăm dò. Preflight hỏi: origin này có được không, method dự định có được không, những header dự định có được không. Nó không mang body nghiệp vụ.\n\n```http\nOPTIONS /notes/n1 HTTP/1.1\nOrigin: https://app.banhoc.test\nAccess-Control-Request-Method: PUT\nAccess-Control-Request-Headers: content-type,x-csrf-token\n```\n\nServer có thể trả:\n\n```http\nHTTP/1.1 200 OK\nAccess-Control-Allow-Origin: https://app.banhoc.test\nAccess-Control-Allow-Methods: GET, PUT\nAccess-Control-Allow-Headers: Content-Type, X-CSRF-Token\nAccess-Control-Allow-Credentials: true\nVary: Origin\n```\n\nBrowser đối chiếu rồi mới gửi PUT. Nếu preflight fail, handler PUT không chạy; hãy tìm `OPTIONS` trong Network tab và access log thay vì đặt breakpoint ở business handler mãi.\n\n### Preflight không phải permission nghiệp vụ\n\nViệc server cho `PUT` trong `Access-Control-Allow-Methods` chỉ cho browser biết script từ origin đó được thử gửi PUT. Request thật vẫn phải có session hợp lệ, CSRF token hợp lệ, schema đúng và user sở hữu note. Middleware CORS thường chạy trước dependency xác thực để preflight không bị trả 401 do thiếu cookie; preflight không nên tạo hoặc sửa dữ liệu.\n\nTrả 2xx cho OPTIONS mà thiếu allow header vẫn fail ở browser. Ngược lại, trả allow header không đảm bảo PUT thành công; nó có thể nhận 401, 403, 409 hay 500, và response lỗi cũng cần CORS header để frontend đọc status/body. Nếu middleware chỉ gắn header cho happy path, lỗi exception trông thành “CORS error” và che nguyên nhân server thực.\n\n### Credentialed request cần hai phía đồng ý\n\nVới `fetch` cross-origin, frontend dùng `credentials: \"include\"` để browser cân nhắc gửi cookie và chấp nhận `Set-Cookie`. API đáp `Access-Control-Allow-Credentials: true` và một `Access-Control-Allow-Origin` cụ thể khớp request. Wildcard `*` không đại diện cho origin cụ thể khi response dùng credential; browser sẽ không trao response cho script.\n\nCookie còn phải vượt các quy tắc `Secure`, `SameSite`, Domain/Path và chính sách browser. CORS đúng không ép browser gửi cookie bị SameSite chặn. Ngược lại, cookie đã được gửi không chứng minh CORS đúng. Debug cần nhìn cả request Cookie, response Set-Cookie, CORS header và console, không suy từ một dấu hiệu.\n\nHeader `Authorization` không tự được browser gửi như cookie; code phải đặt nó, và việc đó thường kích hoạt preflight. Tuy nhiên nó vẫn là credential, cần HTTPS, che log và phân quyền phía server. “Không dùng cookie nên không cần nghĩ CORS” là sai nếu frontend và API khác origin.\n\n### Wildcard có chỗ dùng, nhưng không phải thuốc chữa\n\n`Access-Control-Allow-Origin: *` hợp với tài nguyên công khai không credential, chẳng hạn một catalog ai cũng được phép đọc. Nó không khiến server chấp nhận mọi request header/method một cách tự động và không đi cùng credentialed response. Nếu dữ liệu thực sự public, wildcard có thể đơn giản hơn allowlist dài và tránh lỗi onboarding origin đối tác.\n\nVới dữ liệu người dùng, phản chiếu mọi `Origin` nhận được rồi thêm `Allow-Credentials: true` thực chất là cho bất kỳ website nào đọc response dưới phiên nạn nhân. Mẫu code `allow_origin = request.headers[\"Origin\"]` chỉ an toàn sau kiểm allowlist nghiêm ngặt. Môi trường development có thể cho `http://localhost:5173`, nhưng đừng đưa wildcard regex localhost/preview vào production mà không biết phạm vi.\n\n### Header nào script được đọc\n\nCORS không chỉ kiểm request. Browser chỉ tự phơi bày một nhóm response header an toàn cho JavaScript. Nếu frontend cần đọc `ETag`, `Content-Disposition` hoặc `X-Request-ID`, server có thể phải liệt kê chúng trong `Access-Control-Expose-Headers`. Header vẫn hiện trong Network panel nhưng `response.headers.get(...)` có thể trả `null`; đó là dấu hiệu exposure, không phải server quên gửi.\n\n`Set-Cookie` không trở thành đọc được dù expose; browser quản lý cookie. Đây là chủ ý bảo mật. Frontend không nên cần lấy session id từ response rồi tự lưu. Với Bàn học, expose `ETag` cho phép client lưu validator và gửi `If-Match`, nhưng điều đó cũng thêm `If-Match` vào danh sách header preflight được phép.\n\n### Cache preflight và cache response\n\n`Access-Control-Max-Age` cho browser cache kết quả preflight một thời gian, giảm một round-trip cho các request lặp. Giới hạn thực tế phụ thuộc browser; đừng ghi con số tuyệt đối nếu chưa đo môi trường. Khi thay policy khẩn cấp, cache cũ có thể tiếp tục cho request thật được gửi trong cửa sổ đó, nên max-age dài là đánh đổi giữa latency và tốc độ rollout/revoke policy.\n\nResponse thay đổi `Access-Control-Allow-Origin` theo request Origin cần `Vary: Origin` để shared cache không dùng biến thể của A cho B. Middleware tốt thường thêm nó khi phản chiếu origin. CDN cũng phải forward `Origin` vào cache key/policy đúng. Test localhost trực tiếp không phát hiện cấu hình CDN làm mất header.\n\n### Redirect làm luồng khó đọc\n\nPreflight hoặc request thật gặp redirect HTTP→HTTPS, thêm slash, đổi host đăng nhập có thể hành xử khác nhau giữa client và làm credential bị bỏ khi sang origin khác. Tránh redirect trên endpoint API nếu có thể: frontend nên gọi URL canonical HTTPS đúng ngay từ đầu. FastAPI tự redirect `/notes` sang `/notes/` là lỗi hay gặp khi route và client lệch dấu slash.\n\nNetwork tab cho thấy chuỗi request; hãy kiểm URL cuối, status và header ở từng hop. Cấu hình CORS trên service đích không giúp nếu reverse proxy trả redirect/error trước khi request tới middleware. HSTS và cấu hình base URL đúng giải quyết gốc tốt hơn việc thêm origin lung tung.\n\n### Private network, file và WebSocket không nên suy diễn\n\nMở `index.html` bằng `file://` có thể tạo origin đặc biệt và khác hẳn dev server. Hãy chạy frontend bằng HTTP dev server để topology giống thật. Browser cũng tăng bảo vệ khi trang công khai truy cập địa chỉ mạng riêng; lỗi liên quan không nên chữa bằng allowlist CORS mù quáng.\n\nWebSocket bắt đầu bằng HTTP handshake nhưng không dùng toàn bộ CORS protocol giống fetch. Server WebSocket phải tự kiểm `Origin` theo policy và xác thực kết nối. EventSource, font, canvas và module script có các quy tắc liên quan riêng; hiểu mô hình origin trước, rồi đọc contract của API browser cụ thể thay vì áp một công thức.\n\n### Cấu hình FastAPI đúng phạm vi\n\n`CORSMiddleware` nên bao phủ ứng dụng để cả response lỗi có header, với allowlist đọc từ cấu hình môi trường đã kiểm. `allow_methods` và `allow_headers` chỉ liệt kê nhu cầu thật giúp audit, dù wildcard có thể tiện trong API public không credential. Origin phải gồm scheme và port; `https://app.banhoc.test/` có dấu path không phải origin hợp lệ trong allowlist.\n\nNếu middleware xác thực custom chặn OPTIONS trước CORS, preflight sẽ fail. Thứ tự middleware quan trọng. Reverse proxy đôi lúc xử lý OPTIONS hoặc bỏ header, vì vậy xác minh qua hostname production/staging. Không thêm CORS middleware nhiều tầng với policy khác nhau; header trùng hoặc mâu thuẫn khiến browser từ chối và đội vận hành khó biết tầng nào sở hữu policy.\n\n### Quy trình debug dựa trên bằng chứng\n\nBước một, ghi chính xác origin của trang từ `location.origin` và URL API. Bước hai, xem request có preflight không; nếu có, kiểm request method/header mà browser hỏi. Bước ba, kiểm response OPTIONS có status 2xx và allow origin/method/header khớp. Bước bốn, xem request thật có được gửi, có cookie/token không. Bước năm, xem response thật—kể cả lỗi—có allow origin và credentials phù hợp không.\n\nSau đó mới so cấu hình proxy, CDN và FastAPI. `curl` mô phỏng được header để xem server trả gì nhưng không chứng minh browser sẽ cho đọc; curl không thi hành CORS. Automation bằng browser mới kiểm end-to-end. Log request id cho OPTIONS và request thật giúp ghép hai nửa nhưng không ghi credential.\n\n### Khi không cần CORS\n\nServer-to-server, CLI và mobile client không dựa vào browser same-origin policy, nên CORS không cấp bảo mật cho chúng. Frontend và API cùng origin qua reverse proxy không cần ngoại lệ CORS; đây thường là kiến trúc đơn giản cho web app dùng cookie. Form submit/redirect có luồng browser khác và không nên mặc định coi là fetch CORS.\n\nKhông bật CORS chỉ vì API “không chạy trên Postman”—đó chắc chắn là nguyên nhân khác. Cũng không thêm header vào request frontend như `Access-Control-Allow-Origin`; đây là response header do server có thẩm quyền gửi. CORS chỉ đáng cấu hình khi có JavaScript ở origin A cần truy cập tài nguyên origin B.",

vi_du: "### FastAPI và frontend Bàn học\n\nAPI dưới đây cho đúng một frontend, cho cookie, cho `GET`/`PUT`, nhận Content-Type và CSRF header, đồng thời expose ETag. Session check được rút gọn để tập trung CORS; production phải dùng cơ chế bài trước.\n\n```python\nfrom fastapi import Cookie, FastAPI, Header, HTTPException, Response\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom pydantic import BaseModel\n\napp = FastAPI()\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"https://app.banhoc.test\"],\n    allow_credentials=True,\n    allow_methods=[\"GET\", \"PUT\"],\n    allow_headers=[\"Content-Type\", \"X-CSRF-Token\", \"If-Match\"],\n    expose_headers=[\"ETag\", \"X-Request-ID\"],\n    max_age=600,\n)\n\nclass NoteIn(BaseModel):\n    title: str\n\nnote = {\"owner\": \"u1\", \"title\": \"Học CORS\", \"version\": 1}\n\ndef user_from_session(session: str | None) -> str:\n    if session != \"demo-session-u1\":\n        raise HTTPException(401, \"chưa đăng nhập\")\n    return \"u1\"\n\n@app.get(\"/notes/n1\")\ndef read_note(response: Response, session: str | None = Cookie(default=None)):\n    user_from_session(session)\n    response.headers[\"ETag\"] = f'\"note-n1-v{note[\"version\"]}\"'\n    response.headers[\"Cache-Control\"] = \"private, no-cache\"\n    return note\n\n@app.put(\"/notes/n1\")\ndef update_note(data: NoteIn, response: Response,\n                x_csrf_token: str | None = Header(default=None),\n                if_match: str | None = Header(default=None),\n                session: str | None = Cookie(default=None)):\n    user_from_session(session)\n    if x_csrf_token != \"demo-csrf\":\n        raise HTTPException(403, \"CSRF token sai\")\n    current = f'\"note-n1-v{note[\"version\"]}\"'\n    if if_match != current:\n        raise HTTPException(412, \"phiên bản đã đổi\")\n    note.update(title=data.title, version=note[\"version\"] + 1)\n    response.headers[\"ETag\"] = f'\"note-n1-v{note[\"version\"]}\"'\n    return note\n```\n\nFrontend chạy ở đúng origin cho phép:\n\n```javascript\nconst response = await fetch(\"https://api.banhoc.test/notes/n1\", {\n  method: \"PUT\",\n  credentials: \"include\",\n  headers: {\n    \"Content-Type\": \"application/json\",\n    \"X-CSRF-Token\": \"demo-csrf\",\n    \"If-Match\": '\"note-n1-v1\"'\n  },\n  body: JSON.stringify({title: \"Đã hiểu preflight\"})\n});\nif (!response.ok) throw new Error(`${response.status}: ${await response.text()}`);\nconsole.log(await response.json(), response.headers.get(\"ETag\"));\n```\n\nTrước PUT, browser sẽ gửi OPTIONS vì method/header không safelisted. Có thể kiểm server riêng bằng curl, nhưng đây chỉ kiểm response chứ không mô phỏng enforcement:\n\n```bash\ncurl -i -X OPTIONS http://127.0.0.1:8000/notes/n1 \\\n  -H \"Origin: https://app.banhoc.test\" \\\n  -H \"Access-Control-Request-Method: PUT\" \\\n  -H \"Access-Control-Request-Headers: content-type,x-csrf-token,if-match\"\n```\n\nĐổi Origin thành `https://evil.test`, bỏ `X-CSRF-Token`, bỏ cookie, dùng version cũ và gây exception để quan sát từng lớp từ chối. Test cuối cùng phải chạy trong browser vì đó mới là tác nhân thực thi CORS.",

so_sanh: "### CORS, proxy cùng origin và JSONP\n\n| Phương án | Cơ chế | Khi phù hợp | Đánh đổi |\n|---|---|---|---|\n| CORS allowlist | API B cho JavaScript từ origin A đọc response | frontend/API thực sự cần tách origin | preflight, cấu hình credential/cache/proxy phức tạp |\n| Reverse proxy cùng origin | browser chỉ thấy một origin, proxy định tuyến `/api` | web app do cùng đội kiểm soát | vận hành proxy và routing; giảm tính độc lập public endpoint |\n| Server-side proxy/BFF | backend frontend gọi service khác | cần giấu token upstream, tổng hợp dữ liệu | thêm hop, state và điểm lỗi |\n| JSONP lịch sử | tải script cross-origin qua thẻ script | hầu như chỉ hệ thống cũ, GET | thực thi code thay vì trao dữ liệu an toàn; không nên chọn mới |\n\nNếu chỉ có một web frontend và cookie session, cùng origin qua reverse proxy thường dễ bảo mật hơn. Nếu API là sản phẩm cho nhiều website đối tác, CORS explicit là contract cần thiết. Server-to-server proxy không nên được dựng chỉ để “né CORS” nếu nó biến thành open proxy hoặc bỏ xác thực; nó phải có allowlist đích, timeout, giới hạn body và policy credential riêng.",

loi_thuong_gap: [
  "Thêm `Access-Control-Allow-Origin` vào request frontend; nhận biết trong Network tab vì server response vẫn thiếu header quyết định.",
  "Dùng wildcard cùng cookie/credentials; console báo credentialed response không chấp nhận wildcard và script không đọc được.",
  "Chỉ cấu hình GET nhưng request JSON PUT cần OPTIONS; backend log có preflight bị từ chối trước khi handler PUT chạy.",
  "Quên CORS header trên response lỗi; lỗi 500 thật bị browser trình bày như lỗi CORS, gây debug sai tầng.",
  "Phản chiếu mọi Origin với `Allow-Credentials`; website bất kỳ có thể đọc API bằng phiên đang đăng nhập của nạn nhân.",
  "Nhầm CORS với CSRF protection; request đơn giản vẫn gây side effect dù trang độc không đọc được response.",
  "Allowlist thiếu scheme hoặc port development; so `location.origin` sẽ thấy giá trị cấu hình không khớp chính xác.",
  "CDN cache biến thể của origin khác vì thiếu `Vary: Origin`; lỗi xuất hiện không ổn định theo cache hit."
],

tu_kiem_tra: [
  "Vì sao hai host trỏ cùng IP vẫn cross-origin, còn hai service khác máy có thể được browser nhìn như cùng origin?",
  "Một simple POST bị thiếu allow-origin: request có thể đã thay đổi dữ liệu chưa, và JavaScript quan sát được điều gì?",
  "Viết các header preflight cho PUT JSON có `X-CSRF-Token` và giải thích response nào khiến browser gửi request thật.",
  "Tại sao curl thành công không chứng minh cấu hình CORS đúng, nhưng curl vẫn hữu ích ở bước debug nào?",
  "Cookie không xuất hiện dù allow-origin và allow-credentials đúng; hãy liệt kê các lớp còn phải kiểm ngoài CORS.",
  "Nếu API phản chiếu Origin hợp lệ, vì sao shared cache cần `Vary: Origin`, và lỗi thiếu Vary có thể biểu hiện thế nào?",
  "Thiết kế Bàn học cùng origin qua reverse proxy sẽ bỏ được phần nào nhưng vẫn phải giữ những kiểm soát bảo mật nào?"
],

tai_nguyen: [
  "MDN Web Docs — Cross-Origin Resource Sharing (CORS).",
  "Fetch Standard — phần CORS protocol và HTTP-network-or-cache fetch.",
  "OWASP Cross-Origin Resource Sharing Cheat Sheet.",
  "Tài liệu FastAPI — CORS (Cross-Origin Resource Sharing).",
  "Web Security for Developers — Malcolm McDonald."
],

lien_he: [
  "Mảng Frontend: Fetch API, `credentials`, DevTools và cách browser giấu response là nơi lỗi CORS biểu hiện trực tiếp.",
  "Mảng Bảo mật: same-origin policy cô lập website, còn CSRF, XSS, authentication và object authorization vẫn cần cơ chế riêng.",
  "Mảng DevOps: reverse proxy, CDN, TLS termination và cache key có thể thêm, bỏ hoặc dùng sai CORS header sau khi FastAPI trả response.",
  "Mảng Kiểm thử: curl kiểm contract server; browser automation mới kiểm được enforcement và hành vi cookie end-to-end."
]

};
