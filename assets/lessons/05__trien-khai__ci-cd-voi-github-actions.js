window.LESSONS["05:trien-khai:ci-cd-voi-github-actions"] = {

tom_tat: [
  "CI kiểm chứng mỗi thay đổi trong môi trường sạch; CD chuyển đúng artifact đã kiểm chứng qua các môi trường với quyền, phê duyệt, rollout và rollback rõ ràng.",
  "GitHub Actions workflow gồm event, job, step, runner, permission và artifact; hiểu ranh giới này quan trọng hơn học thuộc YAML.",
  "Pipeline Sổ Chi Tiêu chạy lint/test, integration test PostgreSQL–Redis, build image một lần, quét image rồi deploy theo digest thay vì rebuild ở production.",
  "Secret phải có phạm vi nhỏ, log phải được che, workflow từ pull request không tin cậy không được cầm credential triển khai; OIDC giảm nhu cầu lưu cloud key dài hạn.",
  "Deploy tốt cần concurrency control, migration tương thích, health verification và rollback đã diễn tập; dấu kiểm xanh của build không chứng minh production an toàn."
],

can_biet_truoc: [
  "Biết Git commit, branch, pull request, merge và khác biệt giữa source revision với artifact được build từ revision đó.",
  "Đã hiểu Docker image, registry, tag, digest và Docker Compose của hai bài trước.",
  "Biết unit test, integration test, exit code và nguyên tắc process trả mã khác 0 khi kiểm tra thất bại.",
  "Biết biến môi trường, secret, quyền tối thiểu và migration schema PostgreSQL."
],

dinh_nghia: "**Continuous Integration (CI)** là việc tự động hợp nhất và kiểm chứng thay đổi thường xuyên bằng build, phân tích tĩnh và test trong môi trường tái lập. **Continuous Delivery** giữ mọi revision đạt chuẩn ở trạng thái có thể phát hành; **Continuous Deployment** đi xa hơn bằng cách tự đưa revision đạt chuẩn lên production mà không cần nút phê duyệt thủ công.\n\n**GitHub Actions** là bộ máy thực thi workflow khai báo trong repository. Event tạo một workflow run; run chứa các job; mỗi job chạy các step trên runner. Pipeline đúng không chỉ “chạy test”, mà tạo chuỗi bằng chứng nối commit → kết quả kiểm tra → image digest → deployment revision, sao cho có thể trả lời production đang chạy thứ gì và vì sao nó được phép chạy.",

vi_sao_can: "Kiểm thử trên laptop mang theo cache, file chưa commit và database cá nhân. Deploy thủ công lại mang theo trí nhớ: ai build, dùng branch nào, đã migration chưa, có quên biến môi trường không. Khi sự cố xảy ra, đội ngũ khó tái hiện chính artifact và thường sửa nóng làm trạng thái lệch thêm.\n\nCI/CD biến tiêu chuẩn phát hành thành chương trình được review. Một pull request phá validation số tiền, migration không tương thích hoặc image chạy root sẽ bị chặn trước merge. Sau merge, cùng image đã qua kiểm tra được ký danh tính bằng digest và chuyển tới môi trường đích. Tự động hóa rút ngắn thời gian phản hồi, nhưng lợi ích lớn hơn là giảm thao tác không quan sát được.\n\nPipeline cũng là mục tiêu tấn công có quyền ghi registry và production. Workflow bất cẩn có thể biến code trong pull request thành lệnh đọc secret. Vì vậy bảo mật pipeline thuộc thiết kế hệ thống, không phải phần trang trí sau khi YAML chạy xanh.",

co_che: "### Tách CI khỏi CD bằng ranh giới artifact\n\nCI nhận source revision và tạo bằng chứng: test report, image, kết quả quét. CD chỉ nhận artifact đã được định danh, không checkout lại rồi build theo cách khác. Nếu staging và production tự build riêng, hai nơi có thể kéo dependency/base image khác dù cùng commit.\n\n```text\npull request -> lint + unit + integration\nmerge main   -> build image -> registry@sha256:...\ndigest       -> staging -> smoke test -> approval/policy -> production\n```\n\nCommit SHA xác định source; image digest xác định bytes của artifact. Cần lưu cả hai trong label và deployment metadata. Tag như `main` hoặc `latest` tiện cho người đọc nhưng có thể di chuyển, nên không đủ làm khóa rollback.\n\n### Event và phạm vi chạy\n\n`pull_request` phù hợp kiểm tra code trước merge. `push` trên nhánh chính phù hợp build release. `workflow_dispatch` cho thao tác có chủ đích. Không nên deploy production từ mọi branch chỉ vì filter bị bỏ quên.\n\nPath filter có thể tiết kiệm runner nhưng nguy hiểm trong monorepo: thay shared library mà workflow API không chạy. Nếu dùng filter, dependency graph phải đúng. Một pipeline chậm có thể chia job song song; một pipeline bỏ test để nhanh chỉ chuyển thời gian sang incident.\n\n### Runner là máy tính dùng một lần nhưng vẫn là biên tin cậy\n\nGitHub-hosted runner thường bắt đầu sạch, checkout code rồi chạy shell. Mọi action được gọi có thể đọc workspace và token được cấp. Action bên thứ ba là dependency thực thi, không chỉ thư viện cấu hình; nên dùng action uy tín, khóa revision bất biến khi chính sách yêu cầu và cập nhật có kiểm soát.\n\nSelf-hosted runner truy cập mạng nội bộ nhưng giữ trạng thái giữa job nếu quản lý kém. Code pull request không tin cậy có thể cài persistence hoặc đọc credential còn sót. Không cho fork PR chạy trên runner production có quyền cao; tách runner group và hủy máy sau job nếu có thể.\n\n### Workflow CI cho FastAPI\n\n```yaml\nname: ci\non:\n  pull_request:\n  push:\n    branches: [main]\n\npermissions:\n  contents: read\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    services:\n      postgres:\n        image: postgres:16\n        env:\n          POSTGRES_USER: expense\n          POSTGRES_PASSWORD: test-only\n          POSTGRES_DB: expense_test\n        ports: [\"5432:5432\"]\n        options: >-\n          --health-cmd \"pg_isready -U expense -d expense_test\"\n          --health-interval 5s --health-timeout 3s --health-retries 10\n      redis:\n        image: redis:7-alpine\n        ports: [\"6379:6379\"]\n    env:\n      DATABASE_URL: postgresql+asyncpg://expense:test-only@localhost:5432/expense_test\n      REDIS_URL: redis://localhost:6379/0\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n          cache: pip\n      - run: pip install -r requirements-dev.txt\n      - run: ruff check .\n      - run: pytest -q\n```\n\nCredential `test-only` chỉ sống trong database tạm trên runner, không phải secret production. Service container được gọi qua `localhost` vì job chạy trực tiếp trên runner và ports được publish; nếu job bản thân chạy trong container, hostname lại là tên service. Đây là hệ quả network namespace của bài Compose.\n\n### Lock dependency và cache không làm mất tính đúng\n\nCache pip giảm tải xuống, nhưng lock file vẫn quyết định dependency. Cache key cần phụ thuộc nội dung lock file; nếu không, runner có thể dùng gói cũ sau khi dependency đổi. Cache là tối ưu, không phải nguồn chân lý.\n\nKhông cache virtual environment mù quáng giữa Python version hay kiến trúc khác nhau. Khi nghi ngờ cache gây flake, chạy lại với cache lạnh. CI phải thỉnh thoảng chứng minh build từ đầu vẫn hoạt động, nếu không cache che mất dependency không được khai báo.\n\n### Integration test phải kiểm tra failure có ý nghĩa\n\nUnit test service với fake repository chạy nhanh. Integration test dùng PostgreSQL thật để bắt khác biệt SQL, constraint và migration. Pipeline nên chạy migration lên database rỗng, seed dữ liệu tối thiểu, kiểm tra tạo expense trùng idempotency key, Redis mất kết nối và rollback transaction.\n\nKhông dùng `sleep 10` để chờ DB; máy nhanh lãng phí, máy chậm vẫn fail. Healthcheck/poll có timeout phản ánh điều kiện thật. Test thất bại phải giữ log đủ chẩn đoán nhưng lọc connection string.\n\n### Build một lần và xuất digest\n\nJob build chỉ chạy sau test. Nó đăng nhập registry bằng credential ngắn hạn/phạm vi hẹp, build Dockerfile và push image. Output digest được truyền cho deploy job. Không đưa secret runtime vào build; image phải giống nhau giữa staging và production.\n\n```yaml\n  build:\n    if: github.event_name == 'push'\n    needs: test\n    runs-on: ubuntu-latest\n    permissions:\n      contents: read\n      packages: write\n    outputs:\n      digest: ${{ steps.build.outputs.digest }}\n    steps:\n      - uses: actions/checkout@v4\n      - uses: docker/setup-buildx-action@v3\n      - uses: docker/login-action@v3\n        with:\n          registry: ghcr.io\n          username: ${{ github.actor }}\n          password: ${{ secrets.GITHUB_TOKEN }}\n      - id: build\n        uses: docker/build-push-action@v6\n        with:\n          context: .\n          push: true\n          tags: ghcr.io/example/expense-api:${{ github.sha }}\n```\n\nTên owner/image chỉ là placeholder phải thay bằng repository thật. Workflow thực tế nên pin action theo chính sách của dự án; không bịa một commit SHA minh họa vì SHA sai còn nguy hiểm hơn tag rõ ràng.\n\n### Permission mặc định phải bị thu hẹp\n\nKhai báo `permissions: contents: read` ở cấp workflow rồi mở `packages: write` chỉ tại job cần push. Deploy job mới cần identity cloud. Một token có mọi quyền ở mọi step khiến lỗ hổng trong linter cũng có đường tới production.\n\n`GITHUB_TOKEN` có vòng đời run và quyền theo workflow. Secret repository/environment được giải mã khi job đủ điều kiện; chúng không nên truyền qua output hoặc artifact. Masking log không phải lá chắn tuyệt đối: secret biến đổi, encode hoặc ghép chuỗi có thể thoát mask. Cách an toàn là không in.\n\n### Pull request từ fork là đầu vào đối kháng\n\nCode trong PR có thể sửa test để chạy `env`, thay script package hoặc lợi dụng action. Event `pull_request_target` chạy trong ngữ cảnh repository đích và có thể có quyền; checkout code PR rồi thực thi trong event này là bẫy nghiêm trọng. Dùng `pull_request` với quyền đọc cho kiểm tra code không tin cậy.\n\nKhông chạy script do PR kiểm soát sau khi nạp secret. Nếu cần maintainer phê duyệt benchmark đặc biệt, tách workflow/runner và review commit chính xác. Pipeline phải coi source chưa merge giống dữ liệu người dùng: validate và cô lập.\n\n### OIDC thay cloud key dài hạn\n\nOpenID Connect cho runner đổi token có claim về repository, branch và environment lấy credential cloud ngắn hạn. Nhà cung cấp cloud kiểm policy rồi cấp quyền trong vài phút. Nếu token bị lộ, cửa sổ sử dụng nhỏ hơn key lưu nhiều tháng.\n\nOIDC không tự an toàn: trust policy quá rộng cho mọi branch vẫn cho branch độc hại deploy. Ràng buộc repository, ref/environment và audience; deploy production chỉ từ nhánh được bảo vệ. Audit phải nối được identity của run với hành động trên nền tảng.\n\n### Environment, approval và separation of duties\n\nGitHub Environment có thể giữ secret theo môi trường và yêu cầu reviewer trước production. Staging tự động giúp phản hồi nhanh; production có approval phù hợp hệ thống rủi ro cao. Approval chỉ hữu ích nếu màn hình cho reviewer thấy digest, test, migration và diff; nút bấm theo thói quen không tạo an toàn.\n\nContinuous Delivery không kém trưởng thành hơn Continuous Deployment. Ứng dụng tài chính nhỏ có thể cần cửa sổ phát hành hoặc phê duyệt nghiệp vụ. Chọn mức tự động hóa theo khả năng quan sát và rollback, không theo khẩu hiệu.\n\n### Concurrency ngăn deploy cũ đè deploy mới\n\nHai commit merge gần nhau có thể tạo hai run. Run cũ chậm hơn nhưng hoàn thành sau và đưa phiên bản cũ lên production. `concurrency` nhóm theo environment, thường hủy run đang chờ hoặc tuần tự hóa deploy. Tuy nhiên hủy giữa migration có thể nguy hiểm; migration cần idempotent và job critical không nên bị ngắt tùy tiện.\n\nDeploy record nên kiểm tra revision mong muốn trước khi ghi. Đây là bài toán race condition: thứ tự bắt đầu không bảo đảm thứ tự kết thúc.\n\n### Migration trong pipeline\n\nKhông rollback database bằng cách chạy migration down một cách tự động; dữ liệu mới có thể không biểu diễn được trong schema cũ. Dùng expand–migrate–contract để image cũ và mới cùng hiểu schema trong rollout. Pipeline chạy backup/kiểm tra, thực hiện migration một lần rồi health verify.\n\nMigration lâu phải có kế hoạch lock và timeout. Dấu xanh từ database nhỏ ở CI không dự đoán thời gian trên bảng production lớn. Có thể test kế hoạch trên snapshot đã khử nhạy cảm, nhưng không ghi số thời gian nếu chưa đo đúng dữ liệu/môi trường.\n\n### Rollout, smoke test và rollback\n\nSau deploy, smoke test gọi liveness, readiness và một hành vi không phá hủy như đọc summary của tài khoản test. HTTP 200 ở `/` không chứng minh database, authorization hay Redis path đúng. Metric lỗi/latency trong cửa sổ quan sát quyết định tiếp tục hay rollback.\n\nRollback dùng digest trước và giữ config tương thích. Nếu release đã phát message theo schema mới hoặc migration phá hủy cột, image cũ có thể không chạy. Vì vậy rollback là năng lực phải diễn tập, không phải nút “redeploy” tồn tại trên UI.\n\n### Artifact, report và retention\n\nTest report, coverage và SBOM giúp điều tra, nhưng artifact có thể chứa source, dữ liệu test hay log. Đặt retention hợp lý và không upload `.env`, database dump hoặc toàn workspace. Build provenance cho biết ai, workflow nào và source nào tạo artifact; nó tăng khả năng kiểm chứng chuỗi cung ứng.\n\nKhông lấy coverage 100% làm mục tiêu tuyệt đối. Một assertion tốt về authorization đáng giá hơn hàng trăm dòng chạy qua không kiểm kết quả. Gate nên tập trung contract rủi ro: tiền dương, ownership, idempotency, migration và secret.\n\n### Flaky test và kỷ luật phản hồi\n\nTest lúc đỏ lúc xanh làm đội ngũ học cách bỏ qua CI. Retry toàn suite che race condition. Trước hết lưu seed, timestamp, log và cô lập test; quarantine có owner và hạn xử lý chỉ là biện pháp tạm. Pipeline cần nhanh đủ để developer không gom pull request quá lớn, nhưng không hy sinh kiểm tra bảo mật cốt lõi.\n\n### Khi nào pipeline đơn giản tốt hơn\n\nDự án cá nhân có thể bắt đầu bằng một job lint/test và deploy thủ công có checklist. Thêm matrix mười phiên bản, preview environment, canary và ký artifact khi chưa dùng tới làm YAML khó bảo trì. Mỗi bước pipeline là production code có dependency và quyền; chỉ thêm khi nó kiểm soát một rủi ro hoặc rút ngắn feedback đo được.",

vi_du: "### Test contract bảo mật của Sổ Chi Tiêu\n\n```python\nimport pytest\nfrom httpx import ASGITransport, AsyncClient\nfrom app.main import app\n\n@pytest.mark.anyio\nasync def test_user_cannot_read_another_users_expense():\n    async with AsyncClient(\n        transport=ASGITransport(app=app), base_url=\"http://test\"\n    ) as client:\n        response = await client.get(\n            \"/expenses/42\", headers={\"Authorization\": \"Bearer user-b-token\"}\n        )\n    assert response.status_code in {403, 404}\n\n@pytest.mark.anyio\nasync def test_zero_amount_is_rejected():\n    async with AsyncClient(\n        transport=ASGITransport(app=app), base_url=\"http://test\"\n    ) as client:\n        response = await client.post(\n            \"/expenses\", json={\"amount\": 0, \"note\": \"invalid\"}\n        )\n    assert response.status_code == 422\n```\n\nFixture authentication/repository của dự án phải tạo expense thuộc user A và token user B; nếu không, test đầu chỉ là khung chưa đủ chạy. CI phải thất bại khi assertion sai, không đánh dấu xfail vĩnh viễn.\n\n### Script smoke test không làm thay đổi dữ liệu\n\n```python\nimport os\nimport sys\nimport httpx\n\nbase = os.environ[\"DEPLOY_URL\"].rstrip(\"/\")\nwith httpx.Client(timeout=5.0) as client:\n    live = client.get(f\"{base}/health/live\")\n    ready = client.get(f\"{base}/health/ready\")\n\nif live.status_code != 200 or live.json().get(\"status\") != \"alive\":\n    sys.exit(\"liveness failed\")\nif ready.status_code != 200:\n    sys.exit(\"readiness failed\")\nprint(\"deployment is ready\")\n```\n\nScript có timeout, kiểm status lẫn nội dung và trả exit code khác 0. Nó không in header authorization. Với production, endpoint ready không nên công khai chi tiết dependency.\n\n### Diễn tập ba ca biên\n\nMột là gửi hai commit liên tiếp và xác nhận concurrency không cho run cũ thắng. Hai là làm migration thất bại có chủ đích trên staging và xác nhận deploy dừng trước traffic. Ba là deploy image không qua readiness rồi xác nhận nền tảng giữ revision cũ hoặc pipeline rollback theo digest. Chỉ sau các phép thử ấy mới có bằng chứng đường phục hồi hoạt động.",

so_sanh: "### Chọn mức tự động hóa phù hợp\n\n| Cách phát hành | Điểm mạnh | Rủi ro chính | Hợp khi |\n|---|---|---|---|\n| Lệnh thủ công + checklist | Dễ bắt đầu, người vận hành quan sát trực tiếp | Không tái lập, dễ dùng sai revision | Prototype ít người dùng |\n| CI + Continuous Delivery | Artifact được kiểm chứng, production có approval | Reviewer có thể thành nút thắt | Hệ thống cần cửa sổ/phê duyệt |\n| Continuous Deployment | Feedback production nhanh, giảm hàng đợi | Đòi observability và rollback rất tốt | Thay đổi nhỏ, tương thích, rủi ro được kiểm soát |\n| GitOps | Desired state và audit rõ | Thêm controller/repository vận hành | Nhiều service/cluster cần reconciliation |\n\nGitHub Actions thuận tiện khi source ở GitHub, nhưng lock-in nằm ở syntax workflow, environment và action marketplace. Script cốt lõi như `pytest`, build image và smoke test nên chạy được local; workflow chỉ orchestration. Khi đó chuyển CI engine không đòi viết lại logic kiểm chứng.",

loi_thuong_gap: [
  "**Build lại ở từng môi trường.** Cùng commit nhưng image khác digest; build một lần, lưu digest và promote artifact.",
  "**Cấp quyền ghi ở cấp workflow.** Một step lint bị khai thác cũng đẩy image; đặt mặc định read-only và mở quyền tại job cần thiết.",
  "**Checkout code fork trong ngữ cảnh có secret.** PR có thể đọc credential; dùng event/quyền dành cho code không tin cậy và tách deploy.",
  "**Deploy theo tag trôi nổi.** Rollback tên cũ nhưng nhận bytes mới; ghi lại digest của từng environment.",
  "**Dùng `sleep` chờ service.** CI flake theo tốc độ runner; poll điều kiện health với timeout và log rõ.",
  "**Cho mọi replica chạy migration.** Deploy xuất hiện lock/race; dùng job duy nhất và schema tương thích ngược.",
  "**Retry flaky test cho tới xanh.** Lịch sử có nhiều rerun và lỗi bị bỏ qua; thu thập seed, cô lập nguyên nhân và đặt owner sửa.",
  "**Rollback code mà quên dữ liệu/config.** Image cũ không hiểu schema mới; diễn tập rollback toàn contract và dùng expand–contract."
],

tu_kiem_tra: [
  "Tại sao commit SHA và image digest trả lời hai câu hỏi khác nhau, và rollback cần giá trị nào?",
  "Một action lint bị chiếm quyền có thể làm gì nếu job có `packages: write` và secret production? Hãy thu hẹp blast radius.",
  "Giải thích vì sao `pull_request_target` kết hợp checkout code PR có thể nguy hiểm dù workflow nằm trên nhánh chính.",
  "Thiết kế concurrency để hai deploy không đảo thứ tự, nhưng migration đang chạy không bị hủy giữa chừng.",
  "Khi nào approval thủ công tăng an toàn, và khi nào nó chỉ tạo nghi thức bấm nút không có thông tin?",
  "Nêu một migration cho phép revision cũ và mới chạy song song, rồi mô tả bước contract ở release sau.",
  "Pipeline xanh nhưng production trả 500 sau deploy: những bằng chứng nào cần nối để phân biệt artifact, config, migration và dependency?"
],

tai_nguyen: [
  "GitHub Actions Documentation — workflow syntax, security hardening và OpenID Connect.",
  "Continuous Delivery — Jez Humble và David Farley.",
  "Accelerate — Nicole Forsgren, Jez Humble và Gene Kim.",
  "The DevOps Handbook — Gene Kim, Jez Humble, Patrick Debois và John Willis.",
  "Release It! — Michael T. Nygard."
],

lien_he: [
  "Nối sang mảng Kiểm thử phần mềm: test pyramid, fixture và failure injection biến contract nghiệp vụ thành gate phát hành.",
  "Nối sang mảng Bảo mật: least privilege, OIDC và kiểm soát dependency bảo vệ chuỗi cung ứng từ source đến registry.",
  "Nối sang mảng Cơ sở dữ liệu: migration tương thích và transaction quyết định có thể rollout/rollback code mà không làm hỏng dữ liệu hay không.",
  "Nối sang mảng Hệ thống phân tán: concurrency, retry, idempotency và thứ tự hoàn thành giải thích vì sao hai workflow song song có thể tạo trạng thái sai."
]

};
