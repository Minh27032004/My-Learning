window.LESSONS["01:lap-trinh-huong-doi-tuong:solid-dependency-inversion"] = {

tom_tat: [
  "**Module cấp cao không phụ thuộc module cấp thấp — cả hai phụ thuộc vào trừu tượng.** \"Đảo\" ở đây là đảo chiều mũi tên phụ thuộc, không phải đảo thứ tự gọi hàm.",
  "**Trừu tượng phải thuộc về tầng cấp cao.** Đây là điểm hay bị bỏ qua nhất: `Protocol` đặt cạnh code nghiệp vụ, không đặt cạnh code database.",
  "**Dependency Injection là kỹ thuật, DIP là nguyên tắc.** Tiêm phụ thuộc qua `__init__` là cách phổ biến nhất để đạt DIP, nhưng không phải cách duy nhất.",
  "**Dấu hiệu vi phạm rõ nhất: `import psycopg2` hoặc `requests` nằm trong file chứa logic nghiệp vụ**, và class tự tạo phụ thuộc của mình bên trong `__init__`.",
  "**Lợi ích đo được ngay ở kiểm thử**: logic nghiệp vụ kiểm thử được mà không cần database, không cần mạng, chạy trong mili giây."
],

can_biet_truoc: [
  "Bài **SOLID — Interface Segregation** ngay trước — trừu tượng phải nhỏ và đúng vai trò thì đảo phụ thuộc mới có ích.",
  "Bài **Composition over inheritance** — tiêm phụ thuộc qua `__init__` là composition.",
  "Bài **Trừu tượng: ABC, Protocol** — `Protocol` là công cụ chính, và ai *sở hữu* trừu tượng là câu hỏi trung tâm ở đây.",
  "Bài **SOLID — Single Responsibility** — quy tắc \"tầng nghiệp vụ không phụ thuộc hạ tầng\" đã nêu ở đó, bài này giải thích cách làm."
],

dinh_nghia: "Nói đời thường: một cái đèn **không được hàn chết vào đường dây điện**. Nó có phích cắm, và ổ cắm là **giao ước chung** mà cả nhà sản xuất đèn lẫn thợ điện đều tuân theo. Nhờ vậy đổi đèn không phải đục tường, và đi lại dây không phải mua đèn mới.\n\nNói chính xác:\n\n- **DIP** (*Dependency Inversion Principle*) — hai vế:\n  - Module **cấp cao** không phụ thuộc module **cấp thấp**; cả hai phụ thuộc vào **trừu tượng**.\n  - Trừu tượng không phụ thuộc chi tiết; **chi tiết phụ thuộc trừu tượng**.\n- **Module cấp cao** — chứa **logic nghiệp vụ**, quy tắc, quyết định. Phần có giá trị nhất và ổn định nhất.\n- **Module cấp thấp** — chi tiết kỹ thuật: database, HTTP, tệp, email, hàng đợi.\n- **Dependency Injection** (*tiêm phụ thuộc*) — **kỹ thuật** đưa phụ thuộc từ ngoài vào thay vì tự tạo bên trong.\n- **Ai sở hữu trừu tượng** — trừu tượng phải **thuộc về** tầng cấp cao, không thuộc tầng cấp thấp. Đây là chi tiết quyết định.\n\nĐiểm cần đính chính ngay: **\"đảo\" không có nghĩa là đảo thứ tự gọi.** Luồng thực thi vẫn là nghiệp vụ gọi database. Cái bị đảo là **chiều phụ thuộc lúc biên dịch**: thay vì nghiệp vụ `import` database, giờ database `import` trừu tượng do nghiệp vụ định nghĩa.",

vi_sao_can: "DIP là nguyên tắc quyết định phần code có giá trị nhất của bạn có bị trói vào công nghệ hay không:\n\n- **Logic nghiệp vụ là tài sản, hạ tầng là công cụ.** Công thức tính giá, quy tắc duyệt đơn, chính sách chiết khấu — đó là thứ công ty trả tiền cho bạn. Buộc chúng vào một database cụ thể là làm tài sản đó khó di chuyển.\n- **Kiểm thử nhanh hay chậm phụ thuộc vào nó.** Không có DIP, kiểm thử một công thức cần database thật — chạy hàng chục giây và hỏng vì lý do không liên quan. Có DIP, nó chạy trong mili giây.\n- **Nó cho phép đổi công nghệ.** Đổi từ một database sang database khác, đổi nhà cung cấp email, thêm cache — đều là viết một bản cài đặt mới, không sửa logic.\n- **Nó cho phép làm việc song song.** Định nghĩa trừu tượng trước, rồi hai người viết hai tầng cùng lúc.\n- **Nền cho kiến trúc phân tầng.** Mọi kiến trúc nghiêm túc — dù tên gọi khác nhau — đều dựa trên đúng ý này.\n\nTình huống thật rất kinh điển: class `DichVuDonHang` có `self.conn = psycopg2.connect(...)` trong `__init__`. Sáu tháng sau, nhóm muốn viết kiểm thử cho quy tắc \"đơn trên 10 triệu cần duyệt\". Nhưng **không thể tạo object mà không có database**, nên hoặc bạn dựng cả một database cho kiểm thử, hoặc không kiểm thử gì cả. Phần cơ chế sẽ chỉ cách đảo chiều phụ thuộc để quy tắc đó kiểm thử được trong ba dòng.",

co_che: "### Chiều phụ thuộc — trước và sau khi đảo\n\n```\n❌ TRƯỚC — nghiệp vụ phụ thuộc hạ tầng\n\n   DichVuDonHang  ──import──►  KhoPostgres  ──►  psycopg2\n   (cấp cao)                   (cấp thấp)\n\n   Đổi database ⇒ SỬA DichVuDonHang\n```\n\n```\n✓ SAU — cả hai phụ thuộc vào trừu tượng\n\n   DichVuDonHang  ──►  KhoDonHang (Protocol)  ◄──  KhoPostgres\n   (cấp cao)           ⭐ thuộc tầng CAO           (cấp thấp)\n\n   Mũi tên từ KhoPostgres đi NGƯỢC LÊN — đó là \"đảo\"\n```\n\nĐiểm mấu chốt: **mũi tên từ tầng thấp đi ngược lên tầng cao.** Đó chính xác là thứ được đảo.\n\nLuồng thực thi **không đổi**: `DichVuDonHang` vẫn gọi `KhoPostgres`. Chỉ có phụ thuộc lúc biên dịch bị đảo.\n\n### Ai sở hữu trừu tượng — điểm quyết định\n\nĐây là phần hay bị làm sai nhất, và làm sai thì DIP không có tác dụng gì.\n\n```\n❌ Trừu tượng thuộc tầng THẤP\n   ha_tang/kho.py       → định nghĩa Protocol KhoDonHang\n   nghiep_vu/dich_vu.py → from ha_tang.kho import KhoDonHang\n\n   ⇒ nghiệp vụ VẪN import từ hạ tầng — chưa đảo được gì\n```\n\n```\n✓ Trừu tượng thuộc tầng CAO\n   nghiep_vu/cong.py    → định nghĩa Protocol KhoDonHang  ⭐\n   nghiep_vu/dich_vu.py → from .cong import KhoDonHang\n   ha_tang/postgres.py  → from nghiep_vu.cong import KhoDonHang\n\n   ⇒ mũi tên đi từ hạ tầng LÊN nghiệp vụ ✓\n```\n\nPhép kiểm nhanh và rất đáng nhớ: **mở mọi file trong thư mục nghiệp vụ, xem phần `import`.** Có `psycopg2`, `requests`, `boto3`, hay bất kỳ tên thư viện hạ tầng nào — DIP chưa đạt.\n\nMột hệ quả về đặt tên: trừu tượng nên mang tên theo **nhu cầu của tầng cao**, không theo công nghệ.\n\n```\n❌ KhoPostgres, BoGuiSMTP, CacheRedis   ← tên theo công nghệ\n✓ KhoDonHang, BoThongBao, BoNhoDem      ← tên theo nhu cầu\n```\n\nTên theo công nghệ là dấu hiệu trừu tượng được rút ra từ bản cài đặt, chứ không từ nhu cầu — và nó sẽ mang theo chi tiết không cần thiết.\n\n### Vi phạm điển hình\n\n```python\n# ❌ Nghiệp vụ tự tạo phụ thuộc\nimport psycopg2, smtplib          # ⚠️ hạ tầng trong file nghiệp vụ\n\nclass DichVuDonHang:\n    def __init__(self):\n        self.conn = psycopg2.connect(\"dbname=app\")     # ⚠️ TỰ TẠO\n        self.smtp = smtplib.SMTP(\"smtp.x.com\")         # ⚠️ TỰ TẠO\n\n    def tao_don(self, du_lieu: dict) -> int:\n        if du_lieu[\"tong\"] <= 0:\n            raise ValueError(\"tổng phải dương\")\n        if du_lieu[\"tong\"] > 10_000_000:               # ⭐ QUY TẮC NGHIỆP VỤ\n            du_lieu[\"can_duyet\"] = True\n\n        cur = self.conn.cursor()\n        cur.execute(\"INSERT INTO don (...) VALUES (...) RETURNING id\", ...)\n        ma = cur.fetchone()[0]\n        self.conn.commit()\n\n        self.smtp.sendmail(...)\n        return ma\n```\n\nBốn vấn đề:\n\n```\n① Không tạo được object mà không có database và máy chủ mail\n② Không kiểm thử được quy tắc \"trên 10 triệu cần duyệt\"\n③ Đổi database ⇒ sửa file chứa logic nghiệp vụ\n④ Class TỰ QUYẾT ĐỊNH dùng công nghệ nào — người dùng không có lựa chọn\n```\n\nĐiểm ④ là gốc rễ: **class tự tạo phụ thuộc thì người gọi không thay được.**\n\n### Đảo phụ thuộc\n\n```python\n# ── nghiep_vu/cong.py — TRỪU TƯỢNG thuộc tầng cao ──\nfrom typing import Protocol\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass DonHang:\n    ma: int | None\n    tong: float\n    email: str\n    can_duyet: bool = False\n\n\nclass KhoDonHang(Protocol):\n    def luu(self, don: DonHang) -> int: ...\n    def lay(self, ma: int) -> DonHang | None: ...\n\nclass BoThongBao(Protocol):\n    def gui(self, den: str, tin: str) -> None: ...\n```\n\n```python\n# ── nghiep_vu/dich_vu.py — CHỈ import từ cùng tầng ──\nfrom .cong import KhoDonHang, BoThongBao, DonHang\n\nNGUONG_DUYET = 10_000_000\n\nclass DichVuDonHang:\n    def __init__(self, kho: KhoDonHang, thong_bao: BoThongBao):\n        self.kho = kho                    # ⭐ NHẬN vào, không tự tạo\n        self.thong_bao = thong_bao\n\n    def tao_don(self, tong: float, email: str) -> int:\n        if tong <= 0:\n            raise ValueError(f\"tổng phải dương: {tong}\")\n\n        don = DonHang(None, tong, email, can_duyet=tong > NGUONG_DUYET)\n        ma = self.kho.luu(don)\n\n        tin = (\"Đơn của bạn đang chờ duyệt\" if don.can_duyet\n               else \"Đã nhận đơn hàng\")\n        self.thong_bao.gui(email, tin)\n        return ma\n```\n\n```python\n# ── ha_tang/postgres.py — TẦNG THẤP import NGƯỢC LÊN ──\nimport psycopg2\nfrom nghiep_vu.cong import KhoDonHang, DonHang      # ⭐ mũi tên đi lên\n\nclass KhoPostgres:\n    def __init__(self, dsn: str):\n        self.conn = psycopg2.connect(dsn)\n\n    def luu(self, don: DonHang) -> int:\n        cur = self.conn.cursor()\n        cur.execute(\n            \"INSERT INTO don (tong, email, can_duyet) VALUES (%s,%s,%s) \"\n            \"RETURNING id\",\n            (don.tong, don.email, don.can_duyet))\n        ma = cur.fetchone()[0]\n        self.conn.commit()\n        return ma\n\n    def lay(self, ma):\n        ...\n```\n\n```python\n# ── main.py — nơi DUY NHẤT biết mọi thứ cụ thể ──\nfrom nghiep_vu.dich_vu import DichVuDonHang\nfrom ha_tang.postgres import KhoPostgres\nfrom ha_tang.email import GuiEmail\n\ndef main():\n    dv = DichVuDonHang(KhoPostgres(DSN), GuiEmail(SMTP))    # ⭐ ghép ở đây\n    dv.tao_don(15_000_000, \"khach@x.com\")\n```\n\nCấu trúc này gọi là **thành phần gốc** (*composition root*): chỉ một chỗ trong toàn bộ ứng dụng biết mọi bản cài đặt cụ thể, và nó nằm ở điểm vào chương trình.\n\n### Ba cách tiêm phụ thuộc\n\n```python\n# ① Qua __init__ — ⭐ phổ biến nhất, dùng cho phụ thuộc BẮT BUỘC\nclass DichVu:\n    def __init__(self, kho: KhoDonHang):\n        self.kho = kho\n\n# ② Qua tham số phương thức — cho phụ thuộc chỉ MỘT phương thức cần\nclass DichVu:\n    def xuat(self, don, bo_dinh_dang: BoDinhDang) -> str:\n        return bo_dinh_dang.dinh_dang(don)\n\n# ③ Qua thuộc tính — cho phụ thuộc TUỲ CHỌN, có mặc định\nclass DichVu:\n    def __init__(self, kho):\n        self.kho = kho\n        self.logger = LoggerIm()          # mặc định vô hại\n```\n\nCách chọn:\n\n```\nBắt buộc, mọi phương thức đều dùng    → __init__\nChỉ một phương thức cần               → tham số phương thức\nTuỳ chọn, có mặc định hợp lý          → thuộc tính\n```\n\nCách ③ có một lợi ích đáng chú ý: **mặc định vô hại** (`LoggerIm` không làm gì) khiến người dùng không phải truyền gì khi không quan tâm, mà vẫn thay được khi cần.\n\n### Mặc định thuận tiện mà không phá DIP\n\nMột phản đối thường gặp: \"tiêm phụ thuộc làm việc tạo object phiền phức\".\n\n```python\n# ✓ Có mặc định, mà vẫn thay được\nclass DichVuDonHang:\n    def __init__(self, kho: KhoDonHang | None = None,\n                 thong_bao: BoThongBao | None = None):\n        self.kho = kho or KhoBoNho()              # mặc định nhẹ\n        self.thong_bao = thong_bao or ThongBaoIm()\n\nDichVuDonHang()                                    # tiện cho thử nghiệm\nDichVuDonHang(KhoPostgres(DSN), GuiEmail(SMTP))     # đầy đủ khi chạy thật\n```\n\nNhưng cẩn thận: **mặc định không được là một phụ thuộc nặng.**\n\n```python\n# ❌ Mặc định phá DIP — vẫn import psycopg2 vào tầng nghiệp vụ\ndef __init__(self, kho=None):\n    self.kho = kho or KhoPostgres(DSN)    # ⚠️ nghiệp vụ lại biết Postgres\n```\n\nRanh giới: **mặc định phải là bản cài đặt trong bộ nhớ hoặc bản không làm gì** — thứ không kéo theo thư viện ngoài nào.\n\n### DIP không cần class\n\nDIP là về **chiều phụ thuộc**, không về OOP:\n\n```python\n# ❌ Hàm tự quyết định cách ghi\ndef xu_ly(du_lieu):\n    kq = tinh_toan(du_lieu)\n    with open(\"ket_qua.json\", \"w\") as f:      # ⚠️ tự quyết định ghi ra file\n        json.dump(kq, f)\n\n# ✓ Nhận hàm ghi từ ngoài\nfrom typing import Callable\n\ndef xu_ly(du_lieu, ghi: Callable[[dict], None]) -> None:\n    ghi(tinh_toan(du_lieu))\n\nxu_ly(d, ghi=lambda kq: Path(\"ra.json\").write_text(json.dumps(kq)))\nxu_ly(d, ghi=ket_qua.append)               # ⭐ cho kiểm thử\n```\n\nTruyền hàm là cách tiêm phụ thuộc **nhẹ nhất**, và với một thao tác thì nó thường đủ — đúng như đã bàn ở bài Open/Closed.\n\n### Đừng đảo mọi thứ\n\nCân bằng lại, vì DIP bị lạm dụng nhiều:\n\n```python\n# ❌ Trừu tượng vô nghĩa\nclass BoTinhToan(Protocol):\n    def cong(self, a: int, b: int) -> int: ...\n\nclass MayTinh:\n    def __init__(self, bo: BoTinhToan): self.bo = bo\n```\n\nChỉ đảo những phụ thuộc **hướng ra ngoài hệ thống**:\n\n```\nĐẢO:      database, API mạng, hệ thống file, email, hàng đợi,\n          đồng hồ (time.now), số ngẫu nhiên, biến môi trường\n\nKHÔNG:    thư viện chuẩn thuần tuý (math, json, re, datetime để tính toán)\n          class dữ liệu của chính bạn\n          hàm thuần không có tác dụng phụ\n```\n\nHai mục cuối của danh sách đảo đáng chú ý và hay bị bỏ sót: **thời gian và số ngẫu nhiên là phụ thuộc ra ngoài**, và không đảo chúng thì kiểm thử không lặp lại được.\n\n```python\n# ❌ Không kiểm thử được — kết quả đổi theo ngày\ndef tinh_phi_tre_han(han: date) -> float:\n    ngay_tre = (date.today() - han).days\n    return max(0, ngay_tre) * 10_000\n\n# ✓ Tiêm đồng hồ\ndef tinh_phi_tre_han(han: date, hom_nay: date | None = None) -> float:\n    hom_nay = hom_nay or date.today()\n    return max(0, (hom_nay - han).days) * 10_000\n\ntinh_phi_tre_han(date(2026, 1, 1), hom_nay=date(2026, 1, 11))    # 100_000 ✓\n```\n\nMẫu `hom_nay=None` giữ được sự tiện lợi khi dùng thật mà vẫn kiểm thử được — chi phí gần như bằng không.\n\n### DIP và các nguyên tắc khác\n\n```\nSRP  tách tầng      → có tầng rõ ràng mới đảo được\nOCP  thêm bản cài đặt không sửa code cũ → DIP tạo điều kiện\nLSP  mọi bản cài đặt tôn trọng giao ước → DIP mới an toàn\nISP  trừu tượng nhỏ, đúng vai trò       → đảo mới có ích\n```\n\nDIP là nguyên tắc **gộp lại** cả bốn: nó cần SRP để có tầng, ISP để trừu tượng đúng kích thước, LSP để các bản cài đặt thay nhau được, và nó tạo điều kiện cho OCP.\n\n### Cái giá\n\n```\n· Nhiều file, nhiều Protocol hơn\n· Lần một luồng phải mở nhiều chỗ\n· Thành phần gốc phình dần theo số phụ thuộc\n· Người mới khó biết bản cài đặt nào đang chạy\n· Trừu tượng sai chỗ còn tệ hơn không có\n```\n\nNhịp áp dụng, giống các nguyên tắc trước:\n\n```\nBắt đầu:  viết thẳng, gọi trực tiếp\nKhi cần:  kiểm thử được, hoặc đổi công nghệ, hoặc có bản cài đặt thứ hai\n          → lúc đó mới đảo\n```\n\nNhưng có một ngoại lệ đáng làm ngay từ đầu: **tách logic nghiệp vụ khỏi hạ tầng gần như luôn đáng**, kể cả trong dự án nhỏ. Vì nó cho phép kiểm thử — và kiểm thử là thứ bạn cần ngay từ ngày đầu.",

vi_du: "### Trước và sau — đo bằng bài kiểm thử\n\n```python\n# ❌ TRƯỚC — không kiểm thử được\nimport psycopg2, smtplib\n\nclass DichVuDangKy:\n    def __init__(self):\n        self.conn = psycopg2.connect(\"dbname=app\")\n        self.smtp = smtplib.SMTP(\"smtp.x.com\")\n\n    def dang_ky(self, email: str, mat_khau: str) -> int:\n        if \"@\" not in email:\n            raise ValueError(\"email không hợp lệ\")\n        if len(mat_khau) < 8:\n            raise ValueError(\"mật khẩu quá ngắn\")\n\n        cur = self.conn.cursor()\n        cur.execute(\"SELECT 1 FROM nguoi_dung WHERE email=%s\", (email,))\n        if cur.fetchone():\n            raise ValueError(\"email đã tồn tại\")\n\n        cur.execute(\"INSERT INTO nguoi_dung (email, mk) VALUES (%s,%s) \"\n                    \"RETURNING id\", (email, bam(mat_khau)))\n        uid = cur.fetchone()[0]\n        self.conn.commit()\n        self.smtp.sendmail(\"no-reply@x.com\", email, \"Chào mừng\")\n        return uid\n\n# Muốn kiểm thử \"mật khẩu quá ngắn\"?\n# → DichVuDangKy() đã cần database và máy chủ mail rồi\n```\n\n```python\n# ✓ SAU\nfrom typing import Protocol\nfrom dataclasses import dataclass\n\n# ── nghiep_vu/cong.py ──\n@dataclass(frozen=True)\nclass NguoiDung:\n    id: int | None\n    email: str\n    bam_mat_khau: str\n\nclass KhoNguoiDung(Protocol):\n    def tim_theo_email(self, email: str) -> NguoiDung | None: ...\n    def luu(self, nd: NguoiDung) -> int: ...\n\nclass BoThongBao(Protocol):\n    def gui(self, den: str, tin: str) -> None: ...\n\nclass BoBamMatKhau(Protocol):\n    def bam(self, mk: str) -> str: ...\n\n\n# ── nghiep_vu/dich_vu.py ──\nDAI_TOI_THIEU = 8\n\nclass DichVuDangKy:\n    def __init__(self, kho: KhoNguoiDung, thong_bao: BoThongBao,\n                 bam: BoBamMatKhau):\n        self.kho, self.thong_bao, self.bam = kho, thong_bao, bam\n\n    def dang_ky(self, email: str, mat_khau: str) -> int:\n        email = email.strip().lower()\n        if \"@\" not in email:\n            raise ValueError(f\"email không hợp lệ: {email!r}\")\n        if len(mat_khau) < DAI_TOI_THIEU:\n            raise ValueError(f\"mật khẩu cần ít nhất {DAI_TOI_THIEU} ký tự\")\n        if self.kho.tim_theo_email(email) is not None:\n            raise ValueError(\"email đã tồn tại\")\n\n        uid = self.kho.luu(NguoiDung(None, email, self.bam.bam(mat_khau)))\n        self.thong_bao.gui(email, \"Chào mừng bạn\")\n        return uid\n```\n\nKiểm thử — không database, không mạng:\n\n```python\nimport pytest\n\nclass KhoGia:\n    def __init__(self, co_san=()):\n        self.nd = {n.email: n for n in co_san}\n        self.da_luu = []\n    def tim_theo_email(self, email):\n        return self.nd.get(email)\n    def luu(self, nd):\n        self.da_luu.append(nd)\n        return len(self.da_luu)\n\nclass ThongBaoGia:\n    def __init__(self): self.da_gui = []\n    def gui(self, den, tin): self.da_gui.append((den, tin))\n\nclass BamGia:\n    def bam(self, mk): return f\"bam:{mk}\"\n\n\ndef tao_dich_vu(co_san=()):\n    kho, tb = KhoGia(co_san), ThongBaoGia()\n    return DichVuDangKy(kho, tb, BamGia()), kho, tb\n\n\ndef test_mat_khau_qua_ngan():\n    dv, kho, _ = tao_dich_vu()\n    with pytest.raises(ValueError, match=\"ít nhất 8\"):\n        dv.dang_ky(\"a@x.com\", \"ngan\")\n    assert kho.da_luu == []              # ⭐ kiểm được: KHÔNG lưu gì\n\ndef test_email_trung():\n    co_san = [NguoiDung(1, \"a@x.com\", \"bam:cu\")]\n    dv, kho, _ = tao_dich_vu(co_san)\n    with pytest.raises(ValueError, match=\"đã tồn tại\"):\n        dv.dang_ky(\"A@X.COM\", \"matkhaudai\")     # ⭐ kiểm cả việc chuẩn hoá\n\ndef test_dang_ky_thanh_cong_gui_thong_bao():\n    dv, kho, tb = tao_dich_vu()\n    uid = dv.dang_ky(\"  A@X.com \", \"matkhaudai\")\n\n    assert uid == 1\n    assert kho.da_luu[0].email == \"a@x.com\"     # đã chuẩn hoá\n    assert kho.da_luu[0].bam_mat_khau == \"bam:matkhaudai\"\n    assert tb.da_gui == [(\"a@x.com\", \"Chào mừng bạn\")]\n```\n\nSo sánh cụ thể:\n\n```\nTRƯỚC: cần database + máy chủ mail, chạy vài giây, hỏng vì lý do không liên quan\nSAU:   không cần gì, chạy mili giây, kiểm được CẢ TƯƠNG TÁC\n```\n\nDòng `assert kho.da_luu == []` là thứ bản đầu **không kiểm được**: nó xác nhận rằng khi mật khẩu sai thì không có gì được ghi vào kho — một quy tắc quan trọng mà không có DIP thì phải mở database ra xem.\n\n### Đổi công nghệ — không sửa nghiệp vụ\n\n```python\n# ha_tang/postgres.py\nimport psycopg2\nfrom nghiep_vu.cong import KhoNguoiDung, NguoiDung\n\nclass KhoPostgres:\n    def __init__(self, dsn): self.conn = psycopg2.connect(dsn)\n    def tim_theo_email(self, email):\n        cur = self.conn.cursor()\n        cur.execute(\"SELECT id, email, mk FROM nguoi_dung WHERE email=%s\",\n                    (email,))\n        r = cur.fetchone()\n        return NguoiDung(*r) if r else None\n    def luu(self, nd):\n        cur = self.conn.cursor()\n        cur.execute(\"INSERT INTO nguoi_dung (email, mk) VALUES (%s,%s) \"\n                    \"RETURNING id\", (nd.email, nd.bam_mat_khau))\n        uid = cur.fetchone()[0]\n        self.conn.commit()\n        return uid\n\n\n# ha_tang/sqlite.py — công nghệ khác, KHÔNG sửa nghiệp vụ\nimport sqlite3\nfrom nghiep_vu.cong import KhoNguoiDung, NguoiDung\n\nclass KhoSQLite:\n    def __init__(self, dd): self.conn = sqlite3.connect(dd)\n    def tim_theo_email(self, email):\n        r = self.conn.execute(\n            \"SELECT id, email, mk FROM nguoi_dung WHERE email=?\",\n            (email,)).fetchone()\n        return NguoiDung(*r) if r else None\n    def luu(self, nd):\n        cur = self.conn.execute(\n            \"INSERT INTO nguoi_dung (email, mk) VALUES (?,?)\",\n            (nd.email, nd.bam_mat_khau))\n        self.conn.commit()\n        return cur.lastrowid\n\n\n# main.py — chỉ đổi MỘT dòng\ndv = DichVuDangKy(KhoSQLite(\"app.db\"), GuiEmail(SMTP), BamBcrypt())\n```\n\nHai bản cài đặt, cùng giao ước. `DichVuDangKy` không biết và không cần biết cái nào đang chạy.\n\nĐiều kiện để việc này an toàn: cả hai phải **tôn trọng cùng giao ước** — đúng nội dung bài Liskov. Bộ kiểm thử giao ước ở bài đó là cách xác nhận:\n\n```python\n@pytest.fixture(params=[lambda: KhoGia(), lambda: KhoSQLite(\":memory:\")])\ndef kho(request):\n    return request.param()\n\ndef test_tim_email_khong_ton_tai_tra_None(kho):\n    assert kho.tim_theo_email(\"khong@co.com\") is None     # cam kết chung\n```\n\n### Thêm cache — không sửa nghiệp vụ\n\n```python\nclass KhoCoCache:\n    \"\"\"Bọc một kho khác, thêm cache. Cùng giao diện KhoNguoiDung.\"\"\"\n\n    def __init__(self, goc: KhoNguoiDung, kich_thuoc: int = 1000):\n        self._goc = goc\n        self._cache: dict[str, NguoiDung] = {}\n        self._kich_thuoc = kich_thuoc\n\n    def tim_theo_email(self, email):\n        if email in self._cache:\n            return self._cache[email]\n        nd = self._goc.tim_theo_email(email)\n        if nd is not None:\n            if len(self._cache) >= self._kich_thuoc:\n                self._cache.pop(next(iter(self._cache)))     # bỏ cái cũ nhất\n            self._cache[email] = nd\n        return nd\n\n    def luu(self, nd):\n        uid = self._goc.luu(nd)\n        self._cache.pop(nd.email, None)          # ⭐ vô hiệu cache\n        return uid\n\n\n# Xếp chồng — nghiệp vụ không biết gì\ndv = DichVuDangKy(KhoCoCache(KhoPostgres(DSN)), GuiEmail(SMTP), BamBcrypt())\n```\n\nĐây là mẫu **Decorator ở tầng object**: `KhoCoCache` cài cùng giao diện và bọc quanh một bản cài đặt khác. Thêm được cache, thêm được ghi log, thêm được đo thời gian — mỗi thứ một lớp bọc.\n\nChú ý dòng `self._cache.pop(nd.email, None)` trong `luu`: quên vô hiệu cache khi ghi là lỗi kinh điển, và nó cho dữ liệu cũ mà không có lỗi nào báo.\n\n### Tiêm đồng hồ và số ngẫu nhiên\n\n```python\nfrom typing import Protocol, Callable\nfrom datetime import datetime, date\n\nclass DongHo(Protocol):\n    def bay_gio(self) -> datetime: ...\n\nclass DongHoThat:\n    def bay_gio(self): return datetime.now()\n\nclass DongHoCoDinh:\n    def __init__(self, luc: datetime): self.luc = luc\n    def bay_gio(self): return self.luc\n\n\nclass DichVuPhien:\n    HAN_PHIEN_PHUT = 30\n\n    def __init__(self, kho, dong_ho: DongHo | None = None):\n        self.kho = kho\n        self.dong_ho = dong_ho or DongHoThat()      # ⭐ mặc định tiện\n\n    def con_han(self, phien) -> bool:\n        troi_qua = (self.dong_ho.bay_gio() - phien.tao_luc).total_seconds() / 60\n        return troi_qua < self.HAN_PHIEN_PHUT\n\n\n# Kiểm thử — điều khiển được thời gian\ndef test_phien_het_han():\n    tao_luc = datetime(2026, 1, 1, 10, 0)\n    dv = DichVuPhien(KhoGia(), DongHoCoDinh(datetime(2026, 1, 1, 10, 31)))\n    assert dv.con_han(Phien(tao_luc=tao_luc)) is False\n\ndef test_phien_con_han():\n    tao_luc = datetime(2026, 1, 1, 10, 0)\n    dv = DichVuPhien(KhoGia(), DongHoCoDinh(datetime(2026, 1, 1, 10, 29)))\n    assert dv.con_han(Phien(tao_luc=tao_luc)) is True\n```\n\nKhông tiêm đồng hồ thì hai bài kiểm thử này **bất khả thi** — bạn không đợi 31 phút được, và không kiểm được ranh giới chính xác.\n\nTương tự với số ngẫu nhiên:\n\n```python\ndef chia_nhom(nguoi: list[str], so_nhom: int,\n              tron: Callable[[list], None] | None = None) -> list[list[str]]:\n    import random\n    ds = list(nguoi)\n    (tron or random.shuffle)(ds)                # ⭐ tiêm hàm trộn\n    return [ds[i::so_nhom] for i in range(so_nhom)]\n\n# Kiểm thử — kết quả xác định\nchia_nhom([\"a\",\"b\",\"c\",\"d\"], 2, tron=lambda ds: None)   # không trộn\n```\n\n### Thành phần gốc\n\n```python\n# main.py — nơi DUY NHẤT biết mọi thứ cụ thể\nimport os\nfrom nghiep_vu.dich_vu import DichVuDangKy, DichVuDonHang\nfrom ha_tang.postgres import KhoPostgres, KhoDonHangPostgres\nfrom ha_tang.email import GuiEmail\nfrom ha_tang.cache import KhoCoCache\nfrom ha_tang.bam import BamBcrypt\n\n\ndef dung_ung_dung(che_do: str = \"that\"):\n    \"\"\"Thành phần gốc: ghép mọi thứ ở MỘT chỗ.\"\"\"\n    if che_do == \"kiem_thu\":\n        kho_nd, kho_don = KhoGia(), KhoDonHangGia()\n        thong_bao = ThongBaoGia()\n    else:\n        dsn = os.environ[\"DATABASE_URL\"]\n        kho_nd = KhoCoCache(KhoPostgres(dsn))       # ⭐ xếp chồng ở đây\n        kho_don = KhoDonHangPostgres(dsn)\n        thong_bao = GuiEmail(os.environ[\"SMTP_HOST\"])\n\n    return {\n        \"dang_ky\": DichVuDangKy(kho_nd, thong_bao, BamBcrypt()),\n        \"don_hang\": DichVuDonHang(kho_don, thong_bao),\n    }\n\n\nif __name__ == \"__main__\":\n    ud = dung_ung_dung()\n    ud[\"dang_ky\"].dang_ky(\"a@x.com\", \"matkhaudai\")\n```\n\nBa tính chất của thành phần gốc:\n\n```\n① Là nơi DUY NHẤT import các bản cài đặt cụ thể\n② Đọc cấu hình từ biến môi trường (bài venv/pip ở module 5)\n③ Đổi chế độ chạy chỉ bằng một tham số\n```\n\nĐây cũng là chỗ duy nhất trong ứng dụng vi phạm DIP một cách có chủ đích — và điều đó **hoàn toàn đúng**: phải có một nơi biết mọi thứ cụ thể, và tốt nhất là nó nằm ngoài cùng.\n\n### Kiểm tra chiều phụ thuộc tự động\n\n```python\n# tests/test_kien_truc.py\nimport ast\nfrom pathlib import Path\n\nCAM = {\"psycopg2\", \"sqlite3\", \"smtplib\", \"requests\", \"boto3\", \"redis\"}\n\ndef test_nghiep_vu_khong_import_ha_tang():\n    \"\"\"Tầng nghiệp vụ không được biết tới thư viện hạ tầng nào.\"\"\"\n    loi = []\n    for f in Path(\"nghiep_vu\").rglob(\"*.py\"):\n        cay = ast.parse(f.read_text(encoding=\"utf-8\"))\n        for nut in ast.walk(cay):\n            if isinstance(nut, ast.Import):\n                ten = [a.name.split(\".\")[0] for a in nut.names]\n            elif isinstance(nut, ast.ImportFrom):\n                ten = [(nut.module or \"\").split(\".\")[0]]\n            else:\n                continue\n            for t in ten:\n                if t in CAM:\n                    loi.append(f\"{f}:{nut.lineno} import {t}\")\n\n    assert not loi, \"nghiệp vụ import hạ tầng:\\n\" + \"\\n\".join(loi)\n\n\ndef test_nghiep_vu_khong_import_ha_tang_noi_bo():\n    \"\"\"Và không import gói ha_tang của chính dự án.\"\"\"\n    for f in Path(\"nghiep_vu\").rglob(\"*.py\"):\n        nd = f.read_text(encoding=\"utf-8\")\n        assert \"from ha_tang\" not in nd and \"import ha_tang\" not in nd, f\n```\n\nHai bài kiểm thử này biến DIP từ **quy ước mà ai cũng có thể quên** thành **luật được thực thi tự động**. Chúng chạy trong CI và chặn ngay lần đầu ai đó thêm một `import` sai chiều.\n\nĐây là kỹ thuật đáng giá nhất trong bài: nguyên tắc kiến trúc chỉ tồn tại được lâu dài nếu có thứ gì đó kiểm tra nó.\n\n### Khi KHÔNG nên đảo\n\n```python\n# ❌ Trừu tượng hoá thư viện chuẩn thuần tuý\nclass BoTinhToan(Protocol):\n    def can_bac_hai(self, x: float) -> float: ...\n\nclass MayTinh:\n    def __init__(self, bo: BoTinhToan): self.bo = bo\n\n# ✓ Cứ dùng thẳng\nimport math\n\ndef tinh_khoang_cach(a, b) -> float:\n    return math.sqrt((a.x - b.x) ** 2 + (a.y - b.y) ** 2)\n```\n\n`math.sqrt` là hàm thuần, không có tác dụng phụ, không bao giờ đổi, và kiểm thử được trực tiếp. Trừu tượng hoá nó chỉ thêm một tầng vô nghĩa.\n\nDanh sách quyết định:\n\n```\nĐẢO khi phụ thuộc:\n  · đi ra ngoài tiến trình (database, mạng, file, email)\n  · KHÔNG xác định (thời gian, số ngẫu nhiên, biến môi trường)\n  · chậm hoặc tốn tiền\n  · có thể cần đổi bản cài đặt\n\nKHÔNG đảo khi phụ thuộc:\n  · là hàm thuần của thư viện chuẩn (math, json, re)\n  · là class dữ liệu của chính bạn\n  · sẽ không bao giờ có bản thứ hai\n```",

so_sanh: "### Trước và sau khi đảo\n\n| | Phụ thuộc trực tiếp | Đã đảo |\n|---|---|---|\n| Nghiệp vụ import hạ tầng | **✓** | ✗ |\n| Kiểm thử cần database | **✓** | ✗ |\n| Đổi công nghệ | sửa nghiệp vụ | **viết bản cài đặt mới** |\n| Thêm cache, log | sửa nghiệp vụ | **bọc thêm một lớp** |\n| Số file | ít | nhiều hơn |\n| Biết bản nào đang chạy | dễ | phải xem thành phần gốc |\n\nHai hàng cuối là cái giá thật. Chúng được bù lại bằng bốn hàng trên khi code sống lâu và cần kiểm thử.\n\n### Ai sở hữu trừu tượng\n\n| | Trừu tượng ở tầng thấp | Trừu tượng ở tầng cao |\n|---|---|---|\n| Nghiệp vụ import từ | **hạ tầng** ✗ | cùng tầng ✓ |\n| Chiều mũi tên | vẫn xuôi | **đã đảo** |\n| Đạt DIP | **✗** | ✓ |\n| Tên giao diện | theo công nghệ | **theo nhu cầu** |\n\nĐây là điểm hay làm sai nhất: đặt `Protocol` cạnh code database là **chưa đảo được gì**. Phép kiểm: mở file nghiệp vụ, xem nó `import` từ đâu.\n\n### Ba cách tiêm phụ thuộc\n\n| | `__init__` | Tham số phương thức | Thuộc tính |\n|---|---|---|---|\n| Dùng cho | **bắt buộc** | chỉ một phương thức cần | tuỳ chọn |\n| Object luôn hợp lệ | **✓** | ✓ | ✓ nếu có mặc định |\n| Đổi lúc chạy | ✗ | ✓ mỗi lời gọi | ✓ |\n| Phổ biến | **nhất** | vừa | ít |\n\nVới phụ thuộc tuỳ chọn, mẫu `x or MacDinhVoHai()` cho cả tiện lợi lẫn khả năng thay thế — miễn là mặc định không kéo theo thư viện ngoài.\n\n### DIP và Dependency Injection\n\n| | DIP | Dependency Injection |\n|---|---|---|\n| Là gì | **nguyên tắc** | **kỹ thuật** |\n| Nói về | chiều phụ thuộc | cách đưa phụ thuộc vào |\n| Cần trừu tượng | **✓** | không bắt buộc |\n| Có thể có cái này mà không có cái kia | ✓ (dùng import động) | **✓ tiêm class cụ thể vẫn là DI** |\n\nTiêm một class cụ thể qua `__init__` là DI nhưng **chưa phải DIP** — vì nghiệp vụ vẫn phụ thuộc vào kiểu cụ thể. DIP đòi hỏi kiểu tham số là **trừu tượng**.\n\n### Đảo hay không đảo\n\n| Phụ thuộc | Đảo? | Vì sao |\n|---|---|---|\n| Database, API, file, email | **✓** | ra ngoài tiến trình, chậm, cần thay khi test |\n| **Thời gian, số ngẫu nhiên** | **✓** | không xác định ⇒ kiểm thử không lặp lại được |\n| Biến môi trường | ✓ | thay đổi theo môi trường chạy |\n| `math`, `json`, `re` | ✗ | hàm thuần, không tác dụng phụ |\n| Class dữ liệu của bạn | ✗ | không có bản cài đặt thứ hai |\n\nHàng thứ hai hay bị bỏ sót nhất, và nó là nguyên nhân của những bài kiểm thử \"lúc chạy được lúc không\".\n\n### Quan hệ với bốn nguyên tắc kia\n\n| Nguyên tắc | Vai trò với DIP |\n|---|---|\n| **SRP** | tách tầng — điều kiện để có gì mà đảo |\n| **ISP** | trừu tượng nhỏ, đúng vai trò — đảo mới có ích |\n| **LSP** | các bản cài đặt thay nhau được — đảo mới an toàn |\n| **OCP** | DIP tạo điều kiện: thêm bản cài đặt không sửa nghiệp vụ |\n\nDIP là nguyên tắc **gộp lại** cả bốn — nó cần ba cái trước và tạo điều kiện cho cái còn lại.\n\n### Lợi ích đo được\n\n| Chỉ số | Trước | Sau |\n|---|---|---|\n| Thời gian chạy một bài test nghiệp vụ | **giây** | **mili giây** |\n| Cần dựng hạ tầng để test | ✓ | ✗ |\n| Số file sửa khi đổi database | nhiều | **1 file mới** |\n| Kiểm được \"không lưu khi dữ liệu sai\" | ✗ | **✓** |\n| Số nơi biết tới `psycopg2` | rải rác | **1 (thành phần gốc)** |\n\nHàng thứ tư đáng chú ý: DIP không chỉ làm test nhanh hơn, nó còn cho phép kiểm **những điều trước đó không kiểm được** — như việc một tương tác *không* xảy ra.",

loi_thuong_gap: [
  "**Đặt `Protocol` ở tầng hạ tầng.** Nghiệp vụ vẫn phải import từ hạ tầng — chưa đảo được gì.",
  "**Đặt tên trừu tượng theo công nghệ** (`KhoRedis`, `BoGuiSMTP`). Tên nên theo nhu cầu của tầng cao.",
  "**Class tự tạo phụ thuộc trong `__init__`.** `self.conn = psycopg2.connect(...)` khiến người dùng không thay được.",
  "**Mặc định là một phụ thuộc nặng.** `kho or KhoPostgres(DSN)` lại kéo `psycopg2` vào tầng nghiệp vụ.",
  "**Không đảo phụ thuộc vào thời gian và số ngẫu nhiên.** Kiểm thử không lặp lại được, và không kiểm được các trường hợp biên.",
  "**Đảo mọi thứ, kể cả `math` và `json`.** Hàm thuần không cần trừu tượng hoá.",
  "**Tiêm class cụ thể rồi tưởng đã đạt DIP.** DI mà không có trừu tượng thì nghiệp vụ vẫn phụ thuộc kiểu cụ thể.",
  "**Không có thành phần gốc rõ ràng.** Việc ghép nối rải rác khắp nơi, và không ai biết bản nào đang chạy.",
  "**Quên vô hiệu cache khi ghi trong lớp bọc cache.** Trả dữ liệu cũ mà không có lỗi nào báo.",
  "**Trừu tượng rút ra từ một bản cài đặt duy nhất.** Nó mang theo chi tiết của bản đó và bản thứ hai không vừa.",
  "**Không kiểm tra chiều phụ thuộc tự động.** Quy ước mà không có gì thực thi thì sẽ bị vi phạm dần.",
  "**Trừu tượng quá lớn.** Class giả khi test phải cài mười phương thức — dấu hiệu ISP bị vi phạm cùng lúc."
],

tu_kiem_tra: [
  "Phát biểu DIP gồm hai vế nào? \"Đảo\" ở đây là đảo cái gì?",
  "Luồng thực thi có bị đảo không? Cái gì thật sự bị đảo?",
  "Vì sao \"trừu tượng thuộc tầng nào\" là điểm quyết định? Phép kiểm nhanh là gì?",
  "Vì sao tên giao diện không nên theo công nghệ?",
  "Nêu bốn vấn đề của việc class tự tạo phụ thuộc trong `__init__`.",
  "Nêu ba cách tiêm phụ thuộc và trường hợp dùng của mỗi cách.",
  "Mặc định thuận tiện phải thoả điều kiện gì để không phá DIP?",
  "DIP có cần class không? Cho ví dụ đảo phụ thuộc bằng hàm.",
  "Vì sao thời gian và số ngẫu nhiên là phụ thuộc cần đảo?",
  "Những phụ thuộc nào **không** nên đảo?",
  "Phân biệt DIP và Dependency Injection. Tiêm class cụ thể có phải DIP không?",
  "\"Thành phần gốc\" là gì? Vì sao nó được phép biết mọi thứ cụ thể?",
  "Làm sao kiểm tra chiều phụ thuộc tự động? Vì sao điều đó quan trọng?",
  "DIP cần ba nguyên tắc nào và tạo điều kiện cho nguyên tắc nào?",
  "Nêu một điều mà DIP cho phép kiểm thử được, mà không có nó thì không kiểm được."
],

tai_nguyen: [
  "Sách **Clean Architecture** — Robert C. Martin. Chương về DIP và về quy tắc phụ thuộc là nguồn đầy đủ nhất; phần về ranh giới kiến trúc rất đáng đọc.",
  "Sách **Architecture Patterns with Python** — Harry Percival & Bob Gregory. Áp dụng DIP vào Python thật, gồm cả repository và thành phần gốc.",
  "Sách **Dependency Injection Principles, Practices, and Patterns** — Mark Seemann & Steven van Deursen. Viết cho .NET nhưng phần nguyên tắc áp dụng nguyên vẹn.",
  "Sách **Practical Object-Oriented Design** — Sandi Metz. Chương về quản lý phụ thuộc giải thích rất rõ vì sao phụ thuộc là thứ đáng kiểm soát nhất.",
  "Tài liệu Python, module `ast` — dùng để viết bài kiểm thử kiểm tra chiều phụ thuộc tự động."
],

lien_he: [
  "**SOLID — Interface Segregation** ngay trước: trừu tượng nhỏ và đúng vai trò thì đảo mới có ích; trừu tượng lớn làm class giả nặng nề.",
  "**SOLID — Liskov**: đảo phụ thuộc chỉ an toàn khi mọi bản cài đặt tôn trọng cùng giao ước; bộ kiểm thử giao ước là cách xác nhận.",
  "**SOLID — Single Responsibility**: quy tắc \"tầng nghiệp vụ không phụ thuộc hạ tầng\" nêu ở đó, bài này là cách thực hiện.",
  "**SOLID — Open/Closed**: DIP tạo điều kiện — thêm bản cài đặt mới không sửa nghiệp vụ.",
  "**Composition over inheritance**: tiêm phụ thuộc qua `__init__` chính là composition.",
  "**Trừu tượng: ABC, Protocol**: `Protocol` là công cụ chính, và lời khuyên \"đợi bản cài đặt thứ hai\" vẫn áp dụng.",
  "**Design pattern — Repository, Adapter** ngay sau: hai mẫu này là DIP được đặt tên và chuẩn hoá.",
  "**Module, package, import** ở module 5: chiều phụ thuộc là chiều của câu lệnh `import`, và kiểm tra được bằng `ast`.",
  "**venv, pip, biến môi trường** ở module 5: thành phần gốc đọc cấu hình từ biến môi trường.",
  "**Testing** ở mảng 05: class giả và việc kiểm tra tương tác là lợi ích trực tiếp, đo được của DIP.",
  "**Backend & Web** ở mảng 05: kiến trúc phân tầng, repository, và container tiêm phụ thuộc của framework đều dựng trên bài này.",
  "**AI/ML** ở mảng 07: tách logic huấn luyện khỏi nguồn dữ liệu và nơi lưu mô hình cho phép đổi hạ tầng mà không sửa mã huấn luyện."
]

};
