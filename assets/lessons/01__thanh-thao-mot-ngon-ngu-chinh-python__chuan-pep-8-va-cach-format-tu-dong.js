window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:chuan-pep-8-va-cach-format-tu-dong"] = {

tom_tat: [
  "**Đừng định dạng code bằng tay.** Cài một công cụ tự động, để nó chạy khi lưu file, và không bao giờ bàn về dấu cách nữa. Đây là giá trị thực dụng lớn nhất của bài.",
  "**PEP 8 là hướng dẫn, không phải luật.** Chính nó có một mục tên là *\"khi nào nên phá vỡ quy tắc\"*, và câu trả lời là: khi tuân thủ làm code khó đọc hơn.",
  "**Quy ước đặt tên là phần quan trọng nhất** — quan trọng hơn dấu cách rất nhiều, và cũng là phần công cụ **không** sửa hộ được.",
  "**Phân biệt ba loại công cụ**: *formatter* sửa hình thức, *linter* tìm lỗi và mùi code, *type checker* kiểm tra kiểu. Chúng bổ sung nhau, không thay nhau.",
  "**Tự động hoá bằng pre-commit hook và CI**, không dựa vào việc mọi người nhớ chạy. Quy ước chỉ có giá trị khi được thực thi nhất quán."
],

can_biet_truoc: [
  "Bài **Type hint và mypy** ngay trước — `mypy` là một phần của cùng bộ công cụ chất lượng code chạy trong CI.",
  "Bài **Module, package, import** — thứ tự nhóm `import` là một quy ước PEP 8 mà công cụ sắp xếp hộ được.",
  "Bài **Cú pháp, kiểu dữ liệu** — quy tắc thụt lề bốn dấu cách và cấm trộn tab đã nhắc ở đó.",
  "Bài **Git & GitHub** ở mảng 03 (học song song được) — pre-commit hook là cơ chế của Git."
],

dinh_nghia: "Nói đời thường: PEP 8 giống như **quy tắc chính tả và trình bày của một toà soạn**. Không có nó thì mỗi người viết một kiểu, và người đọc mất năng lượng vào hình thức thay vì nội dung. Có nó thì mọi bài trông như một người viết — và bây giờ, có máy làm hộ phần trình bày.\n\nNói chính xác:\n\n- **PEP** — *Python Enhancement Proposal*, tài liệu đề xuất cho Python. **PEP 8** là hướng dẫn phong cách viết code.\n- **Formatter** (*trình định dạng*) — công cụ **viết lại** code theo một phong cách cố định: dấu cách, xuống dòng, dấu nháy. Không đổi ý nghĩa chương trình.\n- **Linter** (*trình soi lỗi*) — công cụ **phát hiện** vấn đề: biến không dùng, import thừa, biến chưa gán, mùi code. Có thể sửa được một phần.\n- **Type checker** — kiểm tra kiểu, như `mypy` ở bài trước.\n- **Pre-commit hook** — script Git tự chạy trước mỗi lần commit; commit bị chặn nếu script thất bại.\n- **Docstring** — chuỗi tài liệu ngay đầu module, class, hàm. Đọc được lúc chạy qua `__doc__` và `help()`.\n\nĐiểm cần nắm ngay: **định dạng code là việc đã được giải quyết.** Cuộc tranh luận về dấu cách và độ dài dòng chỉ còn ý nghĩa lịch sử — cách làm đúng bây giờ là chọn một công cụ, cấu hình một lần, và chuyển năng lượng sang những việc công cụ không làm được: đặt tên, chia hàm, thiết kế API.",

vi_sao_can: "Phong cách code nghe như chuyện thẩm mỹ, nhưng hệ quả của nó rất cụ thể:\n\n- **Code được đọc nhiều hơn được viết.** Bạn viết một hàm trong mười phút và đọc lại nó hàng chục lần trong nhiều năm. Tối ưu cho việc đọc là tối ưu đúng chỗ.\n- **Định dạng không nhất quán làm `git diff` vô dụng.** Một người dùng tab, một người dùng space; đổi một dòng logic thì diff hiện 200 dòng. Không ai review nổi.\n- **Tranh luận về dấu cách là lãng phí thuần tuý.** Mỗi lần review mà nhắc \"thiếu dấu cách sau dấu phẩy\" là một lần đáng lẽ nói về logic. Formatter xoá bỏ hoàn toàn loại tranh luận này.\n- **Linter bắt lỗi thật, không chỉ lỗi hình thức.** Biến gán rồi không dùng thường là dấu hiệu gõ nhầm tên; so sánh bằng `is` với chuỗi là lỗi thật; import thừa làm chậm khởi động.\n- **Nhà tuyển dụng nhìn vào đây.** Code không theo quy ước chung là tín hiệu đầu tiên về việc chưa làm việc trong dự án nghiêm túc.\n\nTình huống thật rất hay gặp: nhóm bạn không thống nhất công cụ. Một người lưu file bằng trình soạn thảo tự định dạng theo kiểu A, người khác kiểu B. Mỗi lần hai người sửa cùng file là một lần xung đột Git khổng lồ — mà **toàn bộ xung đột chỉ là dấu cách**. Phần cơ chế sẽ nói cách xoá bỏ vĩnh viễn loại vấn đề này bằng đúng hai file cấu hình.",

co_che: "### Những quy tắc PEP 8 thật sự quan trọng\n\nPhần lớn PEP 8 để công cụ lo. Nhưng **quy ước đặt tên thì công cụ không sửa được**, và nó là phần có giá trị cao nhất:\n\n```python\n# Module, package        chữ thường, gạch dưới, NGẮN\nxu_ly_van_ban.py\n\n# Hàm, biến, tham số     chữ thường, gạch dưới (snake_case)\ndef tinh_diem_trung_binh(danh_sach_diem): ...\nso_luong_nguoi_dung = 10\n\n# Class                  ChữHoaĐầuTừ (PascalCase)\nclass NguoiDung: ...\nclass HTTPClient: ...          # viết tắt thì giữ nguyên chữ hoa\n\n# Hằng số                CHỮ_HOA_GẠCH_DƯỚI\nSO_LAN_THU_LAI = 3\nDUONG_DAN_MAC_DINH = \"/tmp\"\n\n# Nội bộ                 một gạch dưới đầu\n_bo_dem_noi_bo = {}\ndef _ham_phu(): ...\n\n# Tránh trùng từ khoá    một gạch dưới CUỐI\nclass_ = \"lop\"\nid_ = 5\n```\n\nBa quy ước gạch dưới có ý nghĩa khác nhau và hay bị nhầm:\n\n| Dạng | Ý nghĩa | Có ép buộc không |\n|---|---|---|\n| `_ten` | \"nội bộ, đừng dùng từ ngoài\" | **không** — chỉ là giao ước |\n| `ten_` | tránh trùng từ khoá Python | — |\n| `__ten` | trong class: đổi tên để tránh xung đột kế thừa | có, nhưng vẫn lách được |\n| `__ten__` | phương thức đặc biệt của Python | **đừng tự đặt tên kiểu này** |\n\nDòng đầu quan trọng: `_ten` **không phải cơ chế bảo vệ**. Nó nói \"tôi có quyền đổi cái này mà không báo trước\". Hiệu lực thật sự duy nhất: `from x import *` bỏ qua các tên bắt đầu bằng `_`.\n\nDòng cuối là cảnh báo: tự đặt tên `__cua_toi__` là xâm phạm không gian tên mà Python dành riêng cho mình.\n\n### Tên tốt quan trọng hơn mọi quy tắc hình thức\n\n```python\n# ❌ Đúng PEP 8 về hình thức, nhưng vô dụng\ndef proc(d, f=None, x=True):\n    r = []\n    for i in d:\n        if f is None or i[0] > f:\n            r.append(i)\n    return sorted(r) if x else r\n\n# ✓ Cùng logic, đọc là hiểu\ndef loc_va_sap_xep(ban_ghi, diem_toi_thieu=None, sap_xep=True):\n    ket_qua = [bg for bg in ban_ghi\n               if diem_toi_thieu is None or bg[0] > diem_toi_thieu]\n    return sorted(ket_qua) if sap_xep else ket_qua\n```\n\nCả hai đều qua được mọi công cụ định dạng. Khác biệt nằm ở thứ **không công cụ nào kiểm tra được**, và đó là lý do đặt tên là kỹ năng đáng đầu tư nhất trong bài này.\n\nVài nguyên tắc đặt tên thực dụng:\n\n```\nHàm  → ĐỘNG TỪ: tinh_tong, doc_file, kiem_tra_hop_le\nBiến → DANH TỪ: danh_sach_nguoi_dung, ket_qua\nBool → có/là/nên: co_quyen, la_hop_le, nen_thu_lai\nTên dài mà rõ  ≫  tên ngắn mà mơ hồ\nNgoại lệ: i, j, k cho chỉ số; x, y cho toạ độ — đã là quy ước\n```\n\n### Thứ tự import\n\n```python\n# ① Thư viện chuẩn\nimport json\nimport os\nfrom pathlib import Path\n\n# ② Thư viện bên thứ ba\nimport requests\nfrom flask import Flask\n\n# ③ Module của chính dự án\nfrom goi_cua_toi.xu_ly import lam_sach\nfrom goi_cua_toi import cau_hinh\n```\n\nBa nhóm, cách nhau một dòng trống, mỗi nhóm sắp xếp theo bảng chữ cái. Việc này công cụ làm hộ hoàn toàn — không cần nhớ.\n\nLý do quy ước này có ích: nhìn phần import là biết ngay **module này phụ thuộc vào những gì bên ngoài**. Nhóm ② dài bất thường là dấu hiệu module đang làm quá nhiều việc.\n\n### Độ dài dòng — quy tắc gây tranh cãi nhất\n\n```\nPEP 8:                79 ký tự\nNhiều công cụ hiện đại: 88 ký tự (mặc định)\nNhiều nhóm chọn:       100 hoặc 120\n```\n\nCon số cụ thể **không quan trọng bằng việc cả nhóm dùng chung một con số**. Lý do 79 ký tự tồn tại là lịch sử (màn hình cũ), nhưng nó vẫn có ích thật: nó cho phép mở **hai file cạnh nhau** khi review, và ép bạn không lồng quá sâu.\n\n```python\n# Dòng quá dài — dấu hiệu logic cần tách\nket_qua = [xu_ly(x) for x in du_lieu if x.hop_le and x.diem > nguong and x.loai == \"A\"]\n\n# ✓ Tách điều kiện ra, đặt tên cho nó\ndef du_dieu_kien(x):\n    return x.hop_le and x.diem > nguong and x.loai == \"A\"\n\nket_qua = [xu_ly(x) for x in du_lieu if du_dieu_kien(x)]\n```\n\nĐây là ý đáng nhớ: **giới hạn độ dài dòng có giá trị vì nó là tín hiệu**, không phải vì bản thân con số. Dòng quá dài thường là dòng làm quá nhiều việc.\n\n### Ba loại công cụ — phân biệt cho rõ\n\n```\nFORMATTER     sửa HÌNH THỨC     không đổi ý nghĩa    chạy tự động, không bàn cãi\nLINTER        tìm VẤN ĐỀ        cảnh báo, có thể sửa  cần cấu hình cái nào bật\nTYPE CHECKER  kiểm tra KIỂU     báo lỗi               cần chú thích kiểu\n```\n\nChúng **bổ sung nhau**:\n\n```python\nimport os                   # linter: import thừa\n\ndef f( x,y ):               # formatter: khoảng trắng sai\n    ket_qua = x+y           # formatter: thiếu dấu cách quanh +\n    tam = 10                # linter: biến gán rồi không dùng\n    return ket_qua\n\nf(\"a\", 5)                   # type checker: sai kiểu (nếu có chú thích)\n```\n\nBa công cụ bắt ba lỗi khác nhau trong cùng năm dòng, và không cái nào thay được cái nào.\n\n### Formatter — cách hoạt động\n\nFormatter hiện đại làm việc theo nguyên tắc: **phân tích code thành cây cú pháp, rồi in lại theo quy tắc cố định.** Nó không sửa vặt mà viết lại hoàn toàn phần trình bày.\n\n```python\n# Bạn viết\nd = {'ten':'Minh','tuoi':22,'diem':[8,9,10]}\ndef f(a,b=1,*args,**kw): return a+b\n\n# Formatter viết lại\nd = {\"ten\": \"Minh\", \"tuoi\": 22, \"diem\": [8, 9, 10]}\n\n\ndef f(a, b=1, *args, **kw):\n    return a + b\n```\n\nHai đặc điểm quan trọng của cách tiếp cận này:\n\n```\n① KHÔNG ĐỔI ý nghĩa chương trình — chỉ đổi cách trình bày\n② KẾT QUẢ TẤT ĐỊNH: cùng code vào ⇒ cùng code ra, bất kể ai chạy\n```\n\nĐiểm ② là thứ xoá bỏ tranh luận: không còn \"tôi thích xuống dòng ở đây\" vì công cụ quyết định, và mọi người đều nhận kết quả giống nhau.\n\nMột đánh đổi có thật cần nói: formatter đôi khi trình bày kém hơn con người ở những chỗ có cấu trúc đặc biệt — ví dụ một ma trận số mà bạn muốn căn thành hàng cột. Các công cụ đều có cách bỏ qua một đoạn:\n\n```python\n# fmt: off\nMA_TRAN = [\n    1, 0, 0,\n    0, 1, 0,\n    0, 0, 1,\n]\n# fmt: on\n```\n\nDùng nó tiết kiệm — mỗi lần dùng là một chỗ code không còn nhất quán.\n\n### Linter bắt được gì\n\n```python\n# Lỗi thật, không phải hình thức\nif x == None: ...           # nên là: x is None\nif type(x) == int: ...       # nên là: isinstance(x, int)\nexcept:                      # bắt trần — bài ngoại lệ đã nói\n    pass\ndef f(ds=[]): ...            # giá trị mặc định mutable — bài hàm đã nói\n\nfor i in range(len(ds)):     # nên là: for x in ds\n    print(ds[i])\n\nket_qua = [x for x in ds]    # nên là: list(ds)\nif len(ds) == 0: ...         # nên là: if not ds\n```\n\nBa dòng đầu đáng chú ý vì chúng là **lỗi thật đã học ở các bài trước**, và linter bắt được tự động. Đây là lý do linter đáng giá hơn nhiều so với ấn tượng \"công cụ bắt bẻ vặt\".\n\nLinter cũng đo được độ phức tạp:\n\n```\nĐộ phức tạp chu trình (cyclomatic complexity) = số nhánh rẽ trong hàm\n\n1–5    đơn giản, dễ kiểm thử\n6–10   chấp nhận được\n>10    NÊN TÁCH — số ca kiểm thử cần thiết tăng theo cấp số nhân\n```\n\nCon số này là tín hiệu khách quan cho câu \"hàm này dài quá\", và nó dựa trên một lý do thật: mỗi nhánh rẽ nhân đôi số đường đi cần kiểm thử.\n\n### Docstring\n\n```python\ndef tinh_diem(bai: list[float], trong_so: list[float] | None = None) -> float:\n    \"\"\"Tính điểm trung bình có trọng số.\n\n    Args:\n        bai: điểm từng bài, trong khoảng 0–10.\n        trong_so: trọng số tương ứng. None nghĩa là bằng nhau.\n\n    Returns:\n        Điểm trung bình đã tính trọng số.\n\n    Raises:\n        ValueError: nếu `bai` rỗng hoặc độ dài hai danh sách khác nhau.\n    \"\"\"\n```\n\nDocstring khác chú thích `#` ở chỗ nó **truy cập được lúc chạy**:\n\n```python\ntinh_diem.__doc__\nhelp(tinh_diem)\n```\n\nQuy tắc thực dụng về việc viết gì:\n\n```\nDocstring  →  hàm này LÀM GÌ và dùng THẾ NÀO   (cho người GỌI hàm)\nChú thích # →  vì sao code làm THẾ NÀY          (cho người SỬA hàm)\n```\n\n```python\n# ❌ Chú thích thừa — lặp lại điều code đã nói\ni = i + 1     # tăng i lên 1\n\n# ✓ Chú thích giải thích LÝ DO\n# Dùng thuật toán chậm hơn vì bản nhanh cho kết quả sai với dữ liệu\n# có giá trị âm — xem báo cáo lỗi nội bộ về đơn hàng hoàn tiền.\nket_qua = thuat_toan_cham(du_lieu)\n```\n\nHàm có type hint đầy đủ thì docstring **không cần lặp lại kiểu** — chỉ cần nói ý nghĩa. Đây là chỗ bài trước và bài này ghép vào nhau.\n\n### Khi nào PEP 8 nên bị phá vỡ\n\nBản thân PEP 8 có mục về việc này, và lý do rất hợp lý:\n\n```\n✓ Khi tuân thủ làm code KHÓ ĐỌC hơn\n✓ Khi phải nhất quán với code xung quanh vốn theo phong cách khác\n✓ Khi code có từ trước quy tắc, và sửa lại tạo diff khổng lồ không cần thiết\n✓ Khi phải tương thích với API bên ngoài dùng tên kiểu khác\n```\n\n```python\n# Bọc thư viện dùng camelCase — giữ nguyên tên để người dùng khỏi nhầm\nclass ApiKhachHang:\n    def getUserById(self, userId):     # trùng tên với API gốc, có chủ đích\n        ...\n```\n\nCâu chốt của PEP 8 đáng nhớ: **tính dễ đọc là mục đích, quy tắc chỉ là phương tiện.** Máy móc tuân thủ tới mức làm code khó đọc hơn là hiểu sai tinh thần của nó.\n\n### Tự động hoá — phần thực dụng nhất\n\n**Ở trình soạn thảo — định dạng khi lưu:**\n\nĐây là thiết lập có giá trị nhất: bạn gõ thế nào cũng được, lưu file là code đúng chuẩn. Sau vài ngày bạn quên hẳn việc định dạng tồn tại.\n\n**Trước mỗi commit — pre-commit hook:**\n\n```yaml\n# .pre-commit-config.yaml\nrepos:\n  - repo: <kho của công cụ định dạng>\n    hooks:\n      - id: <formatter>\n      - id: <linter>\n  - repo: <kho mypy>\n    hooks:\n      - id: mypy\n```\n\n```bash\npython -m pip install pre-commit\npre-commit install          # cài hook vào .git/hooks\ngit commit -m \"...\"         # hook tự chạy; hỏng thì commit bị chặn\npre-commit run --all-files  # chạy tay cho toàn bộ dự án\n```\n\n**Trong CI — chốt chặn cuối:**\n\nCI kiểm tra lại vì hook cục bộ có thể bị bỏ qua bằng `--no-verify`. Ba lớp này bổ sung nhau:\n\n```\nTrình soạn thảo  → sửa NGAY, không phiền ai\nPre-commit hook  → chặn TRƯỚC khi vào lịch sử Git\nCI               → chốt chặn cuối, không ai lách được\n```\n\n**Cấu hình tập trung trong `pyproject.toml`:**\n\n```toml\n[tool.<formatter>]\nline-length = 88\ntarget-version = [\"py312\"]\n\n[tool.<linter>]\nline-length = 88\nselect = [\"E\", \"F\", \"I\", \"UP\", \"B\"]     # nhóm quy tắc muốn bật\nignore = [\"E501\"]                        # formatter đã lo độ dài dòng\n\n[tool.mypy]\npython_version = \"3.12\"\ndisallow_untyped_defs = false\n```\n\nMột file cấu hình trong kho code nghĩa là **mọi người và cả CI dùng chung một quy tắc** — không còn phụ thuộc cài đặt cá nhân trên máy từng người.\n\n### Áp dụng vào dự án đang có\n\nĐịnh dạng lại toàn bộ dự án tạo một commit khổng lồ làm hỏng `git blame`. Cách xử lý đúng:\n\n```bash\n# ① Commit RIÊNG chỉ chứa việc định dạng, không lẫn thay đổi logic\n<formatter> .\ngit commit -am \"style: định dạng toàn bộ theo công cụ\"\n\n# ② Ghi commit đó vào danh sách bỏ qua khi truy vết\ngit log -1 --format=%H >> .git-blame-ignore-revs\ngit config blame.ignoreRevsFile .git-blame-ignore-revs\ngit commit -am \"chore: bỏ qua commit định dạng khi git blame\"\n```\n\nSau bước ②, `git blame` **nhảy qua** commit định dạng và vẫn chỉ đúng người sửa logic. Đây là chi tiết nhỏ nhưng cứu được lịch sử của cả dự án — nối trực tiếp với mảng Git ở lộ trình.",

vi_du: "### Trước và sau khi chạy formatter\n\n```python\n# TRƯỚC\nimport sys,os\nfrom pathlib import Path\nimport json\n\ndef xu_ly( du_lieu,nguong = None,sap_xep=True ):\n    ket_qua=[]\n    for x in du_lieu :\n        if nguong==None or x['diem']>nguong :\n            ket_qua.append( {'ten':x['ten'],'diem':x['diem']} )\n    if sap_xep :\n        return sorted(ket_qua,key=lambda d:d['diem'],reverse=True)\n    return ket_qua\n```\n\n```python\n# SAU (formatter + linter sắp xếp import)\nimport json\nimport os\nimport sys\nfrom pathlib import Path\n\n\ndef xu_ly(du_lieu, nguong=None, sap_xep=True):\n    ket_qua = []\n    for x in du_lieu:\n        if nguong is None or x[\"diem\"] > nguong:      # ⭐ linter sửa == None\n            ket_qua.append({\"ten\": x[\"ten\"], \"diem\": x[\"diem\"]})\n    if sap_xep:\n        return sorted(ket_qua, key=lambda d: d[\"diem\"], reverse=True)\n    return ket_qua\n```\n\nLinter còn báo hai thứ formatter không đụng tới: `sys` và `os` import mà không dùng. Đó là ranh giới giữa hai loại công cụ — một cái sửa hình thức, một cái tìm vấn đề.\n\n### Cấu hình một lần cho cả dự án\n\n```toml\n# pyproject.toml — một file, cả nhóm và CI dùng chung\n[tool.<formatter>]\nline-length = 88\ntarget-version = [\"py312\"]\n\n[tool.<linter>]\nline-length = 88\nselect = [\n    \"E\",    # lỗi phong cách\n    \"F\",    # lỗi logic: biến chưa gán, import thừa\n    \"I\",    # sắp xếp import\n    \"UP\",   # cú pháp cũ → cú pháp mới\n    \"B\",    # bẫy thường gặp: giá trị mặc định mutable...\n    \"SIM\",  # đơn giản hoá code\n]\nignore = [\"E501\"]        # formatter đã lo độ dài dòng\n\n[tool.mypy]\npython_version = \"3.12\"\ndisallow_untyped_defs = false\nwarn_unused_ignores = true\n```\n\nNhóm `B` đáng bật nhất: nó bắt đúng những bẫy đã học trong module này — giá trị mặc định mutable, `except` trần, so sánh bằng `is` với chuỗi. Có nó thì linter thành một người review nhớ hết các bài đã học.\n\n### Pre-commit — chặn trước khi vào lịch sử\n\n```bash\npython -m pip install pre-commit\n```\n\n```yaml\n# .pre-commit-config.yaml\nrepos:\n  - repo: https://github.com/pre-commit/pre-commit-hooks\n    rev: v4.6.0\n    hooks:\n      - id: trailing-whitespace\n      - id: end-of-file-fixer\n      - id: check-yaml\n      - id: check-added-large-files      # ⭐ chặn commit nhầm file dữ liệu lớn\n      - id: check-merge-conflict\n```\n\n```bash\npre-commit install\ngit add . && git commit -m \"thêm tính năng\"\n# trailing-whitespace.....Failed\n# - files were modified by this hook\n# ⇒ hook đã SỬA file; git add lại rồi commit lần nữa\n\npre-commit run --all-files      # chạy toàn bộ dự án lần đầu\npre-commit autoupdate           # cập nhật phiên bản các hook\n```\n\nHook `check-added-large-files` đáng nhắc riêng: nó chặn việc commit nhầm file dữ liệu hoặc checkpoint mô hình — thứ rất hay xảy ra với dự án AI/ML và rất khó gỡ khỏi lịch sử Git sau đó.\n\n### Bảng đối chiếu tên\n\n```python\n# ❌                          # ✓\nclass user_data:              class UserData:\nclass HTTPclient:             class HTTPClient:\ndef CalculateTotal():         def calculate_total():\ndef getUserName():            def get_user_name():\nMAX_size = 10                 MAX_SIZE = 10\nmylist = []                   danh_sach_nguoi_dung = []\nl = [1, 2]                    diem = [1, 2]          # tránh l, I, O — dễ nhầm 1 và 0\ndef process(d):               def loc_ban_ghi(ban_ghi):\n```\n\nDòng `l = [1, 2]` có lý do rất cụ thể: chữ `l` thường, chữ `I` hoa và số `1` trông gần giống nhau trong nhiều phông chữ; `O` hoa và số `0` cũng vậy. PEP 8 khuyến nghị tránh hẳn ba ký tự này làm tên biến.\n\n### Docstring và chú thích — đúng vai trò\n\n```python\ndef gui_lai(url: str, du_lieu: dict, so_lan: int = 3) -> dict:\n    \"\"\"Gửi POST tới `url`, tự thử lại khi lỗi mạng tạm thời.\n\n    Chỉ thử lại với lỗi kết nối và timeout. Lỗi 4xx không thử lại\n    vì gửi lại y nguyên thì vẫn lỗi y nguyên.\n\n    Args:\n        url: địa chỉ đích.\n        du_lieu: nội dung JSON gửi đi.\n        so_lan: số lần thử tối đa.\n\n    Returns:\n        Phản hồi đã phân tích thành dict.\n\n    Raises:\n        ConnectionError: nếu thất bại sau `so_lan` lần.\n    \"\"\"\n    for lan in range(so_lan):\n        try:\n            return _gui(url, du_lieu)\n        except (ConnectionError, TimeoutError):\n            if lan == so_lan - 1:\n                raise\n            # Chờ tăng gấp đôi để không dội vào server đang quá tải.\n            # Cộng thêm ngẫu nhiên để nhiều client không thử lại cùng lúc.\n            time.sleep(2 ** lan + random.random())\n```\n\nSo sánh hai loại nội dung:\n\n```\nDocstring  →  \"chỉ thử lại lỗi tạm thời\"  — người GỌI cần biết để dùng đúng\nChú thích  →  \"cộng ngẫu nhiên để không dội cùng lúc\" — người SỬA cần biết lý do\n```\n\nChú thích thứ hai là loại đáng viết nhất: nó ghi lại **một quyết định** mà nhìn code không suy ra được, và nó ngăn người sau \"dọn dẹp\" mất dòng `random()` tưởng như thừa.\n\n### Linter bắt lỗi thật\n\n```python\n# Đoạn code chứa năm vấn đề\nimport os\nimport json\n\ndef xu_ly(ds=[]):                    # B006: mặc định mutable\n    tong = 0\n    tam = []                          # F841: gán rồi không dùng\n    for i in range(len(ds)):          # đề nghị: for x in ds\n        if ds[i] != None:             # E711: nên dùng is not None\n            tong += ds[i]\n    try:\n        return tong / len(ds)\n    except:                           # E722: except trần\n        return 0\n```\n\n```bash\n<linter> xu_ly.py\n# xu_ly.py:1:1: F401 `os` imported but unused\n# xu_ly.py:4:15: B006 Do not use mutable data structures for argument defaults\n# xu_ly.py:6:5: F841 Local variable `tam` is assigned to but never used\n# xu_ly.py:7:5: <đề nghị> Use `for x in ds` instead of `range(len(ds))`\n# xu_ly.py:8:19: E711 Comparison to `None` should be `cond is not None`\n# xu_ly.py:12:5: E722 Do not use bare `except`\n```\n\nTrong sáu cảnh báo này, **bốn cái là lỗi thật đã học ở các bài trước** của module — không phải chuyện hình thức. `B006` là bẫy giá trị mặc định, `E722` là `except` trần, `F841` thường là dấu hiệu gõ nhầm tên biến.\n\n### Đo độ phức tạp để biết khi nào tách hàm\n\n```bash\n<linter> --select=C901 --config 'mccabe.max-complexity=10' .\n# app.py:45:1: C901 `xu_ly_don_hang` is too complex (17 > 10)\n```\n\nCon số 17 nghĩa là hàm có 17 đường đi khác nhau — muốn kiểm thử đủ thì cần 17 ca kiểm thử. Đây là tín hiệu khách quan để tách, thay vì cảm tính \"hàm này dài quá\".\n\n### Định dạng lại dự án cũ mà không phá `git blame`\n\n```bash\n# ① Commit riêng, KHÔNG lẫn thay đổi logic\n<formatter> .\n<linter> --fix .\ngit add -A\ngit commit -m \"style: định dạng toàn bộ dự án\"\n\n# ② Ghi vào danh sách bỏ qua\ngit rev-parse HEAD >> .git-blame-ignore-revs\ngit config blame.ignoreRevsFile .git-blame-ignore-revs\ngit add .git-blame-ignore-revs\ngit commit -m \"chore: bỏ qua commit định dạng khi truy vết\"\n\n# ③ Kiểm chứng — blame vẫn chỉ đúng người sửa logic\ngit blame app.py | head\n```\n\nBước ② là chi tiết nhiều người không biết, và thiếu nó thì `git blame` trên mọi dòng của dự án đều chỉ về commit định dạng — mất toàn bộ giá trị của lịch sử.\n\n### Cấu hình trình soạn thảo\n\n```json\n// .vscode/settings.json — commit vào kho để cả nhóm dùng chung\n{\n  \"[python]\": {\n    \"editor.formatOnSave\": true,\n    \"editor.codeActionsOnSave\": {\n      \"source.fixAll\": \"explicit\",\n      \"source.organizeImports\": \"explicit\"\n    }\n  },\n  \"editor.rulers\": [88],\n  \"files.trimTrailingWhitespace\": true,\n  \"files.insertFinalNewline\": true\n}\n```\n\nCommit file này vào kho là cách làm đúng: người mới clone về là có ngay thiết lập chuẩn, không phải đọc tài liệu và cấu hình tay.\n\n`editor.rulers` vẽ một đường dọc ở cột 88 — nhỏ nhưng hiệu quả, vì bạn thấy dòng dài **trong lúc gõ** chứ không phải sau khi lưu.\n\n### Chọn công cụ — tiêu chí, không phải tên\n\n```\nFORMATTER: chọn cái KHÔNG có nhiều tuỳ chọn\n  → ít tuỳ chọn = ít tranh luận = đúng mục đích của formatter\n\nLINTER: chọn cái NHANH và gộp được nhiều bộ quy tắc\n  → chạy chậm thì không ai chạy, và không ai chạy thì vô dụng\n\nTYPE CHECKER: chọn cái tích hợp tốt với trình soạn thảo của nhóm\n\nQUAN TRỌNG NHẤT: cả nhóm dùng CHUNG một bộ, cấu hình trong kho code\n```\n\nDòng cuối quan trọng hơn mọi lựa chọn cụ thể. Hai người dùng hai formatter khác nhau còn tệ hơn cả việc không ai dùng gì — vì mỗi lần lưu file là một lần định dạng lại toàn bộ theo kiểu của mình, và `git diff` thành vô dụng.",

so_sanh: "### Ba loại công cụ\n\n| | Formatter | Linter | Type checker |\n|---|---|---|---|\n| Việc chính | sửa **hình thức** | tìm **vấn đề** | kiểm tra **kiểu** |\n| Đổi code | **✓ tự động** | một phần | ✗ |\n| Cần cấu hình | rất ít | **nhiều** | vừa |\n| Cần chú thích kiểu | ✗ | ✗ | **✓** |\n| Bắt lỗi logic | ✗ | **✓ một phần** | ✓ |\n| Nên tranh luận về kết quả | **không** | có (chọn quy tắc) | có |\n\nHàng cuối là điểm khác biệt về văn hoá dùng: **formatter là để hết bàn cãi**, còn linter thì cả nhóm nên bàn xem bật nhóm quy tắc nào.\n\n### Docstring và chú thích `#`\n\n| | Docstring | Chú thích `#` |\n|---|---|---|\n| Vị trí | ngay sau `def`/`class` | bất kỳ đâu |\n| Truy cập lúc chạy | **✓ `__doc__`, `help()`** | ✗ |\n| Nội dung | **làm gì, dùng thế nào** | **vì sao làm thế này** |\n| Đối tượng đọc | người **gọi** hàm | người **sửa** hàm |\n| Công cụ sinh tài liệu | ✓ | ✗ |\n\nChú thích lặp lại điều code đã nói (`i = i + 1  # tăng i`) là chú thích tệ. Chú thích tốt ghi lại **một quyết định** hoặc **một cạm bẫy** mà đọc code không suy ra được.\n\n### Ba lớp thực thi quy ước\n\n| | Trình soạn thảo | Pre-commit hook | CI |\n|---|---|---|---|\n| Khi nào chạy | **khi lưu** | trước commit | sau khi đẩy lên |\n| Phản hồi | **tức thì** | vài giây | vài phút |\n| Bỏ qua được | ✓ tắt cấu hình | ✓ `--no-verify` | **✗** |\n| Vai trò | tiện lợi | ngăn lỗi vào lịch sử | **chốt chặn cuối** |\n\nBa lớp bổ sung nhau. Chỉ có CI thì phản hồi quá chậm; chỉ có trình soạn thảo thì người không cấu hình sẽ lọt qua.\n\n### Quy ước đặt tên\n\n| Đối tượng | Quy ước | Ví dụ |\n|---|---|---|\n| Module, package | `chu_thuong` | `xu_ly_van_ban.py` |\n| Hàm, biến | `snake_case` | `tinh_diem_trung_binh` |\n| Class | `PascalCase` | `NguoiDung`, `HTTPClient` |\n| Hằng số | `CHU_HOA` | `SO_LAN_THU_LAI` |\n| Nội bộ | `_mot_gach_duoi` | `_bo_dem` |\n| Tránh từ khoá | `gach_duoi_cuoi_` | `class_`, `id_` |\n| Tránh hẳn | `l`, `I`, `O` | dễ nhầm với `1` và `0` |\n\nĐây là bảng đáng thuộc, vì **công cụ không sửa hộ được** phần này.\n\n### PEP 8: cái nào công cụ lo, cái nào bạn lo\n\n| Quy tắc | Ai lo |\n|---|---|\n| Dấu cách, xuống dòng, thụt lề | **formatter** |\n| Độ dài dòng | **formatter** |\n| Thứ tự và nhóm `import` | **linter/formatter** |\n| Dấu nháy đơn hay kép | **formatter** |\n| Quy ước đặt tên | linter nhắc, **bạn quyết** |\n| **Tên có ý nghĩa không** | **chỉ bạn** |\n| **Chia hàm hợp lý không** | linter đo, **bạn quyết** |\n| **Docstring viết đúng nội dung không** | **chỉ bạn** |\n\nBa hàng in đậm cuối là nơi nên dồn năng lượng: đó là phần quyết định code có đọc được hay không, và không công cụ nào làm hộ.\n\n### Khi nào phá vỡ PEP 8\n\n| Tình huống | Phá vỡ? |\n|---|---|\n| Tuân thủ làm code **khó đọc hơn** | **✓** |\n| Nhất quán với code xung quanh có sẵn | **✓** |\n| Tương thích với API bên ngoài | ✓ |\n| Sửa lại tạo diff khổng lồ không cần thiết | ✓ |\n| Chỉ vì \"tôi thích thế\" | **✗** |\n| Chỉ vì lười cấu hình công cụ | ✗ |\n\nBản thân PEP 8 nói rõ tinh thần: **dễ đọc là mục đích, quy tắc là phương tiện.** Nhưng hai hàng cuối cũng quan trọng — \"tôi thích thế\" không phải lý do khi làm việc nhóm.",

loi_thuong_gap: [
  "**Định dạng code bằng tay.** Lãng phí thời gian và không bao giờ nhất quán. Cài công cụ, bật định dạng khi lưu.",
  "**Mỗi người trong nhóm dùng một formatter khác nhau.** Tệ hơn cả việc không ai dùng gì — mỗi lần lưu là định dạng lại cả file.",
  "**Không commit file cấu hình vào kho.** Mỗi máy một kiểu, CI lại một kiểu khác.",
  "**Định dạng lại cả dự án trong một commit lẫn thay đổi logic.** Không ai review nổi. Tách commit riêng.",
  "**Quên `.git-blame-ignore-revs` sau khi định dạng toàn bộ.** `git blame` chỉ về commit định dạng, mất giá trị lịch sử.",
  "**Chỉ dùng formatter mà không dùng linter.** Bỏ lỡ những lỗi thật: import thừa, biến không dùng, `except` trần, mặc định mutable.",
  "**Tranh luận về dấu cách trong review.** Đó là việc của máy. Review nên nói về logic và thiết kế.",
  "**Đặt tên viết tắt khó hiểu** (`d`, `proc`, `tmp2`). Công cụ không sửa được, và đây mới là thứ quyết định code có đọc nổi không.",
  "**Dùng `l`, `I`, `O` làm tên biến.** Trông gần giống `1` và `0` trong nhiều phông chữ.",
  "**Viết chú thích lặp lại điều code đã nói.** Chú thích nên ghi **lý do**, không ghi **cái gì**.",
  "**Docstring lặp lại thông tin đã có trong type hint.** Nói ý nghĩa, đừng nói lại kiểu.",
  "**Dùng `# fmt: off` bừa bãi.** Mỗi lần dùng là một chỗ không còn nhất quán. Chỉ dùng khi thật sự cần.",
  "**Tuân thủ PEP 8 máy móc tới mức code khó đọc hơn.** Chính PEP 8 nói dễ đọc mới là mục đích.",
  "**Bật mọi quy tắc linter cùng lúc trên dự án có sẵn.** Hàng nghìn cảnh báo và không ai muốn đụng vào. Bật dần từng nhóm.",
  "**Chỉ dựa vào CI mà không có hook cục bộ.** Phản hồi sau vài phút thay vì vài giây, và mỗi lần sửa là một commit thêm."
],

tu_kiem_tra: [
  "Phân biệt formatter, linter, type checker: mỗi cái làm gì và chúng bổ sung nhau ra sao?",
  "Vì sao nói \"định dạng code là việc đã được giải quyết\"? Năng lượng nên chuyển sang đâu?",
  "Nêu quy ước đặt tên cho module, hàm, class, hằng số. Cái nào công cụ không sửa hộ được?",
  "`_ten`, `ten_`, `__ten`, `__ten__` khác nhau thế nào? Cái nào là ép buộc thật?",
  "Vì sao PEP 8 khuyên tránh dùng `l`, `I`, `O` làm tên biến?",
  "Giới hạn độ dài dòng có giá trị vì con số cụ thể hay vì lý do nào khác?",
  "Ba nhóm `import` theo PEP 8 là gì? Quy ước đó cho biết điều gì về module?",
  "Nêu bốn lỗi thật (không phải hình thức) mà linter bắt được, và chúng liên hệ với bài nào trong module.",
  "Độ phức tạp chu trình đo gì? Vì sao trên 10 thì nên tách hàm?",
  "Docstring khác chú thích `#` ở nội dung và ở đối tượng đọc thế nào?",
  "Nêu ba lớp thực thi quy ước và vai trò riêng của từng lớp.",
  "Vì sao cần `.git-blame-ignore-revs` sau khi định dạng lại toàn bộ dự án?",
  "PEP 8 nói khi nào nên phá vỡ quy tắc? Nêu hai trường hợp chính đáng và một trường hợp không.",
  "Vì sao \"cả nhóm dùng chung một bộ công cụ\" quan trọng hơn việc chọn công cụ nào?"
],

tai_nguyen: [
  "**PEP 8 — Style Guide for Python Code**, tài liệu gốc trên trang PEP chính thức. Ngắn, đọc được trong một buổi, và mục *A Foolish Consistency* ở đầu rất đáng đọc.",
  "**PEP 257 — Docstring Conventions** — quy ước viết docstring, ngắn hơn PEP 8 nhiều.",
  "**PEP 20 — The Zen of Python**, gõ `import this` trong REPL. Mười chín câu về triết lý thiết kế của ngôn ngữ.",
  "Sách **Clean Code** — Robert C. Martin. Viết cho Java nhưng các chương về đặt tên và về hàm áp dụng nguyên vẹn cho Python.",
  "Tài liệu của công cụ nhóm bạn chọn — phần cấu hình và phần danh sách quy tắc, để biết bật gì và vì sao."
],

lien_he: [
  "**Type hint và mypy** ngay trước: `mypy` là thành viên thứ ba của bộ công cụ chất lượng chạy trong CI.",
  "**Module, package, import**: thứ tự ba nhóm import là quy ước công cụ sắp xếp hộ.",
  "**Hàm, tham số mặc định**: linter bắt được bẫy giá trị mặc định mutable — lỗi đã học ở bài đó.",
  "**Xử lý ngoại lệ**: linter bắt `except` trần và so sánh `== None` — cũng là lỗi đã học.",
  "**Cú pháp, kiểu dữ liệu**: quy tắc bốn dấu cách và cấm trộn tab bắt nguồn từ đây.",
  "**venv, pip, pyproject** ngay sau: `pyproject.toml` là chỗ chứa cấu hình cho toàn bộ công cụ trong bài này.",
  "**Git & GitHub** ở mảng 03: pre-commit hook là cơ chế của Git, và `.git-blame-ignore-revs` là tính năng của `git blame`.",
  "**Clean Code & thiết kế** ở module 8 mảng 01: bài này lo phần hình thức, module đó lo phần cấu trúc và trách nhiệm.",
  "**Backend & Web** ở mảng 05: CI chạy formatter, linter, type checker và kiểm thử là quy trình chuẩn của mọi dự án nghiêm túc."
]

};
