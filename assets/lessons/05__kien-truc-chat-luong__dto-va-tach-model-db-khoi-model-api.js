window.LESSONS["05:kien-truc-chat-luong:dto-va-tach-model-db-khoi-model-api"] = {

tom_tat: [
  "DTO là model của thông điệp đi qua một ranh giới; model API, entity miền và model persistence phục vụ ba hợp đồng khác nhau dù có vài field giống nhau.",
  "Tách model ngăn mass assignment, lộ dữ liệu, lazy loading và migration database vô tình trở thành breaking change của API.",
  "Mapping tường minh là danh sách cho phép có thể review, không phải phần code thừa cần xóa bằng serialize tự động.",
  "Request tạo mới, request cập nhật và response nên là các kiểu riêng vì ý nghĩa required, nullable và omitted khác nhau.",
  "Tách DTO có giá về boilerplate và CPU; projection, mapper nhỏ và contract test giúp trả chi phí đúng chỗ."
],

can_biet_truoc: [
  "JSON, HTTP request/response và status code.",
  "Pydantic BaseModel, validation và serialization trong FastAPI.",
  "ORM entity, primary key, foreign key và migration schema.",
  "Khái niệm backward compatibility và API versioning."
],

dinh_nghia: "**DTO (Data Transfer Object, đối tượng truyền dữ liệu)** là cấu trúc mô tả dữ liệu được phép đi qua một ranh giới cụ thể. Request DTO định nghĩa client được gửi gì; response DTO định nghĩa server cam kết trả gì. **Model database** mô tả cách lưu, quan hệ, khóa và metadata nội bộ. **Entity/value object miền** biểu diễn hành vi và invariant. Tách model nghĩa là map có chủ đích giữa các hình dạng này, không dùng một class làm cả payload HTTP, trạng thái nghiệp vụ và hàng trong database.",

vi_sao_can: "Booking row có `id`, `email`, `workshop_id`, `status`, `cost_cents`, `payment_reference`, `deleted_at`, `version` và timestamps. Client tạo booking chỉ được gửi email; client xem booking cần mã, trạng thái và thời điểm, tuyệt đối không cần giá vốn hay tham chiếu thanh toán. Nếu FastAPI nhận và trả thẳng ORM model, thêm một cột phục vụ đối soát có thể lập tức xuất hiện trong JSON. Ngược lại, đổi tên cột vì tối ưu database sẽ phá mobile app dù nghiệp vụ không đổi.\n\nRủi ro nghiêm trọng hơn là mass assignment: attacker gửi thêm `status=\"confirmed\"`, `is_admin=true` hoặc `cost_cents=0`, rồi code `Model(**payload)` ghi luôn. DTO input đóng vai trò allowlist. Tách model cũng giữ session và lazy-loading không rò ra serialization. Đánh đổi là mapping phải viết và cập nhật; với script nội bộ dùng một lần, ba model riêng có thể quá sức cần thiết.",

co_che: "### Ba hợp đồng, ba lý do thay đổi\n\nAPI contract thay khi nhu cầu client đổi. Domain thay khi quy tắc đặt vé đổi. Persistence model thay khi index, normalization hoặc chiến lược lưu đổi. Nếu dùng chung một class, ba nhịp thay đổi khóa vào nhau. “Các field hiện giống nhau” chỉ là ảnh chụp hôm nay, không chứng minh trách nhiệm giống nhau.\n\nRequest model còn là tài liệu executable: OpenAPI, giới hạn chuỗi, enum và kiểu. ORM model diễn tả column nullable, relationship và optimistic version. Entity có thể từ chối chuyển trạng thái sai. Mỗi model nên mạnh ở nhiệm vụ của nó thay vì trở thành mẫu số chung nghèo ý nghĩa.\n\n### Create không phải update\n\n`CreateBookingRequest` yêu cầu email và có thể không cho client chọn status. `UpdateBookingRequest` dùng field optional để biểu diễn “không đổi”. Nhưng optional tạo hai trạng thái khác nhau: omitted và gửi `null`. Nếu email không nullable, `null` phải bị từ chối, còn omitted giữ giá trị cũ.\n\nPydantic có thể xuất `model_dump(exclude_unset=True)` để giữ phân biệt ấy. Đừng dùng một `BookingDTO` với mọi field optional cho cả create/update/response; client có thể gửi object rỗng, code phải đoán ngữ nghĩa và tài liệu API nói dối.\n\n### Response là danh sách cho phép\n\n`response_model` khiến FastAPI validate và lọc kết quả theo schema công khai. Đây là hàng rào hữu ích khi handler lỡ trả object có field dư. Tuy nhiên không nên dựa duy nhất vào lọc cuối: mapper rõ ràng giúp reviewer thấy `payment_reference` không bao giờ được chọn.\n\nDanh sách cấm dễ thất bại vì cột nhạy cảm mới mặc định được lộ. Danh sách cho phép an toàn hơn: field mới không xuất hiện cho tới khi có quyết định contract. Cùng lý do, log payload cũng cần schema/redaction riêng chứ không dump `model_dump()` bừa bãi.\n\n### Mapping là nơi dịch nghĩa\n\nMapping không chỉ đổi tên. Database lưu `cost_cents: int`, API có thể trả chuỗi tiền đã định dạng; database lưu UTC datetime, API serialize ISO 8601; status nội bộ `PAYMENT_PENDING_REVIEW` có thể được rút thành trạng thái công khai ổn định. Mapper là anti-corruption layer giữa hai từ vựng.\n\nKhông đặt query trong mapper. Mapper nên thuần để test dễ. Nếu response cần workshop title, repository dùng projection/join lấy đủ dữ liệu rồi mapper chuyển. Truy cập relationship lazy trong mapper có thể tạo N+1 query: danh sách 100 booking phát thêm 100 query.\n\n### Entity giữ invariant\n\nDTO chỉ chứng minh hình dạng hợp lệ, không chứng minh hành động hợp lệ. `seats=1` qua validation nhưng workshop có thể đầy. Email đúng cú pháp nhưng actor không được đặt hộ. Domain/service kiểm những điều cần state hoặc policy.\n\nĐừng nhồi database session vào Pydantic validator. Validator có thể chạy nhiều lần, khó quản lý timeout và làm 422 phụ thuộc mạng. Validation cú pháp ở DTO; uniqueness, quyền và khả dụng trong use case/transaction.\n\n### ORM model không phải API schema\n\nORM cần identity map, lazy proxy, relationship hai chiều và state session. Serialize trực tiếp có thể gặp recursion, detached instance hoặc query ngầm. Nó cũng khiến test API phải dựng ORM object đúng lifecycle.\n\nResponse DTO là dữ liệu thuần, đóng băng contract sau khi session đóng. Với Pydantic có thể đọc attribute từ object khi cấu hình phù hợp, nhưng đó chỉ là tiện ích mapping; vẫn phải chọn model output riêng và kiểm query phát sinh.\n\n### Null, thiếu và giá trị mặc định\n\nDatabase default, domain default và API default không đồng nghĩa. Nếu API bỏ `created_at`, database sinh thời gian; response sau insert phải lấy giá trị đã persist, không tự đoán bằng clock khác. Nếu boolean mặc định `false`, PATCH bỏ field nghĩa là giữ nguyên, không phải đặt false.\n\nMột field nullable trong DB có thể chỉ là chi tiết migration tạm thời, không có nghĩa API nên công khai null. Ngược lại API có thể trả field nullable vì dữ liệu lịch sử thiếu dù schema mới đã NOT NULL. Contract phải bám thực tế dữ liệu và chiến lược rollout.\n\n### Enum và trạng thái tiến hóa\n\nClient thường viết switch kín trên enum. Thêm giá trị mới có thể phá client cũ dù JSON vẫn hợp lệ. Có thể tài liệu hóa fallback `unknown`, hoặc giữ enum công khai nhỏ và map nhiều trạng thái nội bộ về nó. Xóa/đổi tên field cần giai đoạn deprecation.\n\nDatabase migration expand-contract thường thêm cột mới, ghi kép hoặc backfill, chuyển reader rồi mới xóa cột cũ. DTO ổn định cho phép migration diễn ra mà API không đổi. Nếu một model dùng chung, migration nội bộ dễ ép deploy đồng thời mọi client.\n\n### ID và kiểu dữ liệu qua biên\n\nPrimary key số tăng dần có thể tiết lộ quy mô và hỗ trợ enumeration. Public id ngẫu nhiên/opaque giảm tín hiệu nhưng không thay authorization. Dù ID khó đoán, service vẫn phải kiểm actor sở hữu booking.\n\nSố tiền tránh float; lưu integer đơn vị nhỏ nhất và định nghĩa currency. Datetime phải kèm timezone. Decimal serialize cần quy ước chuỗi hoặc số rõ. Những lựa chọn này thuộc contract, không để serializer tự quyết theo version thư viện.\n\n### Bảo vệ mass assignment\n\nCode `row = BookingRow(**request.model_dump())` chỉ an toàn khi request schema cực chặt và tên trùng có chủ đích; nó dễ trở nên nguy hiểm sau refactor. Tốt hơn là gán field tường minh hoặc mapper create chuyên dụng. Với extra field, policy `forbid` giúp phát hiện client gửi nhầm hoặc thăm dò.\n\nKhông nhận `owner_id`, `tenant_id`, `created_by` từ body nếu chúng phải đến từ identity. Server lấy chúng từ actor đã xác minh. Cũng không cho client chọn `version` để vượt optimistic concurrency; version trong `If-Match` có semantic khác và phải kiểm atomically.\n\n### Projection cho endpoint đọc\n\nKhông phải lúc nào cũng cần hydrate entity đầy đủ. Endpoint danh sách chỉ cần id, workshop title, status có thể query projection vừa đủ rồi map response. Điều này giảm cột, memory và nguy cơ lazy load. Nhưng projection không nên được dùng cho command cần invariant của entity, nếu nó bỏ state cần thiết.\n\nCQRS nhẹ—model đọc khác model ghi—thường xuất hiện tự nhiên. Không cần message bus hay hai database để hưởng lợi; hai query/result type đã đủ. Hãy tăng độ phức tạp chỉ khi tải hoặc nhịp thay đổi đòi hỏi.\n\n### Contract test và snapshot\n\nTest response phải kiểm field cho phép, type và ví dụ ca biên, đặc biệt đảm bảo field bí mật vắng mặt. Snapshot toàn JSON tiện nhưng dễ được cập nhật vô thức. Kết hợp JSON Schema/OpenAPI diff trong CI với assertion bảo mật cụ thể.\n\nConsumer-driven contract hữu ích khi frontend deploy độc lập. Nó không thay test nghiệp vụ; schema đúng vẫn có thể trả booking của người khác. Security test cần hai actor và tài nguyên chéo để bắt IDOR.\n\n### Chi phí và khi không tách\n\nMapping hàng triệu row có CPU và allocation; đo profiler rồi dùng projection, batch hoặc serializer tối ưu. Đừng hy sinh contract trước khi có số đo. Một admin script nằm cùng process, không có API ổn định và model rất nhỏ có thể dùng chung dataclass.\n\nDấu hiệu cần tách ngay: dữ liệu nhạy cảm, public API, nhiều client, migration độc lập, ORM relationship, hoặc input/output khác quyền. Boilerplate lúc ấy là chi phí kiểm soát thay đổi, không phải lãng phí.\n\n### Cache cũng phải tôn trọng DTO\n\nCache response theo contract công khai, không cache ORM entity còn gắn session. Khóa cache phải chứa tenant, quyền và phiên bản representation khi các yếu tố ấy đổi kết quả; thiếu tenant có thể trả booking của tổ chức khác. Khi schema response tiến hóa, version khóa hoặc xóa dữ liệu cũ có kiểm soát. Mã hóa object Python bằng pickle trong cache dùng chung còn tạo rủi ro thực thi mã nếu cache bị can thiệp; JSON với schema giới hạn thường dễ kiểm soát hơn.",

vi_du: "### FastAPI với bốn model khác vai trò\n\n```python\nfrom dataclasses import dataclass\nfrom datetime import datetime, timezone\nfrom enum import StrEnum\nfrom fastapi import FastAPI, Header, HTTPException\nfrom pydantic import BaseModel, ConfigDict, EmailStr, Field\n\nclass PublicStatus(StrEnum):\n    HELD = \"held\"\n    CANCELLED = \"cancelled\"\n\nclass CreateBookingRequest(BaseModel):\n    model_config = ConfigDict(extra=\"forbid\")\n    email: EmailStr\n    seats: int = Field(default=1, ge=1, le=1)\n\nclass PatchBookingRequest(BaseModel):\n    email: EmailStr | None = None\n\nclass BookingResponse(BaseModel):\n    id: str\n    workshop_id: str\n    email: EmailStr\n    status: PublicStatus\n    created_at: datetime\n\n@dataclass\nclass BookingRow:                 # minh họa persistence model\n    id: str\n    workshop_id: str\n    owner_id: str\n    email: str\n    internal_status: str\n    payment_reference: str | None\n    cost_cents: int\n    created_at: datetime\n\ndef to_response(row: BookingRow) -> BookingResponse:\n    public = PublicStatus.CANCELLED if row.internal_status == \"CANCELLED\" else PublicStatus.HELD\n    return BookingResponse(id=row.id, workshop_id=row.workshop_id,\n        email=row.email, status=public, created_at=row.created_at)\n\napp = FastAPI()\nrows: dict[str, BookingRow] = {}\n\n@app.post(\"/workshops/{workshop_id}/bookings\", response_model=BookingResponse)\ndef create(workshop_id: str, body: CreateBookingRequest,\n           x_actor_id: str = Header()) -> BookingResponse:\n    booking_id = f\"booking-{len(rows)+1}\"\n    row = BookingRow(booking_id, workshop_id, x_actor_id,\n        str(body.email).casefold(), \"HELD_UNPAID\", None, 50000,\n        datetime.now(timezone.utc))\n    rows[booking_id] = row\n    return to_response(row)\n\n@app.get(\"/bookings/{booking_id}\", response_model=BookingResponse)\ndef get(booking_id: str, x_actor_id: str = Header()) -> BookingResponse:\n    row = rows.get(booking_id)\n    if row is None:\n        raise HTTPException(404, \"booking not found\")\n    if row.owner_id != x_actor_id:\n        raise HTTPException(404, \"booking not found\")\n    return to_response(row)\n```\n\n`payment_reference`, `cost_cents` và `owner_id` không thể xuất hiện vì mapper không chọn chúng. Trả 404 cho cả thiếu và không thuộc sở hữu giảm khả năng dò ID; production vẫn phải xác thực header chứ không tin chuỗi client tự gửi.\n\n### PATCH giữ được omitted\n\n```python\ndef patch_fields(body: PatchBookingRequest) -> dict[str, object]:\n    changes = body.model_dump(exclude_unset=True)\n    if \"email\" in changes and changes[\"email\"] is None:\n        raise ValueError(\"email cannot be null\")\n    return changes\n\nassert patch_fields(PatchBookingRequest()) == {}\n```\n\nClient gửi `{}` khác với `{\"email\": null}`. Nếu dùng `exclude_none=True`, hai ý nghĩa bị nhập làm một và có thể che lỗi contract.",

so_sanh: "### Một model hay model tách biệt\n\n| Tiêu chí | Dùng ORM model trực tiếp | DTO riêng | Projection + DTO |\n|---|---|---|---|\n| Code ban đầu | ngắn | có mapping | nhiều query type hơn |\n| Chống lộ field | yếu, field mới dễ rò | allowlist rõ | mạnh và chỉ đọc cột cần |\n| Migration độc lập | khó | tốt | tốt nhất cho read API |\n| Invariant ghi | dễ mass assignment | service/entity kiểm | không hợp command phức tạp |\n| Hiệu năng danh sách | dễ N+1 | tùy mapper | kiểm soát query tốt |\n\nDTO riêng là mặc định hợp lý cho public API. Projection thêm khi endpoint đọc có nhu cầu khác entity. Dùng chung model chỉ nên là quyết định có phạm vi nhỏ, không phải shortcut lan khắp hệ thống.",

loi_thuong_gap: [
  "Dùng một schema optional cho create và patch; tài liệu cho phép request rỗng và code đoán default.",
  "Trả ORM entity; thêm cột nội bộ rồi security test thấy field mới trong JSON.",
  "Mass assignment từ `model_dump`; attacker thử gửi status hoặc tenant_id và giá trị bị persist.",
  "Validator gọi database; 422 chậm, timeout khó phân loại và transaction bị mở ngoài use case.",
  "Mapper chạm relationship lazy trong vòng lặp; log query cho thấy N+1.",
  "Dùng float cho tiền hoặc datetime không timezone; đối soát lệch và client hiểu khác múi giờ.",
  "Tin public id khó đoán thay cho authorization; actor thứ hai vẫn đọc được booking thứ nhất."
],

tu_kiem_tra: [
  "Vì sao column nullable trong migration không buộc response field phải nullable?",
  "Phân biệt omitted, null và default trong một PATCH email.",
  "Field nào của BookingRow phải bị loại khỏi response và vì sao dùng allowlist an toàn hơn denylist?",
  "Khi nào projection tốt hơn hydrate entity, và khi nào projection làm mất invariant?",
  "Thêm enum status mới có thể phá client cũ theo cách nào dù schema JSON hợp lệ?",
  "Hãy thiết kế test bắt cả mass assignment và IDOR với hai actor."
],

tai_nguyen: [
  "Patterns of Enterprise Application Architecture — Martin Fowler",
  "Designing Data-Intensive Applications — Martin Kleppmann",
  "API Design Patterns — JJ Geewax",
  "FastAPI và Pydantic Official Documentation"
],

lien_he: [
  "Nối sang mảng Cơ sở dữ liệu qua schema migration, projection, index và optimistic concurrency.",
  "Nối sang mảng Bảo mật qua mass assignment, excessive data exposure, IDOR và data minimization.",
  "Nối sang mảng Frontend qua backward-compatible contract, enum tiến hóa và generated client.",
  "Nối sang mảng Kiểm thử qua OpenAPI diff, consumer contract và assertion field nhạy cảm vắng mặt."
]

};
