window.LESSONS["04:van-hanh-thuc-te:migration-voi-alembic"] = {

tom_tat: [
  "Migration là lịch sử thay đổi schema có thứ tự; Alembic quản lý revision graph và chạy thao tác nâng/hạ, không tự bảo đảm thay đổi an toàn trên production.",
  "Autogenerate chỉ so metadata với schema và tạo ứng viên; người viết phải review kiểu dữ liệu, constraint, tên object, dữ liệu cũ và câu downgrade.",
  "Triển khai zero-downtime cần giữ tương thích giữa code cũ và code mới bằng expand–migrate–contract, thay vì rename/drop trong một bước.",
  "DDL PostgreSQL có transaction nhưng lock và table rewrite vẫn có thể chặn traffic; timeout, `CREATE INDEX CONCURRENTLY` và backfill theo lô giải quyết các rủi ro khác nhau.",
  "Schema migration, data migration và deploy ứng dụng là ba dòng thời gian phải phối hợp, quan sát và có kế hoạch tiến/lùi rõ ràng."
],

can_biet_truoc: [
  "DDL PostgreSQL: `CREATE TABLE`, `ALTER TABLE`, constraint, index và transaction.",
  "Model SQLAlchemy và `MetaData`; Alembic dùng metadata để autogenerate diff.",
  "Khái niệm lock, timeout và deploy nhiều instance chạy đồng thời.",
  "Quy tắc booking của Vé Mây cùng các trạng thái `pending`, `paid`, `cancelled`, `expired`."
],

dinh_nghia: "**Database migration** là một thay đổi schema hoặc dữ liệu được đặt phiên bản, có thứ tự phụ thuộc và có thể áp dụng lặp lại nhất quán từ trạng thái đã biết. **Alembic** là công cụ migration cho SQLAlchemy: bảng `alembic_version` ghi revision hiện tại; mỗi file revision khai báo `revision`, `down_revision`, hàm `upgrade()` và `downgrade()`.\n\nAlembic không phải hệ thống sao lưu và `downgrade()` không phải nút undo thần kỳ. Xóa cột làm mất dữ liệu dù DDL rollback được trong lúc transaction chưa commit. Một migration production tốt phải xét đồng thời tính đúng của schema, dữ liệu hiện có, lock, thời gian chạy và sự tương thích với các binary ứng dụng đang sống.",

vi_sao_can: "Vé Mây cần thêm `expires_at` để tự giải phóng booking pending. Sửa model Python không làm cột xuất hiện trong PostgreSQL; chạy `CREATE TABLE` lại sẽ phá dữ liệu. Nếu mỗi máy chủ được sửa tay, staging và production sớm khác nhau, không ai biết cột nào có trước và lỗi chỉ xuất hiện lúc deploy.\n\nMigration biến thay đổi thành artifact review được và tái hiện được trên database mới. Tuy nhiên vấn đề khó là khoảng giao nhau: trong rolling deploy, instance cũ chưa biết `expires_at` vẫn phục vụ cạnh instance mới; worker backfill có thể khóa nhiều hàng; index mới có thể tranh I/O; một constraint đúng trên dữ liệu mới có thể thất bại vì hàng lịch sử.\n\nVì thế bài này không dừng ở lệnh Alembic. Nó giải thích cách chia thay đổi sao cho hệ thống tiếp tục phục vụ, cách kiểm tra trước/sau, và lúc nào nên roll forward thay vì cố downgrade.",

co_che: "### Revision graph chứ không chỉ danh sách file\n\nMỗi revision trỏ tới `down_revision`. Nhánh phát triển song song có thể tạo hai heads; Alembic không đoán thứ tự vì hai thay đổi có thể độc lập hoặc xung đột. Nhóm phải rebase revision chưa phát hành hoặc tạo merge revision. Tên file không quyết định thứ tự; ID và cạnh trong graph mới quyết định.\n\n`alembic current` đọc trạng thái database, `heads` cho biết đỉnh code, `history` cho đường đi. Trước deploy phải kiểm database đang ở revision mà artifact mong đợi. Không chỉnh tay `alembic_version` để “cho qua”: stamp chỉ hợp khi schema đã được tạo đúng bằng cơ chế khác và đã xác minh.\n\n### Autogenerate là bản nháp\n\n`alembic revision --autogenerate` so `target_metadata` với catalog. Nó thấy thêm/bớt bảng, cột, index và một số constraint; nó không hiểu rename nên thường sinh drop + add. Nó không biết backfill nghiệp vụ, không biết một cột mới phải lấy giá trị nào, và không đánh giá lock.\n\nReview phải đọc cả upgrade lẫn downgrade, mở SQL bằng `alembic upgrade ... --sql` khi offline mode phù hợp, và thử trên snapshot dữ liệu gần thật. Đặt tên constraint/index ổn định giúp diff ít nhiễu.\n\n### Expand–migrate–contract cho expires_at\n\nBước expand thêm cột nullable; cả code cũ lẫn mới tiếp tục ghi được. Bản code trung gian bắt đầu ghi `expires_at` cho booking mới và đọc theo fallback. Bước migrate backfill hàng cũ theo lô. Khi metric xác nhận không còn NULL, thêm constraint `NOT NULL`. Chỉ sau khi mọi binary cũ biến mất mới contract, tức bỏ fallback hay cột cũ.\n\n```text\nR0 code cũ + schema cũ\n → expand schema tương thích\n → R1 ghi kép/đọc fallback\n → backfill có checkpoint\n → validate dữ liệu\n → constraint\n → R2 chỉ dùng schema mới\n → contract phần cũ\n```\n\n### Vì sao thêm default có thể khác backfill\n\nGiá trị `now()` cho `expires_at` của hàng cũ thường sai nghiệp vụ: hạn phải dựa trên `created_at`, ví dụ `created_at + interval '15 minutes'`. Default phục vụ insert tương lai; backfill tính lịch sử. Gom hai việc vào một `ALTER` dài làm migration khó quan sát và khó resume.\n\nMột số thay đổi metadata-only nhanh trên PostgreSQL hiện đại, nhưng không nên ghi số phiên bản hay hứa không rewrite nếu chưa kiểm môi trường. Dùng `EXPLAIN` không áp dụng cho DDL; quan sát lock, WAL, dung lượng và thời gian trên rehearsal.\n\n### Lock timeout khác statement timeout\n\n`lock_timeout` giới hạn thời gian chờ lấy lock; `statement_timeout` giới hạn tổng thời gian statement. Đặt lock timeout ngắn giúp migration thất bại sớm thay vì xếp hàng rồi bất ngờ khóa traffic. Statement timeout quá ngắn có thể giết backfill hợp lệ.\n\n```sql\nSET LOCAL lock_timeout = '2s';\nSET LOCAL statement_timeout = '30s';\nALTER TABLE bookings ADD COLUMN expires_at timestamptz;\n```\n\nCon số phải chọn theo SLO và rehearsal, không sao chép máy móc. Khi timeout, transaction rollback; pipeline deploy phải dừng và báo rõ, không stamp revision.\n\n### Backfill theo lô và tính idempotent\n\nUpdate toàn bảng tạo nhiều WAL, giữ row lock lâu và sinh bloat. Worker backfill chọn một lô khóa ngắn, update, commit rồi tiếp tục. Predicate `expires_at IS NULL` làm thao tác có thể chạy lại. Nhiều worker có thể dùng `FOR UPDATE SKIP LOCKED`, nhưng tăng worker không tuyến tính vì tranh I/O.\n\n```sql\nWITH batch AS (\n  SELECT id FROM bookings\n  WHERE expires_at IS NULL\n  ORDER BY id\n  FOR UPDATE SKIP LOCKED\n  LIMIT 1000\n)\nUPDATE bookings AS b\nSET expires_at = b.created_at + interval '15 minutes'\nFROM batch\nWHERE b.id = batch.id\nRETURNING b.id;\n```\n\nCheckpoint nên là dữ liệu thật (`IS NULL` còn bao nhiêu), không chỉ biến nhớ của worker. Nếu booking paid không cần expiry theo semantics, công thức phải phân nhánh và constraint phải phản ánh điều đó.\n\n### Constraint theo hai pha\n\nThêm check constraint trên bảng lớn có thể phải quét dữ liệu. PostgreSQL cho tạo `NOT VALID` để áp dụng cho hàng mới mà chưa quét lịch sử, sau đó `VALIDATE CONSTRAINT`. Validation vẫn dùng tài nguyên nhưng lock nhẹ hơn thay đổi cấu trúc trực tiếp. Cuối cùng có thể chuyển thành `NOT NULL` nếu thực sự cần và phiên bản/môi trường hỗ trợ quy trình mong muốn.\n\n```sql\nALTER TABLE bookings ADD CONSTRAINT ck_booking_expiry\nCHECK (status <> 'pending' OR expires_at IS NOT NULL) NOT VALID;\nALTER TABLE bookings VALIDATE CONSTRAINT ck_booking_expiry;\n```\n\nConstraint này tốt hơn `expires_at NOT NULL` nếu booking paid nhập từ hệ thống cũ hợp lệ không có expiry. Schema phải mã hóa invariant thật, không invariant thuận tiện cho code mới.\n\n### Index và CONCURRENTLY\n\nQuery worker tìm booking pending hết hạn cần partial index `(expires_at) WHERE status='pending'`. `CREATE INDEX` thường chặn write theo cách không phù hợp production; `CREATE INDEX CONCURRENTLY` giảm chặn nhưng không được chạy trong transaction block và có thể để index invalid nếu thất bại.\n\nAlembic dùng `autocommit_block()` cho thao tác này. Sau lỗi phải kiểm `pg_index.indisvalid` và xử lý index invalid có chủ đích. Concurrent không có nghĩa miễn phí: nó quét bảng, dùng I/O, WAL và mất lâu hơn.\n\n### Transactional DDL và ngoại lệ vận hành\n\nPostgreSQL cho rollback nhiều DDL, lợi thế lớn khi migration gồm vài statement ngắn. Nhưng `CREATE INDEX CONCURRENTLY` phá ranh giới transaction; extension hay thao tác ngoài database cũng không cùng atomicity. Một revision chứa external API call là thiết kế khó phục hồi.\n\nGiữ schema migration trong Alembic. Data backfill dài nên là job deploy riêng có version, metric và khả năng resume; revision có thể tạo điều kiện rồi dừng, pipeline chỉ tiến khi backfill hoàn tất.\n\n### Downgrade không phục hồi dữ liệu\n\nDowngrade thêm lại cột đã drop chỉ tạo cột rỗng; booking expiry cũ không tự quay lại. Với production, chiến lược thường là roll forward: tắt feature flag, deploy code tương thích, sửa revision mới. Downgrade hữu ích trong local/test và cho thay đổi đảo được rõ ràng.\n\nTrước thay đổi phá hủy cần backup đã thử restore, thời gian giữ tương thích và quyết định go/no-go. Backup không thay cho expand–contract vì restore toàn database làm mất giao dịch sau thời điểm backup.\n\n### Thứ tự deploy an toàn\n\nSchema expand đi trước code cần schema. Code ngừng dùng cột cũ đi trước schema contract. Nếu migration tự chạy ở startup của mọi replica, nhiều process tranh advisory lock hoặc chạy DDL lúc autoscale; nên có một migration job duy nhất trong pipeline.\n\nHealth check không nên báo ready trước khi schema tối thiểu có mặt. Ứng dụng có thể kiểm revision được hỗ trợ, nhưng đừng để mọi request query `alembic_version`; kiểm lúc startup và phát metric.\n\n### Tách quyền migration và runtime\n\nRole Alembic cần DDL trong schema; role web chỉ cần DML. Nếu runtime sở hữu bảng, injection hoặc bug có bán kính lớn hơn. Pipeline lấy credential migration trong bước ngắn, ghi audit và không đưa nó vào container runtime.\n\nAlembic environment không nên hardcode URL chứa password trong repo. Lấy URL từ secret manager hoặc biến môi trường triển khai; log phải che credential.\n\n### Kiểm thử migration như code\n\nTest “database trống upgrade tới head” bắt lỗi thiếu dependency. Test “revision production gần nhất → head” bắt lỗi dữ liệu cũ. Test downgrade chỉ có ý nghĩa cho revision cam kết đảo được. Quan trọng hơn, rehearsal trên kích thước gần production đo lock/WAL/dung lượng.\n\nSau migration, kiểm constraint, index valid, số NULL, query plan worker expiry và tỷ lệ lỗi app. Chỉ kiểm exit code Alembic là chưa đủ: command có thể thành công nhưng index không được query dùng hoặc backfill sai semantics.\n\n### Drift và thay đổi ngoài luồng\n\nNếu người vận hành sửa schema bằng tay, metadata, revision và catalog lệch nhau. Autogenerate kế tiếp có thể sinh diff bất ngờ. Hạn chế DDL trực tiếp, audit event, và chạy drift check định kỳ. Khi sự cố buộc hotfix, phải lập revision tương ứng sau đó thay vì để “kiến thức truyền miệng”.\n\n### Khi nào không gom vào một revision\n\nKhông gom add cột, backfill hàng triệu dòng, set constraint, tạo index concurrent và drop cột cũ chỉ để có một file đẹp. Revision nhỏ theo ranh giới deploy giúp quan sát và rollback code. Ngược lại, chia một thay đổi atomic nhỏ thành quá nhiều revision rỗng làm graph nhiễu.\n\nTiêu chí là failure domain: phần nào có thể thất bại hoặc cần chờ metric riêng thì nên có checkpoint riêng.",

vi_du: "### Revision expand chạy được\n\n```python\n\"\"\"add nullable booking expiry\n\nRevision ID: 20260901_add_booking_expiry\nRevises: 20260820_booking_base\n\"\"\"\nfrom alembic import op\nimport sqlalchemy as sa\n\nrevision = \"20260901_add_booking_expiry\"\ndown_revision = \"20260820_booking_base\"\nbranch_labels = None\ndepends_on = None\n\ndef upgrade() -> None:\n    op.add_column(\"bookings\", sa.Column(\"expires_at\", sa.DateTime(timezone=True), nullable=True))\n    op.create_check_constraint(\n        \"ck_booking_expiry\",\n        \"bookings\",\n        \"status <> 'pending' OR expires_at IS NOT NULL\",\n        postgresql_not_valid=True,\n    )\n\ndef downgrade() -> None:\n    op.drop_constraint(\"ck_booking_expiry\", \"bookings\", type_=\"check\")\n    op.drop_column(\"bookings\", \"expires_at\")\n```\n\nConstraint ngay lập tức yêu cầu insert pending mới có expiry, nên code ghi mới phải được deploy trước hoặc constraint phải dời sang revision sau. Đây là điểm review quan trọng: expand lý tưởng phải tương thích code cũ. Phiên bản an toàn hơn chỉ add nullable ở revision đầu; deploy dual-write; revision sau mới add constraint.\n\n### Revision index concurrent\n\n```python\nfrom alembic import op\n\nrevision = \"20260903_pending_expiry_index\"\ndown_revision = \"20260902_validate_expiry\"\n\ndef upgrade() -> None:\n    with op.get_context().autocommit_block():\n        op.execute(\"\"\"\n          CREATE INDEX CONCURRENTLY ix_bookings_pending_expires\n          ON bookings (expires_at, id)\n          WHERE status = 'pending'\n        \"\"\")\n\ndef downgrade() -> None:\n    with op.get_context().autocommit_block():\n        op.execute(\"DROP INDEX CONCURRENTLY IF EXISTS ix_bookings_pending_expires\")\n```\n\nTrước chạy phải chắc tên index chưa tồn tại/invalid. Sau chạy kiểm plan bằng query có cùng predicate:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id FROM bookings\nWHERE status = 'pending' AND expires_at <= now()\nORDER BY expires_at, id\nLIMIT 100;\n```\n\nCa biên phải thử: database trống; booking paid không expiry; pending cũ; hai Alembic heads; timeout lock; index concurrent thất bại; chạy lại backfill; deploy code cũ cạnh code mới. Không ghi con số thời gian nếu chưa rehearsal trên dữ liệu thật.",

so_sanh: "### Ba chiến lược thay schema\n\n| Chiến lược | Availability | Độ phức tạp | Phù hợp |\n|---|---|---|---|\n| Dừng hệ thống rồi migrate | Có downtime | Thấp | Nội bộ nhỏ, cửa sổ bảo trì chấp nhận được |\n| Migration phá vỡ cùng deploy | Rủi ro cao | Bề ngoài thấp | Chỉ môi trường đơn instance kiểm soát chặt |\n| Expand–migrate–contract | Giữ rolling deploy | Cao hơn, nhiều bước | Production cần liên tục phục vụ |\n\n| Công cụ | Biết revision | Sinh diff | Chạy backfill dài | Điều phối deploy |\n|---|---:|---:|---:|---:|\n| Alembic | Có | Có, cần review | Có thể nhưng không lý tưởng | Không |\n| SQL thủ công có version | Do nhóm tự quản | Không | Có | Không |\n| Pipeline/orchestrator | Không hiểu schema | Không | Điều phối job | Có |\n\nAlembic giải đúng bài toán lịch sử schema; pipeline giải thứ tự rollout; backup giải thảm họa. Ép một công cụ làm cả ba tạo điểm mù.",

loi_thuong_gap: [
  "Tin autogenerate nhận ra rename. Diff hiện drop cột cũ và add cột mới; nếu chạy sẽ mất dữ liệu. Sửa thành `alter_column`/rename có chủ đích.",
  "Thêm cột `NOT NULL` trước khi code cũ biết ghi. Lỗi insert xuất hiện trong rolling deploy; dùng nullable và dual-write trước.",
  "Backfill toàn bảng trong một transaction. Dấu hiệu là WAL, bloat và row lock tăng; chia lô idempotent có checkpoint.",
  "Tạo index thường trên bảng nóng. Request ghi bị chặn; cân nhắc concurrent và kiểm index invalid.",
  "Dùng downgrade như khôi phục dữ liệu. Cột được tạo lại nhưng nội dung đã mất; cần backup/roll-forward và rehearsal.",
  "Cho mọi replica chạy Alembic lúc startup. Deploy tạo tranh lock và race; dùng một migration job duy nhất.",
  "Stamp revision để vượt lỗi. `alembic_version` nói head nhưng catalog thiếu object; chỉ stamp sau đối chiếu schema có bằng chứng.",
  "Không kiểm multiple heads trước phát hành. Các môi trường chọn đường khác nhau; merge graph hoặc tổ chức ownership revision."
],

tu_kiem_tra: [
  "Vì sao add nullable thường là bước expand an toàn hơn add `NOT NULL` trong rolling deploy?",
  "Autogenerate thấy rename như thế nào, và review phải sửa nguy cơ mất dữ liệu ra sao?",
  "Phân biệt lock timeout với statement timeout trong một migration DDL.",
  "Thiết kế các revision và deploy để đổi `status` text sang trạng thái chuẩn mà code cũ vẫn chạy.",
  "Vì sao `CREATE INDEX CONCURRENTLY` cần autocommit block, và sau thất bại phải kiểm gì?",
  "Một backfill dừng ở 60% rồi chạy lại. Predicate/checkpoint nào làm nó idempotent?",
  "Khi nào roll forward an toàn hơn downgrade, dù downgrade function tồn tại?",
  "Nêu bằng chứng cần thu ngoài exit code Alembic trước khi tuyên bố migration thành công."
],

tai_nguyen: [
  "Tài liệu chính thức Alembic, các phần Tutorial, Autogeneration, Branches và Operation Directives.",
  "Tài liệu chính thức PostgreSQL, các phần ALTER TABLE, Explicit Locking, CREATE INDEX và Constraints.",
  "“Refactoring Databases” — Scott Ambler và Pramod Sadalage.",
  "“Database Reliability Engineering” — Laine Campbell và Charity Majors."
],

lien_he: [
  "Nối sang mảng CI/CD: migration job là một stage có precondition, timeout, telemetry và gate trước rollout code.",
  "Nối sang mảng Bảo mật: tách role DDL khỏi runtime làm nhỏ hậu quả của SQL injection và credential bị lộ.",
  "Nối sang mảng Hệ phân tán: expand–contract là compatibility protocol giữa nhiều phiên bản binary cùng sống, tương tự versioning message.",
  "Nối sang mảng Sao lưu: migration phá hủy cần backup đã thử restore, nhưng restore không thay thế chiến lược zero-downtime."
]

};
