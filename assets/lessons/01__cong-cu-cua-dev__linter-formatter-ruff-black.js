window.LESSONS["01:cong-cu-cua-dev:linter-formatter-ruff-black"] = {

tom_tat: [
  "**Module 5 dạy PEP 8 và cách chạy công cụ; module 8 gom ngưỡng vào `pyproject.toml`. Bài này lấy góc QUY TRÌNH NHÓM:** cấu hình chung trong repo, chạy trong CI, và chiến lược đưa vào dự án cũ.",
  "**Formatter và linter là hai việc khác nhau.** Formatter *viết lại* code theo một cách duy nhất; linter *chỉ ra* vấn đề. Đừng để hai công cụ cùng định dạng.",
  "**Giá trị lớn nhất của formatter không phải code đẹp — mà là xoá bỏ cả một loại tranh cãi** và làm `git diff` chỉ còn thay đổi thật.",
  "**Ruff thay được cả một chồng công cụ** (flake8, isort, pyupgrade, và phần lớn pylint) trong một binary, nhanh hơn hàng chục tới hàng trăm lần vì viết bằng Rust và chạy song song.",
  "**Đưa vào dự án cũ phải siết dần, không bật hết một lượt.** Bật hết tạo hàng nghìn cảnh báo, không ai sửa, và cả đội học cách phớt lờ công cụ."
],

can_biet_truoc: [
  "Bài **PEP 8 và cách format tự động** ở module 5 — quy ước hình thức và cách chạy `black`/`ruff` trên máy cá nhân.",
  "Bài **Đọc và review code người khác** ở module 8 — bảng cấu hình `ruff` gom mọi ngưỡng của module đó.",
  "Bài **VS Code** trong module này — `editor.formatOnSave` và xung đột giữa nhiều formatter.",
  "Bài **Git & GitHub** ở mảng 03 sẽ nói kỹ về CI; ở đây chỉ cần hiểu CI là máy chạy kiểm tra tự động khi có commit."
],

dinh_nghia: "Nói đời thường: một toà soạn không để mỗi phóng viên tự quyết cách viết ngày tháng. Có một quy chuẩn, và có người biên tập áp dụng nó — để người đọc tập trung vào nội dung chứ không vấp vào hình thức.\n\nBa loại công cụ, ba việc khác nhau:\n\n- **Formatter** — **viết lại** code theo một cách trình bày duy nhất. Không đổi ý nghĩa, chỉ đổi hình thức: xuống dòng, dấu cách, dấu ngoặc. Ví dụ: `black`, `ruff format`.\n- **Linter** — **phân tích** code và báo vấn đề: biến không dùng, import thừa, `except` trần, hàm quá phức tạp. Không tự sửa (trừ khi bật `--fix`). Ví dụ: `ruff check`, `flake8`, `pylint`.\n- **Type checker** — kiểm tra tính nhất quán của kiểu dữ liệu. Ví dụ: `mypy`, `pyright`. Đây là loại thứ ba, khác hẳn hai loại trên.\n\nCác khái niệm của bài:\n\n- **Cấu hình chung trong repo** — mọi quy tắc nằm trong `pyproject.toml`, được commit, nên mọi người và cả CI dùng **cùng một** cấu hình.\n- **CI** (*Continuous Integration*) — máy tự động chạy kiểm tra mỗi khi có commit hoặc pull request.\n- **Siết dần** (*ratcheting*) — đặt ngưỡng ở mức hiện tại rồi nâng dần, thay vì bật hết một lượt.\n- **`.git-blame-ignore-revs`** — file liệt kê các commit thuần hình thức, để `git blame` bỏ qua chúng.\n\nMột phân biệt quyết định cách tổ chức:\n\n```\nFORMATTER  không có ĐÚNG/SAI — chỉ có NHẤT QUÁN\n           ⇒ chạy tự động, không thảo luận, không cấu hình nhiều\n\nLINTER     có ĐÚNG/SAI thật — báo lỗi tiềm ẩn\n           ⇒ cần chọn quy tắc, cần thảo luận, cần bỏ qua có lý do\n```",

vi_sao_can: "Ở góc cá nhân, formatter chỉ là tiện. Ở góc **đội**, nó giải quyết mấy vấn đề rất tốn kém:\n\n- **Xoá bỏ cả một loại tranh cãi.** Không ai cãi về dấu ngoặc kép hay độ dài dòng khi formatter tự quyết. Thời gian và thiện chí trong review dành cho thiết kế — thứ chỉ người làm được, như bài module 8 đã nói.\n- **`git diff` chỉ còn thay đổi thật.** Không có formatter chung, mỗi người lưu file là trình soạn thảo định dạng lại một kiểu, và pull request đầy dòng thay đổi giả. Người review phải lọc thủ công.\n- **Xung đột merge giảm hẳn.** Hai người sửa hai chỗ khác nhau nhưng định dạng lại cả file thì Git báo conflict; cùng formatter thì không.\n- **Linter bắt lỗi thật, không chỉ lỗi hình thức.** `except` trần, biến gán rồi không dùng, so sánh `is` với chuỗi — đây là bug tiềm ẩn, không phải chuyện thẩm mỹ.\n- **Chạy trong CI biến quy ước thành ràng buộc.** Chạy trên máy cá nhân là *đề nghị*; chạy trong CI là *bắt buộc*. Khác biệt này quyết định quy ước có sống được không.\n\nTình huống thật rất phổ biến: đội bốn người, không có formatter chung. Người A dùng VS Code với autopep8, người B dùng PyCharm với cấu hình mặc định, người C không dùng gì. Mỗi pull request có 200 dòng thay đổi, trong đó 170 dòng chỉ là đổi định dạng. **Người review không có cách nào tách 30 dòng thật ra khỏi 170 dòng nhiễu**, nên họ duyệt qua loa — và bug lọt qua. Một file `pyproject.toml` mười dòng xoá bỏ hoàn toàn vấn đề này.",

co_che: "### Formatter — vì sao \"không cấu hình\" là tính năng\n\n`black` được quảng bá là *uncompromising* (không thoả hiệp), và triết lý đó có lý do:\n\n```\nÍT TUỲ CHỌN  ⇒  ít thứ để tranh cãi\n             ⇒  mọi dự án Python trông giống nhau\n             ⇒  đọc code dự án lạ không phải làm quen lại\n```\n\nThực tế `black` chỉ cho chỉnh vài thứ, và `ruff format` cũng vậy:\n\n```toml\n[tool.ruff]\nline-length = 88          # gần như là tuỳ chọn DUY NHẤT đáng chỉnh\n```\n\nVì sao 88 chứ không phải 79 của PEP 8: `black` chọn con số này sau khi đo trên nhiều codebase thật — nó giảm đáng kể số dòng bị ngắt so với 79, trong khi vẫn xem được hai file cạnh nhau trên màn hình thông thường. Bạn có thể đổi thành 100 hay 120, nhưng **quan trọng là cả đội dùng một con số**, không phải con số nào.\n\nMột hệ quả cần hiểu: formatter **chuẩn hoá** code, nên nó sẽ viết lại cả những chỗ bạn cố ý trình bày đẹp. Có một lối thoát:\n\n```python\n# fmt: off\nMA_TRAN = [\n    [1, 0, 0],\n    [0, 1, 0],      # ⭐ giữ nguyên cách trình bày — formatter bỏ qua khối này\n    [0, 0, 1],\n]\n# fmt: on\n```\n\nDùng tiết kiệm. Mỗi lần dùng là một chỗ formatter không bảo vệ được nữa.\n\n### Linter — chọn quy tắc thế nào\n\nKhác formatter, linter **cần** cấu hình vì các quy tắc có mức độ hữu ích rất khác nhau.\n\n```toml\n[tool.ruff.lint]\nselect = [\n    \"E\", \"W\",     # pycodestyle — lỗi và cảnh báo PEP 8\n    \"F\",          # Pyflakes — LỖI THẬT: biến không dùng, import thừa\n    \"I\",          # isort — sắp xếp import\n    \"UP\",         # pyupgrade — dùng cú pháp Python mới hơn\n    \"B\",          # flake8-bugbear — BẪY phổ biến\n    \"SIM\",        # flake8-simplify — viết gọn hơn\n]\n```\n\nSáu nhóm này là bộ khởi đầu tốt cho hầu hết dự án. Trong đó **`F` và `B` là hai nhóm bắt lỗi thật**, đáng bật đầu tiên nếu chỉ chọn được hai:\n\n```\nF  Pyflakes    biến gán rồi không dùng, import thừa, tên chưa định nghĩa\nB  bugbear     BẪY: tham số mặc định mutable, except trần,\n               biến vòng lặp bị bắt trong closure, so sánh sai\n```\n\nNhóm `B` đặc biệt đáng giá vì nó bắt đúng những cái bẫy đã học ở module 5 và 7:\n\n```python\n# B006 — tham số mặc định mutable (bài module 7)\ndef them(muc, ds=[]):        # ⚠️ ruff bắt được\n    ...\n\n# B008 — gọi hàm trong giá trị mặc định\ndef f(t=datetime.now()):     # ⚠️ chỉ tính MỘT lần lúc định nghĩa\n    ...\n\n# B902 / BLE001 — bắt Exception trần (bài module 8)\ntry:\n    ...\nexcept:                       # ⚠️\n    pass\n```\n\n### Bỏ qua quy tắc — ba cấp độ\n\n```python\n# ① MỘT DÒNG — phải ghi mã quy tắc cụ thể\nimport cau_hinh  # noqa: F401  — nạp để đăng ký plugin, không dùng trực tiếp\n\n# ❌ noqa trần — bỏ qua MỌI quy tắc trên dòng đó\nimport cau_hinh  # noqa\n```\n\n```toml\n# ② MỘT FILE hoặc MỘT NHÓM FILE\n[tool.ruff.lint.per-file-ignores]\n\"__init__.py\" = [\"F401\"]           # re-export là bình thường ở đây\n\"tests/**\" = [\"S101\", \"PLR2004\"]   # assert và magic number OK trong test\n\"migrations/**\" = [\"E501\"]          # file sinh tự động\n```\n\n```toml\n# ③ CẢ DỰ ÁN\n[tool.ruff.lint]\nignore = [\n    \"E501\",    # độ dài dòng — formatter đã lo\n    \"D203\",    # xung đột với D211, phải chọn một\n]\n```\n\n⚠️ Quy tắc vàng: **`# noqa` phải luôn có mã quy tắc và tốt nhất là kèm lý do.** `# noqa` trần tắt mọi kiểm tra trên dòng đó — kể cả những lỗi thật xuất hiện sau này khi dòng đó được sửa.\n\nRuff kiểm được chính điều này:\n\n```toml\n[tool.ruff.lint]\nselect = [\"RUF100\"]     # phát hiện noqa THỪA (quy tắc đó không còn báo nữa)\n```\n\n`RUF100` giải quyết một vấn đề thật: `noqa` tích tụ theo năm tháng, và không ai dám xoá vì không biết còn cần không. Quy tắc này trả lời hộ.\n\n### Ba nơi chạy — ba vai trò khác nhau\n\nĐây là phần cốt lõi của góc \"quy trình nhóm\":\n\n```\n① TRÌNH SOẠN THẢO (khi lưu)   phản hồi TỨC THÌ, sửa được ngay\n                               → nhưng mỗi người cấu hình khác nhau ⇒ không tin được\n\n② PRE-COMMIT HOOK (khi commit) chặn TRƯỚC khi vào lịch sử Git\n                               → nhưng bỏ qua được bằng --no-verify ⇒ không tin được\n\n③ CI (khi push / mở PR)        RÀNG BUỘC THẬT, không bỏ qua được\n                               → nhưng phản hồi chậm (vài phút)\n```\n\nBa tầng này bổ sung nhau chứ không thay thế nhau:\n\n```\n① nhanh nhất, yếu nhất  → sửa lỗi ngay khi gõ\n② vừa                    → chặn phần lớn lỗi trước khi commit\n③ chậm nhất, mạnh nhất   → là NGUỒN SỰ THẬT duy nhất\n```\n\nNguyên tắc quan trọng: **CI là nguồn sự thật.** Nếu CI xanh mà máy ai đó đỏ, hoặc ngược lại, thì cấu hình đang không đồng bộ — và đó là lỗi cần sửa, không phải chuyện chấp nhận được.\n\nĐể ba tầng dùng **cùng một** cấu hình:\n\n```\n· Mọi quy tắc nằm trong pyproject.toml (được commit)\n· GHIM PHIÊN BẢN công cụ — ruff 0.5 và 0.6 cho kết quả khác nhau\n· Extension của trình soạn thảo đọc cùng file đó\n```\n\nGhim phiên bản là chi tiết hay bị bỏ qua và gây ra tình huống khó chịu nhất: máy bạn `ruff 0.6` báo sạch, CI dùng `ruff 0.5` báo lỗi, và bạn không hiểu vì sao.\n\n```toml\n[project.optional-dependencies]\ndev = [\n    \"ruff==0.6.9\",      # ⭐ ghim CHÍNH XÁC, không dùng >=\n    \"mypy==1.11.2\",\n    \"pytest==8.3.3\",\n]\n```\n\n### Cấu hình CI\n\n```yaml\n# .github/workflows/kiem-tra.yml\nname: Kiểm tra chất lượng\n\non: [push, pull_request]\n\njobs:\n  kiem-tra:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - run: pip install -e \".[dev]\"    # ⭐ cùng phiên bản đã ghim\n\n      - name: Linter\n        run: ruff check --output-format=github .\n\n      - name: Formatter\n        run: ruff format --check .        # ⭐ --check: chỉ KIỂM, không sửa\n\n      - name: Kiểm tra kiểu\n        run: mypy .\n\n      - name: Test\n        run: pytest -q\n```\n\nHai chi tiết đáng chú ý:\n\n```\n--output-format=github   lỗi hiện thành CHÚ THÍCH ngay trên dòng code\n                          trong giao diện pull request — người review\n                          thấy ngay chỗ nào sai\n\n--check                  trong CI phải dùng --check, KHÔNG để formatter\n                          tự sửa rồi commit ngược — CI sửa code là hành vi\n                          gây bất ngờ và khó lần\n```\n\n### Đưa vào dự án cũ — chiến lược siết dần\n\nĐây là phần thực tế nhất, vì phần lớn dự án bạn tham gia sẽ **đã có sẵn code**.\n\n```\n❌ CÁCH SAI: bật hết quy tắc một lượt\n   → 3.000 cảnh báo\n   → không ai sửa\n   → cả đội học cách PHỚT LỜ công cụ\n   → tệ hơn không có công cụ, vì giờ cảnh báo thật cũng bị bỏ qua\n```\n\n```\n✓ CÁCH ĐÚNG — bốn giai đoạn\n\nGIAI ĐOẠN 1  Format toàn bộ MỘT LẦN, commit RIÊNG\n             + thêm vào .git-blame-ignore-revs\n\nGIAI ĐOẠN 2  Bật nhóm quy tắc DỄ và GIÁ TRỊ CAO: F, E, I\n             sửa hết (thường ít, và ruff --fix tự sửa phần lớn)\n\nGIAI ĐOẠN 3  Bật thêm từng nhóm một, mỗi nhóm một pull request\n             B → UP → SIM → ...\n\nGIAI ĐOẠN 4  Bật nhóm KHÓ với per-file-ignores cho file cũ\n             mỗi lần đụng file nào thì dọn file đó\n```\n\nGiai đoạn 1 tạo ra một commit khổng lồ chạm mọi file, và nó phá `git blame`. Cách xử lý:\n\n```bash\n# Format toàn bộ, commit riêng\nruff format .\ngit add -A\ngit commit -m \"style: format toàn bộ codebase bằng ruff format\"\n\n# Ghi lại mã commit đó\ngit rev-parse HEAD >> .git-blame-ignore-revs\ngit add .git-blame-ignore-revs\ngit commit -m \"chore: bỏ qua commit format trong git blame\"\n\n# Cấu hình để git blame tự dùng file này\ngit config blame.ignoreRevsFile .git-blame-ignore-revs\n```\n\nSau bước này, `git blame` bỏ qua commit format và vẫn hiện đúng người sửa nội dung thật. GitHub cũng đọc file này tự động.\n\nGiai đoạn 4 dùng `per-file-ignores` như một danh sách nợ có thể thu nhỏ dần:\n\n```toml\n[tool.ruff.lint.per-file-ignores]\n# NỢ KỸ THUẬT — xoá dần khi đụng tới các file này\n\"legacy/bao_cao.py\" = [\"C901\", \"PLR0913\"]\n\"legacy/xu_ly.py\" = [\"C901\"]\n```\n\nCách này áp đúng **quy tắc hướng đạo sinh** ở bài refactor module 8: không dành hẳn một đợt dọn, mà dọn file nào khi đụng vào file đó. Danh sách trên co lại dần, và mỗi dòng biến mất là một bằng chứng tiến bộ.\n\n### Ruff so với chồng công cụ cũ\n\n```\nTRƯỚC (mỗi thứ một công cụ)      SAU (một công cụ)\nblack     định dạng               ruff format\nisort     sắp xếp import          ruff check --select I\nflake8    linter                  ruff check\npyupgrade cú pháp mới             ruff check --select UP\nbandit    bảo mật cơ bản          ruff check --select S\npylint    (một phần)              ruff check --select PL\n```\n\n```\nƯU: một công cụ, một cấu hình, nhanh hơn hàng chục–hàng trăm lần\n    (viết bằng Rust, chạy song song, có cache)\n    → đủ nhanh để chạy khi LƯU FILE mà không thấy độ trễ\n\nNHƯỢC: chưa phủ hết 100% quy tắc của pylint\n       phát triển nhanh ⇒ PHẢI ghim phiên bản\n       ít tuỳ biến sâu hơn pylint\n```\n\nTốc độ không chỉ là tiện: **công cụ chạy trong 50ms thì bật được khi lưu file; công cụ chạy 30 giây thì chỉ chạy trong CI.** Phản hồi tức thì thay đổi hẳn cách người ta dùng.\n\nKhi nào vẫn cần pylint: khi bạn cần các quy tắc phân tích sâu mà ruff chưa có, hoặc cần viết plugin kiểm tra riêng cho quy ước nội bộ.\n\n### Type checker — loại thứ ba\n\n```\nFORMATTER      hình thức\nLINTER         mẫu code đáng ngờ\nTYPE CHECKER   tính nhất quán của KIỂU DỮ LIỆU  ← khác hẳn\n```\n\nĐưa `mypy` vào dự án cũ cũng dùng chiến lược siết dần, nhưng cơ chế khác:\n\n```toml\n[tool.mypy]\npython_version = \"3.12\"\nignore_missing_imports = true      # thư viện chưa có type stub\n\n# Bắt đầu LỎNG\ncheck_untyped_defs = false\ndisallow_untyped_defs = false\n\n# Siết dần theo TỪNG MODULE\n[[tool.mypy.overrides]]\nmodule = \"du_an.dich_vu.*\"          # module mới, viết chuẩn từ đầu\ndisallow_untyped_defs = true\nstrict = true\n\n[[tool.mypy.overrides]]\nmodule = \"legacy.*\"                  # code cũ, tạm bỏ qua\nignore_errors = true\n```\n\nCách này cho phép **code mới nghiêm ngặt, code cũ được tha** — và ranh giới dịch chuyển dần. Nó thực tế hơn nhiều so với đòi hỏi gõ kiểu cho cả codebase trước khi bật `mypy`.\n\n### Đo tiến độ\n\n```bash\n# Đếm cảnh báo theo loại — biết nên sửa gì trước\nruff check --statistics .\n```\n\n```\n 142  E501    line-too-long\n  38  F401    unused-import        ← ruff --fix tự sửa hết\n  27  C901    complex-structure    ← cần refactor thật\n  12  B006    mutable-argument-default  ← BUG tiềm ẩn, ưu tiên cao\n```\n\nĐọc bảng này để xếp ưu tiên đúng:\n\n```\nF401 (38)  → ruff check --fix . xoá hết trong một giây\nE501 (142) → formatter lo, không cần đụng tay\nB006 (12)  → BUG THẬT, sửa trước dù số lượng ít nhất\nC901 (27)  → refactor, làm dần theo quy tắc hướng đạo sinh\n```\n\nSố lượng lớn không có nghĩa là ưu tiên cao. **12 cảnh báo `B006` đáng giá hơn 142 cảnh báo `E501`**, vì nhóm đầu là bug tiềm ẩn còn nhóm sau chỉ là hình thức mà formatter tự lo.",

vi_du: "### `pyproject.toml` hoàn chỉnh cho một dự án đội\n\n```toml\n[project]\nname = \"ban-hang\"\nrequires-python = \">=3.12\"\ndependencies = [\"fastapi\", \"sqlalchemy\"]\n\n[project.optional-dependencies]\ndev = [\n    \"ruff==0.6.9\",        # ⭐ GHIM CHÍNH XÁC — nếu không, máy bạn và CI\n    \"mypy==1.11.2\",       #    có thể cho kết quả khác nhau\n    \"pytest==8.3.3\",\n    \"pre-commit==3.8.0\",\n]\n\n# ============ RUFF ============\n[tool.ruff]\nline-length = 88\ntarget-version = \"py312\"\nextend-exclude = [\"migrations\", \"legacy_scripts\"]\n\n[tool.ruff.lint]\nselect = [\n    \"E\", \"W\",      # pycodestyle\n    \"F\",           # Pyflakes — LỖI THẬT\n    \"I\",           # isort\n    \"UP\",          # pyupgrade\n    \"B\",           # bugbear — BẪY phổ biến\n    \"SIM\",         # simplify\n    \"C90\",         # độ phức tạp        ← module 8, bài hàm nhỏ\n    \"PLR2004\",     # magic number       ← module 8, bài magic number\n    \"FBT\",         # cờ boolean         ← module 8, bài magic number\n    \"BLE\",         # except trần        ← module 8, bài xử lý lỗi\n    \"RUF100\",      # noqa thừa\n]\n\nignore = [\n    \"E501\",        # độ dài dòng — ruff format đã lo\n]\n\n[tool.ruff.lint.mccabe]\nmax-complexity = 10\n\n[tool.ruff.lint.pylint]\nmax-args = 5\n\n[tool.ruff.lint.per-file-ignores]\n\"__init__.py\" = [\"F401\"]                  # re-export là chủ ý\n\"tests/**\" = [\"S101\", \"PLR2004\", \"FBT\"]   # assert, magic number OK trong test\n\n# ---- NỢ KỸ THUẬT: xoá dần khi đụng tới các file này ----\n\"legacy/bao_cao.py\" = [\"C90\", \"PLR\"]\n\"legacy/dong_bo.py\" = [\"C90\"]\n\n# ============ MYPY ============\n[tool.mypy]\npython_version = \"3.12\"\nignore_missing_imports = true\nwarn_redundant_casts = true\nwarn_unused_ignores = true\n\n[[tool.mypy.overrides]]\nmodule = \"du_an.dich_vu.*\"     # code MỚI: nghiêm ngặt\nstrict = true\n\n[[tool.mypy.overrides]]\nmodule = \"legacy.*\"             # code CŨ: tạm tha\nignore_errors = true\n```\n\nBa khối đáng chú ý:\n\n```\n· dev deps ghim CHÍNH XÁC  → máy bạn và CI cho cùng kết quả\n· per-file-ignores phần \"NỢ KỸ THUẬT\" → danh sách co lại dần, đo được tiến bộ\n· mypy overrides           → code mới nghiêm, code cũ tha, ranh giới dịch dần\n```\n\n### Ba tầng chạy — cấu hình cụ thể\n\n```jsonc\n// ① TRÌNH SOẠN THẢO — .vscode/settings.json (commit)\n{\n  \"editor.formatOnSave\": true,\n  \"editor.codeActionsOnSave\": {\n    \"source.fixAll.ruff\": \"explicit\",\n    \"source.organizeImports.ruff\": \"explicit\"\n  },\n  \"[python]\": { \"editor.defaultFormatter\": \"charliermarsh.ruff\" }\n}\n```\n\n```yaml\n# ② PRE-COMMIT — .pre-commit-config.yaml\nrepos:\n  - repo: https://github.com/astral-sh/ruff-pre-commit\n    rev: v0.6.9              # ⭐ CÙNG phiên bản với pyproject.toml\n    hooks:\n      - id: ruff\n        args: [--fix]\n      - id: ruff-format\n```\n\n```yaml\n# ③ CI — .github/workflows/kiem-tra.yml\nname: Kiểm tra\non: [push, pull_request]\n\njobs:\n  kiem-tra:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n          cache: pip\n      - run: pip install -e \".[dev]\"\n      - run: ruff check --output-format=github .\n      - run: ruff format --check .\n      - run: mypy .\n      - run: pytest -q\n```\n\n```\nBa tầng, MỘT nguồn cấu hình (pyproject.toml), MỘT phiên bản công cụ.\nLệch bất kỳ chỗ nào ⇒ \"máy tôi xanh mà CI đỏ\" ⇒ mất niềm tin vào công cụ.\n```\n\n### Đưa vào dự án cũ — bốn giai đoạn thật\n\n```bash\n# ===== TRƯỚC KHI BẮT ĐẦU: đo xem đang ở đâu =====\n$ pip install ruff==0.6.9\n$ ruff check --statistics --select ALL .\n\n 2847  E501    line-too-long\n  312  D103    undocumented-public-function\n  198  ANN001  missing-type-function-argument\n  142  F401    unused-import\n   67  C901    complex-structure\n   23  B006    mutable-argument-default\n    8  BLE001  blind-except\n\n# → 3.597 cảnh báo. Bật hết là không ai sửa.\n```\n\n```bash\n# ===== GIAI ĐOẠN 1: format một lần, commit RIÊNG =====\n$ ruff format .\n217 files reformatted\n\n$ git add -A\n$ git commit -m \"style: format toàn bộ codebase bằng ruff format\"\n\n# Bảo vệ git blame\n$ git rev-parse HEAD >> .git-blame-ignore-revs\n$ git add .git-blame-ignore-revs\n$ git commit -m \"chore: bỏ qua commit format trong git blame\"\n$ git config blame.ignoreRevsFile .git-blame-ignore-revs\n\n# → 2.847 cảnh báo E501 biến mất. Còn 750.\n```\n\n```bash\n# ===== GIAI ĐOẠN 2: nhóm dễ và giá trị cao =====\n# select = [\"E\", \"F\", \"I\"]\n$ ruff check --fix .\nFound 142 errors (142 fixed, 0 remaining).\n\n# → import thừa và thứ tự import: ruff tự sửa HẾT trong một giây\n$ git commit -am \"style: xoá import thừa, sắp xếp import\"\n```\n\n```bash\n# ===== GIAI ĐOẠN 3: từng nhóm một, mỗi nhóm một PR =====\n# PR 1: thêm \"B\" — bugbear\n$ ruff check --select B .\nlegacy/xu_ly.py:45: B006 Do not use mutable data structures for argument defaults\n... 23 lỗi\n\n# ⚠️ B006 là BUG THẬT (bài module 7) — phải sửa TAY, không auto-fix được\n```\n\n```python\n# Sửa từng chỗ B006\n# ❌ Trước\ndef them_muc(muc, gio=[]):        # ⚠️ list dùng chung giữa mọi lần gọi\n    gio.append(muc)\n    return gio\n\n# ✓ Sau\ndef them_muc(muc: Muc, gio: list[Muc] | None = None) -> list[Muc]:\n    if gio is None:\n        gio = []\n    gio.append(muc)\n    return gio\n```\n\n```bash\n# ===== GIAI ĐOẠN 4: nhóm khó, tha file cũ =====\n# select += [\"C90\", \"PLR\"], nhưng:\n```\n\n```toml\n[tool.ruff.lint.per-file-ignores]\n# NỢ KỸ THUẬT — 67 hàm quá phức tạp, dọn dần theo quy tắc hướng đạo sinh\n\"legacy/bao_cao.py\" = [\"C90\", \"PLR\"]\n\"legacy/dong_bo.py\" = [\"C90\"]\n\"legacy/xu_ly.py\" = [\"C90\", \"PLR\"]\n```\n\n```\nKẾT QUẢ SAU BỐN GIAI ĐOẠN:\n· CI XANH ngay từ hôm nay\n· Code MỚI phải đạt chuẩn đầy đủ\n· Code CŨ được tha, nhưng danh sách tha CO LẠI mỗi lần đụng tới file\n· Không ai phải bỏ một tuần chỉ để dọn dẹp\n```\n\n### `# noqa` đúng và sai\n\n```python\n# ❌ noqa trần — tắt MỌI kiểm tra trên dòng này\nimport cau_hinh  # noqa\n# Sau này dòng này có lỗi thật cũng không ai biết\n\n# ❌ Có mã nhưng không có lý do\nimport cau_hinh  # noqa: F401\n# Người sau không dám xoá vì không biết vì sao\n\n# ✓ Có mã VÀ có lý do\nimport cau_hinh  # noqa: F401 — nạp để đăng ký các handler, không dùng trực tiếp\n```\n\n```python\n# ✓ Trường hợp noqa chính đáng khác\nfrom .mo_hinh import DonHang, Khach, SanPham  # noqa: F401 — re-export API công khai\n\ntry:\n    import orjson as json      # noqa: F811 — thay thế json chuẩn nếu có\nexcept ImportError:\n    import json\n```\n\nDọn `noqa` thừa tự động:\n\n```bash\n# RUF100 phát hiện noqa không còn cần thiết\n$ ruff check --select RUF100 .\nlegacy/cu.py:12: RUF100 Unused `noqa` directive (unused: `E501`)\n\n$ ruff check --select RUF100 --fix .\n# → xoá hết noqa thừa\n```\n\nĐây là giải pháp cho vấn đề rất thật: `noqa` tích tụ qua nhiều năm, không ai dám xoá vì không biết còn cần không. `RUF100` trả lời hộ bằng dữ liệu.\n\n### Xung đột giữa các công cụ\n\n```jsonc\n// ❌ Ba formatter cùng bật — file bị định dạng ba kiểu, đổi liên tục\n{\n  \"python.formatting.provider\": \"autopep8\",\n  \"[python]\": { \"editor.defaultFormatter\": \"ms-python.black-formatter\" },\n  \"editor.codeActionsOnSave\": { \"source.fixAll.ruff\": \"explicit\" }\n}\n```\n\n```jsonc\n// ✓ MỘT formatter duy nhất\n{\n  \"[python]\": { \"editor.defaultFormatter\": \"charliermarsh.ruff\" },\n  \"editor.formatOnSave\": true,\n  \"editor.codeActionsOnSave\": {\n    \"source.fixAll.ruff\": \"explicit\",\n    \"source.organizeImports.ruff\": \"explicit\"\n  }\n}\n```\n\n```jsonc\n// ✓ Và ngăn người khác cài nhầm — .vscode/extensions.json\n{\n  \"recommendations\": [\"charliermarsh.ruff\"],\n  \"unwantedRecommendations\": [\n    \"ms-python.black-formatter\",\n    \"ms-python.isort\",\n    \"ms-python.flake8\"\n  ]\n}\n```\n\nTriệu chứng của xung đột formatter rất dễ nhận ra khi biết: **lưu file hai lần cho ra hai kết quả khác nhau**, hoặc `git diff` hiện thay đổi ở những dòng bạn không hề chạm.\n\n### CI báo lỗi ngay trên pull request\n\n```yaml\n- name: Linter\n  run: ruff check --output-format=github .\n```\n\n```\nTrong giao diện pull request, lỗi hiện thành chú thích NGAY TRÊN DÒNG CODE:\n\n  dich_vu/don.py\n  ┌───────────────────────────────────────────────────────\n  │ 45  def xu_ly(don, co_thue=False, la_vip=False):\n  │     ⚠️ FBT001 Boolean-typed positional argument\n  │     ⚠️ FBT001 Boolean-typed positional argument\n```\n\nGiá trị: người review **không cần chạy gì** cũng thấy vấn đề, và tác giả biết chính xác dòng nào cần sửa. So với việc phải mở tab Actions, đọc log, rồi tự tìm dòng — khác biệt lớn về mức độ người ta thật sự đọc kết quả CI.\n\n### Kiểm tra ba tầng đồng bộ\n\n```bash\n# Chạy đúng những gì CI chạy, trên máy mình\n$ ruff check . && ruff format --check . && mypy . && pytest -q\n```\n\n```json\n// Đưa vào tasks.json để bấm một phím (bài VS Code)\n{\n  \"label\": \"Kiểm tra như CI\",\n  \"type\": \"shell\",\n  \"command\": \"ruff check . && ruff format --check . && mypy . && pytest -q\",\n  \"group\": \"test\"\n}\n```\n\nThói quen chạy lệnh này trước khi push tiết kiệm rất nhiều vòng \"push → chờ 3 phút → CI đỏ → sửa → push lại\".\n\nKhi CI đỏ mà máy xanh, kiểm ba thứ theo thứ tự:\n\n```\n① Phiên bản công cụ có khớp không?   pip show ruff  ↔  log CI\n② Phiên bản Python có khớp không?    python -V      ↔  setup-python\n③ CI có chạy trên file mà .gitignore của bạn đang bỏ qua không?\n```\n\nNguyên nhân ① chiếm đa số, và đó là lý do phải ghim phiên bản chính xác.",

so_sanh: "### Ba loại công cụ\n\n| | Formatter | Linter | Type checker |\n|---|---|---|---|\n| Việc | **viết lại** hình thức | **báo** vấn đề | kiểm **kiểu dữ liệu** |\n| Có đúng/sai | ✗ chỉ nhất quán | **✓ có** | **✓ có** |\n| Cần cấu hình nhiều | ✗ | **✓** | ✓ |\n| Đáng thảo luận trong đội | ✗ | **✓** | ✓ |\n| Ví dụ | `ruff format`, `black` | `ruff check`, `pylint` | `mypy`, `pyright` |\n\nFormatter không có đúng/sai nên **chạy tự động, không thảo luận**. Linter thì ngược lại.\n\n### Ba nơi chạy\n\n| | Trình soạn thảo | Pre-commit | CI |\n|---|---|---|---|\n| Thời điểm | khi **lưu** | khi **commit** | khi **push/PR** |\n| Tốc độ phản hồi | **tức thì** | vài giây | vài phút |\n| Bỏ qua được | ✓ (tắt extension) | ✓ (`--no-verify`) | **✗** |\n| Vai trò | sửa ngay khi gõ | chặn phần lớn | **nguồn sự thật** |\n\nBa tầng bổ sung nhau: nhanh-yếu → vừa → chậm-mạnh. **CI là nguồn sự thật duy nhất.**\n\n### Nhóm quy tắc ruff — ưu tiên bật\n\n| Nhóm | Bắt gì | Ưu tiên | Auto-fix |\n|---|---|---|---|\n| `F` | biến/import không dùng, tên chưa định nghĩa | **cao nhất** | phần lớn |\n| `B` | **bẫy thật**: mặc định mutable, except trần | **cao nhất** | ✗ phải sửa tay |\n| `E`, `W` | PEP 8 | cao | phần lớn |\n| `I` | thứ tự import | cao | **✓ hết** |\n| `UP` | cú pháp cũ | vừa | ✓ |\n| `SIM` | viết gọn hơn | vừa | phần lớn |\n| `C90`, `PLR` | độ phức tạp, số tham số | thấp (cần refactor) | ✗ |\n\nNếu chỉ bật được hai nhóm: **`F` và `B`** — chúng bắt lỗi thật, không phải hình thức.\n\n### Ba cấp bỏ qua quy tắc\n\n| Cấp | Cú pháp | Dùng khi |\n|---|---|---|\n| Một dòng | `# noqa: F401 — lý do` | ngoại lệ có chủ ý |\n| Một nhóm file | `per-file-ignores` | test, `__init__.py`, **nợ kỹ thuật** |\n| Cả dự án | `ignore = [...]` | quy tắc không phù hợp dự án |\n\n⚠️ `# noqa` trần tắt **mọi** kiểm tra trên dòng đó — luôn ghi mã và lý do. `RUF100` dọn `noqa` thừa.\n\n### Ruff và chồng công cụ cũ\n\n| | Chồng cũ | Ruff |\n|---|---|---|\n| Số công cụ | 5–6 | **1** |\n| Số file cấu hình | 3–4 | **1** (`pyproject.toml`) |\n| Tốc độ | 10–60 giây | **< 1 giây** |\n| Chạy được khi lưu file | ✗ | **✓** |\n| Phủ hết quy tắc pylint | ✓ | ✗ chưa |\n| Ổn định API | ✓ | **✗ phải ghim phiên bản** |\n\nTốc độ không chỉ là tiện: nó quyết định công cụ chạy được **khi lưu file** hay chỉ trong CI.\n\n### Đưa vào dự án cũ\n\n| | Bật hết một lượt | Siết dần |\n|---|---|---|\n| Số cảnh báo ban đầu | **hàng nghìn** | vài chục |\n| Ai sửa | **không ai** | sửa được ngay |\n| CI xanh được | sau nhiều tuần | **ngay hôm nay** |\n| Hậu quả | cả đội **phớt lờ công cụ** | tiến bộ đo được |\n\n### Xếp ưu tiên khi có nhiều cảnh báo\n\n| Loại | Số lượng | Ưu tiên | Vì sao |\n|---|---|---|---|\n| `B006` mặc định mutable | 12 | **cao nhất** | **bug thật** |\n| `BLE001` except trần | 8 | cao | nuốt lỗi |\n| `F401` import thừa | 142 | vừa | `--fix` xoá hết trong 1 giây |\n| `E501` dòng dài | 2.847 | thấp | formatter tự lo |\n\n**Số lượng lớn không có nghĩa ưu tiên cao.**",

loi_thuong_gap: [
  "**Để nhiều formatter cùng bật.** Lưu file hai lần ra hai kết quả; `git diff` hiện dòng bạn không chạm.",
  "**Không ghim chính xác phiên bản công cụ.** `ruff 0.5` và `0.6` cho kết quả khác nhau — \"máy tôi xanh mà CI đỏ\".",
  "**Chỉ chạy linter trên máy cá nhân.** Không có CI thì quy ước chỉ là đề nghị, không phải ràng buộc.",
  "**Để CI tự format rồi commit ngược.** CI sửa code là hành vi gây bất ngờ; dùng `--check` để chỉ kiểm.",
  "**Bật hết quy tắc một lượt trên dự án cũ.** Hàng nghìn cảnh báo, không ai sửa, cả đội học cách phớt lờ công cụ.",
  "**Format toàn bộ chung commit với thay đổi logic.** Người review không tách được thay đổi thật.",
  "**Không thêm commit format vào `.git-blame-ignore-revs`.** `git blame` chỉ ra người format, không ra người viết.",
  "**Dùng `# noqa` trần.** Tắt mọi kiểm tra trên dòng đó, kể cả lỗi thật xuất hiện sau này.",
  "**`# noqa` không ghi lý do.** Người sau không dám xoá vì không biết còn cần không.",
  "**Không bật `RUF100`.** `noqa` thừa tích tụ qua nhiều năm mà không ai dọn.",
  "**Xếp ưu tiên theo số lượng cảnh báo.** 12 cảnh báo `B006` đáng giá hơn 2.847 cảnh báo `E501`.",
  "**Cấu hình khác nhau giữa trình soạn thảo, hook, và CI.** Ba tầng phải đọc cùng một `pyproject.toml`.",
  "**Bật `mypy` nghiêm ngặt cho cả dự án cũ.** Dùng `overrides` để code mới nghiêm, code cũ tha.",
  "**Tranh cãi về `line-length`.** Con số nào cũng được — quan trọng là cả đội dùng một con số.",
  "**Lạm dụng `# fmt: off`.** Mỗi lần dùng là một chỗ formatter không bảo vệ được nữa.",
  "**Không chạy đúng lệnh CI trên máy trước khi push.** Tốn nhiều vòng push–chờ–đỏ–sửa."
],

tu_kiem_tra: [
  "Phân biệt formatter, linter, type checker. Vì sao formatter không cần thảo luận còn linter thì cần?",
  "Vì sao \"ít tuỳ chọn\" là tính năng của formatter chứ không phải hạn chế?",
  "Giá trị lớn nhất của formatter trong làm việc nhóm là gì? (Không phải \"code đẹp\".)",
  "Nêu ba nơi chạy linter, tốc độ và độ mạnh của mỗi nơi. Nơi nào là nguồn sự thật?",
  "Vì sao phải ghim chính xác phiên bản công cụ? Hậu quả nếu không?",
  "Nếu chỉ bật được hai nhóm quy tắc ruff, chọn nhóm nào và vì sao?",
  "Nhóm `B` (bugbear) bắt những bẫy nào đã học ở module 5 và 7?",
  "Nêu ba cấp bỏ qua quy tắc. Vì sao `# noqa` trần nguy hiểm?",
  "`RUF100` giải quyết vấn đề gì?",
  "Nêu bốn giai đoạn đưa linter vào dự án cũ. Vì sao không bật hết một lượt?",
  "`.git-blame-ignore-revs` dùng để làm gì? Cấu hình thế nào?",
  "`per-file-ignores` dùng làm danh sách nợ kỹ thuật thế nào? Liên hệ quy tắc hướng đạo sinh.",
  "Vì sao trong CI phải dùng `ruff format --check` chứ không phải `ruff format`?",
  "`--output-format=github` mang lại lợi ích gì cho người review?",
  "Ruff thay được những công cụ nào? Nêu ưu và nhược của việc gộp.",
  "Vì sao tốc độ của ruff không chỉ là chuyện tiện lợi?",
  "Đưa `mypy` vào dự án cũ dùng cơ chế gì để code mới nghiêm, code cũ tha?",
  "Có 2.847 `E501`, 142 `F401`, 12 `B006`. Xếp ưu tiên xử lý và giải thích.",
  "CI đỏ mà máy xanh — kiểm ba thứ nào theo thứ tự?"
],

tai_nguyen: [
  "**Tài liệu chính thức của Ruff** — danh sách đầy đủ các nhóm quy tắc, mã quy tắc, và giải thích vì sao mỗi quy tắc tồn tại. Đây là nguồn nên tra khi gặp mã lạ.",
  "**Tài liệu của Black** — mục *The Black Code Style* giải thích triết lý \"không thoả hiệp\" và lý do chọn 88 ký tự.",
  "**PEP 8** — quy ước gốc mà mọi công cụ đều dựa vào; đọc để hiểu vì sao có các quy tắc `E`/`W`.",
  "**Tài liệu mypy** — mục *Existing code* hướng dẫn cụ thể chiến lược đưa type checker vào dự án đã có.",
  "**Tài liệu GitHub Actions** — cú pháp workflow và cách cache dependencies; cần cho phần CI."
],

lien_he: [
  "**PEP 8 và format tự động** ở module 5: nền tảng; bài này bổ sung góc quy trình nhóm và CI.",
  "**Đọc và review code người khác** ở module 8: bảng cấu hình `ruff` gom mọi ngưỡng của module 8; tự động hoá để người review dành sức cho thiết kế.",
  "**Nhận diện code smell và refactor an toàn** ở module 8: `per-file-ignores` là danh sách nợ kỹ thuật, dọn theo quy tắc hướng đạo sinh.",
  "**Tránh magic number và cờ boolean** ở module 8: quy tắc `PLR2004` và `FBT` bắt tự động hai smell đó.",
  "**Xử lý lỗi tường minh** ở module 8: quy tắc `BLE` bắt `except` trần.",
  "**Hàm nhỏ làm đúng một việc** ở module 8: `C90` và `PLR` thực thi bốn thước đo ở bài đó.",
  "**Mutable vs immutable** ở module 7: `B006` bắt bẫy tham số mặc định mutable.",
  "**Type hint và mypy** ở module 5: type checker là loại công cụ thứ ba, siết dần bằng `overrides`.",
  "**VS Code** trong module này: `formatOnSave`, và `unwantedRecommendations` ngăn xung đột formatter.",
  "**Pre-commit hook** ngay sau: tầng thứ hai trong ba tầng, và cách viết hook riêng.",
  "**Git & GitHub** ở mảng 03: `.git-blame-ignore-revs`, pull request, và cấu hình CI đầy đủ.",
  "**Triển khai** ở mảng 05: CI/CD — kiểm tra chất lượng là bước đầu của một pipeline hoàn chỉnh."
]

};
