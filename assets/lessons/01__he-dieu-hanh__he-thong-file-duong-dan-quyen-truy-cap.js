window.LESSONS["01:he-dieu-hanh:he-thong-file-duong-dan-quyen-truy-cap"] = {

tom_tat: [
  "Tên file **không phải thuộc tính của file**. File được định danh bằng **inode**; tên chỉ là một mục trong thư mục trỏ tới inode đó.",
  "Vì vậy một file có thể có **nhiều tên** (hard link), và xoá tên không xoá file — chỉ giảm bộ đếm liên kết.",
  "Quyền Unix `rwx` cho ba nhóm: chủ sở hữu, nhóm, mọi người. `chmod 755` không phải con số tuỳ tiện — mỗi chữ số là **ba bit** cho ba quyền.",
  "Quyền `x` trên **thư mục** không có nghĩa là chạy, mà là **được phép đi vào**. Đây là hiểu lầm phổ biến nhất.",
  "Ghi file an toàn là **ghi ra file tạm rồi đổi tên**, vì đổi tên là thao tác nguyên tử còn ghi đè thì không."
],

can_biet_truoc: [
  "Bài **Bộ nhớ ảo** — cần biết đĩa chậm hơn RAM hàng nghìn lần.",
  "Bài **Process vs thread** — cần biết mỗi process có bảng file đang mở riêng.",
  "Biết dùng `cd`, `ls` ở mức cơ bản."
],

dinh_nghia: "Nói đời thường: hãy nghĩ tới **thư viện**. Cuốn sách thật nằm trên kệ, có một mã số riêng. **Danh mục** là một quyển sổ ghi \"tên sách này ứng với mã số kia\". Đổi tên trong danh mục không đụng gì tới cuốn sách. Ghi thêm một dòng nữa trong danh mục cùng trỏ tới mã đó là cuốn sách có hai tên.\n\nNói chính xác:\n\n- **Hệ thống file** là cách hệ điều hành tổ chức dữ liệu trên thiết bị lưu trữ thành file và thư mục, kèm siêu dữ liệu.\n- **Inode** là cấu trúc chứa **toàn bộ thông tin về một file trừ tên**: kích thước, quyền, chủ sở hữu, thời gian, và vị trí các khối dữ liệu trên đĩa. Mỗi file có đúng một inode, định danh bằng số.\n- **Thư mục** thực chất là một file đặc biệt, chứa danh sách các cặp **(tên, số inode)**.",

vi_sao_can: "Chủ đề này trông đơn giản nhưng là nguồn của nhiều sự cố thật:\n\n- **Xoá file mà dung lượng không giảm.** Vì còn process đang mở file đó — bộ đếm liên kết chưa về 0. Đây là sự cố vận hành rất hay gặp với file log.\n- **Bug \"permission denied\" trong Docker.** Container chạy user khác với chủ sở hữu file mount vào. Không hiểu quyền thì loay hoay hàng giờ.\n- **Mất dữ liệu khi ghi file.** Ghi đè trực tiếp mà chương trình chết giữa chừng là mất cả bản cũ lẫn bản mới.\n- **Lỗ hổng path traversal.** Người dùng gửi `../../etc/passwd` làm tên file. Đây là lỗ hổng web kinh điển, và phòng nó cần hiểu đường dẫn được phân giải ra sao.\n- **Đọc code triển khai.** `chmod 600` cho file khoá SSH, `chmod 755` cho thư mục — biết vì sao mới không copy-paste mù.\n\nTình huống thật: server hết dung lượng, bạn `rm` một file log 50 GB, `df` vẫn báo đầy. File đã biến mất khỏi `ls` nhưng dung lượng không được trả lại. Phần cơ chế giải thích chính xác vì sao, và cách xử lý đúng.",

co_che: "### Tên file và inode là hai thứ khác nhau\n\nĐây là ý quan trọng nhất của bài, và cũng là ý bị hiểu sai nhiều nhất.\n\n```\nThư mục /home/minh/          Bảng inode                 Đĩa\n┌──────────────────┐        ┌─────────────────┐       ┌──────────┐\n│ \"bao-cao.txt\" →  │───────►│ inode 12345     │──────►│ dữ liệu  │\n│      12345       │        │  kích thước: 2KB│       │ thật     │\n│                  │        │  quyền: rw-r--r-│       └──────────┘\n│ \"ban-sao.txt\" →  │───────►│  chủ: minh      │\n│      12345       │        │  liên kết: 2    │ ← hai tên cùng trỏ tới\n└──────────────────┘        └─────────────────┘\n```\n\nInode chứa **mọi thứ về file trừ tên**. Tên nằm trong thư mục. Hệ quả:\n\n- **Một file có thể có nhiều tên** (hard link), và các tên đó hoàn toàn bình đẳng — không có tên nào là \"tên gốc\".\n- **Đổi tên file rất nhanh** bất kể file lớn cỡ nào, vì chỉ sửa một mục trong thư mục.\n- **Xoá tên không xoá file.** Lệnh `rm` thực ra gọi `unlink()` — nó gỡ một tên và giảm bộ đếm liên kết. **Chỉ khi bộ đếm về 0 và không process nào đang mở** thì dữ liệu mới được giải phóng.\n\n### Vì sao xoá file mà dung lượng không giảm\n\nGiờ thì tình huống ở đầu bài đã rõ:\n\n```\nFile log 50 GB, đang được process nginx mở\n\nrm access.log\n   ↓\nTên bị gỡ khỏi thư mục     → ls không thấy nữa ✓\nBộ đếm liên kết: 1 → 0\nNHƯNG nginx vẫn giữ file đang mở  → dữ liệu KHÔNG được giải phóng\n   ↓\ndf vẫn báo đầy\n```\n\nHai cách xử lý:\n\n```bash\n# Cách 1: khởi động lại hoặc bảo process đóng file\nsystemctl reload nginx\n\n# Cách 2: cắt rỗng file thay vì xoá — không phá liên kết của process\n: > /var/log/nginx/access.log\n```\n\nCách 2 là mẹo đáng nhớ: `: >` ghi rỗng vào file, giải phóng dung lượng ngay mà process vẫn ghi tiếp bình thường.\n\nTìm file đã xoá mà chưa giải phóng:\n\n```bash\nlsof | grep deleted\n```\n\n### Hard link và symbolic link\n\n```\nHARD LINK                        SYMBOLIC LINK (symlink)\n────────────────────             ────────────────────────\n\"ten_a\" ──┐                      \"ten_a\" ──► inode 111 ──► dữ liệu\n          ├──► inode 12345\n\"ten_b\" ──┘                      \"loi_tat\" ──► inode 222\n                                              └─ nội dung: \"ten_a\"\n                                                 (một chuỗi đường dẫn)\n```\n\n| | Hard link | Symbolic link |\n|---|---|---|\n| Trỏ tới | Inode | **Một đường dẫn dạng chuỗi** |\n| Xoá bản gốc | Vẫn dùng được | **Hỏng** (dangling) |\n| Qua được ổ đĩa khác | Không | **Được** |\n| Trỏ tới thư mục | Không (trừ hệ thống) | **Được** |\n| Tạo bằng | `ln a b` | `ln -s a b` |\n\nSymlink là một **file riêng biệt có inode riêng**, nội dung của nó chỉ là một chuỗi đường dẫn. Vì vậy nó hỏng khi đích bị xoá — nó chỉ giữ tên, không giữ liên kết thật.\n\n### Quyền: ba nhóm, ba bit\n\n```\n-rwxr-xr--   1 minh  dev  4096  Aug 30 10:00  script.sh\n│└┬┘└┬┘└┬┘\n│ │  │  └── mọi người khác:  r--  = chỉ đọc\n│ │  └───── nhóm:            r-x  = đọc và chạy\n│ └──────── chủ sở hữu:      rwx  = đọc, ghi, chạy\n└────────── loại: - file thường, d thư mục, l symlink\n```\n\nMỗi nhóm ba bit, nên biểu diễn gọn bằng **một chữ số bát phân**:\n\n```\nr w x     giá trị\n4 2 1\n─────────────────\n1 1 1  =  7  (rwx)\n1 0 1  =  5  (r-x)\n1 0 0  =  4  (r--)\n1 1 0  =  6  (rw-)\n```\n\nGiờ `chmod 755` có nghĩa rõ ràng: `7` cho chủ (rwx), `5` cho nhóm (r-x), `5` cho mọi người (r-x). Ba chữ số không tuỳ tiện — chúng là **chín bit** viết gọn.\n\nĐây chính là lý do hệ bát phân sống sót tới ngày nay: một chữ số bát phân là đúng ba bit, khớp hoàn hảo với bộ ba `rwx`.\n\n### Bẫy lớn nhất: quyền `x` trên thư mục\n\nVới **file**, `x` nghĩa là được thực thi. Với **thư mục**, ba quyền mang ý nghĩa hoàn toàn khác:\n\n| Quyền | Trên file | **Trên thư mục** |\n|---|---|---|\n| `r` | đọc nội dung | **liệt kê được tên** trong thư mục |\n| `w` | sửa nội dung | **tạo, xoá, đổi tên** file bên trong |\n| `x` | chạy được | **đi vào được** (truy cập file bên trong) |\n\nHai hệ quả gây bất ngờ:\n\n**Thư mục có `r` mà không có `x`**: bạn thấy tên file nhưng không đọc được file nào, kể cả khi file đó cho phép đọc. Muốn chạm vào một file, bạn phải có quyền `x` trên **mọi thư mục cha** trên đường đi.\n\n**Thư mục có `w` nghĩa là xoá được file bên trong** — kể cả file bạn không sở hữu và không có quyền ghi. Quyền xoá thuộc về thư mục, không thuộc về file.\n\nĐiều thứ hai nguy hiểm, nên `/tmp` có thêm **sticky bit**:\n\n```bash\nls -ld /tmp\n# drwxrwxrwt   ← chữ t cuối là sticky bit\n```\n\nSticky bit nghĩa là: ai cũng tạo file được trong thư mục này, nhưng **chỉ chủ sở hữu file mới xoá được file đó**. Không có nó thì bất kỳ ai cũng xoá được file tạm của người khác.\n\n### Ghi file an toàn\n\nGhi đè trực tiếp là thao tác **không nguyên tử**:\n\n```\nmở file (nội dung cũ bị xoá)\n   ↓\n   ← chương trình chết ở đây: MẤT CẢ BẢN CŨ LẪN BẢN MỚI\n   ↓\nghi nội dung mới\n```\n\nMẫu an toàn được dùng khắp nơi trong phần mềm nghiêm túc:\n\n```\n① Ghi toàn bộ nội dung mới ra file TẠM cùng thư mục\n② fsync() — ép hệ điều hành đẩy dữ liệu xuống đĩa thật\n③ rename(tạm → đích)   ← thao tác NGUYÊN TỬ\n```\n\nBước ③ là mấu chốt: hệ thống file đảm bảo `rename` hoặc thành công hoàn toàn hoặc không xảy ra. Không có trạng thái nửa vời. Chương trình chết ở bất kỳ đâu thì trên đĩa vẫn còn **hoặc bản cũ nguyên vẹn, hoặc bản mới nguyên vẹn**.\n\nBước ② hay bị bỏ quên nhưng quan trọng: hệ điều hành đệm dữ liệu ghi trong RAM (**page cache**) và đẩy xuống đĩa sau. Mất điện giữa chừng thì file trông như đã đổi tên nhưng nội dung rỗng.\n\n### Đường dẫn tuyệt đối và tương đối\n\n```\n/home/minh/du-an/main.py     ← tuyệt đối, bắt đầu bằng /\ndu-an/main.py                ← tương đối, tính từ thư mục hiện tại\n../khac/main.py              ← .. là thư mục cha\n~/du-an/main.py              ← ~ là thư mục nhà, do SHELL bung ra\n```\n\nChi tiết đáng chú ý: **`~` do shell bung ra, không phải hệ điều hành**. Nên `open(\"~/file.txt\")` trong Python sẽ thất bại — cần `os.path.expanduser` trước.\n\nWindows dùng `\\` và có ký tự ổ đĩa, nên **luôn dùng `pathlib` thay vì nối chuỗi**:\n\n```python\nfrom pathlib import Path\nduong_dan = Path(\"du-an\") / \"data\" / \"file.csv\"   # đúng trên mọi hệ\n```\n\n### Path traversal: lỗ hổng bảo mật\n\n```python\n# ❌ NGUY HIỂM\nten = request.args[\"file\"]          # người dùng gửi: ../../etc/passwd\nnoi_dung = open(f\"/var/uploads/{ten}\").read()\n# → mở /etc/passwd\n```\n\nHệ điều hành phân giải `..` **trước khi** kiểm tra quyền, nên chuỗi đó thoát ra khỏi thư mục dự định.\n\n```python\n# ✓ AN TOÀN — phân giải rồi kiểm tra vẫn nằm trong thư mục cho phép\nfrom pathlib import Path\n\nGOC = Path(\"/var/uploads\").resolve()\nduong_dan = (GOC / ten).resolve()        # resolve() bung hết .. và symlink\n\nif not duong_dan.is_relative_to(GOC):    # Python 3.9+\n    raise ValueError(\"Đường dẫn không hợp lệ\")\n```\n\nMấu chốt là `.resolve()` — nó bung hết `..` và symlink **trước khi** so sánh. Kiểm tra chuỗi thô bằng cách tìm `\"..\"` là không đủ, vì còn nhiều cách mã hoá và symlink lách qua được.",

vi_du: "### Nhìn thấy inode\n\n```bash\nls -li                        # cột đầu là số inode\n# 12345 -rw-r--r-- 2 minh dev  1024 Aug 30 10:00 goc.txt\n#   │                └─ bộ đếm liên kết = 2\n#   └─ số inode\n\nln goc.txt cung-file.txt      # hard link\nls -li\n# 12345 -rw-r--r-- 2 minh dev 1024 ... goc.txt        ← CÙNG inode\n# 12345 -rw-r--r-- 2 minh dev 1024 ... cung-file.txt\n\nln -s goc.txt loi-tat.txt     # symlink\nls -li\n# 67890 lrwxrwxrwx 1 minh dev    7 ... loi-tat.txt -> goc.txt\n#   └─ inode KHÁC, vì symlink là file riêng\n```\n\nSố inode giống nhau là bằng chứng trực tiếp rằng hai tên trỏ tới **cùng một file**, không phải hai bản sao.\n\n### Xoá file mà dung lượng không giảm\n\n```bash\n# Tạo file 1 GB và giữ nó mở bằng một process\ndd if=/dev/zero of=/tmp/lon.bin bs=1M count=1024\ntail -f /tmp/lon.bin > /dev/null &\nPID=$!\n\ndf -h /tmp | tail -1          # dung lượng đã dùng\nrm /tmp/lon.bin               # xoá\nls /tmp/lon.bin               # không tồn tại ✓\ndf -h /tmp | tail -1          # NHƯNG dung lượng vẫn y nguyên!\n\nlsof | grep deleted           # thấy file đã xoá mà còn process giữ\n\nkill $PID                     # đóng process\ndf -h /tmp | tail -1          # giờ dung lượng mới được trả lại\n```\n\n### Quyền thư mục gây bất ngờ\n\n```bash\nmkdir thu_muc && echo \"bí mật\" > thu_muc/file.txt\n\n# Bỏ quyền x của thư mục, giữ nguyên r\nchmod 644 thu_muc             # rw-r--r--, KHÔNG có x\n\nls thu_muc                    # thấy tên file (nhờ quyền r)\ncat thu_muc/file.txt          # Permission denied (thiếu x để đi vào)\n\n# Ngược lại: có x, không có r\nchmod 711 thu_muc             # rwx--x--x\nls thu_muc                    # Permission denied — không liệt kê được\ncat thu_muc/file.txt          # ĐỌC ĐƯỢC nếu biết chính xác tên file!\n```\n\nTrường hợp thứ hai là một mẹo bảo mật có thật: thư mục `711` cho phép truy cập file nếu biết tên, nhưng không cho ai liệt kê xem có những file nào. Web server dùng cấu hình này để chặn duyệt thư mục.\n\n### Ghi file an toàn\n\n```python\nimport os, tempfile\nfrom pathlib import Path\n\ndef ghi_an_toan(duong_dan, noi_dung):\n    \"\"\"Ghi file kiểu hoặc-thành-công-hoàn-toàn-hoặc-không-gì-cả.\"\"\"\n    p = Path(duong_dan)\n\n    # File tạm phải CÙNG THƯ MỤC với đích, vì rename chỉ nguyên tử\n    # khi hai đầu nằm trên cùng một hệ thống file.\n    fd, tam = tempfile.mkstemp(dir=p.parent, prefix=\".tam-\")\n    try:\n        with os.fdopen(fd, \"w\", encoding=\"utf-8\") as f:\n            f.write(noi_dung)\n            f.flush()\n            os.fsync(f.fileno())      # ép xuống đĩa thật, không chỉ page cache\n        os.replace(tam, p)            # NGUYÊN TỬ, ghi đè nếu đích đã có\n    except Exception:\n        os.unlink(tam)                # dọn file tạm nếu hỏng\n        raise\n\nghi_an_toan(\"cau-hinh.json\", '{\"ok\": true}')\n```\n\nBa chi tiết đáng chú ý:\n\n- **File tạm cùng thư mục**: `rename` chỉ nguyên tử trong cùng một hệ thống file. Đặt file tạm ở `/tmp` rồi rename sang ổ khác thì thành sao chép, mất tính nguyên tử.\n- **`os.fsync`**: không có nó thì dữ liệu còn nằm trong page cache, mất điện là mất.\n- **`os.replace` chứ không phải `os.rename`**: `replace` ghi đè được trên cả Windows, `rename` thì không.\n\n### Chặn path traversal\n\n```python\nfrom pathlib import Path\n\nGOC = Path(\"/var/uploads\").resolve()\n\ndef mo_an_toan(ten_nguoi_dung_gui):\n    dich = (GOC / ten_nguoi_dung_gui).resolve()\n    if not dich.is_relative_to(GOC):\n        raise ValueError(f\"Đường dẫn ngoài phạm vi: {ten_nguoi_dung_gui}\")\n    return dich.read_text()\n\nmo_an_toan(\"anh.png\")              # OK\nmo_an_toan(\"../../etc/passwd\")     # ValueError ✓\nmo_an_toan(\"a/../../../etc/passwd\") # ValueError ✓ — resolve() bung hết\n```",

so_sanh: "### Hard link và symlink — chọn cái nào\n\nDùng **symlink** trong hầu hết trường hợp: nó qua được ổ đĩa, trỏ được tới thư mục, và nhìn `ls -l` là biết ngay nó trỏ đi đâu. Đây là lý do symlink phổ biến hơn hẳn.\n\nDùng **hard link** khi cần nhiều tên cho cùng một file mà không muốn phụ thuộc vào đường dẫn — ví dụ hệ thống sao lưu tăng dần: file không đổi thì hard link thay vì chép, tiết kiệm dung lượng mà mỗi bản sao lưu vẫn trông đầy đủ.\n\n### Vài hệ thống file phổ biến\n\n| | ext4 | NTFS | APFS | tmpfs |\n|---|---|---|---|---|\n| Dùng ở | Linux | Windows | macOS | RAM |\n| Journaling | Có | Có | Có (copy-on-write) | — |\n| Phân biệt hoa thường | **Có** | Không (mặc định) | Không (mặc định) | Có |\n| Đặc điểm | Ổn định, mặc định | Tương thích Windows | Snapshot nhanh | **Mất khi tắt máy** |\n\nDòng \"phân biệt hoa thường\" gây ra một lớp bug rất thật: code chạy tốt trên macOS rồi hỏng khi deploy lên Linux, vì `import Utils` và file `utils.py`. Trên macOS chúng là một, trên Linux là hai.\n\n**Quy tắc phòng ngừa: luôn dùng tên file chữ thường, và tên trong code phải khớp chính xác với tên trên đĩa.**\n\n`tmpfs` đáng chú ý: nó là hệ thống file nằm hoàn toàn trong RAM. `/dev/shm` trên Linux chính là tmpfs — dùng cho dữ liệu tạm cần tốc độ cao, và tự biến mất khi khởi động lại.\n\n### Journaling: vì sao máy không hỏng khi mất điện\n\nHệ thống file hiện đại ghi **nhật ký (journal)** trước khi thay đổi thật:\n\n```\n① Ghi vào journal: \"sắp làm X\"\n② Thực hiện X\n③ Xoá mục trong journal\n```\n\nMất điện giữa chừng thì lúc khởi động lại, hệ thống đọc journal và **hoàn tất hoặc huỷ bỏ** các thao tác dở dang. Nhờ vậy cấu trúc hệ thống file luôn nhất quán.\n\nLưu ý quan trọng: journaling bảo vệ **siêu dữ liệu** (cấu trúc thư mục, inode), không nhất thiết bảo vệ **nội dung file** của bạn. Đó là lý do vẫn cần mẫu ghi-tạm-rồi-đổi-tên ở tầng ứng dụng.\n\nĐây cũng chính là ý tưởng **write-ahead log** mà database dùng — bạn sẽ gặp lại ở mảng 04.",

loi_thuong_gap: [
  "**Tưởng `rm` xoá dữ liệu ngay.** Nó chỉ gỡ một tên và giảm bộ đếm liên kết. Còn process giữ file thì dung lượng chưa được trả lại. Dùng `lsof | grep deleted` để tìm.",
  "**Tưởng quyền `x` trên thư mục nghĩa là chạy.** Nó nghĩa là **đi vào được**. Thiếu `x` trên bất kỳ thư mục cha nào là không chạm được file bên trong.",
  "**Tưởng cần quyền ghi trên file mới xoá được nó.** Quyền xoá thuộc về **thư mục chứa**, không thuộc về file. Đây là lý do `/tmp` cần sticky bit.",
  "**Ghi đè file trực tiếp.** Chương trình chết giữa chừng là mất cả bản cũ lẫn bản mới. Ghi ra file tạm cùng thư mục rồi `os.replace`.",
  "**Quên `fsync` khi cần đảm bảo dữ liệu xuống đĩa.** Hệ điều hành đệm trong page cache; mất điện là mất dữ liệu dù đã `close()`.",
  "**Nối đường dẫn bằng chuỗi.** `\"a\" + \"/\" + \"b\"` hỏng trên Windows. Dùng `pathlib.Path`.",
  "**Kiểm tra path traversal bằng cách tìm chuỗi `\"..\"`.** Không đủ — symlink và nhiều cách mã hoá lách qua được. Phải `resolve()` rồi mới so sánh.",
  "**Đặt tên file có chữ hoa.** Chạy tốt trên macOS, hỏng trên Linux. Dùng chữ thường toàn bộ.",
  "**`chmod 777` để \"cho nhanh\".** Nó cho phép mọi người ghi đè, kể cả tiến trình độc. Với file khoá SSH thì SSH còn từ chối dùng."
],

tu_kiem_tra: [
  "Tên file được lưu ở đâu — trong inode hay trong thư mục? Hệ quả của câu trả lời đó là gì?",
  "Bạn `rm` một file log 50 GB nhưng `df` vẫn báo đầy. Chuyện gì đã xảy ra và xử lý thế nào?",
  "Vì sao hard link không qua được ổ đĩa khác còn symlink thì được?",
  "`chmod 755` nghĩa là gì? Giải thích bằng bit chứ không đọc thuộc.",
  "Thư mục có quyền `r` nhưng không có `x` thì bạn làm được gì và không làm được gì?",
  "Vì sao `/tmp` cần sticky bit? Không có nó thì chuyện gì xảy ra?",
  "Mô tả ba bước ghi file an toàn và giải thích vì sao bước cuối phải là `rename`.",
  "Người dùng gửi `../../etc/passwd` làm tên file. Vì sao kiểm tra chuỗi `\"..\"` là không đủ?"
],

tai_nguyen: [
  "Sách **Operating Systems: Three Easy Pieces**, phần *Persistence* chương 39–42 về hệ thống file và inode.",
  "Tài liệu Linux: `man 7 inode`, `man 2 unlink`, `man 2 rename` — đọc mục *NOTES* của `rename` để hiểu bảo đảm nguyên tử.",
  "Tài liệu Python, module `pathlib` và `os` — đặc biệt `os.replace`, `os.fsync`, `Path.resolve`.",
  "**OWASP Path Traversal** — trang mô tả lỗ hổng và các biến thể mã hoá dùng để lách qua kiểm tra chuỗi."
],

lien_he: [
  "**Bộ nhớ ảo**: page cache là chỗ hệ điều hành đệm dữ liệu file trong RAM — cùng cơ chế phân trang đã học.",
  "**Process vs thread**: mỗi process có bảng file đang mở riêng, và đó là lý do file bị xoá vẫn sống khi còn process giữ.",
  "**Deadlock & race condition**: `rename` nguyên tử chính là cách giải race condition ở tầng hệ thống file, cùng ý tưởng compare-and-swap.",
  "**Command line** (module sau): `chmod`, `ls -l`, `ln`, `lsof` là những lệnh bạn sẽ dùng hằng ngày.",
  "**Bảo mật** ở mảng 05: path traversal là một trong những lỗ hổng web phổ biến nhất, và phòng nó cần đúng kiến thức trong bài này.",
  "**Triển khai** ở mảng 05: lỗi permission denied khi mount volume vào Docker là do user id trong container khác với chủ sở hữu file trên máy chủ.",
  "**Transaction** ở mảng 04: write-ahead log của database là chính ý tưởng journaling — ghi ý định trước, thực hiện sau."
]

};
