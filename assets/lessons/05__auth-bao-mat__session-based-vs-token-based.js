window.LESSONS["05:auth-bao-mat:session-based-vs-token-based"] = {

tom_tat: [
  "Session-based lưu trạng thái đăng nhập ở server và gửi cho browser một mã tra cứu ngẫu nhiên; token-based gửi một credential mà API có thể tự kiểm chứng hoặc introspect, nên khác biệt cốt lõi là nơi đặt state và cách thu hồi.",
  "Cookie không đồng nghĩa session và JWT không đồng nghĩa token-based: cookie là cơ chế vận chuyển/lưu phía browser, còn session ID hoặc JWT đều có thể nằm trong cookie.",
  "StudyHub chọn session cookie cho web cùng origin vì logout tức thời và CSRF có biện pháp rõ; API mobile có access token ngắn hạn nhưng vẫn cần state cho refresh token.",
  "“Stateless” chỉ giảm lookup trên từng request; refresh, revoke, đổi role, phát hiện reuse và khóa ký vẫn đưa trạng thái trở lại hệ thống.",
  "Quyết định đúng phụ thuộc client, trust boundary, yêu cầu logout, topology và năng lực vận hành, không phụ thuộc trào lưu."
],

can_biet_truoc: [
  "Authentication, authorization, principal và credential từ bài trước.",
  "HTTP cookie attributes `HttpOnly`, `Secure`, `SameSite`, domain và path.",
  "Database/key-value store, TTL và cache invalidation ở mức cơ bản.",
  "Same-origin policy, CORS và khái niệm CSRF."
],

dinh_nghia: "Trong **session-based authentication**, server tạo một bản ghi phiên chứa hoặc trỏ tới trạng thái đăng nhập, còn client giữ session ID ngẫu nhiên không mang nghĩa. Mỗi request, server dùng ID để tra phiên rồi dựng principal.\n\nTrong **token-based authentication**, client giữ token đại diện cho một grant. Token có thể tự chứa claim và được kiểm chứng cục bộ, như JWT đã ký, hoặc là opaque token phải hỏi authorization server. Vì opaque token cũng cần lookup và session ID cũng là token theo nghĩa rộng, phép so sánh hữu ích phải nói rõ loại token, cách vận chuyển và nơi lưu trạng thái.",

vi_sao_can: "StudyHub phục vụ browser, mobile app và có thể tách dịch vụ video. Nếu áp một khẩu hiệu “JWT scale tốt hơn”, đội ngũ có thể đánh mất logout tức thời, nhét token vào `localStorage`, hoặc để role cũ sống đến hết hạn. Nếu dùng session mà không hiểu topology, một instance giữ RAM riêng sẽ làm request qua load balancer lúc được lúc mất.\n\nThiết kế phiên quyết định blast radius khi credential bị trộm, số lần gọi datastore, hành vi lúc Redis hỏng, cách vô hiệu hóa sau đổi mật khẩu và loại tấn công phía browser. Đây là lựa chọn kiến trúc có đánh đổi security–availability–complexity; nó không chỉ là thay một header.",

co_che: "### Một phiên đăng nhập được sinh ra thế nào\n\nSau khi mật khẩu được verify, server tạo session ID bằng nguồn ngẫu nhiên mật mã, đủ entropy để không đoán được. Bản ghi server chứa `user_id`, thời điểm tạo, thời điểm hoạt động, hạn tuyệt đối, version và dữ liệu tối thiểu. Response đặt ID trong cookie. Request sau chỉ mang khóa tra cứu; quyền vẫn phải tính từ dữ liệu đáng tin.\n\nSession ID không nên chứa user ID tuần tự hay timestamp có thể dự đoán. Hash session ID trước khi lưu giúp bản sao datastore không lập tức trở thành kho credential có thể phát lại. So sánh này giống reset token: server nhận giá trị thô, hash để lookup, nhưng không log nó.\n\n### Cookie là phong bì, không phải nội dung\n\n`HttpOnly` ngăn JavaScript đọc cookie, giảm khả năng token bị lấy trực tiếp qua XSS nhưng không ngăn script độc hại gửi request bằng cookie. `Secure` chỉ gửi qua HTTPS. `SameSite` hạn chế cookie trong request cross-site và hỗ trợ chống CSRF. `Path`/`Domain` càng rộng, càng nhiều endpoint/subdomain nhận credential.\n\nCookie có thể chứa session ID hoặc JWT; bearer token cũng có thể được đặt trong cookie. Vì browser tự đính cookie, ứng dụng phải xét CSRF. Token trong header do JavaScript tự gắn thường tránh CSRF cổ điển nhưng nếu lưu ở nơi JavaScript đọc được, XSS có thể đánh cắp và gửi token ra ngoài. Không có vị trí lưu nào biến XSS thành vô hại.\n\n### Tra cứu session qua nhiều instance\n\nNếu mỗi worker giữ session trong RAM, request kế tiếp đến worker khác sẽ không tìm thấy. Sticky session giảm triệu chứng nhưng làm cân bằng tải và failover kém. Shared store như Redis cho mọi instance cùng thấy state, đổi lại mỗi request có network dependency và datastore trở thành critical path.\n\nDatabase quan hệ có thể đủ cho tải nhỏ và đơn giản vận hành. Redis phù hợp TTL/lookup nhanh nhưng cần chính sách persistence, eviction và failover. Nếu eviction xóa session, user bị logout; nếu fail-open khi store lỗi, request không có bằng chứng lại được chấp nhận—đó là lựa chọn sai. Authentication nên fail closed, đồng thời hệ thống trả 503 có kiểm soát thay vì giả 401 hàng loạt.\n\n### Fixation và rotation\n\nSession fixation xảy ra khi attacker biết trước một session ID rồi khiến nạn nhân đăng nhập trên chính ID ấy. Phòng vệ là tạo ID mới sau mọi thay đổi mức tin cậy: login, MFA hoàn tất, nâng privilege. Không “nâng cấp” anonymous session ID thành authenticated session. Dữ liệu giỏ hàng có thể copy sang phiên mới có chọn lọc.\n\nRotate cũng hữu ích định kỳ nhưng phải xử lý request song song. Có thể cho ID cũ grace period rất ngắn hoặc dùng mapping chuyển tiếp một lần; nếu vô hiệu ngay, hai tab gửi gần nhau tạo lỗi khó hiểu. Grace quá dài lại mở cửa replay. Đây là state machine cần test, không phải gọi `uuid` một lần.\n\n### Ba chiếc đồng hồ của session\n\nIdle timeout hết hạn sau thời gian không hoạt động; absolute timeout giới hạn tổng đời phiên dù user liên tục dùng; renewal timeout yêu cầu đổi ID. Chỉ có idle timeout cho phép bot giữ phiên mãi. Chỉ có absolute timeout có thể làm user rời máy nhưng credential vẫn sống lâu.\n\nServer là nguồn thời gian quyết định. “Remember me” nên là grant riêng với rủi ro rõ, không đơn giản kéo session admin thành nhiều tháng. Hành động nhạy cảm như đổi email có thể yêu cầu recent authentication dù session còn hạn.\n\n### CSRF trong web StudyHub\n\nKhi browser tự gửi cookie tới StudyHub, trang độc hại có thể dụ browser POST. `SameSite` là lớp tốt nhưng không nên là lớp duy nhất trong mọi topology. CSRF token ràng buộc request với phiên và được gửi ở vị trí attacker cross-site không đọc được; server so sánh trước mutation. GET không được thay đổi state.\n\nCORS không phải CSRF protection tổng quát: form HTML có thể gửi một số request mà không cần đọc response. XSS cùng origin có thể vượt nhiều biện pháp CSRF, nên output encoding và CSP vẫn quan trọng.\n\n### Access token tự chứa chuyển chi phí đi đâu\n\nAPI nhận JWT có thể kiểm chữ ký, issuer, audience và hạn mà không gọi store. Điều này giảm lookup và giúp service hoạt động khi authorization server tạm chậm. Nhưng claim là snapshot: role bị hạ hoặc tài khoản khóa không tự biến mất khỏi token đã phát.\n\nGiải pháp là hạn access token ngắn, kiểm tra version/denylist cho action nhạy cảm hoặc introspection. Mỗi giải pháp đưa lookup/state hoặc cửa sổ rủi ro trở lại. Token lớn cũng tăng bytes trên mọi request; không đặt PII hay permission list khổng lồ chỉ vì payload tiện.\n\n### Opaque token không phải session cookie đổi tên vô ích\n\nOpaque access token có thể được resource server introspect với authorization server. Nó cho phép thu hồi tập trung và giấu claim khỏi client, nhưng thêm round-trip và dependency. Cache introspection cải thiện availability nhưng tạo cửa sổ stale. Session thường gắn với tương tác user/browser; opaque access token thường mô hình hóa grant giữa client và API, dù cơ chế lookup giống nhau.\n\nTên gọi không quyết định security. Threat model, audience, sender constraint, TTL và storage mới quyết định credential có bị phát lại ở đâu.\n\n### Logout và thu hồi\n\nSession logout xóa bản ghi server, nên request sau với ID cũ thất bại. Cần xóa cookie client, nhưng xóa client thôi không đủ. “Logout all devices” xóa mọi session của user hoặc tăng `session_version`.\n\nJWT access token tự chứa thường còn dùng được đến `exp`. Denylist theo `jti` cho logout tức thì nhưng cần lookup và dọn TTL. Thay khóa ký để logout một user là quá rộng. Refresh token stateful có thể bị thu hồi để ngăn cấp access token mới, nhưng access token hiện tại vẫn có cửa sổ sống.\n\n### Đổi mật khẩu và đổi quyền\n\nStudyHub lưu `credential_version` hoặc `permissions_version`. Session record mang version lúc tạo; endpoint so với user record theo mức nhạy cảm. Tăng version vô hiệu phiên cũ. Nếu lookup user mỗi request thì mất phần lợi ích cache; có thể chỉ áp ở admin endpoint hoặc dùng event invalidation.\n\nĐổi password không nhất thiết luôn logout thiết bị hiện tại, nhưng phải là policy có chủ ý. Khi nghi compromise, nên revoke mọi phiên và refresh grant. Thay role cần cân nhắc mức tức thời: hạ admin đòi hỏi nhanh hơn thay nhãn giao diện.\n\n### Availability và failure mode\n\nSession store timeout không nên bị diễn giải là “session không tồn tại”; nếu vậy outage biến thành logout hàng loạt và client spam login, làm tải nặng hơn. Phân biệt miss với dependency error, dùng circuit breaker/throttling và trả 503. Token verify cục bộ tránh store outage nhưng vẫn phụ thuộc key distribution và clock.\n\nKey cache quá cũ có thể không nhận khóa mới; clock skew khiến node này chấp nhận, node kia từ chối. Session replication lag có thể làm logout chưa thấy ở replica. Mỗi mô hình có state, chỉ khác state nằm trên đường request hay control plane.\n\n### Ca biên đa thiết bị và request đồng thời\n\nHai lần login tạo hai session riêng giúp user thu hồi từng thiết bị. Không dùng một session ID cố định theo user. Retry login sau timeout không nên tạo vô hạn record; cleanup theo TTL và giới hạn số phiên. Hai request logout/login đan xen cần định nghĩa version nào thắng.\n\nCookie bị thiếu trả 401; cookie malformed không được đưa thẳng vào query gây lỗi. Store trả bản ghi hết hạn phải xóa/từ chối. User bị xóa nhưng session còn tồn tại phải không dựng được principal. WebSocket dài hạn cần quy tắc re-auth hoặc đóng khi hết hạn; kiểm một lần lúc handshake có thể giữ quyền quá lâu.\n\n### Chọn mô hình vừa đủ\n\nMonolith web cùng origin thường hưởng lợi từ session: logout rõ, cookie bảo vệ tốt, logic ít. Mobile/API đa dịch vụ có thể dùng access token ngắn hạn để giảm coupling. Kiến trúc hybrid không phải thất bại: browser nói chuyện với backend-for-frontend bằng session cookie, BFF đổi sang service token nội bộ.\n\nKhông nên dùng JWT khi chỉ có một server và yêu cầu revoke tức thời nhưng không có lý do khác; bạn nhận complexity mà vẫn dựng denylist. Không nên dùng session RAM cục bộ khi autoscaling nhiều instance. Chọn theo failure budget và đội vận hành, không theo chữ “modern”.",

vi_du: "### Session cookie tối giản trong FastAPI\n\nDemo chạy được, dùng memory store nên chỉ dành cho một process. Production thay store và hash ID trước khi lưu.\n\n```python\nimport secrets, time\nfrom fastapi import Cookie, FastAPI, HTTPException, Response\nfrom pydantic import BaseModel\n\napp = FastAPI()\nSESSIONS: dict[str, dict] = {}\nUSERS = {\"alice@example.com\": {\"id\": 1, \"password\": \"demo-only\"}}\n\nclass Login(BaseModel):\n    email: str\n    password: str\n\n@app.post(\"/login\")\ndef login(data: Login, response: Response):\n    user = USERS.get(data.email)\n    if not user or data.password != user[\"password\"]:\n        raise HTTPException(401, \"Sai thông tin đăng nhập\")\n    sid = secrets.token_urlsafe(32)\n    SESSIONS[sid] = {\"user_id\": user[\"id\"], \"expires\": time.time() + 1800}\n    response.set_cookie(\"sid\", sid, httponly=True, secure=True,\n                        samesite=\"lax\", max_age=1800)\n    return {\"ok\": True}\n\n@app.get(\"/me\")\ndef me(sid: str | None = Cookie(default=None)):\n    session = SESSIONS.get(sid or \"\")\n    if not session or session[\"expires\"] <= time.time():\n        if sid: SESSIONS.pop(sid, None)\n        raise HTTPException(401, \"Phiên không hợp lệ\")\n    return {\"user_id\": session[\"user_id\"]}\n\n@app.post(\"/logout\", status_code=204)\ndef logout(response: Response, sid: str | None = Cookie(default=None)):\n    if sid: SESSIONS.pop(sid, None)\n    response.delete_cookie(\"sid\")\n```\n\nPassword plaintext trong dictionary cố ý là giả lập, không phải mẫu lưu trữ; ghép bài trước phải thay bằng Argon2 verify. Test quan trọng là ID đổi sau login, logout làm ID cũ vô hiệu, store timeout trả 503 chứ không 401, và hai user không bao giờ nhận cùng ID.",

so_sanh: "### Bảng quyết định thực dụng\n\n| Trục | Session server-side | JWT access token tự chứa | Opaque access token |\n|---|---|---|---|\n| Request cần lookup | thường có | không bắt buộc | introspection hoặc cache |\n| Thu hồi tức thời | xóa record | khó nếu không thêm state | thuận lợi ở authorization server |\n| Claim bị cũ | nạp lại được | tồn tại đến hết hạn | phụ thuộc introspection cache |\n| Failure chính | store/network | key/clock/validation | authorization server/network |\n| Kích thước credential | nhỏ | có thể lớn | nhỏ |\n| Hợp với | web monolith | API phân tán, TTL ngắn | kiểm soát tập trung |\n\n### Cookie và header là trục độc lập\n\n| Nơi mang credential | Ưu điểm | Rủi ro cần xử lý |\n|---|---|---|\n| HttpOnly cookie | JavaScript không đọc trực tiếp | browser tự gửi, cần CSRF design |\n| Authorization header | rõ cho API/non-browser | nơi client lưu token, XSS/exfiltration |\n\nKhông thể kết luận “token chống CSRF” nếu token lại đặt trong cookie, cũng không thể kết luận “cookie kém an toàn” khi so với bearer token nằm trong `localStorage`. Phải mô tả toàn chuỗi lưu–gửi–verify–revoke.",

loi_thuong_gap: [
  "Gọi JWT là stateless rồi thêm denylist, refresh store và lookup role nhưng không capacity-plan các state ấy.",
  "Lưu bearer token dài hạn trong `localStorage`; nhận biết khi bất kỳ XSS nào đọc và gửi token ra domain khác được.",
  "Dùng cookie nhưng không CSRF protection cho mutation; nhận biết bằng form từ origin khác vẫn đổi state.",
  "Giữ session ID sau login; nhận biết khi ID trước và sau authentication giống nhau, mở session fixation.",
  "Lưu phiên trong RAM khi chạy nhiều worker; nhận biết lỗi đăng nhập chập chờn theo instance.",
  "Biến store outage thành 401; nhận biết đột biến logout/login trùng với Redis timeout.",
  "Đặt quyền dài hạn vào token rồi kỳ vọng đổi role có hiệu lực ngay; nhận biết user bị hạ quyền vẫn gọi API đến `exp`."
],

tu_kiem_tra: [
  "Vì sao “cookie vs JWT” là một phép so sánh sai trục? Hãy nêu hai trục độc lập.",
  "Session store timeout nên khác session miss thế nào ở status, metric và hành vi client?",
  "Thiết kế logout-all-devices cho session và cho access/refresh token; state nằm ở đâu?",
  "Nếu role admin bị hạ ngay lập tức, token tự chứa còn 10 phút tạo cửa sổ gì và có ba cách giảm nào?",
  "Tại sao `HttpOnly` giảm trộm token nhưng không khiến XSS hoặc CSRF biến mất?",
  "WebSocket sống hai giờ nên xử lý session hết hạn sau 30 phút ra sao?"
],

tai_nguyen: [
  "OWASP Session Management Cheat Sheet.",
  "OWASP Cross-Site Request Forgery Prevention Cheat Sheet.",
  "Web Security for Developers — Malcolm McDonald.",
  "FastAPI Security documentation."
],

lien_he: [
  "Nối sang mảng Distributed Systems qua shared store, replication lag, failover và cache invalidation.",
  "Nối sang mảng Browser/Web qua same-origin policy, cookie attributes, XSS và CSRF.",
  "Nối sang mảng System Design qua BFF, load balancer, sticky session và failure budget.",
  "Nối sang mảng Database qua TTL cleanup, uniqueness, hash session ID và transaction thu hồi."
]

};
