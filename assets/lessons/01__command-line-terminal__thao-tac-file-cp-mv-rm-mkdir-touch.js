window.LESSONS["01:command-line-terminal:thao-tac-file-cp-mv-rm-mkdir-touch"] = {

tom_tat: [
  "**Terminal không có thùng rác.** `rm` xoá vĩnh viễn, không hỏi lại, không hoàn tác được. Đây là khác biệt lớn nhất so với giao diện đồ hoạ.",
  "`mv` vừa là **di chuyển** vừa là **đổi tên** — trong cùng một ổ đĩa, cả hai chỉ là sửa một mục trong thư mục nên nhanh tức thì.",
  "**Glob `*` do shell bung ra**, không phải do lệnh xử lý. Hiểu điều này giải thích được nhiều hành vi bất ngờ.",
  "`rm -rf /` là lệnh phá huỷ kinh điển. Thói quen an toàn: **luôn `ls` trước khi `rm`** với cùng mẫu glob đó.",
  "`mkdir -p` tạo cả cây thư mục và **không báo lỗi nếu đã tồn tại** — nên nó là lựa chọn mặc định trong script."
],

can_biet_truoc: [
  "Bài **Điều hướng: cd, ls, pwd, tree** — cần biết di chuyển và đường dẫn tương đối.",
  "Bài **Hệ thống file, đường dẫn, quyền truy cập** — cần hiểu inode và vì sao `rm` chỉ gỡ tên."
],

dinh_nghia: "Nói đời thường: đây là bộ công cụ tạo, chép, dời và vứt đồ. Khác với File Explorer ở một điểm sống còn: **không có thùng rác và không có hộp thoại \"bạn có chắc không\"**. Lệnh chạy là xong.\n\nNói chính xác, năm lệnh nền tảng:\n\n- **`mkdir`** — tạo thư mục\n- **`touch`** — tạo file rỗng, hoặc cập nhật thời gian sửa nếu file đã có\n- **`cp`** — sao chép, tạo ra **inode mới** với nội dung giống hệt\n- **`mv`** — di chuyển hoặc đổi tên, **giữ nguyên inode**\n- **`rm`** — gỡ tên khỏi thư mục (gọi `unlink`), dữ liệu mất khi bộ đếm liên kết về 0",

vi_sao_can: "Đây là nhóm lệnh **nguy hiểm nhất** bạn sẽ dùng hằng ngày:\n\n- **Xoá nhầm là mất thật.** Không có Ctrl+Z, không có thùng rác. Trên server production, một lệnh `rm -rf` sai chỗ là sự cố nghiêm trọng.\n- **Script triển khai đầy các lệnh này.** Mọi Dockerfile, CI pipeline, script deploy đều `mkdir`, `cp`, `rm`. Hiểu sai một cờ là hỏng cả quy trình.\n- **Thao tác hàng loạt.** Đổi tên 200 file theo mẫu bằng chuột là bất khả thi; bằng terminal là một dòng.\n- **Hiểu vì sao đôi khi chậm đôi khi nhanh.** `mv` file 10 GB trong cùng ổ đĩa mất 0,01 giây; sang ổ khác mất vài phút. Biết vì sao là dự đoán được.\n\nTình huống thật: bạn viết script dọn file tạm với `rm -rf $THU_MUC/*`. Biến `$THU_MUC` chưa được gán vì gõ sai tên, nên lệnh thành `rm -rf /*`. Đây là cách nhiều hệ thống thật đã bị xoá sạch. Phần cơ chế sẽ nói cách phòng.",

co_che: "### `mkdir` — tạo thư mục\n\n```bash\nmkdir du-an                    # tạo một thư mục\nmkdir a b c                    # tạo nhiều cùng lúc\n\nmkdir a/b/c                    # ✗ lỗi nếu a hoặc a/b chưa tồn tại\nmkdir -p a/b/c                 # ✓ tạo cả cây, thiếu cái nào tạo cái đó\n\nmkdir du-an                    # ✗ lỗi: File exists\nmkdir -p du-an                 # ✓ im lặng, không báo lỗi\n```\n\nCờ `-p` (*parents*) làm hai việc và cả hai đều quan trọng trong script:\n\n1. Tạo mọi thư mục cha còn thiếu\n2. **Không báo lỗi nếu thư mục đã tồn tại**\n\nĐiều thứ hai khiến `mkdir -p` trở thành **lựa chọn mặc định trong script**. Với `set -e` bật (dừng khi có lỗi), một lệnh `mkdir` gặp thư mục đã tồn tại sẽ giết cả script — điều gần như không bao giờ là ý bạn muốn.\n\n### `touch` — hai công dụng\n\n```bash\ntouch file.txt        # file chưa có → tạo file RỖNG\ntouch file.txt        # file đã có   → cập nhật thời gian sửa, KHÔNG đụng nội dung\n```\n\nCông dụng thứ hai nghe vô dụng nhưng thực tế rất hay dùng: các công cụ build như `make` quyết định có cần build lại hay không dựa vào **thời gian sửa**. `touch` một file nguồn là ép build lại nó mà không phải sửa gì.\n\n```bash\ntouch a.txt b.txt c.txt              # tạo nhiều file rỗng\ntouch \"file có dấu cách.txt\"          # tên có dấu cách phải để trong ngoặc\n```\n\n### `cp` — sao chép tạo inode mới\n\n```bash\ncp nguon.txt dich.txt           # chép file\ncp file.txt /duong/dan/         # chép vào thư mục, giữ nguyên tên\ncp a.txt b.txt c.txt thu-muc/   # chép nhiều file vào một thư mục\n\ncp -r thu-muc/ ban-sao/         # ⭐ -r bắt buộc khi chép THƯ MỤC\ncp -i nguon dich                # hỏi trước khi ghi đè\ncp -v nguon dich                # in ra từng file đang chép\ncp -p nguon dich                # giữ nguyên quyền và thời gian\ncp -a thu-muc/ ban-sao/         # ⭐ chế độ lưu trữ: -r + giữ mọi thuộc tính\n```\n\nĐiểm quan trọng: **`cp` tạo inode mới**. Bản sao là một file độc lập hoàn toàn — sửa bản này không ảnh hưởng bản kia, và dung lượng đĩa tăng thật.\n\nCái bẫy về dấu gạch chéo cuối:\n\n```bash\n# Nếu đích CHƯA tồn tại:\ncp -r nguon/ dich/       → tạo dich/ chứa nội dung của nguon/\n\n# Nếu đích ĐÃ tồn tại:\ncp -r nguon/ dich/       → tạo dich/nguon/ ← lồng thêm một tầng!\ncp -r nguon/. dich/      → đổ nội dung nguon vào dich, không lồng\n```\n\nĐây là nguồn của rất nhiều lỗi trong script deploy. Cách chắc chắn nhất là dùng `rsync` với dấu gạch chéo tường minh, hoặc kiểm tra bằng `ls` sau khi chép.\n\n### `mv` — di chuyển và đổi tên là một\n\n```bash\nmv cu.txt moi.txt              # đổi tên\nmv file.txt /duong/dan/        # di chuyển\nmv file.txt /duong/dan/moi.txt # vừa dời vừa đổi tên\nmv a.txt b.txt thu-muc/        # dời nhiều file\n\nmv -i nguon dich               # hỏi trước khi ghi đè\nmv -n nguon dich               # KHÔNG ghi đè, bỏ qua nếu đích đã có\n```\n\n**Vì sao đổi tên và di chuyển là cùng một lệnh?** Vì ở tầng hệ thống file, cả hai là cùng một thao tác: **sửa mục (tên → inode) trong thư mục**. Đổi tên là sửa trường tên; di chuyển là gỡ mục khỏi thư mục này và thêm vào thư mục kia. Inode và dữ liệu không hề bị đụng tới.\n\nHệ quả về tốc độ rất rõ:\n\n```bash\nmv file-10GB.bin /same/disk/     # ~0,01 giây — chỉ sửa bản ghi thư mục\nmv file-10GB.bin /mnt/o-khac/    # vài phút — phải CHÉP rồi XOÁ\n```\n\nSang ổ đĩa khác thì không thể chỉ sửa bản ghi, vì hai ổ có hai hệ thống file riêng. `mv` lúc đó âm thầm biến thành `cp` rồi `rm`.\n\nĐiều này cũng giải thích vì sao **`mv` trong cùng ổ là nguyên tử** còn sang ổ khác thì không — chi tiết đã gặp ở bài hệ thống file với mẫu ghi-tạm-rồi-đổi-tên.\n\n### `rm` — không có đường lui\n\n```bash\nrm file.txt              # xoá file\nrm a.txt b.txt           # xoá nhiều\n\nrm thu-muc/              # ✗ lỗi: is a directory\nrm -r thu-muc/           # ✓ xoá đệ quy cả thư mục\nrm -f file               # ép xoá, không báo lỗi nếu file không tồn tại\nrm -rf thu-muc/          # ⚠️ tổ hợp mạnh nhất và nguy hiểm nhất\n\nrm -i *.txt              # hỏi từng file\nrm -v *.log              # in ra từng file đang xoá\n```\n\n**Không có thùng rác.** `rm` gọi `unlink()`, gỡ tên khỏi thư mục. Như đã học ở bài hệ thống file, dữ liệu chỉ thật sự mất khi bộ đếm liên kết về 0 và không process nào giữ file. Nhưng với người dùng thông thường thì coi như mất vĩnh viễn — khôi phục cần công cụ chuyên dụng và thường thất bại.\n\n### Ba thói quen phòng thân\n\n**① Luôn `ls` trước khi `rm` với cùng mẫu glob.**\n\n```bash\nls *.log          # xem TRƯỚC những gì sẽ bị xoá\nrm *.log          # rồi mới xoá\n```\n\nBước này mất một giây và đã cứu vô số người. Nếu `ls` ra danh sách khác với hình dung của bạn, đừng chạy `rm`.\n\n**② Dùng `-i` khi thao tác với file quan trọng.**\n\n```bash\nalias rm='rm -i'      # đặt trong ~/.bashrc\n```\n\nNhưng cẩn thận: quen với `rm -i` rồi sang máy khác không có alias là mất cảnh giác. Nhiều người cho rằng alias này tạo cảm giác an toàn giả.\n\n**③ Trong script, luôn kiểm tra biến trước khi dùng.**\n\n```bash\n# ❌ CỰC KỲ NGUY HIỂM — biến rỗng thì thành rm -rf /*\nrm -rf $THU_MUC/*\n\n# ✓ AN TOÀN — set -u làm script chết ngay nếu biến chưa gán\nset -euo pipefail\nrm -rf \"${THU_MUC:?Chưa đặt THU_MUC}\"/*\n```\n\nCú pháp `${BIEN:?thông báo}` là mẹo rất đáng nhớ: nếu biến rỗng hoặc chưa gán, shell **dừng ngay** và in thông báo, thay vì bung thành chuỗi rỗng.\n\n### Glob do shell bung ra\n\nĐây là điểm hay bị hiểu sai và nó giải thích nhiều hành vi lạ.\n\n```bash\nrm *.txt\n```\n\nLệnh `rm` **không bao giờ nhìn thấy ký tự `*`**. Shell bung nó ra trước:\n\n```\nBạn gõ:      rm *.txt\nShell bung:  rm a.txt b.txt bao-cao.txt\nrm nhận:     ba tên file cụ thể\n```\n\nCác mẫu glob:\n\n```bash\n*           # không hoặc nhiều ký tự bất kỳ\n?           # đúng một ký tự bất kỳ\n[abc]       # một ký tự trong tập\n[0-9]       # một chữ số\n{a,b}       # a hoặc b — mở rộng của bash\n\nls *.txt              # mọi file .txt\nls anh?.png           # anh1.png, anh2.png — không khớp anh10.png\nls file[0-9].txt      # file0.txt … file9.txt\ncp file.txt{,.bak}    # ⭐ bung thành: cp file.txt file.txt.bak\n```\n\nDòng cuối là mẹo sao lưu nhanh rất được ưa dùng.\n\nBa hệ quả của việc glob do shell bung:\n\n**① Glob không khớp file ẩn.** `rm *` không xoá `.env` — vì `*` không khớp tên bắt đầu bằng dấu chấm.\n\n**② Không khớp gì thì shell truyền nguyên văn.**\n\n```bash\nls *.xyz\n# ls: cannot access '*.xyz': No such file or directory\n```\n\nThông báo lỗi chứa ký tự `*` là dấu hiệu rõ: không file nào khớp, nên shell đưa nguyên chuỗi cho `ls`.\n\n**③ Quá nhiều file thì lỗi tràn.**\n\n```bash\nrm *.log\n# bash: /bin/rm: Argument list too long\n```\n\nDòng lệnh có giới hạn độ dài (~2 MB). Với hàng trăm nghìn file, dùng `find` để xử lý theo lô:\n\n```bash\nfind . -name '*.log' -delete\n```",

vi_du: "### Dựng cấu trúc dự án bằng một lệnh\n\n```bash\nmkdir -p du-an/{src,tests,docs,data/{raw,processed}}\ntree du-an\n```\n\n```\ndu-an\n├── data\n│   ├── processed\n│   └── raw\n├── docs\n├── src\n└── tests\n```\n\nCú pháp `{a,b,c}` là **brace expansion** của bash — shell bung nó thành nhiều đối số trước khi `mkdir` chạy. Lồng nhau được, như `data/{raw,processed}` ở trên.\n\n### Sao lưu trước khi sửa\n\n```bash\ncp cau-hinh.json{,.bak}       # bung thành: cp cau-hinh.json cau-hinh.json.bak\n\n# Có dấu thời gian để không đè lên bản sao lưu cũ\ncp cau-hinh.json \"cau-hinh.json.$(date +%Y%m%d-%H%M%S)\"\nls\n# cau-hinh.json  cau-hinh.json.20260830-104512  cau-hinh.json.bak\n```\n\nThói quen sao lưu trước khi sửa file cấu hình trên server đáng để rèn — nó mất một giây và cứu bạn khỏi việc khôi phục từ trí nhớ.\n\n### Đổi tên hàng loạt\n\n```bash\n# Đổi mọi .txt thành .md\nfor f in *.txt; do\n    mv \"$f\" \"${f%.txt}.md\"\ndone\n```\n\n`${f%.txt}` cắt bỏ phần `.txt` ở **cuối** chuỗi. Đây là *parameter expansion* của bash:\n\n```bash\nf=\"bao-cao.txt\"\necho \"${f%.txt}\"      # bao-cao      ← cắt hậu tố\necho \"${f#bao-}\"      # cao.txt      ← cắt tiền tố\necho \"${f%.*}\"        # bao-cao      ← cắt phần mở rộng bất kỳ\n```\n\nDấu ngoặc kép quanh `\"$f\"` là **bắt buộc** — thiếu nó thì tên file có dấu cách sẽ bị tách thành nhiều đối số.\n\n### Chép giữ nguyên cấu trúc và thuộc tính\n\n```bash\ncp -a nguon/ dich/            # -a = -r + giữ quyền, thời gian, symlink\n\n# Với dữ liệu lớn, rsync tốt hơn hẳn\nrsync -av --progress nguon/ dich/\n#      │└─ verbose, hiện tiến độ\n#      └── archive: giữ mọi thuộc tính\n```\n\n`rsync` hơn `cp` ở ba điểm: **tiếp tục được khi bị ngắt**, chỉ chép phần khác biệt nếu chạy lại, và hiện tiến độ. Với thư mục vài GB thì nên dùng `rsync`.\n\nLưu ý dấu gạch chéo cuối trong `rsync` có ý nghĩa khác `cp`:\n\n```bash\nrsync -av nguon/ dich/     # đổ NỘI DUNG nguon vào dich\nrsync -av nguon dich/      # tạo dich/nguon/  ← lồng thêm tầng\n```\n\n### Xoá an toàn\n\n```bash\n# ① Xem trước\nls -lh *.log\n# -rw-r--r-- 1 minh dev 1.2G Aug 30 10:00 access.log\n# -rw-r--r-- 1 minh dev 340M Aug 29 22:00 error.log\n\n# ② Rồi mới xoá\nrm *.log\n```\n\n```bash\n# Xoá file cũ hơn 30 ngày — LUÔN chạy không có -delete trước để xem\nfind /var/log -name '*.log' -mtime +30            # xem danh sách\nfind /var/log -name '*.log' -mtime +30 -delete    # rồi mới xoá\n```\n\nMẫu \"chạy để xem, rồi chạy để làm\" áp dụng được cho mọi thao tác phá huỷ. Nó là thói quen phân biệt người cẩn thận với người sắp gặp sự cố.\n\n### Script dọn dẹp an toàn\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\n# ${BIEN:?...} — dừng ngay nếu biến rỗng, tránh thảm hoạ rm -rf /*\nTHU_MUC=\"${1:?Cách dùng: $0 <thư-mục-cần-dọn>}\"\n\n# Kiểm tra thư mục có thật không\n[[ -d \"$THU_MUC\" ]] || { echo \"Không phải thư mục: $THU_MUC\" >&2; exit 1; }\n\n# Chặn xoá nhầm thư mục gốc hoặc thư mục nhà\ncase \"$(realpath \"$THU_MUC\")\" in\n    /|/home|/home/*/|\"$HOME\")\n        echo \"Từ chối xoá thư mục nguy hiểm: $THU_MUC\" >&2; exit 1 ;;\nesac\n\necho \"Sắp xoá nội dung của: $(realpath \"$THU_MUC\")\"\nread -rp \"Gõ 'xoa' để xác nhận: \" tra_loi\n[[ \"$tra_loi\" == \"xoa\" ]] || { echo \"Đã huỷ\"; exit 0; }\n\nrm -rf \"${THU_MUC:?}\"/*\necho \"Xong\"\n```\n\nBốn lớp phòng thủ trong script này: `set -u` bắt biến chưa gán, `${1:?}` bắt thiếu đối số, `case` chặn các đường dẫn nguy hiểm, và xác nhận thủ công. Với lệnh không hoàn tác được thì nhiều lớp là xứng đáng.\n\n### Bẫy glob và file ẩn\n\n```bash\nls -a\n# .  ..  .env  .gitignore  main.py  data.csv\n\nrm *              # xoá main.py và data.csv\nls -a\n# .  ..  .env  .gitignore     ← file ẩn CÒN NGUYÊN\n```\n\nMuốn xoá cả file ẩn:\n\n```bash\nshopt -s dotglob      # bật để * khớp cả file ẩn\nrm *\nshopt -u dotglob      # tắt lại\n```\n\nĐây là con dao hai lưỡi: nó cũng khiến `rm *` xoá luôn `.git`. Nên cẩn thận khi bật.",

so_sanh: "### `cp` và `mv` khác nhau ở tầng dưới\n\n| | `cp` | `mv` (cùng ổ) | `mv` (khác ổ) |\n|---|---|---|---|\n| Inode | **Tạo mới** | **Giữ nguyên** | Tạo mới rồi xoá cũ |\n| Tốc độ | Theo kích thước | **Tức thì** | Theo kích thước |\n| Dung lượng đĩa | Tăng gấp đôi | Không đổi | Tạm thời gấp đôi |\n| Nguyên tử | Không | **Có** | Không |\n| Bị ngắt giữa chừng | File đích dở dang | Không thể xảy ra | Có thể mất dữ liệu |\n\nDòng cuối đáng chú ý khi viết script: **`mv` trong cùng ổ an toàn tuyệt đối**, còn `mv` sang ổ khác thì bị ngắt giữa chừng là có thể mất. Đó là lý do mẫu ghi file an toàn yêu cầu file tạm phải **cùng thư mục** với đích.\n\n### `cp` và `rsync`\n\n| | `cp` | `rsync` |\n|---|---|---|\n| Có sẵn | ✓ mọi hệ thống | Thường phải cài |\n| Tiếp tục sau khi ngắt | ✗ chép lại từ đầu | **✓** |\n| Chỉ chép phần khác | ✗ | **✓** |\n| Hiện tiến độ | ✗ | ✓ |\n| Qua mạng | ✗ | **✓ (qua SSH)** |\n\nQuy tắc: **file nhỏ hoặc thao tác đơn giản dùng `cp`; thư mục lớn, đồng bộ lặp lại, hoặc qua mạng thì dùng `rsync`**.\n\n### Vì sao Unix không có thùng rác\n\nĐây là quyết định thiết kế có chủ đích, phản ánh triết lý Unix: **công cụ làm đúng điều được yêu cầu, không đoán ý người dùng**.\n\nƯu điểm: `rm` nhanh, đơn giản, hành vi dự đoán được, không tốn dung lượng ẩn, hoạt động giống hệt trong script và khi gõ tay.\n\nNhược điểm: xoá nhầm là mất. Và đây là nhược điểm nghiêm trọng thật.\n\nMột số người cài `trash-cli` để có lệnh `trash` thay cho `rm`. Cách này ổn cho máy cá nhân, nhưng có một rủi ro thật: **bạn quen dùng nó rồi lên server không có nó** và mất cảnh giác. Thói quen bền vững hơn là **luôn `ls` trước khi `rm`** — thói quen đó đúng ở mọi máy.\n\n### `rm -rf /` và cơ chế phòng vệ\n\nLệnh này từng xoá sạch hệ thống. Ngày nay GNU coreutils **từ chối** nó:\n\n```bash\nrm -rf /\n# rm: it is dangerous to operate recursively on '/'\n# rm: use --no-preserve-root to override this failsafe\n```\n\nNhưng lưới an toàn này **chỉ chặn đúng chuỗi `/`**. Các biến thể vẫn đi lọt:\n\n```bash\nrm -rf /*              # ← KHÔNG bị chặn\nrm -rf \"$BIEN_RONG\"/*  # ← biến rỗng bung thành /* — không bị chặn\n```\n\nTrường hợp thứ hai là nguyên nhân của phần lớn sự cố thật, vì nó xảy ra **do lỗi lập trình chứ không phải do người gõ ẩu**. Đó là lý do `set -u` và `${BIEN:?}` quan trọng hơn nhiều so với việc nhớ không gõ `rm -rf /`.",

loi_thuong_gap: [
  "**Tưởng có thùng rác.** `rm` xoá vĩnh viễn. Luôn `ls` với cùng mẫu glob trước khi `rm`.",
  "**`rm -rf $BIEN/*` mà không kiểm tra biến.** Biến rỗng bung thành `rm -rf /*`. Dùng `set -u` và `${BIEN:?}`.",
  "**Quên `-r` khi chép hoặc xoá thư mục.** `cp` và `rm` mặc định chỉ làm việc với file.",
  "**Nhầm dấu gạch chéo cuối trong `cp -r`.** Đích đã tồn tại thì `cp -r a/ b/` tạo ra `b/a/` chứ không đổ nội dung vào `b/`. Kiểm tra bằng `ls` sau khi chép.",
  "**Tưởng `rm *` xoá hết.** Glob không khớp file ẩn, nên `.env` và `.gitignore` còn nguyên.",
  "**Quên ngoặc kép quanh biến.** `mv $f dich` với tên file có dấu cách sẽ bị tách thành nhiều đối số. Luôn `\"$f\"`.",
  "**Dùng `cp` cho thư mục vài GB.** Bị ngắt là mất công từ đầu. Dùng `rsync` — nó tiếp tục được.",
  "**Dùng `mkdir` thay vì `mkdir -p` trong script.** Thư mục đã tồn tại là lỗi, và với `set -e` thì cả script chết."
],

tu_kiem_tra: [
  "Vì sao `mv` file 10 GB trong cùng ổ đĩa gần như tức thì, còn sang ổ khác thì mất vài phút?",
  "`rm *.txt` — lệnh `rm` có nhìn thấy ký tự `*` không? Ai bung nó ra, và điều đó dẫn tới hệ quả gì?",
  "Vì sao `rm *` không xoá file `.env`?",
  "Viết một dòng lệnh sao lưu `cau-hinh.json` thành `cau-hinh.json.bak` chỉ gõ tên file một lần.",
  "Script có `rm -rf $DIR/*` và biến `DIR` chưa được gán. Chuyện gì xảy ra, và hai cách phòng là gì?",
  "Nêu hai lý do nên dùng `rsync` thay `cp` khi chép thư mục lớn.",
  "Vì sao `mkdir -p` là lựa chọn mặc định trong script chứ không phải `mkdir`?"
],

tai_nguyen: [
  "Sách **The Linux Command Line** — William Shotts, chương 4–5 về thao tác file. Đọc miễn phí.",
  "Tài liệu Linux: `man cp`, `man mv`, `man rm` — đọc mục *OPTIONS* để biết hết các cờ.",
  "Trang **explainshell.com** — dán lệnh có nhiều cờ vào, nó tách và giải thích từng cái.",
  "Tài liệu bash, mục *Filename Expansion* và *Brace Expansion* — hiểu chính xác shell bung glob ra sao."
],

lien_he: [
  "**Hệ thống file, đường dẫn, quyền** ở module 2: `cp` tạo inode mới còn `mv` giữ inode — đó là toàn bộ khác biệt giữa hai lệnh.",
  "**Điều hướng** là bài ngay trước: thao tác file luôn tính từ thư mục bạn đang đứng.",
  "**Pipe và redirect** (bài sau): kết hợp `find` với `xargs` để thao tác trên số lượng file lớn.",
  "**Viết shell script** (bài cuối module): `set -euo pipefail` và `${BIEN:?}` là hai lớp phòng thủ đã dùng trong bài này.",
  "**Git** ở mảng 03: `git clean -fd` cũng xoá vĩnh viễn, và cũng nên `git clean -n` để xem trước — cùng thói quen \"xem rồi mới làm\".",
  "**Triển khai** ở mảng 05: script deploy đầy `mkdir -p`, `cp`, `rm`. Một cờ sai là hỏng cả lần phát hành."
]

};
