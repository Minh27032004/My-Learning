window.LESSONS["05:thiet-ke-api:dinh-dang-loi-nhat-quan"] = {

tom_tat: [
  "Lỗi API là một hợp đồng máy đọc được gồm HTTP status, mã lỗi ổn định, thông điệp an toàn, chi tiết có cấu trúc và correlation identifier.",
  "Phân loại lỗi theo validation, authentication, authorization, not found, conflict, rate limit, dependency và internal giúp client biết sửa request, retry hay dừng.",
  "Thông điệp cho người dùng có thể đổi hoặc được dịch; client phải rẽ nhánh bằng `code`, không parse câu chữ.",
  "Response không được lộ stack trace, SQL, secret hay sự tồn tại của tài nguyên ngoài quyền; log nội bộ mới giữ chẩn đoán chi tiết.",
  "Một exception handler tập trung trong FastAPI giúp validation, lỗi miền và lỗi bất ngờ cùng một envelope, nhưng không được biến mọi lỗi thành `500` hoặc `200`."
],

can_biet_truoc: [
  "HTTP status code và khác biệt tổng quát giữa lỗi 4xx với 5xx.",
  "Exception trong Python, middleware và dependency của FastAPI.",
  "Pydantic validation và đường dẫn trường trong dữ liệu JSON lồng nhau.",
  "Logging có cấu trúc, request ID/trace ID và nguyên tắc không ghi secret."
],

dinh_nghia: "**Định dạng lỗi nhất quán** là schema response chung cho mọi thất bại của API, trong đó status code giữ semantics HTTP còn body cung cấp định danh lỗi cấp ứng dụng và ngữ cảnh vừa đủ để client xử lý.\n\nMột envelope thực dụng có `type`, `title`, `status`, `code`, `detail`, `instance`, `trace_id` và `errors` tùy chọn. Cấu trúc này gần mô hình Problem Details của HTTP: trường chuẩn phục vụ tooling, `code` là extension ổn định theo miền, `errors` mô tả từng trường validation. Nhất quán không có nghĩa mọi lỗi giống hệt nhau; nó có nghĩa client luôn biết tìm thông tin cốt lõi ở đâu.",

vi_sao_can: "Nếu endpoint tạo đơn trả `{\"error\":\"bad\"}`, endpoint thanh toán ném HTML từ proxy và endpoint khác trả `200 {\"success\":false}`, frontend phải viết nhánh riêng cho từng nơi. Monitoring đếm sai, SDK khó sinh kiểu và người vận hành không nối được ảnh chụp màn hình của khách với log server.\n\nLỗi còn là ranh giới bảo mật. Stack trace cho biết đường dẫn file và thư viện; lỗi database có thể lộ tên bảng; thông báo “đơn ord_7F3 thuộc cus_9” xác nhận dữ liệu của người khác. Che tất cả bằng “Something went wrong” lại khiến validation khó sửa. Thiết kế tốt phân tách thông tin công khai tối thiểu với log nội bộ giàu ngữ cảnh, nối hai phía bằng `trace_id` không chứa bí mật.\n\nSemantics lỗi quyết định retry. `429` hoặc `503` có thể retry sau chỉ dẫn; `422` do quantity âm sẽ không tự hết; `409` có thể yêu cầu đọc lại trạng thái. Nếu tất cả là `500`, client gây retry storm và làm sự cố nặng thêm.",

co_che: "### Bắt đầu bằng taxonomy, không bắt đầu bằng JSON\n\nTrước khi chọn tên trường, phân loại điều gì đã sai và ai có thể sửa. Validation: request không thỏa schema. Authentication: chưa chứng minh danh tính. Authorization: có danh tính nhưng thiếu quyền. Not found: tài nguyên không tồn tại hoặc cố ý che existence. Conflict: request hợp lệ nhưng xung đột trạng thái hiện tại. Rate limit: vượt ngân sách. Dependency: hệ thống hạ nguồn lỗi. Internal: bug hoặc invariant bị phá.\n\nTaxonomy ngăn việc tiện tay ném `400` cho mọi thứ. Nó cũng cho client một decision table: validation hiển thị cạnh form; unauthenticated mở luồng đăng nhập; conflict tải lại đơn; internal hiển thị trace ID và không tự đoán nguyên nhân.\n\n### Status code và mã miền có hai vai trò\n\nHTTP status là lớp thô được gateway/browser hiểu. `code` là định danh chi tiết ổn định như `ORDER_NOT_FOUND`, `ORDER_STATE_CONFLICT`, `VALIDATION_FAILED`. Không tạo một status riêng cho mọi luật nghiệp vụ; cũng không nhét mọi luật vào một `BAD_REQUEST` duy nhất.\n\n| Tình huống | Status | Code gợi ý | Client nên làm |\n|---|---:|---|---|\n| JSON sai kiểu | 422 | `VALIDATION_FAILED` | sửa trường |\n| token thiếu/hỏng | 401 | `UNAUTHENTICATED` | đăng nhập/làm mới token |\n| không có quyền | 403 hoặc 404 | `FORBIDDEN`/`ORDER_NOT_FOUND` | dừng, không retry mù |\n| đơn đã giao, không thể hủy | 409 | `ORDER_STATE_CONFLICT` | tải lại trạng thái |\n| quá nhiều request | 429 | `RATE_LIMITED` | chờ theo policy |\n| lỗi không dự kiến | 500 | `INTERNAL_ERROR` | báo trace ID |\n\n### Problem Details làm nền schema\n\n`type` là định danh loại vấn đề, có thể là URI tài liệu ổn định nhưng không được bịa link chưa tồn tại; có thể dùng `about:blank` khi chưa có catalog. `title` ngắn và ổn định theo loại. `status` lặp status HTTP để body tự mô tả. `detail` nói về occurrence cụ thể nhưng vẫn an toàn. `instance` định danh request/tài nguyên xảy ra lỗi, không chứa dữ liệu nhạy cảm.\n\nExtension `code`, `trace_id`, `errors` phục vụ ứng dụng. Không bắt client parse `type` URL nếu code miền đã là contract. Giữ schema phẳng vừa phải; envelope lồng `{\"error\":{\"error\":...}}` chỉ thêm ceremony.\n\n### Code ổn định, message dành cho con người\n\nFrontend không được viết `if detail == \"Order not found\"`. Khi dịch sang tiếng Việt hoặc sửa dấu câu, logic sẽ gãy. `code` dùng uppercase snake case hoặc quy ước khác nhưng phải bất biến và có catalog.\n\nKhông nhúng dữ liệu động vào code như `ORDER_123_NOT_FOUND`; dữ liệu nằm trong metadata an toàn. Không tạo hàng nghìn code theo mọi validation nhỏ; `VALIDATION_FAILED` cộng danh sách field đủ dùng. Mỗi code cần owner, status mặc định, retryability và mô tả.\n\n### Validation errors cần đường dẫn có cấu trúc\n\nVới `items[1].quantity`, response nên có `location: [\"body\",\"items\",1,\"quantity\"]`, `code: \"greater_than_equal\"`, `message` và có thể `context`. Client ánh xạ path tới control. Không trả nguyên input nếu nó có token, địa chỉ hay thẻ thanh toán.\n\nĐừng hứa message validation của framework ổn định qua mọi nâng cấp. Adapter chuyển lỗi Pydantic thành schema của riêng API. Giới hạn số lỗi trả về; payload cố ý chứa hàng nghìn phần tử sai có thể làm response/log phình.\n\n### Authentication và authorization tránh oracle\n\n`401` nghĩa credentials thiếu/không hợp lệ, không phải “không có quyền”; thường kèm `WWW-Authenticate`. `403` nghĩa server biết principal nhưng từ chối. Tuy nhiên với order của người khác, trả `404 ORDER_NOT_FOUND` có thể tránh xác nhận mã tồn tại.\n\nGiữ quyết định nhất quán để timing/body không trở thành oracle. Nếu ID không tồn tại trả ngay nhưng ID tồn tại ngoài quyền gọi nhiều query, chênh lệch có thể bị đo; cân nhắc query ownership một bước. Log nội bộ vẫn có reason thật, nhưng response ngoài không nêu customer ID hay role thiếu cụ thể nếu thông tin đó nhạy cảm.\n\n### Conflict khác validation\n\nPayload `quantity=-1` sai độc lập với state nên validation. Yêu cầu hủy đơn `shipped` có hình dạng đúng nhưng xung đột trạng thái, thường là `409`. Optimistic concurrency với `If-Match` không khớp có thể dùng `412`. Phân biệt giúp client biết sửa form hay refresh tài nguyên.\n\n`409` body nên cung cấp state công khai hiện tại hoặc action hợp lệ nếu không rò dữ liệu. Đừng tự động hủy/ghi đè chỉ để tránh lỗi; conflict tồn tại để bảo vệ invariant.\n\n### Retryability phải được nói bằng semantics\n\n`400`, `401`, `403`, `404`, `409`, `422` thường không retry nguyên xi. `408`, `429`, `502`, `503`, `504` có thể retry có backoff và jitter, nhưng method phải idempotent hoặc có idempotency key. `500` không mặc nhiên an toàn để retry POST tạo đơn vì server có thể đã commit rồi mất response.\n\n`Retry-After` hữu ích với rate limit/maintenance. Body có thể thêm `retryable`, nhưng header/status và tài liệu vẫn là nguồn chính. Không để client retry vô hạn; retry budget ngăn nhiều tầng nhân số request.\n\n### Lỗi dependency không được bê nguyên ra ngoài\n\nPayment provider trả body chứa merchant key fragment hoặc internal request ID; API không được proxy nguyên xi. Ánh xạ sang `PAYMENT_TEMPORARILY_UNAVAILABLE`, giữ provider status/request ID trong log bảo mật. Phân biệt timeout trước khi biết kết quả với decline nghiệp vụ; decline không phải `500`.\n\nNếu dependency chậm, timeout và circuit breaker giới hạn lan truyền. Error contract cần đủ để client không tạo lại đơn bừa bãi. Bài idempotency tiếp theo sẽ xử lý vùng mơ hồ “đã commit hay chưa”.\n\n### Trace ID nối response với telemetry\n\nMỗi request nhận hoặc sinh correlation ID theo quy tắc an toàn; không tin chuỗi tùy ý dài từ client rồi đưa thẳng vào log. Response lỗi trả `trace_id`, log mọi event cùng ID, distributed tracing truyền context qua service.\n\nTrace ID không phải secret và không phải authorization token. Endpoint hỗ trợ tra trace vẫn phải kiểm quyền. Không dùng email/order ID làm trace ID. Khi báo lỗi, người dùng gửi trace ID và thời điểm; đội vận hành tìm được stack trace nội bộ mà không công khai nó.\n\n### Log đúng mức và redaction\n\nLỗi dự kiến như validation không cần stack trace ở ERROR cho mọi request; nếu không log sẽ nhiễu và tốn tiền. Conflict nghiệp vụ có thể INFO/WARN theo nhu cầu. Exception bất ngờ log stack trace đúng một lần ở boundary, kèm route template, principal đã pseudonymize, code và trace ID.\n\nKhông log Authorization, cookie, mật khẩu, token reset, toàn body thanh toán. Redaction phải diễn ra trước sink, không trông chờ người xem bỏ qua. Structured logging giúp truy vấn `error.code=ORDER_STATE_CONFLICT` thay vì regex câu chữ.\n\n### Handler tập trung nhưng giữ lỗi miền có kiểu\n\nDomain layer nên ném exception có nghĩa như `OrderStateConflict(order_id, current_status)`, không ném `HTTPException` khắp nơi nếu còn dùng ở worker/CLI. Adapter FastAPI ánh xạ exception miền sang status và Problem Details. Nhờ vậy nghiệp vụ không phụ thuộc HTTP và mapping được test tập trung.\n\nKhông bắt `Exception` rồi trả `200`. Handler cuối cùng cho exception không biết trả `500` an toàn và log stack. `CancelledError`, shutdown hoặc lỗi hệ thống đặc biệt có thể cần để framework xử lý; đừng nuốt mọi `BaseException`.\n\n### Proxy và gateway cũng phải theo hợp đồng\n\nỨng dụng có envelope đẹp nhưng gateway trả HTML `502 Bad Gateway` vẫn làm client vỡ. Cấu hình edge chuyển lỗi timeout/rate limit sang JSON cùng content type và trace ID nếu có thể. Tuy nhiên không được giả mạo code miền khi gateway không biết nghiệp vụ; dùng code hạ tầng riêng.\n\nKiểm response `Content-Type: application/problem+json` hoặc quy ước đã chọn. CORS, authentication middleware và body-size limit xảy ra trước route cũng cần integration test, vì unit test handler không thấy chúng.\n\n### Schema tiến hóa mà không phá client\n\nThêm extension tùy chọn thường an toàn nếu client bỏ qua trường lạ. Không đổi kiểu `errors` từ array sang object. Nếu cần cấu trúc mới không tương thích, version contract hoặc thêm trường mới rồi di trú. Catalog code là tài liệu công khai; xóa/đổi nghĩa code là breaking change giống đổi response thành công.\n\nMessage có thể localize theo client, nhưng log nên giữ code và template chuẩn để nhóm sự cố. Nếu API trả localized message, `Accept-Language` làm response biến thể và cache phải xét header; nhiều hệ thống đơn giản hơn khi frontend tự dịch từ code.\n\n### Bảo vệ trước lỗi trong chính error handler\n\nSerializer lỗi không được ném vì context chứa Decimal/datetime lạ. Handler phải nhỏ, có fallback tối thiểu và test với exception bất thường. Nếu logging sink hỏng, request vẫn nên trả response; tránh gọi dependency ngoài trong handler.\n\nGiới hạn detail và errors để chống amplification. Header do client cấp phải sanitize trước khi echo. Không đưa exception string của lỗi chưa phân loại ra ngoài, vì message thư viện thường chứa đường dẫn, SQL hoặc endpoint nội bộ.\n\n### Khi nào không cần envelope quá giàu\n\nService nội bộ cực nhỏ vẫn cần status/code/trace ID, nhưng có thể không cần localization hay URI tài liệu cho từng code. CLI một người dùng có nhu cầu khác public API. Hãy giữ phần lõi tối thiểu ổn định rồi thêm metadata vì use case thật.\n\nKhông ép response thành công vào Problem Details. Lỗi streaming sau khi header đã gửi cũng không thể đổi status như request bình thường; protocol cần thông báo lỗi trong stream. Thiết kế theo điểm thất bại thực tế, không theo mong muốn mọi thứ có cùng JSON.",

vi_du: "### FastAPI với exception miền và handler thống nhất\n\n```python\nfrom uuid import uuid4\nfrom fastapi import FastAPI, Request\nfrom fastapi.exceptions import RequestValidationError\nfrom fastapi.responses import JSONResponse\n\napp = FastAPI()\n\nclass OrderStateConflict(Exception):\n    def __init__(self, order_id: str, state: str):\n        self.order_id, self.state = order_id, state\n\ndef problem(request: Request, status: int, code: str, title: str,\n            detail: str, errors: list | None = None) -> JSONResponse:\n    trace_id = getattr(request.state, \"trace_id\", uuid4().hex)\n    body = {\"type\":\"about:blank\", \"title\":title, \"status\":status,\n            \"code\":code, \"detail\":detail, \"instance\":request.url.path,\n            \"trace_id\":trace_id}\n    if errors is not None:\n        body[\"errors\"] = errors[:50]\n    return JSONResponse(body, status_code=status,\n                        media_type=\"application/problem+json\")\n\n@app.middleware(\"http\")\nasync def attach_trace_id(request: Request, call_next):\n    supplied = request.headers.get(\"x-request-id\", \"\")\n    request.state.trace_id = supplied if supplied.isalnum() and len(supplied) <= 64 else uuid4().hex\n    response = await call_next(request)\n    response.headers[\"X-Request-Id\"] = request.state.trace_id\n    return response\n\n@app.exception_handler(RequestValidationError)\nasync def validation_handler(request: Request, exc: RequestValidationError):\n    fields = [{\"location\": list(e[\"loc\"]), \"code\": e[\"type\"],\n               \"message\": e[\"msg\"]} for e in exc.errors()]\n    return problem(request, 422, \"VALIDATION_FAILED\", \"Dữ liệu không hợp lệ\",\n                   \"Hãy kiểm tra các trường được chỉ ra.\", fields)\n\n@app.exception_handler(OrderStateConflict)\nasync def conflict_handler(request: Request, exc: OrderStateConflict):\n    return problem(request, 409, \"ORDER_STATE_CONFLICT\", \"Không thể đổi trạng thái\",\n                   f\"Đơn đang ở trạng thái {exc.state}.\")\n\n@app.exception_handler(Exception)\nasync def unexpected_handler(request: Request, exc: Exception):\n    # Production log exc_info cùng trace_id tại đây; không trả str(exc).\n    return problem(request, 500, \"INTERNAL_ERROR\", \"Lỗi nội bộ\",\n                   \"Hệ thống không thể hoàn tất yêu cầu. Hãy cung cấp trace_id khi liên hệ hỗ trợ.\")\n\n@app.post(\"/orders/{order_id}/cancellations\")\ndef cancel_order(order_id: str):\n    if order_id == \"ord_shipped\":\n        raise OrderStateConflict(order_id, \"shipped\")\n    return {\"order_id\": order_id, \"status\": \"cancelled\"}\n```\n\n```bash\ncurl -i -X POST http://127.0.0.1:8000/orders/ord_shipped/cancellations\n```\n\nResponse có status `409`, content type Problem Details, code ổn định và trace ID. Ví dụ detail nêu state vì đó là dữ liệu người gọi đã được phép thao tác trong demo; production phải kiểm ownership trước khi nêu bất kỳ trạng thái nào.\n\n### Test cả body lẫn header\n\n```python\nfrom fastapi.testclient import TestClient\nfrom app import app\n\ndef test_conflict_contract():\n    r = TestClient(app).post(\"/orders/ord_shipped/cancellations\",\n                             headers={\"X-Request-Id\":\"test123\"})\n    assert r.status_code == 409\n    assert r.headers[\"content-type\"].startswith(\"application/problem+json\")\n    assert r.json()[\"code\"] == \"ORDER_STATE_CONFLICT\"\n    assert r.json()[\"trace_id\"] == \"test123\"\n    assert \"traceback\" not in r.text.lower()\n```\n\nIntegration test bổ sung cần đi qua reverse proxy để xác nhận `413`, `429`, `502` cũng không trả HTML và không mất correlation ID.",

so_sanh: "### Problem Details, envelope tự chế và lỗi ad-hoc\n\n| Cách | Điểm mạnh | Điểm yếu | Phù hợp |\n|---|---|---|---|\n| Problem Details + extension | vocabulary chuẩn, tooling hiểu, mở rộng được | cần mapping và governance | API công khai/nhiều client |\n| Envelope riêng tối giản | vừa đúng nhu cầu, dễ triển khai | dễ lệch giữa đội nếu thiếu catalog | hệ thống nội bộ kiểm soát |\n| Ad-hoc theo endpoint | nhanh ở prototype đầu | client rẽ nhánh, monitor sai, rò dữ liệu | gần như chỉ demo bỏ đi |\n| gRPC status/details | typed trong hệ sinh thái gRPC | không trực tiếp là HTTP JSON contract | service-to-service gRPC |\n\nChọn Problem Details không tự bảo đảm chất lượng; nhét stack trace vào `detail` vẫn nguy hiểm. Envelope riêng cũng có thể tốt nếu status/code/trace/field errors được định nghĩa chặt. Giá trị nằm ở semantics và kỷ luật vận hành, không chỉ tên chuẩn.\n\n**Khi không nên trả chi tiết:** lỗi authorization, authentication, dependency nội bộ và exception chưa phân loại. Client cần hành động được, không cần sơ đồ hệ thống. Chi tiết đầy đủ thuộc log được kiểm soát truy cập.",

loi_thuong_gap: [
  "**Trả `200` với `success:false`.** Gateway và metric coi là thành công; dùng status HTTP đúng cùng body có code.",
  "**Client parse message.** Đổi ngôn ngữ làm logic gãy; rẽ nhánh bằng code ổn định.",
  "**Trả `str(exception)` ra ngoài.** Pentest thấy SQL/path/secret; log nội bộ và trả detail đã kiểm duyệt.",
  "**Dùng `401` cho mọi thiếu quyền.** Client đăng nhập lại vô ích; phân biệt unauthenticated với forbidden/not found che existence.",
  "**Validation trả lại nguyên input.** Token hoặc PII lọt vào response/log; chỉ trả location và mô tả cần thiết.",
  "**Bắt mọi exception thành `400`.** Bug server bị đổ cho client, alert không chạy; exception không biết phải là 500 và có stack nội bộ.",
  "**App JSON nhưng proxy trả HTML.** SDK parse thất bại khi sự cố thật; test qua gateway và chuẩn hóa edge errors.",
  "**Retry mọi 5xx cho POST.** Đơn có thể tạo trùng sau timeout; dùng retry policy theo semantics và idempotency key."
],

tu_kiem_tra: [
  "Vì sao message không thể là định danh lỗi ổn định dù hiện API chỉ dùng một ngôn ngữ?",
  "Phân biệt `401`, `403`, `404` che existence, `409` và `422` bằng ví dụ đơn hàng.",
  "Một provider timeout sau khi charge thành công. Vì sao trả `500 retryable=true` có thể nguy hiểm?",
  "Trace ID giúp chẩn đoán thế nào và vì sao nó không được dùng làm authorization token?",
  "Lỗi validation chứa `input` do framework cung cấp. Trường hợp nào biến tiện ích này thành rò bí mật?",
  "Thiết kế mapping cho exception domain mà không làm domain phụ thuộc FastAPI.",
  "Vì sao cần test lỗi từ gateway/middleware ngoài test exception handler?"
],

tai_nguyen: [
  "Tài liệu **Problem Details for HTTP APIs** của IETF.",
  "Tài liệu **HTTP Semantics** của IETF về status code và method.",
  "Tài liệu chính thức **FastAPI**, phần Handling Errors, Middleware và Security.",
  "Sách **Release It!** — Michael T. Nygard."
],

lien_he: [
  "Nối sang mảng **Observability**: trace ID, structured log, metric theo error code và distributed tracing biến lỗi thành tín hiệu vận hành.",
  "Nối sang mảng **Bảo mật web**: thông báo lỗi, timing và status có thể thành oracle; redaction ngăn lộ secret/PII.",
  "Nối sang mảng **Hệ thống phân tán**: timeout không cho biết transaction đã commit; retry cần idempotency và backoff.",
  "Nối sang mảng **Kiểm thử**: contract test khóa status/schema/code, chaos test xác nhận dependency và gateway vẫn trả lỗi đúng chuẩn."
]

};
