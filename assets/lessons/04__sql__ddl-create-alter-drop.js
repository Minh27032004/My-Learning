window.LESSONS["04:sql:ddl-create-alter-drop"] = {

tom_tat: [
  "DDL thay đổi cấu trúc và quy tắc của dữ liệu; trong PostgreSQL, nhiều lệnh DDL có transaction nhưng vẫn có thể giữ lock mạnh và chặn truy cập lâu.",
  "`CREATE` nên mô hình hóa bất biến bằng kiểu dữ liệu, `NOT NULL`, `CHECK`, khóa chính, khóa ngoại và `UNIQUE`, thay vì chỉ dựng các cột đủ để ứng dụng chạy.",
  "`ALTER TABLE` trên bảng đang phục vụ người dùng là bài toán triển khai tương thích, không chỉ là một câu lệnh đúng cú pháp; migration nhiều pha thường an toàn hơn thay đổi nguyên khối.",
  "`DROP` phá hủy đối tượng và có thể kéo theo phụ thuộc khi dùng `CASCADE`; phải khảo sát dependency, có kế hoạch phục hồi và ưu tiên đổi tên hoặc ngừng đọc trước khi xóa thật.",
  "Index cũng là một phần của thiết kế schema: nó đổi chi phí đọc, ghi và lock. `CREATE INDEX CONCURRENTLY` giảm chặn thao tác ghi nhưng có giới hạn transaction và cần kiểm tra trạng thái sau lỗi."
],

can_biet_truoc: [
  "Biết bảng, hàng, cột, kiểu dữ liệu SQL và ý nghĩa của `NULL`.",
  "Biết khóa chính, khóa ngoại, transaction, `COMMIT` và `ROLLBACK` ở mức cơ bản.",
  "Đã dùng `SELECT`, `INSERT`, `UPDATE`, `DELETE` và biết index giúp tìm hàng mà không quét toàn bảng.",
  "Biết ứng dụng có thể có nhiều phiên kết nối đồng thời; một migration chạy trong lúc hệ thống đang nhận yêu cầu sẽ cạnh tranh lock với các truy vấn khác."
],

dinh_nghia: "**DDL (Data Definition Language, ngôn ngữ định nghĩa dữ liệu)** là nhóm câu lệnh tạo, thay đổi và xóa các đối tượng cùng quy tắc cấu trúc trong cơ sở dữ liệu. Ba động từ trung tâm là `CREATE`, `ALTER` và `DROP`; đối tượng có thể là table, index, schema, sequence, view hoặc function.\n\nDDL khác DML ở mục tiêu. `UPDATE tickets SET status = 'paid'` đổi các hàng; `ALTER TABLE tickets ADD CONSTRAINT ...` đổi tập trạng thái mà mọi hàng hiện tại và tương lai được phép nhận. Vì vậy DDL là nơi ghi bất biến bền vững mà mọi ứng dụng, tác vụ nền và lệnh SQL thủ công đều phải tuân theo.",

vi_sao_can: "Hệ thống bán vé bắt đầu nhỏ thường chỉ cần vài cột. Khi có thanh toán, hoàn tiền, nhiều múi giờ và báo cáo, schema sơ sài biến thành nguồn lỗi: hai vé cùng chiếm một ghế, giá âm, mã sự kiện trùng, hoặc xóa sự kiện làm vé mồ côi. Kiểm tra ở giao diện không đủ vì tác vụ nhập dữ liệu, phiên bản ứng dụng cũ hay một câu SQL trực tiếp có thể đi vòng qua giao diện.\n\nDDL đưa luật tới nơi dữ liệu được ghi. Tuy nhiên chính sức mạnh đó tạo rủi ro vận hành. Thêm constraint buộc PostgreSQL kiểm tra dữ liệu cũ; đổi kiểu có thể viết lại toàn bảng; lệnh cần `ACCESS EXCLUSIVE` lock có thể đứng chờ rồi chặn cả hàng dài truy vấn phía sau. Một migration đúng trên máy cá nhân chưa chắc an toàn trên bảng hàng trăm triệu hàng. Ta cần học đồng thời cú pháp, mô hình hóa và chiến lược triển khai.",

co_che: "### `CREATE TABLE`: thiết kế luật trước khi thiết kế cột\n\nVí dụ xuyên suốt dùng một nền tảng bán vé. PostgreSQL có thể tạo schema ban đầu như sau:\n\n```sql\nBEGIN;\n\nCREATE TABLE events (\n    event_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    event_code text NOT NULL UNIQUE,\n    title text NOT NULL,\n    starts_at timestamptz NOT NULL,\n    capacity integer NOT NULL CHECK (capacity > 0),\n    created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE customers (\n    customer_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    email text NOT NULL,\n    created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE UNIQUE INDEX customers_email_ci_uq ON customers (lower(email));\n\nCREATE TABLE tickets (\n    ticket_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    event_id bigint NOT NULL REFERENCES events(event_id),\n    customer_id bigint NOT NULL REFERENCES customers(customer_id),\n    seat_no text,\n    price_cents integer NOT NULL CHECK (price_cents >= 0),\n    status text NOT NULL CHECK (status IN ('held', 'paid', 'cancelled')),\n    purchased_at timestamptz,\n    UNIQUE (event_id, seat_no)\n);\n\nCOMMIT;\n```\n\n`bigint` tránh cạn không gian khóa sớm; identity để PostgreSQL sinh số mà vẫn thể hiện rõ quyền ghi. `timestamptz` lưu một thời điểm tuyệt đối, phù hợp sự kiện có người mua ở nhiều múi giờ. Tiền dùng số nguyên cent để phép cộng chính xác, không dùng floating point. `CHECK` chặn giá âm và trạng thái lạ. Khóa ngoại ngăn vé trỏ đến sự kiện không tồn tại. Constraint `UNIQUE (event_id, seat_no)` cho phép cùng tên ghế ở hai sự kiện nhưng cấm bán ghế đó hai lần trong một sự kiện.\n\nCó một ca biên quan trọng: PostgreSQL xem các giá trị `NULL` là phân biệt trong unique constraint mặc định. Vì vậy nhiều vé đứng không có `seat_no` vẫn hợp lệ. Nếu nghiệp vụ coi `NULL` là cùng một giá trị, cần thiết kế khác; không được suy từ tên `UNIQUE` rằng chỉ có một `NULL`.\n\n### Tên constraint và khả năng bảo trì\n\nPostgreSQL tự sinh tên nếu ta bỏ qua, nhưng migration về sau phải tham chiếu tên để `DROP CONSTRAINT`. Tên rõ như `tickets_price_nonnegative_ck` hữu ích hơn một tên phụ thuộc công cụ sinh schema. Quy ước tên cũng giúp log lỗi nói thẳng luật nào bị vi phạm.\n\n```sql\nALTER TABLE tickets\n  DROP CONSTRAINT tickets_price_cents_check,\n  ADD CONSTRAINT tickets_price_nonnegative_ck CHECK (price_cents >= 0);\n```\n\nTên cụ thể của constraint tự sinh phải được xem bằng `\\d tickets` trong `psql` hoặc truy vấn catalog trước khi chạy; đoạn trên chỉ đúng nếu tên cũ thực sự là `tickets_price_cents_check`. Đây là lý do migration không nên đoán tên ở production.\n\n### Transactional DDL: hoàn tác được không có nghĩa là vô hại\n\nPostgreSQL cho phép đặt phần lớn DDL trong transaction. Nếu câu cuối thất bại, `ROLLBACK` có thể trả schema về trạng thái trước đó. Đây là ưu điểm lớn khi nhiều thay đổi phải đi cùng nhau.\n\n```sql\nBEGIN;\nSET LOCAL lock_timeout = '2s';\nALTER TABLE events ADD COLUMN venue_name text;\nALTER TABLE events ADD CONSTRAINT events_title_nonempty_ck\n  CHECK (length(btrim(title)) > 0);\nCOMMIT;\n```\n\n`lock_timeout` làm migration thất bại sớm thay vì chờ vô hạn. Nhưng transaction dài giữ lock tới `COMMIT`; chèn thêm một truy vấn kiểm tra lâu giữa các lệnh có thể kéo dài thời gian chặn. Một rollback cũng không trả lại thời gian mà người dùng đã chờ. Tính nguyên tử giải quyết trạng thái schema dở dang, không giải quyết độ sẵn sàng.\n\nMột số thao tác bị cấm trong transaction block, nổi bật là `CREATE INDEX CONCURRENTLY`. Vì thế đừng mặc định bọc toàn bộ file migration vào một transaction; hãy đọc đặc tính từng lệnh.\n\n### `ALTER TABLE` và cấp lock\n\n`ALTER TABLE` là họ thao tác, không phải một chi phí duy nhất. PostgreSQL thường cần `ACCESS EXCLUSIVE` lock cho thay đổi cấu trúc. Lock này xung đột với cả thao tác đọc thông thường. Nếu migration đang đợi một transaction cũ nhả lock, các truy vấn mới có thể xếp hàng sau migration và biến một chờ nhỏ thành nghẽn lớn.\n\nTrước khi chạy, kiểm tra transaction lâu và đặt timeout. Trong lúc chạy, `pg_stat_activity` cùng `pg_locks` giúp xác định phiên giữ hoặc chờ lock. Không nên giết phiên tùy tiện; phải biết đó là giao dịch nghiệp vụ nào và liệu retry có an toàn.\n\nThêm cột nullable không default thường chỉ đổi metadata và nhanh. Đổi kiểu bằng biểu thức `USING`, đặt `NOT NULL` khi dữ liệu chưa được chứng minh, hoặc thêm constraint cần quét bảng có thể tốn nhiều I/O. Chi tiết phụ thuộc phiên bản và biểu thức, nên quy trình đúng là thử trên bản sao có kích thước gần thật rồi quan sát, không thuộc lòng rằng mọi `ADD COLUMN` đều rẻ.\n\n### Mở rộng rồi thu hẹp: migration tương thích nhiều phiên bản\n\nGiả sử cần đổi `tickets.status` từ text sang kiểu có ý nghĩa mới và thêm thời điểm hủy. Thay trực tiếp có thể làm ứng dụng cũ hỏng. Chiến lược expand/contract chia thành các lần triển khai:\n\n1. Mở rộng: thêm `cancelled_at` nullable và constraint ở trạng thái chưa validate.\n2. Triển khai ứng dụng có thể đọc cả dạng cũ lẫn mới, ghi dữ liệu mới.\n3. Backfill theo lô nhỏ, mỗi lô commit riêng để tránh transaction và lock hàng quá dài.\n4. Validate luật sau khi dữ liệu đã sạch.\n5. Chuyển mọi bản ứng dụng sang schema mới.\n6. Thu hẹp: đặt `NOT NULL` hoặc xóa cột cũ ở một cửa sổ sau.\n\n```sql\nALTER TABLE tickets ADD COLUMN cancelled_at timestamptz;\nALTER TABLE tickets ADD CONSTRAINT tickets_cancel_time_ck\n  CHECK (status <> 'cancelled' OR cancelled_at IS NOT NULL) NOT VALID;\n\n-- Chạy sau khi backfill hoàn tất:\nALTER TABLE tickets VALIDATE CONSTRAINT tickets_cancel_time_ck;\n```\n\n`NOT VALID` nghĩa là constraint mới vẫn kiểm tra hàng được thêm hoặc sửa, nhưng PostgreSQL chưa quét toàn bộ hàng cũ ngay lúc thêm. `VALIDATE CONSTRAINT` thực hiện bước kiểm tra lịch sử riêng. Tách hai việc giúp kiểm soát thời điểm quét, chứ không miễn kiểm tra dữ liệu.\n\n### Đổi kiểu và mệnh đề `USING`\n\nKhi kiểu cũ không tự ép sang kiểu mới, `USING` nói cách biến đổi từng hàng. Đây là chỗ phải xử lý dữ liệu bẩn trước, bởi một giá trị sai khiến cả câu lệnh thất bại.\n\n```sql\nALTER TABLE events ADD COLUMN doors_open_at timestamptz;\n\n-- Ví dụ đổi cột văn bản đã được làm sạch sang timestamptz:\nALTER TABLE legacy_events\n  ALTER COLUMN starts_at TYPE timestamptz\n  USING starts_at::timestamptz;\n```\n\nViệc ép chuỗi thời gian còn phụ thuộc định dạng và múi giờ phiên. Migration đáng tin phải dùng dữ liệu có định dạng xác định, kiểm tra trước các hàng không ép được, và đặt múi giờ rõ ràng. Với bảng lớn, thêm cột mới, ghi song song và backfill thường dễ kiểm soát hơn một lần viết lại toàn bộ.\n\n### Index: DDL có ảnh hưởng trực tiếp đến query plan\n\nKhóa ngoại không tự tạo index ở phía tham chiếu trong PostgreSQL. `tickets.event_id REFERENCES events` bảo vệ toàn vẹn nhưng truy vấn liệt kê vé của một sự kiện vẫn có thể quét cả bảng. Ta thêm index theo mẫu truy vấn:\n\n```sql\nCREATE INDEX tickets_event_status_idx\n  ON tickets (event_id, status)\n  INCLUDE (price_cents, purchased_at);\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT price_cents, purchased_at\nFROM tickets\nWHERE event_id = 42 AND status = 'paid';\n```\n\nThứ tự cột bắt đầu bằng điều kiện equality thường cho phép thu hẹp theo `event_id`, rồi `status`. `INCLUDE` có thể hỗ trợ index-only scan khi visibility map và điều kiện khác phù hợp; nó không bảo đảm planner luôn chọn cách đó. Index làm tăng dung lượng, WAL và chi phí của mỗi lần ghi. Nếu trạng thái `paid` chiếm gần mọi hàng, một index chỉ trên status có độ chọn lọc thấp và có thể bị bỏ qua.\n\nTrên hệ thống đang ghi, dùng:\n\n```sql\nCREATE INDEX CONCURRENTLY tickets_event_status_live_idx\n  ON tickets (event_id, status);\n```\n\nCách concurrent tránh khóa chặn thao tác ghi trong phần lớn quá trình, nhưng chạy lâu hơn, làm nhiều lượt quét và không được đặt trong transaction block. Nếu thất bại, index có thể còn lại ở trạng thái invalid; phải kiểm tra catalog rồi xóa hoặc tạo lại, không chỉ chạy lại mù quáng.\n\n### `DROP`: dependency và bán kính phá hủy\n\n`DROP TABLE ticket_payments` xóa cấu trúc và dữ liệu. Mặc định `RESTRICT` từ chối nếu còn đối tượng phụ thuộc. `CASCADE` tự xóa dependency, tiện trong môi trường thử nghiệm nhưng nguy hiểm ở production vì một view hoặc constraint ngoài dự kiến cũng biến mất.\n\nQuy trình loại bỏ cột an toàn bắt đầu từ code: ngừng ghi, ngừng đọc, quan sát không còn truy cập, sao lưu hoặc xác nhận khả năng phục hồi, rồi mới `DROP`. Đổi tên cột thành `deprecated_...` trong một phiên phát hành có thể làm lỗi phụ thuộc ẩn lộ ra trước khi dữ liệu bị xóa; đổi tên vẫn có lock và không thay thế backup.\n\n```sql\nBEGIN;\nSET LOCAL lock_timeout = '2s';\nALTER TABLE tickets RENAME COLUMN legacy_note TO deprecated_legacy_note;\nCOMMIT;\n\n-- Chỉ ở migration sau, khi đã xác minh:\nALTER TABLE tickets DROP COLUMN deprecated_legacy_note;\n```\n\n### Idempotency không đồng nghĩa migration đúng\n\n`CREATE TABLE IF NOT EXISTS` giúp chạy lại không lỗi nếu đối tượng đã tồn tại, nhưng nó không chứng minh đối tượng có đúng cột, kiểu và constraint mong muốn. Hai môi trường có thể cùng “thành công” trong khi schema khác nhau. Migration có version, chạy đúng một lần và được ghi lịch sử thường đáng tin hơn phủ `IF EXISTS` khắp nơi.\n\nTương tự, `DROP TABLE IF EXISTS` che trường hợp sai tên hoặc migration trước chưa từng tạo bảng. Hãy dùng điều kiện tồn tại cho quy trình thực sự cần dung nạp trạng thái khác nhau, không dùng để biến mọi lỗi thành im lặng.\n\n### Khi nào không nên thực hiện DDL trực tiếp\n\nKhông chạy thay đổi phá vỡ tương thích khi còn nhiều phiên bản ứng dụng; không đổi kiểu nguyên khối khi chưa đo thời gian viết lại; không dùng `CASCADE` chỉ để vượt lỗi dependency; không thêm index theo cảm giác mà chưa có truy vấn và kế hoạch thực thi cần nó. Nếu luật chỉ là sở thích hiển thị thay đổi thường xuyên, nhét nó vào `CHECK` có thể buộc migration liên tục; ngược lại, bất biến bảo vệ tính đúng của dữ liệu nên ở database chứ không chỉ ở giao diện.\n\nDDL tốt là thiết kế vòng đời: tạo luật, đưa luật vào hệ thống đang chạy, quan sát tác động, rồi có đường lùi. Cú pháp chỉ là phần ngắn nhất.",

vi_du: "### Migration hoàn chỉnh có thể chạy trên PostgreSQL\n\nĐoạn sau giả định các bảng ban đầu đã tồn tại. Nó thêm bảng thanh toán, constraint có tên và index phục vụ truy vấn theo vé. Chạy bằng `psql` trên database thử nghiệm trước.\n\n```sql\nBEGIN;\nSET LOCAL lock_timeout = '3s';\nSET LOCAL statement_timeout = '30s';\n\nCREATE TABLE ticket_payments (\n    payment_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    ticket_id bigint NOT NULL REFERENCES tickets(ticket_id),\n    provider_ref text NOT NULL,\n    amount_cents integer NOT NULL,\n    payment_status text NOT NULL,\n    paid_at timestamptz,\n    CONSTRAINT ticket_payments_amount_positive_ck CHECK (amount_cents > 0),\n    CONSTRAINT ticket_payments_status_ck\n      CHECK (payment_status IN ('pending', 'succeeded', 'failed', 'refunded')),\n    CONSTRAINT ticket_payments_provider_ref_uq UNIQUE (provider_ref),\n    CONSTRAINT ticket_payments_paid_time_ck\n      CHECK (payment_status <> 'succeeded' OR paid_at IS NOT NULL)\n);\n\nCREATE INDEX ticket_payments_ticket_idx ON ticket_payments(ticket_id);\nCOMMIT;\n\nINSERT INTO ticket_payments\n  (ticket_id, provider_ref, amount_cents, payment_status, paid_at)\nVALUES\n  (1, 'pay_demo_001', 250000, 'succeeded', now());\n```\n\nCa biên cần thử: `amount_cents = 0` phải bị từ chối; hai hàng cùng `provider_ref` phải vi phạm unique; `succeeded` với `paid_at IS NULL` phải thất bại; `failed` không có `paid_at` vẫn hợp lệ. Nếu `ticket_id = 1` chưa tồn tại, insert thất bại vì khóa ngoại—đó là kết quả đúng, không phải lỗi của script.\n\nĐể xem planner có dùng index hay không, phải có đủ dữ liệu đại diện rồi chạy `ANALYZE ticket_payments`; với bảng chỉ một hàng, sequential scan thường rẻ hơn và hoàn toàn hợp lý. Không nên lấy việc “không dùng index” trên bảng nhỏ làm bằng chứng index sai.",

so_sanh: "### Chọn chiến lược thay đổi schema\n\n| Cách | Ưu điểm | Chi phí và rủi ro | Khi phù hợp |\n|---|---|---|---|\n| `ALTER` trực tiếp một lần | Ít bước, trạng thái cuối rõ | Có thể lock lâu, viết lại bảng, làm code cũ hỏng | Bảng nhỏ, cửa sổ bảo trì, đã đo trước |\n| Expand/contract | Tương thích nhiều phiên bản, dễ dừng giữa chặng | Nhiều lần triển khai, tạm thời có hai biểu diễn | Production cần hoạt động liên tục |\n| Tạo bảng mới rồi chuyển dữ liệu | Kiểm soát biến đổi và đối chiếu tốt | Cần đồng bộ ghi, thêm dung lượng, cutover phức tạp | Đổi mô hình lớn hoặc backfill nặng |\n| Constraint ở application | Thông báo lỗi theo ngữ cảnh, thay đổi nhanh | Bị bỏ qua bởi client khác và race condition | Luật trải nghiệm; vẫn cần database bảo vệ bất biến cốt lõi |\n| Constraint trong database | Một nguồn sự thật cho mọi luồng ghi | Có chi phí validate, thay đổi cần migration | Giá trị hợp lệ, quan hệ và tính duy nhất |\n\n`CREATE INDEX` thường chặn ghi nhiều hơn nhưng hoàn thành nhanh; `CREATE INDEX CONCURRENTLY` ưu tiên độ sẵn sàng đổi lấy thời gian và quy trình phục hồi phức tạp. Không có lựa chọn mặc định cho mọi môi trường: database trống lúc cài mới nên dùng cách thường trong transaction; bảng nóng ở production thường cần concurrent và giám sát riêng.",

loi_thuong_gap: [
  "Thêm cột `NOT NULL` rồi mới nghĩ đến dữ liệu cũ. Dấu hiệu là migration thất bại vì hàng hiện tại có `NULL`; sửa bằng thêm nullable, backfill theo lô, kiểm tra, sau đó mới siết luật.",
  "Cho rằng DDL có transaction nên không thể gây downtime. Dấu hiệu là session chờ lock và truy vấn xếp hàng; cần timeout, khảo sát transaction lâu và chia migration.",
  "Dùng `DROP ... CASCADE` để câu lệnh chạy cho nhanh. Dấu hiệu là view hoặc constraint biến mất ngoài danh sách dự kiến; phải liệt kê dependency và ưu tiên `RESTRICT`.",
  "Tạo index không khớp điều kiện truy vấn. Dấu hiệu từ `EXPLAIN` là sequential scan hoặc index chỉ lọc được rất ít; thiết kế theo equality, range, sort và độ chọn lọc thực tế.",
  "Quên index ở cột khóa ngoại phía con. Dấu hiệu là xóa/cập nhật hàng cha hoặc join theo khóa ngoại quét nhiều block của bảng con; khóa ngoại không tự tạo index đó.",
  "Dùng `IF NOT EXISTS` như công cụ đồng bộ schema. Dấu hiệu là migration báo thành công nhưng hai môi trường có định nghĩa khác nhau; phải quản lý version và kiểm tra catalog.",
  "Đổi kiểu dữ liệu mà không kiểm tra giá trị bẩn, múi giờ hoặc chi phí rewrite. Dấu hiệu là cast thất bại giữa chừng hoặc I/O tăng mạnh; chạy truy vấn tiền kiểm và diễn tập trên dữ liệu gần thật."
],

tu_kiem_tra: [
  "Vì sao `UNIQUE(event_id, seat_no)` vẫn cho phép nhiều vé có `seat_no IS NULL`, và điều đó có phù hợp vé đứng không?",
  "Transactional DDL bảo vệ điều gì, và vì sao nó không tự bảo vệ độ sẵn sàng của hệ thống?",
  "Hãy chia việc thêm cột bắt buộc `venue_id` cho bảng lớn thành các pha tương thích với ứng dụng cũ.",
  "Khi nào `NOT VALID` hữu ích? Nó kiểm tra hàng mới ra sao và phần việc nào vẫn còn phải làm?",
  "Vì sao planner có thể bỏ index trên bảng nhỏ hoặc cột độ chọn lọc thấp dù index tồn tại?",
  "Một `CREATE INDEX CONCURRENTLY` thất bại có thể để lại trạng thái gì, và vì sao không được chạy lại mù quáng?",
  "Bạn sẽ thu thập bằng chứng nào trước khi `DROP COLUMN` trên production, và đường phục hồi dữ liệu là gì?"
],

tai_nguyen: [
  "Tài liệu chính thức PostgreSQL, các chương Data Definition, DDL, Constraints, Indexes và Explicit Locking.",
  "“PostgreSQL: Up and Running” — Regina Obe và Leo Hsu, phần thiết kế database và quản trị đối tượng.",
  "“Designing Data-Intensive Applications” — Martin Kleppmann, các chương về mô hình dữ liệu, tiến hóa schema và độ tin cậy.",
  "“SQL Antipatterns” — Bill Karwin, các mẫu thiết kế schema dễ tạo nợ kỹ thuật và cách nhận diện."
],

lien_he: [
  "Nối sang mảng Kiến trúc phần mềm: expand/contract là cách phối hợp migration với nhiều phiên bản dịch vụ trong rolling deployment.",
  "Nối sang mảng Hệ điều hành: lock DDL, hàng chờ và transaction lâu là ví dụ thực tế của tương tranh và tài nguyên dùng chung.",
  "Nối sang mảng Bảo mật: quyền `CREATE`, `ALTER`, `DROP` cần tách khỏi tài khoản ứng dụng theo nguyên tắc đặc quyền tối thiểu.",
  "Nối sang mảng Đảm bảo chất lượng: migration cần test ca biên, diễn tập rollback và kiểm tra schema sau triển khai như một loại kiểm thử tích hợp."
]

};
