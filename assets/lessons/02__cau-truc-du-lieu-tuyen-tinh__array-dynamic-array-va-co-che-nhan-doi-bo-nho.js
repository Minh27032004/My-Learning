window.LESSONS["02:cau-truc-du-lieu-tuyen-tinh:array-dynamic-array-va-co-che-nhan-doi-bo-nho"] = {

tom_tat: [
  "**Module 1 đã chứng minh vì sao `append` là O(1) khấu hao. Bài này hỏi câu khác: vì sao mảng NHANH hơn mọi cấu trúc khác dù cùng bậc.** Câu trả lời là bố cục bộ nhớ, không phải Big-O.",
  "**`ds[i]` là O(1) nhờ một công thức số học: địa_chỉ = gốc + i × kích_thước_ô.** Không tìm kiếm, không duyệt — chỉ một phép nhân và một phép cộng.",
  "**`list` của Python KHÔNG phải mảng số — nó là mảng CON TRỎ.** Mỗi phần tử là một con trỏ 8 byte trỏ tới một `PyObject` nằm rải rác trên heap.",
  "**Cache locality là lý do thật khiến mảng nhanh.** CPU nạp cả một dòng 64 byte mỗi lần đọc RAM, nên duyệt mảng liên tục gần như miễn phí cho các phần tử kế tiếp.",
  "**Ba lựa chọn trong Python với ba đánh đổi rõ rệt:** `list` linh hoạt nhất, `array.array` tiết kiệm bộ nhớ, NumPy nhanh nhất cho tính toán số."
],

can_biet_truoc: [
  "Bài **Phân tích khấu hao** ở module 1 mảng này — cơ chế nhân đôi và vì sao `append` là O(1) khấu hao. Bài này **không lặp lại** phần đó.",
  "Bài **CPU, RAM, cache** ở module 1 mảng 01 — chênh lệch tốc độ giữa các tầng bộ nhớ; đây là kiến thức trung tâm của bài.",
  "Bài **Bộ nhớ ảo, phân trang** ở module 2 mảng 01 — vì sao bộ nhớ \"liên tục\" là liên tục theo địa chỉ ảo.",
  "Bài **Tham chiếu vs giá trị** và **`PyObject` header** ở module 7 mảng 01 — vì sao list Python lưu con trỏ chứ không lưu giá trị."
],

dinh_nghia: "Nói đời thường: một dãy hộp thư đánh số liền nhau trong sảnh chung cư. Muốn tới hộp số 47, bạn không phải đi từ hộp 1 — bạn đi thẳng tới đó vì biết mỗi hộp rộng bao nhiêu. Còn nếu các hộp thư nằm rải rác khắp toà nhà, mỗi hộp có một mẩu giấy chỉ đường tới hộp tiếp theo, bạn buộc phải đi lần lượt.\n\nCác khái niệm:\n\n- **Mảng** (*array*) — dãy phần tử **cùng kiểu**, **cùng kích thước**, nằm **liền nhau** trong bộ nhớ. Kích thước cố định.\n- **Mảng động** (*dynamic array*) — mảng tự nới rộng khi đầy. `list` của Python, `vector` của C++, `ArrayList` của Java.\n- **Truy cập ngẫu nhiên** (*random access*) — lấy phần tử thứ `i` với chi phí như nhau cho mọi `i`. Đây là đặc quyền của mảng.\n- **Công thức địa chỉ** — `địa_chỉ(i) = địa_chỉ_gốc + i × kích_thước_ô`. Lý do toán học của O(1).\n- **Dòng cache** (*cache line*) — đơn vị CPU nạp từ RAM, thường 64 byte. Đọc một byte thì nạp cả 64 byte quanh nó.\n- **Cache locality** — tính chất dữ liệu cần dùng nằm gần nhau trong bộ nhớ. Mảng có nó; cấu trúc liên kết thì không.\n- **Mảng con trỏ** — mảng mà mỗi ô chứa một *địa chỉ*, không chứa *giá trị*. `list` của Python là loại này.\n\nPhân biệt quan trọng nhất, và là trục của cả bài:\n\n```\nMẢNG SỐ THÔ (C, array.array, NumPy)\n    [ 3 ][ 7 ][ 1 ][ 9 ]          giá trị nằm NGAY trong mảng\n     4B   4B   4B   4B            liên tục hoàn toàn\n\nMẢNG CON TRỎ (list của Python)\n    [ptr][ptr][ptr][ptr]          mảng chỉ chứa ĐỊA CHỈ\n      │    │    │    └──→ PyObject(9)   ← nằm rải rác\n      │    │    └───────→ PyObject(1)      trên heap\n      │    └────────────→ PyObject(7)\n      └─────────────────→ PyObject(3)\n```\n\nCả hai đều cho `ds[i]` là O(1). Nhưng chúng khác nhau rất nhiều về **bộ nhớ thật** và **tốc độ duyệt** — và đó chính là nội dung bài này.",

vi_sao_can: "Module 1 dạy bạn tính bậc; bài này dạy thứ mà bậc không nói:\n\n- **Nó giải thích vì sao mảng là cấu trúc mặc định đúng.** Gần như mọi cấu trúc dữ liệu khác đều xây trên mảng: bảng băm, heap, thậm chí cả cây trong nhiều hiện thực. Hiểu mảng là hiểu nền của tất cả.\n- **Nó cho biết vì sao hai thuật toán cùng O(n) chênh nhau nhiều lần.** Duyệt mảng và duyệt linked list đều O(n), nhưng cái sau có thể chậm hơn nhiều lần trên dữ liệu lớn — vì cache, không vì bậc.\n- **Nó là cơ sở để chọn giữa `list`, `array.array` và NumPy.** Ba lựa chọn này chênh nhau gần mười lần về bộ nhớ và hàng chục lần về tốc độ tính toán. Chọn sai là lãng phí có thể đo được.\n- **Nó giải thích các chi phí ẩn đã gặp ở module 1.** Vì sao `insert(0, x)` là O(n), vì sao cắt lát là copy — cả hai suy ra trực tiếp từ tính liên tục của mảng.\n- **Nó là nền cho mảng 07.** Toàn bộ NumPy, xử lý ảnh, và tensor của deep learning đều là mảng nhiều chiều liên tục. Không nắm bố cục bộ nhớ thì không tối ưu được code ML.\n\nTình huống thật rất cụ thể: bạn xử lý dữ liệu cảm biến 50 triệu số thực trong Python. Dùng `list` thì chương trình ngốn khoảng 1,8 GB và bị container giới hạn 2 GB giết. Đổi sang `array.array(\"f\")` thì còn khoảng 200 MB, chạy được. Đổi tiếp sang NumPy thì vừa 200 MB vừa nhanh hơn hàng chục lần cho các phép tính. **Cả ba đều là \"mảng\" và đều O(1) truy cập — khác biệt nằm hoàn toàn ở bố cục bộ nhớ.**\n\nMột lưu ý về phạm vi: bài này **không** nhắc lại phân tích khấu hao (module 1 đã làm kỹ). Nó tập trung vào phần bậc phức tạp không nói được: bộ nhớ thật, cache, và cách chọn cấu trúc lưu trữ.",

co_che: "### Mảng ở tầng bộ nhớ\n\n```\nMảng 5 số nguyên 4 byte, bắt đầu ở địa chỉ 1000:\n\n địa chỉ   1000   1004   1008   1012   1016\n           ┌────┬────┬────┬────┬────┐\n giá trị   │ 42 │ 17 │  8 │ 99 │  3 │\n           └────┴────┴────┴────┴────┘\n chỉ số      0      1      2      3      4\n```\n\nBa tính chất bắt buộc, và mọi thứ khác suy ra từ chúng:\n\n```\n① LIÊN TỤC      các ô nằm sát nhau, không có khoảng trống\n② CÙNG KÍCH THƯỚC  mọi ô chiếm đúng số byte như nhau\n③ CÙNG KIỂU      để ② đúng\n```\n\n### Vì sao `ds[i]` là O(1)\n\nĐây là lý do toán học, và nó rất đơn giản:\n\n```\nđịa_chỉ(i) = địa_chỉ_gốc + i × kích_thước_ô\n\nVí dụ: ds[3] với gốc 1000, ô 4 byte\n       = 1000 + 3 × 4\n       = 1012                  ⭐ MỘT phép nhân, MỘT phép cộng\n```\n\n```\n⇒ Chi phí KHÔNG phụ thuộc i.\n  ds[0] và ds[999999] tốn như nhau.\n  Đó chính là định nghĩa của truy cập ngẫu nhiên O(1).\n```\n\nSo với linked list, khác biệt lộ ra ngay:\n\n```\nLINKED LIST: không có công thức địa chỉ\n\n  muốn tới nút thứ 3 → phải đi qua nút 0 → 1 → 2 → 3\n  ⇒ O(n)\n\nVÌ SAO: các nút nằm RẢI RÁC, địa chỉ nút sau chỉ biết được\n        sau khi đã đọc nút trước.\n```\n\nHệ quả suy ra trực tiếp từ tính liên tục — hai chi phí ẩn đã gặp ở module 1 giờ có lời giải thích:\n\n```\ninsert(0, x)  phải DỊCH toàn bộ n phần tử sang phải một ô → O(n)\npop(0)        phải DỊCH toàn bộ n−1 phần tử sang trái     → O(n)\nds[a:b]       phải COPY (b−a) ô sang vùng mới              → O(b−a)\n\n⇒ Cả ba KHÔNG phải quyết định thiết kế tuỳ tiện của Python.\n  Chúng là hệ quả bắt buộc của việc giữ mảng liên tục.\n```\n\n### `list` của Python là mảng CON TRỎ\n\nĐây là điểm quan trọng nhất mà người học Python hay hiểu sai:\n\n```\nds = [42, 17, 8]\n\n        list object                    heap\n     ┌──────────────┐\n     │ ob_refcnt    │\n     │ ob_type      │\n     │ ob_size = 3  │       ┌────────────────┐\n     │ allocated= 4 │   ┌──→│ PyObject int 42│\n     │ ob_item ─────┼─┐ │   └────────────────┘\n     └──────────────┘ │ │   ┌────────────────┐\n                      │ │┌─→│ PyObject int 17│\n              ┌───────┘ ││  └────────────────┘\n              ↓         ││  ┌────────────────┐\n         ┌────┬────┬────┐│┌→│ PyObject int  8│\n         │ptr─┼ptr─┼ptr─┘││ └────────────────┘\n         └────┴────┴─────┘│\n           └──────────────┘\n         MẢNG CON TRỎ (liên tục)   GIÁ TRỊ (rải rác trên heap)\n```\n\nHai tầng gián tiếp, và mỗi tầng có chi phí:\n\n```\nTẦNG 1: mảng con trỏ  — LIÊN TỤC, 8 byte mỗi ô\nTẦNG 2: các PyObject  — RẢI RÁC, ~28 byte mỗi int nhỏ\n```\n\nHệ quả về bộ nhớ:\n\n```\nlist 1 triệu số nguyên:\n    mảng con trỏ:  1.000.000 × 8  =  8 MB\n    các PyObject:  1.000.000 × 28 = 28 MB\n    ────────────────────────────────────\n    tổng                          ≈ 36 MB\n\narray.array(\"i\") 1 triệu số nguyên:\n    số thô liên tục: 1.000.000 × 4 = 4 MB     ⭐ ít hơn 9 lần\n```\n\nHệ quả về **tốc độ duyệt** còn quan trọng hơn, và đó là phần tiếp theo.\n\n⚠️ Một ngoại lệ đáng biết: Python **tái dùng** object cho số nguyên nhỏ (khoảng −5 tới 256) và một số chuỗi ngắn. Nên `[1] * 1_000_000` chỉ tốn phần mảng con trỏ (8 MB), vì cả triệu con trỏ cùng trỏ tới **một** object.\n\n### Cache locality — lý do thật khiến mảng nhanh\n\nNối trực tiếp về bài cache CPU ở module 1 mảng 01:\n\n```\nCPU KHÔNG đọc từng byte từ RAM. Nó nạp cả một DÒNG CACHE (~64 byte).\n\nĐọc ds[0] của một mảng int 4 byte:\n    → nạp 64 byte = ds[0] tới ds[15]\n    → 15 phần tử tiếp theo ĐÃ nằm trong cache\n    → đọc chúng gần như MIỄN PHÍ\n```\n\n```\nCHI PHÍ THEO TẦNG (thứ tự độ lớn, không phải con số chính xác):\n\n    cache L1   ~1 đơn vị\n    cache L2   ~3 đơn vị\n    cache L3   ~10 đơn vị\n    RAM        ~100+ đơn vị      ⚠️ đắt hơn L1 hàng trăm lần\n```\n\nBa cấu trúc, ba mức locality:\n\n```\nMẢNG SỐ THÔ (array.array, NumPy)\n    [3][7][1][9][2][8]...        ⭐ locality HOÀN HẢO\n    một lần nạp cache = 16 phần tử\n\nLIST PYTHON\n    [ptr][ptr][ptr]...           mảng con trỏ liên tục\n      ↓    ↓    ↓                nhưng GIÁ TRỊ rải rác\n     obj  obj  obj               → mỗi lần đọc giá trị có thể cache miss\n\nLINKED LIST\n    nút → nút → nút              ⚠️ locality TỆ NHẤT\n    mỗi nút một chỗ, mỗi bước một lần đọc RAM\n```\n\nĐây là câu trả lời cho câu hỏi của bài: **hai thuật toán cùng O(n) chênh nhau nhiều lần vì locality, không vì bậc.** Big-O đếm số thao tác; nó không đếm chi phí *của mỗi* thao tác.\n\n### Ba lựa chọn trong Python\n\n```\n                 list          array.array      NumPy ndarray\n                 ─────────     ───────────      ──────────────\nLưu gì           con trỏ       số THÔ           số THÔ\nBộ nhớ /1tr int  ~36 MB        ~4 MB            ~4 MB\nKiểu phần tử     bất kỳ, trộn  MỘT kiểu số      MỘT kiểu số\nTruy cập ds[i]   O(1)          O(1)             O(1)\nPhép tính vector ✗             ✗                ⭐ ✓\nNhiều chiều      lồng list     ✗                ⭐ ✓\nCó sẵn           ✓             ✓ (thư viện chuẩn) ✗ phải cài\n```\n\nKhi nào dùng cái nào:\n\n```\nlist          mặc định. Dữ liệu trộn kiểu, n nhỏ–vừa, cần linh hoạt.\narray.array   n rất lớn, cùng một kiểu số, KHÔNG cần tính toán vector.\n              → tiết kiệm bộ nhớ mà không cần thêm phụ thuộc.\nNumPy         tính toán số học trên dữ liệu lớn. Gần như luôn đúng\n              cho khoa học dữ liệu và ML.\n```\n\nMã kiểu của `array.array` — bảng cần tra khi dùng:\n\n```\n\"b\" / \"B\"   int 1 byte có dấu / không dấu\n\"h\" / \"H\"   int 2 byte\n\"i\" / \"I\"   int 4 byte\n\"l\" / \"L\"   int 4 hoặc 8 byte (tuỳ nền tảng)\n\"q\" / \"Q\"   int 8 byte\n\"f\"         float 4 byte\n\"d\"         float 8 byte (như float của Python)\n```\n\n⚠️ Đánh đổi của `array.array`: **nó có giới hạn giá trị**. `array(\"i\")` chỉ chứa được số trong khoảng khoảng ±2,1 tỉ; vượt là `OverflowError`. `list` thì không giới hạn vì `int` của Python không giới hạn.\n\n### Mảng nhiều chiều — bố cục hàng-chính\n\n```\nMa trận 3×4 lưu trong bộ nhớ MỘT CHIỀU:\n\n    ma trận            bộ nhớ (hàng-chính, row-major)\n    ┌─────────┐\n    │ a b c d │        [a][b][c][d][e][f][g][h][i][j][k][l]\n    │ e f g h │         └─hàng 0─┘└─hàng 1─┘└─hàng 2─┘\n    │ i j k l │\n    └─────────┘\n\nCÔNG THỨC: địa_chỉ(hàng, cột) = gốc + (hàng × số_cột + cột) × kích_thước_ô\n```\n\nHệ quả rất thực tế cho hiệu năng:\n\n```\nDUYỆT THEO HÀNG (đúng bố cục)     DUYỆT THEO CỘT (ngược bố cục)\nfor h in range(cao):               for c in range(rong):\n    for c in range(rong):              for h in range(cao):\n        xu_ly(m[h][c])                     xu_ly(m[h][c])\n\n⭐ đọc liên tục → cache tốt        ⚠️ nhảy cách nhau rong ô mỗi bước\n                                      → cache miss liên tục\n```\n\nVới ma trận lớn, khác biệt giữa hai vòng lặp này có thể **nhiều lần** — dù chúng làm đúng cùng số thao tác và cùng O(n·m). Đây là ví dụ rõ nhất cho việc **thứ tự truy cập quan trọng ngang bậc phức tạp.**\n\n⚠️ Trong Python thuần, `m[h][c]` với `m` là list lồng list thì locality vốn đã tệ (mỗi hàng là một list riêng nằm rải rác), nên hiệu ứng này rõ nhất với NumPy.\n\n### Bảng chi phí thao tác — và lý do\n\n```\nTHAO TÁC            BẬC        LÝ DO\n──────────────      ────────   ──────────────────────────────────\nds[i]               O(1)       công thức địa chỉ\nds[i] = x           O(1)       ghi thẳng vào ô\nlen(ds)             O(1)       ob_size lưu sẵn\nds.append(x)        O(1) kh    ghi ô trống (module 1)\nds.pop()            O(1) kh    giảm size\nds.insert(0, x)     O(n)       ⚠️ DỊCH mọi phần tử\nds.pop(0)           O(n)       ⚠️ DỊCH mọi phần tử\nds.remove(x)        O(n)       tìm + dịch\nx in ds             O(n)       duyệt tuần tự\nds[a:b]             O(b−a)     ⚠️ COPY\nds1 + ds2           O(n+m)     tạo mảng mới\nds.sort()           O(n log n) Timsort\nds.reverse()        O(n)       hoán vị tại chỗ\nmin/max/sum(ds)     O(n)       duyệt\n```\n\nMọi dòng O(n) trong bảng đều có **cùng một nguyên nhân**: phải giữ mảng liên tục, nên thêm/bớt ở giữa buộc phải dịch phần còn lại.\n\n### Khi nào KHÔNG nên dùng mảng\n\nPhản biện cần thiết:\n\n```\nMẢNG TỆ KHI:\n· Thêm/xoá ở ĐẦU hoặc GIỮA thường xuyên     → deque hoặc linked list\n· Kích thước biến động cực mạnh              → deque (không copy khi nở)\n· Cần độ trễ ỔN ĐỊNH                          → mảng cấp phát trước\n· Cần tra cứu theo KHOÁ, không theo chỉ số    → dict\n· Cần luôn lấy được min/max                   → heap\n\nMẢNG TỐT KHI:\n· Truy cập theo chỉ số                        ⭐ không gì thay được\n· Duyệt tuần tự nhiều                          ⭐ cache locality\n· Thêm/xoá chủ yếu ở CUỐI\n· Kích thước biết trước hoặc tăng đều\n· Tính toán số học hàng loạt (NumPy)\n```\n\nMột nhận xét đáng nhớ: **mảng thắng ở hai điều mà không cấu trúc nào khác có cùng lúc — truy cập O(1) theo chỉ số và cache locality tốt.** Đó là lý do nó là mặc định đúng cho phần lớn trường hợp, và các cấu trúc khác là ngoại lệ có lý do cụ thể.",

vi_du: "### Xác nhận bố cục bộ nhớ\n\n```python\nimport sys\nimport array\n\n# list — mảng CON TRỎ\nds = [42, 17, 8]\nprint(f\"list 3 phần tử, vỏ:      {sys.getsizeof(ds):>6} byte\")\nprint(f\"một PyObject int:        {sys.getsizeof(42):>6} byte\")\nprint(f\"⇒ tổng thật ≈ {sys.getsizeof(ds) + 3 * sys.getsizeof(42)} byte\")\n\n# array.array — số THÔ\nar = array.array(\"i\", [42, 17, 8])\nprint(f\"\\narray('i') 3 phần tử:     {sys.getsizeof(ar):>6} byte\")\nprint(f\"kích thước một ô:        {ar.itemsize:>6} byte\")\n```\n\n```\nlist 3 phần tử, vỏ:          80 byte\nmột PyObject int:            28 byte\n⇒ tổng thật ≈ 164 byte\n\narray('i') 3 phần tử:        76 byte\nkích thước một ô:             4 byte\n```\n\nĐo bộ nhớ thật ở quy mô lớn — `getsizeof` không đủ vì nó **không đệ quy**:\n\n```python\nimport tracemalloc\nimport array\n\ndef do_bo_nho(ten: str, tao) -> None:\n    tracemalloc.start()\n    obj = tao()\n    hien_tai, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n    print(f\"{ten:>34}: {hien_tai / 1024 / 1024:>7.1f} MB\")\n    del obj\n\nn = 1_000_000\ndo_bo_nho(\"list(range(n))\", lambda: list(range(n)))\ndo_bo_nho(\"array.array('i', range(n))\", lambda: array.array(\"i\", range(n)))\ndo_bo_nho(\"array.array('q', range(n))\", lambda: array.array(\"q\", range(n)))\ndo_bo_nho(\"[1] * n  (dùng chung object)\", lambda: [1] * n)\n```\n\n```\nKẾT QUẢ có dạng:\n\n                    list(range(n)):    ~35.0 MB\n        array.array('i', range(n)):     ~4.0 MB    ⭐ ít hơn ~9 lần\n        array.array('q', range(n)):     ~8.0 MB\n      [1] * n  (dùng chung object):     ~7.6 MB    ⭐ chỉ mảng con trỏ\n```\n\nDòng cuối chứng minh cấu trúc hai tầng: `[1] * n` chỉ tốn phần **mảng con trỏ** (8 byte × n), vì cả triệu con trỏ cùng trỏ tới **một** `PyObject` số 1 mà Python tái dùng.\n\n```python\n# Chứng minh trực tiếp: cùng một object\nds = [1] * 5\nprint([id(x) for x in ds])          # ⭐ năm id GIỐNG HỆT nhau\nprint(ds[0] is ds[4])                # True\n\n# Với số lớn thì không tái dùng\nds2 = [10_000] * 3\nprint(ds2[0] is ds2[1])              # True — vì [x]*n copy CON TRỎ\n\nds3 = [10_000, 10_000, 10_000]       # ba literal riêng\nprint(ds3[0] is ds3[1])              # có thể False tuỳ cách Python tối ưu\n```\n\n### Truy cập O(1) — xác nhận bằng phép đo\n\n```python\nimport time\n\ndef do_truy_cap(ds, vi_tri: int, so_lan: int = 1_000_000) -> float:\n    t0 = time.perf_counter()\n    for _ in range(so_lan):\n        _ = ds[vi_tri]\n    return time.perf_counter() - t0\n\nn = 10_000_000\nds = list(range(n))\n\nprint(f\"{'vị trí':>12} {'thời gian 1tr lần':>20}\")\nfor vi_tri in (0, n // 2, n - 1):\n    t = do_truy_cap(ds, vi_tri)\n    print(f\"{vi_tri:>12,} {t:>19.4f}s\")\n```\n\n```\n      vị trí    thời gian 1tr lần\n           0              ~0.050s\n   5,000,000              ~0.050s\n   9,999,999              ~0.050s\n\n⭐ BA con số GẦN BẰNG NHAU dù vị trí cách nhau 10 triệu ô.\n  Đó là bằng chứng thực nghiệm của công thức địa chỉ.\n```\n\nSo với cấu trúc **không** có truy cập ngẫu nhiên:\n\n```python\nfrom collections import deque\n\ndq = deque(range(1_000_000))\n\nfor vi_tri in (0, 500_000, 999_999):\n    t0 = time.perf_counter()\n    for _ in range(10_000):              # ⚠️ chỉ 10k lần, không phải 1tr\n        _ = dq[vi_tri]\n    print(f\"deque[{vi_tri:>7,}]: {time.perf_counter()-t0:>8.4f}s\")\n```\n\n```\ndeque[      0]:  ~0.001s      ⭐ đầu — nhanh\ndeque[500,000]:  ~0.400s      ⚠️ giữa — CHẬM HƠN HÀNG TRĂM LẦN\ndeque[999,999]:  ~0.001s      ⭐ cuối — nhanh\n\n⇒ deque là cấu trúc KHỐI: nhanh ở hai đầu, O(n) ở giữa.\n  Đây là đánh đổi của nó (bài deque sẽ nói kỹ).\n```\n\n### Cache locality — đo được\n\n```python\nimport time\n\nn = 20_000_000\nds = list(range(n))\n\n# ① Duyệt TUẦN TỰ — cache locality tốt\nt0 = time.perf_counter()\ns = 0\nfor i in range(n):\n    s += ds[i]\nt_tuan_tu = time.perf_counter() - t0\n\n# ② Duyệt NHẢY CÁCH — phá cache locality\nBUOC = 16          # nhảy đủ xa để mỗi lần là một dòng cache mới\nt0 = time.perf_counter()\ns = 0\nfor bd in range(BUOC):\n    for i in range(bd, n, BUOC):\n        s += ds[i]\nt_nhay = time.perf_counter() - t0\n\nprint(f\"duyệt tuần tự : {t_tuan_tu:>7.3f}s\")\nprint(f\"duyệt nhảy cách: {t_nhay:>7.3f}s\")\nprint(f\"chậm hơn: {t_nhay/t_tuan_tu:.2f} lần\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\n  duyệt tuần tự : nhanh hơn\n  duyệt nhảy cách: chậm hơn đáng kể\n\n⚠️ CÙNG SỐ THAO TÁC (n phép cộng), cùng O(n).\n  Khác biệt hoàn toàn do THỨ TỰ TRUY CẬP bộ nhớ.\n\n⚠️ Với list Python hiệu ứng bị làm mờ vì giá trị vốn đã rải rác;\n   với NumPy thì rõ hơn nhiều — xem ví dụ dưới.\n```\n\nHiệu ứng rõ nhất trên NumPy, nơi dữ liệu thật sự liên tục:\n\n```python\ntry:\n    import numpy as np\n    import time\n\n    a = np.arange(20_000_000, dtype=np.int64)\n\n    t0 = time.perf_counter()\n    s1 = int(a.sum())                        # ⭐ liên tục\n    t_lien_tuc = time.perf_counter() - t0\n\n    t0 = time.perf_counter()\n    s2 = int(a[::16].sum() * 1)              # ⚠️ bước nhảy 16\n    t_nhay = time.perf_counter() - t0\n\n    print(f\"sum toàn mảng (n phần tử) : {t_lien_tuc:.4f}s\")\n    print(f\"sum bước nhảy 16 (n/16)   : {t_nhay:.4f}s\")\n    print(f\"⇒ chỉ ít hơn 16 lần công việc mà KHÔNG nhanh hơn 16 lần\")\nexcept ImportError:\n    print(\"(NumPy chưa cài — bỏ qua)\")\n```\n\n```\n⭐ Phép sum trên 1/16 số phần tử KHÔNG nhanh hơn 16 lần,\n  vì mỗi phần tử vẫn kéo về một dòng cache 64 byte mà chỉ dùng 8 byte.\n\n⇒ Cache locality là chi phí THẬT, đo được, và Big-O không thấy nó.\n```\n\n### Ba lựa chọn — so sánh đầy đủ\n\n```python\nimport array\nimport sys\nimport time\nimport tracemalloc\n\nn = 5_000_000\n\ndef do_ca_hai(ten: str, tao, tinh_tong) -> None:\n    tracemalloc.start()\n    obj = tao()\n    bo_nho, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n\n    t0 = time.perf_counter()\n    kq = tinh_tong(obj)\n    t = time.perf_counter() - t0\n\n    print(f\"{ten:>16}  bộ nhớ {bo_nho/1024/1024:>7.1f} MB   \"\n          f\"tổng {t:>7.4f}s   kết quả {kq}\")\n\ndo_ca_hai(\"list\", lambda: list(range(n)), sum)\ndo_ca_hai(\"array.array('i')\", lambda: array.array(\"i\", range(n)), sum)\n\ntry:\n    import numpy as np\n    do_ca_hai(\"numpy int32\",\n              lambda: np.arange(n, dtype=np.int32),\n              lambda a: int(a.sum()))\nexcept ImportError:\n    print(\"(NumPy chưa cài)\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\n            list  bộ nhớ  ~175 MB   tổng ~0.09s\narray.array('i')  bộ nhớ   ~20 MB   tổng ~0.35s   ⚠️ TỔNG CHẬM HƠN!\n     numpy int32  bộ nhớ   ~20 MB   tổng ~0.005s  ⭐ nhanh nhất\n```\n\nDòng giữa gây bất ngờ và **đáng giải thích kỹ**:\n\n```\nVÌ SAO sum(array.array) CHẬM HƠN sum(list)?\n\n  array.array lưu số THÔ (4 byte).\n  Nhưng sum() của Python cần PyObject để cộng.\n  ⇒ mỗi phần tử phải được BỌC thành PyObject mới rồi mới cộng\n  ⇒ n lần cấp phát object — rất đắt\n\n  list đã có sẵn PyObject, chỉ cần đọc con trỏ.\n\n⇒ array.array tiết kiệm BỘ NHỚ nhưng KHÔNG nhanh hơn cho\n  các phép tính Python thuần. Nó chỉ nhanh khi truyền cho\n  thư viện C hoặc ghi ra file nhị phân.\n\n⇒ NumPy vừa tiết kiệm bộ nhớ VỪA nhanh, vì phép cộng chạy\n  hoàn toàn trong C, không tạo PyObject nào.\n```\n\nĐây là đánh đổi thật mà nhiều tài liệu bỏ qua: **`array.array` không phải \"list nhanh hơn\" — nó là \"list tốn ít bộ nhớ hơn\".** Chọn nó khi bộ nhớ là ràng buộc, không phải khi tốc độ là ràng buộc.\n\n### Tự cài đặt mảng động\n\nĐể thấy rõ cơ chế — chú ý bài này **dùng lại** kết luận khấu hao của module 1 chứ không chứng minh lại:\n\n```python\nfrom typing import Any, Iterator\n\n\nclass MangDong:\n    \"\"\"Mảng động tối giản, mô phỏng list của Python.\n\n    Dùng list Python làm 'vùng nhớ thô' — trong C đây sẽ là malloc.\n    Cơ chế nhân đôi và phân tích khấu hao: xem bài 'Phân tích khấu hao'\n    ở module 1 mảng này.\n    \"\"\"\n\n    def __init__(self, dung_luong_ban_dau: int = 4) -> None:\n        if dung_luong_ban_dau < 1:\n            raise ValueError(\"dung lượng ban đầu phải ≥ 1\")\n        self._o: list[Any] = [None] * dung_luong_ban_dau\n        self._size = 0\n\n    # ---- Truy vấn: O(1) ----\n\n    def __len__(self) -> int:\n        return self._size\n\n    def __getitem__(self, i: int) -> Any:\n        i = self._chuan_hoa(i)\n        return self._o[i]                    # ⭐ công thức địa chỉ, O(1)\n\n    def __setitem__(self, i: int, x: Any) -> None:\n        i = self._chuan_hoa(i)\n        self._o[i] = x\n\n    def _chuan_hoa(self, i: int) -> int:\n        \"\"\"Hỗ trợ chỉ số âm như Python. Raises: IndexError.\"\"\"\n        if i < 0:\n            i += self._size\n        if not 0 <= i < self._size:\n            raise IndexError(f\"chỉ số {i} ngoài phạm vi 0..{self._size - 1}\")\n        return i\n\n    # ---- Thêm ở CUỐI: O(1) khấu hao ----\n\n    def append(self, x: Any) -> None:\n        if self._size == len(self._o):\n            self._cap_phat_lai(len(self._o) * 2)\n        self._o[self._size] = x\n        self._size += 1\n\n    # ---- Thêm ở GIỮA: O(n) vì phải DỊCH ----\n\n    def insert(self, i: int, x: Any) -> None:\n        if not 0 <= i <= self._size:\n            raise IndexError(f\"chỉ số chèn {i} ngoài phạm vi 0..{self._size}\")\n        if self._size == len(self._o):\n            self._cap_phat_lai(len(self._o) * 2)\n        # ⭐ DỊCH từ CUỐI về, nếu dịch từ đầu sẽ ghi đè\n        for j in range(self._size, i, -1):\n            self._o[j] = self._o[j - 1]\n        self._o[i] = x\n        self._size += 1\n\n    def pop(self, i: int | None = None) -> Any:\n        if self._size == 0:\n            raise IndexError(\"pop từ mảng rỗng\")\n        if i is None:\n            i = self._size - 1\n        i = self._chuan_hoa(i)\n        x = self._o[i]\n        for j in range(i, self._size - 1):    # dịch trái\n            self._o[j] = self._o[j + 1]\n        self._o[self._size - 1] = None        # ⭐ bỏ tham chiếu để GC thu hồi\n        self._size -= 1\n        return x\n\n    # ---- Nội bộ ----\n\n    def _cap_phat_lai(self, dung_luong_moi: int) -> None:\n        moi: list[Any] = [None] * dung_luong_moi\n        for j in range(self._size):\n            moi[j] = self._o[j]\n        self._o = moi\n\n    def __iter__(self) -> Iterator[Any]:\n        for j in range(self._size):\n            yield self._o[j]\n\n    def __repr__(self) -> str:\n        return f\"MangDong({list(self)}, dung_luong={len(self._o)})\"\n\n    @property\n    def dung_luong(self) -> int:\n        return len(self._o)\n```\n\nKiểm tra đầy đủ, gồm cả ca biên:\n\n```python\nm = MangDong()\nfor x in (10, 20, 30):\n    m.append(x)\nassert list(m) == [10, 20, 30]\nassert len(m) == 3\nassert m[0] == 10 and m[-1] == 30        # ⭐ chỉ số âm\n\nm.insert(1, 15)\nassert list(m) == [10, 15, 20, 30]\n\nm.insert(0, 5)                            # chèn đầu\nassert list(m) == [5, 10, 15, 20, 30]\n\nm.insert(len(m), 40)                      # chèn cuối\nassert list(m) == [5, 10, 15, 20, 30, 40]\n\nassert m.pop() == 40\nassert m.pop(0) == 5\nassert list(m) == [10, 15, 20, 30]\n\n# ---- Ca biên ----\nrong = MangDong()\nassert len(rong) == 0\nassert list(rong) == []\n\ntry:\n    rong[0]\nexcept IndexError as e:\n    print(f\"✓ bắt đúng: {e}\")\n\ntry:\n    rong.pop()\nexcept IndexError as e:\n    print(f\"✓ bắt đúng: {e}\")\n\ntry:\n    m.insert(100, 1)\nexcept IndexError as e:\n    print(f\"✓ bắt đúng: {e}\")\n\nmot = MangDong()\nmot.append(42)\nassert mot.pop() == 42 and len(mot) == 0   # biên: một phần tử\n\nprint(\"mọi ca kiểm thử đạt\")\n```\n\nHai chi tiết thiết kế đáng học trong đoạn code trên:\n\n```\n① insert() DỊCH TỪ CUỐI VỀ, không dịch từ đầu\n   Dịch từ đầu: o[1]=o[0], o[2]=o[1] ← o[1] ĐÃ BỊ GHI ĐÈ\n   ⇒ mất dữ liệu. Đây là lỗi kinh điển khi tự cài mảng.\n\n② pop() gán None vào ô cuối\n   Không gán thì mảng vẫn giữ THAM CHIẾU tới object đã bị xoá\n   ⇒ GC không thu hồi được ⇒ rò rỉ bộ nhớ\n   (nối về bài đếm tham chiếu, module 7 mảng 01)\n```\n\nĐiểm ② là loại lỗi không gây sai kết quả nhưng gây rò rỉ bộ nhớ âm thầm — đúng loại bug khó phát hiện nhất.\n\nQuan sát dung lượng nở ra:\n\n```python\nm = MangDong(dung_luong_ban_dau=1)\ntruoc = m.dung_luong\nprint(f\"{'size':>6} {'dung lượng':>12} {'lãng phí':>10}\")\nfor i in range(20):\n    m.append(i)\n    if m.dung_luong != truoc:\n        lang_phi = 1 - len(m) / m.dung_luong\n        print(f\"{len(m):>6} {m.dung_luong:>12} {lang_phi:>9.0%}\")\n        truoc = m.dung_luong\n```\n\n```\n  size   dung lượng   lãng phí\n     2            2         0%\n     3            4        25%\n     5            8        38%\n     9           16        44%\n    17           32        47%\n\n⭐ Ngay sau khi nhân đôi, lãng phí gần 50% — đúng như phân tích\n  ở bài khấu hao. Đó là cái giá của hệ số 2.\n```\n\n### Vì sao `insert(0, x)` là O(n) — chứng minh\n\n```python\nimport time\n\nprint(f\"{'n':>9} {'append (cuối)':>16} {'insert(0) (đầu)':>18} {'tỉ lệ':>10}\")\nfor n in (10_000, 20_000, 40_000):\n    ds = []\n    t0 = time.perf_counter()\n    for i in range(n):\n        ds.append(i)                  # ⭐ O(1) khấu hao\n    t_cuoi = time.perf_counter() - t0\n\n    ds = []\n    t0 = time.perf_counter()\n    for i in range(n):\n        ds.insert(0, i)               # ⚠️ O(n) mỗi lần → O(n²) tổng\n    t_dau = time.perf_counter() - t0\n\n    print(f\"{n:>9,} {t_cuoi:>15.4f}s {t_dau:>17.4f}s {t_dau/t_cuoi:>9.0f}x\")\n```\n\n```\n        n    append (cuối)    insert(0) (đầu)      tỉ lệ\n   10,000          ~0.001s            ~0.020s        ~20x\n   20,000          ~0.002s            ~0.080s        ~40x\n   40,000          ~0.004s            ~0.320s        ~80x\n\n⭐ ĐỌC CỘT CUỐI: tỉ lệ TĂNG GẤP ĐÔI khi n gấp đôi.\n  Đó là dấu hiệu hai bậc khác nhau: O(n) và O(n²).\n\n⇒ append: n×2 → thời gian ×2   (O(n) tổng)\n  insert(0): n×2 → thời gian ×4 (O(n²) tổng)\n```\n\nCách sửa khi cần thêm ở đầu:\n\n```python\nfrom collections import deque\n\n# ✓ deque — O(1) ở CẢ HAI đầu\ndq: deque[int] = deque()\nfor i in range(40_000):\n    dq.appendleft(i)                  # ⭐ O(1)\n\n# ✓ Hoặc: thêm ở cuối rồi ĐẢO một lần\nds = []\nfor i in range(40_000):\n    ds.append(i)\nds.reverse()                          # ⭐ O(n) MỘT lần\n```\n\nCách thứ hai đáng nhớ vì nó áp dụng được rộng: **nếu bạn định `insert(0)` n lần, hãy `append` n lần rồi `reverse` một lần** — O(n) thay vì O(n²).\n\n### Cắt lát là COPY — hệ quả thực tế\n\n```python\nimport time\n\nds = list(range(1_000_000))\n\n# ⚠️ Cắt lát tạo list MỚI\nt0 = time.perf_counter()\ncat = ds[:500_000]\nprint(f\"ds[:500k] (copy 500k ô): {time.perf_counter()-t0:.4f}s\")\n\n# ⭐ islice không copy — trả iterator\nfrom itertools import islice\nt0 = time.perf_counter()\nit = islice(ds, 500_000)\nprint(f\"islice (không copy)    : {time.perf_counter()-t0:.6f}s\")\n```\n\n```\nds[:500k] (copy 500k ô): ~0.0050s\nislice (không copy)    : ~0.000002s      ⭐ nhanh hơn hàng nghìn lần\n\n⚠️ Nhưng islice trả ITERATOR: duyệt một lần, không có len(),\n   không truy cập theo chỉ số. (Bài độ phức tạp không gian, module 1.)\n```\n\nBẫy kinh điển — cắt lát trong vòng lặp:\n\n```python\n# ❌ O(n²) — mỗi vòng copy phần còn lại\ndef xu_ly_v1(ds: list[int]) -> list[int]:\n    kq = []\n    while ds:\n        kq.append(ds[0])\n        ds = ds[1:]                   # ⚠️ COPY n−1 phần tử\n    return kq\n\n# ✓ O(n) — dùng chỉ số\ndef xu_ly_v2(ds: list[int]) -> list[int]:\n    return [ds[i] for i in range(len(ds))]\n\n# ✓ O(n) — hoặc đơn giản là duyệt\ndef xu_ly_v3(ds: list[int]) -> list[int]:\n    return list(ds)\n```\n\n### Ma trận — thứ tự duyệt quan trọng\n\n```python\ntry:\n    import numpy as np\n    import time\n\n    cao, rong = 4_000, 4_000\n    m = np.arange(cao * rong, dtype=np.int64).reshape(cao, rong)\n\n    # ① Theo HÀNG — đúng bố cục row-major\n    t0 = time.perf_counter()\n    s = 0\n    for h in range(cao):\n        s += int(m[h].sum())          # ⭐ mỗi hàng liên tục\n    t_hang = time.perf_counter() - t0\n\n    # ② Theo CỘT — ngược bố cục\n    t0 = time.perf_counter()\n    s = 0\n    for c in range(rong):\n        s += int(m[:, c].sum())       # ⚠️ mỗi cột nhảy cách 'rong' ô\n    t_cot = time.perf_counter() - t0\n\n    print(f\"duyệt theo HÀNG: {t_hang:>7.3f}s\")\n    print(f\"duyệt theo CỘT : {t_cot:>7.3f}s\")\n    print(f\"chậm hơn: {t_cot/t_hang:.1f} lần\")\nexcept ImportError:\n    print(\"(NumPy chưa cài — bỏ qua)\")\n```\n\n```\nXU HƯỚNG: duyệt theo CỘT chậm hơn đáng kể.\n\n⚠️ CÙNG số phần tử, CÙNG O(n·m), CÙNG số phép cộng.\n  Khác biệt hoàn toàn do THỨ TỰ TRUY CẬP bộ nhớ.\n\n⇒ Đây là điều Big-O KHÔNG nói được, và là lý do bài này tồn tại.\n```\n\nÁp dụng thực tế trong xử lý ảnh — nối về mảng 07:\n\n```python\n# Ảnh RGB lưu dạng (cao, rộng, 3) — bố cục row-major\n\n# ✓ Duyệt theo hàng — thân thiện cache\nfor h in range(cao):\n    for c in range(rong):\n        xu_ly(anh[h, c])\n\n# ⚠️ Duyệt theo cột — phá cache\nfor c in range(rong):\n    for h in range(cao):\n        xu_ly(anh[h, c])\n\n# ⭐⭐ Tốt nhất: KHÔNG duyệt bằng Python, dùng phép vector hoá\nanh_moi = anh * 0.5 + 128            # toàn bộ chạy trong C\n```\n\nDòng cuối là bài học quan trọng nhất cho Python: **cách tối ưu hiệu quả nhất thường là đẩy vòng lặp xuống thư viện C**, chứ không phải sắp xếp lại vòng lặp Python.\n\n### Chọn cấu trúc theo ràng buộc thật\n\n```python\n# ═══ TÌNH HUỐNG 1: 50 triệu số đo cảm biến, container 2 GB ═══\n\nimport array\n\n# ❌ list — vượt bộ nhớ\n# du_lieu = [0.0] * 50_000_000        # ~1,8 GB → nguy hiểm\n\n# ✓ array.array — vừa\ndu_lieu = array.array(\"f\", [0.0]) * 50_000_000    # ~200 MB\n\n# ✓✓ NumPy — vừa VÀ tính toán nhanh\n# du_lieu = np.zeros(50_000_000, dtype=np.float32)  # ~200 MB\n```\n\n```python\n# ═══ TÌNH HUỐNG 2: hàng đợi công việc, thêm cuối lấy đầu ═══\nfrom collections import deque\n\n# ❌ list — pop(0) là O(n)\nhang_doi_sai: list[str] = []\nhang_doi_sai.append(\"viec\")\n# viec = hang_doi_sai.pop(0)          # ⚠️ O(n)\n\n# ✓ deque — O(1) hai đầu\nhang_doi: deque[str] = deque()\nhang_doi.append(\"viec\")\nviec = hang_doi.popleft()             # ⭐ O(1)\n```\n\n```python\n# ═══ TÌNH HUỐNG 3: dữ liệu trộn kiểu, n nhỏ ═══\n\n# ✓ list là đúng — array.array không chứa được kiểu trộn\nban_ghi = [\"DH001\", 1_500_000, True, None, [1, 2]]\n```\n\n```python\n# ═══ TÌNH HUỐNG 4: biết trước n, cần độ trễ ổn định ═══\n\nn = 1_000_000\n# ✓ Cấp phát TRƯỚC — không có lần cấp phát lại nào\nkq: list[int | None] = [None] * n\nfor i in range(n):\n    kq[i] = i * i                     # ⭐ chỉ ghi, không nở\n```\n\nBảng quyết định rút gọn:\n\n```\nRÀNG BUỘC CHÍNH               CHỌN\n─────────────────────────     ──────────────────────\nLinh hoạt, kiểu trộn           list\nBộ nhớ chật, cùng kiểu số      array.array\nTính toán số học hàng loạt     NumPy\nThêm/xoá ở ĐẦU                 deque\nĐộ trễ ổn định, biết n         list cấp phát trước\nTra theo khoá                  dict\nLuôn cần min/max               heap\n```\n\n### Bẫy: mảng hai chiều tạo sai\n\n```python\n# ❌ SAI KINH ĐIỂN — ba hàng là CÙNG MỘT list\nma_tran = [[0] * 3] * 3\nma_tran[0][0] = 9\nprint(ma_tran)          # [[9,0,0], [9,0,0], [9,0,0]]   ⚠️ đổi cả ba!\n\nprint(ma_tran[0] is ma_tran[1])      # True — cùng một object\n```\n\n```python\n# ✓ ĐÚNG — mỗi hàng một list riêng\nma_tran = [[0] * 3 for _ in range(3)]\nma_tran[0][0] = 9\nprint(ma_tran)          # [[9,0,0], [0,0,0], [0,0,0]]   ⭐ đúng\n\nprint(ma_tran[0] is ma_tran[1])      # False\n```\n\n```\nVÌ SAO: [x] * n copy CON TRỎ, không copy object.\n        Với số nguyên (bất biến) thì vô hại.\n        Với list (khả biến) thì cả n phần tử trỏ tới CÙNG một list.\n\n⇒ Đây là bài 'tham chiếu vs giá trị' (module 7 mảng 01)\n  hiện ra ở đúng chỗ dễ mắc nhất.\n```\n\nVới NumPy thì không có bẫy này, vì `np.zeros((3, 3))` cấp phát một khối liên tục thật:\n\n```python\n# import numpy as np\n# m = np.zeros((3, 3), dtype=int)\n# m[0, 0] = 9      # ⭐ chỉ đổi một ô — không có chuyện chia sẻ hàng\n```",

so_sanh: "### Ba tính chất bắt buộc của mảng\n\n| Tính chất | Cho phép điều gì |\n|---|---|\n| **Liên tục** | công thức địa chỉ ⇒ `ds[i]` O(1); cache locality |\n| **Cùng kích thước ô** | công thức địa chỉ hoạt động |\n| **Cùng kiểu** | để kích thước ô cố định |\n\nMọi chi phí O(n) của mảng (`insert(0)`, `pop(0)`, cắt lát) đều là **hệ quả bắt buộc** của tính liên tục.\n\n### Mảng số thô và mảng con trỏ\n\n| | Mảng số thô (`array`, NumPy) | Mảng con trỏ (`list`) |\n|---|---|---|\n| Ô chứa | **giá trị** | **địa chỉ** |\n| Byte/ô | 4–8 | 8 (+ ~28 cho `PyObject`) |\n| 1 triệu int | **~4 MB** | ~36 MB |\n| Kiểu phần tử | một kiểu | **bất kỳ, trộn được** |\n| Cache locality | **hoàn hảo** | mảng con trỏ liên tục, giá trị rải rác |\n| `ds[i]` | O(1) | O(1) |\n\n### Ba lựa chọn trong Python\n\n| | `list` | `array.array` | NumPy |\n|---|---|---|---|\n| Bộ nhớ (1tr int) | ~36 MB | **~4 MB** | **~4 MB** |\n| `sum()` Python | nhanh hơn `array` | **⚠️ CHẬM NHẤT** | **nhanh nhất** |\n| Vì sao | có sẵn `PyObject` | **phải bọc `PyObject`** | tính trong C |\n| Kiểu trộn | **✓** | ✗ | ✗ |\n| Nhiều chiều | list lồng | ✗ | **✓** |\n| Giới hạn giá trị | **không** | **✓ có** (`OverflowError`) | ✓ có |\n| Có sẵn | ✓ | ✓ | ✗ phải cài |\n\nHàng 2–3 là điểm bất ngờ quan trọng: **`array.array` tiết kiệm bộ nhớ nhưng KHÔNG nhanh hơn cho phép tính Python thuần.**\n\n### Chi phí thao tác và lý do\n\n| Thao tác | Bậc | Lý do |\n|---|---|---|\n| `ds[i]`, `ds[i] = x` | **O(1)** | công thức địa chỉ |\n| `len(ds)` | O(1) | `ob_size` lưu sẵn |\n| `append`, `pop()` | O(1) kh | module 1 |\n| `insert(0, x)`, `pop(0)` | **O(n)** | ⚠️ **phải dịch mọi phần tử** |\n| `x in ds`, `remove(x)` | O(n) | duyệt tuần tự |\n| `ds[a:b]` | **O(b−a)** | ⚠️ **là COPY** |\n| `sort()` | O(n log n) | Timsort |\n\n### Ba mức cache locality\n\n| Cấu trúc | Locality | Duyệt tuần tự |\n|---|---|---|\n| Mảng số thô | **hoàn hảo** | **nhanh nhất** |\n| `list` Python | con trỏ liên tục, giá trị rải rác | vừa |\n| Linked list | **tệ nhất** | **chậm hơn nhiều lần** |\n\nCả ba đều O(n) khi duyệt — **Big-O không thấy khác biệt này.**\n\n### Khi nào mảng KHÔNG phải lựa chọn đúng\n\n| Nhu cầu | Cấu trúc đúng | Vì sao |\n|---|---|---|\n| Thêm/xoá ở **đầu** | `deque` | mảng phải dịch O(n) |\n| Kích thước biến động mạnh | `deque` | không copy khi nở |\n| Tra theo **khoá** | `dict` | mảng chỉ tra theo chỉ số |\n| Luôn lấy **min/max** | `heap` | mảng phải duyệt O(n) |\n| Độ trễ **ổn định** | mảng **cấp phát trước** | tránh cấp phát lại |\n\n### Thay `insert(0)` bằng gì\n\n| Cách | Bậc tổng n lần | Ghi chú |\n|---|---|---|\n| `ds.insert(0, x)` × n | **O(n²)** | ⚠️ tránh |\n| `deque.appendleft(x)` × n | **O(n)** | mất truy cập O(1) ở giữa |\n| `append` × n rồi `reverse()` | **O(n)** | ⭐ mẹo đơn giản nhất |\n\n### Bẫy tạo ma trận\n\n| Cách viết | Kết quả | Đúng? |\n|---|---|---|\n| `[[0]*3] * 3` | **ba hàng là CÙNG một list** | **✗ sai** |\n| `[[0]*3 for _ in range(3)]` | ba list riêng | **✓ đúng** |\n| `np.zeros((3,3))` | khối liên tục thật | ✓ đúng |\n\nNguyên nhân: `[x] * n` copy **con trỏ**, không copy object — vô hại với số, nguy hiểm với list.",

loi_thuong_gap: [
  "**Tưởng `list` Python lưu giá trị trực tiếp.** Nó lưu **con trỏ**; giá trị nằm rải rác trên heap — đó là lý do tốn ~36 MB cho 1 triệu int.",
  "**Tưởng `array.array` nhanh hơn `list`.** Nó tiết kiệm **bộ nhớ**, nhưng `sum()` trên nó **chậm hơn** vì phải bọc `PyObject` cho mỗi phần tử.",
  "**Dùng `insert(0, x)` trong vòng lặp.** O(n²) tổng; dùng `deque.appendleft` hoặc `append` rồi `reverse`.",
  "**Cắt lát trong vòng lặp.** `ds = ds[1:]` copy toàn bộ mỗi vòng → O(n²).",
  "**Tạo ma trận bằng `[[0]*3] * 3`.** Ba hàng là cùng một list; sửa một ô đổi cả ba hàng.",
  "**Duyệt ma trận theo cột với NumPy.** Ngược bố cục row-major → cache miss liên tục, chậm hơn nhiều lần.",
  "**Quên gán `None` khi xoá phần tử trong mảng tự cài.** Mảng vẫn giữ tham chiếu → GC không thu hồi → rò rỉ bộ nhớ.",
  "**Dịch từ đầu thay vì từ cuối khi `insert`.** Ghi đè dữ liệu chưa kịp dịch — lỗi kinh điển khi tự cài mảng.",
  "**Dùng `list` cho hàng triệu số khi bộ nhớ chật.** Chênh khoảng 9 lần so với `array.array`.",
  "**Dùng `array.array` mà quên giới hạn giá trị.** `array(\"i\")` vượt ~2,1 tỉ là `OverflowError`.",
  "**Tin `sys.getsizeof` là bộ nhớ thật của list.** Nó không đệ quy — chỉ đo mảng con trỏ, không đo các `PyObject`.",
  "**Nghĩ hai thuật toán cùng O(n) thì nhanh như nhau.** Cache locality làm chênh nhau nhiều lần.",
  "**Tối ưu thứ tự vòng lặp Python thay vì vector hoá.** Trong Python, đẩy vòng lặp xuống C hiệu quả hơn nhiều.",
  "**Tưởng `deque[i]` là O(1).** Hai đầu O(1), ở giữa O(n) — đo được là chậm hơn hàng trăm lần.",
  "**Không cấp phát trước khi đã biết n.** `[None] * n` loại bỏ hoàn toàn việc cấp phát lại và cho độ trễ ổn định."
],

tu_kiem_tra: [
  "Nêu ba tính chất bắt buộc của mảng. Mỗi tính chất cho phép điều gì?",
  "Viết công thức địa chỉ và giải thích vì sao `ds[i]` là O(1).",
  "Vì sao `insert(0, x)`, `pop(0)`, và cắt lát đều là O(n)? Chúng có chung nguyên nhân gì?",
  "Vẽ cấu trúc hai tầng của `list` Python. Vì sao 1 triệu int tốn ~36 MB?",
  "Vì sao `[1] * 1_000_000` tốn ít bộ nhớ hơn `list(range(1_000_000))`?",
  "Dòng cache là gì? Vì sao nó khiến mảng nhanh hơn linked list dù cùng O(n)?",
  "Nêu ba mức cache locality của mảng số thô, `list`, và linked list.",
  "So sánh `list`, `array.array`, NumPy về bộ nhớ, tốc độ `sum()`, và tính linh hoạt.",
  "Vì sao `sum(array.array)` CHẬM HƠN `sum(list)`? Điều đó nói gì về khi nào nên dùng `array.array`?",
  "Bố cục row-major là gì? Vì sao duyệt theo cột chậm hơn duyệt theo hàng?",
  "Trong Python, cách tối ưu hiệu quả nhất cho tính toán mảng lớn là gì?",
  "Vì sao `insert` phải dịch từ cuối về đầu, không phải ngược lại?",
  "Vì sao `pop` nên gán `None` vào ô vừa bỏ? Hậu quả nếu không?",
  "Nêu ba cách thay `insert(0, x)` lặp n lần, kèm bậc của mỗi cách.",
  "Vì sao `[[0]*3] * 3` sai? Giải thích bằng khái niệm tham chiếu.",
  "Nêu năm tình huống mảng KHÔNG phải lựa chọn đúng, kèm cấu trúc thay thế.",
  "Mảng thắng ở hai điều nào mà không cấu trúc nào khác có cùng lúc?",
  "Ngay sau khi nhân đôi dung lượng, mảng lãng phí bao nhiêu phần trăm bộ nhớ?"
],

tai_nguyen: [
  "Sách **Computer Systems: A Programmer's Perspective** — Bryant & O'Hallaron. Chương 6 về hệ thống bộ nhớ và cache; nguồn chuẩn để hiểu vì sao locality quan trọng tới vậy.",
  "Sách **Introduction to Algorithms** — CLRS. Phần về mảng và cấu trúc dữ liệu cơ bản; ngắn nhưng chặt chẽ.",
  "**Mã nguồn CPython — `Objects/listobject.c`** — đọc `PyListObject` để thấy cấu trúc `ob_item`, `ob_size`, `allocated` nói trong bài.",
  "**Tài liệu Python — `array`** — bảng mã kiểu đầy đủ, `itemsize`, và giới hạn giá trị của từng mã.",
  "**Tài liệu NumPy — *Internal memory layout of an ndarray*** — giải thích strides, row-major/column-major, và view so với copy."
],

lien_he: [
  "**Phân tích khấu hao** ở module 1 mảng này: cơ chế nhân đôi và vì sao `append` là O(1) — bài này dùng lại kết luận đó chứ không chứng minh lại.",
  "**Độ phức tạp không gian** ở module 1 mảng này: cắt lát là copy, `islice` không copy.",
  "**Ký hiệu Big-O** ở module 1 mảng này: phản biện \"Big-O bỏ qua chi phí của từng thao tác\" — cache locality chính là chi phí đó.",
  "**CPU, RAM, cache** ở module 1 mảng 01: chênh lệch tốc độ giữa các tầng bộ nhớ, nền tảng của cả bài.",
  "**Tham chiếu vs giá trị** ở module 7 mảng 01: vì sao `[[0]*3]*3` sai.",
  "**`PyObject` header** ở module 7 mảng 01: vì sao mỗi int nhỏ tốn ~28 byte.",
  "**Đếm tham chiếu và GC** ở module 7 mảng 01: vì sao phải gán `None` khi xoá phần tử.",
  "**Linked list** ngay sau: cấu trúc không có công thức địa chỉ, và khi nào nó thật sự đáng dùng.",
  "**Deque hai đầu** trong module này: cấu trúc khối, vì sao `dq[i]` ở giữa là O(n).",
  "**Hash** ở module 3 mảng này: bảng băm xây **trên** mảng — mảng là nền của nó.",
  "**Sắp xếp** ở module 6 mảng này: Timsort thao tác trực tiếp trên mảng, tận dụng locality.",
  "**Công cụ & Python cho data** ở mảng 07: NumPy ndarray là mảng nhiều chiều liên tục — bài này là nền để hiểu nó.",
  "**Deep Learning** ở mảng 07: tensor là mảng nhiều chiều; bố cục bộ nhớ quyết định hiệu năng huấn luyện."
]

};
