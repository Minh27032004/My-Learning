window.LESSONS["02:hash:xu-ly-va-cham-chaining-vs-open-addressing"] = {

tom_tat: [
  "**Va chạm không phải lỗi — nó là điều chắc chắn xảy ra.** Số khoá có thể có là vô hạn, số ô là hữu hạn; câu hỏi duy nhất là xử lý thế nào.",
  "**Hai họ giải pháp đối lập:** chaining lưu các khoá va chạm trong một danh sách **ngoài** mảng; open addressing tìm **một ô trống khác** ngay trong mảng.",
  "**Nghịch lý ngày sinh làm va chạm xuất hiện sớm hơn nhiều so với trực giác:** với 1 triệu ô, chỉ cần khoảng 1.200 khoá là đã có va chạm với xác suất trên 50%.",
  "**Open addressing nhanh hơn nhờ cache locality, nhưng xoá rất phiền** — phải dùng dấu \"đã xoá\" (tombstone), và tombstone tích tụ làm bảng chậm dần.",
  "**Python dùng open addressing với dò tìm ngẫu nhiên hoá** — thiết kế này lý giải cả tốc độ lẫn mức tiêu thụ bộ nhớ của `dict`."
],

can_biet_truoc: [
  "Bài **Hàm băm và bảng băm** ngay trước — hai bước hash → chỉ số, hệ số tải, và hợp đồng `__hash__`/`__eq__`.",
  "Bài **Linked list** ở module 2 mảng này — chaining kinh điển dùng danh sách liên kết cho mỗi ô.",
  "Bài **Array và dynamic array** ở module 2 mảng này — cache locality là lý do chính open addressing nhanh hơn.",
  "Bài **Phân tích khấu hao** ở module 1 mảng này — nguyên tắc \"hai ngưỡng phải cách nhau\" xuất hiện lại ở đây."
],

dinh_nghia: "Nói đời thường: bạn tới bãi giữ xe, mỗi người có một ô được đánh số theo biển số xe. Tới nơi thì ô đó đã có xe khác. Hai cách xử lý: **treo thêm một giá đỡ** ngay tại ô đó cho nhiều xe (chaining), hoặc **đi tìm ô trống gần nhất** theo một quy tắc mà ai cũng biết (open addressing).\n\nCác khái niệm:\n\n- **Va chạm** (*collision*) — hai khoá khác nhau cho cùng một chỉ số.\n- **Chaining** (*separate chaining*) — mỗi ô chứa một **danh sách** các cặp khoá–giá trị va chạm nhau.\n- **Open addressing** (*địa chỉ mở*) — mọi phần tử nằm **trong chính mảng**; va chạm thì dò tìm ô trống khác.\n- **Dò tìm** (*probing*) — quy tắc tìm ô tiếp theo khi ô hiện tại đã có người.\n- **Dò tuyến tính** (*linear probing*) — thử ô kế bên: `i+1, i+2, i+3, …`\n- **Dò bậc hai** (*quadratic probing*) — thử `i+1, i+4, i+9, …`\n- **Băm kép** (*double hashing*) — bước nhảy tính bằng một hàm băm thứ hai.\n- **Cụm** (*clustering*) — các ô đã dùng dồn lại thành đám liền nhau, làm dò tìm ngày càng dài.\n- **Tombstone** (dấu đã xoá) — đánh dấu ô từng có dữ liệu nhưng đã bị xoá, để chuỗi dò tìm không bị đứt.\n\nHai họ giải pháp, nhìn bằng hình:\n\n```\nCHAINING                          OPEN ADDRESSING\n────────────────────              ─────────────────────\n[0] → None                        [0] ( — )\n[1] → (a,1) → (d,4) → None        [1] (a,1)\n[2] → (b,2)                       [2] (b,2)\n[3] → None                        [3] (d,4)   ← d va chạm ở [1], dời sang [3]\n[4] → (c,3)                       [4] (c,3)\n\nphần tử nằm NGOÀI mảng            ⭐ mọi phần tử nằm TRONG mảng\ncó thể chứa vô hạn                ⚠️ số phần tử ≤ số ô\n```\n\nĐiểm quyết định mọi khác biệt còn lại:\n\n```\nCHAINING:        hệ số tải CÓ THỂ > 1\nOPEN ADDRESSING: hệ số tải BẮT BUỘC < 1, và thực tế phải giữ ≤ 0,7\n```",

vi_sao_can: "Bài trước dừng ở chỗ \"va chạm là không tránh được\"; bài này trả lời câu tiếp theo:\n\n- **Nó giải thích vì sao `dict` của Python nhanh nhưng tốn bộ nhớ.** Python chọn open addressing, và lựa chọn đó kéo theo cả hai đặc tính — không thể có cái này mà không có cái kia.\n- **Nghịch lý ngày sinh là kiến thức chống trực giác sai.** Người ta hay nghĩ \"bảng lớn thì hiếm va chạm\", trong khi thực tế va chạm xuất hiện ở khoảng căn bậc hai của số ô. Đây là con số cần biết khi thiết kế.\n- **Bài toán xoá trong open addressing là một ví dụ đẹp về hệ quả không lường trước.** Xoá đơn giản làm hỏng cả cấu trúc, và tombstone giải quyết được nhưng lại tạo vấn đề mới.\n- **Nó là nền cho hai bài sau.** Đánh đổi bộ nhớ (bài 3) và trường hợp xấu nhất O(n) (bài 5) đều bắt nguồn từ cách xử lý va chạm.\n- **Ý tưởng này lặp lại ở quy mô hệ thống.** Chaining và open addressing tương ứng với hai chiến lược phân bổ trong hệ phân tán, và mảng 06 sẽ gặp lại chúng.\n\nTình huống thật: bạn tự cài một bộ nhớ đệm dùng open addressing, thêm và xoá liên tục. Ban đầu nhanh, nhưng sau vài giờ chạy nó chậm dần cho tới khi gần như đứng — dù số phần tử **không** tăng. **Nguyên nhân: tombstone tích tụ.** Mỗi lần xoá để lại một dấu, và các dấu đó vẫn phải được dò qua. Bảng \"đầy\" theo nghĩa dò tìm, dù rỗng theo nghĩa dữ liệu.\n\nMột lưu ý về mức độ cần thiết: bạn hiếm khi tự cài bảng băm trong Python. Nhưng hiểu hai chiến lược này là điều kiện để đọc hiểu tài liệu (\"HashMap dùng chaining, chuyển sang cây khi ô quá đông\"), để chọn tham số đúng, và để lập luận về hiệu năng.",

co_che: "### Va chạm xuất hiện sớm hơn bạn nghĩ\n\n**Nghịch lý ngày sinh** — kết quả chống trực giác quan trọng nhất của bài:\n\n```\nCâu hỏi cổ điển: cần bao nhiêu người để xác suất có hai người\ncùng ngày sinh vượt 50%?\n\nTrực giác nói: khoảng 180 (một nửa của 365)\nThực tế:       23 người\n```\n\nÁp cho bảng băm:\n\n```\nVới n ô, va chạm xuất hiện với xác suất > 50% khi số khoá\nđạt khoảng  1,18 × √n\n\n  n = 365      →  ~23 khoá\n  n = 1.000    →  ~37 khoá\n  n = 1.000.000 → ~1.180 khoá\n  n = 2⁶⁴      →  ~5 tỉ khoá\n```\n\n```\n⭐ Ý NGHĨA THỰC TẾ:\n\n  Bảng 1 triệu ô, mới chèn 1.200 khoá (hệ số tải 0,0012!)\n  ⇒ đã gần như chắc chắn có va chạm.\n\n⇒ Va chạm KHÔNG phải chuyện hiếm cần lo khi bảng đầy.\n  Nó xảy ra NGAY khi bảng còn rất rỗng.\n  ⇒ Xử lý va chạm là phần BẮT BUỘC, không phải tối ưu phụ.\n```\n\nVì sao: số **cặp** khoá tăng theo bình phương số khoá. Với k khoá có `k(k−1)/2` cặp, và mỗi cặp có xác suất `1/n` va chạm — nên tổng xác suất tăng rất nhanh.\n\n### Họ ①: chaining\n\n```\nMỗi ô là ĐẦU của một danh sách liên kết:\n\n[0] → None\n[1] → (\"an\", 25) → (\"minh\", 30) → None       ⭐ hai khoá va chạm\n[2] → (\"lan\", 28) → None\n[3] → None\n```\n\n```\nTHÊM:  tính chỉ số → thêm vào danh sách ở ô đó       O(1) + O(độ dài chuỗi)\nTÌM:   tính chỉ số → duyệt danh sách, so khoá        O(độ dài chuỗi)\nXOÁ:   tính chỉ số → tháo khỏi danh sách             O(độ dài chuỗi)\n```\n\n```\nĐỘ DÀI CHUỖI TRUNG BÌNH = hệ số tải α = số_phần_tử / số_ô\n\n  α = 0,75  →  chuỗi trung bình 0,75 phần tử  ⇒ tra cứu ~O(1)\n  α = 5     →  chuỗi trung bình 5 phần tử     ⇒ vẫn chấp nhận được\n  α = 100   →  chuỗi trung bình 100           ⇒ chậm, nhưng vẫn CHẠY\n```\n\nBốn đặc tính của chaining:\n\n```\n⭐ ƯU:\n  · Hệ số tải CÓ THỂ > 1 — bảng không bao giờ \"đầy\"\n  · XOÁ ĐƠN GIẢN — chỉ tháo khỏi danh sách, không cần tombstone\n  · Suy thoái NHẸ NHÀNG khi tải cao\n  · Ít nhạy cảm với chất lượng hàm băm\n\n⚠️ NHƯỢC:\n  · Mỗi nút là một object riêng ⇒ TỐN BỘ NHỚ (con trỏ + header)\n  · CACHE LOCALITY TỆ — nút rải rác trên heap (bài linked list)\n  · Nhiều lần cấp phát object nhỏ\n```\n\nCải tiến thực tế — dùng ở Java `HashMap`:\n\n```\nKhi một ô có QUÁ NHIỀU phần tử (thường ngưỡng 8),\nchuyển danh sách liên kết thành CÂY CÂN BẰNG.\n\n⇒ độ dài chuỗi xấu nhất từ O(k) xuống O(log k)\n⇒ chống được tấn công va chạm cố ý (bài 5)\n```\n\n### Họ ②: open addressing\n\n```\nMọi phần tử nằm TRONG mảng. Va chạm thì DÒ TÌM ô khác.\n\nChèn (\"d\", 4) mà ô 1 đã có (\"a\", 1):\n\n  thử ô 1 → bận\n  thử ô 2 → bận\n  thử ô 3 → TRỐNG ⇒ đặt vào đây\n\n[0] ( — )   [1] (a,1)   [2] (b,2)   [3] (d,4)   [4] ( — )\n```\n\n⚠️ Điều quan trọng nhất: **tìm kiếm phải đi theo ĐÚNG chuỗi dò tìm đó.**\n\n```\nTìm \"d\":  thử ô 1 → là \"a\", không khớp → thử tiếp\n          thử ô 2 → là \"b\", không khớp → thử tiếp\n          thử ô 3 → là \"d\"  ⭐ TÌM THẤY\n\nGặp ô TRỐNG ⇒ dừng, khoá không tồn tại.\n```\n\n### Ba chiến lược dò tìm\n\n**① Dò tuyến tính** — `i, i+1, i+2, i+3, …`\n\n```\n⭐ ƯU: cache locality TỐT NHẤT — các ô kề nhau, cùng dòng cache\n⚠️ NHƯỢC: CỤM CƠ BẢN (primary clustering)\n\n  Các ô đã dùng dồn thành đám liền nhau.\n  Đám càng lớn càng dễ hút thêm ⇒ càng lớn nhanh.\n\n  [x][x][x][x][ ][ ][x][ ]\n   └── cụm 4 ô ──┘\n   ⇒ bất kỳ khoá nào băm vào 4 ô này đều phải dò tới cuối cụm\n```\n\n**② Dò bậc hai** — `i, i+1², i+2², i+3², …` = `i+1, i+4, i+9, …`\n\n```\n⭐ ƯU: phá được cụm cơ bản\n⚠️ NHƯỢC: CỤM PHỤ (secondary clustering) — hai khoá cùng chỉ số\n           ban đầu vẫn đi CÙNG một chuỗi dò\n⚠️ Không đảm bảo thăm hết mọi ô nếu kích thước bảng không phù hợp\n```\n\n**③ Băm kép** — bước nhảy tính bằng hàm băm thứ hai\n\n```\n  chỉ số thứ k = (h1(khoá) + k × h2(khoá)) % n\n\n⭐ ƯU: phá được CẢ HAI loại cụm — mỗi khoá có chuỗi dò RIÊNG\n⚠️ NHƯỢC: cache locality TỆ (nhảy xa), phải tính hai hàm băm\n⚠️ h2 phải KHÁC 0 và nguyên tố cùng nhau với n\n```\n\nPython dùng một biến thể riêng, kết hợp ưu điểm:\n\n```\nCPython: chuỗi dò tìm dùng CẢ các bit CAO của hash\n\n  perturb = hash\n  vòng lặp:\n      i = (i * 5 + perturb + 1) % n\n      perturb >>= 5              ⭐ dịch phải, đưa bit cao vào dần\n\n⭐ Mỗi khoá có chuỗi dò gần như RIÊNG (phá cụm)\n⭐ Vài bước đầu vẫn khá gần nhau (giữ chút locality)\n⭐ Chống tấn công va chạm tốt hơn dò tuyến tính\n```\n\n### ⚠️ Bài toán xoá — vấn đề khó nhất\n\nĐây là điểm yếu lớn nhất của open addressing:\n\n```\nBảng sau khi chèn a (ô 1), b (ô 2), d (ô 3 — do va chạm ở ô 1):\n\n[0] ( — )   [1] (a)   [2] (b)   [3] (d)\n\nXOÁ b bằng cách đặt ô 2 thành TRỐNG:\n\n[0] ( — )   [1] (a)   [2] ( — )   [3] (d)\n\nGiờ TÌM d:\n  thử ô 1 → \"a\", không khớp → tiếp\n  thử ô 2 → TRỐNG ⇒ DỪNG, kết luận \"không có d\"\n\n⚠️ SAI! d vẫn nằm ở ô 3, nhưng chuỗi dò tìm đã BỊ ĐỨT.\n```\n\nGiải pháp — **tombstone**:\n\n```\nXoá thì đánh dấu ô là ĐÃ XOÁ, không phải TRỐNG:\n\n[0] (trống)   [1] (a)   [2] ⟨đã xoá⟩   [3] (d)\n\nQuy tắc dò tìm mới:\n  gặp ⟨đã xoá⟩ → ĐI TIẾP (chuỗi chưa đứt)\n  gặp (trống)  → DỪNG\n\nQuy tắc chèn:\n  ô ⟨đã xoá⟩ đầu tiên gặp được → GHI NHỚ, có thể tái dùng\n  nhưng vẫn phải dò tiếp để chắc khoá chưa tồn tại ở sau\n```\n\n⚠️ Nhưng tombstone tạo vấn đề mới:\n\n```\nTombstone TÍCH TỤ theo thời gian.\n\n  Thêm/xoá liên tục ⇒ bảng đầy tombstone\n  ⇒ chuỗi dò dài ra ⇒ CHẬM DẦN\n  ⇒ dù số phần tử THẬT không tăng\n\n⇒ Phải TÍNH TOMBSTONE vào hệ số tải, và dọn định kỳ\n  bằng cách cấp phát lại (rehash bỏ hết tombstone).\n```\n\nĐây là ví dụ điển hình của **hệ quả không lường trước**: giải pháp cho một vấn đề tạo ra vấn đề thứ hai, và vấn đề thứ hai lại cần một cơ chế thứ ba (dọn định kỳ) để xử lý.\n\n### So sánh hai họ\n\n```\n                        CHAINING            OPEN ADDRESSING\n                        ────────────        ──────────────────\nHệ số tải cho phép      ⭐ > 1 được         ⚠️ < 1, thực tế ≤ 0,7\nXoá                     ⭐ đơn giản         ⚠️ cần tombstone\nCache locality          ⚠️ tệ               ⭐ tốt\nBộ nhớ mỗi phần tử      ⚠️ + con trỏ, header ⭐ chỉ ô mảng\nSuy thoái khi tải cao   ⭐ nhẹ nhàng        ⚠️ đột ngột\nNhạy với hàm băm xấu    ⭐ ít               ⚠️ nhiều\nCấp phát object         ⚠️ nhiều lần        ⭐ một lần\nDùng ở                  Java HashMap        ⭐ Python dict, Rust\n```\n\nHai hàng cuối giải thích lựa chọn khác nhau của các ngôn ngữ:\n\n```\nJava chọn CHAINING       → JVM có bộ cấp phát object rất nhanh\n                          → và có thể chuyển sang cây khi ô quá đông\n\nPython chọn OPEN ADDRESSING → tránh cấp phát object nhỏ\n                            → cache locality tốt hơn\n                            → nhưng phải giữ hệ số tải thấp (~2/3)\n                              ⇒ TỐN BỘ NHỚ HƠN\n```\n\n### Suy thoái khi tải cao — con số cụ thể\n\nSố lần dò trung bình khi tìm **không thấy**, theo hệ số tải α:\n\n```\nCHAINING:          1 + α\n\n  α = 0,5  → 1,5 lần\n  α = 0,9  → 1,9 lần\n  α = 5    → 6 lần        ⭐ vẫn chấp nhận được\n\n\nOPEN ADDRESSING (dò tuyến tính):   ½(1 + 1/(1−α)²)\n\n  α = 0,5  → 2,5 lần\n  α = 0,75 → 8,5 lần\n  α = 0,9  → 50,5 lần     ⚠️ NỔ\n  α = 0,95 → 200,5 lần    ⚠️⚠️\n  α → 1    → vô hạn\n```\n\n```\n⭐ ĐỌC HAI CÔNG THỨC:\n\n  chaining        tăng TUYẾN TÍNH theo α  ⇒ suy thoái nhẹ nhàng\n  open addressing tăng theo 1/(1−α)²      ⇒ NỔ khi α → 1\n\n⇒ Đây là lý do open addressing PHẢI giữ α ≤ 0,7,\n  và vì sao Python cấp phát lại ở ngưỡng 2/3.\n```\n\n### Ngưỡng cấp phát lại — hysteresis lặp lại\n\nNguyên tắc từ bài khấu hao (module 1) xuất hiện lại:\n\n```\nNGƯỠNG NỞ:  hệ số tải > 2/3          → tăng kích thước\nNGƯỠNG CO:  hệ số tải < 1/8 (nếu có) → giảm kích thước\n\n⚠️ HAI NGƯỠNG PHẢI CÁCH XA NHAU.\n  Gần nhau ⇒ thêm/xoá luân phiên gây DAO ĐỘNG\n  ⇒ mỗi thao tác một lần rehash O(n) ⇒ phá vỡ O(1) khấu hao.\n```\n\n⚠️ Với open addressing còn phải tính tombstone:\n\n```\nhệ_số_tải_hiệu_dụng = (số_phần_tử + số_tombstone) / số_ô\n\n⇒ Cấp phát lại dựa trên con số NÀY, không phải chỉ số phần tử.\n  Bỏ qua tombstone là nguyên nhân của bug \"chậm dần theo thời gian\".\n```",

vi_du: "### Nghịch lý ngày sinh — đo thật\n\n```python\nimport random\n\n\ndef xac_suat_va_cham(so_o: int, so_khoa: int, lan_thu: int = 2000) -> float:\n    \"\"\"Ước lượng xác suất có ít nhất một va chạm, bằng mô phỏng.\"\"\"\n    co_va_cham = 0\n    for _ in range(lan_thu):\n        da_dung = set()\n        for _ in range(so_khoa):\n            o = random.randrange(so_o)\n            if o in da_dung:\n                co_va_cham += 1\n                break\n            da_dung.add(o)\n    return co_va_cham / lan_thu\n\n\nso_o = 365\nprint(f\"Bảng {so_o} ô:\")\nfor so_khoa in (10, 20, 23, 30, 50):\n    p = xac_suat_va_cham(so_o, so_khoa)\n    print(f\"  {so_khoa:>3} khoá → xác suất va chạm {p:>5.1%}\")\n```\n\n```\nBảng 365 ô:\n   10 khoá → xác suất va chạm ~11.7%\n   20 khoá → xác suất va chạm ~41.1%\n   23 khoá → xác suất va chạm ~50.7%    ⭐ vượt 50% ở 23 khoá\n   30 khoá → xác suất va chạm ~70.6%\n   50 khoá → xác suất va chạm ~97.0%\n\n⚠️ Chỉ 23 khoá trong 365 ô — hệ số tải 0,063 — đã 50% va chạm.\n```\n\nCông thức xấp xỉ và kiểm chứng:\n\n```python\nimport math\n\n\ndef nguong_50_phan_tram(so_o: int) -> int:\n    \"\"\"Số khoá để xác suất va chạm vượt 50%. Xấp xỉ: 1,18 × √n.\"\"\"\n    return math.ceil(1.1774 * math.sqrt(so_o))\n\n\nprint(f\"{'số ô':>15} {'ngưỡng 50%':>14} {'hệ số tải lúc đó':>20}\")\nfor n in (365, 1_000, 100_000, 1_000_000, 2**32):\n    k = nguong_50_phan_tram(n)\n    print(f\"{n:>15,} {k:>14,} {k/n:>19.6f}\")\n```\n\n```\n           số ô     ngưỡng 50%     hệ số tải lúc đó\n            365             23            0.063014\n          1,000             38            0.038000\n        100,000            373            0.003730\n      1,000,000          1,178            0.001178\n  4,294,967,296         77,163            0.000018\n\n⭐ Bảng CÀNG LỚN, hệ số tải lúc va chạm xuất hiện càng THẤP.\n\n⇒ \"Bảng đủ lớn thì không va chạm\" là SAI HOÀN TOÀN.\n  Va chạm xuất hiện ở khoảng √n, không phải ở n.\n```\n\n### Chaining — hiện thực\n\n```python\nfrom typing import Any, Iterator\n\n\nclass BangBamChaining:\n    \"\"\"Bảng băm dùng chaining. Mỗi ô là một list các cặp (khoá, giá trị).\n\n    Dùng list thay linked list vì trong Python list nhanh hơn\n    và chuỗi thường rất ngắn (bài linked list, module 2).\n    \"\"\"\n\n    NGUONG_TAI = 0.75\n\n    def __init__(self, so_o: int = 8) -> None:\n        self._o: list[list[tuple[Any, Any]]] = [[] for _ in range(so_o)]\n        self._size = 0\n        self.so_lan_do = 0                  # ⭐ đếm để so sánh\n\n    def _chi_so(self, khoa: Any) -> int:\n        return hash(khoa) % len(self._o)\n\n    @property\n    def he_so_tai(self) -> float:\n        return self._size / len(self._o)\n\n    def __len__(self) -> int:\n        return self._size\n\n    def __setitem__(self, khoa: Any, gia_tri: Any) -> None:\n        o = self._o[self._chi_so(khoa)]\n        for i, (k, _) in enumerate(o):\n            self.so_lan_do += 1\n            if k == khoa:\n                o[i] = (khoa, gia_tri)\n                return\n        o.append((khoa, gia_tri))\n        self._size += 1\n        if self.he_so_tai > self.NGUONG_TAI:\n            self._cap_phat_lai(len(self._o) * 2)\n\n    def __getitem__(self, khoa: Any) -> Any:\n        \"\"\"Raises: KeyError.\"\"\"\n        for k, v in self._o[self._chi_so(khoa)]:\n            self.so_lan_do += 1\n            if k == khoa:\n                return v\n        raise KeyError(khoa)\n\n    def __contains__(self, khoa: Any) -> bool:\n        try:\n            self[khoa]\n            return True\n        except KeyError:\n            return False\n\n    def __delitem__(self, khoa: Any) -> None:\n        \"\"\"⭐ XOÁ ĐƠN GIẢN — chỉ tháo khỏi list. Raises: KeyError.\"\"\"\n        o = self._o[self._chi_so(khoa)]\n        for i, (k, _) in enumerate(o):\n            if k == khoa:\n                o.pop(i)\n                self._size -= 1\n                return\n        raise KeyError(khoa)\n\n    def _cap_phat_lai(self, so_o_moi: int) -> None:\n        cu = self._o\n        self._o = [[] for _ in range(so_o_moi)]\n        self._size = 0\n        for o in cu:\n            for k, v in o:\n                self[k] = v\n\n    def thong_ke_chuoi(self) -> dict:\n        do_dai = [len(o) for o in self._o]\n        dung = [d for d in do_dai if d > 0]\n        return {\n            \"ô dùng\": f\"{len(dung)}/{len(do_dai)}\",\n            \"chuỗi dài nhất\": max(do_dai) if do_dai else 0,\n            \"chuỗi TB (ô có dùng)\": round(sum(dung) / len(dung), 2) if dung else 0,\n        }\n\n    def __iter__(self) -> Iterator[Any]:\n        for o in self._o:\n            for k, _ in o:\n                yield k\n```\n\n```python\nb = BangBamChaining()\nfor i, ten in enumerate([\"an\", \"binh\", \"cuong\", \"dung\", \"lan\"]):\n    b[ten] = i\n\nassert len(b) == 5\nassert b[\"cuong\"] == 2\nassert \"lan\" in b and \"khong-co\" not in b\n\ndel b[\"binh\"]                            # ⭐ xoá đơn giản, không tombstone\nassert \"binh\" not in b and len(b) == 4\n\n# ---- Ca biên ----\ntry:\n    b[\"khong-co\"]\nexcept KeyError as e:\n    print(f\"✓ KeyError: {e}\")\ntry:\n    del b[\"khong-co\"]\nexcept KeyError as e:\n    print(f\"✓ KeyError khi xoá: {e}\")\n\nrong = BangBamChaining()\nassert len(rong) == 0 and list(rong) == []\nprint(\"✓ chaining: mọi ca kiểm thử đạt\")\n```\n\n⭐ Chaining chịu được hệ số tải **lớn hơn 1**:\n\n```python\nclass ChainingKhongNo(BangBamChaining):\n    \"\"\"Tắt cấp phát lại để xem hành vi khi tải rất cao.\"\"\"\n    NGUONG_TAI = float(\"inf\")\n\n\nb = ChainingKhongNo(so_o=16)\nfor i in range(160):\n    b[f\"k{i}\"] = i\n\nprint(f\"hệ số tải: {b.he_so_tai:.1f}\")     # 10.0 — VƯỢT XA 1\nprint(f\"thống kê : {b.thong_ke_chuoi()}\")\nassert b[\"k100\"] == 100                     # ⭐ VẪN HOẠT ĐỘNG\n```\n\n```\nhệ số tải: 10.0\nthống kê : {'ô dùng': '16/16', 'chuỗi dài nhất': ~16, 'chuỗi TB (ô có dùng)': 10.0}\n\n⭐ Hệ số tải 10 vẫn chạy được — chỉ CHẬM HƠN, không HỎNG.\n  Open addressing thì hệ số tải 1 là ĐẦY, không chèn thêm được.\n```\n\n### Open addressing — hiện thực với tombstone\n\n```python\nfrom typing import Any, Iterator\n\n\nclass _Trong:\n    \"\"\"Dấu ô CHƯA BAO GIỜ dùng.\"\"\"\n    def __repr__(self) -> str: return \"( — )\"\n\n\nclass _DaXoa:\n    \"\"\"Tombstone: ô TỪNG có dữ liệu nhưng đã bị xoá.\"\"\"\n    def __repr__(self) -> str: return \"⟨đã xoá⟩\"\n\n\nTRONG = _Trong()\nDA_XOA = _DaXoa()\n\n\nclass BangBamMoRong:\n    \"\"\"Open addressing với dò tuyến tính và tombstone.\n\n    ⚠️ Hệ số tải hiệu dụng tính CẢ tombstone — nếu không,\n       bảng sẽ chậm dần theo thời gian.\n    \"\"\"\n\n    NGUONG_TAI = 0.66                    # ⭐ phải giữ THẤP\n\n    def __init__(self, so_o: int = 8) -> None:\n        self._o: list[Any] = [TRONG] * so_o\n        self._size = 0\n        self._so_tombstone = 0\n        self.so_lan_do = 0\n\n    def _chi_so(self, khoa: Any) -> int:\n        return hash(khoa) % len(self._o)\n\n    @property\n    def he_so_tai_hieu_dung(self) -> float:\n        \"\"\"⭐ Tính CẢ tombstone — đây là con số quyết định cấp phát lại.\"\"\"\n        return (self._size + self._so_tombstone) / len(self._o)\n\n    def __len__(self) -> int:\n        return self._size\n\n    def _do_tim(self, khoa: Any) -> Iterator[int]:\n        \"\"\"Sinh chuỗi chỉ số dò tuyến tính.\"\"\"\n        i = self._chi_so(khoa)\n        n = len(self._o)\n        for buoc in range(n):\n            yield (i + buoc) % n         # ⭐ dò TUYẾN TÍNH\n\n    def __setitem__(self, khoa: Any, gia_tri: Any) -> None:\n        o_tai_dung: int | None = None     # tombstone đầu tiên gặp được\n        for i in self._do_tim(khoa):\n            self.so_lan_do += 1\n            muc = self._o[i]\n            if muc is TRONG:\n                # ⭐ chưa từng dùng ⇒ khoá chắc chắn không tồn tại\n                dich = o_tai_dung if o_tai_dung is not None else i\n                if self._o[dich] is DA_XOA:\n                    self._so_tombstone -= 1\n                self._o[dich] = (khoa, gia_tri)\n                self._size += 1\n                break\n            if muc is DA_XOA:\n                if o_tai_dung is None:\n                    o_tai_dung = i        # ⭐ ghi nhớ, nhưng VẪN DÒ TIẾP\n                continue\n            if muc[0] == khoa:\n                self._o[i] = (khoa, gia_tri)   # cập nhật\n                return\n        else:\n            raise RuntimeError(\"bảng đầy — đáng lẽ đã cấp phát lại\")\n\n        if self.he_so_tai_hieu_dung > self.NGUONG_TAI:\n            self._cap_phat_lai(max(8, (self._size + 1) * 3))\n\n    def __getitem__(self, khoa: Any) -> Any:\n        \"\"\"Raises: KeyError.\"\"\"\n        for i in self._do_tim(khoa):\n            self.so_lan_do += 1\n            muc = self._o[i]\n            if muc is TRONG:\n                raise KeyError(khoa)      # ⭐ ô TRỐNG ⇒ dừng\n            if muc is DA_XOA:\n                continue                   # ⭐ tombstone ⇒ ĐI TIẾP\n            if muc[0] == khoa:\n                return muc[1]\n        raise KeyError(khoa)\n\n    def __contains__(self, khoa: Any) -> bool:\n        try:\n            self[khoa]\n            return True\n        except KeyError:\n            return False\n\n    def __delitem__(self, khoa: Any) -> None:\n        \"\"\"⚠️ Đặt TOMBSTONE, không đặt TRỐNG. Raises: KeyError.\"\"\"\n        for i in self._do_tim(khoa):\n            muc = self._o[i]\n            if muc is TRONG:\n                raise KeyError(khoa)\n            if muc is DA_XOA:\n                continue\n            if muc[0] == khoa:\n                self._o[i] = DA_XOA       # ⭐ TOMBSTONE\n                self._size -= 1\n                self._so_tombstone += 1\n                return\n        raise KeyError(khoa)\n\n    def _cap_phat_lai(self, so_o_moi: int) -> None:\n        \"\"\"⭐ Cấp phát lại XOÁ SẠCH tombstone.\"\"\"\n        cu = [m for m in self._o if m is not TRONG and m is not DA_XOA]\n        self._o = [TRONG] * so_o_moi\n        self._size = 0\n        self._so_tombstone = 0\n        for k, v in cu:\n            self[k] = v\n\n    def trang_thai(self) -> str:\n        return \" \".join(\n            \"·\" if m is TRONG else (\"✗\" if m is DA_XOA else str(m[0])[:3])\n            for m in self._o\n        )\n```\n\n```python\nb = BangBamMoRong(so_o=8)\nfor ten in (\"an\", \"binh\", \"cuong\"):\n    b[ten] = ten.upper()\n\nassert b[\"an\"] == \"AN\" and len(b) == 3\nassert \"binh\" in b and \"khong-co\" not in b\n\ndel b[\"binh\"]\nassert \"binh\" not in b and len(b) == 2\nassert b._so_tombstone == 1              # ⭐ tombstone được tạo\nprint(f\"sau khi xoá: {b.trang_thai()}\")\n\nb[\"binh\"] = \"BINH2\"                       # ⭐ tái dùng ô tombstone\nassert b[\"binh\"] == \"BINH2\" and len(b) == 3\n\n# ---- Ca biên ----\ntry:\n    b[\"khong-co\"]\nexcept KeyError as e:\n    print(f\"✓ KeyError: {e}\")\n\nrong = BangBamMoRong()\nassert len(rong) == 0\ntry:\n    del rong[\"x\"]\nexcept KeyError as e:\n    print(f\"✓ xoá từ bảng rỗng: {e}\")\nprint(\"✓ open addressing: mọi ca kiểm thử đạt\")\n```\n\n### ⚠️ Chứng minh vì sao cần tombstone\n\n```python\nclass BangBamXoaSai(BangBamMoRong):\n    \"\"\"❌ Xoá bằng cách đặt TRỐNG — làm ĐỨT chuỗi dò tìm.\"\"\"\n\n    def __delitem__(self, khoa: Any) -> None:\n        for i in self._do_tim(khoa):\n            muc = self._o[i]\n            if muc is TRONG:\n                raise KeyError(khoa)\n            if muc is DA_XOA:\n                continue\n            if muc[0] == khoa:\n                self._o[i] = TRONG        # ⚠️ SAI — đặt TRỐNG\n                self._size -= 1\n                return\n        raise KeyError(khoa)\n\n\n# Dựng tình huống va chạm có kiểm soát\nclass KhoaVaCham:\n    \"\"\"Khoá luôn băm về 0 — ép mọi khoá va chạm.\"\"\"\n    __slots__ = (\"ten\",)\n\n    def __init__(self, ten: str) -> None:\n        self.ten = ten\n\n    def __hash__(self) -> int:\n        return 0                          # ⭐ mọi khoá cùng ô\n\n    def __eq__(self, other) -> bool:\n        return isinstance(other, KhoaVaCham) and self.ten == other.ten\n\n    def __repr__(self) -> str:\n        return self.ten\n\n\na, b_, c = KhoaVaCham(\"a\"), KhoaVaCham(\"b\"), KhoaVaCham(\"c\")\n\nprint(\"── Xoá SAI (đặt TRỐNG) ──\")\nsai = BangBamXoaSai(so_o=8)\nsai[a] = 1; sai[b_] = 2; sai[c] = 3       # cả ba vào ô 0, 1, 2\nprint(f\"  trước khi xoá: {sai.trang_thai()}\")\ndel sai[b_]                                # xoá cái GIỮA chuỗi\nprint(f\"  sau khi xoá b: {sai.trang_thai()}\")\nprint(f\"  c còn tìm được? {c in sai}\")     # ⚠️ False — SAI\n\nprint(\"\\n── Xoá ĐÚNG (tombstone) ──\")\ndung = BangBamMoRong(so_o=8)\ndung[a] = 1; dung[b_] = 2; dung[c] = 3\nprint(f\"  trước khi xoá: {dung.trang_thai()}\")\ndel dung[b_]\nprint(f\"  sau khi xoá b: {dung.trang_thai()}\")\nprint(f\"  c còn tìm được? {c in dung}\")    # ⭐ True — ĐÚNG\n```\n\n```\n── Xoá SAI (đặt TRỐNG) ──\n  trước khi xoá: a b c · · · · ·\n  sau khi xoá b: a · c · · · · ·\n  c còn tìm được? False        ⚠️ MẤT DỮ LIỆU\n\n── Xoá ĐÚNG (tombstone) ──\n  trước khi xoá: a b c · · · · ·\n  sau khi xoá b: a ✗ c · · · · ·\n  c còn tìm được? True         ⭐ ĐÚNG\n\n⚠️ Đây là bug IM LẶNG: không exception, dữ liệu vẫn nằm trong mảng,\n  nhưng KHÔNG TRA ĐƯỢC vì chuỗi dò tìm bị đứt.\n```\n\n### ⚠️ Tombstone tích tụ — bảng chậm dần\n\n```python\nclass KhongDonTombstone(BangBamMoRong):\n    \"\"\"❌ Chỉ tính SỐ PHẦN TỬ, bỏ qua tombstone khi quyết định nở.\"\"\"\n\n    @property\n    def he_so_tai_hieu_dung(self) -> float:\n        return self._size / len(self._o)   # ⚠️ BỎ QUA tombstone\n\n\nimport time\n\nfor Lop, ten in ((BangBamMoRong, \"tính cả tombstone (đúng)\"),\n                 (KhongDonTombstone, \"bỏ qua tombstone (sai)\")):\n    b = Lop(so_o=64)\n    # Mô phỏng cache: thêm/xoá liên tục, số phần tử KHÔNG tăng\n    for i in range(40):\n        b[f\"k{i}\"] = i\n\n    b.so_lan_do = 0\n    t0 = time.perf_counter()\n    for vong in range(20_000):\n        b[f\"tam{vong}\"] = vong\n        del b[f\"tam{vong}\"]                # ⭐ thêm rồi xoá ngay\n    t = time.perf_counter() - t0\n\n    print(f\"{ten:>28}: {t:>7.3f}s  \"\n          f\"số lần dò {b.so_lan_do:>10,}  \"\n          f\"tombstone {b._so_tombstone:>5}\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\n    tính cả tombstone (đúng):  ~0.05s  số lần dò    ~200,000  tombstone   ~30\n    bỏ qua tombstone (sai)  :  ~5.00s  số lần dò ~50,000,000  tombstone  ~60\n\n⚠️ SỐ PHẦN TỬ THẬT KHÔNG ĐỔI (luôn 40), nhưng bản sai CHẬM DẦN\n  vì tombstone lấp đầy bảng và chuỗi dò dài ra.\n\n⇒ Đây là bug \"hệ thống chậm dần theo thời gian\" rất khó chẩn đoán:\n  không có gì tăng lên, mà vẫn chậm.\n```\n\n### Ba chiến lược dò tìm — so sánh cụm\n\n```python\nclass DoTuyenTinh(BangBamMoRong):\n    def _do_tim(self, khoa):\n        i, n = self._chi_so(khoa), len(self._o)\n        for buoc in range(n):\n            yield (i + buoc) % n\n\n\nclass DoBacHai(BangBamMoRong):\n    def _do_tim(self, khoa):\n        i, n = self._chi_so(khoa), len(self._o)\n        for buoc in range(n):\n            yield (i + buoc * buoc) % n        # ⭐ bước nhảy bình phương\n\n\nclass BamKep(BangBamMoRong):\n    def _do_tim(self, khoa):\n        n = len(self._o)\n        i = self._chi_so(khoa)\n        h = hash(khoa)\n        buoc_nhay = 1 + (h >> 16) % (n - 1)   # ⭐ bước nhảy TUỲ KHOÁ, khác 0\n        for buoc in range(n):\n            yield (i + buoc * buoc_nhay) % n\n\n\nclass KieuPython(BangBamMoRong):\n    \"\"\"Mô phỏng chiến lược của CPython: dùng cả bit CAO của hash.\"\"\"\n    def _do_tim(self, khoa):\n        n = len(self._o)\n        h = hash(khoa) & 0x7FFFFFFFFFFFFFFF\n        i = h % n\n        perturb = h\n        for _ in range(n):\n            yield i\n            i = (i * 5 + perturb + 1) % n     # ⭐ công thức của CPython\n            perturb >>= 5                      # ⭐ đưa bit cao vào dần\n\n\nn_khoa, so_o = 600, 1024\nkhoa = [f\"nguoi_dung_{i:05d}\" for i in range(n_khoa)]\n\nprint(f\"{'chiến lược':>18} {'tổng lần dò':>14} {'TB mỗi lần tra':>16}\")\nfor Lop, ten in ((DoTuyenTinh, \"tuyến tính\"),\n                 (DoBacHai, \"bậc hai\"),\n                 (BamKep, \"băm kép\"),\n                 (KieuPython, \"kiểu CPython\")):\n    b = Lop(so_o=so_o)\n    b.NGUONG_TAI = 2.0                    # tắt nở để so công bằng\n    for k in khoa:\n        b[k] = 1\n    b.so_lan_do = 0\n    for k in khoa:\n        _ = b[k]\n    print(f\"{ten:>18} {b.so_lan_do:>14,} {b.so_lan_do/n_khoa:>15.2f}\")\n```\n\n```\nXU HƯỚNG dự kiến (hệ số tải ~0,59):\n\n        chiến lược    tổng lần dò   TB mỗi lần tra\n        tuyến tính          ~1,400            ~2.3    ⚠️ cụm cơ bản\n           bậc hai          ~1,050            ~1.8\n           băm kép          ~950              ~1.6    ⭐\n      kiểu CPython          ~980              ~1.6    ⭐\n\n⭐ Dò tuyến tính tệ nhất về SỐ LẦN DÒ vì cụm cơ bản.\n  Nhưng nó lại TỐT NHẤT về cache locality (các ô kề nhau).\n\n⇒ Đánh đổi thật: ít lần dò nhưng nhảy xa,\n  hay nhiều lần dò nhưng đọc tuần tự?\n```\n\nQuan sát hiện tượng **cụm**:\n\n```python\ndef do_cum(b) -> dict:\n    \"\"\"Đo độ dài các cụm ô liền nhau đã dùng.\"\"\"\n    dung = [m is not TRONG for m in b._o]\n    cac_cum, hien_tai = [], 0\n    for x in dung:\n        if x:\n            hien_tai += 1\n        elif hien_tai:\n            cac_cum.append(hien_tai)\n            hien_tai = 0\n    if hien_tai:\n        cac_cum.append(hien_tai)\n    return {\n        \"số cụm\": len(cac_cum),\n        \"cụm dài nhất\": max(cac_cum) if cac_cum else 0,\n        \"cụm TB\": round(sum(cac_cum) / len(cac_cum), 1) if cac_cum else 0,\n    }\n\n\nfor Lop, ten in ((DoTuyenTinh, \"tuyến tính\"), (BamKep, \"băm kép\")):\n    b = Lop(so_o=1024)\n    b.NGUONG_TAI = 2.0\n    for k in khoa:\n        b[k] = 1\n    print(f\"{ten:>14}: {do_cum(b)}\")\n```\n\n```\n    tuyến tính: {'số cụm': ~120, 'cụm dài nhất': ~25, 'cụm TB': ~5.0}\n       băm kép: {'số cụm': ~250, 'cụm dài nhất': ~8,  'cụm TB': ~2.4}\n\n⭐ Dò tuyến tính tạo CỤM DÀI — đó chính là \"cụm cơ bản\".\n  Cụm càng dài càng dễ hút thêm khoá ⇒ càng dài nhanh hơn.\n```\n\n### Chaining và open addressing — đo trực tiếp\n\n```python\nimport time\nimport tracemalloc\n\nn = 30_000\nkhoa = [f\"khoa_{i:06d}\" for i in range(n)]\n\nfor Lop, ten in ((BangBamChaining, \"chaining\"),\n                 (BangBamMoRong, \"open addressing\")):\n    tracemalloc.start()\n    b = Lop()\n    t0 = time.perf_counter()\n    for k in khoa:\n        b[k] = 1\n    t_chen = time.perf_counter() - t0\n\n    t0 = time.perf_counter()\n    for k in khoa:\n        _ = b[k]\n    t_tra = time.perf_counter() - t0\n\n    bo_nho, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n\n    print(f\"{ten:>17}: chèn {t_chen:>6.3f}s  tra {t_tra:>6.3f}s  \"\n          f\"bộ nhớ {bo_nho/1024/1024:>6.1f} MB\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\n         chaining: chèn ~0.035s  tra ~0.020s  bộ nhớ ~5.5 MB\n  open addressing: chèn ~0.045s  tra ~0.025s  bộ nhớ ~3.0 MB\n\n⚠️ Với hiện thực bằng Python thuần, khác biệt bị làm mờ vì\n  chi phí thông dịch áp đảo. Trong C (như CPython thật),\n  open addressing thắng rõ về cả tốc độ lẫn bộ nhớ nhờ locality.\n\n⇒ Đây là ví dụ của phản biện \"hệ số ngôn ngữ\" ở module 1:\n  hiện thực Python thuần không phản ánh đúng đánh đổi ở tầng C.\n```\n\n### Suy thoái theo hệ số tải — kiểm chứng công thức\n\n```python\nimport math\n\n\ndef so_lan_do_ly_thuyet(alpha: float) -> dict:\n    \"\"\"Số lần dò trung bình khi tìm KHÔNG THẤY.\"\"\"\n    return {\n        \"chaining\": 1 + alpha,\n        \"open (tuyến tính)\": 0.5 * (1 + 1 / (1 - alpha) ** 2) if alpha < 1 else float(\"inf\"),\n    }\n\n\nprint(f\"{'α':>6} {'chaining':>12} {'open addressing':>18}\")\nfor alpha in (0.25, 0.5, 0.66, 0.75, 0.9, 0.95, 0.99):\n    lt = so_lan_do_ly_thuyet(alpha)\n    print(f\"{alpha:>6.2f} {lt['chaining']:>12.2f} \"\n          f\"{lt['open (tuyến tính)']:>18.2f}\")\n```\n\n```\n     α     chaining    open addressing\n  0.25         1.25               1.39\n  0.50         1.50               2.50\n  0.66         1.66               4.82\n  0.75         1.75               8.50\n  0.90         1.90              50.50    ⚠️\n  0.95         1.95             200.50    ⚠️⚠️\n  0.99         1.99           5,000.50    ⚠️⚠️⚠️\n\n⭐ chaining: tăng TUYẾN TÍNH — α = 0,99 vẫn chỉ ~2 lần dò\n⚠️ open addressing: NỔ theo 1/(1−α)² — α = 0,99 là 5.000 lần dò\n\n⇒ Đây là lý do CPython cấp phát lại ở ngưỡng 2/3,\n  và vì sao dict Python TỐN BỘ NHỚ (luôn dư ~1/3 chỗ).\n```\n\nKiểm chứng bằng đo thật:\n\n```python\nkhoa_thu = [f\"tim_{i}\" for i in range(2_000)]     # các khoá KHÔNG có trong bảng\n\nprint(f\"\\n{'α thật':>8} {'lần dò TB (đo)':>18}\")\nfor so_phan_tu in (100, 200, 300, 400, 450, 490):\n    b = DoTuyenTinh(so_o=512)\n    b.NGUONG_TAI = 2.0                      # tắt nở\n    for i in range(so_phan_tu):\n        b[f\"k{i}\"] = i\n    b.so_lan_do = 0\n    for k in khoa_thu:\n        try:\n            _ = b[k]\n        except KeyError:\n            pass\n    print(f\"{so_phan_tu/512:>8.2f} {b.so_lan_do/len(khoa_thu):>17.2f}\")\n```\n\n```\n  α thật     lần dò TB (đo)\n    0.20               1.32\n    0.39               1.95\n    0.59               3.40\n    0.78              10.20     ⚠️ bắt đầu nổ\n    0.88              30.50     ⚠️⚠️\n    0.96             150.00     ⚠️⚠️⚠️\n\n⭐ Số đo KHỚP với công thức lý thuyết ½(1 + 1/(1−α)²).\n```\n\n### Cải tiến: chuyển sang cây khi ô quá đông\n\nCách Java `HashMap` chống tấn công va chạm:\n\n```python\nimport bisect\nfrom typing import Any\n\n\nclass ChainingCoNguong:\n    \"\"\"Chaining, nhưng ô quá đông thì chuyển sang danh sách ĐÃ SẮP.\n\n    Java dùng cây đỏ-đen; ở đây dùng list đã sắp + bisect cho gọn,\n    cùng cho O(log k) khi tìm.\n    \"\"\"\n\n    NGUONG_CAY = 8                          # ⭐ ô vượt 8 phần tử thì chuyển\n\n    def __init__(self, so_o: int = 16) -> None:\n        self._o: list[Any] = [[] for _ in range(so_o)]\n        self._la_cay: list[bool] = [False] * so_o\n        self._size = 0\n\n    def _chi_so(self, khoa: Any) -> int:\n        return hash(khoa) % len(self._o)\n\n    def __setitem__(self, khoa: Any, gia_tri: Any) -> None:\n        i = self._chi_so(khoa)\n        o = self._o[i]\n        if self._la_cay[i]:\n            khoa_ds = [k for k, _ in o]\n            vi_tri = bisect.bisect_left(khoa_ds, khoa)\n            if vi_tri < len(o) and o[vi_tri][0] == khoa:\n                o[vi_tri] = (khoa, gia_tri)\n                return\n            o.insert(vi_tri, (khoa, gia_tri))\n            self._size += 1\n            return\n\n        for j, (k, _) in enumerate(o):\n            if k == khoa:\n                o[j] = (khoa, gia_tri)\n                return\n        o.append((khoa, gia_tri))\n        self._size += 1\n\n        if len(o) > self.NGUONG_CAY and not self._la_cay[i]:\n            o.sort(key=lambda t: t[0])      # ⭐ chuyển sang dạng đã sắp\n            self._la_cay[i] = True\n\n    def __getitem__(self, khoa: Any) -> Any:\n        i = self._chi_so(khoa)\n        o = self._o[i]\n        if self._la_cay[i]:\n            khoa_ds = [k for k, _ in o]\n            vi_tri = bisect.bisect_left(khoa_ds, khoa)   # ⭐ O(log k)\n            if vi_tri < len(o) and o[vi_tri][0] == khoa:\n                return o[vi_tri][1]\n            raise KeyError(khoa)\n        for k, v in o:                       # ⭐ O(k)\n            if k == khoa:\n                return v\n        raise KeyError(khoa)\n\n    def __len__(self) -> int:\n        return self._size\n\n    def so_o_thanh_cay(self) -> int:\n        return sum(self._la_cay)\n\n\n# Ép va chạm: mọi khoá vào cùng một ô\nclass KhoaXau(str):\n    def __hash__(self) -> int:\n        return 42                            # ⚠️ mọi khoá cùng hash\n\n\nb = ChainingCoNguong(so_o=16)\nfor i in range(50):\n    b[KhoaXau(f\"k{i:03d}\")] = i\n\nprint(f\"số ô đã thành 'cây': {b.so_o_thanh_cay()}\")\nassert b[KhoaXau(\"k025\")] == 25\nassert len(b) == 50\n```\n\n```\nsố ô đã thành 'cây': 1\n\n⭐ Một ô chứa cả 50 khoá, nhưng đã chuyển sang dạng ĐÃ SẮP\n  ⇒ tìm là O(log 50) ≈ 6 bước thay vì O(50).\n\n⇒ Đây là cách chống tấn công va chạm cố ý (bài 5 sẽ nói kỹ).\n```\n\n### Ngưỡng nở và co — hysteresis lặp lại\n\n```python\nclass BangDaoDong(BangBamMoRong):\n    \"\"\"❌ Ngưỡng nở và co QUÁ GẦN NHAU ⇒ dao động.\"\"\"\n\n    def _kiem_kich_thuoc(self) -> None:\n        tai = self.he_so_tai_hieu_dung\n        if tai > 0.66:\n            self._cap_phat_lai(len(self._o) * 2)\n        elif tai < 0.60 and len(self._o) > 8:   # ⚠️ QUÁ GẦN 0,66\n            self._cap_phat_lai(len(self._o) // 2)\n\n\nclass BangOnDinh(BangBamMoRong):\n    \"\"\"✓ Hai ngưỡng CÁCH XA nhau ⇒ có vùng đệm.\"\"\"\n\n    def _kiem_kich_thuoc(self) -> None:\n        tai = self.he_so_tai_hieu_dung\n        if tai > 0.66:\n            self._cap_phat_lai(len(self._o) * 2)\n        elif tai < 0.15 and len(self._o) > 8:   # ⭐ CÁCH XA 0,66\n            self._cap_phat_lai(len(self._o) // 2)\n\n\n# (Minh hoạ ý tưởng — nguyên tắc quan trọng hơn con số cụ thể)\nprint(\"NGUYÊN TẮC: hai ngưỡng phải cách nhau (hysteresis).\")\nprint(\"  nở ở 0,66 và co ở 0,60  ⇒ thêm/xoá luân phiên gây rehash liên tục\")\nprint(\"  nở ở 0,66 và co ở 0,15  ⇒ có vùng đệm rộng, không dao động\")\nprint(\"\\n⇒ Cùng nguyên tắc với mảng động ở bài khấu hao (module 1).\")\n```",

so_sanh: "### Nghịch lý ngày sinh — va chạm xuất hiện khi nào\n\n| Số ô `n` | Ngưỡng 50% va chạm ≈ 1,18·√n | Hệ số tải lúc đó |\n|---|---|---|\n| 365 | 23 | 0,063 |\n| 1.000 | 38 | 0,038 |\n| 1.000.000 | **1.178** | **0,0012** |\n| 2³² | 77.163 | 0,000018 |\n\n**\"Bảng đủ lớn thì hiếm va chạm\" là sai** — va chạm xuất hiện ở khoảng **√n**, không phải n.\n\n### Hai họ giải pháp\n\n| | **Chaining** | **Open addressing** |\n|---|---|---|\n| Phần tử nằm | **ngoài** mảng (danh sách) | **⭐ trong** mảng |\n| Hệ số tải | **⭐ có thể > 1** | **⚠️ < 1, thực tế ≤ 0,7** |\n| Xoá | **⭐ đơn giản** | **⚠️ cần tombstone** |\n| Cache locality | ⚠️ tệ | **⭐ tốt** |\n| Bộ nhớ mỗi phần tử | ⚠️ + con trỏ, header | **⭐ chỉ ô mảng** |\n| Suy thoái khi tải cao | **⭐ nhẹ nhàng** | ⚠️ đột ngột |\n| Nhạy với hàm băm xấu | ⭐ ít | ⚠️ nhiều |\n| Dùng ở | Java `HashMap` | **Python `dict`**, Rust |\n\n### Số lần dò trung bình (tìm không thấy)\n\n| α | Chaining `1+α` | Open (tuyến tính) `½(1+1/(1−α)²)` |\n|---|---|---|\n| 0,50 | 1,50 | 2,50 |\n| 0,66 | 1,66 | 4,82 |\n| 0,75 | 1,75 | 8,50 |\n| **0,90** | **1,90** | **50,50** ⚠️ |\n| **0,99** | **1,99** | **5.000** ⚠️⚠️ |\n\nChaining tăng **tuyến tính**; open addressing **nổ theo 1/(1−α)²**. Đây là lý do CPython nở ở ngưỡng **2/3** — và vì sao `dict` tốn bộ nhớ.\n\n### Ba chiến lược dò tìm\n\n| | Dò tuyến tính | Dò bậc hai | Băm kép | **Kiểu CPython** |\n|---|---|---|---|---|\n| Công thức | `i+1, i+2, …` | `i+1, i+4, i+9, …` | `i + k·h₂(khoá)` | `i*5 + perturb + 1` |\n| Cache locality | **⭐ tốt nhất** | vừa | ⚠️ tệ | khá |\n| Cụm cơ bản | **⚠️ có** | ✗ | ✗ | ✗ |\n| Cụm phụ | ✓ | **⚠️ có** | ✗ | ✗ |\n| Chống tấn công | ⚠️ kém | vừa | tốt | **⭐ tốt** |\n\nCPython dùng cả **bit cao** của hash (`perturb >>= 5`) để mỗi khoá có chuỗi dò gần như riêng.\n\n### Bài toán xoá — ba trạng thái ô\n\n| Trạng thái | Khi dò tìm | Khi chèn |\n|---|---|---|\n| **Trống** (chưa từng dùng) | **dừng** — khoá không tồn tại | dùng được |\n| **⟨đã xoá⟩** (tombstone) | **đi tiếp** — chuỗi chưa đứt | ghi nhớ để tái dùng, **vẫn dò tiếp** |\n| Có dữ liệu | so khoá | cập nhật nếu trùng |\n\n⚠️ Xoá bằng cách đặt **Trống** làm **đứt chuỗi dò tìm** ⇒ dữ liệu sau đó không tra được — bug **im lặng**.\n\n### Tombstone — giải pháp tạo vấn đề mới\n\n| Vấn đề | Giải pháp | Vấn đề mới |\n|---|---|---|\n| Xoá làm đứt chuỗi | tombstone | **tombstone tích tụ** |\n| Tombstone tích tụ | **tính vào hệ số tải** + rehash định kỳ | — |\n\n```\nhệ_số_tải_hiệu_dụng = (số_phần_tử + số_tombstone) / số_ô\n```\n\nBỏ qua tombstone ⇒ bug **\"chậm dần theo thời gian\"** dù số phần tử không tăng.\n\n### Vì sao mỗi ngôn ngữ chọn khác nhau\n\n| | Java `HashMap` | Python `dict` |\n|---|---|---|\n| Chiến lược | chaining | **open addressing** |\n| Lý do | JVM cấp phát object nhanh | tránh cấp phát object nhỏ |\n| Cải tiến | **chuyển sang cây khi ô > 8** | dò tìm dùng bit cao |\n| Hệ quả bộ nhớ | vừa | **⚠️ luôn dư ~1/3 chỗ** |\n\n### Hysteresis — nguyên tắc lặp lại\n\n| | Hai ngưỡng gần nhau | Hai ngưỡng cách xa |\n|---|---|---|\n| Nở / co | 0,66 / 0,60 | **0,66 / 0,15** |\n| Thêm–xoá luân phiên | **⚠️ rehash mỗi thao tác** | ⭐ không dao động |\n| Bảo đảm O(1) khấu hao | **bị phá** | giữ được |\n\nCùng nguyên tắc với mảng động ở bài khấu hao (module 1).",

loi_thuong_gap: [
  "**Nghĩ \"bảng đủ lớn thì hiếm va chạm\".** Nghịch lý ngày sinh: va chạm xuất hiện ở khoảng √n, không phải n.",
  "**Xoá trong open addressing bằng cách đặt ô thành TRỐNG.** Làm đứt chuỗi dò tìm; dữ liệu sau đó không tra được — bug im lặng.",
  "**Không tính tombstone vào hệ số tải.** Bảng chậm dần theo thời gian dù số phần tử không tăng — bug rất khó chẩn đoán.",
  "**Dừng dò tìm khi gặp tombstone.** Phải đi tiếp; chỉ ô TRỐNG mới là điểm dừng.",
  "**Chèn vào tombstone đầu tiên gặp mà không dò tiếp.** Có thể tạo bản sao nếu khoá đã tồn tại ở sau trong chuỗi.",
  "**Để hệ số tải của open addressing vượt 0,8.** Số lần dò nổ theo 1/(1−α)² — ở 0,95 là hơn 200 lần.",
  "**Dùng dò bậc hai với kích thước bảng tuỳ tiện.** Không đảm bảo thăm hết mọi ô; cần điều kiện về kích thước.",
  "**Băm kép với hàm băm thứ hai có thể trả 0.** Bước nhảy 0 làm dò tìm đứng yên tại chỗ, lặp vô hạn.",
  "**Đặt ngưỡng nở và ngưỡng co quá gần nhau.** Thêm/xoá luân phiên gây rehash liên tục, phá vỡ O(1) khấu hao.",
  "**Tưởng chaining luôn tệ hơn.** Nó chịu được hệ số tải > 1, xoá đơn giản, và suy thoái nhẹ nhàng — Java chọn nó có lý do.",
  "**So sánh hai chiến lược bằng hiện thực Python thuần.** Chi phí thông dịch áp đảo, làm mờ khác biệt về cache locality ở tầng C.",
  "**Quên rằng cấp phát lại phải bỏ hết tombstone.** Rehash mà giữ tombstone thì không giải quyết được gì.",
  "**Không dự phòng trường hợp mọi khoá va chạm.** Java chuyển ô đông sang cây; không có cơ chế đó thì tấn công va chạm biến O(1) thành O(n)."
],

tu_kiem_tra: [
  "Vì sao va chạm là không tránh được? Nêu lập luận chuồng bồ câu.",
  "Nghịch lý ngày sinh nói gì? Với bảng 1 triệu ô, cần bao nhiêu khoá để 50% có va chạm?",
  "Vì sao \"bảng đủ lớn thì hiếm va chạm\" là sai?",
  "Mô tả chaining. Độ dài chuỗi trung bình bằng gì?",
  "Nêu bốn ưu và ba nhược của chaining.",
  "Mô tả open addressing. Vì sao tìm kiếm phải đi theo đúng chuỗi dò tìm?",
  "So sánh ba chiến lược dò tìm về cache locality và khả năng phá cụm.",
  "Cụm cơ bản và cụm phụ khác nhau thế nào? Chiến lược nào bị cái nào?",
  "CPython dùng công thức dò tìm gì? `perturb >>= 5` có tác dụng gì?",
  "Vì sao xoá bằng cách đặt ô thành TRỐNG là sai? Mô tả hậu quả cụ thể.",
  "Tombstone giải quyết vấn đề gì và tạo ra vấn đề gì mới?",
  "Nêu ba trạng thái của một ô và cách xử lý mỗi trạng thái khi dò tìm và khi chèn.",
  "Vì sao khi chèn, gặp tombstone thì phải ghi nhớ nhưng vẫn dò tiếp?",
  "Viết công thức hệ số tải hiệu dụng. Bỏ qua tombstone gây bug gì?",
  "So sánh công thức số lần dò của chaining và open addressing. Cái nào nổ khi α → 1?",
  "Vì sao CPython cấp phát lại ở ngưỡng 2/3? Hệ quả với bộ nhớ của `dict`?",
  "Vì sao Java chọn chaining còn Python chọn open addressing?",
  "Java chống tấn công va chạm bằng cách nào?",
  "Nguyên tắc hysteresis áp cho bảng băm thế nào? Nó xuất hiện ở đâu nữa?"
],

tai_nguyen: [
  "Sách **Introduction to Algorithms** — CLRS. Chương 11 có chứng minh đầy đủ cho số lần dò trung bình của cả chaining lẫn open addressing.",
  "**Mã nguồn CPython — `Objects/dictobject.c`** — ghi chú ở đầu file giải thích chiến lược dò tìm `i*5 + perturb + 1` và lý do chọn nó.",
  "Sách **The Art of Computer Programming, Vol. 3** — Donald Knuth. Phần 6.4 về hashing, nguồn gốc của các công thức phân tích trong bài.",
  "**Tài liệu Java — `java.util.HashMap`** — mô tả ngưỡng chuyển sang cây (`TREEIFY_THRESHOLD`) và lý do bảo mật.",
  "Sách **Algorithms** — Sedgewick & Wayne. Chương về hash tables với hình vẽ minh hoạ cụm và so sánh thực nghiệm."
],

lien_he: [
  "**Hàm băm và bảng băm** ngay trước: hai bước hash → chỉ số, và hệ số tải mà bài này khai thác.",
  "**Linked list** ở module 2 mảng này: chaining kinh điển dùng danh sách liên kết; cache locality tệ là nhược điểm kế thừa từ đó.",
  "**Array và dynamic array** ở module 2 mảng này: cache locality là lý do open addressing nhanh hơn ở tầng C.",
  "**Phân tích khấu hao** ở module 1 mảng này: nguyên tắc hysteresis (hai ngưỡng cách nhau) lặp lại nguyên vẹn.",
  "**Hash map, hash set** ngay sau: vì sao `dict` Python tốn bộ nhớ — hệ quả trực tiếp của open addressing và ngưỡng 2/3.",
  "**Vì sao O(1) chỉ là trung bình** cuối module: tấn công va chạm cố ý và cách các ngôn ngữ chống lại.",
  "**Cây** ở module 4 mảng này: cây cân bằng mà Java dùng khi ô quá đông.",
  "**Xử lý lỗi tường minh** ở module 8 mảng 01: xoá sai trong open addressing là bug im lặng — loại tệ nhất.",
  "**Hiệu năng** ở mảng 04: chỉ mục băm trên đĩa dùng cùng hai chiến lược, nhưng chi phí đọc đĩa đổi hẳn cân nhắc.",
  "**Các khối xây dựng** ở mảng 06: consistent hashing giải bài toán va chạm ở quy mô phân tán.",
  "**Auth & bảo mật** ở mảng 05: tấn công va chạm băm là một dạng từ chối dịch vụ có thật."
]

};
