window.LESSONS["05:trien-khai:nginx-lam-reverse-proxy"] = {

tom_tat: [
  "Nginx đứng ở biên mạng, nhận kết nối công khai rồi chuyển yêu cầu hợp lệ vào FastAPI đang nghe trên địa chỉ nội bộ; đó là reverse proxy, không phải nơi chứa nghiệp vụ.",
  "Proxy phải truyền đúng `Host`, địa chỉ máy khách và giao thức ban đầu, nếu không ứng dụng sẽ ghi log sai, tạo URL sai hoặc vô tình tin dữ liệu do người dùng giả mạo.",
  "Timeout, giới hạn thân yêu cầu, buffering và số worker là chính sách vận hành; giá trị đúng phụ thuộc kiểu endpoint chứ không có một cấu hình thần kỳ cho mọi API.",
  "Một cấu hình tốt chỉ công khai cổng 80/443, giữ Uvicorn ở loopback hoặc mạng riêng, có health check, request ID và cách reload kiểm chứng được.",
  "Nginx không thay thế xác thực, phân quyền, validation hay rate limit theo danh tính ở tầng FastAPI."
],

can_biet_truoc: [
  "HTTP request/response, header `Host`, status code và phương thức GET/POST.",
  "TCP port, DNS và khác biệt giữa địa chỉ loopback `127.0.0.1` với địa chỉ công khai.",
  "FastAPI chạy bằng ASGI server Uvicorn và khái niệm process.",
  "Quyền file Linux, systemd và log cơ bản."
],

dinh_nghia: "**Reverse proxy** là máy chủ nhận request thay mặt một hay nhiều dịch vụ phía sau, chọn upstream rồi chuyển request đến đó và trả response về cho máy khách. Với Sổ Chi Tiêu, trình duyệt chỉ biết `api.sochitieu.example`; Nginx giữ socket công khai tại cổng 80/443, còn Uvicorn chỉ nghe `127.0.0.1:8000`. Chiều “reverse” nói rằng proxy đại diện cho server, trái với forward proxy đại diện cho client đi ra Internet.\n\nRanh giới này tạo hai lớp trách nhiệm. Nginx xử lý kết nối, TLS, giới hạn kích thước, file tĩnh và chính sách chung theo request. FastAPI hiểu người dùng, giao dịch, quyền xem dữ liệu và transaction PostgreSQL. Đẩy nghiệp vụ vào cấu hình Nginx khiến quy tắc khó test; để Uvicorn lộ Internet lại bỏ mất một lớp kiểm soát biên hữu ích.",

vi_sao_can: "Uvicorn rất phù hợp thực thi ASGI nhưng một tiến trình ứng dụng không nên tự gánh mọi việc ở biên. Kết nối chậm có thể giữ tài nguyên lâu; upload hóa đơn có thể quá lớn; client có thể gửi header bất thường; deploy cần đổi process mà không làm DNS thay đổi. Nginx hấp thụ các chi tiết đó bằng một địa chỉ ổn định.\n\nSổ Chi Tiêu có ba luồng khác nhau: JSON nhỏ tại `/api/transactions`, ảnh hóa đơn tại `/api/receipts`, và kiểm tra sống tại `/health`. Nếu cho mọi luồng cùng timeout và kích thước, hoặc ảnh lớn bị từ chối oan, hoặc endpoint JSON bị treo quá lâu khi upstream lỗi. Reverse proxy cho phép mô tả chính sách theo đường dẫn trong khi code nghiệp vụ vẫn tập trung.\n\nLợi ích bảo mật quan trọng nhất không phải “Nginx an toàn hơn” một cách mơ hồ, mà là giảm bề mặt tiếp xúc: firewall chỉ mở 80/443, cổng 8000 không thể bị gọi vòng qua giới hạn của proxy. Tuy vậy, request đã qua Nginx vẫn là dữ liệu không tin cậy; SQL injection, IDOR và token giả phải bị chặn trong ứng dụng.",

co_che: "### Đường đi của một request\n\nKhi điện thoại gọi `POST /api/transactions`, DNS trả IP của máy chủ và TCP kết nối tới Nginx. Nginx đọc request line cùng header, tìm `server_name`, chọn `location`, kiểm tra giới hạn rồi mở hoặc tái sử dụng kết nối tới Uvicorn. FastAPI tạo giao dịch trong PostgreSQL, trả JSON; Nginx chuyển status, header và body về client. Hai kết nối độc lập cho phép phía ngoài dùng HTTPS còn đoạn loopback dùng HTTP mà không làm lộ nội dung ra mạng vật lý.\n\nĐiểm cần nhớ là Nginx không “gọi hàm Python”. Nó phát một request HTTP mới tới upstream. Bởi vậy mọi chi tiết cần thiết phải tồn tại trong method, path, header và body. Nếu proxy làm mất query string hoặc đổi path sai, route FastAPI không thể đoán ý ban đầu.\n\n### Chọn nơi Uvicorn lắng nghe\n\nTrên một máy, chạy `uvicorn app:app --host 127.0.0.1 --port 8000` khóa cổng vào loopback. `0.0.0.0` nghĩa là lắng nghe mọi interface, không đồng nghĩa “localhost”; dùng nó trong production mà firewall mở cổng 8000 sẽ tạo đường vòng. Với Docker Compose, Nginx gọi tên service qua mạng bridge và Uvicorn có thể nghe `0.0.0.0` bên trong container, nhưng không cần publish `8000:8000` ra host.\n\nUnix socket là lựa chọn khác trên cùng máy. Nó tránh một cổng TCP và có thể kiểm soát bằng quyền file, song chẩn đoán bằng `curl` kém tiện và cấu hình quyền giữa systemd/Nginx dễ sai. TCP loopback thường đủ rõ cho hệ nhỏ.\n\n### Ánh xạ server và location\n\nNginx chọn khối `server` theo địa chỉ/cổng rồi `server_name`; sau đó chọn `location` cụ thể nhất. Dấu gạch chéo ở `proxy_pass` có ý nghĩa. Với `location /api/` và `proxy_pass http://app;`, đường dẫn được giữ nguyên. Nếu viết `proxy_pass http://app/;`, phần khớp `/api/` có thể được thay bằng `/`, khiến `/api/transactions` thành `/transactions`. Đây là lỗi cấu hình chứ không phải lỗi router FastAPI.\n\nKhông dùng regex khi prefix bình thường đủ đáp ứng. Regex làm thứ tự chọn location khó đọc và dễ để một route upload rơi vào chính sách mặc định. Tách `/health`, `/api/receipts/` và `/api/` chỉ khi chúng thật sự có hành vi vận hành khác nhau.\n\n### Header chuyển tiếp và ranh giới tin cậy\n\nFastAPI cần biết host, scheme và IP gốc để log, redirect và tạo liên kết. Nginx nên đặt `Host $host`, `X-Real-IP $remote_addr`, `X-Forwarded-For $proxy_add_x_forwarded_for`, `X-Forwarded-Proto $scheme`. Biến `$proxy_add_x_forwarded_for` nối IP do Nginx quan sát vào chuỗi hiện có; ứng dụng không được mù quáng tin phần client tự gửi.\n\nChỉ bật xử lý proxy headers ở Uvicorn cho địa chỉ proxy đáng tin. Nếu cổng ứng dụng công khai và mọi nguồn đều được tin, kẻ gọi trực tiếp có thể khai `X-Forwarded-Proto: https` hoặc IP nạn nhân. Proxy header là lời khẳng định có thẩm quyền chỉ khi đường mạng bảo đảm người viết nó là proxy của mình.\n\n### Buffering và luồng dữ liệu\n\nMặc định Nginx có thể đọc response upstream vào buffer trước khi gửi tiếp. Điều đó giải phóng worker ứng dụng khỏi client tải chậm và cho phép Nginx phục vụ dữ liệu hiệu quả. Với JSON giao dịch, đây thường là lợi thế. Với Server-Sent Events hoặc streaming liên tục, buffering làm người dùng không nhận từng sự kiện đúng lúc; route đó cần tắt có chủ đích và kiểm tra cả timeout.\n\nRequest buffering cũng có đánh đổi. Nginx có thể nhận xong ảnh hóa đơn rồi mới chuyển vào FastAPI, bảo vệ ứng dụng khỏi người upload cực chậm. Đổi lại cần vùng tạm và tăng độ trễ trước khi code bắt đầu xử lý. Không nên tắt toàn cục chỉ vì một endpoint streaming.\n\n### Timeout là ngân sách, không phải thuốc chữa\n\n`proxy_connect_timeout` giới hạn thời gian nối upstream; `proxy_read_timeout` nói khoảng im lặng khi chờ dữ liệu, không nhất thiết là tổng thời gian request. Đặt vô hạn che giấu truy vấn DB treo và giữ socket. Đặt quá ngắn tạo 504 dù nghiệp vụ hợp lệ. Trước hết định nghĩa ngân sách: API tương tác phải nhanh, tác vụ lâu nên đưa vào queue và trả mã theo dõi thay vì giữ HTTP hàng phút.\n\nKhi thấy 504, đối chiếu access log Nginx, error log và log request ID của FastAPI. Tăng timeout ngay chỉ chuyển triệu chứng thành hàng đợi dài hơn. Hỏi upstream có nhận request không, PostgreSQL chậm ở đâu, hay worker đã cạn.\n\n### Giới hạn kích thước và tài nguyên\n\n`client_max_body_size` nên khớp nghiệp vụ. Sổ Chi Tiêu cho ảnh hóa đơn tối đa 8 MiB thì proxy có thể chặn lớn hơn mức đó một chút trước khi Python đọc body. FastAPI vẫn phải kiểm tra MIME, kích thước sau giải mã và quyền sở hữu; header `Content-Type` có thể giả. Nginx trả 413 trước ứng dụng, vì vậy frontend phải hiểu status này.\n\nGiới hạn request không giải quyết zip bomb, ảnh giải nén khổng lồ hay nhiều upload nhỏ song song. Cần quota theo người dùng và giới hạn ở storage. Rate limit theo IP tại proxy hữu ích chống ồn thô, nhưng người dùng sau NAT chia IP và kẻ tấn công có nhiều IP; quota nghiệp vụ thuộc về FastAPI.\n\n### Cân bằng nhiều worker và trạng thái\n\nUpstream có thể chứa nhiều Uvicorn process hoặc nhiều máy. Nginx phân phối request, nhưng không làm biến toàn cục Python thành dữ liệu chung. Nếu một worker lưu session hoặc trạng thái job trong RAM, request kế tiếp có thể tới worker khác. Session nên nằm trong cookie có chữ ký, Redis hoặc database tùy nhu cầu.\n\nHealth check `/health` chỉ nên chứng minh process đang phục vụ; readiness có thể kiểm tra phụ thuộc thiết yếu. Đừng trả bí mật, phiên bản package hoặc stack trace. Một probe quá nặng gọi database mỗi giây có thể tự tạo tải khi hệ thống đang yếu.\n\n### Reload không gián đoạn và rollback\n\n`nginx -t` phân tích cú pháp và tham chiếu cơ bản trước khi reload. Reload gửi cấu hình mới cho master; worker mới nhận kết nối, worker cũ hoàn tất việc đang làm. Cơ chế đó giảm gián đoạn nhưng không chứng minh route nghiệp vụ đúng. Sau reload phải gọi health và một request canary qua chính hostname công khai.\n\nGiữ cấu hình trong quản lý phiên bản và chuẩn bị file trước đó để rollback. Không sửa trực tiếp production rồi quên chép lại. Nếu thay cả ứng dụng, khởi động upstream mới và xác minh trước khi chuyển traffic; Nginx không cứu được migration database không tương thích ngược.\n\n### Log, request ID và dữ liệu nhạy cảm\n\nAccess log nên có thời gian request, thời gian upstream, status, byte và request ID. Chênh lệch giữa tổng thời gian và upstream time giúp phân biệt client chậm với FastAPI chậm. Nginx có thể tạo `$request_id`, truyền `X-Request-ID` xuống ứng dụng và trả lại để tra cứu xuyên lớp.\n\nKhông log `Authorization`, cookie phiên, query chứa token hay toàn bộ body giao dịch. Log là dữ liệu production, cần phân quyền và thời hạn lưu. IP cũng có thể là dữ liệu cá nhân. Quan sát tốt nghĩa là đủ để điều tra mà không biến hệ thống log thành kho bí mật thứ hai.\n\n### Khi không nên thêm Nginx\n\nNếu nền tảng quản lý đã cung cấp load balancer, TLS, giới hạn body và log ở biên, đặt thêm Nginx trong container chỉ để “đúng kiến trúc” làm tăng một hop và một nơi cấu hình. Với môi trường local, gọi Uvicorn trực tiếp đơn giản hơn. Nginx đáng dùng khi có trách nhiệm biên cụ thể và đội ngũ sở hữu được cấu hình, cập nhật bảo mật cùng giám sát của nó.",

vi_du: "### Cấu hình tối thiểu có chủ đích\n\nFastAPI cung cấp hai route và middleware request ID:\n\n```python\nfrom fastapi import FastAPI, Request, UploadFile, HTTPException\nfrom uuid import uuid4\n\napp = FastAPI()\n\n@app.middleware(\"http\")\nasync def request_id(request: Request, call_next):\n    rid = request.headers.get(\"x-request-id\") or str(uuid4())\n    response = await call_next(request)\n    response.headers[\"X-Request-ID\"] = rid\n    return response\n\n@app.get(\"/health\")\nasync def health():\n    return {\"status\": \"ok\"}\n\n@app.post(\"/api/receipts\")\nasync def receipt(file: UploadFile):\n    if file.content_type not in {\"image/jpeg\", \"image/png\"}:\n        raise HTTPException(415, \"Chỉ nhận JPEG hoặc PNG\")\n    return {\"name\": file.filename}\n```\n\nCấu hình `/etc/nginx/conf.d/sochitieu.conf`:\n\n```nginx\nupstream so_chi_tieu {\n    server 127.0.0.1:8000;\n    keepalive 16;\n}\n\nserver {\n    listen 80;\n    server_name api.sochitieu.example;\n    client_max_body_size 9m;\n\n    location = /health {\n        proxy_pass http://so_chi_tieu;\n        proxy_set_header Host $host;\n    }\n\n    location / {\n        proxy_pass http://so_chi_tieu;\n        proxy_http_version 1.1;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n        proxy_set_header X-Request-ID $request_id;\n        proxy_connect_timeout 3s;\n        proxy_read_timeout 30s;\n    }\n}\n```\n\nKiểm tra theo thứ tự để biết lỗi ở lớp nào:\n\n```bash\ncurl -i http://127.0.0.1:8000/health\nsudo nginx -t\nsudo systemctl reload nginx\ncurl -i -H 'Host: api.sochitieu.example' http://127.0.0.1/health\ncurl -i http://api.sochitieu.example/health\n```\n\nLệnh đầu cô lập FastAPI; lệnh áp chót đi qua Nginx nhưng không phụ thuộc DNS ngoài; lệnh cuối kiểm tra đường thật. Nếu upload vượt 9 MiB trả 413 và không xuất hiện trong log FastAPI, giới hạn proxy đang hoạt động đúng.",

so_sanh: "### Chọn lớp biên phù hợp\n\n| Phương án | Phù hợp khi | Điểm mạnh | Giá phải trả |\n|---|---|---|---|\n| Uvicorn công khai trực tiếp | học local, mạng kín tạm thời | ít thành phần | tự lo TLS, giới hạn và hardening; dễ lộ cổng |\n| Nginx trên cùng máy | VPS, cần kiểm soát rõ | cấu hình trưởng thành, file tĩnh và reload tốt | phải vá, log và giám sát thêm service |\n| Load balancer do cloud quản lý | nhiều instance, đội muốn giảm vận hành | health check và TLS tích hợp | chi phí, phụ thuộc nhà cung cấp |\n| API gateway | nhiều API cần quota/key/chính sách chung | quản trị API phong phú | phức tạp, độ trễ và giá cao hơn |\n\nNginx không tự động thắng mọi cột. Một EC2 duy nhất thường hưởng lợi từ nó; một dịch vụ trên nền tảng đã terminate TLS có thể không cần. Tiêu chí là ai chịu trách nhiệm socket công khai, chứng chỉ, giới hạn và quan sát, chứ không phải sơ đồ nào trông nhiều lớp hơn.",

loi_thuong_gap: [
  "Uvicorn nghe `0.0.0.0:8000` và security group mở cổng; nhận biết bằng cách gọi thẳng `IP:8000`, sửa bằng mạng riêng/firewall.",
  "`proxy_pass` có dấu gạch chéo làm mất `/api`; nhận biết qua 404 của FastAPI và access log path upstream.",
  "Không truyền `X-Forwarded-Proto`, khiến redirect quay về HTTP hoặc cookie thiếu Secure; so header và URL ứng dụng tạo ra.",
  "Tin proxy header từ mọi nguồn; kiểm tra cấu hình trusted proxies và thử gọi trực tiếp với IP giả.",
  "Tăng timeout để chữa truy vấn chậm; dấu hiệu là số kết nối chờ tăng và 504 chỉ xuất hiện muộn hơn.",
  "Đặt `client_max_body_size` toàn cục quá nhỏ hoặc vô hạn; nhận biết qua 413 oan hoặc bộ nhớ/đĩa tạm tăng.",
  "Reload không chạy `nginx -t`; lỗi cú pháp hiện rõ trong lệnh test và error log.",
  "Ghi token/cookie vào access log; rà format log và mẫu dữ liệu, sau đó thu hồi bí mật đã lộ chứ không chỉ xóa dòng."
],

tu_kiem_tra: [
  "Vì sao `127.0.0.1:8000` giảm bề mặt tấn công nhưng không thay thế validation trong FastAPI?",
  "Với `location /api/` và hai dạng `proxy_pass` có/không có dấu `/`, path upstream khác nhau thế nào?",
  "Tại sao ứng dụng chỉ được tin `X-Forwarded-For` từ proxy đã biết?",
  "Endpoint xuất báo cáo 90 giây nên tăng timeout hay chuyển sang job nền? Hãy nêu tiêu chí.",
  "Buffering bảo vệ worker trước client chậm nhưng gây hại cho Server-Sent Events ra sao?",
  "Thiết kế chuỗi kiểm tra phân biệt lỗi DNS, Nginx và Uvicorn mà không đoán mò.",
  "Nếu có ba worker, vì sao biến dict chứa session trong Python gây lỗi lúc có lúc không?"
],

tai_nguyen: [
  "Tài liệu chính thức Nginx — HTTP Proxy Module.",
  "Tài liệu FastAPI — Behind a Proxy.",
  "The Linux Command Line — William Shotts.",
  "Site Reliability Engineering — Google."
],

lien_he: [
  "Mảng Mạng máy tính: nối mô hình reverse proxy với TCP, HTTP header, DNS và địa chỉ IP.",
  "Mảng Bảo mật web: dùng ranh giới trusted proxy để hiểu spoofing, rate limit và bảo vệ upload.",
  "Mảng Database: theo dõi upstream time giúp tách nghẽn proxy khỏi truy vấn PostgreSQL chậm.",
  "Mảng DevOps: cấu hình Nginx trở thành artifact được kiểm tra, triển khai và rollback qua CI/CD."
]

};
