window.LESSONS["01:cong-cu-cua-dev:docker-co-ban-image-container-dockerfile"] = {

tom_tat: [
  "**Container không phải máy ảo.** Nó là một tiến trình bình thường trên kernel của máy chủ, chỉ bị **cô lập** bằng namespace và giới hạn bằng cgroup — nên khởi động trong mili giây, không phải phút.",
  "**Image là bản mẫu bất biến; container là một lần chạy của image.** Một image sinh ra nhiều container; sửa container không đổi image.",
  "**Image gồm nhiều lớp (layer) xếp lên nhau, mỗi lệnh trong Dockerfile là một lớp.** Hiểu cơ chế cache theo lớp là chìa khoá để build nhanh — thứ tự các lệnh quan trọng hơn nội dung.",
  "**Bài trước lo tái lập được ở tầng gói Python; Docker lo tái lập được ở tầng HỆ ĐIỀU HÀNH** — thư viện C, phiên bản Python, biến môi trường, locale.",
  "**Multi-stage build là kỹ thuật quan trọng nhất cho image nhỏ:** build ở một stage có đủ công cụ, rồi chỉ copy kết quả sang stage cuối tối giản."
],

can_biet_truoc: [
  "Bài **Process và thread** ở module 2 — container là tiến trình bị cô lập, không phải máy ảo.",
  "Bài **Hệ thống file, đường dẫn, quyền truy cập** ở module 2 — volume, quyền file trong container.",
  "Bài **Biến môi trường và PATH** ở module 3 — cách cấu hình ứng dụng trong container.",
  "Bài **Quản lý môi trường** ngay trước — lockfile là điều kiện để build Docker tái lập được.",
  "Bài **IP, port, DNS** ở module 4 — ánh xạ cổng và mạng giữa các container."
],

dinh_nghia: "Nói đời thường: chuyển nhà bằng container tàu biển. Bạn đóng đồ vào container ở nhà cũ, và nó xuống tàu, xuống xe tải, tới nhà mới mà không cần mở ra sắp lại. Cảng không cần biết bên trong có gì; nó chỉ cần biết cách nhấc container.\n\nCác khái niệm, theo thứ tự phụ thuộc:\n\n- **Image** — bản mẫu **chỉ đọc**, chứa hệ thống file và metadata (lệnh chạy, biến môi trường, cổng). Bất biến.\n- **Container** — **một lần chạy** của image, cộng thêm một lớp ghi được ở trên. Một image sinh ra nhiều container.\n- **Layer** (lớp) — mỗi lệnh trong Dockerfile tạo một lớp chỉ đọc. Image là các lớp xếp lên nhau qua hệ thống file union.\n- **Dockerfile** — tệp văn bản mô tả cách dựng image.\n- **Registry** — kho lưu image (Docker Hub, GitHub Container Registry).\n- **Volume** — thư mục nằm **ngoài** container, dữ liệu sống sót khi container bị xoá.\n- **Namespace** — cơ chế kernel Linux cô lập tiến trình: nó thấy hệ thống file riêng, mạng riêng, danh sách tiến trình riêng.\n- **cgroup** — cơ chế kernel giới hạn tài nguyên: CPU, RAM, I/O.\n\nPhân biệt quan trọng nhất, và là nguồn hiểu sai phổ biến nhất:\n\n```\nMÁY ẢO (VM)                      CONTAINER\n──────────────────────           ─────────────────────────────\nCó kernel RIÊNG                  DÙNG CHUNG kernel máy chủ\nHypervisor mô phỏng phần cứng    Chỉ là tiến trình bị cô lập\nKhởi động: hàng chục giây–phút    Khởi động: MILI GIÂY\nDung lượng: GB                    Dung lượng: MB\nCô lập: rất mạnh                  Cô lập: yếu hơn VM\nChạy được HĐH khác hẳn            PHẢI cùng loại kernel\n```\n\nDòng cuối có hệ quả thực tế: **container Linux trên Windows/macOS thật ra chạy trong một máy ảo Linux nhỏ** do Docker Desktop dựng. Đó là lý do hiệu năng I/O trên hai hệ này chậm hơn Linux đáng kể.",

vi_sao_can: "Docker giải quyết một lớp vấn đề mà `venv` và lockfile **không** với tới:\n\n- **\"Máy tôi chạy được\" vẫn còn dù đã có lockfile.** Lockfile khoá gói Python, nhưng không khoá: phiên bản Python, thư viện C (`libpq`, `libjpeg`), locale, múi giờ, phiên bản OpenSSL. Một gói Python cần `libpq` 15 mà máy chủ có `libpq` 12 thì vỡ, dù lockfile khớp hoàn hảo.\n- **Cài môi trường cho người mới từ nửa ngày xuống một lệnh.** Dự án cần Postgres, Redis, và một dịch vụ Go — không có Docker thì mỗi người tự cài, mỗi người một phiên bản.\n- **Chạy nhiều phiên bản cùng lúc mà không xung đột.** Dự án A cần Postgres 14, dự án B cần Postgres 16 — hai container, không vấn đề.\n- **Triển khai giống nhau ở mọi nơi.** Cùng một image chạy trên máy bạn, trên CI, và trên production. Không còn \"môi trường staging khác production\".\n- **Là nền của mọi thứ hiện đại phía sau.** Kubernetes, CI runner, serverless — đều dựa trên container. Không hiểu container thì không hiểu được chúng.\n\nTình huống thật khi vào một dự án mới: README ghi 40 dòng hướng dẫn cài đặt — cài Postgres, tạo user, chạy migration, cài Redis, cài `libpq-dev`, đúng phiên bản Python. Bạn mất **cả buổi sáng** và vẫn gặp một lỗi biên dịch `psycopg2` mà README không nhắc. Với Docker, cùng việc đó là `docker compose up` và chờ hai phút.\n\n**Phản biện cần thiết — Docker không miễn phí:**\n\n```\n· Thêm một tầng phức tạp phải học và phải gỡ rối\n· Trên Windows/macOS: I/O chậm, tốn RAM cho máy ảo ngầm\n· Dự án nhỏ một người dùng venv là đủ và đơn giản hơn\n· Debug trong container phức tạp hơn (đúng như bài debugger đã nói)\n```\n\nDùng Docker khi: có **nhiều dịch vụ**, làm việc **nhóm**, hoặc cần **triển khai**. Một script Python một file thì không cần.",

co_che: "### Container là gì ở tầng kernel\n\nĐây là phần quyết định bạn hiểu Docker hay chỉ học thuộc lệnh:\n\n```\nContainer = TIẾN TRÌNH BÌNH THƯỜNG + hai cơ chế của kernel Linux\n\nNAMESPACE — cô lập tầm nhìn\n  mnt     hệ thống file riêng      → thấy / khác với máy chủ\n  pid     danh sách tiến trình riêng → tiến trình chính là PID 1\n  net     mạng riêng                → IP riêng, cổng riêng\n  uts     hostname riêng\n  ipc     bộ nhớ chia sẻ riêng\n  user    ánh xạ user riêng\n\nCGROUP — giới hạn tài nguyên\n  CPU, RAM, I/O đĩa, băng thông mạng\n```\n\nChứng minh container chỉ là tiến trình:\n\n```bash\n# Chạy container\n$ docker run -d --name thu nginx\n\n# TRÊN MÁY CHỦ (Linux), nó hiện ra như tiến trình bình thường\n$ ps aux | grep nginx\nroot   12847  nginx: master process nginx -g daemon off;\n\n# ⭐ Không có \"máy ảo\" nào. Chỉ là tiến trình với namespace riêng.\n```\n\nHai hệ quả quan trọng suy ra trực tiếp từ cơ chế này:\n\n```\n① Container KHỞI ĐỘNG NHANH vì không phải boot kernel\n   → chỉ là fork một tiến trình + thiết lập namespace\n\n② Cô lập YẾU HƠN máy ảo vì dùng chung kernel\n   → lỗ hổng kernel ảnh hưởng cả máy chủ\n   → KHÔNG dùng container làm ranh giới bảo mật cho code không tin cậy\n```\n\n### Image và layer — cơ chế cache\n\n```\nDockerfile                          Layer\n─────────────────────────           ───────────────────\nFROM python:3.12-slim         →     lớp 1 (base)\nRUN apt-get install -y gcc     →     lớp 2\nCOPY pyproject.toml .          →     lớp 3\nRUN pip install -e .           →     lớp 4\nCOPY . .                       →     lớp 5\n```\n\nQuy tắc cache — thuộc lòng dòng này:\n\n```\nDocker dùng lại lớp đã cache NẾU lệnh và ngữ cảnh đầu vào không đổi.\nMột lớp bị vô hiệu ⇒ MỌI LỚP SAU nó cũng bị vô hiệu.\n```\n\nĐây là lý do **thứ tự các lệnh quan trọng hơn nội dung của chúng**:\n\n```dockerfile\n# ❌ SAI THỨ TỰ — sửa một dòng code là cài lại toàn bộ gói\nFROM python:3.12-slim\nCOPY . .                          # ← lớp này đổi mỗi lần sửa code\nRUN pip install -e .              # ← nên bị chạy lại mỗi lần (60 giây)\n```\n\n```dockerfile\n# ✓ ĐÚNG THỨ TỰ — thứ ÍT ĐỔI đặt TRƯỚC\nFROM python:3.12-slim\nCOPY pyproject.toml uv.lock ./    # ← chỉ đổi khi thêm/bớt gói\nRUN uv sync --frozen --no-dev     # ← được CACHE\nCOPY . .                          # ← đổi thường xuyên, nhưng ở CUỐI\n```\n\n```\nSửa một dòng code:\n  Bản sai:  build lại từ lớp 2  → 60+ giây\n  Bản đúng: build lại lớp cuối  → 2 giây\n```\n\nNguyên tắc tổng quát: **sắp xếp lệnh theo tần suất thay đổi, ít đổi lên trước.**\n\n### Layer là cộng dồn — không xoá được\n\nMột đặc điểm dễ gây bất ngờ:\n\n```dockerfile\n# ❌ Image vẫn TO dù đã xoá\nRUN wget https://vidu.com/file-lon.tar.gz     # lớp 1: +500MB\nRUN tar -xzf file-lon.tar.gz                  # lớp 2: +500MB\nRUN rm file-lon.tar.gz                        # lớp 3: xoá, nhưng...\n# → Image vẫn chứa 1GB! Lớp 1 và 2 VẪN TỒN TẠI trong image.\n```\n\n```dockerfile\n# ✓ Làm hết trong MỘT lệnh RUN — chỉ tạo một lớp\nRUN wget https://vidu.com/file-lon.tar.gz \\\n    && tar -xzf file-lon.tar.gz \\\n    && rm file-lon.tar.gz\n```\n\nCùng nguyên tắc với `apt-get`:\n\n```dockerfile\n# ✓ Dọn cache apt TRONG CÙNG lệnh RUN\nRUN apt-get update \\\n    && apt-get install -y --no-install-recommends libpq5 \\\n    && rm -rf /var/lib/apt/lists/*\n```\n\n`rm -rf /var/lib/apt/lists/*` ở một lệnh `RUN` riêng sẽ **không** giảm kích thước image, vì dữ liệu đã nằm trong lớp trước. Chi tiết này giải thích vì sao Dockerfile thật thường có những lệnh `RUN` dài nối bằng `&&`.\n\n### Dockerfile — các lệnh cốt lõi\n\n```dockerfile\nFROM python:3.12-slim         # image gốc\nWORKDIR /app                   # thư mục làm việc (tự tạo)\nCOPY nguon dich                # copy từ máy build vào image\nRUN lenh                       # chạy lệnh LÚC BUILD\nENV TEN=gia_tri                # biến môi trường (còn lúc chạy)\nARG TEN=gia_tri                # biến CHỈ lúc build\nEXPOSE 8000                    # tài liệu hoá cổng (KHÔNG mở cổng thật)\nUSER appuser                   # đổi user (bảo mật)\nCMD [\"python\", \"main.py\"]      # lệnh mặc định, GHI ĐÈ được\nENTRYPOINT [\"python\"]          # lệnh cố định, khó ghi đè hơn\n```\n\nBốn cặp dễ nhầm, cần phân biệt rõ:\n\n```\nRUN vs CMD\n  RUN  chạy LÚC BUILD, kết quả nằm trong image\n  CMD  chạy LÚC KHỞI ĐỘNG container\n\nENV vs ARG\n  ENV  còn tồn tại lúc chạy    → ⚠️ ĐỪNG đặt bí mật vào đây\n  ARG  chỉ có lúc build         → vẫn nằm trong lịch sử build!\n\nCMD vs ENTRYPOINT\n  CMD         docker run image lenh-khac  → ghi đè hoàn toàn\n  ENTRYPOINT  docker run image tham-so    → tham số được THÊM vào\n\nCOPY vs ADD\n  COPY  chỉ copy — dùng cái này\n  ADD   copy + tự giải nén + tải URL → hành vi ngầm, tránh dùng\n```\n\n⚠️ `EXPOSE` là chỗ gây hiểu sai nhiều nhất: nó **chỉ là tài liệu**, không mở cổng nào. Muốn truy cập được từ ngoài phải dùng `-p 8000:8000` khi chạy.\n\n### Multi-stage build — kỹ thuật quan trọng nhất\n\n```dockerfile\n# ❌ MỘT STAGE — image chứa cả công cụ build\nFROM python:3.12\nRUN apt-get update && apt-get install -y gcc g++ make    # +400MB\nCOPY . .\nRUN pip install -e .\nCMD [\"python\", \"-m\", \"app\"]\n# → 1.2 GB, chứa gcc mà production không cần\n```\n\n```dockerfile\n# ✓ MULTI-STAGE\n# ---------- Stage 1: BUILD ----------\nFROM python:3.12 AS builder\n\nRUN apt-get update && apt-get install -y --no-install-recommends gcc\nRUN pip install uv\n\nWORKDIR /app\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-dev      # ⭐ tạo .venv\n\n# ---------- Stage 2: RUNTIME ----------\nFROM python:3.12-slim              # ⭐ image gốc TỐI GIẢN\n\nRUN apt-get update \\\n    && apt-get install -y --no-install-recommends libpq5 \\\n    && rm -rf /var/lib/apt/lists/*\n\nWORKDIR /app\nCOPY --from=builder /app/.venv /app/.venv    # ⭐ chỉ copy KẾT QUẢ\nCOPY ban_hang/ ./ban_hang/\n\nENV PATH=\"/app/.venv/bin:$PATH\"\n\nRUN useradd -m -u 1000 appuser && chown -R appuser /app\nUSER appuser                        # ⭐ KHÔNG chạy bằng root\n\nEXPOSE 8000\nCMD [\"uvicorn\", \"ban_hang.main:app\", \"--host\", \"0.0.0.0\"]\n```\n\n```\nKết quả:  1.2 GB  →  180 MB\n\nVÌ SAO: gcc, header C, cache pip nằm ở stage 1 và bị VỨT BỎ.\n        Chỉ .venv đã build xong được copy sang.\n```\n\nBa lợi ích, không chỉ là dung lượng:\n\n```\n① Triển khai NHANH hơn — ít dữ liệu phải tải\n② BẢO MẬT hơn — không có compiler để kẻ tấn công dùng\n③ Ít lỗ hổng — mỗi gói thừa là một CVE tiềm ẩn\n```\n\n### Container không có trạng thái — volume để lưu\n\n```\nContainer bị xoá ⇒ MỌI thay đổi trong nó MẤT.\nĐây là TÍNH NĂNG, không phải hạn chế.\n```\n\nBa cách lưu dữ liệu ra ngoài:\n\n```bash\n# ① NAMED VOLUME — Docker quản lý. Dùng cho DỮ LIỆU.\ndocker run -v du-lieu-db:/var/lib/postgresql/data postgres:16\n\n# ② BIND MOUNT — trỏ vào thư mục máy chủ. Dùng cho MÃ NGUỒN lúc dev.\ndocker run -v \"$(pwd)\":/app python:3.12\n\n# ③ tmpfs — trong RAM, mất khi container dừng. Dùng cho dữ liệu tạm.\ndocker run --tmpfs /tmp python:3.12\n```\n\n```\nNAMED VOLUME  Docker quản, hiệu năng tốt trên mọi HĐH  → DỮ LIỆU production\nBIND MOUNT    thấy được từ máy chủ, sửa code là có hiệu lực ngay\n              ⚠️ CHẬM trên macOS/Windows                → MÃ NGUỒN lúc dev\n```\n\nDòng cảnh báo có hậu quả thật: bind mount một dự án Node hay Python lớn trên macOS có thể làm ứng dụng chậm gấp nhiều lần, vì mọi lần đọc file phải đi qua tầng chia sẻ file của máy ảo ngầm.\n\n### `docker compose` — nhiều dịch vụ\n\n```yaml\n# docker-compose.yml\nservices:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n    environment:\n      DATABASE_URL: postgresql://app:matkhau@db:5432/banhang\n      #                                     ↑ ⭐ TÊN DỊCH VỤ, không phải localhost\n    depends_on:\n      db:\n        condition: service_healthy      # ⭐ chờ db SẴN SÀNG, không chỉ chạy\n    volumes:\n      - ./ban_hang:/app/ban_hang        # bind mount để sửa code nóng\n\n  db:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_USER: app\n      POSTGRES_PASSWORD: matkhau\n      POSTGRES_DB: banhang\n    volumes:\n      - du-lieu-db:/var/lib/postgresql/data   # ⭐ named volume\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U app\"]\n      interval: 5s\n      retries: 5\n\nvolumes:\n  du-lieu-db:\n```\n\nHai chi tiết quan trọng nhất:\n\n```\n① TÊN DỊCH VỤ là hostname\n   Compose tạo mạng riêng, mỗi dịch vụ có DNS theo tên nó.\n   API gọi db bằng \"db:5432\", KHÔNG phải \"localhost:5432\".\n   ⚠️ localhost trong container nghĩa là CHÍNH container đó.\n\n② depends_on + condition: service_healthy\n   depends_on THÔI chỉ chờ container KHỞI ĐỘNG, không chờ nó SẴN SÀNG.\n   Postgres mất vài giây mới nhận kết nối ⇒ API sẽ lỗi nếu không có healthcheck.\n```\n\nĐiểm ② là nguyên nhân của lỗi \"connection refused lúc khởi động\" mà rất nhiều người gặp.\n\n### Chọn image gốc\n\n```\npython:3.12            ~1 GB    đầy đủ công cụ build      → stage BUILD\npython:3.12-slim       ~150 MB  Debian tối giản           → ⭐ MẶC ĐỊNH TỐT\npython:3.12-alpine     ~50 MB   Alpine Linux, dùng musl   → ⚠️ cẩn thận\ngcr.io/distroless/...  ~50 MB   không có shell            → bảo mật cao nhất\n```\n\n⚠️ **Alpine với Python thường là bẫy**, và lý do rất cụ thể:\n\n```\nAlpine dùng musl libc thay vì glibc.\n⇒ wheel biên dịch sẵn trên PyPI (build cho glibc) KHÔNG dùng được\n⇒ pip phải BIÊN DỊCH TỪ MÃ NGUỒN\n⇒ build chậm hơn nhiều, image có thể LỚN HƠN slim\n⇒ đôi khi lỗi khó hiểu trong thư viện tính toán (numpy, pandas)\n```\n\nKhuyến nghị thực dụng: **dùng `slim` làm mặc định.** Chỉ dùng Alpine khi bạn đã đo và biết chắc nó có lợi cho trường hợp của mình.\n\n### `.dockerignore` — bắt buộc phải có\n\n```\n.git\n.venv\n__pycache__\n*.pyc\n.pytest_cache\n.mypy_cache\n.env\n*.log\nnode_modules\ndata/\n```\n\nHai lý do, cả hai đều quan trọng:\n\n```\n① TỐC ĐỘ — Docker gửi toàn bộ \"build context\" cho daemon TRƯỚC khi build.\n   .git và .venv có thể là hàng trăm MB ⇒ chậm mỗi lần build.\n\n② BẢO MẬT — COPY . . sẽ đưa cả .env chứa bí mật vào image.\n   Image thường được đẩy lên registry ⇒ bí mật bị phát tán.\n```\n\nĐiểm ② là lỗi bảo mật thật và rất phổ biến. Nó cùng loại với vấn đề ở bài pre-commit: **bí mật một khi đã vào artifact thì phải coi như đã lộ.**\n\n### Không chạy bằng root\n\n```dockerfile\n# ❌ Mặc định là root\nFROM python:3.12-slim\nCOPY . .\nCMD [\"python\", \"main.py\"]     # chạy với UID 0\n```\n\n```dockerfile\n# ✓ Tạo user riêng\nRUN useradd --create-home --uid 1000 appuser\nWORKDIR /app\nCOPY --chown=appuser:appuser . .\nUSER appuser\nCMD [\"python\", \"main.py\"]\n```\n\nVì sao quan trọng: container dùng chung kernel với máy chủ. Root trong container là **UID 0 thật**, nên nếu kẻ tấn công thoát được khỏi cô lập, họ có root trên máy chủ. Chạy bằng user thường là một tầng phòng thủ rẻ và hiệu quả.\n\n### Bí mật — cách sai và cách đúng\n\n```dockerfile\n# ❌ Cả ba cách này đều LỘ bí mật\nENV API_KEY=abc123                    # ⚠️ trong image, ai pull cũng đọc được\nARG API_KEY                            # ⚠️ trong lịch sử build\nRUN echo $API_KEY > /app/key.txt      # ⚠️ trong layer\n```\n\n```bash\n# Chứng minh: bất kỳ ai cũng xem được\n$ docker history --no-trunc ten-image | grep API_KEY\n$ docker inspect ten-image | grep -A5 Env\n```\n\n```yaml\n# ✓ Truyền LÚC CHẠY, không lúc build\nservices:\n  api:\n    image: ban-hang\n    environment:\n      API_KEY: ${API_KEY}       # đọc từ .env của máy chủ (KHÔNG commit .env)\n```\n\n```dockerfile\n# ✓ Nếu BUỘC phải có bí mật lúc build — dùng BuildKit secret\nRUN --mount=type=secret,id=pip_token \\\n    pip install --extra-index-url \\\n      \"https://$(cat /run/secrets/pip_token)@pypi.congty.vn/simple\" -e .\n# ⭐ Bí mật được mount TẠM, KHÔNG vào layer nào\n```\n\n### Gỡ rối container\n\n```bash\ndocker ps                          # container đang chạy\ndocker ps -a                       # kể cả đã dừng\ndocker logs -f ten                 # xem log, -f để theo dõi\ndocker exec -it ten sh             # ⭐ vào shell trong container ĐANG chạy\ndocker inspect ten                 # toàn bộ cấu hình\ndocker stats                       # CPU/RAM thời gian thực\n\n# Container thoát ngay khi khởi động — không exec vào được\ndocker run -it --entrypoint sh ten-image    # ⭐ ghi đè entrypoint\n\n# Xem image được dựng thế nào\ndocker history ten-image           # ⭐ từng lớp và kích thước\n\n# Dọn dẹp (Docker ngốn đĩa rất nhanh)\ndocker system df                   # đang dùng bao nhiêu\ndocker system prune -a --volumes    # ⚠️ xoá HẾT, kể cả volume\n```\n\n`docker exec -it ten sh` là lệnh dùng nhiều nhất khi gỡ rối: nó cho bạn một shell **bên trong** container để kiểm tra file, biến môi trường, kết nối mạng — trả lời được câu \"vì sao trong container nó khác\".\n\n`docker history` là công cụ để tối ưu kích thước: nó chỉ ra **lớp nào** đang chiếm nhiều dung lượng nhất.",

vi_du: "### Dockerfile hoàn chỉnh cho ứng dụng Python\n\n```dockerfile\n# syntax=docker/dockerfile:1\n\n# ═══════════ Stage 1: BUILD ═══════════\nFROM python:3.12-slim AS builder\n\n# Công cụ build — chỉ tồn tại ở stage này\nRUN apt-get update \\\n    && apt-get install -y --no-install-recommends \\\n        gcc \\\n        libpq-dev \\\n    && rm -rf /var/lib/apt/lists/*\n\nCOPY --from=ghcr.io/astral-sh/uv:latest /uv /bin/uv\n\nWORKDIR /app\n\n# ⭐ COPY file khoá TRƯỚC — lớp này chỉ đổi khi thêm/bớt gói\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen --no-install-project --no-dev\n\n# ⭐ Rồi mới copy mã nguồn — lớp này đổi mỗi lần sửa code\nCOPY ban_hang/ ./ban_hang/\nRUN uv sync --frozen --no-dev\n\n\n# ═══════════ Stage 2: RUNTIME ═══════════\nFROM python:3.12-slim\n\n# CHỈ thư viện runtime — libpq5, không phải libpq-dev\nRUN apt-get update \\\n    && apt-get install -y --no-install-recommends libpq5 \\\n    && rm -rf /var/lib/apt/lists/*\n\n# User không phải root\nRUN useradd --create-home --uid 1000 appuser\n\nWORKDIR /app\n\n# ⭐ Chỉ copy KẾT QUẢ từ stage build\nCOPY --from=builder --chown=appuser:appuser /app/.venv /app/.venv\nCOPY --chown=appuser:appuser ban_hang/ ./ban_hang/\n\nENV PATH=\"/app/.venv/bin:$PATH\" \\\n    PYTHONUNBUFFERED=1 \\\n    PYTHONDONTWRITEBYTECODE=1\n\nUSER appuser\n\nEXPOSE 8000\n\nHEALTHCHECK --interval=30s --timeout=3s --start-period=5s \\\n    CMD python -c \"import urllib.request; \\\n        urllib.request.urlopen('http://localhost:8000/health')\"\n\nCMD [\"uvicorn\", \"ban_hang.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\nBa biến môi trường ở cuối đáng giải thích:\n\n```\nPYTHONUNBUFFERED=1        Python không đệm stdout\n                          → log hiện NGAY trong docker logs\n                          ⚠️ thiếu nó: log bị giữ trong buffer, tưởng app treo\n\nPYTHONDONTWRITEBYTECODE=1  không sinh .pyc\n                          → image sạch hơn, và .pyc vô ích trong container\n                            (mỗi container là một lần chạy mới)\n\nPATH=/app/.venv/bin:$PATH  dùng Python trong venv\n                          → không cần \"activate\", đúng cách trong container\n```\n\nDòng đầu là chi tiết gây bối rối rất phổ biến: không có `PYTHONUNBUFFERED=1`, `print` và log của bạn có thể **không hiện gì** trong `docker logs` cho tới khi buffer đầy — và bạn tưởng ứng dụng bị treo.\n\n```\n# .dockerignore — BẮT BUỘC có\n.git\n.gitignore\n.venv\n__pycache__\n*.py[cod]\n.pytest_cache\n.mypy_cache\n.ruff_cache\n.env\n.env.*\n*.log\ndata/\nnotebooks/\ntests/\nDockerfile\ndocker-compose.yml\nREADME.md\n```\n\n```bash\n# Đo kết quả\n$ docker build -t ban-hang .\n$ docker images ban-hang\nREPOSITORY   TAG      SIZE\nban-hang     latest   187MB       # so với 1.2GB nếu một stage\n\n# Xem lớp nào chiếm chỗ\n$ docker history ban-hang\nSIZE      CREATED BY\n0B        CMD [\"uvicorn\" ...]\n0B        HEALTHCHECK ...\n0B        USER appuser\n1.2MB     COPY ban_hang/ ./ban_hang/\n58.4MB    COPY /app/.venv /app/.venv       # ⭐ phần lớn là gói Python\n4.1MB     RUN useradd ...\n8.7MB     RUN apt-get install libpq5\n124MB     FROM python:3.12-slim            # ⭐ image gốc\n```\n\nBảng cuối cho biết chỗ đáng tối ưu: image gốc 124MB là phần lớn nhất — muốn nhỏ hơn nữa thì phải nhìn tới distroless, không phải tối ưu code.\n\n### Sức mạnh của cache layer — đo cụ thể\n\n```dockerfile\n# ❌ SAI THỨ TỰ\nFROM python:3.12-slim\nWORKDIR /app\nCOPY . .                                # ← đổi mỗi lần sửa code\nRUN pip install -e .\n```\n\n```bash\n$ echo \"# thêm một comment\" >> ban_hang/main.py\n$ time docker build -t thu .\n\n=> [2/4] COPY . .                          0.1s\n=> [3/4] RUN pip install -e .             47.3s    # ⚠️ cài lại TẤT CẢ\nreal    0m49.2s\n```\n\n```dockerfile\n# ✓ ĐÚNG THỨ TỰ\nFROM python:3.12-slim\nWORKDIR /app\nCOPY pyproject.toml uv.lock ./          # ← ít đổi\nRUN uv sync --frozen --no-dev\nCOPY ban_hang/ ./ban_hang/              # ← đổi thường xuyên, đặt CUỐI\n```\n\n```bash\n$ echo \"# thêm một comment\" >> ban_hang/main.py\n$ time docker build -t thu .\n\n=> CACHED [3/5] COPY pyproject.toml uv.lock ./\n=> CACHED [4/5] RUN uv sync --frozen --no-dev      # ⭐ CACHED\n=> [5/5] COPY ban_hang/ ./ban_hang/        0.2s\nreal    0m1.8s                                     # ⭐ 27 lần nhanh hơn\n```\n\n```\nCÙNG một Dockerfile về nội dung, chỉ khác THỨ TỰ:  49s → 1.8s\n```\n\nĐây là lý do nguyên tắc \"sắp theo tần suất thay đổi\" quan trọng hơn mọi thủ thuật tối ưu khác.\n\n### Layer cộng dồn — chứng minh\n\n```dockerfile\n# ❌ Ba lệnh RUN riêng\nFROM debian:12-slim\nRUN apt-get update && apt-get install -y wget\nRUN wget -q https://vidu.com/dulieu.tar.gz     # +200MB\nRUN tar -xzf dulieu.tar.gz -C /data            # +200MB\nRUN rm dulieu.tar.gz                            # \"xoá\"\n```\n\n```bash\n$ docker images | grep xau\nxau   latest   478MB      # ⚠️ file đã xoá VẪN nằm trong image\n\n$ docker history xau\n0B      RUN rm dulieu.tar.gz              # ⭐ xoá 0 byte!\n201MB   RUN tar -xzf dulieu.tar.gz\n200MB   RUN wget ...                       # ⭐ vẫn còn nguyên\n```\n\n```dockerfile\n# ✓ Một lệnh RUN duy nhất\nFROM debian:12-slim\nRUN apt-get update \\\n    && apt-get install -y --no-install-recommends wget ca-certificates \\\n    && wget -q https://vidu.com/dulieu.tar.gz \\\n    && tar -xzf dulieu.tar.gz -C /data \\\n    && rm dulieu.tar.gz \\\n    && apt-get purge -y wget \\\n    && rm -rf /var/lib/apt/lists/*\n```\n\n```bash\n$ docker images | grep tot\ntot   latest   278MB      # ⭐ giảm 200MB\n```\n\nĐây là lý do Dockerfile thật thường có những lệnh `RUN` dài nối bằng `&&` — không phải để tiết kiệm dòng, mà vì **layer không xoá được**.\n\n### `docker compose` cho môi trường phát triển\n\n```yaml\n# docker-compose.yml\nservices:\n  api:\n    build:\n      context: .\n      target: builder            # ⭐ dùng stage BUILD để có công cụ dev\n    command: >\n      uvicorn ban_hang.main:app\n      --host 0.0.0.0 --reload\n    ports:\n      - \"8000:8000\"\n      - \"5678:5678\"             # cổng debugpy (bài debugger)\n    environment:\n      DATABASE_URL: postgresql+asyncpg://app:matkhau@db:5432/banhang\n      REDIS_URL: redis://cache:6379/0\n      CHO_DEBUG: \"1\"\n    volumes:\n      - ./ban_hang:/app/ban_hang    # ⭐ sửa code có hiệu lực ngay\n      - ./tests:/app/tests\n    depends_on:\n      db:\n        condition: service_healthy\n      cache:\n        condition: service_started\n\n  db:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_USER: app\n      POSTGRES_PASSWORD: matkhau\n      POSTGRES_DB: banhang\n    ports:\n      - \"5432:5432\"             # mở ra ngoài để dùng công cụ GUI\n    volumes:\n      - du-lieu-db:/var/lib/postgresql/data\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U app -d banhang\"]\n      interval: 5s\n      timeout: 3s\n      retries: 5\n\n  cache:\n    image: redis:7-alpine\n    command: redis-server --save \"\" --appendonly no    # không lưu đĩa\n\nvolumes:\n  du-lieu-db:\n```\n\n```bash\n# Người mới clone repo — TOÀN BỘ môi trường trong một lệnh\n$ docker compose up\n\n[+] Running 4/4\n ✔ Network banhang_default    Created\n ✔ Container banhang-db-1     Healthy\n ✔ Container banhang-cache-1  Started\n ✔ Container banhang-api-1    Started\n\napi-1  | INFO:  Uvicorn running on http://0.0.0.0:8000\n```\n\nSo với 40 dòng hướng dẫn cài đặt trong README và cả buổi sáng loay hoay.\n\n```bash\n# Các lệnh hằng ngày\n$ docker compose up -d              # chạy nền\n$ docker compose logs -f api        # xem log của một dịch vụ\n$ docker compose exec api bash      # ⭐ vào shell trong container\n$ docker compose exec db psql -U app banhang    # ⭐ vào database\n$ docker compose run --rm api pytest            # ⭐ chạy test\n$ docker compose down               # dừng, GIỮ volume\n$ docker compose down -v            # ⚠️ dừng và XOÁ volume (mất dữ liệu)\n```\n\n### `localhost` trong container — lỗi phổ biến nhất\n\n```python\n# ❌ SAI khi chạy trong container\nDATABASE_URL = \"postgresql://app:matkhau@localhost:5432/banhang\"\n#                                          ↑ localhost = CHÍNH container api\n#                                            → không có Postgres ở đó\n```\n\n```\nConnectionRefusedError: [Errno 111] Connection refused\n```\n\n```python\n# ✓ ĐÚNG — dùng TÊN DỊCH VỤ\nDATABASE_URL = \"postgresql://app:matkhau@db:5432/banhang\"\n#                                          ↑ Compose tạo DNS theo tên dịch vụ\n```\n\n```bash\n# Chứng minh bên trong container\n$ docker compose exec api sh\n/app $ getent hosts db\n172.18.0.3      db              # ⭐ có bản ghi DNS\n\n/app $ nc -zv db 5432\ndb (172.18.0.3:5432) open       # ⭐ kết nối được\n\n/app $ nc -zv localhost 5432\nlocalhost (127.0.0.1:5432): Connection refused    # ⚠️ đúng như dự đoán\n```\n\nMỗi container có namespace mạng riêng, nên `localhost` **luôn** nghĩa là chính container đó. Đây là hệ quả trực tiếp của cơ chế namespace ở đầu bài.\n\n### Bí mật lọt vào image — và cách chứng minh\n\n```dockerfile\n# ❌ Bí mật nằm vĩnh viễn trong image\nFROM python:3.12-slim\nENV DATABASE_PASSWORD=matkhau_that\nARG GITHUB_TOKEN\nRUN git clone https://$GITHUB_TOKEN@github.com/congty/thu-vien-rieng.git\n```\n\n```bash\n# Bất kỳ ai pull image cũng đọc được\n$ docker inspect ban-hang | grep -A3 '\"Env\"'\n\"Env\": [\n    \"DATABASE_PASSWORD=matkhau_that\",      # ⚠️ LỘ\n\n$ docker history --no-trunc ban-hang | grep github\nRUN git clone https://ghp_xxxxx@github.com/...   # ⚠️ TOKEN LỘ\n```\n\n```dockerfile\n# ✓ BuildKit secret — mount tạm, không vào layer\n# syntax=docker/dockerfile:1\nRUN --mount=type=secret,id=gh_token \\\n    git clone \"https://$(cat /run/secrets/gh_token)@github.com/congty/lib.git\"\n```\n\n```bash\n$ docker build --secret id=gh_token,env=GITHUB_TOKEN -t ban-hang .\n\n$ docker history --no-trunc ban-hang | grep -c github\n0        # ⭐ không có dấu vết\n```\n\n```yaml\n# ✓ Bí mật lúc CHẠY — cách đúng cho phần lớn trường hợp\nservices:\n  api:\n    image: ban-hang\n    environment:\n      DATABASE_PASSWORD: ${DATABASE_PASSWORD}   # từ .env, KHÔNG commit .env\n```\n\nĐây cùng một bài học với hook chặn bí mật ở bài trước: **bí mật đã vào artifact thì phải coi như đã lộ** — và phải thu hồi, không phải xoá đi là xong.\n\n### Build trong CI\n\n```yaml\n# .github/workflows/docker.yml\nname: Build image\non:\n  push:\n    branches: [main]\n    tags: [\"v*\"]\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    permissions:\n      contents: read\n      packages: write\n    steps:\n      - uses: actions/checkout@v4\n\n      - uses: docker/setup-buildx-action@v3\n\n      - uses: docker/login-action@v3\n        with:\n          registry: ghcr.io\n          username: ${{ github.actor }}\n          password: ${{ secrets.GITHUB_TOKEN }}\n\n      - uses: docker/metadata-action@v5\n        id: meta\n        with:\n          images: ghcr.io/${{ github.repository }}\n          tags: |\n            type=ref,event=branch\n            type=semver,pattern={{version}}\n            type=sha,prefix=              # ⭐ tag theo mã commit\n\n      - uses: docker/build-push-action@v6\n        with:\n          context: .\n          push: true\n          tags: ${{ steps.meta.outputs.tags }}\n          cache-from: type=gha            # ⭐ cache layer giữa các lần chạy\n          cache-to: type=gha,mode=max\n```\n\nHai điểm đáng chú ý:\n\n```\ntype=sha    tag image theo mã commit\n            ⇒ mỗi bản triển khai truy được về ĐÚNG commit\n            ⇒ \"latest\" là tag KHÔNG dùng cho production\n\ncache=gha   cache layer trên GitHub Actions\n            ⇒ build 4 phút xuống 40 giây khi chỉ sửa code\n```\n\nĐiểm đầu là nguyên tắc vận hành quan trọng: **không bao giờ triển khai `latest`.** Nó không nói cho bạn biết đang chạy code nào, và không rollback được về một phiên bản xác định.\n\n### Khi nào KHÔNG cần Docker\n\n```\n❌ KHÔNG cần — venv đơn giản hơn:\n· Script Python một file\n· Bài tập học, notebook thăm dò dữ liệu\n· Thư viện không có dịch vụ ngoài\n· Dự án một người, không triển khai\n\n✓ NÊN dùng:\n· Cần nhiều dịch vụ (database, cache, message queue)\n· Làm việc nhóm — thống nhất môi trường\n· Có triển khai lên máy chủ\n· Cần thư viện hệ thống khó cài (GDAL, ffmpeg, tesseract)\n· CI cần môi trường giống production\n```\n\nMột lưu ý thực dụng về hiệu năng trên máy phát triển:\n\n```\nTRÊN macOS / WINDOWS:\n· Container Linux chạy trong MÁY ẢO ngầm ⇒ tốn RAM (2–4 GB)\n· Bind mount CHẬM — dự án lớn có thể chậm gấp nhiều lần\n\nCÁCH GIẢM:\n· Chỉ mount thư mục mã nguồn, KHÔNG mount .venv hay node_modules\n· Dùng named volume cho thư mục ghi nhiều\n· Trên macOS: bật VirtioFS trong cài đặt Docker Desktop\n```\n\nMẹo \"không mount `.venv`\" quan trọng: nếu bạn bind mount cả thư mục dự án gồm `.venv`, mọi lần Python import một module đều phải đi qua tầng chia sẻ file chậm — và ứng dụng khởi động lâu gấp nhiều lần.",

so_sanh: "### Container và máy ảo\n\n| | Máy ảo | Container |\n|---|---|---|\n| Kernel | **riêng** | **dùng chung** máy chủ |\n| Khởi động | chục giây–phút | **mili giây** |\n| Dung lượng | GB | MB |\n| Cô lập | **rất mạnh** | yếu hơn |\n| Chạy HĐH khác | **✓** | ✗ phải cùng loại kernel |\n| Overhead | cao | **gần như không** |\n\nContainer Linux trên Windows/macOS chạy **trong** một máy ảo Linux — đó là lý do I/O chậm hơn.\n\n### Image và container\n\n| | Image | Container |\n|---|---|---|\n| Bản chất | bản mẫu **chỉ đọc** | **một lần chạy** của image |\n| Thay đổi được | ✗ bất biến | ✓ có lớp ghi được |\n| Số lượng | 1 | **nhiều** từ cùng một image |\n| Xoá đi thì | mất bản mẫu | **mất mọi thay đổi trong nó** |\n\n### Bốn cặp lệnh dễ nhầm\n\n| | Nghĩa | Lưu ý |\n|---|---|---|\n| `RUN` | chạy **lúc build** | kết quả nằm trong image |\n| `CMD` | chạy **lúc khởi động** | ghi đè được |\n| `ENV` | biến còn **lúc chạy** | **⚠️ đừng đặt bí mật** |\n| `ARG` | biến **chỉ lúc build** | ⚠️ vẫn nằm trong lịch sử build |\n| `ENTRYPOINT` | lệnh cố định | tham số được **thêm** vào |\n| `COPY` | chỉ copy | **dùng cái này** |\n| `ADD` | copy + giải nén + tải URL | hành vi ngầm, tránh |\n| `EXPOSE` | **chỉ tài liệu** | **không mở cổng nào** |\n\n### Ba cách lưu dữ liệu\n\n| | Named volume | Bind mount | tmpfs |\n|---|---|---|---|\n| Docker quản lý | **✓** | ✗ | ✓ |\n| Thấy từ máy chủ | ✗ | **✓** | ✗ |\n| Hiệu năng trên macOS/Win | **tốt** | **⚠️ chậm** | tốt |\n| Sống sót khi xoá container | ✓ | ✓ | ✗ |\n| Dùng cho | **dữ liệu** | **mã nguồn lúc dev** | dữ liệu tạm |\n\n### Chọn image gốc\n\n| Image | Kích thước | Ưu | Nhược |\n|---|---|---|---|\n| `python:3.12` | ~1 GB | đủ công cụ build | to | \n| `python:3.12-slim` | ~150 MB | **cân bằng tốt nhất** | — |\n| `python:3.12-alpine` | ~50 MB | nhỏ nhất | **⚠️ musl: build chậm, có thể lỗi** |\n| distroless | ~50 MB | bảo mật cao nhất | không có shell để gỡ rối |\n\n**`slim` là mặc định đúng.** Alpine chỉ khi đã đo và biết chắc có lợi.\n\n### Multi-stage build\n\n| | Một stage | Multi-stage |\n|---|---|---|\n| Kích thước | 1.2 GB | **187 MB** |\n| Chứa compiler | **✓ rủi ro** | ✗ |\n| Số CVE tiềm ẩn | nhiều | ít |\n| Tốc độ triển khai | chậm | **nhanh** |\n| Độ phức tạp Dockerfile | thấp | vừa |\n\n### Thứ tự lệnh và cache\n\n| Thứ tự | Sửa một dòng code | Vì sao |\n|---|---|---|\n| `COPY . .` rồi `install` | **49 giây** | lớp COPY đổi ⇒ mọi lớp sau vô hiệu |\n| `COPY lockfile` → `install` → `COPY code` | **1.8 giây** | lớp install được cache |\n\nNguyên tắc: **sắp lệnh theo tần suất thay đổi, ít đổi lên trước.**\n\n### Bí mật — cách sai và đúng\n\n| Cách | An toàn | Vì sao |\n|---|---|---|\n| `ENV API_KEY=...` | **✗** | ai pull image cũng đọc được |\n| `ARG API_KEY` | **✗** | nằm trong lịch sử build |\n| BuildKit `--mount=type=secret` | ✓ | mount tạm, không vào layer |\n| Biến môi trường **lúc chạy** | **✓** | không nằm trong image |\n\n### Khi nào dùng Docker\n\n| Tình huống | Docker? |\n|---|---|\n| Script một file | **✗ venv đủ** |\n| Bài tập, notebook | ✗ |\n| Nhiều dịch vụ (DB, cache) | **✓** |\n| Làm việc nhóm | **✓** |\n| Có triển khai | **✓** |\n| Thư viện hệ thống khó cài | **✓** |",

loi_thuong_gap: [
  "**Tưởng container là máy ảo.** Nó dùng chung kernel — nên nhanh, nhưng cô lập yếu hơn và không dùng làm ranh giới bảo mật cho code không tin cậy.",
  "**`COPY . .` trước khi cài gói.** Sửa một dòng code là cài lại toàn bộ — 49 giây thay vì 1.8 giây.",
  "**Xoá file ở một lệnh `RUN` riêng.** Layer là cộng dồn; file đã xoá vẫn nằm trong image.",
  "**Không có `.dockerignore`.** Build chậm vì gửi cả `.git` và `.venv`, và `COPY . .` đưa `.env` vào image.",
  "**Đặt bí mật vào `ENV` hoặc `ARG`.** Ai pull image cũng đọc được; `docker history` phơi hết.",
  "**Dùng `localhost` để gọi dịch vụ khác trong Compose.** `localhost` là chính container đó; phải dùng tên dịch vụ.",
  "**`depends_on` mà không có `healthcheck`.** Nó chỉ chờ container khởi động, không chờ sẵn sàng — Postgres cần vài giây.",
  "**Chạy container bằng root.** Root trong container là UID 0 thật trên kernel máy chủ.",
  "**Chọn Alpine cho Python.** musl libc làm wheel không dùng được, phải biên dịch — chậm hơn và có thể to hơn `slim`.",
  "**Quên `PYTHONUNBUFFERED=1`.** Log không hiện trong `docker logs`, tưởng ứng dụng treo.",
  "**Tưởng `EXPOSE` mở cổng.** Nó chỉ là tài liệu; phải dùng `-p` hoặc `ports:`.",
  "**Dùng `ADD` thay `COPY`.** `ADD` tự giải nén và tải URL — hành vi ngầm gây bất ngờ.",
  "**Triển khai tag `latest`.** Không biết đang chạy code nào, không rollback về phiên bản xác định được.",
  "**Bind mount cả thư mục dự án gồm `.venv` trên macOS/Windows.** Mỗi lần import đi qua tầng file chậm, ứng dụng khởi động lâu gấp nhiều lần.",
  "**`docker compose down -v` khi chỉ muốn dừng.** Cờ `-v` xoá volume và mất dữ liệu database.",
  "**Không cài dev dependencies riêng.** Image production chứa `pytest`, `ruff` — 43 gói thừa và bấy nhiêu bề mặt tấn công.",
  "**Dùng Docker cho mọi thứ.** Script một file hay notebook thì venv đơn giản hơn và không tốn RAM cho máy ảo ngầm."
],

tu_kiem_tra: [
  "Container khác máy ảo thế nào? Nêu hai cơ chế kernel làm nên container.",
  "Vì sao container khởi động nhanh hơn máy ảo hàng trăm lần?",
  "Vì sao không nên dùng container làm ranh giới bảo mật cho code không tin cậy?",
  "Container Linux trên macOS thật ra chạy ở đâu? Hệ quả với hiệu năng?",
  "Phân biệt image và container. Xoá container thì mất gì?",
  "Phát biểu quy tắc cache layer. Vì sao thứ tự lệnh quan trọng hơn nội dung?",
  "Vì sao `RUN rm file-lon.tar.gz` ở lệnh riêng không giảm kích thước image?",
  "Phân biệt `RUN`/`CMD`, `ENV`/`ARG`, `CMD`/`ENTRYPOINT`, `COPY`/`ADD`.",
  "`EXPOSE` làm gì và KHÔNG làm gì?",
  "Multi-stage build hoạt động thế nào? Nêu ba lợi ích ngoài dung lượng.",
  "Nêu ba cách lưu dữ liệu ra ngoài container và trường hợp dùng mỗi cách.",
  "Vì sao bind mount chậm trên macOS/Windows? Ba cách giảm ảnh hưởng?",
  "Trong Compose, vì sao phải dùng tên dịch vụ thay vì `localhost`?",
  "`depends_on` thôi không đủ — cần thêm gì và vì sao?",
  "Vì sao Alpine thường là bẫy với Python? Khuyến nghị mặc định là gì?",
  "`PYTHONUNBUFFERED=1` giải quyết vấn đề gì?",
  "Nêu ba cách sai và hai cách đúng để xử lý bí mật. Chứng minh bí mật bị lộ bằng lệnh nào?",
  "Vì sao không triển khai tag `latest`? Nên tag theo gì?",
  "Vì sao không nên chạy container bằng root?",
  "Nêu bốn tình huống KHÔNG cần Docker và bốn tình huống nên dùng."
],

tai_nguyen: [
  "**Tài liệu chính thức của Docker** — mục *Get started* và *Dockerfile reference*; đây là nguồn chính xác nhất cho cú pháp và tuỳ chọn.",
  "**Docker Docs — Best practices for writing Dockerfiles** — tài liệu ngắn nhưng dày, nguồn của phần lớn nguyên tắc về layer và cache trong bài.",
  "**Tài liệu Docker Compose** — mục *Compose file reference* cho `healthcheck`, `depends_on`, và cấu hình mạng.",
  "Sách **Docker Deep Dive** — Nigel Poulton. Giải thích cơ chế namespace, cgroup, và union filesystem ở mức vừa đủ sâu mà vẫn đọc được.",
  "**Tài liệu BuildKit** — mục về `--mount=type=secret` và `type=cache`, cần khi tối ưu build và xử lý bí mật."
],

lien_he: [
  "**Process và thread** ở module 2: container là tiến trình bị cô lập bằng namespace, không phải máy ảo.",
  "**Hệ thống file, quyền truy cập** ở module 2: volume, `--chown`, và vì sao không chạy bằng root.",
  "**Biến môi trường và PATH** ở module 3: `ENV PATH=/app/.venv/bin` là cách đúng thay cho activate trong container.",
  "**Quản lý môi trường** ngay trước: lockfile là điều kiện để build Docker tái lập được; `--no-dev` làm image nhỏ.",
  "**IP, port, DNS** ở module 4: Compose tạo DNS theo tên dịch vụ; `localhost` là chính container đó.",
  "**Debugger** trong module này: `debugpy` và `pathMappings` để debug code chạy trong container.",
  "**Pre-commit hook** trong module này: bí mật đã vào artifact phải coi như đã lộ — cùng bài học, khác tầng.",
  "**Linter và formatter** trong module này: build Docker là việc chậm, thuộc về CI chứ không phải hook.",
  "**VS Code** trong module này: extension Dev Containers cho phép chạy cả môi trường phát triển trong container.",
  "**Triển khai** ở mảng 05: image là đơn vị triển khai; tag theo commit để truy vết và rollback.",
  "**Kiến trúc và microservices** ở mảng 06: container là nền của Kubernetes và mọi hạ tầng hiện đại.",
  "**MLOps** ở mảng 07: đóng gói model kèm đúng phiên bản thư viện — nơi tính tái lập ở tầng hệ điều hành thật sự quan trọng."
]

};
