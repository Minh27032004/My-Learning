window.LESSONS["01:bo-nho-co-che-thuc-thi:gil-cua-python-vi-sao-thread-khong-tang-toc-cpu-bound"] = {

tom_tat: [
  "**GIL là một khoá toàn cục cho phép đúng một luồng thực thi bytecode Python tại một thời điểm.** Bốn luồng tính toán trên máy tám nhân vẫn chỉ dùng được một nhân.",
  "**Nó tồn tại vì bộ đếm tham chiếu.** Mỗi phép gán đều ghi vào `ob_refcnt`; không có khoá thì hai luồng cùng ghi sẽ làm hỏng bộ đếm và gây sập hoặc rò rỉ.",
  "**Thread VẪN rất hữu ích cho I/O-bound**: luồng đang chờ mạng hay đĩa sẽ **nhả GIL**, nên các luồng khác chạy được. Tăng tốc thật, đo được.",
  "**Chọn công cụ theo loại công việc**: CPU-bound → nhiều tiến trình; I/O-bound nhiều kết nối → async; I/O-bound đơn giản → thread.",
  "**GIL không làm code tự động an toàn.** `n += 1` vẫn là race condition vì nó gồm nhiều lệnh bytecode và GIL có thể đổi luồng ở giữa."
],

can_biet_truoc: [
  "Bài **Garbage collection và đếm tham chiếu** ngay trước — bộ đếm nằm trong object là lý do gốc rễ GIL tồn tại.",
  "Bài **Concurrency vs parallelism** ở module 2 — phân biệt hai khái niệm; bài này là ứng dụng cụ thể vào Python.",
  "Bài **Deadlock, race condition** ở module 2 — khoá, vùng găng, và vì sao chúng cần thiết.",
  "Bài **Process vs thread** ở module 2 — tiến trình có bộ nhớ riêng, luồng dùng chung."
],

dinh_nghia: "Nói đời thường: một thư viện có **đúng một cái bút** dùng chung. Ai muốn ghi sổ phải cầm bút; ghi xong thì trả lại. Mười người vào cùng lúc cũng chỉ một người ghi được tại một thời điểm. Nhưng nếu ai đó **đi ra ngoài lấy tài liệu** — họ trả bút lại trước khi đi, nên người khác ghi được trong lúc đó.\n\nNói chính xác:\n\n- **GIL** (*Global Interpreter Lock*) — khoá toàn cục của trình thông dịch CPython. Luồng phải giữ nó mới thực thi được bytecode Python.\n- **CPU-bound** — công việc bị giới hạn bởi tốc độ tính toán: xử lý số, nén, phân tích dữ liệu.\n- **I/O-bound** — công việc bị giới hạn bởi việc chờ: đọc file, gọi mạng, truy vấn database.\n- **Nhả GIL** — luồng tự trả khoá lại: khi chờ I/O, khi chạy code C được thiết kế nhả khoá, hoặc theo định kỳ.\n- **Đồng thời** (*concurrency*) — nhiều việc **đang tiến hành** xen kẽ nhau.\n- **Song song** (*parallelism*) — nhiều việc **chạy cùng lúc** trên nhiều nhân.\n\nĐiểm cần phân biệt ngay: **GIL chặn song song, không chặn đồng thời.** Nhiều luồng Python vẫn xen kẽ nhau được — và với công việc chờ đợi thì xen kẽ là đủ để tăng tốc rất nhiều. Đây là lý do câu \"thread trong Python vô dụng\" là sai.",

vi_sao_can: "GIL là đặc điểm gây hiểu nhầm nhiều nhất của Python, và hiểu sai nó dẫn tới chọn sai công cụ:\n\n- **Chọn sai làm chậm hơn chứ không nhanh hơn.** Dùng thread cho tính toán nặng: không nhanh lên mà còn chậm đi do chi phí chuyển luồng. Dùng tiến trình cho I/O: tốn bộ nhớ gấp nhiều lần mà không lợi gì.\n- **Nó giải thích một loạt hiện tượng.** Vì sao dùng bốn luồng mà CPU chỉ chạy 100%? Vì sao NumPy lại tăng tốc được với thread? Vì sao thư viện gọi mạng chạy song song tốt?\n- **Nó vẫn cho phép race condition.** Nhiều người tưởng GIL làm code tự động an toàn — sai, và lỗi loại này rất khó tái hiện.\n- **Nó ảnh hưởng kiến trúc.** Số worker, cách chia việc, chọn máy chủ ứng dụng — mọi quyết định đó đều dựa trên hiểu biết về GIL.\n- **Định hướng AI/ML.** Vòng lặp huấn luyện chạy trong C nên nhả GIL; tiền xử lý dữ liệu bằng Python thuần thì không. Biết chỗ nào bị chặn là biết chỗ nào cần đổi cách.\n\nTình huống thật rất kinh điển: bạn có hàm xử lý ảnh tốn 2 giây, cần chạy cho 8 ảnh. Máy có 8 nhân nên bạn dùng 8 luồng và mong xong trong 2 giây. Kết quả: **16 giây, có khi hơn** — vì cả 8 luồng tranh nhau một GIL. Đổi sang 8 tiến trình thì đúng 2 giây thật. Phần cơ chế giải thích vì sao, và bảng quyết định cho biết khi nào dùng cái nào.",

co_che: "### GIL là gì và vì sao tồn tại\n\n```\nMỘT khoá toàn cục.\nLuồng phải GIỮ khoá mới thực thi được bytecode Python.\n⇒ Tại một thời điểm, đúng MỘT luồng chạy bytecode.\n```\n\nLý do gốc rễ nằm ở bài trước: **bộ đếm tham chiếu nằm trong mỗi object.**\n\n```python\nx = [1, 2]\ny = x            # tăng ob_refcnt của list\ndel y            # giảm ob_refcnt\n```\n\nMỗi phép gán đều **ghi vào** một ô nhớ dùng chung. Nếu hai luồng cùng làm mà không đồng bộ:\n\n```\nLuồng A đọc refcnt = 5\nLuồng B đọc refcnt = 5\nLuồng A ghi 6\nLuồng B ghi 6        ⚠️ đáng lẽ phải là 7\n\n⇒ Bộ đếm sai ⇒ object bị huỷ sớm (sập) hoặc không bao giờ huỷ (rò rỉ)\n```\n\nCó hai cách giải quyết, và CPython chọn cách đơn giản:\n\n```\n① Một khoá cho MỖI object\n   → an toàn, song song thật\n   → nhưng mỗi thao tác phải lấy khoá ⇒ chậm hơn nhiều cho code MỘT luồng\n   → và nguy cơ deadlock khi cần nhiều object cùng lúc\n\n② MỘT khoá toàn cục — GIL\n   → đơn giản, code một luồng nhanh\n   → tích hợp thư viện C dễ (chúng không cần biết về khoá)\n   → nhưng không song song được bytecode Python\n```\n\nĐánh đổi này có lý do lịch sử vững: khi CPython ra đời, máy nhiều nhân còn hiếm, và **tối ưu cho trường hợp phổ biến nhất — code một luồng** là quyết định đúng lúc đó.\n\nCái giá lộ ra khi CPU nhiều nhân trở thành tiêu chuẩn, và nó là lý do các nỗ lực gỡ bỏ GIL vẫn đang tiếp diễn trong cộng đồng Python.\n\n### Khi nào GIL được nhả\n\nĐây là phần quyết định tất cả:\n\n```\nNHẢ GIL khi:\n① Chờ I/O — đọc file, gọi mạng, truy vấn database, sleep\n② Chạy trong thư viện C được viết để nhả khoá (NumPy, nén, mã hoá)\n③ Định kỳ — sau một khoảng thời gian, để luồng khác có lượt\n\nGIỮ GIL khi:\n· Thực thi bytecode Python thuần\n```\n\nĐiểm ① là lý do thread vẫn hữu ích:\n\n```\nLuồng A: gửi request ──► [NHẢ GIL] ──── chờ mạng 200ms ──► [lấy lại GIL]\nLuồng B:                  [lấy GIL] gửi request ──► [NHẢ GIL] ── chờ...\nLuồng C:                             [lấy GIL] gửi request ──► ...\n\n⇒ Ba request chờ SONG SONG, dù chỉ một luồng chạy bytecode tại mỗi thời điểm\n```\n\nThời gian chờ mạng là thời gian **không cần GIL**, nên nó chồng lấn được. Với 100 request mỗi cái chờ 200 ms, tổng thời gian gần bằng 200 ms thay vì 20 giây.\n\nĐiểm ② giải thích vì sao NumPy tăng tốc được với thread: phép nhân ma trận chạy trong C, và thư viện chủ động nhả GIL trước khi vào vòng tính toán.\n\nĐiểm ③ dùng để tránh một luồng tính toán dài chiếm khoá mãi:\n\n```python\nimport sys\nsys.getswitchinterval()        # 0.005 — 5 mili giây\nsys.setswitchinterval(0.001)   # đổi được, hiếm khi cần\n```\n\nSau khoảng này, luồng đang giữ GIL được yêu cầu nhả để luồng khác có lượt. Đây là **cơ chế công bằng**, không phải cơ chế tăng tốc.\n\n### CPU-bound: thread không giúp gì\n\n```python\nimport time, threading\n\ndef tinh_nang(n):\n    tong = 0\n    for i in range(n):\n        tong += i * i\n    return tong\n\nN = 10_000_000\n\n# Một luồng\nt = time.perf_counter()\ntinh_nang(N); tinh_nang(N)\nprint(f\"tuần tự  : {time.perf_counter()-t:.2f}s\")\n\n# Hai luồng\nt = time.perf_counter()\nl1 = threading.Thread(target=tinh_nang, args=(N,))\nl2 = threading.Thread(target=tinh_nang, args=(N,))\nl1.start(); l2.start(); l1.join(); l2.join()\nprint(f\"2 luồng  : {time.perf_counter()-t:.2f}s\")\n```\n\nKết quả điển hình: hai luồng **bằng hoặc chậm hơn** tuần tự. Vì sao chậm hơn:\n\n```\n· Chỉ một luồng chạy tại mỗi thời điểm ⇒ tổng công việc không đổi\n· CỘNG THÊM chi phí chuyển luồng mỗi 5 ms\n· CỘNG THÊM tranh chấp khoá — luồng phải chờ để lấy GIL\n```\n\nĐây là bằng chứng trực tiếp: **thread không cho song song với bytecode Python.**\n\n### I/O-bound: thread rất hiệu quả\n\n```python\nimport time, threading\n\ndef gia_lap_goi_mang():\n    time.sleep(0.5)          # ⭐ sleep NHẢ GIL\n\n# Tuần tự\nt = time.perf_counter()\nfor _ in range(10):\n    gia_lap_goi_mang()\nprint(f\"tuần tự : {time.perf_counter()-t:.2f}s\")     # ~5.0s\n\n# 10 luồng\nt = time.perf_counter()\nluong = [threading.Thread(target=gia_lap_goi_mang) for _ in range(10)]\nfor l in luong: l.start()\nfor l in luong: l.join()\nprint(f\"10 luồng: {time.perf_counter()-t:.2f}s\")     # ~0.5s ⭐ nhanh gấp 10\n```\n\nTăng tốc gần bằng số luồng, vì **thời gian chờ chồng lấn hoàn toàn**. GIL không cản gì cả — nó được nhả trong suốt thời gian chờ.\n\nĐây là lý do câu \"GIL làm thread vô dụng\" sai: với I/O-bound, thread là công cụ đơn giản và hiệu quả nhất.\n\n### Ba công cụ đồng thời\n\n```\nthreading          nhiều luồng, CHUNG bộ nhớ, bị GIL chặn\n                   → I/O-bound, số lượng vừa phải\n\nmultiprocessing    nhiều tiến trình, MỖI cái một GIL riêng\n                   → CPU-bound — song song thật\n\nasyncio            MỘT luồng, chuyển việc tại điểm `await`\n                   → I/O-bound với RẤT nhiều kết nối\n```\n\nSo sánh chi phí:\n\n```\n            Bộ nhớ mỗi đơn vị    Chi phí tạo     Chia sẻ dữ liệu\nthread      ~8 MB (stack)        vừa             trực tiếp\nprocess     ~30 MB+              cao             phải tuần tự hoá\ncoroutine   ~vài KB              rất thấp        trực tiếp\n```\n\nCon số ở cột đầu giải thích vì sao không thể tạo 10.000 luồng, nhưng 10.000 coroutine thì bình thường.\n\n### `multiprocessing` — song song thật\n\n```python\nfrom multiprocessing import Pool\nimport time\n\ndef tinh_nang(n):\n    return sum(i * i for i in range(n))\n\nif __name__ == \"__main__\":          # ⭐ BẮT BUỘC trên Windows\n    N = 10_000_000\n\n    t = time.perf_counter()\n    [tinh_nang(N) for _ in range(4)]\n    print(f\"tuần tự    : {time.perf_counter()-t:.2f}s\")\n\n    t = time.perf_counter()\n    with Pool(4) as p:\n        p.map(tinh_nang, [N] * 4)\n    print(f\"4 tiến trình: {time.perf_counter()-t:.2f}s\")     # ⭐ nhanh gần 4 lần\n```\n\nDòng `if __name__ == \"__main__\"` **bắt buộc** — nối về bài module ở module 5: trên Windows, tiến trình con được tạo bằng cách **import lại module chính**. Không có dòng đó, mỗi tiến trình con lại tạo tiến trình con nữa, cho tới khi treo máy.\n\nCái giá của tiến trình:\n\n```\n① Bộ nhớ — mỗi tiến trình một bản Python đầy đủ\n② Chi phí tạo cao — vài chục tới vài trăm mili giây\n③ Dữ liệu phải TUẦN TỰ HOÁ để truyền qua lại (dùng pickle)\n④ Không chia sẻ trạng thái trực tiếp\n```\n\nĐiểm ③ có hệ quả thực tế lớn:\n\n```python\n# ❌ Truyền dữ liệu lớn qua tiến trình — chi phí tuần tự hoá lớn hơn lợi ích\nwith Pool(4) as p:\n    p.map(xu_ly, danh_sach_100mb)\n\n# ✓ Cho tiến trình con TỰ đọc dữ liệu\nwith Pool(4) as p:\n    p.map(doc_va_xu_ly, danh_sach_duong_dan)\n```\n\nQuy tắc: **truyền tham chiếu nhẹ (đường dẫn, id), đừng truyền dữ liệu nặng.**\n\nVà điểm ③ cũng giải thích một hạn chế: hàm truyền vào `Pool` phải pickle được, nên `lambda` và hàm cục bộ **không dùng được**.\n\n### `asyncio` — một luồng, nhiều việc\n\n```python\nimport asyncio, time\n\nasync def gia_lap_goi_mang(i):\n    await asyncio.sleep(0.5)        # ⭐ nhường quyền tại đây\n    return i\n\nasync def main():\n    t = time.perf_counter()\n    kq = await asyncio.gather(*[gia_lap_goi_mang(i) for i in range(1000)])\n    print(f\"1000 việc: {time.perf_counter()-t:.2f}s\")     # ~0.5s\n\nasyncio.run(main())\n```\n\nMột nghìn việc đồng thời trong **một luồng duy nhất**. Không có GIL nào bị tranh chấp vì chỉ có một luồng.\n\nCơ chế: tại mỗi `await`, coroutine **nhường quyền** cho vòng lặp sự kiện, và vòng lặp chạy việc khác. Đây là đồng thời **hợp tác** — mỗi việc tự nguyện nhường, khác với thread bị hệ điều hành cắt ngang.\n\nHệ quả quan trọng: **một hàm chặn sẽ đóng băng toàn bộ vòng lặp.**\n\n```python\nimport asyncio, time\n\nasync def xau():\n    time.sleep(1)              # ❌ CHẶN — đóng băng mọi coroutine khác\n\nasync def tot():\n    await asyncio.sleep(1)     # ✓ nhường quyền\n\nasync def cpu_nang():\n    tong = sum(i*i for i in range(10_000_000))   # ❌ cũng CHẶN\n    return tong\n```\n\nCách xử lý khi buộc phải gọi code chặn:\n\n```python\nimport asyncio\nfrom concurrent.futures import ThreadPoolExecutor, ProcessPoolExecutor\n\nasync def main():\n    vong = asyncio.get_running_loop()\n\n    # I/O chặn → đẩy sang thread\n    with ThreadPoolExecutor() as ex:\n        kq = await vong.run_in_executor(ex, ham_io_chan, tham_so)\n\n    # CPU nặng → đẩy sang tiến trình\n    with ProcessPoolExecutor() as ex:\n        kq = await vong.run_in_executor(ex, ham_cpu_nang, tham_so)\n```\n\nMẫu này là cách ghép ba công cụ lại: `asyncio` điều phối, thread lo I/O chặn, tiến trình lo CPU.\n\n### GIL không làm code an toàn\n\nĐây là hiểu nhầm nguy hiểm nhất:\n\n```python\nimport threading\n\ndem = 0\n\ndef tang():\n    global dem\n    for _ in range(100_000):\n        dem += 1          # ⚠️ KHÔNG nguyên tử\n\nluong = [threading.Thread(target=tang) for _ in range(4)]\nfor l in luong: l.start()\nfor l in luong: l.join()\n\nprint(dem)                # ⚠️ thường KHÔNG phải 400000\n```\n\nVì sao: `dem += 1` là **nhiều lệnh bytecode**, và GIL có thể đổi luồng ở giữa:\n\n```python\nimport dis\ndis.dis(\"dem += 1\")\n# LOAD_NAME  dem      ← đọc\n# LOAD_CONST 1\n# BINARY_OP  +=       ← cộng\n# STORE_NAME dem      ← ghi\n```\n\n```\nLuồng A: đọc dem = 5\nLuồng B: đọc dem = 5      ← GIL đổi luồng ở đây\nLuồng A: ghi 6\nLuồng B: ghi 6            ⚠️ mất một lần tăng\n```\n\nGIL chỉ đảm bảo **một lệnh bytecode** không bị cắt ngang, không đảm bảo cả biểu thức.\n\nCách sửa vẫn là khoá, đúng như bài race condition ở module 2:\n\n```python\nimport threading\n\ndem = 0\nkhoa = threading.Lock()\n\ndef tang():\n    global dem\n    for _ in range(100_000):\n        with khoa:            # ⭐ vùng găng\n            dem += 1\n```\n\nMột số thao tác **là** nguyên tử vì chúng là một lệnh bytecode:\n\n```python\nds.append(x)        # nguyên tử\nd[k] = v            # nguyên tử\nds.pop()            # nguyên tử\n\nd[k] += 1           # ⚠️ KHÔNG — đọc rồi ghi\nif k not in d: d[k] = 0    # ⚠️ KHÔNG — hai thao tác\n```\n\nNhưng **đừng dựa vào danh sách này**: nó là chi tiết cài đặt và có thể đổi. Dùng khoá hoặc `queue.Queue` cho mọi thứ có trạng thái chia sẻ.\n\n### Bảng quyết định\n\n```\nCÔNG VIỆC LÀ GÌ?\n│\n├─ CPU-bound (tính toán)\n│  ├─ Thư viện đã nhả GIL (NumPy...)  → thread hoặc chạy thẳng\n│  └─ Python thuần                     → multiprocessing ⭐\n│\n└─ I/O-bound (chờ đợi)\n   ├─ Vài chục kết nối, code đồng bộ   → threading ⭐\n   ├─ Hàng nghìn kết nối               → asyncio ⭐\n   └─ Trộn cả hai                      → asyncio + executor\n```\n\nCách xác định loại công việc:\n\n```python\nimport time\n\nt_thuc = time.perf_counter()\nt_cpu = time.process_time()        # ⭐ chỉ đếm thời gian CPU thật sự dùng\n\nham_can_do()\n\nthuc = time.perf_counter() - t_thuc\ncpu = time.process_time() - t_cpu\n\nprint(f\"thực tế: {thuc:.2f}s, CPU: {cpu:.2f}s, tỉ lệ: {cpu/thuc:.0%}\")\n# tỉ lệ gần 100% → CPU-bound\n# tỉ lệ thấp     → I/O-bound (phần lớn thời gian là CHỜ)\n```\n\nPhép đo này là cách khách quan để chọn công cụ, thay vì đoán.\n\n### Thư viện C nhả GIL\n\n```python\nimport numpy as np, time, threading\n\ndef nhan_ma_tran():\n    a = np.random.rand(1000, 1000)\n    b = np.random.rand(1000, 1000)\n    return a @ b            # ⭐ chạy trong C, NHẢ GIL\n\nt = time.perf_counter()\nnhan_ma_tran(); nhan_ma_tran()\nprint(f\"tuần tự : {time.perf_counter()-t:.2f}s\")\n\nt = time.perf_counter()\nl = [threading.Thread(target=nhan_ma_tran) for _ in range(2)]\nfor x in l: x.start()\nfor x in l: x.join()\nprint(f\"2 luồng : {time.perf_counter()-t:.2f}s\")     # ⭐ NHANH HƠN thật\n```\n\nĐây là ngoại lệ quan trọng: NumPy, thư viện nén, mã hoá, và nhiều thư viện học sâu **chủ động nhả GIL** trước khi vào vòng tính toán C.\n\nHệ quả cho AI/ML:\n\n```\nVòng lặp huấn luyện (chạy trong C)     → nhả GIL, thread có ích\nTiền xử lý bằng Python thuần            → giữ GIL, cần tiến trình\nĐọc và giải mã ảnh (thư viện C)         → thường nhả GIL\n```\n\nĐây là lý do các `DataLoader` thường cho chọn giữa worker dạng luồng và dạng tiến trình.\n\n### Hướng đi của Python\n\nCần nói rõ vì đây là chủ đề đang thay đổi:\n\n```\n· Có nỗ lực chính thức xây dựng bản CPython KHÔNG có GIL\n· Nó cho song song thật với thread, đổi lại code một luồng chậm hơn\n· Đang trong giai đoạn thử nghiệm, chưa mặc định\n· Các bản hiện thực Python khác có cách tiếp cận khác nhau\n```\n\nĐiều cần nhớ: **GIL là chi tiết của CPython, không phải của ngôn ngữ Python.** Nhưng vì CPython là bản dùng phổ biến nhất, mọi quyết định kiến trúc hiện nay vẫn phải tính tới nó.\n\nVà kể cả khi GIL biến mất, hai điều vẫn đúng:\n\n```\n· Race condition vẫn tồn tại — vẫn cần khoá\n· asyncio vẫn là lựa chọn tốt nhất cho hàng nghìn kết nối\n```",

vi_du: "### Đo GIL — CPU-bound\n\n```python\nimport time, threading\nfrom multiprocessing import Pool\n\ndef viec_cpu(n: int) -> int:\n    tong = 0\n    for i in range(n):\n        tong += i * i\n    return tong\n\nN = 8_000_000\nSO_VIEC = 4\n\n\ndef do(ten, f):\n    t = time.perf_counter()\n    f()\n    print(f\"{ten:<24} {time.perf_counter()-t:>6.2f}s\")\n\n\nif __name__ == \"__main__\":\n    do(\"tuần tự\", lambda: [viec_cpu(N) for _ in range(SO_VIEC)])\n\n    def dung_thread():\n        l = [threading.Thread(target=viec_cpu, args=(N,)) for _ in range(SO_VIEC)]\n        for x in l: x.start()\n        for x in l: x.join()\n    do(\"4 thread\", dung_thread)\n\n    def dung_process():\n        with Pool(SO_VIEC) as p:\n            p.map(viec_cpu, [N] * SO_VIEC)\n    do(\"4 process\", dung_process)\n\n# tuần tự                   4.20s\n# 4 thread                  4.35s   ⚠️ KHÔNG nhanh hơn, còn chậm hơn chút\n# 4 process                 1.15s   ⭐ nhanh gần 4 lần\n```\n\nBa dòng kết quả là toàn bộ nội dung thực dụng của bài. Thread chậm hơn tuần tự vì cộng thêm chi phí chuyển luồng mà không được gì.\n\nXác nhận bằng cách xem CPU: chạy bản thread và quan sát công cụ theo dõi hệ thống — một nhân đầy, các nhân khác nhàn rỗi.\n\n### Đo GIL — I/O-bound\n\n```python\nimport time, threading\nfrom concurrent.futures import ThreadPoolExecutor\n\ndef viec_io(i: int) -> int:\n    time.sleep(0.3)          # ⭐ mô phỏng chờ mạng — NHẢ GIL\n    return i\n\nSO_VIEC = 20\n\nt = time.perf_counter()\n[viec_io(i) for i in range(SO_VIEC)]\nprint(f\"tuần tự  : {time.perf_counter()-t:.2f}s\")     # ~6.0s\n\nt = time.perf_counter()\nwith ThreadPoolExecutor(max_workers=20) as ex:\n    list(ex.map(viec_io, range(SO_VIEC)))\nprint(f\"20 thread: {time.perf_counter()-t:.2f}s\")     # ~0.3s ⭐ gấp 20 lần\n```\n\nCùng GIL, kết quả ngược hẳn ví dụ trước. Khác biệt duy nhất: `time.sleep` nhả GIL, còn vòng lặp tính toán thì không.\n\nVới công việc thật:\n\n```python\nimport time\nfrom concurrent.futures import ThreadPoolExecutor\nfrom urllib.request import urlopen\n\ndef tai(url: str) -> int:\n    with urlopen(url, timeout=10) as r:\n        return len(r.read())\n\nurls = [\"https://example.com\"] * 20\n\nt = time.perf_counter()\nwith ThreadPoolExecutor(max_workers=10) as ex:\n    tong = sum(ex.map(tai, urls))\nprint(f\"tải {len(urls)} trang: {time.perf_counter()-t:.2f}s, {tong:,} byte\")\n```\n\n`ThreadPoolExecutor` là cách dùng thread gọn nhất: nó lo việc tạo, quản lý và dọn luồng; `map` giữ đúng thứ tự kết quả.\n\n### Xác định loại công việc\n\n```python\nimport time\n\ndef phan_loai(ham, *a, **k):\n    \"\"\"Đo tỉ lệ thời gian CPU trên thời gian thực tế.\"\"\"\n    t_thuc = time.perf_counter()\n    t_cpu = time.process_time()\n    kq = ham(*a, **k)\n    thuc = time.perf_counter() - t_thuc\n    cpu = time.process_time() - t_cpu\n    ti_le = cpu / thuc if thuc else 0\n\n    loai = \"CPU-bound\" if ti_le > 0.8 else (\n           \"I/O-bound\" if ti_le < 0.3 else \"hỗn hợp\")\n    print(f\"{ham.__name__:<16} thực={thuc:.2f}s cpu={cpu:.2f}s \"\n          f\"tỉ lệ={ti_le:>5.0%}  → {loai}\")\n    return kq\n\n\ndef tinh_toan():\n    return sum(i * i for i in range(5_000_000))\n\ndef cho_doi():\n    time.sleep(1)\n\ndef hon_hop():\n    sum(i * i for i in range(2_000_000))\n    time.sleep(0.5)\n\nphan_loai(tinh_toan)     # tỉ lệ ~100% → CPU-bound → dùng process\nphan_loai(cho_doi)       # tỉ lệ ~0%   → I/O-bound → dùng thread/async\nphan_loai(hon_hop)       # ở giữa      → cần tách hai phần\n```\n\n`time.process_time()` chỉ đếm thời gian CPU **thật sự dùng**, không tính thời gian chờ. Chênh lệch giữa nó và `perf_counter` chính là thời gian chờ đợi.\n\nĐây là phép đo nên chạy trước khi chọn công cụ — nó thay việc đoán bằng số liệu.\n\n### `multiprocessing` cho CPU-bound\n\n```python\nfrom multiprocessing import Pool, cpu_count\nimport time, os\n\ndef xu_ly_khoi(khoi: tuple[int, int]) -> int:\n    dau, cuoi = khoi\n    return sum(i * i for i in range(dau, cuoi))\n\n\nif __name__ == \"__main__\":\n    N = 40_000_000\n    so_nhan = cpu_count()\n    print(f\"máy có {so_nhan} nhân\")\n\n    # Chia việc thành các khối\n    kich_thuoc = N // so_nhan\n    khoi = [(i * kich_thuoc, (i + 1) * kich_thuoc) for i in range(so_nhan)]\n\n    t = time.perf_counter()\n    tuan_tu = sum(xu_ly_khoi(k) for k in khoi)\n    tg_tuan_tu = time.perf_counter() - t\n\n    t = time.perf_counter()\n    with Pool(so_nhan) as p:\n        song_song = sum(p.map(xu_ly_khoi, khoi))\n    tg_song_song = time.perf_counter() - t\n\n    print(f\"tuần tự  : {tg_tuan_tu:.2f}s\")\n    print(f\"{so_nhan} tiến trình: {tg_song_song:.2f}s\")\n    print(f\"tăng tốc : {tg_tuan_tu/tg_song_song:.1f}x\")\n    assert tuan_tu == song_song\n```\n\nTăng tốc thực tế **thấp hơn số nhân** vì ba lý do:\n\n```\n· Chi phí tạo tiến trình (vài chục tới vài trăm ms)\n· Chi phí tuần tự hoá dữ liệu vào và kết quả ra\n· Hệ điều hành còn chạy việc khác\n```\n\nQuy tắc thực dụng: **chỉ dùng tiến trình khi mỗi việc con tốn ít nhất vài trăm mili giây.** Việc nhỏ hơn thì chi phí lớn hơn lợi ích.\n\nMinh hoạ ngưỡng đó:\n\n```python\nfrom multiprocessing import Pool\nimport time\n\ndef viec_nho(x): return x * x\n\nif __name__ == \"__main__\":\n    ds = list(range(100_000))\n\n    t = time.perf_counter()\n    [viec_nho(x) for x in ds]\n    print(f\"tuần tự : {time.perf_counter()-t:.3f}s\")\n\n    t = time.perf_counter()\n    with Pool(4) as p:\n        p.map(viec_nho, ds)\n    print(f\"4 process: {time.perf_counter()-t:.3f}s\")     # ⚠️ CHẬM HƠN nhiều\n```\n\nMỗi việc chỉ vài micro giây, còn chi phí gửi qua tiến trình là hàng chục micro giây — nên song song hoá làm chậm đi.\n\nCách sửa: **chia thành khối lớn** thay vì gửi từng phần tử.\n\n```python\nwith Pool(4) as p:\n    p.map(viec_nho, ds, chunksize=10_000)     # ⭐ gộp thành khối\n```\n\n### Truyền dữ liệu qua tiến trình\n\n```python\nfrom multiprocessing import Pool\nimport time, pickle\n\ndu_lieu_lon = list(range(5_000_000))\n\ndef xu_ly_truyen(ds): return sum(ds)\ndef xu_ly_tu_doc(duong_dan):\n    # tiến trình con tự đọc — không truyền dữ liệu\n    return sum(range(5_000_000))\n\nif __name__ == \"__main__\":\n    # Đo chi phí tuần tự hoá\n    t = time.perf_counter()\n    b = pickle.dumps(du_lieu_lon)\n    print(f\"pickle {len(b)/1024/1024:.0f} MB: {time.perf_counter()-t:.2f}s\")\n\n    # ❌ Truyền dữ liệu lớn\n    t = time.perf_counter()\n    with Pool(2) as p:\n        p.map(xu_ly_truyen, [du_lieu_lon, du_lieu_lon])\n    print(f\"truyền dữ liệu: {time.perf_counter()-t:.2f}s\")\n\n    # ✓ Truyền tham chiếu nhẹ\n    t = time.perf_counter()\n    with Pool(2) as p:\n        p.map(xu_ly_tu_doc, [\"a\", \"b\"])\n    print(f\"tự đọc        : {time.perf_counter()-t:.2f}s\")\n```\n\nChi phí `pickle` cho dữ liệu lớn thường **át hết** lợi ích song song. Nguyên tắc: **truyền đường dẫn, id, hoặc khoảng chỉ số — đừng truyền dữ liệu.**\n\n### `asyncio` cho hàng nghìn kết nối\n\n```python\nimport asyncio, time\n\nasync def viec(i: int, tre: float = 0.2) -> int:\n    await asyncio.sleep(tre)\n    return i * i\n\n\nasync def main():\n    for n in (100, 1000, 5000):\n        t = time.perf_counter()\n        kq = await asyncio.gather(*[viec(i) for i in range(n)])\n        print(f\"{n:>5} việc: {time.perf_counter()-t:.2f}s, tổng={sum(kq):,}\")\n\nasyncio.run(main())\n# 100 việc: 0.20s\n# 1000 việc: 0.21s\n# 5000 việc: 0.25s   ⭐ gần như không tăng\n```\n\nNăm nghìn việc đồng thời trong một luồng. Với thread thì 5.000 luồng × 8 MB stack là 40 GB — bất khả thi.\n\nGiới hạn số việc chạy cùng lúc khi cần:\n\n```python\nimport asyncio\n\nasync def co_gioi_han(sem: asyncio.Semaphore, i: int):\n    async with sem:                      # ⭐ tối đa N việc cùng lúc\n        await asyncio.sleep(0.2)\n        return i\n\nasync def main():\n    sem = asyncio.Semaphore(50)          # không quá 50 kết nối đồng thời\n    kq = await asyncio.gather(*[co_gioi_han(sem, i) for i in range(1000)])\n    print(len(kq))\n\nasyncio.run(main())\n```\n\n`Semaphore` cần thiết trong thực tế: server bên kia thường có giới hạn, và gửi 1.000 request cùng lúc là cách nhanh nhất để bị chặn.\n\n### Hàm chặn đóng băng vòng lặp async\n\n```python\nimport asyncio, time\n\nasync def dem(ten: str, n: int):\n    for i in range(n):\n        print(f\"  {ten}: {i}\")\n        await asyncio.sleep(0.1)\n\nasync def chan():\n    print(\"  chặn: bắt đầu\")\n    time.sleep(1)                        # ❌ CHẶN cả vòng lặp\n    print(\"  chặn: xong\")\n\nasync def khong_chan():\n    print(\"  ok: bắt đầu\")\n    await asyncio.sleep(1)               # ✓ nhường quyền\n    print(\"  ok: xong\")\n\n\nasync def main():\n    print(\"Với hàm CHẶN:\")\n    await asyncio.gather(dem(\"đếm\", 3), chan())\n    # \"đếm\" bị đóng băng suốt 1 giây\n\n    print(\"\\nVới hàm KHÔNG chặn:\")\n    await asyncio.gather(dem(\"đếm\", 3), khong_chan())\n    # \"đếm\" chạy xen kẽ bình thường\n\nasyncio.run(main())\n```\n\nĐây là lỗi phổ biến nhất khi mới dùng `asyncio`: **một lời gọi đồng bộ chặn toàn bộ chương trình.** Triệu chứng là ứng dụng \"đứng hình\" không rõ lý do.\n\nCách xử lý:\n\n```python\nimport asyncio\nfrom concurrent.futures import ThreadPoolExecutor, ProcessPoolExecutor\n\ndef io_chan(x):\n    import time; time.sleep(1); return x\n\ndef cpu_nang(n):\n    return sum(i*i for i in range(n))\n\n\nasync def main():\n    vong = asyncio.get_running_loop()\n\n    with ThreadPoolExecutor(max_workers=4) as ex:\n        kq = await asyncio.gather(\n            *[vong.run_in_executor(ex, io_chan, i) for i in range(4)])\n        print(f\"I/O chặn qua thread: {kq}\")\n\n    with ProcessPoolExecutor(max_workers=2) as ex:\n        kq = await asyncio.gather(\n            *[vong.run_in_executor(ex, cpu_nang, 5_000_000) for _ in range(2)])\n        print(f\"CPU qua process: xong\")\n\nasyncio.run(main())\n```\n\nMẫu này ghép cả ba công cụ đúng vai trò: async điều phối, thread cho I/O chặn, tiến trình cho CPU.\n\n### GIL không ngăn race condition\n\n```python\nimport threading, dis\n\n# Xem vì sao dem += 1 không nguyên tử\ndis.dis(compile(\"dem += 1\", \"<x>\", \"exec\"))\n\n\ndem = 0\n\ndef tang_khong_khoa():\n    global dem\n    for _ in range(200_000):\n        dem += 1\n\ndem = 0\nl = [threading.Thread(target=tang_khong_khoa) for _ in range(4)]\nfor x in l: x.start()\nfor x in l: x.join()\nprint(f\"không khoá: {dem:,} (đáng lẽ 800,000)\")     # ⚠️ thiếu\n\n\n# ✓ Có khoá\ndem = 0\nkhoa = threading.Lock()\n\ndef tang_co_khoa():\n    global dem\n    for _ in range(200_000):\n        with khoa:\n            dem += 1\n\nl = [threading.Thread(target=tang_co_khoa) for _ in range(4)]\nfor x in l: x.start()\nfor x in l: x.join()\nprint(f\"có khoá   : {dem:,}\")                        # 800,000 ✓\n```\n\nBản có khoá **chậm hơn** đáng kể — đó là cái giá của tính đúng đắn. Cách nhanh hơn là giảm số lần lấy khoá:\n\n```python\ndef tang_gom():\n    global dem\n    cuc_bo = 0\n    for _ in range(200_000):\n        cuc_bo += 1              # ⭐ biến CỤC BỘ, không tranh chấp\n    with khoa:\n        dem += cuc_bo            # ⭐ lấy khoá MỘT lần\n```\n\nMẫu \"tích luỹ cục bộ, gộp một lần\" là kỹ thuật quan trọng: nó giảm tranh chấp khoá từ hàng trăm nghìn lần xuống một lần.\n\nHoặc dùng cấu trúc an toàn sẵn:\n\n```python\nimport queue, threading\n\nq = queue.Queue()               # ⭐ đã an toàn cho đa luồng\n\ndef san_xuat():\n    for i in range(1000):\n        q.put(i)\n\ndef tieu_thu(kq: list):\n    while True:\n        x = q.get()\n        if x is None: break\n        kq.append(x)\n        q.task_done()\n```\n\n`queue.Queue` xử lý mọi việc khoá bên trong — đây là cách trao đổi dữ liệu giữa luồng an toàn nhất và đơn giản nhất.\n\n### Thư viện C nhả GIL — kiểm chứng\n\n```python\nimport time, threading\n\ntry:\n    import numpy as np\nexcept ImportError:\n    np = None\n\n\ndef numpy_nang():\n    a = np.random.rand(1200, 1200)\n    return a @ a                      # ⭐ chạy trong C, nhả GIL\n\ndef python_nang():\n    return sum(i * i for i in range(8_000_000))     # giữ GIL\n\n\ndef do_song_song(f, ten, n=2):\n    t = time.perf_counter()\n    f(); f()\n    tuan_tu = time.perf_counter() - t\n\n    t = time.perf_counter()\n    l = [threading.Thread(target=f) for _ in range(n)]\n    for x in l: x.start()\n    for x in l: x.join()\n    thread = time.perf_counter() - t\n\n    print(f\"{ten:<16} tuần tự={tuan_tu:.2f}s thread={thread:.2f}s \"\n          f\"tăng tốc={tuan_tu/thread:.2f}x\")\n\n\nif np is not None:\n    do_song_song(numpy_nang, \"NumPy (C)\")      # tăng tốc > 1 ⭐\ndo_song_song(python_nang, \"Python thuần\")      # tăng tốc ≈ 1 hoặc thấp hơn\n```\n\nHai dòng kết quả cho thấy rõ: cùng là \"tính toán nặng\", nhưng cái chạy trong C **song song được** còn Python thuần thì không.\n\nĐây là kiến thức trực tiếp cho AI/ML: phần huấn luyện chạy trong thư viện C nên thread có ích; phần tiền xử lý viết bằng Python thì cần tiến trình.\n\n### Bảng chọn công cụ — dạng code\n\n```python\ndef chon_cong_cu(ti_le_cpu: float, so_viec: int, dung_thu_vien_c: bool) -> str:\n    if ti_le_cpu > 0.8:                       # CPU-bound\n        if dung_thu_vien_c:\n            return \"thread (thư viện đã nhả GIL) hoặc chạy thẳng\"\n        return \"multiprocessing — song song thật\"\n    if so_viec > 500:\n        return \"asyncio — hàng nghìn kết nối, tốn ít bộ nhớ\"\n    return \"ThreadPoolExecutor — đơn giản, đủ dùng\"\n\n\nprint(chon_cong_cu(0.95, 4, False))    # multiprocessing\nprint(chon_cong_cu(0.95, 4, True))     # thread\nprint(chon_cong_cu(0.05, 50, False))   # ThreadPoolExecutor\nprint(chon_cong_cu(0.05, 5000, False)) # asyncio\n```\n\nBa tham số này là ba câu hỏi cần trả lời trước khi viết một dòng code đồng thời nào, và câu đầu đo được bằng `phan_loai()` ở trên.\n\n### Đo chi phí mỗi công cụ\n\n```python\nimport time, threading, asyncio\nfrom multiprocessing import Process\n\nN = 50\n\n# Chi phí tạo thread\nt = time.perf_counter()\nl = [threading.Thread(target=lambda: None) for _ in range(N)]\nfor x in l: x.start()\nfor x in l: x.join()\nprint(f\"{N} thread : {(time.perf_counter()-t)*1000:>7.1f} ms\")\n\n# Chi phí tạo coroutine\nasync def rong(): pass\nasync def main():\n    t = time.perf_counter()\n    await asyncio.gather(*[rong() for _ in range(N)])\n    print(f\"{N} coroutine: {(time.perf_counter()-t)*1000:>7.1f} ms\")\nasyncio.run(main())\n\n# Chi phí tạo process\nif __name__ == \"__main__\":\n    t = time.perf_counter()\n    p = [Process(target=lambda: None) for _ in range(N)]\n    for x in p: x.start()\n    for x in p: x.join()\n    print(f\"{N} process: {(time.perf_counter()-t)*1000:>7.1f} ms\")\n```\n\nThứ tự chi phí điển hình: coroutine rẻ nhất, thread ở giữa, process đắt nhất — thường chênh nhau hai bậc độ lớn. Con số này giải thích vì sao ngưỡng \"vài trăm mili giây mỗi việc\" tồn tại cho tiến trình.",

so_sanh: "### Ba công cụ đồng thời\n\n| | `threading` | `multiprocessing` | `asyncio` |\n|---|---|---|---|\n| Song song thật CPU | **✗ (bị GIL)** | **✓** | ✗ |\n| Bộ nhớ mỗi đơn vị | ~8 MB | **~30 MB+** | **~vài KB** |\n| Chi phí tạo | vừa | **cao** | **rất thấp** |\n| Số đơn vị khả thi | hàng trăm | bằng số nhân | **hàng chục nghìn** |\n| Chia sẻ dữ liệu | **trực tiếp** | phải tuần tự hoá | trực tiếp |\n| Cần khoá | **✓** | ✗ (bộ nhớ riêng) | ít hơn |\n| Đổi code hiện có | ít | ít | **nhiều — phải `async`** |\n| Hợp với | I/O-bound vừa | **CPU-bound** | **I/O-bound nhiều kết nối** |\n\nHàng áp chót là chi phí ẩn của `asyncio`: nó \"lây\" — một hàm `async` thì mọi nơi gọi nó cũng phải `async`.\n\n### GIL được nhả khi nào\n\n| Tình huống | Nhả GIL? | Hệ quả |\n|---|---|---|\n| `time.sleep`, chờ mạng, đọc file | **✓** | thread tăng tốc thật |\n| Thư viện C thiết kế nhả (NumPy...) | **✓** | thread tăng tốc thật |\n| Vòng lặp Python thuần | ✗ | thread vô ích |\n| Định kỳ mỗi ~5 ms | ✓ (công bằng) | không tăng tốc |\n\nHai hàng đầu là toàn bộ lý do thread vẫn hữu ích trong Python.\n\n### CPU-bound và I/O-bound\n\n| | CPU-bound | I/O-bound |\n|---|---|---|\n| Nút cổ chai | tốc độ tính | **thời gian chờ** |\n| Tỉ lệ CPU/thực tế | **~100%** | **thấp** |\n| Thread giúp | ✗ | **✓** |\n| Process giúp | **✓** | ✓ nhưng lãng phí |\n| Async giúp | ✗ | **✓ nhất** |\n| Ví dụ | nén, xử lý ảnh, tính toán | gọi API, truy vấn DB, đọc file |\n\nĐo bằng `time.process_time()` so với `time.perf_counter()` — chênh lệch chính là thời gian chờ.\n\n### GIL đảm bảo gì và không đảm bảo gì\n\n| | Đảm bảo | Không đảm bảo |\n|---|---|---|\n| Một lệnh bytecode không bị cắt | **✓** | |\n| `dem += 1` nguyên tử | | **✗ nhiều lệnh** |\n| `d[k] += 1` nguyên tử | | **✗** |\n| `ds.append(x)` nguyên tử | ✓ (hiện tại) | đừng dựa vào |\n| Code đa luồng tự động đúng | | **✗ vẫn cần khoá** |\n\nHàng cuối là hiểu nhầm nguy hiểm nhất về GIL.\n\n### Bảng chọn công cụ\n\n| Loại công việc | Số lượng | Công cụ |\n|---|---|---|\n| CPU-bound, Python thuần | bất kỳ | **`multiprocessing`** |\n| CPU-bound, thư viện C nhả GIL | bất kỳ | **thread** hoặc chạy thẳng |\n| I/O-bound | < 100 | **`ThreadPoolExecutor`** |\n| I/O-bound | > 500 | **`asyncio`** |\n| Trộn cả hai | — | `asyncio` + `run_in_executor` |\n\nHàng cuối là mẫu thực tế nhất cho ứng dụng thật: async điều phối, executor lo phần chặn.\n\n### Chi phí tương đối\n\n| | Tạo một đơn vị | Bộ nhớ | Chuyển đổi |\n|---|---|---|---|\n| Coroutine | **rẻ nhất** | **~KB** | rẻ nhất |\n| Thread | vừa | ~8 MB | vừa (do hệ điều hành) |\n| Process | **đắt nhất** | **~30 MB+** | rất đắt |\n\nBảng này giải thích ngưỡng thực dụng: **tiến trình chỉ đáng khi mỗi việc tốn ít nhất vài trăm mili giây.**\n\n### Cạm bẫy mỗi công cụ\n\n| Công cụ | Cạm bẫy chính |\n|---|---|\n| `threading` | race condition; tưởng GIL làm code an toàn |\n| `multiprocessing` | thiếu `if __name__`; truyền dữ liệu lớn; hàm phải pickle được |\n| `asyncio` | **một lời gọi chặn đóng băng cả vòng lặp**; `async` lây khắp code |\n\nBa cạm bẫy này gây ra phần lớn sự cố khi mới dùng đồng thời trong Python.",

loi_thuong_gap: [
  "**Dùng thread cho công việc CPU-bound.** Không nhanh hơn, thường chậm hơn do chi phí chuyển luồng. Dùng `multiprocessing`.",
  "**Tưởng GIL làm code đa luồng tự động an toàn.** `dem += 1` vẫn là race condition vì nó gồm nhiều lệnh bytecode.",
  "**Dựa vào danh sách \"thao tác nguyên tử\".** Đó là chi tiết cài đặt, có thể đổi. Dùng khoá hoặc `queue.Queue`.",
  "**Quên `if __name__ == \"__main__\"` với `multiprocessing`.** Trên Windows, tiến trình con import lại module và sinh tiến trình vô hạn.",
  "**Truyền dữ liệu lớn qua `Pool.map`.** Chi phí `pickle` át hết lợi ích. Truyền đường dẫn hoặc khoảng chỉ số.",
  "**Dùng `Pool` cho việc quá nhỏ.** Chi phí gửi qua tiến trình lớn hơn công việc. Dùng `chunksize` hoặc chia khối lớn.",
  "**Truyền `lambda` hoặc hàm cục bộ vào `Pool`.** Chúng không pickle được.",
  "**Gọi hàm chặn trong coroutine.** `time.sleep`, `requests.get` đóng băng cả vòng lặp. Dùng `run_in_executor`.",
  "**Không giới hạn số việc đồng thời trong `asyncio`.** Gửi 1.000 request cùng lúc là cách nhanh nhất bị chặn. Dùng `Semaphore`.",
  "**Lấy khoá trong vòng lặp nóng.** Tích luỹ vào biến cục bộ rồi gộp một lần.",
  "**Tưởng \"GIL nghĩa là Python không làm được đồng thời\".** Nó chặn song song bytecode, không chặn đồng thời; I/O-bound tăng tốc thật.",
  "**Chọn công cụ bằng cảm tính.** Đo tỉ lệ `process_time` trên `perf_counter` trước.",
  "**Dùng tiến trình cho I/O-bound.** Tốn bộ nhớ gấp nhiều lần mà không lợi gì so với thread."
],

tu_kiem_tra: [
  "GIL là gì? Vì sao nó tồn tại — lý do gốc rễ liên quan tới bài trước thế nào?",
  "Nêu hai cách giải quyết vấn đề bộ đếm tham chiếu đa luồng. CPython chọn cách nào và đánh đổi gì?",
  "GIL được nhả trong ba tình huống nào? Tình huống nào làm thread hữu ích cho I/O?",
  "Vì sao hai luồng tính toán lại **chậm hơn** chạy tuần tự?",
  "Vì sao 10 luồng gọi mạng nhanh gấp 10 lần dù có GIL?",
  "Phân biệt CPU-bound và I/O-bound. Đo bằng cách nào một cách khách quan?",
  "Vì sao `dem += 1` không nguyên tử? Xem bytecode để giải thích.",
  "GIL đảm bảo điều gì và **không** đảm bảo điều gì?",
  "Nêu bốn cái giá của `multiprocessing`. Ngưỡng thực dụng để dùng nó là gì?",
  "Vì sao `if __name__ == \"__main__\"` bắt buộc với `multiprocessing` trên Windows?",
  "Vì sao truyền dữ liệu lớn qua `Pool` thường phản tác dụng? Cách đúng là gì?",
  "`asyncio` khác thread ở cơ chế chuyển việc thế nào? Hệ quả nguy hiểm nhất là gì?",
  "Vì sao NumPy tăng tốc được với thread còn vòng lặp Python thì không?",
  "Nêu bảng quyết định chọn công cụ theo ba tham số.",
  "Kể cả khi GIL biến mất, hai điều gì vẫn đúng?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, module `threading`, `multiprocessing`, `asyncio` — ba trang tham khảo chính, mỗi trang có phần so sánh khi nào dùng.",
  "Tài liệu Python, module `concurrent.futures` — `ThreadPoolExecutor` và `ProcessPoolExecutor` với giao diện thống nhất.",
  "Sách **CPython Internals** — Anthony Shaw. Chương về GIL giải thích cơ chế lấy và nhả khoá ở tầng mã nguồn.",
  "Sách **High Performance Python** — Micha Gorelick & Ian Ozsvald. Các chương về đa luồng, đa tiến trình và cách đo.",
  "Sách **Using Asyncio in Python** — Caleb Hattingh. Ngắn gọn, tập trung vào những chỗ dễ sai khi mới dùng `asyncio`."
],

lien_he: [
  "**Garbage collection và đếm tham chiếu** ngay trước: bộ đếm nằm trong object là lý do gốc rễ GIL tồn tại.",
  "**Đo bộ nhớ và tối ưu** ngay sau: tiến trình tốn bộ nhớ gấp nhiều lần thread — con số đó ảnh hưởng quyết định kiến trúc.",
  "**Concurrency vs parallelism** ở module 2: GIL chặn song song, không chặn đồng thời — đây là ứng dụng cụ thể của phân biệt đó.",
  "**Deadlock, race condition** ở module 2: GIL không thay được khoá; `dem += 1` vẫn cần vùng găng.",
  "**Process vs thread** ở module 2: bộ nhớ riêng và bộ nhớ chung giải thích vì sao tiến trình phải tuần tự hoá dữ liệu.",
  "**Module, package, import** ở module 5: `if __name__ == \"__main__\"` bắt buộc vì tiến trình con import lại module chính.",
  "**Mutable vs immutable** trong module này: dữ liệu bất biến không cần khoá — cách giảm tranh chấp tốt nhất.",
  "**Context manager và `with`** ở module 5: `with khoa` đảm bảo mở khoá kể cả khi có ngoại lệ.",
  "**WebSocket vs polling** ở module 4: giữ hàng nghìn kết nối mở là bài toán I/O-bound kinh điển — chỗ `asyncio` toả sáng.",
  "**Backend & Web** ở mảng 05: số worker, chọn máy chủ ứng dụng, và kiến trúc xử lý bất đồng bộ đều dựa trên bài này.",
  "**AI/ML** ở mảng 07: vòng lặp huấn luyện chạy trong C nên nhả GIL; tiền xử lý Python thuần thì cần tiến trình — đây là lý do `DataLoader` cho chọn kiểu worker."
]

};
