window.LESSONS["01:bo-nho-co-che-thuc-thi:do-bo-nho-va-toi-uu-co-ban"] = {

tom_tat: [
  "**Đo trước, tối ưu sau — không có ngoại lệ.** Trực giác về chỗ tốn bộ nhớ sai gần như luôn luôn, và tối ưu nhầm chỗ làm code xấu đi mà không nhanh lên.",
  "**`sys.getsizeof` chỉ đo container, không đo nội dung.** Dùng một mình nó là nguồn kết luận sai phổ biến nhất; `tracemalloc` mới cho bức tranh thật.",
  "**Bốn kỹ thuật có tác dụng lớn nhất**, theo thứ tự đáng thử: dùng generator thay list, `__slots__` cho object nhiều bản, mảng liền khối thay `list` số, và `sys.intern` cho chuỗi lặp.",
  "**Bộ nhớ tiến trình không giảm sau khi dọn là chuyện bình thường** — CPython giữ lại khối để tái dùng. Đo bằng công cụ tầng Python, đừng đo bằng công cụ hệ điều hành.",
  "**Đánh đổi luôn tồn tại**: generator mất khả năng lặp lại, `__slots__` mất tính linh hoạt, mảng liền khối mất tính đa kiểu. Chỉ trả giá khi con số biện minh được."
],

can_biet_truoc: [
  "Bài **Tham chiếu vs giá trị** — object gồm gì, container lưu con trỏ; nền để hiểu mọi con số ở đây.",
  "Bài **Garbage collection** — vì sao bộ nhớ tiến trình không giảm, và năm hình dạng rò rỉ.",
  "Bài **GIL** ngay trước — tiến trình tốn bộ nhớ gấp nhiều lần thread; con số đó thuộc bài này.",
  "Bài **Generator, iterator** ở module 5 — công cụ tiết kiệm bộ nhớ quan trọng nhất."
],

dinh_nghia: "Nói đời thường: trước khi dọn nhà cho rộng, bạn phải biết **cái gì đang chiếm chỗ**. Đo bằng mắt thường hay sai — cái tủ to nhìn cồng kềnh nhưng có khi hàng trăm hộp nhỏ mới là thứ ngốn diện tích.\n\nNói chính xác:\n\n- **`sys.getsizeof(x)`** — kích thước của **chính object đó**, không tính object nó trỏ tới.\n- **`tracemalloc`** — module chuẩn theo dõi mọi lần cấp phát bộ nhớ của Python, kèm vị trí dòng code.\n- **Ảnh chụp** (*snapshot*) — trạng thái cấp phát tại một thời điểm, so sánh được với ảnh khác.\n- **`__slots__`** — khai báo cố định danh sách thuộc tính, bỏ `__dict__` của instance.\n- **Bộ nhớ thường trú** (*RSS*) — bộ nhớ vật lý mà tiến trình đang chiếm, nhìn từ hệ điều hành.\n- **Phân mảnh** — bộ nhớ đã giải phóng nhưng nằm rải rác, không trả về hệ điều hành được.\n\nĐiểm cần nắm ngay: **có hai câu hỏi khác nhau và hai bộ công cụ khác nhau.** *\"Python đang cấp phát bao nhiêu\"* thì dùng `tracemalloc`. *\"Tiến trình chiếm bao nhiêu của hệ điều hành\"* thì dùng công cụ hệ thống. Hai con số này thường lệch nhau, và nhầm lẫn giữa chúng dẫn tới kết luận sai.",

vi_sao_can: "Tối ưu bộ nhớ là việc dễ làm sai theo cả hai hướng:\n\n- **Tối ưu nhầm chỗ là công cốc.** Bạn dành cả ngày rút gọn một cấu trúc chiếm 2% bộ nhớ, trong khi 80% nằm ở một cache mà không ai để ý.\n- **Không đo thì không biết mình có vấn đề hay không.** \"Chương trình chậm\" và \"chương trình tốn RAM\" là hai chuyện khác nhau, và cách chữa cũng khác.\n- **Hết bộ nhớ làm chương trình chết đột ngột.** Khác với chậm — bạn còn kịp phản ứng — hết RAM là bị hệ điều hành giết, thường vào lúc tải cao nhất.\n- **Bộ nhớ ảnh hưởng cả tốc độ.** Dữ liệu liền khối vừa tốn ít vừa chạy nhanh nhờ cache CPU; đây là chỗ hai mục tiêu đi cùng chiều.\n- **Định hướng AI/ML.** Dataset không vừa RAM là tình huống thường ngày, và bốn kỹ thuật trong bài quyết định bạn xử lý được bao nhiêu dữ liệu trên cùng một máy.\n\nTình huống thật rất kinh điển: dịch vụ của bạn ngốn 3 GB và bạn quyết định tối ưu. Bạn đổi vài `list` sang `tuple`, thêm `__slots__` cho một class — bộ nhớ giảm 40 MB. Rồi bạn chạy `tracemalloc` và phát hiện **2,4 GB nằm trong một `dict` cache không giới hạn kích thước**. Một dòng `maxsize` giải quyết vấn đề mà cả ngày tối ưu thủ công không làm được. Đây là lý do quy tắc \"đo trước\" không có ngoại lệ.",

co_che: "### Hai câu hỏi, hai bộ công cụ\n\n```\n\"Python cấp phát bao nhiêu?\"        → tracemalloc, sys.getsizeof\n\"Tiến trình chiếm bao nhiêu RAM?\"    → công cụ hệ điều hành, resource\n\nHai con số THƯỜNG LỆCH NHAU vì:\n· CPython giữ lại khối đã giải phóng để tái dùng\n· Bộ nhớ bị phân mảnh, không trả về hệ điều hành được\n· Bản thân trình thông dịch và thư viện đã chiếm chỗ\n```\n\nHệ quả quan trọng: **\"RSS không giảm sau khi dọn\" không phải bằng chứng rò rỉ.** Chỉ `tracemalloc` mới nói được Python có thật sự giữ object hay không.\n\n### `sys.getsizeof` và giới hạn của nó\n\n```python\nimport sys\n\nds = [1, 2, 3]\nsys.getsizeof(ds)                 # 88 — CHỈ mảng con trỏ + phần đầu\n\nds = [\"chuỗi rất dài \" * 1000] * 3\nsys.getsizeof(ds)                 # vẫn ~88 ⚠️ không tính nội dung\n```\n\nĐây là giới hạn quan trọng nhất: nó **chỉ đo chính object**, đúng như bài đầu module đã nói — container lưu con trỏ.\n\nHàm đo đúng phải đi đệ quy và khử trùng:\n\n```python\nimport sys\nfrom collections.abc import Mapping, Iterable\n\ndef kich_thuoc_sau(o, da_thay: set | None = None) -> int:\n    \"\"\"Đo cả nội dung, mỗi object đếm ĐÚNG MỘT lần.\"\"\"\n    da_thay = set() if da_thay is None else da_thay\n    if id(o) in da_thay:\n        return 0                              # ⭐ tránh đếm trùng và vòng lặp\n    da_thay.add(id(o))\n\n    n = sys.getsizeof(o)\n    if isinstance(o, Mapping):\n        n += sum(kich_thuoc_sau(k, da_thay) + kich_thuoc_sau(v, da_thay)\n                 for k, v in o.items())\n    elif isinstance(o, (str, bytes, bytearray)):\n        pass                                   # ⭐ str đã tính đủ, đừng lặp ký tự\n    elif isinstance(o, Iterable):\n        n += sum(kich_thuoc_sau(x, da_thay) for x in o)\n    if hasattr(o, \"__dict__\"):\n        n += kich_thuoc_sau(vars(o), da_thay)\n    return n\n```\n\nBa chi tiết trong hàm này đều bắt buộc:\n\n```\nda_thay theo id()     · khử trùng khi nhiều ô trỏ cùng object\n                      · chống lặp vô hạn với tham chiếu vòng\ntách riêng str        · str là Iterable, lặp qua nó sẽ đếm từng ký tự\nvars(o)               · object có __dict__ thì phần đó cũng tốn bộ nhớ\n```\n\nQuên chi tiết đầu là con số **phồng lên nhiều lần** với dữ liệu dùng chung — đúng bẫy cache số nguyên nhỏ ở bài một.\n\nNgay cả hàm này cũng chỉ **gần đúng**: nó không tính phần bộ nhớ mà bộ cấp phát dự trữ. Với đo nghiêm túc thì dùng `tracemalloc`.\n\n### `tracemalloc` — công cụ chính\n\n```python\nimport tracemalloc\n\ntracemalloc.start()                    # bắt đầu theo dõi\n\ndu_lieu = [{\"id\": i, \"ten\": f\"muc {i}\"} for i in range(100_000)]\n\nhien_tai, dinh = tracemalloc.get_traced_memory()\nprint(f\"hiện tại: {hien_tai/1024/1024:.1f} MB\")\nprint(f\"đỉnh    : {dinh/1024/1024:.1f} MB\")\n\ntracemalloc.stop()\n```\n\n`get_traced_memory` trả về **cả đỉnh** — con số quan trọng hơn nhiều người nghĩ: chương trình bị giết vì chạm đỉnh, không phải vì mức trung bình.\n\nTìm dòng code cấp phát nhiều nhất:\n\n```python\nimport tracemalloc\n\ntracemalloc.start()\n\ndu_lieu = [\"x\" * 1000 for _ in range(10_000)]\nkhac = {i: [i] * 100 for i in range(1_000)}\n\nanh = tracemalloc.take_snapshot()\ntop = anh.statistics(\"lineno\")\n\nprint(\"10 dòng cấp phát nhiều nhất:\")\nfor i, tk in enumerate(top[:10], 1):\n    print(f\"{i:>2}. {tk.size/1024:>9.1f} KB  {tk.count:>7,} khối  {tk}\")\n\ntracemalloc.stop()\n```\n\nSo sánh hai thời điểm — đây là cách tìm rò rỉ:\n\n```python\nimport tracemalloc\n\ntracemalloc.start()\nnen = tracemalloc.take_snapshot()\n\nfor _ in range(100):\n    xu_ly_mot_yeu_cau()               # chu kỳ nghiệp vụ thật\n\nsau = tracemalloc.take_snapshot()\nkhac_biet = sau.compare_to(nen, \"lineno\")\n\nprint(\"Tăng nhiều nhất sau 100 chu kỳ:\")\nfor tk in khac_biet[:10]:\n    if tk.size_diff > 0:\n        print(f\"  +{tk.size_diff/1024:>8.1f} KB  {tk}\")\n\ntracemalloc.stop()\n```\n\nMẫu này là quy trình chuẩn tìm rò rỉ: **chạy cùng một chu kỳ nhiều lần, so ảnh chụp trước và sau.** Dòng nào tăng đều qua các chu kỳ là ứng viên.\n\nXem cả ngăn xếp gọi thay vì một dòng:\n\n```python\ntracemalloc.start(10)                  # ⭐ giữ 10 khung stack\n...\nanh = tracemalloc.take_snapshot()\nfor tk in anh.statistics(\"traceback\")[:3]:\n    print(f\"{tk.size/1024:.1f} KB:\")\n    for dong in tk.traceback.format():\n        print(f\"    {dong}\")\n```\n\nHữu ích khi cùng một hàm tiện ích được gọi từ nhiều nơi — bạn cần biết **nơi gọi nào** mới là thủ phạm.\n\nChi phí: `tracemalloc` làm chương trình chậm khoảng hai tới ba lần và tốn thêm bộ nhớ. Nó là công cụ **chẩn đoán**, không bật trong sản phẩm.\n\n### Đo bộ nhớ tiến trình\n\n```python\nimport resource, sys\n\ndef bo_nho_tien_trinh_mb() -> float:\n    \"\"\"Bộ nhớ thường trú, nhìn từ hệ điều hành.\"\"\"\n    r = resource.getrusage(resource.RUSAGE_SELF).ru_maxrss\n    return r / 1024 if sys.platform == \"linux\" else r / 1024 / 1024\n```\n\nĐơn vị `ru_maxrss` **khác nhau giữa các hệ điều hành** — kilobyte trên Linux, byte trên macOS. Đây là chi tiết hay gây sai số 1024 lần.\n\nMinh hoạ vì sao hai con số lệch nhau:\n\n```python\nimport gc, tracemalloc\n\ntracemalloc.start()\ntruoc_py, _ = tracemalloc.get_traced_memory()\ntruoc_os = bo_nho_tien_trinh_mb()\n\nds = [object() for _ in range(2_000_000)]\ngiua_py, _ = tracemalloc.get_traced_memory()\ngiua_os = bo_nho_tien_trinh_mb()\n\ndel ds\ngc.collect()\nsau_py, _ = tracemalloc.get_traced_memory()\nsau_os = bo_nho_tien_trinh_mb()\n\nprint(f\"          Python      Tiến trình\")\nprint(f\"trước   {truoc_py/1e6:>8.1f} MB {truoc_os:>10.1f} MB\")\nprint(f\"sau cấp {giua_py/1e6:>8.1f} MB {giua_os:>10.1f} MB\")\nprint(f\"sau dọn {sau_py/1e6:>8.1f} MB {sau_os:>10.1f} MB\")\n# Python: giảm về gần mức ban đầu ✓\n# Tiến trình: thường KHÔNG giảm — giữ lại để tái dùng\n```\n\nHai dòng cuối là bài học chính: **Python đã dọn, hệ điều hành chưa nhận lại.** Kết luận \"rò rỉ\" từ con số bên phải là sai.\n\n### Kỹ thuật ① — generator thay list\n\nĐây là kỹ thuật có tác dụng lớn nhất, và thường dễ áp dụng nhất:\n\n```python\nimport tracemalloc\n\ntracemalloc.start()\n\n# ❌ Dựng cả list\ntong = sum([x * x for x in range(2_000_000)])\n_, dinh_list = tracemalloc.get_traced_memory()\ntracemalloc.reset_peak()\n\n# ✓ Generator\ntong = sum(x * x for x in range(2_000_000))\n_, dinh_gen = tracemalloc.get_traced_memory()\n\nprint(f\"list      : {dinh_list/1024/1024:>7.1f} MB\")\nprint(f\"generator : {dinh_gen/1024/1024:>7.1f} MB\")\ntracemalloc.stop()\n```\n\nChênh lệch thường là hàng chục megabyte cho một dòng code — tỉ lệ lợi ích trên công sức cao nhất trong bài.\n\nÁp dụng cho đường ống xử lý file, đúng mẫu ở module 5:\n\n```python\n# ❌ Nạp cả file\ndong = open(\"lon.txt\", encoding=\"utf-8\").readlines()\nloi = [d for d in dong if \"ERROR\" in d]\nthoi_gian = [d.split()[0] for d in loi]\n\n# ✓ Đường ống lười — bộ nhớ không đổi theo kích thước file\nwith open(\"lon.txt\", encoding=\"utf-8\") as f:\n    dong = (d.rstrip(\"\\n\") for d in f)\n    loi = (d for d in dong if \"ERROR\" in d)\n    thoi_gian = (d.split()[0] for d in loi)\n    from collections import Counter\n    print(Counter(thoi_gian).most_common(5))\n```\n\nĐánh đổi đã nói ở module 5: **generator dùng một lần, không có `len()`, không có chỉ số.** Chỉ đổi khi bạn thật sự chỉ duyệt một lượt.\n\n### Kỹ thuật ② — `__slots__`\n\n```python\nimport sys\n\nclass Thuong:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\nclass CoSlots:\n    __slots__ = (\"x\", \"y\")\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\na, b = Thuong(1, 2), CoSlots(1, 2)\nprint(sys.getsizeof(a) + sys.getsizeof(vars(a)))    # ~152 byte\nprint(sys.getsizeof(b))                              # ~56 byte\n```\n\nCơ chế: object thường có một `__dict__` riêng để lưu thuộc tính. `__slots__` bỏ `__dict__` đó và lưu thuộc tính vào các ô cố định — giống cách biến cục bộ được lưu, đúng như bài một.\n\n```\nObject thường:                 Object có __slots__:\n┌────────────┐                 ┌────────────┐\n│ refcnt,type│                 │ refcnt,type│\n│ __dict__ ──┼──► dict riêng   │ ô x        │\n└────────────┘    (nặng)       │ ô y        │\n                               └────────────┘\n```\n\nLợi ích thứ hai đã nói ở module 6: **bắt lỗi gõ sai tên thuộc tính.**\n\n```python\nb.z = 3        # AttributeError ✓\na.z = 3        # ✓ không lỗi — thuộc tính mới, lỗi im lặng\n```\n\nBa cái giá:\n\n```\n① Không thêm thuộc tính lúc chạy\n② Không có __dict__ ⇒ vài thư viện dựa vào nó sẽ không hoạt động\n③ Kế thừa phức tạp hơn — lớp con không khai báo __slots__ thì __dict__ quay lại\n```\n\nĐiểm ③ là bẫy tinh vi:\n\n```python\nclass Cha:\n    __slots__ = (\"x\",)\n\nclass Con(Cha):\n    pass                    # ⚠️ không khai báo ⇒ Con CÓ __dict__ trở lại\n\nc = Con()\nc.bat_ky = 1                # ✓ chạy được — mất hết lợi ích\n\nclass ConTot(Cha):\n    __slots__ = (\"y\",)      # ⭐ khai báo, kể cả rỗng ()\n```\n\nKhi nào đáng dùng: **hàng trăm nghìn object trở lên của cùng một class.** Dưới ngưỡng đó, lợi ích không bù được mất linh hoạt.\n\nVới `dataclass` thì chỉ cần một tham số:\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass(slots=True)\nclass Diem:\n    x: float\n    y: float\n```\n\n### Kỹ thuật ③ — mảng liền khối\n\n```python\nimport sys, array\n\nn = 1_000_000\n\nds = list(range(n))\na = array.array(\"i\", range(n))\n\nprint(f\"list (chỉ container): {sys.getsizeof(ds)/1024/1024:>6.1f} MB\")\nprint(f\"list (tính cả int)  : {kich_thuoc_sau(ds)/1024/1024:>6.1f} MB\")\nprint(f\"array.array('i')    : {sys.getsizeof(a)/1024/1024:>6.1f} MB\")\n```\n\nChênh lệch gần mười lần, và lý do đã có ở bài một: `list` lưu con trỏ tới object riêng lẻ, `array` lưu giá trị liền nhau.\n\nMã kiểu quyết định kích thước mỗi phần tử:\n\n```python\nimport array\n\n# 'b' 1 byte  · 'h' 2 byte  · 'i' 4 byte  · 'q' 8 byte  · 'd' 8 byte (float)\nfor ma in \"bhiqd\":\n    a = array.array(ma, [1] * 1000)\n    print(f\"'{ma}': {a.itemsize} byte/phần tử, tổng {sys.getsizeof(a):,}\")\n```\n\nChọn mã nhỏ nhất đủ dùng là tối ưu miễn phí: dữ liệu 0–255 lưu bằng `'B'` tốn một phần tám so với `'q'`.\n\nVới tính toán số thì NumPy tốt hơn nhiều — nó vừa liền khối vừa có phép toán trên cả mảng chạy trong C, đúng như bài GIL đã nói.\n\nCòn `bytes` và `bytearray` cho dữ liệu nhị phân:\n\n```python\nb = bytes(1_000_000)              # 1 MB — bất biến\nba = bytearray(1_000_000)         # 1 MB — sửa được\n\n# memoryview — xem một phần mà KHÔNG sao chép\nmv = memoryview(ba)\nphan = mv[1000:2000]              # ⭐ không cấp phát byte nào\nphan[0] = 65                       # sửa được, ảnh hưởng ba gốc\n```\n\n`memoryview` là công cụ quan trọng khi xử lý dữ liệu nhị phân lớn: cắt một `bytes` thường **sao chép**, còn `memoryview` thì không.\n\n```python\nimport time\n\nb = bytes(50_000_000)\n\nt = time.perf_counter()\nfor i in range(100):\n    x = b[i*1000 : i*1000 + 10_000]       # sao chép 10 KB mỗi lần\nprint(f\"slice bytes     : {time.perf_counter()-t:.4f}s\")\n\nmv = memoryview(b)\nt = time.perf_counter()\nfor i in range(100):\n    x = mv[i*1000 : i*1000 + 10_000]      # ⭐ không sao chép\nprint(f\"slice memoryview: {time.perf_counter()-t:.4f}s\")\n```\n\n### Kỹ thuật ④ — gộp chuỗi lặp\n\nĐã giới thiệu ở bài một, giờ là con số:\n\n```python\nimport sys, random\n\nLOAI = [\"Hà Nội\", \"Hồ Chí Minh\", \"Đà Nẵng\", \"Cần Thơ\"]\nn = 500_000\n\n# Mô phỏng đọc từ file — mỗi dòng tạo chuỗi MỚI\nkhong_gop = [\"\".join(random.choice(LOAI)) for _ in range(n)]\nco_gop = [sys.intern(s) for s in khong_gop]\n\nprint(f\"object chuỗi khác nhau, không gộp: {len({id(s) for s in khong_gop}):,}\")\nprint(f\"object chuỗi khác nhau, có gộp   : {len({id(s) for s in co_gop}):,}\")\n```\n\nVới cột phân loại lặp lại nhiều, gộp giảm từ hàng trăm nghìn object xuống vài object.\n\nLựa chọn tốt hơn khi có thể: **thay chuỗi bằng số hoặc `enum`**.\n\n```python\nfrom enum import IntEnum\n\nclass ThanhPho(IntEnum):\n    HA_NOI = 0\n    HCM = 1\n    DA_NANG = 2\n\n# Lưu số thay vì chuỗi — nhỏ hơn nhiều, so sánh nhanh hơn\nimport array\nma_thanh_pho = array.array(\"B\", [0, 1, 2, 0, 1])     # 1 byte mỗi giá trị\n```\n\nĐây là kỹ thuật nền của các thư viện xử lý dữ liệu bảng: cột phân loại được lưu thành mã số cộng một bảng tra.\n\n### Cấu trúc dữ liệu — chọn đúng\n\n```python\nimport sys\n\nn = 100_000\nds = list(range(n))\ntp = tuple(range(n))\nst = set(range(n))\ndc = {i: i for i in range(n)}\n\nfor ten, o in ((\"list\", ds), (\"tuple\", tp), (\"set\", st), (\"dict\", dc)):\n    print(f\"{ten:<6} {sys.getsizeof(o)/1024:>8.1f} KB\")\n```\n\nThứ tự điển hình: `tuple` < `list` << `set` < `dict`.\n\nNhưng **đừng chọn theo bảng này một mình** — nối về bài `list`/`dict`/`set` ở module 5:\n\n```\nCần tra cứu nhiều lần    → set/dict, dù tốn bộ nhớ (O(1) đáng giá)\nChỉ duyệt một lượt        → generator, không cần cấu trúc nào\nCố định, chỉ đọc          → tuple\nDữ liệu số thuần          → array/NumPy\n```\n\nMột kỹ thuật ít biết cho `dict` nhiều bản ghi giống nhau:\n\n```python\n# ❌ Mỗi dict lưu riêng khoá\nban_ghi = [{\"ten\": ..., \"tuoi\": ..., \"email\": ...} for _ in range(500_000)]\n\n# ✓ NamedTuple — không lưu tên trường ở mỗi bản ghi\nfrom typing import NamedTuple\n\nclass BanGhi(NamedTuple):\n    ten: str\n    tuoi: int\n    email: str\n\nban_ghi = [BanGhi(...) for _ in range(500_000)]     # nhẹ hơn nhiều\n```\n\nHoặc `dataclass(slots=True)` khi cần mutable. Với nửa triệu bản ghi, chênh lệch thường là hàng trăm megabyte.\n\n### Xử lý dữ liệu lớn hơn RAM\n\nKhi tối ưu không đủ, đổi cách tiếp cận:\n\n```python\n# ① Xử lý theo dòng — bộ nhớ không đổi theo kích thước file\ndef dem_loi(duong_dan: str) -> int:\n    n = 0\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        for dong in f:\n            if \"ERROR\" in dong:\n                n += 1\n    return n\n\n# ② Xử lý theo lô — khi cần gom nhóm\ndef theo_lo(nguon, kich_thuoc=10_000):\n    lo = []\n    for x in nguon:\n        lo.append(x)\n        if len(lo) == kich_thuoc:\n            yield lo\n            lo = []              # ⭐ list MỚI, không phải clear()\n    if lo:\n        yield lo\n\n# ③ Chỉ giữ thứ cần — không giữ cả bản ghi\nfrom collections import Counter\ndem = Counter()\nwith open(\"lon.jsonl\", encoding=\"utf-8\") as f:\n    import json\n    for dong in f:\n        bg = json.loads(dong)\n        dem[bg[\"loai\"]] += 1      # ⭐ chỉ giữ bảng đếm, không giữ bg\n```\n\nKỹ thuật ③ đáng nhấn: rất nhiều chương trình nạp cả dataset chỉ để tính vài con số thống kê. Chỉ giữ **kết quả tổng hợp** thì bộ nhớ không phụ thuộc kích thước dữ liệu.\n\n### Quy trình tối ưu\n\n```\n① ĐO — tracemalloc, tìm dòng cấp phát nhiều nhất\n② HỎI — chỗ tốn nhất có ĐÁNG tối ưu không? Chiếm bao nhiêu phần trăm?\n③ SỬA — một thay đổi mỗi lần\n④ ĐO LẠI — xác nhận có tác dụng thật\n⑤ CÂN NHẮC — code có xấu đi nhiều không so với lợi ích?\n```\n\nBước ② quan trọng nhất và hay bị bỏ: tối ưu một chỗ chiếm 2% bộ nhớ thì tốt nhất cũng chỉ giảm 2%.\n\nThứ tự thử, theo tỉ lệ lợi ích trên công sức:\n\n```\n① Rò rỉ — cache không giới hạn, danh sách tích luỹ   ⭐ thường lớn nhất\n② Generator thay list                                 ⭐ dễ, hiệu quả\n③ Cấu trúc dữ liệu đúng (array, NamedTuple, slots)\n④ Gộp chuỗi, mã hoá phân loại thành số\n⑤ Xử lý theo lô, chỉ giữ kết quả tổng hợp\n```\n\nMục ① nằm đầu vì nó thường **chiếm phần lớn** vấn đề — đúng như tình huống ở phần *vì sao cần*. Kiểm tra nó trước khi làm bất cứ điều gì khác.",

vi_du: "### Vì sao `getsizeof` một mình gây hiểu nhầm\n\n```python\nimport sys\n\nnho = [1, 2, 3]\nlon = [\"x\" * 100_000, \"y\" * 100_000, \"z\" * 100_000]\n\nprint(f\"list 3 số nhỏ  : {sys.getsizeof(nho):>8,} byte\")\nprint(f\"list 3 chuỗi to: {sys.getsizeof(lon):>8,} byte\")\n# GẦN BẰNG NHAU ⚠️ — cả hai chỉ có 3 con trỏ\n\nprint(f\"đo đúng, nhỏ   : {kich_thuoc_sau(nho):>8,} byte\")\nprint(f\"đo đúng, lớn   : {kich_thuoc_sau(lon):>8,} byte\")   # ~300 KB\n```\n\nKết luận từ `getsizeof` một mình sẽ là \"hai list tốn như nhau\" — sai hoàn toàn.\n\nVà bẫy khử trùng:\n\n```python\nimport sys\n\ndung_chung = [0] * 1000              # 1000 ô, nhưng CÙNG một object 0\n\nsai = sys.getsizeof(dung_chung) + sum(sys.getsizeof(x) for x in dung_chung)\ndung = kich_thuoc_sau(dung_chung)\n\nprint(f\"đếm trùng : {sai:>8,} byte\")     # phồng lên ~28,000 byte thừa\nprint(f\"đếm đúng  : {dung:>8,} byte\")\n```\n\nCache số nguyên nhỏ ở bài một khiến 1000 ô cùng trỏ một object — cộng `getsizeof` từng phần tử là đếm nó 1000 lần.\n\n### `tracemalloc` — tìm dòng tốn nhất\n\n```python\nimport tracemalloc\n\ndef nap_du_lieu():\n    return [{\"id\": i, \"ten\": f\"muc-{i}\", \"the\": [i, i+1, i+2]}\n            for i in range(200_000)]\n\ndef nap_chuoi():\n    return [\"x\" * 500 for _ in range(50_000)]\n\ndef nap_nho():\n    return list(range(1000))\n\n\ntracemalloc.start()\n\na = nap_du_lieu()\nb = nap_chuoi()\nc = nap_nho()\n\nanh = tracemalloc.take_snapshot()\ntong = sum(tk.size for tk in anh.statistics(\"lineno\"))\n\nprint(f\"tổng: {tong/1024/1024:.1f} MB\\n\")\nprint(f\"{'KB':>10}  {'%':>5}  {'khối':>8}  vị trí\")\nfor tk in anh.statistics(\"lineno\")[:5]:\n    print(f\"{tk.size/1024:>10.1f}  {tk.size/tong:>5.0%}  {tk.count:>8,}  \"\n          f\"{tk.traceback[0].filename.split('/')[-1]}:{tk.traceback[0].lineno}\")\n\ntracemalloc.stop()\n```\n\nCột phần trăm là cột quan trọng nhất: nó trả lời câu \"chỗ này có đáng tối ưu không\".\n\n### Tìm rò rỉ bằng so sánh ảnh chụp\n\n```python\nimport tracemalloc, gc\n\nCACHE = {}          # ⚠️ rò rỉ cố ý\nLICH_SU = []        # ⚠️ rò rỉ cố ý\n\ndef xu_ly_yeu_cau(i: int):\n    kq = {\"id\": i, \"du_lieu\": \"x\" * 1000}\n    CACHE[i] = kq                     # không bao giờ dọn\n    LICH_SU.append(kq)                # cũng không\n    return kq\n\n\ntracemalloc.start()\ngc.collect()\nnen = tracemalloc.take_snapshot()\n\nfor i in range(5_000):\n    xu_ly_yeu_cau(i)\n\ngc.collect()\nsau = tracemalloc.take_snapshot()\n\nprint(\"Tăng nhiều nhất:\")\nfor tk in sau.compare_to(nen, \"lineno\")[:5]:\n    if tk.size_diff > 0:\n        v = tk.traceback[0]\n        print(f\"  +{tk.size_diff/1024:>8.1f} KB  \"\n              f\"{v.filename.split('/')[-1]}:{v.lineno}\")\n\ntracemalloc.stop()\nprint(f\"\\nCACHE: {len(CACHE):,} mục · LICH_SU: {len(LICH_SU):,} mục\")\n```\n\nBản sửa:\n\n```python\nfrom functools import lru_cache\nfrom collections import deque\n\nLICH_SU = deque(maxlen=1000)          # ⭐ có trần\n\n@lru_cache(maxsize=1000)              # ⭐ có trần\ndef xu_ly_yeu_cau(i: int):\n    return {\"id\": i, \"du_lieu\": \"x\" * 1000}\n```\n\nHai dòng `maxlen` và `maxsize` giải quyết vấn đề mà mọi kỹ thuật tối ưu khác cộng lại không làm được — đúng như tình huống mở đầu bài.\n\n### Đỉnh bộ nhớ quan trọng hơn mức trung bình\n\n```python\nimport tracemalloc\n\ntracemalloc.start()\n\n# Cách 1 — dựng list trung gian\ntracemalloc.reset_peak()\ntam = [x * x for x in range(2_000_000)]\ntong1 = sum(tam)\ndel tam\n_, dinh1 = tracemalloc.get_traced_memory()\n\n# Cách 2 — generator\ntracemalloc.reset_peak()\ntong2 = sum(x * x for x in range(2_000_000))\n_, dinh2 = tracemalloc.get_traced_memory()\n\nprint(f\"đỉnh với list      : {dinh1/1024/1024:>7.1f} MB\")\nprint(f\"đỉnh với generator : {dinh2/1024/1024:>7.1f} MB\")\nprint(f\"kết quả giống nhau : {tong1 == tong2}\")\n\ntracemalloc.stop()\n```\n\nCả hai cách kết thúc với cùng lượng bộ nhớ (một số nguyên), nhưng **đỉnh** khác nhau hàng chục megabyte. Chương trình bị hệ điều hành giết vì đỉnh, không vì mức cuối.\n\n`reset_peak()` là hàm quan trọng khi đo nhiều đoạn liên tiếp — không có nó, đỉnh cũ vẫn được giữ.\n\n### `__slots__` — đo trên quy mô thật\n\n```python\nimport tracemalloc\nfrom dataclasses import dataclass\n\nclass Thuong:\n    def __init__(self, x, y, z):\n        self.x, self.y, self.z = x, y, z\n\nclass CoSlots:\n    __slots__ = (\"x\", \"y\", \"z\")\n    def __init__(self, x, y, z):\n        self.x, self.y, self.z = x, y, z\n\n@dataclass(slots=True)\nclass DC:\n    x: int\n    y: int\n    z: int\n\nfrom typing import NamedTuple\nclass NT(NamedTuple):\n    x: int\n    y: int\n    z: int\n\n\nN = 300_000\n\ndef do(ten, tao):\n    tracemalloc.start()\n    tracemalloc.reset_peak()\n    ds = [tao(i, i, i) for i in range(N)]\n    hien_tai, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n    print(f\"{ten:<20} {hien_tai/1024/1024:>7.1f} MB \"\n          f\"({hien_tai/N:>5.0f} byte/object)\")\n    del ds\n\ndo(\"class thường\", Thuong)\ndo(\"class __slots__\", CoSlots)\ndo(\"dataclass slots\", DC)\ndo(\"NamedTuple\", NT)\n```\n\nThứ tự điển hình: class thường nặng nhất, `NamedTuple` nhẹ nhất. Với 300.000 object, chênh lệch thường là hàng chục megabyte.\n\nBẫy kế thừa:\n\n```python\nimport sys\n\nclass Cha:\n    __slots__ = (\"x\",)\n\nclass ConXau(Cha):\n    pass                        # ⚠️ __dict__ quay lại\n\nclass ConTot(Cha):\n    __slots__ = ()              # ⭐ khai báo rỗng cũng đủ\n\ncx, ct = ConXau(), ConTot()\ncx.bat_ky = 1                   # ✓ chạy được — mất lợi ích\nprint(f\"ConXau có __dict__: {hasattr(cx, '__dict__')}\")     # True ⚠️\nprint(f\"ConTot có __dict__: {hasattr(ct, '__dict__')}\")     # False ✓\n\ntry:\n    ct.bat_ky = 1\nexcept AttributeError as e:\n    print(f\"✓ {e}\")\n```\n\nQuên `__slots__ = ()` ở lớp con là mất toàn bộ lợi ích mà không có cảnh báo nào.\n\n### Mảng liền khối\n\n```python\nimport sys, array, time\n\nn = 2_000_000\n\nds = list(range(n))\na_int = array.array(\"i\", range(n))\na_byte = array.array(\"B\", [i % 256 for i in range(n)])\n\nprint(f\"list        : {kich_thuoc_sau(ds)/1024/1024:>7.1f} MB\")\nprint(f\"array 'i'   : {sys.getsizeof(a_int)/1024/1024:>7.1f} MB\")\nprint(f\"array 'B'   : {sys.getsizeof(a_byte)/1024/1024:>7.1f} MB\")\n\n# Tốc độ duyệt — liền khối thân thiện cache CPU\nt = time.perf_counter(); sum(ds)\ntg_list = time.perf_counter() - t\n\nt = time.perf_counter(); sum(a_int)\ntg_array = time.perf_counter() - t\n\nprint(f\"\\nsum(list) : {tg_list:.3f}s\")\nprint(f\"sum(array): {tg_array:.3f}s\")\n```\n\nChọn mã kiểu nhỏ nhất đủ dùng là tối ưu miễn phí — dữ liệu 0–255 dùng `'B'` tốn một phần tư so với `'i'`.\n\n`memoryview` cho phép cắt mà không sao chép:\n\n```python\nimport time\n\nb = bytes(30_000_000)\nmv = memoryview(b)\n\nt = time.perf_counter()\nfor i in range(200):\n    _ = b[i*1000 : i*1000 + 50_000]        # sao chép 50 KB mỗi lần\nprint(f\"slice bytes     : {time.perf_counter()-t:.4f}s\")\n\nt = time.perf_counter()\nfor i in range(200):\n    _ = mv[i*1000 : i*1000 + 50_000]       # ⭐ không sao chép\nprint(f\"slice memoryview: {time.perf_counter()-t:.4f}s\")\n```\n\nRất hữu ích khi phân tích gói tin, đọc định dạng nhị phân, hoặc xử lý ảnh — những chỗ mà sao chép lặp lại là nút cổ chai.\n\n### Bản ghi: `dict` và các lựa chọn khác\n\n```python\nimport tracemalloc\nfrom typing import NamedTuple\nfrom dataclasses import dataclass\n\nN = 300_000\n\nclass BanGhiNT(NamedTuple):\n    ten: str\n    tuoi: int\n    email: str\n\n@dataclass(slots=True)\nclass BanGhiDC:\n    ten: str\n    tuoi: int\n    email: str\n\n\ndef do(ten, tao):\n    tracemalloc.start(); tracemalloc.reset_peak()\n    ds = [tao(i) for i in range(N)]\n    m, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n    print(f\"{ten:<22} {m/1024/1024:>7.1f} MB\")\n    del ds\n\ndo(\"dict\", lambda i: {\"ten\": f\"n{i}\", \"tuoi\": i, \"email\": f\"e{i}@x.com\"})\ndo(\"tuple\", lambda i: (f\"n{i}\", i, f\"e{i}@x.com\"))\ndo(\"NamedTuple\", lambda i: BanGhiNT(f\"n{i}\", i, f\"e{i}@x.com\"))\ndo(\"dataclass slots\", lambda i: BanGhiDC(f\"n{i}\", i, f\"e{i}@x.com\"))\n```\n\n`dict` nặng nhất vì **mỗi bản ghi lưu lại tên khoá**. `NamedTuple` và `dataclass(slots=True)` lưu tên trường ở class, không ở từng instance.\n\nVới nửa triệu bản ghi, đổi từ `dict` sang `NamedTuple` thường tiết kiệm hàng trăm megabyte — và code đọc rõ hơn, đúng như bài dataclass ở module 6.\n\n### Chỉ giữ kết quả tổng hợp\n\n```python\nimport json, tracemalloc\nfrom collections import Counter\n\n# ❌ Nạp hết rồi mới tính\ndef thong_ke_xau(duong_dan):\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        bg = [json.loads(d) for d in f]        # ⚠️ cả file trong RAM\n    return {\n        \"tong\": len(bg),\n        \"theo_loai\": Counter(b[\"loai\"] for b in bg),\n        \"tong_tien\": sum(b[\"tien\"] for b in bg),\n    }\n\n# ✓ Duyệt một lượt, chỉ giữ số liệu\ndef thong_ke_tot(duong_dan):\n    tong = tong_tien = 0\n    theo_loai = Counter()\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        for dong in f:\n            b = json.loads(dong)\n            tong += 1\n            tong_tien += b[\"tien\"]\n            theo_loai[b[\"loai\"]] += 1\n            # ⭐ b bị thu gom ngay sau vòng lặp\n    return {\"tong\": tong, \"theo_loai\": theo_loai, \"tong_tien\": tong_tien}\n```\n\nBản thứ hai dùng bộ nhớ **không phụ thuộc kích thước file** — chạy được với file 100 GB trên máy 8 GB RAM.\n\nĐây là kỹ thuật quan trọng nhất khi dữ liệu lớn hơn RAM, và nó thường bị bỏ qua vì bản đầu \"trông sạch hơn\".\n\n### Xử lý theo lô\n\n```python\ndef theo_lo(nguon, kich_thuoc: int = 10_000):\n    lo = []\n    for x in nguon:\n        lo.append(x)\n        if len(lo) == kich_thuoc:\n            yield lo\n            lo = []                    # ⭐ list MỚI, không phải lo.clear()\n    if lo:\n        yield lo                       # ⭐ đừng quên lô cuối\n\n\ndef nap_vao_db(duong_dan, conn):\n    import json\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        ban_ghi = (json.loads(d) for d in f)          # generator\n        for lo in theo_lo(ban_ghi, 5_000):\n            conn.executemany(\"INSERT INTO t VALUES (?,?)\",\n                             [(b[\"id\"], b[\"ten\"]) for b in lo])\n            conn.commit()\n            # ⭐ lô cũ được thu gom trước khi đọc lô mới\n```\n\nHai chi tiết đã nói ở module 5, nhắc lại vì chúng gây lỗi thật: `lo = []` chứ không `lo.clear()` (người nhận lô đang giữ list đó), và `if lo` cho lô cuối.\n\nLô lớn hơn thì ít lần ghi database hơn nhưng tốn bộ nhớ hơn — chọn kích thước là một phép cân bằng, và nên đo thay vì đoán.\n\n### Quy trình tối ưu đầy đủ\n\n```python\nimport tracemalloc, gc\nfrom contextlib import contextmanager\n\n@contextmanager\ndef do_bo_nho(ten: str):\n    \"\"\"Đo bộ nhớ một đoạn code.\"\"\"\n    gc.collect()\n    tracemalloc.start()\n    tracemalloc.reset_peak()\n    truoc, _ = tracemalloc.get_traced_memory()\n    try:\n        yield\n    finally:\n        hien_tai, dinh = tracemalloc.get_traced_memory()\n        tracemalloc.stop()\n        giu = (hien_tai - truoc) / 1024 / 1024\n        print(f\"{ten:<28} giữ lại={giu:>7.1f} MB  \"\n              f\"đỉnh={dinh/1024/1024:>7.1f} MB\")\n\n\n# So sánh ba cách làm cùng một việc\nN = 1_000_000\n\nwith do_bo_nho(\"list comprehension\"):\n    ds = [x * x for x in range(N)]\n    tong = sum(ds)\n    del ds\n\nwith do_bo_nho(\"generator\"):\n    tong = sum(x * x for x in range(N))\n\nwith do_bo_nho(\"array.array\"):\n    import array\n    a = array.array(\"q\", (x * x for x in range(N)))\n    tong = sum(a)\n    del a\n```\n\nContext manager này đáng giữ lại làm công cụ: nó in **cả bộ nhớ giữ lại lẫn đỉnh**, hai con số cần cho hai loại quyết định khác nhau.\n\nDùng nó theo quy trình:\n\n```python\n# ① Đo hiện trạng\nwith do_bo_nho(\"trước tối ưu\"):\n    kq = ham_can_toi_uu()\n\n# ② Sửa MỘT thứ\n# ③ Đo lại\nwith do_bo_nho(\"sau tối ưu\"):\n    kq = ham_da_sua()\n\n# ④ So sánh — có đáng với độ phức tạp thêm vào không?\n```\n\n### Khi tối ưu không đáng\n\n```python\n# ❌ Tối ưu vi mô cho dữ liệu nhỏ\ncau_hinh = tuple(danh_sach)       # tiết kiệm vài chục byte\n                                   # trong khi cache chiếm 2 GB\n\n# ❌ __slots__ cho class có 5 instance\nclass CauHinh:\n    __slots__ = (\"a\", \"b\", \"c\")   # mất linh hoạt, được ~500 byte\n\n# ❌ Generator cho dữ liệu cần duyệt nhiều lần\ndu_lieu = (x for x in nguon)\nprint(len(du_lieu))                # TypeError\nfor x in du_lieu: ...\nfor x in du_lieu: ...              # rỗng — lỗi im lặng\n```\n\nBa câu hỏi trước khi tối ưu:\n\n```\n① Chỗ này chiếm bao nhiêu PHẦN TRĂM tổng bộ nhớ?\n   < 10% → gần như chắc chắn không đáng\n\n② Có RÒ RỈ nào chưa xử lý không?\n   có → sửa rò rỉ trước, thường giải quyết luôn vấn đề\n\n③ Code xấu đi bao nhiêu so với lợi ích?\n   nhiều → cân nhắc lại\n```\n\nCâu ② là câu quan trọng nhất, và nó nhắc lại tình huống mở đầu bài: **kiểm tra rò rỉ trước khi tối ưu bất cứ thứ gì.**",

so_sanh: "### Hai loại công cụ đo\n\n| | `tracemalloc`, `getsizeof` | Công cụ hệ điều hành |\n|---|---|---|\n| Trả lời câu hỏi | \"Python cấp phát bao nhiêu\" | \"tiến trình chiếm bao nhiêu\" |\n| Thấy được dòng code | **✓** | ✗ |\n| Bao gồm trình thông dịch | ✗ | ✓ |\n| Giảm sau khi dọn | **✓** | thường không |\n| Chi phí | chậm 2–3 lần | không |\n| Dùng để | **tìm nguyên nhân** | theo dõi tổng thể |\n\nNhầm hai cột này là nguồn của kết luận \"rò rỉ\" sai phổ biến nhất.\n\n### `getsizeof` và đo đệ quy\n\n| | `sys.getsizeof` | Hàm đo đệ quy |\n|---|---|---|\n| Tính nội dung container | **✗** | ✓ |\n| Khử object trùng | — | **✓ theo `id`** |\n| Xử lý tham chiếu vòng | — | ✓ |\n| Tính `__dict__` của object | ✗ | ✓ |\n| Chính xác tuyệt đối | ✗ | ✗ (vẫn gần đúng) |\n\nCả hai đều chỉ **gần đúng** — chúng không tính phần bộ cấp phát dự trữ. Đo nghiêm túc thì dùng `tracemalloc`.\n\n### Bốn kỹ thuật tối ưu\n\n| Kỹ thuật | Tiết kiệm | Công sức | Đánh đổi |\n|---|---|---|---|\n| **Generator thay list** | **rất lớn** | **thấp** | mất `len`, lặp một lần |\n| `__slots__` | lớn nếu nhiều object | thấp | mất linh hoạt, kế thừa phức tạp |\n| `array`/NumPy | **rất lớn** với số | vừa | một kiểu duy nhất |\n| Gộp chuỗi lặp | lớn với dữ liệu phân loại | thấp | chuỗi không được giải phóng |\n\nHàng đầu có tỉ lệ lợi ích trên công sức cao nhất — nên thử trước.\n\n### Kiểu lưu bản ghi\n\n| | `dict` | `tuple` | `NamedTuple` | `dataclass(slots)` |\n|---|---|---|---|---|\n| Bộ nhớ | **nặng nhất** | **nhẹ nhất** | nhẹ | nhẹ |\n| Lưu tên trường mỗi bản ghi | **✓** | ✗ | ✗ (ở class) | ✗ (ở class) |\n| Truy cập theo tên | ✓ | ✗ | ✓ | ✓ |\n| Mutable | ✓ | ✗ | ✗ | **✓** |\n| Hashable | ✗ | ✓ | ✓ | chỉ khi `frozen` |\n\nVới hàng trăm nghìn bản ghi, đổi `dict` sang `NamedTuple` thường tiết kiệm hàng trăm megabyte — và code rõ hơn.\n\n### Cấu trúc cho dữ liệu số\n\n| | `list` | `array.array` | NumPy |\n|---|---|---|---|\n| Lưu | con trỏ | **giá trị** | giá trị |\n| Bộ nhớ 1 triệu int | ~36 MB | **~4 MB** | ~4 MB |\n| Kiểu trộn lẫn | ✓ | ✗ | ✗ |\n| Phép toán trên cả mảng | ✗ | ✗ | **✓** |\n| Nhả GIL khi tính | — | — | **✓** |\n| Thư viện ngoài | không cần | không cần | cần cài |\n\n`array.array` là lựa chọn tốt khi chỉ cần lưu trữ tiết kiệm và không muốn thêm phụ thuộc.\n\n### Chỉ số nào quan trọng\n\n| Chỉ số | Ý nghĩa | Khi nào quan tâm |\n|---|---|---|\n| Bộ nhớ **giữ lại** | còn chiếm sau khi xong | rò rỉ, tiến trình chạy lâu |\n| **Đỉnh** | cao nhất từng chạm | **bị hệ điều hành giết** |\n| Số khối cấp phát | số lần cấp | chi phí cấp phát |\n| RSS tiến trình | hệ điều hành thấy | theo dõi vận hành |\n\nĐỉnh thường quan trọng hơn mức cuối: chương trình chết vì chạm đỉnh, không vì mức trung bình.\n\n### Thứ tự thử khi tối ưu\n\n| Thứ tự | Việc | Vì sao trước |\n|---|---|---|\n| ① | **Tìm rò rỉ** (cache, danh sách tích luỹ) | thường chiếm phần lớn vấn đề |\n| ② | Generator thay list | dễ, hiệu quả cao |\n| ③ | Cấu trúc đúng (`array`, `NamedTuple`, `slots`) | hiệu quả với số lượng lớn |\n| ④ | Gộp chuỗi, mã hoá phân loại | hiệu quả với dữ liệu lặp |\n| ⑤ | Xử lý theo lô, giữ kết quả tổng hợp | khi dữ liệu > RAM |\n\nBước ① đứng đầu vì nó thường giải quyết luôn vấn đề — làm bước ②–⑤ trước là phí công.\n\n### Khi nào KHÔNG tối ưu\n\n| Tình huống | Lý do |\n|---|---|\n| Chỗ đó chiếm < 10% tổng | tốt nhất cũng chỉ giảm 10% |\n| Chưa đo | **trực giác gần như luôn sai** |\n| Chưa kiểm tra rò rỉ | sửa rò rỉ thường giải quyết luôn |\n| Dữ liệu nhỏ, cố định | không có vấn đề để giải |\n| Code xấu đi nhiều | đánh đổi không đáng |",

loi_thuong_gap: [
  "**Dùng `sys.getsizeof` một mình rồi kết luận.** Nó chỉ đo container; list chứa chuỗi lớn và list chứa số nhỏ cho con số gần bằng nhau.",
  "**Cộng `getsizeof` từng phần tử mà không khử trùng.** Nhiều ô cùng trỏ một object; con số phồng lên nhiều lần.",
  "**Lặp qua `str` trong hàm đo đệ quy.** `str` là iterable — sẽ đếm từng ký tự.",
  "**Coi \"RSS không giảm\" là rò rỉ.** CPython giữ lại khối để tái dùng; đo bằng `tracemalloc`.",
  "**Tối ưu trước khi đo.** Trực giác về chỗ tốn bộ nhớ gần như luôn sai.",
  "**Tối ưu chỗ chiếm 2% tổng.** Tốt nhất cũng chỉ giảm 2%. Xem phần trăm trước.",
  "**Bỏ qua rò rỉ rồi đi tối ưu vi mô.** Một dòng `maxsize` thường hiệu quả hơn cả ngày tối ưu.",
  "**Quên `__slots__ = ()` ở lớp con.** `__dict__` quay lại và mất toàn bộ lợi ích, không có cảnh báo nào.",
  "**Dùng `__slots__` cho class chỉ có vài instance.** Mất linh hoạt mà tiết kiệm không đáng kể.",
  "**Đổi sang generator cho dữ liệu cần duyệt nhiều lần.** Lần thứ hai trả rỗng, im lặng.",
  "**Chỉ nhìn bộ nhớ cuối, bỏ qua đỉnh.** Chương trình bị giết vì chạm đỉnh.",
  "**Quên `reset_peak()` khi đo nhiều đoạn liên tiếp.** Đỉnh cũ vẫn được giữ, số liệu sai.",
  "**Nhầm đơn vị `ru_maxrss`.** Kilobyte trên Linux, byte trên macOS — sai số 1024 lần.",
  "**Bật `tracemalloc` trong sản phẩm.** Nó làm chậm 2–3 lần; chỉ dùng để chẩn đoán.",
  "**Nạp cả dataset chỉ để tính vài con số.** Duyệt một lượt và chỉ giữ kết quả tổng hợp."
],

tu_kiem_tra: [
  "Hai câu hỏi khác nhau về bộ nhớ là gì? Mỗi câu dùng công cụ nào?",
  "Vì sao `sys.getsizeof` một mình gây hiểu nhầm? Cho ví dụ.",
  "Hàm đo đệ quy cần ba chi tiết bắt buộc nào? Bỏ mỗi cái gây hậu quả gì?",
  "Vì sao \"RSS tiến trình không giảm\" chưa chắc là rò rỉ?",
  "`tracemalloc` cho biết những gì mà `getsizeof` không cho?",
  "Mô tả quy trình tìm rò rỉ bằng so sánh ảnh chụp.",
  "Vì sao **đỉnh** bộ nhớ quan trọng hơn mức cuối?",
  "Nêu bốn kỹ thuật tối ưu theo thứ tự lợi ích trên công sức, kèm đánh đổi của mỗi cái.",
  "`__slots__` tiết kiệm bằng cách nào? Nêu ba cái giá và bẫy kế thừa.",
  "Vì sao `list` một triệu số tốn ~36 MB còn `array` chỉ ~4 MB?",
  "`memoryview` giải quyết vấn đề gì? Cho ví dụ cụ thể.",
  "Vì sao `dict` nặng hơn `NamedTuple` khi lưu nhiều bản ghi?",
  "Nêu ba kỹ thuật xử lý dữ liệu lớn hơn RAM. Cái nào quan trọng nhất?",
  "Nêu năm bước của quy trình tối ưu. Bước nào hay bị bỏ nhất?",
  "Nêu ba câu hỏi trước khi tối ưu. Câu nào quan trọng nhất và vì sao?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, module `tracemalloc` — ảnh chụp, so sánh, thống kê theo dòng và theo ngăn xếp.",
  "Tài liệu Python, `sys.getsizeof` — nêu rõ nó không tính object được trỏ tới, và gợi ý công thức đo đệ quy.",
  "Tài liệu Python, module `array` và `memoryview` — bảng mã kiểu và giao thức bộ đệm.",
  "Sách **High Performance Python** — Micha Gorelick & Ian Ozsvald. Các chương về cấu trúc dữ liệu tiết kiệm và cách đo.",
  "Sách **CPython Internals** — Anthony Shaw. Chương về bộ cấp phát giải thích vì sao bộ nhớ không trả về hệ điều hành."
],

lien_he: [
  "**Tham chiếu vs giá trị**: object gồm gì và container lưu con trỏ — nền cho mọi con số ở bài này.",
  "**Garbage collection** trong module này: năm hình dạng rò rỉ là bước ① của quy trình tối ưu.",
  "**GIL** ngay trước: tiến trình tốn bộ nhớ gấp nhiều lần thread — con số đó quyết định chọn công cụ đồng thời.",
  "**Shallow copy vs deep copy**: `deepcopy` là nguồn tốn bộ nhớ và thời gian hay bị bỏ sót.",
  "**Generator, iterator** ở module 5: kỹ thuật tiết kiệm bộ nhớ quan trọng nhất, kèm đánh đổi \"dùng một lần\".",
  "**Class, object, thuộc tính** ở module 6: `__slots__` đã giới thiệu ở đó; đây là con số và bẫy kế thừa.",
  "**Dataclass và NamedTuple** ở module 6: `slots=True` và lý do `NamedTuple` nhẹ nhất.",
  "**List, dict, set, tuple** ở module 5: bảng chi phí bộ nhớ ở đó, giờ có cách đo và cách chọn.",
  "**Decorator** ở module 5: `lru_cache(maxsize=None)` là rò rỉ — kiểm tra đầu tiên khi tối ưu.",
  "**CPU, RAM, cache** ở module 1: dữ liệu liền khối vừa tốn ít vừa nhanh — hai mục tiêu đi cùng chiều.",
  "**AI/ML** ở mảng 07: dataset lớn hơn RAM là tình huống thường ngày, và năm kỹ thuật ở đây quyết định xử lý được bao nhiêu dữ liệu trên một máy.",
  "Đây là **bài cuối module Bộ nhớ & cơ chế thực thi**: module 8 chuyển sang Clean Code — từ \"máy chạy thế nào\" sang \"người đọc thế nào\"."
]

};
