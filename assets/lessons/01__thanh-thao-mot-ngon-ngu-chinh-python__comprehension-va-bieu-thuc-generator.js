window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:comprehension-va-bieu-thuc-generator"] = {

tom_tat: [
  "**Comprehension là cách nói \"tạo một tập hợp mới từ một tập hợp cũ\" trong một biểu thức** — không phải mẹo viết ngắn, mà là cách diễn đạt ý định *biến đổi* thay vì *lặp rồi thêm*.",
  "**Đọc comprehension theo đúng thứ tự viết**: `for` trước, `if` sau, rồi mới tới biểu thức đầu dòng. Đọc từ trái sang phải là đọc ngược.",
  "**Đổi `[]` thành `()` là đổi từ list comprehension sang generator** — cùng cú pháp, khác hẳn về bộ nhớ: một bên dựng cả list, một bên sinh từng phần tử.",
  "**Generator chỉ dùng được một lần.** Đây là nguồn lỗi im lặng số một: lần lặp thứ hai không báo lỗi, nó chỉ trả về rỗng.",
  "**Comprehension lồng ba tầng hoặc có logic phức tạp thì viết vòng lặp thường.** Ngắn không phải mục tiêu; đọc hiểu trong ba giây mới là mục tiêu."
],

can_biet_truoc: [
  "Bài **List, dict, set, tuple** ngay trước — comprehension tạo ra đúng bốn loại đó, và chọn loại nào vẫn theo tiêu chí của bài đó.",
  "Bài **Cú pháp, kiểu dữ liệu, toán tử** — truthiness quyết định `if` trong comprehension lọc cái gì.",
  "Vòng lặp `for` cơ bản. Comprehension là cách viết khác của một mẫu vòng lặp cụ thể, nên phải quen mẫu đó trước.",
  "Bài **Generator, iterator và `yield`** ở sau trong module này sẽ đào sâu; bài này chỉ giới thiệu dạng biểu thức."
],

dinh_nghia: "Nói đời thường: thay vì nói *\"lấy một cái rổ rỗng, duyệt qua từng quả, quả nào chín thì bỏ vào rổ\"*, comprehension cho bạn nói thẳng: *\"rổ gồm những quả chín trong đống này\"*. Cùng kết quả, nhưng câu sau mô tả **cái bạn muốn**, câu trước mô tả **các bước làm**.\n\nNói chính xác:\n\n- **Comprehension** — cú pháp tạo một collection mới bằng một biểu thức duy nhất, gồm ba phần: biểu thức kết quả, một hay nhiều mệnh đề `for`, và các mệnh đề `if` tuỳ chọn.\n- **List comprehension** — `[bieu_thuc for x in nguon if dieu_kien]`, trả về `list`.\n- **Dict comprehension** — `{k: v for ... }`, trả về `dict`.\n- **Set comprehension** — `{bieu_thuc for ...}`, trả về `set`.\n- **Biểu thức generator** (*generator expression*) — `(bieu_thuc for ...)`, trả về một **generator**: không dựng sẵn dữ liệu, chỉ sinh từng phần tử khi được yêu cầu.\n- **Lười** (*lazy*) — chỉ tính khi cần. Generator lười; comprehension thì không.\n\nĐiểm cần nắm ngay và phân biệt cho rõ: **không có \"tuple comprehension\"**. `(x for x in ds)` trông giống nhưng nó là **generator**, không phải `tuple`. Muốn `tuple` thì viết `tuple(x for x in ds)`.",

vi_sao_can: "Comprehension là thứ phân biệt code Python với code \"C viết bằng cú pháp Python\":\n\n- **Đây là cách viết mặc định trong hệ sinh thái Python.** Đọc bất kỳ thư viện nào cũng gặp; không đọc trôi comprehension là đọc code người khác rất chậm.\n- **Nhanh hơn vòng lặp thường một cách đo được.** Comprehension được tối ưu ở tầng bytecode: nó không phải tra tên `append` và gọi phương thức ở mỗi vòng lặp.\n- **Generator giải bài toán dữ liệu không vừa bộ nhớ.** Xử lý file log 10 GB bằng list là hết RAM; bằng generator là chạy với vài chục MB. Đây không phải tối ưu tinh vi mà là điều kiện để chương trình chạy được.\n- **Nền cho toàn bộ xử lý dữ liệu.** Pipeline tiền xử lý trong ML gần như luôn là một chuỗi comprehension và generator nối nhau.\n- **Nhưng nó cũng là chỗ dễ lạm dụng nhất.** Comprehension lồng ba tầng có điều kiện là code không ai đọc nổi, kể cả người viết ra nó tuần sau. Biết **khi nào không dùng** cũng quan trọng như biết dùng.\n\nTình huống thật rất hay gặp: bạn viết một hàm trả về `(x for x in du_lieu if x.hop_le)` cho gọn và tiết kiệm bộ nhớ. Người gọi hàm dùng `len()` trên nó — lỗi. Họ sửa bằng cách lặp qua hai lần: lần đầu để đếm, lần sau để xử lý. Lần thứ hai **không báo lỗi, chỉ trả về rỗng**, và chương trình âm thầm bỏ sót toàn bộ dữ liệu. Đây là hệ quả trực tiếp của tính \"dùng một lần\", và phần cơ chế sẽ nói rõ vì sao nó xảy ra.",

co_che: "### Cấu trúc và thứ tự đọc\n\n```python\n[bieu_thuc for phan_tu in nguon if dieu_kien]\n  ①            ②                    ③\n```\n\nThứ tự **thực thi** là ② → ③ → ①, tức là đúng thứ tự viết từ `for` trở đi, rồi mới quay lại đầu dòng:\n\n```python\n[x ** 2 for x in range(10) if x % 2 == 0]\n#                └─② duyệt   └─③ lọc\n# └─① biến đổi\n\n# Tương đương hoàn toàn với:\nkq = []\nfor x in range(10):          # ②\n    if x % 2 == 0:           # ③\n        kq.append(x ** 2)    # ①\n```\n\nMẹo đọc: **đọc từ `for` trước, rồi quay ngược lên đầu**. Người mới hay đọc từ trái sang phải và thấy rối, vì phần đầu dòng là thứ xảy ra *cuối cùng*.\n\nBốn dạng, khác nhau ở dấu bao ngoài:\n\n```python\n[x * 2 for x in ds]            # list\n{x * 2 for x in ds}            # set — khử trùng lặp luôn\n{x: x * 2 for x in ds}         # dict — cần dấu hai chấm\n(x * 2 for x in ds)            # ⚠️ GENERATOR, không phải tuple\ntuple(x * 2 for x in ds)       # muốn tuple thì phải nói rõ\n```\n\n### `if` đứng sau và `if` đứng trước — hai việc khác nhau\n\nĐây là chỗ nhầm rất phổ biến:\n\n```python\n# if SAU for = LỌC — phần tử không thoả bị BỎ HẲN\n[x for x in range(5) if x % 2 == 0]           # [0, 2, 4]\n\n# if...else TRƯỚC for = CHỌN GIÁ TRỊ — giữ đủ mọi phần tử\n[x if x % 2 == 0 else -1 for x in range(5)]  # [0, -1, 2, -1, 4]\n```\n\n```\n[  biểu_thức_có_thể_có_if_else   for x in nguồn   if điều_kiện_lọc  ]\n   └── chọn giá trị, giữ số lượng      └── lọc, giảm số lượng\n```\n\nPhân biệt bằng câu hỏi: **số phần tử kết quả có bằng số phần tử nguồn không?** Bằng → dùng `if/else` phía trước. Ít hơn → dùng `if` phía sau. Cần cả hai thì viết cả hai:\n\n```python\n[x if x > 0 else 0 for x in ds if x is not None]\n#  └─ thay số âm bằng 0        └─ bỏ hẳn phần tử None\n```\n\n### Nhiều `for` — thứ tự lồng nhau\n\n```python\n[(x, y) for x in \"ab\" for y in [1, 2]]\n# [('a', 1), ('a', 2), ('b', 1), ('b', 2)]\n\n# Đọc như vòng lặp lồng, THEO ĐÚNG THỨ TỰ VIẾT:\nfor x in \"ab\":          # for ngoài viết trước\n    for y in [1, 2]:    # for trong viết sau\n        ...\n```\n\nQuy tắc: **`for` viết trước là vòng ngoài**. Đây là điểm khác với cách trực giác của một số người, vốn tưởng phải viết ngược.\n\n`for` sau dùng được biến của `for` trước — tính chất này cho phép duyệt cấu trúc lồng nhau:\n\n```python\nma_tran = [[1, 2], [3, 4], [5, 6]]\n\n# Làm phẳng\n[x for hang in ma_tran for x in hang]        # [1, 2, 3, 4, 5, 6]\n\n# Chuyển vị — dễ nhầm, và có cách tốt hơn\n[[hang[i] for hang in ma_tran] for i in range(2)]   # [[1,3,5],[2,4,6]]\nlist(zip(*ma_tran))                                 # ⭐ gọn và rõ hơn nhiều\n```\n\nDòng cuối đáng nhớ: `zip(*ma_tran)` là thành ngữ chuyển vị chuẩn của Python. Khi có sẵn hàm dựng sẵn diễn đạt đúng ý định, dùng nó tốt hơn comprehension lồng.\n\n### Phạm vi biến — Python 3 sửa một lỗi thiết kế\n\n```python\nx = \"gia tri goc\"\nds = [x for x in range(5)]\nprint(x)          # 'gia tri goc' — KHÔNG bị ghi đè\n```\n\nComprehension có **phạm vi riêng** trong Python 3. Ở Python 2 thì không, và biến lặp làm hỏng biến bên ngoài — một trong những lỗi thiết kế đã được sửa khi lên Python 3.\n\nNhưng có một ngoại lệ đáng biết:\n\n```python\n# Comprehension ĐỌC được biến bên ngoài\nnguong = 3\n[x for x in range(10) if x > nguong]    # ✓ đọc nguong bình thường\n\n# Nhưng KHÔNG gán ra ngoài được (trừ khi dùng walrus)\n[y := x for x in range(3)]              # walrus thì gán ra ngoài được\n```\n\n### Toán tử walrus trong comprehension\n\nWalrus (`:=`) giải quyết một vấn đề cụ thể: **cần dùng một giá trị đã tính ở cả phần lọc lẫn phần kết quả**.\n\n```python\n# ❌ gọi ham_nang() HAI LẦN cho mỗi phần tử\n[ham_nang(x) for x in ds if ham_nang(x) > 0]\n\n# ✓ gọi một lần, giữ lại kết quả\n[kq for x in ds if (kq := ham_nang(x)) > 0]\n```\n\nVới hàm tốn kém — gọi API, truy vấn database, chạy mô hình — đây là khác biệt gấp đôi thời gian chạy. Nhưng đừng dùng walrus chỉ để cho ngắn: nó làm dòng khó đọc hơn, nên chỉ đáng khi thật sự tránh được việc tính lại.\n\n### Generator: đổi `[]` thành `()`\n\n```python\nimport sys\n\nlist_cp = [x ** 2 for x in range(1_000_000)]\ngen     = (x ** 2 for x in range(1_000_000))\n\nsys.getsizeof(list_cp)     # khoảng 8 MB — đã dựng đủ 1 triệu phần tử\nsys.getsizeof(gen)         # khoảng 200 byte — chưa tính gì cả\n```\n\nSự khác biệt không phải \"generator nhẹ hơn\" mà là **generator chưa làm gì cả**:\n\n```\nlist comprehension:  tính NGAY toàn bộ, cất vào bộ nhớ\ngenerator:           chỉ ghi nhớ CÁCH tính, sinh từng phần tử khi bị hỏi\n```\n\n```python\ngen = (x ** 2 for x in range(5))\nnext(gen)        # 0 — bây giờ mới tính phần tử đầu\nnext(gen)        # 1\nfor v in gen:    # tiếp tục từ chỗ dở: 4, 9, 16\n    print(v)\n```\n\nTính lười này cho một khả năng mà list không có: **làm việc với dãy vô hạn**.\n\n```python\nimport itertools\n\nso_le = (x for x in itertools.count() if x % 2)   # dãy VÔ HẠN\nlist(itertools.islice(so_le, 5))                   # [1, 3, 5, 7, 9]\n# Viết list comprehension ở đây là treo máy vĩnh viễn\n```\n\n### Generator chỉ dùng được một lần\n\nĐây là tính chất gây lỗi nhiều nhất, và lỗi thì **im lặng**:\n\n```python\ngen = (x for x in range(3))\n\nprint(list(gen))     # [0, 1, 2]\nprint(list(gen))     # []  ← KHÔNG lỗi, chỉ rỗng\nprint(sum(gen))      # 0   ← vẫn không lỗi\n```\n\nLý do: generator giữ một **con trỏ vị trí**. Lặp hết là con trỏ ở cuối, và không có cách quay lại đầu. Nó không phải một collection; nó là một *quá trình đang diễn ra*.\n\nHai thứ generator không làm được:\n\n```python\ngen = (x for x in range(3))\nlen(gen)         # TypeError: object of type 'generator' has no len()\ngen[0]           # TypeError: 'generator' object is not subscriptable\n```\n\nQuy tắc thực dụng để tránh cả lớp lỗi này:\n\n```\nHàm TRẢ VỀ generator  → tài liệu phải nói rõ, hoặc trả list cho an toàn\nCần lặp NHIỀU LẦN     → dùng list\nCần len() hoặc [i]    → dùng list\nDữ liệu rất lớn, lặp MỘT lượt → generator\n```\n\nMột phòng vệ hữu ích khi viết hàm cho người khác dùng: nếu dữ liệu không lớn, **cứ trả `list`**. Tiết kiệm vài megabyte không đáng đổi lấy một lỗi im lặng trong code của người khác.\n\n### Khi nào generator thật sự cần thiết\n\n```python\n# ❌ đọc CẢ file 10 GB vào bộ nhớ — hết RAM\ndong = [d.strip() for d in open(\"log_10gb.txt\")]\n\n# ✓ xử lý từng dòng, bộ nhớ gần như không đổi\nwith open(\"log_10gb.txt\") as f:\n    loi = (d for d in f if \"ERROR\" in d)\n    for d in loi:\n        xu_ly(d)\n```\n\nMẫu mạnh nhất là **nối nhiều generator thành một đường ống**:\n\n```python\nwith open(\"log.txt\") as f:\n    dong    = (d.strip() for d in f)                  # bỏ khoảng trắng\n    khong_rong = (d for d in dong if d)                # bỏ dòng rỗng\n    loi     = (d for d in khong_rong if \"ERROR\" in d)  # lọc lỗi\n    thoi_gian = (d.split()[0] for d in loi)            # lấy cột đầu\n\n    for t in thoi_gian:      # ⭐ tới ĐÂY mới thật sự đọc file\n        print(t)\n```\n\nBa dòng đầu **không đọc một byte nào**. Chúng chỉ mô tả các bước. Dữ liệu chỉ chảy khi vòng `for` cuối kéo, và mỗi dòng đi qua cả bốn tầng rồi mới tới dòng tiếp theo. Bộ nhớ dùng là **một dòng tại một thời điểm**, bất kể file lớn cỡ nào.\n\nĐây là cách nghĩ rất đáng học: **mô tả luồng biến đổi trước, thực thi sau** — cùng ý tưởng với `Dataset` và `DataLoader` trong các thư viện học sâu.\n\n### Khi nào KHÔNG dùng comprehension\n\nComprehension bị lạm dụng nhiều, nên phần này quan trọng ngang phần cú pháp.\n\n**① Có tác dụng phụ thì dùng vòng lặp thường:**\n\n```python\n# ❌ tạo một list toàn None chỉ để vứt đi\n[print(x) for x in ds]\n\n# ✓\nfor x in ds:\n    print(x)\n```\n\nComprehension là để **tạo ra một giá trị**. Nếu bạn không dùng giá trị đó thì bạn đã chọn sai công cụ.\n\n**② Quá ba tầng hoặc logic rẽ nhánh phức tạp:**\n\n```python\n# ❌ đọc không nổi\nkq = [xu_ly(x, y) for x in ds1 if x.hop_le\n      for y in x.con if y.tuoi > 18 and y.trang_thai != \"khoa\"]\n\n# ✓ vòng lặp thường, đặt tên cho từng bước\nkq = []\nfor x in ds1:\n    if not x.hop_le:\n        continue\n    nguoi_lon = [y for y in x.con if y.tuoi > 18]\n    for y in nguoi_lon:\n        if y.trang_thai != \"khoa\":\n            kq.append(xu_ly(x, y))\n```\n\nTiêu chuẩn thực dụng: **một comprehension nên đọc hiểu trong khoảng ba giây**. Quá đó thì tách ra — có thể vẫn dùng comprehension, nhưng nhiều bước có tên.\n\n**③ Cần xử lý ngoại lệ:**\n\n```python\n# Không có cách bắt ngoại lệ BÊN TRONG comprehension\n[int(x) for x in [\"1\", \"a\", \"3\"]]    # ValueError, và mất cả kết quả\n\n# ✓ tách phần có thể lỗi ra một hàm\ndef sang_int(x, mac_dinh=None):\n    try:\n        return int(x)\n    except ValueError:\n        return mac_dinh\n\n[v for x in [\"1\", \"a\", \"3\"] if (v := sang_int(x)) is not None]   # [1, 3]\n```\n\n### Vì sao comprehension nhanh hơn vòng lặp\n\n```python\n# Vòng lặp thường, mỗi vòng phải:\nkq = []\nfor x in range(n):\n    kq.append(x * 2)      # ① tra tên kq  ② lấy thuộc tính append  ③ gọi hàm\n\n# Comprehension: bytecode có lệnh LIST_APPEND chuyên dụng,\n# không tra tên và không gọi phương thức qua tầng thông thường\nkq = [x * 2 for x in range(n)]\n```\n\nKhác biệt thường vào khoảng 30–50% — đáng kể nhưng **không phải lý do chính** để dùng comprehension. Lý do chính vẫn là nó diễn đạt đúng ý định. Nếu cần nhanh hơn nữa với dữ liệu số lớn, hướng đi đúng là NumPy chứ không phải vi chỉnh comprehension.\n\nNối về bài **Compiled vs interpreted** ở module 1: sự khác biệt này nằm ở **số lệnh bytecode phải thực thi**, và đó cũng là lý do vì sao gọi hàm trong Python đắt hơn nhiều so với C.\n\n### `map` và `filter` — người tiền nhiệm\n\n```python\nds = [1, 2, 3, 4]\n\nlist(map(lambda x: x * 2, ds))            # [2, 4, 6, 8]\n[x * 2 for x in ds]                        # ⭐ Pythonic hơn\n\nlist(filter(lambda x: x % 2 == 0, ds))    # [2, 4]\n[x for x in ds if x % 2 == 0]              # ⭐\n\n# map với hàm CÓ SẴN thì vẫn gọn và rõ\nlist(map(str, ds))                         # ['1','2','3','4'] — chấp nhận được\n[str(x) for x in ds]                       # cũng tốt\n```\n\nQuy ước trong cộng đồng Python: **comprehension được ưa hơn `map`/`filter` kèm `lambda`**, vì nó đọc gần với tiếng Anh hơn. `map` với một hàm có sẵn (không `lambda`) thì vẫn được dùng và không có gì sai.\n\nMột khác biệt kỹ thuật đáng biết: trong Python 3, `map` và `filter` **trả về iterator lười**, không trả list — nên chúng cũng dùng được một lần, đúng như generator.",

vi_du: "### Từ vòng lặp sang comprehension\n\n```python\nds = [1, 2, 3, 4, 5, 6]\n\n# Biến đổi\nkq = []\nfor x in ds:\n    kq.append(x ** 2)\nkq = [x ** 2 for x in ds]                       # ✓\n\n# Lọc\nkq = []\nfor x in ds:\n    if x % 2 == 0:\n        kq.append(x)\nkq = [x for x in ds if x % 2 == 0]              # ✓\n\n# Vừa lọc vừa biến đổi\nkq = [x ** 2 for x in ds if x % 2 == 0]         # [4, 16, 36]\n\n# Thay giá trị, GIỮ số lượng\nkq = [x if x > 3 else 0 for x in ds]            # [0, 0, 0, 4, 5, 6]\n```\n\nBa dòng cuối là ba mẫu bao phủ phần lớn nhu cầu thực tế. Nhận ra mình đang ở mẫu nào là chọn được cú pháp đúng ngay.\n\n### `dict` comprehension trong xử lý dữ liệu\n\n```python\nnguoi = [{\"id\": 1, \"ten\": \"Minh\", \"diem\": 8},\n         {\"id\": 2, \"ten\": \"An\",   \"diem\": 5},\n         {\"id\": 3, \"ten\": \"Bình\", \"diem\": 9}]\n\n# Đánh chỉ mục theo id — mẫu dùng RẤT nhiều\ntheo_id = {n[\"id\"]: n for n in nguoi}\ntheo_id[2][\"ten\"]                # 'An' — giờ tra O(1) thay vì quét list\n\n# Lọc rồi ánh xạ\ndat = {n[\"ten\"]: n[\"diem\"] for n in nguoi if n[\"diem\"] >= 8}\n# {'Minh': 8, 'Bình': 9}\n\n# Đảo khoá-giá trị\ndiem_ten = {v: k for k, v in dat.items()}\n\n# Đổi tên khoá hàng loạt\nanh_xa = {\"ten\": \"name\", \"diem\": \"score\"}\nmoi = [{anh_xa.get(k, k): v for k, v in n.items()} for n in nguoi]\n```\n\nMẫu `{n[\"id\"]: n for n in nguoi}` đáng ghi nhớ: nó biến một `list` phải quét O(n) thành một `dict` tra O(1). Khi bạn thấy mình viết vòng lặp tìm phần tử theo id nhiều lần, đây là câu trả lời.\n\n### `set` comprehension để khử trùng lặp có điều kiện\n\n```python\nemail = [\"A@x.com\", \"b@y.com\", \"a@X.com\", \"c@z.com\"]\n\n# Chuẩn hoá rồi khử trùng — một bước\nduy_nhat = {e.lower() for e in email}\n# {'a@x.com', 'b@y.com', 'c@z.com'}  ← A@x.com và a@X.com gộp làm một\n\n# Lấy tập tên miền\nten_mien = {e.split(\"@\")[1].lower() for e in email}\n# {'x.com', 'y.com', 'z.com'}\n```\n\nDùng `set` comprehension thay vì `list` rồi `set()` sau: gọn hơn, và không dựng list trung gian.\n\n### Sức mạnh và cạm bẫy của generator\n\n```python\nimport sys\n\n# So bộ nhớ\nlc = [x for x in range(1_000_000)]\ngen = (x for x in range(1_000_000))\nprint(f\"list: {sys.getsizeof(lc):,} byte\")     # ~8.000.000\nprint(f\"gen:  {sys.getsizeof(gen):,} byte\")    # ~200\n\n# Nhưng generator CHỈ DÙNG MỘT LẦN\ng = (x for x in range(3))\nprint(list(g))     # [0, 1, 2]\nprint(list(g))     # []      ← lỗi im lặng!\n\n# Bẫy thật trong code: dùng hai lần mà không nhận ra\ndef thong_ke(du_lieu):\n    n = sum(1 for _ in du_lieu)         # ① lặp hết\n    tong = sum(du_lieu)                  # ② rỗng ⇒ tong = 0\n    return tong / n if n else 0          # kết quả SAI, không lỗi\n\nprint(thong_ke([1, 2, 3]))              # 2.0  ✓ list lặp lại được\nprint(thong_ke(x for x in [1, 2, 3]))   # 0.0  ✗ generator thì không\n```\n\nHàm `thong_ke` **chạy đúng khi test bằng list và sai khi dùng thật với generator**. Đây là dạng lỗi tệ nhất: qua được kiểm thử rồi hỏng trong sản phẩm.\n\nHai cách phòng:\n\n```python\n# ① Vật chất hoá ngay đầu hàm — đơn giản, tốn bộ nhớ\ndef thong_ke(du_lieu):\n    du_lieu = list(du_lieu)          # giờ lặp bao nhiêu lần cũng được\n    return sum(du_lieu) / len(du_lieu) if du_lieu else 0\n\n# ② Chỉ lặp ĐÚNG MỘT lượt — giữ được ưu điểm bộ nhớ\ndef thong_ke(du_lieu):\n    tong = n = 0\n    for x in du_lieu:\n        tong += x\n        n += 1\n    return tong / n if n else 0\n```\n\nCách ② tốt hơn khi dữ liệu lớn, và nó là kỹ năng đáng rèn: **viết hàm chỉ duyệt nguồn một lượt** thì hàm đó dùng được với mọi iterable.\n\n### Đường ống generator xử lý file lớn\n\n```python\ndef doc_log(duong_dan):\n    \"\"\"Sinh từng dòng đã làm sạch. Không đọc cả file vào bộ nhớ.\"\"\"\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        for dong in f:\n            dong = dong.strip()\n            if dong:\n                yield dong\n\ndef phan_tich(dong_lieu):\n    for d in dong_lieu:\n        phan = d.split(\" \", 3)\n        if len(phan) == 4:\n            yield {\"tg\": phan[0], \"muc\": phan[1], \"ip\": phan[2], \"tin\": phan[3]}\n\n# Ghép thành đường ống — không dòng nào chạy cho tới vòng for cuối\nban_ghi = phan_tich(doc_log(\"app.log\"))\nchi_loi = (b for b in ban_ghi if b[\"muc\"] == \"ERROR\")\nip_loi  = (b[\"ip\"] for b in chi_loi)\n\nfrom collections import Counter\nprint(Counter(ip_loi).most_common(5))    # ⭐ chỗ này mới kéo dữ liệu\n```\n\nBộ nhớ dùng ở đây là **một dòng cộng với bảng đếm**, dù file 50 GB. So với việc đọc cả file rồi lọc thì đây là khác biệt giữa \"chạy được\" và \"máy hết RAM\".\n\nChú ý `with` nằm **bên trong** hàm sinh: file được giữ mở suốt quá trình sinh và tự đóng khi generator cạn. Bài **Context manager và câu lệnh `with`** trong module này sẽ nói kỹ về cơ chế đó.\n\n### `any` và `all` với generator — dừng sớm\n\n```python\nso = [1, 3, 5, 8, 9, 11]\n\n# ✓ dừng NGAY khi gặp phần tử chẵn đầu tiên\nco_chan = any(x % 2 == 0 for x in so)\n\n# ❌ tính hết cả 6 phần tử rồi mới kiểm tra\nco_chan = any([x % 2 == 0 for x in so])\n\n# Khác biệt rất lớn khi phép kiểm tra tốn kém\nco_loi = any(kiem_tra_ton_kem(x) for x in danh_sach_10000_phan_tu)\n#            ↑ có thể dừng ở phần tử thứ 3, không chạy 9997 lần còn lại\n```\n\nQuy tắc: **bỏ dấu ngoặc vuông khi truyền comprehension vào `any`, `all`, `sum`, `min`, `max`, `join`**. Chúng nhận iterable, nên generator vừa tiết kiệm bộ nhớ vừa cho phép dừng sớm.\n\nMột ngoại lệ đáng biết: `\"\".join()` thì list **nhanh hơn** generator một chút, vì `join` cần biết tổng độ dài trước nên phải duyệt hai lượt — với generator nó phải tự dựng list bên trong.\n\n### Comprehension trong tiền xử lý dữ liệu ML\n\n```python\nvan_ban = [\"Tôi thích Python\", \"Python rất mạnh\", \"Học Python đi\"]\n\n# ① Token hoá và chuẩn hoá\ntoken = [[t.lower() for t in cau.split()] for cau in van_ban]\n\n# ② Xây từ vựng — set comprehension làm phẳng và khử trùng\ntu_vung = {t for cau in token for t in cau}\n\n# ③ Ánh xạ từ → chỉ số\ntu2id = {t: i for i, t in enumerate(sorted(tu_vung))}\n\n# ④ Mã hoá\nma = [[tu2id[t] for t in cau] for cau in token]\n\n# ⑤ Đệm cho bằng độ dài — bước chuẩn trước khi đưa vào mô hình\ndai_nhat = max(len(c) for c in ma)\ndem = [c + [0] * (dai_nhat - len(c)) for c in ma]\n```\n\nNăm bước, năm comprehension, mỗi bước một dòng và đọc ra ngay ý định. Đây là lý do comprehension đáng thành thạo với định hướng AI/ML: **toàn bộ tiền xử lý dữ liệu là những phép biến đổi tập hợp**, và comprehension là cách diễn đạt tự nhiên nhất cho chúng.\n\nChú ý bước ⑤ dùng `max(len(c) for c in ma)` không có ngoặc vuông — đúng nguyên tắc ở mục trên.\n\n### `enumerate` và `zip` trong comprehension\n\n```python\nten = [\"Minh\", \"An\", \"Bình\"]\ndiem = [8, 5, 9]\n\n# enumerate: cần cả chỉ số\n[f\"{i+1}. {t}\" for i, t in enumerate(ten)]\n# ['1. Minh', '2. An', '3. Bình']\n\n# zip: ghép hai dãy song song\n{t: d for t, d in zip(ten, diem)}\n# {'Minh': 8, 'An': 5, 'Bình': 9}\n\n# Kết hợp cả hai\n[f\"{i}: {t}={d}\" for i, (t, d) in enumerate(zip(ten, diem))]\n\n# ⚠️ zip dừng ở dãy NGẮN NHẤT — dữ liệu thừa bị bỏ IM LẶNG\nlist(zip([1, 2, 3], [\"a\", \"b\"]))            # [(1,'a'), (2,'b')] — mất số 3\nlist(zip([1, 2, 3], [\"a\", \"b\"], strict=True))  # ValueError ✓ báo lỗi\n```\n\nTham số `strict=True` (Python 3.10 trở lên) đáng dùng thành thói quen khi hai dãy **đáng lẽ phải bằng nhau**: nó biến một lỗi dữ liệu im lặng thành một ngoại lệ rõ ràng.\n\n### Khi comprehension là lựa chọn sai\n\n```python\n# ❌ dùng cho tác dụng phụ\n[luu_database(x) for x in ds]        # tạo list None vô nghĩa\n\n# ✓\nfor x in ds:\n    luu_database(x)\n\n# ❌ lồng sâu, có điều kiện phức tạp\nkq = [f(x, y) for x in a if kiem(x) for y in x.con if y.ok and y.n > 3]\n\n# ✓ tách từng bước có tên\nhop_le = [x for x in a if kiem(x)]\nkq = []\nfor x in hop_le:\n    con_tot = [y for y in x.con if y.ok and y.n > 3]\n    kq.extend(f(x, y) for y in con_tot)\n```\n\nBản thứ hai dài hơn ba dòng nhưng **đọc hiểu ngay**, và khi có lỗi thì đặt được điểm dừng ở từng bước. Đó là đánh đổi gần như luôn đáng.",

so_sanh: "### Bốn dạng comprehension\n\n| | Cú pháp | Trả về | Khử trùng lặp | Bộ nhớ |\n|---|---|---|---|---|\n| List | `[x for x in ds]` | `list` | ✗ | đầy đủ |\n| Set | `{x for x in ds}` | `set` | **✓** | đầy đủ + bảng băm |\n| Dict | `{k: v for ...}` | `dict` | ✓ theo khoá | đầy đủ + bảng băm |\n| Generator | `(x for x in ds)` | **generator** | ✗ | **gần như 0** |\n\nHàng cuối là hàng khác biệt về bản chất, không chỉ khác về kiểu trả về: ba dạng trên **tính ngay**, dạng cuối **chưa tính gì**.\n\n### List comprehension và generator\n\n| | List comprehension | Generator |\n|---|---|---|\n| Tính khi nào | **ngay lập tức** | khi được yêu cầu |\n| Bộ nhớ | O(n) | **O(1)** |\n| Lặp lại nhiều lần | ✓ | **✗ chỉ một lần** |\n| `len()` | ✓ | ✗ |\n| Truy cập `[i]` | ✓ | ✗ |\n| Dãy vô hạn | ✗ treo máy | **✓** |\n| Dừng sớm | ✗ | **✓** |\n| Gỡ lỗi | dễ — in ra xem được | khó hơn |\n\nCách chọn gói lại thành ba dòng:\n\n```\nCần lặp nhiều lần, cần len, cần [i]     → list\nDữ liệu rất lớn hoặc vô hạn, lặp 1 lượt → generator\nTruyền vào any/all/sum/min/max          → generator (dừng sớm được)\n```\n\n### `if` trước và `if` sau `for`\n\n| | `if` **sau** `for` | `if/else` **trước** `for` |\n|---|---|---|\n| Vai trò | **lọc** | **chọn giá trị** |\n| Số phần tử kết quả | ít hơn hoặc bằng nguồn | **bằng nguồn** |\n| Có `else` | ✗ không được | **✓ bắt buộc** |\n| Ví dụ | `[x for x in ds if x > 0]` | `[x if x > 0 else 0 for x in ds]` |\n\nCâu hỏi phân biệt: **kết quả có cùng số phần tử với nguồn không?** Đây là cách nhớ chắc chắn hơn học thuộc vị trí cú pháp.\n\n### Comprehension và vòng lặp thường\n\n| | Comprehension | Vòng lặp `for` |\n|---|---|---|\n| Tốc độ | **nhanh hơn ~30–50%** | chậm hơn |\n| Diễn đạt ý định | \"tạo cái mới từ cái cũ\" | \"làm từng bước\" |\n| Tác dụng phụ | **không nên** | ✓ phù hợp |\n| `try/except` bên trong | ✗ | **✓** |\n| `break` / `continue` | ✗ | **✓** |\n| Logic nhiều tầng | khó đọc nhanh | **✓ rõ ràng** |\n| Đặt điểm dừng khi gỡ lỗi | khó | **dễ** |\n\nBốn hàng cuối là lý do vòng lặp thường **không hề lỗi thời**. Comprehension đúng cho phép biến đổi đơn giản; mọi thứ có rẽ nhánh, ngoại lệ, hoặc nhiều bước thì vòng lặp rõ hơn.\n\n### Comprehension và `map`/`filter`\n\n| | Comprehension | `map`/`filter` |\n|---|---|---|\n| Cần `lambda` | ✗ | **✓ với biểu thức tuỳ ý** |\n| Lọc và biến đổi cùng lúc | **✓ một biểu thức** | phải lồng hai hàm |\n| Trả về | list/set/dict/generator | **iterator lười** |\n| Quy ước cộng đồng | **được ưa hơn** | dùng khi có hàm sẵn |\n\n```python\n# Lọc + biến đổi: comprehension rõ hơn hẳn\n[x ** 2 for x in ds if x > 0]\nlist(map(lambda x: x ** 2, filter(lambda x: x > 0, ds)))   # rối\n```\n\n`map` vẫn có chỗ đứng khi hàm đã có sẵn: `map(str, ds)` hoặc `map(int, dong.split())` đọc rất gọn và không cần `lambda`.\n\n### Chi phí bộ nhớ theo quy mô dữ liệu\n\n| Số phần tử | List comprehension | Generator |\n|---|---|---|\n| 1 000 | không đáng kể | không đáng kể |\n| 1 000 000 | ~8 MB | ~200 byte |\n| 100 000 000 | **hết RAM** | ~200 byte |\n| vô hạn | **treo máy** | ✓ chạy được |\n\nBảng này nói rõ khi nào lựa chọn thật sự quan trọng: **dưới vài chục nghìn phần tử thì dùng cái nào cũng được, hãy chọn cái dễ đọc hơn.** Tối ưu bộ nhớ khi chưa cần chỉ làm code khó dùng hơn mà không được gì.",

loi_thuong_gap: [
  "**Tưởng `(x for x in ds)` là tuple.** Đó là generator. Muốn `tuple` phải viết `tuple(x for x in ds)`.",
  "**Lặp qua generator hai lần.** Lần thứ hai trả rỗng và **không báo lỗi** — dạng lỗi tệ nhất vì qua được kiểm thử với list.",
  "**Gọi `len()` hoặc `gen[0]` trên generator.** `TypeError`. Nếu cần thì dùng list.",
  "**Đặt `if/else` sai vị trí.** `if` sau `for` là lọc; `if/else` trước `for` là chọn giá trị. Hỏi \"số phần tử có đổi không\" để biết dùng cái nào.",
  "**Dùng comprehension cho tác dụng phụ.** `[print(x) for x in ds]` tạo một list `None` vô nghĩa. Dùng vòng lặp `for`.",
  "**Viết comprehension lồng ba tầng có điều kiện.** Không ai đọc nổi, kể cả bạn tuần sau. Tách thành các bước có tên.",
  "**Gọi hàm tốn kém hai lần trong cùng comprehension.** `[f(x) for x in ds if f(x) > 0]`. Dùng walrus để gọi một lần.",
  "**Truyền list comprehension vào `any`/`all`.** Mất khả năng dừng sớm. Bỏ dấu ngoặc vuông đi.",
  "**Trả generator từ hàm mà không ghi rõ trong tài liệu.** Người gọi sẽ dùng hai lần và gặp lỗi im lặng. Với dữ liệu nhỏ, cứ trả `list`.",
  "**Nhầm thứ tự nhiều `for`.** `for` viết trước là vòng ngoài.",
  "**Dùng `zip` với hai dãy không bằng nhau mà không biết.** Nó dừng ở dãy ngắn nhất và bỏ dữ liệu im lặng. Dùng `strict=True`.",
  "**Cố bắt ngoại lệ bên trong comprehension.** Không làm được. Tách phần có thể lỗi ra một hàm riêng.",
  "**Dùng generator cho dữ liệu nhỏ để \"tối ưu\".** Dưới vài chục nghìn phần tử thì không khác gì, mà code khó dùng hơn.",
  "**Dựng list trung gian rồi mới `set()`.** Dùng thẳng set comprehension."
],

tu_kiem_tra: [
  "Đọc `[x**2 for x in ds if x > 0]` theo đúng thứ tự thực thi — phần nào chạy trước?",
  "Phân biệt `[x for x in ds if c]` với `[x if c else y for x in ds]`. Câu hỏi nào giúp chọn đúng?",
  "Vì sao `(x for x in ds)` không phải tuple? Làm sao tạo tuple từ comprehension?",
  "Generator khác list comprehension ở ba điểm nào? Điểm nào gây lỗi nhiều nhất?",
  "Vì sao lặp qua generator lần thứ hai không báo lỗi mà chỉ trả rỗng?",
  "Viết một hàm bị sai khi nhận generator nhưng đúng khi nhận list. Hai cách sửa là gì?",
  "Vì sao nên viết `any(x > 0 for x in ds)` thay vì `any([x > 0 for x in ds])`?",
  "Trong `[(x,y) for x in a for y in b]`, `for` nào là vòng ngoài?",
  "Toán tử walrus giải quyết vấn đề gì trong comprehension? Khi nào không nên dùng?",
  "Nêu ba trường hợp nên dùng vòng lặp thường thay vì comprehension.",
  "Vì sao comprehension nhanh hơn vòng lặp `append`? Đó có phải lý do chính để dùng nó không?",
  "Mô tả một đường ống generator xử lý file 10 GB. Bộ nhớ dùng bao nhiêu và vì sao?",
  "Ở quy mô nào thì chọn list hay generator mới thật sự quan trọng?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Data Structures → List Comprehensions* và *Generator Expressions* — mô tả cú pháp chuẩn kèm ví dụ ngắn.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về iterable, iterator và generator là giải thích đầy đủ nhất về tính lười.",
  "Sách **Effective Python** — Brett Slatkin. Có hẳn một nhóm mục về comprehension và generator, gồm cả lời khuyên khi nào không nên dùng.",
  "Tài liệu Python, module `itertools` — bộ công cụ đi kèm generator: `islice`, `chain`, `groupby`, `count`.",
  "Sách **Python Cookbook** — David Beazley & Brian Jones. Chương về iterator và generator có nhiều công thức đường ống dữ liệu dùng được ngay."
],

lien_he: [
  "**List, dict, set, tuple** ngay trước: comprehension tạo ra đúng bốn loại đó; chọn loại nào vẫn theo tiêu chí bài trước.",
  "**Hàm, tham số mặc định** ngay sau: `lambda` trong `map`/`filter` là hàm ẩn danh, và bài đó nói kỹ hơn.",
  "**Generator, iterator và `yield`** trong module này: bài này giới thiệu dạng biểu thức, bài đó nói về giao thức iterator bên dưới.",
  "**Context manager và `with`**: đường ống generator đọc file cần `with` bên trong hàm sinh để đóng file đúng lúc.",
  "**Đọc ghi file, JSON, CSV**: xử lý file lớn theo dòng là ứng dụng trực tiếp của generator.",
  "**Compiled vs interpreted vs bytecode** ở module 1: comprehension nhanh hơn vì ít lệnh bytecode hơn, không phải vì \"tối ưu ma thuật\".",
  "**Stack vs heap** ở module 2: list comprehension cấp phát toàn bộ trên heap ngay; generator thì gần như không.",
  "**Giải thuật & CTDL** ở mảng 02: `any()` với generator là ví dụ đầu tiên về \"thoát sớm\" — ý tưởng nền của nhiều thuật toán tìm kiếm.",
  "**AI/ML** ở mảng 07: token hoá, xây từ vựng, đệm chuỗi đều là comprehension; và `Dataset`/`DataLoader` của các thư viện học sâu là đúng ý tưởng đường ống lười ở bài này."
]

};
