window.LESSONS["07:deep-learning:huan-luyen-tren-gpu-va-mixed-precision"] = {

tom_tat: [
  "GPU tăng tốc khi có nhiều phép toán tensor đủ lớn và song song; model nhỏ, batch nhỏ hoặc pipeline dữ liệu chậm có thể khiến GPU thua CPU.",
  "Model, đầu vào, nhãn và tensor mới tạo phải ở device tương thích. Chuyển dữ liệu qua CPU–GPU là công việc có chi phí, không phải thao tác miễn phí.",
  "Mixed precision dùng dtype thấp cho phép toán phù hợp nhưng giữ các phần nhạy cảm ở độ chính xác cao; autocast chọn phép toán, GradScaler bảo vệ gradient float16 quá nhỏ.",
  "Đo thời gian GPU phải đồng bộ vì kernel chạy bất đồng bộ. Throughput, latency, bộ nhớ đỉnh và độ chính xác đều cần được báo cùng cấu hình.",
  "Ví dụ tiếp tục huấn luyện GRU phân loại rung, chạy được cả CPU lẫn CUDA và chỉ bật AMP khi backend hỗ trợ."
],

can_biet_truoc: [
  "Vòng lặp PyTorch gồm forward, loss, backward và optimizer step.",
  "Tensor shape, dtype, device, autograd và kích thước batch.",
  "Số dấu phẩy động, overflow, underflow và sai số làm tròn ở mức trực giác.",
  "Khái niệm throughput, latency, RAM/VRAM và pipeline nạp dữ liệu."
],

dinh_nghia: "**Huấn luyện trên GPU** là đặt tham số, activation và phần lớn phép toán tensor lên bộ xử lý có nhiều đơn vị tính toán song song. Lợi ích đến từ nhân ma trận, convolution và các kernel xử lý nhiều phần tử đồng thời; GPU không làm từng câu lệnh Python tự nhiên nhanh hơn.\n\n**Mixed precision (độ chính xác hỗn hợp)** cho phép một số operation chạy bằng kiểu số ít bit hơn như float16 hoặc bfloat16 để giảm bộ nhớ và tăng thông lượng, trong khi operation nhạy cảm và bản trọng số cần thiết vẫn dùng float32. Trong PyTorch, autocast chọn dtype theo operation; với float16, GradScaler thường nhân loss để gradient nhỏ không bị làm tròn về 0 trước backward.",

vi_sao_can: "GRU của bài trước phải xử lý hàng triệu cửa sổ rung. Mỗi bước thời gian thực hiện các phép nhân ma trận giống nhau cho cả batch; đây là dạng công việc GPU giỏi. Batch lớn hơn cũng làm ước lượng gradient ổn định hơn trong một số cấu hình và giảm thời gian chờ huấn luyện.\n\nNhưng “có GPU” không đảm bảo nhanh. Nếu DataLoader mất 40 ms đọc và chỉ 2 ms tính, tăng tốc tính xuống 1 ms gần như không thay đổi tổng. Nếu mỗi batch liên tục chuyển tensor nhỏ qua bus, overhead lấn át. Mixed precision còn đổi số học: float16 có miền biểu diễn hẹp, có thể tràn hoặc mất gradient. Do đó bài toán là thiết kế toàn pipeline và chứng minh bằng đo, không chỉ gọi `.cuda()`.",

co_che: "### Vì sao GPU hợp với deep learning\n\nCPU có ít lõi mạnh, cache lớn và tối ưu luồng điều khiển phức tạp. GPU có rất nhiều đơn vị tính toán hướng tới cùng thao tác trên nhiều phần tử. Nhân ma trận của GRU áp một quy tắc cho mọi mẫu và hidden unit, tạo parallelism dày. Kernel là hàm chạy trên GPU; khởi chạy kernel có overhead nên hàng nghìn phép toán vụn chưa chắc hiệu quả.\n\nTốc độ còn phụ thuộc arithmetic intensity: lượng tính toán trên mỗi byte đọc. Operation chỉ cộng một vector có thể bị giới hạn bởi băng thông bộ nhớ; matrix multiplication lớn tái dùng dữ liệu và thường tận dụng phần cứng tốt hơn.\n\n### Device là một phần của kiểu dữ liệu\n\nTensor CPU và tensor CUDA không thể tham gia hầu hết operation chung. `model.to(device)` di chuyển parameter và buffer đã đăng ký; nó không tự di chuyển batch do DataLoader trả. Mỗi iteration phải đưa cả `xb` và `yb` tới device. Tensor tạo mới trong forward nên dùng `x.new_zeros(...)` hoặc chỉ định `device=x.device`, nếu không mặc định CPU gây lỗi giữa chừng.\n\nGọi `.to()` trả tensor/module đã chuyển theo semantics tương ứng; với tensor cần gán kết quả. Hard-code `cuda:0` làm code khó chạy trên CPU và môi trường khác. Chọn device một lần tại biên chương trình rồi truyền rõ ràng.\n\n### Chi phí truyền qua bus\n\nCPU RAM và GPU VRAM là hai miền bộ nhớ. Sao chép batch qua kết nối có latency và băng thông hữu hạn. Giữ model trên GPU xuyên suốt epoch, gom dữ liệu thành batch đủ lớn, tránh kéo logits về CPU từng mẫu. Chỉ chuyển số nhỏ để log khi cần; `.item()` cũng có thể buộc đồng bộ.\n\n`pin_memory=True` cấp vùng nhớ host khóa trang để truyền tới CUDA hiệu quả hơn; `non_blocking=True` có thể cho phép chồng truyền với tính toán khi điều kiện phù hợp. Hai cờ không bảo đảm nhanh và pin quá nhiều làm áp lực RAM. Phải benchmark pipeline thật.\n\n### DataLoader có thể bỏ đói GPU\n\nQuan sát utilization thấp không đủ kết luận model nhỏ; CPU có thể đang giải mã, augment hoặc đọc ổ đĩa. Tăng `num_workers`, cache hợp lý và tránh Python transform theo từng điểm là các hướng thử. Nhưng worker quá nhiều tranh CPU/RAM và tốn khởi tạo, đặc biệt theo hệ điều hành.\n\nĐo thời gian chờ batch riêng với thời gian compute. Profiler hoặc timeline cho thấy khoảng trống giữa kernel. Prefetch chỉ hữu ích khi có công việc để chồng lấp.\n\n### Float32, float16 và bfloat16\n\nFloat32 có nhiều bit mantissa và exponent hơn, cho độ chính xác và miền giá trị rộng. Float16 giảm một nửa bộ nhớ mỗi phần tử và có thể dùng đơn vị tính ma trận chuyên dụng, nhưng exponent hẹp nên dễ overflow/underflow. Bfloat16 giữ số bit exponent gần float32 nhưng mantissa ngắn, nên miền rộng hơn nhưng làm tròn thô hơn.\n\n“Ít bit” không đồng nghĩa model luôn kém chính xác. Deep learning chịu nhiễu nhất định và nhiều operation tích lũy ở float32. Tuy vậy softmax, reduction hoặc chuẩn hóa có thể nhạy; autocast duy trì danh sách/chính sách operation thay vì ép toàn model bằng `.half()`.\n\n### Autocast làm gì và không làm gì\n\nAutocast là context bao quanh forward và loss. Nó chọn dtype theo operation và backend. Nó không thay parameter lưu trữ thành một dtype duy nhất theo cách người dùng phải tự quản; cũng không sửa input chứa `nan`, learning rate quá cao hay mô hình bất ổn.\n\nBackward nên diễn ra ngoài context autocast theo mẫu hướng dẫn, vì backward dùng dtype được chọn từ forward. Không bọc optimizer step trong autocast. Khi inference, autocast vẫn có thể giảm chi phí nhưng không cần GradScaler vì không có gradient.\n\n### Loss scaling chống underflow float16\n\nGradient nhỏ như 10⁻⁸ có thể thành 0 trong float16. Nhân loss với hệ số S làm gradient trung gian lớn lên S lần, sau đó chia trở lại trước cập nhật. GradScaler tự tăng scale khi ổn định và giảm khi phát hiện `inf`/`nan`; nếu gradient không hữu hạn, nó bỏ qua optimizer step để tránh làm hỏng weight.\n\nThứ tự chuẩn là `scaler.scale(loss).backward()`, `scaler.unscale_(optimizer)` nếu cần clip, rồi `scaler.step(optimizer)` và `scaler.update()`. Clip trước unscale sẽ clip gradient đã nhân, làm ngưỡng mất ý nghĩa.\n\nBfloat16 thường không cần loss scaling vì miền exponent rộng, nhưng quyết định phụ thuộc backend. Không bật scaler máy móc cho mọi dtype.\n\n### Master weights và optimizer state\n\nOptimizer cần cập nhật đủ chính xác; hệ AMP thường giữ parameter/optimizer state phù hợp thay vì ép mọi thứ float16. Adam có thêm moment nên VRAM không chỉ gồm weight. Bộ nhớ huấn luyện gồm parameter, gradient, optimizer state, activation và workspace kernel; activation thường tăng theo batch và độ dài chuỗi.\n\nMixed precision giảm activation và đôi khi gradient, cho phép batch lớn hơn. Nhưng dùng toàn bộ phần VRAM vừa đủ sát ngưỡng dễ OOM do batch cuối khác shape, workspace hoặc fragmentation.\n\n### Batch size thay đổi bài toán tối ưu\n\nTăng batch để lấp GPU thay đổi số optimizer step trên mỗi epoch và nhiễu gradient. Không thể giữ mọi hyperparameter rồi coi kết quả tương đương. Gradient accumulation mô phỏng batch lớn về tổng gradient nhưng BatchNorm và một số stochastic behavior vẫn khác.\n\nVới GRU chuỗi 120, tăng hidden size và batch tăng activation. Tìm batch tối đa không phải mục tiêu; tìm throughput tốt trong giới hạn độ chính xác và latency mới đúng.\n\n### Đo thời gian bất đồng bộ\n\nLệnh CUDA thường xếp công việc vào queue rồi trả về trước khi GPU xong. `time.perf_counter()` quanh forward mà không synchronize có thể chỉ đo thời gian enqueue. Dùng CUDA events hoặc `torch.cuda.synchronize()` trước và sau đoạn đo. Synchronize làm mất overlap nên chỉ đặt ở ranh giới benchmark, không chèn vào vòng train thường.\n\nWarm-up vài iteration để loại chi phí khởi tạo context, chọn kernel và cache. Báo median hoặc phân vị qua nhiều lần, cùng batch, shape, dtype và phần cứng. Không ghi con số từ máy khác như sự thật phổ quát.\n\n### Throughput khác latency\n\nThroughput là mẫu mỗi giây khi batch liên tục; latency là thời gian một yêu cầu. Batch lớn thường tăng throughput nhưng buộc yêu cầu chờ gom batch. Huấn luyện ưu tiên throughput; cảnh báo rung trực tuyến có thể ưu tiên latency. Benchmark phải khớp mục tiêu triển khai.\n\nEnd-to-end time gồm nạp dữ liệu, truyền, compute, metric và checkpoint. Kernel time đẹp không cứu pipeline ghi log đồng bộ mỗi batch.\n\n### Quản lý OOM có hệ thống\n\nKhi OOM, giảm batch, chiều dài, hidden size; bật mixed precision; gradient checkpointing; hoặc accumulation. `empty_cache()` chỉ trả cache allocator chưa dùng cho hệ khác, không giải phóng tensor còn tham chiếu và không phải thuốc chữa memory leak.\n\nNếu VRAM tăng mỗi iteration, tìm list đang giữ loss tensor có graph, `retain_graph=True`, hidden state chưa detach hoặc metric tích lũy tensor GPU. Lưu `loss.item()` thay vì lưu `loss` khi chỉ cần số.\n\n### Tính tái lập và kernel\n\nGPU có operation song song mà thứ tự cộng có thể khác, nên kết quả bit-for-bit không luôn mặc định. Seed giúp nhưng chưa đủ. Chế độ deterministic có thể giảm tốc hoặc không hỗ trợ một operation. Phân biệt reproducibility thống kê với tái lập từng bit và ghi yêu cầu dự án.\n\nMixed precision làm sai số khác float32; so metric cuối qua nhiều seed và kiểm tra loss hữu hạn. Sai khác nhỏ không tự động là bug.\n\n### Khi nào không nên dùng GPU hoặc AMP\n\nModel nhỏ và dữ liệu ít có thể chạy CPU nhanh hơn do overhead. Pipeline cần latency một mẫu trên máy không có accelerator nên tối ưu GPU không liên quan. Custom operation chưa an toàn ở dtype thấp, bài toán số học nhạy, hoặc AMP làm metric giảm ngoài dung sai là lý do giữ float32.\n\nKhông ép mixed precision chỉ để theo xu hướng. Nó là tối ưu có điều kiện: phải chứng minh nhanh hơn, vừa bộ nhớ hơn và giữ chất lượng trong tiêu chí nghiệp vụ.",

vi_du: "### Vòng lặp chạy CPU hoặc CUDA\n\n```python\nimport torch\nfrom torch import nn\nfrom torch.utils.data import DataLoader, TensorDataset\n\ndevice = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\nX = torch.randn(256, 120, 3)\ny = torch.randint(0, 3, (256,), dtype=torch.long)\nloader = DataLoader(TensorDataset(X, y), batch_size=32,\n                    shuffle=True, pin_memory=(device.type == \"cuda\"))\n\nclass Model(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.gru = nn.GRU(3, 24, batch_first=True)\n        self.head = nn.Linear(24, 3)\n    def forward(self, x):\n        _, h = self.gru(x)\n        return self.head(h[-1])\n\nmodel = Model().to(device)\nopt = torch.optim.AdamW(model.parameters(), lr=1e-3)\nloss_fn = nn.CrossEntropyLoss()\nuse_amp = device.type == \"cuda\"\nscaler = torch.amp.GradScaler(\"cuda\", enabled=use_amp)\n\nmodel.train()\nfor xb, yb in loader:\n    xb = xb.to(device, non_blocking=use_amp)\n    yb = yb.to(device, non_blocking=use_amp)\n    opt.zero_grad(set_to_none=True)\n    with torch.autocast(device_type=device.type, dtype=torch.float16,\n                        enabled=use_amp):\n        loss = loss_fn(model(xb), yb)\n    scaler.scale(loss).backward()\n    scaler.unscale_(opt)\n    nn.utils.clip_grad_norm_(model.parameters(), 1.0)\n    scaler.step(opt)\n    scaler.update()\nassert torch.isfinite(loss)\nprint(device, float(loss))\n```\n\nTrên CPU, autocast và scaler bị tắt nên cùng code vẫn chạy. Việc chọn float16 chỉ khi CUDA tránh giả định backend CPU hỗ trợ y hệt.\n\n### Benchmark có đồng bộ\n\n```python\nimport time\n\ndef measure(model, xb, repeats=30):\n    model.eval()\n    with torch.inference_mode():\n        for _ in range(5):\n            model(xb)\n        if xb.is_cuda:\n            torch.cuda.synchronize()\n        t0 = time.perf_counter()\n        for _ in range(repeats):\n            model(xb)\n        if xb.is_cuda:\n            torch.cuda.synchronize()\n    return (time.perf_counter() - t0) / repeats\n\nxb = X[:32].to(device)\nseconds = measure(model, xb)\nprint(\"giây mỗi batch trên máy hiện tại:\", seconds)\n```\n\nCode in số đo thật trên môi trường chạy, không khẳng định trước GPU nhanh bao nhiêu.\n\n### Kiểm tra mọi tham số cùng device\n\n```python\ndevices = {p.device for p in model.parameters()}\nassert devices == {device}\nassert model(xb).device == device\n```\n\nCa batch rỗng không có ý nghĩa cho loss mean và nên bị Dataset/DataLoader loại. Batch một mẫu chạy được với GRU; nếu thêm BatchNorm cần xử lý riêng chế độ train.",

so_sanh: "### CPU và GPU theo loại tải\n\n| Tải | CPU | GPU | Lý do chính |\n|---|---|---|---|\n| model nhỏ, batch 1 | thường cạnh tranh | overhead có thể lấn át | ít parallelism |\n| ma trận lớn, batch đều | chậm hơn thường thấy | phù hợp | nhiều phép toán đồng dạng |\n| tiền xử lý Python/đọc file | linh hoạt | thường không giải quyết | nghẽn ngoài kernel |\n| huấn luyện cần VRAM lớn | dùng RAM lớn hơn | có thể OOM | VRAM hữu hạn |\n\n### Ba chế độ số học\n\n| Chế độ | Bộ nhớ | Miền số | Cơ chế bảo vệ | Khi ưu tiên |\n|---|---:|---|---|---|\n| float32 | cao | rộng | không cần scaler | baseline, bài nhạy số |\n| AMP float16 | thấp hơn | hẹp | autocast + GradScaler | GPU hỗ trợ và đã kiểm chất lượng |\n| AMP bfloat16 | thấp hơn | exponent rộng | thường không scaler | phần cứng hỗ trợ bfloat16 tốt |\n\nKhông gán hệ số tăng tốc cố định vì phụ thuộc kiến trúc, shape, kernel và phần cứng.",

loi_thuong_gap: [
  "Chuyển model lên GPU nhưng quên batch hoặc nhãn. Lỗi báo tensor ở hai device; kiểm device tại ranh giới DataLoader.",
  "Tạo hidden state bằng `torch.zeros` mặc định CPU. Dùng `x.new_zeros` hoặc truyền `device=x.device`.",
  "Đo CUDA bằng đồng hồ CPU không synchronize. Thời gian quá nhỏ và dao động; warm-up rồi dùng event hoặc synchronize.",
  "Ép toàn model `.half()` thay vì autocast. Operation nhạy có thể overflow; giữ baseline float32 và dùng policy autocast.",
  "Clip gradient trước `scaler.unscale_`. Ngưỡng áp lên gradient đã nhân; unscale trước rồi clip.",
  "Gọi `.item()` nhiều lần mỗi batch. Mỗi lần có thể đồng bộ CPU–GPU và phá overlap; gom logging thưa hơn.",
  "Dùng `empty_cache()` để chữa leak. Tensor còn tham chiếu vẫn chiếm VRAM; tìm graph bị giữ hoặc list chứa tensor.",
  "Tăng batch chỉ để đầy GPU mà không chỉnh đánh giá tối ưu. Số step và nhiễu gradient đổi; so chất lượng cùng ngân sách rõ ràng."
],

tu_kiem_tra: [
  "Vì sao GPU có thể chậm hơn CPU với GRU rất nhỏ và batch một mẫu?",
  "Autocast khác việc gọi `.half()` toàn model ở quyết định dtype theo operation như thế nào?",
  "Giải thích loss scaling: nhân loss giúp gradient float16, rồi vì sao phải unscale trước clipping?",
  "Tại sao `perf_counter` quanh kernel CUDA có thể đo sai nếu thiếu synchronize?",
  "Một GPU utilization 20% có thể do ba nghẽn nào ngoài compute?",
  "Phân biệt throughput và latency; batch lớn tác động ngược nhau lên hai metric ra sao?",
  "VRAM tăng đều qua iteration dù batch cố định: lập thứ tự kiểm tra tham chiếu graph, hidden state và logging."
],

tai_nguyen: [
  "PyTorch Tutorials — Automatic Mixed Precision examples và Performance Tuning Guide.",
  "Deep Learning with PyTorch — phần sử dụng GPU và tối ưu vòng huấn luyện.",
  "Dive into Deep Learning — Computational Performance.",
  "Programming Massively Parallel Processors — David Kirk và Wen-mei Hwu; nền tảng mô hình thực thi GPU."
],

lien_he: [
  "Nối sang Kiến trúc máy tính: locality, băng thông bộ nhớ và arithmetic intensity giải thích vì sao cùng số FLOP có thời gian khác nhau.",
  "Nối sang Hệ điều hành: pinned memory và nhiều DataLoader worker dùng tài nguyên host; tăng worker vô hạn gây tranh chấp thay vì tăng tốc.",
  "Nối sang Hệ phân tán: multi-GPU thêm truyền gradient và đồng bộ, nên tốc độ không tăng tuyến tính chỉ bằng nhân số GPU.",
  "Nối sang Kỹ nghệ phần mềm: benchmark cần warm-up, cấu hình cố định và metric end-to-end, giống một thí nghiệm hiệu năng có thể tái lập."
]

};
