window.LESSONS["04:thiet-ke-csdl:rang-buoc-not-null-unique-check-foreign-key"] = {

tom_tat: [
  "Ràng buộc là lớp bảo vệ cuối cùng của dữ liệu: mọi đường ghi, kể cả SQL thủ công, job nhập dữ liệu và phiên bản ứng dụng cũ, đều phải vượt qua cùng một luật ở PostgreSQL.",
  "`NOT NULL` buộc giá trị phải hiện diện; `CHECK` kiểm một mệnh đề trên từng hàng; `UNIQUE` ngăn các khóa nghiệp vụ trùng nhau; `FOREIGN KEY` giữ tham chiếu giữa bảng con và bảng cha không bị mồ côi.",
  "Mỗi loại có ngữ nghĩa riêng với `NULL`: `CHECK` chỉ bác bỏ khi biểu thức là `FALSE`, còn `UNIQUE` mặc định có thể chứa nhiều `NULL`. Vì vậy ghép luật sai sẽ tạo lỗ hổng dù câu lệnh trông hợp lý.",
  "Ràng buộc không miễn phí: `UNIQUE` cần index, `FOREIGN KEY` phải dò bảng cha và thao tác xóa ở bảng cha cần index trên cột tham chiếu của bảng con để tránh quét toàn bảng.",
  "Thêm ràng buộc vào bảng đang phục vụ phải là một migration có kế hoạch: khảo sát dữ liệu bẩn, sửa dữ liệu, dùng `NOT VALID` khi phù hợp, `VALIDATE CONSTRAINT`, rồi mới siết đường ghi."
],

can_biet_truoc: [
  "Khóa chính, khóa ứng viên và khóa ngoại trong mô hình quan hệ.",
  "Giá trị `NULL` và logic ba trị `TRUE`, `FALSE`, `UNKNOWN` của SQL.",
  "Transaction, `COMMIT`, `ROLLBACK` và việc PostgreSQL kiểm tra câu lệnh trong transaction.",
  "Index B-tree và cách đọc các nút `Seq Scan`, `Index Scan` trong `EXPLAIN`."
],

dinh_nghia: "**Ràng buộc (constraint)** là một mệnh đề khai báo trong schema mà PostgreSQL phải duy trì cho mọi trạng thái dữ liệu đã được chấp nhận. Nếu một câu lệnh làm mệnh đề sai, câu lệnh hoặc transaction bị từ chối thay vì lưu trạng thái không hợp lệ.\n\nTrong hệ thống đặt vé xuyên suốt module, bốn nhóm luật trả lời bốn câu hỏi khác nhau: `orders.customer_id NOT NULL` nói đơn nào cũng phải chỉ ra khách; `CHECK (quantity > 0)` nói số vé phải dương; `UNIQUE (event_id, seat_code)` nói một ghế chỉ xuất hiện một lần trong một sự kiện; `FOREIGN KEY (customer_id) REFERENCES customers(id)` nói mã khách phải thật sự tồn tại. Chúng là mô tả có thể thực thi của bất biến nghiệp vụ, không phải chú thích dành riêng cho lập trình viên.",

vi_sao_can: "Kiểm tra ở giao diện giúp báo lỗi thân thiện, nhưng giao diện không phải nơi duy nhất ghi dữ liệu. Một worker xử lý hàng đợi, lệnh sửa nóng của quản trị viên, file CSV nhập lại, hoặc hai request chạy đồng thời đều có thể đi vòng qua kiểm tra ứng dụng. Nếu luật chỉ nằm trong service, độ đúng của cơ sở dữ liệu phụ thuộc vào việc mọi tác giả code ở mọi thời điểm nhớ lặp lại cùng một điều kiện.\n\nVí dụ hai tiến trình cùng đọc rằng ghế `A-01` còn trống rồi cùng `INSERT`. Mẫu “đọc trước, ghi sau” ở application có race condition: cả hai lần đọc đều hợp lệ trước khi bất kỳ lần ghi nào hoàn tất. `UNIQUE (event_id, seat_code)` giải quyết ở đúng nơi PostgreSQL tuần tự hóa thay đổi index; một transaction thắng, transaction còn lại nhận lỗi. Tương tự, `FOREIGN KEY` ngăn đơn hàng tham chiếu khách vừa bị xóa bởi transaction khác, điều mà một lần `SELECT` kiểm tra trước không thể đảm bảo lâu dài.\n\nĐổi lại, constraint làm lỗi xuất hiện tại biên ghi và có thể giữ lock khi migration. Mục tiêu không phải dồn mọi luật vào SQL. Luật liên hàng phức tạp, luật phụ thuộc thời gian hiện tại hoặc dịch vụ bên ngoài thường cần transaction, trigger có cân nhắc, hay tầng nghiệp vụ. Constraint phù hợp nhất với luật cục bộ, ổn định và có thể diễn đạt tất định từ dữ liệu trong database.",

co_che: "### Một schema đặt vé có luật ngay tại nơi lưu\n\nTa dùng cùng mô hình cho cả hai bài còn thiếu. `customers` sở hữu đơn, `events` giới hạn sức chứa, `orders` ghi trạng thái thanh toán, còn `tickets` gán ghế cho sự kiện.\n\n```sql\nDROP TABLE IF EXISTS tickets, orders, events, customers CASCADE;\n\nCREATE TABLE customers (\n    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    email text NOT NULL,\n    display_name text NOT NULL,\n    CONSTRAINT customers_email_nonblank CHECK (btrim(email) <> ''),\n    CONSTRAINT customers_email_unique UNIQUE (email)\n);\n\nCREATE TABLE events (\n    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    title text NOT NULL,\n    capacity integer NOT NULL CHECK (capacity > 0),\n    starts_at timestamptz NOT NULL\n);\n\nCREATE TABLE orders (\n    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    customer_id bigint NOT NULL,\n    status text NOT NULL DEFAULT 'pending',\n    total_cents bigint NOT NULL DEFAULT 0,\n    created_at timestamptz NOT NULL DEFAULT clock_timestamp(),\n    CONSTRAINT orders_status_allowed\n        CHECK (status IN ('pending', 'paid', 'cancelled')),\n    CONSTRAINT orders_total_nonnegative CHECK (total_cents >= 0),\n    CONSTRAINT orders_customer_fk FOREIGN KEY (customer_id)\n        REFERENCES customers(id) ON DELETE RESTRICT\n);\n\nCREATE TABLE tickets (\n    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    order_id bigint NOT NULL REFERENCES orders(id) ON DELETE CASCADE,\n    event_id bigint NOT NULL REFERENCES events(id) ON DELETE RESTRICT,\n    seat_code text NOT NULL CHECK (seat_code ~ '^[A-Z]+-[0-9]{2}$'),\n    price_cents integer NOT NULL CHECK (price_cents >= 0),\n    CONSTRAINT one_seat_per_event UNIQUE (event_id, seat_code)\n);\n```\n\nTên constraint có ích khi lỗi đi qua log: `orders_total_nonnegative` truyền đạt nguyên nhân tốt hơn tên tự sinh. Schema cũng cố ý không dùng `money`; số tiền theo đơn vị nhỏ nhất tránh phụ thuộc locale và cho phép `CHECK` bằng số nguyên.\n\n### `NOT NULL`: sự hiện diện là một phần của kiểu dữ liệu\n\n`NOT NULL` được PostgreSQL lưu như thuộc tính của cột. Nó ngăn cả việc bỏ cột trong `INSERT` khi không có default lẫn gán rõ `NULL`. Chuỗi rỗng vẫn là một giá trị, nên `email text NOT NULL` không ngăn `''`; ta cần `CHECK (btrim(email) <> '')` nếu chuỗi chỉ có khoảng trắng cũng vô nghĩa.\n\nKhông nên thêm `NOT NULL` chỉ vì màn hình hiện tại bắt buộc nhập. `cancelled_at` hợp lý khi để `NULL` cho đơn chưa hủy; ép một ngày giả như `1970-01-01` làm mất khả năng phân biệt “chưa xảy ra” với “đã xảy ra vào ngày mặc định”. Ngược lại, cột thật sự bắt buộc mà vẫn nullable khiến mọi query sau phải mang thêm nhánh xử lý không cần thiết.\n\n### `CHECK` và chiếc bẫy logic ba trị\n\nPostgreSQL chấp nhận hàng khi biểu thức `CHECK` là `TRUE` **hoặc `UNKNOWN`**; chỉ `FALSE` bị bác bỏ. Vì `NULL > 0` cho `UNKNOWN`, riêng `CHECK (capacity > 0)` không thay thế `NOT NULL`. Hai constraint diễn đạt hai ý độc lập: có giá trị, và giá trị thuộc miền hợp lệ.\n\n`CHECK` nên dựa trên cột của chính hàng và biểu thức bất biến. Không nên viết kiểm tra dựa vào `now()` để yêu cầu sự kiện luôn ở tương lai: một hàng hợp lệ hôm nay sẽ tự thành “sai” ngày mai mà không có câu lệnh nào kích hoạt kiểm tra. PostgreSQL cũng không thiết kế `CHECK` để truy vấn bảng khác; quan hệ tồn tại thuộc về `FOREIGN KEY`, còn tổng số vé không vượt capacity là bất biến nhiều hàng cần chiến lược transaction/locking riêng.\n\n### `UNIQUE`: ràng buộc được chống lưng bởi index\n\nKhai báo `UNIQUE (event_id, seat_code)` tạo một unique B-tree index. Khi chèn vé, PostgreSQL dò khóa kết hợp trong index; nếu đã có một entry nhìn thấy theo quy tắc đồng thời, việc chèn bị chặn hoặc thất bại. Đây là lý do constraint xử lý được cuộc đua mà `SELECT` trước `INSERT` không xử lý được.\n\nThứ tự cột cũng ảnh hưởng truy vấn. Index `(event_id, seat_code)` phục vụ tốt `WHERE event_id = 7` và `WHERE event_id = 7 AND seat_code = 'A-01'`, nhưng thường không tối ưu cho chỉ `WHERE seat_code = 'A-01'` vì cột đầu không được cố định. Ta chọn thứ tự này vì nghiệp vụ hỏi ghế trong một sự kiện, không hỏi mã ghế trên mọi sự kiện.\n\nTheo mặc định, nhiều `NULL` không bị xem là trùng. Nếu nghiệp vụ muốn “chỉ một hàng chưa có mã”, PostgreSQL có cú pháp `UNIQUE NULLS NOT DISTINCT`; còn cách tương thích với thiết kế cũ là partial unique index. Tuy nhiên ví dụ đặt vé đã dùng `NOT NULL`, nên ngữ nghĩa đó không còn mơ hồ.\n\n### `FOREIGN KEY`: tồn tại, hành động xóa và thời điểm kiểm\n\nKhóa ngoại đảm bảo mỗi `orders.customer_id` khớp một `customers.id`. Nó không tự tạo index trên cột ở bảng con. PostgreSQL đã có index của khóa chính ở bảng cha để kiểm lúc chèn đơn; nhưng khi xóa một khách, hệ quản trị phải tìm mọi đơn tham chiếu. Thiếu index `orders(customer_id)`, thao tác này có thể `Seq Scan` toàn bộ `orders` và giữ lock lâu.\n\n```sql\nCREATE INDEX orders_customer_id_idx ON orders(customer_id);\nCREATE INDEX tickets_order_id_idx ON tickets(order_id);\nCREATE INDEX tickets_event_id_idx ON tickets(event_id);\n```\n\n`ON DELETE RESTRICT` từ chối xóa khách hoặc sự kiện còn dữ liệu lịch sử. `ON DELETE CASCADE` ở `tickets.order_id` nói vé là thành phần sống chết cùng đơn. `SET NULL` chỉ hợp lệ khi cột cho phép `NULL` và nghiệp vụ chấp nhận mất liên kết. Chọn action là quyết định vòng đời dữ liệu; dùng `CASCADE` cho tiện có thể biến một lệnh xóa cha thành vụ xóa hàng loạt khó phục hồi.\n\n### Constraint tức thời và constraint hoãn đến `COMMIT`\n\nPhần lớn ràng buộc được kiểm cuối mỗi câu lệnh. Một `FOREIGN KEY`, `UNIQUE`, `PRIMARY KEY` hoặc `EXCLUDE` có thể khai báo `DEFERRABLE`, rồi hoãn kiểm đến cuối transaction. Điều này hữu ích khi đổi khóa giữa hai hàng hoặc nhập một đồ thị tham chiếu theo thứ tự tạm thời chưa hợp lệ.\n\n```sql\nALTER TABLE orders\n  DROP CONSTRAINT orders_customer_fk,\n  ADD CONSTRAINT orders_customer_fk\n  FOREIGN KEY (customer_id) REFERENCES customers(id)\n  ON DELETE RESTRICT DEFERRABLE INITIALLY IMMEDIATE;\n\nBEGIN;\nSET CONSTRAINTS orders_customer_fk DEFERRED;\n-- các thay đổi trung gian có thể chưa khớp; trạng thái tại COMMIT phải hợp lệ\nCOMMIT;\n```\n\nHoãn kiểm không có nghĩa là bỏ kiểm. Nó chỉ dời thời điểm phát hiện lỗi, khiến câu lệnh gây lỗi xa vị trí `COMMIT`; vì thế không nên bật mặc định nếu không có workflow thật sự cần trạng thái trung gian.\n\n### Cạnh tranh: vì sao “kiểm rồi chèn” vẫn thua\n\nGiả sử hai request cùng chạy `SELECT 1 FROM tickets WHERE event_id=1 AND seat_code='A-01'`, đều không thấy hàng. Nếu ứng dụng tin kết quả đó rồi chèn, cả hai có thể đi tiếp. Unique index buộc hai transaction cạnh tranh trên cùng khóa. Một transaction hoàn tất; transaction kia nhận `unique_violation`. Ứng dụng phải coi lỗi này là kết quả nghiệp vụ “ghế vừa được người khác lấy”, không phải lỗi máy chủ bí ẩn để retry vô hạn.\n\nMẫu tốt là thử ghi và xử lý xung đột, chẳng hạn `INSERT ... ON CONFLICT (event_id, seat_code) DO NOTHING RETURNING id`. Số hàng trả về bằng không cho biết đã mất cuộc đua. Cơ sở dữ liệu trở thành trọng tài duy nhất thay vì cố khóa bằng biến trong từng instance service.\n\n### Query plan và chi phí ẩn của tính đúng\n\nConstraint làm đường ghi tốn thêm tra cứu, nhưng index của `UNIQUE` cũng tăng tốc đường đọc tương ứng. Có thể quan sát bằng:\n\n```sql\nEXPLAIN (COSTS OFF)\nSELECT id FROM tickets\nWHERE event_id = 1 AND seat_code = 'A-01';\n\nEXPLAIN (COSTS OFF)\nDELETE FROM customers WHERE id = 1;\n```\n\nVới bảng rất nhỏ, planner có thể chọn `Seq Scan` vì đọc một trang rẻ hơn đi qua index; đó không chứng minh index vô dụng. Khi bảng con lớn, index trên khóa ngoại tránh quét rộng để kiểm hành động xóa/cập nhật bảng cha. Không nên ghi thời gian cố định nếu chưa đo trên máy và dữ liệu cụ thể; plan, số buffer và số hàng mới là bằng chứng có thể tái lập.\n\n### Migration không khóa dịch vụ một cách liều lĩnh\n\nThêm constraint vào dữ liệu cũ có hai bài toán: dữ liệu hiện tại có sạch không, và việc xác minh giữ lock bao lâu. Với `CHECK` và `FOREIGN KEY`, có thể thêm `NOT VALID`: PostgreSQL áp luật cho hàng mới nhưng chưa quét xác nhận toàn bộ hàng cũ. Sau khi sửa dữ liệu, `VALIDATE CONSTRAINT` thực hiện pha xác minh với mức khóa nhẹ hơn việc thêm và kiểm ngay.\n\n```sql\nALTER TABLE orders ADD CONSTRAINT orders_total_nonnegative_v2\n  CHECK (total_cents >= 0) NOT VALID;\n\nSELECT id, total_cents FROM orders WHERE total_cents < 0;\n-- sửa hoặc cách ly các hàng được tìm thấy\nALTER TABLE orders VALIDATE CONSTRAINT orders_total_nonnegative_v2;\n```\n\n`NOT NULL` có lộ trình riêng: tìm `NULL`, backfill theo lô, chặn đường ghi mới, rồi `SET NOT NULL`. Trên hệ thống lớn cần thử migration với bản sao có phân bố dữ liệu gần production và đặt `lock_timeout`, vì câu DDL chờ lock có thể xếp hàng các request phía sau.\n\n### Khi nào không nên dùng constraint cho toàn bộ luật\n\nKhông dùng `CHECK` để gọi API tỷ giá, đọc bảng cấu hình khác hay so với đồng hồ hiện tại. Không dùng `FOREIGN KEY` xuyên hai database độc lập vì engine không thể duy trì nguyên tử qua ranh giới đó. Không nhồi quy trình “đơn paid thì phải có ít nhất một vé và tổng phải bằng tổng giá vé” vào một biểu thức cột; đây là luật nhiều hàng, nên transaction khóa đúng tập hàng và cập nhật có thứ tự rõ ràng.\n\nConstraint vẫn giữ phần nền: trạng thái thuộc tập cho phép, số tiền không âm, tham chiếu tồn tại, ghế không trùng. Tầng nghiệp vụ điều phối phần giàu ngữ cảnh. Ranh giới này cho tính đúng mạnh mà schema vẫn hiểu và vận hành được.",

vi_du: "### Kịch bản PostgreSQL tự chứng minh từng luật\n\nChạy schema ở trên trước, rồi chạy transaction sau trong `psql`. Các câu lệnh hợp lệ được giữ; savepoint cho phép quan sát từng lỗi mà không làm hỏng toàn bộ buổi thử.\n\n```sql\nINSERT INTO customers(email, display_name)\nVALUES ('lan@example.test', 'Lan') RETURNING id;\n\nINSERT INTO events(title, capacity, starts_at)\nVALUES ('Đêm nhạc nhỏ', 2, '2030-05-01 19:30+07') RETURNING id;\n\nINSERT INTO orders(customer_id, status, total_cents)\nVALUES (1, 'pending', 250000) RETURNING id;\n\nINSERT INTO tickets(order_id, event_id, seat_code, price_cents)\nVALUES (1, 1, 'A-01', 250000) RETURNING id;\n\nBEGIN;\nSAVEPOINT s1;\nINSERT INTO orders(customer_id, status, total_cents)\nVALUES (NULL, 'pending', 0);             -- not_null_violation\nROLLBACK TO s1;\n\nSAVEPOINT s2;\nINSERT INTO orders(customer_id, status, total_cents)\nVALUES (1, 'shipping', 0);               -- check_violation\nROLLBACK TO s2;\n\nSAVEPOINT s3;\nINSERT INTO tickets(order_id, event_id, seat_code, price_cents)\nVALUES (1, 1, 'A-01', 250000);           -- unique_violation\nROLLBACK TO s3;\n\nSAVEPOINT s4;\nINSERT INTO orders(customer_id, status, total_cents)\nVALUES (999999, 'pending', 0);            -- foreign_key_violation\nROLLBACK TO s4;\nCOMMIT;\n```\n\n### Ca biên `NULL`, chuỗi trắng và xóa dây chuyền\n\n```sql\n-- CHECK đơn độc không bắt NULL: bảng tạm minh họa logic ba trị\nCREATE TEMP TABLE demo_check (n integer CHECK (n > 0));\nINSERT INTO demo_check VALUES (NULL);     -- hợp lệ: biểu thức UNKNOWN\nINSERT INTO demo_check VALUES (1);        -- hợp lệ\n\n-- Schema thật bắt email trắng bằng CHECK riêng\nINSERT INTO customers(email, display_name)\nVALUES ('   ', 'Tên tạm');                -- check_violation\n\n-- RESTRICT bảo vệ lịch sử vì khách 1 đang có đơn\nDELETE FROM customers WHERE id = 1;       -- foreign_key_violation\n\n-- CASCADE thể hiện vé phụ thuộc vòng đời đơn\nBEGIN;\nDELETE FROM orders WHERE id = 1;\nSELECT count(*) AS so_ve_con_lai FROM tickets WHERE order_id = 1; -- 0\nROLLBACK;\n```\n\n### Nhận ghế mà không có race condition ở application\n\n```sql\nINSERT INTO tickets(order_id, event_id, seat_code, price_cents)\nVALUES (1, 1, 'A-01', 250000)\nON CONFLICT (event_id, seat_code) DO NOTHING\nRETURNING id;\n```\n\nNếu không có hàng trả về, ghế đã tồn tại. Câu lệnh chạy được trên PostgreSQL và gom “thử” cùng “giữ chỗ” thành một thao tác nguyên tử. Nó không giải quyết giới hạn capacity toàn sự kiện; capacity là tổng nhiều hàng, cần thiết kế khóa hoặc bộ đếm riêng chứ không nên tuyên bố `UNIQUE` giải quyết mọi chuyện.",

so_sanh: "### Chọn đúng công cụ cho đúng dạng bất biến\n\n| Công cụ | Bảo vệ tốt nhất | Có chống ghi đồng thời? | Chi phí/chỗ yếu | Ví dụ đặt vé |\n|---|---|---:|---|---|\n| Kiểm tra ở UI | trải nghiệm nhập liệu | Không | dễ bị bỏ qua | báo email sai sớm |\n| Kiểm tra ở service | luật giàu ngữ cảnh | chỉ khi có khóa đúng | mọi đường ghi phải dùng cùng code | tổng đơn bằng tổng vé |\n| `NOT NULL` | giá trị bắt buộc | Có | không phân biệt chuỗi rỗng | đơn phải có khách |\n| `CHECK` | miền giá trị trong một hàng | Có | `NULL` cho `UNKNOWN`; không hợp luật nhiều hàng | giá vé không âm |\n| `UNIQUE` | không trùng khóa nghiệp vụ | Có | thêm index và chi phí ghi | một ghế mỗi sự kiện |\n| `FOREIGN KEY` | toàn vẹn tham chiếu | Có | kiểm tra và lock; cần index bảng con | đơn trỏ tới khách có thật |\n\n### Khi nào không nên chọn phương án mạnh nhất\n\nKhông phải mọi quan hệ đều cần `CASCADE`, mọi cột đều `NOT NULL`, hay mọi luật đều thành trigger. Dữ liệu nhập thô có thể cần vùng staging cố ý lỏng để giữ nguyên lỗi rồi làm sạch. Hệ thống phân mảnh nhiều database có thể phải chấp nhận toàn vẹn cuối cùng và job đối soát thay cho khóa ngoại xuyên biên. Điều quan trọng là ghi rõ guarantee đã hạ xuống, cách phát hiện bản ghi mồ côi và cách sửa; bỏ constraint mà không có cơ chế bù chỉ chuyển lỗi sang tương lai.",

loi_thuong_gap: [
  "**Dùng `CHECK (x > 0)` rồi tưởng `NULL` bị chặn.** Nhận biết bằng cách thử `INSERT NULL`; sửa bằng `NOT NULL` khi sự hiện diện là bắt buộc.",
  "**Kiểm tra trùng bằng `SELECT` trước `INSERT`.** Test một luồng vẫn qua nhưng hai session đồng thời tạo bản ghi trùng; dùng `UNIQUE` và xử lý `unique_violation` hoặc `ON CONFLICT`.",
  "**Có `FOREIGN KEY` nhưng không index cột bảng con.** `EXPLAIN` hoặc log truy vấn cho thấy xóa/cập nhật bảng cha quét nhiều hàng con; tạo index theo hướng truy vấn thực tế.",
  "**Chọn `ON DELETE CASCADE` vì ngắn.** Một lệnh xóa cha làm số hàng bị xóa lớn bất ngờ; xem trước vòng đời, quyền xóa và audit trước khi dùng.",
  "**Thêm constraint thẳng vào bảng lớn có dữ liệu bẩn.** DDL chờ lock hoặc thất bại giữa deploy; khảo sát vi phạm, dùng lộ trình `NOT VALID`/`VALIDATE` khi loại constraint hỗ trợ.",
  "**Để tên constraint tự sinh khó đọc.** API chỉ nhận tên dài không nói nghiệp vụ; đặt tên ổn định để ánh xạ sang thông báo lỗi có ý nghĩa.",
  "**Đưa luật biến đổi theo thời gian hoặc phụ thuộc bảng khác vào `CHECK`.** Hàng cũ tự trở nên sai mà PostgreSQL không tái kiểm; chuyển sang transaction, job đối soát hoặc mô hình dữ liệu khác."
],

tu_kiem_tra: [
  "Vì sao `CHECK (email <> '')` không thay thế được `NOT NULL`, và chuỗi chỉ có khoảng trắng còn lọt qua thế nào?",
  "Hai transaction cùng đặt ghế `A-01`. Hãy mô tả vì sao `SELECT` kiểm tra trước vẫn sai và unique index phân xử ở bước nào.",
  "Khi xóa một khách, index nào ở bảng nào giúp `FOREIGN KEY` tránh quét rộng? Vì sao index khóa chính ở `customers` chưa đủ?",
  "Phân biệt `RESTRICT`, `CASCADE` và `SET NULL` bằng vòng đời của `orders` và `tickets`; lựa chọn nào làm mất lịch sử ngoài ý muốn?",
  "Thiết kế migration để biến `orders.total_cents` cũ thành không âm mà không giữ lock xác minh quá lâu trên đường ghi.",
  "Luật “số vé đã bán không vượt `events.capacity`” vì sao không phải `CHECK` đơn giản? Đề xuất transaction và đối tượng cần khóa.",
  "Trong trường hợp nào staging table nên cố ý ít constraint hơn bảng đích, và bước nào ngăn dữ liệu bẩn đi tiếp?"
],

tai_nguyen: [
  "Tài liệu PostgreSQL — chương Data Definition, các mục Constraints và DDL Constraints.",
  "Sách **SQL Antipatterns** — Bill Karwin, các chương về khóa, `NULL` và toàn vẹn dữ liệu.",
  "Sách **Designing Data-Intensive Applications** — Martin Kleppmann, phần transaction và tính nhất quán.",
  "Sách **Database Internals** — Alex Petrov, phần cấu trúc index và xử lý transaction."
],

lien_he: [
  "Nối sang mảng **Backend/API**: ánh xạ mã SQLSTATE như `23505` thành phản hồi conflict có nghĩa, nhưng vẫn để database làm trọng tài đồng thời.",
  "Nối sang mảng **Bảo mật**: quyền DML tối thiểu và constraint tạo hai lớp khác nhau; constraint giữ tính hợp lệ nhưng không thay thế authorization.",
  "Nối sang mảng **Kiểm thử**: integration test phải chạy trên PostgreSQL thật để kiểm transaction, `NULL`, cascade và lỗi cạnh tranh mà mock repository không mô phỏng được.",
  "Nối sang mảng **Vận hành hệ thống**: migration constraint cần quan sát lock, timeout, số hàng chưa hợp lệ và kế hoạch rollback thay vì chỉ chạy DDL trong giờ cao điểm."
]

};
