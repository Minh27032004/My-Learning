window.LESSONS["03:github-quy-trinh-nhom:github-actions-ci-chay-test-tu-dong"] = {

tom_tat: [
  "GitHub Actions biến mỗi lần `push` hoặc mở pull request thành một phép kiểm tra có thể lặp lại: dựng đúng môi trường, cài dependency, chạy test và trả trạng thái về commit.",
  "Một workflow là file YAML trong `.github/workflows/`; `on` chọn sự kiện, `jobs` chia công việc, còn `steps` mô tả các lệnh chạy tuần tự trên một runner.",
  "CI đáng tin phải khóa phiên bản môi trường, cài dependency theo lockfile, dùng quyền tối thiểu và thất bại ngay khi test trả exit code khác 0.",
  "Ma trận kiểm thử phát hiện lỗi phụ thuộc phiên bản, cache rút ngắn thời gian tải nhưng không được làm thay đổi tính đúng sai của build.",
  "Workflow chỉ là tín hiệu. Branch protection mới biến tín hiệu đó thành hàng rào không cho merge khi kiểm tra bắt buộc chưa đạt."
],

can_biet_truoc: [
  "Biết `commit`, `push`, branch và pull request; đặc biệt hiểu commit là snapshot bất biến mà CI sẽ checkout.",
  "Biết `package.json`, `package-lock.json`, script `npm test` và exit code của lệnh shell.",
  "Đọc được YAML cơ bản: thụt lề tạo cấu trúc, mảng bắt đầu bằng dấu gạch ngang, chuỗi có thể cần đặt trong dấu nháy.",
  "Phân biệt secret với biến cấu hình công khai; secret không được ghi thẳng vào repository hoặc log."
],

dinh_nghia: "**GitHub Actions** là hệ thống tự động hóa theo sự kiện của GitHub. Với Continuous Integration (CI, tích hợp liên tục), repository lưu một hoặc nhiều workflow YAML; mỗi khi sự kiện phù hợp xảy ra, GitHub tạo runner, checkout một commit cụ thể và thực hiện các lệnh kiểm tra đã khai báo. Kết quả gắn với đúng commit đó dưới dạng check thành công, thất bại, bị hủy hoặc bỏ qua.\n\nĐiểm cốt lõi không phải “chạy test trên mây”, mà là biến tiêu chuẩn tích hợp thành chương trình có thể tái hiện. Máy người viết code có thể còn cache cũ, biến môi trường riêng hoặc file chưa commit. Runner mới không có các tiện nghi vô hình ấy; nếu build chỉ chạy trên laptop của một người, dự án chưa có bằng chứng rằng snapshot trong Git thực sự đầy đủ.",

vi_sao_can: "Trong `rainfall-cli`, một contributor sửa hàm cộng lượng mưa và chạy vài trường hợp bằng tay. Máy của họ dùng Node đã cài sẵn, thư mục `node_modules` còn từ tuần trước và file test mới chưa được đưa vào commit. Pull request nhìn hợp lý nhưng nhánh chính có thể hỏng ngay sau merge. Review bằng mắt không thay thế được thực thi: người review khó phát hiện sai khác runtime, dependency thiếu hoặc một test ở nhánh hiếm.\n\nCI giải quyết ba khoảng cách. Thứ nhất là khoảng cách giữa **working directory** và commit: runner chỉ thấy thứ đã push. Thứ hai là khoảng cách giữa các máy: image sạch làm lộ dependency ngầm. Thứ ba là khoảng cách giữa lời hứa và chính sách: required check có thể ngăn merge thay vì trông chờ mọi người nhớ chạy lệnh.\n\nCI không chứng minh phần mềm không có bug. Nó chỉ chứng minh bộ kiểm tra đã định nghĩa đạt trên môi trường đã định nghĩa. Test thiếu cho CSV rỗng thì workflow xanh vẫn có thể phát hành lỗi. Vì vậy phải đọc trạng thái xanh như một mệnh đề có điều kiện, không như giấy chứng nhận tuyệt đối.",

co_che: "### Từ sự kiện đến một lần chạy\n\nGitHub đọc các file `.yml` hoặc `.yaml` trong `.github/workflows/`. Khi nhận `push`, `pull_request`, lịch định kỳ hay yêu cầu thủ công, bộ điều phối đối chiếu khóa `on`. Nếu khớp, nó tạo một workflow run gắn với SHA của commit. Mỗi `job` được đưa vào một runner phù hợp với `runs-on`; các step trong cùng job chạy theo thứ tự và chia sẻ workspace. Hai job không có quan hệ `needs` có thể chạy song song, vì chúng thường nằm trên runner khác và không tự thấy file của nhau.\n\nChuỗi này giải thích vì sao “file có ở máy tôi” không đủ. `actions/checkout` tải snapshot từ repository vào runner. File chưa commit, `.env` cá nhân và dependency cài toàn cục đều vắng mặt. Chính sự khắc nghiệt đó làm CI có giá trị: nó kiểm tra đầu vào mà đồng đội thực sự nhận được.\n\n### Bốn câu hỏi khi đọc một workflow\n\nVới mỗi đoạn YAML, hãy hỏi: **khi nào chạy**, **runner nào chạy**, **lệnh nào quyết định đạt**, và **dữ liệu nào đi vào**. Ví dụ tối thiểu cho `rainfall-cli`:\n\n```yaml\nname: ci\n\non:\n  pull_request:\n  push:\n    branches: [main]\n\npermissions:\n  contents: read\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 22\n          cache: npm\n      - run: npm ci\n      - run: npm test\n```\n\n`pull_request` cho phản hồi trước merge; `push` vào `main` xác nhận trạng thái sau tích hợp. `permissions` chỉ cho token đọc nội dung. `npm ci` tôn trọng lockfile và bắt đầu từ cây dependency sạch. Step cuối truyền exit code của test cho runner: 0 là đạt, khác 0 làm job thất bại.\n\n### YAML là cấu hình nhưng vẫn có ngữ nghĩa điều khiển\n\nThụt lề sai không chỉ xấu mà có thể đổi cấu trúc. `steps` thuộc `test`; `with` thuộc step `setup-node`. Khóa `on` đôi khi bị parser YAML ngoài GitHub hiểu như boolean, nên đừng tùy tiện đưa workflow qua công cụ dùng chuẩn YAML cũ rồi tin kết quả. GitHub hiển thị lỗi cú pháp hoặc workflow không xuất hiện; cách nhận biết tốt nhất là xem tab Actions và kiểm tra commit chứa file có đúng đường dẫn không.\n\nTên workflow và job nên nói ra trách nhiệm, chẳng hạn `unit-test`, `lint`, `integration-postgres`, thay vì `build-1`. Tên check trở thành giao diện của branch protection; đổi tên tùy hứng có thể làm rule cũ chờ một check không bao giờ tới.\n\n### Checkout không mặc nhiên có toàn bộ lịch sử\n\nAction checkout thường tối ưu bằng lịch sử nông. Test thông thường chỉ cần working tree nên điều đó tốt. Nhưng công cụ tính version từ tag, changelog từ lịch sử hoặc `git diff` qua nhiều commit có thể cho kết quả sai vì ancestor chưa được tải. Khi thực sự cần lịch sử đầy đủ, cấu hình:\n\n```yaml\n- uses: actions/checkout@v4\n  with:\n    fetch-depth: 0\n```\n\nKhông nên sao chép `fetch-depth: 0` vào mọi workflow. Lịch sử lớn làm tăng dữ liệu tải và thời gian khởi động. Quy tắc đúng là cấp đúng lượng lịch sử mà phép kiểm tra sử dụng, giống nguyên tắc quyền tối thiểu.\n\n### Cài dependency phải tái lập được\n\n`npm install` có thể cập nhật lockfile hoặc chọn phiên bản thỏa khoảng cho phép; `npm ci` yêu cầu `package.json` khớp `package-lock.json`, xóa cây dependency hiện có và cài đúng khóa. Trên runner sạch, khác biệt tốc độ không quan trọng bằng khả năng lặp lại. Nếu contributor đổi manifest mà quên lockfile, CI nên đỏ để buộc sửa snapshot dependency.\n\nĐừng cache `node_modules` một cách mù quáng. Cache package tải về của npm giúp `npm ci` nhanh hơn nhưng vẫn để npm dựng cây theo lockfile. Khóa cache cần phụ thuộc lockfile; `setup-node` làm việc đó khi bật `cache: npm`. Cache miss phải chỉ làm chậm, không được làm build sai. Nếu xóa cache khiến test đổi kết quả, workflow đang che dependency ngầm.\n\n### Ma trận biến một job thành nhiều thí nghiệm\n\nNếu package cam kết hỗ trợ nhiều runtime, kiểm một phiên bản là bằng chứng thiếu. Strategy matrix nhân job theo từng giá trị:\n\n```yaml\nstrategy:\n  fail-fast: false\n  matrix:\n    node: [20, 22]\nsteps:\n  - uses: actions/checkout@v4\n  - uses: actions/setup-node@v4\n    with:\n      node-version: ${{ matrix.node }}\n      cache: npm\n  - run: npm ci\n  - run: npm test\n```\n\n`fail-fast: false` cho biết toàn bộ ô nào hỏng thay vì hủy các ô còn lại khi một ô thất bại. Đánh đổi là dùng thêm runner-minute. Không nên tạo ma trận hệ điều hành × runtime × database cho mọi commit nếu rủi ro không tương xứng; có thể chạy tập nhỏ trên pull request và tập rộng theo lịch đêm.\n\n### Job, step và artifact không phải một ổ đĩa chung\n\nStep trong cùng job thấy file do step trước tạo. Job khác thì không. Muốn job `report` đọc coverage từ job `test`, phải upload artifact rồi download ở job sau, hoặc gộp chúng nếu không cần cô lập. Artifact là kết quả của một lần chạy như báo cáo, binary hay log chẩn đoán; cache là dữ liệu tái sử dụng giữa nhiều lần chạy để tăng tốc. Dùng cache làm kênh chuyển kết quả là sai vì cache có thể không tồn tại, bị thay thế hoặc khôi phục từ lần chạy khác.\n\nArtifact cũng cần cân nhắc dữ liệu nhạy cảm. Báo cáo test có thể chứa đường dẫn, payload hay token bị in nhầm. Đừng upload toàn bộ workspace “để debug”; chỉ chọn file cần thiết và đặt thời hạn lưu phù hợp trong giao diện repository.\n\n### Secret đi qua context, không đi vào YAML\n\nSecret được lưu trong cấu hình GitHub rồi tham chiếu bằng `${{ secrets.TEN }}`. GitHub cố che giá trị trong log, nhưng che log không biến mọi cách dùng thành an toàn. Nếu chương trình biến đổi secret, ghi vào file artifact hoặc gửi sang máy khác, giá trị có thể lộ. Pull request từ fork cũng có mô hình tin cậy khác; không nên cấp secret triển khai cho code chưa được tin cậy.\n\nTránh `pull_request_target` nếu mục tiêu chỉ là test code của pull request. Sự kiện đó chạy trong ngữ cảnh nhánh gốc và thường có quyền mạnh hơn; checkout rồi thực thi code từ fork có thể trao chìa khóa cho mã không tin cậy. Với CI thông thường, `pull_request` cùng `contents: read` là nền an toàn dễ hiểu.\n\n### Concurrency ngăn kết quả cũ tiêu tài nguyên\n\nContributor thường push liên tiếp ba bản sửa vào cùng pull request. Không cần giữ cả ba lần test nếu hai commit đầu đã lỗi thời. Nhóm concurrency theo workflow và ref, rồi hủy run cũ:\n\n```yaml\nconcurrency:\n  group: ci-${{ github.workflow }}-${{ github.ref }}\n  cancel-in-progress: true\n```\n\nĐiều này hợp với test không có tác dụng phụ. Không dùng hủy ngang cho migration, phát hành hoặc thao tác bên ngoài nếu việc dừng giữa chừng để lại trạng thái nửa vời. CI nên ưu tiên idempotent: chạy lại cùng commit cho cùng kết luận và không phá môi trường dùng chung.\n\n### Từ check màu xanh đến cổng merge\n\nWorkflow chạy thành công chưa ngăn ai merge khi nó đỏ. Repository phải cấu hình ruleset hoặc branch protection yêu cầu check tương ứng. Khi đó luồng quyết định là: commit mới làm check cũ trở nên lỗi thời; check trên SHA mới phải đạt; review và các điều kiện khác cũng phải thỏa; GitHub mới cho merge.\n\nMột bẫy tinh vi là test trên merge commit giả lập khác với test đúng head commit. GitHub có thể đánh giá khả năng ghép với nhánh đích; nhánh đích thay đổi sau đó khiến kết quả cũ không còn đại diện. Merge queue giải quyết dự án đông bằng cách kiểm tra thứ tự tích hợp dự kiến, nhưng thêm độ trễ. Dự án nhỏ có thể chỉ yêu cầu cập nhật branch trước merge.\n\n### Chẩn đoán một run đỏ theo tầng\n\nĐọc từ ngoài vào trong. Workflow không xuất hiện: kiểm đường dẫn, cú pháp và bộ lọc sự kiện. Job không được tạo: kiểm `if`, matrix và quyền. Step cài đặt hỏng: kiểm runtime, lockfile, registry và mạng. Test hỏng: chạy đúng lệnh và đúng runtime ở local. Test chỉ hỏng trên CI: tìm khác biệt chữ hoa chữ thường của filesystem, timezone, locale, thứ tự không ổn định, port dùng chung hoặc biến môi trường.\n\nKhông “sửa” bằng cách thêm retry vô hạn. Retry chỉ hợp lý cho lỗi tạm thời đã phân loại; nó làm test không ổn định khó thấy hơn. Một flaky test (test lúc đạt lúc trượt với cùng đầu vào) là bug của hệ thống kiểm thử và cần owner, dữ liệu tái tạo cùng giới hạn thời gian sửa.\n\n### Khi nào không nên thêm một workflow nữa\n\nKhông nên đưa mọi tác vụ vào CI. Formatter có thể chạy tức thì ở editor; benchmark dài và tốn tiền có thể chạy theo lịch; deploy production cần environment approval; phép thử cần phần cứng đặc biệt có thể dùng runner tự quản lý với hàng rào riêng. Mỗi check bắt buộc tăng thời gian chờ của mọi contributor. Hãy giữ đường phản hồi pull request ngắn, xác định check nào chặn merge, check nào chỉ cung cấp thông tin và check nào chạy ngoài giờ.",

vi_du: "### Dựng `rainfall-cli` và kiểm tra workflow\n\nVí dụ sau tạo phần tối thiểu chạy được trên Node, không cần package bên ngoài. `npm test` dùng test runner có sẵn:\n\n```sh\nmkdir rainfall-cli && cd rainfall-cli\ngit init\nnpm init -y\nmkdir src test\nprintf 'export function total(values) { return values.reduce((sum, x) => sum + x, 0); }\\n' > src/total.js\nprintf 'import test from \"node:test\";\\nimport assert from \"node:assert/strict\";\\nimport { total } from \"../src/total.js\";\\ntest(\"empty\", () => assert.equal(total([]), 0));\\ntest(\"three readings\", () => assert.equal(total([12, 0, 8]), 20));\\n' > test/total.test.js\nnpm pkg set type=module scripts.test=\"node --test\"\nnpm test\n```\n\nSau đó tạo `.github/workflows/ci.yml` bằng nội dung ở phần cơ chế, rồi commit và push. Dấu hiệu đúng không phải chỉ là tab Actions có một hàng xanh: mở run, xác nhận SHA trùng commit của pull request, runtime đúng cam kết và lệnh thật sự đã chạy hai test.\n\n### Cố ý làm đỏ để kiểm hàng rào\n\nĐổi `sum + x` thành `sum - x`, commit trên branch thử nghiệm và mở pull request. Test `three readings` phải báo expected 20 nhưng nhận giá trị âm. Nếu vẫn merge được, workflow có thể đúng nhưng quy trình chưa đóng: cấu hình required status check còn thiếu. Khôi phục phép cộng bằng commit mới; đừng sửa lịch sử của pull request chỉ để giấu lần thất bại, vì chuỗi đỏ rồi xanh là bằng chứng CI phát hiện regression.",

so_sanh: "### Chọn nơi đặt phép kiểm tra\n\n| Cách kiểm tra | Phản hồi | Môi trường | Có thể chặn merge | Điểm yếu chính |\n|---|---|---|---|---|\n| Chạy tay trước khi push | Nhanh nếu nhớ | Máy cá nhân | Không | Dễ quên, phụ thuộc trạng thái máy |\n| Git hook local | Rất sớm | Máy cá nhân | Không tuyệt đối | Có thể bỏ qua, khó đồng bộ mọi máy |\n| GitHub Actions trên PR | Sau khi push | Runner khai báo | Có | Có độ trễ và chi phí runner |\n| Test theo lịch | Chậm hơn | Runner khai báo | Thường không | Phát hiện muộn nhưng hợp bộ test dài |\n| Self-hosted runner | Tùy hạ tầng | Máy nhóm quản lý | Có | Phải vá, cô lập và bảo vệ máy chạy |\n\nCác lớp này bổ sung nhau. Hook giữ vòng lặp cá nhân nhanh; CI tạo bằng chứng chung; lịch đêm mở rộng độ phủ. Không nên ép một lớp làm mọi việc. Với `rainfall-cli`, unit test nhỏ chạy cả local lẫn PR, còn phép thử tải dataset lớn có thể chạy ban đêm để không giữ hàng merge.",

loi_thuong_gap: [
  "**Workflow không chạy vì đặt sai chỗ.** Tab Actions không có run cho commit mới; kiểm file có nằm đúng `.github/workflows/` và đã được commit hay chưa.",
  "**Dùng `npm install` và bỏ quên lockfile.** CI đôi lúc lấy dependency khác máy local; nhận biết qua `package-lock.json` thay đổi hoặc cây phiên bản không tái lập.",
  "**Check xanh nhưng vẫn merge được khi test đỏ.** Workflow chưa được chọn làm required check trong ruleset hoặc branch protection.",
  "**Cho token quyền ghi mặc định quá rộng.** Xem phần permissions của run; khai báo `permissions: contents: read` rồi chỉ tăng quyền cho job thật sự cần.",
  "**Tin cache hơn lockfile.** Xóa cache thì build hỏng hoặc kết quả đổi; đây là dấu hiệu dependency ngầm, không phải lý do giữ cache mãi.",
  "**Chạy code từ fork cùng secret.** Pull request không tin cậy có thể đọc hoặc gửi bí mật ra ngoài; tách workflow đặc quyền khỏi workflow test code đóng góp.",
  "**Tạo ma trận quá lớn.** Một sửa README cũng chờ hàng chục job; đo thời gian phản hồi rồi chuyển tổ hợp ít rủi ro sang lịch định kỳ."
],

tu_kiem_tra: [
  "Vì sao `actions/checkout` làm lộ file chưa commit và dependency cài toàn cục mà máy tác giả vô tình dùng?",
  "Trong workflow mẫu, dòng nào trực tiếp biến test fail thành job fail, và cơ chế truyền thông tin là gì?",
  "Khi nào `fetch-depth: 0` là cần thiết, và vì sao thêm nó khắp nơi lại là một đánh đổi tệ?",
  "Phân biệt artifact với cache bằng tuổi thọ, mục đích và hậu quả khi dữ liệu không tồn tại.",
  "Tại sao workflow xanh chưa đủ ngăn merge một commit hỏng?",
  "Một test chỉ fail trên Windows và chỉ đôi lúc. Bạn sẽ thu thập bằng chứng theo thứ tự nào trước khi thêm retry?",
  "Vì sao checkout code không tin cậy trong workflow có secret nguy hiểm ngay cả khi GitHub che secret khỏi log?"
],

tai_nguyen: [
  "GitHub Docs — phần GitHub Actions, workflow syntax và security hardening.",
  "Pro Git — các chương về GitHub và distributed workflows.",
  "Continuous Delivery của Jez Humble và David Farley.",
  "Software Engineering at Google — chương Testing Overview và Continuous Integration."
],

lien_he: [
  "Nối sang mảng Kiểm thử phần mềm: CI chỉ đáng tin bằng test suite, fixture và chiến lược chống flaky test mà nó thực thi.",
  "Nối sang mảng DevOps: artifact, environment approval và deployment mở rộng workflow từ kiểm tra sang giao hàng liên tục.",
  "Nối sang mảng Bảo mật: token quyền tối thiểu, secret và code từ fork là một bài toán supply-chain security.",
  "Nối sang mảng Hệ điều hành: runner, process, exit code và biến môi trường giải thích cách mỗi step thật sự được thực thi."
]

};
