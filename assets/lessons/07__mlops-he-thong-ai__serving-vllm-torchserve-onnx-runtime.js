window.LESSONS["07:mlops-he-thong-ai:serving-vllm-torchserve-onnx-runtime"] = {

tom_tat: [
  "Model serving biến artifact thành dịch vụ có contract, quản lý batching, tài nguyên, timeout, quan sát và rollout; gọi `predict` chỉ là lõi nhỏ nhất.",
  "vLLM tối ưu sinh token cho mô hình ngôn ngữ lớn, TorchServe đóng gói model PyTorch theo handler, còn ONNX Runtime thực thi đồ thị ONNX trên nhiều execution provider.",
  "Không có runtime nhanh nhất nói chung: workload, phần cứng, kích thước batch, độ dài chuỗi và yêu cầu latency quyết định kết quả.",
  "Ví dụ xe đạp dùng ONNX Runtime cho dự báo dạng bảng; vLLM được đối chiếu bằng workload LLM riêng, không ép ba công cụ giải cùng một bài toán.",
  "Benchmark phải đo end-to-end ở p50/p95/p99, throughput, lỗi và chất lượng; số đo kernel đơn lẻ không đại diện trải nghiệm người dùng."
],

can_biet_truoc: [
  "Biết request/response, HTTP status, timeout và concurrency cơ bản.",
  "Hiểu tensor, batch và inference mode của PyTorch.",
  "Biết model version bất biến cùng alias triển khai từ bài registry.",
  "Phân biệt latency một request với throughput toàn hệ thống."
],

dinh_nghia: "**Model serving** là lớp thực thi nhận đầu vào qua một contract, biến đổi thành tensor, chạy model version đã chọn, hậu xử lý kết quả và trả phản hồi trong giới hạn độ trễ, độ tin cậy cùng tài nguyên đã cam kết. Runtime inference tối ưu phép tính; server bổ sung mạng, batching, lifecycle, quan sát và bảo vệ quá tải.\n\nvLLM, TorchServe và ONNX Runtime nằm ở các lớp hơi khác nhau. vLLM là engine/server tập trung LLM autoregressive; TorchServe là model server cho PyTorch; ONNX Runtime là runtime nhúng, thường cần ứng dụng tự cung cấp HTTP và scheduling.",

vi_sao_can: "Notebook dự đoán một hàng không gặp hàng trăm request đồng thời, input sai schema, GPU hết bộ nhớ hay client bỏ cuộc giữa chừng. Production buộc model chia sẻ tài nguyên và giữ contract khi version thay đổi. Tối ưu sai mục tiêu cũng phổ biến: tăng batch làm throughput đẹp nhưng người dùng đầu tiên chờ lâu hơn.\n\nHệ thống xe đạp cần dự báo cho từng trạm trước giờ cao điểm. Model dạng bảng nhỏ có thể chạy CPU bằng ONNX Runtime, giảm phụ thuộc Python. Một trợ lý vận hành giải thích dự báo bằng LLM lại có đặc tính sinh từng token và hưởng lợi từ continuous batching của vLLM. Tách workload như vậy thực tế hơn tuyên bố một công cụ thắng mọi nơi.",

co_che: "### Đường đi end-to-end của một request\n\nRequest qua load balancer, xác thực, kiểm schema, tiền xử lý, hàng đợi, batching, runtime, hậu xử lý rồi serialization. Mỗi chặng có thể thất bại và tiêu tốn latency. Nếu chỉ bấm giờ `session.run`, ta bỏ qua JSON lớn, queue wait và feature lookup. Trace cần mang model version để sự cố gắn đúng artifact.\n\n### Contract phải chặn dữ liệu sai trước accelerator\n\nXe đạp yêu cầu `hour` nguyên 0–23, `rain` 0/1 và nhiệt độ hữu hạn. Chuyển chuỗi tùy tiện thành số có thể che lỗi upstream. Request rỗng, cột thừa, NaN và batch quá lớn cần lỗi 4xx rõ ràng; lỗi runtime mới là 5xx. Validation sớm tiết kiệm GPU/CPU và ngăn kết quả vô nghĩa trông hợp lệ.\n\n### Batching đổi latency lấy throughput\n\nBatch gom nhiều request để vector hóa và tận dụng accelerator. Static batching chờ đủ số lượng hoặc timeout; dynamic batching chọn theo hàng đợi. Batch quá nhỏ lãng phí phần cứng, quá lớn gây queue wait và OOM. Cần đo theo phân phối tải thật. Với model xe đạp CPU rất nhỏ, overhead gom batch đôi khi lớn hơn phép dự đoán nên batch=1 có thể đúng.\n\n| Đại lượng | Câu hỏi | Bẫy |\n|---|---|---|\n| p50 latency | request điển hình chờ bao lâu | che đuôi dài |\n| p99 latency | 1% chậm nhất ra sao | nhạy với queue và cold start |\n| throughput | hoàn thành bao nhiêu request/giây | tăng bằng cách làm từng request chờ lâu |\n| concurrency | bao nhiêu request đang xử lý | không đồng nghĩa throughput |\n| error rate | bao nhiêu request thất bại | retry có thể che lỗi gốc |\n\n### Backpressure bảo vệ hệ thống khi tải vượt khả năng\n\nKhông hàng đợi nào nên vô hạn. Khi arrival rate lâu dài lớn hơn service rate, queue chỉ chuyển lỗi thành timeout chậm và ngốn RAM. Đặt giới hạn, trả 429/503 có chỉ dẫn retry, ưu tiên request quan trọng hoặc degrade có chủ đích. Autoscaling phản ứng có độ trễ nên vẫn cần admission control. Client retry phải có backoff và jitter để tránh bão lặp.\n\n### Cold start là một phần của SLO\n\nTải model, cấp GPU memory, compile graph và warm kernel có thể lâu hơn inference nhiều lần. Readiness chỉ bật sau khi model nạp và smoke test thành công. Liveness không nên giết process chỉ vì một request chậm, nếu không tạo vòng restart. Giữ replica ấm giảm cold start nhưng tốn tiền; scale-to-zero hợp workload không nhạy latency.\n\n### vLLM xử lý workload sinh token\n\nLLM autoregressive lặp: mỗi bước sinh một token và dùng KV cache từ đoạn trước. Request có độ dài khác nhau khiến static batch lãng phí. vLLM dùng scheduling/continuous batching để đưa request mới vào khi slot giải phóng và quản lý KV cache hiệu quả. Lợi ích chỉ xuất hiện với model sinh văn bản phù hợp; nó không phải lựa chọn cho RandomForest dự báo xe.\n\n### Time to first token khác time per output token\n\nNgười dùng chat cảm nhận thời gian chờ token đầu, sau đó là nhịp token. Prompt dài làm prefill nặng; output dài giữ KV cache lâu. Chỉ báo request/giây bỏ qua độ dài chuỗi nên benchmark phải ghi phân phối input/output token, TTFT và inter-token latency. Streaming không làm tổng compute biến mất, nhưng cải thiện cảm nhận và cho phép client hủy sớm.\n\n### TorchServe đặt PyTorch sau handler\n\nHandler nhận request, preprocess, gọi model và postprocess. Cơ chế này linh hoạt cho model PyTorch và custom logic, nhưng handler trở thành code production cần test, giới hạn input và quản lý dependency. Worker count không thể tăng tùy ý: mỗi worker có thể giữ bản model riêng và nhân bộ nhớ. Batch, worker, GPU mapping phải được đo cùng nhau.\n\n### ONNX tách đồ thị khỏi framework huấn luyện\n\nExporter chuyển model sang graph ONNX gồm operator và tensor. ONNX Runtime chọn execution provider như CPU hoặc GPU để tối ưu graph. Tách này giúp nhúng vào nhiều ngôn ngữ và runtime gọn, nhưng export có thể không hỗ trợ operator động hoặc cho kết quả số hơi khác. Phải so output trên tập đại diện trước khi chấp nhận artifact.\n\n### Dynamic shape là sự linh hoạt có giá\n\nShape cố định cho phép tối ưu mạnh và cấp bộ nhớ dự đoán được. Dynamic batch hoặc sequence cần profile nhiều đường và có thể giảm tối ưu. Với bảng xe đạp, trục batch dynamic là đủ; số feature phải cố định. Với LLM, chiều sequence biến đổi tự nhiên nên scheduler và memory planning quan trọng hơn.\n\n### Quantization không miễn phí về chất lượng\n\nGiảm precision có thể giảm memory và tăng tốc trên phần cứng hỗ trợ, nhưng sai số làm output thay đổi. Model hồi quy cần so MAE tổng cùng giờ cao điểm sau quantization. LLM cần eval nhiệm vụ, không chỉ perplexity. Nếu runtime tự fallback operator về CPU, latency có thể xấu dù model nhỏ hơn; trace placement trước khi kết luận.\n\n### Canary gắn deployment revision với model version\n\nRegistry alias nói ứng viên nào được chọn; deployment revision nói binary, config và model nào thực sự chạy. Canary chia ít traffic, theo dõi lỗi, p99 và chỉ báo chất lượng rồi tăng dần. Sticky routing có ích khi phiên nhiều lượt cần cùng model. Rollback phải quay cả container/config nếu lỗi nằm ngoài artifact.\n\n### Cache có thể làm sai ngữ nghĩa\n\nCache dự đoán chỉ an toàn khi key bao gồm toàn bộ đầu vào ảnh hưởng kết quả, model version và preprocessing version. Với LLM còn có sampling parameter; temperature khác không thể dùng chung output. Dữ liệu thời tiết thay đổi làm key theo trạm đơn thuần bị stale. TTL giảm rủi ro nhưng không thay invalidation chính xác.\n\n### Timeout và hủy công việc phải đi xuyên tầng\n\nClient hết hạn nhưng server vẫn chạy inference làm lãng phí tài nguyên. Deadline nên truyền qua gateway, queue và worker; scheduler bỏ request không còn giá trị nếu runtime hỗ trợ. Streaming cần xử lý disconnect. Retry request sinh văn bản không tất định có thể cho kết quả khác; API phải nói rõ idempotency và cách dùng request ID.\n\n### Khi nào không nên dựng server riêng\n\nBatch scoring theo giờ có thể chạy job, ghi bảng dự báo rồi API đọc kết quả; server model trực tuyến chỉ tăng điểm lỗi. Model nhỏ dùng trong cùng process ứng dụng có thể nạp trực tiếp ONNX Runtime. Ngược lại, model GPU lớn dùng chung cần scheduler chuyên dụng. Chọn online, batch hay embedded theo freshness và tải, không theo xu hướng.\n\n### Quan sát phải phân biệt lỗi hệ thống với lỗi mô hình\n\nDashboard vận hành cần request rate, queue time, batch size, runtime latency, error code, memory và model version. Metric chất lượng lại thường đến muộn vì nhu cầu thuê thật chỉ biết sau một giờ. Hai vòng tín hiệu không thể nhập làm một: service có thể khỏe nhưng dự báo sai do mưa bất thường; cũng có thể MAE tốt trên request hoàn thành trong khi nhiều request đã timeout. Log không nên chứa toàn bộ feature nhạy cảm. Với dữ liệu xe đạp, lưu histogram và sample đã kiểm soát thường đủ để debug.\n\nCardinality là ca biên nguy hiểm của observability. Đưa `station_id` hoặc `request_id` vào label metric tạo hàng triệu time series và làm chính hệ thống giám sát quá tải. Request ID thuộc trace/log; metric chỉ dùng chiều có tập giá trị hữu hạn như model version, status class và endpoint. Khi cần phân tích từng trạm, ghi bảng phân tích ngoại tuyến có retention rõ.\n\n### An toàn đầu vào và giới hạn tài nguyên\n\nModel endpoint cũng là bề mặt tấn công. Payload cực lớn, shape lồng sâu hoặc chuỗi prompt dài có thể chiếm memory và thời gian dù schema hợp lệ về kiểu. Gateway phải giới hạn byte, số hàng, token và concurrency theo tenant. Với LLM, stop sequence, maximum output tokens và quota phải được áp ở server, không tin client tự kiềm chế. Nội dung prompt không được nối thẳng vào shell, template hoặc đường dẫn artifact.\n\nRate limit theo request đôi khi chưa công bằng: một prompt 20 token và một prompt 20.000 token có chi phí rất khác. Có thể tính quota theo token hoặc đơn vị compute ước lượng, nhưng estimation cũng tốn chi phí và có sai số. Chính sách nên ưu tiên bảo vệ SLO trước, sau đó mới tối ưu độ chính xác của billing.\n\n### Capacity planning bắt đầu từ luật Little\n\nTrực giác của luật Little là L = λW: L là số request trung bình trong hệ thống, λ là tốc độ request hoàn thành mỗi giây, W là thời gian trung bình mỗi request ở lại. Nếu phục vụ 100 request/giây với W = 0,2 giây thì trung bình có 20 request đang hiện diện. Công thức không thay benchmark và không mô tả p99, nhưng giúp phát hiện cấu hình vô lý: giới hạn concurrency thấp hơn nhu cầu trung bình chắc chắn tạo hàng đợi. Khi workload có burst, cần headroom và mô phỏng phân phối đến thật thay vì chỉ dùng trung bình.",

vi_du: "### Xuất hồi quy PyTorch sang ONNX\n\nVí dụ tạo model nhỏ, export, nạp bằng ONNX Runtime và so kết quả. Cài `torch onnx onnxruntime numpy` phù hợp môi trường.\n\n```python\nimport numpy as np\nimport torch\nfrom torch import nn\nimport onnxruntime as ort\n\ntorch.manual_seed(42)\nmodel = nn.Sequential(nn.Linear(3, 16), nn.ReLU(), nn.Linear(16, 1)).eval()\nsample = torch.tensor([[8.0, 0.0, 24.5]], dtype=torch.float32)\ntorch.onnx.export(\n    model, sample, \"bike_demand.onnx\",\n    input_names=[\"features\"], output_names=[\"rentals\"],\n    dynamic_axes={\"features\": {0: \"batch\"}, \"rentals\": {0: \"batch\"}},\n)\nsession = ort.InferenceSession(\"bike_demand.onnx\", providers=[\"CPUExecutionProvider\"])\nx = np.array([[8, 0, 24.5], [18, 1, 30]], dtype=np.float32)\nwith torch.inference_mode():\n    expected = model(torch.from_numpy(x)).numpy()\nactual = session.run([\"rentals\"], {\"features\": x})[0]\nnp.testing.assert_allclose(actual, expected, rtol=1e-4, atol=1e-5)\nprint(actual)\n```\n\n### Bọc contract mà không che lỗi\n\n```python\ndef validate(rows):\n    x = np.asarray(rows, dtype=np.float32)\n    if x.ndim != 2 or x.shape[1] != 3 or x.shape[0] == 0:\n        raise ValueError(\"cần batch không rỗng, mỗi hàng có 3 feature\")\n    if not np.isfinite(x).all():\n        raise ValueError(\"feature chứa NaN hoặc vô cực\")\n    if ((x[:, 0] < 0) | (x[:, 0] > 23)).any():\n        raise ValueError(\"hour ngoài 0..23\")\n    if not np.isin(x[:, 1], [0, 1]).all():\n        raise ValueError(\"rain phải là 0 hoặc 1\")\n    return x\n```\n\nBenchmark nên warm-up, tạo nhiều mức concurrency và in kết quả đo trên chính máy chạy; bài không ghi số latency bịa. So ONNX với PyTorch phải dùng cùng batch, thread, input và kiểm output trước khi đo tốc độ.",

so_sanh: "### Chọn theo workload thay vì logo\n\n| Công cụ | Workload tự nhiên | Điểm mạnh | Không nên ép dùng khi |\n|---|---|---|---|\n| vLLM | LLM sinh token | scheduling, continuous batching, KV cache | model bảng/CNN nhỏ |\n| TorchServe | model PyTorch cần handler/server | đóng gói worker và API | chỉ cần runtime nhúng gọn |\n| ONNX Runtime | graph ONNX đa nền tảng | execution provider, nhúng nhiều ngôn ngữ | model export không tương thích |\n\nBa lựa chọn không hoàn toàn loại trừ nhau vì nằm khác lớp. Một tổ chức có thể dùng ONNX Runtime trong service tự viết và vLLM cho LLM. TorchServe giảm code server cho PyTorch nhưng custom handler vẫn mang chi phí bảo trì. Proof-of-concept phải đo workload đại diện và bao gồm cold start, memory cùng sai số output.",

loi_thuong_gap: [
  "**Chỉ đo `predict`.** p99 production cao vì queue và JSON; đo end-to-end kèm trace từng chặng.",
  "**Tăng batch vô hạn.** Throughput tăng nhưng timeout và OOM xuất hiện; đặt batch timeout và giới hạn memory.",
  "**Readiness trước khi warm model.** Request đầu lỗi hoặc rất chậm; smoke test rồi mới nhận traffic.",
  "**Export ONNX nhưng không so output.** Operator khác tạo sai số; kiểm trên lát cắt đại diện với tolerance.",
  "**Retry không backoff.** Quá tải biến thành bão request; giới hạn lần thử, jitter và backpressure.",
  "**Cache thiếu model version trong key.** Sau rollout trả dự đoán cũ; version hóa và invalidation rõ.",
  "**Dùng vLLM cho mọi model.** Công cụ không khớp workload; bắt đầu từ đặc tính tính toán thay vì tên nổi tiếng."
],

tu_kiem_tra: [
  "Vì sao throughput tăng không chứng minh trải nghiệm người dùng tốt hơn?",
  "Continuous batching giải quyết đặc tính nào của sinh token LLM?",
  "ONNX Runtime khác model server ở trách nhiệm mạng và scheduling ra sao?",
  "Thiết kế benchmark nào so PyTorch với ONNX công bằng và kiểm tính đúng trước tốc độ?",
  "Khi client timeout, vì sao hủy inference phía server vẫn quan trọng?",
  "Khi nào batch scoring tốt hơn endpoint dự đoán trực tuyến cho xe đạp?"
],

tai_nguyen: [
  "Tài liệu chính thức vLLM.",
  "Tài liệu chính thức TorchServe.",
  "Tài liệu chính thức ONNX Runtime.",
  "Designing Machine Learning Systems — Chip Huyen."
],

lien_he: [
  "Nối sang Backend qua API contract, status code, timeout, streaming và idempotency.",
  "Nối sang Hệ điều hành qua process, thread, memory mapping và giới hạn tài nguyên.",
  "Nối sang Distributed Systems qua queue, backpressure, autoscaling và canary routing.",
  "Nối sang Computer Architecture qua vectorization, precision, CPU/GPU memory và kernel warm-up."
]

};
