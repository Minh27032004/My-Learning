window.LESSONS["05:trien-khai:dong-goi-bang-docker-viet-dockerfile-gon"] = {

tom_tat: [
  "Docker image là gói bất biến gồm filesystem, metadata và lệnh khởi động; container là một process đang chạy từ image, không phải máy ảo thu nhỏ.",
  "Dockerfile gọn nhờ chọn base image có chủ đích, khóa dependency, sắp xếp layer để tận dụng cache và dùng multi-stage build khi có công đoạn biên dịch.",
  "Ứng dụng FastAPI phải nghe trên `0.0.0.0`, nhận cổng và cấu hình qua biến môi trường, đồng thời xử lý tín hiệu dừng để deploy không làm rơi request.",
  "Image an toàn không chạy bằng root, không chứa secret, loại file thừa bằng `.dockerignore` và chỉ cài dependency cần cho runtime.",
  "Kích thước nhỏ có ích, nhưng khả năng tái lập, vá bảo mật, quan sát lỗi và thời gian build mới là tiêu chí quyết định một Dockerfile dùng được ở production."
],

can_biet_truoc: [
  "Biết process, PID, filesystem và port TCP; đặc biệt phân biệt địa chỉ loopback `127.0.0.1` với mọi interface `0.0.0.0`.",
  "Biết FastAPI tạo ASGI application và Uvicorn là ASGI server nhận HTTP rồi gọi application đó.",
  "Biết `requirements.txt`, virtual environment và ý nghĩa của việc khóa phiên bản dependency Python.",
  "Biết biến môi trường dùng để đưa cấu hình vào process mà không đóng cứng giá trị trong source code."
],

dinh_nghia: "**Đóng gói bằng Docker** là mô tả cách dựng một **image bất biến** từ source code và dependency, sau đó khởi chạy image ấy thành container có filesystem riêng, namespace riêng và giới hạn tài nguyên do hệ điều hành cung cấp. `Dockerfile` là bản hướng dẫn dựng image theo từng instruction; kết quả của mỗi instruction thường trở thành một layer có thể được cache và tái sử dụng.\n\n“Dockerfile gọn” không đồng nghĩa với ít dòng nhất. Nó là Dockerfile tạo ra artifact tái lập được, chỉ mang những gì runtime cần, có bề mặt tấn công nhỏ, build nhanh khi code thay đổi và khởi động đúng contract của nền tảng. Container vẫn dùng kernel của máy chủ; vì vậy nó cô lập process tốt hơn chạy trực tiếp, nhưng không tạo ranh giới bảo mật mạnh như một máy ảo độc lập.",

vi_sao_can: "Sổ Chi Tiêu API chạy tốt trên laptop chưa chứng minh nó chạy được ở CI hay production. Laptop có sẵn Python, thư viện hệ thống, timezone, file `.env` và user có quyền rộng. Server có thể thiếu đúng một thư viện native, dùng kiến trúc CPU khác hoặc khởi động bằng một lệnh khác. Kiểu lỗi “máy tôi chạy được” sinh ra vì artifact thật chưa được định nghĩa.\n\nDocker biến môi trường chạy thành đầu ra có thể gắn nhãn và chuyển giao. Cùng một image digest được kiểm thử ở CI có thể được đưa lên staging rồi production; ta không cài lại dependency giữa đường. Điều đó giảm drift, hỗ trợ rollback và làm rõ câu hỏi bảo mật: image chứa gì, chạy với user nào, mở cổng nào, nhận secret ở đâu.\n\nDocker không tự giải quyết database migration, quản lý secret, TLS hay giám sát. Nó chỉ cho ta một đơn vị triển khai nhất quán. Nếu xem container như “production trong một hộp”, đội ngũ dễ bỏ sót chính những ranh giới còn lại.",

co_che: "### Từ Dockerfile đến process đang phục vụ HTTP\n\nLuồng thật gồm hai pha. `docker build` đọc build context, thực hiện instruction và tạo image. `docker run` lấy metadata của image, tạo writable layer, thiết lập network namespace rồi chạy `ENTRYPOINT`/`CMD` thành process chính. Nếu process PID 1 kết thúc, container kết thúc; Docker không biết FastAPI “đáng lẽ” phải sống tiếp.\n\n```text\nsource + lock file --docker build--> image có digest\nimage + config ------docker run----> container / PID 1 / port nội bộ\nclient --port publish hoặc proxy---> Uvicorn --ASGI--> FastAPI\n```\n\nImage không chứa trạng thái giao dịch mới phát sinh. Mọi file ghi vào writable layer có thể mất khi container bị thay thế. Vì PostgreSQL mới là nơi giữ sổ thu–chi; container của API phải được xem là có thể hủy và tạo lại.\n\n### Build context và `.dockerignore`\n\nDòng `COPY . .` không đọc “repo” theo nghĩa Git; nó sao chép từ build context mà Docker client gửi cho builder. Nếu context chứa `.git`, virtual environment, test report hoặc `.env`, chúng vừa làm build chậm vừa có nguy cơ nằm trong layer. Xóa file ở instruction sau không chắc loại được bí mật khỏi layer trước.\n\n`.dockerignore` nên chặn ít nhất `.git`, `.venv`, `__pycache__`, cache kiểm thử, file editor và `.env*` ngoại trừ một template không có giá trị thật. Đây là kiểm soát ở biên dữ liệu: file không vào context thì Dockerfile không thể vô tình sao chép nó.\n\n### Chọn base image có chủ đích\n\n`python:slim` thường là điểm cân bằng tốt cho FastAPI: có hệ sinh thái Debian quen thuộc nhưng bỏ nhiều gói không cần. Image Alpine nhỏ hơn trên đĩa, song dùng musl thay glibc; một số Python wheel không có bản tương thích sẽ phải biên dịch, làm build lâu và phát sinh toolchain. “Nhỏ hơn” có thể đổi thành “khó vá và khó debug hơn”.\n\nKhông dùng tag trôi nổi như `python:latest`, vì cùng Dockerfile có thể cho kết quả khác vào ngày mai. Khóa một tag cụ thể giúp ổn định; khóa cả digest cho tính tái lập mạnh hơn nhưng phải có quy trình cập nhật digest để nhận bản vá. Đóng băng mãi một base image cũng là lỗi bảo mật.\n\n### Layer cache và thứ tự `COPY`\n\nBuilder tính cache từ instruction và dữ liệu đầu vào. Dependency đổi ít hơn source code, nên sao chép lock file rồi cài package trước, sau đó mới sao chép application. Khi chỉ sửa `app/main.py`, layer cài dependency còn hợp lệ.\n\n```dockerfile\nFROM python:3.12-slim AS runtime\nWORKDIR /app\n\nENV PYTHONDONTWRITEBYTECODE=1 \\\n    PYTHONUNBUFFERED=1\n\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY app ./app\n\nCMD [\"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\nNếu đặt `COPY . .` trước `pip install`, mọi thay đổi README hay test đều phá cache dependency. Gộp tất cả instruction thành một dòng khổng lồ có thể giảm layer nhưng làm cache kém hữu ích và khó đọc; số layer tự nó không phải mục tiêu.\n\n### Dependency tái lập và kiểm tra hash\n\n`requirements.txt` chỉ ghi `fastapi` cho phép resolver chọn bản mới ở mỗi lần build. Cần khóa phiên bản trực tiếp và bắc cầu bằng lock file do công cụ quản lý dependency sinh ra. Với yêu cầu chuỗi cung ứng nghiêm ngặt, hash của distribution giúp phát hiện gói bị thay thế. Tuy nhiên lock file phải được cập nhật có kiểm soát; dependency cũ mang lỗ hổng không tốt hơn dependency trôi nổi.\n\nPackage manager của hệ điều hành cũng cần kỷ luật tương tự. Khi buộc phải `apt-get install`, chạy cập nhật chỉ mục và cài trong cùng một `RUN`, dùng `--no-install-recommends`, rồi xóa package index. Không cài compiler vào runtime chỉ vì một wheel cần build.\n\n### Multi-stage build: tách xưởng khỏi sản phẩm\n\nNếu Sổ Chi Tiêu cần biên dịch `psycopg` hoặc frontend assets, stage `builder` có compiler và header; stage cuối chỉ nhận wheel hoặc artifact. Toolchain không đi vào production nên image nhẹ và bề mặt tấn công giảm.\n\n```dockerfile\nFROM python:3.12-slim AS builder\nWORKDIR /build\nCOPY requirements.txt .\nRUN pip wheel --wheel-dir=/wheels -r requirements.txt\n\nFROM python:3.12-slim AS runtime\nRUN groupadd --system app && useradd --system --gid app app\nWORKDIR /app\nCOPY --from=builder /wheels /wheels\nRUN pip install --no-cache-dir /wheels/* && rm -rf /wheels\nCOPY --chown=app:app app ./app\nUSER app\nEXPOSE 8000\nCMD [\"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]\n```\n\nMulti-stage không đáng dùng nếu ứng dụng thuần Python đã có wheel và stage builder chẳng loại được gì. Thêm stage khi nó tạo ranh giới thật, không phải để Dockerfile trông cao cấp.\n\n### PID 1, tín hiệu dừng và exec form\n\nDocker gửi `SIGTERM` cho PID 1 rồi chờ trước khi buộc dừng. Dạng JSON của `CMD` chạy Uvicorn trực tiếp; dạng shell `CMD uvicorn ...` thường đặt shell làm PID 1, có thể làm tín hiệu không đến đúng process. Khi nhận tín hiệu, server ngừng nhận kết nối mới và cho request đang xử lý hoàn tất trong giới hạn của nền tảng.\n\nKhông dùng `--reload` ở production: watcher tạo process phụ, tăng tài nguyên và được thiết kế cho phát triển. Số worker cũng không nên đóng cứng tùy tiện. Một worker đơn giản hóa memory; nhiều worker tận dụng nhiều CPU nhưng mỗi worker mở pool database và giữ cache riêng. Giới hạn kết nối PostgreSQL phải quyết định số worker, không phải câu thần chú “CPU × 2 + 1”.\n\n### Port, interface và health endpoint\n\n`EXPOSE 8000` chỉ là metadata, không publish cổng. Trên máy local cần `-p 8000:8000`; ở PaaS, router thường chuyển request tới port do biến môi trường cung cấp. Server nghe `127.0.0.1` chỉ nhận kết nối trong chính container, nên proxy bên ngoài không chạm được. Vì thế Uvicorn nghe `0.0.0.0`.\n\nEndpoint `/health/live` chỉ cần chứng minh event loop còn đáp ứng; endpoint `/health/ready` có thể kiểm tra ứng dụng đã sẵn sàng nhận tải. Không nên để readiness gọi hàng loạt dịch vụ bên ngoài trên mỗi probe, vì một dependency chập chờn sẽ tạo bão kiểm tra và liên tục loại instance. Health response cũng không được lộ chuỗi kết nối hay stack trace.\n\n### User không đặc quyền và filesystem chỉ đọc\n\nProcess mặc định có thể chạy root trong container. Namespace giảm phạm vi nhưng một lỗ hổng ứng dụng kết hợp sai cấu hình runtime vẫn nguy hiểm. Tạo user hệ thống, chuyển quyền đúng thư mục rồi `USER app`. Khi nền tảng hỗ trợ, dùng root filesystem chỉ đọc và chỉ cấp thư mục tạm cần thiết.\n\nĐừng `chmod -R 777 /app`: nó che lỗi ownership bằng cách cho mọi process ghi code. API bình thường chỉ cần đọc code; upload nên đi object storage, dữ liệu bền vững đi PostgreSQL. Nguyên tắc quyền tối thiểu khiến một lỗi path traversal khó biến thành sửa mã ứng dụng.\n\n### Secret không thuộc về image\n\nKhông dùng `ARG DATABASE_URL` rồi ghi vào `ENV`; build argument có thể xuất hiện trong lịch sử hoặc cache. Image phải dùng được ở nhiều môi trường, còn `DATABASE_URL`, `JWT_SECRET` và mật khẩu Redis được đưa vào lúc chạy qua secret manager hoặc biến môi trường của nền tảng.\n\nLog cấu hình để debug cũng cần lọc. In toàn bộ environment là cách nhanh nhất đưa credential vào log tập trung. Chỉ log tên môi trường, revision và các cờ không nhạy cảm.\n\n### Kiểm chứng image thay vì tin vào Dockerfile\n\nBuild thành công mới chứng minh cú pháp hợp lệ. Cần chạy container, gọi health endpoint, chạy test bằng chính image, xem `docker history`, kiểm tra user và quét dependency. Một smoke test hữu ích còn gửi `SIGTERM` để chắc process thoát sạch.\n\nGắn tag theo commit dễ đọc, nhưng deploy nên bám digest vì tag có thể bị trỏ lại. Khi rollback, chọn digest cũ đã qua test; không rebuild source cũ, bởi rebuild có thể kéo base hoặc dependency khác.\n\n### Khi nào không nên dùng Docker\n\nMột script cá nhân chạy một lần, serverless function do nền tảng quản lý runtime hoặc môi trường học nhập môn có thể không cần container. Docker thêm daemon, registry, quét image và kiến thức network/filesystem. Nếu nhóm chưa vận hành nổi chuỗi đó, một PaaS buildpack đơn giản có thể an toàn hơn. Chỉ chọn Docker khi lợi ích artifact nhất quán và khả năng di chuyển lớn hơn chi phí vận hành.",

vi_du: "### FastAPI tối thiểu phù hợp với container\n\nVí dụ dưới đây không cần database để smoke test image; bài Compose sẽ nối PostgreSQL và Redis. Tạo `app/main.py`:\n\n```python\nimport os\nfrom fastapi import FastAPI\n\napp = FastAPI(title=\"Sổ Chi Tiêu API\")\n\n@app.get(\"/health/live\")\ndef live() -> dict[str, str]:\n    return {\"status\": \"alive\", \"revision\": os.getenv(\"APP_REVISION\", \"dev\")}\n\n@app.get(\"/expenses/{expense_id}\")\ndef get_expense(expense_id: int) -> dict[str, int | str]:\n    return {\"id\": expense_id, \"amount\": 125_000, \"currency\": \"VND\"}\n```\n\n`requirements.txt` cần các phiên bản đã được dự án khóa. Sau khi có Dockerfile ở phần cơ chế, chạy:\n\n```bash\ndocker build -t expense-api:local .\ndocker run --rm -p 8000:8000 --read-only --tmpfs /tmp \\\n  -e APP_REVISION=local-test expense-api:local\ncurl http://localhost:8000/health/live\ncurl http://localhost:8000/expenses/1\n```\n\nKết quả health phải có `status` bằng `alive`; request id âm hiện vẫn được nhận, qua đó lộ một ca biên cần validation ở tầng nghiệp vụ. Có thể sửa route thành `expense_id: Annotated[int, Path(gt=0)]` để FastAPI trả 422 trước khi chạm service.\n\n### Kiểm tra user và tín hiệu\n\n```bash\ndocker run --rm expense-api:local id\ndocker inspect expense-api:local --format '{{json .Config.User}}'\n```\n\nKết quả không được là root hoặc chuỗi rỗng. Để thử graceful shutdown, chạy container có tên, gửi một request rồi dùng `docker stop --time 10 expense-api-test`; log phải cho thấy server hoàn tất shutdown thay vì bị kill. Đây là phép thử hành vi runtime, không phải benchmark nên không bịa số thời gian.\n\n### Ca biên cần giữ trong test\n\nImage phải khởi động khi không có file `.env`; thiếu secret bắt buộc thì nên fail sớm với thông báo chỉ nêu tên biến. Request rỗng phải nhận lỗi validation; payload cực lớn cần bị chặn ở proxy hoặc application; đường dẫn ghi file phải thất bại dưới filesystem chỉ đọc trừ `/tmp`. Cuối cùng, build trên kiến trúc đích hoặc tạo image đa kiến trúc nếu máy phát triển ARM còn server AMD64.",

so_sanh: "### Dockerfile gọn, máy ảo và buildpack\n\n| Tiêu chí | Docker image tự viết | PaaS buildpack | Máy ảo cài thủ công |\n|---|---|---|---|\n| Quyền kiểm soát runtime | Cao, thấy rõ base và command | Trung bình, nền tảng suy luận | Cao nhưng trạng thái dễ drift |\n| Tái sử dụng artifact | Tốt nếu deploy theo digest | Phụ thuộc nền tảng | Thường cài lại trên từng máy |\n| Vá hệ điều hành | Phải rebuild image | Nền tảng gánh phần lớn | Đội vận hành tự cập nhật máy |\n| Bề mặt tấn công | Kiểm soát bằng stage và package | Ít tùy biến hơn | Dễ tích tụ service và package |\n| Độ phức tạp ban đầu | Dockerfile, registry, scan | Thấp | Thấp lúc đầu, cao về lâu dài |\n| Phù hợp | API có CI/CD và nhiều môi trường | Nhóm nhỏ muốn ra mắt nhanh | Phần mềm legacy hoặc cần kiểm soát OS |\n\nDistroless còn nhỏ hơn `slim` và thiếu shell, giảm công cụ cho kẻ tấn công nhưng cũng làm debug tại chỗ khó. Đó là lựa chọn tốt khi observability đã trưởng thành và đội ngũ có cách tạo debug image riêng. Với Sổ Chi Tiêu đang học triển khai, `slim` minh bạch hơn.\n\nDocker cũng không thay virtual environment trong quá trình phát triển: virtual environment cách ly package Python nhanh và nhẹ; container cách ly cả user-space filesystem và network. Có thể dùng cả hai ở những biên khác nhau, nhưng không cần tạo `.venv` bên trong image nếu cài package vào môi trường runtime chuyên biệt đã bị cô lập.",

loi_thuong_gap: [
  "**Sao chép cả repo trước khi cài dependency.** Nhận biết bằng việc sửa một dòng code cũng khiến `pip install` chạy lại; tách lock file sang layer sớm hơn.",
  "**Đưa `.env` hoặc SSH key vào build context.** Nhận biết qua `docker history`, kích thước context hoặc quét secret; chặn bằng `.dockerignore` và xoay credential nếu đã lộ.",
  "**Chạy Uvicorn trên `127.0.0.1`.** Container báo healthy nội bộ nhưng host bị connection refused; nghe trên `0.0.0.0` và publish đúng port.",
  "**Dùng shell form cho process chính.** `docker stop` kết thúc chậm hoặc phải SIGKILL; dùng exec form và kiểm tra graceful shutdown.",
  "**Chạy bằng root và cấp quyền 777.** `docker inspect` cho user rỗng, application sửa được code; tạo user không đặc quyền và chỉ cấp đường dẫn cần ghi.",
  "**Dùng tag `latest` làm định danh release.** Hai máy kéo cùng tên nhưng khác bytes; ghi digest và revision vào metadata triển khai.",
  "**Nhét compiler vào runtime vô điều kiện.** Image chứa `gcc` và header dù ứng dụng chỉ chạy wheel; dùng multi-stage khi thực sự có bước build.",
  "**Ghi dữ liệu nghiệp vụ vào container.** Restart hoặc rollout làm mất file giao dịch; đưa state sang PostgreSQL/object storage và thử thay container trong test."
],

tu_kiem_tra: [
  "Vì sao xóa `.env` ở một instruction sau không bảo đảm secret biến mất khỏi image đã từng `COPY` nó?",
  "Hãy giải thích vì sao đổi `COPY requirements.txt` và `COPY app` cho nhau làm thời gian build lặp lại tăng dù image cuối có cùng file.",
  "Nếu mỗi Uvicorn worker mở pool 10 kết nối và PostgreSQL cho ứng dụng 40 kết nối, tối đa bao nhiêu worker hợp lý trước khi dành chỗ cho migration và tác vụ nền?",
  "Tại sao `EXPOSE 8000` không đủ để gọi API từ host, và ba lớp nào phải khớp để request đến FastAPI?",
  "Multi-stage build giảm rủi ro nào, và trong trường hợp thuần Python nào nó chỉ làm Dockerfile phức tạp thêm?",
  "Thiết kế một smoke test phát hiện đồng thời lỗi chạy root, lỗi health endpoint và lỗi xử lý SIGTERM.",
  "Nếu khóa base image bằng digest, quy trình nào vẫn cần có để không biến tính tái lập thành việc giữ mãi lỗ hổng cũ?"
],

tai_nguyen: [
  "Docker Documentation — các mục Dockerfile, build cache và multi-stage builds.",
  "Docker Deep Dive — Nigel Poulton.",
  "FastAPI Documentation — phần Deployment và Server Workers.",
  "Container Security — Liz Rice."
],

lien_he: [
  "Nối sang mảng Hệ điều hành: container dựa vào process, signal, namespace, cgroup và filesystem permission chứ không phải một máy ảo đầy đủ.",
  "Nối sang mảng Mạng máy tính: port publish, địa chỉ bind và reverse proxy quyết định đường đi của request tới Uvicorn.",
  "Nối sang mảng Cơ sở dữ liệu: API phải stateless còn transaction và dữ liệu bền vững thuộc PostgreSQL; pool kết nối chi phối số worker.",
  "Nối sang mảng Bảo mật: least privilege, quản lý secret và kiểm soát chuỗi cung ứng làm giảm hậu quả khi dependency hoặc endpoint bị khai thác."
]

};
