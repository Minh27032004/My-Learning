window.LESSONS["05:thiet-ke-api:nguyen-tac-rest-va-thiet-ke-tai-nguyen"] = {

tom_tat: [
  "REST tổ chức API quanh **tài nguyên** và trạng thái của chúng, không quanh tên hàm phía server; URI trả lời “đối tượng nào”, còn HTTP method trả lời “muốn làm gì”.",
  "Một tài nguyên nghiệp vụ không nhất thiết trùng một bảng cơ sở dữ liệu. `Order` có thể tổng hợp dòng hàng, tiền và liên kết nhưng vẫn có một định danh ổn định.",
  "`GET`, `POST`, `PUT`, `PATCH`, `DELETE` khác nhau về semantics, tính an toàn và tính idempotent; chọn sai làm cache, retry và client hoạt động sai.",
  "Thiết kế tốt phải diễn đạt vòng đời đơn hàng, quyền truy cập, trạng thái HTTP và liên kết giữa tài nguyên, đồng thời không làm lộ cấu trúc nội bộ.",
  "REST không phải mục tiêu tự thân. Với thao tác nghiệp vụ khó biểu diễn như tài nguyên, endpoint hành động có kiểm soát thường rõ hơn việc giả vờ CRUD thuần túy."
],

can_biet_truoc: [
  "HTTP request/response, header, body và các status code phổ biến như `200`, `201`, `204`, `400`, `401`, `403`, `404`, `409`.",
  "JSON object, array, `null` và cách FastAPI dùng Pydantic model để kiểm tra đầu vào.",
  "Khái niệm khóa chính, khóa ngoại và transaction trong cơ sở dữ liệu quan hệ.",
  "Xác thực danh tính khác với phân quyền: biết người gọi là ai chưa có nghĩa họ được đọc mọi đơn hàng."
],

dinh_nghia: "**REST (Representational State Transfer)** là một phong cách kiến trúc trong đó hệ thống phơi bày các **tài nguyên có định danh**, client thao tác trên biểu diễn của tài nguyên bằng semantics chuẩn của HTTP, mỗi request chứa đủ ngữ cảnh cần thiết để server xử lý, và response có thể nói rõ khả năng cache.\n\nTrong API cửa hàng sách, `/orders/ord_7F3` định danh một đơn hàng; JSON là một biểu diễn hiện tại của đơn đó, không phải bản thân tài nguyên. Cùng tài nguyên về lý thuyết có thể có biểu diễn JSON, CSV hoặc HTML. `GET /orders/ord_7F3` đọc biểu diễn; `PATCH` gửi phần thay đổi; `DELETE` yêu cầu bỏ tài nguyên theo chính sách nghiệp vụ.\n\n“RESTful” không đồng nghĩa “mọi thứ là CRUD” và cũng không có nghĩa URL đẹp là đủ. Thiết kế chỉ nhất quán khi URI, method, status code, header, cache, quyền truy cập và chuyển trạng thái cùng kể một câu chuyện không mâu thuẫn.",

vi_sao_can: "API là hợp đồng sống lâu hơn phần lớn implementation. Ứng dụng web, mobile, tác vụ nền và đối tác có thể cùng phụ thuộc vào nó, trong khi cơ sở dữ liệu và code server vẫn thay đổi. Nếu hợp đồng mô tả các hàm nội bộ như `/runCreateOrderService`, mỗi lần refactor sẽ lan sang client. Tài nguyên tạo một ranh giới ổn định hơn: phía ngoài cần biết đơn hàng tồn tại và có trạng thái, không cần biết server gọi repository nào.\n\nSemantics HTTP còn cho hạ tầng ra quyết định đúng. Proxy có thể cache `GET` nhưng không tự cache `POST`; client có thể retry một `PUT` idempotent sau khi mất response nhưng phải thận trọng với `POST`; công cụ quan sát có thể nhóm tỷ lệ lỗi theo method và route. Nếu dùng `POST /getOrder` cho mọi việc, ta tự vứt bỏ vốn từ chung mà browser, gateway và thư viện đã hiểu.\n\nThiết kế tài nguyên cũng buộc đội ngũ nhìn thẳng vào nghiệp vụ. Một đơn `draft` có được xóa không? Sau khi `paid`, trường địa chỉ có được sửa? Khách A có được biết mã đơn của khách B tồn tại? Những câu hỏi này ảnh hưởng bảo mật và tính đúng đắn hơn việc chọn tên controller, nhưng thường bị che khuất nếu API chỉ phản chiếu bảng dữ liệu.",

co_che: "### Bắt đầu từ danh từ nghiệp vụ, không bắt đầu từ controller\n\nHãy liệt kê những thứ người dùng có thể nhận diện và nói về chúng: sách, giỏ hàng, đơn hàng, dòng hàng, khoản thanh toán. Sau đó xác định vòng đời và chủ sở hữu. Với ví dụ xuyên suốt, `Order` có `id`, `customer_id`, các `items`, `status`, `total_amount`, `created_at`. Đây là aggregate nghiệp vụ: thay đổi dòng hàng phải kéo theo tính lại tổng tiền và chỉ hợp lệ khi đơn còn ở trạng thái cho phép.\n\nKhông nên tạo `/order_items/{id}` chỉ vì có bảng `order_items`. Nếu dòng hàng không có ý nghĩa độc lập ngoài đơn, URI `/orders/{order_id}/items/{item_id}` diễn đạt phạm vi tốt hơn. Ngược lại, `Book` sống độc lập và được nhiều đơn tham chiếu nên xứng đáng có `/books/{book_id}`.\n\nMột phép thử hữu ích là hỏi: client có lưu đường dẫn này làm bookmark và hiểu nó sau sáu tháng không? Nếu URI chứa tên lớp, tên hàm hay bước implementation như `/orderRepository/findByPk`, hợp đồng đang rò rỉ chi tiết server.\n\n### URI định danh, method biểu đạt ý định\n\nGiữ URI là danh từ; dùng HTTP method thay cho động từ lặp lại:\n\n| Ý định | Request phù hợp | Lý do |\n|---|---|---|\n| xem bộ sưu tập đơn | `GET /orders` | đọc collection, không đổi trạng thái |\n| xem một đơn | `GET /orders/{order_id}` | định danh member cụ thể |\n| tạo đơn | `POST /orders` | server cấp mã và thêm member mới |\n| thay toàn bộ biểu diễn có thể sửa | `PUT /orders/{order_id}` | cùng payload lặp lại hướng tới cùng trạng thái |\n| sửa một phần | `PATCH /orders/{order_id}` | chỉ gửi trường cần đổi |\n| hủy theo kiểu xóa tài nguyên | `DELETE /orders/{order_id}` | phù hợp chỉ khi nghiệp vụ coi hủy là xóa |\n\n`POST /orders/create` không gây lỗi kỹ thuật, nhưng từ `create` thừa và làm hợp đồng kém đều. Quan trọng hơn, đừng biến mọi method thành `POST` để “đơn giản”. Khi đó retry, cache, CSRF policy và log mất tín hiệu semantics.\n\n### Safety và idempotency là hai thuộc tính khác nhau\n\nMethod **safe** là method không được người gọi kỳ vọng làm thay đổi trạng thái nghiệp vụ; `GET` và `HEAD` thuộc nhóm này. Server vẫn có thể ghi access log hoặc tăng metric, nhưng một crawler gọi `GET` không được vô tình hủy đơn. Vì vậy `GET /orders/ord_7F3/cancel` là lỗi nguy hiểm: trình duyệt prefetch link cũng có thể kích hoạt hành động.\n\nMethod **idempotent** là gọi một lần hay nhiều lần với cùng ý định tạo ra trạng thái cuối tương đương. `PUT` và `DELETE` được định nghĩa idempotent; response lần hai có thể khác, nhưng không được xóa thêm một đơn khác. `POST` thông thường không idempotent: hai request tạo đơn có thể tạo hai bản ghi. Bài sau về idempotency sẽ thêm khóa chống trùng cho trường hợp mạng timeout.\n\n| Method | Safe | Idempotent theo semantics | Tình huống đơn hàng |\n|---|---:|---:|---|\n| `GET` | có | có | đọc đơn |\n| `POST` | không | thường không | tạo đơn mới |\n| `PUT` | không | có | thay biểu diễn tại URI đã biết |\n| `PATCH` | không | không được đảm bảo | cập nhật địa chỉ |\n| `DELETE` | không | có | xóa/hủy tài nguyên theo hợp đồng |\n\n### Collection, member và tài nguyên con\n\nCollection `/orders` trả danh sách và nhận yêu cầu tạo member. Member `/orders/ord_7F3` trả đúng một đơn. Sự phân biệt này giúp status code tự nhiên: tạo thành công trả `201 Created` kèm `Location: /orders/ord_7F3`; đọc collection rỗng vẫn trả `200` với `[]`, không phải `404`, vì collection tồn tại dù chưa có phần tử.\n\nLồng tài nguyên cho biết quan hệ, nhưng lồng quá sâu làm URI giòn. `/customers/cus_9/orders/ord_7F3/items/item_2` chứa nhiều định danh phải kiểm tra và khó tái sử dụng. Nếu `order_id` đã toàn cục, `/orders/ord_7F3/items/item_2` đủ rõ; quyền sở hữu được kiểm tra ở server chứ không được “bảo đảm” chỉ nhờ đường dẫn có customer.\n\nCa biên bảo mật đáng chú ý là **IDOR** (Insecure Direct Object Reference, tham chiếu đối tượng trực tiếp không an toàn). Route đúng hình thức vẫn nguy hiểm nếu query chỉ `WHERE id = :id`. Nó phải ràng buộc `customer_id` của danh tính hiện tại, hoặc kiểm tra role nhân viên. Với khách không sở hữu đơn, nhiều hệ thống trả `404` thay vì `403` để không xác nhận mã đơn có tồn tại.\n\n### Representation không phải hàng trong bảng\n\nJSON công khai nên phục vụ client, không serialize thẳng ORM model. Cột `password_hash`, `fraud_score`, ghi chú nội bộ hoặc khóa nhà cung cấp tuyệt đối không được lọt ra chỉ vì được thêm vào bảng. Response model tạo allowlist rõ ràng; trường mới trong database mặc định không xuất hiện.\n\nTiền không dùng `float`, vì số nhị phân không biểu diễn chính xác nhiều số thập phân. API có thể trả chuỗi decimal như `\"259000.00\"` kèm `currency: \"VND\"`, hoặc số nguyên theo đơn vị nhỏ nhất nếu miền tiền tệ thống nhất. Thời gian nên có múi giờ, chẳng hạn `2026-09-01T10:30:00+07:00`, vì chuỗi thiếu offset gây diễn giải khác nhau giữa server và client.\n\nRepresentation cũng có thể chứa liên kết hoặc mã trạng thái được tính từ nhiều bảng. Điều đó không phá REST; tài nguyên là abstraction phía hợp đồng. Chỉ cần tránh query N+1 khi tạo danh sách: tải 100 đơn rồi gọi database riêng cho từng dòng hàng sẽ biến một request thành hàng trăm truy vấn.\n\n### Chuyển trạng thái là luật nghiệp vụ, không phải phép gán tự do\n\nNếu cho `PATCH {\"status\": \"shipped\"}`, client có thể bỏ qua thanh toán và đẩy đơn từ `draft` sang `shipped`. Trạng thái cần một state machine: `draft → pending_payment → paid → packing → shipped → delivered`; một số trạng thái cho phép `cancelled`, một số không.\n\nCó hai cách công khai. Cách thứ nhất dùng `PATCH` nhưng server kiểm tra transition và quyền. Cách thứ hai mô hình hóa command thành tài nguyên sự kiện, ví dụ `POST /orders/{id}/cancellations`. Cách hai phù hợp khi việc hủy có dữ liệu riêng như lý do, người duyệt và thời điểm. Nó không phải “REST thất bại”; cancellation thật sự là một tài nguyên có vòng đời và audit trail.\n\nKhông nên ép `DELETE` nếu lịch sử đơn phải giữ vì kế toán. `DELETE` khiến người đọc kỳ vọng tài nguyên không còn truy cập được, trong khi nghiệp vụ chỉ đổi trạng thái. Khi đó `POST /orders/{id}/cancellations` hoặc một transition được kiểm soát trung thực hơn.\n\n### Status code là một phần của hợp đồng\n\n`200 OK` dùng khi response có biểu diễn; `201 Created` khi tạo tài nguyên và nên kèm `Location`; `204 No Content` khi thành công mà không trả body. Không trả body JSON cùng `204`, vì client và proxy có thể bỏ nó theo semantics HTTP.\n\nSai cú pháp hoặc validation thường là `400` hay `422` tùy quy ước; thiếu xác thực là `401` và thường có `WWW-Authenticate`; đã xác thực nhưng thiếu quyền là `403`; không tìm thấy là `404`; xung đột trạng thái như sửa đơn đã giao là `409`. Lỗi bất ngờ phía server là `500`, nhưng response không được lộ stack trace hay câu SQL.\n\nKhông phải mọi thất bại nghiệp vụ đều là `200 {\"success\": false}`. Cách đó buộc client hiểu body trước khi biết request thất bại, làm metric và retry policy của gateway sai. Bài định dạng lỗi sẽ xây một envelope có `code`, `message`, `details` và `trace_id` nhưng vẫn giữ status code đúng.\n\n### Stateless không có nghĩa server không lưu dữ liệu\n\nRàng buộc stateless nói mỗi request phải mang đủ ngữ cảnh phiên để xử lý, không nói server không được có database. Đơn hàng dĩ nhiên được lưu. Điều cần tránh là request thứ hai chỉ có nghĩa nếu tình cờ đến đúng process đã giữ state phiên trong RAM.\n\nToken hoặc session cookie có thể xác định người dùng; session vẫn có thể lưu tập trung. Khi triển khai nhiều instance, quyền và dữ liệu phải nhất quán dù load balancer chuyển request. Đừng đặt toàn bộ giỏ hàng chưa lưu vào biến toàn cục của một worker: restart sẽ mất, nhiều worker nhìn thấy nội dung khác nhau, và kẻ tấn công có thể khai thác sự lệch đó.\n\n### Cache cần chỉ thị cụ thể và phải tôn trọng dữ liệu riêng tư\n\n`GET` có thể cache, nhưng đơn hàng là dữ liệu cá nhân. Response nên dùng `Cache-Control: private, no-store` nếu không muốn browser hoặc proxy lưu; tài nguyên sách công khai có thể `public, max-age=...` và dùng `ETag`. Cache key phải phân biệt authorization khi nội dung phụ thuộc người dùng. Một reverse proxy cache `/orders/ord_7F3` chỉ theo URL có thể phát đơn của khách A cho khách B.\n\nConditional request với `ETag` còn giúp tránh ghi đè đồng thời: client đọc phiên bản `\"v8\"`, gửi `If-Match: \"v8\"` khi sửa; server trả `412 Precondition Failed` nếu đơn đã thành `v9`. Đây là optimistic concurrency, hữu ích khi hai nhân viên xử lý cùng đơn.\n\n### Khi nào không nên cố dùng REST\n\nREST phù hợp cho API hướng tài nguyên, cần tận dụng HTTP và có client đa dạng. Nó kém tự nhiên với stream hai chiều thời gian thực, truy vấn đồ thị tùy biến hoặc RPC nội bộ hiệu năng cao có schema chặt. WebSocket, GraphQL hay gRPC có thể phù hợp hơn; bài so sánh cuối module sẽ đặt chúng cạnh REST.\n\nNgay trong REST, đừng biến mọi nghiệp vụ thành phép cập nhật cột. Endpoint hành động như `/orders/{id}/quote-shipping` có thể rõ hơn nếu nó là phép tính không tạo tài nguyên bền vững. Điều cần giữ là semantics trung thực, quyền rõ, retry được cân nhắc và tài liệu nói chính xác; “thuần REST” không đáng để đánh đổi khả năng hiểu hệ thống.",

vi_du: "### FastAPI tối thiểu nhưng có ranh giới an toàn\n\nVí dụ sau chạy bằng `uvicorn app:app --reload` sau khi cài `fastapi` và `uvicorn`. Kho dữ liệu trong RAM chỉ để minh họa; production phải dùng database và transaction.\n\n```python\nfrom datetime import datetime, timezone\nfrom decimal import Decimal\nfrom enum import StrEnum\nfrom uuid import uuid4\n\nfrom fastapi import Depends, FastAPI, Header, HTTPException, Response, status\nfrom pydantic import BaseModel, ConfigDict, Field\n\napp = FastAPI()\n\nclass OrderStatus(StrEnum):\n    DRAFT = \"draft\"\n    PENDING_PAYMENT = \"pending_payment\"\n    PAID = \"paid\"\n\nclass OrderItemIn(BaseModel):\n    book_id: str = Field(min_length=1, max_length=64)\n    quantity: int = Field(ge=1, le=20)\n\nclass OrderCreate(BaseModel):\n    items: list[OrderItemIn] = Field(min_length=1, max_length=50)\n\nclass OrderOut(BaseModel):\n    model_config = ConfigDict(from_attributes=True)\n    id: str\n    items: list[OrderItemIn]\n    status: OrderStatus\n    total_amount: Decimal\n    currency: str\n    created_at: datetime\n\nDB: dict[str, dict] = {}\n\ndef current_customer(x_customer_id: str = Header()) -> str:\n    # Demo nhận header trực tiếp; production phải xác minh token/chữ ký.\n    if not x_customer_id.startswith(\"cus_\"):\n        raise HTTPException(status_code=401, detail=\"invalid identity\")\n    return x_customer_id\n\n@app.post(\"/orders\", response_model=OrderOut, status_code=status.HTTP_201_CREATED)\ndef create_order(payload: OrderCreate, response: Response,\n                 customer_id: str = Depends(current_customer)):\n    order_id = f\"ord_{uuid4().hex[:12]}\"\n    order = {\n        \"id\": order_id,\n        \"customer_id\": customer_id,\n        \"items\": [item.model_dump() for item in payload.items],\n        \"status\": OrderStatus.DRAFT,\n        \"total_amount\": Decimal(\"129000\") * sum(i.quantity for i in payload.items),\n        \"currency\": \"VND\",\n        \"created_at\": datetime.now(timezone.utc),\n    }\n    DB[order_id] = order\n    response.headers[\"Location\"] = f\"/orders/{order_id}\"\n    return order\n\n@app.get(\"/orders/{order_id}\", response_model=OrderOut)\ndef get_order(order_id: str, customer_id: str = Depends(current_customer)):\n    order = DB.get(order_id)\n    # Gộp không tồn tại và không sở hữu để tránh dò mã đơn.\n    if order is None or order[\"customer_id\"] != customer_id:\n        raise HTTPException(status_code=404, detail=\"order not found\")\n    return order\n```\n\nThử tạo rồi đọc:\n\n```bash\ncurl -i -X POST http://127.0.0.1:8000/orders \\\n  -H \"Content-Type: application/json\" -H \"X-Customer-Id: cus_9\" \\\n  -d '{\"items\":[{\"book_id\":\"book_python\",\"quantity\":2}]}'\n\ncurl -i http://127.0.0.1:8000/orders/ord_REPLACE \\\n  -H \"X-Customer-Id: cus_9\"\n```\n\nResponse không có `customer_id` vì `OrderOut` chủ động chọn trường công khai. Tuy nhiên ví dụ chưa giải quyết tạo trùng khi retry, đồng thời dictionary không an toàn giữa nhiều process; đó là giới hạn được ghi rõ, không phải mẫu production giả dạng hoàn chỉnh.\n\n### Kiểm các ca biên trước khi nối database\n\nPayload `items: []` bị chặn bởi `min_length=1`; quantity bằng `0` hoặc lớn hơn `20` bị validation từ chối; mã người dùng sai định dạng trả `401`; khách khác đọc cùng `order_id` nhận `404`. Còn hai request `POST` giống nhau vẫn tạo hai đơn: hành vi đúng với semantics mặc định của POST, nhưng chưa phù hợp nút “Thanh toán” có thể retry. Ta sẽ bổ sung `Idempotency-Key` ở bài riêng.",

so_sanh: "### Tài nguyên REST so với RPC và CRUD phản chiếu database\n\n| Tiêu chí | REST hướng tài nguyên | RPC hướng hàm | CRUD phản chiếu bảng |\n|---|---|---|---|\n| Hợp đồng chính | URI + HTTP semantics + representation | tên procedure và tham số | bảng, cột, khóa |\n| Ví dụ | `POST /orders` | `CreateOrder()` | `POST /order_rows` |\n| Điểm mạnh | dễ hiểu với web, cache và gateway hỗ trợ | diễn đạt command phức tạp trực tiếp | làm prototype quản trị nhanh |\n| Rủi ro | ép nghiệp vụ thành CRUD giả tạo | tên hàm tăng nhanh, ít tận dụng HTTP | rò schema, over-posting, khó đổi database |\n| Phù hợp | API công khai hướng tài nguyên | nội bộ hoặc command rõ | công cụ nội bộ có kiểm soát |\n\nRPC không “kém hiện đại”; nó chỉ chọn abstraction khác. `POST /orders/{id}:cancel` đôi khi đọc rõ hơn một `PATCH` mơ hồ. Tuy vậy, nếu mọi thao tác thành `/getOrder`, `/updateOrderStatus`, `/deleteOrder`, ta đang tự xây lại từ vựng mà HTTP đã có, nhưng kém chuẩn hóa hơn.\n\nCRUD generator hữu ích cho back-office tạm thời, song không nên mở thẳng ra Internet. Cho client gửi mọi cột dễ dẫn tới mass assignment: kẻ tấn công thêm `is_admin`, `fraud_approved` hoặc `customer_id` vào JSON. Response/request model riêng và use case nghiệp vụ là lớp bảo vệ cần thiết.\n\n**Khi không chọn REST:** stream cập nhật giao hàng liên tục cần WebSocket; truy vấn màn hình có hình dạng thay đổi mạnh có thể hợp GraphQL; giao tiếp service-to-service cần hiệu năng và sinh client chặt có thể dùng gRPC. Chọn theo lực căng thật của hệ thống, không theo khẩu hiệu.",

loi_thuong_gap: [
  "**Dùng động từ trong mọi URI** như `/createOrder` và `/getOrderById`. Dấu hiệu là route tăng theo số hàm; sửa bằng cách gom quanh collection/member và để method biểu đạt thao tác.",
  "**Cho `GET` thay đổi trạng thái.** Crawler, prefetch hoặc cache có thể kích hoạt ngoài ý muốn; rà soát mọi GET và bảo đảm nó không hủy, thanh toán hay gửi email nghiệp vụ.",
  "**Serialize thẳng ORM model.** Một cột nội bộ mới bỗng xuất hiện trong JSON; nhận biết bằng snapshot/schema response và sửa bằng response model allowlist.",
  "**Chỉ tìm đơn theo `id`.** Khách đổi mã trên URL vẫn đọc được đơn người khác; query phải ràng buộc principal hiện tại hoặc policy phân quyền.",
  "**Cho client gán tùy ý `status`.** Log xuất hiện transition bất khả thi như `draft → shipped`; dùng state machine và endpoint command có kiểm tra.",
  "**Trả `200` cho mọi thất bại.** Dashboard báo hệ thống khỏe dù body chứa lỗi; dùng status code đúng và envelope lỗi nhất quán.",
  "**Lồng URI theo toàn bộ cây database.** Đường dẫn dài, nhiều ID mâu thuẫn; chỉ giữ quan hệ cha cần cho định danh hoặc ngữ cảnh.",
  "**Cache dữ liệu riêng theo URL chung.** Người dùng thấy response của nhau; đặt cache directive phù hợp và kiểm cache key có xét authorization."
],

tu_kiem_tra: [
  "Vì sao `GET /orders/{id}/cancel` nguy hiểm ngay cả khi frontend không bao giờ tạo link đến URL đó?",
  "Collection `/orders` không có phần tử nên trả `200 []` hay `404`? Giải thích tài nguyên nào tồn tại.",
  "Tại sao `DELETE` có thể idempotent dù lần đầu trả `204` và lần sau trả `404`?",
  "Khi nào dòng hàng nên là `/order-items/{id}`, khi nào nên nằm dưới `/orders/{id}/items/{item_id}`?",
  "Hãy tìm lỗ hổng IDOR trong một handler chỉ gọi `repository.get(order_id)` rồi trả kết quả.",
  "Thiết kế cách hủy đơn cần lưu lý do, người duyệt và thời điểm. Bạn chọn `PATCH`, `DELETE` hay tạo cancellation resource, vì sao?",
  "Một proxy cache response đơn hàng chỉ theo path. Vì sao bearer token trong request chưa đủ ngăn rò dữ liệu?"
],

tai_nguyen: [
  "Sách **RESTful Web APIs** — Leonard Richardson, Mike Amundsen và Sam Ruby.",
  "Tài liệu **HTTP Semantics** của IETF, phần method properties và status codes.",
  "Tài liệu chính thức **FastAPI**, các mục Path Operation, Response Model và Security.",
  "Sách **API Design Patterns** — JJ Geewax."
],

lien_he: [
  "Nối sang mảng **Cơ sở dữ liệu**: ranh giới aggregate của Order quyết định transaction nào phải nguyên tử và index nào phục vụ truy vấn.",
  "Nối sang mảng **Bảo mật web**: kiểm tra ownership ngăn IDOR; response model giảm mass assignment và rò trường nhạy cảm.",
  "Nối sang mảng **Hệ thống phân tán**: idempotency, retry và optimistic concurrency xuất hiện khi response có thể thất lạc hoặc nhiều worker cùng sửa.",
  "Nối sang mảng **Kiểm thử**: state machine và HTTP semantics trở thành contract test, property test và ca kiểm phân quyền."
]

};
