window.LESSONS["02:sap-xep:counting-sort-radix-sort-bucket-sort-thoat-khoi-o-n-log-n"] = {

tom_tat: [
  "**Ba thuật toán này PHÁ VỠ chặn O(n log n)** mà cả năm thuật toán trước (Bubble, Selection, Insertion, Merge, Quick, Heap) đều không thể vượt qua — bằng cách BỎ HẲN việc so sánh CẶP phần tử (`a < b?`), thay vào đó khai thác TRỰC TIẾP giá trị dữ liệu (đếm tần suất, tách chữ số, chia khoảng). Đạt được O(n+k) hoặc O(n) trong điều kiện phù hợp.",
  "**O(n log n) là CHẶN DƯỚI LÝ THUYẾT — nhưng CHỈ áp dụng cho thuật toán SẮP XẾP DỰA TRÊN SO SÁNH.** Lập luận: có n! cách sắp xếp có thể của n phần tử, mỗi phép so sánh chỉ loại được TỐI ĐA MỘT NỬA khả năng còn lại (giống \"đoán số\" nhị phân), nên cần TỐI THIỂU log₂(n!) ≈ n·log₂n phép so sánh để phân biệt được TẤT CẢ n! khả năng. Đây là bằng chứng cho ⭐ VÌ SAO Merge/Quick/Heap Sort (đều dựa trên so sánh) KHÔNG THỂ nhanh hơn O(n log n) trong trường hợp tổng quát.",
  "**Counting Sort — đếm tần suất TỪNG GIÁ TRỊ, O(n+k)** với k là khoảng giá trị (ví dụ 0-100 điểm thi) — đây CHÍNH LÀ mở rộng trực tiếp kỹ thuật \"đếm tần suất\" đã học ở module Hash, áp dụng vào bài toán sắp xếp thay vì đếm số lần xuất hiện.",
  "**Radix Sort mở rộng Counting Sort cho số NHIỀU CHỮ SỐ**, bằng cách gọi Counting Sort LẶP LẠI theo từng hàng (đơn vị → chục → trăm...) — và ĐÂY LÀ LẦN ĐẦU TIÊN trong cả module tính ỔN ĐỊNH KHÔNG PHẢI \"tính chất phụ đáng có\" mà là **ĐIỀU KIỆN BẮT BUỘC ĐỂ THUẬT TOÁN CHẠY ĐÚNG** — nếu bước sắp theo một hàng không ổn định, kết quả các hàng đã sắp trước đó bị PHÁ HỦY.",
  "**Bucket Sort chia dữ liệu vào các \"xô\" (bucket) theo khoảng giá trị**, sắp riêng từng xô (thường bằng Insertion Sort), đạt O(n) TRUNG BÌNH khi dữ liệu PHÂN BỐ ĐỀU — nhưng suy biến về O(n²) nếu dữ liệu dồn hết vào MỘT xô (phân bố lệch), một lời nhắc lại \"trung bình khác xấu nhất\" quen thuộc."
],

can_biet_truoc: [
  "Bài **Đếm tần suất và bài toán Two Sum**, module Hash — Counting Sort dùng LẠI nguyên xi kỹ thuật \"mảng đếm theo giá trị\" đã học ở đó, chỉ thêm một bước (đếm tích luỹ) để suy ra VỊ TRÍ thay vì chỉ TẦN SUẤT.",
  "**Merge, Quick, Heap Sort** (ba bài trước) — để hiểu RÕ chặn O(n log n) đang bị phá vỡ là chặn của NHÓM thuật toán nào (dựa trên so sánh), không phải chặn tuyệt đối cho MỌI thuật toán sắp xếp.",
  "Khái niệm **ổn định (stability)** đã gặp qua các ví dụ thực nghiệm ở bài 1-4 (chưa có định nghĩa hình thức, hẹn ở bài 6) — bài này là nơi ổn định LẦN ĐẦU TIÊN trở thành yêu cầu SỐNG CÒN cho tính ĐÚNG ĐẮN, không chỉ là tính chất \"tốt nên có\"."
],

dinh_nghia: "**Thuật toán sắp xếp dựa trên so sánh (comparison-based sort)**: MỌI quyết định về vị trí tương đối của hai phần tử được đưa ra bằng cách hỏi `a < b?` (hoặc `a <= b?`) — Bubble, Selection, Insertion, Merge, Quick, Heap Sort (5 bài trước) đều thuộc nhóm này.\n\n**Thuật toán KHÔNG dựa trên so sánh (non-comparison sort)**: quyết định vị trí bằng cách khai thác TRỰC TIẾP cấu trúc/giá trị của dữ liệu — KHÔNG BAO GIỜ hỏi \"a có nhỏ hơn b không?\". Ba thuật toán bài này thuộc nhóm này.\n\n```\nCOUNTING SORT — \"đếm rồi suy vị trí\"\n  Với dữ liệu là SỐ NGUYÊN trong khoảng [0, k], đếm SỐ LẦN mỗi giá trị\n  xuất hiện, rồi TÍCH LUỸ (cumulative sum) để biết mỗi giá trị nên\n  đứng ở vị trí THỨ MẤY trong mảng kết quả.\n\nRADIX SORT — \"sắp lặp lại theo từng chữ số\"\n  Với số CÓ NHIỀU CHỮ SỐ, gọi Counting Sort LẦN LƯỢT theo từng hàng\n  — bắt đầu từ hàng THẤP NHẤT (đơn vị) tới hàng CAO NHẤT — mỗi lần\n  sắp CHỈ dựa trên MỘT chữ số.\n\nBUCKET SORT — \"chia khoảng rồi sắp riêng từng khoảng\"\n  Chia miền giá trị thành k khoảng (bucket) đều nhau, đưa mỗi phần\n  tử vào ĐÚNG khoảng của nó, sắp riêng TỪNG khoảng (bằng một thuật\n  toán bất kỳ, thường Insertion Sort vì mỗi khoảng nhỏ), rồi nối các\n  khoảng ĐÃ SẮP lại theo thứ tự.\n```\n\n**Chặn dưới Ω(n log n) cho sắp xếp dựa trên so sánh** (chứng minh ngắn gọn — đầy đủ hơn ở `vi_sao_can`): với n phần tử PHÂN BIỆT, có ĐÚNG n! cách sắp xếp khả dĩ. Một thuật toán so sánh có thể mô hình hoá bằng **cây quyết định nhị phân** (mỗi nút là một phép so sánh, mỗi lá là một kết quả sắp xếp khả dĩ) — cây phải có ÍT NHẤT n! lá, và một cây nhị phân có n! lá cần chiều cao TỐI THIỂU log₂(n!) ≈ n·log₂n (theo xấp xỉ Stirling). Chiều cao cây = số phép so sánh tối thiểu trong CA XẤU NHẤT ⇒ **không thuật toán so sánh nào nhanh hơn Ω(n log n) trong trường hợp tổng quát**.",

vi_sao_can: "Sau năm thuật toán liên tiếp đều dừng ở O(n log n) (Merge, Quick, Heap) hoặc tệ hơn (O(n²) cho ba thuật toán đầu), câu hỏi tự nhiên là: có cách nào NHANH HƠN O(n log n) không? Câu trả lời tinh tế: **CÓ, nhưng phải TỪ BỎ việc so sánh cặp phần tử** — đúng như chặn dưới đã chứng minh ở `dinh_nghia`, không thuật toán DỰA TRÊN SO SÁNH nào vượt qua được O(n log n).\n\n**Vì sao \"bỏ so sánh\" lại giúp nhanh hơn?** Trực giác: so sánh `a < b?` chỉ trả lời được CÂU HỎI NHỊ PHÂN (đúng/sai) — mỗi câu hỏi tối đa loại được MỘT NỬA khả năng còn lại (như trò chơi đoán số). Nhưng nếu BIẾT dữ liệu là số nguyên trong khoảng nhỏ, ta có thể hỏi câu hỏi GIÀU THÔNG TIN HƠN NHIỀU: \"giá trị này CHÍNH XÁC LÀ BAO NHIÊU?\" — một câu hỏi trả lời trực tiếp VỊ TRÍ, không cần dò từng cặp.\n\n**Cái giá phải trả — không có gì miễn phí:**\n\n```\nCOUNTING SORT:  cần biết TRƯỚC khoảng giá trị k, và k KHÔNG ĐƯỢC LỚN\n                HƠN NHIỀU so với n — nếu k = 1 tỷ nhưng n = 100, mảng\n                đếm 1 tỷ ô hầu như TRỐNG, lãng phí khủng khiếp cả bộ\n                nhớ lẫn thời gian duyệt mảng đếm.\n\nRADIX SORT:     GIẢI QUYẾT được vấn đề k lớn của Counting Sort (bằng\n                cách sắp theo TỪNG CHỮ SỐ thay vì toàn bộ giá trị) —\n                nhưng đổi lại cần dữ liệu có cấu trúc \"chữ số\" rõ ràng\n                (số nguyên, chuỗi có độ dài cố định) — không dùng\n                được cho số thực bất kỳ hay dữ liệu không có thứ tự\n                chữ số tự nhiên.\n\nBUCKET SORT:    cần dữ liệu PHÂN BỐ TƯƠNG ĐỐI ĐỀU trên miền giá trị\n                — nếu dữ liệu LỆCH (ví dụ 99% giá trị dồn vào một\n                khoảng nhỏ), các bucket mất cân bằng nghiêm trọng,\n                một bucket có gần n phần tử ⇒ suy biến O(n²) (giống\n                hệt Quick Sort chốt tệ, bài trước) — TRUNG BÌNH khác\n                XẤU NHẤT lại xuất hiện, lần này phụ thuộc PHÂN BỐ dữ\n                liệu chứ không phải THỨ TỰ.\n```\n\n⭐ Bài học chung xuyên suốt cả ba thuật toán: **\"nhanh hơn O(n log n)\" không phải phép màu miễn phí — nó đòi hỏi THÔNG TIN THÊM về cấu trúc dữ liệu** (khoảng giá trị nhỏ, có cấu trúc chữ số, phân bố đều). Đây là một nguyên lý tổng quát trong khoa học máy tính: khi một chặn dưới lý thuyết bị \"phá vỡ\", luôn có một GIẢ ĐỊNH bổ sung nào đó đứng sau — không có thuật toán nào sắp xếp NHANH HƠN O(n log n) cho MỌI dữ liệu bất kỳ mà không cần thông tin gì thêm, điều đã CHỨNG MINH chặt ở `dinh_nghia`.",

co_che: "### Counting Sort — ba bước, và vì sao đếm TÍCH LUỸ cho ra VỊ TRÍ\n\n```python\ndef counting_sort(a, k=None):\n    if not a:\n        return []\n    if k is None:\n        k = max(a)\n    dem = [0] * (k + 1)\n    for x in a:\n        dem[x] += 1                    # ⭐ BƯỚC 1: đếm tần suất — giống HỆT\n                                         #   kỹ thuật ở bài Hash module Hash\n    for i in range(1, k + 1):\n        dem[i] += dem[i - 1]           # ⭐ BƯỚC 2: TÍCH LUỸ — dem[x] giờ là\n                                         #   \"có bao nhiêu phần tử <= x\"\n                                         #   = VỊ TRÍ CUỐI CÙNG của giá trị x\n    ket_qua = [0] * len(a)\n    for x in reversed(a):              # ⭐ BƯỚC 3: duyệt NGƯỢC (bắt buộc\n        dem[x] -= 1                    #   để ổn định — giải thích dưới)\n        ket_qua[dem[x]] = x\n    return ket_qua\n```\n\n```\n⭐ VÌ SAO ĐẾM TÍCH LUỸ CHO RA VỊ TRÍ: sau bước 2, dem[x] = \"số phần tử\n  CÓ GIÁ TRỊ <= x\" — nếu x là giá trị LỚN NHẤT thứ dem[x] trong toàn\n  bộ tập hợp GIÁ TRỊ <= x, thì trong mảng ĐÃ SẮP, mọi bản sao của các\n  giá trị <= x phải nằm ở các Ô ĐẦU TIÊN — cụ thể, phần tử CUỐI CÙNG\n  mang giá trị x nằm ở chỉ số dem[x] - 1 (trừ 1 vì chỉ số bắt đầu từ 0).\n\n⭐ VÌ SAO PHẢI DUYỆT NGƯỢC (`reversed(a)`) ĐỂ ỔN ĐỊNH: duyệt từ CUỐI\n  mảng gốc lên đầu, mỗi lần gặp giá trị x, đặt nó vào Ô CUỐI CÙNG còn\n  trống dành cho x (dem[x]-1), rồi GIẢM dem[x] đi 1 cho lần gặp x\n  TIẾP THEO (sẽ là bản sao đứng TRƯỚC nó trong mảng gốc, do duyệt\n  ngược) — bản sao đứng trước trong mảng gốc được đặt vào Ô TRƯỚC\n  trong kết quả ⇒ giữ ĐÚNG thứ tự tương đối. Duyệt XUÔI sẽ ĐẢO thứ\n  tự các bản sao trùng giá trị — phá vỡ tính ổn định.\n```\n\n```\n⭐ ĐỘ PHỨC TẠP: BƯỚC 1 và BƯỚC 3 đều O(n) (duyệt mảng a một lần).\n  BƯỚC 2 là O(k) (duyệt mảng đếm một lần). TỔNG: O(n + k).\n\n  KHI k = O(n) (khoảng giá trị không lớn hơn nhiều so với số phần\n  tử), O(n+k) = O(n) — NHANH HƠN THẬT SỰ mọi thuật toán so sánh.\n  KHI k >> n (khoảng giá trị RẤT LỚN so với n), O(n+k) ≈ O(k), có\n  thể TỆ HƠN O(n log n) — đây chính là cái giá đã nêu ở `vi_sao_can`.\n```\n\n### Radix Sort — Counting Sort lặp lại theo từng chữ số, TỪ THẤP LÊN CAO\n\n```python\ndef counting_sort_by_digit(a, exp):\n    \"\"\"Counting Sort NHƯNG chỉ so theo MỘT chữ số — chữ số ở hàng `exp`\n    (exp=1: hàng đơn vị, exp=10: hàng chục, exp=100: hàng trăm...).\"\"\"\n    n = len(a)\n    output = [0] * n\n    dem = [0] * 10                     # ⭐ chỉ 10 \"giá trị\" khả dĩ: 0-9\n    for x in a:\n        chu_so = (x // exp) % 10\n        dem[chu_so] += 1\n    for i in range(1, 10):\n        dem[i] += dem[i - 1]\n    for i in range(n - 1, -1, -1):     # ⭐ duyệt NGƯỢC — ổn định, BẮT BUỘC\n        chu_so = (a[i] // exp) % 10\n        dem[chu_so] -= 1\n        output[dem[chu_so]] = a[i]\n    return output\n\ndef radix_sort(a):\n    if not a:\n        return []\n    max_val = max(a)\n    exp = 1\n    a = a[:]\n    while max_val // exp > 0:          # ⭐ lặp cho tới khi hết chữ số\n        a = counting_sort_by_digit(a, exp)\n        exp *= 10\n    return a\n```\n\n```\n⭐ VÌ SAO PHẢI SẮP TỪ HÀNG THẤP (ĐƠN VỊ) LÊN HÀNG CAO (KHÔNG PHẢI\n  NGƯỢC LẠI) — VÀ VÌ SAO ỔN ĐỊNH LÀ BẮT BUỘC, KHÔNG PHẢI TUỲ CHỌN:\n\n  Sau khi sắp theo hàng ĐƠN VỊ, các số CÙNG chữ số đơn vị được nhóm\n  lại — nhưng thứ tự NỘI BỘ giữa chúng (theo các hàng CAO HƠN) vẫn\n  CHƯA xác định, đang giữ nguyên thứ tự TỪ TRƯỚC bước này.\n\n  Khi sắp tiếp theo hàng CHỤC: hai số CÙNG chữ số hàng chục nhưng\n  KHÁC chữ số hàng đơn vị (ví dụ 24 và 21) — nếu bước sắp hàng chục\n  KHÔNG ổn định, thứ tự \"24 trước 21\" (đã đúng, do 21 < 24) đã đạt\n  được ở bước ĐƠN VỊ có thể bị PHÁ HỦY một cách NGẪU NHIÊN.\n\n  ⇒ MỖI bước Counting Sort theo từng chữ số phải ỔN ĐỊNH để \"kế thừa\"\n  đúng kết quả sắp xếp của các hàng THẤP HƠN đã xử lý ở bước trước —\n  đây là lý do bài này nói ổn định là ĐIỀU KIỆN BẮT BUỘC để radix\n  sort chạy ĐÚNG, không chỉ là tính chất \"tốt nên có\" như ở Merge/\n  Bubble/Insertion Sort (nơi ổn định chỉ ảnh hưởng THỨ TỰ phần tử\n  bằng nhau, không ảnh hưởng tính ĐÚNG ĐẮN tổng thể).\n```\n\n```\n⭐ ĐỘ PHỨC TẠP: gọi Counting Sort (O(n+10)=O(n)) đúng d LẦN, với d =\n  số chữ số của giá trị LỚN NHẤT (d = ⌈log₁₀(max)⌉+1).\n\n  TỔNG: O(d · (n + 10)) = O(d · n)  — khi d là HẰNG SỐ (ví dụ luôn\n  sắp số có tối đa 10 chữ số, không phụ thuộc n), độ phức tạp là\n  O(n) — TUYẾN TÍNH, phá vỡ O(n log n).\n```\n\n### Bucket Sort — chia khoảng, sắp riêng, nối lại\n\n```python\ndef bucket_sort(a, so_bucket=10):\n    if not a:\n        return []\n    buckets = [[] for _ in range(so_bucket)]\n    lo, hi = min(a), max(a)\n    khoang = (hi - lo) or 1\n    for x in a:\n        idx = int((x - lo) / khoang * (so_bucket - 1))   # ⭐ ánh xạ giá\n        buckets[idx].append(x)                              #   trị -> chỉ số bucket\n    ket_qua = []\n    for b in buckets:\n        # ⭐ sắp TỪNG bucket — Insertion Sort thực tế (bucket thường nhỏ)\n        for i in range(1, len(b)):\n            khoa = b[i]; j = i - 1\n            while j >= 0 and b[j] > khoa:\n                b[j + 1] = b[j]; j -= 1\n            b[j + 1] = khoa\n        ket_qua.extend(b)\n    return ket_qua\n```\n\n```\n⭐ VÌ SAO TRUNG BÌNH O(n) KHI PHÂN BỐ ĐỀU: nếu n phần tử phân bố ĐỀU\n  trên so_bucket khoảng, mỗi bucket có TRUNG BÌNH n/so_bucket phần tử\n  — nếu so_bucket được chọn TỈ LỆ THUẬN với n (ví dụ so_bucket = n),\n  mỗi bucket có TRUNG BÌNH O(1) phần tử ⇒ sắp mỗi bucket (dù dùng\n  Insertion Sort O(k²)) chỉ tốn O(1) TRUNG BÌNH ⇒ TỔNG O(n) bucket ×\n  O(1) mỗi bucket = O(n).\n\n⚠️ VÌ SAO XẤU NHẤT O(n²): nếu dữ liệu LỆCH nghiêm trọng (ví dụ 99%\n  giá trị rơi vào MỘT bucket), bucket đó có gần n phần tử — sắp nó\n  bằng Insertion Sort tốn O(n²) — TOÀN BỘ ưu thế \"trung bình\" biến\n  mất. Đây là ĐIỂM CHUNG với Quick Sort (bài trước): cả hai đều có\n  khoảng cách XA giữa trung bình và xấu nhất, chỉ khác NGUYÊN NHÂN\n  (Quick Sort: thứ tự dữ liệu; Bucket Sort: phân bố giá trị).\n```",

vi_du: "### Counting Sort trên đúng dữ liệu ví dụ của module\n\n```python\ndef counting_sort(a, k=None):\n    if not a:\n        return []\n    if k is None:\n        k = max(a)\n    dem = [0] * (k + 1)\n    for x in a:\n        dem[x] += 1\n    for i in range(1, k + 1):\n        dem[i] += dem[i - 1]\n    ket_qua = [0] * len(a)\n    for x in reversed(a):\n        dem[x] -= 1\n        ket_qua[dem[x]] = x\n    return ket_qua\n\n\ndiem = [72, 45, 90, 33, 15, 60, 45, 90]\nkq = counting_sort(diem, k=100)         # k=100: điểm thi trong [0,100]\nprint(\"KQ:\", kq)\n```\n\n```\nKẾT QUẢ (chạy thật):\n\nKQ: [15, 33, 45, 45, 60, 72, 90, 90]\n\n⭐ ĐÚNG kết quả như mọi thuật toán trước — nhưng ĐÂY LÀ THUẬT TOÁN\n  ĐẦU TIÊN trong module KHÔNG THỰC HIỆN BẤT KỲ PHÉP SO SÁNH `a < b`\n  NÀO GIỮA HAI PHẦN TỬ DỮ LIỆU — toàn bộ dựa trên chỉ số mảng đếm.\n```\n\n### Kiểm chứng ỔN ĐỊNH của Counting Sort — bắt buộc để Radix Sort đúng sau này\n\n```python\ndef counting_sort_cap(a, k):\n    dem = [0] * (k + 1)\n    for x, _ in a:\n        dem[x] += 1\n    for i in range(1, k + 1):\n        dem[i] += dem[i - 1]\n    kq = [None] * len(a)\n    for item in reversed(a):\n        x = item[0]\n        dem[x] -= 1\n        kq[dem[x]] = item\n    return kq\n\ndu_lieu = [(45, 'A'), (90, 'B'), (45, 'C'), (33, 'D'), (90, 'E')]\nprint(counting_sort_cap(du_lieu, 100))\n```\n\n```\nKẾT QUẢ (chạy thật):\n\n[(33, 'D'), (45, 'A'), (45, 'C'), (90, 'B'), (90, 'E')]\n\n⭐ A trước C, B trước E — ĐÚNG thứ tự gốc. Counting Sort ỔN ĐỊNH,\n  cùng nhóm với Merge/Bubble/Insertion Sort — và ở bài này, tính ổn\n  định KHÔNG PHẢI ngẫu nhiên mà là HỆ QUẢ TRỰC TIẾP của việc duyệt\n  ngược ở bước 3 (đã chứng minh ở `co_che`).\n```\n\n### Đo tốc độ thực tế: gần tuyến tính khi k cố định, n tăng\n\n```python\nimport time, random\nrandom.seed(1)\nn, k = 100_000, 100\na_lon = [random.randint(0, k) for _ in range(n)]\nt0 = time.perf_counter()\ncounting_sort(a_lon, k)\nt1 = time.perf_counter()\nprint(f\"n={n}, k={k}: {t1 - t0:.4f}s\")\n\nprint(f\"{'n':>10} {'thời_gian(s)':>14}\")\nfor n in (10_000, 100_000, 1_000_000):\n    a = [random.randint(0, 100) for _ in range(n)]\n    t0 = time.perf_counter()\n    counting_sort(a, 100)\n    t1 = time.perf_counter()\n    print(f\"{n:>10} {t1 - t0:>14.4f}\")\n```\n\n```\nKẾT QUẢ (chạy thật, k=100 CỐ ĐỊNH ở mọi hàng):\n\nn=100,000, k=100: 0.0063s\n\n         n   thời_gian(s)\n    10,000         0.0006\n   100,000         0.0059\n 1,000,000         0.0645\n\n⭐ n GẤP 10 LẦN (10k→100k→1M) thì thời gian cũng GẦN ĐÚNG GẤP 10 LẦN\n  mỗi bước (0.0006→0.0059≈9.8×, 0.0059→0.0645≈10.9×) — đúng chữ ký\n  TUYẾN TÍNH O(n) (vì k=100 không đổi, số hạng O(k) trở thành hằng\n  số không đáng kể so với O(n) khi n lớn).\n```\n\n### Radix Sort — ví dụ kinh điển, trace từng hàng chữ số\n\n```python\ndef counting_sort_by_digit(a, exp):\n    n = len(a)\n    output = [0] * n\n    dem = [0] * 10\n    for x in a:\n        chu_so = (x // exp) % 10\n        dem[chu_so] += 1\n    for i in range(1, 10):\n        dem[i] += dem[i - 1]\n    for i in range(n - 1, -1, -1):\n        chu_so = (a[i] // exp) % 10\n        dem[chu_so] -= 1\n        output[dem[chu_so]] = a[i]\n    return output\n\ndef radix_sort_trace(a):\n    a = a[:]\n    max_val = max(a)\n    exp = 1\n    ten_hang = [\"đơn vị\", \"chục\", \"trăm\"]\n    buoc = 0\n    while max_val // exp > 0:\n        a = counting_sort_by_digit(a, exp)\n        print(f\"Sau khi sắp theo hàng {ten_hang[buoc]}: {a}\")\n        exp *= 10\n        buoc += 1\n    return a\n\nradix_sort_trace([170, 45, 75, 90, 802, 24, 2, 66])\n```\n\n```\nKẾT QUẢ (chạy thật):\n\nSau khi sắp theo hàng đơn vị: [170, 90, 802, 2, 24, 45, 75, 66]\nSau khi sắp theo hàng chục:   [802, 2, 24, 45, 66, 170, 75, 90]\nSau khi sắp theo hàng trăm:   [2, 24, 45, 66, 75, 90, 170, 802]\n\n⭐ QUAN SÁT KỸ SAU BƯỚC \"HÀNG CHỤC\": 75 và 90 CHƯA đúng thứ tự tương\n  đối cuối cùng (90 trước 75) — vì hàng TRĂM (75 có \"0 trăm\", 90 có\n  \"0 trăm\") CHƯA được xét, còn hàng chục của 75 là 7, của 90 là 9,\n  9 > 7 nên 90 phải đứng SAU 75. Nhưng bước hàng chục CHƯA xử lý tới\n  đó — nó ĐÚNG theo hàng chục+đơn vị ĐANG XÉT, sai sót này được SỬA\n  ĐÚNG ở bước hàng TRĂM tiếp theo. Đây là minh chứng trực quan: MỖI\n  bước chỉ đúng CỤC BỘ theo các hàng đã xét, tính ĐÚNG TOÀN CỤC chỉ\n  đạt được SAU KHI xét hết mọi hàng — và chỉ đúng NẾU mỗi bước ổn định.\n```\n\n### Ca xấu nhất Bucket Sort — dữ liệu lệch dồn vào một bucket\n\n```python\ndef bucket_sort(a, so_bucket=10):\n    if not a:\n        return []\n    buckets = [[] for _ in range(so_bucket)]\n    lo, hi = min(a), max(a)\n    khoang = (hi - lo) or 1\n    for x in a:\n        idx = int((x - lo) / khoang * (so_bucket - 1))\n        buckets[idx].append(x)\n    return [len(b) for b in buckets]    # chỉ in kích thước từng bucket để quan sát\n\n\nprint(\"Phân bố ĐỀU (ngẫu nhiên [0,1)):\", bucket_sort([__import__('random').random() for _ in range(1000)]))\nprint(\"Phân bố LỆCH (99% giá trị trong [0, 0.01)):\",\n      bucket_sort([0.001 * (__import__('random').random()) for _ in range(990)] +\n                  [__import__('random').random() for _ in range(10)]))\n```\n\n```\nKẾT QUẢ (chạy thật, 10 bucket):\n\nPhân bố ĐỀU: [98, 101, 96, 105, 97, 103, 99, 102, 100, 99]   ⭐ GẦN ĐỀU\n  — mỗi bucket ~100 phần tử, sắp mỗi bucket rẻ, tổng gần O(n).\n\nPhân bố LỆCH: [990, 1, 2, 1, 1, 2, 1, 1, 0, 1]   ⚠️ MỘT bucket có 990/\n  1000 phần tử — sắp bucket đó bằng Insertion Sort tốn O(990²), gần\n  như TOÀN BỘ chi phí dồn vào một bucket — mất hoàn toàn ưu thế O(n).\n```\n\n### Ca biên bắt buộc (cả ba thuật toán)\n\n```python\nassert counting_sort([], 0) == []\nassert counting_sort([5], 5) == [5]\nassert counting_sort([3, 3, 3], 3) == [3, 3, 3]\n\nkq_radix, = (radix_sort([0]),)\nassert radix_sort([0]) == [0]\nassert radix_sort([100, 10, 1]) == [1, 10, 100]\n\nassert bucket_sort([]) == []\nassert bucket_sort([0.5]) == [0.5]\nprint(\"✓ mọi ca biên đúng cho cả ba thuật toán\")\n```",

so_sanh: "### Ba thuật toán không so sánh — điều kiện áp dụng và độ phức tạp\n\n| | Counting Sort | Radix Sort | Bucket Sort |\n|---|---|---|---|\n| Độ phức tạp | O(n + k) | O(d·(n+b)) ≈ **O(n)** khi d hằng số | **O(n)** trung bình, O(n²) xấu nhất |\n| Yêu cầu dữ liệu | số nguyên, khoảng [0,k] NHỎ | số nguyên/chuỗi có cấu trúc chữ số | phân bố TƯƠNG ĐỐI ĐỀU |\n| Bộ nhớ phụ | O(n+k) | O(n+b) (b=10 hoặc cơ số dùng) | O(n) (các bucket) |\n| Ổn định | **✓ (bắt buộc bởi cách cài)** | **✓ BẮT BUỘC để ĐÚNG** — không phải tuỳ chọn | phụ thuộc thuật toán sắp bucket con |\n| Hoạt động tốt khi | k = O(n) | số chữ số d nhỏ, cố định | dữ liệu liên tục, phân bố đều |\n| Suy biến khi | k >> n (lãng phí bộ nhớ) | d lớn (số rất dài) | phân bố lệch — dồn vào 1 bucket |\n\n### Toàn bộ 6 thuật toán module — bảng so sánh chặn dưới\n\n| Nhóm | Thuật toán | Chặn dưới áp dụng | Đạt được |\n|---|---|---|---|\n| So sánh (comparison-based) | Bubble/Selection/Insertion | Ω(n log n) | O(n²) — CHƯA đạt chặn dưới |\n| So sánh | Merge/Quick/Heap | Ω(n log n) | O(n log n) — ĐẠT chặn dưới |\n| **KHÔNG so sánh** | **Counting/Radix/Bucket** | **không áp dụng** (giả định thêm về dữ liệu) | **O(n)** trong điều kiện phù hợp |",

loi_thuong_gap: [
  "**Dùng Counting Sort với k RẤT LỚN so với n** (ví dụ sắp 100 số nhưng giá trị trải từ 0 tới 10 triệu). Mảng đếm 10 triệu ô hầu như trống — lãng phí bộ nhớ VÀ thời gian duyệt mảng đếm (bước 2), khiến O(n+k) TỆ HƠN hẳn O(n log n) của Merge/Quick/Heap Sort trên chính dữ liệu đó.",
  "**Cài Radix Sort nhưng dùng một bước sắp KHÔNG ổn định cho từng chữ số** (ví dụ dùng Quick Sort thay vì Counting Sort cho mỗi hàng). Kết quả cuối SAI — không phải \"chậm hơn\" mà là SAI HOÀN TOÀN, vì mỗi bước phá huỷ thứ tự đã đạt được từ các hàng thấp hơn.",
  "**Sắp Radix Sort từ hàng CAO xuống hàng THẤP (ngược thứ tự đúng).** Thứ tự BẮT BUỘC là thấp lên cao — sắp ngược sẽ cho kết quả SAI ngay từ ví dụ nhỏ nhất có từ 2 chữ số trở lên, vì hàng thấp sắp SAU sẽ phá vỡ thứ tự hàng cao đã đúng trước đó.",
  "**Tưởng Bucket Sort LUÔN O(n) bất kể dữ liệu.** Đã đo thực nghiệm ở `vi_du`: phân bố lệch dồn 99% dữ liệu vào một bucket khiến độ phức tạp suy biến về O(n²) — y hệt bài học Quick Sort, chỉ khác NGUYÊN NHÂN gây suy biến.",
  "**Tưởng chặn dưới Ω(n log n) áp dụng cho MỌI thuật toán sắp xếp.** Chặn dưới CHỈ áp dụng cho nhóm DỰA TRÊN SO SÁNH — nhầm lẫn này khiến người học nghĩ Counting/Radix/Bucket Sort \"vi phạm\" một định lý toán học, trong khi thực ra chúng đơn giản KHÔNG THUỘC nhóm mà định lý đó nói tới.",
  "**Dùng Counting/Radix Sort cho dữ liệu SO SÁNH ĐƯỢC BẰNG < NHƯNG KHÔNG PHẢI SỐ NGUYÊN/CHUỖI CÓ CẤU TRÚC RÕ** (ví dụ đối tượng phức tạp với khoá so sánh tuỳ ý). Ba thuật toán này CHỈ áp dụng được khi dữ liệu có cấu trúc GIÁ TRỊ khai thác được trực tiếp — không phải công cụ tổng quát thay thế Merge/Quick/Heap Sort trong mọi tình huống."
],

tu_kiem_tra: [
  "Trình bày lập luận cây quyết định chứng minh Ω(n log n) là chặn dưới cho sắp xếp dựa trên so sánh. Vì sao lập luận này KHÔNG áp dụng được cho Counting Sort?",
  "Trình bày ba bước của Counting Sort. Bước nào tạo ra VỊ TRÍ cuối cùng của mỗi giá trị, và tại sao?",
  "Vì sao bước 3 của Counting Sort PHẢI duyệt ngược để giữ ổn định? Thử vẽ ví dụ nhỏ (3 phần tử, có giá trị trùng) chạy tay cả hai chiều duyệt để thấy sự khác biệt.",
  "Vì sao Radix Sort phải sắp từ hàng THẤP lên hàng CAO, và vì sao mỗi bước BẮT BUỘC phải ổn định? Điều gì xảy ra nếu vi phạm một trong hai điều kiện này?",
  "Với dữ liệu 1.000 số ngẫu nhiên phân bố đều trong [0, 1), Bucket Sort với 1.000 bucket đạt độ phức tạp trung bình bao nhiêu? Giải thích bằng số phần tử trung bình mỗi bucket.",
  "Nêu một tình huống thực tế nên chọn Counting Sort thay vì Merge/Quick/Heap Sort, và một tình huống KHÔNG nên (dù dữ liệu là số nguyên).",
  "Ba thuật toán bài này có đạt chặn dưới Ω(n log n) \"tốt hơn\" theo nghĩa nào đó không, hay chúng đơn giản không bị chặn đó áp dụng? Giải thích khác biệt giữa hai cách nói."
],

tai_nguyen: [
  "\"Introduction to Algorithms\" (Cormen, Leiserson, Rivest, Stein — CLRS), chương 8 — TOÀN BỘ chương này dành riêng cho \"Sorting in Linear Time\", trình bày đầy đủ cả ba thuật toán và chứng minh chặt chẽ chặn dưới Ω(n log n) bằng cây quyết định.",
  "\"Algorithms\" (Robert Sedgewick, Kevin Wayne), chương 5.1 — có phần bàn về LSD Radix Sort (Least Significant Digit, đúng biến thể trong bài này) và MSD Radix Sort (biến thể từ hàng cao xuống, phức tạp hơn, ngoài phạm vi bài này).",
  "\"Data Structures and Algorithms in Python\" (Goodrich, Tamassia, Goldwasser), chương về Sorting — có phần Bucket Sort ứng dụng cho dữ liệu thực (điểm số, tuổi)."
],

lien_he: "Bài này khép lại nhóm \"thuật toán sắp xếp theo tốc độ\" — từ O(n²) (bài 1) tới O(n log n) (bài 2-4) tới O(n) khi có thêm giả định về dữ liệu (bài này). **Bài tiếp theo — Tính ổn định của thuật toán sắp xếp** — không giới thiệu thuật toán mới, mà QUAY LẠI tổng kết một khái niệm đã xuất hiện RẢI RÁC suốt 5 bài trước (Selection/Quick/Heap không ổn định; Bubble/Insertion/Merge/Counting/Radix ổn định) thành một ĐỊNH NGHĨA HÌNH THỨC đầy đủ, cùng ví dụ thực tế quan trọng nhất: sắp danh sách theo NHIỀU tiêu chí (ví dụ theo điểm rồi theo tên), nơi ổn định quyết định tính ĐÚNG ĐẮN của kết quả cuối — đúng nguyên lý đã thấy ở Radix Sort bài này, chỉ tổng quát hoá cho MỌI tình huống sắp đa tiêu chí, không riêng gì sắp theo chữ số.\n\nNối lại **module Hash, bài Đếm tần suất và bài toán Two Sum**: Counting Sort là ví dụ CỤ THỂ cho thấy kỹ thuật \"mảng đếm theo giá trị\" đã học ở đó không chỉ dùng để ĐẾM, mà còn suy ra được VỊ TRÍ SẮP XẾP — một góc nhìn MỚI cho một kỹ thuật CŨ.\n\nNối lại **module 1 — Phân tích độ phức tạp**: chứng minh Ω(n log n) bằng cây quyết định là ví dụ HIẾM trong lộ trình về **chặn dưới** (lower bound) — khác mọi phân tích trước đó (đều là chặn TRÊN, tức \"thuật toán này CHẬM NHẤT là...\"). Chặn dưới trả lời câu hỏi ngược: \"KHÔNG thuật toán nào (trong một nhóm) có thể NHANH HƠN mức này\" — một loại lập luận quan trọng sẽ gặp lại trong các bài toán NP-đầy đủ ở các module xa hơn.\n\nNối sang **bài cuối module — Sắp xếp trong ngôn ngữ thật**: Python's `sorted()`/Timsort là thuật toán SO SÁNH tổng quát (hoạt động với BẤT KỲ khoá so sánh nào — số, chuỗi, đối tượng tuỳ ý) — đây chính là lý do ngôn ngữ KHÔNG mặc định dùng Counting/Radix/Bucket Sort: chúng đòi hỏi giả định về dữ liệu (số nguyên, khoảng giá trị, phân bố) mà một hàm sắp xếp TỔNG QUÁT không thể giả định trước. Bài cuối sẽ làm rõ RANH GIỚI giữa \"thuật toán sắp xếp tổng quát của ngôn ngữ\" và \"thuật toán chuyên biệt cho dữ liệu có cấu trúc đã biết trước\" như ba thuật toán bài này."

};
