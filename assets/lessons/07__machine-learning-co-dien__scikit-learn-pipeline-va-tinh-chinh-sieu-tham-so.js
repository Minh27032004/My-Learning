window.LESSONS["07:machine-learning-co-dien:scikit-learn-pipeline-va-tinh-chinh-sieu-tham-so"] = {

tom_tat: [
  "scikit-learn Pipeline ghép các phép biến đổi và estimator thành một đối tượng có thể fit, predict và cross-validation mà không làm rò thống kê giữa các fold.",
  "ColumnTransformer cho phép xử lý cột số và danh mục bằng các nhánh khác nhau rồi ghép kết quả theo đúng schema.",
  "Tinh chỉnh siêu tham số là tìm cấu hình trên validation, không phải học trọng số mô hình; mọi lựa chọn lặp lại đều tiêu tốn “ngân sách” validation.",
  "GridSearchCV thử lưới đầy đủ, RandomizedSearchCV phân bổ số lượt cố định; lựa chọn phụ thuộc kích thước không gian và chi phí mỗi lần fit.",
  "Với gian lận, scorer, split và ngưỡng quyết định phải phản ánh lớp hiếm, thời gian và công suất cảnh báo; accuracy cùng KFold ngẫu nhiên thường trả lời sai bài toán."
],

can_biet_truoc: [
  "Biết train/validation/test, cross-validation và leakage từ preprocessing fit trên toàn bộ dữ liệu.",
  "Hiểu estimator scikit-learn có `fit`, transformer có `transform`, classifier có `predict` hoặc `predict_proba`.",
  "Biết đặc trưng số cần imputation/scaling còn danh mục thường cần one-hot encoding.",
  "Hiểu precision, recall, average precision và confusion matrix trong dữ liệu mất cân bằng."
],

dinh_nghia: "**Pipeline** trong scikit-learn là một estimator tổng hợp chạy tuần tự các bước biến đổi rồi estimator cuối: khi `fit`, mỗi transformer học trên kết quả của bước trước và chuyển tiếp dữ liệu; khi `predict`, nó chỉ transform bằng state đã học rồi gọi mô hình cuối. Nhờ có cùng giao diện estimator, toàn chuỗi được clone riêng cho từng fold và có thể tinh chỉnh bằng tên tham số dạng `tên_bước__tên_tham_số`.\n\n**Tinh chỉnh siêu tham số (hyperparameter tuning)** là quá trình so sánh các cấu hình được đặt trước khi fit, chẳng hạn độ mạnh regularization `C`, số cây hoặc cách impute. Nó khác tham số mô hình như hệ số logistic: hệ số được optimizer học trong một lần fit, còn siêu tham số được quy trình tìm kiếm chọn từ hiệu năng validation.",

vi_sao_can: "Một notebook thường chạy `fillna`, `StandardScaler.fit_transform` và one-hot trên cả DataFrame rồi mới gọi cross-validation. Các fold validation khi ấy đã góp phần tạo median, trung bình, độ lệch chuẩn và vocabulary. Mô hình cuối không đọc nhãn validation trực tiếp, nhưng quy trình vẫn biết phân phối của phần đáng lẽ chưa thấy. Pipeline đặt ranh giới đúng: mỗi lần cross-validation fit một bản sao từ đầu chỉ trên fold train.\n\nLý do thứ hai là khả năng tái hiện. Nếu production nhận một object chứa cả imputer, encoder và classifier, thứ tự cột cùng state đi chung với mô hình. Nếu chỉ lưu classifier, dịch vụ dễ dùng median mới, quên log transform hoặc sắp one-hot khác thứ tự. Pipeline không giải quyết mọi train–serving skew, đặc biệt đặc trưng lịch sử ngoài process, nhưng nó thu nhỏ đáng kể bề mặt lỗi.\n\nTinh chỉnh cần thiết vì default là điểm khởi đầu chung, không biết tỷ lệ gian lận, chi phí cảnh báo hay số mẫu của dự án. Tuy nhiên tìm kiếm càng nhiều cấu hình càng có nguy cơ “học thuộc validation”. Quy trình đúng vừa tự động hóa thử nghiệm, vừa giữ test đóng kín để ước lượng khách quan sau cùng.",

co_che: "### Pipeline thực hiện fit theo dây chuyền\n\nVới ba bước `imputer → scaler → logistic`, `fit(X, y)` gọi `imputer.fit_transform(X, y)`, dùng kết quả gọi `scaler.fit_transform`, rồi gọi `logistic.fit`. State gồm median, trung bình/độ lệch chuẩn và hệ số classifier nằm trong các bước đã fit. `predict(X_new)` không fit lại; nó gọi `imputer.transform`, `scaler.transform`, rồi `logistic.predict`.\n\nQuy tắc quan trọng là mọi bước trừ bước cuối phải có `transform`; bước cuối chỉ cần `fit` và phương thức phục vụ như `predict`. Pipeline không biến một hàm tùy ý thành transformer nếu hàm không tuân contract. `FunctionTransformer` bọc phép biến đổi stateless; transformer có state nên kế thừa BaseEstimator/TransformerMixin hoặc dùng thành phần chuẩn để clone được.\n\n### Clone là lý do cross-validation sạch\n\nCross-validation không tái dùng object đã fit qua các fold. scikit-learn clone estimator từ cấu hình constructor, tạo state rỗng, fit trên chỉ số train rồi score trên chỉ số validation. Sang fold kế tiếp lại có clone mới. Vì scaler nằm trong Pipeline, mean của fold 1 không chảy sang fold 2.\n\nNếu ta preprocessing bên ngoài rồi truyền ma trận đã biến đổi vào `cross_val_score`, clone chỉ nhìn thấy classifier; state của preprocessing đã học từ toàn dataset và không thể được hoàn tác. Đây là lý do “tôi có dùng CV” chưa đủ. Ranh giới estimator phải bao trọn mọi thao tác học từ dữ liệu: imputation, scaling, vocabulary, feature selection, PCA và target encoding.\n\n### ColumnTransformer giữ nhiều kiểu cột trong một graph\n\nDữ liệu giao dịch có số tiền, tỷ lệ lịch sử và merchant category. Một scaler không thể nhận chuỗi; one-hot không nên áp vào số liên tục. ColumnTransformer chạy các nhánh theo tập cột rồi nối ma trận kết quả. Nhánh số có imputer và scaler; nhánh danh mục có imputer và OneHotEncoder.\n\n`remainder=\"drop\"` bỏ cột chưa liệt kê, giúp schema rõ nhưng có thể làm mất cột mới trong im lặng. `remainder=\"passthrough\"` giữ chúng, tiện lúc khám phá nhưng nguy hiểm nếu ID hoặc nhãn phụ lọt vào. Production nên kiểm tra tên, kiểu, miền giá trị trước Pipeline. Tên cột ổn định tốt hơn selector theo vị trí vì thứ tự CSV có thể đổi.\n\n### Tên tham số hai dấu gạch dưới\n\n`clf__C` nghĩa là tham số `C` của bước `clf`; `preprocess__num__impute__strategy` đi xuyên qua nhiều estimator lồng nhau. `get_params().keys()` cho biết tên hợp lệ, hữu ích hơn đoán. Đặt sai tên làm GridSearchCV báo lỗi ngay, tốt hơn việc âm thầm bỏ cấu hình.\n\nTa có thể thay cả bước bằng object khác, chẳng hạn `{\"clf\": [LogisticRegression(), RandomForestClassifier()]}`, nhưng các tham số riêng của model tạo lưới dạng danh sách dictionary. Không đưa `clf__C` vào nhánh random forest vì estimator đó không có `C`.\n\n### GridSearchCV thực sự tính bao nhiêu lần fit\n\nNếu lưới có 4 giá trị `C`, 2 class weight và 5 fold, tìm kiếm thực hiện 4 × 2 × 5 = 40 lần fit để chấm. Khi `refit=True`, cấu hình thắng được fit thêm một lần trên toàn bộ dữ liệu đưa vào search, tổng 41. Nếu mỗi fit mất 3 phút, lưới nhỏ này đã dùng hơn hai giờ CPU tuần tự.\n\nSố tổ hợp nhân rất nhanh. Năm tham số, mỗi cái 5 giá trị tạo 5⁵ = 3.125 cấu hình; với 5 fold là 15.625 fit. Vì vậy lưới không phải biểu tượng của tính kỹ lưỡng. Bắt đầu bằng không gian có lý do, đo thời gian một fit, ước tính ngân sách rồi mới chạy.\n\n### RandomizedSearchCV dùng ngân sách cố định\n\nRandomized search lấy `n_iter` cấu hình từ phân phối hoặc danh sách. Khi chỉ vài siêu tham số thật sự quan trọng, nó thường khám phá được nhiều giá trị mỗi chiều hơn grid cùng ngân sách. Với tham số liên tục như `C`, phân phối log-uniform phù hợp hơn uniform tuyến tính vì khác biệt từ 0,001 đến 0,01 thường đáng kể tương tự từ 1 đến 10.\n\nRandom search không bảo đảm thử mọi tổ hợp và có biến thiên; đặt `random_state` để tái hiện. Sau vòng rộng có thể thu hẹp vùng và chạy vòng hai, nhưng mọi vòng đều nhìn cùng validation sẽ dần overfit. Ghi lại toàn bộ thử nghiệm và giữ một outer test hoặc nested CV nếu cần ước lượng không thiên lệch.\n\n### Scoring phải gọi đúng nhu cầu lớp hiếm\n\n`scoring=\"accuracy\"` sẽ ưu ái mô hình đoán hợp lệ. `average_precision` dùng điểm liên tục và tóm tắt đường precision–recall, thường hợp cho ranking gian lận. Nếu yêu cầu recall tối thiểu hoặc giới hạn cảnh báo, một scalar chuẩn chưa đủ; có thể dùng multi-metric scoring để xem `average_precision`, `roc_auc`, `recall` và `precision` cùng lúc, nhưng `refit` phải chỉ rõ metric chọn người thắng.\n\nPrecision/recall scorer mặc định chấm `predict` ở ngưỡng estimator, thường 0,5. Tuning classifier bằng recall tại 0,5 trộn chất lượng ranking với lựa chọn ngưỡng. Một chiến lược sạch là chọn mô hình bằng average precision qua CV, tạo dự đoán out-of-fold để chọn ngưỡng theo ràng buộc, rồi khóa cả hai trước test.\n\n### Splitter là một phần của giả thuyết\n\n`StratifiedKFold` giữ tỷ lệ lớp và phù hợp khi hàng gần độc lập. Nếu nhiều giao dịch cùng thẻ, `StratifiedGroupKFold` có thể giữ nhóm không giao nhau trong khi cố cân bằng nhãn. Nếu mục tiêu dự đoán tương lai, `TimeSeriesSplit` hoặc các cửa sổ thời gian tự thiết kế ngăn tương lai vào train.\n\nKhông shuffle chuỗi thời gian chỉ để fold đẹp. Với gian lận, nhãn có độ trễ; cần gap giữa train và validation nếu đặc trưng hoặc nhãn ở biên chưa trưởng thành. Split đúng thường làm điểm thấp hơn random CV, nhưng đó là thông tin có ích về production chứ không phải nhược điểm của công cụ.\n\n### Refit, best estimator và test đóng kín\n\nGridSearchCV lưu `cv_results_`, `best_params_`, `best_score_` và `best_estimator_` khi refit. `best_score_` là trung bình validation của cấu hình thắng, đã chịu selection bias vì thắng trong nhiều lần thử. Nó không thay thế test score.\n\nSau khi thiết kế ổn định, gọi search trên train+validation hoặc tập development, lấy best estimator đã refit, rồi đánh giá đúng một lần trên test. Nếu nhìn test và đổi lưới, test đã trở thành validation. Khi đó cần tập test mới hoặc thừa nhận con số chỉ là kết quả phát triển.\n\n### Nested cross-validation trả lời câu hỏi khó hơn\n\nNested CV có vòng trong chọn siêu tham số và vòng ngoài ước lượng hiệu năng của toàn quy trình chọn. Mỗi outer fold chạy một search chỉ trên phần train của nó, rồi chấm best estimator trên outer validation chưa tham gia lựa chọn. Nó tốn k_outer × n_configs × k_inner lần fit, nhưng hữu ích khi dữ liệu nhỏ và cần báo cáo khách quan.\n\nKhông cần nested CV cho mọi dự án lớn có holdout thời gian đáng tin. Nó cũng không cứu được split sai, drift hoặc feature tương lai. Hãy dùng khi chi phí tính toán chấp nhận được và câu hỏi là “quy trình tuning tổng quát thế nào”, không chỉ “cấu hình nào tốt trên development”.\n\n### Song song, cache và giới hạn tài nguyên\n\n`n_jobs=-1` dùng mọi lõi cho các fit song song, có thể làm máy hết RAM vì mỗi worker giữ bản sao dữ liệu và mô hình. Đặt số worker theo RAM, không chỉ CPU. Các thư viện số còn tự mở thread bên trong, tạo oversubscription: 8 process × 8 thread không nhất thiết nhanh.\n\n`Pipeline(memory=...)` cache kết quả transformer đã fit giữa các cấu hình khi bước trước không đổi, hữu ích với preprocessing đắt. Cache chiếm đĩa và hash dữ liệu; không đáng cho transformer rẻ. Thư mục cache là sản phẩm tạm, không đưa vào repo hay model artifact.\n\n### Đọc cv_results_ thay vì chỉ best score\n\nMột cấu hình trung bình cao hơn 0,001 nhưng độ lệch giữa fold lớn gấp ba có thể kém ổn định. Xem `mean_test_score`, `std_test_score`, `mean_fit_time`, rank và train score nếu bật. Khoảng cách train–validation lớn gợi ý overfit; fit time tăng mạnh có thể loại một cấu hình dù metric nhỉnh nhẹ.\n\nĐừng diễn giải standard deviation giữa 5 fold như confidence interval chính xác khi fold phụ thuộc hoặc dữ liệu theo thời gian. Nó là tín hiệu ổn định tương đối. So sánh theo từng fold và từng giai đoạn, đặc biệt tháng có chiến dịch tấn công khác nhau.\n\n### Pipeline không tự ngăn mọi leakage\n\nNếu DataFrame đã có cột `chargeback_after_30d`, Pipeline sẽ ngoan ngoãn học nó. Nếu hàm tạo rolling feature đã nhìn tương lai trước khi vào Pipeline, clone không sửa được. Nếu cùng khách nằm hai fold, preprocessing sạch nhưng split vẫn rò danh tính. Công cụ bảo đảm phạm vi fit của các bước bên trong, không hiểu ý nghĩa thời gian hay nghiệp vụ.\n\nCustom transformer có thể phá contract bằng biến global, cache dùng chung hoặc sửa X tại chỗ. Kiểm thử clone, fit hai lần trên dữ liệu khác và xác nhận state không chảy. Tránh gọi API mạng không xác định trong transform vì CV sẽ chậm, khó tái hiện và có thể nhận dữ liệu mới giữa các fold.\n\n### Ca biên trong tìm kiếm\n\nDanh mục validation chưa từng xuất hiện ở train sẽ làm OneHotEncoder lỗi nếu thiếu `handle_unknown=\"ignore\"`. Cột toàn missing trong một fold có thể bị imputer xử lý khác dự kiến. Một fold không có lớp dương khiến scorer undefined; splitter và số fold phải phù hợp số dương.\n\nMột số cấu hình không hội tụ. `error_score=\"raise\"` tốt khi phát triển để thấy stack trace; trong chạy lớn có thể dùng giá trị lỗi nhưng phải đọc warnings và `cv_results_`, không để cấu hình hỏng thắng hoặc bị bỏ âm thầm. Tăng `max_iter` không đảm bảo chữa dữ liệu scaling tệ hay solver không phù hợp.\n\n### Khi nào không nên tìm kiếm lớn\n\nKhông tuning khi baseline, split và metric chưa đúng; kết quả chỉ tối ưu sai lầm. Không quét hàng nghìn cấu hình để lấy cải thiện nhỏ hơn nhiễu fold. Không dùng GridSearch cho tham số liên tục trên miền nhiều bậc độ lớn. Và không tuning ngưỡng, feature, model lặp đi lặp lại trên test. Thường một baseline logistic rõ ràng, vài giá trị `C` theo thang log và phân tích lỗi đem lại hiểu biết nhiều hơn một cuộc tìm kiếm mù.",

vi_du: "### Pipeline hoàn chỉnh cho dữ liệu giao dịch\n\nCode tạo dữ liệu hỗn hợp, dùng ColumnTransformer và RandomizedSearchCV. `merchant` được tạo từ đặc trưng số để ví dụ có danh mục; mọi random seed được khóa. Kết quả số cần được lấy bằng cách chạy trong môi trường của người học, không được suy đoán từ bài viết.\n\n```python\nimport numpy as np\nimport pandas as pd\nfrom scipy.stats import loguniform\nfrom sklearn.compose import ColumnTransformer\nfrom sklearn.datasets import make_classification\nfrom sklearn.impute import SimpleImputer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.metrics import average_precision_score, confusion_matrix\nfrom sklearn.model_selection import RandomizedSearchCV, train_test_split\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.preprocessing import OneHotEncoder, RobustScaler\n\nX_num, y = make_classification(\n    n_samples=12_000, n_features=5, n_informative=4,\n    n_redundant=0, weights=[0.99, 0.01],\n    class_sep=1.0, random_state=42,\n)\nX = pd.DataFrame(X_num, columns=[\"amount\", \"velocity\", \"age_days\", \"distance\", \"risk\"])\nX[\"merchant\"] = pd.cut(X[\"risk\"], [-np.inf, -0.5, 0.5, np.inf],\n                       labels=[\"food\", \"retail\", \"travel\"]).astype(\"object\")\nX.loc[X.sample(frac=0.03, random_state=7).index, \"velocity\"] = np.nan\n\nX_dev, X_test, y_dev, y_test = train_test_split(\n    X, y, test_size=0.25, stratify=y, random_state=42\n)\nnumeric = [\"amount\", \"velocity\", \"age_days\", \"distance\", \"risk\"]\ncategorical = [\"merchant\"]\nprep = ColumnTransformer([\n    (\"num\", Pipeline([\n        (\"impute\", SimpleImputer(strategy=\"median\", add_indicator=True)),\n        (\"scale\", RobustScaler()),\n    ]), numeric),\n    (\"cat\", Pipeline([\n        (\"impute\", SimpleImputer(strategy=\"most_frequent\")),\n        (\"onehot\", OneHotEncoder(handle_unknown=\"ignore\")),\n    ]), categorical),\n])\npipe = Pipeline([\n    (\"prep\", prep),\n    (\"clf\", LogisticRegression(max_iter=2000)),\n])\nsearch = RandomizedSearchCV(\n    pipe,\n    param_distributions={\n        \"clf__C\": loguniform(1e-3, 1e2),\n        \"clf__class_weight\": [None, \"balanced\"],\n    },\n    n_iter=12, scoring=\"average_precision\", cv=5,\n    refit=True, random_state=42, n_jobs=2,\n    return_train_score=True,\n)\nsearch.fit(X_dev, y_dev)\ntest_score = search.predict_proba(X_test)[:, 1]\ntest_pred = (test_score >= 0.5).astype(int)\nprint(\"best params:\", search.best_params_)\nprint(\"CV average precision:\", search.best_score_)\nprint(\"test average precision:\", average_precision_score(y_test, test_score))\nprint(confusion_matrix(y_test, test_pred, labels=[0, 1]))\n```\n\nTest chỉ được mở sau khi code, không gian và scorer đã chốt. Trong ví dụ học tập, chạy lại tùy ý không làm thiệt hại thực; trong báo cáo dự án, mỗi lần nhìn test rồi sửa quyết định làm con số bớt khách quan.\n\n### Kiểm tra số lượt fit và độ ổn định\n\n```python\nresults = pd.DataFrame(search.cv_results_)\ncols = [\"rank_test_score\", \"mean_test_score\", \"std_test_score\",\n        \"mean_train_score\", \"mean_fit_time\", \"param_clf__C\",\n        \"param_clf__class_weight\"]\nprint(results.sort_values(\"rank_test_score\")[cols].head(8).to_string(index=False))\n\nexpected_cv_fits = 12 * 5\nprint(\"Số fit để chấm:\", expected_cv_fits,\n      \"; cộng một lần refit cấu hình thắng\")\n```\n\nNếu train score cao hơn validation nhiều, thử regularization mạnh hơn hoặc xem đặc trưng rò rỉ. Nếu `std_test_score` lớn, xem score từng split và cách chia theo thời gian/nhóm. Không chọn cấu hình chỉ vì rank 1 nếu chênh lệch nằm trong nhiễu còn latency tăng đáng kể.\n\n### Lưu và nạp đúng một artifact\n\n```python\nimport joblib\n\njoblib.dump(search.best_estimator_, \"fraud_pipeline.joblib\")\nloaded = joblib.load(\"fraud_pipeline.joblib\")\nnp.testing.assert_allclose(\n    loaded.predict_proba(X_test.iloc[:10]),\n    search.best_estimator_.predict_proba(X_test.iloc[:10]),\n)\n```\n\nChỉ nạp artifact từ nguồn tin cậy vì định dạng pickle/joblib có thể thực thi code. Artifact còn phụ thuộc phiên bản thư viện và schema đầu vào; lưu metadata môi trường, danh sách cột và metric cùng quy trình phát hành, nhưng không nhúng bí mật hay dữ liệu thật.",

so_sanh: "### Chọn chiến lược tìm kiếm theo ngân sách\n\n| Phương án | Cách khám phá | Ưu điểm | Nhược điểm | Hợp khi |\n|---|---|---|---|---|\n| Thử tay có ghi chép | Một cấu hình theo giả thuyết | Học nhanh, dễ phân tích lỗi | Dễ thiên kiến, khó phủ rộng | Baseline và giai đoạn hiểu dữ liệu |\n| GridSearchCV | Tích Descartes mọi giá trị | Tái hiện, phủ đủ lưới nhỏ | Bùng nổ tổ hợp, phí ở chiều ít quan trọng | 1–3 tham số rời rạc, miền hẹp |\n| RandomizedSearchCV | Lấy mẫu theo ngân sách | Phủ miền liên tục tốt hơn | Có thể bỏ vùng tốt | Nhiều tham số, budget cố định |\n| Successive halving | Loại sớm cấu hình yếu | Tiết kiệm tài nguyên | Cần resource có ý nghĩa, xếp hạng sớm có thể nhiễu | Fit đắt và có thể tăng dần mẫu/iteration |\n| Nested CV | Search trong từng outer fold | Ước lượng quy trình ít thiên lệch | Rất nhiều lần fit | Dữ liệu nhỏ, cần báo cáo nghiêm ngặt |\n\nPipeline so với preprocessing thủ công có thêm cấu trúc và đôi lúc khó debug tên cột, nhưng đổi lại giữ state cùng model và chống leakage theo fold. Không nên ép mọi bước vào Pipeline nếu đó là join lịch sử quy mô lớn cần hệ thống dữ liệu riêng; khi ấy phải tạo snapshot point-in-time trước, rồi Pipeline xử lý phần học thống kê trên snapshot.",

loi_thuong_gap: [
  "**Scale trước cross-validation.** Dấu hiệu là `fit_transform(X)` xuất hiện trước `GridSearchCV`; chuyển scaler vào Pipeline để mỗi clone học riêng.",
  "**Dùng accuracy cho gian lận.** Dấu hiệu là mô hình luôn âm thắng lưới; dùng average precision hoặc scorer gắn ràng buộc nghiệp vụ.",
  "**KFold ngẫu nhiên trên dữ liệu thời gian/khách hàng.** Dấu hiệu là score giảm mạnh khi chia theo tháng hoặc thẻ; chọn splitter phản ánh triển khai.",
  "**Lưới tổ hợp quá lớn.** Dấu hiệu là số fit hàng chục nghìn mà không có ước tính; tính tích cấu hình × fold và ưu tiên random search.",
  "**Tinh chỉnh trên test.** Dấu hiệu là thay lưới sau mỗi test score; khóa test và dùng validation/nested CV.",
  "**Chỉ nhìn `best_score_`.** Dấu hiệu là bỏ qua std, fit time và failure; đọc `cv_results_` theo fold.",
  "**Nghĩ Pipeline chữa mọi leakage.** Dấu hiệu là rolling feature đã dùng tương lai trước khi vào pipeline; kiểm toán nguồn và thời điểm từng cột.",
  "**Mở `n_jobs=-1` trên mô hình ngốn RAM.** Dấu hiệu là swap hoặc process bị kill; giới hạn worker và thread nội bộ."
],

tu_kiem_tra: [
  "Giải thích chính xác scaler học dữ liệu nào trong fold 3 khi nó nằm trong Pipeline, và khác gì nếu đã `fit_transform` trước CV.",
  "Lưới có 6 giá trị C, 3 class weight, 2 imputer và 5 fold cần bao nhiêu fit để chấm, rồi `refit=True` thêm bao nhiêu?",
  "Vì sao average precision thích hợp hơn accuracy cho gian lận, và tại sao vẫn chưa đủ để chọn ngưỡng production?",
  "Khi cùng khách có nhiều giao dịch, StratifiedKFold có thể làm score lạc quan bằng cơ chế nào?",
  "Thiết kế quy trình chọn model, chọn ngưỡng và mở test sao cho ngưỡng không học từ test.",
  "Một cấu hình rank 1 hơn rank 2 đúng 0,001 nhưng mất gấp mười thời gian và std lớn hơn. Bạn cần xem gì trước khi chọn?",
  "Pipeline đã đúng nhưng đặc trưng `count_24h` được tính trước trên toàn bảng. Vì sao CV vẫn rò và sửa ở tầng nào?"
],

tai_nguyen: [
  "Hands-On Machine Learning with Scikit-Learn, Keras, and TensorFlow — Aurélien Géron.",
  "Applied Predictive Modeling — Max Kuhn, Kjell Johnson.",
  "The Elements of Statistical Learning — Trevor Hastie, Robert Tibshirani, Jerome Friedman.",
  "scikit-learn User Guide — mục Pipelines and composite estimators, Tuning the hyper-parameters và Cross-validation."
],

lien_he: [
  "Nối sang Software Engineering qua composition và interface: mỗi transformer tuân contract fit/transform nên toàn chuỗi có thể kiểm thử, clone và thay thế.",
  "Nối sang Backend qua model artifact và schema validation; dịch vụ phải khóa thứ tự cột, kiểu dữ liệu, phiên bản thư viện và fallback.",
  "Nối sang System Design qua parallel search, RAM, cache và latency; cấu hình tốt nhất offline chưa chắc đáp ứng ngân sách tài nguyên online.",
  "Nối sang DevOps/MLOps qua reproducibility, model registry và rollback; pipeline, tham số, code và dữ liệu snapshot phải cùng version để tái hiện."
]

};
