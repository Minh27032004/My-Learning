window.LESSONS["04:sql:ham-tong-hop-va-group-by-having"] = {

tom_tat: [
  "Hàm tổng hợp biến nhiều dòng thành một giá trị; `GROUP BY` quyết định “mỗi giá trị ấy đại diện cho nhóm nào”.",
  "`WHERE` lọc dòng đầu vào trước khi gom, còn `HAVING` lọc nhóm sau khi đã tính `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`.",
  "Ngoại trừ `COUNT(*)`, các hàm tổng hợp thông dụng bỏ qua `NULL`; tập rỗng cho `SUM` là `NULL`, không phải 0.",
  "Gom theo đúng grain (độ hạt dữ liệu) quan trọng hơn cú pháp: JOIN một-nhiều sai chỗ có thể nhân doanh thu trước khi tổng hợp.",
  "PostgreSQL có thể dùng `HashAggregate`, `GroupAggregate` hoặc tổng hợp song song; index hữu ích khi giúp lọc hoặc cung cấp thứ tự, không bảo đảm phép gom sẽ nhanh."
],

can_biet_truoc: [
  "Cú pháp `SELECT`, `FROM`, `WHERE`, `ORDER BY` và ý nghĩa logic ba trạng thái của `NULL`.",
  "Khóa chính, khóa ngoại và quan hệ một-nhiều giữa `orders` với `order_items`.",
  "Các kiểu số PostgreSQL, đặc biệt `numeric` cho tiền và phép chia có thể cần ép kiểu.",
  "Khái niệm query plan cơ bản: sequential scan, index scan, sort và số dòng ước lượng."
],

dinh_nghia: "**Hàm tổng hợp** nhận một tập dòng và trả một giá trị tóm lược, chẳng hạn `COUNT(*)`, `SUM(total_amount)` hay `MAX(created_at)`. Không có `GROUP BY`, toàn bộ đầu vào là một nhóm. Có `GROUP BY`, mỗi tổ hợp giá trị khóa tạo một nhóm riêng và câu `SELECT` trả tối đa một dòng cho mỗi nhóm.\n\n```sql\nSELECT status,\n       COUNT(*) AS order_count,\n       SUM(total_amount) AS gross_amount\nFROM orders\nGROUP BY status;\n```\n\n`HAVING` là điều kiện trên nhóm đã hình thành. Nó không phải phiên bản “mạnh hơn” của `WHERE`: hai mệnh đề đứng ở hai thời điểm logic khác nhau và trả lời hai loại câu hỏi khác nhau.",

vi_sao_can: "Dữ liệu giao dịch lưu từng đơn để truy vết, nhưng người vận hành hỏi ở cấp cao hơn: hôm nay bán bao nhiêu vé, sự kiện nào vượt 80% sức chứa, khách nào có tổng chi lớn, ngày nào tỷ lệ hoàn tiền tăng. Đọc từng dòng trong ứng dụng vừa truyền nhiều dữ liệu vừa bỏ phí khả năng tính gần dữ liệu của database.\n\nNguy hiểm lớn nhất không nằm ở nhớ tên `SUM`; nó nằm ở chọn sai tập đầu vào. Nếu nối `orders` với hai `order_items`, giá trị cấp đơn xuất hiện hai lần. `SUM(orders.total_amount)` khi ấy phình lên mà SQL vẫn chạy không lỗi. Tương tự, `AVG` của trung bình từng nhóm thường không bằng trung bình toàn bộ vì các nhóm có kích thước khác nhau.\n\nHệ bán vé cần con số có thể đối soát. Bài này vì thế theo dõi grain ở mỗi bước: một dòng đơn hàng, một dòng mặt hàng, một dòng sự kiện-ngày. Khi grain rõ, `GROUP BY` trở thành phép chuyển mức chi tiết có kiểm soát, thay vì một câu lệnh dùng để “làm hết lỗi must appear in GROUP BY”.",

co_che: "### Bổ sung bảng chi tiết vé\n\n```sql\nCREATE TABLE order_items (\n  order_item_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  order_id bigint NOT NULL REFERENCES orders(order_id),\n  event_id bigint NOT NULL REFERENCES events(event_id),\n  quantity integer NOT NULL CHECK (quantity > 0),\n  unit_price numeric(12,2) NOT NULL CHECK (unit_price >= 0),\n  UNIQUE (order_id, event_id)\n);\n```\n\nGrain của `orders` là một dòng mỗi đơn. Grain của `order_items` là một dòng mỗi cặp đơn–sự kiện. Do giá vé tại thời điểm mua có thể khác `events.base_price`, doanh thu thật phải tính từ `quantity * unit_price` hoặc từ tổng đơn đã chốt theo quy ước kế toán, không lấy giá hiện tại của sự kiện.\n\n### Một nhóm ngầm và nhiều nhóm tường minh\n\n```sql\nSELECT COUNT(*), SUM(total_amount) FROM orders WHERE status = 'paid';\n```\n\nKhông có `GROUP BY`, tập paid là một nhóm, nên kể cả không có dòng đầu vào câu truy vấn vẫn trả **một dòng**: `COUNT(*) = 0`, `SUM(...) = NULL`. Ngược lại:\n\n```sql\nSELECT status, COUNT(*) FROM orders WHERE false GROUP BY status;\n```\n\nkhông trả dòng nào, vì không có giá trị `status` nào để tạo nhóm. Khác biệt này quan trọng khi API mong luôn có một tổng số. Dùng `COALESCE(SUM(total_amount), 0::numeric)` nếu nghiệp vụ định nghĩa tổng tập rỗng là 0.\n\n### Quy tắc cột trong SELECT\n\nSau khi gom theo `status`, database không thể chọn tùy ý một `created_at` đại diện cho cả nhóm. Vì vậy mỗi biểu thức trong `SELECT` phải là khóa nhóm, nằm trong hàm tổng hợp, hoặc được suy ra chức năng theo quy tắc PostgreSQL hỗ trợ.\n\n```sql\n-- Hợp lệ\nSELECT status, MAX(created_at) AS latest FROM orders GROUP BY status;\n\n-- Không có nghĩa xác định và bị từ chối\nSELECT status, created_at FROM orders GROUP BY status;\n```\n\nThêm cột bừa vào `GROUP BY` để hết lỗi có thể đổi câu hỏi: gom thêm `created_at` biến “mỗi trạng thái” thành “mỗi trạng thái-thời điểm”, thường gần như quay về từng đơn. Hãy quyết định grain kết quả trước rồi mới viết danh sách nhóm.\n\n### COUNT có ba khuôn mặt\n\n`COUNT(*)` đếm dòng, bất kể cột có `NULL`. `COUNT(customer_id)` chỉ đếm dòng nhận diện được khách. `COUNT(DISTINCT customer_id)` đếm số khách khác nhau và bỏ `NULL`.\n\n| Biểu thức | Đơn mua tại quầy `customer_id NULL` | Dòng trùng khách |\n|---|---:|---:|\n| `COUNT(*)` | có đếm | đếm mọi đơn |\n| `COUNT(customer_id)` | không đếm | đếm mọi đơn có khách |\n| `COUNT(DISTINCT customer_id)` | không đếm | mỗi khách một lần |\n\n`DISTINCT` có thể cần hash hoặc sort để khử trùng, nên không rắc vào chỉ để che JOIN sai. Nếu một JOIN nhân dòng ngoài ý muốn, sửa grain của JOIN mới là giải pháp.\n\n### NULL trong SUM, AVG, MIN và MAX\n\nCác hàm này bỏ qua giá trị `NULL`. `AVG(x)` về ý nghĩa là tổng các `x` không-null chia số `x` không-null, không phải chia tổng số dòng. Điều đó đôi khi đúng cho “điểm trung bình của người đã chấm”, nhưng sai nếu nghiệp vụ muốn người chưa chấm tính là 0.\n\n`COALESCE` phải đặt đúng phía: `AVG(COALESCE(score,0))` đưa dữ liệu thiếu thành 0 trước khi tính; `COALESCE(AVG(score),0)` chỉ thay kết quả khi cả nhóm không có điểm. Hai câu trả lời khác nhau. Chuyển `NULL` thành 0 là quyết định nghiệp vụ, không phải thói quen làm sạch.\n\n### WHERE đi trước, HAVING đi sau\n\n```sql\nSELECT customer_id, SUM(total_amount) AS paid_total\nFROM orders\nWHERE status = 'paid'\nGROUP BY customer_id\nHAVING SUM(total_amount) >= 1000000;\n```\n\n`WHERE` loại đơn pending/refunded trước khi xây nhóm. `HAVING` sau đó loại khách có tổng paid dưới ngưỡng. Điều kiện không phụ thuộc aggregate như `customer_id IS NOT NULL` nên đặt ở `WHERE`: giảm đầu vào sớm và diễn đạt đúng ý. PostgreSQL có thể tự đẩy một số điều kiện xuống, nhưng người viết vẫn nên phân biệt semantics.\n\nAlias `paid_total` không dùng trực tiếp trong `HAVING` của PostgreSQL; viết lại biểu thức hoặc bọc query. `ORDER BY paid_total` thì hợp lệ vì sắp xếp logic xảy ra sau danh sách kết quả.\n\n### FILTER cho nhiều thước đo trên một lượt gom\n\nPostgreSQL hỗ trợ `FILTER` trên từng aggregate:\n\n```sql\nSELECT customer_id,\n       COUNT(*) FILTER (WHERE status = 'paid') AS paid_orders,\n       COUNT(*) FILTER (WHERE status = 'refunded') AS refunds,\n       SUM(total_amount) FILTER (WHERE status = 'paid') AS paid_amount\nFROM orders\nGROUP BY customer_id;\n```\n\nKhác `WHERE status = 'paid'`, `FILTER` không loại dòng khỏi mọi chỉ số; mỗi aggregate có cửa lọc riêng. Cú pháp này thường rõ hơn `SUM(CASE WHEN ... THEN 1 ELSE 0 END)`, nhưng `CASE` dễ chuyển sang hệ quản trị không hỗ trợ `FILTER`.\n\n### Gom theo thời gian và múi giờ\n\n`date_trunc('day', created_at)` trên `timestamptz` dùng múi giờ phiên. Báo cáo ngày Việt Nam cần cố định:\n\n```sql\nSELECT date_trunc('day', created_at AT TIME ZONE 'Asia/Ho_Chi_Minh') AS local_day,\n       SUM(total_amount)\nFROM orders\nWHERE status = 'paid'\nGROUP BY local_day\nORDER BY local_day;\n```\n\nĐây là timestamp không múi giờ biểu diễn đồng hồ địa phương. Nếu nhiều tenant ở các vùng khác nhau, múi giờ phải là thuộc tính nghiệp vụ; hard-code một vùng cho tất cả sẽ phân loại sai giao dịch sát nửa đêm.\n\n### Bẫy nhân dòng sau JOIN\n\nGiả sử đơn 1 có hai loại vé. Query sau sai nếu `orders.total_amount` là tổng cấp đơn:\n\n```sql\nSELECT o.customer_id, SUM(o.total_amount)\nFROM orders o\nJOIN order_items i ON i.order_id = o.order_id\nWHERE o.status = 'paid'\nGROUP BY o.customer_id;\n```\n\nMỗi item lặp lại đơn một lần. Có hai cách đúng tùy câu hỏi: tổng cấp đơn thì không JOIN items; doanh thu chi tiết thì `SUM(i.quantity * i.unit_price)`. Nếu phải nối thêm bảng nhiều-nhiều, pre-aggregate mỗi nhánh về cùng grain trước khi nối. `SUM(DISTINCT o.total_amount)` không sửa tổng quát: hai đơn hợp lệ cùng số tiền sẽ bị gộp nhầm.\n\n### GROUP BY không bảo đảm thứ tự\n\nKết quả nhóm không tự sắp theo khóa. `HashAggregate` đặc biệt có thể phát nhóm theo vị trí hash. Muốn top khách, phải ghi:\n\n```sql\nORDER BY paid_total DESC, customer_id\nLIMIT 10\n```\n\nKhóa phụ tạo kết quả ổn định khi tổng bằng nhau. `LIMIT` sau tổng hợp không giảm số nhóm phải tính trong trường hợp tổng quát; executor thường phải gom hết rồi chọn top, trừ khi có cấu trúc hoặc phép biến đổi đặc biệt.\n\n### HashAggregate và GroupAggregate\n\n`HashAggregate` giữ bảng băm khóa nhóm; phù hợp khi số nhóm và state tổng hợp vừa `work_mem`. Nếu vượt bộ nhớ, nó có thể chia batch và dùng đĩa. `GroupAggregate` cần đầu vào đã sắp theo khóa, rồi cộng dồn từng nhóm; index hoặc bước sort có thể cung cấp thứ tự ấy. Tổng hợp song song cho phép worker tạo partial aggregate rồi tiến trình chính finalize.\n\nIndex `(status, customer_id)` có thể giúp lọc paid và cung cấp `customer_id`, nhưng vẫn phải đọc giá trị tiền, và index scan không luôn rẻ hơn sequential scan. Expression index theo ngày có thể hỗ trợ một workload cố định, song tạo thêm chi phí ghi. Hãy đọc `EXPLAIN (ANALYZE, BUFFERS)` và chú ý số nhóm thực tế, sort spill, hash batches cùng chênh lệch estimate.\n\n### Khi nào không nên tổng hợp trực tiếp mỗi lần\n\nBảng giao dịch rất lớn, dashboard hỏi cùng một tổng mỗi vài giây và dữ liệu cũ không đổi là dấu hiệu cân nhắc materialized view hay bảng summary. Đổi lại phải giải bài toán độ trễ làm mới, idempotency và đối soát. Không nên cache sớm một query chạy vài chục mili giây; bản sao tổng hợp làm hệ thống phức tạp và có thể hiển thị số cũ. Bài view sau sẽ tiếp tục đúng schema này.",

vi_du: "### Nạp chi tiết vé và đối chiếu\n\n```sql\nINSERT INTO order_items(order_id,event_id,quantity,unit_price) VALUES\n(1,1,2,450000),\n(2,2,1,700000),\n(3,1,1,450000),\n(4,1,1,450000),\n(5,1,2,450000);\n\nSELECT o.customer_id,\n       COUNT(*) AS paid_orders,\n       SUM(o.total_amount) AS paid_amount\nFROM orders o\nWHERE o.status = 'paid'\nGROUP BY o.customer_id\nORDER BY paid_amount DESC, o.customer_id NULLS LAST;\n```\n\nVới dữ liệu bài trước, khách 1 có một đơn paid trị giá 900000; khách 2 có 700000; nhóm `NULL` đại diện mua tại quầy có 450000. `NULL` vẫn tạo một nhóm trong `GROUP BY`, dù `COUNT(customer_id)` trong nhóm ấy bằng 0.\n\n### Một query sai có kết quả tưởng hợp lý\n\nThêm một item khác vào đơn 1 để mô phỏng đơn mua hai sự kiện:\n\n```sql\nINSERT INTO order_items(order_id,event_id,quantity,unit_price)\nVALUES (1,2,1,700000);\n\nSELECT SUM(o.total_amount) AS wrong_total\nFROM orders o\nJOIN order_items i USING (order_id)\nWHERE o.status = 'paid';\n\nSELECT SUM(o.total_amount) AS order_total\nFROM orders o\nWHERE o.status = 'paid';\n\nSELECT SUM(i.quantity * i.unit_price) AS item_total\nFROM orders o\nJOIN order_items i USING (order_id)\nWHERE o.status = 'paid';\n```\n\nBa con số trả lời ba phép tính khác nhau. Query đầu lặp `total_amount` của đơn 1 nên không phải chỉ số nghiệp vụ hợp lệ. Query hai tin tổng được lưu ở header; query ba tính từ dòng vé và sẽ phát hiện dữ liệu mẫu hiện không cân bằng nếu item thêm không được phản ánh vào header. Sai lệch ấy chính là tín hiệu đối soát, không nên che bằng `DISTINCT`.\n\n### Tập rỗng và phép chia an toàn\n\n```sql\nSELECT COUNT(*) AS n,\n       SUM(total_amount) AS raw_sum,\n       COALESCE(SUM(total_amount), 0::numeric) AS business_sum\nFROM orders\nWHERE status = 'paid' AND created_at >= '2100-01-01';\n\nSELECT customer_id,\n       COUNT(*) FILTER (WHERE status = 'refunded')::numeric\n       / NULLIF(COUNT(*) FILTER (WHERE status = 'paid'), 0) AS refund_per_paid\nFROM orders\nGROUP BY customer_id;\n```\n\n`NULLIF(...,0)` tránh lỗi chia 0 và trả `NULL` khi tỷ lệ không xác định vì chưa có đơn paid. Ép `numeric` tránh phép chia số nguyên làm mất phần lẻ. API có muốn đổi `NULL` thành 0 hay chuỗi “không đủ dữ liệu” là quyết định ở hợp đồng hiển thị.\n\n### Plan cần quan sát\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT customer_id, SUM(total_amount)\nFROM orders\nWHERE status = 'paid'\nGROUP BY customer_id\nHAVING SUM(total_amount) >= 500000;\n```\n\nTrên bảng mẫu, plan nhỏ không đại diện production. Khi thử với dữ liệu đủ lớn, tìm nút scan tạo dòng đầu vào, nút aggregate, `actual rows` là số nhóm, và memory/disk usage nếu phiên bản PostgreSQL hiển thị. Không ghi lại một thời gian cụ thể như chân lý; mục tiêu là hiểu dòng dữ liệu co từ nhiều đơn thành ít nhóm ở nút nào.",

so_sanh: "### Chọn đúng công cụ lọc và tổng hợp\n\n| Nhu cầu | Cấu trúc đúng | Lý do |\n|---|---|---|\n| Bỏ đơn refunded trước khi tính | `WHERE status <> 'refunded'` | giảm dòng đầu vào |\n| Giữ khách có tổng trên ngưỡng | `HAVING SUM(...) > ...` | điều kiện chỉ biết sau gom |\n| Đếm mọi đơn | `COUNT(*)` | không phụ thuộc NULL của cột |\n| Đếm khách đã nhận diện | `COUNT(customer_id)` | bỏ dòng customer_id NULL |\n| Nhiều chỉ số theo trạng thái | aggregate `FILTER` | mỗi chỉ số có điều kiện riêng |\n| Tổng tập rỗng theo nghiệp vụ là 0 | `COALESCE(SUM(...),0)` | chuẩn SQL trả NULL |\n\n### Tính trực tiếp hay lưu tổng\n\n| Phương án | Tính mới mỗi truy vấn | Summary/materialized view |\n|---|---|---|\n| Độ mới | theo snapshot hiện tại | phụ thuộc lịch làm mới |\n| Chi phí đọc lặp | có thể cao | thấp hơn |\n| Chi phí ghi/vận hành | đơn giản | phải cập nhật, retry, đối soát |\n| Khả năng sửa công thức | chạy lại query | có thể phải rebuild |\n\nKhông có lựa chọn thắng tuyệt đối. Tổng tiền phục vụ thanh toán thường cần nguồn giao dịch chính xác; dashboard xu hướng có thể chấp nhận trễ để đổi lấy phản hồi nhanh.",

loi_thuong_gap: [
  "Đưa điều kiện aggregate vào `WHERE`; PostgreSQL báo không được dùng aggregate tại đó. Chuyển điều kiện nhóm sang `HAVING`.",
  "Thêm mọi cột vào `GROUP BY` để hết lỗi; số dòng kết quả tăng gần bằng dữ liệu gốc. Xác định grain mong muốn trước.",
  "Nhầm `COUNT(*)` với `COUNT(column)`; chênh lệch xuất hiện khi column có NULL. So sánh cả hai trong truy vấn chẩn đoán.",
  "Tin rằng `SUM` tập rỗng bằng 0; API nhận `null` và phép cộng phía sau hỏng. Dùng `COALESCE` chỉ khi 0 đúng về nghiệp vụ.",
  "JOIN bảng chi tiết rồi cộng cột cấp cha; tổng tăng theo số con. Kiểm `COUNT(*)` theo khóa cha và pre-aggregate đúng grain.",
  "Dùng `SUM(DISTINCT amount)` để chữa nhân dòng; hai giao dịch cùng số tiền bị mất. Khử trùng theo khóa hay sửa JOIN.",
  "Cho rằng GROUP BY tự sắp xếp; thứ tự đổi khi plan chuyển giữa hash và sort. Thêm `ORDER BY` tường minh.",
  "Tính trung bình của các trung bình không có trọng số; nhóm nhỏ và lớn bị coi ngang nhau. Giữ tổng và số lượng để tính trung bình toàn cục."
],

tu_kiem_tra: [
  "Không có dòng paid: `COUNT(*)`, `SUM(total_amount)` và `GROUP BY status` lần lượt trả hình dạng nào?",
  "Giải thích vì sao `AVG(COALESCE(score,0))` khác `COALESCE(AVG(score),0)`.",
  "Viết truy vấn tìm khách có ít nhất hai đơn paid nhưng không tính khách mua tại quầy.",
  "Vì sao `SUM(DISTINCT o.total_amount)` không phải cách sửa JOIN nhân dòng? Nêu phản ví dụ hai đơn cùng giá.",
  "Khi nào `FILTER` tốt hơn một `WHERE` chung, và khi nào `CASE` có lợi hơn?",
  "Một `LIMIT 10` sau `GROUP BY ... ORDER BY SUM(...) DESC` có giúp PostgreSQL bỏ qua phần lớn dòng đầu vào không? Giải thích.",
  "Bạn sẽ đọc dấu hiệu nào trong `EXPLAIN (ANALYZE, BUFFERS)` để biết hash aggregate thiếu bộ nhớ hoặc estimate số nhóm sai?"
],

tai_nguyen: [
  "PostgreSQL Documentation — Aggregate Functions, SELECT và FILTER Clause.",
  "“SQL for Smarties” của Joe Celko — tư duy theo tập và các bài toán tổng hợp.",
  "“The Art of PostgreSQL” của Dimitri Fontaine — truy vấn phân tích thực dụng trên PostgreSQL.",
  "Khóa học Databases của Stanford Online — SQL, đại số quan hệ và grouping."
],

lien_he: [
  "Nối sang mảng Thống kê: `AVG`, tỷ lệ và weighted average chỉ có nghĩa khi mẫu số cùng grain và dữ liệu thiếu được diễn giải đúng.",
  "Nối sang mảng Cấu trúc dữ liệu: `HashAggregate` dùng bảng băm, còn `GroupAggregate` khai thác dữ liệu đã sắp xếp; hai cấu trúc dẫn đến profile bộ nhớ khác nhau.",
  "Nối sang mảng Hệ thống phân tán: summary cập nhật bất đồng bộ phải xử lý retry và idempotency để không cộng doanh thu hai lần.",
  "Nối sang mảng Kiểm thử: đối soát tổng cấp đơn với tổng dòng vé là một invariant có thể tự động kiểm tra bằng integration test."
]

};
