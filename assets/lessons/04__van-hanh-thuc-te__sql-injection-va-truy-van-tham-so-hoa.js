window.LESSONS["04:van-hanh-thuc-te:sql-injection-va-truy-van-tham-so-hoa"] = {

tom_tat: [
  "SQL injection xảy ra khi dữ liệu không đáng tin được parser của PostgreSQL hiểu thành một phần cú pháp; nguồn gốc là trộn hai kênh, không đơn thuần là “có dấu nháy”.",
  "Truy vấn tham số hóa gửi mẫu SQL và giá trị tách biệt để giá trị giữ vai trò dữ liệu, kể cả khi chứa dấu nháy, comment hay từ khóa SQL.",
  "Placeholder chỉ dùng cho giá trị; tên bảng, tên cột và hướng sắp xếp phải chọn từ allowlist do ứng dụng sở hữu.",
  "ORM giảm chỗ phải nối chuỗi nhưng các API như `text()`, `literal_column()` hoặc SQL tự dựng vẫn có thể đưa lỗ hổng trở lại.",
  "Phòng thủ đầy đủ còn cần quyền database tối thiểu, giới hạn thời gian/kết quả, thông báo lỗi kín và log đủ để điều tra mà không ghi bí mật."
],

can_biet_truoc: [
  "Cú pháp `SELECT`, `WHERE`, `ORDER BY`, comment `--` và literal chuỗi của PostgreSQL.",
  "HTTP query parameter và nguyên tắc mọi dữ liệu từ client đều không đáng tin.",
  "SQLAlchemy `text()`, bind parameter và transaction ở bài ORM trước.",
  "Khái niệm role, `GRANT`, schema và quyền thực thi trong PostgreSQL."
],

dinh_nghia: "**SQL injection** là lớp lỗ hổng trong đó đầu vào do bên không tin cậy kiểm soát làm thay đổi cấu trúc hoặc ý nghĩa của câu SQL mà lập trình viên dự định. Nếu ứng dụng nối `email` vào văn bản truy vấn, dấu nháy trong đầu vào có thể kết thúc literal; phần tiếp theo được parser đọc như toán tử, comment hoặc câu lệnh.\n\n**Truy vấn tham số hóa** giữ câu SQL cố định và truyền giá trị qua bind parameter riêng. Driver mã hóa giá trị theo kiểu dữ liệu/protocol; PostgreSQL không ghép lại giá trị đó để parse như SQL. Đây là kiểm soát chính cho mọi giá trị động. Escape thủ công không phải cơ chế tương đương vì phụ thuộc dialect, encoding, kiểu và mọi đường đi dữ liệu phải nhớ gọi đúng hàm.",

vi_sao_can: "Vé Mây cho người dùng tìm booking theo email và cho nhân viên lọc theo trạng thái. Một dòng f-string có thể biến chức năng tìm một khách thành đọc booking của mọi khách. Nguy hiểm không dừng ở mất dữ liệu: tài khoản database của ứng dụng có thể sửa trạng thái thanh toán, đọc payload nhà cung cấp hoặc giữ khóa để gây từ chối dịch vụ.\n\nLỗi này tồn tại vì code nhìn chuỗi như ký tự, còn PostgreSQL nhìn nó như ngôn ngữ. Người viết nghĩ đã tạo một literal; parser có thể thấy literal đã kết thúc và một biểu thức Boolean mới bắt đầu. Chặn vài ký tự không giải quyết được grammar rộng, các kiểu số không cần dấu nháy, toán tử, encoding và nhiều ngữ cảnh SQL khác nhau.\n\nTham số hóa tạo một ranh giới kiến trúc có thể review: SQL quyết định bởi code tin cậy, dữ liệu quyết định bởi caller. Khi identifier thật sự phải động, allowlist thu hẹp lựa chọn về tập token do code định nghĩa. Least privilege giới hạn bán kính thiệt hại nếu một đường nối chuỗi vẫn lọt qua.",

co_che: "### Parser không biết “ý định” của lập trình viên\n\nCode nguy hiểm sau dựng một chuỗi hoàn chỉnh trước khi PostgreSQL nhìn thấy nó:\n\n```python\nsql = f\"SELECT id, status FROM bookings WHERE user_id = {user_id}\"\nrows = conn.execute(text(sql)).all()  # KHÔNG làm vậy\n```\n\nNếu `user_id` là `42 OR true`, database nhận predicate luôn đúng. Không có dấu nháy nào để escape. Đây là lý do lời khuyên “thay `'` bằng `''`” vừa không đủ vừa tạo cảm giác an toàn giả.\n\n### Hai kênh của bind parameter\n\nVới `WHERE user_id = :uid`, SQLAlchemy biên dịch placeholder theo paramstyle của driver và gửi giá trị riêng. Giá trị `\"42 OR true\"` được xử lý như một giá trị cho cột bigint; PostgreSQL sẽ báo sai kiểu, không biến nó thành toán tử. Với text, chuỗi chứa `'`, `--` hay `/*` vẫn là nội dung literal.\n\n```python\nstmt = text(\"\"\"\n  SELECT b.id, b.status, b.created_at\n  FROM bookings AS b JOIN users AS u ON u.id = b.user_id\n  WHERE u.email = :email\n  ORDER BY b.created_at DESC, b.id DESC\n  LIMIT :limit\n\"\"\")\nrows = session.execute(stmt, {\"email\": email, \"limit\": 50}).mappings().all()\n```\n\nParameterization không đồng nghĩa database chuẩn bị và lưu plan vĩnh viễn cho mọi câu. Driver/server có thể dùng unnamed statement, prepared statement hoặc plan generic/custom tùy cấu hình. Lợi ích bảo mật đến từ tách cú pháp và giá trị; đừng giải thích sai rằng nó an toàn chỉ vì “compile một lần”.\n\n### Identifier không phải value\n\n`ORDER BY :column` không biến tham số thành tên cột; nó là một giá trị hằng, vì placeholder nằm ở vị trí expression. Tên cột, bảng, toán tử và từ khóa thay đổi grammar nên phải chọn từ allowlist.\n\n```python\nSORTS = {\n    \"newest\": \"b.created_at DESC, b.id DESC\",\n    \"oldest\": \"b.created_at ASC, b.id ASC\",\n    \"amount_high\": \"b.total_cents DESC, b.id DESC\",\n}\norder_sql = SORTS.get(requested_sort)\nif order_sql is None:\n    raise ValueError(\"kiểu sắp xếp không hợp lệ\")\nstmt = text(f\"\"\"\n  SELECT b.id, b.status, b.total_cents, b.created_at\n  FROM bookings AS b\n  WHERE b.user_id = :uid\n  ORDER BY {order_sql}\n  LIMIT :limit\n\"\"\")\n```\n\nỞ đây f-string chấp nhận được vì đoạn chèn lấy từ dictionary hằng do server sở hữu, không từ client. Một lựa chọn khác là xây biểu thức SQLAlchemy bằng column object; nguyên tắc tin cậy vẫn giống nhau.\n\n### Second-order injection\n\nDữ liệu nguy hiểm có thể được lưu an toàn bằng parameter ở request thứ nhất, rồi bị lấy ra và nối vào SQL ở tác vụ quản trị sau. Lần lưu không có khai thác; lần sử dụng thứ hai mới đổi cú pháp. Vì vậy không được coi dữ liệu “đã ở trong database” là tin cậy. Ranh giới phải được giữ ở mỗi query sink.\n\nVí dụ tên export do user đặt được lưu như text. Job đêm dùng tên đó để dựng `CREATE TABLE export_<name>` sẽ nguy hiểm nếu không map sang identifier nội bộ. Giải pháp tốt là sinh ID server-side, không cố làm sạch một tên tự do thành SQL identifier.\n\n### Injection mù và kênh suy luận\n\nỨng dụng có thể không hiển thị hàng dữ liệu nhưng kẻ tấn công vẫn suy luận qua phản hồi đúng/sai, thông báo lỗi hoặc thời gian. Một predicate điều kiện có thể làm trang trả số hàng khác; hàm gây trì hoãn tạo timing channel. Do đó “API chỉ trả 200/500” không chứng minh an toàn.\n\nĐừng kiểm thử production bằng payload phá hoại. Test tự động trên database cô lập nên xác nhận input đặc biệt không làm thay đổi số hàng và query log chỉ có một statement mong đợi. Security review tập trung vào sink dựng SQL, không săn một danh sách payload hữu hạn.\n\n### Multi-statement không phải điều kiện cần\n\nMột số driver cấm nhiều statement trong một call, nhưng injection vẫn có thể thay predicate, `UNION` dữ liệu hoặc gọi biểu thức tốn tài nguyên trong chính một statement. Tắt multi-statement giảm một nhánh tấn công chứ không chữa lỗi nối chuỗi.\n\nTương tự, read-only endpoint không chắc vô hại. Role có thể đọc payment payload; query nặng có thể làm cạn worker và connection pool; `SELECT` có thể gọi function nếu role được cấp quyền không hợp lý.\n\n### ORM an toàn ở đâu và thủng ở đâu\n\nBiểu thức `Booking.user_id == user_input` tạo bind parameter tự động. Nhưng ORM không thanh tẩy mọi chuỗi được đưa cho API escape hatch. `text(f\"...\")`, `literal_column(user_input)`, tự viết `order_by(text(user_input))` hay ghép filter là những sink cần review.\n\n```python\n# An toàn: value trở thành bind parameter\nstmt = select(Booking).where(Booking.status == requested_status)\n\n# Nguy hiểm: client kiểm soát grammar\nstmt = select(Booking).order_by(text(request.args[\"sort\"]))\n```\n\nSAST có thể tìm f-string gần `execute`, nhưng wrapper và second-order flow khiến nó bỏ sót; đồng thời câu SQL ghép từ hằng có thể bị báo giả. Code review và test integration vẫn cần thiết.\n\n### Kiểu dữ liệu là lớp bảo vệ bổ sung\n\nParse `user_id` thành integer và giới hạn `limit` giúp contract API rõ, ngăn giá trị phi lý và giảm tải. Nó không thay thế bind parameter: email, search term và JSON không thể chỉ ép thành số; hơn nữa một ngày code refactor có thể bỏ validation. Defense in depth nghĩa là mỗi lớp độc lập giảm rủi ro.\n\nGiới hạn độ dài search term và `LIMIT` ngăn một input hợp lệ về cú pháp tạo query quá đắt. Với tìm kiếm chứa wildcard, quyết định rõ client có được dùng `%` và `_` hay chúng là ký tự thường. Parameterization ngăn injection nhưng không tự escape wildcard của `LIKE`.\n\n### LIKE có hai bài toán khác nhau\n\n`WHERE name ILIKE :pattern` an toàn về cú pháp khi pattern là parameter. Tuy nhiên `%` vẫn có nghĩa wildcard theo ngôn ngữ LIKE. Nếu sản phẩm muốn tìm literal, ứng dụng phải escape `\\`, `%`, `_` và khai báo `ESCAPE '\\'`; đây là semantic injection vào mẫu tìm kiếm, không phải SQL injection.\n\n```python\ndef literal_like(s: str) -> str:\n    return \"%\" + s.replace(\"\\\\\", \"\\\\\\\\\").replace(\"%\", \"\\\\%\").replace(\"_\", \"\\\\_\") + \"%\"\n\nstmt = text(\"SELECT id, name FROM events WHERE name ILIKE :p ESCAPE '\\\\' LIMIT 20\")\nrows = session.execute(stmt, {\"p\": literal_like(term)}).all()\n```\n\n### Least privilege làm nhỏ bán kính thiệt hại\n\nRole web không cần `SUPERUSER`, không nên sở hữu schema, không cần `CREATE` hay quyền trên bảng migration history. Có thể tách role migration có DDL khỏi role runtime chỉ có `SELECT/INSERT/UPDATE` đúng bảng. Nếu endpoint chỉ đọc qua view, cấp quyền trên view thay vì bảng gốc khi thiết kế cho phép.\n\nQuyền không cứu được dữ liệu mà role hợp pháp được đọc. Nếu web role cần xem mọi booking để phục vụ admin, injection trên đường admin vẫn rò tất cả. Row-level security có thể bổ sung tenant isolation nhưng policy sai cũng là lỗi; luôn đặt `user_id`/tenant từ danh tính server xác thực, không tin ID trong request.\n\n### Plan và index dưới truy vấn tìm kiếm\n\nParameterization không làm mất index một cách mặc định. Với `users.email = :email`, unique index do constraint `UNIQUE(email)` tạo ra cho phép lookup. Nếu viết `lower(email) = lower(:email)`, index thường trên `email` không trực tiếp khớp expression; cần index biểu thức `lower(email)` hoặc kiểu/extension thích hợp theo yêu cầu sản phẩm.\n\n```sql\nCREATE UNIQUE INDEX ux_users_email_lower ON users (lower(email));\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id FROM users WHERE lower(email) = lower('an@example.test');\n```\n\nKhông khẳng định plan sẽ là Index Scan khi bảng rất nhỏ: planner có thể chọn sequential scan vì rẻ hơn. Hãy đọc actual rows và buffers trên dữ liệu đại diện.\n\n### Logging không được tái tạo lỗ hổng dữ liệu\n\nLog template SQL, tên operation, thời lượng, số hàng và request ID. Tránh log password, token, payment payload hoặc toàn bộ parameter. Khi điều tra, cần biết endpoint nào phát query bất thường nhưng không nên biến hệ thống log thành kho bí mật thứ hai.\n\nThông báo trả client không lộ SQL text, tên bảng hay stack trace. Server vẫn giữ exception có cấu trúc trong log bảo vệ quyền truy cập. Đừng nuốt lỗi thành kết quả rỗng; điều đó che tấn công và làm vận hành hiểu nhầm là không có dữ liệu.\n\n### Timeout và giới hạn tài nguyên\n\nBind parameter ngăn đổi grammar, nhưng search hợp lệ có thể dùng regex tốn kém hoặc trả hàng triệu dòng. Đặt `statement_timeout`, page size tối đa và query shape có index. Timeout phải dẫn tới rollback transaction; connection không được trả về pool trong trạng thái aborted.\n\nỞ tầng API, rate limit theo danh tính và chi phí endpoint. Ở database, role runtime có thể có timeout mặc định. Đây là kiểm soát availability bổ sung, không phải lý do chấp nhận SQL ghép chuỗi.\n\n### Khi stored procedure không tự động an toàn\n\nFunction dùng SQL tĩnh với parameter có thể giảm bề mặt quyền. Nhưng PL/pgSQL dựng `EXECUTE '...' || input` vẫn injection. Dynamic SQL trong PostgreSQL phải dùng `format('%I', identifier)` cho identifier tin cậy có kiểm soát và `USING` cho value; `%L` đúng ngữ cảnh vẫn khó review hơn `USING`.\n\n`SECURITY DEFINER` đặc biệt nhạy cảm vì function chạy bằng quyền owner. Phải cố định `search_path`, giới hạn quyền execute và tránh dynamic object resolution. Nếu không cần nâng quyền, giữ mặc định security invoker.\n\n### Khi nào truy vấn tham số hóa chưa đủ\n\nNó không xử lý broken authorization: query an toàn nhưng quên `user_id = current_user` vẫn lộ dữ liệu. Nó không chống credential database bị đánh cắp, backup công khai, dependency độc hại hay log chứa bí mật. Nó cũng không xác nhận business rule như nhân viên nào được hoàn vé.\n\nBài học đúng là đặt parameterization ở nền móng, rồi xây validation, authorization, least privilege, observability và resource limits phía trên. Không có một hàm “sanitize” thay cho kiến trúc này.",

vi_du: "### Thí nghiệm cô lập, không dùng payload phá hoại\n\nScript sau dùng PostgreSQL và SQLAlchemy, tạo dữ liệu trong transaction rồi rollback. Nó chứng minh input chứa cú pháp chỉ được so như email. Cần schema Vé Mây từ bài trước.\n\n```python\nfrom sqlalchemy import create_engine, text\n\nengine = create_engine(\"postgresql+psycopg://app:app@localhost/vemay\")\n\nSAFE = text(\"\"\"\n  SELECT b.id\n  FROM bookings AS b JOIN users AS u ON u.id = b.user_id\n  WHERE u.email = :email\n  ORDER BY b.id\n\"\"\")\n\ndef find_booking_ids(conn, email: str) -> list[int]:\n    return list(conn.execute(SAFE, {\"email\": email}).scalars())\n\nwith engine.begin() as conn:\n    alice = conn.execute(text(\"INSERT INTO users(email) VALUES (:e) RETURNING id\"),\n                         {\"e\": \"alice@example.test\"}).scalar_one()\n    event = conn.execute(text(\"\"\"\n      INSERT INTO events(name, starts_at, seats_remaining)\n      VALUES (:n, now() + interval '7 days', 10) RETURNING id\n    \"\"\"), {\"n\": \"Đêm PostgreSQL\"}).scalar_one()\n    conn.execute(text(\"\"\"\n      INSERT INTO bookings(user_id,event_id,status,total_cents)\n      VALUES (:u,:e,'paid',250000)\n    \"\"\"), {\"u\": alice, \"e\": event})\n\n    assert len(find_booking_ids(conn, \"alice@example.test\")) == 1\n    assert find_booking_ids(conn, \"' OR true --\") == []\n    assert find_booking_ids(conn, \"alice@example.test' --\") == []\n    conn.rollback()  # dữ liệu thí nghiệm không tồn tại sau khi chạy\n```\n\nKhông đưa phiên bản nối chuỗi vào helper production “để minh họa”; code nguy hiểm dễ bị copy. Test trên xác nhận contract bằng kết quả và rollback, không tuyên bố ngăn mọi kỹ thuật tấn công.\n\n### Allowlist cho bộ lọc và sort\n\n```python\nfrom sqlalchemy import asc, desc, select\n\nSORT_EXPRESSIONS = {\n    \"newest\": (desc(Booking.created_at), desc(Booking.id)),\n    \"oldest\": (asc(Booking.created_at), asc(Booking.id)),\n    \"amount_high\": (desc(Booking.total_cents), desc(Booking.id)),\n}\nVALID_STATUS = {\"pending\", \"paid\", \"cancelled\", \"expired\"}\n\ndef list_bookings(session, uid: int, status: str | None, sort: str):\n    if sort not in SORT_EXPRESSIONS:\n        raise ValueError(\"sort không hợp lệ\")\n    if status is not None and status not in VALID_STATUS:\n        raise ValueError(\"status không hợp lệ\")\n    stmt = select(Booking).where(Booking.user_id == uid)\n    if status is not None:\n        stmt = stmt.where(Booking.status == status)\n    return session.scalars(stmt.order_by(*SORT_EXPRESSIONS[sort]).limit(50)).all()\n```\n\n`uid` và `status` vẫn thành bind parameter; tuple sort là column object do server tạo. Ca biên gồm status rỗng, sort lạ, Unicode trong email, email rất dài, không có kết quả và timeout. API nên phân biệt validation error với database unavailable, nhưng không trả chi tiết SQL.\n\n### Kiểm quyền runtime bằng PostgreSQL\n\n```sql\n-- Chạy bằng role quản trị khi triển khai, không bằng runtime role.\nCREATE ROLE vemay_runtime LOGIN PASSWORD 'replace-through-secret-manager';\nGRANT CONNECT ON DATABASE vemay TO vemay_runtime;\nGRANT USAGE ON SCHEMA public TO vemay_runtime;\nGRANT SELECT, INSERT, UPDATE ON users, events, bookings, booking_items, payment_attempts TO vemay_runtime;\nGRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO vemay_runtime;\n\n-- Không cấp CREATE, DROP, ALTER hoặc quyền trên bảng alembic_version.\n-- Kiểm tra bằng một connection mang role runtime trong môi trường test.\n```\n\nPassword trong ví dụ chỉ là placeholder và không được commit dưới dạng giá trị thật. Production nên lấy credential từ secret manager/môi trường triển khai, xoay vòng và tách role migration.",

so_sanh: "### Những biện pháp thường bị đánh đồng\n\n| Biện pháp | Ngăn đổi cú pháp SQL | Xử lý identifier động | Giới hạn hậu quả | Ghi chú |\n|---|---:|---:|---:|---|\n| Bind parameter | Có, cho value | Không | Không | Kiểm soát bắt buộc |\n| Escape thủ công | Không đáng tin | Không | Không | Tránh dùng |\n| Allowlist | Có nếu tập lựa chọn kín | Có | Không | Hợp cho sort/cột |\n| Ép kiểu/validation | Một phần | Không | Giảm input phi lý | Lớp bổ sung |\n| ORM expression | Thường tự bind value | Chỉ khi dùng column object | Không | Escape hatch vẫn nguy hiểm |\n| Least privilege | Không | Không | Có | Giảm bán kính thiệt hại |\n| WAF | Có thể chặn mẫu đã biết | Không | Một phần | Không hiểu đầy đủ query context |\n\nMột thiết kế tốt dùng bind parameter cộng allowlist và quyền tối thiểu. WAF hoặc regex có thể thêm tín hiệu, nhưng không được đứng thay cho sửa query sink.",

loi_thuong_gap: [
  "Escape dấu nháy rồi tin input số an toàn. Nhận biết bằng f-string/concatenation quanh `execute`; input số vẫn có thể chèn toán tử nếu không bind.",
  "Dùng placeholder cho tên cột và ngạc nhiên khi sort không hoạt động. Identifier thuộc grammar; chọn column object từ allowlist.",
  "Tin ORM tuyệt đối nhưng đưa request vào `text()` hoặc `literal_column()`. Review mọi API nhận SQL fragment, không chỉ raw driver calls.",
  "Chỉ kiểm payload ở request đầu. Dữ liệu lưu trong database có thể gây second-order injection khi job sau nối nó vào dynamic SQL.",
  "Cho runtime role sở hữu schema. Thử kết nối bằng role đó và kiểm `CREATE TABLE`/`DROP` phải bị từ chối.",
  "Log cả parameter để điều tra. Dấu hiệu là email, token hoặc payment payload xuất hiện trong log; chuyển sang metadata và redaction.",
  "Nghĩ parameter hóa xử lý wildcard LIKE. Query không injection nhưng `%` làm nghĩa tìm kiếm đổi; escape pattern nếu sản phẩm muốn literal.",
  "Bắt database error rồi trả nguyên message. Client nhìn thấy tên bảng/cột; ánh xạ sang lỗi công khai ổn định và giữ chi tiết trong log bảo vệ."
],

tu_kiem_tra: [
  "Vì sao thay dấu nháy không ngăn được injection vào trường `user_id` dạng số?",
  "Giải thích tại sao `ORDER BY :column` không phải cách tham số hóa identifier.",
  "Một ORM query dùng `text(request.args['filter'])`. ORM còn bảo vệ phần nào và đã mất bảo vệ ở đâu?",
  "Phân biệt SQL injection với wildcard làm rộng kết quả `LIKE`; biện pháp cho từng lỗi là gì?",
  "Mô tả second-order injection trong job export của Vé Mây và thiết kế loại bỏ identifier do user kiểm soát.",
  "Nếu endpoint chỉ có SELECT, attacker vẫn có thể gây những loại thiệt hại nào?",
  "Vì sao least privilege không thay thế bind parameter, và bind parameter không thay thế authorization?",
  "Thiết kế integration test chứng minh chuỗi chứa comment SQL được coi là email, không dựa vào việc tìm một payload duy nhất."
],

tai_nguyen: [
  "OWASP SQL Injection Prevention Cheat Sheet và OWASP Query Parameterization Cheat Sheet.",
  "Tài liệu chính thức PostgreSQL, các phần SQL Syntax, Database Roles, GRANT và PL/pgSQL Statements.",
  "Tài liệu chính thức SQLAlchemy, phần Using Textual SQL và Sending Parameters.",
  "“The Web Application Hacker’s Handbook” — Dafydd Stuttard và Marcus Pinto, các chương về injection và kiểm thử ứng dụng web."
],

lien_he: [
  "Nối sang mảng Bảo mật ứng dụng: SQL injection là một trường hợp của injection khi dữ liệu vượt ranh giới interpreter; cùng mô hình áp dụng cho shell command và template.",
  "Nối sang mảng API: authentication xác định ai gọi, authorization thêm predicate theo chủ thể; query có tham số vẫn có thể lộ dữ liệu nếu thiếu predicate này.",
  "Nối sang mảng Quan sát hệ thống: structured logging và cảnh báo query bất thường hỗ trợ phát hiện, nhưng phải redaction để log không thành nguồn rò rỉ mới.",
  "Nối sang mảng DevOps: role migration và role runtime phải có credential, quyền và vòng đời khác nhau; bài Alembic kế tiếp dùng chính sự tách vai trò đó."
]

};
