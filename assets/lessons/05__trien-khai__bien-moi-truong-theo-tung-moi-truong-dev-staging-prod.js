window.LESSONS["05:trien-khai:bien-moi-truong-theo-tung-moi-truong-dev-staging-prod"] = {

tom_tat: [
  "Cùng một artifact FastAPI phải nhận cấu hình khác nhau ở dev, staging và prod; biến môi trường là một kênh đưa giá trị vào process, không phải kho bí mật tự thân.",
  "Ứng dụng nên đọc, kiểm tra kiểu và đóng băng cấu hình lúc khởi động; thiếu biến bắt buộc thì dừng ngay thay vì nhận traffic rồi mới lỗi.",
  "File `.env` chỉ là tiện ích cho máy phát triển, không phải kho bí mật và không phải định dạng mà hệ điều hành tự hiểu.",
  "FastAPI có thể cấp một đối tượng settings qua dependency injection, nhờ vậy code nghiệp vụ không gọi `os.getenv()` rải rác và test thay cấu hình có kiểm soát.",
  "Secret cần được che khỏi log, giới hạn quyền đọc, xoay vòng và truyền bằng cơ chế của nền tảng triển khai; biến môi trường chỉ giải quyết khâu vận chuyển, không tự tạo ra bảo mật.",
  "Secret là dữ liệu cấp quyền hoặc mở khóa tài sản khác: mật khẩu cơ sở dữ liệu, khóa ký JWT, API key, private key. Tên máy chủ hay cờ bật tính năng có thể là cấu hình nhưng không nhất thiết là secret."
],

can_biet_truoc: [
  "Khái niệm process và môi trường thực thi của process trong hệ điều hành.",
  "FastAPI dependency injection, lifespan và mô hình request–response.",
  "Kiểu dữ liệu Python, ngoại lệ và type hint.",
  "Khái niệm URL kết nối PostgreSQL, HTTP timeout và khóa bí mật.",
  "Biến môi trường của hệ điều hành, vòng đời process và sự khác nhau giữa giá trị tồn tại trong shell cha với giá trị mà process con thừa kế."
],

dinh_nghia: "**Cấu hình theo môi trường** tách những giá trị thay đổi khi deploy khỏi mã nguồn: URL PostgreSQL, bucket hóa đơn, hostname, mức log và khóa ký. Dev, staging, prod dùng cùng code nhưng nhận bộ giá trị độc lập.\n\n**Cấu hình bằng biến môi trường** là cách để hệ thống chạy ứng dụng gắn các cặp tên–giá trị vào môi trường của process trước khi process bắt đầu. Python đọc chúng qua `os.environ`; ứng dụng chuyển chuỗi nhận được thành kiểu miền như số nguyên, Boolean, URL hoặc danh sách rồi kiểm tra ràng buộc.\n\nĐiểm cốt lõi là tách hai thứ có vòng đời khác nhau: **artifact** chứa code có thể được xây một lần, còn **configuration** quyết định artifact ấy nói chuyện với database nào, tin proxy nào và bật tính năng gì ở từng nơi triển khai. Cùng một image có thể chạy ở máy lập trình, staging và production mà không sửa code hay xây lại image.\n\nBiến môi trường không đồng nghĩa với `.env`. `.env` là file văn bản theo quy ước `TEN=gia_tri` được thư viện đọc rồi nạp vào process, chủ yếu tiện cho phát triển cục bộ. Trên production, orchestrator, service manager hoặc secret store thường cấp giá trị trực tiếp.\n\n**Secret** là một giá trị mà việc biết hoặc sở hữu nó cho phép chủ thể thực hiện hành động nhạy cảm: đăng nhập vào cơ sở dữ liệu, ký token, gọi API tính phí, giải mã dữ liệu hoặc mạo danh dịch vụ. Vì secret mang năng lực, phạm vi thiệt hại của nó được quyết định bởi quyền, thời hạn và nơi credential được chấp nhận, không bởi tên file chứa nó.\n\n**Quản lý secret** là toàn bộ vòng đời tạo, lưu, phân phối, sử dụng, quan sát, xoay và thu hồi các giá trị ấy. Mục tiêu không phải “không ai từng nhìn thấy secret” — ứng dụng hợp lệ buộc phải dùng nó — mà là chỉ đúng workload được đọc đúng secret trong đúng khoảng thời gian, mọi lần cấp phát quan trọng có thể kiểm toán, và việc thay secret không làm dịch vụ ngừng hoạt động.\n\n`.env` là file văn bản theo quy ước `TÊN=giá_trị`, thường được thư viện đọc để đưa cấu hình vào process khi phát triển cục bộ. Nó không tự mã hóa, không cấp quyền chi tiết, không xoay khóa, không có audit log và không thu hồi bản sao. Do đó `.env` có thể tiện cho máy cá nhân, nhưng **không phải secret manager**. “Không commit `.env`” là điều kiện cần, hoàn toàn chưa phải thiết kế bảo mật đầy đủ.",

vi_sao_can: "API “Bàn Trống” cần `DATABASE_URL`, `BOOKING_SIGNING_KEY`, thời hạn chờ kết nối và danh sách origin được phép. Nếu các giá trị ấy nằm trong code, mỗi lần đổi mật khẩu database phải sửa source, xây lại image và rất dễ để khóa đi vào lịch sử Git. Nếu dùng một file cấu hình giống nhau cho mọi nơi, máy cá nhân có thể vô tình trỏ vào dữ liệu thật.\n\nCấu hình ngoài code giải quyết ba bài toán vận hành. Thứ nhất, artifact trở nên bất biến: bản đã kiểm thử chính là bản được đưa lên production. Thứ hai, quyền sở hữu rõ hơn: nhóm vận hành cấp endpoint và secret mà không cần sửa nghiệp vụ. Thứ ba, rollback dễ hơn vì phiên bản code và bộ giá trị triển khai là hai chiều độc lập.\n\nNhưng tách cấu hình cũng tạo rủi ro mới. Một lỗi chính tả trong tên biến chỉ xuất hiện khi deploy; mọi giá trị ban đầu đều là chuỗi; secret có thể lộ qua log, trang chẩn đoán hoặc quyền xem cấu hình container. Vì vậy bài toán không dừng ở `os.getenv()`: cần schema, kiểm tra lúc boot, chính sách mặc định, telemetry không tiết lộ và quy trình xoay khóa.\n\nTrong “Cổng thanh toán lớp học”, một dòng `PAYMENT_API_KEY=...` có thể cho phép tạo giao dịch tính tiền; `DATABASE_URL` có thể mở toàn bộ hồ sơ học viên; `JWT_SIGNING_KEY` có thể cho phép tự phát hành token quản trị. Nếu ba giá trị cùng nằm trong repository, một lần chia sẻ nhầm repository hoặc log CI là đủ biến lỗi quản lý mã nguồn thành sự cố dữ liệu và tài chính.\n\nSecret có tính chất khác mật khẩu người dùng. Mật khẩu người dùng nên được băm một chiều để server không cần biết bản rõ; API key hay khóa ký thường phải được process sử dụng ở dạng có thể đọc được. Vì không thể “băm rồi dùng” theo cùng cách, ta phải bảo vệ đường đi và giới hạn quyền của credential. Mã hóa file chỉ chuyển câu hỏi sang “khóa giải mã nằm ở đâu”; nếu khóa nằm cạnh ciphertext thì rủi ro gần như không đổi.\n\nVấn đề còn nằm ở vận hành. Một credential sống nhiều năm khiến mọi bản sao cũ vẫn hữu dụng. Một credential dùng chung cho dev, staging và production khiến lỗi ở môi trường ít bảo vệ lan sang dữ liệu thật. Một lần xoay khóa đột ngột có thể làm toàn bộ replica FastAPI trả lỗi nếu bên phát hành và bên kiểm tra không chấp nhận giai đoạn chồng lấn. Quản lý secret tốt vì thế đồng thời giảm xác suất lộ, giảm blast radius (phạm vi ảnh hưởng), rút ngắn thời gian credential còn giá trị và tạo đường phục hồi có thể diễn tập.",

co_che: "### Ba môi trường của Sổ Chi Tiêu\n\nDev ưu tiên phản hồi nhanh và dữ liệu giả; staging mô phỏng topology production nhưng tuyệt đối không dùng chung database hay secret; prod ưu tiên kiểm soát thay đổi, audit và khả năng phục hồi. Tên biến có thể giống nhau, còn danh tính tài nguyên và quyền phải tách hẳn.\n\n### Biên giới giữa shell, process và ứng dụng\n\nKhi chạy `BOOKING_MODE=production uvicorn app:app` trên hệ phù hợp, shell tạo process con và truyền một bản đồ môi trường. Process khác không tự nhìn thấy thay đổi về sau; process đã chạy cũng không nhận giá trị mới chỉ vì người quản trị sửa cấu hình ở control plane. Đây là lý do thay secret thường đòi restart hoặc cơ chế tải lại được thiết kế riêng.\n\nPython nhận mọi giá trị dưới dạng chuỗi. `os.environ[\"PORT\"]` là `\"8000\"`, không phải số 8000; chuỗi `\"false\"` vẫn truthy nếu dùng `bool(\"false\")`. Lớp settings phải chịu trách nhiệm chuyển kiểu và báo lỗi có ngữ cảnh. Đừng để từng route tự diễn giải một kiểu, vì lúc đó cùng `BOOKING_DEBUG=off` có thể bị hiểu khác nhau.\n\n### Hợp đồng cấu hình của Bàn Trống\n\nMột bộ cấu hình tốt nhỏ, có tên biểu đạt đơn vị và phân biệt bắt buộc với tùy chọn:\n\n| Tên | Kiểu sau parse | Chính sách | Lý do |\n|---|---|---|---|\n| `DATABASE_URL` | URL bí mật | bắt buộc | sai đích có thể ghi nhầm dữ liệu |\n| `BOOKING_SIGNING_KEY` | chuỗi bí mật | bắt buộc, tối thiểu 32 ký tự | ký token xác nhận đặt bàn |\n| `DB_CONNECT_TIMEOUT_SECONDS` | số thực dương | mặc định 3.0 | không để boot treo vô hạn |\n| `TRUSTED_ORIGINS` | danh sách URL | mặc định rỗng | CORS phải cho phép có chủ ý |\n| `LOG_LEVEL` | enum | mặc định `INFO` | sai giá trị phải bị từ chối |\n\nTên có đơn vị tránh câu hỏi “5000 là giây hay mili giây?”. Tiền tố miền giảm va chạm với biến chung của hệ thống. Giá trị mặc định chỉ hợp lý nếu hành vi an toàn: timeout có thể có mặc định; signing key thì tuyệt đối không được tự sinh mỗi lần boot vì token của replica này sẽ không được replica khác xác nhận.\n\n### Parse một lần và đóng băng\n\nVới Pydantic Settings, schema cấu hình trở thành code có type hint. Đối tượng nên được tạo lúc ứng dụng khởi động và xem như bất biến. Việc đọc `os.environ` ở mỗi request vừa khó test vừa tạo hành vi lạ nếu thư viện nào đó sửa environment trong process.\n\n```python\nfrom functools import lru_cache\nfrom typing import Literal\nfrom pydantic import AnyHttpUrl, Field, SecretStr, field_validator\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    model_config = SettingsConfigDict(\n        env_file=\".env\",\n        env_prefix=\"\",\n        extra=\"ignore\",\n        frozen=True,\n    )\n\n    database_url: SecretStr = Field(alias=\"DATABASE_URL\")\n    booking_signing_key: SecretStr = Field(\n        alias=\"BOOKING_SIGNING_KEY\", min_length=32\n    )\n    db_connect_timeout_seconds: float = Field(\n        3.0, alias=\"DB_CONNECT_TIMEOUT_SECONDS\", gt=0, le=30\n    )\n    trusted_origins: list[AnyHttpUrl] = Field(\n        default_factory=list, alias=\"TRUSTED_ORIGINS\"\n    )\n    log_level: Literal[\"DEBUG\", \"INFO\", \"WARNING\", \"ERROR\"] = \"INFO\"\n\n    @field_validator(\"trusted_origins\", mode=\"before\")\n    @classmethod\n    def split_origins(cls, value):\n        if isinstance(value, str):\n            return [part.strip() for part in value.split(\",\") if part.strip()]\n        return value\n\n@lru_cache\ndef get_settings() -> Settings:\n    return Settings()\n```\n\n`SecretStr` ngăn biểu diễn mặc định in nguyên secret, nhưng không mã hóa nó; code vẫn có thể gọi `get_secret_value()`. `frozen=True` chặn sửa nhầm sau boot. Cache bảo đảm dependency trả cùng một đối tượng thay vì parse file cho từng request.\n\n### Fail-fast trước khi nhận traffic\n\nValidation cú pháp chưa chứng minh database có thể kết nối. Ứng dụng có thể kiểm tra dependency quan trọng trong lifespan: mở pool với timeout, thực hiện truy vấn nhẹ rồi mới hoàn tất startup. Nếu thất bại, process thoát và nền tảng không đưa nó vào load balancer.\n\nKhông phải dependency nào cũng nên chặn boot. Dịch vụ gửi email phụ trợ có thể tạm hỏng mà API vẫn nhận đặt bàn rồi đưa công việc vào queue. Quyết định dựa trên khả năng phục vụ đúng: database chính là bắt buộc; analytics thường không phải. Liveness chỉ trả lời process còn sống, readiness mới nói replica đã sẵn sàng nhận traffic.\n\n### Dependency injection thay cho truy cập toàn cục\n\nRoute có thể nhận `Settings` qua `Depends(get_settings)`. Lợi ích không nằm ở việc viết ít ký tự mà ở chỗ dependency trở thành ranh giới có thể thay trong test.\n\n```python\nfrom fastapi import Depends, FastAPI\n\napp = FastAPI()\n\n@app.get(\"/v1/runtime-info\")\ndef runtime_info(settings: Settings = Depends(get_settings)):\n    return {\n        \"log_level\": settings.log_level,\n        \"trusted_origin_count\": len(settings.trusted_origins),\n        # Không bao giờ trả database_url hoặc signing key.\n    }\n```\n\nTest dùng `app.dependency_overrides[get_settings] = lambda: test_settings` rồi xóa override sau test. Cách này tránh sửa environment toàn tiến trình khi test chạy song song. Nếu cần test chính việc parse, dùng `monkeypatch.setenv`, gọi `get_settings.cache_clear()` trước và sau ca kiểm thử.\n\n### `.env` là tiện ích, không phải két sắt\n\nFile `.env.example` chỉ ghi tên và giá trị giả an toàn để người mới biết hợp đồng. `.env` thật phải nằm ngoài Git. Quyền file cần giới hạn, nhưng ngay cả vậy secret vẫn tồn tại dạng rõ trên đĩa và trong bộ nhớ process.\n\nProduction nên dùng secret manager hoặc cơ chế secret của nền tảng. Cơ chế ấy xác thực workload, kiểm soát ai được đọc, ghi audit và có thể hỗ trợ rotation. Đưa secret từ kho vào biến môi trường vẫn có hạn chế: người có quyền xem cấu hình process/container có thể đọc nó, crash dump có thể chứa nó, và cập nhật thường cần restart. Với yêu cầu nghiêm ngặt, ứng dụng lấy secret ngắn hạn qua SDK hoặc file được mount và có chiến lược reload.\n\n### Không để secret đi qua log và lỗi HTTP\n\nRủi ro phổ biến nhất không phải hacker đọc RAM mà là lập trình viên log toàn bộ settings để debug. URL PostgreSQL thường chứa mật khẩu. Query string có thể bị access log, proxy và hệ thống theo dõi lưu lại, nên token không được đặt trong URL.\n\nChỉ log allowlist trường không nhạy cảm như `log_level`, tên môi trường và timeout. Khi báo validation error, ghi tên biến thiếu chứ không ghi giá trị nhận được. Endpoint chẩn đoán phải yêu cầu quyền phù hợp và vẫn không xuất secret. Bộ lọc che chuỗi chỉ là lớp cuối; thiết kế tốt không đưa dữ liệu bí mật vào record ngay từ đầu.\n\n### Rotation mà không làm token cũ chết ngay\n\nNếu thay `BOOKING_SIGNING_KEY` tức thì trên nhiều replica, replica cũ và mới có thể không xác nhận token của nhau trong lúc rolling deployment. Giải pháp là key ring: một khóa hiện hành dùng để ký, một hoặc vài khóa trước chỉ dùng để xác minh, mỗi khóa có `kid` (key identifier). Sau khi tuổi thọ token cũ kết thúc mới loại khóa trước.\n\nDatabase credential cũng cần giai đoạn chồng lấn hoặc pool reconnect. Rotation là quy trình phân tán, không phải phép gán chuỗi. Cần quan sát tỷ lệ xác minh thất bại, bảo đảm đồng hồ máy chủ hợp lý và có kế hoạch rollback mà không phục hồi khóa đã bị lộ.\n\n### Cấu hình nào không thuộc biến môi trường\n\nBiến môi trường phù hợp với giá trị nhỏ, phẳng và thay theo deployment. Một tài liệu JSON hàng trăm KB, chứng thư nhiều dòng hoặc bảng luật giá động khó kiểm tra và dễ vướng giới hạn nền tảng; file mount, database cấu hình hay dịch vụ chuyên dụng phù hợp hơn.\n\nFeature flag đổi liên tục cũng không nên buộc restart mọi replica. Nó cần control plane, cache, giá trị mặc định khi mạng lỗi và lịch sử ai đổi. Ngược lại, các bất biến ảnh hưởng cấu trúc process như port, DSN và mức log khởi tạo rất hợp với environment.\n\n### Ca biên cần quyết định trước\n\nBiến tồn tại nhưng rỗng khác với biến không tồn tại. Danh sách origin có dấu phẩy cuối cần được chuẩn hóa. Unicode trong mật khẩu phải được driver xử lý đúng; không tự nối URL vì ký tự `@`, `:` và `/` cần percent-encoding. Timeout bằng 0 hoặc âm phải bị từ chối. Một URL hợp lệ về cú pháp vẫn có thể trỏ nhầm production, nên môi trường nhạy cảm có thể kiểm tra tên database hoặc chứng thư máy chủ.\n\nKhi nhiều worker khởi động đồng thời, tất cả cùng kiểm tra database có thể tạo “bão kết nối”. Pool nhỏ lúc boot, retry có backoff và jitter, cùng giới hạn deadline toàn cục sẽ tránh biến một lần restart thành sự cố thứ hai.\n\n### Phân loại trước khi chọn chỗ lưu\n\nKhông phải mọi cấu hình đều cần che giấu. `LOG_LEVEL=INFO`, tên vùng triển khai hoặc URL công khai của frontend có thể được commit nếu không chứa thông tin nhạy cảm. Ngược lại, mật khẩu, bearer token, private key và chuỗi kết nối kèm credential phải được coi là secret. Phân loại đúng tránh hai cực: để lộ giá trị nguy hiểm, hoặc bọc mọi thứ trong bí mật đến mức không thể review cấu hình.\n\nVới Cổng thanh toán lớp học, ta lập inventory (danh mục) có chủ sở hữu và mục đích: `DATABASE_URL` thuộc nhóm nền tảng dữ liệu; `JWT_SIGNING_KEY` thuộc nhóm xác thực; `PAYMENT_API_KEY` thuộc tích hợp thanh toán. Mỗi mục cần biết môi trường nào dùng, dịch vụ nào đọc, quyền cụ thể, ngày tạo và cách thu hồi. Inventory không chứa bản rõ; nó là bản đồ để biết cần xoay gì khi một runner CI hay máy lập trình bị xâm nhập.\n\nMột cách thử nhanh: nếu công khai giá trị mà kẻ khác vẫn không tăng được quyền hay đọc dữ liệu, nó có lẽ chỉ là cấu hình. Tuy nhiên URL nội bộ hoặc tên tài khoản vẫn có thể là thông tin nhạy cảm phục vụ trinh sát; mức phân loại phải theo threat model, không theo phần mở rộng file.\n\n### `.gitignore` chặn đường vào mới, không sửa quá khứ\n\nĐặt `.env`, `.env.*` vào `.gitignore` và chỉ cho phép `.env.example` không có giá trị thật. File mẫu nên mô tả tên biến cùng placeholder vô hại:\n\n```dotenv\nAPP_ENV=development\nDATABASE_URL=postgresql://USER:PASSWORD@HOST/DB\nJWT_SIGNING_KEY=replace-with-random-development-key\nPAYMENT_API_KEY=replace-with-sandbox-key\n```\n\n`git status --short` trước commit giúp nhận ra file lạ; secret scanner ở pre-commit và CI là lớp phòng thủ tiếp theo. Scanner dựa trên mẫu và entropy có false positive lẫn false negative: nó bắt được nhiều token có hình dạng quen thuộc, nhưng không thể chứng minh repository sạch. Review diff vẫn cần thiết.\n\nĐiểm dễ hiểu sai là `.gitignore` chỉ tác động tới file chưa được Git theo dõi. Nếu `.env` đã từng commit, thêm ignore không xóa blob khỏi commit cũ, fork, clone của đồng đội, cache CI hay bản sao trên dịch vụ lưu trữ. `git rm --cached` chỉ ngừng theo dõi ở commit mới. Việc viết lại lịch sử có thể giảm khả năng tìm thấy nhưng không thể thu hồi những bản sao đã rời khỏi tầm kiểm soát. Bởi vậy phản ứng ưu tiên là revoke hoặc rotate, không phải vội force-push một lịch sử “trông sạch”.\n\n### Nạp cấu hình FastAPI mà không rò bản rõ\n\nProcess FastAPI cần nhận cấu hình một lần khi khởi động và fail closed (từ chối chạy khi thiếu điều kiện an toàn). Pydantic Settings phù hợp vì gom việc ánh xạ biến môi trường, kiểm kiểu và validation vào một ranh giới rõ. File `.env` chỉ được bật có chủ đích cho local; production không nên âm thầm phụ thuộc một file nằm trong image.\n\n```python\nfrom functools import lru_cache\nfrom typing import Literal\n\nfrom pydantic import Field, SecretStr, model_validator\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    app_env: Literal[\"development\", \"test\", \"production\"] = \"development\"\n    database_url: SecretStr\n    jwt_signing_key: SecretStr = Field(min_length=32)\n    payment_api_key: SecretStr\n\n    model_config = SettingsConfigDict(\n        env_file=\".env\",\n        env_file_encoding=\"utf-8\",\n        extra=\"ignore\",\n    )\n\n    @model_validator(mode=\"after\")\n    def reject_placeholders_in_production(self):\n        if self.app_env == \"production\":\n            values = (\n                self.database_url.get_secret_value(),\n                self.jwt_signing_key.get_secret_value(),\n                self.payment_api_key.get_secret_value(),\n            )\n            if any(\"replace-with\" in value for value in values):\n                raise ValueError(\"production secret đang dùng placeholder\")\n        return self\n\n@lru_cache\ndef get_settings() -> Settings:\n    return Settings()\n```\n\n`SecretStr` làm `repr` và JSON thông thường che giá trị, giảm nguy cơ log vô tình; nó không mã hóa bộ nhớ và code vẫn có thể gọi `get_secret_value()`. `lru_cache` tránh đọc lại cấu hình mỗi request, đồng thời có hệ quả: secret thay đổi ở nguồn không tự xuất hiện trong process đang chạy. Chọn nạp lúc khởi động nghĩa là rotation thường cần rolling restart; đó là contract vận hành phải ghi rõ.\n\nTrong production, có thể đặt `env_file=None` theo cấu hình riêng hoặc bảo đảm file không có trong artifact. Quan trọng hơn, không trả chi tiết validation chứa dữ liệu nhạy cảm qua endpoint. Startup log chỉ nên nói tên biến thiếu, môi trường và mã lỗi; tuyệt đối không in toàn bộ `settings.model_dump()`.\n\n### Đường đi từ secret store đến workload\n\nMột secret manager cung cấp mã hóa khi lưu, kiểm soát truy cập, version, audit và đôi khi sinh credential ngắn hạn. Nó không loại bỏ secret khỏi hệ thống; nó thu hẹp và quan sát đường cấp phát. Luồng lý tưởng là:\n\n```text\nDanh tính workload → xin quyền đọc secret phiên bản hiện hành\n                    → secret store kiểm policy và ghi audit\n                    → nền tảng đưa giá trị vào process hoặc file RAM tạm thời\n                    → FastAPI đọc lúc khởi động\n                    → client dùng credential cho đúng dependency\n```\n\nBootstrap problem (bài toán khởi tạo tin cậy) là câu hỏi “ứng dụng dùng credential nào để vào secret store?”. Không nên giải bằng một API key dài hạn khác đóng trong image. Nền tảng container hoặc cloud thường cấp workload identity (danh tính workload) dựa trên service account và token ngắn hạn; secret store xác thực danh tính ấy rồi áp policy. Nhờ vậy repository chỉ chứa tên hoặc đường dẫn secret, không chứa chìa khóa mở kho.\n\nEnvironment variable đơn giản và được thư viện hỗ trợ rộng, nhưng process con kế thừa nó; công cụ debug, crash dump hoặc giao diện quản trị sai quyền có thể nhìn thấy. File được mount cho phép kiểm quyền file và cập nhật atomically, song ứng dụng phải xử lý thời điểm file đổi. Sidecar hoặc SDK gọi secret manager lúc chạy hỗ trợ refresh tốt hơn nhưng thêm dependency mạng, timeout, cache và chi phí vận hành. Không có kênh truyền nào “an toàn tuyệt đối”; lựa chọn phụ thuộc khả năng nền tảng và mô hình rotation.\n\n### Least privilege và tách môi trường\n\nCredential production không được dùng ở laptop. Dev dùng database giả hoặc tài khoản chỉ có dữ liệu thử; staging có project và khóa riêng; production có danh tính riêng cho từng service. Tách môi trường ngăn lỗi cấu hình local trở thành quyền truy cập production, đồng thời cho phép thu hồi một lớp mà không làm sập lớp khác.\n\nTrong cùng production, service thanh toán chỉ cần đọc secret của nó, không cần liệt kê toàn bộ kho. Tài khoản database cho API có thể chỉ được thao tác schema ứng dụng, không có quyền tạo superuser hay đọc bảng ngoài phạm vi. Payment key nên bị giới hạn thao tác và tài khoản nếu nhà cung cấp hỗ trợ. Quyền nhỏ biến “secret bị lộ” từ thảm họa toàn hệ thống thành sự cố có ranh giới.\n\nĐừng dùng chung một JWT signing key giữa nhiều ứng dụng nếu chúng không cùng trust boundary (ranh giới tin cậy). Khóa dùng chung khiến bất kỳ ứng dụng nào có quyền ký đều có thể phát token được ứng dụng khác tin. Tách issuer, audience và key giúp authorization không vô tình mở rộng theo kiến trúc tổ chức.\n\n### Rotation không downtime cần hai phía phối hợp\n\nThay secret không phải ghi đè một chuỗi. Trong cụm nhiều replica, có khoảng thời gian process cũ và mới chạy đồng thời. Với password database, quy trình an toàn thường là tạo credential B, cấp cùng quyền tối thiểu, cập nhật secret store sang B, rolling restart và theo dõi kết nối, sau đó vô hiệu hóa A. Nếu database chỉ cho một password trên cùng user, có thể tạo user thứ hai để đạt giai đoạn chồng lấn.\n\nKhóa ký JWT khó hơn vì token cũ còn sống. Dùng cặp khóa bất đối xứng và `kid` (key identifier, định danh khóa) trong header: signer mới phát token bằng private key B, verifier tạm chấp nhận public key A và B. Chờ tối đa bằng thời hạn access token cộng độ lệch đồng hồ hợp lý rồi bỏ A. Nếu xóa A ngay, mọi token chưa hết hạn trở thành lỗi đăng nhập hàng loạt; nếu giữ A vô thời hạn, rotation không thu hồi được khóa đã lộ.\n\nAPI key bên thứ ba có thể hỗ trợ hai key hoạt động song song hoặc không. Khi không có overlap, cần cửa sổ bảo trì, retry có kiểm soát hoặc phối hợp phía nhà cung cấp. Không nên retry vô hạn một yêu cầu thanh toán sau lỗi authentication: retry không sửa credential sai và có thể tạo tải hoặc giao dịch trùng nếu semantics của endpoint không idempotent.\n\n### Quan sát hệ thống mà không quan sát secret\n\nTa cần biết secret nào đang được dùng mà không ghi bản rõ. Log nên chứa `secret_name`, version hoặc fingerprint không đảo ngược đã được duyệt, workload identity, kết quả cấp quyền và correlation ID. Không log Authorization header, DSN đầy đủ, request body tùy tiện hay exception object từ SDK nếu chưa biết nó có phản chiếu credential.\n\nMetric hữu ích gồm số lỗi lấy secret, lỗi xác thực dependency sau deployment, tuổi của phiên bản đang dùng, tỷ lệ replica đã chuyển sang version mới và lần truy cập bất thường theo danh tính. Health check không nên trả “database password đúng”; nó chỉ trả trạng thái dependency ở mức tối thiểu và đặt endpoint sau kiểm soát truy cập phù hợp.\n\nRedaction theo tên trường chưa đủ vì secret có thể nằm trong URL, header hoặc chuỗi exception. Nên thiết kế logging theo allowlist: chỉ ghi các trường đã biết là an toàn. Trong FastAPI, middleware ghi toàn bộ header để debug là một bẫy lớn; `Authorization`, cookie và các header key tùy chỉnh phải bị loại ngay tại nguồn.\n\n### Khi phát hiện secret đã lộ\n\nĐầu tiên xác định credential và hệ thống chấp nhận nó, rồi revoke hoặc rotate càng sớm càng tốt. Nếu nghi phạm vẫn còn quyền ghi repository hoặc CI, chỉ tạo key mới có thể khiến key mới lộ tiếp; cần khóa đường xâm nhập song song. Tiếp theo tìm dấu hiệu sử dụng trái phép trong audit log của secret store, database, nhà cung cấp và hệ thống định danh; phạm vi thời gian bắt đầu trước commit hoặc log đầu tiên đã biết vì bản rò có thể xuất hiện sớm hơn lúc phát hiện.\n\nSau khi quyền cũ hết hiệu lực mới xử lý phân phối bản mới, restart có kiểm soát và xác nhận mọi replica hoạt động. Làm sạch lịch sử Git là quyết định riêng: hữu ích để giảm phát tán tình cờ và đáp ứng quy trình, nhưng gây thay đổi commit hash, ảnh hưởng clone và nhánh mở. Nó không thay thế rotation. Cuối cùng ghi lại nguyên nhân: file đã được stage ra sao, scanner vì sao không chặn, quyền có quá rộng không, và thời gian phát hiện dài ở đâu.\n\nCa biên cần diễn tập gồm secret rỗng, có newline cuối file, ký tự Unicode, placeholder còn sót, secret store timeout, quyền bị thu hồi giữa lúc deploy, replica dùng hai version, đồng hồ lệch khi xác minh token và rotation khi dependency chỉ cho một credential. Một quy trình chỉ chạy ở happy path chưa phải quy trình phục hồi.\n\n### Khi nào không nên dùng `.env`\n\nKhông dùng `.env` làm nguồn sự thật production, không đóng nó vào Docker image, không gửi qua chat và không dùng một file chung cho cả nhóm. Quyền file `600` có ích trên máy Unix nhưng không giải quyết bản sao lưu, malware trong cùng tài khoản hay lịch sử đồng bộ đám mây. Mã hóa `.env` để commit chỉ hợp lý khi tổ chức có công cụ quản lý người nhận, khóa giải mã, rotation và review rõ ràng; nếu không, nó tạo cảm giác an toàn giả.\n\nNgược lại, không nhất thiết dựng secret manager phức tạp cho một bài tập local không có dữ liệu thật. `.env` bị ignore, dùng credential sandbox giới hạn quyền, `.env.example`, validation khởi động và kiểm tra trước commit có thể là lựa chọn cân đối. Khi hệ thống có nhiều môi trường, nhiều người, audit bắt buộc hoặc rotation thường xuyên, chi phí của secret manager mới được hoàn lại bằng giảm thao tác tay và blast radius.",

vi_du: "### Chạy Bàn Trống ở máy phát triển\n\nTạo `.env` cục bộ, không commit:\n\n```dotenv\nDATABASE_URL=postgresql+psycopg://booking:local-only@localhost:5432/booking\nBOOKING_SIGNING_KEY=development-key-at-least-32-characters\nDB_CONNECT_TIMEOUT_SECONDS=2.5\nTRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173\nLOG_LEVEL=DEBUG\n```\n\nChạy kiểm tra độc lập trước khi khởi động server:\n\n```python\nif __name__ == \"__main__\":\n    try:\n        settings = Settings()\n    except Exception as exc:\n        raise SystemExit(f\"Cấu hình không hợp lệ: {exc}\") from exc\n    print({\n        \"log_level\": settings.log_level,\n        \"origins\": [str(x) for x in settings.trusted_origins],\n        \"timeout\": settings.db_connect_timeout_seconds,\n    })\n```\n\nThử xóa `BOOKING_SIGNING_KEY`, đặt timeout thành `-1`, rồi đặt origin thành chuỗi rỗng. Hai ca đầu phải dừng với lỗi validation; ca cuối phải thành danh sách rỗng. Đó là kiểm thử hành vi, không cần kết nối dịch vụ bên ngoài.\n\n### Test không làm nhiễm ca kế tiếp\n\n```python\ndef test_missing_signing_key(monkeypatch):\n    monkeypatch.setenv(\"DATABASE_URL\", \"postgresql://u:p@db/test\")\n    monkeypatch.delenv(\"BOOKING_SIGNING_KEY\", raising=False)\n    get_settings.cache_clear()\n    try:\n        Settings(_env_file=None)\n        assert False, \"phải từ chối cấu hình thiếu khóa\"\n    except Exception as exc:\n        assert \"BOOKING_SIGNING_KEY\" in str(exc)\n    finally:\n        get_settings.cache_clear()\n```\n\n`_env_file=None` làm test không vô tình đọc `.env` của máy người chạy. `finally` dọn cache kể cả assertion thất bại. Trong test suite thật, nên dùng `pytest.raises` và fixture để việc dọn dẹp nhất quán.\n\n### Ứng dụng FastAPI có thể chạy và kiểm thử\n\nVí dụ sau giữ endpoint thanh toán nhỏ nhưng thể hiện bốn nguyên tắc: cấu hình được kiểm tra trước khi phục vụ, secret bị che trong biểu diễn, dependency nhận settings qua injection, và response không chứa credential.\n\n```python\n# app.py\nfrom functools import lru_cache\nfrom typing import Annotated, Literal\n\nfrom fastapi import Depends, FastAPI, HTTPException\nfrom pydantic import BaseModel, Field, SecretStr\nfrom pydantic_settings import BaseSettings, SettingsConfigDict\n\nclass Settings(BaseSettings):\n    app_env: Literal[\"development\", \"test\", \"production\"] = \"development\"\n    payment_api_key: SecretStr = Field(min_length=12)\n    jwt_signing_key: SecretStr = Field(min_length=32)\n    model_config = SettingsConfigDict(env_file=\".env\", extra=\"ignore\")\n\n@lru_cache\ndef get_settings() -> Settings:\n    return Settings()\n\nclass PaymentIn(BaseModel):\n    student_id: int = Field(gt=0)\n    amount: int = Field(gt=0, le=10_000_000)\n\napp = FastAPI()\n\n@app.on_event(\"startup\")\ndef validate_settings() -> None:\n    get_settings()  # thiếu hoặc sai kiểu thì process không nhận traffic\n\n@app.post(\"/payments\")\ndef create_payment(\n    body: PaymentIn,\n    settings: Annotated[Settings, Depends(get_settings)],\n):\n    key = settings.payment_api_key.get_secret_value()\n    if not key.startswith(\"pay_\"):\n        # Chỉ mô tả loại lỗi; không nối key vào thông báo.\n        raise HTTPException(status_code=503, detail=\"payment provider chưa sẵn sàng\")\n    return {\"student_id\": body.student_id, \"amount\": body.amount, \"status\": \"accepted\"}\n```\n\nCài `fastapi`, `uvicorn`, `pydantic-settings`, rồi chạy trong PowerShell mà không tạo file chứa secret:\n\n```powershell\n$env:PAYMENT_API_KEY = \"pay_local_sandbox_123\"\n$env:JWT_SIGNING_KEY = \"local-only-key-with-at-least-32-characters\"\nuvicorn app:app --reload\n```\n\nGọi `POST /payments` với JSON `{\"student_id\": 7, \"amount\": 150000}` sẽ trả trạng thái `accepted`. Đầu vào `amount=0`, số âm hoặc vượt 10.000.000 bị Pydantic từ chối trước khi gọi nhà cung cấp. Xóa `JWT_SIGNING_KEY` rồi khởi động lại phải làm startup thất bại; đây là hành vi an toàn hơn việc chạy với khóa mặc định.\n\nTest sau chứng minh hai đặc tính mà không ghi giá trị thật vào assertion failure:\n\n```python\n# test_app.py\nfrom fastapi.testclient import TestClient\nfrom app import Settings, app, get_settings\n\ndef test_payment_response_never_contains_key():\n    secret = \"pay_test_never_return_this\"\n    app.dependency_overrides[get_settings] = lambda: Settings(\n        payment_api_key=secret,\n        jwt_signing_key=\"x\" * 32,\n    )\n    try:\n        response = TestClient(app).post(\n            \"/payments\", json={\"student_id\": 7, \"amount\": 150000}\n        )\n        assert response.status_code == 200\n        assert secret not in response.text\n    finally:\n        app.dependency_overrides.clear()\n\ndef test_invalid_key_returns_generic_error():\n    app.dependency_overrides[get_settings] = lambda: Settings(\n        payment_api_key=\"invalid_value\",\n        jwt_signing_key=\"x\" * 32,\n    )\n    try:\n        response = TestClient(app).post(\n            \"/payments\", json={\"student_id\": 7, \"amount\": 150000}\n        )\n        assert response.status_code == 503\n        assert \"invalid_value\" not in response.text\n    finally:\n        app.dependency_overrides.clear()\n```\n\nTest này không chứng minh log, crash dump và hạ tầng đều sạch; nó chỉ bảo vệ contract HTTP. Cần thêm test cho logging filter và kiểm tra artifact container. Khi dùng `--reload`, process con được tạo lại và thừa kế môi trường; đây là tiện ích development, không phải cơ chế refresh secret production.",

so_sanh: "### Chọn kênh cấu hình theo vòng đời\n\n| Cách cấp giá trị | Điểm mạnh | Giới hạn | Dùng phù hợp |\n|---|---|---|---|\n| biến môi trường | phổ biến, đơn giản, hợp container | toàn chuỗi, khó reload, có thể lộ qua quyền inspect | DSN, port, timeout, tên môi trường |\n| `.env` | dễ thiết lập máy cá nhân | secret dạng rõ trên đĩa, dễ commit nhầm | phát triển cục bộ |\n| file mount | giữ cấu trúc và chứng thư nhiều dòng | phải xử lý quyền, thay file và reload | TLS certificate, JSON cấu hình |\n| secret manager qua SDK | audit, khóa ngắn hạn, rotation tốt | thêm mạng, SDK, cache và tình huống mất dịch vụ | secret production có yêu cầu cao |\n| database/feature-flag service | đổi lúc chạy, quản trị tập trung | tạo dependency mới và vấn đề nhất quán | luật thay thường xuyên, rollout tính năng |\n\nKhông có lựa chọn thắng mọi trường hợp. Một hệ nhỏ có thể bắt đầu bằng environment do nền tảng cấp. Khi secret cần rotation không restart hoặc cấu hình lớn có cấu trúc, cố nhét tất cả vào chuỗi sẽ làm hệ thống khó vận hành hơn. Tiêu chí đúng là vòng đời, độ nhạy cảm và hành vi khi nguồn cấu hình không sẵn sàng.\n\n### So sánh các cách đưa secret vào FastAPI\n\n| Phương án | Điểm mạnh | Rủi ro và chi phí | Phù hợp |\n|---|---|---|---|\n| Hard-code trong source | Không cần cấu hình lúc chạy | Đi vào Git, image và review; muốn đổi phải build lại; gần như không thể giới hạn người đã clone | Không nên dùng cho secret, kể cả key “tạm” |\n| `.env` bị ignore | Dễ dùng local, tương thích nhiều thư viện, onboarding nhanh | Văn bản rõ; không audit, không rotation, dễ gửi nhầm hoặc được sao lưu | Phát triển cá nhân với credential sandbox |\n| Biến môi trường do nền tảng cấp | Đơn giản, không cần SDK trong ứng dụng | Có thể lộ qua debug/process; refresh thường cần restart; giới hạn kích thước và cấu trúc | Dịch vụ nhỏ, nền tảng đã bảo vệ cấu hình |\n| File secret được mount | Có thể phân quyền file và thay phiên bản atomically | Code phải đọc lại đúng cách; backup hoặc sidecar sai quyền vẫn làm lộ | Container orchestration, thư viện hỗ trợ đọc file |\n| Secret manager qua SDK/agent | Policy chi tiết, audit, version, credential ngắn hạn và rotation | Thêm latency, cache, timeout, chi phí, lock-in và bootstrap identity | Production nhiều dịch vụ hoặc yêu cầu kiểm toán |\n\nKhông có hàng nào tự động bảo mật chỉ vì tên công nghệ. Secret manager với policy `read all` và token quản trị dài hạn trong image còn tệ hơn biến môi trường được nền tảng cấp đúng quyền. Ngược lại, `.env` local chứa key sandbox chỉ đọc, hết hạn nhanh có thể có rủi ro chấp nhận được.\n\n### Secret tĩnh và credential ngắn hạn\n\n| Thuộc tính | Secret tĩnh dài hạn | Credential ngắn hạn được cấp động |\n|---|---|---|\n| Khi bị sao chép | Hữu dụng đến lúc bị phát hiện và thu hồi | Tự hết hạn sau cửa sổ ngắn |\n| Vận hành | Dễ tích hợp, ít thành phần | Cần identity, clock, cơ chế gia hạn và xử lý outage |\n| Rotation | Thường là sự kiện thủ công dễ gây downtime | Là hoạt động thường xuyên được thiết kế sẵn |\n| Điều tra | Khó gắn bản sao với workload | Có thể gắn lần cấp với danh tính và phiên |\n| Khi không nên dùng | Hệ thống nhạy cảm, nhiều bản sao, khó thu hồi | Ứng dụng offline dài ngày hoặc dependency không hỗ trợ |\n\nCredential ngắn hạn giảm thời gian khai thác nhưng không chữa quyền quá rộng: token sống năm phút mà có quyền xóa toàn bộ database vẫn nguy hiểm trong năm phút đó. Thiết kế tốt kết hợp thời hạn ngắn, audience hẹp, quyền tối thiểu, kênh cấp đáng tin và audit. Với Cổng thanh toán lớp học, workload identity vào database tốt hơn password chung nếu nền tảng hỗ trợ; API key tĩnh của nhà cung cấp vẫn phải được bọc bằng quy trình rotation vì ta không thể đơn phương đổi giao thức của họ.",

loi_thuong_gap: [
  "Dùng `bool(os.getenv(\"DEBUG\"))`: chuỗi `\"false\"` vẫn thành `True`; nhận biết qua việc production bật debug dù biến ghi false, sửa bằng parser kiểu nghiêm ngặt.",
  "Cho signing key một giá trị mặc định: nhiều replica tự sinh khóa khác nhau hoặc production dùng khóa mẫu; nhận biết qua token lúc được lúc không, sửa bằng bắt buộc fail-fast.",
  "Log toàn bộ `os.environ` khi điều tra lỗi: mật khẩu xuất hiện trong log aggregator; nhận biết bằng quét tên biến nhạy cảm và thu hồi khóa ngay nếu đã lộ.",
  "Cache settings nhưng test quên `cache_clear()`: ca sau dùng cấu hình ca trước; nhận biết khi test pass riêng nhưng fail theo bộ.",
  "Nối thủ công URL database từ user và password: ký tự đặc biệt phá cú pháp; nhận biết qua lỗi xác thực dù credential đúng, sửa bằng builder hoặc percent-encoding chuẩn.",
  "Tin rằng cập nhật secret ở control plane tự đổi process đang chạy: replica tiếp tục dùng giá trị cũ; nhận biết bằng mã `kid` hoặc phiên bản cấu hình trong telemetry, sửa bằng rollout/reload có chủ đích.",
  "Đưa `.env` thật vào image hoặc Git: secret tồn tại trong layer/lịch sử dù file đã xóa; nhận biết bằng quét lịch sử và image, phải thu hồi secret chứ không chỉ xóa file.",
  "**Thêm `.env` vào `.gitignore` sau khi đã commit rồi cho rằng đã an toàn.** Dấu hiệu: `git log --all -- .env` hoặc scanner lịch sử vẫn tìm thấy blob. Cách xử lý đầu tiên là thu hồi credential, không phải chỉ xóa file ở HEAD."
],

tu_kiem_tra: [
  "Vì sao đọc cấu hình một lần lúc boot thường dễ suy luận và dễ test hơn gọi `os.getenv()` trong từng route?",
  "Tại sao `SecretStr` giảm lộ do in nhầm nhưng không biến environment thành nơi lưu trữ được mã hóa?",
  "Trong Bàn Trống, dependency nào nên làm readiness thất bại: database chính, email hay analytics? Hãy bảo vệ lựa chọn bằng khả năng phục vụ đúng.",
  "Thiết kế quá trình đổi signing key để rolling deployment vẫn xác minh được token phát trước đó.",
  "Phân biệt biến không tồn tại, biến rỗng và biến có chuỗi `\"null\"`; schema của bạn xử lý từng trường hợp ra sao?",
  "Nếu 40 worker restart cùng lúc khi database vừa hồi phục, retry cố định gây hiện tượng gì và backoff có jitter giúp ở đâu?",
  "Khi nào feature flag không nên là biến môi trường dù về kỹ thuật vẫn có thể biểu diễn bằng chuỗi?",
  "Vì sao mã hóa `.env` rồi đặt khóa giải mã trong cùng repository không giảm đáng kể rủi ro? Hãy chỉ ra chính xác trust boundary không thay đổi."
],

tai_nguyen: [
  "The Twelve-Factor App — mục Config.",
  "FastAPI Documentation — Settings and Environment Variables.",
  "Pydantic Documentation — Settings Management.",
  "OWASP Secrets Management Cheat Sheet.",
  "OWASP Secrets Management Cheat Sheet — hướng dẫn vòng đời, rotation, auditing và xử lý secret trong CI/CD."
],

lien_he: [
  "Nối sang mảng Hệ điều hành: môi trường thuộc về process và được kế thừa khi tạo process con, giải thích vì sao process đang chạy không tự nhận giá trị mới.",
  "Nối sang mảng Bảo mật web: quản lý secret, nguyên tắc quyền tối thiểu và rotation quyết định hậu quả khi credential bị lộ.",
  "Nối sang mảng DevOps: container, readiness probe và rolling deployment biến hợp đồng settings thành hành vi triển khai cụ thể.",
  "Nối sang mảng Kiểm thử: dependency override và cô lập environment giúp test tái lập, không phụ thuộc máy người chạy.",
  "Mảng 03 Git & GitHub: hiểu staging, commit, `.gitignore` và lịch sử giải thích vì sao xóa file ở commit mới không xóa các blob cũ hay bản clone đã phát tán."
]

};
