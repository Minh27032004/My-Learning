window.LESSONS["02:cay:heap-va-priority-queue"] = {

tom_tat: [
  "**Heap = cây nhị phân (bài 1) + HAI ràng buộc khác hẳn BST**: bắt buộc **hoàn chỉnh** (complete — không được chọn, phải luôn đúng vậy) và **thứ tự cha-con** (min-heap: mọi cha ≤ mọi con; max-heap: mọi cha ≥ mọi con). **KHÔNG có** ràng buộc trái-nhỏ-phải-lớn như BST — đây là lỗi hiểu lầm phổ biến nhất của bài.",
  "**Vì LUÔN hoàn chỉnh, heap là cấu trúc DUY NHẤT trong cả module Cây biểu diễn hiệu quả bằng MẢNG** — dùng lại chính công thức `2i+1`/`2i+2`/`(i-1)//2` mà bài Cây nhị phân đã suy ra, và bài đó đã đo: mảng chỉ hiệu quả khi cây hoàn chỉnh, nếu không sẽ lãng phí theo cấp số nhân. Heap là ca DÙNG ĐƯỢC.",
  "**Đây là lời trả nợ trực tiếp cho bài Queue** (module Cấu trúc dữ liệu tuyến tính): \"hàng đợi ưu tiên KHÔNG PHẢI queue, nó cài bằng heap\" — bài này cài heap thật, chứng minh vì sao nó cho lấy min/max nhanh hơn hẳn quét tuyến tính hay giữ mảng luôn sắp.",
  "**Xây một heap từ mảng bất kỳ tốn O(n), KHÔNG PHẢI O(n log n)** — đây là điểm dễ đoán sai nhất bài (trực giác \"n lần sift-down, mỗi lần O(log n)\" cho ra O(n log n), nhưng SAI vì hầu hết nút nằm gần lá, sift-down ở đó rất ngắn). Bài chứng minh chặt bằng tổng cấp số, không chỉ khẳng định suông.",
  "**`heapq` chuẩn của Python chỉ cài MIN-HEAP** — cách giả lập max-heap là đảo dấu giá trị trước khi đẩy vào, đảo lại khi lấy ra. Đây là mẹo thực dụng dùng liên tục khi giải bài tập, đáng nhớ kỹ."
],

can_biet_truoc: [
  "Bài **Cây nhị phân, đầy đủ / hoàn chỉnh** — đặc biệt tính chất **hoàn chỉnh** và **biểu diễn mảng** (công thức chỉ số cha/con, và phép đo lãng phí bộ nhớ khi cây KHÔNG hoàn chỉnh). Bài này là nơi công thức đó thật sự được DÙNG, không chỉ học lý thuyết.",
  "Bài **Queue FIFO và circular queue**, module Cấu trúc dữ liệu tuyến tính — câu đã hứa \"hàng đợi ưu tiên không phải queue, cài bằng heap\", bài này trả nợ trực tiếp.",
  "Bài **Cây cân bằng: AVL, Red-Black**, ngay trước — để thấy rõ đối lập: AVL trả giá cân bằng CHẶT (nhiều phép xoay) để hỗ trợ TÌM một giá trị bất kỳ; heap chỉ cần lấy min/max, đổi lấy cài đặt đơn giản hơn nhiều.",
  "Bài **Đếm tần suất và bài toán Two Sum**, module Hash — `Counter.most_common(k)` đã dùng heap ngầm bên trong (`heapq.nlargest`), bài này mở nắp cơ chế đó."
],

dinh_nghia: "**Bất biến heap** — hai điều kiện, tách bạch rõ ràng, không được nhầm với nhau:\n\n```\nĐIỀU KIỆN 1 — HÌNH DẠNG: cây phải HOÀN CHỈNH (bài Cây nhị phân đã định nghĩa)\n  ⇒ BẮT BUỘC, không phải lựa chọn — mọi thao tác chèn/xoá heap PHẢI\n    duy trì tính hoàn chỉnh, không có ngoại lệ.\n\nĐIỀU KIỆN 2 — THỨ TỰ: tại MỌI nút X có con C\n  MIN-HEAP:  gia_tri(X)  ≤  gia_tri(C)      (cha KHÔNG LỚN HƠN con)\n  MAX-HEAP:  gia_tri(X)  ≥  gia_tri(C)      (cha KHÔNG NHỎ HƠN con)\n\n⚠️ CHỈ so cha với CON TRỰC TIẾP — KHÔNG so hai cây con trái/phải với\n  nhau, KHÔNG có khái niệm \"trái nhỏ phải lớn\" như BST.\n```\n\nVí dụ một min-heap hợp lệ (7 phần tử, biểu diễn cả hai dạng):\n\n```\nDạng cây:                          Dạng mảng (theo thứ tự level-order):\n         2\n       ╱   ╲                       chỉ số:  0  1  2  3  4  5  6\n      5     3                      giá trị: 2  5  3  8  9  7  6\n     ╱ ╲   ╱ ╲\n    8   9 7   6\n\nKiểm CHỈ cha-con:\n  2 ≤ 5 ✓   2 ≤ 3 ✓   5 ≤ 8 ✓   5 ≤ 9 ✓   3 ≤ 7 ✓   3 ≤ 6 ✓\n\n⚠️ Chú ý: 8 và 9 không có quan hệ gì với nhau theo bất biến heap\n  (không phải anh em phải so sánh) — 9 thậm chí LỚN HƠN 3 dù 3 ở\n  \"bên phải xa hơn\". Đây là điểm khác BST RÕ RỆT NHẤT: BST đòi CẢ\n  CÂY CON phải thoả thứ tự so với gốc; heap CHỈ đòi cha-con TRỰC TIẾP.\n```\n\nVì bất biến heap YẾU HƠN BST (chỉ ràng buộc cục bộ, không toàn cục), heap **KHÔNG hỗ trợ tìm kiếm một giá trị bất kỳ hiệu quả** (không có \"đi trái hay phải\" để loại nửa cây như BST) — heap CHỈ giỏi đúng một việc: **lấy ra phần tử nhỏ nhất (min-heap) hoặc lớn nhất (max-heap) ngay lập tức**, và đó chính là tất cả những gì một **hàng đợi ưu tiên (priority queue)** cần.\n\n**Hàng đợi ưu tiên** là một kiểu dữ liệu trừu tượng (không phải cấu trúc cụ thể) chỉ có ba thao tác: thêm phần tử, xem/lấy phần tử ưu tiên cao nhất, và kiểm tra rỗng. Nó KHÔNG có tính chất FIFO của queue thường (bài Queue đã nói) — thứ tự LẤY RA phụ thuộc ĐỘ ƯU TIÊN, không phụ thuộc thứ tự ĐƯA VÀO. Heap là cách cài đặt hàng đợi ưu tiên hiệu quả nhất được biết tới.",

vi_sao_can: "Bài Queue (module trước) đã đóng lại bằng một câu chưa giải thích: *\"hàng đợi ưu tiên không phải queue, cài bằng heap\"*. Bài này là nơi câu đó được cài đặt thật và chứng minh vì sao heap là lựa chọn ĐÚNG, so với hai cách \"hiển nhiên\" khác:\n\n```\nBA CÁCH CÀI HÀNG ĐỢI ƯU TIÊN — so sánh nhanh trước khi vào chi tiết:\n\n① Mảng KHÔNG sắp, quét tuyến tính khi cần lấy min/max\n   thêm: O(1)      lấy min/max: O(n)     ⚠️ chậm khi lấy thường xuyên\n\n② Mảng LUÔN GIỮ SẮP (chèn đúng vị trí)\n   thêm: O(n)      lấy min/max: O(1)     ⚠️ chậm khi thêm thường xuyên\n\n③ HEAP\n   thêm: O(log n)  lấy min/max: O(log n) ⭐ CẢ HAI đều nhanh, cân bằng\n```\n\nĐây là lý do heap tồn tại: nó không phải \"nhanh nhất\" ở bất kỳ MỘT thao tác nào (cách ① thắng ở thêm, cách ② thắng ở lấy) — nó là lựa chọn ĐỀU TAY khi CẢ HAI thao tác đều xảy ra thường xuyên, điều đúng trong hầu hết ứng dụng thực tế (lịch trình tiến trình hệ điều hành — bài Hệ điều hành module 1 đã nhắc quét CPU chọn tiến trình ưu tiên cao nhất; thuật toán Dijkstra tìm đường ngắn nhất — module Đồ thị sắp tới; nén Huffman — module Kỹ thuật giải thuật cốt lõi).\n\nLý do thứ hai bài này quan trọng: nó là ví dụ RÕ NHẤT trong cả module Cây cho thấy **hạ thấp yêu cầu (chỉ cần min/max, không cần tìm bất kỳ) đổi lấy cài đặt đơn giản hơn hẳn** (mảng phẳng, không con trỏ, không phép xoay như AVL). Đây là mẫu tư duy kỹ thuật đáng nhớ: đừng dùng công cụ mạnh hơn mức cần thiết — BST/AVL \"mạnh hơn\" heap (hỗ trợ nhiều thao tác hơn) nhưng ĐẮT hơn để cài đặt và duy trì; nếu bài toán chỉ cần min/max, heap là lựa chọn rẻ hơn, không phải kém hơn.\n\nCuối cùng, chứng minh \"xây heap từ mảng là O(n)\" đáng học kỹ vì nó là một trong số ít chỗ trong khoa học máy tính mà **trực giác đầu tiên (O(n log n)) SAI**, và việc SỬA trực giác đó cần đúng kỹ thuật đã luyện ở module 1 (tổng cấp số) — một dịp tốt để ôn lại kỹ thuật đó trong ngữ cảnh mới.",

co_che: "### Sift-up (đẩy lên) — dùng khi CHÈN một phần tử mới\n\n```\nChèn LUÔN đặt phần tử mới vào Ô TRỐNG TIẾP THEO theo thứ tự mảng\n(giữ tính HOÀN CHỈNH tự động — đây là lý do dùng mảng: \"ô trống tiếp\ntheo\" luôn CHÍNH LÀ cuối mảng, không cần tìm).\n\nSau đó, phần tử mới có thể VI PHẠM bất biến thứ tự với CHA của nó\n⇒ \"nổi\" (sift up) bằng cách đổi chỗ với cha, LẶP LẠI cho tới khi\n  hết vi phạm hoặc chạm gốc.\n```\n\n```python\ndef sift_up(mang, i):\n    while i > 0:\n        cha = (i - 1) // 2\n        if mang[cha] <= mang[i]:          # ⭐ min-heap: cha ĐÃ nhỏ hơn, dừng\n            break\n        mang[cha], mang[i] = mang[i], mang[cha]\n        i = cha\n```\n\n```\n⭐ VÌ SAO CHỈ CẦN SO VỚI CHA, KHÔNG CẦN SO VỚI ANH EM: bất biến heap\n  chỉ ràng buộc cha-con — nếu phần tử mới ≥ cha, bất biến ĐÃ ĐÚNG với\n  MỌI tổ tiên khác (vì tổ tiên xa hơn ≤ cha ≤ phần tử mới, tính bắc\n  cầu), không cần kiểm thêm.\n\n⇒ Chi phí: TỐI ĐA \"chiều cao\" bước (đúng mẫu \"đi từ một nút lên gốc\"\n  — đối xứng với mẫu \"đi từ gốc xuống\" đã dùng suốt module). Vì heap\n  LUÔN hoàn chỉnh, chiều cao LUÔN là ⌊log₂ n⌋ — không có ca xấu nhất\n  suy biến như BST trần.\n```\n\n### Sift-down (chìm xuống) — dùng khi LẤY RA phần tử ưu tiên cao nhất\n\n```\nLấy min/max LUÔN lấy GỐC (nhỏ nhất trong min-heap luôn ở gốc, hệ quả\ntrực tiếp của bất biến cha ≤ con). Sau khi lấy gốc, cần một phần tử\nkhác thế vào — quy ước chuẩn: đưa phần tử CUỐI MẢNG lên gốc (giữ\nhoàn chỉnh — cắt đúng ô cuối, không tạo lỗ), rồi \"chìm\" nó xuống.\n```\n\n```python\ndef sift_down(mang, i, kich_thuoc):\n    while True:\n        trai, phai = 2 * i + 1, 2 * i + 2\n        nho_nhat = i\n        if trai < kich_thuoc and mang[trai] < mang[nho_nhat]:\n            nho_nhat = trai\n        if phai < kich_thuoc and mang[phai] < mang[nho_nhat]:\n            nho_nhat = phai\n        if nho_nhat == i:                  # ⭐ không con nào nhỏ hơn ⇒ dừng\n            break\n        mang[i], mang[nho_nhat] = mang[nho_nhat], mang[i]\n        i = nho_nhat\n```\n\n```\n⚠️ ĐIỂM DỄ SAI NHẤT: phải so với CON NHỎ HƠN TRONG HAI CON, không\n  phải con bất kỳ. Đổi chỗ với con LỚN HƠN có thể làm con NHỎ HƠN\n  bị đẩy lên vị trí VẪN vi phạm với ông (tổ tiên xa hơn) — sift-down\n  cài sai chỗ này là lỗi phổ biến nhất khi tự viết heap.\n\n⇒ Chi phí: TỐI ĐA \"chiều cao\" bước, ĐỐI XỨNG với sift-up.\n```\n\n### Xây heap từ mảng bất kỳ trong O(n) — chứng minh, không chỉ khẳng định\n\n```\nCÁCH SAI TRỰC GIÁC (nhưng dễ nghĩ tới đầu tiên): chèn từng phần tử\nmột vào heap rỗng, mỗi lần sift-up.\n  n lần chèn × O(log n) mỗi lần = O(n log n)\n\nCÁCH ĐÚNG (heapify chuẩn): coi mảng đã có sẵn TẤT CẢ phần tử, rồi\ngọi sift-down từ CUỐI mảng ngược lên đầu — chỉ cần bắt đầu từ nút\nTRONG CÙNG cuối cùng (mọi thứ sau nó đều là lá, không cần sift-down).\n```\n\n```python\ndef xay_heap(mang):\n    n = len(mang)\n    nut_trong_cuoi = n // 2 - 1              # ⭐ nút không phải lá CUỐI CÙNG\n    for i in range(nut_trong_cuoi, -1, -1):  # ⭐ ĐI NGƯỢC, từ dưới lên\n        sift_down(mang, i, n)\n    return mang\n```\n\n```\n⭐ VÌ SAO O(n), KHÔNG PHẢI O(n log n) — chứng minh bằng tổng cấp số:\n\n  Ở TẦNG GẦN LÁ NHẤT (chiều cao cục bộ h=0): có tới n/2 nút — NHƯNG\n  chúng LÀ LÁ, sift_down không làm gì (0 bước).\n\n  Ở TẦNG CAO HƠN MỘT (h=1): có n/4 nút, mỗi nút sift-down TỐI ĐA 1 bước.\n  Ở TẦNG h=2: có n/8 nút, tối đa 2 bước mỗi nút.\n  ...\n  Ở GỐC (h = log₂ n): có 1 nút, tối đa log₂ n bước.\n\n  TỔNG SỐ BƯỚC  =  Σ (n / 2^(h+1)) × h,  cộng từ h=0 tới log₂ n\n\n              =  n × Σ h / 2^(h+1)                (đặt n ra ngoài)\n\n  Chuỗi Σ h/2^(h+1) (h chạy từ 0 tới vô cùng) HỘI TỤ VỀ MỘT HẰNG SỐ\n  (đúng bằng 1) — ĐÂY LÀ MẤU CHỐT: chuỗi KHÔNG PHỤ THUỘC n, nó chỉ\n  là một con số cố định.\n\n  ⇒  TỔNG SỐ BƯỚC  ≈  n × 1  =  O(n)                              ∎\n```\n\n```\n⭐ TRỰC GIÁC SAI Ở ĐÂU: cách tính \"n lần × O(log n)\" giả định MỌI nút\n  đều tốn O(log n) bước sift-down — ĐÚNG cho nút GẦN GỐC, nhưng SAI\n  cho ĐA SỐ nút (một nửa số nút LÀ LÁ, tốn ĐÚNG 0 bước). Đa số nút\n  rẻ, thiểu số nút đắt — tổng vẫn là O(n), không phải O(n log n).\n\n⇒ Đây CHÍNH LÀ kỹ thuật \"tổng cấp số nhân/hình học\" đã dùng ở bài\n  Cây nhị phân (đếm nút tối đa mỗi tầng) — chỉ đổi chiều: ở đó CỘNG\n  DỒN SỐ NÚT, ở đây CỘNG DỒN CHI PHÍ mỗi nút, nhưng cùng kỹ thuật\n  \"tổng theo cấp số hội tụ\" để tránh kết luận vội bằng nhân đơn giản.\n```\n\n### heapq — chỉ có min-heap, và mẹo giả lập max-heap\n\n```\nThư viện chuẩn heapq của Python CHỈ cài min-heap (lấy được PHẦN TỬ\nNHỎ NHẤT nhanh). Không có \"maxheq\" riêng — lý do: hai loại đối xứng\nhoàn toàn về mặt thuật toán, thư viện chuẩn chỉ cần cung cấp MỘT,\nvà \"nhỏ nhất\" phù hợp nhiều bài toán hơn theo quy ước chung.\n\nMẸO GIẢ LẬP MAX-HEAP: đảo dấu giá trị khi đẩy vào, đảo lại khi lấy ra.\n  đẩy x vào    →  heapq.heappush(dong, -x)\n  lấy lớn nhất →  -heapq.heappop(dong)\n\n⭐ VÌ SAO ĐÚNG: \"x lớn nhất\" ⟺ \"−x nhỏ nhất\" — một phép biến đổi\n  TUYẾN TÍNH ĐƠN GIẢN, không cần viết lại thuật toán heap.\n```",

vi_du: "### Cài đặt đầy đủ: MinHeap bằng mảng, ba thao tác\n\n```python\nclass MinHeap:\n    def __init__(self):\n        self._mang = []\n\n    def __len__(self):\n        return len(self._mang)\n\n    def chen(self, gia_tri):\n        self._mang.append(gia_tri)         # ⭐ luôn thêm vào CUỐI — giữ hoàn chỉnh\n        self._sift_up(len(self._mang) - 1)\n\n    def lay_nho_nhat(self):\n        if not self._mang:\n            raise IndexError(\"lấy từ heap rỗng\")\n        nho_nhat = self._mang[0]\n        cuoi = self._mang.pop()             # ⭐ cắt phần tử CUỐI — giữ hoàn chỉnh\n        if self._mang:\n            self._mang[0] = cuoi\n            self._sift_down(0)\n        return nho_nhat\n\n    def xem_nho_nhat(self):\n        if not self._mang:\n            raise IndexError(\"xem heap rỗng\")\n        return self._mang[0]\n\n    def _sift_up(self, i):\n        while i > 0:\n            cha = (i - 1) // 2\n            if self._mang[cha] <= self._mang[i]:\n                break\n            self._mang[cha], self._mang[i] = self._mang[i], self._mang[cha]\n            i = cha\n\n    def _sift_down(self, i):\n        n = len(self._mang)\n        while True:\n            trai, phai = 2 * i + 1, 2 * i + 2\n            nho_nhat = i\n            if trai < n and self._mang[trai] < self._mang[nho_nhat]:\n                nho_nhat = trai\n            if phai < n and self._mang[phai] < self._mang[nho_nhat]:\n                nho_nhat = phai\n            if nho_nhat == i:\n                break\n            self._mang[i], self._mang[nho_nhat] = self._mang[nho_nhat], self._mang[i]\n            i = nho_nhat\n```\n\n### Kiểm chứng bất biến sau một chuỗi thao tác trộn lẫn\n\n```python\ndef kiem_bat_bien_heap(mang):\n    \"\"\"Kiểm ĐỆ QUY qua chỉ số: mọi cha phải ≤ mọi con.\"\"\"\n    n = len(mang)\n    for i in range(n):\n        trai, phai = 2 * i + 1, 2 * i + 2\n        if trai < n and mang[i] > mang[trai]:\n            return False\n        if phai < n and mang[i] > mang[phai]:\n            return False\n    return True\n\n\nh = MinHeap()\nfor gt in (5, 3, 8, 1, 9, 2, 7, 4, 6):\n    h.chen(gt)\n    assert kiem_bat_bien_heap(h._mang)           # ⭐ ĐÚNG SAU MỖI LẦN CHÈN\n\nket_qua = []\nwhile len(h):\n    ket_qua.append(h.lay_nho_nhat())\n    if len(h):\n        assert kiem_bat_bien_heap(h._mang)       # ⭐ ĐÚNG SAU MỖI LẦN LẤY RA\n\nassert ket_qua == sorted((5, 3, 8, 1, 9, 2, 7, 4, 6))    # ⭐ lấy ra theo ĐÚNG thứ tự tăng dần\nprint(\"✓ dãy lấy ra:\", ket_qua)\n```\n\n```\n✓ dãy lấy ra: [1, 2, 3, 4, 5, 6, 7, 8, 9]\n\n⭐ ĐÂY LÀ \"HEAP SORT\" — lấy lần lượt phần tử nhỏ nhất ra khỏi heap\n  cho tới hết luôn cho dãy đã sắp. Module Sắp xếp (bài sau) sẽ dùng\n  chính kỹ thuật này làm MỘT thuật toán sắp xếp, độ phức tạp O(n log n).\n```\n\n### Ca biên bắt buộc\n\n```python\n# Heap rỗng\nh_rong = MinHeap()\nassert len(h_rong) == 0\ntry:\n    h_rong.lay_nho_nhat()\n    assert False, \"phải raise\"\nexcept IndexError as e:\n    print(f\"✓ heap rỗng: {e}\")\n\n# Heap MỘT phần tử\nh_mot = MinHeap()\nh_mot.chen(42)\nassert h_mot.xem_nho_nhat() == 42\nassert h_mot.lay_nho_nhat() == 42\nassert len(h_mot) == 0\n\n# Chèn giá trị TRÙNG — heap CHO PHÉP trùng (khác BST bài trước)\nh_trung = MinHeap()\nfor gt in (5, 5, 5, 3, 3):\n    h_trung.chen(gt)\nassert len(h_trung) == 5                      # ⭐ KHÔNG loại bỏ trùng như BST\nassert h_trung.lay_nho_nhat() == 3\nassert h_trung.lay_nho_nhat() == 3\nprint(\"✓ mọi ca biên đúng như suy luận — heap CHO PHÉP giá trị trùng\")\n```\n\n### Đo thực nghiệm: xây heap O(n) so với chèn từng phần tử O(n log n)\n\nĐo bằng SỐ LẦN ĐỔI CHỖ (swap) thay vì thời gian — chính xác hơn, không phụ thuộc máy chạy:\n\n```python\ndef xay_heap_dem_swap(mang):\n    \"\"\"Giống xay_heap ở co_che, có đếm số lần đổi chỗ.\"\"\"\n    n = len(mang)\n    dem = [0]\n    def sift_down_dem(i):\n        while True:\n            trai, phai = 2 * i + 1, 2 * i + 2\n            nho_nhat = i\n            if trai < n and mang[trai] < mang[nho_nhat]:\n                nho_nhat = trai\n            if phai < n and mang[phai] < mang[nho_nhat]:\n                nho_nhat = phai\n            if nho_nhat == i:\n                break\n            mang[i], mang[nho_nhat] = mang[nho_nhat], mang[i]\n            dem[0] += 1\n            i = nho_nhat\n    for i in range(n // 2 - 1, -1, -1):\n        sift_down_dem(i)\n    return dem[0]\n\n\ndef chen_tung_phan_tu_dem_swap(gia_tri_list):\n    \"\"\"Chèn từng phần tử vào heap rỗng bằng sift-up, đếm số lần đổi chỗ.\"\"\"\n    mang = []\n    dem = [0]\n    def sift_up_dem(i):\n        while i > 0:\n            cha = (i - 1) // 2\n            if mang[cha] <= mang[i]:\n                break\n            mang[cha], mang[i] = mang[i], mang[cha]\n            dem[0] += 1\n            i = cha\n    for gt in gia_tri_list:\n        mang.append(gt)\n        sift_up_dem(len(mang) - 1)\n    return dem[0]\n\n\nprint(f\"{'n':>8} {'xây O(n)':>12} {'chèn GIẢM DẦN':>16} {'tỉ lệ':>8}\")\nfor n in (1_000, 4_000, 16_000, 64_000):\n    gia_tri = list(range(n - 1, -1, -1))     # ⭐ GIẢM DẦN — ca XẤU NHẤT để chèn:\n                                               #   mỗi phần tử mới LUÔN nhỏ nhất\n                                               #   từ trước tới giờ, phải sift-up\n                                               #   TỚI TẬN GỐC mỗi lần\n    swap_xay = xay_heap_dem_swap(gia_tri.copy())\n    swap_chen = chen_tung_phan_tu_dem_swap(gia_tri)\n\n    print(f\"{n:>8,} {swap_xay:>12,} {swap_chen:>16,} {swap_chen / swap_xay:>7.1f}×\")\n```\n\n```\nKẾT QUẢ (đếm swap là số nguyên xác định — chạy lại cho ĐÚNG cùng số,\nkhông dao động như đo thời gian):\n\n       n     xây O(n)  chèn GIẢM DẦN    tỉ lệ\n   1,000          992            7,987     8.1×\n   4,000        3,990           39,917    10.0×\n  16,000       15,988          191,631    12.0×\n  64,000       63,986          894,481    14.0×\n\n⭐ ĐỌC CỘT \"xây O(n)\": tăng GẦN ĐÚNG TUYẾN TÍNH theo n (n gấp 4 →\n  swap gấp gần đúng 4) ở CẢ BỐN mốc — khớp O(n) đã chứng minh, và\n  ĐỘC LẬP VỚI THỨ TỰ giá trị trong mảng đầu vào (bài chứng minh ở\n  `co_che` không hề giả định gì về THỨ TỰ, chỉ dùng VỊ TRÍ trong cây).\n\n⭐ ĐỌC CỘT \"tỉ lệ\": TĂNG DẦN đều đặn theo n (8,1× → 10,0× → 12,0× →\n  14,0×, mỗi lần n gấp 4 thì tỉ lệ cộng thêm ~2) — đúng chữ ký của\n  MỘT BẬC LOG: gấp 4 lần n cộng thêm log₂4 = 2 vào \"số bước trung\n  bình mỗi phần tử\". Đây CHÍNH LÀ hiệu số giữa O(n) và O(n log n)\n  hiện ra bằng số đo được, không chỉ trên giấy.\n\n⚠️ THỨ TỰ CHÈN ở đây cố tình chọn GIẢM DẦN — ca XẤU NHẤT cho chèn\n  (mỗi phần tử mới luôn nhỏ hơn mọi phần tử đã có, buộc sift-up chạy\n  tới tận gốc). Chèn theo thứ tự NGẪU NHIÊN cho kết quả GẦN NHAU hơn\n  nhiều giữa hai cách (thường chỉ chênh dưới 2×) — vì trung bình, một\n  phần tử ngẫu nhiên chỉ cần sift-up VÀI bước, không phải toàn bộ\n  chiều cao. Đây là một điểm tinh tế đáng nhớ: **\"O(n log n) so với\n  O(n)\" là so sánh CA XẤU NHẤT** — giống hệt bài học \"trung bình\n  khác xấu nhất\" đã gặp ở module Hash, nay lặp lại trong ngữ cảnh heap.\n```\n\n### Dùng `heapq` chuẩn — và mẹo giả lập max-heap\n\n```python\nimport heapq\n\n# Min-heap trực tiếp\ndong = []\nfor gt in (5, 3, 8, 1, 9):\n    heapq.heappush(dong, gt)\nassert heapq.heappop(dong) == 1               # ⭐ nhỏ nhất ra trước\n\n# Xây heap từ mảng có sẵn trong O(n) — CHÍNH LÀ heapify đã chứng minh\nmang = [5, 3, 8, 1, 9, 2, 7]\nheapq.heapify(mang)                            # ⭐ O(n), sửa TẠI CHỖ (in-place)\nassert mang[0] == min(mang)                    # gốc luôn là nhỏ nhất\n\n# Giả lập MAX-heap bằng đảo dấu\ndong_max = []\nfor gt in (5, 3, 8, 1, 9):\n    heapq.heappush(dong_max, -gt)               # ⭐ đẩy vào: đảo dấu\nassert -heapq.heappop(dong_max) == 9            # ⭐ lấy ra: đảo dấu lại — LỚN NHẤT\n\n# Tìm k phần tử LỚN NHẤT — không cần sort toàn bộ mảng\ndiem = [55, 82, 91, 40, 76, 88, 63, 99, 21, 70]\ntop3 = heapq.nlargest(3, diem)\nassert top3 == [99, 91, 88]\nprint(\"✓ top 3 điểm số:\", top3, \"— O(n log k), rẻ hơn sort(O(n log n)) khi k nhỏ\")\n```",

so_sanh: "### Ba cách cài hàng đợi ưu tiên\n\n| | Thêm | Lấy min/max | Ghi chú |\n|---|---|---|---|\n| Mảng không sắp | O(1) | **⚠️ O(n)** | chỉ tốt nếu HIẾM khi lấy |\n| Mảng luôn giữ sắp | **⚠️ O(n)** | O(1) | chỉ tốt nếu HIẾM khi thêm |\n| **Heap** | **O(log n)** | **O(log n)** | ⭐ cân bằng cả hai, tốt khi CẢ HAI đều thường xuyên |\n| BST cân bằng (bài trước) | O(log n) | O(log n) (tìm min/max) | hoạt động được nhưng NẶNG hơn — cân bằng bằng xoay, không cần cho việc chỉ lấy min/max |\n\n### Heap so với BST — khác biệt cốt lõi\n\n| | Heap | BST (2 bài trước) |\n|---|---|---|\n| Ràng buộc hình dạng | **hoàn chỉnh, BẮT BUỘC** | tự do |\n| Ràng buộc thứ tự | chỉ **cha-con TRỰC TIẾP** | **toàn bộ cây con** (trái < gốc < phải) |\n| Biểu diễn | **mảng phẳng** | con trỏ |\n| Tìm một giá trị bất kỳ | **✗ không hỗ trợ hiệu quả** | ✓ O(chiều cao) |\n| Lấy min/max | **⭐ O(1) xem, O(log n) lấy ra** | O(log n) (phải đi tới tận cùng một nhánh) |\n| Duyệt ra dãy đã sắp | ✗ (chỉ lấy được TUẦN TỰ bằng cách rút hết) | **✓ inorder trực tiếp** |\n\n### Xây heap — hai cách, một bậc khác nhau\n\n| Cách | Độ phức tạp | Khi dùng |\n|---|---|---|\n| **`heapify` (từ dưới lên, chuẩn)** | **O(n)** | đã có sẵn TOÀN BỘ dữ liệu |\n| Chèn từng phần tử (từ trên xuống) | O(n log n) | dữ liệu đến DẦN THEO THỜI GIAN (streaming) |\n\n### `heapq` Python — tổng hợp API\n\n| Hàm | Việc làm | Độ phức tạp |\n|---|---|---|\n| `heappush(h, x)` | chèn x | O(log n) |\n| `heappop(h)` | lấy & xoá nhỏ nhất | O(log n) |\n| `heap[0]` | xem nhỏ nhất, KHÔNG xoá | O(1) |\n| `heapify(list)` | biến list CÓ SẴN thành heap tại chỗ | **O(n)** |\n| `nlargest(k, it)` / `nsmallest(k, it)` | top-k mà KHÔNG sort toàn bộ | O(n log k) |",

loi_thuong_gap: [
  "**Nhầm heap với BST — tưởng heap giữ thứ tự trái nhỏ, phải lớn.** Heap CHỈ ràng buộc cha-con; hai anh em (con trái và con phải của cùng một cha) KHÔNG có quan hệ thứ tự nào với nhau.",
  "**Tưởng heap là mảng ĐÃ SẮP HOÀN TOÀN.** Chỉ gốc chắc chắn là nhỏ nhất (min-heap) — phần tử ở giữa mảng KHÔNG theo thứ tự tăng dần gì cả, chỉ đúng bất biến cha-con cục bộ.",
  "**Sift-down so sánh với con BẤT KỲ thay vì con NHỎ HƠN trong hai con.** Đổi chỗ với con lớn hơn có thể để lại vi phạm với con còn lại — lỗi khó phát hiện vì heap \"trông vẫn gần đúng\".",
  "**Tưởng xây heap từ mảng có sẵn là O(n log n).** Trực giác \"n lần × O(log n)\" bỏ qua việc phần lớn nút là lá, sift-down ở đó gần như miễn phí — bậc thật là O(n), đã chứng minh ở `co_che`.",
  "**Quên rằng heapq của Python chỉ có min-heap.** Cần max-heap mà không đảo dấu sẽ lấy nhầm phần tử NHỎ NHẤT thay vì lớn nhất — lỗi logic âm thầm, không báo lỗi gì.",
  "**Dùng heap để TÌM một giá trị cụ thể.** Heap không hỗ trợ — phải quét O(n) toàn mảng, y hệt mảng không sắp. Cần tìm kiếm thì dùng BST/hash, không phải heap.",
  "**Lấy phần tử CUỐI mảng đưa lên gốc rồi QUÊN sift-down.** Sau khi thay gốc, gần như chắc chắn VI PHẠM bất biến — bắt buộc phải sift-down ngay, không phải bước tuỳ chọn.",
  "**Dùng heap khi chỉ cần lấy min/max MỘT LẦN DUY NHẤT.** `min()`/`max()` trên mảng thường là O(n), rẻ hơn hẳn chi phí XÂY một heap chỉ để dùng một lần."
],

tu_kiem_tra: [
  "Phát biểu hai điều kiện của bất biến heap. Vì sao heap KHÔNG hỗ trợ tìm kiếm giá trị bất kỳ hiệu quả, dù nó là một cây?",
  "Vẽ một min-heap 6 phần tử hợp lệ mà hai \"anh em\" (con trái/phải của cùng một cha) không theo thứ tự tăng dần nào giữa chúng.",
  "Trình bày sift-up. Vì sao chỉ cần so với CHA, không cần so với các tổ tiên xa hơn?",
  "Trình bày sift-down. Vì sao phải chọn CON NHỎ HƠN trong hai con để so sánh và đổi chỗ?",
  "Trình bày chứng minh xây heap từ mảng là O(n). Chỉ ra bước nào trong lập luận \"n × O(log n)\" đã sai.",
  "Có ba cách cài hàng đợi ưu tiên (mảng không sắp, mảng luôn sắp, heap). Vì sao heap được coi là \"cân bằng\" chứ không phải \"nhanh nhất tuyệt đối\"?",
  "Vì sao `heapq` của Python chỉ cài min-heap? Viết đoạn code giả lập max-heap từ ba giá trị 4, 9, 2.",
  "Nêu một tình huống nên dùng BST/AVL thay vì heap, dù cả hai đều \"là cây\". Lý do cốt lõi là gì?"
],

tai_nguyen: [
  "\"Introduction to Algorithms\" (Cormen, Leiserson, Rivest, Stein — CLRS), chương Heapsort — có chứng minh hình thức đầy đủ cho O(n) của `BUILD-HEAP`.",
  "\"Data Structures and Algorithms in Python\" (Goodrich, Tamassia, Goldwasser), chương Heaps — trình bày cả cài đặt dựa trên mảng lẫn dựa trên cây liên kết.",
  "Tài liệu chính thức Python — module `heapq` trong Python Standard Library — mô tả đầy đủ API và một số công thức thực dụng (ví dụ cài đặt hàng đợi ưu tiên có xoá phần tử) trong phần \"Recipes\" ở cuối trang tài liệu module."
],

lien_he: "Bài này **trả xong lời hứa dài nhất của module Cây tính tới nay** — bắt đầu từ bài Queue (module Cấu trúc dữ liệu tuyến tính), đi qua bài Cây nhị phân (biểu diễn mảng), tới đây là nơi cả hai gặp nhau: hàng đợi ưu tiên THẬT SỰ được cài bằng heap, dùng ĐÚNG biểu diễn mảng đã học.\n\nBài **tiếp theo trong module này — Trie** — chuyển hướng khác hẳn: heap tối ưu cho \"lấy min/max nhanh\", trie tối ưu cho \"tìm theo TIỀN TỐ chuỗi nhanh\" — cả hai đều đánh đổi ĐỘ TỔNG QUÁT (không hỗ trợ mọi thao tác như BST) lấy TỐC ĐỘ cho một nhu cầu HẸP cụ thể — cùng một mẫu tư duy thiết kế, khác bài toán.\n\nNối sang **module 8 — Kỹ thuật giải thuật cốt lõi**: mã hoá Huffman (nén dữ liệu không mất thông tin) xây một cây nhị phân bằng cách LẶP LẠI \"lấy ra hai phần tử nhỏ nhất, gộp lại, đẩy vào lại\" — chính là vòng lặp priority-queue-bằng-heap ở bài này, chỉ đổi \"giá trị\" đưa vào bằng \"một cây con nhỏ\".\n\nNối sang **module 1 — Nền tảng CS, bài Lập lịch CPU** (đã học trước khi tới mảng 02): thuật toán lập lịch \"ưu tiên tiến trình ngắn nhất\" (Shortest Job First) trong hệ điều hành thật CHÍNH LÀ một hàng đợi ưu tiên — mỗi tiến trình mới đến được `chen()` vào heap theo thời gian chạy dự kiến, hệ điều hành luôn `lay_nho_nhat()` để chọn tiến trình chạy tiếp theo."

};
