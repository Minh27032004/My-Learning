window.LESSONS["01:command-line-terminal:quyen-file-chmod-chown-sudo"] = {

tom_tat: [
  "`chmod` đổi **quyền**, `chown` đổi **chủ sở hữu**. Hai việc khác nhau — nhầm là sửa mãi không được.",
  "Hai cách viết `chmod`: **bát phân** (`755`) đặt tuyệt đối, **ký hiệu** (`u+x`) sửa tương đối. Ký hiệu an toàn hơn vì không xoá quyền đang có.",
  "**`umask` quyết định quyền mặc định của file mới.** Nó là mặt nạ *trừ đi*, không phải giá trị được cộng vào.",
  "`sudo` không phải \"chế độ thần thánh\" — nó chạy một lệnh dưới danh nghĩa người khác theo đúng chính sách trong `/etc/sudoers`.",
  "**`chmod 777` gần như luôn sai.** Nó cho phép mọi người ghi đè, và SSH còn từ chối dùng khoá riêng có quyền lỏng."
],

can_biet_truoc: [
  "Bài **Hệ thống file, đường dẫn, quyền truy cập** ở module 2 — **bắt buộc đọc trước**, vì bài đó giải thích mô hình quyền `rwx` và ý nghĩa đặc biệt của quyền trên thư mục.",
  "Bài **Điều hướng** — cần đọc được kết quả `ls -l`.",
  "Bài **Hệ nhị phân** — cần hiểu vì sao một chữ số bát phân là ba bit."
],

dinh_nghia: "Bài ở module 2 đã trả lời *quyền là gì*. Bài này trả lời *thay đổi nó bằng lệnh nào và làm sao cho an toàn*.\n\nNói chính xác:\n\n- **`chmod`** (*change mode*) — đổi bit quyền của file hoặc thư mục.\n- **`chown`** (*change owner*) — đổi chủ sở hữu và nhóm. Cần quyền root.\n- **`umask`** — mặt nạ **loại bỏ** quyền khỏi giá trị mặc định khi tạo file mới.\n- **`sudo`** (*substitute user do*) — chạy một lệnh dưới danh nghĩa người dùng khác, mặc định là `root`, theo chính sách đã cấu hình.",

vi_sao_can: "Đây là nhóm lệnh bạn buộc phải dùng khi làm việc với server:\n\n- **`Permission denied` là lỗi phổ biến nhất khi triển khai.** Không biết đọc `ls -l` và sửa bằng `chmod`/`chown` là bế tắc.\n- **SSH từ chối khoá có quyền lỏng.** `chmod 600 ~/.ssh/id_ed25519` là bắt buộc, không phải khuyến nghị.\n- **Docker volume và quyền.** Container chạy user id khác với chủ sở hữu file trên máy chủ — nguồn của rất nhiều rắc rối.\n- **Script không chạy được.** Quên `chmod +x` là `Permission denied` dù file hoàn toàn đúng.\n- **Bảo mật.** `chmod 777` để \"cho nhanh\" là mở cửa cho bất kỳ tiến trình nào trên máy ghi đè file của bạn.\n\nTình huống thật: bạn tạo khoá SSH, copy public key lên server, nhưng vẫn bị hỏi mật khẩu. SSH im lặng từ chối dùng khoá riêng vì quyền của nó là `644` — ai cũng đọc được. Một lệnh `chmod 600` là xong, nhưng không biết thì loay hoay rất lâu vì SSH không nói rõ lý do.",

co_che: "### `chmod` — hai cách viết\n\n**Cách bát phân: đặt tuyệt đối.**\n\n```bash\nchmod 755 script.sh        # rwxr-xr-x\nchmod 644 tai-lieu.txt     # rw-r--r--\nchmod 600 bi-mat.key       # rw-------\nchmod 700 thu-muc-rieng/   # rwx------\n```\n\nMỗi chữ số là ba bit, như đã học ở module 2:\n\n```\n     chủ   nhóm  khác\n      7     5     5\n      │     │     │\n     rwx   r-x   r-x\n     421   4 1   4 1\n```\n\n**Cách ký hiệu: sửa tương đối.**\n\n```bash\nchmod u+x script.sh        # thêm quyền chạy cho CHỦ sở hữu\nchmod g-w file.txt         # bỏ quyền ghi của NHÓM\nchmod o=r file.txt         # đặt quyền của NGƯỜI KHÁC thành chỉ đọc\nchmod a+r file.txt         # thêm quyền đọc cho TẤT CẢ\nchmod +x script.sh         # viết tắt của a+x (chịu ảnh hưởng của umask)\n\nchmod u+x,g-w,o-rwx file   # kết hợp nhiều thay đổi\n```\n\n| Ký hiệu | Nghĩa |\n|---|---|\n| `u` | user — chủ sở hữu |\n| `g` | group — nhóm |\n| `o` | others — mọi người khác |\n| `a` | all — cả ba |\n| `+` | thêm quyền |\n| `-` | bỏ quyền |\n| `=` | đặt chính xác, xoá các quyền khác |\n\n**Nên dùng cách nào?** Ký hiệu **an toàn hơn** vì nó chỉ sửa đúng thứ bạn nói, không đụng tới các bit khác. `chmod 644` trên một file đang là `755` sẽ **âm thầm xoá quyền chạy** — có thể không phải điều bạn muốn.\n\nDùng bát phân khi bạn muốn đặt một trạng thái xác định, ví dụ trong script triển khai. Dùng ký hiệu khi sửa một file cụ thể.\n\n### Đệ quy — và cái bẫy lớn\n\n```bash\nchmod -R 755 thu-muc/      # ⚠️ áp cho MỌI file và thư mục bên trong\n```\n\nCái bẫy: **file và thư mục cần quyền khác nhau**. Thư mục cần `x` để đi vào được; file dữ liệu thì không nên có `x`.\n\n```bash\n# ❌ Sai — mọi file .txt, .jpg cũng thành chạy được\nchmod -R 755 du-an/\n\n# ✓ Đúng — tách riêng file và thư mục\nfind du-an/ -type d -exec chmod 755 {} +    # thư mục: rwxr-xr-x\nfind du-an/ -type f -exec chmod 644 {} +    # file:    rw-r--r--\nchmod +x du-an/script.sh                    # rồi thêm x cho đúng script cần\n```\n\nCó cách viết gọn hơn dùng `X` hoa:\n\n```bash\nchmod -R u=rwX,go=rX du-an/\n#            ↑\n#            X hoa: thêm x CHỈ cho thư mục và file vốn đã có x\n```\n\n`X` hoa là một trong những tính năng hữu ích mà ít người biết.\n\n### `chown` — đổi chủ sở hữu\n\n```bash\nsudo chown minh file.txt           # đổi chủ\nsudo chown minh:dev file.txt       # đổi cả chủ và nhóm\nsudo chown :dev file.txt           # chỉ đổi nhóm\nsudo chgrp dev file.txt            # cách khác để đổi nhóm\nsudo chown -R minh:dev thu-muc/    # đệ quy\n```\n\n**`chown` cần quyền root.** Lý do là bảo mật: nếu người dùng thường đổi được chủ sở hữu, họ có thể tạo một file rồi gán cho người khác — ví dụ đặt một file độc vào thư mục nhà của người đó, hoặc lách hạn ngạch dung lượng.\n\nBạn **cho đi được nhưng không lấy về được** — và trên Linux thì ngay cả cho đi cũng bị chặn với người dùng thường.\n\n### `umask` — quyền mặc định của file mới\n\nĐây là phần hay bị hiểu sai. `umask` là **mặt nạ trừ đi**, không phải giá trị được đặt.\n\n```bash\numask                # 0022 (giá trị phổ biến)\n\n# Quyền mặc định trước khi áp mặt nạ:\n#   file    666  (rw-rw-rw-)   ← file KHÔNG BAO GIỜ mặc định có x\n#   thư mục 777  (rwxrwxrwx)\n\n# Áp umask 022 (nghĩa là bỏ bit w của nhóm và người khác):\n#   file    666 - 022 = 644  (rw-r--r--)\n#   thư mục 777 - 022 = 755  (rwxr-xr-x)\n```\n\nChính xác hơn thì đây là phép **AND với phần bù**, nhưng với các giá trị thông thường thì phép trừ cho kết quả đúng và dễ nhớ hơn.\n\nĐiểm đáng chú ý: **file mới không bao giờ tự có quyền `x`**. Đó là lý do bạn luôn phải `chmod +x` sau khi tạo script — không phải hệ thống khó tính, mà là biện pháp an toàn: file tải về hay tạo ra không tự nhiên chạy được.\n\n```bash\numask 077            # chặt hơn: chỉ chủ sở hữu đọc được file mới\ntouch thu.txt\nls -l thu.txt        # -rw------- ← 666 - 077 = 600\n```\n\nĐặt `umask 077` trong `~/.bashrc` là lựa chọn tốt cho máy dùng chung.\n\n### `sudo` — chạy dưới danh nghĩa người khác\n\n```bash\nsudo lenh                    # chạy với quyền root\nsudo -u nguoi_khac lenh      # chạy dưới danh nghĩa người dùng khác\nsudo -i                      # mở shell root tương tác\nsudo -l                      # ⭐ xem mình được phép chạy những gì\nsudo !!                      # chạy lại lệnh vừa rồi với sudo\n```\n\n`sudo` **không phải công tắc bật quyền vô hạn**. Nó tra `/etc/sudoers` để xem bạn được phép chạy lệnh gì, dưới danh nghĩa ai, có cần mật khẩu không. Quản trị viên cấu hình được rất chi tiết:\n\n```\n# /etc/sudoers — chỉ sửa bằng lệnh visudo, KHÔNG sửa trực tiếp\nminh ALL=(ALL) ALL                          # được chạy mọi lệnh\ndev  ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart myapp\n#                        └─ chỉ được restart đúng dịch vụ này, không cần mật khẩu\n```\n\nLuôn dùng `visudo` để sửa file này — nó **kiểm tra cú pháp trước khi lưu**. Sai cú pháp trong `sudoers` có thể khoá mọi người khỏi quyền quản trị, và sửa lại cần khởi động vào chế độ cứu hộ.\n\n### Vì sao chuyển hướng không chạy dưới `sudo`\n\n```bash\n# ❌ Permission denied dù đã có sudo\nsudo echo \"noi dung\" > /etc/cau-hinh\n\n# ✓ Đúng\necho \"noi dung\" | sudo tee /etc/cau-hinh > /dev/null\n```\n\nLý do đã gặp ở bài pipe: **chuyển hướng `>` do shell thực hiện**, và shell đang chạy với quyền người dùng thường. `sudo` chỉ nâng quyền cho `echo`, mà `echo` không phải thứ mở file. Còn `tee` thì chạy dưới `sudo` nên mở được file trong `/etc`.\n\nCùng lý do, muốn ghi nối thì dùng `sudo tee -a`.\n\n### `sudo` và `su` khác nhau\n\n| | `sudo` | `su` |\n|---|---|---|\n| Mật khẩu cần nhập | **Của chính bạn** | Của tài khoản đích |\n| Phạm vi | Một lệnh | Cả một phiên shell |\n| Ghi log | **✓ đầy đủ ai làm gì** | Ít |\n| Kiểm soát chi tiết | **✓ theo từng lệnh** | Được ăn cả |\n\n`sudo` được ưa dùng hơn hẳn vì hai điểm: **không phải chia sẻ mật khẩu root cho ai** và **mọi lệnh đều được ghi log** kèm tên người chạy. Nhiều bản phân phối hiện nay còn khoá hẳn tài khoản root và chỉ cho dùng `sudo`.\n\n### Bit đặc biệt\n\n```bash\nchmod u+s file        # setuid  — chạy với quyền CHỦ SỞ HỮU file\nchmod g+s thu-muc/    # setgid  — file tạo trong đây kế thừa nhóm của thư mục\nchmod +t thu-muc/     # sticky  — chỉ chủ file mới xoá được file của mình\n```\n\n**setuid** là cơ chế đứng sau lệnh `passwd`: người dùng thường cần sửa `/etc/shadow` (file chỉ root ghi được) để đổi mật khẩu của chính mình. `passwd` có bit setuid nên khi chạy nó tạm thời mang quyền root.\n\n```bash\nls -l /usr/bin/passwd\n# -rwsr-xr-x 1 root root ...\n#    ↑ chữ s thay cho x = setuid\n```\n\nĐây cũng là **bề mặt tấn công quan trọng**: một chương trình setuid có lỗ hổng cho phép leo thang lên quyền root. Vì vậy tuyệt đối không đặt setuid cho script tự viết.\n\n**setgid trên thư mục** rất hữu ích cho thư mục dùng chung của nhóm: mọi file tạo trong đó tự động thuộc nhóm của thư mục, nên đồng đội đọc được mà không phải `chgrp` thủ công.\n\n**sticky bit** đã gặp ở module 2 với `/tmp`.",

vi_du: "### Sửa lỗi khoá SSH bị từ chối\n\n```bash\nssh server\n# server's password:          ← vẫn hỏi mật khẩu dù đã có khoá\n\nls -l ~/.ssh/\n# -rw-r--r-- 1 minh minh 464 id_ed25519        ← 644, quá lỏng\n# -rw-r--r-- 1 minh minh 101 id_ed25519.pub\n\nchmod 700 ~/.ssh\nchmod 600 ~/.ssh/id_ed25519         # khoá riêng: chỉ chủ đọc được\nchmod 644 ~/.ssh/id_ed25519.pub     # khoá công khai: ai đọc cũng được\nchmod 600 ~/.ssh/config\n\nssh server                          # ✓ vào được, không hỏi mật khẩu\n```\n\nSSH **im lặng bỏ qua** khoá riêng có quyền quá lỏng, và không giải thích lý do trừ khi bạn chạy `ssh -v`. Đây là cơ chế bảo vệ hợp lý — khoá riêng mà cả máy đọc được thì coi như đã lộ.\n\n### Script không chạy được\n\n```bash\n./trien-khai.sh\n# bash: ./trien-khai.sh: Permission denied\n\nls -l trien-khai.sh\n# -rw-r--r-- 1 minh dev 1234 ...     ← thiếu x\n\nchmod +x trien-khai.sh\nls -l trien-khai.sh\n# -rwxr-xr-x 1 minh dev 1234 ...     ← đã có x ✓\n./trien-khai.sh                       # chạy được\n```\n\nMột cách khác không cần `chmod`, hữu ích khi bạn không có quyền sửa file:\n\n```bash\nbash trien-khai.sh        # gọi bash và đưa file làm đối số\n```\n\nCách này chỉ cần quyền **đọc**, vì bạn đang chạy `bash` chứ không chạy file kia.\n\n### Đặt quyền đúng cho cây thư mục web\n\n```bash\n# ❌ Sai — mọi ảnh và file HTML cũng thành chạy được\nchmod -R 755 /var/www/site\n\n# ✓ Đúng — tách thư mục và file\nfind /var/www/site -type d -exec chmod 755 {} +\nfind /var/www/site -type f -exec chmod 644 {} +\n\n# Chủ sở hữu là user chạy web server\nsudo chown -R www-data:www-data /var/www/site\n\n# Kiểm tra\nls -l /var/www/site | head\n```\n\nBa lệnh này là mẫu chuẩn khi triển khai web tĩnh. Thư mục `755` để web server đi vào được, file `644` để đọc được nhưng không chạy được.\n\n### Rắc rối quyền với Docker volume\n\n```bash\n# Trên máy chủ, file thuộc user id 1000\nls -ln du-lieu/\n# -rw-r--r-- 1 1000 1000 ... file.txt\n\n# Container chạy với user khác → không ghi được\ndocker run -v $(pwd)/du-lieu:/data ung-dung\n# PermissionError: [Errno 13] Permission denied: '/data/file.txt'\n```\n\nBa cách xử lý:\n\n```bash\n# ① Chạy container với đúng user id của máy chủ\ndocker run --user $(id -u):$(id -g) -v $(pwd)/du-lieu:/data ung-dung\n\n# ② Đổi chủ sở hữu trên máy chủ cho khớp user trong container\nsudo chown -R 1001:1001 du-lieu/\n\n# ③ Trong Dockerfile, tạo user có đúng id\n# RUN useradd -u 1000 -m ungdung\n# USER ungdung\n```\n\nCách ① là nhanh nhất khi phát triển; cách ③ là đúng nhất cho production.\n\nNguyên nhân gốc: **quyền dựa trên user id dạng số, không phải tên**. Container và máy chủ có bảng tên người dùng riêng, nhưng id thì dùng chung — nên `minh` trên máy chủ và `appuser` trong container có thể cùng là id `1000`.\n\n### Thư mục dùng chung cho nhóm\n\n```bash\nsudo mkdir /srv/du-an-chung\nsudo chgrp dev /srv/du-an-chung\nsudo chmod 2775 /srv/du-an-chung\n#              ↑ số 2 ở đầu = setgid\n\nls -ld /srv/du-an-chung\n# drwxrwsr-x 2 root dev ...\n#       ↑ chữ s = setgid đang bật\n\n# Giờ mọi file ai tạo trong đây đều thuộc nhóm dev,\n# nên cả nhóm đọc và ghi được mà không phải chgrp thủ công\n```\n\nKhông có setgid, file mới sẽ thuộc nhóm mặc định của người tạo, và đồng đội không đọc được — một nguồn bực bội rất phổ biến khi làm việc nhóm trên server.\n\n### Xem mình được `sudo` những gì\n\n```bash\nsudo -l\n# User minh may chay cac lenh sau tren server:\n#     (ALL) ALL\n#     (root) NOPASSWD: /usr/bin/systemctl restart myapp\n```\n\nLệnh này rất hữu ích khi vào một server lạ: nó cho biết ngay bạn có quyền gì mà không phải thử từng lệnh.\n\n### `umask` trong thực tế\n\n```bash\numask                    # 0022\ntouch a.txt && ls -l a.txt\n# -rw-r--r--              ← 666 - 022 = 644\n\numask 077                # chặt hơn\ntouch b.txt && ls -l b.txt\n# -rw-------              ← 666 - 077 = 600\n\nmkdir d && ls -ld d\n# drwx------              ← 777 - 077 = 700\n\numask 022                # trả lại\n```\n\nĐặt trong `~/.bashrc` nếu muốn áp dụng thường xuyên:\n\n```bash\numask 077                # mọi file mới chỉ mình đọc được\n```",

so_sanh: "### Bát phân hay ký hiệu\n\n| | Bát phân (`755`) | Ký hiệu (`u+x`) |\n|---|---|---|\n| Cách hoạt động | **Đặt tuyệt đối**, ghi đè hết | **Sửa tương đối** |\n| Rủi ro | Xoá mất quyền đang có | Thấp |\n| Rõ ràng | Cần biết nghĩa từng số | Đọc là hiểu |\n| Hợp với | Script triển khai, đặt trạng thái xác định | Sửa một file cụ thể |\n\nQuy tắc thực dụng: **script dùng bát phân** (kết quả xác định, không phụ thuộc trạng thái trước đó), **gõ tay dùng ký hiệu** (an toàn hơn, không vô tình xoá quyền khác).\n\n### Các bộ quyền thường dùng\n\n| Quyền | Ý nghĩa | Dùng cho |\n|---|---|---|\n| `600` | chỉ chủ đọc/ghi | **Khoá riêng SSH, `.env`** |\n| `644` | chủ ghi, mọi người đọc | File thường, tài liệu, ảnh |\n| `700` | chỉ chủ toàn quyền | Thư mục riêng tư như `~/.ssh` |\n| `755` | chủ toàn quyền, người khác đọc và chạy | **Script, thư mục** |\n| `775` | chủ và nhóm ghi được | Thư mục dùng chung của nhóm |\n| `777` | **ai cũng làm gì cũng được** | ⚠️ Gần như luôn sai |\n\n### Vì sao `chmod 777` gần như luôn sai\n\nNó cho **bất kỳ tiến trình nào trên máy** quyền ghi đè file của bạn. Hậu quả cụ thể:\n\n- Một dịch vụ bị chiếm quyền có thể sửa code của bạn\n- Trên máy dùng chung, ai cũng thay được nội dung file\n- SSH **từ chối** dùng khoá riêng có quyền lỏng\n- Nhiều web server từ chối chạy file có quyền ghi cho mọi người\n\nNgười ta hay dùng `777` khi gặp `Permission denied` và không muốn tìm hiểu. Cách đúng là **xác định user nào cần quyền gì**, rồi dùng `chown` đặt đúng chủ sở hữu, và `chmod` mức tối thiểu đủ dùng.\n\nGần như mọi trường hợp \"phải dùng 777\" thật ra là **vấn đề chủ sở hữu**, giải bằng `chown` mới đúng.\n\n### Nguyên tắc đặc quyền tối thiểu\n\nĐây là nguyên tắc bảo mật xuyên suốt, không chỉ áp dụng cho file:\n\n```\nCho đúng mức quyền tối thiểu cần để làm việc — không hơn.\n```\n\nÁp dụng ở mọi tầng:\n\n| Tầng | Áp dụng |\n|---|---|\n| File | `600` cho secret, không phải `644` |\n| Người dùng | Không chạy ứng dụng dưới quyền root |\n| Container | `USER appuser` trong Dockerfile, không chạy root |\n| Database | User của ứng dụng chỉ có `SELECT`, `INSERT` — không có `DROP` |\n| Cloud | IAM role chỉ cho đúng bucket cần dùng |\n\nLý do: **giới hạn thiệt hại khi bị chiếm quyền**. Một ứng dụng chạy dưới quyền root bị khai thác thì kẻ tấn công có cả máy; chạy dưới user hạn chế thì chúng chỉ có phần đó.\n\nBạn sẽ gặp lại nguyên tắc này ở mảng 04 (quyền database), mảng 05 (bảo mật) và mảng 06 (thiết kế hệ thống).",

loi_thuong_gap: [
  "**`chmod 777` để cho nhanh.** Gần như luôn là vấn đề chủ sở hữu — sửa bằng `chown` mới đúng. `777` mở cửa cho mọi tiến trình ghi đè file của bạn.",
  "**Quên `chmod 600` cho khoá SSH.** SSH im lặng bỏ qua khoá có quyền lỏng và bạn không hiểu vì sao vẫn bị hỏi mật khẩu.",
  "**`chmod -R 755` cả cây thư mục.** File dữ liệu cũng thành chạy được. Tách riêng bằng `find -type d` và `-type f`, hoặc dùng `X` hoa.",
  "**`sudo lenh > /etc/file`.** Chuyển hướng do shell làm với quyền thường. Dùng `| sudo tee`.",
  "**Sửa `/etc/sudoers` trực tiếp bằng editor.** Sai cú pháp có thể khoá mọi người khỏi quyền quản trị. Luôn dùng `visudo`.",
  "**Nhầm `chmod` với `chown`.** Quyền và chủ sở hữu là hai thứ khác nhau. `Permission denied` có thể do sai chủ chứ không phải sai quyền.",
  "**Đặt setuid cho script tự viết.** Đây là lỗ hổng leo thang đặc quyền nghiêm trọng. Không bao giờ làm.",
  "**Chạy ứng dụng dưới quyền root trong container.** Bị khai thác là mất cả máy chủ. Dùng `USER` trong Dockerfile.",
  "**Tưởng `umask 022` nghĩa là đặt quyền 022.** Nó là mặt nạ *trừ đi*: file mới có `666 - 022 = 644`."
],

tu_kiem_tra: [
  "`chmod` và `chown` khác nhau ở chỗ nào? Nêu một tình huống `Permission denied` mà `chmod` không sửa được.",
  "Bạn có khoá SSH nhưng vẫn bị hỏi mật khẩu. Nguyên nhân phổ biến nhất và cách sửa?",
  "Vì sao `chmod -R 755 du-an/` là ý tồi? Viết cách làm đúng.",
  "`umask` đang là `022`. File mới tạo có quyền gì? Thư mục mới thì sao? Giải thích công thức.",
  "Vì sao file mới không bao giờ tự có quyền `x`, kể cả khi umask là `000`?",
  "Vì sao `sudo echo \"x\" > /etc/file` thất bại còn `echo \"x\" | sudo tee /etc/file` thì được?",
  "Lệnh `passwd` cho người dùng thường sửa được `/etc/shadow` mà file đó chỉ root ghi được. Cơ chế nào cho phép điều đó, và vì sao nó nguy hiểm?",
  "Nêu ba nơi khác ngoài file mà nguyên tắc đặc quyền tối thiểu được áp dụng."
],

tai_nguyen: [
  "Sách **The Linux Command Line** — William Shotts, chương 9 *Permissions*. Đọc miễn phí.",
  "Tài liệu Linux: `man chmod`, `man chown`, `man umask`, `man sudoers`.",
  "Tài liệu Docker, mục *Dockerfile USER* và *Understand permissions with bind mounts*.",
  "**OWASP** — nguyên tắc *Principle of Least Privilege*, mô tả cách áp dụng ở mọi tầng hệ thống."
],

lien_he: [
  "**Hệ thống file, đường dẫn, quyền** ở module 2: bài đó giải thích mô hình quyền; bài này là các lệnh để thay đổi nó.",
  "**Pipe và redirect**: `sudo tee` là mẫu bắt buộc vì chuyển hướng do shell thực hiện, không phải do sudo.",
  "**grep, find, sed, awk**: `find -type d -exec chmod` là cách duy nhất đúng để đặt quyền khác nhau cho file và thư mục.",
  "**ssh, scp, rsync** (bài kế tiếp): quyền `600` cho khoá riêng là điều kiện bắt buộc để SSH hoạt động.",
  "**Biến môi trường**: file `.env` chứa secret nên đặt `chmod 600`, không phải `644`.",
  "**Triển khai** ở mảng 05: quyền file và chủ sở hữu là nguyên nhân số một của lỗi khi mount volume vào Docker.",
  "**Auth & bảo mật** ở mảng 05: nguyên tắc đặc quyền tối thiểu áp dụng cho user database, IAM role, và quyền của service account."
]

};
