window.LESSONS["05:phuc-vu-model-ai:streaming-response-cho-llm-sse"] = {

tom_tat: [
  "SSE giữ một HTTP response mở và gửi chuỗi event UTF-8 theo thứ tự; nó phù hợp khi server đẩy token LLM một chiều, không cần giao tiếp hai chiều như WebSocket.",
  "Streaming có ích khi rút ngắn thời gian tới token đầu tiên, không làm tổng thời gian sinh tự biến mất. Muốn UX tốt phải đo TTFT, tốc độ token và tỷ lệ stream bị ngắt.",
  "Mỗi event phải có ranh giới rõ, dữ liệu JSON được encode đúng, heartbeat giữ kết nối qua proxy và event kết thúc cho client biết stream hoàn tất có chủ đích.",
  "Client ngắt kết nối phải hủy việc sinh càng sớm càng tốt; nếu không GPU tiếp tục tiêu tiền cho kết quả không ai đọc. Cancellation cần truyền qua mọi lớp tới model provider.",
  "Proxy buffering, timeout, backpressure, authentication trước khi mở stream và không đưa secret vào query string là các yêu cầu triển khai chứ không phải chi tiết phụ."
],

can_biet_truoc: [
  "HTTP response header, persistent connection, chunked transfer và vai trò của reverse proxy.",
  "Python async generator, `yield`, cancellation và event loop.",
  "FastAPI `StreamingResponse`, dependency xác thực và JSON serialization.",
  "LLM tokenization, sampling và khác biệt giữa prompt token với completion token."
],

dinh_nghia: "**Server-Sent Events (SSE)** là quy ước truyền sự kiện một chiều từ server tới client trên một HTTP response có media type `text/event-stream`. Mỗi sự kiện gồm các dòng như `event:`, `id:` và `data:`, kết thúc bằng một dòng trống. Browser có `EventSource` để tự kết nối và có thể gửi `Last-Event-ID` khi reconnect.\n\nTrong Media Insight, `POST /v1/insights/stream` nhận prompt mô tả ảnh/audio đã được xác minh, rồi phát lần lượt event `meta`, nhiều event `token`, cuối cùng `done` hoặc `error`. Vì `EventSource` chuẩn chỉ mở GET, ví dụ web dùng `fetch()` để POST và tự parse stream; quyết định này tránh nhét prompt nhạy cảm vào URL.",

vi_sao_can: "Một LLM có thể bắt đầu sinh sau một khoảng chuẩn bị rồi tạo từng token. Nếu API đợi toàn bộ đoạn văn mới trả, người dùng nhìn màn hình trống dù server đã có phần kết quả. SSE đưa phần đã sẵn sàng tới giao diện, giảm perceived latency và cho phép hiển thị trạng thái như “đang phân tích audio”.\n\nNhưng streaming tạo một hệ thống phân tán kéo dài: client có thể đóng tab, Wi-Fi đổi mạng, proxy buffer, deploy làm worker dừng, token có thể cắt giữa ký tự hoặc cấu trúc JSON. Response 200 đã gửi rồi thì server không thể đổi sang status 500 khi model lỗi ở giữa. Vì vậy protocol ứng dụng phải biểu diễn lỗi và kết thúc bên trong event.\n\nSSE đáng chọn khi luồng chủ yếu server → client, cần đi qua hạ tầng HTTP quen thuộc và thứ tự sự kiện quan trọng. Nó không phải mặc định cho mọi inference: model chỉ trả một nhãn nhỏ thì streaming làm code phức tạp mà không đem lại giá trị.",

co_che: "### Wire format: dòng trống mới đóng một event\n\nSSE là văn bản UTF-8. `data: xin` rồi `data: chào` trong cùng event được ghép bằng newline; hai ký tự newline liên tiếp mới dispatch. Comment bắt đầu bằng `:` có thể làm heartbeat mà không tạo message. Server không được đơn giản `yield token` vì token có thể chứa newline làm hỏng framing.\n\nĐóng token trong JSON giải quyết newline, dấu ngoặc kép và Unicode: `data: {\"text\":\"...\"}\\n\\n`. JSON phải được serialize bằng thư viện, không nội suy chuỗi. Một event nên nhỏ nhưng không nhất thiết đúng một model token; batching vài token giảm overhead syscall, đổi lại chữ xuất hiện ít mượt hơn.\n\n### Thiết kế event như một protocol có version\n\nMedia Insight gửi `meta` trước để client biết `request_id`, `model_version`; `token` mang đoạn văn; `usage` mang thống kê khi provider có; `done` xác nhận kết thúc. `error` có code công khai. Không gửi stack trace hay prompt trong event.\n\nClient không nên đoán stream kết thúc thành công chỉ vì socket đóng. Mạng mất và `done` đều làm reader thấy EOF, nhưng ý nghĩa khác nhau. Biến `sawDone` cho phép UI quyết định hiển thị kết quả hoàn chỉnh hay nút thử lại.\n\n### Thời gian tới token đầu tiên và throughput\n\nTTFT (time to first token) gồm queue, tokenize prompt, prefill và đường mạng. Sau đó inter-token latency phản ánh decode, batching và buffering. Tổng latency có thể không giảm so với JSON một lần; streaming chủ yếu thay thời điểm người dùng quan sát kết quả.\n\nĐo cả TTFT, token/giây, tổng duration và số completion token. Nếu TTFT tốt ở ứng dụng nhưng người dùng vẫn thấy từng cụm lớn mỗi 5 giây, proxy đang buffer hoặc frontend parser chưa đọc incremental. Metric phía server một mình không chứng minh trải nghiệm đầu cuối.\n\n### Async generator nối producer với socket\n\n`StreamingResponse` kéo từng bytes từ iterator/generator và gửi xuống ASGI server. Khi generator `await` model provider, event loop xử lý connection khác. Nếu generator gọi model CPU-bound đồng bộ, nó vẫn chặn worker như bài endpoint.\n\nKhông tạo background task vô hạn rồi bỏ reference. Cấu trúc generator nên sở hữu producer và cleanup trong `try/finally`. Nếu provider dùng callback từ thread, cầu nối sang `asyncio.Queue` cần giới hạn dung lượng; queue vô hạn biến client chậm thành tăng RAM.\n\n### Backpressure: người đọc chậm hơn người viết\n\nTCP có send buffer. Khi browser hoặc mạng đọc chậm, `send` cuối cùng phải chờ; đó là backpressure tự nhiên. Nhưng nếu producer đẩy token vào queue không giới hạn nhanh hơn socket, buffer ứng dụng phá cơ chế này. `asyncio.Queue(maxsize=N)` khiến producer chờ hoặc chính sách hủy được kích hoạt.\n\nKhông thể giữ vô hạn connection của client nền. Đặt max stream duration, max output token và idle timeout. Những giới hạn này vừa kiểm soát chi phí vừa ngăn slow-client attack chiếm file descriptor.\n\n### Cancellation phải đi đến nguồn tính toán\n\nASGI nhận biết disconnect; FastAPI có `request.is_disconnected()`, và coroutine có thể nhận `CancelledError`. Chỉ thoát generator chưa chắc dừng request tới provider bên ngoài. Adapter LLM phải hỗ trợ cancel/close; nếu không, ghi nhận “orphan generation” để biết phần chi phí bị lãng phí.\n\nKhông nuốt `CancelledError` trong `except Exception` rồi tiếp tục vòng lặp. `finally` phải đóng provider stream, giải phóng semaphore và ghi metric reason=`client_disconnect`. Cancellation là luồng điều khiển bình thường, không phải lỗi cần stack trace mức error.\n\n### Proxy buffering có thể làm streaming giả\n\nReverse proxy/CDN thường buffer response để tối ưu throughput. Khi đó server yield đều nhưng client nhận một cục. Cần tắt buffering cho route SSE, tránh compression middleware gom buffer, và kiểm timeout idle. Header `Cache-Control: no-cache` ngăn cache; một số proxy hiểu `X-Accel-Buffering: no`, nhưng cấu hình hạ tầng vẫn là nguồn quyết định.\n\nKiểm thử phải đi qua cùng ingress production, không chỉ curl trực tiếp localhost. Dùng timestamp ở từng event và DevTools để thấy khoảng cách nhận. Heartbeat comment mỗi khoảng yên lặng giữ NAT/proxy không coi connection chết, nhưng heartbeat quá dày gây overhead.\n\n### Authentication trước khi gửi status 200\n\nMọi validation, auth và quota có thể xác định nhanh phải chạy trước khi tạo `StreamingResponse`. Sau khi header 200 đã xuống dây, lỗi chỉ còn được gửi dưới dạng event. Với browser `EventSource`, custom Authorization header không được hỗ trợ thuận tiện; cookie có CSRF concern, query token lọt log/history.\n\nMedia Insight dùng `fetch` POST với Authorization header. Nếu buộc dùng GET EventSource, có thể phát short-lived one-time stream ticket qua một POST đã xác thực; ticket scope hẹp, TTL ngắn và không chứa dữ liệu người dùng.\n\n### Retry và Last-Event-ID không tự tạo replay\n\n`EventSource` có thể tự reconnect và gửi `Last-Event-ID`, nhưng server chỉ replay được nếu đã lưu event hoặc có generation deterministic và trạng thái. LLM sampling thường không deterministic hoàn toàn; sinh lại có thể cho nội dung khác. Đánh số event mà không có event store tạo cảm giác resume giả.\n\nĐơn giản nhất là không auto-resume completion: client đánh dấu dở dang và yêu cầu generation mới với idempotency semantics rõ. Nếu nghiệp vụ cần resume, lưu token/event theo stream ID, ràng buộc ID với user, đặt retention và replay từ event kế tiếp. Không cho client đọc stream ID của tenant khác.\n\n### UTF-8, token và ranh giới ký tự\n\nModel token không đồng nghĩa ký tự hay từ. Provider tốt trả chuỗi đã decode; nếu nhận byte fragments, dùng incremental UTF-8 decoder vì một ký tự tiếng Việt có thể bị tách qua chunk. Frontend cũng cần `TextDecoder(..., {stream:true})`, không gọi decode độc lập cho từng Uint8Array.\n\nMarkdown có thể chưa cân bằng dấu backtick trong lúc stream. Render HTML trực tiếp từ partial Markdown tạo nhấp nháy và nguy cơ XSS nếu sanitizer sai. Hiển thị text tạm hoặc sanitize mỗi lần; chỉ render hoàn chỉnh khi done là lựa chọn an toàn hơn.\n\n### Moderation khi nội dung đã được phát\n\nKiểm prompt trước không bảo đảm output an toàn. Nếu phát từng token ngay, hệ thống moderation hậu kỳ không thể “thu hồi” chữ người dùng đã thấy. Có thể buffer theo câu/chunk để moderation trước khi phát, nhưng TTFT và độ mượt xấu đi. Đây là đánh đổi sản phẩm, không có mẹo giao thức xóa bỏ.\n\nVới dữ liệu nhạy cảm, tránh gửi prompt/output vào log và trace. Event payload đi qua proxy, browser extension và telemetry frontend; chính sách dữ liệu phải bao phủ toàn đường đi.\n\n### Graceful shutdown và deploy\n\nStream dài đối đầu rolling deploy. Khi instance nhận termination, ngừng nhận stream mới, cho stream cũ thời gian drain, sau đó gửi event `error`/`done` nếu còn có thể. Nếu grace period ngắn hơn max duration, deploy chắc chắn cắt người dùng.\n\nConnection affinity thường không cần khi một stream đã gắn với TCP connection, nhưng reconnect có thể sang instance khác. Resume dựa vào RAM cục bộ vì thế thất bại; event store dùng chung cần thiết nếu hỗ trợ replay.\n\n### Khi nào không dùng SSE\n\nWebSocket hợp hơn nếu client gửi liên tục audio frames, điều khiển generation, hoặc cần song công độ trễ thấp. JSON đồng bộ hợp hơn cho nhãn đơn. Hàng đợi hợp hơn khi tác vụ kéo dài phút/giờ và phải sống qua disconnect. SSE qua HTTP/2 cũng cần kiểm giới hạn concurrent stream của proxy/browser; “dùng HTTP” không có nghĩa mọi hạ tầng tự tương thích.",

vi_du: "### FastAPI phát event và dừng khi client rời đi\n\nModel giả sinh từng từ để code chạy được. Production thay `fake_llm` bằng adapter provider có cancellation.\n\n```python\nimport asyncio, json, uuid\nfrom collections.abc import AsyncIterator\nfrom fastapi import FastAPI, HTTPException, Request\nfrom fastapi.responses import StreamingResponse\nfrom pydantic import BaseModel, Field\n\napp = FastAPI()\n\nclass StreamIn(BaseModel):\n    prompt: str = Field(min_length=1, max_length=2_000)\n    max_tokens: int = Field(default=80, ge=1, le=256)\n\ndef sse(event: str, payload: dict, event_id: int | None = None) -> bytes:\n    lines = []\n    if event_id is not None: lines.append(f\"id: {event_id}\")\n    lines.append(f\"event: {event}\")\n    lines.append(\"data: \" + json.dumps(payload, ensure_ascii=False, separators=(\",\", \":\")))\n    return (\"\\n\".join(lines) + \"\\n\\n\").encode(\"utf-8\")\n\nasync def fake_llm(prompt: str, limit: int) -> AsyncIterator[str]:\n    answer = (\"Media Insight ghi nhận nội dung và đang tạo bản tóm tắt an toàn. \").split()\n    for word in answer[:limit]:\n        await asyncio.sleep(0.08)\n        yield word + \" \"\n\n@app.post(\"/v1/insights/stream\")\nasync def stream(payload: StreamIn, request: Request):\n    if not payload.prompt.strip():\n        raise HTTPException(422, detail={\"code\": \"blank_prompt\"})\n    request_id = str(uuid.uuid4())\n\n    async def events():\n        sequence = 0; completed = False\n        try:\n            yield sse(\"meta\", {\"request_id\": request_id, \"model_version\": \"demo-1\"}, sequence)\n            async for text in fake_llm(payload.prompt, payload.max_tokens):\n                if await request.is_disconnected():\n                    return\n                sequence += 1\n                yield sse(\"token\", {\"text\": text}, sequence)\n            sequence += 1\n            yield sse(\"done\", {\"finish_reason\": \"stop\"}, sequence)\n            completed = True\n        except asyncio.CancelledError:\n            raise\n        except Exception:\n            sequence += 1\n            yield sse(\"error\", {\"code\": \"generation_failed\", \"request_id\": request_id}, sequence)\n        finally:\n            # Đóng provider stream và ghi completed/disconnected vào metric tại đây.\n            _ = completed\n\n    return StreamingResponse(events(), media_type=\"text/event-stream\", headers={\n        \"Cache-Control\": \"no-cache, no-transform\",\n        \"X-Accel-Buffering\": \"no\",\n    })\n```\n\nFrontend dùng fetch vì endpoint là POST:\n\n```javascript\nconst response = await fetch(\"/v1/insights/stream\", {\n  method: \"POST\",\n  headers: {\"Content-Type\": \"application/json\", \"Authorization\": \"Bearer ...\"},\n  body: JSON.stringify({prompt: \"Tóm tắt media đã tải lên\", max_tokens: 80})\n});\nif (!response.ok) throw new Error(`HTTP ${response.status}`);\nconst reader = response.body.getReader();\nconst decoder = new TextDecoder(\"utf-8\");\nlet buffer = \"\", sawDone = false;\nwhile (true) {\n  const {value, done} = await reader.read();\n  buffer += decoder.decode(value || new Uint8Array(), {stream: !done});\n  const frames = buffer.split(\"\\n\\n\"); buffer = frames.pop();\n  for (const frame of frames) {\n    const event = frame.match(/^event: (.+)$/m)?.[1];\n    const data = JSON.parse(frame.match(/^data: (.+)$/m)?.[1] || \"{}\");\n    if (event === \"token\") document.querySelector(\"#output\").textContent += data.text;\n    if (event === \"done\") sawDone = true;\n  }\n  if (done) break;\n}\nif (!sawDone) console.warn(\"Stream kết thúc trước event done\");\n```\n\nParser demo giả định mỗi event chỉ có một dòng data do server của ta kiểm soát. Client SSE tổng quát phải hỗ trợ nhiều dòng `data:`, CRLF, comment và frame bị chia ở bất kỳ byte nào; production nên dùng parser đã kiểm thử thay vì regex tự viết.",

so_sanh: "### SSE, WebSocket và polling phục vụ nhu cầu khác nhau\n\n| Tiêu chí | SSE | WebSocket | Polling |\n|---|---|---|---|\n| Hướng chính | Server → client | Song công | Client hỏi định kỳ |\n| Hạ tầng | HTTP response dài | Upgrade protocol, connection dài | Request HTTP ngắn |\n| Reconnect | EventSource hỗ trợ cơ bản | Ứng dụng tự thiết kế | Tự nhiên ở lần poll sau |\n| Backpressure ứng dụng | Vẫn phải kiểm soát buffer | Phải tự thiết kế rõ | Queue server hấp thụ giữa poll |\n| Hợp với Media Insight | Token/tình trạng tăng dần | Audio live, lệnh stop/edit hai chiều | Job hàng đợi lâu |\n\nKhông nên chọn WebSocket chỉ vì nghe “real-time”: nó tăng protocol state, authentication khi reconnect và vận hành proxy. Không nên chọn SSE nếu client phải đẩy microphone liên tục. Polling tạo nhiều request nhưng rất bền với disconnect và đơn giản cho tác vụ phút/giờ. Tiêu chí là hướng dữ liệu và vòng đời, không phải công nghệ mới hơn.",

loi_thuong_gap: [
  "Yield chuỗi token trực tiếp; nhận biết token có newline làm client tách sai frame. JSON-encode payload và kết thúc event bằng dòng trống.",
  "Chỉ test localhost; nhận biết production nhận token thành từng cục lớn. Tắt proxy buffering/compression phù hợp và test qua ingress thật.",
  "Không hủy generation khi browser đóng; nhận biết GPU utilization cao nhưng completed stream thấp. Truyền cancellation tới provider.",
  "Dùng queue token vô hạn; nhận biết RAM tăng khi mạng client chậm. Đặt `maxsize` và chính sách timeout/hủy.",
  "Cho secret/prompt vào URL EventSource; nhận biết access log và browser history chứa dữ liệu. Dùng POST fetch hoặc ticket một lần scope hẹp.",
  "Coi EOF là thành công; nhận biết UI hiển thị đoạn cụt như kết quả hoàn chỉnh. Bắt buộc event `done` và theo dõi nó.",
  "Nuốt `CancelledError`; nhận biết stream ma không chịu dừng khi deploy. Re-raise cancellation và cleanup trong `finally`.",
  "Render partial Markdown thành HTML không sanitize; nhận biết DOM thay đổi bất thường/XSS. Hiển thị text hoặc sanitize đúng ngữ cảnh."
],

tu_kiem_tra: [
  "Tại sao TTFT giảm không chứng minh tổng chi phí hay tổng latency giảm?",
  "Một token chứa newline được đóng gói thế nào để không phá SSE framing?",
  "Response đã gửi 200 rồi model mới lỗi: server và client phải biểu diễn thất bại ra sao?",
  "Vì sao `Last-Event-ID` không tự khiến LLM resume đúng nội dung, và muốn replay thật cần lưu gì?",
  "Queue token giới hạn dung lượng truyền backpressure đến producer theo cơ chế nào?",
  "Thiết kế auth cho browser EventSource mà không để bearer token lâu dài trong query string.",
  "Moderation theo câu đổi TTFT và an toàn như thế nào; trường hợp sản phẩm nào chấp nhận đánh đổi ấy?"
],

tai_nguyen: [
  "MDN Web Docs — Using server-sent events và EventSource.",
  "HTML Living Standard — Server-sent events.",
  "FastAPI Documentation — StreamingResponse và xử lý client disconnect.",
  "Python Documentation — asynchronous generators, asyncio cancellation và Queue."
],

lien_he: [
  "Nối sang mảng Mạng máy tính: TCP backpressure, proxy buffering, keep-alive và timeout quyết định stream có thật sự tới từng phần hay không.",
  "Nối sang mảng Concurrent Python: async generator, bounded queue và cancellation structured concurrency giữ tài nguyên không bị rò.",
  "Nối sang mảng Frontend Web: incremental decoding, DOM rendering, AbortController và XSS quyết định trải nghiệm/an toàn ở phía nhận.",
  "Nối sang mảng Hệ thống phân tán: event ID, replay, idempotency và graceful shutdown xuất hiện khi stream phải sống qua reconnect/deploy."
]

};
