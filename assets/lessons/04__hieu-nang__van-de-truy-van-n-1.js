window.LESSONS["04:hieu-nang:van-de-truy-van-n-1"] = {

tom_tat: [
  "N+1 xảy ra khi ứng dụng chạy một query lấy N bản ghi cha rồi phát thêm một query cho từng bản ghi; chi phí nổi bật là N round trip và cạnh tranh connection, không chỉ là thời gian SQL.",
  "Một query lớn không luôn tốt hơn, nhưng số query tăng tuyến tính theo số hàng của trang là dấu hiệu thiết kế cần xem lại.",
  "Ba cách sửa chính là join/aggregate trong SQL, batch theo tập khóa, hoặc eager loading có kiểm soát; chọn theo hình dạng kết quả và khả năng phình số hàng.",
  "`EXPLAIN` từng query con có thể đều đẹp vì dùng index, trong khi endpoint vẫn chậm; phải đo query count và latency toàn request.",
  "Ví dụ tiếp tục trang 50 đơn `PAID`, lần này cần tên khách và tổng tiền nhưng code ngây thơ truy vấn riêng cho từng đơn."
],

can_biet_truoc: [
  "Quan hệ một-nhiều, khóa chính và khóa ngoại giữa `orders`, `customers`, `order_items`.",
  "Round trip mạng, connection pool và khái niệm latency so với throughput.",
  "SQL `JOIN`, aggregate, `GROUP BY`, `ANY(array)` hoặc `IN`.",
  "Cách đọc `Index Scan`, `Nested Loop`, `actual rows` và `loops` từ bài EXPLAIN ngay trước."
],

dinh_nghia: "**N+1 query problem** là mẫu truy cập dữ liệu trong đó chương trình chạy một truy vấn ban đầu lấy N đối tượng, rồi trong vòng lặp hoặc qua lazy loading chạy thêm một truy vấn cho từng đối tượng để lấy dữ liệu liên quan. Tổng số thường là `1 + N`; nếu truy vấn hai quan hệ riêng biệt cho mỗi đối tượng, nó thành `1 + 2N`.\n\nTên gọi mô tả số lượt nói chuyện với database, không khẳng định mọi query con đều chậm. Trái lại, chúng thường là những `Index Scan` rất nhanh. Endpoint chậm vì cộng dồn latency giao thức, parse/bind/execute, lấy connection, serialization và scheduling hàng chục hoặc hàng nghìn lần.",

vi_sao_can: "N+1 dễ lọt qua review vì code diễn đạt đúng nghiệp vụ: lấy danh sách đơn, rồi với từng đơn gọi repository lấy khách hàng và dòng hàng. Test có ba đơn chỉ phát bảy query nên vẫn nhanh. Production trả 100 đơn, latency database chỉ 3 ms mỗi lượt cũng biến phần chờ tuần tự thành hàng trăm millisecond trước khi tính công việc SQL; nếu nhiều request đồng thời, connection pool còn khuếch đại hàng đợi.\n\nĐiểm nguy hiểm là tối ưu từng query không giải quyết kiến trúc. `SELECT ... FROM order_items WHERE order_id=$1` có index đúng và plan dưới một millisecond vẫn bị gọi 50 lần. Thêm covering index có thể giảm chút I/O nhưng không loại 50 lần trao đổi. Bài EXPLAIN trước nhìn vào bên trong một câu SQL; bài này lùi camera ra toàn request để thấy số lần SQL được phát.\n\nN+1 cũng làm tải khó dự báo. Đổi page size từ 20 lên 100 không chỉ tăng dữ liệu năm lần mà tăng số query quan hệ năm lần. Một màn hình vô tình hiển thị 500 bản ghi có thể chiếm pool lâu, gây timeout cho endpoint không liên quan. Vì vậy query count nên được xem như một ngân sách có test, tương tự số request HTTP đi ra dịch vụ ngoài.",

co_che: "### Một request biến thành 101 cuộc hội thoại\n\nGiả sử query đầu trả 50 đơn. Code tiếp tục lấy một customer và một tổng tiền cho mỗi đơn: `1 + 2×50 = 101` query. Mỗi lượt có các bước lấy connection, gửi Parse/Bind/Execute hoặc truy vấn text, database lập/tra plan, chạy, đóng gói hàng, truyền về và giải mã. Prepared statement giảm parse nhưng không xóa round trip.\n\nNếu mọi query chạy tuần tự, latency cộng gần tuyến tính. Nếu tung 100 promise song song, phép cộng không biến mất: pool 10 connection chia chúng thành nhiều đợt, database phải scheduling nhiều tác vụ nhỏ, và request này tranh tài nguyên với request khác. “Parallelize N+1” thường đổi độ trễ cá nhân thành áp lực toàn hệ thống.\n\n### Lazy loading che giấu điểm phát query\n\nORM có thể trả proxy cho `order.customer`. Lần truy cập thuộc tính đầu tiên mới query. Template `for order in orders` trông như chỉ đọc object nhưng thực tế chạm database. Serializer tự duyệt quan hệ còn nguy hiểm hơn vì query phát ngoài service/repository, nơi reviewer không chờ đợi I/O.\n\nCách nhận biết không phải cấm ORM mà là bật SQL logging trong test, gắn trace span cho từng query, và đếm theo request. Dấu vân tay điển hình là cùng SQL lặp lại nhiều lần, chỉ tham số `order_id` hoặc `customer_id` đổi. APM có thể nhóm normalized query để thấy pattern này.\n\n### Vì sao index đẹp vẫn không cứu được\n\nVới `order_items(order_id)`, từng query con có thể là `Index Scan` chạm vài block. Nhưng 50 lần vẫn lặp protocol và có thể chạm lại các page. Database tối ưu tốt nhất khi nhìn thấy tập công việc: `WHERE order_id = ANY($1)` cho planner cơ hội dùng bitmap/index scan một lần, gom I/O và trả toàn bộ dòng để ứng dụng nhóm.\n\nĐây là khác biệt giữa độ phức tạp tính toán và số lần I/O từ xa. Cả phương án lặp và batch có thể đọc O(N) item, nhưng một bên có O(N) round trip, bên kia O(1) round trip cho một trang. Big-O giống nhau ở số hàng không có nghĩa latency giống nhau.\n\n### Sửa bằng một query aggregate\n\nKhi API cần đúng một hàng cho mỗi order, SQL có thể trả đúng hình dạng ấy:\n\n```sql\nWITH page AS MATERIALIZED (\n  SELECT id, customer_id, created_at\n  FROM orders\n  WHERE status = 'PAID'\n  ORDER BY created_at DESC, id DESC\n  LIMIT 50\n)\nSELECT p.id, p.created_at, c.name,\n       COALESCE(sum(oi.quantity * oi.unit_price), 0) AS total\nFROM page p\nJOIN customers c ON c.id = p.customer_id\nLEFT JOIN order_items oi ON oi.order_id = p.id\nGROUP BY p.id, p.created_at, c.name\nORDER BY p.created_at DESC, p.id DESC;\n```\n\nChọn trang trước tránh aggregate toàn lịch sử. `LEFT JOIN` giữ order rỗng; `COALESCE` biến `NULL` thành 0 theo hợp đồng API. Index từ bài trước phục vụ page, còn `order_items(order_id)` phục vụ join. Đây là một round trip và database chịu trách nhiệm grouping.\n\n### Sửa bằng batch khi một query thành cồng kềnh\n\nĐôi khi ORM khó ánh xạ aggregate, hoặc ứng dụng cần item chi tiết. Giữ query trang, lấy danh sách `order_ids`, rồi chạy một query cho mọi item và một query cho customers chưa có trong cache:\n\n```sql\nSELECT order_id, id, quantity, unit_price\nFROM order_items\nWHERE order_id = ANY($1::bigint[])\nORDER BY order_id, id;\n```\n\nỨng dụng nhóm kết quả bằng map theo `order_id`. Số query trở thành hằng số, thường 2–3 cho một trang, dù N thay đổi. Với đầu vào rỗng, bỏ hẳn batch query hoặc truyền mảng rỗng đã ép kiểu; không tạo `IN ()` vì cú pháp đó không hợp lệ.\n\nBatch cũng cần giới hạn. Mảng hàng trăm nghìn khóa làm câu query, bộ nhớ và kết quả quá lớn. Chia chunk là đúng cho job nền, nhưng endpoint phân trang nên giới hạn N ngay từ đầu. Nếu batch theo composite key, có thể dùng `VALUES` hoặc unnest hai mảng cẩn thận thay vì nối chuỗi SQL.\n\n### Eager loading và hai kiểu phình dữ liệu\n\nORM thường cung cấp eager loading bằng join hoặc truy vấn phụ theo tập khóa. Join một quan hệ nhiều item khiến order bị lặp cho từng item. Nếu đồng thời join hai collection, số hàng có thể nhân chéo: một order có 4 items và 3 payments tạo 12 hàng trước khi ORM khử trùng. Đây là cartesian explosion, mặt trái của “gộp tất cả vào một query”.\n\nSplit query/batch thường tốt hơn cho nhiều collection: một query orders, một query items cho tập order, một query payments. Ba round trip cố định có thể tốt hơn một result set phình hàng trăm lần. Mục tiêu không phải số query thấp nhất bằng mọi giá, mà là số query bị chặn trên và lượng dữ liệu hợp lý.\n\n### DataLoader: batch trong một cửa sổ ngắn\n\nTrong GraphQL hoặc resolver độc lập, DataLoader gom nhiều yêu cầu `load(customer_id)` xuất hiện trong cùng tick thành một batch, rồi cache theo phạm vi request. Nó giữ kiến trúc resolver mà tránh query từng khóa. Cache phải theo request: cache toàn cục dễ trả dữ liệu cũ, trộn quyền tenant hoặc tăng bộ nhớ không giới hạn.\n\nBatch function phải trả kết quả đúng thứ tự khóa đầu vào và biểu diễn khóa không tồn tại, thường bằng `null` hoặc lỗi ở vị trí tương ứng. Query SQL không bảo đảm thứ tự theo mảng `IN`, nên cần map kết quả về khóa, không zip mù quáng.\n\n### Connection pool không phải máy nhân tốc độ\n\nPool tái sử dụng connection và giới hạn concurrency để bảo vệ database. N+1 giữ hoặc mượn connection lặp lại, làm `wait time` tăng. Tăng pool từ 10 lên 100 có thể giảm hàng đợi trong ứng dụng nhưng đẩy 100 tác vụ đồng thời vào PostgreSQL, gây cache churn và context switching. Đó là di chuyển nút thắt, không xóa công việc.\n\nTheo dõi ba đại lượng cùng nhau: query count/request, thời gian chờ pool, và tổng thời gian database. Query count tăng theo page size là bằng chứng trực tiếp; pool saturation là hậu quả ở tải cao; plan của normalized query cho biết mỗi lượt làm gì.\n\n### Transaction và ảnh chụp dữ liệu\n\nNhiều query riêng có thể quan sát dữ liệu ở các thời điểm khác nhau dưới `READ COMMITTED`: order được lấy trước, item bị thêm trước query con, nên tổng tiền không khớp ảnh chụp ban đầu. Một query duy nhất nhìn một statement snapshot nhất quán. Bọc N+1 trong transaction có thể thống nhất hơn tùy isolation, nhưng giữ transaction lâu và không xử lý round trip.\n\nBatch vài query trong transaction là đánh đổi: ảnh chụp rõ hơn nhưng cần giữ transaction ngắn. Không gọi API bên ngoài giữa các query. Nếu nghiệp vụ yêu cầu total đã chốt, cân nhắc lưu total trên order và duy trì bằng logic transaction; đó là denormalization có chủ đích, không chỉ là tối ưu đọc.\n\n### Đo và khóa lỗi bằng test\n\nTest hiệu năng bằng thời gian dễ nhiễu; test query count thường ổn định. Với endpoint 50 đơn, có thể assert tối đa ba hoặc bốn SQL tùy thiết kế. Đừng assert đúng chuỗi SQL quá chặt vì ORM upgrade có thể đổi alias. Hook datasource/interceptor đếm statement trong phạm vi request và bỏ query setup khỏi bộ đếm.\n\nKết hợp test cardinality: 0, 1, 50 order phải có query count không tăng tuyến tính. Sau đó dùng load test để đo latency và pool, vì query-count test không phát hiện một query gộp đã trở thành quá nặng. Hai lớp kiểm tra bắt hai thất bại khác nhau.\n\n### Khi nào không nên gộp thành một query\n\nKhông join mọi thứ khi result set nhân chéo, payload chứa cột lớn lặp lại, hoặc quan hệ hiếm khi được dùng. Không eager load collection vô hạn. Với cache customer hiệu quả và chỉ cần một vài customer miss, batch các miss có thể hợp lý hơn join. Với CQRS/read model, một bảng đã tổng hợp có thể phù hợp cho dashboard nhiều hơn query động phức tạp.\n\nNgược lại, đừng dùng “có cache” để bỏ qua N+1 mà không đo miss rate và cold start. Đừng đánh đổi 101 query nhỏ thành một query khóa bảng hoặc aggregate hàng triệu dòng; chọn page trước và đọc plan. Sửa đúng là làm lộ kích thước tập dữ liệu, chặn số round trip và kiểm soát lượng hàng trung gian.",

vi_du: "### Bản tái hiện bằng Node.js và PostgreSQL\n\nVí dụ dùng package `pg`; schema và dữ liệu lấy từ bài EXPLAIN. Bản lỗi cố ý tuần tự để nhìn rõ N+1:\n\n```js\nimport pg from 'pg';\nconst pool = new pg.Pool({ connectionString: process.env.DATABASE_URL });\n\nasync function listOrdersN1(limit = 50) {\n  const client = await pool.connect();\n  let queryCount = 0;\n  const q = async (text, params) => {\n    queryCount++;\n    return client.query(text, params);\n  };\n  try {\n    const { rows: orders } = await q(`\n      SELECT id, customer_id, created_at\n      FROM orders WHERE status = 'PAID'\n      ORDER BY created_at DESC, id DESC LIMIT $1`, [limit]);\n\n    for (const order of orders) {\n      const customer = await q(\n        'SELECT name FROM customers WHERE id = $1', [order.customer_id]);\n      const total = await q(`\n        SELECT COALESCE(sum(quantity * unit_price), 0) AS total\n        FROM order_items WHERE order_id = $1`, [order.id]);\n      order.customer_name = customer.rows[0]?.name ?? null;\n      order.total = total.rows[0].total;\n    }\n    return { orders, queryCount };\n  } finally {\n    client.release();\n  }\n}\n```\n\nVới N hàng trả về, bộ đếm cho `1 + 2N`; đây là suy ra từ code, không phải benchmark. `N=0` chỉ có một query. Customer bị thiếu cho `customer_id` hợp lệ không xảy ra nhờ foreign key, nhưng optional relation cần xử lý `null`.\n\n### Bản batch ba query\n\n```js\nasync function listOrdersBatch(limit = 50) {\n  const client = await pool.connect();\n  try {\n    const { rows: orders } = await client.query(`\n      SELECT id, customer_id, created_at\n      FROM orders WHERE status = 'PAID'\n      ORDER BY created_at DESC, id DESC LIMIT $1`, [limit]);\n    if (orders.length === 0) return [];\n\n    const orderIds = orders.map(o => o.id);\n    const customerIds = [...new Set(orders.map(o => o.customer_id))];\n    const [{ rows: customers }, { rows: totals }] = await Promise.all([\n      client.query('SELECT id, name FROM customers WHERE id = ANY($1::bigint[])', [customerIds]),\n      client.query(`\n        SELECT order_id, COALESCE(sum(quantity * unit_price), 0) AS total\n        FROM order_items WHERE order_id = ANY($1::bigint[])\n        GROUP BY order_id`, [orderIds])\n    ]);\n\n    const customerById = new Map(customers.map(x => [String(x.id), x]));\n    const totalByOrder = new Map(totals.map(x => [String(x.order_id), x.total]));\n    return orders.map(o => ({\n      ...o,\n      customer_name: customerById.get(String(o.customer_id))?.name ?? null,\n      total: totalByOrder.get(String(o.id)) ?? '0'\n    }));\n  } finally {\n    client.release();\n  }\n}\n```\n\nĐoạn này có tối đa ba query, nhưng `Promise.all` trên cùng một `pg.Client` không đem lại hai connection song song; dùng nó không phải lý do cải thiện. Cải thiện đến từ batch. Nếu cần concurrency thật phải mượn hai connection, thường không đáng cho hai query ngắn và làm tăng tải pool.\n\nChạy `EXPLAIN (ANALYZE, BUFFERS)` cho hai batch query. Xác minh index `order_items_order_id_idx` và primary key khách hàng được dùng khi tập khóa nhỏ. Khi limit rất lớn, planner có thể chọn scan khác; đó không tự động là regression.",

so_sanh: "### Bốn chiến lược tải quan hệ\n\n| Chiến lược | Số round trip theo N | Ưu điểm | Rủi ro / không nên dùng |\n|---|---:|---|---|\n| Lazy load trong vòng lặp | 1 + N hoặc 1 + kN | Code cục bộ đơn giản, chỉ tải khi chạm | Latency tuyến tính, query ẩn, pool bão hòa |\n| Một query `JOIN` + aggregate | 1 | Snapshot một statement, database nhóm tốt | Join nhiều collection làm phình số hàng; SQL/ORM mapping phức tạp |\n| Batch theo tập khóa | Hằng số theo số quan hệ | Tránh nhân chéo, giữ item chi tiết | Phải nhóm kết quả, giới hạn kích thước mảng và xử lý khóa thiếu |\n| Denormalized read model | 1 query đơn giản | Dashboard đọc nhanh, plan ổn định | Ghi phức tạp, dữ liệu có thể trễ, cần cơ chế đồng bộ |\n\nKhông đặt mục tiêu “luôn đúng một query”. Hai hoặc ba query có kích thước bị chặn thường tốt hơn một join tạo tích Descartes. Tiêu chuẩn là latency, lượng hàng truyền, tính nhất quán cần thiết và khả năng dự báo khi N tăng.",

loi_thuong_gap: [
  "Chỉ chạy `EXPLAIN` query con rồi kết luận endpoint nhanh; nhận biết bằng hàng chục span SQL giống nhau. Hãy đo query count trên toàn request.",
  "Dùng `Promise.all` cho hàng trăm query để “sửa” N+1; dấu hiệu là pool wait và timeout tăng. Batch khóa thay vì phóng đại concurrency.",
  "Eager join nhiều collection cùng lúc; nhận biết số hàng SQL lớn hơn tích số phần tử quan hệ và payload lặp cột order.",
  "Batch bằng cách nối ID vào chuỗi SQL; dấu hiệu là câu lệnh khó parameterize và có rủi ro injection. Dùng `ANY($1::bigint[])` hoặc bảng tạm phù hợp.",
  "Không xử lý tập khóa rỗng; nhận biết code sinh `IN ()` hoặc chạy query vô ích. Trả sớm trước batch.",
  "Cache quan hệ toàn cục không có tenant/request scope; dấu hiệu là dữ liệu cũ, rò quyền hoặc bộ nhớ tăng. Xác định vòng đời và invalidation.",
  "Assert thời gian rất nhỏ trong unit test nhưng không assert số query; máy CI nhiễu và dataset bé che N+1. Đếm statement theo request.",
  "Gộp thành aggregate toàn bảng rồi mới giới hạn; plan cho thấy hàng triệu item đi vào aggregate. Chọn trang order trước."
],

tu_kiem_tra: [
  "Trang có N order và code lấy hai quan hệ bằng hai query riêng mỗi order sẽ phát bao nhiêu query? Điều gì thay đổi khi N=0?",
  "Vì sao 50 `Index Scan` tốt vẫn có thể thua một bitmap/index scan theo tập khóa?",
  "Khi nào ba query batch hợp lý hơn một query join duy nhất?",
  "Vì sao tăng connection pool có thể làm latency database tệ hơn dù hàng đợi ứng dụng ngắn lại?",
  "Một DataLoader nhận `[7, 2, 9]` nhưng SQL trả hàng theo `[2, 7]`; batch function phải làm gì với thứ tự và khóa 9?",
  "Khó: join orders với items và payments làm 10 order biến thành 600 hàng. Hãy suy ra cardinality quan hệ có thể gây ra kết quả ấy và đề xuất split query.",
  "Khó: batch query count không đổi nhưng endpoint vẫn chậm khi page size tăng. Bạn sẽ kiểm tra trường nào trong plan và metric nào ở ứng dụng?"
],

tai_nguyen: [
  "PostgreSQL Documentation — Performance Tips và Indexes.",
  "Patterns of Enterprise Application Architecture — Martin Fowler, các mẫu Data Mapper và Lazy Load.",
  "Designing Data-Intensive Applications — Martin Kleppmann.",
  "High Performance PostgreSQL for Rails — Andrew Atkinson."
],

lien_he: [
  "Nối sang mảng Mạng máy tính: latency mỗi round trip tích lũy dù payload nhỏ, giống gọi nhiều HTTP service tuần tự.",
  "Nối sang mảng Concurrency: pool là semaphore giới hạn tài nguyên; tăng promise không tạo thêm năng lực database.",
  "Nối sang mảng Thiết kế API: page size và trường quan hệ client yêu cầu quyết định ngân sách query và payload.",
  "Nối sang mảng Kiểm thử: instrumentation đếm SQL tạo regression test ít nhiễu, còn load test xác minh queueing dưới tải."
]

};
