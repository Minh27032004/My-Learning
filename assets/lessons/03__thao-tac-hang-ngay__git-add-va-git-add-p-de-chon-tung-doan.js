window.LESSONS["03:thao-tac-hang-ngay:git-add-va-git-add-p-de-chon-tung-doan"] = {

tom_tat: [
  "`git add` chép nội dung được chọn từ cây làm việc vào index để xây snapshot của commit kế tiếp; nó không gửi lên remote và cũng không “theo dõi mọi thay đổi tương lai” của file.",
  "Pathspec quyết định đường dẫn nào được xét, còn tùy chọn như `-u`, `-A` và `--intent-to-add` thay đổi phạm vi; lệnh rộng phải đi kèm review bằng `git diff --cached`.",
  "`git add -p` tách diff thành các hunk (đoạn thay đổi) và hỏi từng đoạn, nhờ đó một file chứa sửa lỗi lẫn refactor vẫn có thể được chia thành các commit có một mục đích.",
  "Hunk là sản phẩm của thuật toán diff và context, không trùng tuyệt đối với hàm hoặc ý tưởng; dùng `s` để tách, `e` để sửa patch khi cần, và bỏ cuộc nếu việc chọn trở nên khó kiểm chứng.",
  "Trong “Sổ việc”, ta stage riêng sửa lỗi chuẩn hóa tiêu đề, để lại tính năng nhãn và log debug cho commit sau; hai diff trước/sau staging chứng minh ranh giới đó."
],

can_biet_truoc: [
  "Đọc được `git status` dạng dài và hai cột XY, đặc biệt các trạng thái ` M`, `M `, `MM`, `??`, ` D`.",
  "Phân biệt `HEAD`, index và worktree; hiểu `git diff` xem phần chưa stage còn `git diff --cached` xem đề xuất commit.",
  "Biết `.gitignore` lọc ứng viên untracked nhưng có thể bị vượt qua có chủ đích bằng `git add -f`; force không biến secret thành an toàn.",
  "Đọc được unified diff: dòng context bắt đầu bằng khoảng trắng, dòng xóa bằng `-`, dòng thêm bằng `+`, và header `@@` xác định vùng."
],

dinh_nghia: "`git add <pathspec>` cập nhật index bằng nội dung hiện tại của các đường dẫn khớp: file mới trở thành staged addition, file sửa trở thành staged modification, và tùy phạm vi, việc xóa được ghi vào snapshot đề xuất. `git add -p [pathspec]` chuyển sang chế độ tương tác theo patch, trình bày từng hunk rồi chỉ áp dụng các hunk người dùng chấp nhận vào index.\n\n“Add” dễ gây hiểu nhầm vì nó không chỉ thêm file mới; tên chính xác về mặt cơ chế gần với “cập nhật index”. Sau mỗi lần add, chỉnh sửa tiếp không tự đi theo. Commit đọc index, không đọc thẳng editor hay toàn bộ worktree.",

vi_sao_can: "Lập trình hiếm khi diễn ra đúng ranh giới commit. Trong lúc sửa lỗi parse tiêu đề, bạn thấy cơ hội thêm nhãn, formatter đổi một đoạn khác, và câu log tạm xuất hiện. Nếu commit tất cả, review khó hiểu và revert sửa lỗi cũng kéo theo tính năng chưa hoàn thiện. Index cho phép biến một worktree lộn xộn thành snapshot có luận điểm rõ.\n\nChọn theo file đủ khi mỗi file chỉ chứa một mục đích. Nhưng “Sổ việc” có `src/tasks.js` chứa cả bug fix và tính năng labels; `git add -p` cho phép chọn bug fix mà không cần copy phần còn lại ra nơi khác. Giá trị không nằm ở lịch sử “đẹp” cho hình thức: commit nhỏ, độc lập làm review chính xác hơn, bisect khoanh lỗi tốt hơn và revert ít tác dụng phụ hơn.\n\nĐánh đổi là index có thể khác worktree, làm người dùng quên phần chưa stage hoặc stage nhầm một nửa thay đổi phụ thuộc. Vì vậy partial staging chỉ an toàn khi xem staged diff, chạy kiểm tra trên snapshot thích hợp và đọc lại status. Nếu các hunk phụ thuộc chặt, nên refactor quy trình làm việc thay vì ép tách.",

co_che: "### Index là snapshot đề xuất, không phải túi tên file\n\nKhi chạy `git add src/tasks.js`, Git đọc nội dung file lúc đó, tạo blob nếu cần và cập nhật entry đường dẫn trong index. Một phút sau bạn gõ thêm dòng log; index vẫn giữ blob trước đó. Status chuyển thành `MM`: X vì index khác `HEAD`, Y vì worktree khác index.\n\nĐiều này giải thích vì sao câu “tôi đã add file rồi” không đảm bảo mọi nội dung hiện trên màn hình sẽ vào commit. Câu hỏi đúng là “index hiện chứa phiên bản nào?”. Trả lời bằng `git diff --cached`, không bằng ký ức về thời điểm gõ lệnh.\n\n### Pathspec không đơn thuần là tên file\n\nĐối số của `git add` là pathspec, có thể là file, thư mục hoặc pattern Git hiểu. Chạy từ thư mục con ảnh hưởng cách đường dẫn tương đối được diễn giải, dù Git vẫn tìm repository root. Dùng dấu `--` để ngăn tên đường dẫn bắt đầu bằng dấu gạch bị hiểu như tùy chọn:\n\n```sh\ngit add -- src/tasks.js test/\n```\n\nQuote pattern khi muốn Git, không phải shell, xử lý wildcard. Với thao tác quan trọng, liệt kê file cụ thể dễ audit hơn pattern rộng. `git status --short` trước và `git diff --cached --name-status` sau tạo thành cặp kiểm soát phạm vi.\n\n### Khác nhau giữa `.`, `-u` và `-A`\n\nPhạm vi add có lịch sử hành vi giữa phiên bản Git, nhưng mô hình hiện đại cần nắm:\n\n| Lệnh | File mới | File tracked sửa | File tracked xóa | Phạm vi |\n|---|---:|---:|---:|---|\n| `git add file` | có nếu chỉ định | có | ghi nhận nếu pathspec khớp | pathspec |\n| `git add -u` | không | có | có | tracked trong phạm vi |\n| `git add -A` | có | có | có | mọi thay đổi trong phạm vi quy định |\n| `git add .` | có | có | có theo hành vi Git hiện đại | từ thư mục hiện tại xuống |\n\n`-A` tiện cho thay đổi cơ học đã review, nhưng nguy hiểm khi worktree có secret hoặc file thử nghiệm chưa ignore. `-u` hữu ích khi chỉ muốn cập nhật các file đã tracked và loại trừ file mới. Dùng rộng không sai; dùng rộng mà không xem staged diff mới là sai quy trình.\n\n### Từ diff tới hunk\n\nDiff nhóm các dòng thay đổi gần nhau cùng context thành hunk. Header như `@@ -3,4 +3,7 @@` mô tả vùng ở bản cũ và mới. Hunk không biết “đây là sửa lỗi” hay “đây là tính năng”; nó chỉ dựa trên khoảng cách dòng và thuật toán diff. Hai ý định nằm sát nhau có thể vào cùng hunk, còn một ý định trải xa có thể thành nhiều hunk.\n\nVì vậy `add -p` là công cụ hỗ trợ quyết định, không tự hiểu semantic. Người dùng phải đọc cả dòng context và biết phần được chọn có biên dịch độc lập hay không.\n\n### Vòng hỏi tương tác cơ bản\n\nChạy:\n\n```sh\ngit add -p -- src/tasks.js\n```\n\nGit hiển thị một hunk và prompt. Các lựa chọn quan trọng thường gồm `y` stage hunk, `n` bỏ qua, `q` thoát, `a` stage hunk này và các hunk sau trong file, `d` bỏ hunk này và phần sau, `s` thử tách hunk, `e` sửa patch thủ công, `?` xem trợ giúp. Bộ lựa chọn có thể thay đổi theo ngữ cảnh; hãy đọc prompt hiện tại.\n\nNhấn `y` chỉ cập nhật index cho đoạn đó. Worktree vẫn chứa toàn bộ file mới. Vì thế sau phiên tương tác, file thường mang `MM`, hoàn toàn dự kiến.\n\n### Tách hunk bằng `s`\n\nKhi một hunk chứa hai cụm thay đổi có đủ khoảng context ở giữa, `s` yêu cầu Git chia nhỏ. Sau khi tách, bạn chọn riêng từng cụm. Nếu Git báo không thể split, hai thay đổi quá gần theo cấu trúc diff hiện tại.\n\nCó thể thử giảm context qua chế độ tương tác hoặc dùng `e`, nhưng đây cũng là tín hiệu thiết kế: hai thay đổi sát nhau có thể phụ thuộc. Đôi khi cách an toàn là sửa worktree tạm thời để mỗi ý định thành hunk rõ, stage, rồi khôi phục phần tiếp theo; stash hoặc editor hỗ trợ, nhưng phải tránh mất dữ liệu.\n\n### Sửa patch bằng `e` và ý nghĩa của các dòng\n\nEdit hunk mở một patch tạm. Để không stage một dòng thêm mới, xóa dòng `+` khỏi patch. Để không stage một dòng bị xóa, đổi tiền tố `-` thành khoảng trắng, biến nó thành context. Không tùy tiện sửa nội dung context; patch phải áp dụng sạch lên bản index.\n\nĐây là thao tác mạnh nhưng dễ tạo snapshot không tồn tại nguyên vẹn trong worktree. Sau khi edit, luôn xem `git diff --cached` và nếu có thể chạy test trên staged snapshot. Nếu chưa đọc thành thạo unified diff, chọn `n`, quay lại chỉnh file rõ ràng hơn là đoán.\n\n### File mới và `--intent-to-add`\n\nFile hoàn toàn untracked chưa có bản nền trong index, nên trải nghiệm patch có thể không như file tracked. `git add -N path` hay `--intent-to-add` tạo entry biểu thị ý định thêm mà chưa stage toàn nội dung, giúp diff và partial add nhìn file như thay đổi từ rỗng.\n\n```sh\ngit add -N test/new.test.js\ngit diff -- test/new.test.js\ngit add -p -- test/new.test.js\n```\n\nĐây không phải commit file rỗng. Nó thay đổi metadata index để Git có thể trình bày diff. Status và diff sẽ cho thấy trạng thái đặc biệt; review kỹ trước commit.\n\n### Xóa và đổi tên trong staging\n\n`git add -u` ghi nhận sửa đổi và xóa của tracked files. `git rm` vừa xóa worktree vừa stage deletion; `git mv` thực chất tiện ích cho di chuyển rồi cập nhật, trong khi rename cuối cùng vẫn được suy ra khi so snapshot.\n\nPartial staging một rename kèm sửa nội dung có thể khó đọc vì similarity detection chỉ diễn ra trên snapshot đang so. Nếu lịch sử cần rõ, commit rename thuần trước rồi sửa nội dung sau, miễn việc chia này không phá build hoặc quy định nhóm. Đó là trường hợp commit cơ học riêng giúp reviewer phân biệt chuyển vị trí với thay logic.\n\n### Bỏ stage không đồng nghĩa bỏ sửa đổi\n\nSau khi add nhầm, mục tiêu thường là đưa index về `HEAD` nhưng giữ worktree. Lệnh hiện đại:\n\n```sh\ngit restore --staged src/tasks.js\n```\n\nNó khác `git restore src/tasks.js`, vốn có thể ghi nội dung từ index xuống worktree và làm mất chỉnh sửa chưa lưu. Status thường gợi ý cú pháp phù hợp. Với repository chưa có commit đầu tiên, `HEAD` chưa tồn tại nên một số cách unstage có hành vi/giới hạn khác; `git rm --cached` hoặc lệnh phù hợp cần được chọn sau khi đọc trợ giúp và status.\n\n### Review snapshot trước commit\n\nBa kiểm tra tối thiểu:\n\n```sh\ngit status --short\ngit diff --cached --check\ngit diff --cached\n```\n\n`--check` tìm một số lỗi whitespace và conflict marker, không thay test. Diff cached là nội dung commit dự kiến. Nếu thấy `.env`, log debug hay phần labels chưa hoàn thiện, unstage hoặc dùng patch để điều chỉnh. Sau commit, kiểm lại status để không bỏ quên phần worktree quan trọng.\n\n### Test đúng thứ mình sắp commit\n\nMột rủi ro tinh tế của partial staging: test chạy trên worktree đầy đủ có thể pass nhờ phần chưa stage, trong khi snapshot index không chạy được. Ví dụ bug fix gọi helper labels chỉ tồn tại ở hunk bị bỏ lại. Test worktree không chứng minh commit độc lập.\n\nCách chắc chắn tùy quy trình: dùng một worktree tạm từ tree được ghi từ index, stash phần unstaged có kiểm soát rồi test, hoặc công cụ hook hỗ trợ temporarily hiding unstaged changes. Các cách này có chi phí và rủi ro thao tác; tối thiểu hãy đọc staged diff theo dependency và tránh tách hunk làm code cú pháp dở dang.\n\n### `git add -p` không thay thế thiết kế commit từ đầu\n\nPatch staging cứu một phiên làm việc hỗn hợp, nhưng nếu ngày nào cũng phải edit hunk phức tạp, nguyên nhân có thể là task quá rộng, hàm quá dính hoặc thiếu checkpoint. Làm từng thay đổi nhỏ, test rồi stage ngay thường dễ hơn phân loại hàng trăm dòng cuối ngày.\n\nKhông nên partial stage generated file, lockfile hoặc migration khi chỉ chọn vài dòng làm artifact không còn tương ứng với source. Với file mà công cụ yêu cầu tính toàn vẹn, stage toàn bộ phiên bản sinh từ đúng source hoặc không stage.\n\n### Ca biên và bẫy dữ liệu\n\nBinary file không có hunk dòng hữu ích. Thay đổi line ending có thể biến cả file thành một hunk khổng lồ. Formatter chạy sau add tạo `MM` và có thể khiến staged snapshot chưa format. Submodule entry đại diện commit khác chứ không phải nội dung file thường. Conflict có nhiều stage entries trong index; `add` sau khi giải quyết mang nghĩa đánh dấu kết quả đã resolved.\n\nĐối với secret, `git add -f` chỉ vượt ignore, không đưa ra cảnh báo bảo mật. Nếu phải force-add file mẫu bị pattern rộng che, trước hết cân nhắc sửa pattern để ý định được lưu và review, thay vì dựa vào force mỗi lần.\n\n### Khi nào không nên dùng partial staging\n\nKhông tách khi hunk phụ thuộc làm snapshot không build; khi file do máy sinh phải đồng bộ nguyên khối; khi đang giải conflict mà chưa hiểu các phía; hoặc khi edit patch vượt khả năng kiểm chứng. Có thể tạo commit tạm cục bộ rồi interactive rebase sau, nhưng lịch sử rewriting cũng có rủi ro nếu đã chia sẻ. Phương án đơn giản nhất thường là thu hẹp thay đổi ngay trong editor.",

vi_du: "### Chuẩn bị hai ý định trong cùng `tasks.js`\n\n```sh\nmkdir add-lab && cd add-lab\ngit init --initial-branch=main\nmkdir src test\ncat > src/tasks.js <<'JS'\nexport function normalizeTitle(title) {\n  return title.trim();\n}\n\nexport function addTask(title) {\n  return { title: normalizeTitle(title), done: false };\n}\nJS\ngit add src/tasks.js\ngit -c user.name=\"Hoc Git\" -c user.email=\"hoc@example.invalid\" commit -m \"Tạo thao tác thêm việc\"\n```\n\nSau đó sửa file thành:\n\n```js\nexport function normalizeTitle(title) {\n  if (typeof title !== \"string\") throw new TypeError(\"title must be a string\");\n  return title.trim().replace(/\\s+/g, \" \");\n}\n\nexport function addTask(title) {\n  return { title: normalizeTitle(title), done: false, labels: [] };\n}\n\nexport function addLabel(task, label) {\n  console.debug(\"adding label\", label);\n  return { ...task, labels: [...task.labels, label] };\n}\n```\n\nÝ định thứ nhất là sửa lỗi đầu vào và khoảng trắng trong `normalizeTitle`. Ý định thứ hai thêm labels, còn `console.debug` chưa nên commit. Chạy `git add -p src/tasks.js`, chọn `y` cho hunk normalize và `n` cho hunk labels. Nếu diff gộp, thử `s`; cấu trúc có dòng trống thường cho phép tách.\n\n### Xác minh ranh giới thay vì tin lựa chọn vừa bấm\n\n```sh\ngit status --short\ngit diff --cached -- src/tasks.js\ngit diff -- src/tasks.js\ngit diff --cached --check\n```\n\nStatus dự kiến là `MM src/tasks.js`. Cached diff chỉ được chứa kiểm tra kiểu và chuẩn hóa nhiều khoảng trắng. Unstaged diff chứa trường `labels`, hàm `addLabel` và log debug. Nếu cached diff gọi `addLabel` nhưng helper lại ở unstaged, phải sửa ranh giới vì commit không độc lập.\n\n### Thử file mới theo từng đoạn\n\n```sh\ncat > test/tasks.test.js <<'JS'\nimport test from \"node:test\";\nimport assert from \"node:assert/strict\";\nimport { normalizeTitle } from \"../src/tasks.js\";\n\ntest(\"gộp khoảng trắng\", () => {\n  assert.equal(normalizeTitle(\"  học   Git  \"), \"học Git\");\n});\n\ntest(\"thử nghiệm chưa hoàn tất\", () => {\n  // TODO: viết assertion cho labels\n});\nJS\n\ngit add -N test/tasks.test.js\ngit add -p test/tasks.test.js\n```\n\nChọn test hoàn chỉnh, bỏ test TODO nếu hunk tách được. Sau cùng xem staged diff toàn repository. Nếu hai test nằm sát và không split an toàn, hãy xóa tạm TODO khỏi worktree, stage file sạch, rồi thêm lại TODO sau; dễ kiểm chứng hơn edit patch mù.",

so_sanh: "### Chọn cách stage theo hình dạng thay đổi\n\n| Cách | Độ chi tiết | Ưu điểm | Rủi ro | Tình huống phù hợp |\n|---|---|---|---|---|\n| `git add file` | toàn file | rõ, nhanh | gom nhiều ý định cùng file | file chỉ có một mục đích |\n| `git add -u` | tracked paths | ghi nhận sửa/xóa, bỏ file mới | quên file mới cần thiết | cleanup tracked files |\n| `git add -A` | toàn phạm vi | snapshot đồng bộ rộng | dễ cuốn rác/secret | thay đổi cơ học đã review |\n| `git add -p` | từng hunk | chia commit theo ý định | snapshot có thể không chạy | file chứa thay đổi độc lập |\n| `git add -N` rồi `-p` | dòng trong file mới | partial stage file untracked | trạng thái intent dễ gây nhầm | file mới có phần hoàn tất riêng |\n\n### Stage, stash và commit tạm giải quyết vấn đề khác nhau\n\n| Công cụ | Mục tiêu chính | Worktree sau thao tác | Lịch sử công khai |\n|---|---|---|---|\n| staging | thiết kế snapshot kế | vẫn có thể chứa phần chưa stage | chưa đổi |\n| stash | cất tạm thay đổi khỏi worktree | thường sạch theo lựa chọn | không phải branch công khai |\n| commit cục bộ | tạo checkpoint có object commit | có thể sạch | có lịch sử cục bộ, có thể chỉnh trước chia sẻ |\n\nKhông dùng stash thay cho review commit, cũng không dùng partial staging để giấu code phụ thuộc. Chọn công cụ theo việc cần xây snapshot, dọn bàn làm việc hay tạo điểm phục hồi.",

loi_thuong_gap: [
  "Tưởng add là cờ theo dõi mọi sửa đổi tương lai; sau add lại sửa file nhưng commit thiếu phần mới. Dấu hiệu là `MM`; xem cả hai diff và add lại nếu thật sự muốn toàn bộ.",
  "Dùng `git add .` từ thư mục con rồi tưởng đã stage cả repository; đường dẫn ngoài cây con có thể bị bỏ. Xác minh bằng `git diff --cached --name-only`, hoặc dùng phạm vi rõ từ root.",
  "Chọn hunk theo màu xanh/đỏ mà không đọc context; snapshot staged gọi hàm chưa tồn tại. Kiểm dependency và test đúng snapshot index.",
  "Nhấn `s` nhưng Git không tách vì hai cụm quá gần; tiếp tục dùng `e` dù chưa hiểu patch khiến apply lỗi hoặc stage sai. Quay lại editor để tạo ranh giới rõ hơn.",
  "Stage generated file một phần; artifact không còn tương ứng source và lần build sau ghi đè. Những file có tính toàn vẹn nên cập nhật nguyên khối theo pipeline.",
  "Add force `.env` vì bị ignore; lệnh thành công nhưng secret đi vào index. `git diff --cached` và secret scanning phải chặn trước commit; tốt hơn là không force.",
  "Dùng `git restore file` khi chỉ muốn unstage; worktree bị ghi đè và mất sửa đổi. Dùng `git restore --staged file` cho mục tiêu đưa index về mốc mà giữ file làm việc."
],

tu_kiem_tra: [
  "Sau `git add file`, bạn thêm một dòng nữa. Hãy mô tả chính xác ba phiên bản HEAD/index/worktree và hai diff sẽ hiển thị gì.",
  "Vì sao hunk không phải đơn vị semantic? Cho ví dụ hai ý định bị gộp và một ý định bị chia thành nhiều hunk.",
  "Trong edit patch, vì sao bỏ một dòng thêm cần xóa dòng `+`, còn bỏ một dòng xóa cần đổi `-` thành context? Giải thích theo phép biến đổi từ index.",
  "Partial staging pass test trên worktree nhưng commit lại fail. Hãy dựng một dependency cụ thể gây hiện tượng và đề xuất cách test snapshot staged.",
  "So sánh `git add -u`, `git add -A` và pathspec cụ thể khi repository có file mới, file sửa, file xóa cùng secret untracked.",
  "Khi nào commit rename riêng trước sửa logic giúp review, và khi nào chia như vậy tạo snapshot không build nên không đáng làm?"
],

tai_nguyen: [
  "Sách “Pro Git” của Scott Chacon và Ben Straub, phần Recording Changes to the Repository và Interactive Staging.",
  "Tài liệu đi kèm Git: `git help add`, `git help diff`, `git help restore` và `git helpglossary` phần pathspec.",
  "Sách “Git in Practice” của Mike McQuaid, các kỹ thuật tạo commit dễ review và sửa lịch sử cục bộ.",
  "Sách “Version Control with Git” của Jon Loeliger và Matthew McCullough, phần index, blobs và commit construction."
],

lien_he: [
  "Nối sang mảng Kiểm thử: partial staging tạo khác biệt giữa code đang test trong worktree và code sắp commit, nên cần tư duy fixture/môi trường kiểm thử đúng snapshot.",
  "Nối sang mảng Cấu trúc dữ liệu: index ánh xạ path tới blob, còn content-addressing khiến add nội dung tạo object bất biến và tái sử dụng object trùng.",
  "Nối sang mảng Thiết kế phần mềm: nếu thay đổi semantic không thể tách thành hunk độc lập, đó có thể là tín hiệu coupling cao hoặc task decomposition chưa tốt.",
  "Nối sang mảng Bảo mật: staging là điểm kiểm soát cuối trước commit; ignore, staged diff và secret scanning tạo nhiều lớp phòng vệ thay vì tin một pattern duy nhất."
]

};
