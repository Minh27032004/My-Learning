window.LESSONS["02:phan-tich-do-phuc-tap:do-phuc-tap-khong-gian"] = {

tom_tat: [
  "**Độ phức tạp không gian đếm bộ nhớ THÊM, không tính dữ liệu đầu vào.** Một hàm nhận list n phần tử và chỉ dùng ba biến là O(1) không gian — dù đầu vào chiếm n ô.",
  "**Ngăn xếp lời gọi cũng là bộ nhớ.** Hàm đệ quy sâu n tầng dùng O(n) không gian dù không tạo biến nào — và đây là chỗ hay bị bỏ sót nhất.",
  "**Generator đổi O(n) không gian thành O(1)** bằng cách sinh từng phần tử thay vì dựng cả danh sách — kỹ thuật quan trọng nhất của bài trong thực tế Python.",
  "**Đánh đổi bộ nhớ ↔ thời gian là hai chiều.** Thêm bảng nhớ giảm thời gian (`@cache`); tính lại thay vì lưu giảm bộ nhớ. Không có lựa chọn nào luôn đúng.",
  "**Bộ nhớ thường là ràng buộc CỨNG hơn thời gian.** Chậm thì phải chờ; hết RAM thì chương trình bị hệ điều hành giết."
],

can_biet_truoc: [
  "Bài **Ký hiệu Big-O** và **Vòng lặp lồng nhau** ngay trước — cùng cách đếm, áp cho bộ nhớ.",
  "Bài **Stack và heap** ở module 2 mảng 01 — ngăn xếp lời gọi nằm ở đâu và vì sao nó có giới hạn.",
  "Bài **Generator, iterator và yield** ở module 5 mảng 01 — cơ chế sinh giá trị theo yêu cầu.",
  "Bài **Đo bộ nhớ và tối ưu cơ bản** ở module 7 mảng 01 — `tracemalloc`, `sys.getsizeof`, và chi phí thật của object Python."
],

dinh_nghia: "Nói đời thường: chép một cuốn sách. Bạn có thể đọc từng trang rồi chép ngay từng trang — chỉ cần chỗ cho một trang trên bàn. Hoặc photo cả cuốn trước rồi mới chép — cần chỗ cho cả cuốn. Cùng kết quả, khác hẳn diện tích bàn cần có.\n\nCác khái niệm:\n\n- **Độ phức tạp không gian** — lượng bộ nhớ **thêm** mà thuật toán cần, biểu diễn theo kích thước đầu vào n.\n- **Không gian phụ trợ** (*auxiliary space*) — bộ nhớ thêm, **không** tính dữ liệu đầu vào. Đây là thứ ta thường muốn nói.\n- **Tổng không gian** (*total space*) — bao gồm cả đầu vào. Ít dùng, vì đầu vào là thứ bạn không chọn được.\n- **Ngăn xếp lời gọi** (*call stack*) — vùng bộ nhớ lưu các khung hàm đang dở dang. Mỗi lời gọi lồng thêm một khung.\n- **Tại chỗ** (*in-place*) — thuật toán sửa trực tiếp dữ liệu đầu vào, dùng O(1) không gian phụ trợ.\n- **Đánh đổi bộ nhớ–thời gian** (*space–time tradeoff*) — dùng thêm bộ nhớ để chạy nhanh hơn, hoặc ngược lại.\n\nMột phân biệt quyết định cách trả lời mọi câu hỏi về không gian:\n\n```\n\"Độ phức tạp không gian của hàm này?\"\n\n  → Hầu như luôn có nghĩa là KHÔNG GIAN PHỤ TRỢ.\n\n    def tong(ds):           # ds có n phần tử\n        kq = 0\n        for x in ds:\n            kq += x\n        return kq\n\n  Phụ trợ: O(1)   ← chỉ biến kq và x\n  Tổng:    O(n)   ← tính cả ds\n\n  ⇒ Trả lời đúng: O(1).\n```\n\nLý do quy ước này hợp lý: **đầu vào là thứ bạn buộc phải có**, không phải lựa chọn thiết kế. Cái đánh giá được là bạn dùng **thêm** bao nhiêu.",

vi_sao_can: "Bộ nhớ hay bị coi là chuyện phụ so với thời gian, nhưng thực tế nó thường nghiêm trọng hơn:\n\n- **Hết bộ nhớ là lỗi CỨNG, chậm chỉ là bất tiện.** Thuật toán chậm thì người dùng chờ; thuật toán ngốn RAM thì tiến trình bị hệ điều hành giết (`MemoryError`, hoặc bị OOM killer trên Linux) và mất luôn công việc đang làm.\n- **Bộ nhớ đắt hơn CPU khi triển khai.** Trên máy chủ thuê, nâng RAM thường tốn hơn nâng CPU. Một thuật toán O(n) không gian với n = 100 triệu có thể vượt hạn mức container.\n- **Đệ quy sâu là bẫy phổ biến nhất.** Python giới hạn độ sâu khoảng 1000; một hàm đệ quy trên danh sách 10.000 phần tử sẽ `RecursionError` dù thuật toán về lý thuyết đúng.\n- **Generator là kỹ thuật Python đặc trưng để đổi bậc không gian.** Đọc file 50 GB bằng `readlines()` là không thể; bằng vòng lặp trên file object là bình thường. Cùng logic, khác bậc không gian.\n- **Đánh đổi bộ nhớ–thời gian là quyết định thiết kế thường gặp nhất.** `@cache` ở bài trước đổi O(2ⁿ) thời gian thành O(n) — nhưng phải trả bằng O(n) bộ nhớ. Biết cả hai phía mới quyết định được.\n\nTình huống thật rất kinh điển: bạn viết script xử lý file log 20 GB, mở đầu bằng `dong = open(f).readlines()`. Trên máy phát triển bạn test với file mẫu 10 MB, chạy tốt. Lên máy chủ có 4 GB RAM, script bị giết ngay khi khởi động. **Đổi một dòng — duyệt trực tiếp `for dong in open(f)` — làm bộ nhớ từ O(n) xuống O(1)** và script chạy được với file bao nhiêu cũng được.\n\nMột lưu ý về cân bằng: bài này không nói \"luôn tối ưu bộ nhớ\". Với dữ liệu nhỏ, một bảng tra O(n) làm code đơn giản hơn nhiều và bộ nhớ không thành vấn đề. Nguyên tắc vẫn là **biết mình đang đánh đổi cái gì**.",

co_che: "### Đếm không gian phụ trợ\n\nQuy trình giống bài trước, nhưng đếm **ô bộ nhớ** thay vì **thao tác**:\n\n```\n① Xác định n\n② Liệt kê mọi thứ thuật toán TẠO RA (không tính đầu vào)\n③ Kích thước mỗi thứ theo n\n④ Cộng lại, rút gọn\n⑤ ĐỪNG QUÊN ngăn xếp lời gọi nếu có đệ quy\n```\n\nBước ⑤ là bước bị bỏ sót nhiều nhất, và là phần đặc trưng của độ phức tạp không gian.\n\n### O(1) — không gian hằng số\n\n```python\ndef tong(ds: list[int]) -> int:\n    kq = 0                      # 1 ô\n    for x in ds:                # 1 ô cho x\n        kq += x\n    return kq\n# Phụ trợ: O(1)  — số biến KHÔNG phụ thuộc n\n```\n\n```python\ndef dao_nguoc_tai_cho(ds: list[int]) -> None:\n    \"\"\"Đảo ngược danh sách TẠI CHỖ. O(1) không gian phụ trợ.\"\"\"\n    trai, phai = 0, len(ds) - 1\n    while trai < phai:\n        ds[trai], ds[phai] = ds[phai], ds[trai]    # ⭐ hoán vị, không tạo list mới\n        trai += 1\n        phai -= 1\n```\n\n```python\n# So sánh: cùng kết quả, khác bậc KHÔNG GIAN\ndao_v1 = ds[::-1]               # O(n) — tạo list MỚI\ndao_v2 = list(reversed(ds))     # O(n) — tạo list MỚI\ndao_nguoc_tai_cho(ds)           # O(1) — sửa tại chỗ\nds.reverse()                     # O(1) — cách có sẵn, tại chỗ\n```\n\nBốn dòng này minh hoạ một mẫu quan trọng trong Python: **phương thức đổi tên theo quy ước phản ánh việc nó có tại chỗ hay không.**\n\n```\nds.sort()      TẠI CHỖ, trả None,   O(1) phụ trợ (thực tế Timsort dùng O(n))\nsorted(ds)     TẠO MỚI, trả list,   O(n)\nds.reverse()   TẠI CHỖ, trả None,   O(1)\nreversed(ds)   TRẢ ITERATOR,        O(1)  ⭐ không tạo list\nlist(reversed(ds))  tạo list,        O(n)\n```\n\nQuy ước đặt tên: **động từ trần (`sort`, `reverse`) là tại chỗ và trả `None`; dạng khác (`sorted`, `reversed`) không sửa gốc.** Nối về bài đặt tên ở module 8 mảng 01 — tên nói rõ hành vi.\n\n### O(n) — không gian tuyến tính\n\n```python\ndef binh_phuong(ds: list[int]) -> list[int]:\n    kq = []                     # ⚠️ list này lớn dần tới n\n    for x in ds:\n        kq.append(x * x)\n    return kq\n# Phụ trợ: O(n)\n```\n\n```python\n# Cùng bậc — comprehension không đổi không gian\nkq = [x * x for x in ds]        # O(n)\n```\n\n```python\n# ⭐ O(1) — generator: sinh từng giá trị, KHÔNG dựng list\ndef binh_phuong_gen(ds):\n    for x in ds:\n        yield x * x\n\n# Hoặc biểu thức generator\nkq = (x * x for x in ds)        # O(1) — chú ý dấu NGOẶC TRÒN\n```\n\nĐây là kỹ thuật quan trọng nhất của bài. Cơ chế:\n\n```\nLIST COMPREHENSION  [...]   tính HẾT ngay, lưu HẾT      → O(n)\nGENERATOR           (...)   tính TỪNG cái khi được hỏi   → O(1)\n```\n\n⚠️ Nhưng generator có ba giới hạn thật, phải biết trước khi dùng:\n\n```\n① CHỈ DUYỆT ĐƯỢC MỘT LẦN — duyệt lần hai được rỗng\n② KHÔNG có len(), không truy cập theo chỉ số\n③ Nếu cuối cùng vẫn cần cả danh sách thì không tiết kiệm gì\n```\n\nPhép kiểm: **generator đúng khi bạn xử lý từng phần tử rồi bỏ.** Nếu bạn cần sắp xếp, đếm độ dài, hay truy cập ngẫu nhiên, bạn buộc phải có cả danh sách.\n\n### Ngăn xếp lời gọi — bộ nhớ vô hình\n\n```python\ndef tong_de_quy(ds: list[int], i: int = 0) -> int:\n    if i >= len(ds):\n        return 0\n    return ds[i] + tong_de_quy(ds, i + 1)\n```\n\n```\nKhông tạo biến nào lớn, nhưng:\n\n  tong_de_quy(ds, 0)  ← khung 1 còn dở, chờ kết quả\n    tong_de_quy(ds, 1)  ← khung 2\n      tong_de_quy(ds, 2)  ← khung 3\n        ...\n          tong_de_quy(ds, n)  ← khung n\n\nn khung tồn tại CÙNG LÚC  ⇒  O(n) không gian\n```\n\nSo với vòng lặp:\n\n```python\ndef tong_vong_lap(ds: list[int]) -> int:\n    kq = 0\n    for x in ds:\n        kq += x\n    return kq\n# O(1) không gian — MỘT khung duy nhất\n```\n\n```\nCÙNG O(n) thời gian, khác hẳn không gian:\n  đệ quy:   O(n)\n  vòng lặp: O(1)\n```\n\nHệ quả rất thực tế trong Python: giới hạn độ sâu đệ quy.\n\n```python\nimport sys\nprint(sys.getrecursionlimit())      # thường là 1000\n\ntong_de_quy(list(range(10_000)))    # RecursionError\n```\n\n⚠️ **Đừng nâng giới hạn để chữa.** `sys.setrecursionlimit(100000)` có thể làm tiến trình **crash cứng** (segmentation fault) vì ngăn xếp thật của hệ điều hành cạn — mất luôn khả năng bắt lỗi. Cách đúng là **chuyển sang vòng lặp** hoặc dùng ngăn xếp tường minh.\n\n```python\n# ✓ Ngăn xếp tường minh: đổi bộ nhớ ngăn xếp HĐH sang bộ nhớ heap\ndef duyet_cay_khong_de_quy(goc):\n    \"\"\"Duyệt cây bằng ngăn xếp tường minh — không giới hạn độ sâu.\"\"\"\n    if goc is None:\n        return\n    ngan_xep = [goc]\n    while ngan_xep:\n        nut = ngan_xep.pop()             # O(1)\n        xu_ly(nut)\n        if nut.phai:\n            ngan_xep.append(nut.phai)\n        if nut.trai:\n            ngan_xep.append(nut.trai)\n```\n\nBậc không gian **vẫn** là O(chiều cao cây), nhưng bộ nhớ giờ ở heap (list Python) chứ không ở ngăn xếp hệ điều hành — nên không bị giới hạn 1000 tầng.\n\n### Không gian của đệ quy chia đôi\n\nĐiểm tinh tế mà nhiều người tính sai:\n\n```python\ndef merge_sort(ds: list[int]) -> list[int]:\n    if len(ds) <= 1:\n        return ds[:]\n    giua = len(ds) // 2\n    trai = merge_sort(ds[:giua])       # ⚠️ ds[:giua] tạo list mới\n    phai = merge_sort(ds[giua:])\n    return tron(trai, phai)             # ⚠️ tron tạo list mới\n```\n\n```\nĐỘ SÂU NGĂN XẾP: log n  (chia đôi)\n\nNHƯNG bộ nhớ cho các list con:\n  Tầng 0: n phần tử\n  Tầng 1: n phần tử (hai nửa cộng lại)\n  ...\n\n⚠️ Điểm quan trọng: các nhánh KHÔNG tồn tại cùng lúc.\n   Python tính merge_sort(trái) XONG rồi mới tính merge_sort(phải).\n   Tại một thời điểm, chỉ có MỘT đường từ gốc xuống lá.\n\n  Đường đó có: n + n/2 + n/4 + ... = 2n phần tử\n\n⇒ O(n) không gian, KHÔNG phải O(n log n)\n```\n\nTổng cấp số nhân `n + n/2 + n/4 + … = 2n` là chỗ dễ tính sai. Nếu bạn nghĩ \"log n tầng, mỗi tầng n phần tử ⇒ O(n log n)\" thì sai — vì các tầng **không** tồn tại đồng thời.\n\nSo với quicksort tại chỗ:\n\n```\nmerge sort:      O(n) không gian     — cần mảng phụ để trộn\nquicksort:       O(log n) không gian — chỉ ngăn xếp đệ quy\nheapsort:        O(1) không gian     — hoàn toàn tại chỗ\n\n⇒ Cùng O(n log n) thời gian, ba bậc không gian khác nhau.\n  Đây là lý do heapsort được chọn khi bộ nhớ rất chật.\n```\n\n### Đánh đổi bộ nhớ ↔ thời gian\n\nĐánh đổi đi **cả hai chiều**, và đó là điều đáng nhớ nhất của bài.\n\n**Chiều 1 — thêm bộ nhớ để nhanh hơn:**\n\n```python\n# O(n²) thời gian, O(1) không gian\ndef co_cap_tong_v1(ds, target):\n    for i in range(len(ds)):\n        for j in range(i + 1, len(ds)):\n            if ds[i] + ds[j] == target:\n                return True\n    return False\n\n# O(n) thời gian, O(n) không gian\ndef co_cap_tong_v2(ds, target):\n    da_thay = set()                 # ⚠️ tốn O(n) bộ nhớ\n    for x in ds:\n        if target - x in da_thay:\n            return True\n        da_thay.add(x)\n    return False\n```\n\n```\nĐỔI: O(n) bộ nhớ  →  giảm từ O(n²) xuống O(n) thời gian.\nGần như luôn đáng — trừ khi n quá lớn để chứa set.\n```\n\n**Chiều 2 — tính lại để tiết kiệm bộ nhớ:**\n\n```python\n# O(n) không gian — lưu mọi giá trị\ndef fib_bang(n: int) -> int:\n    bang = [0] * (n + 1)\n    bang[1] = 1\n    for i in range(2, n + 1):\n        bang[i] = bang[i - 1] + bang[i - 2]\n    return bang[n]\n\n# ⭐ O(1) không gian — chỉ giữ HAI giá trị gần nhất\ndef fib_hai_bien(n: int) -> int:\n    if n <= 1:\n        return n\n    a, b = 0, 1\n    for _ in range(2, n + 1):\n        a, b = b, a + b             # ⭐ chỉ cần hai số trước\n    return b\n```\n\n```\nCÙNG O(n) thời gian. Không gian: O(n) → O(1).\n\nVÌ SAO ĐƯỢC: fib(i) chỉ cần fib(i−1) và fib(i−2).\n             Các giá trị cũ hơn KHÔNG bao giờ dùng lại.\n```\n\nĐây là kỹ thuật **cửa sổ trượt** ở tầng bộ nhớ, và nó là bước tối ưu chuẩn của dynamic programming — sẽ gặp lại ở module 8.\n\nPhép kiểm để biết có rút được xuống O(1) không: **bước i cần bao nhiêu bước trước đó?** Nếu chỉ cần k bước gần nhất với k là hằng số, bạn giữ k biến là đủ.\n\n### Chi phí không gian ẩn\n\nGiống chi phí thời gian ẩn ở bài trước, có những dòng trông vô hại mà cấp phát O(n):\n\n```\nTHAO TÁC                        KHÔNG GIAN     GHI CHÚ\n──────────────────────────      ───────────    ──────────────────────\nds[:]                           O(n)           copy toàn bộ\nds[a:b]                         O(b−a)         cắt lát là COPY\nds1 + ds2                       O(n+m)         list MỚI\nlist(x), dict(x), set(x)        O(n)           vật chất hoá\nsorted(ds)                      O(n)           list mới\nds.copy()                       O(n)\n\", \".join(ds)                   O(tổng độ dài)\nf.readlines()                   O(kích thước file)   ⚠️ bẫy lớn nhất\nrange(n)                        O(1)           ⭐ KHÔNG tạo list (Python 3)\nzip(a, b), map(f, a)            O(1)           ⭐ trả iterator\nenumerate(ds)                   O(1)           ⭐ iterator\nreversed(ds)                    O(1)           ⭐ iterator\n(x for x in ds)                 O(1)           ⭐ generator\n```\n\nSáu dòng cuối là công cụ chính để giữ không gian O(1) trong Python. Điểm mấu chốt: **`range`, `zip`, `map`, `enumerate`, `reversed` đều trả iterator, không trả list** — nên chúng không cấp phát theo n.\n\n⚠️ Bẫy đi kèm: bọc chúng trong `list()` là mất hết lợi ích.\n\n```python\nfor i in range(1_000_000): ...          # O(1) không gian\nfor i in list(range(1_000_000)): ...    # ⚠️ O(n) — vô cớ\n```\n\n### Đọc dữ liệu lớn — O(n) và O(1)\n\n```python\n# ❌ O(kích thước file) — nạp HẾT vào RAM\ndef dem_loi_v1(duong_dan: str) -> int:\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        cac_dong = f.readlines()        # ⚠️ file 20 GB → chết\n    return sum(1 for d in cac_dong if \"ERROR\" in d)\n```\n\n```python\n# ✓ O(độ dài dòng dài nhất) ≈ O(1) — duyệt từng dòng\ndef dem_loi_v2(duong_dan: str) -> int:\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        return sum(1 for dong in f if \"ERROR\" in dong)   # ⭐ f là iterator\n```\n\nFile object của Python **là** một iterator theo dòng — nên `for dong in f` đọc từng dòng, giữ trong bộ nhớ đúng một dòng. Đây là ví dụ rõ nhất cho việc **đổi một dòng code làm đổi bậc không gian**.\n\nCùng nguyên tắc cho dữ liệu theo lô:\n\n```python\nfrom typing import Iterator\n\ndef doc_theo_lo(duong_dan: str, kich_thuoc_lo: int = 1000) -> Iterator[list[str]]:\n    \"\"\"Đọc file theo lô. O(kich_thuoc_lo) không gian, không phụ thuộc file.\"\"\"\n    lo: list[str] = []\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        for dong in f:\n            lo.append(dong.rstrip(\"\\n\"))\n            if len(lo) >= kich_thuoc_lo:\n                yield lo\n                lo = []                  # ⭐ list MỚI, không dùng lại\n    if lo:\n        yield lo                          # lô cuối, có thể ngắn hơn\n```\n\nChi tiết `lo = []` thay vì `lo.clear()` quan trọng: người gọi giữ tham chiếu tới lô vừa `yield`, nên xoá nó sẽ làm dữ liệu của họ biến mất — đúng loại bug về tham chiếu đã học ở module 7 mảng 01.\n\n### Bộ nhớ thật của object Python\n\nBig-O nói \"O(n) ô\", nhưng một ô Python đắt hơn nhiều so với một ô C — nối về bài `PyObject` header ở module 7 mảng 01:\n\n```python\nimport sys\n\nprint(sys.getsizeof(0))                    # int nhỏ: khoảng 28 byte\nprint(sys.getsizeof([]))                   # list rỗng: khoảng 56 byte\nprint(sys.getsizeof([0] * 1000))           # list 1000 con trỏ\n\n# ⚠️ getsizeof KHÔNG đệ quy — chỉ đo vỏ, không đo phần tử bên trong\nds = [[0] * 100 for _ in range(100)]\nprint(sys.getsizeof(ds))                    # chỉ vỏ ngoài, không phải tổng thật\n```\n\nBa cách giảm bộ nhớ thật mà **không** đổi bậc:\n\n```\n① array.array / numpy   lưu số thô, không phải PyObject\n② __slots__             bỏ __dict__ của instance\n③ generator             không vật chất hoá\n\n⇒ Cả ba giảm HẰNG SỐ (có thể hàng chục lần), không đổi BẬC.\n```\n\n```python\nimport array\n\nds_python = [i for i in range(1_000_000)]        # ~8 MB con trỏ + ~28 MB int\nds_array = array.array(\"i\", range(1_000_000))    # ~4 MB — số thô 4 byte\n```\n\nĐây là ranh giới đáng nhớ: **Big-O chọn thuật toán; cấu trúc lưu trữ giảm hằng số.** Cả hai đều cần, và cái sau chỉ đáng làm sau khi đã đo.\n\n### Khi nào chấp nhận O(n) không gian\n\nPhản biện cần thiết — bài này không nói \"luôn tối ưu bộ nhớ\":\n\n```\nCHẤP NHẬN O(n) không gian khi:\n· n nhỏ và biết chắc có giới hạn trên\n· Nó giảm được một BẬC thời gian (set thay vòng lặp lồng)\n· Code rõ ràng hơn hẳn\n· Cần duyệt dữ liệu NHIỀU LẦN (generator chỉ duyệt một lần)\n· Cần len(), sắp xếp, hay truy cập ngẫu nhiên\n\nPHẢI giữ O(1) khi:\n· Dữ liệu lớn hơn RAM (file log, luồng dữ liệu)\n· n không có giới hạn trên biết trước\n· Chạy trong môi trường hạn chế bộ nhớ (container nhỏ, thiết bị nhúng)\n· Xử lý luồng liên tục không có điểm kết thúc\n```\n\nDòng \"cần duyệt nhiều lần\" là giới hạn thực tế hay bị quên nhất của generator, và nó gây một lớp bug im lặng — phần ví dụ sẽ minh hoạ.",

vi_du: "### Đếm không gian — sáu ví dụ\n\n```python\n# ── O(1) ──\ndef tim_max(ds: list[int]) -> int:\n    if not ds:\n        raise ValueError(\"danh sách rỗng\")\n    lon_nhat = ds[0]                # 1 ô\n    for x in ds[1:]:                # ⚠️ ds[1:] tạo COPY → O(n)!\n        if x > lon_nhat:\n            lon_nhat = x\n    return lon_nhat\n# ⚠️ Phụ trợ: O(n) vì cắt lát — KHÔNG phải O(1) như trông\n```\n\n```python\n# ✓ Thật sự O(1) — dùng iterator, không cắt lát\ndef tim_max_v2(ds: list[int]) -> int:\n    it = iter(ds)\n    try:\n        lon_nhat = next(it)\n    except StopIteration:\n        raise ValueError(\"danh sách rỗng\") from None\n    for x in it:                    # ⭐ iterator, không copy\n        if x > lon_nhat:\n            lon_nhat = x\n    return lon_nhat\n```\n\nVí dụ đầu là bẫy tinh vi đáng chú ý: `for x in ds[1:]` trông như duyệt bình thường, nhưng `ds[1:]` **cấp phát một list mới n−1 phần tử**. Đây là chi phí không gian ẩn — song song với chi phí thời gian ẩn ở bài trước.\n\n```python\n# ── O(n) — tạo cấu trúc mới ──\ndef dem_tan_suat(van_ban: str) -> dict[str, int]:\n    dem: dict[str, int] = {}\n    for tu in van_ban.split():      # ⚠️ split() cũng tạo list O(n)\n        dem[tu] = dem.get(tu, 0) + 1\n    return dem\n# Phụ trợ: O(số từ) cho list của split + O(số từ KHÁC NHAU) cho dict\n#        = O(n)\n```\n\n```python\n# ── O(n) — ngăn xếp đệ quy ──\ndef dem_nut(nut) -> int:\n    if nut is None:\n        return 0\n    return 1 + dem_nut(nut.trai) + dem_nut(nut.phai)\n# Phụ trợ: O(chiều cao cây)\n#   Cây cân bằng:  O(log n)\n#   Cây suy biến (như danh sách): O(n)   ⚠️ và có thể RecursionError\n```\n\n```python\n# ── O(n²) — ma trận ──\ndef bang_nhan(n: int) -> list[list[int]]:\n    return [[i * j for j in range(n)] for i in range(n)]\n# Phụ trợ: O(n²)\n```\n\n### Đệ quy so với vòng lặp — cùng thời gian, khác không gian\n\n```python\n# ── ĐỆ QUY: O(n) thời gian, O(n) không gian ──\ndef tong_de_quy(ds: list[int], i: int = 0) -> int:\n    if i >= len(ds):\n        return 0\n    return ds[i] + tong_de_quy(ds, i + 1)\n\n# ── VÒNG LẶP: O(n) thời gian, O(1) không gian ──\ndef tong_vong_lap(ds: list[int]) -> int:\n    kq = 0\n    for x in ds:\n        kq += x\n    return kq\n```\n\n```python\nimport sys\n\nprint(sys.getrecursionlimit())        # 1000\n\nds = list(range(5_000))\nprint(tong_vong_lap(ds))               # 12497500 — chạy tốt\ntong_de_quy(ds)                        # RecursionError\n```\n\n```\nRecursionError: maximum recursion depth exceeded\n\n⚠️ Đây KHÔNG phải lỗi logic — thuật toán đúng.\n   Đây là giới hạn KHÔNG GIAN của ngăn xếp lời gọi.\n```\n\n⚠️ Cách chữa **sai** và cách chữa **đúng**:\n\n```python\n# ❌ SAI: nâng giới hạn\nimport sys\nsys.setrecursionlimit(100_000)\ntong_de_quy(list(range(50_000)))\n# → có thể CRASH CỨNG (segmentation fault) vì ngăn xếp HĐH cạn.\n#   Crash cứng KHÔNG bắt được bằng try/except — mất luôn dữ liệu đang xử lý.\n\n# ✓ ĐÚNG: chuyển sang vòng lặp\ndef tong_vong_lap(ds): ...\n\n# ✓ ĐÚNG: ngăn xếp tường minh (khi thuật toán bản chất là đệ quy)\ndef duyet_cay(goc) -> list:\n    \"\"\"Duyệt cây preorder. Bộ nhớ ở HEAP, không giới hạn 1000 tầng.\"\"\"\n    if goc is None:\n        return []\n    kq = []\n    ngan_xep = [goc]\n    while ngan_xep:\n        nut = ngan_xep.pop()\n        kq.append(nut.gia_tri)\n        if nut.phai:                  # ⭐ đẩy PHẢI trước\n            ngan_xep.append(nut.phai)\n        if nut.trai:                  # ⭐ để TRÁI được lấy ra trước\n            ngan_xep.append(nut.trai)\n    return kq\n```\n\nChi tiết thứ tự đẩy vào ngăn xếp là chỗ dễ sai: ngăn xếp là LIFO, nên muốn xử lý trái trước thì phải đẩy phải vào trước. Đây là loại chi tiết mà **viết test biên sẽ bắt được**:\n\n```python\nclass Nut:\n    def __init__(self, gia_tri, trai=None, phai=None):\n        self.gia_tri, self.trai, self.phai = gia_tri, trai, phai\n\n#       1\n#      / ╲\n#     2    3\n#    / ╲\n#   4   5\ncay = Nut(1, Nut(2, Nut(4), Nut(5)), Nut(3))\n\nassert duyet_cay(cay) == [1, 2, 4, 5, 3]    # preorder\nassert duyet_cay(None) == []                 # biên: cây rỗng\nassert duyet_cay(Nut(9)) == [9]              # biên: một nút\n```\n\n### Generator — đổi bậc không gian\n\n```python\n# ❌ O(n) không gian\ndef doc_va_loc_v1(duong_dan: str) -> list[str]:\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        cac_dong = f.readlines()              # ⚠️ nạp HẾT\n    return [d for d in cac_dong if \"ERROR\" in d]   # ⚠️ list thứ HAI\n```\n\n```python\n# ✓ O(1) không gian\nfrom typing import Iterator\n\ndef doc_va_loc_v2(duong_dan: str) -> Iterator[str]:\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        for dong in f:                         # ⭐ f là iterator theo dòng\n            if \"ERROR\" in dong:\n                yield dong\n\n# Dùng\nfor dong in doc_va_loc_v2(\"app.log\"):\n    print(dong, end=\"\")\n```\n\n```\nFile 20 GB:\n  v1: cần ~20 GB RAM  →  MemoryError hoặc bị OOM killer giết\n  v2: cần ~vài KB     →  chạy bình thường\n\nCÙNG kết quả, CÙNG O(n) thời gian, khác hẳn O(n) vs O(1) không gian.\n```\n\n⚠️ **Ba giới hạn của generator — và bug im lặng chúng gây ra:**\n\n```python\ngen = doc_va_loc_v2(\"app.log\")\n\nso_dong = sum(1 for _ in gen)         # duyệt lần 1 — OK\nprint(so_dong)                         # 42\n\nfor dong in gen:                       # ⚠️ duyệt lần 2 — RỖNG\n    print(dong)                        # KHÔNG in gì cả, KHÔNG báo lỗi\n```\n\n```\n⚠️ Đây là bug IM LẶNG — không exception, chỉ là kết quả rỗng.\n   Loại bug tệ nhất theo bài xử lý lỗi (module 8 mảng 01).\n```\n\n```python\n# ✓ Nếu cần duyệt nhiều lần: vật chất hoá MỘT lần, có ý thức\ncac_dong = list(doc_va_loc_v2(\"app.log\"))    # O(n) — nhưng CHỦ Ý\nprint(len(cac_dong))\nfor dong in cac_dong:                         # duyệt lại được\n    print(dong, end=\"\")\n\n# ✓ Hoặc dùng itertools.tee nếu chỉ cần hai lượt và dữ liệu vừa phải\nimport itertools\na, b = itertools.tee(doc_va_loc_v2(\"app.log\"), 2)\n# ⚠️ tee LƯU ĐỆM phần chênh lệch giữa hai iterator → có thể tốn O(n)\n```\n\nDòng cảnh báo về `tee` đáng chú ý: nó **không** miễn phí. Nếu bạn duyệt hết `a` rồi mới duyệt `b`, `tee` phải giữ toàn bộ dữ liệu — mất hết lợi ích.\n\n### Rút O(n) xuống O(1) bằng cửa sổ trượt\n\n```python\n# ── O(n) không gian — lưu cả bảng ──\ndef fib_bang(n: int) -> int:\n    if n <= 1:\n        return n\n    bang = [0] * (n + 1)\n    bang[1] = 1\n    for i in range(2, n + 1):\n        bang[i] = bang[i - 1] + bang[i - 2]\n    return bang[n]\n\n# ── ✓ O(1) không gian — chỉ giữ hai giá trị ──\ndef fib_hai_bien(n: int) -> int:\n    if n <= 1:\n        return n\n    truoc_truoc, truoc = 0, 1\n    for _ in range(2, n + 1):\n        truoc_truoc, truoc = truoc, truoc_truoc + truoc\n    return truoc\n\nassert fib_hai_bien(0) == 0           # biên\nassert fib_hai_bien(1) == 1           # biên\nassert fib_hai_bien(10) == 55\nassert fib_hai_bien(50) == 12586269025\nassert all(fib_bang(i) == fib_hai_bien(i) for i in range(30))   # ⭐ đối chiếu\n```\n\nPhép kiểm áp cho ví dụ khác — tổng tích luỹ lớn nhất:\n\n```python\n# BÀI TOÁN: tổng lớn nhất của một dãy con liên tiếp (Kadane)\ndef tong_con_lon_nhat(ds: list[int]) -> int:\n    \"\"\"O(n) thời gian, O(1) không gian.\n\n    Mỗi bước chỉ cần: tổng tốt nhất KẾT THÚC tại i−1.\n    Không cần lưu toàn bộ lịch sử ⇒ hai biến là đủ.\n    \"\"\"\n    if not ds:\n        raise ValueError(\"danh sách rỗng\")\n    tot_nhat_tai_day = tot_nhat = ds[0]\n    for x in ds[1:]:\n        tot_nhat_tai_day = max(x, tot_nhat_tai_day + x)\n        tot_nhat = max(tot_nhat, tot_nhat_tai_day)\n    return tot_nhat\n\nassert tong_con_lon_nhat([-2, 1, -3, 4, -1, 2, 1, -5, 4]) == 6   # [4,-1,2,1]\nassert tong_con_lon_nhat([-5, -2, -8]) == -2      # ⭐ biên: TOÀN SỐ ÂM\nassert tong_con_lon_nhat([3]) == 3                 # biên: một phần tử\n```\n\nCase \"toàn số âm\" là trường hợp biên mà phần lớn người viết Kadane lần đầu làm sai — họ khởi tạo `tot_nhat = 0` và hàm trả về 0 thay vì −2. Khởi tạo bằng `ds[0]` xử lý đúng.\n\n(Chú ý `ds[1:]` ở đây vẫn tạo copy O(n); với dữ liệu lớn nên dùng `itertools.islice(ds, 1, None)` hoặc vòng lặp theo chỉ số.)\n\n### Ba thuật toán sắp xếp — cùng thời gian, khác không gian\n\n```python\n# ── MERGE SORT: O(n log n) thời gian, O(n) không gian ──\ndef merge_sort(ds: list[int]) -> list[int]:\n    if len(ds) <= 1:\n        return ds[:]\n    giua = len(ds) // 2\n    return tron(merge_sort(ds[:giua]), merge_sort(ds[giua:]))\n\ndef tron(a: list[int], b: list[int]) -> list[int]:\n    kq, i, j = [], 0, 0\n    while i < len(a) and j < len(b):\n        if a[i] <= b[j]:\n            kq.append(a[i]); i += 1\n        else:\n            kq.append(b[j]); j += 1\n    kq.extend(a[i:]); kq.extend(b[j:])\n    return kq\n```\n\n```\nPHÂN TÍCH KHÔNG GIAN của merge sort — chỗ dễ tính sai:\n\n  Độ sâu ngăn xếp: log n\n  Các nhánh KHÔNG tồn tại cùng lúc (trái xong mới tới phải)\n  Một đường từ gốc xuống lá: n + n/2 + n/4 + ... = 2n\n\n⇒ O(n), KHÔNG phải O(n log n)\n```\n\n```python\n# ── QUICKSORT tại chỗ: O(n log n) thời gian, O(log n) không gian ──\nimport random\n\ndef quicksort(ds: list[int], lo: int = 0, hi: int | None = None) -> None:\n    \"\"\"Sắp xếp TẠI CHỖ. Không gian: O(log n) cho ngăn xếp đệ quy.\"\"\"\n    if hi is None:\n        hi = len(ds) - 1\n    while lo < hi:                          # ⭐ vòng lặp thay một nhánh đệ quy\n        p = phan_hoach(ds, lo, hi)\n        # Đệ quy vào nửa NHỎ, lặp trên nửa LỚN → độ sâu tối đa log n\n        if p - lo < hi - p:\n            quicksort(ds, lo, p - 1)\n            lo = p + 1\n        else:\n            quicksort(ds, p + 1, hi)\n            hi = p - 1\n\n\ndef phan_hoach(ds: list[int], lo: int, hi: int) -> int:\n    # Chọn chốt NGẪU NHIÊN để tránh trường hợp xấu nhất với dữ liệu đã sắp\n    k = random.randint(lo, hi)\n    ds[k], ds[hi] = ds[hi], ds[k]\n    chot = ds[hi]\n    i = lo\n    for j in range(lo, hi):\n        if ds[j] <= chot:\n            ds[i], ds[j] = ds[j], ds[i]\n            i += 1\n    ds[i], ds[hi] = ds[hi], ds[i]\n    return i\n\n\nds = [5, 2, 9, 1, 7, 3]\nquicksort(ds)\nassert ds == [1, 2, 3, 5, 7, 9]\n\nrong: list[int] = []\nquicksort(rong)\nassert rong == []                            # biên\n\nmot = [42]\nquicksort(mot)\nassert mot == [42]                           # biên\n\ntrung = [3, 3, 3, 3]\nquicksort(trung)\nassert trung == [3, 3, 3, 3]                 # biên: mọi phần tử bằng nhau\n```\n\nHai chi tiết thiết kế đáng học trong bản quicksort này:\n\n```\n① Đệ quy vào nửa NHỎ, lặp trên nửa LỚN\n   → độ sâu ngăn xếp tối đa log n, KHÔNG phải n\n   → nếu đệ quy cả hai nhánh, trường hợp xấu nhất là O(n) không gian\n\n② Chốt NGẪU NHIÊN\n   → tránh O(n²) thời gian với dữ liệu đã sắp xếp sẵn\n   → \"đã sắp xếp sẵn\" là trường hợp RẤT hay gặp trong thực tế\n```\n\nĐiểm ② nối về phản biện ở bài Big-O: **dữ liệu thật hiếm khi ngẫu nhiên**, và quicksort chốt cố định gặp đúng trường hợp xấu nhất với dữ liệu đã sắp.\n\n```\nBẢNG SO SÁNH BA THUẬT TOÁN CÙNG O(n log n):\n\n              Thời gian     Không gian    Ổn định?   Tại chỗ?\n              ──────────    ──────────    ────────   ────────\nmerge sort    O(n log n)    O(n)          ✓          ✗\nquicksort     O(n log n)*   O(log n)      ✗          ✓\nheapsort      O(n log n)    O(1)          ✗          ✓\n\n* quicksort xấu nhất O(n²), nhưng chốt ngẫu nhiên làm điều đó\n  gần như không xảy ra.\n\n⇒ Cùng bậc thời gian, ba bậc không gian khác nhau.\n  Chọn theo RÀNG BUỘC: bộ nhớ chật → heapsort; cần ổn định → merge sort.\n```\n\n### Chi phí không gian ẩn — bảng và ví dụ\n\n```python\nn = 1_000_000\n\n# ⭐ O(1) không gian — iterator\nfor i in range(n): pass\nfor i, x in enumerate(ds): pass\nfor a, b in zip(ds1, ds2): pass\nfor y in map(str, ds): pass\nfor x in reversed(ds): pass\ntong = sum(x * x for x in ds)              # ⭐ generator, không list\n\n# ⚠️ O(n) không gian — vô cớ\nfor i in list(range(n)): pass              # ⚠️ list() phá hết\ntong = sum([x * x for x in ds])            # ⚠️ list trung gian thừa\nds_moi = ds[:]                              # copy\ncat = ds[10:20]                             # copy 10 phần tử\n```\n\nDòng `sum([...])` so với `sum(...)` là ví dụ nhỏ mà rõ:\n\n```\nsum([x*x for x in ds])   dựng list n phần tử rồi cộng   → O(n) không gian\nsum(x*x for x in ds)     cộng dần từng phần tử           → O(1) không gian\n\nCÙNG kết quả, cùng thời gian. Chỉ khác một cặp dấu ngoặc.\n```\n\n### Giảm hằng số bộ nhớ — không đổi bậc\n\n```python\nimport array\nimport sys\n\nn = 1_000_000\n\n# List Python: mỗi phần tử là một PyObject int + một con trỏ\nds_list = list(range(n))\n\n# array: số thô 4 byte mỗi phần tử\nds_array = array.array(\"i\", range(n))\n\nprint(f\"list  vỏ: {sys.getsizeof(ds_list):>12,} byte\")\nprint(f\"array vỏ: {sys.getsizeof(ds_array):>12,} byte\")\n# ⚠️ getsizeof KHÔNG tính các object int mà list trỏ tới\n#    → dùng tracemalloc để đo TỔNG thật (bài module 7 mảng 01)\n```\n\n```python\nimport tracemalloc\n\ntracemalloc.start()\nds = list(range(1_000_000))\nhien_tai, dinh = tracemalloc.get_traced_memory()\nprint(f\"list:  {hien_tai / 1024 / 1024:.1f} MB\")\ntracemalloc.stop()\n\ntracemalloc.start()\nds = array.array(\"i\", range(1_000_000))\nhien_tai, dinh = tracemalloc.get_traced_memory()\nprint(f\"array: {hien_tai / 1024 / 1024:.1f} MB\")\ntracemalloc.stop()\n```\n\n```\nCẢ HAI đều là O(n) không gian.\nKhác biệt là HẰNG SỐ — và nó có thể nhiều lần.\n\n⇒ Big-O chọn THUẬT TOÁN; cấu trúc lưu trữ giảm HẰNG SỐ.\n  Cái sau chỉ đáng làm sau khi đã ĐO.\n```\n\n```python\n# __slots__ — giảm bộ nhớ mỗi instance (bài OOP, module 6 mảng 01)\nclass DiemKhongSlots:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\nclass DiemCoSlots:\n    __slots__ = (\"x\", \"y\")          # ⭐ bỏ __dict__ của instance\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\n# Với hàng triệu instance, khác biệt là đáng kể.\n# Đánh đổi: không thêm được thuộc tính động, khó dùng với đa kế thừa.\n```\n\n### Khi O(n) không gian là lựa chọn đúng\n\n```python\n# Tình huống: đối soát 100.000 giao dịch với 100.000 bản ghi ngân hàng\n\n# ── ❌ O(1) không gian, O(n·m) thời gian ──\ndef doi_soat_v1(giao_dich: list[GD], ngan_hang: list[BanGhi]) -> list[GD]:\n    thieu = []\n    for gd in giao_dich:                      # n\n        if not any(b.ma == gd.ma for b in ngan_hang):   # ⚠️ m\n            thieu.append(gd)\n    return thieu\n# 100.000 × 100.000 = 10 tỉ phép so sánh → hàng giờ\n```\n\n```python\n# ── ✓ O(m) không gian, O(n + m) thời gian ──\ndef doi_soat_v2(giao_dich: list[GD], ngan_hang: list[BanGhi]) -> list[GD]:\n    ma_ngan_hang = {b.ma for b in ngan_hang}   # O(m) bộ nhớ\n    return [gd for gd in giao_dich if gd.ma not in ma_ngan_hang]\n# 200.000 thao tác → tức thì\n```\n\n```\nĐÁNH ĐỔI: thêm ~100.000 chuỗi trong bộ nhớ (vài MB)\n          để giảm từ hàng giờ xuống tức thì.\n\n⇒ RÕ RÀNG đáng. Vài MB là không đáng kể với máy hiện đại.\n```\n\n```python\n# Nhưng nếu ngân hàng có 500 TRIỆU bản ghi thì set không chứa nổi:\ndef doi_soat_v3(giao_dich: list[GD], duong_dan_nh: str) -> list[GD]:\n    \"\"\"Cả hai nguồn ĐÃ SẮP theo mã → đối soát bằng hai con trỏ.\n\n    O(n + m) thời gian, O(1) không gian.\n    \"\"\"\n    thieu = []\n    gd_da_sap = sorted(giao_dich, key=lambda g: g.ma)\n    with open(duong_dan_nh, encoding=\"utf-8\") as f:   # ⭐ đọc theo dòng\n        ma_nh = (dong.split(\",\")[0] for dong in f)     # ⭐ generator\n        hien_tai = next(ma_nh, None)\n        for gd in gd_da_sap:\n            while hien_tai is not None and hien_tai < gd.ma:\n                hien_tai = next(ma_nh, None)\n            if hien_tai != gd.ma:\n                thieu.append(gd)\n    return thieu\n```\n\nBa phiên bản này là bài học trung tâm của bài:\n\n```\nv1: O(1) bộ nhớ, O(n·m) thời gian    → không chạy được vì CHẬM\nv2: O(m) bộ nhớ, O(n+m) thời gian    → tốt nhất cho m vừa phải\nv3: O(1) bộ nhớ, O(n+m) thời gian    → cần khi m QUÁ LỚN,\n                                        nhưng đòi hỏi dữ liệu ĐÃ SẮP\n\n⇒ Không có phương án nào luôn đúng. Chọn theo RÀNG BUỘC THẬT.\n```\n\nĐiều kiện của v3 đáng nhấn mạnh: nó chỉ hoạt động khi **cả hai nguồn đã sắp xếp**. Đó là một ràng buộc thật, và nếu phải tự sắp thì lại cần O(m) bộ nhớ — trừ khi dùng sắp xếp ngoài (external sort), chủ đề của mảng 04.",

so_sanh: "### Phụ trợ và tổng\n\n| | Không gian phụ trợ | Tổng không gian |\n|---|---|---|\n| Tính đầu vào | **✗** | ✓ |\n| Dùng khi nói \"độ phức tạp không gian\" | **✓ hầu như luôn** | ít |\n| `def tong(ds)` với n phần tử | **O(1)** | O(n) |\n| Vì sao hợp lý | đầu vào là thứ **buộc phải có** | — |\n\n### Bậc không gian thường gặp\n\n| Bậc | Nghĩa | Ví dụ |\n|---|---|---|\n| O(1) | vài biến | tổng, tìm max, cửa sổ trượt |\n| O(log n) | ngăn xếp đệ quy chia đôi | quicksort tại chỗ, tìm nhị phân đệ quy |\n| O(n) | một cấu trúc mới | list kết quả, set, bảng nhớ, ngăn xếp đệ quy tuyến tính |\n| O(n²) | ma trận | bảng DP hai chiều, ma trận kề |\n\n### Đệ quy và vòng lặp\n\n| | Đệ quy | Vòng lặp |\n|---|---|---|\n| Thời gian | O(n) | O(n) |\n| Không gian | **O(n)** (ngăn xếp) | **O(1)** |\n| Giới hạn Python | **~1000 tầng** | không |\n| Sửa đúng cách khi tràn | **chuyển sang vòng lặp** hoặc ngăn xếp tường minh | — |\n| `setrecursionlimit` | **✗ có thể crash cứng** | — |\n\n### List và generator\n\n| | `[...]` list | `(...)` generator |\n|---|---|---|\n| Không gian | **O(n)** | **O(1)** |\n| Duyệt nhiều lần | ✓ | **✗ chỉ một lần** |\n| `len()` | ✓ | ✗ |\n| Truy cập theo chỉ số | ✓ | ✗ |\n| Sắp xếp được | ✓ | ✗ (phải vật chất hoá) |\n| Lỗi khi duyệt lần hai | — | **✗ im lặng trả rỗng** |\n\nHàng cuối là bug tệ nhất: không exception, chỉ kết quả rỗng.\n\n### Iterator hay list — trả về gì\n\n| Trả **iterator** — O(1) | Trả **list** — O(n) |\n|---|---|\n| `range(n)` | `list(range(n))` |\n| `enumerate(ds)` | — |\n| `zip(a, b)` | `list(zip(a, b))` |\n| `map(f, ds)` | `[f(x) for x in ds]` |\n| `reversed(ds)` | `ds[::-1]` |\n| `(x for x in ds)` | `[x for x in ds]` |\n| `f` (file object) | `f.readlines()` |\n\nBọc bất kỳ cái nào bên trái trong `list()` là mất hết lợi ích.\n\n### Ba thuật toán sắp xếp cùng O(n log n)\n\n| | Merge sort | Quicksort | Heapsort |\n|---|---|---|---|\n| Không gian | **O(n)** | O(log n) | **O(1)** |\n| Thời gian xấu nhất | O(n log n) | **O(n²)** | O(n log n) |\n| Ổn định | **✓** | ✗ | ✗ |\n| Tại chỗ | ✗ | ✓ | ✓ |\n| Chọn khi | **cần ổn định** | mặc định tốt | **bộ nhớ rất chật** |\n\n### Không gian của merge sort — chỗ dễ tính sai\n\n| Cách tính | Kết quả | Đúng? |\n|---|---|---|\n| log n tầng × n mỗi tầng | O(n log n) | **✗ sai** |\n| Một đường gốc→lá: n + n/2 + … = 2n | **O(n)** | **✓ đúng** |\n\nLý do: các nhánh **không** tồn tại cùng lúc — trái xong mới tới phải.\n\n### Đánh đổi hai chiều\n\n| Chiều | Kỹ thuật | Đổi gì |\n|---|---|---|\n| Thêm bộ nhớ → nhanh hơn | `set`, `@cache`, bảng tra | O(n) bộ nhớ ⇒ giảm một bậc thời gian |\n| Tính lại → ít bộ nhớ hơn | **cửa sổ trượt**, generator | O(n) → O(1) bộ nhớ, thời gian không đổi |\n\nPhép kiểm cho cửa sổ trượt: **bước i cần bao nhiêu bước trước?** Hằng số k ⇒ giữ k biến là đủ.\n\n### Khi nào chấp nhận O(n) không gian\n\n| Chấp nhận | Phải giữ O(1) |\n|---|---|\n| n nhỏ, có giới hạn trên biết trước | dữ liệu lớn hơn RAM |\n| giảm được một **bậc thời gian** | n không có giới hạn trên |\n| code rõ hơn hẳn | container hạn chế bộ nhớ |\n| cần duyệt **nhiều lần** | luồng dữ liệu liên tục |\n| cần `len()`, sắp xếp, truy cập ngẫu nhiên | — |\n\n### Giảm hằng số — không đổi bậc\n\n| Kỹ thuật | Giảm gì | Đánh đổi |\n|---|---|---|\n| `array.array` / NumPy | số thô thay `PyObject` | mất tính linh hoạt của list |\n| `__slots__` | bỏ `__dict__` mỗi instance | không thêm thuộc tính động |\n| generator | không vật chất hoá | duyệt một lần |\n\nCả ba giảm **hằng số**, không đổi **bậc** — và chỉ đáng làm sau khi đã đo.",

loi_thuong_gap: [
  "**Tính cả đầu vào vào độ phức tạp không gian.** Hầu như luôn có nghĩa là không gian **phụ trợ**.",
  "**Quên ngăn xếp lời gọi.** Hàm đệ quy không tạo biến nào vẫn dùng O(độ sâu) không gian.",
  "**Nâng `setrecursionlimit` để chữa `RecursionError`.** Có thể crash cứng (segfault), không bắt được bằng `try/except`.",
  "**Tính không gian merge sort là O(n log n).** Các nhánh không tồn tại cùng lúc; tổng một đường là 2n ⇒ O(n).",
  "**Dùng `ds[1:]` trong vòng lặp tưởng là O(1).** Cắt lát là copy — chi phí không gian ẩn.",
  "**Bọc iterator trong `list()` vô cớ.** `list(range(n))` biến O(1) thành O(n) mà không được gì.",
  "**`sum([...])` thay vì `sum(...)`.** List trung gian thừa — chỉ khác một cặp dấu ngoặc.",
  "**`f.readlines()` với file lớn.** File object đã là iterator theo dòng; dùng `for dong in f`.",
  "**Duyệt generator lần thứ hai.** Trả rỗng, **không có exception** — bug im lặng tệ nhất.",
  "**Tưởng `itertools.tee` miễn phí.** Nó lưu đệm phần chênh lệch; duyệt hết cái đầu rồi mới cái sau thì tốn O(n).",
  "**Dùng `lo.clear()` thay vì `lo = []` khi yield lô.** Người gọi giữ tham chiếu, xoá làm dữ liệu của họ biến mất.",
  "**Quicksort đệ quy cả hai nhánh.** Xấu nhất O(n) không gian; đệ quy vào nửa nhỏ, lặp trên nửa lớn cho O(log n).",
  "**Quicksort chốt cố định.** Dữ liệu đã sắp xếp sẵn là trường hợp rất hay gặp và cho O(n²).",
  "**Tin `sys.getsizeof` là bộ nhớ thật.** Nó không đệ quy — chỉ đo vỏ, không đo phần tử bên trong; dùng `tracemalloc`.",
  "**Khởi tạo Kadane bằng 0.** Sai với dãy toàn số âm; khởi tạo bằng `ds[0]`.",
  "**Tối ưu bộ nhớ trước khi đo.** Giảm hằng số chỉ đáng làm khi đã biết bộ nhớ thật là vấn đề.",
  "**Coi bộ nhớ là chuyện phụ.** Chậm thì chờ; hết RAM thì tiến trình bị giết và mất công việc đang làm."
],

tu_kiem_tra: [
  "Phân biệt không gian phụ trợ và tổng không gian. Khi nói \"độ phức tạp không gian\" thì ý là cái nào, và vì sao?",
  "Vì sao ngăn xếp lời gọi tính vào độ phức tạp không gian? Cho ví dụ hàm O(1) biến mà O(n) không gian.",
  "Vì sao KHÔNG nên nâng `setrecursionlimit`? Hai cách chữa đúng là gì?",
  "Ngăn xếp tường minh đổi gì so với đệ quy? Bậc không gian có đổi không?",
  "Vì sao không gian của merge sort là O(n) chứ không phải O(n log n)? Tính tổng một đường gốc→lá.",
  "Nêu quy ước đặt tên phân biệt phương thức tại chỗ và không tại chỗ trong Python.",
  "Generator đổi bậc không gian thế nào? Nêu ba giới hạn của nó.",
  "Vì sao duyệt generator lần thứ hai là bug \"im lặng\"? Cách xử lý khi cần duyệt nhiều lần?",
  "`itertools.tee` có miễn phí không? Khi nào nó tốn O(n)?",
  "Nêu sáu thứ trong Python trả iterator (O(1)) và dạng list tương ứng (O(n)).",
  "Vì sao `sum(x*x for x in ds)` tốt hơn `sum([x*x for x in ds])`?",
  "Kỹ thuật cửa sổ trượt rút O(n) xuống O(1) thế nào? Phép kiểm để biết áp được?",
  "Với `fib`, vì sao chỉ cần hai biến? Điều gì cho phép bỏ các giá trị cũ?",
  "So sánh merge sort, quicksort, heapsort về thời gian, không gian, ổn định, tại chỗ.",
  "Vì sao quicksort nên đệ quy vào nửa nhỏ và lặp trên nửa lớn?",
  "Vì sao quicksort cần chốt ngẫu nhiên? Liên hệ với phản biện \"dữ liệu thật hiếm khi ngẫu nhiên\".",
  "Nêu đánh đổi bộ nhớ–thời gian theo cả hai chiều, kèm ví dụ mỗi chiều.",
  "Nêu bốn tình huống chấp nhận O(n) không gian và bốn tình huống phải giữ O(1).",
  "Nêu ba cách giảm bộ nhớ thật mà không đổi bậc, kèm đánh đổi của mỗi cách.",
  "Vì sao `sys.getsizeof` không cho bộ nhớ thật của một list lồng nhau?",
  "Trong ví dụ đối soát ba phiên bản, vì sao không có phương án nào luôn đúng?"
],

tai_nguyen: [
  "Sách **Introduction to Algorithms** — CLRS. Phần phân tích không gian của các thuật toán sắp xếp, và chương về quicksort tại chỗ.",
  "Sách **Algorithms** — Sedgewick & Wayne. Bảng so sánh không gian phụ trợ của mọi thuật toán sắp xếp, rất tiện để tra.",
  "**Tài liệu Python — `tracemalloc`** — cách đo bộ nhớ thật theo dòng code; công cụ đúng thay cho `sys.getsizeof`.",
  "**Tài liệu Python — `itertools`** — `islice`, `tee`, `chain` và các công cụ giữ không gian O(1); đọc phần cảnh báo về `tee`.",
  "**Tài liệu Python — `array` và `sys.getrecursionlimit`** — giới hạn thật và cách lưu số thô thay `PyObject`."
],

lien_he: [
  "**Ký hiệu Big-O** và **Vòng lặp lồng nhau** ở module này: cùng cách đếm, áp cho bộ nhớ thay vì thao tác.",
  "**Phân tích khấu hao** ngay sau: vì sao `list` cấp phát thừa chỗ, và điều đó ảnh hưởng bộ nhớ thế nào.",
  "**Stack và heap** ở module 2 mảng 01: ngăn xếp lời gọi nằm ở đâu và vì sao nó có giới hạn cứng.",
  "**Generator, iterator và yield** ở module 5 mảng 01: cơ chế đằng sau kỹ thuật đổi bậc không gian của bài này.",
  "**Đo bộ nhớ và tối ưu cơ bản** ở module 7 mảng 01: `tracemalloc`, chi phí thật của `PyObject`, và nguyên tắc đo trước khi tối ưu.",
  "**Mutable vs immutable** ở module 7 mảng 01: vì sao cắt lát là copy, và bẫy tham chiếu khi yield lô.",
  "**Dataclass và `__slots__`** ở module 6 mảng 01: giảm bộ nhớ mỗi instance.",
  "**Sắp xếp** ở module 6 mảng này: merge sort, quicksort, heapsort đầy đủ, và vì sao Timsort chọn đánh đổi như nó chọn.",
  "**Kỹ thuật giải thuật cốt lõi** ở module 8 mảng này: dynamic programming và bước tối ưu không gian bằng cửa sổ trượt.",
  "**Cây** ở module 4 mảng này: độ sâu cây quyết định không gian đệ quy; cây suy biến cho O(n).",
  "**Hiệu năng** ở mảng 04: sắp xếp ngoài (external sort) khi dữ liệu lớn hơn RAM.",
  "**Hiệu năng và vận hành** ở mảng 05: giới hạn bộ nhớ container và vì sao OOM killer giết tiến trình."
]

};
