window.LESSONS["02:phan-tich-do-phuc-tap:phan-tich-khau-hao-amortized-vi-sao-append-la-o-1"] = {

tom_tat: [
  "**Khấu hao là trung bình trên MỘT DÃY thao tác, không phải trung bình xác suất.** Nó là bảo đảm chắc chắn: n thao tác luôn tốn O(n) tổng, dù một thao tác lẻ có thể tốn O(n).",
  "**`list.append` là O(1) khấu hao vì Python cấp phát thừa chỗ theo TỈ LỆ, không theo lượng cố định.** Nhân đôi cho tổng chi phí O(n); cộng thêm 10 ô cho O(n²).",
  "**Chìa khoá là tổng cấp số nhân:** 1 + 2 + 4 + … + n = 2n − 1 < 2n. Toàn bộ chi phí cấp phát lại trong đời một list chỉ bằng khoảng hai lần chi phí thêm n phần tử.",
  "**Khấu hao KHÁC trung bình.** Trung bình dựa trên giả định về phân bố đầu vào; khấu hao không giả định gì — nó đúng cho mọi dãy thao tác.",
  "**Khấu hao không cứu được ứng dụng cần độ trễ ổn định.** Một `append` lẻ có thể chậm gấp nghìn lần bình thường — vấn đề thật với hệ thống thời gian thực."
],

can_biet_truoc: [
  "Bài **Ký hiệu Big-O** và **Vòng lặp lồng nhau** ở module này — đặc biệt hai trục \"trường hợp\" và \"ký hiệu\", và công thức tổng cấp số nhân.",
  "Bài **Độ phức tạp không gian** ngay trước — cấp phát thừa chỗ là đánh đổi bộ nhớ lấy thời gian.",
  "Bài **CPU, RAM, cache** ở module 1 mảng 01 — vì sao mảng liên tục trong bộ nhớ lại nhanh.",
  "Bài **List, dict, set, tuple** ở module 5 mảng 01 — bài này giải thích cơ chế bên dưới `append`."
],

dinh_nghia: "Nói đời thường: bạn trả tiền thuê nhà hằng tháng, nhưng mỗi năm phải đóng thêm một khoản sửa chữa lớn. Nếu chia khoản lớn đó cho 12 tháng, chi phí *mỗi tháng* vẫn đều đặn và dự đoán được — dù có một tháng bạn phải chi nhiều gấp mười. Đó chính là khấu hao.\n\nCác khái niệm:\n\n- **Độ phức tạp khấu hao** (*amortized complexity*) — chi phí **trung bình trên một dãy n thao tác**, tính bằng *tổng chi phí chia n*. Đây là **bảo đảm chắc chắn**, không phải kỳ vọng thống kê.\n- **Mảng động** (*dynamic array*) — mảng tự nới rộng khi hết chỗ. `list` của Python là một mảng động.\n- **Dung lượng** (*capacity*) — số ô đã cấp phát. **Kích thước** (*size*) — số ô đang dùng. Luôn có `size ≤ capacity`.\n- **Cấp phát lại** (*reallocation*) — khi `size` chạm `capacity`: xin vùng bộ nhớ mới lớn hơn, **copy toàn bộ** phần tử sang, giải phóng vùng cũ.\n- **Hệ số tăng trưởng** (*growth factor*) — dung lượng mới bằng dung lượng cũ nhân bao nhiêu. Đây là con số quyết định mọi thứ trong bài.\n- **Phương pháp tổng hợp** (*aggregate method*) — cộng tổng chi phí của n thao tác rồi chia n.\n- **Phương pháp kế toán** (*accounting method*) — mỗi thao tác rẻ \"trả trước\" một khoản tín dụng, dùng để bù cho thao tác đắt sau này.\n\nPhân biệt quan trọng nhất của bài — ba loại \"trung bình\" hay bị lẫn:\n\n```\nXẤU NHẤT      chi phí lớn nhất của MỘT thao tác\n              append: O(n)   ← khi phải cấp phát lại\n\nKHẤU HAO      tổng chi phí n thao tác, chia n\n              append: O(1)   ← BẢO ĐẢM, không giả định gì\n\nTRUNG BÌNH    kỳ vọng theo một PHÂN BỐ đầu vào giả định\n              tra dict: O(1) ← giả định hàm băm phân bố đều\n```\n\nKhác biệt then chốt giữa hai dòng cuối: **khấu hao không giả định gì về dữ liệu**, nên nó đúng kể cả với đầu vào do kẻ tấn công chọn. Trung bình thì có thể bị phá — và module 3 sẽ cho thấy `dict` bị phá thế nào.",

vi_sao_can: "Khấu hao là khái niệm giải thích một nghịch lý mà ai học thuật toán cũng gặp:\n\n- **Nó giải thích vì sao `append` vừa là O(n) vừa là O(1).** Không có khái niệm khấu hao, hai câu đó nghe như mâu thuẫn — và bạn không biết nên tin câu nào khi phân tích code.\n- **Nó là lý do phần lớn cấu trúc dữ liệu thực tế dùng được.** `list`, `dict`, `set` của Python, `vector` của C++, `ArrayList` của Java — tất cả đều dựa vào cấp phát theo tỉ lệ. Không hiểu khấu hao thì không hiểu vì sao chúng nhanh.\n- **Nó cho bạn một quy tắc thiết kế cụ thể: tăng theo TỈ LỆ, không theo lượng cố định.** Đây là lỗi thật mà người ta mắc khi tự viết cấu trúc dữ liệu hoặc buffer.\n- **Nó chỉ ra khi nào khấu hao KHÔNG đủ.** Hệ thống thời gian thực, giao dịch tài chính, điều khiển thiết bị — những chỗ mà một lần chậm bất thường là không chấp nhận được.\n- **Nó phân biệt được với \"trung bình\".** Khi tài liệu ghi \"O(1) amortized\" và \"O(1) average\", hai câu đó có mức bảo đảm **khác nhau** — và biết khác biệt đó là điều kiện để đọc tài liệu đúng.\n\nTình huống thật rất cụ thể: bạn tự viết một lớp buffer để gom log rồi ghi theo lô, và mở rộng nó bằng cách cấp thêm 100 ô mỗi lần đầy. Test với 1.000 dòng chạy tốt. Production có 1 triệu dòng mỗi giờ, và hàm gom log ngốn CPU bất thường. **Nguyên nhân: cấp phát cộng thêm hằng số cho O(n²) tổng, trong khi nhân đôi cho O(n).** Với 1 triệu phần tử, khác biệt là khoảng 5 tỉ phép copy so với 2 triệu.\n\nMột lưu ý về phạm vi: bài này tập trung vào mảng động vì nó là ví dụ rõ nhất và bạn dùng hằng ngày. Cùng lập luận áp cho bảng băm (module 3), heap, và cấu trúc union-find — nên hiểu kỹ ở đây thì các chỗ sau nhẹ hẳn.",

co_che: "### Vấn đề: mảng cần chỗ liên tục\n\n```\nList Python là MẢNG CON TRỎ LIÊN TỤC trong bộ nhớ:\n\n    ds = [a, b, c]\n\n    [ptr→a][ptr→b][ptr→c][ ][ ][ ]\n     └──── size = 3 ────┘\n     └──────── capacity = 6 ───────┘\n```\n\nLiên tục là **lý do** `ds[i]` chạy O(1): địa chỉ ô thứ `i` tính được bằng một phép nhân và một phép cộng, không phải đi tìm.\n\nNhưng liên tục cũng tạo ra vấn đề: **khi hết chỗ, không thể \"nối thêm\" vào cuối** — vùng bộ nhớ ngay sau đó có thể đã bị thứ khác dùng. Bắt buộc phải:\n\n```\n① Xin vùng mới LỚN HƠN\n② COPY toàn bộ size phần tử sang       ← đây là chi phí O(n)\n③ Giải phóng vùng cũ\n```\n\nBước ② là toàn bộ câu chuyện của bài này.\n\n### Vì sao cấp phát cộng thêm hằng số cho O(n²)\n\nGiả sử mỗi lần đầy thì cấp thêm **đúng c ô** (c là hằng số):\n\n```\nCấp phát lại xảy ra khi size = c, 2c, 3c, 4c, ..., n\n\nMỗi lần copy toàn bộ phần tử hiện có:\n    lần 1: copy c\n    lần 2: copy 2c\n    lần 3: copy 3c\n    ...\n    lần n/c: copy n\n\nTổng = c + 2c + 3c + ... + n\n     = c · (1 + 2 + 3 + ... + n/c)\n     = c · (n/c)(n/c + 1)/2\n     ≈ n²/(2c)\n```\n\n```\n⇒ TỔNG O(n²)  ⇒  MỖI append là O(n) khấu hao\n\nĐây là TỔNG CẤP SỐ CỘNG — đúng dạng vòng lặp tam giác ở bài trước.\n```\n\nCon số cụ thể cho thấy mức độ:\n\n```\nc = 10, n = 1.000.000:\n    tổng copy ≈ 10¹²/20 = 5 × 10¹⁰ = 50 tỉ phép copy\n```\n\n### Vì sao nhân đôi cho O(1) khấu hao\n\nGiờ giả sử mỗi lần đầy thì **nhân đôi** dung lượng:\n\n```\nCấp phát lại xảy ra khi size = 1, 2, 4, 8, 16, ..., n\n\nMỗi lần copy:\n    lần 1: copy 1\n    lần 2: copy 2\n    lần 3: copy 4\n    lần 4: copy 8\n    ...\n    lần cuối: copy n/2, rồi n\n\nTổng = 1 + 2 + 4 + 8 + ... + n\n```\n\nĐây là **tổng cấp số nhân**, và nó có một tính chất đẹp:\n\n```\n1 + 2 + 4 + ... + 2^k = 2^(k+1) − 1\n\nVới 2^k = n:\n    tổng = 2n − 1  <  2n\n```\n\n```\n⇒ TỔNG chi phí cấp phát lại của n lần append  <  2n\n⇒ Cộng n lần ghi giá trị vào ô: tổng < 3n\n⇒ Chia cho n:  MỖI append là O(1) khấu hao\n```\n\nĐiểm đáng chú ý và phản trực giác: **tổng mọi chi phí cấp phát lại trong suốt đời một list chỉ khoảng 2n** — tức là bằng chi phí duyệt list hai lần. Toàn bộ việc \"copy tốn kém\" cộng lại vẫn là tuyến tính.\n\nSo sánh trực tiếp cho n = 1.000.000:\n\n```\nCỘNG THÊM 10 ô:  ≈ 50.000.000.000 phép copy\nNHÂN ĐÔI:        ≈      2.000.000 phép copy\n\n⇒ Khác nhau khoảng 25.000 lần.\n  CÙNG một ý tưởng \"cấp thêm khi đầy\", khác nhau ở CÁCH cấp thêm.\n```\n\n### Vì sao tỉ lệ mới quan trọng, không phải con số 2\n\nHệ số nào lớn hơn 1 cũng cho O(1) khấu hao:\n\n```\nHệ số 2:    1 + 2 + 4 + ... + n = 2n\nHệ số 1,5:  tổng ≈ 3n\nHệ số 1,125 (Python): tổng ≈ 9n\n\nTổng quát với hệ số r > 1:\n    tổng = n · r/(r−1)\n\n  r = 2      →  2n\n  r = 1,5    →  3n\n  r = 1,125  →  9n\n\n⇒ Mọi r > 1 cho HẰNG SỐ nhân với n  ⇒  O(1) khấu hao\n⇒ r = 1 (cộng thêm cố định) làm công thức vô nghĩa — và cho O(n²)\n```\n\nĐây là điểm cốt lõi: **ranh giới không phải \"nhân đôi hay không\", mà là \"nhân hay cộng\".** Nhân với 1,01 vẫn cho O(1) khấu hao; cộng thêm một triệu ô mỗi lần vẫn cho O(n²).\n\nĐánh đổi giữa các hệ số:\n\n```\nHỆ SỐ LỚN (2)      ít lần cấp phát lại, nhưng LÃNG PHÍ tới 50% bộ nhớ\nHỆ SỐ NHỎ (1,125)  tiết kiệm bộ nhớ, nhưng cấp phát lại nhiều hơn\n\nPython chọn hệ số nhỏ (khoảng 1,125) vì:\n  · list Python thường không quá lớn\n  · lãng phí bộ nhớ đáng lo hơn với hàng nghìn list nhỏ\n  · và hằng số 9n vẫn là O(1) khấu hao\n```\n\nMột lợi ích phụ ít người biết của hệ số nhỏ: **vùng bộ nhớ vừa giải phóng có thể tái dùng được** cho lần cấp phát sau, còn nhân đôi thì vùng mới luôn lớn hơn tổng mọi vùng cũ cộng lại.\n\n### Hai phương pháp chứng minh\n\n**Phương pháp tổng hợp** — cộng tổng rồi chia, đúng cách vừa làm ở trên:\n\n```\nTổng chi phí n thao tác  =  T(n)\nChi phí khấu hao mỗi thao tác  =  T(n)/n\n\nVới append nhân đôi: T(n) < 3n  ⇒  khấu hao < 3  ⇒  O(1)\n```\n\n**Phương pháp kế toán** — trực quan hơn, và dễ áp cho cấu trúc phức tạp:\n\n```\nQuy ước: mỗi append \"trả\" 3 đồng thay vì 1 đồng.\n\n    1 đồng — ghi giá trị vào ô của mình\n    1 đồng — để dành, dùng để copy CHÍNH NÓ khi cấp phát lại\n    1 đồng — để dành, dùng để copy MỘT phần tử cũ\n\nKhi cấp phát lại từ n/2 lên n phần tử:\n    cần copy n/2 phần tử\n    có sẵn: n/2 đồng (mỗi phần tử mới thêm từ lần trước đã để dành)\n    ⇒ ĐỦ TIỀN\n\n⇒ Mỗi append tốn 3 đồng cố định  ⇒  O(1) khấu hao\n```\n\nGiá trị của phương pháp kế toán: nó cho bạn một cách **kiểm tra** thiết kế của mình. Nếu không tìm được một khoản trả trước cố định đủ bù cho các thao tác đắt, thiết kế của bạn không O(1) khấu hao.\n\n### Khấu hao KHÁC trung bình — phân biệt cho rõ\n\n```\n                  KHẤU HAO                TRUNG BÌNH\n                  ──────────────────      ────────────────────────\nDựa trên          một DÃY thao tác        PHÂN BỐ đầu vào giả định\nGiả định về dữ liệu  KHÔNG có gì           có\nMức bảo đảm       CHẮC CHẮN               kỳ vọng thống kê\nKẻ tấn công phá được  ✗                    ✓ (nếu đoán được hàm băm)\nVí dụ             list.append             dict lookup\n```\n\nHệ quả thực tế của dòng cuối rất đáng biết:\n\n```\n· list.append O(1) KHẤU HAO:\n  n lần append LUÔN tốn O(n) tổng. Không có đầu vào nào phá được.\n\n· dict lookup O(1) TRUNG BÌNH:\n  nếu mọi khoá băm về CÙNG một chỗ thì thành O(n) mỗi lần tra.\n  Kẻ tấn công cố ý tạo khoá va chạm ⇒ tấn công từ chối dịch vụ.\n  (Python có ngẫu nhiên hoá hàm băm để chống — module 3 nói kỹ.)\n```\n\nĐây là lý do phải đọc kỹ tài liệu: **\"amortized\" là lời hứa mạnh hơn \"average\".**\n\n### Các thao tác O(1) khấu hao trong Python\n\n```\nTHAO TÁC                  XẤU NHẤT      KHẤU HAO / TRUNG BÌNH\n────────────────────      ──────────    ──────────────────────\nlist.append(x)            O(n)          O(1) KHẤU HAO\nlist.pop()                O(n)*         O(1) khấu hao\ndict[k] = v               O(n)          O(1) trung bình\nset.add(x)                O(n)          O(1) trung bình\nstr thành list rồi join   —             O(n)\n\n* pop() có thể co dung lượng lại, nên cũng cần khấu hao\n```\n\n### Vấn đề co lại — và cái bẫy dao động\n\nNếu list co lại khi vơi, phải cẩn thận:\n\n```\n❌ THIẾT KẾ SAI: co khi size < capacity/2\n\n    capacity = 8, size = 4\n    append  → size = 5\n    pop     → size = 4 < 8/2 ⇒ CO xuống capacity = 4\n    append  → size = 5 > 4   ⇒ NỞ lên capacity = 8\n    pop     → CO lại...\n\n⇒ Mỗi thao tác gây một lần cấp phát lại O(n)\n⇒ KHÔNG còn O(1) khấu hao — thành O(n) mỗi thao tác\n```\n\n```\n✓ THIẾT KẾ ĐÚNG: co khi size < capacity/4, và co xuống capacity/2\n\n    ⇒ Sau khi co, size = capacity/2 — còn xa cả hai ngưỡng\n    ⇒ Cần ít nhất capacity/4 thao tác nữa mới cấp phát lại\n    ⇒ Đủ để \"để dành\" tiền cho lần sau  ⇒  O(1) khấu hao\n```\n\nBài học tổng quát và đáng nhớ: **phải có \"vùng đệm\" giữa ngưỡng nở và ngưỡng co.** Hai ngưỡng trùng nhau tạo dao động, và dao động phá mọi bảo đảm khấu hao. Cùng nguyên tắc xuất hiện lại ở bảng băm và ở cơ chế tự động co giãn của hệ thống.\n\n### Khi khấu hao KHÔNG đủ\n\nPhản biện quan trọng của bài:\n\n```\nKHẤU HAO nói về TỔNG, không nói về TỪNG lần.\n\nMột append lẻ có thể tốn O(n):\n    list 10 triệu phần tử, đúng lúc đầy\n    ⇒ lần append đó phải copy 10 triệu con trỏ\n    ⇒ chậm hơn bình thường HÀNG NGHÌN LẦN\n```\n\nBa lĩnh vực mà điều này là vấn đề thật:\n\n```\n① HỆ THỐNG THỜI GIAN THỰC\n   điều khiển thiết bị, xử lý âm thanh — cần độ trễ ỔN ĐỊNH\n   một lần chậm 50ms là hỏng\n\n② GIAO DỊCH TÀI CHÍNH TẦN SỐ CAO\n   độ trễ đuôi (p99, p999) quan trọng hơn độ trễ trung bình\n\n③ HỆ THỐNG CÓ SLA VỀ ĐỘ TRỄ ĐUÔI\n   \"99,9% request dưới 100ms\" — khấu hao không bảo đảm được điều này\n```\n\nBa cách xử lý khi cần độ trễ ổn định:\n\n```\n① CẤP PHÁT TRƯỚC đủ chỗ\n   biết trước n → xin đủ ngay từ đầu, không bao giờ cấp phát lại\n\n② DÙNG CẤU TRÚC KHÁC\n   deque (danh sách các khối) — không cần copy toàn bộ khi nở\n   linked list — O(1) thật, nhưng mất tính liên tục và cache locality\n\n③ CẤP PHÁT LẠI DẦN (incremental)\n   mỗi thao tác copy vài phần tử sang vùng mới thay vì copy hết một lần\n   → phức tạp hơn nhiều, và tốn bộ nhớ gấp đôi trong lúc chuyển\n```\n\nCách ① là cách đơn giản và hiệu quả nhất khi biết trước kích thước — và đó là lý do nhiều API cho phép truyền `capacity` khi khởi tạo.\n\n### Vì sao mảng động vẫn thắng linked list\n\nMột câu hỏi tự nhiên: nếu copy tốn kém vậy, sao không dùng linked list — thêm phần tử là O(1) **thật**, không cần khấu hao?\n\n```\n                    Mảng động        Linked list\n                    ──────────       ────────────\nThêm cuối           O(1) khấu hao    O(1) thật\nTruy cập ds[i]      O(1)             O(n)      ⚠️\nBộ nhớ mỗi phần tử  1 con trỏ        1 con trỏ + 1 con trỏ next\nCache locality      ✓ RẤT tốt        ✗ rất tệ\nDuyệt tuần tự       nhanh            CHẬM HƠN NHIỀU LẦN\n```\n\nHai dòng cuối là lý do quyết định, và nối về bài cache CPU ở module 1 mảng 01:\n\n```\nMảng: phần tử nằm LIỀN NHAU\n  → đọc một phần tử thì CPU nạp cả một dòng cache (~64 byte)\n  → các phần tử kế tiếp ĐÃ nằm trong cache, đọc gần như miễn phí\n\nLinked list: các nút rải RẢI RÁC trên heap\n  → mỗi nút là một lần đọc RAM riêng, có thể cache miss\n  → và cache miss đắt hơn cache hit hàng trăm lần\n```\n\nKết luận rất đáng nhớ: **mảng động chấp nhận một chi phí khấu hao O(1) để đổi lấy truy cập O(1) và cache locality tốt** — và đó gần như luôn là đánh đổi đúng. Đây là ví dụ điển hình cho phản biện ở bài Big-O: hai cấu trúc cùng bậc trên giấy có thể chênh nhau nhiều lần trên thực tế.",

vi_du: "### Quan sát cơ chế cấp phát của Python\n\n```python\nimport sys\n\nds = []\ndung_luong_truoc = sys.getsizeof(ds)\nprint(f\"{'size':>6} {'bytes':>8} {'tăng':>8}\")\nprint(f\"{0:>6} {dung_luong_truoc:>8} {'—':>8}\")\n\nfor i in range(1, 130):\n    ds.append(i)\n    hien_tai = sys.getsizeof(ds)\n    if hien_tai != dung_luong_truoc:              # ⭐ chỉ in khi CẤP PHÁT LẠI\n        tang = hien_tai - dung_luong_truoc\n        print(f\"{len(ds):>6} {hien_tai:>8} {tang:>8}\")\n        dung_luong_truoc = hien_tai\n```\n\nKết quả có dạng (con số byte tuỳ phiên bản Python và nền tảng, nhưng **mẫu** thì ổn định):\n\n```\n  size    bytes     tăng\n     0       56        —\n     1       88       32\n     5      120       32\n     9      184       64\n    17      248       64\n    25      312       64\n    33      376       64\n    41      472       96\n    53      568       96\n    ...\n```\n\nHai điều đọc được từ bảng này:\n\n```\n① Cấp phát lại xảy ra ở size = 1, 5, 9, 17, 25, 33, 41, 53, ...\n   → KHOẢNG CÁCH giữa các lần TĂNG DẦN\n   → đó là dấu hiệu của tăng trưởng theo TỈ LỆ\n\n② Nếu là cộng thêm hằng số, khoảng cách sẽ ĐỀU NHAU\n   → và ta đã có O(n²)\n```\n\nĐo hệ số tăng trưởng thật:\n\n```python\nimport sys\n\nds = []\nmoc = []\ntruoc = sys.getsizeof(ds)\nfor i in range(200_000):\n    ds.append(i)\n    hien_tai = sys.getsizeof(ds)\n    if hien_tai != truoc:\n        moc.append(len(ds))\n        truoc = hien_tai\n\nprint(\"Tỉ lệ giữa các mốc cấp phát lại liên tiếp:\")\nfor a, b in zip(moc[10:20], moc[11:21]):\n    print(f\"  {a:>8} → {b:>8}   tỉ lệ = {b / a:.4f}\")\n```\n\n```\nTỉ lệ giữa các mốc cấp phát lại liên tiếp:\n     ...      ...      tỉ lệ ≈ 1,125\n\n⭐ Python dùng hệ số khoảng 1,125 — nhỏ hơn 2 nhiều.\n  Vẫn là NHÂN, nên vẫn O(1) khấu hao (tổng ≈ 9n).\n```\n\n### Tự viết mảng động — hai cách cấp phát\n\n```python\nclass MangDongSai:\n    \"\"\"Cấp thêm HẰNG SỐ ô mỗi lần đầy → O(n) khấu hao mỗi append.\"\"\"\n\n    BUOC = 10\n\n    def __init__(self) -> None:\n        self._o: list[object | None] = [None] * self.BUOC\n        self._size = 0\n        self.so_phep_copy = 0                # ⭐ đếm để chứng minh\n\n    def append(self, x: object) -> None:\n        if self._size == len(self._o):\n            self._cap_phat_lai(len(self._o) + self.BUOC)   # ⚠️ CỘNG\n        self._o[self._size] = x\n        self._size += 1\n\n    def _cap_phat_lai(self, dung_luong_moi: int) -> None:\n        moi: list[object | None] = [None] * dung_luong_moi\n        for i in range(self._size):          # ⭐ chi phí O(size)\n            moi[i] = self._o[i]\n            self.so_phep_copy += 1\n        self._o = moi\n\n    def __len__(self) -> int:\n        return self._size\n\n\nclass MangDongDung:\n    \"\"\"NHÂN dung lượng mỗi lần đầy → O(1) khấu hao mỗi append.\"\"\"\n\n    HE_SO = 2\n\n    def __init__(self) -> None:\n        self._o: list[object | None] = [None] * 1\n        self._size = 0\n        self.so_phep_copy = 0\n\n    def append(self, x: object) -> None:\n        if self._size == len(self._o):\n            self._cap_phat_lai(len(self._o) * self.HE_SO)  # ⭐ NHÂN\n        self._o[self._size] = x\n        self._size += 1\n\n    def _cap_phat_lai(self, dung_luong_moi: int) -> None:\n        moi: list[object | None] = [None] * dung_luong_moi\n        for i in range(self._size):\n            moi[i] = self._o[i]\n            self.so_phep_copy += 1\n        self._o = moi\n\n    def __len__(self) -> int:\n        return self._size\n```\n\nĐo tổng số phép copy — đây là bằng chứng trực tiếp:\n\n```python\nprint(f\"{'n':>9} {'CỘNG 10':>14} {'NHÂN 2':>12} {'tỉ lệ':>10}\")\nfor n in (1_000, 10_000, 100_000):\n    sai = MangDongSai()\n    dung = MangDongDung()\n    for i in range(n):\n        sai.append(i)\n        dung.append(i)\n    ti_le = sai.so_phep_copy / max(1, dung.so_phep_copy)\n    print(f\"{n:>9,} {sai.so_phep_copy:>14,} {dung.so_phep_copy:>12,} \"\n          f\"{ti_le:>9.1f}x\")\n```\n\n```\n        n        CỘNG 10       NHÂN 2      tỉ lệ\n    1,000         49,500          511      96.9x\n   10,000      4,995,000        8,191     609.8x\n  100,000    499,950,000       65,535    7628.2x\n\n⭐ Đọc hai cột giữa theo CHIỀU DỌC:\n\n  CỘNG 10:  n×10 → copy ×100    ⇒ O(n²)   ✓ khớp lý thuyết\n  NHÂN 2:   n×10 → copy ×10     ⇒ O(n)    ✓ khớp lý thuyết\n\n  Và cột \"tỉ lệ\" tăng theo n — dấu hiệu hai bậc KHÁC NHAU.\n```\n\nKiểm chứng công thức `tổng < 2n` cho bản nhân đôi:\n\n```python\nfor n in (1_000, 10_000, 100_000, 1_000_000):\n    dung = MangDongDung()\n    for i in range(n):\n        dung.append(i)\n    print(f\"n={n:>9,}  copy={dung.so_phep_copy:>9,}  \"\n          f\"copy/n={dung.so_phep_copy/n:>5.2f}  (lý thuyết < 2)\")\n```\n\n```\nn=    1,000  copy=      511  copy/n= 0.51  (lý thuyết < 2)\nn=   10,000  copy=    8,191  copy/n= 0.82  (lý thuyết < 2)\nn=  100,000  copy=   65,535  copy/n= 0.66  (lý thuyết < 2)\nn=1,000,000  copy=  524,287  copy/n= 0.52  (lý thuyết < 2)\n\n⭐ Tỉ lệ copy/n luôn DƯỚI 2 và KHÔNG tăng theo n.\n  Đó chính là định nghĩa của O(1) khấu hao.\n```\n\nSố phép copy đúng bằng `2^k − 1` với `2^k` là dung lượng cuối — khớp chính xác với tổng cấp số nhân trong phần cơ chế.\n\n### Hệ số tăng trưởng — đánh đổi bộ nhớ\n\n```python\nclass MangDongTuyChinh:\n    def __init__(self, he_so: float) -> None:\n        self.he_so = he_so\n        self._o: list[object | None] = [None]\n        self._size = 0\n        self.so_phep_copy = 0\n        self.so_lan_cap_phat = 0\n\n    def append(self, x: object) -> None:\n        if self._size == len(self._o):\n            moi = max(len(self._o) + 1, int(len(self._o) * self.he_so))\n            self._cap_phat_lai(moi)\n        self._o[self._size] = x\n        self._size += 1\n\n    def _cap_phat_lai(self, dung_luong_moi: int) -> None:\n        moi: list[object | None] = [None] * dung_luong_moi\n        for i in range(self._size):\n            moi[i] = self._o[i]\n            self.so_phep_copy += 1\n        self._o = moi\n        self.so_lan_cap_phat += 1\n\n    @property\n    def lang_phi(self) -> float:\n        \"\"\"Tỉ lệ ô đã cấp phát nhưng chưa dùng.\"\"\"\n        return 1 - self._size / len(self._o)\n\n\nn = 100_000\nprint(f\"{'hệ số':>7} {'số lần cấp phát':>16} {'copy/n':>9} {'lãng phí':>10}\")\nfor hs in (1.125, 1.25, 1.5, 2.0, 3.0):\n    m = MangDongTuyChinh(hs)\n    for i in range(n):\n        m.append(i)\n    print(f\"{hs:>7.3f} {m.so_lan_cap_phat:>16} \"\n          f\"{m.so_phep_copy/n:>9.2f} {m.lang_phi:>9.1%}\")\n```\n\n```\n  hệ số  số lần cấp phát    copy/n   lãng phí\n  1.125               98      8.21      2.5%\n  1.250               52      4.24      8.9%\n  1.500               29      2.51     18.7%\n  2.000               17      1.05     30.7%\n  3.000               11      0.75     52.4%\n\n⭐ ĐỌC BẢNG:\n\n  Hệ số LỚN  → ít lần cấp phát, ít copy, nhưng LÃNG PHÍ nhiều bộ nhớ\n  Hệ số NHỎ  → nhiều lần cấp phát, nhiều copy, tiết kiệm bộ nhớ\n\n  MỌI hệ số > 1 đều cho copy/n là HẰNG SỐ (không tăng theo n)\n  ⇒ tất cả đều O(1) khấu hao, chỉ khác HẰNG SỐ\n```\n\nCột \"lãng phí\" giải thích lựa chọn của Python: với hàng nghìn list nhỏ trong một chương trình, lãng phí 30% bộ nhớ đáng lo hơn việc copy thêm vài lần.\n\n### Chứng minh khấu hao KHÔNG cứu được độ trễ đuôi\n\n```python\nimport time\n\nds = []\nthoi_gian: list[float] = []\n\nfor i in range(2_000_000):\n    t0 = time.perf_counter_ns()\n    ds.append(i)\n    thoi_gian.append(time.perf_counter_ns() - t0)\n\nthoi_gian.sort()\nn = len(thoi_gian)\nprint(f\"trung vị (p50): {thoi_gian[n//2]:>10,} ns\")\nprint(f\"p99:            {thoi_gian[int(n*0.99)]:>10,} ns\")\nprint(f\"p999:           {thoi_gian[int(n*0.999)]:>10,} ns\")\nprint(f\"CHẬM NHẤT:      {thoi_gian[-1]:>10,} ns\")\nprint(f\"tỉ lệ chậm nhất / trung vị: \"\n      f\"{thoi_gian[-1] / max(1, thoi_gian[n//2]):>.0f}x\")\n```\n\n```\nKết quả có dạng (con số tuỳ máy, TỈ LỆ là điều đáng chú ý):\n\n  trung vị (p50):         ~50 ns\n  p99:                    ~80 ns\n  p999:                  ~120 ns\n  CHẬM NHẤT:       hàng chục nghìn ns\n  tỉ lệ chậm nhất / trung vị:  hàng trăm đến hàng nghìn lần\n\n⭐ KẾT LUẬN:\n  · Khấu hao O(1) là ĐÚNG — tổng thời gian tuyến tính theo n\n  · Nhưng MỘT LẦN append lẻ chậm hơn bình thường hàng trăm–nghìn lần\n  · Với SLA \"99,9% request dưới X ms\", khấu hao KHÔNG bảo đảm được\n```\n\nĐây là phản biện thực chất của bài: **khấu hao là lời hứa về tổng, không phải về từng lần.**\n\n### Ba cách xử lý khi cần độ trễ ổn định\n\n```python\n# ── ① CẤP PHÁT TRƯỚC khi biết n ──\ndef xu_ly_biet_truoc(n: int) -> list[int]:\n    kq: list[int | None] = [None] * n        # ⭐ cấp phát MỘT lần\n    for i in range(n):\n        kq[i] = i * i                         # ⭐ chỉ ghi, không bao giờ nở\n    return kq                                 # type: ignore[return-value]\n\n# Không có lần cấp phát lại nào ⇒ mọi thao tác O(1) THẬT\n```\n\n```python\n# ── ② DÙNG deque — cấu trúc khối, không copy toàn bộ khi nở ──\nfrom collections import deque\n\nhang_doi: deque[int] = deque()\nfor i in range(1_000_000):\n    hang_doi.append(i)                        # ⭐ O(1) THẬT, không khấu hao\n\n# deque là danh sách các KHỐI liên kết nhau.\n# Đầy một khối thì cấp thêm MỘT khối mới — KHÔNG copy khối cũ.\n# Đánh đổi: mất truy cập O(1) theo chỉ số (deque[i] là O(n) ở giữa).\n```\n\n```python\n# ── ③ Giới hạn kích thước cố định (ring buffer) ──\nfrom collections import deque\n\n# maxlen: tự bỏ phần tử cũ nhất khi đầy — KHÔNG bao giờ cấp phát lại\nlog_gan_nhat: deque[str] = deque(maxlen=1000)\nfor dong in doc_log():\n    log_gan_nhat.append(dong)                 # ⭐ O(1) thật, bộ nhớ O(1)\n\n# Rất phù hợp cho: log gần nhất, cửa sổ trượt, bộ đệm âm thanh\n```\n\nCách ③ đáng chú ý vì nó giải quyết **cả** hai vấn đề: độ trễ ổn định **và** bộ nhớ O(1). Đánh đổi là bạn mất dữ liệu cũ — nhưng với log gần nhất hay cửa sổ trượt thì đó chính là điều bạn muốn.\n\n### Cái bẫy dao động khi co lại\n\n```python\nclass MangDongDaoDong:\n    \"\"\"❌ Co khi size < capacity/2 → DAO ĐỘNG.\"\"\"\n\n    def __init__(self) -> None:\n        self._o: list[object | None] = [None] * 4\n        self._size = 0\n        self.so_lan_cap_phat = 0\n\n    def append(self, x: object) -> None:\n        if self._size == len(self._o):\n            self._cap_phat_lai(len(self._o) * 2)\n        self._o[self._size] = x\n        self._size += 1\n\n    def pop(self) -> object:\n        if self._size == 0:\n            raise IndexError(\"pop từ mảng rỗng\")\n        self._size -= 1\n        x = self._o[self._size]\n        # ⚠️ NGƯỠNG CO TRÙNG với vùng ngay dưới ngưỡng nở\n        if self._size < len(self._o) // 2 and len(self._o) > 4:\n            self._cap_phat_lai(len(self._o) // 2)\n        return x\n\n    def _cap_phat_lai(self, dl: int) -> None:\n        moi: list[object | None] = [None] * dl\n        for i in range(self._size):\n            moi[i] = self._o[i]\n        self._o = moi\n        self.so_lan_cap_phat += 1\n\n\nclass MangDongOnDinh:\n    \"\"\"✓ Co khi size < capacity/4, co xuống capacity/2 → có VÙNG ĐỆM.\"\"\"\n\n    def __init__(self) -> None:\n        self._o: list[object | None] = [None] * 4\n        self._size = 0\n        self.so_lan_cap_phat = 0\n\n    def append(self, x: object) -> None:\n        if self._size == len(self._o):\n            self._cap_phat_lai(len(self._o) * 2)\n        self._o[self._size] = x\n        self._size += 1\n\n    def pop(self) -> object:\n        if self._size == 0:\n            raise IndexError(\"pop từ mảng rỗng\")\n        self._size -= 1\n        x = self._o[self._size]\n        if self._size < len(self._o) // 4 and len(self._o) > 4:   # ⭐ /4\n            self._cap_phat_lai(len(self._o) // 2)                  # ⭐ xuống /2\n        return x\n\n    def _cap_phat_lai(self, dl: int) -> None:\n        moi: list[object | None] = [None] * dl\n        for i in range(self._size):\n            moi[i] = self._o[i]\n        self._o = moi\n        self.so_lan_cap_phat += 1\n\n\n# Kịch bản tấn công: append/pop luân phiên quanh ngưỡng\ndef thu(lop, so_lan: int):\n    m = lop()\n    for i in range(64):            # đưa size lên gần ngưỡng\n        m.append(i)\n    cap_phat_ban_dau = m.so_lan_cap_phat\n    for _ in range(so_lan):        # ⭐ luân phiên\n        m.append(1)\n        m.pop()\n    return m.so_lan_cap_phat - cap_phat_ban_dau\n\n\nprint(f\"1000 lần append/pop luân phiên:\")\nprint(f\"  Dao động: {thu(MangDongDaoDong, 1000):>5} lần cấp phát lại\")\nprint(f\"  Ổn định:  {thu(MangDongOnDinh, 1000):>5} lần cấp phát lại\")\n```\n\n```\n1000 lần append/pop luân phiên:\n  Dao động:  2000 lần cấp phát lại      ⚠️ MỖI thao tác một lần!\n  Ổn định:      0 lần cấp phát lại      ⭐\n\n⇒ Bản dao động: 2000 lần × O(n) copy  ⇒  mỗi thao tác thành O(n)\n  Bảo đảm khấu hao BỊ PHÁ HOÀN TOÀN.\n```\n\nBài học tổng quát: **hai ngưỡng phải cách nhau.** Nguyên tắc này xuất hiện lại ở bảng băm (module 3), ở cơ chế tự động co giãn của hệ thống, và cả ở bộ điều nhiệt — nơi người ta gọi nó là *hysteresis*.\n\n### Mảng động so với linked list — đo thật\n\n```python\nimport time\nfrom collections import deque\n\nn = 2_000_000\n\n# Thêm vào cuối\nt0 = time.perf_counter()\nds = []\nfor i in range(n):\n    ds.append(i)\nt_list = time.perf_counter() - t0\n\nt0 = time.perf_counter()\ndq: deque[int] = deque()\nfor i in range(n):\n    dq.append(i)\nt_deque = time.perf_counter() - t0\n\nprint(f\"append n lần:  list {t_list:.3f}s   deque {t_deque:.3f}s\")\n\n# Duyệt tuần tự — nơi cache locality lộ ra\nt0 = time.perf_counter()\ns = 0\nfor x in ds:\n    s += x\nt_list_duyet = time.perf_counter() - t0\n\nt0 = time.perf_counter()\ns = 0\nfor x in dq:\n    s += x\nt_deque_duyet = time.perf_counter() - t0\n\nprint(f\"duyệt hết:     list {t_list_duyet:.3f}s   deque {t_deque_duyet:.3f}s\")\n\n# Truy cập theo chỉ số ở GIỮA\nt0 = time.perf_counter()\nfor _ in range(10_000):\n    _ = ds[n // 2]\nprint(f\"10k lần ds[n/2]:   list {time.perf_counter() - t0:.4f}s\")\n\nt0 = time.perf_counter()\nfor _ in range(10_000):\n    _ = dq[n // 2]\nprint(f\"10k lần dq[n/2]:   deque {time.perf_counter() - t0:.4f}s  ⚠️ O(n)\")\n```\n\n```\nXU HƯỚNG dự kiến (con số tuỳ máy):\n\n  append n lần:      hai bên gần nhau — deque có thể hơi nhanh hơn\n  duyệt hết:         list NHANH HƠN — cache locality\n  10k lần [n/2]:     list nhanh hơn deque HÀNG NGHÌN LẦN — O(1) vs O(n)\n\n⇒ Mảng động trả một chi phí khấu hao nhỏ để đổi lấy:\n     · truy cập O(1) theo chỉ số\n     · cache locality tốt khi duyệt\n  Và đó gần như luôn là đánh đổi ĐÚNG.\n```\n\nDòng cuối là kết luận thực tế của cả bài: khấu hao **không phải là điểm yếu** của mảng động — nó là cái giá rất nhỏ để có hai lợi thế lớn.\n\n### Phương pháp kế toán — áp cho một cấu trúc khác\n\nĐể thấy phương pháp này tổng quát, áp cho bộ đếm nhị phân:\n\n```python\nclass BoDemNhiPhan:\n    \"\"\"Bộ đếm nhị phân. Tăng 1 có thể đổi nhiều bit, nhưng O(1) khấu hao.\"\"\"\n\n    def __init__(self, so_bit: int = 32) -> None:\n        self.bit = [0] * so_bit\n        self.so_lan_doi_bit = 0\n\n    def tang(self) -> None:\n        i = 0\n        while i < len(self.bit) and self.bit[i] == 1:\n            self.bit[i] = 0                   # ⭐ lật 1 → 0\n            self.so_lan_doi_bit += 1\n            i += 1\n        if i < len(self.bit):\n            self.bit[i] = 1                    # ⭐ lật 0 → 1\n            self.so_lan_doi_bit += 1\n\n\nfor n in (1_000, 10_000, 100_000):\n    bd = BoDemNhiPhan(32)\n    for _ in range(n):\n        bd.tang()\n    print(f\"n={n:>8,}  số lần đổi bit={bd.so_lan_doi_bit:>9,}  \"\n          f\"trên mỗi lần tăng={bd.so_lan_doi_bit/n:.3f}\")\n```\n\n```\nn=   1,000  số lần đổi bit=    1,998  trên mỗi lần tăng=1.998\nn=  10,000  số lần đổi bit=   19,998  trên mỗi lần tăng=2.000\nn= 100,000  số lần đổi bit=  199,998  trên mỗi lần tăng=2.000\n\n⭐ Xấu nhất MỘT lần tăng: đổi 32 bit (khi 0111...1 → 1000...0)\n  Khấu hao: đúng 2 bit mỗi lần tăng — HẰNG SỐ, không tăng theo n\n```\n\nChứng minh bằng phương pháp kế toán:\n\n```\nMỗi lần tăng \"trả\" 2 đồng:\n    1 đồng — lật MỘT bit từ 0 → 1\n    1 đồng — để dành, dùng để lật CHÍNH bit đó về 0 sau này\n\nMỗi lần lật 1 → 0 đều được trả bằng tiền để dành từ lúc nó lên 1.\n⇒ Không bao giờ thiếu tiền  ⇒  2 đồng/lần tăng  ⇒  O(1) khấu hao\n```\n\nSố đo `2.000` khớp chính xác với phân tích — và đó là điều nên làm với mọi phân tích khấu hao: **suy ra hằng số trên giấy, rồi đo để xác nhận.**",

so_sanh: "### Ba loại \"trung bình\" — phân biệt cho rõ\n\n| | Xấu nhất | **Khấu hao** | Trung bình |\n|---|---|---|---|\n| Đo gì | một thao tác | **tổng n thao tác / n** | kỳ vọng thống kê |\n| Giả định về dữ liệu | không | **không** | **có** (phân bố) |\n| Mức bảo đảm | tuyệt đối | **chắc chắn** | kỳ vọng |\n| Kẻ tấn công phá được | — | **✗** | **✓** |\n| Ví dụ | `append` O(n) | `append` **O(1)** | `dict` tra O(1) |\n\n**\"Amortized\" là lời hứa mạnh hơn \"average\"** — đọc tài liệu phải phân biệt.\n\n### Cộng thêm hằng số và nhân theo tỉ lệ\n\n| | Cộng thêm c ô | Nhân với r > 1 |\n|---|---|---|\n| Mốc cấp phát lại | c, 2c, 3c, … (**đều nhau**) | 1, r, r², … (**xa dần**) |\n| Tổng chi phí copy | **n²/(2c)** | **n·r/(r−1)** |\n| Bậc tổng | **O(n²)** | **O(n)** |\n| Khấu hao mỗi lần | **O(n)** | **O(1)** |\n| n = 10⁶ với c=10 / r=2 | ~50 tỉ copy | ~2 triệu copy |\n\nRanh giới không phải \"nhân đôi hay không\" mà là **\"nhân hay cộng\"**.\n\n### Hệ số tăng trưởng — đánh đổi\n\n| Hệ số | Số lần cấp phát | copy/n | Lãng phí bộ nhớ |\n|---|---|---|---|\n| 1,125 (Python) | nhiều nhất | ~8 | **~2,5%** |\n| 1,5 | vừa | ~2,5 | ~19% |\n| 2,0 | ít | ~1 | ~31% |\n| 3,0 | ít nhất | ~0,75 | **~52%** |\n\nMọi hệ số > 1 đều cho O(1) khấu hao — chỉ khác **hằng số**. Python chọn nhỏ vì lãng phí bộ nhớ đáng lo hơn với nhiều list nhỏ.\n\n### Hai phương pháp chứng minh\n\n| | Tổng hợp | Kế toán |\n|---|---|---|\n| Cách làm | cộng tổng, chia n | mỗi thao tác trả trước một khoản |\n| Dễ cho | dãy đơn giản | **cấu trúc phức tạp** |\n| Cho biết | kết quả | **có thiết kế đúng không** |\n| Ví dụ trong bài | `append`, tổng < 3n | 3 đồng/append, 2 đồng/tăng bộ đếm |\n\n### Cái bẫy dao động\n\n| | Co khi size < cap/2 | Co khi size < cap/4, xuống cap/2 |\n|---|---|---|\n| Vùng đệm giữa hai ngưỡng | **✗ không có** | **✓ có** |\n| 1000 lần append/pop luân phiên | **2000 lần cấp phát** | **0 lần** |\n| Khấu hao | **BỊ PHÁ → O(n)** | O(1) |\n\nNguyên tắc: **hai ngưỡng phải cách nhau** (hysteresis). Xuất hiện lại ở bảng băm và ở tự động co giãn hệ thống.\n\n### Khi khấu hao KHÔNG đủ\n\n| Lĩnh vực | Vì sao | Cách xử lý |\n|---|---|---|\n| Thời gian thực (âm thanh, điều khiển) | cần độ trễ **ổn định** | cấp phát trước |\n| Giao dịch tần số cao | quan tâm p99, p999 | cấp phát trước, `deque` |\n| SLA về độ trễ đuôi | khấu hao không bảo đảm p999 | ring buffer `maxlen` |\n\nKhấu hao là lời hứa về **tổng**, không phải về **từng lần**.\n\n### Ba cách có độ trễ ổn định\n\n| Cách | Độ trễ | Bộ nhớ | Đánh đổi |\n|---|---|---|---|\n| Cấp phát trước `[None]*n` | **O(1) thật** | O(n) | phải **biết trước n** |\n| `deque` | **O(1) thật** | O(n) | `dq[i]` giữa là **O(n)** |\n| `deque(maxlen=k)` | **O(1) thật** | **O(k)** | **mất dữ liệu cũ** |\n\nCách cuối giải quyết cả độ trễ và bộ nhớ — và với log gần nhất hay cửa sổ trượt, mất dữ liệu cũ chính là điều bạn muốn.\n\n### Mảng động so với linked list\n\n| | Mảng động | Linked list |\n|---|---|---|\n| Thêm cuối | O(1) **khấu hao** | O(1) **thật** |\n| `ds[i]` | **O(1)** | **O(n)** ⚠️ |\n| Bộ nhớ mỗi phần tử | 1 con trỏ | 1 con trỏ + 1 `next` |\n| Cache locality | **✓ rất tốt** | ✗ rất tệ |\n| Duyệt tuần tự | **nhanh** | chậm hơn nhiều lần |\n\nMảng động trả một chi phí khấu hao **nhỏ** để đổi lấy truy cập O(1) và cache locality — gần như luôn là đánh đổi đúng.\n\n### Thao tác Python cần khấu hao\n\n| Thao tác | Xấu nhất | Thực tế |\n|---|---|---|\n| `list.append(x)` | O(n) | **O(1) khấu hao** |\n| `list.pop()` | O(n) | O(1) khấu hao |\n| `dict[k] = v` | O(n) | O(1) **trung bình** |\n| `set.add(x)` | O(n) | O(1) **trung bình** |\n| `deque.append(x)` | **O(1)** | O(1) thật |\n| `list.insert(0, x)` | **O(n)** | **O(n)** — không khấu hao được |",

loi_thuong_gap: [
  "**Lẫn khấu hao với trung bình.** Khấu hao không giả định gì về dữ liệu; trung bình thì có, và có thể bị phá.",
  "**Nghĩ \"O(n) xấu nhất\" và \"O(1) khấu hao\" là mâu thuẫn.** Hai phát biểu về hai thứ khác nhau, cả hai đều đúng.",
  "**Cấp phát thêm một lượng CỐ ĐỊNH khi tự viết buffer.** Cho O(n²) tổng — lỗi thật rất hay gặp.",
  "**Tưởng phải nhân đôi mới được O(1) khấu hao.** Mọi hệ số > 1 đều được; ranh giới là *nhân hay cộng*.",
  "**Chọn hệ số tăng trưởng lớn cho mọi trường hợp.** Hệ số 3 lãng phí hơn 50% bộ nhớ.",
  "**Để ngưỡng co trùng ngay dưới ngưỡng nở.** Gây dao động và phá hoàn toàn bảo đảm khấu hao.",
  "**Dùng khấu hao để trấn an về độ trễ đuôi.** Một `append` lẻ có thể chậm hàng nghìn lần — không đạt SLA p999.",
  "**Không cấp phát trước khi đã biết n.** `[None] * n` loại bỏ hẳn mọi lần cấp phát lại.",
  "**Dùng `list` làm hàng đợi FIFO.** `pop(0)` là O(n) và **không** khấu hao được; dùng `deque`.",
  "**Tưởng `deque[i]` là O(1).** Truy cập giữa deque là O(n) — đó là cái giá của cấu trúc khối.",
  "**Quên `deque(maxlen=k)` khi cần cửa sổ trượt.** Nó cho O(1) thật và bộ nhớ O(1) cùng lúc.",
  "**Chọn linked list vì \"append là O(1) thật\".** Mất truy cập O(1) và cache locality — thường lỗ nặng.",
  "**Tin `sys.getsizeof` là dung lượng chính xác.** Nó đo vỏ list, hữu ích để *thấy mốc cấp phát*, không phải bộ nhớ tổng.",
  "**Phân tích khấu hao trên giấy mà không đo.** Tỉ lệ copy/n phải là hằng số không tăng theo n — kiểm được trong ba mươi giây.",
  "**Áp khấu hao cho thao tác không khấu hao được.** `insert(0, x)` là O(n) mỗi lần, không có cách nào chia nhỏ chi phí."
],

tu_kiem_tra: [
  "Định nghĩa độ phức tạp khấu hao. Vì sao nó là *bảo đảm chắc chắn* chứ không phải kỳ vọng?",
  "Phân biệt xấu nhất, khấu hao, trung bình. Cho ví dụ Python cho mỗi loại.",
  "Vì sao \"amortized\" là lời hứa mạnh hơn \"average\"? Cho ví dụ điều gì phá được average.",
  "Vì sao mảng cần vùng bộ nhớ liên tục? Điều đó cho lợi ích gì và gây vấn đề gì?",
  "Suy ra tổng chi phí copy khi cấp phát thêm c ô mỗi lần. Bậc là gì?",
  "Suy ra tổng chi phí copy khi nhân đôi. Vì sao tổng < 2n?",
  "Với n = 10⁶, so sánh số phép copy giữa cộng-thêm-10 và nhân-đôi.",
  "Vì sao mọi hệ số > 1 đều cho O(1) khấu hao? Viết công thức tổng theo r.",
  "Python dùng hệ số bao nhiêu? Vì sao chọn nhỏ thay vì 2?",
  "Nêu đánh đổi giữa hệ số lớn và hệ số nhỏ, theo hai tiêu chí.",
  "Trình bày phương pháp kế toán cho `append` với 3 đồng. Mỗi đồng dùng làm gì?",
  "Phương pháp kế toán cho bộ đếm nhị phân dùng mấy đồng? Giải thích.",
  "Vì sao ngưỡng co không được trùng ngay dưới ngưỡng nở? Thiết kế đúng là gì?",
  "Khấu hao KHÔNG đủ trong ba lĩnh vực nào? Vì sao?",
  "Nêu ba cách có độ trễ ổn định, kèm đánh đổi của mỗi cách.",
  "`deque(maxlen=k)` giải quyết được mấy vấn đề cùng lúc? Đánh đổi là gì?",
  "Vì sao mảng động vẫn thắng linked list dù phải khấu hao? Nêu hai lý do.",
  "Vì sao `list.insert(0, x)` KHÔNG khấu hao được thành O(1)?",
  "Làm sao kiểm chứng O(1) khấu hao bằng thực nghiệm? Đại lượng nào phải là hằng số?"
],

tai_nguyen: [
  "Sách **Introduction to Algorithms** — CLRS. Chương 17 dành riêng cho phân tích khấu hao: phương pháp tổng hợp, kế toán, và phương pháp thế năng (potential method) mà bài này chưa nói tới.",
  "Sách **Algorithms** — Sedgewick & Wayne. Phần *Resizing arrays* với phân tích và đo thực nghiệm của mảng động.",
  "**Mã nguồn CPython — `Objects/listobject.c`**, hàm `list_resize`. Đọc để thấy công thức tăng trưởng thật; đây là ứng dụng trực tiếp của bài *đọc mã nguồn khi tài liệu không nói rõ* ở module 9 mảng 01.",
  "**Tài liệu Python — `collections.deque`** — cấu trúc khối, `maxlen`, và bảng độ phức tạp của từng thao tác.",
  "**Tài liệu Python — TimeComplexity** trong wiki chính thức: cột phân biệt rõ \"amortized\" và \"average\" cho từng thao tác."
],

lien_he: [
  "**Ký hiệu Big-O** ở module này: hai trục \"trường hợp\" và \"ký hiệu\"; khấu hao là một trục thứ ba, không thuộc hai trục đó.",
  "**Vòng lặp lồng nhau** ở module này: tổng cấp số cộng cho O(n²), tổng cấp số nhân cho O(n) — cùng hai công thức, dùng lại ở đây.",
  "**Độ phức tạp không gian** ngay trước: cấp phát thừa chỗ là đánh đổi bộ nhớ lấy thời gian; hệ số tăng trưởng quyết định mức lãng phí.",
  "**Cấu trúc dữ liệu tuyến tính** ở module 2: mảng động, `deque` cấu trúc khối, và vì sao `insert(0, x)` là O(n).",
  "**Hash** ở module 3: bảng băm cũng cấp phát lại theo tỉ lệ, và nguyên tắc hai ngưỡng cách nhau xuất hiện lại.",
  "**CPU, RAM, cache** ở module 1 mảng 01: cache locality là lý do mảng thắng linked list dù cùng bậc.",
  "**List, dict, set, tuple** ở module 5 mảng 01: bài này giải thích cơ chế bên dưới `append`.",
  "**Đo bộ nhớ và tối ưu cơ bản** ở module 7 mảng 01: `sys.getsizeof` để thấy mốc cấp phát, `tracemalloc` để đo tổng.",
  "**Đọc tài liệu chính thức** ở module 9 mảng 01: đọc `listobject.c` khi muốn biết công thức tăng trưởng thật.",
  "**Hiệu năng và vận hành** ở mảng 05: độ trễ đuôi p99/p999 và vì sao trung bình không đủ để đặt SLA.",
  "**Các khối xây dựng** ở mảng 06: ring buffer và cơ chế tự động co giãn — nguyên tắc hysteresis ở quy mô hệ thống."
]

};
