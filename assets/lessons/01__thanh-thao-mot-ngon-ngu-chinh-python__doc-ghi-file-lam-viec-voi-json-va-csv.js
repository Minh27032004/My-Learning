window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:doc-ghi-file-lam-viec-voi-json-va-csv"] = {

tom_tat: [
  "**Luôn ghi rõ `encoding=\"utf-8\"` khi mở file văn bản.** Bỏ qua nó là code chạy trên máy bạn mà lỗi trên máy khác — nguyên nhân số một của lỗi tiếng Việt trong Python.",
  "**`open()` trả về hai thế giới khác nhau**: chế độ văn bản cho `str` (có giải mã, có xử lý xuống dòng), chế độ nhị phân cho `bytes` (thô, không đụng vào gì).",
  "**Lặp trực tiếp qua file là đọc theo dòng, không nạp cả file.** `for dong in f` chạy được với file 50 GB; `f.read()` thì không.",
  "**Đừng tự tách CSV bằng `split(\",\")`.** Trường có dấu phẩy trong dấu nháy sẽ vỡ. Dùng module `csv` — nó xử lý nháy, xuống dòng trong ô, và ký tự thoát.",
  "**JSON không có kiểu ngày tháng, tuple, hay set.** `json.dump` sẽ báo lỗi hoặc lặng lẽ đổi `tuple` thành `list` — biết trước để không mất dữ liệu."
],

can_biet_truoc: [
  "Bài **Xử lý ngoại lệ** ngay trước — `FileNotFoundError`, `PermissionError`, `UnicodeDecodeError`, `JSONDecodeError` đều gặp ở đây.",
  "Bài **Unicode, UTF-8** ở module 1 — `str` là ký tự, `bytes` là byte; đây là bài áp dụng trực tiếp của nó.",
  "Bài **Hệ thống file, đường dẫn, quyền truy cập** ở module 2 — đường dẫn tương đối/tuyệt đối và quyền đọc ghi.",
  "Bài **Comprehension và generator** — xử lý file lớn theo dòng là ứng dụng chính của generator."
],

dinh_nghia: "Nói đời thường: một file trên đĩa chỉ là **một dãy byte**. Đọc nó ra thành chữ đọc được cần biết **quy ước mã hoá** — giống như biết một bản nhạc được viết theo hệ ký âm nào. Đoán sai quy ước thì vẫn đọc ra được thứ gì đó, chỉ là sai.\n\nNói chính xác:\n\n- **`open()`** — mở file, trả về một object file. Tham số quan trọng: chế độ (`mode`) và bảng mã (`encoding`).\n- **Chế độ văn bản** (`\"r\"`, `\"w\"`) — đọc/ghi `str`. Python **giải mã** byte thành ký tự và chuẩn hoá ký tự xuống dòng.\n- **Chế độ nhị phân** (`\"rb\"`, `\"wb\"`) — đọc/ghi `bytes` thô. Không giải mã, không đụng vào xuống dòng.\n- **`encoding`** — bảng mã dùng để chuyển byte ↔ ký tự. Mặc định **phụ thuộc hệ điều hành**, nên phải ghi rõ.\n- **JSON** — định dạng văn bản trao đổi dữ liệu. Có `object`, `array`, `string`, `number`, `true/false/null`.\n- **CSV** — bảng dạng văn bản, ngăn cách bằng dấu phẩy. Nghe đơn giản nhưng có nhiều quy tắc thoát ký tự.\n- **`pathlib.Path`** — cách làm việc với đường dẫn theo hướng đối tượng, thay cho ghép chuỗi.\n\nĐiểm cần nắm ngay: **tham số `encoding` mặc định của `open()` không giống nhau giữa các máy.** Trên nhiều bản Linux và macOS nó là UTF-8; trên Windows nó thường là bảng mã địa phương. Đây chính là lý do một file tiếng Việt đọc tốt ở máy này và ra ký tự lạ ở máy khác — và cách chữa chỉ là gõ thêm chín ký tự.",

vi_sao_can: "Đọc ghi file là việc gần như mọi chương trình đều làm, và là nơi những lỗi khó chịu nhất trú ngụ:\n\n- **Lỗi mã hoá là lỗi khó chịu nhất với người Việt.** Chữ có dấu biến thành ký tự lạ, hoặc `UnicodeDecodeError` giữa chừng khi đã xử lý xong nửa file. Nguyên nhân gần như luôn là thiếu `encoding=\"utf-8\"`.\n- **File lớn làm hết bộ nhớ.** `f.read()` trên file 10 GB là chương trình chết. Biết đọc theo dòng là khác biệt giữa chạy được và không.\n- **JSON và CSV là hai định dạng trao đổi dữ liệu phổ biến nhất.** API trả JSON, dữ liệu bảng đến dạng CSV. Đọc và ghi đúng hai thứ này là kỹ năng dùng hằng ngày.\n- **Tự tách CSV bằng `split` là cái bẫy trông rất vô hại.** Nó chạy đúng với dữ liệu mẫu và vỡ với dữ liệu thật — cụ thể là ngay khi có một địa chỉ chứa dấu phẩy.\n- **Định hướng AI/ML.** Nạp dataset, ghi kết quả, lưu cấu hình thí nghiệm — toàn bộ đều là bài này. Và dataset thật thì luôn có dòng hỏng, ký tự lạ, cột thiếu.\n\nTình huống thật rất kinh điển: bạn viết script xử lý file CSV xuất từ bảng tính. Chạy tốt trên máy bạn. Gửi cho đồng nghiệp thì `UnicodeDecodeError`, hoặc tệ hơn — chạy xong mà mọi chữ tiếng Việt thành ký tự lạ, và không có lỗi nào báo. Nguyên nhân: file được lưu bằng một bảng mã, đọc bằng bảng mã khác. Phần cơ chế sẽ nói cách phòng và cách chữa khi đã lỡ.",

co_che: "### `open()` và bảng mã\n\n```python\n# ❌ Phụ thuộc hệ điều hành — nguồn lỗi số một\nwith open(\"du_lieu.txt\") as f:\n    ...\n\n# ✓ Luôn ghi rõ\nwith open(\"du_lieu.txt\", encoding=\"utf-8\") as f:\n    ...\n```\n\nChuyện gì xảy ra khi bảng mã sai:\n\n```\nFile lưu UTF-8, đọc bằng bảng mã 1 byte\n  → \"chào\" thành \"chÃ o\"   (mojibake — KHÔNG báo lỗi, âm thầm sai)\n\nFile lưu UTF-8, đọc bằng bảng mã không hợp lệ cho byte đó\n  → UnicodeDecodeError giữa chừng\n```\n\nTrường hợp đầu nguy hiểm hơn: **không có lỗi nào**, dữ liệu chỉ sai. Bạn xử lý xong, ghi ra file, và phát hiện ra sau khi đã mất bản gốc.\n\nBa tham số của `open()` nên biết:\n\n```python\nopen(duong_dan,\n     mode=\"r\",            # r đọc · w ghi đè · a nối · x tạo mới (lỗi nếu đã có)\n     encoding=\"utf-8\",    # ⭐ luôn ghi\n     newline=None,        # xử lý ký tự xuống dòng — xem mục CSV\n     errors=\"strict\")     # làm gì khi gặp byte không giải mã được\n```\n\nTham số `errors` khi phải xử lý dữ liệu bẩn:\n\n```python\nerrors=\"strict\"    # mặc định — ném UnicodeDecodeError\nerrors=\"replace\"   # thay ký tự hỏng bằng \"�\", chạy tiếp\nerrors=\"ignore\"    # bỏ hẳn byte hỏng — ⚠️ MẤT dữ liệu im lặng\nerrors=\"surrogateescape\"   # giữ byte hỏng để ghi lại nguyên vẹn\n```\n\nDùng `errors=\"ignore\"` là quyết định nên cân nhắc kỹ: nó biến một lỗi rõ ràng thành mất dữ liệu âm thầm — đúng phản mẫu đã nói ở bài ngoại lệ. `replace` tốt hơn vì ít nhất bạn **nhìn thấy** chỗ hỏng.\n\n### Chế độ văn bản và nhị phân\n\n```python\nwith open(\"a.txt\", \"r\", encoding=\"utf-8\") as f:\n    s = f.read()          # str — đã giải mã\n\nwith open(\"a.bin\", \"rb\") as f:\n    b = f.read()          # bytes — thô\n```\n\n| | Văn bản (`\"r\"`) | Nhị phân (`\"rb\"`) |\n|---|---|---|\n| Kiểu trả về | `str` | `bytes` |\n| Giải mã | ✓ theo `encoding` | ✗ |\n| Chuẩn hoá xuống dòng | ✓ | ✗ |\n| `encoding` | **bắt buộc nên ghi** | ✗ không dùng |\n| Dùng cho | text, JSON, CSV | ảnh, mô hình, file nén |\n\nHàng \"chuẩn hoá xuống dòng\" đáng giải thích: Windows kết thúc dòng bằng `\\r\\n`, Linux và macOS bằng `\\n`. Chế độ văn bản **tự chuyển hết về `\\n` khi đọc** và chuyển ngược khi ghi. Đây là tiện ích lớn, nhưng cũng là nguyên nhân của lỗi dòng trống trong CSV — xem mục dưới.\n\n### Đọc file lớn — bốn cách và khi nào dùng\n\n```python\n# ① read() — CẢ file vào một chuỗi\nwith open(f, encoding=\"utf-8\") as fh:\n    s = fh.read()                 # file 10 GB ⇒ hết RAM\n\n# ② readlines() — CẢ file thành list các dòng\n    ds = fh.readlines()           # cũng hết RAM, còn tốn hơn\n\n# ③ ⭐ Lặp trực tiếp — TỪNG DÒNG, bộ nhớ không đổi\n    for dong in fh:\n        xu_ly(dong.rstrip(\"\\n\"))\n\n# ④ Đọc theo khối — cho file nhị phân\n    while khoi := fh.read(8192):\n        xu_ly(khoi)\n```\n\nCách ③ là mặc định đúng cho file văn bản. Nó hoạt động được vì object file **chính là một iterator**: mỗi lần lặp, nó đọc thêm một dòng từ bộ đệm. Nối trực tiếp với bài generator — đây là tính lười ở tầng thư viện chuẩn.\n\n```python\nimport sys\n\n# So sánh bộ nhớ trên file 1 GB\nwith open(\"lon.txt\", encoding=\"utf-8\") as f:\n    s = f.read()              # ~1 GB trong RAM\n\nwith open(\"lon.txt\", encoding=\"utf-8\") as f:\n    for dong in f:            # ~vài KB\n        pass\n```\n\nLưu ý `dong` vẫn còn ký tự `\\n` ở cuối. Dùng `.rstrip(\"\\n\")` chứ không phải `.strip()`: `.strip()` bỏ cả khoảng trắng đầu dòng, và với dữ liệu có thụt lề có nghĩa thì đó là mất thông tin.\n\n### Ghi file\n\n```python\n# w — GHI ĐÈ, xoá sạch nội dung cũ ngay khi mở\nwith open(\"kq.txt\", \"w\", encoding=\"utf-8\") as f:\n    f.write(\"dòng 1\\n\")                    # write KHÔNG tự thêm \\n\n    f.writelines([\"a\\n\", \"b\\n\"])           # cũng không tự thêm\n    print(\"dòng 2\", file=f)                # ⭐ print thì CÓ tự thêm\n\n# a — nối vào cuối\nwith open(\"nhat_ky.txt\", \"a\", encoding=\"utf-8\") as f:\n    f.write(\"dòng mới\\n\")\n\n# x — tạo mới, LỖI nếu file đã tồn tại\ntry:\n    with open(\"quan_trong.txt\", \"x\", encoding=\"utf-8\") as f:\n        f.write(\"...\")\nexcept FileExistsError:\n    print(\"file đã tồn tại, không ghi đè\")\n```\n\nChế độ `\"x\"` ít người dùng nhưng rất đáng biết: nó là cách **chống ghi đè nhầm** ở mức hệ điều hành, an toàn hơn việc tự kiểm tra `os.path.exists` rồi mới ghi (lại là khe hở đua tranh ở bài ngoại lệ).\n\nCạm bẫy của `\"w\"`: nó **cắt file về rỗng ngay lúc mở**, trước cả khi bạn ghi gì. Nếu chương trình lỗi sau đó, bạn mất luôn nội dung cũ.\n\n### Ghi an toàn — mẫu ghi tạm rồi đổi tên\n\n```python\nimport os, tempfile\n\ndef ghi_an_toan(duong_dan, noi_dung):\n    \"\"\"Ghi kiểu tất-cả-hoặc-không-gì. File cũ chỉ mất khi bản mới đã hoàn chỉnh.\"\"\"\n    thu_muc = os.path.dirname(duong_dan) or \".\"\n    fd, tam = tempfile.mkstemp(dir=thu_muc, suffix=\".tmp\")\n    try:\n        with os.fdopen(fd, \"w\", encoding=\"utf-8\") as f:\n            f.write(noi_dung)\n            f.flush()\n            os.fsync(f.fileno())        # ⭐ ép ghi thật xuống đĩa\n        os.replace(tam, duong_dan)      # ⭐ đổi tên là thao tác nguyên tử\n    except BaseException:\n        os.unlink(tam)\n        raise\n```\n\nHai dòng đánh dấu ⭐ là mấu chốt:\n\n```\nfsync       dữ liệu mới nằm ở bộ đệm hệ điều hành, mất điện là mất.\n            fsync ép nó xuống đĩa thật.\nos.replace  đổi tên trong cùng hệ thống file là NGUYÊN TỬ:\n            hoặc file cũ, hoặc file mới, không bao giờ có trạng thái nửa vời.\n```\n\nMẫu này đáng dùng cho mọi file quan trọng: cấu hình, kết quả tính toán dài, checkpoint mô hình. Nó là lý do một chương trình bị tắt giữa chừng vẫn không làm hỏng dữ liệu đã có.\n\n### `pathlib` thay cho ghép chuỗi\n\n```python\nfrom pathlib import Path\n\n# ❌ ghép chuỗi — sai dấu gạch trên Windows\nduong_dan = thu_muc + \"/\" + ten_file\n\n# ✓ pathlib — toán tử / hoạt động đúng trên mọi hệ điều hành\np = Path(thu_muc) / ten_file\n\np.exists(); p.is_file(); p.is_dir()\np.stem            # tên không có phần mở rộng\np.suffix          # \".csv\"\np.parent          # thư mục cha\np.name            # tên file đầy đủ\np.absolute()\n\n# Đọc ghi một phát — tiện cho file nhỏ\nnoi_dung = p.read_text(encoding=\"utf-8\")\np.write_text(\"xin chào\", encoding=\"utf-8\")\ndu_lieu = p.read_bytes()\n\n# Duyệt thư mục\nfor f in Path(\"du_lieu\").glob(\"*.csv\"):        # một tầng\n    print(f)\nfor f in Path(\"du_lieu\").rglob(\"*.csv\"):       # đệ quy mọi tầng\n    print(f)\n\nPath(\"a/b/c\").mkdir(parents=True, exist_ok=True)   # tạo cả cây thư mục\n```\n\nMẫu tìm file dữ liệu **nằm cạnh module** — rất hay cần và hay làm sai:\n\n```python\n# ❌ phụ thuộc thư mục người dùng đang đứng\nwith open(\"du_lieu/bang.csv\") as f: ...\n\n# ✓ tính từ vị trí CỦA FILE .py này\nGOC = Path(__file__).resolve().parent\nwith open(GOC / \"du_lieu\" / \"bang.csv\", encoding=\"utf-8\") as f: ...\n```\n\nĐây là ứng dụng trực tiếp của bài module trước: `__file__` cho biết file mã nguồn nằm đâu, nên đường dẫn không còn phụ thuộc chỗ chạy.\n\n### JSON\n\n```python\nimport json\n\n# Ghi\nvoi_tv = {\"ten\": \"Minh\", \"diem\": [8, 9], \"dat\": True, \"ghi_chu\": None}\nwith open(\"a.json\", \"w\", encoding=\"utf-8\") as f:\n    json.dump(voi_tv, f, ensure_ascii=False, indent=2)\n#                        ⭐ ensure_ascii=False giữ nguyên tiếng Việt\n\n# Đọc\nwith open(\"a.json\", encoding=\"utf-8\") as f:\n    d = json.load(f)\n\n# Với chuỗi thay vì file — thêm chữ s\ns = json.dumps(voi_tv, ensure_ascii=False)\nd = json.loads(s)\n```\n\n`ensure_ascii=False` là tham số quan trọng nhất với dữ liệu tiếng Việt:\n\n```python\njson.dumps({\"ten\": \"Chào\"})                      # {\"ten\": \"Ch\\u00e0o\"}\njson.dumps({\"ten\": \"Chào\"}, ensure_ascii=False)  # {\"ten\": \"Chào\"}\n```\n\nCả hai đều **hợp lệ và đọc lại đúng**, nhưng bản thứ hai người đọc được, file nhỏ hơn, và `git diff` có nghĩa.\n\nBảng chuyển kiểu — đây là chỗ hay mất dữ liệu:\n\n| Python | JSON | Đọc lại thành |\n|---|---|---|\n| `dict` | object | `dict` |\n| `list` | array | `list` |\n| **`tuple`** | array | **`list`** ⚠️ |\n| `str` | string | `str` |\n| `int`, `float` | number | `int`, `float` |\n| `True`/`False` | `true`/`false` | `bool` |\n| `None` | `null` | `None` |\n| **`set`** | **✗ TypeError** | — |\n| **`datetime`** | **✗ TypeError** | — |\n\nHai hàng cuối phải xử lý bằng tay:\n\n```python\nfrom datetime import datetime, date\n\nclass MaHoa(json.JSONEncoder):\n    def default(self, o):\n        if isinstance(o, (datetime, date)):\n            return o.isoformat()          # \"2026-08-30T10:00:00\"\n        if isinstance(o, set):\n            return sorted(o)              # set → list, có thứ tự ổn định\n        return super().default(o)\n\njson.dumps({\"luc\": datetime.now(), \"the\": {1, 2}},\n           cls=MaHoa, ensure_ascii=False)\n```\n\nLưu ý hàng `tuple`: nó **không báo lỗi** mà lặng lẽ thành `list`. Ghi rồi đọc lại thì `(1, 2)` thành `[1, 2]` — mất tính hashable, không làm khoá `dict` được nữa. Đây là mất dữ liệu im lặng, và biết trước là cách duy nhất để tránh.\n\nXử lý JSON hỏng:\n\n```python\ntry:\n    d = json.loads(s)\nexcept json.JSONDecodeError as e:\n    print(f\"JSON sai ở dòng {e.lineno} cột {e.colno}: {e.msg}\")\n    print(f\"quanh vị trí: {s[max(0, e.pos-30):e.pos+30]!r}\")\n```\n\n`JSONDecodeError` mang theo `lineno`, `colno`, `pos` — dùng chúng để chỉ đúng chỗ hỏng thay vì chỉ in \"JSON không hợp lệ\".\n\n### JSON Lines — cho dữ liệu lớn\n\n```python\n# Mỗi dòng là một JSON độc lập\n# {\"id\": 1, \"ten\": \"a\"}\n# {\"id\": 2, \"ten\": \"b\"}\n\n# Ghi — thêm được mà không cần đọc lại file cũ\nwith open(\"du_lieu.jsonl\", \"a\", encoding=\"utf-8\") as f:\n    for bg in ban_ghi:\n        f.write(json.dumps(bg, ensure_ascii=False) + \"\\n\")\n\n# Đọc — từng dòng, bộ nhớ không đổi\ndef doc_jsonl(duong_dan):\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        for so, dong in enumerate(f, 1):\n            dong = dong.strip()\n            if not dong:\n                continue\n            try:\n                yield json.loads(dong)\n            except json.JSONDecodeError as e:\n                print(f\"bỏ qua dòng {so}: {e}\")     # một dòng hỏng không phá cả file\n```\n\nSo với một file JSON khổng lồ chứa một mảng lớn, JSON Lines hơn ở ba điểm: **nối thêm được**, **đọc theo dòng được**, và **một dòng hỏng không làm hỏng cả file**. Đây là lý do nó là định dạng mặc định cho log và cho dataset lớn.\n\n### CSV — và vì sao đừng `split(\",\")`\n\n```python\ndong = 'Minh,\"Hà Nội, Việt Nam\",22'\n\ndong.split(\",\")\n# ['Minh', '\"Hà Nội', ' Việt Nam\"', '22']     ← ❌ VỠ, 4 trường thay vì 3\n\nimport csv, io\nnext(csv.reader(io.StringIO(dong)))\n# ['Minh', 'Hà Nội, Việt Nam', '22']           ← ✓ đúng 3 trường\n```\n\nModule `csv` xử lý ba thứ mà `split` không:\n\n```\n① Dấu phẩy BÊN TRONG dấu nháy\n② Dấu nháy bên trong trường:  \"anh \"\"Minh\"\" đó\"\n③ Ký tự XUỐNG DÒNG bên trong một ô\n```\n\nĐiểm ③ là lý do vì sao đọc CSV theo dòng bằng tay cũng sai: một bản ghi CSV có thể **trải trên nhiều dòng văn bản**.\n\n```python\nimport csv\n\n# Đọc theo tên cột — ⭐ nên dùng\nwith open(\"a.csv\", newline=\"\", encoding=\"utf-8\") as f:\n    for hang in csv.DictReader(f):\n        print(hang[\"ten\"], hang[\"diem\"])       # hang là dict\n\n# Ghi\nwith open(\"b.csv\", \"w\", newline=\"\", encoding=\"utf-8\") as f:\n    w = csv.DictWriter(f, fieldnames=[\"ten\", \"diem\"])\n    w.writeheader()\n    w.writerow({\"ten\": \"Minh\", \"diem\": 8})\n    w.writerows(danh_sach)\n```\n\n**`newline=\"\"` là bắt buộc** với module `csv`, và lý do rất cụ thể:\n\n```\nKhông có newline=\"\" trên Windows:\n  csv ghi \\r\\n  →  chế độ văn bản đổi \\n thành \\r\\n nữa\n  →  thành \\r\\r\\n  →  file có DÒNG TRỐNG xen kẽ\n```\n\nĐây là lỗi kinh điển \"CSV của tôi cứ cách dòng\", và cách chữa chỉ là thêm `newline=\"\"`. Module `csv` tự lo phần xuống dòng, nên phải bảo `open()` đừng can thiệp.\n\nMọi giá trị đọc từ CSV đều là **chuỗi** — CSV không có kiểu:\n\n```python\nfor hang in csv.DictReader(f):\n    diem = float(hang[\"diem\"])       # phải tự chuyển\n    tuoi = int(hang[\"tuoi\"])\n```\n\n### Ký tự BOM — bẫy của file xuất từ bảng tính\n\n```python\n# File xuất từ nhiều phần mềm bảng tính bắt đầu bằng BOM (\\ufeff)\nwith open(\"xuat.csv\", newline=\"\", encoding=\"utf-8\") as f:\n    hang = next(csv.DictReader(f))\n    print(hang.keys())        # dict_keys(['\\ufeffma', 'ten'])  ← cột đầu HỎNG\n    hang[\"ma\"]                # KeyError!\n\n# ✓ utf-8-sig tự bỏ BOM nếu có, và vẫn đúng nếu không có\nwith open(\"xuat.csv\", newline=\"\", encoding=\"utf-8-sig\") as f:\n    ...\n```\n\nQuy tắc thực dụng: **đọc CSV từ nguồn không kiểm soát thì dùng `utf-8-sig`.** Nó an toàn trong cả hai trường hợp có và không có BOM. Khi ghi thì dùng `utf-8` thường, trừ khi bạn biết chắc người nhận cần BOM.",

vi_du: "### Chẩn đoán và chữa lỗi mã hoá\n\n```python\n# Bước 1: xem byte thật để đoán bảng mã\nwith open(\"bi_an.txt\", \"rb\") as f:\n    dau = f.read(200)\nprint(dau)\n# b'Ch\\xc3\\xa0o'      → \\xc3\\xa0 là \"à\" trong UTF-8\n# b'Ch\\xe0o'          → \\xe0 là \"à\" trong bảng mã 1 byte\n# b'\\xef\\xbb\\xbfCh...' → có BOM ⇒ dùng utf-8-sig\n\n# Bước 2: thử vài bảng mã\nfor bm in (\"utf-8\", \"utf-8-sig\", \"cp1258\", \"latin-1\"):\n    try:\n        with open(\"bi_an.txt\", encoding=bm) as f:\n            print(f\"{bm:<12} {f.readline()[:60]!r}\")\n    except UnicodeDecodeError as e:\n        print(f\"{bm:<12} LỖI: {e.reason}\")\n```\n\nMẹo đọc kết quả: `latin-1` **không bao giờ lỗi** vì nó ánh xạ mọi byte thành một ký tự. Nó chạy được không có nghĩa là đúng — nếu chỉ `latin-1` cho ra chữ đọc được nhưng lạ, thì bảng mã thật là thứ khác.\n\nKhi phải xử lý dữ liệu bẩn, không thể chọn một bảng mã duy nhất:\n\n```python\n# Vẫn thấy được chỗ hỏng, không mất dòng\nwith open(\"ban.txt\", encoding=\"utf-8\", errors=\"replace\") as f:\n    for so, dong in enumerate(f, 1):\n        if \"\\ufffd\" in dong:                # ký tự thay thế \"�\"\n            print(f\"dòng {so} có byte hỏng: {dong[:60]!r}\")\n```\n\nCách này tốt hơn `errors=\"ignore\"` vì nó **để lại dấu vết** — bạn biết chỗ nào cần xem lại thay vì mất dữ liệu âm thầm.\n\n### Xử lý file log lớn\n\n```python\nfrom collections import Counter\nfrom pathlib import Path\n\ndef doc_dong(duong_dan):\n    \"\"\"Sinh từng dòng đã làm sạch, bỏ dòng rỗng.\"\"\"\n    with open(duong_dan, encoding=\"utf-8\", errors=\"replace\") as f:\n        for dong in f:\n            dong = dong.rstrip(\"\\n\")\n            if dong:\n                yield dong\n\ndef thong_ke_loi(duong_dan):\n    dem = Counter()\n    tong = 0\n    for dong in doc_dong(duong_dan):        # ⭐ bộ nhớ không đổi\n        tong += 1\n        if \"ERROR\" in dong:\n            phan = dong.split(\" \", 3)\n            if len(phan) >= 3:\n                dem[phan[2]] += 1\n    return tong, dem\n\ntong, dem = thong_ke_loi(Path(\"app.log\"))\nprint(f\"{tong:,} dòng, {sum(dem.values()):,} lỗi\")\nfor ip, n in dem.most_common(5):\n    print(f\"  {ip:<16} {n:>6}\")\n```\n\nHàm này chạy với file 50 GB và dùng vài megabyte. Ba bài trước gặp nhau ở đây: generator cho tính lười, `Counter` cho đếm, `errors=\"replace\"` cho dữ liệu bẩn.\n\n### JSON: lưu và nạp cấu hình\n\n```python\nimport json\nfrom pathlib import Path\n\nMAC_DINH = {\"host\": \"localhost\", \"port\": 8000, \"debug\": False}\n\ndef nap_cau_hinh(duong_dan=\"cau_hinh.json\"):\n    p = Path(duong_dan)\n    if not p.exists():\n        return dict(MAC_DINH)                  # ⭐ bản sao, không trả chính MAC_DINH\n    try:\n        nguoi_dung = json.loads(p.read_text(encoding=\"utf-8\"))\n    except json.JSONDecodeError as e:\n        raise ValueError(f\"{p} không phải JSON hợp lệ (dòng {e.lineno}): {e.msg}\") from e\n    return {**MAC_DINH, **nguoi_dung}          # người dùng ghi đè mặc định\n\ndef luu_cau_hinh(cf, duong_dan=\"cau_hinh.json\"):\n    Path(duong_dan).write_text(\n        json.dumps(cf, ensure_ascii=False, indent=2, sort_keys=True),\n        encoding=\"utf-8\")\n```\n\nBa chi tiết đáng giải thích:\n\n```\ndict(MAC_DINH)   trả BẢN SAO — nếu trả thẳng MAC_DINH, người gọi sửa nó là\n                 làm bẩn giá trị mặc định toàn cục (bài mutable)\nraise ... from e chuyển JSONDecodeError thành lỗi có ngữ cảnh, giữ nguyên nhân\nsort_keys=True   khoá theo thứ tự cố định ⇒ git diff sạch, so sánh file dễ\n```\n\n### CSV: đọc dữ liệu thật, có dòng hỏng\n\n```python\nimport csv\nfrom pathlib import Path\n\ndef doc_diem(duong_dan):\n    tot, hong = [], []\n    with open(duong_dan, newline=\"\", encoding=\"utf-8-sig\") as f:\n        doc = csv.DictReader(f)\n\n        can = {\"ten\", \"diem\"}\n        thieu = can - set(doc.fieldnames or [])\n        if thieu:\n            raise ValueError(f\"CSV thiếu cột: {sorted(thieu)}\")   # ⭐ kiểm tra SỚM\n\n        for so, hang in enumerate(doc, 2):      # 2 vì dòng 1 là tiêu đề\n            try:\n                tot.append({\"ten\": hang[\"ten\"].strip(),\n                            \"diem\": float(hang[\"diem\"])})\n            except (ValueError, AttributeError) as e:\n                hong.append((so, hang.get(\"diem\"), str(e)))\n\n    if hong:\n        print(f\"⚠️  {len(hong)} dòng hỏng:\")\n        for so, gt, ly_do in hong[:5]:\n            print(f\"   dòng {so}: {gt!r} — {ly_do}\")\n    return tot, hong\n```\n\nBốn kỹ thuật đáng dùng lại:\n\n```\nutf-8-sig            xử lý BOM từ phần mềm bảng tính\nnewline=\"\"           tránh dòng trống trên Windows\nkiểm tra cột SỚM     báo lỗi rõ ràng thay vì KeyError ở dòng 5.000\nenumerate(doc, 2)    số dòng khớp với thứ bạn thấy khi mở file\n```\n\nĐiểm cuối nhỏ nhưng rất được việc: người dùng mở file lên và tìm đúng dòng bạn báo.\n\n### CSV lớn — xử lý theo dòng\n\n```python\nimport csv\n\ndef loc_csv(vao, ra, dieu_kien):\n    \"\"\"Lọc CSV lớn mà không nạp cả file.\"\"\"\n    with open(vao, newline=\"\", encoding=\"utf-8-sig\") as fv, \\\n         open(ra, \"w\", newline=\"\", encoding=\"utf-8\") as fr:\n        doc = csv.DictReader(fv)\n        ghi = csv.DictWriter(fr, fieldnames=doc.fieldnames)\n        ghi.writeheader()\n        n = 0\n        for hang in doc:                  # ⭐ từng dòng\n            if dieu_kien(hang):\n                ghi.writerow(hang)\n                n += 1\n    return n\n\nn = loc_csv(\"lon.csv\", \"loc.csv\", lambda h: float(h.get(\"diem\") or 0) >= 8)\nprint(f\"giữ lại {n:,} dòng\")\n```\n\nMở hai file trong một `with` bằng dấu `\\` xuống dòng là mẫu chuẩn — cả hai đều được đóng đúng, kể cả khi có lỗi giữa chừng.\n\n### Ghi an toàn cho kết quả tính toán dài\n\n```python\nimport json, os, tempfile\nfrom pathlib import Path\n\ndef luu_json_an_toan(duong_dan, du_lieu):\n    p = Path(duong_dan)\n    fd, tam = tempfile.mkstemp(dir=p.parent, suffix=\".tmp\")\n    try:\n        with os.fdopen(fd, \"w\", encoding=\"utf-8\") as f:\n            json.dump(du_lieu, f, ensure_ascii=False, indent=2)\n            f.flush()\n            os.fsync(f.fileno())\n        os.replace(tam, p)              # nguyên tử\n    except BaseException:\n        os.unlink(tam)\n        raise\n\n# Dùng cho checkpoint: mất điện giữa chừng vẫn còn bản trước nguyên vẹn\nluu_json_an_toan(\"ket_qua.json\", {\"epoch\": 12, \"loss\": 0.043})\n```\n\nSo với `json.dump` thẳng vào file đích: cách thẳng sẽ **cắt file cũ về rỗng ngay khi mở**, nên mất điện giữa chừng là mất cả bản cũ lẫn bản mới. Với dữ liệu chạy nhiều giờ mới có, khác biệt này rất đáng.\n\n### `pathlib` cho công việc hằng ngày\n\n```python\nfrom pathlib import Path\n\nGOC = Path(__file__).resolve().parent      # ⭐ không phụ thuộc nơi chạy\nDU_LIEU = GOC / \"du_lieu\"\nKET_QUA = GOC / \"ket_qua\"\nKET_QUA.mkdir(exist_ok=True)\n\n# Xử lý mọi CSV, ghi kết quả sang JSON cùng tên\nfor f in sorted(DU_LIEU.glob(\"*.csv\")):\n    print(f\"xử lý {f.name} ({f.stat().st_size:,} byte)\")\n    hang, _ = doc_diem(f)\n    (KET_QUA / f\"{f.stem}.json\").write_text(\n        json.dumps(hang, ensure_ascii=False, indent=2), encoding=\"utf-8\")\n\n# Dọn file tạm cũ hơn 7 ngày\nimport time\nnguong = time.time() - 7 * 86400\nfor f in GOC.rglob(\"*.tmp\"):\n    if f.stat().st_mtime < nguong:\n        print(f\"xoá {f}\")\n        f.unlink()\n```\n\n`f.stem` cho tên không có phần mở rộng, nên `du_lieu/bang.csv` thành `ket_qua/bang.json` chỉ trong một dòng. Đây là loại việc mà ghép chuỗi làm được nhưng dài và dễ sai.\n\n### So sánh ba định dạng trên cùng dữ liệu\n\n```python\nimport json, csv, io\n\nds = [{\"ten\": \"Minh\", \"diem\": 8.5, \"lop\": \"12A\"},\n      {\"ten\": \"An\",   \"diem\": 7.0, \"lop\": \"12B\"}]\n\n# JSON — giữ kiểu, có cấu trúc lồng\nprint(json.dumps(ds, ensure_ascii=False))\n# [{\"ten\": \"Minh\", \"diem\": 8.5, \"lop\": \"12A\"}, ...]\n\n# CSV — gọn, mở được bằng bảng tính, MẤT kiểu\nbuf = io.StringIO()\nw = csv.DictWriter(buf, fieldnames=[\"ten\", \"diem\", \"lop\"])\nw.writeheader(); w.writerows(ds)\nprint(buf.getvalue())\n# ten,diem,lop\n# Minh,8.5,12A\n\n# JSON Lines — nối thêm được, đọc theo dòng được\nfor d in ds:\n    print(json.dumps(d, ensure_ascii=False))\n```\n\nQuan sát quan trọng: đọc lại CSV thì `8.5` thành chuỗi `\"8.5\"`, còn JSON giữ nguyên `float`. Đó là đánh đổi trung tâm giữa hai định dạng.",

so_sanh: "### Chế độ văn bản và nhị phân\n\n| | Văn bản | Nhị phân |\n|---|---|---|\n| Kiểu | `str` | `bytes` |\n| Giải mã | ✓ theo `encoding` | ✗ |\n| Chuẩn hoá xuống dòng | ✓ | ✗ |\n| Sai bảng mã | mojibake hoặc lỗi | không xảy ra |\n| Dùng cho | text, JSON, CSV | ảnh, mô hình, file nén, hash |\n\nKhi nghi ngờ về bảng mã, **mở ở chế độ nhị phân để xem byte thật** là bước chẩn đoán đầu tiên. Nó cho biết sự thật, không qua tầng phiên dịch nào.\n\n### Bốn cách đọc file\n\n| | Bộ nhớ | Dùng khi |\n|---|---|---|\n| `read()` | **cả file** | file nhỏ, cần toàn bộ nội dung |\n| `readlines()` | **cả file + list** | hiếm khi đáng dùng |\n| `for dong in f` | **một dòng** | **mặc định cho file văn bản** |\n| `read(n)` theo khối | một khối | file nhị phân lớn |\n\nHàng thứ hai gần như luôn có lựa chọn tốt hơn: cần list thì `list(f)` cũng vậy, cần lặp thì dùng hàng ba.\n\n### JSON, CSV, JSON Lines\n\n| | JSON | CSV | JSON Lines |\n|---|---|---|---|\n| Cấu trúc lồng nhau | **✓** | ✗ chỉ bảng phẳng | ✓ |\n| Giữ kiểu dữ liệu | **✓** | ✗ tất cả là chuỗi | ✓ |\n| Đọc theo dòng | ✗ | ✓ | **✓** |\n| Nối thêm không đọc lại | ✗ | ✓ | **✓** |\n| Một dòng hỏng phá cả file | **✓** | ✗ | ✗ |\n| Mở bằng bảng tính | ✗ | **✓** | ✗ |\n| Kích thước | lớn hơn | **nhỏ nhất** | lớn hơn |\n\nCách chọn:\n\n```\nCấu hình, dữ liệu lồng nhau, API      → JSON\nBảng phẳng, cần mở bằng bảng tính     → CSV\nLog, dataset lớn, ghi dần             → JSON Lines\n```\n\n### `split(\",\")` và module `csv`\n\n| | `split(\",\")` | `csv` |\n|---|---|---|\n| Dấu phẩy trong dấu nháy | **✗ vỡ** | ✓ |\n| Dấu nháy trong trường | ✗ | ✓ |\n| Xuống dòng trong ô | ✗ | ✓ |\n| Đọc theo tên cột | ✗ | **✓ `DictReader`** |\n| Ghi đúng quy tắc thoát | ✗ | ✓ |\n\nKhông có trường hợp nào `split(\",\")` đúng hơn module `csv`. Nó chỉ có vẻ đơn giản hơn — cho tới khi dữ liệu thật xuất hiện.\n\n### Các bảng mã hay gặp\n\n| | Dùng khi | Ghi chú |\n|---|---|---|\n| `utf-8` | **mặc định cho mọi thứ** | tiêu chuẩn hiện nay |\n| `utf-8-sig` | **đọc** CSV từ nguồn lạ | tự bỏ BOM nếu có |\n| `latin-1` | chẩn đoán | **không bao giờ lỗi** ⇒ chạy được ≠ đúng |\n| `cp1258` | file cũ tiếng Việt trên Windows | bảng mã cũ |\n\nHàng `latin-1` là bẫy chẩn đoán: người ta thử nó thấy hết lỗi và tưởng đã sửa xong, trong khi dữ liệu đã sai.\n\n### `errors=` khi giải mã\n\n| | Hành vi | Mất dữ liệu | Nên dùng |\n|---|---|---|---|\n| `strict` | ném lỗi | ✗ | **mặc định** |\n| `replace` | thay bằng `�` | không (thấy được) | **dữ liệu bẩn** |\n| `ignore` | bỏ byte hỏng | **✓ im lặng** | hiếm khi |\n| `surrogateescape` | giữ byte thô | ✗ | khi cần ghi lại nguyên vẹn |\n\n`replace` hơn `ignore` ở đúng một điểm nhưng là điểm quyết định: nó **để lại dấu vết** để bạn tìm được chỗ hỏng.\n\n### Ghi thẳng và ghi an toàn\n\n| | `open(f, \"w\")` | Ghi tạm rồi `os.replace` |\n|---|---|---|\n| Số dòng code | ít | nhiều hơn |\n| File cũ khi lỗi giữa chừng | **mất** | **còn nguyên** |\n| Nguyên tử | ✗ | **✓** |\n| Dùng cho | file tạm, không quan trọng | **cấu hình, checkpoint, kết quả dài** |\n\nCột phải đáng dùng cho mọi file mà **mất đi thì tốn công làm lại**. Với dữ liệu chạy nhiều giờ, vài dòng code thêm là rẻ.\n\n### `os.path` và `pathlib`\n\n| | `os.path` | `pathlib` |\n|---|---|---|\n| Ghép đường dẫn | `os.path.join(a, b)` | **`Path(a) / b`** |\n| Kiểu trả về | chuỗi | object `Path` |\n| Nối chuỗi phương thức | ✗ | **✓** |\n| Đọc ghi nhanh | ✗ | **`read_text` / `write_text`** |\n| Duyệt thư mục | `os.walk` | **`glob` / `rglob`** |\n| Khuyến nghị | code cũ | **code mới** |\n\n`pathlib` là lựa chọn mặc định cho code mới. `os.path` vẫn cần khi làm việc với thư viện chỉ nhận chuỗi — khi đó dùng `str(p)`.",

loi_thuong_gap: [
  "**Quên `encoding=\"utf-8\"`.** Chạy trên máy bạn, lỗi trên máy khác. Nguyên nhân số một của lỗi tiếng Việt.",
  "**Quên `newline=\"\"` khi làm việc với module `csv`.** File CSV có dòng trống xen kẽ trên Windows.",
  "**Quên `utf-8-sig` khi đọc CSV xuất từ bảng tính.** BOM dính vào tên cột đầu tiên, gây `KeyError` khó hiểu.",
  "**Tự tách CSV bằng `split(\",\")`.** Vỡ ngay khi có dấu phẩy trong dấu nháy.",
  "**Dùng `f.read()` với file lớn.** Hết bộ nhớ. Lặp trực tiếp qua file.",
  "**Dùng `errors=\"ignore\"` để \"cho hết lỗi\".** Mất dữ liệu im lặng. Dùng `replace` để còn thấy chỗ hỏng.",
  "**Thử `latin-1` thấy hết lỗi rồi tưởng đã đúng.** Nó không bao giờ lỗi, nhưng dữ liệu có thể đã sai.",
  "**Quên `ensure_ascii=False` khi ghi JSON tiếng Việt.** File đầy `\\uXXXX`, không đọc được, `git diff` vô nghĩa.",
  "**Tưởng `tuple` ghi vào JSON rồi đọc lại vẫn là `tuple`.** Nó thành `list` — mất tính hashable, không báo lỗi.",
  "**Ghi thẳng đè lên file quan trọng.** `\"w\"` cắt file về rỗng ngay khi mở; lỗi giữa chừng là mất cả bản cũ.",
  "**Dùng đường dẫn tương đối cho file dữ liệu đi kèm module.** Chạy từ thư mục khác là lỗi. Dùng `Path(__file__).parent`.",
  "**Ghép đường dẫn bằng `+ \"/\" +`.** Sai trên Windows. Dùng `pathlib` hoặc `os.path.join`.",
  "**Dùng `.strip()` thay `.rstrip(\"\\n\")` khi đọc dòng.** Mất khoảng trắng đầu dòng có nghĩa.",
  "**Không kiểm tra cột CSV trước khi lặp.** `KeyError` ở dòng 5.000 thay vì một thông báo rõ ràng ngay đầu.",
  "**Quên rằng mọi giá trị từ CSV là chuỗi.** `hang[\"diem\"] > 5` so sánh chuỗi với số — `TypeError`, hoặc tệ hơn là so sánh chuỗi với chuỗi cho kết quả sai."
],

tu_kiem_tra: [
  "Vì sao phải luôn ghi `encoding=\"utf-8\"`? Chuyện gì xảy ra khi bảng mã sai — hai trường hợp?",
  "Chế độ văn bản khác nhị phân ở ba điểm nào? Khi nghi ngờ mã hoá thì mở ở chế độ nào để chẩn đoán?",
  "Bốn cách đọc file — cái nào là mặc định đúng cho file văn bản lớn và vì sao?",
  "Vì sao `newline=\"\"` là bắt buộc với module `csv`? Không có nó thì hiện tượng gì xảy ra?",
  "Cho `Minh,\"Hà Nội, Việt Nam\",22` — `split(\",\")` cho kết quả gì, và vì sao sai?",
  "`utf-8-sig` khác `utf-8` ở đâu? Khi nào dùng nó để đọc, khi nào dùng để ghi?",
  "Vì sao `errors=\"replace\"` tốt hơn `errors=\"ignore\"`?",
  "Vì sao `latin-1` không bao giờ báo lỗi, và vì sao đó là cái bẫy khi chẩn đoán?",
  "`ensure_ascii=False` làm gì? Không có nó thì file JSON tiếng Việt trông thế nào?",
  "Kiểu Python nào không ghi được vào JSON? Kiểu nào bị đổi âm thầm?",
  "Mô tả mẫu ghi an toàn. `os.fsync` và `os.replace` mỗi cái giải quyết vấn đề gì?",
  "Vì sao dùng `Path(__file__).parent` thay vì đường dẫn tương đối cho file dữ liệu đi kèm module?",
  "JSON Lines hơn JSON thường ở ba điểm nào?",
  "Vì sao `enumerate(doc, 2)` khi đọc `DictReader` thay vì `enumerate(doc)`?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Reading and Writing Files* trong tutorial, và tham khảo của hàm `open()` — bảng đầy đủ các chế độ và tham số.",
  "Tài liệu Python, module `csv` — phần *Dialects and Formatting Parameters* giải thích quy tắc thoát ký tự, và ghi chú về `newline=\"\"`.",
  "Tài liệu Python, module `json` — phần về `JSONEncoder` khi cần ghi kiểu dữ liệu tuỳ chỉnh.",
  "Tài liệu Python, module `pathlib` — bảng đối chiếu với `os.path` ở cuối trang rất tiện khi chuyển code cũ.",
  "Bài viết kinh điển của Joel Spolsky về Unicode (*The Absolute Minimum Every Software Developer Must Know About Unicode*) — nền để hiểu vì sao bảng mã lại quan trọng đến vậy."
],

lien_he: [
  "**Unicode, UTF-8** ở module 1: `str` là ký tự, `bytes` là byte — bài này là ứng dụng trực tiếp và là nơi lý thuyết đó thành lỗi thật.",
  "**Xử lý ngoại lệ** ngay trước: `FileNotFoundError`, `UnicodeDecodeError`, `JSONDecodeError` — và mẫu `raise ... from e` để giữ ngữ cảnh.",
  "**Comprehension và generator**: đọc file theo dòng là ứng dụng chính của tính lười.",
  "**Context manager và `with`** trong module này: `with open(...)` là context manager bạn dùng nhiều nhất.",
  "**Module, package, import** trước đó: `Path(__file__).parent` để tìm file dữ liệu đi kèm module.",
  "**List, dict, set, tuple**: JSON ánh xạ thẳng sang `dict` và `list`; `tuple` và `set` thì không có tương ứng.",
  "**Hệ thống file, quyền truy cập** ở module 2: `PermissionError` và ý nghĩa của các chế độ mở file.",
  "**HTTP** ở module 4: `Content-Type: application/json; charset=utf-8` là cùng vấn đề mã hoá, ở tầng mạng.",
  "**Database** ở mảng 04: nạp CSV vào bảng và xuất kết quả ra CSV là công việc thường xuyên.",
  "**AI/ML** ở mảng 07: nạp dataset, ghi checkpoint, lưu cấu hình thí nghiệm — và ghi an toàn là thứ cứu bạn khi tác vụ chạy nhiều giờ bị ngắt."
]

};
