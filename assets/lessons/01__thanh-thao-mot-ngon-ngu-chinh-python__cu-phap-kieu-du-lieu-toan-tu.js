window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:cu-phap-kieu-du-lieu-toan-tu"] = {

tom_tat: [
  "**Biến trong Python là cái nhãn dán lên object, không phải cái hộp chứa giá trị.** Gán không sao chép dữ liệu — hiểu điều này là tránh được cả một lớp lỗi.",
  "**Python kiểu động nhưng chặt** (*dynamically but strongly typed*): biến đổi kiểu tự do, nhưng `\"3\" + 5` báo lỗi thay vì đoán bừa. Hai tính chất này khác nhau và hay bị gộp.",
  "**Ranh giới mutable / immutable là ranh giới quan trọng nhất trong bài.** Nó quyết định object nào dùng được làm khoá `dict`, làm giá trị mặc định của hàm, và khi nào sửa một biến làm biến khác đổi theo.",
  "**`==` so sánh giá trị, `is` so sánh danh tính.** `is` chỉ dùng đúng với `None`, `True`, `False`. Dùng `is` để so số hay chuỗi là lỗi chực chờ.",
  "**`/` luôn trả `float`, `//` làm tròn xuống — kể cả với số âm.** `-7 // 2` bằng `-4`, không phải `-3`, và điều này khác C."
],

can_biet_truoc: [
  "Bài **Compiled vs interpreted vs bytecode** ở module 1 — Python dịch ra bytecode rồi thông dịch, và mỗi thao tác phải tra kiểu lúc chạy.",
  "Bài **Stack vs heap** ở module 2 — mọi object Python nằm trên heap, biến chỉ là tên trỏ tới object. Bài này đi sâu vào hệ quả của điều đó.",
  "Bài **Số thực dấu phẩy động IEEE 754** ở module 1 — `0.1 + 0.2 != 0.3` không phải lỗi của Python.",
  "Bài **Unicode, UTF-8** ở module 1 — `str` của Python là dãy ký tự Unicode, `bytes` là dãy byte thô."
],

dinh_nghia: "Nói đời thường: trong nhiều ngôn ngữ, biến giống như **một cái hộp có dán tên** — bạn bỏ giá trị vào hộp. Trong Python thì ngược lại: **giá trị nằm sẵn ngoài kia, và biến là tờ giấy nhớ bạn dán lên nó**. Nhiều tờ giấy dán lên cùng một thứ là chuyện thường.\n\nNói chính xác:\n\n- **Object** — mọi thứ trong Python đều là object: số, chuỗi, hàm, class, cả module. Mỗi object có **danh tính** (địa chỉ), **kiểu**, và **giá trị**.\n- **Biến** (*name*) — một tên được **buộc** (*bind*) vào một object. Gán là buộc lại tên vào object khác, **không** ghi lên object cũ.\n- **Kiểu động** (*dynamically typed*) — kiểu gắn với **object**, không gắn với biến. Cùng một biến có thể trỏ tới `int` rồi trỏ tới `str`.\n- **Kiểu chặt** (*strongly typed*) — Python **không tự ép kiểu** giữa những kiểu không liên quan. `\"3\" + 5` là `TypeError`.\n- **Immutable** — object không sửa được sau khi tạo: `int`, `float`, `str`, `tuple`, `frozenset`, `bytes`.\n- **Mutable** — object sửa được tại chỗ: `list`, `dict`, `set`, `bytearray`, và phần lớn object do bạn tự định nghĩa.\n\nMột điểm cần chỉnh ngay nếu bạn từng học C hay Java: câu \"gán biến này cho biến kia\" trong Python **không sao chép gì cả**. Nó chỉ thêm một cái nhãn nữa lên cùng một object. Đây là nguồn của rất nhiều lỗi \"tôi sửa `b` mà `a` cũng đổi\".",

vi_sao_can: "Đây là bài nền của cả module, và cũng là nơi các lỗi khó hiểu nhất của người mới bắt nguồn:\n\n- **Lỗi mutable là loại lỗi im lặng.** Sửa một `list` làm `list` khác đổi theo, hoặc một giá trị mặc định của hàm \"nhớ\" dữ liệu của lần gọi trước. Chương trình không báo lỗi, chỉ cho kết quả sai.\n- **Đọc hiểu thông báo lỗi.** `TypeError: can only concatenate str (not \"int\") to str` là hệ quả trực tiếp của kiểu chặt. Biết vì sao thì sửa trong ba giây.\n- **Nền cho mọi bài sau.** `list`/`dict`/`set`, comprehension, tham số mặc định, decorator — không bài nào hiểu được nếu chưa nắm \"biến là nhãn\".\n- **Định hướng AI/ML.** NumPy và pandas làm việc trên dữ liệu mutable rất lớn, và câu hỏi \"đây là bản sao hay là view\" là câu hỏi bạn sẽ gặp mỗi ngày. Nó chính là bài này ở quy mô lớn hơn.\n- **Chuẩn bị cho phỏng vấn thật.** Câu hỏi về `is` vs `==`, về giá trị mặc định mutable, về `0.1+0.2` là những câu gần như luôn xuất hiện.\n\nTình huống thật rất kinh điển: bạn viết một hàm nhận `list` và thêm phần tử vào đó, rồi thắc mắc vì sao `list` của người gọi cũng bị đổi. Hoặc bạn viết `def them(x, ds=[])` và phát hiện lần gọi thứ hai đã có sẵn dữ liệu của lần gọi thứ nhất. Cả hai đều **không phải lỗi của Python** — chúng là hệ quả logic của mô hình \"biến là nhãn\", và phần cơ chế sẽ chỉ rõ tại sao.",

co_che: "### Biến là nhãn, không phải hộp\n\n```python\na = [1, 2, 3]\nb = a                  # KHÔNG sao chép — chỉ thêm một nhãn nữa\nb.append(4)\n\nprint(a)               # [1, 2, 3, 4]  ← a cũng đổi!\nprint(a is b)          # True — CÙNG một object\nprint(id(a) == id(b))  # True — cùng địa chỉ\n```\n\nHình dung đúng:\n\n```\n     a ──┐\n         ├──► [1, 2, 3, 4]      một object duy nhất trên heap\n     b ──┘\n```\n\nBây giờ so sánh với gán lại:\n\n```python\na = [1, 2, 3]\nb = a\nb = [9, 9]             # GÁN LẠI: b bỏ nhãn khỏi object cũ, dán lên object mới\n\nprint(a)               # [1, 2, 3]  ← a KHÔNG đổi\n```\n\nQuy tắc phân biệt hai trường hợp trên, và đây là quy tắc đáng nhớ nhất của bài:\n\n```\nb.append(4)   → SỬA object mà b đang trỏ tới   ⇒ mọi nhãn khác thấy thay đổi\nb = [9, 9]    → BUỘC LẠI nhãn b sang object khác ⇒ nhãn khác không ảnh hưởng\n```\n\nMuốn có bản sao thật thì phải nói rõ:\n\n```python\nimport copy\n\nb = a.copy()             # sao chép NÔNG (shallow) — một tầng\nb = list(a)              # cũng nông\nb = a[:]                 # cũng nông\nb = copy.deepcopy(a)     # sao chép SÂU — mọi tầng lồng nhau\n```\n\nSao chép nông đủ cho `list` phẳng, nhưng có bẫy khi lồng nhau:\n\n```python\na = [[1, 2], [3, 4]]\nb = a.copy()\nb[0].append(99)\n\nprint(a)     # [[1, 2, 99], [3, 4]]  ← a vẫn đổi!\n```\n\nLý do: `copy()` sao chép **danh sách ngoài**, nhưng các `list` con vẫn là **cùng object**. Đúng logic \"biến là nhãn\" áp cho từng phần tử. Cần `deepcopy` để tách hẳn — đổi lại nó chậm hơn nhiều vì phải đi hết cây dữ liệu.\n\n### Kiểu động nhưng chặt\n\nHai tính chất tách biệt, và gộp chúng lại là hiểu sai:\n\n```python\n# ĐỘNG: kiểu gắn với object, không gắn với biến\nx = 42          # x trỏ tới một int\nx = \"xin chao\"  # giờ x trỏ tới một str — hoàn toàn hợp lệ\nx = [1, 2]      # và giờ là list\n\n# CHẶT: không tự ép kiểu giữa các kiểu không liên quan\n\"3\" + 5         # TypeError: can only concatenate str (not \"int\") to str\n```\n\nSo với JavaScript — vốn động **và lỏng** — thì sự khác biệt rất rõ:\n\n```\nJavaScript:  \"3\" + 5  →  \"35\"    (tự đổi số thành chuỗi)\nJavaScript:  \"3\" - 5  →  -2      (tự đổi chuỗi thành số!)\nPython:      \"3\" + 5  →  TypeError\n```\n\nBáo lỗi ngay là **ưu điểm**, không phải bất tiện: JavaScript sẽ chạy tiếp với `\"35\"` và bạn phát hiện ra sai ở một chỗ hoàn toàn khác, có khi sau nhiều giờ. Python dừng đúng tại dòng gây lỗi.\n\nMuốn đổi kiểu thì nói rõ:\n\n```python\nint(\"3\") + 5         # 8\n\"3\" + str(5)         # \"35\"\nint(\"3.7\")           # ValueError! int() không đọc được chuỗi có dấu chấm\nint(float(\"3.7\"))    # 3 — phải qua float trước\n```\n\nDòng thứ ba là cạm bẫy hay gặp khi đọc dữ liệu từ file CSV: cột số thực đọc lên là chuỗi `\"3.7\"`, và `int()` trực tiếp thì lỗi.\n\n### Mutable và immutable\n\nĐây là ranh giới có nhiều hệ quả nhất:\n\n| Immutable | Mutable |\n|---|---|\n| `int`, `float`, `bool`, `complex` | `list` |\n| `str` | `dict` |\n| `tuple` | `set` |\n| `frozenset` | `bytearray` |\n| `bytes` | object tự định nghĩa (thường) |\n\nBa hệ quả thực tế:\n\n**① Chỉ immutable làm được khoá `dict` và phần tử `set`.**\n\n```python\nd = {(1, 2): \"toa do\"}        # ✓ tuple immutable\nd = {[1, 2]: \"toa do\"}        # TypeError: unhashable type: 'list'\n```\n\nLý do sâu xa: `dict` tìm khoá bằng **giá trị băm** (*hash*). Nếu khoá sửa được thì hash đổi sau khi đã cất, và không bao giờ tìm lại được. Python chặn từ đầu bằng cách chỉ cho phép object immutable làm khoá.\n\n**② Sửa immutable luôn tạo object mới.**\n\n```python\ns = \"xin\"\nprint(id(s))\ns += \" chao\"           # KHÔNG sửa chuỗi cũ — tạo chuỗi MỚI\nprint(id(s))           # id khác\n```\n\nĐây là lý do nối chuỗi trong vòng lặp là phản mẫu:\n\n```python\n# ❌ mỗi lần lặp tạo một chuỗi mới, sao chép lại toàn bộ ⇒ O(n²)\nkq = \"\"\nfor t in danh_sach:\n    kq += t\n\n# ✓ join sao chép đúng một lần ⇒ O(n)\nkq = \"\".join(danh_sach)\n```\n\nVới 100.000 phần tử, khác biệt là vài chục lần. Đây là một trong ít tối ưu Python mà đáng làm theo phản xạ.\n\n**③ `tuple` immutable, nhưng chỉ ở một tầng.**\n\n```python\nt = (1, 2, [3, 4])\nt[0] = 99            # TypeError ✓ đúng như mong đợi\nt[2].append(5)       # ✓ CHẠY ĐƯỢC! list bên trong vẫn mutable\nprint(t)             # (1, 2, [3, 4, 5])\n```\n\n`tuple` đảm bảo **các nhãn bên trong nó không đổi**, chứ không đảm bảo các object mà nhãn đó trỏ tới không đổi. Hệ quả: một `tuple` chứa `list` **không** dùng được làm khoá `dict`, vì nó không thật sự bất biến.\n\n### `is` và `==`\n\n```python\na = [1, 2, 3]\nb = [1, 2, 3]\n\na == b        # True  — GIÁ TRỊ bằng nhau\na is b        # False — hai object KHÁC nhau\n```\n\n```\n==  gọi phương thức __eq__ ⇒ so sánh nội dung\nis  so sánh id() ⇒ có phải CÙNG một object trong bộ nhớ\n```\n\nCạm bẫy khiến rất nhiều người hiểu sai:\n\n```python\na = 256\nb = 256\na is b        # True\n\na = 257\nb = 257\na is b        # False (!) trong nhiều trường hợp\n\na = \"xin\"\nb = \"xin\"\na is b        # True\n\na = \"xin chao ban\"\nb = \"xin chao ban\"\na is b        # có thể False\n```\n\nGiải thích: CPython **cache sẵn** các số nguyên nhỏ và một số chuỗi giống định danh, để tiết kiệm bộ nhớ. Đây là **chi tiết cài đặt**, không phải phần của ngôn ngữ — nó khác nhau giữa các phiên bản và các bản hiện thực Python.\n\nKết luận thực dụng, không cần nhớ chi tiết cache:\n\n```\nDùng is CHỈ với: None, True, False\n     (và khi bạn thật sự muốn hỏi \"có phải cùng một object không\")\nDùng == cho mọi so sánh giá trị khác\n```\n\n```python\nif x is None:      # ✓ đúng cách kiểm tra None\nif x == None:      # chạy được nhưng không đúng phong cách\nif not x:          # ⚠️ KHÁC HẲN: 0, \"\", [] cũng vào nhánh này\n```\n\nDòng cuối là một lỗi logic thật, không phải chuyện phong cách: nếu `x = 0` là giá trị hợp lệ thì `if not x` sẽ xử lý nó như \"không có giá trị\".\n\n### Số\n\n```python\n2 ** 1000            # int Python KHÔNG giới hạn — số nguyên lớn tuỳ ý\n\n7 / 2                # 3.5   — LUÔN trả float, kể cả 4/2 = 2.0\n7 // 2               # 3     — chia lấy nguyên, LÀM TRÒN XUỐNG\n7 % 2                # 1\n-7 // 2              # -4    (!) không phải -3\n-7 % 2               # 1     (!) không phải -1\n```\n\nHai dòng cuối khác C và khác nhiều ngôn ngữ khác, nên đáng giải thích: Python **làm tròn xuống** (về phía âm vô cùng), còn C **làm tròn về 0**. Python chọn vậy để giữ một tính chất nhất quán: dấu của `%` luôn theo dấu của số chia. Nhờ đó `-1 % 12 == 11`, rất tiện cho tính toán vòng tròn như chỉ số mảng hay giờ trên đồng hồ.\n\n`float` thì tuân theo IEEE 754 như mọi ngôn ngữ khác — nối về bài module 1:\n\n```python\n0.1 + 0.2 == 0.3          # False\nprint(0.1 + 0.2)          # 0.30000000000000004\n\n# So sánh float thì dùng dung sai\nimport math\nmath.isclose(0.1 + 0.2, 0.3)     # True\n\n# Tiền thì KHÔNG dùng float\nfrom decimal import Decimal\nDecimal(\"0.1\") + Decimal(\"0.2\") == Decimal(\"0.3\")    # True\n```\n\nQuy tắc dứt khoát: **tiền và mọi thứ cần chính xác thập phân thì dùng `Decimal` hoặc dùng số nguyên đơn vị nhỏ nhất** (lưu xu thay vì lưu đồng). Sai lệch của `float` tích luỹ qua hàng nghìn giao dịch là sai lệch thật.\n\n### Chuỗi\n\n```python\ns = \"xin chào\"\nlen(s)               # 8 — đếm KÝ TỰ, không đếm byte\nlen(s.encode())      # 10 — byte, vì \"à\" chiếm 2 byte trong UTF-8\n```\n\nĐây là nối trực tiếp với bài Unicode: `str` là dãy **ký tự**, `bytes` là dãy **byte**, và chuyển đổi phải nói rõ bảng mã. Trộn hai kiểu là `TypeError` — kiểu chặt lại cứu bạn.\n\nf-string là cách định dạng nên dùng:\n\n```python\nten, diem = \"Minh\", 8.567\n\nf\"{ten} được {diem:.2f} điểm\"      # 'Minh được 8.57 điểm'\nf\"{diem:>10.2f}\"                    # căn phải, rộng 10\nf\"{1234567:,}\"                      # '1,234,567' — dấu phân cách nghìn\nf\"{0.856:.1%}\"                      # '85.6%'\nf\"{ten=}\"                           # \"ten='Minh'\" — rất tiện khi gỡ lỗi\n```\n\nDòng cuối (`f\"{ten=}\"`) đáng nhớ: nó in ra cả tên biến và giá trị, thay thế được phần lớn nhu cầu `print(\"ten =\", ten)` khi gỡ lỗi nhanh.\n\n### Truthiness — cái gì là `False`\n\n```python\n# Được coi là False:\nFalse, None, 0, 0.0, \"\", [], {}, set(), ()\n\n# MỌI thứ khác là True — kể cả:\n\"0\"        # chuỗi không rỗng ⇒ True\n\"False\"    # ⇒ True\n[0]        # list có một phần tử ⇒ True\n-1         # ⇒ True\n```\n\nHai dòng đầu là bẫy khi đọc dữ liệu từ file hoặc từ biến môi trường:\n\n```python\nimport os\nif os.environ.get(\"DEBUG\"):          # DEBUG=\"0\" vẫn vào đây! chuỗi \"0\" là True\n    ...\n\n# ✓ so sánh tường minh\nif os.environ.get(\"DEBUG\") == \"1\":\n    ...\n```\n\nĐây là loại lỗi rất khó thấy khi đọc code, vì dòng `if os.environ.get(\"DEBUG\")` trông hoàn toàn hợp lý.\n\n### Toán tử đáng chú ý\n\n```python\n# Gán nhiều, và đổi chỗ không cần biến tạm\na, b = 1, 2\na, b = b, a\n\n# Giải nén với phần còn lại\ndau, *giua, cuoi = [1, 2, 3, 4, 5]     # dau=1, giua=[2,3,4], cuoi=5\n\n# So sánh chuỗi — chỉ Python có cú pháp này gọn thế\nif 0 <= diem <= 10:                    # thay cho diem >= 0 and diem <= 10\n    ...\n\n# Toán tử walrus: gán NGAY TRONG biểu thức\nif (n := len(danh_sach)) > 10:\n    print(f\"quá dài: {n} phần tử\")     # dùng lại n, không phải gọi len() hai lần\n\n# and / or trả về GIÁ TRỊ, không trả về bool\n\"a\" or \"b\"          # 'a'   — trả về toán hạng đầu tiên đúng\nNone or \"mac dinh\"  # 'mac dinh'\n0 and 5             # 0     — trả về toán hạng đầu tiên sai\n```\n\nHai dòng cuối là cơ chế đằng sau thành ngữ `x = tuy_chon or gia_tri_mac_dinh`. Nhưng nó có bẫy đúng như phần truthiness: nếu `tuy_chon = 0` là giá trị hợp lệ thì nó bị coi là sai và bị thay bằng mặc định. Trường hợp đó phải viết rõ:\n\n```python\nx = tuy_chon if tuy_chon is not None else gia_tri_mac_dinh\n```\n\n### Khối lệnh bằng thụt lề\n\nPython dùng thụt lề thay cho dấu ngoặc nhọn. Đây là quyết định thiết kế có đánh đổi rõ ràng:\n\n```\nLợi:  code buộc phải trình bày đúng cấu trúc; không có tranh cãi về vị trí ngoặc;\n      không thể có tình huống \"thụt lề nói một chuyện, ngoặc nói chuyện khác\"\nGiá:  dán code từ nơi khác dễ hỏng; TRỘN tab với space là lỗi rất khó thấy\n```\n\n```python\n# Luôn dùng 4 dấu cách, KHÔNG dùng tab. Chuẩn PEP 8 nói vậy,\n# và trộn hai loại cho lỗi TabError hoặc — tệ hơn — cho logic sai mà không báo lỗi.\ndef f():\n    if True:\n        return 1     # 8 dấu cách\n```\n\nCấu hình biên tập viên đổi tab thành 4 space là việc nên làm một lần rồi quên. Bài về PEP 8 ở cuối module sẽ nói về công cụ tự động hoá việc này.",

vi_du: "### Chứng minh \"biến là nhãn\" bằng `id()`\n\n```python\na = [1, 2, 3]\nb = a\nprint(f\"{id(a)=} {id(b)=} {a is b=}\")     # cùng id, is → True\n\nb.append(4)\nprint(f\"{a=}\")                             # a=[1, 2, 3, 4]\n\nb = [9]                                    # buộc lại nhãn\nprint(f\"{a=} {b=} {a is b=}\")              # a=[1,2,3,4] b=[9] False\n```\n\nCú pháp `f\"{a=}\"` in cả tên và giá trị — dùng nó khi khảo sát hành vi thay vì viết `print(\"a =\", a)`.\n\n### Lỗi hàm sửa `list` của người gọi\n\n```python\n# ❌ Hàm sửa list của người gọi — \"tác dụng phụ\" ngoài mong đợi\ndef them_mac_dinh(ds):\n    ds.append(\"mac dinh\")      # SỬA object mà người gọi cũng đang trỏ tới\n    return ds\n\ngoc = [\"a\", \"b\"]\nmoi = them_mac_dinh(goc)\nprint(goc)      # ['a', 'b', 'mac dinh']  ← goc BỊ ĐỔI\n\n# ✓ Không sửa đầu vào — trả về cái mới\ndef them_mac_dinh(ds):\n    return ds + [\"mac dinh\"]   # tạo list MỚI\n\ngoc = [\"a\", \"b\"]\nmoi = them_mac_dinh(goc)\nprint(goc, moi)  # ['a', 'b'] ['a', 'b', 'mac dinh']\n```\n\nNguyên tắc thiết kế rút ra: **hàm nên nói rõ nó sửa đầu vào hay trả về cái mới**, và không nên làm cả hai. Thư viện chuẩn của Python theo đúng quy ước này — `ds.sort()` sửa tại chỗ và trả `None`; `sorted(ds)` trả list mới và không sửa gì. Việc `sort()` trả `None` là **có chủ đích**: nó ngăn bạn viết `x = ds.sort()` và tưởng mình có bản đã sắp xếp.\n\n### Cạm bẫy sao chép nông với dữ liệu lồng nhau\n\n```python\nimport copy\n\ngoc = {\"ten\": \"Minh\", \"diem\": [8, 9]}\n\nnong = goc.copy()\nnong[\"diem\"].append(10)\nprint(goc)        # {'ten': 'Minh', 'diem': [8, 9, 10]}  ← vẫn đổi\n\nsau = copy.deepcopy(goc)\nsau[\"diem\"].append(99)\nprint(goc)        # {'ten': 'Minh', 'diem': [8, 9, 10]}  ← không đổi ✓\n```\n\nKhi nào dùng cái nào:\n\n```\ncopy()      dữ liệu PHẲNG (chỉ chứa immutable) — nhanh, đủ dùng\ndeepcopy()  dữ liệu LỒNG NHAU và bạn cần tách hẳn — chậm hơn nhiều\nkhông copy  khi bạn KHÔNG sửa gì — rẻ nhất, và thường là lựa chọn đúng\n```\n\nLựa chọn thứ ba đáng nhấn: rất nhiều lời gọi `deepcopy` trong code thật là không cần thiết, và chúng làm chậm chương trình đáng kể với dữ liệu lớn. Chỉ sao chép khi thật sự sẽ sửa.\n\n### Bẫy `is` với số và chuỗi\n\n```python\n# Cùng một biểu thức, kết quả khác nhau — đây là lý do đừng dùng is\nfor n in (5, 256, 257, 1000):\n    a = n\n    b = n\n    print(f\"{n:>5}: a is b = {a is b}\")\n\n# Nhưng viết trực tiếp trong cùng một dòng lệnh thì lại khác,\n# vì trình biên dịch gộp hằng số giống nhau lại\nprint(257 is 257)          # thường True, và có cảnh báo SyntaxWarning\n```\n\nBài học không phải là học thuộc ngưỡng cache mà là: **kết quả của `is` với số và chuỗi phụ thuộc chi tiết cài đặt, nên đừng bao giờ dựa vào nó.** Chỉ dùng `is` với `None`, `True`, `False`.\n\n### Chia số nguyên và số âm\n\n```python\nfor a, b in [(7, 2), (-7, 2), (7, -2), (-7, -2)]:\n    print(f\"{a:>3} // {b:>2} = {a//b:>3}   {a:>3} % {b:>2} = {a%b:>3}\")\n\n#   7 //  2 =   3     7 %  2 =   1\n#  -7 //  2 =  -4    -7 %  2 =   1\n#   7 // -2 =  -4     7 % -2 =  -1\n#  -7 // -2 =   3    -7 % -2 =  -1\n```\n\nĐọc bảng này thấy quy luật: **dấu của `%` luôn theo dấu của số chia**, và `//` làm tròn xuống. Tính chất đó có ứng dụng trực tiếp:\n\n```python\n# Truy cập vòng tròn — không cần xử lý số âm riêng\ngio = [\"12h\", \"1h\", \"2h\"]\nfor lech in (-1, 0, 1, 5):\n    print(gio[lech % len(gio)])      # luôn ra chỉ số hợp lệ\n```\n\nTrong C, `-1 % 3` cho `-1` và đoạn code trên sẽ lỗi. Ở Python nó cho `2` và chạy đúng — một trong ít chỗ mà Python thuận tiện hơn hẳn.\n\n### Định dạng số cho báo cáo\n\n```python\ndu_lieu = [(\"CPU\", 0.8567), (\"RAM\", 0.234), (\"Đĩa\", 0.9812)]\n\nfor ten, ti_le in du_lieu:\n    print(f\"{ten:<6} {ti_le:>7.1%} {'█' * int(ti_le * 20)}\")\n\n# CPU      85.7% █████████████████\n# RAM      23.4% ████\n# Đĩa      98.1% ███████████████████\n```\n\nBa mã định dạng dùng nhiều nhất: `<` căn trái, `>` căn phải, `.1%` phần trăm một chữ số thập phân. Nắm ba cái này là in được gần như mọi bảng mà không cần thư viện.\n\n### Kiểm tra kiểu đúng cách\n\n```python\nx = 5\n\ntype(x) == int              # chạy được nhưng KHÔNG nhận lớp con\nisinstance(x, int)          # ✓ nên dùng — nhận cả lớp con\nisinstance(x, (int, float)) # ✓ kiểm nhiều kiểu cùng lúc\n\n# ⚠️ bool LÀ lớp con của int trong Python\nisinstance(True, int)       # True (!)\nTrue + True                 # 2\nsum([True, False, True])    # 2 — đếm số True, thành ngữ hay dùng\n```\n\nHai dòng cuối vừa là bẫy vừa là công cụ: `sum()` trên danh sách `bool` là cách đếm số điều kiện đúng rất gọn, dùng nhiều trong xử lý dữ liệu. Nhưng nếu bạn viết hàm nhận `int` và có người truyền `True` vào, `isinstance` sẽ không chặn.\n\n### Chuỗi và byte — nơi kiểu chặt cứu bạn\n\n```python\ns = \"xin chào\"\nb = s.encode(\"utf-8\")          # str → bytes, PHẢI nói bảng mã\nprint(b)                        # b'xin ch\\xc3\\xa0o'\nprint(len(s), len(b))           # 8 10\n\nb.decode(\"utf-8\")               # bytes → str\n\ns + b                           # TypeError ✓ Python chặn việc trộn hai kiểu\n```\n\nSo với Python 2 — nơi `str` và `bytes` lẫn nhau được — thì việc tách rõ này loại bỏ hẳn một lớp lỗi mã hoá. Đây là lý do chính khiến việc chuyển sang Python 3 dù đau nhưng đáng.\n\n### Bộ ba lỗi truthiness hay gặp trong code thật\n\n```python\n# ① Biến môi trường\nif os.environ.get(\"DEBUG\"):              # \"0\" cũng là True!\nif os.environ.get(\"DEBUG\") == \"1\":       # ✓\n\n# ② Giá trị 0 hợp lệ\ndef ap_dung_giam_gia(gia, giam=None):\n    giam = giam or 0.1                   # ❌ giam=0 bị thay bằng 0.1\n    giam = 0.1 if giam is None else giam # ✓\n    return gia * (1 - giam)\n\n# ③ list rỗng và None là hai chuyện khác nhau\ndef xu_ly(ds=None):\n    if not ds:                           # ❌ không phân biệt None với []\n        ...\n    if ds is None:                        # ✓ \"không truyền gì\"\n        ...\n    elif len(ds) == 0:                    # ✓ \"truyền list rỗng\"\n        ...\n```\n\nCả ba đều là code **chạy được, không báo lỗi, và sai**. Đó là lý do phần truthiness đáng đọc kỹ hơn cảm giác ban đầu — nó không phải chi tiết cú pháp mà là nguồn lỗi logic.",

so_sanh: "### Mutable và immutable\n\n| | Immutable | Mutable |\n|---|---|---|\n| Sửa được tại chỗ | ✗ | ✓ |\n| Làm khoá `dict` / phần tử `set` | **✓** | **✗** |\n| Làm giá trị mặc định của hàm | ✓ an toàn | **✗ nguy hiểm** |\n| Gán cho biến khác rồi sửa | không ảnh hưởng gì | **biến kia thấy thay đổi** |\n| Ví dụ | `int`, `str`, `tuple` | `list`, `dict`, `set` |\n\nHàng \"làm khoá `dict`\" có lý do sâu xa đáng nhớ: `dict` định vị khoá bằng hash, nên khoá **phải** không đổi suốt thời gian nằm trong `dict`. Cho phép khoá mutable là mở đường cho dữ liệu biến mất khỏi `dict` mà không ai xoá nó.\n\nHàng \"làm giá trị mặc định\" là chủ đề của bài **Hàm, tham số mặc định, `*args`, `**kwargs`** — ở đó sẽ thấy vì sao `def f(ds=[])` là một trong những bẫy nổi tiếng nhất của Python.\n\n### `is` và `==`\n\n| | `==` | `is` |\n|---|---|---|\n| So sánh | **giá trị** | **danh tính** (`id`) |\n| Gọi phương thức nào | `__eq__` | không gọi gì, so địa chỉ |\n| Ghi đè được | ✓ | ✗ |\n| Tốc độ | tuỳ `__eq__` | rất nhanh |\n| Dùng cho | mọi so sánh giá trị | **chỉ** `None`, `True`, `False` |\n\nCột `is` \"không ghi đè được\" là ưu điểm trong đúng một trường hợp: kiểm tra `None`. Vì `is` không gọi code nào của object, nó **không thể bị một class tự định nghĩa làm cho sai lệch**. Với `==`, một class có `__eq__` viết cẩu thả có thể trả `True` khi so với `None`.\n\n### `/`, `//`, `%` — Python và C\n\n| Biểu thức | Python | C |\n|---|---|---|\n| `7 / 2` | `3.5` (**float**) | `3` (int) |\n| `-7 / 2` | `-3.5` | `-3` |\n| `-7 // 2` | **`-4`** | — |\n| `-7 % 2` | **`1`** | `-1` |\n| Số nguyên lớn | **không giới hạn** | tràn số |\n\nHai hàng giữa là khác biệt hành vi thật, không phải chi tiết nhỏ: chuyển thuật toán từ C sang Python mà không để ý sẽ cho kết quả sai với số âm. Python chọn cách của mình để `%` luôn cho kết quả không âm khi số chia dương — tiện cho tính vòng tròn.\n\nHàng cuối là ưu điểm lớn của Python trong tính toán: `2 ** 1000` chạy đúng, không tràn. Đổi lại là chậm hơn, vì `int` của Python là object trên heap chứ không phải một thanh ghi.\n\n### Kiểu động/tĩnh và chặt/lỏng — bốn ô\n\n| | Kiểu chặt | Kiểu lỏng |\n|---|---|---|\n| **Động** | **Python**, Ruby | JavaScript, PHP |\n| **Tĩnh** | Java, C#, Rust, Go | C (ép kiểu ngầm nhiều) |\n\nHai trục này độc lập, và gộp chúng lại là ngộ nhận phổ biến:\n\n```\nĐộng / tĩnh  →  kiểu được kiểm tra LÚC CHẠY hay LÚC BIÊN DỊCH\nChặt / lỏng  →  có TỰ ĐỘNG ép kiểu giữa các kiểu không liên quan hay không\n```\n\nPython ở ô \"động + chặt\": bạn được tự do đổi kiểu của biến, nhưng không được phép cộng chuỗi với số. Ô này đánh đổi *linh hoạt khi viết* lấy *phát hiện lỗi muộn hơn*. Bài **Type hint và mypy** ở cuối module cho cách lấy lại một phần lợi ích của kiểu tĩnh mà không mất tính động.\n\n### `sort()` và `sorted()` — quy ước đáng học\n\n| | `ds.sort()` | `sorted(ds)` |\n|---|---|---|\n| Sửa `ds` | **✓ tại chỗ** | ✗ |\n| Trả về | **`None`** | list mới |\n| Bộ nhớ | không thêm | thêm một bản |\n| Dùng được với | chỉ `list` | mọi iterable |\n\nViệc `sort()` trả `None` là **cố ý**: nó khiến `x = ds.sort()` cho `x = None` ngay lập tức, thay vì âm thầm cho bạn tưởng đã có bản sắp xếp. Đây là ví dụ tốt về quy ước \"hàm sửa tại chỗ thì không trả về giá trị\" — và là quy ước bạn nên theo trong code của mình.",

loi_thuong_gap: [
  "**Tưởng `b = a` sao chép dữ liệu.** Nó chỉ thêm một nhãn. Sửa `b` là `a` cũng thấy thay đổi nếu object mutable.",
  "**Dùng `copy()` cho dữ liệu lồng nhau.** Sao chép nông chỉ tách tầng ngoài; các object con vẫn dùng chung. Cần `deepcopy`.",
  "**Gọi `deepcopy` khi không cần.** Rất chậm với dữ liệu lớn. Chỉ sao chép khi thật sự sẽ sửa.",
  "**Dùng `is` để so sánh số hoặc chuỗi.** Kết quả phụ thuộc cache của bản hiện thực. `is` chỉ dùng với `None`, `True`, `False`.",
  "**Dùng `if not x` để kiểm tra `None`.** `0`, `\"\"`, `[]` cũng vào nhánh đó. Viết `if x is None`.",
  "**Tin vào truthiness của chuỗi từ môi trường hoặc file.** `\"0\"` và `\"False\"` đều là `True`. So sánh tường minh.",
  "**Dùng `x = tuy_chon or mac_dinh` khi `0` là giá trị hợp lệ.** `0` bị coi là sai và bị thay. Dùng `if ... is None`.",
  "**Nối chuỗi bằng `+=` trong vòng lặp.** Mỗi lần tạo chuỗi mới ⇒ O(n²). Dùng `\"\".join(...)`.",
  "**Dùng `float` cho tiền.** Sai lệch IEEE 754 tích luỹ. Dùng `Decimal` hoặc số nguyên đơn vị nhỏ nhất.",
  "**So sánh `float` bằng `==`.** Dùng `math.isclose()`.",
  "**Tưởng `tuple` bất biến hoàn toàn.** `list` bên trong `tuple` vẫn sửa được, và khi đó `tuple` không dùng được làm khoá `dict`.",
  "**Chuyển thuật toán từ C mà không kiểm tra `//` và `%` với số âm.** `-7 // 2` là `-4` ở Python, khác C.",
  "**Gọi `int(\"3.7\")`.** `ValueError`. Phải `int(float(\"3.7\"))` — hay gặp khi đọc CSV.",
  "**Viết `x = ds.sort()`.** `sort()` trả `None`. Dùng `sorted(ds)` nếu cần giá trị trả về.",
  "**Trộn tab và space khi thụt lề.** Cấu hình biên tập viên dùng 4 dấu cách, một lần rồi quên."
],

tu_kiem_tra: [
  "Vì sao nói \"biến trong Python là nhãn, không phải hộp\"? Nêu một lỗi mà cách hiểu sai gây ra.",
  "Phân biệt kiểu động với kiểu chặt. Python thuộc ô nào trong bốn ô, và điều đó có nghĩa gì cụ thể?",
  "Vì sao `list` không làm được khoá `dict` mà `tuple` thì được? Trường hợp nào `tuple` cũng không được?",
  "`a = [1,2]; b = a; b.append(3)` và `a = [1,2]; b = a; b = [3]` — kết quả của `a` khác nhau thế nào, và vì sao?",
  "`copy()` và `deepcopy()` khác nhau ở đâu? Khi nào không cần sao chép gì cả?",
  "Vì sao `256 is 256` cho `True` mà `257 is 257` có thể cho `False`? Bài học rút ra là gì?",
  "`is` chỉ nên dùng với những giá trị nào, và vì sao nó là cách đúng để kiểm tra `None`?",
  "`-7 // 2` bằng bao nhiêu ở Python, khác C thế nào, và Python được lợi gì từ lựa chọn đó?",
  "Vì sao nối chuỗi bằng `+=` trong vòng lặp là O(n²)?",
  "Liệt kê những giá trị được coi là `False`. Vì sao `if os.environ.get(\"DEBUG\")` là code nguy hiểm?",
  "`and` và `or` trả về gì? Thành ngữ `x = a or b` có bẫy gì?",
  "Vì sao `ds.sort()` trả về `None` thay vì trả về list đã sắp xếp?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *The Python Tutorial* và *Built-in Types* — nguồn tra cứu chuẩn mực nhất về hành vi của từng kiểu.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về mô hình dữ liệu và về danh tính/giá trị object là giải thích đầy đủ nhất về nội dung bài này.",
  "Sách **Python Crash Course** — Eric Matthes. Dành cho người mới, nhiều bài tập thực hành cú pháp cơ bản.",
  "Sách **Effective Python** — Brett Slatkin. Mỗi mục là một lời khuyên ngắn kèm lý do; các mục về truthiness và về sao chép rất sát bài này.",
  "Lệnh `help()` và `dir()` trong REPL của Python — công cụ tra cứu nhanh nhất, dùng được ngay khi không có mạng."
],

lien_he: [
  "**Stack vs heap** ở module 2: mọi object Python nằm trên heap, và \"biến là nhãn\" là hệ quả trực tiếp của điều đó.",
  "**Compiled vs interpreted vs bytecode** ở module 1: mỗi thao tác phải tra kiểu lúc chạy — đó là cái giá của kiểu động.",
  "**Số thực dấu phẩy động IEEE 754** ở module 1: `0.1 + 0.2 != 0.3` là chuyện của phần cứng, không phải của Python.",
  "**Unicode, UTF-8** ở module 1: `str` là ký tự, `bytes` là byte, và kiểu chặt ngăn bạn trộn chúng.",
  "**List, dict, set, tuple** là bài ngay sau: chọn cấu trúc nào dựa trực tiếp trên ranh giới mutable/immutable của bài này.",
  "**Hàm, tham số mặc định** trong module này: giá trị mặc định mutable là bẫy nổi tiếng nhất, và nó là bài này áp dụng vào hàm.",
  "**Type hint và mypy** ở cuối module: cách lấy lại một phần lợi ích của kiểu tĩnh mà vẫn giữ tính động.",
  "**Giải thuật & CTDL** ở mảng 02: `\"\".join()` so với `+=` là ví dụ đầu tiên bạn gặp về O(n) so với O(n²) trong code thật.",
  "**AI/ML** ở mảng 07: câu hỏi \"đây là bản sao hay là view\" trong NumPy và pandas chính là bài này ở quy mô dữ liệu lớn."
]

};
