window.LESSONS["01:lap-trinh-huong-doi-tuong:class-object-thuoc-tinh-phuong-thuc"] = {

tom_tat: [
  "**Class gói dữ liệu và hành vi tác động lên dữ liệu đó vào một chỗ.** Đó là toàn bộ lý do nó tồn tại — không phải để \"mô hình hoá thế giới thực\" như nhiều tài liệu nói.",
  "**`self` không phải từ khoá, nó chỉ là tham số đầu tiên.** `a.f(1)` được Python dịch thành `Lop.f(a, 1)` — hiểu phép dịch này là hết mọi bí ẩn về `self`.",
  "**Thuộc tính class dùng chung cho mọi instance; thuộc tính instance riêng cho từng object.** Đặt `list` làm thuộc tính class là bẫy giống hệt bẫy giá trị mặc định mutable ở module 5.",
  "**`__init__` không tạo object** — `__new__` tạo, `__init__` chỉ khởi tạo cái đã tạo. Phân biệt này giải thích vì sao `__init__` không được `return` gì.",
  "**Đừng viết class khi một hàm là đủ.** Class chỉ đáng khi có **trạng thái tồn tại giữa các lời gọi** hoặc nhiều hàm cùng thao tác trên một cụm dữ liệu."
],

can_biet_truoc: [
  "Bài **Cú pháp, kiểu dữ liệu, toán tử** ở module 5 — \"biến là nhãn\" và mutable/immutable là nền cho bẫy thuộc tính class.",
  "Bài **Hàm, tham số mặc định** ở module 5 — phương thức là hàm, và `self` chỉ là tham số đầu tiên.",
  "Bài **Decorator** ở module 5 — `@property`, `@staticmethod`, `@classmethod` đều là decorator.",
  "Bài **List, dict, set, tuple** ở module 5 — `NamedTuple` và `dataclass` đã được so sánh ở đó; bài này giải thích class thường."
],

dinh_nghia: "Nói đời thường: **class là bản thiết kế, object là căn nhà xây từ bản thiết kế đó**. Một bản thiết kế, xây được nhiều căn; mỗi căn có đồ đạc riêng nhưng cùng bố cục.\n\nNói chính xác:\n\n- **Class** — khuôn mẫu định nghĩa **thuộc tính** (dữ liệu) và **phương thức** (hành vi).\n- **Object** / **instance** — một thực thể cụ thể được tạo từ class. Trong Python, class **cũng là object**.\n- **Thuộc tính instance** (*instance attribute*) — dữ liệu riêng của từng object, lưu trong `__dict__` của object đó.\n- **Thuộc tính class** (*class attribute*) — dữ liệu **dùng chung** cho mọi instance, lưu trong `__dict__` của class.\n- **Phương thức** — hàm định nghĩa trong class. Nhận `self` (object hiện tại) làm tham số đầu.\n- **`self`** — **không phải từ khoá**. Chỉ là tên quy ước cho tham số đầu tiên.\n- **`__init__`** — phương thức khởi tạo, chạy **sau khi** object đã được tạo.\n\nĐiểm cần đính chính ngay vì tài liệu hay nói sai: **`__init__` không phải constructor.** Object đã tồn tại trước khi `__init__` chạy — nó được tạo bởi `__new__`. `__init__` chỉ điền dữ liệu vào cái đã có, và đó là lý do nó bắt buộc trả về `None`.",

vi_sao_can: "OOP là cách tổ chức code chiếm ưu thế trong phần lớn hệ thống thật, và Python dùng nó ở khắp nơi:\n\n- **Mọi thứ trong Python đều là object.** `int`, `str`, `list`, hàm, module, cả class — tất cả. Hiểu class là hiểu ngôn ngữ vận hành thế nào, không chỉ là học một cú pháp mới.\n- **Đọc mã nguồn thư viện đòi hỏi nó.** Không có thư viện nghiêm túc nào chỉ gồm hàm rời rạc.\n- **Quản lý trạng thái phức tạp.** Khi một cụm dữ liệu và năm hàm thao tác lên nó luôn đi cùng nhau, class là cách gói chúng lại. Không có class thì bạn phải truyền cùng bốn tham số qua mọi hàm.\n- **Nền cho toàn bộ module này.** Kế thừa, đa hình, SOLID, design pattern — tất cả dựng trên bài này.\n- **Định hướng AI/ML và Backend.** Model, Dataset, Layer trong thư viện học sâu đều là class; framework web dùng class cho model dữ liệu và cho view.\n\nTình huống thật rất kinh điển: bạn viết class quản lý giỏ hàng, đặt `san_pham = []` ngay trong thân class cho tiện. Test một object thì đúng. Lên môi trường thật, **mọi người dùng thấy chung một giỏ hàng** — vì `list` đó thuộc về class, không thuộc về từng object. Đây là cùng một bẫy với `def f(ds=[])` ở module 5, chỉ khác chỗ xuất hiện, và phần cơ chế sẽ chỉ rõ vì sao.",

co_che: "### Class tối giản và cách đọc nó\n\n```python\nclass NguoiDung:\n    \"\"\"Đại diện một người dùng trong hệ thống.\"\"\"\n\n    def __init__(self, ten: str, email: str):\n        self.ten = ten            # ⭐ thuộc tính INSTANCE\n        self.email = email\n        self.dang_hoat_dong = True\n\n    def chao(self) -> str:\n        return f\"Xin chào {self.ten}\"\n\nnd = NguoiDung(\"Minh\", \"m@x.com\")     # tạo instance\nnd.chao()                             # 'Xin chào Minh'\nnd.ten                                # 'Minh'\n```\n\nChuyện gì xảy ra khi gọi `NguoiDung(\"Minh\", \"m@x.com\")`:\n\n```\n① Python gọi NguoiDung.__new__(NguoiDung)  → TẠO object rỗng\n② Python gọi obj.__init__(\"Minh\", \"m@x.com\") → ĐIỀN dữ liệu vào\n③ Trả về obj\n```\n\nHai bước tách bạch, và đó là lý do `__init__` **không được `return` gì khác `None`**:\n\n```python\nclass A:\n    def __init__(self):\n        return 5          # TypeError: __init__() should return None\n```\n\nObject đã tồn tại rồi; `__init__` không có gì để trả về. Trong 99% trường hợp bạn chỉ cần `__init__` — `__new__` chỉ dùng khi cần can thiệp vào việc **tạo** object, ví dụ khi kế thừa từ kiểu immutable.\n\n### `self` — chỉ là tham số đầu tiên\n\nĐây là phần gỡ bí ẩn quan trọng nhất của bài:\n\n```python\nnd.chao()\n\n#  ⇕  Python thật sự làm:\n\nNguoiDung.chao(nd)\n```\n\nKiểm chứng trực tiếp:\n\n```python\nNguoiDung.chao(nd)          # 'Xin chào Minh' — gọi được, y hệt\nnd.chao                     # <bound method NguoiDung.chao of <...>>\nNguoiDung.chao              # <function NguoiDung.chao at 0x...>\n```\n\n`nd.chao` là **bound method** — một hàm đã được \"buộc\" sẵn `nd` vào tham số đầu. `NguoiDung.chao` là hàm thường.\n\nHệ quả: `self` **không phải từ khoá**, đổi tên được:\n\n```python\nclass A:\n    def f(toi):             # chạy được, nhưng ĐỪNG làm\n        return toi\n```\n\nQuy ước `self` mạnh tới mức đổi tên là code không ai đọc nổi — nhưng biết nó chỉ là quy ước thì hiểu đúng cơ chế.\n\nLỗi hay gặp của người mới:\n\n```python\nclass A:\n    def f():                # ❌ quên self\n        return 1\n\nA().f()   # TypeError: f() takes 0 positional arguments but 1 was given\n#         ← Python truyền instance vào mà hàm không có chỗ nhận\n```\n\nThông báo lỗi \"nhận 0 nhưng được truyền 1\" giờ đọc ra ngay nguyên nhân.\n\n### Thuộc tính instance và thuộc tính class\n\n```python\nclass NguoiDung:\n    loai = \"thuong\"          # ⭐ thuộc tính CLASS — dùng chung\n\n    def __init__(self, ten):\n        self.ten = ten        # ⭐ thuộc tính INSTANCE — riêng\n\na = NguoiDung(\"Minh\")\nb = NguoiDung(\"An\")\n\na.ten, b.ten                  # 'Minh', 'An'  — khác nhau\na.loai, b.loai                # 'thuong', 'thuong' — CÙNG một object\n\nNguoiDung.loai = \"vip\"        # đổi ở class\na.loai, b.loai                # 'vip', 'vip' — CẢ HAI đổi theo\n```\n\nHai kho lưu tách biệt:\n\n```\nNguoiDung.__dict__   →  {'loai': 'thuong', '__init__': <function>, 'chao': ...}\na.__dict__           →  {'ten': 'Minh'}\nb.__dict__           →  {'ten': 'An'}\n```\n\nThứ tự tra cứu khi đọc `a.loai`:\n\n```\n① Tìm trong a.__dict__          không có\n② Tìm trong NguoiDung.__dict__   CÓ → trả về\n③ Tìm trong các class cha        (bài kế thừa sẽ nói)\n④ Không thấy → AttributeError\n```\n\nNhưng **khi gán thì luôn ghi vào instance**:\n\n```python\na.loai = \"vip\"           # TẠO thuộc tính instance mới, che thuộc tính class\na.loai, b.loai           # 'vip', 'thuong'  ← b KHÔNG đổi\na.__dict__               # {'ten': 'Minh', 'loai': 'vip'}\nNguoiDung.loai           # 'thuong' — class không đổi\n\ndel a.loai               # xoá thuộc tính instance\na.loai                   # 'thuong' — lại thấy thuộc tính class\n```\n\nQuy tắc gói lại:\n\n```\nĐỌC  → tra instance trước, rồi mới tới class\nGHI  → LUÔN ghi vào instance (trừ khi gán qua tên class)\n```\n\n### Bẫy mutable ở thuộc tính class\n\nĐây là bẫy nghiêm trọng nhất của bài, và là bẫy `def f(ds=[])` ở module 5 xuất hiện lại:\n\n```python\nclass GioHang:\n    san_pham = []                    # ❌ BẪY — dùng chung cho MỌI instance\n\n    def them(self, sp):\n        self.san_pham.append(sp)     # SỬA object, không gán lại\n\na, b = GioHang(), GioHang()\na.them(\"sách\")\nb.san_pham                            # ['sách']  ← (!) giỏ của b cũng có\n```\n\nVì sao: `self.san_pham.append(...)` **đọc** `san_pham` (không thấy ở instance → lấy của class) rồi **sửa object đó tại chỗ**. Không có phép gán nào, nên không có thuộc tính instance nào được tạo.\n\n```python\n# ✓ Sửa: tạo list MỚI cho mỗi instance trong __init__\nclass GioHang:\n    def __init__(self):\n        self.san_pham = []           # ⭐ gán ⇒ tạo thuộc tính instance\n\n    def them(self, sp):\n        self.san_pham.append(sp)\n```\n\nĐáng chú ý: với **immutable** thì thuộc tính class hoàn toàn an toàn, vì không ai sửa tại chỗ được:\n\n```python\nclass A:\n    dem = 0                # ✓ int immutable\n    ten = \"mac dinh\"       # ✓ str immutable\n    THUE = 0.1             # ✓ hằng số — đây là công dụng ĐÚNG\n```\n\nQuy tắc: **thuộc tính class chỉ dùng cho hằng số và giá trị immutable dùng chung. Mọi dữ liệu mutable phải khởi tạo trong `__init__`.**\n\n### Ba loại phương thức\n\n```python\nclass Nhiet:\n    DON_VI = \"C\"\n\n    def __init__(self, do_c: float):\n        self.do_c = do_c\n\n    # ① Phương thức instance — nhận self, truy cập dữ liệu instance\n    def sang_f(self) -> float:\n        return self.do_c * 9 / 5 + 32\n\n    # ② Phương thức class — nhận cls, thường dùng làm hàm tạo phụ\n    @classmethod\n    def tu_do_f(cls, do_f: float) -> \"Nhiet\":\n        return cls((do_f - 32) * 5 / 9)     # ⭐ cls, KHÔNG hard-code Nhiet\n\n    # ③ Phương thức tĩnh — không nhận gì, chỉ nhóm về mặt logic\n    @staticmethod\n    def hop_le(do_c: float) -> bool:\n        return do_c >= -273.15\n```\n\n```python\nn = Nhiet(100)\nn.sang_f()                # 212.0\nNhiet.tu_do_f(212).do_c   # 100.0  — hàm tạo phụ\nNhiet.hop_le(-300)        # False  — gọi qua class hay instance đều được\n```\n\nSo sánh vai trò:\n\n| | Instance | `@classmethod` | `@staticmethod` |\n|---|---|---|---|\n| Tham số đầu | `self` | `cls` | không có |\n| Truy cập dữ liệu instance | **✓** | ✗ | ✗ |\n| Truy cập thuộc tính class | ✓ | **✓** | ✗ |\n| Dùng chính | hành vi trên dữ liệu | **hàm tạo phụ** | hàm phụ trợ liên quan |\n\n**`classmethod` dùng nhiều nhất làm hàm tạo phụ** — và `cls` quan trọng hơn vẻ ngoài:\n\n```python\nclass NhietKeChinhXac(Nhiet):\n    pass\n\nNhietKeChinhXac.tu_do_f(212)      # trả về NhietKeChinhXac ✓ nhờ dùng cls\n#                                   nếu hard-code Nhiet thì trả về Nhiet — SAI\n```\n\n`staticmethod` thì ít cần: nếu phương thức không dùng `self` cũng không dùng `cls`, thường **nó nên là một hàm ở tầng module**. Lý do giữ nó trong class chỉ là để nhóm về mặt logic khi nó gắn chặt với class đó.\n\n### `@property` — thuộc tính tính toán\n\n```python\nclass HinhChuNhat:\n    def __init__(self, rong: float, cao: float):\n        self.rong = rong\n        self.cao = cao\n\n    @property\n    def dien_tich(self) -> float:\n        return self.rong * self.cao      # ⭐ tính mỗi lần đọc\n\nh = HinhChuNhat(3, 4)\nh.dien_tich          # 12 — truy cập như THUỘC TÍNH, không có ()\nh.dien_tich = 20     # AttributeError: property has no setter\n```\n\nGiá trị của `@property`: nó cho phép **đổi từ thuộc tính thường sang tính toán mà không phá code người dùng**.\n\n```python\n# Phiên bản 1 — thuộc tính thường\nclass A:\n    def __init__(self):\n        self.dien_tich = 12\n\n# Phiên bản 2 — đổi sang tính toán, người dùng KHÔNG phải sửa gì\nclass A:\n    @property\n    def dien_tich(self):\n        return self.rong * self.cao\n```\n\nCả hai đều dùng bằng `a.dien_tich`. Đây là lý do Python **không cần getter/setter mặc định** như Java: bạn cứ để thuộc tính công khai, và chỉ đổi sang `property` khi thật sự cần thêm logic.\n\nThêm setter khi cần kiểm tra:\n\n```python\nclass Nhiet:\n    def __init__(self, do_c):\n        self.do_c = do_c              # ⭐ đi qua setter, được kiểm tra luôn\n\n    @property\n    def do_c(self):\n        return self._do_c\n\n    @do_c.setter\n    def do_c(self, gt):\n        if gt < -273.15:\n            raise ValueError(f\"dưới độ không tuyệt đối: {gt}\")\n        self._do_c = gt\n\nn = Nhiet(25)\nn.do_c = -300        # ValueError ✓\n```\n\nChú ý `self.do_c = do_c` trong `__init__` **cũng đi qua setter** — nên dữ liệu được kiểm tra ngay từ lúc tạo object. Đây là mẫu đáng dùng.\n\n### Thuộc tính động và `__slots__`\n\nPython cho phép thêm thuộc tính bất kỳ lúc nào:\n\n```python\nnd = NguoiDung(\"Minh\", \"m@x.com\")\nnd.tuoi = 22              # ✓ tạo thuộc tính mới ngay lúc chạy\nnd.__dict__               # {'ten': 'Minh', 'email': ..., 'tuoi': 22}\n```\n\nLinh hoạt, nhưng có cái giá: **gõ sai tên thuộc tính không báo lỗi**.\n\n```python\nnd.emial = \"sai@x.com\"    # tạo thuộc tính MỚI, không ai báo\nnd.email                   # vẫn là giá trị cũ — lỗi im lặng\n```\n\n`__slots__` khoá danh sách thuộc tính lại:\n\n```python\nclass Diem:\n    __slots__ = (\"x\", \"y\")           # ⭐ chỉ cho phép hai thuộc tính này\n\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\nd = Diem(1, 2)\nd.z = 3        # AttributeError: 'Diem' object has no attribute 'z'\nd.__dict__     # AttributeError — không còn __dict__\n```\n\nHai lợi ích: bắt lỗi gõ sai tên, và **tiết kiệm bộ nhớ đáng kể** (không có `__dict__` cho mỗi object). Với hàng triệu object nhỏ thì chênh lệch rất lớn.\n\nĐánh đổi: mất tính linh hoạt, và phức tạp thêm khi kế thừa. `__slots__` chỉ đáng dùng khi bạn **thật sự tạo rất nhiều object** — không phải tối ưu nên làm mặc định.\n\n### Quy ước công khai và nội bộ\n\nPython **không có `private` thật**:\n\n```python\nclass A:\n    def __init__(self):\n        self.cong_khai = 1\n        self._noi_bo = 2          # quy ước: \"đừng dùng từ ngoài\"\n        self.__rat_rieng = 3      # đổi tên (name mangling)\n\na = A()\na._noi_bo                # 2 — vẫn truy cập được, chỉ là bạn không nên\na.__rat_rieng            # AttributeError\na._A__rat_rieng          # 3 — vẫn lấy được, chỉ là tên bị đổi\n```\n\nHai gạch dưới **không phải để bảo mật** — nó để tránh xung đột tên khi kế thừa: `A.__x` thành `_A__x`, `B.__x` thành `_B__x`, nên lớp con không vô tình ghi đè.\n\nTriết lý Python ở đây khác Java rõ rệt: **\"chúng ta đều là người lớn\"** — quy ước thay cho ép buộc. Đánh đổi là dựa vào kỷ luật của người dùng; đổi lại là không có rào cản nhân tạo khi cần gỡ lỗi hay mở rộng.\n\n### Khi nào KHÔNG nên viết class\n\nPhần này quan trọng ngang phần cú pháp, vì OOP bị lạm dụng rất nhiều:\n\n```python\n# ❌ Class chỉ có một phương thức và không giữ trạng thái\nclass BoTinhToan:\n    def tinh(self, x):\n        return x * 2\n\nBoTinhToan().tinh(5)\n\n# ✓ Một hàm là đủ\ndef tinh(x):\n    return x * 2\n```\n\n```python\n# ❌ Class chỉ chứa dữ liệu, không có hành vi\nclass Diem:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\n# ✓ Dùng dataclass hoặc NamedTuple (đã gặp ở module 5)\nfrom dataclasses import dataclass\n\n@dataclass\nclass Diem:\n    x: float\n    y: float\n```\n\nBa câu hỏi để quyết định:\n\n```\n① Có TRẠNG THÁI tồn tại giữa các lời gọi không?      không → hàm\n② Có NHIỀU hàm cùng thao tác trên một cụm dữ liệu?    không → hàm\n③ Cần NHIỀU BẢN độc lập, mỗi bản dữ liệu riêng?       không → module\n```\n\nTrả lời \"không\" cả ba thì class chỉ là lớp bọc thừa. Nhiều \"class\" trong code thật thực chất là **một namespace** — và Python đã có namespace sẵn: đó là module.",

vi_du: "### Từ hàm rời rạc sang class\n\n```python\n# ❌ Trạng thái rải rác, phải truyền qua mọi hàm\ndef tao_gio():\n    return {\"san_pham\": [], \"giam_gia\": 0}\n\ndef them(gio, ten, gia, sl=1):\n    gio[\"san_pham\"].append({\"ten\": ten, \"gia\": gia, \"sl\": sl})\n\ndef tong(gio):\n    t = sum(s[\"gia\"] * s[\"sl\"] for s in gio[\"san_pham\"])\n    return t * (1 - gio[\"giam_gia\"])\n\ngio = tao_gio()\nthem(gio, \"sách\", 100_000)      # phải nhớ truyền gio vào MỌI hàm\nprint(tong(gio))\n```\n\n```python\n# ✓ Class gói dữ liệu và hành vi\nclass GioHang:\n    THUE = 0.1                              # ⭐ hằng số — thuộc tính class ĐÚNG chỗ\n\n    def __init__(self):\n        self.san_pham: list[dict] = []      # ⭐ mutable ⇒ phải ở __init__\n        self.giam_gia = 0.0\n\n    def them(self, ten: str, gia: float, sl: int = 1) -> None:\n        if gia < 0 or sl < 1:\n            raise ValueError(f\"giá hoặc số lượng không hợp lệ: {gia}, {sl}\")\n        self.san_pham.append({\"ten\": ten, \"gia\": gia, \"sl\": sl})\n\n    @property\n    def tam_tinh(self) -> float:\n        return sum(s[\"gia\"] * s[\"sl\"] for s in self.san_pham)\n\n    @property\n    def tong(self) -> float:\n        return self.tam_tinh * (1 - self.giam_gia) * (1 + self.THUE)\n\n    def __len__(self) -> int:\n        return sum(s[\"sl\"] for s in self.san_pham)\n\ngio = GioHang()\ngio.them(\"sách\", 100_000, 2)\ngio.giam_gia = 0.1\nprint(f\"{len(gio)} món, tổng {gio.tong:,.0f}đ\")\n```\n\nBốn lợi ích cụ thể của bản thứ hai:\n\n```\n① Không phải truyền `gio` qua mọi hàm\n② Kiểm tra đầu vào nằm ngay chỗ dữ liệu vào\n③ tam_tinh và tong là property ⇒ luôn đúng, không sợ quên cập nhật\n④ len(gio) dùng được nhờ __len__ — hoà vào cú pháp Python\n```\n\nĐiểm ③ đáng nhấn: nếu lưu `self.tong` như một thuộc tính thường, mỗi lần thêm sản phẩm phải nhớ tính lại — và quên là dữ liệu sai. Property loại bỏ hẳn khả năng đó.\n\n### Nhìn tận mắt thuộc tính instance và class\n\n```python\nclass A:\n    chung = \"class\"\n\n    def __init__(self):\n        self.rieng = \"instance\"\n\na, b = A(), A()\n\nprint(A.__dict__.keys())      # dict_keys(['chung', '__init__', ...])\nprint(a.__dict__)             # {'rieng': 'instance'}\n\nprint(a.chung, b.chung)       # class class\nA.chung = \"đã đổi\"\nprint(a.chung, b.chung)       # đã đổi đã đổi   ← CẢ HAI\n\na.chung = \"chỉ của a\"         # ⭐ tạo thuộc tính INSTANCE che class\nprint(a.chung, b.chung)       # chỉ của a / đã đổi\nprint(a.__dict__)             # {'rieng': ..., 'chung': 'chỉ của a'}\n\ndel a.chung\nprint(a.chung)                # đã đổi — lại thấy thuộc tính class\n```\n\nChạy đoạn này một lần là nhớ vĩnh viễn quy tắc \"đọc tra hai chỗ, ghi luôn vào instance\".\n\n### Bẫy mutable — tái hiện và sửa\n\n```python\n# ❌ Bẫy\nclass Blog:\n    bai_viet = []\n\n    def dang(self, tieu_de):\n        self.bai_viet.append(tieu_de)\n\nb1, b2 = Blog(), Blog()\nb1.dang(\"Bài của blog 1\")\nprint(b2.bai_viet)          # ['Bài của blog 1']  ← (!) blog 2 cũng có\nprint(b1.bai_viet is b2.bai_viet)    # True — CÙNG một list\n\n# ✓ Sửa\nclass Blog:\n    def __init__(self):\n        self.bai_viet = []\n\nb1, b2 = Blog(), Blog()\nb1.dang(\"Bài của blog 1\")\nprint(b2.bai_viet)          # [] ✓\nprint(b1.bai_viet is b2.bai_viet)    # False — hai list riêng\n```\n\nDòng `is` là cách chẩn đoán nhanh nhất: `True` nghĩa là hai object đang dùng chung, và đó gần như luôn là lỗi khi nói về dữ liệu instance.\n\nBiến thể tinh vi hơn — thuộc tính class **có vẻ** an toàn:\n\n```python\nclass CauHinh:\n    mac_dinh = {\"debug\": False}      # ❌ dict cũng là mutable\n\n    def bat_debug(self):\n        self.mac_dinh[\"debug\"] = True   # sửa dict CHUNG\n\nc1, c2 = CauHinh(), CauHinh()\nc1.bat_debug()\nprint(c2.mac_dinh)          # {'debug': True}  ← lan sang c2\n```\n\n### `classmethod` làm hàm tạo phụ\n\n```python\nfrom datetime import date\n\nclass NguoiDung:\n    def __init__(self, ten: str, nam_sinh: int):\n        self.ten = ten\n        self.nam_sinh = nam_sinh\n\n    @classmethod\n    def tu_tuoi(cls, ten: str, tuoi: int) -> \"NguoiDung\":\n        return cls(ten, date.today().year - tuoi)\n\n    @classmethod\n    def tu_dict(cls, d: dict) -> \"NguoiDung\":\n        return cls(d[\"ten\"], d[\"nam_sinh\"])\n\n    @classmethod\n    def tu_chuoi(cls, s: str) -> \"NguoiDung\":\n        ten, nam = s.split(\",\")\n        return cls(ten.strip(), int(nam))\n\n# Bốn cách tạo, cùng một class\nNguoiDung(\"Minh\", 2004)\nNguoiDung.tu_tuoi(\"An\", 22)\nNguoiDung.tu_dict({\"ten\": \"Bình\", \"nam_sinh\": 2003})\nNguoiDung.tu_chuoi(\"Chi, 2005\")\n```\n\nĐây là mẫu rất đáng dùng: `__init__` giữ một dạng dữ liệu chuẩn, còn các `classmethod` lo việc **chuyển đổi từ các nguồn khác nhau**. So với việc nhồi mọi dạng đầu vào vào `__init__` bằng `if/elif`, cách này rõ ràng hơn nhiều và mỗi hàm tạo có tên nói lên ý nghĩa.\n\nVì sao phải dùng `cls` chứ không viết thẳng `NguoiDung`:\n\n```python\nclass NguoiDungVIP(NguoiDung):\n    pass\n\nv = NguoiDungVIP.tu_tuoi(\"An\", 22)\ntype(v)          # <class 'NguoiDungVIP'> ✓ nhờ cls\n#                  nếu hard-code NguoiDung thì ra NguoiDung — sai kiểu\n```\n\n### `@property` với kiểm tra dữ liệu\n\n```python\nclass TaiKhoan:\n    def __init__(self, chu: str, so_du: float = 0):\n        self.chu = chu\n        self.so_du = so_du               # ⭐ đi qua setter\n\n    @property\n    def so_du(self) -> float:\n        return self._so_du\n\n    @so_du.setter\n    def so_du(self, gt: float) -> None:\n        if not isinstance(gt, (int, float)):\n            raise TypeError(f\"số dư phải là số, nhận {type(gt).__name__}\")\n        if gt < 0:\n            raise ValueError(f\"số dư không được âm: {gt}\")\n        self._so_du = float(gt)\n\n    @property\n    def hien_thi(self) -> str:            # ⭐ property chỉ đọc\n        return f\"{self._so_du:,.0f}đ\"\n\ntk = TaiKhoan(\"Minh\", 1_000_000)\nprint(tk.hien_thi)        # 1,000,000đ\ntk.so_du = 500_000        # ✓ qua kiểm tra\ntk.so_du = -100           # ValueError ✓\ntk.hien_thi = \"x\"         # AttributeError — không có setter ✓\n```\n\nBa điểm thiết kế:\n\n```\nself.so_du trong __init__  → dữ liệu được kiểm tra NGAY từ lúc tạo\n_so_du (một gạch dưới)      → quy ước \"kho nội bộ, đừng đụng\"\nhien_thi không có setter    → chỉ đọc, thể hiện đúng ý định\n```\n\nCạm bẫy phải tránh: đặt tên kho nội bộ **trùng** tên property gây đệ quy vô hạn.\n\n```python\n@property\ndef so_du(self):\n    return self.so_du        # ❌ gọi lại chính nó ⇒ RecursionError\n```\n\n### `__slots__` — đo lợi ích thật\n\n```python\nimport sys\n\nclass DiemThuong:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\nclass DiemSlots:\n    __slots__ = (\"x\", \"y\")\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\na, b = DiemThuong(1, 2), DiemSlots(1, 2)\nprint(sys.getsizeof(a) + sys.getsizeof(a.__dict__))    # ~152 byte\nprint(sys.getsizeof(b))                                 # ~56 byte\n\n# Với 1 triệu object thì chênh lệch là hàng trăm megabyte\nds = [DiemSlots(i, i) for i in range(1_000_000)]\n\n# Và bắt được lỗi gõ sai tên\nb.z = 3         # AttributeError ✓\na.z = 3         # ✓ không lỗi — thuộc tính mới, lỗi im lặng\n```\n\nKhi nào dùng: **rất nhiều object nhỏ cùng loại** — điểm toạ độ, bản ghi dữ liệu, node cây. Khi nào đừng: class thông thường trong ứng dụng, nơi số lượng object nhỏ và tính linh hoạt có giá trị hơn.\n\n### Kiểm tra object lúc chạy\n\n```python\nnd = NguoiDung(\"Minh\", 2004)\n\ntype(nd)                      # <class 'NguoiDung'>\nisinstance(nd, NguoiDung)     # True — ⭐ nên dùng, nhận cả lớp con\nnd.__class__.__name__         # 'NguoiDung'\n\nvars(nd)                       # {'ten': 'Minh', 'nam_sinh': 2004} = nd.__dict__\ndir(nd)                        # mọi tên truy cập được, gồm cả kế thừa\n\nhasattr(nd, \"ten\")             # True\ngetattr(nd, \"ten\")             # 'Minh'\ngetattr(nd, \"khong_co\", \"mặc định\")   # ⭐ có giá trị dự phòng\nsetattr(nd, \"tuoi\", 22)\ndelattr(nd, \"tuoi\")\n\n# Chỉ lấy thuộc tính công khai\n{k: v for k, v in vars(nd).items() if not k.startswith(\"_\")}\n```\n\n`getattr` với giá trị mặc định rất hữu ích khi làm việc với object không đồng nhất — nó là `dict.get` phiên bản cho thuộc tính, và tránh được `try/except AttributeError`.\n\n### Khi class là lựa chọn sai\n\n```python\n# ❌ \"Class\" thực chất chỉ là namespace\nclass TienIch:\n    @staticmethod\n    def lam_sach(s): return s.strip().lower()\n    @staticmethod\n    def tach_tu(s): return s.split()\n\nTienIch.lam_sach(\"  A  \")\n\n# ✓ Module ĐÃ là namespace — Python không cần class cho việc này\n# tien_ich.py\ndef lam_sach(s): return s.strip().lower()\ndef tach_tu(s): return s.split()\n\n# dùng: from tien_ich import lam_sach\n```\n\nĐây là thói quen mang từ Java sang, nơi mọi thứ **buộc phải** nằm trong class. Python có module làm namespace, nên class toàn `staticmethod` là một tầng bọc không cần thiết.\n\n```python\n# ❌ Class chỉ để giữ một giá trị rồi gọi một lần\nclass BoXuLy:\n    def __init__(self, du_lieu):\n        self.du_lieu = du_lieu\n    def chay(self):\n        return [x * 2 for x in self.du_lieu]\n\nBoXuLy([1, 2, 3]).chay()\n\n# ✓\ndef xu_ly(du_lieu):\n    return [x * 2 for x in du_lieu]\n```\n\nDấu hiệu nhận biết: **object được tạo ra, dùng một lần, rồi vứt.** Nếu vòng đời object chỉ dài bằng một lời gọi hàm thì nó không cần là object.",

so_sanh: "### Thuộc tính instance và thuộc tính class\n\n| | Instance | Class |\n|---|---|---|\n| Lưu ở | `obj.__dict__` | `Lop.__dict__` |\n| Riêng cho từng object | **✓** | ✗ dùng chung |\n| Khai báo ở | trong `__init__` | thân class |\n| Đọc | tra instance → class | tra class |\n| Ghi qua `self.x = ...` | tạo/sửa ở instance | **không đụng tới class** |\n| Dùng cho | dữ liệu riêng | **hằng số, cấu hình chung** |\n| An toàn với mutable | ✓ | **✗ BẪY** |\n\nHàng cuối là hàng gây lỗi nhiều nhất. Quy tắc dứt khoát: **mutable thì luôn khởi tạo trong `__init__`.**\n\n### Ba loại phương thức\n\n| | Instance | `@classmethod` | `@staticmethod` |\n|---|---|---|---|\n| Tham số đầu | `self` | `cls` | không |\n| Đọc dữ liệu instance | **✓** | ✗ | ✗ |\n| Đọc thuộc tính class | ✓ | **✓** | ✗ |\n| Hoạt động đúng khi kế thừa | ✓ | **✓ nhờ `cls`** | ✓ |\n| Dùng chính | hành vi trên dữ liệu | **hàm tạo phụ** | nhóm logic |\n| Tần suất dùng | rất nhiều | vừa | **ít — thường nên là hàm module** |\n\nHàng cuối là lời khuyên thực dụng: `staticmethod` không dùng `self` cũng không dùng `cls`, nên phần lớn trường hợp nó nên là một hàm ở tầng module. Giữ nó trong class chỉ khi nó gắn chặt về mặt khái niệm.\n\n### `@property` và phương thức thường\n\n| | `@property` | Phương thức `get_x()` |\n|---|---|---|\n| Cách gọi | `obj.x` | `obj.get_x()` |\n| Có tham số | ✗ | ✓ |\n| Gợi ý chi phí tính toán | ✗ **trông như rẻ** | ✓ |\n| Đổi từ thuộc tính thường sang | **✓ không phá code** | ✗ phải sửa nơi gọi |\n| Phù hợp | tính nhanh, như một thuộc tính | tính tốn kém, có tham số |\n\nRanh giới đáng nhớ: **`property` nên rẻ.** Người dùng thấy `obj.x` sẽ nghĩ đó là đọc một giá trị có sẵn, và có thể gọi nó trong vòng lặp. Nếu bên trong là một truy vấn database thì đó là bất ngờ khó chịu — trường hợp đó dùng phương thức có tên rõ ràng như `tai_du_lieu()`.\n\n### Class, dataclass, NamedTuple\n\n| | Class thường | `@dataclass` | `NamedTuple` |\n|---|---|---|---|\n| Viết `__init__` tay | **✓ phải viết** | ✗ tự sinh | ✗ tự sinh |\n| Có `__repr__`, `__eq__` | phải tự viết | **✓ tự sinh** | ✓ |\n| Mutable | ✓ | ✓ (mặc định) | **✗** |\n| Hashable | ✓ mặc định | ✗ (mặc định) | **✓** |\n| Nhiều hành vi phức tạp | **✓** | ✓ | hạn chế |\n| Dùng khi | **hành vi phong phú** | **chứa dữ liệu + ít hành vi** | bản ghi bất biến |\n\nCách chọn: **dữ liệu là chính thì `dataclass`; hành vi là chính thì class thường.** Viết `__init__` chỉ để gán năm thuộc tính là dấu hiệu bạn cần `dataclass` — bài riêng trong module này sẽ đi sâu.\n\n### Mức độ \"riêng tư\"\n\n| | `x` | `_x` | `__x` |\n|---|---|---|---|\n| Truy cập từ ngoài | ✓ | **✓ nhưng không nên** | qua `_Lop__x` |\n| Có ép buộc | ✗ | ✗ | **đổi tên, không phải khoá** |\n| `import *` lấy | ✓ | ✗ | ✗ |\n| Mục đích | API công khai | **báo hiệu nội bộ** | tránh xung đột khi kế thừa |\n\nĐiểm cần nhớ: **Python không có `private` thật.** Hai gạch dưới không phải để bảo mật mà để tránh lớp con vô tình ghi đè — đây là mục đích duy nhất của nó, và dùng nó cho \"bảo mật\" là hiểu sai.\n\n### `__slots__` — có và không\n\n| | Không có | Có `__slots__` |\n|---|---|---|\n| Thêm thuộc tính lúc chạy | ✓ | **✗** |\n| Bắt lỗi gõ sai tên | ✗ | **✓** |\n| Bộ nhớ mỗi object | lớn hơn | **nhỏ hơn nhiều** |\n| Có `__dict__` | ✓ | ✗ |\n| Kế thừa | đơn giản | phức tạp hơn |\n| Dùng khi | **mặc định** | rất nhiều object nhỏ |\n\n### Khi nào cần class\n\n| Câu hỏi | \"Không\" nghĩa là |\n|---|---|\n| Có trạng thái giữa các lời gọi? | dùng **hàm** |\n| Nhiều hàm thao tác trên cùng cụm dữ liệu? | dùng **hàm** |\n| Cần nhiều bản độc lập? | dùng **module** |\n| Chỉ chứa dữ liệu, không có hành vi? | dùng **`dataclass`** |\n| Toàn `staticmethod`? | dùng **module** |\n\nBảng này đáng chạy qua trước khi viết class. OOP là công cụ, không phải mục tiêu — và ở Python, hàm cùng module giải quyết được nhiều việc hơn người quen với Java thường nghĩ.",

loi_thuong_gap: [
  "**Đặt `list`, `dict`, `set` làm thuộc tính class.** Mọi instance dùng chung — cùng bản chất với bẫy `def f(ds=[])`. Khởi tạo trong `__init__`.",
  "**Quên `self` trong định nghĩa phương thức.** `TypeError: takes 0 positional arguments but 1 was given` — Python truyền instance vào mà hàm không có chỗ nhận.",
  "**Tưởng `__init__` là constructor.** `__new__` tạo object, `__init__` chỉ khởi tạo. Đó là lý do `__init__` không được `return` gì.",
  "**Hard-code tên class trong `classmethod` thay vì dùng `cls`.** Lớp con sẽ nhận về object sai kiểu.",
  "**Đặt tên kho nội bộ trùng tên property.** `return self.so_du` trong property `so_du` gây `RecursionError`.",
  "**Dùng `@property` cho việc tốn kém.** Người dùng thấy `obj.x` tưởng là rẻ và gọi trong vòng lặp. Dùng phương thức có tên rõ ràng.",
  "**Viết getter/setter cho mọi thuộc tính theo thói quen Java.** Python để thuộc tính công khai, chỉ đổi sang `property` khi thật sự cần logic.",
  "**Tưởng `__x` là private thật.** Nó chỉ đổi tên thành `_Lop__x`; mục đích là tránh xung đột khi kế thừa, không phải bảo mật.",
  "**Viết class toàn `staticmethod`.** Module đã là namespace; đó là thói quen từ Java.",
  "**Viết class cho thứ chỉ dùng một lần rồi vứt.** Nếu vòng đời object bằng một lời gọi hàm thì nó nên là hàm.",
  "**Viết class chỉ chứa dữ liệu và tự viết `__init__` dài.** Dùng `@dataclass`.",
  "**Dùng `__slots__` như một tối ưu mặc định.** Nó chỉ đáng khi có rất nhiều object; đổi lại là mất linh hoạt và phức tạp khi kế thừa.",
  "**Lưu giá trị tính toán làm thuộc tính thường rồi quên cập nhật.** Dùng `@property` để nó luôn đúng.",
  "**Dùng `type(x) == Lop` thay vì `isinstance(x, Lop)`.** Cách đầu không nhận lớp con."
],

tu_kiem_tra: [
  "`a.f(1)` được Python dịch thành gì? `self` có phải từ khoá không?",
  "Vì sao quên `self` lại báo lỗi \"takes 0 positional arguments but 1 was given\"?",
  "`__init__` có phải constructor không? Vì sao nó không được `return` giá trị?",
  "Mô tả thứ tự tra cứu khi đọc `a.x`. Khi **ghi** `a.x = 1` thì chuyện gì xảy ra?",
  "Vì sao `list` làm thuộc tính class là bẫy mà `int` thì không?",
  "Chẩn đoán nhanh \"hai object dùng chung dữ liệu\" bằng lệnh nào?",
  "Ba loại phương thức khác nhau ở đâu? `staticmethod` khi nào nên là hàm module?",
  "Vì sao `classmethod` phải dùng `cls` chứ không viết thẳng tên class?",
  "`@property` cho lợi ích gì mà `get_x()` không có? Khi nào KHÔNG nên dùng property?",
  "Vì sao `return self.so_du` trong property `so_du` gây lỗi? Cách đặt tên đúng?",
  "`_x` và `__x` khác nhau thế nào? Mục đích thật của hai gạch dưới là gì?",
  "`__slots__` cho hai lợi ích nào, đánh đổi gì, và khi nào đáng dùng?",
  "Nêu ba câu hỏi để quyết định có nên viết class hay không.",
  "Vì sao class toàn `staticmethod` là dấu hiệu thiết kế sai trong Python?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Classes* trong tutorial — phần về phạm vi và không gian tên giải thích rất rõ cơ chế tra cứu thuộc tính.",
  "Sách **Fluent Python** — Luciano Ramalho. Các chương về object Python, về `property` và về quản lý thuộc tính.",
  "Sách **Python Tricks** — Dan Bader. Phần về class và OOP viết ngắn gọn, nhiều ví dụ về các bẫy thường gặp.",
  "Sách **Effective Python** — Brett Slatkin. Nhiều mục về khi nào dùng class, khi nào dùng hàm, và về `property`.",
  "Tài liệu Python, mục *Data model* — nơi định nghĩa `__new__`, `__init__`, `__slots__` và mọi phương thức đặc biệt."
],

lien_he: [
  "**Đóng gói (encapsulation)** ngay sau: đi sâu vào quy ước công khai/nội bộ và vào `property` như công cụ kiểm soát truy cập.",
  "**Kế thừa và MRO** sau đó: mở rộng thứ tự tra cứu thuộc tính sang các class cha.",
  "**Magic method** trong module này: `__init__` là một trong số đó; bài đó gom `__repr__`, `__eq__`, `__len__` thành hệ thống.",
  "**Dataclass và NamedTuple** trong module này: khi class chỉ chứa dữ liệu thì có công cụ tốt hơn.",
  "**Cú pháp, kiểu dữ liệu** ở module 5: bẫy thuộc tính class mutable là \"biến là nhãn\" xuất hiện lại.",
  "**Hàm, tham số mặc định** ở module 5: `def f(ds=[])` và `class A: ds = []` là cùng một bẫy ở hai chỗ.",
  "**Decorator** ở module 5: `@property`, `@classmethod`, `@staticmethod` đều là decorator dựng sẵn.",
  "**Type hint và mypy** ở module 5: chú thích kiểu cho thuộc tính và phương thức làm class tự tài liệu.",
  "**Clean Code** ở module 8 mảng 01: \"class nên nhỏ và có một trách nhiệm\" là chủ đề trung tâm, và SOLID trong module này đi sâu vào đó."
]

};
