window.LESSONS["01:bo-nho-co-che-thuc-thi:mutable-vs-immutable-va-bay-tham-so-mac-dinh"] = {

tom_tat: [
  "**Module 5 đã dạy bẫy `def f(ds=[])` và cách sửa. Bài này trả lời câu \"vì sao\"**: giá trị mặc định là một object nằm trong `func.__defaults__`, được tạo lúc `def` chạy và sống bằng vòng đời của hàm.",
  "**Immutable không có nghĩa là \"không sửa được ô nhớ\"** — nó có nghĩa là **không có phương thức nào sửa tại chỗ**. `x += 1` với `int` luôn tạo object mới; với `list` thì sửa tại chỗ.",
  "**`+=` là hai lệnh khác nhau tuỳ kiểu**: `INPLACE_ADD` gọi `__iadd__` nếu có, không thì rơi về `__add__` rồi gán lại. Đây là gốc của lỗi `t[0] += [1]` vừa chạy vừa báo lỗi.",
  "**Bẫy mặc định không chỉ với `list`** — nó xảy ra với mọi biểu thức được tính lúc `def` chạy: `datetime.now()`, `uuid4()`, một instance class, hay một `dict` cấu hình.",
  "**Đối tượng bất biến cho ba lợi ích ở tầng bộ nhớ**: chia sẻ an toàn giữa các nơi, hashable, và không cần sao chép phòng thủ."
],

can_biet_truoc: [
  "Bài **Tham chiếu vs giá trị** ngay trước — phần đầu object, bảng tên, con trỏ; bài này dựa hoàn toàn vào đó.",
  "Bài **Hàm, tham số mặc định** ở module 5 — bẫy và cách sửa đã dạy; bài này giải thích cơ chế.",
  "Bài **Cú pháp, kiểu dữ liệu** ở module 5 — ranh giới mutable/immutable ở góc người dùng.",
  "Bài **Magic method** ở module 6 — `__add__`, `__iadd__`, `__hash__` xuất hiện trực tiếp ở đây."
],

dinh_nghia: "Nói đời thường: module 5 nói *\"đừng viết `def f(ds=[])`\"* và bạn đã làm theo. Bài này mở nắp ra xem **cái list đó nằm ở đâu** — và hoá ra nó được cất trong chính object hàm, như một món đồ gắn liền với cái máy chứ không phải đồ dùng một lần.\n\nNói chính xác:\n\n- **Immutable** — kiểu **không cung cấp thao tác sửa tại chỗ**. Mọi \"thay đổi\" đều tạo object mới.\n- **Mutable** — kiểu có phương thức sửa nội dung mà **giữ nguyên `id`**.\n- **`func.__defaults__`** — `tuple` chứa các giá trị mặc định của tham số vị trí. Được tạo **một lần**, lúc câu lệnh `def` thực thi.\n- **`func.__kwdefaults__`** — tương tự, cho tham số chỉ-tên.\n- **Sửa tại chỗ** (*in-place*) — thao tác đổi nội dung object mà không đổi địa chỉ.\n- **`__iadd__`** — phương thức cho `+=` sửa tại chỗ. Không có nó thì `+=` rơi về `__add__` rồi gán lại tên.\n\nĐiểm cần đính chính so với cách hiểu thông thường: **\"immutable\" không phải thuộc tính của ô nhớ.** Ô nhớ nào cũng ghi đè được về mặt vật lý. Immutable là **giao ước ở tầng kiểu**: kiểu đó không cung cấp phương thức nào cho phép bạn sửa. Đó là lý do `int` có `__add__` mà không có `__iadd__`.",

vi_sao_can: "Hiểu tầng dưới biến ba \"quy tắc phải nhớ\" thành ba hệ quả tự suy ra được:\n\n- **Bẫy mặc định hết bí ẩn.** Biết `__defaults__` là gì thì bạn tự nhận ra mọi biến thể của bẫy, kể cả những dạng module 5 chưa nêu — như một instance class hay một `dict` cấu hình.\n- **`+=` hết gây bất ngờ.** Cùng một toán tử cho hai hành vi khác nhau tuỳ kiểu, và lỗi `t[0] += [1]` \"vừa báo lỗi vừa thành công\" chỉ hiểu được ở tầng này.\n- **Nó quyết định khi nào cần sao chép phòng thủ.** Nhận `tuple` thì không cần sao chép; nhận `list` thì cần. Biết lý do là biết khi nào bỏ được chi phí đó.\n- **Nó là nền cho hai bài sau.** Sao chép nông/sâu và dọn rác đều xoay quanh việc object nào chia sẻ được, object nào không.\n- **Định hướng AI/ML và Backend.** Cấu hình bất biến chia sẻ được giữa các luồng mà không cần khoá; dữ liệu mutable dùng chung là nguồn của lỗi khó lần nhất.\n\nTình huống thật rất kinh điển: bạn viết `def ghi(tin, luc=datetime.now())` và mọi dòng log đều mang **cùng một mốc thời gian** — thời điểm chương trình khởi động. Module 5 đã nêu ví dụ này; bài này chỉ rõ mốc đó được cất ở đâu, và cho bạn cách **nhìn thấy** nó bằng `ghi.__defaults__`.",

co_che: "### Immutable là giao ước, không phải tính chất vật lý\n\n```python\nx = 5\nprint(id(x))\nx += 1\nprint(id(x))       # ⭐ id KHÁC — object mới\n\nds = [1]\nprint(id(ds))\nds += [2]\nprint(id(ds))      # ⭐ id GIỐNG — sửa tại chỗ\n```\n\nCùng toán tử `+=`, hai kết quả khác nhau. Lý do nằm ở phương thức mà kiểu cung cấp:\n\n```python\nhasattr(int, \"__iadd__\")     # False — không có, rơi về __add__\nhasattr(list, \"__iadd__\")    # True  — có, sửa tại chỗ\n```\n\nCơ chế đầy đủ của `a += b`:\n\n```\n① Có type(a).__iadd__ không?\n   CÓ    → gọi nó, nó sửa a tại chỗ và trả về chính a\n   KHÔNG → tính a = a + b   (gọi __add__, tạo object MỚI, gán lại tên)\n② Gán kết quả vào tên a\n```\n\nBước ② luôn xảy ra, kể cả khi `__iadd__` đã sửa tại chỗ — và điều đó tạo ra một lỗi rất lạ mà mục sau sẽ nói.\n\nDanh sách hai nhóm, nhìn từ tầng kiểu:\n\n```\nIMMUTABLE   int, float, complex, bool, str, bytes, tuple, frozenset, range\n            → KHÔNG có phương thức sửa nội dung\n\nMUTABLE     list, dict, set, bytearray, và class thường\n            → có append/update/add/__setitem__...\n```\n\n### Ba lợi ích của immutable ở tầng bộ nhớ\n\n**① Chia sẻ an toàn — không cần sao chép phòng thủ:**\n\n```python\nHANG_SO = (1, 2, 3)          # tuple\n\ndef f():\n    return HANG_SO           # ✓ trả thẳng, không ai sửa được\n\nDANH_SACH = [1, 2, 3]        # list\n\ndef g():\n    return list(DANH_SACH)   # ⚠️ PHẢI sao chép, nếu không người gọi sửa được\n```\n\nSao chép phòng thủ tốn thời gian và bộ nhớ. Với dữ liệu bất biến, chi phí đó bằng không.\n\n**② Hashable — làm khoá `dict` được:**\n\n```python\nhash((1, 2))        # ✓\nhash([1, 2])        # TypeError\n```\n\nLý do đã gặp ở module 5: `dict` định vị khoá bằng hash, nên hash **phải không đổi** suốt thời gian object nằm trong `dict`. Chỉ object không sửa được mới đảm bảo điều đó.\n\n**③ Cho phép cache và gộp:**\n\n```python\na = 100\nb = 100\na is b              # True — CPython dùng chung được vì int không sửa được\n```\n\nGộp chỉ an toàn với immutable. Nếu `int` sửa được thì `a += 1` sẽ đổi luôn giá trị của `b` — thảm hoạ.\n\nĐây là lời giải thích cho cache số nguyên nhỏ ở bài trước: **nó khả thi chính vì `int` immutable.**\n\n### Giá trị mặc định sống ở đâu\n\nĐây là phần cốt lõi của bài:\n\n```python\ndef them(x, ds=[]):\n    ds.append(x)\n    return ds\n\nthem.__defaults__          # ([],)  ⭐ tuple chứa các giá trị mặc định\nid(them.__defaults__[0])   # địa chỉ của list đó\n\nthem(1)\nthem.__defaults__          # ([1],)  ⭐ chính list đó bị sửa\nthem(2)\nthem.__defaults__          # ([1, 2],)\n```\n\nChuyện gì xảy ra khi Python thực thi câu lệnh `def`:\n\n```\n① TÍNH mọi biểu thức mặc định — []  →  tạo một list object\n② Tạo object hàm\n③ Gắn tuple các giá trị đó vào func.__defaults__\n④ Buộc tên \"them\" vào object hàm\n```\n\nBước ① chạy **đúng một lần**, lúc `def` được thực thi — tức là lúc module được import (nối về bài import ở module 5: `def` là một câu lệnh được thực thi).\n\nMỗi lần gọi hàm mà không truyền `ds`:\n\n```\n① Lấy con trỏ từ func.__defaults__[0]\n② Sao chép con trỏ đó vào ô cục bộ \"ds\"\n③ KHÔNG tạo object mới\n```\n\nVì vậy mọi lần gọi đều thao tác trên **cùng một list**, và list đó sống bằng vòng đời của object hàm.\n\n```\nfunc object\n┌────────────────┐\n│ __code__       │\n│ __defaults__ ──┼──► tuple ──► [1, 2]   ⭐ object này KHÔNG BAO GIỜ\n│ __name__       │                          được tạo lại\n└────────────────┘\n```\n\n### Mọi biểu thức mặc định đều bị đóng băng\n\nBiết cơ chế thì suy ra được mọi biến thể của bẫy:\n\n```python\nfrom datetime import datetime\nimport uuid\n\ndef f1(ds=[]): ...                    # ❌ list dùng chung\ndef f2(d={}): ...                     # ❌ dict dùng chung\ndef f3(s=set()): ...                  # ❌ set dùng chung\ndef f4(luc=datetime.now()): ...       # ❌ THỜI ĐIỂM đóng băng lúc import\ndef f5(ma=uuid.uuid4()): ...          # ❌ cùng một UUID mãi mãi\ndef f6(cf=CauHinh()): ...             # ❌ một instance dùng chung\ndef f7(n=len(DANH_SACH)): ...         # ❌ độ dài LÚC ĐÓ, không cập nhật\n\ndef g1(n=0): ...                      # ✓ int immutable\ndef g2(s=\"\"): ...                     # ✓ str immutable\ndef g3(t=(1, 2)): ...                 # ✓ tuple immutable\ndef g4(x=None): ...                   # ✓ None — mẫu chuẩn\n```\n\nBốn dòng `f4`–`f7` là những dạng module 5 chưa nêu hết, và chúng **suy ra trực tiếp** từ quy tắc \"biểu thức mặc định tính lúc `def` chạy\".\n\nDòng `f7` đặc biệt tinh vi: nó không mutable, nhưng nó chụp lại một **giá trị tại thời điểm import** và không bao giờ cập nhật.\n\n### Vì sao Python thiết kế như vậy\n\nMột câu hỏi hợp lý: sao không tính lại mặc định mỗi lần gọi?\n\n```\nLỢI của cách hiện tại\n· Nhanh — không phải tính lại biểu thức ở mỗi lời gọi\n· Đơn giản, nhất quán: def là một câu lệnh, và mọi câu lệnh chạy một lần\n· Cho phép thủ thuật hữu ích: đóng băng biến trong vòng lặp (lambda i=i:)\n\nGIÁ\n· Bẫy mutable — và nó đủ nghiêm trọng để mọi tài liệu Python phải cảnh báo\n```\n\nĐiểm thứ ba của cột lợi đáng chú ý: **cùng một cơ chế gây bẫy lại là cách sửa bẫy khác** — bẫy `lambda` trong vòng lặp ở module 5:\n\n```python\nham = [lambda i=i: i for i in range(3)]     # ⭐ dùng __defaults__ có chủ đích\n[f() for f in ham]                          # [0, 1, 2]\n\nfor f in ham:\n    print(f.__defaults__)                   # (0,) (1,) (2,) — mỗi hàm một giá trị\n```\n\nHiểu cơ chế thì cả hai hiện tượng — bẫy và thủ thuật — trở thành một.\n\n### Mẫu `None` và vì sao nó đúng\n\n```python\ndef them(x, ds=None):\n    if ds is None:\n        ds = []            # ⭐ tạo object MỚI ở mỗi lần gọi\n    ds.append(x)\n    return ds\n\nthem.__defaults__          # (None,) — None là object DUY NHẤT, bất biến\n```\n\n`None` hoàn hảo cho vai trò này vì ba lý do:\n\n```\n· Immutable — không ai sửa được\n· Là object DUY NHẤT toàn chương trình — so bằng `is` rất nhanh\n· Không trùng với bất kỳ giá trị hợp lệ nào\n```\n\nĐiểm thứ ba là lý do dùng `is None` chứ không `if not ds` — đã nói ở module 5, giờ có thêm lý do bộ nhớ: `is` chỉ so hai con trỏ, không gọi phương thức nào.\n\nKhi `None` cũng là giá trị hợp lệ, dùng một object canh:\n\n```python\n_KHONG_TRUYEN = object()        # ⭐ object duy nhất, không trùng gì cả\n\ndef f(x=_KHONG_TRUYEN):\n    if x is _KHONG_TRUYEN:\n        print(\"không truyền gì\")\n    else:\n        print(f\"truyền {x!r} (kể cả None)\")\n\nf()          # không truyền gì\nf(None)      # truyền None\n```\n\n`object()` trần là object nhỏ nhất có thể tạo (16 byte) và **chắc chắn không bằng bất cứ thứ gì khác** — đúng thứ cần cho vai trò canh.\n\n### Bẫy `+=` với `tuple` chứa `list`\n\nĐây là hiện tượng lạ nhất trong bài, và chỉ giải thích được ở tầng này:\n\n```python\nt = (1, 2, [3])\nt[2] += [4]\n# TypeError: 'tuple' object does not support item assignment\n\nprint(t)     # (1, 2, [3, 4])   ⭐ VẪN ĐỔI dù đã báo lỗi!\n```\n\nGiải thích theo hai bước của `+=`:\n\n```\n① t[2].__iadd__([4])  →  list được sửa TẠI CHỖ, thành [3, 4]  ✓ THÀNH CÔNG\n② t[2] = <kết quả>     →  tuple không cho gán  ✗ TypeError\n```\n\nBước ① đã xong trước khi bước ② báo lỗi. Kết quả: **thao tác vừa thành công một nửa vừa ném ngoại lệ.**\n\nCách tránh:\n\n```python\nt[2].extend([4])       # ✓ chỉ gọi phương thức, không có phép gán\nt[2].append(4)         # ✓\n```\n\nBài học rộng hơn: **`+=` trên phần tử của container bất biến luôn có rủi ro này.** Với `list` nằm trong `tuple`, dùng phương thức thay vì toán tử.\n\n### `+=` và `=+` trong hàm\n\nCùng cơ chế giải thích một hiện tượng khác:\n\n```python\ndef f(ds):\n    ds += [1]          # __iadd__ → sửa TẠI CHỖ → người gọi thấy\n\ndef g(ds):\n    ds = ds + [1]      # __add__ → object MỚI → người gọi KHÔNG thấy\n\na = []\nf(a); print(a)         # [1]\n\nb = []\ng(b); print(b)         # []\n```\n\nHai dòng trông gần giống nhau, hành vi khác hẳn. Với `int` thì cả hai đều không ảnh hưởng người gọi, vì `int` không có `__iadd__`.\n\nĐây là lý do thực dụng để **tránh `+=` trên tham số hàm** khi bạn không muốn sửa dữ liệu người gọi.\n\n### Sao chép phòng thủ — khi nào cần\n\n```python\nclass Kho:\n    def __init__(self, muc):\n        self._muc = list(muc)        # ⭐ sao chép — vì list mutable\n\nclass KhoBatBien:\n    def __init__(self, muc):\n        self._muc = tuple(muc)       # ⭐ đổi sang immutable, không cần lo nữa\n```\n\nBảng quyết định:\n\n```\nNhận vào là IMMUTABLE (tuple, str, int, frozenset)\n  → KHÔNG cần sao chép, giữ tham chiếu là an toàn\n\nNhận vào là MUTABLE (list, dict, set)\n  → PHẢI sao chép nếu bạn giữ nó lâu dài\n  → hoặc đổi sang dạng immutable\n```\n\nChi phí đo được:\n\n```python\nimport time\n\nds = list(range(1_000_000))\nt = time.perf_counter(); list(ds)\nprint(f\"sao chép list 1 triệu: {time.perf_counter()-t:.4f}s\")\n\nt_ = tuple(ds)\nt = time.perf_counter(); x = t_          # chỉ gán con trỏ\nprint(f\"gán tuple            : {time.perf_counter()-t:.9f}s\")\n```\n\nVới dữ liệu lớn và nhiều lần truyền qua lại, chi phí sao chép phòng thủ tích luỹ đáng kể — và dùng immutable loại bỏ nó hoàn toàn.\n\n### Immutable và đa luồng\n\nMột lợi ích nữa, quan trọng cho bài GIL sắp tới:\n\n```python\n# ✓ Bất biến — nhiều luồng đọc đồng thời, KHÔNG cần khoá\nCAU_HINH = (\"localhost\", 8000, True)\n\n# ⚠️ Mutable — nhiều luồng cùng sửa ⇒ cần khoá\nCAU_HINH_DIC = {\"host\": \"localhost\"}\nkhoa = threading.Lock()\n\nwith khoa:\n    CAU_HINH_DIC[\"host\"] = \"x\"\n```\n\nLý do: dữ liệu **không đổi** thì không có tình huống một luồng đọc giữa chừng lúc luồng khác đang ghi. Đây là nguyên tắc nền của lập trình đồng thời, và nó là hệ quả trực tiếp của tính bất biến.\n\nNối về bài **Deadlock, race condition** ở module 2: phần lớn race condition biến mất nếu dữ liệu chia sẻ là bất biến.\n\n### `frozen=True` và `__slots__` — bất biến ở class tự viết\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Diem:\n    x: float\n    y: float\n\nd = Diem(1, 2)\nd.x = 5              # FrozenInstanceError\nhash(d)              # ✓ hashable\n```\n\nCơ chế: `frozen=True` sinh ra `__setattr__` ném lỗi. Nó **không** khoá bộ nhớ — vẫn lách được bằng `object.__setattr__`:\n\n```python\nobject.__setattr__(d, \"x\", 99)      # ✓ chạy được\nprint(d.x)                           # 99\n```\n\nĐây lại là triết lý \"chúng ta đều là người lớn\" ở module 6: Python chặn lỗi vô tình, không chặn người cố ý.\n\nVà `frozen` chỉ nông một tầng, đúng như `tuple`:\n\n```python\n@dataclass(frozen=True)\nclass A:\n    ds: list\n\na = A([1])\na.ds.append(2)       # ✓ chạy được — list bên trong vẫn mutable\nhash(a)              # TypeError: unhashable type: 'list'\n```\n\nMuốn bất biến thật thì mọi trường cũng phải immutable — dùng `tuple` thay `list`, `frozenset` thay `set`.",

vi_du: "### Nhìn thấy `__defaults__`\n\n```python\ndef them(x, ds=[]):\n    ds.append(x)\n    return ds\n\nprint(f\"defaults: {them.__defaults__}\")\nprint(f\"id list : {id(them.__defaults__[0])}\")\n\nthem(1)\nprint(f\"sau lần 1: {them.__defaults__}\")\nthem(2)\nprint(f\"sau lần 2: {them.__defaults__}\")\nprint(f\"id vẫn   : {id(them.__defaults__[0])}\")     # ⭐ KHÔNG đổi\n\n# defaults: ([],)\n# id list : 140234...\n# sau lần 1: ([1],)\n# sau lần 2: ([1, 2],)\n# id vẫn   : 140234...   ← cùng một object từ đầu tới cuối\n```\n\nBa dòng `id` giống nhau là bằng chứng trực tiếp: không có object mới nào được tạo giữa các lần gọi.\n\nSửa được cả từ ngoài:\n\n```python\nthem.__defaults__[0].clear()\nprint(them(3))          # [3] — đã đặt lại\n\n# Hoặc thay hẳn giá trị mặc định\nthem.__defaults__ = ([\"khởi tạo\"],)\nprint(them(4))          # ['khởi tạo', 4]\n```\n\nKhông ai làm vậy trong code thật, nhưng nó chứng minh rõ: **giá trị mặc định là dữ liệu gắn vào object hàm**, không phải phép thuật gì.\n\n### Bốn biến thể của bẫy\n\n```python\nfrom datetime import datetime\nimport time, uuid\n\n# ① list — bẫy kinh điển\ndef f1(x, ds=[]):\n    ds.append(x); return ds\n\nprint(f1(1), f1(2), f1(3))        # [1,2,3] [1,2,3] [1,2,3]\n\n\n# ② datetime — mốc thời gian đóng băng\ndef f2(tin, luc=datetime.now()):\n    return f\"[{luc:%H:%M:%S}] {tin}\"\n\nprint(f2(\"a\")); time.sleep(2); print(f2(\"b\"))\n# Hai dòng CÙNG mốc thời gian\nprint(f2.__defaults__)             # (datetime(...),) — một object duy nhất\n\n\n# ③ uuid — cùng một mã mãi mãi\ndef f3(ma=uuid.uuid4()):\n    return str(ma)\n\nprint(f3() == f3())                # True — không phải mã mới\n\n\n# ④ instance class — dùng chung trạng thái\nclass BoDem:\n    def __init__(self): self.n = 0\n    def tang(self): self.n += 1; return self.n\n\ndef f4(bd=BoDem()):\n    return bd.tang()\n\nprint(f4(), f4(), f4())            # 1 2 3 — cùng một BoDem\n```\n\nBốn ví dụ, một nguyên nhân duy nhất: **biểu thức mặc định chạy một lần lúc `def` thực thi.**\n\nBản sửa cho cả bốn:\n\n```python\ndef f1(x, ds=None):\n    ds = [] if ds is None else ds\n    ds.append(x); return ds\n\ndef f2(tin, luc=None):\n    luc = luc or datetime.now()\n    return f\"[{luc:%H:%M:%S}] {tin}\"\n\ndef f3(ma=None):\n    return str(ma or uuid.uuid4())\n\ndef f4(bd=None):\n    bd = bd or BoDem()\n    return bd.tang()\n```\n\nLưu ý `or` chỉ dùng được khi giá trị hợp lệ không bao giờ \"giả\" — với số thì phải `is None`, vì `0` là giá trị hợp lệ.\n\n### Bộ dò bẫy mặc định\n\n```python\nimport inspect\n\nBAT_BIEN = (int, float, complex, bool, str, bytes, tuple, frozenset, type(None))\n\ndef soi_mac_dinh(*hams):\n    \"\"\"Cảnh báo hàm có giá trị mặc định nguy hiểm.\"\"\"\n    for f in hams:\n        canh_bao = []\n        ts = inspect.signature(f).parameters\n        for ten, p in ts.items():\n            if p.default is inspect.Parameter.empty:\n                continue\n            gt = p.default\n            if not isinstance(gt, BAT_BIEN):\n                canh_bao.append(f\"{ten}={type(gt).__name__} (mutable)\")\n            elif isinstance(gt, tuple) and any(\n                    not isinstance(x, BAT_BIEN) for x in gt):\n                canh_bao.append(f\"{ten}=tuple chứa mutable\")\n        if canh_bao:\n            print(f\"⚠️  {f.__name__}: {', '.join(canh_bao)}\")\n        else:\n            print(f\"✓  {f.__name__}\")\n\n\ndef a(x, ds=[]): ...\ndef b(x, d={}): ...\ndef c(x, t=(1, [2])): ...\ndef d(x, n=0, s=\"\", t=(1, 2), z=None): ...\n\nsoi_mac_dinh(a, b, c, d)\n# ⚠️  a: ds=list (mutable)\n# ⚠️  b: d=dict (mutable)\n# ⚠️  c: t=tuple chứa mutable\n# ✓  d\n```\n\nBộ dò này bắt được cả trường hợp `tuple` chứa `list` — dạng mà mắt thường dễ bỏ qua. Nó **không** bắt được `datetime.now()` (kết quả là object bất biến), nên vẫn cần đọc code.\n\nLinter chuyên dụng làm việc này tốt hơn (quy tắc `B006` đã nhắc ở bài PEP 8), nhưng viết tay một lần giúp hiểu rõ nguyên tắc.\n\n### Cùng cơ chế, dùng đúng chỗ\n\n```python\n# Bẫy lambda trong vòng lặp — module 5 đã nêu\nham = []\nfor i in range(3):\n    ham.append(lambda: i)\nprint([f() for f in ham])          # [2, 2, 2] — closure bắt BIẾN\n\n# Sửa bằng chính cơ chế __defaults__\nham = []\nfor i in range(3):\n    ham.append(lambda i=i: i)      # ⭐ đóng băng GIÁ TRỊ lúc def chạy\nprint([f() for f in ham])          # [0, 1, 2] ✓\n\nfor f in ham:\n    print(f.__defaults__)          # (0,) (1,) (2,)\n```\n\nMỗi `lambda` có `__defaults__` riêng, được tính tại thời điểm nó được tạo. Đây là **cùng một cơ chế** gây bẫy `ds=[]`, chỉ khác là ở đây nó đúng ý muốn.\n\nSo với closure thường:\n\n```\nclosure       bắt BIẾN — tra giá trị lúc GỌI\n__defaults__  bắt GIÁ TRỊ — chụp lúc ĐỊNH NGHĨA\n```\n\nMột câu này giải thích cả hai hiện tượng.\n\n### `+=` — hai hành vi\n\n```python\nimport dis\n\ndef voi_int():\n    x = 1\n    x += 1\n    return x\n\ndef voi_list():\n    ds = [1]\n    ds += [2]\n    return ds\n\n# Cả hai đều dùng lệnh INPLACE_ADD hoặc tương đương\ndis.dis(voi_int)\ndis.dis(voi_list)\n```\n\nCùng bytecode, khác kết quả — vì lệnh đó gọi `__iadd__` nếu kiểu có:\n\n```python\nprint(hasattr(int, \"__iadd__\"))       # False\nprint(hasattr(list, \"__iadd__\"))      # True\nprint(hasattr(tuple, \"__iadd__\"))     # False\nprint(hasattr(str, \"__iadd__\"))       # False\n\n# Kiểm chứng bằng id\nx = 1;   print(id(x)); x += 1;   print(id(x))    # KHÁC\nds = []; print(id(ds)); ds += [1]; print(id(ds))  # GIỐNG\n```\n\nHệ quả về hiệu năng — nối về bài chuỗi ở module 5:\n\n```python\nimport time\n\nn = 30_000\n\nt = time.perf_counter()\ns = \"\"\nfor i in range(n):\n    s += \"x\"                       # str KHÔNG có __iadd__ ⇒ tạo chuỗi mới mỗi vòng\nprint(f\"str +=  : {time.perf_counter()-t:.4f}s\")\n\nt = time.perf_counter()\nds = []\nfor i in range(n):\n    ds += [\"x\"]                    # list CÓ __iadd__ ⇒ sửa tại chỗ\nprint(f\"list += : {time.perf_counter()-t:.4f}s\")\n\nt = time.perf_counter()\ns = \"\".join(\"x\" for _ in range(n))\nprint(f\"join    : {time.perf_counter()-t:.4f}s\")\n```\n\nBây giờ câu \"nối chuỗi trong vòng lặp là O(n²)\" có lời giải thích đầy đủ: **`str` immutable nên mỗi `+=` sao chép lại toàn bộ chuỗi cũ.**\n\n### Bẫy `tuple` chứa `list`\n\n```python\nt = (1, 2, [3])\n\ntry:\n    t[2] += [4]\nexcept TypeError as e:\n    print(f\"lỗi: {e}\")\n\nprint(t)                # (1, 2, [3, 4])  ⭐ ĐÃ ĐỔI dù báo lỗi\n```\n\nTheo dõi từng bước:\n\n```python\nt = (1, 2, [3])\nds = t[2]\nprint(id(ds))\n\n# Bước ① — thành công\nds.__iadd__([4])\nprint(t, id(t[2]))      # (1, 2, [3, 4]) — cùng id\n\n# Bước ② — thất bại\ntry:\n    t[2] = ds\nexcept TypeError as e:\n    print(f\"bước 2 lỗi: {e}\")\n```\n\nCách tránh — dùng phương thức, không dùng toán tử:\n\n```python\nt = (1, 2, [3])\nt[2].extend([4])        # ✓ không có phép gán nào\nt[2].append(5)          # ✓\nprint(t)                # (1, 2, [3, 4, 5])\n```\n\n### Sao chép phòng thủ — cần và không cần\n\n```python\nfrom dataclasses import dataclass, field\n\n# ⚠️ Không sao chép — người gọi sửa được ruột\nclass KhoXau:\n    def __init__(self, muc):\n        self._muc = muc\n\ngoc = [1, 2]\nk = KhoXau(goc)\ngoc.append(3)\nprint(k._muc)           # [1, 2, 3] ← ruột bị đổi từ ngoài\n\n\n# ✓ Sao chép khi nhận mutable\nclass KhoTot:\n    def __init__(self, muc):\n        self._muc = list(muc)        # ⭐ bản sao riêng\n\ngoc = [1, 2]\nk = KhoTot(goc)\ngoc.append(3)\nprint(k._muc)           # [1, 2] ✓\n\n\n# ✓ Hoặc đổi sang immutable — không cần lo nữa\n@dataclass(frozen=True)\nclass KhoBatBien:\n    muc: tuple = ()\n\n    @classmethod\n    def tu_iterable(cls, it):\n        return cls(tuple(it))        # ⭐ chuyển một lần, sau đó an toàn\n\nk = KhoBatBien.tu_iterable([1, 2])\nprint(hash(k))          # ✓ hashable luôn\n```\n\nĐo chi phí sao chép phòng thủ:\n\n```python\nimport time\n\nds = list(range(2_000_000))\ntp = tuple(ds)\n\nt = time.perf_counter()\nfor _ in range(10):\n    ban_sao = list(ds)               # phải sao chép mỗi lần\nprint(f\"10 lần sao chép list : {time.perf_counter()-t:.4f}s\")\n\nt = time.perf_counter()\nfor _ in range(10):\n    tham_chieu = tp                  # ⭐ chỉ gán con trỏ\nprint(f\"10 lần gán tuple     : {time.perf_counter()-t:.6f}s\")\n```\n\nChênh lệch hàng nghìn lần. Với dữ liệu lớn truyền qua nhiều tầng, dùng immutable là tối ưu thật, không phải chuyện phong cách.\n\n### Object canh khi `None` là giá trị hợp lệ\n\n```python\n_CHUA_DAT = object()\n\nclass CauHinh:\n    def __init__(self):\n        self._d = {}\n\n    def dat(self, khoa, gt):\n        self._d[khoa] = gt\n\n    def lay(self, khoa, mac_dinh=_CHUA_DAT):\n        if khoa in self._d:\n            return self._d[khoa]\n        if mac_dinh is _CHUA_DAT:\n            raise KeyError(f\"chưa đặt {khoa!r} và không có mặc định\")\n        return mac_dinh\n\n\ncf = CauHinh()\ncf.dat(\"timeout\", None)             # ⭐ None là giá trị HỢP LỆ\n\nprint(cf.lay(\"timeout\"))            # None ✓ phân biệt được\nprint(cf.lay(\"khong_co\", None))     # None — mặc định do người gọi đưa\ncf.lay(\"khong_co\")                  # KeyError ✓\n```\n\n`object()` trần chỉ tốn 16 byte và **chắc chắn không `is` với bất cứ thứ gì khác** — đúng thứ cần cho vai trò canh. Thư viện chuẩn dùng đúng kỹ thuật này ở nhiều chỗ.\n\n### Bất biến cho đa luồng\n\n```python\nimport threading, time\n\n# ⚠️ Mutable dùng chung — cần khoá\nDEM_MUTABLE = {\"n\": 0}\nkhoa = threading.Lock()\n\ndef tang_co_khoa():\n    for _ in range(100_000):\n        with khoa:\n            DEM_MUTABLE[\"n\"] += 1\n\n# ✓ Bất biến — đọc thoải mái, không khoá\nCAU_HINH = (\"localhost\", 8000, True)\n\ndef doc_cau_hinh():\n    for _ in range(100_000):\n        host, cong, debug = CAU_HINH      # ⭐ an toàn, không khoá\n\nluong = [threading.Thread(target=tang_co_khoa) for _ in range(4)]\nfor t in luong: t.start()\nfor t in luong: t.join()\nprint(DEM_MUTABLE[\"n\"])                   # 400000 ✓ nhờ khoá\n```\n\nBài GIL ngay sau sẽ nói kỹ về đa luồng. Điểm cần nhớ ở đây: **dữ liệu bất biến không cần đồng bộ hoá**, và đó là một trong những lý do mạnh nhất để chọn nó.\n\n### Bất biến trong class tự viết\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass ViTri:\n    vi_do: float\n    kinh_do: float\n    the: tuple[str, ...] = ()        # ⭐ tuple, KHÔNG phải list\n\nv = ViTri(21.03, 105.85, (\"thủ đô\",))\n\n# Bất biến ở tầng thuộc tính\ntry:\n    v.vi_do = 0\nexcept Exception as e:\n    print(type(e).__name__)          # FrozenInstanceError\n\n# Nhưng lách được — Python không khoá bộ nhớ\nobject.__setattr__(v, \"vi_do\", 0)\nprint(v.vi_do)                       # 0\n\n# Hashable ⇒ dùng làm khoá\nkho = {ViTri(21.03, 105.85): \"Hà Nội\"}\nprint(kho[ViTri(21.03, 105.85)])     # Hà Nội\n\n\n# ❌ Trường là list ⇒ không bất biến thật\n@dataclass(frozen=True)\nclass ViTriXau:\n    the: list\n\nvx = ViTriXau([\"a\"])\nvx.the.append(\"b\")                   # ✓ chạy được\nprint(vx.the)                        # ['a', 'b']\ntry:\n    hash(vx)\nexcept TypeError as e:\n    print(f\"lỗi: {e}\")               # unhashable type: 'list'\n```\n\nHai kết luận: `frozen` chặn **gán thuộc tính**, không chặn sửa nội dung object bên trong; và muốn hashable thì mọi trường cũng phải hashable.\n\n### Bảng kiểm nhanh\n\n```python\ndef la_bat_bien(x) -> bool:\n    \"\"\"Kiểm tra thực nghiệm: object có hashable không.\"\"\"\n    try:\n        hash(x)\n        return True\n    except TypeError:\n        return False\n\nmau = [1, 1.5, \"a\", (1, 2), frozenset([1]), None, True,\n       [1], {1: 2}, {1, 2}, bytearray(b\"a\"), (1, [2])]\n\nfor x in mau:\n    print(f\"{type(x).__name__:<10} {repr(x)[:16]:<18} \"\n          f\"{'bất biến' if la_bat_bien(x) else 'THAY ĐỔI ĐƯỢC'}\")\n```\n\nPhép kiểm bằng `hash` gần đúng nhưng **không hoàn hảo**: một class tự viết mutable vẫn hashable theo mặc định (hash theo danh tính). Nó chỉ đáng tin với các kiểu dựng sẵn — và đó cũng là một điểm đáng nhớ về giới hạn của phép kiểm tự động.",

so_sanh: "### Module 5 và bài này\n\n| Câu hỏi | Module 5 | Bài này |\n|---|---|---|\n| `def f(ds=[])` sai ở đâu | \"tạo một lần, dùng chung\" | **object nằm trong `func.__defaults__`, sống bằng vòng đời hàm** |\n| Cách sửa | dùng `None` | **vì sao `None` là lựa chọn đúng: duy nhất, bất biến, không trùng giá trị hợp lệ** |\n| Biến thể nào của bẫy | `list`, `dict`, `datetime.now()` | **mọi biểu thức tính lúc `def` chạy — suy ra được** |\n| `+=` với list và int | khác nhau | **`__iadd__` có hay không** |\n\nBài này không thay module 5 — nó biến \"quy tắc phải nhớ\" thành \"hệ quả tự suy ra\".\n\n### Immutable và mutable ở tầng kiểu\n\n| | Immutable | Mutable |\n|---|---|---|\n| Có `__iadd__` | ✗ | **✓** (list) |\n| `x += y` đổi `id` | **✓** | ✗ |\n| Hashable | ✓ | ✗ (mặc định) |\n| Gộp/cache được | **✓** | ✗ |\n| Cần sao chép phòng thủ | ✗ | **✓** |\n| Cần khoá khi đa luồng | ✗ | **✓** |\n| Ví dụ | `int`, `str`, `tuple` | `list`, `dict`, `set` |\n\nBốn hàng cuối là bốn lợi ích cụ thể của immutable, và chúng đều bắt nguồn từ một tính chất: **giá trị không đổi thì chia sẻ được vô điều kiện.**\n\n### `a += b` — hai đường đi\n\n| | Kiểu có `__iadd__` | Kiểu không có |\n|---|---|---|\n| Gọi gì | `__iadd__` | `__add__` |\n| Tạo object mới | ✗ | **✓** |\n| `id` sau phép toán | **giữ nguyên** | đổi |\n| Người gọi hàm thấy | **✓** | ✗ |\n| Chi phí lặp n lần | O(n) | **O(n²)** với chuỗi |\n\nHàng cuối là lời giải thích cho quy tắc \"dùng `join` thay `+=`\" ở module 5.\n\n### Các dạng giá trị mặc định\n\n| Mặc định | An toàn | Vì sao |\n|---|---|---|\n| `0`, `\"\"`, `None`, `(1,2)` | **✓** | immutable |\n| `[]`, `{}`, `set()` | ✗ | mutable, dùng chung |\n| `datetime.now()` | ✗ | **giá trị đóng băng lúc import** |\n| `uuid4()` | ✗ | cùng một mã mãi mãi |\n| `MotClass()` | ✗ | một instance dùng chung |\n| `len(DS)` | ✗ | chụp giá trị lúc đó |\n| `(1, [2])` | ✗ | tuple **chứa** mutable |\n\nBa hàng giữa là những dạng mắt thường dễ bỏ qua vì kết quả trông có vẻ bất biến.\n\n### `None` và object canh\n\n| | `None` | `object()` canh |\n|---|---|---|\n| Bộ nhớ | 16 byte, **dùng chung** | 16 byte mỗi cái |\n| Người gọi truyền được | **✓** | ✗ (không lấy được) |\n| Phân biệt \"không truyền\" với `None` | **✗** | **✓** |\n| Dùng khi | `None` không phải giá trị hợp lệ | **`None` là giá trị hợp lệ** |\n\nObject canh là kỹ thuật thư viện chuẩn dùng nhiều, và nó chỉ cần thiết trong đúng một trường hợp ở hàng cuối.\n\n### Khi nào cần sao chép phòng thủ\n\n| Nhận vào | Sao chép? | Cách rẻ hơn |\n|---|---|---|\n| `tuple`, `str`, `int`, `frozenset` | **✗** | — |\n| `list`, `dict`, `set` | **✓ nếu giữ lâu dài** | đổi sang immutable |\n| Generator | ✓ (`list(...)`) | hoặc duyệt một lượt |\n| Chỉ đọc rồi bỏ | ✗ | — |\n\nHàng thứ hai có lựa chọn thứ ba đáng cân nhắc: **chuyển sang `tuple` một lần** thay vì sao chép ở mỗi lần truyền.\n\n### Bất biến và đồng thời\n\n| | Dữ liệu bất biến | Dữ liệu mutable |\n|---|---|---|\n| Nhiều luồng đọc | **✓ không khoá** | ✓ nếu không ai ghi |\n| Có luồng ghi | không xảy ra | **cần khoá** |\n| Race condition | **✗ không thể** | ✓ |\n| Chi phí đồng bộ | **0** | khoá, hàng đợi |\n\nBảng này là lý do các ngôn ngữ hướng đồng thời ưu tiên bất biến, và là nền cho bài GIL ngay sau.",

loi_thuong_gap: [
  "**Tưởng bẫy mặc định chỉ xảy ra với `list` và `dict`.** Mọi biểu thức tính lúc `def` chạy đều bị đóng băng — kể cả `datetime.now()`, `uuid4()`, hay một instance class.",
  "**Dùng `or` thay `is None` khi `0` hoặc `\"\"` là giá trị hợp lệ.** `ds = ds or []` thay mất một list rỗng người gọi cố ý truyền.",
  "**`t[0] += [x]` với `t` là `tuple`.** Vừa sửa được list vừa ném `TypeError`. Dùng `.extend()` hoặc `.append()`.",
  "**Dùng `+=` trên tham số hàm khi không muốn sửa dữ liệu người gọi.** Với `list` nó sửa tại chỗ; dùng `ds = ds + [...]` nếu muốn bản mới.",
  "**Nối chuỗi bằng `+=` trong vòng lặp.** `str` không có `__iadd__` nên mỗi vòng sao chép lại toàn bộ — O(n²).",
  "**Giữ tham chiếu tới `list` người gọi truyền vào.** Họ sửa list của họ là ruột object bạn đổi theo. Sao chép hoặc đổi sang `tuple`.",
  "**Sao chép phòng thủ dữ liệu đã bất biến.** `tuple(t)` trên một `tuple` là chi phí thừa.",
  "**Tưởng `frozen=True` khoá bộ nhớ.** Nó chỉ sinh `__setattr__` ném lỗi; `object.__setattr__` vẫn lách được.",
  "**Dùng `list` làm trường của `dataclass(frozen=True)`.** Nội dung vẫn sửa được và object mất tính hashable. Dùng `tuple`.",
  "**Dựa vào `hash()` để kiểm tra một object có bất biến không.** Class tự viết mutable vẫn hashable theo danh tính.",
  "**Chia sẻ `dict` cấu hình giữa nhiều luồng mà không khoá.** Đổi sang `tuple` hoặc `frozen dataclass` là hết cần khoá.",
  "**Sửa `func.__defaults__` trong code thật.** Nó chứng minh cơ chế rất tốt, nhưng là thứ không ai đọc code mong đợi."
],

tu_kiem_tra: [
  "\"Immutable\" là tính chất của ô nhớ hay của kiểu? Giải thích bằng `__iadd__`.",
  "Mô tả bốn bước Python làm khi thực thi câu lệnh `def`. Bước nào gây ra bẫy?",
  "`func.__defaults__` là gì? Chứng minh bẫy bằng nó.",
  "Nêu bốn biến thể của bẫy mặc định mà module 5 chưa nêu hết. Chúng có chung nguyên nhân gì?",
  "Vì sao Python thiết kế mặc định tính một lần? Nêu một lợi ích và một cái giá.",
  "Cùng cơ chế gây bẫy lại sửa được bẫy nào ở module 5? Giải thích.",
  "Nêu ba lý do `None` là lựa chọn đúng cho giá trị mặc định.",
  "Khi nào cần object canh thay vì `None`? Vì sao `object()` trần phù hợp?",
  "Mô tả hai bước của `a += b`. Vì sao `t[0] += [1]` vừa thành công vừa báo lỗi?",
  "Vì sao nối chuỗi bằng `+=` là O(n²) còn `list += ` thì không?",
  "Nêu ba lợi ích của immutable ở tầng bộ nhớ.",
  "Vì sao cache số nguyên nhỏ chỉ khả thi với kiểu bất biến?",
  "Khi nào cần sao chép phòng thủ, khi nào không? Lựa chọn thứ ba là gì?",
  "`frozen=True` chặn được gì và không chặn được gì? Cho ví dụ lách qua.",
  "Vì sao dữ liệu bất biến không cần khoá khi đa luồng?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *FAQ → Why are default values shared between objects?* — giải thích chính thức về cơ chế và lý do thiết kế.",
  "Tài liệu Python, mục *Data model* phần *Emulating numeric types* — định nghĩa `__add__`, `__iadd__` và thứ tự Python thử chúng.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về object mutable, bí danh, và bản sao phòng thủ.",
  "Sách **Effective Python** — Brett Slatkin. Mục về giá trị mặc định động và mẫu `None`.",
  "Tài liệu Python, module `dis` — xem bytecode của `+=` để thấy `INPLACE_ADD` và cách nó phân giải."
],

lien_he: [
  "**Tham chiếu vs giá trị** ngay trước: bảng tên, con trỏ, phần đầu object — bài này dựng hoàn toàn trên đó.",
  "**Shallow copy vs deep copy** ngay sau: khi nào cần sao chép là câu hỏi tiếp theo của bài này.",
  "**Garbage collection** trong module này: object mặc định không bao giờ về refcount 0 vì hàm luôn giữ nó.",
  "**GIL** trong module này: bất biến không cần khoá — nền cho phần bàn về đa luồng.",
  "**Hàm, tham số mặc định** ở module 5: bẫy và cách sửa; bài này là phần \"vì sao\".",
  "**Cú pháp, kiểu dữ liệu** ở module 5: ranh giới mutable/immutable, và `\"\".join` thay `+=`.",
  "**Magic method** ở module 6: `__iadd__`, `__add__`, `__hash__` là ba phương thức trung tâm của bài này.",
  "**Dataclass và NamedTuple** ở module 6: `frozen=True` và lý do trường phải là `tuple`.",
  "**Deadlock, race condition** ở module 2: phần lớn race condition biến mất nếu dữ liệu chia sẻ là bất biến.",
  "**Module, package, import** ở module 5: `def` là câu lệnh được thực thi lúc import — thời điểm mặc định bị đóng băng.",
  "**AI/ML** ở mảng 07: cấu hình thí nghiệm dùng `frozen dataclass` để chia sẻ an toàn và làm khoá theo dõi kết quả."
]

};
