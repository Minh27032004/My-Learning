window.LESSONS["07:ai-agents:framework-langgraph-claude-agent-sdk"] = {

tom_tat: [
  "LangGraph và Claude Agent SDK giải quyết hai lớp trừu tượng khác nhau: LangGraph nhấn mạnh workflow có state và transition; Claude Agent SDK cung cấp vòng lặp agent, tool và quyền quanh Claude.",
  "Chọn framework theo control flow, durability, permission model, observability và mức lock-in cần chấp nhận, không theo số dòng demo đầu tiên.",
  "State phải có schema, reducer và invariant. Checkpoint chỉ hữu ích khi đi cùng idempotency cho hiệu ứng phụ và chiến lược migration.",
  "Trong agent hoàn tiền, rule nghiệp vụ vẫn là hàm xác định; framework điều phối việc đọc, phê duyệt và tiếp tục sau ngắt, không thay thế guardrail.",
  "Đôi khi một vòng lặp Python nhỏ là lựa chọn tốt nhất. Framework đáng dùng khi nhu cầu branching, pause/resume, tracing hoặc nhiều tool đã vượt chi phí học và vận hành."
],

can_biet_truoc: [
  "State machine, nút, cạnh có điều kiện, reducer và checkpoint.",
  "Vòng lặp tool use: model tạo tool request, runtime thực thi, rồi đưa kết quả trở lại model.",
  "Async I/O, timeout, cancellation, retry và idempotency cho hành động có hiệu ứng phụ.",
  "Các bài MCP, hệ đa agent và guardrail trong cùng module, đặc biệt trust boundary và human approval."
],

dinh_nghia: "**LangGraph** là framework tổ chức ứng dụng agent như một đồ thị stateful: nút đọc state và phát update, cạnh quyết định thứ tự hoặc nhánh, còn runtime hỗ trợ thực thi và khả năng lưu tiến trình. Nó phù hợp khi developer muốn biểu diễn control flow một cách tường minh.\n\n**Claude Agent SDK** là bộ công cụ xây agent dùng Claude với vòng lặp, tool, context và cơ chế kiểm soát hành động. Nó phù hợp khi trọng tâm là cho Claude làm việc qua capability trong một harness có permission và lifecycle thay vì tự ghép từng message.\n\nHai tên có thể cùng xuất hiện trong một hệ, và API cụ thể thay đổi theo phiên bản. Bài này dạy mental model cùng tiêu chí kiến trúc; khi hiện thực phải đối chiếu tài liệu chính thức của phiên bản đã khóa.",

vi_sao_can: "Một demo agent thường chỉ cần `while`: gọi model, nếu có tool request thì chạy tool, nếu có answer thì dừng. Production nhanh chóng thêm branching, retry, approval kéo dài nhiều giờ, resume sau deploy, trace, cancellation và schema evolution. Code vòng lặp tự viết khi đó trở thành một workflow engine không tên.\n\nFramework đóng gói các mẫu lặp lại và tạo vocabulary chung. Với O-1042, ta muốn chạy hai lượt đọc, kiểm policy bằng code, dừng ở human approval rồi tiếp tục mà không gọi lại các bước đã xong. LangGraph giúp nhìn thấy các transition đó. Nếu muốn một agent Claude khám phá tool trong sandbox và permission callback quyết định từng hành động, Agent SDK đưa ra primitive gần vòng lặp hơn.\n\nĐổi lại, framework mang semantics riêng, dependency và lock-in. Checkpoint mặc định có thể lưu PII; reducer sai có thể nhân đôi message; nâng version có thể làm state cũ không đọc được. Vì vậy “framework làm hộ” không đồng nghĩa “hệ thống đúng”. Ta chỉ nên mua abstraction sau khi hiểu trách nhiệm còn lại.",

co_che: "### Tách ba lớp trước khi chọn thư viện\n\nMột agent production có ít nhất ba lớp. **Reasoning layer** chọn bước tiếp theo. **Workflow layer** giữ state, branching, retry và pause. **Execution layer** thực thi tool dưới quyền, timeout và audit. LangGraph nghiêng về workflow; Claude Agent SDK nghiêng về reasoning/execution quanh Claude. Guardrail nghiệp vụ cắt xuyên cả ba.\n\nNếu đội gọi mọi thứ là “agent framework”, yêu cầu dễ bị lẫn. Câu “cần human approval” là yêu cầu workflow và permission. Câu “cần model đọc policy” thuộc reasoning. Câu “không được hoàn quá tiền đã trả” là invariant domain, nên nằm trong code độc lập framework.\n\n### LangGraph: state là giao diện giữa các nút\n\nTrong mental model LangGraph, mỗi nút nhận state hiện tại và trả update. Runtime hợp nhất update theo reducer được khai báo. Cạnh bình thường nối trình tự; cạnh có điều kiện chọn nhánh dựa trên state. Đồ thị không nhất thiết là DAG vì agent loop có thể quay lại, nhưng vòng lặp phải có điều kiện dừng.\n\nState của hoàn tiền có thể gồm `order_id`, `order`, `policy`, `decision`, `approval`, `operation_id`, `errors`. Không nên nhét client kết nối hoặc secret vào state checkpoint. Dữ liệu lớn nên lưu ngoài và chỉ giữ reference có kiểm quyền.\n\n### Reducer quyết định ý nghĩa của cập nhật song song\n\nKhi hai nút cùng phát update, runtime cần biết ghi đè, nối danh sách hay hợp nhất theo khóa. Reducer “append messages” hợp với lịch sử hội thoại nhưng sai với `decision`: hai quyết định không được nối rồi chọn ngẫu nhiên. `errors` có thể cộng dồn; `order` phải single-assignment hoặc kiểm version.\n\nReducer cần associative nếu runtime có thể hợp nhất theo thứ tự khác. Với phép nối list, thứ tự song song có thể không ổn định; đừng dùng vị trí cuối làm nguồn chân lý. Gắn event ID và timestamp logic nếu thứ tự có ý nghĩa.\n\n### Routing bằng code hay bằng model\n\nNếu điều kiện đã biết, viết cạnh có điều kiện bằng code: `eligible == False` đi kết thúc; `requires_approval == True` đi nút duyệt. Dùng model router khi ý định ngôn ngữ mở quyết định worker nào phù hợp. Sau model router vẫn cần allowlist và fallback cho nhãn ngoài schema.\n\nMột anti-pattern là hỏi LLM “đơn có vượt số tiền đã trả không”. Phép so sánh integer có đáp án xác định, rẻ và audit được bằng code. Framework không buộc mọi nút phải là model call.\n\n### Checkpoint: tiếp tục state, không quay ngược thế giới\n\nCheckpoint ghi state tại transition để một thread có thể resume. Nó hữu ích khi người duyệt phản hồi sau nhiều giờ hoặc process restart. Nhưng checkpoint trước một tool call không biến tool đó thành transaction. Nếu Payment đã ghi rồi process chết trước checkpoint kế, resume có thể gọi lại.\n\nGiải pháp là outbox/idempotency: tạo `operation_id` ổn định trong state trước hành động, executor dùng unique constraint, rồi tra kết quả khi resume. Checkpoint và ledger giải hai vấn đề khác nhau; phải dùng cùng nhau.\n\n### Interrupt và human approval\n\nWorkflow cần biểu diễn `WAITING_APPROVAL` như state hợp lệ, không giữ thread hoặc connection mở. Tại điểm ngắt, lưu command canonical và hash. Khi người duyệt trả lời, runtime nạp đúng thread, kiểm principal và hash, ghi approval event rồi đi tiếp.\n\nApproval cũ phải hết hạn nếu order hoặc policy version đổi. Resume không chỉ đọc boolean `approved`; nó kiểm precondition đã dùng khi duyệt. Nếu không, một quyết định đúng hôm qua có thể thực thi trên state khác hôm nay.\n\n### Claude Agent SDK: harness quanh vòng lặp Claude\n\nMental model của Agent SDK là một agent Claude nhận instruction, quan sát environment qua tool, tiếp tục nhiều lượt và bị runtime kiểm quyền. SDK giúp developer không phải tự xử lý mọi chi tiết tool loop. Giá trị nổi bật khi agent làm công việc mở như duyệt codebase, thao tác file hoặc điều tra qua nhiều capability.\n\nTool vẫn phải có schema và mô tả trung thực. Permission callback hoặc hook nên xem tool, input canonical, context người dùng và risk tier. Trả allow cho mọi thứ để demo chạy đã loại bỏ lợi ích an toàn của harness.\n\n### MCP trong framework: adapter, không phải policy\n\nCả workflow lẫn agent harness có thể tiêu thụ MCP tools. Chuỗi trách nhiệm vẫn là: MCP server công bố capability; framework đưa capability vào runtime; model đề nghị; policy của ứng dụng duyệt; client gọi. Không được hiểu rằng tool “đến từ MCP” nên mặc nhiên an toàn.\n\nTrong O-1042, Commerce MCP tool có thể nằm trong node `load_order`; Payment MCP tool chỉ xuất hiện trong executor sau approval. Cấu trúc này giảm khả năng model gọi Payment sớm hơn việc chỉ nhắc trong system prompt.\n\n### Context engineering và state không phải một thứ\n\nWorkflow state là nguồn dữ liệu bền của chương trình. Model context là lát cắt được chọn cho một lượt suy luận. Đưa toàn bộ state và trace vào context làm token tăng, lộ PII và khiến model chú ý sai. Một hàm projection nên chọn field cần thiết theo node.\n\nVí dụ agent Chính sách chỉ cần trạng thái giao, ngày mua và lý do; nó không cần tên, địa chỉ hay toàn bộ approval history. Context nhỏ vừa giảm chi phí vừa là kiểm soát quyền đọc.\n\n### Streaming và backpressure\n\nFramework có thể stream token hoặc event để UI phản hồi sớm. Nhưng token “đang chuẩn bị hoàn tiền” trước guardrail có thể tạo kỳ vọng sai. Phân biệt event nội bộ, preview không cam kết và kết quả đã được policy chấp nhận.\n\nConsumer chậm cần buffer hữu hạn hoặc bỏ event không quan trọng; không để stream debug làm đầy memory. Cancellation từ người dùng phải lan đến model call và tool đọc, nhưng tool ghi đang commit cần semantics rõ: hủy chờ không đồng nghĩa đảo ngược hiệu ứng.\n\n### Observability theo transition\n\nTrace nên trả lời: node nào chạy, state version nào, model/tool nào, routing vì sao, latency, token, retry và policy decision. Gắn correlation ID xuyên MCP và ledger. Che payload nhạy cảm ở exporter chứ không hy vọng mọi node nhớ tự che.\n\nFramework trace dễ tạo cảm giác đã quan sát đủ, nhưng span xanh không chứng minh business invariant. Thêm assertion/metric như duplicate proposal, approval mismatch và unauthorized attempt.\n\n### Kiểm thử node, đường đi và resume\n\nUnit test từng node bằng state nhỏ và tool giả. Path test kiểm cạnh: đơn không tồn tại kết thúc; eligible đi approval; từ chối không gọi executor. Resume test tạo checkpoint ở mọi ranh giới rồi restart. Fault test làm tool timeout trước và sau hiệu ứng phụ.\n\nModel node cần golden constraints hơn golden wording. Assert schema, nguồn và decision, không khóa nguyên câu văn dễ đổi. Với routing xác suất, chạy nhiều seed/cấu hình trên eval set và báo tỷ lệ, không dựa một lần pass.\n\n### Phiên bản state và migration\n\nWorkflow sống lâu có checkpoint từ code cũ. Thêm field có default thường dễ; đổi nghĩa `amount` từ integer đồng sang decimal major unit có thể nguy hiểm. Ghi `schema_version`, viết migration thuần và test snapshot cũ. Không deploy node mới giả định field mà checkpoint cũ thiếu.\n\nTool schema cũng thay đổi. Pin phiên bản adapter hoặc xử lý capability negotiation; trace phải biết schema nào đã được dùng để giải thích lỗi sau này.\n\n### So chi phí bằng workload thật\n\nĐếm số dòng demo đánh lừa: framework có setup ban đầu nhưng giảm code pause/resume; vòng lặp tay ngắn lúc đầu nhưng nợ tính năng về sau. Benchmark latency phải tách model/tool khỏi overhead runtime, vì model call thường lớn hơn nhiều nhưng workflow throughput vẫn có thể nghẽn ở persistence.\n\nĐo developer cost bằng thời gian thêm một branch, viết test resume và điều tra incident, không chỉ thời gian hello-world. Lock-in thể hiện ở state schema, decorator, trace và deployment, không chỉ import.\n\n### Khi nào chọn vòng lặp Python nhỏ\n\nNếu agent có hai tool đọc, tối đa ba bước, không pause, không cần durability và một đội nhỏ sở hữu toàn code, vòng lặp rõ có thể thắng. Viết state dataclass, timeout, policy và tests trước. Đừng cài workflow engine để tránh viết năm mươi dòng dễ hiểu.\n\nKhi branching tăng, approval kéo dài, cần replay hoặc nhiều worker đồng thời, framework bắt đầu trả lại giá trị. Quyết định nên được xem lại khi workload đổi; không có lựa chọn vĩnh viễn.",

vi_du: "### Đồ thị hoàn tiền tối giản bằng Python\n\nVí dụ chạy bằng thư viện chuẩn để làm rõ semantics trước khi gắn API framework. Các node là hàm, router là code, checkpoint là dictionary. Nó không giả làm cú pháp LangGraph hay Claude Agent SDK nên không bị lỗi theo phiên bản.\n\n```python\nfrom dataclasses import dataclass, asdict\n\n@dataclass\nclass RefundState:\n    order_id: str\n    paid: int | None = None\n    eligible: bool | None = None\n    approved: bool | None = None\n    operation_id: str | None = None\n    status: str = \"START\"\n\nCHECKPOINTS, LEDGER = {}, {}\n\ndef save(thread_id, state):\n    CHECKPOINTS[thread_id] = asdict(state)\n\ndef load_order(s):\n    if s.order_id != \"O-1042\":\n        s.status = \"NOT_FOUND\"; return s\n    s.paid = 450_000; s.status = \"LOADED\"; return s\n\ndef apply_policy(s):\n    s.eligible = s.paid is not None and s.paid > 0\n    s.status = \"WAITING_APPROVAL\" if s.eligible else \"REJECTED\"\n    s.operation_id = f\"{s.order_id}:refund:1\" if s.eligible else None\n    return s\n\ndef execute(s):\n    if s.operation_id in LEDGER:\n        s.status = \"PROPOSED\"\n        return s\n    if s.status != \"APPROVED\" or not s.approved:\n        raise PermissionError(\"approval required\")\n    command = {\"order_id\": s.order_id, \"amount\": s.paid}\n    LEDGER.setdefault(s.operation_id, command)  # idempotent trong demo\n    s.status = \"PROPOSED\"; return s\n\nthread = \"chat-88\"\ns = apply_policy(load_order(RefundState(\"O-1042\")))\nsave(thread, s)\nprint(CHECKPOINTS[thread])\n\n# Một tiến trình khác resume sau khi người có quyền duyệt.\ns = RefundState(**CHECKPOINTS[thread])\ns.approved = True; s.status = \"APPROVED\"\nexecute(s); execute(s)\nassert len(LEDGER) == 1\nprint(s.status, LEDGER)\n```\n\n### Ánh xạ sang hai hướng framework\n\nTrong LangGraph, `load_order`, `apply_policy`, `execute` trở thành nodes; route từ policy chọn `WAITING_APPROVAL` hoặc `REJECTED`; checkpointer lưu theo thread. Trong Agent SDK, Claude có thể điều tra và tạo đề xuất, còn permission layer chặn tool executor cho tới khi approval artifact hợp lệ. Dù dùng hướng nào, `LEDGER.setdefault` trong demo phải được thay bằng transaction cùng unique constraint thật.\n\nCa biên cần test: order rỗng; paid bằng 0; checkpoint cũ thiếu `operation_id`; approval hết hạn; process chết sau ghi ledger; hai resume đồng thời; tool schema đổi. Framework không tự suy ra mong muốn của nghiệp vụ cho các ca này.",

so_sanh: "### Bảng quyết định kiến trúc\n\n| Tiêu chí | LangGraph | Claude Agent SDK | Vòng lặp tự viết |\n|---|---|---|---|\n| control flow tường minh | mạnh: node/cạnh/state | thiên về vòng lặp agent | hoàn toàn tự thiết kế |\n| pause/resume dài hạn | phù hợp qua persistence | cần ghép lifecycle phù hợp | phải tự xây |\n| gắn với model | có thể phối nhiều thành phần | tập trung Claude | tùy ý |\n| permission/tool harness | phải thiết kế/tích hợp | là mối quan tâm trung tâm | tự chịu trách nhiệm |\n| độ phức tạp ban đầu | trung bình | trung bình | thấp cho ca nhỏ |\n| lock-in | graph/state/runtime | agent loop và SDK semantics | code nội bộ, nhưng nợ bảo trì |\n\nBảng không tuyên bố framework nào “tốt hơn”. Workflow phê duyệt nhiều nhánh nghiêng về graph. Agent khám phá môi trường bằng Claude nghiêng về SDK. Một pipeline cố định nhỏ nghiêng về code thường. Hệ lớn có thể đặt agent SDK trong một node graph, nhưng chỉ nên ghép khi mỗi lớp giải một nhu cầu đo được.\n\n### Framework và workflow engine truyền thống\n\nWorkflow engine truyền thống mạnh ở job xác định, retry và durability; agent framework thêm reasoning xác suất và context. Nếu toàn quy trình là rule, dùng workflow engine hoặc queue có thể phù hợp hơn. Nếu có một bước ngôn ngữ mở giữa các bước xác định, cô lập đúng bước đó thay vì biến toàn pipeline thành agent.",

loi_thuong_gap: [
  "**Chọn theo demo ít dòng.** Đến lúc thêm approval và resume mới phát hiện abstraction lệch; lập bảng yêu cầu trước khi thử API.",
  "**Lưu toàn bộ object vào checkpoint.** Secret, connection và PII bị serialize; chỉ lưu state nghiệp vụ tối thiểu cùng reference bảo vệ.",
  "**Dùng reducer nối cho mọi field.** Decision và order bị nhân đôi khi chạy song song; khai semantics hợp nhất riêng từng field.",
  "**Tin checkpoint chống duplicate.** Crash sau tool ghi vẫn tạo lần gọi lại; dùng operation key và transaction tại executor.",
  "**Để model route cả điều kiện xác định.** Kết quả dao động cho phép so sánh đơn giản; dùng code cho invariant và branch biết trước.",
  "**Resume approval mà không kiểm version.** State thay đổi sau lúc duyệt; ràng buộc approval với command hash và precondition.",
  "**Nâng dependency mà không test checkpoint cũ.** Workflow đang chờ không đọc được; pin version, migration và fixture snapshot."
],

tu_kiem_tra: [
  "Vì sao checkpoint sau mỗi node vẫn không ngăn hoàn tiền đôi khi response từ Payment bị mất?",
  "Field nào trong RefundState nên dùng append reducer, field nào phải single writer, và tại sao?",
  "Khi nào model-based routing hợp lý hơn conditional edge bằng code? Cho một ví dụ của mỗi loại.",
  "Thiết kế projection context cho agent Chính sách sao cho không lộ địa chỉ khách nhưng vẫn đủ quyết định.",
  "Nếu approval tồn tại 24 giờ và policy đổi sau 2 giờ, resume cần kiểm những precondition nào?",
  "Hãy nêu một workload mà vòng lặp Python tự viết tốt hơn cả hai framework và chỉ báo nào khiến bạn xem xét chuyển đổi."
],

tai_nguyen: [
  "Tài liệu chính thức LangGraph — Concepts, Persistence và Human-in-the-loop.",
  "Tài liệu chính thức Claude Agent SDK — Overview, Tools và Permissions.",
  "Designing Data-Intensive Applications — Martin Kleppmann.",
  "Release It! — Michael T. Nygard."
],

lien_he: [
  "Mảng Đồ thị: nodes, conditional edges, cycle và reachability cung cấp ngôn ngữ chính xác để đọc workflow LangGraph.",
  "Mảng Backend: schema evolution, dependency pinning, async I/O và API contract quyết định framework có sống được qua nhiều release.",
  "Mảng Database: checkpoint, event log, outbox và unique constraint tạo durability thực thay vì chỉ lưu vài message.",
  "Mảng DevOps/SRE: tracing, canary, cancellation, resource limits và migration giúp vận hành agent qua deploy và incident."
]

};
