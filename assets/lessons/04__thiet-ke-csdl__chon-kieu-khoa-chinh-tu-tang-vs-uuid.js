window.LESSONS["04:thiet-ke-csdl:chon-kieu-khoa-chinh-tu-tang-vs-uuid"] = {

tom_tat: [
  "Khóa chính là định danh nội bộ ổn định của một hàng; nó không nên mang ý nghĩa nghiệp vụ dễ đổi như email, mã ghế hay số điện thoại.",
  "`bigint GENERATED ... AS IDENTITY` nhỏ, dễ đọc, được PostgreSQL sinh tập trung và thường tạo mẫu ghi tăng dần thân thiện với B-tree; đổi lại, client phải chờ database cấp ID và giá trị dễ đoán.",
  "UUID rộng 128 bit, có thể sinh trước ở nhiều máy mà gần như không cần phối hợp và phù hợp khi hợp nhất dữ liệu; đổi lại, khóa cùng mọi khóa ngoại và index liên quan đều lớn hơn `bigint` 64 bit.",
  "Không được đánh đồng “UUID” với một mẫu ghi duy nhất: UUID ngẫu nhiên phân tán vị trí chèn trong index, còn UUID có thứ tự thời gian cải thiện tính cục bộ nhưng làm lộ một phần thứ tự tạo.",
  "Quyết định phải theo workload: ai sinh ID, dữ liệu có hợp nhất ngoại tuyến không, ID có lộ ra API không, bao nhiêu index mang khóa đó, và `EXPLAIN (ANALYZE, BUFFERS)` nói gì trên dữ liệu đại diện."
],

can_biet_truoc: [
  "Khóa chính, khóa ngoại, khóa nghiệp vụ và unique index trong mô hình quan hệ.",
  "Cấu trúc B-tree ở mức nút, trang dữ liệu và ý nghĩa của tính cục bộ khi chèn.",
  "Kiểu số nguyên 64 bit, biểu diễn 128 bit của UUID và xác suất va chạm cơ bản.",
  "Câu lệnh `INSERT ... RETURNING`, transaction và cách đọc `EXPLAIN (ANALYZE, BUFFERS)` của PostgreSQL."
],

dinh_nghia: "**Khóa chính (primary key)** là cột hoặc nhóm cột định danh duy nhất và không `NULL` cho mỗi hàng. “Tự tăng hay UUID” thực chất là chọn không gian định danh và cơ chế cấp phát cho khóa thay thế: một số nguyên 64 bit lấy từ sequence của PostgreSQL, hoặc một giá trị UUID 128 bit có thể được sinh ở database hay phía client.\n\nTrong hệ thống đặt vé, `orders.id` chỉ trả lời “đây là đơn nào”; `customer_id`, `status` và mã thanh toán mới mang nghĩa nghiệp vụ. Dù chọn `bigint` hay `uuid`, ta vẫn cần các ràng buộc nghiệp vụ như một ghế duy nhất trong sự kiện. Khóa chính không tự ngăn hai đơn cùng giành một ghế, và UUID không phải cơ chế bảo mật hay thay thế authorization.",

vi_sao_can: "Kiểu khóa lan ra toàn schema. Nếu `orders.id` xuất hiện trong `tickets.order_id`, bảng audit, bảng thanh toán và nhiều secondary index, thêm 8 byte ở mỗi giá trị không chỉ tốn ở bảng cha. Nó nhân theo số hàng con, số index và dữ liệu truyền qua cache, WAL, replica, backup. Vì vậy chọn khóa là quyết định vật lý bên dưới một API tưởng như chỉ trả chuỗi.\n\nMặt khác, kiến trúc sinh dữ liệu quyết định khả năng phối hợp. Một ứng dụng duy nhất ghi vào PostgreSQL có thể dùng identity rất đơn giản. Ứng dụng di động tạo đơn khi mất mạng, nhiều region nhận ghi, hay pipeline nhập dữ liệu từ các nguồn độc lập cần ID trước khi kết nối; UUID cho mỗi tác nhân tự cấp định danh mà không thuê một dải sequence trung tâm.\n\nKhông có câu trả lời “UUID luôn tốt hơn vì phân tán” hoặc “số tự tăng luôn nhanh hơn”. ID tuần tự công khai có thể làm lộ tốc độ tăng đơn và cho phép dò tài nguyên nếu API kiểm quyền sai, nhưng UUID chỉ làm việc đoán khó hơn chứ không sửa lỗi kiểm quyền. UUID ngẫu nhiên làm index lớn và chèn rải rác; UUID có thứ tự giảm nhược điểm đó nhưng đổi lại tiết lộ thứ tự tương đối. Bài toán là chọn đánh đổi phù hợp, không chọn biểu tượng hiện đại hơn.",

co_che: "### Tách định danh nội bộ khỏi định danh nghiệp vụ\n\nEmail đổi được, mã ghế chỉ duy nhất bên trong một sự kiện, mã thanh toán do nhà cung cấp bên ngoài kiểm soát. Dùng chúng làm khóa chính khiến mọi khóa ngoại phải cập nhật khi nghiệp vụ đổi và buộc storage phụ thuộc định dạng bên ngoài. Khóa thay thế ổn định giúp quan hệ sống lâu hơn dữ liệu hiển thị.\n\nTrong ví dụ đặt vé, ta so hai bảng có cùng payload:\n\n```sql\nCREATE TABLE orders_seq (\n    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    customer_id bigint NOT NULL REFERENCES customers(id),\n    status text NOT NULL CHECK (status IN ('pending','paid','cancelled')),\n    total_cents bigint NOT NULL CHECK (total_cents >= 0),\n    created_at timestamptz NOT NULL DEFAULT clock_timestamp()\n);\n\nCREATE TABLE orders_uuid (\n    id uuid PRIMARY KEY,\n    customer_id bigint NOT NULL REFERENCES customers(id),\n    status text NOT NULL CHECK (status IN ('pending','paid','cancelled')),\n    total_cents bigint NOT NULL CHECK (total_cents >= 0),\n    created_at timestamptz NOT NULL DEFAULT clock_timestamp()\n);\n```\n\nVí dụ truyền UUID từ client để không phụ thuộc một hàm sinh UUID cụ thể của server. PostgreSQL kiểm cú pháp và tính duy nhất; client chịu trách nhiệm dùng bộ sinh ngẫu nhiên mật mã đúng chuẩn.\n\n### Identity vận hành bằng sequence, không phải phép đếm hàng\n\n`GENERATED ALWAYS AS IDENTITY` gắn cột với một sequence. Khi `INSERT` không truyền `id`, PostgreSQL lấy giá trị kế tiếp bằng `nextval`; thao tác này an toàn khi nhiều session gọi đồng thời vì sequence cấp các giá trị khác nhau. Nó không chạy `SELECT max(id)+1`, mẫu có race condition và phải quét hoặc khóa dữ liệu.\n\nSequence nằm ngoài cơ chế rollback thông thường. Nếu transaction lấy ID 42 rồi thất bại, lần chèn sau có thể là 43. Khoảng trống là hành vi đúng: mục tiêu của khóa là duy nhất, không phải liên tục. Đòi số hóa đơn không được hụt là yêu cầu pháp lý/nghiệp vụ khác, cần quy trình cấp số riêng; không nên ép khóa chính kiêm nhiệm.\n\n`GENERATED ALWAYS` ngăn ứng dụng vô tình ghi đè ID; khi nhập dữ liệu có chủ đích có thể dùng `OVERRIDING SYSTEM VALUE`. `BY DEFAULT` mềm hơn nhưng cũng dễ tạo giá trị va chạm với sequence nếu nhập ID lớn mà quên chỉnh sequence. Sau import, phải đối chiếu `max(id)` và trạng thái sequence thay vì đợi lỗi production.\n\n### Đường đi của một lần chèn số tăng dần trong B-tree\n\nPrimary key tạo unique B-tree index. Với ID tăng, khóa mới thường đi vào trang lá bên phải nhất. Các lần chèn gần nhau tái sử dụng một vùng trang đang nóng trong buffer cache; khi trang đầy, PostgreSQL tách trang. Tính cục bộ này thường giảm số trang index bị chạm so với khóa ngẫu nhiên.\n\nNhưng “tuần tự” không đồng nghĩa miễn phí. Nhiều writer có thể cùng tranh vùng cuối index; sequence cũng có cache và trạng thái cần cấp phát. Trên một PostgreSQL thông thường, đây thường là lựa chọn hiệu quả, nhưng ở tải ghi cực cao phải đo chứ không suy luận từ hình vẽ. Partitioning, số connection, WAL và disk có thể nghẽn trước khóa.\n\n### UUID 128 bit mua quyền sinh phân tán bằng dung lượng\n\nUUID có 128 bit, được PostgreSQL lưu cố định 16 byte; `bigint` là 8 byte. Chênh lệch thô 8 byte chưa bao gồm alignment, header tuple và overhead index, nên không thể lấy phép nhân đơn giản làm kích thước file chính xác. Tuy nhiên hướng tác động chắc chắn: mỗi entry khóa và mỗi khóa ngoại UUID mang payload rộng hơn.\n\nĐiểm mua được là không gian rất lớn. Một client có thể sinh ID trước khi gửi request, dùng cùng ID làm idempotency key, dựng quan hệ cha-con ngoại tuyến rồi đồng bộ sau. Hai cụm độc lập ít phải phân dải số khi hợp nhất. Điều này loại bỏ một round-trip chỉ để xin ID, nhưng request `INSERT ... RETURNING` vốn thường cần trả kết quả khác, nên đừng tuyên bố UUID luôn giảm latency nếu chưa đo luồng thật.\n\n### UUID ngẫu nhiên và UUID theo thời gian không giống nhau\n\nMột UUID ngẫu nhiên phân bố khóa trên toàn không gian B-tree. Mỗi insert có thể chạm một trang lá khác, làm nhiều trang bẩn, giảm tính cục bộ cache và tạo page split ở nhiều vị trí. Hậu quả rõ hơn khi index lớn hơn RAM và workload ghi nặng; với bảng nhỏ nằm trọn trong cache, khác biệt có thể không đáng kể.\n\nUUID có thứ tự thời gian sắp phần thời gian ở đoạn đầu để các giá trị mới gần nhau hơn. Nó giữ khả năng sinh phân tán trong khi làm hành vi index gần với khóa tăng dần. Đánh đổi là thứ tự tạo có thể suy ra và chất lượng phụ thuộc implementation bộ sinh. Không nên tự ghép timestamp, MAC address và random bằng code ứng dụng chưa được kiểm tra; dùng thư viện đáng tin và ghi rõ định dạng mà hệ thống chấp nhận.\n\n### Xác suất va chạm không thay thế unique constraint\n\nUUID ngẫu nhiên có không gian cực lớn nên xác suất hai bộ sinh tốt tạo cùng giá trị là rất thấp, nhưng “rất thấp” không phải guarantee của schema. Lỗi thư viện, seed ngẫu nhiên hỏng, replay message hoặc client cố tình gửi lại cùng ID đều có thể tạo trùng. Primary key vẫn phải kiểm duy nhất và ứng dụng phải phân biệt retry cùng một thao tác với xung đột khác dữ liệu.\n\nMột thiết kế idempotent có thể nhận `order_id` do client sinh. Khi retry, `INSERT ... ON CONFLICT (id) DO UPDATE` chỉ an toàn nếu kiểm payload cũ khớp payload mới; nếu không, cùng ID có thể che một lỗi nghiêm trọng. ID là chìa khóa đối chiếu, không tự quyết định hai yêu cầu có cùng ý nghĩa.\n\n### Khóa rộng lan truyền qua index phụ\n\nPostgreSQL heap table không tổ chức vật lý theo primary key như clustered index của một số hệ khác. Secondary index lưu khóa index và con trỏ tới tuple heap, không tự động chứa primary key theo đúng mô hình của mọi database. Dù vậy UUID vẫn lan qua các cột khóa ngoại và index dựng trên chính những cột ấy, ví dụ `tickets(order_id)`.\n\n```sql\nCREATE TABLE tickets_seq (\n    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    order_id bigint NOT NULL REFERENCES orders_seq(id),\n    event_id bigint NOT NULL,\n    seat_code text NOT NULL,\n    UNIQUE (event_id, seat_code)\n);\n\nCREATE INDEX tickets_seq_order_idx ON tickets_seq(order_id);\n\nCREATE TABLE tickets_uuid (\n    id uuid PRIMARY KEY,\n    order_id uuid NOT NULL REFERENCES orders_uuid(id),\n    event_id bigint NOT NULL,\n    seat_code text NOT NULL,\n    UNIQUE (event_id, seat_code)\n);\n\nCREATE INDEX tickets_uuid_order_idx ON tickets_uuid(order_id);\n```\n\nSo sánh phải bao gồm `pg_relation_size` của bảng, primary key index và index khóa ngoại sau khi nạp cùng payload. Chỉ nhìn kích thước bảng cha bỏ qua phần chi phí nhân lên quan trọng nhất.\n\n### Query plan: kiểu khóa không đổi phép toán logic\n\nTra cứu đúng một primary key thường dùng `Index Scan` ở cả hai phương án:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT * FROM orders_seq WHERE id = 1000;\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT * FROM orders_uuid\nWHERE id = '018f0f2e-7b5a-7cc0-9d11-2b9912468abc'::uuid;\n```\n\nPlan node có thể giống nhau nhưng số trang, độ sâu index và tỷ lệ cache hit khác khi dữ liệu lớn. Bảng nhỏ có thể bị planner chọn `Seq Scan`; ép planner tắt sequential scan để tạo hình đẹp không phải benchmark trung thực. Cần nạp dữ liệu đại diện, `ANALYZE`, chạy nhiều lượt có phân biệt cache nóng/lạnh, rồi báo `BUFFERS`, WAL và kích thước, không ghi một con số thời gian như chân lý phổ quát.\n\n### API công khai: khó đoán không phải là phân quyền\n\nURL `/orders/1042` cho người ngoài biết ID gần đó tồn tại và có thể hé lộ quy mô. UUID giảm khả năng liệt kê tùy tiện, hữu ích cho tính riêng tư bề mặt và URL khó va chạm. Tuy nhiên attacker có UUID lấy từ log, referrer hoặc tài khoản khác vẫn truy cập được nếu API không kiểm chủ sở hữu. Authorization phải dựa trên `customer_id`/principal trong mọi truy vấn.\n\nMột phương án lai là giữ `bigint` làm khóa chính nội bộ và thêm `public_id uuid UNIQUE NOT NULL` cho API. Nó giữ join nội bộ hẹp nhưng tốn thêm cột và unique index, đồng thời mọi mapping phải nhất quán. Chỉ dùng khi thật sự cần hai thuộc tính; nếu UUID làm khóa chính đã đáp ứng workload, hai ID tăng độ phức tạp không cần thiết.\n\n### Sharding, nhiều region và hợp nhất dữ liệu\n\nSequence cục bộ của hai shard đều có thể sinh ID 1. Có thể cấp dải, nhúng shard ID vào số, dùng sequence bước nhảy khác nhau hoặc dịch vụ cấp ID; mỗi cách đưa phối hợp và cấu trúc vào khóa. UUID cho các shard tự sinh và hợp nhất dễ hơn, nhưng không giải quyết xung đột nghiệp vụ: hai shard vẫn có thể bán cùng ghế nếu unique constraint chỉ cục bộ.\n\nĐây là ranh giới quan trọng. UUID giải bài toán **định danh hàng** phân tán; nó không tạo transaction toàn cục cho `UNIQUE (event_id, seat_code)`. Hệ thống đặt vé nhiều vùng phải chọn một vùng sở hữu mỗi sự kiện, consensus/lock phân tán, hoặc chấp nhận giữ chỗ tạm và đối soát. Đừng gán cho kiểu dữ liệu khả năng mà kiến trúc nhất quán mới cung cấp.\n\n### Migration giữa hai kiểu khóa là thay đổi diện rộng\n\nĐổi primary key sau khi có nhiều khóa ngoại không phải `ALTER COLUMN TYPE` đơn giản. Cần thêm cột mới, backfill, tạo unique index, thêm cột tương ứng vào mọi bảng con, đồng bộ ghi đôi trong giai đoạn chuyển tiếp, kiểm chứng mapping, chuyển foreign key và cuối cùng mới bỏ cột cũ. Mỗi bước có rủi ro lock và làm hai phiên bản ứng dụng không tương thích.\n\nVì chi phí này lớn, hãy chọn dựa trên kiến trúc dự kiến nhưng không “future-proof” vô hạn. Một ứng dụng CRUD tập trung chưa có kế hoạch ghi ngoại tuyến thường hưởng lợi từ identity đơn giản. Khi yêu cầu sinh ID ở edge là thật, UUID đáng giá ngay từ đầu.\n\n### Khi nào không nên dùng mỗi phương án\n\nKhông nên dùng identity làm định danh duy nhất xuyên các cụm sẽ hợp nhất độc lập nếu không có chiến lược namespace. Không nên lộ số tăng làm token truy cập hoặc coi sự khó đoán của nó là bảo mật. Ngược lại, không nên chọn UUID ngẫu nhiên chỉ vì “microservice hay dùng” khi toàn bộ ghi đi qua một database, storage/index là nút thắt và con người thường xuyên phải đọc ID trong log.\n\nKhông nên dùng khóa tự nhiên dài, thay đổi được chỉ để né cả hai lựa chọn. Và không nên tối ưu theo benchmark một bảng trống: tác động xuất hiện ở fan-out khóa ngoại, index vượt cache, nhịp ghi và quy trình vận hành.",

vi_du: "### Thí nghiệm PostgreSQL có thể chạy lại\n\nĐoạn dưới tạo hai bảng độc lập để quan sát semantics. Nó không ghi sẵn kết quả thời gian vì kết quả phụ thuộc máy; chính câu SQL in số đo thật của môi trường người học.\n\n```sql\nDROP TABLE IF EXISTS key_seq_demo, key_uuid_demo;\n\nCREATE TABLE key_seq_demo (\n    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    payload text NOT NULL\n);\n\nCREATE TABLE key_uuid_demo (\n    id uuid PRIMARY KEY,\n    payload text NOT NULL\n);\n\nINSERT INTO key_seq_demo(payload)\nSELECT md5(g::text) FROM generate_series(1, 100000) AS g;\n\n-- UUID được tạo tất định từ md5 chỉ để script tự chứa và tái lập.\n-- Đây KHÔNG phải cách sinh UUID production.\nINSERT INTO key_uuid_demo(id, payload)\nSELECT (substr(h,1,8)||'-'||substr(h,9,4)||'-4'||substr(h,14,3)||\n        '-a'||substr(h,18,3)||'-'||substr(h,21,12))::uuid,\n       h\nFROM (SELECT md5(g::text) AS h FROM generate_series(1,100000) AS g) s;\n\nANALYZE key_seq_demo;\nANALYZE key_uuid_demo;\n\nSELECT relname,\n       pg_size_pretty(pg_relation_size(oid)) AS table_or_index_size\nFROM pg_class\nWHERE relname IN ('key_seq_demo','key_seq_demo_pkey',\n                  'key_uuid_demo','key_uuid_demo_pkey')\nORDER BY relname;\n```\n\nScript dùng `md5` để tạo phân bố tái lập, không tuyên bố đó là UUID mật mã an toàn. Trong production, ID client phải đến từ CSPRNG của thư viện UUID chuẩn; ví dụ học tập cố ý tách phép đo storage khỏi lựa chọn extension PostgreSQL.\n\n### Kiểm tra sequence không rollback\n\n```sql\nCREATE TEMP TABLE gap_demo (\n    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    note text\n);\n\nINSERT INTO gap_demo(note) VALUES ('đầu tiên');\nBEGIN;\nINSERT INTO gap_demo(note) VALUES ('sẽ hủy') RETURNING id;\nROLLBACK;\nINSERT INTO gap_demo(note) VALUES ('sau rollback');\nSELECT * FROM gap_demo ORDER BY id;\n```\n\nKết quả có khoảng trống ở ID đã cấp rồi rollback. Đây là ca biên cần chấp nhận, không được “sửa” bằng `max(id)+1`, vì hai transaction có thể cùng tính một số và gây trùng.\n\n### So plan bằng khóa thật vừa nạp\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT payload FROM key_seq_demo WHERE id = 50000;\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT payload FROM key_uuid_demo\nWHERE id = (\n  SELECT id FROM key_uuid_demo WHERE payload = md5('50000') LIMIT 1\n);\n```\n\nSubquery thứ hai chỉ tìm UUID tương ứng cho demo; muốn đo riêng lookup primary key, hãy lấy ID trước trong `psql`, rồi chạy literal nhiều lần. Nếu gộp subquery vào phép đo, plan còn bao gồm tìm theo `payload` và không còn là so sánh công bằng.\n\n### Mẫu API sinh UUID trước và kiểm retry\n\n```sql\nCREATE TABLE api_orders (\n    id uuid PRIMARY KEY,\n    customer_id bigint NOT NULL REFERENCES customers(id),\n    request_hash text NOT NULL,\n    total_cents bigint NOT NULL CHECK (total_cents >= 0)\n);\n\nINSERT INTO api_orders(id, customer_id, request_hash, total_cents)\nVALUES ('018f0f2e-7b5a-7cc0-9d11-2b9912468abc', 1, 'sha256:abc', 250000)\nON CONFLICT (id) DO NOTHING\nRETURNING id;\n\nSELECT request_hash, total_cents\nFROM api_orders\nWHERE id = '018f0f2e-7b5a-7cc0-9d11-2b9912468abc';\n```\n\nRequest retry chỉ được coi là thành công cũ nếu hash/payload khớp. Nếu cùng UUID nhưng số tiền khác, phải báo xung đột thay vì lặng lẽ trả đơn cũ. Primary key cung cấp điểm gặp; semantics idempotency vẫn do nghiệp vụ xác minh.",

so_sanh: "### Bảng quyết định cho workload đặt vé\n\n| Trục | `bigint` identity | UUID ngẫu nhiên | UUID có thứ tự thời gian |\n|---|---|---|---|\n| Độ rộng giá trị | 8 byte | 16 byte | 16 byte |\n| Nơi sinh thuận tiện | PostgreSQL/sequence | client hoặc server | client/server với thư viện phù hợp |\n| Sinh khi ngoại tuyến | cần cơ chế cấp dải khác | tốt | tốt |\n| Mẫu chèn B-tree | thường tăng về phía phải | phân tán | gần theo thời gian |\n| Dễ đọc trong log | cao | thấp | thấp |\n| Dễ đoán thứ tự/quy mô | cao | thấp | có thể lộ thứ tự tương đối |\n| Hợp nhất nhiều nguồn | cần namespace hoặc remap | thuận lợi | thuận lợi |\n| Chi phí ở khóa ngoại | thấp hơn | cao hơn | cao hơn |\n| Guarantee chống truy cập trái phép | không có | không có | không có |\n\n### Phương án lai có đáng không?\n\n`id bigint PRIMARY KEY` cộng `public_id uuid UNIQUE` hữu ích khi join nội bộ rất lớn nhưng API cần định danh khó liệt kê hoặc dữ liệu đến từ đối tác. Nó cũng phải trả giá cho hai index, hai cột, mapping và khả năng developer dùng nhầm ID. Với hệ thống nhỏ, một UUID primary key hoặc một identity kèm authorization đúng thường đơn giản hơn. Chỉ thêm lớp public ID khi yêu cầu riêng tư/giao tiếp bên ngoài có bằng chứng, không phải như nghi thức.",

loi_thuong_gap: [
  "**Dùng `SELECT max(id)+1` để tự tăng.** Hai transaction có thể nhận cùng số; nhận biết bằng lỗi trùng ngẫu nhiên dưới tải, sửa bằng identity/sequence.",
  "**Đòi ID tự tăng phải liên tục.** Rollback và sequence cache tạo khoảng trống bình thường; nếu cần số chứng từ liên tục, tách thành quy trình nghiệp vụ riêng.",
  "**Gọi UUID là bảo mật.** ID khó đoán không thay authorization; thử truy cập UUID của người dùng khác để phát hiện lỗi kiểm quyền đối tượng.",
  "**So dung lượng chỉ ở bảng cha.** Khóa ngoại và index trên khóa ngoại mới nhân chi phí; đo toàn bộ bảng `tickets`, audit và index liên quan.",
  "**Cho rằng mọi UUID chèn ngẫu nhiên.** Xem version/thuật toán bộ sinh và phân bố byte; UUID theo thời gian có hành vi B-tree khác UUID ngẫu nhiên.",
  "**Dùng UUID tự chế từ timestamp hoặc hash ngắn.** Dấu hiệu là code nối chuỗi/bit thủ công và không phân tích entropy; dùng thư viện chuẩn, vẫn giữ primary key.",
  "**Import ID identity nhưng quên đồng bộ sequence.** Lần chèn sau va chạm giá trị đã có; sau migration kiểm `max(id)` và giá trị sequence trước khi mở ghi.",
  "**Tin benchmark nhỏ nằm trọn trong RAM.** Chênh lệch biến mất hoặc đảo chiều; đo khi index đạt kích thước đại diện và đọc cả `BUFFERS`, WAL, p95/p99."
],

tu_kiem_tra: [
  "Vì sao sequence có thể bỏ số sau `ROLLBACK`, và tại sao cố lấp khoảng trống bằng `max(id)+1` làm giảm tính đúng?",
  "Hãy liệt kê mọi nơi `orders.id` lan tới trong hệ thống đặt vé; nơi nào khiến chênh lệch 8 byte bị nhân lớn nhất?",
  "UUID giúp hợp nhất đơn từ hai region nhưng vì sao không ngăn hai region bán cùng ghế `A-01`?",
  "Một plan `Index Scan` ở cả `bigint` và UUID có chứng minh hiệu năng bằng nhau không? Cần thêm số đo nào?",
  "Khi nào `bigint` nội bộ cộng `public_id uuid` hợp lý, và chi phí vận hành nào khiến phương án này không mặc định tốt?",
  "Thiết kế lộ trình đổi bảng cha cùng ba bảng con từ `bigint` sang UUID mà hai phiên bản ứng dụng vẫn chạy trong thời gian deploy.",
  "Client retry cùng UUID nhưng payload khác. Vì sao `ON CONFLICT DO NOTHING` đơn độc che lỗi, và bạn sẽ kiểm gì?"
],

tai_nguyen: [
  "Tài liệu PostgreSQL — các mục Numeric Types, UUID Type, Identity Columns, Sequence Manipulation Functions và Indexes.",
  "Sách **Designing Data-Intensive Applications** — Martin Kleppmann, phần định danh, phân vùng và hệ thống phân tán.",
  "Sách **Database Internals** — Alex Petrov, các chương B-tree, storage engine và transaction processing.",
  "Sách **SQL Antipatterns** — Bill Karwin, phần chọn khóa chính và mô hình khóa thay thế."
],

lien_he: [
  "Nối sang mảng **Hệ phân tán**: UUID giảm phối hợp khi cấp định danh, nhưng consensus hoặc ownership vẫn cần cho bất biến bán ghế duy nhất.",
  "Nối sang mảng **Backend/API**: `INSERT ... RETURNING` phù hợp identity, còn ID do client sinh hỗ trợ idempotency; cả hai đều phải kiểm authorization theo principal.",
  "Nối sang mảng **Cấu trúc dữ liệu và giải thuật**: mẫu khóa quyết định tính cục bộ của B-tree, số lần chạm trang và hiệu quả cache chứ không chỉ độ phức tạp O(log n).",
  "Nối sang mảng **DevOps/Vận hành**: đổi kiểu khóa là migration nhiều pha cần backfill, dual-write, validation, quan sát lock và kế hoạch quay lui."
]

};
