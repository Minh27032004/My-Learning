window.LESSONS["02:sap-xep:heap-sort"] = {

tom_tat: [
  "**Heap Sort trả lời đúng câu hỏi để ngỏ ở bài Quick Sort: có thuật toán nào vừa ĐẢM BẢO O(n log n) như Merge Sort, VỪA in-place O(1) bộ nhớ phụ như Quick Sort không?** Câu trả lời là CÓ — bằng cách dùng lại CHÍNH cấu trúc **heap** đã cài đặt đầy đủ ở module Cây (bài Heap và Priority Queue), biến thao tác \"lấy phần tử lớn nhất lặp lại\" thành một thuật toán sắp xếp.",
  "**Cơ chế: xây MAX-HEAP từ mảng gốc trong O(n) (đã chứng minh ở bài Heap), rồi LẶP LẠI n lần: đổi chỗ GỐC (luôn là phần tử lớn nhất) với phần tử CUỐI heap hiện tại, thu nhỏ heap đi 1, rồi sift-down lại gốc.** Mỗi lần lặp \"chốt\" đúng MỘT phần tử vào vị trí cuối cùng của nó — không cần mảng phụ nào khác ngoài mảng gốc.",
  "**O(n log n) ở CẢ BA trường hợp tốt/trung bình/xấu nhất — ĐẢM BẢO tuyệt đối như Merge Sort, khác Quick Sort.** Đã đo thật: dữ liệu ngẫu nhiên, đã sắp, và sắp ngược đều cho số phép so sánh XẤP XỈ NHAU (79-85 phép so sánh với n=16) — không có \"ca xấu nhất kích hoạt bởi thứ tự dữ liệu\" như Quick Sort.",
  "**Heap Sort KHÔNG ổn định** — giống Selection Sort và Quick Sort — vì phép đổi chỗ gốc-với-cuối và sift-down có thể nhảy qua các phần tử bằng nhau. Đã chứng minh bằng ví dụ chạy thật, đảo cả HAI cặp giá trị bằng nhau trong dữ liệu ví dụ.",
  "**Đánh đổi thực tế: hằng số ẩn CAO HƠN Quick Sort trung bình** (thực đo ở n=1024: Heap ~17.000-18.000 so sánh so Quick trung bình ~15.000) — vì thao tác sift-down phải \"nhảy\" theo cấu trúc cây trong mảng (chỉ số 2i+1, 2i+2), kém thân thiện với cache CPU hơn quét tuần tự liên tiếp của Quick Sort. Đây là lý do Heap Sort ít được dùng làm thuật toán CHÍNH trong thư viện chuẩn, dù có đảm bảo tốt hơn."
],

can_biet_truoc: [
  "**Bài Heap và Priority Queue, module Cây** — BẮT BUỘC đã hiểu `sift_down`, cách xây heap từ mảng trong O(n), và biểu diễn heap bằng mảng phẳng (công thức chỉ số `2i+1`, `2i+2`, `(i-1)//2`). Bài này KHÔNG giảng lại các khái niệm đó, chỉ TÁI SỬ DỤNG trực tiếp.",
  "Bài **Merge Sort** và **Quick Sort** — hai bài ngay trước — để hiểu vì sao Heap Sort được trình bày như câu trả lời cho \"đảm bảo + in-place cùng lúc\", điều CHƯA thuật toán nào trước đó đạt được đồng thời.",
  "Khái niệm **max-heap** (cha luôn LỚN HƠN HOẶC BẰNG con) — ngược với min-heap thường dùng cho priority queue lấy nhỏ nhất. Bài Heap module Cây có cài cả hai, bài này CHỌN max-heap vì sắp tăng dần cần lấy LỚN NHẤT ra trước để đặt vào CUỐI mảng."
],

dinh_nghia: "**Heap Sort** là thuật toán sắp xếp dựa trên quan sát: nếu có một **max-heap** (bài Heap, module Cây — cha luôn ≥ mọi con), phần tử LỚN NHẤT LUÔN nằm ở GỐC, lấy ra được ngay O(1) (đọc) / O(log n) (lấy ra và khôi phục bất biến). Sắp xếp tăng dần chỉ đơn giản là: **lặp lại việc lấy phần tử lớn nhất ra khỏi heap, đặt vào cuối mảng, cho tới khi heap rỗng.**\n\n```\nHAI GIAI ĐOẠN CỦA HEAP SORT:\n\nGIAI ĐOẠN 1 — XÂY MAX-HEAP (O(n), đã chứng minh ở bài Heap module Cây):\n  biến TOÀN BỘ mảng gốc thành một max-heap hợp lệ, TẠI CHỖ.\n\nGIAI ĐOẠN 2 — RÚT LẶP LẠI (n lần, mỗi lần O(log n)):\n  Lặp n-1 lần, mỗi lần:\n    a. Đổi chỗ GỐC (phần tử lớn nhất hiện tại) với phần tử CUỐI\n       CÙNG của heap (chưa bị \"chốt\" từ trước).\n    b. THU NHỎ heap đi 1 (phần tử vừa đổi ra không còn thuộc heap\n       nữa — nó đã ở ĐÚNG vị trí cuối cùng của mảng đã sắp).\n    c. SIFT-DOWN gốc mới (vừa đổi vào) để khôi phục bất biến heap\n       trên phần heap ĐÃ THU NHỎ.\n```\n\nĐiểm mấu chốt cần thấy: **KHÔNG cần mảng phụ nào** — \"heap\" và \"phần đã sắp\" cùng tồn tại trên MỘT mảng duy nhất, chỉ khác nhau ở RANH GIỚI (biến `kich_thuoc` giảm dần mỗi vòng lặp). Đây chính là lý do Heap Sort in-place O(1) bộ nhớ phụ, khác Merge Sort (O(n) mảng phụ) nhưng giống Quick Sort (chỉ khác: Quick Sort cần O(log n) cho ngăn xếp đệ quy, còn Heap Sort có thể viết HOÀN TOÀN không đệ quy — cả hai giai đoạn đều là VÒNG LẶP).",

vi_sao_can: "Hai bài trước đã đặt ra một thế lưỡng nan chưa được giải quyết:\n\n```\nMERGE SORT:  ✓ đảm bảo O(n log n)     ✗ tốn O(n) bộ nhớ phụ\nQUICK SORT:  ✗ không đảm bảo (O(n²)   ✓ chỉ O(log n) bộ nhớ phụ\n             xấu nhất)\n```\n\nHeap Sort giải quyết thế lưỡng nan này bằng cách khai thác một cấu trúc đã học TỪ TRƯỚC (heap, module Cây) chứ không phải phát minh kỹ thuật chia-để-trị mới. Đây là lý do bài này được đặt SAU Merge/Quick Sort chứ không phải ngay sau module Cây: chỉ khi đã thấy rõ HAI đánh đổi đối lập (đảm bảo-nhưng-tốn-bộ-nhớ so với nhanh-nhưng-không-đảm bảo), giá trị của \"vừa đảm bảo vừa in-place\" mới thật sự nổi bật.\n\n**Vì sao vẫn cần Quick Sort nếu Heap Sort có vẻ \"tốt hơn cả hai mặt\"?** Đây là câu hỏi quan trọng cần trả lời rõ, không để người học nghĩ Heap Sort là \"thuật toán thắng tuyệt đối\". Thực đo ở `vi_du` cho thấy Heap Sort có HẰNG SỐ ẨN cao hơn — nguyên nhân sâu xa: thao tác sift-down truy cập bộ nhớ theo chỉ số `2i+1`, `2i+2` — các vị trí này CÁCH XA nhau trong bộ nhớ khi i lớn (không liên tục như quét tuần tự của Quick Sort), khiến CPU cache miss nhiều hơn. Đây là một bài học quan trọng: **độ phức tạp Big-O không phải toàn bộ câu chuyện** — hai thuật toán CÙNG BẬC có thể chênh lệch tốc độ THỰC nhiều lần vì cách chúng truy cập bộ nhớ, một chủ đề đã gợi mở ở module 1 (bài CPU/RAM/Cache) nhưng ít khi thấy RÕ bằng số đo cụ thể như ở đây.\n\nCuối cùng, đây là dịp để THẤY GIÁ TRỊ THỰC của việc học heap kỹ ở module Cây: không chỉ để cài đặt priority queue, mà còn làm NỀN TẢNG cho một thuật toán sắp xếp hoàn chỉnh — một ví dụ cụ thể cho nguyên lý \"một cấu trúc dữ liệu được thiết kế tốt thường giải quyết được nhiều bài toán tưởng chừng không liên quan\".",

co_che: "### Dùng LẠI sift_down từ bài Heap — chỉ đổi hướng so sánh sang MAX-heap\n\n```python\ndef sift_down(a, i, kich_thuoc):\n    while True:\n        trai, phai = 2 * i + 1, 2 * i + 2\n        lon_nhat = i\n        if trai < kich_thuoc and a[trai] > a[lon_nhat]:   # ⭐ MAX-heap: tìm con LỚN\n            lon_nhat = trai                                #   HƠN (khác min-heap ở\n        if phai < kich_thuoc and a[phai] > a[lon_nhat]:    #   bài Heap module Cây,\n            lon_nhat = phai                                 #   nơi tìm con NHỎ hơn)\n        if lon_nhat == i:\n            break\n        a[i], a[lon_nhat] = a[lon_nhat], a[i]\n        i = lon_nhat\n```\n\n```\n⭐ THAM SỐ kich_thuoc (KHÁC bài Heap gốc, nơi luôn dùng len(a)): ở Heap\n  Sort, \"heap\" và \"phần đã sắp\" SỐNG CHUNG một mảng — kich_thuoc là\n  RANH GIỚI di động, giảm dần mỗi vòng lặp giai đoạn 2. sift_down\n  KHÔNG BAO GIỜ được phép động vào phần mảng từ kich_thuoc trở đi\n  (đó là phần ĐÃ SẮP XONG, không còn là heap nữa).\n```\n\n### Giai đoạn 1 — xây max-heap trong O(n) (chứng minh Y HỆT bài Heap module Cây)\n\n```python\ndef xay_max_heap(a):\n    n = len(a)\n    for i in range(n // 2 - 1, -1, -1):   # ⭐ từ nút TRONG cuối cùng, đi NGƯỢC lên\n        sift_down(a, i, n)\n```\n\n```\n⭐ CHỨNG MINH O(n) đã làm đầy đủ ở bài Heap (module Cây) — không lặp\n  lại ở đây. Điểm cần nhớ: phần lớn nút GẦN LÁ (sift-down rẻ), thiểu\n  số nút GẦN GỐC (sift-down đắt) — tổng vẫn là O(n), không phải\n  O(n log n) như trực giác \"n lần × O(log n)\" sai lầm.\n```\n\n### Giai đoạn 2 — rút lặp lại, mỗi lần \"chốt\" một phần tử\n\n```python\ndef heap_sort(a):\n    a = a[:]\n    n = len(a)\n    xay_max_heap(a)                        # ⭐ Giai đoạn 1: O(n)\n    for cuoi in range(n - 1, 0, -1):       # ⭐ Giai đoạn 2: n-1 lần\n        a[0], a[cuoi] = a[cuoi], a[0]       #   đổi GỐC (lớn nhất) ra vị trí CUỐI\n        sift_down(a, 0, cuoi)               #   khôi phục heap trên [0, cuoi)\n    return a\n```\n\n```\n⭐ VÌ SAO ĐỔI CHỖ a[0] VÀ a[cuoi] LUÔN ĐÚNG: a[0] là GỐC của max-heap\n  hiện tại — theo bất biến heap, đây LUÔN là phần tử LỚN NHẤT trong\n  toàn bộ heap [0, cuoi]. Đưa nó ra vị trí `cuoi` nghĩa là đặt nó vào\n  ĐÚNG vị trí cuối cùng của nó trong mảng ĐÃ SẮP TĂNG DẦN — không cần\n  di chuyển nó thêm lần nào nữa (giống chốt của Quick Sort \"dừng hẳn\").\n\n⭐ VÌ SAO SIFT-DOWN CHỈ CẦN GỌI Ở GỐC, KHÔNG PHẢI XÂY LẠI TOÀN BỘ HEAP:\n  sau khi đổi chỗ, CHỈ CÓ gốc (a[0], vừa nhận giá trị từ a[cuoi] cũ)\n  có thể vi phạm bất biến — mọi nút khác VẪN giữ đúng quan hệ cha-con\n  từ trước (không hề bị động tới). Một lần sift-down từ gốc đủ để\n  khôi phục toàn bộ heap — O(log n), không phải O(n).\n\n  TỔNG CHI PHÍ giai đoạn 2: (n-1) lần × O(log n) mỗi lần = O(n log n).\n\nTỔNG CHI PHÍ TOÀN BỘ HEAP SORT:  O(n)  [giai đoạn 1]  +  O(n log n)\n  [giai đoạn 2]  =  O(n log n)  (số hạng O(n) bị \"nuốt\" bởi O(n log n)\n  lớn hơn — quy tắc cộng độ phức tạp đã học ở module 1).\n```\n\n### Vì sao O(n log n) ở CẢ BA trường hợp — không có \"ca xấu nhất do thứ tự dữ liệu\"\n\n```\nKhác Quick Sort (nơi thứ tự dữ liệu ĐẦU VÀO quyết định chốt rơi vào\nđâu, gây ra khoảng cách xa giữa trung bình và xấu nhất): ở Heap Sort,\nSỐ LẦN LẶP giai đoạn 2 LUÔN ĐÚNG n-1 (không phụ thuộc dữ liệu), và\nMỖI lần sift-down LUÔN đi tối đa ⌊log₂ kich_thuoc⌋ bước — vì heap\nLUÔN hoàn chỉnh (complete), không có \"suy biến hình dạng\" kiểu BST\nlệch hay Quick Sort chốt tệ.\n\n⇒ Đây là lý do Heap Sort ĐẢM BẢO O(n log n), không dao động theo thứ\n  tự dữ liệu đầu vào — điểm đối lập trực tiếp với Quick Sort đã học.\n```",

vi_du: "### Chạy trên đúng dữ liệu ví dụ của module — so với cả 4 thuật toán trước\n\n```python\ndef sift_down(a, i, kich_thuoc, dem):\n    while True:\n        trai, phai = 2 * i + 1, 2 * i + 2\n        lon_nhat = i\n        if trai < kich_thuoc:\n            dem[\"so_sanh\"] += 1\n            if a[trai] > a[lon_nhat]:\n                lon_nhat = trai\n        if phai < kich_thuoc:\n            dem[\"so_sanh\"] += 1\n            if a[phai] > a[lon_nhat]:\n                lon_nhat = phai\n        if lon_nhat == i:\n            break\n        a[i], a[lon_nhat] = a[lon_nhat], a[i]\n        dem[\"hoan_doi\"] += 1\n        i = lon_nhat\n\ndef heap_sort(a, dem=None):\n    if dem is None:\n        dem = {\"so_sanh\": 0, \"hoan_doi\": 0}\n    a = a[:]\n    n = len(a)\n    for i in range(n // 2 - 1, -1, -1):\n        sift_down(a, i, n, dem)\n    for cuoi in range(n - 1, 0, -1):\n        a[0], a[cuoi] = a[cuoi], a[0]\n        dem[\"hoan_doi\"] += 1\n        sift_down(a, 0, cuoi, dem)\n    return a, dem\n\n\ndiem = [72, 45, 90, 33, 15, 60, 45, 90]\nkq, dem = heap_sort(diem)\nprint(\"KQ:\", kq, \"so_sanh:\", dem[\"so_sanh\"], \"hoan_doi:\", dem[\"hoan_doi\"])\n```\n\n```\nKẾT QUẢ (chạy thật):\n\nKQ: [15, 33, 45, 45, 60, 72, 90, 90]  so_sanh=26  hoan_doi=17\n\n⭐ SO VỚI CÁC BÀI TRƯỚC (n=8): Bubble=25, Selection=28, Insertion=17,\n  Merge=17, Quick=18, Heap=26 — ở n NHỎ, Heap Sort KHÔNG thắng về số\n  so sánh (nhiều hơn cả Bubble!) — hằng số ẩn của việc DUY TRÌ cấu\n  trúc heap thể hiện rõ ngay ở quy mô nhỏ. Ưu thế O(n log n) chỉ lộ\n  rõ khi n đủ lớn — bảng tăng trưởng dưới đây xác nhận.\n```\n\n### Trace từng bước — thấy \"heap co dần, phần đã sắp lớn dần\" trên CÙNG một mảng\n\n```python\ndef heap_sort_trace(a):\n    a = a[:]\n    n = len(a)\n    print(\"Mảng gốc:\", a)\n    for i in range(n // 2 - 1, -1, -1):\n        sift_down(a, i, n, {\"so_sanh\": 0, \"hoan_doi\": 0})\n    print(\"Sau khi xây max-heap:\", a)\n    for cuoi in range(n - 1, 0, -1):\n        a[0], a[cuoi] = a[cuoi], a[0]\n        sift_down(a, 0, cuoi, {\"so_sanh\": 0, \"hoan_doi\": 0})\n        print(f\"  đưa {a[cuoi]} về cuối (vị trí {cuoi})\"\n              f\" | heap còn lại: {a[:cuoi]} | đã sắp: {a[cuoi:]}\")\n    return a\n\nheap_sort_trace([45, 90, 15, 33, 72])\n```\n\n```\nKẾT QUẢ (chạy thật):\n\nMảng gốc: [45, 90, 15, 33, 72]\nSau khi xây max-heap: [90, 72, 15, 33, 45]\n  đưa 90 về cuối (vị trí 4) | heap còn lại: [72, 45, 15, 33] | đã sắp: [90]\n  đưa 72 về cuối (vị trí 3) | heap còn lại: [45, 33, 15] | đã sắp: [72, 90]\n  đưa 45 về cuối (vị trí 2) | heap còn lại: [33, 15] | đã sắp: [45, 72, 90]\n  đưa 33 về cuối (vị trí 1) | heap còn lại: [15] | đã sắp: [33, 45, 72, 90]\n\n⭐ THẤY RÕ \"RANH GIỚI DI ĐỘNG\" TRÊN MỘT MẢNG DUY NHẤT: phần \"heap còn\n  lại\" (bên trái) THU NHỎ dần, phần \"đã sắp\" (bên phải) LỚN dần, TẤT\n  CẢ diễn ra TRÊN CÙNG MẢNG a — không có mảng phụ nào được tạo ra,\n  khác hẳn Merge Sort (bài 2) nơi các mảng con MỚI được cấp phát liên\n  tục ở mỗi lần trộn.\n```\n\n### Đo tăng trưởng: ba loại dữ liệu cho kết quả GẦN NHAU — bằng chứng \"không có ca xấu nhất do thứ tự\"\n\n```python\nimport math, random\nrandom.seed(3)\nprint(f\"{'n':>6} {'ngẫu_nhiên':>12} {'đã_sắp':>10} {'giảm_dần':>10} {'n*log2n':>10}\")\nfor n in (16, 64, 256, 1024):\n    a_rand = list(range(n)); random.shuffle(a_rand)\n    _, d1 = heap_sort(a_rand)\n    a_asc = list(range(n))\n    _, d2 = heap_sort(a_asc)\n    a_desc = list(range(n, 0, -1))\n    _, d3 = heap_sort(a_desc)\n    print(f\"{n:>6} {d1['so_sanh']:>12} {d2['so_sanh']:>10} {d3['so_sanh']:>10} {n*math.log2(n):>10.1f}\")\n```\n\n```\nKẾT QUẢ (chạy thật):\n\n     n   ngẫu_nhiên    đã_sắp  giảm_dần   n*log2n\n    16           79        85        72      64.0\n    64          562       593       525     384.0\n   256         3291      3452      3106    2048.0\n  1024        17351     18060     16407   10240.0\n\n⭐ BA CỘT GẦN NHAU (chênh lệch dưới ~10% ở mọi n) — HOÀN TOÀN khác\n  Quick Sort, nơi chênh lệch giữa \"đã sắp + chốt tệ\" và \"ngẫu nhiên\"\n  lên tới HÀNG CHỤC LẦN. Đây là bằng chứng số cho tính chất \"không có\n  ca xấu nhất do thứ tự dữ liệu\" đã chứng minh ở `co_che`. Cả ba cột\n  đều xấp xỉ 1.6-1.8× n·log₂n — hằng số ẩn cao hơn Merge/Quick (thường\n  ~1.0-1.4× n·log₂n) do chi phí truy cập bộ nhớ không liên tục của\n  cấu trúc heap, đúng như phân tích ở `vi_sao_can`.\n```\n\n### Kiểm chứng KHÔNG ổn định\n\n```python\ndef sift_down_cap(a, i, kich_thuoc):\n    while True:\n        trai, phai = 2 * i + 1, 2 * i + 2\n        lon_nhat = i\n        if trai < kich_thuoc and a[trai][0] > a[lon_nhat][0]:\n            lon_nhat = trai\n        if phai < kich_thuoc and a[phai][0] > a[lon_nhat][0]:\n            lon_nhat = phai\n        if lon_nhat == i:\n            break\n        a[i], a[lon_nhat] = a[lon_nhat], a[i]\n        i = lon_nhat\n\ndef heap_sort_cap(a):\n    a = a[:]; n = len(a)\n    for i in range(n // 2 - 1, -1, -1):\n        sift_down_cap(a, i, n)\n    for cuoi in range(n - 1, 0, -1):\n        a[0], a[cuoi] = a[cuoi], a[0]\n        sift_down_cap(a, 0, cuoi)\n    return a\n\ndu_lieu = [(45, 'A'), (90, 'B'), (45, 'C'), (33, 'D'), (90, 'E')]\nprint(heap_sort_cap(du_lieu))\n```\n\n```\nKẾT QUẢ (chạy thật):\n\n[(33, 'D'), (45, 'C'), (45, 'A'), (90, 'E'), (90, 'B')]\n\n⚠️ CẢ HAI CẶP giá trị bằng nhau đều bị đảo thứ tự: C trước A (gốc A\n  trước C), VÀ E trước B (gốc B trước E) — Heap Sort không ổn định\n  còn RÕ RÀNG HƠN Selection/Quick Sort ở ví dụ này (chúng chỉ đảo một\n  cặp). Nguyên nhân: thao tác sift-down và đổi chỗ gốc-cuối có thể\n  \"nhảy\" xa và nhiều lần hơn hẳn swap đơn giản của Selection Sort.\n```\n\n### Ca biên bắt buộc\n\n```python\nassert heap_sort([])[0] == []\nassert heap_sort([7])[0] == [7]\nassert heap_sort([5, 5, 5, 5])[0] == [5, 5, 5, 5]\nassert heap_sort([3, 1])[0] == [1, 3]\nassert heap_sort(list(range(9, 0, -1)))[0] == list(range(1, 10))   # n LẺ, giảm dần\nprint(\"✓ mọi ca biên đúng, kể cả n lẻ và mảng toàn phần tử bằng nhau\")\n```",

so_sanh: "### Bảng đầy đủ ba thuật toán O(n log n) đã học\n\n| | Merge Sort | Quick Sort | **Heap Sort** |\n|---|---|---|---|\n| Đảm bảo xấu nhất | **✓ O(n log n)** | ✗ O(n²) nếu chốt tệ | **✓ O(n log n)** |\n| Bộ nhớ phụ | O(n) | O(log n) (đệ quy) | **O(1)** ⭐ tốt nhất |\n| Ổn định | **✓** | ✗ | ✗ |\n| Cần đệ quy | ✓ | ✓ | ✗ (viết được hoàn toàn bằng vòng lặp) |\n| Thân thiện cache CPU | trung bình | **tốt nhất** (quét tuần tự) | kém nhất (nhảy theo chỉ số cây) |\n| Hằng số ẩn thực đo | trung bình | thấp | **cao nhất** trong ba |\n| Dùng cấu trúc dữ liệu phụ trợ | mảng tạm | không | **heap (mảng, tái dùng module Cây)** |\n\n### Khi nào chọn Heap Sort thay vì hai thuật toán kia\n\n| Ưu tiên | Chọn |\n|---|---|\n| Cần đảm bảo O(n log n) TUYỆT ĐỐI + bộ nhớ CỰC HẠN chế (không đủ O(n) hay cả O(log n) ngăn xếp sâu) | **Heap Sort** |\n| Cần đảm bảo O(n log n) và có đủ bộ nhớ O(n) | Merge Sort (thường ổn định hơn cũng là lợi thế) |\n| Ưu tiên tốc độ trung bình thực đo, chấp nhận rủi ro xấu nhất hiếm gặp | Quick Sort (với chốt ngẫu nhiên) |\n| Hệ thống thời gian thực, không được phép \"đôi khi chậm\" | Heap Sort hoặc Merge Sort — KHÔNG Quick Sort thuần |",

loi_thuong_gap: [
  "**Quên đổi hướng so sánh trong sift_down từ MIN-heap (bài Heap module Cây) sang MAX-heap.** Copy nguyên code min-heap mà không đổi `<` thành `>` sẽ cho ra mảng SẮP GIẢM DẦN thay vì tăng dần, hoặc lỗi logic khó nhận ra ngay.",
  "**Gọi sift_down với `kich_thuoc = len(a)` cố định thay vì biến `cuoi` giảm dần.** Đây là lỗi phá vỡ toàn bộ thuật toán: sift-down sẽ ĐỘNG VÀO phần mảng ĐÃ SẮP XONG (coi nó vẫn là một phần của heap), làm hỏng kết quả đã chốt trước đó.",
  "**Tưởng Heap Sort \"tốt hơn tuyệt đối\" Quick Sort vì có đảm bảo.** Bảng đo thực nghiệm ở `vi_du` cho thấy Heap Sort có hằng số ẩn CAO HƠN — trong thực tế, khi KHÔNG cần đảm bảo tuyệt đối (đa số ứng dụng), Quick Sort với chốt ngẫu nhiên thường được ưu tiên vì nhanh hơn trung bình.",
  "**Nhầm giai đoạn 1 (xây heap) là O(n log n) thay vì O(n).** Lỗi này đã cảnh báo kỹ ở bài Heap module Cây — trực giác \"n lần sift-down × O(log n)\" bỏ qua việc đa số nút gần lá, sift-down ở đó gần như miễn phí.",
  "**Không nhận ra Heap Sort là thuật toán DUY NHẤT trong ba thuật toán O(n log n) không cần đệ quy.** Cả hai giai đoạn đều là vòng lặp thuần — điều này có thể là lợi thế trong môi trường giới hạn độ sâu ngăn xếp (ví dụ nhúng, hệ thống thời gian thực).",
  "**Dùng Heap Sort khi cần tính ổn định.** Heap Sort không ổn định, và trong ví dụ đo được ở `vi_du`, nó thậm chí đảo thứ tự NHIỀU cặp giá trị bằng nhau hơn cả Selection/Quick Sort — không phải lựa chọn khi thứ tự tương đối của phần tử bằng nhau quan trọng."
],

tu_kiem_tra: [
  "Trình bày hai giai đoạn của Heap Sort bằng lời. Giai đoạn nào tốn O(n), giai đoạn nào tốn O(n log n)?",
  "Vì sao sift-down chỉ cần gọi ở GỐC sau mỗi lần đổi chỗ, không cần xây lại toàn bộ heap từ đầu?",
  "Giải thích tham số `kich_thuoc` (ranh giới di động) khác gì so với cách dùng sift_down trong bài Heap gốc (module Cây). Điều gì xảy ra nếu quên giảm ranh giới này?",
  "Vì sao Heap Sort KHÔNG có \"ca xấu nhất do thứ tự dữ liệu đầu vào\" như Quick Sort? Số đo thực nghiệm nào ở bài này chứng minh điều đó?",
  "So sánh bộ nhớ phụ của ba thuật toán O(n log n) đã học (Merge, Quick, Heap). Vì sao Heap Sort đạt O(1) trong khi hai thuật toán kia không?",
  "Nêu một tình huống thực tế nên chọn Heap Sort thay vì Quick Sort, dù Quick Sort thường nhanh hơn trung bình.",
  "Vì sao Heap Sort không ổn định dù dùng cấu trúc \"có tổ chức\" như heap, trong khi Merge Sort (cũng có cấu trúc) lại ổn định?"
],

tai_nguyen: [
  "\"Introduction to Algorithms\" (Cormen, Leiserson, Rivest, Stein — CLRS), chương 6 — trình bày Heap Sort như ứng dụng trực tiếp của chương Heap, kèm chứng minh độ phức tạp chặt chẽ.",
  "\"Algorithms\" (Robert Sedgewick, Kevin Wayne), chương 2.4 — có phần so sánh thực đo tốc độ giữa Heap Sort và các thuật toán O(n log n) khác trên phần cứng thật.",
  "Bài **Heap và Priority Queue** (module Cây, cùng lộ trình) — tài liệu tham khảo trực tiếp cho cơ chế sift-down/sift-up mà bài này tái sử dụng nguyên vẹn."
],

lien_he: "Bài này khép lại bộ ba thuật toán O(n log n) (Merge, Quick, Heap) — mỗi thuật toán đại diện một điểm khác nhau trên đường đánh đổi giữa ĐẢM BẢO, BỘ NHỚ, và TỐC ĐỘ THỰC ĐO. **Bài tiếp theo — Counting/Radix/Bucket Sort** — chuyển hướng HOÀN TOÀN khác: thay vì tối ưu trong khuôn khổ \"thuật toán dựa trên SO SÁNH\" (mọi thuật toán từ bài 1 tới đây đều so sánh CẶP phần tử), nhóm thuật toán này BỎ QUA so sánh, khai thác trực tiếp GIÁ TRỊ của dữ liệu (ví dụ đếm tần suất từng giá trị) để phá vỡ CHẶN DƯỚI O(n log n) mà mọi thuật toán so sánh phải tuân theo — đạt O(n) trong điều kiện phù hợp.\n\nNối lại **bài Heap và Priority Queue (module Cây)**: bài đó đã hứa \"module Sắp xếp sẽ dùng chính kỹ thuật này làm một thuật toán sắp xếp\" — bài này là nơi lời hứa đó được trả đầy đủ, với phân tích độ phức tạp và số đo thực nghiệm mà bài Heap gốc chưa có điều kiện đi sâu.\n\nNối lại **module 1 — CPU, RAM, Cache**: khoảng cách hằng số ẩn giữa Heap Sort (truy cập bộ nhớ nhảy cóc) và Quick Sort (truy cập tuần tự) đo được ở `vi_du` là minh chứng CỤ THỂ cho bài học \"chênh lệch tốc độ CPU/RAM/Cache hàng triệu lần\" — hai thuật toán CÙNG BẬC Big-O có thể chênh lệch tốc độ thực đáng kể chỉ vì CÁCH truy cập bộ nhớ.\n\nNối sang **bài 6 — Tính ổn định**: Heap Sort gia nhập nhóm KHÔNG ổn định (cùng Selection, Quick) — bảng phân loại đầy đủ cho cả sáu thuật toán đã học sẽ được tổng kết ở bài đó."

};
