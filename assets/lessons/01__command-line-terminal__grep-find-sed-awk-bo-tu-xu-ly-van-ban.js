window.LESSONS["01:command-line-terminal:grep-find-sed-awk-bo-tu-xu-ly-van-ban"] = {

tom_tat: [
  "Bốn công cụ, bốn việc khác nhau: **`grep` tìm dòng · `find` tìm file · `sed` sửa dòng · `awk` xử lý theo cột**.",
  "Chọn đúng công cụ quan trọng hơn thuộc nhiều cờ. Nhầm việc là viết ra lệnh dài gấp ba mà vẫn dễ vỡ.",
  "**Luôn để mẫu trong nháy đơn**: `grep 'loi.*404'`. Không có nháy thì shell bung `*` thành tên file trước khi lệnh chạy.",
  "`sed -i` sửa file tại chỗ và **không hoàn tác được** — luôn chạy thử không có `-i` trước, hoặc dùng `-i.bak` để giữ bản sao.",
  "**Đừng dùng regex để phân tích JSON, HTML hay CSV có dấu phẩy trong ô.** Dùng `jq` cho JSON, thư viện CSV cho CSV."
],

can_biet_truoc: [
  "Bài **Pipe và redirect** — bốn công cụ này gần như luôn dùng qua pipe.",
  "Bài **Điều hướng** và **Thao tác file** — cần quen đường dẫn và glob.",
  "Không cần biết regex từ trước, bài sẽ giới thiệu đủ dùng."
],

dinh_nghia: "Nói đời thường: bạn có một tủ hồ sơ khổng lồ. **`find`** là người đi tìm *đúng tập hồ sơ nào* trong tủ. **`grep`** là người lật từng trang tìm *dòng có chứa từ khoá*. **`sed`** là người cầm bút xoá sửa *từng dòng*. **`awk`** là người đọc *bảng biểu*, biết cột nào là gì và cộng trừ được.\n\nNói chính xác:\n\n- **`grep`** (*global regular expression print*) — lọc và in ra các dòng khớp một mẫu.\n- **`find`** — duyệt cây thư mục và chọn file theo tiêu chí: tên, kích thước, thời gian, quyền.\n- **`sed`** (*stream editor*) — biến đổi văn bản theo dòng, mạnh nhất ở việc thay thế.\n- **`awk`** — ngôn ngữ nhỏ xử lý văn bản có cấu trúc cột, có biến, điều kiện, vòng lặp và tính toán.",

vi_sao_can: "Bốn công cụ này là **kỹ năng chẩn đoán sự cố** cơ bản nhất:\n\n- **Đọc log trên server.** File 2 GB không mở bằng editor được. `grep ERROR app.log | tail -50` cho kết quả tức thì.\n- **Tìm chỗ code cần sửa.** `grep -rn 'ten_ham' src/` chỉ đúng file và dòng, nhanh hơn mở IDE.\n- **Sửa hàng loạt.** Đổi một tên biến trong 200 file bằng `sed` là một dòng lệnh.\n- **Dọn dẹp và tìm thủ phạm.** Ổ đầy thì `find` ra file lớn và cũ ngay.\n- **Rút thống kê từ log.** `awk` cộng, đếm, gom nhóm mà không cần xuất sang Excel.\n\nTình huống thật: production báo lỗi lúc 3 giờ sáng. Bạn SSH vào server, không có IDE, không có giao diện. Bốn công cụ này là toàn bộ những gì bạn có để tìm ra chuyện gì đã xảy ra — và chúng đủ.",

co_che: "### Chọn công cụ nào\n\nĐây là phần quan trọng nhất của bài. Thuộc cờ thì tra được, chọn sai công cụ thì lệnh dài gấp ba mà vẫn dễ vỡ.\n\n```\nTìm FILE theo tên, kích thước, ngày sửa      → find\nTìm DÒNG chứa nội dung nào đó                → grep\nTHAY THẾ trong dòng                          → sed\nXử lý theo CỘT, tính toán, gom nhóm          → awk\nXử lý JSON                                   → jq  (KHÔNG dùng grep)\nQuá phức tạp cho một dòng                    → viết Python\n```\n\nDòng cuối đáng nhớ: khi lệnh `awk` dài quá hai dòng và có nhiều điều kiện lồng nhau, viết Python sẽ dễ đọc và dễ sửa hơn nhiều. Công cụ dòng lệnh mạnh nhưng có giới hạn hợp lý của nó.\n\n### `grep` — tìm dòng\n\n```bash\ngrep 'mau' file.txt              # in các dòng chứa mẫu\ngrep -i 'mau' file.txt           # không phân biệt hoa thường\ngrep -v 'mau' file.txt           # ĐẢO NGƯỢC: in dòng KHÔNG chứa\ngrep -n 'mau' file.txt           # kèm số dòng\ngrep -c 'mau' file.txt           # chỉ đếm số dòng khớp\ngrep -r 'mau' thu-muc/           # ⭐ đệ quy trong cả cây thư mục\ngrep -l 'mau' *.py               # chỉ in TÊN FILE có chứa, không in dòng\ngrep -w 'mau' file.txt           # khớp nguyên từ, không khớp \"maufile\"\ngrep -A 3 -B 3 'loi' app.log     # ⭐ in thêm 3 dòng trước và sau\n```\n\nTổ hợp dùng nhiều nhất khi gỡ lỗi:\n\n```bash\ngrep -rn 'ten_ham' src/          # tìm định nghĩa và nơi gọi, kèm số dòng\ngrep -A 5 -B 5 'Traceback' app.log   # xem ngữ cảnh quanh chỗ lỗi\n```\n\nCờ `-A`/`-B` (after/before) rất quan trọng khi đọc log: một dòng `ERROR` đơn độc thường không đủ để hiểu chuyện gì xảy ra.\n\n### Regex cơ bản\n\n```\n.        một ký tự bất kỳ\n*        lặp 0 hoặc nhiều lần cái đứng trước\n+        lặp 1 hoặc nhiều lần        (cần -E)\n?        có hoặc không               (cần -E)\n^        đầu dòng\n$        cuối dòng\n[abc]    một ký tự trong tập\n[^abc]   một ký tự KHÔNG trong tập\n[0-9]    một chữ số\n\\|       hoặc                        (dùng | với -E)\n\\.       dấu chấm theo nghĩa đen\n```\n\n**Luôn dùng `-E`** (regex mở rộng) để có cú pháp hiện đại, không phải thoát ký tự lung tung:\n\n```bash\ngrep -E '^(ERROR|WARN)' app.log        # dòng bắt đầu bằng ERROR hoặc WARN\ngrep -E '[0-9]{3}-[0-9]{4}' danh-ba    # khớp mẫu số điện thoại\ngrep -E '\\.(jpg|png)$' danh-sach       # kết thúc bằng .jpg hoặc .png\n```\n\n### Luôn để mẫu trong nháy đơn\n\nĐây là quy tắc quan trọng nhất khi dùng cả bốn công cụ:\n\n```bash\n# ❌ SAI — shell bung * thành tên file TRƯỚC khi grep chạy\ngrep loi.*404 app.log\n\n# ✓ ĐÚNG — nháy đơn giữ nguyên văn mẫu\ngrep 'loi.*404' app.log\n```\n\nNháy đơn ngăn shell diễn giải `*`, `$`, `?`, `[`, khoảng trắng. Nếu cần shell bung biến thì dùng nháy kép, nhưng phải hiểu rõ mình đang làm gì.\n\n### `find` — tìm file\n\n```bash\nfind . -name '*.py'                  # theo tên (phân biệt hoa thường)\nfind . -iname '*.PY'                 # không phân biệt hoa thường\nfind . -type f                       # chỉ file\nfind . -type d                       # chỉ thư mục\nfind . -size +100M                   # lớn hơn 100 MB\nfind . -mtime -7                     # sửa trong 7 ngày qua\nfind . -mtime +30                    # sửa cách đây hơn 30 ngày\nfind . -empty                        # file hoặc thư mục rỗng\nfind . -maxdepth 2                   # ⭐ giới hạn độ sâu\n\n# Kết hợp điều kiện\nfind . -name '*.log' -size +10M -mtime +7\n```\n\nCú pháp thời gian dễ nhầm: **`-7` là \"trong vòng 7 ngày\", `+7` là \"cũ hơn 7 ngày\"**. Nhớ sai là xoá nhầm file mới thay vì file cũ.\n\nHành động trên kết quả:\n\n```bash\nfind . -name '*.tmp' -delete                    # xoá\nfind . -name '*.py' -exec wc -l {} \\;           # chạy lệnh cho TỪNG file\nfind . -name '*.py' -exec wc -l {} +            # ⭐ gộp nhiều file một lệnh, nhanh hơn\nfind . -name '*.log' -print0 | xargs -0 gzip    # qua xargs, an toàn với dấu cách\n```\n\nKhác biệt giữa `\\;` và `+`: dấu chấm phẩy chạy lệnh **một lần cho mỗi file** (1000 file = 1000 lần gọi process); dấu cộng **gộp nhiều file vào một lần gọi**. Với số lượng lớn, `+` nhanh hơn hàng chục lần.\n\n**Luôn chạy `find` không có hành động trước** để xem danh sách, rồi mới thêm `-delete`.\n\n### `sed` — thay thế trong dòng\n\n```bash\nsed 's/cu/moi/' file.txt         # thay lần ĐẦU TIÊN trong mỗi dòng\nsed 's/cu/moi/g' file.txt        # ⭐ g = global, thay MỌI lần trong dòng\nsed 's/cu/moi/gi' file.txt       # thêm i = không phân biệt hoa thường\n\nsed -n '10,20p' file.txt         # chỉ in dòng 10 đến 20\nsed '5d' file.txt                # xoá dòng 5\nsed '/^#/d' file.txt             # xoá mọi dòng bắt đầu bằng #\nsed '/^$/d' file.txt             # xoá dòng trống\n```\n\nCấu trúc lệnh `s`: `s/tìm/thay/cờ`. Dấu ngăn cách không bắt buộc là `/` — khi mẫu chứa nhiều dấu gạch chéo thì đổi sang ký tự khác cho dễ đọc:\n\n```bash\nsed 's|/duong/dan/cu|/duong/dan/moi|g' file.txt   # dùng | thay /\n```\n\n**Sửa file tại chỗ — nguy hiểm:**\n\n```bash\nsed 's/cu/moi/g' file.txt             # ✓ chỉ IN ra, file không đổi\nsed -i 's/cu/moi/g' file.txt          # ⚠️ SỬA FILE, không hoàn tác được\nsed -i.bak 's/cu/moi/g' file.txt      # ⭐ sửa nhưng giữ file.txt.bak\n```\n\nQuy trình an toàn: chạy **không có `-i`** để xem kết quả, kiểm tra đúng ý rồi mới thêm `-i.bak`.\n\nMột khác biệt gây khó chịu: trên macOS (BSD sed), `-i` **bắt buộc phải có đối số**:\n\n```bash\nsed -i '' 's/cu/moi/g' file.txt       # macOS — cần '' rỗng\nsed -i 's/cu/moi/g' file.txt          # Linux (GNU sed)\n```\n\nScript cần chạy trên cả hai thì nên dùng `-i.bak` — cú pháp này hoạt động ở cả hai.\n\n### `awk` — xử lý theo cột\n\n`awk` mạnh hơn ba cái kia nhiều vì nó là một **ngôn ngữ lập trình nhỏ**.\n\n```bash\nawk '{print $1}' file            # in cột 1\nawk '{print $1, $3}' file        # cột 1 và 3\nawk '{print NF}' file            # NF = số cột của dòng này\nawk '{print NR, $0}' file        # NR = số thứ tự dòng, $0 = cả dòng\nawk -F: '{print $1}' /etc/passwd # -F đổi dấu ngăn cách cột thành :\n```\n\nCác biến dựng sẵn đáng nhớ:\n\n| Biến | Nghĩa |\n|---|---|\n| `$0` | cả dòng |\n| `$1`, `$2`… | cột 1, 2… |\n| `NF` | số cột trong dòng hiện tại |\n| `NR` | số thứ tự dòng đang xử lý |\n| `FS` | dấu ngăn cách cột đầu vào |\n\nCấu trúc `mẫu { hành động }`:\n\n```bash\nawk '$3 > 100 {print $1}' file           # in cột 1 của dòng có cột 3 > 100\nawk '/loi/ {print}' file                 # in dòng khớp regex — giống grep\nawk 'NR > 1 {print}' file                # bỏ dòng tiêu đề\nawk '{tong += $2} END {print tong}' file # ⭐ cộng dồn cột 2, in ở cuối\n```\n\nKhối `BEGIN` chạy trước, `END` chạy sau khi đọc hết:\n\n```bash\nawk 'BEGIN {print \"Bắt đầu\"} {n++} END {print \"Tổng\", n, \"dòng\"}' file\n```\n\nĐây là chỗ `awk` vượt xa `grep` và `sed`: nó **giữ trạng thái** qua các dòng, nên tính tổng, đếm, gom nhóm được.\n\n### Gom nhóm bằng mảng kết hợp\n\n```bash\n# Đếm số dòng theo từng giá trị của cột 1\nawk '{dem[$1]++} END {for (k in dem) print dem[k], k}' file\n```\n\n`dem[$1]++` tạo một mảng kết hợp (giống dict trong Python). Đây là tính năng mạnh nhất của `awk` và cũng là lý do nó thay được cả một script Python ngắn.",

vi_du: "### Chẩn đoán sự cố production\n\n```bash\n# ① Có bao nhiêu lỗi?\ngrep -c ERROR app.log\n# 847\n\n# ② Lỗi loại gì, cái nào nhiều nhất?\ngrep ERROR app.log | awk '{print $4}' | sort | uniq -c | sort -rn | head\n#    623 DatabaseTimeout\n#    198 ConnectionRefused\n#     26 ValidationError\n\n# ③ Bắt đầu từ lúc nào?\ngrep ERROR app.log | head -1\n# 2026-08-30 03:14:22 ERROR DatabaseTimeout ...\n\n# ④ Xem ngữ cảnh quanh lỗi đầu tiên\ngrep -B 10 -A 10 '03:14:22' app.log\n\n# ⑤ Lỗi phân bố theo giờ thế nào?\ngrep ERROR app.log | awk '{print substr($2,1,2)}' | sort | uniq -c\n#     12 02\n#    623 03      ← 3 giờ sáng có sự cố\n#    212 04\n```\n\nNăm lệnh này là quy trình chẩn đoán chuẩn: đếm, phân loại, định vị thời điểm, xem ngữ cảnh, nhìn phân bố. Không cần công cụ nào ngoài những gì có sẵn trên mọi server Linux.\n\n### Tìm file gây đầy ổ đĩa\n\n```bash\n# 10 file lớn nhất trong /var\nfind /var -type f -size +50M -exec ls -lh {} + 2>/dev/null \\\n  | sort -k5 -rh | head -10\n\n# File log cũ hơn 30 ngày — XEM trước\nfind /var/log -name '*.log' -mtime +30 -size +10M\n\n# Rồi mới nén\nfind /var/log -name '*.log' -mtime +30 -size +10M -exec gzip {} +\n```\n\n`2>/dev/null` ở đây hợp lý: `find` sẽ báo *permission denied* cho nhiều thư mục hệ thống, và những dòng đó chỉ gây nhiễu.\n\n### Đổi tên biến trong cả dự án\n\n```bash\n# ① Xem sẽ đụng vào những file nào\ngrep -rl 'ten_cu' src/\n\n# ② Xem cụ thể từng dòng\ngrep -rn 'ten_cu' src/\n\n# ③ Chạy thử trên một file\nsed 's/ten_cu/ten_moi/g' src/main.py | diff src/main.py -\n\n# ④ Thấy đúng thì áp dụng, giữ bản sao lưu\nfind src/ -name '*.py' -exec sed -i.bak 's/\\bten_cu\\b/ten_moi/g' {} +\n\n# ⑤ Kiểm tra rồi dọn file .bak\ngrep -rn 'ten_moi' src/ | head\nfind src/ -name '*.bak' -delete\n```\n\nChi tiết quan trọng: `\\b` là **ranh giới từ**. Không có nó thì `ten_cu` cũng khớp bên trong `ten_cu_khac` và bạn sửa nhầm.\n\nNăm bước này trông dài nhưng mỗi bước đều rẻ, và chúng biến một thao tác nguy hiểm thành có kiểm soát.\n\n### `awk` tính toán từ log\n\n```bash\n# Access log: cột 10 là số byte trả về\n# Tổng băng thông đã dùng\nawk '{tong += $10} END {printf \"%.2f GB\\n\", tong/1024/1024/1024}' access.log\n# 4.73 GB\n\n# Thời gian phản hồi trung bình (cột 11, đơn vị ms)\nawk '{tong += $11; n++} END {printf \"TB: %.1f ms trên %d request\\n\", tong/n, n}' access.log\n# TB: 243.7 ms trên 84512 request\n\n# Băng thông theo mã trạng thái (cột 9)\nawk '{byte[$9] += $10; dem[$9]++}\n     END {for (ma in byte)\n            printf \"%s: %d request, %.1f MB\\n\", ma, dem[ma], byte[ma]/1024/1024}' access.log\n# 200: 78234 request, 4210.3 MB\n# 404: 5102 request, 12.7 MB\n# 500: 1176 request, 8.2 MB\n```\n\nLệnh cuối dùng **hai mảng kết hợp cùng lúc** — một tổng byte, một đếm số lượng, gom theo mã trạng thái. Viết bằng Python cũng khoảng chừng đó dòng.\n\n### Xử lý CSV — và giới hạn của nó\n\n```bash\n# CSV đơn giản: cột 3 là số tiền\nawk -F, 'NR > 1 {tong += $3} END {print \"Tổng:\", tong}' don-hang.csv\n\n# Lọc theo điều kiện rồi tính\nawk -F, 'NR > 1 && $4 == \"da_giao\" {tong += $3} END {print tong}' don-hang.csv\n```\n\n**Nhưng dừng lại ở đây.** Nếu CSV có dấu phẩy bên trong ô được bọc nháy:\n\n```\nid,ten,ghi_chu\n1,\"Nguyễn, Văn A\",ok\n```\n\nthì `-F,` tách sai hoàn toàn. `awk` không hiểu quy tắc nháy của CSV.\n\n```python\n# ✓ Dùng đúng công cụ\nimport csv\nwith open(\"don-hang.csv\") as f:\n    tong = sum(float(h[\"so_tien\"]) for h in csv.DictReader(f))\n```\n\nĐây là ranh giới quan trọng: **định dạng có quy tắc thoát ký tự thì phải dùng bộ phân tích đúng chuẩn**, không dùng regex hay tách cột thô.\n\n### JSON: đừng dùng grep\n\n```bash\n# ❌ Dễ vỡ — phụ thuộc cách xuống dòng và khoảng trắng\ncurl -s api/users | grep '\"email\"' | sed 's/.*: \"\\(.*\\)\".*/\\1/'\n\n# ✓ Dùng jq — hiểu cấu trúc JSON thật\ncurl -s api/users | jq -r '.[] | select(.active) | .email'\n\n# jq tính toán được\ncurl -s api/orders | jq '[.[] | .amount] | add'\n```\n\nJSON có thể xuống dòng bất kỳ đâu, nên regex chỉ hoạt động cho tới khi API đổi cách định dạng. `jq` phân tích cấu trúc thật nên không bao giờ vỡ vì lý do đó.\n\n### Bảng tra nhanh\n\n```bash\ngrep -rn 'mau' .                    # tìm nội dung trong cây thư mục\ngrep -c 'mau' file                  # đếm số dòng khớp\ngrep -v 'mau' file                  # loại bỏ dòng khớp\n\nfind . -name '*.py'                 # tìm file theo tên\nfind . -size +100M                  # file lớn\nfind . -mtime -1                    # file sửa trong 24h\n\nsed -n '10,20p' file                # in dòng 10-20\nsed 's/a/b/g' file                  # thay thế\nsed -i.bak 's/a/b/g' file           # thay thế và sửa file\n\nawk '{print $2}' file               # in cột 2\nawk -F, '{print $1}' file           # đổi dấu ngăn cột\nawk '{s += $1} END {print s}' file  # cộng dồn\n```",

so_sanh: "### Bốn công cụ, bốn vai trò\n\n| | `grep` | `find` | `sed` | `awk` |\n|---|---|---|---|---|\n| Đối tượng | **dòng** | **file** | **dòng** | **cột** |\n| Việc chính | lọc | chọn file | thay thế | tính toán |\n| Giữ trạng thái | ✗ | ✗ | hạn chế | **✓ có biến** |\n| Tính toán số | ✗ | ✗ | ✗ | **✓** |\n| Độ khó | Dễ | Trung bình | Trung bình | **Cao nhất** |\n\nMột nhận xét đáng để ý: **`awk` làm được việc của cả `grep` và `sed`**. `awk '/mau/'` chính là `grep 'mau'`, và `awk '{gsub(/a/,\"b\"); print}'` chính là `sed 's/a/b/g'`.\n\nNhưng vẫn nên dùng `grep` cho việc tìm và `sed` cho việc thay: **ngắn hơn, rõ ý hơn, và người đọc code hiểu ngay bạn định làm gì**. Dùng `awk` cho mọi thứ là kiểu viết code khoe kỹ thuật mà khó bảo trì.\n\n### Công cụ hiện đại thay thế\n\n| Cũ | Mới | Hơn ở chỗ |\n|---|---|---|\n| `grep -r` | **`rg`** (ripgrep) | Nhanh hơn nhiều lần, tự bỏ qua `.gitignore` |\n| `find` | **`fd`** | Cú pháp ngắn hơn hẳn, mặc định hợp lý hơn |\n| `cat` | `bat` | Có tô màu cú pháp và số dòng |\n\n```bash\nrg 'ten_ham'              # tương đương grep -rn nhưng nhanh hơn, sạch hơn\nfd '\\.py$'                # tương đương find . -name '*.py'\n```\n\n`ripgrep` đáng cài nhất trong ba cái: nó nhanh hơn `grep -r` vài lần trên dự án lớn và **tự bỏ qua `node_modules`, `.git`, `__pycache__`** nhờ đọc `.gitignore`.\n\nNhưng **vẫn phải biết `grep` và `find`**, vì server bạn SSH vào sẽ không có công cụ mới. Học cái cũ trước, cài cái mới cho máy của mình.\n\n### Khi nào bỏ dòng lệnh mà viết Python\n\nDấu hiệu nên chuyển:\n\n- Lệnh dài quá ba dòng và có nhiều `|` lồng logic\n- Cần cấu trúc dữ liệu phức tạp hơn mảng một chiều\n- Xử lý JSON, XML, CSV có quy tắc thoát ký tự\n- Cần bắt lỗi và xử lý ngoại lệ\n- Đoạn lệnh sẽ được người khác đọc và sửa\n\n```bash\n# ❌ Quá phức tạp cho một dòng — không ai đọc lại được sau một tuần\nawk -F, 'NR>1 {if ($3>100 && $4==\"x\") {a[$1]+=$3; n[$1]++}} END {for (k in a) if (n[k]>2) printf \"%s %.2f\\n\", k, a[k]/n[k]}' file.csv\n```\n\n```python\n# ✓ Dài hơn nhưng đọc được, sửa được, test được\nimport csv\nfrom collections import defaultdict\n\ntong, dem = defaultdict(float), defaultdict(int)\nwith open(\"file.csv\") as f:\n    for hang in csv.DictReader(f):\n        if float(hang[\"gia_tri\"]) > 100 and hang[\"loai\"] == \"x\":\n            tong[hang[\"ma\"]] += float(hang[\"gia_tri\"])\n            dem[hang[\"ma\"]] += 1\n\nfor ma in tong:\n    if dem[ma] > 2:\n        print(f\"{ma} {tong[ma]/dem[ma]:.2f}\")\n```\n\nRanh giới thực dụng: **một dòng lệnh gõ một lần rồi bỏ thì dùng `awk`; đoạn logic sẽ chạy lại nhiều lần hoặc người khác phải đọc thì viết Python.**",

loi_thuong_gap: [
  "**Quên nháy đơn quanh mẫu.** `grep loi.*404` bị shell bung `*` thành tên file trước khi grep chạy. Luôn `grep 'loi.*404'`.",
  "**Dùng `sed -i` mà chưa chạy thử.** Không hoàn tác được. Chạy không có `-i` để xem trước, rồi dùng `-i.bak`.",
  "**Nhầm `-mtime -7` với `+7`.** Dấu trừ là \"trong vòng 7 ngày\", dấu cộng là \"cũ hơn 7 ngày\". Nhầm là xoá nhầm file mới.",
  "**Dùng `grep` để phân tích JSON.** JSON xuống dòng tuỳ ý nên regex vỡ khi API đổi định dạng. Dùng `jq`.",
  "**Dùng `awk -F,` cho CSV có dấu phẩy trong ô.** Tách sai hoàn toàn. Dùng thư viện CSV.",
  "**Quên `\\b` khi thay tên biến bằng `sed`.** `ten_cu` sẽ khớp cả bên trong `ten_cu_khac`.",
  "**Dùng `find -exec ... \\;` cho hàng nghìn file.** Nó gọi process một lần mỗi file. Dùng `+` để gộp lại.",
  "**Chạy `find -delete` mà chưa xem danh sách.** Luôn chạy không có hành động trước.",
  "**Viết `awk` một dòng dài 200 ký tự.** Nếu phải sửa lại sau một tuần thì bạn sẽ viết lại từ đầu. Chuyển sang Python."
],

tu_kiem_tra: [
  "Bốn công cụ, bốn đối tượng khác nhau. Nêu công cụ đúng cho mỗi việc: tìm file lớn hơn 1 GB · đếm dòng chứa \"ERROR\" · đổi tên biến trong 50 file · tính tổng cột 3.",
  "Vì sao `grep loi.*404 app.log` có thể cho kết quả sai? Sửa thế nào?",
  "`find . -mtime -7` và `find . -mtime +7` khác nhau ra sao?",
  "Vì sao không nên dùng `grep` để lấy giá trị từ JSON?",
  "`find . -name '*.py' -exec wc -l {} \\;` và `... {} +` khác nhau thế nào về hiệu năng?",
  "Viết lệnh `awk` tính tổng cột 2 của một file, bỏ qua dòng tiêu đề.",
  "Bạn cần đổi `getUserData` thành `fetchUserData` trong cả dự án. Viết quy trình 4 bước an toàn."
],

tai_nguyen: [
  "Sách **The Linux Command Line** — William Shotts, chương 19–20 về tìm kiếm và xử lý văn bản. Đọc miễn phí.",
  "Sách **sed & awk** — Dougherty & Robbins (O'Reilly). Kinh điển cho hai công cụ này.",
  "Trang **regex101.com** — thử regex trực tiếp, có giải thích từng phần. Nhớ chọn chế độ POSIX khi viết cho grep.",
  "Tài liệu `jq`, mục *Tutorial* — công cụ bắt buộc phải biết nếu làm việc với API.",
  "Trang **explainshell.com** — dán lệnh phức tạp vào để hiểu từng cờ."
],

lien_he: [
  "**Pipe và redirect** là bài ngay trước: bốn công cụ này chỉ phát huy hết sức mạnh khi ghép qua pipe.",
  "**Thao tác file**: `find` thường đi kèm `-delete`, `-exec rm` — áp dụng đúng thói quen xem-trước-rồi-làm.",
  "**Viết shell script** (bài cuối module): các lệnh này là nguyên liệu chính của mọi script vận hành.",
  "**Hệ thống file**: `find` tìm theo quyền, thời gian, kích thước — toàn bộ là siêu dữ liệu trong inode.",
  "**Regex** cũng dùng trong Python (`re`), JavaScript, và trong `WHERE ... LIKE` của SQL. Học một lần dùng nhiều nơi.",
  "**Vận hành** ở mảng 05: chẩn đoán sự cố production trên server chỉ có bốn công cụ này, và chúng đủ.",
  "**Tiền xử lý dữ liệu** ở mảng 07: `awk` lọc nhanh file dữ liệu lớn trước khi nạp vào pandas, tiết kiệm rất nhiều bộ nhớ."
]

};
