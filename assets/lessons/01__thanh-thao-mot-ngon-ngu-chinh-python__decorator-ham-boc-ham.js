window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:decorator-ham-boc-ham"] = {

tom_tat: [
  "**Decorator không có gì huyền bí: `@deco` chỉ là `ham = deco(ham)`.** Toàn bộ phần còn lại của bài là hệ quả của một dòng đó.",
  "**Nền tảng là hai tính chất**: hàm là object (gán được, truyền được, trả về được), và closure (hàm trong nhớ được biến của hàm ngoài).",
  "**Luôn dùng `@functools.wraps`.** Thiếu nó, hàm bị bọc mất tên, mất docstring, mất chữ ký — và mọi công cụ dựa vào chúng đều hỏng.",
  "**Decorator có tham số cần ba tầng hàm**, vì `@deco(x)` được gọi trước rồi kết quả mới đem đi trang trí. Hiểu thứ tự này là hết bối rối.",
  "**Xếp chồng decorator đọc từ dưới lên**: cái gần `def` nhất bọc trước, nên nó nằm trong cùng khi chạy."
],

can_biet_truoc: [
  "Bài **Hàm, tham số mặc định, `*args`, `**kwargs`** — decorator dựng hoàn toàn trên `*args`/`**kwargs` và trên bẫy `lambda` trong vòng lặp.",
  "Bài **Cú pháp, kiểu dữ liệu** — \"biến là nhãn\" áp cho hàm: gán tên hàm cho tên khác không sao chép gì.",
  "Bài **Module, package, import** — `def` là câu lệnh được thực thi, và decorator chạy **lúc định nghĩa**, không phải lúc gọi.",
  "Bài **Xử lý ngoại lệ** — decorator retry và decorator ghi log lỗi là hai ứng dụng chính."
],

dinh_nghia: "Nói đời thường: decorator giống như **gói một món quà**. Món quà không đổi, nhưng bên ngoài có thêm lớp giấy làm một việc gì đó — đo thời gian, kiểm tra quyền, ghi sổ. Người nhận vẫn tưởng mình cầm đúng món quà cũ, chỉ là mỗi lần mở lại có thêm vài bước.\n\nNói chính xác:\n\n- **Hàm là object hạng nhất** (*first-class*) — gán được cho biến, truyền vào hàm khác, trả về từ hàm khác, cất vào `list` hay `dict`.\n- **Closure** — hàm bên trong **giữ được** các biến của hàm bên ngoài, kể cả sau khi hàm ngoài đã kết thúc.\n- **Decorator** — một hàm nhận vào một hàm và trả về một hàm (thường là hàm mới có bọc thêm hành vi).\n- **Cú pháp `@`** — đường tắt: `@deco` đặt trên `def f` tương đương `f = deco(f)`.\n- **`functools.wraps`** — decorator dùng bên trong decorator, để hàm bọc \"giả dạng\" hàm gốc: giữ `__name__`, `__doc__`, `__wrapped__`.\n\nĐiểm cần nắm ngay: **decorator chạy một lần, lúc định nghĩa hàm — không phải mỗi lần gọi.** Phần chạy mỗi lần gọi là *hàm bọc bên trong*. Phân biệt được hai thời điểm này là gỡ được hầu hết sự bối rối về decorator.",

vi_sao_can: "Decorator là ranh giới giữa \"biết cú pháp Python\" và \"hiểu Python\":\n\n- **Bạn đã dùng nó rồi mà chưa biết.** `@property`, `@staticmethod`, `@dataclass`, `@app.route`, `@pytest.fixture` — mọi framework Python đều dựa trên decorator. Không hiểu nó là dùng framework theo trí nhớ.\n- **Nó giải quyết vấn đề lặp code chéo chức năng.** Ghi log, đo thời gian, kiểm tra quyền, thử lại, cache — những việc này lặp ở hàng chục hàm và không liên quan tới logic nghiệp vụ. Decorator tách chúng ra một chỗ.\n- **Đọc mã nguồn thư viện đòi hỏi nó.** Không đọc được decorator là không đọc được phần lớn code Python chất lượng cao.\n- **`functools.lru_cache` là tối ưu một dòng.** Thêm một dòng lên trên hàm đệ quy có thể biến thời gian chạy từ hàng phút xuống mili giây.\n- **Nền để hiểu closure.** Closure còn xuất hiện ở callback, ở hàm factory, và ở bẫy `lambda` trong vòng lặp đã gặp ở bài hàm.\n\nTình huống thật rất hay gặp: bạn viết decorator đo thời gian, dùng cho mười hàm. Mọi thứ ổn cho tới khi bạn chạy công cụ sinh tài liệu và thấy **cả mười hàm đều tên là `bao`, cùng một docstring rỗng**. Rồi `pytest` báo lỗi vì không phân biệt được các bài kiểm thử. Nguyên nhân chỉ là thiếu một dòng `@functools.wraps(ham)`, và phần cơ chế sẽ chỉ rõ vì sao nó bắt buộc chứ không phải tuỳ chọn.",

co_che: "### Nền tảng ①: hàm là object\n\n```python\ndef chao(ten):\n    return f\"Xin chào {ten}\"\n\n# Gán cho tên khác — KHÔNG sao chép, chỉ thêm nhãn (bài 1)\nf = chao\nf(\"Minh\")                 # 'Xin chào Minh'\nf is chao                 # True — cùng một object\n\n# Truyền vào hàm khác\ndef ap_dung(ham, gt):\n    return ham(gt)\nap_dung(chao, \"An\")\n\n# Cất vào cấu trúc dữ liệu\nbang = {\"chao\": chao, \"in\": print}\nbang[\"chao\"](\"Bình\")\n\n# Hàm có thuộc tính như mọi object\nchao.__name__             # 'chao'\nchao.__doc__\nchao.ghi_chu = \"của tôi\"   # gán được thuộc tính mới!\n```\n\nDòng cuối đáng chú ý: hàm là object bình thường nên **gắn thuộc tính vào được**. Đây là kỹ thuật một số decorator dùng để đánh dấu hàm.\n\n### Nền tảng ②: closure\n\n```python\ndef tao_nhan(loi_chao):\n    def chao(ten):\n        return f\"{loi_chao}, {ten}!\"      # ⭐ dùng biến của hàm NGOÀI\n    return chao\n\nxin_chao = tao_nhan(\"Xin chào\")\ntam_biet = tao_nhan(\"Tạm biệt\")\n\nxin_chao(\"Minh\")          # 'Xin chào, Minh!'\ntam_biet(\"Minh\")          # 'Tạm biệt, Minh!'\n```\n\n`tao_nhan` đã kết thúc từ lâu, nhưng `loi_chao` **vẫn sống**. Hàm bên trong giữ một tham chiếu tới nó:\n\n```python\nxin_chao.__closure__[0].cell_contents      # 'Xin chào'\nxin_chao.__code__.co_freevars              # ('loi_chao',)\n```\n\nHai thuộc tính này là bằng chứng cụ thể: `co_freevars` liệt kê các biến \"tự do\" mà hàm mượn từ ngoài, `__closure__` chứa giá trị thật của chúng.\n\nCơ chế: khi Python thấy hàm trong dùng biến của hàm ngoài, nó **không cho biến đó chết** theo khung stack của hàm ngoài mà chuyển sang một ô nhớ tồn tại lâu dài. Nối về bài stack/heap: biến đó rời stack và sống trên heap cùng object hàm.\n\n**Closure bắt biến, không bắt giá trị** — đây là gốc của bẫy đã gặp ở bài hàm:\n\n```python\nham = [lambda: i for i in range(3)]\n[f() for f in ham]           # [2, 2, 2] — cả ba nhìn CÙNG một biến i\n\nham = [lambda i=i: i for i in range(3)]\n[f() for f in ham]           # [0, 1, 2] — giá trị bị đóng băng lúc định nghĩa\n```\n\nMuốn **gán** vào biến của hàm ngoài thì phải khai báo `nonlocal`:\n\n```python\ndef bo_dem():\n    n = 0\n    def tang():\n        nonlocal n           # ⭐ thiếu dòng này là UnboundLocalError\n        n += 1\n        return n\n    return tang\n\nd = bo_dem()\nd(); d(); d()                # 1, 2, 3\n```\n\nVì sao thiếu `nonlocal` là lỗi: Python thấy `n += 1` tức có phép **gán**, nên coi `n` là biến cục bộ của `tang`; rồi nó đọc `n` trước khi gán — chưa có giá trị — nên báo `UnboundLocalError`. `nonlocal` nói rõ \"`n` là biến của hàm ngoài\".\n\n### Từ closure tới decorator\n\nDecorator chỉ là closure mà biến được bắt **chính là một hàm**:\n\n```python\ndef do_thoi_gian(ham):                     # nhận một hàm\n    def bao(*args, **kwargs):              # closure bắt \"ham\"\n        t = time.perf_counter()\n        kq = ham(*args, **kwargs)          # gọi hàm gốc\n        print(f\"{ham.__name__}: {time.perf_counter()-t:.4f}s\")\n        return kq                          # ⭐ ĐỪNG QUÊN return\n    return bao                             # trả về hàm mới\n\n# Cách dùng thủ công\ndef tinh(n):\n    return sum(range(n))\ntinh = do_thoi_gian(tinh)                  # ⭐ đây là tất cả những gì @ làm\n\n# Cú pháp @ — hoàn toàn tương đương\n@do_thoi_gian\ndef tinh(n):\n    return sum(range(n))\n```\n\n```\n@deco\ndef f(): ...\n\n        ⇕  hoàn toàn tương đương\n\ndef f(): ...\nf = deco(f)\n```\n\nGhi nhớ tương đương này là đủ để suy ra mọi hành vi khác của decorator.\n\nLỗi số một khi mới viết decorator: **quên `return kq`**. Hàm bọc chạy đúng nhưng trả về `None`, và lỗi hiện ra ở nơi khác.\n\n### `functools.wraps` — vì sao bắt buộc\n\n```python\n# ❌ Không có wraps\ndef do_thoi_gian(ham):\n    def bao(*a, **k):\n        return ham(*a, **k)\n    return bao\n\n@do_thoi_gian\ndef tinh_tong(n):\n    \"\"\"Tính tổng 0..n-1.\"\"\"\n    return sum(range(n))\n\ntinh_tong.__name__     # 'bao'   ← ⚠️ mất tên\ntinh_tong.__doc__      # None    ← ⚠️ mất docstring\nhelp(tinh_tong)        # vô dụng\n```\n\n```python\n# ✓ Có wraps\nimport functools\n\ndef do_thoi_gian(ham):\n    @functools.wraps(ham)                  # ⭐ một dòng\n    def bao(*a, **k):\n        return ham(*a, **k)\n    return bao\n\ntinh_tong.__name__     # 'tinh_tong' ✓\ntinh_tong.__doc__      # 'Tính tổng 0..n-1.' ✓\ntinh_tong.__wrapped__  # hàm GỐC — lấy lại được khi cần\n```\n\nNhững thứ hỏng khi thiếu `wraps`, đều là hỏng thật chứ không chỉ mất thẩm mỹ:\n\n```\n· help() và công cụ sinh tài liệu → vô dụng\n· pytest → mọi bài kiểm thử tên \"bao\", khó lần khi lỗi\n· truy vết lỗi → thấy \"bao\" thay vì tên hàm thật\n· inspect.signature → sai chữ ký\n· pickle → không tuần tự hoá được\n· framework dựa vào __name__ (định tuyến web) → hành vi sai\n```\n\nQuy tắc không có ngoại lệ: **mọi decorator đều dùng `@functools.wraps`.**\n\n### Decorator có tham số — ba tầng\n\n```python\ndef lap_lai(so_lan):                       # ① nhận THAM SỐ\n    def decorator(ham):                    # ② nhận HÀM\n        @functools.wraps(ham)\n        def bao(*a, **k):                  # ③ nhận ĐỐI SỐ của hàm\n            for _ in range(so_lan):\n                kq = ham(*a, **k)\n            return kq\n        return bao\n    return decorator\n\n@lap_lai(3)\ndef chao():\n    print(\"xin chào\")\n```\n\nVì sao phải ba tầng — theo dõi thứ tự thực thi:\n\n```\n@lap_lai(3)\ndef chao(): ...\n\n         ⇕  tương đương\n\ndef chao(): ...\nchao = lap_lai(3)(chao)\n        └───┬───┘└─┬─┘\n            │      └── ② kết quả đó được gọi với hàm chao\n            └───────── ① lap_lai(3) chạy TRƯỚC, trả về decorator\n```\n\n```\n@deco       → deco(ham)          ⇒ 2 tầng\n@deco(x)    → deco(x)(ham)       ⇒ 3 tầng\n```\n\nMẹo nhận biết: **đếm số cặp ngoặc sau tên decorator.** Có ngoặc thì cần thêm một tầng.\n\nMuốn decorator dùng được **cả hai kiểu** (`@deco` và `@deco(x)`):\n\n```python\ndef lap_lai(ham=None, *, so_lan=2):\n    def decorator(f):\n        @functools.wraps(f)\n        def bao(*a, **k):\n            for _ in range(so_lan):\n                kq = f(*a, **k)\n            return kq\n        return bao\n    # Gọi trực tiếp @lap_lai → ham là hàm\n    # Gọi @lap_lai(so_lan=3) → ham là None\n    return decorator if ham is None else decorator(ham)\n\n@lap_lai\ndef a(): ...\n\n@lap_lai(so_lan=5)\ndef b(): ...\n```\n\nDấu `*` ép `so_lan` thành tham số chỉ-tên — đúng kỹ thuật ở bài hàm, và ở đây nó **bắt buộc** để phân biệt được hai cách gọi.\n\n### Xếp chồng decorator\n\n```python\n@a\n@b\n@c\ndef f(): ...\n\n# tương đương\nf = a(b(c(f)))\n```\n\n```\nÁP DỤNG: từ DƯỚI lên — c trước, rồi b, rồi a\nCHẠY:    từ NGOÀI vào — a trước, rồi b, rồi c, rồi f\n```\n\n```python\ndef ghi(ten):\n    def deco(ham):\n        @functools.wraps(ham)\n        def bao(*a, **k):\n            print(f\"→ vào {ten}\")\n            kq = ham(*a, **k)\n            print(f\"← ra {ten}\")\n            return kq\n        return bao\n    return deco\n\n@ghi(\"NGOÀI\")\n@ghi(\"TRONG\")\ndef f():\n    print(\"  thân hàm\")\n\nf()\n# → vào NGOÀI\n# → vào TRONG\n#   thân hàm\n# ← ra TRONG\n# ← ra NGOÀI\n```\n\nThứ tự này có hệ quả thực tế quan trọng:\n\n```python\n# ✓ ĐÚNG: kiểm tra quyền TRƯỚC khi tra cache\n@yeu_cau_dang_nhap\n@cache\ndef lay_du_lieu(uid): ...\n\n# ❌ SAI: cache chạy trước ⇒ trả dữ liệu cache cho người CHƯA đăng nhập\n@cache\n@yeu_cau_dang_nhap\ndef lay_du_lieu(uid): ...\n```\n\nĐây là lỗ hổng bảo mật thật sinh ra từ việc đảo hai dòng. Quy tắc: **decorator kiểm tra và xác thực đặt ở trên cùng.**\n\n### Decorator giữ trạng thái\n\n```python\ndef dem_lan_goi(ham):\n    @functools.wraps(ham)\n    def bao(*a, **k):\n        bao.so_lan += 1                    # ⭐ thuộc tính trên chính hàm bọc\n        return ham(*a, **k)\n    bao.so_lan = 0\n    return bao\n\n@dem_lan_goi\ndef f(): pass\n\nf(); f(); f()\nf.so_lan          # 3\n```\n\nGắn trạng thái vào **thuộc tính của hàm bọc** thay vì dùng `nonlocal` có một lợi ích rõ: người dùng đọc và đặt lại được (`f.so_lan = 0`). `functools.lru_cache` dùng đúng kỹ thuật này để cung cấp `f.cache_info()` và `f.cache_clear()`.\n\n### `functools.lru_cache` — decorator dựng sẵn đáng nhớ nhất\n\n```python\nfrom functools import lru_cache\n\n@lru_cache(maxsize=None)\ndef fib(n):\n    return n if n < 2 else fib(n-1) + fib(n-2)\n\nfib(35)              # tức thì; không có cache thì mất vài giây\nfib.cache_info()     # CacheInfo(hits=33, misses=36, ...)\nfib.cache_clear()\n```\n\nVì sao nhanh đến vậy: `fib(35)` không cache phải gọi khoảng 30 triệu lần vì mỗi nhánh tính lại từ đầu; có cache thì mỗi giá trị `n` chỉ tính đúng một lần — **từ cấp số nhân xuống tuyến tính**.\n\nBa điều kiện bắt buộc để dùng `lru_cache`, và vi phạm điều nào cũng gây lỗi:\n\n```\n① Hàm phải THUẦN: cùng đầu vào luôn cho cùng đầu ra, không tác dụng phụ\n   ⇒ hàm đọc database hay đọc thời gian thì KHÔNG dùng được\n② Mọi đối số phải HASHABLE\n   ⇒ truyền list vào là TypeError (bài list/dict/set)\n③ Cache giữ tham chiếu tới đối số và kết quả\n   ⇒ maxsize=None trên hàm nhiều đầu vào khác nhau là RÒ RỈ BỘ NHỚ\n```\n\nĐiểm ③ đáng nhấn: `maxsize=None` nghĩa là cache **không bao giờ bị dọn**. Với hàm nhận id người dùng làm đối số, cache lớn dần theo số người dùng cho tới khi hết bộ nhớ. Đặt `maxsize=1024` hoặc một con số hợp lý là an toàn hơn nhiều.\n\n### Decorator cho class và class làm decorator\n\n```python\n# ① Class LÀ decorator — khi cần trạng thái phức tạp\nclass DemLanGoi:\n    def __init__(self, ham):\n        functools.update_wrapper(self, ham)     # tương đương wraps\n        self.ham = ham\n        self.so_lan = 0\n\n    def __call__(self, *a, **k):                # ⭐ làm object gọi được\n        self.so_lan += 1\n        return self.ham(*a, **k)\n\n@DemLanGoi\ndef f(): pass\n\n# ② Decorator áp cho CLASS — nhận class, trả class\ndef them_repr(cls):\n    def __repr__(self):\n        truong = \", \".join(f\"{k}={v!r}\" for k, v in vars(self).items())\n        return f\"{cls.__name__}({truong})\"\n    cls.__repr__ = __repr__\n    return cls\n\n@them_repr\nclass Diem:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\nprint(Diem(1, 2))      # Diem(x=1, y=2)\n```\n\nCách ② chính là cơ chế của `@dataclass` trong thư viện chuẩn: nó nhận class, đọc các chú thích kiểu, rồi **sinh ra** `__init__`, `__repr__`, `__eq__` và gắn vào class.\n\n### Khi nào KHÔNG dùng decorator\n\nDecorator dễ bị lạm dụng, nên phần này quan trọng:\n\n```\n❌ Logic nghiệp vụ chính — nó phải nằm trong hàm, đọc thấy được\n❌ Chỉ dùng một lần ở một chỗ — gọi hàm bình thường rõ hơn\n❌ Cần đọc hoặc sửa tham số theo tên — dễ sai và khó bảo trì\n❌ Chồng quá ba tầng — không ai lần được luồng thực thi\n\n✓ Việc chéo chức năng lặp ở nhiều hàm: log, đo, cache, retry, kiểm quyền\n✓ Đăng ký hàm vào một bảng (định tuyến, plugin)\n✓ Sửa hành vi mà không đụng vào mã nguồn hàm gốc\n```\n\nĐánh đổi phải nói rõ: decorator **giấu bớt luồng thực thi**. Người đọc hàm không thấy được chuyện gì xảy ra trước và sau. Với việc phụ trợ thì đó là ưu điểm (tách bạch mối quan tâm); với logic chính thì đó là che giấu. Ranh giới nằm ở câu hỏi: **bỏ decorator đi thì hàm còn đúng nghiệp vụ không?** Còn → dùng decorator hợp lý. Không → nó là logic chính, đưa vào hàm.",

vi_du: "### Bộ ba decorator dùng được ngay\n\n```python\nimport functools, time, logging\n\ndef do_thoi_gian(ham):\n    \"\"\"In thời gian chạy của hàm.\"\"\"\n    @functools.wraps(ham)\n    def bao(*a, **k):\n        t = time.perf_counter()\n        try:\n            return ham(*a, **k)\n        finally:                                    # ⭐ đo cả khi có lỗi\n            print(f\"{ham.__name__}: {time.perf_counter()-t:.4f}s\")\n    return bao\n\n\ndef ghi_log(muc=logging.INFO):\n    \"\"\"Ghi log lời gọi và kết quả.\"\"\"\n    def deco(ham):\n        @functools.wraps(ham)\n        def bao(*a, **k):\n            log = logging.getLogger(ham.__module__)\n            log.log(muc, \"gọi %s(%s)\", ham.__name__,\n                    \", \".join([repr(x) for x in a] +\n                              [f\"{k2}={v!r}\" for k2, v in k.items()]))\n            try:\n                kq = ham(*a, **k)\n            except Exception:\n                log.exception(\"%s ném ngoại lệ\", ham.__name__)\n                raise                               # ⭐ ghi log rồi NÉM LẠI\n            log.log(muc, \"%s → %r\", ham.__name__, kq)\n            return kq\n        return bao\n    return deco\n\n\ndef thu_lai(so_lan=3, cho=1.0, loi=(ConnectionError, TimeoutError)):\n    \"\"\"Thử lại với thời gian chờ tăng gấp đôi.\"\"\"\n    def deco(ham):\n        @functools.wraps(ham)\n        def bao(*a, **k):\n            cuoi = None\n            for lan in range(so_lan):\n                try:\n                    return ham(*a, **k)\n                except loi as e:                    # ⭐ CHỈ lỗi tạm thời\n                    cuoi = e\n                    if lan < so_lan - 1:\n                        time.sleep(cho * 2 ** lan)\n            raise ConnectionError(\n                f\"{ham.__name__} thất bại sau {so_lan} lần\") from cuoi\n        return bao\n    return deco\n\n\n@do_thoi_gian\n@thu_lai(so_lan=3, cho=0.5)\ndef goi_api(url):\n    ...\n```\n\nBa chi tiết đáng học từ đoạn trên:\n\n```\ntry/finally trong do_thoi_gian  → vẫn in thời gian khi hàm ném lỗi\nraise sau log.exception          → ghi log KHÔNG được nuốt ngoại lệ\nloi=(ConnectionError, ...)       → chỉ thử lại lỗi tạm thời (bài ngoại lệ)\n```\n\n### Cache — tự viết rồi so với bản chuẩn\n\n```python\n# Tự viết để hiểu cơ chế\ndef cache_don_gian(ham):\n    kho = {}\n    @functools.wraps(ham)\n    def bao(*a):\n        if a not in kho:            # tuple đối số làm khoá ⇒ phải hashable\n            kho[a] = ham(*a)\n        return kho[a]\n    bao.kho = kho                   # cho phép xem và xoá\n    return bao\n\n@cache_don_gian\ndef fib(n):\n    return n if n < 2 else fib(n-1) + fib(n-2)\n\nfib(50)\nlen(fib.kho)          # 51\n\n# Bản chuẩn — nên dùng cái này\nfrom functools import lru_cache\n\n@lru_cache(maxsize=1024)            # ⭐ có giới hạn, không rò rỉ bộ nhớ\ndef fib(n):\n    return n if n < 2 else fib(n-1) + fib(n-2)\n```\n\nĐo cụ thể để thấy khác biệt:\n\n```python\nimport time\n\ndef fib_cham(n):\n    return n if n < 2 else fib_cham(n-1) + fib_cham(n-2)\n\nt = time.perf_counter(); fib_cham(30)\nprint(f\"không cache: {time.perf_counter()-t:.3f}s\")     # ~0.3s\n\nt = time.perf_counter(); fib(30)\nprint(f\"có cache:    {time.perf_counter()-t:.6f}s\")     # ~0.00002s\n```\n\nVới `n = 35` thì bản không cache mất vài giây, `n = 40` mất gần một phút — trong khi bản có cache vẫn tức thì. Đây là ví dụ rõ nhất trong cả module về việc một dòng code đổi được **bậc độ phức tạp**.\n\nCạm bẫy `lru_cache` cần nhớ:\n\n```python\n@lru_cache\ndef xu_ly(ds):\n    return sum(ds)\n\nxu_ly([1, 2, 3])          # TypeError: unhashable type: 'list'\nxu_ly((1, 2, 3))          # ✓ tuple thì được\n\n@lru_cache\ndef lay_gio():\n    return time.time()     # ❌ hàm KHÔNG thuần — luôn trả giá trị đầu tiên\n```\n\n### Đăng ký hàm vào bảng — mẫu của mọi framework\n\n```python\nBANG = {}\n\ndef dinh_tuyen(duong_dan):\n    def deco(ham):\n        BANG[duong_dan] = ham       # ⭐ đăng ký, KHÔNG bọc gì\n        return ham                   # trả về nguyên hàm gốc\n    return deco\n\n@dinh_tuyen(\"/\")\ndef trang_chu():\n    return \"Trang chủ\"\n\n@dinh_tuyen(\"/ve\")\ndef ve_chung_toi():\n    return \"Giới thiệu\"\n\ndef xu_ly(duong_dan):\n    ham = BANG.get(duong_dan)\n    return ham() if ham else \"404\"\n\nprint(xu_ly(\"/\"))          # Trang chủ\nprint(xu_ly(\"/khong\"))     # 404\n```\n\nĐây là cách `@app.route` của các framework web hoạt động — decorator **không bọc gì cả**, nó chỉ ghi tên hàm vào một `dict` rồi trả hàm về nguyên vẹn. Hiểu 15 dòng này là hiểu cơ chế cốt lõi của định tuyến web.\n\n### Kiểm tra đầu vào\n\n```python\ndef kiem_kieu(**kieu_mong_doi):\n    def deco(ham):\n        @functools.wraps(ham)\n        def bao(*a, **k):\n            import inspect\n            gan = inspect.signature(ham).bind(*a, **k)\n            gan.apply_defaults()\n            for ten, kieu in kieu_mong_doi.items():\n                gt = gan.arguments.get(ten)\n                if gt is not None and not isinstance(gt, kieu):\n                    raise TypeError(\n                        f\"{ham.__name__}(): {ten} phải là {kieu.__name__}, \"\n                        f\"nhận {type(gt).__name__}\")\n            return ham(*a, **k)\n        return bao\n    return deco\n\n@kiem_kieu(tuoi=int, ten=str)\ndef tao(ten, tuoi):\n    return {\"ten\": ten, \"tuoi\": tuoi}\n\ntao(\"Minh\", 22)        # ✓\ntao(\"Minh\", \"22\")      # TypeError: tao(): tuoi phải là int, nhận str\n```\n\n`inspect.signature(...).bind(...)` là kỹ thuật đáng biết: nó ánh xạ đối số thật sang **tên tham số**, nên decorator kiểm tra được theo tên dù người gọi truyền theo vị trí hay theo tên.\n\nNhưng nói thẳng đánh đổi: với nhu cầu kiểm kiểu, **type hint cộng `mypy` tốt hơn** — bắt lỗi lúc viết code chứ không phải lúc chạy, và không tốn thời gian ở mỗi lời gọi. Decorator kiểu này chỉ đáng khi dữ liệu đến từ ngoài lúc chạy.\n\n### Thứ tự xếp chồng — hệ quả bảo mật\n\n```python\ndef yeu_cau_quyen(quyen):\n    def deco(ham):\n        @functools.wraps(ham)\n        def bao(nguoi_dung, *a, **k):\n            if quyen not in nguoi_dung.get(\"quyen\", []):\n                raise PermissionError(f\"cần quyền {quyen!r}\")\n            return ham(nguoi_dung, *a, **k)\n        return bao\n    return deco\n\ndef cache_theo_uid(ham):\n    kho = {}\n    @functools.wraps(ham)\n    def bao(nguoi_dung, *a, **k):\n        khoa = (nguoi_dung[\"id\"], a)\n        if khoa not in kho:\n            kho[khoa] = ham(nguoi_dung, *a, **k)\n        return kho[khoa]\n    return bao\n\n# ✓ ĐÚNG — kiểm quyền ở ngoài cùng, chạy TRƯỚC\n@yeu_cau_quyen(\"admin\")\n@cache_theo_uid\ndef bao_cao(nguoi_dung, thang): ...\n\n# ❌ SAI — cache ở ngoài, trả kết quả cho người chưa đủ quyền\n@cache_theo_uid\n@yeu_cau_quyen(\"admin\")\ndef bao_cao(nguoi_dung, thang): ...\n```\n\nBản sai chỉ khác bản đúng ở thứ tự hai dòng, mà hậu quả là **rò rỉ dữ liệu**: lần đầu một admin gọi thì kết quả vào cache; lần sau một người thường gọi thì cache trả về ngay, không qua kiểm quyền. Quy tắc: **xác thực và kiểm quyền luôn ở trên cùng.**\n\n### Gỡ lỗi decorator\n\n```python\n@do_thoi_gian\ndef f():\n    \"\"\"Tài liệu của f.\"\"\"\n    ...\n\nprint(f.__name__)         # 'f' nếu có wraps, 'bao' nếu không\nprint(f.__wrapped__)      # hàm GỐC — wraps để lại lối quay về\n\n# Gọi hàm gốc, bỏ qua mọi lớp bọc\nf.__wrapped__()\n\n# Đếm số lớp bọc\ng = f\nn = 0\nwhile hasattr(g, \"__wrapped__\"):\n    g = g.__wrapped__\n    n += 1\nprint(f\"{n} lớp decorator, hàm gốc: {g}\")\n```\n\n`__wrapped__` là món quà của `functools.wraps`: nó cho phép **quay ngược về hàm gốc**, rất hữu ích khi kiểm thử muốn bỏ qua lớp cache hoặc lớp retry.\n\n### Decorator chạy lúc nào — chứng minh\n\n```python\nprint(\"① module bắt đầu\")\n\ndef deco(ham):\n    print(f\"② decorator CHẠY cho {ham.__name__}\")\n    @functools.wraps(ham)\n    def bao(*a, **k):\n        print(f\"④ hàm bọc chạy\")\n        return ham(*a, **k)\n    return bao\n\n@deco\ndef f():\n    print(\"⑤ thân hàm\")\n\nprint(\"③ định nghĩa xong, chưa gọi f\")\nf()\n\n# ① module bắt đầu\n# ② decorator CHẠY cho f          ← lúc ĐỊNH NGHĨA\n# ③ định nghĩa xong, chưa gọi f\n# ④ hàm bọc chạy                  ← lúc GỌI\n# ⑤ thân hàm\n```\n\nĐây là thí nghiệm đáng chạy một lần: nó tách bạch **hai thời điểm** mà người mới hay gộp làm một. Nối về bài import: decorator chạy khi module được nạp, nên một decorator chậm làm chậm cả quá trình khởi động.",

so_sanh: "### `@deco` và `@deco(x)`\n\n| | `@deco` | `@deco(x)` |\n|---|---|---|\n| Số tầng hàm | **2** | **3** |\n| Tương đương | `f = deco(f)` | `f = deco(x)(f)` |\n| Nhận vào ở tầng ngoài | hàm | tham số |\n| Nhận biết | không có ngoặc | **có ngoặc** |\n\nMẹo: **đếm ngoặc.** Có ngoặc thì tầng ngoài cùng nhận tham số chứ không nhận hàm, nên phải thêm một tầng.\n\n### Có và không có `functools.wraps`\n\n| | Không có | Có `wraps` |\n|---|---|---|\n| `__name__` | `'bao'` | **tên thật** |\n| `__doc__` | `None` | **giữ nguyên** |\n| `help()` | vô dụng | ✓ |\n| Truy vết lỗi | thấy `bao` | **tên thật** |\n| `pytest` phân biệt test | ✗ | ✓ |\n| `__wrapped__` | ✗ | **✓ quay về hàm gốc** |\n\nKhông có trường hợp nào bỏ `wraps` là đúng. Nó là một dòng và sửa được tất cả các hàng trên.\n\n### Thứ tự xếp chồng\n\n| | Thứ tự áp dụng | Thứ tự chạy |\n|---|---|---|\n| Hướng | **dưới lên** | **ngoài vào** |\n| `@a @b @c def f` | `c`, `b`, `a` | `a`, `b`, `c`, `f` |\n| Gần `def` nhất | áp dụng **trước** | chạy **sau cùng** |\n\nHệ quả thiết kế: **decorator kiểm tra, xác thực, giới hạn tần suất đặt trên cùng** (chạy trước); **decorator cache, đo lường đặt gần `def`**.\n\n### Closure và biến toàn cục\n\n| | Closure | Biến toàn cục |\n|---|---|---|\n| Phạm vi | riêng cho mỗi lần tạo | dùng chung cả chương trình |\n| Nhiều bản độc lập | **✓** | ✗ |\n| Kiểm thử | dễ cô lập | khó, ảnh hưởng lẫn nhau |\n| Gán vào | cần `nonlocal` | cần `global` |\n\nCột trái là lý do closure đáng dùng: `tao_nhan(\"Xin chào\")` và `tao_nhan(\"Tạm biệt\")` cho hai hàm **hoàn toàn độc lập**, mỗi hàm giữ dữ liệu riêng.\n\n### Decorator hàm và class làm decorator\n\n| | Hàm | Class có `__call__` |\n|---|---|---|\n| Cú pháp | ngắn gọn | dài hơn |\n| Giữ trạng thái | qua closure hoặc thuộc tính | **tự nhiên qua `self`** |\n| Trạng thái phức tạp | rối | **✓ rõ ràng** |\n| Giữ metadata | `functools.wraps` | `functools.update_wrapper` |\n| Dùng khi | **đa số trường hợp** | nhiều trạng thái, nhiều phương thức phụ |\n\n### `lru_cache` và cache tự viết\n\n| | Tự viết | `functools.lru_cache` |\n|---|---|---|\n| Giới hạn kích thước | tự làm | **`maxsize`** |\n| Thống kê | tự làm | **`cache_info()`** |\n| Xoá cache | tự làm | **`cache_clear()`** |\n| An toàn đa luồng | ✗ | ✓ |\n| Xử lý tham số tên | phải tự lo | ✓ |\n\nTự viết một lần để hiểu cơ chế; sau đó **luôn dùng bản chuẩn**.\n\nBa điều kiện dùng `lru_cache`, nhắc lại vì hay bị vi phạm: hàm phải **thuần**, đối số phải **hashable**, và `maxsize=None` trên hàm nhiều đầu vào là **rò rỉ bộ nhớ**.\n\n### Khi nào dùng decorator\n\n| Tình huống | Decorator? |\n|---|---|\n| Log, đo thời gian, cache, retry | **✓ đúng chỗ nhất** |\n| Kiểm quyền, giới hạn tần suất | ✓ |\n| Đăng ký hàm vào bảng | ✓ |\n| Logic nghiệp vụ chính | **✗** |\n| Dùng đúng một lần một chỗ | ✗ gọi hàm thường |\n| Cần sửa nhiều tham số theo tên | ✗ khó bảo trì |\n\nCâu hỏi phân định: **bỏ decorator đi thì hàm còn đúng nghiệp vụ không?** Còn đúng (chỉ mất log, mất cache) → decorator hợp lý. Không còn đúng → đó là logic chính, phải nằm trong hàm.",

loi_thuong_gap: [
  "**Quên `return kq` trong hàm bọc.** Hàm chạy đúng nhưng luôn trả `None`, và lỗi hiện ra ở nơi khác.",
  "**Quên `@functools.wraps`.** Mất tên, mất docstring, hỏng `pytest`, hỏng truy vết, hỏng công cụ sinh tài liệu.",
  "**Viết decorator có tham số mà chỉ có hai tầng.** `@deco(x)` cần ba tầng vì `deco(x)` được gọi trước.",
  "**Đặt decorator cache lên trên decorator kiểm quyền.** Cache trả dữ liệu cho người chưa đủ quyền — lỗ hổng bảo mật thật.",
  "**Quên `nonlocal` khi gán vào biến của hàm ngoài.** `UnboundLocalError`, vì Python coi đó là biến cục bộ.",
  "**Tưởng closure bắt giá trị.** Nó bắt **biến**. Đó là lý do `[lambda: i for i in range(3)]` cho `[2,2,2]`.",
  "**Dùng `lru_cache` cho hàm không thuần.** Hàm đọc database, đọc thời gian, sinh số ngẫu nhiên sẽ trả mãi giá trị đầu tiên.",
  "**Dùng `lru_cache` với đối số là `list` hay `dict`.** `TypeError: unhashable`. Đổi sang `tuple`.",
  "**`lru_cache(maxsize=None)` cho hàm nhận nhiều giá trị khác nhau.** Cache không bao giờ dọn — rò rỉ bộ nhớ.",
  "**Ghi log lỗi trong decorator rồi nuốt ngoại lệ.** Phải `raise` lại sau khi ghi.",
  "**Thử lại mọi loại lỗi trong decorator retry.** Chỉ thử lại lỗi tạm thời; lỗi dữ liệu thử lại vô nghĩa.",
  "**Chồng quá ba decorator.** Không ai lần được luồng thực thi.",
  "**Đưa logic nghiệp vụ vào decorator.** Người đọc hàm không thấy được chuyện gì xảy ra.",
  "**Quên rằng decorator chạy lúc import.** Một decorator chậm làm chậm cả quá trình khởi động chương trình.",
  "**Không đo thời gian trong `finally`.** Hàm ném lỗi thì không có số đo — mất đúng lúc cần biết nhất."
],

tu_kiem_tra: [
  "`@deco` phía trên `def f` tương đương với dòng code nào?",
  "Nêu hai tính chất nền tảng mà decorator dựa vào. Mỗi cái là gì?",
  "Closure bắt biến hay bắt giá trị? Điều đó giải thích bẫy nào ở bài hàm?",
  "Vì sao thiếu `nonlocal` khi viết `n += 1` trong hàm trong lại gây `UnboundLocalError`?",
  "Liệt kê năm thứ bị hỏng khi quên `functools.wraps`.",
  "Vì sao `@deco(x)` cần ba tầng hàm còn `@deco` chỉ cần hai?",
  "`@a @b @c def f` tương đương gì? Thứ tự áp dụng và thứ tự chạy khác nhau ra sao?",
  "Vì sao đặt `@cache` trên `@yeu_cau_dang_nhap` là lỗ hổng bảo mật?",
  "Decorator chạy lúc nào — lúc định nghĩa hay lúc gọi? Phần nào chạy mỗi lần gọi?",
  "Nêu ba điều kiện để dùng `lru_cache` an toàn. Vi phạm mỗi cái gây hậu quả gì?",
  "Vì sao `@lru_cache` biến `fib` từ cấp số nhân xuống tuyến tính?",
  "`@app.route` của framework web hoạt động thế nào? Nó có bọc hàm không?",
  "`__wrapped__` dùng để làm gì, và nó đến từ đâu?",
  "Nêu câu hỏi giúp phân định khi nào nên và không nên dùng decorator."
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Function definitions → Decorators* trong tham khảo ngôn ngữ — định nghĩa chính xác của cú pháp `@`.",
  "Tài liệu Python, module `functools` — `wraps`, `lru_cache`, `cache`, `partial`, `singledispatch` đều là công cụ quanh chủ đề này.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về decorator và closure là giải thích đầy đủ nhất, gồm cả thời điểm thực thi.",
  "Sách **Effective Python** — Brett Slatkin. Có mục riêng về việc luôn dùng `functools.wraps`, giải thích từng thứ bị hỏng.",
  "Tài liệu Python, module `inspect` — `signature`, `bind`, `unwrap` khi cần viết decorator làm việc với tham số theo tên."
],

lien_he: [
  "**Hàm, tham số mặc định, `*args`, `**kwargs`** trước đó: decorator dựng hoàn toàn trên chúng; bẫy `lambda` trong vòng lặp cũng là closure.",
  "**Generator, iterator và `yield`** ngay sau: cũng dựa trên ý tưởng hàm giữ được trạng thái giữa các lần gọi.",
  "**Context manager và `with`** sau nữa: `@contextmanager` là decorator biến một generator thành context manager — hai bài gặp nhau ở đó.",
  "**Xử lý ngoại lệ**: decorator retry và decorator ghi log là hai ứng dụng chính, và nguyên tắc \"chỉ thử lại lỗi tạm thời\" đến từ đó.",
  "**Module, package, import**: decorator chạy lúc module được nạp — cùng thời điểm với mọi code ở tầng module.",
  "**List, dict, set, tuple**: `lru_cache` đòi hỏi đối số hashable, đúng vì nó dùng `dict` bên trong.",
  "**Type hint và mypy** ở cuối module: kiểm kiểu bằng type hint tốt hơn bằng decorator kiểm tra lúc chạy.",
  "**Lập trình hướng đối tượng** ở module 6 mảng 01: `@property`, `@staticmethod`, `@classmethod`, `@dataclass` đều là decorator.",
  "**Backend & Web** ở mảng 05: `@app.route`, middleware xác thực, giới hạn tần suất — toàn bộ là bài này áp dụng.",
  "**AI/ML** ở mảng 07: decorator đo thời gian và cache dùng liên tục khi thử nghiệm mô hình và tiền xử lý dữ liệu."
]

};
