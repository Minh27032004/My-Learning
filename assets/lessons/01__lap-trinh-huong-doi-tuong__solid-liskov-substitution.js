window.LESSONS["01:lap-trinh-huong-doi-tuong:solid-liskov-substitution"] = {

tom_tat: [
  "**Lớp con phải thay được lớp cha ở mọi chỗ mà không làm chương trình sai.** Đây là điều kiện để kế thừa và đa hình thật sự hoạt động — không có nó thì `if isinstance` quay lại.",
  "**Vi phạm rõ nhất: lớp con `raise NotImplementedError` cho phương thức của cha.** Nó công khai từ chối một phần giao ước mà code gọi đang dựa vào.",
  "**Ba luật cụ thể**: lớp con **không được siết chặt** điều kiện đầu vào, **không được nới lỏng** cam kết đầu ra, và **phải giữ bất biến** của lớp cha.",
  "**Hình vuông kế thừa hình chữ nhật là ví dụ kinh điển**: đúng về toán, sai về code — vì `HinhChuNhat` cam kết ngầm rằng đổi chiều rộng không đổi chiều cao.",
  "**Vi phạm Liskov thường là dấu hiệu quan hệ \"là một\" sai**, và cách chữa gần như luôn là tách giao diện hoặc chuyển sang composition."
],

can_biet_truoc: [
  "Bài **Kế thừa và MRO** — dấu hiệu \"lớp con raise NotImplementedError\" đã nhắc ở đó; bài này giải thích vì sao nó nghiêm trọng.",
  "Bài **Đa hình và duck typing** — Liskov là điều kiện để đa hình đúng đắn.",
  "Bài **Composition over inheritance** — cách chữa vi phạm Liskov thường là chuyển sang composition.",
  "Bài **SOLID — Open/Closed** ngay trước — OCP chỉ hoạt động nếu các bản cài đặt thay thế nhau được, tức là tuân thủ Liskov."
],

dinh_nghia: "Nói đời thường: bạn thuê một chiếc xe và được đưa cho \"xe cao cấp hơn\". Nếu xe đó **không lùi được**, hoặc **chỉ chạy khi trời không mưa**, thì nó không thật sự là một chiếc xe dùng thay được — dù về danh nghĩa nó là xe.\n\nNói chính xác:\n\n- **LSP** (*Liskov Substitution Principle*) — nếu `S` là lớp con của `T`, thì **mọi chỗ dùng `T` đều thay bằng `S` được** mà chương trình vẫn đúng.\n- **Điều kiện đầu vào** (*precondition*) — điều kiện mà người gọi phải thoả để phương thức chạy đúng.\n- **Cam kết đầu ra** (*postcondition*) — điều mà phương thức hứa sẽ đúng sau khi chạy.\n- **Bất biến** — điều luôn đúng về trạng thái object, ở mọi thời điểm nhìn từ bên ngoài.\n- **Giao ước hành vi** (*behavioral contract*) — không chỉ chữ ký hàm, mà cả **những gì phương thức hứa sẽ làm**. Liskov nói về giao ước này.\n\nĐiểm cần nắm ngay: **Liskov nói về HÀNH VI, không phải về chữ ký.** Một lớp con có đúng mọi tên phương thức, đúng mọi kiểu tham số, và `mypy` không phàn nàn gì — mà vẫn vi phạm Liskov nặng nề. Trình biên dịch và công cụ kiểm kiểu **không bắt được** loại lỗi này; chỉ có suy nghĩ về ngữ nghĩa mới bắt được.",

vi_sao_can: "Liskov là nguyên tắc quyết định kế thừa và đa hình có thật sự dùng được hay không:\n\n- **Vi phạm Liskov làm `if isinstance` quay lại.** Code gọi phải kiểm tra \"nếu là loại này thì làm khác\" — và như vậy là mất toàn bộ lợi ích của đa hình, quay về đúng thứ Open/Closed cố loại bỏ.\n- **Nó gây lỗi ở chỗ xa nguyên nhân.** Hàm viết đúng theo giao ước của lớp cha, chạy tốt nhiều tháng, rồi hỏng khi ai đó truyền vào một lớp con mới. Người viết hàm không sai, người viết lớp con cũng tưởng mình không sai.\n- **Nó là điều kiện của Open/Closed.** Thêm loại mới không sửa code cũ chỉ đúng **nếu** loại mới hành xử đúng giao ước. Không thì thêm loại mới vẫn phải sửa code cũ.\n- **Nó chỉ ra kế thừa sai bản chất.** Vi phạm Liskov gần như luôn có nghĩa quan hệ \"là một\" không đúng, và đó là tín hiệu để nhìn lại thiết kế.\n- **Công cụ không bắt được.** `mypy` kiểm tra chữ ký, không kiểm tra lời hứa. Đây là loại lỗi chỉ con người phát hiện được.\n\nTình huống thật rất kinh điển: bạn có `KhoLuuTru` với `luu()` và `doc()`. Sau đó thêm `KhoChiDoc` kế thừa nó, và `luu()` ném `NotImplementedError`. Mọi hàm nhận `KhoLuuTru` giờ **có thể đổ** tuỳ vào object thật được truyền vào — mà không hàm nào trong số đó viết sai. Phần cơ chế sẽ chỉ rõ vì sao đây là lỗi thiết kế chứ không phải lỗi cài đặt, và cách sửa đúng.",

co_che: "### Phát biểu và cách kiểm tra\n\n```\nNếu S là lớp con của T:\n  mọi chương trình dùng T đều phải chạy đúng khi thay bằng S\n  mà NGƯỜI GỌI không cần biết gì.\n```\n\nPhép kiểm thực dụng nhất:\n\n```\nViết một hàm nhận lớp CHA.\nTruyền lần lượt MỌI lớp con vào.\nCó lớp nào làm hàm sai, đổ, hoặc cần xử lý riêng không?\n  → Có: vi phạm Liskov\n```\n\nĐiểm mấu chốt: **người gọi không được cần biết** mình đang cầm lớp con nào. Ngay khi code gọi phải viết `if isinstance(x, LoaiDacBiet)`, giao ước đã vỡ.\n\n### Vi phạm dễ thấy nhất: từ chối một phương thức\n\n```python\nclass KhoLuuTru:\n    def doc(self, khoa: str) -> bytes | None: ...\n    def luu(self, khoa: str, gt: bytes) -> None: ...\n    def xoa(self, khoa: str) -> bool: ...\n\nclass KhoChiDoc(KhoLuuTru):\n    def doc(self, khoa): ...\n    def luu(self, khoa, gt):\n        raise NotImplementedError(\"kho này chỉ đọc\")     # ❌ VI PHẠM\n    def xoa(self, khoa):\n        raise NotImplementedError(\"kho này chỉ đọc\")     # ❌\n```\n\nVì sao đây là lỗi **thiết kế** chứ không phải lỗi cài đặt:\n\n```python\ndef sao_luu(nguon: KhoLuuTru, dich: KhoLuuTru, khoa: list[str]) -> None:\n    for k in khoa:\n        gt = nguon.doc(k)\n        if gt is not None:\n            dich.luu(k, gt)          # ⚠️ đổ nếu dich là KhoChiDoc\n```\n\nHàm `sao_luu` **viết hoàn toàn đúng** theo giao ước của `KhoLuuTru`. Nó không sai. Nhưng nó đổ — và không có cách nào để nó biết trước, ngoài việc kiểm tra kiểu, thứ mà đa hình sinh ra để loại bỏ.\n\nCách sửa: **tách giao diện theo năng lực**.\n\n```python\nfrom typing import Protocol\n\nclass KhoDocDuoc(Protocol):\n    def doc(self, khoa: str) -> bytes | None: ...\n\nclass KhoGhiDuoc(KhoDocDuoc, Protocol):\n    def luu(self, khoa: str, gt: bytes) -> None: ...\n    def xoa(self, khoa: str) -> bool: ...\n\n\ndef sao_luu(nguon: KhoDocDuoc, dich: KhoGhiDuoc, khoa: list[str]) -> None:\n    #             ⭐ nêu rõ CẦN gì            ⭐ mypy chặn nếu truyền kho chỉ đọc\n    for k in khoa:\n        gt = nguon.doc(k)\n        if gt is not None:\n            dich.luu(k, gt)\n```\n\nGiờ `KhoChiDoc` chỉ cài `doc` — nó **không hứa** điều nó không làm được, và `mypy` chặn ngay lúc viết code nếu ai đó truyền nó vào chỗ cần ghi.\n\nĐây cũng chính là Interface Segregation — bài ngay sau — và hai nguyên tắc này thường gặp nhau ở cùng một chỗ.\n\n### Ba luật cụ thể\n\n**Luật ①: không siết chặt điều kiện đầu vào**\n\nLớp con không được đòi hỏi **nhiều hơn** lớp cha.\n\n```python\nclass BoXuLy:\n    def xu_ly(self, ds: list[int]) -> int:\n        \"\"\"Nhận danh sách số nguyên bất kỳ, kể cả rỗng.\"\"\"\n        return sum(ds)\n\nclass BoXuLySai(BoXuLy):\n    def xu_ly(self, ds):\n        if not ds:\n            raise ValueError(\"không nhận danh sách rỗng\")     # ❌ SIẾT chặt\n        if any(x < 0 for x in ds):\n            raise ValueError(\"không nhận số âm\")               # ❌ SIẾT chặt\n        return sum(ds)\n```\n\nCode gọi được viết theo lớp cha, nên nó **có quyền** truyền `[]` và số âm. Lớp con từ chối là phá vỡ mong đợi đó.\n\n```python\ndef tong_an_toan(bo: BoXuLy, ds: list[int]) -> int:\n    return bo.xu_ly(ds)       # đúng với BoXuLy, đổ với BoXuLySai\n\ntong_an_toan(BoXuLy(), [])          # 0 ✓\ntong_an_toan(BoXuLySai(), [])       # ValueError ✗\n```\n\nChiều ngược lại thì **hợp lệ**: lớp con **nới lỏng** đầu vào — nhận thêm những thứ lớp cha không nhận. Người gọi vẫn dùng được như cũ.\n\n**Luật ②: không nới lỏng cam kết đầu ra**\n\nLớp con phải hứa **ít nhất** những gì lớp cha hứa.\n\n```python\nclass BoTim:\n    def tim(self, tu_khoa: str) -> list[dict]:\n        \"\"\"Trả về danh sách kết quả, đã sắp xếp theo độ liên quan giảm dần.\n        Không bao giờ trả None.\"\"\"\n        return [...]\n\nclass BoTimSai(BoTim):\n    def tim(self, tu_khoa):\n        if not ket_qua:\n            return None              # ❌ cha hứa KHÔNG BAO GIỜ trả None\n        return ket_qua               # ❌ và không sắp xếp\n```\n\n```python\ndef hien_thi(bo: BoTim, tu: str) -> None:\n    for kq in bo.tim(tu)[:10]:       # ⚠️ TypeError nếu trả None\n        print(kq[\"tieu_de\"])\n```\n\nChú ý: cam kết \"đã sắp xếp\" nằm trong **docstring**, không nằm trong chữ ký. `mypy` không bắt được việc lớp con không sắp xếp — nhưng code gọi vẫn dựa vào nó, và lỗi biểu hiện là \"kết quả hiển thị sai thứ tự\", rất khó lần.\n\nChiều ngược lại **hợp lệ**: lớp con hứa **nhiều hơn** — ví dụ đảm bảo thêm rằng kết quả không trùng lặp. Người gọi không dùng tới cũng không sao.\n\n**Luật ③: giữ bất biến của lớp cha**\n\n```python\nclass TaiKhoan:\n    \"\"\"Bất biến: so_du >= 0 ở mọi thời điểm.\"\"\"\n    def __init__(self, so_du: float = 0):\n        self._so_du = so_du\n\n    def rut(self, tien: float) -> None:\n        if tien > self._so_du:\n            raise ValueError(\"không đủ số dư\")\n        self._so_du -= tien\n\n    @property\n    def so_du(self) -> float:\n        return self._so_du\n\n\nclass TaiKhoanThauChi(TaiKhoan):\n    def __init__(self, so_du=0, han_muc=1_000_000):\n        super().__init__(so_du)\n        self.han_muc = han_muc\n\n    def rut(self, tien):\n        if tien > self._so_du + self.han_muc:\n            raise ValueError(\"vượt hạn mức thấu chi\")\n        self._so_du -= tien          # ❌ so_du có thể ÂM — phá bất biến của cha\n```\n\n```python\ndef bao_cao(tk: TaiKhoan) -> str:\n    # Viết dựa trên bất biến \"so_du >= 0\"\n    return f\"Số dư: {tk.so_du:,.0f}đ ({tk.so_du / 1_000_000:.1f} triệu)\"\n\n# Với TaiKhoanThauChi sau khi thấu chi ⇒ hiện số âm, và mọi phép tính\n# giả định số dương đều sai\n```\n\nBất biến là **lời hứa mạnh nhất** vì rất nhiều code dựa vào nó mà không nói ra. Lớp con phá bất biến là phá thứ không ai kiểm tra.\n\nCách sửa: đây là **hai loại tài khoản khác nhau**, không phải quan hệ cha–con.\n\n```python\nclass TaiKhoanCoSo(Protocol):\n    @property\n    def so_du(self) -> float: ...\n    def rut(self, tien: float) -> None: ...\n    def nap(self, tien: float) -> None: ...\n\nclass TaiKhoanThuong:\n    \"\"\"Bất biến: so_du >= 0\"\"\"\n    ...\n\nclass TaiKhoanThauChi:\n    \"\"\"Bất biến: so_du >= -han_muc\"\"\"\n    ...\n```\n\nHai class ngang hàng, mỗi cái nêu rõ bất biến của mình. Code gọi cần biết chắc số dư không âm thì nhận `TaiKhoanThuong`, không nhận giao diện chung.\n\n### Hình vuông và hình chữ nhật — ví dụ kinh điển\n\n```python\nclass HinhChuNhat:\n    def __init__(self, rong: float, cao: float):\n        self._rong, self._cao = rong, cao\n\n    @property\n    def rong(self): return self._rong\n    @rong.setter\n    def rong(self, gt): self._rong = gt\n\n    @property\n    def cao(self): return self._cao\n    @cao.setter\n    def cao(self, gt): self._cao = gt\n\n    @property\n    def dien_tich(self): return self._rong * self._cao\n\n\nclass HinhVuong(HinhChuNhat):\n    \"\"\"Về TOÁN HỌC: hình vuông LÀ MỘT hình chữ nhật.\"\"\"\n    def __init__(self, canh): super().__init__(canh, canh)\n\n    @HinhChuNhat.rong.setter\n    def rong(self, gt):\n        self._rong = self._cao = gt      # giữ tính vuông\n\n    @HinhChuNhat.cao.setter\n    def cao(self, gt):\n        self._rong = self._cao = gt\n```\n\n```python\ndef thu_dien_tich(h: HinhChuNhat) -> None:\n    h.rong = 5\n    h.cao = 4\n    assert h.dien_tich == 20, f\"mong đợi 20, nhận {h.dien_tich}\"\n\nthu_dien_tich(HinhChuNhat(1, 1))     # ✓\nthu_dien_tich(HinhVuong(1))          # AssertionError: nhận 16 ✗\n```\n\nĐiều đáng suy nghĩ: **về toán học thì hình vuông đúng là hình chữ nhật.** Vậy sai ở đâu?\n\nSai ở chỗ `HinhChuNhat` có một **cam kết ngầm** mà toán học không có:\n\n```\n\"Đổi chiều rộng KHÔNG làm đổi chiều cao.\"\n```\n\nCam kết này không viết ở đâu cả, nhưng mọi code dùng `HinhChuNhat` đều dựa vào nó. `HinhVuong` không giữ được nó.\n\nBài học rộng hơn: **quan hệ \"là một\" trong đời thực không tự động đúng trong code.** Điều quyết định là **hành vi có thay thế được không**, không phải phân loại khái niệm.\n\nCách sửa — bỏ setter, cho object bất biến:\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass HinhChuNhat:\n    rong: float\n    cao: float\n    @property\n    def dien_tich(self): return self.rong * self.cao\n\n@dataclass(frozen=True)\nclass HinhVuong:                      # ⭐ KHÔNG kế thừa\n    canh: float\n    @property\n    def dien_tich(self): return self.canh ** 2\n```\n\nKhông có setter thì không có cam kết ngầm để phá — **bất biến loại bỏ cả một lớp vi phạm Liskov**. Đây là lý do sâu xa vì sao object bất biến làm thiết kế đơn giản hơn.\n\n### Chim cánh cụt — vi phạm dạng thứ hai\n\n```python\nclass Chim:\n    def bay(self): ...\n\nclass ChimCanhCut(Chim):\n    def bay(self):\n        raise NotImplementedError(\"chim cánh cụt không bay được\")   # ❌\n```\n\nSai ở chỗ đặt `bay()` vào `Chim` — nó giả định **mọi** chim đều bay. Sửa bằng cách tách theo năng lực:\n\n```python\nclass Chim:\n    def an(self): ...\n    def keu(self): ...\n\nclass ChimBayDuoc(Chim):\n    def bay(self): ...\n\nclass ChimBoi(Chim):\n    def boi(self): ...\n\nclass ChimSe(ChimBayDuoc): ...\nclass ChimCanhCut(ChimBoi): ...\n\ndef cho_bay(c: ChimBayDuoc):      # ⭐ nêu rõ cần loại nào\n    c.bay()\n```\n\nNguyên tắc rút ra: **đặt một phương thức vào lớp cha là hứa rằng MỌI lớp con làm được nó.** Không chắc thì đừng đặt vào đó.\n\n### Vi phạm ở kiểu trả về và ngoại lệ\n\n```python\nclass BoDoc:\n    def doc(self, dd: str) -> str:\n        \"\"\"Raises: FileNotFoundError nếu không có file.\"\"\"\n        ...\n\nclass BoDocMang(BoDoc):\n    def doc(self, dd):\n        raise ConnectionError(\"mất mạng\")     # ❌ ngoại lệ MỚI, cha không nêu\n```\n\n```python\ntry:\n    noi_dung = bo.doc(dd)\nexcept FileNotFoundError:            # người gọi chỉ chuẩn bị cho cái này\n    noi_dung = \"\"\n# ConnectionError bay lên và làm đổ chương trình\n```\n\nDanh sách ngoại lệ cũng là **một phần của giao ước**. Lớp con ném loại mới là buộc mọi code gọi phải sửa — đúng thứ Liskov ngăn.\n\nCách sửa: bọc lại thành ngoại lệ nằm trong giao ước, giữ nguyên nhân gốc.\n\n```python\nclass BoDocMang(BoDoc):\n    def doc(self, dd):\n        try:\n            return self._tai(dd)\n        except ConnectionError as e:\n            raise FileNotFoundError(f\"không đọc được {dd}\") from e   # ⭐\n```\n\nĐây là mẫu `raise ... from e` ở bài ngoại lệ, dùng đúng chỗ: giữ giao ước với người gọi mà không mất thông tin gỡ lỗi.\n\n### Dấu hiệu nhận biết\n\n```\n① Lớp con raise NotImplementedError cho phương thức của cha\n② Lớp con để phương thức TRỐNG (\"không làm gì\")\n③ Code gọi phải kiểm tra isinstance để xử lý riêng\n④ Lớp con thêm điều kiện kiểm tra đầu vào mà cha không có\n⑤ Lớp con ném loại ngoại lệ mà cha không nêu\n⑥ Tài liệu lớp con nói \"khác với lớp cha ở chỗ...\"\n⑦ Lớp con ghi đè và KHÔNG gọi super() khi vẫn cần logic cha\n```\n\nDấu hiệu ③ là dấu hiệu **hậu quả**: khi bạn thấy mình viết `if isinstance(...)` trong code gọi, gần như chắc chắn có vi phạm Liskov ở đâu đó phía trên.\n\nDấu hiệu ⑥ tinh tế mà rất đáng chú ý: nếu tài liệu phải giải thích lớp con hành xử khác lớp cha thế nào, thì người gọi **cần biết** mình đang cầm loại nào — và như vậy là không thay thế được.\n\n### Ba cách sửa\n\n```\n① Tách giao diện theo NĂNG LỰC\n   KhoLuuTru → KhoDocDuoc + KhoGhiDuoc\n\n② Chuyển sang COMPOSITION\n   TaiKhoanThauChi KHÔNG kế thừa TaiKhoan; cả hai cài cùng Protocol\n\n③ Làm object BẤT BIẾN\n   Bỏ setter ⇒ không còn cam kết ngầm để phá\n```\n\nCách ③ ít được nhắc nhưng mạnh nhất: **phần lớn vi phạm Liskov liên quan tới việc thay đổi trạng thái.** Object bất biến loại bỏ cả lớp vấn đề đó — và đây là lý do sâu xa vì sao `frozen=True` ở bài dataclass đáng làm mặc định khi có thể.\n\n### Ghi giao ước ra thành lời\n\nVì công cụ không bắt được vi phạm Liskov, cách phòng duy nhất là **viết giao ước ra**:\n\n```python\nclass KhoLuuTru(Protocol):\n    def doc(self, khoa: str) -> bytes | None:\n        \"\"\"Đọc giá trị theo khoá.\n\n        Điều kiện: khoa không rỗng.\n        Cam kết:   trả về bytes nếu có, None nếu không. KHÔNG ném ngoại lệ\n                   khi khoá không tồn tại.\n        Ngoại lệ:  OSError nếu kho không truy cập được.\n        \"\"\"\n```\n\nBa mục — điều kiện, cam kết, ngoại lệ — là ba thứ Liskov nói tới. Viết chúng ra cho ba lợi ích:\n\n```\n· Người viết lớp con biết mình phải giữ gì\n· Người gọi biết mình được dựa vào gì\n· Người review có tiêu chí khách quan để đối chiếu\n```\n\nKiểm thử giao ước là bước tiếp theo — phần ví dụ sẽ chỉ cách viết một bộ kiểm thử chạy cho **mọi** bản cài đặt.",

vi_du: "### Phép thử thay thế\n\n```python\nfrom typing import Protocol\n\nclass BoXuLy(Protocol):\n    def xu_ly(self, ds: list[int]) -> int: ...\n\nclass Tong:\n    def xu_ly(self, ds): return sum(ds)\n\nclass Tich:\n    def xu_ly(self, ds):\n        kq = 1\n        for x in ds: kq *= x\n        return kq\n\nclass LonNhat:\n    def xu_ly(self, ds):\n        return max(ds)          # ⚠️ đổ với danh sách rỗng\n\n\ndef kiem_thay_the(*bo: BoXuLy) -> None:\n    \"\"\"Chạy CÙNG bộ dữ liệu qua MỌI bản cài đặt.\"\"\"\n    du_lieu = [[], [1], [1, 2, 3], [-1, 0, 5]]\n    for b in bo:\n        for ds in du_lieu:\n            try:\n                b.xu_ly(ds)\n            except Exception as e:\n                print(f\"⚠️ {type(b).__name__} đổ với {ds!r}: \"\n                      f\"{type(e).__name__}: {e}\")\n\nkiem_thay_the(Tong(), Tich(), LonNhat())\n# ⚠️ LonNhat đổ với []: ValueError: max() arg is an empty sequence\n```\n\n`LonNhat` **siết chặt điều kiện đầu vào** so với giao ước: giao diện nói \"nhận `list[int]`\", không nói \"phải không rỗng\".\n\nHai cách sửa, và cách nào đúng phụ thuộc vào giao ước bạn muốn:\n\n```python\n# ✓ ① Nới giao ước — bản cài đặt tự xử lý trường hợp rỗng\nclass LonNhat:\n    def xu_ly(self, ds): return max(ds, default=0)\n\n# ✓ ② Siết giao ước Ở LỚP CHA — nói rõ ngay từ đầu\nclass BoXuLy(Protocol):\n    def xu_ly(self, ds: list[int]) -> int:\n        \"\"\"Điều kiện: ds KHÔNG rỗng.\"\"\"\n```\n\nCách ② hợp lệ vì nó đặt điều kiện **ở giao diện**, nên mọi người gọi đều biết. Vi phạm Liskov là khi **lớp con** thêm điều kiện mà lớp cha không nêu.\n\n### Kiểm thử giao ước — chạy cho mọi bản cài đặt\n\n```python\nimport pytest\nfrom typing import Protocol\n\nclass KhoLuuTru(Protocol):\n    def luu(self, khoa: str, gt: bytes) -> None: ...\n    def doc(self, khoa: str) -> bytes | None: ...\n    def xoa(self, khoa: str) -> bool: ...\n\n\nclass KhoBoNho:\n    def __init__(self): self._d: dict[str, bytes] = {}\n    def luu(self, khoa, gt): self._d[khoa] = gt\n    def doc(self, khoa): return self._d.get(khoa)\n    def xoa(self, khoa): return self._d.pop(khoa, None) is not None\n\nclass KhoCoTienTo:\n    def __init__(self, tien_to=\"app:\"):\n        self._d, self.tien_to = {}, tien_to\n    def luu(self, khoa, gt): self._d[self.tien_to + khoa] = gt\n    def doc(self, khoa): return self._d.get(self.tien_to + khoa)\n    def xoa(self, khoa): return self._d.pop(self.tien_to + khoa, None) is not None\n\n\n# ⭐ MỘT bộ kiểm thử, chạy cho MỌI bản cài đặt\n@pytest.fixture(params=[KhoBoNho, KhoCoTienTo])\ndef kho(request):\n    return request.param()\n\n\nclass TestGiaoUocKho:\n    def test_luu_roi_doc_lai_dung_gia_tri(self, kho):\n        kho.luu(\"a\", b\"x\")\n        assert kho.doc(\"a\") == b\"x\"\n\n    def test_doc_khoa_khong_ton_tai_tra_None_khong_nem(self, kho):\n        assert kho.doc(\"chua_co\") is None      # ⭐ cam kết: KHÔNG ném\n\n    def test_ghi_de_lay_gia_tri_moi(self, kho):\n        kho.luu(\"a\", b\"cu\"); kho.luu(\"a\", b\"moi\")\n        assert kho.doc(\"a\") == b\"moi\"\n\n    def test_xoa_tra_True_khi_co_False_khi_khong(self, kho):\n        kho.luu(\"a\", b\"x\")\n        assert kho.xoa(\"a\") is True\n        assert kho.xoa(\"a\") is False\n\n    def test_sau_khi_xoa_doc_tra_None(self, kho):\n        kho.luu(\"a\", b\"x\"); kho.xoa(\"a\")\n        assert kho.doc(\"a\") is None\n```\n\nĐây là kỹ thuật giá trị nhất trong cả bài: **bộ kiểm thử giao ước** chạy cùng một tập kiểm tra cho mọi bản cài đặt. Thêm một `Kho` mới là thêm nó vào `params` — và nếu nó vi phạm giao ước, bài kiểm thử **chỉ ra chính xác điều khoản nào bị vi phạm**.\n\nĐây là cách duy nhất tự động hoá được việc kiểm tra Liskov, vì `mypy` chỉ thấy chữ ký.\n\n```python\n# Thêm một bản cài đặt vi phạm để thấy nó bị bắt\nclass KhoNemKhiThieu:\n    def __init__(self): self._d = {}\n    def luu(self, khoa, gt): self._d[khoa] = gt\n    def doc(self, khoa):\n        return self._d[khoa]              # ❌ ném KeyError thay vì trả None\n    def xoa(self, khoa): return self._d.pop(khoa, None) is not None\n\n# Thêm vào params ⇒ test_doc_khoa_khong_ton_tai_tra_None_khong_nem THẤT BẠI\n```\n\n### Hình vuông — chạy thử để thấy\n\n```python\nclass HinhChuNhat:\n    def __init__(self, rong, cao):\n        self._rong, self._cao = rong, cao\n\n    @property\n    def rong(self): return self._rong\n    @rong.setter\n    def rong(self, v): self._rong = v\n\n    @property\n    def cao(self): return self._cao\n    @cao.setter\n    def cao(self, v): self._cao = v\n\n    @property\n    def dien_tich(self): return self._rong * self._cao\n\n\nclass HinhVuong(HinhChuNhat):\n    def __init__(self, canh): super().__init__(canh, canh)\n\n    @HinhChuNhat.rong.setter\n    def rong(self, v): self._rong = self._cao = v\n\n    @HinhChuNhat.cao.setter\n    def cao(self, v): self._rong = self._cao = v\n\n\ndef doi_kich_thuoc(h: HinhChuNhat, r: float, c: float) -> float:\n    h.rong = r\n    h.cao = c\n    return h.dien_tich\n\nprint(doi_kich_thuoc(HinhChuNhat(1, 1), 5, 4))    # 20 ✓\nprint(doi_kich_thuoc(HinhVuong(1), 5, 4))         # 16 ✗ — mong đợi 20\n```\n\nHàm `doi_kich_thuoc` **không sai**. Nó dựa trên cam kết ngầm mà `HinhChuNhat` tạo ra bằng việc có hai setter độc lập.\n\n```python\n# ✓ Sửa: bất biến, không kế thừa\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass HinhChuNhat:\n    rong: float\n    cao: float\n\n    @property\n    def dien_tich(self): return self.rong * self.cao\n\n    def voi_kich_thuoc(self, rong=None, cao=None) -> \"HinhChuNhat\":\n        return HinhChuNhat(rong if rong is not None else self.rong,\n                           cao if cao is not None else self.cao)\n\n@dataclass(frozen=True)\nclass HinhVuong:\n    canh: float\n\n    @property\n    def dien_tich(self): return self.canh ** 2\n\n\n# Cần đa hình thì dùng Protocol theo NĂNG LỰC THẬT\nclass CoDienTich(Protocol):\n    @property\n    def dien_tich(self) -> float: ...\n\ndef tong_dien_tich(ds: list[CoDienTich]) -> float:\n    return sum(h.dien_tich for h in ds)\n\ntong_dien_tich([HinhChuNhat(2, 3), HinhVuong(4)])     # 22.0 ✓\n```\n\nCả hai đều \"có diện tích\", và đó là điểm chung **thật sự**. Ép chúng vào quan hệ cha–con là tạo ra một cam kết không giữ được.\n\n### Vi phạm về ngoại lệ\n\n```python\nfrom typing import Protocol\n\nclass NguonCauHinh(Protocol):\n    def doc(self) -> dict:\n        \"\"\"Đọc cấu hình.\n\n        Cam kết:  trả về dict, có thể rỗng.\n        Ngoại lệ: ValueError nếu nội dung sai định dạng.\n                  KHÔNG ném loại nào khác.\n        \"\"\"\n\nclass NguonFile:\n    def __init__(self, p): self.p = p\n    def doc(self):\n        import json\n        from pathlib import Path\n        try:\n            return json.loads(Path(self.p).read_text(encoding=\"utf-8\"))\n        except FileNotFoundError:\n            return {}                                    # ⭐ giữ cam kết\n        except json.JSONDecodeError as e:\n            raise ValueError(f\"{self.p}: JSON sai dòng {e.lineno}\") from e\n\nclass NguonMang:\n    def __init__(self, url): self.url = url\n    def doc(self):\n        try:\n            return self._tai()\n        except (ConnectionError, TimeoutError) as e:\n            raise ValueError(f\"không tải được {self.url}\") from e   # ⭐ bọc lại\n\n\ndef nap(nguon: NguonCauHinh, mac_dinh: dict) -> dict:\n    try:\n        return {**mac_dinh, **nguon.doc()}\n    except ValueError as e:\n        print(f\"dùng cấu hình mặc định: {e}\")\n        return dict(mac_dinh)\n\nnap(NguonFile(\"khong_co.json\"), {\"port\": 8000})     # {'port': 8000} ✓\nnap(NguonMang(\"http://x\"), {\"port\": 8000})          # ✓ không đổ\n```\n\nCả hai bản cài đặt **quy mọi lỗi về `ValueError`** như giao ước nêu, và dùng `from e` để giữ nguyên nhân gốc cho việc gỡ lỗi. Hàm `nap` chỉ cần chuẩn bị cho một loại ngoại lệ.\n\nKhông có kỷ luật này, `nap` sẽ phải bắt `Exception` — và như vậy là che luôn mọi lỗi lập trình, đúng phản mẫu ở bài xử lý ngoại lệ.\n\n### Danh sách chỉ đọc — vi phạm tinh vi\n\n```python\n# ❌ Kế thừa list rồi chặn phương thức\nclass DanhSachChiDoc(list):\n    def append(self, x):\n        raise TypeError(\"chỉ đọc\")       # ❌ vi phạm Liskov\n    def __setitem__(self, i, v):\n        raise TypeError(\"chỉ đọc\")\n\ndef them_muc(ds: list, x) -> None:\n    ds.append(x)                          # đúng với list, đổ với DanhSachChiDoc\n\nthem_muc([1, 2], 3)                       # ✓\nthem_muc(DanhSachChiDoc([1, 2]), 3)       # TypeError ✗\n```\n\n```python\n# ✓ Dùng kiểu ĐÃ chỉ đọc sẵn\nfrom collections.abc import Sequence\n\ndef doc_du_lieu() -> Sequence[int]:      # ⭐ Sequence KHÔNG hứa append\n    return (1, 2, 3)                      # tuple — chỉ đọc thật\n\ndef xu_ly(ds: Sequence[int]) -> int:     # ⭐ nêu rõ chỉ cần đọc\n    return sum(ds)\n\nxu_ly([1, 2, 3])                          # ✓ list cũng là Sequence\nxu_ly((1, 2, 3))                          # ✓\n```\n\nĐây là ứng dụng của nguyên tắc \"nhận rộng nhất có thể\" ở bài trừu tượng: nhận `Sequence` thay vì `list` nghĩa là **không hứa** khả năng ghi, nên `tuple` dùng được và không ai bị bất ngờ.\n\n### Chuyển từ kế thừa sang composition\n\n```python\n# ❌ Vi phạm\nclass KhoFile:\n    def doc(self, k): ...\n    def luu(self, k, v): ...\n    def xoa(self, k): ...\n\nclass KhoFileNenChiDoc(KhoFile):\n    def luu(self, k, v): raise NotImplementedError\n    def xoa(self, k): raise NotImplementedError\n\n\n# ✓ Tách theo năng lực + composition\nfrom typing import Protocol\n\nclass DocDuoc(Protocol):\n    def doc(self, k: str) -> bytes | None: ...\n\nclass GhiDuoc(Protocol):\n    def luu(self, k: str, v: bytes) -> None: ...\n    def xoa(self, k: str) -> bool: ...\n\n\nclass KhoFile:                              # cài cả hai\n    def doc(self, k): ...\n    def luu(self, k, v): ...\n    def xoa(self, k): ...\n\nclass KhoNenChiDoc:                         # ⭐ CHỈ cài DocDuoc\n    def __init__(self, duong_dan_nen):\n        self._nen = duong_dan_nen\n    def doc(self, k):\n        import gzip\n        ...\n\n\n# Hàm nêu rõ nó cần năng lực gì\ndef thong_ke(kho: DocDuoc, khoa: list[str]) -> int:\n    return sum(len(kho.doc(k) or b\"\") for k in khoa)\n\ndef nhan_ban(nguon: DocDuoc, dich: GhiDuoc, khoa: list[str]) -> None:\n    for k in khoa:\n        v = nguon.doc(k)\n        if v is not None:\n            dich.luu(k, v)\n\n\nthong_ke(KhoNenChiDoc(\"a.gz\"), [\"x\"])              # ✓\nnhan_ban(KhoNenChiDoc(\"a.gz\"), KhoFile(), [\"x\"])   # ✓\nnhan_ban(KhoFile(), KhoNenChiDoc(\"a.gz\"), [\"x\"])   # ❌ mypy CHẶN lúc viết code\n```\n\nDòng cuối là điểm mấu chốt: lỗi được bắt **lúc viết code** thay vì lúc chạy, vì `KhoNenChiDoc` không hề hứa `luu`. So với bản kế thừa, nơi lỗi chỉ lộ ra khi chạy đúng nhánh đó.\n\n### Bộ kiểm nhanh\n\n```python\ndef soi_liskov(cha, *con):\n    \"\"\"Tìm dấu hiệu vi phạm Liskov trong các lớp con.\"\"\"\n    import inspect\n\n    pt_cha = {t for t, v in vars(cha).items()\n              if callable(v) and not t.startswith(\"_\")}\n\n    for c in con:\n        print(f\"\\n{c.__name__} (con của {cha.__name__}):\")\n        for ten in sorted(pt_cha):\n            pt = getattr(c, ten, None)\n            if pt is None:\n                print(f\"  ⚠️ THIẾU {ten}\")\n                continue\n            if ten not in vars(c):\n                continue                       # không ghi đè — ổn\n            try:\n                nguon = inspect.getsource(pt)\n            except (OSError, TypeError):\n                continue\n            if \"NotImplementedError\" in nguon:\n                print(f\"  ❌ {ten}: raise NotImplementedError — VI PHẠM\")\n            elif nguon.strip().endswith(\"pass\"):\n                print(f\"  ⚠️ {ten}: thân rỗng — có thể vi phạm\")\n            elif \"raise\" in nguon and \"super()\" not in nguon:\n                print(f\"  ⚠️ {ten}: có raise, xem lại giao ước ngoại lệ\")\n\n\nclass Cha:\n    def a(self): return 1\n    def b(self): return 2\n\nclass ConXau(Cha):\n    def a(self): raise NotImplementedError(\"không hỗ trợ\")\n    def b(self): pass\n\nsoi_liskov(Cha, ConXau)\n# ConXau (con của Cha):\n#   ❌ a: raise NotImplementedError — VI PHẠM\n#   ⚠️ b: thân rỗng — có thể vi phạm\n```\n\nBộ kiểm này bắt được dấu hiệu ① và ② — hai dấu hiệu dễ thấy nhất. Các vi phạm còn lại (siết điều kiện, phá bất biến) thì **chỉ bộ kiểm thử giao ước** ở trên mới phát hiện được, vì chúng nằm ở hành vi chứ không ở cú pháp.",

so_sanh: "### Ba luật của Liskov\n\n| Luật | Lớp con không được | Lớp con được phép |\n|---|---|---|\n| Điều kiện đầu vào | **siết chặt** (đòi hỏi thêm) | nới lỏng (nhận thêm) |\n| Cam kết đầu ra | **nới lỏng** (hứa ít hơn) | siết chặt (hứa nhiều hơn) |\n| Bất biến | **phá vỡ** | giữ hoặc mạnh hơn |\n| Ngoại lệ | ném loại **mới** | ném ít loại hơn |\n\nMẹo nhớ: **lớp con phải dễ tính hơn với đầu vào và nghiêm khắc hơn với đầu ra.** Ngược lại là vi phạm.\n\n### Bảy dấu hiệu vi phạm\n\n| Dấu hiệu | Mức | Công cụ bắt được? |\n|---|---|---|\n| `raise NotImplementedError` | **nghiêm trọng** | ✓ dễ quét |\n| Phương thức thân rỗng | cao | ✓ quét được |\n| Code gọi phải `isinstance` | **nghiêm trọng** | ✗ |\n| Siết chặt kiểm tra đầu vào | cao | ✗ chỉ test giao ước |\n| Ném ngoại lệ loại mới | vừa | ✗ |\n| Tài liệu nói \"khác lớp cha ở...\" | vừa | ✗ |\n| Ghi đè mà không gọi `super()` | vừa | ✗ |\n\nCột cuối là lý do Liskov khó: **`mypy` chỉ kiểm tra chữ ký, không kiểm tra lời hứa.** Chỉ hai dấu hiệu đầu quét tự động được.\n\n### Ba cách sửa\n\n| | Tách giao diện | Composition | Bất biến |\n|---|---|---|---|\n| Giải quyết | năng lực khác nhau | quan hệ \"là một\" sai | thay đổi trạng thái |\n| Ví dụ | `KhoDocDuoc`/`KhoGhiDuoc` | tài khoản thấu chi | hình vuông/chữ nhật |\n| `mypy` bắt được lỗi | **✓** | ✓ | ✓ |\n| Công sức | vừa | vừa | **thấp nhất** |\n\nCột cuối ít được nhắc nhưng mạnh nhất: **phần lớn vi phạm Liskov liên quan tới việc đổi trạng thái**, nên object bất biến loại bỏ cả lớp vấn đề.\n\n### Liskov và các nguyên tắc khác\n\n| | Quan hệ với Liskov |\n|---|---|\n| **Open/Closed** | OCP chỉ hoạt động **nếu** các bản cài đặt thay thế nhau được |\n| **Interface Segregation** | giao diện lớn ép lớp con hứa điều nó không làm được ⇒ gây vi phạm |\n| **Dependency Inversion** | phụ thuộc vào trừu tượng chỉ an toàn khi trừu tượng đó được tôn trọng |\n| **Composition over inheritance** | cách chữa vi phạm Liskov phổ biến nhất |\n\nLiskov là nguyên tắc **nền** cho ba nguyên tắc kia: không có nó thì trừu tượng chỉ là hình thức.\n\n### \"Là một\" trong đời thực và trong code\n\n| Quan hệ | Đời thực | Trong code |\n|---|---|---|\n| Hình vuông là hình chữ nhật | **✓ đúng** | **✗** nếu có setter độc lập |\n| Chim cánh cụt là chim | ✓ | **✗** nếu `Chim` có `bay()` |\n| Kho chỉ đọc là kho | ✓ | **✗** nếu `Kho` có `luu()` |\n| Số nguyên là số thực | ✓ | ✓ (đều bất biến) |\n\nBài học: **phân loại khái niệm không quyết định; hành vi thay thế được mới quyết định.** Và hàng cuối gợi ý vì sao: object bất biến ít khi vi phạm Liskov.\n\n### Cách phát hiện\n\n| Phương pháp | Bắt được gì | Tự động |\n|---|---|---|\n| Quét `NotImplementedError` | dấu hiệu ①② | **✓** |\n| Đọc tài liệu lớp con | dấu hiệu ⑥ | ✗ |\n| Tìm `isinstance` trong code gọi | dấu hiệu ③ | ✓ grep |\n| **Bộ kiểm thử giao ước** | **mọi vi phạm hành vi** | **✓** |\n\nHàng cuối là công cụ mạnh nhất: một bộ kiểm thử chạy cho **mọi** bản cài đặt, kiểm tra từng điều khoản của giao ước. Đây là cách duy nhất tự động hoá được việc kiểm tra Liskov.",

loi_thuong_gap: [
  "**Lớp con `raise NotImplementedError` cho phương thức của cha.** Vi phạm rõ ràng nhất — nó công khai từ chối giao ước.",
  "**Lớp con để phương thức thân rỗng.** Tệ hơn ném lỗi: không ai biết nó không làm gì.",
  "**Thêm kiểm tra đầu vào mà lớp cha không có.** Code gọi viết theo lớp cha có quyền truyền những giá trị đó.",
  "**Trả `None` khi lớp cha hứa luôn trả về giá trị.** `TypeError` ở chỗ xa nguyên nhân.",
  "**Bỏ một cam kết ngầm như \"kết quả đã sắp xếp\".** `mypy` không bắt được, và lỗi biểu hiện rất khó lần.",
  "**Ném loại ngoại lệ mà lớp cha không nêu.** Buộc mọi code gọi phải sửa. Bọc lại bằng `raise ... from e`.",
  "**Phá bất biến của lớp cha.** Ví dụ cho `so_du` âm khi cha cam kết không âm.",
  "**Kế thừa `list` rồi chặn `append`.** Mọi hàm nhận `list` đều có thể đổ. Dùng `Sequence` khi chỉ cần đọc.",
  "**Đặt một phương thức vào lớp cha khi không phải lớp con nào cũng làm được.** `Chim.bay()` là ví dụ kinh điển.",
  "**Dựa vào quan hệ \"là một\" trong đời thực.** Hình vuông là hình chữ nhật về toán, nhưng không thay thế được trong code có setter.",
  "**Viết `if isinstance` trong code gọi để xử lý riêng một lớp con.** Đây là hậu quả của vi phạm ở phía trên.",
  "**Không viết giao ước ra thành lời.** Không có điều kiện, cam kết, danh sách ngoại lệ thì không ai biết phải giữ gì.",
  "**Tin rằng `mypy` bắt được vi phạm Liskov.** Nó chỉ kiểm tra chữ ký, không kiểm tra hành vi."
],

tu_kiem_tra: [
  "Phát biểu LSP là gì? Phép kiểm thực dụng để phát hiện vi phạm?",
  "Vì sao nói Liskov nói về **hành vi** chứ không về chữ ký? Hệ quả với công cụ kiểm kiểu?",
  "Nêu ba luật cụ thể. Mẹo nhớ chiều nào được phép, chiều nào không?",
  "Vì sao `KhoChiDoc` kế thừa `KhoLuuTru` là lỗi thiết kế chứ không phải lỗi cài đặt?",
  "Vì sao hình vuông kế thừa hình chữ nhật vi phạm Liskov dù về toán học nó đúng?",
  "\"Cam kết ngầm\" trong ví dụ hình chữ nhật là gì? Cách sửa triệt để?",
  "Vì sao object bất biến loại bỏ được cả một lớp vi phạm Liskov?",
  "Danh sách ngoại lệ có phải một phần của giao ước không? Cách giữ giao ước khi bản cài đặt gặp loại lỗi khác?",
  "Nêu bảy dấu hiệu vi phạm. Dấu hiệu nào là **hậu quả** chứ không phải nguyên nhân?",
  "Nêu ba cách sửa vi phạm Liskov, mỗi cách hợp với loại vấn đề nào?",
  "\"Bộ kiểm thử giao ước\" hoạt động thế nào? Vì sao nó là công cụ mạnh nhất cho Liskov?",
  "Liskov liên hệ với Open/Closed và Interface Segregation thế nào?",
  "Vì sao nhận `Sequence` thay vì `list` giúp tránh vi phạm Liskov?",
  "Ba mục nào nên viết trong docstring để nêu rõ giao ước?"
],

tai_nguyen: [
  "Sách **Clean Architecture** — Robert C. Martin. Chương về LSP, gồm cả ví dụ hình vuông/chữ nhật và phân tích vì sao nó sai.",
  "Bài báo gốc của Barbara Liskov và Jeannette Wing về *behavioral subtyping* — nguồn của ba luật; đọc để hiểu phát biểu hình thức.",
  "Sách **Effective Java** — Joshua Bloch. Mục về thiết kế cho kế thừa hoặc cấm kế thừa, áp dụng nguyên vẹn cho Python.",
  "Sách **Practical Object-Oriented Design** — Sandi Metz. Chương về kế thừa giải thích rõ khái niệm giao ước hành vi.",
  "Tài liệu `pytest`, phần *Parametrizing fixtures* — kỹ thuật chạy một bộ kiểm thử cho nhiều bản cài đặt."
],

lien_he: [
  "**Kế thừa và MRO**: dấu hiệu \"lớp con raise NotImplementedError\" đã nêu ở đó; bài này giải thích cơ chế và cách sửa.",
  "**Đa hình và duck typing**: Liskov là điều kiện để đa hình đúng đắn; thiếu nó thì `isinstance` quay lại.",
  "**Composition over inheritance**: cách chữa vi phạm Liskov phổ biến nhất.",
  "**Trừu tượng: ABC, Protocol**: \"bản cài đặt phải để trống một phương thức\" là dấu hiệu trừu tượng sai — cùng vấn đề nhìn từ góc khác.",
  "**SOLID — Open/Closed** ngay trước: OCP chỉ hoạt động khi các bản cài đặt thay thế nhau được.",
  "**SOLID — Interface Segregation** ngay sau: giao diện lớn ép lớp con hứa điều nó không làm được.",
  "**Dataclass và NamedTuple**: `frozen=True` loại bỏ cả lớp vi phạm liên quan tới thay đổi trạng thái.",
  "**Đóng gói**: bất biến là khái niệm trung tâm ở cả hai bài — ở đó là giữ nó, ở đây là không phá nó khi kế thừa.",
  "**Xử lý ngoại lệ** ở module 5: `raise ... from e` là cách bọc ngoại lệ mà vẫn giữ giao ước.",
  "**Testing** ở mảng 05: bộ kiểm thử giao ước là mẫu kiểm thử quan trọng khi có nhiều bản cài đặt cùng giao diện.",
  "**Backend & Web** ở mảng 05: đổi giữa các nhà cung cấp lưu trữ chỉ an toàn khi mọi bản cài đặt tôn trọng cùng giao ước."
]

};
