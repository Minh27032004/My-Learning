window.LESSONS["05:trien-khai:https-voi-let-s-encrypt"] = {

tom_tat: [
  "HTTPS cho Sổ Chi Tiêu dùng TLS để xác thực máy chủ và bảo vệ token, giao dịch trên đường truyền; Let’s Encrypt tự động hóa việc cấp chứng chỉ qua giao thức ACME.",
  "**TLS dùng mã hoá bất đối xứng chỉ để thoả thuận khoá, rồi chuyển sang đối xứng để truyền dữ liệu.** Lý do là bất đối xứng chậm hơn hàng trăm lần.",
  "**Chứng chỉ không chứng minh website an toàn — nó chỉ chứng minh bạn đang nói với đúng tên miền đó.** Một trang lừa đảo hoàn toàn có thể có ổ khoá xanh.",
  "**Chuỗi tin cậy là điểm gãy phổ biến nhất.** Lỗi \"chứng chỉ không hợp lệ\" trên di động mà trình duyệt máy tính vẫn xanh gần như luôn là **thiếu chứng chỉ trung gian** trên server.",
  "**Chứng chỉ gắn với tên miền, không gắn với IP.** Đổi server không cần cấp lại chứng chỉ — nhưng phải nhớ mang nó theo.",
  "HTTPS cho Sổ Chi Tiêu dùng TLS để xác thực máy chủ và bảo vệ token, giao dịch trên đường truyền; Let’s Encrypt tự động hóa việc cấp chứng chỉ qua giao thức ACME."
],

can_biet_truoc: [
  "Bài **HTTP: method, header, body, status code** ngay trước — HTTPS không đổi gì trong đó, chỉ bọc lại.",
  "Bài **TCP vs UDP** — bắt tay TLS xảy ra **sau** bắt tay TCP, nên cộng thêm RTT.",
  "Bài **IP, port, DNS** — chứng chỉ gắn với tên miền, nên hiểu quan hệ tên miền ↔ IP là điều kiện để hiểu bài này.",
  "Bài **SSH, scp, rsync** ở module 3 — cặp khoá công khai/riêng ở đó là đúng ý tưởng dùng lại trong TLS.",
  "Mô hình nền của module “Triển khai”."
],

dinh_nghia: "**Let’s Encrypt** là tổ chức phát hành chứng chỉ công khai, miễn phí và tự động hóa; Certbot là một ACME client phổ biến chứ không phải bản thân nhà phát hành.\n\nNói đời thường: TLS giống như **hai người gặp nhau lần đầu ở nơi công cộng, cần nói chuyện riêng**. Trước tiên một bên xuất trình giấy tờ do một cơ quan mà cả hai tin cậy cấp (chứng chỉ). Sau đó hai người thống nhất một mật khẩu riêng theo cách mà người ngồi cạnh nghe hết cũng không suy ra được. Từ đó trở đi họ nói bằng mật khẩu ấy.\n\nNói chính xác:\n\n- **TLS** (*Transport Layer Security*) — giao thức nằm giữa tầng Giao vận và tầng Ứng dụng, cung cấp ba đảm bảo. **SSL** là tên cũ của nó; mọi phiên bản SSL đều đã bị coi là không an toàn, nhưng cái tên vẫn sống trong tên thư viện và tên file.\n- **HTTPS** — HTTP chạy trên TLS, mặc định cổng 443.\n- Ba đảm bảo:\n  - **Bí mật** (*confidentiality*) — người nghe giữa đường không đọc được nội dung.\n  - **Toàn vẹn** (*integrity*) — nội dung bị sửa một byte là phát hiện được.\n  - **Xác thực** (*authentication*) — bạn biết chắc mình đang nói với đúng chủ của tên miền đó.\n- **Chứng chỉ** (*certificate*) — tài liệu số gắn một **khoá công khai** với một **tên miền**, được một **CA** ký.\n- **CA** (*Certificate Authority*) — tổ chức mà trình duyệt và hệ điều hành tin cậy sẵn, có quyền ký chứng chỉ.\n\nĐiểm cần nắm ngay, vì nó là nguồn của rất nhiều ngộ nhận: **ổ khoá xanh chỉ nói về danh tính và kênh truyền, không nói gì về đạo đức hay chất lượng của website.** Một trang giả mạo ngân hàng có tên miền riêng vẫn xin được chứng chỉ hợp lệ và vẫn có ổ khoá xanh. TLS đảm bảo *bạn đang nói với đúng cái tên miền hiển thị*, không đảm bảo *cái tên miền đó tử tế*.\n\n**Let’s Encrypt** là tổ chức phát hành chứng chỉ công khai, miễn phí và tự động hóa; Certbot là một ACME client phổ biến chứ không phải bản thân nhà phát hành.\n\nNói đời thường: TLS giống như **hai người gặp nhau lần đầu ở nơi công cộng, cần nói chuyện riêng**. Trước tiên một bên xuất trình giấy tờ do một cơ quan mà cả hai tin cậy cấp (chứng chỉ). Sau đó hai người thống nhất một mật khẩu riêng theo cách mà người ngồi cạnh nghe hết cũng không suy ra được. Từ đó trở đi họ nói bằng mật khẩu ấy.\n\nNói chính xác:\n\n- **TLS** (*Transport Layer Security*) — giao thức nằm giữa tầng Giao vận và tầng Ứng dụng, cung cấp ba đảm bảo. **SSL** là tên cũ của nó; mọi phiên bản SSL đều đã bị coi là không an toàn, nhưng cái tên vẫn sống trong tên thư viện và tên file.\n- **HTTPS** — HTTP chạy trên TLS, mặc định cổng 443.\n- Ba đảm bảo:\n  - **Bí mật** (*confidentiality*) — người nghe giữa đường không đọc được nội dung.\n  - **Toàn vẹn** (*integrity*) — nội dung bị sửa một byte là phát hiện được.\n  - **Xác thực** (*authentication*) — bạn biết chắc mình đang nói với đúng chủ của tên miền đó.\n- **Chứng chỉ** (*certificate*) — tài liệu số gắn một **khoá công khai** với một **tên miền**, được một **CA** ký.\n- **CA** (*Certificate Authority*) — tổ chức mà trình duyệt và hệ điều hành tin cậy sẵn, có quyền ký chứng chỉ.\n\nĐiểm cần nắm ngay, vì nó là nguồn của rất nhiều ngộ nhận: **ổ khoá xanh chỉ nói về danh tính và kênh truyền, không nói gì về đạo đức hay chất lượng của website.** Một trang giả mạo ngân hàng có tên miền riêng vẫn xin được chứng chỉ hợp lệ và vẫn có ổ khoá xanh. TLS đảm bảo *bạn đang nói với đúng cái tên miền hiển thị*, không đảm bảo *cái tên miền đó tử tế*.\n\n**HTTPS với Let's Encrypt** thuộc Backend & Web, module **Triển khai**. Hãy định nghĩa nó bằng ba lớp: contract quan sát được từ bên ngoài; state/invariant phải giữ bên trong; và giả định môi trường để guarantee còn đúng. Nếu bỏ lớp giả định, câu “luôn nhanh”, “luôn nhất quán” hay “mô hình tốt” thường trở thành sai.",

vi_sao_can: "HTTPS không còn là tuỳ chọn, và hiểu nó là yêu cầu cơ bản của backend:\n\n- **Bắt buộc trên thực tế.** Trình duyệt gắn nhãn \"không an toàn\" cho HTTP, nhiều tính năng web (định vị, camera, service worker) **chỉ hoạt động trên HTTPS**. Không có HTTPS thì ứng dụng thiếu tính năng, không chỉ thiếu bảo mật.\n- **Sự cố HTTPS là loại sự cố tệ nhất.** Chứng chỉ hết hạn làm **toàn bộ** website chết, và chết theo cách người dùng thấy màn hình đỏ đáng sợ. Đây là dạng sự cố đã hạ nhiều dịch vụ lớn, và luôn là lỗi vận hành có thể tránh được hoàn toàn.\n- **Chẩn đoán chuỗi tin cậy.** \"Máy tôi vào được mà điện thoại khách báo lỗi\" là tình huống gần như tuần nào cũng gặp ở đời làm nghề, và nguyên nhân thường chỉ là một file cấu hình thiếu một dòng.\n- **Quyết định kiến trúc.** Kết thúc TLS ở đâu — load balancer, reverse proxy, hay chính ứng dụng — ảnh hưởng tới hiệu năng, tới việc lấy IP thật của client, và tới việc dữ liệu có được mã hoá trong mạng nội bộ hay không.\n- **Hiểu giới hạn của bảo mật.** Biết TLS bảo vệ cái gì và **không** bảo vệ cái gì mới đánh giá được rủi ro thật.\n\nTình huống thật rất đáng nhớ: bạn triển khai website, mở trên máy tính thấy ổ khoá xanh, tuyên bố xong việc. Hai ngày sau khách hàng gửi ảnh chụp điện thoại báo \"kết nối không riêng tư\". Nguyên nhân: server chỉ gửi chứng chỉ của site mà **thiếu chứng chỉ trung gian**; trình duyệt máy tính của bạn tình cờ đã có sẵn chứng chỉ trung gian đó trong cache nên vẫn ghép được chuỗi, còn điện thoại thì không. Phần ví dụ cho cách phát hiện lỗi này trong ba mươi giây — và đây là lý do **không bao giờ được kiểm tra HTTPS chỉ bằng trình duyệt của mình**.\n\nHTTPS không còn là tuỳ chọn, và hiểu nó là yêu cầu cơ bản của backend:\n\n- **Bắt buộc trên thực tế.** Trình duyệt gắn nhãn \"không an toàn\" cho HTTP, nhiều tính năng web (định vị, camera, service worker) **chỉ hoạt động trên HTTPS**. Không có HTTPS thì ứng dụng thiếu tính năng, không chỉ thiếu bảo mật.\n- **Sự cố HTTPS là loại sự cố tệ nhất.** Chứng chỉ hết hạn làm **toàn bộ** website chết, và chết theo cách người dùng thấy màn hình đỏ đáng sợ. Đây là dạng sự cố đã hạ nhiều dịch vụ lớn, và luôn là lỗi vận hành có thể tránh được hoàn toàn.\n- **Chẩn đoán chuỗi tin cậy.** \"Máy tôi vào được mà điện thoại khách báo lỗi\" là tình huống gần như tuần nào cũng gặp ở đời làm nghề, và nguyên nhân thường chỉ là một file cấu hình thiếu một dòng.\n- **Quyết định kiến trúc.** Kết thúc TLS ở đâu — load balancer, reverse proxy, hay chính ứng dụng — ảnh hưởng tới hiệu năng, tới việc lấy IP thật của client, và tới việc dữ liệu có được mã hoá trong mạng nội bộ hay không.\n- **Hiểu giới hạn của bảo mật.** Biết TLS bảo vệ cái gì và **không** bảo vệ cái gì mới đánh giá được rủi ro thật.\n\nTình huống thật rất đáng nhớ: bạn triển khai website, mở trên máy tính thấy ổ khoá xanh, tuyên bố xong việc. Hai ngày sau khách hàng gửi ảnh chụp điện thoại báo \"kết nối không riêng tư\". Nguyên nhân: server chỉ gửi chứng chỉ của site mà **thiếu chứng chỉ trung gian**; trình duyệt máy tính của bạn tình cờ đã có sẵn chứng chỉ trung gian đó trong cache nên vẫn ghép được chuỗi, còn điện thoại thì không. Phần ví dụ cho cách phát hiện lỗi này trong ba mươi giây — và đây là lý do **không bao giờ được kiểm tra HTTPS chỉ bằng trình duyệt của mình**.\n\nChủ đề này quan trọng vì nó thay đổi một quyết định trên critical path của hệ thống. Contract, bảo mật và quan sát được quan trọng ngang với happy-path code. Trong project thật, giá trị không nằm ở việc gọi đúng API mà ở khả năng dự đoán điều gì xảy ra khi dữ liệu lớn hơn, dependency chậm, input lệch phân phối, hai thao tác cạnh tranh hoặc deploy phiên bản mới.",

co_che: "### Từ bài Nginx sang cổng 443\n\nSổ Chi Tiêu giữ Uvicorn ở mạng nội bộ như bài trước. Nginx trở thành TLS endpoint: khóa riêng không đi vào container FastAPI, còn request sau khi giải mã vẫn được proxy đến ứng dụng. Ranh giới này đơn giản hóa code nhưng buộc người vận hành bảo vệ file khóa và tự động gia hạn.\n\n### TLS nằm ở đâu\n\n```\n┌──────────────────────┐\n│  HTTP                │  ← không biết gì về mã hoá, không đổi một dòng\n├──────────────────────┤\n│  TLS                 │  ← chèn vào đây\n├──────────────────────┤\n│  TCP                 │\n├──────────────────────┤\n│  IP                  │\n└──────────────────────┘\n```\n\nVì TLS nằm dưới HTTP, **mọi thứ của HTTP đều được mã hoá**: đường dẫn, header, cookie, body. Nhưng cũng vì nó nằm **trên** IP và TCP, những thứ sau vẫn lộ ra cho người quan sát giữa đường:\n\n```\nBị mã hoá:  đường dẫn, header, cookie, body, status code\nVẫn LỘ:     IP nguồn/đích, cổng, tên miền (qua SNI), kích thước và thời điểm gói tin\n```\n\nĐiều này có hệ quả thực tế cụ thể: nhà mạng **biết bạn vào tên miền nào**, chỉ không biết bạn xem trang nào trong đó. Đây là lý do HTTPS không phải công cụ ẩn danh, và cũng là lý do việc chặn website theo tên miền vẫn khả thi dù đã có HTTPS.\n\nSNI (*Server Name Indication*) là lý do tên miền lộ ra: khi nhiều website dùng chung một IP, server phải biết bạn muốn site nào **để chọn đúng chứng chỉ** — mà lúc đó chưa mã hoá được, vì việc chọn chứng chỉ chính là bước đang diễn ra. Một bài toán con gà và quả trứng có thật, và có những nỗ lực mã hoá cả SNI nhưng chưa phổ biến.\n\n### Hai loại mã hoá và vì sao cần cả hai\n\n| | Đối xứng | Bất đối xứng |\n|---|---|---|\n| Khoá | **một** khoá dùng chung | **cặp** công khai + riêng |\n| Tốc độ | rất nhanh | chậm hơn hàng trăm lần |\n| Vấn đề | làm sao đưa khoá cho nhau an toàn? | chậm, không dùng cho dữ liệu lớn |\n| Ví dụ | AES, ChaCha20 | RSA, ECDSA |\n\nMỗi loại giải được đúng điểm yếu của loại kia, nên TLS dùng cả hai theo đúng thế mạnh:\n\n```\nBất đối xứng  → chỉ dùng trong BẮT TAY, để xác thực danh tính\n                và thoả thuận một khoá đối xứng\nĐối xứng      → dùng cho TOÀN BỘ dữ liệu sau đó\n```\n\nĐây là một mẫu thiết kế rất đẹp và đáng học rộng hơn phạm vi TLS: **dùng công cụ đắt cho việc nhỏ một lần, công cụ rẻ cho việc lớn lặp lại**.\n\n### Bắt tay TLS 1.3, từng bước\n\n```\nClient                                          Server\n  │                                                │\n  │═══════ bắt tay TCP đã xong trước đó ══════════ │\n  │                                                │\n  │─── ClientHello ──────────────────────────────► │\n  │    · phiên bản TLS hỗ trợ                      │\n  │    · danh sách bộ mã hoá                       │\n  │    · SNI: tên miền muốn tới                    │\n  │    · phần khoá của tôi cho việc trao đổi khoá  │\n  │                                                │\n  │ ◄── ServerHello ─────────────────────────────── │\n  │    · chọn bộ mã hoá nào                        │\n  │    · phần khoá của server                      │\n  │    · CHỨNG CHỈ + chuỗi trung gian              │\n  │    · chữ ký chứng minh nắm khoá riêng           │\n  │                                                │\n  │    [client kiểm tra chứng chỉ — xem mục dưới]   │\n  │    [hai bên tự tính ra CÙNG một khoá đối xứng]  │\n  │                                                │\n  │─── Finished ─────────────────────────────────► │\n  │═══════ từ đây mọi thứ được mã hoá ════════════ │\n  │─── GET /api/users (đã mã hoá) ───────────────► │\n```\n\nĐiểm tinh tế nhất: **hai bên tính ra cùng một khoá mà khoá đó chưa bao giờ được truyền đi.** Cơ chế là *Diffie–Hellman*: mỗi bên gửi một \"phần công khai\", và mỗi bên kết hợp phần công khai của bên kia với phần riêng của mình để ra cùng một kết quả. Người nghe được cả hai phần công khai vẫn **không** tính ra được kết quả đó.\n\nHệ quả rất quan trọng, gọi là **forward secrecy**: vì khoá phiên không bao giờ được truyền và bị xoá sau khi phiên kết thúc, kẻ tấn công có ghi lại toàn bộ lưu lượng hôm nay, rồi **mai lấy được khoá riêng của server, vẫn không giải mã được** phiên hôm nay. Đây là lý do TLS 1.3 loại bỏ hẳn các cách trao đổi khoá không có tính chất này.\n\n### Số vòng đi-về và cái giá của TLS\n\n```\nTLS 1.2:  TCP 1 RTT + TLS 2 RTT = 3 RTT trước byte dữ liệu đầu tiên\nTLS 1.3:  TCP 1 RTT + TLS 1 RTT = 2 RTT\nTLS 1.3 nối lại phiên cũ:  0 RTT — gửi dữ liệu ngay trong gói đầu\n```\n\nVới RTT 100 ms, chênh lệch giữa TLS 1.2 và 1.3 là **100 ms mỗi kết nối mới** — cảm nhận được rõ rệt. Đây là một trong những lý do TLS 1.3 nên bật, không chỉ vì an toàn hơn.\n\nChế độ 0-RTT có một đánh đổi cần biết: dữ liệu gửi trong gói đầu **có thể bị phát lại** (*replay*) bởi kẻ tấn công. Vì vậy nó chỉ nên dùng cho request bất biến như `GET`, đúng như hợp đồng method ở bài trước. Đây là ví dụ hay về việc các tính chất ở bài trước có ứng dụng trực tiếp ở đây.\n\n### Chứng chỉ chứa gì\n\n```\nSubject:        CN = example.com\nSAN:            DNS:example.com, DNS:www.example.com   ← trình duyệt CHỈ đọc mục này\nIssuer:         CN = R3, O = Let's Encrypt\nValidity:       2026-06-01 → 2026-08-30\nPublic Key:     (khoá công khai của server)\nSignature:      (chữ ký của CA lên toàn bộ nội dung trên)\n```\n\nMột lưu ý thực dụng thường gây mất thời gian: trình duyệt hiện đại **bỏ qua hoàn toàn trường `CN`** và chỉ đọc **SAN** (*Subject Alternative Name*). Chứng chỉ có `CN=example.com` mà SAN thiếu `example.com` sẽ **bị từ chối** dù nhìn có vẻ đúng. Khi tự sinh chứng chỉ cho môi trường thử nghiệm, quên SAN là lỗi số một.\n\n### Chuỗi tin cậy — điểm gãy phổ biến nhất\n\nCA gốc không ký trực tiếp cho website. Có một chuỗi:\n\n```\n┌─────────────────────────────┐\n│ CA gốc (root)               │  ← nằm SẴN trong máy bạn, tự ký\n│ ISRG Root X1                │     hệ điều hành/trình duyệt cài kèm\n└──────────────┬──────────────┘\n               │ ký cho\n┌──────────────▼──────────────┐\n│ CA trung gian (intermediate)│  ← SERVER PHẢI GỬI CÁI NÀY\n│ R3                          │\n└──────────────┬──────────────┘\n               │ ký cho\n┌──────────────▼──────────────┐\n│ Chứng chỉ của bạn           │  ← server gửi\n│ example.com                 │\n└─────────────────────────────┘\n```\n\nVì sao có tầng trung gian? Vì khoá riêng của CA gốc quá quý — nó được giữ ngoại tuyến, trong két, gần như không bao giờ dùng. Nếu một CA trung gian bị xâm phạm, chỉ cần thu hồi trung gian đó chứ không phải cập nhật CA gốc trên hàng tỉ thiết bị.\n\n**Đây là nguồn của lỗi HTTPS phổ biến nhất trong thực tế.** Server phải gửi chứng chỉ của mình **cộng với** chứng chỉ trung gian. Nếu chỉ gửi chứng chỉ của mình:\n\n```\nTrình duyệt máy tính:  có thể ĐÃ cache trung gian từ website khác ⇒ vẫn xanh\nĐiện thoại, curl, Java: chưa có ⇒ BÁO LỖI\n```\n\nKết quả là bạn thấy xanh, khách hàng thấy đỏ — và bạn không tin khách hàng. Đây là lý do phải kiểm tra bằng công cụ dòng lệnh chứ không bằng trình duyệt của mình.\n\nCấu hình đúng là dùng **file fullchain**:\n\n```nginx\nssl_certificate     /etc/letsencrypt/live/example.com/fullchain.pem;  # ✓ site + trung gian\n# ssl_certificate   /etc/letsencrypt/live/example.com/cert.pem;       # ✗ CHỈ site\nssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;\n```\n\nChọn `cert.pem` thay vì `fullchain.pem` là một dòng cấu hình sai, và triệu chứng của nó thì rất khó lần ra nếu không biết trước cơ chế này.\n\n### Client kiểm tra những gì\n\nKhi nhận chứng chỉ, client kiểm tra một loạt điều kiện — và **chỉ một điều kiện sai là từ chối**:\n\n```\n① Tên miền có nằm trong SAN không?\n② Còn trong thời hạn không?\n③ Ghép được chuỗi tới một CA gốc mà tôi tin cậy không?\n④ Mỗi chữ ký trong chuỗi có đúng không?\n⑤ Chứng chỉ có bị thu hồi không?\n```\n\nBảng đối chiếu lỗi cần thuộc, vì thông báo của trình duyệt thường không nói rõ:\n\n| Lỗi | Điều kiện nào sai | Nguyên nhân thật |\n|---|---|---|\n| `NAME_MISMATCH` | ① | vào bằng `www` mà SAN chỉ có tên trần |\n| `DATE_INVALID` | ② | hết hạn, hoặc **đồng hồ máy client sai** |\n| `AUTHORITY_INVALID` | ③ | thiếu trung gian, hoặc chứng chỉ tự ký |\n| `REVOKED` | ⑤ | khoá riêng đã bị lộ |\n\nDòng `DATE_INVALID` đáng chú ý: nguyên nhân thường **không** phải chứng chỉ hết hạn mà là **đồng hồ máy người dùng sai**. Điện thoại hết pin lâu ngày, máy ảo mới dựng — đồng hồ lệch vài năm là mọi chứng chỉ trên thế giới đều \"chưa có hiệu lực\". Gặp báo cáo lỗi này từ đúng một người dùng, hãy hỏi giờ trên máy họ trước khi đi kiểm tra server.\n\n### Ba mức xác thực chứng chỉ\n\n| Mức | Kiểm tra gì | Thời gian | Trình duyệt hiển thị |\n|---|---|---|---|\n| **DV** (Domain Validated) | chỉ chứng minh bạn nắm tên miền | vài phút | ổ khoá thường |\n| **OV** (Organization) | kiểm tra doanh nghiệp tồn tại | vài ngày | ổ khoá thường |\n| **EV** (Extended) | kiểm tra pháp lý sâu | vài tuần | ổ khoá thường |\n\nCột cuối là cột đáng nói: **trình duyệt hiện đại hiển thị cả ba giống nhau**. Thanh xanh có tên công ty của EV đã bị loại bỏ, vì nghiên cứu cho thấy người dùng không hề để ý tới nó. Hệ quả thực dụng: **DV miễn phí đủ cho gần như mọi trường hợp**, và mã hoá thì cả ba mức giống nhau hoàn toàn — OV/EV không mã hoá mạnh hơn một chút nào.\n\n### Chứng chỉ tự ký và CA nội bộ\n\n```bash\n# Tự sinh — nhớ SAN, nếu không trình duyệt vẫn từ chối\nopenssl req -x509 -newkey rsa:2048 -nodes -days 365 \\\n  -keyout key.pem -out cert.pem \\\n  -subj \"/CN=localhost\" \\\n  -addext \"subjectAltName=DNS:localhost,IP:127.0.0.1\"\n```\n\nChứng chỉ tự ký cho **bí mật và toàn vẹn** đầy đủ, nhưng **không** cho xác thực danh tính — không ai bảo đảm rằng bên kia đúng là ai họ nói. Vì vậy:\n\n```\nDùng được:   môi trường phát triển, thử nghiệm nội bộ\nKHÔNG dùng:  môi trường thật có người dùng ngoài\n```\n\nVà một cảnh báo nghiêm túc: đừng dạy người dùng (hay chính mình) bấm \"tiếp tục dù không an toàn\", cũng đừng thêm cờ bỏ qua kiểm tra chứng chỉ trong code sản phẩm. Làm vậy là **vô hiệu hoá hoàn toàn** khả năng chống tấn công người-đứng-giữa, tức bỏ mất một trong ba đảm bảo, trong khi hai đảm bảo còn lại vẫn tạo cảm giác an toàn giả.\n\n### Kết thúc TLS ở đâu\n\nĐây là quyết định kiến trúc thật sẽ gặp khi deploy:\n\n```\n① Kết thúc ở load balancer / reverse proxy  (phổ biến nhất)\n   Người dùng ──TLS──► LB ──HTTP thường──► ứng dụng\n\n② Mã hoá lại ở chặng trong\n   Người dùng ──TLS──► LB ──TLS──► ứng dụng\n\n③ Xuyên qua, không giải mã\n   Người dùng ────────TLS─────────► ứng dụng\n```\n\n| | ① Kết thúc ở LB | ② Mã hoá lại | ③ Xuyên qua |\n|---|---|---|---|\n| Quản lý chứng chỉ | **một chỗ** | nhiều chỗ | ở ứng dụng |\n| LB đọc được HTTP | ✓ cache, định tuyến theo URL | ✓ | **✗** |\n| Mạng nội bộ được mã hoá | ✗ | **✓** | ✓ |\n| CPU | ít nhất | nhiều nhất | ít |\n\nPhương án ① là mặc định hợp lý cho phần lớn hệ thống: chứng chỉ quản lý một chỗ, và LB đọc được HTTP nên làm được cache, định tuyến, nén. Nhưng nó đòi hỏi **mạng giữa LB và ứng dụng phải đáng tin cậy** — nếu chặng đó đi qua Internet công cộng thì đây là lựa chọn sai và phải dùng ②.\n\nKhi chọn ①, đừng quên hệ quả đã nói ở bài trước: ứng dụng chỉ thấy IP của LB, và phải đọc `X-Forwarded-For` cùng `X-Forwarded-Proto` để biết IP thật và biết người dùng vào bằng HTTPS hay HTTP.\n\n### Từ bài Nginx sang cổng 443\n\nSổ Chi Tiêu giữ Uvicorn ở mạng nội bộ như bài trước. Nginx trở thành TLS endpoint: khóa riêng không đi vào container FastAPI, còn request sau khi giải mã vẫn được proxy đến ứng dụng. Ranh giới này đơn giản hóa code nhưng buộc người vận hành bảo vệ file khóa và tự động gia hạn.\n\n### TLS nằm ở đâu\n\n```\n┌──────────────────────┐\n│  HTTP                │  ← không biết gì về mã hoá, không đổi một dòng\n├──────────────────────┤\n│  TLS                 │  ← chèn vào đây\n├──────────────────────┤\n│  TCP                 │\n├──────────────────────┤\n│  IP                  │\n└──────────────────────┘\n```\n\nVì TLS nằm dưới HTTP, **mọi thứ của HTTP đều được mã hoá**: đường dẫn, header, cookie, body. Nhưng cũng vì nó nằm **trên** IP và TCP, những thứ sau vẫn lộ ra cho người quan sát giữa đường:\n\n```\nBị mã hoá:  đường dẫn, header, cookie, body, status code\nVẫn LỘ:     IP nguồn/đích, cổng, tên miền (qua SNI), kích thước và thời điểm gói tin\n```\n\nĐiều này có hệ quả thực tế cụ thể: nhà mạng **biết bạn vào tên miền nào**, chỉ không biết bạn xem trang nào trong đó. Đây là lý do HTTPS không phải công cụ ẩn danh, và cũng là lý do việc chặn website theo tên miền vẫn khả thi dù đã có HTTPS.\n\nSNI (*Server Name Indication*) là lý do tên miền lộ ra: khi nhiều website dùng chung một IP, server phải biết bạn muốn site nào **để chọn đúng chứng chỉ** — mà lúc đó chưa mã hoá được, vì việc chọn chứng chỉ chính là bước đang diễn ra. Một bài toán con gà và quả trứng có thật, và có những nỗ lực mã hoá cả SNI nhưng chưa phổ biến.\n\n### Hai loại mã hoá và vì sao cần cả hai\n\n| | Đối xứng | Bất đối xứng |\n|---|---|---|\n| Khoá | **một** khoá dùng chung | **cặp** công khai + riêng |\n| Tốc độ | rất nhanh | chậm hơn hàng trăm lần |\n| Vấn đề | làm sao đưa khoá cho nhau an toàn? | chậm, không dùng cho dữ liệu lớn |\n| Ví dụ | AES, ChaCha20 | RSA, ECDSA |\n\nMỗi loại giải được đúng điểm yếu của loại kia, nên TLS dùng cả hai theo đúng thế mạnh:\n\n```\nBất đối xứng  → chỉ dùng trong BẮT TAY, để xác thực danh tính\n                và thoả thuận một khoá đối xứng\nĐối xứng      → dùng cho TOÀN BỘ dữ liệu sau đó\n```\n\nĐây là một mẫu thiết kế rất đẹp và đáng học rộng hơn phạm vi TLS: **dùng công cụ đắt cho việc nhỏ một lần, công cụ rẻ cho việc lớn lặp lại**.\n\n### Bắt tay TLS 1.3, từng bước\n\n```\nClient                                          Server\n  │                                                │\n  │═══════ bắt tay TCP đã xong trước đó ══════════ │\n  │                                                │\n  │─── ClientHello ──────────────────────────────► │\n  │    · phiên bản TLS hỗ trợ                      │\n  │    · danh sách bộ mã hoá                       │\n  │    · SNI: tên miền muốn tới                    │\n  │    · phần khoá của tôi cho việc trao đổi khoá  │\n  │                                                │\n  │ ◄── ServerHello ─────────────────────────────── │\n  │    · chọn bộ mã hoá nào                        │\n  │    · phần khoá của server                      │\n  │    · CHỨNG CHỈ + chuỗi trung gian              │\n  │    · chữ ký chứng minh nắm khoá riêng           │\n  │                                                │\n  │    [client kiểm tra chứng chỉ — xem mục dưới]   │\n  │    [hai bên tự tính ra CÙNG một khoá đối xứng]  │\n  │                                                │\n  │─── Finished ─────────────────────────────────► │\n  │═══════ từ đây mọi thứ được mã hoá ════════════ │\n  │─── GET /api/users (đã mã hoá) ───────────────► │\n```\n\nĐiểm tinh tế nhất: **hai bên tính ra cùng một khoá mà khoá đó chưa bao giờ được truyền đi.** Cơ chế là *Diffie–Hellman*: mỗi bên gửi một \"phần công khai\", và mỗi bên kết hợp phần công khai của bên kia với phần riêng của mình để ra cùng một kết quả. Người nghe được cả hai phần công khai vẫn **không** tính ra được kết quả đó.\n\nHệ quả rất quan trọng, gọi là **forward secrecy**: vì khoá phiên không bao giờ được truyền và bị xoá sau khi phiên kết thúc, kẻ tấn công có ghi lại toàn bộ lưu lượng hôm nay, rồi **mai lấy được khoá riêng của server, vẫn không giải mã được** phiên hôm nay. Đây là lý do TLS 1.3 loại bỏ hẳn các cách trao đổi khoá không có tính chất này.\n\n### Số vòng đi-về và cái giá của TLS\n\n```\nTLS 1.2:  TCP 1 RTT + TLS 2 RTT = 3 RTT trước byte dữ liệu đầu tiên\nTLS 1.3:  TCP 1 RTT + TLS 1 RTT = 2 RTT\nTLS 1.3 nối lại phiên cũ:  0 RTT — gửi dữ liệu ngay trong gói đầu\n```\n\nVới RTT 100 ms, chênh lệch giữa TLS 1.2 và 1.3 là **100 ms mỗi kết nối mới** — cảm nhận được rõ rệt. Đây là một trong những lý do TLS 1.3 nên bật, không chỉ vì an toàn hơn.\n\nChế độ 0-RTT có một đánh đổi cần biết: dữ liệu gửi trong gói đầu **có thể bị phát lại** (*replay*) bởi kẻ tấn công. Vì vậy nó chỉ nên dùng cho request bất biến như `GET`, đúng như hợp đồng method ở bài trước. Đây là ví dụ hay về việc các tính chất ở bài trước có ứng dụng trực tiếp ở đây.\n\n### Chứng chỉ chứa gì\n\n```\nSubject:        CN = example.com\nSAN:            DNS:example.com, DNS:www.example.com   ← trình duyệt CHỈ đọc mục này\nIssuer:         CN = R3, O = Let's Encrypt\nValidity:       2026-06-01 → 2026-08-30\nPublic Key:     (khoá công khai của server)\nSignature:      (chữ ký của CA lên toàn bộ nội dung trên)\n```\n\nMột lưu ý thực dụng thường gây mất thời gian: trình duyệt hiện đại **bỏ qua hoàn toàn trường `CN`** và chỉ đọc **SAN** (*Subject Alternative Name*). Chứng chỉ có `CN=example.com` mà SAN thiếu `example.com` sẽ **bị từ chối** dù nhìn có vẻ đúng. Khi tự sinh chứng chỉ cho môi trường thử nghiệm, quên SAN là lỗi số một.\n\n### Chuỗi tin cậy — điểm gãy phổ biến nhất\n\nCA gốc không ký trực tiếp cho website. Có một chuỗi:\n\n```\n┌─────────────────────────────┐\n│ CA gốc (root)               │  ← nằm SẴN trong máy bạn, tự ký\n│ ISRG Root X1                │     hệ điều hành/trình duyệt cài kèm\n└──────────────┬──────────────┘\n               │ ký cho\n┌──────────────▼──────────────┐\n│ CA trung gian (intermediate)│  ← SERVER PHẢI GỬI CÁI NÀY\n│ R3                          │\n└──────────────┬──────────────┘\n               │ ký cho\n┌──────────────▼──────────────┐\n│ Chứng chỉ của bạn           │  ← server gửi\n│ example.com                 │\n└─────────────────────────────┘\n```\n\nVì sao có tầng trung gian? Vì khoá riêng của CA gốc quá quý — nó được giữ ngoại tuyến, trong két, gần như không bao giờ dùng. Nếu một CA trung gian bị xâm phạm, chỉ cần thu hồi trung gian đó chứ không phải cập nhật CA gốc trên hàng tỉ thiết bị.\n\n**Đây là nguồn của lỗi HTTPS phổ biến nhất trong thực tế.** Server phải gửi chứng chỉ của mình **cộng với** chứng chỉ trung gian. Nếu chỉ gửi chứng chỉ của mình:\n\n```\nTrình duyệt máy tính:  có thể ĐÃ cache trung gian từ website khác ⇒ vẫn xanh\nĐiện thoại, curl, Java: chưa có ⇒ BÁO LỖI\n```\n\nKết quả là bạn thấy xanh, khách hàng thấy đỏ — và bạn không tin khách hàng. Đây là lý do phải kiểm tra bằng công cụ dòng lệnh chứ không bằng trình duyệt của mình.\n\nCấu hình đúng là dùng **file fullchain**:\n\n```nginx\nssl_certificate     /etc/letsencrypt/live/example.com/fullchain.pem;  # ✓ site + trung gian\n# ssl_certificate   /etc/letsencrypt/live/example.com/cert.pem;       # ✗ CHỈ site\nssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;\n```\n\nChọn `cert.pem` thay vì `fullchain.pem` là một dòng cấu hình sai, và triệu chứng của nó thì rất khó lần ra nếu không biết trước cơ chế này.\n\n### Client kiểm tra những gì\n\nKhi nhận chứng chỉ, client kiểm tra một loạt điều kiện — và **chỉ một điều kiện sai là từ chối**:\n\n```\n① Tên miền có nằm trong SAN không?\n② Còn trong thời hạn không?\n③ Ghép được chuỗi tới một CA gốc mà tôi tin cậy không?\n④ Mỗi chữ ký trong chuỗi có đúng không?\n⑤ Chứng chỉ có bị thu hồi không?\n```\n\nBảng đối chiếu lỗi cần thuộc, vì thông báo của trình duyệt thường không nói rõ:\n\n| Lỗi | Điều kiện nào sai | Nguyên nhân thật |\n|---|---|---|\n| `NAME_MISMATCH` | ① | vào bằng `www` mà SAN chỉ có tên trần |\n| `DATE_INVALID` | ② | hết hạn, hoặc **đồng hồ máy client sai** |\n| `AUTHORITY_INVALID` | ③ | thiếu trung gian, hoặc chứng chỉ tự ký |\n| `REVOKED` | ⑤ | khoá riêng đã bị lộ |\n\nDòng `DATE_INVALID` đáng chú ý: nguyên nhân thường **không** phải chứng chỉ hết hạn mà là **đồng hồ máy người dùng sai**. Điện thoại hết pin lâu ngày, máy ảo mới dựng — đồng hồ lệch vài năm là mọi chứng chỉ trên thế giới đều \"chưa có hiệu lực\". Gặp báo cáo lỗi này từ đúng một người dùng, hãy hỏi giờ trên máy họ trước khi đi kiểm tra server.\n\n### Ba mức xác thực chứng chỉ\n\n| Mức | Kiểm tra gì | Thời gian | Trình duyệt hiển thị |\n|---|---|---|---|\n| **DV** (Domain Validated) | chỉ chứng minh bạn nắm tên miền | vài phút | ổ khoá thường |\n| **OV** (Organization) | kiểm tra doanh nghiệp tồn tại | vài ngày | ổ khoá thường |\n| **EV** (Extended) | kiểm tra pháp lý sâu | vài tuần | ổ khoá thường |\n\nCột cuối là cột đáng nói: **trình duyệt hiện đại hiển thị cả ba giống nhau**. Thanh xanh có tên công ty của EV đã bị loại bỏ, vì nghiên cứu cho thấy người dùng không hề để ý tới nó. Hệ quả thực dụng: **DV miễn phí đủ cho gần như mọi trường hợp**, và mã hoá thì cả ba mức giống nhau hoàn toàn — OV/EV không mã hoá mạnh hơn một chút nào.\n\n### Chứng chỉ tự ký và CA nội bộ\n\n```bash\n# Tự sinh — nhớ SAN, nếu không trình duyệt vẫn từ chối\nopenssl req -x509 -newkey rsa:2048 -nodes -days 365 \\\n  -keyout key.pem -out cert.pem \\\n  -subj \"/CN=localhost\" \\\n  -addext \"subjectAltName=DNS:localhost,IP:127.0.0.1\"\n```\n\nChứng chỉ tự ký cho **bí mật và toàn vẹn** đầy đủ, nhưng **không** cho xác thực danh tính — không ai bảo đảm rằng bên kia đúng là ai họ nói. Vì vậy:\n\n```\nDùng được:   môi trường phát triển, thử nghiệm nội bộ\nKHÔNG dùng:  môi trường thật có người dùng ngoài\n```\n\nVà một cảnh báo nghiêm túc: đừng dạy người dùng (hay chính mình) bấm \"tiếp tục dù không an toàn\", cũng đừng thêm cờ bỏ qua kiểm tra chứng chỉ trong code sản phẩm. Làm vậy là **vô hiệu hoá hoàn toàn** khả năng chống tấn công người-đứng-giữa, tức bỏ mất một trong ba đảm bảo, trong khi hai đảm bảo còn lại vẫn tạo cảm giác an toàn giả.\n\n### Kết thúc TLS ở đâu\n\nĐây là quyết định kiến trúc thật sẽ gặp khi deploy:\n\n```\n① Kết thúc ở load balancer / reverse proxy  (phổ biến nhất)\n   Người dùng ──TLS──► LB ──HTTP thường──► ứng dụng\n\n② Mã hoá lại ở chặng trong\n   Người dùng ──TLS──► LB ──TLS──► ứng dụng\n\n③ Xuyên qua, không giải mã\n   Người dùng ────────TLS─────────► ứng dụng\n```\n\n| | ① Kết thúc ở LB | ② Mã hoá lại | ③ Xuyên qua |\n|---|---|---|---|\n| Quản lý chứng chỉ | **một chỗ** | nhiều chỗ | ở ứng dụng |\n| LB đọc được HTTP | ✓ cache, định tuyến theo URL | ✓ | **✗** |\n| Mạng nội bộ được mã hoá | ✗ | **✓** | ✓ |\n| CPU | ít nhất | nhiều nhất | ít |\n\nPhương án ① là mặc định hợp lý cho phần lớn hệ thống: chứng chỉ quản lý một chỗ, và LB đọc được HTTP nên làm được cache, định tuyến, nén. Nhưng nó đòi hỏi **mạng giữa LB và ứng dụng phải đáng tin cậy** — nếu chặng đó đi qua Internet công cộng thì đây là lựa chọn sai và phải dùng ②.\n\nKhi chọn ①, đừng quên hệ quả đã nói ở bài trước: ứng dụng chỉ thấy IP của LB, và phải đọc `X-Forwarded-For` cùng `X-Forwarded-Proto` để biết IP thật và biết người dùng vào bằng HTTPS hay HTTP.\n\n### Mental model\n\nArtifact bất biến, config ngoài build, rollout dần và rollback đã diễn tập.\n\n![Sơ đồ Triển khai](assets/diagrams/05-trien-khai.svg)\n\n### Cơ chế từng bước\n\n1. Xác định input và boundary: ai tạo dữ liệu, schema/shape nào hợp lệ, lỗi được biểu diễn ra sao. 2. Theo dõi state qua từng ranh giới trong sơ đồ; tại mỗi bước ghi rõ dữ liệu được copy, cache, biến đổi hay persist. 3. Nêu invariant của **HTTPS với Let's Encrypt** và chỉ ra thao tác nào có thể phá nó. 4. Chọn phép đo: latency percentile, rows scanned, throughput, memory, loss/metric hoặc error budget tùy miền. 5. Dựng phản ví dụ nhỏ nhất trước khi tối ưu.\n\n### Tính đúng đắn và độ phức tạp\n\nKhông ghi Big-O máy móc nếu bottleneck là I/O, network hoặc accelerator. Tách CPU complexity, memory, số round-trip, bytes transferred và contention. Với hệ phân tán/ML, thêm quality/consistency guarantee. Một tối ưu hợp lệ phải giữ contract hoặc công khai contract mới.\n\n### Failure và phục hồi\n\nKiểm ít nhất timeout, retry, partial result, duplicate, stale state và resource exhaustion khi phù hợp. input độc hại, timeout, retry trùng, secret, dependency failure, backward compatibility và overload. Phục hồi phải idempotent hoặc có deduplication; alert phải gắn với tác động người dùng/SLO, không chỉ mức tài nguyên.",

vi_du: "### Kiểm tra chứng chỉ đúng cách\n\n```bash\n# Xem toàn bộ chuỗi server GỬI RA — đây là phép thử quan trọng nhất\nopenssl s_client -connect example.com:443 -servername example.com </dev/null\n\n# Certificate chain\n#  0 s:CN = example.com          ← chứng chỉ của site\n#    i:C = US, O = Let's Encrypt, CN = R3\n#  1 s:C = US, O = Let's Encrypt, CN = R3    ← TRUNG GIAN: PHẢI CÓ DÒNG NÀY\n#    i:C = US, O = ISRG, CN = ISRG Root X1\n#\n# Verify return code: 0 (ok)     ← 0 là tốt, khác 0 là có vấn đề\n```\n\nHai chỗ cần đọc: **có dòng `1` (trung gian) hay không**, và `Verify return code` có bằng 0 hay không. Chỉ có dòng `0` mà không có dòng `1` chính là lỗi \"máy tôi xanh, điện thoại khách đỏ\".\n\nLưu ý `-servername` là bắt buộc: đó là SNI. Thiếu nó, server nhiều site sẽ trả về chứng chỉ mặc định — và bạn sẽ đi chẩn đoán một vấn đề không tồn tại.\n\n```bash\n# Xem ngày hết hạn\necho | openssl s_client -connect example.com:443 -servername example.com 2>/dev/null \\\n  | openssl x509 -noout -dates\n# notBefore=Jun  1 00:00:00 2026 GMT\n# notAfter=Aug 30 23:59:59 2026 GMT\n\n# Xem SAN — trình duyệt chỉ đọc mục này\necho | openssl s_client -connect example.com:443 -servername example.com 2>/dev/null \\\n  | openssl x509 -noout -text | grep -A1 \"Subject Alternative Name\"\n#     DNS:example.com, DNS:www.example.com\n\n# curl cũng kiểm tra được, ngắn hơn\ncurl -vI https://example.com 2>&1 | grep -E 'subject|issuer|expire'\n```\n\n### Cảnh báo hết hạn trước khi thành sự cố\n\nĐây là script nên đặt vào giám sát, vì chứng chỉ hết hạn là sự cố **hoàn toàn tránh được**:\n\n```bash\n#!/bin/bash\n# canh-bao-chung-chi.sh — báo động khi còn dưới N ngày\nNGUONG=14\n\nfor host in example.com api.example.com; do\n  het=$(echo | openssl s_client -connect \"$host:443\" -servername \"$host\" 2>/dev/null \\\n        | openssl x509 -noout -enddate | cut -d= -f2)\n  con=$(( ($(date -d \"$het\" +%s) - $(date +%s)) / 86400 ))\n\n  if [ \"$con\" -lt \"$NGUONG\" ]; then\n    echo \"⚠️  $host: còn $con ngày (hết hạn $het)\"\n  else\n    echo \"✓  $host: còn $con ngày\"\n  fi\ndone\n```\n\nHai điều đáng lưu ý về script này. Thứ nhất, nó kiểm tra **từ bên ngoài**, đúng như người dùng thấy — quan trọng hơn việc đọc file trên đĩa, vì file có thể đã gia hạn mà server chưa nạp lại. Thứ hai, **tự động gia hạn cũng phải giám sát**: cơ chế tự gia hạn có thể lặng lẽ hỏng vài tháng mà không ai biết, cho tới đúng ngày hết hạn.\n\n### Chẩn đoán \"máy tôi xanh, khách đỏ\"\n\n```bash\n# ① Xem server gửi mấy chứng chỉ\nopenssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null \\\n  | grep -c \"^ [0-9] s:\"\n# 1  ⇒ CHỈ có chứng chỉ site, THIẾU trung gian  ← đây là lỗi\n# 2  ⇒ có cả trung gian, đúng\n\n# ② Kiểm tra bằng client KHÔNG có cache trung gian\ncurl -I https://example.com\n# curl: (60) SSL certificate problem: unable to get local issuer certificate\n#                                     ↑ đúng triệu chứng thiếu trung gian\n\n# ③ Sửa: dùng fullchain, rồi nạp lại cấu hình\ngrep ssl_certificate /etc/nginx/sites-enabled/example.com\n# ssl_certificate /etc/letsencrypt/live/example.com/cert.pem;   ← SAI\n\nsudo sed -i 's|/cert.pem|/fullchain.pem|' /etc/nginx/sites-enabled/example.com\nsudo nginx -t && sudo systemctl reload nginx\n\n# ④ Xác nhận lại\nopenssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null \\\n  | grep -c \"^ [0-9] s:\"\n# 2  ✓\n```\n\nBước ① và ④ giống nhau là có chủ đích: **luôn kiểm chứng lại sau khi sửa bằng đúng phép thử đã phát hiện lỗi**. Bước `nginx -t` cũng bắt buộc — nó kiểm tra cú pháp trước khi reload, tránh làm chết website bằng một lỗi gõ.\n\n### Chuyển hướng HTTP sang HTTPS, và HSTS\n\n```nginx\n# Server nghe cổng 80 chỉ để chuyển hướng\nserver {\n    listen 80;\n    server_name example.com www.example.com;\n    return 301 https://$host$request_uri;\n}\n\nserver {\n    listen 443 ssl http2;\n    server_name example.com www.example.com;\n\n    ssl_certificate     /etc/letsencrypt/live/example.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;\n    ssl_protocols       TLSv1.2 TLSv1.3;      # bỏ hẳn các phiên bản cũ\n\n    # Nói với trình duyệt: một năm tới, CHỈ vào bằng HTTPS\n    add_header Strict-Transport-Security \"max-age=31536000\" always;\n}\n```\n\nVì sao cần HSTS khi đã có chuyển hướng 301? Vì **request đầu tiên vẫn đi bằng HTTP**, và đó đúng là khoảnh khắc kẻ tấn công có thể chặn để giữ người dùng ở HTTP. HSTS làm trình duyệt **tự đổi sang HTTPS trước khi gửi request**, xoá bỏ cửa sổ tấn công đó.\n\nCảnh báo phải nói rõ: **HSTS rất khó hoàn tác.** Trình duyệt nhớ trong `max-age` giây và không có cách nào buộc nó quên. Nếu sau đó HTTPS của bạn hỏng, người dùng **không thể** truy cập bằng HTTP để tạm dùng. Nên bắt đầu bằng giá trị nhỏ:\n\n```nginx\nadd_header Strict-Transport-Security \"max-age=300\" always;      # thử 5 phút\n# chạy ổn định vài tuần rồi mới nâng lên 31536000\n```\n\n### Đo cái giá của TLS\n\n```bash\ncurl -w \"@-\" -o /dev/null -s https://example.com <<'EOF'\n  tcp:  %{time_connect}s\n  tls:  %{time_appconnect}s\nEOF\n#   tcp:  0.052s\n#   tls:  0.108s      ← TLS thêm ~56 ms\n\n# Xem phiên bản TLS thực tế đang dùng\ncurl -sI https://example.com -w '%{http_version} %{ssl_verify_result}\\n' -o /dev/null\nopenssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null \\\n  | grep -E \"Protocol|Cipher\"\n# Protocol  : TLSv1.3\n# Cipher    : TLS_AES_256_GCM_SHA384\n```\n\nThấy `TLSv1.2` ở đây là một cơ hội tối ưu cụ thể: bật TLS 1.3 tiết kiệm đúng một RTT cho mỗi kết nối mới, không phải đổi gì trong ứng dụng.\n\n### Chứng chỉ tự ký cho môi trường phát triển\n\n```bash\n# Sinh, nhớ addext SAN — thiếu là trình duyệt từ chối dù CN đúng\nopenssl req -x509 -newkey rsa:2048 -nodes -days 365 \\\n  -keyout dev-key.pem -out dev-cert.pem \\\n  -subj \"/CN=localhost\" \\\n  -addext \"subjectAltName=DNS:localhost,DNS:*.localhost,IP:127.0.0.1\"\n\n# Kiểm tra SAN đã vào chưa\nopenssl x509 -in dev-cert.pem -noout -text | grep -A1 \"Alternative Name\"\n\n# Dùng với curl: chỉ định CA tin cậy, ĐỪNG dùng -k\ncurl --cacert dev-cert.pem https://localhost:8443/\n```\n\nGiải thích vì sao `--cacert` tốt hơn `-k` (bỏ qua kiểm tra): `--cacert` vẫn **kiểm tra đầy đủ**, chỉ là tin thêm một CA cụ thể; `-k` thì tắt hẳn kiểm tra. Thói quen dùng `-k` rất dễ theo bạn vào code sản phẩm, và ở đó nó là lỗ hổng thật. Cùng lý do đó, tuyệt đối không đặt `verify=False` hay tương đương trong code chạy thật:\n\n```python\n# ❌ tắt kiểm tra — mất hoàn toàn khả năng chống người-đứng-giữa\nrequests.get(\"https://api.example.com\", verify=False)\n\n# ✓ chỉ định CA nội bộ, vẫn kiểm tra đầy đủ\nrequests.get(\"https://api.internal\", verify=\"/etc/ssl/ca-noi-bo.pem\")\n```\n\n### Kiểm tra chứng chỉ đúng cách\n\n```bash\n# Xem toàn bộ chuỗi server GỬI RA — đây là phép thử quan trọng nhất\nopenssl s_client -connect example.com:443 -servername example.com </dev/null\n\n# Certificate chain\n#  0 s:CN = example.com          ← chứng chỉ của site\n#    i:C = US, O = Let's Encrypt, CN = R3\n#  1 s:C = US, O = Let's Encrypt, CN = R3    ← TRUNG GIAN: PHẢI CÓ DÒNG NÀY\n#    i:C = US, O = ISRG, CN = ISRG Root X1\n#\n# Verify return code: 0 (ok)     ← 0 là tốt, khác 0 là có vấn đề\n```\n\nHai chỗ cần đọc: **có dòng `1` (trung gian) hay không**, và `Verify return code` có bằng 0 hay không. Chỉ có dòng `0` mà không có dòng `1` chính là lỗi \"máy tôi xanh, điện thoại khách đỏ\".\n\nLưu ý `-servername` là bắt buộc: đó là SNI. Thiếu nó, server nhiều site sẽ trả về chứng chỉ mặc định — và bạn sẽ đi chẩn đoán một vấn đề không tồn tại.\n\n```bash\n# Xem ngày hết hạn\necho | openssl s_client -connect example.com:443 -servername example.com 2>/dev/null \\\n  | openssl x509 -noout -dates\n# notBefore=Jun  1 00:00:00 2026 GMT\n# notAfter=Aug 30 23:59:59 2026 GMT\n\n# Xem SAN — trình duyệt chỉ đọc mục này\necho | openssl s_client -connect example.com:443 -servername example.com 2>/dev/null \\\n  | openssl x509 -noout -text | grep -A1 \"Subject Alternative Name\"\n#     DNS:example.com, DNS:www.example.com\n\n# curl cũng kiểm tra được, ngắn hơn\ncurl -vI https://example.com 2>&1 | grep -E 'subject|issuer|expire'\n```\n\n### Cảnh báo hết hạn trước khi thành sự cố\n\nĐây là script nên đặt vào giám sát, vì chứng chỉ hết hạn là sự cố **hoàn toàn tránh được**:\n\n```bash\n#!/bin/bash\n# canh-bao-chung-chi.sh — báo động khi còn dưới N ngày\nNGUONG=14\n\nfor host in example.com api.example.com; do\n  het=$(echo | openssl s_client -connect \"$host:443\" -servername \"$host\" 2>/dev/null \\\n        | openssl x509 -noout -enddate | cut -d= -f2)\n  con=$(( ($(date -d \"$het\" +%s) - $(date +%s)) / 86400 ))\n\n  if [ \"$con\" -lt \"$NGUONG\" ]; then\n    echo \"⚠️  $host: còn $con ngày (hết hạn $het)\"\n  else\n    echo \"✓  $host: còn $con ngày\"\n  fi\ndone\n```\n\nHai điều đáng lưu ý về script này. Thứ nhất, nó kiểm tra **từ bên ngoài**, đúng như người dùng thấy — quan trọng hơn việc đọc file trên đĩa, vì file có thể đã gia hạn mà server chưa nạp lại. Thứ hai, **tự động gia hạn cũng phải giám sát**: cơ chế tự gia hạn có thể lặng lẽ hỏng vài tháng mà không ai biết, cho tới đúng ngày hết hạn.\n\n### Chẩn đoán \"máy tôi xanh, khách đỏ\"\n\n```bash\n# ① Xem server gửi mấy chứng chỉ\nopenssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null \\\n  | grep -c \"^ [0-9] s:\"\n# 1  ⇒ CHỈ có chứng chỉ site, THIẾU trung gian  ← đây là lỗi\n# 2  ⇒ có cả trung gian, đúng\n\n# ② Kiểm tra bằng client KHÔNG có cache trung gian\ncurl -I https://example.com\n# curl: (60) SSL certificate problem: unable to get local issuer certificate\n#                                     ↑ đúng triệu chứng thiếu trung gian\n\n# ③ Sửa: dùng fullchain, rồi nạp lại cấu hình\ngrep ssl_certificate /etc/nginx/sites-enabled/example.com\n# ssl_certificate /etc/letsencrypt/live/example.com/cert.pem;   ← SAI\n\nsudo sed -i 's|/cert.pem|/fullchain.pem|' /etc/nginx/sites-enabled/example.com\nsudo nginx -t && sudo systemctl reload nginx\n\n# ④ Xác nhận lại\nopenssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null \\\n  | grep -c \"^ [0-9] s:\"\n# 2  ✓\n```\n\nBước ① và ④ giống nhau là có chủ đích: **luôn kiểm chứng lại sau khi sửa bằng đúng phép thử đã phát hiện lỗi**. Bước `nginx -t` cũng bắt buộc — nó kiểm tra cú pháp trước khi reload, tránh làm chết website bằng một lỗi gõ.\n\n### Chuyển hướng HTTP sang HTTPS, và HSTS\n\n```nginx\n# Server nghe cổng 80 chỉ để chuyển hướng\nserver {\n    listen 80;\n    server_name example.com www.example.com;\n    return 301 https://$host$request_uri;\n}\n\nserver {\n    listen 443 ssl http2;\n    server_name example.com www.example.com;\n\n    ssl_certificate     /etc/letsencrypt/live/example.com/fullchain.pem;\n    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;\n    ssl_protocols       TLSv1.2 TLSv1.3;      # bỏ hẳn các phiên bản cũ\n\n    # Nói với trình duyệt: một năm tới, CHỈ vào bằng HTTPS\n    add_header Strict-Transport-Security \"max-age=31536000\" always;\n}\n```\n\nVì sao cần HSTS khi đã có chuyển hướng 301? Vì **request đầu tiên vẫn đi bằng HTTP**, và đó đúng là khoảnh khắc kẻ tấn công có thể chặn để giữ người dùng ở HTTP. HSTS làm trình duyệt **tự đổi sang HTTPS trước khi gửi request**, xoá bỏ cửa sổ tấn công đó.\n\nCảnh báo phải nói rõ: **HSTS rất khó hoàn tác.** Trình duyệt nhớ trong `max-age` giây và không có cách nào buộc nó quên. Nếu sau đó HTTPS của bạn hỏng, người dùng **không thể** truy cập bằng HTTP để tạm dùng. Nên bắt đầu bằng giá trị nhỏ:\n\n```nginx\nadd_header Strict-Transport-Security \"max-age=300\" always;      # thử 5 phút\n# chạy ổn định vài tuần rồi mới nâng lên 31536000\n```\n\n### Đo cái giá của TLS\n\n```bash\ncurl -w \"@-\" -o /dev/null -s https://example.com <<'EOF'\n  tcp:  %{time_connect}s\n  tls:  %{time_appconnect}s\nEOF\n#   tcp:  0.052s\n#   tls:  0.108s      ← TLS thêm ~56 ms\n\n# Xem phiên bản TLS thực tế đang dùng\ncurl -sI https://example.com -w '%{http_version} %{ssl_verify_result}\\n' -o /dev/null\nopenssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null \\\n  | grep -E \"Protocol|Cipher\"\n# Protocol  : TLSv1.3\n# Cipher    : TLS_AES_256_GCM_SHA384\n```\n\nThấy `TLSv1.2` ở đây là một cơ hội tối ưu cụ thể: bật TLS 1.3 tiết kiệm đúng một RTT cho mỗi kết nối mới, không phải đổi gì trong ứng dụng.\n\n### Chứng chỉ tự ký cho môi trường phát triển\n\n```bash\n# Sinh, nhớ addext SAN — thiếu là trình duyệt từ chối dù CN đúng\nopenssl req -x509 -newkey rsa:2048 -nodes -days 365 \\\n  -keyout dev-key.pem -out dev-cert.pem \\\n  -subj \"/CN=localhost\" \\\n  -addext \"subjectAltName=DNS:localhost,DNS:*.localhost,IP:127.0.0.1\"\n\n# Kiểm tra SAN đã vào chưa\nopenssl x509 -in dev-cert.pem -noout -text | grep -A1 \"Alternative Name\"\n\n# Dùng với curl: chỉ định CA tin cậy, ĐỪNG dùng -k\ncurl --cacert dev-cert.pem https://localhost:8443/\n```\n\nGiải thích vì sao `--cacert` tốt hơn `-k` (bỏ qua kiểm tra): `--cacert` vẫn **kiểm tra đầy đủ**, chỉ là tin thêm một CA cụ thể; `-k` thì tắt hẳn kiểm tra. Thói quen dùng `-k` rất dễ theo bạn vào code sản phẩm, và ở đó nó là lỗ hổng thật. Cùng lý do đó, tuyệt đối không đặt `verify=False` hay tương đương trong code chạy thật:\n\n```python\n# ❌ tắt kiểm tra — mất hoàn toàn khả năng chống người-đứng-giữa\nrequests.get(\"https://api.example.com\", verify=False)\n\n# ✓ chỉ định CA nội bộ, vẫn kiểm tra đầy đủ\nrequests.get(\"https://api.internal\", verify=\"/etc/ssl/ca-noi-bo.pem\")\n```\n\n### Khung thực hành có thể chạy/triển khai\n\n```python\n# Khung handler cho: HTTPS với Let's Encrypt\ndef handle(request, deps):\n    data = validate(request)          # boundary validation\n    authorize(request.user, data)    # deny by default\n    result = deps.service.execute(data)\n    return serialize(result)          # stable API contract\n```\n\nBiến khung này thành thí nghiệm bằng dataset/input nhỏ có expected output, một ca biên và một failure được tiêm chủ động. Ghi phiên bản dependency, seed/config và môi trường. Không lấy một lần chạy làm benchmark; warm-up, lặp nhiều vòng và báo phân phối hoặc khoảng dao động.",

so_sanh: "### HTTP và HTTPS\n\n| | HTTP | HTTPS |\n|---|---|---|\n| Cổng | 80 | 443 |\n| Bí mật | ✗ đọc được toàn bộ | ✓ |\n| Toàn vẹn | ✗ sửa được không ai biết | ✓ |\n| Xác thực server | ✗ | ✓ |\n| Chi phí bắt tay | 1 RTT (TCP) | 2 RTT (TLS 1.3) |\n| Tính năng trình duyệt | hạn chế | đầy đủ |\n| Ngữ nghĩa HTTP | — | **giống hệt** |\n\nHàng \"toàn vẹn\" ít được nhắc mà quan trọng không kém bí mật: trên HTTP, một thiết bị giữa đường có thể **chèn quảng cáo hoặc mã độc vào trang** của bạn mà cả hai đầu đều không biết. Đây từng là hành vi có thật của một số nhà mạng, và toàn vẹn của TLS là thứ chấm dứt nó.\n\n### Đối xứng và bất đối xứng\n\n| | Đối xứng | Bất đối xứng |\n|---|---|---|\n| Số khoá | 1 | 2 (cặp) |\n| Tốc độ | rất nhanh | chậm hàng trăm lần |\n| Bài toán khoá | phải đưa khoá cho nhau an toàn | công khai được, không cần bí mật |\n| Vai trò trong TLS | **truyền dữ liệu** | **bắt tay, xác thực** |\n\nĐây là cặp bù trừ hoàn hảo, và cách TLS kết hợp chúng là mẫu thiết kế nên nhớ: bất đối xứng giải bài toán \"làm sao thoả thuận khoá qua kênh công khai\", đối xứng giải bài toán \"làm sao mã hoá nhanh\".\n\n### TLS 1.2 và TLS 1.3\n\n| | TLS 1.2 | TLS 1.3 |\n|---|---|---|\n| RTT bắt tay | 2 | **1** |\n| Nối lại phiên | 1 RTT | **0 RTT** |\n| Forward secrecy | tuỳ cấu hình | **bắt buộc** |\n| Bộ mã hoá yếu | còn cho phép | **đã loại bỏ** |\n| Cấu hình sai được không | **dễ sai** | khó sai hơn |\n\nHàng cuối là ưu điểm bị đánh giá thấp: TLS 1.3 **loại bỏ các lựa chọn tồi** khỏi giao thức, nên gần như không thể cấu hình thành trạng thái không an toàn. Đây là triết lý thiết kế tốt và áp dụng được rộng hơn mật mã học: *làm cho lựa chọn sai trở nên bất khả thi, thay vì viết tài liệu cảnh báo*.\n\n### DV, OV, EV\n\n| | DV | OV | EV |\n|---|---|---|---|\n| Chứng minh gì | nắm tên miền | + doanh nghiệp tồn tại | + kiểm tra pháp lý sâu |\n| Thời gian cấp | vài phút, tự động | vài ngày | vài tuần |\n| Chi phí | **miễn phí** | có phí | phí cao |\n| Mức mã hoá | **giống nhau** | giống nhau | giống nhau |\n| Trình duyệt hiển thị | **giống nhau** | giống nhau | giống nhau |\n\nBa hàng cuối gộp lại thành một kết luận thực dụng: **DV miễn phí là lựa chọn đúng cho gần như mọi trường hợp.** OV/EV chỉ còn ý nghĩa khi có yêu cầu tuân thủ hoặc hợp đồng bắt buộc, không phải vì an toàn hơn.\n\n### TLS bảo vệ và không bảo vệ những gì\n\n| Bị mã hoá | Vẫn lộ ra |\n|---|---|\n| đường dẫn URL | IP nguồn và đích |\n| header, cookie | cổng |\n| body request và response | **tên miền** (qua SNI) |\n| status code | kích thước, thời điểm gói tin |\n\nCột phải là cột người mới hay bỏ qua, và nó có ba hệ quả cần biết: nhà mạng **biết bạn vào website nào**; phân tích lưu lượng có thể đoán được hành vi qua kích thước và thời điểm gói tin; và HTTPS **không phải công cụ ẩn danh**. Muốn ẩn cả tên miền thì cần thêm lớp khác, không phải việc của TLS.\n\n### HTTP và HTTPS\n\n| | HTTP | HTTPS |\n|---|---|---|\n| Cổng | 80 | 443 |\n| Bí mật | ✗ đọc được toàn bộ | ✓ |\n| Toàn vẹn | ✗ sửa được không ai biết | ✓ |\n| Xác thực server | ✗ | ✓ |\n| Chi phí bắt tay | 1 RTT (TCP) | 2 RTT (TLS 1.3) |\n| Tính năng trình duyệt | hạn chế | đầy đủ |\n| Ngữ nghĩa HTTP | — | **giống hệt** |\n\nHàng \"toàn vẹn\" ít được nhắc mà quan trọng không kém bí mật: trên HTTP, một thiết bị giữa đường có thể **chèn quảng cáo hoặc mã độc vào trang** của bạn mà cả hai đầu đều không biết. Đây từng là hành vi có thật của một số nhà mạng, và toàn vẹn của TLS là thứ chấm dứt nó.\n\n### Đối xứng và bất đối xứng\n\n| | Đối xứng | Bất đối xứng |\n|---|---|---|\n| Số khoá | 1 | 2 (cặp) |\n| Tốc độ | rất nhanh | chậm hàng trăm lần |\n| Bài toán khoá | phải đưa khoá cho nhau an toàn | công khai được, không cần bí mật |\n| Vai trò trong TLS | **truyền dữ liệu** | **bắt tay, xác thực** |\n\nĐây là cặp bù trừ hoàn hảo, và cách TLS kết hợp chúng là mẫu thiết kế nên nhớ: bất đối xứng giải bài toán \"làm sao thoả thuận khoá qua kênh công khai\", đối xứng giải bài toán \"làm sao mã hoá nhanh\".\n\n### TLS 1.2 và TLS 1.3\n\n| | TLS 1.2 | TLS 1.3 |\n|---|---|---|\n| RTT bắt tay | 2 | **1** |\n| Nối lại phiên | 1 RTT | **0 RTT** |\n| Forward secrecy | tuỳ cấu hình | **bắt buộc** |\n| Bộ mã hoá yếu | còn cho phép | **đã loại bỏ** |\n| Cấu hình sai được không | **dễ sai** | khó sai hơn |\n\nHàng cuối là ưu điểm bị đánh giá thấp: TLS 1.3 **loại bỏ các lựa chọn tồi** khỏi giao thức, nên gần như không thể cấu hình thành trạng thái không an toàn. Đây là triết lý thiết kế tốt và áp dụng được rộng hơn mật mã học: *làm cho lựa chọn sai trở nên bất khả thi, thay vì viết tài liệu cảnh báo*.\n\n### DV, OV, EV\n\n| | DV | OV | EV |\n|---|---|---|---|\n| Chứng minh gì | nắm tên miền | + doanh nghiệp tồn tại | + kiểm tra pháp lý sâu |\n| Thời gian cấp | vài phút, tự động | vài ngày | vài tuần |\n| Chi phí | **miễn phí** | có phí | phí cao |\n| Mức mã hoá | **giống nhau** | giống nhau | giống nhau |\n| Trình duyệt hiển thị | **giống nhau** | giống nhau | giống nhau |\n\nBa hàng cuối gộp lại thành một kết luận thực dụng: **DV miễn phí là lựa chọn đúng cho gần như mọi trường hợp.** OV/EV chỉ còn ý nghĩa khi có yêu cầu tuân thủ hoặc hợp đồng bắt buộc, không phải vì an toàn hơn.\n\n### TLS bảo vệ và không bảo vệ những gì\n\n| Bị mã hoá | Vẫn lộ ra |\n|---|---|\n| đường dẫn URL | IP nguồn và đích |\n| header, cookie | cổng |\n| body request và response | **tên miền** (qua SNI) |\n| status code | kích thước, thời điểm gói tin |\n\nCột phải là cột người mới hay bỏ qua, và nó có ba hệ quả cần biết: nhà mạng **biết bạn vào website nào**; phân tích lưu lượng có thể đoán được hành vi qua kích thước và thời điểm gói tin; và HTTPS **không phải công cụ ẩn danh**. Muốn ẩn cả tên miền thì cần thêm lớp khác, không phải việc của TLS.\n\nĐừng so tên công nghệ; so theo workload. Với **HTTPS với Let's Encrypt**, đặt ít nhất hai phương án cạnh nhau trên các trục: guarantee/correctness, latency trung vị và p99, throughput, memory/storage, độ phức tạp vận hành, chi phí, khả năng debug và lock-in. Phương án đơn giản thường thắng ở quy mô nhỏ; phương án phân tán/tối ưu chỉ đáng dùng khi bottleneck đã đo vượt chi phí phức tạp mới.",

loi_thuong_gap: [
  "**Dùng `cert.pem` thay vì `fullchain.pem`.** Thiếu chứng chỉ trung gian: máy bạn xanh, điện thoại khách đỏ. Đây là lỗi HTTPS số một.",
  "**Kiểm tra HTTPS chỉ bằng trình duyệt của mình.** Trình duyệt có thể đã cache trung gian từ site khác. Phải kiểm tra bằng `openssl s_client` hoặc `curl`.",
  "**Không giám sát ngày hết hạn.** Chứng chỉ hết hạn làm chết toàn bộ website. Kể cả khi đã bật tự gia hạn — cơ chế đó cũng có thể lặng lẽ hỏng.",
  "**Quên SAN khi tự sinh chứng chỉ.** Trình duyệt bỏ qua `CN` và chỉ đọc SAN.",
  "**Chứng chỉ chỉ có `example.com` mà không có `www.example.com`.** Người dùng vào bằng `www` sẽ thấy lỗi tên không khớp.",
  "**Đặt `verify=False` hoặc `curl -k` trong code chạy thật.** Mất hoàn toàn khả năng chống người-đứng-giữa, trong khi vẫn có cảm giác an toàn giả.",
  "**Bật HSTS với `max-age` một năm ngay từ đầu.** Không hoàn tác được; HTTPS hỏng là người dùng không vào được bằng HTTP nữa. Bắt đầu nhỏ.",
  "**Nghĩ ổ khoá xanh nghĩa là website đáng tin.** Nó chỉ chứng minh bạn đang nói với đúng tên miền đó. Trang lừa đảo cũng có ổ khoá xanh."
],

tu_kiem_tra: [
  "TLS cung cấp ba đảm bảo nào? Nêu một ví dụ tấn công mà mỗi đảm bảo ngăn được.",
  "Vì sao TLS dùng cả mã hoá đối xứng và bất đối xứng thay vì chỉ một loại?",
  "Forward secrecy là gì, và vì sao nó quan trọng khi kẻ tấn công có thể ghi lại lưu lượng hôm nay?",
  "Chuỗi tin cậy gồm những gì? Server bắt buộc phải gửi những phần nào, và vì sao?",
  "Vì sao \"máy tôi vào được mà điện thoại khách báo lỗi\" thường là thiếu chứng chỉ trung gian?",
  "Ổ khoá xanh chứng minh và không chứng minh điều gì?",
  "Với HTTPS, người quan sát giữa đường thấy được những gì và không thấy được những gì?",
  "HSTS giải quyết vấn đề gì mà chuyển hướng 301 không giải quyết được? Rủi ro của nó là gì?"
],

tai_nguyen: [
  "Sách **Bulletproof SSL and TLS** — Ivan Ristić. Tài liệu đầy đủ nhất về TLS trong thực tiễn, gồm cả cấu hình server và các sự cố kinh điển.",
  "Tài liệu **MDN Web Docs**, mục về HTTPS, HSTS và mixed content — tra cứu nhanh khi cấu hình.",
  "Tài liệu Linux: `man openssl-s_client`, `man openssl-x509`. Hai lệnh này bao phủ gần như mọi việc chẩn đoán chứng chỉ.",
  "Sách **Serious Cryptography** — Jean-Philippe Aumasson. Nếu muốn hiểu tại sao Diffie–Hellman hoạt động chứ không chỉ dùng nó.",
  "Tài liệu của Let's Encrypt, mục *Chain of Trust* — giải thích chuỗi tin cậy bằng đúng các tên chứng chỉ bạn thấy trong `openssl s_client`."
],

lien_he: [
  "**HTTP: method, header, status code** ngay trước: HTTPS không đổi ngữ nghĩa nào của HTTP, chỉ bọc lại. Mọi header ở bài đó vẫn nguyên.",
  "**Cookie, session và trạng thái** ngay sau: cờ `Secure` của cookie chỉ có nghĩa khi có HTTPS, và đó là mắt nối trực tiếp.",
  "**TCP vs UDP** trước nữa: bắt tay TLS xảy ra sau bắt tay TCP, và tổng RTT là điều quyết định trải nghiệm.",
  "**IP, port, DNS**: chứng chỉ gắn với tên miền chứ không gắn với IP — nên đổi server không cần cấp lại chứng chỉ.",
  "**SSH, scp, rsync** ở module 3: cặp khoá công khai/riêng và cảnh báo *host key changed* là cùng một ý tưởng với chứng chỉ, chỉ khác cách xây niềm tin — SSH tin lần đầu, TLS tin qua CA."
]

};
