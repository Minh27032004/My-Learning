window.LESSONS["05:testing:test-database-rieng-va-du-lieu-mau"] = {

tom_tat: [
  "Database test phải tách vật lý và credential khỏi production; đổi tên schema trên cùng production server chưa phải hàng rào đủ an toàn.",
  "Chạy migration thật trên database cùng engine production giúp bắt khác biệt SQL, constraint, timezone và transaction mà SQLite hoặc mock không thể chứng minh.",
  "Mỗi test cần trạng thái biết trước: rollback transaction nhanh cho đa số case, còn truncate hoặc database riêng phù hợp với worker và nhiều connection.",
  "Dữ liệu mẫu nên nhỏ, biểu đạt invariant và được tạo qua factory; dump production vừa chậm vừa mang rủi ro dữ liệu cá nhân.",
  "Concurrency, row lock, unique constraint và quyền database là nội dung phải kiểm, không phải chi tiết hạ tầng có thể giả bằng dict."
],

can_biet_truoc: [
  "PostgreSQL transaction, commit, rollback và isolation cơ bản.",
  "Migration có đánh version và ORM session/identity map.",
  "Fixture pytest có scope, teardown và factory fixture.",
  "Invariant đặt chỗ: tổng ghế held/confirmed không vượt capacity và một payment reference không được dùng hai lần."
],

dinh_nghia: "**Test database** là một database instance hoặc database/schema được cấp riêng cho kiểm thử, dùng credential không có quyền lên dữ liệu thật và có vòng đời tạo–migrate–seed–reset–hủy xác định. Nó phải đủ giống production tại các semantics mà ứng dụng dựa vào.\n\n**Test data builder/factory** là code tạo bản ghi hợp lệ với mặc định tối thiểu, cho từng test ghi đè các giá trị mang ý nghĩa. **Fixture data** là trạng thái mẫu được nạp có chủ đích. Chúng khác production dump: mục tiêu là diễn đạt scenario, không sao chép thế giới thật.",

vi_sao_can: "Repository fake có thể nói hai hold cùng giữ ghế thành công tuần tự, nhưng production nhận request đồng thời. SQLite có thể chấp nhận kiểu dữ liệu hoặc cú pháp khác PostgreSQL. Mock `session.commit()` luôn thành công không biết migration quên unique constraint. Vì persistence có semantics riêng, một phần suite phải hỏi chính database thật.\n\nDatabase dùng chung lại tạo test phụ thuộc thứ tự. Bản ghi từ case trước làm case “email duy nhất” thất bại; chạy song song còn va chạm mạnh hơn. Nếu teardown lỗi, CI sau bắt đầu từ rác không rõ nguồn. Quản lý vòng đời dữ liệu vì thế là điều kiện để kết quả tái lập.\n\nRủi ro lớn nhất là kết nối nhầm production. Một lệnh `TRUNCATE` rất hữu ích trong test nhưng thảm họa trên dữ liệu thật. Tên biến `DATABASE_URL` giống nhau, secret CI rộng quyền và script reset thiếu guard tạo đường tai nạn. Thiết kế test database phải bắt đầu từ blast radius, không chỉ từ tốc độ.",

co_che: "### Cô lập ở nhiều lớp\n\nLớp đầu là hạ tầng: container hoặc cluster test, network và account riêng. Lớp hai là database/schema theo worker hoặc run. Lớp ba là transaction theo test. Không lớp nào thay hoàn toàn lớp khác. Transaction rollback nhanh nhưng không bảo vệ nếu URL trỏ nhầm; container riêng bảo vệ production nhưng test trong cùng container vẫn có thể ảnh hưởng nhau.\n\nCredential của ứng dụng test chỉ có quyền trên database test. Credential migration có thể mạnh hơn nhưng chỉ nằm trong job setup. Tên database nên chứa dấu hiệu rõ như `_test` và run ID. Guard trước thao tác hủy kiểm tra host allowlist, database suffix và một marker table được tạo khi provision; nếu thiếu bất kỳ điều kiện nào thì dừng.\n\n### Dùng cùng loại engine với production\n\nSQLite in-memory rất nhanh nhưng khác PostgreSQL về type, JSON, timezone, locking, collation, partial index và DDL transaction. Nó hợp để test code SQL portable rất hẹp, không hợp làm bằng chứng duy nhất cho repository PostgreSQL.\n\n| Hành vi | Fake/SQLite có thể bỏ sót | PostgreSQL test cho thấy |\n|---|---|---|\n| hai request giữ ghế | không có row lock thật | blocking và isolation thực |\n| email không phân biệt hoa thường | collation khác | index trên normalized value |\n| JSON query | dialect/hàm khác | operator và index đúng |\n| UUID/timezone | coercion rộng lượng | kiểu và chuyển đổi thật |\n| migration | không chạy dialect production | thứ tự DDL và constraint |\n\nContainer làm startup đắt hơn, nhưng có thể session-scope instance rồi tạo database hoặc transaction mới cho từng test. Tối ưu phần provision không đồng nghĩa chia sẻ dữ liệu mutable.\n\n### Migration là đường tạo schema duy nhất\n\nTest không nên gọi `metadata.create_all()` nếu production dùng migration. `create_all` dựng schema cuối nhưng bỏ qua đường nâng cấp: rename column, backfill, index concurrent hoặc default tạm thời. Một migration sai thứ tự có thể pass trên database mới mà fail khi nâng từ phiên bản trước.\n\nPipeline tối thiểu tạo database trống, chạy toàn bộ migration, rồi mới chạy integration tests. Một job migration riêng có thể khởi tạo schema ở version trước, seed một vài row tương thích, nâng lên head và xác nhận dữ liệu còn nguyên. Không cần ghi số phiên bản thư viện vào bài; dự án dùng công cụ migration nào thì gọi CLI chính thức của nó.\n\n### Rollback mỗi test: nhanh nhưng có điều kiện\n\nPattern phổ biến mở connection, bắt đầu outer transaction, bind ORM session vào connection, chạy test rồi rollback. Mọi INSERT/UPDATE biến mất mà không quét bảng.\n\n```python\n@pytest.fixture\ndef db_session(engine):\n    connection = engine.connect()\n    transaction = connection.begin()\n    session = Session(bind=connection, expire_on_commit=False)\n    try:\n        yield session\n    finally:\n        session.close()\n        transaction.rollback()\n        connection.close()\n```\n\nNếu code dưới test gọi `session.commit()`, tùy cách bind nó có thể kết thúc transaction mà fixture dựa vào. Một biến thể dùng savepoint và event hook mở lại nested transaction. Nhưng càng nhiều phép thuật, nguy cơ test khác production càng cao; repository có unit of work được inject thường dễ kiểm soát hơn.\n\nRollback không thu hồi side effect ngoài connection: sequence có thể tăng, notification có thể phát, worker connection khác có thể commit. Test không nên assert ID liên tiếp. Với background worker, cần database per test/run hoặc cleanup có chủ đích.\n\n### Truncate/reset dành cho nhiều connection\n\nKhi FastAPI, worker và scheduler mở pool riêng, outer transaction của fixture không bao phủ chúng. Khi đó chạy mỗi test với schema/database riêng hoặc truncate các bảng sau test. Truncate phải xét foreign key và sequence; thứ tự thủ công dễ hỏng khi schema đổi.\n\nDatabase theo worker cân bằng tốt cho parallel CI: worker `gw0` và `gw1` không thấy nhau, trong mỗi worker test có thể rollback hoặc truncate. Database theo test cô lập mạnh nhất nhưng provision nhiều. Chọn theo topology thật, không theo thói quen.\n\n### Factory tạo dữ liệu hợp lệ từ invariant\n\nFactory nên dùng mặc định rõ, nhỏ và hợp lệ. Test chỉ ghi đè yếu tố gây hành vi. Một workshop sold-out cần `capacity=2, confirmed=2`; không cần 40 cột ngẫu nhiên.\n\n```python\n@pytest.fixture\ndef make_workshop(db_session):\n    counter = 0\n    def make(*, capacity=10, confirmed=0, owner_id=None):\n        nonlocal counter\n        counter += 1\n        owner_id = owner_id or f\"teacher-{counter}\"\n        item = Workshop(title=f\"Workshop {counter}\", capacity=capacity,\n                        confirmed=confirmed, owner_id=owner_id)\n        db_session.add(item)\n        db_session.flush()\n        return item\n    return make\n```\n\n`flush()` gửi SQL và lấy ID nhưng chưa commit, nên constraint được kiểm trong transaction test. Factory không nuốt `IntegrityError`; test constraint cần thấy exception thật. Dữ liệu ngẫu nhiên vô hạn làm failure khó tái hiện; nếu dùng generator, log seed và thu nhỏ case.\n\n### Dữ liệu tham chiếu khác dữ liệu scenario\n\nMã trạng thái hoặc danh mục tỉnh có thể là reference data do migration/seed versioned quản lý. Workshop, user và hold là scenario data do từng test tạo. Trộn hai loại vào một dump khổng lồ khiến test phụ thuộc các row không ai nhớ.\n\nSeed phải idempotent nếu chạy lại: upsert theo natural key hoặc được chạy chỉ trên database trống với precondition rõ. Không sửa seed cũ làm môi trường lịch sử thay đổi âm thầm; dữ liệu reference có thể đi theo migration mới.\n\n### Không dùng dump production\n\nDump có email, tên, token hash hoặc nội dung người dùng là dữ liệu nhạy cảm. “Ẩn danh hóa” sơ sài vẫn có thể tái định danh qua tổ hợp thuộc tính. Dump còn lớn, không xác định và thay đổi theo ngày, khiến expected mỏng manh.\n\nNếu cần phân bố dữ liệu gần thật cho performance test, tạo synthetic dataset bằng quy tắc công khai, giữ generator và seed. Đó là workload test khác integration correctness; không đưa benchmark nặng vào mỗi pull request.\n\n### Constraint là executable invariant\n\nValidation FastAPI trả 422 sớm nhưng database vẫn cần NOT NULL, CHECK, FOREIGN KEY và UNIQUE cho invariant xuyên mọi writer. Worker hoặc script có thể bỏ qua HTTP. Integration test cố tình ghi sai trực tiếp và xác nhận transaction bị rollback.\n\nVí dụ payment reference unique nên có unique constraint. Test đầu INSERT thành công; lần hai với booking khác ném `IntegrityError`; sau rollback session phải được đưa về trạng thái dùng được. Chỉ assert loại lỗi nếu tên constraint không được adapter dịch ổn định; tốt hơn repository map constraint đã biết thành lỗi miền.\n\n### Concurrency phải dùng connection độc lập\n\nHai ORM session dùng cùng connection không mô phỏng hai transaction tranh chấp. Test oversell mở hai connection, mỗi bên đọc capacity và cố giữ ghế. Đồng bộ tại điểm trước UPDATE bằng barrier; không dùng sleep. Kết quả hợp lệ là một thành công, một xung đột, và tổng cuối không vượt capacity.\n\nImplementation có thể dùng `SELECT ... FOR UPDATE`, serializable isolation hoặc atomic UPDATE với điều kiện `held + quantity <= capacity`. Test nên assert invariant cuối và outcome, không khóa vào câu SQL cụ thể. Nếu cả hai transaction chờ nhau mãi, test cần timeout để CI không treo và log lock diagnostics.\n\n### FastAPI session phải có vòng đời request\n\nDependency `get_session` yield một session và đóng sau response. Trong integration test, override dependency để route dùng session/connection được kiểm soát. Nhưng nếu event nền chạy sau response, nó không được giữ session đã đóng; worker phải tự mở unit of work.\n\n```python\ndef get_session():\n    with Session(engine) as session:\n        yield session\n```\n\nKhông dùng một global ORM session cho toàn app. Session không phải thread-safe, identity map giữ object cũ và transaction lỗi làm các request sau cùng hỏng. Engine/pool có thể sống lâu; session nên ngắn.\n\n### Kiểm quyền database như một phần bảo mật\n\nApplication role không cần quyền DROP schema hay đọc bảng audit bí mật. Integration environment có thể chạy một nhóm test với đúng role runtime: SELECT/INSERT các bảng cần thiết thành công, thao tác DDL thất bại. Migration role được kiểm riêng.\n\nQuery phải parameterized; test payload chứa dấu nháy không phải bằng chứng duy nhất chống SQL injection, nhưng bắt nối chuỗi ngây thơ. Log SQL và failure artifact cần che email/token. Database URL không được in nguyên trong output CI.\n\n### Ca biên của dữ liệu\n\nKiểm chuỗi Unicode tiếng Việt và normalization nếu unique title/email dựa vào chúng; kiểm timestamp quanh UTC boundary; kiểm quantity 0, âm và vượt integer range mà schema chấp nhận; kiểm xóa workshop đang có hold; kiểm nullable đúng ý nghĩa. Không tự bịa giới hạn: lấy từ schema và quy tắc dự án.\n\nMột test empty database chứng minh query list trả `[]`, không 404 tùy contract. Một row chứng minh không có lỗi grouping. Nhiều row với cùng timestamp kiểm thứ tự ổn định phải có tie-breaker như ID; nếu chỉ `ORDER BY created_at`, kết quả hòa không được đảm bảo.\n\n### Khi nào không nên chạm database\n\nHàm tính số tiền, state machine hold và policy quyền thuần nên unit test không DB. Dùng database cho mọi nhánh làm phản hồi chậm và che thiết kế coupling. Ngược lại, đừng mock transaction để kiểm atomicity. Đặt phép thử ở tầng thấp nhất vẫn chứa cơ chế có thể gây lỗi.",

vi_du: "### Test constraint và phục hồi session\n\n```python\ndef test_payment_reference_is_unique(db_session, make_booking):\n    first = make_booking(payment_reference=\"pay-42\")\n    db_session.flush()\n\n    second = make_booking(payment_reference=\"pay-42\")\n    with pytest.raises(IntegrityError):\n        db_session.flush()\n    db_session.rollback()\n\n    assert first.id is not None\n```\n\nTrong code thật, factory có thể flush bên trong; test duplicate khi ấy cần API cho phép trì hoãn flush hoặc tạo model trực tiếp để exception nằm đúng dòng. Sau `IntegrityError`, PostgreSQL đánh transaction failed; phải rollback trước câu SQL tiếp. Quên bước này tạo lỗi “current transaction is aborted” gây hiểu nhầm.\n\n### Test atomic update chống oversell\n\nRepository có thể chạy một câu `UPDATE workshops SET held = held + :q WHERE id = :id AND held + :q <= capacity RETURNING held`. Hai transaction cạnh tranh vẫn chỉ một câu thỏa điều kiện khi còn một ghế. Integration test chạy hai connection và cuối cùng query bằng connection thứ ba sau khi cả hai kết thúc. Expected dựa trên tổng cuối, không dựa worker nào thắng vì scheduler không cam kết.\n\n### Quy trình provision an toàn\n\nCI tạo container PostgreSQL tạm, đợi health check, tạo role runtime, chạy migration bằng role migration, chạy tests bằng role runtime và hủy container. Laptop có thể tái dùng container nhưng mỗi lần test vẫn reset database. Script cleanup từ chối chạy nếu host không phải localhost/container network hoặc marker test thiếu. Hàng rào dư thừa đáng giá vì hậu quả xóa nhầm không thể bù bằng vài giây tiết kiệm.",

so_sanh: "### Chiến lược reset và đánh đổi\n\n| Chiến lược | Tốc độ | Cô lập | Dùng tốt khi | Không phù hợp khi |\n|---|---|---|---|---|\n| rollback mỗi test | rất nhanh | tốt trong một connection | request đồng bộ dùng session inject | worker/connection khác commit |\n| truncate | vừa | tốt nếu đủ bảng | app mở nhiều connection | database lớn, FK phức tạp |\n| schema per worker | vừa | tốt khi chạy song song | pytest-xdist, CI nhiều worker | extension không schema-aware |\n| database/container per test | chậm | mạnh nhất | test phá schema, migration | hàng nghìn case nhỏ |\n| SQLite in-memory | nhanh | riêng process | logic SQL portable hẹp | PostgreSQL semantics |\n\nFactory code dễ refactor và đặt giá trị cạnh test; fixture SQL tĩnh dễ xem đúng snapshot nhưng giòn khi schema thêm cột. API setup gần hành vi người dùng nhưng chậm và có thể không tạo được trạng thái bất hợp lệ cần test. Thường dùng factory cho scenario, migration cho reference, và vài fixture SQL chỉ khi dataset quan hệ phức tạp thực sự ổn định.\n\nTestcontainers hoặc compose đều có thể provision engine thật. Cái đầu tích hợp vòng đời với test runner; cái sau minh bạch cho nhiều service. Chọn theo hạ tầng nhóm, nhưng health check và cleanup phải xác định, không dựa một khoảng sleep cố định.",

loi_thuong_gap: [
  "Dùng SQLite thay PostgreSQL rồi kết luận lock/JSON đúng: nhận biết khi production-only bug xuất hiện; chạy integration trên cùng engine.",
  "Gọi `create_all` thay migration: database mới xanh nhưng upgrade thật lỗi; provision bằng migration production.",
  "Rollback một connection trong khi worker commit connection khác: row còn lại sau test; dùng database/schema riêng hoặc cleanup phù hợp topology.",
  "Assert ID phải bằng 1: sequence không rollback như dữ liệu; chỉ assert ID tồn tại hoặc quan hệ đúng.",
  "Factory tạo quá nhiều dữ liệu ngầm: test chậm và collision khó hiểu; mặc định tối thiểu, ghi đè invariant ngay tại case.",
  "Dùng dump production đã “xóa email”: vẫn có khả năng tái định danh và secret; sinh dữ liệu tổng hợp.",
  "Reset script chỉ kiểm suffix `_test`: URL production có thể bị đặt tên sai; thêm host allowlist, role hạn quyền và marker.",
  "Sau `IntegrityError` tiếp tục query không rollback: nhận lỗi transaction aborted; rollback hoặc kết thúc unit of work."
],

tu_kiem_tra: [
  "Vì sao container PostgreSQL session-scoped không đồng nghĩa dữ liệu được phép session-scoped?",
  "`metadata.create_all()` bỏ sót loại lỗi migration nào?",
  "Khi nào outer transaction rollback không thể dọn side effect của test?",
  "Thiết kế test hai transaction chống oversell mà không dùng `sleep()` như thế nào?",
  "Vì sao không nên assert ID liên tiếp sau rollback?",
  "Reference data và scenario data khác vòng đời ra sao?",
  "Ba hàng rào nào ngăn script test xóa nhầm production?",
  "Một query sắp xếp theo timestamp có giá trị hòa cần thêm gì để test ổn định?"
],

tai_nguyen: [
  "Database Reliability Engineering — Laine Campbell và Charity Majors.",
  "Architecture Patterns with Python — Harry Percival và Bob Gregory.",
  "PostgreSQL Documentation — Transactions, Constraints và Explicit Locking.",
  "SQLAlchemy Documentation — Joining a Session into an External Transaction."
],

lien_he: [
  "Nối sang mảng Database qua migration, isolation, constraint, index và row lock được kiểm bằng engine thật.",
  "Nối sang mảng Security qua least privilege, tách credential, che dữ liệu cá nhân và guard thao tác phá hủy.",
  "Nối sang mảng Docker/DevOps qua provision container, health check và database theo CI worker.",
  "Nối sang mảng Hệ phân tán qua outbox và worker nhiều connection, nơi rollback của request không còn đủ."
]

};
