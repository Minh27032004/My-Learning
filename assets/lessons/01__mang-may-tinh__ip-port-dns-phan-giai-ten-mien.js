window.LESSONS["01:mang-may-tinh:ip-port-dns-phan-giai-ten-mien"] = {

tom_tat: [
  "**Địa chỉ IP định danh một máy, cổng định danh một tiến trình trên máy đó.** Thiếu một trong hai là không giao được dữ liệu.",
  "Một kết nối được định danh bằng **bộ bốn**: IP nguồn, cổng nguồn, IP đích, cổng đích. Đây là lý do một server nghe cổng 443 phục vụ được hàng nghìn người cùng lúc.",
  "**IP riêng không định tuyến được trên Internet.** `192.168.x.x`, `10.x.x.x`, `172.16–31.x.x` chỉ dùng trong mạng nội bộ; ra ngoài phải qua **NAT**.",
  "**DNS là cây phân cấp, không phải một cuốn danh bạ.** Phân giải là đi từ gốc xuống, hỏi lần lượt: root → TLD → name server của tên miền.",
  "**TTL của DNS là lý do đổi bản ghi không có hiệu lực ngay.** Hạ TTL trước khi chuyển server là kỹ thuật bắt buộc, và phải hạ trước đó vài ngày."
],

can_biet_truoc: [
  "Bài **Mô hình TCP/IP 4 tầng** ngay trước — bài này đi sâu vào định danh của tầng Internet (IP) và tầng Giao vận (cổng).",
  "Bài **Hệ nhị phân, hexa, bit và byte** ở module 1 — cần hiểu byte để thấy `192.168.1.1` thực chất là bốn byte.",
  "Bài **Tiến trình và luồng** ở module 2 — cổng gắn với một tiến trình đang chờ dữ liệu.",
  "Bài **Biến môi trường và PATH** ở module 3 — không bắt buộc, nhưng khái niệm \"thứ tự tra cứu\" của PATH giống thứ tự tra cứu tên miền một cách thú vị."
],

dinh_nghia: "Nói đời thường: **IP là địa chỉ toà nhà, cổng là số phòng, DNS là tổng đài tra số nhà theo tên**. Bạn nói \"nhà anh Nam ở Cầu Giấy\", tổng đài tra ra số nhà, rồi bạn còn cần biết phòng nào trong toà nhà đó.\n\nNói chính xác:\n\n- **Địa chỉ IP** (*Internet Protocol address*) — con số định danh một giao diện mạng trong một mạng. IPv4 dài **32 bit**, viết thành bốn số 0–255. IPv6 dài **128 bit**, viết bằng hexa.\n- **Cổng** (*port*) — số **16 bit** (1–65535) mà hệ điều hành dùng để biết dữ liệu đến thuộc về tiến trình nào. Cổng thuộc tầng Giao vận, nên **TCP cổng 80 và UDP cổng 80 là hai cổng khác nhau**.\n- **Socket** — cặp (IP, cổng). Một đầu của một kết nối.\n- **DNS** (*Domain Name System*) — hệ thống phân tán, phân cấp, dịch tên miền thành địa chỉ IP và một số thông tin khác.\n- **Bản ghi** (*record*) — một dòng dữ liệu trong DNS. Loại `A` trả về IPv4, `AAAA` trả về IPv6, `CNAME` trỏ sang tên khác, `MX` chỉ server mail.\n\nĐiểm cần nắm ngay: **IP thuộc tầng 3, cổng thuộc tầng 4, DNS là một ứng dụng ở tầng 7 phục vụ cho việc dùng tầng 3**. DNS không phải một phần của IP — nó là một dịch vụ chạy trên mạng để giúp con người khỏi phải nhớ số.",

vi_sao_can: "Đây là bộ khái niệm bạn sẽ đụng tới mỗi ngày, không phải kiến thức để thi:\n\n- **Cấu hình mọi thứ.** Chuỗi kết nối database `postgres://user:pass@10.0.1.5:5432/db` chứa cả IP và cổng. Docker `-p 8080:80` là chuyển tiếp cổng. Biến `HOST=0.0.0.0` là chọn bind vào IP nào.\n- **Đọc hiểu lỗi.** `EADDRINUSE` là cổng đã bị chiếm. `ENOTFOUND` là DNS không phân giải được. `ECONNREFUSED` là không ai nghe cổng đó. Ba lỗi này chiếm phần lớn thời gian gỡ lỗi mạng của người mới, và mỗi lỗi chỉ đúng một nguyên nhân.\n- **Deploy và trỏ tên miền.** Mua tên miền, trỏ về server, chờ mãi không thấy đổi — vì TTL. Không biết TTL thì sẽ ngồi F5 vô nghĩa hàng giờ.\n- **Bảo mật.** Security group của cloud là danh sách \"IP nào được vào cổng nào\". Sai một dòng là mở database ra Internet.\n- **Kiến trúc.** Ở mảng System Design, service discovery, health check, blue-green deployment đều dựa trên DNS và cổng.\n\nTình huống thật rất kinh điển: bạn đổi bản ghi `A` của tên miền sang server mới. Máy bạn vẫn vào server cũ, còn máy đồng nghiệp đã vào server mới. Không biết về TTL và cache DNS nhiều tầng thì việc này trông như phép thuật — và bạn sẽ đi tìm lỗi ở chỗ hoàn toàn sai. Phần cơ chế giải thích chính xác vì sao, và phần ví dụ cho quy trình chuyển server đúng cách.",

co_che: "### Địa chỉ IPv4 thật ra là gì\n\n`192.168.1.10` trông như bốn con số, nhưng thực chất là **một số 32 bit** được viết theo kiểu dễ đọc:\n\n```\n   192   .   168   .    1    .   10\n11000000 . 10101000 . 00000001 . 00001010\n└──────────── 32 bit = 4 byte ─────────────┘\n```\n\nMỗi phần gọi là một *octet*, giá trị 0–255 vì một byte chỉ chứa được tới 255. Đây là lý do `192.168.1.256` là địa chỉ **không hợp lệ** — một lỗi gõ sai rất hay gặp trong file cấu hình.\n\nTổng số địa chỉ IPv4 là 2³² ≈ **4,3 tỉ**. Nghe nhiều, nhưng đã cạn từ lâu: thế giới có nhiều hơn 4,3 tỉ thiết bị nối mạng. Hai giải pháp ra đời từ thực tế cạn địa chỉ này là **NAT** và **IPv6**, cả hai đều nói ở dưới.\n\n### Mạng con và tiền tố `/24`\n\nMột địa chỉ IP luôn được hiểu kèm **mặt nạ mạng** (*netmask*), chia 32 bit thành hai phần: phần đầu chỉ **mạng**, phần sau chỉ **máy trong mạng đó**.\n\n```\n192.168.1.10/24\n              └─ 24 bit đầu là phần mạng, 8 bit còn lại là phần máy\n\n11000000.10101000.00000001 . 00001010\n└──────── mạng ───────────┘  └─ máy ─┘\n\n⇒ mạng    192.168.1.0\n⇒ máy     từ 192.168.1.1 tới 192.168.1.254\n⇒ số máy  2⁸ − 2 = 254   (trừ địa chỉ mạng .0 và broadcast .255)\n```\n\nVì sao cần chia như vậy? Vì đây là **cơ sở để máy quyết định gửi trực tiếp hay gửi qua router**:\n\n```\nMáy bạn 192.168.1.10/24 muốn gửi tới:\n\n192.168.1.50   → cùng mạng con  → gửi TRỰC TIẾP qua tầng Liên kết\n93.184.216.34  → khác mạng con  → gửi cho ROUTER (default gateway)\n```\n\nĐây chính là chỗ nối với bài trước: quyết định \"MAC đích là ai\" phụ thuộc vào phép so sánh mạng con này. Cùng mạng thì MAC đích là MAC của máy kia; khác mạng thì MAC đích là MAC của router.\n\nMột vài tiền tố hay gặp, đáng nhớ luôn:\n\n| Tiền tố | Số máy dùng được | Gặp ở đâu |\n|---|---|---|\n| `/32` | 1 | chỉ một địa chỉ, dùng trong firewall rule |\n| `/24` | 254 | mạng LAN gia đình, subnet nhỏ |\n| `/16` | 65 534 | VPC cỡ trung trên cloud |\n| `/8` | 16 triệu | dải `10.0.0.0/8` |\n| `/0` | mọi địa chỉ | `0.0.0.0/0` nghĩa là \"tất cả\" |\n\n`0.0.0.0/0` xuất hiện rất nhiều trong cấu hình cloud và đáng cảnh giác: một security group cho phép SSH từ `0.0.0.0/0` nghĩa là **mở cổng 22 cho toàn thế giới**.\n\n### IP riêng, IP công khai và NAT\n\nBa dải địa chỉ được dành riêng cho mạng nội bộ và **không được định tuyến trên Internet**:\n\n```\n10.0.0.0/8         10.0.0.0    – 10.255.255.255     (16 triệu địa chỉ)\n172.16.0.0/12      172.16.0.0  – 172.31.255.255     (1 triệu)\n192.168.0.0/16     192.168.0.0 – 192.168.255.255    (65 nghìn)\n```\n\nRouter trên Internet **vứt bỏ** mọi gói có đích thuộc các dải này. Nhờ vậy, hàng triệu mạng gia đình đều dùng chung `192.168.1.x` mà không xung đột.\n\nVậy máy trong nhà ra Internet bằng cách nào? Bằng **NAT** (*Network Address Translation*) ở router:\n\n```\nMáy bạn                Router NAT                    Server\n192.168.1.10:51234 ──► đổi thành 203.0.113.9:40001 ──► 93.184.x.x:443\n                       ghi vào bảng:\n                       40001 ↔ 192.168.1.10:51234\n\nGói trả về 203.0.113.9:40001 ──► tra bảng ──► 192.168.1.10:51234\n```\n\nRouter viết lại **cả IP và cổng nguồn**, rồi ghi nhớ để dịch ngược cho gói hồi âm. Đây là lý do NAT là cơ chế của cả tầng 3 và tầng 4, không thuần tầng 3.\n\nHệ quả rất thực dụng: **máy sau NAT gọi ra được, nhưng bên ngoài không gọi vào được** — vì router không biết chuyển gói đến vô cớ cho máy nào trong nhà. Muốn vào được phải cấu hình *port forwarding* thủ công. Đây chính là lý do bạn không thể cho bạn bè truy cập server chạy trên laptop ở nhà chỉ bằng cách đưa IP.\n\nĐánh đổi của NAT: nó cứu IPv4 khỏi cạn kiệt, nhưng **phá vỡ nguyên tắc kết nối đầu-cuối** của Internet, làm phức tạp mọi giao thức ngang hàng (gọi video, game, torrent) và buộc chúng phải dùng thêm kỹ thuật xuyên NAT.\n\n### IPv6\n\nIPv6 dùng **128 bit**, tức 2¹²⁸ địa chỉ — con số lớn tới mức không có nguy cơ cạn:\n\n```\n2001:0db8:0000:0000:0000:ff00:0042:8329\n2001:db8::ff00:42:8329          ← viết gọn: bỏ 0 đầu, :: thay chuỗi 0 dài nhất\n\n::1                            ← localhost của IPv6, tương đương 127.0.0.1\n```\n\nDấu `::` chỉ được dùng **một lần** trong địa chỉ, vì nếu dùng hai lần thì không suy ra được mỗi chỗ thay bao nhiêu nhóm 0.\n\nVới IPv6, mỗi thiết bị lại có địa chỉ công khai thật, NAT không còn cần thiết. Nhưng chuyển đổi diễn ra rất chậm vì **IPv4 và IPv6 không nói chuyện trực tiếp được** — phải chạy song song cả hai, và NAT đã \"đủ tốt\" nên áp lực chuyển đổi giảm.\n\nĐiểm cần lưu ý khi lập trình: địa chỉ IPv6 chứa dấu `:`, nên trong URL phải bọc trong ngoặc vuông để không lẫn với dấu phân cách cổng:\n\n```\nhttp://[2001:db8::1]:8080/api\n```\n\nQuên ngoặc vuông là một lỗi phân tích URL rất hay gặp khi hệ thống bắt đầu hỗ trợ IPv6.\n\n### Cổng — 65535 cửa vào một máy\n\nIP đưa gói tới đúng máy. Nhưng trên máy đó có hàng chục tiến trình đang chờ dữ liệu. **Cổng là số hiệu để hệ điều hành biết giao cho ai.**\n\n```\nMột máy: 203.0.113.9\n ├─ cổng 22   → tiến trình sshd\n ├─ cổng 80   → tiến trình nginx\n ├─ cổng 443  → tiến trình nginx\n ├─ cổng 5432 → tiến trình postgres\n └─ cổng 8000 → ứng dụng của bạn\n```\n\nDải cổng chia làm ba vùng theo quy ước:\n\n| Dải | Tên | Đặc điểm |\n|---|---|---|\n| 0–1023 | *well-known* | Cần **quyền root** để nghe trên Linux |\n| 1024–49151 | *registered* | Dịch vụ phổ biến: 3306 MySQL, 5432 Postgres, 6379 Redis |\n| 49152–65535 | *ephemeral* | Hệ điều hành tự cấp cho cổng **nguồn** |\n\nQuy tắc \"dưới 1024 cần root\" giải thích một chuyện thực tế: vì sao ứng dụng thường chạy ở cổng 8080 hay 3000 rồi để nginx đứng trước ở cổng 80. **Chạy ứng dụng dưới quyền root chỉ để nghe cổng 80 là lựa chọn tồi** — một lỗ hổng trong ứng dụng sẽ thành quyền root trên cả máy.\n\nCổng phổ biến nên thuộc lòng:\n\n```\n 22  SSH        80  HTTP      443  HTTPS     53  DNS\n3306 MySQL    5432 Postgres  6379 Redis    27017 MongoDB\n```\n\n### Bộ bốn định danh một kết nối\n\nĐây là ý quan trọng nhất về cổng, và cũng là ý bị hiểu sai nhiều nhất. Một kết nối **không** được định danh bằng cổng, mà bằng **bộ bốn**:\n\n```\n(IP nguồn, cổng nguồn, IP đích, cổng đích)\n```\n\nNhờ vậy một server nghe duy nhất cổng 443 vẫn phục vụ được hàng nghìn kết nối đồng thời:\n\n```\n(1.2.3.4, 51234, 203.0.113.9, 443)   ← người A\n(1.2.3.4, 51235, 203.0.113.9, 443)   ← cũng người A, tab khác\n(5.6.7.8, 44012, 203.0.113.9, 443)   ← người B\n```\n\nBa kết nối, cùng cổng đích, khác bộ bốn ⇒ hệ điều hành phân biệt được hoàn toàn.\n\nHiểu điều này xoá tan một quan niệm sai phổ biến: *\"server chỉ có 65535 cổng nên tối đa 65535 người dùng\"*. Sai — giới hạn 65535 áp cho **cổng nguồn của một máy khách khi nối tới cùng một đích**, không phải cho số kết nối server nhận được.\n\n### DNS là một cây, không phải một danh bạ\n\nNếu DNS là một file duy nhất chứa mọi tên miền, nó sẽ vừa khổng lồ vừa là điểm chết duy nhất. Thực tế DNS là **cây phân cấp, mỗi cấp tự quản lý phần của mình**:\n\n```\n                        . (root)\n                    ┌──────┼──────┐\n                  com     org     vn        ← TLD\n                    │              │\n              example            com        ← tên miền cấp 2\n                    │              │\n                  www           fpt\n```\n\nĐọc tên miền **từ phải sang trái** mới đúng thứ tự cây: `www.example.com.` — dấu chấm cuối là root, thường được lược đi.\n\nBốn vai trò trong hệ thống:\n\n| Vai trò | Việc của nó |\n|---|---|\n| **Stub resolver** | Thư viện trong máy bạn, nơi `getaddrinfo()` gọi vào |\n| **Recursive resolver** | Máy chủ đi hỏi hộ bạn, có cache. Router nhà, hoặc `8.8.8.8` |\n| **Root & TLD server** | Chỉ trả lời \"hỏi tiếp ai\", không giữ dữ liệu cuối |\n| **Authoritative server** | Nắm dữ liệu thật của tên miền |\n\n### Phân giải một tên miền, từng bước\n\n```\nTrình duyệt cần IP của www.example.com\n\n① cache trình duyệt?      không có\n② cache hệ điều hành?     không có\n③ /etc/hosts?             không có\n④ hỏi recursive resolver (ví dụ 8.8.8.8)\n     ↓ resolver có cache? nếu có, trả về ngay — hết\n     ↓ không có, nó đi hỏi:\n     ├─► root server:  \"www.example.com ở đâu?\"\n     │   ← \"tôi không biết, nhưng .com do các server này quản\"\n     ├─► TLD .com:     \"www.example.com ở đâu?\"\n     │   ← \"example.com do ns1.example.com quản\"\n     └─► ns1.example.com (authoritative):\n         ← \"www.example.com A 93.184.x.x, TTL 3600\"\n⑤ resolver lưu cache 3600 giây rồi trả kết quả cho bạn\n```\n\nHai điều đáng chú ý:\n\n- **Root và TLD không trả lời câu hỏi, chúng chỉ chỉ đường.** Đây là thứ giúp hệ thống chịu tải nổi: root server không cần biết gì về hàng tỉ tên miền.\n- **Cache có ở rất nhiều tầng**, và đó là gốc rễ của mọi rắc rối khi đổi bản ghi.\n\n### Các loại bản ghi\n\n| Loại | Trả về | Dùng khi |\n|---|---|---|\n| `A` | một IPv4 | trỏ tên miền về server |\n| `AAAA` | một IPv6 | như trên, cho IPv6 |\n| `CNAME` | **một tên khác** | trỏ `www` về tên chính, hoặc về CDN |\n| `MX` | server nhận mail + độ ưu tiên | cấu hình email |\n| `TXT` | chuỗi tự do | xác minh sở hữu tên miền, SPF/DKIM |\n| `NS` | server nào có quyền | uỷ quyền tên miền con |\n\nHai luật về `CNAME` mà hầu hết người mới vi phạm:\n\n1. **Không đặt `CNAME` ở gốc tên miền** (`example.com`, không có tiền tố). Lý do: gốc bắt buộc phải có bản ghi `NS` và `SOA`, mà `CNAME` theo chuẩn không được đứng cùng bản ghi khác. Nhiều nhà cung cấp DNS làm sẵn giải pháp riêng (tên gọi khác nhau như *ALIAS*, *ANAME*, *flattening*).\n2. **`CNAME` tốn thêm một vòng tra cứu.** Trỏ `CNAME` chồng nhiều tầng làm chậm lần truy cập đầu tiên.\n\n### TTL — vì sao đổi DNS không có hiệu lực ngay\n\nMỗi bản ghi kèm một **TTL** tính bằng giây. Resolver được phép giữ cache trong khoảng đó.\n\n```\nwww.example.com.  3600  IN  A  93.184.x.x\n                  └── giữ cache 3600 giây = 1 giờ\n```\n\nBạn đổi bản ghi lúc 10:00. Một resolver vừa cache lúc 09:59 sẽ **vẫn trả IP cũ tới 10:59**. Bạn không có cách nào buộc nó bỏ cache. Đây là lý do:\n\n```\nMáy bạn      → resolver A, cache mới → thấy IP MỚI\nĐồng nghiệp  → resolver B, cache cũ  → vẫn thấy IP CŨ\n```\n\nQuy trình đúng khi chuyển server, và **thứ tự là điều cốt lõi**:\n\n```\nT−48h  hạ TTL xuống 300 giây  → chờ TTL CŨ hết hạn ở mọi nơi\nT−0    đổi bản ghi A sang IP mới\nT+5m   gần như mọi resolver đã cập nhật\nT+24h  nâng TTL lại 3600 cho hiệu năng\n```\n\nBước T−48h là bước bị bỏ nhiều nhất, và bỏ nó thì mọi bước sau vô nghĩa: hạ TTL **cùng lúc** với đổi IP không giúp gì, vì cache cũ vẫn còn sống theo TTL cũ. TTL chỉ có tác dụng cho những lần tra cứu **sau** khi nó được lan ra.\n\nMột lưu ý nữa: trong thời gian chuyển, **cả hai server phải cùng chạy được**. Tắt server cũ ngay khi đổi bản ghi là tự gây gián đoạn cho những người còn cache cũ.\n\n### `/etc/hosts` và thứ tự tra cứu\n\nTrước khi hỏi DNS, hệ điều hành xem file cục bộ:\n\n```bash\n# /etc/hosts  (Windows: C:\\Windows\\System32\\drivers\\etc\\hosts)\n127.0.0.1    localhost\n127.0.0.1    myapp.local\n203.0.113.9  staging.example.com\n```\n\nFile này **thắng DNS**, không có TTL, có hiệu lực ngay. Rất hữu ích để thử một server mới trước khi đổi DNS thật:\n\n```bash\n# thêm dòng trỏ tên miền về server mới, chỉ trên máy mình\necho \"203.0.113.99  example.com\" | sudo tee -a /etc/hosts\n# → mở trình duyệt kiểm tra server mới với tên miền thật, TLS thật\n# → xong thì XOÁ dòng đó, đừng để quên\n```\n\nĐể quên dòng này là một cạm bẫy khó chịu: vài tuần sau tên miền đã trỏ chỗ khác mà máy bạn vẫn vào server cũ, và không lệnh DNS nào giải thích được vì sao.",

vi_du: "### Xem cấu hình mạng của chính mình\n\n```bash\n# IP và mạng con\nip addr show                 # Linux\nipconfig                     # Windows\n# inet 192.168.1.10/24 brd 192.168.1.255 scope global wlan0\n#      └─ IP ─────┘ └─ tiền tố mạng con\n\n# Router mặc định — nơi mọi gói ra ngoài mạng con đi qua\nip route | grep default\n# default via 192.168.1.1 dev wlan0\n\n# IP công khai của bạn (khác IP riêng, vì có NAT ở giữa)\ncurl -s ifconfig.me\n```\n\nSo hai kết quả cuối là thấy NAT ngay: `ip addr` cho `192.168.1.10`, còn `curl ifconfig.me` cho một địa chỉ hoàn toàn khác — địa chỉ công khai của router nhà bạn. Toàn bộ nhà bạn dùng chung địa chỉ đó khi ra Internet.\n\n### Cổng nào đang được nghe\n\n```bash\nss -tlnp\n#  -t TCP   -l đang LISTEN   -n số thay vì tên   -p tiến trình nào\n\n# State  Local Address:Port   Process\n# LISTEN 127.0.0.1:5432       postgres    ← chỉ máy này vào được\n# LISTEN 0.0.0.0:8000         python      ← mọi nơi vào được\n# LISTEN [::]:443             nginx       ← IPv6, mọi nơi\n```\n\nCột `Local Address` là cột quyết định:\n\n| Giá trị | Nghĩa |\n|---|---|\n| `127.0.0.1` | **chỉ** tiến trình trên chính máy này |\n| `0.0.0.0` | mọi giao diện mạng, ngoài vào được |\n| `192.168.1.10` | chỉ giao diện đó — vào từ LAN được, từ Internet thì không |\n\nĐây là công cụ đầu tiên phải chạy khi \"gọi API từ ngoài không được\", và nó thường trả lời xong câu hỏi ngay tại đó.\n\n### Xử lý cổng đã bị chiếm\n\n```bash\n# Lỗi: EADDRINUSE / Address already in use\nss -tlnp | grep 8000\n# LISTEN 0.0.0.0:8000  users:((\"python\",pid=12345,fd=3))\n\nkill 12345          # xin tiến trình tự dọn rồi thoát\nkill -9 12345       # cưỡng chế, chỉ dùng khi trên không xong\n\n# Trên Windows\n# netstat -ano | findstr :8000\n# taskkill /PID 12345 /F\n```\n\nNối với bài **Tín hiệu, tiến trình nền, exit code** ở module 2: `kill` gửi SIGTERM cho tiến trình dọn dẹp tử tế, `kill -9` gửi SIGKILL không thể bắt. Với server đang giữ kết nối database, dùng `-9` ngay có thể để lại kết nối treo phía database.\n\n### Tra DNS bằng `dig`\n\n```bash\ndig example.com\n# ;; ANSWER SECTION:\n# example.com.  3600  IN  A  93.184.x.x\n#               └── TTL còn lại trong cache\n\ndig +short example.com          # chỉ kết quả\ndig example.com MX              # bản ghi mail\ndig example.com NS              # server nào có quyền\ndig +trace example.com          # ⭐ xem TOÀN BỘ đường đi từ root xuống\n```\n\n`dig +trace` là lệnh nên chạy một lần cho thấm: nó in ra đúng chuỗi root → TLD → authoritative đã mô tả ở phần cơ chế. Đọc output của nó xong thì khái niệm \"DNS là cây\" không còn trừu tượng nữa.\n\nMột mẹo chẩn đoán quan trọng — **hỏi thẳng server có quyền, bỏ qua mọi cache**:\n\n```bash\n# ① tìm server có quyền\ndig +short example.com NS\n# ns1.example.com.\n\n# ② hỏi trực tiếp nó\ndig @ns1.example.com example.com A\n```\n\nKết quả từ `@ns1` là **sự thật hiện tại**. So nó với kết quả `dig example.com` thông thường:\n\n```\n@ns1 trả IP mới, dig thường trả IP cũ  ⇒ bản ghi ĐÃ đổi, chỉ là cache chưa hết\nCả hai đều trả IP cũ                   ⇒ bản ghi CHƯA đổi, kiểm tra lại bảng DNS\n```\n\nPhân biệt được hai trường hợp này tiết kiệm hàng giờ ngồi chờ vô nghĩa.\n\n### Xoá cache DNS cục bộ\n\n```bash\n# Windows\nipconfig /flushdns\n\n# macOS\nsudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder\n\n# Linux dùng systemd-resolved\nsudo resolvectl flush-caches\n```\n\nCần nói rõ giới hạn: lệnh này **chỉ xoá cache trên máy bạn**. Cache ở resolver của nhà mạng thì bạn không chạm được, và đó thường mới là thủ phạm. Xoá cache máy mình xong vẫn thấy IP cũ là chuyện bình thường, không phải lệnh chạy sai.\n\n### Kiểm tra một cổng từ xa\n\n```bash\nnc -zv example.com 443        # thử TCP\nnc -zvu dns-server 53         # thử UDP (-u)\n\n# Không có nc? dùng bash thuần\ntimeout 3 bash -c 'cat < /dev/null > /dev/tcp/example.com/443' \\\n  && echo mở || echo đóng\n```\n\nMẹo `/dev/tcp` rất hữu ích trên container tối giản không có `nc`, `telnet`, hay `curl` — nó là tính năng dựng sẵn của bash, không cần cài gì.\n\n### Quy trình chuyển server không gián đoạn\n\n```bash\n# T−48h: hạ TTL, CHƯA đổi IP\n# example.com.  300  IN  A  93.184.x.x       ← chỉ đổi 3600 → 300\ndig example.com | grep -E \"^example\"          # xác nhận TTL đã là 300\n\n# T−1h: kiểm tra server mới bằng /etc/hosts, chưa ảnh hưởng ai\necho \"203.0.113.99  example.com\" | sudo tee -a /etc/hosts\ncurl -I https://example.com                   # kiểm tra cả TLS lẫn nội dung\nsudo sed -i '/203.0.113.99/d' /etc/hosts      # XOÁ ngay sau khi xong\n\n# T−0: đổi bản ghi A sang 203.0.113.99\n# Theo dõi từ nhiều resolver khác nhau\nfor r in 8.8.8.8 1.1.1.1 9.9.9.9; do\n  echo -n \"$r → \"; dig +short @$r example.com\ndone\n\n# T+24h: nâng TTL lại 3600\n```\n\nVòng lặp `for` trên là cách kiểm tra thực tế nhất: mỗi resolver có cache riêng, nên thấy chúng lần lượt đổi sang IP mới là bằng chứng quá trình lan toả đang diễn ra đúng. **Giữ server cũ sống cho tới khi cả ba đều trả IP mới**, rồi mới tắt.",

so_sanh: "### IP, cổng, tên miền — ba thứ hay bị lẫn\n\n| | Địa chỉ IP | Cổng | Tên miền |\n|---|---|---|---|\n| Tầng | 3 (Internet) | 4 (Giao vận) | 7 (Ứng dụng) |\n| Định danh | một máy trong mạng | một tiến trình trên máy | một cái tên cho người đọc |\n| Kiểu dữ liệu | 32 bit / 128 bit | 16 bit (1–65535) | chuỗi |\n| Ai cấp | DHCP, nhà cung cấp | hệ điều hành hoặc lập trình viên | nhà đăng ký tên miền |\n| Có bắt buộc? | **có** | **có** | không — dùng IP trực tiếp vẫn được |\n\nHàng cuối đáng suy nghĩ: **DNS là tiện lợi, không phải bắt buộc**. Truy cập trực tiếp bằng IP luôn được. Đây là lý do khi DNS hỏng mà `ping 8.8.8.8` vẫn chạy — một phép thử phân biệt \"mất mạng\" với \"hỏng DNS\" trong ba giây.\n\n### IPv4 và IPv6\n\n| | IPv4 | IPv6 |\n|---|---|---|\n| Độ dài | 32 bit | 128 bit |\n| Số địa chỉ | 4,3 tỉ (**đã cạn**) | 2¹²⁸ |\n| Cách viết | `192.168.1.1` | `2001:db8::1` |\n| NAT | gần như bắt buộc | không cần |\n| Trong URL | `http://1.2.3.4:80` | `http://[2001:db8::1]:80` |\n| Localhost | `127.0.0.1` | `::1` |\n\nĐiểm ít được nhắc: IPv6 **không** \"nhanh hơn\" IPv4 như quảng cáo hay nói. Lợi ích thật là không cần NAT, header đơn giản hơn, và tự cấu hình địa chỉ dễ hơn. Tốc độ cảm nhận được đôi khi tốt hơn chỉ vì bỏ được một tầng NAT, không phải vì bản thân giao thức nhanh hơn.\n\n### `127.0.0.1` và `0.0.0.0`\n\nHai giá trị này bị nhầm liên tục và hậu quả rất cụ thể:\n\n| | `127.0.0.1` (localhost) | `0.0.0.0` |\n|---|---|---|\n| Ý nghĩa khi **bind** | chỉ nhận từ chính máy này | nhận trên **mọi** giao diện |\n| Ý nghĩa khi **gọi** | gọi chính máy này | không hợp lệ để gọi |\n| Gói tin ra dây | **không**, không rời khỏi máy | có |\n| Dùng khi | dev cục bộ, database nội bộ | server cần nhận từ ngoài |\n\nQuy tắc thực dụng: **`0.0.0.0` cho ứng dụng cần phục vụ bên ngoài, `127.0.0.1` cho mọi thứ chỉ dùng nội bộ**. Database bind `0.0.0.0` mà không có firewall là một trong những lỗi cấu hình gây rò rỉ dữ liệu phổ biến nhất.\n\n### `CNAME` và `A`\n\n| | `A` | `CNAME` |\n|---|---|---|\n| Trả về | địa chỉ IP | một tên miền khác |\n| Số vòng tra cứu | 1 | **2 hoặc hơn** |\n| Đặt ở gốc tên miền | ✓ | **✗ không được** |\n| Cùng tồn tại với bản ghi khác | ✓ | ✗ |\n| Khi IP đổi | phải sửa | **tự theo, không phải sửa** |\n\nHàng cuối là lý do `CNAME` tồn tại: trỏ `www.example.com` bằng `CNAME` về `d123.cloudfront.net` thì nhà cung cấp CDN đổi IP bao nhiêu lần cũng không cần bạn sửa gì. Đánh đổi là thêm một vòng tra cứu và không dùng được ở gốc tên miền.\n\n### Cổng nguồn và cổng đích\n\n| | Cổng đích | Cổng nguồn |\n|---|---|---|\n| Ai chọn | lập trình viên / quy ước | **hệ điều hành tự cấp** |\n| Giá trị | cố định, đã biết (443, 5432) | ngẫu nhiên trong dải 49152–65535 |\n| Đổi mỗi kết nối | không | **có** |\n| Cần biết trước | có | không |\n\nĐây là chỗ người mới hay bối rối khi đọc `tcpdump`: cổng nguồn khác nhau ở mọi dòng vì mỗi kết nối được cấp một cổng nguồn mới. Cổng nào **cố định qua nhiều dòng** thì đó là cổng dịch vụ.",

loi_thuong_gap: [
  "**Bind vào `127.0.0.1` rồi thắc mắc sao bên ngoài không gọi được.** Kiểm tra bằng `ss -tlnp`; cần `0.0.0.0` để nhận từ ngoài.",
  "**Ngược lại: bind database vào `0.0.0.0` mà không có firewall.** Mở database ra Internet — một trong những nguyên nhân rò rỉ dữ liệu phổ biến nhất.",
  "**Đổi bản ghi DNS rồi kết luận \"không có tác dụng\" sau 5 phút.** TTL cũ vẫn còn hiệu lực. Kiểm tra bằng `dig @ns-có-quyền` để biết bản ghi đã đổi thật chưa.",
  "**Hạ TTL cùng lúc với đổi IP.** Vô nghĩa — cache cũ sống theo TTL cũ. Phải hạ TTL trước ít nhất một khoảng bằng TTL cũ.",
  "**Tắt server cũ ngay khi đổi bản ghi DNS.** Người còn cache cũ sẽ bị gián đoạn. Giữ cả hai chạy song song tới khi lan toả xong.",
  "**Đặt `CNAME` ở gốc tên miền.** Không hợp chuẩn. Dùng `A`, hoặc dùng cơ chế riêng của nhà cung cấp (ALIAS/ANAME).",
  "**Để quên dòng thử nghiệm trong `/etc/hosts`.** Vài tuần sau máy bạn vào server cũ mà không lệnh DNS nào giải thích được.",
  "**Tưởng server chỉ chịu được 65535 kết nối vì chỉ có 65535 cổng.** Kết nối được định danh bằng bộ bốn, không phải chỉ cổng đích.",
  "**Chạy ứng dụng bằng root chỉ để nghe cổng 80.** Đặt nginx đứng trước, hoặc dùng chuyển tiếp cổng. Một lỗ hổng ứng dụng sẽ thành quyền root.",
  "**Quên bọc ngoặc vuông cho IPv6 trong URL.** `http://[2001:db8::1]:8080` mới đúng — không có ngoặc thì dấu `:` bị hiểu là phân cách cổng.",
  "**Xoá cache DNS máy mình rồi tưởng đã xoá mọi cache.** Cache ở resolver nhà mạng bạn không chạm tới được."
],

tu_kiem_tra: [
  "`192.168.1.10/24` — phần nào là mạng, phần nào là máy, và có bao nhiêu máy dùng được trong mạng đó?",
  "Vì sao ba dải `10.x`, `172.16–31.x`, `192.168.x` không định tuyến được trên Internet? Máy trong nhà ra Internet bằng cách nào?",
  "Một kết nối TCP được định danh bằng những gì? Vì sao server nghe một cổng 443 lại phục vụ được hàng nghìn người?",
  "Khác nhau giữa bind `127.0.0.1` và `0.0.0.0` là gì? Hậu quả cụ thể của việc chọn sai?",
  "Mô tả các bước phân giải `www.example.com` từ lúc chưa có cache nào.",
  "Bạn đổi bản ghi `A` lúc 10:00, TTL cũ là 3600. Sớm nhất và muộn nhất khi nào mọi người thấy IP mới?",
  "Làm sao phân biệt \"bản ghi chưa đổi\" với \"đã đổi nhưng cache chưa hết\"?",
  "Vì sao không đặt `CNAME` ở gốc tên miền, và `CNAME` bù lại cho ta lợi ích gì?",
  "`ping 8.8.8.8` chạy được nhưng `ping google.com` báo lỗi tên. Hỏng ở đâu?"
],

tai_nguyen: [
  "Sách **Computer Networking: A Top-Down Approach** — Kurose & Ross, chương về tầng Ứng dụng có phần DNS rất rõ.",
  "Sách **DNS and BIND** — Cricket Liu & Paul Albitz. Chuẩn mực về DNS, dùng để tra cứu khi cần đi sâu.",
  "Tài liệu Linux: `man ip`, `man ss`, `man dig`, `man hosts`. `man dig` liệt kê đủ các tuỳ chọn chẩn đoán.",
  "Tài liệu của nhà cung cấp DNS bạn đang dùng — mục về ALIAS/ANAME ở gốc tên miền, vì mỗi bên đặt tên và triển khai khác nhau.",
  "Sách **The TCP/IP Guide** — Charles Kozierok. Miễn phí đọc trực tuyến, phần về mạng con và NAT giải thích rất chi tiết."
],

lien_he: [
  "**Mô hình TCP/IP 4 tầng** ngay trước: IP là định danh tầng 3, cổng là định danh tầng 4 — bài này đi sâu vào cả hai.",
  "**TCP vs UDP** ngay sau: cùng dùng khái niệm cổng nhưng cách quản lý kết nối hoàn toàn khác.",
  "**HTTPS, TLS handshake**: chứng chỉ TLS gắn với **tên miền**, không gắn với IP — nên đổi IP không cần cấp lại chứng chỉ.",
  "**Proxy, reverse proxy, CDN**: CDN hoạt động dựa trên việc DNS trả IP khác nhau tuỳ vị trí người hỏi.",
  "**Tín hiệu và exit code** ở module 2: `kill` và `kill -9` khi dọn tiến trình chiếm cổng khác nhau ở mức độ tử tế.",
  "**SSH, scp, rsync** ở module 3: `ssh -L 5432:localhost:5432` giờ đọc được — chuyển tiếp cổng cục bộ sang cổng trên server.",
  "**Database** ở mảng 04: Postgres cổng 5432, MySQL 3306, và quyết định bind vào đâu là quyết định an toàn quan trọng nhất khi dựng database.",
  "**Backend & Web** ở mảng 05: cấu hình Docker `-p`, biến `HOST`/`PORT`, và trỏ tên miền khi deploy đều là ứng dụng trực tiếp của bài này.",
  "**System Design** ở mảng 06: service discovery và blue-green deployment dựa trên DNS, và TTL quyết định tốc độ chuyển đổi."
]

};
