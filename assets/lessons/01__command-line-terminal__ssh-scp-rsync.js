window.LESSONS["01:command-line-terminal:ssh-scp-rsync"] = {

tom_tat: [
  "**SSH mã hoá kênh và xác thực hai chiều**: bạn kiểm tra server đúng là server, server kiểm tra bạn đúng là bạn.",
  "**Đăng nhập bằng khoá tốt hơn mật khẩu**: không gõ được nên không đoán được, không lộ khi bị nghe lén, và tự động hoá được.",
  "Cảnh báo *host key changed* **không bao giờ nên bỏ qua** — nó có thể là tấn công người-đứng-giữa, mà cũng có thể chỉ là server được dựng lại.",
  "**`rsync` hơn `scp` ở mọi mặt** với thư mục: tiếp tục được khi đứt, chỉ truyền phần khác biệt, hiện tiến độ.",
  "File `~/.ssh/config` biến `ssh -p 2222 -i ~/.ssh/key deploy@1.2.3.4` thành `ssh prod`. Đây là thứ đáng cấu hình sớm nhất."
],

can_biet_truoc: [
  "Bài **Quyền file: chmod, chown, sudo** — khoá riêng bắt buộc phải `chmod 600`.",
  "Bài **Thao tác file** — cần biết `cp`, và sự khác biệt của dấu gạch chéo cuối.",
  "Bài **Mạng máy tính** (module 4) sẽ nói kỹ hơn về TCP và port — chưa đọc cũng theo được bài này."
],

dinh_nghia: "Nói đời thường: SSH giống như **một đường ống kín nối thẳng máy bạn tới máy chủ**. Không ai trên đường nghe được nội dung, và hai đầu đều xác nhận danh tính của nhau trước khi nói chuyện.\n\nNói chính xác:\n\n- **SSH** (*Secure Shell*) là giao thức đăng nhập từ xa và truyền dữ liệu qua kênh đã mã hoá, mặc định trên cổng 22.\n- **`scp`** (*secure copy*) — sao chép file qua SSH, cú pháp giống `cp`.\n- **`rsync`** — đồng bộ thư mục, chỉ truyền **phần khác biệt**, chạy được qua SSH.\n- **Cặp khoá** gồm **khoá riêng** (giữ bí mật trên máy bạn) và **khoá công khai** (đặt trên server). Chỉ khoá riêng ký được thứ mà khoá công khai xác minh.",

vi_sao_can: "Không dùng được SSH là không làm được backend:\n\n- **Mọi server đều truy cập qua SSH.** Không có giao diện đồ hoạ, không có lựa chọn khác.\n- **Git dùng SSH.** `git clone git@github.com:...` chính là SSH. Cấu hình sai là không push được.\n- **Triển khai và CI/CD.** Script deploy dùng SSH để chạy lệnh trên server, và dùng `rsync` để đẩy file.\n- **Chuyển dữ liệu lớn.** Tải dataset 50 GB từ server về, đứt mạng giữa chừng — `rsync` tiếp tục được, `scp` thì phải làm lại từ đầu.\n- **Bảo mật.** Hiểu cảnh báo *host key changed* là hiểu khi nào mình đang bị tấn công.\n\nTình huống thật: bạn train model trên server suốt đêm, sáng ra SSH bị đứt và **process chết theo** vì nhận SIGHUP. Sáu tiếng tính toán mất trắng. Phần cơ chế cho biết cách phòng bằng `tmux` — và vì sao nó là thói quen bắt buộc khi làm việc từ xa.",

co_che: "### Hai chiều xác thực\n\nSSH giải hai bài toán tách biệt, và người mới thường chỉ để ý tới bài toán thứ hai:\n\n```\n① Server chứng minh nó là server thật     → host key\n② Bạn chứng minh bạn là bạn               → mật khẩu hoặc khoá\n```\n\nBước ① quan trọng không kém: nếu bỏ qua, kẻ tấn công có thể giả làm server, nhận mật khẩu của bạn, rồi chuyển tiếp sang server thật mà bạn không hề biết.\n\n### Host key và lần kết nối đầu tiên\n\n```bash\nssh user@server.com\n# The authenticity of host 'server.com' can't be established.\n# ED25519 key fingerprint is SHA256:abc123...\n# Are you sure you want to continue connecting (yes/no)?\n```\n\nLần đầu, máy bạn chưa biết server. Nó hiện **dấu vân tay** của host key và hỏi bạn.\n\nĐúng ra bạn phải **đối chiếu dấu vân tay này với nguồn tin cậy** — bảng điều khiển của nhà cung cấp, hoặc trang tài liệu chính thức. Thực tế hầu hết mọi người gõ `yes` ngay. Đây gọi là *trust on first use*: rủi ro chỉ nằm ở lần đầu, và sau đó khoá được ghi vào `~/.ssh/known_hosts` để kiểm tra mọi lần sau.\n\n```bash\n# GitHub công bố dấu vân tay của họ trong tài liệu — nên đối chiếu\nssh-keyscan github.com | ssh-keygen -lf -\n```\n\n### Khi host key đổi\n\n```\n@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@\n@    WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!     @\n@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@\nIT IS POSSIBLE THAT SOMEONE IS DOING SOMETHING NASTY!\n```\n\nHai khả năng, và bạn **phải xác định là khả năng nào**:\n\n| Nguyên nhân | Có bình thường không |\n|---|---|\n| Server được cài lại, đổi IP, hoặc thay máy | ✓ Bình thường |\n| Có người đứng giữa giả làm server | ✗ **Đang bị tấn công** |\n\n```bash\n# Chỉ làm việc này khi CHẮC CHẮN server vừa được dựng lại\nssh-keygen -R server.com        # xoá khoá cũ khỏi known_hosts\nssh user@server.com             # kết nối lại, xác nhận vân tay mới\n```\n\n**Đừng bao giờ dùng `StrictHostKeyChecking=no` để cho nhanh** — nó tắt hẳn bước bảo vệ ①, đúng thứ mà cảnh báo đang cố nói với bạn.\n\n### Đăng nhập bằng khoá\n\n```bash\n# ① Tạo cặp khoá — ed25519 là thuật toán hiện đại, khoá ngắn và mạnh\nssh-keygen -t ed25519 -C \"minh@laptop\"\n# Enter file: (Enter để dùng mặc định ~/.ssh/id_ed25519)\n# Enter passphrase: ← NÊN đặt, xem giải thích bên dưới\n\n# ② Copy khoá CÔNG KHAI lên server\nssh-copy-id user@server.com\n\n# ③ Kiểm tra\nssh user@server.com      # vào thẳng, không hỏi mật khẩu\n```\n\nCơ chế bên dưới không phải là gửi khoá đi:\n\n```\nBạn ────► server: \"tôi muốn đăng nhập bằng khoá này\"\nserver ──► bạn:   một chuỗi ngẫu nhiên (thử thách)\nbạn ────► server: chữ ký của chuỗi đó, tạo bằng KHOÁ RIÊNG\nserver:           xác minh chữ ký bằng KHOÁ CÔNG KHAI đã lưu\n                  khớp → cho vào\n```\n\n**Khoá riêng không bao giờ rời khỏi máy bạn.** Đây là ưu điểm cốt lõi so với mật khẩu: mật khẩu phải gửi đi (dù đã mã hoá), còn khoá riêng thì không.\n\n### Passphrase và ssh-agent\n\nĐặt passphrase cho khoá riêng nghĩa là khoá được mã hoá trên đĩa. Ai lấy được file cũng không dùng được nếu không biết passphrase.\n\nNhưng gõ passphrase mỗi lần thì phiền. `ssh-agent` giữ khoá đã giải mã trong bộ nhớ:\n\n```bash\neval \"$(ssh-agent -s)\"           # khởi động agent\nssh-add ~/.ssh/id_ed25519        # nhập passphrase MỘT LẦN\nssh server                        # từ giờ không hỏi nữa\n\nssh-add -l                        # xem khoá nào đang được giữ\nssh-add -D                        # xoá hết khỏi agent\n```\n\nTrên macOS và hầu hết môi trường desktop Linux, agent đã chạy sẵn và tích hợp với keychain.\n\n**Đánh đổi rõ ràng**: không passphrase thì tiện hơn nhưng mất laptop là mất luôn quyền truy cập mọi server. Có passphrase cộng ssh-agent cho gần như cùng mức tiện lợi mà an toàn hơn hẳn — nên đây là lựa chọn mặc định đúng.\n\nNgoại lệ: khoá dùng cho CI/CD tự động thì không đặt passphrase được, nên phải bảo vệ bằng cách khác (secret của nền tảng CI, quyền hạn chế).\n\n### Quyền file bắt buộc\n\n```bash\nchmod 700 ~/.ssh\nchmod 600 ~/.ssh/id_ed25519          # khoá riêng — CHỈ chủ đọc được\nchmod 644 ~/.ssh/id_ed25519.pub      # khoá công khai — ai đọc cũng được\nchmod 600 ~/.ssh/config\nchmod 600 ~/.ssh/authorized_keys     # trên server\n```\n\nSSH **im lặng từ chối** dùng khoá riêng có quyền quá lỏng. Nó không nói rõ lý do trừ khi bạn chạy `ssh -v`. Đây là nguyên nhân số một của tình huống \"đã cài khoá mà vẫn bị hỏi mật khẩu\".\n\n### File `~/.ssh/config`\n\nĐây là thứ đáng cấu hình sớm nhất:\n\n```\nHost prod\n    HostName 203.0.113.42\n    User deploy\n    Port 2222\n    IdentityFile ~/.ssh/id_prod\n\nHost github.com\n    User git\n    IdentityFile ~/.ssh/id_github\n\nHost *\n    ServerAliveInterval 60      # gửi gói giữ kết nối mỗi 60 giây\n    ServerAliveCountMax 3\n```\n\nGiờ thay vì:\n\n```bash\nssh -p 2222 -i ~/.ssh/id_prod deploy@203.0.113.42\n```\n\nchỉ cần:\n\n```bash\nssh prod\nscp file.txt prod:/tmp/\nrsync -av du-lieu/ prod:/data/\n```\n\n`ServerAliveInterval` đáng chú ý: nó chống việc kết nối bị **NAT hoặc firewall cắt** khi im lặng quá lâu — nguyên nhân phổ biến của việc SSH tự đứt sau vài phút không gõ gì.\n\n### `scp` — sao chép đơn giản\n\n```bash\nscp file.txt user@server:/duong/dan/       # đẩy lên\nscp user@server:/duong/dan/file.txt .      # tải về\nscp -r thu-muc/ user@server:/duong/dan/    # cả thư mục, cần -r\nscp -P 2222 file.txt user@server:/tmp/     # cổng khác — chú ý P HOA\n```\n\nChữ `P` hoa là cái bẫy nhỏ: `ssh` dùng `-p` thường, `scp` dùng `-P` hoa. Nhầm là báo lỗi khó hiểu.\n\n`scp` đơn giản nhưng có ba hạn chế nghiêm trọng: **không tiếp tục được khi đứt**, **luôn chép lại toàn bộ**, và **không hiện tiến độ** với file lớn. Với thư mục hoặc dữ liệu lớn thì `rsync` tốt hơn ở mọi mặt.\n\n### `rsync` — đồng bộ thông minh\n\n```bash\nrsync -avz nguon/ user@server:/dich/\n#      │││\n#      ││└─ z: nén khi truyền — tiết kiệm băng thông\n#      │└── v: hiện chi tiết\n#      └─── a: archive = -r + giữ quyền, thời gian, symlink\n\nrsync -avz --progress nguon/ server:/dich/    # hiện tiến độ từng file\nrsync -avz --delete nguon/ server:/dich/      # ⚠️ xoá file ở đích mà nguồn không có\nrsync -avzn nguon/ server:/dich/              # ⭐ n = chạy thử, KHÔNG làm gì thật\nrsync -avz --exclude='*.pyc' --exclude='.git' nguon/ server:/dich/\n```\n\n**Dấu gạch chéo cuối ở NGUỒN quyết định kết quả** — đây là chỗ sai nhiều nhất:\n\n```bash\nrsync -av nguon/ dich/     # đổ NỘI DUNG của nguon vào dich\nrsync -av nguon  dich/     # tạo dich/nguon/  ← lồng thêm một tầng\n```\n\nMẹo nhớ: dấu gạch chéo nghĩa là *\"nội dung bên trong\"*.\n\nCờ `--delete` **rất nguy hiểm**: nó xoá mọi thứ ở đích không có trong nguồn. Luôn chạy với `-n` trước để xem danh sách.\n\n**Vì sao `rsync` nhanh hơn nhiều khi chạy lại:** nó so sánh kích thước và thời gian sửa, chỉ truyền file đã đổi. Với file lớn, thuật toán rolling checksum còn cho phép chỉ truyền **các khối khác biệt bên trong file** thay vì cả file.\n\n### Chạy lệnh từ xa và tunnel\n\n```bash\nssh server 'df -h'                    # chạy một lệnh rồi thoát\nssh server 'cd /app && git pull && systemctl restart app'\nssh server 'tail -f /var/log/app.log' # theo dõi log realtime\n\n# Chuyển tiếp cổng — truy cập dịch vụ chỉ nghe trên localhost của server\nssh -L 5432:localhost:5432 server\n#      │    │         │\n#      │    │         └─ cổng trên server\n#      │    └─ địa chỉ nhìn từ phía server\n#      └─ cổng trên máy bạn\n# → giờ psql localhost:5432 trên máy bạn nối tới database của server\n```\n\nChuyển tiếp cổng là kỹ thuật rất hữu ích: database production thường chỉ nghe trên `localhost` vì lý do bảo mật, và đây là cách truy cập nó mà không phải mở cổng ra internet.\n\n### `tmux`: không mất việc khi đứt kết nối\n\nĐây là phần quan trọng nhất của bài với người chạy tác vụ dài.\n\nKhi SSH đứt, shell nhận **SIGHUP** và gửi tiếp cho mọi process con — chúng chết theo. Sáu tiếng training mất trắng.\n\n```bash\nssh server\ntmux new -s train           # tạo phiên tên \"train\"\npython train.py             # chạy trong phiên đó\n\n# Ctrl+B rồi D  → tách ra (detach), phiên VẪN CHẠY trên server\nexit                        # thoát SSH, tác vụ không bị ảnh hưởng\n\n# Hôm sau\nssh server\ntmux ls                     # train: 1 windows (created ...)\ntmux attach -t train        # quay lại đúng chỗ, xem output đầy đủ\n```\n\nPhiên tmux là process **con của server**, không phải con của phiên SSH — nên SIGHUP không chạm tới nó.\n\nCác phím cơ bản (`Ctrl+B` rồi phím):\n\n```\nd    tách ra\nc    tạo cửa sổ mới\nn/p  cửa sổ kế/trước\n%    chia dọc\n\"    chia ngang\n[    chế độ cuộn (q để thoát)\n```\n\n**Quy tắc đáng rèn thành phản xạ: chạy bất cứ thứ gì lâu hơn một phút trên server thì mở tmux trước.** Mất một giây và cứu bạn khỏi việc làm lại từ đầu.",

vi_du: "### Thiết lập SSH lần đầu\n\n```bash\n# ① Tạo khoá\nssh-keygen -t ed25519 -C \"minh@laptop\"\n\n# ② Đặt quyền cho đúng\nchmod 700 ~/.ssh\nchmod 600 ~/.ssh/id_ed25519\n\n# ③ Copy lên server\nssh-copy-id -i ~/.ssh/id_ed25519.pub deploy@203.0.113.42\n\n# ④ Cấu hình cho gọn\ncat >> ~/.ssh/config << 'EOF'\nHost prod\n    HostName 203.0.113.42\n    User deploy\n    IdentityFile ~/.ssh/id_ed25519\n    ServerAliveInterval 60\nEOF\nchmod 600 ~/.ssh/config\n\n# ⑤ Dùng\nssh prod\n```\n\n### Chẩn đoán khi khoá không hoạt động\n\n```bash\nssh -v prod\n# ...\n# debug1: Offering public key: /home/minh/.ssh/id_ed25519\n# debug1: Authentications that can continue: password\n#          ↑ server từ chối khoá\n```\n\nBa nguyên nhân theo thứ tự phổ biến:\n\n```bash\n# ① Quyền khoá riêng quá lỏng (phổ biến nhất)\nls -l ~/.ssh/id_ed25519\nchmod 600 ~/.ssh/id_ed25519\n\n# ② Quyền trên SERVER sai\nssh prod 'ls -ld ~/.ssh ~/.ssh/authorized_keys'\nssh prod 'chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys'\n\n# ③ Khoá công khai chưa được thêm đúng\nssh prod 'cat ~/.ssh/authorized_keys'\n```\n\n`ssh -v` (hoặc `-vvv` cho chi tiết hơn) là công cụ chẩn đoán chính. Nó cho biết SSH thử khoá nào và server từ chối ở bước nào.\n\n### `rsync` cho việc triển khai\n\n```bash\n# ① LUÔN chạy thử trước — xem sẽ đụng vào gì\nrsync -avzn --delete \\\n  --exclude='.git' --exclude='__pycache__' --exclude='.env' \\\n  ./ prod:/var/www/app/\n\n# ② Xem danh sách thấy đúng rồi mới bỏ n\nrsync -avz --delete \\\n  --exclude='.git' --exclude='__pycache__' --exclude='.env' \\\n  ./ prod:/var/www/app/\n\n# ③ Khởi động lại dịch vụ\nssh prod 'sudo systemctl restart app'\n```\n\nChú ý `--exclude='.env'`: file cấu hình trên server phải khác với trên máy dev, nên **không được đè lên**. Quên dòng này là đẩy cấu hình dev đè lên production.\n\n### Tải dataset lớn về\n\n```bash\n# ❌ scp — đứt mạng là làm lại từ đầu\nscp prod:/data/dataset-50GB.tar.gz .\n\n# ✓ rsync — tiếp tục được, có tiến độ\nrsync -avz --progress --partial prod:/data/dataset-50GB.tar.gz .\n#                                ↑ giữ lại phần đã tải để lần sau tiếp tục\n\n# Đứt giữa chừng? Chạy lại đúng lệnh đó — nó tiếp tục từ chỗ dở\n```\n\nCờ `--partial` là mấu chốt: không có nó, `rsync` xoá file dở khi bị ngắt.\n\n### Chạy training không sợ mất\n\n```bash\nssh prod\ntmux new -s train\n\n# Trong tmux\ncd /data/du-an\nsource venv/bin/activate\npython train.py 2>&1 | tee train-$(date +%Y%m%d).log\n\n# Ctrl+B rồi D để tách ra\nexit\n\n# Vài giờ sau, từ máy khác cũng được\nssh prod\ntmux attach -t train\n```\n\nKết hợp `tee` với `tmux` cho hai lớp bảo vệ: tmux giữ process sống, `tee` ghi lại toàn bộ output ra file phòng khi phiên tmux cũng mất.\n\n### Truy cập database production an toàn\n\n```bash\n# Database chỉ nghe trên localhost của server — không mở ra internet\nssh -L 5432:localhost:5432 prod -N\n#                                  ↑ N = chỉ tunnel, không mở shell\n\n# Ở terminal khác, trên máy bạn\npsql -h localhost -p 5432 -U app_user ten_db\n```\n\nĐây là cách đúng để nối vào database production từ máy cá nhân: **không mở cổng database ra internet**, mà đi qua kênh SSH đã mã hoá và đã xác thực.\n\n### Copy khoá công khai thủ công\n\nKhi không có `ssh-copy-id` (ví dụ trên Windows Git Bash):\n\n```bash\ncat ~/.ssh/id_ed25519.pub | ssh user@server \\\n  'mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys'\n```\n\nMột dòng này làm đúng những gì `ssh-copy-id` làm: tạo thư mục, đặt quyền, ghi nối khoá, đặt quyền file.\n\nLưu ý `>>` chứ không phải `>` — dùng `>` sẽ **xoá mất mọi khoá đã có**, kể cả khoá của đồng nghiệp.",

so_sanh: "### `scp` và `rsync`\n\n| | `scp` | `rsync` |\n|---|---|---|\n| Có sẵn | ✓ luôn có | Thường phải cài |\n| Tiếp tục khi đứt | ✗ | **✓ với `--partial`** |\n| Chỉ truyền phần khác | ✗ chép lại hết | **✓** |\n| Tiến độ | ✗ | **✓** |\n| Đồng bộ hai chiều | ✗ | ✓ `--delete` |\n| Loại trừ file | ✗ | **✓ `--exclude`** |\n| Cú pháp | Đơn giản hơn | Nhiều cờ, gạch chéo cuối quan trọng |\n\nQuy tắc: **một file nhỏ dùng `scp`; thư mục, dữ liệu lớn, hoặc đồng bộ lặp lại thì dùng `rsync`.**\n\nĐáng chú ý: `scp` đã bị coi là lỗi thời. OpenSSH khuyến nghị dùng `sftp` hoặc `rsync` thay thế, vì giao thức cũ của `scp` có một số vấn đề bảo mật đã biết. Bản `scp` mới thực ra chạy trên `sftp` bên dưới.\n\n### Khoá và mật khẩu\n\n| | Mật khẩu | Khoá SSH |\n|---|---|---|\n| Bị đoán / brute-force | **✓ dễ bị** | ✗ không khả thi |\n| Gửi qua mạng | Có (đã mã hoá) | **Không bao giờ** |\n| Tự động hoá | Khó, phải nhúng mật khẩu | **✓ tự nhiên** |\n| Thu hồi từng người | Phải đổi mật khẩu chung | **✓ xoá một dòng** |\n| Mất laptop | Không ảnh hưởng | **Nguy hiểm nếu không có passphrase** |\n\nƯu điểm ít được nhắc nhất là dòng áp chót: với khoá, mỗi người có khoá riêng, nên **thu hồi quyền của một người chỉ cần xoá một dòng** trong `authorized_keys`. Với mật khẩu dùng chung thì phải đổi và báo lại cho tất cả.\n\nCấu hình server nghiêm túc thường **tắt hẳn đăng nhập bằng mật khẩu**:\n\n```\n# /etc/ssh/sshd_config\nPasswordAuthentication no\nPermitRootLogin no\n```\n\nHai dòng này loại bỏ gần như toàn bộ các cuộc tấn công dò mật khẩu tự động — thứ mà bất kỳ server nào có IP công khai cũng hứng chịu hàng nghìn lần mỗi ngày.\n\n### Giữ tác vụ sống khi mất kết nối\n\n| | `nohup` | `tmux` / `screen` | `systemd` |\n|---|---|---|---|\n| Sống qua đứt kết nối | ✓ | ✓ | ✓ |\n| **Quay lại xem được** | ✗ | **✓** | qua log |\n| Tương tác tiếp | ✗ | **✓** | ✗ |\n| Tự khởi động lại khi chết | ✗ | ✗ | **✓** |\n| Chạy khi máy khởi động | ✗ | ✗ | **✓** |\n\n**`tmux` cho việc bạn cần theo dõi** (training, migration, gỡ lỗi). **`systemd` cho dịch vụ chạy lâu dài.** `nohup` chỉ hợp với việc chạy một lần rồi quên.\n\nMột lợi ích của tmux ít người nghĩ tới: bạn attach lại được **từ máy khác**. Chạy training từ laptop ở công ty, tối về nhà mở máy khác `tmux attach` là xem tiếp được.",

loi_thuong_gap: [
  "**Quên `chmod 600` cho khoá riêng.** SSH im lặng từ chối và bạn không hiểu vì sao vẫn bị hỏi mật khẩu. Dùng `ssh -v` để thấy lý do.",
  "**Bỏ qua cảnh báo host key changed.** Có thể là tấn công người-đứng-giữa. Xác minh server có thật sự vừa được dựng lại trước khi `ssh-keygen -R`.",
  "**Dùng `StrictHostKeyChecking=no` cho tiện.** Nó tắt hẳn lớp bảo vệ chống giả mạo server.",
  "**Nhầm dấu gạch chéo cuối trong `rsync`.** `nguon/` đổ nội dung vào đích; `nguon` tạo thêm một tầng thư mục. Luôn chạy `-n` trước.",
  "**Dùng `--delete` mà chưa chạy thử.** Nó xoá mọi thứ ở đích không có trong nguồn. Bắt buộc `-avzn` trước.",
  "**Quên `--exclude='.env'` khi deploy.** Cấu hình dev đè lên production.",
  "**Chạy tác vụ dài mà không dùng tmux.** Đứt mạng là mất hết. Mở tmux trước, luôn luôn.",
  "**Dùng `>` thay `>>` khi thêm khoá vào `authorized_keys`.** Xoá mất mọi khoá đã có, kể cả của đồng nghiệp.",
  "**Nhầm `-p` với `-P` khi đổi cổng.** `ssh -p 2222` nhưng `scp -P 2222`.",
  "**Không đặt passphrase cho khoá cá nhân.** Mất laptop là mất quyền truy cập mọi server. Dùng passphrase kèm `ssh-agent`."
],

tu_kiem_tra: [
  "SSH xác thực hai chiều. Hai chiều đó là gì, và bỏ qua chiều nào thì bị tấn công kiểu gì?",
  "Khi đăng nhập bằng khoá, khoá riêng có được gửi lên server không? Mô tả cơ chế thử thách–chữ ký.",
  "Bạn thấy cảnh báo *host key changed*. Hai khả năng là gì và làm sao phân biệt?",
  "`rsync -av nguon/ dich/` và `rsync -av nguon dich/` khác nhau thế nào?",
  "Nêu ba lý do `rsync` tốt hơn `scp` cho thư mục lớn.",
  "Vì sao process chết khi SSH đứt? Liên hệ với tín hiệu nào, và tmux tránh được bằng cách nào?",
  "Viết lệnh SSH cho phép nối tới database chỉ nghe trên localhost của server, mà không mở cổng ra internet.",
  "Vì sao dùng khoá thu hồi quyền của một thành viên dễ hơn dùng mật khẩu chung?"
],

tai_nguyen: [
  "Tài liệu Linux: `man ssh`, `man ssh_config`, `man rsync`. Mục `ssh_config` liệt kê mọi tuỳ chọn dùng được trong `~/.ssh/config`.",
  "Sách **SSH Mastery** — Michael W. Lucas. Ngắn, thực dụng, bao quát cả phần cấu hình server.",
  "Tài liệu GitHub, mục *Connecting to GitHub with SSH* — hướng dẫn từng bước, kèm dấu vân tay chính thức để đối chiếu.",
  "Tài liệu **tmux**, hoặc tìm *tmux cheat sheet* — chỉ cần thuộc năm phím là đủ dùng.",
  "Sách **The Linux Command Line** — William Shotts, chương 17 về mạng."
],

lien_he: [
  "**Quyền file: chmod, chown, sudo** là bài ngay trước: `chmod 600` cho khoá riêng là điều kiện bắt buộc.",
  "**Tín hiệu và exit code** ở module 2: SIGHUP là lý do tác vụ chết khi đứt SSH, và tmux là cách tránh.",
  "**Thao tác file**: `rsync` và `cp` cùng có bẫy dấu gạch chéo cuối, nhưng quy tắc hơi khác nhau.",
  "**Mạng máy tính** (module kế tiếp): SSH chạy trên TCP cổng 22; chuyển tiếp cổng là ứng dụng trực tiếp của kiến thức về port.",
  "**Git & GitHub** ở mảng 03: `git clone git@github.com:...` chính là SSH. Cấu hình khoá ở đây dùng luôn cho git.",
  "**Triển khai** ở mảng 05: script deploy dùng SSH chạy lệnh từ xa và `rsync` đẩy file — đúng những gì bài này dạy.",
  "**Auth & bảo mật** ở mảng 05: tắt đăng nhập mật khẩu và tắt đăng nhập root là hai bước đầu tiên khi làm cứng một server."
]

};
