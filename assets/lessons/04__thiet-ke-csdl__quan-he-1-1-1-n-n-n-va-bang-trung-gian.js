window.LESSONS["04:thiet-ke-csdl:quan-he-1-1-1-n-n-n-va-bang-trung-gian"] = {

tom_tat: [
  "Quan hệ 1–1, 1–n và n–n không phải ba kiểu đường trang trí; mỗi kiểu quyết định vị trí khóa ngoại, constraint và cách PostgreSQL join dữ liệu.",
  "Quan hệ n–n luôn cần một bảng trung gian trong mô hình quan hệ, và bảng đó thường là một thực thể nghiệp vụ có thuộc tính riêng chứ không chỉ hai ID.",
  "Ví dụ bán vé dùng `show_seat` để mô tả ghế được mở bán theo suất và `booking_item` để ghi giao dịch chọn ghế với giá tại thời điểm mua.",
  "Khóa ngoại không tự có index ở phía tham chiếu trong PostgreSQL; index phải xuất phát từ truy vấn, thao tác xóa/cập nhật và độ chọn lọc.",
  "Cardinality đúng vẫn chưa đủ: phải cưỡng chế tính duy nhất, optionality và chính sách xóa bằng DDL có thể kiểm thử."
],

can_biet_truoc: [
  "ERD, cardinality và optionality của bài trước.",
  "Khóa chính, khóa ứng viên, khóa ngoại và `UNIQUE`.",
  "Cú pháp PostgreSQL cho `CREATE TABLE`, khóa ghép, `JOIN` và transaction.",
  "Khái niệm B-tree index và nguyên tắc cột đầu của index ghép ở mức cơ bản."
],

dinh_nghia: "**Quan hệ 1–1** nghĩa là mỗi bản thể ở hai phía liên kết với tối đa một bản thể phía kia. **Quan hệ 1–n** cho phép một bản thể phía cha liên kết với nhiều bản thể phía con, còn mỗi con liên kết tối đa một cha. **Quan hệ n–n** cho phép nhiều bản thể ở cả hai phía; trong cơ sở dữ liệu quan hệ, nó được hiện thực bằng **bảng trung gian** chứa khóa ngoại tới hai phía.\n\nCác con số là giới hạn tối đa. Mức tối thiểu vẫn phải ghi riêng: `0..1`, `1..1`, `0..n` hay `1..n`. DDL chỉ đúng khi khóa ngoại, `NOT NULL`, `UNIQUE` và đôi khi khóa ghép cùng nhau cưỡng chế được phát biểu đó.",

vi_sao_can: "Một lựa chọn sai không chỉ làm sơ đồ thiếu thanh lịch. Nó cho phép trạng thái vô lý hoặc chặn trạng thái hợp lệ. Nếu đặt `seat_id` trực tiếp trong `booking`, một booking chỉ mua được một ghế. Nếu đặt mảng ghế trong booking, PostgreSQL không thể dùng foreign key kiểm tra từng phần tử và cũng khó ngăn hai booking mua cùng ghế của một show.\n\nTrong hệ thống vé, cùng ghế A12 được dùng ở hàng trăm show và mỗi show mở bán hàng nghìn ghế. Đây là n–n giữa `show` và `seat`, nhưng quan hệ còn mang `price`, `sales_status` và có thể `ticket_type`. Bảng trung gian không phải thủ tục hành chính: nó là nơi dữ liệu nghiệp vụ thực sự sống.\n\nHiệu năng cũng phụ thuộc hình dạng quan hệ. Truy vấn từ một show sang ghế cần index bắt đầu bằng `show_id`; truy vấn lịch sử một ghế có thể cần hướng ngược lại. PostgreSQL không tự tạo index cho cột foreign key ở bảng con. Nếu đội ngũ chỉ vẽ đường nối mà không xem đường truy cập, thao tác join hoặc xóa bản ghi cha có thể quét cả bảng trung gian.",

co_che: "### Quan hệ 1–n: đặt khóa ngoại ở phía nhiều\n\n`event 1 — n show` ánh xạ tự nhiên bằng `show.event_id`. Mỗi hàng show chứa một giá trị cha; nhiều hàng có thể lặp cùng `event_id`. Đặt danh sách `show_ids` trong event đảo ngược thiết kế quan hệ, khiến mỗi lần thêm show phải cập nhật hàng cha và không thể dùng foreign key cho từng phần tử.\n\n```sql\nCREATE TABLE event (\n  event_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  title text NOT NULL\n);\n\nCREATE TABLE show (\n  show_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  event_id bigint NOT NULL REFERENCES event(event_id),\n  starts_at timestamptz NOT NULL\n);\n\nCREATE INDEX show_event_starts_idx ON show (event_id, starts_at);\n```\n\n`NOT NULL` biến phía show thành bắt buộc: show không thể vô chủ. Phía event vẫn có thể có không show nào, vì không constraint nào buộc phải tồn tại hàng con. Muốn “event đã xuất bản phải có ít nhất một show” cần quy tắc theo trạng thái, thường được kiểm ở transaction hoặc constraint trigger; foreign key đơn thuần không diễn đạt được.\n\nIndex ghép phục vụ truy vấn các suất của một event theo thời gian. Nó cũng giúp PostgreSQL kiểm tra hàng con khi xóa event. Tuy nhiên thêm index làm mỗi `INSERT show` phải cập nhật thêm cây B-tree. Nếu hệ thống không bao giờ tìm theo event và không xóa event, index có thể không đáng; quyết định phải dựa vào workload, không phải cứ foreign key là thêm máy móc.\n\n### Quan hệ 1–1: khóa ngoại cộng tính duy nhất\n\nGiả sử mỗi organizer có tối đa một hồ sơ xuất hóa đơn và hồ sơ không tồn tại nếu thiếu organizer. Đặt khóa chính của bảng phụ đồng thời làm khóa ngoại tạo mô hình 1–1 chặt:\n\n```sql\nCREATE TABLE organizer (\n  organizer_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  name text NOT NULL\n);\n\nCREATE TABLE organizer_billing_profile (\n  organizer_id bigint PRIMARY KEY\n    REFERENCES organizer(organizer_id) ON DELETE CASCADE,\n  legal_name text NOT NULL,\n  tax_code text NOT NULL UNIQUE\n);\n```\n\nMột `organizer_id` chỉ xuất hiện một lần vì nó là primary key. Hồ sơ bắt buộc có organizer vì nó là foreign key. Nhưng organizer có thể chưa có hồ sơ, tức quan hệ thực tế là organizer `0..1` profile. Để bắt buộc đúng một ở cả hai phía ngay tại mọi thời điểm rất khó vì hai hàng phải được tạo đồng thời; deferrable constraint hoặc transaction cấp ứng dụng có thể cần thiết.\n\nKhông phải cứ 1–1 là phải tách bảng. Nếu hai phần có cùng vòng đời, cùng quyền truy cập và hầu như luôn được đọc cùng nhau, gộp cột vào organizer đơn giản hơn. Tách hợp lý khi phần phụ rất rộng, hiếm đọc, nhạy cảm cần quyền riêng, hoặc thuộc module có vòng đời triển khai khác. “Chuẩn hoá yêu cầu mọi nhóm cột 1–1 phải thành bảng riêng” là hiểu sai.\n\n### Quan hệ n–n: hai khóa ngoại tạo ra một tập liên kết\n\nShow và seat là n–n: một show mở nhiều seat, một seat vật lý được tái sử dụng qua nhiều show. Bảng `show_seat` biến mỗi cặp thành một hàng:\n\n```sql\nCREATE TABLE seat (\n  seat_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  venue_id bigint NOT NULL,\n  seat_label text NOT NULL,\n  UNIQUE (venue_id, seat_label)\n);\n\nCREATE TABLE show_seat (\n  show_id bigint NOT NULL REFERENCES show ON DELETE CASCADE,\n  seat_id bigint NOT NULL REFERENCES seat,\n  price numeric(12,2) NOT NULL CHECK (price >= 0),\n  sales_status text NOT NULL,\n  PRIMARY KEY (show_id, seat_id)\n);\n```\n\nKhóa chính ghép ngăn một seat xuất hiện hai lần trong cùng show. Thứ tự `(show_id, seat_id)` đồng thời tạo B-tree hữu ích khi liệt kê toàn bộ ghế của show. Nó không tối ưu truy vấn chỉ có `seat_id`, vì `seat_id` không phải cột đầu; nếu cần xem lịch sử sử dụng một ghế, thêm index `(seat_id, show_id)` sau khi xác nhận truy vấn đó đủ quan trọng.\n\n### Bảng trung gian thường có danh tính nghiệp vụ riêng\n\n`show_seat` không chỉ nối hai bảng. Giá phụ thuộc vào đúng cặp show–seat; ghế A12 có thể giá 500.000 đồng tối thứ Sáu và 650.000 đồng tối thứ Bảy. `sales_status` cũng thuộc việc ghế được mở ở show cụ thể, không thuộc seat vật lý.\n\nKhi quan hệ có thuộc tính, đội ngũ nên đặt tên theo nghiệp vụ thay vì `show_seat_map`. Một tên tốt báo hiệu vòng đời và trách nhiệm. Có trường hợp bảng nối cần khóa thay thế riêng, chẳng hạn các hệ thống khác tham chiếu một “inventory item” hoặc hàng quan hệ có lịch sử phiên bản. Khi không có nhu cầu tham chiếu độc lập, khóa ghép diễn đạt đúng tính duy nhất và tránh một `id` vô nghĩa che mất constraint cặp.\n\n### Booking và ghế: phân biệt danh mục với giao dịch\n\n`show_seat` nói ghế nào có thể bán và giá hiện hành. `booking_item` nói một booking đã chọn gì với giá chốt lúc giao dịch. Hai bảng trông gần nhau nhưng không thể nhập làm một: một bên là inventory, một bên là lịch sử mua.\n\n```sql\nCREATE TABLE booking (\n  booking_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  customer_email text NOT NULL,\n  status text NOT NULL,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nCREATE TABLE booking_item (\n  booking_id bigint NOT NULL REFERENCES booking ON DELETE CASCADE,\n  show_id bigint NOT NULL,\n  seat_id bigint NOT NULL,\n  unit_price numeric(12,2) NOT NULL CHECK (unit_price >= 0),\n  PRIMARY KEY (booking_id, show_id, seat_id),\n  UNIQUE (show_id, seat_id),\n  FOREIGN KEY (show_id, seat_id)\n    REFERENCES show_seat(show_id, seat_id)\n);\n```\n\nForeign key ghép bảo đảm chỉ bán cặp show–seat đã tồn tại. `UNIQUE (show_id, seat_id)` bảo đảm tại tối đa một booking, bất kể booking nào. Đây là ví dụ cardinality được tinh chỉnh bởi quy tắc nghiệp vụ: về cấu trúc một booking có nhiều item, nhưng một inventory seat chỉ có tối đa một item đang được biểu diễn trong schema đơn giản.\n\nSchema trên chưa xử lý giữ ghế hết hạn và booking bị hủy. Nếu giữ lại item của booking hủy, unique constraint sẽ chặn bán lại. Có thể xóa item khi hủy, chuyển lịch sử sang bảng audit, hoặc dùng partial unique index chỉ cho trạng thái giữ/đã bán. Vì partial index không thể tham chiếu trạng thái ở bảng booking khác, một thiết kế thực tế thường đưa trạng thái giữ vào bảng inventory hoặc tạo bảng `seat_hold` riêng. Đường nối ERD không giải quyết được chính sách thời gian này.\n\n### Optionality được hiện thực bằng NULL hoặc bằng sự vắng mặt của hàng\n\nMột payment có thể chưa có `refunded_at`; đây là thuộc tính tùy chọn phù hợp với NULL. Nhưng hồ sơ thanh toán chưa tồn tại không nhất thiết cần một hàng payment đầy NULL. Sự vắng mặt của hàng biểu đạt `0`, hàng hiện hữu biểu đạt một lần thử.\n\nChọn giữa NULL và bảng con dựa trên ngữ nghĩa, lực lượng và vòng đời. Nếu một booking có nhiều lần thử thanh toán, cột `payment_status` duy nhất trong booking làm mất lịch sử; bảng payment 1–n đúng hơn. Nếu chỉ có một ghi chú ngắn tùy chọn không bao giờ được tham chiếu độc lập, tạo bảng riêng làm join phức tạp mà ít giá trị.\n\n### Hành động xóa là quyết định nghiệp vụ, không phải tiện ích cú pháp\n\n`ON DELETE CASCADE` phù hợp với `booking_item` phụ thuộc hoàn toàn vào booking trong mô hình không giữ audit. Nó nguy hiểm với `payment`: xóa booking rồi làm mất chứng từ có thể vi phạm yêu cầu kế toán. `RESTRICT` hoặc mặc định `NO ACTION` buộc người vận hành xử lý có chủ đích. `SET NULL` giữ hàng con nhưng làm mất liên kết; chỉ dùng nếu “không còn cha” vẫn có nghĩa nghiệp vụ.\n\n| Quan hệ | Hành động thường hợp lý | Lý do cần kiểm tra |\n|---|---|---|\n| event → show | RESTRICT | show đã bán vé không nên biến mất theo một thao tác xóa event |\n| show → show_seat | CASCADE trước mở bán | inventory phụ thuộc show, nhưng sau bán có thể cần khóa xóa |\n| booking → booking_item | CASCADE hoặc audit riêng | dòng giao dịch phụ thuộc booking nhưng lịch sử có thể phải giữ |\n| booking → payment | RESTRICT | thanh toán có nghĩa vụ đối soát độc lập |\n\nKhông có lựa chọn mặc định đúng cho mọi bảng. Hãy mô phỏng xóa dữ liệu đã có giao dịch và hỏi hàng nào pháp lý hoặc vận hành vẫn cần.\n\n### Join và query plan đi theo hướng truy cập\n\nĐể liệt kê ghế còn bán của show, PostgreSQL có thể dùng primary key của `show_seat` để lấy một dải theo `show_id`, rồi anti-join với `booking_item` qua unique index `(show_id, seat_id)`:\n\n```sql\nSELECT ss.seat_id, s.seat_label, ss.price\nFROM show_seat AS ss\nJOIN seat AS s USING (seat_id)\nWHERE ss.show_id = $1\n  AND NOT EXISTS (\n    SELECT 1\n    FROM booking_item AS bi\n    WHERE bi.show_id = ss.show_id\n      AND bi.seat_id = ss.seat_id\n  )\nORDER BY s.seat_label;\n```\n\nVới ít hàng, sequential scan có thể rẻ hơn. Với hàng nghìn ghế mỗi show và nhiều show, các index ghép cho phép khoanh đúng dải. `EXPLAIN (ANALYZE, BUFFERS)` mới cho biết plan thực tế; không nên khẳng định thuật toán join chỉ từ câu SQL. Planner có thể chọn nested loop, hash anti join hoặc merge tùy thống kê và kích thước.\n\n### Đồng thời: constraint là tuyến phòng thủ cuối\n\nHai người có thể cùng thấy A12 còn trống trước khi một người commit. Kiểm tra bằng `SELECT` rồi `INSERT` không nguyên tử. Unique constraint `(show_id, seat_id)` khiến chỉ một transaction commit được; transaction kia nhận lỗi unique violation và ứng dụng phải báo ghế vừa được lấy.\n\nNếu quy trình cần giữ ghế trong mười phút, mô hình phải thêm thời hạn và cơ chế thu hồi. Có thể khóa hàng inventory bằng `SELECT ... FOR UPDATE`, hoặc thử insert vào bảng hold có unique key. Mỗi cách đánh đổi throughput, độ phức tạp và xử lý timeout. Điều cốt lõi là không nhầm cardinality trên giấy với bảo đảm khi nhiều request chạy đồng thời.\n\n### Khi nào không nên dùng bảng trung gian tổng quát\n\nĐừng tạo một bảng kiểu `entity_relation(left_type, left_id, right_type, right_id)` để chứa mọi quan hệ. Nó có vẻ linh hoạt nhưng PostgreSQL không thể đặt foreign key tới nhiều bảng theo `type`, tên cột mất nghĩa và query plan khó có thống kê chính xác theo từng loại. Bảng trung gian chuyên biệt dài hơn vài dòng DDL nhưng bảo toàn kiểu, constraint và ngôn ngữ miền.\n\nNgược lại, không cần bảng trung gian cho 1–n đơn giản chỉ để “đồng nhất kiến trúc”. `event_show(event_id, show_id)` trong khi show đã thuộc đúng một event tạo thêm join và có thể cho phép show nối nhiều event nếu constraint không đủ. Chọn hình dạng nhỏ nhất diễn đạt đúng quy tắc hiện tại và có đường tiến hóa rõ.",

vi_du: "### Một transaction mua ghế có thể chạy trên PostgreSQL\n\nSau khi đã có `show_seat`, `booking` và `booking_item`, luồng tối thiểu là tạo booking rồi chèn ghế trong cùng transaction. Đoạn dưới dùng CTE và trả về ID:\n\n```sql\nBEGIN;\n\nWITH new_booking AS (\n  INSERT INTO booking (customer_email, status)\n  VALUES ('lan@example.test', 'confirmed')\n  RETURNING booking_id\n)\nINSERT INTO booking_item (booking_id, show_id, seat_id, unit_price)\nSELECT nb.booking_id, ss.show_id, ss.seat_id, ss.price\nFROM new_booking AS nb\nCROSS JOIN show_seat AS ss\nWHERE ss.show_id = 1\n  AND ss.seat_id IN (12, 13);\n\nCOMMIT;\n```\n\nNếu cặp `(1,12)` đã ở booking khác, toàn bộ câu `INSERT` thất bại vì unique constraint; transaction có thể rollback nên không để booking nửa vời. Nếu seat 13 không thuộc show 1, câu `SELECT` chỉ tạo một dòng thay vì hai. Ứng dụng phải kiểm tra số hàng trả về, hoặc dùng `VALUES` rồi foreign key để trường hợp thiếu trở thành lỗi rõ ràng. Im lặng mua thiếu ghế là ca biên nguy hiểm.\n\nMột phiên bản kiểm tra đủ hai ghế:\n\n```sql\nWITH requested(seat_id) AS (VALUES (12::bigint), (13::bigint)),\navailable AS (\n  SELECT ss.*\n  FROM requested AS r\n  JOIN show_seat AS ss\n    ON ss.show_id = 1 AND ss.seat_id = r.seat_id\n)\nSELECT count(*) = (SELECT count(*) FROM requested) AS all_exist\nFROM available;\n```\n\nKết quả `false` phải dừng transaction. Với đầu vào rỗng, hai vế đều bằng 0 và biểu thức trả `true`; ứng dụng vẫn cần quy tắc booking phải có ít nhất một item. Đây là ví dụ một phép kiểm tra kỹ thuật đúng nhưng chưa đủ nghĩa nghiệp vụ.\n\n### Kiểm tra hướng index bằng EXPLAIN\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT * FROM show_seat WHERE show_id = 42;\n\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT * FROM show_seat WHERE seat_id = 42;\n```\n\nPrimary key `(show_id, seat_id)` phù hợp câu đầu. Câu hai có thể quét bảng nếu chưa có index đảo. Không được ghi thời gian cố định từ ví dụ này vì kết quả phụ thuộc số hàng, cache, phần cứng và thống kê; hãy nạp dữ liệu đại diện rồi đọc `actual time`, `rows` và `Buffers` trên chính môi trường.",

so_sanh: "### Ba hình dạng quan hệ và dấu hiệu nhận biết\n\n| Hình dạng | Cách ánh xạ thường dùng | Constraint quyết định | Ví dụ bán vé |\n|---|---|---|---|\n| 1–1 | khóa ngoại `UNIQUE`, hoặc primary key đồng thời là foreign key | `UNIQUE` chặn nhiều hàng con | organizer–billing profile |\n| 1–n | khóa ngoại ở phía n | `NOT NULL` quyết định con có bắt buộc cha | event–show, booking–payment |\n| n–n | bảng trung gian có hai khóa ngoại | khóa ghép hoặc `UNIQUE` chặn cặp trùng | show–seat qua show_seat |\n| quan hệ theo thời gian | bảng sự kiện/phiên bản riêng | khoảng thời gian, trạng thái, unique có điều kiện | seat hold có hạn |\n\n### Khóa ghép hay ID thay thế cho bảng trung gian\n\n| Tiêu chí | Khóa ghép `(show_id, seat_id)` | `show_seat_id` riêng |\n|---|---|---|\n| Diễn đạt cặp duy nhất | trực tiếp trong primary key | vẫn phải thêm `UNIQUE (show_id, seat_id)` |\n| Foreign key từ booking_item | hai cột, dài hơn | một cột ngắn hơn |\n| Tham chiếu độc lập từ hệ khác | kém tiện | thuận tiện hơn |\n| Index theo show | có sẵn nếu show đứng đầu | phải tạo riêng tùy primary key |\n| Thay đổi một thành phần khóa | ảnh hưởng khóa ngoại | ID ổn định hơn |\n\nKhông có người thắng tuyệt đối. Với inventory ghế được nhiều bảng tham chiếu, ID thay thế có thể giảm độ rộng khóa ngoại; nhưng bỏ unique cặp sẽ tạo hai inventory cho cùng ghế. Với bảng nối nhỏ, không ai tham chiếu riêng, khóa ghép thường nói đúng ý hơn.",

loi_thuong_gap: [
  "**Quên `UNIQUE` trong quan hệ 1–1.** Dấu hiệu là nhiều profile cùng trỏ một organizer vẫn chèn được; foreign key một mình chỉ kiểm tra cha tồn tại.",
  "**Đặt khóa ngoại ở phía một của quan hệ 1–n.** Cột biến thành danh sách hoặc chỉ chứa được một con; chuyển khóa sang mỗi hàng phía n.",
  "**Coi bảng trung gian chỉ gồm hai ID.** Khi giá hoặc trạng thái bị đặt nhầm vào show hay seat, hãy hỏi thuộc tính phụ thuộc vào cặp nào.",
  "**Thêm ID thay thế rồi quên constraint cặp.** Nhận biết bằng hai hàng khác ID nhưng cùng `show_id, seat_id`; giữ `UNIQUE` nghiệp vụ.",
  "**Tin rằng PostgreSQL tự index foreign key.** `EXPLAIN` hoặc thao tác xóa cha chậm sẽ lộ quét bảng con; tạo index theo đường truy cập đã xác nhận.",
  "**Dùng `ON DELETE CASCADE` để khỏi xử lý lỗi.** Nếu xóa event làm mất show đã bán hoặc payment, chính sách xóa đã phá lịch sử.",
  "**Kiểm tra ghế trống rồi insert ngoài transaction.** Lỗi chỉ xuất hiện dưới tải đồng thời; dùng unique constraint và xử lý conflict.",
  "**Không xét đầu vào rỗng hoặc mua thiếu một phần.** Câu `INSERT ... SELECT` có thể thành công với ít hàng hơn yêu cầu; so số lượng trước khi commit."
],

tu_kiem_tra: [
  "Tại sao `NOT NULL show.event_id` chỉ buộc show có event nhưng không buộc event phải có show?",
  "Hãy viết DDL biến organizer–billing profile thành quan hệ tối đa một ở phía profile.",
  "Vì sao primary key `(show_id, seat_id)` không giúp nhiều cho truy vấn chỉ lọc `seat_id`?",
  "`unit_price` nên nằm ở seat, show_seat hay booking_item? Nêu ý nghĩa khác nhau của giá ở từng vị trí.",
  "Khi booking bị hủy, `UNIQUE (show_id, seat_id)` ảnh hưởng việc bán lại thế nào? Đề xuất một mô hình có lịch sử.",
  "Câu khó: hai transaction cùng mua A12 có thể đều đọc thấy ghế trống. Cơ chế nào quyết định người thắng tại thời điểm commit?",
  "Trong trường hợp nào khóa thay thế cho show_seat đáng dùng, và constraint nào vẫn tuyệt đối không được bỏ?",
  "Vì sao một bảng quan hệ đa hình dùng `left_type/left_id` làm suy yếu foreign key và thống kê planner?"
],

tai_nguyen: [
  "Sách *SQL and Relational Theory*.",
  "Sách *Fundamentals of Database Systems*.",
  "Tài liệu PostgreSQL, phần Constraints, Indexes và Using EXPLAIN.",
  "Sách *Designing Data-Intensive Applications*."
],

lien_he: [
  "Mảng 01 về concurrency nối trực tiếp với race condition bán cùng ghế; cardinality phải được bảo vệ bằng transaction và unique constraint.",
  "Mảng 02 về cấu trúc dữ liệu giúp hiểu B-tree khóa ghép và vì sao thứ tự cột quyết định hướng tìm kiếm hiệu quả.",
  "Mảng 03 về Git cung cấp quy trình review migration khi thay đổi khóa ngoại hoặc chính sách `ON DELETE`.",
  "Mảng 06 về backend sẽ dùng chính quan hệ booking–item–payment để thiết kế aggregate, API và xử lý lỗi conflict."
]

};
