window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:context-manager-va-cau-lenh-with"] = {

tom_tat: [
  "**`with` là `try/finally` được đóng gói.** Nó giải đúng một bài toán: đảm bảo phần dọn dẹp luôn chạy, kể cả khi có ngoại lệ hay `return` giữa chừng.",
  "**Giao thức chỉ gồm hai phương thức**: `__enter__` chạy lúc vào, `__exit__` chạy lúc ra. `as x` nhận giá trị mà `__enter__` **trả về**, không phải nhận object context manager.",
  "**`__exit__` trả `True` là NUỐT ngoại lệ.** Đây là bẫy im lặng: lỡ tay `return True` là mọi lỗi trong khối `with` biến mất.",
  "**`@contextmanager` biến một generator có đúng một `yield` thành context manager** — đây là chỗ ba bài decorator, generator, context manager gặp nhau.",
  "**Không chỉ dùng cho file.** Khoá, giao dịch database, đo thời gian, đổi thư mục tạm, ẩn cảnh báo — bất cứ cặp việc \"mở rồi phải đóng\" nào."
],

can_biet_truoc: [
  "Bài **Xử lý ngoại lệ** — `with` chính là `try/finally`, nên phải hiểu `finally` trước.",
  "Bài **Generator, iterator và `yield`** ngay trước — `@contextmanager` dựa hoàn toàn vào generator.",
  "Bài **Decorator** — `@contextmanager` là một decorator, và hiểu nó cần cả hai bài.",
  "Bài **Đọc ghi file** — `with open(...)` là context manager bạn đã dùng nhiều nhất."
],

dinh_nghia: "Nói đời thường: `with` giống như **thuê một căn phòng có hợp đồng dọn dẹp**. Bạn vào, làm việc của mình, và dù bạn ra về bình thường hay chạy vội vì có việc gấp, phòng vẫn được dọn. Bạn không phải nhớ dọn.\n\nNói chính xác:\n\n- **Context manager** — object cài đặt hai phương thức: `__enter__()` và `__exit__()`.\n- **`__enter__()`** — chạy khi vào khối `with`. **Giá trị nó trả về** là cái mà `as x` nhận.\n- **`__exit__(exc_type, exc_value, traceback)`** — chạy khi rời khối, **dù đi ra bằng cách nào**. Ba tham số mô tả ngoại lệ nếu có, hoặc `None` cả ba nếu không.\n- **Trả `True` từ `__exit__`** — báo cho Python rằng ngoại lệ **đã được xử lý**, tức là nuốt nó.\n- **`@contextlib.contextmanager`** — decorator biến hàm generator có đúng một `yield` thành context manager.\n- **`contextlib.ExitStack`** — quản lý số lượng context manager không biết trước.\n\nĐiểm cần nắm ngay và hay bị hiểu sai: **`as f` không nhận object context manager, nó nhận thứ `__enter__` trả về.** Với `open()` thì hai thứ đó trùng nhau nên không ai để ý; với context manager tự viết thì chúng thường khác nhau, và đây là chỗ gây bối rối đầu tiên.",

vi_sao_can: "Quản lý tài nguyên là nơi mà \"quên một dòng\" biến thành sự cố sản xuất:\n\n- **Rò rỉ tài nguyên là lỗi tích luỹ.** Quên đóng file, quên trả kết nối database về pool, quên mở khoá — chương trình chạy tốt hàng giờ rồi đột nhiên chết vì hết file descriptor hoặc hết kết nối. Nguyên nhân xa nơi triệu chứng.\n- **`try/finally` viết tay thì dễ quên và dài dòng.** Với ba tài nguyên là ba tầng lồng nhau, và chỉ cần sót một `finally` là hỏng.\n- **Giao dịch database bắt buộc phải có.** Commit khi thành công, rollback khi lỗi — không có cơ chế đảm bảo thì dữ liệu sẽ ở trạng thái nửa vời.\n- **Bạn sẽ đọc nó khắp nơi.** `with open()`, `with kết_nối`, `with khoá`, `with torch.no_grad()` — mọi thư viện đều dùng.\n- **Nó gói lại ba bài trước.** `@contextmanager` là decorator áp lên generator để tạo context manager. Viết được nó là đã nắm cả ba.\n\nTình huống thật rất kinh điển: bạn viết vòng lặp mở hàng nghìn file để đọc, không dùng `with`. Trên máy bạn chạy được vì bộ thu gom rác đóng file kịp. Trên máy chủ chạy bản Python khác hoặc tải cao hơn thì `OSError: Too many open files`. Code không đổi, chỉ môi trường đổi. Đây chính là lý do **không được dựa vào bộ thu gom rác để dọn tài nguyên**, và phần cơ chế sẽ nói rõ.",

co_che: "### `with` là `try/finally`\n\n```python\nwith open(\"a.txt\", encoding=\"utf-8\") as f:\n    du_lieu = f.read()\n\n#  ⇕  Python thật sự làm:\n\ncm = open(\"a.txt\", encoding=\"utf-8\")\nf = cm.__enter__()                     # ⭐ as f nhận GIÁ TRỊ TRẢ VỀ này\ntry:\n    du_lieu = f.read()\nexcept BaseException as e:\n    if not cm.__exit__(type(e), e, e.__traceback__):\n        raise                          # ⭐ __exit__ trả True thì KHÔNG raise\nelse:\n    cm.__exit__(None, None, None)\n```\n\nHai điều rút ra ngay từ đoạn tương đương này:\n\n```\n① as f nhận thứ __enter__ TRẢ VỀ, không phải object context manager\n② __exit__ trả True ⇒ ngoại lệ bị NUỐT\n```\n\n`__exit__` cũng chạy khi bạn thoát bằng `return`, `break`, hay `continue` — vì tất cả đều là \"rời khỏi khối\".\n\n### Viết context manager bằng class\n\n```python\nclass MoFile:\n    def __init__(self, duong_dan, che_do=\"r\"):\n        self.duong_dan = duong_dan\n        self.che_do = che_do\n        self.f = None\n\n    def __enter__(self):\n        print(\"① vào\")\n        self.f = open(self.duong_dan, self.che_do, encoding=\"utf-8\")\n        return self.f                    # ⭐ đây là thứ \"as\" nhận\n\n    def __exit__(self, loai, gia_tri, truy_vet):\n        print(\"③ ra\")\n        if self.f:\n            self.f.close()\n        return False                     # ⭐ KHÔNG nuốt ngoại lệ\n\nwith MoFile(\"a.txt\") as f:              # f là file object, KHÔNG phải MoFile\n    print(\"② thân\")\n    noi_dung = f.read()\n```\n\nBa tham số của `__exit__` cho phép xử lý khác nhau tuỳ tình huống:\n\n```python\ndef __exit__(self, loai, gia_tri, truy_vet):\n    if loai is None:\n        print(\"thoát bình thường\")\n    else:\n        print(f\"thoát vì {loai.__name__}: {gia_tri}\")\n    return False\n```\n\n### Trả `True` từ `__exit__` — bẫy nuốt ngoại lệ\n\n```python\nclass NuotLoi:\n    def __enter__(self):\n        return self\n    def __exit__(self, *args):\n        return True                      # ❌ NUỐT MỌI ngoại lệ\n\nwith NuotLoi():\n    1 / 0                                # không có lỗi nào hiện ra!\n    print(\"dòng này KHÔNG chạy\")\nprint(\"chương trình chạy tiếp bình thường\")\n```\n\nĐây là biến thể của `except: pass` ở bài ngoại lệ, và cũng nguy hiểm y như vậy. Quy tắc:\n\n```\n__exit__ trả False (hoặc không return gì) ⇒ ngoại lệ đi tiếp lên trên  ← MẶC ĐỊNH ĐÚNG\n__exit__ trả True                          ⇒ nuốt — chỉ dùng CÓ CHỦ ĐÍCH\n```\n\nLỗi rất hay gặp: viết `__exit__` kết thúc bằng một biểu thức trả về giá trị thật, ví dụ `return self.f.close()`. May là `close()` trả `None` nên vô hại, nhưng nếu là hàm trả `True` thì bạn có một cái nuốt ngoại lệ mà không biết.\n\nTrường hợp nuốt **có chủ đích** là chính đáng:\n\n```python\nimport contextlib\n\nwith contextlib.suppress(FileNotFoundError):\n    os.remove(\"co_the_khong_ton_tai.txt\")\n\n# tương đương\ntry:\n    os.remove(\"co_the_khong_ton_tai.txt\")\nexcept FileNotFoundError:\n    pass\n```\n\n`suppress` hơn `try/except/pass` ở chỗ nó **nói rõ ý định** ngay ở dòng đầu và không thể vô tình bắt rộng hơn dự định.\n\n### `@contextmanager` — ba bài gặp nhau\n\n```python\nfrom contextlib import contextmanager\n\n@contextmanager\ndef mo_file(duong_dan, che_do=\"r\"):\n    f = open(duong_dan, che_do, encoding=\"utf-8\")     # phần __enter__\n    try:\n        yield f                                        # ⭐ giá trị cho \"as\"\n    finally:\n        f.close()                                      # phần __exit__\n\nwith mo_file(\"a.txt\") as f:\n    print(f.read())\n```\n\nSơ đồ tương ứng:\n\n```\nTrước yield   →  __enter__\nyield x       →  giá trị x cho mệnh đề \"as\"\nSau yield     →  __exit__\n```\n\nBa quy tắc bắt buộc, vi phạm là lỗi:\n\n```\n① Đúng MỘT yield. Hai yield ⇒ RuntimeError \"generator didn't stop\"\n② Phần dọn dẹp phải trong finally, nếu không thì ngoại lệ trong khối with\n   làm nó không chạy\n③ Muốn nuốt ngoại lệ thì bắt bằng except rồi KHÔNG raise lại\n```\n\nĐiểm ② là lỗi phổ biến nhất khi mới dùng `@contextmanager`:\n\n```python\n# ❌ có ngoại lệ trong khối with ⇒ dòng close() không bao giờ chạy\n@contextmanager\ndef sai(duong_dan):\n    f = open(duong_dan, encoding=\"utf-8\")\n    yield f\n    f.close()                # ← bị bỏ qua nếu khối with ném lỗi\n\n# ✓\n@contextmanager\ndef dung(duong_dan):\n    f = open(duong_dan, encoding=\"utf-8\")\n    try:\n        yield f\n    finally:\n        f.close()\n```\n\nBắt ngoại lệ trong `@contextmanager`:\n\n```python\n@contextmanager\ndef bo_qua_loi(*loai):\n    try:\n        yield\n    except loai:\n        pass                 # nuốt CÓ CHỦ ĐÍCH — tương đương return True\n```\n\n### Nhiều context manager\n\n```python\n# Lồng nhau — dài dòng\nwith open(\"vao.txt\", encoding=\"utf-8\") as fv:\n    with open(\"ra.txt\", \"w\", encoding=\"utf-8\") as fr:\n        fr.write(fv.read())\n\n# ✓ Một dòng, tương đương hoàn toàn\nwith open(\"vao.txt\", encoding=\"utf-8\") as fv, \\\n     open(\"ra.txt\", \"w\", encoding=\"utf-8\") as fr:\n    fr.write(fv.read())\n\n# Python 3.10 trở lên — bọc ngoặc, xuống dòng thoải mái\nwith (\n    open(\"vao.txt\", encoding=\"utf-8\") as fv,\n    open(\"ra.txt\", \"w\", encoding=\"utf-8\") as fr,\n):\n    fr.write(fv.read())\n```\n\nThứ tự đảm bảo: mở từ trái sang phải, **đóng từ phải sang trái**. Nếu `open` thứ hai lỗi thì cái thứ nhất **vẫn được đóng** — đúng như lồng nhau.\n\nKhi số lượng không biết trước, dùng `ExitStack`:\n\n```python\nfrom contextlib import ExitStack\n\ndef gop_file(danh_sach, ra):\n    with ExitStack() as stack:\n        cac_file = [stack.enter_context(open(p, encoding=\"utf-8\"))\n                    for p in danh_sach]                     # ⭐ n file\n        with open(ra, \"w\", encoding=\"utf-8\") as fr:\n            for f in cac_file:\n                fr.write(f.read())\n    # mọi file đóng đúng thứ tự ngược, dù có lỗi ở bất kỳ đâu\n```\n\n`ExitStack` còn nhận cả hàm dọn dẹp thường:\n\n```python\nwith ExitStack() as stack:\n    stack.callback(print, \"dọn dẹp cuối cùng\")     # gọi khi rời khối\n    stack.enter_context(mo_khoa())\n```\n\n### Vì sao không dựa vào bộ thu gom rác\n\n```python\n# ❌ Dựa vào GC — hành vi khác nhau giữa các bản hiện thực Python\nfor duong_dan in danh_sach_10000_file:\n    du_lieu = open(duong_dan, encoding=\"utf-8\").read()\n    xu_ly(du_lieu)\n# CPython thường đóng file ngay nhờ đếm tham chiếu;\n# bản hiện thực khác thì KHÔNG — và bạn hết file descriptor.\n\n# ✓\nfor duong_dan in danh_sach_10000_file:\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        xu_ly(f.read())\n```\n\nLý do kỹ thuật: CPython dùng **đếm tham chiếu**, nên object bị huỷ ngay khi không còn ai trỏ tới. Nhưng đó là **chi tiết cài đặt**, không phải bảo đảm của ngôn ngữ. Các bản hiện thực khác dọn rác theo chu kỳ, và file có thể mở hàng phút trước khi được đóng.\n\nĐây là cùng bài học với `is` và cache số nguyên ở bài 1: **đừng dựa vào chi tiết cài đặt của một bản hiện thực.**\n\n### `__exit__` và giao dịch\n\nMẫu quan trọng nhất trong thực tế — dùng thông tin ngoại lệ để quyết định:\n\n```python\nclass GiaoDich:\n    def __init__(self, ket_noi):\n        self.kn = ket_noi\n\n    def __enter__(self):\n        self.kn.execute(\"BEGIN\")\n        return self.kn\n\n    def __exit__(self, loai, gia_tri, truy_vet):\n        if loai is None:\n            self.kn.execute(\"COMMIT\")        # ⭐ không lỗi ⇒ ghi nhận\n        else:\n            self.kn.execute(\"ROLLBACK\")      # ⭐ có lỗi ⇒ huỷ bỏ\n        return False                          # ngoại lệ vẫn bay lên trên\n```\n\n```python\nwith GiaoDich(kn) as c:\n    c.execute(\"UPDATE tai_khoan SET so_du = so_du - 100 WHERE id = 1\")\n    c.execute(\"UPDATE tai_khoan SET so_du = so_du + 100 WHERE id = 2\")\n# Lỗi giữa hai câu lệnh ⇒ ROLLBACK ⇒ không có chuyện tiền bay mất\n```\n\n`return False` ở cuối rất quan trọng: giao dịch được rollback, **nhưng người gọi vẫn biết là có lỗi**. Trả `True` ở đây là rollback rồi giả vờ mọi thứ ổn — sai nghiêm trọng.\n\n### Context manager có thể dùng lại không\n\n```python\ncm = mo_file(\"a.txt\")        # tạo bằng @contextmanager\n\nwith cm as f:\n    pass\nwith cm as f:                # ❌ RuntimeError: generator didn't yield\n    pass\n```\n\nContext manager tạo bằng `@contextmanager` **dùng một lần**, vì generator bên dưới đã cạn — đúng bài trước. Context manager viết bằng class thì tuỳ thiết kế, nhưng an toàn nhất là **luôn tạo mới**:\n\n```python\nwith mo_file(\"a.txt\") as f: ...\nwith mo_file(\"a.txt\") as f: ...      # ✓ tạo mới mỗi lần\n```\n\nMột số context manager của thư viện chuẩn hỗ trợ **tái nhập** (`threading.RLock`), nhưng đó là ngoại lệ có chủ đích, không phải quy tắc chung.\n\n### Object vừa là decorator vừa là context manager\n\n```python\nfrom contextlib import ContextDecorator\nimport time\n\nclass do_thoi_gian(ContextDecorator):\n    def __init__(self, ten=\"\"):\n        self.ten = ten\n    def __enter__(self):\n        self.t = time.perf_counter()\n        return self\n    def __exit__(self, *args):\n        print(f\"{self.ten}: {time.perf_counter()-self.t:.4f}s\")\n        return False\n\n# Dùng như context manager\nwith do_thoi_gian(\"khối code\"):\n    tinh_toan()\n\n# ...và như decorator, không cần viết thêm gì\n@do_thoi_gian(\"hàm\")\ndef tinh_toan(): ...\n```\n\n`ContextDecorator` là ví dụ đẹp về việc hai khái niệm của module này thực chất rất gần nhau: cả hai đều là \"làm gì đó trước và sau một đoạn code\".\n\nLưu ý: với `@contextmanager`, tính năng này đã có sẵn — hàm được trang trí bằng `@contextmanager` dùng được luôn làm decorator.\n\n### Các context manager có sẵn đáng biết\n\n```python\nimport contextlib, os, tempfile, decimal\n\n# Nuốt một loại ngoại lệ, có chủ đích\nwith contextlib.suppress(FileNotFoundError):\n    os.remove(\"tam.txt\")\n\n# Chuyển hướng đầu ra\nwith open(\"log.txt\", \"w\", encoding=\"utf-8\") as f:\n    with contextlib.redirect_stdout(f):\n        print(\"dòng này vào file\")\n\n# Thư mục tạm — tự xoá cả cây khi ra\nwith tempfile.TemporaryDirectory() as tm:\n    (Path(tm) / \"a.txt\").write_text(\"thử\", encoding=\"utf-8\")\n# tm đã bị xoá hoàn toàn\n\n# Khoá cho đa luồng\nimport threading\nkhoa = threading.Lock()\nwith khoa:\n    vung_gang()          # tự mở khoá kể cả khi có ngoại lệ\n\n# Đóng object chỉ có close() mà không có __exit__\nfrom urllib.request import urlopen\nwith contextlib.closing(urlopen(url)) as p:\n    du_lieu = p.read()\n```\n\n`threading.Lock` là ứng dụng đáng nhớ nhất về mặt an toàn: quên mở khoá vì một ngoại lệ là **deadlock**, và `with` loại bỏ hoàn toàn khả năng đó. Nối về bài **Deadlock, race condition** ở module 2.",

vi_du: "### Đo thời gian — ba cách viết cùng một thứ\n\n```python\nimport time\nfrom contextlib import contextmanager, ContextDecorator\n\n# ① Class — dài nhưng rõ từng bước\nclass DoThoiGian:\n    def __init__(self, ten=\"\"):\n        self.ten = ten\n    def __enter__(self):\n        self.t = time.perf_counter()\n        return self\n    def __exit__(self, *args):\n        self.giay = time.perf_counter() - self.t\n        print(f\"{self.ten}: {self.giay:.4f}s\")\n        return False\n\n# ② @contextmanager — ngắn nhất ⭐\n@contextmanager\ndef do_thoi_gian(ten=\"\"):\n    t = time.perf_counter()\n    try:\n        yield\n    finally:                                  # ⭐ đo cả khi có lỗi\n        print(f\"{ten}: {time.perf_counter()-t:.4f}s\")\n\n# Dùng\nwith DoThoiGian(\"cách 1\") as d:\n    sum(range(10_000_000))\nprint(d.giay)                                 # class cho phép lấy lại số đo\n\nwith do_thoi_gian(\"cách 2\"):\n    sum(range(10_000_000))\n\n@do_thoi_gian(\"cách 2 làm decorator\")          # ⭐ dùng luôn được như decorator\ndef tinh():\n    return sum(range(10_000_000))\n```\n\nCách ② ngắn hơn hẳn và tự động dùng được làm decorator. Cách ① đáng chọn khi cần **lấy lại dữ liệu sau khi ra khỏi khối** — ở đây là `d.giay`, thứ mà `@contextmanager` không cho trực tiếp.\n\n### Giao dịch database\n\n```python\nimport sqlite3\nfrom contextlib import contextmanager\n\n@contextmanager\ndef giao_dich(duong_dan):\n    kn = sqlite3.connect(duong_dan)\n    try:\n        yield kn\n        kn.commit()                 # ⭐ chỉ commit khi KHÔNG có ngoại lệ\n    except Exception:\n        kn.rollback()               # ⭐ có lỗi ⇒ huỷ toàn bộ\n        raise                       # ⭐ vẫn báo cho người gọi\n    finally:\n        kn.close()                  # ⭐ luôn đóng kết nối\n\n# Chuyển tiền — tất cả hoặc không gì\ntry:\n    with giao_dich(\"nh.db\") as kn:\n        kn.execute(\"UPDATE tk SET so_du = so_du - ? WHERE id = ?\", (100, 1))\n        kn.execute(\"UPDATE tk SET so_du = so_du + ? WHERE id = ?\", (100, 2))\n        if kn.execute(\"SELECT so_du FROM tk WHERE id=1\").fetchone()[0] < 0:\n            raise ValueError(\"không đủ số dư\")\nexcept ValueError as e:\n    print(f\"giao dịch bị huỷ: {e}\")      # dữ liệu đã rollback về nguyên trạng\n```\n\nBa dòng `commit`, `rollback`, `raise` xếp đúng thứ tự là toàn bộ nghiệp vụ giao dịch. Bỏ `raise` là biến một giao dịch thất bại thành im lặng — người gọi tưởng tiền đã chuyển.\n\n### Đổi thư mục tạm thời\n\n```python\nimport os\nfrom contextlib import contextmanager\nfrom pathlib import Path\n\n@contextmanager\ndef trong_thu_muc(duong_dan):\n    cu = Path.cwd()\n    os.chdir(duong_dan)\n    try:\n        yield Path(duong_dan)\n    finally:\n        os.chdir(cu)                # ⭐ quay về dù có lỗi\n\nprint(Path.cwd())\nwith trong_thu_muc(\"/tmp\"):\n    print(Path.cwd())               # /tmp\n    1 / 0                           # kể cả lỗi\nprint(Path.cwd())                   # đã quay về đúng chỗ cũ\n```\n\nMẫu \"lưu trạng thái, đổi, khôi phục\" này áp dụng cho rất nhiều thứ: biến môi trường, mức log, cấu hình toàn cục, seed ngẫu nhiên. Đây là công dụng của `with` mà người mới ít nghĩ tới.\n\n```python\n@contextmanager\ndef bien_moi_truong(**bien):\n    cu = {k: os.environ.get(k) for k in bien}\n    os.environ.update({k: str(v) for k, v in bien.items()})\n    try:\n        yield\n    finally:\n        for k, v in cu.items():\n            if v is None:\n                os.environ.pop(k, None)\n            else:\n                os.environ[k] = v\n\nwith bien_moi_truong(DEBUG=\"1\", API_KEY=\"thu\"):\n    chay_kiem_thu()\n# môi trường đã khôi phục nguyên trạng\n```\n\n### Ghi file an toàn — gói lại thành context manager\n\n```python\nimport os, tempfile\nfrom contextlib import contextmanager\nfrom pathlib import Path\n\n@contextmanager\ndef ghi_an_toan(duong_dan, encoding=\"utf-8\"):\n    \"\"\"Ghi tất-cả-hoặc-không-gì. File cũ chỉ mất khi bản mới hoàn chỉnh.\"\"\"\n    p = Path(duong_dan)\n    fd, tam = tempfile.mkstemp(dir=p.parent, suffix=\".tmp\")\n    try:\n        with os.fdopen(fd, \"w\", encoding=encoding) as f:\n            yield f                         # ⭐ người dùng ghi vào file TẠM\n            f.flush()\n            os.fsync(f.fileno())\n        os.replace(tam, p)                  # ⭐ đổi tên nguyên tử\n    except BaseException:\n        os.unlink(tam)                      # lỗi ⇒ dọn file tạm, giữ file cũ\n        raise\n\n# Dùng đơn giản như open()\nwith ghi_an_toan(\"quan_trong.json\") as f:\n    json.dump(du_lieu_lon, f, ensure_ascii=False, indent=2)\n# Lỗi giữa chừng ⇒ file cũ NGUYÊN VẸN, không có file dở dang\n```\n\nĐây là mẫu ghi an toàn ở bài đọc ghi file, giờ đã thành một công cụ dùng lại được với cú pháp giống hệt `open()`. Đó chính là giá trị của context manager: **đóng gói một quy trình đúng thành một thứ dễ dùng đúng.**\n\n### `ExitStack` cho số lượng không biết trước\n\n```python\nfrom contextlib import ExitStack\n\ndef gop_csv(danh_sach_vao, duong_dan_ra):\n    \"\"\"Gộp n file CSV, giữ một dòng tiêu đề.\"\"\"\n    import csv\n    with ExitStack() as stack:\n        fr = stack.enter_context(\n            open(duong_dan_ra, \"w\", newline=\"\", encoding=\"utf-8\"))\n        ghi = None\n        for p in danh_sach_vao:\n            f = stack.enter_context(\n                open(p, newline=\"\", encoding=\"utf-8-sig\"))       # ⭐ thêm dần\n            doc = csv.DictReader(f)\n            if ghi is None:\n                ghi = csv.DictWriter(fr, fieldnames=doc.fieldnames)\n                ghi.writeheader()\n            ghi.writerows(doc)\n    # mọi file đóng theo thứ tự ngược, kể cả khi lỗi ở file thứ 7\n\ngop_csv([\"a.csv\", \"b.csv\", \"c.csv\"], \"gop.csv\")\n```\n\nKhông có `ExitStack` thì phải viết `try/finally` lồng nhau theo số file — bất khả thi khi số file chỉ biết lúc chạy.\n\n### Khoá cho đa luồng\n\n```python\nimport threading\n\nclass BoDemAnToan:\n    def __init__(self):\n        self._n = 0\n        self._khoa = threading.Lock()\n\n    def tang(self):\n        with self._khoa:                # ⭐ tự mở khoá kể cả khi lỗi\n            self._n += 1\n\n    @property\n    def gia_tri(self):\n        with self._khoa:\n            return self._n\n\nbd = BoDemAnToan()\nluong = [threading.Thread(target=lambda: [bd.tang() for _ in range(10_000)])\n         for _ in range(10)]\nfor t in luong: t.start()\nfor t in luong: t.join()\nprint(bd.gia_tri)        # đúng 100000\n```\n\nSo với `khoa.acquire()` và `khoa.release()` viết tay: nếu có ngoại lệ giữa hai dòng đó, khoá **không bao giờ được mở** và mọi luồng khác treo vĩnh viễn. `with` loại bỏ hoàn toàn khả năng đó — nối trực tiếp về bài **Deadlock, race condition** ở module 2.\n\n### Ba lỗi hay gặp với `@contextmanager`\n\n```python\n# ❌ ① Không có try/finally — dọn dẹp bị bỏ qua khi có lỗi\n@contextmanager\ndef sai1():\n    print(\"vào\")\n    yield\n    print(\"ra\")          # KHÔNG chạy nếu khối with ném lỗi\n\n# ❌ ② Hai yield\n@contextmanager\ndef sai2():\n    yield 1\n    yield 2              # RuntimeError: generator didn't stop\n\n# ❌ ③ Nuốt ngoại lệ ngoài ý muốn\n@contextmanager\ndef sai3():\n    try:\n        yield\n    except Exception:\n        pass             # mọi lỗi biến mất im lặng\n\n# ✓ Bản đúng\n@contextmanager\ndef dung():\n    print(\"vào\")\n    try:\n        yield\n    finally:\n        print(\"ra\")      # LUÔN chạy\n```\n\nKiểm chứng nhanh bằng một dòng lỗi cố ý:\n\n```python\nfor cm in (sai1, dung):\n    print(f\"--- {cm.__name__}\")\n    try:\n        with cm():\n            1 / 0\n    except ZeroDivisionError:\n        print(\"bắt được lỗi\")\n\n# --- sai1\n# vào\n# bắt được lỗi          ← thiếu dòng \"ra\"\n# --- dung\n# vào\n# ra                     ← ✓\n# bắt được lỗi\n```\n\n### Ẩn cảnh báo và đổi cấu hình tạm thời\n\n```python\nimport warnings, decimal, random\n\n# Ẩn cảnh báo trong một đoạn\nwith warnings.catch_warnings():\n    warnings.simplefilter(\"ignore\")\n    ham_cu_co_canh_bao()\n# cấu hình cảnh báo đã khôi phục\n\n# Đổi độ chính xác số thập phân tạm thời\nwith decimal.localcontext() as ctx:\n    ctx.prec = 50\n    print(decimal.Decimal(1) / decimal.Decimal(7))     # 50 chữ số\nprint(decimal.Decimal(1) / decimal.Decimal(7))        # về mặc định\n\n# Seed ngẫu nhiên tạm thời — cho kiểm thử lặp lại được\n@contextmanager\ndef seed_co_dinh(n):\n    trang_thai = random.getstate()\n    random.seed(n)\n    try:\n        yield\n    finally:\n        random.setstate(trang_thai)       # ⭐ khôi phục, không ảnh hưởng bên ngoài\n\nwith seed_co_dinh(42):\n    print(random.random())                # luôn cùng một giá trị\n```\n\nMẫu `seed_co_dinh` rất hữu ích với định hướng AI/ML: nó cho phép một đoạn kiểm thử lặp lại được mà **không làm hỏng trạng thái ngẫu nhiên** của phần còn lại chương trình.",

so_sanh: "### `with` và `try/finally`\n\n| | `try/finally` | `with` |\n|---|---|---|\n| Đảm bảo dọn dẹp | ✓ | ✓ |\n| Số dòng | nhiều hơn | **ít hơn** |\n| Quên dọn dẹp | **dễ quên** | không thể quên |\n| Nhiều tài nguyên | lồng nhiều tầng | **một dòng** |\n| Dùng lại logic dọn dẹp | sao chép mỗi nơi | **viết một lần** |\n| Dùng khi | không có context manager | **mặc định** |\n\nHàng áp chót là giá trị lớn nhất mà người mới hay bỏ qua: `with` cho phép **đóng gói một quy trình đúng** (ghi an toàn, giao dịch, khôi phục trạng thái) thành một thứ mà người khác khó dùng sai.\n\n### Class và `@contextmanager`\n\n| | Class | `@contextmanager` |\n|---|---|---|\n| Số dòng | nhiều | **ít** |\n| Đọc dễ | rõ từng bước | **rất gọn** |\n| Giữ trạng thái để dùng sau khối | **✓ qua `self`** | ✗ |\n| Dùng lại nhiều lần | ✓ nếu thiết kế đúng | **✗ một lần** |\n| Kế thừa, thêm phương thức | **✓** | ✗ |\n| Làm decorator luôn | cần `ContextDecorator` | **✓ có sẵn** |\n| Dùng khi | cần API phong phú, cần lấy dữ liệu sau | **đa số trường hợp** |\n\nHàng \"giữ trạng thái để dùng sau khối\" là lý do chính để chọn class: `with DoThoiGian() as d: ...` rồi đọc `d.giay` sau đó.\n\n### `__exit__` trả `True` hay `False`\n\n| | Trả `False`/`None` | Trả `True` |\n|---|---|---|\n| Ngoại lệ | **bay tiếp lên trên** | **bị nuốt** |\n| Người gọi biết có lỗi | ✓ | ✗ |\n| Mặc định đúng | **✓** | ✗ |\n| Dùng khi | hầu hết mọi lúc | `suppress`, có chủ đích rõ ràng |\n\nĐây là biến thể của `except: pass`, và nguy hiểm y như vậy — chỉ khác là nó ẩn hơn, vì nằm trong một phương thức mà người đọc khối `with` không nhìn thấy.\n\n### Tài nguyên nên quản lý bằng `with`\n\n| Tài nguyên | Hậu quả nếu quên dọn |\n|---|---|\n| File | hết file descriptor, dữ liệu chưa ghi xuống đĩa |\n| Kết nối database | cạn pool, ứng dụng treo |\n| Khoá luồng | **deadlock** — mọi luồng treo vĩnh viễn |\n| Socket | rò rỉ cổng, cạn tài nguyên hệ thống |\n| Thư mục tạm | đầy đĩa dần |\n| Trạng thái toàn cục | rò rỉ sang phần code khác, lỗi rất khó lần |\n\nHàng \"khoá luồng\" là nghiêm trọng nhất: quên mở khoá vì một ngoại lệ làm treo toàn bộ chương trình, và triệu chứng không chỉ ra nguyên nhân.\n\n### `with` nhiều dòng — ba cách viết\n\n| | Lồng nhau | Dấu phẩy | Bọc ngoặc (3.10+) |\n|---|---|---|---|\n| Thụt lề | sâu dần | **phẳng** | phẳng |\n| Xuống dòng dễ | ✓ | cần dấu `\\` | **✓ tự nhiên** |\n| Đóng đúng thứ tự ngược | ✓ | ✓ | ✓ |\n| Số lượng biết trước | ✓ | ✓ | ✓ |\n| Số lượng **không** biết trước | ✗ | ✗ | ✗ → **`ExitStack`** |\n\n### Ba bài của module gặp nhau\n\n| | Decorator | Generator | Context manager |\n|---|---|---|---|\n| Nền tảng | hàm là object, closure | `yield` giữ trạng thái | `__enter__` / `__exit__` |\n| Giải bài toán | thêm hành vi quanh hàm | sinh dữ liệu lười | **dọn dẹp đảm bảo** |\n| Điểm gặp | `@contextmanager` **là decorator** | ...áp lên **generator**... | ...tạo ra **context manager** |\n\nBảng này là lý do ba bài được xếp liền nhau: `@contextmanager` chỉ hiểu được khi đã nắm cả decorator lẫn generator, và nó là ví dụ đẹp nhất về việc các cơ chế của Python ghép vào nhau thế nào.",

loi_thuong_gap: [
  "**Không dùng `with` khi mở file.** Dựa vào bộ thu gom rác là dựa vào chi tiết cài đặt; đổi môi trường là `Too many open files`.",
  "**Quên `try/finally` trong `@contextmanager`.** Ngoại lệ trong khối `with` làm phần dọn dẹp không chạy.",
  "**Viết hai `yield` trong `@contextmanager`.** `RuntimeError: generator didn't stop`.",
  "**`__exit__` trả `True` ngoài ý muốn.** Mọi ngoại lệ trong khối `with` biến mất im lặng.",
  "**Tưởng `as f` nhận object context manager.** Nó nhận thứ `__enter__` **trả về** — thường là hai thứ khác nhau.",
  "**Dùng lại một context manager tạo bằng `@contextmanager`.** Generator đã cạn. Tạo mới mỗi lần.",
  "**Bắt `Exception` trong `@contextmanager` mà không `raise` lại.** Nuốt lỗi giống `except: pass`.",
  "**Trong giao dịch: rollback rồi không `raise`.** Người gọi tưởng giao dịch thành công.",
  "**`acquire()` / `release()` khoá bằng tay.** Ngoại lệ giữa hai dòng là deadlock. Dùng `with khoa`.",
  "**Đổi trạng thái toàn cục mà không khôi phục trong `finally`.** Thư mục làm việc, biến môi trường, mức log rò rỉ sang phần code khác.",
  "**Viết `try/finally` lồng nhau cho nhiều tài nguyên.** Dùng dấu phẩy, hoặc `ExitStack` khi số lượng không biết trước.",
  "**Dùng `try/except/pass` thay vì `contextlib.suppress`.** `suppress` nói rõ ý định và không bắt rộng hơn dự định.",
  "**Quên rằng `__exit__` cũng chạy khi `return` hoặc `break`.** Đây là tính năng, nhưng phải biết để không bất ngờ.",
  "**Viết context manager cho việc không có phần dọn dẹp.** Nếu không có gì phải dọn thì `with` chỉ làm code rối thêm."
],

tu_kiem_tra: [
  "`with cm as x` tương đương với đoạn `try/finally` nào? Viết ra đầy đủ.",
  "`as f` nhận giá trị gì? Vì sao với `open()` thì không ai để ý sự khác biệt?",
  "`__exit__` nhận ba tham số nào? Dùng chúng để làm gì trong một giao dịch database?",
  "`__exit__` trả `True` thì chuyện gì xảy ra? Vì sao đó là bẫy?",
  "Nêu ba quy tắc bắt buộc khi viết `@contextmanager`. Vi phạm mỗi cái gây lỗi gì?",
  "Vì sao không được dựa vào bộ thu gom rác để đóng file?",
  "Khi mở hai file bằng một `with`, thứ tự đóng là gì? Nếu file thứ hai lỗi khi mở thì sao?",
  "Khi nào cần `ExitStack` thay vì `with` nhiều tài nguyên bằng dấu phẩy?",
  "Vì sao context manager tạo bằng `@contextmanager` không dùng lại được?",
  "Mẫu \"lưu trạng thái, đổi, khôi phục\" áp dụng cho những thứ gì ngoài file?",
  "Trong giao dịch, vì sao `__exit__` phải trả `False` sau khi rollback?",
  "Vì sao `with khoa` an toàn hơn `acquire()`/`release()` viết tay?",
  "Ba bài decorator, generator, context manager gặp nhau ở đâu?",
  "Khi nào chọn class thay vì `@contextmanager`?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *The with statement* trong tham khảo ngôn ngữ, và *Context Manager Types* — định nghĩa chính xác của giao thức.",
  "Tài liệu Python, module `contextlib` — `contextmanager`, `suppress`, `ExitStack`, `closing`, `redirect_stdout` đều nằm ở đó kèm ví dụ.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về context manager giải thích cả `@contextmanager` lẫn quan hệ với generator.",
  "Sách **Effective Python** — Brett Slatkin. Các mục về việc dùng `contextlib` cho `with` và về quản lý trạng thái tạm thời.",
  "Tài liệu Python, module `threading` — phần về `Lock` và `RLock`, nơi `with` là cách dùng được khuyến nghị."
],

lien_he: [
  "**Xử lý ngoại lệ**: `with` là `try/finally` đóng gói; `contextlib.suppress` là `except ... pass` có kỷ luật.",
  "**Generator, iterator và `yield`** ngay trước: `@contextmanager` dựa hoàn toàn vào generator, và quy tắc `try/finally` trong generator là chung.",
  "**Decorator**: `@contextmanager` là một decorator, và `ContextDecorator` cho object dùng được cả hai vai.",
  "**Đọc ghi file, JSON, CSV**: `with open(...)` là context manager dùng nhiều nhất; mẫu ghi an toàn ở bài đó giờ đóng gói lại được.",
  "**Deadlock, race condition** ở module 2: `with khoa` là cách chắc chắn nhất để không quên mở khoá.",
  "**Hệ thống file, quyền truy cập** ở module 2: file descriptor là tài nguyên có giới hạn, và rò rỉ nó là lỗi tích luỹ.",
  "**Lập trình hướng đối tượng** ở module 6 mảng 01: `__enter__`/`__exit__` là ví dụ về phương thức đặc biệt (*dunder*), cùng họ với `__iter__`, `__next__`, `__call__`.",
  "**Database** ở mảng 04: giao dịch, commit, rollback là ứng dụng quan trọng nhất của bài này.",
  "**AI/ML** ở mảng 07: `with torch.no_grad()` và các context manager đổi cấu hình tạm thời là mẫu gặp hằng ngày; `seed_co_dinh` giúp thí nghiệm lặp lại được."
]

};
