window.LESSONS["05:phuc-vu-model-ai:hang-doi-cho-tac-vu-suy-luan-nang"] = {

tom_tat: [
  "Hàng đợi tách thời gian nhận yêu cầu khỏi thời gian chạy inference: API xác thực rồi tạo job, worker lấy job khi có năng lực, còn client truy vấn trạng thái hoặc nhận webhook.",
  "Độ bền không đồng nghĩa “chạy đúng một lần”. Hệ thống thực tế thường giao ít nhất một lần, nên job phải idempotent, có lease, retry có backoff và dead-letter policy.",
  "Media Insight dùng job state machine `queued → running → succeeded/failed/cancelled`; mọi chuyển trạng thái có điều kiện để worker cũ không ghi đè worker mới sau khi lease hết.",
  "Queue bảo vệ API khỏi inference dài nhưng không tự tạo capacity. Admission control, bounded backlog, quota theo tenant và metric tuổi job mới ngăn overload kéo dài.",
  "Payload lớn như ảnh/audio nằm trong object storage quarantine; message chỉ mang opaque key, checksum và metadata tối thiểu, không nhét file hay secret vào broker."
],

can_biet_truoc: [
  "HTTP 202 Accepted, polling, webhook và idempotency key.",
  "Khái niệm producer, consumer, message broker, acknowledgement và visibility timeout/lease.",
  "Transaction cơ sở, unique constraint và compare-and-set khi cập nhật trạng thái.",
  "FastAPI dependency, background process và model inference CPU/GPU-bound."
],

dinh_nghia: "**Hàng đợi suy luận** là ranh giới bất đồng bộ giữa nơi chấp nhận công việc và nơi thực thi model. API ghi một bản ghi job bền vững, đưa tham chiếu vào broker rồi trả `202 Accepted` cùng URL trạng thái. Worker thuê job trong một khoảng thời gian, tải đầu vào đã duyệt, chạy model, lưu kết quả và xác nhận message.\n\nTrong Media Insight, ảnh/audio đã qua bài upload được lưu dưới key không đoán được. `POST /v1/jobs` không đợi model tóm tắt media; nó trả job ID. Cách này phù hợp khi inference có thể kéo dài, cần retry qua deploy hoặc phải xếp lịch GPU. Nó không phải “FastAPI BackgroundTasks”, vì công việc cần sống độc lập với process web.",

vi_sao_can: "Giữ HTTP connection trong vài phút buộc timeout của browser, CDN, load balancer và server cùng đủ dài. Chỉ một lớp đóng sớm là client không biết model còn chạy hay đã mất. Retry request có thể tạo hai inference đắt tiền. Khi traffic tăng đột biến, mọi API worker cùng chờ GPU và health check cũng nghẽn.\n\nQueue biến burst thành backlog nhìn thấy được và cho worker kéo việc theo capacity. API vẫn phản hồi nhanh; deploy web không giết job; GPU pool mở rộng riêng. Quan trọng hơn, job có danh tính và trạng thái để audit, hủy, retry có kiểm soát và tính quota.\n\nNhưng queue không giảm tổng lượng tính toán. Nếu đầu vào đến 20 job/phút mà worker xử lý 10, tuổi backlog tăng vô hạn. Vì vậy hàng đợi chỉ hữu ích khi đi kèm admission control và kế hoạch capacity, không phải chiếc hộp che quá tải.",

co_che: "### Tách control plane và data plane\n\nControl plane là API tạo job, đọc trạng thái, yêu cầu hủy và quản lý quota. Data plane là worker tải media, preprocess và inference. Database giữ source of truth cho trạng thái nghiệp vụ; broker tối ưu việc giao tín hiệu. Không nên coi broker là nơi duy nhất lưu kết quả lâu dài nếu retention message ngắn.\n\nLuồng: client gửi idempotency key → API xác thực quyền với object → transaction tạo job → publish job ID → worker claim → chạy → transaction lưu kết quả → ack. Response 202 nói “đã chấp nhận”, không nói “sẽ chắc chắn thành công”. Trả `Location: /v1/jobs/{id}` giúp client biết chỗ theo dõi.\n\n### State machine ngăn trạng thái vô lý\n\nCác trạng thái tối thiểu là `queued`, `running`, `succeeded`, `failed`, `cancel_requested`, `cancelled`. Chỉ cho phép cạnh hợp lệ. `succeeded → running` không được xảy ra chỉ vì message cũ được giao lại. Mỗi transition ghi timestamp, attempt và worker/lease token.\n\nPhần trăm progress dễ gây hiểu lầm với model không biết trước tổng việc. Dùng phase như `downloading`, `decoding`, `inferencing`, `uploading_result` hoặc số item hoàn tất/tổng khi có nghĩa. Không tăng progress bằng timer giả.\n\n### Transactional outbox đóng khe mất message\n\nNếu API insert job rồi crash trước publish, database có job queued nhưng worker không biết. Nếu publish trước rồi insert fail, worker nhận ID không tồn tại. Không có transaction nguyên tử chung giữa database và broker theo mặc định.\n\nOutbox giải quyết bằng một transaction ghi cả job và hàng `outbox_events`. Publisher riêng đọc hàng chưa gửi, publish rồi đánh dấu. Publisher có thể publish lặp khi crash giữa hai bước, nên consumer vẫn idempotent. Đây là at-least-once end-to-end, không phải phép màu exactly-once.\n\n### Delivery semantics và lời hứa “exactly once”\n\nBroker thường giao lại nếu worker chết trước ack hoặc ack bị mất. “Ít nhất một lần” nghĩa một message có thể xử lý nhiều lần; “nhiều nhất một lần” có thể mất việc. Exactly-once ở broker không bao phủ side effect ngoài broker như ghi object hoặc gọi API billing.\n\nThiết kế đúng là effect-once: trước khi chạy, claim job có điều kiện; kết quả ghi bằng unique key; callback có idempotency key. Nếu inference lặp vẫn tốn tiền dù kết quả không nhân đôi, nên lease và heartbeat giảm xác suất duplicate nhưng không loại bỏ hoàn toàn.\n\n### Lease, heartbeat và fencing token\n\nWorker claim job với `lease_until` và token tăng dần. Trong lúc chạy, heartbeat gia hạn. Nếu worker treo, job có thể được worker khác claim sau timeout. Worker cũ tỉnh lại không được ghi kết quả nếu token không còn khớp; token đóng vai fencing token chặn “zombie worker”.\n\nVisibility timeout phải dài hơn khoảng heartbeat chứ không nhất thiết dài hơn worst-case inference. Timeout quá ngắn gây duplicate; quá dài khiến job kẹt lâu sau crash. Heartbeat nhẹ và monitor job quá hạn giúp cân bằng.\n\n### Acknowledge đúng thời điểm\n\nAck trước khi lưu kết quả có thể làm mất job nếu process chết. Ack sau khi commit kết quả khiến redelivery an toàn vì worker nhìn thấy succeeded và bỏ qua. Nếu upload result object rồi database commit fail, object mồ côi cần garbage collector; đặt deterministic result key theo job giúp lần retry ghi cùng nơi.\n\nKhông giữ database transaction mở suốt inference. Transaction dài khóa row, phình log và dễ timeout. Claim trong transaction ngắn, chạy ngoài transaction, finalize bằng conditional update.\n\n### Retry phải phân loại lỗi\n\nLỗi mạng tạm, rate limit provider và GPU unavailable có thể retry. File corrupt, codec không hỗ trợ hoặc policy từ chối là permanent; retry chỉ đốt tài nguyên. Bug deterministic trong model cũng không khỏi sau 20 lần.\n\nExponential backoff kèm jitter tránh hàng nghìn job thức dậy cùng lúc. Giới hạn attempt và tổng tuổi job. Khi vượt, chuyển failed/dead-letter với reason code, alert theo tỷ lệ chứ không alert từng job. Việc replay dead letter cần quyền và audit vì có thể tái kích hoạt chi phí lớn.\n\n### Idempotency từ mép API\n\nClient timeout sau POST không biết job đã được tạo. Nó retry cùng `Idempotency-Key`; unique constraint `(tenant_id, key)` trả lại job cũ nếu fingerprint request khớp. Nếu cùng key nhưng payload khác, trả conflict thay vì âm thầm dùng kết quả sai.\n\nKey có TTL dựa trên thời gian client có thể retry. Không dùng hash payload đơn thuần làm quyền truy cập: hai tenant upload cùng file không được nhìn thấy job nhau. Scope luôn gồm tenant/user và endpoint semantics.\n\n### Payload broker phải nhỏ và bất biến\n\nKhông base64 ảnh trong JSON message: dung lượng tăng, broker lưu nhiều bản, retry kéo băng thông. Message chỉ chứa job ID, object key, checksum, model/preprocess version. Worker kiểm object vẫn đúng checksum trước dùng.\n\nObject key phải immutable hoặc versioned. Nếu client thay nội dung ở cùng key sau validation, worker xử lý byte chưa duyệt. Bucket quarantine chỉ cho uploader ghi; promotion/copy tới immutable key đã duyệt tạo trust boundary rõ.\n\n### Scheduling GPU và batching\n\nFIFO tuyệt đối có thể để một job video dài chặn nhiều ảnh nhỏ. Tách queue theo loại tài nguyên/model, ưu tiên có giới hạn và weighted fair scheduling theo tenant. Priority do client tự khai phải bị kiểm quyền, nếu không ai cũng gửi “urgent”.\n\nDynamic batching gom request tương thích để tận dụng GPU, nhưng chờ gom batch tăng latency. Batch cần cùng model/version và hình dạng có thể padding. Đặt max batch size cùng max wait; theo dõi throughput lẫn tuổi job, không tối ưu utilization đến mức SLA hỏng.\n\n### Backlog là tín hiệu capacity, không phải kho vô đáy\n\nĐịnh luật trực giác: nếu arrival rate λ lớn hơn service rate μ đủ lâu, queue tăng không giới hạn. Metric quan trọng là tuổi job cũ nhất và thời gian từ queued tới running, không chỉ số message. Một triệu job nhỏ và mười job video có ý nghĩa tải khác nhau.\n\nAdmission control từ chối hoặc trì hoãn tenant đã vượt quota trước khi ghi backlog. Trả 429 khi quota người gọi hết; 503 khi hệ thống không nhận thêm. Có thể ước tính chi phí theo duration/pixel/token và cấp weighted quota, nhưng phải chống client khai sai metadata.\n\n### Polling, webhook và quyền đọc kết quả\n\nPolling đơn giản và bền: client GET trạng thái với backoff, server trả `Retry-After`. Poll mỗi 100 ms cho job 10 phút chỉ tạo tải vô ích. Webhook giảm poll nhưng server phải ký payload, chống replay, retry delivery và không gọi URL nội bộ tùy ý.\n\nJob ID ngẫu nhiên không thay authorization. Mỗi GET/result/cancel kiểm tenant sở hữu. Result URL có thời hạn; không đặt PII trong job ID. Retention tự động xóa media và output theo chính sách, nhưng audit có thể giữ metadata tối thiểu lâu hơn.\n\n### Cancellation là yêu cầu, không phải tức thì\n\nClient đặt `cancel_requested`; worker kiểm giữa các phase hoặc dùng API cancel của provider. GPU kernel đang chạy có thể không dừng an toàn. Khi hoàn tất cùng lúc với cancel, conditional transition quyết định semantics: thường succeeded thắng nếu kết quả đã commit, hoặc policy bỏ kết quả.\n\nHủy không được đơn giản delete row vì worker vẫn cần biết dừng và audit cần lịch sử. Quota/billing phải nói rõ phần tính toán đã tiêu trước khi hủy có được tính hay không.\n\n### Quan sát và truy vết một job\n\nGhi correlation giữa request ID, job ID, message ID, attempt, lease token và model version. Histogram gồm queue wait, download, preprocess, inference, result upload. Counter retry chia theo reason; gauge worker busy và oldest job age.\n\nKhông log payload media hoặc presigned URL có credential. Trace qua thời gian chờ dài có thể không nên giữ một span mở; dùng span links giữa producer và consumer. Dashboard phải tách lỗi permanent của input khỏi lỗi hạ tầng để on-call không bị nhiễu.\n\n### Bảo mật worker\n\nWorker xử lý dữ liệu đối kháng và có quyền đọc object/model, nên chạy quyền tối thiểu, network egress allowlist, resource limit và image được vá. Tách worker theo mức tin cậy/model giúp một parser compromise không chạm mọi dữ liệu.\n\nBroker không phải kênh tin cậy tuyệt đối: authenticate, TLS, ACL producer/consumer, validate schema và không deserialize pickle từ message. Secret reference được resolve tại runtime; không truyền secret thực trong payload sẽ bị lưu và replay.\n\n### Khi nào queue là quá tay\n\nInference dưới vài trăm mili giây, tải ổn định và retry từ client chấp nhận được thường dùng endpoint đồng bộ đơn giản hơn. Queue thêm broker, worker deployment, state store, cleanup, DLQ và operational burden. SSE phù hợp nếu kết quả có thể hiển thị tăng dần nhưng không cần sống qua disconnect. Chọn queue vì durability/capacity isolation, không chỉ vì từ “async”.",

vi_du: "### Mô phỏng hàng đợi bền vững bằng SQLite\n\nVí dụ một process dùng SQLite để minh họa state transition và idempotency; production thay `asyncio.Queue` bằng broker và giữ nguyên nguyên tắc conditional update/outbox.\n\n```python\nimport asyncio, sqlite3, uuid\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI, Header, HTTPException, Response\nfrom pydantic import BaseModel\n\ndb = sqlite3.connect(\"jobs.db\", check_same_thread=False, isolation_level=None)\ndb.execute(\"PRAGMA journal_mode=WAL\")\ndb.execute(\"\"\"CREATE TABLE IF NOT EXISTS jobs(\n id TEXT PRIMARY KEY, tenant TEXT, idem TEXT, input_key TEXT,\n status TEXT, result TEXT, attempt INTEGER DEFAULT 0,\n UNIQUE(tenant, idem))\"\"\")\nqueue: asyncio.Queue[str] = asyncio.Queue(maxsize=100)\n\nclass JobIn(BaseModel):\n    input_key: str\n\nasync def worker():\n    while True:\n        job_id = await queue.get()\n        try:\n            changed = db.execute(\n                \"UPDATE jobs SET status='running', attempt=attempt+1 \"\n                \"WHERE id=? AND status='queued'\", (job_id,)).rowcount\n            if not changed: continue\n            await asyncio.sleep(0.5)  # thay bằng PredictionService\n            db.execute(\"UPDATE jobs SET status='succeeded', result=? \"\n                       \"WHERE id=? AND status='running'\",\n                       ('{\"label\":\"safe\"}', job_id))\n        except Exception:\n            db.execute(\"UPDATE jobs SET status='failed' WHERE id=? AND status='running'\", (job_id,))\n        finally:\n            queue.task_done()\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    task = asyncio.create_task(worker())\n    yield\n    task.cancel()\n\napp = FastAPI(lifespan=lifespan)\n\n@app.post(\"/v1/jobs\", status_code=202)\nasync def create_job(payload: JobIn, response: Response,\n                     idempotency_key: str = Header(), x_tenant: str = Header()):\n    existing = db.execute(\"SELECT id,input_key FROM jobs WHERE tenant=? AND idem=?\",\n                          (x_tenant, idempotency_key)).fetchone()\n    if existing:\n        if existing[1] != payload.input_key:\n            raise HTTPException(409, detail={\"code\":\"idempotency_payload_mismatch\"})\n        job_id = existing[0]\n    else:\n        if queue.full():\n            raise HTTPException(503, detail={\"code\":\"backlog_full\"})\n        job_id = str(uuid.uuid4())\n        db.execute(\"INSERT INTO jobs(id,tenant,idem,input_key,status) VALUES(?,?,?,?,?)\",\n                   (job_id, x_tenant, idempotency_key, payload.input_key, \"queued\"))\n        await queue.put(job_id)\n    response.headers[\"Location\"] = f\"/v1/jobs/{job_id}\"\n    return {\"job_id\": job_id, \"status\": \"queued\"}\n\n@app.get(\"/v1/jobs/{job_id}\")\ndef get_job(job_id: str, x_tenant: str = Header()):\n    row = db.execute(\"SELECT status,result,attempt FROM jobs WHERE id=? AND tenant=?\",\n                     (job_id, x_tenant)).fetchone()\n    if not row: raise HTTPException(404, detail={\"code\":\"job_not_found\"})\n    return {\"job_id\": job_id, \"status\": row[0], \"result\": row[1], \"attempt\": row[2]}\n```\n\nDemo còn khe database-queue: process crash sau INSERT trước `queue.put` làm job kẹt, chính là lý do production cần transactional outbox hoặc sweeper tìm queued job chưa publish. SQLite và queue trong RAM không dùng cho nhiều instance; đoạn code nhằm làm state/idempotency chạy được và phơi bày giới hạn thay vì giả làm kiến trúc production.",

so_sanh: "### BackgroundTasks không thay thế broker\n\n| Cách chạy | Sống qua web process crash | Retry/lease | Tách capacity | Trường hợp phù hợp |\n|---|---|---|---|---|\n| FastAPI BackgroundTasks | Không bảo đảm | Tự viết, thường không có | Không | Email/log best-effort rất ngắn |\n| Broker + worker | Có nếu cấu hình durability | Có nền tảng để xây | Có | Inference dài, burst, GPU pool |\n| Workflow engine | Có, lưu lịch sử bước | Retry/timer/compensation mạnh | Có | Pipeline nhiều bước kéo dài |\n| HTTP đồng bộ | Kết nối mất thì mơ hồ | Client retry | Kém | Dự đoán nhanh, tải ổn định |\n\nWorkflow engine đáng cân nhắc khi Media Insight có chuỗi scan → transcode → nhiều model → human review với timer ngày và compensation. Broker đủ cho job đơn hoặc pipeline ngắn. BackgroundTasks tiện nhưng chạy cùng process web; deploy/crash có thể làm mất việc, nên không dùng cho inference đã thu tiền hoặc cần kết quả chắc chắn.",

loi_thuong_gap: [
  "Trả 200 cùng kết quả cho tác vụ dài; nhận biết timeout ngẫu nhiên ở proxy. Tạo job, trả 202 và Location.",
  "Quảng cáo exactly-once nhưng side effect không idempotent; nhận biết duplicate result/billing sau worker crash. Dùng conditional state và idempotency key.",
  "Ack trước khi commit kết quả; nhận biết job biến mất khi process chết. Commit bền vững rồi mới ack.",
  "Retry mọi lỗi; nhận biết file corrupt chạy lại nhiều lần. Phân loại transient/permanent và giới hạn attempt/tuổi.",
  "Nhét media base64 vào message; nhận biết broker đầy và retry tốn băng thông. Lưu object immutable, gửi key/checksum.",
  "Chỉ nhìn queue length; nhận biết job cũ chờ lâu dù số lượng nhỏ. Theo dõi oldest age, wait time và estimated work.",
  "Không fencing worker hết lease; nhận biết kết quả cũ ghi đè attempt mới. Conditional update theo lease token.",
  "Tin job ID ngẫu nhiên là authorization; nhận biết tenant đọc được job khi lộ ID. Kiểm quyền ở mọi status/result/cancel endpoint."
],

tu_kiem_tra: [
  "Mô tả hai khe crash mà transactional outbox xử lý và vì sao consumer vẫn phải idempotent.",
  "Worker A hết lease, worker B hoàn tất, A tỉnh lại: fencing token ngăn ghi đè bằng câu UPDATE nào?",
  "Tại sao không giữ transaction database mở trong suốt inference GPU?",
  "Phân loại retry cho timeout object storage, WAV corrupt, GPU hết bộ nhớ và bug deterministic.",
  "Cùng Idempotency-Key nhưng input_key khác phải trả gì và vì sao không tạo job mới âm thầm?",
  "Arrival 20 job/phút, capacity 10: autoscale có giới hạn nào và admission control nên phản hồi ra sao?",
  "Thiết kế webhook chống giả mạo, replay và SSRF mà không lộ kết quả cho tenant khác."
],

tai_nguyen: [
  "Designing Data-Intensive Applications — Martin Kleppmann.",
  "Enterprise Integration Patterns — Gregor Hohpe và Bobby Woolf.",
  "FastAPI Documentation — Background Tasks và Response Status Code.",
  "Celery Documentation — Tasks, retries và routing (đọc như một hiện thực cụ thể, không coi là kiến trúc duy nhất)."
],

lien_he: [
  "Nối sang mảng Cơ sở dữ liệu: transaction, unique constraint, conditional update và outbox bảo vệ trạng thái job qua crash.",
  "Nối sang mảng Hệ thống phân tán: at-least-once, lease, fencing token, retry và idempotency là nền tảng của consumer đúng.",
  "Nối sang mảng Hệ điều hành/Cloud: process isolation, autoscaling, GPU scheduling và graceful shutdown quyết định capacity worker.",
  "Nối sang mảng Bảo mật backend: tenant authorization, signed webhook, SSRF defense và retention bảo vệ dữ liệu bất đồng bộ."
]

};
