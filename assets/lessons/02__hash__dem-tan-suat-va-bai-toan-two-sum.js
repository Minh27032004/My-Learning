window.LESSONS["02:hash:dem-tan-suat-va-bai-toan-two-sum"] = {

tom_tat: [
  "**Module 1 đã dùng two-sum ba lần làm ví dụ. Bài này không lặp lại nó — mà rút ra HỌ MẪU đằng sau:** \"lưu cái đã thấy để tra ngay\" và \"nhóm theo khoá dẫn xuất\".",
  "**Mẫu ①: đếm tần suất.** `Counter` và `defaultdict` biến hàng chục dòng thành vài dòng, và `most_common(k)` dùng heap nên là O(n log k) chứ không phải O(n log n).",
  "**Mẫu ②: nhóm theo khoá dẫn xuất.** Bài anagram là ví dụ mẫu mực — khoá không phải phần tử, mà là **một hàm của phần tử**.",
  "**Mẫu ③: tổng tiền tố cộng bảng băm.** Đây là kỹ thuật mạnh nhất của bài: nó giải bài \"dãy con có tổng bằng k\" trong O(n), thay cho O(n²) ngây thơ.",
  "**Điểm chung của cả ba mẫu: đổi một vòng lặp lồng nhau lấy một bảng băm** — O(n²) thành O(n), trả bằng O(n) bộ nhớ."
],

can_biet_truoc: [
  "Ba bài trước của module này — cơ chế bảng băm, va chạm, và đánh đổi bộ nhớ.",
  "Bài **Vòng lặp lồng nhau** ở module 1 mảng này — two-sum đã xuất hiện ở đó; bài này đi tiếp từ chỗ đó.",
  "Bài **List, dict, set, tuple** ở module 5 mảng 01 — cú pháp `dict` và comprehension.",
  "Bài **Hàm băm và bảng băm** trong module này — vì sao khoá phải hashable, và bẫy `True == 1`."
],

dinh_nghia: "Nói đời thường: kiểm phiếu bầu. Bạn không so từng lá phiếu với mọi lá khác — bạn lập một bảng, mỗi ứng viên một dòng, và đi qua từng lá phiếu **một lần** để cộng vào dòng tương ứng. Đó là toàn bộ ý tưởng của bài này.\n\nCác khái niệm:\n\n- **Đếm tần suất** (*frequency counting*) — đếm số lần mỗi giá trị xuất hiện. Một lượt duyệt, O(n).\n- **Khoá dẫn xuất** (*derived key*) — khoá **tính ra** từ phần tử, không phải chính phần tử. Ví dụ: các chữ cái đã sắp xếp của một từ.\n- **Nhóm theo khoá** (*grouping*) — gom các phần tử có cùng khoá dẫn xuất vào một nhóm.\n- **Tổng tiền tố** (*prefix sum*) — `P[i]` là tổng các phần tử từ đầu tới vị trí `i`. Nó biến bài toán tổng đoạn thành bài toán hiệu hai số.\n- **Bảng bổ trợ** (*complement*) — với một mục tiêu `k` và một phần tử `x`, phần bổ trợ là `k − x`.\n\nHọ mẫu chung — đây là điều đáng nhớ nhất của bài:\n\n```\nMẪU GỐC:  \"Thay vì so MỌI CẶP, hãy LƯU cái đã thấy rồi TRA NGAY.\"\n\n  O(n²)                          O(n)\n  ─────────────────────          ─────────────────────────\n  for i in ds:                   da_thay = {}\n      for j in ds:               for x in ds:\n          kiểm tra (i, j)            tra da_thay cho cái CẦN\n                                     lưu x vào da_thay\n\n⇒ Đổi một vòng lặp lấy một bảng băm.\n⇒ Trả giá bằng O(n) bộ nhớ.\n```\n\nBa biến thể của mẫu này, theo độ khó tăng dần:\n\n```\n① ĐẾM TẦN SUẤT       khoá = chính phần tử\n② NHÓM THEO KHOÁ     khoá = một HÀM của phần tử\n③ TỔNG TIỀN TỐ + HASH khoá = một ĐẠI LƯỢNG TÍCH LUỸ\n```",

vi_sao_can: "Bài này biến ba bài lý thuyết trước thành công cụ giải bài toán:\n\n- **Nó rút ra mẫu, không chỉ lời giải.** Nhớ \"two-sum dùng set\" chỉ giải được two-sum. Nhớ \"lưu cái đã thấy để tra ngay\" giải được cả một họ bài toán, kể cả những bài bạn chưa gặp.\n- **Đếm tần suất là thao tác phổ biến nhất trong xử lý dữ liệu thật.** Đếm từ, đếm lỗi theo loại, đếm người dùng theo khu vực — và `Counter` làm việc đó trong một dòng.\n- **Khoá dẫn xuất là bước nhảy tư duy quan trọng.** Nhận ra rằng khoá **không nhất thiết là phần tử** mở ra rất nhiều lời giải — nhóm anagram, nhóm điểm thẳng hàng, nhóm bản ghi trùng lặp.\n- **Tổng tiền tố cộng hash là kỹ thuật mạnh mà ít người biết.** Nó biến một lớp bài toán từ O(n²) xuống O(n), và xuất hiện rất nhiều trong phỏng vấn lẫn công việc thật.\n- **`most_common(k)` là ví dụ đẹp về chọn đúng cấu trúc.** Nó dùng heap nên O(n log k) — với top 10 của một triệu bản ghi, nhanh hơn `sorted(...)[:10]` khoảng năm lần.\n\nTình huống thật: bạn cần tìm các đơn hàng nghi ngờ trùng lặp trong 200.000 bản ghi — cùng khách, cùng ngày, lệch tiền dưới ngưỡng. Cách so mọi cặp là 2×10¹⁰ phép so sánh, không chạy được. **Nhóm theo khoá dẫn xuất `(mã khách, ngày)` trước rồi chỉ so trong từng nhóm** đưa nó về gần O(n) — vì mỗi khách chỉ đặt vài đơn mỗi ngày. Đây chính là mẫu ② áp vào việc thật.\n\nMột lưu ý về phạm vi: bài này **không** trình bày lại two-sum cơ bản (module 1 đã làm ba lần). Nó đi thẳng vào các biến thể khó hơn và các mẫu tổng quát hơn.",

co_che: "### Mẫu ①: đếm tần suất\n\n```\nBÀI TOÁN: mỗi giá trị xuất hiện bao nhiêu lần?\n\nNGÂY THƠ  O(n²):     với mỗi x, đếm số lần x xuất hiện trong ds\nĐÚNG      O(n):      một lượt, cộng vào bảng\n```\n\nBa cách viết trong Python, từ dài tới ngắn:\n\n```python\n# ① Thủ công\ndem = {}\nfor x in ds:\n    dem[x] = dem.get(x, 0) + 1\n\n# ② defaultdict — không cần .get\nfrom collections import defaultdict\ndem = defaultdict(int)\nfor x in ds:\n    dem[x] += 1                    # ⭐ khoá thiếu tự tạo với giá trị 0\n\n# ③ Counter — ngắn nhất\nfrom collections import Counter\ndem = Counter(ds)                  # ⭐ một dòng\n```\n\n⚠️ Khác biệt quan trọng giữa `defaultdict` và `dict` thường:\n\n```\nd = defaultdict(int)\n_ = d[\"chưa có\"]        # ⚠️ TẠO khoá với giá trị 0, không phải KeyError\nprint(len(d))            # 1 — khoá đã được thêm!\n\n⇒ Chỉ ĐỌC thôi cũng SINH khoá. Đây là nguồn bug khi kiểm tra\n  \"khoá này có tồn tại không\".\n\n✓ Muốn đọc mà không tạo: dùng d.get(k) hoặc k in d\n```\n\n`Counter` có thêm các thao tác mà `dict` không có:\n\n```\nc.most_common(k)     k phần tử nhiều nhất — ⭐ O(n log k) nhờ HEAP\nc.total()            tổng mọi số đếm (Python 3.10+)\nc1 + c2              cộng số đếm\nc1 - c2              trừ, ⚠️ BỎ các số ≤ 0\nc1 & c2              giao — lấy min từng khoá\nc1 | c2              hợp — lấy max từng khoá\n```\n\n⚠️ Hai cạm bẫy của `Counter`:\n\n```\n① c[\"khoá chưa có\"] trả 0, KHÔNG ném KeyError\n   ⇒ tiện, nhưng che lỗi gõ sai tên khoá\n   ⇒ và KHÔNG tạo khoá (khác defaultdict)\n\n② c1 - c2 BỎ các giá trị ≤ 0\n   Counter({\"a\": 1}) - Counter({\"a\": 3})  →  Counter()  (rỗng!)\n   ⇒ muốn giữ số âm: dùng c1.subtract(c2)\n```\n\n**Vì sao `most_common(k)` là O(n log k)** — điểm đáng chú ý:\n\n```\nCách ngây thơ:  sorted(dem.items(), key=...)[:k]   →  O(n log n)\nCounter:        dùng HEAP kích thước k             →  O(n log k)\n\nVới n = 100.000 khoá và k = 10:\n  O(n log n) = 100.000 × 17 = 1,7 triệu\n  O(n log k) = 100.000 × 3,3 = 330 nghìn\n  ⇒ nhanh hơn khoảng 5 lần\n\n⚠️ Nhưng most_common() KHÔNG có tham số → sắp xếp TOÀN BỘ → O(n log n)\n```\n\n### Mẫu ②: nhóm theo khoá dẫn xuất\n\nBước nhảy tư duy: **khoá không nhất thiết là phần tử.**\n\n```\nBÀI TOÁN: nhóm các từ là đảo chữ của nhau (anagram)\n\n  \"eat\", \"tea\", \"ate\"  →  cùng nhóm\n  \"tan\", \"nat\"          →  cùng nhóm\n\nNGÂY THƠ  O(n² · m):  so từng cặp từ xem có phải anagram không\n\nĐÚNG      O(n · m log m):\n  KHOÁ DẪN XUẤT = các chữ cái ĐÃ SẮP\n     \"eat\" → \"aet\"\n     \"tea\" → \"aet\"    ⭐ CÙNG khoá ⇒ cùng nhóm\n     \"ate\" → \"aet\"\n```\n\n```\nCÔNG THỨC CHUNG CỦA MẪU:\n\n  nhom = defaultdict(list)\n  for x in ds:\n      nhom[khoa_dan_xuat(x)].append(x)\n\n⇒ Vấn đề rút gọn thành: TÌM ĐÚNG HÀM khoá dẫn xuất.\n```\n\nBa ví dụ về khoá dẫn xuất, cho thấy độ tổng quát:\n\n```\nBÀI TOÁN                          KHOÁ DẪN XUẤT\n──────────────────────────        ──────────────────────────\nNhóm anagram                      chữ cái đã sắp\nNhóm đơn nghi trùng               (mã khách, ngày)\nNhóm điểm thẳng hàng qua gốc      hệ số góc rút gọn\nNhóm file trùng nội dung          hash nội dung file\nNhóm bản ghi cùng người           email đã chuẩn hoá\n```\n\n⚠️ Khoá dẫn xuất **phải hashable** — nên thường là `tuple` hoặc `str`, không phải `list`.\n\n```python\n# ❌ list không hashable\nnhom[sorted(tu)].append(tu)          # TypeError\n\n# ✓ chuyển sang tuple hoặc str\nnhom[tuple(sorted(tu))].append(tu)\nnhom[\"\".join(sorted(tu))].append(tu)\n```\n\n### Mẫu ③: tổng tiền tố cộng bảng băm\n\nKỹ thuật mạnh nhất của bài. Ý tưởng nền:\n\n```\nTỔNG TIỀN TỐ:  P[i] = ds[0] + ds[1] + ... + ds[i]\n\n  ds  = [ 3,  4,  7,  2,  5]\n  P   = [ 3,  7, 14, 16, 21]\n\nTỔNG ĐOẠN từ i tới j  =  P[j] − P[i−1]\n\n  tổng ds[1..3] = 4+7+2 = 13\n                = P[3] − P[0] = 16 − 3 = 13   ⭐ khớp\n```\n\n```\n⭐ Ý NGHĨA: bài toán \"tổng ĐOẠN\" biến thành bài toán \"HIỆU hai số\".\n\n  Mà bài toán hiệu hai số CHÍNH LÀ two-sum!\n  ⇒ dùng lại đúng mẫu \"lưu cái đã thấy để tra ngay\".\n```\n\nÁp cho bài \"đếm số dãy con liên tiếp có tổng bằng k\":\n\n```\nCẦN TÌM:  P[j] − P[i−1] = k\nBIẾN ĐỔI: P[i−1] = P[j] − k\n\n⇒ Duyệt j từ trái sang phải, tại mỗi bước hỏi:\n  \"đã thấy tổng tiền tố nào bằng P[j] − k chưa?\"\n\n⇒ ĐÚNG mẫu two-sum, chỉ khác là tra trên TỔNG TIỀN TỐ.\n```\n\n```\nBẬC:  O(n²) ngây thơ  →  O(n) với mẫu này\n```\n\n⚠️ **Chi tiết bắt buộc, hay bị sai:** phải khởi tạo bảng với `{0: 1}`.\n\n```\nVÌ SAO: dãy con bắt đầu từ vị trí 0 có P[i−1] = P[−1] = 0.\n\n  ds = [3], k = 3\n  P[0] = 3, cần tìm P[i−1] = 3 − 3 = 0\n  ⇒ phải có sẵn 0 trong bảng thì mới đếm được dãy [3]\n\n⇒ Quên {0: 1} là bỏ sót MỌI dãy con bắt đầu từ đầu mảng.\n```\n\n### Các biến thể của two-sum\n\nModule 1 đã trình bày two-sum cơ bản. Đây là các biến thể, xếp theo độ khó:\n\n```\nBIẾN THỂ                        KỸ THUẬT                  BẬC\n────────────────────────        ────────────────────      ──────────\nTrả CHỈ SỐ, không phải giá trị  dict{giá trị → chỉ số}    O(n)\nMảng ĐÃ SẮP                     hai con trỏ, O(1) bộ nhớ  O(n)\nĐẾM SỐ CẶP (có trùng lặp)       Counter, cẩn thận x = k−x O(n)\nBa số có tổng = 0               sắp + hai con trỏ         O(n²)\nDãy con liên tiếp tổng = k      tổng tiền tố + hash       O(n)\nDãy con dài nhất tổng = k       tổng tiền tố + dict chỉ số O(n)\n```\n\n⚠️ Biến thể \"đếm số cặp\" có một bẫy tinh vi:\n\n```\nKhi x = k − x (tức k = 2x), cặp (x, x) cần HAI phần tử x.\n\n  ds = [3, 3, 3], k = 6\n  số cặp = C(3,2) = 3, không phải 3×3 = 9\n\n⇒ phải xử lý riêng trường hợp x == k − x:\n    số cặp = dem[x] × (dem[x] − 1) / 2\n```\n\n### Khi nào mẫu này KHÔNG áp dụng\n\nPhản biện cần thiết:\n\n```\nMẪU HASH KHÔNG ÁP DỤNG KHI:\n\n· Cần THỨ TỰ hoặc tìm KHOẢNG      → sắp xếp + hai con trỏ, hoặc cây\n· Khoá KHÔNG hashable              → chuyển sang tuple, hoặc dùng cách khác\n· Bộ nhớ CHẬT                      → sắp xếp tại chỗ (O(1) bộ nhớ, O(n log n))\n· Cần MỌI cặp thoả điều kiện       → không tránh được O(số cặp)\n· Điều kiện là BẤT ĐẲNG THỨC       → hash chỉ tra được BẰNG NHAU\n\n  \"tìm cặp có tổng = k\"     ⭐ hash được\n  \"tìm cặp có tổng < k\"     ⚠️ hash KHÔNG được — cần sắp + hai con trỏ\n  \"tìm cặp có tổng GẦN k\"   ⚠️ hash KHÔNG được\n```\n\nDòng cuối là giới hạn cốt lõi và đáng nhớ: **bảng băm chỉ trả lời câu hỏi \"có đúng giá trị này không\", không trả lời được \"có giá trị nào gần/nhỏ hơn không\".** Đó là hệ quả trực tiếp của việc hàm băm phá vỡ thứ tự (bài 3).\n\n### So sánh ba cách tiếp cận\n\n```\nBÀI TOÁN: tìm cặp có tổng = k\n\n                Thời gian    Bộ nhớ   Cần đã sắp?  Mở rộng cho \"gần k\"?\n                ──────────   ──────   ───────────  ────────────────────\nMọi cặp         O(n²)        O(1)     không        ⭐ được\nSắp + 2 con trỏ O(n log n)   O(1)     tự sắp       ⭐ được\nBảng băm        ⭐ O(n)      O(n)     không        ⚠️ KHÔNG\n```\n\nBảng này là điểm quan trọng nhất của phần phản biện: **hash nhanh nhất nhưng kém linh hoạt nhất.** Nếu bài toán có khả năng đổi thành \"gần k\" hay \"nhỏ hơn k\", cách sắp xếp + hai con trỏ là lựa chọn bền hơn.",

vi_du: "### Mẫu ①: đếm tần suất — bốn cách\n\n```python\nfrom collections import Counter, defaultdict\n\nvan_ban = \"con mèo con chó con gà con mèo\"\ncac_tu = van_ban.split()\n\n# ① Thủ công\ndem1 = {}\nfor tu in cac_tu:\n    dem1[tu] = dem1.get(tu, 0) + 1\n\n# ② defaultdict\ndem2 = defaultdict(int)\nfor tu in cac_tu:\n    dem2[tu] += 1\n\n# ③ Counter\ndem3 = Counter(cac_tu)\n\n# ④ Counter từ generator — không dựng list trung gian\ndem4 = Counter(van_ban.split())\n\nassert dict(dem1) == dict(dem2) == dict(dem3) == dict(dem4)\nprint(dem3)\nprint(f\"top 2: {dem3.most_common(2)}\")\n```\n\n```\nCounter({'con': 4, 'mèo': 2, 'chó': 1, 'gà': 1})\ntop 2: [('con', 4), ('mèo', 2)]\n```\n\n⚠️ Ba cạm bẫy của `Counter` và `defaultdict`:\n\n```python\n# ① Counter trả 0 cho khoá chưa có — KHÔNG ném KeyError\nc = Counter([\"a\", \"b\"])\nprint(c[\"khong-co\"])        # 0\nprint(\"khong-co\" in c)      # False  ⭐ và KHÔNG tạo khoá\nprint(len(c))                # 2\n\n# ⚠️ Tiện, nhưng CHE lỗi gõ sai tên khoá\n\n\n# ② defaultdict TẠO khoá khi chỉ ĐỌC\nd = defaultdict(int)\n_ = d[\"chua-co\"]             # ⚠️ TẠO khoá với giá trị 0\nprint(len(d))                # 1 — khoá đã được thêm!\nprint(dict(d))               # {'chua-co': 0}\n\n# ✓ Đọc mà không tạo\nd2 = defaultdict(int)\n_ = d2.get(\"chua-co\")        # ⭐ trả None, KHÔNG tạo\nprint(len(d2))               # 0\n\n\n# ③ Counter trừ BỎ giá trị ≤ 0\na = Counter({\"x\": 1, \"y\": 5})\nb = Counter({\"x\": 3, \"y\": 2})\nprint(a - b)                 # Counter({'y': 3})  ⚠️ mất 'x'\n\nc2 = Counter({\"x\": 1, \"y\": 5})\nc2.subtract(b)               # ⭐ GIỮ số âm\nprint(c2)                    # Counter({'y': 3, 'x': -2})\n```\n\n### `most_common(k)` dùng heap — đo thật\n\n```python\nimport random\nimport time\nfrom collections import Counter\n\nn_khoa = 200_000\ndem = Counter({f\"khoa_{i}\": random.randint(1, 1000) for i in range(n_khoa)})\nk = 10\n\n# ⭐ most_common(k) — dùng heap kích thước k, O(n log k)\nt0 = time.perf_counter()\ntop_a = dem.most_common(k)\nt_heap = time.perf_counter() - t0\n\n# ⚠️ sorted rồi cắt — sắp xếp TOÀN BỘ, O(n log n)\nt0 = time.perf_counter()\ntop_b = sorted(dem.items(), key=lambda t: -t[1])[:k]\nt_sort = time.perf_counter() - t0\n\nassert [x[1] for x in top_a] == [x[1] for x in top_b]\nprint(f\"most_common({k}) : {t_heap:>7.4f}s   O(n log k)\")\nprint(f\"sorted()[:{k}]    : {t_sort:>7.4f}s   O(n log n)\")\nprint(f\"⇒ nhanh hơn {t_sort/t_heap:.1f} lần\")\n```\n\n```\nmost_common(10) :  ~0.030s   O(n log k)\nsorted()[:10]    :  ~0.150s   O(n log n)\n⇒ nhanh hơn ~5.0 lần\n\n⭐ VÌ SAO: heap kích thước 10 chỉ cần log₂10 ≈ 3,3 phép so sánh\n  mỗi phần tử, thay vì log₂200.000 ≈ 17.\n\n⚠️ NHƯNG most_common() KHÔNG tham số thì sắp xếp TOÀN BỘ:\n```\n\n```python\nt0 = time.perf_counter()\n_ = dem.most_common()            # ⚠️ không có k → O(n log n)\nprint(f\"most_common() không k: {time.perf_counter()-t0:.4f}s\")\n```\n\n### Bẫy `True == 1` khi đếm\n\n```python\nfrom collections import Counter\n\n# ⚠️ Dữ liệu trộn kiểu\ndu_lieu = [1, True, 1.0, 0, False, \"1\"]\nprint(Counter(du_lieu))\n```\n\n```\nCounter({1: 3, 0: 2, '1': 1})\n\n⚠️ 1, True, 1.0 GỘP thành một khoá (đếm 3)\n⚠️ 0, False GỘP thành một khoá (đếm 2)\n\n⇒ Vì 1 == True == 1.0 nên chúng CÙNG hash và CÙNG khoá (bài 1).\n```\n\n```python\n# ✓ Cần phân biệt kiểu: đưa kiểu vào khoá dẫn xuất\ndem_kieu = Counter((type(x).__name__, x) for x in du_lieu)\nfor (kieu, gt), so in sorted(dem_kieu.items(), key=str):\n    print(f\"  {kieu:>5} {gt!r:>6} → {so}\")\n```\n\n### Mẫu ②: nhóm theo khoá dẫn xuất\n\n```python\nfrom collections import defaultdict\n\n\ndef nhom_anagram(cac_tu: list[str]) -> list[list[str]]:\n    \"\"\"Nhóm các từ là đảo chữ của nhau.\n\n    O(n · m log m) với m là độ dài từ trung bình.\n    Khoá dẫn xuất: các chữ cái ĐÃ SẮP.\n    \"\"\"\n    nhom: dict[str, list[str]] = defaultdict(list)\n    for tu in cac_tu:\n        khoa = \"\".join(sorted(tu))          # ⭐ KHOÁ DẪN XUẤT\n        nhom[khoa].append(tu)\n    return list(nhom.values())\n\n\nkq = nhom_anagram([\"eat\", \"tea\", \"tan\", \"ate\", \"nat\", \"bat\"])\nkq_sap = sorted([sorted(g) for g in kq])\nassert kq_sap == [[\"ate\", \"eat\", \"tea\"], [\"bat\"], [\"nat\", \"tan\"]]\n\n# ---- Ca biên ----\nassert nhom_anagram([]) == []\nassert nhom_anagram([\"a\"]) == [[\"a\"]]\nassert sorted(nhom_anagram([\"\", \"\"])[0]) == [\"\", \"\"]      # ⭐ chuỗi rỗng\nprint(\"✓ nhóm anagram: mọi ca kiểm thử đạt\")\n```\n\nBiến thể khoá dẫn xuất — đếm chữ cái thay vì sắp xếp:\n\n```python\ndef nhom_anagram_v2(cac_tu: list[str]) -> list[list[str]]:\n    \"\"\"O(n · m) — không cần sắp xếp, dùng vector đếm 26 chữ cái.\n\n    Chỉ đúng với chữ cái a–z. Nhanh hơn khi từ DÀI.\n    \"\"\"\n    nhom: dict[tuple, list[str]] = defaultdict(list)\n    for tu in cac_tu:\n        dem = [0] * 26\n        for c in tu:\n            dem[ord(c) - ord(\"a\")] += 1\n        nhom[tuple(dem)].append(tu)          # ⭐ tuple để hashable\n    return list(nhom.values())\n\n\na = sorted([sorted(g) for g in nhom_anagram([\"eat\", \"tea\", \"tan\", \"nat\"])])\nb = sorted([sorted(g) for g in nhom_anagram_v2([\"eat\", \"tea\", \"tan\", \"nat\"])])\nassert a == b\n```\n\n```\nSO SÁNH:\n  sắp xếp:   O(m log m) mỗi từ — đơn giản, dùng được mọi ký tự\n  đếm 26 ô:  O(m) mỗi từ       — nhanh hơn khi từ DÀI, nhưng chỉ a–z\n\n⚠️ Phải dùng tuple(dem), không phải dem — list KHÔNG hashable (bài 1).\n```\n\nÁp mẫu này vào việc thật — bài tìm đơn trùng lặp ở module 1:\n\n```python\nfrom collections import defaultdict\nfrom dataclasses import dataclass\n\n\n@dataclass(frozen=True)\nclass DonHang:\n    ma: str\n    ma_khach: str\n    ngay: str\n    tong: float\n\n\ndef tim_don_nghi_trung(don: list[DonHang],\n                       nguong_lech: float = 1000) -> list[tuple[str, str]]:\n    \"\"\"Tìm cặp đơn nghi trùng: cùng khách, cùng ngày, lệch tiền dưới ngưỡng.\n\n    Khoá dẫn xuất: (mã khách, ngày).\n    O(n + tổng k²) với k là kích thước nhóm.\n    Thực tế k rất nhỏ ⇒ gần O(n). Xấu nhất (mọi đơn cùng nhóm) vẫn O(n²).\n    \"\"\"\n    nhom: dict[tuple[str, str], list[DonHang]] = defaultdict(list)\n    for d in don:\n        nhom[(d.ma_khach, d.ngay)].append(d)      # ⭐ KHOÁ DẪN XUẤT\n\n    kq = []\n    for cac_don in nhom.values():\n        for i in range(len(cac_don)):\n            for j in range(i + 1, len(cac_don)):   # ⭐ chỉ so TRONG nhóm\n                a, b = cac_don[i], cac_don[j]\n                if abs(a.tong - b.tong) < nguong_lech:\n                    kq.append((a.ma, b.ma))\n    return kq\n\n\ndon = [\n    DonHang(\"D1\", \"K1\", \"2026-01-01\", 100_000),\n    DonHang(\"D2\", \"K1\", \"2026-01-01\", 100_500),   # ⭐ nghi trùng với D1\n    DonHang(\"D3\", \"K1\", \"2026-01-02\", 100_000),   # khác ngày\n    DonHang(\"D4\", \"K2\", \"2026-01-01\", 100_000),   # khác khách\n]\nassert tim_don_nghi_trung(don) == [(\"D1\", \"D2\")]\nassert tim_don_nghi_trung([]) == []                # biên\nassert tim_don_nghi_trung([don[0]]) == []          # biên: một đơn\n```\n\nĐo để thấy khác biệt bậc:\n\n```python\nimport random\nimport time\n\n\ndef tim_ngay_tho(don, nguong=1000):\n    \"\"\"O(n²) — so MỌI cặp.\"\"\"\n    kq = []\n    for i in range(len(don)):\n        for j in range(i + 1, len(don)):\n            a, b = don[i], don[j]\n            if (a.ma_khach == b.ma_khach and a.ngay == b.ngay\n                    and abs(a.tong - b.tong) < nguong):\n                kq.append((a.ma, b.ma))\n    return kq\n\n\nfor n in (2_000, 4_000):\n    du_lieu = [\n        DonHang(f\"D{i}\", f\"K{random.randint(0, n // 3)}\",\n                f\"2026-01-{random.randint(1, 28):02d}\",\n                random.randint(50_000, 200_000))\n        for i in range(n)\n    ]\n    t0 = time.perf_counter()\n    a = tim_ngay_tho(du_lieu)\n    t1 = time.perf_counter() - t0\n\n    t0 = time.perf_counter()\n    b = tim_don_nghi_trung(du_lieu)\n    t2 = time.perf_counter() - t0\n\n    assert sorted(a) == sorted(b)\n    print(f\"n={n:>6,}  mọi cặp {t1:>7.4f}s   nhóm trước {t2:>7.4f}s   \"\n          f\"nhanh hơn {t1/t2:>5.0f}×\")\n```\n\n```\nn= 2,000  mọi cặp  ~0.400s   nhóm trước  ~0.004s   nhanh hơn  ~100×\nn= 4,000  mọi cặp  ~1.600s   nhóm trước  ~0.008s   nhanh hơn  ~200×\n\n⭐ Tỉ lệ TĂNG GẤP ĐÔI khi n gấp đôi ⇒ hai bậc khác nhau.\n⚠️ Nhưng bậc XẤU NHẤT vẫn là O(n²) — nếu mọi đơn cùng khách cùng ngày.\n  Phải nói rõ cả hai (đúng như module 1 đã nhấn mạnh).\n```\n\n### Mẫu ③: tổng tiền tố cộng bảng băm\n\n```python\nfrom collections import defaultdict\n\n\ndef dem_day_con_tong_k(ds: list[int], k: int) -> int:\n    \"\"\"Đếm số dãy con LIÊN TIẾP có tổng bằng k. O(n) thời gian, O(n) bộ nhớ.\n\n    Ý tưởng: tổng đoạn (i..j) = P[j] − P[i−1].\n    Cần P[j] − P[i−1] = k  ⇔  P[i−1] = P[j] − k\n    ⇒ đúng mẫu two-sum, tra trên TỔNG TIỀN TỐ.\n    \"\"\"\n    dem_tong = defaultdict(int)\n    dem_tong[0] = 1                    # ⭐⭐ BẮT BUỘC: dãy bắt đầu từ vị trí 0\n    tong_chay = 0\n    ket_qua = 0\n    for x in ds:\n        tong_chay += x\n        ket_qua += dem_tong[tong_chay - k]   # ⭐ đã thấy tổng này mấy lần?\n        dem_tong[tong_chay] += 1\n    return ket_qua\n\n\nassert dem_day_con_tong_k([1, 1, 1], 2) == 2          # [1,1] ở hai vị trí\nassert dem_day_con_tong_k([1, 2, 3], 3) == 2          # [1,2] và [3]\nassert dem_day_con_tong_k([3], 3) == 1                # ⭐ cần {0: 1}\nassert dem_day_con_tong_k([1, -1, 0], 0) == 3         # ⭐ SỐ ÂM và số 0\nassert dem_day_con_tong_k([], 0) == 0                 # biên\nassert dem_day_con_tong_k([1, 2], 100) == 0           # không có\nprint(\"✓ đếm dãy con tổng k: mọi ca kiểm thử đạt\")\n```\n\n⚠️ Chứng minh vì sao `{0: 1}` là bắt buộc:\n\n```python\ndef thieu_khoi_tao(ds: list[int], k: int) -> int:\n    \"\"\"❌ Quên dem_tong[0] = 1.\"\"\"\n    dem_tong = defaultdict(int)\n    tong_chay = 0\n    ket_qua = 0\n    for x in ds:\n        tong_chay += x\n        ket_qua += dem_tong[tong_chay - k]\n        dem_tong[tong_chay] += 1\n    return ket_qua\n\n\nprint(f\"đúng : {dem_day_con_tong_k([3], 3)}\")     # 1\nprint(f\"thiếu: {thieu_khoi_tao([3], 3)}\")          # ⚠️ 0 — BỎ SÓT\n\nprint(f\"đúng : {dem_day_con_tong_k([1, 2, 3], 6)}\")  # 1  — cả dãy\nprint(f\"thiếu: {thieu_khoi_tao([1, 2, 3], 6)}\")      # ⚠️ 0\n```\n\n```\n⚠️ Quên {0: 1} bỏ sót MỌI dãy con bắt đầu từ đầu mảng.\n\n  Lý do: dãy (0..j) có tổng = P[j] − P[−1] = P[j] − 0\n  ⇒ phải có sẵn giá trị 0 trong bảng.\n\n⇒ Đây là lỗi phổ biến nhất của mẫu này, và test [3] với k=3 bắt được.\n```\n\nBiến thể — **dãy con dài nhất** có tổng bằng k:\n\n```python\ndef day_con_dai_nhat_tong_k(ds: list[int], k: int) -> int:\n    \"\"\"Độ dài dãy con LIÊN TIẾP dài nhất có tổng = k. O(n).\n\n    Khác bài đếm: lưu CHỈ SỐ ĐẦU TIÊN thấy mỗi tổng,\n    vì muốn dãy DÀI NHẤT thì cần điểm bắt đầu SỚM NHẤT.\n    \"\"\"\n    lan_dau: dict[int, int] = {0: -1}     # ⭐ tổng 0 xuất hiện ở \"vị trí −1\"\n    tong_chay = 0\n    dai_nhat = 0\n    for i, x in enumerate(ds):\n        tong_chay += x\n        if tong_chay - k in lan_dau:\n            dai_nhat = max(dai_nhat, i - lan_dau[tong_chay - k])\n        if tong_chay not in lan_dau:      # ⭐ CHỈ ghi lần ĐẦU\n            lan_dau[tong_chay] = i\n    return dai_nhat\n\n\nassert day_con_dai_nhat_tong_k([1, -1, 5, -2, 3], 3) == 4   # [1,-1,5,-2]\nassert day_con_dai_nhat_tong_k([-2, -1, 2, 1], 1) == 2      # [-1,2]\nassert day_con_dai_nhat_tong_k([1, 2, 3], 100) == 0         # không có\nassert day_con_dai_nhat_tong_k([], 0) == 0                   # biên\nassert day_con_dai_nhat_tong_k([5], 5) == 1                  # ⭐ cần {0: -1}\n```\n\n```\n⭐ HAI CHI TIẾT KHÁC với bài đếm:\n\n  ① {0: −1} thay vì {0: 1}\n     — lưu CHỈ SỐ, và \"trước phần tử đầu\" là vị trí −1\n\n  ② CHỈ ghi lần ĐẦU tiên thấy mỗi tổng\n     — ghi đè sẽ làm dãy NGẮN đi\n     — đây là khác biệt then chốt giữa \"đếm\" và \"dài nhất\"\n```\n\nBiến thể ba — chia hết cho k:\n\n```python\ndef co_day_con_chia_het_k(ds: list[int], k: int) -> bool:\n    \"\"\"Có dãy con liên tiếp (độ dài ≥ 2) có tổng chia hết cho k không? O(n).\n\n    Ý tưởng: hai tổng tiền tố CÙNG SỐ DƯ khi chia k\n    ⇒ hiệu của chúng chia hết cho k.\n    \"\"\"\n    if k == 0:\n        raise ValueError(\"k phải khác 0\")\n    lan_dau = {0: -1}\n    tong = 0\n    for i, x in enumerate(ds):\n        tong += x\n        du = tong % k                     # ⭐ khoá là SỐ DƯ, không phải tổng\n        if du in lan_dau:\n            if i - lan_dau[du] >= 2:      # ⭐ độ dài ≥ 2\n                return True\n        else:\n            lan_dau[du] = i\n    return False\n\n\nassert co_day_con_chia_het_k([23, 2, 4, 6, 7], 6) is True    # [2,4]\nassert co_day_con_chia_het_k([23, 2, 6, 4, 7], 13) is True   # cả dãy\nassert co_day_con_chia_het_k([1, 2, 3], 100) is False\nassert co_day_con_chia_het_k([1], 2) is False                 # biên: quá ngắn\n```\n\n```\n⭐ BƯỚC NHẢY TƯ DUY: khoá không phải TỔNG, mà là SỐ DƯ của tổng.\n\n⇒ Cùng mẫu \"lưu cái đã thấy để tra ngay\", chỉ đổi hàm khoá dẫn xuất.\n⇒ Đây là chỗ ba mẫu của bài GẶP NHAU.\n```\n\n### Các biến thể của two-sum\n\nModule 1 đã trình bày bản cơ bản; đây là các biến thể:\n\n```python\ndef two_sum_chi_so(ds: list[int], k: int) -> tuple[int, int] | None:\n    \"\"\"Trả CHỈ SỐ của cặp, không phải giá trị. O(n).\"\"\"\n    da_thay: dict[int, int] = {}         # ⭐ giá trị → chỉ số\n    for i, x in enumerate(ds):\n        if k - x in da_thay:\n            return (da_thay[k - x], i)\n        da_thay[x] = i                    # ⭐ lưu SAU khi tra\n    return None\n\n\nassert two_sum_chi_so([2, 7, 11, 15], 9) == (0, 1)\nassert two_sum_chi_so([3, 3], 6) == (0, 1)          # ⭐ hai phần tử bằng nhau\nassert two_sum_chi_so([3], 6) is None                # ⭐ KHÔNG dùng lại chính nó\nassert two_sum_chi_so([], 0) is None                 # biên\n```\n\n```\n⭐ Thứ tự \"tra TRƯỚC, lưu SAU\" là chi tiết bắt buộc:\n  lưu trước thì [3] với k=6 sẽ tự khớp với chính nó.\n```\n\n```python\nfrom collections import Counter\n\n\ndef dem_cap_tong_k(ds: list[int], k: int) -> int:\n    \"\"\"ĐẾM SỐ CẶP (i < j) có ds[i] + ds[j] = k. O(n).\n\n    ⚠️ Cẩn thận trường hợp x == k − x.\n    \"\"\"\n    dem = Counter(ds)\n    tong = 0\n    for x, so_luong in dem.items():\n        bo_tro = k - x\n        if bo_tro not in dem:\n            continue\n        if x < bo_tro:\n            tong += so_luong * dem[bo_tro]           # ⭐ mỗi cặp đếm MỘT lần\n        elif x == bo_tro:\n            tong += so_luong * (so_luong - 1) // 2   # ⭐ chọn 2 từ so_luong\n    return tong\n\n\nassert dem_cap_tong_k([1, 5, 7, -1], 6) == 2         # (1,5), (7,-1)\nassert dem_cap_tong_k([3, 3, 3], 6) == 3             # ⭐ C(3,2) = 3\nassert dem_cap_tong_k([1, 1, 1, 1], 2) == 6          # ⭐ C(4,2) = 6\nassert dem_cap_tong_k([], 0) == 0                     # biên\nassert dem_cap_tong_k([3], 6) == 0                    # ⭐ cần HAI phần tử\n```\n\n```\n⚠️ BẪY: khi x == k − x (tức k = 2x), số cặp là C(m,2) = m(m−1)/2,\n  KHÔNG phải m × m.\n\n  [3,3,3] với k=6:  C(3,2) = 3 cặp, không phải 9.\n\n⇒ Đây là ca kiểm thử bắt buộc cho bài này.\n```\n\n**Ba số có tổng bằng 0** — nơi hash **không** đủ:\n\n```python\ndef ba_so_tong_0(ds: list[int]) -> list[tuple[int, int, int]]:\n    \"\"\"Mọi bộ ba KHÁC NHAU có tổng = 0. O(n²) thời gian, O(1) bộ nhớ thêm.\n\n    Dùng SẮP XẾP + hai con trỏ, không dùng hash —\n    vì cần LOẠI TRÙNG một cách xác định.\n    \"\"\"\n    a = sorted(ds)\n    kq: list[tuple[int, int, int]] = []\n    n = len(a)\n    for i in range(n - 2):\n        if i > 0 and a[i] == a[i - 1]:\n            continue                              # ⭐ bỏ qua trùng ở vị trí i\n        if a[i] > 0:\n            break                                  # ⭐ đã dương thì không thể tổng 0\n        trai, phai = i + 1, n - 1\n        while trai < phai:\n            tong = a[i] + a[trai] + a[phai]\n            if tong == 0:\n                kq.append((a[i], a[trai], a[phai]))\n                trai += 1\n                phai -= 1\n                while trai < phai and a[trai] == a[trai - 1]:\n                    trai += 1                      # ⭐ bỏ trùng\n                while trai < phai and a[phai] == a[phai + 1]:\n                    phai -= 1\n            elif tong < 0:\n                trai += 1\n            else:\n                phai -= 1\n    return kq\n\n\nassert ba_so_tong_0([-1, 0, 1, 2, -1, -4]) == [(-1, -1, 2), (-1, 0, 1)]\nassert ba_so_tong_0([0, 0, 0]) == [(0, 0, 0)]        # ⭐ ba số 0\nassert ba_so_tong_0([0, 0, 0, 0]) == [(0, 0, 0)]     # ⭐ vẫn MỘT bộ\nassert ba_so_tong_0([]) == []                         # biên\nassert ba_so_tong_0([1, 2]) == []                     # biên: dưới 3 phần tử\nprint(\"✓ ba số tổng 0: mọi ca kiểm thử đạt\")\n```\n\n```\n⭐ VÌ SAO KHÔNG DÙNG HASH Ở ĐÂY:\n\n  Dùng hash cũng ra O(n²), nhưng LOẠI TRÙNG rất phiền —\n  phải dùng set các bộ ba đã sắp, tốn thêm bộ nhớ và code rối.\n\n  Sắp xếp trước làm việc loại trùng thành TỰ NHIÊN:\n  chỉ cần bỏ qua các giá trị bằng phần tử trước.\n\n⇒ Hash không phải luôn là câu trả lời — đây là ví dụ cụ thể.\n```\n\n### Khi hash KHÔNG áp dụng được\n\n```python\nimport bisect\n\n\ndef cap_tong_bang_k(ds: list[int], k: int) -> tuple[int, int] | None:\n    \"\"\"Tổng ĐÚNG BẰNG k — hash làm được, O(n).\"\"\"\n    da_thay = set()\n    for x in ds:\n        if k - x in da_thay:\n            return (k - x, x)\n        da_thay.add(x)\n    return None\n\n\ndef cap_tong_gan_k(ds: list[int], k: int) -> tuple[int, int] | None:\n    \"\"\"Tổng GẦN k NHẤT — hash KHÔNG làm được.\n\n    Phải sắp xếp + hai con trỏ. O(n log n).\n    \"\"\"\n    a = sorted(ds)\n    trai, phai = 0, len(a) - 1\n    tot_nhat = None\n    lech_nho_nhat = float(\"inf\")\n    while trai < phai:\n        tong = a[trai] + a[phai]\n        lech = abs(tong - k)\n        if lech < lech_nho_nhat:\n            lech_nho_nhat = lech\n            tot_nhat = (a[trai], a[phai])\n        if tong < k:\n            trai += 1                     # ⭐ cần LỚN hơn\n        elif tong > k:\n            phai -= 1                     # ⭐ cần NHỎ hơn\n        else:\n            break                          # đúng bằng k\n    return tot_nhat\n\n\nassert cap_tong_bang_k([1, 3, 5, 8], 11) == (3, 8)\nassert cap_tong_bang_k([1, 3, 5, 8], 100) is None\n\nassert cap_tong_gan_k([1, 3, 5, 8], 11) == (3, 8)     # đúng bằng\nassert cap_tong_gan_k([1, 3, 5, 8], 12) == (5, 8)     # ⭐ gần nhất: 13\nassert cap_tong_gan_k([1, 2], 100) == (1, 2)          # biên\n```\n\n```\n⭐ GIỚI HẠN CỐT LÕI CỦA BẢNG BĂM:\n\n  Nó trả lời được:  \"có ĐÚNG giá trị này không?\"\n  Nó KHÔNG trả lời: \"có giá trị nào GẦN/NHỎ HƠN không?\"\n\n⇒ Hệ quả trực tiếp của việc hàm băm PHÁ VỠ THỨ TỰ (bài 3).\n\n⇒ Nếu bài toán có khả năng đổi thành \"gần k\" hay \"nhỏ hơn k\",\n  cách sắp xếp + hai con trỏ là lựa chọn BỀN HƠN.\n```\n\n### So sánh ba cách tiếp cận\n\n```python\nimport random\nimport time\n\n\ndef cap_moi_cap(ds, k):\n    \"\"\"O(n²), O(1) bộ nhớ.\"\"\"\n    for i in range(len(ds)):\n        for j in range(i + 1, len(ds)):\n            if ds[i] + ds[j] == k:\n                return (ds[i], ds[j])\n    return None\n\n\ndef cap_sap_hai_con_tro(ds, k):\n    \"\"\"O(n log n), O(n) cho bản sao đã sắp.\"\"\"\n    a = sorted(ds)\n    trai, phai = 0, len(a) - 1\n    while trai < phai:\n        t = a[trai] + a[phai]\n        if t == k:\n            return (a[trai], a[phai])\n        if t < k:\n            trai += 1\n        else:\n            phai -= 1\n    return None\n\n\nn = 20_000\nds = random.sample(range(n * 10), n)\nk = -1                                    # ⭐ XẤU NHẤT: không có cặp nào\n\nfor ten, ham in ((\"mọi cặp O(n²)\", cap_moi_cap),\n                 (\"sắp + 2 con trỏ O(n log n)\", cap_sap_hai_con_tro),\n                 (\"bảng băm O(n)\", cap_tong_bang_k)):\n    t0 = time.perf_counter()\n    ham(ds, k)\n    print(f\"{ten:>28}: {time.perf_counter()-t0:>8.4f}s\")\n```\n\n```\nXU HƯỚNG dự kiến (n = 20.000, không có cặp nào):\n\n               mọi cặp O(n²):  ~9.0000s\n  sắp + 2 con trỏ O(n log n):  ~0.0080s\n               bảng băm O(n):  ~0.0030s   ⭐ nhanh nhất\n\n⭐ NHƯNG bảng so sánh đầy đủ:\n\n                    Thời gian   Bộ nhớ  Cần đã sắp  Mở rộng \"gần k\"\n  mọi cặp           O(n²)       O(1)    không       ⭐ được\n  sắp + 2 con trỏ   O(n log n)  O(n)    tự sắp      ⭐ được\n  bảng băm          ⭐ O(n)     O(n)    không       ⚠️ KHÔNG\n\n⇒ Hash NHANH NHẤT nhưng KÉM LINH HOẠT NHẤT.\n```\n\n### Ứng dụng thật: phân tích log\n\nKết hợp cả ba mẫu:\n\n```python\nfrom collections import Counter, defaultdict\nfrom dataclasses import dataclass\n\n\n@dataclass(frozen=True)\nclass BanGhiLog:\n    thoi_gian: int\n    duong_dan: str\n    ma_trang_thai: int\n    do_tre_ms: int\n\n\ndef phan_tich_log(log: list[BanGhiLog]) -> dict:\n    \"\"\"Phân tích log bằng ba mẫu của bài. Một lượt duyệt, O(n).\"\"\"\n\n    # ── MẪU ①: đếm tần suất ──\n    dem_trang_thai = Counter(b.ma_trang_thai for b in log)\n    dem_duong_dan = Counter(b.duong_dan for b in log)\n\n    # ── MẪU ②: nhóm theo khoá dẫn xuất ──\n    # khoá dẫn xuất: (đường dẫn, nhóm mã trạng thái)\n    theo_duong_dan: dict[str, list[int]] = defaultdict(list)\n    loi_theo_duong_dan: Counter = Counter()\n    for b in log:\n        theo_duong_dan[b.duong_dan].append(b.do_tre_ms)\n        if b.ma_trang_thai >= 500:                    # ⭐ khoá dẫn xuất\n            loi_theo_duong_dan[b.duong_dan] += 1\n\n    do_tre_tb = {\n        dd: round(sum(ds) / len(ds), 1)\n        for dd, ds in theo_duong_dan.items()\n    }\n\n    return {\n        \"tổng bản ghi\": len(log),\n        \"theo trạng thái\": dict(dem_trang_thai.most_common()),\n        \"top 3 đường dẫn\": dem_duong_dan.most_common(3),   # ⭐ O(n log 3)\n        \"độ trễ TB\": do_tre_tb,\n        \"lỗi 5xx nhiều nhất\": loi_theo_duong_dan.most_common(2),\n    }\n\n\nlog = [\n    BanGhiLog(1, \"/api/don\", 200, 45),\n    BanGhiLog(2, \"/api/don\", 500, 3000),\n    BanGhiLog(3, \"/api/khach\", 200, 30),\n    BanGhiLog(4, \"/api/don\", 200, 50),\n    BanGhiLog(5, \"/api/don\", 503, 5000),\n]\nkq = phan_tich_log(log)\nfor khoa, gia_tri in kq.items():\n    print(f\"  {khoa:>20}: {gia_tri}\")\n\nassert phan_tich_log([])[\"tổng bản ghi\"] == 0          # biên\n```\n\n```\n          tổng bản ghi: 5\n       theo trạng thái: {200: 3, 500: 1, 503: 1}\n       top 3 đường dẫn: [('/api/don', 4), ('/api/khach', 1)]\n             độ trễ TB: {'/api/don': 1023.8, '/api/khach': 30.0}\n    lỗi 5xx nhiều nhất: [('/api/don', 2)]\n\n⭐ MỘT lượt duyệt, O(n), giải được năm câu hỏi khác nhau.\n  Cách ngây thơ sẽ duyệt lại năm lần, hoặc dùng vòng lặp lồng nhau.\n```\n\n### Bảng nhận dạng mẫu\n\n```python\nNHAN_DANG = [\n    (\"đếm số lần xuất hiện\",              \"Counter\"),\n    (\"top k phần tử nhiều nhất\",          \"Counter.most_common(k) — O(n log k)\"),\n    (\"nhóm các phần tử 'giống nhau'\",     \"defaultdict(list) + khoá dẫn xuất\"),\n    (\"tìm cặp có tổng = k\",               \"set/dict — lưu cái đã thấy\"),\n    (\"đếm số cặp có tổng = k\",            \"Counter, cẩn thận x == k−x\"),\n    (\"dãy con liên tiếp có tổng = k\",     \"tổng tiền tố + hash, khởi tạo {0:1}\"),\n    (\"dãy con dài nhất có tổng = k\",      \"tổng tiền tố + dict chỉ số, {0:-1}\"),\n    (\"dãy con chia hết cho k\",            \"tổng tiền tố + SỐ DƯ làm khoá\"),\n    (\"tìm cặp có tổng GẦN k\",             \"⚠️ sắp + hai con trỏ (hash KHÔNG được)\"),\n    (\"ba số có tổng = 0\",                 \"⚠️ sắp + hai con trỏ (loại trùng dễ hơn)\"),\n]\n\nfor dau_hieu, ky_thuat in NHAN_DANG:\n    print(f\"{dau_hieu:>34}  →  {ky_thuat}\")\n```\n\n```\n              đếm số lần xuất hiện  →  Counter\n           top k phần tử nhiều nhất  →  Counter.most_common(k) — O(n log k)\n     nhóm các phần tử 'giống nhau'  →  defaultdict(list) + khoá dẫn xuất\n              tìm cặp có tổng = k  →  set/dict — lưu cái đã thấy\n          đếm số cặp có tổng = k  →  Counter, cẩn thận x == k−x\n   dãy con liên tiếp có tổng = k  →  tổng tiền tố + hash, khởi tạo {0:1}\n     dãy con dài nhất có tổng = k  →  tổng tiền tố + dict chỉ số, {0:-1}\n           dãy con chia hết cho k  →  tổng tiền tố + SỐ DƯ làm khoá\n           tìm cặp có tổng GẦN k  →  ⚠️ sắp + hai con trỏ (hash KHÔNG được)\n               ba số có tổng = 0  →  ⚠️ sắp + hai con trỏ (loại trùng dễ hơn)\n\n⭐ DẤU HIỆU CHUNG: thấy hai vòng lặp lồng nhau so MỌI CẶP\n  ⇒ hỏi \"tôi có thể LƯU cái đã thấy để TRA NGAY không?\"\n```",

so_sanh: "### Ba mẫu của bài\n\n| Mẫu | Khoá là gì | Bài toán điển hình |\n|---|---|---|\n| **① Đếm tần suất** | chính phần tử | đếm từ, đếm lỗi theo loại |\n| **② Nhóm theo khoá dẫn xuất** | **một hàm của phần tử** | anagram, đơn nghi trùng |\n| **③ Tổng tiền tố + hash** | **một đại lượng tích luỹ** | dãy con có tổng = k |\n\nĐiểm chung: **đổi một vòng lặp lồng nhau lấy một bảng băm** — O(n²) → O(n), trả bằng O(n) bộ nhớ.\n\n### Ba cách đếm tần suất\n\n| | Thủ công | `defaultdict(int)` | **`Counter`** |\n|---|---|---|---|\n| Độ dài | 3 dòng | 3 dòng | **⭐ 1 dòng** |\n| Đọc khoá thiếu | `KeyError` | **⚠️ TẠO khoá** | trả 0, **không tạo** |\n| `most_common` | ✗ | ✗ | **⭐ ✓** |\n| Phép cộng/trừ Counter | ✗ | ✗ | ✓ |\n\n### Cạm bẫy `Counter` và `defaultdict`\n\n| Cạm bẫy | Hậu quả | Cách tránh |\n|---|---|---|\n| `defaultdict` **tạo khoá khi đọc** | `len()` tăng ngoài ý muốn | dùng `.get(k)` hoặc `k in d` |\n| `Counter[k]` trả 0 | che lỗi gõ sai tên khoá | kiểm `k in c` khi cần |\n| **`c1 - c2` bỏ giá trị ≤ 0** | mất khoá âm | dùng `c1.subtract(c2)` |\n| `True == 1 == 1.0` | ba giá trị gộp một khoá | đưa `type(x)` vào khoá |\n\n### `most_common(k)` — vì sao nhanh hơn\n\n| | `most_common(k)` | `sorted(...)[:k]` | `most_common()` |\n|---|---|---|---|\n| Bậc | **⭐ O(n log k)** | O(n log n) | O(n log n) |\n| Cấu trúc dùng | **heap kích thước k** | sắp xếp toàn bộ | sắp xếp toàn bộ |\n| n = 200k, k = 10 | ~0,03s | ~0,15s | ~0,15s |\n\n⚠️ `most_common()` **không tham số** thì sắp xếp toàn bộ — mất hết lợi thế.\n\n### Khoá dẫn xuất — các ví dụ\n\n| Bài toán | Khoá dẫn xuất |\n|---|---|\n| Nhóm anagram | chữ cái **đã sắp**, hoặc vector đếm 26 ô |\n| Đơn nghi trùng | `(mã khách, ngày)` |\n| Điểm thẳng hàng qua gốc | hệ số góc rút gọn |\n| File trùng nội dung | hash nội dung |\n| Dãy con chia hết cho k | **số dư của tổng tiền tố** |\n\n⚠️ Khoá dẫn xuất phải **hashable** ⇒ `tuple` hoặc `str`, không phải `list`.\n\n### Hai biến thể anagram\n\n| | Sắp chữ cái | Vector đếm 26 ô |\n|---|---|---|\n| Bậc mỗi từ | O(m log m) | **⭐ O(m)** |\n| Ký tự hỗ trợ | **⭐ mọi ký tự** | chỉ a–z |\n| Nhanh hơn khi | từ ngắn | **từ dài** |\n\n### Tổng tiền tố — ba biến thể\n\n| Bài toán | Bảng lưu gì | Khởi tạo | Chi tiết then chốt |\n|---|---|---|---|\n| **Đếm** dãy con tổng = k | tổng → **số lần** | `{0: 1}` | cộng dồn số lần |\n| **Dài nhất** tổng = k | tổng → **chỉ số đầu tiên** | `{0: -1}` | **chỉ ghi lần đầu** |\n| Chia hết cho k | **số dư** → chỉ số đầu | `{0: -1}` | khoá là số dư |\n\n⚠️ **Quên khởi tạo là lỗi phổ biến nhất** — bỏ sót mọi dãy con bắt đầu từ đầu mảng. Test `[3]` với `k=3` bắt được.\n\n### Các biến thể two-sum\n\n| Biến thể | Kỹ thuật | Bậc | Bẫy |\n|---|---|---|---|\n| Trả chỉ số | `dict{giá trị → chỉ số}` | O(n) | **tra trước, lưu sau** |\n| Mảng đã sắp | hai con trỏ | O(n) | O(1) bộ nhớ |\n| **Đếm số cặp** | `Counter` | O(n) | **⚠️ `x == k−x` ⇒ C(m,2)** |\n| Ba số tổng 0 | **sắp + hai con trỏ** | O(n²) | loại trùng |\n\n### Khi hash KHÔNG áp dụng được\n\n| Câu hỏi | Hash được? | Dùng gì |\n|---|---|---|\n| \"tổng **đúng bằng** k\" | **⭐ ✓** | `set`/`dict` |\n| \"tổng **nhỏ hơn** k\" | **⚠️ ✗** | sắp + hai con trỏ |\n| \"tổng **gần** k nhất\" | **⚠️ ✗** | sắp + hai con trỏ |\n| \"mọi bộ ba tổng 0\" | được nhưng rối | sắp + hai con trỏ |\n\n**Bảng băm chỉ trả lời \"có đúng giá trị này không\"** — hệ quả của việc hàm băm phá vỡ thứ tự (bài 3).\n\n### Ba cách tìm cặp có tổng = k\n\n| | Mọi cặp | Sắp + 2 con trỏ | **Bảng băm** |\n|---|---|---|---|\n| Thời gian | O(n²) | O(n log n) | **⭐ O(n)** |\n| Bộ nhớ | **⭐ O(1)** | O(n) | O(n) |\n| Cần đã sắp | không | tự sắp | không |\n| Mở rộng cho \"gần k\" | **⭐ được** | **⭐ được** | **⚠️ KHÔNG** |\n\n**Hash nhanh nhất nhưng kém linh hoạt nhất** — cân nhắc khi yêu cầu có thể đổi.\n\n### Bảng nhận dạng\n\n| Dấu hiệu | Kỹ thuật |\n|---|---|\n| **hai vòng lặp so mọi cặp** | **⭐ hỏi: lưu cái đã thấy được không?** |\n| \"đếm số lần\" | `Counter` |\n| \"top k\" | `most_common(k)` |\n| \"nhóm các phần tử giống nhau\" | `defaultdict(list)` + khoá dẫn xuất |\n| \"dãy con **liên tiếp**\" + tổng | **tổng tiền tố + hash** |\n| \"gần nhất\", \"nhỏ hơn\" | ⚠️ **không** dùng hash |",

loi_thuong_gap: [
  "**Quên khởi tạo `{0: 1}` trong bài tổng tiền tố.** Bỏ sót mọi dãy con bắt đầu từ đầu mảng — lỗi phổ biến nhất của mẫu này.",
  "**Dùng `{0: 1}` cho bài \"dài nhất\".** Bài đếm lưu số lần, bài dài nhất lưu **chỉ số** và khởi tạo `{0: -1}`.",
  "**Ghi đè chỉ số trong bài \"dài nhất\".** Phải **chỉ ghi lần đầu** thấy mỗi tổng, nếu không dãy sẽ ngắn đi.",
  "**Lưu trước khi tra trong two-sum.** `[3]` với `k=6` sẽ tự khớp với chính nó.",
  "**Quên trường hợp `x == k − x` khi đếm số cặp.** Phải dùng `C(m,2)`, không phải `m × m`.",
  "**Dùng `list` làm khoá dẫn xuất.** Không hashable — phải chuyển sang `tuple` hoặc `str`.",
  "**Dùng `defaultdict` rồi kiểm tra khoá bằng `d[k]`.** Chỉ đọc thôi cũng **tạo khoá**; dùng `.get()` hoặc `in`.",
  "**Dùng `c1 - c2` với `Counter` rồi mất khoá.** Phép trừ bỏ giá trị ≤ 0; dùng `subtract()` nếu cần giữ.",
  "**Gọi `most_common()` không tham số khi chỉ cần top k.** Mất lợi thế O(n log k), thành O(n log n).",
  "**Đếm tần suất trên dữ liệu trộn kiểu.** `1`, `True`, `1.0` gộp thành một khoá.",
  "**Dùng hash cho bài \"gần k\" hoặc \"nhỏ hơn k\".** Bảng băm chỉ tra được **bằng nhau**; cần sắp + hai con trỏ.",
  "**Dùng hash cho bài ba số tổng 0.** Ra cùng bậc nhưng loại trùng rất rối; sắp xếp làm việc đó tự nhiên.",
  "**Nói \"đã tối ưu xuống O(n)\" khi chỉ cải thiện trường hợp thực tế.** Nhóm theo khoá vẫn O(n²) xấu nhất nếu mọi phần tử cùng nhóm.",
  "**Không kiểm ca biên khi viết các mẫu này.** Mảng rỗng, một phần tử, mọi phần tử bằng nhau, và **số âm** — nơi lỗi thường nằm."
],

tu_kiem_tra: [
  "Phát biểu mẫu gốc của cả bài trong một câu. Nó đổi gì lấy gì?",
  "Nêu ba biến thể của mẫu, và khoá là gì trong mỗi biến thể.",
  "So sánh ba cách đếm tần suất. `defaultdict` có hành vi gì dễ gây bug?",
  "Vì sao `most_common(k)` là O(n log k)? Điều gì xảy ra khi gọi không tham số?",
  "Nêu ba cạm bẫy của `Counter`.",
  "Vì sao `Counter([1, True, 1.0])` cho một khoá thay vì ba?",
  "\"Khoá dẫn xuất\" là gì? Nêu bốn ví dụ khác nhau.",
  "Vì sao khoá dẫn xuất phải là `tuple` hoặc `str`, không phải `list`?",
  "So sánh hai cách nhóm anagram. Cách nào tốt hơn khi từ dài, và vì sao?",
  "Viết công thức tổng đoạn theo tổng tiền tố. Vì sao nó biến bài toán thành two-sum?",
  "Vì sao phải khởi tạo bảng với `{0: 1}`? Ca kiểm thử nào bắt được lỗi này?",
  "Bài \"đếm dãy con\" và \"dãy con dài nhất\" khác nhau ở hai chi tiết nào?",
  "Bài \"chia hết cho k\" dùng khoá gì? Vì sao nó vẫn thuộc cùng mẫu?",
  "Vì sao trong two-sum phải tra trước rồi mới lưu?",
  "Khi đếm số cặp có tổng = k, trường hợp `x == k − x` xử lý thế nào? Cho ví dụ số.",
  "Vì sao bài ba số tổng 0 nên dùng sắp xếp thay vì hash?",
  "Nêu giới hạn cốt lõi của bảng băm. Nó là hệ quả của điều gì ở bài trước?",
  "So sánh ba cách tìm cặp tổng = k theo bốn tiêu chí. Cách nào bền hơn khi yêu cầu có thể đổi?",
  "Dấu hiệu nào trong code gợi ý rằng nên áp mẫu của bài này?"
],

tai_nguyen: [
  "**Tài liệu Python — `collections`** — `Counter`, `defaultdict`, `OrderedDict`; đọc kỹ phần ghi chú về hành vi của phép trừ `Counter` và `most_common`.",
  "**Mã nguồn CPython — `Lib/collections/__init__.py`**, hàm `Counter.most_common` — thấy nó gọi `heapq.nlargest` khi có tham số `n`.",
  "Sách **Elements of Programming Interviews** — Aziz, Lee, Prakash. Chương về hash tables có đầy đủ các biến thể two-sum và bài toán tổng tiền tố.",
  "Sách **Introduction to Algorithms** — CLRS. Phần về tổng tiền tố trong chương quy hoạch động, và ứng dụng của nó.",
  "**Tài liệu Python — `heapq.nlargest` và `nsmallest`** — cơ chế đằng sau `most_common(k)`, và khi nào chúng nhanh hơn `sorted`."
],

lien_he: [
  "**Vòng lặp lồng nhau** ở module 1 mảng này: two-sum cơ bản xuất hiện ở đó; bài này rút ra mẫu tổng quát và các biến thể.",
  "**Hàm băm và bảng băm** trong module này: vì sao khoá dẫn xuất phải hashable, và bẫy `True == 1`.",
  "**Hash map, hash set** trong module này: giới hạn \"hash phá vỡ thứ tự\" — lý do hash không giải được bài \"gần k\".",
  "**Vì sao O(1) chỉ là trung bình** ngay sau: điều gì có thể phá vỡ hiệu năng của các mẫu trong bài này.",
  "**Kỹ thuật con trỏ chậm/nhanh** ở module 2 mảng này: hai con trỏ trên mảng đã sắp — lựa chọn thay thế khi hash không được.",
  "**Thang so sánh các bậc** ở module 1 mảng này: đổi O(n²) lấy O(n) là bước nhảy qua ranh giới thực tế.",
  "**Độ phức tạp không gian** ở module 1 mảng này: mọi mẫu ở đây trả giá bằng O(n) bộ nhớ.",
  "**List, dict, set, tuple** ở module 5 mảng 01: cú pháp và các thao tác cơ bản.",
  "**Sắp xếp** ở module 6 mảng này: khi hash không áp dụng được, sắp xếp là lựa chọn thay thế chính.",
  "**Kỹ thuật giải thuật cốt lõi** ở module 8 mảng này: tổng tiền tố là nền của nhiều bài quy hoạch động.",
  "**Luyện phỏng vấn** ở module 10 mảng này: đây là nhóm bài toán được hỏi nhiều nhất.",
  "**SQL** ở mảng 04: `GROUP BY` chính là mẫu nhóm theo khoá dẫn xuất, thực hiện ở tầng database.",
  "**Công cụ & Python cho data** ở mảng 07: `pandas.groupby` và `value_counts` là hai mẫu này ở quy mô bảng dữ liệu."
]

};
