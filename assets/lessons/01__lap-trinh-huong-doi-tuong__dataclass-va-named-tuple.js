window.LESSONS["01:lap-trinh-huong-doi-tuong:dataclass-va-named-tuple"] = {

tom_tat: [
  "**`@dataclass` sinh ra `__init__`, `__repr__`, `__eq__` từ chú thích kiểu.** Nó không phải kiểu class mới — chỉ là một decorator viết hộ code lặp đi lặp lại.",
  "**Cùng một bẫy giá trị mặc định mutable** ở module 5 lại xuất hiện: `ds: list = []` trong `dataclass` bị chặn thẳng bằng lỗi, phải dùng `field(default_factory=list)`.",
  "**`frozen=True` cho bất biến và hashable**; bỏ nó thì object mutable và **không** hashable. Đây là hệ quả trực tiếp của quy tắc `__eq__`/`__hash__` ở bài trước.",
  "**`NamedTuple` là `tuple` thật** — giải nén được, so sánh được với `tuple`, nhẹ nhất. `dataclass` là class thường — linh hoạt hơn, sửa được.",
  "**Chọn theo dữ liệu có đổi không và có cần hashable không**, chứ không theo cái nào \"hiện đại hơn\"."
],

can_biet_truoc: [
  "Bài **Magic method** ngay trước — `dataclass` sinh ra đúng những phương thức đó, và bẫy `__eq__`/`__hash__` giải thích `frozen`.",
  "Bài **Class, object, thuộc tính** — `dataclass` chỉ là class thường có decorator.",
  "Bài **List, dict, set, tuple** ở module 5 — `NamedTuple` đã được so sánh sơ ở đó.",
  "Bài **Type hint và mypy** ở module 5 — `dataclass` đọc chú thích kiểu để sinh code."
],

dinh_nghia: "Nói đời thường: viết một class chỉ để chứa dữ liệu là **việc lặp đi lặp lại**: gán từng thuộc tính trong `__init__`, viết `__repr__` để in ra cho dễ nhìn, viết `__eq__` để so sánh. `@dataclass` là cái máy làm hộ ba việc đó từ một danh sách trường.\n\nNói chính xác:\n\n- **`@dataclass`** — decorator đọc **chú thích kiểu** ở thân class rồi sinh ra `__init__`, `__repr__`, `__eq__` và một số phương thức khác.\n- **`field()`** — hàm mô tả chi tiết một trường: giá trị mặc định động, có tham gia so sánh không, có xuất hiện trong `repr` không.\n- **`default_factory`** — hàm được gọi **mỗi lần tạo object** để sinh giá trị mặc định. Cách đúng cho mutable.\n- **`frozen=True`** — làm object bất biến; gán sau khi tạo là `FrozenInstanceError`.\n- **`NamedTuple`** — `tuple` có tên trường. Là `tuple` thật, nên bất biến và hashable sẵn.\n- **`TypedDict`** — `dict` có mô tả kiểu cho từng khoá. Là `dict` thật, không phải class.\n- **`__post_init__`** — chạy ngay sau `__init__` do `dataclass` sinh ra; chỗ để kiểm tra dữ liệu.\n\nĐiểm cần nắm ngay: **`@dataclass` không tạo ra một loại object đặc biệt.** Kết quả vẫn là một class Python bình thường — kế thừa được, thêm phương thức được, dùng `@property` được. Nó chỉ tiết kiệm cho bạn phần code máy móc.",

vi_sao_can: "Phần lớn class trong một dự án thật là class chứa dữ liệu, và viết tay chúng vừa tốn công vừa dễ sai:\n\n- **Giảm code máy móc rất đáng kể.** Một class năm trường viết tay tốn khoảng hai mươi dòng cho `__init__`, `__repr__`, `__eq__`; `@dataclass` làm trong sáu dòng.\n- **Không quên `__repr__` nữa.** Bài trước đã nói `__repr__` quan trọng thế nào với gỡ lỗi; `dataclass` cho nó miễn phí, nên không còn lý do để thiếu.\n- **Không sai quy tắc `__eq__`/`__hash__`.** `dataclass` xử lý đúng quan hệ giữa hai cái đó theo `eq` và `frozen`, thay vì để bạn tự nhớ.\n- **Chú thích kiểu thành bắt buộc.** Trường không có chú thích thì `dataclass` bỏ qua — nên class tự nhiên có tài liệu về kiểu.\n- **Định hướng AI/ML và Backend.** Cấu hình thí nghiệm, tham số mô hình, bản ghi dữ liệu, đối tượng truyền giữa các tầng — tất cả là dữ liệu có cấu trúc, và đây là công cụ đúng cho chúng.\n\nTình huống thật rất hay gặp: bạn viết class `CauHinh` với mười trường, viết tay `__init__` gán mười dòng. Sáu tháng sau thêm một trường và **quên cập nhật `__repr__`** — nên log không bao giờ hiện trường mới, và bạn mất một buổi gỡ lỗi vì tưởng giá trị chưa được truyền vào. Với `@dataclass`, thêm một dòng khai báo là mọi thứ tự đồng bộ. Phần cơ chế sẽ chỉ rõ nó sinh ra chính xác những gì.",

co_che: "### `@dataclass` sinh ra gì\n\n```python\n# Viết tay — khoảng 20 dòng\nclass Diem:\n    def __init__(self, x: float, y: float, nhan: str = \"\"):\n        self.x, self.y, self.nhan = x, y, nhan\n\n    def __repr__(self):\n        return f\"Diem(x={self.x!r}, y={self.y!r}, nhan={self.nhan!r})\"\n\n    def __eq__(self, k):\n        if not isinstance(k, Diem):\n            return NotImplemented\n        return (self.x, self.y, self.nhan) == (k.x, k.y, k.nhan)\n```\n\n```python\n# @dataclass — 5 dòng, cùng kết quả\nfrom dataclasses import dataclass\n\n@dataclass\nclass Diem:\n    x: float\n    y: float\n    nhan: str = \"\"\n\nd = Diem(1, 2)\nd                        # Diem(x=1, y=2, nhan='')\nd == Diem(1, 2)          # True\nd.x = 5                  # ✓ sửa được (mặc định mutable)\n```\n\nNó sinh chính xác những gì, tuỳ tham số:\n\n| Tham số | Mặc định | Sinh ra |\n|---|---|---|\n| `init` | `True` | `__init__` |\n| `repr` | `True` | `__repr__` |\n| `eq` | `True` | `__eq__` |\n| `order` | `False` | `__lt__`, `__le__`, `__gt__`, `__ge__` |\n| `frozen` | `False` | `__setattr__`, `__delattr__` chặn gán |\n| `slots` | `False` | `__slots__` |\n| `kw_only` | `False` | mọi trường thành chỉ-tên |\n\n**Chỉ trường có chú thích kiểu mới được tính:**\n\n```python\n@dataclass\nclass A:\n    x: int              # ✓ trường của dataclass\n    y = 5               # ✗ chỉ là thuộc tính class thường, KHÔNG vào __init__\n\nA(1)                    # ✓ y không phải tham số\nA.y                     # 5\n```\n\nĐây là chỗ hay nhầm: quên hai chấm là trường biến mất khỏi `__init__` mà không có lỗi nào.\n\n### Bẫy mutable — lại xuất hiện\n\n```python\n@dataclass\nclass GioHang:\n    san_pham: list = []          # ValueError khi định nghĩa class\n```\n\n```\nValueError: mutable default <class 'list'> for field san_pham is not allowed:\n            use default_factory\n```\n\nPython **chặn thẳng** thay vì để bạn dính bẫy — vì đây đúng là bẫy `def f(ds=[])` ở module 5: giá trị mặc định tính một lần, mọi object dùng chung.\n\n```python\nfrom dataclasses import dataclass, field\n\n@dataclass\nclass GioHang:\n    san_pham: list[str] = field(default_factory=list)     # ⭐ gọi MỖI lần tạo\n    thuoc_tinh: dict = field(default_factory=dict)\n    tao_luc: str = field(default_factory=lambda: str(datetime.now()))\n\na, b = GioHang(), GioHang()\na.san_pham.append(\"sách\")\nb.san_pham              # [] ✓ hai list riêng\n```\n\nSo sánh cơ chế:\n\n```\ndefault=[]              → một list DUY NHẤT tạo lúc định nghĩa class  ✗ bị chặn\ndefault_factory=list    → gọi list() MỖI lần tạo object              ✓\n```\n\nLưu ý Python **chỉ chặn được các kiểu mutable nó biết** (`list`, `dict`, `set`). Một class mutable tự viết vẫn lọt qua:\n\n```python\n@dataclass\nclass A:\n    kho: MotClassMutable = MotClassMutable()     # ⚠️ KHÔNG bị chặn, vẫn là bẫy\n```\n\nQuy tắc an toàn: **mọi giá trị mặc định không phải kiểu immutable đơn giản thì dùng `default_factory`.**\n\n### `field()` — điều khiển chi tiết\n\n```python\nfrom dataclasses import dataclass, field\n\n@dataclass\nclass NguoiDung:\n    ten: str\n    email: str\n    mat_khau: str = field(repr=False)               # ⭐ KHÔNG in ra\n    tuoi: int = field(default=0, compare=False)     # ⭐ không tính khi so sánh\n    vai_tro: list[str] = field(default_factory=list)\n    _cache: dict = field(default_factory=dict, init=False, repr=False, compare=False)\n\nnd = NguoiDung(\"Minh\", \"m@x.com\", \"bimat\", 22)\nprint(nd)\n# NguoiDung(ten='Minh', email='m@x.com', tuoi=22, vai_tro=[])\n#                                    ⭐ mat_khau KHÔNG xuất hiện\n\nNguoiDung(\"Minh\", \"m@x.com\", \"bimat\", 22) == NguoiDung(\"Minh\", \"m@x.com\", \"bimat\", 99)\n# True — tuoi có compare=False\n```\n\nBốn tham số hay dùng nhất:\n\n```\nrepr=False        không in ra — cho mật khẩu, token, dữ liệu lớn\ncompare=False     không tính khi so sánh — cho cache, dấu thời gian\ninit=False        không nhận qua __init__ — cho trường tính toán hoặc nội bộ\ndefault_factory   giá trị mặc định động — BẮT BUỘC cho mutable\n```\n\n`repr=False` cho mật khẩu là kỹ thuật bảo mật thật: nó ngăn dữ liệu nhạy cảm rò rỉ vào **log và truy vết lỗi** — nơi rất nhiều người có quyền đọc.\n\n### `__post_init__` — kiểm tra và tính toán\n\n```python\n@dataclass\nclass HinhChuNhat:\n    rong: float\n    cao: float\n    dien_tich: float = field(init=False)      # ⭐ tính, không nhận vào\n\n    def __post_init__(self):\n        if self.rong <= 0 or self.cao <= 0:\n            raise ValueError(f\"kích thước phải dương: {self.rong}×{self.cao}\")\n        self.dien_tich = self.rong * self.cao\n\nh = HinhChuNhat(3, 4)\nh.dien_tich          # 12\nHinhChuNhat(-1, 4)   # ValueError ✓\n```\n\n`__post_init__` chạy **ngay sau** `__init__` do `dataclass` sinh ra, nên mọi trường đã được gán. Đây là chỗ chuẩn để:\n\n```\n· Kiểm tra dữ liệu (bài xử lý ngoại lệ ở module 5)\n· Tính trường phái sinh\n· Chuẩn hoá giá trị (viết thường, bỏ khoảng trắng)\n```\n\nMột lưu ý: trường tính toán như `dien_tich` **cố định sau khi tạo**. Nếu `rong` đổi thì `dien_tich` không tự cập nhật — đó là lý do `@property` thường đúng hơn:\n\n```python\n@dataclass\nclass HinhChuNhat:\n    rong: float\n    cao: float\n\n    @property\n    def dien_tich(self) -> float:\n        return self.rong * self.cao       # ⭐ luôn đúng\n```\n\nChọn `field(init=False)` khi việc tính **tốn kém** và dữ liệu không đổi; chọn `@property` khi tính rẻ hoặc dữ liệu đổi được.\n\nVới `frozen=True` thì `__post_init__` không gán trực tiếp được:\n\n```python\n@dataclass(frozen=True)\nclass A:\n    x: int\n    kep: int = field(init=False)\n\n    def __post_init__(self):\n        object.__setattr__(self, \"kep\", self.x * 2)     # ⭐ lối duy nhất\n```\n\n### `frozen=True` — bất biến và hashable\n\n```python\n@dataclass\nclass A:\n    x: int\n\nhash(A(1))          # TypeError: unhashable type: 'A'\n```\n\nĐây chính là quy tắc ở bài magic method: `dataclass` sinh `__eq__`, nên Python đặt `__hash__ = None`.\n\n```python\n@dataclass(frozen=True)\nclass Diem:\n    x: float\n    y: float\n\nd = Diem(1, 2)\nd.x = 5              # FrozenInstanceError ✓\nhash(d)              # ✓ hashable\n{Diem(1, 2), Diem(1, 2)}        # {Diem(x=1, y=2)} — một phần tử\n{Diem(1, 2): \"gốc toạ độ\"}      # ✓ làm khoá dict được\n```\n\nBảng quyết định tính hashable:\n\n| `eq` | `frozen` | `__hash__` | Hashable |\n|:---:|:---:|---|:---:|\n| `True` | `False` | `None` | **✗** (mặc định) |\n| `True` | `True` | tự sinh | **✓** |\n| `False` | bất kỳ | kế thừa | ✓ (so sánh theo danh tính) |\n\nHàng đầu là mặc định, và nó **đúng**: object mutable không nên hashable, vì sửa nó sau khi cất vào `set` sẽ làm nó \"biến mất\".\n\n`frozen` chỉ **nông một tầng** — đúng như `tuple`:\n\n```python\n@dataclass(frozen=True)\nclass A:\n    ds: list = field(default_factory=list)\n\na = A()\na.ds = [1]           # FrozenInstanceError ✓\na.ds.append(1)       # ✓ CHẠY ĐƯỢC — list bên trong vẫn mutable\nhash(a)              # TypeError: unhashable type: 'list'\n```\n\nMuốn bất biến thật thì các trường cũng phải immutable: dùng `tuple` thay `list`, `frozenset` thay `set`.\n\n### `order=True` — so sánh thứ tự\n\n```python\n@dataclass(order=True)\nclass PhienBan:\n    chinh: int\n    phu: int\n    va: int = 0\n\nsorted([PhienBan(1, 2), PhienBan(1, 0), PhienBan(2, 0)])\n# [PhienBan(1,0), PhienBan(1,2), PhienBan(2,0)]\n```\n\nSo sánh theo **thứ tự khai báo trường**, giống `tuple`. Muốn thứ tự khác thì thêm một trường sắp xếp ẩn:\n\n```python\n@dataclass(order=True)\nclass CongViec:\n    uu_tien_sap_xep: int = field(init=False, repr=False)\n    ten: str = field(compare=False)\n    uu_tien: int = field(compare=False)\n\n    def __post_init__(self):\n        self.uu_tien_sap_xep = -self.uu_tien       # ⭐ số âm ⇒ ưu tiên cao trước\n\nimport heapq\nh = []\nfor cv in (CongViec(\"thấp\", 1), CongViec(\"cao\", 10), CongViec(\"vừa\", 5)):\n    heapq.heappush(h, cv)\nheapq.heappop(h).ten          # 'cao'\n```\n\nMẫu này rất hữu ích với hàng đợi ưu tiên: `heapq` cần object so sánh được, và `compare=False` ở các trường khác đảm bảo **chỉ** trường sắp xếp được dùng — tránh lỗi khi hai công việc cùng ưu tiên mà `ten` không so sánh được.\n\n### `slots=True` — tiết kiệm bộ nhớ\n\n```python\n@dataclass(slots=True)\nclass Diem:\n    x: float\n    y: float\n\nd = Diem(1, 2)\nd.z = 3              # AttributeError ✓ bắt lỗi gõ sai tên\n```\n\nGiống `__slots__` ở bài class: nhẹ hơn, bắt lỗi gõ sai, đổi lại mất tính linh hoạt. Đáng dùng khi tạo **rất nhiều** object.\n\n### `NamedTuple`\n\n```python\nfrom typing import NamedTuple\n\nclass Diem(NamedTuple):\n    x: float\n    y: float\n    nhan: str = \"\"\n\nd = Diem(1, 2)\nd.x                  # 1        — truy cập theo tên\nd[0]                 # 1        — ⭐ VÀ theo chỉ số\nx, y, nhan = d       # ⭐ giải nén được\nd == (1, 2, \"\")      # ⭐ True — so sánh được với tuple thường\nlen(d)               # 3\nhash(d)              # ✓ hashable sẵn\nd.x = 5              # AttributeError ✓ bất biến\n\nd._replace(x=10)     # Diem(x=10, y=2, nhan='') — tạo bản MỚI\nd._asdict()          # {'x': 1, 'y': 2, 'nhan': ''}\nDiem._fields         # ('x', 'y', 'nhan')\n```\n\nĐiểm mấu chốt: **`NamedTuple` LÀ một `tuple` thật.** Mọi thứ dùng được với `tuple` đều dùng được với nó — giải nén, so sánh với `tuple`, làm khoá `dict`, truyền vào hàm nhận `tuple`.\n\nĐó vừa là ưu điểm vừa là nhược điểm:\n\n```python\nd = Diem(1, 2)\nd + (3,)             # (1, 2, '', 3) — ⚠️ thành tuple thường, mất kiểu\nd * 2                # ⚠️ lặp lại tuple\nDiem(1, 2) == (1, 2, \"\")     # True — ⚠️ có thể là bất ngờ\n```\n\nNó thừa hưởng **toàn bộ** giao diện của `tuple`, kể cả những thao tác vô nghĩa với dữ liệu của bạn — đúng vấn đề \"kế thừa cho toàn bộ, không chọn lọc\" ở bài composition.\n\n### `TypedDict` — vẫn là `dict`\n\n```python\nfrom typing import TypedDict\n\nclass CauHinh(TypedDict):\n    host: str\n    port: int\n    debug: bool\n\ncf: CauHinh = {\"host\": \"localhost\", \"port\": 8000, \"debug\": True}\ncf[\"host\"]           # truy cập như dict\ncf[\"hots\"]           # ❌ mypy bắt được lỗi gõ sai\n\nimport json\njson.dumps(cf)       # ⭐ chạy thẳng — nó LÀ dict\n```\n\n`TypedDict` **không tạo class**, không có phương thức, không kiểm tra lúc chạy. Nó chỉ thêm thông tin kiểu cho một `dict` thường.\n\nĐiều đó làm nó lựa chọn đúng cho **dữ liệu JSON**: không phải chuyển đổi qua lại, `json.dumps` chạy thẳng, và thư viện nào nhận `dict` cũng dùng được.\n\nGiới hạn: nó **không kiểm tra gì lúc chạy**. `json.load()` trả về `dict` bất kỳ, và chú thích `-> CauHinh` chỉ là lời hứa của bạn — đúng như đã nói ở bài type hint.\n\n### `dataclass` vẫn là class thường\n\n```python\n@dataclass\nclass TaiKhoan:\n    chu: str\n    _so_du: float = 0.0\n\n    @property                                 # ⭐ property dùng được\n    def so_du(self) -> float:\n        return self._so_du\n\n    def nap(self, tien: float) -> None:       # ⭐ phương thức dùng được\n        if tien <= 0:\n            raise ValueError(f\"số tiền phải dương: {tien}\")\n        self._so_du += tien\n\n    @classmethod                               # ⭐ classmethod dùng được\n    def tu_dict(cls, d: dict) -> \"TaiKhoan\":\n        return cls(d[\"chu\"], d.get(\"so_du\", 0))\n\n@dataclass\nclass TaiKhoanVIP(TaiKhoan):                   # ⭐ kế thừa được\n    ti_le_thuong: float = 0.05\n```\n\nĐây là điểm khác biệt lớn nhất với `NamedTuple`: `dataclass` không hạn chế gì cả. Bạn dùng nó như một class thường, chỉ bớt phần code máy móc.\n\nCạm bẫy khi kế thừa: **trường có mặc định phải đứng sau trường không có mặc định**, tính trên toàn bộ chuỗi kế thừa.\n\n```python\n@dataclass\nclass Cha:\n    a: int = 0\n\n@dataclass\nclass Con(Cha):\n    b: int          # TypeError: non-default argument 'b' follows default argument\n```\n\nCách chữa là `kw_only=True`, làm mọi trường thành chỉ-tên nên thứ tự không còn quan trọng:\n\n```python\n@dataclass(kw_only=True)\nclass Con(Cha):\n    b: int          # ✓\n\nCon(a=1, b=2)\n```\n\n`kw_only=True` cũng là lựa chọn tốt cho class nhiều trường nói chung — nó buộc lời gọi phải nêu tên, đúng nguyên tắc đã học ở bài hàm.\n\n### Tiện ích của `dataclasses`\n\n```python\nfrom dataclasses import asdict, astuple, replace, fields, is_dataclass\n\n@dataclass\nclass Diem:\n    x: float\n    y: float\n\nd = Diem(1, 2)\n\nasdict(d)            # {'x': 1, 'y': 2}  — ĐỆ QUY vào dataclass lồng nhau\nastuple(d)           # (1, 2)\nreplace(d, x=10)     # Diem(x=10, y=2) — tạo bản MỚI\nis_dataclass(d)      # True\n[f.name for f in fields(d)]     # ['x', 'y']\n```\n\n`replace` đặc biệt hữu ích với `frozen=True`: không sửa được thì tạo bản mới với một trường đổi.\n\nMột cảnh báo về `asdict`: nó **đệ quy sâu và sao chép mọi thứ**, nên với cấu trúc lồng nhau lớn thì chậm. Cần nhanh thì tự viết:\n\n```python\ndef sang_dict_nong(d):\n    return {f.name: getattr(d, f.name) for f in fields(d)}\n```",

vi_du: "### Trước và sau `@dataclass`\n\n```python\n# ❌ Viết tay — và dễ quên cập nhật khi thêm trường\nclass CauHinh:\n    def __init__(self, host, port=8000, debug=False, timeout=30, thu_lai=3):\n        self.host = host\n        self.port = port\n        self.debug = debug\n        self.timeout = timeout\n        self.thu_lai = thu_lai\n\n    def __repr__(self):\n        return (f\"CauHinh(host={self.host!r}, port={self.port!r}, \"\n                f\"debug={self.debug!r}, timeout={self.timeout!r})\")\n        #        ⚠️ QUÊN thu_lai — log không bao giờ hiện nó\n\n    def __eq__(self, k):\n        if not isinstance(k, CauHinh): return NotImplemented\n        return (self.host, self.port, self.debug) == (k.host, k.port, k.debug)\n        #       ⚠️ QUÊN timeout và thu_lai — hai cấu hình khác nhau bị coi là bằng\n```\n\n```python\n# ✓ @dataclass — không thể quên, vì chỉ có một danh sách trường\nfrom dataclasses import dataclass\n\n@dataclass\nclass CauHinh:\n    host: str\n    port: int = 8000\n    debug: bool = False\n    timeout: int = 30\n    thu_lai: int = 3\n\ncf = CauHinh(\"localhost\", debug=True)\nprint(cf)\n# CauHinh(host='localhost', port=8000, debug=True, timeout=30, thu_lai=3)\n\nCauHinh(\"a\") == CauHinh(\"a\")           # True\nCauHinh(\"a\", timeout=1) == CauHinh(\"a\")  # False ✓ tính đủ mọi trường\n```\n\nHai lỗi trong bản viết tay đều **im lặng**: `repr` thiếu trường thì gỡ lỗi mất thông tin; `eq` thiếu trường thì hai cấu hình khác nhau bị coi là giống nhau — có thể làm cache trả sai kết quả.\n\n### Bẫy mutable — Python chặn giúp\n\n```python\nfrom dataclasses import dataclass, field\nfrom datetime import datetime\n\n@dataclass\nclass DonHang:\n    ma: str\n    san_pham: list[str] = []          # ValueError ngay khi định nghĩa class\n```\n\n```\nValueError: mutable default <class 'list'> for field san_pham is not allowed:\n            use default_factory\n```\n\n```python\n# ✓\n@dataclass\nclass DonHang:\n    ma: str\n    san_pham: list[str] = field(default_factory=list)\n    thuoc_tinh: dict[str, str] = field(default_factory=dict)\n    tao_luc: datetime = field(default_factory=datetime.now)   # ⭐ MỖI lần một giờ khác\n\nimport time\na = DonHang(\"DH1\")\ntime.sleep(1)\nb = DonHang(\"DH2\")\n\na.san_pham.append(\"sách\")\nprint(b.san_pham)                # [] ✓ list riêng\nprint(a.tao_luc != b.tao_luc)    # True ✓ giờ khác nhau\n```\n\nSo với bẫy `def f(luc=datetime.now())` ở module 5: ở đó Python **không** chặn được và bạn phải tự biết; ở đây `dataclass` chặn với `list`/`dict`/`set`, và `default_factory` xử lý luôn trường hợp `datetime.now`.\n\n### Ẩn dữ liệu nhạy cảm khỏi log\n\n```python\n@dataclass\nclass ThongTinKetNoi:\n    host: str\n    nguoi_dung: str\n    mat_khau: str = field(repr=False)              # ⭐ KHÔNG in\n    api_key: str = field(default=\"\", repr=False)\n    port: int = 5432\n\ntt = ThongTinKetNoi(\"db.local\", \"admin\", \"bimat123\", \"key-abc\")\n\nprint(tt)\n# ThongTinKetNoi(host='db.local', nguoi_dung='admin', port=5432)\n#                                       ⭐ mật khẩu và key KHÔNG lộ\n\nimport logging\nlogging.error(\"kết nối thất bại: %r\", tt)     # ⭐ log an toàn\n\ntt.mat_khau      # 'bimat123' — vẫn dùng được bình thường\n```\n\nĐây là biện pháp bảo mật thật, không phải chuyện thẩm mỹ: truy vết lỗi và log thường được gửi tới hệ thống thu thập tập trung, nơi rất nhiều người có quyền đọc. Một `__repr__` in ra mật khẩu là rò rỉ.\n\n### `frozen=True` cho khoá `dict` và cache\n\n```python\nfrom dataclasses import dataclass\nfrom functools import lru_cache\n\n@dataclass(frozen=True)\nclass ToaDo:\n    vi_do: float\n    kinh_do: float\n\n@lru_cache(maxsize=1000)\ndef tra_dia_chi(td: ToaDo) -> str:            # ⭐ cần hashable\n    print(f\"gọi API cho {td}\")\n    return f\"Địa chỉ tại {td.vi_do}, {td.kinh_do}\"\n\nt = ToaDo(21.03, 105.85)\ntra_dia_chi(t)          # gọi API...\ntra_dia_chi(t)          # ⭐ lấy từ cache, không gọi lại\ntra_dia_chi(ToaDo(21.03, 105.85))   # cũng từ cache — bằng nhau thì cùng hash\n\ntra_dia_chi.cache_info()\n```\n\nKhông có `frozen=True` thì `lru_cache` báo `TypeError: unhashable type` — nối thẳng về ba điều kiện dùng `lru_cache` ở bài decorator.\n\nDùng làm khoá `dict`:\n\n```python\nkhoang_cach: dict[tuple[ToaDo, ToaDo], float] = {}\na, b = ToaDo(21.0, 105.0), ToaDo(10.8, 106.7)\nkhoang_cach[(a, b)] = 1600.0\nkhoang_cach[(ToaDo(21.0, 105.0), ToaDo(10.8, 106.7))]     # 1600.0 ✓\n```\n\n### `frozen` chỉ nông một tầng\n\n```python\n@dataclass(frozen=True)\nclass Sai:\n    ten: str\n    the: list[str] = field(default_factory=list)\n\ns = Sai(\"a\")\ns.ten = \"b\"          # FrozenInstanceError ✓\ns.the.append(\"x\")    # ✓ CHẠY ĐƯỢC — không thật sự bất biến\nhash(s)              # TypeError: unhashable type: 'list'\n\n# ✓ Bất biến thật — mọi trường cũng phải immutable\n@dataclass(frozen=True)\nclass Dung:\n    ten: str\n    the: tuple[str, ...] = ()          # ⭐ tuple thay list\n\nd = Dung(\"a\", (\"x\", \"y\"))\nhash(d)              # ✓\nd.the.append         # AttributeError ✓ tuple không có append\n```\n\nĐây là bài `tuple` ở module 5 lặp lại: `frozen` đảm bảo **các nhãn không đổi**, không đảm bảo object mà nhãn trỏ tới không đổi.\n\n### Kiểm tra dữ liệu trong `__post_init__`\n\n```python\n@dataclass\nclass NguoiDung:\n    ten: str\n    email: str\n    tuoi: int = 0\n    vai_tro: list[str] = field(default_factory=list)\n\n    def __post_init__(self):\n        # Chuẩn hoá\n        self.ten = self.ten.strip()\n        self.email = self.email.strip().lower()\n\n        # Kiểm tra\n        if not self.ten:\n            raise ValueError(\"tên không được rỗng\")\n        if \"@\" not in self.email:\n            raise ValueError(f\"email không hợp lệ: {self.email!r}\")\n        if not 0 <= self.tuoi <= 150:\n            raise ValueError(f\"tuổi ngoài khoảng: {self.tuoi}\")\n\nnd = NguoiDung(\"  Minh  \", \"M@X.COM\")\nnd.ten, nd.email          # ('Minh', 'm@x.com') ✓ đã chuẩn hoá\nNguoiDung(\"Minh\", \"sai\")  # ValueError: email không hợp lệ: 'sai'\n```\n\nLưu ý giới hạn: `__post_init__` chỉ chạy **lúc tạo**. Gán sau đó không được kiểm tra:\n\n```python\nnd.email = \"cũng sai\"     # ✓ không lỗi — __post_init__ không chạy lại\n```\n\nMuốn kiểm tra mọi lúc thì dùng `@property` với setter (bài đóng gói), hoặc dùng `frozen=True` để cấm gán hẳn.\n\n### `NamedTuple` — khi `tuple` là đúng\n\n```python\nfrom typing import NamedTuple\n\nclass KetQua(NamedTuple):\n    thanh_cong: bool\n    du_lieu: dict | None = None\n    loi: str = \"\"\n\ndef goi_api(url: str) -> KetQua:\n    if not url.startswith(\"https\"):\n        return KetQua(False, loi=\"cần HTTPS\")\n    return KetQua(True, {\"gt\": 42})\n\n# ⭐ Giải nén tự nhiên — điểm mạnh nhất của NamedTuple\nok, du_lieu, loi = goi_api(\"https://x.com\")\nif ok:\n    print(du_lieu)\n\n# Hoặc dùng theo tên\nkq = goi_api(\"http://x.com\")\nif not kq.thanh_cong:\n    print(kq.loi)\n\n# Là tuple thật\nkq[0]                      # False\nlen(kq)                    # 3\nkq == (False, None, \"cần HTTPS\")     # True\n```\n\n`NamedTuple` là lựa chọn tự nhiên cho **giá trị trả về nhiều thành phần**: nó giải nén được như `tuple` (cú pháp quen thuộc) mà vẫn có tên (đọc ra nghĩa).\n\nSo với trả `tuple` trần:\n\n```python\n# ❌ Sáu tháng sau không ai biết phần tử thứ hai là gì\nreturn True, {\"gt\": 42}, \"\"\n\n# ✓\nreturn KetQua(True, {\"gt\": 42})\n```\n\n### Ba lựa chọn trên cùng một dữ liệu\n\n```python\nfrom dataclasses import dataclass\nfrom typing import NamedTuple, TypedDict\nimport json, sys\n\n# ① NamedTuple — bất biến, nhẹ nhất, giải nén được\nclass DiemNT(NamedTuple):\n    x: float\n    y: float\n\n# ② dataclass frozen — bất biến, có thể thêm phương thức\n@dataclass(frozen=True, slots=True)\nclass DiemDC:\n    x: float\n    y: float\n    def khoang_cach_toi_goc(self) -> float:\n        return (self.x ** 2 + self.y ** 2) ** 0.5\n\n# ③ TypedDict — vẫn là dict, hợp với JSON\nclass DiemTD(TypedDict):\n    x: float\n    y: float\n\n\nnt, dc = DiemNT(3, 4), DiemDC(3, 4)\ntd: DiemTD = {\"x\": 3, \"y\": 4}\n\nprint(sys.getsizeof(nt), sys.getsizeof(dc), sys.getsizeof(td))\n# NamedTuple nhỏ nhất, TypedDict lớn nhất\n\nx, y = nt                       # ✓ giải nén\n# x, y = dc                     # ✗ dataclass không giải nén được\n\ndc.khoang_cach_toi_goc()        # 5.0 — ✓ phương thức\n# nt.khoang_cach...             # ✗ NamedTuple thêm phương thức được nhưng hạn chế\n\njson.dumps(td)                  # ✓ chạy thẳng\njson.dumps(nt)                  # '[3, 4]' — ⚠️ thành mảng, MẤT tên trường\njson.dumps(dc)                  # ✗ TypeError\njson.dumps(asdict(dc))          # ✓ phải chuyển trước\n```\n\nBa dòng `json.dumps` cuối là khác biệt thực dụng quan trọng nhất khi làm việc với API:\n\n```\nTypedDict    → dump thẳng\nNamedTuple   → thành mảng JSON, mất tên trường ⚠️\ndataclass    → phải asdict() trước\n```\n\n### `heapq` với `dataclass(order=True)`\n\n```python\nimport heapq\nfrom dataclasses import dataclass, field\n\n@dataclass(order=True)\nclass CongViec:\n    khoa_sap_xep: tuple = field(init=False, repr=False)\n    ten: str = field(compare=False)\n    uu_tien: int = field(compare=False)\n    han: str = field(compare=False, default=\"\")\n\n    def __post_init__(self):\n        self.khoa_sap_xep = (-self.uu_tien, self.han)   # ⭐ ưu tiên cao trước\n\nh: list[CongViec] = []\nfor cv in (CongViec(\"viết báo cáo\", 3, \"2026-09-01\"),\n           CongViec(\"sửa lỗi nghiêm trọng\", 10, \"2026-08-30\"),\n           CongViec(\"họp\", 5, \"2026-08-31\")):\n    heapq.heappush(h, cv)\n\nwhile h:\n    cv = heapq.heappop(h)\n    print(f\"{cv.uu_tien:>3} {cv.ten}\")\n#  10 sửa lỗi nghiêm trọng\n#   5 họp\n#   3 viết báo cáo\n```\n\nHai chi tiết quan trọng: `compare=False` ở mọi trường khác đảm bảo `heapq` **chỉ** dùng `khoa_sap_xep`, và khoá là `tuple` để có tiêu chí phụ khi ưu tiên bằng nhau. Không có `compare=False`, hai công việc cùng ưu tiên sẽ so tiếp tới `ten` — có thể chạy được nhưng thứ tự không như mong đợi.\n\n### Kế thừa `dataclass`\n\n```python\n@dataclass\nclass NhanVien:\n    ten: str\n    luong: float = 0.0\n\n@dataclass\nclass NhanVienBanHang(NhanVien):\n    hoa_hong: float          # TypeError: non-default follows default\n```\n\n```python\n# ✓ Cách 1 — cho giá trị mặc định\n@dataclass\nclass NhanVienBanHang(NhanVien):\n    hoa_hong: float = 0.0\n\n# ✓ Cách 2 — kw_only, thứ tự không còn quan trọng ⭐\n@dataclass(kw_only=True)\nclass NhanVienBanHang(NhanVien):\n    hoa_hong: float\n\nNhanVienBanHang(ten=\"Minh\", luong=10, hoa_hong=5)\n```\n\nCách 2 tốt hơn khi trường **thật sự bắt buộc** — cách 1 cho giá trị mặc định giả tạo chỉ để lách quy tắc thứ tự, và người dùng có thể vô tình bỏ qua nó.\n\n### Cấu hình thí nghiệm — ứng dụng cho AI/ML\n\n```python\nfrom dataclasses import dataclass, field, asdict\nimport json\nfrom pathlib import Path\n\n@dataclass(frozen=True)\nclass CauHinhHuanLuyen:\n    ten_mo_hinh: str\n    toc_do_hoc: float = 1e-3\n    kich_thuoc_lo: int = 32\n    so_epoch: int = 10\n    seed: int = 42\n    lop_an: tuple[int, ...] = (128, 64)        # ⭐ tuple, không phải list\n\n    def __post_init__(self):\n        if not 0 < self.toc_do_hoc < 1:\n            raise ValueError(f\"tốc độ học ngoài khoảng: {self.toc_do_hoc}\")\n        if self.kich_thuoc_lo < 1:\n            raise ValueError(f\"kích thước lô phải dương: {self.kich_thuoc_lo}\")\n\n    def luu(self, duong_dan: str) -> None:\n        Path(duong_dan).write_text(\n            json.dumps(asdict(self), ensure_ascii=False, indent=2),\n            encoding=\"utf-8\")\n\n    @classmethod\n    def nap(cls, duong_dan: str) -> \"CauHinhHuanLuyen\":\n        d = json.loads(Path(duong_dan).read_text(encoding=\"utf-8\"))\n        d[\"lop_an\"] = tuple(d[\"lop_an\"])       # ⚠️ JSON trả list, cần tuple\n        return cls(**d)\n\n\ncf = CauHinhHuanLuyen(\"resnet\", toc_do_hoc=3e-4)\ncf.luu(\"cf.json\")\nassert CauHinhHuanLuyen.nap(\"cf.json\") == cf      # ✓ đi và về nguyên vẹn\n\n# frozen ⇒ hashable ⇒ dùng làm khoá để theo dõi kết quả\nket_qua: dict[CauHinhHuanLuyen, float] = {}\nket_qua[cf] = 0.94\n\n# Thử biến thể — replace tạo bản mới\nfrom dataclasses import replace\nfor lr in (1e-3, 3e-4, 1e-4):\n    bien_the = replace(cf, toc_do_hoc=lr)\n    ket_qua[bien_the] = ...\n```\n\nBốn tính chất hội tụ ở đây và giải thích vì sao đây là công cụ đúng cho cấu hình thí nghiệm:\n\n```\nfrozen        → cấu hình không bị sửa nhầm giữa chừng, và hashable\nhashable      → làm khoá dict để ánh xạ cấu hình → kết quả\n__post_init__ → sai tham số thì lỗi NGAY, không phải sau ba giờ huấn luyện\nasdict/replace → lưu ra JSON, và sinh biến thể để quét tham số\n```\n\nDòng `d[\"lop_an\"] = tuple(...)` là bẫy đã nói ở bài đọc ghi file: JSON không có kiểu `tuple`, nên đọc lại thành `list` — và `list` phá `frozen` lẫn `hash`.",

so_sanh: "### Bốn cách biểu diễn dữ liệu có cấu trúc\n\n| | Class thường | `@dataclass` | `NamedTuple` | `TypedDict` |\n|---|---|---|---|---|\n| Bản chất | class | **class** | **`tuple`** | **`dict`** |\n| Tự sinh `__init__` | ✗ | ✓ | ✓ | — |\n| Tự sinh `__repr__` | ✗ | **✓** | ✓ | — |\n| Tự sinh `__eq__` | ✗ | **✓** | ✓ | — |\n| Mutable | ✓ | ✓ (mặc định) | **✗** | ✓ |\n| Hashable | ✓ | chỉ khi `frozen` | **✓** | ✗ |\n| Giải nén `a, b = x` | ✗ | ✗ | **✓** | ✗ |\n| Truy cập `x[0]` | ✗ | ✗ | **✓** | theo khoá |\n| Thêm phương thức | ✓ | **✓** | hạn chế | ✗ |\n| Kế thừa | ✓ | **✓** | hạn chế | ✓ |\n| Bộ nhớ | lớn | lớn (nhỏ với `slots`) | **nhỏ nhất** | lớn nhất |\n| `json.dumps` thẳng | ✗ | ✗ | ⚠️ thành mảng | **✓** |\n\nHai hàng cuối quyết định nhiều trường hợp thực tế hơn người ta nghĩ.\n\n### Chọn cái nào\n\n| Tình huống | Chọn |\n|---|---|\n| Dữ liệu + hành vi phong phú | **class thường** hoặc `dataclass` |\n| Dữ liệu là chính, cần sửa | **`@dataclass`** |\n| Dữ liệu bất biến, cần hashable | **`@dataclass(frozen=True)`** |\n| Giá trị trả về nhiều thành phần | **`NamedTuple`** |\n| Bản ghi bất biến, cần giải nén | **`NamedTuple`** |\n| Rất nhiều object, cần nhẹ | `NamedTuple` hoặc `dataclass(slots=True)` |\n| Dữ liệu JSON đi thẳng | **`TypedDict`** |\n| Cần kiểm tra lúc chạy nghiêm ngặt | thư viện xác thực dữ liệu |\n\nHàng cuối đáng nhắc: cả ba công cụ trong bài **không kiểm tra kiểu lúc chạy**. `dataclass` nhận `CauHinh(port=\"tám nghìn\")` không hề phàn nàn. Cần kiểm tra thật thì viết `__post_init__` hoặc dùng thư viện chuyên cho việc đó.\n\n### `dataclass` và `NamedTuple`\n\n| | `@dataclass` | `NamedTuple` |\n|---|---|---|\n| Sửa được | **✓** (trừ `frozen`) | ✗ |\n| Hashable | chỉ khi `frozen` | **✓ luôn** |\n| Giải nén | ✗ | **✓** |\n| So sánh với `tuple` thường | ✗ | ✓ (có thể là bất ngờ) |\n| Thừa hưởng thao tác `tuple` | ✗ | **✓ kể cả vô nghĩa** |\n| Kế thừa | **✓ tự nhiên** | hạn chế |\n| `field()` điều khiển chi tiết | **✓** | ✗ |\n| Bộ nhớ | lớn hơn | **nhỏ hơn** |\n\nHàng \"thừa hưởng thao tác `tuple`\" là nhược điểm ít được nhắc: `d + (3,)` và `d * 2` chạy được nhưng vô nghĩa — đúng vấn đề \"kế thừa cho toàn bộ\" ở bài composition.\n\n### `frozen` và tính hashable\n\n| `eq` | `frozen` | Hashable | Dùng khi |\n|:---:|:---:|:---:|---|\n| `True` | `False` | **✗** | dữ liệu thay đổi (mặc định) |\n| `True` | `True` | **✓** | khoá `dict`, `set`, `lru_cache` |\n| `False` | bất kỳ | ✓ | hiếm — so sánh theo danh tính |\n\nHàng đầu là mặc định và nó **đúng**: object mutable không nên hashable, vì sửa nó sau khi cất vào `set` làm nó \"biến mất\".\n\nLưu ý `frozen` chỉ **nông một tầng** — trường là `list` thì nội dung vẫn sửa được và object vẫn không hashable. Muốn bất biến thật thì dùng `tuple` và `frozenset`.\n\n### `field()` — các tham số\n\n| Tham số | Tác dụng | Dùng cho |\n|---|---|---|\n| `default_factory` | giá trị mặc định động | **mọi mutable** |\n| `repr=False` | không in ra | **mật khẩu, token**, dữ liệu lớn |\n| `compare=False` | không tính khi so sánh | cache, dấu thời gian |\n| `init=False` | không nhận qua `__init__` | trường tính toán, nội bộ |\n| `kw_only=True` | thành chỉ-tên | tránh lỗi thứ tự khi kế thừa |\n\n`repr=False` cho dữ liệu nhạy cảm là biện pháp bảo mật thật — nó ngăn rò rỉ vào log và truy vết lỗi.\n\n### `field(init=False)` và `@property`\n\n| | `field(init=False)` + `__post_init__` | `@property` |\n|---|---|---|\n| Tính khi nào | **một lần, lúc tạo** | mỗi lần đọc |\n| Tự cập nhật khi dữ liệu đổi | **✗** | **✓** |\n| Chi phí đọc | không | mỗi lần tính lại |\n| Xuất hiện trong `repr`/`asdict` | ✓ | ✗ |\n| Dùng khi | tính **tốn kém**, dữ liệu cố định | tính rẻ, dữ liệu đổi được |\n\nHàng thứ hai là chỗ chọn sai gây lỗi: trường tính sẵn không cập nhật khi `rong` đổi, và bạn có dữ liệu cũ mà không có cảnh báo nào.\n\n### Tiện ích `dataclasses`\n\n| Hàm | Trả về | Ghi chú |\n|---|---|---|\n| `asdict(x)` | `dict` | **đệ quy sâu**, chậm với cấu trúc lớn |\n| `astuple(x)` | `tuple` | đệ quy |\n| `replace(x, **k)` | object **mới** | cách \"sửa\" object `frozen` |\n| `fields(x)` | thông tin trường | để viết code tổng quát |\n| `is_dataclass(x)` | `bool` | |\n\n`replace` là công cụ chính khi làm việc với `frozen`: không sửa được thì tạo bản mới với một trường đổi — mẫu rất hữu ích để quét tham số.\n\n### JSON — điểm khác biệt thực dụng\n\n| | `json.dumps` | Đọc lại |\n|---|---|---|\n| `TypedDict` | **✓ thẳng** | ✓ nguyên vẹn |\n| `dataclass` | ✗ cần `asdict()` | `cls(**d)` |\n| `NamedTuple` | ⚠️ thành **mảng**, mất tên | phải tự dựng lại |\n\nHàng cuối là bẫy: `json.dumps(nt)` cho `[3, 4]` chứ không phải `{\"x\": 3, \"y\": 4}` — dữ liệu mất tên trường mà không có cảnh báo nào. Dùng `nt._asdict()` trước khi dump.",

loi_thuong_gap: [
  "**Quên chú thích kiểu cho trường.** `y = 5` không có hai chấm thì không phải trường của `dataclass` — nó biến mất khỏi `__init__` mà không báo lỗi.",
  "**Dùng `list`/`dict` làm giá trị mặc định.** Python chặn với `ValueError`; dùng `field(default_factory=list)`.",
  "**Dùng một class mutable tự viết làm mặc định.** Python **không** chặn được, và bẫy vẫn còn nguyên.",
  "**Tưởng `dataclass` mặc định hashable.** Nó sinh `__eq__` nên `__hash__` bị đặt `None`. Cần `frozen=True`.",
  "**Tưởng `frozen=True` là bất biến sâu.** Trường `list` vẫn sửa nội dung được, và object vẫn không hashable. Dùng `tuple`.",
  "**Đặt trường không mặc định sau trường có mặc định khi kế thừa.** `TypeError`. Dùng `kw_only=True`.",
  "**Tin `__post_init__` kiểm tra mọi lúc.** Nó chỉ chạy lúc tạo; gán sau đó không qua kiểm tra nào.",
  "**Dùng `field(init=False)` cho trường phái sinh mà dữ liệu gốc đổi được.** Trường không tự cập nhật. Dùng `@property`.",
  "**In `dataclass` chứa mật khẩu hoặc token ra log.** Dùng `field(repr=False)`.",
  "**`json.dumps` một `NamedTuple`.** Ra mảng, mất tên trường. Dùng `._asdict()` trước.",
  "**Đọc `tuple` từ JSON mà quên chuyển lại.** JSON trả `list`, phá `frozen` và `hash`.",
  "**Dùng `asdict()` cho cấu trúc lồng nhau lớn trong vòng lặp nóng.** Nó sao chép đệ quy — chậm.",
  "**Tin `dataclass` kiểm tra kiểu lúc chạy.** Chú thích kiểu chỉ để `mypy` đọc; `CauHinh(port=\"x\")` chạy bình thường.",
  "**Dùng `NamedTuple` cho dữ liệu sẽ cần sửa.** Nó bất biến; đổi sang `dataclass` về sau là phá code người dùng.",
  "**Quên `compare=False` khi dùng `order=True` với `heapq`.** Các trường phụ tham gia so sánh, cho thứ tự không mong đợi."
],

tu_kiem_tra: [
  "`@dataclass` sinh ra những phương thức nào theo mặc định? Trường nào được tính là trường của nó?",
  "Vì sao `san_pham: list = []` bị chặn? `default_factory` khác gì `default`?",
  "Python có chặn được mọi giá trị mặc định mutable không?",
  "Vì sao `dataclass` mặc định không hashable? Liên hệ với quy tắc `__eq__`/`__hash__` ở bài magic method.",
  "`frozen=True` bảo đảm gì và **không** bảo đảm gì? Cho ví dụ.",
  "Nêu bốn tham số của `field()` và trường hợp dùng của mỗi cái.",
  "Vì sao `field(repr=False)` cho mật khẩu là biện pháp bảo mật thật?",
  "`__post_init__` chạy lúc nào? Giới hạn của nó là gì?",
  "Với `frozen=True`, làm sao gán trường trong `__post_init__`?",
  "So sánh `field(init=False)` và `@property` cho trường phái sinh.",
  "`NamedTuple` có ba khả năng nào mà `dataclass` không có? Và một nhược điểm nào?",
  "Ba cách biểu diễn khác nhau thế nào khi `json.dumps`?",
  "Vì sao kế thừa `dataclass` gặp lỗi thứ tự trường? Hai cách chữa và cách nào tốt hơn?",
  "Vì sao cần `compare=False` khi dùng `order=True` với `heapq`?",
  "Ba công cụ trong bài có kiểm tra kiểu lúc chạy không? Nếu cần thì làm gì?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, module `dataclasses` — tham khảo đầy đủ mọi tham số của `@dataclass` và `field()`.",
  "**PEP 557** — đề xuất gốc của `dataclasses`, giải thích lý do thiết kế và vì sao không chọn cách khác.",
  "Tài liệu Python, mục `typing.NamedTuple` và `typing.TypedDict` — cú pháp và giới hạn của từng cái.",
  "Sách **Fluent Python** — Luciano Ramalho. Có hẳn một chương so sánh `dataclass`, `NamedTuple` và class thường.",
  "Sách **Effective Python** — Brett Slatkin. Các mục về việc dùng `dataclass` thay cho `dict` lồng nhau và cho `tuple` trần."
],

lien_he: [
  "**Magic method** ngay trước: `dataclass` sinh ra chính những phương thức đó, và bẫy `__eq__`/`__hash__` giải thích vì sao cần `frozen`.",
  "**Class, object, thuộc tính**: `dataclass` chỉ là class thường; `slots=True` là `__slots__` đã học ở đó.",
  "**Đóng gói**: `field(repr=False)` và `@property` trong `dataclass` là công cụ kiểm soát cái gì lộ ra.",
  "**Kế thừa và MRO**: quy tắc thứ tự trường khi kế thừa `dataclass`, và cách `kw_only` chữa nó.",
  "**Composition over inheritance**: `NamedTuple` thừa hưởng toàn bộ thao tác `tuple`, kể cả vô nghĩa — đúng vấn đề ở bài đó.",
  "**Hàm, tham số mặc định** ở module 5: bẫy `def f(ds=[])` là cùng một bẫy, và `kw_only` là kỹ thuật tham số chỉ-tên đã học.",
  "**Type hint và mypy** ở module 5: `dataclass` đọc chú thích kiểu để sinh code; `TypedDict` cũng đã gặp ở đó.",
  "**Đọc ghi file, JSON, CSV** ở module 5: `tuple` thành `list` khi qua JSON là bẫy đã nói ở đó.",
  "**Decorator** ở module 5: `@dataclass` là decorator áp lên class, và `lru_cache` cần object hashable.",
  "**List, dict, set, tuple** ở module 5: bảng so sánh `tuple`/`NamedTuple`/`dataclass` ở đó, giờ đã hiểu đủ lý do.",
  "**AI/ML** ở mảng 07: `dataclass(frozen=True)` là công cụ chuẩn cho cấu hình thí nghiệm — bất biến, hashable, lưu ra JSON được."
]

};
