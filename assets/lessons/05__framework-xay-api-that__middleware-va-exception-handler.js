window.LESSONS["05:framework-xay-api-that:middleware-va-exception-handler"] = {

tom_tat: [
  "Middleware bao quanh toàn bộ request–response nên phù hợp cho concern xuyên cắt như request ID, timing, CORS và security header; business rule không thuộc đây.",
  "Thứ tự đăng ký quyết định thứ tự chạy: request đi từ lớp ngoài vào trong, response và exception quay ngược ra ngoài.",
  "Exception handler chuyển lỗi có ngữ nghĩa thành HTTP contract ổn định; lỗi bất ngờ phải được log với correlation ID nhưng không lộ stack trace cho client.",
  "Không phải mọi exception đều giống nhau: lỗi client, xung đột nghiệp vụ, dependency tạm lỗi và bug server cần status, khả năng retry và mức log khác nhau.",
  "API đơn hàng dùng `X-Request-ID`, structured error body và domain exception; đồng thời phòng header injection, request ID giả và logging dữ liệu nhạy cảm."
],

can_biet_truoc: [
  "Vòng đời HTTP request/response, header, body và nhóm status code 4xx–5xx.",
  "Python exception, `try/except/finally`, `raise` và stack trace.",
  "FastAPI endpoint, dependency và khái niệm ASGI application.",
  "JSON logging và correlation ID ở mức khái niệm."
],

dinh_nghia: "**Middleware** là một lớp ASGI nhận mọi request trước endpoint và nhận response hoặc exception trên đường quay ra; nó có thể quan sát, bổ sung hoặc từ chối luồng. **Exception handler** là hàm ánh xạ một loại exception sang HTTP response cụ thể.\n\nMiddleware giải quyết chính sách áp dụng rộng theo giao thức. Exception handler giữ một error contract nhất quán mà không bắt từng exception trong mọi route. Cả hai nằm ở biên HTTP: domain nên ném lỗi có ý nghĩa như `OutOfStock`, nhưng không nên import `JSONResponse` hay tự chọn status code.",

vi_sao_can: "Khi API đơn hàng có mười endpoint, việc mỗi hàm tự đo thời gian, gắn request ID, thêm header và bọc `try/except` tạo ra mười phiên bản hơi khác nhau. Một route quên header bảo mật; route khác log cả token; route thứ ba trả lỗi cùng nguyên nhân nhưng schema khác. Client phải viết nhánh riêng, còn người trực production không nối được log giữa proxy, API và database.\n\nMiddleware tạo một điểm thi hành chính sách toàn cục. Exception handler biến lỗi domain thành ngôn ngữ HTTP mà vẫn giữ domain độc lập. Nhưng vị trí toàn cục cũng làm sai lầm lan rộng: middleware đọc toàn bộ body có thể phá streaming; bắt `Exception` rồi trả 200 che mọi bug; tin request ID của client cho phép log injection; cấu hình CORS rộng làm browser gửi credential sai origin.\n\nMục tiêu không phải “không bao giờ có 500”. Response 500 trung thực, có mã theo dõi và log đầy đủ tốt hơn response 200 chứa `{success:false}`. Operability và bảo mật gặp nhau ở đây: server cần đủ chi tiết để điều tra, client chỉ cần thông tin an toàn và có thể hành động.",

co_che: "### Hình dung middleware như các lớp hành tây\n\nGiả sử có ba lớp: request ID ngoài cùng, logging ở giữa, CORS trong cùng, rồi router. Request đi theo A → B → C → endpoint. Response đi endpoint → C → B → A. Nếu endpoint ném lỗi, exception cũng bung theo chiều ngược cho tới lớp hoặc handler xử lý nó.\n\nThứ tự này ảnh hưởng bằng chứng. Muốn request ID xuất hiện trong log của mọi lớp, middleware request ID phải bao ngoài logging. Muốn timing bao gồm cả xử lý CORS và exception, timer phải bắt đầu đủ sớm. Không nên dựa vào trực giác tên danh sách; hãy viết integration test quan sát header và log.\n\n### ASGI thuần và middleware tiện dụng\n\nASGI middleware là callable nhận `scope`, `receive`, `send`, rồi gọi app bên trong. Đây là tầng thấp nhưng kiểm soát streaming tốt. Middleware kiểu `@app.middleware(\"http\")` với `call_next` dễ viết cho header/timing. Một số tác vụ cần theo dõi từng chunk response hoặc tránh buffering thì ASGI thuần chính xác hơn.\n\nMiddleware không nên giữ trạng thái request trong thuộc tính mutable của instance vì cùng một object phục vụ request đồng thời. Biến cục bộ hoặc `contextvars` phù hợp hơn. Global `current_request_id` có race: request sau ghi đè khi request trước chưa log xong.\n\n### Request ID: nhận, kiểm tra, hay tự sinh\n\nCorrelation ID giúp nối log của một request qua nhiều service. Nếu reverse proxy đáng tin đã gắn ID, ứng dụng có thể nhận sau khi validate độ dài và ký tự. Với request trực tiếp từ Internet, tốt nhất tự sinh UUID; không đưa chuỗi tùy ý của client vào log vì newline/control character có thể giả dòng log.\n\nResponse nên trả ID để người dùng báo lỗi. Khi gọi downstream, truyền ID qua header theo quy ước nội bộ. Request ID không phải secret và không thay trace ID; một background job sinh sau response có thể cần correlation mới liên kết với order ID.\n\n### Context variable thay cho biến toàn cục\n\n`ContextVar` giữ giá trị theo execution context của coroutine. Middleware đặt token trước `call_next`, logger đọc ở bất kỳ tầng nào, rồi middleware reset trong `finally`. Reset là bắt buộc để worker tái sử dụng context mà không rò ID cũ.\n\nTask tạo bằng cơ chế đặc biệt hoặc chạy trong thread có thể không tự mang context như mong đợi, nên integration test phải bao gồm background work. Không nhét toàn bộ Request object vào context; giữ dữ liệu nhỏ như request ID, user ID đã làm mờ.\n\n### Timing đo gì và không đo gì\n\nĐồng hồ đơn điệu như `perf_counter` phù hợp đo khoảng thời gian vì không nhảy khi hệ thống chỉnh giờ. Header `Server-Timing` hữu ích lúc phát triển, nhưng công khai chi tiết database có thể tiết lộ kiến trúc. Metric latency nên gắn route template `/orders/{order_id}`, không gắn ID thật; nếu không cardinality tăng vô hạn.\n\nThời gian middleware đo tới lúc tạo response chưa chắc bao gồm lúc client tải hết streaming body. Nếu cần đo byte và thời gian truyền thật, phải bọc ASGI `send`. Đây là ví dụ lý do abstraction tiện dụng đôi khi không đủ.\n\n### Phân loại lỗi trước khi chọn status\n\nLỗi validation hình dạng là 422 trong FastAPI. Không tìm thấy tài nguyên là 404. Trạng thái hiện tại xung đột với command, như xác nhận đơn đã hủy, thường là 409. Không xác thực là 401 và cần `WWW-Authenticate`; đã biết danh tính nhưng thiếu quyền là 403, dù đôi khi trả 404 để tránh lộ tài nguyên.\n\nDependency timeout hoặc database tạm mất có thể là 503; server không nên khẳng định client sửa payload là hết. Bug như `AttributeError` là 500. Cùng status vẫn cần machine-readable `code` ổn định để client không parse câu tiếng Việt.\n\n| Nhóm | Ví dụ đơn hàng | Status thường dùng | Client nên làm gì |\n|---|---|---|---|\n| dữ liệu sai hình dạng | quantity bằng 0 | 422 | sửa payload |\n| tài nguyên không thấy/không lộ | ID không tồn tại | 404 | dừng hoặc hỏi lại |\n| xung đột state | đơn đã hủy nhưng lại confirm | 409 | tải state mới |\n| chưa xác thực | token thiếu/hết hạn | 401 | đăng nhập/làm mới token |\n| hạ tầng tạm lỗi | pool database cạn | 503 | retry có backoff nếu an toàn |\n| bug không dự kiến | truy cập thuộc tính sai | 500 | báo request ID, không retry bão |\n\n### Domain exception giữ business khỏi HTTP\n\n`OutOfStock(sku, available)` diễn đạt sự kiện nghiệp vụ. Handler HTTP quyết định code `OUT_OF_STOCK`, status 409 và có công khai `available` hay không. Worker xử lý message có thể bắt cùng exception rồi đưa message vào dead-letter queue thay vì tạo JSONResponse.\n\nĐừng tạo một class exception cho mọi câu chữ. Một taxonomy nhỏ, có field typed, dễ duy trì hơn hàng trăm subclass. Đồng thời tránh ném exception cho nhánh điều khiển bình thường trong vòng lặp nóng; ở HTTP boundary, exception cho failure hiếm là hợp lý.\n\n### Error contract dành cho máy trước, người sau\n\nMột body hữu ích có `code`, `message`, `request_id`, và tùy chọn `details` an toàn. `code` ổn định qua dịch ngôn ngữ; `message` để con người đọc; `details` chỉ chứa field client sửa được. Không trả tên table, SQL, đường dẫn file, stack trace hoặc token.\n\nVersioning error code cũng là API design. Đổi `OUT_OF_STOCK` thành `NO_INVENTORY` có thể phá client dù status vẫn 409. Hãy tài liệu hóa code public và test chúng.\n\n### Handler cụ thể phải thắng handler tổng quát\n\nFastAPI/Starlette chọn handler theo loại exception. Handler domain cụ thể tạo response nghiệp vụ; handler cuối cho `Exception` ghi log và trả thông báo chung. Không bắt `BaseException`, vì cancellation và tín hiệu hệ thống không phải lỗi ứng dụng thông thường.\n\nHandler tổng quát phải dùng `logger.exception` để giữ traceback nội bộ. Nếu chỉ log `str(exc)`, lỗi như `KeyError('x')` thiếu call stack và gần như không điều tra được. Ngược lại, response tuyệt đối không chứa `repr(exc)`.\n\n### Lỗi xảy ra trước và sau response bắt đầu\n\nNếu exception xảy ra trước khi header được gửi, handler có thể thay response. Nếu streaming response đã gửi 200 và vài chunk rồi mới lỗi, HTTP status không thể quay ngược thành 500. Server chỉ có thể đóng connection và log. Vì vậy streaming cần protocol cấp ứng dụng nếu client phải biết kết quả hoàn chỉnh, chẳng hạn checksum hoặc record kết thúc.\n\nBackground task chạy sau response cũng không thể sửa response đã gửi. Lỗi phải được log/metric và có cơ chế retry riêng. Dùng background task nội process cho thao tác quan trọng như gửi hàng dễ mất việc khi process chết; job queue bền vững phù hợp hơn.\n\n### CORS không phải cơ chế xác thực\n\nCORS là chính sách browser về việc JavaScript origin nào được đọc response. `curl`, mobile app và attacker server không bị CORS chặn. Cho phép origin `*` cùng credential là mô hình sai; khi dùng cookie, liệt kê origin tin cậy và chống CSRF.\n\nSecurity header như HSTS, CSP hay `X-Content-Type-Options` có vai trò khác nhau và thường do reverse proxy phối hợp. Không copy một bộ header mà không hiểu: CSP chủ yếu quan trọng với trang HTML, còn JSON API cần đúng content type và cache policy.\n\n### Logging không được trở thành vụ rò dữ liệu thứ hai\n\nKhông log Authorization, cookie phiên, password, địa chỉ đầy đủ hay body thanh toán. Redaction theo key hữu ích nhưng không hoàn hảo vì bí mật có thể nằm trong chuỗi tự do. Tốt hơn là log allowlist: method, route template, status, duration, request ID và principal ID đã cân nhắc.\n\nLog 4xx phổ biến ở mức info/warning có sampling; log mọi 404 như error tạo nhiễu. 500 cần traceback. Metric đếm theo code lỗi cho xu hướng, trace cho đường đi, log cho chi tiết; không công cụ nào thay toàn bộ hai công cụ còn lại.\n\n### Khi nào không nên dùng middleware\n\nQuy tắc “chỉ chủ đơn được hủy” cần order và user nên nằm trong policy/service, không middleware đọc path tùy tiện. Transaction cũng không nên bọc mù mọi request. Biến đổi response business theo endpoint trong middleware tạo coupling khó thấy. Nếu concern chỉ áp dụng một router, dependency hoặc route class có thể rõ hơn chính sách global.",

vi_du: "### Request ID và error contract chạy được\n\nLưu thành `main.py`, chạy `uvicorn main:app --reload`. Ví dụ chấp nhận ID client chỉ khi khớp allowlist ngắn; production nên thống nhất proxy nào là trust boundary.\n\n```python\nimport logging\nimport re\nimport time\nimport uuid\nfrom contextvars import ContextVar\n\nfrom fastapi import FastAPI, Request\nfrom fastapi.responses import JSONResponse\n\nlog = logging.getLogger(\"orders\")\nrequest_id_var: ContextVar[str] = ContextVar(\"request_id\", default=\"-\")\nSAFE_ID = re.compile(r\"^[A-Za-z0-9_-]{1,64}$\")\n\nclass OutOfStock(Exception):\n    def __init__(self, sku: str, available: int):\n        self.sku = sku\n        self.available = available\n\napp = FastAPI()\n\n@app.middleware(\"http\")\nasync def request_context(request: Request, call_next):\n    supplied = request.headers.get(\"X-Request-ID\", \"\")\n    request_id = supplied if SAFE_ID.fullmatch(supplied) else str(uuid.uuid4())\n    token = request_id_var.set(request_id)\n    started = time.perf_counter()\n    try:\n        response = await call_next(request)\n    finally:\n        elapsed_ms = (time.perf_counter() - started) * 1000\n        log.info(\"request_complete method=%s path=%s duration_ms=%.2f request_id=%s\",\n                 request.method, request.url.path, elapsed_ms, request_id)\n        request_id_var.reset(token)\n    response.headers[\"X-Request-ID\"] = request_id\n    return response\n\n@app.exception_handler(OutOfStock)\nasync def out_of_stock_handler(request: Request, exc: OutOfStock):\n    return JSONResponse(status_code=409, content={\n        \"code\": \"OUT_OF_STOCK\",\n        \"message\": \"sản phẩm không đủ tồn kho\",\n        \"details\": {\"sku\": exc.sku, \"available\": exc.available},\n        \"request_id\": request_id_var.get(),\n    })\n\n@app.exception_handler(Exception)\nasync def unexpected_handler(request: Request, exc: Exception):\n    log.exception(\"unexpected_error request_id=%s\", request_id_var.get())\n    return JSONResponse(status_code=500, content={\n        \"code\": \"INTERNAL_ERROR\",\n        \"message\": \"server không thể hoàn tất yêu cầu\",\n        \"request_id\": request_id_var.get(),\n    })\n\n@app.post(\"/orders/{sku}\")\nasync def place_order(sku: str, quantity: int = 1):\n    available = 2\n    if quantity > available:\n        raise OutOfStock(sku, available)\n    return {\"sku\": sku, \"quantity\": quantity}\n```\n\nGọi `curl -i -X POST 'http://127.0.0.1:8000/orders/BOOK-01?quantity=3' -H 'X-Request-ID: demo_123'`. Response 409 có cùng ID trong header/body, còn log ghi thời gian. Gửi ID chứa newline không dễ qua HTTP client, nhưng regex vẫn là defense in depth; chuỗi không hợp lệ được thay bằng UUID.\n\n### Integration test kiểm contract thay vì implementation\n\n```python\nfrom fastapi.testclient import TestClient\n\nclient = TestClient(app, raise_server_exceptions=False)\n\ndef test_out_of_stock_has_stable_shape():\n    r = client.post(\"/orders/BOOK-01?quantity=3\", headers={\"X-Request-ID\": \"case-7\"})\n    assert r.status_code == 409\n    assert r.headers[\"X-Request-ID\"] == \"case-7\"\n    assert r.json()[\"code\"] == \"OUT_OF_STOCK\"\n    assert \"request_id\" in r.json()\n\ndef test_unknown_bug_does_not_leak_traceback():\n    @app.get(\"/boom\")\n    def boom():\n        raise RuntimeError(\"database password=secret\")\n    r = client.get(\"/boom\")\n    assert r.status_code == 500\n    assert \"secret\" not in r.text\n```\n\nTest thứ hai không chứng minh log đã redaction; nó chỉ chứng minh response không lộ exception. Log sink cần test/configuration riêng.",

so_sanh: "### Chọn middleware, dependency hay handler\n\n| Nhu cầu | Công cụ phù hợp | Vì sao |\n|---|---|---|\n| request ID cho mọi route | middleware | bao trọn request và response |\n| lấy current user cho nhóm endpoint | dependency | có type, cache theo request, khai rõ tại route |\n| đổi `OutOfStock` thành 409 | exception handler | ánh xạ một taxonomy lỗi nhất quán |\n| kiểm chủ sở hữu đơn | service/policy | cần domain object và quy tắc nghiệp vụ |\n| rollback transaction use case | service/unit of work | ranh giới commit không trùng mọi HTTP request |\n\nMiddleware mạnh vì khó bị endpoint quên, nhưng cũng khó biết route nào thực sự cần. Dependency hiện ra trong chữ ký và dễ override. Handler tách failure path khỏi happy path nhưng taxonomy quá lớn sẽ rối.\n\n### Bắt lỗi tại route hay toàn cục\n\n| Cách | Ưu điểm | Nhược điểm |\n|---|---|---|\n| `try/except` trong từng route | thấy ngay ngữ cảnh cục bộ | lặp, response lệch nhau, dễ nuốt bug |\n| handler theo loại exception | contract tập trung | cần thiết kế exception domain |\n| handler bắt mọi `Exception` | response 500 an toàn | nếu dùng quá tay che phân loại và khó test |\n\nRoute vẫn có thể bắt lỗi khi cần bù trừ cục bộ, nhưng không nên copy boilerplate chuyển mọi lỗi thành JSON.",

loi_thuong_gap: [
  "Bắt `Exception` rồi trả status 200; dashboard báo thành công trong khi client phải đọc field lỗi, retry và cache đều hành xử sai.",
  "Trả `str(exc)` hoặc stack trace cho client; kiểm response 500 thấy SQL, đường dẫn hoặc secret là dấu hiệu nghiêm trọng.",
  "Dùng biến global cho request ID; dưới tải đồng thời, log của hai request mang cùng hoặc tráo ID.",
  "Log toàn bộ header/body; tìm thấy Authorization, cookie hay địa chỉ khách trong log sink cho thấy data minimization thất bại.",
  "Đọc toàn bộ body trong middleware để log; upload/streaming tăng memory, endpoint phía sau có thể không còn đọc được body đúng cách.",
  "Gắn metric label bằng URL thật; hàng triệu order ID tạo cardinality cao và hệ metrics tốn bộ nhớ.",
  "Xem CORS như authentication; `curl` từ origin bất kỳ vẫn gọi được API là bằng chứng CORS không chặn attacker ngoài browser.",
  "Cho background task quan trọng chạy sau response mà không có queue bền; process restart làm việc biến mất dù client đã nhận 2xx."
],

tu_kiem_tra: [
  "Với middleware A ngoài B, hãy viết thứ tự bốn sự kiện request/response và giải thích lớp nào đo được nhiều thời gian hơn.",
  "Vì sao request ID từ client phải giới hạn ký tự và độ dài dù nó không phải secret?",
  "Khi nào nên trả 401, 403, 404 và 409 trong API đơn hàng?",
  "Tại sao response model lỗi cần `code` ổn định thay vì buộc client parse `message`?",
  "Exception xảy ra sau khi streaming response đã gửi header 200 có thể đổi status thành 500 không?",
  "Câu khó: handler `Exception` trả JSON 500 nhưng middleware logging không ghi traceback. Khách có request ID; đội vận hành còn thiếu bằng chứng gì và sửa ở đâu?",
  "Vì sao transaction toàn request và authorization theo order không nên mặc định đặt trong middleware?"
],

tai_nguyen: [
  "FastAPI Documentation — Middleware và Handling Errors.",
  "Starlette Documentation — Middleware và Exceptions.",
  "Python Documentation — `contextvars` và `logging`.",
  "Release It! — Michael T. Nygard."
],

lien_he: [
  "Nối sang mảng Observability: request ID, metric theo route template và trace context là ba tín hiệu bổ sung chứ không thay nhau.",
  "Nối sang mảng Bảo mật Web: CORS, CSRF, security header và redaction cần threat model theo cách client xác thực.",
  "Nối sang mảng Kiến trúc phần mềm: domain exception giữ core độc lập với FastAPI và dùng lại được trong worker.",
  "Nối sang mảng Hệ phân tán: retry, idempotency và durable queue xử lý lỗi xảy ra sau khi response hoặc side effect đã bắt đầu."
]

};
