window.LESSONS["01:bo-nho-co-che-thuc-thi:shallow-copy-vs-deep-copy"] = {

tom_tat: [
  "**Sao chép nông sao chép mảng con trỏ; sao chép sâu đi theo từng con trỏ và tạo lại object.** Module 5 nói kết quả khác nhau — bài này chỉ rõ cái gì được sao và cái gì không.",
  "**`deepcopy` giải được bài toán tham chiếu vòng** nhờ một bảng ghi nhớ những object đã sao. Không có nó thì cấu trúc trỏ về chính nó sẽ đệ quy vô hạn.",
  "**Chi phí `deepcopy` không tuyến tính theo số phần tử mà theo số object**, và nó thường chậm hơn sao chép nông hàng chục lần — đủ để thành nút cổ chai thật.",
  "**Có ba tầng kiểm soát**: `__copy__`, `__deepcopy__`, và `__reduce__`. Class giữ tài nguyên (file, kết nối) phải cài đặt chúng, nếu không sao chép sẽ hỏng.",
  "**Cách rẻ nhất thường là không sao chép**: dùng dữ liệu bất biến, hoặc thiết kế hàm không sửa đầu vào."
],

can_biet_truoc: [
  "Bài **Tham chiếu vs giá trị** — container lưu con trỏ; đây là nền để hiểu \"nông\" nghĩa là gì.",
  "Bài **Mutable vs immutable** ngay trước — chỉ dữ liệu mutable mới cần sao chép.",
  "Bài **Cú pháp, kiểu dữ liệu** ở module 5 — `copy()` và `deepcopy()` đã giới thiệu ở góc người dùng.",
  "Bài **Magic method** ở module 6 — `__copy__`, `__deepcopy__` là các phương thức đặc biệt."
],

dinh_nghia: "Nói đời thường: bạn photocopy một tập hồ sơ có kẹp ảnh. **Photo nông** là chép các trang giấy nhưng vẫn kẹp đúng những tấm ảnh gốc vào bản mới — sửa ảnh là cả hai bản đổi. **Photo sâu** là chép cả ảnh, nên hai tập hoàn toàn độc lập.\n\nNói chính xác:\n\n- **Sao chép nông** (*shallow copy*) — tạo container mới, **sao chép các con trỏ** bên trong. Các object con được **dùng chung**.\n- **Sao chép sâu** (*deep copy*) — tạo container mới, và **đệ quy sao chép** mọi object con.\n- **Bảng ghi nhớ** (*memo*) — `dict` mà `deepcopy` dùng để nhớ object nào đã sao, ánh xạ `id` gốc → bản sao.\n- **Tham chiếu vòng** — object trỏ tới chính nó, trực tiếp hoặc qua trung gian.\n- **`__copy__` / `__deepcopy__`** — phương thức cho phép class tự quyết định cách nó được sao chép.\n- **`__reduce__`** — giao thức mà cả `copy` lẫn `pickle` dùng khi không có hai phương thức trên.\n\nĐiểm cần nắm ngay: **\"nông\" và \"sâu\" không phải hai lựa chọn tuỳ thích.** Chọn sai theo hướng nào cũng có giá — nông thì dữ liệu dính nhau, sâu thì chậm và có thể sao chép cả những thứ không nên sao. Câu hỏi đúng là *\"tầng nào cần độc lập\"*.",

vi_sao_can: "Sao chép là chỗ mà chọn sai gây lỗi im lặng ở cả hai hướng:\n\n- **Sao chép nông sai gây lỗi dữ liệu khó lần.** Bạn tưởng đã tách hai bản, sửa bản này và bản kia đổi theo — và triệu chứng xuất hiện ở nơi hoàn toàn khác.\n- **Sao chép sâu thừa gây chậm thật.** `deepcopy` trên một cấu trúc lồng nhau lớn tốn hàng trăm mili giây; gọi nó trong vòng lặp là chương trình chậm hàng chục lần mà không rõ vì sao.\n- **`deepcopy` có thể sao chép thứ không nên sao.** Một object giữ kết nối database hoặc file đang mở — sao chép nó cho ra một bản vô nghĩa hoặc gây lỗi khó hiểu.\n- **Nó là chỗ nhiều thư viện phải can thiệp.** Biết `__deepcopy__` tồn tại là biết vì sao vài class hành xử khác khi sao chép.\n- **Định hướng AI/ML.** Sao chép cấu hình thí nghiệm, sao chép trạng thái mô hình, sao chép batch dữ liệu — mỗi thứ cần một mức sao chép khác nhau, và chọn sai thì hoặc chậm hoặc sai.\n\nTình huống thật rất kinh điển: bạn có cấu hình mặc định dạng `dict` lồng nhau, và mỗi lần tạo phiên mới thì `cf = MAC_DINH.copy()`. Mọi thứ chạy tốt cho tới khi ai đó sửa một khoá **ở tầng trong** — và cấu hình mặc định toàn cục bị đổi vĩnh viễn cho mọi phiên sau. Module 5 đã nêu hiện tượng; bài này chỉ rõ vì sao `copy()` không cứu được và ba cách xử lý với ba chi phí khác nhau.",

co_che: "### Nông nghĩa là gì ở tầng bộ nhớ\n\n```python\nimport copy\n\ngoc = [[1, 2], [3, 4]]\nnong = copy.copy(goc)          # hoặc goc.copy(), list(goc), goc[:]\n\nprint(goc is nong)             # False — container KHÁC\nprint(goc[0] is nong[0])       # True  — phần tử CÙNG object\n```\n\n```\ngoc ──► list mới A          nong ──► list mới B\n        [ptr]──┐                     [ptr]──┐\n        [ptr]─┐│                     [ptr]─┐│\n              ││                           ││\n              │└──────► [1, 2] ◄───────────┘│\n              └───────► [3, 4] ◄────────────┘\n                        ⭐ DÙNG CHUNG\n```\n\nSao chép nông làm đúng một việc: **cấp một mảng con trỏ mới và sao chép từng con trỏ vào đó**. Số byte phải sao là `8 × số phần tử`, bất kể object con lớn cỡ nào.\n\nHệ quả trực tiếp:\n\n```python\nnong.append([5, 6])       # ✓ chỉ đổi container B — goc không thấy\nnong[0].append(99)        # ⚠️ sửa object DÙNG CHUNG — goc thấy\nprint(goc)                # [[1, 2, 99], [3, 4]]\n\nnong[0] = [9]             # ✓ ghi đè con trỏ trong B — goc không thấy\nprint(goc[0])             # [1, 2, 99]\n```\n\nBa dòng này là toàn bộ hành vi của sao chép nông, và chúng suy ra được từ sơ đồ trên:\n\n```\nSửa CONTAINER (append, ghi đè ô)   → độc lập\nSửa OBJECT CON qua con trỏ         → dùng chung\n```\n\n### Sâu nghĩa là gì\n\n```python\nsau = copy.deepcopy(goc)\n\nprint(goc[0] is sau[0])        # False — object con cũng MỚI\nsau[0].append(99)\nprint(goc)                      # không đổi ✓\n```\n\n`deepcopy` đi theo **từng con trỏ**, sao chép object nó trỏ tới, rồi đệ quy vào bên trong object đó.\n\nNhưng nó **không sao chép mọi thứ**:\n\n```python\nds = [1, \"abc\", (1, 2), [3]]\nsau = copy.deepcopy(ds)\n\nprint(ds[0] is sau[0])    # True  — int bất biến, không cần sao\nprint(ds[1] is sau[1])    # True  — str bất biến\nprint(ds[2] is sau[2])    # True  — tuple chỉ chứa bất biến\nprint(ds[3] is sau[3])    # False — list mutable ⇒ sao chép\n```\n\nQuy tắc: **`deepcopy` bỏ qua object bất biến** vì không ai sửa được chúng, nên chia sẻ là an toàn. Đây là tối ưu quan trọng — nếu không, sao chép một cấu trúc chứa hàng triệu số nguyên sẽ tạo hàng triệu object thừa.\n\nMột ngoại lệ đáng chú ý:\n\n```python\nt = (1, [2])              # tuple CHỨA mutable\nsau = copy.deepcopy(t)\nprint(t is sau)           # False — phải sao vì bên trong có list\nprint(t[1] is sau[1])     # False\n```\n\n### Bảng ghi nhớ và tham chiếu vòng\n\nĐây là phần thú vị nhất về cơ chế:\n\n```python\na = [1, 2]\na.append(a)               # ⭐ list chứa CHÍNH NÓ\nprint(a)                  # [1, 2, [...]]\n\nsau = copy.deepcopy(a)    # ✓ không đệ quy vô hạn\nprint(sau[2] is sau)      # True — cấu trúc vòng được TÁI TẠO đúng\n```\n\nCách `deepcopy` làm được:\n\n```\nGiữ một dict \"memo\": id(object gốc) → bản sao\n\n① Trước khi sao một object, tra memo\n② Đã có → trả bản sao cũ, KHÔNG sao lại\n③ Chưa có → tạo bản sao, ghi vào memo NGAY, rồi mới đệ quy vào nội dung\n```\n\nBước ③ theo đúng thứ tự đó là mấu chốt: ghi vào memo **trước khi** đệ quy, nên khi đệ quy quay lại chính object đó thì bước ② đã có sẵn câu trả lời.\n\nMemo còn giải quyết một vấn đề tinh tế hơn — **giữ nguyên cấu trúc chia sẻ**:\n\n```python\nchung = [1, 2]\ngoc = [chung, chung]          # HAI ô cùng trỏ MỘT object\n\nprint(goc[0] is goc[1])       # True\n\nsau = copy.deepcopy(goc)\nprint(sau[0] is sau[1])       # True ⭐ vẫn CHUNG, không thành hai bản\n\nsau[0].append(3)\nprint(sau[1])                 # [1, 2, 3] — đúng như gốc\n```\n\nNếu `deepcopy` sao chép mù quáng, hai ô sẽ thành hai object riêng — và **cấu trúc dữ liệu bị thay đổi**, dù nội dung nhìn giống nhau. Memo đảm bảo bản sao có **cùng hình dạng chia sẻ** với bản gốc.\n\nTruyền memo thủ công khi cần:\n\n```python\nmemo = {}\nb1 = copy.deepcopy(goc, memo)\nb2 = copy.deepcopy(goc, memo)     # ⭐ dùng chung memo\nprint(b1 is b2)                    # True — trả lại bản đã sao\n```\n\n### Chi phí — không theo số phần tử mà theo số object\n\n```python\nimport copy, time\n\n# Phẳng, 1 triệu số nguyên\nphang = list(range(1_000_000))\n\nt = time.perf_counter(); copy.copy(phang)\nprint(f\"nông, phẳng : {time.perf_counter()-t:.4f}s\")\n\nt = time.perf_counter(); copy.deepcopy(phang)\nprint(f\"sâu,  phẳng : {time.perf_counter()-t:.4f}s\")\n\n# Lồng nhau, cùng tổng số phần tử\nlong_nhau = [[i] for i in range(100_000)]\n\nt = time.perf_counter(); copy.deepcopy(long_nhau)\nprint(f\"sâu,  lồng  : {time.perf_counter()-t:.4f}s\")\n```\n\nKết quả điển hình cho thấy hai điều:\n\n```\n· deepcopy trên list PHẲNG toàn số nguyên nhanh bất ngờ\n  → vì int bất biến, deepcopy chỉ sao mảng con trỏ\n\n· deepcopy trên cấu trúc LỒNG chậm hơn nhiều dù ít phần tử hơn\n  → mỗi list con là một object phải sao chép + một lần tra memo\n```\n\nCông thức trực giác:\n\n```\nChi phí deepcopy ≈ số object MUTABLE trong cây × (cấp phát + tra memo)\n```\n\nSố phần tử bất biến gần như không tính. Đây là lý do một `dict` cấu hình nhỏ nhưng lồng sâu có thể chậm hơn một `list` triệu phần tử.\n\n### Ba tầng kiểm soát\n\nKhi sao chép một object tự viết, `copy` thử theo thứ tự:\n\n```\n① __deepcopy__(self, memo)   — nếu có, dùng nó (cho deepcopy)\n② __copy__(self)             — nếu có, dùng nó (cho copy)\n③ __reduce_ex__ / __reduce__ — giao thức chung với pickle\n④ Mặc định: tạo object mới, sao chép __dict__\n```\n\nMức ④ là lý do class thường sao chép được mà không cần làm gì. Nhưng nó **sai** với class giữ tài nguyên:\n\n```python\nimport sqlite3, copy\n\nclass Kho:\n    def __init__(self, duong_dan):\n        self.duong_dan = duong_dan\n        self.conn = sqlite3.connect(duong_dan)\n        self.cache = {}\n\nk = Kho(\":memory:\")\ncopy.deepcopy(k)          # TypeError: cannot pickle 'sqlite3.Connection'\n```\n\nSửa bằng `__deepcopy__`:\n\n```python\nclass Kho:\n    def __init__(self, duong_dan):\n        self.duong_dan = duong_dan\n        self.conn = sqlite3.connect(duong_dan)\n        self.cache = {}\n\n    def __deepcopy__(self, memo):\n        moi = Kho.__new__(Kho)            # ⭐ tạo object rỗng, KHÔNG gọi __init__\n        memo[id(self)] = moi              # ⭐ ghi memo TRƯỚC khi đệ quy\n        moi.duong_dan = self.duong_dan\n        moi.cache = copy.deepcopy(self.cache, memo)\n        moi.conn = sqlite3.connect(self.duong_dan)   # ⭐ kết nối MỚI\n        return moi\n```\n\nBa chi tiết trong đoạn này đều bắt buộc:\n\n```\nKho.__new__(Kho)      tạo object mà không chạy __init__ (tránh tác dụng phụ)\nmemo[id(self)] = moi  ghi TRƯỚC khi đệ quy — nếu không, vòng lặp sẽ vô hạn\ntruyền memo xuống      để cấu trúc chia sẻ được giữ đúng\n```\n\nBỏ qua chi tiết thứ hai là lỗi hay gặp nhất khi tự viết `__deepcopy__`.\n\nNgăn sao chép hoàn toàn khi nó vô nghĩa:\n\n```python\nclass KetNoiDuyNhat:\n    def __copy__(self):\n        raise TypeError(f\"{type(self).__name__} không được sao chép\")\n    __deepcopy__ = lambda self, memo: self.__copy__()\n```\n\nĐây là lựa chọn tốt cho object đại diện một tài nguyên duy nhất: **báo lỗi rõ ràng còn hơn cho ra một bản sao vô nghĩa.**\n\n### Bốn cách sao chép nông\n\n```python\nds = [[1], [2]]\n\ncopy.copy(ds)        # tổng quát, dùng được với mọi kiểu hỗ trợ\nds.copy()            # phương thức của list, dict, set\nlist(ds)             # dựng lại từ iterable\nds[:]                # slice toàn bộ\n```\n\nBốn cách cho **cùng kết quả** với `list`, nhưng khác nhau ở chỗ khác:\n\n```\ncopy.copy(x)   ✓ hoạt động với class tự viết, gọi __copy__ nếu có\nx.copy()       ✓ nhanh nhất, nhưng chỉ có ở list/dict/set/bytearray\nlist(x)        ✓ nhận MỌI iterable — kể cả generator ⇒ vật chất hoá luôn\nx[:]           ✓ chỉ với kiểu hỗ trợ slice; giữ đúng kiểu con nếu có\n```\n\nDòng thứ ba đáng nhớ: `list(x)` vừa sao chép vừa **vật chất hoá generator** — đúng mẫu `ds = list(ds)` ở đầu hàm đã gặp ở module 5.\n\nMột khác biệt tinh tế với lớp con:\n\n```python\nclass MyList(list): pass\n\nm = MyList([1, 2])\ntype(copy.copy(m))    # MyList — giữ kiểu\ntype(list(m))         # list   — mất kiểu con\ntype(m[:])            # MyList\n```\n\n### `dict` lồng nhau — bẫy phổ biến nhất\n\n```python\nMAC_DINH = {\n    \"db\": {\"host\": \"localhost\", \"port\": 5432},\n    \"debug\": False,\n}\n\ncf = MAC_DINH.copy()                # ⚠️ NÔNG\ncf[\"debug\"] = True                  # ✓ tầng ngoài — an toàn\ncf[\"db\"][\"host\"] = \"prod.db\"        # ⚠️ tầng trong — SỬA CẢ MAC_DINH\n\nprint(MAC_DINH[\"db\"][\"host\"])       # 'prod.db' ← hỏng vĩnh viễn\n```\n\nBa cách xử lý với ba chi phí:\n\n```python\n# ① deepcopy — đúng nhưng chậm nhất\ncf = copy.deepcopy(MAC_DINH)\n\n# ② Sao chép có chọn lọc — nhanh hơn, cần biết cấu trúc\ncf = {**MAC_DINH, \"db\": {**MAC_DINH[\"db\"]}}\n\n# ③ ⭐ Làm mặc định BẤT BIẾN — không cần sao chép gì\nfrom types import MappingProxyType\n\nMAC_DINH = MappingProxyType({\n    \"db\": MappingProxyType({\"host\": \"localhost\", \"port\": 5432}),\n    \"debug\": False,\n})\nMAC_DINH[\"debug\"] = True     # TypeError ✓ chặn ngay\n```\n\nCách ③ đảo ngược vấn đề: thay vì nhớ sao chép ở mọi nơi dùng, **làm cho việc sửa nhầm trở thành không thể**. Đây là ứng dụng trực tiếp của bài trước — bất biến loại bỏ nhu cầu sao chép phòng thủ.\n\n`MappingProxyType` là lớp bọc chỉ đọc quanh một `dict`: nó **không sao chép**, chỉ chặn ghi. Chi phí bằng không.\n\n### Sao chép có chọn lọc\n\nThường thì bạn không cần sao chép cả cây:\n\n```python\ndef tao_phien(mac_dinh: dict, ghi_de: dict) -> dict:\n    \"\"\"Chỉ sao chép những tầng thật sự sẽ bị sửa.\"\"\"\n    return {\n        **mac_dinh,\n        **ghi_de,\n        \"db\": {**mac_dinh[\"db\"], **ghi_de.get(\"db\", {})},   # ⭐ chỉ tầng này\n    }\n```\n\nSo với `deepcopy`: nhanh hơn nhiều vì chỉ chạm vào phần cần, đổi lại là **phụ thuộc vào việc biết cấu trúc**. Nếu cấu trúc thêm một tầng lồng mà bạn quên cập nhật, bẫy quay lại.\n\nCách chọn:\n\n```\nCấu trúc ỔN ĐỊNH, hiệu năng quan trọng   → sao chép có chọn lọc\nCấu trúc thay đổi, an toàn quan trọng     → deepcopy\nDữ liệu có thể làm bất biến               → ⭐ không sao chép gì\n```\n\n### `copy` và `pickle` — cùng một giao thức\n\n```python\nimport copy, pickle\n\nclass A:\n    def __init__(self, x): self.x = x\n\na = A([1, 2])\nb1 = copy.deepcopy(a)\nb2 = pickle.loads(pickle.dumps(a))     # ⭐ cũng cho bản sao sâu\n```\n\nCả hai dùng `__reduce_ex__` khi không có phương thức chuyên dụng. Hệ quả:\n\n```\n· Class không pickle được thì thường cũng deepcopy không được\n· Sửa __reduce__ ảnh hưởng CẢ HAI\n· pickle chậm hơn deepcopy vì phải tuần tự hoá ra byte rồi đọc lại\n```\n\nDùng `pickle` để sao chép sâu là mẹo hay gặp nhưng **không nên**: chậm hơn, và nó thất bại với những thứ `deepcopy` xử lý được (như hàm cục bộ, lambda).\n\n### Khi nào không nên sao chép\n\n```python\n# ❌ Sao chép phòng xa trong vòng lặp nóng\nfor bg in ban_ghi:\n    tam = copy.deepcopy(cau_hinh)       # ⚠️ mỗi vòng một lần\n    xu_ly(bg, tam)\n\n# ✓ Nếu xu_ly không sửa cau_hinh thì không cần sao chép\nfor bg in ban_ghi:\n    xu_ly(bg, cau_hinh)\n\n# ✓ Nếu có sửa, sao chép MỘT lần ngoài vòng lặp khi được\ntam = copy.deepcopy(cau_hinh)\nfor bg in ban_ghi:\n    tam.update(...)\n    xu_ly(bg, tam)\n```\n\nCâu hỏi trước khi gọi `deepcopy`:\n\n```\n① Object này có thật sự bị sửa không?           không → đừng sao\n② Có làm nó bất biến được không?                 có   → làm, rẻ hơn\n③ Chỉ vài tầng cần độc lập?                      → sao chép có chọn lọc\n④ Cấu trúc phức tạp và an toàn quan trọng?       → deepcopy\n```\n\nBa câu đầu loại bỏ phần lớn các lời gọi `deepcopy` trong code thật.",

vi_du: "### Nhìn thấy nông và sâu bằng `id`\n\n```python\nimport copy\n\ngoc = [[1, 2], [3, 4]]\nnong = copy.copy(goc)\nsau = copy.deepcopy(goc)\n\ndef soi(ten, x):\n    print(f\"{ten:<6} container={id(x):>15}  con[0]={id(x[0]):>15}\")\n\nsoi(\"goc\", goc)\nsoi(\"nông\", nong)      # container KHÁC, con[0] GIỐNG goc\nsoi(\"sâu\", sau)        # cả hai đều KHÁC\n\nprint(f\"nông: container khác={goc is not nong}, con dùng chung={goc[0] is nong[0]}\")\nprint(f\"sâu : container khác={goc is not sau}, con dùng chung={goc[0] is sau[0]}\")\n```\n\nHành vi suy ra từ đó:\n\n```python\ngoc = [[1, 2], [3, 4]]\nnong = copy.copy(goc)\n\nnong.append([9])        # ✓ chỉ container mới\nprint(len(goc), len(nong))       # 2 3\n\nnong[0].append(99)      # ⚠️ object dùng chung\nprint(goc[0])                     # [1, 2, 99]\n\nnong[1] = [\"mới\"]       # ✓ ghi đè con trỏ trong container mới\nprint(goc[1])                     # [3, 4] — không đổi\n```\n\n### `deepcopy` bỏ qua object bất biến\n\n```python\nimport copy\n\nds = [1, \"chuỗi dài không được sao chép\", (1, 2), [3], {\"a\": 1}]\nsau = copy.deepcopy(ds)\n\nfor i, (a, b) in enumerate(zip(ds, sau)):\n    print(f\"[{i}] {type(a).__name__:<5} dùng chung={a is b}\")\n\n# [0] int   dùng chung=True\n# [1] str   dùng chung=True\n# [2] tuple dùng chung=True\n# [3] list  dùng chung=False\n# [4] dict  dùng chung=False\n```\n\nĐây là tối ưu quan trọng: một cấu trúc chứa hàng triệu số nguyên và chuỗi thì `deepcopy` **không tạo bản sao nào cho chúng** — chỉ sao chép các container mutable.\n\nNgoại lệ:\n\n```python\nt = (1, [2])\nsau = copy.deepcopy(t)\nprint(t is sau, t[1] is sau[1])     # False False — tuple chứa mutable thì phải sao\n```\n\n### Tham chiếu vòng\n\n```python\nimport copy\n\n# ① Trỏ tới chính nó\na = [1, 2]\na.append(a)\nprint(a)                     # [1, 2, [...]]\n\nsau = copy.deepcopy(a)\nprint(sau[2] is sau)         # True ⭐ cấu trúc vòng được tái tạo đúng\nprint(sau[2] is a)           # False — và nó là bản MỚI\n\n\n# ② Vòng qua trung gian\nclass Nut:\n    def __init__(self, ten):\n        self.ten = ten\n        self.cha = None\n        self.con = []\n\n    def them_con(self, c):\n        c.cha = self          # ⭐ con trỏ về cha ⇒ tạo vòng\n        self.con.append(c)\n        return c\n\n    def __repr__(self):\n        return f\"Nut({self.ten!r})\"\n\ngoc = Nut(\"gốc\")\nb = goc.them_con(Nut(\"b\"))\nc = goc.them_con(Nut(\"c\"))\n\nsau = copy.deepcopy(goc)\nprint(sau.con[0].cha is sau)        # True ⭐ vòng đúng, không trỏ về gốc cũ\nprint(sau.con[0].cha is goc)        # False\nprint(sau.con[0] is b)              # False — object mới hoàn toàn\n```\n\nKhông có bảng ghi nhớ, ví dụ ② sẽ đệ quy vô hạn: sao `goc` → sao `con[0]` → sao `cha` (chính là `goc`) → sao `con[0]`...\n\n### Bảng ghi nhớ giữ cấu trúc chia sẻ\n\n```python\nimport copy\n\nchung = {\"gia_tri\": 1}\ngoc = {\"a\": chung, \"b\": chung, \"c\": {\"gia_tri\": 1}}\n\nprint(goc[\"a\"] is goc[\"b\"])     # True  — chung một object\nprint(goc[\"a\"] is goc[\"c\"])     # False — nội dung giống, object khác\n\nsau = copy.deepcopy(goc)\nprint(sau[\"a\"] is sau[\"b\"])     # True  ⭐ vẫn chung\nprint(sau[\"a\"] is sau[\"c\"])     # False ⭐ vẫn tách\n\nsau[\"a\"][\"gia_tri\"] = 99\nprint(sau[\"b\"][\"gia_tri\"])      # 99 — đúng như gốc\nprint(sau[\"c\"][\"gia_tri\"])      # 1\nprint(goc[\"a\"][\"gia_tri\"])      # 1  — gốc không bị ảnh hưởng\n```\n\n`deepcopy` không chỉ tạo bản sao — nó **tái tạo đúng hình dạng chia sẻ** của cấu trúc gốc. Nếu nó sao chép mù quáng, `sau[\"a\"]` và `sau[\"b\"]` sẽ thành hai object riêng và ngữ nghĩa dữ liệu bị đổi.\n\nDùng chung memo qua nhiều lần gọi:\n\n```python\nmemo = {}\nb1 = copy.deepcopy(goc, memo)\nb2 = copy.deepcopy(goc, memo)\nprint(b1 is b2)                  # True — memo trả lại bản đã sao\n\nprint(len(memo))                 # số object đã được sao chép\n```\n\n### Đo chi phí thật\n\n```python\nimport copy, time\n\ndef do(ten, f, lan=5):\n    t = time.perf_counter()\n    for _ in range(lan):\n        f()\n    print(f\"{ten:<32} {(time.perf_counter()-t)/lan*1000:>8.2f} ms\")\n\n# Phẳng — 1 triệu int\nphang = list(range(1_000_000))\ndo(\"nông, 1tr int phẳng\", lambda: copy.copy(phang))\ndo(\"sâu,  1tr int phẳng\", lambda: copy.deepcopy(phang))\n\n# Lồng — 100k list con\nlong_nhau = [[i, i + 1] for i in range(100_000)]\ndo(\"nông, 100k list con\", lambda: copy.copy(long_nhau))\ndo(\"sâu,  100k list con\", lambda: copy.deepcopy(long_nhau))\n\n# Dict lồng sâu nhưng nhỏ\nsau_tang = {\"a\": {\"b\": {\"c\": {\"d\": {\"e\": list(range(100))}}}}}\ndo(\"sâu,  dict 5 tầng nhỏ\", lambda: copy.deepcopy(sau_tang))\n```\n\nQuan sát điển hình:\n\n```\n· deepcopy trên 1 triệu int nhanh bất ngờ — int bất biến, không sao\n· deepcopy trên 100k list con chậm hơn nhiều dù ÍT phần tử hơn\n· dict 5 tầng nhỏ rất nhanh — ít object\n```\n\nKết luận: **đếm số object mutable, đừng đếm số phần tử.** Đây là cách ước lượng chi phí `deepcopy` đúng nhất.\n\nSo với sao chép có chọn lọc:\n\n```python\ncf = {\"db\": {\"host\": \"a\", \"port\": 1}, \"cache\": {\"ttl\": 60}, \"debug\": False}\n\ndo(\"deepcopy dict cấu hình\", lambda: copy.deepcopy(cf))\ndo(\"chọn lọc dict cấu hình\",\n   lambda: {**cf, \"db\": {**cf[\"db\"]}, \"cache\": {**cf[\"cache\"]}})\n```\n\nSao chép có chọn lọc thường nhanh hơn vài lần — đáng khi nó nằm trong vòng lặp nóng.\n\n### Bẫy `dict` cấu hình lồng nhau\n\n```python\nimport copy\n\nMAC_DINH = {\n    \"db\": {\"host\": \"localhost\", \"port\": 5432, \"pool\": {\"min\": 1, \"max\": 10}},\n    \"cache\": {\"ttl\": 300},\n    \"debug\": False,\n}\n\n# ❌ Nông\ndef tao_phien_xau(ghi_de):\n    cf = MAC_DINH.copy()\n    cf.update(ghi_de)\n    return cf\n\np1 = tao_phien_xau({\"debug\": True})\np1[\"db\"][\"host\"] = \"prod\"          # ⚠️ sửa luôn MAC_DINH\n\np2 = tao_phien_xau({})\nprint(p2[\"db\"][\"host\"])            # 'prod' ← ô nhiễm sang phiên khác\n\n\n# ✓ ① deepcopy\ndef tao_phien_1(ghi_de):\n    cf = copy.deepcopy(MAC_DINH)\n    cf.update(ghi_de)\n    return cf\n\n# ✓ ② Gộp đệ quy — chỉ sao tầng cần\ndef gop_sau(goc: dict, ghi_de: dict) -> dict:\n    kq = {}\n    for k, v in goc.items():\n        if k in ghi_de and isinstance(v, dict) and isinstance(ghi_de[k], dict):\n            kq[k] = gop_sau(v, ghi_de[k])       # ⭐ đệ quy vào dict lồng\n        elif isinstance(v, dict):\n            kq[k] = {**v}                        # ⭐ sao chép nông một tầng\n        else:\n            kq[k] = v                            # bất biến — dùng chung\n    for k, v in ghi_de.items():\n        if k not in kq:\n            kq[k] = v\n    return kq\n\np = gop_sau(MAC_DINH, {\"db\": {\"host\": \"prod\"}, \"debug\": True})\nprint(p[\"db\"])                     # {'host': 'prod', 'port': 5432, 'pool': {...}}\nprint(MAC_DINH[\"db\"][\"host\"])      # 'localhost' ✓ không bị đụng\n```\n\nHàm `gop_sau` là mẫu rất hay dùng cho cấu hình nhiều tầng: nó **gộp** thay vì ghi đè, và chỉ sao chép những `dict` thật sự cần.\n\n### Làm cấu hình bất biến — cách rẻ nhất\n\n```python\nfrom types import MappingProxyType\n\ndef dong_bang(d: dict) -> MappingProxyType:\n    \"\"\"Đóng băng dict lồng nhau thành chỉ đọc.\"\"\"\n    return MappingProxyType({\n        k: dong_bang(v) if isinstance(v, dict) else\n           tuple(v) if isinstance(v, list) else v\n        for k, v in d.items()\n    })\n\nMAC_DINH = dong_bang({\n    \"db\": {\"host\": \"localhost\", \"port\": 5432},\n    \"the\": [\"a\", \"b\"],\n})\n\nMAC_DINH[\"debug\"] = True           # TypeError ✓\nMAC_DINH[\"db\"][\"host\"] = \"x\"       # TypeError ✓\nMAC_DINH[\"the\"].append(\"c\")        # AttributeError ✓ (đã thành tuple)\n\n# Đọc thì bình thường\nprint(MAC_DINH[\"db\"][\"host\"])      # localhost\n```\n\n`MappingProxyType` **không sao chép** — nó là một lớp bọc chỉ đọc quanh `dict` gốc. Chi phí tạo gần bằng không, và nó chặn mọi phép ghi vô tình.\n\nĐánh đổi: nó chỉ chặn **qua chính nó**. Ai còn giữ tham chiếu tới `dict` gốc vẫn sửa được:\n\n```python\ngoc = {\"a\": 1}\nchi_doc = MappingProxyType(goc)\ngoc[\"a\"] = 99\nprint(chi_doc[\"a\"])                # 99 — proxy phản ánh thay đổi\n```\n\nVì vậy mẫu đúng là **đóng băng ngay lúc tạo và không giữ tham chiếu tới `dict` gốc** — đúng như hàm `dong_bang` ở trên làm.\n\n### `__deepcopy__` cho class giữ tài nguyên\n\n```python\nimport copy, sqlite3\n\nclass KhoDuLieu:\n    def __init__(self, duong_dan: str):\n        self.duong_dan = duong_dan\n        self.conn = sqlite3.connect(duong_dan)\n        self.cache: dict = {}\n        self.thong_ke = {\"doc\": 0, \"ghi\": 0}\n\n    def __deepcopy__(self, memo):\n        moi = KhoDuLieu.__new__(KhoDuLieu)     # ⭐ không gọi __init__\n        memo[id(self)] = moi                    # ⭐ ghi memo TRƯỚC\n\n        moi.duong_dan = self.duong_dan          # str bất biến\n        moi.cache = copy.deepcopy(self.cache, memo)\n        moi.thong_ke = copy.deepcopy(self.thong_ke, memo)\n        moi.conn = sqlite3.connect(self.duong_dan)   # ⭐ kết nối MỚI\n        return moi\n\n    def __repr__(self):\n        return f\"KhoDuLieu({self.duong_dan!r}, cache={len(self.cache)} mục)\"\n\n\nk = KhoDuLieu(\":memory:\")\nk.cache[\"a\"] = [1, 2]\nk.thong_ke[\"doc\"] = 5\n\nk2 = copy.deepcopy(k)\nprint(k2)\nprint(k.conn is k2.conn)             # False ✓ kết nối riêng\nprint(k.cache is k2.cache)           # False ✓ cache riêng\n\nk2.cache[\"a\"].append(3)\nprint(k.cache[\"a\"])                  # [1, 2] ✓ độc lập\n```\n\nBa dòng đánh dấu ⭐ đều bắt buộc. Bỏ dòng `memo[id(self)] = moi` thì mọi tham chiếu vòng sẽ đệ quy vô hạn:\n\n```python\n# Minh hoạ vì sao cần ghi memo trước\nclass Sai:\n    def __init__(self):\n        self.tu_tro = self             # ⭐ vòng\n\n    def __deepcopy__(self, memo):\n        moi = Sai.__new__(Sai)\n        # QUÊN memo[id(self)] = moi\n        moi.tu_tro = copy.deepcopy(self.tu_tro, memo)   # ⇒ đệ quy vô hạn\n        return moi\n\n# copy.deepcopy(Sai())    # RecursionError\n```\n\n### Ngăn sao chép khi nó vô nghĩa\n\n```python\nimport copy\n\nclass KetNoiDuyNhat:\n    \"\"\"Đại diện một kết nối vật lý — sao chép là vô nghĩa.\"\"\"\n\n    def __init__(self, host):\n        self.host = host\n\n    def __copy__(self):\n        raise TypeError(f\"{type(self).__name__} không được sao chép — \"\n                        f\"dùng chung một instance hoặc tạo mới bằng __init__\")\n\n    def __deepcopy__(self, memo):\n        return self.__copy__()\n\n\nk = KetNoiDuyNhat(\"db.local\")\ntry:\n    copy.deepcopy(k)\nexcept TypeError as e:\n    print(f\"✓ {e}\")\n\n# Và nó lan lên container chứa nó\ntry:\n    copy.deepcopy({\"kho\": k})\nexcept TypeError as e:\n    print(f\"✓ container cũng bị chặn: {e}\")\n```\n\nThông báo lỗi nêu rõ **phải làm gì thay thế** — đây là chi tiết quan trọng: một `TypeError` trống rỗng chỉ làm người dùng bối rối.\n\n### Sao chép nông \"đủ sâu\" cho `dataclass`\n\n```python\nfrom dataclasses import dataclass, field, replace\nimport copy\n\n@dataclass\nclass CauHinh:\n    host: str = \"localhost\"\n    port: int = 8000\n    the: list[str] = field(default_factory=list)\n\ncf = CauHinh(the=[\"a\"])\n\n# replace — tạo object MỚI nhưng NÔNG với trường mutable\ncf2 = replace(cf, port=9000)\nprint(cf.the is cf2.the)          # True ⚠️ dùng chung list\n\ncf2.the.append(\"b\")\nprint(cf.the)                      # ['a', 'b'] ⚠️\n\n# ✓ Cách 1 — deepcopy\ncf3 = copy.deepcopy(cf)\n\n# ✓ Cách 2 — replace kèm sao chép trường mutable\ncf4 = replace(cf, port=9000, the=list(cf.the))\ncf4.the.append(\"c\")\nprint(cf.the)                      # không đổi ✓\n\n# ✓ Cách 3 — dùng tuple ngay từ đầu\n@dataclass(frozen=True)\nclass CauHinhTot:\n    host: str = \"localhost\"\n    port: int = 8000\n    the: tuple[str, ...] = ()\n\nc = CauHinhTot(the=(\"a\",))\nc2 = replace(c, port=9000)         # ⭐ không lo gì — mọi trường bất biến\n```\n\n`dataclasses.replace` là **sao chép nông** — điểm dễ bỏ qua vì tên nó gợi ý \"tạo bản mới\". Cách 3 loại bỏ hẳn vấn đề.\n\n### Bốn cách sao chép nông — khác biệt\n\n```python\nimport copy\n\nclass DanhSachRieng(list):\n    def __init__(self, *a):\n        super().__init__(*a)\n        self.nhan = \"gốc\"\n\nm = DanhSachRieng([1, 2])\n\nfor ten, f in [(\"copy.copy\", copy.copy), (\"list()\", list),\n               (\"slice [:]\", lambda x: x[:]), (\".copy()\", lambda x: x.copy())]:\n    kq = f(m)\n    print(f\"{ten:<12} kiểu={type(kq).__name__:<16} \"\n          f\"có .nhan={hasattr(kq, 'nhan')}\")\n\n# copy.copy    kiểu=DanhSachRieng  có .nhan=True   ⭐ giữ đầy đủ\n# list()       kiểu=list           có .nhan=False\n# slice [:]    kiểu=DanhSachRieng  có .nhan=False\n# .copy()      kiểu=list           có .nhan=False\n```\n\nVới `list` thuần thì bốn cách tương đương. Với lớp con thì **chỉ `copy.copy` giữ đủ kiểu và thuộc tính** — vì nó dùng giao thức sao chép, không dựng lại từ nội dung.\n\n### Quy trình quyết định\n\n```python\ndef can_sao_chep(du_lieu, se_sua: bool, co_the_bat_bien: bool,\n                 long_nhau: bool) -> str:\n    if not se_sua:\n        return \"không sao chép — chỉ đọc\"\n    if co_the_bat_bien:\n        return \"đổi sang tuple/frozenset/MappingProxyType — rẻ nhất\"\n    if not long_nhau:\n        return \"copy.copy() hoặc list()/dict() — đủ\"\n    return \"deepcopy, hoặc sao chép có chọn lọc nếu biết cấu trúc\"\n\n\nprint(can_sao_chep([1, 2], se_sua=False, co_the_bat_bien=False, long_nhau=False))\nprint(can_sao_chep([1, 2], se_sua=True, co_the_bat_bien=True, long_nhau=False))\nprint(can_sao_chep([[1]], se_sua=True, co_the_bat_bien=False, long_nhau=True))\n```\n\nHai câu hỏi đầu loại bỏ phần lớn lời gọi `deepcopy` trong code thật — và đó là điểm chính của bài.",

so_sanh: "### Nông và sâu\n\n| | Sao chép nông | Sao chép sâu |\n|---|---|---|\n| Container mới | ✓ | ✓ |\n| Object con | **dùng chung** | **sao chép lại** |\n| Byte phải sao | `8 × số phần tử` | toàn bộ cây mutable |\n| Chi phí | O(số phần tử) | **O(số object mutable)** |\n| Xử lý tham chiếu vòng | không cần | **✓ nhờ memo** |\n| Giữ cấu trúc chia sẻ | — | **✓** |\n| Sửa tầng trong | ảnh hưởng gốc | không |\n\nHàng \"chi phí\" là điểm hay ước lượng sai: `deepcopy` một triệu số nguyên **nhanh**, còn một trăm nghìn list con thì chậm.\n\n### Bốn cách sao chép nông\n\n| | `copy.copy(x)` | `x.copy()` | `list(x)` | `x[:]` |\n|---|---|---|---|---|\n| Mọi kiểu | **✓** | chỉ list/dict/set | iterable | có slice |\n| Gọi `__copy__` | **✓** | ✗ | ✗ | ✗ |\n| Giữ kiểu lớp con | **✓** | ✗ | ✗ | ✓ |\n| Giữ thuộc tính riêng | **✓** | ✗ | ✗ | ✗ |\n| Nhận generator | ✗ | ✗ | **✓** | ✗ |\n| Tốc độ | chậm hơn chút | **nhanh nhất** | nhanh | nhanh |\n\nDùng `copy.copy` cho class tự viết; `list(x)` khi muốn vừa sao chép vừa vật chất hoá generator; `.copy()` khi cần nhanh nhất trên kiểu dựng sẵn.\n\n### `deepcopy` sao chép gì\n\n| Kiểu phần tử | Có sao chép? | Vì sao |\n|---|---|---|\n| `int`, `float`, `str`, `bytes` | **✗** | bất biến — chia sẻ an toàn |\n| `tuple` chỉ chứa bất biến | ✗ | bất biến toàn phần |\n| `tuple` chứa `list` | **✓** | nội dung đổi được |\n| `list`, `dict`, `set` | ✓ | mutable |\n| Class thường | ✓ | sao chép `__dict__` |\n| Class có `__deepcopy__` | theo bạn định nghĩa | |\n\nHàng đầu là tối ưu quan trọng nhất: không có nó, `deepcopy` trên dữ liệu số sẽ chậm gấp nhiều lần.\n\n### Ba tầng kiểm soát sao chép\n\n| Thứ tự | Phương thức | Dùng khi |\n|---|---|---|\n| ① | `__deepcopy__(self, memo)` | class giữ tài nguyên, cần logic riêng |\n| ② | `__copy__(self)` | sao chép nông cần tuỳ chỉnh |\n| ③ | `__reduce_ex__` / `__reduce__` | **chung với `pickle`** |\n| ④ | mặc định: sao `__dict__` | class thường |\n\nHàng ③ giải thích vì sao class không `pickle` được thì thường cũng `deepcopy` không được.\n\n### Ba cách xử lý `dict` lồng nhau\n\n| | `deepcopy` | Sao chép chọn lọc | Làm bất biến |\n|---|---|---|---|\n| An toàn | **✓ mọi tầng** | ✓ tầng đã xử lý | **✓ tuyệt đối** |\n| Tốc độ | chậm nhất | nhanh | **nhanh nhất** |\n| Phụ thuộc biết cấu trúc | ✗ | **✓** | ✗ |\n| Rủi ro khi cấu trúc đổi | không | **quên tầng mới** | không |\n| Chặn lỗi ở đâu | không chặn | không chặn | **lúc ghi** |\n\nCột cuối đảo ngược vấn đề: thay vì nhớ sao chép ở mọi nơi dùng, làm cho việc sửa nhầm **không thể xảy ra**.\n\n### `deepcopy` và `pickle`\n\n| | `copy.deepcopy` | `pickle.loads(pickle.dumps(x))` |\n|---|---|---|\n| Tốc độ | **nhanh hơn** | chậm — qua byte |\n| Hàm, lambda | ✓ (giữ tham chiếu) | **✗** |\n| Kết nối, file | ✗ (trừ khi tự cài) | ✗ |\n| Giao thức | `__deepcopy__` → `__reduce__` | `__reduce__` |\n| Dùng để sao chép | **✓** | ✗ mẹo không nên dùng |\n\n### Khi nào cần sao chép\n\n| Tình huống | Cách rẻ nhất |\n|---|---|\n| Chỉ đọc, không sửa | **không sao chép** |\n| Dữ liệu có thể bất biến | **`tuple`, `frozenset`, `MappingProxyType`** |\n| Mutable phẳng một tầng | `copy.copy()` |\n| Lồng nhau, cấu trúc ổn định | sao chép có chọn lọc |\n| Lồng nhau, cấu trúc thay đổi | `deepcopy` |\n| Trong vòng lặp nóng | **xem lại: có thật sự cần không** |\n\nHai hàng đầu loại bỏ phần lớn lời gọi `deepcopy` trong code thật.",

loi_thuong_gap: [
  "**Dùng `.copy()` cho `dict` lồng nhau rồi sửa tầng trong.** Cấu hình toàn cục bị ô nhiễm vĩnh viễn.",
  "**Gọi `deepcopy` trong vòng lặp nóng.** Chi phí nhân theo số vòng; sao chép một lần ngoài vòng lặp nếu được.",
  "**Ước lượng chi phí `deepcopy` theo số phần tử.** Nó theo **số object mutable** — một triệu int nhanh hơn trăm nghìn list con.",
  "**`deepcopy` một object giữ kết nối hoặc file.** Cho ra bản sao vô nghĩa hoặc `TypeError`. Cài `__deepcopy__` hoặc chặn hẳn.",
  "**Viết `__deepcopy__` mà quên ghi memo trước khi đệ quy.** Tham chiếu vòng gây `RecursionError`.",
  "**Gọi `__init__` trong `__deepcopy__`.** Chạy lại tác dụng phụ của khởi tạo. Dùng `Cls.__new__(Cls)`.",
  "**Quên truyền `memo` xuống các lời gọi `deepcopy` con.** Mất cấu trúc chia sẻ và có thể lặp vô hạn.",
  "**Tưởng `dataclasses.replace` là sao chép sâu.** Nó nông; trường `list` vẫn dùng chung.",
  "**Dùng `pickle` để sao chép sâu.** Chậm hơn và thất bại với lambda, hàm cục bộ.",
  "**Sao chép dữ liệu chỉ đọc.** Nếu không ai sửa thì không cần sao chép gì.",
  "**Dùng `list(x)` cho lớp con của `list` rồi mất kiểu.** Dùng `copy.copy` nếu cần giữ kiểu và thuộc tính.",
  "**Tưởng `MappingProxyType` là bản sao.** Nó là lớp bọc chỉ đọc — sửa `dict` gốc thì proxy vẫn phản ánh."
],

tu_kiem_tra: [
  "Sao chép nông sao chép chính xác cái gì? Vẽ sơ đồ con trỏ.",
  "Ba thao tác nào trên bản sao nông ảnh hưởng bản gốc, và ba thao tác nào thì không?",
  "Vì sao `deepcopy` không sao chép `int` và `str`? Ngoại lệ với `tuple` là gì?",
  "Bảng ghi nhớ hoạt động thế nào? Nêu ba bước theo đúng thứ tự.",
  "Vì sao phải ghi memo **trước** khi đệ quy?",
  "Ngoài chống đệ quy vô hạn, memo còn giải quyết vấn đề gì?",
  "Chi phí `deepcopy` tỉ lệ với cái gì? Vì sao một triệu `int` nhanh hơn trăm nghìn `list`?",
  "Nêu bốn tầng mà `copy` thử khi sao chép một object tự viết.",
  "Vì sao class không `pickle` được thì thường cũng `deepcopy` không được?",
  "Viết `__deepcopy__` cho class giữ kết nối cần ba chi tiết bắt buộc nào?",
  "So sánh bốn cách sao chép nông. Cái nào giữ được kiểu lớp con và thuộc tính riêng?",
  "Nêu ba cách xử lý `dict` cấu hình lồng nhau và chi phí của mỗi cách.",
  "`MappingProxyType` có sao chép không? Nó chặn được gì và không chặn được gì?",
  "`dataclasses.replace` là nông hay sâu? Ba cách xử lý trường `list`?",
  "Nêu bốn câu hỏi nên đặt trước khi gọi `deepcopy`."
],

tai_nguyen: [
  "Tài liệu Python chính thức, module `copy` — mô tả chính xác `copy` và `deepcopy` sao chép gì, và cơ chế memo.",
  "Tài liệu Python, mục *Data model* phần `__copy__`, `__deepcopy__`, `__reduce__` — giao thức đầy đủ.",
  "Tài liệu Python, module `pickle` — phần *Pickling Class Instances* giải thích `__reduce__` mà `copy` dùng chung.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về object mutable và bản sao, gồm cả bản sao phòng thủ.",
  "Tài liệu Python, `types.MappingProxyType` — lớp bọc chỉ đọc, dùng cho cấu hình bất biến."
],

lien_he: [
  "**Tham chiếu vs giá trị**: container lưu con trỏ — nền để hiểu \"nông\" là sao chép mảng con trỏ.",
  "**Mutable vs immutable** ngay trước: chỉ dữ liệu mutable mới cần sao chép; đổi sang bất biến là cách rẻ nhất.",
  "**Garbage collection** ngay sau: tham chiếu vòng ở bài này chính là thứ bộ đếm tham chiếu không dọn được.",
  "**Đo bộ nhớ và tối ưu** cuối module: `deepcopy` là một trong những nguồn tốn bộ nhớ và thời gian bị bỏ sót nhiều nhất.",
  "**Cú pháp, kiểu dữ liệu** ở module 5: `copy()` và `deepcopy()` ở góc người dùng.",
  "**Đóng gói** ở module 6: sao chép phòng thủ khi nhận và khi trả dữ liệu mutable.",
  "**Dataclass và NamedTuple** ở module 6: `replace` là sao chép nông; `frozen=True` với trường `tuple` loại bỏ vấn đề.",
  "**Magic method** ở module 6: `__copy__` và `__deepcopy__` là phương thức đặc biệt, cùng họ với `__reduce__`.",
  "**Đọc ghi file, JSON** ở module 5: `json.loads(json.dumps(x))` là một cách sao chép sâu thô sơ — chỉ đúng với dữ liệu JSON được.",
  "**AI/ML** ở mảng 07: sao chép cấu hình thí nghiệm và trạng thái mô hình là chỗ chọn sai mức sao chép gây cả lỗi lẫn chậm."
]

};
