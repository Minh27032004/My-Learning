window.LESSONS["05:trien-khai:khai-quat-ve-cloud-aws-ec2-s3-rds"] = {

tom_tat: [
  "EC2 cung cấp máy tính, S3 cung cấp object storage, RDS vận hành database; ba dịch vụ giải ba kiểu trạng thái khác nhau của Sổ Chi Tiêu và không thay thế lẫn nhau.",
  "Artifact bất biến, config ngoài build, rollout dần và rollback đã diễn tập.",
  "Kết luận chỉ đáng tin khi có baseline, phép đo/kiểm thử và điều kiện áp dụng được ghi rõ.",
  "Deploy lên Render, Railway hay Fly.io là ánh xạ contract của container vào dịch vụ nền tảng: build/image, command, port, healthcheck, config, storage, region và scaling.",
  "Ba nền tảng đều có thể chạy FastAPI nhưng khác mô hình vận hành; chọn theo workload, database, vùng người dùng và khả năng debug thay vì bảng xếp hạng chung.",
  "Sổ Chi Tiêu API phải stateless, bind `0.0.0.0:$PORT`, dùng PostgreSQL/Redis bền vững bên ngoài instance và fail sớm khi thiếu cấu hình."
],

can_biet_truoc: [
  "Mô hình nền của module “Triển khai”.",
  "Biết đọc Python hoặc pseudocode ở mức cơ bản.",
  "Phân biệt correctness, performance, security và operability; một giải pháp có thể tốt ở trục này nhưng tệ ở trục khác.",
  "Đã học Dockerfile, image digest, process PID 1 và graceful shutdown của Sổ Chi Tiêu API.",
  "Đã học topology app–PostgreSQL–Redis, migration và persistence trong Docker Compose."
],

dinh_nghia: "**Cloud** trong phạm vi bài này là thuê tài nguyên qua API và mô hình trách nhiệm chia sẻ, không phải một nơi trừu tượng khiến vận hành biến mất. EC2 chạy FastAPI/Nginx, S3 giữ ảnh hóa đơn dạng object, RDS giữ giao dịch quan hệ trong PostgreSQL được quản lý.\n\n**Cloud** trong phạm vi bài này là thuê tài nguyên qua API và mô hình trách nhiệm chia sẻ, không phải một nơi trừu tượng khiến vận hành biến mất. EC2 chạy FastAPI/Nginx, S3 giữ ảnh hóa đơn dạng object, RDS giữ giao dịch quan hệ trong PostgreSQL được quản lý.\n\n**Khái quát về cloud: AWS EC2/S3/RDS** thuộc Backend & Web, module **Triển khai**. Hãy định nghĩa nó bằng ba lớp: contract quan sát được từ bên ngoài; state/invariant phải giữ bên trong; và giả định môi trường để guarantee còn đúng. Nếu bỏ lớp giả định, câu “luôn nhanh”, “luôn nhất quán” hay “mô hình tốt” thường trở thành sai.\n\n**Triển khai lên PaaS (Platform as a Service)** là cung cấp source hoặc container image cùng contract runtime để nền tảng build, đặt lịch process, nối mạng, cấp domain/TLS, đưa cấu hình, thu log và thay revision. Render, Railway và Fly.io đều giảm phần quản trị server, nhưng abstraction và topology ưu tiên của chúng khác nhau.\n\nDeploy hoàn tất không phải lúc giao diện báo “successful”, mà khi revision mong muốn nhận traffic, qua readiness, dùng đúng config/schema, tạo được telemetry và có đường phục hồi. Nền tảng điều khiển hạ tầng; đội ứng dụng vẫn chịu trách nhiệm cho tính đúng nghiệp vụ, bảo mật dữ liệu và thiết kế failure.\n\n**ORM (Object–Relational Mapper)** là lớp ánh xạ bảng, cột và quan hệ của relational database sang class, thuộc tính và reference trong code; nó sinh SQL từ biểu thức và chuyển row kết quả thành object. **Session** là vùng làm việc theo dõi các object trong một unit of work, phối hợp flush và transaction trên connection.\n\nORM không phải database và không phải repository tự động đúng. Nó giảm code lặp, giữ parameter binding và mapping nhất quán, nhưng developer vẫn phải hiểu SQL được sinh, cardinality quan hệ, index, isolation và thời điểm I/O xảy ra.\n\n**Triển khai lên PaaS (Platform as a Service)** là cung cấp source hoặc container image cùng contract runtime để nền tảng build, đặt lịch process, nối mạng, cấp domain/TLS, đưa cấu hình, thu log và thay revision. Render, Railway và Fly.io đều giảm phần quản trị server, nhưng abstraction và topology ưu tiên của chúng khác nhau.\n\nDeploy hoàn tất không phải lúc giao diện báo “successful”, mà khi revision mong muốn nhận traffic, qua readiness, dùng đúng config/schema, tạo được telemetry và có đường phục hồi. Nền tảng điều khiển hạ tầng; đội ứng dụng vẫn chịu trách nhiệm cho tính đúng nghiệp vụ, bảo mật dữ liệu và thiết kế failure.\n\n**ORM (Object–Relational Mapper)** là lớp ánh xạ bảng, cột và quan hệ của relational database sang class, thuộc tính và reference trong code; nó sinh SQL từ biểu thức và chuyển row kết quả thành object. **Session** là vùng làm việc theo dõi các object trong một unit of work, phối hợp flush và transaction trên connection.\n\nORM không phải database và không phải repository tự động đúng. Nó giảm code lặp, giữ parameter binding và mapping nhất quán, nhưng developer vẫn phải hiểu SQL được sinh, cardinality quan hệ, index, isolation và thời điểm I/O xảy ra.",

vi_sao_can: "Chủ đề này quan trọng vì nó thay đổi một quyết định trên critical path của hệ thống. Contract, bảo mật và quan sát được quan trọng ngang với happy-path code. Trong project thật, giá trị không nằm ở việc gọi đúng API mà ở khả năng dự đoán điều gì xảy ra khi dữ liệu lớn hơn, dependency chậm, input lệch phân phối, hai thao tác cạnh tranh hoặc deploy phiên bản mới.\n\nMột Docker image chỉ là artifact. Để người dùng gọi được API, cần máy chạy, router nhận HTTPS, DNS, port, healthcheck, secret, database, log và chính sách restart. Tự dựng từng phần trên VM cho quyền kiểm soát cao nhưng tiêu tốn công vận hành, đặc biệt với nhóm nhỏ.\n\nPaaS biến nhiều việc ấy thành cấu hình và API. Lợi ích là thời gian ra mắt, môi trường preview và rollback nhanh hơn. Đánh đổi là giới hạn runtime, giá theo tài nguyên/traffic, hành vi sleep hoặc volume đặc thù và lock-in ở cấu hình. Không có nền tảng “tốt nhất” độc lập với workload.\n\nSổ Chi Tiêu chứa dữ liệu tài chính cá nhân nên lựa chọn còn phải xét region, backup, quyền truy cập, log và khả năng xóa dữ liệu. Một demo trả 200 chưa đủ; cần chứng minh dữ liệu sống qua restart và user không đọc được giao dịch của người khác.\n\nAPI đơn hàng ghi ít nhất hai loại record: `orders` và `order_lines`; đồng thời phải giảm tồn kho. Nếu insert order thành công nhưng process chết trước khi insert line, dữ liệu nửa vời không còn ý nghĩa. Nếu hai request cùng thấy tồn kho bằng 1 rồi cùng trừ, cửa hàng bán quá số có thật. Dictionary trong RAM không giải quyết persistence, nhiều worker hay restart.\n\nViết SQL tay cho mọi endpoint cho quyền kiểm soát cao nhưng mapping, transaction boilerplate và thay đổi schema nhanh chóng lặp. ORM giúp biểu diễn quan hệ và gom unit of work. Cái giá là I/O có thể bị che sau truy cập thuộc tính, query sinh ra không tối ưu, session bị giữ quá lâu và developer tưởng `commit()` chỉ “lưu object” thay vì một ranh giới đồng thời quan trọng.\n\nAPI thật còn đối mặt pool cạn, deadlock, migration lệch phiên bản và thông tin nhạy cảm trong log SQL. Bài này dùng SQLAlchemy theo phong cách hiện đại và SQLite để ví dụ chạy gọn; khi triển khai PostgreSQL/MySQL, hành vi concurrency, kiểu dữ liệu và locking phải được kiểm thử trên đúng engine production.\n\nMột Docker image chỉ là artifact. Để người dùng gọi được API, cần máy chạy, router nhận HTTPS, DNS, port, healthcheck, secret, database, log và chính sách restart. Tự dựng từng phần trên VM cho quyền kiểm soát cao nhưng tiêu tốn công vận hành, đặc biệt với nhóm nhỏ.\n\nPaaS biến nhiều việc ấy thành cấu hình và API. Lợi ích là thời gian ra mắt, môi trường preview và rollback nhanh hơn. Đánh đổi là giới hạn runtime, giá theo tài nguyên/traffic, hành vi sleep hoặc volume đặc thù và lock-in ở cấu hình. Không có nền tảng “tốt nhất” độc lập với workload.\n\nSổ Chi Tiêu chứa dữ liệu tài chính cá nhân nên lựa chọn còn phải xét region, backup, quyền truy cập, log và khả năng xóa dữ liệu. Một demo trả 200 chưa đủ; cần chứng minh dữ liệu sống qua restart và user không đọc được giao dịch của người khác.\n\nAPI đơn hàng ghi ít nhất hai loại record: `orders` và `order_lines`; đồng thời phải giảm tồn kho. Nếu insert order thành công nhưng process chết trước khi insert line, dữ liệu nửa vời không còn ý nghĩa. Nếu hai request cùng thấy tồn kho bằng 1 rồi cùng trừ, cửa hàng bán quá số có thật. Dictionary trong RAM không giải quyết persistence, nhiều worker hay restart.\n\nViết SQL tay cho mọi endpoint cho quyền kiểm soát cao nhưng mapping, transaction boilerplate và thay đổi schema nhanh chóng lặp. ORM giúp biểu diễn quan hệ và gom unit of work. Cái giá là I/O có thể bị che sau truy cập thuộc tính, query sinh ra không tối ưu, session bị giữ quá lâu và developer tưởng `commit()` chỉ “lưu object” thay vì một ranh giới đồng thời quan trọng.\n\nAPI thật còn đối mặt pool cạn, deadlock, migration lệch phiên bản và thông tin nhạy cảm trong log SQL. Bài này dùng SQLAlchemy theo phong cách hiện đại và SQLite để ví dụ chạy gọn; khi triển khai PostgreSQL/MySQL, hành vi concurrency, kiểu dữ liệu và locking phải được kiểm thử trên đúng engine production.",

co_che: "### Ánh xạ hệ thống đã xây sang AWS\n\nBài Nginx tạo biên HTTP, bài HTTPS tạo danh tính tên miền, bài cấu hình tách địa chỉ và secret. Khi chuyển lên AWS, các nguyên tắc không đổi: EC2 chỉ nhận 80/443 từ Internet; RDS chỉ nhận cổng database từ security group ứng dụng; S3 mặc định riêng tư và FastAPI cấp quyền tải có thời hạn.\n\n### Ánh xạ hệ thống đã xây sang AWS\n\nBài Nginx tạo biên HTTP, bài HTTPS tạo danh tính tên miền, bài cấu hình tách địa chỉ và secret. Khi chuyển lên AWS, các nguyên tắc không đổi: EC2 chỉ nhận 80/443 từ Internet; RDS chỉ nhận cổng database từ security group ứng dụng; S3 mặc định riêng tư và FastAPI cấp quyền tải có thời hạn.\n\n### Mental model\n\nArtifact bất biến, config ngoài build, rollout dần và rollback đã diễn tập.\n\n![Sơ đồ Triển khai](assets/diagrams/05-trien-khai.svg)\n\n### Cơ chế từng bước\n\n1. Xác định input và boundary: ai tạo dữ liệu, schema/shape nào hợp lệ, lỗi được biểu diễn ra sao. 2. Theo dõi state qua từng ranh giới trong sơ đồ; tại mỗi bước ghi rõ dữ liệu được copy, cache, biến đổi hay persist. 3. Nêu invariant của **Khái quát về cloud: AWS EC2/S3/RDS** và chỉ ra thao tác nào có thể phá nó. 4. Chọn phép đo: latency percentile, rows scanned, throughput, memory, loss/metric hoặc error budget tùy miền. 5. Dựng phản ví dụ nhỏ nhất trước khi tối ưu.\n\n### Tính đúng đắn và độ phức tạp\n\nKhông ghi Big-O máy móc nếu bottleneck là I/O, network hoặc accelerator. Tách CPU complexity, memory, số round-trip, bytes transferred và contention. Với hệ phân tán/ML, thêm quality/consistency guarantee. Một tối ưu hợp lệ phải giữ contract hoặc công khai contract mới.\n\n### Failure và phục hồi\n\nKiểm ít nhất timeout, retry, partial result, duplicate, stale state và resource exhaustion khi phù hợp. input độc hại, timeout, retry trùng, secret, dependency failure, backward compatibility và overload. Phục hồi phải idempotent hoặc có deduplication; alert phải gắn với tác động người dùng/SLO, không chỉ mức tài nguyên.\n\n### Bắt đầu bằng contract, không bắt đầu bằng nút Deploy\n\nTrước khi chọn nhà cung cấp, ghi contract: image nào; command nào; process nghe cổng nào; biến nào bắt buộc; endpoint nào là live/ready; CPU/RAM tối thiểu; dữ liệu nằm đâu; region nào; migration chạy lúc nào; rollback về revision nào. Contract rõ giúp so nền tảng trên cùng một bài toán.\n\n```text\nInternet -> TLS/router -> web service FastAPI\n                         |-> managed PostgreSQL\n                         |-> managed Redis\nCI -> registry digest -> revision -> health -> traffic\n```\n\nNếu cấu hình chỉ tồn tại qua thao tác UI, review và tái tạo khó. Ưu tiên infrastructure/config as code do nền tảng hỗ trợ, nhưng không ghi secret vào file.\n\n### Source deploy và image deploy\n\nSource deploy tiện: nền tảng phát hiện Python, cài dependency rồi chạy command. Đổi lại buildpack/version có thể thay và artifact giữa nền tảng khác nhau. Image deploy dùng Dockerfile của bài đầu, cho CI build một lần và promotion theo digest; đổi lại phải vận hành registry và cập nhật base image.\n\nVới Sổ Chi Tiêu đã có pipeline, image deploy giữ chuỗi bằng chứng tốt hơn. Prototype nhỏ có thể dùng source deploy để giảm công. Điều quan trọng là ghi builder/runtime version và không để production cài dependency không khóa.\n\n### Port động và địa chỉ bind\n\nPaaS thường đưa cổng vào biến `PORT`. Process phải nghe `0.0.0.0`, vì router ở ngoài network namespace. Command có thể là:\n\n```bash\nuvicorn app.main:app --host 0.0.0.0 --port \"$PORT\"\n```\n\nNếu cấu hình không dùng shell expansion, ứng dụng Python nên đọc `PORT` rồi truyền cho server hoặc dùng cú pháp command của nền tảng. Đừng giả định shell có mặt trong distroless image. Lỗi bind `127.0.0.1` thường cho log “server started” nhưng router nhận connection refused.\n\n### Render: mô hình service rõ ràng\n\nRender phù hợp cách nghĩ web service, background worker, cron job và managed datastore. Với API truyền thống, ta khai build/image, start command, health path và environment. Ưu điểm là tài nguyên hiển thị theo loại service; đổi lại cần hiểu gói dịch vụ, region và giới hạn hiện hành từ tài liệu/bảng giá tại thời điểm triển khai.\n\nKhông ghi con số giá, thời gian sleep hay danh sách region cố định vào bài vì chúng thay đổi. Khi chọn, kiểm tra chính sách hiện tại và chạy proof of concept ở region mục tiêu. Đặc biệt xác minh database backup/restore thay vì suy từ nhãn “managed”.\n\n### Railway: trải nghiệm project và liên kết service\n\nRailway thiên về project gồm service và datastore có thể nối biến cấu hình nhanh. Nó thuận tiện cho demo/full-stack nhỏ khi muốn dựng PostgreSQL/Redis cùng application với ít thao tác. Sự tiện lợi của reference variable không loại nhu cầu biết URL nào public, URL nào private và secret nào được đưa vào process.\n\nRủi ro thực tế là tạo nhiều environment/service thử nghiệm rồi quên tài nguyên tính phí, hoặc phụ thuộc vào magic variable khiến khó chuyển nơi khác. Giữ application đọc chuẩn `DATABASE_URL`, `REDIS_URL`, `PORT`; lớp ánh xạ tên biến nên mỏng.\n\n### Fly.io: process gần người dùng và topology phân tán\n\nFly.io thường hấp dẫn khi muốn chạy machine ở nhiều region hoặc kiểm soát network/process gần mức hạ tầng hơn. Điều đó không tự biến database thành đa vùng. API stateless có thể nhân bản gần người dùng, nhưng mọi request vẫn gọi một PostgreSQL ở region gốc; latency database có thể nuốt hết lợi ích edge.\n\nĐưa dữ liệu tài chính sang multi-region là bài toán consistency, replication và failover, không phải một cờ scale. Bắt đầu một region gần database; chỉ phân tán sau khi đo request path và hiểu semantics khi partition.\n\n### Bảng quyết định phải lấy workload làm tâm\n\n| Câu hỏi | Render | Railway | Fly.io |\n|---|---|---|---|\n| Mental model ban đầu | Web/worker/datastore | Project nối nhiều service | App/machine/region/network |\n| Điểm hấp dẫn | Service types rõ, vận hành web quen thuộc | Dựng stack và environment nhanh | Kiểm soát vị trí process, topology linh hoạt |\n| Điều cần kiểm chứng | Plan, region, health, datastore | Chi phí tài nguyên, private networking, biến liên kết | Region, volume, database latency, machine lifecycle |\n| Hợp với Sổ Chi Tiêu khi | API web một vùng, muốn ít vận hành | Prototype/nhóm nhỏ cần app+data nhanh | Có lý do latency vùng hoặc cần kiểm soát process |\n\nBảng là khung khảo sát, không cam kết tính năng/giá vĩnh viễn. Quyết định cuối dựa trên tài liệu hiện hành và thử nghiệm nhỏ.\n\n### Stateless web process và filesystem tạm\n\nInstance có thể bị thay trong deploy, crash hoặc scale. File upload, SQLite và export lưu trên filesystem tạm có thể biến mất hoặc chỉ tồn tại ở một replica. API không được dựa vào sticky local state.\n\nGiao dịch vào PostgreSQL; file hóa đơn vào object storage; cache vào Redis. Nếu dùng persistent volume của nền tảng, phải hiểu nó gắn region/machine nào và ảnh hưởng scale ra sao. Volume không tự cho nhiều writer an toàn.\n\n### Managed PostgreSQL không xóa trách nhiệm dữ liệu\n\nManaged service có thể lo process, patch và snapshot theo plan, nhưng application vẫn phải quản lý schema, index, connection pool và quyền user. Đặt DB cùng region/private network với API để giảm latency và tránh public exposure khi có thể.\n\nTạo role ứng dụng không có quyền quản trị. Migration dùng role riêng nếu cần DDL. Bật TLS theo contract nhà cung cấp và xác minh certificate đúng cách; không tắt verify chỉ để hết lỗi. Kiểm tra retention backup và thực hiện restore sang database khác trước khi tin vào nó.\n\n### Redis: cache, rate-limit và khả năng suy giảm\n\nNếu managed Redis lỗi, API nên vẫn đọc/ghi giao dịch qua PostgreSQL, có thể chậm hơn. Circuit breaker/timeout ngắn ngăn mỗi request treo theo Redis. Cache không được là nguồn duy nhất cho quyền hoặc số dư.\n\nRate-limit chống brute force có yêu cầu khác cache. Nếu counter mất khi failover, cần lớp giới hạn ở edge/gateway hoặc datastore phù hợp. Nền tảng giúp chạy Redis, không quyết định semantics cho ta.\n\n### Secret và environment\n\nKhai `DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`, `APP_ENV`, `APP_REVISION` trong secret/config store. Staging và production dùng credential khác. Không sao chép nguyên production secret sang preview environment, nơi code PR và nhiều người có quyền truy cập hơn.\n\nỨng dụng validate config ngay startup: thiếu biến thì thoát với thông báo tên biến, không in giá trị. Rotation JWT/database cần kế hoạch hai key hoặc reconnect; thay secret tức thời có thể làm mọi phiên đăng nhập mất hiệu lực.\n\n### Domain, TLS và proxy headers\n\nPaaS thường terminate TLS ở router rồi chuyển HTTP nội bộ. FastAPI cần biết request gốc là HTTPS khi tạo redirect/cookie; chỉ tin forwarded headers từ proxy tin cậy, không tin mọi client tùy tiện gửi `X-Forwarded-For`. Sai cấu hình gây redirect loop, cookie thiếu `Secure` hoặc log sai IP.\n\nCORS không phải cơ chế authentication. Chỉ cho origin frontend cần thiết, nhưng API vẫn xác minh token và ownership. Domain custom cần DNS và certificate; rollout DNS có cache nên giữ endpoint cũ trong giai đoạn chuyển.\n\n### Health, startup và readiness\n\nLiveness chứng minh process phản hồi. Readiness chứng minh revision có thể nhận traffic, bao gồm config/schema thiết yếu. Startup có thể chậm vì import hoặc connection; đặt timeout dựa trên đo thật, không che deadlock bằng timeout vô hạn.\n\nProbe không trả version dependency hay lỗi database chi tiết cho Internet. Log nội bộ mang correlation id và revision. Nếu nền tảng chỉ có một health path, chọn kiểm tra rẻ đại diện cho khả năng phục vụ và bổ sung smoke test từ pipeline.\n\n### Release command và migration\n\nMigration phải chạy đúng một lần với schema tương thích. Nền tảng có thể có release command/job; nếu không, CI gọi job one-off trước rollout. Không gắn `alembic upgrade head` vào command của mọi web replica.\n\nDeploy code cũ/mới chồng nhau là bình thường. Thêm cấu trúc trước, backfill ngoài request path, đổi code, rồi xóa sau. Nếu migration thất bại, không chuyển traffic; lưu log nhưng lọc URL chứa mật khẩu.\n\n### Scaling theo bottleneck thật\n\nScale ngang API tăng process nhưng cũng nhân pool DB. Nếu một instance có 4 worker × pool 5, ba instance có thể mở 60 connection. Nền tảng scale thành công trong khi database từ chối kết nối.\n\nCPU-bound workload có thể cần worker/machine khác; I/O-bound thường hưởng lợi concurrency nhưng phải có timeout. Autoscaling theo CPU không thấy pool exhaustion hay p99 do DB. Chọn metric gần user impact và đặt trần theo capacity downstream.\n\n### Region và đường đi của dữ liệu\n\nĐặt API, DB và Redis cùng region thường quan trọng hơn đặt API gần người dùng nhưng xa DB. Một request tạo expense có nhiều round-trip SQL; khoảng cách giữa app và DB nhân latency. Đo từ region thử nghiệm, không ghi con số tưởng tượng.\n\nYêu cầu cư trú dữ liệu có thể giới hạn region. Log, backup và support access cũng là bản sao dữ liệu phải xét. Chọn nhà cung cấp không chỉ dựa latency.\n\n### Log, metric và dấu vết revision\n\nMỗi log nên có timestamp, level, request id và `APP_REVISION`. Metric tối thiểu gồm request rate, error, latency percentile, saturation pool và dependency timeout. Không log token, note tài chính đầy đủ hay connection URL.\n\nLog retention có chi phí và nghĩa vụ riêng tư. Sampling trace hợp lý hơn ghi mọi payload. Khi incident, dashboard phải trả lời revision nào làm error tăng và rollback có phục hồi không.\n\n### Kiểm tra sau deploy\n\nPipeline gọi health, đăng nhập tài khoản test, đọc một resource được phép và xác nhận request chéo user bị từ chối trong môi trường an toàn. Theo dõi error/latency một khoảng được định nghĩa bởi đội; bài không bịa số phút chung cho mọi hệ thống.\n\nDeploy thành công nhưng smoke fail phải giữ traffic ở revision cũ hoặc rollback digest. Nếu platform rollout tức thời không canary, giảm blast radius bằng staging và release nhỏ.\n\n### Rollback và dữ liệu\n\nRollback application chọn image digest trước, giữ environment/schema tương thích. Không rebuild tag cũ. Nếu migration đã xóa cột hoặc code mới ghi enum mới, image cũ có thể crash; expand–contract là điều kiện cho rollback.\n\nConfiguration cũng có revision. Đổi đồng thời code, secret, database plan và DNS khiến khó biết nguyên nhân. Tách thay đổi, lưu audit và có runbook phục hồi.\n\n### Preview environment không được thành cửa sau\n\nPreview cho PR rất hữu ích nhưng chạy code chưa tin cậy. Không cấp production database, cloud key hoặc dữ liệu thật. Dùng dataset giả, credential giới hạn, expiration tự động và domain rõ để tránh người dùng nhầm.\n\nChi phí preview tăng theo số PR; cleanup phải tự động. Một preview bị bỏ quên vẫn là endpoint có dependency cũ và bề mặt tấn công.\n\n### Chi phí và giới hạn\n\nPaaS giảm nhân công nhưng giá có thể tăng theo RAM, egress, build minute, log và datastore. Free/entry plan thích hợp học tập có thể sleep, giới hạn connection hoặc thiếu backup; phải đọc điều khoản hiện hành trước production.\n\nTối ưu quá sớm bằng chuyển platform thường đắt hơn sửa query/pool. Theo dõi cost theo environment/service và đặt cảnh báo. Đừng mua high availability cho cache vô nghĩa trong khi database chưa có restore test.\n\n### Khi nào không nên chọn PaaS này\n\nKhông chọn chỉ vì tutorial ngắn. Workload cần kernel module, GPU đặc thù, network appliance, compliance riêng hoặc chi phí ổn định ở quy mô lớn có thể hợp VM/Kubernetes/cloud native khác. Ngược lại, Kubernetes cho API nhỏ có thể làm đội ngũ dành nhiều thời gian cho control plane hơn sản phẩm.\n\nKhả năng rời nền tảng tốt nhất đến từ image chuẩn, protocol chuẩn, config ngoài code, database portable và backup có thể restore; không đến từ việc tránh mọi dịch vụ managed. Chấp nhận lock-in có ý thức khi lợi ích vận hành lớn hơn chi phí chuyển.\n\n### Mapping không làm relational model biến mất\n\nClass `Order` có collection `lines`, nhưng database lưu foreign key `order_lines.order_id`. Khi đọc collection, ORM phải phát SQL JOIN hoặc SELECT khác. Object graph khiến code dễ đọc; relational model quyết định số row, constraint và kế hoạch thực thi. Nếu không biết query thật, developer dễ coi truy cập thuộc tính là O(1) trong khi nó là network round-trip.\n\nTên bảng/cột, nullability và foreign key là contract dữ liệu. Type hint Python không thay `NOT NULL`; validator API không bảo vệ script hay service khác ghi trực tiếp. Invariant quan trọng nên có lớp phòng thủ database khi diễn đạt được.\n\n### Engine, connection và session là ba thứ khác nhau\n\nEngine giữ cấu hình dialect và connection pool. Connection là một phiên kết nối vật lý/logical tới database. Session là unit of work cấp ORM, mượn connection khi cần và có identity map. Tạo engine mỗi request phá pooling; giữ một session global tạo race và state cũ giữa người dùng.\n\nThông thường engine/session factory sống theo process, còn session sống theo request hoặc use case. “Một session mỗi request” là default tiện, không phải luật tuyệt đối: streaming dài hay background job cần ranh giới riêng.\n\n### Identity map và trạng thái entity\n\nTrong một session, tải cùng primary key thường trả cùng object Python. Identity map đảm bảo thay đổi nhất quán trong unit of work và tránh tạo hai đại diện xung đột. Nó không phải cache toàn hệ thống: session khác hoặc process khác không thấy object ấy, và dữ liệu có thể stale.\n\nEntity đi qua trạng thái transient, pending, persistent, detached. Truy cập lazy relationship trên detached object có thể lỗi vì session đã đóng. Response serialization không nên bất ngờ kích hoạt query sau cleanup; hãy tải đủ dữ liệu và map sang response DTO trước khi rời use case.\n\n### Flush khác commit\n\n`session.add(order)` chỉ đăng ký thay đổi. Flush phát SQL trong transaction, thường tự chạy trước query hoặc commit. Flush có thể cấp ID từ database và phát hiện constraint sớm, nhưng transaction chưa bền vững với bên ngoài. Commit kết thúc transaction thành công; rollback bỏ thay đổi chưa commit.\n\nĐừng trả 201 trước khi commit nếu contract hứa đơn đã được nhận bền vững. Ngược lại, commit giữa chừng order rồi commit line sau phá tính nguyên tử. Một use case nên sở hữu quyết định commit.\n\n### Transaction tạo đơn\n\nLuồng an toàn: bắt đầu transaction; kiểm/giảm tồn kho bằng thao tác chống cạnh tranh; insert order; insert lines; commit. Bất kỳ lỗi nào rollback toàn bộ. Gọi cổng thanh toán bên trong transaction database lâu dài lại nguy hiểm: network chậm giữ lock và connection. Thường cần outbox/saga hoặc tách trạng thái `pending_payment`.\n\nTransaction chỉ nguyên tử trong một database. Email, message broker và payment API không tự rollback theo SQL. Outbox ghi event cùng transaction rồi worker gửi sau giúp tránh “đã tạo đơn nhưng mất event”.\n\n### Chống overselling bằng thao tác nguyên tử\n\nCách ngây thơ `SELECT stock`, kiểm trong Python, rồi `UPDATE` có race. Hai transaction cùng đọc 1 trước khi transaction kia ghi. Một cách là update có điều kiện:\n\n```sql\nUPDATE products\nSET stock = stock - :quantity\nWHERE id = :id AND stock >= :quantity;\n```\n\nNếu affected rows bằng 0, sản phẩm không tồn tại hoặc không đủ tồn. Thao tác này để database phân xử atomically. Một lựa chọn khác là khóa row `SELECT ... FOR UPDATE` trên engine hỗ trợ, nhưng phải khóa theo thứ tự ổn định để giảm deadlock. Constraint `CHECK (stock >= 0)` là hàng rào cuối, song chỉ constraint không cho thông báo nghiệp vụ đẹp.\n\n### Isolation và retry có giới hạn\n\nIsolation level quyết định transaction nhìn thấy thay đổi cạnh tranh nào. Mức mạnh hơn giảm anomaly nhưng có thể tăng lock, abort và latency. Không chọn theo khẩu hiệu; xác định invariant rồi test lịch xen kẽ.\n\nDeadlock là hệ quả có thể xảy ra của locking, không nhất thiết là bug database. Database hủy một transaction; ứng dụng có thể retry toàn unit of work với backoff. Chỉ retry khi thao tác idempotent hoặc chưa phát side effect ngoài transaction. Retry vô hạn tạo bão tải.\n\n### N+1: vòng lặp Python, nhiều chuyến mạng\n\nTải 100 order bằng một SELECT rồi mỗi lần truy cập `order.lines` phát thêm SELECT tạo 101 query. Big-O của code nhìn như O(n), nhưng chi phí n round-trip thống trị. SQL logging hoặc query counter trong test phát hiện rõ.\n\n`selectinload` thường dùng một query cho parent và một query `WHERE order_id IN (...)` cho children. `joinedload` dùng JOIN một query, nhưng order có nhiều lines làm lặp cột parent và có thể tạo tích Descartes nếu eager nhiều collection. Không có eager strategy thắng mọi trường hợp.\n\n### Projection tốt hơn tải cả entity cho màn hình đọc\n\nEndpoint summary chỉ cần ID, status và total không nhất thiết tạo full entity, relationship và tracking. SELECT đúng cột hoặc DTO projection giảm byte, memory và nguy cơ lazy load. Entity hữu ích khi thực hiện behavior và thay đổi state; read model có thể tối ưu theo query.\n\nRepository không nên trả một “queryable” sống ra ngoài rồi để route tùy ý nối filter. Điều đó làm transaction/I/O rò qua boundary và test khó biết query nào chạy.\n\n### Connection pool là tài nguyên hữu hạn\n\nPool tái sử dụng connection đắt đỏ. Nếu mỗi request giữ connection trong lúc gọi payment API, chỉ vài request chậm có thể làm request khác timeout chờ pool. Dependency phải đóng session trong `finally`; transaction cần ngắn.\n\nPool size không nên tăng mù quáng. Database có giới hạn connection và mỗi connection tốn memory. Nhiều app replica nhân pool size lên; tổng mới là con số database thấy. Metric cần có thời gian checkout, số connection đang dùng và timeout.\n\n### Async ORM chỉ có ý nghĩa với driver async\n\nSQLAlchemy async dùng `AsyncSession` và driver bất đồng bộ. Nó cho event loop làm request khác khi chờ socket, không làm query SQL chạy nhanh hơn. Không dùng cùng một `AsyncSession` đồng thời cho nhiều task; session mang transaction state mutable.\n\nLazy loading ngầm trong async đặc biệt khó vì truy cập thuộc tính không có chỗ `await`. Tải quan hệ chủ động hoặc projection làm I/O hiển thị. Nếu stack hiện tại đồng bộ và tải vừa, sync ORM trong thread worker có thể đơn giản, đáng tin hơn migration async nửa vời.\n\n### Parameter binding và giới hạn bảo mật\n\nBiểu thức ORM bind value thành parameter, giảm SQL injection so với nối chuỗi. Nhưng tên cột/order-by động không phải value parameter; phải chọn từ allowlist, không ghép raw input. Raw SQL vẫn hợp lý cho query đặc biệt nếu bind parameter đúng.\n\nTài khoản runtime chỉ cần quyền SELECT/INSERT/UPDATE cần thiết, không nên là superuser hay có quyền migration. Secret database đến từ secret manager/biến môi trường, không hard-code và không hiện trong exception/log. Multi-tenant cần tenant predicate bắt buộc hoặc row-level security; một filter bị quên có thể thành data breach.\n\n### Migration là một phần deployment\n\nThay class ORM không tự sửa database production một cách an toàn. Migration có version tạo schema chuyển tiếp. Deploy zero-downtime thường theo expand–migrate–contract: thêm cấu trúc tương thích, deploy code đọc/ghi hai dạng nếu cần, backfill, rồi mới xóa cũ.\n\nMigration khóa bảng lớn có thể gây downtime dù code đúng. Hãy test trên dữ liệu đại diện, backup/restore và quan sát lock. Không chạy `create_all` như chiến lược migration production.\n\n### Khi nào không nên dùng ORM\n\nQuery phân tích phức tạp, bulk load cực lớn, tận dụng tính năng riêng engine hoặc service rất nhỏ với vài câu SQL có thể rõ hơn bằng SQL/Core. Không nên ép mọi query qua repository CRUD chung khiến SQL tệ. ORM đáng dùng khi mapping và unit of work đem lại giá trị; quyền xem SQL thật phải luôn còn.\n\n### Bắt đầu bằng contract, không bắt đầu bằng nút Deploy\n\nTrước khi chọn nhà cung cấp, ghi contract: image nào; command nào; process nghe cổng nào; biến nào bắt buộc; endpoint nào là live/ready; CPU/RAM tối thiểu; dữ liệu nằm đâu; region nào; migration chạy lúc nào; rollback về revision nào. Contract rõ giúp so nền tảng trên cùng một bài toán.\n\n```text\nInternet -> TLS/router -> web service FastAPI\n                         |-> managed PostgreSQL\n                         |-> managed Redis\nCI -> registry digest -> revision -> health -> traffic\n```\n\nNếu cấu hình chỉ tồn tại qua thao tác UI, review và tái tạo khó. Ưu tiên infrastructure/config as code do nền tảng hỗ trợ, nhưng không ghi secret vào file.\n\n### Source deploy và image deploy\n\nSource deploy tiện: nền tảng phát hiện Python, cài dependency rồi chạy command. Đổi lại buildpack/version có thể thay và artifact giữa nền tảng khác nhau. Image deploy dùng Dockerfile của bài đầu, cho CI build một lần và promotion theo digest; đổi lại phải vận hành registry và cập nhật base image.\n\nVới Sổ Chi Tiêu đã có pipeline, image deploy giữ chuỗi bằng chứng tốt hơn. Prototype nhỏ có thể dùng source deploy để giảm công. Điều quan trọng là ghi builder/runtime version và không để production cài dependency không khóa.\n\n### Port động và địa chỉ bind\n\nPaaS thường đưa cổng vào biến `PORT`. Process phải nghe `0.0.0.0`, vì router ở ngoài network namespace. Command có thể là:\n\n```bash\nuvicorn app.main:app --host 0.0.0.0 --port \"$PORT\"\n```\n\nNếu cấu hình không dùng shell expansion, ứng dụng Python nên đọc `PORT` rồi truyền cho server hoặc dùng cú pháp command của nền tảng. Đừng giả định shell có mặt trong distroless image. Lỗi bind `127.0.0.1` thường cho log “server started” nhưng router nhận connection refused.\n\n### Render: mô hình service rõ ràng\n\nRender phù hợp cách nghĩ web service, background worker, cron job và managed datastore. Với API truyền thống, ta khai build/image, start command, health path và environment. Ưu điểm là tài nguyên hiển thị theo loại service; đổi lại cần hiểu gói dịch vụ, region và giới hạn hiện hành từ tài liệu/bảng giá tại thời điểm triển khai.\n\nKhông ghi con số giá, thời gian sleep hay danh sách region cố định vào bài vì chúng thay đổi. Khi chọn, kiểm tra chính sách hiện tại và chạy proof of concept ở region mục tiêu. Đặc biệt xác minh database backup/restore thay vì suy từ nhãn “managed”.\n\n### Railway: trải nghiệm project và liên kết service\n\nRailway thiên về project gồm service và datastore có thể nối biến cấu hình nhanh. Nó thuận tiện cho demo/full-stack nhỏ khi muốn dựng PostgreSQL/Redis cùng application với ít thao tác. Sự tiện lợi của reference variable không loại nhu cầu biết URL nào public, URL nào private và secret nào được đưa vào process.\n\nRủi ro thực tế là tạo nhiều environment/service thử nghiệm rồi quên tài nguyên tính phí, hoặc phụ thuộc vào magic variable khiến khó chuyển nơi khác. Giữ application đọc chuẩn `DATABASE_URL`, `REDIS_URL`, `PORT`; lớp ánh xạ tên biến nên mỏng.\n\n### Fly.io: process gần người dùng và topology phân tán\n\nFly.io thường hấp dẫn khi muốn chạy machine ở nhiều region hoặc kiểm soát network/process gần mức hạ tầng hơn. Điều đó không tự biến database thành đa vùng. API stateless có thể nhân bản gần người dùng, nhưng mọi request vẫn gọi một PostgreSQL ở region gốc; latency database có thể nuốt hết lợi ích edge.\n\nĐưa dữ liệu tài chính sang multi-region là bài toán consistency, replication và failover, không phải một cờ scale. Bắt đầu một region gần database; chỉ phân tán sau khi đo request path và hiểu semantics khi partition.\n\n### Bảng quyết định phải lấy workload làm tâm\n\n| Câu hỏi | Render | Railway | Fly.io |\n|---|---|---|---|\n| Mental model ban đầu | Web/worker/datastore | Project nối nhiều service | App/machine/region/network |\n| Điểm hấp dẫn | Service types rõ, vận hành web quen thuộc | Dựng stack và environment nhanh | Kiểm soát vị trí process, topology linh hoạt |\n| Điều cần kiểm chứng | Plan, region, health, datastore | Chi phí tài nguyên, private networking, biến liên kết | Region, volume, database latency, machine lifecycle |\n| Hợp với Sổ Chi Tiêu khi | API web một vùng, muốn ít vận hành | Prototype/nhóm nhỏ cần app+data nhanh | Có lý do latency vùng hoặc cần kiểm soát process |\n\nBảng là khung khảo sát, không cam kết tính năng/giá vĩnh viễn. Quyết định cuối dựa trên tài liệu hiện hành và thử nghiệm nhỏ.\n\n### Stateless web process và filesystem tạm\n\nInstance có thể bị thay trong deploy, crash hoặc scale. File upload, SQLite và export lưu trên filesystem tạm có thể biến mất hoặc chỉ tồn tại ở một replica. API không được dựa vào sticky local state.\n\nGiao dịch vào PostgreSQL; file hóa đơn vào object storage; cache vào Redis. Nếu dùng persistent volume của nền tảng, phải hiểu nó gắn region/machine nào và ảnh hưởng scale ra sao. Volume không tự cho nhiều writer an toàn.\n\n### Managed PostgreSQL không xóa trách nhiệm dữ liệu\n\nManaged service có thể lo process, patch và snapshot theo plan, nhưng application vẫn phải quản lý schema, index, connection pool và quyền user. Đặt DB cùng region/private network với API để giảm latency và tránh public exposure khi có thể.\n\nTạo role ứng dụng không có quyền quản trị. Migration dùng role riêng nếu cần DDL. Bật TLS theo contract nhà cung cấp và xác minh certificate đúng cách; không tắt verify chỉ để hết lỗi. Kiểm tra retention backup và thực hiện restore sang database khác trước khi tin vào nó.\n\n### Redis: cache, rate-limit và khả năng suy giảm\n\nNếu managed Redis lỗi, API nên vẫn đọc/ghi giao dịch qua PostgreSQL, có thể chậm hơn. Circuit breaker/timeout ngắn ngăn mỗi request treo theo Redis. Cache không được là nguồn duy nhất cho quyền hoặc số dư.\n\nRate-limit chống brute force có yêu cầu khác cache. Nếu counter mất khi failover, cần lớp giới hạn ở edge/gateway hoặc datastore phù hợp. Nền tảng giúp chạy Redis, không quyết định semantics cho ta.\n\n### Secret và environment\n\nKhai `DATABASE_URL`, `REDIS_URL`, `JWT_SECRET`, `APP_ENV`, `APP_REVISION` trong secret/config store. Staging và production dùng credential khác. Không sao chép nguyên production secret sang preview environment, nơi code PR và nhiều người có quyền truy cập hơn.\n\nỨng dụng validate config ngay startup: thiếu biến thì thoát với thông báo tên biến, không in giá trị. Rotation JWT/database cần kế hoạch hai key hoặc reconnect; thay secret tức thời có thể làm mọi phiên đăng nhập mất hiệu lực.\n\n### Domain, TLS và proxy headers\n\nPaaS thường terminate TLS ở router rồi chuyển HTTP nội bộ. FastAPI cần biết request gốc là HTTPS khi tạo redirect/cookie; chỉ tin forwarded headers từ proxy tin cậy, không tin mọi client tùy tiện gửi `X-Forwarded-For`. Sai cấu hình gây redirect loop, cookie thiếu `Secure` hoặc log sai IP.\n\nCORS không phải cơ chế authentication. Chỉ cho origin frontend cần thiết, nhưng API vẫn xác minh token và ownership. Domain custom cần DNS và certificate; rollout DNS có cache nên giữ endpoint cũ trong giai đoạn chuyển.\n\n### Health, startup và readiness\n\nLiveness chứng minh process phản hồi. Readiness chứng minh revision có thể nhận traffic, bao gồm config/schema thiết yếu. Startup có thể chậm vì import hoặc connection; đặt timeout dựa trên đo thật, không che deadlock bằng timeout vô hạn.\n\nProbe không trả version dependency hay lỗi database chi tiết cho Internet. Log nội bộ mang correlation id và revision. Nếu nền tảng chỉ có một health path, chọn kiểm tra rẻ đại diện cho khả năng phục vụ và bổ sung smoke test từ pipeline.\n\n### Release command và migration\n\nMigration phải chạy đúng một lần với schema tương thích. Nền tảng có thể có release command/job; nếu không, CI gọi job one-off trước rollout. Không gắn `alembic upgrade head` vào command của mọi web replica.\n\nDeploy code cũ/mới chồng nhau là bình thường. Thêm cấu trúc trước, backfill ngoài request path, đổi code, rồi xóa sau. Nếu migration thất bại, không chuyển traffic; lưu log nhưng lọc URL chứa mật khẩu.\n\n### Scaling theo bottleneck thật\n\nScale ngang API tăng process nhưng cũng nhân pool DB. Nếu một instance có 4 worker × pool 5, ba instance có thể mở 60 connection. Nền tảng scale thành công trong khi database từ chối kết nối.\n\nCPU-bound workload có thể cần worker/machine khác; I/O-bound thường hưởng lợi concurrency nhưng phải có timeout. Autoscaling theo CPU không thấy pool exhaustion hay p99 do DB. Chọn metric gần user impact và đặt trần theo capacity downstream.\n\n### Region và đường đi của dữ liệu\n\nĐặt API, DB và Redis cùng region thường quan trọng hơn đặt API gần người dùng nhưng xa DB. Một request tạo expense có nhiều round-trip SQL; khoảng cách giữa app và DB nhân latency. Đo từ region thử nghiệm, không ghi con số tưởng tượng.\n\nYêu cầu cư trú dữ liệu có thể giới hạn region. Log, backup và support access cũng là bản sao dữ liệu phải xét. Chọn nhà cung cấp không chỉ dựa latency.\n\n### Log, metric và dấu vết revision\n\nMỗi log nên có timestamp, level, request id và `APP_REVISION`. Metric tối thiểu gồm request rate, error, latency percentile, saturation pool và dependency timeout. Không log token, note tài chính đầy đủ hay connection URL.\n\nLog retention có chi phí và nghĩa vụ riêng tư. Sampling trace hợp lý hơn ghi mọi payload. Khi incident, dashboard phải trả lời revision nào làm error tăng và rollback có phục hồi không.\n\n### Kiểm tra sau deploy\n\nPipeline gọi health, đăng nhập tài khoản test, đọc một resource được phép và xác nhận request chéo user bị từ chối trong môi trường an toàn. Theo dõi error/latency một khoảng được định nghĩa bởi đội; bài không bịa số phút chung cho mọi hệ thống.\n\nDeploy thành công nhưng smoke fail phải giữ traffic ở revision cũ hoặc rollback digest. Nếu platform rollout tức thời không canary, giảm blast radius bằng staging và release nhỏ.\n\n### Rollback và dữ liệu\n\nRollback application chọn image digest trước, giữ environment/schema tương thích. Không rebuild tag cũ. Nếu migration đã xóa cột hoặc code mới ghi enum mới, image cũ có thể crash; expand–contract là điều kiện cho rollback.\n\nConfiguration cũng có revision. Đổi đồng thời code, secret, database plan và DNS khiến khó biết nguyên nhân. Tách thay đổi, lưu audit và có runbook phục hồi.\n\n### Preview environment không được thành cửa sau\n\nPreview cho PR rất hữu ích nhưng chạy code chưa tin cậy. Không cấp production database, cloud key hoặc dữ liệu thật. Dùng dataset giả, credential giới hạn, expiration tự động và domain rõ để tránh người dùng nhầm.\n\nChi phí preview tăng theo số PR; cleanup phải tự động. Một preview bị bỏ quên vẫn là endpoint có dependency cũ và bề mặt tấn công.\n\n### Chi phí và giới hạn\n\nPaaS giảm nhân công nhưng giá có thể tăng theo RAM, egress, build minute, log và datastore. Free/entry plan thích hợp học tập có thể sleep, giới hạn connection hoặc thiếu backup; phải đọc điều khoản hiện hành trước production.\n\nTối ưu quá sớm bằng chuyển platform thường đắt hơn sửa query/pool. Theo dõi cost theo environment/service và đặt cảnh báo. Đừng mua high availability cho cache vô nghĩa trong khi database chưa có restore test.\n\n### Khi nào không nên chọn PaaS này\n\nKhông chọn chỉ vì tutorial ngắn. Workload cần kernel module, GPU đặc thù, network appliance, compliance riêng hoặc chi phí ổn định ở quy mô lớn có thể hợp VM/Kubernetes/cloud native khác. Ngược lại, Kubernetes cho API nhỏ có thể làm đội ngũ dành nhiều thời gian cho control plane hơn sản phẩm.\n\nKhả năng rời nền tảng tốt nhất đến từ image chuẩn, protocol chuẩn, config ngoài code, database portable và backup có thể restore; không đến từ việc tránh mọi dịch vụ managed. Chấp nhận lock-in có ý thức khi lợi ích vận hành lớn hơn chi phí chuyển.\n\n### Mapping không làm relational model biến mất\n\nClass `Order` có collection `lines`, nhưng database lưu foreign key `order_lines.order_id`. Khi đọc collection, ORM phải phát SQL JOIN hoặc SELECT khác. Object graph khiến code dễ đọc; relational model quyết định số row, constraint và kế hoạch thực thi. Nếu không biết query thật, developer dễ coi truy cập thuộc tính là O(1) trong khi nó là network round-trip.\n\nTên bảng/cột, nullability và foreign key là contract dữ liệu. Type hint Python không thay `NOT NULL`; validator API không bảo vệ script hay service khác ghi trực tiếp. Invariant quan trọng nên có lớp phòng thủ database khi diễn đạt được.\n\n### Engine, connection và session là ba thứ khác nhau\n\nEngine giữ cấu hình dialect và connection pool. Connection là một phiên kết nối vật lý/logical tới database. Session là unit of work cấp ORM, mượn connection khi cần và có identity map. Tạo engine mỗi request phá pooling; giữ một session global tạo race và state cũ giữa người dùng.\n\nThông thường engine/session factory sống theo process, còn session sống theo request hoặc use case. “Một session mỗi request” là default tiện, không phải luật tuyệt đối: streaming dài hay background job cần ranh giới riêng.\n\n### Identity map và trạng thái entity\n\nTrong một session, tải cùng primary key thường trả cùng object Python. Identity map đảm bảo thay đổi nhất quán trong unit of work và tránh tạo hai đại diện xung đột. Nó không phải cache toàn hệ thống: session khác hoặc process khác không thấy object ấy, và dữ liệu có thể stale.\n\nEntity đi qua trạng thái transient, pending, persistent, detached. Truy cập lazy relationship trên detached object có thể lỗi vì session đã đóng. Response serialization không nên bất ngờ kích hoạt query sau cleanup; hãy tải đủ dữ liệu và map sang response DTO trước khi rời use case.\n\n### Flush khác commit\n\n`session.add(order)` chỉ đăng ký thay đổi. Flush phát SQL trong transaction, thường tự chạy trước query hoặc commit. Flush có thể cấp ID từ database và phát hiện constraint sớm, nhưng transaction chưa bền vững với bên ngoài. Commit kết thúc transaction thành công; rollback bỏ thay đổi chưa commit.\n\nĐừng trả 201 trước khi commit nếu contract hứa đơn đã được nhận bền vững. Ngược lại, commit giữa chừng order rồi commit line sau phá tính nguyên tử. Một use case nên sở hữu quyết định commit.\n\n### Transaction tạo đơn\n\nLuồng an toàn: bắt đầu transaction; kiểm/giảm tồn kho bằng thao tác chống cạnh tranh; insert order; insert lines; commit. Bất kỳ lỗi nào rollback toàn bộ. Gọi cổng thanh toán bên trong transaction database lâu dài lại nguy hiểm: network chậm giữ lock và connection. Thường cần outbox/saga hoặc tách trạng thái `pending_payment`.\n\nTransaction chỉ nguyên tử trong một database. Email, message broker và payment API không tự rollback theo SQL. Outbox ghi event cùng transaction rồi worker gửi sau giúp tránh “đã tạo đơn nhưng mất event”.\n\n### Chống overselling bằng thao tác nguyên tử\n\nCách ngây thơ `SELECT stock`, kiểm trong Python, rồi `UPDATE` có race. Hai transaction cùng đọc 1 trước khi transaction kia ghi. Một cách là update có điều kiện:\n\n```sql\nUPDATE products\nSET stock = stock - :quantity\nWHERE id = :id AND stock >= :quantity;\n```\n\nNếu affected rows bằng 0, sản phẩm không tồn tại hoặc không đủ tồn. Thao tác này để database phân xử atomically. Một lựa chọn khác là khóa row `SELECT ... FOR UPDATE` trên engine hỗ trợ, nhưng phải khóa theo thứ tự ổn định để giảm deadlock. Constraint `CHECK (stock >= 0)` là hàng rào cuối, song chỉ constraint không cho thông báo nghiệp vụ đẹp.\n\n### Isolation và retry có giới hạn\n\nIsolation level quyết định transaction nhìn thấy thay đổi cạnh tranh nào. Mức mạnh hơn giảm anomaly nhưng có thể tăng lock, abort và latency. Không chọn theo khẩu hiệu; xác định invariant rồi test lịch xen kẽ.\n\nDeadlock là hệ quả có thể xảy ra của locking, không nhất thiết là bug database. Database hủy một transaction; ứng dụng có thể retry toàn unit of work với backoff. Chỉ retry khi thao tác idempotent hoặc chưa phát side effect ngoài transaction. Retry vô hạn tạo bão tải.\n\n### N+1: vòng lặp Python, nhiều chuyến mạng\n\nTải 100 order bằng một SELECT rồi mỗi lần truy cập `order.lines` phát thêm SELECT tạo 101 query. Big-O của code nhìn như O(n), nhưng chi phí n round-trip thống trị. SQL logging hoặc query counter trong test phát hiện rõ.\n\n`selectinload` thường dùng một query cho parent và một query `WHERE order_id IN (...)` cho children. `joinedload` dùng JOIN một query, nhưng order có nhiều lines làm lặp cột parent và có thể tạo tích Descartes nếu eager nhiều collection. Không có eager strategy thắng mọi trường hợp.\n\n### Projection tốt hơn tải cả entity cho màn hình đọc\n\nEndpoint summary chỉ cần ID, status và total không nhất thiết tạo full entity, relationship và tracking. SELECT đúng cột hoặc DTO projection giảm byte, memory và nguy cơ lazy load. Entity hữu ích khi thực hiện behavior và thay đổi state; read model có thể tối ưu theo query.\n\nRepository không nên trả một “queryable” sống ra ngoài rồi để route tùy ý nối filter. Điều đó làm transaction/I/O rò qua boundary và test khó biết query nào chạy.\n\n### Connection pool là tài nguyên hữu hạn\n\nPool tái sử dụng connection đắt đỏ. Nếu mỗi request giữ connection trong lúc gọi payment API, chỉ vài request chậm có thể làm request khác timeout chờ pool. Dependency phải đóng session trong `finally`; transaction cần ngắn.\n\nPool size không nên tăng mù quáng. Database có giới hạn connection và mỗi connection tốn memory. Nhiều app replica nhân pool size lên; tổng mới là con số database thấy. Metric cần có thời gian checkout, số connection đang dùng và timeout.\n\n### Async ORM chỉ có ý nghĩa với driver async\n\nSQLAlchemy async dùng `AsyncSession` và driver bất đồng bộ. Nó cho event loop làm request khác khi chờ socket, không làm query SQL chạy nhanh hơn. Không dùng cùng một `AsyncSession` đồng thời cho nhiều task; session mang transaction state mutable.\n\nLazy loading ngầm trong async đặc biệt khó vì truy cập thuộc tính không có chỗ `await`. Tải quan hệ chủ động hoặc projection làm I/O hiển thị. Nếu stack hiện tại đồng bộ và tải vừa, sync ORM trong thread worker có thể đơn giản, đáng tin hơn migration async nửa vời.\n\n### Parameter binding và giới hạn bảo mật\n\nBiểu thức ORM bind value thành parameter, giảm SQL injection so với nối chuỗi. Nhưng tên cột/order-by động không phải value parameter; phải chọn từ allowlist, không ghép raw input. Raw SQL vẫn hợp lý cho query đặc biệt nếu bind parameter đúng.\n\nTài khoản runtime chỉ cần quyền SELECT/INSERT/UPDATE cần thiết, không nên là superuser hay có quyền migration. Secret database đến từ secret manager/biến môi trường, không hard-code và không hiện trong exception/log. Multi-tenant cần tenant predicate bắt buộc hoặc row-level security; một filter bị quên có thể thành data breach.\n\n### Migration là một phần deployment\n\nThay class ORM không tự sửa database production một cách an toàn. Migration có version tạo schema chuyển tiếp. Deploy zero-downtime thường theo expand–migrate–contract: thêm cấu trúc tương thích, deploy code đọc/ghi hai dạng nếu cần, backfill, rồi mới xóa cũ.\n\nMigration khóa bảng lớn có thể gây downtime dù code đúng. Hãy test trên dữ liệu đại diện, backup/restore và quan sát lock. Không chạy `create_all` như chiến lược migration production.\n\n### Khi nào không nên dùng ORM\n\nQuery phân tích phức tạp, bulk load cực lớn, tận dụng tính năng riêng engine hoặc service rất nhỏ với vài câu SQL có thể rõ hơn bằng SQL/Core. Không nên ép mọi query qua repository CRUD chung khiến SQL tệ. ORM đáng dùng khi mapping và unit of work đem lại giá trị; quyền xem SQL thật phải luôn còn.",

vi_du: "### Khung thực hành có thể chạy/triển khai\n\n```python\n# Khung handler cho: Khái quát về cloud: AWS EC2/S3/RDS\ndef handle(request, deps):\n    data = validate(request)          # boundary validation\n    authorize(request.user, data)    # deny by default\n    result = deps.service.execute(data)\n    return serialize(result)          # stable API contract\n```\n\nBiến khung này thành thí nghiệm bằng dataset/input nhỏ có expected output, một ca biên và một failure được tiêm chủ động. Ghi phiên bản dependency, seed/config và môi trường. Không lấy một lần chạy làm benchmark; warm-up, lặp nhiều vòng và báo phân phối hoặc khoảng dao động.\n\n### Settings fail sớm nhưng không lộ secret\n\n```python\nimport os\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Settings:\n    database_url: str\n    redis_url: str\n    jwt_secret: str\n    port: int\n    revision: str\n\ndef load_settings() -> Settings:\n    required = [\"DATABASE_URL\", \"REDIS_URL\", \"JWT_SECRET\"]\n    missing = [name for name in required if not os.getenv(name)]\n    if missing:\n        raise RuntimeError(\"Thiếu biến bắt buộc: \" + \", \".join(missing))\n    port = int(os.getenv(\"PORT\", \"8000\"))\n    if not 1 <= port <= 65535:\n        raise RuntimeError(\"PORT ngoài khoảng hợp lệ\")\n    return Settings(\n        database_url=os.environ[\"DATABASE_URL\"],\n        redis_url=os.environ[\"REDIS_URL\"],\n        jwt_secret=os.environ[\"JWT_SECRET\"],\n        port=port,\n        revision=os.getenv(\"APP_REVISION\", \"unknown\"),\n    )\n```\n\nThông báo chỉ nêu tên biến thiếu. Không dùng `repr(settings)` trong log vì dataclass chứa secret. Production có thể dùng thư viện settings với kiểu URL/secret chuyên dụng, nhưng nguyên tắc vẫn vậy.\n\n### Health endpoint tách live và ready\n\n```python\nfrom fastapi import FastAPI, Response\nfrom sqlalchemy import text\n\napp = FastAPI()\n\n@app.get(\"/health/live\")\nasync def live():\n    return {\"status\": \"alive\"}\n\n@app.get(\"/health/ready\")\nasync def ready(response: Response):\n    try:\n        async with engine.connect() as conn:\n            await conn.execute(text(\"SELECT 1\"))\n    except Exception:\n        response.status_code = 503\n        return {\"status\": \"not_ready\"}\n    return {\"status\": \"ready\"}\n```\n\nTruy vấn có timeout ở engine/pool; response không lộ hostname hay exception. Redis không nằm trong readiness nếu ứng dụng có thể suy giảm an toàn khi cache hỏng.\n\n### Checklist proof of concept có kết quả quan sát được\n\nDeploy cùng digest lên một project thử của từng nền tảng, cùng region gần nhất có thể và cùng cỡ tài nguyên tương đương. Ghi lại: thời gian build/deploy do hệ thống báo; cold start nếu plan có sleep; latency từ vị trí người dùng; connection DB; hành vi SIGTERM; log retrieval; rollback; restart và persistence. Không so số khi điều kiện khác nhau.\n\nThử ca biên: thiếu `JWT_SECRET` phải fail; Redis dừng thì expense vẫn đọc được; database sai credential thì readiness 503; restart instance không làm mất giao dịch; payload quá lớn bị chặn; user B không đọc expense của user A. Đây là dữ liệu quyết định hữu ích hơn cảm giác UI đẹp.\n\n### Schema và transaction tạo đơn bằng SQLite\n\nVí dụ chạy độc lập sau khi cài SQLAlchemy. SQLite phù hợp minh họa mapping/rollback, không đại diện đầy đủ row locking production.\n\n```python\nfrom sqlalchemy import CheckConstraint, ForeignKey, String, create_engine, select, update\nfrom sqlalchemy.orm import DeclarativeBase, Mapped, Session, mapped_column, relationship\n\nclass Base(DeclarativeBase):\n    pass\n\nclass Product(Base):\n    __tablename__ = \"products\"\n    __table_args__ = (CheckConstraint(\"stock >= 0\", name=\"ck_stock_nonnegative\"),)\n    id: Mapped[int] = mapped_column(primary_key=True)\n    sku: Mapped[str] = mapped_column(String(32), unique=True)\n    stock: Mapped[int]\n\nclass Order(Base):\n    __tablename__ = \"orders\"\n    id: Mapped[int] = mapped_column(primary_key=True)\n    customer_id: Mapped[str] = mapped_column(String(64), index=True)\n    status: Mapped[str] = mapped_column(String(20), default=\"pending\")\n    lines: Mapped[list[\"OrderLine\"]] = relationship(back_populates=\"order\", cascade=\"all, delete-orphan\")\n\nclass OrderLine(Base):\n    __tablename__ = \"order_lines\"\n    id: Mapped[int] = mapped_column(primary_key=True)\n    order_id: Mapped[int] = mapped_column(ForeignKey(\"orders.id\"))\n    product_id: Mapped[int] = mapped_column(ForeignKey(\"products.id\"))\n    quantity: Mapped[int] = mapped_column(CheckConstraint(\"quantity > 0\"))\n    order: Mapped[Order] = relationship(back_populates=\"lines\")\n\nengine = create_engine(\"sqlite:///shop.db\")\nBase.metadata.create_all(engine)  # chỉ dành cho demo/test\n\ndef place_order(customer_id: str, product_id: int, quantity: int) -> int:\n    with Session(engine) as session, session.begin():\n        result = session.execute(\n            update(Product)\n            .where(Product.id == product_id, Product.stock >= quantity)\n            .values(stock=Product.stock - quantity)\n        )\n        if result.rowcount != 1:\n            raise ValueError(\"không đủ tồn kho hoặc sản phẩm không tồn tại\")\n        order = Order(customer_id=customer_id)\n        order.lines.append(OrderLine(product_id=product_id, quantity=quantity))\n        session.add(order)\n        session.flush()  # lấy ID nhưng vẫn trong transaction\n        order_id = order.id\n    return order_id  # context commit trước khi ra đây\n\nwith Session(engine) as session, session.begin():\n    if session.scalar(select(Product).where(Product.id == 1)) is None:\n        session.add(Product(id=1, sku=\"BOOK-01\", stock=2))\n\nprint(place_order(\"an\", 1, 1))\n```\n\nChạy lần đầu tạo file database và in ID. Gọi thêm với quantity vượt stock sẽ ném lỗi; context manager rollback nên không có order rỗng. Trong API thật, map lỗi thiếu tồn thành domain exception/409, không trả `ValueError` hay SQL cho client.\n\n### Dependency session cho FastAPI\n\n```python\nfrom collections.abc import Generator\nfrom fastapi import Depends\nfrom typing import Annotated\n\ndef get_session() -> Generator[Session, None, None]:\n    with Session(engine) as session:\n        yield session\n\nDbSession = Annotated[Session, Depends(get_session)]\n```\n\nDependency chịu trách nhiệm đóng session. Service/use case nên điều khiển `begin` và commit để test failure giữa các bước. Nếu chọn quy ước commit trong dependency, phải tài liệu hóa rõ và cẩn thận exception sau endpoint nhưng trước response.\n\n### Test rollback thay vì chỉ test happy path\n\nTạo product stock 1, gọi quantity 2, bắt lỗi rồi query số order và stock. Kỳ vọng số order không tăng, stock vẫn 1. Test này chứng minh transaction boundary; chỉ assert status 409 chưa chứng minh database không bị thay đổi nửa chừng.\n\n### Settings fail sớm nhưng không lộ secret\n\n```python\nimport os\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass Settings:\n    database_url: str\n    redis_url: str\n    jwt_secret: str\n    port: int\n    revision: str\n\ndef load_settings() -> Settings:\n    required = [\"DATABASE_URL\", \"REDIS_URL\", \"JWT_SECRET\"]\n    missing = [name for name in required if not os.getenv(name)]\n    if missing:\n        raise RuntimeError(\"Thiếu biến bắt buộc: \" + \", \".join(missing))\n    port = int(os.getenv(\"PORT\", \"8000\"))\n    if not 1 <= port <= 65535:\n        raise RuntimeError(\"PORT ngoài khoảng hợp lệ\")\n    return Settings(\n        database_url=os.environ[\"DATABASE_URL\"],\n        redis_url=os.environ[\"REDIS_URL\"],\n        jwt_secret=os.environ[\"JWT_SECRET\"],\n        port=port,\n        revision=os.getenv(\"APP_REVISION\", \"unknown\"),\n    )\n```\n\nThông báo chỉ nêu tên biến thiếu. Không dùng `repr(settings)` trong log vì dataclass chứa secret. Production có thể dùng thư viện settings với kiểu URL/secret chuyên dụng, nhưng nguyên tắc vẫn vậy.\n\n### Health endpoint tách live và ready\n\n```python\nfrom fastapi import FastAPI, Response\nfrom sqlalchemy import text\n\napp = FastAPI()\n\n@app.get(\"/health/live\")\nasync def live():\n    return {\"status\": \"alive\"}\n\n@app.get(\"/health/ready\")\nasync def ready(response: Response):\n    try:\n        async with engine.connect() as conn:\n            await conn.execute(text(\"SELECT 1\"))\n    except Exception:\n        response.status_code = 503\n        return {\"status\": \"not_ready\"}\n    return {\"status\": \"ready\"}\n```\n\nTruy vấn có timeout ở engine/pool; response không lộ hostname hay exception. Redis không nằm trong readiness nếu ứng dụng có thể suy giảm an toàn khi cache hỏng.\n\n### Checklist proof of concept có kết quả quan sát được\n\nDeploy cùng digest lên một project thử của từng nền tảng, cùng region gần nhất có thể và cùng cỡ tài nguyên tương đương. Ghi lại: thời gian build/deploy do hệ thống báo; cold start nếu plan có sleep; latency từ vị trí người dùng; connection DB; hành vi SIGTERM; log retrieval; rollback; restart và persistence. Không so số khi điều kiện khác nhau.\n\nThử ca biên: thiếu `JWT_SECRET` phải fail; Redis dừng thì expense vẫn đọc được; database sai credential thì readiness 503; restart instance không làm mất giao dịch; payload quá lớn bị chặn; user B không đọc expense của user A. Đây là dữ liệu quyết định hữu ích hơn cảm giác UI đẹp.\n\n### Schema và transaction tạo đơn bằng SQLite\n\nVí dụ chạy độc lập sau khi cài SQLAlchemy. SQLite phù hợp minh họa mapping/rollback, không đại diện đầy đủ row locking production.\n\n```python\nfrom sqlalchemy import CheckConstraint, ForeignKey, String, create_engine, select, update\nfrom sqlalchemy.orm import DeclarativeBase, Mapped, Session, mapped_column, relationship\n\nclass Base(DeclarativeBase):\n    pass\n\nclass Product(Base):\n    __tablename__ = \"products\"\n    __table_args__ = (CheckConstraint(\"stock >= 0\", name=\"ck_stock_nonnegative\"),)\n    id: Mapped[int] = mapped_column(primary_key=True)\n    sku: Mapped[str] = mapped_column(String(32), unique=True)\n    stock: Mapped[int]\n\nclass Order(Base):\n    __tablename__ = \"orders\"\n    id: Mapped[int] = mapped_column(primary_key=True)\n    customer_id: Mapped[str] = mapped_column(String(64), index=True)\n    status: Mapped[str] = mapped_column(String(20), default=\"pending\")\n    lines: Mapped[list[\"OrderLine\"]] = relationship(back_populates=\"order\", cascade=\"all, delete-orphan\")\n\nclass OrderLine(Base):\n    __tablename__ = \"order_lines\"\n    id: Mapped[int] = mapped_column(primary_key=True)\n    order_id: Mapped[int] = mapped_column(ForeignKey(\"orders.id\"))\n    product_id: Mapped[int] = mapped_column(ForeignKey(\"products.id\"))\n    quantity: Mapped[int] = mapped_column(CheckConstraint(\"quantity > 0\"))\n    order: Mapped[Order] = relationship(back_populates=\"lines\")\n\nengine = create_engine(\"sqlite:///shop.db\")\nBase.metadata.create_all(engine)  # chỉ dành cho demo/test\n\ndef place_order(customer_id: str, product_id: int, quantity: int) -> int:\n    with Session(engine) as session, session.begin():\n        result = session.execute(\n            update(Product)\n            .where(Product.id == product_id, Product.stock >= quantity)\n            .values(stock=Product.stock - quantity)\n        )\n        if result.rowcount != 1:\n            raise ValueError(\"không đủ tồn kho hoặc sản phẩm không tồn tại\")\n        order = Order(customer_id=customer_id)\n        order.lines.append(OrderLine(product_id=product_id, quantity=quantity))\n        session.add(order)\n        session.flush()  # lấy ID nhưng vẫn trong transaction\n        order_id = order.id\n    return order_id  # context commit trước khi ra đây\n\nwith Session(engine) as session, session.begin():\n    if session.scalar(select(Product).where(Product.id == 1)) is None:\n        session.add(Product(id=1, sku=\"BOOK-01\", stock=2))\n\nprint(place_order(\"an\", 1, 1))\n```\n\nChạy lần đầu tạo file database và in ID. Gọi thêm với quantity vượt stock sẽ ném lỗi; context manager rollback nên không có order rỗng. Trong API thật, map lỗi thiếu tồn thành domain exception/409, không trả `ValueError` hay SQL cho client.\n\n### Dependency session cho FastAPI\n\n```python\nfrom collections.abc import Generator\nfrom fastapi import Depends\nfrom typing import Annotated\n\ndef get_session() -> Generator[Session, None, None]:\n    with Session(engine) as session:\n        yield session\n\nDbSession = Annotated[Session, Depends(get_session)]\n```\n\nDependency chịu trách nhiệm đóng session. Service/use case nên điều khiển `begin` và commit để test failure giữa các bước. Nếu chọn quy ước commit trong dependency, phải tài liệu hóa rõ và cẩn thận exception sau endpoint nhưng trước response.\n\n### Test rollback thay vì chỉ test happy path\n\nTạo product stock 1, gọi quantity 2, bắt lỗi rồi query số order và stock. Kỳ vọng số order không tăng, stock vẫn 1. Test này chứng minh transaction boundary; chỉ assert status 409 chưa chứng minh database không bị thay đổi nửa chừng.",

so_sanh: "Đừng so tên công nghệ; so theo workload. Với **Khái quát về cloud: AWS EC2/S3/RDS**, đặt ít nhất hai phương án cạnh nhau trên các trục: guarantee/correctness, latency trung vị và p99, throughput, memory/storage, độ phức tạp vận hành, chi phí, khả năng debug và lock-in. Phương án đơn giản thường thắng ở quy mô nhỏ; phương án phân tán/tối ưu chỉ đáng dùng khi bottleneck đã đo vượt chi phí phức tạp mới.\n\n### PaaS, VM và Kubernetes\n\n| Phương án | Đội ngũ quản lý | Quyền kiểm soát | Chi phí ẩn | Khi hợp lý |\n|---|---|---|---|---|\n| Render/Railway/Fly.io | App, data contract, config, observability | Vừa, theo abstraction nền tảng | Giá dịch vụ, giới hạn, lock-in | Nhóm nhỏ muốn giao hàng nhanh |\n| VM | Cả OS, patch, firewall, process, backup | Cao | On-call và drift cấu hình | Legacy/OS đặc thù, đội có vận hành |\n| Kubernetes | Workload cộng cluster/add-on nếu tự quản | Rất cao | Độ phức tạp control plane | Nhiều service và nhu cầu platform rõ |\n| Serverless function | Function/config, ít server | Thấp đến vừa | Cold start, giới hạn runtime | Tải theo sự kiện, stateless ngắn |\n\nTrong ba PaaS, không thể tuyên bố một bên thắng mọi trục. Render thường dễ ánh xạ web service; Railway giảm ma sát dựng project nhiều service; Fly.io cho topology process/region linh hoạt hơn. Chính tính linh hoạt có thể là ưu điểm với đội hiểu network và là chi phí với đội chỉ cần một API một vùng.\n\nKhi nào không nên di chuyển? Nếu hệ thống hiện tại đáp ứng SLO, chi phí chấp nhận được và không có rủi ro chiến lược, migration chỉ để theo xu hướng tạo downtime và việc học mới. Tối ưu query, index hoặc pool thường có lợi trước chuyển nhà cung cấp.\n\n### ORM, SQLAlchemy Core và SQL viết tay\n\n| Cách | Mạnh ở đâu | Rủi ro/chi phí |\n|---|---|---|\n| ORM entity | quan hệ, unit of work, business object | lazy I/O, N+1, tracking memory |\n| SQLAlchemy Core | query tường minh, bind parameter, composable | tự mapping nhiều hơn |\n| SQL viết tay | toàn quyền SQL/engine feature | boilerplate, portability và mapping |\n\nMột codebase có thể dùng cả ba. Command thay đổi aggregate dùng ORM; reporting projection dùng Core; query tối ưu đặc thù dùng SQL bind parameter. Tính nhất quán không có nghĩa ép mọi vấn đề qua một abstraction.\n\n### Loading strategy theo hình dạng kết quả\n\n| Chiến lược | Số query điển hình | Đánh đổi |\n|---|---:|---|\n| lazy cho từng parent | 1 + n | đơn giản nhưng N+1 qua mạng |\n| `selectinload` | thường 2 | câu IN lớn, phù hợp collection vừa |\n| `joinedload` | thường 1 | lặp cột parent, bùng row với nhiều collection |\n| projection | 1 | tối ưu đọc, không có full entity behavior |\n\nKhông chọn bằng số query một mình. Một JOIN trả triệu row có thể tệ hơn hai query nhỏ; đo row, byte, latency và query plan trên dữ liệu đại diện.\n\n### PaaS, VM và Kubernetes\n\n| Phương án | Đội ngũ quản lý | Quyền kiểm soát | Chi phí ẩn | Khi hợp lý |\n|---|---|---|---|---|\n| Render/Railway/Fly.io | App, data contract, config, observability | Vừa, theo abstraction nền tảng | Giá dịch vụ, giới hạn, lock-in | Nhóm nhỏ muốn giao hàng nhanh |\n| VM | Cả OS, patch, firewall, process, backup | Cao | On-call và drift cấu hình | Legacy/OS đặc thù, đội có vận hành |\n| Kubernetes | Workload cộng cluster/add-on nếu tự quản | Rất cao | Độ phức tạp control plane | Nhiều service và nhu cầu platform rõ |\n| Serverless function | Function/config, ít server | Thấp đến vừa | Cold start, giới hạn runtime | Tải theo sự kiện, stateless ngắn |\n\nTrong ba PaaS, không thể tuyên bố một bên thắng mọi trục. Render thường dễ ánh xạ web service; Railway giảm ma sát dựng project nhiều service; Fly.io cho topology process/region linh hoạt hơn. Chính tính linh hoạt có thể là ưu điểm với đội hiểu network và là chi phí với đội chỉ cần một API một vùng.\n\nKhi nào không nên di chuyển? Nếu hệ thống hiện tại đáp ứng SLO, chi phí chấp nhận được và không có rủi ro chiến lược, migration chỉ để theo xu hướng tạo downtime và việc học mới. Tối ưu query, index hoặc pool thường có lợi trước chuyển nhà cung cấp.\n\n### ORM, SQLAlchemy Core và SQL viết tay\n\n| Cách | Mạnh ở đâu | Rủi ro/chi phí |\n|---|---|---|\n| ORM entity | quan hệ, unit of work, business object | lazy I/O, N+1, tracking memory |\n| SQLAlchemy Core | query tường minh, bind parameter, composable | tự mapping nhiều hơn |\n| SQL viết tay | toàn quyền SQL/engine feature | boilerplate, portability và mapping |\n\nMột codebase có thể dùng cả ba. Command thay đổi aggregate dùng ORM; reporting projection dùng Core; query tối ưu đặc thù dùng SQL bind parameter. Tính nhất quán không có nghĩa ép mọi vấn đề qua một abstraction.\n\n### Loading strategy theo hình dạng kết quả\n\n| Chiến lược | Số query điển hình | Đánh đổi |\n|---|---:|---|\n| lazy cho từng parent | 1 + n | đơn giản nhưng N+1 qua mạng |\n| `selectinload` | thường 2 | câu IN lớn, phù hợp collection vừa |\n| `joinedload` | thường 1 | lặp cột parent, bùng row với nhiều collection |\n| projection | 1 | tối ưu đọc, không có full entity behavior |\n\nKhông chọn bằng số query một mình. Một JOIN trả triệu row có thể tệ hơn hai query nhỏ; đo row, byte, latency và query plan trên dữ liệu đại diện.",

loi_thuong_gap: [
  "Dùng thuật ngữ “Khái quát về cloud: AWS EC2/S3/RDS” nhưng không nêu contract và giả định.",
  "Chỉ test happy path hoặc cùng phân phối với dữ liệu phát triển.",
  "Tối ưu metric cục bộ nhưng làm xấu p99, consistency, security hoặc chi phí vận hành.",
  "Tin vào default của framework/database/model mà không kiểm version và execution/runtime behavior.",
  "Không có rollback, migration tương thích hoặc cách tái hiện kết quả.",
  "**Bind vào `127.0.0.1` hoặc cổng đóng cứng.** Log báo started nhưng router không kết nối; nghe `0.0.0.0` và dùng `PORT` của runtime.",
  "**Lưu SQLite/upload trên filesystem tạm.** Dữ liệu mất hoặc lệch giữa replica sau restart; chuyển state bền vững sang database/object storage.",
  "**Để migration chạy ở mọi web process.** Rollout treo vì lock/race; dùng release job duy nhất và schema tương thích."
],

tu_kiem_tra: [
  "Vẽ lại luồng của “Khái quát về cloud: AWS EC2/S3/RDS” và ghi state tại từng node.",
  "Nêu một invariant, một failure mode và tín hiệu quan sát failure đó.",
  "Thiết kế benchmark/eval tránh leakage và warm-cache bias.",
  "Khi scale tăng 10 lần, bottleneck nào xuất hiện trước và vì sao?",
  "Nêu tình huống phương án đơn giản hơn là lựa chọn tốt hơn.",
  "Viết contract runtime tối thiểu để cùng image Sổ Chi Tiêu chạy được trên cả ba PaaS mà không đổi source.",
  "Vì sao đặt API ở ba region nhưng PostgreSQL một region có thể làm request chậm hơn, dù API gần người dùng hơn?",
  "Nếu Redis mất, phần nào được phép suy giảm và rate-limit cần lớp bảo vệ bổ sung nào?"
],

tai_nguyen: [
  "Web Security for Developers — Malcolm McDonald",
  "Tài liệu chính thức của công nghệ đang dùng",
  "Roadmap nội bộ: Backend & Web / Triển khai",
  "Tài liệu chính thức Render — web services, health checks, deploys và databases.",
  "Tài liệu chính thức Railway — services, environments, variables và networking."
],

lien_he: [
  "Mảng Mạng máy tính: VPC, subnet, route và security group hiện thực hóa ranh giới công khai–riêng tư đã dùng với Nginx.",
  "Mảng Database: RDS vẫn là PostgreSQL, nên transaction, index, migration và connection pool tiếp tục quyết định tính đúng cùng hiệu năng.",
  "Mảng Bảo mật: IAM thay khóa AWS cố định bằng quyền tối thiểu gắn với workload; S3 private buộc thiết kế authorization cho từng hóa đơn.",
  "Mảng Hệ điều hành: EC2 vẫn là máy Linux phải vá, theo dõi đĩa, process và bộ nhớ; cloud không xóa trách nhiệm đó.",
  "Mảng Thiết kế hệ thống: tách compute, object storage và relational database mở đường cho scale ngang và thiết kế phục hồi."
]

};
