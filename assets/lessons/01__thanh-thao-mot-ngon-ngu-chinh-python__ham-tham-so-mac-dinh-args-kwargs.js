window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:ham-tham-so-mac-dinh-args-kwargs"] = {

tom_tat: [
  "**Giá trị mặc định được tính MỘT LẦN lúc định nghĩa hàm, không phải mỗi lần gọi.** Đây là bẫy nổi tiếng nhất của Python: `def f(ds=[])` khiến `ds` nhớ dữ liệu giữa các lần gọi.",
  "**Python truyền tham số theo kiểu \"truyền tham chiếu tới object\"** — không phải truyền giá trị, cũng không phải truyền tham chiếu tới biến. Hàm sửa được `list` của người gọi, nhưng gán lại thì không.",
  "**`*args` gom tham số vị trí thành `tuple`, `**kwargs` gom tham số tên thành `dict`.** Dấu `*` và `**` khi **gọi** hàm thì làm ngược lại — trải ra.",
  "**`*` và `/` trong danh sách tham số ép cách gọi**: sau `*` bắt buộc gọi bằng tên, trước `/` bắt buộc gọi bằng vị trí. Đây là công cụ thiết kế API, không phải cú pháp trang trí.",
  "**Hàm nên hoặc sửa đầu vào, hoặc trả về giá trị mới — không làm cả hai.** Thư viện chuẩn theo đúng quy ước này, và code của bạn cũng nên vậy."
],

can_biet_truoc: [
  "Bài **Cú pháp, kiểu dữ liệu, toán tử** — \"biến là nhãn\" và ranh giới mutable/immutable là nền để hiểu cả bài này.",
  "Bài **List, dict, set, tuple** — `*args` là `tuple`, `**kwargs` là `dict`, và tính chất của chúng vẫn đúng.",
  "Bài **Stack vs heap** ở module 2 — mỗi lần gọi hàm tạo một khung stack mới, còn object thì nằm trên heap.",
  "Bài **Comprehension** ngay trước — `lambda` xuất hiện ở đó với `map`/`filter`, bài này nói rõ nó là gì."
],

dinh_nghia: "Nói đời thường: một hàm giống như **một cái máy có mấy khe cắm đầu vào**. Python cho bạn thiết kế khe rất linh hoạt: khe bắt buộc, khe có sẵn giá trị mặc định, khe nhận bao nhiêu cũng được, và khe chỉ nhận khi gọi đúng tên.\n\nNói chính xác:\n\n- **Tham số** (*parameter*) — tên trong định nghĩa hàm. **Đối số** (*argument*) — giá trị thật truyền vào khi gọi.\n- **Tham số vị trí** (*positional*) — khớp theo thứ tự: `f(1, 2)`.\n- **Tham số tên** (*keyword*) — khớp theo tên: `f(a=1, b=2)`.\n- **Giá trị mặc định** — giá trị dùng khi người gọi không truyền. **Được tính một lần lúc định nghĩa hàm.**\n- **`*args`** — gom mọi tham số vị trí thừa vào một `tuple`.\n- **`**kwargs`** — gom mọi tham số tên thừa vào một `dict`.\n- **`lambda`** — hàm ẩn danh, chỉ chứa **một biểu thức** và tự trả về kết quả của biểu thức đó.\n\nĐiểm cần đính chính ngay, vì đây là nguồn tranh cãi vô ích: Python **không** phải \"truyền theo giá trị\" cũng **không** phải \"truyền theo tham chiếu\" theo nghĩa của C++. Cách nói chính xác là **truyền tham chiếu tới object** (*call by object reference*): hàm nhận được cùng object mà người gọi đang trỏ tới. Sửa object đó thì người gọi thấy; gán tên tham số sang object khác thì người gọi không thấy.",

vi_sao_can: "Hàm là đơn vị tổ chức code cơ bản nhất, và cách Python xử lý tham số có vài chỗ khác hẳn trực giác:\n\n- **Bẫy giá trị mặc định mutable gây lỗi thật trong sản phẩm.** Nó không báo lỗi, chỉ tích luỹ dữ liệu sai qua các lần gọi — và chỉ lộ ra khi hàm được gọi lần thứ hai, thứ ba, tức là thường sau khi đã lên môi trường thật.\n- **`*args`/`**kwargs` xuất hiện khắp nơi.** Mọi decorator, mọi lớp bao, mọi hàm chuyển tiếp đều dùng. Không đọc được nó là không đọc được phần lớn thư viện.\n- **Thiết kế chữ ký hàm là thiết kế API.** Chữ ký tốt làm người dùng khó gọi sai; chữ ký tệ tạo ra lỗi cho người khác. `*` và `/` là công cụ chính cho việc này.\n- **Nền trực tiếp cho decorator.** Bài decorator trong module này dựa hoàn toàn vào `*args`, `**kwargs`, và ý tưởng \"hàm là đối tượng\".\n- **Định hướng AI/ML.** Các thư viện học máy có hàm hàng chục tham số; hiểu tham số chỉ-tên và cách chuyển tiếp `**kwargs` là điều kiện để đọc và dùng chúng đúng.\n\nTình huống thật kinh điển: bạn viết `def them_log(tin, lich_su=[])` để tiện. Kiểm thử một lần thấy đúng. Lên môi trường thật, sau vài giờ, `lich_su` chứa hàng nghìn dòng của **mọi người dùng trước đó** — vừa là lỗi logic vừa là rò rỉ dữ liệu giữa các phiên. Nguyên nhân chỉ là một cặp ngoặc vuông ở sai chỗ, và phần cơ chế sẽ chỉ rõ vì sao Python lại hành xử như vậy.",

co_che: "### Sáu loại tham số và thứ tự bắt buộc\n\n```python\ndef f(a, b=2, *args, c, d=4, **kwargs):\n#     │  │      │      │  │      └── ⑥ gom tham số tên thừa → dict\n#     │  │      │      │  └───────── ⑤ chỉ-tên, có mặc định\n#     │  │      │      └──────────── ④ CHỈ-TÊN, bắt buộc\n#     │  │      └─────────────────── ③ gom tham số vị trí thừa → tuple\n#     │  └────────────────────────── ② vị trí, có mặc định\n#     └───────────────────────────── ① vị trí, bắt buộc\n    ...\n```\n\nThứ tự này **bắt buộc**, viết sai là `SyntaxError`. Lý do rất logic: Python phải phân giải được đối số nào ứng với tham số nào, và nếu tham số bắt buộc đứng sau tham số có mặc định thì không có cách nào biết người gọi định truyền cho ai.\n\n```python\ndef f(a=1, b):     # SyntaxError: non-default argument follows default argument\n```\n\n### Bẫy lớn nhất: giá trị mặc định mutable\n\n```python\ndef them(x, ds=[]):        # ❌ BẪY\n    ds.append(x)\n    return ds\n\nprint(them(1))    # [1]\nprint(them(2))    # [1, 2]  ← (!) không phải [2]\nprint(them(3))    # [1, 2, 3]\n```\n\nNguyên nhân nằm ở **thời điểm** giá trị mặc định được tạo:\n\n```\nLúc ĐỊNH NGHĨA hàm (một lần duy nhất):\n    Python tính biểu thức []  →  tạo MỘT list\n    gắn list đó vào hàm, ở thuộc tính __defaults__\n\nMỗi lần GỌI mà không truyền ds:\n    ds được buộc vào ĐÚNG list đó — không tạo mới\n```\n\nKiểm chứng trực tiếp:\n\n```python\ndef them(x, ds=[]):\n    ds.append(x)\n    return ds\n\nprint(them.__defaults__)     # ([],)\nthem(1)\nprint(them.__defaults__)     # ([1],)  ← chính list mặc định bị sửa\n```\n\nCách sửa chuẩn — dùng `None` làm mốc:\n\n```python\ndef them(x, ds=None):\n    if ds is None:\n        ds = []            # tạo list MỚI ở MỖI lần gọi\n    ds.append(x)\n    return ds\n```\n\nVì sao phải là `None` chứ không phải `if not ds`? Vì `if not ds` cũng đúng với `[]` do người gọi **cố ý truyền vào** — và khi đó bạn thay list của họ bằng list mới, làm mất tác dụng phụ mà họ mong đợi. Đây là bài truthiness ở bài 1 áp dụng vào thiết kế hàm.\n\nCùng cái bẫy đó với mọi mutable:\n\n```python\ndef f(d={}):        # ❌\ndef f(s=set()):     # ❌\ndef f(t=(1,2)):     # ✓ tuple immutable — an toàn\ndef f(n=0):         # ✓ int immutable — an toàn\ndef f(t=datetime.now()):   # ❌ THỜI ĐIỂM bị đóng băng lúc import!\n```\n\nDòng cuối là biến thể tinh vi và rất hay gặp: `datetime.now()` chạy **một lần lúc module được import**, nên mọi lần gọi đều nhận đúng thời điểm khởi động chương trình. Cách sửa vẫn là dùng `None` rồi tính bên trong hàm.\n\nMột lưu ý cân bằng: hành vi này **không phải lỗi của Python**. Nó là hệ quả nhất quán của \"`def` là một câu lệnh được thực thi\" — khi Python chạy tới `def`, nó tạo object hàm và tính luôn các giá trị mặc định. Biết vậy thì hành vi không còn bí ẩn.\n\n### Truyền tham số: hàm sửa được gì của người gọi\n\n```python\ndef sua(ds, n):\n    ds.append(99)      # SỬA object ⇒ người gọi THẤY\n    n += 1             # GÁN LẠI tên nội bộ ⇒ người gọi KHÔNG thấy\n    ds = [0]           # GÁN LẠI ⇒ người gọi KHÔNG thấy\n    ds.append(100)     # sửa list MỚI, không liên quan người gọi\n\na, b = [1, 2], 5\nsua(a, b)\nprint(a, b)            # [1, 2, 99] 5\n```\n\nQuy tắc rút gọn, đúng cho mọi trường hợp:\n\n```\nGọi PHƯƠNG THỨC sửa tại chỗ (append, sort, update, x[i]=v)\n    → người gọi thấy thay đổi\n\nGÁN LẠI tên tham số (ten = ...)\n    → chỉ đổi nhãn trong hàm, người gọi không thấy\n```\n\nHệ quả thiết kế quan trọng: **hàm sửa đầu vào là hàm có tác dụng phụ**, và tác dụng phụ ngoài mong đợi là nguồn lỗi khó lần. Ba lựa chọn:\n\n```python\n# ① Không sửa — trả về cái mới. An toàn nhất, tốn bộ nhớ hơn.\ndef sap_xep(ds):\n    return sorted(ds)\n\n# ② Sửa tại chỗ — nói RÕ trong tên và trả về None\ndef sap_xep_tai_cho(ds):\n    ds.sort()\n    # không return gì\n\n# ③ ❌ Vừa sửa vừa trả — người dùng không biết chuyện gì xảy ra\ndef sap_xep(ds):\n    ds.sort()\n    return ds\n```\n\nThư viện chuẩn theo đúng quy ước này: `sorted()` trả bản mới, `list.sort()` sửa tại chỗ và trả `None`. Việc trả `None` là **cố ý** — nó khiến `x = ds.sort()` lộ ra ngay thay vì âm thầm cho bạn tưởng đã có bản sắp xếp.\n\n### `*args` — gom tham số vị trí\n\n```python\ndef tong(*args):\n    print(type(args))      # <class 'tuple'>\n    return sum(args)\n\ntong(1, 2, 3)              # 6\ntong()                     # 0 — args là tuple rỗng\n```\n\nDùng chung với tham số thường:\n\n```python\ndef ghi_log(muc, *phan, dau=\"[LOG]\"):\n    print(dau, muc, *phan)\n\nghi_log(\"ERROR\", \"kết nối\", \"thất bại\")\n# [LOG] ERROR kết nối thất bại\n```\n\nChú ý `dau` nằm **sau** `*phan` nên nó là tham số **chỉ-tên**: bắt buộc gọi `dau=\"...\"`, không truyền theo vị trí được. Đây không phải hạn chế mà là **tính năng** — xem mục dưới.\n\n### `**kwargs` — gom tham số tên\n\n```python\ndef cau_hinh(**kwargs):\n    print(type(kwargs))    # <class 'dict'>\n    for k, v in kwargs.items():\n        print(f\"{k} = {v}\")\n\ncau_hinh(host=\"localhost\", port=8000)\n```\n\nỨng dụng chính là **chuyển tiếp tham số** mà không cần biết chúng là gì:\n\n```python\ndef ket_noi(host, port=5432, **tuy_chon):\n    print(f\"nối tới {host}:{port}, tuỳ chọn: {tuy_chon}\")\n\ndef ket_noi_co_thu_lai(host, so_lan=3, **kwargs):\n    for i in range(so_lan):\n        try:\n            return ket_noi(host, **kwargs)     # chuyển tiếp nguyên vẹn\n        except OSError:\n            if i == so_lan - 1:\n                raise\n\nket_noi_co_thu_lai(\"db.local\", port=5433, timeout=10, ssl=True)\n```\n\nHàm bao **không cần biết** `ket_noi` nhận những tham số nào. Đây là nền tảng của decorator, và cũng là lý do `*args, **kwargs` xuất hiện dày đặc trong code thư viện.\n\nĐánh đổi phải nói rõ: `**kwargs` làm chữ ký hàm **mất tính tự tài liệu**. Người đọc không biết truyền được gì, trình soạn thảo không gợi ý được, và gõ sai tên tham số thì không ai báo lỗi:\n\n```python\nket_noi_co_thu_lai(\"db.local\", tiemout=10)   # gõ nhầm timeout → lọt vào kwargs, im lặng\n```\n\nVì vậy: **dùng `**kwargs` khi thật sự cần chuyển tiếp, không dùng để lười liệt kê tham số.**\n\n### `*` và `**` khi GỌI hàm — trải ra\n\nCùng ký hiệu, ý nghĩa **ngược lại** tuỳ chỗ đứng:\n\n```\nTrong ĐỊNH NGHĨA:  GOM nhiều thứ vào một\nKhi GỌI:           TRẢI một thứ ra thành nhiều\n```\n\n```python\ndef f(a, b, c):\n    return a + b + c\n\nds = [1, 2, 3]\nf(*ds)                        # = f(1, 2, 3)\n\nd = {\"a\": 1, \"b\": 2, \"c\": 3}\nf(**d)                        # = f(a=1, b=2, c=3)\n\n# Trộn được\nf(*[1, 2], **{\"c\": 3})\n\n# Ứng dụng: gộp dict, ghép list\ngop = {**mac_dinh, **nguoi_dung}     # nguoi_dung ghi đè mac_dinh\ngop_ds = [*a, *b]\n```\n\nDòng `{**mac_dinh, **nguoi_dung}` là thành ngữ rất hay dùng cho cấu hình: giá trị mặc định trước, giá trị người dùng sau, và cái sau thắng.\n\n### `*` và `/` — ép cách gọi\n\nĐây là công cụ thiết kế API mà ít người mới biết:\n\n```python\ndef f(a, b, /, c, d, *, e, f):\n#           │        │\n#           │        └── sau dấu *: BẮT BUỘC gọi bằng tên\n#           └── trước dấu /: BẮT BUỘC gọi bằng vị trí\n    ...\n\nf(1, 2, 3, d=4, e=5, f=6)      # ✓\nf(a=1, ...)                     # ✗ a chỉ nhận vị trí\nf(1, 2, 3, 4, 5, 6)             # ✗ e, f phải gọi bằng tên\n```\n\nVì sao đáng dùng — hai lý do thật:\n\n**① Tham số chỉ-tên làm lời gọi tự giải thích và chống lỗi:**\n\n```python\n# ❌ đọc lời gọi không hiểu gì\ntao_ket_noi(\"localhost\", 5432, True, False, 30)\n\n# ✓ ép người gọi phải nêu tên\ndef tao_ket_noi(host, port=5432, *, ssl=False, tu_dong_nap=True, timeout=30):\n    ...\ntao_ket_noi(\"localhost\", ssl=True, timeout=60)\n```\n\nLợi ích phụ rất giá trị: sau này **thêm hoặc đổi thứ tự** các tham số chỉ-tên không phá code của ai, vì không ai gọi chúng theo vị trí.\n\n**② Tham số chỉ-vị-trí cho phép đổi tên tham số sau này** mà không phá code người dùng. Đó là lý do nhiều hàm dựng sẵn của Python dùng `/`.\n\nQuy tắc thực dụng: **tham số thứ ba trở đi, đặc biệt là cờ `True`/`False`, nên là chỉ-tên.** Một lời gọi có `True, False, True` là lời gọi không ai đọc hiểu.\n\n### `lambda` — hàm một biểu thức\n\n```python\nbinh_phuong = lambda x: x ** 2      # tương đương def, nhưng...\n\ndef binh_phuong(x):                  # ...cách này rõ hơn khi cần ĐẶT TÊN\n    return x ** 2\n```\n\nQuy ước PEP 8: **đừng gán `lambda` cho một tên** — nếu cần tên thì dùng `def`. Lý do thực dụng: hàm định nghĩa bằng `def` có tên thật trong truy vết lỗi, còn `lambda` hiện là `<lambda>` và làm việc gỡ lỗi khó hơn.\n\nChỗ `lambda` thật sự đúng là khi truyền **trực tiếp** một hàm nhỏ:\n\n```python\nnguoi = [{\"ten\": \"Minh\", \"tuoi\": 22}, {\"ten\": \"An\", \"tuoi\": 20}]\n\nsorted(nguoi, key=lambda n: n[\"tuoi\"])          # ✓ đúng chỗ\nmax(nguoi, key=lambda n: n[\"tuoi\"])\nsorted(nguoi, key=lambda n: (-n[\"tuoi\"], n[\"ten\"]))   # sắp nhiều khoá\n\n# Với thao tác chuẩn, operator rõ hơn lambda\nfrom operator import itemgetter\nsorted(nguoi, key=itemgetter(\"tuoi\"))\n```\n\nHạn chế của `lambda`: **chỉ một biểu thức**, không có câu lệnh, không `if/elif/else` dạng khối, không `try`. Đó là hạn chế cố ý — nó ép `lambda` phải nhỏ.\n\n### Bẫy đóng gói biến trong vòng lặp\n\n```python\n# ❌ Ba hàm đều cho 4, không phải 0, 2, 4\nham = [lambda: i * 2 for i in range(3)]\nprint([f() for f in ham])          # [4, 4, 4]\n```\n\nNguyên nhân: `lambda` **tra `i` lúc được gọi**, không phải lúc được tạo. Khi vòng lặp xong, `i` bằng 2, nên cả ba hàm đều thấy `i = 2`.\n\nCách sửa — buộc giá trị vào tham số mặc định, vốn được tính **lúc định nghĩa**:\n\n```python\n# ✓ dùng chính cơ chế \"mặc định tính một lần\" cho việc có ích\nham = [lambda i=i: i * 2 for i in range(3)]\nprint([f() for f in ham])          # [0, 2, 4]\n```\n\nĐiểm thú vị: đây là **cùng một cơ chế** đã gây ra bẫy giá trị mặc định mutable, nhưng lần này được dùng đúng chỗ. Hiểu cơ chế thì cả hai đều hết bí ẩn.\n\n### Docstring và chú thích kiểu\n\n```python\ndef tinh_diem(bai: list[float], trong_so: list[float] | None = None) -> float:\n    \"\"\"Tính điểm trung bình có trọng số.\n\n    Args:\n        bai: điểm từng bài, trong khoảng 0–10.\n        trong_so: trọng số tương ứng. None thì coi như bằng nhau.\n\n    Returns:\n        Điểm trung bình.\n\n    Raises:\n        ValueError: nếu bai rỗng hoặc độ dài hai danh sách khác nhau.\n    \"\"\"\n    if not bai:\n        raise ValueError(\"danh sách bài không được rỗng\")\n    if trong_so is None:\n        trong_so = [1.0] * len(bai)         # ✓ mẫu None, không phải [1.0]\n    if len(bai) != len(trong_so):\n        raise ValueError(\"độ dài không khớp\")\n    return sum(b * t for b, t in zip(bai, trong_so)) / sum(trong_so)\n```\n\nDocstring truy cập được lúc chạy qua `ham.__doc__` và qua `help(ham)` — nó không phải chú thích chết mà là một phần của object hàm. Chú thích kiểu thì bài **Type hint và mypy** ở cuối module sẽ nói kỹ.",

vi_du: "### Bẫy mặc định mutable — nhìn tận mắt\n\n```python\ndef them(x, ds=[]):\n    ds.append(x)\n    return ds\n\nprint(them(1), them(2), them(3))       # [1,2,3] [1,2,3] [1,2,3]\n#                                       ← BA lần in ra CÙNG một list\nprint(them.__defaults__)                # ([1, 2, 3],)\n\n# Sửa\ndef them(x, ds=None):\n    if ds is None:\n        ds = []\n    ds.append(x)\n    return ds\n\nprint(them(1), them(2), them(3))       # [1] [2] [3] ✓\n```\n\nDòng `them.__defaults__` là bằng chứng trực tiếp: giá trị mặc định là một object gắn vào hàm, và nó bị sửa. Chạy thử một lần là nhớ mãi.\n\nBiến thể với thời gian, tinh vi hơn nhiều:\n\n```python\nfrom datetime import datetime\nimport time\n\ndef ghi(tin, luc=datetime.now()):       # ❌ đóng băng lúc import\n    return f\"[{luc:%H:%M:%S}] {tin}\"\n\nprint(ghi(\"a\")); time.sleep(2); print(ghi(\"b\"))\n# Hai dòng CÙNG mốc thời gian\n\ndef ghi(tin, luc=None):                  # ✓\n    luc = luc or datetime.now()\n    return f\"[{luc:%H:%M:%S}] {tin}\"\n```\n\n### Hàm có sửa đầu vào không — kiểm tra bằng thực nghiệm\n\n```python\ndef f(ds, d, n, s):\n    ds.append(1)        # sửa được\n    d[\"moi\"] = 1        # sửa được\n    n += 1              # KHÔNG — int immutable, đây là gán lại\n    s += \"x\"            # KHÔNG — str immutable\n\na, b, c, e = [], {}, 0, \"\"\nf(a, b, c, e)\nprint(a, b, c, repr(e))     # [1] {'moi': 1} 0 ''\n```\n\nQuy luật rút ra rất gọn: **mutable thì hàm sửa được, immutable thì không** — vì với immutable, mọi \"thay đổi\" đều thực chất là tạo object mới và gán lại tên nội bộ.\n\nMuốn chắc chắn không đụng vào dữ liệu người gọi:\n\n```python\ndef xu_ly_an_toan(ds):\n    ds = list(ds)          # ⭐ làm việc trên bản sao\n    ds.sort()\n    return ds\n```\n\nDòng `ds = list(ds)` còn có lợi ích thứ hai: nó **vật chất hoá generator**, nên hàm dùng được với cả list lẫn generator — nối về bài comprehension.\n\n### `*args` và `**kwargs` trong hàm bao\n\n```python\nimport time, functools\n\ndef do_thoi_gian(ham):\n    @functools.wraps(ham)                       # giữ tên và docstring\n    def bao(*args, **kwargs):                   # ⭐ nhận MỌI chữ ký\n        t = time.perf_counter()\n        kq = ham(*args, **kwargs)               # ⭐ chuyển tiếp nguyên vẹn\n        print(f\"{ham.__name__}: {time.perf_counter()-t:.4f}s\")\n        return kq\n    return bao\n\n@do_thoi_gian\ndef tinh(n, buoc=1):\n    return sum(range(0, n, buoc))\n\ntinh(1_000_000, buoc=2)\n```\n\nHai dòng đánh dấu ⭐ là toàn bộ ý tưởng của decorator: **gom mọi thứ vào rồi trải nguyên vẹn ra**. Nhờ vậy `bao` dùng được cho hàm có chữ ký bất kỳ. Bài decorator sẽ nói kỹ; ở đây chỉ cần thấy `*args`/`**kwargs` phục vụ việc gì.\n\n### Thiết kế chữ ký chống gọi sai\n\n```python\n# ❌ Chữ ký để người ta gọi sai\ndef gui_mail(den, tu, tieu_de, noi_dung, html, gap, thu_lai):\n    ...\ngui_mail(\"a@x.com\", \"b@y.com\", \"Chào\", \"...\", True, False, 3)\n#                                             ↑ ba giá trị không ai đọc hiểu\n\n# ✓ Ép nêu tên từ tham số thứ ba trở đi\ndef gui_mail(den, tu, *, tieu_de, noi_dung, html=False, gap=False, thu_lai=3):\n    ...\ngui_mail(\"a@x.com\", \"b@y.com\",\n         tieu_de=\"Chào\", noi_dung=\"...\", html=True, thu_lai=5)\n```\n\nBa lợi ích cụ thể của bản thứ hai:\n\n```\n① Đọc lời gọi là hiểu — không phải mở định nghĩa hàm ra tra\n② Đảo thứ tự các tham số tên không phá gì\n③ THÊM tham số mới về sau cũng không phá code cũ\n```\n\nĐiểm ③ đáng giá nhất khi bạn viết thư viện dùng chung: chữ ký chỉ-tên cho bạn không gian tiến hoá API mà không làm hỏng người dùng.\n\n### Gộp cấu hình bằng `**`\n\n```python\nMAC_DINH = {\"host\": \"localhost\", \"port\": 5432, \"timeout\": 30}\n\ndef ket_noi(**tuy_chon):\n    cf = {**MAC_DINH, **tuy_chon}      # ⭐ người dùng ghi đè mặc định\n    print(cf)\n\nket_noi()                               # dùng hết mặc định\nket_noi(port=5433, ssl=True)            # ghi đè port, thêm ssl\n```\n\nMẫu này an toàn hơn hẳn việc sửa trực tiếp `MAC_DINH`: `{**a, **b}` tạo `dict` **mới**, nên `MAC_DINH` không bao giờ bị hàm nào làm bẩn. Đây lại là bài mutable áp dụng vào thiết kế.\n\n### `key=` với `lambda` — sắp xếp thực tế\n\n```python\nnv = [\n    {\"ten\": \"Minh\", \"phong\": \"IT\", \"luong\": 20},\n    {\"ten\": \"An\",   \"phong\": \"HR\", \"luong\": 15},\n    {\"ten\": \"Bình\", \"phong\": \"IT\", \"luong\": 25},\n]\n\nsorted(nv, key=lambda n: n[\"luong\"])                    # tăng dần\nsorted(nv, key=lambda n: -n[\"luong\"])                   # giảm dần\nsorted(nv, key=lambda n: (n[\"phong\"], -n[\"luong\"]))     # phòng ↑, lương ↓\n\nmax(nv, key=lambda n: n[\"luong\"])[\"ten\"]                # 'Bình'\n\n# Nhóm — lưu ý PHẢI sắp xếp trước theo cùng khoá\nfrom itertools import groupby\nnv_sap = sorted(nv, key=lambda n: n[\"phong\"])\nfor phong, nhom in groupby(nv_sap, key=lambda n: n[\"phong\"]):\n    print(phong, [n[\"ten\"] for n in nhom])\n```\n\nMẫu `key=lambda n: (a, -b)` cho sắp xếp nhiều tiêu chí là thành ngữ đáng thuộc: `tuple` được so sánh theo từng phần tử từ trái sang, và dấu trừ đảo chiều cho số.\n\nCảnh báo về `groupby`: nó chỉ gom **các phần tử liền kề** giống nhau, nên **bắt buộc phải sắp xếp trước**. Quên bước đó là kết quả sai mà không báo lỗi — khác hẳn với hàm nhóm của SQL.\n\n### Bẫy `lambda` trong vòng lặp\n\n```python\n# ❌\nham = []\nfor i in range(3):\n    ham.append(lambda: i)\nprint([f() for f in ham])       # [2, 2, 2]\n\n# ✓ đóng băng giá trị bằng tham số mặc định\nham = [lambda i=i: i for i in range(3)]\nprint([f() for f in ham])       # [0, 1, 2]\n\n# ✓ hoặc dùng partial — rõ ý hơn\nfrom functools import partial\nham = [partial(lambda x: x, i) for i in range(3)]\nprint([f() for f in ham])       # [0, 1, 2]\n```\n\nBẫy này hay gặp khi gắn hàm xử lý sự kiện trong vòng lặp — ví dụ tạo nhiều nút bấm, mỗi nút gọi một hàm với tham số riêng. Tất cả các nút cuối cùng đều làm cùng một việc.\n\n### Hàm nhận hàm — bậc cao\n\n```python\ndef ap_dung(ds, bien_doi=None, loc=None):\n    \"\"\"Áp dụng biến đổi và lọc. None nghĩa là bỏ qua bước đó.\"\"\"\n    kq = ds\n    if loc is not None:\n        kq = [x for x in kq if loc(x)]\n    if bien_doi is not None:\n        kq = [bien_doi(x) for x in kq]\n    return kq\n\nap_dung([1,2,3,4,5], bien_doi=lambda x: x**2, loc=lambda x: x % 2)\n# [1, 9, 25]\n```\n\nDùng `None` làm mặc định thay vì `lambda x: x` và `lambda x: True` có hai lý do: tránh tạo hàm thừa, và **phân biệt được \"không truyền\" với \"truyền một hàm luôn đúng\"**. Đây là mẫu `None` ở đầu bài áp dụng cho tham số kiểu hàm.\n\n### Kiểm tra chữ ký lúc chạy\n\n```python\nimport inspect\n\ndef f(a, b=2, *args, c, **kwargs):\n    pass\n\nprint(inspect.signature(f))       # (a, b=2, *args, c, **kwargs)\n\nfor ten, ts in inspect.signature(f).parameters.items():\n    print(f\"{ten:<8} {str(ts.kind):<20} mặc định={ts.default}\")\n\n# a        POSITIONAL_OR_KEYWORD  mặc định=<class 'inspect._empty'>\n# b        POSITIONAL_OR_KEYWORD  mặc định=2\n# args     VAR_POSITIONAL         mặc định=<class 'inspect._empty'>\n# c        KEYWORD_ONLY           mặc định=<class 'inspect._empty'>\n# kwargs   VAR_KEYWORD            mặc định=<class 'inspect._empty'>\n```\n\n`inspect` hữu ích khi viết công cụ hoặc khi đọc thư viện lạ: nó cho biết chính xác hàm nhận gì mà không phải đọc mã nguồn. Cột `kind` cũng là cách kiểm chứng lại sáu loại tham số đã nói ở phần cơ chế.",

so_sanh: "### Sáu loại tham số\n\n| Loại | Cú pháp | Gọi bằng | Bắt buộc |\n|---|---|---|---|\n| Chỉ-vị-trí | `a, /` | vị trí | ✓ |\n| Vị trí hoặc tên | `a` | cả hai | ✓ |\n| Có mặc định | `a=1` | cả hai | ✗ |\n| Gom vị trí | `*args` | vị trí | ✗ |\n| Chỉ-tên | `*, a` | **tên** | ✓ |\n| Gom tên | `**kwargs` | tên | ✗ |\n\nThứ tự trong bảng cũng là thứ tự bắt buộc khi viết định nghĩa hàm. Viết sai là `SyntaxError` ngay, nên không có nguy cơ lỗi âm thầm ở đây.\n\n### `*` và `**` — định nghĩa so với gọi\n\n| | Trong định nghĩa | Khi gọi |\n|---|---|---|\n| `*` | **gom** vị trí → `tuple` | **trải** iterable ra |\n| `**` | **gom** tên → `dict` | **trải** dict ra |\n| Ví dụ | `def f(*a)` | `f(*ds)` |\n\nMột ký hiệu, hai ý nghĩa ngược nhau — đây là chỗ người mới nhầm nhiều nhất. Mẹo nhớ: **ở định nghĩa thì hàm đang thu vào, ở lời gọi thì bạn đang phát ra.**\n\n### Mutable và immutable làm giá trị mặc định\n\n| | Immutable (`0`, `\"\"`, `None`, `(1,2)`) | Mutable (`[]`, `{}`, `set()`) |\n|---|---|---|\n| An toàn | **✓** | **✗** |\n| Chia sẻ giữa các lần gọi | có, nhưng vô hại | **có, và gây lỗi** |\n| Cách viết đúng | dùng trực tiếp | **dùng `None` rồi tạo trong hàm** |\n\nCùng hàng này còn có một trường hợp ít ai để ý: **lời gọi hàm làm giá trị mặc định**, như `datetime.now()` hay `uuid4()`. Chúng chạy một lần lúc định nghĩa, nên giá trị bị đóng băng — cùng bản chất bẫy, khác biểu hiện.\n\n### `lambda` và `def`\n\n| | `lambda` | `def` |\n|---|---|---|\n| Số câu lệnh | **một biểu thức** | tuỳ ý |\n| Có tên | `<lambda>` | **tên thật** |\n| Docstring | ✗ | ✓ |\n| Truy vết lỗi rõ ràng | ✗ | **✓** |\n| Dùng đúng chỗ | truyền trực tiếp làm `key=` | mọi trường hợp khác |\n\nPEP 8 khuyên **không gán `lambda` cho một tên**. Lý do thực dụng chứ không phải hình thức: khi hàm lỗi, truy vết hiện `<lambda>` thay vì tên có nghĩa, và đó là lúc bạn cần thông tin nhất.\n\n### Ba cách xử lý dữ liệu đầu vào\n\n| | Sửa tại chỗ | Trả về bản mới | Vừa sửa vừa trả |\n|---|---|---|---|\n| Bộ nhớ | **ít nhất** | tốn gấp đôi | ít |\n| Tác dụng phụ | có, **rõ ràng** | **không** | có, **bị che giấu** |\n| Trả về | `None` | dữ liệu mới | dữ liệu cũ đã sửa |\n| Ví dụ chuẩn | `ds.sort()` | `sorted(ds)` | — |\n| Nên dùng | khi dữ liệu lớn | **mặc định** | **không bao giờ** |\n\nCột cuối là phản mẫu cần tránh: nó khiến người gọi tưởng mình nhận bản mới trong khi bản cũ đã bị sửa. Nếu buộc phải sửa tại chỗ, hãy **nói rõ trong tên hàm** và trả `None`.\n\n### Khi nào dùng `**kwargs`\n\n| Tình huống | Nên dùng `**kwargs`? |\n|---|---|\n| Hàm bao / decorator chuyển tiếp | **✓ đúng chỗ nhất** |\n| Gộp cấu hình từ nhiều nguồn | ✓ |\n| Hàm nhận nhiều tuỳ chọn tuỳ ý | ✓ nếu có tài liệu rõ |\n| Lười liệt kê tham số | **✗** |\n| API công khai cho người khác dùng | **✗ mất tính tự tài liệu** |\n\nHai hàng cuối là cảnh báo chính: `**kwargs` khiến trình soạn thảo không gợi ý được, và **gõ sai tên tham số không ai báo lỗi**. Với API công khai, liệt kê tham số tường minh là món quà cho người dùng.",

loi_thuong_gap: [
  "**Dùng `[]`, `{}`, `set()` làm giá trị mặc định.** Chúng được tạo một lần và nhớ dữ liệu giữa các lần gọi. Dùng `None` rồi tạo trong hàm.",
  "**Dùng `datetime.now()` hay `uuid4()` làm mặc định.** Bị đóng băng lúc import. Cùng bản chất bẫy trên.",
  "**Sửa `if ds is None` thành `if not ds`.** Khi người gọi cố ý truyền `[]`, bạn thay list của họ và mất tác dụng phụ họ mong đợi.",
  "**Tưởng hàm không sửa được dữ liệu người gọi.** Mutable thì sửa được. Muốn an toàn thì `ds = list(ds)` ở đầu hàm.",
  "**Viết hàm vừa sửa tại chỗ vừa trả về dữ liệu.** Người gọi không biết chuyện gì xảy ra. Chọn một trong hai.",
  "**Đặt tham số bắt buộc sau tham số có mặc định.** `SyntaxError` — nhưng biết lý do thì không mất thời gian đoán.",
  "**Dùng `**kwargs` cho API công khai.** Mất gợi ý của trình soạn thảo, và gõ sai tên tham số thì im lặng.",
  "**Không dùng `*` để ép tham số cờ thành chỉ-tên.** Lời gọi `f(x, True, False, 3)` không ai đọc hiểu.",
  "**Gán `lambda` cho một biến.** Dùng `def` — truy vết lỗi sẽ có tên thật.",
  "**Tạo `lambda` trong vòng lặp mà không đóng băng biến.** Mọi hàm dùng chung giá trị cuối. Dùng `lambda i=i:` hoặc `partial`.",
  "**Quên `functools.wraps` trong decorator.** Hàm bị bao mất tên và docstring gốc.",
  "**Dùng `groupby` mà quên sắp xếp trước.** Nó chỉ gom phần tử liền kề — kết quả sai mà không báo lỗi.",
  "**Sửa trực tiếp `dict` cấu hình toàn cục trong hàm.** Dùng `{**MAC_DINH, **tuy_chon}` để tạo bản mới.",
  "**Nhầm `*` khi gọi với `*` khi định nghĩa.** Định nghĩa là gom vào, gọi là trải ra."
],

tu_kiem_tra: [
  "Vì sao `def f(ds=[])` khiến `ds` nhớ dữ liệu giữa các lần gọi? Giá trị mặc định được tạo lúc nào?",
  "Chứng minh bẫy đó bằng `f.__defaults__`. Cách sửa chuẩn là gì?",
  "Vì sao dùng `if ds is None` chứ không phải `if not ds`?",
  "`def f(luc=datetime.now())` sai ở đâu, và biểu hiện của nó khác bẫy `[]` thế nào?",
  "Python truyền tham số theo kiểu gì? Hàm sửa được gì và không sửa được gì của người gọi?",
  "Nêu sáu loại tham số theo đúng thứ tự bắt buộc. Vì sao thứ tự đó là bắt buộc?",
  "`*` và `**` có ý nghĩa gì trong định nghĩa hàm, và khi gọi hàm?",
  "Dấu `*` trong `def f(a, *, b)` làm gì? Nêu ba lợi ích của tham số chỉ-tên.",
  "Vì sao `**kwargs` không nên dùng cho API công khai?",
  "Vì sao `[lambda: i for i in range(3)]` cho `[2,2,2]`? Hai cách sửa?",
  "Vì sao PEP 8 khuyên không gán `lambda` cho một tên?",
  "Vì sao `sorted()` trả bản mới còn `list.sort()` trả `None`? Đó là quy ước gì?",
  "`{**MAC_DINH, **tuy_chon}` an toàn hơn việc sửa trực tiếp `MAC_DINH` ở chỗ nào?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Defining Functions* và *More on Defining Functions* — mô tả đủ sáu loại tham số kèm ví dụ.",
  "Tài liệu Python, mục *FAQ → Why are default values shared between objects?* — giải thích chính thức về bẫy giá trị mặc định.",
  "Sách **Effective Python** — Brett Slatkin. Nhiều mục ngắn về tham số chỉ-tên, `*args`, và giá trị mặc định động.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về hàm như đối tượng hạng nhất, và chương về closure giải thích kỹ bẫy `lambda` trong vòng lặp.",
  "Tài liệu Python, module `functools` và `inspect` — `partial`, `wraps`, `signature` là ba công cụ dùng nhiều nhất khi làm việc với hàm."
],

lien_he: [
  "**Cú pháp, kiểu dữ liệu, toán tử**: \"biến là nhãn\" và mutable/immutable là nguyên nhân gốc của cả bẫy mặc định lẫn việc hàm sửa được dữ liệu người gọi.",
  "**List, dict, set, tuple**: `*args` là `tuple`, `**kwargs` là `dict` — mọi tính chất của chúng vẫn đúng.",
  "**Comprehension và biểu thức generator** ngay trước: `lambda` xuất hiện ở đó, và `ds = list(ds)` vật chất hoá generator.",
  "**Decorator — hàm bọc hàm** trong module này: dựng hoàn toàn trên `*args`, `**kwargs` và `functools.wraps`.",
  "**Type hint và mypy** ở cuối module: chú thích kiểu cho tham số, và cách mô tả kiểu của `*args`/`**kwargs`.",
  "**Xử lý ngoại lệ** trong module này: `raise ValueError` khi tham số không hợp lệ là mẫu kiểm tra đầu vào chuẩn.",
  "**Stack vs heap** ở module 2: mỗi lần gọi hàm tạo một khung stack; giá trị mặc định thì nằm trên heap và sống cùng object hàm.",
  "**Clean Code** ở module 8 mảng 01: số lượng tham số, tác dụng phụ, và đặt tên hàm là ba chủ đề trung tâm ở đó.",
  "**AI/ML** ở mảng 07: các thư viện học máy có hàm hàng chục tham số chỉ-tên; hiểu `**kwargs` là điều kiện để đọc và chuyển tiếp cấu hình đúng."
]

};
