window.LESSONS["04:van-hanh-thuc-te:connection-pooling"] = {

tom_tat: [
  "Connection pool tái sử dụng connection PostgreSQL đã thiết lập và biến giới hạn connection thành hàng đợi có kiểm soát; nó không làm database có thêm năng lực.",
  "Kích thước pool phải tính trên toàn bộ replica và worker, chừa ngân sách cho migration, giám sát và quản trị; cấu hình “mỗi process 20” có thể nhân thành hàng trăm.",
  "Checkout ngắn, transaction ngắn và backpressure quan trọng hơn pool thật lớn. Pool đầy là tín hiệu tải hoặc leak cần quan sát, không phải lời mời tăng số tùy ý.",
  "Timeout kết nối, timeout chờ pool và statement timeout bảo vệ ba giai đoạn khác nhau; lỗi phải rollback để connection sạch trước khi trả về.",
  "PgBouncer và pool trong SQLAlchemy giải các tầng khác nhau; transaction pooling có hạn chế với session state và prepared statement cần hiểu trước khi dùng."
],

can_biet_truoc: [
  "TCP connection, TLS handshake và process/thread từ mảng hệ điều hành và mạng.",
  "Transaction PostgreSQL, `commit`, `rollback` và trạng thái aborted sau lỗi.",
  "SQLAlchemy Engine, Session và phạm vi request ở bài ORM.",
  "Khái niệm latency percentile, queue, timeout và backpressure."
],

dinh_nghia: "**Connection pooling** là cơ chế giữ một tập connection database đã mở để nhiều đơn vị công việc mượn, dùng rồi trả lại, thay vì thiết lập và xác thực connection mới cho từng query. SQLAlchemy `QueuePool` đặt giới hạn connection thường trực (`pool_size`), phần vượt tạm thời (`max_overflow`) và thời gian chờ checkout (`pool_timeout`).\n\nPool là bộ điều phối tài nguyên hữu hạn, không phải cache kết quả và không phải bộ tăng tốc vô hạn. Mỗi connection PostgreSQL gắn với backend, bộ nhớ và trạng thái session; quá nhiều connection làm tăng contention và chi phí scheduling. Mục tiêu là giữ đủ concurrency để database bận hữu ích, đồng thời tạo hàng đợi/backpressure trước khi nó quá tải.",

vi_sao_can: "Mỗi request Vé Mây mở connection mới sẽ trả chi phí TCP, TLS, authentication và thiết lập session, đồng thời tạo bão kết nối lúc traffic tăng. Giữ connection vô hạn lại đẩy PostgreSQL tới `max_connections`, làm migration và lệnh cứu hộ không còn chỗ đăng nhập.\n\nMột cấu hình nhìn nhỏ ở laptop có thể nổ trên production: 8 web replica, mỗi replica 4 worker process, mỗi process pool 10 và overflow 10 tạo trần lý thuyết 640 connection. Con số này chưa gồm job thanh toán, Alembic, dashboard và admin. Pool vì vậy là bài toán ngân sách cấp hệ thống, không phải tham số cục bộ của một Engine.\n\nPool còn làm lỗi vòng đời lộ rõ. Request streaming giữ transaction trong lúc client tải chậm; exception không rollback trả connection bẩn; job gọi API ngoài khi đang giữ connection. Tất cả kéo dài checkout, làm request khác xếp hàng và tạo hiệu ứng dây chuyền. Quan sát pool wait cùng query latency giúp phân biệt database chậm với ứng dụng giữ tài nguyên sai.",

co_che: "### Vòng đời checkout và checkin\n\nEngine nhận query; pool trả connection nhàn rỗi hoặc mở connection nếu còn quota. Khi transaction xong và Session đóng, connection được reset rồi trả hàng đợi. “Đóng” SQLAlchemy connection thường là checkin, không đóng socket vật lý.\n\nNếu mọi slot bận, caller chờ tối đa `pool_timeout`; hết hạn nhận lỗi. Overflow connection được tạo khi pool cơ sở đầy và thường đóng sau checkin, giúp hấp thụ burst ngắn nhưng không nên biến thành tải ổn định.\n\n### Công thức ngân sách toàn dịch vụ\n\nTrần xấu nhất gần bằng `replica × process mỗi replica × (pool_size + max_overflow)` cho mỗi Engine độc lập. Thread trong cùng process thường chia Engine; process fork không được chia connection đã mở. Worker Celery, scheduler và read replica có pool riêng.\n\nTừ `max_connections` PostgreSQL phải trừ superuser reserve, autovacuum/background, replication, migration, monitoring, admin và dịch vụ khác. Phần còn lại mới chia cho Vé Mây. Không cấu hình sát trần vì khi incident chính là lúc cần connection quan sát.\n\n```text\nNgân sách DB 120\n- 20 vận hành/background\n- 20 payment worker\n= 80 cho web\n8 replica × 2 process → khoảng 5 connection cơ sở/process\n```\n\nĐây là ví dụ tính, không phải khuyến nghị production. Phải đo concurrency query thật và capacity database.\n\n### Little's Law như trực giác\n\nSố query đồng thời xấp xỉ throughput × thời gian giữ connection. Nếu 200 request/s nhưng mỗi request chỉ giữ connection 20 ms, concurrency trung bình khoảng 4; không cần 200 connection. Nếu code giữ transaction 500 ms để gọi payment API, concurrency thành khoảng 100 dù SQL vẫn nhanh.\n\nVì thế giảm hold time thường tốt hơn tăng pool. Metric cần là checkout duration, không chỉ statement duration.\n\n### Session theo request nhưng checkout muộn\n\nCó thể tạo Session đầu request mà chưa checkout cho tới query đầu. Sau query cuối, commit/rollback và đóng càng sớm càng tốt. Không giữ transaction trong lúc render template lớn, gửi email hay gọi mạng.\n\n```python\ndef create_booking(command):\n    validate_without_db(command)\n    with Session(engine) as session:\n        with session.begin():\n            booking = persist_booking(session, command)\n    enqueue_payment(booking.id)  # ngoài transaction; cần outbox nếu yêu cầu nguyên tử\n```\n\nĐoạn enqueue ngoài transaction tạo cửa sổ mất message; thiết kế outbox giải bài toán đó nhưng không nên giữ connection trong lúc chờ broker để “vá”.\n\n### Ba loại timeout\n\nConnect timeout giới hạn mở socket/auth. Pool timeout giới hạn chờ slot trong ứng dụng. Statement timeout giới hạn query đang chạy trên server. Request deadline phải bao các lớp và để đủ thời gian cleanup; nếu HTTP timeout trước nhưng query vẫn chạy, hệ thống làm việc vô ích.\n\nTimeout không phải retry mù. Query đọc idempotent có thể retry có jitter trong budget; transaction tạo booking cần idempotency key và phải biết commit có xảy ra không.\n\n### Reset khi trả pool\n\nRollback-on-return dọn transaction chưa kết thúc. Nó không nhất thiết dọn mọi session state như temporary table, `SET` lâu dài, advisory lock hay LISTEN. Tránh phụ thuộc trạng thái connection hoặc reset rõ ràng. Dùng `SET LOCAL` trong transaction để timeout tự trở lại sau commit/rollback.\n\nConnection gặp network error phải invalidated, không trả như connection tốt. SQLAlchemy dialect nhận diện nhiều disconnect, nhưng ứng dụng vẫn phải xử lý exception.\n\n### pre_ping và recycle\n\n`pool_pre_ping=True` chạy kiểm tra nhẹ khi checkout để phát hiện connection bị load balancer/firewall đóng khi nhàn rỗi. Nó thêm round trip cho checkout; chỉ bật vì môi trường có stale connection hoặc độ tin cậy đáng giá chi phí.\n\n`pool_recycle` giới hạn tuổi connection trước khi thay ở checkout, hữu ích khi hạ tầng có idle lifetime. Nó không thu hồi connection đang bận và không sửa leak.\n\n### Pool quá nhỏ và quá lớn\n\nQuá nhỏ tạo wait dù database còn dư năng lực; p95 pool wait tăng trong khi query duration thấp. Quá lớn cho phép burst tràn thẳng vào database, tăng lock contention, cache churn và context switch; query duration tăng đồng loạt.\n\nĐiểm tốt được tìm bằng load test với workload đại diện, tăng dần concurrency và quan sát throughput, latency, CPU, I/O, lock, active connection. Khi throughput ngừng tăng mà latency tăng, thêm connection không giúp.\n\n### Leak và dấu hiệu nhận biết\n\nLeak là checkout không checkin do Session không đóng, generator chưa kết thúc hoặc exception path bỏ cleanup. Pool `checkedout` tăng và không trở xuống sau traffic; database có connection idle in transaction; stack trace checkout hoặc event listener giúp truy nguồn.\n\nContext manager và middleware `finally` là phòng thủ chính. Garbage collection không phải lifecycle manager đáng tin.\n\n### Idle in transaction nguy hiểm hơn idle\n\nConnection `idle` không có transaction chỉ chiếm slot. `idle in transaction` giữ snapshot và có thể giữ lock, cản vacuum dọn tuple chết, làm bloat. Đặt `idle_in_transaction_session_timeout` phía server như dây an toàn, nhưng phải sửa code giữ transaction.\n\nQuan sát `pg_stat_activity` với `state`, `xact_start`, `query_start`, `wait_event_type`; đừng kill hàng loạt chỉ dựa vào tuổi connection vì pool cố ý giữ connection lâu.\n\n### N+1 nhân áp lực pool gián tiếp\n\nTrong một request, N+1 thường dùng cùng connection tuần tự nên không checkout N connection. Nhưng nó kéo dài thời gian giữ slot; theo Little's Law, concurrency slot cần tăng. Sửa loader giảm cả query count, hold time và pool wait.\n\nQuery plan thiếu index cũng gây hiệu ứng tương tự. Pool timeout chỉ là triệu chứng ở tầng ngoài; root cause có thể là sequential scan trên bookings.\n\n### Transaction lock và pool exhaustion\n\nMột query chờ row lock vẫn giữ connection. Nhiều request cùng tranh ghế sự kiện nóng có thể chiếm hết pool dù CPU database thấp. `pg_stat_activity.wait_event_type='Lock'` và `pg_locks` phân biệt với query tính toán chậm.\n\nAtomic update `WHERE seats_remaining >= :qty` rút ngắn lock hơn quy trình đọc–gọi API–ghi. Đặt payment call ngoài lock và dùng trạng thái pending/idempotency.\n\n### Async không tạo thêm capacity\n\nAsync cho một process chờ I/O hiệu quả, nhưng mỗi query đang chạy vẫn cần connection. Async pool phải dùng async driver/engine; không gọi sync DB trong event loop. Pool size vẫn dựa trên DB budget, không dựa vào số coroutine có thể tạo.\n\nHàng nghìn coroutine cùng checkout sẽ xếp hàng trong app. Cần semaphore/request limit và deadline để backpressure lan tới caller có kiểm soát.\n\n### Fork và worker process\n\nSocket mở trước `fork` không an toàn khi hai process dùng chung file descriptor/protocol. Tạo Engine sau fork hoặc dispose pool trong child theo hướng dẫn framework. Gunicorn preload có thể vô tình mang pool từ parent nếu code kết nối lúc import.\n\nTest bằng PID logging cho checkout và kiểm mỗi backend thuộc đúng worker. Lỗi fork thường ngẫu nhiên, manifest thành protocol error hoặc connection reset.\n\n### PgBouncer giải tầng nào\n\nApplication pool giảm connect churn trong process. PgBouncer đứng giữa nhiều client và PostgreSQL, có thể multiplex connection. Session pooling giữ server connection suốt client session; transaction pooling trả server connection sau mỗi transaction, tăng multiplexing nhưng session state không bền.\n\nVới transaction pooling, temporary table, session-level advisory lock, `SET` không LOCAL và một số cách dùng prepared statement cần xem xét theo cấu hình. Không bật rồi giả định tương thích. Application pool trước PgBouncer vẫn cần giới hạn; nếu quá lớn, chỉ chuyển hàng đợi sang PgBouncer.\n\n### Read replica và nhiều pool\n\nEngine đọc/ghi tách đôi tạo hai ngân sách. Sau khi tạo booking, đọc ngay replica có thể chưa thấy do replication lag. Routing phải theo consistency requirement: read-your-writes về primary trong một khoảng hoặc dùng token/LSN nếu hệ thống thiết kế sâu hơn.\n\nPool replica đầy không nên tự động đổ mọi đọc sang primary nếu điều đó làm primary chết theo. Circuit breaker và degradation policy phải rõ.\n\n### Health check không được gây bão\n\nLiveness không cần query database mỗi giây; process sống khác database sẵn sàng. Readiness có thể kiểm checkout/query nhẹ với tần suất cache, nhưng hàng trăm pod probe đồng thời tạo tải. Pool metrics tốt hơn cho quan sát liên tục.\n\nKhi database down, retry connect đồng loạt gây thundering herd. Dùng exponential backoff có jitter và giới hạn tổng deadline.\n\n### Shutdown êm\n\nKhi pod nhận stop, ngừng nhận request mới, chờ transaction trong giới hạn, đóng Session và dispose Engine. Kill ngay giữa commit tạo kết quả không chắc chắn cho caller; idempotency key giúp retry an toàn.\n\nDispose đóng connection nhàn rỗi; connection đang checkout cần owner trả. Shutdown timeout phải nhỏ hơn termination grace nhưng đủ cho query bình thường.\n\n### Quan sát đúng bốn lớp\n\nỨng dụng: checked-in, checked-out, overflow, pool wait, checkout duration. PostgreSQL: active/idle/idle-in-transaction, wait events, transaction age. Query: calls, total/mean latency, plan và buffers. Hệ thống: CPU, I/O, network, replica lag.\n\nChỉ nhìn số connection không nói chúng hữu ích hay đang chờ lock. Dashboard nên đặt pool wait cạnh statement latency và HTTP latency để thấy quan hệ nhân quả.\n\n### Khi nào NullPool hợp lý\n\nCLI chạy một lần, migration job hoặc môi trường có PgBouncer transaction pool đôi khi dùng `NullPool` để không giữ client connection. Server web thường hưởng lợi từ QueuePool. Serverless có số instance bùng nhanh cần proxy/pool ngoài process và giới hạn concurrency; pool cục bộ không phối hợp được tổng toàn fleet.\n\nKhông chọn theo “best practice” chung; chọn nơi hàng đợi nên sống và ai có đủ thông tin để áp backpressure.",

vi_du: "### Engine và lifecycle cho Vé Mây\n\n```python\nfrom contextlib import contextmanager\nfrom sqlalchemy import create_engine, event, text\nfrom sqlalchemy.orm import Session\nimport time\n\nengine = create_engine(\n    \"postgresql+psycopg://vemay_runtime:secret@localhost/vemay\",\n    pool_size=5,\n    max_overflow=2,\n    pool_timeout=3,\n    pool_pre_ping=True,\n    connect_args={\"connect_timeout\": 5},\n)\n\n@contextmanager\ndef session_scope():\n    with Session(engine) as session:\n        try:\n            with session.begin():\n                session.execute(text(\"SET LOCAL statement_timeout = '2s'\"))\n                yield session\n        except Exception:\n            # session.begin() rollback; giữ except để metric/phân loại rồi ném tiếp\n            raise\n\ndef expire_pending(limit: int = 100):\n    with session_scope() as session:\n        ids = session.execute(text(\"\"\"\n          WITH picked AS (\n            SELECT id FROM bookings\n            WHERE status = 'pending' AND expires_at <= now()\n            ORDER BY expires_at, id\n            FOR UPDATE SKIP LOCKED\n            LIMIT :limit\n          )\n          UPDATE bookings AS b SET status = 'expired'\n          FROM picked WHERE b.id = picked.id\n          RETURNING b.id\n        \"\"\"), {\"limit\": limit}).scalars().all()\n    return ids\n```\n\nPassword chỉ minh họa cú pháp; production lấy secret ngoài source. Pool 5+2 chưa phải cấu hình được khuyến nghị nếu chưa tính replica/process và load test.\n\n### Đo pool wait, không ghi benchmark bịa\n\n```python\ncheckout_started = {}\n\n@event.listens_for(engine, \"checkout\")\ndef on_checkout(dbapi_connection, connection_record, connection_proxy):\n    connection_record.info[\"checked_out_at\"] = time.perf_counter()\n\n@event.listens_for(engine, \"checkin\")\ndef on_checkin(dbapi_connection, connection_record):\n    started = connection_record.info.pop(\"checked_out_at\", None)\n    if started is not None:\n        observe_checkout_seconds(time.perf_counter() - started)\n```\n\nMuốn đo thời gian chờ chính xác, instrument quanh lúc bắt đầu operation tới checkout event hoặc dùng telemetry integration; đoạn trên đo thời gian giữ connection. Load test phải in cấu hình, số replica giả lập, dataset và percentile. Không ghi một con số “pool tăng tốc X lần” vì kết quả phụ thuộc handshake và workload.\n\n### Query kiểm tra production\n\n```sql\nSELECT state, wait_event_type, wait_event, count(*)\nFROM pg_stat_activity\nWHERE datname = current_database()\nGROUP BY state, wait_event_type, wait_event\nORDER BY count(*) DESC;\n\nSELECT pid, now() - xact_start AS transaction_age, state, wait_event_type,\n       left(query, 120) AS query_excerpt\nFROM pg_stat_activity\nWHERE xact_start IS NOT NULL\nORDER BY xact_start\nLIMIT 20;\n```\n\nCa biên: pool size 1 với hai request; database restart làm stale socket; query timeout để transaction aborted; client hủy request; worker fork; replica lag; traffic bằng 0; burst vượt overflow. Mỗi ca cần xác nhận connection quay về, metric tăng đúng và lỗi công khai không lộ credential.",

so_sanh: "### Các nơi đặt pool\n\n| Lựa chọn | Tái sử dụng trong process | Phối hợp nhiều replica | Giữ session state | Trường hợp điển hình |\n|---|---:|---:|---:|---|\n| SQLAlchemy QueuePool | Có | Không | Có trên connection lúc checkout | Web service ổn định |\n| NullPool | Không | Không | Không dựa vào tái sử dụng | CLI/migration ngắn |\n| PgBouncer session | Có ở proxy | Có | Có trong client session | Giảm connection backend |\n| PgBouncer transaction | Có ở transaction | Có | Hạn chế | Fleet lớn, transaction ngắn |\n\n| Triệu chứng | Pool wait | Query duration | Hướng điều tra đầu tiên |\n|---|---:|---:|---|\n| Pool quá nhỏ | Cao | Thấp | Budget và concurrency |\n| Query/index chậm | Tăng theo | Cao | Plan, buffers, statistics |\n| Chờ lock | Tăng theo | Cao | `pg_locks`, transaction age |\n| Leak Session | Tăng dần | Có thể thấp | checkout không checkin |\n\nTăng pool chỉ hợp hàng đầu tiên sau khi xác nhận database còn headroom. Ba hàng còn lại cần sửa nguyên nhân.",

loi_thuong_gap: [
  "Nhân `pool_size` theo cảm giác nhưng quên replica và process. Tính trần toàn fleet trước khi deploy.",
  "Giữ transaction khi gọi payment API. Checkout duration cao dù SQL nhanh; commit trạng thái pending rồi dùng outbox/idempotency.",
  "Tăng pool khi query thiếu index. Database contention tăng và throughput không tăng; xem plan và buffers trước.",
  "Không đóng Session trên exception/generator. `checkedout` không trở về baseline; dùng context manager và `finally`.",
  "Trả connection đang aborted vào pool mà không rollback. Request sau gặp lỗi transaction; bảo đảm reset-on-return và lifecycle đúng.",
  "Chia Engine đã kết nối qua fork. Lỗi protocol ngẫu nhiên giữa worker; tạo/dispose pool theo PID.",
  "Bật PgBouncer transaction mode nhưng phụ thuộc temporary table hoặc `SET` session. Lỗi đổi theo connection backend; chuyển sang transaction-local hoặc thiết kế lại.",
  "Health probe query quá thường xuyên. Khi database yếu, probe tạo thêm bão; tách liveness/readiness và cache hợp lý."
],

tu_kiem_tra: [
  "Tính trần connection cho 6 replica, 3 process, pool 4 và overflow 2; vì sao đó chưa phải số nên cấp hết từ PostgreSQL?",
  "Dùng Little's Law giải thích vì sao giảm hold time từ 500 ms xuống 25 ms thay đổi nhu cầu pool dù throughput giữ nguyên.",
  "Phân biệt connect timeout, pool timeout và statement timeout bằng vị trí lỗi.",
  "Vì sao N+1 thường không lấy N connection cùng lúc nhưng vẫn làm pool exhaustion?",
  "Những session state nào nguy hiểm với PgBouncer transaction pooling?",
  "Dashboard thấy CPU thấp, pool wait cao, nhiều connection chờ `Lock`. Bạn điều tra và sửa theo thứ tự nào?",
  "Tại sao async cho phép nhiều coroutine nhưng không cho PostgreSQL xử lý vô hạn query đồng thời?",
  "Thiết kế shutdown để booking không bị tạo hai lần khi client retry sau connection reset lúc commit."
],

tai_nguyen: [
  "Tài liệu chính thức SQLAlchemy, các phần Connection Pooling, Engine Configuration và Session Basics.",
  "Tài liệu chính thức PostgreSQL, các phần Connections and Authentication, Monitoring Database Activity và Explicit Locking.",
  "Tài liệu chính thức PgBouncer, các phần Features, Pool Modes và Usage.",
  "“Database Reliability Engineering” — Laine Campbell và Charity Majors."
],

lien_he: [
  "Nối sang mảng Hệ điều hành: mỗi PostgreSQL backend và socket là tài nguyên hữu hạn; pool là semaphore/hàng đợi ở cấp ứng dụng.",
  "Nối sang mảng Mạng máy tính: connect churn trả giá TCP/TLS, còn stale connection liên quan timeout của load balancer và NAT.",
  "Nối sang mảng Thiết kế hệ thống: backpressure, circuit breaker, idempotency và outbox quyết định cách dịch vụ suy giảm khi database bão hòa.",
  "Nối sang mảng Quan sát: pool wait, lock wait và query plan phải được tương quan để tránh chữa triệu chứng bằng tăng connection."
]

};
