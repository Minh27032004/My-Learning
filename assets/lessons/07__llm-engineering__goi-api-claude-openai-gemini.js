window.LESSONS["07:llm-engineering:goi-api-claude-openai-gemini"] = {

tom_tat: [
  "Gọi LLM API không chỉ là gửi một chuỗi và nhận một chuỗi; contract thật gồm authentication, model, role/instructions, content đa phần, trạng thái hoàn tất, usage, request ID và lỗi có cấu trúc.",
  "Claude, OpenAI và Gemini biểu diễn cùng ý tưởng bằng object khác nhau. Lớp adapter nên chuẩn hoá nhu cầu của ứng dụng, nhưng vẫn giữ metadata gốc để không che mất capability và tín hiệu lỗi.",
  "API key chỉ ở server/secret manager, không nằm trong source code, trình duyệt hay log. Model không phải nơi giữ bí mật và output không được tin như dữ liệu đã kiểm chứng.",
  "Timeout, rate limit và lỗi 5xx là bình thường. Retry chỉ an toàn khi có backoff, jitter, giới hạn lần thử và hiểu tác động của request trùng; lỗi 4xx cấu hình thường không nên retry.",
  "Ví dụ Mây Shop dùng cùng `SupportRequest` để gọi ba provider, nhưng model ID được cấu hình bằng biến môi trường vì tên model và quyền truy cập thay đổi theo thời gian."
],

can_biet_truoc: [
  "Python: biến môi trường, `dataclass`, exception, protocol và cài package bằng môi trường ảo.",
  "HTTP: header, status code, timeout, request/response JSON và phân biệt lỗi client 4xx với lỗi server 5xx.",
  "Hai bài trước: sampling và context window, vì các tham số/usage xuất hiện trực tiếp trong request.",
  "Async/concurrency ở mức trực giác; gọi mạng không nên khoá worker vô hạn."
],

dinh_nghia: "**LLM API call** là một giao dịch mạng có xác thực: ứng dụng đóng gói chỉ dẫn và content theo schema của provider, chọn model/cấu hình, gửi HTTPS request, rồi giải mã response thành text, tool call, usage hoặc lỗi. SDK chính thức là lớp typed convenience quanh API; nó không loại bỏ latency, quota, partial completion hay thay đổi phiên bản.\n\nTrong bài này, “gọi Claude/OpenAI/Gemini” không có nghĩa ép ba dịch vụ thành giống hệt. Ta định nghĩa một contract nhỏ mà Mây Shop thật sự cần — soạn trả lời từ chính sách — và viết adapter ánh xạ contract đó sang từng SDK. Capability ngoài contract vẫn phải có đường truy cập, nếu không abstraction thấp nhất chung sẽ làm mất lợi thế từng provider.",

vi_sao_can: "Một snippet quickstart chứng minh credential và mạng hoạt động, chưa chứng minh hệ thống production đúng. Khi request timeout, ta không biết server chưa xử lý hay đã xử lý xong nhưng response thất lạc. Khi output bị cắt, `.text` vẫn có thể khác rỗng. Khi đổi provider, role names, content blocks và usage fields khác nhau. Những sai biệt đó nằm trên critical path của người dùng.\n\nMây Shop cần tránh ba kiểu coupling. Business code không nên biết `response.output_text` hay `content[0].text`; secret không nên đi cùng prompt; và model name không nên rải khắp repo. Adapter + configuration + error taxonomy khiến thay model là thay cấu hình hoặc một module, không phải sửa mọi handler.\n\nNhưng cần phản biện “multi-provider từ ngày đầu”: nếu chưa có yêu cầu dự phòng, compliance hay benchmark, một provider và SDK thẳng thường đơn giản hơn. Abstraction chỉ đáng trả giá khi contract đã rõ; xây một giao diện “hỗ trợ mọi feature” trước khi dùng sẽ thành framework nội bộ khó bảo trì.",

co_che: "### Request đi qua những ranh giới nào\n\nLuồng tối thiểu là handler → prompt builder → provider adapter → SDK/HTTP → service → response parser → validator → business decision. Mỗi ranh giới có kiểu lỗi khác. Prompt builder có thể thiếu policy; mạng có thể timeout; provider có thể rate-limit; parser có thể gặp content type mới; validator có thể từ chối câu trả lời dù HTTP 200.\n\nĐừng gộp tất cả thành `except Exception: return None`. Một `None` không cho biết nên retry, sửa request, hỏi lại người dùng hay cảnh báo vận hành.\n\n### Xác thực mà không làm rò key\n\nSDK chính thức thường đọc `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GEMINI_API_KEY` từ môi trường. Local dùng secret injection hoặc file `.env` không commit; production dùng secret manager và identity/quyền tối thiểu. Key phải có rotation, scope/quota phù hợp và không gửi xuống frontend.\n\nNếu browser gọi trực tiếp bằng key dài hạn, bất kỳ người dùng nào cũng có thể xem key trong developer tools. Backend proxy không chỉ “giấu chuỗi”: nó thực thi auth của người dùng, rate limit, input limit và audit. Không ghi toàn bộ Authorization header hay prompt chứa PII vào log.\n\n### Model ID là cấu hình có vòng đời\n\nTên model, alias, region và availability thay đổi; ví dụ học không nên tuyên bố một model là “mới nhất” mãi mãi. Vì thế code dùng `os.environ[\"..._MODEL\"]`. Deployment pin model/version theo yêu cầu tái lập, còn quá trình upgrade chạy eval trước.\n\nKhông tự fallback sang model khác khi quota hết nếu capability/safety khác. Fallback là quyết định sản phẩm có metric và thông báo; nếu nhãn/JSON contract đổi, một response “thành công” có thể nguy hiểm hơn lỗi rõ ràng.\n\n### Roles và instructions không hoàn toàn đồng nghĩa\n\nOpenAI Responses API có `instructions` và `input`; Anthropic Messages API có `system` cùng `messages`; Gemini có system instruction/config hoặc Interactions input tuỳ API. Ý nghĩa chung là phân tách policy của ứng dụng và lời người dùng, nhưng precedence/content representation không nên giả định giống bit-for-bit.\n\nAdapter nhận `system_text` và `user_text`, rồi ánh xạ rõ. Không nối chúng thành một chuỗi có nhãn giả, vì khi đó model khó phân biệt trust boundary và prompt injection dễ hơn.\n\n### Content là danh sách phần, không chỉ text\n\nResponse có thể gồm nhiều content blocks: text, tool call, image/annotation hay reasoning metadata. OpenAI khuyên dùng helper `output_text` khi chỉ cần text thay vì giả định phần tử đầu. Anthropic message content là danh sách block; phải ghép các block text phù hợp. Gemini response cũng có candidates/parts hoặc `output_text` ở Interactions.\n\nMột adapter text-only nên từ chối hoặc ghi nhận block lạ, không âm thầm coi tool call là câu trả lời rỗng. Bài function calling sau sẽ mở rộng union type thay vì phá contract cũ.\n\n### Trạng thái hoàn tất quan trọng ngang nội dung\n\nHTTP 200 chỉ nói server trả response hợp lệ. Generation có thể incomplete do giới hạn token, content filter hoặc lý do khác. Contract nội bộ nên có `finish_reason`/status, usage và provider request ID. Chỉ publish khi trạng thái chấp nhận được và validator pass.\n\nNếu câu bị cắt, retry với prompt giống hệt có thể sinh câu khác và tốn gấp đôi. Có thể tăng giới hạn trong trần chi phí, yêu cầu output ngắn hơn hoặc tiếp tục có kiểm soát; lựa chọn phụ thuộc dạng output.\n\n### Timeout là giới hạn chờ, không phải huỷ chắc chắn\n\nClient timeout nghĩa là client ngừng đợi. Request có thể vẫn hoàn tất phía server. Với tác vụ chỉ sinh draft, gọi trùng chủ yếu tốn tiền; với tool/action về sau, gọi trùng có thể tạo side effect. Vì vậy tách generation khỏi action và dùng idempotency/deduplication ở lớp hành động.\n\nĐặt connect timeout và read/overall timeout theo SLO, không để vô hạn. Timeout quá ngắn tạo retry storm; quá dài giữ worker và làm p99 xấu. Streaming thay đổi trải nghiệm first-token nhưng không đảm bảo tổng thời gian ngắn hơn.\n\n### Phân loại lỗi trước khi retry\n\n401/403 thường là credential/quyền: dừng và alert. 400 do schema/model: bug hoặc config, retry y hệt vô ích. 429 có thể do rate/quota: tôn trọng hướng dẫn server, backoff. 5xx và lỗi mạng tạm thời có thể retry hữu hạn. Content policy/validation failure cần đổi input hoặc chuyển người, không phải hammer endpoint.\n\nExponential backoff tăng khoảng chờ theo lần thử; jitter thêm ngẫu nhiên để nhiều worker không thức dậy cùng lúc. Có tổng deadline và retry budget, nếu không hàng đợi chậm sẽ tự khuếch đại tải.\n\n### Rate limit cần backpressure\n\nKhi lượng ticket tăng đột biến, chỉ retry không giải quyết giới hạn. Semaphore giới hạn concurrency, queue hấp thụ burst, admission control từ chối sớm tác vụ ít quan trọng, và metric theo provider/model cho biết bottleneck. Batch có thể hiệu quả cho offline nhưng tăng latency từng ticket.\n\nKhông dùng một API key chung không quota nội bộ cho mọi tenant: khách ồn có thể làm tất cả bị 429. Gắn usage với tenant/request để kiểm soát công bằng và điều tra chi phí.\n\n### Adapter nên chuẩn hoá cái gì\n\nChuẩn hoá input business (`system`, `user`, `max_output_tokens`), result (`text`, `provider`, `model`, `usage`, `finish`) và taxonomy lỗi. Không chuẩn hoá sớm tool schema, cache semantics, reasoning settings hoặc multimodal nếu ứng dụng chưa cần; các capability ấy khác đáng kể.\n\nGiữ `raw_metadata` giới hạn, không nhất thiết giữ raw prompt. Request ID của provider cực hữu ích khi mở ticket hỗ trợ. Adapter tests dùng fake client; integration tests thật chạy riêng vì tốn tiền và phụ thuộc mạng.\n\n### OpenAI Responses API\n\nSDK Python tạo `OpenAI()`, đọc key từ môi trường, rồi `client.responses.create(model=..., instructions=..., input=...)`. Text-only có thể đọc `response.output_text`; vẫn kiểm `response.status`, `incomplete_details` và `usage` khi contract cần.\n\nKhông hard-code một model “latest” trong bài. Deployment truyền `OPENAI_MODEL`. API surface được đối chiếu official OpenAI documentation tại thời điểm soạn; khi code thật phải pin dependency và đọc changelog trước upgrade.\n\n### Anthropic Messages API\n\nSDK Python tạo `Anthropic()`, gọi `client.messages.create(model=..., max_tokens=..., system=..., messages=[...])`. `max_tokens` là tham số bắt buộc trong mẫu Messages phổ biến. Kết quả `message.content` là blocks; ghép block có `type == \"text\"`. `stop_reason` và usage cần được chuyển sang result nội bộ.\n\nKhông lấy `content[0].text` mà không kiểm type: khi tool use xuất hiện, phần tử có thể không phải text. Model ID đi qua `ANTHROPIC_MODEL`, tránh bài học hết hạn vì alias thay đổi.\n\n### Gemini API\n\nTài liệu chính thức hiện khuyến nghị Interactions API cho dự án mới, trong khi `generateContent` vẫn được hỗ trợ cho tác vụ unary và có lượng code đang dùng lớn. Với Interactions Python: `genai.Client()` rồi `client.interactions.create(model=..., input=...)`, đọc `interaction.output_text`.\n\nĐiểm này minh hoạ vì sao adapter có ích nhưng cũng vì sao phải đọc docs: API mặc định thay đổi theo thời gian. Ví dụ production chọn Interactions hay generateContent theo feature/lifecycle, không trộn hai kiểu trong cùng adapter mà không test.\n\n### Usage, chi phí và quan sát\n\nGhi input/output tokens, latency, provider, model, status, số retry và request ID; không gắn raw PII. Aggregation theo route giúp phát hiện prompt phình hoặc model fallback ngoài ý muốn. Cost không chỉ giá/token: retry, output bị bỏ và context dư đều là waste.\n\nĐo p50/p95/p99 riêng. Average che timeout tail. Phân biệt queue time, network, time-to-first-token và total generation để tối ưu đúng tầng.\n\n### Validation sau HTTP 200\n\nText có thể rỗng, sai ngôn ngữ, hứa chính sách không có nguồn hoặc chứa prompt injection echo. Mây Shop kiểm độ dài, citation IDs, prohibited claims và schema ở các bài tiếp theo. Validator failure là một kết quả domain, không giả thành provider outage.\n\nKhông dùng regex duy nhất để “lọc an toàn” ngôn ngữ tự nhiên. Với hành động đổi/hoàn tiền, model chỉ đề xuất; code kiểm quyền và dữ liệu rồi người dùng xác nhận.\n\n### Testing pyramid cho integration\n\nUnit test prompt builder và adapter bằng fake response, gồm block text, nhiều block, incomplete và exception. Contract test chạy ít request thật trên sandbox/account test để phát hiện SDK drift. Eval test kiểm chất lượng trên ticket cố định. Load test phải có ngân sách/quota riêng, không vô tình gọi production model hàng nghìn lần.\n\nMock không chứng minh credential, model access hay response thật; live test không nên là unit test mặc định. Cả hai bổ sung nhau.\n\n### Khi nào không nên viết multi-provider adapter\n\nNếu prototype chỉ gọi text một nơi và thời gian là ưu tiên, gọi SDK trực tiếp với một wrapper mỏng đủ tốt. Nếu cần feature độc quyền như hosted tools, abstraction chung có thể cản trở. Thiết kế escape hatch hoặc adapter theo capability, đừng ép mẫu số chung thấp nhất.\n\nMulti-provider có chi phí eval: cùng prompt không bảo đảm cùng hành vi. “Đổi backend bằng một biến môi trường” chỉ đúng về cú pháp; về sản phẩm vẫn cần regression, safety và capacity test.",

vi_du: "### Ba hàm gọi cùng một contract\n\nĐoạn mã cần cài các SDK chính thức và có key/model trong môi trường. Nó không được chạy trong gate vì gọi dịch vụ thật; phần parser có thể unit test bằng fake object.\n\n```python\nimport os\nfrom dataclasses import dataclass\n\n@dataclass\nclass SupportRequest:\n    system: str\n    user: str\n    max_output_tokens: int = 300\n\n@dataclass\nclass SupportResult:\n    provider: str\n    model: str\n    text: str\n    finish: str | None\n\ndef call_openai(req: SupportRequest) -> SupportResult:\n    from openai import OpenAI\n    model = os.environ[\"OPENAI_MODEL\"]\n    response = OpenAI().responses.create(\n        model=model,\n        instructions=req.system,\n        input=req.user,\n        max_output_tokens=req.max_output_tokens,\n    )\n    return SupportResult(\"openai\", model, response.output_text,\n                         getattr(response, \"status\", None))\n\ndef call_anthropic(req: SupportRequest) -> SupportResult:\n    from anthropic import Anthropic\n    model = os.environ[\"ANTHROPIC_MODEL\"]\n    message = Anthropic().messages.create(\n        model=model,\n        max_tokens=req.max_output_tokens,\n        system=req.system,\n        messages=[{\"role\": \"user\", \"content\": req.user}],\n    )\n    text = \"\".join(\n        block.text for block in message.content\n        if getattr(block, \"type\", None) == \"text\"\n    )\n    return SupportResult(\"anthropic\", model, text, message.stop_reason)\n\ndef call_gemini(req: SupportRequest) -> SupportResult:\n    from google import genai\n    model = os.environ[\"GEMINI_MODEL\"]\n    # Interactions là interface được docs Gemini khuyến nghị cho dự án mới.\n    interaction = genai.Client().interactions.create(\n        model=model,\n        input=f\"CHỈ DẪN:\\n{req.system}\\n\\nYÊU CẦU:\\n{req.user}\",\n    )\n    return SupportResult(\"gemini\", model, interaction.output_text, None)\n```\n\nNhánh Gemini nối text để giữ ví dụ tối thiểu; production nên dùng system-instruction facility đúng API/config thay vì coi nhãn chuỗi là ranh giới quyền. Đây là một giới hạn công khai, không phải mẫu để sao chép mù quáng.\n\n### Business code không biết SDK\n\n```python\nPOLICY = \"Chỉ dùng chính sách được cung cấp; thiếu dữ kiện thì hỏi lại.\"\nticket = SupportRequest(\n    system=POLICY,\n    user=\"Đơn MS1042, áo bung chỉ. Soạn câu trả lời dưới 80 từ.\",\n)\n\nproviders = {\n    \"openai\": call_openai,\n    \"anthropic\": call_anthropic,\n    \"gemini\": call_gemini,\n}\nchosen = os.environ.get(\"LLM_PROVIDER\", \"openai\")\nresult = providers[chosen](ticket)\nif not result.text.strip():\n    raise RuntimeError(f\"{chosen} trả text rỗng; finish={result.finish}\")\nprint(result.text)\n```\n\nKhông có key trong code. Không có model ID giả định. Tuy nhiên ví dụ chưa có retry/timeout/usage chuẩn hoá; đó là chủ đề riêng của bài streaming và retry, không nên giấu complexity trong decorator thiếu kiểm chứng.\n\n### Fake test cho parser Anthropic\n\n```python\nfrom types import SimpleNamespace\n\nfake = SimpleNamespace(content=[\n    SimpleNamespace(type=\"text\", text=\"Xin chào. \"),\n    SimpleNamespace(type=\"tool_use\", name=\"lookup_order\"),\n    SimpleNamespace(type=\"text\", text=\"Tôi sẽ kiểm tra đơn.\"),\n])\njoined = \"\".join(x.text for x in fake.content if x.type == \"text\")\nassert joined == \"Xin chào. Tôi sẽ kiểm tra đơn.\"\n```\n\nTest này bắt đúng lỗi `content[0]` nhưng không chứng minh SDK/API đang hoạt động. Muốn khẳng định integration pass phải chạy live test với tài khoản và ghi rõ chi phí; lượt soạn bài này không có credential nên không tuyên bố đã gọi thật.",

so_sanh: "### So contract, không xếp hạng nhà cung cấp\n\n| Trục | OpenAI Responses | Anthropic Messages | Gemini Interactions |\n|---|---|---|---|\n| Điểm vào Python | `responses.create` | `messages.create` | `interactions.create` |\n| Chỉ dẫn | `instructions` | `system` | theo input/config API |\n| Text tiện lợi | `output_text` | ghép text blocks | `output_text` |\n| Trạng thái | `status`, incomplete details | `stop_reason` | đọc interaction status/fields |\n| Multi-turn | response/conversation hoặc tự gửi state | gửi messages/state theo API | Interactions quản lý interaction |\n| Điều phải tránh | giả định `output[0]` | giả định `content[0]` là text | lẫn legacy `generateContent` với Interactions |\n\nBảng chỉ mô tả hình dạng API, không nói provider nào “tốt nhất”. Chọn theo eval workload Mây Shop, region/compliance, latency, giá hiện hành, quota, capability và khả năng vận hành. Các tiêu chí biến động phải được đo/đọc tài liệu lúc quyết định, không đóng băng thành nhận xét vĩnh viễn trong bài.\n\n### SDK hay REST trực tiếp\n\n| SDK chính thức | REST tự gọi |\n|---|---|\n| model types, auth và parsing tiện | kiểm soát HTTP/dependency rõ |\n| upgrade có thể đổi object | phải tự theo schema và lỗi |\n| tốt cho đa số ứng dụng | hợp môi trường SDK chưa hỗ trợ |\n\nSDK không phải magic; REST không tự “nhẹ và ổn định” nếu nhóm phải tự bảo trì auth, streaming parser và retry.",

loi_thuong_gap: [
  "Commit API key hoặc nhúng vào frontend; nhận biết key xuất hiện trong bundle, Git history hay network inspector.",
  "Hard-code model alias ở nhiều handler; nhận biết upgrade cần sửa hàng loạt và kết quả production không truy ra model thật.",
  "Giả định response luôn có text ở phần tử đầu; nhận biết lỗi khi tool/content block khác xuất hiện.",
  "Retry mọi exception, kể cả 400/401; nhận biết log lặp lỗi y hệt, quota/latency tăng mà không có cơ hội thành công.",
  "Không đặt timeout; nhận biết worker treo, queue dài và p99 tăng dù median bình thường.",
  "Coi HTTP 200 là nghiệp vụ thành công; nhận biết output incomplete, rỗng hoặc validator từ chối nhưng vẫn gửi khách.",
  "Fallback provider âm thầm; nhận biết behavior/chi phí đổi nhưng telemetry không ghi provider/model.",
  "Mock toàn bộ rồi tuyên bố integration pass; nhận biết không có live contract test cho credential, quyền model và schema response."
],

tu_kiem_tra: [
  "Vì sao timeout không chứng minh request chưa được provider xử lý?",
  "Thiết kế taxonomy cho 400, 401, 429, 5xx, network timeout và output validation failure; trường hợp nào retry?",
  "Adapter text-only gặp tool call nên trả text rỗng, ném lỗi hay mở rộng result? Nêu contract bạn chọn.",
  "Vì sao cùng prompt qua ba provider không tạo một thí nghiệm so sánh công bằng nếu model/config khác?",
  "Mây Shop cần log gì để điều tra mà không lưu PII thô?",
  "Ca khó: fallback giúp availability nhưng làm hỏng structured contract. Bạn đặt circuit breaker và validation ở đâu?",
  "Khi nào gọi SDK trực tiếp tốt hơn xây interface chung?"
],

tai_nguyen: [
  "Official OpenAI documentation — Responses API và API reference.",
  "Anthropic documentation — Messages API và Client SDKs.",
  "Google AI for Developers — Gemini API, Interactions và SDK documentation.",
  "Release It! — Michael T. Nygard, cho timeout, circuit breaker và failure production."
],

lien_he: [
  "Nối sang Backend: adapter, dependency injection, timeout, queue và observability biến SDK call thành service đáng vận hành.",
  "Nối sang Security: secret management, tenant authorization, data minimization và audit phải bao quanh model call.",
  "Nối sang Distributed Systems: timeout, retry, backoff và idempotency xuất hiện vì mạng không cho biết chắc trạng thái phía bên kia.",
  "Nối sang Testing: unit fake, contract test thật và eval chất lượng trả lời ba câu hỏi khác nhau, không thể thay thế nhau.",
  "Nối sang FinOps: usage theo request/tenant cho biết context dư, retry và output bị bỏ đang đốt ngân sách ở đâu."
]

};
