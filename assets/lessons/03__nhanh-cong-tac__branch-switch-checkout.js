window.LESSONS["03:nhanh-cong-tac:branch-switch-checkout"] = {

tom_tat: [
  "Nhánh Git là một tên có thể di chuyển, trỏ tới một commit; tạo nhánh không sao chép thư mục dự án. Mỗi commit mới làm con trỏ của nhánh hiện hành tiến lên, còn các nhánh khác đứng yên.",
  "`git branch` quản lý tên nhánh, `git switch` chuyển ngữ cảnh làm việc, còn `git checkout` là lệnh cũ kiêm nhiều nhiệm vụ. Với công việc mới, ưu tiên `switch` vì ý định rõ và ít gõ nhầm đường dẫn file.",
  "Tạo rồi chuyển bằng `git switch -c feature/export-csv`; quay lại nhánh đã đứng trước đó bằng `git switch -`. Trước khi chuyển, đọc `git status` vì thay đổi chưa commit có thể đi theo hoặc chặn thao tác.",
  "Xoá nhánh bằng `git branch -d` chỉ xoá tên tham chiếu và có kiểm tra đã hợp nhất; `-D` bỏ hàng rào này. Commit thường vẫn cứu được bằng `git reflog`, nhưng không nên biến reflog thành quy trình làm việc.",
  "Detached HEAD là trạng thái HEAD trỏ thẳng vào commit thay vì qua tên nhánh. Nó hữu ích để quan sát lịch sử, nhưng commit tạo tại đó cần được gắn nhánh nếu muốn giữ lâu dài."
],

can_biet_truoc: [
  "Biết `commit` là snapshot và mỗi commit lưu hash của commit cha; nhờ quan hệ cha, Git biểu diễn lịch sử thành đồ thị có hướng.",
  "Phân biệt working tree, staging area và repository; đổi nhánh có thể cập nhật cả working tree lẫn index chứ không chỉ đổi một biến tên nhánh.",
  "Hiểu `HEAD` là tham chiếu cho biết phiên làm việc đang đứng ở đâu, và hash commit là định danh nội dung.",
  "Biết dùng `git status`, `git add`, `git commit` và đọc `git log --oneline --graph --decorate --all`."
],

dinh_nghia: "**Nhánh (branch)** trong Git là một tham chiếu có tên, thường nằm dưới `refs/heads/`, chứa hash của commit ở đầu một dòng phát triển. `HEAD` thường trỏ gián tiếp tới nhánh đang được checkout; khi commit, Git tạo commit con rồi cập nhật nhánh đó sang hash mới.\n\n```text\nrefs/heads/main  ──► C2\nHEAD             ──► refs/heads/feature/export-csv ──► C4\n\nC0 ── C1 ── C2\n             \\\n              C3 ── C4\n```\n\n`git branch` tạo, liệt kê, đổi tên hoặc xoá tham chiếu. `git switch` chọn nhánh/commit để làm việc và cập nhật index cùng working tree cho phù hợp. `git checkout` là giao diện lịch sử làm được cả chuyển nhánh lẫn khôi phục file; nó vẫn hợp lệ, nhưng hai ý nghĩa quá khác nhau nên Git hiện đại có `switch` và `restore` để tách trách nhiệm.",

vi_sao_can: "Nếu An đang thêm xuất CSV mà phải sửa trực tiếp trên `main`, nhánh chính sẽ chứa code dở, thử nghiệm hỏng hoặc commit khó review. Sao chép cả thư mục sang `so-truc-hoc-ban-moi/` cũng không giải quyết tốt: hai bản sao không biết chung tổ tiên, khó thấy chính xác thay đổi, và rất dễ sửa nhầm bản.\n\nNhánh giải quyết vấn đề bằng cách cho hai chuỗi commit cùng chia sẻ lịch sử. `main` vẫn ở bản ổn định; `feature/export-csv` tiến theo thử nghiệm của An. Chi phí tạo nhánh gần như chỉ là ghi một hash và tên, nên ta có thể cô lập một ý định nhỏ thay vì chờ tới dự án lớn.\n\nNhưng nhánh không phải lớp bảo vệ tuyệt đối. Working tree là tài nguyên dùng chung trong một checkout; file chưa commit không “thuộc” nhánh nào. Chuyển nhánh khi đang sửa dở có thể mang thay đổi sang nhánh khác hoặc bị Git từ chối. Hiểu cơ chế này quan trọng hơn học thuộc lệnh, vì phần lớn sự cố “Git làm mất code” thực ra là người dùng nhầm giữa commit thuộc nhánh và thay đổi chưa commit còn nằm ngoài lịch sử.",

co_che: "### Ba con trỏ cần nhìn cùng lúc\n\nGiả sử repository Sổ trực học có `C0─C1─C2`, và `main` trỏ C2. Chạy `git switch -c feature/export-csv` thực hiện hai việc: tạo `refs/heads/feature/export-csv` với cùng hash C2, rồi làm `HEAD` trỏ tới tham chiếu mới. Không có file dự án nào bị nhân đôi vì hai tên dùng chung toàn bộ object C0, C1, C2.\n\nSau khi An commit C3, Git không “gắn nhãn feature” vào C3. Nó tạo object commit có cha là C2 rồi ghi hash C3 vào file tham chiếu của nhánh hiện hành. `main` vẫn giữ C2. Tên nhánh vì thế là con trỏ di động, không phải hộp chứa commit.\n\n### `git branch`: thao tác trên tên, chưa chuyển chỗ đứng\n\n```powershell\ngit branch                         # liệt kê nhánh local; dấu * là nhánh hiện hành\ngit branch feature/export-csv      # tạo tên tại HEAD, nhưng vẫn đứng ở nhánh cũ\ngit branch --show-current          # in đúng tên nhánh hiện hành\ngit branch -m feature/csv-export   # đổi tên nhánh đang đứng\ngit branch -vv                     # thêm commit đầu và upstream nếu có\n```\n\nBẫy cơ bản là chạy `git branch feature/export-csv` rồi tưởng mình đã chuyển. Một commit tiếp theo vẫn đẩy `main` tiến lên nếu dấu `*` còn ở `main`. Hãy kiểm tra bằng `git status -sb` hoặc `git branch --show-current`, đừng suy từ tên thư mục hay prompt cũ.\n\n### `git switch`: chuyển nhánh với ý định rõ\n\n```powershell\ngit switch feature/export-csv     # chuyển tới nhánh đã tồn tại\ngit switch -c feature/timezone    # tạo từ HEAD và chuyển ngay\ngit switch -c hotfix/C2 C2        # tạo tại commit/điểm bắt đầu chỉ định\ngit switch -                      # quay lại nhánh hoặc vị trí trước\n```\n\nKhi chuyển, Git tính khác biệt giữa commit hiện tại và commit đích, sau đó viết phiên bản phù hợp vào index và working tree. Nó không xoá mù quáng: nếu một file chưa commit sẽ bị ghi đè bởi nội dung ở đích, Git dừng với thông báo “Your local changes … would be overwritten”. Hàng rào này bảo vệ dữ liệu, nhưng không thay thế việc đọc `git status`.\n\n### Thay đổi chưa commit có thuộc nhánh không?\n\nKhông. Giả sử An sửa `README.md` trên `main`, chưa commit, rồi chuyển sang feature mà hai nhánh có cùng phiên bản `README.md`. Git có thể giữ nguyên sửa đổi, nên thay đổi “đi theo”. Điều này tiện để nhận ra vừa làm nhầm nhánh: chuyển đúng nhánh rồi commit. Nhưng nếu hai nhánh khác nhau ở file đó, Git thường chặn để tránh ghi đè.\n\nBa lựa chọn an toàn là: commit một snapshot hợp lý; dùng `git stash` khi thực sự cần cất tạm; hoặc hoàn tác thay đổi không cần bằng `git restore`. Không nên dùng `switch --discard-changes` như phản xạ vì nó chủ động bỏ nội dung working tree và index.\n\n### `checkout`: vì sao một lệnh dễ gây nhầm\n\n`git checkout feature/export-csv` chuyển nhánh, nhưng `git checkout -- src/shift.js` khôi phục file từ index. Chỉ khác đối số và dấu `--`, tác động đã chuyển từ “đổi ngữ cảnh” sang “ghi đè file”. Vì vậy bài này dùng `switch` cho nhánh và dành `restore` cho file.\n\n`checkout` vẫn cần biết để đọc tài liệu cũ, script lâu năm và để checkout một commit ở detached HEAD. Không nên máy móc thay mọi chuỗi `checkout` bằng `switch`: `switch` không nhận đường dẫn file, và đó chính là thiết kế nhằm giảm nhập nhằng.\n\n### Detached HEAD: đứng trên commit không qua tên nhánh\n\n```powershell\ngit switch --detach HEAD~1\ngit status -sb\ngit log -1 --oneline --decorate\n```\n\nHEAD lúc này chứa hash commit trực tiếp. Ta có thể chạy test, đọc code cũ, thậm chí commit. Tuy nhiên không có nhánh nào tự tiến theo commit mới. Khi rời đi, commit vẫn là object nhưng không còn tên dễ tìm và về lâu dài có thể bị garbage collection nếu hết đường tham chiếu.\n\nMuốn giữ thử nghiệm vừa commit, tạo nhánh trước khi rời: `git switch -c experiment/old-parser`. Nếu đã rời, tìm hash qua `git reflog`, rồi `git branch rescue/<ten> <hash>`. Detached HEAD không phải lỗi; nó là vị trí có vòng đời cần quản lý.\n\n### Tạo nhánh từ đúng điểm bắt đầu\n\n`git switch -c feature/export-csv` mặc định dùng HEAD. Nếu đang đứng nhầm nhánh, feature sẽ mang theo các commit không liên quan. Trước khi tạo, xem `git log -1 --decorate` và chọn điểm rõ ràng, chẳng hạn `git switch -c feature/export-csv main`.\n\nKhi có remote, `main` local có thể cũ hơn `origin/main`; tạo từ `main` không đồng nghĩa tạo từ trạng thái mới nhất trên server. Bài remote sẽ chỉ cách `git fetch` rồi tạo từ `origin/main`. Không nên `pull` vô thức chỉ để “cho mới”, vì pull còn tích hợp lịch sử vào nhánh hiện hành.\n\n### Đổi tên và xoá: tên mất không đồng nghĩa commit mất ngay\n\n```powershell\ngit branch -m feature/export-csv feature/csv-export\ngit branch -d feature/csv-export\ngit branch -D experiment/throw-away\n```\n\n`-d` từ chối nếu nhánh chưa được hợp nhất vào nhánh hiện hành, giúp tránh bỏ tên duy nhất dẫn tới commit. `-D` cưỡng bức xoá tên bất kể trạng thái. Cả hai không lập tức xoá object commit; reflog và object database thường còn dữ liệu một thời gian. Tuy vậy thời gian giữ phụ thuộc cấu hình và bảo trì, nên cứu hộ chỉ là lưới cuối.\n\nTrước khi xoá, dùng `git log main..feature/csv-export --oneline`: còn dòng nào nghĩa là feature có commit chưa nằm trong `main`. Sau merge, xoá nhánh local là vệ sinh tên; lịch sử đã hợp nhất vẫn còn nguyên.\n\n### Tên nhánh tốt phục vụ cộng tác\n\nTên như `feature/export-csv` nói mục đích; tên `an-new-final-2` nói người và trạng thái cảm tính. Dấu `/` chỉ tạo namespace trong tên hiển thị, không làm nhánh lồng nhau. Tránh tên dễ lẫn hash hoặc cú pháp revision, và đừng nhét ngày tháng nếu hệ thống issue đã theo dõi thời gian.\n\nMột nhánh nên gom một thay đổi có thể review. Nhánh sống quá lâu sẽ lệch xa `main`, làm merge và rebase khó hơn. Ngược lại, tạo nhánh cho từng lỗi chính tả có thể tăng chi phí quy trình nếu nhóm không cần review. Nhánh là công cụ cô lập rủi ro, không phải nghi lễ bắt buộc.\n\n### Mô hình tinh thần để chẩn đoán\n\nKhi không chắc, trả lời lần lượt: HEAD trỏ tới tên hay hash? Tên nhánh đó chứa hash nào? Index và working tree đang khác HEAD ở đâu? Bốn lệnh đọc trạng thái sau không thay đổi dữ liệu:\n\n```powershell\ngit status -sb\ngit branch -vv\ngit log --oneline --graph --decorate --all -12\ngit diff\n```\n\nĐồ thị cho biết commit nằm đâu; `status` cho biết phần chưa thành commit. Tách hai lớp này ngăn suy luận sai rằng file chưa commit đã được “lưu trong feature”.",

vi_du: "### Phiên thực hành chạy được: dựng Sổ trực học\n\nĐoạn PowerShell sau tạo repository trong thư mục tạm, không đụng repository đang học. Git cần danh tính để commit, nên cấu hình được đặt local bằng `--local`, không thay cấu hình toàn máy.\n\n```powershell\n$lab = Join-Path ([IO.Path]::GetTempPath()) 'git-branch-lab'\nif (Test-Path -LiteralPath $lab) { Remove-Item -Recurse -Force -LiteralPath $lab }\nNew-Item -ItemType Directory -Path $lab | Out-Null\nSet-Location $lab\ngit init -b main\ngit config --local user.name 'Hoc Vien'\ngit config --local user.email 'hocvien@example.invalid'\nNew-Item -ItemType Directory src | Out-Null\nSet-Content src/shift.js 'export const shifts = [];'\nSet-Content README.md '# So truc hoc'\ngit add src/shift.js README.md\ngit commit -m 'Khoi tao so truc hoc'\n\ngit switch -c feature/export-csv\nAdd-Content src/shift.js \"`nexport function toCsv(items) { return items.join(','); }\"\ngit add src/shift.js\ngit commit -m 'Them ham xuat CSV'\ngit switch main\ngit log --oneline --graph --decorate --all\n```\n\nKết quả phải có hai đầu nhánh: `main` ở commit khởi tạo và `feature/export-csv` ở commit con. Hash thay đổi theo nội dung và metadata nên không chép một hash giả vào bài.\n\n### Kiểm chứng nhánh chỉ là tham chiếu\n\n```powershell\ngit rev-parse main\ngit rev-parse feature/export-csv\nGet-Content .git/HEAD\nGet-Content .git/refs/heads/main\nGet-Content .git/refs/heads/feature/export-csv\n```\n\n`.git/HEAD` sẽ ghi `ref: refs/heads/main` sau khi đã switch về main. Hai file dưới `refs/heads` chứa hai hash. Trên repository đã chạy `git pack-refs`, một số tham chiếu có thể nằm trong `.git/packed-refs`, nên không nên viết phần mềm sản phẩm bằng cách tự đọc file; dùng `git rev-parse` là giao diện ổn định hơn.\n\n### Thử hàng rào bảo vệ thay đổi chưa commit\n\n```powershell\ngit switch feature/export-csv\nSet-Content src/shift.js '// sua do tren feature'\ngit switch main\ngit status -sb\ngit restore src/shift.js\ngit switch main\n```\n\nVì `src/shift.js` khác nhau giữa hai đầu nhánh và working tree còn sửa, Git sẽ từ chối lần switch đầu để không ghi đè. Sau `restore`, chuyển được. Đây là ca biên quan trọng: Git không luôn cho thay đổi đi theo; quyết định phụ thuộc liệu checkout đích có cần ghi đúng file đó hay không.\n\n### Thử detached HEAD và cứu commit\n\n```powershell\ngit switch --detach feature/export-csv\nAdd-Content README.md \"`nThu nghiem tai commit cu\"\ngit add README.md\ngit commit -m 'Thu nghiem tai detached HEAD'\n$orphan = git rev-parse HEAD\ngit switch main\ngit branch rescue/detached $orphan\ngit log --oneline --graph --decorate --all\n```\n\nNhánh `rescue/detached` làm commit thử nghiệm có tên lại. Trong shell thật, không nhất thiết lưu `$orphan` nếu còn reflog, nhưng lưu ngay minh hoạ cách biến một trạng thái mong manh thành tham chiếu rõ ràng.\n\n### Ca biên cần tự thử\n\nRepository mới chưa có commit tạo ra “unborn branch”: HEAD đã nói `main` nhưng `refs/heads/main` chưa tồn tại. Nhánh chỉ có commit để trỏ sau commit đầu. Tên nhánh trùng tên tag cũng có thể gây nhập nhằng; dùng `refs/heads/<ten>` hoặc `git switch <ten>` khi ý định là nhánh. Cuối cùng, filesystem không phân biệt hoa thường có thể khiến `Feature/X` và `feature/x` gây rắc rối giữa máy; quy ước tên chữ thường tránh lỗi cộng tác đa nền tảng.",

so_sanh: "### Chọn lệnh theo ý định\n\n| Ý định | Lệnh nên dùng | Lệnh cũ/tương đương | Vì sao |\n|---|---|---|---|\n| Liệt kê hoặc tạo tên nhánh | `git branch [ten]` | — | Không đổi working tree |\n| Tạo và chuyển | `git switch -c ten [start]` | `git checkout -b ten [start]` | `switch` nói rõ đang đổi nhánh |\n| Chuyển nhánh có sẵn | `git switch ten` | `git checkout ten` | Không nhập nhằng với đường dẫn |\n| Khôi phục file | `git restore file` | `git checkout -- file` | Tách thao tác có nguy cơ ghi đè file |\n| Xem commit cũ | `git switch --detach hash` | `git checkout hash` | Trạng thái detached được viết tường minh |\n\n### Nhánh Git so với sao chép thư mục\n\n| Tiêu chí | Nhánh Git | Bản sao thư mục |\n|---|---|---|\n| Chia sẻ lịch sử/object | Có | Không tự biết quan hệ |\n| So sánh thay đổi | `git diff main...feature` | Công cụ ngoài, dễ lẫn file rác |\n| Hợp nhất | Git tìm merge base | Chép tay và đoán phiên bản |\n| File chưa commit | Dùng chung trong checkout | Cách ly vật lý |\n| Khi không nên dùng | Cần chạy đồng thời hai bản working tree | Có thể dùng `git worktree` thay vì copy |\n\nNếu cần mở `main` để chạy server trong khi vẫn sửa feature, chuyển qua lại một working tree là bất tiện. `git worktree` tạo working tree thứ hai có quản lý, dùng chung object database nhưng giữ index và file làm việc riêng. Đó là lựa chọn tốt hơn sao chép thư mục, song không cần cho phiên làm việc tuần tự đơn giản.",

loi_thuong_gap: [
  "Chạy `git branch ten` rồi commit với niềm tin đã sang nhánh mới. Dấu hiệu: `git branch --show-current` vẫn là `main`; sửa bằng cách tạo/chuyển đúng nhánh trước commit tiếp theo.",
  "Gọi thay đổi chưa commit là “code trên nhánh”. Dấu hiệu: switch sang nhánh khác mà `git status` vẫn hiện file; hãy commit, stash hoặc restore có chủ đích.",
  "Dùng `git checkout -- file` theo trí nhớ và vô tình mất sửa đổi. Dấu hiệu: working tree sạch nhưng nội dung quay về index; ưu tiên `restore` và xem `git diff` trước thao tác ghi đè.",
  "Commit trong detached HEAD rồi rời đi. Dấu hiệu: `git status -sb` ghi `HEAD (no branch)`; tạo nhánh tại commit hoặc cứu hash bằng reflog.",
  "Tạo feature từ nhánh sai. Dấu hiệu: `git log main..feature` chứa commit không liên quan; kiểm điểm bắt đầu trước khi tạo, và dùng đối số `main`/`origin/main` rõ ràng.",
  "Dùng `git branch -D` để dọn nhanh mà chưa xem commit riêng. Dấu hiệu: `git log main..nhanh` còn kết quả; chỉ cưỡng bức khi chắc chắn nội dung có thể bỏ.",
  "Giữ nhánh quá lâu và gom nhiều mục tiêu. Dấu hiệu: diff lớn, merge base quá cũ, review không thể tách ý định; chia nhánh theo thay đổi có thể kiểm chứng."
],

tu_kiem_tra: [
  "Sau `git branch feature/x`, HEAD và hai tham chiếu trỏ thế nào? Vì sao commit kế tiếp chưa chắc thuộc feature?",
  "Giải thích bằng index và working tree vì sao Git đôi khi cho thay đổi chưa commit đi theo khi switch, đôi khi từ chối.",
  "Vẽ đồ thị trước và sau `git switch -c feature/x`, rồi sau một commit. Object nào mới được tạo ở mỗi bước?",
  "Khi nào detached HEAD là hữu ích? Nêu quy trình giữ lại một commit tạo trong trạng thái này mà không dựa vào may mắn.",
  "So sánh `branch -d` với `branch -D`; hàng rào “đã merge” được đánh giá tương đối với nhánh nào?",
  "Câu khó: vì sao hai nhánh không phải hai “tập commit” tách biệt, và một commit merge có thể thuộc lịch sử truy cập được từ cả hai tên?",
  "Nếu cần chạy hai phiên bản đồng thời, vì sao `git worktree` phù hợp hơn switch liên tục hoặc sao chép thư mục?"
],

tai_nguyen: [
  "Sách “Pro Git”, các chương Git Branching và Git Internals.",
  "Tài liệu tham khảo chính thức của Git cho `git-branch`, `git-switch`, `git-checkout` và `gitrevisions`.",
  "Khoá học “Version Control with Git” của Atlassian.",
  "Sách “Git in Practice” của Mike McQuaid."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: lịch sử commit là đồ thị có hướng không chu trình, còn tên nhánh là tham chiếu tới một nút; cách nhìn này chuẩn bị cho merge base.",
  "Nối sang mảng Hệ điều hành: working tree, index và object database là ba lớp trạng thái trên filesystem; thao tác an toàn phụ thuộc việc phân biệt dữ liệu chưa bền vững với snapshot đã ghi.",
  "Nối sang mảng Clean Code: một nhánh nhỏ, một mục tiêu làm diff dễ review giống nguyên tắc hàm nhỏ làm đúng một việc.",
  "Nối sang mảng CI/CD: pipeline thường kích hoạt theo tên nhánh hoặc pull request, nên quy ước nhánh trở thành một phần giao diện vận hành của nhóm."
]

};
