window.LESSONS["03:thao-tac-hang-ngay:git-init-va-git-clone"] = {

tom_tat: [
  "`git init` biến một thư mục hiện có thành repository bằng cách tạo kho dữ liệu `.git`; nó không tự thêm file, không tạo commit và không gửi gì lên GitHub.",
  "`git clone` tạo một thư mục làm việc mới từ repository đã tồn tại, đồng thời chép lịch sử, thiết lập remote `origin` và checkout một nhánh ban đầu.",
  "Chọn `init` khi dự án đang nằm trên máy nhưng chưa được Git quản lý; chọn `clone` khi nguồn sự thật đã ở một repository khác mà bạn cần tham gia.",
  "Cả hai lệnh đều cần kiểm tra thư mục đích, nhánh hiện tại, remote và trạng thái file ngay sau khi chạy; một lệnh exit code 0 chưa chứng minh bạn đang đứng đúng repository.",
  "Ví dụ xuyên suốt là CLI Node.js “Sổ việc”; các bài sau sẽ dùng chính repository này để thiết kế `.gitignore`, đọc `status`, chọn từng đoạn bằng `git add -p`, rồi học commit, log, diff, show và blame."
],

can_biet_truoc: [
  "Biết thư mục làm việc hiện tại là gì và dùng được `pwd` trên Bash hoặc `Get-Location` trên PowerShell; Git luôn diễn giải đường dẫn tương đối từ vị trí này.",
  "Phân biệt file hệ thống với thư mục: `.git` là thư mục metadata, còn `.gitignore` là file quy tắc sẽ học ở bài kế tiếp.",
  "Biết khái niệm repository, commit, branch và remote ở mức nhập môn: repository giữ lịch sử; commit là một ảnh chụp; branch là tên di chuyển theo commit; remote là tên cấu hình trỏ đến repository khác.",
  "Đã cài Git và kiểm tra được bằng `git --version`; không cần tài khoản GitHub để chạy `git init` hoặc clone từ repository công khai qua HTTPS."
],

dinh_nghia: "`git init [đường-dẫn]` khởi tạo kho Git cục bộ: Git tạo cấu trúc `.git` chứa cơ sở dữ liệu object, refs, cấu hình và vùng staging, nhưng để nguyên nội dung dự án. `git clone <nguồn> [thư-mục]` là thao tác ghép: tạo thư mục đích, khởi tạo repository, lấy refs và object từ nguồn, cấu hình remote mặc định tên `origin`, rồi checkout một commit thành cây file làm việc.\n\nHai lệnh trả lời hai điểm xuất phát khác nhau. Với dự án “Sổ việc” vừa tự tạo trên máy, điểm xuất phát là thư mục chưa có lịch sử nên dùng `init`. Với đồng đội mới tham gia sau khi repository đã được chia sẻ, điểm xuất phát là lịch sử có sẵn nên dùng `clone`. GitHub chỉ là một nơi có thể lưu repository từ xa; `git init` và `git clone` là lệnh Git, không phải lệnh riêng của GitHub.",

vi_sao_can: "Một dự án thường bắt đầu theo một trong hai hướng: bạn đã có mã nguồn rời rạc và muốn bắt đầu ghi lịch sử, hoặc lịch sử đã tồn tại ở máy chủ/ổ đĩa khác và bạn muốn có bản làm việc đầy đủ. Nhầm hai hướng gây hậu quả khó hiểu. Chạy `git init` trong bản ZIP tải từ GitHub tạo lịch sử mới không liên quan lịch sử thật. Ngược lại, clone vào nơi đã chứa file có thể bị Git từ chối để tránh ghi đè.\n\nKhởi tạo đúng còn tạo nền cho mọi lệnh sau. `status` phải biết đâu là gốc repository; `add` ghi lựa chọn vào index thuộc `.git`; `commit` đưa object vào kho; `log` đi theo refs; `push` cần remote. Nếu `.git` nằm nhầm một cấp thư mục, Git vẫn hoạt động nhưng phạm vi theo dõi sai: có thể gom cả file cá nhân ở thư mục cha hoặc bỏ sót file con bạn tưởng đã quản lý.\n\nTrong “Sổ việc”, ta cần giữ `src/tasks.js`, `test/tasks.test.js` và `package.json`, nhưng không muốn lịch sử đầu tiên bị lẫn `node_modules`, `.env` hay dữ liệu chạy thử. Bởi vậy `init` chỉ mở kho; quyết định file nào đi vào lịch sử được trì hoãn sang `.gitignore`, `status` và staging. Sự tách trách nhiệm này là chủ ý an toàn của Git.",

co_che: "### Trước hết xác định đúng điểm xuất phát\n\nHãy hỏi “repository có lịch sử đáng tin ở đâu chưa?”. Nếu chưa, thư mục `so-viec` trên máy là nguồn ban đầu và `git init` phù hợp. Nếu đã có repository trung tâm hoặc bản sao của đồng đội, hãy clone. Không dùng tuổi của file để quyết định: một thư mục lâu năm vẫn có thể chưa từng dùng Git; một thư mục vừa tải ZIP có mã mới nhưng không mang metadata lịch sử.\n\nKiểm tra trước khi khởi tạo:\n\n```sh\npwd\nls -la\ngit rev-parse --show-toplevel\n```\n\nLệnh cuối trả lỗi khi không ở trong repository. Nếu nó in một đường dẫn cha, thư mục hiện tại đã nằm bên trong một repository; chạy `git init` tiếp có thể chỉ “reinitialize” kho cha hoặc tạo repository lồng nhau, tùy vị trí. Đây là lúc dừng để hiểu cấu trúc chứ không gõ lệnh theo quán tính.\n\n### `git init` thực sự tạo những gì\n\nTrong thư mục dự án:\n\n```sh\nmkdir so-viec\ncd so-viec\ngit init\n```\n\nGit tạo `.git/`. Cây tối giản thường có `HEAD`, `config`, `objects/`, `refs/`, `hooks/` và `info/`. `HEAD` cho biết nhánh hoặc commit đang được checkout. `config` chứa cấu hình riêng của kho. `objects` sẽ chứa nội dung bất biến sau khi có object. `refs` giữ các tên như branch và tag. Các hook mẫu không tự chạy chỉ vì tồn tại.\n\nĐiều Git cố ý không làm cũng quan trọng: nó không đọc toàn bộ file thành lịch sử, không tự chạy `git add`, không có commit đầu tiên, không tạo repository GitHub, không đặt remote và không tải thư viện Node.js. Vì thế ngay sau `init`, `git status` có thể báo “No commits yet” cùng một danh sách untracked; đó là trạng thái đúng.\n\n### HEAD chưa sinh ra commit đầu tiên\n\nSau `init`, `HEAD` thường trỏ tượng trưng đến một nhánh chưa ra đời. Có tên nhánh không đồng nghĩa có commit. `git log` lúc này báo chưa có commit; `git rev-parse HEAD` cũng thất bại. Commit đầu tiên đặc biệt vì không có parent. Khi commit đó được tạo, tên nhánh mới trỏ đến object commit và các lệnh duyệt lịch sử mới có điểm bắt đầu.\n\nMuốn chủ động tên nhánh ban đầu, dùng:\n\n```sh\ngit init --initial-branch=main\n```\n\nHoặc cấu hình mặc định trên máy cho các kho tương lai. Không nên ghi cứng giả định rằng mọi Git đều dùng cùng một tên nhánh; repository clone sẽ theo nhánh mặc định mà nguồn quảng bá, còn repository cũ có thể dùng tên khác. Luôn xác nhận bằng `git branch --show-current`.\n\n### Tìm gốc repository thay vì đoán bằng mắt\n\nGit đi từ thư mục hiện tại lên các thư mục cha để tìm `.git`. Do đó bạn có thể chạy lệnh tại `so-viec/src/`, nhưng repository root vẫn là `so-viec/`. Lệnh đáng tin là:\n\n```sh\ngit rev-parse --show-toplevel\ngit rev-parse --git-dir\n```\n\nLệnh đầu cho gốc cây làm việc; lệnh sau cho nơi metadata. Phần lớn repository dùng `.git` ngay trong gốc, nhưng worktree và submodule có thể dùng một file `.git` trỏ sang nơi khác. Vì vậy script chuyên nghiệp không nên khăng khăng kiểm tra `.git` là thư mục; hãy hỏi chính Git.\n\nCa biên nguy hiểm là khởi tạo ở thư mục cha, ví dụ `~/projects` thay vì `~/projects/so-viec`. Khi ấy `status` thấy nhiều dự án anh em. Dấu hiệu nhận biết là `--show-toplevel` in đường dẫn rộng hơn dự kiến. Trước commit đầu tiên, sửa khá dễ: xác định chính xác `.git` vừa tạo và xóa nó có chủ đích. Sau khi đã có lịch sử, không nên xóa vội vì `.git` chứa toàn bộ commit chưa được sao lưu.\n\n### `git clone` gồm nhiều pha chứ không chỉ tải file\n\nLệnh điển hình:\n\n```sh\ngit clone https://example.invalid/nhom/so-viec.git\ncd so-viec\n```\n\nURL trên chỉ minh họa cú pháp, không phải tài nguyên để truy cập. Về cơ chế, Git suy ra tên thư mục, tạo kho cục bộ, liên lạc với nguồn để biết refs, chuyển các object cần thiết, tạo remote-tracking refs như `origin/main`, chọn nhánh ban đầu, rồi checkout snapshot vào cây làm việc. Chính vì có object và refs, bản clone có thể xem log, diff hoặc tạo commit khi mất mạng; chỉ thao tác trao đổi với remote mới cần kết nối.\n\nClone từ đường dẫn cục bộ cũng hợp lệ và rất hữu ích để học không phụ thuộc mạng:\n\n```sh\ngit clone ../so-viec-nguon so-viec-ban-sao\n```\n\nNguồn có thể là repository thường hoặc bare repository. Clone không phải sao chép mù toàn bộ thư mục: file bị ignore, file untracked và secret chỉ nằm trong cây làm việc của nguồn sẽ không xuất hiện, vì chúng chưa thuộc commit.\n\n### `origin` là tên quy ước, không phải đám mây\n\nSau clone, kiểm tra:\n\n```sh\ngit remote -v\ngit branch --show-current\ngit branch --remotes\n```\n\n`origin` chỉ là nhãn cấu hình mà clone tạo mặc định cho URL nguồn. Nó có thể trỏ tới GitHub, GitLab, máy chủ nội bộ hoặc đường dẫn trên cùng ổ đĩa. Đổi tên hoặc có nhiều remote hoàn toàn được. `origin/main` là remote-tracking ref cục bộ ghi nhận trạng thái `main` của remote ở lần fetch gần nhất; nó không phải một kết nối sống tự cập nhật từng giây.\n\nNhánh cục bộ `main` thường được thiết lập để theo dõi `origin/main`. Quan hệ upstream giúp `git pull` và `git push` suy ra đích, nhưng không trao quyền ghi. Xác thực HTTPS/SSH và quyền trên máy chủ là lớp riêng.\n\n### Clone đầy đủ, clone nông và clone một nhánh\n\nCác lựa chọn giảm dữ liệu có mục đích cụ thể:\n\n```sh\ngit clone --depth=1 <nguồn> so-viec-ci\ngit clone --single-branch --branch main <nguồn> so-viec-main\n```\n\n`--depth=1` tạo shallow clone chỉ có phần lịch sử gần đầu nhánh. Nó nhanh và nhẹ cho job CI chỉ cần build snapshot hiện tại, nhưng `log`, `blame`, phép tìm merge-base và thao tác tích hợp lịch sử có thể thiếu dữ kiện. Có thể fetch sâu thêm về sau, song đó là chi phí bị trì hoãn. `--single-branch` giới hạn refs được lấy; nó không phải hàng rào bảo mật vì server vẫn quyết định quyền truy cập.\n\nĐối với máy phát triển của “Sổ việc”, ưu tiên clone đầy đủ vì các bài `log`, `show`, `blame` cần lịch sử. Đừng tối ưu vài giây tải xuống rồi làm mất ngữ cảnh điều tra lỗi trong nhiều giờ.\n\n### Checkout có thể thất bại dù fetch đã thành công\n\nClone có hai kết quả cần phân biệt: dữ liệu Git đã về và cây làm việc đã được dựng. Tên file hợp lệ trên Linux có thể xung đột chữ hoa/chữ thường hoặc chứa ký tự bất tiện trên Windows. Khi checkout thất bại, repository có thể vẫn chứa object; xóa và clone lặp lại không giải quyết nguyên nhân.\n\nNgoài ra, Git LFS hoặc submodule là các lớp bổ sung. Clone superproject không nhất thiết lấy nội dung submodule nếu không dùng tùy chọn phù hợp; file con trỏ LFS không trở thành binary thật nếu môi trường thiếu LFS. Dấu hiệu nằm trong output và `status`, không nên kết luận “clone hỏng” chỉ bằng kích thước thư mục.\n\n### Repository lồng nhau và submodule không giống thư mục thường\n\nNếu chạy `git init` bên trong `so-viec/plugins/xuat-csv`, Git trong thư mục con sẽ dừng ở kho con, còn Git ở kho cha xem thư mục ấy như nội dung có ranh giới đặc biệt. Việc vô tình copy cả dự án có `.git` vào dự án khác thường dẫn đến cảnh báo embedded repository khi `add`.\n\nSubmodule là cơ chế chính thức để kho cha ghi một gitlink trỏ đến commit của kho con, kèm cấu hình nguồn trong `.gitmodules`. Nó phù hợp khi hai thành phần thực sự có vòng đời và lịch sử độc lập. Với vài file chung đơn giản, submodule tăng gánh nặng clone, checkout và cập nhật; không nên dùng chỉ để “sắp xếp thư mục đẹp”.\n\n### Xác minh sau lệnh bằng bốn câu hỏi\n\nSau `init` hoặc `clone`, đừng chỉ đọc dòng “done”. Hãy kiểm tra:\n\n```sh\ngit rev-parse --show-toplevel\ngit status --short --branch\ngit remote -v\ngit log -1 --oneline\n```\n\nVới kho vừa init, lệnh log thất bại là dự kiến vì chưa có commit, remote có thể rỗng, còn status liệt kê file chưa theo dõi. Với clone, ta mong có commit đầu nhánh, remote đúng nguồn và cây làm việc sạch. Bốn kiểm tra lần lượt trả lời: phạm vi ở đâu, cây làm việc ra sao, sẽ trao đổi với ai, và lịch sử hiện đứng tại đâu.\n\n### Khi nào không nên dùng lệnh nào\n\nKhông `init` một bản ZIP nếu mục tiêu là đóng góp ngược vào dự án gốc; hãy clone để giữ lịch sử và remote. Không clone chỉ để lấy một file tài liệu; tải file thích hợp hơn nếu không cần lịch sử. Không clone lồng vào thư mục đang có dữ liệu quý rồi tìm cách ép ghi đè. Không dùng shallow clone trên máy cần điều tra lịch sử lâu dài. Và không chạy lại `init` như một “cách sửa Git” khi chưa chẩn đoán: reinitialize thường không phục hồi object bị mất, không sửa remote sai và không làm sạch index.",

vi_du: "### Phòng thí nghiệm hoàn toàn cục bộ cho “Sổ việc”\n\nCác lệnh sau chạy trên Bash; PowerShell có thể tạo file tương đương bằng `New-Item` và `Set-Content`. Ta tạo dự án, khởi tạo, làm commit hạt giống rồi clone sang thư mục anh em để quan sát:\n\n```sh\nmkdir git-lab && cd git-lab\nmkdir so-viec-nguon && cd so-viec-nguon\ngit init --initial-branch=main\nmkdir src test data\nprintf '{\"name\":\"so-viec\",\"scripts\":{\"test\":\"node --test\"}}\\n' > package.json\nprintf 'export const tasks = [];\\n' > src/tasks.js\nprintf 'SO_VIEC_TOKEN=demo-only\\n' > .env\n\ngit status --short\n# ?? .env\n# ?? package.json\n# ?? src/\n\nprintf '.env\\nnode_modules/\\ndata/*.json\\n' > .gitignore\ngit add .gitignore package.json src/tasks.js\ngit -c user.name=\"Hoc Git\" -c user.email=\"hoc@example.invalid\" commit -m \"Khởi tạo CLI Sổ việc\"\ncd ..\ngit clone so-viec-nguon so-viec-ban-sao\ncd so-viec-ban-sao\ngit status --short --branch\ngit remote -v\ngit log -1 --oneline\n```\n\nKết quả hash commit thay đổi theo metadata nên không ghi cứng. Các bất biến cần quan sát là bản sao ở nhánh `main`, status sạch, `origin` trỏ đường dẫn `so-viec-nguon`, log có commit “Khởi tạo CLI Sổ việc”, và `.env` không xuất hiện trong bản sao vì chưa từng đi vào commit. Đây là bằng chứng clone chuyển lịch sử được theo dõi, không sao chép mọi byte trong thư mục nguồn.\n\n### Thử ca biên có chủ ý\n\n```sh\ncd ..\nmkdir thu-muc-khong-rong\nprintf 'đừng ghi đè tôi\\n' > thu-muc-khong-rong/ghi-chu.txt\ngit clone so-viec-nguon thu-muc-khong-rong\n# fatal: destination path ... already exists and is not an empty directory.\n\ncd so-viec-ban-sao/src\ngit rev-parse --show-toplevel\ngit rev-parse --git-dir\n```\n\nClone từ chối thư mục đích không rỗng, nhờ đó file `ghi-chu.txt` còn nguyên. Khi đứng sâu trong `src`, Git vẫn tìm ra gốc của `so-viec-ban-sao`. Nếu thử `git log` trong một kho mới init chưa commit, hãy coi lỗi đó là mô tả trạng thái rỗng, không phải lý do để init lần nữa.",

so_sanh: "### Bảng quyết định theo nguồn sự thật\n\n| Tình huống | Lựa chọn | Vì sao | Kiểm tra ngay sau đó |\n|---|---|---|---|\n| Mã chỉ có trên máy, chưa có lịch sử | `git init` | Giữ nguyên cây file và mở kho cục bộ | gốc kho, untracked, tên nhánh |\n| Repository đã tồn tại ở server hoặc máy khác | `git clone` | Lấy cả object, refs và cấu hình nguồn | remote, commit đầu nhánh, status |\n| Tải ZIP từ trang web để đọc nhanh | giữ ZIP, hoặc clone lại đúng nguồn | ZIP không mang `.git` | có cần `log`/đóng góp không |\n| Job CI chỉ build commit mới nhất | `clone --depth=1` có cân nhắc | giảm dữ liệu truyền | công cụ có cần lịch sử không |\n| Thêm dự án độc lập vào dự án cha | cân nhắc submodule | biểu diễn rõ hai lịch sử | quy trình clone/update của nhóm |\n\n### `clone` khác sao chép thư mục\n\n| Thuộc tính | Sao chép bằng Explorer/`cp` | `git clone` |\n|---|---|---|\n| File untracked và ignored | thường bị chép theo | không có vì không thuộc commit |\n| Lịch sử Git | chỉ có nếu vô tình chép cả `.git` | được dựng có chủ đích từ nguồn |\n| Remote `origin` | giữ cấu hình cũ nếu chép `.git`, dễ nhầm | cấu hình theo nguồn clone |\n| Tên thư mục đích | tự chọn | suy ra hoặc truyền đối số |\n| Khả năng tối ưu truyền object | không | có negotiation và packfile |\n\nSao chép cả `.git` đôi khi tạo được một bản repository hoạt động, nhưng nó mang theo cấu hình cục bộ và có thể cả hook hoặc thiết lập không mong muốn. Clone diễn đạt ý định rõ hơn, thiết lập nguồn mới nhất quán và không kéo file ngoài lịch sử.",

loi_thuong_gap: [
  "Chạy `git init` ở thư mục cha quá rộng; nhận biết bằng `git rev-parse --show-toplevel` trả về đường dẫn không phải gốc dự án, và `status` liệt kê nhiều thư mục anh em.",
  "Tưởng `init` đã lưu file; dấu hiệu là `git log` chưa có commit và toàn bộ mã hiện dưới `??` trong `git status --short`. Cần thiết kế ignore rồi stage/commit có chọn lọc.",
  "Tải ZIP rồi init để đóng góp vào repository gốc; dấu hiệu là không có lịch sử và `git remote -v` rỗng. Clone lại nguồn chuẩn thay vì tạo một dòng lịch sử không liên quan.",
  "Clone nhầm tài khoản hoặc URL rồi chỉ phát hiện khi push; nhận biết sớm bằng `git remote get-url origin` và kiểm tra cả fetch URL lẫn push URL.",
  "Dùng `--depth=1` trên máy phát triển nhưng sau đó cần blame hoặc merge với nhánh cũ; dấu hiệu là `git rev-parse --is-shallow-repository` trả `true`. Fetch sâu thêm hoặc clone đầy đủ.",
  "Vô tình đặt repository trong repository; Git cảnh báo embedded repository khi add, còn `rev-parse --show-toplevel` thay đổi khi bước qua ranh giới. Chỉ dùng submodule khi thật sự muốn hai vòng đời.",
  "Cho rằng clone đã lấy secret hay file dữ liệu chưa commit từ máy đồng đội; file đó không thuộc object nên không thể xuất hiện. Dùng kênh quản lý secret và dữ liệu riêng, không ép đưa chúng vào Git."
],

tu_kiem_tra: [
  "Một thư mục dự án có sẵn 200 file nhưng chưa có `.git`. Sau `git init`, byte nội dung nào đã được đưa vào object database, và bằng lệnh nào bạn kiểm chứng câu trả lời?",
  "Vì sao `origin/main` không phải nhánh đang chạy trên máy và cũng không tự đổi ngay khi người khác push? Hãy nêu thao tác làm nó cập nhật.",
  "Bạn đứng trong `so-viec/src/parsers/`. Làm sao xác định gốc cây làm việc mà không dựa vào số lần `cd ..`, và vì sao kiểm tra `.git` bằng mắt có thể sai với worktree?",
  "Clone báo checkout thất bại trên Windows nhưng fetch object đã xong. Vì sao chạy clone lại nguyên xi có thể không giúp gì, và bạn sẽ tách hai pha để chẩn đoán ra sao?",
  "Khi nào shallow clone là đánh đổi hợp lý? Liệt kê ít nhất hai thao tác lịch sử có thể cho kết quả thiếu hoặc không thực hiện được.",
  "Một đồng đội gửi thư mục đã copy gồm cả `.git`. So với clone từ remote chuẩn, bạn cần kiểm tra thêm những cấu hình và dữ liệu cục bộ nào trước khi tin bản sao đó?"
],

tai_nguyen: [
  "Sách “Pro Git” của Scott Chacon và Ben Straub, các chương Getting Started và Git Basics.",
  "Tài liệu tham khảo đi kèm Git: `git help init`, `git help clone`, `git help rev-parse` và `git help remote`.",
  "Sách “Version Control with Git” của Jon Loeliger và Matthew McCullough.",
  "Khóa học “Introduction to Git and GitHub” trên Coursera, dùng để luyện quy trình repository cục bộ và từ xa."
],

lien_he: [
  "Nối sang mảng Hệ điều hành: Git phải đi qua filesystem để tạo file, thư mục và quyền truy cập; khác biệt chữ hoa/thường hoặc ký tự tên file giải thích vì sao checkout có thể khác giữa Windows và Linux.",
  "Nối sang mảng Mạng máy tính: clone qua HTTPS hoặc SSH gồm phân giải tên, kết nối, xác thực và truyền packfile; lỗi mạng không đồng nghĩa object cục bộ đã mất.",
  "Nối sang mảng Bảo mật: remote URL, credential helper, SSH key và secret trong cây làm việc thuộc các ranh giới tin cậy khác nhau; Git không phải kho bí mật.",
  "Nối sang mảng Cấu trúc dữ liệu: object database định danh nội dung và refs là các con trỏ có tên; cách biểu diễn này chuẩn bị cho việc hiểu commit graph ở bài `git log`."
]

};
