window.LESSONS["04:thiet-ke-csdl:chuan-hoa-1nf-2nf-3nf"] = {

tom_tat: [
  "Chuẩn hoá là quá trình dùng phụ thuộc hàm để đặt mỗi sự thật vào đúng một nơi, nhờ đó tránh anomaly khi chèn, sửa và xóa dữ liệu.",
  "1NF yêu cầu cấu trúc hàng–cột có giá trị nguyên tử theo miền đang xét; 2NF loại phụ thuộc vào một phần khóa ghép; 3NF loại phụ thuộc bắc cầu từ khóa qua thuộc tính không khóa.",
  "Ví dụ bán vé bắt đầu từ một bảng hóa đơn phẳng chứa thông tin khách, show, venue, ghế và giá, rồi phân rã thành các bảng có ý nghĩa riêng.",
  "Phân rã tốt phải bảo toàn thông tin khi join lại và nên bảo toàn các phụ thuộc quan trọng để constraint vẫn cưỡng chế được cục bộ.",
  "Chuẩn hoá không tự động làm mọi truy vấn nhanh hơn; nó ưu tiên tính đúng và khả năng cập nhật, còn index và bài phá chuẩn hoá xử lý workload đọc cụ thể."
],

can_biet_truoc: [
  "Khóa chính, khóa ứng viên, superkey và khóa ghép.",
  "Quan hệ 1–1, 1–n, n–n cùng cách ánh xạ bằng khóa ngoại và bảng trung gian.",
  "Cú pháp PostgreSQL cho `CREATE TABLE`, `INSERT`, `UPDATE`, `JOIN` và constraint.",
  "Khái niệm tập hợp và phép chiếu ở mức trực giác: chọn một số cột từ một quan hệ."
],

dinh_nghia: "**Chuẩn hoá cơ sở dữ liệu** là quá trình phân tích các phụ thuộc giữa thuộc tính và phân rã một quan hệ thành các quan hệ nhỏ hơn nhằm giảm dư thừa gây bất nhất, đồng thời vẫn có thể khôi phục thông tin bằng phép join đúng.\n\nMột **phụ thuộc hàm** `X → Y` nghĩa là trong mọi trạng thái hợp lệ của dữ liệu, hai hàng bằng nhau trên tập thuộc tính X bắt buộc bằng nhau trên Y. 1NF, 2NF và 3NF là các mức điều kiện tăng dần: 1NF đặt nền cấu trúc quan hệ; 2NF loại phụ thuộc bộ phận vào khóa ứng viên; 3NF loại phụ thuộc bắc cầu của thuộc tính không khóa.",

vi_sao_can: "Hãy tưởng tượng một bảng `ticket_sale_flat` có một hàng cho mỗi ghế đã bán nhưng lặp `customer_email`, `customer_name`, `event_title`, `venue_name` và `starts_at`. Khách đổi tên thì phải sửa hàng trăm vé. Nếu một hàng bị sót, cùng `customer_id` có hai tên. Xóa chiếc vé cuối cùng của một show có thể làm mất luôn giờ diễn và địa điểm. Muốn tạo show trước khi bán vé lại không có chỗ lưu nếu khóa chính buộc phải chứa booking và seat.\n\nBa hiện tượng đó lần lượt là update anomaly, delete anomaly và insert anomaly. Chúng không phải lỗi cú pháp SQL mà là hậu quả của việc nhiều sự thật có vòng đời khác nhau bị ép vào cùng một hàng. Transaction chỉ bảo đảm nhóm câu lệnh cùng commit; nó không tự biết phải tìm tất cả bản sao của tên venue để cập nhật.\n\nChuẩn hoá làm giảm số nơi cần thay đổi và biến quy tắc thành constraint gần dữ liệu. Khi `venue_name` nằm ở `venue`, đổi tên là một `UPDATE` trên một hàng. Khi show tham chiếu venue bằng foreign key, không show nào trỏ tới địa điểm không tồn tại. Đổi lại, màn hình hóa đơn phải join nhiều bảng. Đó là đánh đổi có chủ đích: tính đúng của ghi dữ liệu trước, đường đọc được tối ưu bằng index, view, cache hoặc phá chuẩn hoá có kiểm soát sau khi đo.",

co_che: "### Bảng phẳng ban đầu và các sự thật bị trộn\n\nXét bảng nhập từ một hệ thống cũ:\n\n```sql\nCREATE TABLE ticket_sale_flat (\n  booking_id bigint NOT NULL,\n  customer_id bigint NOT NULL,\n  customer_email text NOT NULL,\n  customer_name text NOT NULL,\n  show_id bigint NOT NULL,\n  event_id bigint NOT NULL,\n  event_title text NOT NULL,\n  venue_id bigint NOT NULL,\n  venue_name text NOT NULL,\n  starts_at timestamptz NOT NULL,\n  seat_id bigint NOT NULL,\n  seat_label text NOT NULL,\n  unit_price numeric(12,2) NOT NULL,\n  PRIMARY KEY (booking_id, show_id, seat_id)\n);\n```\n\nMột hàng đồng thời phát biểu ít nhất sáu sự thật: booking thuộc customer; customer có email và tên; show thuộc event và venue; event có title; venue có name; seat có label trong venue; booking mua seat của show với giá chốt. Các sự thật có determinant khác nhau nhưng bị lặp theo khóa dòng vé.\n\nChuẩn hoá không bắt đầu bằng đếm cột. Ta phải viết các phụ thuộc được nghiệp vụ cam kết. Nếu email có thể dùng chung trong gia đình thì `customer_email → customer_id` không đúng. Nếu một event đổi title theo ngôn ngữ thì `event_id → event_title` chỉ đúng trong mô hình một ngôn ngữ. Phụ thuộc là phát biểu về mọi dữ liệu hợp lệ, không phải quy luật tình cờ đúng trên file mẫu hôm nay.\n\n### Phụ thuộc hàm: đọc mũi tên như lời hứa\n\nVới giả định của hệ thống vé đơn giản, ta có:\n\n```\ncustomer_id → customer_email, customer_name\nevent_id    → event_title\nvenue_id    → venue_name\nshow_id     → event_id, venue_id, starts_at\n(venue_id, seat_id) → seat_label\n(booking_id, show_id, seat_id) → unit_price\nbooking_id  → customer_id\n```\n\nVế trái là determinant, không nhất thiết là primary key của bảng hiện tại. Dòng `show_id → venue_id` nói cùng show không thể xuất hiện với hai venue. Nếu nghiệp vụ cho một show dùng nhiều địa điểm, phụ thuộc này sai và mô hình phải đổi; chuẩn hoá không sửa được giả định sai.\n\nTa cần tìm closure (bao đóng) để biết một tập thuộc tính suy ra được gì. Từ `booking_id`, suy ra `customer_id`, rồi suy ra email và tên. Đây là chuỗi bắc cầu. Từ `(booking_id, show_id, seat_id)`, suy ra mọi cột trong bảng nên bộ ba là superkey; nếu không bỏ được thành phần nào mà vẫn suy ra tất cả, nó là candidate key.\n\n### 1NF: một ô mang một giá trị theo miền\n\nFirst Normal Form (1NF) thường được diễn giải là “không có danh sách trong một ô”. Ý sâu hơn: mỗi thuộc tính lấy một giá trị thuộc miền đã định, mỗi hàng có thể được nhận diện, và không có nhóm cột lặp như `seat_1`, `seat_2`, `seat_3`.\n\nThiết kế sau vi phạm tinh thần 1NF:\n\n```sql\nCREATE TABLE bad_booking (\n  booking_id bigint PRIMARY KEY,\n  seat_ids text NOT NULL  -- ví dụ '12,13,21'\n);\n```\n\nChuỗi đó không cho foreign key kiểm tra từng seat, không ngăn trùng `12,12`, và truy vấn phải parse text. Sửa bằng một hàng cho mỗi phần tử trong `booking_item`. Số ghế có thể là không hoặc nhiều mà không cần đổi schema.\n\nJSON trong PostgreSQL không mặc nhiên là sai. Một payload webhook được lưu nguyên bản để audit có thể là một giá trị tài liệu nguyên tử đối với miền ứng dụng. Nhưng nếu thường xuyên join, lọc, đặt unique hoặc foreign key trên từng phần tử, dữ liệu đó đang đóng vai quan hệ và nên được mô hình hóa thành hàng/cột. “Nguyên tử” phụ thuộc phép toán mà hệ thống cần, không chỉ phụ thuộc kiểu vật lý.\n\n### Anomaly của 1NF kém: cập nhật một phần tử không còn nguyên tử\n\nVới danh sách text, đổi seat 13 thành 14 phải thao tác chuỗi; `LIKE '%13%'` còn có thể nhầm 113. Hai transaction cùng sửa hai phần tử có thể ghi đè toàn bộ cột của nhau. Khi mỗi item là một hàng, primary key và row lock hoạt động đúng phạm vi.\n\nCa biên là booking rỗng. Một bảng cha booking có thể tồn tại trước khi item được thêm; 1NF không buộc phải có item. Quy tắc “booking confirmed có ít nhất một item” vượt quá cấu trúc cơ bản và cần transaction hoặc trigger. Chuẩn hoá không thay thế mọi invariant nghiệp vụ.\n\n### 2NF chỉ trở thành vấn đề khi có khóa ghép\n\nSecond Normal Form (2NF) yêu cầu quan hệ đã ở 1NF và mọi thuộc tính không khóa phụ thuộc đầy đủ vào toàn bộ mỗi candidate key, không phụ thuộc một phần thực sự của khóa ghép. Nếu khóa ứng viên chỉ có một cột, không thể có phụ thuộc bộ phận; quan hệ đó tự động đạt 2NF, dù vẫn có thể vi phạm 3NF.\n\nTrong `ticket_sale_flat`, khóa `(booking_id, show_id, seat_id)` nhưng `customer_id` chỉ phụ thuộc `booking_id`; thông tin show chỉ phụ thuộc `show_id`; nhãn ghế phụ thuộc seat trong venue. Vì vậy mỗi dòng vé lặp dữ liệu của booking và show. Ta tách:\n\n```sql\nCREATE TABLE booking (\n  booking_id bigint PRIMARY KEY,\n  customer_id bigint NOT NULL\n);\n\nCREATE TABLE show (\n  show_id bigint PRIMARY KEY,\n  event_id bigint NOT NULL,\n  venue_id bigint NOT NULL,\n  starts_at timestamptz NOT NULL\n);\n\nCREATE TABLE booking_item (\n  booking_id bigint NOT NULL REFERENCES booking,\n  show_id bigint NOT NULL REFERENCES show,\n  seat_id bigint NOT NULL,\n  unit_price numeric(12,2) NOT NULL,\n  PRIMARY KEY (booking_id, show_id, seat_id)\n);\n```\n\n`unit_price` phụ thuộc toàn bộ giao dịch: giá đã chốt có thể khác giữa booking, show và seat. Không được tách nó chỉ vì có từ “price”. Phải hỏi giá nào và tại thời điểm nào.\n\n### Phân biệt phụ thuộc bộ phận với thuộc tính của quan hệ\n\nTrong bảng `show_seat(show_id, seat_id, price)`, khóa là cặp và `price` phụ thuộc cặp: cùng seat vật lý có giá khác ở show khác, cùng show có hạng ghế khác giá. Đây là 2NF hợp lệ. Nếu mọi seat trong show luôn cùng giá theo quy tắc bất biến, lúc đó `show_id → price` và price nên chuyển sang show hoặc bảng hạng giá.\n\nSai lầm phổ biến là nhìn dữ liệu mẫu có mọi giá bằng nhau rồi kết luận phụ thuộc. Phải hỏi liệu hai giá khác nhau có hợp lệ trong tương lai. Phụ thuộc hàm đến từ quy tắc miền, không được suy chỉ từ vài hàng.\n\n### 3NF loại đường vòng qua thuộc tính không khóa\n\nThird Normal Form (3NF) yêu cầu đã đạt 2NF và không có thuộc tính không khóa phụ thuộc bắc cầu vào candidate key qua một thuộc tính không khóa khác. Trong `show`, nếu lưu cả `venue_id` và `venue_name`, ta có `show_id → venue_id → venue_name`. Tên venue không mô tả show trực tiếp; nó mô tả venue.\n\nTách `venue(venue_id, venue_name)` và để show giữ `venue_id`. Tương tự, `event_title` đi về event, thông tin customer đi về customer:\n\n```sql\nCREATE TABLE customer (\n  customer_id bigint PRIMARY KEY,\n  email text NOT NULL,\n  display_name text NOT NULL\n);\n\nCREATE TABLE venue (\n  venue_id bigint PRIMARY KEY,\n  name text NOT NULL\n);\n\nCREATE TABLE event (\n  event_id bigint PRIMARY KEY,\n  title text NOT NULL\n);\n\nALTER TABLE booking\n  ADD FOREIGN KEY (customer_id) REFERENCES customer;\nALTER TABLE show\n  ADD FOREIGN KEY (event_id) REFERENCES event,\n  ADD FOREIGN KEY (venue_id) REFERENCES venue;\n```\n\nGiờ đổi tên venue sửa đúng một hàng. Các show tự nhìn thấy tên mới qua join. Nếu hóa đơn cũ phải giữ tên pháp lý tại thời điểm bán, đó là snapshot có nghĩa lịch sử, không phải bản sao vô tình; phải đặt tên như `billed_venue_name` và quy định bất biến sau commit.\n\n### Định nghĩa 3NF chính xác hơn khẩu quyết\n\nKhẩu quyết “mọi thuộc tính phụ thuộc vào khóa, toàn bộ khóa và không gì ngoài khóa” hữu ích nhưng có thể che trường hợp nhiều candidate key. Định nghĩa chính thức: với mọi phụ thuộc hàm không tầm thường `X → A`, hoặc X là superkey, hoặc A là prime attribute (thuộc một candidate key).\n\nBoyce–Codd Normal Form (BCNF) mạnh hơn: với mọi phụ thuộc không tầm thường, X phải là superkey. Phần lớn schema nghiệp vụ đơn giản đạt cả 3NF và BCNF, nhưng có quan hệ với candidate key chồng lấp đạt 3NF mà chưa đạt BCNF. Bài này dừng ở 3NF; điều quan trọng là không dùng khẩu quyết để bỏ sót khóa ứng viên khác primary key được chọn.\n\n### Phân rã không mất mát: join lại không sinh hàng ma\n\nTách bảng chỉ an toàn khi natural join theo thuộc tính chung khôi phục đúng quan hệ ban đầu, không mất và không sinh tổ hợp giả. Ví dụ tách `show(show_id, venue_id)` và `venue(venue_id, name)` là lossless vì `venue_id` xác định một venue, foreign key giữ liên kết.\n\nTách một quan hệ `R(A,B,C)` thành `R1(A,B)` và `R2(B,C)` có thể sinh hàng ngoài ý muốn nếu B không xác định A hoặc C. Với dữ liệu `(a1,b1,c1)` và `(a2,b1,c2)`, hai bảng chiếu join lại tạo bốn tổ hợp, gồm `(a1,b1,c2)` và `(a2,b1,c1)` chưa từng tồn tại. Vì vậy “mỗi bảng ít cột hơn” không đồng nghĩa chuẩn hoá đúng.\n\nMột điều kiện đủ quen thuộc cho phân rã nhị phân lossless là phần giao `R1 ∩ R2` phải xác định toàn bộ R1 hoặc toàn bộ R2 theo tập phụ thuộc. Trong thiết kế thực tế, primary key–foreign key thường cung cấp chính bảo đảm đó.\n\n### Bảo toàn phụ thuộc: constraint có kiểm tra tại một chỗ không\n\nPhân rã dependency-preserving nếu các phụ thuộc ban đầu có thể được kiểm tra trên từng bảng con mà không cần join. Điều này quan trọng vì unique constraint cục bộ rẻ và rõ hơn trigger phải join nhiều bảng trong mỗi lần ghi.\n\nĐôi khi BCNF buộc đánh đổi bảo toàn phụ thuộc; 3NF có thể được chọn để giữ constraint đơn giản. Không nên phân rã theo cảm giác đến mức một quy tắc cơ bản chỉ còn trong tài liệu. Nếu PostgreSQL không thể cưỡng chế bằng khóa, unique, check hay foreign key cục bộ, phải ghi rõ transaction hoặc trigger nào chịu trách nhiệm.\n\n### Schema 3NF cho lát cắt bán vé\n\nMột bản rút gọn sau chuẩn hoá có luồng phụ thuộc rõ:\n\n```sql\nCREATE TABLE seat (\n  seat_id bigint PRIMARY KEY,\n  venue_id bigint NOT NULL REFERENCES venue,\n  seat_label text NOT NULL,\n  UNIQUE (venue_id, seat_label)\n);\n\nCREATE TABLE show_seat (\n  show_id bigint NOT NULL REFERENCES show,\n  seat_id bigint NOT NULL REFERENCES seat,\n  list_price numeric(12,2) NOT NULL CHECK (list_price >= 0),\n  PRIMARY KEY (show_id, seat_id)\n);\n\nALTER TABLE booking_item\n  ADD FOREIGN KEY (show_id, seat_id)\n  REFERENCES show_seat(show_id, seat_id);\n```\n\nMỗi bảng nói một loại sự thật. `seat_label` phụ thuộc seat trong venue; `list_price` phụ thuộc cặp show–seat; `unit_price` ở booking_item là giá giao dịch. Hai cột giá có thể bằng nhau lúc mua nhưng không cùng nghĩa, nên không thể loại một chỉ dựa trên giá trị trùng.\n\n### Index sau chuẩn hoá: join không mặc nhiên chậm\n\nChuẩn hoá tạo nhiều join hơn, nhưng join trên primary key và foreign key có index phù hợp thường rất hiệu quả. Primary key tự tạo unique B-tree. PostgreSQL không tự tạo index ở phía foreign key, nên đường truy cập từ customer sang booking cần `booking(customer_id)`, từ event sang show cần `(event_id, starts_at)`.\n\n```sql\nCREATE INDEX booking_customer_created_idx\n  ON booking (customer_id, booking_id);\nCREATE INDEX show_event_starts_idx\n  ON show (event_id, starts_at);\n```\n\nThứ tự phải theo truy vấn. Index đầu không giúp tối ưu sắp theo `created_at` vì ví dụ schema chưa có cột đó; thiết kế thật nên dùng `(customer_id, created_at DESC)` nếu màn hình lịch sử cần. Đây là lý do schema logic và vật lý là hai lớp: chuẩn hoá quyết định sự thật nằm đâu, index quyết định cách đến đó nhanh.\n\n### Khi nào dừng ở 3NF và khi nào không nên tách thêm\n\n3NF là mặc định tốt cho dữ liệu giao dịch, không phải nghi lễ phải đưa mọi bảng tới dạng học thuật cao nhất. Tách `money(amount, currency)` thành bảng riêng chỉ vì có hai cột không phải chuẩn hoá nếu cặp đó là value object của giao dịch. Tách ngày thành bảng ngày chỉ hữu ích trong data warehouse có dimension, không cần cho mọi timestamp OLTP.\n\nKhông nên tiếp tục phân rã nếu không có phụ thuộc gây anomaly, nếu các thuộc tính cùng determinant và vòng đời, hoặc nếu việc tách làm constraint quan trọng không còn bảo toàn mà chẳng giảm dư thừa. Ngược lại, đừng viện hiệu năng để giữ một bảng phẳng trước khi đo: một bảng rộng lặp text làm tốn cache, tăng write amplification và có thể còn chậm hơn các bảng hẹp được index tốt.",

vi_du: "### Tái hiện update anomaly rồi loại bỏ nó\n\nNạp hai vé cùng venue vào bảng phẳng:\n\n```sql\nINSERT INTO ticket_sale_flat VALUES\n(1001, 7, 'lan@example.test', 'Lan', 501, 41, 'Mùa Thu', 9, 'Nhà hát Cũ',\n '2026-10-02 19:30+07', 12, 'A12', 500000),\n(1001, 7, 'lan@example.test', 'Lan', 501, 41, 'Mùa Thu', 9, 'Nhà hát Cũ',\n '2026-10-02 19:30+07', 13, 'A13', 500000);\n\nUPDATE ticket_sale_flat\nSET venue_name = 'Nhà hát Sông Hàn'\nWHERE venue_id = 9 AND seat_id = 12;\n\nSELECT DISTINCT venue_id, venue_name\nFROM ticket_sale_flat\nWHERE venue_id = 9;\n```\n\nKết quả trả hai tên cho cùng venue vì câu cập nhật sót một hàng. Database không vi phạm constraint nào: schema chưa nói `venue_id → venue_name`.\n\nSau khi tách venue và show, cập nhật chỉ còn:\n\n```sql\nUPDATE venue\nSET name = 'Nhà hát Sông Hàn'\nWHERE venue_id = 9;\n\nSELECT sh.show_id, v.name\nFROM show AS sh\nJOIN venue AS v USING (venue_id)\nWHERE sh.show_id = 501;\n```\n\nMọi truy vấn qua venue thấy một tên. Để migration dữ liệu cũ an toàn, trước khi tách phải tìm determinant có nhiều giá trị:\n\n```sql\nSELECT venue_id, count(DISTINCT venue_name) AS distinct_names\nFROM ticket_sale_flat\nGROUP BY venue_id\nHAVING count(DISTINCT venue_name) > 1;\n```\n\nNếu có kết quả, không được tùy tiện chọn `min(venue_name)`. Cần quy tắc làm sạch hoặc nguồn chuẩn. Chuẩn hoá schema không tự sửa lịch sử đã bất nhất.\n\n### Xác nhận join không mất hàng\n\nSau migration, so số dòng và tổng tiền giữa nguồn với join đích:\n\n```sql\nSELECT count(*) AS rows, sum(unit_price) AS total\nFROM ticket_sale_flat;\n\nSELECT count(*) AS rows, sum(bi.unit_price) AS total\nFROM booking_item AS bi\nJOIN booking AS b USING (booking_id)\nJOIN customer AS c USING (customer_id)\nJOIN show AS sh USING (show_id)\nJOIN event AS e USING (event_id)\nJOIN venue AS v USING (venue_id);\n```\n\nHai phép tổng là smoke test, chưa chứng minh tuyệt đối lossless: các sai lệch bù nhau vẫn có thể cho cùng tổng. Nên đối chiếu khóa từng hàng bằng `EXCEPT` theo cả hai chiều. Ca dữ liệu rỗng cũng cần chạy; `sum` trả NULL chứ không trả 0, nên dùng `coalesce(sum(unit_price), 0)` khi hệ thống cần số không.",

so_sanh: "### 1NF, 2NF và 3NF sửa ba lớp vấn đề khác nhau\n\n| Dạng chuẩn | Câu hỏi chẩn đoán | Dấu hiệu vi phạm trong ví dụ | Cách sửa chính |\n|---|---|---|---|\n| 1NF | một ô có phải chứa tập/lặp nhóm không? | `seat_ids = '12,13'` | một hàng `booking_item` cho mỗi ghế |\n| 2NF | thuộc tính có phụ thuộc chỉ một phần khóa ghép không? | `booking_id → customer_id` trong khóa ba cột | tách booking và show khỏi dòng vé |\n| 3NF | khóa có suy ra thuộc tính qua một thuộc tính không khóa không? | `show_id → venue_id → venue_name` | tách venue, giữ foreign key ở show |\n| BCNF | mọi determinant có là superkey không? | trường hợp khóa ứng viên chồng lấp | phân rã mạnh hơn, cân nhắc bảo toàn phụ thuộc |\n\n### Chuẩn hoá và bảng phẳng báo cáo\n\n| Tiêu chí | Schema giao dịch 3NF | Bảng phẳng phục vụ đọc |\n|---|---|---|\n| Cập nhật sự thật | một nơi, constraint rõ | phải đồng bộ nhiều bản sao |\n| Truy vấn chi tiết | cần join | đơn giản hơn cho một số báo cáo |\n| Kích thước text lặp | thấp | có thể cao |\n| Dữ liệu lịch sử | mô hình sự kiện/snapshot rõ | dễ trộn hiện tại với lịch sử |\n| Độ trễ nhất quán | ngay trong transaction | có thể trễ nếu làm mới bất đồng bộ |\n\nKhông phải chọn một cho toàn hệ thống. OLTP có thể giữ nguồn chuẩn 3NF, rồi tạo materialized view hoặc warehouse cho phân tích. Bài kế tiếp sẽ đặt tiêu chí nghiêm ngặt cho việc lưu dư này.",

loi_thuong_gap: [
  "**Suy phụ thuộc từ vài hàng mẫu.** Mọi giá vé hiện bằng nhau không chứng minh `show_id → price`; hãy hỏi giá khác nhau có hợp lệ theo nghiệp vụ không.",
  "**Cho rằng bảng có ID một cột chắc chắn đạt 2NF và 3NF.** Nó chỉ tự động tránh phụ thuộc bộ phận của khóa một cột, vẫn có thể chứa phụ thuộc bắc cầu.",
  "**Tách bảng theo chủ đề mà không kiểm lossless join.** Dấu hiệu là join lại sinh nhiều hàng hơn nguồn; kiểm khóa phần giao và đối chiếu bằng `EXCEPT`.",
  "**Coi mọi JSON là vi phạm 1NF.** Payload nguyên bản có thể là một giá trị tài liệu; chỉ tách khi phần tử cần query, join hoặc constraint độc lập.",
  "**Xóa `unit_price` vì đã có `list_price`.** Hóa đơn cũ đổi theo giá hiện tại; phân biệt snapshot giao dịch với giá danh mục.",
  "**Chuẩn hoá schema nhưng bỏ qua dữ liệu bất nhất cũ.** Migration gặp nhiều tên cho một venue; chạy truy vấn phát hiện trước và định nghĩa nguồn chuẩn.",
  "**Tin rằng nhiều join luôn chậm.** Chưa xem `EXPLAIN` và index khóa ngoại thì kết luận không có bằng chứng; đo trên dữ liệu đại diện.",
  "**Phân rã đến mức constraint phải join nhiều bảng.** Nếu không giảm anomaly đáng kể mà mất bảo toàn phụ thuộc, thiết kế đã quá tay."
],

tu_kiem_tra: [
  "Viết các phụ thuộc hàm chính của bảng phẳng bán vé và chỉ ra determinant của `venue_name`.",
  "Vì sao bảng có primary key một cột tự động không vi phạm 2NF nhưng vẫn có thể vi phạm 3NF?",
  "`show_seat.price` phụ thuộc `show_id` hay cặp `(show_id, seat_id)`? Điều kiện nghiệp vụ nào làm đáp án thay đổi?",
  "Khi nào JSON là một giá trị hợp lý, và dấu hiệu nào cho thấy nó đang che một quan hệ nhiều hàng?",
  "Giải thích insert, update và delete anomaly bằng ba thao tác trên `ticket_sale_flat`.",
  "Câu khó: đưa ví dụ phân rã `R(A,B,C)` join lại sinh hàng ma và nêu điều kiện khiến phân rã nhị phân lossless.",
  "Tại sao so `count` và `sum` chưa đủ chứng minh migration chính xác? Bạn sẽ dùng `EXCEPT` như thế nào?",
  "Vì sao `unit_price` và `list_price` có thể cùng tồn tại mà không phải dư thừa gây 3NF sai?"
],

tai_nguyen: [
  "Sách *SQL and Relational Theory*.",
  "Sách *Database System Concepts*.",
  "Sách *Fundamentals of Database Systems*.",
  "Tài liệu PostgreSQL, phần Constraints, Data Definition và Queries."
],

lien_he: [
  "Mảng 02 về tập hợp và hash giúp hiểu khóa ứng viên, tính duy nhất và phép join dưới góc nhìn thuật toán.",
  "Mảng 03 về Git nối sang migration có kiểm soát: phân rã bảng cần version, review và đường rollback dữ liệu.",
  "Mảng 06 về backend sẽ dùng transaction để bảo vệ invariant trải qua booking và booking_item sau khi chuẩn hoá.",
  "Mảng 07 về phân tích dữ liệu sẽ chủ động dùng mô hình chiều và bảng rộng, một đánh đổi khác với nguồn OLTP 3NF."
]

};
