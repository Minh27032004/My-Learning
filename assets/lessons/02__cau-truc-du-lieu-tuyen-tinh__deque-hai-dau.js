window.LESSONS["02:cau-truc-du-lieu-tuyen-tinh:deque-hai-dau"] = {

tom_tat: [
  "**Bốn bài trước đã nhắc `deque` nhiều lần như một giải pháp. Bài này mở nó ra:** nó là **danh sách liên kết các KHỐI mảng** — kết hợp cache locality của mảng với chèn/xoá O(1) của linked list.",
  "**Cấu trúc khối giải thích mọi đặc tính của `deque`:** hai đầu O(1) vì chỉ đụng khối biên; `dq[i]` ở giữa là O(n) vì phải đếm qua từng khối; không cần copy toàn bộ khi nở.",
  "**`deque` thay được cả stack lẫn queue** — và làm tốt cả hai. Đây là lý do nó là lựa chọn mặc định trong Python cho mọi cấu trúc tuyến tính không cần truy cập giữa.",
  "**`maxlen` biến nó thành ring buffer trong một dòng:** tự bỏ phần tử ở đầu kia khi đầy, bộ nhớ O(k) cố định, độ trễ ổn định vì không bao giờ cấp phát lại.",
  "**`rotate` là thao tác chỉ `deque` có, và nó O(k) chứ không O(n)** — chỉ dời con trỏ hai đầu, không dịch phần tử."
],

can_biet_truoc: [
  "Bài **Array và dynamic array** trong module này — cache locality và vì sao mảng phải dịch khi chèn ở đầu.",
  "Bài **Linked list** trong module này — cấu trúc nút và tham chiếu; `deque` là biến thể của nó ở cấp khối.",
  "Bài **Stack** và **Queue** trong module này — hai vai trò mà `deque` đảm nhận được cả hai.",
  "Bài **Phân tích khấu hao** ở module 1 mảng này — vì sao `list` phải copy khi nở, còn `deque` thì không."
],

dinh_nghia: "Nói đời thường: một đoàn tàu gồm nhiều toa. Mỗi toa chứa được vài chục hành khách và các toa nối với nhau. Thêm khách vào đầu hoặc cuối đoàn thì chỉ đụng tới toa biên; đầy toa thì móc thêm một toa mới, **không phải dời cả đoàn sang chỗ khác**. Nhưng muốn tìm hành khách thứ 500 thì phải đi qua từng toa mà đếm.\n\nCác khái niệm:\n\n- **Deque** (*double-ended queue*, đọc là \"đếch\") — cấu trúc cho phép thêm và lấy ở **cả hai đầu**, đều O(1).\n- **Khối** (*block*) — một mảng nhỏ cố định. `deque` của CPython dùng khối chứa được **64 con trỏ**.\n- **Danh sách liên kết các khối** — các khối nối nhau bằng con trỏ hai chiều, như một danh sách liên kết đôi mà mỗi \"nút\" là một mảng nhỏ.\n- **`maxlen`** — giới hạn số phần tử. Vượt quá thì phần tử ở **đầu kia** tự bị bỏ.\n- **`rotate(k)`** — xoay `deque`: `k > 0` dời phải, `k < 0` dời trái.\n\nĐịnh vị `deque` giữa hai cấu trúc đã học — đây là trục của cả bài:\n\n```\nMẢNG (list)                DEQUE                    LINKED LIST\n───────────────            ─────────────────        ──────────────\n[a][b][c][d][e]            [a][b] ⇄ [c][d] ⇄ [e]    a → b → c → d\nmột khối liên tục          nhiều KHỐI nối nhau       mỗi phần tử một nút\n\nds[i]:     O(1) ⭐          dq[i]:  O(n) ở giữa       O(n)\nthêm đầu:  O(n) ⚠️          O(1) ⭐                   O(1) ⭐\nlocality:  tốt ⭐           khá tốt (trong khối)      tệ ⚠️\nnở:        COPY toàn bộ     ⭐ chỉ thêm MỘT khối      không cần\n```\n\nĐọc bảng này theo hàng: **`deque` lấy được phần lớn ưu điểm của cả hai, và trả giá đúng một chỗ — truy cập ngẫu nhiên.**",

vi_sao_can: "Bốn bài trước đã dùng `deque` như một giải pháp mà chưa giải thích. Bài này lấp chỗ đó, và có bốn lý do cụ thể:\n\n- **Bạn sẽ dùng nó rất nhiều.** Mọi hàng đợi, mọi BFS, mọi cửa sổ trượt trong Python đều nên dùng `deque`. Biết vì sao nó nhanh giúp bạn biết khi nào nó **không** phù hợp.\n- **Nó cho thấy một mẫu thiết kế quan trọng: chia thành khối.** Ý tưởng \"đừng chọn một cực, hãy chia nhỏ rồi liên kết\" xuất hiện lại ở B-tree của database (mảng 04), ở phân trang bộ nhớ (module 2 mảng 01), và ở sharding hệ thống phân tán (mảng 06).\n- **Nó giải thích cái bẫy `dq[i]`.** Rất nhiều người dùng `deque` rồi truy cập theo chỉ số trong vòng lặp và không hiểu vì sao chậm — đây là lỗi có thật và bài này chỉ ra nguyên nhân.\n- **`maxlen` giải quyết ba vấn đề cùng lúc trong một dòng:** giới hạn bộ nhớ, ring buffer, và độ trễ ổn định. Bài khấu hao ở module 1 đã nêu vấn đề độ trễ đuôi; đây là câu trả lời gọn nhất.\n\nTình huống thật rất phổ biến: bạn viết chương trình theo dõi log, giữ 1.000 dòng gần nhất để hiển thị. Dùng `list` thì phải `append` rồi `pop(0)` khi vượt 1.000 — và `pop(0)` là O(n). Dùng `deque(maxlen=1000)` thì **một dòng khai báo là xong**, mọi thao tác O(1), bộ nhớ cố định, và không bao giờ có lần cấp phát lại gây khựng.\n\nMột lưu ý về ranh giới rõ ràng: **nếu bạn cần truy cập theo chỉ số thường xuyên, `deque` là lựa chọn sai.** Đây không phải chi tiết nhỏ — nó là đánh đổi trung tâm của cấu trúc này.",

co_che: "### Cấu trúc khối — mở nắp `deque`\n\n```\ndeque KHÔNG phải một mảng liên tục, cũng KHÔNG phải linked list\nthuần tuý. Nó là DANH SÁCH LIÊN KẾT ĐÔI CỦA CÁC KHỐI MẢNG.\n\n         khối trái          khối giữa         khối phải\n       ┌─────────────┐   ┌─────────────┐   ┌─────────────┐\n  ⇄    │ · · a b c d │ ⇄ │ e f g h i j │ ⇄ │ k l · · · · │   ⇄\n       └─────────────┘   └─────────────┘   └─────────────┘\n            ↑ trái=2                            ↑ phải=2\n\n  · = ô trống dành sẵn\n  Mỗi khối của CPython chứa 64 con trỏ.\n```\n\nBa quan sát rút ra trực tiếp từ hình này, và chúng giải thích **mọi** đặc tính của `deque`:\n\n```\n① HAI KHỐI BIÊN có ô trống dành sẵn\n   ⇒ thêm/xoá hai đầu chỉ ghi vào ô trống  ⇒ O(1)\n\n② TRONG MỘT KHỐI, phần tử nằm LIÊN TỤC\n   ⇒ cache locality tốt khi duyệt tuần tự\n\n③ MUỐN TỚI phần tử thứ i, phải ĐI QUA từng khối mà đếm\n   ⇒ dq[i] ở giữa là O(n)\n```\n\n### Vì sao thêm ở đầu là O(1)\n\nSo sánh trực tiếp với `list`:\n\n```\nlist.insert(0, x)\n\n    [a][b][c][d][e]\n     →  →  →  →  →     ⚠️ DỊCH toàn bộ n phần tử\n    [x][a][b][c][d][e]\n\n    ⇒ O(n)\n```\n\n```\ndeque.appendleft(x)\n\n    ┌─────────────┐\n    │ · · a b c d │      còn ô trống ở đầu khối\n    └─────────────┘\n         ↓ ghi vào ô trống, lùi con trỏ trái\n    ┌─────────────┐\n    │ · x a b c d │\n    └─────────────┘\n\n    ⇒ O(1), KHÔNG dịch phần tử nào\n```\n\nKhi khối biên hết ô trống:\n\n```\n    ┌─────────────┐\n    │ x y a b c d │      ĐẦY\n    └─────────────┘\n         ↓ móc thêm MỘT khối mới ở đầu\n    ┌─────────────┐   ┌─────────────┐\n    │ · · · · · z │ ⇄ │ x y a b c d │\n    └─────────────┘   └─────────────┘\n\n⭐ CHỈ cấp phát một khối 64 ô.\n  KHÔNG copy phần tử cũ nào — khác hẳn list phải copy toàn bộ.\n```\n\nĐây là khác biệt then chốt so với mảng động ở bài đầu module:\n\n```\nlist nở:   cấp vùng LỚN HƠN + COPY toàn bộ n phần tử  → O(n) một lần\ndeque nở:  cấp MỘT khối 64 ô, nối vào                 → O(1) THẬT\n\n⇒ list: O(1) KHẤU HAO (có lần chậm bất thường)\n  deque: O(1) THẬT     (mọi lần đều nhanh như nhau)\n```\n\nHệ quả rất thực tế, nối về phần độ trễ đuôi ở bài khấu hao: **`deque` cho độ trễ ổn định**, còn `list` thỉnh thoảng có một `append` chậm hơn hàng nghìn lần khi phải cấp phát lại.\n\n### Vì sao `dq[i]` ở giữa là O(n)\n\n```\nMẢNG: địa_chỉ(i) = gốc + i × ô          ⇒ nhảy thẳng, O(1)\n\nDEQUE: các khối RỜI NHAU trong bộ nhớ\n       ⇒ không có công thức địa chỉ chung\n       ⇒ phải đi từ đầu (hoặc cuối) qua từng khối, đếm dần\n```\n\n```\nTối ưu nhỏ của CPython: nó chọn đi từ đầu GẦN HƠN.\n\n    dq[5]         với n = 1000  → đi từ TRÁI, qua ~1 khối\n    dq[995]       với n = 1000  → đi từ PHẢI, qua ~1 khối\n    dq[500]       với n = 1000  → đi ~8 khối     ⚠️ chậm nhất\n\n⇒ Vẫn là O(n), nhưng hằng số nhỏ hơn n/64 vì nhảy theo KHỐI\n  chứ không theo từng phần tử.\n```\n\n⚠️ Bẫy thực tế rất phổ biến:\n\n```python\n# ❌ O(n²) — mỗi lần dq[i] là O(n)\nfor i in range(len(dq)):\n    xu_ly(dq[i])\n\n# ✓ O(n) — duyệt tuần tự, deque tối ưu cho việc này\nfor x in dq:\n    xu_ly(x)\n```\n\nĐây là lỗi có thật và khó nhận ra, vì code trông hoàn toàn bình thường.\n\n### `maxlen` — ring buffer trong một dòng\n\n```python\ndq = deque(maxlen=3)\n```\n\n```\nThêm khi đã đầy:\n\n    append(x)      → bỏ phần tử ở ĐẦU TRÁI\n    appendleft(x)  → bỏ phần tử ở ĐẦU PHẢI\n\n    deque([1,2,3], maxlen=3)\n    .append(4)   → deque([2,3,4])      ⭐ 1 bị bỏ\n    .appendleft(0) → deque([0,2,3])    ⭐ 4 bị bỏ\n```\n\nBa vấn đề được giải quyết cùng lúc:\n\n```\n① BỘ NHỚ O(k) CỐ ĐỊNH — không bao giờ phình\n② TỰ ĐỘNG bỏ mục cũ — không cần viết logic\n③ ĐỘ TRỄ ỔN ĐỊNH — không bao giờ cấp phát lại sau khi đạt maxlen\n```\n\nĐiểm ③ trả lời trực tiếp vấn đề nêu ở bài khấu hao module 1: khi cần bảo đảm p99, `deque(maxlen=k)` là câu trả lời gọn nhất.\n\n⚠️ Hai lưu ý:\n\n```\n· deque KHÔNG cho biết phần tử nào vừa bị bỏ\n  → cần biết (để ghi log, đóng tài nguyên) thì phải tự cài\n\n· maxlen là BẤT BIẾN sau khi tạo — không đổi được\n```\n\n### `rotate` — thao tác chỉ `deque` có\n\n```python\ndq.rotate(k)      # k > 0: dời PHẢI;  k < 0: dời TRÁI\n```\n\n```\ndeque([1,2,3,4,5])\n\n  .rotate(2)   → deque([4,5,1,2,3])     hai phần tử cuối ra đầu\n  .rotate(-2)  → deque([3,4,5,1,2])     hai phần tử đầu ra cuối\n```\n\nVì sao nó nhanh:\n\n```\nrotate(k) là O(k), KHÔNG phải O(n)\n\nCƠ CHẾ: nó thực hiện k lần \"lấy một đầu, đặt sang đầu kia\"\n        — mỗi lần O(1), không dịch phần tử nào.\n\n⇒ rotate(1) là O(1)\n⇒ rotate(n//2) là O(n) — trường hợp xấu nhất\n```\n\nSo với cách làm tương đương trên `list`:\n\n```python\nds = ds[-k:] + ds[:-k]      # ⚠️ O(n) — tạo HAI list mới rồi nối\n```\n\n### Chi phí thao tác — bảng đầy đủ\n\n```\nTHAO TÁC                 deque        list          Ghi chú\n──────────────────       ─────        ──────        ─────────────────\nappend(x)                O(1) ⭐      O(1) khấu hao\nappendleft(x)            O(1) ⭐      O(n) ⚠️\npop()                    O(1)         O(1)\npopleft()                O(1) ⭐      O(n) ⚠️\ndq[0], dq[-1]            O(1) ⭐      O(1)\ndq[i] ở giữa             O(n) ⚠️      O(1) ⭐       ← ĐÁNH ĐỔI CHÍNH\ninsert(i, x)             O(n)         O(n)\nrotate(k)                O(k) ⭐      O(n)          list không có sẵn\nextend / extendleft      O(k)         O(k) / O(k·n)\nx in dq                  O(n)         O(n)\nlen(dq)                  O(1)         O(1)\nsort()                   ✗ không có   O(n log n)    ⚠️ deque KHÔNG sắp được\ncắt lát dq[a:b]          ✗ không có   O(b−a)        ⚠️ deque KHÔNG cắt lát\n```\n\nHai dòng cuối là hạn chế thật, hay bị quên:\n\n```python\n# ❌ Không làm được\n# dq.sort()\n# dq[2:5]\n\n# ✓ Phải chuyển sang list trước — O(n)\ndq = deque(sorted(dq))\ncat = list(dq)[2:5]\nimport itertools\ncat = list(itertools.islice(dq, 2, 5))    # ⭐ không copy toàn bộ\n```\n\n### An toàn đa luồng — một nửa sự thật\n\n```\nAN TOÀN (nhờ GIL, các thao tác này là nguyên tử):\n  append, appendleft, pop, popleft, len\n\nKHÔNG AN TOÀN:\n  các chuỗi thao tác kết hợp\n\n    if dq:                    ⚠️ luồng khác có thể popleft() ở giữa\n        x = dq.popleft()      ⚠️ ⇒ IndexError\n```\n\n```python\n# ✓ Cách đúng khi nhiều luồng: bắt lỗi thay vì kiểm tra trước\ntry:\n    x = dq.popleft()\nexcept IndexError:\n    x = None                  # ⭐ nguyên tử, không có khe hở\n\n# ✓ Hoặc dùng queue.Queue — có khoá và có chặn (bài queue)\n```\n\nMẫu \"thử rồi bắt lỗi\" thay vì \"kiểm tra rồi làm\" là nguyên tắc chung khi lập trình đồng thời — nối về bài race condition ở module 2 mảng 01.\n\n### Khi nào KHÔNG dùng `deque`\n\nPhản biện cần thiết:\n\n```\nDEQUE SAI KHI:\n· Cần truy cập theo chỉ số thường xuyên   → list\n· Cần cắt lát                              → list\n· Cần sắp xếp tại chỗ                      → list\n· Cần tra theo khoá                        → dict\n· Cần lấy phần tử nhỏ nhất                 → heap\n· Nhiều luồng cần chặn khi rỗng            → queue.Queue\n\nDEQUE ĐÚNG KHI:\n· Hàng đợi (FIFO)                          ⭐ lựa chọn mặc định\n· Ngăn xếp (LIFO)                          ⭐ cũng tốt\n· Cửa sổ trượt                             ⭐ maxlen\n· BFS                                       ⭐ popleft\n· Giữ N mục gần nhất                       ⭐ maxlen\n· Cần độ trễ ổn định                       ⭐ không cấp phát lại\n```\n\nQuy tắc gọn: **cần chỉ số thì `list`; cần hai đầu thì `deque`.**\n\n### Mẫu thiết kế \"chia thành khối\"\n\nÝ tưởng của `deque` xuất hiện lại ở nhiều nơi, và đáng nhận ra:\n\n```\nVẤN ĐỀ CHUNG: một cực thì tốt việc này, cực kia tốt việc khác\n\nGIẢI PHÁP CHUNG: chia thành các KHỐI vừa phải, rồi liên kết chúng\n\n  deque          khối 64 con trỏ, nối bằng danh sách đôi\n  B-tree         nút chứa nhiều khoá — mảng 04, index database\n  phân trang     bộ nhớ chia trang 4 KB — module 2 mảng 01\n  sharding       dữ liệu chia mảnh theo khoá — mảng 06\n  chunking file  đọc file theo lô — module 1 mảng này\n\n⇒ Cùng một tư duy: KHÔNG chọn một cực, mà chia nhỏ để lấy\n  ưu điểm của cả hai.\n```\n\nĐây là mẫu thiết kế đáng ghi nhớ hơn cả bản thân `deque` — vì bạn sẽ gặp lại nó ở mọi tầng của hệ thống.",

vi_du: "### Quan sát cấu trúc khối qua hành vi\n\n```python\nfrom collections import deque\nimport time\n\n# ⭐ Đo dq[i] theo VỊ TRÍ — bằng chứng của cấu trúc khối\ndq = deque(range(1_000_000))\n\nprint(f\"{'vị trí':>12} {'10k lần truy cập':>20}\")\nfor vi_tri in (0, 1_000, 250_000, 500_000, 999_999):\n    t0 = time.perf_counter()\n    for _ in range(10_000):\n        _ = dq[vi_tri]\n    print(f\"{vi_tri:>12,} {time.perf_counter()-t0:>19.4f}s\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\n      vị trí     10k lần truy cập\n           0              ~0.001s   ⭐ đầu — nhanh\n       1,000              ~0.005s\n     250,000              ~0.200s\n     500,000              ~0.400s   ⚠️ GIỮA — chậm nhất\n     999,999              ~0.001s   ⭐ cuối — nhanh (đi từ phải)\n\n⭐ Hình chuông: nhanh ở HAI ĐẦU, chậm nhất ở GIỮA.\n  Đây là bằng chứng trực tiếp của cấu trúc khối —\n  CPython chọn đi từ đầu gần hơn.\n\n⇒ list thì phẳng: mọi vị trí như nhau (công thức địa chỉ).\n```\n\n### Bẫy `dq[i]` trong vòng lặp\n\n```python\nfrom collections import deque\nimport time\n\nn = 30_000\ndq = deque(range(n))\n\n# ❌ O(n²) — mỗi dq[i] là O(n)\nt0 = time.perf_counter()\ns = 0\nfor i in range(len(dq)):\n    s += dq[i]                         # ⚠️ mỗi lần đi qua các khối\nt_chi_so = time.perf_counter() - t0\n\n# ✓ O(n) — duyệt tuần tự\nt0 = time.perf_counter()\ns = 0\nfor x in dq:                            # ⭐ đi thẳng qua từng khối một lần\n    s += x\nt_tuan_tu = time.perf_counter() - t0\n\nprint(f\"dq[i] trong vòng lặp: {t_chi_so:>8.4f}s\")\nprint(f\"duyệt tuần tự       : {t_tuan_tu:>8.4f}s\")\nprint(f\"chậm hơn: {t_chi_so/t_tuan_tu:.0f} lần\")\n```\n\n```\ndq[i] trong vòng lặp:  ~4.5000s\nduyệt tuần tự       :  ~0.0015s\nchậm hơn: ~3000 lần\n\n⚠️ Code trông HOÀN TOÀN BÌNH THƯỜNG nhưng là O(n²).\n  Đây là lỗi có thật và rất khó nhận ra khi đọc.\n\n⇒ Với deque: LUÔN duyệt tuần tự, KHÔNG bao giờ dùng chỉ số trong vòng lặp.\n```\n\n### Thêm ở đầu — `deque` so với `list`\n\n```python\nimport time\nfrom collections import deque\n\nprint(f\"{'n':>9} {'list.insert(0)':>18} {'deque.appendleft':>20} {'tỉ lệ':>9}\")\nfor n in (10_000, 20_000, 40_000):\n    t0 = time.perf_counter()\n    ds: list[int] = []\n    for i in range(n):\n        ds.insert(0, i)                # ⚠️ O(n) mỗi lần\n    t_list = time.perf_counter() - t0\n\n    t0 = time.perf_counter()\n    dq: deque[int] = deque()\n    for i in range(n):\n        dq.appendleft(i)               # ⭐ O(1) mỗi lần\n    t_deque = time.perf_counter() - t0\n\n    print(f\"{n:>9,} {t_list:>17.4f}s {t_deque:>19.4f}s \"\n          f\"{t_list/t_deque:>8.0f}x\")\n```\n\n```\n        n     list.insert(0)     deque.appendleft     tỉ lệ\n   10,000            ~0.020s              ~0.001s       ~20x\n   20,000            ~0.080s              ~0.002s       ~40x\n   40,000            ~0.320s              ~0.004s       ~80x\n\n⭐ Tỉ lệ GẤP ĐÔI khi n gấp đôi ⇒ O(n²) so với O(n).\n```\n\n### Độ trễ ổn định — `deque` không cấp phát lại\n\nNối trực tiếp về phần độ trễ đuôi ở bài khấu hao module 1:\n\n```python\nimport time\nfrom collections import deque\n\nn = 2_000_000\n\ndef do_do_tre(them_mot, so_lan: int) -> list[float]:\n    thoi_gian = []\n    for i in range(so_lan):\n        t0 = time.perf_counter_ns()\n        them_mot(i)\n        thoi_gian.append(time.perf_counter_ns() - t0)\n    return thoi_gian\n\nds: list[int] = []\ndq: deque[int] = deque()\n\nt_list = do_do_tre(ds.append, n)\nt_deque = do_do_tre(dq.append, n)\n\nfor ten, t in ((\"list.append\", t_list), (\"deque.append\", t_deque)):\n    t = sorted(t)\n    m = len(t)\n    print(f\"{ten}:\")\n    print(f\"   p50 {t[m//2]:>8,} ns\"\n          f\"   p999 {t[int(m*0.999)]:>9,} ns\"\n          f\"   MAX {t[-1]:>11,} ns\"\n          f\"   max/p50 = {t[-1]/max(1,t[m//2]):>6.0f}x\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\nlist.append:\n   p50 ~50 ns   p999 ~120 ns   MAX hàng chục nghìn ns   max/p50 = hàng trăm–nghìn lần\ndeque.append:\n   p50 ~55 ns   p999 ~100 ns   MAX nhỏ hơn nhiều        max/p50 = nhỏ hơn nhiều\n\n⭐ list: có những lần append CHẬM ĐỘT BIẾN — đó là lúc cấp phát lại\n        và COPY toàn bộ mảng.\n⭐ deque: chỉ móc thêm MỘT khối 64 ô ⇒ không có đỉnh nhọn.\n\n⇒ Cần bảo đảm SLA p999? Chọn deque.\n  Cần truy cập theo chỉ số? Chọn list.\n```\n\n### `maxlen` — ba vấn đề, một dòng\n\n```python\nfrom collections import deque\n\n# ── Giữ 5 dòng log gần nhất ──\nlog = deque(maxlen=5)\nfor i in range(10):\n    log.append(f\"dòng-{i}\")\nassert list(log) == [\"dòng-5\", \"dòng-6\", \"dòng-7\", \"dòng-8\", \"dòng-9\"]\nassert len(log) == 5                    # ⭐ KHÔNG BAO GIỜ vượt 5\n\n# ── Bỏ ở đầu nào tuỳ thêm ở đầu nào ──\nd = deque([1, 2, 3], maxlen=3)\nd.append(4)\nassert list(d) == [2, 3, 4]             # ⭐ thêm phải → bỏ TRÁI\nd.appendleft(0)\nassert list(d) == [0, 2, 3]             # ⭐ thêm trái → bỏ PHẢI\n\n# ── maxlen là BẤT BIẾN ──\nassert d.maxlen == 3\n# d.maxlen = 5      ⚠️ AttributeError — không đổi được\n```\n\nSo với cách tự làm bằng `list`:\n\n```python\nimport time\n\nn, k = 200_000, 1_000\n\n# ❌ list — pop(0) là O(k)\nt0 = time.perf_counter()\nds: list[int] = []\nfor i in range(n):\n    ds.append(i)\n    if len(ds) > k:\n        ds.pop(0)                       # ⚠️ dịch k phần tử\nt_list = time.perf_counter() - t0\n\n# ✓ deque(maxlen) — O(1)\nt0 = time.perf_counter()\ndq: deque[int] = deque(maxlen=k)\nfor i in range(n):\n    dq.append(i)                        # ⭐ tự bỏ, O(1)\nt_deque = time.perf_counter() - t0\n\nprint(f\"list + pop(0)     : {t_list:>7.4f}s\")\nprint(f\"deque(maxlen={k}) : {t_deque:>7.4f}s   nhanh hơn \"\n      f\"{t_list/t_deque:.0f} lần\")\n```\n\n⚠️ Hạn chế: `deque` **không cho biết** phần tử nào bị bỏ. Nếu cần biết:\n\n```python\nclass RingCoThongBao:\n    \"\"\"Ring buffer báo lại mục bị bỏ — để đóng tài nguyên hoặc ghi log.\"\"\"\n\n    def __init__(self, suc_chua: int) -> None:\n        self._dq: deque = deque()\n        self._suc_chua = suc_chua\n\n    def them(self, x):\n        \"\"\"Trả về mục bị bỏ, hoặc None.\"\"\"\n        bi_bo = None\n        if len(self._dq) >= self._suc_chua:\n            bi_bo = self._dq.popleft()    # ⭐ O(1)\n        self._dq.append(x)\n        return bi_bo\n\n    def __iter__(self):\n        return iter(self._dq)\n\n\nr = RingCoThongBao(3)\nfor x in \"ABCD\":\n    bo = r.them(x)\n    if bo:\n        print(f\"bị bỏ: {bo}\")           # bị bỏ: A\nassert list(r) == [\"B\", \"C\", \"D\"]\n```\n\n### `rotate` — O(k) chứ không O(n)\n\n```python\nfrom collections import deque\n\ndq = deque([1, 2, 3, 4, 5])\n\ndq.rotate(2)\nassert list(dq) == [4, 5, 1, 2, 3]      # ⭐ hai cuối ra đầu\n\ndq.rotate(-2)\nassert list(dq) == [1, 2, 3, 4, 5]      # ⭐ về như cũ\n\n# Ca biên\nd0 = deque([1, 2, 3])\nd0.rotate(0)\nassert list(d0) == [1, 2, 3]\nd0.rotate(3)                             # ⭐ xoay đúng một vòng\nassert list(d0) == [1, 2, 3]\nd0.rotate(7)                             # ⭐ 7 % 3 = 1\nassert list(d0) == [3, 1, 2]\n\ndeque().rotate(5)                        # ⭐ rỗng — không lỗi\n```\n\nĐo để thấy nó là O(k):\n\n```python\nimport time\n\nn = 1_000_000\nprint(f\"{'k':>10} {'rotate(k)':>14}\")\nfor k in (1, 100, 10_000, 500_000):\n    dq = deque(range(n))\n    t0 = time.perf_counter()\n    dq.rotate(k)\n    print(f\"{k:>10,} {time.perf_counter()-t0:>13.5f}s\")\n```\n\n```\n         k      rotate(k)\n         1        ~0.00000s   ⭐ gần như tức thì\n       100        ~0.00001s\n    10,000        ~0.00050s\n   500,000        ~0.02000s   ⚠️ k = n/2, chậm nhất\n\n⭐ Thời gian tỉ lệ với k, KHÔNG với n ⇒ O(k)\n```\n\nỨng dụng — lập lịch luân phiên:\n\n```python\nfrom collections import deque\n\nnguoi_truc = deque([\"An\", \"Bình\", \"Cường\"])\n\nfor tuan in range(1, 6):\n    print(f\"tuần {tuan}: {nguoi_truc[0]} trực\")\n    nguoi_truc.rotate(-1)               # ⭐ O(1) — người đầu xuống cuối\n```\n\n```\ntuần 1: An trực\ntuần 2: Bình trực\ntuần 3: Cường trực\ntuần 4: An trực\ntuần 5: Bình trực\n\n⇒ Với list phải viết: ds.append(ds.pop(0)) — và pop(0) là O(n).\n```\n\n### `deque` làm cả stack lẫn queue\n\n```python\nfrom collections import deque\n\n# ── STACK (LIFO) — dùng một đầu ──\nstack: deque[int] = deque()\nstack.append(1); stack.append(2); stack.append(3)\nassert stack.pop() == 3                 # ⭐ vào sau ra trước\n\n# ── QUEUE (FIFO) — dùng hai đầu ──\nqueue_: deque[int] = deque()\nqueue_.append(1); queue_.append(2); queue_.append(3)\nassert queue_.popleft() == 1            # ⭐ vào trước ra trước\n\n# ── DEQUE — dùng cả hai đầu tuỳ ý ──\ndq: deque[str] = deque()\ndq.append(\"phải-1\")\ndq.appendleft(\"trái-1\")\ndq.append(\"phải-2\")\nassert list(dq) == [\"trái-1\", \"phải-1\", \"phải-2\"]\nassert dq.pop() == \"phải-2\"\nassert dq.popleft() == \"trái-1\"\n```\n\nSo sánh với lựa chọn tối ưu cho từng vai trò:\n\n```python\nimport time\n\nn = 500_000\n\n# STACK: list nhỉnh hơn một chút\nt0 = time.perf_counter()\ns_list: list[int] = []\nfor i in range(n): s_list.append(i)\nwhile s_list: s_list.pop()\nt1 = time.perf_counter() - t0\n\nt0 = time.perf_counter()\ns_dq: deque[int] = deque()\nfor i in range(n): s_dq.append(i)\nwhile s_dq: s_dq.pop()\nt2 = time.perf_counter() - t0\n\nprint(f\"STACK — list: {t1:.4f}s   deque: {t2:.4f}s\")\n\n# QUEUE: deque thắng ÁP ĐẢO\nt0 = time.perf_counter()\nq_dq: deque[int] = deque()\nfor i in range(n): q_dq.append(i)\nwhile q_dq: q_dq.popleft()\nt3 = time.perf_counter() - t0\nprint(f\"QUEUE — deque: {t3:.4f}s   (list sẽ là O(n²), không đo)\")\n```\n\n```\nSTACK — list: ~0.05s   deque: ~0.06s     ⭐ list nhỉnh hơn chút\nQUEUE — deque: ~0.06s  (list là O(n²))   ⭐ deque thắng áp đảo\n\n⇒ STACK: dùng list (đơn giản hơn, có cắt lát và sắp xếp)\n  QUEUE: BẮT BUỘC dùng deque\n```\n\n### Cửa sổ trượt — ứng dụng đặc trưng\n\n```python\nfrom collections import deque\n\n\ndef trung_binh_truot(du_lieu: list[float], k: int) -> list[float]:\n    \"\"\"Trung bình cửa sổ k phần tử. O(n) thời gian, O(k) không gian.\n\n    Giữ TỔNG chạy thay vì tính lại mỗi cửa sổ.\n    \"\"\"\n    if k < 1:\n        raise ValueError(\"k phải ≥ 1\")\n    cua_so: deque[float] = deque(maxlen=k)\n    tong = 0.0\n    kq: list[float] = []\n    for x in du_lieu:\n        if len(cua_so) == k:\n            tong -= cua_so[0]           # ⭐ dq[0] là O(1)\n        cua_so.append(x)                 # tự bỏ phần tử cũ\n        tong += x\n        if len(cua_so) == k:\n            kq.append(tong / k)\n    return kq\n\n\nassert trung_binh_truot([1, 2, 3, 4, 5], 3) == [2.0, 3.0, 4.0]\nassert trung_binh_truot([1, 2], 3) == []          # ⭐ ngắn hơn cửa sổ\nassert trung_binh_truot([5], 1) == [5.0]          # biên\n```\n\n```\n⭐ Chú ý cua_so[0] là O(1) — truy cập ĐẦU, không phải giữa.\n  Đây là cách dùng deque ĐÚNG: chỉ chạm hai đầu.\n```\n\nBài toán khó hơn — **maximum của mọi cửa sổ**, nơi `deque` là lời giải kinh điển:\n\n```python\nfrom collections import deque\n\n\ndef max_cua_so_truot(ds: list[int], k: int) -> list[int]:\n    \"\"\"Giá trị LỚN NHẤT của mọi cửa sổ độ dài k. O(n) thời gian.\n\n    deque lưu CHỈ SỐ, giữ giá trị GIẢM DẦN từ trái sang phải\n    ⇒ phần tử đầu deque luôn là max của cửa sổ hiện tại.\n\n    Cách ngây thơ là O(n·k); cách này O(n) vì mỗi chỉ số\n    được thêm và bỏ ĐÚNG MỘT LẦN.\n    \"\"\"\n    if k < 1:\n        raise ValueError(\"k phải ≥ 1\")\n    dq: deque[int] = deque()            # lưu CHỈ SỐ, không lưu giá trị\n    kq: list[int] = []\n\n    for i, x in enumerate(ds):\n        # ① bỏ các chỉ số đã RA KHỎI cửa sổ\n        while dq and dq[0] <= i - k:\n            dq.popleft()                 # ⭐ O(1)\n        # ② bỏ các giá trị NHỎ HƠN x ở cuối — chúng không bao giờ là max nữa\n        while dq and ds[dq[-1]] <= x:\n            dq.pop()                     # ⭐ O(1)\n        dq.append(i)\n        if i >= k - 1:\n            kq.append(ds[dq[0]])        # ⭐ đầu deque là max\n    return kq\n\n\nassert max_cua_so_truot([1, 3, -1, -3, 5, 3, 6, 7], 3) == [3, 3, 5, 5, 6, 7]\nassert max_cua_so_truot([1, 2, 3], 1) == [1, 2, 3]      # ⭐ k = 1\nassert max_cua_so_truot([5, 4, 3], 3) == [5]            # ⭐ giảm dần\nassert max_cua_so_truot([3, 4, 5], 3) == [5]            # ⭐ tăng dần\nassert max_cua_so_truot([2, 2, 2], 2) == [2, 2]         # ⭐ bằng nhau\nassert max_cua_so_truot([1], 1) == [1]                   # biên\n```\n\nĐối chiếu với cách ngây thơ để xác nhận và đo:\n\n```python\nimport random\nimport time\n\ndef max_ngay_tho(ds: list[int], k: int) -> list[int]:\n    \"\"\"O(n·k) — tính max lại cho mỗi cửa sổ.\"\"\"\n    return [max(ds[i:i + k]) for i in range(len(ds) - k + 1)]\n\n\n# Đối chiếu trên dữ liệu ngẫu nhiên\nfor _ in range(200):\n    n = random.randint(1, 40)\n    ds = [random.randint(-50, 50) for _ in range(n)]\n    k = random.randint(1, n)\n    assert max_cua_so_truot(ds, k) == max_ngay_tho(ds, k), (ds, k)\nprint(\"✓ khớp cách ngây thơ trên 200 ca ngẫu nhiên\")\n\n# Đo\nds = [random.randint(0, 10**6) for _ in range(200_000)]\nk = 1_000\n\nt0 = time.perf_counter()\na = max_cua_so_truot(ds, k)\nt_deque = time.perf_counter() - t0\n\nt0 = time.perf_counter()\nb = max_ngay_tho(ds, k)\nt_ngay_tho = time.perf_counter() - t0\n\nassert a == b\nprint(f\"deque  O(n)   : {t_deque:>7.4f}s\")\nprint(f\"ngây thơ O(nk): {t_ngay_tho:>7.4f}s   chậm hơn \"\n      f\"{t_ngay_tho/t_deque:.0f} lần\")\n```\n\n```\ndeque  O(n)   :  ~0.05s\nngây thơ O(nk):  ~8.00s   chậm hơn ~160 lần\n\n⭐ Vì sao O(n): mỗi chỉ số được append ĐÚNG MỘT LẦN và pop\n  nhiều nhất MỘT LẦN ⇒ tổng thao tác ≤ 2n.\n\n⇒ Đây là kỹ thuật \"deque đơn điệu\" (monotonic deque) —\n  một trong những ứng dụng đẹp nhất của cấu trúc này.\n```\n\n### BFS — `deque` là lựa chọn chuẩn\n\n```python\nfrom collections import deque\n\n\ndef bfs_khoang_cach(do_thi: dict[str, list[str]], bat_dau: str) -> dict[str, int]:\n    \"\"\"Khoảng cách từ bat_dau tới mọi đỉnh. O(V + E).\"\"\"\n    kc = {bat_dau: 0}\n    hang_doi = deque([bat_dau])         # ⭐ deque, không phải list\n    while hang_doi:\n        d = hang_doi.popleft()          # ⭐ O(1) — list sẽ là O(n)\n        for ke in do_thi.get(d, []):\n            if ke not in kc:\n                kc[ke] = kc[d] + 1\n                hang_doi.append(ke)\n    return kc\n\n\ndo_thi = {\"A\": [\"B\", \"C\"], \"B\": [\"D\"], \"C\": [\"D\"], \"D\": [\"E\"], \"E\": []}\nassert bfs_khoang_cach(do_thi, \"A\") == {\"A\": 0, \"B\": 1, \"C\": 1, \"D\": 2, \"E\": 3}\nassert bfs_khoang_cach({\"X\": []}, \"X\") == {\"X\": 0}      # biên: một đỉnh\n```\n\n### Hạn chế của `deque`\n\n```python\nfrom collections import deque\nimport itertools\n\ndq = deque([3, 1, 2])\n\n# ❌ KHÔNG sắp xếp được tại chỗ\n# dq.sort()                             AttributeError\n\n# ✓ Phải qua list — O(n log n) + O(n) copy\ndq = deque(sorted(dq))\nassert list(dq) == [1, 2, 3]\n\n# ❌ KHÔNG cắt lát được\n# cat = dq[0:2]                         TypeError\n\n# ✓ islice — không copy toàn bộ\ncat = list(itertools.islice(dq, 0, 2))\nassert cat == [1, 2]\n\n# ⚠️ extendleft ĐẢO NGƯỢC thứ tự — dễ bất ngờ\nd = deque([3, 4])\nd.extendleft([2, 1])                    # ⭐ thêm 2 rồi thêm 1 vào ĐẦU\nassert list(d) == [1, 2, 3, 4]          # không phải [2, 1, 3, 4]\n```\n\nDòng cuối là bẫy thật: `extendleft` thêm **từng phần tử một** vào đầu, nên thứ tự bị đảo. Muốn giữ thứ tự thì truyền dãy đã đảo:\n\n```python\nd = deque([3, 4])\nd.extendleft(reversed([1, 2]))\nassert list(d) == [1, 2, 3, 4]          # ⭐ rõ ràng hơn\n```\n\n### An toàn đa luồng — nửa sự thật\n\n```python\nfrom collections import deque\nimport threading\n\ndq: deque[int] = deque()\n\n# ⭐ AN TOÀN: từng thao tác là nguyên tử\ndef nguoi_san_xuat():\n    for i in range(1000):\n        dq.append(i)                    # ⭐ nguyên tử\n\n# ⚠️ KHÔNG AN TOÀN: chuỗi kiểm-tra-rồi-làm\ndef nguoi_tieu_thu_sai():\n    while True:\n        if dq:                          # ⚠️ luồng khác có thể lấy mất ở đây\n            x = dq.popleft()            # ⚠️ ⇒ IndexError\n\n# ✓ AN TOÀN: thử rồi bắt lỗi\ndef nguoi_tieu_thu_dung():\n    while True:\n        try:\n            x = dq.popleft()            # ⭐ nguyên tử, không có khe hở\n        except IndexError:\n            break                        # hết việc\n```\n\n```\n⚠️ deque KHÔNG có cơ chế CHỜ khi rỗng.\n   Nhiều luồng cần chờ ⇒ dùng queue.Queue (bài queue trước).\n\n⇒ Nguyên tắc chung khi lập trình đồng thời:\n  THỬ RỒI BẮT LỖI, thay vì KIỂM TRA RỒI LÀM.\n```\n\n### Bảng chọn cấu trúc\n\n```python\n# ⭐ Hàng đợi / BFS\nfrom collections import deque\nhang: deque[str] = deque()\nhang.append(\"việc\"); viec = hang.popleft()\n\n# ⭐ N mục gần nhất / cửa sổ trượt\ngan_nhat = deque(maxlen=100)\n\n# ⭐ Luân phiên\nnguoi_truc = deque([\"A\", \"B\", \"C\"])\nnguoi_truc.rotate(-1)\n\n# ⭐ Cần chỉ số, cắt lát, sắp xếp → list\nds = [3, 1, 2]\nds.sort(); _ = ds[0:2]; _ = ds[1]\n\n# ⭐ Ngăn xếp → list cũng được, đơn giản hơn\nstack: list[int] = []\nstack.append(1); stack.pop()\n\n# ⭐ Nhiều luồng cần chặn → queue.Queue\nimport queue\nq: \"queue.Queue[str]\" = queue.Queue(maxsize=100)\n```",

so_sanh: "### Ba cấu trúc tuyến tính\n\n| | `list` (mảng) | **`deque` (khối)** | Linked list |\n|---|---|---|---|\n| Bố cục | một khối liên tục | **nhiều khối nối nhau** | mỗi phần tử một nút |\n| `[i]` giữa | **⭐ O(1)** | **O(n)** ⚠️ | O(n) |\n| Thêm/xoá đầu | **O(n)** ⚠️ | **⭐ O(1)** | ⭐ O(1) |\n| Thêm/xoá cuối | O(1) khấu hao | **⭐ O(1) thật** | O(1) (có tail) |\n| Khi nở | **COPY toàn bộ** | **⭐ thêm 1 khối** | không cần |\n| Độ trễ | có đỉnh nhọn ⚠️ | **⭐ ổn định** | ổn định |\n| Cache locality | **⭐ tốt nhất** | khá tốt | **tệ** ⚠️ |\n| Bộ nhớ | **⭐ thấp** | thấp | **cao** ⚠️ |\n\n`deque` lấy phần lớn ưu điểm của cả hai và trả giá đúng một chỗ: **truy cập ngẫu nhiên**.\n\n### Ba quan sát từ cấu trúc khối\n\n| Quan sát | Hệ quả |\n|---|---|\n| Khối biên có ô trống dành sẵn | thêm/xoá hai đầu **O(1)** |\n| Trong khối, phần tử liên tục | **cache locality khá tốt** |\n| Muốn tới phần tử i phải đếm qua khối | **`dq[i]` giữa là O(n)** |\n\n### `list` nở và `deque` nở\n\n| | `list` | `deque` |\n|---|---|---|\n| Khi đầy | cấp vùng lớn hơn + **COPY n phần tử** | thêm **một khối 64 ô** |\n| Bậc một lần | O(n) | **⭐ O(1)** |\n| Bậc tổng | O(1) **khấu hao** | O(1) **thật** |\n| Độ trễ đuôi (p999) | ⚠️ có đỉnh nhọn | **⭐ ổn định** |\n\nCần bảo đảm SLA p999 ⇒ chọn `deque`.\n\n### Chi phí thao tác\n\n| Thao tác | `deque` | `list` |\n|---|---|---|\n| `append` / `pop()` | **O(1)** | O(1) khấu hao |\n| `appendleft` / `popleft` | **⭐ O(1)** | **O(n)** ⚠️ |\n| `dq[0]`, `dq[-1]` | **O(1)** | O(1) |\n| `dq[i]` giữa | **O(n)** ⚠️ | **⭐ O(1)** |\n| `rotate(k)` | **⭐ O(k)** | không có (phải O(n)) |\n| `sort()` | **✗ không có** | O(n log n) |\n| cắt lát | **✗ không có** | O(b−a) |\n\n### `maxlen` — ba vấn đề một dòng\n\n| Vấn đề | Cách `maxlen` giải |\n|---|---|\n| Bộ nhớ phình vô hạn | **O(k) cố định** |\n| Phải tự viết logic bỏ mục cũ | **tự động** |\n| Độ trễ có đỉnh nhọn | **không bao giờ cấp phát lại** |\n\n⚠️ Hạn chế: không cho biết **phần tử nào** bị bỏ; `maxlen` **bất biến** sau khi tạo.\n\n### Thêm ở đầu nào thì bỏ ở đầu nào\n\n| Thao tác | Khi đầy thì bỏ |\n|---|---|\n| `append(x)` | phần tử **đầu trái** |\n| `appendleft(x)` | phần tử **đầu phải** |\n\n### `deque` làm stack hay queue\n\n| Vai trò | Lựa chọn tốt nhất | Vì sao |\n|---|---|---|\n| **Stack** | `list` (nhỉnh hơn) | đơn giản hơn, có `sort` và cắt lát |\n| **Queue** | **⭐ `deque` bắt buộc** | `list.pop(0)` là O(n) ⇒ O(n²) |\n| **Cả hai đầu** | `deque` | `list` không làm được O(1) |\n| **Nhiều luồng, cần chặn** | `queue.Queue` | `deque` không có cơ chế chờ |\n\n### An toàn đa luồng\n\n| | An toàn | Không an toàn |\n|---|---|---|\n| Thao tác | `append`, `appendleft`, `pop`, `popleft`, `len` | **chuỗi kiểm-tra-rồi-làm** |\n| Ví dụ | — | `if dq: dq.popleft()` ⚠️ |\n| Cách đúng | — | **thử rồi bắt `IndexError`** |\n\n### Khi nào KHÔNG dùng `deque`\n\n| Nhu cầu | Dùng gì |\n|---|---|\n| Truy cập theo chỉ số thường xuyên | **`list`** |\n| Cắt lát, sắp xếp tại chỗ | **`list`** |\n| Tra theo khoá | `dict` |\n| Lấy phần tử nhỏ nhất | `heap` |\n| Nhiều luồng cần chặn | `queue.Queue` |\n\n**Cần chỉ số thì `list`; cần hai đầu thì `deque`.**\n\n### Mẫu \"chia thành khối\" ở các tầng khác\n\n| Nơi xuất hiện | Khối là gì | Học ở |\n|---|---|---|\n| `deque` | 64 con trỏ | bài này |\n| **B-tree** | nút chứa nhiều khoá | mảng 04 |\n| **Phân trang bộ nhớ** | trang 4 KB | module 2 mảng 01 |\n| **Sharding** | mảnh dữ liệu theo khoá | mảng 06 |\n| Đọc file theo lô | lô n dòng | module 1 mảng này |\n\nCùng một tư duy: **không chọn một cực, mà chia nhỏ để lấy ưu điểm của cả hai.**",

loi_thuong_gap: [
  "**Dùng `dq[i]` trong vòng lặp.** Mỗi lần là O(n) ⇒ O(n²) tổng; đo được là chậm hơn hàng nghìn lần. Luôn duyệt tuần tự.",
  "**Tưởng `deque[i]` là O(1) như `list`.** Chỉ hai đầu mới O(1); ở giữa là O(n).",
  "**Dùng `deque` khi cần truy cập theo chỉ số.** Đây là đánh đổi trung tâm — cần chỉ số thì dùng `list`.",
  "**Quên `deque` không có `sort()` và cắt lát.** Phải chuyển sang `list`, và đó là O(n) copy.",
  "**Dùng `extendleft` mà quên nó đảo thứ tự.** `extendleft([1,2])` cho `[2,1,...]`; truyền `reversed(...)` nếu muốn giữ thứ tự.",
  "**Tưởng `maxlen` đổi được sau khi tạo.** Nó bất biến; muốn đổi phải tạo `deque` mới.",
  "**Cần biết phần tử nào bị `maxlen` bỏ.** `deque` không báo; phải tự cài `popleft` trước khi `append`.",
  "**Dùng `if dq: dq.popleft()` khi có nhiều luồng.** Luồng khác có thể lấy mất giữa hai câu ⇒ `IndexError`; thử rồi bắt lỗi.",
  "**Dùng `deque` cho nhiều luồng cần chờ khi rỗng.** `deque` không có cơ chế chặn; dùng `queue.Queue`.",
  "**Dùng `list` làm hàng đợi vì \"đằng nào cũng có `pop`\".** `pop(0)` là O(n) ⇒ O(n²) tổng.",
  "**Dùng `deque` làm stack rồi ngạc nhiên vì không nhanh hơn `list`.** Với stack, `list` nhỉnh hơn và có nhiều tính năng hơn.",
  "**Tự viết ring buffer khi `deque(maxlen=k)` đã đủ.** Một dòng thay cho một class.",
  "**Quên rằng `rotate(k)` là O(k).** Với k gần n/2 thì nó thành O(n) — không phải luôn rẻ.",
  "**Không nhận ra kỹ thuật deque đơn điệu.** Bài max cửa sổ trượt có lời giải O(n) thay vì O(n·k)."
],

tu_kiem_tra: [
  "`deque` được cài bằng cấu trúc gì? Vẽ sơ đồ và nêu kích thước khối của CPython.",
  "Nêu ba quan sát rút ra từ cấu trúc khối, và hệ quả của mỗi cái.",
  "Vì sao `appendleft` là O(1) còn `list.insert(0)` là O(n)?",
  "Khi khối biên đầy, `deque` làm gì? Khác `list` thế nào?",
  "Vì sao `deque.append` là O(1) **thật** còn `list.append` chỉ O(1) **khấu hao**?",
  "Vì sao `dq[i]` ở giữa là O(n)? CPython tối ưu điều này thế nào?",
  "Đo `dq[i]` theo vị trí cho ra hình gì? Vì sao?",
  "Nêu bẫy phổ biến nhất khi dùng `deque` trong vòng lặp.",
  "`maxlen` giải quyết mấy vấn đề cùng lúc? Nêu hai hạn chế của nó.",
  "Thêm ở đầu nào thì phần tử ở đầu nào bị bỏ khi đầy?",
  "`rotate(k)` có bậc gì? Vì sao không phải O(n)?",
  "Nêu hai thứ `list` có mà `deque` không có.",
  "`extendleft` có hành vi gì dễ gây bất ngờ? Cách viết rõ ràng hơn?",
  "Thao tác nào của `deque` an toàn đa luồng, thao tác nào không?",
  "Vì sao `if dq: dq.popleft()` không an toàn? Cách đúng là gì?",
  "Với stack nên dùng `list` hay `deque`? Với queue thì sao? Vì sao khác nhau?",
  "Kỹ thuật deque đơn điệu trong bài max cửa sổ trượt hoạt động thế nào? Vì sao nó là O(n)?",
  "Nêu năm nơi khác mà mẫu thiết kế \"chia thành khối\" xuất hiện."
],

tai_nguyen: [
  "**Mã nguồn CPython — `Modules/_collectionsmodule.c`** — hiện thực `deque`; đọc phần định nghĩa `BLOCKLEN` và cấu trúc `block` để thấy đúng cơ chế mô tả trong bài.",
  "**Tài liệu Python — `collections.deque`** — danh sách đầy đủ các phương thức, `maxlen`, và ghi chú về độ phức tạp của truy cập ở giữa.",
  "**Tài liệu Python — `collections` mục *deque recipes*** — các mẫu dùng sẵn: cửa sổ trượt, `tail` của file, luân phiên.",
  "Sách **Introduction to Algorithms** — CLRS. Chương 10 về deque ở mức trừu tượng, và chương 18 về B-tree — cùng mẫu \"chia thành khối\".",
  "**Tài liệu Python — `itertools.islice`** — cách lấy một đoạn của `deque` mà không copy toàn bộ."
],

lien_he: [
  "**Array và dynamic array** trong module này: `deque` mượn cache locality của mảng, nhưng bỏ tính liên tục toàn cục.",
  "**Linked list** trong module này: `deque` là danh sách liên kết ở cấp **khối**, không phải cấp phần tử.",
  "**Stack** trong module này: `deque` làm được, nhưng `list` nhỉnh hơn cho vai trò này.",
  "**Queue** ngay trước: `deque` là lựa chọn **bắt buộc**; `list.pop(0)` là O(n).",
  "**Phân tích khấu hao** ở module 1 mảng này: `deque` cho O(1) **thật** nên không có đỉnh nhọn độ trễ.",
  "**Độ phức tạp không gian** ở module 1 mảng này: `maxlen` cho bộ nhớ O(k) cố định.",
  "**Bộ nhớ ảo, phân trang** ở module 2 mảng 01: cùng mẫu \"chia thành khối\" ở tầng hệ điều hành.",
  "**Deadlock và race condition** ở module 2 mảng 01: vì sao \"thử rồi bắt lỗi\" an toàn hơn \"kiểm tra rồi làm\".",
  "**Kỹ thuật con trỏ chậm/nhanh** ngay sau: bài cuối module, cùng chủ đề cửa sổ và hai con trỏ.",
  "**Đồ thị** ở module 5 mảng này: BFS dùng `deque` là chuẩn mực.",
  "**Kỹ thuật giải thuật cốt lõi** ở module 8 mảng này: deque đơn điệu là kỹ thuật cửa sổ trượt kinh điển.",
  "**Hiệu năng** ở mảng 04: B-tree của database dùng đúng mẫu chia khối để giảm số lần đọc đĩa.",
  "**Các khối xây dựng** ở mảng 06: sharding là mẫu chia khối ở quy mô hệ thống phân tán."
]

};
