window.LESSONS["01:command-line-terminal:viet-shell-script-co-ban-bien-if-for"] = {

tom_tat: [
  "**`set -euo pipefail` là dòng đầu tiên của mọi script nghiêm túc.** Không có nó, script chạy tiếp sau khi lỗi và gây hậu quả tệ hơn là dừng lại.",
  "**Luôn để biến trong ngoặc kép**: `\"$bien\"`. Thiếu ngoặc là tên file có dấu cách bị tách, và biến rỗng biến mất hoàn toàn.",
  "Shell **không có kiểu dữ liệu** — mọi thứ đều là chuỗi. So sánh chuỗi dùng `=`, so sánh số dùng `-eq`. Nhầm là sai âm thầm.",
  "`[[ ]]` an toàn hơn `[ ]` ở mọi mặt trong bash. Dùng `[[ ]]` mặc định.",
  "**Shell script hợp cho việc điều phối lệnh.** Quá 100 dòng hoặc cần cấu trúc dữ liệu thì chuyển sang Python."
],

can_biet_truoc: [
  "Toàn bộ module 3: điều hướng, thao tác file, pipe, grep/find/sed/awk, biến môi trường, quyền file.",
  "Bài **Tín hiệu và exit code** ở module 2 — script hoàn toàn dựa trên exit code."
],

dinh_nghia: "Nói đời thường: shell script là **danh sách các lệnh bạn vốn gõ tay, ghi lại thành file để chạy lại được**. Thêm chút điều kiện và vòng lặp để nó tự quyết định vài chuyện.\n\nNói chính xác: **shell script** là file văn bản chứa các lệnh shell, thực thi tuần tự bởi một interpreter được chỉ định ở dòng đầu. Nó có biến, điều kiện, vòng lặp, hàm — nhưng khác biệt cốt lõi so với ngôn ngữ lập trình thông thường là **mọi giá trị đều là chuỗi**, và **luồng điều khiển dựa trên exit code** chứ không phải giá trị boolean.",

vi_sao_can: "Shell script là chất keo gắn mọi thứ trong công việc dev:\n\n- **Tự động hoá việc lặp lại.** Deploy, backup, xử lý dữ liệu hàng loạt — làm tay thì sai sót, làm script thì lặp lại được và ghi lại được.\n- **Mọi CI/CD đều là shell script.** GitHub Actions, GitLab CI, Jenkins — phần `run:` chính là bash.\n- **Dockerfile và entrypoint.** Mỗi `RUN` là một lệnh shell, và `entrypoint.sh` là script.\n- **Đọc được script người khác viết.** Cài thư viện, dựng môi trường — bạn sẽ gặp hàng ngày.\n- **Lỗi trong script gây hậu quả thật.** Một script backup viết sai âm thầm không backup gì trong sáu tháng, và bạn chỉ phát hiện khi cần khôi phục.\n\nTình huống thật: script deploy của bạn có `cd /var/www/app` rồi `rm -rf *`. Nếu `cd` thất bại (thư mục không tồn tại), script **vẫn chạy tiếp** và `rm -rf *` xoá sạch thư mục hiện tại — có thể là thư mục nhà. Một dòng `set -e` ngăn được thảm hoạ đó.",

co_che: "### Cấu trúc một script\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\n# Nội dung script\necho \"Xin chào\"\n```\n\n**Dòng shebang `#!`** cho hệ điều hành biết dùng interpreter nào. `#!/usr/bin/env bash` tốt hơn `#!/bin/bash` vì nó tìm bash qua `PATH` — quan trọng trên macOS nơi bash mặc định rất cũ và bản mới nằm ở chỗ khác.\n\n```bash\nchmod +x script.sh    # cấp quyền chạy\n./script.sh           # chạy\nbash script.sh        # hoặc gọi qua bash, không cần quyền x\n```\n\n### `set -euo pipefail` — giải thích từng chữ\n\nĐây là dòng quan trọng nhất của bài.\n\n```bash\nset -e            # dừng NGAY khi một lệnh trả về khác 0\nset -u            # dùng biến chưa khai báo là lỗi\nset -o pipefail   # pipeline thất bại nếu BẤT KỲ lệnh nào trong đó lỗi\n\nset -euo pipefail # gộp cả ba\n```\n\n**`-e` cứu bạn khỏi hậu quả dây chuyền:**\n\n```bash\n# ❌ Không có set -e\ncd /thu-muc-khong-ton-tai    # thất bại, in lỗi\nrm -rf *                      # NHƯNG VẪN CHẠY — xoá thư mục hiện tại!\n\n# ✓ Có set -e\nset -e\ncd /thu-muc-khong-ton-tai    # thất bại → script DỪNG ngay tại đây\nrm -rf *                      # không bao giờ chạy tới\n```\n\n**`-u` bắt lỗi gõ sai tên biến:**\n\n```bash\nTHU_MUC=/var/www\nrm -rf $THU_MUCC/*    # gõ thừa một chữ C\n# Không có -u: biến rỗng → rm -rf /*  ← THẢM HOẠ\n# Có -u:      script chết ngay với thông báo \"unbound variable\" ✓\n```\n\n**`-o pipefail` bắt lỗi giữa pipeline:**\n\n```bash\nchay_test | tee ket-qua.txt\n# Không có pipefail: exit code là của tee (thành công) → CI báo xanh dù test đỏ\n# Có pipefail:      exit code là của chay_test → CI báo đỏ đúng ✓\n```\n\n**Khi `-e` gây phiền:** đôi khi bạn *muốn* một lệnh thất bại mà không dừng script.\n\n```bash\nset -e\n\n# Cách 1: || true\ngrep 'mau' file.txt || true      # grep không tìm thấy trả về 1 — bỏ qua\n\n# Cách 2: đưa vào if\nif grep -q 'mau' file.txt; then\n    echo \"tìm thấy\"\nelse\n    echo \"không thấy\"           # ✓ set -e không kích hoạt trong điều kiện if\nfi\n\n# Cách 3: lưu exit code lại\nset +e\nlenh_co_the_loi\nma=$?\nset -e\n[[ $ma -eq 0 ]] || echo \"lệnh thất bại với mã $ma\"\n```\n\n### Biến — mọi thứ đều là chuỗi\n\n```bash\nten=\"Minh\"              # KHÔNG có dấu cách quanh dấu =\nso=42\nduong_dan=\"/var/log\"\n\necho \"$ten\"             # Minh\necho \"${ten}\"           # dạng đầy đủ, cần khi ghép: \"${ten}_hau_to\"\n\nket_qua=$(ls | wc -l)   # ⭐ lưu kết quả lệnh vào biến\nhom_nay=$(date +%Y-%m-%d)\n```\n\n**Không được có dấu cách quanh `=`.** `ten = \"Minh\"` bị hiểu là chạy lệnh tên `ten` với hai đối số — một trong những lỗi cú pháp gây bối rối nhất cho người mới.\n\n### Ngoặc kép: quy tắc quan trọng nhất\n\n```bash\nfile=\"bao cao.txt\"\n\nrm $file        # ❌ shell tách thành hai đối số: \"bao\" và \"cao.txt\"\nrm \"$file\"      # ✓ một đối số duy nhất\n```\n\nQuy tắc: **luôn để biến trong ngoặc kép**, trừ khi bạn cố ý muốn tách từ.\n\nHai lý do:\n\n1. **Tên có dấu cách** bị tách thành nhiều đối số\n2. **Biến rỗng biến mất hoàn toàn**, làm lệnh thiếu đối số\n\n```bash\nbien=\"\"\n[ $bien = \"x\" ]      # ❌ bung thành: [ = \"x\" ]  → lỗi cú pháp\n[ \"$bien\" = \"x\" ]    # ✓ bung thành: [ \"\" = \"x\" ] → hoạt động đúng\n```\n\n### Giá trị mặc định và kiểm tra bắt buộc\n\n```bash\n${bien:-mac_dinh}     # dùng \"mac_dinh\" nếu bien rỗng hoặc chưa gán\n${bien:=mac_dinh}     # như trên, VÀ gán luôn giá trị đó cho bien\n${bien:?thông báo}    # ⭐ DỪNG script với thông báo nếu bien rỗng\n${bien:+thay_the}     # dùng \"thay_the\" nếu bien CÓ giá trị\n\nPORT=\"${PORT:-8000}\"                      # mặc định hợp lý\nAPI_KEY=\"${API_KEY:?Cần đặt biến API_KEY}\" # bắt buộc phải có\n```\n\nCú pháp `${bien:?}` là lớp phòng thủ rất đáng dùng cho các lệnh phá huỷ:\n\n```bash\nrm -rf \"${THU_MUC:?Chưa đặt THU_MUC}\"/*\n```\n\n### Điều kiện — dựa trên exit code\n\n```bash\nif lenh; then\n    echo \"lệnh thành công (exit code 0)\"\nelse\n    echo \"lệnh thất bại\"\nfi\n```\n\n`if` **không kiểm tra true/false** mà kiểm tra **exit code**: 0 là thành công. Đây là hệ quả trực tiếp của quy ước exit code đã học ở module 2.\n\n**Dùng `[[ ]]` chứ không phải `[ ]`:**\n\n```bash\n# So sánh chuỗi\n[[ \"$a\" == \"$b\" ]]           # bằng\n[[ \"$a\" != \"$b\" ]]           # khác\n[[ -z \"$a\" ]]                # rỗng\n[[ -n \"$a\" ]]                # không rỗng\n[[ \"$a\" == bao* ]]           # khớp mẫu — chỉ [[ ]] làm được\n[[ \"$a\" =~ ^[0-9]+$ ]]       # khớp regex — chỉ [[ ]] làm được\n\n# So sánh SỐ — phải dùng ký hiệu khác\n[[ $a -eq $b ]]              # bằng\n[[ $a -lt $b ]]              # nhỏ hơn\n[[ $a -gt $b ]]              # lớn hơn\n\n# Kiểm tra file\n[[ -f \"$f\" ]]                # là file thường và tồn tại\n[[ -d \"$d\" ]]                # là thư mục\n[[ -e \"$p\" ]]                # tồn tại (bất kể loại gì)\n[[ -r \"$f\" ]]                # đọc được\n[[ -x \"$f\" ]]                # chạy được\n[[ -s \"$f\" ]]                # tồn tại VÀ không rỗng\n\n# Kết hợp\n[[ -f \"$f\" && -r \"$f\" ]]     # && và || dùng được trong [[ ]]\n```\n\n**Cạm bẫy so sánh số và chuỗi:**\n\n```bash\n[[ \"10\" == \"9\" ]]      # sai — so sánh CHUỖI\n[[ \"10\" > \"9\" ]]       # sai! so chuỗi: \"1\" < \"9\" theo bảng mã\n[[ 10 -gt 9 ]]          # ✓ đúng — so sánh SỐ\n```\n\nDòng giữa là bẫy nguy hiểm vì nó **không báo lỗi**, chỉ cho kết quả sai. Version `10` bị coi là nhỏ hơn version `9`.\n\n**Vì sao `[[ ]]` hơn `[ ]`:** `[` thực ra là một **lệnh** (có file `/usr/bin/[`), nên nó tuân theo quy tắc tách từ của shell — biến rỗng gây lỗi cú pháp. `[[ ]]` là **cú pháp của bash**, xử lý an toàn hơn, và hỗ trợ khớp mẫu lẫn regex.\n\nNgoại lệ: script cần chạy trên `sh` thuần (Alpine, busybox) thì phải dùng `[ ]` và đặt `#!/bin/sh`.\n\n### Vòng lặp\n\n```bash\n# Lặp qua danh sách\nfor f in *.txt; do\n    echo \"Xử lý: $f\"\ndone\n\n# Lặp qua dãy số\nfor i in {1..5}; do echo \"$i\"; done\nfor i in $(seq 1 5); do echo \"$i\"; done\n\n# Lặp qua đối số của script\nfor arg in \"$@\"; do echo \"$arg\"; done\n\n# ⭐ Đọc file theo dòng — cách ĐÚNG\nwhile IFS= read -r dong; do\n    echo \"Dòng: $dong\"\ndone < file.txt\n```\n\nDòng cuối cần giải thích vì nó có ba chi tiết đều quan trọng:\n\n- **`IFS=`** — tắt việc cắt khoảng trắng đầu và cuối dòng\n- **`-r`** — không diễn giải dấu `\\` làm ký tự thoát\n- **`< file.txt`** — chuyển hướng vào vòng lặp\n\nThiếu một trong ba là dữ liệu bị biến dạng âm thầm.\n\n**Bẫy: glob không khớp gì.**\n\n```bash\nfor f in *.txt; do echo \"$f\"; done\n# Nếu KHÔNG có file .txt nào, biến f nhận giá trị \"*.txt\" nguyên văn\n\n# ✓ Cách phòng\nshopt -s nullglob     # glob không khớp thì cho danh sách RỖNG\nfor f in *.txt; do echo \"$f\"; done   # không lặp lần nào\n```\n\n### Hàm\n\n```bash\nchao() {\n    local ten=\"$1\"          # local: biến chỉ tồn tại trong hàm\n    echo \"Xin chào, $ten\"\n}\n\nchao \"Minh\"                 # gọi hàm — KHÔNG có dấu ngoặc\n\n# Trả về giá trị: dùng echo rồi bắt bằng $()\ntinh_tong() {\n    echo $(( $1 + $2 ))\n}\nkq=$(tinh_tong 3 4)         # 7\n\n# return chỉ trả EXIT CODE (0-255), không phải giá trị\nkiem_tra() {\n    [[ -f \"$1\" ]] && return 0 || return 1\n}\nif kiem_tra \"file.txt\"; then echo \"có\"; fi\n```\n\nHai điểm khác biệt so với ngôn ngữ thông thường:\n\n- **`return` trả exit code, không trả giá trị.** Muốn trả dữ liệu thì `echo` và bắt bằng `$()`.\n- **`local` rất quan trọng.** Không có nó, biến trong hàm là biến toàn cục và ghi đè biến bên ngoài.\n\n### Đối số dòng lệnh\n\n```bash\n$0        # tên script\n$1 $2 $3  # đối số thứ nhất, hai, ba\n$#        # số lượng đối số\n\"$@\"      # ⭐ TẤT CẢ đối số, mỗi cái là một từ riêng\n\"$*\"      # tất cả đối số gộp thành MỘT chuỗi\n```\n\n**Luôn dùng `\"$@\"` chứ không phải `$@` hay `\"$*\"`.** Chỉ `\"$@\"` giữ đúng ranh giới đối số khi có dấu cách.\n\n```bash\n# Kiểm tra số đối số\nif [[ $# -lt 2 ]]; then\n    echo \"Cách dùng: $0 <nguồn> <đích>\" >&2\n    exit 1\nfi\nNGUON=\"$1\"\nDICH=\"$2\"\n```\n\nGhi hướng dẫn ra **stderr** (`>&2`) và thoát với mã **khác 0** — đó là quy ước đúng cho lỗi sử dụng.\n\n### `trap` — dọn dẹp dù kết thúc kiểu gì\n\n```bash\nTAM=$(mktemp -d)                    # tạo thư mục tạm an toàn\ntrap 'rm -rf \"$TAM\"' EXIT           # ⭐ chạy khi script kết thúc, BẤT KỂ lý do\n\n# ... làm việc với $TAM ...\n# Dù script thành công, lỗi, hay bị Ctrl+C — thư mục tạm đều được xoá\n```\n\n`trap ... EXIT` là `try/finally` của shell. Đây là cách đúng để đảm bảo dọn dẹp, thay vì đặt lệnh `rm` ở cuối script (sẽ không chạy nếu script chết giữa chừng).\n\n```bash\ntrap 'echo \"Bị ngắt\"; exit 130' INT TERM    # bắt Ctrl+C và SIGTERM\n```",

vi_du: "### Khung script chuẩn\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\n# --- Cấu hình ---\nreadonly THU_MUC_SCRIPT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\nreadonly LOG=\"${THU_MUC_SCRIPT}/chay.log\"\n\n# --- Dọn dẹp ---\nTAM=$(mktemp -d)\ntrap 'rm -rf \"$TAM\"' EXIT\n\n# --- Hàm tiện ích ---\nghi_log() {\n    echo \"[$(date +'%Y-%m-%d %H:%M:%S')] $*\" | tee -a \"$LOG\"\n}\n\nloi() {\n    echo \"[LỖI] $*\" >&2\n    exit 1\n}\n\n# --- Kiểm tra đầu vào ---\nif [[ $# -lt 1 ]]; then\n    echo \"Cách dùng: $0 <thư-mục-nguồn>\" >&2\n    exit 1\nfi\n\nNGUON=\"$1\"\n[[ -d \"$NGUON\" ]] || loi \"Không phải thư mục: $NGUON\"\n\n# --- Việc chính ---\nghi_log \"Bắt đầu xử lý $NGUON\"\n\nso_file=0\nshopt -s nullglob\nfor f in \"$NGUON\"/*.txt; do\n    ghi_log \"Xử lý $(basename \"$f\")\"\n    ((so_file++))\ndone\n\nghi_log \"Xong — đã xử lý $so_file file\"\n```\n\nDòng `THU_MUC_SCRIPT` đáng chú ý: nó tìm ra **thư mục chứa chính script này**, bất kể bạn chạy script từ đâu. Đây là mẫu chuẩn để script không phụ thuộc working directory — cùng vấn đề đã gặp ở bài điều hướng.\n\n### Script backup có kiểm tra\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\nreadonly NGUON=\"${1:?Cách dùng: $0 <nguồn> <đích>}\"\nreadonly DICH=\"${2:?Cách dùng: $0 <nguồn> <đích>}\"\nreadonly NGAY=$(date +%Y%m%d-%H%M%S)\nreadonly FILE_NEN=\"${DICH}/backup-${NGAY}.tar.gz\"\n\n[[ -d \"$NGUON\" ]] || { echo \"Nguồn không tồn tại: $NGUON\" >&2; exit 1; }\nmkdir -p \"$DICH\"\n\necho \"Đang nén $NGUON …\"\ntar -czf \"$FILE_NEN\" -C \"$(dirname \"$NGUON\")\" \"$(basename \"$NGUON\")\"\n\n# ⭐ KIỂM TRA file nén có đọc được không — bước hay bị bỏ qua nhất\nif ! tar -tzf \"$FILE_NEN\" > /dev/null 2>&1; then\n    rm -f \"$FILE_NEN\"\n    echo \"File nén hỏng, đã xoá\" >&2\n    exit 1\nfi\n\nkich_thuoc=$(du -h \"$FILE_NEN\" | cut -f1)\necho \"Xong: $FILE_NEN ($kich_thuoc)\"\n\n# Xoá backup cũ hơn 30 ngày\nfind \"$DICH\" -name 'backup-*.tar.gz' -mtime +30 -delete\necho \"Đã dọn backup cũ hơn 30 ngày\"\n```\n\nBước kiểm tra file nén là điểm quan trọng nhất: **một backup không khôi phục được thì không phải backup**. Rất nhiều script backup chạy êm suốt nhiều tháng rồi mới lộ ra là file hỏng khi cần dùng.\n\n### Đọc file theo dòng cho đúng\n\n```bash\n# ❌ SAI — for tách theo khoảng trắng, không theo dòng\nfor dong in $(cat file.txt); do\n    echo \"$dong\"        # dòng \"Nguyễn Văn A\" bị tách thành ba\ndone\n\n# ✓ ĐÚNG\nwhile IFS= read -r dong; do\n    echo \"$dong\"\ndone < file.txt\n\n# ✓ Đọc CSV, tách theo dấu phẩy\nwhile IFS=, read -r ma ten so_tien; do\n    echo \"$ma → $ten: $so_tien\"\ndone < don-hang.csv\n```\n\nDòng cuối cho thấy `IFS` mạnh thế nào: đặt `IFS=,` là `read` tự tách cột theo dấu phẩy và gán vào từng biến.\n\n### Xử lý đối số có tuỳ chọn\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n\nCHI_TIET=0\nDAU_RA=\"\"\n\nwhile [[ $# -gt 0 ]]; do\n    case \"$1\" in\n        -v|--verbose)  CHI_TIET=1; shift ;;\n        -o|--output)   DAU_RA=\"$2\"; shift 2 ;;\n        -h|--help)     echo \"Cách dùng: $0 [-v] [-o file] <đầu vào>\"; exit 0 ;;\n        -*)            echo \"Tuỳ chọn lạ: $1\" >&2; exit 1 ;;\n        *)             break ;;\n    esac\ndone\n\nDAU_VAO=\"${1:?Thiếu tham số đầu vào}\"\n\n[[ $CHI_TIET -eq 1 ]] && echo \"Chế độ chi tiết đang bật\"\necho \"Đầu vào: $DAU_VAO, đầu ra: ${DAU_RA:-stdout}\"\n```\n\n`shift` đẩy danh sách đối số sang trái. `shift 2` dùng cho tuỳ chọn có giá trị đi kèm.\n\n### Chạy song song\n\n```bash\n# Tuần tự — chậm\nfor url in $(cat danh-sach.txt); do\n    curl -sO \"$url\"\ndone\n\n# Song song 4 tiến trình — nhanh hơn nhiều với việc chờ mạng\ncat danh-sach.txt | xargs -P 4 -I {} curl -sO {}\n\n# Hoặc bằng bash thuần\nfor url in $(cat danh-sach.txt); do\n    curl -sO \"$url\" &          # & đẩy xuống chạy nền\ndone\nwait                            # chờ tất cả xong\n```\n\nCách dùng `xargs -P` gọn hơn và kiểm soát được số lượng song song. Cách dùng `&` không giới hạn số tiến trình — với 1.000 URL là tạo 1.000 process cùng lúc.\n\n### Kiểm tra script bằng ShellCheck\n\n```bash\nshellcheck script.sh\n```\n\n```\nIn script.sh line 5:\nrm -rf $THU_MUC/*\n       ^-------^ SC2086: Double quote to prevent globbing and word splitting.\n```\n\n**ShellCheck là công cụ đáng cài nhất cho người viết shell script.** Nó bắt được gần như mọi lỗi phổ biến trong bài này: thiếu ngoặc kép, so sánh sai kiểu, biến chưa khai báo, dùng `[ ]` không an toàn.\n\nChạy nó trên mọi script trước khi đưa vào dùng — mất năm giây và bắt được lỗi mà mắt người bỏ sót.",

so_sanh: "### Khi nào shell, khi nào Python\n\n| | Shell script | Python |\n|---|---|---|\n| Gọi lệnh hệ thống | **Tự nhiên, ngắn gọn** | Cồng kềnh (`subprocess`) |\n| Xử lý chuỗi | Hạn chế | **Mạnh** |\n| Cấu trúc dữ liệu | Chỉ mảng và mảng kết hợp | **Đầy đủ** |\n| Xử lý lỗi | Thô sơ | **try/except** |\n| Số học | Vụng về | **Tự nhiên** |\n| Test được | Khó | **Dễ** |\n| Có sẵn trên server | **Luôn có** | Thường có |\n| Độ dài hợp lý | **Dưới ~100 dòng** | Không giới hạn |\n\n**Dùng shell khi**: điều phối các lệnh có sẵn, dựng môi trường, deploy, thao tác file hàng loạt, entrypoint cho container.\n\n**Chuyển sang Python khi**: cần cấu trúc dữ liệu, xử lý JSON hoặc CSV có quy tắc, tính toán phức tạp, cần bắt lỗi tinh vi, script vượt 100 dòng, hoặc code sẽ được nhiều người bảo trì.\n\nDấu hiệu rõ nhất cần chuyển: **bạn bắt đầu dùng mảng kết hợp và hàm lồng nhau trong bash**. Lúc đó Python đã dễ hơn.\n\n### `[ ]`, `[[ ]]` và `(( ))`\n\n| | `[ ]` | `[[ ]]` | `(( ))` |\n|---|---|---|---|\n| Bản chất | **Một lệnh** | Cú pháp bash | Cú pháp bash |\n| Cần ngoặc kép quanh biến | **Bắt buộc** | Không bắt buộc | Không |\n| Khớp mẫu `==` | ✗ | **✓** | ✗ |\n| Regex `=~` | ✗ | **✓** | ✗ |\n| `&&` `\\|\\|` bên trong | ✗ dùng `-a` `-o` | **✓** | ✓ |\n| Số học | `-eq -lt` | `-eq -lt` | **`< > ==` tự nhiên** |\n| Chạy trên `sh` thuần | **✓** | ✗ | ✗ |\n\n```bash\n[[ \"$ten\" == \"minh\" ]]        # so chuỗi\n[[ \"$ten\" =~ ^m ]]            # regex\n(( so > 10 ))                 # so sánh số, cú pháp tự nhiên\n(( tong += 5 ))               # phép gán số học\n```\n\nQuy tắc: **`[[ ]]` cho chuỗi và file, `(( ))` cho số**. Chỉ dùng `[ ]` khi bắt buộc chạy trên `sh` thuần — ví dụ trong container Alpine.\n\n### Bảng lỗi thường gặp nhất\n\nĐây là bốn lỗi chiếm phần lớn bug trong shell script:\n\n| Lỗi | Hậu quả | Cách phòng |\n|---|---|---|\n| Thiếu ngoặc kép quanh biến | Tên có dấu cách bị tách; biến rỗng biến mất | Luôn `\"$bien\"` |\n| Không có `set -e` | Script chạy tiếp sau lỗi, gây hậu quả dây chuyền | Luôn `set -euo pipefail` |\n| So sánh số bằng `==` | Kết quả sai âm thầm, không báo lỗi | Dùng `-eq` hoặc `(( ))` |\n| `for` đọc file theo dòng | Dòng bị tách theo khoảng trắng | `while IFS= read -r` |\n\nBa trong bốn lỗi này **không gây thông báo lỗi** — chúng chỉ cho kết quả sai. Đó là lý do ShellCheck có giá trị: nó bắt được cả loại lỗi im lặng này.",

loi_thuong_gap: [
  "**Quên `set -euo pipefail`.** Script chạy tiếp sau lỗi và gây hậu quả tệ hơn nhiều so với dừng lại.",
  "**Quên ngoặc kép quanh biến.** Tên file có dấu cách bị tách; biến rỗng biến mất làm lệnh thiếu đối số.",
  "**Có dấu cách quanh dấu `=` khi gán.** `ten = \"x\"` bị hiểu là chạy lệnh `ten`. Phải viết `ten=\"x\"`.",
  "**So sánh số bằng `==` hoặc `>`.** Đó là so sánh chuỗi — `\"10\" > \"9\"` cho kết quả sai mà không báo lỗi. Dùng `-gt` hoặc `(( ))`.",
  "**Dùng `for dong in $(cat file)`.** Nó tách theo khoảng trắng chứ không theo dòng. Dùng `while IFS= read -r dong; do ... done < file`.",
  "**Quên `local` trong hàm.** Biến trở thành toàn cục và ghi đè biến bên ngoài, gây bug rất khó tìm.",
  "**Dùng `$@` thay vì `\"$@\"`.** Chỉ dạng có ngoặc kép mới giữ đúng ranh giới đối số.",
  "**Dọn dẹp ở cuối script thay vì dùng `trap`.** Script chết giữa chừng là file tạm nằm lại mãi.",
  "**Không chạy ShellCheck.** Nó bắt được gần hết các lỗi trên trong năm giây.",
  "**Viết script bash 500 dòng.** Quá ngưỡng đó thì Python dễ đọc, dễ test và dễ sửa hơn nhiều."
],

tu_kiem_tra: [
  "Giải thích từng chữ trong `set -euo pipefail` và cho mỗi chữ một ví dụ về thảm hoạ nó ngăn được.",
  "Vì sao `rm $file` nguy hiểm còn `rm \"$file\"` thì không? Nêu hai lý do.",
  "`[[ \"10\" > \"9\" ]]` cho kết quả gì và vì sao? Viết lại cho đúng.",
  "Vì sao `for dong in $(cat file.txt)` là cách sai để đọc file theo dòng? Viết cách đúng và giải thích ba thành phần của nó.",
  "`${THU_MUC:?}` làm gì, và vì sao nó quan trọng với lệnh `rm -rf`?",
  "Trong hàm bash, `return 5` trả về cái gì? Muốn trả về một chuỗi thì làm thế nào?",
  "Vì sao nên dùng `trap 'rm -rf $TAM' EXIT` thay vì đặt `rm -rf $TAM` ở cuối script?",
  "Nêu ba dấu hiệu cho thấy nên chuyển script từ bash sang Python."
],

tai_nguyen: [
  "**ShellCheck** (shellcheck.net) — chạy trực tuyến hoặc cài local. Công cụ quan trọng nhất cho người viết shell script.",
  "**Google Shell Style Guide** — quy ước viết script rõ ràng, kèm giải thích lý do từng quy tắc.",
  "Sách **The Linux Command Line** — William Shotts, phần IV về viết script. Đọc miễn phí.",
  "Tài liệu bash: `man bash`, đặc biệt mục *Parameter Expansion* và *Conditional Expressions*.",
  "Khoá **The Missing Semester** (MIT), bài 2 — phần scripting rất thực dụng."
],

lien_he: [
  "**Toàn bộ module 3**: script là nơi ghép mọi thứ đã học — điều hướng, thao tác file, pipe, grep/sed/awk, biến môi trường, quyền.",
  "**Tín hiệu và exit code** ở module 2: `if` dựa trên exit code, `trap` bắt tín hiệu, `exit 1` báo lỗi cho CI.",
  "**Pipe và redirect**: `set -o pipefail` giải quyết đúng vấn đề exit code của pipeline đã nêu ở bài đó.",
  "**Biến môi trường**: script nên đặt `PATH` tường minh vì shell không tương tác không đọc `.bashrc`.",
  "**Clean Code** ở module 8: hàm nhỏ, tên có nghĩa, xử lý lỗi tường minh — áp dụng cho shell script y như cho Python.",
  "**Triển khai & CI/CD** ở mảng 05: mọi pipeline đều là shell script. Thiếu `set -euo pipefail` là nguyên nhân kinh điển của việc CI báo xanh dù có lỗi.",
  "**Docker** ở mảng 05: `entrypoint.sh` là shell script, và nó cần xử lý SIGTERM đúng cách để container dừng êm ái."
]

};
