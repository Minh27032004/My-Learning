window.LESSONS["05:kien-truc-chat-luong:phan-tang-router-service-repository"] = {

tom_tat: [
  "Router là biên HTTP: đọc đường dẫn, xác thực đầu vào, lấy danh tính người gọi và chuyển kết quả miền thành response; nó không quyết định còn vé hay không.",
  "Service giữ use case và invariant nghiệp vụ, chẳng hạn mỗi email chỉ được giữ một chỗ và số vé không được âm; vì vậy cùng logic có thể được gọi từ HTTP, job hoặc CLI.",
  "Repository che cách lưu và truy vấn booking, nhưng không nên biến thành lớp bọc mọi câu SQL một cách máy móc.",
  "Ranh giới tốt làm dependency hướng vào trong, giúp test từng quyết định và thay hạ tầng mà không làm rò chi tiết HTTP hay ORM.",
  "Phân tầng không tự chữa race condition, phân quyền sai hay transaction vỡ; những bảo đảm ấy phải được thiết kế rõ tại đúng tầng."
],

can_biet_truoc: [
  "HTTP method, status code, header và JSON body.",
  "Python async/await, type hint và ngoại lệ.",
  "FastAPI router, Pydantic model và dependency cơ bản.",
  "Khái niệm transaction, unique constraint và race condition."
],

dinh_nghia: "**Phân tầng router → service → repository** là cách chia backend theo ba loại trách nhiệm. Router chuyển giao thức HTTP thành lời gọi use case. Service điều phối quy tắc nghiệp vụ và ranh giới transaction. Repository cung cấp các thao tác lưu/đọc theo ngôn ngữ miền, đồng thời cô lập SQL, ORM hoặc dịch vụ lưu trữ. Mũi tên biểu thị chiều gọi ở runtime; chiều phụ thuộc kiểu nên hướng từ lớp ngoài vào contract của lớp trong. Đây là phép chia trách nhiệm, không phải yêu cầu mỗi request bắt buộc đi qua đúng ba class.",

vi_sao_can: "Một endpoint đặt vé ban đầu rất dễ viết trong một hàm: parse JSON, kiểm tra số vé, chạy hai câu SQL, gửi email và trả JSON. Đến khi có thêm tác vụ nhập booking từ quầy, nhà phát triển thường sao chép phần kiểm tra. Một nhánh quên unique constraint, một nhánh trả lỗi khác, test phải dựng cả database và HTTP dù chỉ muốn kiểm quy tắc “mỗi email một vé”. Vấn đề không phải hàm dài về mặt thẩm mỹ; vấn đề là **một thay đổi nghiệp vụ có nhiều nơi để quên**.\n\nPhân tầng tạo chỗ cư trú rõ cho từng quyết định. Khi HTTP đổi từ REST sang message consumer, use case vẫn sống. Khi PostgreSQL thay SQLite, router không biết. Khi cần kiểm quyền, lớp biên xác minh token nhưng service vẫn nhận actor đã định kiểu và tự bảo vệ hành động nhạy cảm. Giá phải trả là thêm tên, mapping và đường đi khi debug. Với CRUD nội bộ chỉ vài endpoint, tách ba tầng cứng nhắc có thể làm code dài hơn mà không giảm rủi ro.",

co_che: "### Theo dấu một request thật\n\nKhách gửi `POST /workshops/ws-101/bookings` cùng email và `Idempotency-Key`. Reverse proxy giới hạn kích thước body trước. FastAPI parse JSON thành `CreateBookingRequest`; router lấy `Actor` từ credential đã xác minh rồi gọi `BookingService.reserve`. Service không nhận `Request`, `Depends` hay `HTTPException`, vì ba thứ ấy mang nghĩa HTTP. Nó nhận dữ liệu miền: workshop_id, email, actor và khóa chống gửi lặp. Repository mở transaction, khóa suất workshop, tìm booking trùng, ghi booking rồi commit. Router cuối cùng biến `Booking` thành DTO công khai và trả 201.\n\nLuồng có vẻ dài nhưng mỗi chặng trả lời một câu hỏi khác: “request có đúng cú pháp?”, “người này được phép làm gì?”, “quy tắc đặt chỗ là gì?”, “ghi nguyên tử bằng cơ chế nào?”. Trộn chúng làm lỗi khó phân loại và buộc test nghiệp vụ phụ thuộc framework.\n\n### Router là bộ chuyển đổi giao thức\n\nRouter nên biết path parameter, query, cookie, header, content type, status code và response model. Nó có thể chuẩn hóa email ở DTO nếu contract công khai quy định vậy, nhưng không nên tự trừ `remaining_seats`. Router cũng là nơi hợp lý để áp giới hạn đầu vào và gọi dependency xác thực. Tuy nhiên xác thực danh tính khác với phân quyền nghiệp vụ: token hợp lệ không chứng minh khách được sửa workshop của người khác.\n\nMột router mỏng không có nghĩa một dòng. Nó vẫn phải diễn đạt contract HTTP rõ ràng, kể cả 201 với `Location`, 409 khi xung đột và 422 khi schema sai. “Mỏng” nghĩa là không chứa quyết định phải dùng lại ngoài HTTP.\n\n### Service sở hữu use case\n\nService kể câu chuyện nghiệp vụ bằng động từ: `reserve`, `cancel`, `publish`, không phải `insert_row`. Nó kiểm tra workshop còn mở, actor được đặt cho email nào, và booking lặp phải trả kết quả cũ hay báo xung đột. Service điều phối nhiều cổng: repository, clock, mail outbox. Việc gom invariant ở đây khiến command từ quản trị viên và endpoint công khai có cùng hành vi.\n\nKhông nên nhét mọi helper vào service. Hàm chuẩn hóa chuỗi thuần có thể là value object; phép tính giá có thể là domain function. Service là ranh giới use case chứ không phải “thùng chứa code không biết đặt đâu”. Nếu nó phình thành nghìn dòng, hãy tách theo khả năng nghiệp vụ, không tách tùy tiện thành `ServiceHelper`.\n\n### Repository nói ngôn ngữ miền\n\nContract tốt là `get_open_workshop_for_update(id)` và `save_booking(booking)`, vì nó bộc lộ ý định và yêu cầu khóa. Contract yếu là `execute(sql)` hoặc `find(table, filters)`: service vẫn biết schema, toán tử ORM và cách khóa, nên lớp bọc không tách được gì. Repository có thể trả entity miền, projection hoặc `None`; quy ước phải rõ để phân biệt không tìm thấy với lỗi kết nối.\n\nRepository không xóa thực tế của database. Unique constraint, isolation level, pagination ổn định và query plan vẫn cần được thiết kế. Cô lập hạ tầng nhằm đặt kiến thức ấy vào một nơi, không nhằm giả vờ mọi kho dữ liệu giống nhau.\n\n### Transaction phải bao quanh invariant\n\nHai request cùng đọc `remaining_seats = 1`, rồi cùng trừ xuống 0 nếu chỉ “đọc rồi ghi” ngoài transaction. Tách tầng không ngăn được điều này. Ranh giới transaction cần bao cả kiểm tra và cập nhật, thường do service yêu cầu một unit of work hoặc gọi operation nguyên tử của repository. Database phải là hàng rào cuối bằng lock, conditional update hay constraint.\n\nGửi email bên trong transaction cũng nguy hiểm: mail đã đi nhưng commit thất bại, hoặc giữ lock trong lúc mạng chậm. Outbox ghi sự kiện cùng transaction rồi worker gửi sau giải quyết cửa sổ đó. Đổi lại hệ thống chấp nhận giao hàng bất đồng bộ và phải xử lý gửi lặp.\n\n### Chiều phụ thuộc và khả năng thay thế\n\nRouter import service contract; service import repository protocol; implementation PostgreSQL import contract ấy. Service không import module FastAPI hoặc SQLAlchemy. Quy tắc này giữ phần quyết định ổn định ở trung tâm, còn chi tiết hay đổi nằm ngoài. Composition root lắp implementation khi ứng dụng khởi động.\n\nĐừng hiểu “thay database dễ dàng” là có thể đổi một dòng. SQL dialect, transaction và tính năng riêng vẫn tạo chi phí. Lợi ích thực tế hơn là test service bằng fake nhỏ, giữ query ở một vùng và review ranh giới bảo mật dễ hơn.\n\n### Dữ liệu đi qua ranh giới\n\nKhông truyền thẳng ORM object ra router. Lazy loading có thể phát query sau khi session đóng; thuộc tính nội bộ như `cost_price` có thể bị serialize. Service trả một entity hoặc result được định nghĩa ở lớp miền, router map sang response DTO. Ở chiều vào, DTO HTTP không nên len tới repository vì field tùy chọn theo PATCH không đồng nghĩa cột nullable.\n\nMapping là code có chủ đích. Nó trông lặp nhưng đóng vai trò danh sách cho phép: chỉ field được chọn mới qua biên. Với payload lớn, có thể dùng projection để tránh nạp cả entity; tối ưu ấy vẫn giữ contract rõ.\n\n### Lỗi đi ngược lên như thế nào\n\nRepository chuyển lỗi hạ tầng có ý nghĩa thành lỗi cổng, chẳng hạn unique violation thành `DuplicateBooking`; nó không nên nuốt timeout rồi trả `None`. Service ném lỗi miền như `WorkshopFull` hoặc `ForbiddenAction`. Adapter HTTP ánh xạ chúng sang status và error body tập trung. Nhờ vậy service không biết 409, còn router không cần đọc chuỗi exception.\n\nLỗi bất ngờ phải giữ traceback, có correlation id trong log và trả thông báo vô hại. Không gửi SQL, stack trace hay email người khác cho client. Log cũng cần che token và dữ liệu cá nhân; “để debug” không phải lý do ghi toàn bộ body.\n\n### Bảo mật không phải một tầng riêng\n\nMỗi biên bảo vệ một loại rủi ro. Proxy và router chặn body quá lớn, schema lạ và credential thiếu. Service kiểm object-level authorization: actor chỉ đặt cho chính mình trừ khi có quyền hỗ trợ. Repository dùng parameter binding và tài khoản database tối thiểu quyền. Response DTO chống lộ cột. Audit event ghi ai làm gì, nhưng không chứa secret.\n\nNếu chỉ đặt `require_user` ở router, một job nội bộ gọi thẳng service có thể bỏ qua quyền. Nếu service nhận `is_admin: bool` do client gửi, kẻ tấn công tự cấp quyền. Actor phải đến từ ngữ cảnh đã xác minh và service quyết định dựa trên policy đáng tin.\n\n### Quan sát và kiểm thử theo đường biên\n\nUnit test service dùng fake repository để phủ hết workshop đầy, booking trùng, email không thuộc actor và lỗi lưu. Integration test repository chạy trên đúng loại database production để kiểm lock và constraint; SQLite không chứng minh PostgreSQL isolation đúng. API test kiểm status, schema và header. Một ít end-to-end test nối toàn chuỗi.\n\nTrace span nên bám use case và query, không ghi dữ liệu nhạy cảm. Metric hữu ích gồm tỷ lệ 409 vì hết vé, latency repository và số rollback. Đếm 500 chung chung chưa chỉ ra tầng nào hỏng.\n\n### Khi không nên dựng đủ ba lớp\n\nPrototype một endpoint, webhook chỉ xác minh chữ ký rồi nối vào queue, hoặc CRUD quản trị không có invariant có thể dùng router gọi gateway trực tiếp. Mỗi abstraction cần một lý do thay đổi hoặc một ranh giới test. Tạo interface cho class chỉ có một implementation vẫn hợp lý nếu nó đảo chiều phụ thuộc quan trọng; làm vậy cho mọi helper là nghi lễ.\n\nKhi latency cực nhạy, mapping và object allocation có chi phí, nhưng thường I/O lớn hơn nhiều. Hãy profile trước. Có thể gộp file mà vẫn giữ ranh giới khái niệm; kiến trúc là quan hệ trách nhiệm, không phải số thư mục.",

vi_du: "### Ví dụ FastAPI chạy độc lập\n\nLưu đoạn sau thành `main.py`, cài `fastapi` và `uvicorn`, rồi chạy `uvicorn main:app`. Repository bộ nhớ dùng lock để minh họa thao tác nguyên tử; production cần transaction database và shared state thay vì RAM từng process.\n\n```python\nfrom dataclasses import dataclass\nfrom threading import Lock\nfrom typing import Protocol\nfrom uuid import uuid4\nfrom fastapi import Depends, FastAPI, Header, HTTPException, status\nfrom pydantic import BaseModel, EmailStr, Field\n\n@dataclass(frozen=True)\nclass Booking:\n    id: str\n    workshop_id: str\n    email: str\n\nclass BookingPort(Protocol):\n    def reserve_once(self, workshop_id: str, email: str, key: str) -> Booking: ...\n\nclass WorkshopFull(Exception): pass\n\nclass MemoryBookingRepository:\n    def __init__(self) -> None:\n        self.seats = {\"ws-101\": 2}\n        self.by_key: dict[str, Booking] = {}\n        self.lock = Lock()\n\n    def reserve_once(self, workshop_id: str, email: str, key: str) -> Booking:\n        with self.lock:\n            if key in self.by_key:\n                return self.by_key[key]\n            if self.seats.get(workshop_id, 0) <= 0:\n                raise WorkshopFull(workshop_id)\n            booking = Booking(str(uuid4()), workshop_id, email)\n            self.seats[workshop_id] -= 1\n            self.by_key[key] = booking\n            return booking\n\nclass BookingService:\n    def __init__(self, repo: BookingPort) -> None:\n        self.repo = repo\n\n    def reserve(self, workshop_id: str, email: str, key: str, actor_email: str) -> Booking:\n        if email.casefold() != actor_email.casefold():\n            raise PermissionError(\"cannot book for another account\")\n        return self.repo.reserve_once(workshop_id, email.casefold(), key)\n\nclass CreateBookingRequest(BaseModel):\n    email: EmailStr\n    seats: int = Field(default=1, ge=1, le=1)\n\nclass BookingResponse(BaseModel):\n    id: str\n    workshop_id: str\n    email: EmailStr\n\napp = FastAPI()\nrepo = MemoryBookingRepository()\nservice = BookingService(repo)\n\ndef current_email(x_user_email: str = Header()) -> str:\n    return x_user_email  # demo; production phải xác minh token/chữ ký\n\n@app.post(\"/workshops/{workshop_id}/bookings\", response_model=BookingResponse,\n          status_code=status.HTTP_201_CREATED)\ndef create_booking(workshop_id: str, body: CreateBookingRequest,\n                   idempotency_key: str = Header(min_length=8, max_length=80),\n                   actor: str = Depends(current_email)) -> Booking:\n    try:\n        return service.reserve(workshop_id, str(body.email), idempotency_key, actor)\n    except WorkshopFull:\n        raise HTTPException(409, \"workshop is full\")\n    except PermissionError:\n        raise HTTPException(403, \"forbidden\")\n```\n\nThử hai lần cùng key sẽ nhận cùng `id`; dùng hai key sẽ hết hai vé, lần ba nhận 409. Ca biên workshop không tồn tại hiện cũng ra “full”, cho thấy contract còn thiếu `WorkshopNotFound`. Header danh tính chỉ để ví dụ chạy được, tuyệt đối không dùng như cơ chế xác thực thật vì client tự sửa được.\n\n### Kiểm service không cần HTTP\n\n```python\nfake = MemoryBookingRepository()\nuse_case = BookingService(fake)\na = use_case.reserve(\"ws-101\", \"lan@example.com\", \"request-0001\", \"lan@example.com\")\nb = use_case.reserve(\"ws-101\", \"lan@example.com\", \"request-0001\", \"lan@example.com\")\nassert a == b\nassert fake.seats[\"ws-101\"] == 1\n```\n\nTest này nhanh và tập trung vào invariant gửi lặp. Nó không thay integration test database, bởi `Lock` trong process không mô phỏng nhiều worker hay rollback.",

so_sanh: "### Chọn mức phân tách theo rủi ro\n\n| Cách tổ chức | Lợi thế | Chi phí và điểm mù | Hợp khi |\n|---|---|---|---|\n| Router làm mọi việc | ít file, đọc demo nhanh | khó dùng lại, transaction và quyền dễ rải rác | prototype ngắn hạn |\n| Router + service | invariant có chỗ rõ, test dễ | query có thể rò vào service | ứng dụng nhỏ, persistence đơn giản |\n| Router + service + repository | cô lập hạ tầng, test từng biên | mapping và abstraction tăng | nhiều use case hoặc database phức tạp |\n| Vertical slice theo tính năng | code một use case nằm gần nhau | có thể trùng tiện ích giữa slice | đội lớn, thay đổi theo feature |\n\nLayered architecture và vertical slice không loại trừ nhau: có thể đặt router, service, repository của booking trong cùng thư mục `booking/`. Điều cần giữ là chiều phụ thuộc và quyền sở hữu quyết định. Đừng chọn theo sơ đồ đẹp; chọn theo dạng thay đổi thường xảy ra.",

loi_thuong_gap: [
  "Router gọi ORM và commit trực tiếp; nhận biết bằng việc API test phải biết tên cột hoặc session.",
  "Service ném `HTTPException`; nhận biết khi chạy use case từ CLI vẫn phải import FastAPI.",
  "Repository chỉ bọc từng câu CRUD; nhận biết khi service tự dựng filter, join và lock.",
  "Kiểm số vé rồi cập nhật ở hai transaction; load test đồng thời làm số vé âm hoặc overbooking.",
  "Tin header danh tính do client gửi; kiểm thử đổi header vẫn truy cập được tài nguyên người khác.",
  "Serialize ORM object tự động; response bất ngờ lộ cột nội bộ hoặc phát sinh N+1 query.",
  "Bắt `Exception` rồi trả 200; dashboard sạch giả tạo trong khi client không phân biệt thất bại."
],

tu_kiem_tra: [
  "Vì sao kiểm token thuộc router nhưng kiểm actor được đặt vé cho email nào vẫn phải ở service?",
  "Ranh giới transaction nào giữ được invariant khi hai request tranh vé cuối cùng?",
  "Repository nên trả `None` hay ném lỗi khi database timeout, và vì sao hai trường hợp không thể gộp?",
  "Hãy chỉ ra một dữ liệu nhạy cảm có thể rò nếu trả thẳng ORM entity.",
  "Trong webhook chỉ ghi message vào queue, bạn sẽ bỏ tầng nào và tiêu chí nào biện minh cho quyết định?",
  "Fake repository có thể làm test xanh nhưng che lỗi production nào liên quan nhiều worker?"
],

tai_nguyen: [
  "Architecture Patterns with Python — Harry Percival và Bob Gregory",
  "Cosmic Python — tài liệu trực tuyến đi kèm sách Architecture Patterns with Python",
  "Domain-Driven Design — Eric Evans",
  "FastAPI Official Documentation"
],

lien_he: [
  "Nối sang mảng Cơ sở dữ liệu qua transaction, isolation level, unique constraint và query plan của repository.",
  "Nối sang mảng Bảo mật qua xác thực ở biên, object-level authorization trong use case và tài khoản database tối thiểu quyền.",
  "Nối sang mảng Kiểm thử qua test pyramid: unit cho service, integration cho repository, contract test cho HTTP.",
  "Nối sang mảng DevOps qua trace, metric, outbox worker và cấu hình connection pool khi triển khai nhiều process."
]

};
