window.LESSONS["04:database-cho-ai-ml:pgvector-tren-postgresql"] = {

tom_tat: [
  "pgvector đưa kiểu vector và các phép đo khoảng cách vào PostgreSQL, nhờ đó embedding có thể sống cùng tài liệu, quyền truy cập và transaction thay vì phải đồng bộ sang một kho riêng.",
  "Truy vấn `ORDER BY embedding <=> query LIMIT k` có thể là exact search hoặc dùng index gần đúng; hình dạng câu SQL quyết định PostgreSQL có nhận ra đường dùng index hay không.",
  "HNSW ưu tiên độ trễ truy vấn và recall, còn IVFFlat cần dữ liệu để huấn luyện danh sách nhưng xây nhanh và tiết kiệm bộ nhớ hơn; cả hai đều đổi độ chính xác lấy tốc độ.",
  "Lọc theo tenant, ngôn ngữ và trạng thái tài liệu phải được thiết kế cùng vector index, vì lọc sau khi lấy ứng viên có thể trả thiếu kết quả dù index vẫn chạy rất nhanh.",
  "pgvector hợp với hệ thống đã dùng PostgreSQL và quy mô vừa; khi tải vector thống trị, cần shard độc lập hoặc một vector database chuyên dụng."
],

can_biet_truoc: [
  "Khái niệm embedding: một đoạn văn được biểu diễn bằng vector số thực có số chiều cố định.",
  "Cosine distance, dot product và L2 distance; biết rằng metric lúc truy vấn phải khớp cách mô hình được huấn luyện.",
  "Index PostgreSQL, `EXPLAIN (ANALYZE, BUFFERS)` và lý do planner có thể chọn sequential scan.",
  "Transaction, khóa chính, khóa ngoại và Row-Level Security trong PostgreSQL."
],

dinh_nghia: "**pgvector** là extension của PostgreSQL bổ sung kiểu cột `vector(n)`, các toán tử khoảng cách và access method để tìm hàng có vector gần vector truy vấn. Nó không biến PostgreSQL thành một mô hình AI: mô hình embedding vẫn chạy ở ứng dụng hoặc dịch vụ khác; PostgreSQL nhận các dãy số đã tạo, lưu chúng cùng metadata rồi xếp hạng láng giềng.\n\nTrong trợ lý hỗ trợ sàn thương mại điện tử, mỗi đoạn chính sách có `document_id`, nội dung, phiên bản, tenant và embedding 768 chiều. Một transaction có thể đồng thời cập nhật nội dung, vector và trạng thái xuất bản. Tính chất này là lợi thế cốt lõi so với việc ghi PostgreSQL trước rồi phát sự kiện đồng bộ sang một kho vector khác.",

vi_sao_can: "Tìm bằng từ khóa bỏ lỡ cách diễn đạt khác nhau: khách hỏi “mở hộp rồi có trả được không?” trong khi chính sách viết “sản phẩm đã bóc niêm phong”. Embedding đưa hai câu gần nhau trong không gian vector. Nhưng nếu chỉ lưu vector trong file, ứng dụng thiếu truy vấn đồng thời, lọc tenant, cập nhật nguyên tử và phục hồi sau lỗi.\n\npgvector giải quyết khoảng trống đó ngay trong database đang giữ nguồn sự thật. Khi nhân viên xuất bản phiên bản chính sách mới, hệ thống không có cửa sổ mà nội dung mới đi cùng embedding cũ. Khi khách thuộc thị trường Việt Nam, SQL có thể kết hợp điều kiện quan hệ với xếp hạng ngữ nghĩa. Giá phải trả là vector index tiêu thụ RAM, cập nhật tạo write amplification, và một PostgreSQL phục vụ cả OLTP lẫn truy hồi có thể tranh CPU, I/O. Vì vậy đây là quyết định kiến trúc, không chỉ là cài một extension.",

co_che: "### Lược đồ giữ nội dung và embedding cùng một hàng\n\nVí dụ dùng một trung tâm hỗ trợ có nhiều cửa hàng. DDL dưới đây chạy trên PostgreSQL sau khi extension pgvector đã được cài ở server:\n\n```sql\nCREATE EXTENSION IF NOT EXISTS vector;\n\nCREATE TABLE support_chunk (\n  chunk_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  tenant_id bigint NOT NULL,\n  document_id bigint NOT NULL,\n  chunk_no integer NOT NULL CHECK (chunk_no >= 0),\n  language text NOT NULL CHECK (language IN ('vi', 'en')),\n  content text NOT NULL CHECK (length(content) > 0),\n  published boolean NOT NULL DEFAULT false,\n  embedding vector(3),\n  updated_at timestamptz NOT NULL DEFAULT now(),\n  UNIQUE (tenant_id, document_id, chunk_no)\n);\n```\n\nVí dụ dùng 3 chiều để người học tự chạy và nhìn số; production phải khai đúng số chiều mà model trả về. `vector(3)` từ chối mảng sai chiều ngay lúc ghi, tốt hơn để lỗi lộ ra khi truy vấn. Cho phép `NULL` hữu ích trong pipeline hai bước, nhưng truy vấn phải loại hàng chưa có embedding. Nếu nghiệp vụ đòi “đã xuất bản thì chắc chắn có vector”, một constraint hoặc transaction xuất bản riêng nên thực thi invariant ấy.\n\n### Toán tử khoảng cách không thể hoán đổi tùy ý\n\npgvector dùng `<->` cho L2 distance, `<#>` cho negative inner product và `<=>` cho cosine distance. PostgreSQL sắp xếp tăng dần, nên inner product bị đổi dấu để vector giống nhất đứng trước. Chọn operator class tương ứng khi tạo index; index cosine không phục vụ đúng một câu truy vấn L2.\n\n```sql\nINSERT INTO support_chunk\n  (tenant_id, document_id, chunk_no, language, content, published, embedding)\nVALUES\n  (7, 101, 0, 'vi', 'Đổi trả trong 30 ngày nếu còn nguyên phụ kiện', true, '[1,0,0]'),\n  (7, 102, 0, 'vi', 'Thời gian giao hàng tiêu chuẩn từ hai đến bốn ngày', true, '[0,1,0]'),\n  (7, 103, 0, 'vi', 'Bảo hành không áp dụng cho hư hỏng do vào nước', true, '[0.8,0.2,0]');\n\nSELECT chunk_id, content,\n       embedding <=> '[0.9,0.1,0]'::vector AS cosine_distance\nFROM support_chunk\nWHERE tenant_id = 7 AND published AND embedding IS NOT NULL\nORDER BY embedding <=> '[0.9,0.1,0]'::vector\nLIMIT 2;\n```\n\nCosine distance nhỏ hơn nghĩa là hướng gần hơn. Nếu model đã chuẩn hóa vector về độ dài 1, dot product thường cho cùng thứ hạng và có thể rẻ hơn về toán học; nhưng chỉ được đổi metric sau khi xác nhận hợp đồng của model. Trộn embedding từ hai model trong một cột tạo khoảng cách vô nghĩa dù cùng số chiều. Cách an toàn là lưu `embedding_model` và tách index hoặc bảng theo model.\n\n### Exact search và hình dạng câu truy vấn\n\nKhông có vector index, PostgreSQL tính khoảng cách cho mọi hàng thỏa `WHERE`, rồi dùng top-N sort để giữ k hàng tốt nhất. Đây là exact search: chậm tuyến tính theo số ứng viên nhưng không bỏ sót láng giềng thật. Với vài nghìn đoạn, sequential scan có thể nhanh hơn đi qua index vì dữ liệu đã nằm trong cache và planner tránh random access.\n\nBiểu thức trong `ORDER BY` phải là khoảng cách trực tiếp và đi cùng `LIMIT`. Viết `ORDER BY 1 - (embedding <=> q) DESC` có cùng ý nghĩa với người đọc nhưng có thể không khớp access method. Đừng bọc toán tử trong hàm tùy tiện. Hãy kiểm tra bằng:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT chunk_id\nFROM support_chunk\nWHERE tenant_id = 7 AND published\nORDER BY embedding <=> '[0.9,0.1,0]'::vector\nLIMIT 10;\n```\n\nĐọc `actual time`, số hàng bị loại và shared buffers; không kết luận từ cost dự đoán. Bảng quá nhỏ khiến planner cố ý chọn scan, nên việc “index không chạy” chưa chắc là lỗi.\n\n### HNSW: đồ thị nhiều tầng cho truy vấn nhanh\n\nHNSW nối mỗi vector với một số láng giềng rồi tìm từ tầng thưa xuống tầng dày. Nó không duyệt toàn bộ không gian, vì vậy latency giảm nhưng có thể bỏ qua hàng tốt nhất. Index có thể tạo khi bảng chưa có dữ liệu và thường cho cân bằng recall/latency tốt, đổi lại xây lâu, ngốn RAM và insert phải cập nhật đồ thị.\n\n```sql\nCREATE INDEX support_chunk_embedding_hnsw\nON support_chunk USING hnsw (embedding vector_cosine_ops)\nWITH (m = 16, ef_construction = 64);\n\nBEGIN;\nSET LOCAL hnsw.ef_search = 80;\nSELECT chunk_id, content\nFROM support_chunk\nWHERE tenant_id = 7 AND published\nORDER BY embedding <=> '[0.9,0.1,0]'::vector\nLIMIT 10;\nCOMMIT;\n```\n\n`m` tăng số liên kết, có thể cải thiện recall nhưng tăng kích thước và chi phí ghi. `ef_construction` mở rộng tìm kiếm lúc xây. `hnsw.ef_search` điều khiển số ứng viên lúc đọc; tăng nó thường tăng recall và độ trễ. Cần đo trên tập câu hỏi thật, không chọn tham số vì một bài blog.\n\n### IVFFlat: chia không gian thành các vùng\n\nIVFFlat học các tâm cụm, gán vector vào lists rồi chỉ thăm một số lists gần query. Nó cần dữ liệu đại diện trước khi tạo index; tạo trên bảng rỗng không có mẫu tốt để chia vùng. So với HNSW, index thường nhẹ và xây nhanh hơn, nhưng chất lượng phụ thuộc `lists`, phân bố embedding và số `probes`.\n\n```sql\nCREATE INDEX support_chunk_embedding_ivf\nON support_chunk USING ivfflat (embedding vector_cosine_ops)\nWITH (lists = 100);\n\nBEGIN;\nSET LOCAL ivfflat.probes = 10;\nSELECT chunk_id\nFROM support_chunk\nWHERE published\nORDER BY embedding <=> '[0.9,0.1,0]'::vector\nLIMIT 10;\nCOMMIT;\n```\n\nQuá ít lists làm mỗi vùng lớn và scan nhiều; quá nhiều lists khiến dữ liệu thưa và phải probe rộng. Sau thay đổi lớn về dữ liệu hoặc model, phân vùng cũ có thể kém đại diện; đây là lý do rebuild và benchmark là một phần vận hành.\n\n### Lọc metadata là bài toán khó hơn câu SQL trông thấy\n\nVector index tìm ứng viên gần trên toàn tập, trong khi B-tree trên `(tenant_id, published)` lọc theo thuộc tính. Planner không tự biến hai index thành một index đa chiều hoàn hảo. Nếu tenant 7 chỉ chiếm phần rất nhỏ, approximate scan có thể gặp nhiều vector của tenant khác rồi loại bỏ, kết quả cuối chưa đủ k hàng.\n\nCó bốn hướng: tăng `ef_search` hoặc `probes`; tạo partial index cho nhóm ổn định; partition theo tenant/miền lớn; hoặc dùng iterative scan nếu phiên bản triển khai hỗ trợ. Partition mỗi khách hàng nhỏ lại gây quá nhiều relation, nên chỉ phù hợp với số tenant hữu hạn và chênh tải rõ. Một B-tree bổ trợ vẫn cần thiết cho truy vấn quản trị và exact scan có điều kiện.\n\n```sql\nCREATE INDEX support_chunk_scope\nON support_chunk (tenant_id, language, published);\n\nCREATE INDEX support_vi_published_hnsw\nON support_chunk USING hnsw (embedding vector_cosine_ops)\nWHERE language = 'vi' AND published;\n```\n\nPartial index chỉ dùng khi predicate trong query suy ra được điều kiện index. Nếu ứng dụng truyền trạng thái theo biểu thức phức tạp, hãy xem plan thật.\n\n### Transaction, xóa và cập nhật model\n\nMột ưu điểm hiếm của pgvector là cùng transaction với dữ liệu quan hệ. Quy trình cập nhật nên ghi phiên bản tài liệu mới ở trạng thái chưa xuất bản, sinh đủ chunks và embeddings, rồi lật trạng thái trong transaction ngắn. Không giữ transaction mở trong lúc gọi API embedding qua mạng: nó giữ snapshot và tài nguyên quá lâu.\n\nKhi đổi model, không cập nhật từng hàng vào cột cũ rồi để hai không gian trộn nhau. Tạo cột hoặc bảng thế hệ mới, backfill có checkpoint, xây index, so recall, chuyển đọc, sau đó mới thu hồi thế hệ cũ. UPDATE hàng loạt cũng sinh dead tuples; theo dõi autovacuum, WAL và thời gian replica bắt kịp.\n\n### Đo recall cùng query plan\n\nLatency thấp không có nghĩa truy hồi đúng. Lấy một tập query, chạy exact search để lập top-k chuẩn, sau đó chạy approximate với các tham số khác nhau. Recall@k = số id giao nhau / k. Đồng thời ghi p50, p95, buffers và kích thước index. Nếu chỉ đo ba vector đồ chơi, mọi phương án đều đẹp.\n\nBenchmark phải giữ nguyên filter thực tế, phân bố tenant và độ đồng thời. Cache nóng và lạnh cho kết quả khác. Một cấu hình có recall tốt khi không lọc có thể thất bại ở tenant nhỏ. Quyết định cuối nên là đường cong recall–latency dưới tải, không phải một con số tách khỏi ngữ cảnh.\n\n### Ca biên và khi không nên dùng\n\nBảng rỗng trả mảng kết quả rỗng, không phải lỗi. Query vector sai chiều phải bị chặn trước khi gửi SQL. `NULL`, NaN hoặc vector zero cần chính sách rõ vì cosine với vector không có hướng không mang ý nghĩa. Timeout phải hủy statement và trả fallback, tránh để pool kết nối nghẽn dây chuyền.\n\nKhông nên dùng pgvector chỉ vì đã có PostgreSQL nếu hàng tỷ vector, tải truy hồi độc lập tăng rất nhanh, cần phân tán đa vùng chuyên biệt hoặc nhóm vận hành muốn scale compute vector tách hẳn OLTP. Ngược lại, tách sớm ở vài chục nghìn chunks thường mua thêm đồng bộ hai chiều, backup kép và lỗi nhất quán mà chưa nhận được lợi ích đo được.",

vi_du: "### Một truy vấn RAG có kiểm soát phạm vi\n\nĐoạn SQL sau khóa tenant ngay trong truy vấn, loại bản nháp và trả cả điểm để ứng dụng đặt ngưỡng. Nó dùng CTE chỉ để truyền query vector một lần:\n\n```sql\nWITH q AS (\n  SELECT '[0.9,0.1,0]'::vector AS v\n)\nSELECT c.chunk_id, c.document_id, c.content,\n       1 - (c.embedding <=> q.v) AS similarity\nFROM support_chunk AS c\nCROSS JOIN q\nWHERE c.tenant_id = 7\n  AND c.language = 'vi'\n  AND c.published\n  AND c.embedding IS NOT NULL\nORDER BY c.embedding <=> q.v\nLIMIT 5;\n```\n\nỨng dụng không nên ghép vector bằng chuỗi từ đầu vào người dùng; dùng parameter của driver. Sau khi nhận kết quả, kiểm tra đủ số đoạn và similarity theo ngưỡng đã hiệu chỉnh. Không có đoạn đạt thì câu trả lời đúng là từ chối hoặc chuyển tìm từ khóa, không ép model bịa từ context yếu.\n\n### Đối chứng exact với approximate\n\nTrong môi trường kiểm thử, tạm tắt các plan index cho truy vấn chuẩn trong một transaction riêng, thu id top-k, rồi bật lại và so giao. Cách cấu hình cụ thể phụ thuộc PostgreSQL/pgvector đang chạy, nên mục tiêu của mã kiểm thử là hai đường truy vấn giống hệt predicate. Dữ liệu vàng phải chứa câu hỏi đổi trả, giao hàng, bảo hành và các câu gây nhầm giữa tenant.",

so_sanh: "### Chọn exact, HNSW hay IVFFlat\n\n| Phương án | Điểm mạnh | Chi phí | Hợp với trợ lý hỗ trợ |\n|---|---|---|---|\n| Exact scan | Không mất recall do index; đơn giản | Thời gian tăng theo số hàng sau lọc | Tập nhỏ, đối chứng chất lượng, tenant rất hẹp |\n| HNSW | Query nhanh, thường giữ recall tốt | RAM và build cao; insert đắt hơn | Luồng hỏi đáp đọc nhiều, dữ liệu tăng dần |\n| IVFFlat | Index gọn, xây tương đối nhanh | Cần dữ liệu huấn luyện và chỉnh lists/probes | Nạp theo lô, ngân sách RAM chặt |\n| Vector DB riêng | Scale và phân tán chuyên cho vector | Thêm hệ thống, đồng bộ và kỹ năng vận hành | Vector là tải chính, cần scale tách OLTP |\n\n### pgvector so với Full-Text Search\n\nFull-Text Search mạnh ở tên sản phẩm, mã lỗi và từ bắt buộc; vector search mạnh ở diễn đạt đồng nghĩa. Với “đơn DH-1234” embedding không thay inverted index. Hybrid retrieval lấy ứng viên từ cả hai rồi hợp nhất hoặc rerank thường bền hơn. Đánh đổi là query phức tạp và cần bộ đánh giá relevance. Không nên xóa tìm kiếm từ khóa chỉ vì demo ngữ nghĩa trông ấn tượng.",

loi_thuong_gap: [
  "**Tạo HNSW rồi tin rằng chắc chắn được dùng.** Nhận biết bằng `EXPLAIN`; bảng nhỏ hoặc biểu thức `ORDER BY` bị bọc có thể dẫn tới sequential scan.",
  "**Dùng sai operator class.** Kết quả xếp hạng lệch hoặc index không phù hợp khi query cosine nhưng index được tạo cho L2.",
  "**Trộn embedding từ nhiều model.** Hàng vẫn ghi được nếu cùng chiều nhưng khoảng cách mất nghĩa; nhận biết qua metadata model hoặc phân bố điểm đổi đột ngột.",
  "**Lọc tenant sau khi lấy top-k ở ứng dụng.** Tenant nhỏ nhận ít hoặc không có context dù kho còn nhiều đoạn phù hợp.",
  "**Tạo IVFFlat trước khi nạp dữ liệu.** Các lists không đại diện; recall thấp dù tăng `LIMIT`.",
  "**Giữ transaction trong khi gọi dịch vụ embedding.** Quan sát transaction lâu, dead tuples không được dọn và pool kết nối cạn.",
  "**Chỉ đo latency.** Hệ thống nhanh nhưng lấy sai chính sách; phải so recall@k với exact search và bộ câu hỏi vàng."
],

tu_kiem_tra: [
  "Vì sao `ORDER BY 1 - distance DESC` có thể cho cùng thứ tự logic nhưng lại làm planner không chọn vector index?",
  "Nếu tenant chỉ chiếm 0,1% bảng, approximate index toàn cục có thể trả thiếu k kết quả bằng cơ chế nào?",
  "Khi nào sequential scan là lựa chọn đúng dù đã có HNSW?",
  "Hãy thiết kế migration đổi từ embedding 768 chiều sang model 1024 chiều mà không trộn hai không gian.",
  "Vì sao tăng `ef_search` thường làm recall và latency cùng tăng?",
  "Một benchmark pgvector đáng tin phải ghi ít nhất những đại lượng và phân bố dữ liệu nào?"
],

tai_nguyen: [
  "Tài liệu chính thức pgvector: phần Querying, Indexing và Filtering.",
  "PostgreSQL Documentation: Indexes, Query Planning và `EXPLAIN`.",
  "Designing Data-Intensive Applications — Martin Kleppmann.",
  "Database Internals — Alex Petrov."
],

lien_he: [
  "Nối với mảng Thuật toán qua HNSW: đồ thị láng giềng và tìm kiếm gần đúng giải thích vì sao không duyệt mọi vector.",
  "Nối với mảng Backend qua transaction, connection pool và Row-Level Security: retrieval cũng phải tuân thủ ranh giới tenant.",
  "Nối với mảng DevOps qua WAL, autovacuum, replica lag và quan sát p95 khi rebuild index.",
  "Nối với mảng AI qua RAG: các hàng top-k trở thành context, còn recall của index đặt trần cho chất lượng câu trả lời."
]

};
