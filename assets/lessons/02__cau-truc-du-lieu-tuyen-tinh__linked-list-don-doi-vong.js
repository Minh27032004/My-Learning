window.LESSONS["02:cau-truc-du-lieu-tuyen-tinh:linked-list-don-doi-vong"] = {

tom_tat: [
  "**Linked list đánh đổi truy cập ngẫu nhiên để lấy chèn/xoá O(1) — nhưng chỉ khi bạn ĐÃ CẦM nút đó.** Nếu phải đi tìm nút trước, chi phí lại thành O(n) và lợi thế biến mất.",
  "**Bài trước cho thấy mảng thắng nhờ cache locality. Bài này nói thẳng: trong Python, linked list gần như LUÔN thua `list` và `deque`** — nhưng bạn vẫn phải hiểu nó.",
  "**Ba biến thể, ba đánh đổi:** đơn (ít bộ nhớ, chỉ đi một chiều), đôi (đi hai chiều, xoá O(1), tốn thêm một con trỏ), vòng (không có `None` kết thúc, hợp cho luân phiên).",
  "**Kỹ thuật nút giả (sentinel) xoá bỏ gần hết các trường hợp đặc biệt** — không phải mẹo, mà là cách viết code chèn/xoá không có nhánh `if` cho đầu danh sách.",
  "**Lý do thật để học: nó là nền của rất nhiều thứ khác** — LRU cache, danh sách kề của đồ thị, chaining trong bảng băm, và cả cách hệ điều hành quản lý tiến trình."
],

can_biet_truoc: [
  "Bài **Array và dynamic array** ngay trước — công thức địa chỉ, cache locality, và vì sao mảng phải dịch khi chèn.",
  "Bài **Độ phức tạp không gian** ở module 1 mảng này — ngăn xếp đệ quy, và vì sao đệ quy sâu là vấn đề.",
  "Bài **Tham chiếu vs giá trị** ở module 7 mảng 01 — mỗi nút là một object, `next` là một tham chiếu.",
  "Bài **Đếm tham chiếu và garbage collection** ở module 7 mảng 01 — vì sao danh sách vòng cần chú ý khi giải phóng."
],

dinh_nghia: "Nói đời thường: một cuộc truy tìm kho báu. Mỗi mẩu giấy chỉ tới chỗ giấu mẩu tiếp theo. Bạn không thể nhảy thẳng tới mẩu thứ bảy — phải đi qua sáu mẩu trước. Nhưng chèn thêm một chặng vào giữa thì rất dễ: chỉ cần sửa hai mẩu giấy, không phải dời cả hành trình.\n\nCác khái niệm:\n\n- **Nút** (*node*) — một object chứa **giá trị** và ít nhất một **tham chiếu** tới nút khác.\n- **Danh sách liên kết đơn** (*singly linked list*) — mỗi nút có một con trỏ `next`. Đi được một chiều.\n- **Danh sách liên kết đôi** (*doubly linked list*) — mỗi nút có `next` và `prev`. Đi được hai chiều.\n- **Danh sách liên kết vòng** (*circular*) — nút cuối trỏ về nút đầu, không có `None` kết thúc.\n- **`head`** — tham chiếu tới nút đầu. Mất `head` là mất cả danh sách.\n- **`tail`** — tham chiếu tới nút cuối. Giữ nó để thêm cuối O(1).\n- **Nút giả** (*sentinel / dummy node*) — nút rỗng đặt ở đầu (và/hoặc cuối) để mọi nút thật đều có nút trước, xoá bỏ các trường hợp đặc biệt.\n\nPhân biệt then chốt, và là điều quyết định khi nào linked list đáng dùng:\n\n```\n\"Chèn/xoá là O(1)\"  —  CÂU NÀY THIẾU MỘT VẾ\n\nĐẦY ĐỦ: chèn/xoá là O(1) NẾU đã cầm sẵn tham chiếu tới nút trước đó.\n\n  Đã cầm nút    →  sửa 2 con trỏ  →  O(1)   ⭐\n  Chỉ biết chỉ số →  duyệt tìm nút →  O(n)   ⚠️ lợi thế BIẾN MẤT\n```\n\nĐây là lý do linked list ít hữu ích hơn vẻ ngoài: **trong phần lớn tình huống, bạn chỉ biết vị trí chứ không cầm sẵn nút.**",

vi_sao_can: "Cần nói thẳng: trong Python, bạn gần như sẽ **không** tự viết linked list cho việc thật. Nhưng có bốn lý do rõ ràng để hiểu nó:\n\n- **Nó là nền của nhiều cấu trúc bạn dùng hằng ngày.** `deque` của Python là danh sách liên kết các khối; LRU cache là danh sách liên kết đôi kết hợp `dict`; bảng băm chaining dùng linked list cho các khoá va chạm; hệ điều hành xâu chuỗi tiến trình bằng danh sách liên kết.\n- **Nó dạy tư duy con trỏ.** Thao tác nối lại các tham chiếu là kỹ năng nền cho **cây** (module 4) và **đồ thị** (module 5) — nơi mỗi nút có nhiều con trỏ thay vì một.\n- **Nó là chủ đề phỏng vấn phổ biến nhất.** Đảo ngược danh sách, phát hiện chu trình, gộp hai danh sách đã sắp — những bài này kiểm tra đúng khả năng thao tác con trỏ mà không dựa vào thư viện.\n- **Nó cho một ví dụ rõ ràng về đánh đổi cấu trúc.** Mảng và linked list là hai cực đối lập hoàn hảo, và hiểu cặp này giúp bạn đánh giá mọi cấu trúc khác.\n\nTình huống thật khi nó **đúng** là lựa chọn: bạn xây LRU cache — cần \"chuyển một phần tử lên đầu\" mỗi lần truy cập. Với `list`, thao tác đó là `remove` + `insert(0)` = O(n). Với danh sách liên kết đôi cộng một `dict` ánh xạ khoá tới nút, bạn **cầm sẵn** nút nên tháo và nối lại chỉ tốn O(1). Đây chính là cách `functools.lru_cache` hoạt động bên trong.\n\n**Phản biện phải nói rõ ngay từ đầu:** với dữ liệu thật trong Python, `list` và `deque` gần như luôn thắng linked list tự viết — kể cả ở những chỗ lý thuyết nói linked list tốt hơn. Phần cuối bài sẽ đo để chứng minh, và nêu rõ ba trường hợp hiếm hoi mà nó thật sự đáng.",

co_che: "### Cấu trúc một nút\n\n```\nDANH SÁCH ĐƠN\n\n  head\n   │\n   ↓\n ┌─────┬───┐   ┌─────┬───┐   ┌─────┬───┐\n │ 10  │ ●─┼──→│ 20  │ ●─┼──→│ 30  │ ✗ │  ← next = None\n └─────┴───┘   └─────┴───┘   └─────┴───┘\n   giá  next\n```\n\n```\nDANH SÁCH ĐÔI\n\n        head                              tail\n         │                                 │\n         ↓                                 ↓\n  ✗ ←┬───┬─────┬───┐   ┌───┬─────┬───┐   ┌───┬─────┬───┬→ ✗\n     │ ● │ 10  │ ●─┼──→│ ●─┼ 20  │ ●─┼──→│ ●─┼ 30  │ ✗ │\n     └───┴─────┴───┘←──┼─● │     │   │←──┼─● │     │   │\n      prev  giá  next  └───┴─────┴───┘   └───┴─────┴───┘\n```\n\n```\nDANH SÁCH VÒNG\n\n   ┌────────────────────────────────┐\n   ↓                                │\n ┌─────┬───┐   ┌─────┬───┐   ┌─────┬┴──┐\n │ 10  │ ●─┼──→│ 20  │ ●─┼──→│ 30  │ ● │\n └─────┴───┘   └─────┴───┘   └─────┴───┘\n   ⭐ không có None — đi mãi không hết\n```\n\nĐiểm cốt lõi: **các nút nằm rải rác trên heap**, không liền nhau như mảng. Không có công thức địa chỉ, nên không có truy cập ngẫu nhiên.\n\n### Vì sao không có `ds[i]` O(1)\n\n```\nMẢNG:        địa_chỉ(i) = gốc + i × ô          ⇒ O(1)\n\nLINKED LIST: địa chỉ nút i CHỈ biết được sau khi đọc nút i−1\n             ⇒ phải đi từ head qua i bước\n             ⇒ O(n)\n```\n\nĐây là **hệ quả bắt buộc** của việc bỏ tính liên tục, đúng cách bài trước chỉ ra rằng mọi chi phí O(n) của mảng là hệ quả bắt buộc của việc giữ tính liên tục. Hai cấu trúc trả giá ở hai chỗ đối lập nhau.\n\n### Chèn và xoá — vế bị thiếu\n\n```\nCHÈN sau một nút ĐÃ CẦM:  O(1)\n\n  trước:  [A] ──→ [C]\n\n  ① tạo nút mới B\n  ② B.next = A.next     (B trỏ tới C)\n  ③ A.next = B          (A trỏ tới B)\n\n  sau:    [A] ──→ [B] ──→ [C]\n\n  ⭐ Chỉ hai phép gán. KHÔNG dịch phần tử nào.\n```\n\n⚠️ **Thứ tự hai bước bắt buộc như vậy.** Làm ngược lại:\n\n```\n  ③ trước: A.next = B    → mất tham chiếu tới C\n  ② sau:   B.next = A.next = B   → B trỏ vào chính nó!\n\n  ⇒ MẤT toàn bộ phần đuôi danh sách.\n```\n\nĐây là lỗi kinh điển khi thao tác con trỏ, và nguyên tắc chung rất đáng nhớ: **nối cái mới trước, cắt cái cũ sau.**\n\n```\nXOÁ một nút:  O(1) NẾU cầm nút TRƯỚC nó\n\n  trước:  [A] ──→ [B] ──→ [C]\n  A.next = B.next          (A trỏ thẳng tới C)\n  sau:    [A] ──→ [C]      (B không ai trỏ tới → GC thu hồi)\n```\n\n```\n⚠️ VỚI DANH SÁCH ĐƠN: muốn xoá B mà chỉ cầm B thì KHÔNG làm được O(1)\n   — phải duyệt từ head tìm nút trước B ⇒ O(n)\n\n⭐ VỚI DANH SÁCH ĐÔI: cầm B là đủ, vì B.prev cho biết nút trước\n   B.prev.next = B.next\n   B.next.prev = B.prev\n   ⇒ O(1) THẬT\n```\n\nĐây là lý do chính khiến danh sách **đôi** phổ biến hơn đơn trong thư viện thật: nó cho phép xoá O(1) khi chỉ cầm nút cần xoá.\n\n### Nút giả — xoá bỏ trường hợp đặc biệt\n\nKhông có nút giả, mọi thao tác đều phải có nhánh riêng cho đầu danh sách:\n\n```python\n# ❌ Nhiều nhánh đặc biệt\ndef xoa(self, gia_tri):\n    if self.head is None:              # ⚠️ danh sách rỗng\n        return False\n    if self.head.gia_tri == gia_tri:   # ⚠️ xoá nút ĐẦU — khác mọi nút khác\n        self.head = self.head.next\n        return True\n    hien_tai = self.head\n    while hien_tai.next:\n        if hien_tai.next.gia_tri == gia_tri:\n            hien_tai.next = hien_tai.next.next\n            return True\n        hien_tai = hien_tai.next\n    return False\n```\n\n```python\n# ✓ Có nút giả — MỌI nút thật đều có nút trước\ndef xoa(self, gia_tri):\n    truoc = self._dau_gia                # ⭐ luôn tồn tại\n    while truoc.next:\n        if truoc.next.gia_tri == gia_tri:\n            truoc.next = truoc.next.next  # ⭐ một dòng cho MỌI vị trí\n            return True\n        truoc = truoc.next\n    return False\n```\n\n```\nGIÁ TRỊ CỦA NÚT GIẢ:\n  · Không còn nhánh \"nếu là nút đầu\"\n  · Không còn nhánh \"nếu danh sách rỗng\"\n  · Ít code hơn ⇒ ít chỗ sai hơn\n  · Chi phí: một object thừa\n```\n\nVới danh sách đôi, dùng **hai** nút giả (đầu và cuối) thì mọi nút thật đều có cả `prev` lẫn `next` khác `None` — code còn gọn hơn nữa. Đây là cách hiện thực chuẩn trong nhiều thư viện.\n\n### Ba biến thể — đánh đổi\n\n```\n                    ĐƠN          ĐÔI          VÒNG\n                    ─────        ─────        ─────\nCon trỏ mỗi nút     1            2            1 (hoặc 2)\nBộ nhớ mỗi nút      ít nhất      +1 con trỏ   như đơn/đôi\nĐi ngược            ✗            ✓            ✓ (đi vòng)\nXoá khi cầm nút     O(n)         ⭐ O(1)      O(n) nếu đơn\nThêm cuối (có tail) O(1)         O(1)         O(1)\nXoá cuối            O(n)         ⭐ O(1)      O(n) nếu đơn\nPhức tạp code       thấp         vừa          ⚠️ dễ lặp vô hạn\n```\n\nKhi nào dùng biến thể nào:\n\n```\nĐƠN   ngăn xếp, danh sách kề đồ thị, chaining bảng băm\n      → chỉ cần đi một chiều, muốn tiết kiệm bộ nhớ\n\nĐÔI   LRU cache, danh sách phát nhạc, undo/redo, deque\n      → cần xoá O(1) khi cầm nút, hoặc cần đi hai chiều\n\nVÒNG  lập lịch luân phiên (round-robin), bộ đệm vòng,\n      trò chơi chia lượt\n      → khi \"hết danh sách\" không có nghĩa\n```\n\n⚠️ Danh sách vòng có hai cạm bẫy riêng:\n\n```\n① Vòng lặp duyệt phải có ĐIỀU KIỆN DỪNG khác None\n   → thường là \"quay lại nút bắt đầu\"\n   → quên là LẶP VÔ HẠN\n\n② Chu trình tham chiếu ⇒ bộ đếm tham chiếu KHÔNG giải phóng được\n   → phải nhờ garbage collector theo thế hệ\n   (bài GC, module 7 mảng 01)\n```\n\n### Chi phí thao tác\n\n```\nTHAO TÁC                   ĐƠN        ĐÔI       MẢNG (list)\n─────────────────────      ────       ────      ───────────\nTruy cập ds[i]             O(n)       O(n)      ⭐ O(1)\nTìm theo giá trị           O(n)       O(n)      O(n)\nThêm ĐẦU                   ⭐ O(1)    ⭐ O(1)   ⚠️ O(n)\nThêm CUỐI (có tail)        O(1)       O(1)      O(1) khấu hao\nXoá ĐẦU                    ⭐ O(1)    ⭐ O(1)   ⚠️ O(n)\nXoá CUỐI                   O(n)       ⭐ O(1)   O(1)\nXoá khi CẦM nút            O(n)       ⭐ O(1)   O(n)\nChèn sau nút ĐÃ CẦM        O(1)       O(1)      O(n)\nBộ nhớ mỗi phần tử         cao        cao hơn   ⭐ thấp\nCache locality             ⚠️ tệ      ⚠️ tệ     ⭐ tốt\n```\n\nĐọc bảng theo cột: **linked list thắng ở các thao tác đầu danh sách và ở chèn/xoá khi đã cầm nút; mảng thắng ở truy cập, bộ nhớ, và locality.**\n\n### ⚠️ Vì sao Python hiếm khi dùng linked list\n\nPhần phản biện quan trọng nhất của bài:\n\n```\n① MỖI NÚT LÀ MỘT PyObject\n   Nút Python: ~56 byte cho object + ~28 byte cho giá trị int\n   Ô list:     8 byte con trỏ + ~28 byte giá trị\n   ⇒ linked list tốn hơn NHIỀU LẦN\n\n② CACHE LOCALITY TỆ\n   Nút rải rác trên heap ⇒ mỗi bước duyệt có thể là một cache miss\n   ⇒ duyệt chậm hơn mảng nhiều lần (bài trước đã đo)\n\n③ CHI PHÍ TRUY CẬP THUỘC TÍNH\n   nut.next trong Python phải tra __dict__ hoặc __slots__\n   ⇒ đắt hơn nhiều so với đọc một ô mảng\n\n④ ĐÃ CÓ deque\n   deque cho O(1) hai đầu, viết bằng C, cấu trúc khối\n   ⇒ có mọi lợi thế của linked list mà không có nhược điểm\n```\n\nBa trường hợp linked list **vẫn** đáng dùng trong Python:\n\n```\n① Cần XOÁ O(1) khi đã cầm nút, và có dict ánh xạ khoá → nút\n   → LRU cache. Đây là trường hợp chính đáng nhất.\n\n② Danh sách kề của đồ thị thưa\n   → nhưng thực tế dùng dict[đỉnh] = list[đỉnh] tiện hơn\n\n③ Học và phỏng vấn\n   → thao tác con trỏ là nền cho cây và đồ thị\n```\n\nKết luận thẳng thắn: **nếu bạn định dùng linked list trong Python cho việc thật, hãy kiểm tra lại xem `deque` hoặc `dict` có giải quyết được không.** Gần như luôn là có.\n\n### Ba kỹ thuật thao tác nền tảng\n\n**① Đảo ngược bằng ba con trỏ**\n\n```\ntruoc = None,  hien_tai = head\n\nMỗi bước:\n  ① sau = hien_tai.next        ← LƯU trước khi phá\n  ② hien_tai.next = truoc      ← đảo hướng\n  ③ truoc = hien_tai           ← tiến lên\n  ④ hien_tai = sau\n\nKết thúc: truoc là head mới\n```\n\nBước ① là chỗ hay quên: **phải lưu `next` trước khi ghi đè nó**, nếu không sẽ mất phần đuôi. Đây lại là nguyên tắc \"nối cái mới trước, cắt cái cũ sau\" nhìn từ góc khác.\n\n**② Duyệt an toàn khi vừa duyệt vừa xoá**\n\n```\n⚠️ SAI: xoá nút hiện tại rồi vẫn dùng hien_tai.next\n        → hien_tai đã bị tháo khỏi danh sách\n\n✓ ĐÚNG: luôn giữ tham chiếu tới nút TRƯỚC, và chỉ tiến khi KHÔNG xoá\n```\n\n**③ Hai con trỏ**\n\nKỹ thuật quan trọng nhất, và bài cuối module sẽ đi sâu:\n\n```\nchậm đi 1 bước, nhanh đi 2 bước\n\n⇒ khi nhanh tới cuối, chậm ở GIỮA\n⇒ nếu có chu trình, nhanh sẽ GẶP LẠI chậm\n```",

vi_du: "### Danh sách liên kết đơn — hiện thực đầy đủ\n\n```python\nfrom typing import Any, Iterator, Optional\n\n\nclass Nut:\n    \"\"\"Một nút của danh sách đơn. Dùng __slots__ để bớt bộ nhớ.\"\"\"\n    __slots__ = (\"gia_tri\", \"next\")          # ⭐ bỏ __dict__ mỗi instance\n\n    def __init__(self, gia_tri: Any, next: Optional[\"Nut\"] = None) -> None:\n        self.gia_tri = gia_tri\n        self.next = next\n\n    def __repr__(self) -> str:\n        return f\"Nut({self.gia_tri!r})\"\n\n\nclass DanhSachDon:\n    \"\"\"Danh sách liên kết đơn có nút giả ở đầu và tham chiếu tail.\n\n    Nút giả xoá bỏ mọi trường hợp đặc biệt cho nút đầu.\n    tail cho phép thêm cuối O(1).\n    \"\"\"\n\n    def __init__(self) -> None:\n        self._dau_gia = Nut(None)             # ⭐ nút giả, KHÔNG chứa dữ liệu\n        self._tail = self._dau_gia            # trỏ tới nút cuối cùng\n        self._size = 0\n\n    def __len__(self) -> int:\n        return self._size\n\n    def __iter__(self) -> Iterator[Any]:\n        nut = self._dau_gia.next               # ⭐ bỏ qua nút giả\n        while nut is not None:\n            yield nut.gia_tri\n            nut = nut.next\n\n    def __repr__(self) -> str:\n        return f\"DanhSachDon({list(self)})\"\n\n    # ---- Thêm ----\n\n    def them_dau(self, gia_tri: Any) -> None:\n        \"\"\"O(1).\"\"\"\n        moi = Nut(gia_tri, self._dau_gia.next)   # ⭐ NỐI trước\n        self._dau_gia.next = moi                  # ⭐ CẮT sau\n        if moi.next is None:                      # danh sách vốn rỗng\n            self._tail = moi\n        self._size += 1\n\n    def them_cuoi(self, gia_tri: Any) -> None:\n        \"\"\"O(1) nhờ giữ tail.\"\"\"\n        moi = Nut(gia_tri)\n        self._tail.next = moi\n        self._tail = moi\n        self._size += 1\n\n    # ---- Xoá ----\n\n    def xoa_dau(self) -> Any:\n        \"\"\"O(1). Raises: IndexError nếu rỗng.\"\"\"\n        dau = self._dau_gia.next\n        if dau is None:\n            raise IndexError(\"xoá từ danh sách rỗng\")\n        self._dau_gia.next = dau.next\n        if dau is self._tail:                     # ⚠️ xoá nút CUỐI CÙNG\n            self._tail = self._dau_gia            #    phải cập nhật tail\n        self._size -= 1\n        dau.next = None                            # ⭐ cắt để GC thu hồi\n        return dau.gia_tri\n\n    def xoa_gia_tri(self, gia_tri: Any) -> bool:\n        \"\"\"Xoá lần xuất hiện ĐẦU TIÊN. O(n).\n\n        Nhờ nút giả, KHÔNG cần nhánh riêng cho nút đầu.\n        \"\"\"\n        truoc = self._dau_gia\n        while truoc.next is not None:\n            if truoc.next.gia_tri == gia_tri:\n                bi_xoa = truoc.next\n                truoc.next = bi_xoa.next\n                if bi_xoa is self._tail:\n                    self._tail = truoc\n                bi_xoa.next = None\n                self._size -= 1\n                return True\n            truoc = truoc.next\n        return False\n\n    # ---- Truy cập ----\n\n    def lay(self, i: int) -> Any:\n        \"\"\"O(n) — KHÔNG có công thức địa chỉ. Raises: IndexError.\"\"\"\n        if not 0 <= i < self._size:\n            raise IndexError(f\"chỉ số {i} ngoài phạm vi 0..{self._size - 1}\")\n        nut = self._dau_gia.next\n        for _ in range(i):                        # ⚠️ phải ĐI TỪNG BƯỚC\n            nut = nut.next\n        return nut.gia_tri\n\n    def dao_nguoc(self) -> None:\n        \"\"\"Đảo ngược TẠI CHỖ bằng ba con trỏ. O(n) thời gian, O(1) không gian.\"\"\"\n        truoc = None\n        hien_tai = self._dau_gia.next\n        self._tail = hien_tai                     # nút đầu cũ thành tail mới\n        while hien_tai is not None:\n            sau = hien_tai.next                   # ⭐ LƯU trước khi phá\n            hien_tai.next = truoc                 # đảo hướng\n            truoc = hien_tai                      # tiến\n            hien_tai = sau\n        self._dau_gia.next = truoc\n        if self._tail is None:                    # danh sách rỗng\n            self._tail = self._dau_gia\n```\n\nKiểm tra đầy đủ, gồm mọi ca biên:\n\n```python\nds = DanhSachDon()\nassert len(ds) == 0 and list(ds) == []\n\n# Thêm\nds.them_cuoi(10)\nds.them_cuoi(20)\nds.them_dau(5)\nassert list(ds) == [5, 10, 20]\nassert len(ds) == 3\n\n# Truy cập O(n)\nassert ds.lay(0) == 5 and ds.lay(2) == 20\n\n# Xoá\nassert ds.xoa_dau() == 5\nassert list(ds) == [10, 20]\nassert ds.xoa_gia_tri(20) is True\nassert list(ds) == [10]\nassert ds.xoa_gia_tri(999) is False        # không có\n\n# Đảo ngược\nds2 = DanhSachDon()\nfor x in (1, 2, 3, 4):\n    ds2.them_cuoi(x)\nds2.dao_nguoc()\nassert list(ds2) == [4, 3, 2, 1]\nds2.them_cuoi(0)                            # ⭐ kiểm tail còn đúng sau khi đảo\nassert list(ds2) == [4, 3, 2, 1, 0]\n\n# ---- CA BIÊN ----\nrong = DanhSachDon()\nrong.dao_nguoc()                            # đảo danh sách rỗng\nassert list(rong) == []\nrong.them_cuoi(1)                           # ⭐ tail phải đúng sau khi đảo rỗng\nassert list(rong) == [1]\n\nmot = DanhSachDon()\nmot.them_cuoi(9)\nmot.dao_nguoc()\nassert list(mot) == [9]\nassert mot.xoa_dau() == 9\nassert len(mot) == 0\n\ntry:\n    DanhSachDon().xoa_dau()\nexcept IndexError as e:\n    print(f\"✓ bắt đúng: {e}\")\n\ntry:\n    DanhSachDon().lay(0)\nexcept IndexError as e:\n    print(f\"✓ bắt đúng: {e}\")\n\nprint(\"mọi ca kiểm thử đạt\")\n```\n\nBa chi tiết dễ sai mà bộ test trên bắt được:\n\n```\n① xoa_dau() phải CẬP NHẬT tail khi xoá nút cuối cùng\n   Quên thì tail trỏ vào nút đã tháo ⇒ them_cuoi() sau đó ghi vào hư không\n\n② dao_nguoc() phải cập nhật tail\n   Quên thì them_cuoi() sau khi đảo sẽ nối sai chỗ\n   → test \"ds2.them_cuoi(0)\" bắt đúng lỗi này\n\n③ Gán next = None cho nút đã tháo\n   Không gán thì nút bị xoá vẫn trỏ vào danh sách ⇒ GC khó thu hồi\n```\n\n### Lỗi kinh điển: sai thứ tự gán con trỏ\n\n```python\n# ❌ SAI — cắt trước, nối sau\ndef them_sau_sai(nut_truoc, gia_tri):\n    moi = Nut(gia_tri)\n    nut_truoc.next = moi            # ⚠️ MẤT tham chiếu tới phần đuôi\n    moi.next = nut_truoc.next       # ⚠️ giờ nut_truoc.next LÀ moi\n    # ⇒ moi.next = moi  →  nút tự trỏ vào chính nó!\n\n# ✓ ĐÚNG — nối trước, cắt sau\ndef them_sau_dung(nut_truoc, gia_tri):\n    moi = Nut(gia_tri, nut_truoc.next)   # ⭐ moi trỏ tới phần đuôi TRƯỚC\n    nut_truoc.next = moi                  # ⭐ rồi mới đổi nut_truoc\n```\n\nChứng minh hậu quả của bản sai:\n\n```python\na, b = Nut(\"A\"), Nut(\"B\")\na.next = b\n\nthem_sau_sai(a, \"X\")\nx = a.next\nprint(f\"a.next      = {x}\")            # Nut('X')\nprint(f\"a.next.next = {x.next}\")        # ⚠️ Nut('X') — TỰ TRỎ VÀO MÌNH\nprint(f\"tự trỏ: {x.next is x}\")         # True\n# ⇒ Nút B đã MẤT KHỎI danh sách, và duyệt sẽ LẶP VÔ HẠN\n```\n\nNguyên tắc rút ra, áp cho mọi thao tác con trỏ kể cả ở cây và đồ thị: **luôn nối tham chiếu mới trước khi ghi đè tham chiếu cũ.**\n\n### Danh sách liên kết đôi — xoá O(1) khi cầm nút\n\n```python\nfrom typing import Any, Iterator, Optional\n\n\nclass NutDoi:\n    __slots__ = (\"gia_tri\", \"prev\", \"next\")\n\n    def __init__(self, gia_tri: Any = None) -> None:\n        self.gia_tri = gia_tri\n        self.prev: Optional[\"NutDoi\"] = None\n        self.next: Optional[\"NutDoi\"] = None\n\n\nclass DanhSachDoi:\n    \"\"\"Danh sách liên kết đôi với HAI nút giả (đầu và cuối).\n\n    Nhờ hai nút giả, mọi nút thật đều có prev và next khác None\n    ⇒ không có trường hợp đặc biệt nào.\n    \"\"\"\n\n    def __init__(self) -> None:\n        self._dau = NutDoi()                  # nút giả đầu\n        self._cuoi = NutDoi()                 # nút giả cuối\n        self._dau.next = self._cuoi\n        self._cuoi.prev = self._dau\n        self._size = 0\n\n    def __len__(self) -> int:\n        return self._size\n\n    def __iter__(self) -> Iterator[Any]:\n        nut = self._dau.next\n        while nut is not self._cuoi:\n            yield nut.gia_tri\n            nut = nut.next\n\n    def duyet_nguoc(self) -> Iterator[Any]:\n        \"\"\"⭐ Chỉ danh sách ĐÔI làm được.\"\"\"\n        nut = self._cuoi.prev\n        while nut is not self._dau:\n            yield nut.gia_tri\n            nut = nut.prev\n\n    def _chen_giua(self, truoc: NutDoi, sau: NutDoi, gia_tri: Any) -> NutDoi:\n        \"\"\"Chèn giữa hai nút đã biết. O(1) — dùng chung cho mọi thao tác thêm.\"\"\"\n        moi = NutDoi(gia_tri)\n        moi.prev, moi.next = truoc, sau       # ⭐ nối MỚI trước\n        truoc.next = moi                       # ⭐ rồi cắt cũ\n        sau.prev = moi\n        self._size += 1\n        return moi\n\n    def them_dau(self, gia_tri: Any) -> NutDoi:\n        return self._chen_giua(self._dau, self._dau.next, gia_tri)\n\n    def them_cuoi(self, gia_tri: Any) -> NutDoi:\n        return self._chen_giua(self._cuoi.prev, self._cuoi, gia_tri)\n\n    def thao(self, nut: NutDoi) -> Any:\n        \"\"\"⭐⭐ Xoá một nút ĐÃ CẦM. O(1) THẬT — đây là lợi thế của danh sách đôi.\n\n        Raises: ValueError nếu nút là nút giả.\n        \"\"\"\n        if nut is self._dau or nut is self._cuoi:\n            raise ValueError(\"không thể tháo nút giả\")\n        nut.prev.next = nut.next\n        nut.next.prev = nut.prev\n        nut.prev = nut.next = None            # ⭐ cắt hẳn để GC thu hồi\n        self._size -= 1\n        return nut.gia_tri\n\n    def chuyen_len_dau(self, nut: NutDoi) -> None:\n        \"\"\"⭐ Thao ra rồi nối lại ở đầu. O(1) — nền của LRU cache.\"\"\"\n        if self._dau.next is nut:\n            return                             # đã ở đầu\n        # tháo\n        nut.prev.next = nut.next\n        nut.next.prev = nut.prev\n        # nối lại ở đầu\n        dau_cu = self._dau.next\n        nut.prev, nut.next = self._dau, dau_cu\n        self._dau.next = nut\n        dau_cu.prev = nut\n```\n\n```python\ndsd = DanhSachDoi()\nn10 = dsd.them_cuoi(10)\nn20 = dsd.them_cuoi(20)\nn30 = dsd.them_cuoi(30)\nassert list(dsd) == [10, 20, 30]\nassert list(dsd.duyet_nguoc()) == [30, 20, 10]     # ⭐ đi ngược\n\n# ⭐ Xoá nút GIỮA khi đã cầm — O(1), không duyệt\nassert dsd.thao(n20) == 20\nassert list(dsd) == [10, 30]\nassert len(dsd) == 2\n\n# ⭐ Chuyển lên đầu — O(1), nền của LRU\ndsd.chuyen_len_dau(n30)\nassert list(dsd) == [30, 10]\n\n# ---- CA BIÊN ----\nrong = DanhSachDoi()\nassert list(rong) == [] and list(rong.duyet_nguoc()) == []\n\nmot = DanhSachDoi()\nn = mot.them_dau(1)\nmot.chuyen_len_dau(n)                   # ⭐ chuyển nút đã ở đầu\nassert list(mot) == [1]\nassert mot.thao(n) == 1\nassert list(mot) == []\n\ntry:\n    rong.thao(rong._dau)\nexcept ValueError as e:\n    print(f\"✓ bắt đúng: {e}\")\n\nprint(\"danh sách đôi: mọi ca kiểm thử đạt\")\n```\n\n### LRU cache — nơi linked list THẬT SỰ đúng\n\nĐây là trường hợp chính đáng nhất, và nó cho thấy vì sao cần **cả** dict lẫn danh sách đôi:\n\n```python\nfrom typing import Any, Optional\n\n\nclass NutLRU:\n    __slots__ = (\"khoa\", \"gia_tri\", \"prev\", \"next\")\n\n    def __init__(self, khoa: Any = None, gia_tri: Any = None) -> None:\n        self.khoa = khoa\n        self.gia_tri = gia_tri\n        self.prev: Optional[\"NutLRU\"] = None\n        self.next: Optional[\"NutLRU\"] = None\n\n\nclass LRUCache:\n    \"\"\"Cache bỏ mục ÍT DÙNG GẦN ĐÂY NHẤT khi đầy.\n\n    dict           → tìm nút theo khoá:        O(1)\n    danh sách đôi  → chuyển lên đầu / bỏ cuối: O(1)\n\n    ⇒ get và put đều O(1). Không cấu trúc đơn lẻ nào làm được điều này.\n    \"\"\"\n\n    def __init__(self, suc_chua: int) -> None:\n        if suc_chua < 1:\n            raise ValueError(\"sức chứa phải ≥ 1\")\n        self.suc_chua = suc_chua\n        self._theo_khoa: dict[Any, NutLRU] = {}\n        self._dau = NutLRU()                   # nút giả: mới nhất ở sau _dau\n        self._cuoi = NutLRU()                  # nút giả: cũ nhất ở trước _cuoi\n        self._dau.next = self._cuoi\n        self._cuoi.prev = self._dau\n\n    def _thao(self, nut: NutLRU) -> None:\n        nut.prev.next = nut.next\n        nut.next.prev = nut.prev\n\n    def _noi_dau(self, nut: NutLRU) -> None:\n        nut.prev, nut.next = self._dau, self._dau.next\n        self._dau.next.prev = nut\n        self._dau.next = nut\n\n    def get(self, khoa: Any) -> Any:\n        \"\"\"O(1). Trả None nếu không có.\"\"\"\n        nut = self._theo_khoa.get(khoa)\n        if nut is None:\n            return None\n        self._thao(nut)                        # ⭐ O(1) nhờ đã cầm nút\n        self._noi_dau(nut)                     # ⭐ đánh dấu \"vừa dùng\"\n        return nut.gia_tri\n\n    def put(self, khoa: Any, gia_tri: Any) -> None:\n        \"\"\"O(1).\"\"\"\n        nut = self._theo_khoa.get(khoa)\n        if nut is not None:\n            nut.gia_tri = gia_tri\n            self._thao(nut)\n            self._noi_dau(nut)\n            return\n\n        if len(self._theo_khoa) >= self.suc_chua:\n            cu_nhat = self._cuoi.prev          # ⭐ O(1) nhờ danh sách ĐÔI\n            self._thao(cu_nhat)\n            del self._theo_khoa[cu_nhat.khoa]  # ⭐ nút giữ khoá để xoá khỏi dict\n\n        moi = NutLRU(khoa, gia_tri)\n        self._theo_khoa[khoa] = moi\n        self._noi_dau(moi)\n\n    def __len__(self) -> int:\n        return len(self._theo_khoa)\n\n    def cac_khoa_theo_thu_tu(self) -> list:\n        \"\"\"Mới dùng nhất trước.\"\"\"\n        kq, nut = [], self._dau.next\n        while nut is not self._cuoi:\n            kq.append(nut.khoa)\n            nut = nut.next\n        return kq\n```\n\n```python\nc = LRUCache(3)\nc.put(\"a\", 1); c.put(\"b\", 2); c.put(\"c\", 3)\nassert c.cac_khoa_theo_thu_tu() == [\"c\", \"b\", \"a\"]\n\nassert c.get(\"a\") == 1                       # dùng \"a\" → lên đầu\nassert c.cac_khoa_theo_thu_tu() == [\"a\", \"c\", \"b\"]\n\nc.put(\"d\", 4)                                 # đầy → bỏ \"b\" (cũ nhất)\nassert c.get(\"b\") is None\nassert c.cac_khoa_theo_thu_tu() == [\"d\", \"a\", \"c\"]\n\nc.put(\"a\", 99)                                # cập nhật khoá đã có\nassert c.get(\"a\") == 99\nassert len(c) == 3\n\n# ---- CA BIÊN ----\nc1 = LRUCache(1)\nc1.put(\"x\", 1)\nc1.put(\"y\", 2)                                # ⭐ sức chứa 1 → bỏ ngay \"x\"\nassert c1.get(\"x\") is None and c1.get(\"y\") == 2\n\ntry:\n    LRUCache(0)\nexcept ValueError as e:\n    print(f\"✓ bắt đúng: {e}\")\n\nprint(\"LRU cache: mọi ca kiểm thử đạt\")\n```\n\n```\n⭐ VÌ SAO CẦN CẢ HAI CẤU TRÚC:\n\n  Chỉ dict          → không biết cái nào CŨ NHẤT\n  Chỉ danh sách đôi → tìm khoá phải duyệt O(n)\n  Chỉ list          → chuyển lên đầu là remove + insert(0) = O(n)\n\n  dict + danh sách đôi ⇒ CẢ HAI thao tác O(1)\n\n⇒ Đây là cách functools.lru_cache của Python hoạt động bên trong.\n```\n\n### Danh sách vòng — và bẫy lặp vô hạn\n\n```python\nclass DanhSachVong:\n    \"\"\"Danh sách vòng đơn. Chỉ giữ tham chiếu tới nút CUỐI.\n\n    Giữ 'cuoi' thay vì 'dau' vì cuoi.next chính là đầu\n    ⇒ thêm đầu và thêm cuối đều O(1).\n    \"\"\"\n\n    def __init__(self) -> None:\n        self._cuoi: Optional[Nut] = None\n        self._size = 0\n\n    def __len__(self) -> int:\n        return self._size\n\n    def them_cuoi(self, gia_tri: Any) -> None:\n        moi = Nut(gia_tri)\n        if self._cuoi is None:\n            moi.next = moi                     # ⭐ tự trỏ vào mình\n            self._cuoi = moi\n        else:\n            moi.next = self._cuoi.next         # nối vào đầu\n            self._cuoi.next = moi\n            self._cuoi = moi\n        self._size += 1\n\n    def __iter__(self) -> Iterator[Any]:\n        \"\"\"⚠️ PHẢI có điều kiện dừng — không có None để dựa vào.\"\"\"\n        if self._cuoi is None:\n            return\n        nut = self._cuoi.next                  # nút đầu\n        for _ in range(self._size):            # ⭐ dừng theo SỐ LƯỢNG\n            yield nut.gia_tri\n            nut = nut.next\n\n    def luan_phien(self, so_luot: int) -> Iterator[Any]:\n        \"\"\"⭐ Ứng dụng chính: đi vòng mãi không hết.\"\"\"\n        if self._cuoi is None:\n            return\n        nut = self._cuoi.next\n        for _ in range(so_luot):\n            yield nut.gia_tri\n            nut = nut.next                      # ⭐ tự động quay vòng\n```\n\n```python\ndv = DanhSachVong()\nfor ten in (\"An\", \"Bình\", \"Cường\"):\n    dv.them_cuoi(ten)\n\nassert list(dv) == [\"An\", \"Bình\", \"Cường\"]\n\n# ⭐ Lập lịch luân phiên — 7 lượt cho 3 người\nassert list(dv.luan_phien(7)) == [\n    \"An\", \"Bình\", \"Cường\", \"An\", \"Bình\", \"Cường\", \"An\"\n]\n\n# Ca biên\nrong = DanhSachVong()\nassert list(rong) == [] and list(rong.luan_phien(5)) == []\n\nmot = DanhSachVong()\nmot.them_cuoi(\"X\")\nassert list(mot.luan_phien(3)) == [\"X\", \"X\", \"X\"]     # ⭐ tự trỏ vào mình\n```\n\n```\n⚠️ HAI CẠM BẪY CỦA DANH SÁCH VÒNG:\n\n① Duyệt bằng \"while nut is not None\" ⇒ LẶP VÔ HẠN\n   → phải dừng theo số lượng, hoặc theo \"quay lại nút bắt đầu\"\n\n② Chu trình tham chiếu ⇒ bộ đếm tham chiếu KHÔNG giải phóng được\n   → chỉ garbage collector theo thế hệ mới dọn được\n   (bài GC và đếm tham chiếu, module 7 mảng 01)\n```\n\nChứng minh vấn đề ②:\n\n```python\nimport gc\nimport weakref\n\ndv = DanhSachVong()\ndv.them_cuoi(1)\nyeu = weakref.ref(dv._cuoi)\n\ndel dv\nprint(f\"sau del, nút còn sống? {yeu() is not None}\")   # ⚠️ True — chu trình\ngc.collect()                                            # ⭐ GC dọn chu trình\nprint(f\"sau gc.collect():      {yeu() is not None}\")   # False\n```\n\n### Đo thật: linked list so với `list` và `deque`\n\nĐây là phần phản biện — chứng minh bằng số:\n\n```python\nimport time\nfrom collections import deque\n\nn = 200_000\n\n# ── THÊM ĐẦU — lý thuyết nói linked list THẮNG ──\nt0 = time.perf_counter()\ndsd = DanhSachDon()\nfor i in range(n):\n    dsd.them_dau(i)\nt_linked = time.perf_counter() - t0\n\nt0 = time.perf_counter()\ndq: deque[int] = deque()\nfor i in range(n):\n    dq.appendleft(i)\nt_deque = time.perf_counter() - t0\n\nt0 = time.perf_counter()\nds: list[int] = []\nfor i in range(n):\n    ds.append(i)\nds.reverse()                          # ⭐ mẹo: append rồi đảo một lần\nt_list = time.perf_counter() - t0\n\nprint(\"THÊM ĐẦU n lần:\")\nprint(f\"  linked list tự viết : {t_linked:>7.4f}s\")\nprint(f\"  deque.appendleft    : {t_deque:>7.4f}s\")\nprint(f\"  list.append+reverse : {t_list:>7.4f}s\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\nTHÊM ĐẦU n lần:\n  linked list tự viết :  chậm nhất      ⚠️ dù LÝ THUYẾT là O(1)\n  deque.appendleft    :  nhanh          ⭐ O(1) và viết bằng C\n  list.append+reverse :  nhanh nhất     ⭐ locality tốt nhất\n\n⇒ Linked list THUA ở đúng thao tác mà nó được cho là mạnh nhất.\n  Vì: mỗi nút là một PyObject phải cấp phát, và truy cập\n      thuộc tính .next đắt hơn nhiều so với thao tác trong C.\n```\n\n```python\n# ── DUYỆT — nơi cache locality lộ rõ ──\nt0 = time.perf_counter()\ns = sum(dsd)\nt_linked = time.perf_counter() - t0\n\nt0 = time.perf_counter()\ns = sum(dq)\nt_deque = time.perf_counter() - t0\n\nt0 = time.perf_counter()\ns = sum(ds)\nt_list = time.perf_counter() - t0\n\nprint(\"\\nDUYỆT HẾT:\")\nprint(f\"  linked list : {t_linked:>7.4f}s\")\nprint(f\"  deque       : {t_deque:>7.4f}s\")\nprint(f\"  list        : {t_list:>7.4f}s   ⭐ nhanh nhất\")\n```\n\n```python\n# ── BỘ NHỚ ──\nimport tracemalloc\n\ndef do_bo_nho(ten, tao):\n    tracemalloc.start()\n    obj = tao()\n    bo_nho, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n    print(f\"  {ten:>20}: {bo_nho/1024/1024:>7.1f} MB\")\n    del obj\n\nm = 100_000\nprint(\"\\nBỘ NHỚ cho 100k phần tử:\")\n\ndef tao_linked():\n    d = DanhSachDon()\n    for i in range(m):\n        d.them_cuoi(i)\n    return d\n\ndo_bo_nho(\"linked list\", tao_linked)\ndo_bo_nho(\"deque\", lambda: deque(range(m)))\ndo_bo_nho(\"list\", lambda: list(range(m)))\n```\n\n```\nBỘ NHỚ cho 100k phần tử:\n           linked list:  ~9–12 MB     ⚠️ mỗi nút là một object\n                 deque:   ~4–5 MB\n                  list:   ~4–5 MB     ⭐ ít nhất\n\n⇒ Linked list tốn NHIỀU LẦN bộ nhớ so với list, dù dùng __slots__.\n```\n\n```\n═══ KẾT LUẬN THẲNG THẮN ═══\n\nTrong Python, linked list tự viết THUA list và deque ở:\n  · tốc độ thêm đầu   (dù lý thuyết là O(1))\n  · tốc độ duyệt      (cache locality)\n  · bộ nhớ            (mỗi nút một PyObject)\n\nNó CHỈ thắng khi:\n  · cần xoá O(1) một nút ĐÃ CẦM, kết hợp dict → LRU cache\n\n⇒ Học nó để hiểu tư duy con trỏ và để làm nền cho cây/đồ thị,\n  KHÔNG phải để dùng thay list.\n```\n\n### So sánh trực tiếp: chèn giữa\n\nThử nghiệm công bằng — cả hai đều phải **tìm** vị trí trước:\n\n```python\nimport time\n\nn = 20_000\n\n# list: tìm O(1) nhờ chỉ số, nhưng CHÈN O(n) vì phải dịch\nt0 = time.perf_counter()\nds = list(range(n))\nfor _ in range(1_000):\n    ds.insert(n // 2, 0)              # ⚠️ dịch n/2 phần tử\nt_list = time.perf_counter() - t0\n\n# linked list: tìm O(n) bằng duyệt, nhưng CHÈN O(1)\ndsd = DanhSachDon()\nfor i in range(n):\n    dsd.them_cuoi(i)\n\ndef chen_giua_linked(d, so_lan):\n    for _ in range(so_lan):\n        nut = d._dau_gia\n        for _ in range(len(d) // 2):   # ⚠️ DUYỆT tìm vị trí — O(n)\n            nut = nut.next\n        moi = Nut(0, nut.next)          # ⭐ chèn O(1)\n        nut.next = moi\n        d._size += 1\n\nt0 = time.perf_counter()\nchen_giua_linked(dsd, 1_000)\nt_linked = time.perf_counter() - t0\n\nprint(f\"1000 lần chèn giữa (n={n:,}):\")\nprint(f\"  list.insert(n/2) : {t_list:>7.4f}s   (tìm O(1) + dịch O(n))\")\nprint(f\"  linked list      : {t_linked:>7.4f}s   (tìm O(n) + chèn O(1))\")\n```\n\n```\n⭐ CẢ HAI đều O(n) cho một lần chèn giữa!\n\n  list:        dịch n/2 phần tử — nhưng bằng memmove trong C, RẤT nhanh\n  linked list: duyệt n/2 nút    — mỗi bước một lần truy cập thuộc tính Python\n\n⇒ list thường THẮNG, dù \"lý thuyết\" nói linked list chèn O(1).\n  Vì vế \"đã cầm sẵn nút\" KHÔNG thoả trong tình huống này.\n\n⇒ Đây là minh hoạ rõ nhất cho câu ở đầu bài:\n  \"chèn O(1)\" là một lời hứa CÓ ĐIỀU KIỆN.\n```\n\n### Ba bài toán phỏng vấn kinh điển\n\n```python\ndef dao_nguoc(head: Optional[Nut]) -> Optional[Nut]:\n    \"\"\"Đảo ngược danh sách. O(n) thời gian, O(1) không gian.\"\"\"\n    truoc, hien_tai = None, head\n    while hien_tai:\n        sau = hien_tai.next            # ⭐ LƯU trước khi phá\n        hien_tai.next = truoc\n        truoc, hien_tai = hien_tai, sau\n    return truoc\n\n\ndef tim_giua(head: Optional[Nut]) -> Optional[Nut]:\n    \"\"\"Tìm nút giữa bằng hai con trỏ. Một lượt duyệt, O(1) không gian.\n\n    Danh sách chẵn phần tử → trả nút thứ hai của cặp giữa.\n    \"\"\"\n    cham = nhanh = head\n    while nhanh and nhanh.next:\n        cham = cham.next               # 1 bước\n        nhanh = nhanh.next.next        # 2 bước\n    return cham\n\n\ndef gop_hai_da_sap(a: Optional[Nut], b: Optional[Nut]) -> Optional[Nut]:\n    \"\"\"Gộp hai danh sách ĐÃ SẮP thành một danh sách đã sắp. O(n+m).\"\"\"\n    gia = Nut(None)                    # ⭐ nút giả — không cần nhánh đặc biệt\n    duoi = gia\n    while a and b:\n        if a.gia_tri <= b.gia_tri:     # ⭐ <= giữ tính ỔN ĐỊNH\n            duoi.next, a = a, a.next\n        else:\n            duoi.next, b = b, b.next\n        duoi = duoi.next\n    duoi.next = a or b                 # ⭐ nối phần còn lại\n    return gia.next\n\n\ndef tu_ds(cac_gia_tri) -> Optional[Nut]:\n    \"\"\"Tiện ích: dựng danh sách từ iterable.\"\"\"\n    gia = Nut(None)\n    duoi = gia\n    for x in cac_gia_tri:\n        duoi.next = Nut(x)\n        duoi = duoi.next\n    return gia.next\n\n\ndef sang_ds(head: Optional[Nut]) -> list:\n    kq = []\n    while head:\n        kq.append(head.gia_tri)\n        head = head.next\n    return kq\n\n\n# ---- Kiểm tra, gồm ca biên ----\nassert sang_ds(dao_nguoc(tu_ds([1, 2, 3]))) == [3, 2, 1]\nassert sang_ds(dao_nguoc(tu_ds([]))) == []            # biên: rỗng\nassert sang_ds(dao_nguoc(tu_ds([1]))) == [1]          # biên: một nút\n\nassert tim_giua(tu_ds([1, 2, 3])).gia_tri == 2\nassert tim_giua(tu_ds([1, 2, 3, 4])).gia_tri == 3     # ⭐ chẵn → nút thứ hai\nassert tim_giua(tu_ds([1])).gia_tri == 1              # biên\nassert tim_giua(tu_ds([])) is None                     # biên\n\nassert sang_ds(gop_hai_da_sap(tu_ds([1, 3, 5]), tu_ds([2, 4, 6]))) \\\n    == [1, 2, 3, 4, 5, 6]\nassert sang_ds(gop_hai_da_sap(tu_ds([]), tu_ds([1, 2]))) == [1, 2]   # biên\nassert sang_ds(gop_hai_da_sap(tu_ds([]), tu_ds([]))) == []           # biên\nassert sang_ds(gop_hai_da_sap(tu_ds([1, 1]), tu_ds([1]))) == [1, 1, 1]  # trùng\n\nprint(\"ba bài kinh điển: mọi ca kiểm thử đạt\")\n```\n\nHai điểm thiết kế đáng học trong ba hàm trên:\n\n```\n① gop_hai_da_sap dùng NÚT GIẢ\n   Không có nó, phải viết nhánh riêng để khởi tạo head\n   ⇒ dài hơn và dễ sai hơn\n\n② duoi.next = a or b\n   Nối thẳng phần CÒN LẠI thay vì duyệt tiếp từng nút\n   ⇒ tận dụng việc phần còn lại vốn đã được sắp\n```",

so_sanh: "### Ba biến thể\n\n| | Đơn | Đôi | Vòng |\n|---|---|---|---|\n| Con trỏ mỗi nút | **1** | 2 | 1–2 |\n| Bộ nhớ | **ít nhất** | nhiều nhất | như đơn/đôi |\n| Đi ngược | ✗ | **✓** | ✓ (đi vòng) |\n| Xoá khi **cầm nút** | O(n) | **⭐ O(1)** | O(n) nếu đơn |\n| Xoá cuối | O(n) | **O(1)** | O(n) nếu đơn |\n| Rủi ro riêng | — | — | **⚠️ lặp vô hạn, chu trình GC** |\n| Dùng cho | stack, danh sách kề, chaining | **LRU, undo/redo, deque** | round-robin, bộ đệm vòng |\n\n### Linked list và mảng — hai cực đối lập\n\n| Thao tác | Linked list (đôi) | `list` |\n|---|---|---|\n| `ds[i]` | **O(n)** ⚠️ | **⭐ O(1)** |\n| Thêm/xoá **đầu** | **⭐ O(1)** | **O(n)** ⚠️ |\n| Thêm cuối | O(1) (có tail) | O(1) khấu hao |\n| Xoá khi **cầm nút** | **⭐ O(1)** | O(n) |\n| Bộ nhớ | **cao** ⚠️ | ⭐ thấp |\n| Cache locality | **tệ** ⚠️ | ⭐ tốt |\n\nMỗi cấu trúc trả giá ở đúng chỗ cái kia mạnh — **mảng trả giá vì giữ liên tục, linked list trả giá vì bỏ liên tục.**\n\n### \"Chèn O(1)\" — lời hứa có điều kiện\n\n| Tình huống | Chi phí thật |\n|---|---|\n| **Đã cầm** tham chiếu tới nút trước | **O(1)** ⭐ |\n| Chỉ biết **chỉ số** | O(n) tìm + O(1) chèn = **O(n)** |\n| Chỉ biết **giá trị** | O(n) tìm + O(1) chèn = **O(n)** |\n\nVế thứ hai và ba là tình huống phổ biến hơn — đó là lý do linked list ít hữu ích hơn vẻ ngoài.\n\n### Đo thật trong Python\n\n| Phép đo | linked list tự viết | `deque` | `list` |\n|---|---|---|---|\n| Thêm đầu n lần | **chậm nhất** ⚠️ | nhanh | **nhanh nhất** |\n| Duyệt hết | chậm | vừa | **⭐ nhanh nhất** |\n| Bộ nhớ 100k | **~9–12 MB** ⚠️ | ~4–5 MB | **⭐ ~4–5 MB** |\n\nLinked list **thua ở đúng thao tác nó được cho là mạnh nhất** — vì mỗi nút là một `PyObject` phải cấp phát, và `.next` là một lần tra thuộc tính Python.\n\n### Vì sao Python hiếm dùng linked list\n\n| Lý do | Hệ quả |\n|---|---|\n| Mỗi nút là `PyObject` | tốn ~56+ byte, gấp nhiều lần một ô list |\n| Nút rải rác trên heap | **cache miss mỗi bước duyệt** |\n| `nut.next` tra thuộc tính | đắt hơn nhiều so với đọc ô mảng |\n| **Đã có `deque`** | O(1) hai đầu, viết bằng C, không nhược điểm |\n\n### Khi nào linked list VẪN đáng dùng\n\n| Trường hợp | Vì sao |\n|---|---|\n| **LRU cache** (dict + đôi) | cần xoá O(1) nút đã cầm — không cấu trúc đơn lẻ nào làm được |\n| Danh sách kề đồ thị thưa | nhưng `dict[đỉnh] = list` thường tiện hơn |\n| Học / phỏng vấn | nền cho **cây** và **đồ thị** |\n\n### Hai nguyên tắc thao tác con trỏ\n\n| Nguyên tắc | Vì sao |\n|---|---|\n| **Nối cái mới TRƯỚC, cắt cái cũ SAU** | ngược lại làm mất phần đuôi, nút tự trỏ vào mình |\n| **Lưu `next` trước khi ghi đè** | dùng trong đảo ngược; quên là mất danh sách |\n\n### Nút giả — giá trị\n\n| Không nút giả | Có nút giả |\n|---|---|\n| Nhánh riêng cho danh sách **rỗng** | không cần |\n| Nhánh riêng cho nút **đầu** | không cần |\n| Nhiều `if` ⇒ nhiều chỗ sai | **một dòng cho mọi vị trí** |\n| — | chi phí: **một object thừa** |\n\nDanh sách đôi dùng **hai** nút giả thì mọi nút thật đều có `prev` và `next` khác `None`.\n\n### Ba lỗi dễ mắc và test bắt được\n\n| Lỗi | Triệu chứng | Test bắt được |\n|---|---|---|\n| Quên cập nhật `tail` khi xoá nút cuối | `them_cuoi` sau đó ghi vào hư không | thêm sau khi xoá hết |\n| Quên cập nhật `tail` khi đảo ngược | `them_cuoi` nối sai chỗ | `dao_nguoc()` rồi `them_cuoi()` |\n| Sai thứ tự gán con trỏ | **nút tự trỏ vào mình**, lặp vô hạn | duyệt sau khi chèn |",

loi_thuong_gap: [
  "**Nói \"chèn/xoá là O(1)\" mà bỏ vế điều kiện.** Chỉ O(1) khi **đã cầm** nút trước đó; biết chỉ số thì vẫn O(n).",
  "**Gán con trỏ sai thứ tự.** Cắt trước nối sau làm nút tự trỏ vào chính nó và mất phần đuôi.",
  "**Quên lưu `next` trước khi ghi đè** trong thuật toán đảo ngược — mất toàn bộ phần còn lại.",
  "**Quên cập nhật `tail` khi xoá nút cuối cùng.** `tail` trỏ vào nút đã tháo; thêm cuối sau đó ghi vào hư không.",
  "**Quên cập nhật `tail` sau khi đảo ngược.** Lỗi im lặng, chỉ lộ ra khi thêm cuối sau đó.",
  "**Không gán `next = None` cho nút đã tháo.** Nút bị xoá vẫn trỏ vào danh sách, GC khó thu hồi.",
  "**Viết nhánh `if` riêng cho nút đầu thay vì dùng nút giả.** Nhiều nhánh nghĩa là nhiều chỗ sai.",
  "**Duyệt danh sách vòng bằng `while nut is not None`.** Lặp vô hạn — phải dừng theo số lượng hoặc theo nút bắt đầu.",
  "**Quên rằng danh sách vòng tạo chu trình tham chiếu.** Bộ đếm tham chiếu không giải phóng được, phải chờ GC theo thế hệ.",
  "**Dùng danh sách đơn khi cần xoá nút đã cầm.** Danh sách đơn phải duyệt tìm nút trước ⇒ O(n); danh sách đôi cho O(1).",
  "**Tự viết linked list trong Python cho việc thật.** `deque` cho O(1) hai đầu, viết bằng C, nhanh hơn và tốn ít bộ nhớ hơn.",
  "**Tin rằng linked list thêm đầu nhanh hơn `list`.** Phép đo cho thấy ngược lại — cấp phát `PyObject` và tra thuộc tính rất đắt.",
  "**So sánh chèn giữa mà quên tính chi phí TÌM.** Cả hai đều O(n); `list` thường thắng vì dịch bằng C.",
  "**Quên `__slots__` cho lớp nút.** Mỗi instance mang thêm một `__dict__` — tốn đáng kể với hàng trăm nghìn nút.",
  "**Dùng đệ quy để duyệt linked list dài.** Python giới hạn độ sâu ~1000 ⇒ `RecursionError`."
],

tu_kiem_tra: [
  "Vì sao linked list không có truy cập ngẫu nhiên O(1)? Liên hệ với công thức địa chỉ của mảng.",
  "Phát biểu đầy đủ câu \"chèn/xoá là O(1)\". Vế bị thiếu là gì và vì sao nó quan trọng?",
  "Nêu thứ tự đúng của hai phép gán khi chèn nút. Chuyện gì xảy ra nếu làm ngược?",
  "Vì sao danh sách **đôi** xoá được O(1) khi cầm nút, còn danh sách **đơn** thì không?",
  "Nút giả giải quyết vấn đề gì? Vì sao danh sách đôi thường dùng hai nút giả?",
  "So sánh ba biến thể về bộ nhớ, khả năng đi ngược, và chi phí xoá cuối.",
  "Nêu hai cạm bẫy riêng của danh sách vòng.",
  "Vì sao chu trình tham chiếu cần garbage collector theo thế hệ?",
  "Nêu bốn lý do linked list chậm trong Python.",
  "Phép đo cho thấy linked list thua `list` ngay ở thao tác thêm đầu — giải thích vì sao.",
  "Khi so sánh chèn giữa, vì sao `list` thường thắng dù \"lý thuyết\" nói linked list chèn O(1)?",
  "Nêu ba trường hợp linked list vẫn đáng dùng trong Python.",
  "Vì sao LRU cache cần **cả** `dict` lẫn danh sách đôi? Mỗi cấu trúc giải quyết phần nào?",
  "Trình bày thuật toán đảo ngược bằng ba con trỏ. Bước nào hay quên nhất?",
  "Kỹ thuật hai con trỏ tìm nút giữa hoạt động thế nào? Danh sách chẵn phần tử thì trả nút nào?",
  "Trong `gop_hai_da_sap`, `duoi.next = a or b` làm gì và vì sao nó đúng?",
  "Nêu ba lỗi dễ mắc khi tự cài linked list và test nào bắt được mỗi lỗi.",
  "Vì sao lớp nút nên có `__slots__`?"
],

tai_nguyen: [
  "Sách **Introduction to Algorithms** — CLRS. Chương 10 về danh sách liên kết, kèm kỹ thuật nút giả (sentinel) được trình bày chặt chẽ.",
  "Sách **Algorithms** — Sedgewick & Wayne. Phần về linked list với hình vẽ từng bước cho các thao tác nối lại con trỏ.",
  "**Mã nguồn CPython — `Modules/_collectionsmodule.c`** — hiện thực `deque` bằng danh sách liên kết các **khối**; đọc để thấy cách kết hợp ưu điểm của cả hai cấu trúc.",
  "**Mã nguồn CPython — `Lib/functools.py`**, phần `lru_cache` — hiện thực LRU bằng danh sách liên kết đôi vòng kết hợp `dict`, đúng như ví dụ trong bài.",
  "**Tài liệu Python — `weakref` và `gc`** — công cụ để quan sát chu trình tham chiếu của danh sách vòng."
],

lien_he: [
  "**Array và dynamic array** ngay trước: hai cấu trúc đối lập hoàn hảo; mảng trả giá vì giữ liên tục, linked list trả giá vì bỏ liên tục.",
  "**Ký hiệu Big-O** ở module 1 mảng này: \"chèn O(1)\" là ví dụ điển hình của một bậc đúng về lý thuyết mà sai về điều kiện áp dụng.",
  "**Độ phức tạp không gian** ở module 1 mảng này: vì sao duyệt linked list bằng đệ quy gây `RecursionError`.",
  "**Stack LIFO** ngay sau: ngăn xếp cài bằng danh sách đơn là ứng dụng tự nhiên nhất.",
  "**Queue FIFO** trong module này: hàng đợi cần cả `head` lẫn `tail`.",
  "**Deque hai đầu** trong module này: `deque` là danh sách liên kết các **khối** — kết hợp ưu điểm của mảng và linked list.",
  "**Kỹ thuật con trỏ chậm/nhanh** cuối module: đi sâu vào kỹ thuật hai con trỏ giới thiệu ở đây.",
  "**Hash** ở module 3 mảng này: chaining dùng linked list cho các khoá va chạm.",
  "**Cây** ở module 4 mảng này: mỗi nút có **nhiều** con trỏ — tư duy con trỏ của bài này là nền trực tiếp.",
  "**Đồ thị** ở module 5 mảng này: danh sách kề, và duyệt đồ thị là thao tác con trỏ tổng quát hoá.",
  "**Đếm tham chiếu và GC** ở module 7 mảng 01: vì sao danh sách vòng cần GC theo thế hệ.",
  "**Dataclass và `__slots__`** ở module 6 mảng 01: giảm bộ nhớ mỗi nút.",
  "**Các khối xây dựng** ở mảng 06: LRU cache là chiến lược thay thế cache cơ bản nhất trong thiết kế hệ thống."
]

};
