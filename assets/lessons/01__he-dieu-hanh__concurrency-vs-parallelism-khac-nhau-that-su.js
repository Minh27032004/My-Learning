window.LESSONS["01:he-dieu-hanh:concurrency-vs-parallelism-khac-nhau-that-su"] = {

tom_tat: [
  "**Concurrency là cách cấu trúc chương trình** để xử lý nhiều việc đang dở. **Parallelism là thực sự chạy nhiều việc cùng lúc** trên nhiều lõi.",
  "Một máy **một lõi** vẫn concurrency được, nhưng **không thể** parallelism. Đây là phép thử phân biệt hai khái niệm.",
  "Concurrency giải bài toán **chờ đợi**; parallelism giải bài toán **tính toán**. Chọn sai công cụ là không cải thiện gì.",
  "Ba mô hình: **process** (parallelism thật) · **thread** (cả hai, nhưng bị GIL chặn trong Python) · **async** (concurrency thuần, một luồng).",
  "Async rẻ hơn thread hàng chục lần nhưng **không bị tước quyền** — một hàm tính toán nặng không `await` sẽ chặn toàn bộ chương trình."
],

can_biet_truoc: [
  "Bài **Process vs thread** — cần biết khác biệt về bộ nhớ và GIL.",
  "Bài **Lập lịch CPU và context switch** — cần biết chi phí chuyển đổi.",
  "Bài **Deadlock, race condition** — cần biết vì sao dùng chung dữ liệu nguy hiểm."
],

dinh_nghia: "Nói đời thường: một đầu bếp nấu ba món cùng lúc — bỏ gà vào lò, trong lúc chờ thì thái rau, rồi khuấy nồi canh. Đó là **concurrency**: một người, ba việc đang dở, xoay vòng giữa các khoảng chờ. Còn **parallelism** là ba đầu bếp, mỗi người một món, thật sự làm cùng lúc.\n\nCâu nói kinh điển của Rob Pike, một trong những người tạo ra Go, tóm gọn:\n\n> *Concurrency là về **xử lý** nhiều việc cùng lúc. Parallelism là về **làm** nhiều việc cùng lúc.*\n\nNói chính xác:\n\n- **Concurrency** là thuộc tính của **cách cấu trúc chương trình**: chia công việc thành các tác vụ độc lập có thể tiến triển xen kẽ nhau.\n- **Parallelism** là thuộc tính của **cách thực thi**: nhiều phép tính thật sự diễn ra tại cùng một thời điểm vật lý, đòi hỏi nhiều đơn vị xử lý.",

vi_sao_can: "Vì chọn sai công cụ là **không cải thiện gì mà còn phức tạp thêm**:\n\n- **Dùng async cho việc nặng CPU.** Không nhanh hơn chút nào, còn làm code khó đọc hơn hẳn. Async chỉ giúp khi đang *chờ*.\n- **Dùng thread cho việc nặng CPU trong Python.** GIL chặn — bạn được concurrency nhưng không được parallelism.\n- **Dùng process cho việc nhẹ và nhiều.** Mỗi process tốn hàng MB; 1.000 process là không khả thi.\n- **Hiểu vì sao Node.js một luồng mà chịu tải cao.** Nó concurrency rất tốt qua event loop, dù không parallelism.\n- **Cấu hình server đúng.** Uvicorn với `--workers` là parallelism, `async def` là concurrency. Hai thứ khác nhau và bổ sung cho nhau.\n\nTình huống thật: bạn viết API gọi ba dịch vụ ngoài, mỗi cái mất 1 giây. Viết tuần tự mất 3 giây. Chuyển sang `async` và `asyncio.gather` còn **1 giây** — trên đúng một lõi CPU, không có gì chạy song song cả. Đó là concurrency thuần tuý, và nó giải đúng bài toán.",

co_che: "### Phép thử phân biệt: một lõi\n\nCách chắc chắn nhất để phân biệt hai khái niệm:\n\n```\nMáy MỘT lõi:\n  Concurrency  ✓  làm được — xen kẽ các tác vụ\n  Parallelism  ✗  không thể — chỉ có một đơn vị xử lý\n\nMáy NHIỀU lõi:\n  Concurrency  ✓\n  Parallelism  ✓\n```\n\nNếu một khái niệm vẫn thực hiện được trên máy một lõi, đó là concurrency.\n\n### Nhìn theo trục thời gian\n\n```\nTUẦN TỰ (một lõi, không concurrency)\n  Việc A: ████████\n  Việc B:         ████████\n  Việc C:                 ████████\n                                  └─ tổng 24 đơn vị\n\nCONCURRENCY (một lõi, xen kẽ)\n  Việc A: ██░░██░░██░░\n  Việc B: ░░██░░██░░██\n  Việc C: ░░░░░░░░░░░░████████\n          └─ tổng thời gian KHÔNG giảm nếu là việc tính toán\n             nhưng GIẢM MẠNH nếu các ô ░ là lúc CHỜ\n\nPARALLELISM (ba lõi)\n  Lõi 1 — Việc A: ████████\n  Lõi 2 — Việc B: ████████\n  Lõi 3 — Việc C: ████████\n                          └─ tổng 8 đơn vị, nhanh gấp 3\n```\n\nĐiểm mấu chốt nằm ở hàng giữa: **concurrency chỉ tăng tốc khi có thời gian chờ để lấp**. Với việc tính toán thuần trên một lõi, xen kẽ không giúp gì — còn tốn thêm chi phí chuyển đổi.\n\n### Hai loại công việc\n\nĐây là phân loại quyết định mọi lựa chọn về sau:\n\n| | Nặng I/O | Nặng CPU |\n|---|---|---|\n| Thời gian đi đâu | **Chờ** mạng, đĩa, database | **Tính toán** trong CPU |\n| Ví dụ | Gọi API, truy vấn DB, đọc file | Nén ảnh, huấn luyện model, sắp xếp |\n| Cần gì | **Concurrency** | **Parallelism** |\n| Công cụ đúng | async, thread | process, thư viện C, GPU |\n| Trần lý thuyết | Rất cao (hàng nghìn) | **Bằng số lõi** |\n\nCâu hỏi đầu tiên phải hỏi khi tối ưu: **thời gian đang đi đâu — chờ hay tính?** Trả lời sai câu này là chọn sai mọi thứ sau đó.\n\n### Event loop: concurrency không cần thread\n\nAsync hoạt động bằng một vòng lặp đơn giản đến bất ngờ:\n\n```\nvòng lặp sự kiện:\n    chạy tác vụ cho tới khi nó gặp await (tức là phải chờ)\n    ghi nhận \"tác vụ này đang chờ sự kiện X\"\n    chuyển sang tác vụ khác đã sẵn sàng\n    khi sự kiện X xảy ra → đánh dấu tác vụ đó sẵn sàng trở lại\n```\n\n**Toàn bộ việc này diễn ra trên MỘT thread.** Không có context switch của hệ điều hành, không cần vào nhân, không có khoá.\n\nĐiểm khác biệt cốt lõi so với thread nằm ở chỗ **ai quyết định chuyển**:\n\n```\nTHREAD — bị tước quyền (preemptive)\n  Hệ điều hành có thể ngắt BẤT KỲ ĐÂU, kể cả giữa x += 1\n  → race condition xảy ra được\n  → an toàn tự động: một thread lỗi không treo cả chương trình\n\nASYNC — hợp tác (cooperative)\n  Chỉ chuyển tại các điểm await, do BẠN đặt\n  → giữa hai await là vùng nguyên tử, ít race condition hơn hẳn\n  → nguy hiểm: một tác vụ không await sẽ chặn TẤT CẢ\n```\n\nĐây là đánh đổi trung tâm của async, và nó là con dao hai lưỡi thật sự.\n\n### Vì sao await là điểm nguy hiểm\n\n```python\n# Trông có vẻ an toàn vì chỉ có một thread — nhưng KHÔNG\nso_du = await lay_so_du(tk)        # ← có thể chuyển sang tác vụ khác Ở ĐÂY\nawait ghi_so_du(tk, so_du - 100)   # tác vụ kia có thể đã đổi số dư rồi\n```\n\nGiữa hai dòng có một điểm `await`, và event loop có thể chạy tác vụ khác ngay tại đó. Race condition vẫn xảy ra — chỉ là **các điểm nguy hiểm giờ nhìn thấy được**, vì chúng luôn nằm đúng ở chỗ có chữ `await`.\n\nĐây là ưu điểm thật của async: bạn **đọc được** chỗ nào có thể bị chen ngang, thay vì phải giả định mọi dòng đều nguy hiểm như với thread.\n\n### Vì sao một hàm nặng CPU chặn toàn bộ\n\n```python\nasync def xu_ly():\n    tong = 0\n    for i in range(100_000_000):   # 5 giây tính toán, KHÔNG có await\n        tong += i\n    return tong\n```\n\nTrong 5 giây đó, event loop **không giành lại được quyền điều khiển**. Mọi request khác treo. Không có hệ điều hành nào cứu, vì async không bị tước quyền.\n\nVới thread thì hệ điều hành cưỡng chế cắt sau vài mili giây, nên các thread khác vẫn tiến triển.\n\nCách xử lý đúng: đẩy phần nặng ra khỏi event loop.\n\n```python\nimport asyncio\nfrom concurrent.futures import ProcessPoolExecutor\n\nasync def xu_ly_dung():\n    vong = asyncio.get_running_loop()\n    with ProcessPoolExecutor() as pool:\n        # Chạy ở PROCESS khác → event loop rảnh phục vụ request khác\n        return await vong.run_in_executor(pool, ham_nang_cpu)\n```\n\n### Ba mô hình, ba chi phí\n\n| | Process | Thread | Async |\n|---|---|---|---|\n| Concurrency | ✓ | ✓ | ✓ |\n| Parallelism thật | **✓** | ✓ (không trong Python) | **✗** |\n| Bộ nhớ mỗi đơn vị | ~10 MB | ~1 MB | **~1 KB** |\n| Chi phí chuyển | ~5–20 µs | ~1–5 µs | **~0,1 µs** |\n| Số lượng chịu được | hàng trăm | hàng nghìn | **hàng trăm nghìn** |\n| Bị tước quyền | ✓ | ✓ | **✗** |\n| Race condition | Không | **Có, khắp nơi** | Có, nhưng chỉ tại `await` |\n\nBa dòng cuối là chỗ đánh đổi thật sự nằm. Async thắng tuyệt đối về quy mô và chi phí, nhưng phải trả bằng **tính an toàn tự động**.",

vi_du: "### Concurrency giải bài toán chờ\n\n```python\nimport asyncio, time\n\nasync def goi_dich_vu(ten, giay):\n    print(f\"  bắt đầu {ten}\")\n    await asyncio.sleep(giay)        # mô phỏng chờ mạng — NHẢ quyền điều khiển\n    print(f\"  xong {ten}\")\n    return ten\n\nasync def tuan_tu():\n    t = time.perf_counter()\n    await goi_dich_vu(\"A\", 1)\n    await goi_dich_vu(\"B\", 1)\n    await goi_dich_vu(\"C\", 1)\n    print(f\"tuần tự: {time.perf_counter()-t:.2f}s\")     # ~3,0s\n\nasync def dong_thoi():\n    t = time.perf_counter()\n    await asyncio.gather(                # ← ba tác vụ cùng tiến triển\n        goi_dich_vu(\"A\", 1),\n        goi_dich_vu(\"B\", 1),\n        goi_dich_vu(\"C\", 1),\n    )\n    print(f\"đồng thời: {time.perf_counter()-t:.2f}s\")   # ~1,0s\n\nasyncio.run(tuan_tu())\nasyncio.run(dong_thoi())\n```\n\nNhanh gấp ba, trên **một lõi duy nhất**, không có thread nào. Ba cuộc chờ chồng lên nhau thành một. Đây là concurrency thuần tuý.\n\n### Async KHÔNG giúp cho việc nặng CPU\n\n```python\nimport asyncio, time\n\nasync def nang_cpu(n=20_000_000):\n    tong = 0\n    for i in range(n):               # không có await → không nhả quyền\n        tong += i\n    return tong\n\nasync def thu():\n    t = time.perf_counter()\n    await asyncio.gather(nang_cpu(), nang_cpu(), nang_cpu())\n    print(f\"3 tác vụ CPU với asyncio: {time.perf_counter()-t:.2f}s\")\n\nasyncio.run(thu())\n# ~3× thời gian một tác vụ — KHÔNG nhanh hơn tuần tự chút nào\n```\n\nKhông có `await` nào bên trong nên không có chỗ để xen kẽ. `asyncio.gather` chỉ khiến chúng chạy lần lượt. Đây là sai lầm rất phổ biến khi mới học async.\n\n### Một tác vụ chặn cả chương trình\n\n```python\nimport asyncio, time\n\nasync def nhip_tim():\n    for i in range(5):\n        print(f\"  nhịp {i}\")\n        await asyncio.sleep(0.5)\n\nasync def ke_pha_hoai():\n    await asyncio.sleep(0.6)\n    print(\"  >>> bắt đầu tính toán nặng\")\n    time.sleep(2)                    # ❌ sleep ĐỒNG BỘ — chặn cả event loop\n    print(\"  >>> xong\")\n\nasync def main():\n    await asyncio.gather(nhip_tim(), ke_pha_hoai())\n\nasyncio.run(main())\n```\n\nKết quả: nhịp tim in được vài lần rồi **im bặt 2 giây**. Toàn bộ chương trình đứng vì một tác vụ không nhả quyền.\n\nSửa bằng cách đẩy sang thread pool:\n\n```python\nasync def sua_dung():\n    await asyncio.sleep(0.6)\n    vong = asyncio.get_running_loop()\n    # time.sleep chạy ở thread khác → event loop vẫn rảnh\n    await vong.run_in_executor(None, time.sleep, 2)\n```\n\n### Chọn đúng công cụ — so sánh trực tiếp\n\n```python\nimport time, asyncio, threading\nfrom concurrent.futures import ProcessPoolExecutor\n\ndef nang_cpu(n=15_000_000):\n    x = 0\n    for i in range(n): x += i\n    return x\n\ndef nang_io():\n    time.sleep(1)\n\nif __name__ == \"__main__\":\n    # --- Việc nặng CPU ---\n    t = time.perf_counter(); [nang_cpu() for _ in range(4)]\n    print(f\"CPU tuần tự : {time.perf_counter()-t:.2f}s\")\n\n    t = time.perf_counter()\n    ws = [threading.Thread(target=nang_cpu) for _ in range(4)]\n    [w.start() for w in ws]; [w.join() for w in ws]\n    print(f\"CPU + thread: {time.perf_counter()-t:.2f}s   ← không cải thiện (GIL)\")\n\n    t = time.perf_counter()\n    with ProcessPoolExecutor(4) as p:\n        list(p.map(nang_cpu, [15_000_000]*4))\n    print(f\"CPU + process: {time.perf_counter()-t:.2f}s  ← nhanh ~4 lần ✓\")\n\n    # --- Việc nặng I/O ---\n    t = time.perf_counter(); [nang_io() for _ in range(8)]\n    print(f\"I/O tuần tự : {time.perf_counter()-t:.2f}s\")\n\n    t = time.perf_counter()\n    ws = [threading.Thread(target=nang_io) for _ in range(8)]\n    [w.start() for w in ws]; [w.join() for w in ws]\n    print(f\"I/O + thread: {time.perf_counter()-t:.2f}s   ← nhanh ~8 lần ✓\")\n```\n\nBảng kết quả tiêu biểu:\n\n| | Tuần tự | Thread | Process |\n|---|---|---|---|\n| Nặng CPU | 4,0s | 4,1s ✗ | **1,1s** ✓ |\n| Nặng I/O | 8,0s | **1,0s** ✓ | 1,2s (thừa) |\n\nBảng này là toàn bộ bài học, gói trong một lần chạy.\n\n### Kết hợp cả hai trong thực tế\n\n```python\n# Uvicorn: mỗi worker là một PROCESS, bên trong mỗi worker là event loop async\n# uvicorn app:app --workers 4\n#\n#   Process 1 ─ event loop ─ hàng nghìn kết nối đồng thời\n#   Process 2 ─ event loop ─ hàng nghìn kết nối đồng thời\n#   Process 3 ─ event loop ─ ...\n#   Process 4 ─ event loop ─ ...\n#   └── parallelism (4 lõi)   └── concurrency (trong mỗi lõi)\n```\n\nĐây là kiến trúc chuẩn của server Python hiện đại: **process cho parallelism, async cho concurrency**. Hai cơ chế giải hai bài toán khác nhau nên bổ sung cho nhau chứ không thay thế.\n\nSố worker thường đặt bằng số lõi. Đặt nhiều hơn chỉ làm tăng chi phí chuyển ngữ cảnh.",

so_sanh: "### Bảng quyết định\n\n```\nCâu hỏi đầu tiên: thời gian đi đâu?\n\n├─ CHỜ (mạng, đĩa, database)  → cần CONCURRENCY\n│    ├─ vài chục việc, code đồng bộ có sẵn  → thread pool\n│    └─ hàng nghìn kết nối                  → async ⭐\n│\n└─ TÍNH (CPU chạy hết công suất) → cần PARALLELISM\n     ├─ Python thuần        → multiprocessing\n     ├─ Có thư viện C sẵn   → numpy, pandas (đã nhả GIL) ⭐\n     └─ Rất nặng, song song hoá được → GPU\n```\n\nNhánh áp chót đáng chú ý: với việc nặng CPU trong Python, **đẩy xuống numpy thường tốt hơn multiprocessing** — không tốn chi phí tạo process, không phải tuần tự hoá dữ liệu qua lại, và code đơn giản hơn hẳn.\n\n### Đo trước khi chọn\n\n```bash\n# Linux — xem chương trình đang chờ hay đang tính\ntop\n#   %CPU gần 100%  → nặng CPU  → cần parallelism\n#   %CPU thấp, %wa cao → nặng I/O → cần concurrency\n```\n\nĐây là bước hầu hết người ta bỏ qua. Đoán sai loại công việc là mọi nỗ lực tối ưu sau đó vô nghĩa.\n\n### Các ngôn ngữ giải bài toán này ra sao\n\n| Ngôn ngữ | Cách tiếp cận | Đặc điểm |\n|---|---|---|\n| **Python** | async + multiprocessing | GIL buộc phải tách hai hướng |\n| **Go** | goroutine + channel | Runtime tự trải goroutine lên nhiều lõi — **được cả hai** |\n| **JavaScript** | event loop | Một luồng, concurrency rất tốt, parallelism qua Worker |\n| **Java** | thread thật + virtual thread | Không có GIL nên thread cho parallelism thật |\n| **Rust** | async + thread | Trình biên dịch **chặn race condition ngay lúc build** |\n\nGo đáng học ở chỗ nó khiến sự phân biệt này gần như biến mất với người dùng: bạn viết `go ham()` và runtime tự lo việc trải lên nhiều lõi. Goroutine chỉ tốn ~2 KB nên chạy hàng triệu cái được, và runtime tự động dùng hết số lõi có sẵn.\n\nĐó là lý do Go được chọn nhiều cho dịch vụ mạng — nó cho cả concurrency lẫn parallelism với một mô hình lập trình duy nhất.\n\nRust đi hướng khác: hệ thống kiểu của nó **từ chối biên dịch** code có khả năng race condition. Đây gọi là *fearless concurrency* — bạn viết code song song mà không phải sợ, vì trình biên dịch đã kiểm tra hộ.",

loi_thuong_gap: [
  "**Dùng async cho việc nặng CPU.** Không nhanh hơn chút nào vì không có chỗ để xen kẽ. Dùng process hoặc thư viện C.",
  "**Gọi hàm chặn trong hàm async.** `time.sleep`, `requests.get`, `open().read()` đều chặn cả event loop. Dùng bản async (`asyncio.sleep`, `httpx`, `aiofiles`) hoặc `run_in_executor`.",
  "**Tưởng `async def` tự động làm code nhanh hơn.** Nó chỉ cho phép xen kẽ tại các điểm `await`. Không có `await` thì không có gì đồng thời.",
  "**Quên `await`.** `ham_async()` không có `await` chỉ tạo ra một coroutine chưa chạy. Python cảnh báo nhưng dễ bị bỏ qua.",
  "**Tưởng async miễn nhiễm race condition.** Vẫn xảy ra tại các điểm `await` — chỉ là các điểm đó nhìn thấy được.",
  "**Tạo hàng nghìn thread cho I/O.** Mỗi thread ~1 MB stack. Quá vài trăm thì chuyển sang async.",
  "**Trộn `asyncio` với `threading` mà không cẩn thận.** Gọi hàm asyncio từ thread khác cần `run_coroutine_threadsafe`.",
  "**Không đo trước khi tối ưu.** Chạy `top` xem `%CPU` — đó là dữ liệu quyết định chọn công cụ nào."
],

tu_kiem_tra: [
  "Phân biệt concurrency và parallelism trong một câu. Dùng phép thử \"máy một lõi\" để kiểm chứng.",
  "Chương trình gọi ba API, mỗi cái 1 giây. Async giảm còn 1 giây. Có phải nó đã chạy song song không?",
  "Vì sao `asyncio.gather` với ba hàm tính toán nặng lại không nhanh hơn chạy tuần tự?",
  "Vì sao một hàm async không có `await` có thể làm treo cả server, trong khi với thread thì không?",
  "Trong async, race condition có xảy ra không? Nếu có thì ở đâu, và vì sao điều đó lại là một ưu điểm?",
  "Bạn có 8 lõi và cần xử lý 10.000 kết nối HTTP đồng thời. Kiến trúc nào phù hợp, và vì sao?",
  "Vì sao goroutine của Go chạy được hàng triệu cái còn thread hệ điều hành thì không?"
],

tai_nguyen: [
  "Bài nói **Concurrency is not Parallelism** — Rob Pike. Bài thuyết trình kinh điển định nghĩa sự phân biệt này.",
  "Tài liệu Python, module `asyncio` — đọc mục *Developing with asyncio*, đặc biệt phần *Running Blocking Code*.",
  "Sách **Operating Systems: Three Easy Pieces**, phần *Concurrency* — nền tảng lý thuyết.",
  "Tài liệu Python, `concurrent.futures` — `ThreadPoolExecutor` và `ProcessPoolExecutor` là cách đơn giản nhất để bắt đầu."
],

lien_he: [
  "**Process vs thread**: ba mô hình trong bài này chính là ba cách hiện thực hoá concurrency, mỗi cách một chi phí.",
  "**Lập lịch CPU và context switch**: async rẻ hơn vì nó chuyển đổi trong không gian người dùng, không cần vào nhân.",
  "**Deadlock & race condition**: async giảm race condition vì các điểm chuyển nhìn thấy được, nhưng không loại bỏ hẳn.",
  "**Tín hiệu và tiến trình nền** (bài sau): xử lý tắt máy êm ái trong server async cần hiểu cả tín hiệu lẫn event loop.",
  "**Backend & Web** ở mảng 05: `async def` trong FastAPI và số worker của Uvicorn là hai lựa chọn khác nhau — một cho concurrency, một cho parallelism.",
  "**Hiệu năng & vận hành** ở mảng 05: bước đầu tiên luôn là xác định nghẽn ở I/O hay ở CPU, trước khi chọn bất kỳ công cụ nào.",
  "**Huấn luyện model** ở mảng 07: `DataLoader` dùng process để nạp dữ liệu song song trong khi GPU đang tính — kết hợp cả hai đúng như kiến trúc server đã mô tả."
]

};
