window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:venv-pip-requirements-txt-pyproject-toml"] = {

tom_tat: [
  "**Một môi trường ảo cho mỗi dự án — không có ngoại lệ.** Cài thư viện vào Python hệ thống là con đường chắc chắn dẫn tới xung đột phiên bản không gỡ được.",
  "**Môi trường ảo không có gì huyền bí**: nó chỉ là một thư mục có `python` riêng và `site-packages` riêng, cộng với việc đổi `PATH` khi kích hoạt.",
  "**Luôn gõ `python -m pip`, đừng gõ `pip`.** Đây là cách duy nhất chắc chắn cài vào đúng bản Python đang chạy — thói quen này xoá bỏ cả lớp lỗi \"đã cài rồi mà vẫn không import được\".",
  "**Phân biệt hai loại phụ thuộc**: cái bạn *khai báo* cần (khoảng phiên bản rộng) và cái bạn *thật sự đã cài* (khoá cứng từng phiên bản). Hai thứ ở hai file khác nhau và phục vụ hai mục đích khác nhau.",
  "**`pyproject.toml` là chuẩn hiện nay** cho cả khai báo phụ thuộc lẫn cấu hình công cụ. `requirements.txt` vẫn dùng, nhưng cho vai trò khoá phiên bản."
],

can_biet_truoc: [
  "Bài **Module, package, import** — `sys.path` và `site-packages`; môi trường ảo chính là đổi chỗ đó.",
  "Bài **Biến môi trường và PATH** ở module 3 — kích hoạt môi trường ảo là sửa `PATH`.",
  "Bài **PEP 8 và format tự động** ngay trước — `pyproject.toml` là nơi chứa cấu hình các công cụ đó.",
  "Bài **Điều hướng và thao tác file** ở module 3 — làm việc với thư mục dự án."
],

dinh_nghia: "Nói đời thường: cài thư viện vào Python hệ thống giống như **để chung đồ của mọi dự án vào một cái tủ**. Dự án A cần bản cũ, dự án B cần bản mới — không thể có cả hai. Môi trường ảo là **cho mỗi dự án một cái tủ riêng**.\n\nNói chính xác:\n\n- **Môi trường ảo** (*virtual environment*) — một thư mục chứa bản sao (hoặc liên kết) của trình thông dịch Python cùng một `site-packages` riêng.\n- **`site-packages`** — thư mục nơi `pip` cài thư viện vào, và nằm trong `sys.path`.\n- **Kích hoạt** (*activate*) — thêm thư mục `bin`/`Scripts` của môi trường vào đầu `PATH`, để `python` và `pip` trỏ vào bản của môi trường đó.\n- **`pip`** — công cụ cài gói từ kho phần mềm Python.\n- **`requirements.txt`** — danh sách gói, mỗi dòng một gói. Đơn giản, không có cấu trúc.\n- **`pyproject.toml`** — file cấu hình chuẩn của dự án Python: metadata, phụ thuộc, và cấu hình mọi công cụ.\n- **Cài chế độ chỉnh sửa** (*editable*, `pip install -e .`) — cài dự án sao cho sửa mã nguồn là có hiệu lực ngay, không phải cài lại.\n\nĐiểm cần nắm ngay: **kích hoạt môi trường ảo không phải phép màu**, nó chỉ sửa `PATH`. Bạn hoàn toàn có thể không kích hoạt mà gọi thẳng `./moi_truong/bin/python` — kết quả y hệt. Hiểu điều này làm mọi lỗi liên quan trở nên chẩn đoán được.",

vi_sao_can: "Đây là bài về hạ tầng, và bỏ qua nó là chuốc lấy một loại khổ sở rất đặc trưng của Python:\n\n- **Xung đột phiên bản là chuyện chắc chắn xảy ra.** Dự án cũ cần thư viện bản 1.x, dự án mới cần 2.x, và hai bản không tương thích. Không có môi trường ảo thì bạn phải chọn một dự án để làm hỏng.\n- **\"Chạy được trên máy tôi\" gần như luôn là lỗi phụ thuộc.** Máy bạn có sẵn một thư viện được cài từ lâu; máy đồng nghiệp thì không. Không khoá phiên bản thì không tái lập được môi trường.\n- **Triển khai đòi hỏi tái lập chính xác.** Server phải chạy đúng những phiên bản đã kiểm thử. Sai một phiên bản là hành vi khác đi mà không ai biết.\n- **Đóng gói để dùng lại.** Muốn `import goi_cua_toi` từ bất cứ đâu, muốn chia sẻ cho người khác — đều bắt đầu từ `pyproject.toml`.\n- **Định hướng AI/ML.** Các thư viện học máy có ràng buộc phiên bản rất chặt với nhau và với phiên bản CUDA. Đây là lĩnh vực mà môi trường ảo không phải \"nên có\" mà là **bắt buộc**.\n\nTình huống thật rất kinh điển: bạn `pip install` một thư viện cho dự án mới, và dự án cũ **đột nhiên hỏng** — vì thư viện mới kéo theo một bản cập nhật của gói phụ thuộc chung. Không có bản ghi phiên bản cũ, không có cách quay lại, và bạn mất cả buổi để dựng lại. Phần cơ chế sẽ nói cách làm cho tình huống này **không thể xảy ra**.",

co_che: "### Môi trường ảo thật ra là gì\n\n```bash\npython -m venv moi_truong\n```\n\n```\nmoi_truong/\n├── bin/                    (Windows: Scripts/)\n│   ├── python              → liên kết tới Python hệ thống\n│   ├── pip\n│   └── activate            script sửa PATH\n├── lib/python3.12/site-packages/     ⭐ nơi pip cài vào\n└── pyvenv.cfg              trỏ về Python gốc\n```\n\n**Không có bản sao Python đầy đủ** trong đó — chỉ có một liên kết tới bản gốc cộng thư mục `site-packages` riêng. Đây là lý do tạo môi trường ảo rất nhanh và chiếm rất ít chỗ.\n\nKích hoạt làm đúng một việc:\n\n```bash\nsource moi_truong/bin/activate      # Linux, macOS\nmoi_truong\\Scripts\\activate         # Windows CMD\n. moi_truong/Scripts/activate       # Windows Git Bash\n\n# Kiểm chứng\nwhich python                        # .../moi_truong/bin/python\necho $PATH | tr ':' '\\n' | head -1  # .../moi_truong/bin — ⭐ ĐẦU tiên\n\ndeactivate                          # khôi phục PATH cũ\n```\n\nVì `PATH` được tra **từ trái sang phải** (đúng bài PATH ở module 3), `python` giờ trỏ vào bản trong môi trường. Đó là toàn bộ cơ chế.\n\nHệ quả thực dụng: **không kích hoạt cũng dùng được**, chỉ cần gọi đường dẫn đầy đủ:\n\n```bash\n./moi_truong/bin/python script.py\n./moi_truong/bin/python -m pip install requests\n```\n\nCách này rất tiện trong script tự động và trong `cron`, nơi không có shell tương tác để kích hoạt.\n\n### Vì sao luôn `python -m pip`\n\n```bash\n# ❌ Rủi ro khi máy có nhiều bản Python\npip install requests\n\n# ✓ Chắc chắn cài vào ĐÚNG Python đang dùng\npython -m pip install requests\n```\n\nVấn đề: lệnh `pip` là một file thực thi riêng, và nó có thể thuộc về một bản Python khác với bản mà `python` đang trỏ tới. Kết quả là **cài vào một nơi, chạy ở nơi khác** — và thông báo lỗi (`ModuleNotFoundError`) không hề gợi ý nguyên nhân này.\n\n```bash\n# Chẩn đoán khi nghi ngờ\nwhich python; which pip\npython -c \"import sys; print(sys.executable)\"\npython -m pip --version        # in ra pip này thuộc Python nào\npython -m pip show requests    # xem cài ở đâu\n```\n\n`python -m pip` loại bỏ hoàn toàn khả năng lệch nhau, vì nó dùng đúng trình thông dịch đang chạy để gọi `pip`. Đây là thói quen đáng rèn.\n\n### Cách `pip` giải quyết phụ thuộc\n\n```bash\npython -m pip install requests\n```\n\n```\n① Tìm gói trên kho phần mềm\n② Đọc danh sách phụ thuộc CỦA NÓ\n③ Cài đệ quy mọi phụ thuộc\n④ Chọn phiên bản thoả MỌI ràng buộc đã biết\n```\n\nCú pháp ràng buộc phiên bản:\n\n```\nrequests              bản mới nhất — ⚠️ không tái lập được\nrequests==2.31.0      ĐÚNG bản này\nrequests>=2.28        từ 2.28 trở lên\nrequests>=2.28,<3     từ 2.28, dưới 3.0 — ⭐ mẫu an toàn thông dụng\nrequests~=2.28.0      tương đương >=2.28.0,<2.29 — cho phép bản vá\n```\n\nDòng `>=2.28,<3` phản ánh quy ước **đánh số phiên bản ngữ nghĩa**:\n\n```\nCHÍNH.PHỤ.VÁ      ví dụ 2.31.4\n  │    │   └── sửa lỗi, tương thích ngược\n  │    └────── thêm tính năng, tương thích ngược\n  └─────────── THAY ĐỔI PHÁ VỠ tương thích\n```\n\nVì vậy `<3` nghĩa là \"nhận mọi bản vá và tính năng mới, nhưng không nhận thay đổi phá vỡ\". Đây là mức ràng buộc cân bằng nhất cho thư viện.\n\nMột giới hạn thật cần biết: **`pip` không có chỗ cho hai phiên bản của cùng một gói trong một môi trường.** Nếu gói A cần `X<2` và gói B cần `X>=2`, không có lời giải — phải nâng cấp A, hạ cấp B, hoặc tách hai môi trường.\n\n### Hai loại phụ thuộc — phân biệt cho rõ\n\nĐây là ý quan trọng nhất của bài:\n\n```\nKHAI BÁO (abstract)   \"dự án tôi CẦN gì\"        khoảng rộng   pyproject.toml\nKHOÁ CỨNG (concrete)  \"tôi ĐÃ CÀI đúng gì\"      từng phiên bản  requirements.txt\n```\n\n```toml\n# pyproject.toml — cái bạn CẦN\ndependencies = [\n    \"requests>=2.28,<3\",\n    \"pandas>=2.0\",\n]\n```\n\n```\n# requirements.txt — cái đã CÀI, gồm cả phụ thuộc của phụ thuộc\nrequests==2.31.0\nurllib3==2.0.7          ← không tự cài, nhưng requests kéo theo\ncertifi==2023.11.17     ← cũng vậy\npandas==2.1.4\nnumpy==1.26.2\n```\n\nVì sao cần cả hai:\n\n```\nThư viện dùng chung → khai báo RỘNG, để người dùng ghép được với gói khác\nỨng dụng triển khai  → khoá CỨNG, để môi trường thật giống hệt môi trường đã test\n```\n\nMột thư viện khoá cứng phiên bản là gây khổ cho mọi người dùng nó — họ sẽ gặp xung đột không cần thiết. Một ứng dụng không khoá cứng là mở cửa cho \"chạy được trên máy tôi\".\n\n```bash\n# Sinh file khoá cứng từ môi trường hiện tại\npython -m pip freeze > requirements.txt\n\n# Tái lập chính xác ở nơi khác\npython -m pip install -r requirements.txt\n```\n\nCảnh báo về `pip freeze`: nó liệt kê **mọi thứ trong môi trường**, kể cả gói bạn cài để thử rồi quên. Nếu môi trường bẩn thì file khoá cũng bẩn. Đây là lý do nữa để mỗi dự án một môi trường sạch.\n\n### `pyproject.toml`\n\n```toml\n[build-system]\nrequires = [\"setuptools>=68\"]\nbuild-backend = \"setuptools.build_meta\"\n\n[project]\nname = \"goi-cua-toi\"\nversion = \"0.1.0\"\ndescription = \"Công cụ xử lý dữ liệu\"\nrequires-python = \">=3.10\"\ndependencies = [\n    \"requests>=2.28,<3\",\n    \"pandas>=2.0\",\n]\n\n[project.optional-dependencies]\ndev = [\"pytest>=7\", \"mypy>=1.8\"]        # ⭐ chỉ cần khi phát triển\n\n[project.scripts]\ncong-cu = \"goi_cua_toi.main:main\"        # ⭐ tạo lệnh dòng lệnh\n\n# Cấu hình công cụ — cùng một file, đúng bài trước\n[tool.mypy]\npython_version = \"3.12\"\n\n[tool.pytest.ini_options]\ntestpaths = [\"tests\"]\n```\n\nBa mục đáng nhấn:\n\n```\noptional-dependencies  người DÙNG không phải cài pytest và mypy\n                       → pip install .        chỉ phụ thuộc chính\n                       → pip install \".[dev]\" thêm công cụ phát triển\n\nscripts                sinh ra lệnh \"cong-cu\" gọi được từ terminal\n                       → không cần viết script khởi động riêng\n\n[tool.*]               MỌI công cụ đọc cấu hình từ đây\n                       → một file thay cho năm file cấu hình rải rác\n```\n\n### `pip install -e .` — cách sửa gốc cho `ModuleNotFoundError`\n\n```bash\npython -m pip install -e .\n```\n\nNó tạo một liên kết từ `site-packages` về thư mục mã nguồn của bạn, thay vì sao chép. Hai hệ quả:\n\n```\n① import goi_cua_toi hoạt động TỪ BẤT KỲ ĐÂU — không phụ thuộc chỗ đứng\n② Sửa mã nguồn là có hiệu lực NGAY — không phải cài lại\n```\n\nĐây chính là cách sửa đúng cho vấn đề đã nêu ở bài module: thay vì chèn `sys.path.insert(...)` vào từng file, bạn đăng ký dự án một lần vào môi trường.\n\nNối với bố cục `src/` đã nói ở bài đó: đặt code trong `src/` **buộc** bạn phải cài mới import được, nên bài kiểm thử chạy trên đúng thứ sẽ phát hành — bắt được lỗi thiếu file trong cấu hình đóng gói.\n\n### Không bao giờ commit thư mục môi trường ảo\n\n```gitignore\n# .gitignore\nmoi_truong/\n.venv/\nvenv/\n__pycache__/\n*.pyc\n.mypy_cache/\n.pytest_cache/\n*.egg-info/\ndist/\nbuild/\n```\n\nBa lý do, mỗi lý do đủ để không commit:\n\n```\n① Rất nặng — hàng trăm megabyte\n② Chứa đường dẫn tuyệt đối của máy bạn ⇒ máy khác dùng không được\n③ Có file nhị phân biên dịch cho ĐÚNG hệ điều hành của bạn\n```\n\nThứ cần commit là **`pyproject.toml` và `requirements.txt`** — công thức để dựng lại, không phải kết quả đã dựng.\n\n### Vòng đời một dự án\n\n```bash\n# ① Tạo và kích hoạt\nmkdir du_an && cd du_an\npython -m venv .venv\nsource .venv/bin/activate\n\n# ② Nâng pip trước — tránh lỗi khó hiểu với gói mới\npython -m pip install --upgrade pip\n\n# ③ Cài phụ thuộc\npython -m pip install requests pandas\npython -m pip install -e \".[dev]\"        # dự án + công cụ phát triển\n\n# ④ Khoá phiên bản để tái lập\npython -m pip freeze > requirements.txt\n\n# ⑤ Người khác dựng lại y hệt\ngit clone ... && cd du_an\npython -m venv .venv && source .venv/bin/activate\npython -m pip install -r requirements.txt\npython -m pip install -e .\n```\n\nBước ② hay bị bỏ và gây lỗi khó hiểu: `pip` cũ không hiểu định dạng gói mới, và thông báo lỗi thường nói về chuyện khác.\n\n### Kiểm tra và gỡ rối\n\n```bash\npython -m pip list                 # mọi gói đã cài\npython -m pip list --outdated      # gói có bản mới\npython -m pip show pandas          # thông tin: phiên bản, vị trí, phụ thuộc\npython -m pip check                # ⭐ kiểm tra phụ thuộc có xung đột không\n\n# Xem cây phụ thuộc — cần cài thêm công cụ\npython -m pip install pipdeptree\npipdeptree\n```\n\n`pip check` là lệnh đáng chạy sau mỗi lần cài gói mới: nó báo khi hai gói yêu cầu hai phiên bản không tương thích của cùng một phụ thuộc — tình huống mà `pip` có thể đã âm thầm cài một bản làm hỏng gói kia.\n\n### Khi môi trường hỏng\n\nMôi trường ảo là **thứ dùng rồi bỏ được**. Đừng mất thời gian sửa:\n\n```bash\ndeactivate\nrm -rf .venv\npython -m venv .venv\nsource .venv/bin/activate\npython -m pip install -r requirements.txt\n```\n\nToàn bộ mất chưa tới một phút, và đây là lý do mạnh nhất để **luôn có `requirements.txt` cập nhật**: nó biến một môi trường hỏng từ thảm hoạ thành phiền phức nhỏ.\n\n### Các công cụ thế hệ mới\n\nNgoài `venv` + `pip`, còn có những công cụ gộp nhiều việc lại:\n\n```\nvenv + pip     ⭐ có sẵn, không cài gì, đủ cho phần lớn dự án\n               ✗ phải tự quản lý hai file, không có file khoá chuẩn\n\nCông cụ gộp    ✓ quản lý môi trường + phụ thuộc + khoá phiên bản trong một\n               ✓ file khoá ghi cả mã băm, tái lập chính xác hơn\n               ✗ thêm một công cụ phải cài và phải học\n\nCông cụ cho    ✓ quản lý được cả gói ngoài Python (thư viện C, CUDA)\nkhoa học dữ    ✓ mạnh trong lĩnh vực AI/ML nơi phụ thuộc nhị phân phức tạp\nliệu           ✗ nặng, chậm hơn\n```\n\nLời khuyên thực dụng: **bắt đầu bằng `venv` + `pip`** vì nó có sẵn và bạn phải hiểu nó dù dùng công cụ nào. Chuyển sang công cụ khác khi gặp đúng vấn đề mà nó giải quyết — thường là khi cần tái lập tuyệt đối chính xác, hoặc khi làm việc với thư viện có phần nhị phân phức tạp.\n\nĐừng chọn công cụ theo độ mới. Mọi công cụ đều dựng trên cùng các khái niệm ở bài này: môi trường riêng, khai báo phụ thuộc, khoá phiên bản.",

vi_du: "### Dựng dự án từ đầu\n\n```bash\nmkdir phan_tich_log && cd phan_tich_log\npython -m venv .venv\nsource .venv/bin/activate\npython -m pip install --upgrade pip\n\nmkdir -p src/phan_tich_log tests\ntouch src/phan_tich_log/__init__.py\n```\n\n```toml\n# pyproject.toml\n[build-system]\nrequires = [\"setuptools>=68\"]\nbuild-backend = \"setuptools.build_meta\"\n\n[project]\nname = \"phan-tich-log\"\nversion = \"0.1.0\"\nrequires-python = \">=3.10\"\ndependencies = [\"click>=8.1,<9\"]\n\n[project.optional-dependencies]\ndev = [\"pytest>=7\", \"mypy>=1.8\"]\n\n[project.scripts]\nphan-tich = \"phan_tich_log.main:main\"\n\n[tool.setuptools.packages.find]\nwhere = [\"src\"]                        # ⭐ nói rõ code nằm trong src/\n```\n\n```bash\npython -m pip install -e \".[dev]\"\n\n# Kiểm chứng: import được TỪ BẤT KỲ ĐÂU\ncd /tmp && python -c \"import phan_tich_log; print(phan_tich_log.__file__)\"\n# .../phan_tich_log/src/phan_tich_log/__init__.py   ⭐ trỏ về mã nguồn\ncd -\n\n# Lệnh dòng lệnh đã có sẵn\nwhich phan-tich\n```\n\nDòng `cd /tmp` rồi import được là bằng chứng cho lợi ích chính của `-e .`: **hết phụ thuộc vào thư mục đang đứng**, đúng vấn đề đã nêu ở bài module.\n\n### Chẩn đoán \"đã cài rồi mà không import được\"\n\n```bash\n# ① Python nào đang chạy?\nwhich python\npython -c \"import sys; print(sys.executable)\"\n\n# ② Môi trường ảo đã kích hoạt chưa?\npython -c \"import sys; print(sys.prefix != sys.base_prefix)\"\n# True  = đang trong môi trường ảo\n# False = đang dùng Python hệ thống ⇒ nguyên nhân thường là đây\n\n# ③ pip đang cài vào đâu?\npython -m pip --version\n# pip 24.0 from .../.venv/lib/python3.12/site-packages/pip\n\n# ④ Gói cài ở đâu?\npython -m pip show requests | grep -i location\n\n# ⑤ Chỗ đó có trong sys.path không?\npython -c \"import sys; print('\\n'.join(sys.path))\"\n```\n\nBảng chẩn đoán theo kết quả:\n\n| Triệu chứng | Nguyên nhân | Sửa |\n|---|---|---|\n| Bước ② trả `False` | quên kích hoạt | `source .venv/bin/activate` |\n| `pip` ở ngoài `.venv` | dùng `pip` trần | luôn dùng `python -m pip` |\n| Cài đúng chỗ mà vẫn lỗi | tên file trùng thư viện | đổi tên file của bạn |\n| Chỉ lỗi với code của mình | chưa `pip install -e .` | cài chế độ chỉnh sửa |\n\nHàng thứ ba nối về bài module: đặt tên file là `json.py` hay `random.py` thì file của bạn thắng thư viện chuẩn.\n\n### Hai file, hai vai trò\n\n```toml\n# pyproject.toml — KHAI BÁO, khoảng rộng\ndependencies = [\n    \"requests>=2.28,<3\",\n    \"pandas>=2.0\",\n]\n```\n\n```bash\npython -m pip freeze > requirements.txt\ncat requirements.txt\n```\n\n```\n# requirements.txt — ĐÃ CÀI, khoá cứng, gồm phụ thuộc gián tiếp\ncertifi==2023.11.17\ncharset-normalizer==3.3.2\nidna==3.6\nnumpy==1.26.2\npandas==2.1.4\npython-dateutil==2.8.2\npytz==2023.3.post1\nrequests==2.31.0\nsix==1.16.0\nurllib3==2.0.7\n```\n\nChú ý: bạn khai báo hai gói, nhưng thực tế có mười. Tám gói còn lại được kéo theo, và **chính chúng mới hay là nguồn của lỗi \"chạy được trên máy tôi\"** — vì bạn không kiểm soát phiên bản của chúng nếu không khoá cứng.\n\nTách phụ thuộc phát triển ra file riêng:\n\n```bash\npython -m pip freeze > requirements.txt              # môi trường chạy thật\npython -m pip freeze > requirements-dev.txt          # có thêm pytest, mypy\n\n# Cách sạch hơn: dùng optional-dependencies trong pyproject.toml\npython -m pip install -e .            # chỉ phụ thuộc chính\npython -m pip install -e \".[dev]\"     # thêm công cụ phát triển\n```\n\nCách thứ hai tốt hơn vì danh sách nằm ở **một chỗ duy nhất**, không phải đồng bộ giữa hai file.\n\n### Tái lập môi trường ở máy khác\n\n```bash\ngit clone https://.../phan_tich_log.git\ncd phan_tich_log\n\npython -m venv .venv\nsource .venv/bin/activate\npython -m pip install --upgrade pip\n\n# ① Khoá cứng trước — đảm bảo phiên bản y hệt\npython -m pip install -r requirements.txt\n\n# ② Rồi cài dự án ở chế độ chỉnh sửa\npython -m pip install -e . --no-deps\n#                            ⭐ --no-deps: đừng đụng vào phiên bản vừa khoá\n\npython -m pip check          # xác nhận không có xung đột\npytest\n```\n\nCờ `--no-deps` ở bước ② là chi tiết quan trọng: không có nó, `pip` sẽ đọc `pyproject.toml` và có thể **nâng cấp** một gói lên bản mới hơn, phá vỡ đúng thứ bạn vừa khoá.\n\n### Xung đột phiên bản — nhận biết và xử lý\n\n```bash\npython -m pip install \"pandas==2.1.4\" \"numpy==1.20\"\n# ERROR: Cannot install ... conflict is caused by:\n#     pandas 2.1.4 depends on numpy>=1.22.4\n#     The user requested numpy==1.20\n\npython -m pip check\n# thu-vien-a 1.0 has requirement urllib3<2, but you have urllib3 2.0.7\n```\n\nBốn hướng xử lý, theo thứ tự nên thử:\n\n```\n① Nâng cấp gói cũ lên bản chấp nhận phụ thuộc mới     ← tốt nhất\n② Hạ cấp gói mới xuống bản tương thích                ← tạm được\n③ Tìm gói thay thế cho cái đang chặn                   ← khi ① ② không được\n④ Tách thành hai dịch vụ, hai môi trường riêng         ← khi thật sự bế tắc\n```\n\nĐiều cần nhớ: **`pip` không thể cài hai phiên bản của cùng một gói trong một môi trường.** Không có mẹo nào lách được — đây là giới hạn thiết kế, và phương án ④ tồn tại chính vì vậy.\n\n### Biến môi trường cho cấu hình\n\n```bash\n# .env — KHÔNG commit\nDATABASE_URL=postgresql://localhost/db\nAPI_KEY=bi_mat\nDEBUG=1\n```\n\n```bash\necho \".env\" >> .gitignore\ncp .env .env.example      # commit BẢN MẪU, xoá hết giá trị thật\n```\n\n```python\nimport os\n\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"sqlite:///cuc_bo.db\")\nDEBUG = os.environ.get(\"DEBUG\") == \"1\"       # ⭐ so sánh tường minh\n\nAPI_KEY = os.environ.get(\"API_KEY\")\nif API_KEY is None:\n    raise RuntimeError(\"thiếu biến môi trường API_KEY\")   # báo lỗi SỚM\n```\n\nDòng `DEBUG = ... == \"1\"` là bài truthiness ở bài đầu module: `os.environ.get(\"DEBUG\")` trả chuỗi, và chuỗi `\"0\"` là `True`. So sánh tường minh là cách duy nhất đúng.\n\nViệc kiểm tra `API_KEY` ngay lúc khởi động cũng đáng học: **thà chương trình không chạy còn hơn chạy được rồi hỏng giữa chừng** vì thiếu cấu hình.\n\n### Đóng gói và chia sẻ\n\n```bash\npython -m pip install build\npython -m build\n\nls dist/\n# goi_cua_toi-0.1.0-py3-none-any.whl      ⭐ bản wheel — cài nhanh\n# goi_cua_toi-0.1.0.tar.gz                bản mã nguồn\n\n# Cài thử từ file wheel vào một môi trường SẠCH\npython -m venv /tmp/thu && /tmp/thu/bin/python -m pip install dist/*.whl\n/tmp/thu/bin/python -c \"import goi_cua_toi; print('ok')\"\n```\n\nBước cài thử vào môi trường sạch rất đáng làm trước khi chia sẻ: nó phát hiện lỗi **thiếu file trong gói** — lỗi mà bạn không bao giờ thấy trên máy mình vì mã nguồn vẫn nằm đó.\n\nCài trực tiếp từ Git, không cần phát hành:\n\n```bash\npython -m pip install git+https://github.com/nguoi_dung/kho.git\npython -m pip install git+https://github.com/nguoi_dung/kho.git@v1.2.0\n```\n\nCách này rất tiện cho thư viện nội bộ của nhóm, hoặc khi cần một bản vá chưa được phát hành.\n\n### Môi trường ảo trong container\n\n```dockerfile\nFROM python:3.12-slim\n\nWORKDIR /app\n\n# ⭐ Copy file phụ thuộc TRƯỚC — tận dụng cache tầng\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\n\n# Copy mã nguồn SAU — sửa code không phải cài lại thư viện\nCOPY src/ ./src/\nCOPY pyproject.toml .\nRUN pip install --no-deps -e .\n\nCMD [\"python\", \"-m\", \"goi_cua_toi.main\"]\n```\n\nThứ tự hai khối `COPY` là kỹ thuật đáng nhớ: mỗi lệnh trong Dockerfile tạo một tầng được cache. Copy `requirements.txt` trước nghĩa là **sửa code không làm mất cache của bước cài thư viện** — thời gian build giảm từ vài phút xuống vài giây.\n\nTrong container thường **không cần môi trường ảo**, vì bản thân container đã là môi trường cô lập. Đây là ngoại lệ hợp lý duy nhất của quy tắc \"luôn dùng venv\".\n\n### Bảng lệnh dùng hằng ngày\n\n```bash\n# Môi trường\npython -m venv .venv                       # tạo\nsource .venv/bin/activate                  # kích hoạt (Linux/macOS)\n.venv\\Scripts\\activate                     # kích hoạt (Windows)\ndeactivate                                 # thoát\nrm -rf .venv                               # xoá — dựng lại rất nhanh\n\n# Gói\npython -m pip install <goi>\npython -m pip install \"<goi>>=1.0,<2\"\npython -m pip install -e .                 # dự án, chế độ chỉnh sửa\npython -m pip install -e \".[dev]\"          # kèm công cụ phát triển\npython -m pip uninstall <goi>\npython -m pip install --upgrade <goi>\n\n# Thông tin\npython -m pip list\npython -m pip list --outdated\npython -m pip show <goi>\npython -m pip check                        # kiểm tra xung đột\n\n# Khoá và tái lập\npython -m pip freeze > requirements.txt\npython -m pip install -r requirements.txt\n```",

so_sanh: "### Có và không có môi trường ảo\n\n| | Python hệ thống | Môi trường ảo |\n|---|---|---|\n| Xung đột giữa các dự án | **✓ chắc chắn xảy ra** | ✗ |\n| Tái lập được môi trường | rất khó | **✓** |\n| Cần quyền quản trị | thường có | **✗** |\n| Xoá làm lại | nguy hiểm | **✓ an toàn, nhanh** |\n| Biết chính xác dự án cần gì | ✗ | **✓** |\n\nHàng cuối ít được nhắc mà rất giá trị: trong môi trường sạch, `pip freeze` cho đúng những gì dự án cần. Trong Python hệ thống, nó cho một danh sách hỗn độn của mọi thứ bạn từng cài.\n\n### `pyproject.toml` và `requirements.txt`\n\n| | `pyproject.toml` | `requirements.txt` |\n|---|---|---|\n| Vai trò | **khai báo** \"cần gì\" | **khoá** \"đã cài gì\" |\n| Ràng buộc phiên bản | khoảng rộng | **từng bản cụ thể** |\n| Gồm phụ thuộc gián tiếp | ✗ | **✓** |\n| Chứa metadata dự án | **✓** | ✗ |\n| Chứa cấu hình công cụ | **✓** | ✗ |\n| Dùng cho | **thư viện**, cấu trúc dự án | **ứng dụng**, triển khai |\n| Sinh bằng | viết tay | `pip freeze` |\n\nHai file **không thay thế nhau**. Dự án nghiêm túc thường có cả hai: `pyproject.toml` nói ý định, `requirements.txt` khoá thực tế.\n\n### Ràng buộc phiên bản\n\n| Cú pháp | Nghĩa | Dùng khi |\n|---|---|---|\n| `goi` | bản mới nhất | **không bao giờ** trong file cam kết |\n| `goi==1.2.3` | đúng bản này | **ứng dụng**, file khoá |\n| `goi>=1.2` | từ 1.2 trở lên | quá lỏng — có thể nhận bản phá vỡ |\n| `goi>=1.2,<2` | trong cùng bản chính | **✓ thư viện** |\n| `goi~=1.2.0` | chỉ nhận bản vá | khi rất thận trọng |\n\nHàng đầu đáng nhấn: không ghi phiên bản nghĩa là **hôm nay cài khác ngày mai cài**, và bạn mất khả năng tái lập.\n\n### Cài thường và cài chế độ chỉnh sửa\n\n| | `pip install .` | `pip install -e .` |\n|---|---|---|\n| Cách lưu | **sao chép** vào `site-packages` | **liên kết** về mã nguồn |\n| Sửa code có hiệu lực ngay | ✗ phải cài lại | **✓** |\n| Import từ bất kỳ đâu | ✓ | ✓ |\n| Dùng khi | triển khai, kiểm tra bản phát hành | **phát triển** |\n\nTrong lúc phát triển thì `-e .` gần như luôn đúng. Nhưng **trước khi phát hành**, hãy cài bản thường vào một môi trường sạch để kiểm tra gói có đủ file không.\n\n### Thứ commit và thứ không commit\n\n| Commit | Không commit |\n|---|---|\n| `pyproject.toml` | `.venv/` |\n| `requirements.txt` | `__pycache__/` |\n| `.gitignore` | `*.pyc` |\n| `.env.example` | **`.env`** |\n| `.pre-commit-config.yaml` | `dist/`, `build/`, `*.egg-info/` |\n\nNguyên tắc gói lại một câu: **commit công thức, đừng commit kết quả.** `.env` nằm ở cột phải vì nó chứa bí mật; `.env.example` ở cột trái vì nó cho người khác biết cần đặt biến nào.\n\n### `pip` và các công cụ thế hệ mới\n\n| | `venv` + `pip` | Công cụ gộp |\n|---|---|---|\n| Có sẵn | **✓ không cài gì** | phải cài thêm |\n| Quản lý môi trường | tách rời | **gộp chung** |\n| File khoá có mã băm | ✗ | **✓** |\n| Tốc độ giải phụ thuộc | trung bình | thường nhanh hơn |\n| Phải học thêm | ✗ | ✓ |\n| Khái niệm nền | **giống nhau** | giống nhau |\n\nHàng cuối là điểm quan trọng nhất: mọi công cụ đều dựng trên cùng ba khái niệm — **môi trường riêng, khai báo phụ thuộc, khoá phiên bản**. Học `venv` + `pip` trước là học phần nền, và nó chuyển sang công cụ nào cũng dùng được.\n\n### Bốn cách xử lý xung đột phiên bản\n\n| | Nâng gói cũ | Hạ gói mới | Đổi gói khác | Tách môi trường |\n|---|---|---|---|---|\n| Rủi ro | vừa | **cao — mất tính năng** | cao | thấp |\n| Công sức | thấp | thấp | **cao** | vừa |\n| Bền vững | **✓** | ✗ tạm thời | ✓ | ✓ |\n| Thử thứ mấy | **①** | ② | ③ | ④ |\n\nThứ tự này phản ánh một nguyên tắc chung: **ưu tiên tiến lên trước khi lùi lại**. Hạ cấp giải quyết vấn đề hôm nay nhưng để lại nó cho lần nâng cấp sau.",

loi_thuong_gap: [
  "**Cài thư viện vào Python hệ thống.** Xung đột giữa các dự án là chuyện sớm muộn, và gỡ ra rất khó.",
  "**Gõ `pip install` thay vì `python -m pip install`.** Máy nhiều bản Python thì cài một nơi, chạy nơi khác.",
  "**Quên kích hoạt môi trường ảo.** Kiểm tra bằng `sys.prefix != sys.base_prefix`.",
  "**Commit thư mục `.venv/`.** Rất nặng, chứa đường dẫn tuyệt đối của máy bạn, và có file nhị phân cho đúng hệ điều hành của bạn.",
  "**Không ghi phiên bản trong file phụ thuộc.** Hôm nay cài khác ngày mai cài — mất khả năng tái lập.",
  "**Khoá cứng phiên bản trong một thư viện dùng chung.** Gây xung đột không cần thiết cho mọi người dùng nó.",
  "**Không khoá cứng trong một ứng dụng triển khai.** Mở cửa cho \"chạy được trên máy tôi\".",
  "**Chạy `pip freeze` trên môi trường bẩn.** File khoá chứa cả những gói cài để thử rồi quên.",
  "**Quên `--no-deps` khi cài `-e .` sau khi đã khôi phục từ `requirements.txt`.** `pip` có thể nâng cấp và phá phiên bản vừa khoá.",
  "**Commit file `.env`.** Rò rỉ khoá bí mật. Commit `.env.example` thay vào đó.",
  "**Sửa `sys.path` trong code thay vì `pip install -e .`.** Vá tạm, phải lặp lại ở mọi file.",
  "**Không nâng `pip` trước khi cài gói mới.** `pip` cũ không hiểu định dạng gói mới, và thông báo lỗi nói về chuyện khác.",
  "**Mất thời gian sửa môi trường ảo hỏng.** Xoá và dựng lại mất chưa tới một phút.",
  "**Copy toàn bộ mã nguồn trước khi cài thư viện trong Dockerfile.** Mất cache tầng, mỗi lần sửa code là cài lại toàn bộ.",
  "**Chọn công cụ quản lý phụ thuộc theo độ mới.** Mọi công cụ dựng trên cùng khái niệm; hiểu nền trước, đổi công cụ khi gặp đúng vấn đề nó giải quyết."
],

tu_kiem_tra: [
  "Môi trường ảo thật ra là gì? Kích hoạt nó làm chính xác việc gì?",
  "Vì sao dùng được môi trường ảo mà không cần kích hoạt? Khi nào cách đó tiện hơn?",
  "Vì sao luôn gõ `python -m pip` thay vì `pip`? Nó ngăn được lỗi gì?",
  "Phân biệt phụ thuộc \"khai báo\" và \"khoá cứng\". Mỗi loại nằm ở file nào và phục vụ mục đích gì?",
  "Vì sao thư viện nên khai báo rộng còn ứng dụng nên khoá cứng?",
  "`requests>=2.28,<3` nghĩa là gì? Nó dựa trên quy ước đánh số phiên bản nào?",
  "`pip install -e .` khác `pip install .` ở đâu? Nó sửa được vấn đề nào của bài module?",
  "Nêu ba lý do không commit thư mục môi trường ảo.",
  "Vì sao cần `--no-deps` khi cài `-e .` sau khi khôi phục từ `requirements.txt`?",
  "Liệt kê năm bước chẩn đoán \"đã cài rồi mà không import được\".",
  "`pip` có cài được hai phiên bản của cùng một gói không? Hệ quả với việc xử lý xung đột?",
  "Nêu bốn cách xử lý xung đột phiên bản theo thứ tự nên thử, và vì sao thứ tự đó.",
  "Vì sao trong Dockerfile phải copy `requirements.txt` trước mã nguồn?",
  "Vì sao `DEBUG = os.environ.get(\"DEBUG\") == \"1\"` chứ không phải `if os.environ.get(\"DEBUG\")`?"
],

tai_nguyen: [
  "**Python Packaging User Guide** — tài liệu chính thức của cộng đồng đóng gói. Phần *Tutorials* và *Guides* bao phủ đúng nội dung bài này.",
  "Tài liệu Python chính thức, module `venv` — mô tả cấu trúc thư mục môi trường ảo và các tuỳ chọn tạo.",
  "**PEP 621** — chuẩn khai báo metadata dự án trong `pyproject.toml`. Đọc để biết mỗi trường nghĩa là gì.",
  "Tài liệu `pip` — mục *Requirements File Format* và *pip install* giải thích đầy đủ cú pháp ràng buộc phiên bản.",
  "**Semantic Versioning** (semver.org) — quy ước đánh số phiên bản mà toàn bộ hệ sinh thái dựa vào."
],

lien_he: [
  "**Module, package, import**: `site-packages` nằm trong `sys.path`; `pip install -e .` là cách sửa gốc cho `ModuleNotFoundError`.",
  "**Biến môi trường và PATH** ở module 3: kích hoạt môi trường ảo chỉ là sửa `PATH`, và `PATH` tra từ trái sang phải.",
  "**PEP 8 và format tự động** ngay trước: `pyproject.toml` chứa cấu hình cho formatter, linter, `mypy`, `pytest`.",
  "**Type hint và mypy**: `mypy` cài như một phụ thuộc phát triển trong `optional-dependencies`.",
  "**Cú pháp, kiểu dữ liệu**: bẫy truthiness với biến môi trường — chuỗi `\"0\"` là `True`.",
  "**Xử lý ngoại lệ**: kiểm tra biến môi trường bắt buộc ngay lúc khởi động là mẫu \"báo lỗi sớm\".",
  "**Git & GitHub** ở mảng 03: `.gitignore` và quy tắc commit công thức chứ không commit kết quả.",
  "**Backend & Web** ở mảng 05: Dockerfile, biến môi trường cho cấu hình, và triển khai đều dựa trên bài này.",
  "**AI/ML** ở mảng 07: phụ thuộc của các thư viện học sâu rất chặt và gắn với phiên bản driver — đây là lĩnh vực môi trường ảo là bắt buộc.",
  "Đây là **bài cuối module 5**: module 6 chuyển sang Lập trình hướng đối tượng, nơi bạn tổ chức code thành class thay vì hàm rời rạc."
]

};
