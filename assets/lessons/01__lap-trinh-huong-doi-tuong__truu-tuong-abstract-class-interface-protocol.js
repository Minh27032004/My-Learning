window.LESSONS["01:lap-trinh-huong-doi-tuong:truu-tuong-abstract-class-interface-protocol"] = {

tom_tat: [
  "**Trừu tượng là tách \"cái gì phải làm được\" khỏi \"làm bằng cách nào\".** Nó biến giao diện ngầm định của duck typing thành một giao ước có tên và kiểm tra được.",
  "**`raise NotImplementedError` chỉ báo lỗi khi gọi tới; `ABC` + `@abstractmethod` báo lỗi ngay lúc tạo object.** Phát hiện sớm hơn nhiều bậc.",
  "**Phân biệt hai hướng ràng buộc**: `ABC` là **danh nghĩa** (phải kế thừa mới tính), `Protocol` là **cấu trúc** (đúng hình dạng là tính).",
  "**`Protocol` là lựa chọn mặc định cho code mới trong Python** — nó giữ được duck typing, không ép người dùng kế thừa, và dùng được với class của thư viện khác.",
  "**Đừng tạo trừu tượng khi mới có một bản cài đặt.** Trừu tượng đúng đến từ việc *nhận ra* điểm chung giữa hai ba bản thật, không đến từ dự đoán."
],

can_biet_truoc: [
  "Bài **Đa hình và duck typing** ngay trước — giao diện ngầm định và `Protocol` đã giới thiệu ở đó.",
  "Bài **Kế thừa và MRO** — `ABC` hoạt động qua kế thừa, nên phải hiểu kế thừa trước.",
  "Bài **Type hint và mypy** ở module 5 — `Protocol` được kiểm tra bằng công cụ tĩnh.",
  "Bài **Đóng gói** — trừu tượng và đóng gói là hai mặt: một cái quyết định *phải có gì*, một cái quyết định *lộ ra gì*."
],

dinh_nghia: "Nói đời thường: **ổ cắm điện là một trừu tượng**. Nó nói \"cái gì cắm vào đây phải có hai chân, đúng điện áp\" — nhưng không quan tâm bên kia là quạt hay máy tính. Nhà sản xuất thiết bị chỉ cần tuân thủ giao ước đó.\n\nNói chính xác:\n\n- **Trừu tượng** (*abstraction*) — mô tả **cái gì** phải làm được, bỏ qua **làm bằng cách nào**.\n- **Class trừu tượng** (*abstract class*) — class **không tạo instance trực tiếp được**, dùng làm khuôn cho lớp con. Trong Python: kế thừa `ABC`.\n- **`@abstractmethod`** — đánh dấu phương thức mà lớp con **bắt buộc** phải cài đặt.\n- **Interface** — tập phương thức mà một kiểu phải có. Python **không có từ khoá `interface`**; vai trò đó do `ABC` hoặc `Protocol` đảm nhiệm.\n- **Kiểu danh nghĩa** (*nominal typing*) — tương thích vì **khai báo** kế thừa. Đây là `ABC`.\n- **Kiểu cấu trúc** (*structural typing*) — tương thích vì **có đúng hình dạng**. Đây là `Protocol`, và cũng là duck typing được viết ra.\n- **`NotImplementedError`** — ngoại lệ báo \"phương thức này chưa được cài đặt\".\n\nĐiểm cần phân biệt ngay: **`ABC` và `Protocol` giải cùng một bài toán theo hai hướng ngược nhau.** `ABC` bắt lớp con phải nói \"tôi là loại này\"; `Protocol` chỉ nhìn xem class có đúng phương thức hay không. Chọn cái nào phụ thuộc vào việc bạn **kiểm soát được** các class cài đặt hay không.",

vi_sao_can: "Trừu tượng là công cụ để nhiều người làm việc trên cùng một hệ thống mà không giẫm chân nhau:\n\n- **Nó biến giao ước ngầm thành tường minh.** Duck typing rất linh hoạt nhưng không ai biết `nguon` cần có phương thức gì cho tới khi đọc thân hàm. Trừu tượng viết điều đó ra.\n- **Nó bắt lỗi sớm.** Quên cài đặt một phương thức bắt buộc: với `ABC` thì lỗi ngay lúc tạo object; không có nó thì lỗi khi chạy tới đúng dòng đó, có thể là trên môi trường thật.\n- **Nó cho phép chia việc.** Định nghĩa giao diện trước, rồi hai người viết hai bản cài đặt song song mà không phải chờ nhau.\n- **Nó là nền của việc thay thế được.** Đổi database, đổi nhà cung cấp lưu trữ, thay bằng bản giả khi kiểm thử — tất cả cần một giao diện chung.\n- **Nền cho SOLID.** Hai nguyên tắc quan trọng nhất — Interface Segregation và Dependency Inversion — nói trực tiếp về trừu tượng.\n\nTình huống thật rất hay gặp: nhóm bạn thống nhất \"mọi bộ xử lý phải có `xu_ly()` và `don_dep()`\". Ba tháng sau, một người viết class mới nhưng **quên `don_dep()`**. Code chạy tốt trong kiểm thử vì `don_dep` chỉ được gọi khi tắt ứng dụng. Lên môi trường thật, mỗi lần triển khai lại là một lần rò rỉ kết nối. Với `ABC`, class đó **không tạo được instance** ngay từ dòng đầu tiên. Phần cơ chế sẽ chỉ rõ ba mức ràng buộc và mức nào phù hợp khi nào.",

co_che: "### Ba mức ràng buộc\n\n```\n① Quy ước       docstring nói \"lớp con nên có xu_ly()\"     — không kiểm tra gì\n② NotImplementedError  lỗi khi GỌI tới                      — muộn\n③ ABC + abstractmethod lỗi khi TẠO OBJECT                   — sớm ⭐\n```\n\n**Mức ②:**\n\n```python\nclass BoXuLy:\n    def xu_ly(self, x):\n        raise NotImplementedError(\"lớp con phải cài đặt xu_ly()\")\n\nclass XuLySai(BoXuLy):\n    pass                          # quên cài đặt\n\nb = XuLySai()                     # ✓ tạo được — vấn đề chưa lộ ra\nb.xu_ly(1)                        # NotImplementedError — lộ ra ở ĐÂY\n```\n\n**Mức ③:**\n\n```python\nfrom abc import ABC, abstractmethod\n\nclass BoXuLy(ABC):\n    @abstractmethod\n    def xu_ly(self, x): ...\n\n    @abstractmethod\n    def don_dep(self): ...\n\nclass XuLySai(BoXuLy):\n    def xu_ly(self, x):\n        return x * 2\n    # quên don_dep()\n\nXuLySai()\n# TypeError: Can't instantiate abstract class XuLySai\n#            with abstract method don_dep          ⭐ lỗi NGAY, nêu rõ thiếu gì\n```\n\nKhác biệt thực tế rất lớn: mức ② để lỗi trốn tới lúc chạy đúng nhánh đó; mức ③ chặn ngay khi ai đó tạo object — thường là ngay dòng đầu của kiểm thử.\n\n### `ABC` hoạt động thế nào\n\n```python\nfrom abc import ABC, abstractmethod\n\nclass Hinh(ABC):\n    @abstractmethod\n    def dien_tich(self) -> float: ...\n\n    @abstractmethod\n    def chu_vi(self) -> float: ...\n\n    def mo_ta(self) -> str:                    # ⭐ phương thức THƯỜNG\n        return (f\"{type(self).__name__}: \"\n                f\"S={self.dien_tich():.2f}, P={self.chu_vi():.2f}\")\n\nHinh()      # TypeError — không tạo instance của class trừu tượng\n```\n\nĐặc điểm quan trọng: **`ABC` được phép có phương thức đã cài đặt sẵn.** `mo_ta` gọi hai phương thức trừu tượng và hoạt động với mọi lớp con — đây là chỗ `ABC` mạnh hơn interface thuần.\n\n```python\nclass HinhTron(Hinh):\n    def __init__(self, r: float):\n        if r <= 0:\n            raise ValueError(f\"bán kính phải dương: {r}\")\n        self.r = r\n\n    def dien_tich(self): return 3.14159 * self.r ** 2\n    def chu_vi(self): return 2 * 3.14159 * self.r\n\nHinhTron(2).mo_ta()      # 'HinhTron: S=12.57, P=12.57'\n```\n\nCơ chế bên dưới: `ABCMeta` đếm các phương thức còn `__isabstractmethod__ = True` khi tạo class; còn cái nào chưa cài đặt thì chặn việc tạo instance.\n\n```python\nHinh.__abstractmethods__          # frozenset({'dien_tich', 'chu_vi'})\nHinhTron.__abstractmethods__      # frozenset() — rỗng ⇒ tạo được\n```\n\n### Kết hợp `@abstractmethod` với các decorator khác\n\n```python\nclass Kho(ABC):\n    @property\n    @abstractmethod\n    def dung_luong(self) -> int: ...        # ⭐ abstractmethod ở TRONG CÙNG\n\n    @classmethod\n    @abstractmethod\n    def tu_cau_hinh(cls, cf: dict) -> \"Kho\": ...\n\n    @staticmethod\n    @abstractmethod\n    def kiem_tra(cf: dict) -> bool: ...\n```\n\nThứ tự bắt buộc: **`@abstractmethod` luôn nằm dưới cùng**, sát `def`. Đảo thứ tự thì `@property` bọc ngoài và dấu hiệu trừu tượng bị mất — class con quên cài đặt vẫn tạo được, và bạn mất toàn bộ lợi ích.\n\n### `Protocol` — kiểm tra theo hình dạng\n\n```python\nfrom typing import Protocol\n\nclass CoTheXuLy(Protocol):\n    def xu_ly(self, x: int) -> str: ...\n    def don_dep(self) -> None: ...\n\ndef chay(bo: CoTheXuLy, du_lieu: list[int]) -> list[str]:\n    try:\n        return [bo.xu_ly(x) for x in du_lieu]\n    finally:\n        bo.don_dep()\n\nclass XuLyThat:                              # ⭐ KHÔNG kế thừa gì\n    def xu_ly(self, x: int) -> str: return str(x * 2)\n    def don_dep(self) -> None: pass\n\nchay(XuLyThat(), [1, 2])     # ✓ mypy chấp nhận — đúng hình dạng\n```\n\nBa lợi ích so với `ABC`:\n\n```\n① Class bên thứ ba dùng được — họ không cần biết Protocol của bạn tồn tại\n② Không tạo ràng buộc kế thừa — class cài đặt độc lập hoàn toàn\n③ Class giả cho kiểm thử viết trong ba dòng, không kế thừa gì\n```\n\nCái giá: **`Protocol` không kiểm tra lúc chạy** trừ khi bật:\n\n```python\nfrom typing import runtime_checkable\n\n@runtime_checkable\nclass CoTheXuLy(Protocol):\n    def xu_ly(self, x: int) -> str: ...\n\nisinstance(XuLyThat(), CoTheXuLy)      # True\n```\n\nGiới hạn quan trọng: phép kiểm tra đó **chỉ xét tên phương thức có tồn tại**, không xét chữ ký, không xét kiểu tham số. Một class có `def xu_ly(self)` (thiếu tham số) vẫn qua được `isinstance`.\n\n### `ABC` và `Protocol` — chọn cái nào\n\n```\nABC       DANH NGHĨA — \"anh phải KHAI BÁO mình là loại này\"\nProtocol  CẤU TRÚC   — \"anh chỉ cần CÓ đúng phương thức\"\n```\n\n| | `ABC` | `Protocol` |\n|---|---|---|\n| Phải kế thừa | **✓** | ✗ |\n| Kiểm tra lúc tạo object | **✓** | ✗ |\n| Kiểm tra tĩnh (`mypy`) | ✓ | **✓** |\n| Dùng với class bên thứ ba | ✗ | **✓** |\n| Chia sẻ code cài đặt sẵn | **✓** | hạn chế |\n| Ép cài đặt đủ | **✓ mạnh nhất** | chỉ qua `mypy` |\n| Ràng buộc | chặt | **lỏng** |\n\nCách chọn thực dụng:\n\n```\nBạn KIỂM SOÁT mọi class cài đặt, và muốn chia sẻ code chung  → ABC\nCần nhận cả class của thư viện khác, hoặc muốn ràng buộc lỏng → Protocol\nCần cả hai                                                     → dùng cả hai\n```\n\nDòng cuối không phải nói cho có: bạn viết một `Protocol` làm giao diện công khai cho người dùng, và một `ABC` làm class nền tiện lợi cho ai muốn dùng — họ chọn cách nào cũng được.\n\n### Trừu tượng có thể có cài đặt mặc định\n\n```python\nclass NguonDuLieu(ABC):\n    @abstractmethod\n    def doc(self) -> list[dict]: ...\n\n    def doc_loc(self, dieu_kien) -> list[dict]:         # ⭐ dùng chung\n        return [b for b in self.doc() if dieu_kien(b)]\n\n    def dem(self) -> int:                                # ⭐ dùng chung\n        return len(self.doc())\n\n    def doc_theo_lo(self, kich_thuoc=100):               # ⭐ dùng chung\n        bg = self.doc()\n        for i in range(0, len(bg), kich_thuoc):\n            yield bg[i:i + kich_thuoc]\n```\n\nLớp con chỉ cần cài `doc()` là **có ngay ba phương thức tiện ích**. Đây là mẫu *Template Method*: class cha định nghĩa khung, lớp con điền phần thay đổi.\n\nNhưng cân bằng lại — đây cũng là chỗ `ABC` tạo ràng buộc: mọi lớp con **bị buộc** dùng cách cài đặt mặc định đó, và đổi nó là ảnh hưởng tất cả. Nếu ba phương thức trên chỉ là tiện ích thì để chúng thành **hàm nhận vào một `Protocol`** sẽ lỏng hơn:\n\n```python\ndef doc_loc(nguon: NguonDuLieuProtocol, dieu_kien) -> list[dict]:\n    return [b for b in nguon.doc() if dieu_kien(b)]\n```\n\nCâu hỏi phân định: **code chung này có thật sự thuộc về mọi bản cài đặt không?** Có → `ABC`. Chỉ là tiện ích → hàm ngoài.\n\n### Đăng ký lớp con ảo\n\n```python\nfrom abc import ABC\n\nclass CoTheXuat(ABC): ...\n\nclass ClassCuaThuVienKhac:               # không sửa được mã nguồn của họ\n    def xuat(self): return \"...\"\n\nCoTheXuat.register(ClassCuaThuVienKhac)   # ⭐ đăng ký làm lớp con \"ảo\"\n\nisinstance(ClassCuaThuVienKhac(), CoTheXuat)     # True\nissubclass(ClassCuaThuVienKhac, CoTheXuat)       # True\n```\n\nCơ chế này cho phép `ABC` nhận class bên thứ ba. Nhưng nó **không kiểm tra gì cả** — đăng ký một class hoàn toàn không có `xuat()` vẫn thành công, và `isinstance` vẫn trả `True`.\n\nVì vậy `register` là một lối thoát hiểm, không phải giải pháp. Cần nhận class bên thứ ba thì `Protocol` đúng hơn nhiều — nó kiểm tra thật.\n\n### `ABC` trong thư viện chuẩn\n\n```python\nfrom collections.abc import Iterable, Sized, Sequence, Mapping\n\nisinstance([1, 2], Iterable)      # True\nisinstance([1, 2], Sequence)      # True\nisinstance({\"a\": 1}, Mapping)     # True\nisinstance(\"abc\", Sequence)       # True\n```\n\nCác `ABC` này rất hữu ích cho chú thích kiểu vì chúng **rộng hơn kiểu cụ thể**:\n\n```python\n# ❌ Ép người dùng phải đưa đúng list\ndef xu_ly(ds: list[int]) -> int:\n    return sum(ds)\n\n# ✓ Nhận mọi thứ lặp được — tuple, set, generator, class tự viết\nfrom collections.abc import Iterable\n\ndef xu_ly(ds: Iterable[int]) -> int:\n    return sum(ds)\n```\n\nNguyên tắc chung khi chú thích kiểu: **nhận rộng nhất có thể ở tham số, trả về cụ thể nhất có thể.** Nhận `Iterable` và trả `list` là mẫu tốt.\n\nMột số `ABC` còn cho sẵn phương thức: kế thừa `Sequence` và cài `__getitem__` với `__len__` là tự có `__contains__`, `__iter__`, `__reversed__`, `index`, `count`.\n\n### Khi nào KHÔNG nên trừu tượng\n\nPhần này quan trọng ngang phần cơ chế, vì trừu tượng sớm là một trong những sai lầm thiết kế tốn kém nhất:\n\n```python\n# ❌ Một bản cài đặt duy nhất, nhưng đã có class trừu tượng\nclass NguonDuLieu(ABC):\n    @abstractmethod\n    def doc(self): ...\n\nclass NguonPostgres(NguonDuLieu):\n    def doc(self): ...\n\n# ...và sẽ mãi chỉ có một bản. Class trừu tượng chỉ thêm một tầng.\n```\n\nVấn đề sâu hơn con số dòng code: **trừu tượng dựng từ một ví dụ duy nhất gần như luôn sai hình dạng.** Khi bản cài đặt thứ hai xuất hiện, nó thường không vừa với giao diện bạn đoán — và bạn phải sửa cả hai.\n\n```\nQuy tắc thực dụng: đợi tới bản cài đặt THỨ HAI, có khi thứ ba,\n                   rồi mới rút ra điểm chung.\n\nTrừu tượng đúng đến từ việc NHẬN RA điểm chung giữa các bản thật,\nkhông đến từ việc DỰ ĐOÁN chúng sẽ giống nhau ở đâu.\n```\n\nHai dấu hiệu trừu tượng sai:\n\n```\n① Giao diện có phương thức mà một số bản cài đặt phải bỏ trống\n   → dấu hiệu Interface Segregation bị vi phạm\n② Thêm bản cài đặt mới phải SỬA giao diện\n   → giao diện đang mô tả một bản cụ thể, không phải điểm chung\n```\n\n### `NotImplementedError` và `NotImplemented`\n\nHai thứ khác nhau, rất hay bị nhầm:\n\n```python\nraise NotImplementedError        # ⭐ NGOẠI LỆ — \"chưa cài đặt\"\nreturn NotImplemented            # ⭐ GIÁ TRỊ — dùng trong toán tử\n```\n\n```python\nclass A:\n    def __eq__(self, k):\n        if not isinstance(k, A):\n            return NotImplemented     # ⭐ để Python thử k.__eq__(self)\n        return self.x == k.x\n\n    def xu_ly(self):\n        raise NotImplementedError     # ⭐ lớp con phải cài đặt\n```\n\n`NotImplemented` là một object đặc biệt nói \"tôi không xử lý được phép toán này, thử cách khác đi\". Dùng `raise NotImplemented` (thiếu `Error`) là lỗi thật sự khó hiểu vì thông báo không nói gì có ích.",

vi_du: "### Ba mức ràng buộc — so sánh trực tiếp\n\n```python\nfrom abc import ABC, abstractmethod\n\n# ① Quy ước — không kiểm tra gì\nclass Muc1:\n    \"\"\"Lớp con NÊN cài đặt xu_ly().\"\"\"\n\nclass Con1(Muc1): pass\nCon1().xu_ly                # AttributeError — muộn nhất, thông báo mơ hồ\n\n# ② NotImplementedError\nclass Muc2:\n    def xu_ly(self, x):\n        raise NotImplementedError(f\"{type(self).__name__} phải cài đặt xu_ly()\")\n\nclass Con2(Muc2): pass\nc = Con2()                  # ✓ tạo được\nc.xu_ly(1)                  # NotImplementedError: Con2 phải cài đặt xu_ly()\n\n# ③ ABC — ⭐ sớm nhất\nclass Muc3(ABC):\n    @abstractmethod\n    def xu_ly(self, x): ...\n\nclass Con3(Muc3): pass\nCon3()\n# TypeError: Can't instantiate abstract class Con3 with abstract method xu_ly\n```\n\nMức ③ chặn ở **dòng tạo object** — thường là dòng đầu tiên của bất kỳ bài kiểm thử nào, nên gần như không thể lọt lên môi trường thật.\n\nMức ② vẫn có chỗ dùng: khi bạn muốn lớp con **có thể** không cài đặt (phương thức tuỳ chọn), nhưng gọi tới thì phải báo rõ ràng.\n\n### `ABC` với code dùng chung\n\n```python\nfrom abc import ABC, abstractmethod\nfrom pathlib import Path\nimport json\n\nclass BoNap(ABC):\n    \"\"\"Nạp dữ liệu từ một nguồn. Lớp con chỉ cần cài _doc_tho().\"\"\"\n\n    @abstractmethod\n    def _doc_tho(self) -> str:\n        \"\"\"Trả về nội dung thô dạng chuỗi.\"\"\"\n\n    @property\n    @abstractmethod\n    def ten_nguon(self) -> str: ...\n\n    # ⭐ Cài đặt sẵn — mọi lớp con dùng chung\n    def nap(self) -> list[dict]:\n        tho = self._doc_tho()\n        if not tho.strip():\n            raise ValueError(f\"{self.ten_nguon}: nguồn rỗng\")\n        try:\n            du_lieu = json.loads(tho)\n        except json.JSONDecodeError as e:\n            raise ValueError(f\"{self.ten_nguon}: JSON sai dòng {e.lineno}\") from e\n        if not isinstance(du_lieu, list):\n            raise ValueError(f\"{self.ten_nguon}: cần một mảng\")\n        return du_lieu\n\n    def nap_an_toan(self, mac_dinh=None) -> list[dict]:\n        try:\n            return self.nap()\n        except (ValueError, OSError) as e:\n            print(f\"cảnh báo: {e}\")\n            return mac_dinh if mac_dinh is not None else []\n\n    def __repr__(self) -> str:\n        return f\"{type(self).__name__}({self.ten_nguon!r})\"\n\n\nclass NapFile(BoNap):\n    def __init__(self, duong_dan: str | Path):\n        self.p = Path(duong_dan)\n\n    def _doc_tho(self) -> str:\n        return self.p.read_text(encoding=\"utf-8\")\n\n    @property\n    def ten_nguon(self) -> str:\n        return str(self.p)\n\n\nclass NapChuoi(BoNap):                     # ⭐ cho kiểm thử\n    def __init__(self, noi_dung: str):\n        self.noi_dung = noi_dung\n\n    def _doc_tho(self) -> str:\n        return self.noi_dung\n\n    @property\n    def ten_nguon(self) -> str:\n        return \"<chuỗi>\"\n\n\n# Mỗi lớp con viết ~6 dòng, nhận về toàn bộ logic kiểm tra và xử lý lỗi\nprint(NapChuoi('[{\"a\": 1}]').nap())          # [{'a': 1}]\nprint(NapChuoi('không phải json').nap_an_toan())\n# cảnh báo: <chuỗi>: JSON sai dòng 1\n# []\n```\n\nĐây là chỗ `ABC` thắng `Protocol` rõ ràng: **toàn bộ logic kiểm tra và xử lý lỗi viết một lần** ở class cha. Với `Protocol`, mỗi bản cài đặt phải tự lo, hoặc phải tách ra hàm ngoài.\n\n### `Protocol` cho giao diện mở\n\n```python\nfrom typing import Protocol, runtime_checkable\n\n@runtime_checkable\nclass KhoLuuTru(Protocol):\n    \"\"\"Bất cứ thứ gì lưu và lấy được theo khoá.\"\"\"\n    def luu(self, khoa: str, gt: bytes) -> None: ...\n    def lay(self, khoa: str) -> bytes | None: ...\n    def xoa(self, khoa: str) -> bool: ...\n\n\nclass KhoBoNho:                    # ⭐ không kế thừa gì\n    def __init__(self):\n        self._d: dict[str, bytes] = {}\n    def luu(self, khoa, gt): self._d[khoa] = gt\n    def lay(self, khoa): return self._d.get(khoa)\n    def xoa(self, khoa): return self._d.pop(khoa, None) is not None\n\n\nclass KhoFile:                      # ⭐ cũng không kế thừa gì\n    def __init__(self, thu_muc):\n        from pathlib import Path\n        self.tm = Path(thu_muc); self.tm.mkdir(parents=True, exist_ok=True)\n    def luu(self, khoa, gt): (self.tm / khoa).write_bytes(gt)\n    def lay(self, khoa):\n        p = self.tm / khoa\n        return p.read_bytes() if p.exists() else None\n    def xoa(self, khoa):\n        p = self.tm / khoa\n        if p.exists(): p.unlink(); return True\n        return False\n\n\n# Hàm nghiệp vụ — không biết dữ liệu nằm ở đâu\ndef cache_ket_qua(kho: KhoLuuTru, khoa: str, tinh) -> bytes:\n    co = kho.lay(khoa)\n    if co is not None:\n        return co\n    kq = tinh()\n    kho.luu(khoa, kq)\n    return kq\n\ncache_ket_qua(KhoBoNho(), \"a\", lambda: b\"kq\")      # ✓\ncache_ket_qua(KhoFile(\"/tmp/c\"), \"a\", lambda: b\"kq\")  # ✓\n```\n\nGiá trị lớn nhất: nếu ngày mai có thư viện cung cấp một class có đúng ba phương thức đó, nó **dùng được ngay** — không cần ai sửa gì, không cần họ biết `KhoLuuTru` tồn tại.\n\n### Class giả cho kiểm thử — ba dòng\n\n```python\ndef test_cache_dung_lai_ket_qua():\n    class KhoGia:                       # ⭐ không kế thừa, không cài đủ mọi thứ\n        def __init__(self): self.d, self.so_lan_luu = {}, 0\n        def luu(self, k, v): self.d[k] = v; self.so_lan_luu += 1\n        def lay(self, k): return self.d.get(k)\n        def xoa(self, k): return self.d.pop(k, None) is not None\n\n    kho = KhoGia()\n    dem = 0\n    def tinh():\n        nonlocal dem\n        dem += 1\n        return b\"kq\"\n\n    cache_ket_qua(kho, \"a\", tinh)\n    cache_ket_qua(kho, \"a\", tinh)       # lần hai lấy từ cache\n\n    assert dem == 1, \"phải chỉ tính một lần\"\n    assert kho.so_lan_luu == 1\n```\n\nVới `ABC`, class giả **phải kế thừa** và cài đặt **đủ mọi** phương thức trừu tượng, kể cả những cái bài kiểm thử này không dùng. `Protocol` cho phép class giả gọn hơn hẳn.\n\n### Thứ tự decorator — lỗi im lặng\n\n```python\nfrom abc import ABC, abstractmethod\n\n# ❌ SAI thứ tự — mất tác dụng, KHÔNG báo lỗi\nclass Sai(ABC):\n    @abstractmethod\n    @property\n    def ten(self): ...\n\nclass ConSai(Sai): pass\nConSai()          # ✓ TẠO ĐƯỢC — dấu hiệu trừu tượng đã mất\n\n# ✓ ĐÚNG — abstractmethod dưới cùng, sát def\nclass Dung(ABC):\n    @property\n    @abstractmethod\n    def ten(self) -> str: ...\n\nclass ConDung(Dung): pass\nConDung()\n# TypeError: Can't instantiate abstract class ConDung with abstract method ten ✓\n```\n\nĐây là lỗi im lặng đúng nghĩa: bản sai không báo gì, chỉ là **mọi ràng buộc bạn tưởng mình có đều không tồn tại**. Quy tắc: `@abstractmethod` luôn nằm sát `def`.\n\n### `collections.abc` cho chú thích kiểu\n\n```python\nfrom collections.abc import Iterable, Sequence, Mapping, Callable\n\n# ❌ Hẹp không cần thiết\ndef tong(ds: list[int]) -> int:\n    return sum(ds)\n\ntong((1, 2))                  # mypy: tuple không phải list\ntong(x for x in [1, 2])       # mypy: generator không phải list\n\n# ✓ Nhận rộng\ndef tong(ds: Iterable[int]) -> int:\n    return sum(ds)\n\ntong([1, 2]); tong((1, 2)); tong({1, 2}); tong(x for x in [1, 2])   # ✓ hết\n```\n\nChọn `ABC` nào theo thứ bạn thật sự cần:\n\n```\nChỉ lặp một lượt                → Iterable\nCần len()                       → Sized  (hoặc Collection)\nCần chỉ số và slice             → Sequence\nCần tra theo khoá               → Mapping\nCần gọi được                    → Callable\n```\n\nĐây là ứng dụng trực tiếp của nguyên tắc \"nhận rộng, trả cụ thể\":\n\n```python\ndef loc(ds: Iterable[int], dk: Callable[[int], bool]) -> list[int]:\n    #        └─ nhận rộng nhất                          └─ trả cụ thể\n    return [x for x in ds if dk(x)]\n```\n\nTrả `list` chứ không trả `Iterable` là có chủ đích: người gọi biết chắc mình có thể `len()` và lặp nhiều lần — nối về bài generator ở module 5.\n\n### Kế thừa `ABC` của thư viện chuẩn\n\n```python\nfrom collections.abc import Sequence\n\nclass DaySo(Sequence):\n    \"\"\"Chỉ cần cài __getitem__ và __len__.\"\"\"\n    def __init__(self, so): self._so = list(so)\n    def __getitem__(self, i): return self._so[i]\n    def __len__(self): return len(self._so)\n\nd = DaySo([3, 1, 4])\n\n# ⭐ Nhận MIỄN PHÍ từ Sequence:\n3 in d                  # True  — __contains__\nlist(reversed(d))       # [4, 1, 3] — __reversed__\nd.index(4)              # 2\nd.count(3)              # 1\nfor x in d: pass        # __iter__\nisinstance(d, Sequence) # True\n```\n\nHai dòng cài đặt đổi lấy sáu tính năng. Đây là mẫu *Template Method* trong thư viện chuẩn, và là lý do nên xem `collections.abc` trước khi tự viết một collection.\n\n### Trừu tượng sớm — cái giá cụ thể\n\n```python\n# Tháng 1: một nguồn dữ liệu, nhưng đã dựng trừu tượng\nclass NguonDuLieu(ABC):\n    @abstractmethod\n    def truy_van(self, sql: str) -> list[dict]: ...     # ⚠️ \"sql\" trong giao diện\n\nclass NguonPostgres(NguonDuLieu):\n    def truy_van(self, sql): ...\n\n# Tháng 6: thêm nguồn API — KHÔNG có SQL\nclass NguonAPI(NguonDuLieu):\n    def truy_van(self, sql: str) -> list[dict]:\n        raise NotImplementedError(\"API không dùng SQL\")   # ⚠️ vi phạm Liskov\n```\n\nGiao diện được rút ra từ **một** bản cài đặt nên nó mang theo chi tiết của bản đó (`sql`). Bản thứ hai không vừa, và cách chữa vội là để một phương thức trống — đúng dấu hiệu trừu tượng sai.\n\n```python\n# ✓ Giao diện rút ra từ điểm chung THẬT của cả hai\nclass NguonDuLieu(ABC):\n    @abstractmethod\n    def lay(self, dieu_kien: dict) -> list[dict]:\n        \"\"\"Lấy bản ghi theo điều kiện. Mỗi nguồn tự dịch sang cách của mình.\"\"\"\n\nclass NguonPostgres(NguonDuLieu):\n    def lay(self, dieu_kien):\n        menh_de = \" AND \".join(f\"{k} = %s\" for k in dieu_kien)\n        ...\n\nclass NguonAPI(NguonDuLieu):\n    def lay(self, dieu_kien):\n        ...      # gọi HTTP với query string\n```\n\nBài học: **đợi bản cài đặt thứ hai rồi mới rút giao diện.** Điểm chung thật chỉ lộ ra khi có ít nhất hai ví dụ để so sánh.\n\n### Dùng cả `ABC` lẫn `Protocol`\n\n```python\nfrom abc import ABC, abstractmethod\nfrom typing import Protocol\n\n# Giao diện CÔNG KHAI — ai có đúng hình dạng đều dùng được\nclass BoThongBao(Protocol):\n    def gui(self, den: str, noi_dung: str) -> bool: ...\n\n# Class NỀN tiện lợi — cho ai muốn dùng code chung\nclass BoThongBaoNen(ABC):\n    so_lan_thu = 3\n\n    @abstractmethod\n    def _gui_mot_lan(self, den: str, noi_dung: str) -> bool: ...\n\n    def gui(self, den: str, noi_dung: str) -> bool:      # ⭐ retry dùng chung\n        for lan in range(self.so_lan_thu):\n            if self._gui_mot_lan(den, noi_dung):\n                return True\n        return False\n\nclass GuiEmail(BoThongBaoNen):          # dùng class nền, có retry sẵn\n    def _gui_mot_lan(self, den, noi_dung): ...\n\nclass GuiTuThuVienKhac:                  # ⭐ không kế thừa, vẫn hợp lệ\n    def gui(self, den, noi_dung): return True\n\ndef thong_bao(bo: BoThongBao, den: str, tin: str) -> None:\n    if not bo.gui(den, tin):\n        print(\"gửi thất bại\")\n\nthong_bao(GuiEmail(), \"a@x.com\", \"chào\")\nthong_bao(GuiTuThuVienKhac(), \"a@x.com\", \"chào\")     # ✓ cả hai đều được\n```\n\nĐây là mẫu tốt cho thư viện: **`Protocol` là giao ước, `ABC` là tiện ích.** Người dùng chọn cách nào cũng được, và bạn không ép ai vào cây kế thừa của mình.",

so_sanh: "### Ba mức ràng buộc\n\n| | Quy ước | `NotImplementedError` | `ABC` |\n|---|---|---|---|\n| Phát hiện lỗi | không | khi **gọi** | khi **tạo object** ⭐ |\n| Thông báo rõ ràng | ✗ `AttributeError` | ✓ | **✓ nêu rõ thiếu gì** |\n| Cần import gì | không | không | `abc` |\n| Cho phương thức tuỳ chọn | ✓ | **✓** | ✗ bắt buộc hết |\n| Dùng khi | nguyên mẫu | phương thức **tuỳ chọn** | **giao ước bắt buộc** |\n\nCột giữa vẫn có chỗ: khi lớp con **được phép** không cài đặt, nhưng gọi tới thì phải báo rõ ràng.\n\n### `ABC` và `Protocol`\n\n| | `ABC` | `Protocol` |\n|---|---|---|\n| Kiểu | **danh nghĩa** | **cấu trúc** |\n| Phải kế thừa | ✓ | ✗ |\n| Kiểm tra lúc tạo object | **✓** | ✗ |\n| Kiểm tra tĩnh | ✓ | ✓ |\n| `isinstance` | ✓ | chỉ với `runtime_checkable`, và **nông** |\n| Class bên thứ ba | qua `register` (không kiểm tra) | **✓ tự nhiên** |\n| Chia sẻ code cài đặt | **✓ mạnh** | hạn chế |\n| Class giả cho test | phải cài đủ | **✓ gọn** |\n| Ràng buộc | chặt | **lỏng** |\n\nCách chọn:\n\n```\nKiểm soát mọi bản cài đặt + có code chung đáng chia sẻ  → ABC\nNhận cả class bên ngoài, hoặc muốn ràng buộc lỏng        → Protocol ⭐ mặc định\nThư viện công khai                                        → cả hai\n```\n\n### `NotImplementedError` và `NotImplemented`\n\n| | `NotImplementedError` | `NotImplemented` |\n|---|---|---|\n| Là gì | **ngoại lệ** | **giá trị đặc biệt** |\n| Dùng với | `raise` | `return` |\n| Ngữ cảnh | phương thức chưa cài đặt | `__eq__`, `__add__`... |\n| Ý nghĩa | \"lớp con phải làm\" | \"tôi không xử lý được, thử cách khác\" |\n\nNhầm hai cái này gây lỗi rất khó hiểu: `raise NotImplemented` cho thông báo vô nghĩa, còn `return NotImplementedError` làm toán tử trả về một class thay vì kết quả.\n\n### Chú thích kiểu: cụ thể hay trừu tượng\n\n| Tham số nhận | Nhận được gì | Đánh giá |\n|---|---|---|\n| `list[int]` | chỉ `list` | quá hẹp |\n| `Sequence[int]` | `list`, `tuple`, `str` | vừa, nếu cần chỉ số |\n| `Iterable[int]` | thêm `set`, generator | **rộng nhất, thường đúng** |\n| `Any` | mọi thứ | quá rộng, mất kiểm tra |\n\nNguyên tắc: **nhận rộng nhất có thể ở tham số, trả cụ thể nhất có thể ở giá trị trả về.** Trả `list` cho người gọi biết chắc họ lặp lại được và `len()` được.\n\n### Khi nào tạo trừu tượng\n\n| Tình huống | Tạo? |\n|---|---|\n| Đã có **hai hoặc ba** bản cài đặt thật | **✓** |\n| Cần thay bản thật bằng bản giả khi test | ✓ |\n| Nhiều người viết bản cài đặt song song | ✓ |\n| Mới có **một** bản, đoán sẽ có bản hai | **✗ đợi** |\n| Chỉ để \"cho đúng OOP\" | ✗ |\n\nHàng thứ tư là sai lầm tốn kém nhất: giao diện rút từ một ví dụ mang theo chi tiết của ví dụ đó, và bản thứ hai gần như luôn không vừa.\n\n### Dấu hiệu trừu tượng sai\n\n| Dấu hiệu | Nghĩa là |\n|---|---|\n| Một bản cài đặt phải để phương thức trống hoặc `raise` | giao diện quá rộng — vi phạm Interface Segregation |\n| Thêm bản cài đặt phải **sửa** giao diện | giao diện mô tả một bản cụ thể |\n| Giao diện mang tên riêng của một công nghệ (`sql`, `redis`) | rút ra từ một ví dụ |\n| Chỉ có một lớp con, và sẽ mãi vậy | trừu tượng thừa |\n| Lớp con phải kiểm tra `type(self)` | thiết kế đa hình chưa đúng |\n\nHàng đầu nối thẳng sang bài **Interface Segregation** trong module: giao diện lớn ép người cài đặt phải làm cả những gì họ không cần.",

loi_thuong_gap: [
  "**Đặt `@abstractmethod` sai thứ tự với `@property`.** Phải nằm dưới cùng, sát `def`. Sai thứ tự thì mất tác dụng và **không báo lỗi**.",
  "**Dùng `raise NotImplementedError` khi `ABC` phù hợp hơn.** Lỗi chỉ lộ ra khi gọi tới, có thể là trên môi trường thật.",
  "**Nhầm `NotImplementedError` với `NotImplemented`.** Cái đầu là ngoại lệ để `raise`, cái sau là giá trị để `return` trong toán tử.",
  "**Tạo trừu tượng khi mới có một bản cài đặt.** Giao diện sẽ mang theo chi tiết của bản đó và bản thứ hai không vừa.",
  "**Để một bản cài đặt `raise NotImplementedError` cho phương thức của giao diện.** Dấu hiệu giao diện quá rộng, vi phạm Liskov.",
  "**Dùng `ABC.register` tưởng là có kiểm tra.** Nó không kiểm tra gì — đăng ký class không có phương thức nào vẫn thành công.",
  "**Tin `isinstance` với `Protocol` kiểm tra đầy đủ.** Nó chỉ xét tên phương thức tồn tại, không xét chữ ký hay kiểu.",
  "**Quên `@runtime_checkable` khi cần `isinstance` với `Protocol`.** `TypeError` lúc chạy.",
  "**Chú thích tham số là `list` khi chỉ cần lặp qua.** Từ chối `tuple`, `set`, generator vô cớ. Dùng `Iterable`.",
  "**Ép người dùng thư viện kế thừa `ABC` của mình.** Dùng `Protocol` — họ chỉ cần có đúng phương thức.",
  "**Nhồi mọi phương thức tiện ích vào `ABC`.** Lớp con bị buộc dùng cách cài đặt đó. Tiện ích không thuộc về mọi bản thì để thành hàm ngoài.",
  "**Tự viết collection từ đầu thay vì kế thừa `collections.abc`.** Kế thừa `Sequence` và cài hai phương thức là có sáu tính năng.",
  "**Giao diện mang tên riêng của một công nghệ.** `truy_van(sql)` ép mọi bản cài đặt phải là SQL."
],

tu_kiem_tra: [
  "Nêu ba mức ràng buộc và thời điểm mỗi mức phát hiện lỗi.",
  "`ABC` chặn việc tạo instance bằng cơ chế nào? Kiểm tra bằng thuộc tính nào?",
  "Vì sao `@abstractmethod` phải nằm dưới `@property`? Sai thứ tự thì hiện tượng gì xảy ra?",
  "Phân biệt kiểu danh nghĩa và kiểu cấu trúc. `ABC` và `Protocol` thuộc loại nào?",
  "Nêu ba lợi ích của `Protocol` so với `ABC`, và một cái giá.",
  "Giới hạn của `isinstance` với `runtime_checkable` `Protocol` là gì?",
  "Khi nào `ABC` thắng `Protocol` rõ ràng?",
  "`ABC.register` làm gì và vì sao nó không phải giải pháp tốt cho class bên thứ ba?",
  "Phân biệt `NotImplementedError` và `NotImplemented`.",
  "Vì sao không nên tạo trừu tượng khi mới có một bản cài đặt? Cái giá cụ thể là gì?",
  "Nêu năm dấu hiệu của trừu tượng sai.",
  "Nguyên tắc \"nhận rộng, trả cụ thể\" nghĩa là gì? Cho ví dụ.",
  "Kế thừa `collections.abc.Sequence` cho lại những gì miễn phí?",
  "Mẫu \"dùng cả `Protocol` lẫn `ABC`\" hoạt động thế nào và hợp với loại code nào?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, module `abc` — `ABC`, `abstractmethod`, `register` và cơ chế `ABCMeta`.",
  "Tài liệu Python, module `collections.abc` — bảng liệt kê phương thức bắt buộc và phương thức được cho miễn phí của từng ABC.",
  "Tài liệu Python, module `typing`, phần *Protocols* — cú pháp, `runtime_checkable`, và giới hạn của nó.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về interface, `ABC` và `Protocol` là phần đầy đủ nhất về chủ đề này.",
  "Sách **Architecture Patterns with Python** — Harry Percival & Bob Gregory. Áp dụng trừu tượng vào kiến trúc thật, gồm cả khi nào không nên."
],

lien_he: [
  "**Đa hình và duck typing** ngay trước: trừu tượng là cách viết giao diện ngầm định của duck typing ra thành giao ước.",
  "**Kế thừa và MRO**: `ABC` hoạt động qua kế thừa; `Protocol` thì không cần.",
  "**Composition over inheritance** ngay sau: `Protocol` làm composition dễ hơn vì không tạo ràng buộc kế thừa.",
  "**SOLID — Interface Segregation**: giao diện quá rộng ép người cài đặt làm cả những gì họ không cần — đúng dấu hiệu trừu tượng sai ở bài này.",
  "**SOLID — Dependency Inversion**: phụ thuộc vào trừu tượng chứ không vào bản cài đặt cụ thể.",
  "**SOLID — Liskov Substitution**: bản cài đặt `raise NotImplementedError` là vi phạm trực tiếp.",
  "**Design pattern — Factory, Strategy**: cả hai đều dựa trên một giao diện chung cho nhiều bản cài đặt.",
  "**Type hint và mypy** ở module 5: `Protocol` là cách lấy an toàn kiểu mà không mất tính động.",
  "**Đóng gói**: trừu tượng quyết định *phải có gì*, đóng gói quyết định *lộ ra gì* — hai mặt của thiết kế giao diện.",
  "**Backend & Web** ở mảng 05: repository và adapter đều là trừu tượng để đổi được nguồn dữ liệu và nhà cung cấp.",
  "**AI/ML** ở mảng 07: `Dataset` của các thư viện học sâu là một trừu tượng — chỉ cần cài `__len__` và `__getitem__` là dùng được với toàn bộ hệ sinh thái."
]

};
