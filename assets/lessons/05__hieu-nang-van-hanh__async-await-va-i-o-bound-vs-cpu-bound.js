window.LESSONS["05:hieu-nang-van-hanh:async-await-va-i-o-bound-vs-cpu-bound"] = {

tom_tat: [
  "`async def` cho phép một worker FastAPI phục vụ yêu cầu khác trong lúc chờ socket, database hoặc Redis; nó không làm phép tính Python chạy nhanh hơn.",
  "I/O-bound bị giới hạn bởi thời gian chờ thiết bị hay dịch vụ bên ngoài, còn CPU-bound bị giới hạn bởi số chu kỳ tính toán; chọn sai mô hình làm event loop bị đứng.",
  "Trong chuỗi đặt vé, các lần đọc suất diễn và gọi cổng thanh toán có thể chồng thời gian chờ, nhưng dựng PDF hoặc ký hàng nghìn mã QR phải chuyển sang process hay hàng đợi tác vụ.",
  "Cancellation, timeout, giới hạn đồng thời và backpressure là phần của tính đúng đắn, không chỉ là tối ưu hiệu năng.",
  "Không được giữ transaction database qua một lời gọi mạng chậm; cách đó vừa chiếm connection vừa kéo dài lock và mở rộng phạm vi lỗi."
],

can_biet_truoc: [
  "Request–response của HTTP và ý nghĩa status code 2xx, 4xx, 5xx.",
  "Process, thread, concurrency và parallelism; đặc biệt là GIL của CPython.",
  "Coroutine, socket và connection pool ở mức khái niệm.",
  "FastAPI dependency injection và cách khai báo path operation bằng `def` hoặc `async def`."
],

dinh_nghia: "**Asynchronous I/O** là cách một tác vụ chủ động nhường quyền điều khiển khi chưa thể tiến tiếp vì đang chờ I/O, để event loop chạy coroutine khác trên cùng thread. Trong Python, `await` đánh dấu điểm có thể nhường; đối tượng được chờ phải là awaitable và thư viện bên dưới phải thật sự dùng I/O không chặn.\n\n**I/O-bound** mô tả công việc mà phần lớn thời gian trôi qua trong lúc chờ database, mạng hoặc đĩa. **CPU-bound** mô tả công việc mà lõi xử lý bận tính toán, chẳng hạn nén ảnh, dựng PDF phức tạp hay ký số hàng loạt. Hai nhãn nói về nút thắt của một đoạn việc cụ thể, không phải nhãn vĩnh viễn của cả ứng dụng.\n\n`async` là mô hình điều phối concurrency có tính hợp tác: coroutine chỉ nhường tại `await`. Nó không tự tạo thread, không biến hàm đồng bộ thành bất đồng bộ và không đem lại parallelism cho mã Python CPU-bound.",

vi_sao_can: "Một yêu cầu đặt vé có thể chỉ dùng vài mili giây CPU nhưng chờ database 20 ms, Redis 5 ms và cổng thanh toán 300 ms. Nếu worker đứng yên trong toàn bộ các khoảng chờ, muốn chịu nhiều kết nối ta phải tăng rất nhiều thread hoặc process. Mỗi thread có stack, chi phí chuyển ngữ cảnh và rủi ro tranh chấp; mỗi process lại nhân bản bộ nhớ ứng dụng.\n\nEvent loop giải bài toán khác: khi socket chưa có dữ liệu, hệ điều hành theo dõi socket đó, còn thread Python xử lý yêu cầu khác. Một worker vì thế có thể giữ nhiều kết nối đang chờ với lượng tài nguyên hợp lý. Lợi ích chỉ xuất hiện khi đường đi không chứa lời gọi chặn.\n\nNguy hiểm nằm ở chỗ FastAPI cho phép viết `async def` rất dễ, khiến người mới tưởng mọi thứ bên trong tự động không chặn. Chỉ một `time.sleep(2)`, một HTTP client đồng bộ hoặc phép băm mật khẩu quá nặng chạy ngay trên event loop có thể làm mọi request cùng worker ngừng tiến. Độ trễ đuôi tăng mạnh dù CPU trung bình có thể chưa đầy; đây là lỗi vận hành khó nhìn nếu chỉ xem throughput.\n\nTrong API đặt vé, tính đúng còn quan trọng hơn tốc độ. Client ngắt kết nối giữa lúc thanh toán không có nghĩa cổng thanh toán đã hủy; retry thiếu idempotency có thể thu tiền hai lần. Timeout không phải chiếc kéo hoàn tác thế giới bên ngoài. Thiết kế async tốt phải xác định deadline, trạng thái bền vững và cách hòa giải kết quả đến muộn.",

co_che: "### Event loop thực sự luân phiên cái gì\n\nEvent loop giữ hàng đợi coroutine có thể chạy và đăng ký mối quan tâm với hệ điều hành: socket nào cần đọc, socket nào chờ ghi, timer nào sắp hết hạn. Khi coroutine gọi `await client.get(...)`, thư viện gửi phần có thể gửi, đăng ký socket rồi trả quyền điều khiển. Event loop không ngồi kiểm tra liên tục; nó dùng cơ chế thông báo I/O của hệ điều hành để ngủ cho tới khi có sự kiện.\n\nKhi dữ liệu đến, coroutine được đưa lại vào hàng đợi sẵn sàng. Python tiếp tục nó ngay sau `await`, với kết quả hoặc ngoại lệ. Vì sự chuyển lượt xảy ra ở điểm rõ ràng nên async thường dễ suy luận hơn nhiều thread cùng sửa dữ liệu, nhưng không có nghĩa là hết race condition: hai coroutine vẫn có thể đọc cùng trạng thái, cùng `await`, rồi cùng ghi dựa trên dữ liệu cũ.\n\n### `await` không đồng nghĩa chạy song song\n\nViết tuần tự:\n\n```python\nshow = await show_client.get(show_id)\nprice = await pricing_client.quote(show_id)\n```\n\nthì lần thứ hai chỉ bắt đầu sau lần thứ nhất. Nếu hai kết quả độc lập, `asyncio.gather` cho phép hai thời gian chờ chồng lên nhau. Nhưng phép tính Python giữa các điểm `await` vẫn chạy độc chiếm thread event loop. Một vòng lặp tạo 100.000 chữ ký không hề nhường chỉ vì hàm ngoài mang từ khóa `async`.\n\nConcurrency là nhiều việc cùng tiến triển theo thời gian; parallelism là nhiều việc thực sự chạy đồng thời trên nhiều lõi. Async cung cấp loại đầu cho I/O. Process pool hoặc worker Celery nhiều process mới cung cấp loại sau cho CPU-bound trong CPython.\n\n### FastAPI xử lý `def` và `async def`\n\nVới path operation khai báo `async def`, FastAPI gọi nó trên event loop. Vì vậy mọi thư viện I/O trong đường đi cần giao diện async: HTTPX `AsyncClient`, driver database async, Redis async. Với path operation `def`, framework đưa hàm sang thread pool để lời gọi đồng bộ không khóa event loop.\n\nThread pool là cầu nối hữu ích cho thư viện cũ, không phải tài nguyên vô hạn. Nếu mỗi request giữ một thread khi gọi dịch vụ mất 30 giây, pool sẽ cạn và các request mới xếp hàng. Tăng số thread che triệu chứng trong một khoảng ngắn nhưng tăng bộ nhớ và áp lực lên dịch vụ hạ nguồn.\n\n### Nhận diện I/O-bound và CPU-bound bằng quan sát\n\nĐừng phân loại theo tên hàm. Đọc file nhỏ có thể bị cache trong RAM và rất nhanh; giải mã JSON khổng lồ lại đốt CPU. Hãy đo wall time, CPU time và profile. Nếu wall time cao trong khi CPU time thấp, tác vụ thường đang chờ. Nếu một lõi luôn bận và thời gian gần bằng CPU time, nó thiên về tính toán.\n\nTrong chuỗi đặt vé: truy vấn PostgreSQL và gọi thanh toán là I/O-bound; tạo một mã QR nhỏ có thể không đáng kể; dựng 5.000 vé PDF có font và ảnh là CPU-bound. Ranh giới thay đổi theo kích thước đầu vào, nên endpoint cần giới hạn số vé thay vì giả định workload luôn nhỏ.\n\n### Giới hạn đồng thời thay vì phóng hàng nghìn coroutine\n\n`gather` không tạo thread, nhưng mỗi coroutine vẫn tiêu thụ socket, connection pool, bộ nhớ và quota. Phóng 10.000 lời gọi pricing cùng lúc có thể tự gây từ chối dịch vụ lên hệ thống của mình. Semaphore đặt ngân sách đồng thời:\n\n```python\nlimit = asyncio.Semaphore(20)\n\nasync def quote_one(show_id: str):\n    async with limit:\n        return await pricing.quote(show_id)\n```\n\nCon số 20 phải liên hệ với connection pool và khả năng hạ nguồn. Nếu pool chỉ có 10 connection, semaphore 100 không tạo thêm throughput; 90 coroutine chỉ xếp hàng ở vị trí khó quan sát hơn. Backpressure nghĩa là khi năng lực đã đầy, hệ thống làm chậm hoặc từ chối có kiểm soát thay vì nhận vô hạn rồi sụp.\n\n### Timeout là ngân sách, không phải phép màu\n\nMỗi lời gọi mạng phải có timeout kết nối, đọc và tổng thời gian phù hợp với deadline của request. Endpoint có ngân sách 800 ms thì không thể để ba bước mỗi bước timeout 1 giây. Nên truyền deadline còn lại xuống các tầng và chừa thời gian tạo response.\n\nKhi `asyncio.timeout` hủy coroutine, Python ném `CancelledError` tại điểm chờ. Thư viện có thể đóng socket, nhưng server bên kia có thể đã nhận lệnh. Với thanh toán, cần idempotency key gắn với reservation và endpoint tra cứu trạng thái. Không retry mù một thao tác có side effect.\n\n### Cancellation và dọn tài nguyên\n\nClient đóng tab, reverse proxy hết timeout hoặc server shutdown đều có thể hủy task. Code phải dùng context manager để trả connection về pool và `finally` cho phần dọn bắt buộc. Không nuốt `CancelledError`; nuốt nó khiến shutdown treo và giữ tài nguyên lâu hơn dự kiến.\n\nKhông nên dùng `asyncio.shield` quanh toàn bộ đặt vé chỉ để tránh hủy. Shield làm công việc tiếp tục dù người gọi đã biến mất và có thể tạo tải ma. Nếu một bước bắt buộc phải hoàn tất, hãy ghi trạng thái bền vững rồi chuyển sang hàng đợi nền có retry và quan sát được.\n\n### Transaction không được bắc qua mạng bên ngoài\n\nGiữ transaction database trong lúc `await payment.charge()` là thiết kế nguy hiểm. Trong hàng trăm mili giây đó, connection bị chiếm, row lock có thể chặn người khác, và timeout mạng biến thành lock kéo dài. Cách bền hơn là transaction ngắn tạo reservation ở trạng thái `PENDING_PAYMENT`, commit, gọi thanh toán bằng idempotency key, rồi transaction ngắn khác chuyển trạng thái.\n\nĐây là một state machine, không phải transaction phân tán giả. Nếu process chết sau khi thanh toán thành công nhưng trước cập nhật, job hòa giải truy vấn cổng thanh toán và sửa trạng thái. Ta chấp nhận nhất quán cuối cùng có kiểm soát để không khóa database theo độ trễ Internet.\n\n### CPU-bound phải rời event loop\n\n`asyncio.to_thread` phù hợp cho lời gọi đồng bộ ngắn hoặc thư viện giải phóng GIL, nhưng không làm hai vòng lặp Python nặng chạy song song thực sự. Với phép tính lớn, dùng process pool hoặc hàng đợi Celery/RQ. Process có không gian nhớ riêng và tận dụng nhiều lõi, đổi lại phải tuần tự hóa dữ liệu, quản lý timeout và chịu chi phí khởi động.\n\nKhông gửi toàn bộ ảnh/PDF lớn qua message broker; lưu object storage rồi gửi định danh. Đồng thời giới hạn kích thước đầu vào để kẻ tấn công không biến endpoint tạo vé thành công cụ đốt CPU.\n\n### Bảo mật trong đường đi bất đồng bộ\n\nAsync tăng số kết nối sống đồng thời, nên giới hạn body, header, timeout đọc và số kết nối là hàng rào chống slow-client. Mọi lời gọi URL do người dùng ảnh hưởng cần chống SSRF: allowlist host, không tin redirect tùy ý, và chặn địa chỉ nội bộ.\n\nContext của request như người dùng và request ID cần truyền bằng `contextvars`, không lưu trong biến toàn cục. Biến toàn cục sẽ bị request khác ghi đè khi coroutine đổi lượt. Secret không được đưa vào log khi exception của HTTP client chứa header.\n\n### Khi nào không nên dùng async\n\nMột dịch vụ CRUD nhỏ, lưu lượng thấp và phụ thuộc toàn thư viện đồng bộ có thể rõ ràng hơn với `def` cùng thread pool có giới hạn. Chuyển nửa vời sang async tạo hai hệ sinh thái client, hai kiểu test và lỗi quên `await`. Async cũng không chữa truy vấn SQL chậm, pool sai kích thước hay downstream quá tải.\n\nChỉ đổi mô hình sau khi xác định thời gian chờ I/O là nút thắt và đội ngũ có khả năng quan sát cancellation, pool, timeout. Đôi khi thêm index database đem lại nhiều hơn việc viết lại toàn bộ stack.",

vi_du: "### Endpoint đặt vé có deadline và giới hạn fan-out\n\nVí dụ chạy được dưới đây dùng FastAPI và mô phỏng hai dịch vụ bằng `asyncio.sleep`; không ghi số benchmark giả. Chạy `uvicorn app:app`, rồi gửi `POST /reservations` với JSON `{\"show_id\":\"show-42\",\"seats\":[\"A1\",\"A2\"]}`.\n\n```python\nimport asyncio\nimport hashlib\nfrom typing import Annotated\n\nfrom fastapi import FastAPI, Header, HTTPException\nfrom pydantic import BaseModel, Field\n\napp = FastAPI()\npayment_slots = asyncio.Semaphore(16)\n\nclass ReservationIn(BaseModel):\n    show_id: str = Field(min_length=1, max_length=64)\n    seats: list[str] = Field(min_length=1, max_length=8)\n\nasync def read_show(show_id: str) -> dict:\n    await asyncio.sleep(0.02)\n    return {\"id\": show_id, \"open\": True, \"price\": 120_000}\n\nasync def charge(key: str, amount: int) -> str:\n    async with payment_slots:\n        await asyncio.sleep(0.05)\n        return \"pay_\" + hashlib.sha256(key.encode()).hexdigest()[:12]\n\n@app.post(\"/reservations\", status_code=201)\nasync def reserve(\n    body: ReservationIn,\n    idempotency_key: Annotated[str, Header(min_length=16, max_length=128)],\n):\n    if len(set(body.seats)) != len(body.seats):\n        raise HTTPException(422, \"Danh sách ghế bị trùng\")\n    try:\n        async with asyncio.timeout(0.8):\n            show = await read_show(body.show_id)\n            if not show[\"open\"]:\n                raise HTTPException(409, \"Suất diễn đã đóng\")\n            amount = show[\"price\"] * len(body.seats)\n            payment_id = await charge(idempotency_key, amount)\n    except TimeoutError as exc:\n        raise HTTPException(504, \"Dịch vụ phụ thuộc quá chậm\") from exc\n    return {\"status\": \"CONFIRMED\", \"payment_id\": payment_id}\n```\n\nVí dụ cố ý giữ dữ liệu trong bộ nhớ và chưa có transaction để tập trung vào lịch chạy. Bản production phải ràng buộc unique cho idempotency key, xác thực người dùng có quyền đặt suất diễn, lưu state machine và hòa giải thanh toán không rõ kết quả.\n\n### Thí nghiệm thấy sự khác biệt mà không gán số trước\n\n```python\nimport asyncio\nimport time\n\nasync def io_job():\n    await asyncio.sleep(0.1)\n\nasync def concurrent():\n    await asyncio.gather(*(io_job() for _ in range(10)))\n\nstart = time.perf_counter()\nasyncio.run(concurrent())\nprint(f\"10 lần chờ chồng nhau: {time.perf_counter() - start:.3f}s\")\n```\n\nKết quả phụ thuộc máy và bộ lập lịch nên chương trình tự in số đo. Đổi `await asyncio.sleep(0.1)` thành `time.sleep(0.1)` sẽ khóa event loop; đó là cách tái hiện lỗi rõ nhất.",

so_sanh: "### Chọn mô hình theo nơi thời gian bị tiêu thụ\n\n| Phương án | Hợp với | Lợi thế | Chi phí và dấu hiệu không nên dùng |\n|---|---|---|---|\n| FastAPI `async def` | Nhiều I/O có driver async | Ít thread, cancellation rõ, giữ nhiều kết nối chờ | Một lời gọi chặn ảnh hưởng cả worker; không tăng tốc CPU |\n| FastAPI `def` + thread pool | Thư viện đồng bộ, tải vừa | Mã tuyến tính, tích hợp thư viện cũ | Pool cạn khi I/O lâu; thread không vượt GIL cho Python CPU-bound |\n| Process pool | Phép tính độc lập, dữ liệu vừa | Parallelism trên nhiều lõi | Tốn bộ nhớ, tuần tự hóa và vận hành lifecycle |\n| Celery/RQ worker | Công việc lâu, cần retry bền vững | Tách khỏi deadline HTTP, scale riêng | Cần broker, idempotency, theo dõi và xử lý trùng |\n\nKhông có cột “nhanh nhất” vì throughput phụ thuộc workload. Một endpoint chủ yếu SQL nhanh có thể chạy tốt ở cả hai kiểu; điều quyết định là thư viện, pool, deadline và khả năng vận hành của đội.",

loi_thuong_gap: [
  "**Gọi `requests.get` trong `async def`.** Nhận biết qua event-loop lag và mọi endpoint cùng chậm khi downstream chậm; dùng HTTP client async hoặc cô lập tạm trong thread pool.",
  "**Cho rằng thêm `await` tạo parallelism.** CPU vẫn bám một lõi và latency không giảm; chuyển phép tính lớn sang process hay worker nền.",
  "**`gather` danh sách không giới hạn.** Socket, RAM hoặc quota tăng vọt; chia lô và dùng semaphore gắn với pool thực.",
  "**Giữ transaction khi gọi thanh toán.** Pool database cạn, lock wait tăng; tách thành state machine với transaction ngắn.",
  "**Retry sau timeout mà thiếu idempotency.** Xuất hiện hai khoản thu cho một reservation; dùng khóa duy nhất và API tra cứu trạng thái.",
  "**Nuốt `CancelledError`.** Deploy shutdown kéo dài và connection không được trả; dọn trong `finally` rồi để cancellation lan truyền.",
  "**Lưu request ID hay user trong biến toàn cục.** Log lẫn danh tính giữa request; dùng `contextvars` hoặc truyền tường minh."
],

tu_kiem_tra: [
  "Vì sao `async def` chứa `time.sleep(1)` có thể tệ hơn path operation đồng bộ `def` chứa cùng lời gọi?",
  "Trong luồng đọc suất diễn và lấy báo giá, điều kiện nào cho phép dùng `gather`, và điều kiện nào buộc chạy tuần tự?",
  "Timeout phía client xảy ra sau khi cổng thanh toán nhận lệnh. Những trạng thái nào phải tồn tại để retry an toàn?",
  "Vì sao semaphore 100 không hữu ích nếu HTTP connection pool chỉ có 10 connection?",
  "Một hàm nén ảnh bằng thư viện native giải phóng GIL nên dùng thread hay process? Bạn cần đo và kiểm tra điều gì trước khi chọn?",
  "Hãy chỉ ra ranh giới transaction phù hợp để không giữ row lock qua lời gọi mạng.",
  "Event-loop lag tăng nhưng CPU toàn máy thấp. Nêu ba nguyên nhân có thể kiểm chứng thay vì kết luận thiếu CPU."
],

tai_nguyen: [
  "Python documentation — Coroutines and Tasks (`asyncio`).",
  "FastAPI documentation — Concurrency and async/await.",
  "Architecture Patterns with Python — Harry Percival và Bob Gregory.",
  "Designing Data-Intensive Applications — Martin Kleppmann."
],

lien_he: [
  "Nối sang mảng Hệ điều hành: event loop dựa vào cơ chế hệ điều hành báo socket sẵn sàng, còn process pool đổi bộ nhớ lấy parallelism.",
  "Nối sang mảng Database: transaction ngắn, connection pool và row lock quyết định liệu endpoint async có thật sự chịu tải.",
  "Nối sang mảng Bảo mật web: timeout, giới hạn body, SSRF và idempotency bảo vệ đường đi có nhiều I/O.",
  "Nối sang bài tác vụ nền trong chính module: CPU-bound và việc phải sống lâu hơn request sẽ được đưa sang Celery/RQ."
]

};
