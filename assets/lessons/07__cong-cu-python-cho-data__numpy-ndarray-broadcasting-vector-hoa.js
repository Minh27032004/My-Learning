window.LESSONS["07:cong-cu-python-cho-data:numpy-ndarray-broadcasting-vector-hoa"] = {

tom_tat: [
  "`ndarray` là một khối dữ liệu đồng nhất có `shape`, `dtype` và cách bố trí bộ nhớ; hiểu ba thuộc tính này quan trọng hơn học thuộc danh sách hàm NumPy.",
  "Vector hoá chuyển vòng lặp Python thành phép toán trên cả mảng, để vòng lặp nặng chạy trong mã máy đã biên dịch và thường giảm chi phí tạo đối tượng Python.",
  "Broadcasting chỉ mở rộng kích thước có giá trị 1 theo quy tắc so từ trục cuối; nó không tùy ý đoán ý định và có thể tạo kết quả đúng kích thước nhưng sai nghĩa.",
  "View chia sẻ vùng nhớ với mảng gốc còn copy sở hữu dữ liệu riêng; phép sửa tại chỗ vì thế có thể lan sang biến tưởng như độc lập.",
  "Trong ví dụ thuê xe, ma trận đặc trưng được chuẩn hoá theo từng cột, tính dự báo tuyến tính và kiểm tra ca thiếu dữ liệu mà không cần vòng lặp theo từng dòng."
],

can_biet_truoc: [
  "Python cơ bản: `list`, vòng lặp `for`, slicing và cách gọi hàm.",
  "Khái niệm hàng, cột và ma trận; biết một bảng quan sát có thể biểu diễn thành ma trận số.",
  "Trung bình, độ lệch chuẩn và ý nghĩa của chuẩn hoá một biến số.",
  "Biết cài và import gói Python; ví dụ dùng `numpy` nhưng không phụ thuộc file bên ngoài."
],

dinh_nghia: "**NumPy** là thư viện tính toán mảng cho Python. Đối tượng trung tâm `ndarray` biểu diễn một mảng nhiều chiều có kích thước cố định, các phần tử thường cùng `dtype` và nằm trong một vùng nhớ có quy luật. **Vector hoá** là diễn đạt phép tính cho cả mảng thay vì tự lặp qua từng phần tử bằng Python. **Broadcasting** là bộ quy tắc cho phép NumPy kết hợp hai mảng có `shape` khác nhau khi các trục tương ứng bằng nhau hoặc một bên có kích thước 1.\n\nBa khái niệm gắn chặt với nhau: `ndarray` cung cấp dữ liệu liên tục và metadata; vector hoá giao việc lặp cho NumPy; broadcasting làm cho một vector hoặc số vô hướng tham gia phép toán với ma trận mà không cần nhân bản thủ công. Chúng không thay đổi toán học, chỉ thay đổi cách biểu diễn và nơi vòng lặp được thực thi.",

vi_sao_can: "Một pipeline dự báo nhu cầu thuê xe có hàng trăm nghìn giờ quan sát. Nếu mỗi lượt thuê, nhiệt độ và lượng mưa là một đối tượng Python riêng, interpreter phải kiểm tra kiểu và điều phối từng phép cộng. Chi phí quản lý có thể lớn hơn phép toán số học. `ndarray` gom dữ liệu cùng kiểu để mã C bên dưới xử lý theo khối, đồng thời là ngôn ngữ dữ liệu chung mà scikit-learn, pandas và PyTorch đều hiểu.\n\nLợi ích sâu hơn tốc độ là **diễn đạt đúng cấu trúc bài toán**. Khi chuẩn hoá, ta muốn trừ mỗi cột cho trung bình của chính cột đó. Biểu thức `(X - mean) / std` nói trực tiếp điều ấy; vòng lặp hai tầng che mất ý định trong chi tiết chỉ số. Tuy nhiên biểu thức ngắn không tự bảo đảm đúng. Nếu `mean` có shape `(n_rows, 1)` thay vì `(n_features,)`, broadcasting vẫn có thể chạy và chuẩn hoá theo hàng. Vì vậy người làm data phải đọc shape như lập trình viên hệ thống đọc kiểu dữ liệu.",

co_che: "### Một `ndarray` thật sự chứa gì\n\nHãy hình dung mảng không phải một hộp lồng nhiều danh sách, mà là phần mô tả đặt cạnh một vùng byte. `shape=(4, 3)` nói có bốn hàng và ba cột; `dtype=float64` nói mỗi phần tử chiếm tám byte và được giải mã thành số thực; `strides` nói phải nhảy bao nhiêu byte khi tăng chỉ số ở từng trục. Mảng C-contiguous thường đặt các phần tử cùng hàng cạnh nhau. Nhờ metadata, cùng vùng nhớ có thể được nhìn dưới shape khác mà không chép dữ liệu.\n\n```python\nimport numpy as np\n\na = np.arange(12, dtype=np.float64).reshape(4, 3)\nprint(a.shape, a.dtype, a.itemsize, a.strides)\nprint(a.nbytes)                 # bằng size * itemsize\n```\n\n`size` là 12 nên `nbytes` là 96. Con số này suy ra từ quy ước kiểu, không phải benchmark. Nếu đổi sang `float32`, vùng dữ liệu còn 48 byte nhưng độ chính xác biểu diễn giảm. Dùng kiểu nhỏ hơn hữu ích cho tensor lớn và GPU; không nên đổi chỉ vì muốn “tối ưu” khi phép cộng dồn nhạy với sai số.\n\n### `dtype` là hợp đồng tính toán\n\nMột Python `list` có thể trộn số, chuỗi và `None`; mảng số hiệu quả vì thường dùng một cách diễn giải byte. Khi tạo `np.array([1, 2, 3])`, NumPy suy luận kiểu nguyên. Phép chia tạo số thực, nhưng phép sửa tại chỗ có thể bị ràng buộc bởi kiểu đích. Giá trị quá lớn đối với kiểu nguyên hữu hạn có thể tràn; chuỗi lẫn vào có thể khiến cả mảng thành chuỗi hoặc `object`, làm mất lợi thế vector hoá.\n\nTrong dữ liệu thuê xe, `rentals` là số đếm nhưng trung bình dự báo là số thực. Giữ nhãn gốc ở kiểu nguyên là hợp lý; trước phép chuẩn hoá và đại số tuyến tính, ma trận đặc trưng nên chuyển rõ sang số thực. Kiểm tra `X.dtype` ngay sau khi đọc dữ liệu giúp phát hiện cột lượng mưa chứa ký hiệu `\"?\"` trước khi lỗi xuất hiện sâu trong mô hình.\n\n### Shape là kiểu dữ liệu thứ hai\n\nShape không chỉ để in khi debug. Vector `(3,)`, vector hàng `(1, 3)` và vector cột `(3, 1)` chứa ba số nhưng tham gia đại số khác nhau. Tích `X @ w` với `X.shape == (n, 3)` và `w.shape == (3,)` trả về `(n,)`. Nếu cần dự báo dạng cột để ghép với nhiều đầu ra, dùng `w[:, None]` để nhận `(3, 1)` và kết quả `(n, 1)`.\n\nMột thói quen tốt là viết chú thích shape ở ranh giới: `X: (số_giờ, số_đặc_trưng)`. Sau mỗi reshape quan trọng, dùng `assert X.ndim == 2` và kiểm số cột. Assertion không thay kiểm tra dữ liệu production, nhưng bắt lỗi giả định ngay gần nơi nó phát sinh.\n\n### Quy tắc broadcasting từ phải sang trái\n\nNumPy căn hai shape ở bên phải rồi xét từng trục. Hai kích thước tương thích khi chúng bằng nhau, hoặc một trong hai bằng 1. Thiếu trục phía trái được xem như kích thước 1. Vì vậy `(24, 3)` trừ `(3,)` hợp lệ: vector ba trung bình áp vào 24 hàng. `(24, 3)` trừ `(24, 1)` cũng hợp lệ, nhưng lúc này mỗi hàng bị trừ cùng một giá trị. Cả hai chạy; chỉ một cách đúng với chuẩn hoá theo cột.\n\n| Shape trái | Shape phải | Shape kết quả | Cách hiểu |\n|---|---|---|---|\n| `(336, 3)` | `(3,)` | `(336, 3)` | một giá trị cho mỗi đặc trưng |\n| `(336, 3)` | `(336, 1)` | `(336, 3)` | một giá trị cho mỗi giờ |\n| `(336, 1)` | `(24,)` | `(336, 24)` | tạo lưới giờ, có thể tốn nhớ |\n| `(336, 3)` | `(2,)` | lỗi | trục cuối 3 và 2 không tương thích |\n\nBroadcasting thường không nhân bản vật lý toán hạng kích thước 1; NumPy có thể dùng stride bằng 0 để đọc lại cùng giá trị. Nhưng **kết quả** `(336, 24)` vẫn phải tồn tại nếu phép toán sinh mảng mới. Việc đầu vào nhỏ không bảo đảm phép toán ít tốn RAM.\n\n### Vector hoá chuyển vòng lặp đi đâu\n\nVector hoá không xoá vòng lặp. Nó chuyển vòng lặp từ bytecode Python xuống implementation đã biên dịch, nơi kiểu phần tử đã biết và dữ liệu có thể được đọc liên tiếp. `np.maximum(rentals - 20, 0)` vẫn thăm từng phần tử, nhưng không gọi lại interpreter cho mỗi lượt. Đây là lý do đúng; nói “NumPy chạy song song” là quá mức vì nhiều ufunc chỉ chạy một luồng, còn một số phép đại số có thể nhờ thư viện BLAS đa luồng.\n\nVector hoá cũng có giá. Biểu thức `((X - mean) ** 2).sum()` có thể tạo mảng tạm cho phép trừ và bình phương. Với dữ liệu vừa, sự rõ ràng đáng giá. Với tensor rất lớn, mảng tạm làm đỉnh bộ nhớ tăng; có thể chia lô, dùng tham số `out`, hoặc chọn phép toán ghép phù hợp. Không nên viết lại bằng vòng lặp Python trước khi đo.\n\n### Ufunc, reduction và trục\n\nUfunc như `np.sqrt`, `np.maximum` áp một quy tắc phần tử. Reduction như `sum`, `mean`, `max` loại bỏ một hoặc nhiều trục. `X.mean(axis=0)` giảm trục hàng nên để lại một trung bình cho mỗi cột. `X.mean(axis=1)` giảm trục cột nên để lại một trung bình cho mỗi quan sát. Câu “axis 0 là cột” dễ gây nhầm; chính xác hơn là **axis được nêu là trục bị gộp**.\n\n`keepdims=True` giữ kích thước 1 tại trục đã gộp. Điều này hữu ích khi muốn broadcasting có chủ đích: trung bình theo hàng `X.mean(axis=1, keepdims=True)` có shape `(n, 1)`. Với chuẩn hoá theo cột, vector `(3,)` đã đủ và thường dễ đọc hơn.\n\n### View, copy và phép sửa tại chỗ\n\nSlicing cơ bản thường tạo view. `morning = rentals[:24]` có thể chia sẻ vùng nhớ với `rentals`; gán `morning[0] = 0` làm phần tử đầu của mảng gốc đổi theo. Fancy indexing như `rentals[[0, 3, 7]]` thường tạo copy. Đừng học thuộc mọi ngoại lệ; dùng `np.shares_memory(a, b)` khi quyền sở hữu quan trọng và gọi `.copy()` để tuyên bố cần độc lập.\n\nView tiết kiệm RAM và thời gian, nhưng dễ tạo bug khi một hàm âm thầm sửa đầu vào. Trong pipeline ML, ưu tiên hàm trả mảng mới trừ khi API ghi rõ sửa tại chỗ. Nếu tối ưu bộ nhớ bằng `X -= mean`, hãy chắc chắn `X` không còn được dùng làm dữ liệu thô để vẽ hoặc kiểm toán.\n\n### Thiếu dữ liệu, vô cực và so sánh số thực\n\n`NaN` là số thực đặc biệt biểu diễn giá trị không xác định. `NaN != NaN`, vì vậy không tìm thiếu dữ liệu bằng `x == np.nan`; dùng `np.isnan` hoặc `np.isfinite`. Hàm `np.mean` lan truyền `NaN`; `np.nanmean` bỏ qua chúng, nhưng chỉ hợp lệ khi việc bỏ qua đúng với câu hỏi. Nếu cảm biến mưa mất dữ liệu đúng lúc bão, trung bình trên các giờ còn lại tạo thiên lệch chứ không phải chữa lỗi.\n\nSo sánh kết quả số thực bằng `==` cũng dễ sai do làm tròn nhị phân. Test đại số nên dùng `np.allclose` với dung sai được giải thích. Với số đếm nguyên như lượt thuê, so sánh chính xác vẫn phù hợp.\n\n### Khi nào không nên dùng NumPy trực tiếp\n\nNumPy không phải bảng có tên cột, kiểu ngày giờ phong phú và khóa ghép; pandas phù hợp hơn cho làm sạch bảng. Dữ liệu thưa cực lớn cần cấu trúc sparse để không lưu hàng tỷ số 0. Tensor cần gradient và GPU nên đi qua PyTorch. Logic rẽ nhánh phức tạp trên vài chục đối tượng có thể rõ hơn bằng Python thuần. Chọn NumPy khi bài toán thật sự là tính toán mảng số trong RAM, không vì nó là công cụ quen tay.",

vi_du: "### Pipeline NumPy cho 14 ngày thuê xe\n\nĐoạn sau tự tạo 336 giờ dữ liệu xác định, chèn một giá trị mưa thiếu, chuẩn hoá ba đặc trưng và dự báo tuyến tính. Các hệ số chỉ minh hoạ cơ chế, không được tuyên bố là mô hình đã huấn luyện.\n\n```python\nimport numpy as np\n\nrng = np.random.default_rng(42)\nn = 14 * 24\nhour = np.arange(n) % 24\ntemperature = 24 + 6 * np.sin(2 * np.pi * (hour - 8) / 24)\nrain = rng.choice([0.0, 0.0, 0.0, 2.0, 8.0], size=n)\nis_weekend = ((np.arange(n) // 24) % 7 >= 5).astype(float)\nrentals = np.maximum(\n    0, 35 + 18 * np.sin(2 * np.pi * (hour - 7) / 24)\n    + 0.9 * temperature - 2.2 * rain + 8 * is_weekend\n).round().astype(int)\n\nrain[50] = np.nan\nX = np.column_stack([temperature, rain, is_weekend]).astype(np.float64)\nfinite_rows = np.isfinite(X).all(axis=1)\nX_clean, y_clean = X[finite_rows], rentals[finite_rows]\n\nmean = X_clean.mean(axis=0)\nstd = X_clean.std(axis=0)\nsafe_std = np.where(std == 0, 1.0, std)\nZ = (X_clean - mean) / safe_std\nweights = np.array([7.0, -5.0, 2.0])\nprediction = Z @ weights + y_clean.mean()\n\nassert Z.shape == (335, 3)\nassert np.isfinite(Z).all()\nprint(np.round(prediction[:5], 1))\n```\n\n`safe_std` xử lý cột hằng: chia cho độ lệch chuẩn 0 sẽ sinh vô cực hoặc `NaN`. Chuyển mẫu số thành 1 giữ cột đó ở 0 sau khi trừ trung bình. Trong mô hình thật, ta còn cân nhắc loại cột vì nó không mang thông tin. Quan trọng hơn, `mean` và `std` phải được tính trên tập train rồi tái dùng cho validation/test; tính trên toàn bộ dữ liệu gây leakage.\n\n### Kiểm tra broadcasting bằng phản ví dụ\n\n```python\ncol_mean = X_clean.mean(axis=0)                 # (3,)\ncorrect = X_clean - col_mean                    # theo cột\nrow_mean = X_clean.mean(axis=1, keepdims=True)  # (335, 1)\ndifferent_question = X_clean - row_mean         # theo hàng\n\nassert np.allclose(correct.mean(axis=0), 0.0)\nassert np.allclose(different_question.mean(axis=1), 0.0)\n```\n\nHai assertion đều đúng nhưng chứng minh hai invariant khác nhau. Đây là ca nguy hiểm hơn lỗi shape: chương trình chạy và số trông hợp lý. Cách phòng vệ là đặt tên theo ý nghĩa, ghi shape và test đúng trục thống kê mong muốn.\n\n### Các ca biên cần thử\n\nMảng rỗng khiến `mean` không có nghĩa và phát cảnh báo; pipeline nên từ chối batch rỗng hoặc trả trạng thái rõ. Một hàng vẫn tính được trung bình nhưng độ lệch chuẩn bằng 0. Cột toàn `NaN` không thể được cứu bằng `nanmean` một cách đáng tin. Số thuê cực lớn có thể tràn nếu ép xuống `int16`. Mảng không liên tục sau phép chuyển vị vẫn cho kết quả đúng, nhưng thư viện bên dưới có thể phải copy trước đại số tuyến tính.",

so_sanh: "### Chọn công cụ theo cấu trúc dữ liệu\n\n| Phương án | Mạnh ở đâu | Chi phí hoặc giới hạn | Dùng cho pipeline thuê xe |\n|---|---|---|---|\n| Python `list` | logic không đồng nhất, ít phần tử, dễ debug | nhiều đối tượng, vòng lặp số chậm | đọc vài cấu hình, không xử lý toàn bộ cột |\n| NumPy `ndarray` | mảng số dày, broadcasting, đại số tuyến tính | không có tên cột; dữ liệu phải vừa RAM | ma trận đặc trưng và phép dự báo |\n| pandas `DataFrame` | bảng có schema, ngày giờ, groupby, merge | overhead nhãn; phép theo từng ô chậm | làm sạch và ghép dữ liệu trạm |\n| PyTorch `Tensor` | gradient, GPU, mô hình học sâu | quản lý device và graph phức tạp hơn | huấn luyện neural network ở bài sau |\n\nNumPy thắng Python thuần khi số học mảng đủ lớn để chi phí gọi hàm được bù lại. Với ba số, tạo mảng có thể tốn hơn vòng lặp và làm code khó đọc. pandas không “thay thế” NumPy: nó thường lưu cột số bằng mảng và bổ sung nhãn. PyTorch có cú pháp gần NumPy nhưng tensor gắn với device, gradient và quy tắc sửa tại chỗ khác; chuyển qua lại có thể copy hoặc chia sẻ bộ nhớ tùy trường hợp.",

loi_thuong_gap: [
  "Nhìn thấy phép toán chạy được rồi cho rằng broadcasting đúng; dấu hiệu là mean sau chuẩn hoá bằng 0 ở sai trục.",
  "Dùng `reshape` hoặc `squeeze` cho đến khi hết lỗi mà không ghi ý nghĩa từng trục; dấu hiệu là code vỡ khi batch chỉ có một hàng.",
  "So sánh `NaN` bằng `==`; dấu hiệu là bộ lọc không bắt được phần tử mà khi in ra hiện `nan`.",
  "Sửa một slice và bất ngờ thấy mảng gốc đổi; kiểm bằng `np.shares_memory` và tạo `.copy()` khi cần độc lập.",
  "Ép mọi cột xuống kiểu nhỏ để tiết kiệm RAM; dấu hiệu là số âm hoặc kết quả cuộn vòng sau phép tính lớn.",
  "Vector hoá thành tích Descartes khổng lồ; dấu hiệu là process hết RAM dù hai đầu vào riêng lẻ nhỏ.",
  "Tính thống kê chuẩn hoá trên cả test; không có exception nhưng metric tốt giả tạo vì đã để thông tin test đi vào preprocessing."
],

tu_kiem_tra: [
  "Vì sao `(200, 4) - (4,)` hợp lệ, và vector bốn phần tử được áp theo nghĩa nào?",
  "Phân biệt `shape`, `size`, `dtype`, `itemsize` và `strides` bằng một mảng `(10, 3)` kiểu `float32`.",
  "Tại sao vector hoá vẫn có vòng lặp, và vòng lặp đó khác vòng lặp Python ở đâu?",
  "Viết invariant để phân biệt chuẩn hoá theo hàng với chuẩn hoá theo cột dù cả hai đều chạy.",
  "Trong trường hợp nào một view tốt hơn copy, và khi nào việc chia sẻ bộ nhớ trở thành rủi ro?",
  "Nếu một cột có độ lệch chuẩn 0, thay mẫu số bằng 1 giữ tính đúng nào và che giấu vấn đề nào?",
  "Vì sao broadcasting `(1_000_000, 1)` với `(10_000,)` nguy hiểm dù không nhân bản hai đầu vào?"
],

tai_nguyen: [
  "NumPy User Guide — phần Array fundamentals và Broadcasting.",
  "Python Data Science Handbook — Jake VanderPlas.",
  "Effective Python for Data Scientists — Matt Harrison.",
  "Hands-On Machine Learning with Scikit-Learn, Keras, and TensorFlow — Aurélien Géron."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: `ndarray` đánh đổi tính linh hoạt của `list` để lấy bố trí bộ nhớ đồng nhất và truy cập theo stride.",
  "Nối sang mảng Đại số tuyến tính cho ML: tích `X @ w`, vector cột và broadcasting là hình thức thực thi của các phép toán ma trận.",
  "Nối sang mảng Machine Learning cổ điển: scaler phải `fit` trên train rồi `transform` validation/test để tránh data leakage.",
  "Nối sang mảng Deep Learning: PyTorch tensor kế thừa trực giác shape và broadcasting nhưng bổ sung gradient cùng thiết bị CPU/GPU."
]

};
