window.LESSONS["01:cong-cu-cua-dev:pre-commit-hook-chay-kiem-tra-tu-dong"] = {

tom_tat: [
  "**Hook là script Git tự chạy tại các thời điểm nhất định.** `pre-commit` chạy trước khi commit được tạo — chặn được lỗi trước khi nó vào lịch sử.",
  "**Hook là tầng THỨ HAI trong ba tầng, không phải tầng cuối.** Nó bỏ qua được bằng `--no-verify`, nên **CI vẫn phải chạy lại mọi thứ** — hook nhanh, CI mới là ràng buộc.",
  "**Hook phải NHANH.** Vượt vài giây là người ta bắt đầu dùng `--no-verify` thường xuyên, và bạn mất luôn tầng này.",
  "**Framework `pre-commit` giải quyết ba vấn đề mà hook thủ công không giải được:** chia sẻ cho cả đội, cài môi trường riêng cho từng hook, và chỉ chạy trên file đã thay đổi.",
  "**Hook viết riêng bắt được thứ linter không biết:** quy ước nội bộ, bí mật lọt vào code, `breakpoint()` quên xoá, quy ước commit message của đội."
],

can_biet_truoc: [
  "Bài **Linter và formatter** ngay trước — ba tầng chạy kiểm tra, và cấu hình chung trong `pyproject.toml`.",
  "Bài **Viết shell script cơ bản** ở module 3 — hook bản chất là script; cần biết exit code.",
  "Bài **Tín hiệu, tiến trình nền, exit code** ở module 2 — hook chặn commit bằng cách trả exit code khác 0.",
  "Kiến thức Git cơ bản: `commit`, `add`, vùng staging. Chi tiết ở mảng 03."
],

dinh_nghia: "Nói đời thường: cửa an ninh sân bay không thay thế được điều tra viên, nhưng nó chặn phần lớn vấn đề ngay tại cửa — rẻ hơn nhiều so với phát hiện khi máy bay đã cất cánh.\n\nCác khái niệm:\n\n- **Git hook** — script Git tự động chạy tại một thời điểm trong vòng đời của thao tác Git. Nằm trong `.git/hooks/`.\n- **`pre-commit` (hook)** — chạy **trước** khi commit được tạo. Trả về exit code khác 0 thì **commit bị huỷ**.\n- **`pre-commit` (framework)** — công cụ Python quản lý hook, cấu hình bằng `.pre-commit-config.yaml`. **Trùng tên với hook nhưng là hai thứ khác nhau** — đây là nguồn nhầm lẫn phổ biến.\n- **Vùng staging** — nơi chứa thay đổi đã `git add`, chờ commit. Hook chỉ nên kiểm tra file trong vùng này.\n- **`--no-verify`** — cờ bỏ qua hook: `git commit --no-verify`.\n- **Exit code** — 0 là thành công (cho commit đi tiếp), khác 0 là thất bại (chặn commit).\n\nCác hook thường dùng:\n\n```\npre-commit    trước khi tạo commit        ← chính của bài này\ncommit-msg    sau khi nhập thông điệp     ← kiểm định dạng commit message\npre-push      trước khi push              ← chạy test (chậm hơn, ít thường xuyên hơn)\npost-merge    sau khi merge               ← cài lại dependency nếu file khoá đổi\n```\n\nMột phân biệt then chốt định hình cả bài:\n\n```\nHOOK   chạy trên MÁY LẬP TRÌNH VIÊN  ⇒ bỏ qua được ⇒ KHÔNG tin được\nCI     chạy trên MÁY CHỦ              ⇒ không bỏ qua được ⇒ NGUỒN SỰ THẬT\n\nHook là TIỆN ÍCH giúp phát hiện sớm, KHÔNG phải cơ chế bảo đảm.\n```",

vi_sao_can: "Hook lấp một khoảng trống cụ thể giữa trình soạn thảo và CI:\n\n- **Phản hồi sớm rẻ hơn phản hồi muộn rất nhiều.** Lỗi bắt ở hook: sửa ngay, 5 giây. Lỗi bắt ở CI: push, chờ 3 phút, đọc log, sửa, push lại — 10 phút và một lần chuyển ngữ cảnh.\n- **Giữ lịch sử Git sạch.** Không có hook, lịch sử đầy commit `fix lint`, `fix format`, `fix lint again`. Có hook, những commit đó không bao giờ tồn tại.\n- **Bắt được thứ CI khó bắt.** Một khoá API lọt vào code: CI có thể bắt, nhưng lúc đó nó **đã nằm trong lịch sử Git** và phải coi như đã rò rỉ. Hook chặn trước khi commit tồn tại — đây là khác biệt về bản chất, không phải tốc độ.\n- **Người mới vào đội không cần biết quy ước.** Hook tự chạy formatter đúng cấu hình dự án; họ không phải nhớ gì cả.\n- **Hook riêng bắt được quy ước nội bộ.** Linter không biết công ty bạn cấm import trực tiếp từ module `legacy`, hay bắt buộc mọi migration phải có phần rollback.\n\nTình huống thật rất tốn kém: một người vô tình `git add .` và commit file `.env` chứa khoá API của cổng thanh toán. CI báo lỗi sau đó — nhưng đã muộn. **Khoá đã nằm trong lịch sử Git, đã push lên máy chủ, và phải coi như bị lộ.** Quy trình xử lý gồm: thu hồi khoá, tạo khoá mới, cập nhật mọi nơi dùng, và viết lại lịch sử Git (rất phiền nếu người khác đã pull). Một hook `detect-secrets` chạy trong 200 mili giây ngăn toàn bộ chuyện đó.\n\nMột lưu ý về phạm vi: **hook không thay thế CI.** Ai cũng gõ được `--no-verify`, và người mới clone repo về mà quên `pre-commit install` thì không có hook nào chạy cả. Đó là lý do phần cuối bài nói về việc chạy chính hook đó trong CI.",

co_che: "### Hook thủ công — và ba vấn đề của nó\n\n```bash\n# .git/hooks/pre-commit  (phải chmod +x)\n#!/bin/sh\nruff check . || exit 1\nruff format --check . || exit 1\n```\n\nCách này chạy được, nhưng có ba vấn đề nghiêm trọng:\n\n```\n① .git/hooks/ KHÔNG được commit\n   → mỗi người phải tự cài, người mới không biết\n\n② Không quản lý được PHIÊN BẢN công cụ\n   → máy A có ruff 0.5, máy B có 0.6 ⇒ kết quả khác nhau\n\n③ Chạy trên TOÀN BỘ dự án\n   → sửa một file mà kiểm 2.000 file ⇒ chậm ⇒ người ta dùng --no-verify\n```\n\nVấn đề ① là vấn đề gốc: **thư mục `.git/` không nằm trong Git.** Đây chính là lý do framework `pre-commit` ra đời.\n\n### Framework `pre-commit` — cách nó giải quyết cả ba\n\n```bash\npip install pre-commit\npre-commit install        # ⭐ cài hook vào .git/hooks/\n```\n\n```yaml\n# .pre-commit-config.yaml — file này ĐƯỢC COMMIT\nrepos:\n  - repo: https://github.com/astral-sh/ruff-pre-commit\n    rev: v0.6.9                    # ⭐ GHIM phiên bản\n    hooks:\n      - id: ruff\n        args: [--fix]\n      - id: ruff-format\n```\n\n```\n① File cấu hình ĐƯỢC COMMIT     → cả đội dùng chung\n② rev ghim phiên bản             → mọi máy chạy cùng phiên bản\n③ Mặc định chỉ chạy trên FILE ĐÃ STAGE  → nhanh\n```\n\nCơ chế đáng hiểu: framework **tự tạo môi trường ảo riêng cho mỗi hook** trong `~/.cache/pre-commit/`. Nghĩa là hook dùng đúng phiên bản ghim trong `rev`, **độc lập** với những gì cài trong venv của dự án. Đây là lý do nó đáng tin hơn hook thủ công.\n\nHệ quả thực tế: lần chạy đầu tiên chậm (phải tải và dựng môi trường), các lần sau rất nhanh.\n\n### Bộ hook cơ bản\n\n```yaml\nrepos:\n  # ---- Kiểm tra chung, rất nhanh ----\n  - repo: https://github.com/pre-commit/pre-commit-hooks\n    rev: v4.6.0\n    hooks:\n      - id: trailing-whitespace       # xoá khoảng trắng cuối dòng\n      - id: end-of-file-fixer          # bảo đảm dòng trống cuối file\n      - id: check-yaml                 # YAML có hợp lệ không\n      - id: check-toml\n      - id: check-added-large-files    # ⭐ chặn file lớn lọt vào repo\n        args: [--maxkb=500]\n      - id: check-merge-conflict       # ⭐ chặn dấu <<<<<<< còn sót\n      - id: detect-private-key         # ⭐ chặn khoá riêng tư\n      - id: debug-statements           # ⭐ chặn breakpoint() quên xoá\n\n  # ---- Python ----\n  - repo: https://github.com/astral-sh/ruff-pre-commit\n    rev: v0.6.9\n    hooks:\n      - id: ruff\n        args: [--fix]\n      - id: ruff-format\n```\n\nBốn hook đánh dấu ⭐ đáng chú ý vì chúng bắt những thứ **linter không bắt**:\n\n```\ncheck-added-large-files  file 200MB vào Git là VĨNH VIỄN — xoá không\n                         giảm kích thước repo, mọi người clone đều tải\n\ncheck-merge-conflict     dấu <<<<<<< sót lại — code vẫn commit được,\n                         và vỡ lúc chạy\n\ndetect-private-key       khoá SSH/TLS lọt vào repo\n\ndebug-statements         breakpoint() và pdb.set_trace() quên xoá\n                         ← đúng cạm bẫy ở bài debugger\n```\n\n### Vì sao hook phải nhanh\n\n```\n< 2 giây     không ai để ý\n2–5 giây     bắt đầu khó chịu\n5–15 giây    người ta dùng --no-verify \"khi đang vội\"\n> 15 giây    --no-verify thành THÓI QUEN ⇒ mất luôn tầng này\n```\n\nĐây là ràng buộc thiết kế quan trọng nhất của hook, và nó quyết định **cái gì được đặt vào hook**:\n\n```\nHỢP với pre-commit           KHÔNG hợp với pre-commit\n─────────────────────        ──────────────────────────\nformatter (mili giây)         toàn bộ bộ test (phút)\nlinter trên file đã đổi        kiểm tra kiểu cả dự án\nkiểm tra bí mật                build Docker image\nkiểm tra định dạng file        gọi mạng\n```\n\nBa cách giữ hook nhanh:\n\n```\n① Chỉ chạy trên FILE ĐÃ STAGE (mặc định của framework)\n② Lọc theo loại file — hook Python không chạy khi sửa README\n③ Việc chậm chuyển sang pre-push hoặc CI\n```\n\n```yaml\n# Chuyển việc chậm sang pre-push\n- repo: local\n  hooks:\n    - id: pytest-nhanh\n      name: Test nhanh\n      entry: pytest -q -m \"not slow\"\n      language: system\n      pass_filenames: false\n      stages: [pre-push]        # ⭐ chỉ chạy khi push, không phải mỗi commit\n```\n\n### Viết hook riêng — phần giá trị nhất\n\nĐây là chỗ hook làm được thứ linter không làm được: **kiểm tra quy ước riêng của đội bạn.**\n\n```yaml\n- repo: local\n  hooks:\n    - id: cam-import-legacy\n      name: Cấm import trực tiếp từ legacy\n      entry: python tools/kiem_import_legacy.py\n      language: system\n      types: [python]              # chỉ chạy trên file .py\n      exclude: ^legacy/            # trừ chính thư mục legacy\n```\n\n```python\n#!/usr/bin/env python\n\"\"\"Chặn import trực tiếp từ package legacy.\n\nMọi truy cập tới legacy phải đi qua tầng adapter ở du_an/adapter/,\nđể khi gỡ bỏ legacy chỉ phải sửa một chỗ. Xem quyết định kiến trúc\ntrong docs/adr/003-co-lap-legacy.md\n\"\"\"\nimport ast\nimport sys\nfrom pathlib import Path\n\n\ndef kiem_tra_file(duong_dan: Path) -> list[str]:\n    \"\"\"Trả về danh sách vi phạm trong một file.\"\"\"\n    try:\n        cay = ast.parse(duong_dan.read_text(encoding=\"utf-8\"))\n    except SyntaxError:\n        return []          # ruff đã lo lỗi cú pháp, không báo trùng\n\n    vi_pham = []\n    for nut in ast.walk(cay):\n        if isinstance(nut, ast.ImportFrom) and nut.module:\n            if nut.module.startswith(\"legacy\"):\n                vi_pham.append(\n                    f\"{duong_dan}:{nut.lineno}: import trực tiếp từ \"\n                    f\"'{nut.module}' — dùng du_an.adapter thay thế\"\n                )\n        elif isinstance(nut, ast.Import):\n            for ten in nut.names:\n                if ten.name.startswith(\"legacy\"):\n                    vi_pham.append(\n                        f\"{duong_dan}:{nut.lineno}: import trực tiếp từ \"\n                        f\"'{ten.name}' — dùng du_an.adapter thay thế\"\n                    )\n    return vi_pham\n\n\ndef main() -> int:\n    tat_ca = []\n    for ten_file in sys.argv[1:]:        # ⭐ framework truyền file đã stage\n        tat_ca.extend(kiem_tra_file(Path(ten_file)))\n\n    for dong in tat_ca:\n        print(dong, file=sys.stderr)\n\n    return 1 if tat_ca else 0            # ⭐ khác 0 ⇒ CHẶN commit\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\nBốn điểm thiết kế đáng học trong script này:\n\n```\n· Dùng ast, KHÔNG dùng regex — regex sẽ khớp cả comment và chuỗi\n· Nhận file qua sys.argv — framework chỉ truyền file ĐÃ STAGE\n· Thông báo có ĐƯỜNG DẪN:DÒNG — trình soạn thảo bấm vào nhảy tới được\n· Thông báo nói CÁCH SỬA (\"dùng du_an.adapter\"), không chỉ nói sai\n```\n\nĐiểm cuối là điều bài xử lý lỗi ở module 8 đã nhấn mạnh, áp vào đây: **thông báo tốt nói cái gì sai, giá trị thực tế, và mong đợi gì.**\n\n### Kiểm tra commit message\n\n```yaml\n- repo: local\n  hooks:\n    - id: kiem-commit-message\n      name: Kiểm định dạng commit message\n      entry: python tools/kiem_commit_msg.py\n      language: system\n      stages: [commit-msg]        # ⭐ hook KHÁC, chạy sau khi nhập message\n```\n\n```python\n#!/usr/bin/env python\n\"\"\"Kiểm commit message theo quy ước Conventional Commits.\"\"\"\nimport re\nimport sys\nfrom pathlib import Path\n\nMAU = re.compile(\n    r\"^(feat|fix|docs|style|refactor|test|chore|perf)\"\n    r\"(\\([a-z0-9-]+\\))?: .{1,72}$\"\n)\n\nVI_DU = \"\"\"Ví dụ hợp lệ:\n  feat(don-hang): thêm chức năng huỷ đơn\n  fix: sửa lỗi tính thuế cho đơn có khuyến mãi\n  refactor(thanh-toan): tách class QuanLyThanhToan\"\"\"\n\n\ndef main() -> int:\n    # Git truyền đường dẫn file chứa message làm tham số\n    duong_dan = Path(sys.argv[1])\n    dong_dau = duong_dan.read_text(encoding=\"utf-8\").splitlines()[0]\n\n    if dong_dau.startswith(\"Merge \") or dong_dau.startswith(\"Revert \"):\n        return 0                        # ⭐ Git tự sinh, bỏ qua\n\n    if not MAU.match(dong_dau):\n        print(f\"Commit message không đúng quy ước:\\n  {dong_dau}\\n\",\n              file=sys.stderr)\n        print(VI_DU, file=sys.stderr)\n        return 1\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\n```bash\npre-commit install --hook-type commit-msg      # ⭐ phải cài RIÊNG loại hook này\n```\n\nDòng cuối là chỗ hay quên: `pre-commit install` chỉ cài hook `pre-commit`. Mỗi loại hook khác phải cài thêm bằng `--hook-type`.\n\n### `--no-verify` — khi nào chính đáng\n\n```bash\ngit commit --no-verify -m \"...\"      # bỏ qua mọi hook\n```\n\n```\nCHÍNH ĐÁNG:\n· Commit tạm trên nhánh riêng, sẽ squash trước khi merge\n· Hook đang hỏng vì lý do môi trường (không phải vì code sai)\n· Sự cố production, cần commit hotfix gấp — SỬA LẠI SAU\n· Commit file sinh tự động mà hook hiểu nhầm\n\nKHÔNG chính đáng:\n· \"Đang vội\"\n· \"Lỗi này không quan trọng\"\n· Dùng thường xuyên tới mức thành thói quen\n```\n\nDấu hiệu cảnh báo cần xử lý: **nếu cả đội dùng `--no-verify` thường xuyên, vấn đề nằm ở HOOK, không ở người.** Hook quá chậm, báo sai, hoặc kiểm thứ không đáng. Sửa hook, đừng trách người.\n\nVà đây chính là lý do CI phải chạy lại mọi thứ: **`--no-verify` luôn tồn tại và không cấm được.**\n\n### Chạy chính hook đó trong CI\n\n```yaml\n# .github/workflows/kiem-tra.yml\n- name: Chạy pre-commit\n  run: |\n    pip install pre-commit\n    pre-commit run --all-files --show-diff-on-failure\n```\n\n```\n--all-files            kiểm TOÀN BỘ dự án, không chỉ file đã đổi\n--show-diff-on-failure  hiện đúng thay đổi mà hook muốn áp dụng\n```\n\nCách này có ưu điểm lớn: **một nguồn cấu hình duy nhất.** Không phải viết lại danh sách kiểm tra trong workflow CI, nên hai bên không thể lệch nhau.\n\n⚠️ Nhưng có một hạn chế thật cần biết: hook `--fix` **tự sửa file** rồi trả về lỗi. Trong CI, việc sửa đó bị vứt đi, chỉ còn kết quả thất bại. Đó là hành vi **đúng** cho CI — CI báo lỗi, người sửa; CI không tự sửa rồi commit ngược.\n\n### Khi hook hỏng hoặc chậm\n\n```bash\n# Chạy thử toàn bộ mà không cần commit\npre-commit run --all-files\n\n# Chạy MỘT hook cụ thể\npre-commit run ruff --all-files\n\n# Cập nhật rev lên phiên bản mới nhất\npre-commit autoupdate\n\n# Dọn cache khi hook cư xử lạ\npre-commit clean\npre-commit gc\n\n# Gỡ hook\npre-commit uninstall\n```\n\n`pre-commit autoupdate` đáng chạy định kỳ (hằng quý), và **luôn commit riêng** kết quả của nó — vì nâng phiên bản linter thường kéo theo một loạt thay đổi định dạng, đúng như bài trước đã nói về commit thuần hình thức.\n\n### Bảo đảm người mới cài hook\n\nVấn đề thật: người clone repo về mà quên `pre-commit install` thì **không có hook nào chạy**, và họ không hề biết.\n\nBa cách xử lý, theo mức độ chắc chắn tăng dần:\n\n```\n① Ghi trong README            → dựa vào người đọc\n② Đưa vào script setup dự án  → dựa vào người chạy script\n③ CHẠY pre-commit TRONG CI    → chắc chắn, không phụ thuộc ai\n```\n\n```bash\n# ② Script khởi tạo dự án — tools/setup.sh\n#!/usr/bin/env bash\nset -euo pipefail\n\npython -m venv .venv\n.venv/bin/pip install -e \".[dev]\"\n.venv/bin/pre-commit install\n.venv/bin/pre-commit install --hook-type commit-msg\necho \"✓ Xong. Kích hoạt môi trường: source .venv/bin/activate\"\n```\n\nCách ③ là cách duy nhất **thật sự** bảo đảm, và nó khẳng định lại nguyên tắc của bài trước: **hook là tiện ích, CI là ràng buộc.**",

vi_du: "### Cấu hình đầy đủ cho một dự án Python\n\n```yaml\n# .pre-commit-config.yaml\ndefault_install_hook_types: [pre-commit, commit-msg]   # ⭐ cài cả hai loại\n\nrepos:\n  # ---- Kiểm tra chung, cực nhanh ----\n  - repo: https://github.com/pre-commit/pre-commit-hooks\n    rev: v4.6.0\n    hooks:\n      - id: trailing-whitespace\n      - id: end-of-file-fixer\n      - id: check-yaml\n      - id: check-toml\n      - id: check-json\n      - id: check-added-large-files\n        args: [--maxkb=500]\n      - id: check-merge-conflict\n      - id: check-case-conflict        # macOS/Windows không phân biệt hoa thường\n      - id: detect-private-key\n      - id: debug-statements            # breakpoint() quên xoá\n      - id: mixed-line-ending\n        args: [--fix=lf]\n\n  # ---- Python: linter + formatter ----\n  - repo: https://github.com/astral-sh/ruff-pre-commit\n    rev: v0.6.9                         # ⭐ CÙNG phiên bản với pyproject.toml\n    hooks:\n      - id: ruff\n        args: [--fix, --exit-non-zero-on-fix]\n      - id: ruff-format\n\n  # ---- Bí mật ----\n  - repo: https://github.com/gitleaks/gitleaks\n    rev: v8.18.4\n    hooks:\n      - id: gitleaks\n\n  # ---- Hook riêng của đội ----\n  - repo: local\n    hooks:\n      - id: cam-import-legacy\n        name: Cấm import trực tiếp từ legacy\n        entry: python tools/kiem_import_legacy.py\n        language: system\n        types: [python]\n        exclude: ^legacy/\n\n      - id: kiem-commit-message\n        name: Kiểm định dạng commit message\n        entry: python tools/kiem_commit_msg.py\n        language: system\n        stages: [commit-msg]\n\n      - id: test-nhanh\n        name: Test nhanh\n        entry: pytest -q -m \"not slow\" --no-header\n        language: system\n        pass_filenames: false\n        stages: [pre-push]              # ⭐ CHẬM ⇒ chỉ khi push\n```\n\nBa tuỳ chọn đáng giải thích:\n\n```\n--exit-non-zero-on-fix   ruff sửa file rồi VẪN báo lỗi\n                          → commit bị chặn, bạn xem lại thay đổi rồi add lại\n                          → tránh commit code mình chưa nhìn\n\npass_filenames: false     hook nhận danh sách file làm tham số theo mặc định;\n                          pytest không cần, nên tắt\n\nstages: [pre-push]        chuyển việc chậm ra khỏi mỗi lần commit\n```\n\n```bash\n# Cài đặt\n$ pip install pre-commit\n$ pre-commit install\npre-commit installed at .git/hooks/pre-commit\npre-commit installed at .git/hooks/commit-msg\n\n$ pre-commit install --hook-type pre-push\npre-commit installed at .git/hooks/pre-push\n```\n\n### Hook chạy như thế nào\n\n```bash\n$ git add dich_vu/don_hang.py\n$ git commit -m \"feat(don-hang): thêm chức năng huỷ đơn\"\n\ntrailing-whitespace......................................Passed\nend-of-file-fixer........................................Passed\ncheck-yaml...........................(no files to check)Skipped   ⭐ lọc theo loại file\ncheck-added-large-files..................................Passed\ndetect-private-key.......................................Passed\ndebug-statements.........................................Failed\n- hook id: debug-statements\n- exit code: 1\n\ndich_vu/don_hang.py:45:5: breakpoint() called\n\nruff.....................................................Passed\nruff-format..............................................Passed\ncam-import-legacy........................................Passed\n\n# ⚠️ Commit BỊ CHẶN — breakpoint() quên xoá\n```\n\nĐây là đúng cạm bẫy nêu ở bài debugger, giờ có cơ chế chặn tự động:\n\n```bash\n# Sửa rồi commit lại\n$ sed -i '/breakpoint()/d' dich_vu/don_hang.py\n$ git add dich_vu/don_hang.py\n$ git commit -m \"feat(don-hang): thêm chức năng huỷ đơn\"\n\n... tất cả Passed ...\nkiem-commit-message......................................Passed\n[main a3f2b1c] feat(don-hang): thêm chức năng huỷ đơn\n```\n\nTrường hợp hook **tự sửa** file:\n\n```bash\n$ git commit -m \"fix: sửa lỗi tính thuế\"\n\nruff.....................................................Failed\n- hook id: ruff\n- exit code: 1\n- files were modified by this hook          ⭐ ruff ĐÃ SỬA file\n\nFound 3 errors (3 fixed, 0 remaining).\n\n# Commit bị chặn. Xem ruff đã sửa gì:\n$ git diff\n-import os, sys\n+import sys\n\n# Đồng ý thì add lại rồi commit\n$ git add -u\n$ git commit -m \"fix: sửa lỗi tính thuế\"\n... Passed ...\n```\n\nCơ chế \"sửa xong vẫn chặn\" là cố ý và đúng: **bạn phải nhìn thay đổi trước khi nó vào lịch sử**, thay vì để công cụ âm thầm sửa code rồi commit hộ.\n\n### Hook riêng — chặn bí mật lọt vào code\n\n```yaml\n- repo: local\n  hooks:\n    - id: kiem-bi-mat\n      name: Kiểm bí mật trong code\n      entry: python tools/kiem_bi_mat.py\n      language: system\n      types: [text]\n```\n\n```python\n#!/usr/bin/env python\n\"\"\"Chặn khoá API, mật khẩu, chuỗi kết nối lọt vào Git.\n\nMột bí mật đã commit phải coi như ĐÃ LỘ, kể cả khi xoá ngay sau đó —\nnó vẫn nằm trong lịch sử và trên mọi bản clone.\n\"\"\"\nimport re\nimport sys\nfrom pathlib import Path\n\n# (tên mẫu, biểu thức, gợi ý sửa)\nMAU_BI_MAT = [\n    (\"Khoá AWS\", re.compile(r\"AKIA[0-9A-Z]{16}\"),\n     \"dùng biến môi trường AWS_ACCESS_KEY_ID\"),\n    (\"Chuỗi kết nối có mật khẩu\",\n     re.compile(r\"(postgres|mysql|mongodb)://[^:\\s]+:[^@\\s]+@\"),\n     \"dùng biến môi trường DATABASE_URL\"),\n    (\"Khoá riêng tư\", re.compile(r\"-----BEGIN [A-Z ]*PRIVATE KEY-----\"),\n     \"không bao giờ commit khoá riêng tư\"),\n    (\"Gán mật khẩu trực tiếp\",\n     re.compile(r\"\"\"(?i)(password|mat_khau|secret|api_key)\\s*=\\s*['\"][^'\"]{8,}['\"]\"\"\"),\n     \"đọc từ os.environ\"),\n]\n\n# Dòng có dấu này được bỏ qua — dùng cho ví dụ trong tài liệu\nBO_QUA = \"# khong-phai-bi-mat\"\n\n\ndef quet(duong_dan: Path) -> list[str]:\n    try:\n        noi_dung = duong_dan.read_text(encoding=\"utf-8\")\n    except (UnicodeDecodeError, FileNotFoundError):\n        return []                       # file nhị phân hoặc đã bị xoá\n\n    vi_pham = []\n    for so_dong, dong in enumerate(noi_dung.splitlines(), 1):\n        if BO_QUA in dong:\n            continue\n        for ten, mau, goi_y in MAU_BI_MAT:\n            if mau.search(dong):\n                vi_pham.append(\n                    f\"{duong_dan}:{so_dong}: phát hiện {ten} — {goi_y}\"\n                )\n    return vi_pham\n\n\ndef main() -> int:\n    tat_ca = []\n    for ten in sys.argv[1:]:\n        tat_ca.extend(quet(Path(ten)))\n\n    if tat_ca:\n        print(\"\\n⛔ PHÁT HIỆN BÍ MẬT — commit bị chặn\\n\", file=sys.stderr)\n        for d in tat_ca:\n            print(\"  \" + d, file=sys.stderr)\n        print(\"\\nNếu đây là ví dụ trong tài liệu, thêm \"\n              f\"'{BO_QUA}' vào cuối dòng.\", file=sys.stderr)\n        return 1\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\n```bash\n$ git add cau_hinh.py\n$ git commit -m \"chore: thêm cấu hình\"\n\nkiem-bi-mat..............................................Failed\n\n⛔ PHÁT HIỆN BÍ MẬT — commit bị chặn\n\n  cau_hinh.py:12: phát hiện Chuỗi kết nối có mật khẩu — dùng biến môi trường DATABASE_URL\n  cau_hinh.py:15: phát hiện Gán mật khẩu trực tiếp — đọc từ os.environ\n\nNếu đây là ví dụ trong tài liệu, thêm '# khong-phai-bi-mat' vào cuối dòng.\n```\n\nBa đặc điểm khiến hook này dùng được thật:\n\n```\n· Có LỐI THOÁT (BO_QUA) — không có thì người ta sẽ dùng --no-verify\n· Thông báo nói CÁCH SỬA cụ thể cho từng loại\n· Bắt UnicodeDecodeError — file ảnh, file nhị phân không làm hook nổ\n```\n\nLối thoát là chi tiết thiết kế quan trọng: **hook không có lối thoát hợp lý sẽ bị vô hiệu hoá hoàn toàn.**\n\n```python\n# ✓ Cách đúng, và hook không báo\nimport os\n\nDATABASE_URL = os.environ[\"DATABASE_URL\"]\nMAT_KHAU_API = os.environ.get(\"API_SECRET\")\n\n# ✓ Ví dụ trong tài liệu — có lối thoát\n# DATABASE_URL = \"postgres://user:pass@localhost/db\"  # khong-phai-bi-mat\n```\n\n### Hook kiểm quy ước migration\n\nVí dụ về loại quy ước mà **không linter nào biết**:\n\n```python\n#!/usr/bin/env python\n\"\"\"Mọi migration phải có hàm downgrade() thật.\n\nKhông có nó thì không rollback được khi triển khai lỗi — và lúc\ncần rollback thì đã quá muộn để viết.\n\"\"\"\nimport ast\nimport sys\nfrom pathlib import Path\n\n\ndef co_downgrade_that(duong_dan: Path) -> bool:\n    cay = ast.parse(duong_dan.read_text(encoding=\"utf-8\"))\n    for nut in ast.walk(cay):\n        if isinstance(nut, ast.FunctionDef) and nut.name == \"downgrade\":\n            # Thân hàm chỉ có pass hoặc docstring ⇒ không phải downgrade thật\n            cau_lenh = [\n                c for c in nut.body\n                if not isinstance(c, ast.Pass)\n                and not (isinstance(c, ast.Expr)\n                         and isinstance(c.value, ast.Constant))\n            ]\n            return len(cau_lenh) > 0\n    return False\n\n\ndef main() -> int:\n    thieu = [\n        ten for ten in sys.argv[1:]\n        if not co_downgrade_that(Path(ten))\n    ]\n    for ten in thieu:\n        print(f\"{ten}: thiếu downgrade() thật — không rollback được\",\n              file=sys.stderr)\n    return 1 if thieu else 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n```\n\n```yaml\n- id: kiem-migration\n  name: Migration phải có downgrade\n  entry: python tools/kiem_migration.py\n  language: system\n  files: ^migrations/versions/.*\\.py$      # ⭐ chỉ chạy trên migration\n```\n\nChú ý hook này không chỉ kiểm hàm **có tồn tại** không, mà kiểm nó **có nội dung thật** không — bắt được cả trường hợp viết `def downgrade(): pass` cho qua.\n\n### Chạy hook trong CI — bảo đảm thật\n\n```yaml\n# .github/workflows/kiem-tra.yml\nname: Kiểm tra\non: [push, pull_request]\n\njobs:\n  pre-commit:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - uses: actions/cache@v4          # ⭐ cache môi trường hook\n        with:\n          path: ~/.cache/pre-commit\n          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}\n\n      - run: pip install pre-commit\n      - run: pre-commit run --all-files --show-diff-on-failure\n\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n      - run: pip install -e \".[dev]\"\n      - run: pytest -q                  # ⭐ test đầy đủ ở CI, không ở hook\n```\n\nBa quyết định trong workflow này:\n\n```\n· Chạy CHÍNH pre-commit → một nguồn cấu hình, hai bên không lệch\n· Cache theo hash của .pre-commit-config.yaml → nhanh, tự làm mới khi đổi cấu hình\n· Test đầy đủ ở JOB RIÊNG → hook giữ nhanh, CI lo phần chậm\n```\n\nKết quả khi ai đó dùng `--no-verify`:\n\n```\n$ git commit --no-verify -m \"fix: sửa gấp\"\n[main 7f3a2c1] fix: sửa gấp\n\n$ git push\n\n→ CI chạy pre-commit --all-files\n→ ❌ debug-statements: breakpoint() ở dich_vu/don.py:45\n→ Pull request bị chặn merge\n```\n\nĐây là minh hoạ rõ nhất cho nguyên tắc của bài: **hook bỏ qua được, CI thì không.**\n\n### Gỡ rối hook\n\n```bash\n# Hook không chạy?\n$ ls -la .git/hooks/pre-commit\n# Không có file → chưa chạy pre-commit install\n\n$ pre-commit install\n\n# Hook chạy nhưng cư xử lạ — dọn cache\n$ pre-commit clean\n$ pre-commit run --all-files\n\n# Muốn biết hook nào chậm\n$ time pre-commit run --all-files\n\n# Chạy thử một hook cụ thể trên một file\n$ pre-commit run cam-import-legacy --files dich_vu/don.py\n\n# Nâng phiên bản định kỳ — commit RIÊNG\n$ pre-commit autoupdate\n$ git commit -am \"chore: nâng phiên bản pre-commit hooks\"\n```\n\nLệnh cuối đáng thành thói quen hằng quý. Và **commit riêng** vì nâng phiên bản linter thường kéo theo thay đổi định dạng hàng loạt — đúng nguyên tắc tách commit thuần hình thức ở bài trước.\n\n### Script khởi tạo cho người mới\n\n```bash\n#!/usr/bin/env bash\n# tools/setup.sh — chạy một lần sau khi clone\nset -euo pipefail\n\necho \"→ Tạo môi trường ảo...\"\npython -m venv .venv\n\necho \"→ Cài dependency...\"\n.venv/bin/pip install --upgrade pip\n.venv/bin/pip install -e \".[dev]\"\n\necho \"→ Cài Git hook...\"\n.venv/bin/pre-commit install\n.venv/bin/pre-commit install --hook-type commit-msg\n.venv/bin/pre-commit install --hook-type pre-push\n\necho \"→ Chạy thử toàn bộ hook...\"\n.venv/bin/pre-commit run --all-files || true    # ⭐ không chặn setup nếu đỏ\n\necho \"\"\necho \"✓ Xong. Kích hoạt môi trường:\"\necho \"    source .venv/bin/activate\"\n```\n\n`|| true` ở bước cuối là chủ ý: người mới clone một dự án cũ có thể gặp hook đỏ ở code chưa dọn — không nên vì thế mà script setup thất bại. Họ vẫn thấy kết quả để biết tình trạng dự án.",

so_sanh: "### Ba tầng kiểm tra\n\n| | Trình soạn thảo | Pre-commit hook | CI |\n|---|---|---|---|\n| Thời điểm | khi lưu | khi commit | khi push/PR |\n| Tốc độ | tức thì | **phải < 2 giây** | vài phút |\n| Bỏ qua được | ✓ | **✓ `--no-verify`** | **✗** |\n| Chặn lỗi vào lịch sử Git | ✗ | **✓** | ✗ (đã vào rồi) |\n| Vai trò | sửa khi gõ | **phát hiện sớm** | **ràng buộc thật** |\n\nHook là **tiện ích**, CI là **bảo đảm**. Hook chặn bí mật *trước khi* nó vào lịch sử — đó là khác biệt về bản chất, không phải tốc độ.\n\n### Hook thủ công và framework `pre-commit`\n\n| | `.git/hooks/` thủ công | Framework |\n|---|---|---|\n| Commit được vào Git | **✗** | **✓** |\n| Chia sẻ cho cả đội | ✗ | ✓ |\n| Ghim phiên bản công cụ | ✗ | **✓ `rev`** |\n| Môi trường riêng mỗi hook | ✗ | ✓ |\n| Chỉ chạy trên file đã stage | thủ công | **✓ mặc định** |\n| Lọc theo loại file | thủ công | ✓ `types`, `files` |\n\nVấn đề gốc của cách thủ công: **`.git/` không nằm trong Git.**\n\n### Việc nào đặt ở hook nào\n\n| Việc | Thời gian | Đặt ở |\n|---|---|---|\n| Formatter | mili giây | **pre-commit** |\n| Linter trên file đã đổi | < 1 giây | **pre-commit** |\n| Kiểm bí mật | < 1 giây | **pre-commit** |\n| Kiểm commit message | tức thì | **commit-msg** |\n| Test nhanh | vài giây | **pre-push** |\n| Kiểm kiểu cả dự án | 10s+ | **CI** |\n| Toàn bộ bộ test | phút | **CI** |\n| Build Docker | phút | **CI** |\n\nNgưỡng quyết định: **vượt 2 giây thì cân nhắc chuyển ra pre-push hoặc CI.**\n\n### Ngưỡng tốc độ và hành vi người dùng\n\n| Thời gian hook | Hành vi thực tế |\n|---|---|\n| < 2 giây | không ai để ý |\n| 2–5 giây | bắt đầu khó chịu |\n| 5–15 giây | `--no-verify` \"khi đang vội\" |\n| > 15 giây | **`--no-verify` thành thói quen** ⇒ mất tầng này |\n\nCả đội hay dùng `--no-verify` ⇒ vấn đề ở **hook**, không ở người.\n\n### Hook có sẵn đáng bật\n\n| Hook | Bắt gì | Linter bắt được? |\n|---|---|---|\n| `debug-statements` | `breakpoint()` quên xoá | một phần |\n| `check-added-large-files` | file lớn vào repo **vĩnh viễn** | **✗** |\n| `check-merge-conflict` | dấu `<<<<<<<` sót lại | ✗ |\n| `detect-private-key` | khoá SSH/TLS | ✗ |\n| `check-case-conflict` | tên file khác hoa thường | ✗ |\n| `end-of-file-fixer` | thiếu dòng cuối | ✗ |\n\nGiá trị của nhóm này: chúng bắt thứ **nằm ngoài phạm vi của linter**.\n\n### `--no-verify` — chính đáng hay không\n\n| Tình huống | Chính đáng |\n|---|---|\n| Commit tạm, sẽ squash | **✓** |\n| Hook hỏng vì môi trường | ✓ |\n| Hotfix sự cố production | ✓ (sửa lại sau) |\n| \"Đang vội\" | **✗** |\n| \"Lỗi này không quan trọng\" | ✗ |\n| Dùng thường xuyên | **✗ — sửa hook** |\n\n### Bảo đảm hook được cài\n\n| Cách | Chắc chắn | Phụ thuộc |\n|---|---|---|\n| Ghi trong README | thấp | người đọc |\n| Script setup dự án | vừa | người chạy script |\n| **Chạy pre-commit trong CI** | **cao** | **không ai** |\n\nChỉ cách thứ ba thật sự bảo đảm — và nó khẳng định lại: hook là tiện ích, CI là ràng buộc.",

loi_thuong_gap: [
  "**Viết hook thẳng vào `.git/hooks/`.** Thư mục đó không nằm trong Git, nên không ai khác có hook.",
  "**Để hook chạy quá lâu.** Vượt 15 giây thì `--no-verify` thành thói quen và bạn mất luôn tầng này.",
  "**Đặt toàn bộ bộ test vào pre-commit.** Chuyển sang pre-push hoặc CI.",
  "**Không ghim `rev`.** Mỗi máy chạy phiên bản khác nhau, kết quả khác nhau.",
  "**Ghim `rev` khác phiên bản trong `pyproject.toml`.** Hook và CI cho kết quả khác nhau.",
  "**Quên `pre-commit install --hook-type commit-msg`.** Hook `commit-msg` không được cài, và không có báo lỗi nào.",
  "**Tin rằng hook là đủ.** `--no-verify` luôn tồn tại; CI phải chạy lại mọi thứ.",
  "**Không chạy pre-commit trong CI.** Người quên `pre-commit install` thì không có gì kiểm tra họ cả.",
  "**Viết hook riêng bằng regex trên mã nguồn.** Khớp cả comment và chuỗi ký tự; dùng `ast` cho code Python.",
  "**Hook riêng không có lối thoát hợp lý.** Người ta sẽ vô hiệu hoá hoàn toàn thay vì dùng ngoại lệ.",
  "**Thông báo lỗi của hook chỉ nói sai, không nói cách sửa.** Cùng nguyên tắc với bài xử lý lỗi ở module 8.",
  "**Hook riêng không xử lý file nhị phân.** `UnicodeDecodeError` làm hook nổ trên file ảnh.",
  "**Để CI tự sửa rồi commit ngược.** CI báo lỗi, người sửa — CI không sửa hộ.",
  "**Chạy `pre-commit autoupdate` chung commit với thay đổi logic.** Nâng phiên bản linter kéo theo thay đổi định dạng hàng loạt.",
  "**Không cache `~/.cache/pre-commit` trong CI.** Mỗi lần chạy dựng lại toàn bộ môi trường hook.",
  "**Trách người dùng `--no-verify` thay vì sửa hook.** Cả đội dùng nghĩa là hook có vấn đề."
],

tu_kiem_tra: [
  "Git hook là gì? Nó chặn commit bằng cơ chế nào?",
  "Phân biệt hook `pre-commit` và framework `pre-commit`. Vì sao dễ nhầm?",
  "Nêu ba vấn đề của hook viết thẳng vào `.git/hooks/`. Vấn đề gốc là gì?",
  "Framework `pre-commit` giải quyết ba vấn đề đó thế nào?",
  "Vì sao framework tạo môi trường ảo riêng cho mỗi hook?",
  "Nêu ngưỡng tốc độ và hành vi tương ứng của người dùng. Ngưỡng nào là giới hạn thực tế?",
  "Việc nào hợp với pre-commit, việc nào không? Nêu ba cách giữ hook nhanh.",
  "Nêu bốn hook có sẵn bắt được thứ linter không bắt, và mỗi cái bắt gì.",
  "Vì sao `check-added-large-files` quan trọng? File lớn vào Git có hậu quả gì?",
  "Vì sao hook chặn bí mật khác về bản chất với CI chặn bí mật?",
  "Viết hook riêng cho code Python nên dùng `ast` hay regex? Vì sao?",
  "Nêu bốn điểm thiết kế của một hook riêng tốt.",
  "Vì sao hook riêng cần có lối thoát? Hậu quả nếu không có?",
  "`--exit-non-zero-on-fix` làm gì? Vì sao \"sửa xong vẫn chặn\" là đúng?",
  "Khi nào `--no-verify` là chính đáng, khi nào không? Cả đội hay dùng thì vấn đề ở đâu?",
  "Vì sao phải chạy pre-commit trong CI dù đã có hook trên máy?",
  "`--all-files` và `--show-diff-on-failure` dùng để làm gì trong CI?",
  "Nêu ba cách bảo đảm người mới cài hook, xếp theo độ chắc chắn.",
  "Vì sao `pre-commit autoupdate` nên commit riêng?"
],

tai_nguyen: [
  "**Tài liệu chính thức của pre-commit** (pre-commit.com) — cấu hình đầy đủ, danh sách hook có sẵn, và cách viết hook riêng bằng nhiều ngôn ngữ.",
  "**Kho `pre-commit/pre-commit-hooks`** — bộ hook kiểm tra chung; đọc mã nguồn của chúng là cách học viết hook riêng tốt nhất.",
  "**Tài liệu Git mục *githooks*** (`git help githooks`) — danh sách đầy đủ các loại hook, thứ tự chạy, và tham số Git truyền cho mỗi loại.",
  "**Tài liệu module `ast` của Python** — cần khi viết hook phân tích code Python đúng cách thay vì dùng regex.",
  "**Conventional Commits** — quy ước commit message được dùng rộng rãi; nguồn của mẫu regex trong bài."
],

lien_he: [
  "**Linter và formatter** ngay trước: hook là tầng thứ hai trong ba tầng; cấu hình chung và ghim phiên bản là điều kiện để ba tầng nhất quán.",
  "**Debugger** trong module này: hook `debug-statements` chặn `breakpoint()` quên xoá — đúng cạm bẫy nêu ở bài đó.",
  "**VS Code** trong module này: format khi lưu bắt lỗi sớm hơn hook, nhưng hook mới chặn được vào lịch sử.",
  "**Xử lý lỗi tường minh** ở module 8: thông báo của hook phải nói cái gì sai và cách sửa, không chỉ nói sai.",
  "**Đọc và review code người khác** ở module 8: hook lo phần máy làm được, người review dành sức cho thiết kế.",
  "**Viết shell script cơ bản** ở module 3: hook bản chất là script; exit code khác 0 chặn commit.",
  "**Tín hiệu, tiến trình, exit code** ở module 2: cơ chế mà Git dùng để biết hook thành công hay thất bại.",
  "**Git & GitHub** ở mảng 03: `--no-verify`, lịch sử Git, và vì sao bí mật đã commit phải coi như đã lộ.",
  "**Auth & bảo mật** ở mảng 05: quản lý bí mật đúng cách bằng biến môi trường và kho bí mật.",
  "**Triển khai** ở mảng 05: CI/CD đầy đủ, nơi pre-commit là một bước trong pipeline lớn hơn.",
  "**Docker cơ bản** trong module này: build image là việc chậm, thuộc về CI chứ không phải hook."
]

};
