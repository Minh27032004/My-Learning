window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:module-package-import-va-name"] = {

tom_tat: [
  "**`import` không phải \"chèn file vào\" — nó làm ba việc**: tìm file, **chạy toàn bộ file đó**, rồi buộc một tên vào module thu được. Hiểu bước giữa là hiểu hầu hết các lỗi import.",
  "**Mỗi module chỉ chạy đúng một lần trong một tiến trình.** Lần `import` thứ hai lấy từ cache `sys.modules`. Đây là lý do sửa file mà chương trình đang chạy không thấy thay đổi.",
  "**`if __name__ == \"__main__\"` phân biệt \"chạy trực tiếp\" với \"bị import\".** Thiếu nó, mọi code ở tầng module sẽ chạy khi người khác import bạn.",
  "**`python file.py` và `python -m goi.file` đặt `sys.path` khác nhau** — đây là nguyên nhân số một của `ModuleNotFoundError` khi code chạy được ở chỗ này mà không chạy ở chỗ kia.",
  "**Import vòng tròn hầu như luôn là dấu hiệu thiết kế sai**, không phải hạn chế của Python. Cách sửa đúng là tách phần dùng chung ra, không phải chuyển `import` xuống trong hàm."
],

can_biet_truoc: [
  "Bài **Hàm, tham số mặc định** ngay trước — `def` là câu lệnh được thực thi, và `import` cũng vậy: đó là mắt nối chính.",
  "Bài **Biến môi trường và PATH** ở module 3 — `sys.path` là PATH của Python, cùng ý tưởng \"danh sách nơi đi tìm\".",
  "Bài **Hệ thống file, đường dẫn** ở module 2 — đường dẫn tương đối và tuyệt đối.",
  "Bài **Điều hướng cd, ls, pwd** ở module 3 — thư mục làm việc hiện tại ảnh hưởng trực tiếp tới việc Python tìm thấy module hay không."
],

dinh_nghia: "Nói đời thường: mỗi file Python là **một ngăn kéo có tên**. `import` là hành động **mở ngăn kéo đó ra, sắp xếp bên trong (chạy code), rồi dán một cái nhãn để bạn với tới**. Ngăn kéo đã mở rồi thì lần sau không mở lại — bạn dùng luôn cái đã có.\n\nNói chính xác:\n\n- **Module** — một file `.py`. Tên module là tên file bỏ phần `.py`.\n- **Package** — một thư mục chứa module, thường có file `__init__.py`. Package cũng là một module, chỉ khác là nó chứa module khác.\n- **`import`** — câu lệnh làm ba việc: **tìm** module, **thực thi** nó (nếu chưa từng), **buộc** một tên trong không gian tên hiện tại.\n- **`sys.path`** — danh sách thư mục Python đi tìm module, theo thứ tự.\n- **`sys.modules`** — cache các module đã nạp. Chìa khoá của việc \"chỉ chạy một lần\".\n- **`__name__`** — biến tự động có trong mọi module: bằng `\"__main__\"` nếu file được chạy trực tiếp, bằng tên module nếu bị import.\n- **Namespace package** — package không có `__init__.py`. Python 3 cho phép, nhưng có đánh đổi.\n\nĐiểm cần nắm ngay: **`import x` chạy toàn bộ file `x.py` từ trên xuống dưới.** Mọi câu lệnh ở tầng ngoài cùng — kể cả `print`, kể cả gọi hàm, kể cả kết nối database — đều được thực thi. `def` và `class` chỉ *tạo object* chứ chưa chạy thân hàm, nên chúng vô hại; còn lời gọi hàm thì không.",

vi_sao_can: "Đây là bài quyết định code của bạn có tổ chức được hay không, và là nguồn của những lỗi khó chịu nhất với người mới:\n\n- **`ModuleNotFoundError` là lỗi số một của người học Python.** Cùng một file, chạy ở thư mục này thì được, thư mục khác thì không. Không hiểu `sys.path` thì chỉ có thể thử mò.\n- **Code có tổ chức bắt đầu từ đây.** Một file 2.000 dòng không phải vì dự án lớn mà vì tác giả chưa biết tách module. Tách đúng làm code dễ đọc, dễ kiểm thử, dễ dùng lại.\n- **Thiếu `if __name__ == \"__main__\"` gây tác dụng phụ thật.** Người khác import module của bạn để dùng một hàm, và toàn bộ phần script chạy theo — mất dữ liệu, gửi mail thật, hoặc đơn giản là chậm không hiểu vì sao.\n- **Import vòng tròn xuất hiện ngay khi dự án vượt vài file.** Biết nó là dấu hiệu thiết kế sai thì sửa đúng gốc; không biết thì đi vá bằng những cách làm code tệ hơn.\n- **Nền cho môi trường ảo và đóng gói.** Bài `venv`, `pip`, `pyproject.toml` ở cuối module dựa hoàn toàn vào việc hiểu Python tìm module ở đâu.\n\nTình huống thật rất kinh điển: bạn có `du_an/main.py` và `du_an/tien_ich/xu_ly.py`. Chạy `python main.py` từ trong `du_an` thì được. Đồng nghiệp chạy `python du_an/main.py` từ thư mục cha thì `ModuleNotFoundError`. Cả hai đều \"chạy file đó\", nhưng `sys.path` khác nhau. Phần cơ chế sẽ chỉ rõ vì sao, và cách viết code không phụ thuộc chỗ đứng khi chạy.",

co_che: "### `import` làm chính xác ba việc\n\n```python\nimport toan_hoc\n```\n\n```\n① TÌM   duyệt sys.path theo thứ tự, tìm toan_hoc.py hoặc toan_hoc/\n② CHẠY  thực thi TOÀN BỘ file, từ trên xuống dưới, tạo ra một module object\n        (bỏ qua bước này nếu module đã có trong sys.modules)\n③ BUỘC  gán tên \"toan_hoc\" trong không gian tên hiện tại → module object đó\n```\n\nBước ② là bước bị hiểu sai nhiều nhất. Thử ngay:\n\n```python\n# tien_ich.py\nprint(\"tôi đang chạy!\")          # ← chạy khi bị import\n\ndef cong(a, b):\n    print(\"trong hàm\")           # ← KHÔNG chạy, chỉ tạo object hàm\n    return a + b\n\nket_qua = cong(1, 2)             # ← CHẠY khi bị import\n```\n\n```python\n# main.py\nimport tien_ich                   # in ra: \"tôi đang chạy!\" và \"trong hàm\"\n```\n\nQuy tắc phân biệt:\n\n```\ndef / class  →  chỉ TẠO object, thân bên trong chưa chạy  ⇒ an toàn\nMọi thứ khác →  CHẠY ngay khi import                      ⇒ cần cân nhắc\n```\n\nHệ quả thiết kế quan trọng: **module không nên có tác dụng phụ khi được import.** Kết nối database, đọc file lớn, gọi API — đặt chúng ở tầng module là ép mọi người import bạn phải trả giá đó.\n\n### `sys.path` — Python tìm module ở đâu\n\n```python\nimport sys\nfor p in sys.path:\n    print(p)\n\n# ''  hoặc thư mục chứa script đang chạy   ← ⭐ phần tử ĐẦU, quan trọng nhất\n# /usr/lib/python3.12\n# /usr/lib/python3.12/lib-dynload\n# .../site-packages                         ← nơi pip cài thư viện\n```\n\nThứ tự tìm là **từ trên xuống, lấy cái đầu tiên khớp**. Hệ quả trực tiếp và rất hay gây khổ:\n\n```bash\n# Đặt tên file trùng thư viện chuẩn ⇒ file của bạn THẮNG\ntouch random.py\npython -c \"import random; print(random.choice([1,2]))\"\n# AttributeError: module 'random' has no attribute 'choice'\n```\n\nPython tìm thấy `random.py` của bạn trước `random` của thư viện chuẩn. Những tên hay bị đặt nhầm: `random.py`, `json.py`, `email.py`, `test.py`, `string.py`, `types.py`.\n\nDấu hiệu nhận biết lỗi này: thông báo dạng *\"module X has no attribute Y\"* với một thư viện chuẩn mà bạn biết chắc là có. Kiểm tra bằng:\n\n```python\nimport random\nprint(random.__file__)     # nếu trỏ vào thư mục dự án của bạn ⇒ đã trùng tên\n```\n\n### Phần tử đầu của `sys.path` — nguồn của mọi `ModuleNotFoundError`\n\nĐây là phần quan trọng nhất của bài:\n\n```\npython main.py            → sys.path[0] = thư mục CHỨA main.py\npython -m goi.main        → sys.path[0] = thư mục HIỆN TẠI (nơi bạn đứng)\npython (REPL)             → sys.path[0] = thư mục hiện tại\n```\n\nÁp vào tình huống đã nêu ở đầu bài:\n\n```\ndu_an/\n├── main.py\n└── tien_ich/\n    ├── __init__.py\n    └── xu_ly.py\n```\n\n```bash\ncd du_an && python main.py\n# sys.path[0] = .../du_an  ⇒ import tien_ich.xu_ly  ✓\n\ncd .. && python du_an/main.py\n# sys.path[0] = .../du_an  ⇒ VẪN ✓ (thư mục chứa main.py)\n\ncd .. && python -m du_an.main\n# sys.path[0] = thư mục cha ⇒ phải viết import du_an.tien_ich.xu_ly\n```\n\nHai cách chạy, hai gốc khác nhau, nên cùng một dòng `import` không thể đúng cho cả hai. Đây là lý do các dự án nghiêm túc **chọn một cách chạy và ghi rõ trong tài liệu**, thường là `python -m`.\n\n### Các dạng `import`\n\n```python\nimport toan_hoc                       # ① tên module\ntoan_hoc.cong(1, 2)\n\nimport toan_hoc as th                 # ② đặt bí danh\nth.cong(1, 2)\n\nfrom toan_hoc import cong             # ③ lấy một tên cụ thể\ncong(1, 2)\n\nfrom toan_hoc import cong, tru        # ④ nhiều tên\n\nfrom toan_hoc import *                # ⑤ ❌ TRÁNH\n```\n\nVì sao `import *` là phản mẫu, ba lý do cụ thể:\n\n```\n① Không biết tên nào đã vào không gian tên ⇒ đọc code không biết cong() ở đâu ra\n② Ghi đè âm thầm: hai module cùng có tên load() thì cái sau thắng\n③ Công cụ phân tích tĩnh và trình soạn thảo không lần được nguồn gốc\n```\n\nMột điểm tinh tế đáng biết: **`from x import y` vẫn chạy toàn bộ `x`**. Nó không hề \"chỉ nạp phần cần\" — nó nạp cả module rồi lấy ra một tên. Vì vậy nó không nhanh hơn `import x`.\n\nKhác biệt thật giữa hai dạng nằm ở chỗ khác:\n\n```python\n# import x  → truy cập qua module, luôn thấy giá trị MỚI NHẤT\nimport cau_hinh\nprint(cau_hinh.DEBUG)      # đọc lại thuộc tính mỗi lần\n\n# from x import y → buộc tên MỘT LẦN, không thấy thay đổi về sau\nfrom cau_hinh import DEBUG\nprint(DEBUG)               # giá trị lúc import, dù cau_hinh.DEBUG đã đổi\n```\n\nĐây là lý do với **giá trị cấu hình có thể thay đổi lúc chạy**, nên dùng `import cau_hinh` rồi truy cập qua module.\n\n### Package và `__init__.py`\n\n```\ndu_an/\n├── main.py\n└── xu_ly/\n    ├── __init__.py          ← biến thư mục thành package\n    ├── van_ban.py\n    └── hinh_anh.py\n```\n\n```python\nimport xu_ly.van_ban\nxu_ly.van_ban.lam_sach(\"...\")\n\nfrom xu_ly import van_ban\nfrom xu_ly.van_ban import lam_sach\n```\n\n`__init__.py` chạy **khi package được import lần đầu**, kể cả khi bạn chỉ import module con. Nó có hai công dụng chính:\n\n```python\n# xu_ly/__init__.py\n\n# ① Dựng API công khai gọn cho người dùng\nfrom .van_ban import lam_sach, tach_tu\nfrom .hinh_anh import doi_kich_thuoc\n\n# ⇒ người dùng viết:  from xu_ly import lam_sach\n#    thay vì:         from xu_ly.van_ban import lam_sach\n\n# ② Khai báo tên nào là công khai\n__all__ = [\"lam_sach\", \"tach_tu\", \"doi_kich_thuoc\"]\n```\n\nĐánh đổi của công dụng ①: nó làm **mọi module con bị nạp** ngay khi ai đó import package, kể cả họ chỉ cần một hàm. Với package nặng (ví dụ có import thư viện tính toán lớn), điều này làm thời gian khởi động chậm rõ rệt. Package lớn thường để `__init__.py` gần như rỗng vì lý do đó.\n\nPython 3 cho phép package **không có** `__init__.py` (*namespace package*). Nó hoạt động, nhưng nên vẫn tạo file đó: có `__init__.py` thì ý định rõ ràng, tránh được vài trường hợp nhập nhằng khi hai thư mục cùng tên nằm trên `sys.path`, và nhiều công cụ đóng gói vẫn dựa vào nó.\n\n### Import tuyệt đối và tương đối\n\n```python\n# TUYỆT ĐỐI — tính từ gốc sys.path\nfrom xu_ly.van_ban import lam_sach\n\n# TƯƠNG ĐỐI — tính từ vị trí file hiện tại\nfrom .van_ban import lam_sach        # cùng package\nfrom ..tien_ich import ghi_log       # package cha\n```\n\n| | Tuyệt đối | Tương đối |\n|---|---|---|\n| Đọc là biết ở đâu | **✓** | phải biết mình đang ở đâu |\n| Đổi tên package | phải sửa | **không phải sửa** |\n| Chạy file trực tiếp | ✓ | **✗ lỗi** |\n| PEP 8 khuyên | **✓ ưu tiên** | chấp nhận trong package |\n\nHàng \"chạy file trực tiếp\" là cạm bẫy hay gặp:\n\n```bash\npython xu_ly/van_ban.py\n# ImportError: attempted relative import with no known parent package\n```\n\nNguyên nhân: chạy trực tiếp thì `__name__` là `\"__main__\"` và Python **không biết file này thuộc package nào**, nên dấu chấm không tính được ra cái gì. Cách chạy đúng:\n\n```bash\npython -m xu_ly.van_ban        # ✓ Python biết ngữ cảnh package\n```\n\n### `__name__ == \"__main__\"`\n\n```python\n# tien_ich.py\ndef cong(a, b):\n    return a + b\n\nprint(f\"__name__ = {__name__}\")\n\nif __name__ == \"__main__\":\n    # Chỉ chạy khi GỌI TRỰC TIẾP file này\n    print(\"đang chạy như một script\")\n    print(cong(2, 3))\n```\n\n```bash\npython tien_ich.py\n# __name__ = __main__\n# đang chạy như một script\n# 5\n\npython -c \"import tien_ich\"\n# __name__ = tien_ich          ← khối if KHÔNG chạy\n```\n\nBa công dụng thật của mẫu này:\n\n```\n① Một file vừa dùng làm thư viện, vừa chạy được như công cụ dòng lệnh\n② Đặt code thử nhanh mà không làm phiền người import\n③ Bắt buộc với multiprocessing trên Windows — thiếu nó là tiến trình con\n   import lại module và sinh tiến trình vô hạn\n```\n\nĐiểm ③ ít người biết nhưng gây lỗi rất khó hiểu: trên Windows, tiến trình con được tạo bằng cách **import lại module chính**. Không có `if __name__`, phần tạo tiến trình chạy lại trong mỗi tiến trình con, và chương trình sinh tiến trình cho tới khi treo máy.\n\nMẫu chuẩn cho một script:\n\n```python\ndef main():\n    ...            # toàn bộ logic ở trong hàm, không ở tầng module\n\nif __name__ == \"__main__\":\n    main()\n```\n\nĐặt logic trong `main()` chứ không viết thẳng dưới `if` có hai lợi ích: biến không bị rò ra phạm vi module, và người khác gọi được `main()` từ nơi khác khi cần.\n\n### Module chỉ chạy một lần — `sys.modules`\n\n```python\n# a.py\nprint(\"a đang chạy\")\nX = []\n\n# main.py\nimport a\nimport a          # KHÔNG in lần thứ hai\nimport a\n# Kết quả: chỉ một dòng \"a đang chạy\"\n```\n\n```python\nimport sys\nprint(\"a\" in sys.modules)     # True — đã nằm trong cache\n```\n\nHai hệ quả quan trọng:\n\n**① Trạng thái ở tầng module được chia sẻ toàn chương trình.**\n\n```python\n# cau_hinh.py\nCACHE = {}\n\n# file1.py — from cau_hinh import CACHE; CACHE[\"a\"] = 1\n# file2.py — from cau_hinh import CACHE; print(CACHE)   → {'a': 1}\n```\n\nĐây là cách cài đặt singleton đơn giản nhất trong Python, và nó hoạt động tốt. Nhưng cũng là lý do trạng thái toàn cục ở tầng module rất khó kiểm thử: các bài kiểm thử ảnh hưởng lẫn nhau qua cùng một object.\n\n**② Sửa file khi chương trình đang chạy thì không có tác dụng.** REPL và Jupyter notebook đều vậy. Nạp lại được nhưng có hạn chế:\n\n```python\nimport importlib, a\nimportlib.reload(a)      # nạp lại, nhưng các tên đã \"from a import x\" vẫn cũ\n```\n\nTrong Jupyter, cách thực dụng hơn là khởi động lại kernel. `reload` để lại trạng thái nửa vời — các object đã tạo vẫn thuộc class cũ — và thường gây bối rối hơn là giúp ích.\n\n### Import vòng tròn\n\n```python\n# a.py\nimport b\ndef f(): return b.g()\n\n# b.py\nimport a\ndef g(): return a.f()\n```\n\n```bash\npython a.py\n# ImportError: cannot import name ... (partially initialized module)\n```\n\nVì sao xảy ra, theo đúng ba bước ở đầu bài:\n\n```\n① chạy a.py → gặp import b\n② bắt đầu chạy b.py → gặp import a\n③ a ĐÃ có trong sys.modules (đang chạy dở) ⇒ dùng luôn bản CHƯA HOÀN CHỈNH\n④ b cần a.f, mà a mới chạy tới dòng 1 ⇒ chưa có f ⇒ lỗi\n```\n\nBa cách sửa, theo thứ tự **từ tốt nhất tới tệ nhất**:\n\n```python\n# ✓ ① Tách phần dùng chung ra module thứ ba — sửa đúng gốc\n# chung.py chứa thứ cả a và b cần; a và b đều import chung, không import nhau\n\n# ~ ② Import bên trong hàm — hoãn tới lúc gọi\ndef f():\n    import b            # chạy khi gọi f, lúc đó b đã nạp xong\n    return b.g()\n\n# ~ ③ import module thay vì from ... import\nimport b                # thay cho: from b import g\n```\n\nCách ② và ③ **làm cho chạy được** nhưng không sửa vấn đề thật: hai module vẫn phụ thuộc lẫn nhau, tức là chúng đáng lẽ phải là một, hoặc phải có một tầng chung. Import vòng tròn gần như luôn là **tín hiệu thiết kế**, không phải hạn chế kỹ thuật cần lách.\n\nMột ngoại lệ chính đáng của cách ③: import chỉ để **chú thích kiểu**. Khi đó dùng khối `if TYPE_CHECKING` — bài type hint sẽ nói.\n\n### Bố cục dự án được khuyến nghị\n\n```\ndu_an/\n├── pyproject.toml\n├── README.md\n├── src/\n│   └── goi_cua_toi/\n│       ├── __init__.py\n│       ├── main.py\n│       ├── xu_ly/\n│       │   ├── __init__.py\n│       │   └── van_ban.py\n│       └── tien_ich/\n│           ├── __init__.py\n│           └── ghi_log.py\n└── tests/\n    └── test_van_ban.py\n```\n\nVì sao có tầng `src/` — lý do rất thực dụng: nếu code nằm thẳng ở thư mục gốc, chạy `python` ở gốc dự án sẽ import code **từ thư mục nguồn** thay vì từ bản đã cài. Bài kiểm thử khi đó không kiểm tra thứ người dùng thật sự nhận được, và thiếu file trong cấu hình đóng gói sẽ không bị phát hiện. Đặt trong `src/` buộc bạn phải cài package rồi mới import được — nên kiểm thử chạy trên đúng thứ sẽ phát hành.\n\nCách làm việc chuẩn với bố cục này:\n\n```bash\npip install -e .              # cài ở chế độ \"sửa file là có hiệu lực ngay\"\npython -m goi_cua_toi.main    # chạy\npytest                        # kiểm thử — import được vì đã cài\n```\n\n`pip install -e .` là mấu chốt: nó thêm dự án vào `sys.path` một cách chính thức, nên **mọi cách chạy đều import được**, không phụ thuộc bạn đang đứng ở thư mục nào. Đây là cách sửa gốc cho `ModuleNotFoundError` — tốt hơn nhiều so với việc sửa `sys.path` bằng tay trong code.",

vi_du: "### Quan sát ba bước của `import`\n\n```python\n# tien_ich.py\nprint(f\"[tien_ich] đang chạy, __name__ = {__name__}\")\n\nDANH_SACH = []\nprint(\"[tien_ich] đã tạo DANH_SACH\")\n\ndef them(x):\n    DANH_SACH.append(x)\n    return DANH_SACH\n\nif __name__ == \"__main__\":\n    print(\"[tien_ich] chạy như script\")\n    print(them(1))\n```\n\n```bash\npython tien_ich.py\n# [tien_ich] đang chạy, __name__ = __main__\n# [tien_ich] đã tạo DANH_SACH\n# [tien_ich] chạy như script\n# [1]\n\npython -c \"import tien_ich; print(tien_ich.them(9))\"\n# [tien_ich] đang chạy, __name__ = tien_ich\n# [tien_ich] đã tạo DANH_SACH\n# [9]                              ← khối if KHÔNG chạy\n```\n\nHai lần chạy, cùng một file, khác nhau đúng ở giá trị `__name__`. Chạy thử một lần là hiểu vĩnh viễn.\n\n### Chẩn đoán `ModuleNotFoundError`\n\n```python\n# chan_doan.py — chạy khi bí\nimport sys, os\n\nprint(\"Thư mục hiện tại:\", os.getcwd())\nprint(\"File đang chạy:\", __file__)\nprint(\"\\nsys.path:\")\nfor i, p in enumerate(sys.path):\n    dau = \"⭐\" if i == 0 else \"  \"\n    print(f\"{dau} [{i}] {p!r}\")\n```\n\nQuy trình chẩn đoán ba bước:\n\n```bash\n# ① Module cần tìm nằm ở đâu?\nfind . -name \"xu_ly\" -o -name \"xu_ly.py\"\n\n# ② Thư mục CHA của nó có nằm trong sys.path không?\npython -c \"import sys; print('\\n'.join(sys.path))\"\n\n# ③ Nếu không: cài dự án ở chế độ editable — cách sửa ĐÚNG\npip install -e .\n```\n\nCách sửa **tạm** hay gặp trên mạng, và vì sao nên tránh:\n\n```python\n# ⚠️ chạy được nhưng là vá tạm\nimport sys, pathlib\nsys.path.insert(0, str(pathlib.Path(__file__).parent.parent))\nimport xu_ly\n```\n\nBa vấn đề của nó: phải lặp lại ở mọi file, dễ sai khi cấu trúc thư mục đổi, và khiến dự án không cài được như một package thật. Chấp nhận được cho một script dùng một lần; không nên có trong dự án dùng lâu dài.\n\n### Trùng tên với thư viện chuẩn\n\n```bash\n# Tạo lại lỗi\necho 'print(\"file cua toi\")' > random.py\npython -c \"import random; print(random.choice([1,2,3]))\"\n# file cua toi\n# AttributeError: module 'random' has no attribute 'choice'\n\n# Xác nhận nguyên nhân\npython -c \"import random; print(random.__file__)\"\n# ./random.py                     ← chính là file của bạn\n\nrm random.py __pycache__/random*.pyc 2>/dev/null\n```\n\nLưu ý phải xoá cả file `.pyc` trong `__pycache__`: Python cache bytecode, và một số trường hợp file `.pyc` mồ côi vẫn được dùng sau khi file `.py` đã xoá — làm lỗi tưởng như không chịu biến mất.\n\n### `__init__.py` dựng API gọn\n\n```python\n# xu_ly/van_ban.py\ndef lam_sach(s):\n    return \" \".join(s.split()).strip()\n\ndef tach_tu(s):\n    return lam_sach(s).lower().split()\n\ndef _chuan_hoa_dau(s):        # dấu _ đầu tên = nội bộ, không xuất ra\n    return s\n```\n\n```python\n# xu_ly/__init__.py\n\"\"\"Bộ công cụ xử lý dữ liệu.\"\"\"\nfrom .van_ban import lam_sach, tach_tu\n\n__all__ = [\"lam_sach\", \"tach_tu\"]      # cái gì là công khai\n__version__ = \"1.0.0\"\n```\n\n```python\n# Người dùng — gọn hơn hẳn\nfrom xu_ly import lam_sach, tach_tu\nprint(tach_tu(\"  Xin   CHÀO  bạn \"))     # ['xin', 'chào', 'bạn']\n\nimport xu_ly\nprint(xu_ly.__version__)                  # 1.0.0\n```\n\nQuy ước dấu gạch dưới đầu tên (`_chuan_hoa_dau`) không phải cơ chế bảo vệ thật — Python không cấm ai gọi nó. Nó là **giao ước**: \"đây là chi tiết nội bộ, tôi có quyền đổi mà không báo trước\". Hiệu lực của nó nằm ở chỗ `import *` bỏ qua các tên bắt đầu bằng `_`.\n\n### Import tương đối trong package\n\n```python\n# xu_ly/van_ban.py\nfrom .tien_ich import ghi_log        # cùng package\nfrom ..cau_hinh import DEBUG          # package cha\n\ndef lam_sach(s):\n    if DEBUG:\n        ghi_log(f\"làm sạch: {s!r}\")\n    return \" \".join(s.split())\n```\n\n```bash\npython xu_ly/van_ban.py\n# ImportError: attempted relative import with no known parent package\n\npython -m xu_ly.van_ban        # ✓\n```\n\nMuốn file trong package vừa dùng được như module vừa chạy được để thử, thêm khối `main` và luôn chạy bằng `-m`:\n\n```python\nif __name__ == \"__main__\":\n    print(lam_sach(\"  thử   nghiệm  \"))\n```\n\n### Tái hiện và sửa import vòng tròn\n\n```python\n# ❌ Vòng tròn\n# nguoi_dung.py\nfrom don_hang import DonHang\nclass NguoiDung:\n    def don_hang(self):\n        return [DonHang(...)]\n\n# don_hang.py\nfrom nguoi_dung import NguoiDung\nclass DonHang:\n    def chu(self):\n        return NguoiDung(...)\n```\n\n```python\n# ✓ Sửa đúng gốc: tách phần chung, hoặc gộp lại\n# mo_hinh.py — cả hai class ở cùng một chỗ vì chúng vốn dính nhau\nclass NguoiDung:\n    def don_hang(self): ...\n\nclass DonHang:\n    def chu(self): ...\n```\n\n```python\n# ✓ Hoặc: chỉ cần cho chú thích kiểu thì dùng TYPE_CHECKING\nfrom typing import TYPE_CHECKING\nif TYPE_CHECKING:\n    from don_hang import DonHang       # chỉ công cụ kiểm kiểu thấy\n\nclass NguoiDung:\n    def don_hang(self) -> list[\"DonHang\"]:      # tên trong chuỗi\n        from don_hang import DonHang            # import khi cần\n        return [DonHang()]\n```\n\nKhối `if TYPE_CHECKING` luôn là `False` lúc chạy, nên không tạo vòng tròn thật; nhưng `mypy` vẫn đọc được để kiểm kiểu. Đây là ngoại lệ chính đáng duy nhất cho việc import trong hàm.\n\nCâu hỏi nên tự hỏi khi gặp vòng tròn: **hai module này có thật sự là hai khái niệm riêng biệt không?** Nếu chúng cần nhau ở mọi lúc thì có lẽ chúng là một.\n\n### `python -m` và tác dụng thật\n\n```bash\npython -m http.server 8000           # chạy module như script\npython -m venv moi_truong            # tạo môi trường ảo\npython -m pip install requests       # ⭐ pip của ĐÚNG Python này\npython -m pytest                     # pytest thấy thư mục hiện tại\npython -m json.tool < du_lieu.json   # định dạng JSON cho dễ đọc\n```\n\nDòng `python -m pip` đáng thành thói quen: máy có nhiều bản Python thì lệnh `pip` trần có thể là pip của bản khác, và bạn cài thư viện vào một nơi rồi chạy ở nơi khác. `python -m pip` **đảm bảo cài đúng vào bản Python bạn đang dùng** — nó loại bỏ hẳn một lớp lỗi \"đã cài rồi mà vẫn không import được\".\n\n```bash\n# Kiểm tra khi nghi ngờ\nwhich python; which pip\npython -c \"import sys; print(sys.executable)\"\npython -m pip --version          # in ra pip này thuộc Python nào\n```\n\n### Module có tác dụng phụ — trước và sau\n\n```python\n# ❌ ket_noi.py — kết nối NGAY khi import\nimport psycopg2\nKET_NOI = psycopg2.connect(\"...\")          # chạy lúc import!\n\n# Hậu quả: mọi bài kiểm thử import module này đều cần database thật;\n# import chậm; và không đổi được cấu hình trước khi kết nối.\n\n# ✓ Hoãn tới khi thật sự cần\nimport psycopg2\n_ket_noi = None\n\ndef lay_ket_noi():\n    global _ket_noi\n    if _ket_noi is None:\n        _ket_noi = psycopg2.connect(\"...\")\n    return _ket_noi\n```\n\nMẫu \"khởi tạo lười\" này giữ được tiện lợi của biến toàn cục mà bỏ được tác dụng phụ lúc import. Nó cũng cho phép bài kiểm thử thay thế `lay_ket_noi` bằng bản giả — điều không làm được với biến khởi tạo sẵn.\n\n### Xem module đã nạp\n\n```python\nimport sys\n\n# Module không thuộc thư viện chuẩn đã nạp\nfor ten, mod in sorted(sys.modules.items()):\n    f = getattr(mod, \"__file__\", \"\") or \"\"\n    if \"site-packages\" in f:\n        print(f\"{ten:<25} {f}\")\n\nprint(f\"\\nTổng: {len(sys.modules)} module đã nạp\")\n```\n\nHữu ích khi khởi động chương trình chậm bất thường: nó cho thấy module nào bị kéo theo. Đo cụ thể hơn thì dùng:\n\n```bash\npython -X importtime -c \"import goi_cua_toi\" 2>&1 | sort -k2 -rn | head\n```\n\nLệnh này liệt kê thời gian import của từng module, sắp xếp giảm dần — cách nhanh nhất để tìm ra một `__init__.py` đang kéo theo cả thư viện tính toán nặng.",

so_sanh: "### `import x` và `from x import y`\n\n| | `import x` | `from x import y` |\n|---|---|---|\n| Chạy toàn bộ `x` | ✓ | **✓ — cũng chạy hết** |\n| Tên trong không gian tên | `x` | `y` |\n| Truy cập | `x.y` | `y` |\n| Thấy giá trị mới nhất | **✓** | ✗ buộc một lần |\n| Rõ nguồn gốc khi đọc code | **✓** | kém hơn |\n| Nguy cơ trùng tên | thấp | **cao hơn** |\n\nHàng \"chạy toàn bộ\" bác bỏ một ngộ nhận phổ biến: `from x import y` **không** nạp ít hơn và **không** nhanh hơn.\n\nHàng \"thấy giá trị mới nhất\" là khác biệt thực dụng nhất: với cấu hình có thể đổi lúc chạy, dùng `import cau_hinh` rồi `cau_hinh.DEBUG`.\n\n### `python file.py` và `python -m goi.file`\n\n| | `python file.py` | `python -m goi.file` |\n|---|---|---|\n| `sys.path[0]` | thư mục **chứa file** | thư mục **hiện tại** |\n| Import tương đối | **✗ lỗi** | **✓** |\n| Biết ngữ cảnh package | ✗ | ✓ |\n| `__name__` | `\"__main__\"` | `\"__main__\"` |\n| Dùng khi | script độc lập | **file trong package** |\n\nĐây là bảng đáng nhớ nhất của bài: **phần lớn `ModuleNotFoundError` và `ImportError` về import tương đối đều giải thích được bằng hai dòng đầu.**\n\n### Import tuyệt đối và tương đối\n\n| | Tuyệt đối | Tương đối |\n|---|---|---|\n| Cú pháp | `from goi.mod import f` | `from .mod import f` |\n| Đọc là biết vị trí | **✓** | cần biết ngữ cảnh |\n| Đổi tên package | phải sửa hết | **không phải sửa** |\n| Di chuyển module con | phải sửa | không phải sửa |\n| Chạy file trực tiếp | ✓ | **✗** |\n| PEP 8 | **ưu tiên** | chấp nhận trong package |\n\nCách dùng thực tế: **tuyệt đối cho mọi import từ ngoài vào; tương đối cho các module anh em bên trong cùng một package**. Cách này cho code vừa dễ đọc vừa chịu được việc đổi tên package.\n\n### Có và không có `__init__.py`\n\n| | Có `__init__.py` | Namespace package |\n|---|---|---|\n| Ý định rõ ràng | **✓** | mọi thư mục đều thành package |\n| Dựng API gọn | **✓** | ✗ |\n| Đặt `__all__`, `__version__` | ✓ | ✗ |\n| Gộp từ nhiều nơi | ✗ | ✓ |\n| Công cụ đóng gói | hỗ trợ tốt | đôi khi trục trặc |\n\nKhuyến nghị: **cứ tạo `__init__.py`, kể cả rỗng.** Namespace package sinh ra cho trường hợp chia một package lớn ra nhiều nơi cài đặt — không phải cho dự án thông thường.\n\n### Ba cách sửa import vòng tròn\n\n| | Tách module chung | Import trong hàm | `import x` thay `from x import y` |\n|---|---|---|---|\n| Sửa gốc vấn đề | **✓** | ✗ | ✗ |\n| Code rõ ràng hơn | **✓** | ✗ khó lần phụ thuộc | trung bình |\n| Chi phí lúc chạy | không | mỗi lần gọi tra `sys.modules` | không |\n| Khi nào dùng | **mặc định** | chỉ khi bất khả kháng | tình huống đơn giản |\n\nCột đầu là cột đúng. Hai cột sau làm chương trình chạy được nhưng để lại một phụ thuộc vòng — và nó sẽ quay lại gây khó khi dự án lớn thêm.\n\n### Nơi đặt code khởi tạo\n\n| | Tầng module | Trong hàm | `if __name__` |\n|---|---|---|---|\n| Chạy khi import | **✓** | ✗ | ✗ |\n| Chạy khi gọi trực tiếp | ✓ | khi gọi | ✓ |\n| Phù hợp cho | hằng số, `def`, `class` | logic | điểm vào của script |\n| Tác dụng phụ | **tránh** | ✓ | ✓ |\n\nQuy tắc gói lại một câu: **tầng module chỉ nên chứa định nghĩa và hằng số; mọi hành động đặt trong hàm; điểm vào đặt dưới `if __name__ == \"__main__\"`.**",

loi_thuong_gap: [
  "**Đặt tên file trùng thư viện chuẩn** (`random.py`, `json.py`, `email.py`, `test.py`). File của bạn thắng và thư viện thật bị che. Kiểm tra bằng `module.__file__`.",
  "**Quên xoá `__pycache__` sau khi xoá file trùng tên.** Bytecode cache khiến lỗi tưởng như không chịu biến mất.",
  "**Thiếu `if __name__ == \"__main__\"`.** Người khác import module của bạn và toàn bộ phần script chạy theo.",
  "**Đặt tác dụng phụ ở tầng module** (kết nối database, đọc file, gọi API). Mọi người import bạn đều phải trả giá đó, và kiểm thử thành bất khả thi.",
  "**Chạy trực tiếp file có import tương đối.** `ImportError: attempted relative import`. Dùng `python -m goi.mod`.",
  "**Dùng `from x import *`.** Không biết tên nào đã vào, ghi đè âm thầm, công cụ phân tích bó tay.",
  "**Tưởng `from x import y` nạp ít hơn `import x`.** Cả hai đều chạy toàn bộ `x`.",
  "**Dùng `from cau_hinh import DEBUG` cho giá trị đổi lúc chạy.** Tên được buộc một lần; dùng `import cau_hinh` rồi `cau_hinh.DEBUG`.",
  "**Sửa `sys.path` bằng tay trong code dự án.** Vá tạm, phải lặp lại ở mọi file. Cách đúng là `pip install -e .`.",
  "**Sửa import vòng tròn bằng cách chuyển `import` vào trong hàm.** Chạy được nhưng để lại phụ thuộc vòng. Tách module chung.",
  "**Nhồi nhiều `from .x import ...` vào `__init__.py` của package lớn.** Mọi module con bị nạp dù người dùng chỉ cần một hàm.",
  "**Dùng `pip install` trần khi máy có nhiều bản Python.** Cài vào một nơi, chạy ở nơi khác. Dùng `python -m pip`.",
  "**Trông chờ `importlib.reload` sửa được mọi thứ khi đang phát triển.** Nó để lại trạng thái nửa vời; khởi động lại kernel thì sạch hơn.",
  "**Đặt code dự án thẳng ở thư mục gốc thay vì trong `src/`.** Kiểm thử chạy trên mã nguồn chứ không phải trên bản đã cài, nên lỗi đóng gói không bị phát hiện."
],

tu_kiem_tra: [
  "`import x` làm chính xác những bước nào? Bước nào hay bị hiểu sai nhất?",
  "Cái gì trong một module chạy lúc import, cái gì không?",
  "`sys.path[0]` khác nhau thế nào giữa `python file.py` và `python -m goi.file`? Hệ quả là gì?",
  "Vì sao đặt tên file là `random.py` làm hỏng `import random`? Kiểm tra bằng cách nào?",
  "`from x import y` có nạp ít hơn `import x` không? Khác biệt thật giữa hai dạng là gì?",
  "Vì sao nên dùng `import cau_hinh` thay vì `from cau_hinh import DEBUG` với giá trị đổi lúc chạy?",
  "`__init__.py` dùng để làm gì? Đánh đổi khi nhồi nhiều import vào đó?",
  "Vì sao chạy trực tiếp một file có import tương đối lại lỗi?",
  "Nêu ba công dụng của `if __name__ == \"__main__\"`. Vì sao nó bắt buộc với multiprocessing trên Windows?",
  "Vì sao module chỉ chạy một lần? Hai hệ quả của điều đó?",
  "Giải thích import vòng tròn theo ba bước của `import`. Ba cách sửa và cách nào đúng?",
  "`if TYPE_CHECKING` giải quyết vấn đề gì mà không tạo vòng tròn thật?",
  "Vì sao dự án nên đặt code trong `src/`? `pip install -e .` giải quyết vấn đề gì?",
  "Vì sao nên gõ `python -m pip install` thay vì `pip install`?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Modules* trong tutorial và *The import system* trong tham khảo ngôn ngữ — phần sau giải thích đầy đủ cơ chế tìm và nạp.",
  "Tài liệu Python, mục *`__main__` — Top-level code environment* — giải thích chính thức về `__name__` và `python -m`.",
  "Sách **Python Packaging User Guide** (tài liệu chính thức của cộng đồng đóng gói) — phần về bố cục `src/` và cài đặt editable.",
  "Sách **Fluent Python** — Luciano Ramalho. Phần về module và không gian tên, cùng chương về object hạng nhất.",
  "Lệnh `python -X importtime` và module `importlib` trong tài liệu chuẩn — hai công cụ chẩn đoán khi import chậm hoặc sai."
],

lien_he: [
  "**Hàm, tham số mặc định** ngay trước: `def` là câu lệnh được thực thi — cùng nguyên lý với việc `import` chạy cả file.",
  "**Biến môi trường và PATH** ở module 3: `sys.path` là PATH của Python, và cả hai đều lấy kết quả khớp đầu tiên.",
  "**Hệ thống file, đường dẫn** ở module 2: thư mục làm việc hiện tại quyết định `sys.path[0]` trong nhiều trường hợp.",
  "**venv, pip, requirements, pyproject** ở cuối module: `site-packages` nằm trong `sys.path`, và môi trường ảo chính là đổi chỗ đó.",
  "**Type hint và mypy** ở cuối module: `if TYPE_CHECKING` là cách tránh import vòng tròn khi chỉ cần kiểu.",
  "**Đọc ghi file** trong module này: `__file__` và `pathlib` để tìm file dữ liệu đi kèm module.",
  "**Clean Code** ở module 8 mảng 01: tách module là hình thức cơ bản nhất của phân tách trách nhiệm.",
  "**Git & GitHub** ở mảng 03: `__pycache__` là thứ luôn phải đưa vào `.gitignore`.",
  "**Backend & Web** ở mảng 05: bố cục package và điểm vào `python -m` là cách mọi framework tổ chức dự án."
]

};
