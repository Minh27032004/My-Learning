window.LESSONS["05:auth-bao-mat:jwt-cau-truc-ky-han-dung-refresh-token"] = {

tom_tat: [
  "JWT là định dạng claim gồm header, payload và signature; Base64url chỉ mã hóa biểu diễn chứ không che dữ liệu, còn chữ ký bảo vệ tính toàn vẹn chứ không tạo bí mật.",
  "Verifier phải cố định thuật toán được phép và kiểm đủ issuer, audience, thời gian, loại token; “signature hợp lệ” chưa đồng nghĩa token phù hợp endpoint.",
  "Access token nên ngắn hạn và có phạm vi nhỏ; refresh token sống lâu hơn, chỉ đến authorization server, được lưu/rotate/revoke như credential cấp cao.",
  "Refresh rotation kèm phát hiện reuse biến chuỗi token thành state machine: dùng lại token cũ là dấu hiệu sao chép và nên thu hồi cả token family.",
  "StudyHub dùng access JWT cho API mobile nhưng giữ refresh record dạng hash ở server, nhờ vậy scale request thường mà vẫn có điểm kiểm soát vòng đời."
],

can_biet_truoc: [
  "Session-based và token-based, bearer credential và replay attack.",
  "Hash, HMAC, chữ ký bất đối xứng, public key và private key ở mức khái niệm.",
  "JSON, Unix timestamp, HTTP `Authorization: Bearer` và status 401.",
  "Authentication khác authorization; claim không thay thế policy trên resource."
],

dinh_nghia: "**JSON Web Token (JWT)** là một định dạng compact để chuyển các claim dưới dạng JSON. Dạng ký JWS phổ biến có ba đoạn Base64url ngăn bằng dấu chấm: protected header, payload và signature trên đúng bytes của hai đoạn đầu. Bên nhận kiểm chữ ký và các điều kiện ngữ nghĩa trước khi tin claim.\n\n**Access token** là credential trình bày với resource server để gọi API. **Refresh token** là credential dài hạn hơn dùng tại authorization server để xin access token mới; nó không cần là JWT và thường nên opaque để server kiểm soát reuse, rotation và thu hồi.",

vi_sao_can: "JWT hấp dẫn vì API StudyHub có thể kiểm chứng request mà không gọi session store. Nhưng vẻ đơn giản che nhiều quyết định: khóa nào đáng tin, token dành cho API nào, đồng hồ lệch ra sao, role đổi khi nào có hiệu lực, và token bị copy được xử lý thế nào. Decode payload thành công chỉ chứng minh chuỗi là JSON dễ đọc, không chứng minh người phát hay tính hợp lệ.\n\nLỗi JWT thường nằm ở validation contract chứ không ở phép toán chữ ký: chấp nhận sai algorithm, bỏ audience, dùng ID token như access token, đặt TTL quá dài hoặc lưu refresh token như plaintext. Bài này xây một verifier với policy đóng và một vòng refresh có state rõ.",

co_che: "### Ba đoạn và bytes thực sự được ký\n\nToken có dạng `base64url(header).base64url(payload).base64url(signature)`. Header thường nêu `alg`, có thể có `kid`. Payload chứa claim. Signature được tính trên chuỗi ASCII của hai đoạn đầu cùng dấu chấm. Thay một byte payload làm signature không còn khớp.\n\nBase64url bỏ ký tự gây khó trong URL và thường bỏ padding; bất kỳ ai có token đều decode được header/payload. Không đặt password, secret, dữ liệu sức khỏe hay PII không cần thiết vào đó. Muốn confidentiality cần cơ chế encryption phù hợp, nhưng token nhỏ và tối thiểu vẫn tốt hơn.\n\n### Claim đã đăng ký có semantics riêng\n\n`iss` xác định issuer; `sub` định danh subject trong namespace của issuer; `aud` giới hạn bên nhận; `exp` là thời điểm không được chấp nhận sau đó; `nbf` là chưa được dùng trước; `iat` là lúc phát; `jti` là ID token. Claim không tự được enforce chỉ vì có mặt: verifier phải cấu hình yêu cầu và so giá trị.\n\nKhông dùng `sub` làm email nếu email có thể đổi hoặc tái sử dụng. `aud` đặc biệt quan trọng khi một issuer phát token cho nhiều API: token dành cho video service không được dùng ở admin API. Custom claim nên có namespace/contract và kích thước kiểm soát.\n\n### Decode, verify và authorize là ba tầng\n\nDecode chỉ chuyển Base64url thành JSON. Verify cryptographic kiểm signature bằng khóa tin cậy. Validate claims kiểm issuer, audience, thời gian và token type. Sau đó authorization mới xét action/resource. Một thư viện có hàm `decode` đôi khi đồng thời verify tùy tham số, nên tên hàm không thay thế việc đọc contract.\n\nKhông bao giờ dùng payload trước khi verify để truy vấn tenant, chọn quyền hay tạo log “user đã xác thực”. Có thể đọc `kid` từ header chưa tin cậy chỉ để chọn trong tập khóa đã cấu hình, nhưng phải giới hạn định dạng và không biến nó thành file path/URL tùy ý.\n\n### Algorithm confusion và policy đóng\n\nVerifier phải chỉ định danh sách thuật toán chấp nhận từ cấu hình server, không tin `alg` trong token để tự quyết định. `alg: none` hoặc nhầm khóa public thành HMAC secret là các lớp lỗi lịch sử minh họa nguyên tắc này. Khóa phải gắn với algorithm/key type dự kiến.\n\nNếu StudyHub chọn RS256 cho access token, resource server chỉ chấp nhận RS256 từ issuer ấy. Đừng trộn HS256 và RS256 trong cùng đường verify nếu thư viện có thể diễn giải vật liệu khóa mơ hồ. Lỗi validation phải fail closed.\n\n### Đối xứng hay bất đối xứng\n\nHMAC dùng cùng secret để ký và verify. Mọi service được phép verify cũng có khả năng phát token giả nếu secret lộ hoặc bị lạm dụng. Nó đơn giản cho một service duy nhất. Chữ ký bất đối xứng giữ private key ở authorization server, còn API chỉ có public key; compromise API verifier không tự cho quyền mint token.\n\nĐổi lại, asymmetric key management phức tạp hơn và phép verify nặng hơn. Đừng gửi private key cho resource server. Secret/key phải ở secret manager, không commit, không log và có quy trình rotation.\n\n### `kid` và rotation khóa ký\n\nAuthorization server phát token mới với key hiện hành và `kid`. Resource server giữ một key set gồm key mới và key cũ trong thời gian token cũ còn hạn. Quy trình an toàn: publish key mới; đợi verifier nhận; bắt đầu ký bằng key mới; giữ key cũ đủ lâu; rồi loại bỏ.\n\nXóa key cũ ngay làm logout hàng loạt. Giữ key bị compromise quá lâu kéo dài rủi ro. Cache key set cần TTL, retry có giới hạn và last-known-good; endpoint cung cấp key bị lỗi không nên khiến verifier tải một URL lấy từ token.\n\n### Thời gian, clock skew và giới hạn\n\n`exp` phải bắt buộc cho access token. Leeway nhỏ có thể hấp thụ lệch đồng hồ/network nhưng đồng thời kéo dài cửa sổ chấp nhận; không dùng leeway lớn để che NTP hỏng. `iat` quá xa tương lai hay token sống vượt maximum TTL nên bị từ chối ngay cả khi `exp` hợp lệ về hình thức.\n\nMọi node cần đồng bộ thời gian và metric cho lỗi expired/not-yet-valid. Không ghi token vào log. Một access token hết hạn trả 401 với mã lỗi máy đọc được; client chỉ refresh một lần, tránh vòng lặp vô hạn.\n\n### Access token phải nhỏ, ngắn và đúng audience\n\nAccess token là bearer: ai cầm được có thể dùng, trừ khi triển khai sender-constrained scheme riêng. TTL ngắn giới hạn replay sau trộm và độ cũ của permission. Nhưng quá ngắn làm refresh thường xuyên, tăng tải và lỗi trải nghiệm khi authorization server outage.\n\nKhông có con số TTL phổ quát. StudyHub chọn theo độ nhạy endpoint, khả năng refresh và thời gian phát hiện. Token video chỉ nên có audience/scope xem video, không mang admin role. Claim authorization chỉ là giới hạn sơ bộ; ownership vẫn đọc resource.\n\n### Refresh token không nên đi tới mọi API\n\nClient gửi refresh token duy nhất tới token endpoint qua TLS. Resource server không cần thấy nó. Vì sống lâu và có thể sinh access token mới, nó có giá trị cao hơn access token; lưu trên mobile cần secure storage, trên web thường ưu tiên HttpOnly cookie/BFF tùy kiến trúc.\n\nServer lưu hash của opaque refresh token cùng user, client, family ID, expiration, revoked time và token kế tiếp. Database leak không cho phép dùng trực tiếp giá trị hash như bearer. Token ngẫu nhiên phải đủ mạnh và không chứa claim cần decode.\n\n### Rotation là state transition nguyên tử\n\nKhi nhận refresh token R1 hợp lệ, server trong transaction đánh dấu R1 đã dùng, tạo R2 và access token A2, rồi trả về. R1 không hợp lệ lần hai. Nếu hai request đồng thời dùng R1, chỉ một UPDATE điều kiện `used_at IS NULL` thắng.\n\nNetwork timeout tạo ca khó: server có thể đã commit R2 nhưng client không nhận và retry R1. Chính sách nghiêm coi reuse là compromise sẽ logout user dù đây là retry vô tội. Có thể dùng grace/idempotency binding rất ngắn và giữ response mã hóa, nhưng complexity tăng. Client nên serialize refresh request trong một process/tab.\n\n### Reuse detection và token family\n\nNếu R1 đã dùng rồi xuất hiện sau khi R2 được phát, có thể attacker và user đang giữ hai bản sao. Server revoke toàn family, buộc đăng nhập lại và ghi security event. Chỉ từ chối R1 nhưng giữ R2 cho phép attacker đã thắng race tiếp tục.\n\nFamily giúp “logout thiết bị này” thay vì logout mọi thiết bị. Mỗi lần login tạo family mới; logout all tăng credential version hoặc revoke mọi family. Cleanup phải xóa record hết hạn nhưng giữ dấu reuse đủ theo policy điều tra.\n\n### Revocation và cửa sổ access token\n\nRevoke refresh family ngăn token mới, không làm A2 đã phát biến mất. Để action cực nhạy cảm phản ứng tức thời, API có thể kiểm user/session version hoặc denylist `jti`, nhưng đó là lookup. Phương án thường là TTL access ngắn cộng step-up authentication cho đổi email, thanh toán hoặc admin.\n\nKey rotation khẩn cấp thu hồi diện rộng nhưng blast radius lớn. Không dùng nó như nút logout một user. Thiết kế phải nói rõ maximum exposure window thay vì hứa “logout ngay” khi thực tế token còn hạn.\n\n### Token substitution và phân biệt loại token\n\nID token chứng minh sự kiện đăng nhập cho client; access token dành cho API. Nếu cùng issuer/audience mơ hồ, API có thể nhận nhầm ID token. Thêm `typ` hoặc claim purpose, audience riêng, key/policy riêng và endpoint chỉ chấp nhận đúng loại.\n\nRefresh token cũng không được đi qua access-token verifier. Prefix/format khác giúp routing nhưng security nằm ở validation policy. Các môi trường dev/staging/prod phải có issuer và key tách biệt để token test không chạy ở production.\n\n### Failure và observability không lộ credential\n\nPhân loại nội bộ: malformed, bad signature, unknown key, wrong issuer, wrong audience, expired, revoked. Response ngoài thường gọn để không thành oracle. Metric theo reason và issuer giúp phát hiện clock skew hoặc deployment key lỗi; log chỉ hash/fingerprint ngắn của `jti` khi thật cần correlation.\n\nJWKS fetch timeout, database refresh timeout và ký token lỗi phải trả failure có kiểm soát. Không fail-open dùng key cũ vô hạn ngoài policy. Rate limit token endpoint vì signature/password/refresh lookup đều là tài nguyên có thể bị lạm dụng.\n\n### Ca biên phải test\n\nThiếu đoạn, JSON trùng key, claim sai kiểu, `aud` là chuỗi hay mảng, `exp` thiếu, token cực lớn, Unicode bất thường, `kid` không tồn tại và signature hỏng đều phải từ chối không thành 500. Hai refresh đồng thời chỉ một thành công. User disabled giữa access lifetime phải tuân policy đã công bố.\n\nKhông tự viết parser mật mã. Dùng thư viện trưởng thành, khóa version dependency và test negative cases. Việc ví dụ minh họa tự tạo JWT không cấp phép mang nó nguyên xi thành authorization server production.\n\n### Khi JWT không đem lại lợi ích\n\nMonolith web có shared session store, revoke tức thời và không có resource server độc lập thường dùng session đơn giản hơn. JWT làm payload lớn, rotation key và validation phức tạp mà không loại state vòng đời. Ngược lại, nhiều API cần verify cục bộ và chấp nhận token ngắn hạn có thể hưởng lợi rõ.\n\nOpaque access token tốt khi cần control tập trung/introspection và không muốn claim lộ cho client. JWT không “an toàn hơn” chỉ vì có chữ ký; security đến từ toàn bộ issuance, storage, validation và revocation.",

vi_du: "### Phát và kiểm access JWT bằng FastAPI\n\nVí dụ dùng `PyJWT`; secret môi trường chỉ để demo HS256 một service. Production không hard-code và cần rate limit, key management, refresh database.\n\n```python\nfrom datetime import datetime, timedelta, timezone\nimport os, uuid, jwt\nfrom fastapi import Depends, FastAPI, Header, HTTPException\n\napp = FastAPI()\nKEY = os.environ[\"STUDYHUB_JWT_KEY\"]\nISSUER = \"https://auth.studyhub.example\"\nAUDIENCE = \"studyhub-api\"\n\ndef issue_access(user_id: int) -> str:\n    now = datetime.now(timezone.utc)\n    claims = {\"iss\": ISSUER, \"aud\": AUDIENCE, \"sub\": str(user_id),\n              \"iat\": now, \"nbf\": now, \"exp\": now + timedelta(minutes=10),\n              \"jti\": str(uuid.uuid4()), \"typ\": \"access\"}\n    return jwt.encode(claims, KEY, algorithm=\"HS256\")\n\ndef principal(authorization: str | None = Header(default=None)) -> int:\n    scheme, _, token = (authorization or \"\").partition(\" \")\n    if scheme.lower() != \"bearer\" or not token:\n        raise HTTPException(401, \"Thiếu access token\")\n    try:\n        payload = jwt.decode(token, KEY, algorithms=[\"HS256\"],\n            issuer=ISSUER, audience=AUDIENCE,\n            options={\"require\": [\"exp\", \"iat\", \"nbf\", \"iss\", \"aud\", \"sub\"]})\n        if payload.get(\"typ\") != \"access\":\n            raise jwt.InvalidTokenError(\"wrong token type\")\n        return int(payload[\"sub\"])\n    except (jwt.InvalidTokenError, ValueError):\n        raise HTTPException(401, \"Access token không hợp lệ\")\n\n@app.get(\"/me\")\ndef me(user_id: int = Depends(principal)):\n    return {\"user_id\": user_id}\n```\n\nĐiểm quan trọng là `algorithms`, `issuer`, `audience` và required claims nằm trong cấu hình verifier, không lấy từ payload. Test phải ký token sai audience, thiếu exp, quá hạn, `typ=refresh` và signature sai rồi khẳng định đều bị từ chối.\n\n```python\ndef test_wrong_audience_is_rejected():\n    now = datetime.now(timezone.utc)\n    bad = jwt.encode({\"iss\": ISSUER, \"aud\": \"video-api\", \"sub\": \"1\",\n                      \"iat\": now, \"nbf\": now,\n                      \"exp\": now + timedelta(minutes=5)}, KEY, algorithm=\"HS256\")\n    try:\n        jwt.decode(bad, KEY, algorithms=[\"HS256\"], issuer=ISSUER, audience=AUDIENCE)\n        assert False\n    except jwt.InvalidAudienceError:\n        pass\n```",

so_sanh: "### JWT, opaque token và session ID\n\n| Thuộc tính | JWT access | Opaque access | Session ID |\n|---|---|---|---|\n| Nội dung client thấy | claim đọc được | chuỗi ngẫu nhiên | chuỗi ngẫu nhiên |\n| Kiểm chứng | chữ ký + claim | introspection/lookup | session lookup |\n| Thu hồi | khó nếu không state | tập trung | xóa record |\n| Dữ liệu cũ | đến hết TTL | theo cache | cập nhật record/nguồn |\n| Mục đích điển hình | grant tới API | grant kiểm soát tập trung | trạng thái browser login |\n\n### Access và refresh không phải cùng token với TTL khác\n\n| | Access token | Refresh token |\n|---|---|---|\n| Nơi trình bày | resource server | authorization server |\n| Tuổi thọ | ngắn | dài hơn |\n| Phạm vi | audience/scope hẹp | xin token theo grant |\n| Lưu server | có thể không | nên có state/hash/rotation |\n| Khi bị trộm | gọi API đến expiry | duy trì truy cập, giá trị cao hơn |\n\nDùng JWT cho refresh thường không đem lợi ích tự kiểm chứng vì token endpoint vẫn cần state để phát hiện reuse và revoke. Opaque random token giảm claim thừa và ép lookup đúng nơi kiểm soát vòng đời.",

loi_thuong_gap: [
  "Chỉ decode payload rồi tin `sub`; nhận biết khi token sửa nội dung hoặc signature rác vẫn dựng được user.",
  "Tin `alg`/`kid` từ header không giới hạn; nhận biết verifier tự chọn thuật toán hoặc fetch URL/path từ input.",
  "Kiểm signature nhưng bỏ issuer/audience/type; nhận biết token của service hay môi trường khác được API chấp nhận.",
  "Đặt PII hoặc secret trong payload; nhận biết chỉ cần Base64url decode là đọc được.",
  "Cho access token sống quá lâu để khỏi refresh; nhận biết user bị khóa vẫn gọi API trong cửa sổ lớn.",
  "Lưu refresh token thô và cho dùng lặp; nhận biết database leak cho bearer dùng ngay, replay không tạo cảnh báo.",
  "Refresh đồng thời không có transaction; nhận biết cùng token sinh nhiều token kế tiếp hợp lệ.",
  "Log nguyên JWT trong query/header; nhận biết credential xuất hiện ở access log, tracing hoặc error report."
],

tu_kiem_tra: [
  "Base64url khác encryption ở đâu, và signature bảo đảm điều gì nhưng không bảo đảm điều gì?",
  "Vì sao signature hợp lệ từ đúng issuer vẫn chưa đủ nếu audience sai?",
  "Mô tả thứ tự rotation khóa không làm token cũ chết sớm và không ký bằng key verifier chưa biết.",
  "Hai request đồng thời dùng cùng refresh token: câu lệnh/transaction nào đảm bảo chỉ một request thắng?",
  "Reuse R1 sau khi R2 đã phát nên ảnh hưởng một token, một family hay mọi thiết bị? Giải thích threat model.",
  "User bị khóa ngay sau khi access token phát. Maximum exposure window là gì và giảm nó phải trả giá nào?",
  "Tại sao JWT refresh token thường không giúp “stateless” khi hệ thống cần rotation và reuse detection?"
],

tai_nguyen: [
  "RFC 7519 — JSON Web Token.",
  "RFC 8725 — JSON Web Token Best Current Practices.",
  "OAuth 2.0 Security Best Current Practice.",
  "OWASP JSON Web Token Cheat Sheet for Java — dùng các nguyên tắc, không sao chép API Java."
],

lien_he: [
  "Nối sang mảng Cryptography qua HMAC, chữ ký bất đối xứng, key rotation và ranh giới public/private key.",
  "Nối sang mảng Distributed Systems qua clock skew, key distribution, cache last-known-good và revoke propagation.",
  "Nối sang mảng Database qua transaction rotation, unique constraint và lưu hash refresh token.",
  "Nối sang mảng API Design qua audience, scope, error contract và retry refresh có kiểm soát."
]

};
