window.LESSONS["07:deep-learning:optimizer-sgd-momentum-adam"] = {

tom_tat: [
  "Optimizer dùng gradient để thay tham số. SGD đi theo gradient batch hiện tại; Momentum tích lũy vận tốc; Adam chuẩn hóa bước theo trung bình và bình phương gradient của từng tham số.",
  "Learning rate quyết định độ dài bước, không phải “tốc độ” theo nghĩa càng lớn càng tốt. Quá nhỏ bò chậm; quá lớn dao động hoặc diverge.",
  "Momentum làm mượt nhiễu và tăng tốc theo hướng gradient nhất quán, nhưng thêm trạng thái vận tốc và có thể vượt qua vùng tối ưu nếu cấu hình hung hăng.",
  "Adam thường cho baseline nhanh và ít nhạy với scale gradient, song tốn khoảng hai buffer trạng thái mỗi tham số và không đảm bảo generalization tốt hơn SGD.",
  "Thí nghiệm nhà kính dùng đúng cùng model, batch order và khởi tạo để so quỹ đạo công bằng; optimizer là một lựa chọn thực nghiệm, không có người thắng mọi bài toán."
],

can_biet_truoc: [
  "Backpropagation và `.grad`: optimizer không tính gradient mà đọc gradient đã được backward điền.",
  "Loss hồi quy/phân loại; biết một bước cập nhật nhằm giảm loss cục bộ nhưng metric validation mới quyết định mô hình dùng được.",
  "Vector và norm; gradient, vận tốc và moment của Adam đều là tensor cùng shape tham số.",
  "Vòng lặp PyTorch: `zero_grad`, forward, loss, backward, `step`; bài này giải thích chi tiết bước cuối."
],

dinh_nghia: "**Optimizer** là quy tắc biến trạng thái tham số θ và gradient gₜ = ∂Lₜ/∂θ ở bước t thành tham số mới. SGD cơ bản dùng θₜ₊₁ = θₜ − ηgₜ, với η là learning rate.\n\n**Momentum** duy trì vận tốc—trung bình có quán tính của gradient—để giảm rung và tăng bước theo hướng ổn định. **Adam** duy trì moment bậc một mₜ (trung bình gradient) và bậc hai vₜ (trung bình gradient bình phương), rồi chia mₜ cho căn vₜ để mỗi tham số có bước thích nghi.\n\nOptimizer không biết ý nghĩa “cần tưới”; nó chỉ thấy tensor, gradient và siêu tham số. Chất lượng cuối phụ thuộc loss, dữ liệu, schedule, regularization và điểm dừng, không chỉ tên optimizer.",

vi_sao_can: "Gradient là mũi tên dốc lên nhanh nhất tại vị trí hiện tại; trừ gradient cho hướng xuống. Nhưng địa hình loss có khe dài, dốc khác nhau theo chiều và nhiễu do mini-batch. Một quy tắc bước ngây thơ có thể lắc ngang khe, bò chậm dọc khe hoặc nhảy ra ngoài.\n\nSGD, Momentum và Adam là ba cách xử lý lịch sử và scale. SGD đơn giản, rẻ và đôi khi tổng quát hóa tốt. Momentum nhớ hướng để vượt vùng phẳng. Adam tự điều chỉnh theo độ lớn gradient từng tham số, hữu ích khi gradient thưa hoặc scale không đều.\n\nTrong nhà kính, dữ liệu mùa và sensor làm batch có nhiễu. Chọn optimizer phải đi cùng split thời gian, learning-rate schedule và metric. Nếu chỉ so train loss ở epoch đầu, Adam thường trông thắng nhưng có thể không thắng validation cuối hoặc chi phí bộ nhớ GPU.",

co_che: "### Gradient là hướng, learning rate là độ dài bước\n\nVới một tham số θ, khai triển cục bộ L(θ+Δ) ≈ L(θ)+gΔ. Chọn Δ=−ηg làm số hạng gΔ âm nếu η>0, nên dự kiến giảm loss khi bước đủ nhỏ. Đây là lý do dấu trừ xuất hiện.\n\nXấp xỉ chỉ đúng gần điểm hiện tại. η quá lớn làm phần cong bị bỏ qua, bước có thể tăng loss. η quá nhỏ giữ đúng cục bộ nhưng cần quá nhiều bước. Vì vậy learning rate thường là siêu tham số ảnh hưởng mạnh nhất.\n\n### SGD thực tế dùng mini-batch\n\nTên stochastic gradient descent xuất phát từ gradient ước lượng bằng mẫu hoặc mini-batch, không phải toàn tập. Gradient batch khác nhau tạo nhiễu. Nhiễu có thể giúp thoát vùng phẳng nhưng cũng làm loss rung.\n\nBatch lớn cho gradient ổn định và khai thác phần cứng, nhưng tốn bộ nhớ và có ít lần cập nhật mỗi epoch. Batch nhỏ tạo nhiều bước và nhiễu hơn. Đổi batch size mà giữ mọi thứ khác không hẳn công bằng vì số bước và scale reduction có thể đổi.\n\n### Hình dung khe loss dài và hẹp\n\nGiả sử loss cong mạnh theo một chiều và thoải theo chiều kia. Gradient trỏ nhiều vào thành khe, SGD bật qua lại theo chiều dốc trong khi tiến chậm theo đáy. Giảm η giảm rung nhưng càng chậm.\n\nMomentum tích lũy các gradient dọc đáy vì chúng cùng dấu, còn thành phần ngang đổi dấu nên triệt tiêu một phần. Cơ chế này giống quả cầu có quán tính, nhưng phép so sánh chỉ là trực giác; công thức mới quyết định hành vi.\n\n### Công thức Momentum và hai quy ước\n\nMột dạng phổ biến: vₜ = μvₜ₋₁ + gₜ, θₜ₊₁ = θₜ − ηvₜ. μ thường gần 1 và quyết định mức nhớ. Một số tài liệu đặt `(1−μ)` trước gradient; khi đó scale vận tốc khác và không thể sao chép learning rate trực tiếp.\n\nPyTorch SGD với `momentum` có quy ước riêng được tài liệu hóa. Khi tự đối chiếu số bằng tay, phải dùng đúng quy ước implementation. Đừng kết luận framework sai chỉ vì công thức trên blog dùng chuẩn hóa khác.\n\n### Dampening và Nesterov\n\nDampening giảm đóng góp gradient mới vào buffer. Nesterov momentum tính gradient với cái nhìn “đi trước” theo vận tốc, nhằm phản ứng sớm khi sắp vượt qua vùng tốt. PyTorch yêu cầu một số điều kiện khi bật Nesterov.\n\nCác biến thể này có lợi trong vài kiến trúc, nhưng thêm trục tuning. Baseline nên bắt đầu SGD+Momentum rõ ràng; chỉ bật Nesterov khi validation hoặc recipe đáng tin cho thấy lợi ích.\n\n### Adam: moment bậc một\n\nAdam dùng mₜ = β₁mₜ₋₁ + (1−β₁)gₜ. Đây là exponential moving average của gradient. β₁ lớn nhớ dài và làm mượt; quá lớn phản ứng chậm khi hướng thay đổi.\n\nm đóng vai trò gần Momentum nhưng được chuẩn hóa cách viết. Ở bước đầu, m khởi tạo 0 nên bị nhỏ có hệ thống; Adam dùng bias correction m̂ₜ = mₜ/(1−β₁ᵗ).\n\n### Adam: moment bậc hai\n\nvₜ = β₂vₜ₋₁ + (1−β₂)gₜ², bình phương theo từng phần tử. v lớn ở tham số thường có gradient lớn; chia cho √v̂ làm bước tương đối nhỏ hơn. Update là θₜ₊₁ = θₜ − η m̂ₜ/(√v̂ₜ+ε).\n\nε tránh chia zero và ảnh hưởng hành vi khi v rất nhỏ, đặc biệt mixed precision. Adam không dùng Hessian; “adaptive” ở đây là scale theo lịch sử bình phương gradient, không phải hiểu đầy đủ độ cong.\n\n### Bias correction không phải tùy chọn trang trí\n\nNếu β₂=0,999 và v₀=0, các bước đầu v rất nhỏ chỉ vì chưa có lịch sử, không phải gradient nhỏ. Chia cho 1−β₂ᵗ hiệu chỉnh kỳ vọng. Bỏ nó làm learning rate hiệu dụng đầu quá khác.\n\nTại t lớn, βᵗ tiến về 0 nên correction gần 1. Tác động mạnh nhất ở warm-up—đúng lúc mô hình nhạy với khởi tạo.\n\n### AdamW và weight decay\n\nTrong SGD, L2 penalty và weight decay thường tương đương dưới điều kiện chuẩn. Với Adam, cộng λθ vào gradient bị bộ chuẩn hóa thích nghi biến đổi, nên không tương đương decay trực tiếp. AdamW tách weight decay khỏi gradient loss.\n\nVì vậy khi muốn regularization theo weight decay với Adam, `torch.optim.AdamW` thường là lựa chọn rõ nghĩa hơn `Adam(weight_decay=...)`. Tuy nhiên bài regularization sau mới phân tích cách chọn và loại tham số không decay như bias/normalization.\n\n### State làm optimizer tốn bộ nhớ\n\nSGD không Momentum gần như không cần buffer lâu dài. Momentum cần một tensor vận tốc cùng kích thước tham số. Adam cần m và v, thường thêm khoảng hai lần bộ nhớ tham số, chưa kể gradient và master weights trong mixed precision.\n\nVới model nhỏ 289 tham số, chênh lệch không đáng kể. Với hàng tỷ tham số, optimizer state là nút thắt và dẫn đến sharding/offload. Chọn Adam vì tiện mà bỏ qua memory budget có thể khiến batch phải nhỏ đến mức hiệu năng tệ.\n\n### Parameter groups\n\nPyTorch cho nhóm tham số có learning rate hoặc weight decay khác nhau. Transfer learning thường dùng backbone learning rate nhỏ và head mới lớn hơn. Có thể loại bias khỏi decay.\n\nRủi ro là bỏ sót tham số hoặc đưa một tham số vào hai nhóm. Kiểm tổng `id(parameter)` duy nhất và số phần tử; log cấu hình group cùng checkpoint để tái lập.\n\n### Thứ tự `zero_grad`, backward và step\n\nQuy trình chuẩn: xóa gradient → forward → loss → backward → tùy chọn clip → step. Nếu `step` trước backward, optimizer dùng gradient cũ hoặc None. Nếu quên zero, gradient tích lũy.\n\nMột phong cách khác zero ở cuối loop vẫn đúng nếu không có early return/exception. Zero ở đầu làm invariant dễ thấy hơn: mỗi iteration bắt đầu sạch. `set_to_none=True` tiết kiệm ghi bộ nhớ và giúp phân biệt tham số không nhận gradient.\n\n### Learning-rate schedule và warm-up\n\nOptimizer định nghĩa update tại learning rate hiện tại; scheduler thay η theo bước/epoch. Warm-up tăng η từ nhỏ để tránh update đầu bất ổn khi activation và moment chưa ổn. Decay giảm bước về cuối để tinh chỉnh.\n\nGọi scheduler trước hay sau optimizer có thể đổi lịch và từng có khác biệt API; phải theo tài liệu và test giá trị learning rate đã log. Bài schedule sau sẽ đi sâu, ở đây chỉ cần không gán mọi hiệu quả cho optimizer.\n\n### Gradient clipping\n\nClip-by-norm co toàn bộ gradient khi norm vượt ngưỡng, giữ hướng tương đối; clip-by-value cắt từng phần tử và có thể đổi hướng mạnh. Clipping bảo vệ khỏi bước hiếm quá lớn, đặc biệt RNN.\n\nNgưỡng quá thấp làm mọi bước bị kẹp và che vấn đề learning rate hoặc dữ liệu. Log tỷ lệ bước bị clip. Với automatic mixed precision, phải unscale gradient trước khi clip để ngưỡng có nghĩa trên gradient thật.\n\n### NaN và cách khoanh vùng\n\nLoss NaN có thể đến từ đầu vào, forward overflow, loss sai, gradient overflow hoặc optimizer state hỏng. Kiểm finite theo thứ tự: batch → logits/loss → gradients sau backward → parameters sau step. Điều này khoanh giai đoạn đầu tiên hỏng.\n\nGiảm learning rate là thử nghiệm hợp lý khi gradient hữu hạn nhưng parameter hỏng sau step; không phải thuốc chữa NaN đầu vào. Khôi phục checkpoint trước khi NaN vì state Adam đã chứa NaN sẽ tiếp tục đầu độc cập nhật.\n\n### So sánh optimizer công bằng\n\nDùng cùng split, preprocessing, seed khởi tạo, batch order, số bước và budget tuning. Mỗi optimizer cần learning rate riêng; dùng cùng η rồi tuyên bố người thắng là bất công vì scale update khác.\n\nChạy nhiều seed và báo mean/độ phân tán khi kết luận. Chọn checkpoint theo validation metric đã định trước. Train loss nhanh nhất không nhất thiết validation tốt nhất hay thời gian thực ngắn nhất.\n\n### Ca biên trong vòng lặp\n\nBatch có loss không phụ thuộc model làm mọi grad None; optimizer step không sửa gì. Gradient toàn zero giữ tham số nguyên nhưng Momentum buffer cũ có thể vẫn đẩy nếu buffer chưa zero—một điểm khác SGD thuần.\n\nLoad model checkpoint mà không load optimizer state làm Momentum/Adam mất lịch sử; resume không tương đương. Ngược lại, fine-tune nhiệm vụ mới đôi khi chủ đích khởi tạo optimizer mới. Phải phân biệt resume và transfer.\n\n### Khi nào chọn cái nào\n\nAdam/AdamW là baseline tiện cho transformer, gradient thưa và thử nghiệm nhanh. SGD+Momentum vẫn mạnh với nhiều bài thị giác và khi recipe đã trưởng thành, đôi khi cho generalization tốt. SGD thuần hữu ích để học cơ chế hoặc bài lồi đơn giản, nhưng thường chậm trên khe xấu.\n\nKhông nên biến lựa chọn thành giáo điều. Hãy đặt budget, sweep learning rate hợp lý, theo dõi validation và memory. Optimizer tốt nhất là optimizer đáp ứng mục tiêu trong ràng buộc, không phải tên phổ biến nhất.",

vi_du: "### Nhìn update một chiều bằng tay\n\n```python\ndef sgd(theta, grad, lr):\n    return theta - lr * grad\n\ntheta = 2.0\ngrad = 4.0\nassert sgd(theta, grad, 0.1) == 1.6\nprint(sgd(theta, grad, 0.1))\n```\n\nGradient dương nên trừ nó làm θ giảm. Nếu learning rate 1, θ nhảy thành −2; với loss θ², loss vẫn bằng 4, không giảm—minh họa bước quá lớn có thể vượt qua đáy.\n\n### Cài Momentum và Adam tối giản để quan sát state\n\n```python\nimport math\n\ngrads = [4.0, 3.0, -1.0]\n\ntheta_m, velocity = 2.0, 0.0\nfor g in grads:\n    velocity = 0.9 * velocity + g\n    theta_m -= 0.1 * velocity\nprint(\"Momentum:\", theta_m, \"v:\", velocity)\n\ntheta_a, m, v = 2.0, 0.0, 0.0\nb1, b2, lr, eps = 0.9, 0.999, 0.1, 1e-8\nfor t, g in enumerate(grads, start=1):\n    m = b1 * m + (1 - b1) * g\n    v = b2 * v + (1 - b2) * g * g\n    m_hat = m / (1 - b1 ** t)\n    v_hat = v / (1 - b2 ** t)\n    theta_a -= lr * m_hat / (math.sqrt(v_hat) + eps)\nprint(\"Adam:\", theta_a, \"m:\", m, \"v:\", v)\n```\n\nGradient thứ ba đổi dấu. Momentum vẫn chịu quán tính của hai bước trước; Adam cũng nhớ nhưng chuẩn hóa bằng v. Đây là hành vi thiết kế, không phải chậm phản hồi do bug.\n\n### So ba optimizer trên cùng dữ liệu tổng hợp nhà kính\n\n```python\nimport copy\nimport torch\nfrom torch import nn\n\ntorch.manual_seed(21)\nx = torch.randn(512, 8)\nlogit_that = -1.8*x[:, 0] + 1.1*x[:, 1] + 0.7*x[:, 3] - 0.4*x[:, 5]\ny = (logit_that + 0.25*torch.randn(512) > 0).float()\n\nbase = nn.Sequential(nn.Linear(8, 16), nn.ReLU(), nn.Linear(16, 1))\ninitial = copy.deepcopy(base.state_dict())\n\ndef chay(tao_optimizer):\n    model = nn.Sequential(nn.Linear(8, 16), nn.ReLU(), nn.Linear(16, 1))\n    model.load_state_dict(initial)\n    opt = tao_optimizer(model.parameters())\n    losses = []\n    for _ in range(80):\n        opt.zero_grad(set_to_none=True)\n        logits = model(x).squeeze(-1)\n        loss = nn.functional.binary_cross_entropy_with_logits(logits, y)\n        loss.backward()\n        opt.step()\n        losses.append(loss.item())\n    return losses[-1]\n\nket_qua = {\n    \"SGD\": chay(lambda p: torch.optim.SGD(p, lr=0.05)),\n    \"Momentum\": chay(lambda p: torch.optim.SGD(p, lr=0.05, momentum=0.9)),\n    \"Adam\": chay(lambda p: torch.optim.Adam(p, lr=0.01)),\n}\nprint(ket_qua)\nassert all(torch.isfinite(torch.tensor(v)) for v in ket_qua.values())\n```\n\nLearning rate khác vì update scale khác. Đây chỉ là smoke experiment trên train full-batch tổng hợp, không chứng minh generalization. Muốn kết luận phải thêm validation theo thời gian và nhiều seed.\n\n### Kiểm state khi resume\n\n```python\nmodel = nn.Linear(8, 1)\nopt = torch.optim.AdamW(model.parameters(), lr=1e-3)\ndummy_x = torch.randn(4, 8)\ndummy_y = torch.randn(4, 1)\n\nopt.zero_grad(set_to_none=True)\nloss = ((model(dummy_x) - dummy_y) ** 2).mean()\nloss.backward()\nopt.step()\n\ncheckpoint = {\"model\": model.state_dict(), \"optimizer\": opt.state_dict()}\nassert len(checkpoint[\"optimizer\"][\"state\"]) > 0\nprint(\"đã có state Adam cho\", len(checkpoint[\"optimizer\"][\"state\"]), \"tensor tham số\")\n```\n\nLưu mỗi model đủ cho inference; resume huấn luyện cần cả optimizer, scheduler, scaler mixed precision, epoch/step và trạng thái ngẫu nhiên nếu cần tái lập chặt.",

so_sanh: "### SGD, Momentum và Adam\n\n| Thuộc tính | SGD | SGD + Momentum | Adam/AdamW |\n|---|---|---|---|\n| State mỗi tham số | gần 0 | 1 buffer vận tốc | 2 buffer m và v |\n| Xử lý nhiễu | không làm mượt lịch sử | làm mượt theo hướng | làm mượt và chuẩn hóa từng phần tử |\n| Nhạy learning rate | cao | cao nhưng hội tụ nhanh hơn khi tune | thường dễ có baseline |\n| Gradient thưa/scale lệch | yếu | vừa | mạnh |\n| Bộ nhớ optimizer | thấp nhất | trung bình | cao nhất |\n| Generalization | đôi khi rất tốt | recipe phổ biến cho vision | không mặc định thắng cuối |\n\n### Resume và fine-tune\n\n| Tình huống | Model weights | Optimizer state | Lý do |\n|---|---|---|---|\n| tiếp tục job bị ngắt | tải | tải | giữ đúng vận tốc/moment và bước |\n| inference | tải | không cần | không cập nhật tham số |\n| fine-tune nhiệm vụ mới | tải backbone | thường tạo mới | gradient và head đã đổi |\n| tái lập chính xác | tải | tải, kèm scheduler/RNG | state ngoài model ảnh hưởng quỹ đạo |\n\nAdamW không “nâng cấp tuyệt đối” của SGD; nó đổi chi phí và inductive behavior. So sánh phải gắn với validation, thời gian và bộ nhớ.",

loi_thuong_gap: [
  "**Dùng cùng learning rate để so mọi optimizer.** Scale update khác nên kết luận thiên lệch; sweep vùng hợp lý riêng và giữ cùng budget.",
  "**Gọi `step()` trước `backward()` hoặc quên `step()`.** Tham số không đổi hoặc dùng gradient cũ; snapshot một tham số trước/sau để kiểm.",
  "**Quên `zero_grad`.** Gradient cộng qua batch ngoài ý muốn; norm tăng bất thường và kết quả phụ thuộc số iteration.",
  "**Resume chỉ model, bỏ optimizer state.** Loss có thể nhảy vì Momentum/Adam mất lịch sử; checkpoint resume phải chứa state dict liên quan.",
  "**Áp weight decay Adam như thể luôn giống L2.** Adaptive scaling phá tương đương; dùng AdamW khi muốn decay tách rời và ghi rõ parameter groups.",
  "**Clip gradient trước khi unscale trong mixed precision.** Ngưỡng áp lên gradient đã scale; unscale rồi mới đo/clip.",
  "**Thấy NaN liền giảm learning rate mà không khoanh vùng.** Nếu batch đã NaN thì optimizer vô tội; kiểm finite theo từng ranh giới.",
  "**Chọn optimizer bằng train loss của một seed.** Kết luận không phản ánh tổng quát hóa và độ biến thiên; dùng validation, nhiều seed và cùng số bước."
],

tu_kiem_tra: [
  "Dùng khai triển cục bộ giải thích vì sao SGD trừ gradient và vì sao lý luận có thể hỏng khi learning rate lớn.",
  "Trong khe loss dài, Momentum triệt rung ngang và tăng tốc dọc bằng cơ chế nào?",
  "Giải thích vai trò riêng của m, v, bias correction và ε trong Adam; phần nào làm bước thích nghi theo tham số?",
  "Model có P tham số float32. Ước lượng buffer optimizer của SGD, Momentum và Adam theo số phần tử, chưa tính gradient.",
  "Vì sao L2 penalty và weight decay không còn tương đương đơn giản dưới Adam? AdamW tách thao tác nào?",
  "Thiết kế thí nghiệm so optimizer công bằng cho dữ liệu nhà kính theo thời gian: giữ gì cố định, tune gì riêng, báo metric nào?",
  "Một batch có gradient zero nhưng Momentum buffer khác zero. Tham số có thể vẫn đổi không? Suy luận từ công thức."
],

tai_nguyen: [
  "\"Deep Learning\" — Goodfellow, Bengio, Courville, chương Optimization for Training Deep Models.",
  "\"Dive into Deep Learning\" — phần Minibatch SGD, Momentum, AdaGrad, RMSProp và Adam.",
  "PyTorch documentation — `torch.optim.SGD`, `Adam`, `AdamW` và Learning Rate Scheduler.",
  "Khóa “Improving Deep Neural Networks” — optimization algorithms, learning-rate decay và practical tuning."
],

lien_he: [
  "Nối sang mảng Tối ưu toán học: SGD dùng gradient bậc nhất, Momentum thêm trạng thái động lực, Adam dùng thống kê gradient chứ không phải Hessian đầy đủ.",
  "Nối sang mảng Hệ thống phân tán: optimizer state và gradient tạo phần lớn memory/communication cost; sharding và accumulation xuất phát từ ràng buộc này.",
  "Nối sang mảng MLOps: checkpoint resume phải version cả optimizer, scheduler và scaler; monitoring cần phát hiện loss/gradient/parameter không hữu hạn.",
  "Nối sang mảng Thực nghiệm: so optimizer là controlled experiment với nhiều seed và validation cố định, không phải chọn đường loss đẹp nhất sau khi nhìn test."
]

};
