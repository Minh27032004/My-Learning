window.LESSONS["05:thiet-ke-api:phan-trang-loc-sap-xep"] = {

tom_tat: [
  "Phân trang là hợp đồng về thứ tự và tính tiếp nối, không chỉ là `LIMIT`; nếu thứ tự không ổn định, bản ghi sẽ bị lặp hoặc mất khi dữ liệu thay đổi.",
  "Offset dễ hiểu và nhảy trang được nhưng chậm ở trang sâu, dễ lệch khi có insert/delete. Cursor hiệu quả và ổn định hơn cho luồng liên tục nhưng không nhảy tùy ý.",
  "Lọc và sắp xếp phải dùng allowlist ánh xạ sang biểu thức server kiểm soát; tuyệt đối không nối tên cột hoặc SQL từ query string.",
  "Mọi thứ tự cần khóa phá hòa duy nhất, chẳng hạn `(created_at DESC, id DESC)`, để cursor xác định chính xác ranh giới.",
  "API phải giới hạn `page_size`, độ phức tạp bộ lọc và trường sort nhằm bảo vệ database trước request hợp lệ về cú pháp nhưng đắt đỏ."
],

can_biet_truoc: [
  "SQL `WHERE`, `ORDER BY`, `LIMIT`, `OFFSET` và khái niệm index ghép.",
  "Query parameter của HTTP và cách FastAPI kiểm kiểu, giới hạn bằng `Query`.",
  "Thứ tự ổn định, khóa duy nhất và so sánh tuple theo từ điển.",
  "Base64 chỉ là encoding có thể đảo ngược, không phải mã hóa hay chữ ký."
],

dinh_nghia: "**Phân trang** chia một collection có thứ tự thành những lát kết quả có kích thước giới hạn và cung cấp thông tin để lấy lát kế tiếp. **Lọc** chọn các phần tử thỏa điều kiện; **sắp xếp** xác định thứ tự toàn phần trước khi cắt trang.\n\nOffset pagination định vị bằng số phần tử bỏ qua (`offset`, `page`). Cursor pagination định vị bằng giá trị khóa ở ranh giới cuối trang trước, thường gọi keyset pagination ở tầng database. Cursor tốt phải gắn với thứ tự, bộ lọc và hướng duyệt; nó là token tiếp tục một truy vấn cụ thể, không phải số trang được che bằng Base64.",

vi_sao_can: "Không thể trả hàng triệu đơn trong một response: RAM, thời gian serialize, băng thông và thời gian client render đều tăng. Giới hạn kích thước còn là hàng rào chống một request hút cạn connection pool. Nhưng cắt danh sách tùy tiện lại tạo lỗi âm thầm: nhân viên cuộn qua đơn mới nhất trong lúc đơn mới được thêm, rồi thấy một đơn hai lần và bỏ sót đơn khác.\n\nDatabase cũng không “bỏ qua 500.000 dòng miễn phí”. Với offset sâu, nó thường phải đi qua nhiều entry rồi mới vứt chúng. Keyset dùng điều kiện trên khóa đã có index để tiếp tục gần vị trí trước. Đánh đổi là không nhảy thẳng trang 937 và cursor phức tạp hơn.\n\nLọc/sort là bề mặt bảo mật. Cho client truyền `sort=whatever` rồi nối vào SQL tạo injection; cho lọc trên trường không có index tạo truy vấn chậm có thể thành denial of service. Thiết kế phải đồng thời phục vụ sản phẩm, tính đúng đắn và ngân sách tài nguyên.",

co_che: "### Thứ tự phải là toàn phần trước khi nói đến trang\n\n`ORDER BY created_at DESC` chưa đủ vì nhiều đơn có cùng timestamp. Database được phép trả nhóm bằng nhau theo thứ tự bất kỳ, nên bản ghi có thể đổi trang giữa hai request. Thêm khóa duy nhất: `ORDER BY created_at DESC, id DESC`. Cặp này tạo thứ tự toàn phần nếu `id` duy nhất.\n\nThứ tự mặc định phải được tài liệu hóa; không có `ORDER BY` thì thứ tự SQL không được đảm bảo. Dù hôm nay trông giống khóa chính tăng dần, query plan hoặc vacuum có thể làm nó đổi. Cursor phải chứa cả `created_at` và `id`; chỉ chứa timestamp sẽ không biết đã đi qua phần tử nào trong nhóm hòa.\n\n### Offset: đơn giản nhưng mang ảnh chụp không ổn định\n\nRequest `?page=3&page_size=20` quy ra `OFFSET 40 LIMIT 20`. Nó dễ hiển thị số trang, nhảy tới cuối và phù hợp back-office nhỏ. Tổng số `COUNT(*)` cho phép UI vẽ “trang 3/42”, nhưng count có filter phức tạp có thể đắt.\n\nGiả sử trang 1 có đơn theo thời gian `[100..81]`. Một đơn mới được chèn lên đầu trước khi lấy `OFFSET 20`; đơn 81 dịch sang vị trí 21 và xuất hiện lại. Nếu một bản ghi đầu danh sách bị xóa, phần tử ở ranh giới có thể bị bỏ qua. Transaction snapshot có thể giữ nhất quán, nhưng giữ transaction lâu qua nhiều HTTP request không thực tế.\n\n### Cursor/keyset tiếp tục sau khóa cuối\n\nVới thứ tự giảm dần `(created_at, id)`, sau bản ghi cuối `(T, I)`, trang kế dùng điều kiện:\n\n```sql\nWHERE customer_id = :customer_id\n  AND (created_at, id) < (:last_created_at, :last_id)\nORDER BY created_at DESC, id DESC\nLIMIT :limit_plus_one\n```\n\nIndex `(customer_id, created_at DESC, id DESC)` cho database tìm ranh giới thay vì đếm rồi bỏ hàng loạt. Lấy `page_size + 1`: nếu có phần tử dư thì `has_more=true`, bỏ phần tử dư khỏi response và tạo cursor từ phần tử cuối thực trả. Cách này tránh một query count chỉ để biết còn trang không.\n\n### Cursor là trạng thái truy vấn được đóng gói\n\nCursor nên chứa version schema, khóa sort, chiều và dấu vết filter. Ví dụ payload nội bộ: `{\"v\":1,\"created_at\":\"...\",\"id\":\"ord_7F3\",\"filter_hash\":\"...\"}`. Base64url giúp truyền qua URL, nhưng ai cũng giải mã và sửa được. Nếu token chứa thông tin không muốn client thay, ký HMAC; nếu chứa dữ liệu nhạy cảm, cần mã hóa hoặc tốt hơn là không chứa nó.\n\nServer phải từ chối cursor dùng với filter khác. Nếu trang đầu lọc `status=paid`, trang sau đổi thành `status=shipped` nhưng giữ cursor, ranh giới không còn nghĩa. Có thể đưa filter chuẩn hóa vào chữ ký hoặc hash. Đặt version giúp thay format cursor mà vẫn phát hiện token cũ.\n\n### Forward và backward không đối xứng một cách ngây thơ\n\nDuyệt tiếp theo thứ tự giảm dùng `<`; duyệt ngược thường đảo comparator và `ORDER BY`, lấy kết quả rồi đảo lại trước response. Nếu chỉ đổi `<` thành `>` mà giữ sort, client nhận thứ tự sai. Thiết kế ban đầu chỉ cần `next_cursor` nếu sản phẩm là infinite scroll; thêm previous chỉ khi thật sự cần vì test matrix tăng mạnh.\n\nCursor có thể hết hạn nếu hệ thống không cam kết giữ dữ liệu hoặc format mãi. Response lỗi phải phân biệt token sai chữ ký, hết hạn và không còn hỗ trợ mà không tiết lộ khóa ký. Không ghi toàn cursor vào log nếu nó chứa filter người dùng; log fingerprint rút gọn đủ chẩn đoán.\n\n### Cú pháp lọc nên nhỏ và có kiểu\n\nAPI đơn hàng có thể hỗ trợ `status`, `created_from`, `created_to`, `min_total`, `book_id`. Mỗi trường được parse thành kiểu cụ thể và ánh xạ vào biểu thức đã viết sẵn. Không nhận một ngôn ngữ SQL thu nhỏ nếu đội ngũ chưa sẵn sàng xây parser, authorization theo trường, cost model và error reporting.\n\nXác định AND/OR rõ ràng. `status=paid&status=shipped` có thể là OR; các loại filter khác kết hợp AND. Khoảng ngày kiểm `from ≤ to`; giới hạn độ rộng nếu query lịch sử nhiều năm quá đắt. Chuỗi tìm kiếm phải giới hạn chiều dài, normalize có chủ ý và escape wildcard nếu dùng `LIKE`.\n\n### Sort dùng allowlist, không dùng tên cột trực tiếp\n\nCho `sort=-created_at,total_amount` là giao diện tiện, nhưng server ánh xạ `created_at` → `Order.created_at`, không `text(query_value)`. Chỉ cho trường có semantics và index phù hợp. Tie-breaker `id` được tự thêm nếu client không nêu.\n\n```python\nSORTS = {\n    \"created_at\": Order.created_at,\n    \"total_amount\": Order.total_amount,\n}\n# Sai: order_by(text(user_supplied))\n# Đúng: column = SORTS.get(name); từ chối nếu không có.\n```\n\nKhông cho sort theo trường mà người gọi không được thấy; timing và thứ tự có thể rò thông tin. Chẳng hạn khách không được sort theo `fraud_score` dù response không trả score, vì họ có thể suy luận đánh giá nội bộ.\n\n### Index phải đi theo pattern truy vấn\n\nIndex ghép không phải danh sách thần kỳ. Với truy vấn ownership + trạng thái + thứ tự, có thể cần `(customer_id, status, created_at DESC, id DESC)`. Nếu `status` tùy chọn, một index chưa chắc phục vụ tối ưu mọi biến thể; xem execution plan và tần suất thật.\n\nNhiều sort option đồng nghĩa nhiều index, làm write chậm và tốn đĩa. Vì vậy UI “sort theo mọi cột” có chi phí backend. Đôi khi chỉ hỗ trợ mới nhất/cũ nhất là lựa chọn sản phẩm tốt. Search text phức tạp có thể chuyển sang search engine, nhưng phải chấp nhận eventual consistency.\n\n### Tổng số phần tử là một tính năng có giá\n\n`total_count` nghe vô hại nhưng có thể buộc database quét nhiều. Với cursor infinite scroll, `has_more` thường đủ. Admin báo cáo cần tổng chính xác có thể chạy query riêng, cache hoặc job tiền xử lý. Có thể trả ước lượng nhưng phải gọi đúng là estimate, không đặt tên `total` khiến client tưởng chính xác.\n\nKhi filter thay đổi liên tục, cache count ít hiệu quả. Hãy hỏi UI thực sự cần “1.283.441 kết quả” hay chỉ cần “hơn 1.000”. Bỏ count thường giảm đáng kể tail latency mà không làm trải nghiệm kém.\n\n### Giới hạn tài nguyên và chống lạm dụng\n\nĐặt mặc định, tối thiểu và tối đa cho `page_size`; ví dụ 50 mặc định, 200 tối đa dựa trên đo tải của hệ thống, không coi số này là chân lý chung. Reject thay vì âm thầm nâng giá trị có thể giúp client phát hiện lỗi, nhưng clamp thân thiện hơn; chọn và tài liệu hóa.\n\nGiới hạn số điều kiện, độ dài chuỗi, range thời gian và sort fields. Rate limit theo principal, đặt database statement timeout, cancel query khi client ngắt nếu stack hỗ trợ. Một request đã xác thực vẫn có thể vô tình hoặc cố ý tạo tải lớn.\n\n### Dữ liệu thay đổi và semantics nhất quán\n\nKeyset tránh phần lớn duplicate do insert trước ranh giới, nhưng không tạo snapshot bất biến. Nếu một đơn cũ được cập nhật `created_at` hay khóa sort, nó có thể di chuyển. Chọn sort theo trường bất biến như `created_at` và `id`; nếu cần snapshot tuyệt đối, cursor có thể mang `as_of` và query dữ liệu versioned, đổi lấy lưu trữ/phức tạp.\n\nXóa bản ghi tại ranh giới không làm cursor hỏng nếu cursor chứa giá trị khóa, không chỉ ID cần tra lại. Với soft delete, phải giữ filter `deleted_at IS NULL` nhất quán qua các trang. Quyền thay đổi giữa chừng phải được áp dụng lại ở mỗi request; cursor không phải vé vượt authorization.\n\n### Ca biên phải có quyết định rõ\n\nCollection rỗng trả `items: []`, `next_cursor: null`. `page_size=0`, âm hoặc quá lớn bị validation. Cursor hỏng không nên biến thành `500`; trả lỗi client có code ổn định. Filter không có kết quả vẫn là `200`, không phải `404`.\n\nGiá trị `null` cần semantics riêng: `shipped_at=null` là lọc chưa giao hay bỏ filter? Unicode trong tìm kiếm cần normalize thống nhất. Timestamp phải có timezone. Khi hai dòng giống mọi khóa sort trừ ID, tie-breaker đảm bảo không mất. Đây là những nơi test property sinh dữ liệu ngẫu nhiên có giá trị hơn vài happy path.\n\n### Khi nào chọn offset, cursor hoặc export\n\nOffset hợp danh sách nhỏ, tương đối tĩnh và cần nhảy trang. Cursor hợp feed lớn, thay đổi thường xuyên, chủ yếu next/previous. Export hàng trăm nghìn dòng không nên giả làm `page_size=100000`; tạo asynchronous export job, kiểm quyền, lưu file có hạn và audit lượt tải.\n\nKhông cần cursor cho bảng cấu hình 30 dòng. Độ phức tạp encode/sign/test không đem lại lợi ích. Ngược lại, offset cho bảng đơn hàng tăng nhanh sẽ tạo vấn đề đúng lúc traffic lớn nhất. Chọn dựa vào độ lớn, pattern điều hướng và mức biến động.",

vi_du: "### FastAPI cursor trong bộ nhớ để thấy đúng thuật toán\n\nVí dụ chạy được và ký cursor bằng HMAC. Production thay list bằng query keyset và lấy secret từ secret manager.\n\n```python\nimport base64, hashlib, hmac, json\nfrom datetime import datetime, timezone\nfrom fastapi import FastAPI, HTTPException, Query\nfrom pydantic import BaseModel\n\napp = FastAPI()\nSECRET = b\"demo-only-change-me\"\nORDERS = [\n {\"id\":\"ord_03\",\"status\":\"paid\",\"created_at\":\"2026-09-01T03:00:00+00:00\"},\n {\"id\":\"ord_02\",\"status\":\"paid\",\"created_at\":\"2026-09-01T02:00:00+00:00\"},\n {\"id\":\"ord_01\",\"status\":\"draft\",\"created_at\":\"2026-09-01T01:00:00+00:00\"},\n]\n\ndef encode_cursor(key: tuple[str, str], status: str | None) -> str:\n    raw = json.dumps({\"v\":1,\"key\":key,\"status\":status}, separators=(\",\", \":\")).encode()\n    sig = hmac.new(SECRET, raw, hashlib.sha256).digest()\n    return base64.urlsafe_b64encode(raw + sig).decode().rstrip(\"=\")\n\ndef decode_cursor(token: str, status: str | None) -> tuple[str, str]:\n    try:\n        blob = base64.urlsafe_b64decode(token + \"=\" * (-len(token) % 4))\n        raw, sig = blob[:-32], blob[-32:]\n        if not hmac.compare_digest(sig, hmac.new(SECRET, raw, hashlib.sha256).digest()):\n            raise ValueError(\"signature\")\n        data = json.loads(raw)\n        if data[\"v\"] != 1 or data[\"status\"] != status:\n            raise ValueError(\"context\")\n        return tuple(data[\"key\"])\n    except (ValueError, KeyError, json.JSONDecodeError) as exc:\n        raise HTTPException(400, detail=\"invalid cursor\") from exc\n\n@app.get(\"/orders\")\ndef list_orders(status: str | None = None, page_size: int = Query(2, ge=1, le=100),\n                cursor: str | None = None):\n    rows = [o for o in ORDERS if status is None or o[\"status\"] == status]\n    rows.sort(key=lambda o: (o[\"created_at\"], o[\"id\"]), reverse=True)\n    if cursor:\n        boundary = decode_cursor(cursor, status)\n        rows = [o for o in rows if (o[\"created_at\"], o[\"id\"]) < boundary]\n    window = rows[:page_size + 1]\n    items = window[:page_size]\n    next_cursor = None\n    if len(window) > page_size:\n        last = items[-1]\n        next_cursor = encode_cursor((last[\"created_at\"], last[\"id\"]), status)\n    return {\"items\": items, \"next_cursor\": next_cursor, \"has_more\": next_cursor is not None}\n```\n\n```bash\ncurl 'http://127.0.0.1:8000/orders?page_size=1&status=paid'\n# Lấy next_cursor từ JSON rồi truyền đúng status ở request sau.\n```\n\nHMAC ngăn sửa token nhưng không che payload. Secret hard-code chỉ dành cho demo. Khi xoay khóa production, cursor ngắn hạn có thể hết hiệu lực hoặc token mang key ID để server xác minh bằng khóa cũ trong cửa sổ chuyển tiếp.\n\n### Test ranh giới hòa và filter đổi\n\nThêm hai đơn cùng `created_at`, duyệt với `page_size=1`, gom ID và khẳng định không trùng, không thiếu. Sau đó dùng cursor của `status=paid` với `status=draft` và mong `400`. Đây là ca bắt được lỗi mà test ba bản ghi có timestamp khác nhau không bao giờ thấy.",

so_sanh: "### Offset, cursor và asynchronous export\n\n| Tiêu chí | Offset/page | Cursor/keyset | Export job |\n|---|---|---|---|\n| Nhảy trang bất kỳ | tốt | kém | không áp dụng |\n| Trang sâu | thường chậm | gần ổn định nếu có index | chạy nền |\n| Dữ liệu chèn/xóa liên tục | dễ lặp/mất | ổn định hơn | snapshot theo job |\n| Hiển thị tổng trang | tự nhiên nhưng count có giá | không tự nhiên | báo tiến độ |\n| Độ phức tạp API | thấp | vừa/cao | cao, cần lưu file/audit |\n| Trường hợp | bảng admin nhỏ | feed đơn hàng | tải dữ liệu hàng loạt |\n\nCursor không luôn “tốt hơn”. Trang kết quả tìm kiếm nhỏ cần nhảy tới trang cuối có thể dùng offset rất hợp lý. Export không thay pagination cho UI; nó giải quyết nhu cầu khác và phải chống rò file, URL tải cần hết hạn.\n\n**Khi không nên cho lọc tùy ý:** dataset nhạy cảm, query khó dự báo hoặc UI chỉ có ba lựa chọn cố định. Ba parameter có kiểu thường an toàn, dễ index và dễ tài liệu hơn một DSL mạnh nhưng không có cost control.",

loi_thuong_gap: [
  "**Không có `ORDER BY` hoặc thiếu tie-breaker.** Chạy lặp thấy phần tử đổi chỗ/qua trang; thêm khóa duy nhất vào thứ tự và cursor.",
  "**Offset rất sâu.** Latency tăng theo số trang dù page size giữ nguyên; đo execution plan và chuyển keyset nếu pattern là cuộn tiếp.",
  "**Nối `sort` vào SQL.** Payload lạ gây syntax error hoặc injection; dùng allowlist ánh xạ sang column object.",
  "**Base64 cursor được coi là bí mật.** Client sửa khóa/filter thành công; ký token và vẫn áp dụng authorization mỗi request.",
  "**Cursor không gắn filter.** Đổi status giữa hai trang tạo kết quả vô nghĩa; ký hash hoặc payload filter chuẩn hóa.",
  "**Luôn chạy count chính xác.** Endpoint chậm dù chỉ cần 20 dòng; dùng `limit + 1`, estimate hoặc job riêng.",
  "**Không giới hạn page size/range.** Một request hợp lệ làm cạn RAM hoặc connection; validation, timeout và rate limit.",
  "**Dùng cursor như quyền truy cập.** Người khác có token đọc được dữ liệu; kiểm principal và ownership lại từ đầu."
],

tu_kiem_tra: [
  "Vì sao `(created_at)` không đủ làm cursor nếu timestamp có thể trùng? Viết điều kiện keyset đầy đủ.",
  "Mô tả kịch bản insert khiến offset trả lặp một đơn giữa trang 1 và 2.",
  "Base64url, HMAC và encryption giải quyết ba vấn đề khác nhau thế nào?",
  "Tại sao `COUNT(*)` có thể chiếm phần lớn latency dù response chỉ trả 20 items?",
  "Bạn cho phép sort theo `fraud_score` nhưng không trả trường đó. Điều gì vẫn có thể bị rò?",
  "Thiết kế cursor hỗ trợ xoay khóa mà không giữ mọi khóa cũ vĩnh viễn.",
  "Khi nào snapshot tuyệt đối đáng chi phí hơn semantics “tiếp tục ổn định trước insert mới”?"
],

tai_nguyen: [
  "Sách **Designing Data-Intensive Applications** — Martin Kleppmann.",
  "Sách **SQL Performance Explained** — Markus Winand.",
  "Tài liệu chính thức **FastAPI**, phần Query Parameters và Security.",
  "Tài liệu của hệ quản trị cơ sở dữ liệu đang dùng về composite index và execution plan."
],

lien_he: [
  "Nối sang mảng **Cơ sở dữ liệu**: thứ tự keyset quyết định index ghép và cách đọc execution plan.",
  "Nối sang mảng **Mật mã ứng dụng**: HMAC bảo vệ tính toàn vẹn cursor, còn mã hóa giải quyết tính bí mật.",
  "Nối sang mảng **Hiệu năng**: page size, count và filter là bài toán tail latency, tải và backpressure.",
  "Nối sang mảng **Kiểm thử**: property test với khóa trùng, insert/delete giữa trang phát hiện lỗi pagination tốt hơn happy path."
]

};
