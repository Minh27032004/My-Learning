window.LESSONS["04:sql:self-join-va-cross-join"] = {

tom_tat: [
  "Self join không phải loại JOIN mới: đó là cùng một bảng xuất hiện nhiều vai trò thông qua alias, điển hình nhân viên–quản lý.",
  "Một self join chỉ đi một cạnh quan hệ; muốn đi sâu tùy ý qua cây tổ chức cần recursive CTE, không nên nối alias đến một độ sâu đoán trước.",
  "`CROSS JOIN` tạo mọi tổ hợp giữa hai nguồn, có cardinality |A| × |B|; đây có thể là chủ ý tốt hoặc một vụ nổ dữ liệu.",
  "Cross join phù hợp để dựng lưới ngày × sự kiện, biến thể × mức giá hoặc bộ ca kiểm thử, nhưng phải ước lượng số dòng trước.",
  "Self join và cross join đều buộc người viết đặt alias rõ, hiểu grain kết quả và kiểm soát cặp trùng, cặp chính nó cùng dữ liệu thiếu."
],

can_biet_truoc: [
  "INNER/LEFT JOIN, điều kiện `ON`, cardinality và sự khác nhau giữa filter trong ON với WHERE.",
  "Khóa chính, self-referencing foreign key và giá trị NULL trong quan hệ tùy chọn.",
  "Phép tích Descartes và công thức nhân số phần tử của hai tập hữu hạn.",
  "Khái niệm cây, cha–con và chu trình để hiểu giới hạn của mô hình quản lý nhân viên."
],

dinh_nghia: "**Self join** là một bảng được tham chiếu từ hai lần trở lên trong cùng query, mỗi lần mang alias và vai trò logic riêng. **Cross join** là phép ghép không có điều kiện: mỗi dòng bên trái kết hợp với mọi dòng bên phải.\n\n```sql\nSELECT e.full_name AS employee, m.full_name AS manager\nFROM employees e\nLEFT JOIN employees m ON m.employee_id = e.manager_id;\n\nSELECT e.event_name, t.tier_name\nFROM events e\nCROSS JOIN price_tiers t;\n```\n\nCâu đầu có điều kiện và có thể không tìm thấy manager; câu sau cố ý sinh toàn bộ ma trận sự kiện–hạng vé. Self join mô tả **vai trò**; cross join mô tả **cardinality mọi tổ hợp**. Chúng có thể kết hợp nhưng không đồng nghĩa.",

vi_sao_can: "Không phải mọi quan hệ nối hai bảng khác tên. Nhân viên quản lý nhân viên, danh mục có danh mục cha, sự kiện có sự kiện thay thế, thành phố có tuyến tới thành phố. Tách thành hai bảng “manager” và “employee” sẽ sao chép cùng loại thực thể, làm một người thăng chức phải chuyển nơi lưu. Self-referencing foreign key giữ một nguồn sự thật và self join diễn giải hai vai trò khi đọc.\n\nỞ hướng ngược lại, đôi lúc dữ liệu cần những dòng **chưa tồn tại**: dashboard phải hiện cả ngày không bán vé, cấu hình cần mọi cặp sự kiện–hạng giá trước khi nhập giá, test cần mọi trình duyệt–locale. Cross join tạo không gian khả năng rồi left join dữ liệu thực tế vào. Nếu chỉ group các giao dịch đang có, ngày 0 doanh thu biến mất; phép tích với calendar phục hồi chúng.\n\nHai kỹ thuật này dễ bị lạm dụng. Một dấu phẩy quên điều kiện có thể biến 100.000 × 100.000 thành 10¹⁰ cặp. Nối self join ba tầng cố định làm mất người ở tầng bốn. Bài học chính là kiểm soát hình dạng kết quả trước khi chạy, không phải thuộc cú pháp ngắn.",

co_che: "### Mô hình nhân viên tự tham chiếu\n\n```sql\nCREATE TABLE employees (\n  employee_id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  full_name text NOT NULL,\n  manager_id bigint REFERENCES employees(employee_id),\n  department text NOT NULL,\n  hired_at date NOT NULL,\n  CHECK (manager_id IS NULL OR manager_id <> employee_id)\n);\nCREATE INDEX employees_manager_id_idx ON employees(manager_id);\n```\n\nCEO có `manager_id NULL`. Foreign key bảo đảm manager tồn tại khi có ID, nhưng không tự ngăn chu trình dài A→B→C→A. CHECK chỉ chặn tự quản lý trực tiếp. Ngăn chu trình xuyên nhiều dòng cần logic ghi cẩn thận, thường bằng trigger/recursive query và transaction; bài trigger sau sẽ trở lại đánh đổi này.\n\n### Alias biến một bảng thành hai vai trò\n\n```sql\nSELECT e.employee_id,\n       e.full_name AS employee_name,\n       m.full_name AS manager_name\nFROM employees AS e\nLEFT JOIN employees AS m\n  ON m.employee_id = e.manager_id;\n```\n\nAlias không tạo bản sao vật lý. Nó tạo hai range variables để parser biết `employee_id` thuộc vai trò nào. Không qualify cột chung sẽ báo ambiguous. Tên `e`, `m` ngắn nhưng có nghĩa trong query nhỏ; với nhiều tầng, `employee`, `direct_manager`, `grand_manager` đọc an toàn hơn `a`, `b`, `c`.\n\nLEFT JOIN giữ CEO. INNER JOIN loại mọi nhân viên không manager; điều đó có thể đúng khi cần “các quan hệ báo cáo”, nhưng sai cho sơ đồ toàn công ty. Một lần nữa, loại JOIN phụ thuộc quần thể cần bảo toàn.\n\n### Một self join đi đúng một cạnh\n\nMuốn thêm quản lý cấp hai, có thể nối lần nữa:\n\n```sql\nSELECT e.full_name AS employee,\n       m.full_name AS manager,\n       gm.full_name AS grand_manager\nFROM employees e\nLEFT JOIN employees m  ON m.employee_id = e.manager_id\nLEFT JOIN employees gm ON gm.employee_id = m.manager_id;\n```\n\nQuery trả tối đa hai cấp, không “duyệt cây”. Thêm bảy alias vì công ty hiện có bảy tầng đóng băng giả định vào SQL, cột kết quả thưa và hỏng khi tổ chức đổi. Self join phù hợp độ sâu cố định và nhỏ: nhân viên–quản lý trực tiếp, sản phẩm–sản phẩm thay thế. Recursive CTE phù hợp đường tổ tiên dài tùy ý và phải có bảo vệ chu trình.\n\n### Tìm cặp trong cùng bảng\n\nSelf join còn dùng để tạo cặp nhân viên cùng phòng:\n\n```sql\nSELECT a.full_name, b.full_name, a.department\nFROM employees a\nJOIN employees b\n  ON b.department = a.department\n AND a.employee_id < b.employee_id;\n```\n\nĐiều kiện `<` làm hai việc: loại cặp một người với chính họ và chỉ giữ một hướng của cặp, tránh cả `(An,Bình)` lẫn `(Bình,An)`. Nếu dùng `<>`, mỗi cặp xuất hai lần. Nếu ID không phản ánh thứ tự nghiệp vụ cũng không sao; ở đây nó chỉ là canonical ordering để khử đối xứng.\n\n### Phát hiện bản ghi nghi trùng\n\n```sql\nSELECT a.customer_id, b.customer_id, a.email\nFROM customers a\nJOIN customers b\n  ON lower(a.email) = lower(b.email)\n AND a.customer_id < b.customer_id;\n```\n\nSchema mẫu có `UNIQUE(email)` phân biệt theo collation/toán tử mặc định; phép `lower` tìm trùng không phân biệt hoa thường nhưng bỏ qua NULL vì `lower(NULL) = lower(NULL)` là UNKNOWN. Với production, normalize email khi ghi hoặc dùng unique expression index nếu đó là luật thật. Self join để dò dữ liệu là công cụ chẩn đoán, không thay constraint ngăn lỗi tái diễn.\n\n### CROSS JOIN và phép nhân cardinality\n\n```sql\nCREATE TABLE price_tiers (\n  tier_name text PRIMARY KEY,\n  multiplier numeric(5,2) NOT NULL CHECK (multiplier > 0)\n);\nINSERT INTO price_tiers VALUES ('standard',1.00),('vip',1.80),('student',0.70);\n\nSELECT e.event_id, t.tier_name,\n       round(e.base_price * t.multiplier, 2) AS suggested_price\nFROM events e CROSS JOIN price_tiers t;\n```\n\n2 sự kiện × 3 hạng tạo 6 dòng. Nếu một phía rỗng, kết quả rỗng vì n × 0 = 0. Nếu mỗi phía một dòng, có một tổ hợp. `CROSS JOIN` nói thẳng rằng không có điều kiện; cú pháp `FROM events e, price_tiers t` tương đương nhưng dễ che một JOIN bị quên điều kiện, nên nên tránh trong code mới.\n\n### Lưới ngày để giữ số 0\n\n```sql\nWITH days AS (\n  SELECT d::date AS sale_day\n  FROM generate_series(DATE '2026-09-01', DATE '2026-09-07', INTERVAL '1 day') d\n), sales AS (\n  SELECT i.event_id,\n         (o.created_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date AS sale_day,\n         SUM(i.quantity) AS tickets\n  FROM orders o\n  JOIN order_items i ON i.order_id=o.order_id\n  WHERE o.status='paid'\n  GROUP BY i.event_id, sale_day\n)\nSELECT e.event_id, d.sale_day, COALESCE(s.tickets,0) AS tickets\nFROM events e\nCROSS JOIN days d\nLEFT JOIN sales s\n  ON s.event_id=e.event_id AND s.sale_day=d.sale_day\nORDER BY e.event_id, d.sale_day;\n```\n\nCross join tạo grain “một dòng mỗi event-day”; left join điền số thật nếu có; `COALESCE` đổi unmatched thành 0 vì ở đây 0 vé là diễn giải nghiệp vụ đúng. Nếu chỉ aggregate orders, những ngày im lặng không có dòng và biểu đồ dễ nối nhầm hai điểm.\n\n### CROSS JOIN LATERAL: mỗi dòng một tập phụ thuộc\n\n`LATERAL` cho nguồn bên phải tham chiếu cột bên trái. Ví dụ mỗi sự kiện tạo các ngày từ hôm nay tới ngày diễn:\n\n```sql\nSELECT e.event_id, d.day\nFROM events e\nCROSS JOIN LATERAL generate_series(\n  DATE '2026-09-01',\n  (e.starts_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date,\n  INTERVAL '1 day'\n) AS d(day);\n```\n\nĐây vẫn là phép mở rộng nhiều dòng trên mỗi event, nhưng số dòng bên phải phụ thuộc event. LATERAL mạnh cho top-N mỗi nhóm hoặc hàm sinh tập, song nested execution có thể đắt; cần nhìn plan và giới hạn miền sinh.\n\n### Ước lượng trước khi chạy\n\nNếu 50.000 sự kiện cross 365 ngày, kết quả là 18.250.000 dòng trước mọi JOIN sau. Thêm 5 tier thành 91.250.000. Công thức đơn giản này phải được ghi ra trước khi chạy production. `EXPLAIN` không thực thi cho phép xem estimate; thêm filter vào từng nguồn **trước** cross join để thu nhỏ miền.\n\n```sql\nFROM (SELECT * FROM events WHERE starts_at >= now()) e\nCROSS JOIN (SELECT * FROM price_tiers WHERE active) t\n```\n\nPlanner có thể đẩy predicate, nhưng cấu trúc trên làm chủ ý rõ. `LIMIT 100` ngoài cùng không phải giấy phép an toàn cho mọi query: executor có thể dừng sớm ở plan đơn giản, nhưng ORDER BY, aggregate hoặc join phía sau có thể buộc tạo/xử lý lượng lớn trước khi limit.\n\n### Query plan của self join\n\nIndex `employees(manager_id)` hỗ trợ hướng manager→direct reports. Hướng employee→manager dùng primary key của manager. Khi liệt kê toàn công ty, hash join có thể rẻ hơn nhiều index lookup; khi tìm reports của một manager, index scan thường hợp lý. Cùng một cặp bảng alias không buộc self join dùng thuật toán đặc biệt: planner vẫn chọn nested loop/hash/merge như JOIN bình thường.\n\nStatistics của hai alias đến từ cùng bảng nhưng điều kiện có thể tương quan, chẳng hạn manager thường cùng department. Estimate sai có thể lan qua nhiều self join. Extended statistics có thể giúp một số tương quan cột trong một bảng, song trước tiên cần kiểm `actual rows` so với estimate thay vì đoán.\n\n### Khi nào không nên dùng\n\nKhông dùng self join chuỗi dài để thay recursive CTE khi độ sâu không cố định. Không dùng cross join để “thử mọi khả năng” nếu không có cận; nhiều bài toán tổ hợp tăng theo cấp số nhân chứ không chỉ hai tập. Không tạo ma trận event-day toàn lịch sử cho mỗi request nếu chỉ cần khoảng 7 ngày; thu nhỏ range hoặc lưu calendar dùng chung. Và không dùng SQL tạo hàng triệu tổ hợp rồi lọc trong ứng dụng: filter gần nguồn để database tránh sinh dữ liệu vô ích.",

vi_du: "### Dữ liệu tổ chức có gốc và nhiều tầng\n\n```sql\nINSERT INTO employees(full_name,manager_id,department,hired_at) VALUES\n('Lan',NULL,'Executive','2020-01-10');\nINSERT INTO employees(full_name,manager_id,department,hired_at) VALUES\n('Minh',1,'Engineering','2021-03-01'),\n('Ngọc',1,'Sales','2021-06-15');\nINSERT INTO employees(full_name,manager_id,department,hired_at) VALUES\n('Phúc',2,'Engineering','2023-02-01'),\n('Quỳnh',2,'Engineering','2024-05-20'),\n('Sơn',3,'Sales','2022-08-08');\n\nSELECT e.employee_id, e.full_name,\n       COALESCE(m.full_name,'[không có quản lý]') AS manager\nFROM employees e\nLEFT JOIN employees m ON m.employee_id=e.manager_id\nORDER BY e.employee_id;\n```\n\nKết quả có sáu nhân viên; Lan không biến mất. Đổi LEFT thành INNER sẽ có năm quan hệ báo cáo. Hai số đều có thể đúng, nhưng đại diện hai grain: nhân viên và cạnh quản lý.\n\n### Cặp đồng nghiệp không lặp\n\n```sql\nSELECT a.full_name AS first_employee,\n       b.full_name AS second_employee,\n       a.department\nFROM employees a\nJOIN employees b\n  ON a.department=b.department\n AND a.employee_id < b.employee_id\nORDER BY a.department, a.employee_id, b.employee_id;\n```\n\nEngineering có Minh, Phúc, Quỳnh nên tạo 3 × 2 / 2 = 3 cặp. Sales có Ngọc, Sơn nên tạo 1 cặp. Executive chỉ Lan nên không tạo cặp. Công thức n(n−1)/2 là kiểm tra độc lập hữu ích cho query pair generation.\n\n### Bắt ca biên cấu trúc\n\n```sql\n-- CHECK chặn tự quản lý trực tiếp\nBEGIN;\nUPDATE employees SET manager_id=employee_id WHERE employee_id=4;\nROLLBACK;\n\n-- Hai người khác phòng không được ghép\nSELECT COUNT(*)\nFROM employees a JOIN employees b\n ON a.department=b.department AND a.employee_id < b.employee_id\nWHERE a.department <> b.department; -- luôn 0\n\n-- Cross join với nguồn rỗng trả 0 dòng\nSELECT COUNT(*)\nFROM events CROSS JOIN (SELECT * FROM price_tiers WHERE false) t;\n```\n\nCHECK đầu tiên sẽ làm transaction lỗi; trong psql cần `ROLLBACK` sau lỗi. Nó không chặn chu trình hai người. Có thể thử trong transaction riêng bằng cách đổi manager của Minh thành Phúc sau khi Phúc đang báo cáo Minh: foreign key vẫn hợp lệ nhưng đồ thị có chu trình. Đây là bằng chứng constraint tham chiếu không đồng nghĩa constraint cây.\n\n### Tạo ma trận giá và tìm cấu hình thiếu\n\n```sql\nCREATE TABLE event_prices(\n  event_id bigint REFERENCES events(event_id),\n  tier_name text REFERENCES price_tiers(tier_name),\n  price numeric(12,2) NOT NULL,\n  PRIMARY KEY(event_id,tier_name)\n);\n\nINSERT INTO event_prices VALUES (1,'standard',450000),(1,'vip',810000);\n\nSELECT e.event_id, t.tier_name\nFROM events e\nCROSS JOIN price_tiers t\nLEFT JOIN event_prices p\n  ON p.event_id=e.event_id AND p.tier_name=t.tier_name\nWHERE p.event_id IS NULL\nORDER BY e.event_id,t.tier_name;\n```\n\nCross join định nghĩa mọi cấu hình bắt buộc; left anti-pattern `WHERE p.event_id IS NULL` tìm lỗ hổng. Khóa chính ghép bảo đảm mỗi cấu hình tối đa một giá. Đây là ứng dụng thực tế hơn việc sinh tổ hợp chỉ để minh họa.\n\n### Quan sát cardinality bằng EXPLAIN\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT e.event_id, t.tier_name\nFROM events e CROSS JOIN price_tiers t;\n```\n\nSo `actual rows` ở hai scan với nút join. Với E event và T tier, nút trên phải có E×T dòng. Nếu estimate lệch nhiều do statistics cũ, `ANALYZE events; ANALYZE price_tiers;` cập nhật mẫu thống kê. Không chạy `EXPLAIN ANALYZE` cho phép tích khổng lồ trên production chỉ để tò mò, vì ANALYZE thực thi thật; dùng `EXPLAIN` trước để đánh giá rủi ro.",

so_sanh: "### Self join, recursive CTE và window function\n\n| Nhu cầu | Self join | Recursive CTE | Window function |\n|---|---|---|---|\n| Quản lý trực tiếp | rõ, đơn giản | dư thừa | không lần theo khóa cha |\n| Tổ tiên sâu tùy ý | phải đoán số alias | phù hợp, cần chặn cycle | không phù hợp |\n| So cặp trong cùng nhóm | phù hợp nhưng O(n²) cặp | không cần | đôi khi `lag/lead` rẻ hơn nếu chỉ hàng kề |\n| Xếp hạng trong phòng | self join đếm người tốt hơn có thể làm được nhưng nặng | không cần | phù hợp nhất |\n\n### CROSS JOIN so với JOIN có điều kiện\n\n| Tiêu chí | CROSS JOIN | INNER JOIN ... ON |\n|---|---|---|\n| Số cặp ứng viên | mọi cặp | chỉ cặp thỏa điều kiện logic |\n| Dùng để tạo miền đầy đủ | rất phù hợp | cần một quan hệ khóa |\n| Rủi ro | bùng | điều kiện sai vẫn có thể bùng |\n| Ý định đọc code | tuyên bố tích Descartes | tuyên bố quan hệ |\n\nCross join không phải “JOIN tệ”. Nó tệ khi cardinality ngoài kiểm soát; nó chính xác khi mọi tổ hợp là domain cần khảo sát.",

loi_thuong_gap: [
  "Không đặt alias cho self join; cột trở nên ambiguous. Đặt alias theo vai trò và qualify mọi khóa.",
  "Dùng `a.id <> b.id` khi cần cặp không thứ tự; mỗi cặp xuất hai hướng. Dùng `a.id < b.id`.",
  "INNER self join sơ đồ nhân sự; CEO biến mất. Dùng LEFT nếu grain là mọi nhân viên.",
  "Tin foreign key tự ngăn chu trình quản lý; dữ liệu A→B→A vẫn hợp lệ. Kiểm bằng recursive logic khi ghi hoặc định kỳ.",
  "Quên điều kiện JOIN và vô tình tạo Cartesian product; số dòng nhảy theo tích. So estimate và review `ON` trước khi chạy.",
  "Đặt LIMIT ngoài một cross join lớn rồi cho rằng luôn an toàn; sort/aggregate có thể xử lý toàn bộ trước. Dùng EXPLAIN và thu nhỏ nguồn.",
  "Sinh lưới ngày quá rộng cho mỗi request; CPU và bộ nhớ tăng vô ích. Giới hạn khoảng thời gian theo nhu cầu màn hình.",
  "Nối alias cố định để duyệt cây tùy độ sâu; người ở tầng sâu bị cắt. Chuyển sang recursive CTE có cycle guard."
],

tu_kiem_tra: [
  "Vì sao self join không phải loại JOIN thứ năm? Alias thay đổi điều gì và không thay đổi điều gì?",
  "Engineering có 5 người. Query cặp với `a.id < b.id` trả bao nhiêu dòng? Hãy suy ra bằng công thức.",
  "Foreign key `manager_id` bảo đảm những invariant nào và không bảo đảm invariant cây nào?",
  "Tại sao lưới event × day rồi LEFT JOIN sales giữ được ngày 0 vé, còn GROUP BY sales đơn thuần không giữ?",
  "50.000 event × 365 ngày × 5 tier tạo bao nhiêu dòng trước filter? Bạn sẽ kiểm plan thế nào mà chưa thực thi?",
  "Khi nào `lag/lead` hợp lý hơn self join để so dòng cùng bảng? Nêu tiêu chí “hàng kề” so với “mọi cặp”.",
  "Giải thích vì sao `LIMIT` không luôn cứu một cross join có ORDER BY tổng hợp phía ngoài."
],

tai_nguyen: [
  "PostgreSQL Documentation — Table Expressions, LATERAL Subqueries và generate_series.",
  "“SQL for Smarties” của Joe Celko — cây, cặp và truy vấn theo tập.",
  "“SQL Queries for Mere Mortals” của John L. Viescas và Michael J. Hernandez.",
  "“The Art of PostgreSQL” của Dimitri Fontaine — cách xây query phân tích và đọc plan."
],

lien_he: [
  "Nối sang mảng Cây và đồ thị: manager_id tạo danh sách cha; chu trình và đường tổ tiên là bài toán duyệt đồ thị, không được foreign key giải quyết.",
  "Nối sang mảng Tổ hợp: cross join hai tập có mn cặp, còn cặp không thứ tự trong một nhóm có n(n−1)/2 phần tử.",
  "Nối sang mảng Kiểm thử phần mềm: cross join tạo ma trận môi trường × locale × vai trò, nhưng pairwise testing có thể giảm số ca khi toàn tích quá lớn.",
  "Nối sang mảng Data visualization: calendar grid phải có cả ô 0; domain ngày đầy đủ giúp biểu đồ không diễn giải thiếu dữ liệu thành đường nối liên tục."
]

};
