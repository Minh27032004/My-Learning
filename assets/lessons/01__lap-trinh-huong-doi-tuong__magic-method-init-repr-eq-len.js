window.LESSONS["01:lap-trinh-huong-doi-tuong:magic-method-init-repr-eq-len"] = {

tom_tat: [
  "**Magic method là cách class của bạn hoà vào cú pháp Python.** `len(x)` gọi `x.__len__()`, `a + b` gọi `a.__add__(b)` — không có gì huyền bí, chỉ là quy ước tên.",
  "**`__repr__` là magic method đáng viết nhất.** Nó xuất hiện trong mọi phiên gỡ lỗi, mọi truy vết lỗi, mọi lần in một `list` object — và mặc định thì vô dụng.",
  "**Định nghĩa `__eq__` làm object mất tính hashable** trừ khi bạn định nghĩa luôn `__hash__`. Đây là bẫy im lặng: object đột nhiên không làm khoá `dict` được nữa.",
  "**Nhóm theo giao thức, đừng học thuộc danh sách**: đại diện, so sánh, toán tử, container, gọi được, quản lý ngữ cảnh. Mỗi nhóm mở ra một phần cú pháp Python.",
  "**Chỉ cài đặt khi ý nghĩa hiển nhiên.** `Vector + Vector` thì rõ; `NguoiDung + NguoiDung` thì một phương thức có tên tốt hơn nhiều."
],

can_biet_truoc: [
  "Bài **Class, object, thuộc tính** — `__init__` là magic method đầu tiên bạn gặp.",
  "Bài **Đa hình và duck typing** ngay trước — magic method chính là cách Python cài đặt duck typing ở tầng ngôn ngữ.",
  "Bài **Generator, iterator** và **Context manager** ở module 5 — `__iter__`, `__next__`, `__enter__`, `__exit__` đã gặp ở đó.",
  "Bài **List, dict, set, tuple** ở module 5 — tính hashable và quan hệ của nó với `__hash__`."
],

dinh_nghia: "Nói đời thường: Python có một **danh sách các câu hỏi ngầm** nó đặt cho mọi object: *\"anh dài bao nhiêu?\"*, *\"anh cộng được không?\"*, *\"in anh ra thì trông thế nào?\"*. Magic method là cách bạn **trả lời những câu hỏi đó** cho class của mình.\n\nNói chính xác:\n\n- **Magic method** (còn gọi *dunder method*, từ *double underscore*) — phương thức có tên dạng `__ten__`, được Python gọi **ngầm** khi gặp một cú pháp tương ứng.\n- **Giao thức** (*protocol*) — một nhóm magic method cùng phục vụ một tính năng. Ví dụ: giao thức iterator gồm `__iter__` và `__next__`.\n- **`__repr__`** — biểu diễn cho **lập trình viên**. Lý tưởng là chuỗi tạo lại được object.\n- **`__str__`** — biểu diễn cho **người dùng cuối**. Nếu không có, Python dùng `__repr__`.\n- **Hashable** — object có `__hash__` và giá trị băm không đổi. Điều kiện làm khoá `dict` hoặc phần tử `set`.\n- **`NotImplemented`** — giá trị trả về từ toán tử để nói \"tôi không xử lý được, thử cách khác\".\n\nĐiểm cần nắm ngay: **magic method không phải \"phép thuật\".** `len(x)` chỉ là đường tắt cho `type(x).__len__(x)`. Biết vậy thì mọi hành vi của chúng đều suy ra được, và không cần học thuộc.",

vi_sao_can: "Magic method là thứ làm class của bạn trở thành công dân hạng nhất trong Python:\n\n- **Chúng làm class dùng được với toàn bộ thư viện chuẩn.** Có `__iter__` là `sorted()`, `sum()`, `\", \".join()`, `for` đều chạy — bạn không viết thêm dòng nào cho chúng.\n- **`__repr__` tệ làm gỡ lỗi khổ sở.** In một `list` gồm mười object mà thấy mười dòng `<Obj at 0x7f...>` là mất toàn bộ giá trị của việc in ra.\n- **Bẫy `__eq__` và `__hash__` gây lỗi thật.** Object đang dùng làm khoá `dict`, bạn thêm `__eq__` để so sánh cho tiện, và toàn bộ phần code dùng `set` hỏng.\n- **Đọc mã nguồn thư viện đòi hỏi nó.** Mọi thư viện chất lượng cao dùng magic method rất nhiều; không quen thì không hiểu tại sao code chạy được.\n- **Định hướng AI/ML.** `Dataset` chỉ cần `__len__` và `__getitem__` là dùng được với toàn bộ hệ sinh thái học sâu — đó là giao thức, không phải kế thừa.\n\nTình huống thật rất kinh điển: bạn có class `Diem` dùng làm khoá trong một `dict` cache. Mọi thứ chạy tốt. Rồi bạn thêm `__eq__` để so sánh hai điểm cho tiện. Đột nhiên `TypeError: unhashable type: 'Diem'` xuất hiện ở một chỗ hoàn toàn khác trong dự án. Bạn không hề đụng vào chỗ đó. Phần cơ chế sẽ giải thích chính xác vì sao Python làm vậy và nó bảo vệ bạn khỏi điều gì.",

co_che: "### Magic method chỉ là quy ước tên\n\n```python\nlen(x)          →  type(x).__len__(x)\nx[0]            →  type(x).__getitem__(x, 0)\na + b           →  type(a).__add__(a, b)\nstr(x)          →  type(x).__str__(x)\nx()             →  type(x).__call__(x)\nfor i in x      →  it = type(x).__iter__(x); type(it).__next__(it)...\nwith x:         →  type(x).__enter__(x) ... type(x).__exit__(x, ...)\n```\n\nMột chi tiết quan trọng: Python tra magic method trên **class**, không trên instance.\n\n```python\nclass A: pass\na = A()\na.__len__ = lambda: 5      # gán vào INSTANCE\nlen(a)                      # TypeError — Python không tìm ở instance\n\nA.__len__ = lambda self: 5  # gán vào CLASS\nlen(a)                      # 5 ✓\n```\n\nĐây là lý do bạn không thể \"vá\" magic method cho một object riêng lẻ, và cũng là lý do hiệu năng: Python tra thẳng vào bảng của class, bỏ qua `__getattr__`.\n\n### Nhóm ① — Đại diện: `__repr__` và `__str__`\n\n```python\nclass Diem:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\nd = Diem(1, 2)\nprint(d)        # <__main__.Diem object at 0x7f8b1c0d5f10>  ← vô dụng\n[d, d]          # [<...>, <...>]                            ← còn tệ hơn\n```\n\n```python\nclass Diem:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\n    def __repr__(self) -> str:                    # ⭐ cho LẬP TRÌNH VIÊN\n        return f\"Diem({self.x!r}, {self.y!r})\"\n\n    def __str__(self) -> str:                     # cho NGƯỜI DÙNG\n        return f\"({self.x}, {self.y})\"\n\nd = Diem(1, 2)\nrepr(d)         # 'Diem(1, 2)'\nstr(d)          # '(1, 2)'\nprint(d)        # (1, 2)          — print dùng __str__\nd               # Diem(1, 2)      — REPL dùng __repr__\n[d, d]          # [Diem(1, 2), Diem(1, 2)]   ⭐ list LUÔN dùng __repr__\nf\"{d}\"          # '(1, 2)'\nf\"{d!r}\"        # 'Diem(1, 2)'\n```\n\nQuy tắc quan trọng: **`__repr__` nên là chuỗi tạo lại được object.**\n\n```python\neval(repr(d)) == d      # lý tưởng là True\n```\n\nKhông đạt được thì dùng dạng góc nhọn để báo hiệu:\n\n```python\ndef __repr__(self):\n    return f\"<KetNoi {self.host}:{self.cong} {'mở' if self.mo else 'đóng'}>\"\n```\n\n**Chỉ viết một cái thì viết `__repr__`** — vì `__str__` mặc định gọi `__repr__`, còn ngược lại thì không. Với phần lớn class, `__repr__` là đủ và `__str__` chỉ cần khi cách hiển thị cho người dùng thật sự khác.\n\nMột lý do nữa `__repr__` quan trọng hơn nhiều người nghĩ: **nó xuất hiện trong truy vết lỗi và trong thông báo của `assert`**. Bài kiểm thử thất bại mà in ra `<Obj at 0x...>` thì không nói gì cả.\n\nDùng `type(self).__name__` để lớp con tự đúng:\n\n```python\ndef __repr__(self):\n    return f\"{type(self).__name__}({self.x!r}, {self.y!r})\"\n```\n\n### Nhóm ② — So sánh, và bẫy `__hash__`\n\n```python\nclass Diem:\n    def __init__(self, x, y): self.x, self.y = x, y\n\nDiem(1, 2) == Diem(1, 2)      # False! — mặc định so sánh DANH TÍNH\n```\n\nMặc định `__eq__` là so sánh `is`, nên hai object khác nhau luôn không bằng nhau.\n\n```python\nclass Diem:\n    def __init__(self, x, y): self.x, self.y = x, y\n\n    def __eq__(self, k):\n        if not isinstance(k, Diem):\n            return NotImplemented          # ⭐ để Python thử k.__eq__(self)\n        return (self.x, self.y) == (k.x, k.y)\n\nDiem(1, 2) == Diem(1, 2)      # True ✓\n{Diem(1, 2)}                   # TypeError: unhashable type: 'Diem'  ⚠️\n```\n\n**Đây là bẫy quan trọng nhất của bài.** Định nghĩa `__eq__` làm Python **tự đặt `__hash__ = None`**.\n\nLý do rất chính đáng: `dict` và `set` dựa trên một quy tắc — **hai object bằng nhau phải có cùng hash**. Nếu bạn đổi định nghĩa \"bằng nhau\" mà giữ hash mặc định (dựa trên địa chỉ), quy tắc đó vỡ và `dict` sẽ **không tìm thấy khoá dù nó có ở đó**.\n\nPython chọn báo lỗi rõ ràng thay vì để dữ liệu biến mất âm thầm.\n\nHai cách xử lý:\n\n```python\n# ① Object BẤT BIẾN — định nghĩa __hash__ dựa trên cùng dữ liệu với __eq__\nclass Diem:\n    def __init__(self, x, y): self.x, self.y = x, y\n    def __eq__(self, k):\n        return isinstance(k, Diem) and (self.x, self.y) == (k.x, k.y)\n    def __hash__(self):\n        return hash((self.x, self.y))     # ⭐ CÙNG dữ liệu với __eq__\n\n{Diem(1, 2), Diem(1, 2)}      # {Diem(1, 2)} — một phần tử ✓\n\n# ② Object THAY ĐỔI ĐƯỢC — để nguyên, không hashable là ĐÚNG\nclass GioHang:\n    def __eq__(self, k): ...\n    # không định nghĩa __hash__ ⇒ không làm khoá dict được ⇒ an toàn\n```\n\nCách ② quan trọng: **object mutable KHÔNG NÊN hashable.** Nếu bạn cất nó vào `set` rồi sửa nó, hash đổi và nó \"biến mất\" khỏi `set` — đúng vấn đề đã nói ở bài `list`/`dict`/`set` ở module 5.\n\nSo sánh thứ tự — chỉ cần hai phương thức:\n\n```python\nfrom functools import total_ordering\n\n@total_ordering\nclass PhienBan:\n    def __init__(self, chinh, phu):\n        self.chinh, self.phu = chinh, phu\n    def __eq__(self, k):\n        return (self.chinh, self.phu) == (k.chinh, k.phu)\n    def __lt__(self, k):\n        return (self.chinh, self.phu) < (k.chinh, k.phu)\n    def __hash__(self):\n        return hash((self.chinh, self.phu))\n```\n\n`@total_ordering` sinh ra `__le__`, `__gt__`, `__ge__` từ `__eq__` và `__lt__`. Có `__lt__` là `sorted()`, `min()`, `max()` chạy được.\n\nMẹo dùng `tuple` để so sánh nhiều trường: `tuple` so sánh theo từng phần tử từ trái sang, nên `(a, b) < (c, d)` cho đúng thứ tự ưu tiên mà không cần viết `if` lồng nhau.\n\n### Nhóm ③ — Container\n\n```python\nclass BoSuuTap:\n    def __init__(self, muc=()):\n        self._muc = list(muc)\n\n    def __len__(self): return len(self._muc)\n    def __getitem__(self, i): return self._muc[i]\n    def __setitem__(self, i, v): self._muc[i] = v\n    def __delitem__(self, i): del self._muc[i]\n    def __contains__(self, x): return x in self._muc\n    def __iter__(self): return iter(self._muc)\n    def __bool__(self): return bool(self._muc)\n\nb = BoSuuTap([1, 2, 3])\nlen(b); b[0]; b[0] = 9; del b[0]; 2 in b; list(b); bool(b)\nsorted(b); sum(b); max(b); \", \".join(str(x) for x in b)\n```\n\nBảy phương thức mở ra toàn bộ cú pháp collection của Python — và mọi hàm dựng sẵn nhận iterable đều dùng được.\n\nMột chi tiết đáng biết: **chỉ cần `__getitem__` là đã lặp được**, nhờ giao thức cũ:\n\n```python\nclass ChiCoGetitem:\n    def __getitem__(self, i):\n        if i >= 3: raise IndexError\n        return i * 10\n\nlist(ChiCoGetitem())      # [0, 10, 20] — Python gọi [0], [1], [2]... tới IndexError\n```\n\nNhưng nên định nghĩa `__iter__` tường minh: nó rõ ràng hơn và hiệu quả hơn.\n\n**`__bool__` và `__len__`** có quan hệ cần biết:\n\n```\nif x:  →  gọi __bool__ nếu có\n          không có thì gọi __len__ (0 là False)\n          không có cả hai thì LUÔN True\n```\n\n```python\nclass A: pass\nbool(A())        # True — mọi object không có hai phương thức trên đều là True\n```\n\nĐây là lý do một class chứa dữ liệu **nên có `__len__`**: không có nó, `if bo_suu_tap:` luôn đúng kể cả khi rỗng — một lỗi logic im lặng.\n\n### Nhóm ④ — Toán tử\n\n```python\nclass Vector:\n    def __init__(self, x, y): self.x, self.y = x, y\n\n    def __add__(self, k):\n        if not isinstance(k, Vector): return NotImplemented\n        return Vector(self.x + k.x, self.y + k.y)\n\n    def __mul__(self, n):\n        if not isinstance(n, (int, float)): return NotImplemented\n        return Vector(self.x * n, self.y * n)\n\n    def __rmul__(self, n):                # ⭐ cho 3 * v\n        return self.__mul__(n)\n\n    def __neg__(self): return Vector(-self.x, -self.y)\n    def __abs__(self): return (self.x**2 + self.y**2) ** 0.5\n    def __repr__(self): return f\"Vector({self.x}, {self.y})\"\n\nv = Vector(1, 2) + Vector(3, 4)\nv * 3\n3 * v            # ⭐ nhờ __rmul__ — int.__mul__ không biết Vector\n-v\nabs(Vector(3, 4))    # 5.0\n```\n\nHai điểm cần hiểu:\n\n**`return NotImplemented` khi kiểu không hợp** — nó nói với Python \"tôi không xử lý được\", để Python thử phương thức phản chiếu của toán hạng kia. Trả về sai kiểu hoặc `raise` sẽ chặn cơ chế đó và cho lỗi khó hiểu.\n\n**Phương thức phản chiếu (`__radd__`, `__rmul__`)** cần cho trường hợp toán hạng trái không biết class của bạn:\n\n```\n3 * v  →  int.__mul__(3, v)   → NotImplemented\n       →  Vector.__rmul__(v, 3) → ✓\n```\n\nToán tử gán tại chỗ:\n\n```python\ndef __iadd__(self, k):        # cho v += k\n    self.x += k.x             # SỬA tại chỗ, không tạo object mới\n    self.y += k.y\n    return self               # ⭐ BẮT BUỘC return\n```\n\nKhông có `__iadd__` thì `v += k` tự động thành `v = v + k` — vẫn chạy nhưng tạo object mới. Định nghĩa nó chỉ đáng khi sửa tại chỗ rẻ hơn nhiều, ví dụ với mảng lớn.\n\n### Nhóm ⑤ — Gọi được, ngữ cảnh, thuộc tính\n\n```python\nclass BoNhan:\n    def __init__(self, he_so): self.he_so = he_so\n    def __call__(self, x):                      # ⭐ object gọi được như hàm\n        return x * self.he_so\n\ngap_doi = BoNhan(2)\ngap_doi(5)                # 10\nlist(map(gap_doi, [1, 2, 3]))     # [2, 4, 6] — dùng được ở mọi chỗ nhận hàm\n```\n\n`__call__` cho phép object **có trạng thái** mà vẫn dùng được như hàm — đây là điểm mạnh so với closure khi cần cấu hình phức tạp hoặc cần thêm phương thức phụ.\n\n```python\nclass MoTaiNguyen:\n    def __enter__(self):\n        print(\"mở\")\n        return self\n    def __exit__(self, loai, gt, tv):\n        print(\"đóng\")\n        return False              # ⭐ KHÔNG nuốt ngoại lệ\n\nwith MoTaiNguyen() as r:\n    ...\n```\n\nĐây là bài context manager ở module 5, nhìn từ góc độ magic method.\n\n```python\nclass LinhHoat:\n    def __getattr__(self, ten):           # chỉ gọi khi KHÔNG tìm thấy\n        return f\"không có {ten}\"\n\n    def __setattr__(self, ten, gt):       # gọi ở MỌI phép gán\n        print(f\"gán {ten}\")\n        super().__setattr__(ten, gt)      # ⭐ BẮT BUỘC — nếu không thì đệ quy\n```\n\n`__setattr__` là chỗ dễ gây đệ quy vô hạn nhất: viết `self.x = gt` bên trong nó sẽ gọi lại chính nó. Phải dùng `super().__setattr__` hoặc `self.__dict__[ten] = gt`.\n\n### Bảng giao thức\n\n| Giao thức | Phương thức | Mở ra cú pháp |\n|---|---|---|\n| Khởi tạo | `__init__`, `__new__` | `Lop(...)` |\n| Đại diện | `__repr__`, `__str__`, `__format__` | `repr()`, `print()`, f-string |\n| So sánh | `__eq__`, `__lt__`, `__hash__` | `==`, `<`, `sorted()`, `set` |\n| Container | `__len__`, `__getitem__`, `__contains__`, `__iter__` | `len()`, `x[i]`, `in`, `for` |\n| Toán tử | `__add__`, `__mul__`, `__neg__`, `__abs__` | `+`, `*`, `-x`, `abs()` |\n| Gọi được | `__call__` | `x()` |\n| Ngữ cảnh | `__enter__`, `__exit__` | `with` |\n| Thuộc tính | `__getattr__`, `__setattr__` | `x.y` |\n| Chân trị | `__bool__` | `if x:` |\n\nHọc theo nhóm chứ đừng học thuộc danh sách: **mỗi nhóm mở ra một phần cú pháp**, và bạn chỉ cài đặt nhóm mình cần.\n\n### Khi nào KHÔNG nên cài đặt\n\n```python\n# ❌ Ý nghĩa không hiển nhiên\nclass NguoiDung:\n    def __add__(self, k): ...       # cộng hai người dùng nghĩa là gì?\n    def __lshift__(self, k): ...    # cú pháp lạ, không ai đoán được\n\n# ✓ Phương thức có tên rõ ràng\nclass NguoiDung:\n    def gop_quyen_voi(self, k): ...\n```\n\nTiêu chí: **người đọc có đoán đúng ý nghĩa mà không cần đọc tài liệu không?**\n\n```\n✓ Vector + Vector, Tien + Tien       — hiển nhiên\n✓ len(bo_suu_tap), x in bo_suu_tap    — hiển nhiên\n✓ so_sanh để sắp xếp                  — hiển nhiên\n✗ NguoiDung + NguoiDung               — không ai đoán được\n✗ config >> file                      — cú pháp lạ\n```\n\nBa cạm bẫy nữa cần tránh:\n\n```\n· __getattr__ để chuyển tiếp mọi thứ → mất kiểm soát giao diện,\n  mypy và trình soạn thảo bó tay\n· __eq__ mà quên __hash__            → mất tính hashable âm thầm\n· Magic method có tác dụng phụ nặng  → người đọc `a + b` không ngờ nó\n  ghi database\n```\n\nDòng cuối quan trọng: cú pháp toán tử tạo cảm giác **rẻ và không có tác dụng phụ**. Nếu `a + b` gọi một API mạng thì đó là bất ngờ khó chịu; dùng phương thức có tên rõ ràng.",

vi_du: "### `__repr__` — khác biệt khi gỡ lỗi\n\n```python\n# ❌ Không có __repr__\nclass DonHang:\n    def __init__(self, ma, tong, trang_thai):\n        self.ma, self.tong, self.trang_thai = ma, tong, trang_thai\n\nds = [DonHang(\"DH1\", 100, \"moi\"), DonHang(\"DH2\", 200, \"xong\")]\nprint(ds)\n# [<__main__.DonHang object at 0x7f8b1c0d5f10>,\n#  <__main__.DonHang object at 0x7f8b1c0d5fd0>]     ← vô dụng\n\n# ✓ Có __repr__\nclass DonHang:\n    def __init__(self, ma, tong, trang_thai):\n        self.ma, self.tong, self.trang_thai = ma, tong, trang_thai\n\n    def __repr__(self):\n        return (f\"{type(self).__name__}(ma={self.ma!r}, \"\n                f\"tong={self.tong!r}, trang_thai={self.trang_thai!r})\")\n\n    def __str__(self):\n        return f\"Đơn {self.ma}: {self.tong:,.0f}đ ({self.trang_thai})\"\n\nds = [DonHang(\"DH1\", 100_000, \"moi\"), DonHang(\"DH2\", 200_000, \"xong\")]\nprint(ds)\n# [DonHang(ma='DH1', tong=100000, trang_thai='moi'),\n#  DonHang(ma='DH2', tong=200000, trang_thai='xong')]\n\nprint(ds[0])          # Đơn DH1: 100,000đ (moi)\n```\n\nBa chỗ `__repr__` phát huy tác dụng mà nhiều người không nghĩ tới:\n\n```python\n# ① Truy vết lỗi\nassert ds[0].tong > 500_000\n# AssertionError — nhưng nếu dùng pytest thì nó in cả object ra\n\n# ② Thông báo lỗi tự viết\nraise ValueError(f\"đơn không hợp lệ: {ds[0]!r}\")\n\n# ③ Ghi log\nlogging.info(\"xử lý %r\", ds[0])\n```\n\nMẫu viết `__repr__` tự động cho class nhiều trường:\n\n```python\ndef __repr__(self):\n    truong = \", \".join(f\"{k}={v!r}\" for k, v in vars(self).items()\n                       if not k.startswith(\"_\"))          # ⭐ bỏ nội bộ\n    return f\"{type(self).__name__}({truong})\"\n```\n\n### Bẫy `__eq__` và `__hash__` — tái hiện\n\n```python\nclass Diem:\n    def __init__(self, x, y): self.x, self.y = x, y\n\n# Trước khi thêm __eq__ — hashable\ncache = {Diem(1, 2): \"giá trị\"}      # ✓ chạy được\nprint(len(cache))                     # 1\n\n# Thêm __eq__ cho tiện so sánh\nclass Diem:\n    def __init__(self, x, y): self.x, self.y = x, y\n    def __eq__(self, k):\n        return isinstance(k, Diem) and (self.x, self.y) == (k.x, k.y)\n\nDiem(1, 2) == Diem(1, 2)     # True ✓ đúng ý muốn\ncache = {Diem(1, 2): \"gt\"}   # TypeError: unhashable type: 'Diem'  ⚠️\n{Diem(1, 2)}                  # TypeError\n\nprint(Diem.__hash__)          # None — Python đã tự đặt\n```\n\nVì sao Python làm vậy — thử hình dung nếu nó không làm:\n\n```python\n# Giả sử __hash__ vẫn dựa trên địa chỉ\nd1, d2 = Diem(1, 2), Diem(1, 2)\nd1 == d2          # True  — bằng nhau\nhash(d1) == hash(d2)   # False — khác hash\n\nkho = {d1: \"gt\"}\nkho[d2]           # KeyError! — dù d2 == d1\n#                   dict tìm theo hash, không tìm thấy ô nào\n```\n\nDữ liệu \"biến mất\" dù nó có ở đó. Python chọn báo lỗi rõ ràng thay vì để chuyện này xảy ra.\n\n```python\n# ✓ Sửa cho object BẤT BIẾN\nclass Diem:\n    __slots__ = (\"x\", \"y\")            # ⭐ chống thêm thuộc tính (bài class)\n\n    def __init__(self, x, y):\n        object.__setattr__(self, \"x\", x)\n        object.__setattr__(self, \"y\", y)\n\n    def __eq__(self, k):\n        return isinstance(k, Diem) and (self.x, self.y) == (k.x, k.y)\n\n    def __hash__(self):\n        return hash((self.x, self.y))     # ⭐ CÙNG dữ liệu với __eq__\n\n    def __repr__(self):\n        return f\"Diem({self.x}, {self.y})\"\n\n{Diem(1, 2), Diem(1, 2), Diem(3, 4)}      # {Diem(1, 2), Diem(3, 4)} ✓\n{Diem(1, 2): \"gốc\"}[Diem(1, 2)]           # 'gốc' ✓\n```\n\nQuy tắc vàng: **`__hash__` phải dựa trên đúng những trường mà `__eq__` dùng**, và những trường đó **không được đổi** trong vòng đời object.\n\nVới object mutable, cách đúng là **để nguyên, không hashable**:\n\n```python\nclass GioHang:\n    def __init__(self): self._sp = []\n    def them(self, x): self._sp.append(x)\n    def __eq__(self, k):\n        return isinstance(k, GioHang) and self._sp == k._sp\n    # KHÔNG định nghĩa __hash__ ⇒ không làm khoá dict được ⇒ đúng\n```\n\n### Class hoà vào cú pháp Python\n\n```python\nfrom functools import total_ordering\n\n@total_ordering\nclass Tien:\n    __slots__ = (\"xu\", \"don_vi\")\n\n    def __init__(self, xu: int, don_vi: str = \"VND\"):\n        if not isinstance(xu, int):\n            raise TypeError(\"lưu tiền bằng số nguyên đơn vị nhỏ nhất\")\n        object.__setattr__(self, \"xu\", xu)\n        object.__setattr__(self, \"don_vi\", don_vi)\n\n    def _cung_don_vi(self, k):\n        if self.don_vi != k.don_vi:\n            raise ValueError(f\"khác đơn vị: {self.don_vi} và {k.don_vi}\")\n\n    # Đại diện\n    def __repr__(self): return f\"Tien({self.xu!r}, {self.don_vi!r})\"\n    def __str__(self): return f\"{self.xu/100:,.2f} {self.don_vi}\"\n    def __format__(self, spec):\n        return format(self.xu / 100, spec) + f\" {self.don_vi}\"\n\n    # So sánh\n    def __eq__(self, k):\n        return (isinstance(k, Tien)\n                and (self.xu, self.don_vi) == (k.xu, k.don_vi))\n    def __lt__(self, k):\n        if not isinstance(k, Tien): return NotImplemented\n        self._cung_don_vi(k)\n        return self.xu < k.xu\n    def __hash__(self): return hash((self.xu, self.don_vi))\n\n    # Toán tử\n    def __add__(self, k):\n        if not isinstance(k, Tien): return NotImplemented\n        self._cung_don_vi(k)\n        return Tien(self.xu + k.xu, self.don_vi)\n    def __sub__(self, k):\n        if not isinstance(k, Tien): return NotImplemented\n        self._cung_don_vi(k)\n        return Tien(self.xu - k.xu, self.don_vi)\n    def __mul__(self, n):\n        if not isinstance(n, int): return NotImplemented\n        return Tien(self.xu * n, self.don_vi)\n    __rmul__ = __mul__                          # ⭐ 3 * tien\n    def __neg__(self): return Tien(-self.xu, self.don_vi)\n    def __abs__(self): return Tien(abs(self.xu), self.don_vi)\n    def __bool__(self): return self.xu != 0\n\n\na, b = Tien(150_00), Tien(250_00)\nprint(a + b)                    # 400.00 VND\nprint(3 * a)                    # 450.00 VND\nprint(sorted([b, a]))           # [Tien(15000, 'VND'), Tien(25000, 'VND')]\nprint(max(a, b))                # Tien(25000, 'VND')\nprint(a < b, a == Tien(150_00)) # True True\nprint(f\"{a:>12,.2f}\")           # '      150.00 VND'\nprint(bool(Tien(0)))            # False\nprint({a, Tien(150_00)})        # {Tien(15000, 'VND')} — một phần tử\nprint(sum([a, b], Tien(0)))     # 400.00 VND\n```\n\nBa chi tiết đáng học từ class này:\n\n```\nlưu xu (int)          → tiền không dùng float (bài IEEE 754 ở module 1)\n__rmul__ = __mul__     → gán trực tiếp, ngắn hơn viết lại\nsum([...], Tien(0))    → sum cần giá trị khởi đầu vì mặc định là int 0\n```\n\n### Container tự viết\n\n```python\nclass BangDiem:\n    \"\"\"Ánh xạ tên học sinh → điểm, có kiểm tra.\"\"\"\n\n    def __init__(self, du_lieu=None):\n        self._d: dict[str, float] = {}\n        if du_lieu:\n            for k, v in dict(du_lieu).items():\n                self[k] = v                    # ⭐ đi qua __setitem__, có kiểm tra\n\n    def __getitem__(self, ten: str) -> float:\n        try:\n            return self._d[ten]\n        except KeyError:\n            raise KeyError(f\"không có học sinh {ten!r}\") from None\n\n    def __setitem__(self, ten: str, diem: float) -> None:\n        if not 0 <= diem <= 10:\n            raise ValueError(f\"điểm ngoài khoảng 0–10: {diem}\")\n        self._d[ten] = float(diem)\n\n    def __delitem__(self, ten): del self._d[ten]\n    def __contains__(self, ten): return ten in self._d\n    def __len__(self): return len(self._d)\n    def __iter__(self): return iter(self._d)\n    def __bool__(self): return bool(self._d)\n    def __repr__(self): return f\"BangDiem({self._d!r})\"\n\n    @property\n    def trung_binh(self) -> float:\n        return sum(self._d.values()) / len(self._d) if self._d else 0.0\n\n\nbd = BangDiem({\"Minh\": 8, \"An\": 9})\nbd[\"Bình\"] = 7.5\nbd[\"Chi\"] = 15               # ValueError ✓ kiểm tra ở __setitem__\n\nlen(bd)                       # 3\n\"Minh\" in bd                  # True\nlist(bd)                      # ['Minh', 'An', 'Bình']\nsorted(bd)                    # ['An', 'Bình', 'Minh']\ndict(bd.items()) if hasattr(bd, \"items\") else None\nbd.trung_binh                 # 8.166...\n\nfor ten in bd:\n    print(f\"{ten}: {bd[ten]}\")\n```\n\nChú ý `raise KeyError(...) from None`: nó thay thông báo mặc định bằng thông báo có ích, và `from None` giấu chuỗi ngoại lệ nội bộ vốn chỉ gây nhiễu ở đây — đúng trường hợp `from None` chính đáng đã nói ở bài ngoại lệ.\n\nMuốn class này giống `dict` hoàn toàn thì kế thừa `collections.abc.MutableMapping` — nó cho miễn phí `keys()`, `values()`, `items()`, `get()`, `pop()`, `update()` từ năm phương thức bạn đã viết.\n\n### `__call__` — object có trạng thái dùng như hàm\n\n```python\nclass BoDemGoi:\n    def __init__(self, ham):\n        self.ham = ham\n        self.so_lan = 0\n        self.tong_thoi_gian = 0.0\n\n    def __call__(self, *a, **k):\n        import time\n        self.so_lan += 1\n        t = time.perf_counter()\n        try:\n            return self.ham(*a, **k)\n        finally:\n            self.tong_thoi_gian += time.perf_counter() - t\n\n    @property\n    def trung_binh(self):\n        return self.tong_thoi_gian / self.so_lan if self.so_lan else 0\n\n    def __repr__(self):\n        return (f\"<BoDemGoi {self.ham.__name__}: {self.so_lan} lần, \"\n                f\"TB {self.trung_binh*1000:.2f}ms>\")\n\n\ndef tinh(n): return sum(range(n))\n\nd = BoDemGoi(tinh)\nd(1000); d(2000); d(3000)\nprint(d)              # <BoDemGoi tinh: 3 lần, TB 0.05ms>\nprint(d.so_lan)       # 3\n```\n\nSo với một closure làm cùng việc: closure giữ được trạng thái nhưng **không lộ ra được** (`d.so_lan`, `d.trung_binh`). `__call__` cho cả hai — dùng như hàm, mà vẫn có thuộc tính và phương thức phụ.\n\nĐây cũng là cách viết decorator bằng class, nối về bài decorator ở module 5.\n\n### `__getattr__` — tiện và nguy hiểm\n\n```python\nclass CauHinh:\n    def __init__(self, d: dict):\n        object.__setattr__(self, \"_d\", d)     # ⭐ tránh __setattr__ của chính mình\n\n    def __getattr__(self, ten):               # chỉ gọi khi KHÔNG tìm thấy\n        try:\n            gt = self._d[ten]\n        except KeyError:\n            raise AttributeError(f\"không có cấu hình {ten!r}\") from None\n        return CauHinh(gt) if isinstance(gt, dict) else gt\n\n    def __setattr__(self, ten, gt):\n        raise AttributeError(\"cấu hình chỉ đọc\")\n\n    def __repr__(self): return f\"CauHinh({self._d!r})\"\n\n\ncf = CauHinh({\"db\": {\"host\": \"localhost\", \"port\": 5432}, \"debug\": True})\ncf.db.host          # 'localhost'  ⭐ truy cập lồng nhau tự nhiên\ncf.debug            # True\ncf.khong_co         # AttributeError: không có cấu hình 'khong_co' ✓\ncf.debug = False    # AttributeError: cấu hình chỉ đọc ✓\n```\n\nHai cạm bẫy của `__getattr__` phải biết:\n\n```python\n# ① Đệ quy vô hạn nếu truy cập thuộc tính chưa tồn tại bên trong nó\ndef __getattr__(self, ten):\n    return self._d[ten]      # nếu _d chưa được gán ⇒ gọi lại __getattr__ ⇒ ∞\n\n# ② mypy và trình soạn thảo không biết gì về các thuộc tính động\ncf.hots             # gõ sai — không công cụ nào bắt được\n```\n\nVì vậy `__getattr__` chỉ nên dùng cho **dữ liệu thật sự động** (cấu hình đọc từ file, phản hồi API). Với dữ liệu có cấu trúc biết trước, `dataclass` hoặc `TypedDict` tốt hơn nhiều — bài sau sẽ nói.\n\n### Kiểm tra magic method của một class\n\n```python\ndef soi_magic(cls):\n    \"\"\"Liệt kê magic method mà class TỰ định nghĩa (không kể kế thừa).\"\"\"\n    tu_viet = sorted(k for k in cls.__dict__\n                     if k.startswith(\"__\") and k.endswith(\"__\"))\n    print(f\"{cls.__name__}: {tu_viet}\")\n\n    # Cảnh báo bẫy phổ biến\n    if \"__eq__\" in tu_viet and \"__hash__\" not in tu_viet:\n        print(\"  ⚠️ có __eq__ mà không có __hash__ ⇒ MẤT tính hashable\")\n    if \"__init__\" in tu_viet and \"__repr__\" not in tu_viet:\n        print(\"  ⚠️ không có __repr__ ⇒ gỡ lỗi sẽ khó\")\n\nsoi_magic(Tien)\nsoi_magic(DonHang)\n```\n\nHàm này đáng chạy khi review: hai cảnh báo trên là hai lỗi phổ biến nhất liên quan tới magic method, và cả hai đều **im lặng**.\n\n### Kế thừa `collections.abc` — nhận miễn phí\n\n```python\nfrom collections.abc import MutableMapping\n\nclass BangDiem(MutableMapping):\n    def __init__(self, d=None):\n        self._d = {}\n        if d: self.update(d)                  # ⭐ update() có sẵn\n\n    def __getitem__(self, k): return self._d[k]\n    def __setitem__(self, k, v):\n        if not 0 <= v <= 10:\n            raise ValueError(f\"điểm ngoài khoảng: {v}\")\n        self._d[k] = float(v)\n    def __delitem__(self, k): del self._d[k]\n    def __iter__(self): return iter(self._d)\n    def __len__(self): return len(self._d)\n\nbd = BangDiem({\"Minh\": 8})\n\n# ⭐ Nhận MIỄN PHÍ, không viết dòng nào:\nbd.keys(); bd.values(); bd.items()\nbd.get(\"An\", 0); bd.pop(\"Minh\"); bd.setdefault(\"An\", 5)\nbd.update({\"Bình\": 7}); \"Bình\" in bd; bd == {\"An\": 5.0, \"Bình\": 7.0}\n```\n\nNăm phương thức đổi lấy hơn mười tính năng — và tất cả đều **đi qua `__setitem__` của bạn**, nên phần kiểm tra điểm luôn có hiệu lực. Đây là mẫu *Template Method* trong thư viện chuẩn, và là lý do nên xem `collections.abc` trước khi tự viết một container.",

so_sanh: "### `__repr__` và `__str__`\n\n| | `__repr__` | `__str__` |\n|---|---|---|\n| Cho ai | **lập trình viên** | người dùng cuối |\n| Mục tiêu | **tạo lại được object** | dễ đọc |\n| Dùng bởi | REPL, `repr()`, **list/dict**, truy vết | `print()`, `str()`, f-string |\n| Mặc định | `<Lop at 0x...>` | gọi `__repr__` |\n| Nếu chỉ viết một | **✓ viết cái này** | thiếu thì dùng `__repr__` |\n\nHàng \"dùng bởi\" giải thích vì sao `__repr__` quan trọng hơn: in một `list` object **luôn** dùng `__repr__`, kể cả khi bạn gọi `print`.\n\n### `__eq__` và `__hash__`\n\n| Tình huống | `__eq__` | `__hash__` | Hashable |\n|---|---|---|---|\n| Không định nghĩa gì | mặc định (`is`) | mặc định (địa chỉ) | **✓** |\n| Chỉ định nghĩa `__eq__` | tự viết | **`None` tự động** | **✗** |\n| Định nghĩa cả hai | tự viết | tự viết | ✓ |\n| Object **mutable** | tự viết | **đừng định nghĩa** | ✗ đúng |\n\nQuy tắc: **hai object bằng nhau phải có cùng hash.** Python đặt `__hash__ = None` để ngăn bạn vi phạm quy tắc đó một cách âm thầm.\n\nVới object mutable, **không hashable là hành vi đúng** — nếu không, sửa object sau khi cất vào `set` sẽ làm nó \"biến mất\".\n\n### Các nhóm giao thức\n\n| Nhóm | Cài đặt tối thiểu | Nhận lại được |\n|---|---|---|\n| Đại diện | `__repr__` | gỡ lỗi, log, truy vết dùng được |\n| So sánh | `__eq__` + `__lt__` + `@total_ordering` | `==`, `<`, `sorted`, `min`, `max` |\n| Container | `__len__` + `__getitem__` + `__iter__` | `len`, `x[i]`, `for`, `sum`, `join` |\n| Toán tử | `__add__` và các phản chiếu | `+`, `+=`, `sum()` |\n| Gọi được | `__call__` | dùng ở mọi chỗ nhận hàm |\n| Ngữ cảnh | `__enter__` + `__exit__` | `with` |\n\nHàng đầu là hàng nên làm trước cho **mọi** class: chi phí ba dòng, lợi ích ở mọi phiên gỡ lỗi.\n\n### `__bool__`, `__len__` và chân trị\n\n| Class có | `if x:` dùng gì |\n|---|---|\n| `__bool__` | **`__bool__`** |\n| chỉ `__len__` | `__len__() != 0` |\n| không có cả hai | **luôn `True`** |\n\nHàng cuối là bẫy: class chứa dữ liệu mà không có `__len__` thì `if bo_suu_tap:` luôn đúng kể cả khi rỗng — lỗi logic im lặng.\n\n### Toán tử: `__add__`, `__radd__`, `__iadd__`\n\n| | `__add__` | `__radd__` | `__iadd__` |\n|---|---|---|---|\n| Kích hoạt bởi | `a + b` | `b + a` khi `b` không biết `a` | `a += b` |\n| Tạo object mới | ✓ | ✓ | **✗ sửa tại chỗ** |\n| Phải `return` | ✓ | ✓ | **✓ `return self`** |\n| Thiếu thì sao | `TypeError` | `3 * v` lỗi | tự dùng `__add__` |\n\n`__iadd__` chỉ đáng định nghĩa khi sửa tại chỗ **rẻ hơn nhiều** so với tạo mới — ví dụ với mảng lớn.\n\n### Khi nào cài đặt magic method\n\n| Tình huống | Cài đặt? |\n|---|---|\n| Mọi class (gỡ lỗi) | **`__repr__` — luôn** |\n| Class chứa dữ liệu, cần so sánh | `__eq__` + `__hash__` |\n| Class là collection | `__len__`, `__iter__`, `__getitem__` |\n| Toán học, tiền tệ, vector | toán tử — ý nghĩa hiển nhiên |\n| Đối tượng nghiệp vụ chung | **✗ dùng phương thức có tên** |\n| Cú pháp lạ (`>>`, `<<`) | **✗** |\n\nTiêu chí xuyên suốt: **người đọc có đoán đúng ý nghĩa mà không cần đọc tài liệu không?**\n\n### Tự viết và kế thừa `collections.abc`\n\n| | Tự viết hết | Kế thừa `MutableMapping` |\n|---|---|---|\n| Số phương thức phải viết | ~12 | **5** |\n| `keys`, `values`, `items` | tự viết | **miễn phí** |\n| `get`, `pop`, `update` | tự viết | **miễn phí** |\n| Kiểm tra của bạn có hiệu lực | tuỳ bạn | **✓ mọi thứ đi qua `__setitem__`** |\n| `isinstance(x, Mapping)` | ✗ | ✓ |\n\nHàng áp chót là lợi ích tinh tế: `update()` của `MutableMapping` gọi `self[k] = v`, nên phần kiểm tra dữ liệu của bạn **luôn được áp dụng** — khác hẳn việc kế thừa `dict` (bài kế thừa đã nói).",

loi_thuong_gap: [
  "**Không viết `__repr__`.** Gỡ lỗi thấy `<Obj at 0x...>`, in `list` object vô dụng, truy vết lỗi không nói gì.",
  "**Định nghĩa `__eq__` mà quên `__hash__`.** Object mất tính hashable âm thầm; lỗi hiện ra ở chỗ khác trong dự án.",
  "**Cho object mutable một `__hash__`.** Sửa object sau khi cất vào `set` là nó \"biến mất\" — dữ liệu hỏng không báo lỗi.",
  "**`__hash__` dựa trên trường khác với `__eq__`.** Vi phạm quy tắc \"bằng nhau thì cùng hash\", `dict` tra không ra khoá.",
  "**Quên `return NotImplemented` trong toán tử khi kiểu không hợp.** Chặn cơ chế phương thức phản chiếu, cho lỗi khó hiểu.",
  "**Quên `__radd__`/`__rmul__`.** `3 * v` lỗi trong khi `v * 3` chạy.",
  "**Quên `return self` trong `__iadd__`.** `a += b` gán `None` cho `a`.",
  "**Gọi `self.x = gt` bên trong `__setattr__`.** Đệ quy vô hạn. Dùng `super().__setattr__` hoặc `self.__dict__[...]`.",
  "**Truy cập thuộc tính chưa tồn tại bên trong `__getattr__`.** Đệ quy vô hạn.",
  "**Dùng `__getattr__` để chuyển tiếp mọi thứ.** Mất kiểm soát giao diện, `mypy` và trình soạn thảo bó tay.",
  "**Class chứa dữ liệu mà không có `__len__` hay `__bool__`.** `if bo_suu_tap:` luôn đúng kể cả khi rỗng.",
  "**Nạp chồng toán tử khi ý nghĩa không hiển nhiên.** `NguoiDung + NguoiDung` không ai đoán được.",
  "**Magic method có tác dụng phụ nặng.** `a + b` gọi API mạng là bất ngờ khó chịu — cú pháp toán tử gợi ý \"rẻ\".",
  "**Gán magic method vào instance thay vì class.** Python tra trên class; gán vào instance không có tác dụng.",
  "**Tự viết cả một container thay vì kế thừa `collections.abc`.** Năm phương thức đổi lấy hơn mười tính năng."
],

tu_kiem_tra: [
  "`len(x)` thực chất gọi gì? Vì sao gán `__len__` vào instance không có tác dụng?",
  "`__repr__` và `__str__` khác nhau ở đối tượng đọc và ở chỗ được dùng thế nào?",
  "Nếu chỉ viết một trong hai thì viết cái nào, và vì sao?",
  "Vì sao định nghĩa `__eq__` làm object mất tính hashable? Python bảo vệ bạn khỏi điều gì?",
  "Quy tắc vàng giữa `__eq__` và `__hash__` là gì?",
  "Vì sao object mutable **không nên** hashable?",
  "`@total_ordering` cần bạn viết những gì và sinh ra những gì?",
  "Cần những magic method nào để một class dùng được với `for`, `len`, `in`, `sorted`?",
  "`if x:` được quyết định thế nào khi class có/không có `__bool__` và `__len__`?",
  "Vì sao toán tử nên `return NotImplemented` khi gặp kiểu không hợp?",
  "Khi nào cần `__radd__`? Cho một ví dụ cụ thể.",
  "Hai cạm bẫy đệ quy vô hạn liên quan tới `__getattr__` và `__setattr__` là gì?",
  "`__call__` cho lợi ích gì so với closure?",
  "Kế thừa `collections.abc.MutableMapping` cho lại những gì, và vì sao phần kiểm tra của bạn luôn có hiệu lực?",
  "Tiêu chí nào quyết định có nên nạp chồng một toán tử?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Data model* — danh sách đầy đủ mọi magic method kèm ngữ nghĩa chính xác. Đây là tài liệu tra cứu chính.",
  "Tài liệu Python, module `collections.abc` — bảng liệt kê phương thức bắt buộc và phương thức được cho miễn phí của từng ABC.",
  "Sách **Fluent Python** — Luciano Ramalho. Cuốn này gần như xoay quanh magic method; các chương về object Pythonic và về nạp chồng toán tử là phần đầy đủ nhất.",
  "Tài liệu Python, module `functools` — `total_ordering` và các tiện ích liên quan.",
  "Sách **Python Tricks** — Dan Bader. Phần về dunder method viết ngắn gọn, hợp để đọc lần đầu."
],

lien_he: [
  "**Class, object, thuộc tính**: `__init__` là magic method đầu tiên; `__slots__` cũng thuộc nhóm này.",
  "**Đa hình và duck typing**: magic method chính là cách Python cài đặt duck typing ở tầng ngôn ngữ.",
  "**Trừu tượng: ABC, Protocol**: `collections.abc` cho miễn phí nhiều magic method từ vài phương thức bạn viết.",
  "**Đóng gói**: `__repr__` nên bỏ qua thuộc tính bắt đầu bằng `_`.",
  "**Dataclass và NamedTuple** ngay sau: chúng **tự sinh** `__init__`, `__repr__`, `__eq__` — bài này giải thích cái chúng sinh ra là gì.",
  "**Generator, iterator** ở module 5: `__iter__` và `__next__` là giao thức iterator.",
  "**Context manager và `with`** ở module 5: `__enter__` và `__exit__` là giao thức quản lý ngữ cảnh.",
  "**Decorator** ở module 5: `__call__` cho phép viết decorator bằng class có trạng thái.",
  "**List, dict, set, tuple** ở module 5: tính hashable và bảng băm giải thích vì sao `__eq__`/`__hash__` phải đi cùng nhau.",
  "**Số thực IEEE 754** ở module 1: lý do class `Tien` lưu số nguyên thay vì `float`.",
  "**AI/ML** ở mảng 07: `Dataset` chỉ cần `__len__` và `__getitem__` là dùng được với toàn bộ hệ sinh thái học sâu."
]

};
