window.LESSONS["01:bo-nho-co-che-thuc-thi:garbage-collection-va-reference-counting"] = {

tom_tat: [
  "**CPython dùng hai cơ chế chồng lên nhau**: đếm tham chiếu dọn ngay lập tức phần lớn rác, và bộ dọn theo thế hệ chỉ lo phần còn lại — **tham chiếu vòng**.",
  "**Đếm tham chiếu có một điểm mù duy nhất nhưng nghiêm trọng**: object trỏ vòng về nhau thì bộ đếm không bao giờ về 0, dù không ai bên ngoài còn dùng chúng.",
  "**Bộ dọn theo thế hệ dựa trên một quan sát thực nghiệm**: phần lớn object chết trẻ. Nên nó quét nhóm mới thường xuyên, nhóm già thì hiếm.",
  "**`__del__` là thứ nên tránh** — thời điểm gọi không đảm bảo, ngoại lệ trong nó bị nuốt, và nó từng khiến object trong vòng không dọn được. Dùng `with` và `weakref.finalize`.",
  "**Rò rỉ bộ nhớ trong Python không phải \"quên giải phóng\"** mà là **vô tình còn giữ tham chiếu**: cache không giới hạn, danh sách observer, biến toàn cục tích luỹ."
],

can_biet_truoc: [
  "Bài **Tham chiếu vs giá trị** — `ob_refcnt` nằm trong phần đầu mỗi object; bài này nói cơ chế dùng nó.",
  "Bài **Shallow copy vs deep copy** ngay trước — tham chiếu vòng đã gặp ở đó, giờ là vấn đề cần giải quyết.",
  "Bài **Context manager và `with`** ở module 5 — cách dọn tài nguyên đúng, thay cho `__del__`.",
  "Bài **Design pattern — Observer** ở module 6 — rò rỉ do danh sách observer là ví dụ chính ở đây."
],

dinh_nghia: "Nói đời thường: mỗi món đồ trong kho có **một cái thẻ ghi số người đang mượn**. Ai trả thì trừ một; về 0 thì bỏ ngay. Nhưng có trường hợp hai món **giữ phiếu mượn của nhau** — không ai bên ngoài cần chúng, mà thẻ nào cũng còn số 1. Cần một đợt kiểm kê định kỳ để phát hiện những cụm như vậy.\n\nNói chính xác:\n\n- **Đếm tham chiếu** (*reference counting*) — mỗi object giữ số nơi đang trỏ tới nó. Về 0 là **huỷ ngay lập tức**.\n- **Tham chiếu vòng** (*reference cycle*) — nhóm object trỏ tới nhau, tạo thành vòng khép kín.\n- **Bộ dọn rác theo thế hệ** (*generational GC*) — cơ chế phụ, quét tìm và dọn các vòng không còn ai bên ngoài dùng.\n- **Thế hệ** (*generation*) — ba nhóm object theo tuổi: 0 (mới), 1, 2 (già). Nhóm càng già càng ít bị quét.\n- **`weakref`** — tham chiếu **không làm tăng bộ đếm**, nên không giữ object sống.\n- **`__del__`** — phương thức được gọi khi object sắp bị huỷ. Còn gọi là *finalizer*.\n\nĐiểm cần phân biệt ngay: **\"garbage collector\" trong Python chỉ nói về phần dọn vòng.** Phần lớn object không bao giờ đi qua nó — chúng được đếm tham chiếu dọn ngay. Đây là khác biệt lớn với Java hay Go, nơi bộ dọn rác lo **toàn bộ** bộ nhớ.",

vi_sao_can: "Hiểu cơ chế dọn rác giải thích những hành vi mà tầng trên chỉ mô tả được:\n\n- **Nó giải thích vì sao `with` quan trọng.** Bài context manager ở module 5 nói \"đừng dựa vào bộ thu gom rác\"; bài này nói chính xác vì sao — và vì sao code chạy được trên CPython có thể hỏng ở bản hiện thực khác.\n- **Rò rỉ bộ nhớ trong Python có hình dạng riêng.** Bạn không quên `free()`, nhưng bạn quên xoá khỏi cache, quên huỷ đăng ký observer, quên dọn danh sách toàn cục. Biết cơ chế là biết chỗ tìm.\n- **`__del__` gây lỗi rất khó lần.** Thời điểm không đảm bảo, ngoại lệ bị nuốt, và thứ tự huỷ lúc thoát chương trình không xác định.\n- **`weakref` giải quyết một lớp bài toán thật** — cache, observer, quan hệ cha–con — mà không có nó thì phải quản lý thủ công.\n- **Định hướng AI/ML và Backend.** Tiến trình chạy nhiều ngày mà bộ nhớ tăng dần là sự cố phổ biến; công cụ và cách nghĩ để tìm nó nằm ở bài này.\n\nTình huống thật rất kinh điển: dịch vụ của bạn chạy ổn định vài giờ rồi bộ nhớ tăng đều tới khi hệ điều hành giết tiến trình. Không có `malloc`/`free` nào để kiểm tra. Nguyên nhân hoá ra là một `dict` cache không giới hạn kích thước, hoặc một danh sách handler mà không ai huỷ đăng ký. Phần cơ chế sẽ chỉ cách nhìn ra chúng bằng `gc` và `tracemalloc`.",

co_che: "### Đếm tham chiếu — cơ chế chính\n\n```python\nimport sys\n\nds = [1, 2, 3]\nsys.getrefcount(ds)      # 2 (một từ tên `ds`, một từ đối số hàm)\n\nds2 = ds\nsys.getrefcount(ds)      # 3\n\ndel ds2\nsys.getrefcount(ds)      # 2\n\ndel ds\n# refcount về 0 ⇒ HUỶ NGAY, bộ nhớ trả lại ngay lập tức\n```\n\nMỗi thao tác trên tham chiếu đều cập nhật bộ đếm:\n\n```\nTĂNG khi:  gán tên, thêm vào container, truyền làm đối số,\n           gán làm thuộc tính, closure bắt được\n\nGIẢM khi:  del tên, gán lại tên sang object khác, xoá khỏi container,\n           hàm kết thúc (khung stack bị huỷ), container bị huỷ\n```\n\nBa ưu điểm của đếm tham chiếu:\n\n```\n① TỨC THÌ — object chết là bộ nhớ về ngay, không chờ đợt quét\n② DỰ ĐOÁN ĐƯỢC — biết chính xác lúc nào object bị huỷ\n③ Chi phí TRẢI ĐỀU — không có khoảng dừng dài như GC quét toàn bộ\n```\n\nĐiểm ① là lý do file trong CPython thường được đóng ngay khi hết tham chiếu — và cũng là lý do **thói quen dựa vào đó rất nguy hiểm**, vì đó là chi tiết cài đặt.\n\nBa nhược điểm:\n\n```\n① Tốn bộ nhớ  — 8 byte mỗi object cho bộ đếm\n② Tốn thời gian — mỗi phép gán phải cập nhật bộ đếm\n③ KHÔNG dọn được tham chiếu vòng  ← vấn đề thật sự\n```\n\nNhược điểm ② còn có hệ quả lớn hơn: bộ đếm nằm **trong** object, nên mỗi lần chạm vào object là ghi vào bộ nhớ đó. Đây là một trong những lý do khiến việc chia sẻ object giữa nhiều luồng trở nên phức tạp — và dẫn thẳng tới bài GIL ngay sau.\n\n### Điểm mù: tham chiếu vòng\n\n```python\nimport sys\n\na = []\nb = []\na.append(b)          # a trỏ tới b\nb.append(a)          # b trỏ tới a  ⭐ VÒNG\n\nprint(sys.getrefcount(a) - 1)     # 2: từ tên `a` và từ list b\n\ndel a, b\n# ⚠️ Hai list vẫn còn: mỗi cái được cái kia giữ, refcount = 1\n#    Không ai bên ngoài truy cập được, nhưng bộ nhớ KHÔNG được trả lại\n```\n\n```\nTrước del:                    Sau del:\n  a ──► [list A] ──┐            [list A] ──┐\n                   │                ▲      │\n  b ──► [list B] ◄─┘                └──────┘\n         │                        [list B]\n         └──► trỏ ngược lên A     refcount mỗi cái = 1\n                                  ⇒ KHÔNG BAO GIỜ về 0\n```\n\nVòng xuất hiện tự nhiên hơn nhiều người nghĩ:\n\n```python\n# ① Cây có con trỏ ngược lên cha\nclass Nut:\n    def __init__(self, ten):\n        self.ten, self.cha, self.con = ten, None, []\n    def them(self, c):\n        c.cha = self          # ⭐ vòng: cha → con → cha\n        self.con.append(c)\n\n# ② Object tự tham chiếu qua closure\nclass A:\n    def __init__(self):\n        self.callback = lambda: self      # ⭐ closure giữ self\n\n# ③ Danh sách hai chiều, đồ thị, quan hệ nhiều-nhiều\n# ④ Ngoại lệ đã bắt — traceback giữ khung stack, khung giữ ngoại lệ\n```\n\nTrường hợp ④ đáng chú ý vì nó vô hình:\n\n```python\ntry:\n    1 / 0\nexcept ZeroDivisionError as e:\n    # e.__traceback__ giữ khung stack, khung stack giữ biến e ⇒ VÒNG\n    pass\n# Python tự động `del e` khi ra khỏi except để phá vòng này\n```\n\nĐây là lý do Python **tự xoá tên ngoại lệ** khi ra khỏi khối `except` — một chi tiết ít ai biết, và nó giải thích vì sao `print(e)` sau khối `except` báo `NameError`.\n\n### Bộ dọn theo thế hệ\n\nĐể dọn vòng, CPython chạy một bộ dọn phụ. Nó dựa trên một quan sát thực nghiệm:\n\n```\nPhần lớn object CHẾT TRẺ.\nObject sống qua vài đợt quét thì thường sống rất lâu.\n```\n\nNên nó chia object thành ba **thế hệ**:\n\n```\nThế hệ 0  object mới tạo          → quét THƯỜNG XUYÊN\nThế hệ 1  sống qua 1 đợt quét      → quét ít hơn\nThế hệ 2  sống qua nhiều đợt       → quét HIẾM\n```\n\n```python\nimport gc\n\ngc.get_threshold()        # (700, 10, 10) — ngưỡng kích hoạt\ngc.get_count()            # (số object trong mỗi thế hệ hiện tại)\n```\n\nÝ nghĩa của `(700, 10, 10)`:\n\n```\n700  số object được cấp phát trừ số bị huỷ vượt 700 ⇒ quét thế hệ 0\n10   sau 10 lần quét thế hệ 0     ⇒ quét luôn thế hệ 1\n10   sau 10 lần quét thế hệ 1     ⇒ quét luôn thế hệ 2 (toàn bộ)\n```\n\nCon số đầu **không phải \"700 object\"** mà là hiệu giữa số cấp phát và số giải phóng — nên chương trình tạo và huỷ object đều đặn thì hiếm khi kích hoạt quét.\n\nCách bộ dọn tìm rác vòng:\n\n```\n① Với mỗi object trong thế hệ đang quét, tạo một bản sao bộ đếm\n② Duyệt các tham chiếu GIỮA CÁC OBJECT trong nhóm, trừ bản sao đó\n③ Object nào còn bản sao > 0 ⇒ có tham chiếu TỪ BÊN NGOÀI ⇒ còn sống\n④ Lan từ nhóm sống sang những gì nó trỏ tới\n⑤ Phần còn lại là rác vòng ⇒ huỷ\n```\n\nÝ tưởng cốt lõi ở bước ②–③: **trừ đi các tham chiếu nội bộ nhóm**. Cái gì còn dư nghĩa là có ai đó bên ngoài đang giữ.\n\nQuan trọng: **chỉ object có thể chứa tham chiếu mới được theo dõi.** `int`, `str`, `float` không thể tạo vòng nên bộ dọn bỏ qua chúng hoàn toàn:\n\n```python\nimport gc\n\ngc.is_tracked([])          # True  — list có thể chứa tham chiếu\ngc.is_tracked({})          # True\ngc.is_tracked(1)           # False\ngc.is_tracked(\"abc\")       # False\ngc.is_tracked((1, 2))      # ⚠️ False sau khi GC xác định tuple chỉ chứa bất biến\n```\n\nDòng cuối là một tối ưu tinh tế: `tuple` chỉ chứa object bất biến sẽ được **bỏ theo dõi**, vì nó không thể tham gia vòng.\n\n### Điều khiển bộ dọn\n\n```python\nimport gc\n\ngc.collect()               # quét ngay, trả về số object đã dọn\ngc.collect(0)              # chỉ thế hệ 0\ngc.disable()               # TẮT bộ dọn vòng (đếm tham chiếu vẫn chạy)\ngc.enable()\ngc.isenabled()\n\ngc.set_threshold(1000, 15, 15)     # đổi ngưỡng\n```\n\n**Tắt bộ dọn có ích không?** Trong một số trường hợp:\n\n```\n✓ Chương trình chạy ngắn, biết chắc không tạo vòng\n✓ Giai đoạn nạp dữ liệu lớn — tránh quét liên tục khi cấp phát nhiều\n✓ Ứng dụng nhạy độ trễ, muốn kiểm soát thời điểm quét\n\n✗ Chương trình chạy lâu có tạo vòng ⇒ rò rỉ chắc chắn\n```\n\nMẫu thực dụng: tắt trong đoạn nạp dữ liệu, bật lại sau.\n\n```python\nimport gc\n\ngc.disable()\ndu_lieu = nap_du_lieu_lon()      # cấp phát hàng triệu object\ngc.enable()\ngc.collect()                      # dọn một lần\n```\n\nLý do có tác dụng: trong lúc cấp phát dồn dập, ngưỡng 700 bị vượt liên tục và bộ dọn quét đi quét lại một tập object **đang sống**, tốn công vô ích.\n\n### `__del__` — và vì sao nên tránh\n\n```python\nclass A:\n    def __del__(self):\n        print(\"A bị huỷ\")\n\na = A()\ndel a          # \"A bị huỷ\" — in ngay, nhờ đếm tham chiếu\n```\n\nTrông tiện, nhưng có bốn vấn đề nghiêm trọng:\n\n**① Thời điểm không đảm bảo:**\n\n```python\na = A()\nb = a\ndel a          # KHÔNG in gì — b vẫn giữ\ndel b          # giờ mới in\n```\n\nVà trong object nằm trong vòng, `__del__` chỉ chạy khi bộ dọn quét tới — có thể là rất lâu sau, hoặc **không bao giờ** nếu chương trình kết thúc trước.\n\n**② Ngoại lệ bị nuốt:**\n\n```python\nclass B:\n    def __del__(self):\n        raise ValueError(\"lỗi trong __del__\")\n\nb = B()\ndel b\n# In cảnh báo ra stderr rồi TIẾP TỤC — ngoại lệ KHÔNG bay lên\n```\n\nĐây là lỗi im lặng đúng nghĩa: bạn không bắt được, không xử lý được.\n\n**③ Thứ tự huỷ lúc thoát chương trình không xác định:**\n\n```python\nclass C:\n    def __del__(self):\n        print(len(DANH_SACH))     # ⚠️ DANH_SACH có thể đã bị xoá\n\nDANH_SACH = [1, 2]\nc = C()\n# Khi thoát chương trình, thứ tự dọn module không đảm bảo\n```\n\n**④ Nó làm object khó gỡ lỗi.** Object có `__del__` trong một vòng từng không dọn được ở các bản Python cũ; bản mới đã sửa, nhưng thói quen tránh nó vẫn đúng.\n\nCách đúng — dùng `with`:\n\n```python\n# ❌ Dựa vào __del__\nclass Kho:\n    def __init__(self, p): self.f = open(p)\n    def __del__(self): self.f.close()\n\n# ✓ Context manager — thời điểm rõ ràng, ngoại lệ không bị nuốt\nclass Kho:\n    def __init__(self, p): self.p = p\n    def __enter__(self):\n        self.f = open(self.p, encoding=\"utf-8\")\n        return self\n    def __exit__(self, *a):\n        self.f.close()\n        return False\n\nwith Kho(\"a.txt\") as k:\n    ...\n```\n\nHoặc `weakref.finalize` khi cần một lưới an toàn:\n\n```python\nimport weakref\n\nclass Kho:\n    def __init__(self, p):\n        self.f = open(p, encoding=\"utf-8\")\n        self._dong = weakref.finalize(self, self.f.close)   # ⭐\n\n    def dong(self):\n        self._dong()          # gọi tường minh, chỉ chạy một lần\n```\n\n`weakref.finalize` hơn `__del__` ở ba điểm: gọi được tường minh, **chỉ chạy đúng một lần**, và nó không giữ tham chiếu mạnh tới object nên không tạo vòng.\n\n### `weakref` — tham chiếu không giữ object\n\n```python\nimport weakref, gc\n\nclass A:\n    def __init__(self, ten): self.ten = ten\n\na = A(\"x\")\nr = weakref.ref(a)         # ⭐ KHÔNG tăng refcount\n\nprint(r())                 # <A object> — gọi để lấy object\nprint(r() is a)            # True\n\ndel a\ngc.collect()\nprint(r())                 # None ⭐ object đã bị dọn\n```\n\nBa ứng dụng chính:\n\n**① Cache không giữ object sống:**\n\n```python\nimport weakref\n\ncache = weakref.WeakValueDictionary()\n\nclass DuLieuLon:\n    def __init__(self, ma): self.ma = ma\n\nd = DuLieuLon(\"a\")\ncache[\"a\"] = d\nprint(len(cache))          # 1\n\ndel d\nprint(len(cache))          # 0 ⭐ tự biến mất khi không ai khác giữ\n```\n\nSo với `dict` thường: cache thường **giữ object sống mãi**, và đó là một trong những nguồn rò rỉ phổ biến nhất.\n\n**② Phá vòng trong quan hệ cha–con:**\n\n```python\nclass Nut:\n    def __init__(self, ten):\n        self.ten = ten\n        self.con = []\n        self._cha = None\n\n    @property\n    def cha(self):\n        return self._cha() if self._cha else None      # ⭐ gọi weakref\n\n    def them(self, c):\n        c._cha = weakref.ref(self)     # ⭐ tham chiếu YẾU lên cha\n        self.con.append(c)             # tham chiếu MẠNH xuống con\n        return c\n```\n\nQuy tắc chung cho cấu trúc cây: **mạnh xuống dưới, yếu lên trên.** Khi gốc bị huỷ, cả cây được dọn bằng đếm tham chiếu, không cần bộ dọn vòng.\n\n**③ Observer không rò rỉ** — đã gặp ở bài design pattern:\n\n```python\nclass Nguon:\n    def __init__(self):\n        self._qs = weakref.WeakSet()      # ⭐ không giữ observer sống\n\n    def dang_ky(self, qs):\n        self._qs.add(qs)\n\n    def phat(self, sk):\n        for qs in list(self._qs):          # bản sao — tránh sửa khi lặp\n            qs.nhan(sk)\n```\n\nĐánh đổi phải nói rõ: observer **phải được giữ ở nơi khác**, nếu không nó biến mất ngay. Đây là lý do nhiều thư viện chọn tham chiếu mạnh cộng huỷ đăng ký tường minh thay vì `weakref`.\n\n### Rò rỉ bộ nhớ trong Python có hình dạng gì\n\nKhông phải \"quên `free()`\" mà là **vô tình còn giữ tham chiếu**:\n\n```python\n# ① Cache không giới hạn\nCACHE = {}\ndef tinh(x):\n    if x not in CACHE:\n        CACHE[x] = tinh_nang(x)\n    return CACHE[x]                # ⚠️ CACHE lớn mãi\n\n# ✓ Giới hạn kích thước\nfrom functools import lru_cache\n\n@lru_cache(maxsize=1024)           # ⭐ có giới hạn\ndef tinh(x): ...\n\n\n# ② Danh sách toàn cục tích luỹ\nLICH_SU = []\ndef xu_ly(bg):\n    LICH_SU.append(bg)             # ⚠️ không bao giờ dọn\n\n# ✓ Dùng deque có maxlen\nfrom collections import deque\nLICH_SU = deque(maxlen=1000)       # ⭐ tự bỏ cái cũ\n\n\n# ③ Observer không huỷ đăng ký\nbus.dang_ky(handler)               # ⚠️ bus giữ handler mãi\n\n# ✓ Huỷ đăng ký, hoặc dùng weakref\n\n\n# ④ Closure giữ dữ liệu lớn ngoài ý muốn\ndef tao_ham():\n    du_lieu_lon = nap_100mb()\n    return lambda: len(du_lieu_lon)    # ⚠️ closure giữ 100 MB\n\n# ✓ Chỉ giữ thứ cần\ndef tao_ham():\n    n = len(nap_100mb())               # ⭐ giữ một số nguyên\n    return lambda: n\n\n\n# ⑤ Ngoại lệ lưu lại giữ cả khung stack\nLOI = []\ntry:\n    ...\nexcept Exception as e:\n    LOI.append(e)                  # ⚠️ e.__traceback__ giữ MỌI biến cục bộ\n\n# ✓ Lưu chuỗi thay vì object ngoại lệ\nimport traceback\nLOI.append(traceback.format_exc())\n```\n\nTrường hợp ⑤ tinh vi và tốn bộ nhớ nhiều nhất: một object ngoại lệ giữ traceback, traceback giữ mọi khung stack, mỗi khung giữ mọi biến cục bộ tại thời điểm lỗi. Lưu một ngoại lệ có thể giữ sống hàng chục megabyte.\n\n### Nhìn thấy vòng bằng `gc`\n\n```python\nimport gc\n\ngc.set_debug(gc.DEBUG_SAVEALL)     # giữ lại rác đã dọn để soi\n\na, b = [], []\na.append(b); b.append(a)\ndel a, b\n\ngc.collect()\nprint(f\"số object rác: {len(gc.garbage)}\")\nfor o in gc.garbage[:5]:\n    print(f\"  {type(o).__name__}: {repr(o)[:50]}\")\n\ngc.set_debug(0)\ngc.garbage.clear()                  # ⭐ nhớ dọn, nếu không nó giữ rác thật\n```\n\nHai hàm điều tra hữu ích:\n\n```python\ngc.get_referrers(obj)       # ai đang trỏ TỚI obj  ← để tìm rò rỉ\ngc.get_referents(obj)       # obj đang trỏ tới ai\n```\n\n`get_referrers` là công cụ chính khi hỏi *\"vì sao object này chưa được dọn\"* — nó liệt kê mọi nơi còn giữ tham chiếu.\n\nCảnh báo: bản thân lời gọi `get_referrers` tạo thêm tham chiếu tạm, và kết quả có thể chứa cả khung stack của chính bạn. Đọc kết quả cần bỏ qua những mục đó.\n\n### CPython và các bản hiện thực khác\n\nĐây là điểm có hệ quả thực tế lớn:\n\n```\nCPython        đếm tham chiếu + dọn vòng theo thế hệ\n               → object thường chết NGAY khi hết tham chiếu\n\nBản khác       chỉ có bộ dọn rác đánh dấu-quét\n               → object chết KHÔNG XÁC ĐỊNH THỜI ĐIỂM\n```\n\nHệ quả:\n\n```python\n# ⚠️ Chạy được trên CPython, HỎNG ở bản hiện thực khác\nfor duong_dan in danh_sach_10000_file:\n    du_lieu = open(duong_dan).read()      # không đóng tường minh\n    xu_ly(du_lieu)\n# CPython: file đóng ngay khi hết tham chiếu\n# Bản khác: file mở hàng phút ⇒ \"Too many open files\"\n\n# ✓ Đúng ở mọi nơi\nfor duong_dan in danh_sach_10000_file:\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        xu_ly(f.read())\n```\n\nĐây chính là lời giải thích cho quy tắc \"luôn dùng `with`\" ở bài context manager: nó không phải chuyện phong cách mà là **chuyện đúng sai khi đổi môi trường chạy**.\n\n### Bộ cấp phát và bộ nhớ trả về hệ điều hành\n\nMột điểm hay gây bối rối khi đo:\n\n```python\nimport gc\n\nds = [object() for _ in range(1_000_000)]\ndel ds\ngc.collect()\n# ⚠️ Bộ nhớ tiến trình (nhìn từ hệ điều hành) có thể KHÔNG giảm\n```\n\nLý do: CPython có bộ cấp phát riêng cho object nhỏ. Khi object bị huỷ, bộ nhớ trả về **kho nội bộ của Python**, không nhất thiết trả về hệ điều hành. Python giữ lại để tái sử dụng cho object sau.\n\nHệ quả thực dụng:\n\n```\n· \"Bộ nhớ không giảm\" ≠ rò rỉ\n· Đo bằng công cụ hệ điều hành có thể gây hiểu nhầm\n· Dùng tracemalloc để đo bộ nhớ Python cấp phát — chính xác hơn\n```\n\nBài cuối module sẽ nói kỹ về đo đạc.",

vi_du: "### Theo dõi vòng đời object\n\n```python\nimport sys, gc\n\nclass TheoDoi:\n    def __init__(self, ten):\n        self.ten = ten\n        print(f\"  + tạo {ten}\")\n\n    def __del__(self):\n        print(f\"  - huỷ {self.ten}\")\n\n\nprint(\"① Đếm tham chiếu — huỷ ngay\")\na = TheoDoi(\"a\")\ndel a                       # \"- huỷ a\" in NGAY\n\nprint(\"② Còn tham chiếu khác\")\nb = TheoDoi(\"b\")\nc = b\ndel b                       # không in gì\nprint(\"  (b đã del, c vẫn giữ)\")\ndel c                       # giờ mới in\n\nprint(\"③ Trong container\")\nkho = [TheoDoi(\"d\")]\nkho.clear()                 # \"- huỷ d\"\n\nprint(\"④ Hàm kết thúc\")\ndef f():\n    e = TheoDoi(\"e\")\n    return \"xong\"\nf()                         # \"- huỷ e\" khi hàm kết thúc\n```\n\nBốn trường hợp, cùng một quy tắc: **bộ đếm về 0 thì huỷ ngay.** Không có đợt quét nào tham gia.\n\n### Vòng — object không chết\n\n```python\nimport gc\n\nclass Nut:\n    def __init__(self, ten):\n        self.ten = ten\n        self.lien_ket = None\n    def __del__(self):\n        print(f\"  - huỷ {self.ten}\")\n\n\nprint(\"KHÔNG vòng:\")\na = Nut(\"a\")\ndel a                       # \"- huỷ a\" ngay\n\nprint(\"CÓ vòng:\")\nx = Nut(\"x\")\ny = Nut(\"y\")\nx.lien_ket = y\ny.lien_ket = x              # ⭐ vòng\ndel x, y\nprint(\"  (đã del, chưa thấy huỷ)\")\n\nn = gc.collect()            # ⭐ bộ dọn vòng vào cuộc\nprint(f\"  gc dọn {n} object\")\n# \"- huỷ x\" và \"- huỷ y\" in ra ở ĐÂY\n```\n\nSự chênh lệch giữa hai trường hợp là toàn bộ lý do bộ dọn vòng tồn tại.\n\nĐo bằng số liệu thay vì `__del__`:\n\n```python\nimport gc\n\ndef tao_vong(n):\n    for _ in range(n):\n        a, b = [], []\n        a.append(b); b.append(a)\n        # ra khỏi vòng lặp: a, b mất tên nhưng vẫn giữ nhau\n\ngc.collect()\ntruoc = len(gc.get_objects())\ntao_vong(10_000)\ngiua = len(gc.get_objects())\ngc.collect()\nsau = len(gc.get_objects())\n\nprint(f\"trước: {truoc:,}\")\nprint(f\"sau khi tạo vòng: {giua:,}  (+{giua-truoc:,})\")\nprint(f\"sau gc.collect(): {sau:,}  (dọn {giua-sau:,})\")\n```\n\n### Ngưỡng và thế hệ\n\n```python\nimport gc\n\nprint(f\"ngưỡng : {gc.get_threshold()}\")     # (700, 10, 10)\nprint(f\"đếm    : {gc.get_count()}\")\n\n# Tạo nhiều object để thấy bộ đếm tăng\nds = [[] for _ in range(500)]\nprint(f\"sau 500 list: {gc.get_count()}\")\n\nds2 = [[] for _ in range(500)]\nprint(f\"sau 1000    : {gc.get_count()}\")    # thế hệ 0 đã bị quét, số reset\n\n# Số object trong mỗi thế hệ\nfor i in range(3):\n    print(f\"thế hệ {i}: {len(gc.get_objects(i)):,} object\")\n```\n\nKiểm chứng \"object sống lâu chuyển lên thế hệ già\":\n\n```python\nimport gc\n\nsong_lau = [[] for _ in range(100)]     # giữ tham chiếu ⇒ không chết\n\nfor i in range(3):\n    gc.collect(0)                        # quét thế hệ 0 nhiều lần\n\nprint(f\"thế hệ 0: {len(gc.get_objects(0)):,}\")\nprint(f\"thế hệ 1: {len(gc.get_objects(1)):,}\")\nprint(f\"thế hệ 2: {len(gc.get_objects(2)):,}\")\n# Các list còn sống đã chuyển dần lên thế hệ cao hơn\n```\n\nĐây là ý tưởng cốt lõi của bộ dọn theo thế hệ: **object đã sống sót vài đợt thì ít khả năng chết**, nên quét chúng thường xuyên là phí công.\n\n### Tắt bộ dọn khi nạp dữ liệu lớn\n\n```python\nimport gc, time\n\ndef nap(n):\n    return [{\"id\": i, \"the\": [i, i+1]} for i in range(n)]\n\nn = 500_000\n\ngc.collect()\nt = time.perf_counter()\nd1 = nap(n)\nprint(f\"gc bật : {time.perf_counter()-t:.3f}s\")\ndel d1; gc.collect()\n\ngc.disable()\nt = time.perf_counter()\nd2 = nap(n)\nprint(f\"gc tắt : {time.perf_counter()-t:.3f}s\")\ngc.enable()\ndel d2; gc.collect()\n```\n\nBản tắt thường nhanh hơn 10–30% cho tác vụ cấp phát dồn dập. Lý do: trong lúc nạp, ngưỡng bị vượt liên tục và bộ dọn quét đi quét lại **những object đang sống**, không tìm được rác nào.\n\nMẫu an toàn để dùng:\n\n```python\nimport gc\nfrom contextlib import contextmanager\n\n@contextmanager\ndef tam_tat_gc():\n    da_bat = gc.isenabled()\n    gc.disable()\n    try:\n        yield\n    finally:\n        if da_bat:\n            gc.enable()          # ⭐ khôi phục đúng trạng thái cũ\n\nwith tam_tat_gc():\n    du_lieu = nap(500_000)\ngc.collect()\n```\n\nContext manager đảm bảo bật lại **kể cả khi có ngoại lệ** — đúng bài học ở module 5. Và nó kiểm tra trạng thái cũ thay vì bật vô điều kiện.\n\n### `__del__` — bốn vấn đề\n\n```python\nimport gc\n\n# ① Thời điểm không đảm bảo\nclass A:\n    def __del__(self): print(\"A huỷ\")\n\na = A(); b = a\ndel a                   # không in\ndel b                   # giờ mới in\n\n\n# ② Ngoại lệ bị nuốt\nclass B:\n    def __del__(self):\n        raise ValueError(\"lỗi này không bắt được\")\n\ntry:\n    b = B()\n    del b\n    print(\"không có ngoại lệ nào bay lên ⚠️\")\nexcept ValueError:\n    print(\"không bao giờ tới đây\")\n\n\n# ③ Trong vòng — chỉ chạy khi gc quét\nclass C:\n    def __init__(self, ten): self.ten, self.k = ten, None\n    def __del__(self): print(f\"C {self.ten} huỷ\")\n\nx, y = C(\"x\"), C(\"y\")\nx.k = y; y.k = x\ndel x, y\nprint(\"  đã del, chưa huỷ\")\ngc.collect()\nprint(\"  sau gc.collect()\")\n\n\n# ④ Thứ tự lúc thoát chương trình không xác định\n#    (chạy trong script để thấy — module có thể đã bị dọn)\n```\n\nBốn vấn đề này gộp lại thành một lời khuyên: **đừng dùng `__del__` để dọn tài nguyên.**\n\n### Ba cách dọn tài nguyên, so sánh\n\n```python\nimport weakref\n\n# ❌ __del__ — thời điểm mơ hồ\nclass Kho1:\n    def __init__(self, p): self.f = open(p, encoding=\"utf-8\")\n    def __del__(self):\n        self.f.close()\n\n\n# ✓ Context manager — thời điểm RÕ RÀNG\nclass Kho2:\n    def __init__(self, p): self.p = p; self.f = None\n    def __enter__(self):\n        self.f = open(self.p, encoding=\"utf-8\")\n        return self\n    def __exit__(self, *a):\n        if self.f: self.f.close()\n        return False\n\nwith Kho2(\"a.txt\") as k:\n    ...                     # ⭐ đóng chắc chắn ở đây\n\n\n# ✓ weakref.finalize — lưới an toàn, gọi được tường minh\nclass Kho3:\n    def __init__(self, p):\n        self.f = open(p, encoding=\"utf-8\")\n        self._xong = weakref.finalize(self, self.f.close)\n\n    def dong(self):\n        self._xong()         # ⭐ chỉ chạy MỘT lần, kể cả gọi nhiều lần\n\n    @property\n    def con_mo(self):\n        return self._xong.alive\n\nk = Kho3(\"a.txt\")\nprint(k.con_mo)             # True\nk.dong()\nprint(k.con_mo)             # False\nk.dong()                    # ✓ gọi lại vô hại\n```\n\nBa ưu điểm của `finalize` so với `__del__`:\n\n```\n· Gọi tường minh được — không phải chờ bộ dọn\n· Chỉ chạy ĐÚNG MỘT lần\n· Có .alive để kiểm tra trạng thái\n· Không giữ tham chiếu mạnh ⇒ không tạo vòng\n```\n\n### `weakref` — cache tự dọn\n\n```python\nimport weakref, gc\n\nclass AnhLon:\n    def __init__(self, ma):\n        self.ma = ma\n        self.du_lieu = bytearray(1024 * 1024)     # 1 MB\n    def __repr__(self):\n        return f\"AnhLon({self.ma!r})\"\n\n\n# ❌ dict thường — giữ object sống mãi\ncache_thuong = {}\nfor i in range(3):\n    a = AnhLon(f\"anh{i}\")\n    cache_thuong[a.ma] = a\ndel a\ngc.collect()\nprint(f\"cache thường: {len(cache_thuong)} mục\")     # 3 — vẫn giữ 3 MB\n\n\n# ✓ WeakValueDictionary — tự biến mất\ncache_yeu = weakref.WeakValueDictionary()\ngiu = []\nfor i in range(3):\n    a = AnhLon(f\"anh{i}\")\n    cache_yeu[a.ma] = a\n    if i == 0:\n        giu.append(a)        # ⭐ chỉ giữ cái đầu\ndel a\ngc.collect()\nprint(f\"cache yếu   : {len(cache_yeu)} mục\")        # 1 — chỉ cái còn được giữ\nprint(f\"còn lại     : {list(cache_yeu.keys())}\")\n```\n\nĐánh đổi rõ ràng: `WeakValueDictionary` **không giữ được gì cả**. Nó chỉ hữu ích khi object đã có nơi khác giữ, và cache chỉ là lối tắt tra cứu.\n\nMuốn cache thật sự giữ dữ liệu thì dùng cache có giới hạn:\n\n```python\nfrom functools import lru_cache\n\n@lru_cache(maxsize=100)      # ⭐ giữ, nhưng có trần\ndef tai_anh(ma: str) -> AnhLon:\n    return AnhLon(ma)\n\ntai_anh.cache_info()\ntai_anh.cache_clear()\n```\n\n### Phá vòng cây bằng `weakref`\n\n```python\nimport weakref, gc\n\nclass Nut:\n    def __init__(self, ten):\n        self.ten = ten\n        self.con: list = []\n        self._cha = None\n\n    @property\n    def cha(self):\n        return self._cha() if self._cha is not None else None\n\n    def them(self, c: \"Nut\") -> \"Nut\":\n        c._cha = weakref.ref(self)      # ⭐ YẾU lên trên\n        self.con.append(c)               # ⭐ MẠNH xuống dưới\n        return c\n\n    def __repr__(self):\n        return f\"Nut({self.ten!r})\"\n\n    def __del__(self):\n        print(f\"  - huỷ {self.ten}\")\n\n\ngoc = Nut(\"gốc\")\na = goc.them(Nut(\"a\"))\nb = a.them(Nut(\"b\"))\n\nprint(f\"cha của b: {b.cha}\")        # Nut('a')\nprint(f\"cha của gốc: {goc.cha}\")    # None\n\ndel goc, a, b\nprint(\"đã del — cả cây huỷ NGAY, không cần gc\")\n```\n\nSo với dùng tham chiếu mạnh hai chiều: cây sẽ chỉ được dọn khi bộ dọn vòng quét tới, tức là muộn hơn và tốn công quét.\n\nQuy tắc đáng nhớ cho mọi cấu trúc có liên kết ngược: **mạnh theo chiều sở hữu, yếu theo chiều tham chiếu.**\n\n### Năm hình dạng của rò rỉ\n\n```python\nimport gc, sys, traceback\nfrom collections import deque\nfrom functools import lru_cache\n\n# ① Cache không giới hạn\nCACHE_XAU = {}\ndef tinh_xau(x):\n    if x not in CACHE_XAU:\n        CACHE_XAU[x] = x ** 2\n    return CACHE_XAU[x]\n\nfor i in range(100_000):\n    tinh_xau(i)\nprint(f\"cache xấu: {len(CACHE_XAU):,} mục\")     # tăng mãi\n\n@lru_cache(maxsize=1000)                         # ✓\ndef tinh_tot(x): return x ** 2\n\nfor i in range(100_000):\n    tinh_tot(i)\nprint(f\"cache tốt: {tinh_tot.cache_info().currsize} mục\")   # tối đa 1000\n\n\n# ② Danh sách toàn cục\nLICH_SU_XAU = []\nLICH_SU_TOT = deque(maxlen=1000)                 # ✓\n\n\n# ③ Closure giữ dữ liệu lớn\ndef tao_xau():\n    lon = bytearray(10 * 1024 * 1024)            # 10 MB\n    return lambda: len(lon)                       # ⚠️ giữ cả 10 MB\n\ndef tao_tot():\n    n = len(bytearray(10 * 1024 * 1024))          # ⭐ chỉ giữ số\n    return lambda: n\n\nf_xau, f_tot = tao_xau(), tao_tot()\nprint(f\"closure xấu giữ: {sys.getsizeof(f_xau.__closure__[0].cell_contents):,} byte\")\nprint(f\"closure tốt giữ: {sys.getsizeof(f_tot.__closure__[0].cell_contents):,} byte\")\n\n\n# ④ Lưu object ngoại lệ\nLOI_XAU, LOI_TOT = [], []\n\ndef gay_loi():\n    du_lieu_lon = bytearray(5 * 1024 * 1024)     # 5 MB biến cục bộ\n    raise ValueError(\"lỗi\")\n\ntry:\n    gay_loi()\nexcept ValueError as e:\n    LOI_XAU.append(e)                # ⚠️ traceback giữ khung stack ⇒ giữ 5 MB\n    LOI_TOT.append(traceback.format_exc())       # ✓ chỉ chuỗi\n\nprint(f\"traceback có khung stack: {LOI_XAU[0].__traceback__ is not None}\")\n```\n\nTrường hợp ④ là nguồn rò rỉ tinh vi nhất: lưu một object ngoại lệ có thể giữ sống **toàn bộ biến cục bộ tại thời điểm lỗi** qua chuỗi traceback.\n\n### Tìm nguồn giữ tham chiếu\n\n```python\nimport gc\n\nclass DuLieu:\n    def __init__(self, ten): self.ten = ten\n    def __repr__(self): return f\"DuLieu({self.ten!r})\"\n\n\nd = DuLieu(\"quan trọng\")\nkho = [d]\nbang = {\"khoa\": d}\n\nclass Giu:\n    def __init__(self, x): self.x = x\ng = Giu(d)\n\n# Ai đang giữ d?\nprint(f\"refcount: {sys.getrefcount(d) - 1}\")\nfor nguoi_giu in gc.get_referrers(d):\n    mo_ta = type(nguoi_giu).__name__\n    if isinstance(nguoi_giu, dict) and \"khoa\" in nguoi_giu:\n        mo_ta += \" (bang)\"\n    elif isinstance(nguoi_giu, list):\n        mo_ta += \" (kho)\"\n    print(f\"  giữ bởi: {mo_ta}\")\n\n# Thả từng cái\nkho.clear(); bang.clear(); del g\nprint(f\"sau khi dọn, refcount: {sys.getrefcount(d) - 1}\")     # 1 — chỉ tên d\n```\n\n`gc.get_referrers` là công cụ chính khi hỏi *\"vì sao object này chưa chết\"*. Kết quả có thể lẫn khung stack của chính đoạn kiểm tra — lọc bớt khi đọc.\n\n### Bộ dò rò rỉ đơn giản\n\n```python\nimport gc\nfrom collections import Counter\n\ndef chup_so_object() -> Counter:\n    \"\"\"Đếm object theo kiểu.\"\"\"\n    gc.collect()\n    return Counter(type(o).__name__ for o in gc.get_objects())\n\n\ndef so_sanh(truoc: Counter, sau: Counter, top: int = 10) -> None:\n    chenh = Counter()\n    for k in set(truoc) | set(sau):\n        d = sau[k] - truoc[k]\n        if d > 0:\n            chenh[k] = d\n    print(f\"{'kiểu':<24} {'tăng':>8}\")\n    for k, v in chenh.most_common(top):\n        print(f\"{k:<24} {v:>8,}\")\n\n\n# Dùng: chụp trước và sau một chu kỳ nghiệp vụ\ntruoc = chup_so_object()\n\nfor _ in range(1000):\n    xu_ly_mot_yeu_cau()          # thay bằng hàm thật\n\nsau = chup_so_object()\nso_sanh(truoc, sau)\n```\n\nCách dùng đúng: chạy **cùng một chu kỳ nghiệp vụ nhiều lần** và so số object trước/sau. Kiểu nào tăng đều qua các chu kỳ là ứng viên rò rỉ.\n\nLưu ý: `gc.get_objects()` chỉ liệt kê object **được theo dõi** — không có `int`, `str` đơn lẻ. Với rò rỉ dạng chuỗi thì cần `tracemalloc`, và bài sau sẽ nói.\n\n### Vì sao \"bộ nhớ không giảm\" không phải rò rỉ\n\n```python\nimport gc, tracemalloc\n\ntracemalloc.start()\n\nnen = tracemalloc.take_snapshot()\n\nds = [object() for _ in range(500_000)]\nsau_cap = tracemalloc.take_snapshot()\n\ndel ds\ngc.collect()\nsau_don = tracemalloc.take_snapshot()\n\nfor ten, snap in ((\"sau cấp phát\", sau_cap), (\"sau dọn\", sau_don)):\n    tk = snap.compare_to(nen, \"lineno\")\n    tong = sum(s.size_diff for s in tk)\n    print(f\"{ten:<16} {tong/1024/1024:>8.1f} MB so với nền\")\n\ntracemalloc.stop()\n```\n\n`tracemalloc` cho thấy Python **đã giải phóng** bộ nhớ, dù công cụ hệ điều hành có thể vẫn báo tiến trình chiếm nhiều — vì CPython giữ lại các khối để tái sử dụng.\n\nĐây là lý do chẩn đoán rò rỉ phải dùng công cụ ở tầng Python, không dựa vào số liệu tiến trình.\n\n### Phụ thuộc bản hiện thực — vì sao `with` bắt buộc\n\n```python\nimport gc\n\nclass MoFile:\n    def __init__(self, ten):\n        self.ten = ten\n        print(f\"  mở {ten}\")\n    def __del__(self):\n        print(f\"  đóng {self.ten}\")\n\n\nprint(\"CPython — đếm tham chiếu đóng ngay:\")\nfor i in range(3):\n    f = MoFile(f\"file{i}\")\n    # không đóng tường minh\n# Mỗi vòng, f bị gán lại ⇒ file cũ hết tham chiếu ⇒ đóng ngay\n\nprint(\"\\nVới with — đúng ở MỌI bản hiện thực:\")\nfrom contextlib import contextmanager\n\n@contextmanager\ndef mo(ten):\n    f = MoFile(ten)\n    try:\n        yield f\n    finally:\n        print(f\"  đóng {ten} (tường minh)\")\n\nfor i in range(3):\n    with mo(f\"file{i}\") as f:\n        pass\n```\n\nBản đầu **chỉ đúng nhờ chi tiết cài đặt của CPython**. Bản sau đúng ở mọi nơi, và đó là toàn bộ lý do quy tắc \"luôn dùng `with`\" tồn tại.",

so_sanh: "### Hai cơ chế của CPython\n\n| | Đếm tham chiếu | Bộ dọn theo thế hệ |\n|---|---|---|\n| Vai trò | **dọn phần lớn rác** | chỉ dọn **vòng** |\n| Thời điểm | **ngay lập tức** | định kỳ, không xác định |\n| Chi phí | trải đều mỗi phép gán | dồn vào lúc quét |\n| Dọn được vòng | **✗** | ✓ |\n| Tắt được | ✗ | **✓ `gc.disable()`** |\n| Object nào tham gia | mọi object | chỉ object **có thể chứa tham chiếu** |\n\nHàng cuối là tối ưu quan trọng: `int`, `str`, `float` không bao giờ đi qua bộ dọn vòng.\n\n### Ưu và nhược của đếm tham chiếu\n\n| Ưu | Nhược |\n|---|---|\n| Huỷ **tức thì** — tài nguyên trả ngay | 8 byte mỗi object cho bộ đếm |\n| Thời điểm **dự đoán được** | mỗi phép gán phải cập nhật |\n| Không có khoảng dừng dài | **không dọn được vòng** |\n| Đơn giản, dễ suy luận | ghi vào object ⇒ khó chia sẻ giữa luồng |\n\nHàng cuối dẫn thẳng tới bài GIL: bộ đếm nằm trong object nên nhiều luồng cùng chạm vào là phải đồng bộ hoá.\n\n### Ba thế hệ\n\n| | Thế hệ 0 | Thế hệ 1 | Thế hệ 2 |\n|---|---|---|---|\n| Chứa | object mới | sống qua 1 đợt | sống qua nhiều đợt |\n| Tần suất quét | **cao nhất** | vừa | **thấp nhất** |\n| Ngưỡng mặc định | 700 | 10 (đợt gen-0) | 10 (đợt gen-1) |\n| Giả định | phần lớn chết trẻ | | đã sống thì sống lâu |\n\nGiả định ở hàng cuối là quan sát thực nghiệm, và nó đúng với hầu hết chương trình — nên quét nhóm già thường xuyên là phí công.\n\n### `__del__`, `with`, `weakref.finalize`\n\n| | `__del__` | `with` | `weakref.finalize` |\n|---|---|---|---|\n| Thời điểm | **không đảm bảo** | **rõ ràng** | khi object chết, hoặc gọi tay |\n| Gọi tường minh | ✗ | ✓ | **✓** |\n| Chạy đúng một lần | ✗ | ✓ | **✓** |\n| Ngoại lệ bay lên | **✗ bị nuốt** | ✓ | ✓ |\n| Kiểm tra trạng thái | ✗ | — | **✓ `.alive`** |\n| Nên dùng | **hiếm khi** | **mặc định** | lưới an toàn |\n\nCột giữa là lựa chọn đúng cho hầu hết trường hợp. Cột phải bổ sung khi bạn không kiểm soát được nơi object bị bỏ.\n\n### Năm hình dạng rò rỉ\n\n| Hình dạng | Dấu hiệu | Cách sửa |\n|---|---|---|\n| Cache không giới hạn | `dict` toàn cục lớn dần | `lru_cache(maxsize=...)` |\n| Danh sách tích luỹ | `list` toàn cục chỉ append | `deque(maxlen=...)` |\n| Observer không huỷ đăng ký | số handler tăng | huỷ đăng ký, hoặc `WeakSet` |\n| Closure giữ dữ liệu lớn | hàm nhỏ mà tốn nhiều bộ nhớ | chỉ giữ thứ cần |\n| **Lưu object ngoại lệ** | tăng đột biến sau lỗi | lưu `format_exc()` |\n\nHàng cuối tinh vi nhất: một ngoại lệ giữ traceback, traceback giữ mọi khung stack và biến cục bộ tại thời điểm lỗi.\n\n### `weakref` — ba loại\n\n| | `ref` | `WeakValueDictionary` | `WeakSet` |\n|---|---|---|---|\n| Giữ object sống | ✗ | ✗ | ✗ |\n| Dùng cho | tham chiếu ngược | **cache** | **observer** |\n| Truy cập | `r()` trả `None` nếu chết | như `dict` | như `set` |\n| Rủi ro | quên kiểm tra `None` | **object biến mất bất ngờ** | như trên |\n\nRủi ro chung: object **phải được giữ ở nơi khác**. Nếu không, nó biến mất ngay — và đó là lý do nhiều thư viện chọn tham chiếu mạnh kèm huỷ đăng ký tường minh.\n\n### CPython và bản hiện thực khác\n\n| | CPython | Bản dùng GC đánh dấu-quét |\n|---|---|---|\n| Cơ chế chính | **đếm tham chiếu** | chỉ GC |\n| Object chết khi | hết tham chiếu, **ngay** | khi GC quét tới |\n| File tự đóng | thường có | **không** |\n| `with` bắt buộc | nên | **bắt buộc** |\n| Tốc độ tổng thể | trải đều | thường nhanh hơn, có khoảng dừng |\n\nHàng thứ ba là lý do thực tế nhất để luôn dùng `with`: code dựa vào đếm tham chiếu **chạy được trên CPython và hỏng ở nơi khác**.\n\n### Khi nào can thiệp vào `gc`\n\n| Tình huống | Làm gì |\n|---|---|\n| Nạp dữ liệu lớn một lần | **`gc.disable()` rồi bật lại** |\n| Ứng dụng nhạy độ trễ | tự gọi `collect()` vào lúc rảnh |\n| Nghi rò rỉ | `get_referrers`, `DEBUG_SAVEALL`, đếm theo kiểu |\n| Chương trình ngắn không tạo vòng | tắt hẳn, chấp nhận được |\n| **Bình thường** | **không đụng vào** |\n\nHàng cuối là mặc định đúng: bộ dọn được chỉnh cho hầu hết chương trình, và can thiệp sai gây rò rỉ thật.",

loi_thuong_gap: [
  "**Dựa vào đếm tham chiếu để đóng file.** Chạy được trên CPython, hỏng ở bản hiện thực khác. Dùng `with`.",
  "**Dùng `__del__` để dọn tài nguyên.** Thời điểm không đảm bảo, ngoại lệ bị nuốt, thứ tự lúc thoát không xác định.",
  "**Tưởng `gc.collect()` dọn mọi thứ.** Nó chỉ dọn **vòng**; phần lớn rác đã được đếm tham chiếu dọn từ trước.",
  "**Tắt `gc` rồi quên bật lại.** Chương trình chạy lâu có tạo vòng sẽ rò rỉ chắc chắn. Dùng context manager.",
  "**Coi \"bộ nhớ tiến trình không giảm\" là rò rỉ.** CPython giữ lại khối để tái sử dụng; đo bằng `tracemalloc`.",
  "**Cache bằng `dict` không giới hạn kích thước.** Nguồn rò rỉ phổ biến nhất. Dùng `lru_cache(maxsize=...)`.",
  "**Lưu object ngoại lệ vào danh sách.** Traceback giữ mọi khung stack và biến cục bộ — có thể hàng chục megabyte.",
  "**Closure giữ dữ liệu lớn ngoài ý muốn.** Chỉ giữ giá trị cần, không giữ cả object gốc.",
  "**Dùng `weakref` mà quên object phải được giữ ở nơi khác.** Nó biến mất ngay và không bao giờ hoạt động.",
  "**Quên kiểm tra `weakref.ref()` trả `None`.** Object có thể đã chết giữa lúc tạo tham chiếu và lúc dùng.",
  "**Quên `gc.garbage.clear()` sau khi dùng `DEBUG_SAVEALL`.** Chính danh sách đó giữ rác sống.",
  "**Tham chiếu mạnh hai chiều trong cấu trúc cây.** Tạo vòng không cần thiết; dùng yếu cho liên kết ngược lên cha.",
  "**Tin `gc.get_objects()` liệt kê mọi object.** Nó chỉ có object được theo dõi — không có `int`, `str` đơn lẻ."
],

tu_kiem_tra: [
  "CPython dùng mấy cơ chế dọn rác? Mỗi cơ chế lo phần nào?",
  "Nêu ba ưu và ba nhược của đếm tham chiếu. Nhược điểm nào là vấn đề thật sự?",
  "Vì sao tham chiếu vòng không được đếm tham chiếu dọn? Vẽ sơ đồ.",
  "Nêu bốn tình huống tạo vòng tự nhiên. Vì sao Python tự xoá tên ngoại lệ sau khối `except`?",
  "Bộ dọn theo thế hệ dựa trên quan sát thực nghiệm nào? Ba thế hệ khác nhau ra sao?",
  "`(700, 10, 10)` nghĩa là gì? Con số 700 đếm cái gì chính xác?",
  "Mô tả năm bước bộ dọn tìm rác vòng. Ý tưởng cốt lõi ở bước nào?",
  "Vì sao `int` và `str` không được bộ dọn theo dõi? `tuple` thì sao?",
  "Nêu bốn vấn đề của `__del__`. Hai cách thay thế và ưu điểm của mỗi cách?",
  "`weakref.finalize` hơn `__del__` ở bốn điểm nào?",
  "Nêu ba ứng dụng của `weakref` và đánh đổi chung của chúng.",
  "Quy tắc \"mạnh xuống dưới, yếu lên trên\" áp dụng cho cấu trúc nào và giải quyết gì?",
  "Nêu năm hình dạng rò rỉ trong Python. Cái nào tinh vi và tốn bộ nhớ nhất?",
  "Vì sao \"bộ nhớ tiến trình không giảm\" chưa chắc là rò rỉ?",
  "Vì sao quy tắc \"luôn dùng `with`\" là chuyện đúng sai chứ không phải phong cách?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, module `gc` — mọi hàm điều khiển, ý nghĩa ngưỡng, và cờ gỡ lỗi.",
  "Tài liệu Python, module `weakref` — `ref`, `finalize`, `WeakValueDictionary`, `WeakSet` kèm ví dụ.",
  "Tài liệu Python, mục *Data model* phần `__del__` — nêu rõ các cảnh báo về thời điểm và ngoại lệ.",
  "Sách **CPython Internals** — Anthony Shaw. Chương về quản lý bộ nhớ, bộ cấp phát và bộ dọn theo thế hệ.",
  "Tài liệu Python, module `tracemalloc` — công cụ chuẩn để đo bộ nhớ Python cấp phát và tìm rò rỉ."
],

lien_he: [
  "**Tham chiếu vs giá trị**: `ob_refcnt` trong phần đầu object chính là bộ đếm mà bài này nói về.",
  "**Shallow copy vs deep copy** ngay trước: tham chiếu vòng ở đó là thứ đếm tham chiếu không dọn được.",
  "**GIL** ngay sau: bộ đếm nằm trong object nên nhiều luồng cùng chạm là phải đồng bộ — một trong những lý do GIL tồn tại.",
  "**Đo bộ nhớ và tối ưu** cuối module: `tracemalloc` và cách phân biệt rò rỉ thật với bộ nhớ được giữ lại.",
  "**Context manager và `with`** ở module 5: \"đừng dựa vào bộ thu gom rác\" — bài này là lời giải thích đầy đủ.",
  "**Decorator** ở module 5: `lru_cache(maxsize=None)` là rò rỉ; đây là lý do phải đặt trần.",
  "**Design pattern — Observer** ở module 6: rò rỉ do danh sách handler, và `WeakSet` là một cách xử lý.",
  "**Xử lý ngoại lệ** ở module 5: lưu object ngoại lệ giữ cả khung stack — lý do nên lưu `format_exc()`.",
  "**Hệ điều hành** ở module 2: bộ nhớ ảo và việc tiến trình giữ lại trang nhớ giải thích vì sao số liệu hệ điều hành gây hiểu nhầm.",
  "**Backend & Web** ở mảng 05: dịch vụ chạy lâu tăng bộ nhớ dần là sự cố phổ biến, và năm hình dạng rò rỉ ở đây là danh sách kiểm tra.",
  "**AI/ML** ở mảng 07: giữ tham chiếu tới tensor trung gian hoặc lưu lịch sử huấn luyện không giới hạn là hai dạng rò rỉ hay gặp."
]

};
