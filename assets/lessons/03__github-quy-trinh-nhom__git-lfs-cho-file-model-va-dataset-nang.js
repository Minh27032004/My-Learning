window.LESSONS["03:github-quy-trinh-nhom:git-lfs-cho-file-model-va-dataset-nang"] = {

tom_tat: [
  "Git LFS giữ file pointer nhỏ trong lịch sử Git, còn nội dung binary lớn được lưu dưới dạng object trên LFS server và tải qua filter khi checkout.",
  "`git lfs track` chỉ tạo hoặc sửa quy tắc trong `.gitattributes`; phải commit file này trước hoặc cùng lúc với file lớn để mọi máy áp dụng giống nhau.",
  "Tracking từ hôm nay không biến các blob lớn đã commit trong quá khứ thành LFS; migration lịch sử là thao tác viết lại commit, cần kế hoạch phối hợp riêng.",
  "LFS giảm sức nặng của Git object database nhưng không làm dữ liệu biến mất: storage, bandwidth, quyền truy cập, backup và chi phí vẫn phải được quản trị.",
  "Dataset thay đổi thường xuyên hoặc cần truy vấn từng phần có thể phù hợp object storage, registry hay công cụ version dữ liệu hơn Git LFS."
],

can_biet_truoc: [
  "Hiểu commit là snapshot, blob được giữ trong lịch sử và xóa file ở commit mới không xóa blob ở commit cũ.",
  "Biết `.gitignore` loại file chưa được theo dõi, còn `.gitattributes` gán hành vi cho đường dẫn đã theo dõi.",
  "Biết clone, checkout, fetch, pull, push và remote; phân biệt repository local với nơi lưu trên GitHub.",
  "Hiểu checksum SHA-256 dùng định danh nội dung và pointer không phải chính model hoặc dataset."
],

dinh_nghia: "**Git Large File Storage (Git LFS)** là phần mở rộng của Git dùng cho nội dung lớn, đặc biệt binary. Trong commit, đường dẫn được lưu bằng một **pointer file** nhỏ chứa đặc tả, object ID theo SHA-256 và kích thước. Nội dung thật được chuyển vào kho LFS của remote. Khi checkout, filter `smudge` hoặc cơ chế tương đương lấy object từ LFS server và đặt nội dung thật vào working tree; khi add, filter `clean` thay nội dung staged bằng pointer.\n\nVì vậy Git LFS không phải nén file, không phải `.gitignore`, cũng không phải dịch vụ đồng bộ độc lập. Git vẫn version đường dẫn và pointer; LFS thay nơi giữ payload lớn. Hai phần phải khớp: clone có commit mà thiếu LFS object chỉ nhận pointer hoặc lỗi tải, còn object được upload mà pointer chưa push thì lịch sử không tham chiếu nó.",

vi_sao_can: "`rainfall-cli` ban đầu chỉ có source và CSV mẫu vài dòng. Sau đó nhóm thêm `models/rainfall.bin` và `data/history.csv`. Binary model thay đổi nhỏ về trọng số nhưng Git thường phải lưu blob mới gần như toàn bộ; lịch sử tăng mãi dù working tree chỉ cần phiên bản hiện tại. Mỗi clone phải tải các blob cũ, làm onboarding và CI chậm, còn diff binary hầu như không giúp review.\n\nLFS thay các blob đó bằng pointer nhẹ trong Git object database. Contributor vẫn dùng tên file quen thuộc, còn payload được tải khi cần. Đây là cải thiện cấu trúc phân phối, không phải phép xóa chi phí. Remote vẫn lưu từng object LFS, clone và CI vẫn tiêu bandwidth, và dữ liệu nhạy cảm vẫn nguy hiểm nếu push lên server.\n\nGitHub còn áp giới hạn và quota tùy gói dịch vụ, có thể thay đổi theo thời gian. Vì vậy quy trình phải kiểm tài liệu và billing hiện hành thay vì chép một con số cố định vào policy sống lâu.",

co_che: "### Vì sao Git thường không hợp binary lớn\n\nGit rất mạnh khi snapshot gồm nhiều file source nhỏ và người dùng cần lịch sử phân tán. Với text, delta compression có thể giảm phần trùng giữa các phiên bản và diff có ý nghĩa. Với model nén, archive, video hoặc dữ liệu nhị phân, một thay đổi nhỏ logic có thể làm bytes đổi khắp file. Git phải giữ blob lịch sử để mọi commit checkout được; xóa file ở HEAD không làm clone quên các phiên bản cũ.\n\nGiả sử model 500 MB được cập nhật mười lần. Không được phép khẳng định repository chắc chắn thành 5 GB vì pack và delta tùy dữ liệu, nhưng hướng tăng là thật và cần đo bằng `git count-objects -vH`, kích thước pack, thời gian clone cùng quota remote. LFS tách payload khỏi luồng object Git thông thường để clone source không phải mang mọi binary qua cùng cơ chế.\n\n### Pointer thực sự chứa gì\n\nMột pointer điển hình là text ba dòng:\n\n```text\nversion https://git-lfs.github.com/spec/v1\noid sha256:4cac19622fc3ada9c0fdeadb33f88f367b541f38b89102a3f1261ac81fd5bcb5\nsize 84977953\n```\n\n`oid` định danh bytes chính xác, còn `size` giúp kiểm tính toàn vẹn và lập kế hoạch tải. Hash trong ví dụ chỉ minh họa format, không đại diện file `rainfall-cli`. Git commit hash pointer như một blob text bình thường. LFS server ánh xạ OID tới object thật.\n\nNếu mở file bằng `git show HEAD:models/rainfall.bin`, bạn thường thấy pointer; nếu mở working tree sau checkout thành công, bạn thấy binary thật. Hai góc nhìn khác nhau vì filter nằm giữa index và working tree.\n\n### Cài client và khởi tạo filter\n\nGit LFS là chương trình riêng, không nên giả định mọi bản Git đều kèm sẵn. Sau khi cài theo hệ điều hành, kiểm:\n\n```sh\ngit lfs version\ngit lfs install\ngit lfs env\n```\n\n`git lfs install` cấu hình filter cho người dùng hiện tại và có thể thiết lập hook cần thiết. Nó không tự chọn file trong repository và không upload file ngay. `git lfs env` giúp chẩn đoán endpoint, filter và thư mục local. Trên máy CI, action checkout hoặc image có thể hỗ trợ LFS nhưng phải bật rõ; “máy tôi đã install” không truyền sang runner.\n\n### `track` tạo chính sách đường dẫn\n\nTrong `rainfall-cli`, chọn pattern có chủ đích:\n\n```sh\ngit lfs track \"models/*.bin\"\ngit lfs track \"data/*.csv\"\ngit diff -- .gitattributes\ngit add .gitattributes\ngit commit -m \"track model and historical data with LFS\"\n```\n\nLệnh ghi các thuộc tính như `filter=lfs diff=lfs merge=lfs -text` vào `.gitattributes`. File này phải được version vì nó là hợp đồng của repository. Nếu chỉ cấu hình local, đồng đội add cùng đường dẫn theo Git thường và lịch sử bị trộn.\n\nPattern tuân theo quy tắc `.gitattributes`, không hoàn toàn là glob shell. Dấu nháy ngăn shell tự bung pattern trước khi Git LFS nhận. Theo dõi toàn bộ `*.csv` có thể quá rộng: fixture CSV vài KB cần review diff sẽ bị biến thành pointer. Pattern theo thư mục thường diễn đạt ý định tốt hơn.\n\n### Add file và kiểm index trước commit\n\nSau khi có rule:\n\n```sh\ngit add models/rainfall.bin data/history.csv\ngit lfs ls-files\ngit diff --cached -- .gitattributes\ngit diff --cached --numstat\n```\n\n`git lfs ls-files` phải liệt kê đường dẫn. Có thể kiểm pointer staged bằng `git show :models/rainfall.bin`; nó phải là text spec, không đổ binary ra terminal. Working tree vẫn chứa file thật để chương trình dùng. Commit pointer và `.gitattributes` cùng một thay đổi logic giúp checkout commit đó tự mô tả đúng filter.\n\nĐừng chỉ nhìn `git status` thấy file staged rồi kết luận LFS hoạt động. Git status không phân biệt blob thường với pointer. Phép kiểm đúng là `git lfs ls-files`, attributes áp dụng và nội dung index.\n\n### Push là hai luồng phối hợp\n\nKhi push, Git gửi commit, tree và pointer qua giao thức Git; LFS client upload object còn thiếu qua LFS endpoint. Hook pre-push thường đảm bảo object cần thiết được gửi trước khi ref remote hoàn tất. Nếu upload LFS lỗi do quota, mạng hoặc quyền, push có thể thất bại.\n\nKiểm bằng:\n\n```sh\ngit lfs status\ngit lfs push --dry-run origin main\ngit push origin main\n```\n\nKhông nên dùng `git lfs push --all` như phản xạ; nó có thể gửi nhiều object lịch sử, tốn thời gian và quota. Chỉ dùng khi migration hoặc phục hồi remote có kế hoạch rõ. Sau push quan trọng, clone vào thư mục tạm là bằng chứng end-to-end tốt hơn việc máy tác giả mở được file từ cache local.\n\n### Clone, fetch và checkout lấy payload ra sao\n\nClone bình thường với LFS client đã cấu hình thường tải object cần cho checkout. Muốn lấy source trước mà chưa tải file lớn, đặt `GIT_LFS_SKIP_SMUDGE=1` cho thao tác clone, rồi chọn tải sau:\n\n```sh\nGIT_LFS_SKIP_SMUDGE=1 git clone https://github.com/example/rainfall-cli.git\ncd rainfall-cli\ngit lfs pull --include=\"models/rainfall.bin\"\n```\n\nCú pháp đặt biến ở đầu lệnh là kiểu POSIX; trong PowerShell phải dùng biến môi trường theo cú pháp PowerShell. Đây là bẫy tài liệu đa nền tảng. `git lfs fetch` tải object vào cache local nhưng không nhất thiết cập nhật working tree; `git lfs checkout` thay pointer bằng object đã có; `git lfs pull` thường kết hợp fetch và checkout cho ref hiện tại.\n\n### Include và exclude giúp CI không tải vô ích\n\nJob lint source không cần model 500 MB. Có thể checkout mà bỏ LFS, hoặc cấu hình include chỉ file cần cho integration test. Tiết kiệm này trực tiếp giảm thời gian và bandwidth. Nhưng test phải thất bại rõ khi asset bắt buộc thiếu, không âm thầm dùng model giả rồi báo xanh.\n\nTách workflow: unit test parser dùng fixture nhỏ trong Git thường; integration job tải model theo điều kiện; benchmark dataset lớn chạy theo lịch. Kiến trúc test tốt giảm sự phụ thuộc vào payload nặng thay vì dùng cache để che nó.\n\n### Tracking mới không sửa lịch sử cũ\n\nĐây là lỗi nhận thức phổ biến nhất. Nếu `models/rainfall.bin` đã được commit như blob thường hôm qua, chạy `git lfs track` hôm nay chỉ áp dụng khi file được add lại theo rule. Blob hôm qua vẫn nằm trong commit cũ và clone lịch sử vẫn mang nó.\n\nCó thể chuyển phiên bản hiện tại bằng remove khỏi index rồi add lại, nhưng điều đó vẫn không xóa quá khứ. Muốn loại blob cũ phải dùng migration như `git lfs migrate import`, thao tác sẽ viết lại commit hash. Viết lại lịch sử ảnh hưởng mọi branch, tag, fork và clone; không được làm chỉ vì repository local đang nặng.\n\n### Migration lịch sử là dự án phối hợp\n\nTrước migration cần inventory đường dẫn và dung lượng, xác định refs, backup, đóng băng push, thông báo contributor, thử trên bản clone, xác minh object LFS remote và chuẩn bị hướng dẫn re-clone. Commit hash đổi nên pull request mở, link SHA, chữ ký và build provenance có thể bị ảnh hưởng. Remote có thể cần force-push; đây là thao tác phá tương thích và phải được chủ repository phê duyệt.\n\nNếu repository đã được dùng rộng, đôi khi chấp nhận lịch sử nặng và chỉ dùng LFS từ mốc hiện tại ít rủi ro hơn. LFS không tự biện minh cho rewrite. Đo thời gian clone, chi phí và số người bị ảnh hưởng để quyết định.\n\n### Xóa file LFS cũng không xóa object ngay\n\n`git rm models/old.bin` rồi commit làm file biến mất ở commit mới, nhưng pointer cũ vẫn tham chiếu object trong lịch sử. Chính sách lưu giữ và garbage collection của LFS server quyết định khi object không còn được tính hoặc bị xóa. Xóa nhầm secret khỏi HEAD hoàn toàn không đủ; phải coi secret đã lộ, revoke nó và làm quy trình xử lý lịch sử theo nền tảng.\n\nKhông dùng LFS để lưu `.env`, khóa riêng, credential hoặc dữ liệu cá nhân chỉ vì file lớn. LFS là cơ chế lưu trữ, không phải mã hóa hay kiểm soát rò rỉ.\n\n### Locking giải quyết binary không merge được\n\nMột số binary như file thiết kế không thể merge theo dòng. Git LFS có cơ chế lock để một người tuyên bố đang sửa đường dẫn, giảm hai phiên bản cạnh tranh. Lock là quy ước phối hợp và cần server hỗ trợ; nó không thay thế giao tiếp, và người có quyền phù hợp có thể xử lý lock.\n\nModel huấn luyện thường được sinh lại thay vì chỉnh tay, nên lock có thể không hữu ích. Với dataset append theo pipeline, phân vùng bất biến theo ngày có thể tránh xung đột tốt hơn khóa một file khổng lồ. Chọn mô hình dữ liệu trước khi chọn công cụ khóa.\n\n### Archive và người không cài LFS là ca biên thật\n\nNgười tải ZIP source từ trang release có thể nhận pointer thay vì object LFS tùy cấu hình archive của repository. GitHub cho phép quản trị việc đưa LFS object vào archive, và việc tải có thể tính bandwidth. README phải nói cách lấy asset; đừng giả định ZIP giống clone.\n\nContributor không có LFS client có thể checkout pointer text và chương trình báo format model sai. Dấu hiệu là file chỉ vài dòng bắt đầu bằng spec URL. Hướng dẫn onboarding nên có `git lfs install`, `git lfs pull` và phép kiểm checksum hoặc smoke test.\n\n### Storage và bandwidth là một phần thiết kế\n\nMỗi version khác nội dung có OID mới và chiếm storage; người dùng tải object tiêu bandwidth. Cache runner giúp giảm lặp trong một số trường hợp nhưng cache cũng có quota, thời hạn và rủi ro stale. Theo dõi dashboard billing và đặt lifecycle cho artifact ngoài LFS.\n\nKhông ghi con số quota cố định vào bài học vì gói dịch vụ thay đổi. Policy nội bộ nên liên kết tài liệu hiện hành, đặt cảnh báo chi phí và owner. Một push nhầm dataset hàng chục phiên bản có thể tạo hóa đơn dù Git repository nhìn nhỏ.\n\n### Khi Git LFS là lựa chọn sai\n\nDataset terabyte, cập nhật theo partition và cần truy vấn cột phù hợp object storage cùng catalog hơn. Model phát hành theo version và metadata có thể hợp model registry. Binary build có thể để package registry hoặc release artifact. Dữ liệu sinh ra lại được thì pipeline cùng checksum đôi khi tốt hơn version từng byte.\n\nLFS hợp khi số file và tần suất thay đổi vừa phải, cần đường dẫn version cùng code, và nhóm chấp nhận tải toàn object được chọn. Nó không có partial diff, query, schema evolution hay lineage chuyên sâu. Đừng ép Git thành data lake chỉ vì lệnh quen.",

vi_du: "### Thí nghiệm pointer bằng file nhỏ\n\nKhông cần tạo file hàng trăm MB để học cơ chế. Trong repository thử nghiệm có Git LFS:\n\n```sh\nmkdir rainfall-lfs && cd rainfall-lfs\ngit init\ngit lfs install\nmkdir models data\ngit lfs track \"models/*.bin\"\nprintf 'MODEL-V1\\nweights=12,8,3\\n' > models/rainfall.bin\nprintf 'station,mm\\nhue,12\\nhue,8\\n' > data/history.csv\ngit add .gitattributes models/rainfall.bin data/history.csv\ngit lfs ls-files\ngit show :models/rainfall.bin\ngit show :data/history.csv\n```\n\nLệnh `git show` đầu in pointer vì `.bin` khớp rule. Lệnh sau in CSV thật vì ta cố ý chưa track `data/*.csv`. Sự đối chứng chứng minh rule quyết định cách **index** lưu nội dung, không phải kích thước file tự động. Sau đó mới quyết định dataset lịch sử có cần rule riêng hay fixture nhỏ nên ở Git thường.\n\n### Phát hiện pointer chưa được materialize\n\nMột smoke check POSIX có thể từ chối chạy model nếu file vẫn là pointer:\n\n```sh\nif head -n 1 models/rainfall.bin | grep -q 'git-lfs.github.com/spec'; then\n  echo 'Model chưa được tải; chạy git lfs pull' >&2\n  exit 1\nfi\n```\n\nThông báo này tốt hơn để parser binary báo lỗi khó hiểu. Tuy nhiên nó chỉ chẩn đoán một trạng thái; kiểm checksum hoặc để chính loader xác thực format vẫn cần cho file hỏng và tải thiếu.",

so_sanh: "### Chọn nơi lưu asset theo cách sử dụng\n\n| Phương án | Version cùng commit | Clone source nhẹ | Truy vấn từng phần | Review diff | Phù hợp nhất |\n|---|---|---|---|---|---|\n| Git thường | Có | Không với lịch sử lớn | Không | Tốt cho text | Source, fixture nhỏ |\n| Git LFS | Có qua pointer | Có nếu bỏ tải payload | Theo file | Kém với binary | Model, media cỡ vừa |\n| Release/package registry | Gắn tag hoặc version | Có | Theo artifact | Không | Binary phát hành |\n| Object storage | Qua key/version riêng | Có | Tốt nếu thiết kế partition | Không | Dataset rất lớn |\n| Model/data registry | Có metadata chuyên miền | Có | Tùy hệ thống | So sánh bằng metric | ML lineage, stage model |\n| `.gitignore` local | Không | Có | Không | Không | Cache, file sinh lại được |\n\nĐiểm quyết định là quan hệ với code. Nếu commit phải chỉ đúng model để tái tạo kết quả, pointer LFS có lợi. Nếu hàng nghìn job cần query một partition của dataset, object storage phù hợp hơn. Nếu file là build output có thể tạo lại, đừng version payload; version source, lockfile và quy trình build.",

loi_thuong_gap: [
  "**Track sau khi đã commit rồi tưởng lịch sử được làm nhẹ.** Clone vẫn nặng vì blob cũ còn nguyên; kiểm pack và commit lịch sử, không chỉ HEAD.",
  "**Quên commit `.gitattributes`.** Máy tác giả dùng LFS nhưng đồng đội add file như Git thường; review rule cùng file pointer.",
  "**Track pattern quá rộng.** Fixture CSV nhỏ biến thành pointer và mất diff review; dùng đường dẫn theo thư mục hoặc pattern hẹp.",
  "**Pointer xuất hiện trong working tree.** File chỉ ba dòng spec và ứng dụng báo sai format; cài LFS rồi `git lfs pull`.",
  "**Upload Git thành công nhưng LFS object thiếu.** Clone sạch báo lỗi tải object; dùng `git lfs status`, kiểm quyền, quota và remote.",
  "**Dùng LFS như kho secret.** Xóa file không thu hồi dữ liệu đã tải; revoke credential và xử lý sự cố bảo mật.",
  "**Rewrite lịch sử không phối hợp.** Đồng đội push lại commit cũ hoặc branch phân kỳ; migration phải có freeze và hướng dẫn re-clone.",
  "**CI tải mọi model ở mọi job.** Lint mất nhiều phút và tốn bandwidth; tách job, include đúng asset hoặc bỏ smudge."
],

tu_kiem_tra: [
  "Vì sao `git lfs track` thay đổi `.gitattributes` nhưng chưa upload bất kỳ payload nào?",
  "Working tree chứa binary thật trong khi `git show HEAD:path` cho pointer; filter nào giải thích hai góc nhìn?",
  "Tại sao xóa model ở commit mới không làm repository hoặc LFS storage lập tức mất mọi phiên bản cũ?",
  "Bạn sẽ kiểm gì trước commit để chắc file lớn staged dưới dạng pointer thay vì blob thường?",
  "Khi nào `GIT_LFS_SKIP_SMUDGE=1` hữu ích cho CI, và test phải phản ứng thế nào nếu asset bắt buộc thiếu?",
  "Migration bằng `git lfs migrate import` làm thay đổi commit hash; hãy liệt kê ít nhất bốn nhóm bị ảnh hưởng.",
  "Dataset cập nhật hàng giờ theo partition có phù hợp LFS không? Hãy lập luận từ kiểu truy cập, version và bandwidth thay vì chỉ từ kích thước."
],

tai_nguyen: [
  "GitHub Docs — Managing large files và About Git Large File Storage.",
  "Git LFS documentation — Getting Started, `git-lfs-track`, `git-lfs-migrate` và `git-lfs-fetch`.",
  "Pro Git — chương Git Internals để hiểu blob, tree, packfile và refs.",
  "Designing Data-Intensive Applications của Martin Kleppmann — nền tảng về lưu trữ, version và dữ liệu lớn."
],

lien_he: [
  "Nối sang mảng Git internals: pointer chỉ hữu ích khi hiểu index, blob và lý do mọi commit cũ vẫn cần object của nó.",
  "Nối sang mảng Machine Learning Engineering: model registry bổ sung metric, lineage và stage mà Git LFS không cung cấp.",
  "Nối sang mảng Cloud Storage: object storage, lifecycle và egress cost là lựa chọn tự nhiên khi dataset vượt mô hình theo file.",
  "Nối sang mảng Bảo mật: LFS không mã hóa secret; dữ liệu nhạy cảm cần phân quyền, audit, retention và quy trình thu hồi riêng.",
  "Nối sang mảng CI/CD: checkout có chọn lọc giúp job source nhẹ, còn integration test chỉ tải đúng model hoặc dataset nó cần."
]

};
