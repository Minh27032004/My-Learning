window.LESSONS["01:bo-nho-co-che-thuc-thi:tham-chieu-vs-gia-tri-bien-tro-toi-dau"] = {

tom_tat: [
  "**Module 5 đã nói \"biến là nhãn\" ở góc người dùng. Bài này nhìn xuống dưới**: nhãn đó nằm trong một `dict`, và thứ nó trỏ tới là một khối bộ nhớ có cấu trúc xác định.",
  "**Mọi object trong CPython đều có phần đầu chung**: bộ đếm tham chiếu và con trỏ tới kiểu. Đây là lý do `id()`, `type()`, `sys.getrefcount()` chạy được với mọi thứ.",
  "**Không gian tên là `dict` thật** — `x = 5` là `locals()['x'] = <object 5>`. Hiểu điều này giải thích cả tốc độ tra biến lẫn vì sao Python tốn bộ nhớ.",
  "**`id()` không phải \"địa chỉ\" theo nghĩa trừu tượng** — trong CPython nó **là** địa chỉ ô nhớ, và điều đó giải thích vì sao `id` bị tái sử dụng sau khi object chết.",
  "**Cache số nguyên nhỏ và gộp chuỗi là chi tiết cài đặt**, không phải quy tắc ngôn ngữ. Biết chúng để hiểu `is` cho kết quả kỳ lạ, không phải để dựa vào."
],

can_biet_truoc: [
  "Bài **Cú pháp, kiểu dữ liệu, toán tử** ở module 5 — \"biến là nhãn\" và mutable/immutable ở góc người dùng.",
  "Bài **Stack vs heap** ở module 2 — object nằm trên heap, khung stack giữ tên biến.",
  "Bài **CPU, RAM, cache** ở module 1 — con trỏ, ô nhớ, và vì sao truy cập rải rác chậm hơn liền khối.",
  "Bài **Hệ nhị phân, bit và byte** ở module 1 — kích thước một con trỏ là 8 byte trên máy 64 bit."
],

dinh_nghia: "Nói đời thường: module 5 nói *\"biến là tờ giấy nhớ dán lên đồ vật\"*. Bài này mở tủ ra xem: **tờ giấy nhớ được cất trong một cuốn sổ tra cứu**, và **đồ vật có một cái nhãn dán sẵn ghi nó là loại gì và bao nhiêu người đang dùng nó**.\n\nNói chính xác:\n\n- **Object** — một khối bộ nhớ trên heap. Trong CPython, mọi object bắt đầu bằng phần đầu chung `PyObject`.\n- **Bộ đếm tham chiếu** (*refcount*) — số nơi đang trỏ tới object. Nằm trong phần đầu của mỗi object.\n- **Con trỏ kiểu** (*type pointer*) — trường trong phần đầu, trỏ tới object mô tả kiểu. Đây là thứ `type(x)` đọc.\n- **Không gian tên** (*namespace*) — bảng ánh xạ tên → object. Với module và instance thì nó là một `dict` thật.\n- **`id()`** — số định danh duy nhất của object trong suốt vòng đời nó. Trong CPython là **địa chỉ ô nhớ**.\n- **Gộp** (*interning*) — cơ chế dùng chung một object cho những giá trị hay gặp, để tiết kiệm bộ nhớ.\n\nĐiểm cần phân biệt ngay với module 5: ở đó câu hỏi là *\"gán có sao chép không\"*. Ở đây câu hỏi là **\"cái nhãn nằm ở đâu, object gồm những gì, và ai đang giữ nó\"**. Cùng một hiện tượng, nhìn từ tầng dưới.",

vi_sao_can: "Hiểu tầng bộ nhớ giải thích những thứ mà tầng người dùng chỉ mô tả được:\n\n- **Nó giải thích các hành vi \"kỳ lạ\" của `is`.** `256 is 256` cho `True` còn `257 is 257` có thể cho `False` — module 5 nói \"đừng dựa vào\", bài này nói **vì sao**.\n- **Nó là nền cho ba bài sau.** Đếm tham chiếu là cơ chế dọn rác; sao chép nông/sâu là chuyện con trỏ; tối ưu bộ nhớ dựa trên việc biết object gồm gì.\n- **Nó giải thích vì sao Python tốn bộ nhớ.** Một số nguyên trong C tốn 4 byte; trong Python tốn khoảng 28. Biết con số đó đến từ đâu là biết khi nào cần đổi cấu trúc dữ liệu.\n- **Nó cần cho việc gỡ lỗi rò rỉ bộ nhớ.** Không biết đọc `sys.getrefcount` thì không lần được ai đang giữ object.\n- **Định hướng AI/ML.** Xử lý mảng hàng triệu phần tử — biết `list` lưu con trỏ còn `array`/NumPy lưu giá trị liền khối là biết vì sao chênh lệch bộ nhớ tới hàng chục lần.\n\nTình huống thật rất hay gặp: bạn nạp một danh sách 10 triệu số nguyên vào Python và thấy nó ngốn hơn 400 MB, trong khi cùng dữ liệu đó ở dạng nhị phân chỉ 40 MB. Không có gì hỏng cả — đó là cái giá của việc mọi thứ đều là object. Phần cơ chế sẽ tính ra chính xác con số đó đến từ đâu, và bài cuối module sẽ nói cách giảm nó.",

co_che: "### Một object gồm những gì\n\nTrong CPython, **mọi** object bắt đầu bằng một phần đầu chung:\n\n```\n┌─────────────────────────────┐\n│ ob_refcnt   (8 byte)        │  ← bộ đếm tham chiếu\n│ ob_type     (8 byte)        │  ← con trỏ tới object kiểu\n├─────────────────────────────┤\n│ dữ liệu riêng của kiểu      │\n└─────────────────────────────┘\n```\n\nHai trường đầu giải thích ba hàm dựng sẵn:\n\n```\ntype(x)              → đọc ob_type\nsys.getrefcount(x)   → đọc ob_refcnt\nid(x)                → địa chỉ của chính khối này\n```\n\nVới một số nguyên nhỏ, phần \"dữ liệu riêng\" là độ dài và các chữ số:\n\n```python\nimport sys\nsys.getsizeof(0)        # 28 — không phải 4 hay 8\nsys.getsizeof(1)        # 28\nsys.getsizeof(2**100)   # lớn hơn — int Python không giới hạn\n```\n\n**28 byte cho số `0`** phân rã ra:\n\n```\n8  ob_refcnt\n8  ob_type\n8  ob_size (số \"chữ số\" nội bộ)\n4  một chữ số 30 bit\n──\n28 byte\n```\n\nSo với C: một `int` là 4 byte. Python tốn **gấp bảy lần** — và đó là cái giá của việc mọi thứ đều là object có kiểu, có bộ đếm, và tự mô tả.\n\nĐánh đổi này không phải lãng phí vô ích: nhờ nó mà Python có kiểu động, số nguyên không tràn, kiểm tra kiểu lúc chạy, và dọn rác tự động. Nhưng biết con số giúp bạn quyết định đúng khi dữ liệu lớn.\n\n### Biến nằm ở đâu\n\nMột biến **không phải một ô nhớ chứa giá trị**. Nó là một mục trong bảng tên:\n\n```python\nx = 5\n\nglobals()[\"x\"]          # 5 — biến toàn cục nằm trong dict này\nglobals()[\"y\"] = 10     # tạo biến y bằng cách sửa dict\nprint(y)                # 10 ✓\n```\n\n```\nBẢNG TÊN (dict)              HEAP\n┌──────────┐                ┌──────────────┐\n│ \"x\" ─────┼───────────────►│ int object 5 │\n│ \"y\" ─────┼──────┐         │ refcnt: 2    │\n└──────────┘      │         └──────────────┘\n                  │         ┌──────────────┐\n                  └────────►│ int object 10│\n                            └──────────────┘\n```\n\nGán `x = 5` làm ba việc:\n\n```\n① Tìm hoặc tạo object cho giá trị 5\n② Ghi cặp (\"x\", con trỏ tới object đó) vào bảng tên\n③ Tăng refcount của object\n```\n\nGán lại `x = 7` thì:\n\n```\n① Tìm hoặc tạo object 7\n② Ghi đè con trỏ trong bảng tên\n③ Tăng refcount của 7, GIẢM refcount của 5\n```\n\nĐây là lý do gán không sao chép: **thứ được ghi vào bảng tên là một con trỏ 8 byte**, bất kể object lớn hay nhỏ.\n\nMột chi tiết quan trọng: **biến cục bộ trong hàm không dùng `dict`.** CPython tối ưu chúng thành các ô đánh số trong khung stack:\n\n```python\ndef f():\n    a = 1\n    return a\n\nf.__code__.co_varnames      # ('a',) — tên biến cục bộ, biết TRƯỚC khi chạy\nf.__code__.co_nlocals       # 1 — số ô cần cấp\n```\n\nHệ quả đo được: **biến cục bộ nhanh hơn biến toàn cục** vì tra theo chỉ số thay vì tra `dict`. Đây là lý do thành ngữ \"gán hàm hay dùng vào biến cục bộ trong vòng lặp nóng\" có tác dụng thật.\n\n### `id()` — không trừu tượng như tên gọi\n\n```python\nx = [1, 2, 3]\nid(x)                # ví dụ 140234567890123\nhex(id(x))           # '0x7f8b1c0d5f0b'\n```\n\nTài liệu Python định nghĩa `id()` là \"một số nguyên duy nhất và không đổi trong vòng đời object\". Trong CPython, nó **chính là địa chỉ ô nhớ**.\n\nCụm \"trong vòng đời object\" là phần quan trọng và hay bị bỏ qua:\n\n```python\nid([1, 2, 3])        # ví dụ 140234567890123\nid([4, 5, 6])        # ⚠️ CÓ THỂ ra CÙNG số!\n```\n\nLý do: list đầu tiên **không còn ai giữ** nên bị huỷ ngay sau khi `id()` trả về, và list thứ hai được cấp đúng ô nhớ vừa giải phóng.\n\nĐây là bẫy thật khi gỡ lỗi:\n\n```python\n# ❌ So sánh id của các object tạm\nprint(id(tao_a()) == id(tao_b()))    # kết quả VÔ NGHĨA\n\n# ✓ Giữ tham chiếu rồi mới so\na, b = tao_a(), tao_b()\nprint(a is b)\n```\n\n### Đếm tham chiếu — nhìn thấy được\n\n```python\nimport sys\n\nds = [1, 2, 3]\nsys.getrefcount(ds)      # 2 — không phải 1\n```\n\nVì sao là 2: một tham chiếu từ tên `ds`, và **một tham chiếu tạm từ chính lời gọi `getrefcount`** (đối số truyền vào cũng là một tham chiếu).\n\n```python\nds = [1, 2, 3]\nprint(sys.getrefcount(ds))    # 2\n\nds2 = ds\nprint(sys.getrefcount(ds))    # 3\n\nkho = [ds]\nprint(sys.getrefcount(ds))    # 4  — list cũng giữ tham chiếu\n\ndel ds2\nprint(sys.getrefcount(ds))    # 3\n\nkho.clear()\nprint(sys.getrefcount(ds))    # 2\n```\n\nMỗi nơi \"biết\" tới object đều làm tăng bộ đếm:\n\n```\n· một tên biến\n· một phần tử trong list, tuple, set\n· một giá trị trong dict\n· một thuộc tính của object khác\n· một tham số hàm đang chạy\n· một closure đang giữ nó\n```\n\nKhi bộ đếm về 0, object bị huỷ **ngay lập tức** — đây là cơ chế dọn rác chính của CPython, và bài về garbage collection sẽ nói kỹ.\n\n### Cache số nguyên nhỏ\n\nMục này giải thích hiện tượng mà module 5 chỉ nói \"đừng dựa vào\":\n\n```python\na, b = 256, 256\na is b               # True\n\na, b = 257, 257\na is b               # False (khi gán trên hai dòng riêng)\n```\n\nCPython **tạo sẵn** object cho các số nguyên nhỏ khi khởi động, và mọi phép tính cho ra giá trị trong khoảng đó đều trả về **cùng object**:\n\n```python\nx = 100\ny = 99 + 1\nx is y               # True — cùng object đã tạo sẵn\n\nx = 1000\ny = 999 + 1\nx is y               # False — hai object riêng\n```\n\nLý do tồn tại: số nhỏ xuất hiện cực nhiều (chỉ số vòng lặp, bộ đếm, cờ), nên tạo sẵn tiết kiệm được rất nhiều lần cấp phát.\n\nMột hiện tượng kèm theo hay gây bối rối:\n\n```python\n# Trong cùng MỘT dòng lệnh hoặc một hàm, trình biên dịch gộp hằng số\ndef f():\n    a = 1000\n    b = 1000\n    return a is b\n\nf()                  # True — hai hằng số giống nhau trong CÙNG một khối mã\n```\n\nĐây là tối ưu lúc biên dịch, khác với cache số nhỏ lúc chạy. Kết quả phụ thuộc **code được biên dịch thành mấy khối** — nên nó không dự đoán được và không nên dựa vào.\n\n### Gộp chuỗi\n\n```python\na = \"xin\"\nb = \"xin\"\na is b               # True\n\na = \"xin chao ban\"\nb = \"xin chao ban\"\na is b               # có thể True do gộp hằng số, nhưng...\n\na = \"\".join([\"xin\", \" chao\"])\nb = \"xin chao\"\na is b               # False — chuỗi tạo LÚC CHẠY không được gộp\n```\n\nQuy tắc chung của CPython: chuỗi **trông giống tên định danh** (chỉ chữ, số, gạch dưới) được gộp tự động, vì chúng hay dùng làm khoá `dict` và tên thuộc tính.\n\nGộp thủ công khi cần:\n\n```python\nimport sys\n\na = sys.intern(\"chuoi rat dai duoc dung lai nhieu lan\")\nb = sys.intern(\"chuoi rat dai duoc dung lai nhieu lan\")\na is b               # True\n```\n\nCông dụng thật của `sys.intern`: khi xử lý dữ liệu lớn có **rất nhiều chuỗi lặp lại** — ví dụ cột \"thành phố\" trong một triệu bản ghi — gộp chúng lại tiết kiệm bộ nhớ đáng kể, và so sánh cũng nhanh hơn vì `==` kiểm tra `is` trước.\n\nĐiểm cần nhớ: **mọi cơ chế gộp là chi tiết cài đặt.** Chúng khác nhau giữa các phiên bản CPython và giữa các bản hiện thực Python. Dùng `==` để so sánh giá trị; `is` chỉ dùng với `None`, `True`, `False`.\n\n### Truyền tham số\n\nMôđun 5 đã nói kết quả; đây là cơ chế:\n\n```python\ndef f(ds):\n    print(id(ds))          # ⭐ CÙNG id với bên ngoài\n    ds.append(1)           # sửa object → bên ngoài thấy\n    ds = [9]               # ghi đè Ô CỤC BỘ → bên ngoài không thấy\n    print(id(ds))          # id khác\n\na = []\nprint(id(a))\nf(a)\nprint(a)                   # [1]\n```\n\nChuyện gì xảy ra khi gọi hàm:\n\n```\n① Cấp một khung stack mới\n② SAO CHÉP CON TRỎ từ đối số vào ô cục bộ của tham số\n③ Tăng refcount của object\n```\n\nBước ② là mấu chốt: **cái được sao chép là con trỏ 8 byte, không phải object.** Vì vậy:\n\n```\nSửa object qua con trỏ      → bên ngoài thấy\nGhi đè con trỏ trong ô cục bộ → bên ngoài không thấy\n```\n\nCách gọi tên chính xác là **truyền tham chiếu tới object** (*call by object reference*), khác cả \"truyền giá trị\" của C lẫn \"truyền tham chiếu\" của C++.\n\n### Container lưu con trỏ\n\nĐây là điểm có hệ quả lớn nhất về bộ nhớ:\n\n```python\nds = [1, 2, 3]\n```\n\n```\nlist object                          các int object riêng lẻ\n┌──────────────┐                    ┌─────┐\n│ refcnt, type │                    │  1  │◄──┐\n│ ob_size = 3  │                    └─────┘   │\n│ allocated= 4 │                    ┌─────┐   │\n│ ob_item ─────┼──► [ptr]───────────┤  2  │◄──┼──┐\n└──────────────┘    [ptr]───────────┴─────┘   │  │\n                    [ptr]─────────────────────┘  │\n                    [   ]  ← chỗ dự trữ          │\n```\n\nHệ quả:\n\n```python\nimport sys\n\nds = list(range(1_000_000))\nsys.getsizeof(ds)                              # ~8 MB — CHỈ mảng con trỏ\nsum(sys.getsizeof(x) for x in ds) + sys.getsizeof(ds)   # ~36 MB — thực tế\n```\n\n**`sys.getsizeof` chỉ đo container**, không đo object bên trong. Đây là giới hạn quan trọng phải nhớ, và bài cuối module sẽ nói cách đo đúng.\n\nSo với mảng liền khối:\n\n```python\nimport array\na = array.array(\"i\", range(1_000_000))\nsys.getsizeof(a)          # ~4 MB — lưu GIÁ TRỊ, không lưu con trỏ\n```\n\nChênh lệch gần mười lần, và còn một lợi ích nữa: dữ liệu liền khối **thân thiện với cache CPU** — nối về bài CPU/RAM/cache ở module 1. Duyệt một `array` nhanh hơn duyệt `list` không chỉ vì ít byte hơn, mà vì bộ xử lý đọc được nhiều phần tử trong một lần nạp cache.\n\nĐây chính là lý do NumPy tồn tại, và là nền cho toàn bộ tính toán số trong Python.\n\n### `tuple` cũng lưu con trỏ\n\nMột hệ quả đã gặp ở module 5, giờ có lời giải thích:\n\n```python\nt = (1, 2, [3, 4])\nt[2].append(5)       # ✓ chạy được\n```\n\n`tuple` đảm bảo **mảng con trỏ của nó không đổi**. Nó không đảm bảo gì về các object mà con trỏ trỏ tới:\n\n```\ntuple object\n┌──────────────┐\n│ refcnt, type │\n│ ob_size = 3  │\n│ [ptr]────────┼──► int 1      ← con trỏ CỐ ĐỊNH\n│ [ptr]────────┼──► int 2\n│ [ptr]────────┼──► list ──────► nội dung SỬA ĐƯỢC\n└──────────────┘\n```\n\nCùng lý do giải thích vì sao `tuple` chứa `list` không hashable: `hash` của `tuple` tính từ hash các phần tử, mà `list` không có hash.\n\n### Chi phí bộ nhớ theo kiểu\n\n```python\nimport sys\n\nsys.getsizeof(None)          # 16\nsys.getsizeof(True)          # 28\nsys.getsizeof(0)             # 28\nsys.getsizeof(1.5)           # 24\nsys.getsizeof(\"\")            # 49\nsys.getsizeof(\"a\")           # 50   — mỗi ký tự ASCII thêm 1 byte\nsys.getsizeof([])            # 56\nsys.getsizeof(())            # 40   — tuple nhẹ hơn list\nsys.getsizeof({})            # 64\nsys.getsizeof(set())         # 216  — bảng băm để trống nhiều ô\n```\n\nBa quan sát đáng nhớ:\n\n```\n· tuple nhẹ hơn list      → list phải giữ thêm sức chứa và chỗ dự trữ\n· set/dict nặng nhất       → bảng băm cố ý để trống để tránh va chạm\n· chuỗi rỗng đã 49 byte    → phần đầu cộng metadata mã hoá\n```\n\nCon số cụ thể khác nhau giữa các phiên bản; điều đáng nhớ là **thứ tự tương đối** và lý do đằng sau.\n\n### Vì sao Python chậm hơn C ở vòng lặp số\n\nGộp lại những gì đã có:\n\n```python\ntong = 0\nfor x in ds:\n    tong += x\n```\n\nMỗi vòng lặp, Python phải:\n\n```\n① Lấy con trỏ phần tử từ mảng của list\n② Đọc ob_type để biết nó là kiểu gì\n③ Tra bảng phương thức của kiểu để tìm phép cộng\n④ Cấp một object MỚI cho kết quả (int immutable)\n⑤ Cập nhật refcount của các object liên quan\n```\n\nTrong C, cùng vòng lặp đó là một lệnh cộng thanh ghi. Chênh lệch hàng chục tới hàng trăm lần **không phải vì Python \"thông dịch\"** — nối về bài compiled/interpreted ở module 1 — mà vì **mỗi thao tác phải tra kiểu và cấp phát object**.\n\nĐây cũng là lý do NumPy nhanh: nó lưu giá trị liền khối và thực hiện phép cộng trên **cả mảng** bằng code C, bỏ qua toàn bộ năm bước trên cho từng phần tử.",

vi_du: "### Nhìn vào phần đầu của object\n\n```python\nimport sys, ctypes\n\nds = [1, 2, 3]\n\n# Ba hàm này đều đọc phần đầu chung\nprint(f\"type    : {type(ds)}\")\nprint(f\"id      : {id(ds)} = {hex(id(ds))}\")\nprint(f\"refcount: {sys.getrefcount(ds)}\")\n\n# Đọc thẳng bộ đếm từ bộ nhớ — cùng con số, không có tham chiếu tạm\nprint(f\"refcnt thô: {ctypes.c_long.from_address(id(ds)).value}\")\n```\n\n`ctypes.c_long.from_address(id(x))` đọc đúng 8 byte đầu của object — chính là `ob_refcnt`. Nó cho con số **không cộng thêm 1** như `getrefcount`, vì không có lời gọi hàm nào tạo tham chiếu tạm.\n\nKỹ thuật này chỉ dùng để **quan sát và học**, không dùng trong code thật: nó phụ thuộc bố cục bộ nhớ của CPython và có thể làm hỏng chương trình nếu địa chỉ sai.\n\n### Biến chỉ là mục trong bảng tên\n\n```python\nx = 5\n\n# Sửa bảng tên trực tiếp\nglobals()[\"y\"] = 10\nprint(y)                        # 10 ✓ biến y tồn tại\n\n# Xem bảng tên\nprint({k: v for k, v in globals().items() if not k.startswith(\"__\")})\n\n# Cùng object, hai tên\na = [1, 2]\nb = a\nprint(id(a) == id(b))           # True\nprint([k for k, v in globals().items() if v is a])    # ['a', 'b']\n```\n\nDòng cuối liệt kê **mọi tên đang trỏ tới cùng object** — cách trực quan để thấy \"nhãn\" và \"đồ vật\" là hai thứ tách biệt.\n\nBiến cục bộ thì khác:\n\n```python\ndef f():\n    a = 1\n    b = 2\n    return locals()\n\nprint(f())                      # {'a': 1, 'b': 2}\nprint(f.__code__.co_varnames)   # ('a', 'b') — biết TRƯỚC khi chạy\nprint(f.__code__.co_nlocals)    # 2\n```\n\n`locals()` trong hàm **dựng ra một `dict` từ các ô stack**, chứ không phải trả về bảng tên thật. Đó là lý do sửa `locals()` trong hàm không có tác dụng:\n\n```python\ndef g():\n    a = 1\n    locals()[\"a\"] = 99\n    return a\n\nprint(g())      # 1 — không phải 99\n```\n\n### Biến cục bộ nhanh hơn biến toàn cục\n\n```python\nimport time\n\nN = 3_000_000\nds = list(range(100))\n\n# Toàn cục — tra dict mỗi lần\ndef dung_toan_cuc():\n    tong = 0\n    for _ in range(N):\n        tong += len(ds)          # tra \"len\" và \"ds\" trong dict toàn cục\n    return tong\n\n# Cục bộ — tra theo chỉ số\ndef dung_cuc_bo(ds=ds, len=len):    # ⭐ đưa vào ô cục bộ\n    tong = 0\n    for _ in range(N):\n        tong += len(ds)\n    return tong\n\nfor f in (dung_toan_cuc, dung_cuc_bo):\n    t = time.perf_counter()\n    f()\n    print(f\"{f.__name__:<16} {time.perf_counter()-t:.3f}s\")\n```\n\nBản cục bộ nhanh hơn khoảng 20–30%. Lý do: `LOAD_FAST` (tra ô theo chỉ số) rẻ hơn `LOAD_GLOBAL` (tra `dict` toàn cục, có khi phải tra tiếp bảng dựng sẵn).\n\nXem bytecode để thấy:\n\n```python\nimport dis\n\ndef a():\n    return len([1])          # LOAD_GLOBAL len\n\ndef b(len=len):\n    return len([1])          # LOAD_FAST len\n\ndis.dis(a)\ndis.dis(b)\n```\n\nCảnh báo cân bằng: **đây là tối ưu vi mô**, chỉ đáng trong vòng lặp chạy hàng triệu lần. Mẹo `def f(len=len)` làm chữ ký hàm khó hiểu, nên chỉ dùng khi đã đo và thấy đáng.\n\n### `id` bị tái sử dụng — bẫy khi gỡ lỗi\n\n```python\nprint(id([1, 2, 3]))\nprint(id([4, 5, 6]))\n# Hai dòng có thể in ra CÙNG một số\n```\n\nGiải thích từng bước:\n\n```\n① Tạo [1,2,3] tại địa chỉ A\n② id() trả về A\n③ Không ai giữ list ⇒ refcount = 0 ⇒ HUỶ NGAY, giải phóng A\n④ Tạo [4,5,6] ⇒ bộ cấp phát dùng lại A\n⑤ id() trả về A\n```\n\n```python\n# ✓ Giữ tham chiếu thì id khác nhau\na = [1, 2, 3]\nb = [4, 5, 6]\nprint(id(a) == id(b))      # False ✓\n```\n\nQuy tắc: **`id()` chỉ có nghĩa khi object còn sống.** So sánh `id` của các object tạm là vô nghĩa.\n\n### Đếm tham chiếu tăng giảm ở đâu\n\n```python\nimport sys\n\ndef dem(o, nhan=\"\"):\n    print(f\"{nhan:<24} refcount = {sys.getrefcount(o) - 1}\")\n    #                                            ⭐ trừ tham chiếu tạm\n\nds = [1, 2, 3]\ndem(ds, \"sau khi tạo\")                 # 1\n\nds2 = ds\ndem(ds, \"thêm một tên\")                # 2\n\nkho = [ds, ds]\ndem(ds, \"vào list hai lần\")            # 4\n\nd = {\"a\": ds}\ndem(ds, \"vào dict\")                    # 5\n\nclass A: pass\na = A()\na.thuoc_tinh = ds\ndem(ds, \"thành thuộc tính\")            # 6\n\ndel ds2, d, a\nkho.clear()\ndem(ds, \"sau khi dọn hết\")             # 1\n```\n\nChú ý `- 1` để trừ tham chiếu tạm do chính lời gọi hàm tạo ra — không có nó, mọi con số lệch một đơn vị.\n\nMột trường hợp đáng chú ý:\n\n```python\ndef f(x):\n    dem(x, \"trong hàm\")     # cao hơn 1 — tham số là một tham chiếu\n\nds = [1]\ndem(ds, \"ngoài hàm\")       # 1\nf(ds)                       # 2\n```\n\n### Cache số nguyên — tìm ngưỡng thật\n\n```python\ndef cung_object(n: int) -> bool:\n    \"\"\"Hai biến gán cùng giá trị có trỏ cùng object không?\"\"\"\n    a = n\n    b = n\n    return a is b\n\n# Dùng biến trung gian để tránh trình biên dịch gộp hằng số\nfor n in (-6, -5, 0, 100, 256, 257, 1000):\n    x = n\n    y = n\n    print(f\"{n:>6}: {x is y}\")\n```\n\nDùng `int()` để chắc chắn tạo object mới:\n\n```python\nprint(int(\"256\") is 256)      # thường True — trong dải cache\nprint(int(\"257\") is 257)      # thường False\n```\n\nHiện tượng gộp hằng số lúc biên dịch:\n\n```python\ndef f():\n    a = 1000\n    b = 1000\n    return a is b\n\nprint(f())                    # True — CÙNG một khối mã\n\nx = 1000\ny = 1000\nprint(x is y)                 # ở REPL từng dòng: có thể False\n```\n\nXem hằng số của khối mã:\n\n```python\nprint(f.__code__.co_consts)   # (None, 1000) — chỉ MỘT object 1000\n```\n\nĐây là bằng chứng trực tiếp: trình biên dịch thấy hai hằng số giống nhau và chỉ lưu một.\n\n**Kết luận không đổi so với module 5**: chi tiết này thay đổi giữa các phiên bản và không được dựa vào. Dùng `==`.\n\n### Gộp chuỗi và ứng dụng thật\n\n```python\nimport sys\n\n# Chuỗi giống định danh — gộp tự động\na = \"nguoi_dung\"\nb = \"nguoi_dung\"\nprint(a is b)                          # True\n\n# Chuỗi tạo lúc chạy — không gộp\nc = \"nguoi\" + \"_dung\"                  # gộp lúc biên dịch → True\nd = \"\".join([\"nguoi\", \"_dung\"])        # tạo lúc chạy → False\nprint(c is a, d is a)\n\n# Gộp thủ công\ne = sys.intern(\"\".join([\"nguoi\", \"_dung\"]))\nprint(e is a)                          # True ✓\n```\n\nỨng dụng thật — dữ liệu có nhiều chuỗi lặp:\n\n```python\nimport sys, random\n\nTHANH_PHO = [\"Hà Nội\", \"Hồ Chí Minh\", \"Đà Nẵng\", \"Cần Thơ\", \"Hải Phòng\"]\n\n# Mô phỏng đọc từ file: mỗi dòng tạo một chuỗi MỚI\nkhong_gop = [\"\".join(random.choice(THANH_PHO)) for _ in range(200_000)]\nco_gop = [sys.intern(s) for s in khong_gop]\n\ndef tong_bo_nho(ds):\n    dinh_danh = {id(s) for s in ds}          # ⭐ đếm object DUY NHẤT\n    return sum(sys.getsizeof(s) for s in ds if id(s) in dinh_danh\n               and not dinh_danh.discard(id(s)))\n\nprint(f\"object chuỗi duy nhất, không gộp: {len({id(s) for s in khong_gop}):,}\")\nprint(f\"object chuỗi duy nhất, có gộp   : {len({id(s) for s in co_gop}):,}\")\n# không gộp: hàng trăm nghìn object\n# có gộp   : 5 object\n```\n\nVới dữ liệu thật có cột phân loại lặp lại nhiều, `sys.intern` giảm bộ nhớ đáng kể. Lợi ích phụ: so sánh `==` nhanh hơn vì Python kiểm tra `is` trước, và với chuỗi đã gộp thì nó khớp ngay.\n\nĐánh đổi: chuỗi đã gộp **không bao giờ được giải phóng** trong suốt vòng đời tiến trình. Chỉ gộp những giá trị thật sự lặp nhiều.\n\n### Truyền tham số — theo dõi bằng `id`\n\n```python\ndef f(ds):\n    print(f\"  vào hàm    : id={id(ds)}\")\n    ds.append(99)\n    print(f\"  sau append : id={id(ds)}\")     # ⭐ id KHÔNG đổi\n    ds = [0]\n    print(f\"  sau gán lại: id={id(ds)}\")     # ⭐ id ĐỔI — ô cục bộ trỏ chỗ khác\n\na = [1, 2]\nprint(f\"trước gọi : id={id(a)}\")\nf(a)\nprint(f\"sau gọi   : id={id(a)}, giá trị={a}\")\n# id trước = id trong hàm ≠ id sau khi gán lại\n# giá trị = [1, 2, 99] — append có tác dụng, gán lại thì không\n```\n\n```\nTRƯỚC gán lại:              SAU gán lại:\nngoài: a ──┐                ngoài: a ──► [1,2,99]\n           ├──► [1,2,99]\ntrong: ds ─┘                trong: ds ──► [0]\n```\n\nSơ đồ này là toàn bộ nội dung của \"truyền tham chiếu tới object\".\n\n### `list` lưu con trỏ — đo cụ thể\n\n```python\nimport sys, array\n\nn = 500_000\n\nds = list(range(n))\nkich_thuoc_container = sys.getsizeof(ds)\nkich_thuoc_that = kich_thuoc_container + sum(sys.getsizeof(x) for x in ds)\n\na = array.array(\"i\", range(n))\n\nprint(f\"list, chỉ container : {kich_thuoc_container:>12,} byte\")\nprint(f\"list, tính cả phần tử: {kich_thuoc_that:>12,} byte\")\nprint(f\"array.array('i')     : {sys.getsizeof(a):>12,} byte\")\n```\n\nKết quả điển hình: `list` tính đủ khoảng 18 MB, `array` khoảng 2 MB — chênh gần chín lần.\n\nChú ý: với `range(n)` nhỏ, nhiều phần tử **dùng chung object nhờ cache số nguyên nhỏ**, nên phép cộng ở trên đếm trùng. Cách đo đúng:\n\n```python\ndef bo_nho_that(ds) -> int:\n    \"\"\"Đếm mỗi object DUY NHẤT một lần.\"\"\"\n    da_dem = set()\n    tong = sys.getsizeof(ds)\n    for x in ds:\n        if id(x) not in da_dem:\n            da_dem.add(id(x))\n            tong += sys.getsizeof(x)\n    return tong\n\nprint(f\"đo đúng: {bo_nho_that(ds):,} byte\")\n```\n\nĐây là lý do `sys.getsizeof` một mình không đủ, và bài cuối module sẽ giới thiệu công cụ đo tử tế hơn.\n\n### Tại sao `tuple` chứa `list` không hashable\n\n```python\nt1 = (1, 2, 3)\nhash(t1)              # ✓\n\nt2 = (1, 2, [3])\nhash(t2)              # TypeError: unhashable type: 'list'\n\n# Vì hash của tuple tính từ hash các PHẦN TỬ\nt2[2].append(4)       # ✓ nội dung đổi được\nprint(t2)             # (1, 2, [3, 4])\nprint(id(t2[2]))      # con trỏ KHÔNG đổi\n```\n\n`tuple` bất biến ở tầng **mảng con trỏ**. Nếu một con trỏ trỏ tới object mutable thì giá trị tổng thể đổi được, nên nó không thể có hash ổn định.\n\nĐây là lời giải thích ở tầng bộ nhớ cho quy tắc \"khoá `dict` phải immutable\" ở module 5.\n\n### Đo chi phí object\n\n```python\nimport sys\n\nmau = [None, True, 0, 2**64, 1.5, \"\", \"a\"*100, [], [0]*10,\n       (), (0,)*10, {}, {\"a\": 1}, set(), {1, 2, 3}]\n\nfor x in mau:\n    ten = type(x).__name__\n    mo_ta = repr(x)[:24]\n    print(f\"{ten:<6} {mo_ta:<26} {sys.getsizeof(x):>6} byte\")\n```\n\nQuan sát đáng nhớ từ bảng kết quả:\n\n```\n· None chỉ 16 byte    → nó là object DUY NHẤT, dùng chung toàn chương trình\n· int 0 tốn 28 byte   → gấp bảy lần int của C\n· tuple nhẹ hơn list  → không cần chỗ dự trữ cho việc thêm phần tử\n· set/dict rất nặng   → bảng băm để trống nhiều ô, đổi bộ nhớ lấy tốc độ\n```\n\nHàng cuối nối về bài `list`/`dict`/`set` ở module 5: bảng so sánh ở đó nói `set` \"tốn bộ nhớ\", và đây là con số cụ thể cùng lý do.\n\n### Vòng lặp số: Python và mảng liền khối\n\n```python\nimport time, array\n\nn = 3_000_000\nds = list(range(n))\na = array.array(\"i\", range(n))\n\nt = time.perf_counter(); sum(ds)\nprint(f\"sum(list) : {time.perf_counter()-t:.3f}s\")\n\nt = time.perf_counter(); sum(a)\nprint(f\"sum(array): {time.perf_counter()-t:.3f}s\")\n\nt = time.perf_counter()\ntong = 0\nfor x in ds:\n    tong += x\nprint(f\"vòng for  : {time.perf_counter()-t:.3f}s\")\n```\n\nVòng `for` viết tay chậm hơn `sum()` rõ rệt vì mỗi vòng phải thực thi thêm nhiều lệnh bytecode. `sum()` chạy vòng lặp trong C và bỏ qua phần lớn chi phí đó.\n\nĐiều này giải thích một lời khuyên hay gặp: **dùng hàm dựng sẵn thay vì tự viết vòng lặp** — không phải vì \"đẹp\" mà vì vòng lặp nằm trong C.\n\nVà nó dẫn thẳng tới lý do NumPy tồn tại: nó không chỉ lưu giá trị liền khối, nó còn thực hiện phép toán trên **cả mảng** trong C, bỏ qua toàn bộ chi phí tra kiểu và cấp phát cho từng phần tử.",

so_sanh: "### Module 5 và bài này — cùng hiện tượng, hai tầng\n\n| Câu hỏi | Module 5 trả lời | Bài này trả lời |\n|---|---|---|\n| Gán có sao chép không | không, chỉ thêm nhãn | **sao chép một con trỏ 8 byte vào bảng tên** |\n| `256 is 256` vì sao True | \"cache, đừng dựa vào\" | **object tạo sẵn lúc khởi động, dùng chung** |\n| Hàm sửa được list không | có, nếu sửa tại chỗ | **con trỏ được sao chép, object thì không** |\n| `tuple` chứa `list` | \"bất biến một tầng\" | **mảng con trỏ cố định, object được trỏ thì không** |\n| `set` tốn bộ nhớ | \"bảng băm tốn hơn\" | **216 byte rỗng, cố ý để trống ô** |\n\nBài này không thay module 5 — nó **giải thích** những gì module 5 mô tả.\n\n### Biến toàn cục và biến cục bộ\n\n| | Toàn cục | Cục bộ |\n|---|---|---|\n| Lưu ở | `dict` của module | **ô đánh số trong khung stack** |\n| Lệnh bytecode | `LOAD_GLOBAL` | **`LOAD_FAST`** |\n| Tốc độ tra | chậm hơn | **nhanh hơn ~20–30%** |\n| Biết trước khi chạy | ✗ | **✓ `co_varnames`** |\n| Sửa qua `globals()`/`locals()` | ✓ có tác dụng | **✗ không có tác dụng** |\n\nHàng cuối là điểm bất ngờ: `locals()` trong hàm **dựng ra một `dict` mới** từ các ô stack, nên sửa nó không đổi được biến thật.\n\n### Ba hàm đọc phần đầu object\n\n| Hàm | Đọc trường | Ghi chú |\n|---|---|---|\n| `type(x)` | `ob_type` | luôn chính xác |\n| `id(x)` | địa chỉ khối | **chỉ có nghĩa khi object còn sống** |\n| `sys.getrefcount(x)` | `ob_refcnt` | **cao hơn 1** do tham chiếu tạm |\n\nHai ghi chú này là hai bẫy phổ biến nhất khi dùng chúng để gỡ lỗi.\n\n### Chi phí bộ nhớ theo kiểu\n\n| Kiểu | Byte (rỗng) | Vì sao |\n|---|---|---|\n| `None` | 16 | object **duy nhất** toàn chương trình |\n| `float` | 24 | phần đầu + 8 byte số |\n| `int 0` | 28 | phần đầu + độ dài + một chữ số |\n| `tuple` | 40 | phần đầu + độ dài, **không có chỗ dự trữ** |\n| `list` | 56 | phần đầu + độ dài + sức chứa + con trỏ mảng |\n| `str` | 49 | phần đầu + metadata mã hoá |\n| `dict` | 64 | bảng băm nhỏ nhất |\n| `set` | 216 | bảng băm để trống nhiều ô |\n\nCon số khác nhau giữa các phiên bản; **thứ tự tương đối và lý do** mới là thứ đáng nhớ.\n\n### `list` và mảng liền khối\n\n| | `list` | `array.array` / NumPy |\n|---|---|---|\n| Lưu gì | **con trỏ** tới object | **giá trị** trực tiếp |\n| Bộ nhớ 1 triệu int | ~36 MB | **~4 MB** |\n| Kiểu phần tử | tuỳ ý, trộn được | **cố định một kiểu** |\n| Thân thiện cache CPU | kém — nhảy rải rác | **tốt — liền khối** |\n| Phép toán trên cả mảng | ✗ | ✓ (NumPy) |\n| Linh hoạt | **cao nhất** | thấp |\n\nBảng này là lý do NumPy tồn tại, và là kiến thức nền cho mảng AI/ML.\n\n### Các cơ chế gộp\n\n| | Cache số nhỏ | Gộp hằng số | `sys.intern` |\n|---|---|---|---|\n| Khi nào | lúc khởi động | **lúc biên dịch** | khi bạn gọi |\n| Phạm vi | toàn chương trình | một khối mã | toàn chương trình |\n| Dự đoán được | tương đối | **khó** | ✓ |\n| Nên dựa vào | **✗** | ✗ | ✓ có chủ đích |\n| Giải phóng | không | theo khối mã | **không bao giờ** |\n\nHai cột đầu là **chi tiết cài đặt** — biết để hiểu, không để dựa vào. Cột cuối là công cụ dùng được, với điều kiện chỉ gộp giá trị thật sự lặp nhiều.\n\n### Vì sao Python chậm ở vòng lặp số\n\n| Bước mỗi vòng | Python | C |\n|---|---|---|\n| Lấy phần tử | theo con trỏ | đọc thẳng ô nhớ |\n| Biết kiểu | **tra `ob_type`** | biết lúc biên dịch |\n| Tìm phép cộng | **tra bảng phương thức** | một lệnh máy |\n| Lưu kết quả | **cấp object mới** | ghi thanh ghi |\n| Quản lý bộ nhớ | **cập nhật refcount** | không có |\n\nBa bước in đậm là chi phí Python phải trả cho kiểu động và dọn rác tự động. Đây là lời giải thích đầy đủ cho câu \"Python chậm vì phải tra kiểu lúc chạy\" ở module 1.",

loi_thuong_gap: [
  "**So sánh `id()` của các object tạm.** `id([1,2,3]) == id([4,5,6])` có thể `True` vì object đầu đã bị huỷ và ô nhớ được dùng lại.",
  "**Quên rằng `sys.getrefcount` cao hơn 1.** Lời gọi hàm tạo một tham chiếu tạm; trừ 1 nếu muốn con số thật.",
  "**Dựa vào cache số nguyên nhỏ hoặc gộp chuỗi.** Chúng là chi tiết cài đặt, khác nhau giữa các phiên bản. Dùng `==`.",
  "**Tưởng `sys.getsizeof(ds)` là bộ nhớ thật của list.** Nó chỉ đo container, không đo object bên trong.",
  "**Cộng `getsizeof` mọi phần tử mà không khử trùng.** Nhiều phần tử dùng chung object nhờ cache; phải đếm theo `id` duy nhất.",
  "**Sửa `locals()` trong hàm rồi mong biến đổi theo.** Nó dựng một `dict` mới từ các ô stack.",
  "**Dùng `sys.intern` bừa bãi.** Chuỗi đã gộp không bao giờ được giải phóng; chỉ gộp giá trị lặp nhiều.",
  "**Áp dụng mẹo `def f(len=len)` khắp nơi.** Đây là tối ưu vi mô, chỉ đáng trong vòng lặp hàng triệu lần, và làm chữ ký hàm khó hiểu.",
  "**Đọc bộ nhớ bằng `ctypes.from_address` trong code thật.** Chỉ dùng để quan sát; sai địa chỉ là hỏng chương trình.",
  "**Tưởng `tuple` bất biến nghĩa là nội dung không đổi.** Nó chỉ đảm bảo mảng con trỏ cố định.",
  "**Dùng `list` cho hàng triệu số rồi ngạc nhiên vì tốn bộ nhớ.** Mỗi phần tử là một object riêng cộng một con trỏ.",
  "**Tự viết vòng `for` cộng dồn thay vì dùng `sum()`.** Vòng lặp của hàm dựng sẵn nằm trong C, nhanh hơn nhiều."
],

tu_kiem_tra: [
  "Phần đầu chung của mọi object CPython gồm hai trường nào? Ba hàm dựng sẵn nào đọc chúng?",
  "Vì sao `sys.getsizeof(0)` là 28 chứ không phải 4 hay 8? Phân rã con số đó.",
  "Gán `x = 5` làm ba việc gì? Gán lại `x = 7` khác thế nào?",
  "Biến toàn cục và biến cục bộ lưu khác nhau ra sao? Hệ quả về tốc độ?",
  "Vì sao sửa `locals()` trong hàm không có tác dụng?",
  "`id()` trong CPython thật sự là gì? Vì sao `id([1,2,3]) == id([4,5,6])` có thể `True`?",
  "Vì sao `sys.getrefcount(x)` cho số cao hơn thực tế một đơn vị?",
  "Liệt kê sáu nơi làm tăng bộ đếm tham chiếu của một object.",
  "Cache số nguyên nhỏ và gộp hằng số lúc biên dịch khác nhau thế nào?",
  "Chuỗi nào được gộp tự động? `sys.intern` dùng khi nào và đánh đổi gì?",
  "Khi gọi hàm, cái gì được sao chép? Điều đó giải thích hai hành vi nào của tham số?",
  "Vì sao `list` một triệu số nguyên tốn khoảng 36 MB còn `array` chỉ 4 MB?",
  "Vì sao `tuple` chứa `list` không hashable? Giải thích ở tầng bộ nhớ.",
  "Nêu năm bước Python phải làm mỗi vòng lặp cộng số. Ba bước nào là chi phí của kiểu động?",
  "Vì sao `sum(ds)` nhanh hơn vòng `for` viết tay?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Data model* phần đầu — định nghĩa object, danh tính, kiểu, giá trị.",
  "Tài liệu Python, module `sys` — `getsizeof`, `getrefcount`, `intern`, và ghi chú về giới hạn của chúng.",
  "Sách **CPython Internals** — Anthony Shaw. Đi thẳng vào cấu trúc `PyObject`, bộ cấp phát, và cách biến được lưu.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về danh tính, giá trị và bí danh giải thích rất kỹ phần này.",
  "Tài liệu Python, module `dis` — xem bytecode để thấy `LOAD_FAST` và `LOAD_GLOBAL` khác nhau."
],

lien_he: [
  "**Cú pháp, kiểu dữ liệu, toán tử** ở module 5: bài đó nói \"biến là nhãn\"; bài này giải thích nhãn nằm ở đâu và object gồm gì.",
  "**Mutable vs immutable và bẫy tham số mặc định** ngay sau: bẫy đó nhìn từ góc bộ nhớ — object mặc định sống ở đâu.",
  "**Shallow copy vs deep copy** trong module này: sao chép nông là sao chép mảng con trỏ.",
  "**Garbage collection và đếm tham chiếu** trong module này: bộ đếm ở bài này chính là cơ chế dọn rác chính.",
  "**Đo bộ nhớ và tối ưu** cuối module: dựa trên việc biết object gồm gì và container lưu con trỏ.",
  "**Stack vs heap** ở module 2: object trên heap, tên biến cục bộ trong khung stack.",
  "**CPU, RAM, cache** ở module 1: dữ liệu liền khối thân thiện cache — lý do `array` nhanh hơn `list`.",
  "**Compiled vs interpreted vs bytecode** ở module 1: \"Python chậm vì tra kiểu lúc chạy\" giờ đã có lời giải thích đầy đủ.",
  "**List, dict, set, tuple** ở module 5: bảng chi phí bộ nhớ ở đó giờ có con số và lý do.",
  "**Magic method** ở module 6: `hash` của `tuple` tính từ hash các phần tử — lý do `tuple` chứa `list` không hashable.",
  "**AI/ML** ở mảng 07: NumPy lưu giá trị liền khối và tính toán trong C — bài này là lý do nó tồn tại."
]

};
