window.LESSONS["07:deep-learning:lan-truyen-tien-va-lan-truyen-nguoc"] = {

tom_tat: [
  "Lan truyền tiến biến đầu vào thành dự đoán và loss; lan truyền ngược đi ngược đồ thị tính toán để tìm mỗi tham số đã góp phần làm loss thay đổi bao nhiêu.",
  "Backpropagation không phải optimizer. Nó tính gradient bằng chain rule; optimizer dùng gradient đó để cập nhật tham số. Trộn hai vai trò khiến việc debug trở nên mơ hồ.",
  "Autograd lưu các phép toán và giá trị trung gian cần thiết trong forward. Vì vậy huấn luyện tốn bộ nhớ hơn inference, và gọi `.detach()` sai chỗ có thể cắt mất đường truyền gradient.",
  "Gradient được cộng dồn vào `.grad` theo mặc định trong PyTorch. Phải xóa đúng thời điểm; đặc tính này vừa là bẫy vừa cho phép gradient accumulation khi batch không vừa bộ nhớ.",
  "Trên cùng mạng nhà kính, bài này kiểm gradient bằng sai phân hữu hạn và theo dõi shape qua từng tầng, để thấy chain rule là phép tính cụ thể chứ không phải phép màu."
],

can_biet_truoc: [
  "Neuron, tầng, ReLU và logit của bài trước; biết mạng `8 → 16 → 8 → 1` tạo dự đoán từ tám cảm biến.",
  "Đạo hàm một biến và ý nghĩa độ dốc; chain rule ở mức `(f∘g)' = f'(g(x))g'(x)`.",
  "Phép nhân ma trận, chuyển vị và shape tensor, vì gradient của một tầng tuyến tính cũng là các phép nhân ma trận.",
  "PyTorch tensor cơ bản; biết `requires_grad`, `.backward()` và `torch.no_grad()` sẽ được giải thích trong bài."
],

dinh_nghia: "**Lan truyền tiến** (forward propagation) là quá trình áp dụng lần lượt các tầng từ đầu vào đến dự đoán, rồi tính một scalar loss đo độ sai.\n\n**Lan truyền ngược** (backpropagation) là thuật toán áp dụng chain rule theo thứ tự ngược trên đồ thị tính toán để tính gradient ∂L/∂θ cho mọi tham số θ. Ký hiệu ∂L/∂θ đọc là “loss L thay đổi tức thời theo θ”; dấu cho hướng tăng, độ lớn cho độ nhạy cục bộ.\n\nBackprop chỉ trả lời “nếu nhích tham số thì loss đổi thế nào”. Nó không tự chọn bước nhích. SGD, Momentum hay Adam nhận gradient và quyết định cập nhật—đó là bài optimizer sau.",

vi_sao_can: "Mạng nhà kính có 289 tham số; mạng thực tế có thể hàng triệu. Thử tăng giảm từng số rồi huấn luyện lại là bất khả thi. Sai phân hữu hạn cần ít nhất hai lần forward cho mỗi tham số, nên chi phí tăng tỷ lệ với số tham số và còn chịu sai số làm tròn.\n\nBackprop khai thác việc mọi tham số cùng dẫn tới một loss. Nó tái sử dụng đạo hàm trung gian khi đi ngược đồ thị, nhờ đó chi phí tính toàn bộ gradient chỉ cùng bậc với vài lần forward, thay vì nhân thêm số tham số. Đây là lý do mạng lớn có thể học được.\n\nHiểu cơ chế còn giúp chẩn đoán: loss không giảm có thể do gradient bằng 0 sau ReLU, bị cắt bởi `.detach()`, nổ thành vô cực, chưa xóa gradient, hoặc tham số không nằm trong optimizer. Nếu chỉ biết gọi ba dòng API, các lỗi ấy đều trông giống “mô hình không học”.",

co_che: "### Forward tạo một đồ thị, không chỉ một con số\n\nVới một mẫu x, mạng tính z₁ = W₁x+b₁, h₁ = ReLU(z₁), z₂ = W₂h₁+b₂, h₂ = ReLU(z₂), rồi logit s = W₃h₂+b₃. Loss L so s với nhãn y. Mỗi tensor kết quả biết nó được tạo từ phép nào và phụ thuộc tensor nào nếu autograd đang bật.\n\nĐồ thị này được tạo động theo lần chạy. Một nhánh `if` bằng Python có thể khiến mỗi mẫu đi qua đường khác, và PyTorch chỉ ghi những phép thực sự xảy ra. Sau `backward`, đồ thị trung gian thường được giải phóng để tiết kiệm bộ nhớ; gọi backward lần hai trên cùng đồ thị cần `retain_graph=True`, nhưng lạm dụng tùy chọn này dễ gây tốn RAM/GPU.\n\n### Chain rule như một dây chuyền trách nhiệm\n\nXét chuỗi đơn giản z = wx+b, a = ReLU(z), L = (a−y)². Muốn ∂L/∂w, ta nhân ba độ dốc cục bộ: ∂L/∂a × ∂a/∂z × ∂z/∂w. Cụ thể ∂L/∂a = 2(a−y), ∂a/∂z bằng 1 nếu z>0 và 0 nếu z<0, còn ∂z/∂w = x.\n\nMỗi thừa số trả lời một câu hỏi nhỏ. Loss phản ứng với a ra sao? ReLU truyền thay đổi từ z sang a không? z phản ứng với w theo đầu vào x thế nào? Nhân chúng nối tác động từ cuối về đầu. Nếu một thừa số bằng 0, toàn đường gradient bị chặn.\n\n### Reverse-mode phù hợp vì loss là scalar\n\nMạng có nhiều tham số nhưng thường chỉ một loss scalar. Reverse-mode automatic differentiation bắt đầu với ∂L/∂L = 1 rồi đi ngược, phân phối gradient tới mọi tổ tiên. Nó tính “một đầu ra theo nhiều đầu vào” hiệu quả.\n\nForward-mode đi theo chiều ngược lại: một hướng nhiễu ở đầu vào tới nhiều đầu ra. Nó hữu ích khi số đầu vào ít hơn đầu ra, nhưng không hợp bằng reverse-mode cho hàng triệu tham số và một loss. Backprop chính là reverse-mode được tổ chức theo các phép của mạng neuron.\n\n### Gradient qua tầng tuyến tính\n\nVới batch, Z = XWᵀ+b và gradient từ tầng sau là G = ∂L/∂Z. Khi đó ∂L/∂W = GᵀX, ∂L/∂b là tổng G theo chiều batch, và ∂L/∂X = GW. Các shape tự kiểm tra tính hợp lệ: G `(B,m)`, X `(B,n)` cho GᵀX `(m,n)`, đúng shape W.\n\nBias dùng chung cho mọi mẫu nên đóng góp gradient phải cộng qua batch. Nếu loss là trung bình, hệ số 1/B đã nằm trong G; nếu loss là tổng, gradient lớn theo batch size. Đây là một lý do đổi reduction hoặc batch size có thể buộc chỉnh learning rate.\n\n### Gradient qua ReLU và điểm gãy tại zero\n\nReLU có đạo hàm 1 ở z>0, 0 ở z<0. Tại z=0 đạo hàm toán học không duy nhất; framework chọn một quy ước, PyTorch dùng 0. Một điểm đơn lẻ hầu như không ảnh hưởng với số thực liên tục, nhưng nhiều activation đúng 0 có thể xuất hiện do dữ liệu hoặc khởi tạo.\n\nMask `z > 0` từ forward đủ để backward quyết định gradient nào đi qua. Vì phải giữ mask hoặc z, huấn luyện tốn bộ nhớ activation. Inference không cần backward nên `torch.inference_mode()` có thể bỏ phần theo dõi này.\n\n### Nhánh hội tụ: gradient phải cộng\n\nNếu một tensor h được dùng ở hai nhánh, L phụ thuộc h qua cả hai đường. Gradient tại h là tổng đóng góp, không phải chọn một đường. Ví dụ L = h² + 3h cho ∂L/∂h = 2h+3. Autograd tích lũy các luồng khi mọi tensor con đã gửi gradient về.\n\nNguyên tắc cộng này cũng giải thích weight sharing: cùng một tham số được dùng nhiều lần trong RNN hoặc convolution, gradient của tham số là tổng từ tất cả vị trí/thời điểm. Không cần sao chép tham số cho từng lần dùng.\n\n### `.backward()` bắt đầu từ đâu\n\nNếu loss là scalar, `loss.backward()` ngầm dùng gradient khởi đầu 1. Nếu tensor đầu ra có nhiều phần tử, PyTorch không biết muốn đạo hàm tổ hợp nào; phải truyền vector gradient hoặc giảm về scalar, thường bằng `.mean()`.\n\n`pred.backward(torch.ones_like(pred))` tính gradient của tổng các phần tử pred. Nó không tương đương gradient của trung bình nếu batch size lớn hơn 1. Việc chọn scalar objective là một quyết định mô hình hóa, không chỉ mẹo để API chạy.\n\n### Vì sao `.grad` cộng dồn\n\nMỗi lần backward, PyTorch cộng gradient mới vào `parameter.grad`. Thiết kế này hỗ trợ nhiều nhánh và gradient accumulation qua nhiều micro-batch. Vòng lặp thông thường gọi `optimizer.zero_grad()` trước forward hoặc trước backward của bước mới.\n\nNếu quên xóa, bước thứ hai dùng tổng gradient của cả hai batch; learning rate hiệu dụng và hướng cập nhật thay đổi. Nếu cố ý accumulation K micro-batch để mô phỏng batch lớn, thường chia loss cho K trước backward rồi chỉ `step()` sau K lần. Cần xử lý micro-batch cuối nếu số mẫu không chia hết K.\n\n### `detach`, `no_grad` và những đường bị cắt\n\n`.detach()` tạo tensor dùng chung dữ liệu nhưng không còn quan hệ gradient với nguồn. Chuyển sang NumPy cũng cần detach vì NumPy không biết autograd. Điều này đúng khi ghi log, sai khi tensor còn tham gia loss.\n\n`with torch.no_grad()` tắt ghi đồ thị trong một vùng, phù hợp cập nhật thủ công hoặc validation. `torch.inference_mode()` còn tối ưu thêm cho inference nhưng nghiêm ngặt hơn về tensor tạo bên trong. Không bọc forward huấn luyện bằng các context này, nếu không loss không có `grad_fn`.\n\n### In-place operation có thể phá giá trị đã lưu\n\nBackward đôi khi cần giá trị đầu vào của forward. Nếu sửa tensor tại chỗ bằng phép có dấu gạch dưới như `relu_()` hoặc `add_()`, giá trị đã lưu có thể mất. PyTorch thường phát hiện bằng version counter và báo lỗi rằng biến cần cho gradient đã bị sửa.\n\nKhông phải mọi in-place đều sai; framework dùng chúng có kiểm soát. Nhưng tối ưu bộ nhớ thủ công trước khi profiler chứng minh nhu cầu là đánh đổi nguy hiểm. Phiên bản out-of-place dễ suy luận và debug hơn.\n\n### Vanishing và exploding gradient\n\nChain rule nhân nhiều Jacobian. Nếu các hệ số thường nhỏ hơn 1, gradient co theo độ sâu—gradient tiêu biến. Nếu thường lớn hơn 1, nó phình nhanh—gradient bùng nổ. Sigmoid bão hòa có đạo hàm gần 0 nên làm vấn đề đầu nặng hơn; chuỗi dài trong RNN khiến phép nhân lặp nhiều lần.\n\nReLU, khởi tạo phù hợp, BatchNorm và residual connection giúp đường gradient thuận lợi. Gradient clipping giới hạn độ lớn, hữu ích với RNN nhưng không chữa nguyên nhân cấu trúc. Theo dõi norm từng tầng tốt hơn chỉ nhìn loss NaN sau khi đã hỏng.\n\n### Kiểm gradient bằng sai phân hữu hạn\n\nVới một tham số θ, xấp xỉ trung tâm là [L(θ+ε)−L(θ−ε)]/(2ε). Nó chậm nhưng độc lập với backprop, nên phù hợp kiểm một mô hình nhỏ hoặc custom autograd function. ε quá lớn gây sai số xấp xỉ; quá nhỏ làm hai loss gần nhau đến mức sai số floating-point lấn át.\n\nDùng `float64`, mạng nhỏ, tránh điểm gãy ReLU và so sai số tương đối. Không chạy gradient check trên toàn mạng production mỗi batch. Đây là test phát triển, không phải thuật toán huấn luyện.\n\n### Vòng đời đầy đủ của một bước học\n\nThứ tự khái niệm là: xóa gradient cũ → forward → tính loss → backward → kiểm/biến đổi gradient nếu cần → optimizer step. `step()` thay tham số, nên mọi thống kê dựa trên tham số cũ phải lấy trước nó. Validation không gọi backward và cần chuyển `model.eval()` cho dropout/BatchNorm.\n\nBackprop không đảm bảo loss giảm sau mọi bước. Gradient chỉ là độ dốc cục bộ; learning rate quá lớn có thể nhảy qua vùng tốt. Cũng không đảm bảo tìm global minimum trong bài toán phi lồi. Nó cung cấp hướng thông tin, còn optimizer và dữ liệu quyết định hành trình.\n\n### Ca biên làm gradient biến mất hoặc vô nghĩa\n\nBatch rỗng khiến loss trung bình NaN. Tất cả nhãn giống nhau vẫn có gradient nhưng mô hình có thể học bias và bỏ qua đặc trưng. Đầu vào NaN tạo gradient NaN. Loss không phụ thuộc một tham số thì `.grad` có thể là `None`, khác với tensor gradient toàn 0.\n\nTham số có `requires_grad=False` hữu ích khi đóng băng backbone trong transfer learning; nếu vô tình đặt sai, optimizer không thể sửa nó. Ngược lại, đưa tensor target vào graph không thường gây sai nhưng tốn theo dõi vô ích. Biên pipeline nên kiểm finite, shape và dtype trước khi quy lỗi cho autograd.\n\n### Khi nào không cần backprop\n\nMô hình tuyến tính lồi có nghiệm đóng hoặc solver chuyên dụng có thể ổn định hơn. Cây quyết định học bằng chia nút, không dùng gradient. Thuật toán tiến hóa hoặc tìm kiếm đen phù hợp khi phép tính không khả vi, nhưng thường cần nhiều đánh giá hơn.\n\nNgay trong mạng neuron, inference không cần backprop. Tắt gradient giảm bộ nhớ và tăng tốc. Không nên giữ đồ thị chỉ để lấy xác suất phục vụ API; đó là lãng phí có thể gây rò bộ nhớ nếu giữ tham chiếu loss qua nhiều request.",

vi_du: "### So autograd với phép tính tay\n\n```python\nimport torch\n\ntorch.set_default_dtype(torch.float64)\nx = torch.tensor(2.0)\ny = torch.tensor(1.0)\nw = torch.tensor(0.5, requires_grad=True)\nb = torch.tensor(-0.2, requires_grad=True)\n\nz = w * x + b          # 0.8\na = torch.relu(z)       # 0.8\nloss = (a - y) ** 2     # 0.04\nloss.backward()\n\n# dL/dw = 2(a-y) * 1 * x = 2(-0.2)*2 = -0.8\n# dL/db = 2(a-y) * 1 * 1 = -0.4\nassert torch.allclose(w.grad, torch.tensor(-0.8))\nassert torch.allclose(b.grad, torch.tensor(-0.4))\nprint(float(loss), float(w.grad), float(b.grad))\n```\n\nNếu đổi bias để z âm, ReLU chặn đường và cả hai gradient bằng 0. Đây không phải lỗi autograd; đó là đạo hàm của kiến trúc đã chọn tại mẫu ấy.\n\n### Gradient check cho mạng nhà kính cực nhỏ\n\n```python\nfrom torch import nn\n\ntorch.manual_seed(3)\nmodel = nn.Sequential(nn.Linear(2, 3), nn.Tanh(), nn.Linear(3, 1)).double()\nx = torch.tensor([[0.2, -0.7], [-0.1, 0.4]], dtype=torch.float64)\ny = torch.tensor([[1.0], [0.0]], dtype=torch.float64)\ncriterion = nn.BCEWithLogitsLoss()\n\nloss = criterion(model(x), y)\nloss.backward()\n\np = model[0].weight\ni, j = 1, 0\ngrad_auto = p.grad[i, j].item()\nold = p.data[i, j].item()\neps = 1e-6\n\nwith torch.no_grad():\n    p[i, j] = old + eps\n    plus = criterion(model(x), y).item()\n    p[i, j] = old - eps\n    minus = criterion(model(x), y).item()\n    p[i, j] = old\n\ngrad_fd = (plus - minus) / (2 * eps)\nrelative_error = abs(grad_auto - grad_fd) / max(1e-12, abs(grad_auto) + abs(grad_fd))\nprint(grad_auto, grad_fd, relative_error)\nassert relative_error < 1e-6\n```\n\nTanh được dùng trong test để tránh đúng điểm gãy của ReLU. Ta khôi phục tham số sau hai lần nhiễu; quên việc này làm các test sau chạy trên model đã bị thay đổi.\n\n### Minh họa cộng dồn gradient\n\n```python\nq = torch.tensor(2.0, requires_grad=True)\n(q ** 2).backward()\nassert q.grad.item() == 4.0\n(3 * q).backward()\nassert q.grad.item() == 7.0  # 4 + 3, không phải 3\n\nq.grad = None\n(3 * q).backward()\nassert q.grad.item() == 3.0\n```\n\nGán `.grad = None` và `optimizer.zero_grad(set_to_none=True)` có thể tránh ghi zero vào bộ nhớ; lần backward sau tạo gradient mới. Code không được giả định `.grad` luôn là tensor trước backward.\n\n### Một bước huấn luyện đúng thứ tự\n\n```python\ntorch.set_default_dtype(torch.float32)\nmodel = nn.Sequential(nn.Linear(8, 16), nn.ReLU(), nn.Linear(16, 1))\noptimizer = torch.optim.SGD(model.parameters(), lr=0.05)\nx = torch.randn(32, 8)\ny = torch.randint(0, 2, (32, 1)).float()\n\noptimizer.zero_grad(set_to_none=True)\nlogit = model(x)\nloss = nn.functional.binary_cross_entropy_with_logits(logit, y)\nloss.backward()\n\nfor name, parameter in model.named_parameters():\n    assert parameter.grad is not None, f\"mất gradient tại {name}\"\n    assert torch.isfinite(parameter.grad).all(), f\"gradient hỏng tại {name}\"\n\noptimizer.step()\nprint(\"loss trước cập nhật:\", loss.item())\n```\n\nĐoạn này không tuyên bố model đã học từ batch ngẫu nhiên; nó chỉ kiểm cơ học của một bước. Bài optimizer sẽ giải thích `lr=0.05` ảnh hưởng cập nhật ra sao.",

so_sanh: "### Ba cách lấy đạo hàm\n\n| Cách | Cơ chế | Ưu điểm | Giới hạn |\n|---|---|---|---|\n| Đạo hàm ký hiệu | biến đổi biểu thức đại số | công thức chính xác, dễ trình bày | biểu thức phình, khó với control flow |\n| Sai phân hữu hạn | chạy L(θ±ε) | độc lập, tốt để kiểm tra | chậm theo số tham số, sai số số học |\n| Autograd reverse-mode | ghi đồ thị rồi chain rule ngược | hiệu quả cho scalar loss, linh hoạt | giữ activation, cần quản lý graph |\n\n### Phân biệt các vai trò trong huấn luyện\n\n| Thành phần | Nhận gì | Trả/làm gì | Lỗi điển hình |\n|---|---|---|---|\n| Forward | batch và tham số | logit/dự đoán | shape sai, NaN đầu vào |\n| Loss | dự đoán và nhãn | một scalar mục tiêu | chọn sai loss hoặc reduction |\n| Backprop | loss và đồ thị | điền `.grad` | detach, gradient 0/NaN |\n| Optimizer | tham số và `.grad` | cập nhật tham số | learning rate sai, quên `step` |\n\nBackprop và optimizer thường xuất hiện liền nhau trong code nhưng không thể dùng thay nhau. Ta có thể backward để phân tích saliency mà không cập nhật; cũng có optimizer không gradient trong các bài toán khác.",

loi_thuong_gap: [
  "**Quên xóa gradient giữa các bước.** `.grad` tăng dần dù batch đổi; nhận biết bằng in norm trước và sau `zero_grad`, sửa bằng xóa có chủ đích hoặc accumulation đúng thiết kế.",
  "**Gọi `.detach()`, `.item()` hoặc NumPy trước khi tính loss.** Loss báo không có `grad_fn` hoặc tham số nhận `.grad=None`; giữ tensor trong graph đến sau backward.",
  "**Dùng `no_grad` quanh forward huấn luyện.** Loss vẫn có số nhưng không có đường về tham số; chỉ dùng context này cho validation, inference hoặc cập nhật thủ công.",
  "**Backward một vector không cung cấp gradient khởi đầu.** PyTorch yêu cầu scalar; giảm bằng mean/sum theo đúng ý nghĩa objective, đừng chọn tùy tiện chỉ để hết lỗi.",
  "**Sửa tensor in-place mà backward cần.** Thông báo version mismatch xuất hiện; bật anomaly detection để định vị rồi đổi sang phép không tại chỗ.",
  "**Nhầm `.grad=None` với gradient bằng 0.** `None` thường nghĩa không có đường phụ thuộc hoặc chưa backward; tensor 0 nghĩa có đường nhưng đạo hàm cục bộ bằng 0.",
  "**Dùng gradient check bằng float32 và ε quá nhỏ.** Sai số tương đối lớn giả tạo; dùng float64, mô hình nhỏ và thử vài ε quanh 10⁻⁶.",
  "**Giữ loss tensor của mọi batch trong list.** Mỗi loss giữ đồ thị và activation, bộ nhớ tăng liên tục; lưu `loss.item()` nếu chỉ cần số để vẽ."
],

tu_kiem_tra: [
  "Với z=wx+b, a=ReLU(z), L=(a−y)², viết ∂L/∂w và giải thích từng thừa số bằng lời.",
  "Vì sao reverse-mode thích hợp cho một loss và hàng triệu tham số hơn forward-mode?",
  "Tầng tuyến tính nhận X `(32,8)` và tạo Z `(32,16)`. Hãy suy ra shape gradient của W, b và X.",
  "Một tham số có `.grad=None` sau backward. Nêu ít nhất ba nguyên nhân khác nhau và phép kiểm tra để phân biệt.",
  "Muốn mô phỏng batch 128 bằng bốn micro-batch 32, cần đặt `zero_grad`, chia loss và `step` ở đâu?",
  "Vì sao sai phân trung tâm đáng tin hơn sai phân một phía nhưng vẫn không dùng để huấn luyện mạng lớn?",
  "Nếu validation được chạy không có `no_grad`, kết quả số có nhất thiết sai không? Điều gì bị lãng phí và rủi ro bộ nhớ đến từ đâu?"
],

tai_nguyen: [
  "\"Deep Learning\" — Goodfellow, Bengio, Courville, chương Numerical Computation và Deep Feedforward Networks.",
  "\"Dive into Deep Learning\" — phần Backpropagation, Computational Graphs và Automatic Differentiation.",
  "PyTorch Tutorials — “A Gentle Introduction to torch.autograd” và phần Automatic Differentiation trong Learn the Basics.",
  "Khóa “Neural Networks and Deep Learning” — các bài vectorized backpropagation và gradient checking."
],

lien_he: [
  "Nối sang mảng Toán cho AI: backprop là chain rule và phép nhân Jacobian–vector được tổ chức hiệu quả; đại số tuyến tính kiểm shape của mọi gradient.",
  "Nối sang mảng Kiểm thử phần mềm: gradient check là differential testing—so một implementation nhanh với oracle chậm, độc lập trên trường hợp nhỏ.",
  "Nối sang mảng Hệ thống: activation phải giữ cho backward tạo áp lực bộ nhớ GPU; gradient checkpointing đánh đổi tính lại forward để giảm bộ nhớ.",
  "Nối sang mảng Debugging: anomaly detection, hook gradient và norm theo tầng biến lỗi “không học” thành tín hiệu quan sát được thay vì đoán optimizer."
]

};
