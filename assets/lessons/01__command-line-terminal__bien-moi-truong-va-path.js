window.LESSONS["01:command-line-terminal:bien-moi-truong-va-path"] = {

tom_tat: [
  "**Biến môi trường là cấu hình mà process con kế thừa từ process cha.** Đây là cách chuẩn để truyền cấu hình vào chương trình mà không sửa code.",
  "`BIEN=x` chỉ tạo **biến shell**; `export BIEN=x` mới biến nó thành **biến môi trường** mà process con nhìn thấy.",
  "**`PATH` là danh sách thư mục shell tìm lệnh, theo đúng thứ tự.** Hiểu nó là hiểu vì sao `python` gọi ra bản này chứ không phải bản kia.",
  "Biến môi trường **chỉ truyền xuống, không truyền lên**. Script con không đổi được biến của shell cha — đó là lý do phải dùng `source`.",
  "**Không bao giờ commit `.env` chứa secret.** Nhưng cũng đừng tưởng biến môi trường là an toàn tuyệt đối — `/proc/<pid>/environ` đọc được."
],

can_biet_truoc: [
  "Bài **Process vs thread** — cần biết process con kế thừa gì từ cha.",
  "Bài **Điều hướng** — cần quen gõ lệnh và đường dẫn.",
  "Bài **Pipe và redirect** — sẽ dùng lại một số khái niệm về process."
],

dinh_nghia: "Nói đời thường: biến môi trường giống như **tờ giấy nhắn bạn dán lên người trước khi sai ai đó đi làm việc**. Người đó mang tờ giấy theo, và nếu họ sai người khác đi tiếp thì tờ giấy cũng được sao chép theo. Nhưng người đó sửa tờ giấy của mình thì tờ giấy gốc của bạn không đổi.\n\nNói chính xác:\n\n- **Biến môi trường (environment variable)** là cặp tên–giá trị nằm trong không gian địa chỉ của process, và được **sao chép sang process con** khi tạo process mới.\n- **`PATH`** là một biến môi trường đặc biệt chứa danh sách thư mục, ngăn cách bằng `:` (Windows dùng `;`), mà shell duyệt qua để tìm chương trình tương ứng với lệnh bạn gõ.\n- **Biến shell** là biến chỉ tồn tại trong process shell hiện tại, **không** được truyền cho process con trừ khi `export`.",

vi_sao_can: "Đây là cơ chế cấu hình chuẩn của toàn ngành:\n\n- **Cấu hình ứng dụng.** `DATABASE_URL`, `API_KEY`, `PORT` đều truyền qua biến môi trường. Đây là nguyên tắc thứ ba của *Twelve-Factor App*, và mọi nền tảng triển khai đều theo.\n- **Cùng một image chạy nhiều môi trường.** Container dev, staging, production dùng chung một image, chỉ khác biến môi trường.\n- **Gỡ lỗi \"lệnh không tìm thấy\".** `command not found` gần như luôn là vấn đề `PATH`.\n- **Hiểu vì sao `python` gọi nhầm phiên bản.** Có ba bản Python trên máy, `PATH` quyết định bản nào được gọi. Đây là nguồn của rất nhiều rắc rối với virtual environment.\n- **Bảo mật secret.** Biết vì sao không hard-code khoá vào code, và biết cả giới hạn của biến môi trường.\n\nTình huống thật: bạn kích hoạt virtual environment, `pip install` thành công, nhưng `python main.py` báo `ModuleNotFoundError`. Nguyên nhân gần như luôn là `PATH` đang trỏ tới một Python khác với `pip`. Phần cơ chế cho biết cách chẩn đoán trong ba lệnh.",

co_che: "### Môi trường được kế thừa như thế nào\n\nKhi một process tạo process con, hệ điều hành **sao chép toàn bộ bảng biến môi trường** sang con.\n\n```\nShell (PID 1000)\n  PATH=/usr/bin:/bin\n  HOME=/home/minh\n  API_KEY=abc123\n        │\n        │ tạo process con\n        ▼\npython (PID 1001)\n  PATH=/usr/bin:/bin        ← BẢN SAO\n  HOME=/home/minh           ← BẢN SAO\n  API_KEY=abc123            ← BẢN SAO\n```\n\nHai hệ quả rất quan trọng:\n\n**① Chỉ truyền XUỐNG, không truyền LÊN.** Process con sửa biến của mình thì cha không hề biết. Đây không phải hạn chế mà là **cách ly có chủ đích** — nếu không, một script con lỗi có thể phá môi trường của shell cha.\n\n**② Kế thừa xảy ra tại thời điểm TẠO process.** Đổi biến sau khi chương trình đã chạy thì chương trình đó không thấy gì. Muốn nó thấy phải khởi động lại.\n\nĐiều thứ hai giải thích một chuyện rất hay gặp: sửa `.env` rồi mà server vẫn dùng giá trị cũ — vì server đã đọc biến lúc khởi động.\n\n### Biến shell và biến môi trường\n\n```bash\nBIEN_SHELL=xin-chao              # chỉ shell này thấy\nexport BIEN_MOI_TRUONG=xin-chao  # process con cũng thấy\n\necho $BIEN_SHELL                 # xin-chao ✓ shell hiện tại thấy cả hai\necho $BIEN_MOI_TRUONG            # xin-chao ✓\n\nbash -c 'echo \"[$BIEN_SHELL]\"'         # []          ← con KHÔNG thấy\nbash -c 'echo \"[$BIEN_MOI_TRUONG]\"'    # [xin-chao]  ← con thấy ✓\n```\n\nSự khác biệt nằm ở chỗ `export` **đánh dấu biến để đưa vào bảng môi trường** khi tạo process con. Không `export` thì biến chỉ nằm trong bộ nhớ của riêng shell.\n\n```bash\nenv                  # liệt kê biến MÔI TRƯỜNG\nset                  # liệt kê mọi biến, kể cả biến shell (rất dài)\nexport               # liệt kê biến đã export\nunset BIEN           # xoá biến\n```\n\n### Đặt biến cho một lệnh duy nhất\n\n```bash\nDEBUG=1 python main.py     # DEBUG chỉ tồn tại cho lệnh này\necho $DEBUG                # rỗng — không ảnh hưởng shell\n```\n\nCú pháp `BIEN=x lenh` rất hữu ích: nó không làm bẩn môi trường của shell, và rõ ràng về phạm vi ảnh hưởng. Nên dùng dạng này khi thử nghiệm.\n\n### `PATH`: shell tìm lệnh ra sao\n\n```bash\necho $PATH\n# /home/minh/.local/bin:/usr/local/bin:/usr/bin:/bin\n#         ①                  ②            ③      ④\n```\n\nKhi bạn gõ `python`, shell duyệt các thư mục **theo đúng thứ tự từ trái sang phải** và **dừng lại ở kết quả đầu tiên tìm được**.\n\n```\nGõ: python\n  ① /home/minh/.local/bin/python  → có? → chạy cái này, DỪNG\n  ② /usr/local/bin/python          → không xét tới nữa\n  ③ /usr/bin/python\n```\n\n**Thứ tự quyết định tất cả.** Đây chính là cơ chế mà virtual environment dùng: `activate` chỉ đơn giản là **chèn thư mục `venv/bin` vào đầu `PATH`**.\n\n```bash\nsource venv/bin/activate\necho $PATH\n# /duong/dan/venv/bin:/usr/local/bin:/usr/bin   ← venv/bin lên đầu\n```\n\nKhông có phép màu nào cả. `deactivate` chỉ khôi phục `PATH` cũ.\n\n### Chẩn đoán lệnh nào đang được gọi\n\nBa lệnh này giải quyết phần lớn rắc rối liên quan tới `PATH`:\n\n```bash\nwhich python         # in đường dẫn được chọn\n# /home/minh/du-an/venv/bin/python\n\ntype python          # cho biết là builtin, alias, hàm hay file\n# python is /home/minh/du-an/venv/bin/python\n\nwhich -a python      # ⭐ liệt kê MỌI bản trong PATH, theo thứ tự\n# /home/minh/du-an/venv/bin/python\n# /usr/local/bin/python\n# /usr/bin/python\n```\n\n`which -a` là công cụ chẩn đoán mạnh nhất: nó cho thấy có bao nhiêu bản và bản nào đang thắng.\n\n### Thêm thư mục vào `PATH`\n\n```bash\nexport PATH=\"$HOME/.local/bin:$PATH\"     # thêm vào ĐẦU — ưu tiên cao\nexport PATH=\"$PATH:$HOME/.local/bin\"     # thêm vào CUỐI — ưu tiên thấp\n```\n\n**Luôn giữ lại `$PATH` cũ.** Viết `export PATH=\"/duong/moi\"` sẽ **xoá sạch** PATH và ngay lập tức `ls`, `git`, mọi thứ đều `command not found`. Lỗi này khiến terminal gần như không dùng được cho tới khi mở phiên mới.\n\n### `.` không nằm trong `PATH` — và đó là chủ đích\n\n```bash\n./script.sh          # ✓ phải có ./\nscript.sh            # ✗ command not found\n```\n\nUnix cố ý **không** đưa thư mục hiện tại vào `PATH`. Lý do là bảo mật: nếu có, kẻ tấn công chỉ cần đặt một file tên `ls` chứa mã độc vào thư mục dùng chung, và bất kỳ ai `cd` vào đó rồi gõ `ls` sẽ chạy mã độc thay vì lệnh thật.\n\nDấu `./` bắt buộc là một lời nhắc: *bạn đang chạy thứ gì đó từ thư mục này, hãy chắc là bạn biết nó là gì*.\n\n### Các biến môi trường chuẩn\n\n| Biến | Nghĩa |\n|---|---|\n| `PATH` | Danh sách thư mục tìm lệnh |\n| `HOME` | Thư mục nhà — `~` bung thành giá trị này |\n| `USER` | Tên đăng nhập |\n| `PWD` | Thư mục hiện tại |\n| `OLDPWD` | Thư mục vừa rời — `cd -` dùng nó |\n| `SHELL` | Shell mặc định |\n| `LANG` | Ngôn ngữ và bảng mã, ví dụ `en_US.UTF-8` |\n| `TZ` | Múi giờ |\n| `EDITOR` | Trình soạn thảo mặc định — `git commit` dùng |\n\n`LANG` đáng chú ý với người Việt: đặt sai là chương trình hiển thị sai dấu, và một số công cụ sắp xếp chuỗi theo thứ tự lạ.\n\n### File cấu hình nào được đọc\n\nĐây là chỗ gây bối rối kinh điển:\n\n```\nShell ĐĂNG NHẬP (SSH vào server, đăng nhập TTY)\n  → ~/.bash_profile  hoặc  ~/.profile\n\nShell TƯƠNG TÁC không đăng nhập (mở tab terminal mới)\n  → ~/.bashrc\n\nShell KHÔNG tương tác (chạy script)\n  → không đọc file nào cả!\n```\n\nDòng cuối rất quan trọng: **script không đọc `.bashrc`**. Đó là lý do lệnh chạy được khi bạn gõ tay nhưng `command not found` khi chạy trong cron hoặc CI.\n\nCách xử lý phổ biến: đặt cấu hình trong `~/.bashrc`, rồi cho `~/.bash_profile` gọi nó:\n\n```bash\n# ~/.bash_profile\n[[ -f ~/.bashrc ]] && source ~/.bashrc\n```\n\nVà trong script, **luôn dùng đường dẫn đầy đủ** hoặc tự đặt `PATH` thay vì trông chờ vào cấu hình cá nhân.\n\n### `source` và tại sao nó tồn tại\n\n```bash\n./script.sh          # chạy trong process CON → biến không truyền ngược lại\nsource script.sh     # chạy TRONG shell hiện tại → biến có hiệu lực\n. script.sh          # viết tắt của source\n```\n\nĐây chính là lý do `source venv/bin/activate` phải dùng `source`. Nếu chạy `./activate` thì `PATH` được sửa trong process con rồi process đó kết thúc — shell của bạn không đổi gì.\n\nCùng lý do với `cd`: biến môi trường và working directory đều là **trạng thái của process**, và process con không sửa được trạng thái của cha.\n\n### File `.env` và secret\n\n```bash\n# .env — KHÔNG BAO GIỜ commit file này\nDATABASE_URL=postgresql://user:matkhau@localhost/db\nAPI_KEY=sk-abc123\n```\n\n```bash\n# .gitignore\n.env\n.env.*\n!.env.example        # nhưng vẫn commit file mẫu\n```\n\nMẫu chuẩn của ngành: commit `.env.example` với các tên biến và giá trị giả, để người mới biết cần đặt những gì.\n\n**Nhưng biến môi trường không phải nơi cất secret an toàn tuyệt đối:**\n\n```bash\ncat /proc/<pid>/environ | tr '\\0' '\\n'   # đọc được môi trường của process khác\nps eww <pid>                              # cũng hiện biến môi trường\n```\n\nBất kỳ ai có quyền trên máy đều đọc được. Với secret thật sự nhạy cảm trong production, dùng dịch vụ quản lý bí mật chuyên dụng (Vault, AWS Secrets Manager) và chỉ nạp vào bộ nhớ khi cần.\n\nBiến môi trường **tốt hơn hard-code trong code** rất nhiều, nhưng chưa phải mức bảo vệ cao nhất.",

vi_du: "### Xem và đặt biến\n\n```bash\necho $HOME                # /home/minh\necho $PATH                # /usr/local/bin:/usr/bin:/bin\nenv | sort | head -5      # liệt kê biến môi trường\n\nexport MY_APP_ENV=dev\necho $MY_APP_ENV          # dev\n\n# Dùng trong Python\npython -c \"import os; print(os.environ['MY_APP_ENV'])\"   # dev\n```\n\n### Chứng minh biến chỉ truyền xuống\n\n```bash\n# Tạo script thử\ncat > thu.sh << 'EOF'\n#!/usr/bin/env bash\necho \"Trong script thấy: $BIEN\"\nBIEN=\"da-doi-trong-script\"\necho \"Script đã đổi thành: $BIEN\"\nEOF\nchmod +x thu.sh\n\nexport BIEN=\"gia-tri-goc\"\n./thu.sh\n# Trong script thấy: gia-tri-goc        ← kế thừa được ✓\n# Script đã đổi thành: da-doi-trong-script\n\necho $BIEN\n# gia-tri-goc                            ← shell cha KHÔNG đổi ✓\n\nsource thu.sh\necho $BIEN\n# da-doi-trong-script                    ← source thì đổi thật\n```\n\nThí nghiệm này làm rõ toàn bộ sự khác biệt giữa chạy script và `source` script.\n\n### Chẩn đoán \"pip install rồi mà không import được\"\n\nĐây là rắc rối phổ biến nhất của người mới học Python:\n\n```bash\n# ① python nào đang được gọi?\nwhich python\n# /usr/bin/python                      ← Python hệ thống\n\n# ② pip nào?\nwhich pip\n# /home/minh/du-an/venv/bin/pip        ← pip của venv — KHÔNG KHỚP!\n\n# ③ Có bao nhiêu bản?\nwhich -a python\n# /usr/bin/python\n\n# → pip cài gói vào venv, nhưng python đang chạy là bản hệ thống\n```\n\nCách sửa dứt điểm:\n\n```bash\nsource venv/bin/activate      # đưa venv/bin lên đầu PATH\nwhich python                  # /home/minh/du-an/venv/bin/python ✓\nwhich pip                     # /home/minh/du-an/venv/bin/pip    ✓\n\n# Cách chắc chắn hơn — luôn gọi pip qua chính python đó\npython -m pip install requests\n```\n\nDòng cuối là mẹo đáng dùng mặc định: `python -m pip` đảm bảo pip chạy **đúng bản Python đang gọi**, không thể lệch nhau.\n\n### Cấu hình ứng dụng bằng biến môi trường\n\n```python\nimport os\n\n# ❌ Hard-code — không đổi được giữa các môi trường, dễ lộ nếu commit\nDATABASE_URL = \"postgresql://user:matkhau@localhost/db\"\n\n# ✓ Đọc từ môi trường, có giá trị mặc định cho biến không nhạy cảm\nDATABASE_URL = os.environ.get(\"DATABASE_URL\", \"sqlite:///dev.db\")\nDEBUG = os.environ.get(\"DEBUG\", \"0\") == \"1\"\nPORT = int(os.environ.get(\"PORT\", 8000))\n\n# ✓ Với secret BẮT BUỘC — dùng [] để chương trình chết ngay nếu thiếu\nAPI_KEY = os.environ[\"API_KEY\"]   # KeyError nếu chưa đặt\n```\n\nDòng cuối là chủ đích: **thà chết lúc khởi động còn hơn chạy với cấu hình sai**. Nếu dùng `.get()` với mặc định rỗng, ứng dụng sẽ khởi động rồi thất bại một cách khó hiểu ở request đầu tiên.\n\n```python\n# Nạp file .env khi phát triển (thư viện python-dotenv)\nfrom dotenv import load_dotenv\nload_dotenv()          # đọc .env vào os.environ\n```\n\n### Cùng một image, ba môi trường\n\n```bash\n# Dev\ndocker run -e DATABASE_URL=sqlite:///dev.db -e DEBUG=1 ung-dung\n\n# Production — cùng image, khác cấu hình\ndocker run -e DATABASE_URL=postgresql://prod... -e DEBUG=0 ung-dung\n\n# Từ file\ndocker run --env-file .env.production ung-dung\n```\n\nĐây là lý do biến môi trường trở thành chuẩn: **image không đổi giữa các môi trường**, nên thứ bạn test ở staging đúng là thứ chạy ở production.\n\n### Bẫy cron không có PATH\n\n```bash\n# Chạy tay thì được\npython /home/minh/backup.py     # ✓\n\n# Trong crontab thì lỗi\n0 2 * * * python /home/minh/backup.py\n# /bin/sh: python: command not found\n```\n\nNguyên nhân: cron chạy với môi trường **tối giản** — `PATH` thường chỉ là `/usr/bin:/bin`, và **không đọc `.bashrc`**.\n\nBa cách sửa:\n\n```bash\n# ① Đường dẫn đầy đủ — chắc chắn nhất\n0 2 * * * /home/minh/venv/bin/python /home/minh/backup.py\n\n# ② Đặt PATH trong crontab\nPATH=/home/minh/venv/bin:/usr/local/bin:/usr/bin:/bin\n0 2 * * * python /home/minh/backup.py\n\n# ③ Nạp môi trường trong script\n0 2 * * * bash -lc 'source ~/.bashrc && python ~/backup.py'\n```\n\nCách ① đáng tin nhất và nên là mặc định.\n\n### Chèn thư mục vào PATH đúng cách\n\n```bash\n# Thêm vào ~/.bashrc\nexport PATH=\"$HOME/.local/bin:$PATH\"\n\n# Áp dụng ngay mà không phải mở terminal mới\nsource ~/.bashrc\n\n# ❌ ĐỪNG BAO GIỜ làm thế này — xoá sạch PATH\nexport PATH=\"/duong/moi\"\n# ls: command not found\n# git: command not found\n```\n\nNếu lỡ tay, cứu bằng cách đặt lại tối thiểu rồi mở phiên mới:\n\n```bash\nexport PATH=/usr/local/bin:/usr/bin:/bin\n```",

so_sanh: "### Ba cách truyền cấu hình vào chương trình\n\n| | Biến môi trường | File cấu hình | Đối số dòng lệnh |\n|---|---|---|---|\n| Hợp với | **Secret, khác nhau theo môi trường** | Cấu hình phức tạp, có cấu trúc | Tuỳ chọn từng lần chạy |\n| Commit vào git | ✗ | ✓ (trừ secret) | — |\n| Cấu trúc lồng nhau | ✗ chỉ chuỗi phẳng | **✓ YAML, JSON** | ✗ |\n| Container | **✓ chuẩn ngành** | Phải mount volume | Cồng kềnh |\n| Rủi ro lộ | `ps`, `/proc` đọc được | Lộ nếu commit nhầm | Hiện trong `ps` |\n\nThực tế thường **kết hợp cả ba**: file cấu hình cho phần có cấu trúc và không nhạy cảm, biến môi trường cho secret và những gì đổi theo môi trường, đối số dòng lệnh để ghi đè khi chạy thử.\n\n### Vì sao biến môi trường thắng trong thế giới container\n\nNguyên tắc *Twelve-Factor App* nói: **cấu hình phải tách khỏi code**. Biến môi trường thoả điều đó tốt nhất vì:\n\n- Mọi ngôn ngữ, mọi hệ điều hành đều đọc được — không cần thư viện\n- Không nằm trong image nên cùng một image chạy được mọi nơi\n- Nền tảng triển khai (Docker, Kubernetes, Heroku, Vercel) đều hỗ trợ sẵn\n- Không có nguy cơ commit nhầm vào git như file cấu hình\n\nHạn chế thật: **chỉ chứa được chuỗi phẳng**. Cấu hình phức tạp phải mã hoá thành JSON trong một biến — cách này hoạt động nhưng xấu và khó đọc.\n\n### Giới hạn bảo mật\n\nBiến môi trường **không phải két sắt**:\n\n```bash\n# Bất kỳ ai có quyền đọc process đều xem được\ncat /proc/<pid>/environ | tr '\\0' '\\n'\nps eww <pid>\n\n# Chúng cũng bị kế thừa xuống MỌI process con,\n# kể cả thư viện bên thứ ba mà bạn gọi\n```\n\nThang mức độ bảo vệ, từ tệ tới tốt:\n\n```\n① Hard-code trong code           ← tệ nhất, lộ ngay khi push lên GitHub\n② File .env (đã gitignore)       ← ổn cho phát triển\n③ Biến môi trường từ nền tảng    ← ổn cho production nhỏ\n④ Dịch vụ quản lý bí mật         ← chuẩn cho production nghiêm túc\n```\n\nMức ② và ③ đủ cho hầu hết dự án cá nhân và nhóm nhỏ. Mức ④ cần thiết khi có nhiều người truy cập hạ tầng, hoặc secret có giá trị cao — nó cho phép xoay khoá tự động và ghi log ai đã truy cập khoá nào.",

loi_thuong_gap: [
  "**Quên `export`.** `BIEN=x` chỉ có trong shell hiện tại; process con không thấy. Cần `export BIEN=x`.",
  "**Chạy `./venv/bin/activate` thay vì `source venv/bin/activate`.** Chạy thường sửa PATH trong process con rồi process đó kết thúc — shell của bạn không đổi gì.",
  "**Ghi đè `PATH` mà không giữ `$PATH` cũ.** `export PATH=\"/moi\"` làm mọi lệnh biến mất. Luôn `export PATH=\"/moi:$PATH\"`.",
  "**Trông chờ `.bashrc` được đọc trong script hoặc cron.** Shell không tương tác không đọc nó. Dùng đường dẫn đầy đủ.",
  "**Commit file `.env`.** Secret lên GitHub bị bot quét trong vài giây. Thêm vào `.gitignore` ngay từ commit đầu tiên.",
  "**Dùng `os.environ.get()` với mặc định rỗng cho secret bắt buộc.** Ứng dụng khởi động được rồi thất bại khó hiểu sau. Dùng `os.environ['KEY']` để chết ngay lúc khởi động.",
  "**Đổi biến rồi mong process đang chạy tự cập nhật.** Môi trường được sao chép lúc tạo process. Phải khởi động lại.",
  "**Tưởng biến môi trường là chỗ cất secret an toàn.** `/proc/<pid>/environ` và `ps eww` đọc được. Tốt hơn hard-code nhưng chưa phải mức cao nhất."
],

tu_kiem_tra: [
  "Khác biệt giữa `BIEN=x` và `export BIEN=x` là gì? Chứng minh bằng một lệnh.",
  "Vì sao `source venv/bin/activate` phải dùng `source` chứ không chạy trực tiếp được?",
  "Bạn `pip install requests` thành công nhưng `python` báo `ModuleNotFoundError`. Viết ba lệnh chẩn đoán.",
  "`PATH` có `/a:/b:/c` và cả ba thư mục đều có file `python`. Bản nào được chạy, vì sao?",
  "Vì sao Unix cố ý không đưa `.` vào `PATH`? Nêu kịch bản tấn công cụ thể.",
  "Lệnh chạy được khi gõ tay nhưng `command not found` trong cron. Nguyên nhân và ba cách sửa?",
  "Vì sao dùng `os.environ['API_KEY']` tốt hơn `os.environ.get('API_KEY', '')` cho một secret bắt buộc?"
],

tai_nguyen: [
  "Trang **12factor.net**, mục *III. Config* — nguyên tắc tách cấu hình khỏi code, nền tảng của cách làm hiện đại.",
  "Tài liệu bash, mục *Shell Variables* và *Bash Startup Files* — giải thích chính xác file nào được đọc khi nào.",
  "Tài liệu Linux: `man environ`, `man 7 credentials`.",
  "Tài liệu Python, module `os` mục `os.environ`, và thư viện `python-dotenv`.",
  "Tài liệu Docker, mục *Environment variables* — cách truyền biến vào container."
],

lien_he: [
  "**Process vs thread**: biến môi trường được sao chép khi tạo process con — đúng cơ chế kế thừa đã học ở đó.",
  "**Điều hướng**: working directory và biến môi trường đều là trạng thái của process, và cả hai đều không truyền ngược lên cha. Đó là lý do cả `cd` lẫn `export` phải là builtin.",
  "**Viết shell script** (bài cuối module): script luôn nên đặt `PATH` tường minh thay vì trông chờ môi trường người dùng.",
  "**Git** ở mảng 03: `.gitignore` phải có `.env` ngay từ commit đầu tiên. Lỡ commit rồi thì phải đổi secret, không chỉ xoá file.",
  "**Auth & bảo mật** ở mảng 05: quản lý secret là chủ đề riêng, và biến môi trường chỉ là bậc thang đầu tiên.",
  "**Triển khai** ở mảng 05: cùng một Docker image chạy dev, staging, production chỉ khác biến môi trường — đó là lợi ích lớn nhất của cách này.",
  "**Công cụ của dev** ở module 9: virtual environment hoạt động hoàn toàn dựa trên việc sửa `PATH`, không có gì bí ẩn."
]

};
