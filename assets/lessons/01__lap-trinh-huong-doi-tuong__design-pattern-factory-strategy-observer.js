window.LESSONS["01:lap-trinh-huong-doi-tuong:design-pattern-factory-strategy-observer"] = {

tom_tat: [
  "**Design pattern là tên gọi chung cho những giải pháp đã được dùng nhiều lần** — giá trị lớn nhất của chúng là **từ vựng chung**, không phải bản thân đoạn code.",
  "**Factory tách việc TẠO object khỏi việc DÙNG nó**, để thêm loại mới không phải sửa nơi dùng.",
  "**Strategy đóng gói một thuật toán thành object thay thế được** — nó chính là bài Open/Closed được đặt tên.",
  "**Observer cho phép một nguồn thông báo cho nhiều nơi mà không biết chúng là ai** — nền của mọi hệ thống sự kiện.",
  "**Trong Python, nhiều mẫu gọn hơn hẳn sách gốc**: Strategy thường chỉ là một hàm truyền vào, Factory thường chỉ là một `dict`."
],

can_biet_truoc: [
  "Bài **SOLID — Open/Closed** — Strategy và Factory là hai cách cụ thể để đạt OCP.",
  "Bài **SOLID — Dependency Inversion** — cả ba mẫu đều dựa trên việc phụ thuộc vào trừu tượng.",
  "Bài **Trừu tượng: ABC, Protocol** — `Protocol` là công cụ mô tả giao diện của các mẫu này.",
  "Bài **Composition over inheritance** — cả ba mẫu là composition, không phải kế thừa.",
  "Bài **Decorator** ở module 5 — decorator đăng ký được dùng trong Factory."
],

dinh_nghia: "Nói đời thường: trong nghề mộc có những kiểu ghép gỗ đã được đặt tên — mộng, chốt, ngàm. Người thợ không phát minh lại chúng, và khi nói \"ghép mộng\" thì đồng nghiệp hiểu ngay. **Design pattern là những kiểu ghép đã đặt tên trong lập trình.**\n\nNói chính xác:\n\n- **Design pattern** — mô tả một giải pháp lặp lại cho một loại vấn đề thiết kế, kèm ngữ cảnh áp dụng và đánh đổi.\n- **Factory** — mẫu tách **việc tạo object** khỏi **việc dùng object**.\n- **Strategy** — mẫu đóng gói một **thuật toán** thành object, cho phép thay thế lúc chạy.\n- **Observer** — mẫu cho một object (**subject**) thông báo cho nhiều object khác (**observer**) khi trạng thái đổi.\n- **Subject** / **Observer** — bên phát và bên nhận trong mẫu Observer.\n\nĐiểm cần nói rõ ngay: **pattern không phải mục tiêu.** Nhiều người học pattern rồi tìm chỗ áp dụng — đó là ngược. Pattern là thứ bạn **nhận ra** khi gặp đúng vấn đề nó giải, và trong Python thì rất nhiều mẫu của sách gốc **đã có sẵn trong ngôn ngữ** dưới dạng đơn giản hơn.",

vi_sao_can: "Ba mẫu trong bài này chiếm phần lớn những gì bạn gặp trong code thật:\n\n- **Chúng là từ vựng chung.** Nói \"chỗ này dùng Strategy\" thay vì giải thích năm câu — cả nhóm hiểu ngay ý định thiết kế.\n- **Bạn đã dùng chúng mà chưa gọi tên.** `sorted(key=...)` là Strategy; `dict.fromkeys` và các hàm `tu_*` là Factory; mọi hệ thống sự kiện là Observer.\n- **Đọc mã nguồn thư viện đòi hỏi nhận ra chúng.** Thư viện chất lượng cao dùng pattern rất nhiều, và tài liệu của chúng gọi tên trực tiếp.\n- **Chúng là SOLID ở dạng cụ thể.** Bài Open/Closed nói \"thêm loại không sửa code cũ\"; Strategy và Factory là hai cách làm điều đó.\n- **Biết mẫu giúp tránh cài đặt sai.** Observer có ba cạm bẫy kinh điển — rò rỉ bộ nhớ, thứ tự không xác định, một observer lỗi làm hỏng cả chuỗi — và biết trước thì tránh được.\n\nTình huống thật rất hay gặp: bạn viết hệ thống thông báo, mỗi khi đơn hàng được tạo thì gửi email, cập nhật kho, ghi log, đẩy dữ liệu sang hệ thống phân tích. Hàm `tao_don` dài dần và giờ **phụ thuộc vào bốn hệ thống khác**. Thêm một việc nữa là sửa nó lần thứ năm. Đây đúng là vấn đề Observer sinh ra để giải — và phần cơ chế sẽ chỉ cách làm cùng với ba cạm bẫy phải tránh.",

co_che: "### Factory — tách việc tạo khỏi việc dùng\n\nVấn đề: code dùng object phải biết **cách tạo** nó, nên thêm loại mới là sửa mọi nơi tạo.\n\n```python\n# ❌ Nơi dùng phải biết mọi loại\ndef xu_ly_file(duong_dan: str):\n    if duong_dan.endswith(\".csv\"):\n        bo_doc = DocCSV(delimiter=\",\", encoding=\"utf-8-sig\")\n    elif duong_dan.endswith(\".json\"):\n        bo_doc = DocJSON(encoding=\"utf-8\")\n    elif duong_dan.endswith(\".xlsx\"):\n        bo_doc = DocExcel(sheet=0)\n    return bo_doc.doc(duong_dan)\n```\n\nBa vấn đề: chi tiết khởi tạo (`delimiter`, `encoding`) rò rỉ ra nơi dùng; thêm định dạng là sửa hàm này; và cùng chuỗi `if` đó sẽ lặp ở mọi nơi cần đọc file.\n\n```python\n# ✓ Factory — một chỗ biết cách tạo\nfrom typing import Protocol\n\nclass BoDoc(Protocol):\n    def doc(self, duong_dan: str) -> list[dict]: ...\n\n\nBANG: dict[str, type] = {}\n\ndef dang_ky(*duoi: str):\n    def deco(cls):\n        for d in duoi:\n            BANG[d] = cls\n        return cls\n    return deco\n\n\n@dang_ky(\".csv\")\nclass DocCSV:\n    def __init__(self, delimiter=\",\"): self.delimiter = delimiter\n    def doc(self, dd): ...\n\n@dang_ky(\".json\")\nclass DocJSON:\n    def doc(self, dd): ...\n\n\ndef tao_bo_doc(duong_dan: str) -> BoDoc:\n    from pathlib import Path\n    duoi = Path(duong_dan).suffix.lower()\n    if duoi not in BANG:\n        raise ValueError(f\"không hỗ trợ {duoi!r}, có: {sorted(BANG)}\")\n    return BANG[duoi]()\n\n\ndef xu_ly_file(duong_dan: str):\n    return tao_bo_doc(duong_dan).doc(duong_dan)      # ⭐ không biết loại nào\n```\n\nBa dạng Factory, từ nhẹ tới nặng:\n\n```\n① HÀM tạo             tao_bo_doc(duoi) — ⭐ đủ cho hầu hết trường hợp\n② classmethod tạo     Nhiet.tu_do_f(212) — hàm tạo phụ, đã gặp ở bài class\n③ Abstract Factory    tạo cả một HỌ object liên quan — hiếm cần\n```\n\nDạng ① là dạng bạn sẽ dùng 90% thời gian. Sách gốc mô tả những dạng phức tạp hơn vì ngôn ngữ ở thời điểm đó không có hàm hạng nhất và không có `dict` tiện lợi.\n\n**Trong Python, Factory thường chỉ là một `dict` cộng một hàm.** Đừng dựng cả một cây class cho việc đó.\n\n### Strategy — thuật toán thay thế được\n\nVấn đề: một thao tác có nhiều cách làm, và cách làm cần chọn được lúc chạy.\n\n```python\n# ❌ Thuật toán bị nhốt trong if\nclass BoNen:\n    def nen(self, du_lieu: bytes, kieu: str) -> bytes:\n        if kieu == \"gzip\":\n            import gzip; return gzip.compress(du_lieu)\n        elif kieu == \"bz2\":\n            import bz2; return bz2.compress(du_lieu)\n        elif kieu == \"khong\":\n            return du_lieu\n```\n\n```python\n# ✓ Strategy — mỗi thuật toán một object\nfrom typing import Protocol\n\nclass ChienLuocNen(Protocol):\n    duoi: str\n    def nen(self, du_lieu: bytes) -> bytes: ...\n    def giai_nen(self, du_lieu: bytes) -> bytes: ...\n\n\nclass NenGzip:\n    duoi = \".gz\"\n    def __init__(self, muc: int = 9): self.muc = muc      # ⭐ có cấu hình\n    def nen(self, d):\n        import gzip; return gzip.compress(d, self.muc)\n    def giai_nen(self, d):\n        import gzip; return gzip.decompress(d)\n\nclass KhongNen:\n    duoi = \"\"\n    def nen(self, d): return d\n    def giai_nen(self, d): return d\n\n\nclass KhoLuu:\n    def __init__(self, nen: ChienLuocNen | None = None):\n        self.nen = nen or KhongNen()          # ⭐ mặc định vô hại\n\n    def luu(self, ten: str, du_lieu: bytes) -> str:\n        from pathlib import Path\n        dd = ten + self.nen.duoi\n        Path(dd).write_bytes(self.nen.nen(du_lieu))\n        return dd\n\n\nkho = KhoLuu(NenGzip(muc=6))\nkho.nen = KhongNen()                          # ⭐ đổi LÚC CHẠY\n```\n\nHai điều Strategy cho mà `if/elif` không cho:\n\n```\n① Thuật toán có CẤU HÌNH riêng (muc=6) — if không mang được trạng thái\n② Đổi được lúc chạy, và người dùng thư viện tự viết chiến lược mới\n```\n\n**Trong Python, Strategy thường chỉ là một hàm:**\n\n```python\nfrom typing import Callable\n\ndef luu(ten: str, du_lieu: bytes,\n        nen: Callable[[bytes], bytes] = lambda d: d) -> None:\n    ...\n\nimport gzip, functools\nluu(\"a\", d, nen=gzip.compress)\nluu(\"a\", d, nen=functools.partial(gzip.compress, compresslevel=6))\n```\n\nQuy tắc chọn:\n\n```\nMột thao tác, không trạng thái        → truyền HÀM\nNhiều thao tác liên quan (nén+giải nén) → dùng CLASS\nCần cấu hình phức tạp                  → dùng CLASS\n```\n\n`sorted(ds, key=...)` là Strategy dựng sẵn của Python, và nó minh hoạ đúng dạng nhẹ: một thao tác, truyền hàm.\n\n### Observer — một nguồn, nhiều nơi nhận\n\nVấn đề: khi một việc xảy ra, nhiều phần khác cần biết — nhưng nguồn không nên biết chúng là ai.\n\n```python\n# ❌ Nguồn biết mọi nơi nhận\nclass DichVuDonHang:\n    def __init__(self, mail, kho, log, phan_tich):\n        self.mail, self.kho = mail, kho\n        self.log, self.phan_tich = log, phan_tich       # ⚠️ 4 phụ thuộc\n\n    def tao_don(self, don):\n        ma = self._luu(don)\n        self.mail.gui(don.email, \"Đã nhận đơn\")\n        self.kho.giu_hang(don.san_pham)\n        self.log.ghi(f\"đơn {ma}\")\n        self.phan_tich.gui_su_kien(\"don_moi\", ma)       # ⚠️ thêm việc = sửa đây\n        return ma\n```\n\n```python\n# ✓ Observer\nfrom typing import Protocol\nfrom dataclasses import dataclass\n\n@dataclass(frozen=True)\nclass SuKienDonMoi:\n    ma: int\n    email: str\n    tong: float\n\n\nclass NguoiQuanSat(Protocol):\n    def nhan(self, sk: SuKienDonMoi) -> None: ...\n\n\nclass NguonSuKien:\n    def __init__(self):\n        self._quan_sat: list[NguoiQuanSat] = []\n\n    def dang_ky(self, qs: NguoiQuanSat) -> None:\n        if qs not in self._quan_sat:\n            self._quan_sat.append(qs)\n\n    def huy_dang_ky(self, qs: NguoiQuanSat) -> None:\n        if qs in self._quan_sat:\n            self._quan_sat.remove(qs)\n\n    def phat(self, sk) -> None:\n        for qs in list(self._quan_sat):        # ⭐ bản sao — xem cạm bẫy ②\n            try:\n                qs.nhan(sk)\n            except Exception:                   # ⭐ cô lập lỗi — cạm bẫy ③\n                import logging\n                logging.exception(\"observer %r lỗi\", qs)\n\n\nclass DichVuDonHang:\n    def __init__(self, kho, nguon: NguonSuKien):\n        self.kho, self.nguon = kho, nguon      # ⭐ chỉ 2 phụ thuộc\n\n    def tao_don(self, don) -> int:\n        ma = self.kho.luu(don)\n        self.nguon.phat(SuKienDonMoi(ma, don.email, don.tong))\n        return ma\n```\n\n`DichVuDonHang` giờ **không biết** ai đang lắng nghe. Thêm một việc là đăng ký thêm một observer ở thành phần gốc — không sửa dòng nào của nghiệp vụ.\n\n### Ba cạm bẫy của Observer\n\n**① Rò rỉ bộ nhớ** — nguồn giữ tham chiếu tới observer, nên observer không bao giờ được thu gom:\n\n```python\nimport weakref\n\nclass NguonSuKienYeu:\n    def __init__(self):\n        self._qs: list = []\n\n    def dang_ky(self, qs) -> None:\n        self._qs.append(weakref.ref(qs))       # ⭐ tham chiếu YẾU\n\n    def phat(self, sk) -> None:\n        con_song = []\n        for r in self._qs:\n            qs = r()\n            if qs is None:                      # đã bị thu gom\n                continue\n            con_song.append(r)\n            try:\n                qs.nhan(sk)\n            except Exception:\n                logging.exception(\"observer lỗi\")\n        self._qs = con_song                     # ⭐ dọn tham chiếu chết\n```\n\nTham chiếu yếu không giữ object sống, nên observer bị huỷ khi không còn ai khác dùng. Đánh đổi: observer **phải** được giữ ở nơi khác, nếu không nó biến mất ngay.\n\n**② Sửa danh sách trong lúc phát** — một observer huỷ đăng ký chính nó khi nhận sự kiện:\n\n```python\nfor qs in self._quan_sat:       # ❌ danh sách đổi giữa chừng ⇒ bỏ sót\n    qs.nhan(sk)\n\nfor qs in list(self._quan_sat): # ✓ lặp trên BẢN SAO\n    qs.nhan(sk)\n```\n\nĐây đúng là bẫy \"sửa list trong lúc lặp\" ở bài generator, xuất hiện lại ở tầng thiết kế.\n\n**③ Một observer lỗi làm hỏng cả chuỗi:**\n\n```python\nfor qs in list(self._quan_sat):\n    try:\n        qs.nhan(sk)\n    except Exception:\n        logging.exception(\"observer %r lỗi\", qs)   # ⭐ ghi log, KHÔNG dừng\n```\n\nQuyết định thiết kế ở đây quan trọng: **nuốt lỗi hay để nó bay lên?** Với Observer thì thường nên cô lập, vì các observer độc lập nhau — email hỏng không nên chặn việc cập nhật kho. Nhưng phải **ghi log đầy đủ**, nếu không đây thành `except: pass` trá hình.\n\nNếu một observer là **bắt buộc thành công**, nó không nên là observer — nó là một bước trong quy trình chính.\n\n### Observer bằng hàm — dạng nhẹ\n\n```python\nfrom collections import defaultdict\nfrom typing import Callable\n\nclass BusSuKien:\n    def __init__(self):\n        self._nghe: dict[str, list[Callable]] = defaultdict(list)\n\n    def nghe(self, ten: str):\n        \"\"\"Dùng làm decorator hoặc gọi trực tiếp.\"\"\"\n        def deco(f):\n            self._nghe[ten].append(f)\n            return f\n        return deco\n\n    def phat(self, ten: str, **du_lieu) -> None:\n        for f in list(self._nghe[ten]):\n            try:\n                f(**du_lieu)\n            except Exception:\n                logging.exception(\"handler %s lỗi\", getattr(f, \"__name__\", f))\n\n\nbus = BusSuKien()\n\n@bus.nghe(\"don_moi\")\ndef gui_mail(ma, email, **_):          # ⭐ **_ để bỏ qua trường không dùng\n    print(f\"gửi mail tới {email} về đơn {ma}\")\n\n@bus.nghe(\"don_moi\")\ndef cap_nhat_kho(ma, **_):\n    print(f\"giữ hàng cho đơn {ma}\")\n\nbus.phat(\"don_moi\", ma=1, email=\"a@x.com\", tong=100_000)\n```\n\nDạng này gọn hơn nhiều và đủ dùng cho phần lớn trường hợp. `**_` là kỹ thuật đáng nhớ: handler chỉ khai báo tham số nó cần, và bỏ qua phần còn lại — nên thêm trường vào sự kiện không phá handler cũ.\n\n### Ba mẫu và SOLID\n\n```\nFactory   → OCP: thêm loại không sửa nơi dùng\n          → DIP: nơi dùng phụ thuộc trừu tượng, không phụ thuộc class cụ thể\n\nStrategy  → OCP: thêm thuật toán không sửa nơi dùng\n          → DIP: ngữ cảnh phụ thuộc giao diện chiến lược\n\nObserver  → OCP: thêm việc không sửa nguồn\n          → SRP: mỗi observer một trách nhiệm\n          → DIP: nguồn không biết ai lắng nghe\n```\n\nBa mẫu này **không phải phát minh mới** — chúng là SOLID được đóng gói và đặt tên cho những tình huống hay gặp.\n\n### Đừng lạm dụng pattern\n\nCân bằng lại, vì đây là chỗ người mới hay sai nhất:\n\n```python\n# ❌ Pattern cho vấn đề không tồn tại\nclass BoTaoSoFactory:\n    def tao(self, gt: int) -> int:\n        return gt\n\nclass ChienLuocCongProtocol(Protocol):\n    def cong(self, a: int, b: int) -> int: ...\n\n# ✓\ndef cong(a: int, b: int) -> int:\n    return a + b\n```\n\nBa dấu hiệu lạm dụng:\n\n```\n· Pattern có nhiều tầng hơn số biến thể nó phục vụ\n· Chỉ có MỘT bản cài đặt, và sẽ mãi vậy\n· Phải mở năm file để lần một luồng đơn giản\n```\n\nNhiều mẫu trong sách gốc **đã có sẵn trong Python** dưới dạng gọn hơn:\n\n```\nStrategy   → truyền hàm; sorted(key=...)\nFactory    → dict ánh xạ + hàm\nCommand    → hàm hoặc functools.partial\nIterator   → giao thức __iter__ (bài generator)\nDecorator  → cú pháp @ (bài decorator)\nSingleton  → module (bài sau sẽ nói)\nTemplate   → hàm bậc cao\n```\n\nCột phải thường **là cách làm đúng trong Python**. Dùng dạng class đầy đủ chỉ khi thật sự cần trạng thái hoặc nhiều thao tác.",

vi_du: "### Factory đầy đủ — bộ đọc dữ liệu\n\n```python\nfrom typing import Protocol\nfrom pathlib import Path\n\nclass BoDoc(Protocol):\n    duoi: tuple[str, ...]\n    def doc(self, duong_dan: str | Path) -> list[dict]: ...\n\n\nBANG_DOC: dict[str, type] = {}\n\ndef dang_ky_doc(cls):\n    for d in cls.duoi:\n        if d in BANG_DOC:\n            raise ValueError(f\"đuôi {d!r} đã đăng ký cho \"\n                             f\"{BANG_DOC[d].__name__}\")       # ⭐ chống trùng\n        BANG_DOC[d] = cls\n    return cls\n\n\n@dang_ky_doc\nclass DocCSV:\n    duoi = (\".csv\", \".tsv\")\n\n    def __init__(self, delimiter: str | None = None):\n        self.delimiter = delimiter\n\n    def doc(self, duong_dan):\n        import csv\n        p = Path(duong_dan)\n        dl = self.delimiter or (\"\\t\" if p.suffix == \".tsv\" else \",\")\n        with open(p, newline=\"\", encoding=\"utf-8-sig\") as f:\n            return list(csv.DictReader(f, delimiter=dl))\n\n\n@dang_ky_doc\nclass DocJSON:\n    duoi = (\".json\",)\n\n    def doc(self, duong_dan):\n        import json\n        du_lieu = json.loads(Path(duong_dan).read_text(encoding=\"utf-8\"))\n        if not isinstance(du_lieu, list):\n            raise ValueError(f\"{duong_dan}: cần một mảng JSON\")\n        return du_lieu\n\n\n@dang_ky_doc\nclass DocJSONL:\n    duoi = (\".jsonl\", \".ndjson\")\n\n    def doc(self, duong_dan):\n        import json\n        kq = []\n        with open(duong_dan, encoding=\"utf-8\") as f:\n            for so, dong in enumerate(f, 1):\n                dong = dong.strip()\n                if not dong:\n                    continue\n                try:\n                    kq.append(json.loads(dong))\n                except json.JSONDecodeError:\n                    print(f\"bỏ dòng {so}\")\n        return kq\n\n\ndef tao_bo_doc(duong_dan: str | Path, **tuy_chon) -> BoDoc:\n    duoi = Path(duong_dan).suffix.lower()\n    cls = BANG_DOC.get(duoi)\n    if cls is None:\n        raise ValueError(f\"không hỗ trợ {duoi!r}, \"\n                         f\"có: {sorted(BANG_DOC)}\")\n    return cls(**tuy_chon)\n\n\ndef doc_bat_ky(duong_dan: str | Path, **tuy_chon) -> list[dict]:\n    return tao_bo_doc(duong_dan, **tuy_chon).doc(duong_dan)\n\n\n# Dùng — không biết loại nào\nfor f in Path(\"du_lieu\").iterdir():\n    try:\n        bg = doc_bat_ky(f)\n        print(f\"{f.name}: {len(bg)} bản ghi\")\n    except ValueError as e:\n        print(f\"bỏ qua {f.name}: {e}\")\n```\n\nThêm định dạng mới — không sửa dòng nào:\n\n```python\n@dang_ky_doc\nclass DocParquet:\n    duoi = (\".parquet\",)\n    def doc(self, duong_dan):\n        ...\n\n# doc_bat_ky() tự nhận được — kể cả từ một module khác\n```\n\nKiểm tra trùng đuôi trong `dang_ky_doc` là chi tiết quan trọng: hai plugin cùng đăng ký `.csv` thì cái sau ghi đè cái trước **im lặng** nếu không kiểm.\n\n### Strategy — chiến lược lấy mẫu dữ liệu\n\n```python\nfrom typing import Protocol\nimport random\n\nclass ChienLuocLayMau(Protocol):\n    ten: str\n    def lay(self, ds: list, n: int) -> list: ...\n\n\nclass LayNgauNhien:\n    ten = \"ngẫu nhiên\"\n    def __init__(self, seed: int | None = None):\n        self.rng = random.Random(seed)            # ⭐ có trạng thái riêng\n    def lay(self, ds, n):\n        return self.rng.sample(ds, min(n, len(ds)))\n\nclass LayDauTien:\n    ten = \"n đầu tiên\"\n    def lay(self, ds, n): return ds[:n]\n\nclass LayCachDeu:\n    ten = \"cách đều\"\n    def lay(self, ds, n):\n        if n >= len(ds): return list(ds)\n        buoc = len(ds) / n\n        return [ds[int(i * buoc)] for i in range(n)]\n\nclass LayPhanTang:\n    ten = \"phân tầng\"\n    def __init__(self, khoa, seed=None):\n        self.khoa, self.rng = khoa, random.Random(seed)\n    def lay(self, ds, n):\n        from collections import defaultdict\n        nhom = defaultdict(list)\n        for x in ds:\n            nhom[self.khoa(x)].append(x)\n        moi_nhom = max(1, n // len(nhom))\n        kq = []\n        for cac_x in nhom.values():\n            kq += self.rng.sample(cac_x, min(moi_nhom, len(cac_x)))\n        return kq[:n]\n\n\nclass BoChuanBiDuLieu:\n    def __init__(self, lay_mau: ChienLuocLayMau):\n        self.lay_mau = lay_mau\n\n    def chuan_bi(self, ds: list, n: int) -> list:\n        mau = self.lay_mau.lay(ds, n)\n        print(f\"lấy {len(mau)}/{len(ds)} bằng cách {self.lay_mau.ten}\")\n        return mau\n\n\ndu_lieu = [{\"id\": i, \"nhan\": \"A\" if i % 3 else \"B\"} for i in range(100)]\n\nbo = BoChuanBiDuLieu(LayNgauNhien(seed=42))\nbo.chuan_bi(du_lieu, 10)\n\nbo.lay_mau = LayPhanTang(khoa=lambda x: x[\"nhan\"], seed=42)   # ⭐ đổi lúc chạy\nbo.chuan_bi(du_lieu, 10)\n```\n\nHai điều Strategy dạng class cho mà truyền hàm không tiện bằng: **trạng thái riêng** (`self.rng` với seed cố định để lặp lại được) và **tên hiển thị** đi kèm thuật toán.\n\nVới trường hợp đơn giản hơn thì hàm vẫn tốt hơn:\n\n```python\nfrom typing import Callable\n\ndef chuan_bi(ds: list, n: int,\n             lay_mau: Callable[[list, int], list] = lambda d, k: d[:k]) -> list:\n    return lay_mau(ds, n)\n\nchuan_bi(du_lieu, 10)                                    # mặc định\nchuan_bi(du_lieu, 10, lay_mau=lambda d, k: random.sample(d, k))\n```\n\n### Strategy dựng sẵn của Python\n\n```python\nnv = [{\"ten\": \"Minh\", \"luong\": 20, \"phong\": \"IT\"},\n      {\"ten\": \"An\", \"luong\": 15, \"phong\": \"HR\"},\n      {\"ten\": \"Bình\", \"luong\": 25, \"phong\": \"IT\"}]\n\nsorted(nv, key=lambda x: x[\"luong\"])                     # Strategy\nsorted(nv, key=lambda x: (x[\"phong\"], -x[\"luong\"]))      # Strategy phức tạp hơn\nmax(nv, key=lambda x: x[\"luong\"])\nmin(nv, key=lambda x: len(x[\"ten\"]))\n\nfrom functools import reduce\nreduce(lambda a, b: a + b[\"luong\"], nv, 0)               # Strategy gộp\n\nimport heapq\nheapq.nlargest(2, nv, key=lambda x: x[\"luong\"])\n```\n\nMọi tham số `key=` là một Strategy. Đây là bằng chứng rằng mẫu này **đã nằm trong ngôn ngữ**, và dựng một `Protocol` cho việc so sánh là làm lại thứ đã có.\n\n### Observer đầy đủ, có xử lý ba cạm bẫy\n\n```python\nimport logging, weakref\nfrom collections import defaultdict\nfrom dataclasses import dataclass\nfrom typing import Callable, Protocol\n\nlog = logging.getLogger(__name__)\n\n\n@dataclass(frozen=True)\nclass SuKien:\n    ten: str\n    du_lieu: dict\n\n\nclass BusSuKien:\n    def __init__(self, co_lap_loi: bool = True):\n        self._nghe: dict[str, list[Callable]] = defaultdict(list)\n        self.co_lap_loi = co_lap_loi\n\n    def nghe(self, ten: str, f: Callable | None = None):\n        \"\"\"Dùng làm decorator hoặc gọi trực tiếp.\"\"\"\n        def dang_ky(g):\n            self._nghe[ten].append(g)\n            return g\n        return dang_ky if f is None else dang_ky(f)\n\n    def bo_nghe(self, ten: str, f: Callable) -> bool:\n        try:\n            self._nghe[ten].remove(f)\n            return True\n        except ValueError:\n            return False\n\n    def phat(self, ten: str, **du_lieu) -> list[Exception]:\n        \"\"\"Phát sự kiện. Trả về danh sách lỗi đã cô lập.\"\"\"\n        sk = SuKien(ten, du_lieu)\n        loi: list[Exception] = []\n\n        for f in list(self._nghe[ten]):            # ⭐ cạm bẫy ②: bản sao\n            try:\n                f(sk)\n            except Exception as e:                  # ⭐ cạm bẫy ③: cô lập\n                if not self.co_lap_loi:\n                    raise\n                log.exception(\"handler %s lỗi khi xử lý %r\",\n                              getattr(f, \"__name__\", f), ten)\n                loi.append(e)\n        return loi\n\n\nbus = BusSuKien()\n\n\n@bus.nghe(\"don_moi\")\ndef gui_email(sk: SuKien) -> None:\n    print(f\"  → mail tới {sk.du_lieu['email']}\")\n\n@bus.nghe(\"don_moi\")\ndef cap_nhat_kho(sk: SuKien) -> None:\n    print(f\"  → giữ hàng cho đơn {sk.du_lieu['ma']}\")\n\n@bus.nghe(\"don_moi\")\ndef handler_hong(sk: SuKien) -> None:\n    raise RuntimeError(\"dịch vụ phân tích đang lỗi\")\n\n@bus.nghe(\"don_moi\")\ndef ghi_log(sk: SuKien) -> None:\n    print(f\"  → ghi log đơn {sk.du_lieu['ma']}\")\n\n\nloi = bus.phat(\"don_moi\", ma=1, email=\"a@x.com\", tong=500_000)\n#   → mail tới a@x.com\n#   → giữ hàng cho đơn 1\n#   (log lỗi của handler_hong)\n#   → ghi log đơn 1          ⭐ VẪN CHẠY dù handler trước lỗi\nprint(f\"{len(loi)} handler lỗi\")\n```\n\nTham số `co_lap_loi` là quyết định thiết kế đáng có: mặc định cô lập (an toàn cho hệ thống sự kiện), nhưng **bật lên được** khi bạn muốn lỗi bay lên — ví dụ trong kiểm thử.\n\nTrả về danh sách lỗi thay vì nuốt hoàn toàn cũng quan trọng: nó cho người gọi biết có gì đó hỏng, thay vì để mọi thứ trông như thành công — đúng nguyên tắc \"không im lặng\" ở bài xử lý ngoại lệ.\n\n### Cạm bẫy ②: sửa danh sách khi đang phát\n\n```python\nbus = BusSuKien()\n\ndef chi_chay_mot_lan(sk):\n    print(\"chạy một lần\")\n    bus.bo_nghe(\"khoi_dong\", chi_chay_mot_lan)     # ⭐ tự huỷ đăng ký\n\ndef luon_chay(sk):\n    print(\"luôn chạy\")\n\nbus.nghe(\"khoi_dong\", chi_chay_mot_lan)\nbus.nghe(\"khoi_dong\", luon_chay)\n\nbus.phat(\"khoi_dong\")\n# chạy một lần\n# luôn chạy        ⭐ nhờ lặp trên list(...) — nếu không sẽ BỎ SÓT\n\nbus.phat(\"khoi_dong\")\n# luôn chạy\n```\n\nKhông có `list(...)`, handler thứ hai bị bỏ qua vì danh sách ngắn lại giữa chừng — và không có lỗi nào báo. Đây là bẫy \"sửa list khi đang lặp\" ở bài generator, xuất hiện ở tầng thiết kế.\n\n### Cạm bẫy ①: rò rỉ bộ nhớ\n\n```python\nimport weakref, gc\n\nclass NguonManh:\n    def __init__(self): self._qs = []\n    def dang_ky(self, qs): self._qs.append(qs)\n    def phat(self, sk):\n        for qs in list(self._qs): qs.nhan(sk)\n\nclass NguonYeu:\n    def __init__(self): self._qs = []\n    def dang_ky(self, qs): self._qs.append(weakref.ref(qs))\n    def phat(self, sk):\n        con = []\n        for r in self._qs:\n            qs = r()\n            if qs is not None:\n                con.append(r); qs.nhan(sk)\n        self._qs = con\n\n\nclass QuanSat:\n    def __init__(self, ten): self.ten = ten\n    def nhan(self, sk): print(f\"{self.ten} nhận\")\n\n\n# Tham chiếu MẠNH — observer không được thu gom\nn1 = NguonManh()\nn1.dang_ky(QuanSat(\"A\"))        # không giữ biến nào\ngc.collect()\nn1.phat(\"x\")                     # A nhận  ⚠️ vẫn sống — rò rỉ\n\n# Tham chiếu YẾU\nn2 = NguonYeu()\nn2.dang_ky(QuanSat(\"B\"))\ngc.collect()\nn2.phat(\"x\")                     # (không in gì) ✓ đã được thu gom\n\n# Nhưng phải GIỮ observer ở đâu đó\nqs = QuanSat(\"C\")\nn2.dang_ky(qs)\nn2.phat(\"x\")                     # C nhận ✓\n```\n\nĐánh đổi rõ ràng: tham chiếu mạnh giữ observer sống mãi (rò rỉ); tham chiếu yếu để observer biến mất nếu không ai giữ (bất ngờ khó chịu).\n\nGiải pháp thực dụng thường dùng: **tham chiếu mạnh cộng với `huy_dang_ky()` tường minh**, và gọi nó trong `__exit__` hoặc khi đóng thành phần:\n\n```python\nfrom contextlib import contextmanager\n\n@contextmanager\ndef lang_nghe(bus, ten, f):\n    bus.nghe(ten, f)\n    try:\n        yield\n    finally:\n        bus.bo_nghe(ten, f)          # ⭐ tự huỷ đăng ký\n\nwith lang_nghe(bus, \"don_moi\", ghi_log):\n    bus.phat(\"don_moi\", ma=1, email=\"a@x.com\")\n# ra khỏi khối ⇒ đã huỷ đăng ký, không rò rỉ\n```\n\nĐây là bài context manager ở module 5 giải quyết đúng cạm bẫy của Observer.\n\n### Ba mẫu dùng chung trong một hệ thống\n\n```python\nfrom typing import Protocol\nfrom dataclasses import dataclass\n\n# ── Strategy: cách tính phí ──\nclass ChienLuocPhi(Protocol):\n    ten: str\n    def tinh(self, don) -> float: ...\n\nclass PhiCoDinh:\n    ten = \"cố định\"\n    def __init__(self, muc: float): self.muc = muc\n    def tinh(self, don): return self.muc\n\nclass PhiTheoTrongLuong:\n    ten = \"theo trọng lượng\"\n    def __init__(self, don_gia: float): self.don_gia = don_gia\n    def tinh(self, don): return don.can_nang * self.don_gia\n\n\n# ── Factory: tạo chiến lược từ cấu hình ──\nBANG_PHI = {\n    \"co_dinh\": lambda cf: PhiCoDinh(cf[\"muc\"]),\n    \"trong_luong\": lambda cf: PhiTheoTrongLuong(cf[\"don_gia\"]),\n}\n\ndef tao_chien_luoc_phi(cf: dict) -> ChienLuocPhi:\n    kieu = cf.get(\"kieu\")\n    if kieu not in BANG_PHI:\n        raise ValueError(f\"kiểu phí {kieu!r} không hỗ trợ, \"\n                         f\"có: {sorted(BANG_PHI)}\")\n    return BANG_PHI[kieu](cf)\n\n\n# ── Observer: thông báo khi đơn được tạo ──\nclass DichVuDonHang:\n    def __init__(self, kho, phi: ChienLuocPhi, bus: BusSuKien):\n        self.kho, self.phi, self.bus = kho, phi, bus\n\n    def tao_don(self, don) -> int:\n        don.phi_van_chuyen = self.phi.tinh(don)      # ⭐ Strategy\n        ma = self.kho.luu(don)\n        self.bus.phat(\"don_moi\", ma=ma, email=don.email,\n                      tong=don.tong + don.phi_van_chuyen)   # ⭐ Observer\n        return ma\n\n\n# ── Thành phần gốc: ghép mọi thứ ──\ncf = {\"phi\": {\"kieu\": \"trong_luong\", \"don_gia\": 5_000}}\n\nbus = BusSuKien()\nbus.nghe(\"don_moi\", gui_email)\nbus.nghe(\"don_moi\", cap_nhat_kho)\n\ndv = DichVuDonHang(\n    kho=KhoPostgres(DSN),\n    phi=tao_chien_luoc_phi(cf[\"phi\"]),               # ⭐ Factory\n    bus=bus,\n)\n```\n\nBa mẫu, ba vai trò rõ ràng:\n\n```\nFactory   đọc cấu hình → tạo đúng chiến lược\nStrategy  đóng gói cách tính phí, đổi được qua cấu hình\nObserver  tách các việc phụ khỏi luồng chính\n```\n\nVà cả ba đều hội tụ ở **thành phần gốc** — đúng mẫu ở bài Dependency Inversion.\n\n### Khi pattern là thừa\n\n```python\n# ❌ Factory cho một loại duy nhất\nclass BoTaoNguoiDung:\n    def tao(self, ten: str, email: str) -> NguoiDung:\n        return NguoiDung(ten, email)\n\n# ✓\nNguoiDung(\"Minh\", \"m@x.com\")\n\n\n# ❌ Strategy cho một thuật toán duy nhất\nclass ChienLuocSapXep(Protocol):\n    def sap(self, ds): ...\n\nclass SapTangDan:\n    def sap(self, ds): return sorted(ds)\n\n# ✓\nsorted(ds)\n\n\n# ❌ Observer cho một người nghe duy nhất, mãi mãi\nbus = BusSuKien()\nbus.nghe(\"xong\", ghi_log)\nbus.phat(\"xong\")\n\n# ✓\nghi_log()\n```\n\nPhép kiểm chung: **pattern có nhiều tầng hơn số biến thể nó phục vụ không?** Có → bỏ pattern đi.\n\nNhịp áp dụng giống các bài SOLID: dùng cách đơn giản nhất chạy được, và **rút ra pattern khi biến thể thứ hai hoặc thứ ba xuất hiện thật** — không phải khi bạn đoán nó sẽ xuất hiện.",

so_sanh: "### Ba mẫu — vấn đề chúng giải\n\n| | Factory | Strategy | Observer |\n|---|---|---|---|\n| Vấn đề | tạo object nào? | làm bằng cách nào? | ai cần biết? |\n| Tách rời | tạo ↔ dùng | thuật toán ↔ ngữ cảnh | nguồn ↔ nơi nhận |\n| Số bên tham gia | 1 nơi dùng | 1 ngữ cảnh, 1 chiến lược | **1 nguồn, N nơi nhận** |\n| Chiều gọi | dùng → factory | ngữ cảnh → chiến lược | **nguồn → nhiều nơi nhận** |\n| SOLID liên quan | OCP, DIP | **OCP** | OCP, SRP, DIP |\n\nHàng \"số bên tham gia\" là điểm phân biệt nhanh nhất: chỉ Observer có quan hệ một–nhiều.\n\n### Dạng Python gọn và dạng sách gốc\n\n| Mẫu | Sách gốc | Python gọn |\n|---|---|---|\n| Strategy | interface + nhiều class | **truyền hàm**, `key=` |\n| Factory | cây class factory | **`dict` + hàm** |\n| Command | interface `execute()` | hàm, `functools.partial` |\n| Iterator | class có `next()` | **`__iter__`, generator** |\n| Decorator | class bọc class | **cú pháp `@`** |\n| Template Method | class trừu tượng | hàm bậc cao |\n\nCột phải thường **là cách làm đúng trong Python**. Dùng dạng class đầy đủ khi thật sự cần trạng thái hoặc nhiều thao tác liên quan.\n\n### Strategy: hàm hay class\n\n| | Truyền hàm | Class |\n|---|---|---|\n| Số thao tác | **1** | nhiều (nén + giải nén) |\n| Trạng thái, cấu hình | qua closure hoặc `partial` | **✓ tự nhiên** |\n| Metadata (tên, đuôi file) | ✗ | **✓** |\n| Độ nhẹ | **nhẹ nhất** | nặng hơn |\n| Kiểm thử | dễ | dễ |\n\nQuy tắc: **một thao tác không trạng thái → hàm; nhiều thao tác liên quan hoặc cần cấu hình → class.**\n\n### Ba dạng Factory\n\n| | Hàm tạo | `classmethod` | Abstract Factory |\n|---|---|---|---|\n| Độ phức tạp | **thấp** | thấp | cao |\n| Tạo | một loại theo tham số | một loại, từ dạng dữ liệu khác | **cả một họ object** |\n| Ví dụ | `tao_bo_doc(duoi)` | `Nhiet.tu_do_f(212)` | bộ tạo giao diện theo nền tảng |\n| Tần suất cần | **rất nhiều** | nhiều | hiếm |\n\nDạng ba hiếm khi cần trong ứng dụng thông thường. Gặp nó trong sách rồi áp dụng bừa là nguồn của những kiến trúc phức tạp vô ích.\n\n### Ba cạm bẫy của Observer\n\n| Cạm bẫy | Biểu hiện | Cách xử lý |\n|---|---|---|\n| **Rò rỉ bộ nhớ** | observer không được thu gom | `weakref`, hoặc huỷ đăng ký tường minh |\n| **Sửa danh sách khi phát** | handler bị bỏ sót, im lặng | lặp trên `list(...)` |\n| **Một lỗi làm hỏng chuỗi** | các handler sau không chạy | `try/except` + ghi log đầy đủ |\n\nCả ba đều là **lỗi im lặng** — không có ngoại lệ nào chỉ ra chúng. Biết trước là cách duy nhất tránh được.\n\n### Cô lập lỗi trong Observer: nên hay không\n\n| | Cô lập lỗi | Để lỗi bay lên |\n|---|---|---|\n| Một handler hỏng | các handler khác vẫn chạy | **dừng toàn bộ** |\n| Người gọi biết có lỗi | qua giá trị trả về / log | **✓ ngay** |\n| Phù hợp | **việc phụ độc lập** | việc bắt buộc thành công |\n| Rủi ro | thành `except: pass` nếu không ghi log | mất các handler sau |\n\nNguyên tắc: **nếu một việc bắt buộc phải thành công thì nó không nên là observer** — nó là một bước trong quy trình chính.\n\n### Khi nào dùng pattern\n\n| Tình huống | Dùng? |\n|---|---|\n| Đã có 2–3 biến thể thật | **✓** |\n| Cần đổi hành vi lúc chạy | ✓ Strategy |\n| Nhiều nơi cần biết một sự kiện | ✓ Observer |\n| Chi tiết khởi tạo phức tạp, lặp nhiều nơi | ✓ Factory |\n| Mới có một biến thể | **✗ đợi** |\n| Python đã có sẵn dạng gọn | **✗ dùng cái có sẵn** |\n| Chỉ để \"cho đúng OOP\" | ✗ |\n\nHàng áp chót là hàng bị bỏ qua nhiều nhất: dựng `Protocol` cho việc so sánh trong khi `sorted(key=...)` đã có sẵn là làm lại thứ ngôn ngữ đã cho.",

loi_thuong_gap: [
  "**Học pattern rồi tìm chỗ áp dụng.** Ngược quy trình — pattern là thứ nhận ra khi gặp đúng vấn đề.",
  "**Dựng Strategy dạng class cho một thao tác không trạng thái.** Truyền hàm là đủ và nhẹ hơn nhiều.",
  "**Dựng Factory cho một loại duy nhất.** Gọi thẳng hàm tạo.",
  "**Dùng Abstract Factory khi một hàm tạo là đủ.** Dạng phức tạp nhất hiếm khi cần trong ứng dụng thường.",
  "**Quên `list(...)` khi lặp qua observer.** Handler tự huỷ đăng ký làm bỏ sót handler sau — im lặng.",
  "**Không cô lập lỗi trong Observer.** Một handler hỏng chặn hết các handler còn lại.",
  "**Cô lập lỗi mà không ghi log.** Thành `except: pass` trá hình — mọi thứ trông như thành công.",
  "**Giữ tham chiếu mạnh tới observer mà không có cách huỷ đăng ký.** Rò rỉ bộ nhớ tích luỹ.",
  "**Dùng `weakref` mà quên giữ observer ở nơi khác.** Nó bị thu gom ngay và không bao giờ nhận sự kiện.",
  "**Đặt việc bắt buộc thành công làm observer.** Nếu nó phải thành công thì nó thuộc quy trình chính.",
  "**Không kiểm tra trùng khoá khi đăng ký trong Factory.** Bản đăng ký sau ghi đè bản trước im lặng.",
  "**Quên rằng bảng đăng ký chỉ đầy khi module được import.** Plugin đặt đúng chỗ mà không hoạt động.",
  "**Làm lại thứ Python đã có.** `key=`, `functools.partial`, giao thức iterator đã là các mẫu này ở dạng gọn."
],

tu_kiem_tra: [
  "Giá trị lớn nhất của design pattern là gì? Vì sao \"học pattern rồi tìm chỗ áp dụng\" là ngược?",
  "Factory giải quyết vấn đề gì? Nêu ba dạng và dạng nào dùng nhiều nhất.",
  "Vì sao trong Python, Factory thường chỉ là một `dict` cộng một hàm?",
  "Strategy cho hai điều gì mà chuỗi `if/elif` không cho?",
  "Khi nào Strategy nên là hàm, khi nào nên là class?",
  "`sorted(ds, key=...)` là mẫu gì? Điều đó nói lên gì về việc dựng `Protocol` cho so sánh?",
  "Observer giải quyết vấn đề gì? Điểm nào phân biệt nó với hai mẫu kia?",
  "Nêu ba cạm bẫy của Observer và cách xử lý mỗi cái. Vì sao cả ba đều nguy hiểm?",
  "Vì sao phải lặp trên `list(self._quan_sat)` chứ không lặp thẳng?",
  "Cô lập lỗi trong Observer nên hay không? Điều kiện để nó không thành `except: pass`?",
  "Khi nào một việc **không** nên là observer?",
  "So sánh tham chiếu mạnh và yếu cho observer — mỗi cách có rủi ro gì?",
  "Ba mẫu này liên hệ với SOLID thế nào?",
  "Nêu bảy mẫu của sách gốc mà Python đã có sẵn dạng gọn hơn.",
  "Phép kiểm chung để biết pattern là thừa?"
],

tai_nguyen: [
  "Sách **Design Patterns** — Gamma, Helm, Johnson, Vlissides. Cuốn gốc; đọc phần mô tả vấn đề và đánh đổi, đừng chép nguyên cấu trúc class.",
  "Sách **Head First Design Patterns** — Freeman & Robson. Dễ tiếp cận hơn nhiều, giải thích rõ vấn đề trước khi đưa giải pháp.",
  "Sách **Fluent Python** — Luciano Ramalho. Có chương riêng về việc các mẫu kinh điển trở nên gọn thế nào trong Python.",
  "Sách **Architecture Patterns with Python** — Harry Percival & Bob Gregory. Áp dụng Observer và bus sự kiện vào dự án thật.",
  "Tài liệu Python, module `functools` — `partial` và `singledispatch` là công cụ thay thế cho vài mẫu kinh điển."
],

lien_he: [
  "**SOLID — Open/Closed**: Strategy và Factory là hai cách cụ thể để đạt OCP; bảng đăng ký đã gặp ở đó.",
  "**SOLID — Dependency Inversion**: cả ba mẫu đều dựa trên phụ thuộc vào trừu tượng, và hội tụ ở thành phần gốc.",
  "**SOLID — Single Responsibility**: Observer tách các việc phụ ra khỏi luồng chính, mỗi handler một trách nhiệm.",
  "**Composition over inheritance**: cả ba mẫu là composition; Strategy là ví dụ rõ nhất.",
  "**Trừu tượng: ABC, Protocol**: `Protocol` mô tả giao diện của chiến lược, của bộ tạo, của người quan sát.",
  "**Decorator** ở module 5: decorator đăng ký dùng trong Factory, và `@bus.nghe(...)` dùng trong Observer.",
  "**Context manager và `with`** ở module 5: `with lang_nghe(...)` giải quyết cạm bẫy rò rỉ của Observer.",
  "**Xử lý ngoại lệ** ở module 5: cô lập lỗi trong Observer phải kèm ghi log, nếu không thành `except: pass`.",
  "**Generator, iterator** ở module 5: bẫy \"sửa list khi đang lặp\" xuất hiện lại trong Observer.",
  "**Design pattern — Singleton, Adapter, Repository** ngay sau: ba mẫu còn lại, gồm cả những mẫu Python xử lý rất khác sách gốc.",
  "**Backend & Web** ở mảng 05: bus sự kiện, hàng đợi công việc, và webhook đều là Observer ở quy mô hệ thống.",
  "**AI/ML** ở mảng 07: callback trong vòng lặp huấn luyện là Observer; đổi bộ tối ưu hay hàm mất mát là Strategy."
]

};
