window.LESSONS["05:auth-bao-mat:oauth2-va-dang-nhap-bang-google-github"] = {

tom_tat: [
  "OAuth 2.0 là khung ủy quyền: StudyHub nhận quyền đọc danh tính cơ bản từ Google hoặc GitHub mà không hề thấy mật khẩu tại nhà cung cấp. OpenID Connect bổ sung lớp xác thực bằng `id_token`; nhầm hai vai trò này tạo ra lỗ hổng đăng nhập.",
  "Luồng Authorization Code kết hợp PKCE ràng buộc lần bắt đầu và lần đổi mã bằng `state`, `nonce`, `code_verifier`; callback chỉ an toàn khi kiểm tra đủ redirect URI, chữ ký, issuer, audience và thời hạn.",
  "Tài khoản nội bộ phải có định danh riêng. Khóa `(provider, provider_subject)` liên kết Google/GitHub với StudyHub; email chỉ là thuộc tính có thể đổi, không phải khóa bền vững.",
  "Đăng nhập liên kết làm giảm gánh nặng mật khẩu nhưng chuyển một phần rủi ro sang nhà cung cấp, cấu hình OAuth, quản lý secret, cookie phiên và quy trình khôi phục tài khoản.",
  "Ví dụ FastAPI triển khai một phiên bản tối thiểu có thể chạy bằng nhà cung cấp giả lập, đồng thời chỉ rõ phần nào phải thay bằng thư viện chuẩn và discovery metadata khi đưa lên production."
],

can_biet_truoc: [
  "HTTP redirect, query string, status code 302 và cookie `HttpOnly`, `Secure`, `SameSite`.",
  "Phân biệt authentication (xác thực danh tính) với authorization (kiểm tra quyền), cùng khái niệm session phía server.",
  "Hash mật mã SHA-256, chữ ký số bất đối xứng và lý do không thể coi base64url là mã hóa.",
  "FastAPI dependency, route bất đồng bộ và biến môi trường; không cần biết trước SDK riêng của Google hay GitHub."
],

dinh_nghia: "**OAuth 2.0** là một khung giao thức để chủ tài nguyên cho một ứng dụng khách quyền truy cập có giới hạn vào tài nguyên do một authorization server bảo vệ, mà không chuyển mật khẩu của chủ tài nguyên cho ứng dụng khách. Trong StudyHub, người học bấm “Đăng nhập bằng GitHub”; GitHub hỏi sự đồng ý rồi cấp một authorization code ngắn hạn cho StudyHub. Backend đổi code lấy access token và dùng token ấy gọi endpoint danh tính.\n\nOAuth 2.0 tự nó nói về **ủy quyền**, không chuẩn hóa đầy đủ việc “ai vừa đăng nhập”. **OpenID Connect (OIDC)** là lớp nhận dạng xây trên OAuth 2.0, thêm scope `openid`, `id_token`, discovery metadata và quy tắc xác minh danh tính. Google hỗ trợ OIDC. Với GitHub OAuth App, StudyHub thường lấy hồ sơ qua API của GitHub rồi tự thiết lập phiên nội bộ.\n\nBa actor phải gọi đúng tên: browser của người dùng là user agent; StudyHub là OAuth client; Google/GitHub là authorization server kiêm resource server. `client_id` là định danh công khai, còn `client_secret` là bí mật của backend chứ không phải mật khẩu người dùng.",

vi_sao_can: "Nếu StudyHub tự thu mật khẩu Google, nó vừa yêu cầu người dùng tin một trang thứ ba vừa phải bảo vệ một bí mật có thể mở cả hộp thư. OAuth thay việc trao mật khẩu bằng một quyền hạn hẹp, có thời hạn và có thể thu hồi. Nhà cung cấp còn gánh MFA, phát hiện đăng nhập bất thường và quy trình khôi phục mà một đội nhỏ khó làm tốt.\n\nLợi ích ấy không miễn phí. Một callback ghép tài khoản bằng email chưa xác minh có thể trao nhầm workspace. Một `state` không được đối chiếu cho phép login CSRF: nạn nhân tưởng đăng nhập tài khoản mình nhưng phiên StudyHub lại gắn với tài khoản của kẻ tấn công. Một access token lọt vào log có thể bị dùng ngoài StudyHub cho tới khi hết hạn hoặc bị thu hồi.\n\nVì vậy mục tiêu không phải “thêm nút Google”. Mục tiêu là dựng một biên tin cậy: dữ liệu nào đến từ browser không đáng tin, dữ liệu nào đã được nhà cung cấp ký, quyền nào thực sự cần, định danh nào bền vững, và StudyHub sẽ phản ứng ra sao khi provider chậm, người dùng từ chối consent hoặc khóa liên kết cuối cùng bị mất.",

co_che: "### Bản đồ của một lần đăng nhập\n\nLuồng an toàn bắt đầu ở backend, không bắt đầu bằng việc frontend tự chế URL. StudyHub sinh ba giá trị ngẫu nhiên cho từng lần thử: `state` chống tráo giao dịch, `nonce` ràng buộc ID token với yêu cầu OIDC, và `code_verifier` cho PKCE. Từ verifier, backend tính `code_challenge = BASE64URL(SHA256(verifier))`. Backend lưu state tạm thời cùng verifier, nonce, provider và đường dẫn hợp lệ muốn quay lại; browser chỉ giữ một cookie tham chiếu ngắn hạn.\n\n```\nBrowser        StudyHub                  Provider\n  | GET /login/google |                     |\n  |------------------>| tạo state + PKCE    |\n  |<------ 302 authorize?state=... ---------|\n  |---------------- consent --------------->|\n  |<------ 302 /callback?code=...&state=... |\n  |------------------>| POST token + verifier\n  |                    |<-- tokens ----------|\n  |                    | verify / fetch user |\n  |<------ cookie phiên StudyHub ------------|\n```\n\nAuthorization code đi qua browser nên được coi là dữ liệu có thể lộ; nó dùng một lần, sống ngắn và chỉ đổi được khi client chứng minh verifier. Access token chỉ chạy trên kênh backend-to-provider. Kết thúc luồng, StudyHub phát **cookie phiên của chính mình**. Không nên dùng access token Google như session token cho mọi API StudyHub: audience, vòng đời, khả năng thu hồi và semantics của nó thuộc provider.\n\n### `state`, `nonce` và PKCE giải ba bài toán khác nhau\n\n`state` liên kết callback với browser đã khởi tạo yêu cầu. Giá trị phải đủ ngẫu nhiên, dùng một lần, hết hạn nhanh và được so sánh chính xác. Đừng nhét URL tùy ý vào state rồi redirect trực tiếp; hãy lưu một mã tham chiếu tới danh sách đường dẫn nội bộ cho phép, nếu không callback biến thành open redirect.\n\n`nonce` nằm trong yêu cầu OIDC và phải xuất hiện y nguyên trong claim của `id_token`. Nó ngăn một ID token hợp lệ từ giao dịch khác bị phát lại. PKCE lại bảo vệ authorization code: kẻ chỉ nhìn thấy code không có verifier nên không đổi được token. Ba cơ chế bổ sung nhau, không cơ chế nào thay thế hai cái còn lại.\n\nỨng dụng backend bí mật vẫn nên dùng PKCE. `client_secret` xác thực ứng dụng với token endpoint nhưng không ràng buộc code với browser cụ thể; hơn nữa secret có thể bị cấu hình sai hoặc lộ. PKCE giảm tác hại của việc chặn code với chi phí rất nhỏ.\n\n### Đổi code và xác minh phản hồi\n\nToken endpoint phải được gọi bằng HTTPS từ backend, với timeout ngắn hợp lý và không tự động retry mù quáng. Retry sau timeout có thể gặp code đã được dùng nhưng phản hồi đầu bị mất; ứng dụng nên trả lỗi có thể thử đăng nhập lại, thay vì lặp vô hạn. Không ghi toàn bộ response token vào log.\n\nVới OIDC, nhận được JWT không đồng nghĩa đã xác thực. StudyHub lấy bộ khóa công khai từ `jwks_uri` trong discovery metadata, chọn khóa theo `kid`, rồi kiểm tra chữ ký và thuật toán được cấu hình cho phép. Sau đó kiểm `iss` đúng provider, `aud` chứa client ID, `exp` chưa qua, `iat` hợp lý và `nonce` trùng. Không lấy thuật toán từ token rồi tin vô điều kiện. Cache JWKS có thời hạn và khi gặp `kid` mới có thể tải lại một lần để chịu được key rotation.\n\nVới GitHub OAuth, backend dùng access token gọi API hồ sơ và endpoint email nếu cần. Mọi trường từ API vẫn phải được kiểm kiểu và giới hạn độ dài trước khi lưu. Token chỉ được cấp scope tối thiểu: đăng nhập thường không cần quyền repo. Scope rộng làm consent đáng sợ hơn và tăng blast radius khi token lộ.\n\n### Định danh nội bộ không phải email\n\nStudyHub tạo `users.id` độc lập với provider, rồi lưu bảng liên kết `external_identities(user_id, provider, subject)`. Cặp `(provider, subject)` có unique constraint. `subject` là định danh ổn định provider công bố; cùng chuỗi subject ở hai provider không mặc nhiên là cùng người.\n\nEmail có thể đổi, tái sử dụng hoặc không được provider xác minh. Quy tắc tự động nối hai tài khoản chỉ vì email giống nhau dễ dẫn tới account takeover. Cách thận trọng là yêu cầu người dùng đang có phiên StudyHub xác nhận thao tác “liên kết provider mới”, có thể kèm re-authentication. Nếu sản phẩm quyết định gộp theo email đã xác minh, đó phải là quyết định rủi ro được ghi rõ, có audit event và đường hoàn tác.\n\nCa biên đáng chú ý: provider không trả email; tài khoản tổ chức che email; người dùng thu hồi email permission; một provider trả email với chữ hoa khác nhau; hai callback đến đồng thời. Unique constraint và transaction giúp hai callback không tạo hai user. Không nên lowercase mọi định danh provider; chỉ chuẩn hóa email theo chính sách riêng, còn subject giữ nguyên.\n\n### Phiên StudyHub sau callback\n\nCallback nên đổi session ID để chặn session fixation, rồi đặt cookie ngẫu nhiên chỉ tham chiếu tới state phía server. Cookie có `HttpOnly` để JavaScript không đọc, `Secure` để chỉ qua HTTPS và `SameSite=Lax` thường phù hợp với top-level OAuth redirect. Nếu kiến trúc cross-site đòi `SameSite=None`, bắt buộc `Secure` và phải bổ sung phòng vệ CSRF cho thao tác thay đổi dữ liệu.\n\nSession chứa `user_id`, thời điểm xác thực, phiên bản quyền và hạn tuyệt đối; không chứa access token provider ở browser. Nếu StudyHub không cần gọi provider sau đăng nhập, token nên bị bỏ sau khi lấy danh tính. Nếu cần đồng bộ lịch hoặc repo, token phải mã hóa khi lưu, tách quyền truy cập, có quy trình refresh/revoke và tuyệt đối không xuất hiện trong telemetry.\n\nĐăng xuất StudyHub chỉ xóa phiên nội bộ; nó không nhất thiết đăng xuất Google/GitHub. Giao diện cần nói rõ điều này. Global logout có thể thu hồi mọi session StudyHub của user, còn thu hồi provider token là thao tác riêng và có thể làm ngừng chức năng tích hợp.\n\n### Callback là endpoint nhạy cảm\n\nRedirect URI phải đăng ký chính xác tại provider và backend phải dùng một giá trị cố định theo môi trường. Cho phép wildcard hoặc dựng URI từ header `Host` không tin cậy mở cửa cho code bị gửi sang domain khác. Reverse proxy phải cấu hình danh sách proxy tin cậy trước khi dùng forwarded headers.\n\nCallback nhận cả nhánh thành công lẫn `error=access_denied`. Lỗi hiển thị cho người dùng không được phản chiếu nguyên văn mô tả từ query. Backend xóa state dù thành công hay thất bại để chống phát lại, nhưng log audit có thể giữ mã tương quan, provider, loại kết quả và user nội bộ nếu đã biết; không giữ code/token.\n\nCác callback đồng thời, nút back, refresh trang và mạng chập chờn là chuyện bình thường. Hành vi đúng là lần đầu tiêu thụ state thành công, các lần sau nhận thông báo “liên kết đã hết hạn, hãy thử lại”, không tạo thêm tài khoản và không trả stack trace.\n\n### Account linking và unlinking\n\nLiên kết Google vào user đang đăng nhập là thao tác nhạy cảm hơn đăng nhập mới: attacker chiếm một session ngắn có thể cài “cửa sau” lâu dài. StudyHub yêu cầu phiên vừa được xác thực, hiển thị provider account sắp nối, ghi audit và gửi thông báo. Không cho gỡ phương thức đăng nhập cuối cùng nếu user chưa thêm phương thức khác hoặc recovery code.\n\nMột provider identity chỉ thuộc một user. Khi unique constraint báo identity đã gắn nơi khác, hệ thống không tự chuyển quyền sở hữu. Quy trình hỗ trợ phải xác minh cả hai phía; nhân viên không nên chỉ nhìn email rồi sửa database.\n\n### Mobile, SPA và backend truyền thống\n\nVới server-rendered FastAPI, backend giữ secret và đổi code. Với SPA thuần hoặc ứng dụng mobile, client là public client: không thể giấu `client_secret`, vì bất kỳ người dùng nào cũng đọc binary hoặc bundle. Authorization Code + PKCE là cốt lõi; không nhúng secret giả vào frontend. Mobile dùng redirect URI/app link được đăng ký và hệ điều hành bảo vệ.\n\nImplicit flow từng trả token qua URL fragment nhưng làm token đi qua user agent và khó áp dụng nhiều phòng vệ hiện đại; dự án mới không có lý do chọn nó. Resource Owner Password Credentials còn tệ hơn vì ứng dụng thu mật khẩu provider, phá mục tiêu ban đầu của OAuth.\n\n### Mô hình đe dọa và quan sát vận hành\n\nDashboard nên tách số lần bắt đầu, callback, state mismatch, provider denial, token exchange timeout, invalid issuer/audience, user mới và account link. Tỷ lệ callback thấp có thể do cookie `SameSite`, redirect URI sai hoặc provider outage. Nhiều state mismatch từ một mạng có thể là tấn công, nhưng cũng có thể do người dùng mở nhiều tab; đừng khóa tài khoản chỉ từ một tín hiệu.\n\nRate limit `/login` và `/callback` theo nhiều chiều nhưng không làm hỏng người dùng chung NAT. Cảnh báo khi scope cấu hình thay đổi, redirect URI production đổi, hoặc lượng account linking tăng đột biến. Audit event dùng mã định danh nội bộ và correlation ID, không dùng token làm định danh.\n\n### Khi nào không nên thêm social login\n\nỨng dụng nội bộ của doanh nghiệp có thể cần SSO qua OIDC/SAML của tổ chức thay vì nút Google công cộng. Hệ thống offline hoặc môi trường điều khiển công nghiệp không nên phụ thuộc availability của provider. Sản phẩm cho trẻ em, y tế hoặc khu vực có yêu cầu dữ liệu nghiêm ngặt phải đánh giá consent, độ tuổi và việc chuyển metadata trước khi tích hợp.\n\nNgay cả với StudyHub, thêm ba provider không mặc nhiên tốt hơn một provider: mỗi provider tăng cấu hình, test matrix, quy trình sự cố và support cho account linking. Chỉ thêm khi lợi ích chuyển đổi đăng nhập và nhu cầu người dùng lớn hơn chi phí vận hành.",

vi_du: "### FastAPI tối thiểu với provider giả lập\n\nVí dụ dưới chạy bằng `uvicorn app:app --reload`. Nó tập trung vào state và PKCE; provider giả giúp kiểm thử không cần client secret thật. Production phải thay hàm đổi code bằng HTTP client có timeout và xác minh OIDC/JWKS qua thư viện đã được duy trì.\n\n```python\nimport base64, hashlib, secrets, time\nfrom fastapi import FastAPI, HTTPException, Request\nfrom fastapi.responses import RedirectResponse\n\napp = FastAPI()\npending: dict[str, dict] = {}\nsessions: dict[str, str] = {}\n\ndef b64url(raw: bytes) -> str:\n    return base64.urlsafe_b64encode(raw).rstrip(b\"=\").decode()\n\n@app.get(\"/login/{provider}\")\ndef login(provider: str):\n    if provider not in {\"google\", \"github\"}:\n        raise HTTPException(404, \"Provider không được hỗ trợ\")\n    state = secrets.token_urlsafe(32)\n    verifier = secrets.token_urlsafe(48)\n    pending[state] = {\"provider\": provider, \"verifier\": verifier,\n                      \"expires\": time.time() + 300}\n    challenge = b64url(hashlib.sha256(verifier.encode()).digest())\n    # Route /mock-authorize đóng vai provider trong bài thực hành.\n    return RedirectResponse(\n        f\"/mock-authorize?state={state}&code_challenge={challenge}\"\n    )\n\n@app.get(\"/mock-authorize\")\ndef mock_authorize(state: str, code_challenge: str):\n    code = \"demo-code:\" + code_challenge\n    return RedirectResponse(f\"/callback?state={state}&code={code}\")\n\n@app.get(\"/callback\")\ndef callback(state: str, code: str):\n    attempt = pending.pop(state, None)  # tiêu thụ đúng một lần\n    if not attempt or attempt[\"expires\"] < time.time():\n        raise HTTPException(400, \"Giao dịch đăng nhập không hợp lệ\")\n    expected = b64url(hashlib.sha256(attempt[\"verifier\"].encode()).digest())\n    if not secrets.compare_digest(code, \"demo-code:\" + expected):\n        raise HTTPException(400, \"Authorization code không hợp lệ\")\n    # subject này bình thường lấy từ claim đã xác minh hoặc provider API.\n    user_id = f\"user-for-{attempt['provider']}-subject-123\"\n    sid = secrets.token_urlsafe(32)\n    sessions[sid] = user_id\n    response = RedirectResponse(\"/me\", status_code=303)\n    response.set_cookie(\"studyhub_session\", sid, httponly=True,\n                        secure=False, samesite=\"lax\", max_age=3600)\n    return response\n\n@app.get(\"/me\")\ndef me(request: Request):\n    user_id = sessions.get(request.cookies.get(\"studyhub_session\", \"\"))\n    if not user_id:\n        raise HTTPException(401, \"Chưa đăng nhập\")\n    return {\"user_id\": user_id}\n```\n\n`secure=False` chỉ để localhost nhận cookie HTTP. Production bắt buộc HTTPS và `secure=True`; kho pending/session phải chuyển sang storage dùng chung có TTL và thao tác lấy-xóa nguyên tử. Thử mở `/login/google`, sau đó refresh URL callback: lần thứ hai phải trả 400 vì state đã bị tiêu thụ. Đổi một ký tự trong code cũng phải thất bại.\n\n### Ma trận kiểm thử callback\n\n| Ca kiểm thử | Kết quả bắt buộc |\n|---|---|\n| state đúng, code đúng, chưa hết hạn | tạo hoặc tìm đúng user, xoay session ID |\n| thiếu state hoặc state ngẫu nhiên | 400 chung chung, không gọi token endpoint |\n| callback được phát lại | thất bại, không sinh phiên thứ hai |\n| provider trả `access_denied` | thông báo an toàn, xóa pending state |\n| subject cũ nhưng email mới | vẫn vào cùng user nội bộ |\n| email trùng nhưng subject mới | không tự động chiếm tài khoản cũ |\n| token sai issuer hoặc audience | từ chối trước khi tạo session |",

so_sanh: "### Chọn đúng cơ chế đăng nhập\n\n| Phương án | StudyHub giữ mật khẩu? | Điểm mạnh | Chi phí/rủi ro | Khi phù hợp |\n|---|---:|---|---|---|\n| Mật khẩu nội bộ + Argon2 | có hash | không phụ thuộc provider; recovery tự chủ | phải vận hành MFA, reset, chống stuffing | cần tài khoản độc lập hoặc offline |\n| OAuth 2.0 + provider API | không | tích hợp GitHub đơn giản; xin scope API | profile API không phải ID token; semantics riêng provider | đăng nhập GitHub và tích hợp repo |\n| OpenID Connect Authorization Code + PKCE | không | chuẩn hóa danh tính, discovery, nonce | xác minh claim/JWKS và key rotation | Google, enterprise IdP, SSO hiện đại |\n| SAML enterprise | không | phổ biến ở hệ thống doanh nghiệp cũ | XML, metadata và vận hành phức tạp hơn | khách hàng yêu cầu SSO SAML |\n| Magic link | không | trải nghiệm nhẹ, không cần social account | phụ thuộc an toàn email; link bị forward | sản phẩm ít dùng, rủi ro vừa |\n\nOIDC không thay RBAC: provider chứng minh một subject, còn StudyHub vẫn phải quyết định subject ấy có được sửa học liệu trong workspace hay không. Social login cũng không tự động an toàn hơn mọi mật khẩu; nếu tài khoản provider bị chiếm hoặc quy trình linking yếu, thiệt hại lan sang StudyHub.",

loi_thuong_gap: [
  "Dùng email làm primary key hoặc tự gộp tài khoản khi email giống nhau; nhận biết qua hai provider khác nhau dẫn vào cùng user mà không có bước xác nhận.",
  "Chỉ decode `id_token` rồi tin claims, không verify chữ ký, `iss`, `aud`, `exp`, `nonce`; test token tự ký vẫn đăng nhập được là dấu hiệu rõ nhất.",
  "Không lưu và đối chiếu `state`, hoặc dùng state cố định; callback từ một browser khác vẫn thành công cho thấy login CSRF còn mở.",
  "Đưa `client_secret` vào JavaScript/mobile bundle hay repository; tìm thấy chuỗi secret trong DevTools hoặc lịch sử Git là sự cố phải rotate.",
  "Ghi access token, authorization code hoặc toàn bộ query callback vào log; kiểm tra log/trace/APM thấy credential là bằng chứng, không phải lỗi thẩm mỹ.",
  "Xin scope rộng “để sau dùng”; màn consent liệt kê repo/email/quyền ghi dù chức năng chỉ cần tên và avatar.",
  "Dựng redirect URI từ header không tin cậy hoặc cho phép return URL ngoài site; sửa `Host`/`next` mà redirect sang domain khác chứng minh open redirect.",
  "Xóa liên kết provider cuối cùng mà không có phương thức recovery; user đăng xuất xong không còn đường quay lại tài khoản."
],

tu_kiem_tra: [
  "Vì sao `client_secret` không thay thế được PKCE, và PKCE không thay thế được `state`?",
  "Hãy liệt kê mọi trường phải kiểm trong một `id_token` trước khi lấy `sub`; trường nào ràng buộc token với đúng StudyHub?",
  "Hai callback đồng thời mang cùng state đi vào hai worker. Storage cần thao tác nguyên tử nào để chỉ một callback tạo phiên?",
  "Một user Google đổi email rồi đăng nhập lại. Thiết kế đúng tìm tài khoản bằng dữ liệu nào, và vì sao?",
  "Nếu provider token exchange timeout sau khi provider đã tiêu thụ code, retry tự động gây trải nghiệm gì? Bạn sẽ trả lỗi và quan sát ra sao?",
  "StudyHub cần đọc repo GitHub mỗi đêm. Token nên nằm ở đâu, scope nào, và logout nội bộ có cần revoke token không?",
  "Tại sao cookie `SameSite=Strict` có thể làm hỏng callback, còn `SameSite=None` lại mở rộng bề mặt CSRF?"
],

tai_nguyen: [
  "OAuth 2 in Action — Justin Richer và Antonio Sanso.",
  "OAuth 2.0 Security Best Current Practice — tài liệu chuẩn của IETF.",
  "OpenID Connect Core — đặc tả của OpenID Foundation.",
  "Web Security for Developers — Malcolm McDonald."
],

lien_he: [
  "Mảng Cơ sở dữ liệu: unique constraint và transaction bảo đảm một `(provider, subject)` không tạo hai user khi callback chạy đồng thời.",
  "Mảng Mạng máy tính: HTTPS, cookie và redirect quyết định authorization code có bị lộ hoặc phiên có đi sai origin hay không.",
  "Mảng Kiến trúc hệ thống: session store dùng chung, timeout, circuit breaker và quan sát provider outage trở nên bắt buộc khi StudyHub chạy nhiều instance.",
  "Mảng DevOps: client secret, encryption key và redirect URI phải được phân tách theo môi trường; đây là cầu nối trực tiếp tới bài quản lý secret của module."
]

};
