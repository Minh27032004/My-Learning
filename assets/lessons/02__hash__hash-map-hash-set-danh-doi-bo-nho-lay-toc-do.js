window.LESSONS["02:hash:hash-map-hash-set-danh-doi-bo-nho-lay-toc-do"] = {

tom_tat: [
  "**Hai bài trước giải thích cơ chế; bài này định giá nó.** `dict` tốn khoảng 4–9 lần bộ nhớ so với `list` cùng số phần tử — đó là giá của tra cứu O(1).",
  "**Ba nguồn tiêu tốn bộ nhớ, xếp theo mức độ:** phải dư ~1/3 chỗ (hệ số tải 2/3), lưu cả khoá lẫn hash, và mỗi phần tử là một `PyObject` riêng.",
  "**`set` KHÔNG nhẹ hơn `dict` như trực giác** — nó thường tốn tương đương hoặc hơn, vì hệ số tải thấp hơn và không có thiết kế compact hai tầng.",
  "**Ngưỡng hoà vốn rất thấp: khoảng 10–30 phần tử.** Dưới ngưỡng đó, duyệt `list` nhanh hơn tra `set` vì chi phí tính hash lấn át.",
  "**Ba cách giảm bộ nhớ mà không mất O(1):** `__slots__` cho object, lưu khoá gọn hơn, và các cấu trúc xác suất như Bloom filter khi chấp nhận sai số."
],

can_biet_truoc: [
  "Bài **Hàm băm và bảng băm** và **Xử lý va chạm** trong module này — hệ số tải, open addressing, và vì sao Python phải dư chỗ.",
  "Bài **Array và dynamic array** ở module 2 mảng này — mảng con trỏ so với mảng số thô, và `PyObject` header.",
  "Bài **Độ phức tạp không gian** ở module 1 mảng này — đánh đổi bộ nhớ ↔ thời gian theo cả hai chiều.",
  "Bài **Ước lượng 10⁸ phép tính/giây** ở module 1 mảng này — cách ước lượng bộ nhớ theo thứ tự độ lớn."
],

dinh_nghia: "Nói đời thường: một kho hàng có đánh số vị trí thì lấy hàng rất nhanh, nhưng phải để trống nhiều lối đi và biển chỉ dẫn. Một kho chất đống thì tận dụng hết diện tích, nhưng tìm một món phải lục cả kho. **Bạn không thể có cả hai** — và bài này định lượng cái giá đó.\n\nCác khái niệm:\n\n- **Đánh đổi bộ nhớ–thời gian** (*space–time tradeoff*) — dùng thêm bộ nhớ để chạy nhanh hơn, hoặc ngược lại.\n- **Chi phí trên mỗi phần tử** (*per-element overhead*) — số byte một cấu trúc tốn thêm cho mỗi phần tử, ngoài dữ liệu thật.\n- **Ngưỡng hoà vốn** (*break-even point*) — kích thước n mà tại đó hai cấu trúc cho hiệu năng như nhau.\n- **Cấu trúc xác suất** (*probabilistic data structure*) — cấu trúc đổi **độ chính xác** lấy bộ nhớ. Ví dụ: Bloom filter.\n- **Âm tính giả / dương tính giả** — Bloom filter có thể nói \"có\" khi thật ra không có (dương tính giả), nhưng **không bao giờ** nói \"không\" khi thật ra có.\n\nBa cấu trúc, ba vị trí trên trục đánh đổi:\n\n```\n          ÍT BỘ NHỚ ←──────────────────────→ NHANH TRA CỨU\n\n  list          set / dict          set + chỉ mục phụ\n  O(n) tra      O(1) tra            O(1) nhiều chiều\n  ít byte       ~4–9× list          nhiều hơn nữa\n\n  Bloom filter\n  ⭐ ít byte NHẤT, O(1) tra\n  ⚠️ nhưng có DƯƠNG TÍNH GIẢ\n```\n\nCâu hỏi trung tâm của bài, và nó không có câu trả lời chung:\n\n```\n\"Đổi bao nhiêu bộ nhớ để lấy bao nhiêu tốc độ, và có đáng không?\"\n\n⇒ Phụ thuộc n, phụ thuộc số lần tra cứu, và phụ thuộc\n  ngân sách bộ nhớ thật của bạn.\n```",

vi_sao_can: "Hai bài trước dạy cơ chế; bài này dạy **quyết định**:\n\n- **\"Đổi list thành set\" không miễn phí.** Hai module trước đã khuyên điều đó khoảng mười lần, luôn kèm ghi chú \"O(n) bộ nhớ\" mà chưa định lượng. Với 50 triệu phần tử, khác biệt là giữa chạy được và bị container giết.\n- **Ngưỡng hoà vốn thấp hơn nhiều người nghĩ.** Với n dưới khoảng 10, `list` **nhanh hơn** `set` — và rất nhiều vòng lặp trong code thật có n nhỏ như vậy.\n- **`set` không nhẹ hơn `dict`.** Đây là hiểu lầm phổ biến: người ta nghĩ \"set chỉ lưu khoá nên nhẹ hơn\", nhưng đo ra thường ngược lại.\n- **Có những cách giảm bộ nhớ mà không mất O(1).** `__slots__`, khoá gọn hơn, và cấu trúc xác suất — biết chúng là biết mình còn lựa chọn nào khi bộ nhớ chật.\n- **Bloom filter là một mẫu tư duy quan trọng.** Nó dạy rằng đôi khi lời giải đúng là **hạ yêu cầu** thay vì tối ưu hiện thực — và mẫu này gặp lại ở rất nhiều nơi trong hệ thống lớn.\n\nTình huống thật rất cụ thể: bạn lọc trùng 80 triệu URL trong một trình thu thập dữ liệu. Dùng `set` thì cần khoảng 10 GB — vượt máy chủ. Ba lựa chọn: chia lô và xử lý từng phần (chậm hơn nhưng chính xác), lưu hash thay vì chuỗi gốc (giảm nhiều lần, chấp nhận rủi ro va chạm rất nhỏ), hoặc Bloom filter (giảm hàng chục lần, chấp nhận bỏ sót vài URL do dương tính giả). **Cả ba đều đúng — chọn cái nào phụ thuộc bạn chấp nhận đánh đổi gì.**\n\nMột lưu ý: bài này dùng nhiều con số đo. Chúng là **thứ tự độ lớn**, thay đổi theo phiên bản Python và nền tảng — điều đáng nhớ là **tỉ lệ và xu hướng**, không phải con số tuyệt đối.",

co_che: "### Ba nguồn tiêu tốn bộ nhớ\n\n```\n① PHẢI DƯ CHỖ — hệ số tải\n\n   Open addressing cần α ≤ 2/3 (bài trước: số lần dò nổ theo 1/(1−α)²)\n   ⇒ bảng luôn có ít nhất 1/3 ô TRỐNG\n   ⇒ lãng phí ~50% so với số phần tử thật\n\n② LƯU CẢ KHOÁ VÀ HASH\n\n   Mỗi mục là (hash, khoá, giá trị) = 3 × 8 byte = 24 byte\n   · hash lưu sẵn để so sánh nhanh trước khi gọi __eq__\n   · khoá phải lưu để xác nhận đúng khoá (bài 1)\n\n③ MỖI PHẦN TỬ LÀ MỘT PyObject\n\n   Bảng chỉ lưu CON TRỎ; giá trị thật nằm rải rác trên heap\n   ⇒ mỗi int nhỏ thêm ~28 byte, mỗi chuỗi ngắn thêm ~50 byte\n   (đúng như bài mảng động, module 2)\n```\n\nCộng lại cho một `dict[int, int]` một triệu phần tử:\n\n```\nmảng chỉ số (tầng 1)   ~4–8 MB     (số nguyên nhỏ, thưa)\nmảng mục (tầng 2)      ~24 MB      (hash + khoá + giá trị)\ncác PyObject int       ~56 MB      (2 triệu int × 28 byte)\n──────────────────────────────\ntổng                   ~80 MB\n\nso với list 1 triệu int: ~36 MB\n⇒ dict tốn khoảng 2 lần list, và đó là trường hợp NHẸ.\n```\n\n⚠️ Với khoá là chuỗi thì chênh lệch lớn hơn nhiều, vì mỗi chuỗi Python có header khoảng 49 byte cộng nội dung.\n\n### Vì sao `set` không nhẹ hơn `dict`\n\nĐây là hiểu lầm phổ biến nhất về hai cấu trúc này:\n\n```\nTRỰC GIÁC:  set chỉ lưu khoá, dict lưu cả khoá lẫn giá trị\n            ⇒ set phải nhẹ hơn\n\nTHỰC TẾ:    set thường tốn TƯƠNG ĐƯƠNG hoặc HƠN dict\n```\n\nBa lý do:\n\n```\n① dict có thiết kế COMPACT hai tầng (từ 3.6)\n   tầng 1 thưa chỉ chứa SỐ NGUYÊN nhỏ\n   tầng 2 đặc chứa các mục\n   ⇒ tiết kiệm ~20–25%\n\n   set KHÔNG có thiết kế đó — nó là bảng thưa một tầng\n   ⇒ mỗi ô trống vẫn chiếm đủ chỗ cho (hash, khoá)\n\n② set giữ hệ số tải THẤP HƠN (khoảng 3/5 thay vì 2/3)\n   ⇒ dư nhiều chỗ hơn\n\n③ Chênh lệch \"không lưu giá trị\" chỉ là 8 byte/mục\n   ⇒ không bù nổi hai điểm trên\n```\n\nHệ quả thực dụng: **đừng chọn `set` vì nghĩ nó nhẹ hơn.** Chọn nó vì bạn cần ngữ nghĩa tập hợp (kiểm tra thành viên, phép giao/hợp/hiệu).\n\n### Ngưỡng hoà vốn — thấp hơn bạn nghĩ\n\n```\nTRA CỨU:  x in list  →  O(n) nhưng HẰNG SỐ RẤT NHỎ\n          x in set   →  O(1) nhưng phải TÍNH HASH\n\nChi phí tính hash ≈ vài chục nano giây\nChi phí so sánh một phần tử ≈ vài nano giây\n\n⇒ với n nhỏ, duyệt list NHANH HƠN tra set\n```\n\n```\nNGƯỠNG HOÀ VỐN thực tế: khoảng 10–30 phần tử\n\n  n < 10       list thường nhanh hơn\n  n ≈ 10–30    xấp xỉ nhau\n  n > 50       set thắng rõ và khoảng cách nới rộng nhanh\n  n > 1.000    set thắng áp đảo\n```\n\n⚠️ Điều này chỉ quan trọng khi vòng lặp **nóng** — chạy hàng triệu lần. Với code chạy một lần, chọn cái nào **đọc rõ hơn** mới đúng, không phải cái nhanh hơn vài nano giây.\n\nMột chi tiết đáng biết: chi phí tạo `set` từ đầu cũng đáng kể, nên mẫu này là lãng phí:\n\n```python\n# ❌ Tạo set MỚI mỗi vòng — O(m) mỗi lần\nfor x in ds:\n    if x in set(ds2):        # ⚠️ dựng lại set n lần\n        ...\n\n# ✓ Tạo MỘT lần bên ngoài\ntap2 = set(ds2)              # ⭐ O(m) một lần\nfor x in ds:\n    if x in tap2:            # ⭐ O(1)\n        ...\n```\n\n### Đánh đổi theo cả hai chiều\n\nNối về bài độ phức tạp không gian ở module 1:\n\n```\nCHIỀU 1 — THÊM BỘ NHỚ ĐỂ NHANH HƠN\n\n  list → set          O(n) tra → O(1) tra,  tốn thêm O(n) bộ nhớ\n  thêm chỉ mục phụ    tra theo trường khác cũng O(1)\n  bộ nhớ đệm kết quả  không tính lại,       tốn bộ nhớ lưu\n\nCHIỀU 2 — TÍNH LẠI ĐỂ ÍT BỘ NHỚ HƠN\n\n  set → generator     O(1) bộ nhớ,          nhưng duyệt lại phải tính lại\n  lưu hash thay khoá  ít byte hơn nhiều,    chấp nhận rủi ro va chạm\n  Bloom filter        ít nhất,              chấp nhận dương tính giả\n```\n\nĐiểm quan trọng: **không có chiều nào luôn đúng.** Bài này cho bạn con số để quyết định, không cho bạn một quy tắc.\n\n### Ba cách giảm bộ nhớ mà giữ O(1)\n\n**① Lưu hash thay vì khoá gốc**\n\n```\nThay vì lưu URL dài 80 ký tự, lưu 8 byte hash của nó.\n\n  set các URL:        ~130 byte/URL\n  set các hash 64-bit: ~60 byte/hash\n\n  ⇒ giảm khoảng 2 lần\n\n⚠️ ĐÁNH ĐỔI: có rủi ro va chạm.\n   Với hash 64 bit và 100 triệu phần tử, xác suất va chạm\n   vẫn rất nhỏ (theo nghịch lý ngày sinh, ngưỡng là ~5 tỉ).\n\n⚠️ VÀ: không lấy lại được khoá gốc — chỉ kiểm tra \"đã thấy chưa\".\n```\n\n**② `__slots__` cho object làm khoá**\n\n```\nMỗi instance thường có một __dict__ riêng (~100+ byte).\n__slots__ bỏ nó đi.\n\n⇒ giảm đáng kể khi có hàng triệu object.\n⚠️ Đánh đổi: không thêm được thuộc tính động.\n```\n\n**③ Cấu trúc xác suất — Bloom filter**\n\n```\nÝ TƯỞNG: không lưu phần tử, chỉ lưu MỘT MẢNG BIT.\n\n  Thêm x:  bật k bit tại các vị trí h₁(x), h₂(x), …, hₖ(x)\n  Hỏi x:   nếu MỌI k bit đều bật → \"CÓ THỂ có\"\n           nếu có bit nào tắt    → \"CHẮC CHẮN không có\"\n```\n\n```\n⭐ KHÔNG BAO GIỜ có âm tính giả\n   (nói \"không có\" thì chắc chắn không có)\n\n⚠️ CÓ dương tính giả\n   (nói \"có thể có\" nhưng thật ra không có)\n\n⚠️ KHÔNG XOÁ ĐƯỢC\n   (tắt một bit có thể phá thông tin của phần tử khác)\n\n⚠️ KHÔNG liệt kê được các phần tử\n```\n\nCông thức kích thước — đáng biết để ước lượng:\n\n```\nVới n phần tử và tỉ lệ dương tính giả mong muốn p:\n\n  số bit  m ≈ −n·ln(p) / (ln 2)²\n  số hàm băm k ≈ (m/n)·ln 2\n\nVí dụ n = 1 triệu, p = 1%:\n  m ≈ 9,6 triệu bit ≈ 1,2 MB\n  k ≈ 7 hàm băm\n\nSo với set 1 triệu chuỗi: ~100 MB\n⇒ giảm khoảng 80 lần\n```\n\n**Mẫu dùng đúng của Bloom filter** — đây là điều đáng nhớ nhất:\n\n```\nDùng nó làm BỘ LỌC ĐẦU, không phải nguồn sự thật:\n\n  ① hỏi Bloom filter\n  ② nếu nói \"không có\"     → CHẮC CHẮN không có, xong ngay\n  ③ nếu nói \"có thể có\"    → tra tiếp nguồn thật (database, đĩa)\n\n⇒ Loại bỏ phần lớn truy vấn tốn kém với chi phí bộ nhớ rất nhỏ.\n⇒ Dương tính giả chỉ gây một lần tra thừa, KHÔNG gây sai kết quả.\n```\n\nĐây là mẫu tư duy tổng quát: **chấp nhận sai số ở chỗ sai số không gây hại.**\n\n### Khi nào KHÔNG dùng hash\n\nPhản biện cần thiết:\n\n```\nKHÔNG dùng set/dict khi:\n· n rất nhỏ (< 10) và trong vòng lặp nóng    → list nhanh hơn\n· Cần THỨ TỰ SẮP XẾP                          → list đã sắp + bisect\n· Cần tìm KHOẢNG (\"mọi khoá từ a đến b\")      → cây cân bằng\n· Cần tìm phần tử GẦN NHẤT                     → cây, hoặc cấu trúc không gian\n· Bộ nhớ cực chật, chấp nhận sai số            → Bloom filter\n· Khoá không hashable                           → list, hoặc chuyển sang tuple\n\nDÙNG set/dict khi:\n· Kiểm tra thành viên nhiều lần               ⭐ trường hợp chính\n· Đếm tần suất\n· Loại trùng\n· Ánh xạ khoá → giá trị\n· Phép tập hợp (giao, hợp, hiệu)\n```\n\nDòng \"cần tìm khoảng\" đáng chú ý: **bảng băm phá vỡ thứ tự** — đó là cái giá ít người để ý. Hàm băm cố ý làm các khoá gần nhau rơi vào các ô xa nhau, nên `dict` không trả lời được câu hỏi \"những khoá nào nằm giữa a và b\". Cây cân bằng (module 4) làm được, với chi phí O(log n) thay vì O(1).",

vi_du: "### Đo bộ nhớ thật — ba cấu trúc\n\n```python\nimport tracemalloc\n\n\ndef do_bo_nho(ten: str, tao) -> int:\n    tracemalloc.start()\n    obj = tao()\n    bo_nho, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n    print(f\"  {ten:>28}: {bo_nho / 1024 / 1024:>7.1f} MB\")\n    del obj\n    return bo_nho\n\n\nn = 1_000_000\nprint(f\"KHOÁ LÀ SỐ NGUYÊN (n = {n:,}):\")\nm_list = do_bo_nho(\"list(range(n))\", lambda: list(range(n)))\nm_set = do_bo_nho(\"set(range(n))\", lambda: set(range(n)))\nm_dict = do_bo_nho(\"dict.fromkeys(range(n))\", lambda: dict.fromkeys(range(n)))\n\nprint(f\"\\n  set / list  = {m_set / m_list:.1f}×\")\nprint(f\"  dict / list = {m_dict / m_list:.1f}×\")\nprint(f\"  set / dict  = {m_set / m_dict:.2f}×   ⭐ chú ý con số này\")\n```\n\n```\nKẾT QUẢ có dạng:\n\nKHOÁ LÀ SỐ NGUYÊN (n = 1,000,000):\n              list(range(n)):    ~35.0 MB\n               set(range(n)):    ~72.0 MB\n     dict.fromkeys(range(n)):    ~68.0 MB\n\n  set / list  = 2.1×\n  dict / list = 1.9×\n  set / dict  = 1.06×   ⭐ set KHÔNG nhẹ hơn dict — nó NẶNG hơn chút\n```\n\nVới khoá là **chuỗi**, chênh lệch lớn hơn nhiều:\n\n```python\nn = 300_000\nchuoi = [f\"nguoi_dung_{i:08d}\" for i in range(n)]\n\nprint(f\"KHOÁ LÀ CHUỖI (n = {n:,}):\")\nm_list = do_bo_nho(\"list\", lambda: list(chuoi))\nm_set = do_bo_nho(\"set\", lambda: set(chuoi))\nm_dict = do_bo_nho(\"dict (chuỗi → int)\",\n                   lambda: {s: i for i, s in enumerate(chuoi)})\n\nprint(f\"\\n  set / list  = {m_set / m_list:.1f}×\")\nprint(f\"  dict / list = {m_dict / m_list:.1f}×\")\n```\n\n```\nKHOÁ LÀ CHUỖI (n = 300,000):\n                        list:     ~2.4 MB\n                         set:    ~16.8 MB\n          dict (chuỗi → int):    ~18.5 MB\n\n  set / list  = 7.0×\n  dict / list = 7.7×\n\n⚠️ CHÊNH LỆCH LỚN HƠN NHIỀU so với khoá số nguyên.\n\n⇒ VÌ SAO: list chỉ lưu CON TRỎ tới chuỗi (8 byte/phần tử),\n  còn set/dict phải lưu con trỏ + hash + dư chỗ.\n  (Các object chuỗi thật thì cả ba đều dùng chung.)\n```\n\n### Phân tích ba nguồn tiêu tốn\n\n```python\nimport sys\n\n\ndef phan_tich(n: int) -> None:\n    d = dict.fromkeys(range(n))\n    s = set(range(n))\n    l = list(range(n))\n\n    print(f\"n = {n:,}\")\n    print(f\"  list  vỏ: {sys.getsizeof(l):>10,} byte  \"\n          f\"= {sys.getsizeof(l)/n:>5.1f} byte/phần tử\")\n    print(f\"  dict  vỏ: {sys.getsizeof(d):>10,} byte  \"\n          f\"= {sys.getsizeof(d)/n:>5.1f} byte/phần tử\")\n    print(f\"  set   vỏ: {sys.getsizeof(s):>10,} byte  \"\n          f\"= {sys.getsizeof(s)/n:>5.1f} byte/phần tử\")\n    print(\"  ⚠️ getsizeof chỉ đo VỎ, không đo các PyObject bên trong\")\n\n\nfor n in (1_000, 100_000):\n    phan_tich(n)\n    print()\n```\n\n```\nn = 1,000\n  list  vỏ:      8,856 byte  =   8.9 byte/phần tử\n  dict  vỏ:     36,960 byte  =  37.0 byte/phần tử\n  set   vỏ:     32,984 byte  =  33.0 byte/phần tử\n\nn = 100,000\n  list  vỏ:    800,984 byte  =   8.0 byte/phần tử   ⭐ chỉ con trỏ\n  dict  vỏ:  4,720,720 byte  =  47.2 byte/phần tử\n  set   vỏ:  4,194,528 byte  =  41.9 byte/phần tử\n\n⭐ list: 8 byte/phần tử — đúng một con trỏ\n⭐ dict/set: ~40–47 byte — gồm hash + khoá + giá trị + CHỖ DƯ\n\n⇒ Khoảng 5× chi phí trên mỗi phần tử. Đó là giá của O(1).\n```\n\nQuan sát **chỗ dư** trực tiếp:\n\n```python\nimport sys\n\nprint(f\"{'n':>7} {'byte':>10} {'byte/n':>9} {'ghi chú':>22}\")\ntruoc = 0\nfor n in range(0, 45):\n    d = dict.fromkeys(range(n))\n    b = sys.getsizeof(d)\n    if b != truoc:\n        ghi_chu = \"⭐ cấp phát lại\" if truoc else \"\"\n        print(f\"{n:>7} {b:>10,} {b/max(1,n):>9.1f} {ghi_chu:>22}\")\n        truoc = b\n```\n\n```\n      n       byte    byte/n                ghi chú\n      0         64       inf\n      1        224     224.0         ⭐ cấp phát lại\n      6        352      58.7         ⭐ cấp phát lại\n     11        640      58.2         ⭐ cấp phát lại\n     22      1,168      53.1         ⭐ cấp phát lại\n     43      2,272      52.8         ⭐ cấp phát lại\n\n⭐ ĐỌC BẢNG:\n  · Cấp phát lại xảy ra ở n = 6, 11, 22, 43 — khoảng cách GẤP ĐÔI\n  · Ngay sau cấp phát lại, byte/n RẤT CAO (dư nhiều chỗ)\n  · byte/n giảm dần cho tới lần cấp phát tiếp theo\n\n⇒ Đây chính là hệ số tải 2/3 hiện ra bằng số:\n  dict LUÔN dư chỗ, và đó là nguồn tiêu tốn số một.\n```\n\n### Ngưỡng hoà vốn — đo thật\n\n```python\nimport time\n\n\ndef do_tra_cuu(n: int, so_lan: int = 200_000) -> tuple[float, float]:\n    \"\"\"So thời gian tra cứu trên list và set với n phần tử.\"\"\"\n    ds = list(range(n))\n    tap = set(ds)\n    can_tim = n - 1                     # ⭐ XẤU NHẤT cho list: phần tử cuối\n\n    t0 = time.perf_counter()\n    for _ in range(so_lan):\n        _ = can_tim in ds\n    t_list = time.perf_counter() - t0\n\n    t0 = time.perf_counter()\n    for _ in range(so_lan):\n        _ = can_tim in tap\n    t_set = time.perf_counter() - t0\n\n    return t_list, t_set\n\n\nprint(f\"{'n':>7} {'list':>10} {'set':>10} {'ai thắng':>12}\")\nfor n in (1, 3, 5, 10, 20, 50, 100, 1_000):\n    t_l, t_s = do_tra_cuu(n)\n    thang = \"list\" if t_l < t_s else \"set\"\n    print(f\"{n:>7} {t_l:>9.4f}s {t_s:>9.4f}s {thang:>12}\")\n```\n\n```\nXU HƯỚNG dự kiến:\n\n      n       list        set     ai thắng\n      1    ~0.008s    ~0.012s         list    ⭐ list nhanh hơn\n      3    ~0.010s    ~0.012s         list\n      5    ~0.012s    ~0.012s      xấp xỉ\n     10    ~0.016s    ~0.012s          set    ⭐ ngưỡng hoà vốn quanh đây\n     20    ~0.026s    ~0.012s          set\n     50    ~0.055s    ~0.012s          set\n    100    ~0.100s    ~0.012s          set\n  1,000    ~0.900s    ~0.012s          set    ⭐ chênh ~75 lần\n\n⭐ NGƯỠNG HOÀ VỐN: khoảng 5–15 phần tử.\n\n⚠️ Chú ý cột 'set' gần như KHÔNG ĐỔI theo n — đó là O(1).\n  Cột 'list' tăng tuyến tính — đó là O(n).\n```\n\n⚠️ Nhưng ngưỡng phụ thuộc **vị trí phần tử cần tìm**:\n\n```python\nn = 20\nds = list(range(n))\ntap = set(ds)\nso_lan = 200_000\n\nfor ten, muc_tieu in ((\"ĐẦU danh sách\", 0),\n                      (\"GIỮA\", n // 2),\n                      (\"CUỐI\", n - 1),\n                      (\"KHÔNG CÓ\", n + 100)):\n    t0 = time.perf_counter()\n    for _ in range(so_lan):\n        _ = muc_tieu in ds\n    t_l = time.perf_counter() - t0\n\n    t0 = time.perf_counter()\n    for _ in range(so_lan):\n        _ = muc_tieu in tap\n    t_s = time.perf_counter() - t0\n\n    print(f\"{ten:>16}: list {t_l:>7.4f}s   set {t_s:>7.4f}s   \"\n          f\"{'list' if t_l < t_s else 'set':>4} thắng\")\n```\n\n```\n   ĐẦU danh sách: list  ~0.008s   set  ~0.012s   list thắng   ⭐\n            GIỮA: list  ~0.016s   set  ~0.012s    set thắng\n            CUỐI: list  ~0.026s   set  ~0.012s    set thắng\n        KHÔNG CÓ: list  ~0.028s   set  ~0.012s    set thắng   ⚠️ tệ nhất cho list\n\n⇒ Với list, TRƯỜNG HỢP XẤU NHẤT là \"không có\" — phải duyệt hết.\n  Với set, mọi trường hợp như nhau.\n\n⇒ Nếu phần lớn truy vấn là \"KHÔNG CÓ\" thì set thắng sớm hơn nhiều.\n```\n\n### Bẫy: tạo `set` trong vòng lặp\n\n```python\nimport time\n\nds1 = list(range(2_000))\nds2 = list(range(1_000, 3_000))\n\n# ❌ Tạo set MỚI mỗi vòng — O(n·m)\nt0 = time.perf_counter()\nkq = [x for x in ds1 if x in set(ds2)]      # ⚠️ dựng set 2000 lần\nt_sai = time.perf_counter() - t0\n\n# ✓ Tạo MỘT lần\nt0 = time.perf_counter()\ntap2 = set(ds2)                              # ⭐ O(m) một lần\nkq2 = [x for x in ds1 if x in tap2]          # ⭐ O(n)\nt_dung = time.perf_counter() - t0\n\nassert kq == kq2\nprint(f\"tạo set mỗi vòng: {t_sai:>7.4f}s\")\nprint(f\"tạo set một lần : {t_dung:>7.4f}s   nhanh hơn \"\n      f\"{t_sai/t_dung:.0f} lần\")\n```\n\n```\ntạo set mỗi vòng:  ~0.9000s\ntạo set một lần :  ~0.0004s   nhanh hơn ~2000 lần\n\n⚠️ Đây là chi phí ẩn (bài vòng lặp lồng nhau, module 1) ở dạng khác:\n  `set(ds2)` trông như một biểu thức đơn giản, thật ra là O(m).\n```\n\n### Ba cách giảm bộ nhớ mà giữ O(1)\n\n**① Lưu hash thay vì khoá gốc**\n\n```python\nimport tracemalloc\n\nn = 500_000\nurl = [f\"https://vidu.com/trang/{i:08d}/chi-tiet\" for i in range(n)]\n\ndef do(ten, tao):\n    tracemalloc.start()\n    obj = tao()\n    b, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n    print(f\"  {ten:>26}: {b/1024/1024:>6.1f} MB\")\n    del obj\n    return b\n\nprint(f\"Lọc trùng {n:,} URL:\")\nb1 = do(\"set các URL gốc\", lambda: set(url))\nb2 = do(\"set các hash 64-bit\", lambda: {hash(u) for u in url})\nprint(f\"\\n  giảm {b1/b2:.1f} lần\")\n```\n\n```\nLọc trùng 500,000 URL:\n           set các URL gốc:   ~62.0 MB\n       set các hash 64-bit:   ~32.0 MB\n\n  giảm ~1.9 lần\n\n⚠️ ĐÁNH ĐỔI:\n  · có rủi ro VA CHẠM (hai URL khác nhau cùng hash)\n  · KHÔNG lấy lại được URL gốc\n  · hash(str) của Python KHÔNG ổn định giữa các lần chạy (bài 1)\n    ⇒ muốn lưu xuống đĩa phải dùng hashlib\n```\n\n```python\nimport hashlib\n\ndef hash_on_dinh_64(s: str) -> int:\n    \"\"\"Hash 64-bit ỔN ĐỊNH giữa các lần chạy — dùng khi cần lưu.\"\"\"\n    return int.from_bytes(hashlib.blake2b(s.encode(), digest_size=8).digest(), \"big\")\n\n\n# Ước lượng rủi ro va chạm theo nghịch lý ngày sinh (bài 2)\nimport math\n\nfor n_url in (10**6, 10**7, 10**8):\n    # xác suất va chạm ≈ n² / (2 × 2⁶⁴)\n    p = n_url ** 2 / (2 * 2**64)\n    print(f\"n = {n_url:>12,}  xác suất va chạm ≈ {p:.2e}\")\n```\n\n```\nn =    1,000,000  xác suất va chạm ≈ 2.71e-08\nn =   10,000,000  xác suất va chạm ≈ 2.71e-06\nn =  100,000,000  xác suất va chạm ≈ 2.71e-04\n\n⭐ Với 100 triệu URL, xác suất có VA CHẠM NÀO ĐÓ là ~0,027%.\n  Chấp nhận được cho trình thu thập dữ liệu (bỏ sót vài URL).\n  KHÔNG chấp nhận được cho hệ thống tài chính.\n\n⇒ Đánh đổi này phụ thuộc HẬU QUẢ của một lần sai, không phải\n  chỉ phụ thuộc con số xác suất.\n```\n\n**② `__slots__` cho object làm khoá**\n\n```python\nimport tracemalloc\nfrom dataclasses import dataclass\n\n\n@dataclass(frozen=True)\nclass DiemThuong:\n    x: int\n    y: int\n\n\n@dataclass(frozen=True)\nclass DiemSlots:\n    __slots__ = (\"x\", \"y\")               # ⭐ bỏ __dict__ mỗi instance\n    x: int\n    y: int\n\n\nn = 200_000\nfor Lop in (DiemThuong, DiemSlots):\n    tracemalloc.start()\n    s = {Lop(i, i) for i in range(n)}\n    b, _ = tracemalloc.get_traced_memory()\n    tracemalloc.stop()\n    print(f\"  set {n:,} × {Lop.__name__:>12}: {b/1024/1024:>6.1f} MB\")\n    del s\n```\n\n```\n  set 200,000 ×   DiemThuong:   ~38.0 MB\n  set 200,000 ×    DiemSlots:   ~26.0 MB\n\n⭐ Giảm khoảng 30% — đáng kể khi có hàng triệu object.\n⚠️ Đánh đổi: không thêm được thuộc tính động, và phức tạp hơn\n  khi dùng với đa kế thừa.\n```\n\n**③ Bloom filter**\n\n```python\nimport hashlib\nimport math\nfrom typing import Any\n\n\nclass BloomFilter:\n    \"\"\"Cấu trúc xác suất: kiểm tra thành viên với bộ nhớ rất nhỏ.\n\n    ⭐ KHÔNG BAO GIỜ âm tính giả — nói 'không có' là CHẮC CHẮN không có.\n    ⚠️ CÓ dương tính giả — nói 'có thể có' thì phải kiểm tra tiếp.\n    ⚠️ KHÔNG xoá được, KHÔNG liệt kê được.\n    \"\"\"\n\n    def __init__(self, so_phan_tu_du_kien: int, ti_le_duong_gia: float = 0.01):\n        if so_phan_tu_du_kien < 1:\n            raise ValueError(\"số phần tử dự kiến phải ≥ 1\")\n        if not 0 < ti_le_duong_gia < 1:\n            raise ValueError(\"tỉ lệ dương tính giả phải trong (0, 1)\")\n\n        n, p = so_phan_tu_du_kien, ti_le_duong_gia\n        # ⭐ công thức chuẩn\n        self.so_bit = max(8, math.ceil(-n * math.log(p) / (math.log(2) ** 2)))\n        self.so_ham_bam = max(1, round((self.so_bit / n) * math.log(2)))\n        self._bit = bytearray((self.so_bit + 7) // 8)\n        self._so_da_them = 0\n\n    def _cac_vi_tri(self, x: Any):\n        \"\"\"Sinh k vị trí bit từ MỘT lần băm (kỹ thuật double hashing).\"\"\"\n        d = hashlib.blake2b(repr(x).encode(), digest_size=16).digest()\n        h1 = int.from_bytes(d[:8], \"big\")\n        h2 = int.from_bytes(d[8:], \"big\") | 1     # ⭐ bảo đảm LẺ, khác 0\n        for i in range(self.so_ham_bam):\n            yield (h1 + i * h2) % self.so_bit\n\n    def them(self, x: Any) -> None:\n        for vi_tri in self._cac_vi_tri(x):\n            self._bit[vi_tri // 8] |= 1 << (vi_tri % 8)\n        self._so_da_them += 1\n\n    def __contains__(self, x: Any) -> bool:\n        \"\"\"False = CHẮC CHẮN không có. True = CÓ THỂ có.\"\"\"\n        return all(\n            self._bit[vi_tri // 8] & (1 << (vi_tri % 8))\n            for vi_tri in self._cac_vi_tri(x)\n        )\n\n    @property\n    def so_byte(self) -> int:\n        return len(self._bit)\n\n    def ti_le_duong_gia_uoc_tinh(self) -> float:\n        \"\"\"Tỉ lệ dương tính giả THỰC TẾ với số phần tử đã thêm.\"\"\"\n        n, m, k = self._so_da_them, self.so_bit, self.so_ham_bam\n        if n == 0:\n            return 0.0\n        return (1 - math.exp(-k * n / m)) ** k\n```\n\n```python\nn = 1_000_000\nbf = BloomFilter(n, ti_le_duong_gia=0.01)\n\nprint(f\"Bloom filter cho {n:,} phần tử, dương tính giả mục tiêu 1%:\")\nprint(f\"  số bit      : {bf.so_bit:>12,}\")\nprint(f\"  số hàm băm  : {bf.so_ham_bam:>12}\")\nprint(f\"  bộ nhớ      : {bf.so_byte/1024/1024:>11.2f} MB\")\n\n# So với set\nimport tracemalloc\ntracemalloc.start()\ns = {f\"phan_tu_{i}\" for i in range(200_000)}\nb_set, _ = tracemalloc.get_traced_memory()\ntracemalloc.stop()\nprint(f\"\\n  set 200.000 chuỗi: {b_set/1024/1024:.1f} MB\")\nprint(f\"  ⇒ Bloom cho 1 TRIỆU phần tử còn nhỏ hơn set cho 200.000\")\n```\n\n```\nBloom filter cho 1,000,000 phần tử, dương tính giả mục tiêu 1%:\n  số bit      :    9,585,059\n  số hàm băm  :            7\n  bộ nhớ      :        1.14 MB\n\n  set 200.000 chuỗi: ~24.0 MB\n  ⇒ Bloom cho 1 TRIỆU phần tử còn nhỏ hơn set cho 200.000\n```\n\nKiểm chứng hai tính chất:\n\n```python\nbf = BloomFilter(10_000, ti_le_duong_gia=0.01)\nda_them = [f\"co_that_{i}\" for i in range(10_000)]\nfor x in da_them:\n    bf.them(x)\n\n# ⭐ KHÔNG BAO GIỜ âm tính giả\nam_tinh_gia = sum(1 for x in da_them if x not in bf)\nprint(f\"âm tính giả: {am_tinh_gia}\")          # ⭐ PHẢI là 0\nassert am_tinh_gia == 0, \"Bloom filter KHÔNG được có âm tính giả!\"\n\n# ⚠️ CÓ dương tính giả\nkhong_co = [f\"khong_co_{i}\" for i in range(10_000)]\nduong_tinh_gia = sum(1 for x in khong_co if x in bf)\nprint(f\"dương tính giả: {duong_tinh_gia}/10.000 = \"\n      f\"{duong_tinh_gia/10_000:.2%}\")\nprint(f\"ước tính lý thuyết: {bf.ti_le_duong_gia_uoc_tinh():.2%}\")\n```\n\n```\nâm tính giả: 0                              ⭐ ĐÚNG như bảo đảm\ndương tính giả: ~103/10.000 = ~1.03%\nước tính lý thuyết: ~1.00%\n\n⭐ Số đo KHỚP công thức lý thuyết.\n```\n\n⚠️ Chuyện gì xảy ra khi vượt số phần tử dự kiến:\n\n```python\nbf = BloomFilter(1_000, ti_le_duong_gia=0.01)\nkhong_co = [f\"test_{i}\" for i in range(5_000)]\n\nprint(f\"{'đã thêm':>10} {'dương tính giả':>18}\")\nfor moc in (500, 1_000, 2_000, 5_000, 10_000):\n    while bf._so_da_them < moc:\n        bf.them(f\"phan_tu_{bf._so_da_them}\")\n    dg = sum(1 for x in khong_co if x in bf) / len(khong_co)\n    canh_bao = \"  ⚠️\" if bf._so_da_them > 1_000 else \"\"\n    print(f\"{moc:>10,} {dg:>17.1%}{canh_bao}\")\n```\n\n```\n   đã thêm     dương tính giả\n       500               0.1%\n     1,000               1.0%    ⭐ đúng mục tiêu\n     2,000               8.5%  ⚠️\n     5,000              45.0%  ⚠️\n    10,000              85.0%  ⚠️\n\n⚠️ Vượt số phần tử dự kiến ⇒ tỉ lệ dương tính giả TĂNG NHANH.\n  Ở mức 10× dự kiến, Bloom filter gần như VÔ DỤNG (nói \"có\" với mọi thứ).\n\n⇒ PHẢI ước lượng đúng n từ đầu, hoặc dùng scalable Bloom filter.\n```\n\n### Mẫu dùng đúng: Bloom filter làm bộ lọc đầu\n\n```python\nimport time\n\n\nclass KhoDuLieuGia:\n    \"\"\"Giả lập database: tra cứu CHẬM.\"\"\"\n\n    def __init__(self, cac_khoa: set[str]) -> None:\n        self._du_lieu = cac_khoa\n        self.so_lan_truy_van = 0\n\n    def co(self, khoa: str) -> bool:\n        self.so_lan_truy_van += 1\n        time.sleep(0.00001)              # ⭐ giả lập độ trễ I/O\n        return khoa in self._du_lieu\n\n\nco_that = {f\"khoa_{i}\" for i in range(10_000)}\ncan_kiem = [f\"khoa_{i}\" for i in range(0, 10_000, 100)] + \\\n           [f\"khong_co_{i}\" for i in range(2_000)]\n\n# ── ❌ Không có Bloom: truy vấn database MỌI lần ──\ndb1 = KhoDuLieuGia(co_that)\nt0 = time.perf_counter()\nkq1 = [k for k in can_kiem if db1.co(k)]\nt1 = time.perf_counter() - t0\n\n# ── ✓ Có Bloom làm bộ lọc đầu ──\nbf = BloomFilter(len(co_that), ti_le_duong_gia=0.01)\nfor k in co_that:\n    bf.them(k)\n\ndb2 = KhoDuLieuGia(co_that)\nt0 = time.perf_counter()\nkq2 = []\nfor k in can_kiem:\n    if k not in bf:\n        continue                         # ⭐ CHẮC CHẮN không có — bỏ qua ngay\n    if db2.co(k):                        # ⭐ chỉ tra DB khi Bloom nói \"có thể\"\n        kq2.append(k)\nt2 = time.perf_counter() - t0\n\nassert sorted(kq1) == sorted(kq2), \"kết quả PHẢI giống nhau!\"\nprint(f\"không Bloom: {t1:>6.3f}s  {db1.so_lan_truy_van:>6,} truy vấn DB\")\nprint(f\"có Bloom   : {t2:>6.3f}s  {db2.so_lan_truy_van:>6,} truy vấn DB\")\nprint(f\"⇒ giảm {(1 - db2.so_lan_truy_van/db1.so_lan_truy_van):.1%} truy vấn, \"\n      f\"bộ nhớ thêm {bf.so_byte/1024:.0f} KB\")\n```\n\n```\nkhông Bloom:  ~0.025s   2,100 truy vấn DB\ncó Bloom   :  ~0.002s     120 truy vấn DB\n⇒ giảm 94.3% truy vấn, bộ nhớ thêm 12 KB\n\n⭐ KẾT QUẢ VẪN HOÀN TOÀN CHÍNH XÁC.\n  Dương tính giả chỉ gây một lần tra DB THỪA, không gây sai kết quả.\n\n⇒ ĐÂY LÀ MẪU DÙNG ĐÚNG: Bloom filter là BỘ LỌC ĐẦU,\n  không phải nguồn sự thật.\n```\n\nỨng dụng thật của mẫu này:\n\n```\n· Database (Cassandra, LevelDB, RocksDB) — tránh đọc đĩa khi khoá không tồn tại\n· Trình duyệt — kiểm tra URL có trong danh sách độc hại không\n· CDN — kiểm tra tài nguyên có trong cache không\n· Trình thu thập dữ liệu — lọc URL đã thăm\n```\n\n### Khi hash phá vỡ thứ tự\n\nCái giá ít người để ý của bảng băm:\n\n```python\nimport bisect\nimport time\n\nn = 200_000\nds_da_sap = list(range(0, n * 2, 2))         # số chẵn 0, 2, 4, ...\ntap = set(ds_da_sap)\n\n# ① Kiểm tra thành viên — set thắng áp đảo\nt0 = time.perf_counter()\nfor i in range(0, n * 2, 100):\n    _ = i in tap\nt_set = time.perf_counter() - t0\n\nt0 = time.perf_counter()\nfor i in range(0, n * 2, 100):\n    vi_tri = bisect.bisect_left(ds_da_sap, i)\n    _ = vi_tri < len(ds_da_sap) and ds_da_sap[vi_tri] == i\nt_bisect = time.perf_counter() - t0\n\nprint(f\"kiểm tra thành viên: set {t_set:.4f}s   bisect {t_bisect:.4f}s\")\n\n# ② Tìm KHOẢNG — set KHÔNG làm được hiệu quả\nt0 = time.perf_counter()\ntrong_khoang = [x for x in tap if 1000 <= x <= 2000]     # ⚠️ O(n)\nt_set_khoang = time.perf_counter() - t0\n\nt0 = time.perf_counter()\ntrai = bisect.bisect_left(ds_da_sap, 1000)\nphai = bisect.bisect_right(ds_da_sap, 2000)\ntrong_khoang2 = ds_da_sap[trai:phai]                      # ⭐ O(log n + k)\nt_bisect_khoang = time.perf_counter() - t0\n\nassert sorted(trong_khoang) == trong_khoang2\nprint(f\"tìm khoảng [1000,2000]: set {t_set_khoang:.4f}s   \"\n      f\"bisect {t_bisect_khoang:.6f}s\")\nprint(f\"⇒ bisect nhanh hơn {t_set_khoang/t_bisect_khoang:.0f} lần\")\n```\n\n```\nkiểm tra thành viên: set 0.0012s   bisect 0.0080s   ⭐ set thắng ~7 lần\ntìm khoảng [1000,2000]: set 0.0090s   bisect 0.000015s\n⇒ bisect nhanh hơn ~600 lần\n\n⭐ BẢNG BĂM PHÁ VỠ THỨ TỰ — đó là cái giá ít người để ý.\n\n  Hàm băm CỐ Ý làm các khoá gần nhau rơi vào ô xa nhau\n  ⇒ \"những khoá nào nằm giữa a và b\" phải duyệt HẾT ⇒ O(n)\n\n⇒ Cần truy vấn khoảng ⇒ dùng list đã sắp + bisect, hoặc\n  cây cân bằng (module 4). Chấp nhận O(log n) thay vì O(1).\n```\n\n### Bảng quyết định — chọn cấu trúc nào\n\n```python\ndef goi_y(n: int, so_lan_tra: int, can_thu_tu: bool,\n          bo_nho_chat: bool, chap_nhan_sai_so: bool) -> str:\n    if can_thu_tu:\n        return \"list đã sắp + bisect, hoặc cây cân bằng (module 4)\"\n    if bo_nho_chat and chap_nhan_sai_so:\n        return \"Bloom filter (bộ lọc đầu) + nguồn thật\"\n    if bo_nho_chat:\n        return \"set các hash 64-bit, hoặc chia lô xử lý\"\n    if n < 15 and so_lan_tra > 100_000:\n        return \"list — dưới ngưỡng hoà vốn, tránh chi phí tính hash\"\n    if so_lan_tra < 5:\n        return \"list — tra ít, không đáng dựng set\"\n    return \"set / dict — trường hợp mặc định\"\n\n\nTINH_HUONG = [\n    (\"lọc trùng 80 triệu URL, RAM 8 GB\", 80_000_000, 80_000_000, False, True, True),\n    (\"tra 5 mã trạng thái, vòng lặp nóng\", 5, 1_000_000, False, False, False),\n    (\"tìm đơn hàng theo khoảng ngày\", 100_000, 1_000, True, False, False),\n    (\"đếm tần suất từ trong văn bản\", 50_000, 500_000, False, False, False),\n    (\"kiểm tra 3 phần tử, chạy một lần\", 3, 1, False, False, False),\n]\n\nfor ten, n, tra, thu_tu, chat, sai_so in TINH_HUONG:\n    print(f\"{ten:>38}\")\n    print(f\"{'→ ' + goi_y(n, tra, thu_tu, chat, sai_so):>38}\\n\")\n```\n\n```\n     lọc trùng 80 triệu URL, RAM 8 GB\n  → Bloom filter (bộ lọc đầu) + nguồn thật\n\n   tra 5 mã trạng thái, vòng lặp nóng\n  → list — dưới ngưỡng hoà vốn, tránh chi phí tính hash\n\n       tìm đơn hàng theo khoảng ngày\n  → list đã sắp + bisect, hoặc cây cân bằng (module 4)\n\n       đếm tần suất từ trong văn bản\n  → set / dict — trường hợp mặc định\n\n     kiểm tra 3 phần tử, chạy một lần\n  → list — tra ít, không đáng dựng set\n```\n\n### Ước lượng bộ nhớ trước khi viết code\n\nÁp dụng quy trình từ bài ước lượng (module 1):\n\n```python\ndef uoc_luong_set(n: int, loai_khoa: str) -> str:\n    \"\"\"Ước lượng THÔ bộ nhớ của một set n phần tử.\"\"\"\n    byte_moi_muc = {\n        \"int nhỏ\": 32 + 28,          # ô bảng + PyObject int\n        \"chuỗi ngắn (20 ký tự)\": 32 + 70,\n        \"chuỗi dài (100 ký tự)\": 32 + 150,\n        \"tuple 2 số\": 32 + 60 + 56,\n    }[loai_khoa]\n    tong = n * byte_moi_muc\n    for don_vi, chia in ((\"GB\", 1024**3), (\"MB\", 1024**2), (\"KB\", 1024)):\n        if tong >= chia:\n            return f\"{tong/chia:.1f} {don_vi}\"\n    return f\"{tong} byte\"\n\n\nprint(f\"{'n':>13} {'int':>10} {'chuỗi 20':>12} {'chuỗi 100':>12}\")\nfor n in (10**5, 10**6, 10**7, 10**8):\n    print(f\"{n:>13,} {uoc_luong_set(n, 'int nhỏ'):>10} \"\n          f\"{uoc_luong_set(n, 'chuỗi ngắn (20 ký tự)'):>12} \"\n          f\"{uoc_luong_set(n, 'chuỗi dài (100 ký tự)'):>12}\")\n```\n\n```\n            n        int     chuỗi 20    chuỗi 100\n      100,000     5.7 MB       9.7 MB      17.4 MB\n    1,000,000    57.2 MB      97.3 MB     173.7 MB\n   10,000,000   572.2 MB     972.7 MB       1.7 GB   ⚠️\n  100,000,000     5.6 GB       9.5 GB      17.0 GB   ⚠️⚠️\n\n⭐ CÁCH DÙNG: biết n và loại khoá ⇒ biết NGAY có chứa nổi không,\n  TRƯỚC KHI viết dòng code nào.\n\nNgân sách thường gặp:\n  container nhỏ  512 MB – 2 GB\n  máy dev        8 – 32 GB\n\n⇒ 10 triệu chuỗi 100 ký tự (1,7 GB) đã vượt container 1 GB.\n```",

so_sanh: "### Ba nguồn tiêu tốn bộ nhớ của `dict`/`set`\n\n| Nguồn | Nguyên nhân | Mức độ |\n|---|---|---|\n| **Phải dư chỗ** | hệ số tải ≤ 2/3 (bài 2) | **lãng phí ~50%** |\n| **Lưu cả khoá + hash** | cần `__eq__` xác nhận, hash để so nhanh | 24 byte/mục |\n| **Mỗi phần tử là `PyObject`** | bảng chỉ lưu con trỏ | +28 byte/int, +50 byte/chuỗi |\n\n### Bộ nhớ thực đo (thứ tự độ lớn)\n\n| Cấu trúc | 1 triệu int | 300k chuỗi | byte/phần tử |\n|---|---|---|---|\n| `list` | ~35 MB | ~2,4 MB | **~8 (chỉ con trỏ)** |\n| `dict` | ~68 MB | ~18,5 MB | ~40–47 |\n| `set` | ~72 MB | ~16,8 MB | ~42 |\n\n| Tỉ lệ | Khoá int | Khoá chuỗi |\n|---|---|---|\n| `set` / `list` | ~2,1× | **~7,0×** |\n| **`set` / `dict`** | **~1,06×** ⚠️ | ~0,9× |\n\n### ⚠️ `set` KHÔNG nhẹ hơn `dict`\n\n| Lý do | Chi tiết |\n|---|---|\n| `dict` có **compact hai tầng** | tiết kiệm ~20–25%; `set` không có |\n| `set` giữ hệ số tải **thấp hơn** | ~3/5 thay vì 2/3 ⇒ dư nhiều chỗ hơn |\n| Chênh lệch \"không lưu giá trị\" | chỉ 8 byte/mục — không bù nổi |\n\n**Chọn `set` vì ngữ nghĩa tập hợp, không vì nghĩ nó nhẹ hơn.**\n\n### Ngưỡng hoà vốn `list` và `set`\n\n| n | Ai thắng | Ghi chú |\n|---|---|---|\n| < 10 | **`list`** | chi phí tính hash lấn át |\n| 10–30 | xấp xỉ | **⭐ ngưỡng hoà vốn** |\n| > 50 | `set` | khoảng cách nới rộng nhanh |\n| > 1.000 | `set` áp đảo | chênh ~75 lần |\n\n⚠️ Ngưỡng phụ thuộc **vị trí phần tử**: nếu phần lớn truy vấn là \"không có\" thì `set` thắng sớm hơn (vì `list` phải duyệt hết).\n\n### Đánh đổi hai chiều\n\n| Chiều | Kỹ thuật | Đổi gì |\n|---|---|---|\n| **Thêm bộ nhớ → nhanh hơn** | `list` → `set` | O(n) bộ nhớ ⇒ O(n) → O(1) tra |\n| | chỉ mục phụ | tra theo trường khác cũng O(1) |\n| **Ít bộ nhớ hơn** | lưu hash thay khoá | ~2× ít hơn, **rủi ro va chạm** |\n| | `__slots__` | ~30% ít hơn, mất thuộc tính động |\n| | **Bloom filter** | **~80× ít hơn**, có **dương tính giả** |\n\n### Bloom filter — bảo đảm và giới hạn\n\n| | Bảo đảm |\n|---|---|\n| Nói **\"không có\"** | **⭐ CHẮC CHẮN không có** (không âm tính giả) |\n| Nói **\"có thể có\"** | ⚠️ phải kiểm tra tiếp (có dương tính giả) |\n| Xoá phần tử | **✗ không được** |\n| Liệt kê phần tử | **✗ không được** |\n| Vượt số dự kiến | ⚠️ dương tính giả **tăng rất nhanh** |\n\n| Đã thêm / dự kiến | Dương tính giả |\n|---|---|\n| 0,5× | 0,1% |\n| **1,0×** | **1,0%** ⭐ đúng mục tiêu |\n| 2× | ~8,5% ⚠️ |\n| 10× | ~85% ⚠️⚠️ gần như vô dụng |\n\n### Mẫu dùng đúng: bộ lọc đầu\n\n| Bước | Hành động |\n|---|---|\n| ① | hỏi Bloom filter |\n| ② nói \"không có\" | **xong ngay** — chắc chắn không có |\n| ③ nói \"có thể có\" | tra tiếp **nguồn thật** |\n\n⭐ Kết quả **vẫn chính xác 100%**; dương tính giả chỉ gây một lần tra thừa.\n\nDùng ở: Cassandra, LevelDB, RocksDB, trình duyệt (URL độc hại), CDN, trình thu thập dữ liệu.\n\n### Cái giá ít người để ý: mất thứ tự\n\n| Thao tác | `set` | `list` đã sắp + `bisect` |\n|---|---|---|\n| Kiểm tra thành viên | **⭐ O(1)** | O(log n) |\n| **Tìm khoảng [a, b]** | **⚠️ O(n)** | **⭐ O(log n + k)** |\n| Lấy min/max | O(n) | **O(1)** |\n| Duyệt theo thứ tự | O(n log n) | **O(n)** |\n\nHàm băm **cố ý** làm khoá gần nhau rơi vào ô xa nhau ⇒ mất hoàn toàn thông tin thứ tự.\n\n### Khi nào KHÔNG dùng hash\n\n| Nhu cầu | Dùng gì |\n|---|---|\n| n < 10, vòng lặp nóng | **`list`** |\n| Tra cứu rất ít lần | `list` (không đáng dựng `set`) |\n| Cần **thứ tự sắp xếp** | `list` đã sắp + `bisect` |\n| Cần **tìm khoảng** | cây cân bằng (module 4) |\n| Bộ nhớ chật, chấp nhận sai số | **Bloom filter** |\n| Khoá không hashable | `list`, hoặc chuyển sang `tuple` |\n\n### Ước lượng bộ nhớ `set` (thô)\n\n| n | int | chuỗi 20 ký tự | chuỗi 100 ký tự |\n|---|---|---|---|\n| 10⁵ | 5,7 MB | 9,7 MB | 17,4 MB |\n| 10⁶ | 57 MB | 97 MB | 174 MB |\n| **10⁷** | 572 MB | 973 MB | **1,7 GB** ⚠️ |\n| **10⁸** | **5,6 GB** ⚠️ | **9,5 GB** ⚠️ | **17 GB** ⚠️ |\n\nDùng bảng này **trước khi viết code** — đúng quy trình ước lượng ở module 1.",

loi_thuong_gap: [
  "**Nghĩ `set` nhẹ hơn `dict`.** Đo ra thường ngược lại — `dict` có compact hai tầng còn `set` thì không.",
  "**Dùng `set` cho n rất nhỏ trong vòng lặp nóng.** Dưới khoảng 10 phần tử, duyệt `list` nhanh hơn vì không phải tính hash.",
  "**Tạo `set` bên trong vòng lặp.** `set(ds2)` là O(m) mỗi lần — chi phí ẩn làm code thành O(n·m).",
  "**Không ước lượng bộ nhớ trước khi dùng `set` cho dữ liệu lớn.** 10 triệu chuỗi dài đã vượt container 1 GB.",
  "**Lưu `hash(str)` xuống đĩa để tiết kiệm chỗ.** Python ngẫu nhiên hoá hash chuỗi — lần chạy sau ra khác; dùng `hashlib`.",
  "**Dùng Bloom filter làm nguồn sự thật.** Nó có dương tính giả — phải dùng làm **bộ lọc đầu**, rồi tra tiếp nguồn thật.",
  "**Ước lượng sai số phần tử của Bloom filter.** Vượt 10× dự kiến thì dương tính giả lên ~85%, gần như vô dụng.",
  "**Cố xoá phần tử khỏi Bloom filter.** Tắt một bit phá thông tin của phần tử khác — không xoá được.",
  "**Dùng `set` khi cần truy vấn khoảng.** Bảng băm phá vỡ thứ tự ⇒ tìm khoảng là O(n); dùng `bisect` hoặc cây.",
  "**Dùng `set` khi cần min/max thường xuyên.** O(n) mỗi lần; dùng heap hoặc cấu trúc có thứ tự.",
  "**Tin `sys.getsizeof` là bộ nhớ thật.** Nó chỉ đo vỏ, không đo các `PyObject` bên trong — dùng `tracemalloc`.",
  "**Tối ưu bộ nhớ trước khi đo.** `__slots__` và lưu hash chỉ đáng làm khi đã xác định bộ nhớ là ràng buộc thật.",
  "**Chọn cấu trúc theo tốc độ khi code chạy một lần.** Lúc đó chọn cái **đọc rõ hơn** mới đúng.",
  "**Quên rằng đánh đổi phụ thuộc HẬU QUẢ của một lần sai.** Va chạm hash 0,027% chấp nhận được cho crawler, không cho hệ thống tài chính."
],

tu_kiem_tra: [
  "Nêu ba nguồn tiêu tốn bộ nhớ của `dict`. Nguồn nào lớn nhất?",
  "Vì sao `dict` phải luôn dư khoảng 1/3 chỗ? Liên hệ công thức số lần dò ở bài trước.",
  "Vì sao `set` không nhẹ hơn `dict`? Nêu ba lý do.",
  "Ngưỡng hoà vốn giữa `list` và `set` khoảng bao nhiêu? Điều gì làm ngưỡng đó thay đổi?",
  "Vì sao với n nhỏ thì `list` nhanh hơn `set` dù O(n) so với O(1)?",
  "Vì sao tạo `set` trong vòng lặp là lỗi? Nó thuộc loại lỗi nào đã học ở module 1?",
  "Nêu đánh đổi bộ nhớ–thời gian theo cả hai chiều, kèm ví dụ mỗi chiều.",
  "Nêu ba cách giảm bộ nhớ mà vẫn giữ O(1), kèm đánh đổi của mỗi cách.",
  "Lưu hash thay vì khoá gốc tiết kiệm bao nhiêu? Ba đánh đổi là gì?",
  "Bloom filter bảo đảm gì và không bảo đảm gì? Vì sao \"không có âm tính giả\" là tính chất quan trọng nhất?",
  "Viết công thức tính số bit và số hàm băm của Bloom filter.",
  "Chuyện gì xảy ra khi thêm nhiều hơn số phần tử dự kiến vào Bloom filter?",
  "Mô tả mẫu \"bộ lọc đầu\". Vì sao kết quả vẫn chính xác 100% dù có dương tính giả?",
  "Nêu bốn hệ thống thật dùng Bloom filter và dùng để làm gì.",
  "Vì sao bảng băm phá vỡ thứ tự? Hệ quả với truy vấn khoảng?",
  "So sánh `set` và `list` đã sắp + `bisect` trên bốn thao tác.",
  "Nêu sáu tình huống KHÔNG nên dùng `set`/`dict`.",
  "Ước lượng bộ nhớ của `set` 10 triệu chuỗi 20 ký tự. Có vừa container 1 GB không?",
  "Vì sao đánh đổi \"lưu hash thay khoá\" phụ thuộc hậu quả chứ không chỉ phụ thuộc xác suất?"
],

tai_nguyen: [
  "**Tài liệu Python — `tracemalloc`** — cách đo bộ nhớ thật; công cụ đúng thay cho `sys.getsizeof` khi có object lồng nhau.",
  "**Mã nguồn CPython — `Objects/setobject.c`** — so sánh với `dictobject.c` để thấy vì sao `set` không có thiết kế compact hai tầng.",
  "Bài báo **Space/Time Trade-offs in Hash Coding with Allowable Errors** — Burton Bloom (1970), nguồn gốc của Bloom filter; ngắn và đọc được.",
  "Sách **Introduction to Algorithms** — CLRS. Chương 11 có phần về đánh đổi bộ nhớ của bảng băm và phân tích hệ số tải.",
  "**Tài liệu của Cassandra hoặc RocksDB** — mục về Bloom filter, cho thấy mẫu \"bộ lọc đầu\" trong hệ thống thật."
],

lien_he: [
  "**Hàm băm và bảng băm** ở module này: hệ số tải và cấu trúc compact hai tầng — nguồn gốc của chi phí bộ nhớ.",
  "**Xử lý va chạm** ngay trước: công thức số lần dò nổ theo 1/(1−α)² là lý do phải giữ hệ số tải ≤ 2/3.",
  "**Vì sao O(1) chỉ là trung bình** ngay sau: điều gì phá vỡ lời hứa O(1) mà bài này vừa định giá.",
  "**Array và dynamic array** ở module 2 mảng này: mảng con trỏ và `PyObject` header — nguồn tiêu tốn thứ ba.",
  "**Độ phức tạp không gian** ở module 1 mảng này: đánh đổi bộ nhớ ↔ thời gian theo hai chiều.",
  "**Ước lượng 10⁸ phép tính/giây** ở module 1 mảng này: quy trình ước lượng bộ nhớ trước khi viết code.",
  "**Vòng lặp lồng nhau** ở module 1 mảng này: tạo `set` trong vòng lặp là chi phí ẩn ở dạng khác.",
  "**Dataclass và `__slots__`** ở module 6 mảng 01: cách giảm bộ nhớ mỗi instance.",
  "**Cây** ở module 4 mảng này: cây cân bằng cho O(log n) nhưng **giữ thứ tự** — thứ mà bảng băm không có.",
  "**Tìm kiếm** ở module 7 mảng này: `bisect` trên list đã sắp, lựa chọn thay thế khi cần truy vấn khoảng.",
  "**Hiệu năng** ở mảng 04: chỉ mục database dùng Bloom filter để tránh đọc đĩa vô ích.",
  "**Các khối xây dựng** ở mảng 06: cấu trúc xác suất (Bloom filter, HyperLogLog, Count-Min Sketch) trong hệ thống lớn.",
  "**Hiệu năng và vận hành** ở mảng 05: ngân sách bộ nhớ container và vì sao ước lượng trước là bắt buộc."
]

};
