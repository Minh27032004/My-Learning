window.LESSONS["01:lap-trinh-huong-doi-tuong:da-hinh-polymorphism-va-duck-typing"] = {

tom_tat: [
  "**Đa hình là: cùng một lời gọi, hành vi khác nhau tuỳ object.** Giá trị của nó là code gọi **không cần biết** mình đang làm việc với kiểu nào.",
  "**Python dùng duck typing**: cái quyết định là object **có phương thức đó không**, không phải nó thuộc class nào. Không cần kế thừa, không cần khai báo interface.",
  "**Mỗi `if isinstance(...)` để xử lý khác nhau là một cơ hội dùng đa hình.** Chuỗi `if/elif` theo kiểu là dấu hiệu thiết kế chưa đúng.",
  "**Cái giá của duck typing là lỗi phát hiện muộn**: sai kiểu chỉ lộ ra khi chạy đúng nhánh đó. `Protocol` và `mypy` lấy lại được phần lớn an toàn mà không mất tính linh hoạt.",
  "**Đa hình không cần kế thừa.** Trong Python, hai class không liên quan gì vẫn thay thế nhau được nếu có cùng giao diện — đây là khác biệt lớn với Java."
],

can_biet_truoc: [
  "Bài **Kế thừa và MRO** ngay trước — liên kết động (gọi `self.f()` ra phương thức của lớp con) là cơ chế nền của đa hình.",
  "Bài **Class, object, thuộc tính** — cách Python tra cứu thuộc tính lúc chạy.",
  "Bài **Type hint và mypy** ở module 5 — `Protocol` đã giới thiệu ở đó; bài này nói nó giải quyết vấn đề gì.",
  "Bài **Xử lý ngoại lệ** ở module 5 — EAFP (\"cứ làm rồi bắt lỗi\") là triết lý nền của duck typing."
],

dinh_nghia: "Nói đời thường: bạn cắm phích điện vào ổ mà **không cần biết bên kia là quạt, đèn hay máy tính**. Cái duy nhất quan trọng là nó có phích đúng chuẩn. Đa hình là như vậy: bạn gọi `x.chay()` mà không quan tâm `x` là gì, miễn nó biết `chay()`.\n\nNói chính xác:\n\n- **Đa hình** (*polymorphism*) — cùng một lời gọi cho hành vi khác nhau tuỳ kiểu thật của object.\n- **Duck typing** — *\"nếu nó kêu như vịt và đi như vịt thì coi nó là vịt\"*. Cái quyết định là **object có phương thức cần dùng hay không**, không phải nó thuộc class nào.\n- **Liên kết động** (*dynamic dispatch*) — việc quyết định gọi phương thức nào xảy ra **lúc chạy**, dựa trên kiểu thật của object.\n- **Giao diện** (*interface*) — tập phương thức mà một object phải có để dùng được trong một ngữ cảnh. Trong Python thường là **ngầm định**.\n- **`Protocol`** — cách viết giao diện ngầm định đó ra thành kiểu, để công cụ kiểm tra được.\n- **Nạp chồng toán tử** (*operator overloading*) — cho `+`, `<`, `len()` hoạt động với class của bạn qua các phương thức đặc biệt.\n\nĐiểm cần phân biệt ngay: **đa hình trong Python không đòi hỏi kế thừa.** Trong Java, muốn hai class thay thế nhau thì chúng phải cùng kế thừa một class hoặc cùng cài đặt một interface. Trong Python, chỉ cần chúng **có cùng phương thức** — không cần quan hệ gì với nhau.",

vi_sao_can: "Đa hình là thứ biến một đống `if/elif` thành code mở rộng được:\n\n- **Nó loại bỏ chuỗi kiểm tra kiểu.** Không có nó, thêm một loại mới nghĩa là đi sửa mọi chỗ có `if isinstance(...)` — và bỏ sót một chỗ là lỗi.\n- **Nó cho phép mở rộng mà không sửa code cũ.** Thêm một class mới có đúng phương thức là mọi hàm hiện có tự dùng được — đây là nền của nguyên tắc Open/Closed trong SOLID.\n- **Duck typing làm Python rất linh hoạt.** Bạn thay một object thật bằng object giả trong kiểm thử mà không cần class giả kế thừa gì.\n- **Bạn dùng nó liên tục mà chưa gọi tên.** `len(x)` chạy được với `str`, `list`, `dict` và class của bạn; `for x in y` chạy với mọi thứ có `__iter__`. Toàn bộ thư viện chuẩn dựng trên các giao thức ngầm định.\n- **Định hướng Backend và AI/ML.** Đổi giữa các nhà cung cấp lưu trữ, đổi giữa các thuật toán, thay database thật bằng bản trong bộ nhớ khi test — đều là đa hình.\n\nTình huống thật rất hay gặp: bạn viết hàm xuất báo cáo có `if dinh_dang == \"csv\": ... elif dinh_dang == \"json\": ...`. Sáu tháng sau cần thêm XML và Excel, và bạn phát hiện chuỗi `if/elif` đó xuất hiện ở **bốn hàm khác nhau** trong dự án. Thêm một định dạng nghĩa là sửa bốn chỗ, và ai đó sẽ quên một chỗ. Phần cơ chế sẽ chỉ cách thiết kế để thêm định dạng mới **không phải sửa dòng nào của code cũ**.",

co_che: "### Đa hình qua kế thừa\n\n```python\nclass Hinh:\n    def dien_tich(self) -> float:\n        raise NotImplementedError\n\nclass HinhTron(Hinh):\n    def __init__(self, r): self.r = r\n    def dien_tich(self): return 3.14159 * self.r ** 2\n\nclass HinhVuong(Hinh):\n    def __init__(self, a): self.a = a\n    def dien_tich(self): return self.a ** 2\n\n# ⭐ Hàm này KHÔNG biết có bao nhiêu loại hình\ndef tong_dien_tich(ds: list[Hinh]) -> float:\n    return sum(h.dien_tich() for h in ds)\n\ntong_dien_tich([HinhTron(1), HinhVuong(2)])\n```\n\nThêm `HinhTamGiac` thì `tong_dien_tich` **không phải sửa một dòng nào**. Đây là toàn bộ giá trị của đa hình.\n\nCơ chế bên dưới là liên kết động đã gặp ở bài kế thừa: `h.dien_tich()` được phân giải **lúc chạy** dựa trên kiểu thật của `h`, không phải kiểu khai báo.\n\n### Duck typing — đa hình không cần kế thừa\n\n```python\nclass Vit:\n    def keu(self): return \"quạc quạc\"\n\nclass Nguoi:\n    def keu(self): return \"tôi giả tiếng vịt\"\n\nclass Robot:                       # ⭐ không liên quan gì tới hai class trên\n    def keu(self): return \"bíp bíp\"\n\ndef cho_keu(x):\n    print(x.keu())                 # ⭐ không kiểm tra kiểu gì cả\n\nfor o in (Vit(), Nguoi(), Robot()):\n    cho_keu(o)\n```\n\nBa class **không có class cha chung**, không khai báo interface nào, mà vẫn thay thế nhau được. Điều kiện duy nhất: **có phương thức `keu`**.\n\nSo với Java:\n\n```\nJava:   phải khai báo `implements CoTheKeu` thì mới dùng chung được\nPython: chỉ cần CÓ phương thức đó\n```\n\nHệ quả thực dụng rất lớn: bạn dùng được **class của thư viện khác** trong hàm của mình mà không cần họ biết tới bạn, và không cần sửa mã nguồn của họ.\n\n### Duck typing trong chính Python\n\nToàn bộ thư viện chuẩn dựng trên các giao thức ngầm định:\n\n```\nCần __len__          → len(x) chạy được\nCần __iter__         → for x in y, list(y), sum(y) chạy được\nCần __getitem__      → x[0] chạy được\nCần __contains__     → \"a\" in x chạy được\nCần __call__         → x() chạy được\nCần __enter__/__exit__ → with x: chạy được\nCần __eq__, __lt__   → sorted(), max(), min() chạy được\n```\n\n```python\nclass BoSuuTap:\n    def __init__(self, muc): self._muc = list(muc)\n    def __len__(self): return len(self._muc)\n    def __iter__(self): return iter(self._muc)\n    def __contains__(self, x): return x in self._muc\n    def __getitem__(self, i): return self._muc[i]\n\nb = BoSuuTap([1, 2, 3])\nlen(b)             # 3\nlist(b)            # [1, 2, 3]\n2 in b             # True\nb[0]               # 1\nsum(b)             # 6\nmax(b)             # 3\nfor x in b: ...    # chạy được\n```\n\nClass này **không kế thừa gì cả** mà hoạt động như một collection dựng sẵn. Đây là duck typing ở tầng ngôn ngữ: Python không hỏi \"bạn là gì\", nó hỏi \"bạn có `__len__` không\".\n\n### Thay `if isinstance` bằng đa hình\n\nĐây là ứng dụng thực dụng quan trọng nhất của bài:\n\n```python\n# ❌ Chuỗi kiểm tra kiểu — thêm loại mới là sửa ở đây\ndef xuat(du_lieu, dinh_dang):\n    if dinh_dang == \"csv\":\n        return \",\".join(str(x) for x in du_lieu)\n    elif dinh_dang == \"json\":\n        import json; return json.dumps(du_lieu)\n    elif dinh_dang == \"txt\":\n        return \"\\n\".join(str(x) for x in du_lieu)\n    else:\n        raise ValueError(f\"không hỗ trợ: {dinh_dang}\")\n```\n\nBa vấn đề của đoạn trên:\n\n```\n① Thêm định dạng ⇒ SỬA hàm này (và mọi hàm khác có chuỗi if tương tự)\n② Hàm phình dần theo số định dạng\n③ Không thể thêm định dạng từ bên ngoài — phải sửa mã nguồn\n```\n\n```python\n# ✓ Đa hình — mỗi định dạng một class\nclass BoXuat:\n    duoi = \"\"\n    def xuat(self, du_lieu) -> str:\n        raise NotImplementedError\n\nclass XuatCSV(BoXuat):\n    duoi = \".csv\"\n    def xuat(self, du_lieu):\n        return \",\".join(str(x) for x in du_lieu)\n\nclass XuatJSON(BoXuat):\n    duoi = \".json\"\n    def xuat(self, du_lieu):\n        import json; return json.dumps(du_lieu, ensure_ascii=False)\n\ndef ghi_bao_cao(du_lieu, bo_xuat: BoXuat, ten: str) -> None:\n    from pathlib import Path\n    Path(ten + bo_xuat.duoi).write_text(bo_xuat.xuat(du_lieu), encoding=\"utf-8\")\n\nghi_bao_cao([1, 2, 3], XuatCSV(), \"bao_cao\")\n```\n\nGiờ thêm `XuatXML` chỉ là **viết một class mới** — không sửa `ghi_bao_cao`, không sửa các bộ xuất khác. Đây chính là nguyên tắc Open/Closed mà bài SOLID sẽ nói.\n\nMột bước nữa — bảng tra thay cho `if`:\n\n```python\nBANG = {\"csv\": XuatCSV, \"json\": XuatJSON}\n\ndef tao_bo_xuat(ten: str) -> BoXuat:\n    if ten not in BANG:\n        raise ValueError(f\"không hỗ trợ {ten!r}, có: {sorted(BANG)}\")\n    return BANG[ten]()\n\n# Đăng ký từ BÊN NGOÀI — không sửa mã nguồn gốc\nBANG[\"xml\"] = XuatXML\n```\n\nMẫu `dict` ánh xạ tên → class là cách thay `if/elif` phổ biến nhất, và nó cho phép mở rộng lúc chạy.\n\n### Khi nào `isinstance` vẫn đúng\n\nCân bằng lại — không phải mọi `isinstance` đều sai:\n\n```python\n# ✓ Kiểm tra ở BIÊN, khi nhận dữ liệu từ ngoài\ndef xu_ly(x):\n    if not isinstance(x, (int, float)):\n        raise TypeError(f\"cần số, nhận {type(x).__name__}\")\n    ...\n\n# ✓ Chấp nhận nhiều dạng đầu vào, chuẩn hoá về một\ndef mo(nguon):\n    if isinstance(nguon, (str, Path)):\n        return open(nguon, encoding=\"utf-8\")\n    return nguon                       # đã là file object\n\n# ✓ Phân biệt chuỗi với iterable khác — trường hợp đặc biệt kinh điển\ndef lam_phang(x):\n    for p in x:\n        if isinstance(p, str):         # str CŨNG iterable ⇒ đệ quy vô hạn\n            yield p\n        elif hasattr(p, \"__iter__\"):\n            yield from lam_phang(p)\n        else:\n            yield p\n```\n\nRanh giới:\n\n```\n✓ Kiểm tra đầu vào ở biên hệ thống\n✓ Chuẩn hoá nhiều dạng đầu vào\n✓ str là trường hợp đặc biệt (iterable nhưng không nên đệ quy vào)\n✗ Chọn HÀNH VI khác nhau theo kiểu ← đây mới là chỗ dùng đa hình\n```\n\nCâu hỏi phân định: **`if` này để kiểm tra hay để chọn cách làm?** Chọn cách làm → dùng đa hình.\n\n### EAFP — duck typing ở tầng ngoại lệ\n\n```python\n# ❌ LBYL — hỏi \"anh có phải vịt không\"\nif isinstance(x, Vit):\n    x.keu()\n\n# ~ Kiểm tra có phương thức không\nif hasattr(x, \"keu\"):\n    x.keu()\n\n# ✓ EAFP — cứ gọi, hỏng thì bắt   ⭐ Pythonic nhất\ntry:\n    x.keu()\nexcept AttributeError:\n    print(\"object này không kêu được\")\n```\n\nEAFP hợp với duck typing vì cùng một triết lý: **quan tâm tới việc object LÀM được gì, không quan tâm nó LÀ gì.** Nối trực tiếp về bài xử lý ngoại lệ ở module 5.\n\nNhưng có cạm bẫy cần biết:\n\n```python\ntry:\n    x.keu()\nexcept AttributeError:\n    ...     # ⚠️ bắt cả AttributeError phát sinh BÊN TRONG keu()\n```\n\nNếu `keu()` tồn tại nhưng bên trong nó có lỗi `AttributeError`, bạn bắt nhầm và che mất một lỗi thật. Với trường hợp cần chính xác, `hasattr` an toàn hơn:\n\n```python\nif hasattr(x, \"keu\"):\n    x.keu()             # lỗi bên trong keu() bay lên bình thường\n```\n\n### Cái giá của duck typing\n\nNói thẳng nhược điểm:\n\n```python\ndef xu_ly(x):\n    return x.tinh() * 2\n\nxu_ly(\"chuỗi\")     # AttributeError — chỉ lộ ra KHI CHẠY tới đây\n```\n\n```\n① Lỗi phát hiện MUỘN — chỉ khi chạy đúng nhánh đó\n② Trình soạn thảo không gợi ý được — nó không biết x là gì\n③ Giao diện NGẦM ĐỊNH — đọc chữ ký hàm không biết x cần có gì\n④ Sửa code khó — đổi tên một phương thức, không biết ai đang dùng\n```\n\nĐiểm ③ là vấn đề thực tế nhất: `def xu_ly(x)` không nói gì về việc `x` phải có `tinh()`. Người dùng phải đọc thân hàm mới biết.\n\n### `Protocol` — viết giao diện ngầm định ra thành kiểu\n\n```python\nfrom typing import Protocol\n\nclass CoTheKeu(Protocol):\n    def keu(self) -> str: ...          # ⭐ chỉ khai báo\n\ndef cho_keu(x: CoTheKeu) -> None:      # ⭐ chữ ký giờ TỰ NÓI cần gì\n    print(x.keu())\n\nclass Robot:                            # KHÔNG kế thừa CoTheKeu\n    def keu(self) -> str: return \"bíp\"\n\ncho_keu(Robot())        # ✓ mypy chấp nhận — đúng hình dạng\ncho_keu(\"chuỗi\")        # ❌ mypy báo lỗi TRƯỚC khi chạy\n```\n\n`Protocol` cho **cả hai**:\n\n```\nGiữ được duck typing  → không cần kế thừa, class bên thứ ba vẫn dùng được\nLấy lại được an toàn  → mypy kiểm tra, trình soạn thảo gợi ý được\n```\n\nĐây là lý do `Protocol` là công cụ đáng dùng nhất khi viết thư viện: bạn mô tả **cái hàm cần**, chứ không ép người dùng kế thừa class của mình.\n\nMuốn kiểm tra được lúc chạy:\n\n```python\nfrom typing import runtime_checkable\n\n@runtime_checkable\nclass CoTheKeu(Protocol):\n    def keu(self) -> str: ...\n\nisinstance(Robot(), CoTheKeu)     # True\n```\n\nGiới hạn cần biết: `isinstance` với `Protocol` chỉ kiểm tra **tên phương thức có tồn tại**, không kiểm tra chữ ký hay kiểu trả về. Nó là phép thử nông.\n\n### Nạp chồng toán tử\n\n```python\nclass Vector:\n    def __init__(self, x, y): self.x, self.y = x, y\n\n    def __add__(self, k): return Vector(self.x + k.x, self.y + k.y)\n    def __mul__(self, n): return Vector(self.x * n, self.y * n)\n    def __eq__(self, k): return (self.x, self.y) == (k.x, k.y)\n    def __repr__(self): return f\"Vector({self.x}, {self.y})\"\n    def __abs__(self): return (self.x ** 2 + self.y ** 2) ** 0.5\n\nv = Vector(1, 2) + Vector(3, 4)      # Vector(4, 6)\nv * 2                                 # Vector(8, 12)\nabs(Vector(3, 4))                     # 5.0\n```\n\nĐây là đa hình ở tầng toán tử: `+` hoạt động khác nhau với `int`, `str`, `list`, và với class của bạn. Python dịch `a + b` thành `a.__add__(b)`.\n\nCảnh báo về việc lạm dụng: chỉ nạp chồng khi ý nghĩa **hiển nhiên**. `Vector + Vector` thì rõ; `NguoiDung + NguoiDung` thì không ai đoán được nó làm gì, và một phương thức có tên rõ ràng tốt hơn nhiều. Bài magic method sẽ nói kỹ.\n\n### Đa hình tham số hoá\n\nDạng thứ ba của đa hình — cùng code chạy với nhiều kiểu:\n\n```python\ndef dau_tien[T](ds: list[T]) -> T | None:\n    return ds[0] if ds else None\n\ndau_tien([1, 2])        # int | None\ndau_tien([\"a\", \"b\"])    # str | None\n```\n\nTrong Python thì đây chủ yếu là chuyện của type hint, vì bản thân ngôn ngữ vốn đã không quan tâm kiểu. Ở các ngôn ngữ kiểu tĩnh thì nó là tính năng quan trọng (*generics*).\n\nBa dạng đa hình gộp lại:\n\n```\n① Đa hình kế thừa (subtype)   — lớp con thay được lớp cha\n② Duck typing (ad-hoc)         — cùng phương thức là dùng được\n③ Tham số hoá (parametric)     — cùng code, nhiều kiểu\n```\n\nPython dùng ② nhiều nhất, và đó là điểm khác biệt lớn nhất với các ngôn ngữ kiểu tĩnh.",

vi_du: "### Bỏ `if/elif` bằng đa hình\n\n```python\n# ❌ Chuỗi if lặp ở nhiều nơi\ndef tinh_phi(loai, tien):\n    if loai == \"thuong\":\n        return tien * 0.02\n    elif loai == \"vip\":\n        return tien * 0.01\n    elif loai == \"doanh_nghiep\":\n        return max(tien * 0.005, 10_000)\n    raise ValueError(loai)\n\ndef gioi_han(loai):\n    if loai == \"thuong\":\n        return 50_000_000\n    elif loai == \"vip\":\n        return 500_000_000\n    # ⚠️ quên doanh_nghiep — không ai báo\n```\n\n```python\n# ✓ Mỗi loại một class, mọi quy tắc nằm cùng chỗ\nclass LoaiTaiKhoan:\n    ten = \"\"\n    ti_le_phi = 0.0\n    phi_toi_thieu = 0.0\n    gioi_han_ngay = 0.0\n\n    def tinh_phi(self, tien: float) -> float:\n        return max(tien * self.ti_le_phi, self.phi_toi_thieu)\n\n    def cho_phep(self, tien: float) -> bool:\n        return 0 < tien <= self.gioi_han_ngay\n\nclass Thuong(LoaiTaiKhoan):\n    ten, ti_le_phi, gioi_han_ngay = \"thường\", 0.02, 50_000_000\n\nclass VIP(LoaiTaiKhoan):\n    ten, ti_le_phi, gioi_han_ngay = \"VIP\", 0.01, 500_000_000\n\nclass DoanhNghiep(LoaiTaiKhoan):\n    ten, ti_le_phi = \"doanh nghiệp\", 0.005\n    phi_toi_thieu, gioi_han_ngay = 10_000, 5_000_000_000\n\n# Hàm dùng KHÔNG biết có bao nhiêu loại\ndef chuyen_tien(loai: LoaiTaiKhoan, tien: float) -> float:\n    if not loai.cho_phep(tien):\n        raise ValueError(f\"vượt hạn mức {loai.ten}: {loai.gioi_han_ngay:,.0f}đ\")\n    return tien + loai.tinh_phi(tien)\n\nfor lt in (Thuong(), VIP(), DoanhNghiep()):\n    print(f\"{lt.ten:<12} {chuyen_tien(lt, 1_000_000):>12,.0f}đ\")\n```\n\nThêm loại `SinhVien` chỉ là viết thêm một class — **không sửa `chuyen_tien`, không sửa các loại khác, và không thể quên một quy tắc nào** vì mọi quy tắc của một loại nằm cùng chỗ.\n\nĐây là lợi ích quan trọng hơn cả việc bớt `if`: bản đầu để lỗi \"quên `doanh_nghiep` trong `gioi_han`\" xảy ra âm thầm; bản sau thì mọi thuộc tính đều có mặt vì chúng khai báo trong một class.\n\n### Duck typing với nguồn dữ liệu thay thế được\n\n```python\nimport json\nfrom pathlib import Path\n\nclass NguonFile:\n    def __init__(self, duong_dan): self.p = Path(duong_dan)\n    def doc(self) -> list[dict]:\n        return json.loads(self.p.read_text(encoding=\"utf-8\"))\n\nclass NguonAPI:\n    def __init__(self, url): self.url = url\n    def doc(self) -> list[dict]:\n        ...      # gọi HTTP\n\nclass NguonGia:                                  # ⭐ cho kiểm thử\n    def __init__(self, du_lieu): self.du_lieu = du_lieu\n    def doc(self) -> list[dict]:\n        return self.du_lieu\n\n# Hàm nghiệp vụ — không biết dữ liệu từ đâu\ndef thong_ke(nguon) -> dict:\n    bg = nguon.doc()\n    return {\"so_luong\": len(bg),\n            \"tong\": sum(b.get(\"tien\", 0) for b in bg)}\n\n# Sản phẩm\nthong_ke(NguonFile(\"du_lieu.json\"))\n\n# Kiểm thử — KHÔNG cần file, KHÔNG cần mạng, chạy trong mili giây\nkq = thong_ke(NguonGia([{\"tien\": 100}, {\"tien\": 200}]))\nassert kq == {\"so_luong\": 2, \"tong\": 300}\n```\n\n`NguonGia` **không kế thừa gì cả** mà vẫn dùng được. Đây là lợi ích lớn nhất của duck typing trong thực tế: **kiểm thử không cần dựng hạ tầng thật**, và không cần class giả phải nằm trong cây kế thừa nào.\n\n### `Protocol` làm giao diện tường minh\n\n```python\nfrom typing import Protocol, runtime_checkable\n\n@runtime_checkable\nclass NguonDuLieu(Protocol):\n    def doc(self) -> list[dict]: ...\n\ndef thong_ke(nguon: NguonDuLieu) -> dict:      # ⭐ chữ ký tự nói cần gì\n    bg = nguon.doc()\n    return {\"so_luong\": len(bg), \"tong\": sum(b.get(\"tien\", 0) for b in bg)}\n\nthong_ke(NguonGia([]))          # ✓ mypy chấp nhận\nthong_ke(\"chuỗi\")               # ❌ mypy báo lỗi trước khi chạy\n\nisinstance(NguonGia([]), NguonDuLieu)     # True — nhờ runtime_checkable\n```\n\nSo với ba cách viết cùng một ý:\n\n```\ndef thong_ke(nguon)                  → không ai biết nguon cần gì\ndef thong_ke(nguon: NguonFile)       → ép một class cụ thể, mất linh hoạt\ndef thong_ke(nguon: NguonDuLieu)     → ⭐ nói rõ HÌNH DẠNG cần, vẫn mở\n```\n\nDòng cuối là điểm cân bằng: tài liệu rõ ràng, công cụ kiểm tra được, mà bất kỳ class nào có `doc()` vẫn dùng được.\n\n### Class hoà vào cú pháp Python\n\n```python\nclass DanhSachViec:\n    def __init__(self, viec=()):\n        self._viec = list(viec)\n\n    def __len__(self): return len(self._viec)\n    def __iter__(self): return iter(self._viec)\n    def __contains__(self, v): return v in self._viec\n    def __getitem__(self, i): return self._viec[i]\n    def __bool__(self): return bool(self._viec)\n    def __repr__(self): return f\"DanhSachViec({self._viec!r})\"\n\n    def them(self, v):\n        if not v.strip():\n            raise ValueError(\"việc không được rỗng\")\n        self._viec.append(v)\n\nds = DanhSachViec([\"học\", \"code\"])\n\nlen(ds)                 # 2\n\"học\" in ds             # True\nds[0]                   # 'học'\nfor v in ds: print(v)   # duyệt được\nif ds: print(\"còn việc\")\nsorted(ds)              # ['code', 'học']\nlist(ds)                # ['học', 'code']\n\", \".join(ds)           # 'học, code'\n```\n\nSáu magic method biến class thành thứ dùng được với **mọi hàm dựng sẵn nhận iterable**. Bạn không viết code cho `sorted`, `join`, `sum` — chúng chỉ cần `__iter__`.\n\nĐây là duck typing ở tầng ngôn ngữ, và là lý do Python ít cần interface tường minh hơn Java rất nhiều.\n\n### Bảng tra thay chuỗi `if`\n\n```python\nclass BoXuat:\n    duoi = \"\"\n    def xuat(self, du_lieu: list[dict]) -> str:\n        raise NotImplementedError\n\nclass XuatJSON(BoXuat):\n    duoi = \".json\"\n    def xuat(self, du_lieu):\n        import json\n        return json.dumps(du_lieu, ensure_ascii=False, indent=2)\n\nclass XuatCSV(BoXuat):\n    duoi = \".csv\"\n    def xuat(self, du_lieu):\n        import csv, io\n        if not du_lieu:\n            return \"\"\n        buf = io.StringIO()\n        w = csv.DictWriter(buf, fieldnames=list(du_lieu[0]))\n        w.writeheader(); w.writerows(du_lieu)\n        return buf.getvalue()\n\nclass XuatBang(BoXuat):\n    duoi = \".txt\"\n    def xuat(self, du_lieu):\n        if not du_lieu:\n            return \"\"\n        cot = list(du_lieu[0])\n        rong = {c: max(len(c), *(len(str(d[c])) for d in du_lieu)) for c in cot}\n        dong = [\" | \".join(c.ljust(rong[c]) for c in cot)]\n        dong.append(\"-+-\".join(\"-\" * rong[c] for c in cot))\n        for d in du_lieu:\n            dong.append(\" | \".join(str(d[c]).ljust(rong[c]) for c in cot))\n        return \"\\n\".join(dong)\n\nBANG: dict[str, type[BoXuat]] = {\n    \"json\": XuatJSON, \"csv\": XuatCSV, \"bang\": XuatBang,\n}\n\ndef xuat(du_lieu, dinh_dang: str) -> str:\n    lop = BANG.get(dinh_dang)\n    if lop is None:\n        raise ValueError(f\"không hỗ trợ {dinh_dang!r}, có: {sorted(BANG)}\")\n    return lop().xuat(du_lieu)\n\ndu_lieu = [{\"ten\": \"Minh\", \"diem\": 8}, {\"ten\": \"An\", \"diem\": 9}]\nprint(xuat(du_lieu, \"bang\"))\n# ten  | diem\n# -----+-----\n# Minh | 8\n# An   | 9\n```\n\nThêm định dạng mới, **không sửa hàm `xuat`**:\n\n```python\nclass XuatXML(BoXuat):\n    duoi = \".xml\"\n    def xuat(self, du_lieu):\n        muc = \"\".join(\n            \"<muc>\" + \"\".join(f\"<{k}>{v}</{k}>\" for k, v in d.items()) + \"</muc>\"\n            for d in du_lieu)\n        return f\"<danh_sach>{muc}</danh_sach>\"\n\nBANG[\"xml\"] = XuatXML          # ⭐ đăng ký từ bên ngoài\nprint(xuat(du_lieu, \"xml\"))\n```\n\n### `isinstance` dùng đúng chỗ\n\n```python\nfrom pathlib import Path\nfrom typing import IO\n\ndef doc_noi_dung(nguon) -> str:\n    \"\"\"Nhận đường dẫn (str/Path) hoặc file object đã mở.\"\"\"\n    if isinstance(nguon, (str, Path)):          # ✓ chuẩn hoá đầu vào\n        return Path(nguon).read_text(encoding=\"utf-8\")\n    return nguon.read()                          # ✓ duck typing cho phần còn lại\n\ndoc_noi_dung(\"a.txt\")\ndoc_noi_dung(Path(\"a.txt\"))\nwith open(\"a.txt\", encoding=\"utf-8\") as f:\n    doc_noi_dung(f)\nimport io\ndoc_noi_dung(io.StringIO(\"nội dung giả\"))       # ✓ cho kiểm thử\n```\n\nMẫu này kết hợp cả hai: `isinstance` cho **một trường hợp đặc biệt**, duck typing cho phần còn lại. Nó nhận được mọi thứ có `.read()`, kể cả những kiểu chưa tồn tại lúc viết hàm.\n\nTrường hợp `str` là iterable — bẫy kinh điển:\n\n```python\ndef lam_phang(x):\n    for p in x:\n        if isinstance(p, str):        # ⭐ BẮT BUỘC — str iterable vô hạn\n            yield p\n        elif hasattr(p, \"__iter__\"):\n            yield from lam_phang(p)\n        else:\n            yield p\n\nlist(lam_phang([1, [2, [\"ab\", 3]], 4]))    # [1, 2, 'ab', 3, 4]\n\n# Không có dòng isinstance thì \"ab\" → \"a\", \"b\" → \"a\", \"b\"... đệ quy vô hạn\n```\n\n### Nạp chồng toán tử — dùng đúng và sai\n\n```python\n# ✓ Ý nghĩa hiển nhiên\nclass Tien:\n    def __init__(self, xu: int, don_vi=\"VND\"):\n        self.xu, self.don_vi = xu, don_vi     # ⭐ số nguyên, không dùng float\n\n    def __add__(self, k):\n        if not isinstance(k, Tien):\n            return NotImplemented              # ⭐ để Python thử cách khác\n        if self.don_vi != k.don_vi:\n            raise ValueError(f\"khác đơn vị: {self.don_vi} và {k.don_vi}\")\n        return Tien(self.xu + k.xu, self.don_vi)\n\n    def __mul__(self, n):\n        if not isinstance(n, int):\n            return NotImplemented\n        return Tien(self.xu * n, self.don_vi)\n\n    def __eq__(self, k):\n        return isinstance(k, Tien) and (self.xu, self.don_vi) == (k.xu, k.don_vi)\n\n    def __lt__(self, k):\n        if self.don_vi != k.don_vi:\n            raise ValueError(\"không so sánh được hai đơn vị khác nhau\")\n        return self.xu < k.xu\n\n    def __repr__(self):\n        return f\"{self.xu/100:,.2f} {self.don_vi}\"\n\na = Tien(150_00) + Tien(250_00)\nprint(a)                         # 400.00 VND\nprint(sorted([Tien(300), Tien(100)]))\n```\n\nHai chi tiết đáng học:\n\n```\nreturn NotImplemented   → nói \"tôi không xử lý được\", để Python thử __radd__\n                          của toán hạng kia; trả về sai kiểu thì lỗi khó hiểu\nlưu xu (số nguyên)       → tiền không dùng float (bài IEEE 754 ở module 1)\n```\n\n```python\n# ❌ Ý nghĩa không hiển nhiên — đừng nạp chồng\nclass NguoiDung:\n    def __add__(self, k):\n        ...      # cộng hai người dùng nghĩa là gì?\n\n# ✓ Phương thức có tên rõ ràng\nclass NguoiDung:\n    def gop_quyen_voi(self, k): ...\n```\n\n### Đo lợi ích: thêm một loại mới\n\n```python\n# Với if/elif: đếm số chỗ phải sửa\n# grep -rn 'if loai ==' .\n#   thanh_toan.py:45\n#   bao_cao.py:12\n#   xuat_file.py:88\n#   kiem_tra.py:23\n# ⇒ 4 chỗ, và bỏ sót một chỗ là lỗi âm thầm\n\n# Với đa hình: 1 chỗ\nclass SinhVien(LoaiTaiKhoan):\n    ten, ti_le_phi, gioi_han_ngay = \"sinh viên\", 0.0, 10_000_000\n# ⇒ xong. Mọi hàm hiện có tự dùng được.\n```\n\nĐây là phép đo thực dụng nhất cho giá trị của đa hình: **số chỗ phải sửa khi thêm một loại mới.** Nếu con số đó lớn hơn một, thiết kế còn chỗ cải thiện.",

so_sanh: "### Ba dạng đa hình\n\n| | Kế thừa (subtype) | Duck typing | Tham số hoá |\n|---|---|---|---|\n| Cần quan hệ class | **✓** | ✗ | ✗ |\n| Kiểm tra lúc nào | chạy | chạy | (type hint) |\n| Linh hoạt | vừa | **cao nhất** | cao |\n| An toàn | vừa | thấp (trừ khi có `Protocol`) | cao |\n| Python dùng nhiều | vừa | **nhiều nhất** | ít |\n\nPython nghiêng hẳn về duck typing, và đó là khác biệt lớn nhất với Java hay C#.\n\n### Duck typing và interface tường minh\n\n| | Duck typing (Python) | Interface (Java) |\n|---|---|---|\n| Phải khai báo | ✗ | **✓ `implements`** |\n| Dùng class bên thứ ba | **✓ tự nhiên** | ✗ phải bọc lại |\n| Lỗi phát hiện | lúc chạy | **lúc biên dịch** |\n| Giao diện có tài liệu | ngầm định | **tường minh** |\n| Trình soạn thảo gợi ý | kém | **tốt** |\n| Class giả cho kiểm thử | **rất dễ** | phải cài đặt interface |\n\n`Protocol` là cách lấy các ưu điểm của cột phải mà giữ nguyên cột trái — nó mô tả **hình dạng cần**, không ép quan hệ kế thừa.\n\n### `isinstance` — khi nào đúng, khi nào sai\n\n| Tình huống | Đánh giá |\n|---|---|\n| Kiểm tra đầu vào ở biên hệ thống | **✓ đúng** |\n| Chuẩn hoá nhiều dạng đầu vào về một | **✓ đúng** |\n| Phân biệt `str` khỏi iterable khác | **✓ bắt buộc** |\n| Chọn **hành vi** khác nhau theo kiểu | **✗ dùng đa hình** |\n| Chuỗi `if isinstance` dài dần | ✗ dấu hiệu thiết kế sai |\n\nCâu hỏi phân định: **`if` này để kiểm tra hay để chọn cách làm?**\n\n### `if/elif` và đa hình\n\n| | Chuỗi `if/elif` | Đa hình |\n|---|---|---|\n| Thêm loại mới | **sửa mọi chỗ có `if`** | thêm một class |\n| Bỏ sót một chỗ | **✓ dễ, và im lặng** | ✗ |\n| Quy tắc của một loại | rải rác nhiều hàm | **gom một chỗ** |\n| Mở rộng từ bên ngoài | ✗ | **✓** |\n| Đọc luồng thực thi | dễ (thấy hết ở một chỗ) | khó hơn |\n| Phù hợp | 2–3 nhánh, ổn định | **nhiều loại, hay thêm** |\n\nCột `if/elif` không phải luôn sai: với hai nhánh cố định không bao giờ thêm, một `if` đơn giản rõ hơn ba class. Đa hình đáng khi **số loại sẽ tăng**.\n\n### EAFP và kiểm tra trước\n\n| | `try/except AttributeError` | `hasattr` | `isinstance` |\n|---|---|---|---|\n| Pythonic | **✓ nhất** | ✓ | tuỳ |\n| Bắt nhầm lỗi bên trong | **✓ rủi ro** | ✗ | ✗ |\n| Chấp nhận mọi kiểu có phương thức | ✓ | ✓ | ✗ |\n| Rõ ràng khi đọc | vừa | **✓** | ✓ |\n\nRủi ro của cột đầu: nếu phương thức tồn tại nhưng bên trong nó ném `AttributeError`, bạn bắt nhầm và che mất lỗi thật. Khi cần chính xác thì `hasattr` an toàn hơn.\n\n### Nạp chồng toán tử — nên và không nên\n\n| Trường hợp | Nạp chồng? |\n|---|---|\n| `Vector + Vector`, `Tien + Tien` | **✓ ý nghĩa hiển nhiên** |\n| `len(bo_suu_tap)`, `x in bo_suu_tap` | **✓** |\n| So sánh để sắp xếp (`__lt__`) | ✓ |\n| `NguoiDung + NguoiDung` | **✗ không ai đoán được** |\n| `config >> file` (cú pháp lạ) | ✗ |\n\nTiêu chí: **người đọc có đoán đúng ý nghĩa mà không cần đọc tài liệu không?** Không → dùng phương thức có tên.\n\n### Đa hình và số chỗ phải sửa\n\n| Thiết kế | Thêm một loại mới |\n|---|---|\n| `if/elif` rải rác | sửa **n chỗ**, dễ bỏ sót |\n| `if/elif` gom một hàm | sửa 1 chỗ, hàm phình dần |\n| Đa hình + bảng tra | **thêm 1 class, sửa 0 dòng cũ** |\n\nHàng cuối là mục tiêu, và nó chính là nguyên tắc Open/Closed mà bài SOLID sẽ nói: **mở cho mở rộng, đóng cho sửa đổi.**",

loi_thuong_gap: [
  "**Dùng `if isinstance` để chọn hành vi khác nhau.** Đó là chỗ đa hình thay thế được; thêm loại mới sẽ phải sửa mọi chỗ có `if`.",
  "**Để chuỗi `if/elif` theo kiểu lặp ở nhiều hàm.** Bỏ sót một chỗ là lỗi âm thầm.",
  "**Quên rằng `str` cũng là iterable.** Hàm đệ quy làm phẳng sẽ chạy vô hạn nếu không tách riêng `str`.",
  "**Bắt `AttributeError` quanh cả lời gọi.** Có thể bắt nhầm lỗi phát sinh bên trong phương thức đó. Dùng `hasattr` khi cần chính xác.",
  "**Không ghi giao diện cần thiết vào chữ ký hàm.** `def f(x)` không nói gì; dùng `Protocol` để chữ ký tự tài liệu.",
  "**Ép người dùng kế thừa class của mình để dùng thư viện.** Dùng `Protocol` — họ chỉ cần có đúng phương thức.",
  "**Nạp chồng toán tử khi ý nghĩa không hiển nhiên.** `NguoiDung + NguoiDung` không ai đoán được. Dùng phương thức có tên.",
  "**Quên `return NotImplemented` trong `__add__` khi kiểu không hợp.** Trả về sai kiểu gây lỗi khó hiểu ở nơi khác.",
  "**Dùng `type(x) is C` thay `isinstance`.** Từ chối lớp con, phá vỡ đa hình.",
  "**Tưởng đa hình bắt buộc phải có kế thừa.** Trong Python thì không — duck typing đủ.",
  "**Dùng `Protocol` mà quên `@runtime_checkable` khi cần `isinstance`.** Và nhớ rằng phép kiểm tra đó chỉ xét tên phương thức, không xét chữ ký.",
  "**Áp đa hình cho hai nhánh cố định không bao giờ thêm.** Ba class thay cho một `if` là phức tạp hoá không cần thiết.",
  "**Class giả trong kiểm thử phải kế thừa class thật.** Không cần — duck typing cho phép class giả độc lập hoàn toàn."
],

tu_kiem_tra: [
  "Đa hình là gì? Giá trị thực dụng lớn nhất của nó với code gọi là gì?",
  "Duck typing khác đa hình qua kế thừa ở điểm nào? Nêu một hệ quả thực tế.",
  "Vì sao class không kế thừa gì vẫn dùng được `len()`, `for`, `in`, `sorted()`?",
  "Nêu ba vấn đề của chuỗi `if/elif` theo kiểu. Đa hình giải quyết chúng thế nào?",
  "Nêu ba trường hợp `isinstance` vẫn đúng. Câu hỏi nào giúp phân định?",
  "Vì sao phải tách riêng `str` trong hàm đệ quy làm phẳng?",
  "EAFP liên hệ với duck typing thế nào? Rủi ro của `try/except AttributeError` là gì?",
  "Nêu bốn cái giá của duck typing.",
  "`Protocol` cho lại được những gì mà vẫn giữ duck typing?",
  "Giới hạn của `isinstance` với `Protocol` là gì?",
  "Vì sao `__add__` nên `return NotImplemented` khi gặp kiểu không hợp?",
  "Tiêu chí nào quyết định có nên nạp chồng một toán tử?",
  "Vì sao class giả cho kiểm thử không cần kế thừa class thật?",
  "Phép đo nào cho biết thiết kế đa hình đã tốt chưa?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, mục *Data model* — nơi định nghĩa mọi giao thức ngầm định (`__len__`, `__iter__`, `__add__`...).",
  "Tài liệu Python, module `typing`, phần *Protocols* — cú pháp và giới hạn của `Protocol` và `runtime_checkable`.",
  "Sách **Fluent Python** — Luciano Ramalho. Chương về giao thức và interface là giải thích đầy đủ nhất về duck typing trong Python.",
  "Sách **Effective Python** — Brett Slatkin. Các mục về việc dùng đa hình thay cho kiểm tra kiểu.",
  "Sách **Practical Object-Oriented Design** — Sandi Metz. Phần về \"nhắn tin cho object\" giải thích rất rõ vì sao nên hỏi object làm được gì thay vì nó là gì."
],

lien_he: [
  "**Kế thừa và MRO** ngay trước: liên kết động là cơ chế làm đa hình hoạt động.",
  "**Trừu tượng: abstract class, Protocol** ngay sau: cách ép một giao diện phải có, và `Protocol` được nói kỹ ở đó.",
  "**Composition over inheritance**: đa hình qua duck typing làm composition dễ hơn nhiều — không cần cây kế thừa.",
  "**SOLID — Open/Closed**: \"thêm loại mới không sửa code cũ\" chính là ứng dụng trực tiếp của bài này.",
  "**SOLID — Liskov Substitution**: điều kiện để đa hình qua kế thừa thật sự đúng đắn.",
  "**Magic method** trong module này: `__len__`, `__iter__`, `__add__` là đa hình ở tầng toán tử và hàm dựng sẵn.",
  "**Design pattern — Strategy**: mẫu thiết kế này chính là đa hình được đặt tên.",
  "**Type hint và mypy** ở module 5: `Protocol` lấy lại an toàn kiểu mà không mất linh hoạt.",
  "**Xử lý ngoại lệ** ở module 5: EAFP là triết lý nền của duck typing.",
  "**Backend & Web** ở mảng 05: đổi giữa các nhà cung cấp lưu trữ hay thanh toán là đa hình ở quy mô kiến trúc.",
  "**AI/ML** ở mảng 07: mọi lớp mạng nơ-ron có cùng giao diện `forward()`, nên ghép chúng thành mô hình lớn được — đó là duck typing."
]

};
