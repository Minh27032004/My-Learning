window.LESSONS["05:testing:mock-va-fake-dependency-ngoai"] = {

tom_tat: [
  "Mock ghi nhận tương tác để kiểm lời gọi; fake là implementation đơn giản nhưng có hành vi và trạng thái. Chúng phục vụ hai loại câu hỏi khác nhau.",
  "Chỉ thay dependency tại ranh giới do ứng dụng sở hữu, như `PaymentPort` và `EmailPort`; mock HTTP client hoặc ORM sâu bên trong khiến test dính implementation.",
  "Fake phải giữ các invariant quan trọng của hệ thật và có contract test dùng chung, nếu không suite có thể xanh trên một thế giới không tồn tại.",
  "Timeout, retry, idempotency và lỗi không rõ kết quả quan trọng hơn một response 200 đẹp khi kiểm dependency ngoài.",
  "Không cho test gọi dịch vụ thật mặc định: vừa flaky, tốn tiền, vừa có thể rò dữ liệu và credential."
],

can_biet_truoc: [
  "Dependency injection, `Protocol` hoặc abstract interface trong Python.",
  "HTTP timeout, status code, retry và khái niệm idempotency key.",
  "Luồng xác nhận hold: thu tiền, commit trạng thái, phát yêu cầu gửi email.",
  "Unit test và integration test khác nhau theo ranh giới thực thi."
],

dinh_nghia: "**Test double** là đối tượng thay cộng tác viên thật trong test. **Mock** là double được cấu hình kỳ vọng hoặc ghi lại lời gọi để assertion về tương tác: hàm nào được gọi, với tham số gì, bao nhiêu lần. **Stub** chủ yếu trả câu trả lời định sẵn. **Fake** cung cấp implementation rút gọn có logic hoạt động, chẳng hạn payment gateway bộ nhớ lưu idempotency key và trả cùng giao dịch cho lần gọi lặp.\n\nTrong Python, `unittest.mock.Mock` có thể đóng vai stub lẫn mock; tên lớp không quyết định mục đích. Điều quyết định là câu hỏi test: kiểm kết quả trả về hay kiểm cuộc hội thoại giữa các object.",

vi_sao_can: "Xác nhận đặt chỗ chạm cổng thanh toán và email. Gọi thật trong mỗi test làm suite phụ thuộc Internet, quota, tiền và trạng thái sandbox. Một timeout ngẫu nhiên biến thay đổi logic vô hại thành CI đỏ; email thử có thể gửi tới địa chỉ thật nếu dữ liệu bị cấu hình nhầm.\n\nNhưng thay mọi thứ bằng `Mock()` cũng nguy hiểm. Nếu production adapter gửi trường `amount_cents` còn mock chấp nhận bất kỳ keyword nào, test xanh dù request thật bị provider từ chối. Nếu assertion khóa chặt thứ tự năm method nội bộ, một refactor không đổi hành vi vẫn phá hàng chục test.\n\nTa cần một chiến lược hai vòng: unit test dùng double ở port ổn định để khám phá mọi nhánh nhanh; integration test kiểm adapter với fake server hoặc sandbox có kiểm soát để chứng minh serialization, authentication và timeout. Sự tách biệt này cũng tạo điểm kiểm bảo mật: secret chỉ ở adapter/runtime, không lọt vào model miền hay fixture.",

co_che: "### Đặt port do ứng dụng sở hữu\n\nĐừng để service biết `httpx.Client`. Hãy định nghĩa ngôn ngữ mà nghiệp vụ cần: charge một hold theo số tiền và idempotency key. Adapter mới biết URL, header và JSON của provider. Vì interface thuộc ứng dụng, thay provider không lan vào test nghiệp vụ.\n\n```python\nfrom dataclasses import dataclass\nfrom typing import Protocol\n\n@dataclass(frozen=True)\nclass ChargeResult:\n    transaction_id: str\n    status: str\n\nclass PaymentPort(Protocol):\n    def charge(self, *, amount_cents: int, token: str,\n               idempotency_key: str) -> ChargeResult: ...\n\nclass EmailPort(Protocol):\n    def send_confirmation(self, *, recipient: str, booking_id: str) -> None: ...\n```\n\nPort nhỏ nhưng không giả tạo: idempotency key có mặt vì retry là thực tế, số tiền dùng integer cent để tránh sai số float. Không đưa raw card number qua port; service chỉ nhận token do thành phần đạt chuẩn thanh toán tạo ra. Điều này thu hẹp dữ liệu nhạy cảm mà ứng dụng xử lý.\n\n### Mock phù hợp khi cuộc gọi là kết quả quan sát được\n\nViệc “không charge khi hold hết hạn” là yêu cầu quan trọng. Mock chứng minh absence of call rõ hơn fake chỉ còn state rỗng. Dùng `create_autospec` để sai tên tham số bị bắt sớm.\n\n```python\nfrom unittest.mock import create_autospec\n\ndef test_expired_hold_never_charges():\n    payments = create_autospec(PaymentPort, instance=True)\n    emails = create_autospec(EmailPort, instance=True)\n    service = BookingService(repo=repo_with_expired_hold(),\n                             payments=payments, emails=emails)\n\n    with pytest.raises(HoldExpired):\n        service.confirm(\"hold-1\", token=\"tok_test\")\n\n    payments.charge.assert_not_called()\n    emails.send_confirmation.assert_not_called()\n```\n\nAssertion tương tác có giá trị vì charge là side effect gây tiền. Không cần assert repository gọi `get_by_id` đúng một lần; đó là chi tiết tối ưu có thể đổi sang query khác. Quy tắc: chỉ quan sát interaction mà stakeholder nhận ra hậu quả.\n\n### Stub câu trả lời để đẩy service qua nhánh lỗi\n\nMột mock có thể được dùng như stub bằng `return_value` hoặc `side_effect`. Ta điều khiển lỗi từ payment mà không chờ network.\n\n```python\npayments.charge.side_effect = PaymentDeclined(code=\"insufficient_funds\")\n\nwith pytest.raises(CannotConfirm):\n    service.confirm(\"hold-1\", token=\"tok_test\")\n\nassert repository.status(\"hold-1\") == \"held\"\nemails.send_confirmation.assert_not_called()\n```\n\nExpected quan trọng là hold vẫn `held`, cho phép người dùng thử phương thức khác, và email không phát. Đừng assert nguyên văn thông báo provider; adapter phải dịch lỗi ngoài thành taxonomy nội bộ ổn định để không lộ chi tiết hoặc phụ thuộc wording.\n\n### Fake tạo state để thử chuỗi hành động\n\nMock giỏi trả lời “đã gọi chưa”; fake giỏi mô phỏng nhiều lời gọi liên quan. `FakePaymentGateway` dưới đây ghi transaction theo idempotency key, nên retry không thu tiền lần hai.\n\n```python\nclass FakePaymentGateway:\n    def __init__(self):\n        self.transactions = {}\n        self.calls = []\n        self.failure = None\n\n    def charge(self, *, amount_cents, token, idempotency_key):\n        self.calls.append((amount_cents, token, idempotency_key))\n        if self.failure:\n            raise self.failure\n        if idempotency_key not in self.transactions:\n            number = len(self.transactions) + 1\n            self.transactions[idempotency_key] = ChargeResult(\n                transaction_id=f\"fake-{number}\", status=\"captured\")\n        return self.transactions[idempotency_key]\n```\n\nFake này không mô phỏng tiền thật; nó chỉ giữ invariant test cần. Hai token khác nhau nhưng cùng key vẫn trả một transaction, qua đó làm lộ bug nếu service sinh key mới khi retry. Fake cũng cho phép kiểm toàn bộ workflow mà không viết hàng loạt `side_effect=[...]` khó hiểu.\n\n### Fake không được nói dối về hợp đồng\n\nFake bộ nhớ thường quá rộng lượng: chấp nhận chuỗi rỗng, không giới hạn độ dài, không unique và không có concurrency. Mỗi khác biệt có thể che bug. Không cần sao chép cả provider, nhưng phải liệt kê những thuộc tính mà test dựa vào.\n\nChạy một **contract test suite** chung cho fake và adapter thật. Ví dụ: cùng idempotency key trả cùng transaction; amount ≤ 0 bị từ chối; token bị decline được dịch thành `PaymentDeclined`; timeout thành `PaymentUnavailable`. Adapter integration có thể chạy với fake HTTP server điều khiển response, còn một nhóm nhỏ chạy sandbox theo lịch nếu credential tồn tại.\n\n```python\ndef payment_contract(make_gateway):\n    gateway = make_gateway()\n    first = gateway.charge(amount_cents=250_000, token=\"ok\",\n                           idempotency_key=\"booking-7\")\n    second = gateway.charge(amount_cents=250_000, token=\"ok\",\n                            idempotency_key=\"booking-7\")\n    assert second.transaction_id == first.transaction_id\n```\n\nContract dùng chung giảm drift nhưng không chứng minh mọi chi tiết provider. Fake HTTP server vẫn do ta lập trình, nên sandbox test hoặc tài liệu provider vẫn là nguồn kiểm độc lập.\n\n### Patch ở nơi tên được tra cứu\n\nNếu code `booking.py` dùng `from gateway import send`, phải patch `booking.send`, không patch `gateway.send`. Python đã bind tên vào module consumer; sửa nơi khai báo gốc không đổi reference đang dùng. Cách tốt hơn là inject port qua constructor, giảm nhu cầu patch global.\n\n`monkeypatch` của pytest tự hoàn tác sau test và hợp cho biến môi trường, clock module cũ hoặc function chưa thể refactor. Tuy nhiên patch quá sâu như `httpx.Client.send` ảnh hưởng mọi request trong process, kể cả telemetry hay service khác; fake adapter ở ranh giới hẹp an toàn hơn.\n\n### Timeout có ba kết quả chứ không chỉ thành công và thất bại\n\nKhi client timeout, server có thể chưa nhận request, đã nhận nhưng chưa xử lý, hoặc đã charge nhưng response thất lạc. Vì kết quả không rõ, retry charge với key mới có thể thu hai lần. Test phải mô hình hóa “timeout sau khi side effect xảy ra”.\n\nFake có thể lưu transaction rồi ném `TimeoutError` ở lần đầu; lần retry cùng key trả transaction cũ. Đây là nhánh mà stub đơn giản `raise TimeoutError` không diễn tả. Service không nên đánh dấu failed chắc chắn khi trạng thái payment unknown; nó có thể chuyển `payment_pending` và reconciliation tra lại sau.\n\n### Retry cần ngân sách và phân loại lỗi\n\nChỉ retry lỗi tạm thời như timeout hoặc 503, không retry 401, validation hay card declined. Backoff trong unit test phải inject sleeper để không đợi thật. Mock sleeper có thể assert số lần và delay policy, nhưng đừng assert timing wall-clock.\n\nGiới hạn tổng số lần để tránh worker mắc kẹt. Nếu endpoint đồng bộ retry quá lâu, client có thể timeout trước và gửi lại; queue nền với idempotency thường an toàn hơn. Test cần ca mọi lần đều lỗi, thành công ở lần cuối và lỗi không retry.\n\n### Email có semantics khác payment\n\nEmail thường at-least-once: gửi trùng gây phiền nhưng không mất tiền. Payment đòi idempotency nghiêm ngặt. Đừng tái dùng một abstraction “ExternalService.call” cho cả hai vì chính sách retry, audit và mức nhạy cảm khác nhau.\n\n`FakeOutbox` lưu message sau transaction, rồi worker gửi email. Unit test booking chỉ assert outbox có sự kiện đúng, không assert template HTML. Integration test worker–email adapter kiểm recipient, subject và escaping. Nội dung do người dùng nhập phải escape để tránh HTML injection trong email.\n\n### Bảo vệ suite khỏi network thật\n\nMột autouse fixture có thể chặn socket ngoài localhost trong unit suite. Credential test không nên nằm trong source; CI inject secret chỉ cho job sandbox được phê duyệt. Dùng domain allowlist, tài khoản provider riêng và hard spending limit. Log fake không lưu raw token; chỉ lưu token giả hoặc fingerprint đã che.\n\nTest cũng phải chứng minh adapter không ghi Authorization header vào exception. Fake server trả lỗi chứa request echo là cơ hội kiểm sanitizer. Đây là lỗi triển khai thường bị bỏ qua nếu chỉ test status.\n\n### Khi nào không dùng mock hoặc fake\n\nKhông mock hàm thuần; gọi thật rẻ và đáng tin hơn. Không fake PostgreSQL bằng dict để kết luận transaction/lock đúng. Không mock thư viện mà ta không sở hữu ở mọi test; bọc nó trong adapter rồi kiểm adapter bằng integration. Và không gọi sandbox trong vòng unit mặc định, vì availability ngoài không thuộc correctness cục bộ.\n\nNếu một test cần 15 mock và cấu hình thứ tự dài, đối tượng đang có quá nhiều trách nhiệm hoặc test dựng sai tầng. Tách orchestration, policy và adapter thường tạo thiết kế dễ hiểu hơn cả production lẫn test.",

vi_du: "### Kiểm retry idempotent với fake có lỗi sau side effect\n\n```python\nclass TimeoutAfterCapture(FakePaymentGateway):\n    def __init__(self):\n        super().__init__()\n        self.first = True\n    def charge(self, **request):\n        result = super().charge(**request)\n        if self.first:\n            self.first = False\n            raise TimeoutError(\"response lost\")\n        return result\n\ndef test_retry_reuses_idempotency_key():\n    gateway = TimeoutAfterCapture()\n    service = BookingService(repo=repo_with_hold(\"h1\"), payments=gateway,\n                             emails=FakeEmail())\n\n    with pytest.raises(PaymentPending):\n        service.confirm(\"h1\", token=\"tok_test\")\n    service.reconcile(\"h1\", token=\"tok_test\")\n\n    assert len(gateway.transactions) == 1\n    assert gateway.calls[0][2] == gateway.calls[1][2]\n```\n\nTest không tuyên bố timeout nghĩa là charge thất bại. Nó dựng đúng tình huống response mất sau capture và chứng minh hai lần dùng cùng key. Production adapter còn cần endpoint tra transaction; fake có thể bổ sung `find_by_key` khi workflow reconciliation dùng thật.\n\n### Integration test adapter bằng server cục bộ\n\nFake server nhận HTTP thật, xác nhận `Authorization` có mặt nhưng không ghi giá trị, kiểm JSON và cố tình delay hoặc trả 503. Khác mock method, nó bắt được URL sai, serialize sai, cấu hình timeout sai. Nó vẫn chạy localhost và không cần Internet. Một test riêng trả body không phải JSON để chắc adapter sinh lỗi có kiểm soát thay vì lộ stack trace cho FastAPI.",

so_sanh: "### Chọn double theo câu hỏi\n\n| Loại | Nó cung cấp | Assertion điển hình | Điểm mù |\n|---|---|---|---|\n| Stub | câu trả lời định sẵn | trạng thái service sau response | chuỗi nhiều lời gọi |\n| Mock | lịch sử/kỳ vọng tương tác | charge không gọi hoặc gọi một lần | dễ khóa implementation |\n| Fake | implementation rút gọn có state | retry giữ một transaction | drift khỏi hệ thật |\n| Fake HTTP server | protocol qua socket cục bộ | URL, header, JSON, timeout | không phải provider thật |\n| Sandbox | hệ provider kiểm soát | credential và contract thực | chậm, quota, có thể flaky |\n\n`Mock(spec=...)` bắt thuộc tính không tồn tại; `create_autospec` còn kiểm chữ ký lời gọi. Cả hai không kiểm semantics: amount tính bằng đồng hay cent đều là `int`. Type riêng hoặc tên `amount_cents` và contract test mới giảm nhầm đơn vị.\n\nRecord/replay HTTP giúp tái hiện payload thật, nhưng cassette có thể chứa token và dữ liệu cá nhân, nhanh lỗi thời, hoặc che thay đổi provider. Chỉ dùng khi có quy trình scrub bí mật và cập nhật có chủ đích; fake server viết tay thường rõ hơn cho vài response quan trọng.",

loi_thuong_gap: [
  "Mock `httpx` ngay trong test service: test biết header và URL của adapter; nhận biết khi đổi provider làm hàng loạt unit test đỏ.",
  "Fake luôn trả thành công: retry, decline và trạng thái không rõ không bao giờ được chạy; thêm failure modes có tên.",
  "Dùng `assert_called_once_with` cho mọi cộng tác viên: refactor batch hoặc cache phá test dù kết quả giữ nguyên; chỉ assert side effect nghiệp vụ.",
  "Patch nơi hàm được định nghĩa thay vì nơi được tra cứu: network thật vẫn chạy; inject port hoặc patch symbol trong module consumer.",
  "Retry mọi exception: 401 và validation bị gọi lặp vô ích; phân loại permanent/transient và đặt ngân sách.",
  "Sinh idempotency key mới sau timeout: sandbox thỉnh thoảng có giao dịch trùng; fake phải mô phỏng timeout sau capture.",
  "Ghi raw token vào `mock_calls` rồi in khi fail: secret xuất hiện trong CI; chỉ dùng token giả và sanitizer log."
],

tu_kiem_tra: [
  "Khi nào “không được gọi” là yêu cầu nghiệp vụ đủ mạnh để dùng mock?",
  "Vì sao autospec vẫn không bắt được nhầm `amount_cents` với số tiền theo đồng?",
  "Hãy mô tả timeout xảy ra sau capture và lý do retry cần cùng idempotency key.",
  "Contract test chung giữa fake và adapter ngăn loại drift nào, và còn điểm mù nào?",
  "Tại sao fake dict không thể chứng minh PostgreSQL lock chống oversell?",
  "Nếu code import trực tiếp một hàm, phải patch symbol ở module nào?",
  "Email và payment khác nhau thế nào về hậu quả gửi lặp và chiến lược retry?"
],

tai_nguyen: [
  "Test Double — bài viết của Martin Fowler.",
  "Growing Object-Oriented Software, Guided by Tests — Steve Freeman và Nat Pryce.",
  "Architecture Patterns with Python — Harry Percival và Bob Gregory.",
  "Python Standard Library Documentation — unittest.mock."
],

lien_he: [
  "Nối sang mảng Kiến trúc qua Ports and Adapters: double thay port do ứng dụng sở hữu, không chọc vào thư viện sâu.",
  "Nối sang mảng Hệ phân tán qua timeout, retry, idempotency và trạng thái kết quả không rõ.",
  "Nối sang mảng Security qua quản lý credential, che token trong log và chặn egress của test.",
  "Nối sang mảng Database qua outbox và transaction: fake chỉ kiểm workflow, integration mới chứng minh atomicity."
]

};
