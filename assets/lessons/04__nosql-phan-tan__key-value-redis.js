window.LESSONS["04:nosql-phan-tan:key-value-redis"] = {

tom_tat: [
  "Redis là data structure server trong bộ nhớ: key trỏ tới string, hash, set, sorted set, stream và các cấu trúc khác; chọn đúng cấu trúc quan trọng hơn thuộc lòng lệnh.",
  "Tốc độ thấp-latency đến từ dữ liệu trong RAM và đường thực thi lệnh gọn, không biến mạng, serialization hay lệnh O(n) thành miễn phí.",
  "TTL, eviction và persistence là ba cơ chế khác nhau: hết hạn theo nghiệp vụ, giải phóng khi thiếu RAM, và phục hồi sau restart.",
  "Atomic command, transaction và Lua giải quyết các phạm vi cạnh tranh khác nhau; chuỗi GET rồi SET không tự nguyên tử.",
  "Redis phù hợp cho cache, phiên, rate limit và dữ liệu tạm; số dư hay đơn hàng chuẩn vẫn cần PostgreSQL làm nguồn sự thật."
],

can_biet_truoc: [
  "Hash table, set, queue và priority queue ở mức cấu trúc dữ liệu.",
  "Transaction, isolation và unique constraint trong PostgreSQL.",
  "Cache hit, cache miss, latency mạng và serialization.",
  "Replication, failover và khái niệm eventual consistency."
],

dinh_nghia: "**Redis** là máy chủ dữ liệu key–value chạy chủ yếu trên RAM, nhưng value không chỉ là blob: nó có kiểu và lệnh phía server cho string, hash, list, set, sorted set, stream, bitmap cùng nhiều dạng khác. Mỗi lệnh đơn lẻ được thực thi nguyên tử đối với trạng thái Redis; client khác không chen giữa phần đọc và ghi nội bộ của chính lệnh đó.\n\nTrong Chợ Mây, Redis không sở hữu catalog hay đơn hàng. Nó giữ giỏ hàng có TTL, phiên đăng nhập, cache trang sản phẩm và bộ đếm rate limit. Cách định vị này quan trọng: mất cache làm hệ chậm, còn mất nguồn sự thật làm nghiệp vụ sai. Redis có persistence, nhưng “có thể ghi xuống đĩa” không tự biến nó thành lựa chọn đúng cho mọi dữ liệu bền vững.",

vi_sao_can: "Một trang sản phẩm nổi tiếng có thể được đọc hàng nghìn lần trong khi giá chỉ đổi vài lần. Nếu mỗi request đều parse query, đi qua connection pool và đọc PostgreSQL, hệ trả cùng một kết quả bằng công việc lặp lại. Cache gần ứng dụng giảm tải và latency. Phiên đăng nhập, mã OTP, cửa sổ rate limit còn có vòng đời ngắn tự nhiên; TTL phía server phù hợp hơn cron dọn hàng hết hạn.\n\nNhưng đưa Redis vào tạo hai bản của dữ liệu. Cache có thể cũ, key có thể bị đuổi, failover có thể mất ghi chưa replicate, và một hot key có thể dồn tải vào một shard. Vì vậy lý do dùng phải là yêu cầu latency hoặc throughput đã quan sát, không phải khẩu hiệu “Redis rất nhanh”. Chợ Mây thiết kế mọi cache miss quay về PostgreSQL và mọi thao tác tiền tệ vẫn dựa vào transaction cùng constraint của PostgreSQL.",

co_che: "### Key chỉ có nghĩa khi có quy ước\n\nKey `product:P100:v7` chứa cả namespace, định danh và revision. Revision làm invalidation đơn giản: khi catalog tăng revision, request mới đọc key mới; key cũ tự hết hạn. Key `cart:u42` lại đại diện một giỏ theo người dùng. Quy ước phải nêu ownership, kiểu value, TTL và dữ liệu nhạy cảm. Nếu hai service cùng ghi một key mà không có hợp đồng, lỗi sẽ chỉ xuất hiện khi deploy lệch phiên bản.\n\nKey dài tốn RAM trên mỗi entry, còn key quá ngắn gây va chạm nghĩa. Không đưa token bí mật hay email thô vào key vì key xuất hiện trong log và công cụ quan sát. Với Redis Cluster, phần trong `{...}` là hash tag để ép các key vào cùng slot; dùng nó bừa bãi tạo hot shard.\n\n### Chọn cấu trúc theo phép toán cần làm\n\nString phù hợp cache JSON hoặc counter; hash cho các field giỏ hàng; set cho tập membership; sorted set cho leaderboard và lịch theo điểm; stream cho log sự kiện có consumer group. Một JSON blob dễ bắt đầu nhưng sửa một quantity buộc đọc, parse, sửa rồi ghi cả blob và dễ lost update. Hash cho phép `HINCRBY cart:u42 P100 1` nguyên tử.\n\n```text\nSET product:P100:v7 \"{...}\" EX 300\nHSET cart:u42 P100 2 P101 1\nHINCRBY cart:u42 P100 1\nEXPIRE cart:u42 1209600\nZADD flash:ends 1756717200 deal-77\nZRANGEBYSCORE flash:ends -inf 1756717200\n```\n\nĐộ phức tạp vẫn tồn tại. `GET` gần O(1), thao tác sorted set thường O(log n), còn `KEYS *` quét toàn keyspace. “Redis một luồng nên mọi thứ nhanh” là suy luận sai: một lệnh dài chặn lệnh khác trên cùng execution path.\n\n### Cache-aside và khoảng trống nhất quán\n\nỞ cache-aside, ứng dụng đọc Redis; miss thì đọc PostgreSQL, ghi cache rồi trả kết quả. Khi cập nhật, transaction ghi PostgreSQL trước và xóa key cache sau. Nếu xóa trước rồi transaction chậm, request khác có thể nạp giá cũ trở lại. Nếu database commit xong nhưng process chết trước khi xóa, cache cũ sống tới TTL.\n\nKhông có thứ tự hai lệnh ở hai hệ độc lập nào tự tạo atomicity. Chợ Mây dùng TTL ngắn cho giá, outbox trong cùng transaction PostgreSQL để phát sự kiện invalidation, và chấp nhận cửa sổ stale được định nghĩa. Với dữ liệu không được phép cũ như hạn mức thanh toán, request đọc nguồn sự thật thay vì cache.\n\n### TTL không phải lời hứa xóa đúng mili giây\n\nRedis kiểm tra hết hạn khi key được truy cập và quét chủ động theo chu kỳ. Key quá hạn về logic không còn đọc được, nhưng bộ nhớ có thể chưa được thu hồi đúng thời điểm đồng hồ chạm mốc. TTL cũng có thể bị mất nếu lệnh ghi đè key mà không giữ expiry theo cách phù hợp. Sau mỗi đường ghi giỏ hàng, test nên kiểm `TTL cart:u42` chứ không chỉ kiểm value.\n\nTTL nghiệp vụ khác eviction. TTL nói dữ liệu không còn hợp lệ sau một thời điểm; eviction xảy ra khi chạm `maxmemory`. Policy như LRU xấp xỉ chọn nạn nhân theo truy cập, nhưng không bảo đảm hoàn hảo. Nếu cache và dữ liệu không thể tái tạo dùng chung instance với eviction, áp lực RAM có thể xóa thứ bạn tưởng bền.\n\n### Cache stampede và khóa một chuyến nạp\n\nKhi key nổi tiếng hết hạn, hàng nghìn request cùng miss rồi đánh PostgreSQL. Đây là stampede. Có thể thêm jitter vào TTL để nhiều key không hết hạn cùng giây, phục vụ stale-while-revalidate, hoặc dùng lock ngắn bằng `SET lock:key token NX PX 3000`. Chỉ chủ sở hữu token mới được xóa lock; `DEL` mù có thể xóa lock của request kế tiếp sau khi lock cũ hết hạn.\n\nLock cache không thay transaction nghiệp vụ. Timeout khiến client không biết lệnh đã chạy hay chưa; lock còn phụ thuộc clock, lease và failover. Với constraint duy nhất, PostgreSQL `UNIQUE` thường là trọng tài chắc chắn hơn distributed lock tự chế.\n\n### Atomicity: một lệnh, MULTI và Lua\n\n`INCR rate:u42:minute` nguyên tử, còn `GET`, tính trong app rồi `SET` không nguyên tử. `MULTI/EXEC` xếp lệnh và chạy liền nhau, nhưng không rollback như transaction SQL nếu một lỗi kiểu dữ liệu xảy ra giữa batch. `WATCH` cung cấp optimistic concurrency: nếu key đổi trước `EXEC`, transaction bị hủy và client retry.\n\nLua chạy logic phía server mà không bị lệnh khác chen giữa, thích hợp rate limit cần tăng và đặt TTL cùng nhau:\n\n```lua\nlocal n = redis.call('INCR', KEYS[1])\nif n == 1 then redis.call('EXPIRE', KEYS[1], ARGV[1]) end\nif n > tonumber(ARGV[2]) then return {0, n} end\nreturn {1, n}\n```\n\nScript phải ngắn; vòng lặp lớn chặn server. Redis Functions hoặc script không làm cho thao tác chạm hệ khác trở thành atomic.\n\n### Persistence: RDB và AOF trả lời câu hỏi khác\n\nRDB chụp snapshot theo thời điểm, phục hồi nhanh và file gọn, nhưng crash giữa hai snapshot có thể mất thay đổi gần nhất. AOF ghi lại lệnh biến đổi và có chính sách flush khác nhau; bền hơn theo cấu hình nhưng tốn I/O và cần rewrite. Có thể dùng cả hai, song vẫn phải thử restore. File tồn tại không chứng minh backup phục hồi được.\n\nĐối với cache sản phẩm, mất toàn bộ Redis chỉ gây cold cache; Chợ Mây có thể không cần persistence. Với giỏ hàng, nghiệp vụ có thể muốn AOF và bản sao, nhưng vẫn phải công bố khả năng mất vài giây. Nếu yêu cầu tuyệt đối không mất, chọn nguồn bền vững khác và coi Redis là projection.\n\n### Replication và failover có cửa sổ mất dữ liệu\n\nReplica sao chép bất đồng bộ. Primary có thể xác nhận ghi rồi hỏng trước khi replica nhận; replica được nâng cấp sẽ thiếu ghi đó. Lệnh `WAIT` đợi số replica xác nhận nhưng không biến hệ thành linearizable storage dưới mọi partition. Đọc replica có thể cũ.\n\nSentinel giúp giám sát và failover cho mô hình không cluster; Redis Cluster phân mảnh keyspace thành slot và có replica cho từng master. Client phải hiểu redirect và topology. High availability giảm thời gian ngừng, không tự nâng durability hay consistency đến mức ứng dụng tưởng tượng.\n\n### Cluster, multi-key và hot key\n\nCluster định tuyến key theo hash slot. Multi-key command chỉ thuận lợi khi các key cùng slot. Hash tag `{u42}` cho `cart:{u42}` và `coupon:{u42}` cùng chỗ, nhưng gom mọi user vào `{cart}` sẽ phá phân phối. Một sản phẩm viral vẫn là hot key dù hash đều, bởi một key chỉ nằm trên một primary; local cache, read replica phù hợp hoặc chia tải ở tầng ứng dụng mới xử lý được.\n\nPipelining gửi nhiều lệnh mà không chờ từng phản hồi, giảm round trip nhưng không làm batch nguyên tử. Transaction tăng tính liền mạch, pipeline tăng hiệu quả mạng; nhầm hai khái niệm tạo bug cạnh tranh.\n\n### Bộ nhớ không chỉ bằng độ dài value\n\nMỗi key, object, con trỏ và allocator có overhead. Một triệu hash mỗi hash một field thường tốn hơn một hash chứa nhiều field, dù payload giống nhau. Ziplist/listpack encoding tối ưu cấu trúc nhỏ nhưng có ngưỡng chuyển representation. Đừng dự toán chỉ bằng kích thước JSON.\n\nTheo dõi `used_memory`, fragmentation ratio, hit rate, evicted keys, expired keys và latency percentile. Hit rate cao vẫn có thể vô ích nếu miss đúng hot path hoặc serialization chiếm phần lớn latency.\n\n### Ca biên của giỏ hàng và rate limit\n\nQuantity giảm về zero nên xóa field hay giữ zero phải thống nhất. User chưa đăng nhập cần key tạm và quy trình merge idempotent khi đăng nhập. Retry `HINCRBY` sau timeout có thể tăng hai lần; gửi giá trị đích kèm version hoặc operation id khi phép toán không được lặp.\n\nRate limit fixed window cho phép burst ở ranh giới hai phút. Sliding window chính xác hơn nhưng tốn dữ liệu; token bucket cân bằng burst và tốc độ. Clock server nên là nguồn thời gian chung. Empty hash có thể bị xóa, vì vậy code không được dựa vào sự tồn tại của container rỗng.",

vi_du: "### Nguồn sự thật và thao tác idempotent trong PostgreSQL\n\nSQL sau chạy trên PostgreSQL, mô phỏng giỏ bền vững để thấy Redis projection khác nguồn sự thật:\n\n```sql\nCREATE TABLE carts (\n  user_id bigint NOT NULL,\n  sku text NOT NULL,\n  quantity integer NOT NULL CHECK (quantity > 0),\n  version bigint NOT NULL DEFAULT 1,\n  PRIMARY KEY (user_id, sku)\n);\n\nCREATE TABLE cart_operations (\n  operation_id uuid PRIMARY KEY,\n  user_id bigint NOT NULL,\n  created_at timestamptz NOT NULL DEFAULT now()\n);\n\nBEGIN;\nINSERT INTO cart_operations(operation_id, user_id)\nVALUES ('11111111-1111-1111-1111-111111111111', 42)\nON CONFLICT DO NOTHING;\n\nINSERT INTO carts(user_id, sku, quantity)\nSELECT 42, 'P100', 1\nWHERE EXISTS (\n  SELECT 1 FROM cart_operations\n  WHERE operation_id='11111111-1111-1111-1111-111111111111'\n    AND user_id=42\n)\nON CONFLICT (user_id, sku) DO UPDATE\nSET quantity = carts.quantity + 1, version = carts.version + 1;\nCOMMIT;\n```\n\nTrong hệ thật cần phân biệt operation vừa insert với operation đã tồn tại, chẳng hạn dùng CTE `INSERT ... RETURNING`; mục tiêu ở đây là chỉ ra unique index trên `operation_id` làm trọng tài retry. Index primary key hỗ trợ lookup giỏ theo `(user_id, sku)`. Nếu thường tải toàn giỏ theo `user_id`, đoạn đầu của index vẫn dùng được; `EXPLAIN (ANALYZE, BUFFERS)` xác nhận plan trên dữ liệu đại diện.\n\nCache-aside ở ứng dụng có thể viết giả mã:\n\n```javascript\nasync function getProduct(sku, revision) {\n  const key = `product:${sku}:v${revision}`;\n  const hit = await redis.get(key);\n  if (hit !== null) return JSON.parse(hit);\n  const row = await postgres.query('SELECT * FROM product_catalog WHERE sku=$1', [sku]);\n  if (!row) return null;\n  await redis.set(key, JSON.stringify(row), { EX: 300 + Math.floor(Math.random()*30) });\n  return row;\n}\n```\n\nParameter SQL tránh injection; TTL jitter giảm hết hạn đồng loạt. Vẫn có thể nạp trùng khi miss đồng thời, nên chỉ thêm single-flight sau khi đo tải cho thấy cần.",

so_sanh: "### Redis, cache trong process và PostgreSQL\n\n| Tiêu chí | Redis | Cache trong process | PostgreSQL |\n|---|---|---|---|\n| Chia sẻ giữa instance | Có | Không | Có |\n| Latency mạng | Có một round trip | Không | Có, thường nhiều xử lý hơn |\n| Dung lượng | Bị giới hạn bởi RAM cluster | Bị giới hạn mỗi process | Đĩa và buffer cache |\n| TTL/eviction | Tích hợp | Thư viện tự quản | Có thể mô hình hóa, không phải trọng tâm |\n| Constraint quan hệ | Rất hạn chế | Không | Mạnh |\n| Durability | Tùy RDB/AOF/replica | Mất khi process chết | WAL, backup và transaction |\n| Atomic operation | Trên key/cấu trúc, Lua | Trong phạm vi process | Transaction nhiều row/table |\n\nCache trong process tốt cho dữ liệu nhỏ, đọc nhiều và chấp nhận mỗi instance khác nhau; nó loại một round trip. Redis hữu ích khi cần trạng thái tạm dùng chung và phép toán phía server. PostgreSQL đúng hơn khi dữ liệu mang bất biến nghiệp vụ, cần audit hoặc join. Không nên đưa Redis vào hệ nhỏ chỉ để cache query rẻ: invalidation, vận hành và một dependency mạng mới có thể đắt hơn thời gian tiết kiệm.",

loi_thuong_gap: [
  "**Dùng `KEYS *` trên production.** Latency toàn instance tăng khi keyspace lớn; dùng `SCAN` có cursor cho công việc quản trị và thiết kế namespace để không cần quét.",
  "**Quên TTL khi cập nhật giỏ.** `TTL` trả `-1` và key sống mãi; gói cập nhật với đặt hạn trong Lua hoặc test mọi đường ghi.",
  "**Xóa distributed lock không kiểm token.** Request chậm xóa lease mới của request khác; so token và xóa nguyên tử bằng script.",
  "**Coi MULTI như transaction SQL.** Batch gặp lỗi không rollback trạng thái trước đó; thiết kế lệnh đúng kiểu và dùng Lua ngắn khi cần điều kiện nguyên tử.",
  "**Tin persistence đồng nghĩa không mất dữ liệu.** Chưa từng diễn tập restore và không định nghĩa RPO; kiểm cấu hình flush, replica và phục hồi thật.",
  "**Cache object không có version.** Deploy mới đọc payload cũ sai schema; version hóa key hoặc decoder tương thích ngược.",
  "**Một hot key làm nghẽn shard.** CPU/network lệch mạnh dù tổng cluster còn rảnh; dùng local cache, phân rã hợp lý hoặc thay đổi đường đọc."
],

tu_kiem_tra: [
  "Vì sao hash phù hợp hơn JSON string cho thao tác tăng quantity giỏ hàng cạnh tranh?",
  "Phân biệt TTL, eviction và persistence bằng ba sự cố riêng của Chợ Mây.",
  "Giải thích cửa sổ cache cũ khi PostgreSQL commit thành công nhưng process chết trước lúc xóa Redis.",
  "Tại sao pipeline không cung cấp atomicity, còn Lua quá dài lại gây vấn đề khác?",
  "Một ghi đã được primary Redis xác nhận có thể biến mất sau failover bằng chuỗi sự kiện nào?",
  "Thiết kế rate limiter cho phép burst nhỏ nhưng giới hạn tốc độ dài hạn; chọn cấu trúc và nêu đánh đổi.",
  "Khi hit rate 98% vẫn không cải thiện p99 latency, bạn sẽ đo những thành phần nào trước?"
],

tai_nguyen: [
  "Redis Documentation — Data Types, Expiration, Persistence và Cluster.",
  "Designing Data-Intensive Applications — Martin Kleppmann.",
  "Redis in Action — Josiah L. Carlson.",
  "PostgreSQL Documentation — Transactions, Constraints và EXPLAIN."
],

lien_he: [
  "Nối sang **mảng 01 · Cấu trúc dữ liệu**: hash, set và priority queue giải thích độ phức tạp khác nhau của từng lệnh Redis.",
  "Nối sang **mảng 01 · Mạng máy tính**: pipelining tối ưu round trip, còn timeout tạo trạng thái không chắc thao tác đã thực thi.",
  "Nối sang **mảng 05 · Thiết kế hệ thống**: cache-aside, outbox, idempotency và backpressure quyết định Redis hỗ trợ hay phá tính đúng.",
  "Nối sang **mảng 06 · Bảo mật**: session store cần TTL, rotation, phân quyền command và không để secret rò qua key/log."
]

};
