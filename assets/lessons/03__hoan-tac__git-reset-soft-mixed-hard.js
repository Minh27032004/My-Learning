window.LESSONS["03:hoan-tac:git-reset-soft-mixed-hard"] = {

tom_tat: [
  "`git reset <mode> <commit>` trước hết di chuyển nhánh hiện tại tới commit đích, rồi tùy mode mà đồng bộ index và working tree; ba mode khác nhau ở số vùng bị viết lại.",
  "`--soft` chỉ đổi ref nên toàn bộ chênh lệch được giữ trong staging; `--mixed` còn đặt index theo commit đích nên thay đổi nằm ở working tree; `--hard` đặt cả ba vùng theo đích và có thể xóa sửa đổi chưa commit.",
  "Reset theo commit khác reset theo đường dẫn: dạng có pathspec không di chuyển `HEAD`, mà chủ yếu cập nhật index cho những đường dẫn được chọn.",
  "Reset phù hợp để tổ chức lại lịch sử cục bộ chưa chia sẻ. Với commit đã push trên nhánh chung, di chuyển ref ngược tạo lịch sử phân kỳ và thường không phải cách hoàn tác đúng.",
  "Reflog thường cứu được commit bị bỏ khỏi nhánh, nhưng không bảo đảm cứu nội dung working tree bị `--hard` ghi đè nếu nội dung ấy chưa từng thành Git object."
],

can_biet_truoc: [
  "Hiểu `HEAD`, tên nhánh và commit graph: ref nhánh chứa object ID của commit đầu nhánh.",
  "Hiểu index là snapshot chuẩn bị cho commit kế, còn working tree là nội dung file trên filesystem.",
  "Biết đọc `git status`, `git diff`, `git diff --staged` và `git log --oneline --graph` trước thao tác phá hủy.",
  "Biết reflog ghi lịch sử cập nhật ref cục bộ, không phải lịch sử mọi lần sửa file."
],

dinh_nghia: "`git reset` đặt lại một hoặc nhiều thành phần trạng thái của repository theo một nguồn. Với dạng `git reset [--soft|--mixed|--hard] <commit>`, lệnh di chuyển ref hiện tại tới commit đích; sau đó `--mixed` còn chép tree đích vào index, còn `--hard` tiếp tục cập nhật working tree để khớp index.\n\nNói “reset xóa commit” không chính xác. Nó làm nhánh ngừng trỏ qua các commit ở đoạn bị lùi; object thường còn trong database và reflog một thời gian. Điều có thể mất ngay là nội dung chưa commit bị working tree ghi đè bởi mode mạnh.",

vi_sao_can: "Trong nhánh riêng của Sao Mai, lập trình viên tạo ba commit: `A` khởi tạo giỏ hàng, `B` thêm giảm giá, `C` thêm log debug và vô tình kèm cấu hình sandbox. Trước khi push, họ muốn dựng lại `B` và `C` thành các commit có ranh giới đúng. Có lúc cần giữ snapshot của hai commit trong staging để commit lại ngay; có lúc muốn đưa mọi sửa đổi về file để chọn từng hunk; đôi khi nhánh thử nghiệm hoàn toàn vô giá trị và cần trả cả thư mục về `A`.\n\nMột lệnh chỉ “lùi lịch sử” không đủ diễn đạt ba ý định ấy. Sau khi nhánh lùi, nội dung của đoạn lịch sử bị bỏ phải đi đâu: nằm trong index, chỉ nằm trên đĩa, hay bị ghi đè? Ba mode của reset là ba câu trả lời.\n\nReset cũng cần cho thao tác index theo đường dẫn, nhưng sự quá tải cú pháp là nguồn nhầm lẫn. Người học thấy `git reset HEAD file` an toàn để gỡ staging rồi suy rộng rằng mọi reset đều an toàn. Thực tế, thêm `--hard` và bỏ pathspec biến nó thành thao tác rộng trên cả nhánh lẫn filesystem. Bài này tách rõ hai họ cú pháp trước khi bàn cách dùng.",

co_che: "### Một lệnh, ba pha có thứ tự\n\nVới dạng theo commit, có thể suy luận reset như chuỗi tối đa ba pha:\n\n1. **Di chuyển ref:** nhánh hiện tại trỏ tới commit đích.\n2. **Đặt lại index:** tree của commit đích được chép vào staging area.\n3. **Đặt lại working tree:** file trên đĩa được sửa để khớp index.\n\n`--soft` dừng sau pha 1. `--mixed` dừng sau pha 2. `--hard` thực hiện cả ba. Thứ tự này quan trọng vì nó cho phép dự đoán diff sau lệnh, thay vì học thuộc ba từ mơ hồ.\n\n| Mode | Ref/`HEAD` | Index | Working tree |\n|---|---|---|---|\n| `--soft` | chuyển tới đích | giữ nguyên | giữ nguyên |\n| `--mixed` | chuyển tới đích | chép từ đích | giữ nguyên |\n| `--hard` | chuyển tới đích | chép từ đích | chép từ đích |\n\nMode mặc định là `--mixed`. Không viết mode không làm lệnh “nhẹ”; nó vẫn di chuyển nhánh và đặt lại index.\n\n### Trạng thái Sao Mai trước reset\n\nGiả sử graph tuyến tính:\n\n```text\nA -- B -- C  (feature/cart, HEAD)\n```\n\n`A` có giỏ hàng cơ bản. `B` thêm giảm giá 10%. `C` thêm log và cấu hình sandbox. Ngay sau commit C, index và working tree thường cùng snapshot C. Khi reset về A, sự khác nhau giữa A và C gồm tất cả thay đổi của B+C; mode quyết định chênh lệch ấy xuất hiện ở đâu.\n\nĐể quan sát, ghi lại đầu nhánh và xem phạm vi sắp bỏ:\n\n```bash\ngit status --short\ngit log --oneline --decorate -3\ngit diff --stat HEAD~2..HEAD\nold_tip=$(git rev-parse HEAD)\n```\n\nBiến `old_tip` là dây an toàn trong phiên shell. Reflog cũng ghi cập nhật, nhưng ID rõ ràng tránh phụ thuộc vị trí `HEAD@{n}` thay đổi theo hoạt động tiếp theo.\n\n### `--soft`: lùi ref, giữ snapshot đang có\n\n```bash\ngit reset --soft HEAD~2\n```\n\nNhánh chuyển từ C về A. Index vẫn mang snapshot C, working tree cũng vẫn là C. Vì `git diff --staged` so index C với `HEAD` A, toàn bộ thay đổi B+C hiện là staged. `git diff` rỗng nếu trước lệnh hai vùng này giống nhau.\n\nMode này phù hợp khi muốn gộp vài commit cục bộ rồi commit lại, hoặc sửa ranh giới lịch sử mà snapshot cuối đã đúng. Ví dụ:\n\n```bash\ngit reset --soft HEAD~2\ngit diff --staged\ngit commit -m \"Add cart discount and checkout configuration\"\n```\n\nĐánh đổi là staging có thể rất lớn và trộn nhiều mục tiêu. Soft reset không tự làm commit tốt hơn; nó chỉ gom snapshot. Nếu cần chia lại theo hunk, có thể tiếp tục gỡ staging có chọn lọc hoặc chọn `--mixed` ngay từ đầu.\n\n### `--mixed`: lùi ref, đưa thay đổi về working tree\n\n```bash\ngit reset --mixed HEAD~2\n```\n\nNhánh và index cùng A, working tree vẫn C. Vì vậy `git diff --staged` rỗng, còn `git diff` hiện thay đổi B+C. Đây là mode mặc định:\n\n```bash\ngit reset HEAD~2\n```\n\nMixed reset hữu ích khi muốn “tháo” các commit riêng tư thành sửa đổi chưa staging để chọn lại bằng `git add --patch`. Nó giữ nội dung file nhưng làm mất trạng thái staged trước đó. Nếu index từng được chọn hunk rất công phu, chi tiết lựa chọn ấy không còn; chỉ nội dung working tree được giữ.\n\nSau reset, file mới xuất hiện trong C nhưng không có ở A thường trở thành untracked. Đây là ca biên quan trọng: nội dung còn trên đĩa, song nó không xuất hiện trong `git diff` mặc định vì diff bỏ qua untracked. Phải đọc `git status --short`, không chỉ đọc diff.\n\n### `--hard`: cả ba vùng khớp commit đích\n\n```bash\ngit reset --hard HEAD~2\n```\n\nNhánh, index và working tree đều trở về A. File tracked được sửa để khớp tree A; thay đổi tracked chưa commit có thể bị ghi đè. Các commit B và C không còn reachable (có thể đi tới từ ref) qua nhánh này, nhưng thường vẫn hiện trong reflog. File untracked không nhất thiết bị xóa, trừ trường hợp nó cản Git ghi một tracked path từ commit đích; không được coi reset hard là công cụ dọn mọi file rác.\n\nĐây là mode cần hàng rào kiểm tra mạnh nhất:\n\n```bash\ngit status --short\ngit diff\ngit diff --staged\ngit branch rescue-before-hard HEAD\ngit reset --hard <verified-commit-id>\n```\n\nNhánh cứu hộ giữ commit, nhưng không giữ sửa đổi chưa commit. Nếu diff có giá trị, tạo patch hoặc commit trên nhánh tạm trước. Không có cờ “undo chắc chắn” cho bytes chưa từng được Git lưu.\n\n### Tính đích theo tên tương đối và rủi ro lệch mốc\n\n`HEAD~2` nghĩa là đi theo parent thứ nhất hai lần. Trong lịch sử merge, ký hiệu này không đồng nghĩa “hai commit hiển thị phía trên” theo mọi cách sắp xếp log. `HEAD^2` lại chọn parent thứ hai của merge commit. Với thao tác phá hủy, nên phân giải và xem commit trước:\n\n```bash\ntarget=$(git rev-parse HEAD~2)\ngit show --no-patch --decorate --oneline \"$target\"\ngit diff --stat \"$target\"..HEAD\n```\n\nSau khi xác minh, dùng ID hoặc biến đã cố định. Nếu nhánh có tiến triển giữa lúc xem và lúc chạy do hook hay automation, ID cố định giảm khả năng reset sai mốc so với biểu thức tương đối được tính lại.\n\n### Reset theo đường dẫn là họ lệnh khác\n\n```bash\ngit reset HEAD -- src/cart.js\n```\n\nKhi có pathspec, Git không di chuyển ref nhánh. Nó chép entry của `cart.js` từ tree nguồn vào index, gần với `git restore --staged --source=HEAD -- src/cart.js`. Working tree giữ nguyên. Vì vậy lệnh thường dùng để gỡ staging và tương đối hẹp.\n\nKhông thể suy luận bảng mode theo commit một cách máy móc cho mọi dạng pathspec. Cú pháp `git reset [<tree-ish>] [--] <paths>` và cú pháp `git reset [<mode>] [<commit>]` có mục đích khác nhau. `--soft` không đi cùng pathspec theo nghĩa “soft cho một file”, vì ref là trạng thái toàn repository chứ không thể di chuyển riêng từng đường dẫn.\n\nNgày nay `git restore --staged` truyền đạt ý định gỡ staging rõ hơn. Tuy vậy reset theo path vẫn phổ biến trong script và tài liệu, nên cần hiểu cơ chế thay index.\n\n### `ORIG_HEAD`, reflog và đường quay lại\n\nGit thường lưu đầu nhánh trước một số thao tác nguy hiểm trong `ORIG_HEAD`, và reflog ghi các cập nhật ref. Sau reset nhầm, quy trình đọc trước là:\n\n```bash\ngit reflog --date=iso\ngit show --no-patch --oneline <candidate-id>\ngit branch rescue-reset <candidate-id>\n```\n\nChỉ sau khi xác nhận candidate mới quyết định reset lại hay cherry-pick. Tạo branch cứu hộ là thao tác không làm mất vị trí hiện tại và bảo vệ object khỏi trở thành không reachable.\n\nKhông hứa `HEAD@{1}` luôn là đích đúng. Một checkout, commit hoặc reset khác sau sự cố làm số thứ tự dịch chuyển. Reflog là cục bộ: clone khác và remote không tự có nhật ký máy bạn. Nó cũng có chính sách hết hạn; bài riêng về reflog sẽ phân tích sâu hơn.\n\n### Reset trên nhánh đã push\n\nRemote có `A--B--C`, local reset về A. Push bình thường bị từ chối vì cập nhật remote từ C về A không phải fast-forward. Force-push có thể làm remote bỏ B,C khỏi ref, nhưng người khác vẫn có commit ấy và có thể đã xây D trên C. Họ sẽ gặp lịch sử phân kỳ.\n\nNếu mục tiêu là sửa tác động C trên nhánh chung, `git revert C` tạo commit D có bản vá nghịch đảo, giữ A-B-C-D trong ancestry. Nếu B và C chứa dữ liệu nhạy cảm, revert **không xóa dữ liệu khỏi lịch sử**; đó là sự cố khác, cần quy trình thu hồi secret và viết lại lịch sử có phối hợp. Không được dùng reset như cách giả vờ secret chưa từng xuất hiện.\n\nReset sau push chỉ hợp lý khi nhánh được quy ước là lịch sử có thể viết lại, mọi người đã phối hợp và cập nhật remote bằng cơ chế có kiểm tra như `--force-with-lease`. Ngay cả vậy, phải xem commit sắp đẩy và trạng thái remote trước.\n\n### Quan hệ với merge và rebase đang dang dở\n\n`git reset --hard HEAD` đôi khi được dùng để bỏ thay đổi tracked, nhưng không nên thay thế lệnh hủy chuyên biệt mà không đọc trạng thái. Trong merge hoặc rebase, Git giữ metadata ngoài ba cây để biết tiến trình. `git merge --abort` hoặc `git rebase --abort` diễn đạt mục tiêu và cố phục hồi trạng thái trước quy trình.\n\nReset hard có thể làm index và file sạch nhưng để người dùng hiểu sai về việc nào đã được hủy, đặc biệt khi có autostash hay thay đổi trước merge. `git status` thường chỉ rõ lệnh phù hợp. Mô hình ba cây là nền, nhưng sequencer và trạng thái merge bổ sung lớp state khác.\n\n### Ca biên: unborn branch, detached HEAD và submodule\n\nRepository chưa có initial commit có `HEAD` tượng trưng nhưng không có commit để làm nguồn như `HEAD~1`; reset theo commit sẽ không hoạt động như lịch sử thông thường. Ở detached HEAD, reset di chuyển `HEAD` trực tiếp thay vì tên nhánh, nên commit mới có nguy cơ không được ref giữ khi chuyển nơi khác.\n\nSubmodule được index của repository cha lưu như một gitlink trỏ commit, không phải toàn bộ file bên trong. Reset repository cha thay gitlink trong index; việc cập nhật working tree của submodule còn phụ thuộc tùy chọn và lệnh submodule. Đừng chạy hard reset đệ quy nếu chưa kiểm tra từng repository lồng nhau.\n\n### Khi nào không nên dùng\n\nKhông dùng reset để hoàn tác một commit đã công bố khi yêu cầu là giữ lịch sử cộng tác tuyến tính; dùng revert. Không dùng hard reset chỉ vì build lỗi nếu chưa biết file nào thay đổi; restore theo đường dẫn hoặc sửa có chọn lọc có phạm vi nhỏ hơn.\n\nKhông dùng soft reset để “squash” hàng chục commit nếu cần bảo toàn tác giả, message hoặc cấu trúc merge. Interactive rebase cho quyền kiểm soát chi tiết hơn, nhưng cũng viết lại lịch sử. Và không dùng bất kỳ mode nào trước khi xác định commit đích bằng `show` cùng phạm vi diff: lệnh đúng mode nhưng sai đích vẫn gây hậu quả lớn.",

vi_du: "### Chạy ba mode trên ba bản sao độc lập\n\nScript Bash sau tạo một repository nguồn rồi clone cục bộ ba lần để kết quả không ảnh hưởng lẫn nhau:\n\n```bash\nlab=\"$(mktemp -d)\"\ncd \"$lab\"\ngit init -q source\ncd source\ngit config user.name \"Lesson User\"\ngit config user.email \"lesson@example.invalid\"\nmkdir -p src config\nprintf '%s\\n' 'cart=v1' > src/cart.txt\ngit add src/cart.txt && git commit -qm \"A: initialize cart\"\nprintf '%s\\n' 'discount=10' >> src/cart.txt\ngit add src/cart.txt && git commit -qm \"B: add discount\"\nprintf '%s\\n' 'gateway=sandbox' > config/payment.conf\ngit add config/payment.conf && git commit -qm \"C: add sandbox config\"\ntarget=$(git rev-parse HEAD~2)\ncd ..\ngit clone -q source soft\ngit clone -q source mixed\ngit clone -q source hard\n```\n\nKiểm tra soft:\n\n```bash\ncd \"$lab/soft\"\ngit reset --soft \"$target\"\ntest \"$(git rev-parse HEAD)\" = \"$target\"\ntest -z \"$(git diff --name-only)\"\ntest -n \"$(git diff --staged --name-only)\"\ngit status --short\n```\n\nKiểm tra mixed:\n\n```bash\ncd \"$lab/mixed\"\ngit reset --mixed \"$target\"\ntest \"$(git rev-parse HEAD)\" = \"$target\"\ntest -z \"$(git diff --staged --name-only)\"\ntest -n \"$(git diff --name-only)$(git ls-files --others --exclude-standard)\"\ngit status --short\n```\n\n`config/payment.conf` không có ở A nên trở thành untracked; biểu thức kiểm tra ghép cả diff tracked lẫn danh sách untracked. Chỉ kiểm tra `git diff` sẽ bỏ sót file này.\n\nKiểm tra hard và reflog:\n\n```bash\ncd \"$lab/hard\"\nold_tip=$(git rev-parse HEAD)\nprintf '%s\\n' 'uncommitted edit' >> src/cart.txt\ngit reset --hard \"$target\"\ntest \"$(git rev-parse HEAD)\" = \"$target\"\ntest -z \"$(git status --short)\"\ngit cat-file -e \"$old_tip^{commit}\"\ngit reflog -2 --oneline\n```\n\nSửa đổi `uncommitted edit` biến mất; commit C vẫn đọc được qua `old_tip`. Hai sự kiện này chứng minh ranh giới cứu hộ: reflog/object database bảo vệ commit tốt hơn bytes chưa commit.\n\n### Đối chiếu reset theo path\n\n```bash\ncd \"$lab/source\"\nprintf '%s\\n' 'debug=true' >> src/cart.txt\ngit add src/cart.txt\nbefore=$(git rev-parse HEAD)\ngit reset HEAD -- src/cart.txt\nafter=$(git rev-parse HEAD)\ntest \"$before\" = \"$after\"\ntest -z \"$(git diff --staged --name-only)\"\ntest -n \"$(git diff --name-only)\"\n```\n\nRef không đổi, staged diff rỗng, working-tree diff còn. Đây là reset index theo đường dẫn, không phải mode mixed di chuyển lịch sử.",

so_sanh: "### Bảng chọn mode theo nơi muốn giữ thay đổi\n\n| Sau khi lùi commit, muốn thay đổi cũ ở đâu? | Mode | Bước kế tiếp thường dùng |\n|---|---|---|\n| Đã staging, sẵn sàng commit lại | `--soft` | xem staged diff, commit |\n| Trên đĩa, chưa staging để chia lại | `--mixed` | `git add --patch` |\n| Không giữ thay đổi tracked | `--hard` | kiểm tra và chạy test |\n\n### Reset so với restore và revert\n\n| Công cụ | Trọng tâm | Di chuyển nhánh | Viết working tree | Hợp với lịch sử chung |\n|---|---|---:|---:|---:|\n| `restore` | đường dẫn/index | Không | Tùy chọn | Không sửa lịch sử |\n| `reset <commit>` | ref và ba cây | Có | Chỉ hard | Thường không khi lùi |\n| `revert <commit>` | bản vá nghịch đảo | Không lùi | Có để tạo commit | Có |\n\n### Phạm vi nhỏ là mặc định tốt\n\nNếu chỉ một file sai, restore có phạm vi nhỏ hơn hard reset toàn repository. Nếu một commit công khai sai, revert giữ quan hệ ancestry. Reset mạnh nhất khi mục tiêu thật sự là dựng lại lịch sử riêng tư; dùng nó cho mọi lỗi giống dùng búa lớn cho mọi loại ốc.",

loi_thuong_gap: [
  "**Quên rằng mode mặc định là `--mixed`.** Dấu hiệu là staging bị xóa sau `git reset <commit>`. Ghi mode rõ ràng trong thao tác quan trọng và lưu staged diff nếu đã chọn hunk công phu.",
  "**Tin `--hard` chỉ xóa commit.** Dấu hiệu là sửa đổi tracked chưa commit biến mất khỏi file. Hard còn ghi working tree; reflog không bảo đảm cứu bytes chưa từng thành object.",
  "**Nhầm reset theo path với reset theo commit.** Dấu hiệu là `HEAD` không đổi sau `git reset HEAD -- file`. Có pathspec, mục tiêu chính là index chứ không phải ref.",
  "**Chỉ đọc `git diff` sau mixed reset.** Dấu hiệu là file mới không hiện dù `git status` báo `??`. Untracked file không nằm trong diff mặc định.",
  "**Dùng `HEAD~n` mà không xem graph merge.** Dấu hiệu là nhánh lùi tới commit không mong đợi. Phân giải bằng `rev-parse`, xem `show`, rồi dùng ID đã xác minh.",
  "**Reset nhánh đã push rồi force-push theo phản xạ.** Dấu hiệu là remote từ chối non-fast-forward hoặc đồng đội bị diverged. Trên lịch sử chung, revert thường là mô hình đúng.",
  "**Cho rằng hard reset dọn cả file rác.** Dấu hiệu là `debug.log` untracked vẫn còn. Đây không phải mục tiêu của reset; xử lý untracked thuộc `git clean` và cần dry-run.",
  "**Dựa vào `HEAD@{1}` mà không đọc reflog.** Dấu hiệu là khôi phục nhầm vì hoạt động khác đã chen vào. Xác định entry theo thời gian, message và object ID cụ thể."
],

tu_kiem_tra: [
  "Từ trạng thái `HEAD=A`, index=C, working tree=C sau soft reset, giải thích vì sao staged diff chứa B+C còn unstaged diff rỗng.",
  "Một file được tạo ở commit C rồi mixed reset về A. Vì sao nó có thể thành untracked thay vì xuất hiện trong `git diff`?",
  "Vẽ ba pha reset và chỉ ra chính xác pha cuối cùng mà mỗi mode thực hiện.",
  "Vì sao commit bị hard reset thường cứu được bằng reflog, còn dòng code vừa sửa nhưng chưa staging có thể không cứu được?",
  "Phân biệt `git reset HEAD -- cart.js` với `git reset --mixed HEAD~1` về ref, index, phạm vi và working tree.",
  "Trong graph có merge commit M với hai parent, `M~1`, `M^1` và `M^2` chọn những điểm nào? Vì sao điều đó quan trọng trước hard reset?",
  "Hãy chọn giữa soft reset, interactive rebase, commit sửa mới và revert cho bốn tình huống: squash nhánh riêng, sửa commit sâu, lỗi nhỏ đã push, đảo tính năng công khai."
],

tai_nguyen: [
  "Sách “Pro Git”, mục Reset Demystified — mô hình ba cây và diễn giải các mode reset.",
  "Tài liệu tham khảo chính thức của Git cho `git-reset` — các dạng cú pháp theo commit, pathspec và hành vi từng mode.",
  "Sách “Version Control with Git” — ref, index, object reachability và kỹ thuật phục hồi lịch sử.",
  "Khóa học “Git Immersion” — phòng thí nghiệm reset và quan sát thay đổi bằng status, diff, log."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: ref là con trỏ vào commit graph, còn reset thay con trỏ mà không lập tức xóa nút khỏi object database.",
  "Nối sang mảng Hệ điều hành: hard reset ghi file thật trên filesystem, nên khả năng cứu dữ liệu khác hoàn toàn khả năng tìm lại object Git.",
  "Nối sang mảng Hệ thống phân tán: reset một ref đã công bố tạo hai lịch sử ở các clone; non-fast-forward là cơ chế phát hiện cập nhật không đơn điệu.",
  "Nối sang mảng An toàn hệ thống: phân cấp soft–mixed–hard minh họa nguyên tắc chọn thao tác có phạm vi phá hủy nhỏ nhất đủ đạt mục tiêu."
]

};
