window.LESSONS["04:van-hanh-thuc-te:seed-data-va-fixture-cho-test"] = {

tom_tat: [
  "Seed data tạo trạng thái nền có chủ đích cho một môi trường; fixture cấp đúng dữ liệu và tài nguyên mà một test cần, rồi chịu trách nhiệm cô lập hoặc thu dọn chúng.",
  "Dữ liệu test tốt làm lộ invariant nghiệp vụ: một đơn đã thanh toán, một sự kiện hết vé và hai khách trùng email có giá trị hơn hàng nghìn hàng ngẫu nhiên vô nghĩa.",
  "Fixture phải xác định ai sở hữu transaction, ID được tham chiếu thế nào và trạng thái nào còn lại sau test; nếu không, test sẽ phụ thuộc thứ tự chạy.",
  "`INSERT ... ON CONFLICT` giúp seed chạy lại được, nhưng chỉ an toàn khi quy tắc cập nhật phản ánh đúng ý nghĩa của từng hàng và không che dữ liệu sai.",
  "Dataset nhỏ kiểm tính đúng đắn; dataset có phân phối gần production mới hữu ích cho query plan. Một bộ duy nhất hiếm khi làm tốt cả hai việc."
],

can_biet_truoc: [
  "DDL và constraint PostgreSQL: `PRIMARY KEY`, `FOREIGN KEY`, `UNIQUE`, `CHECK`, `NOT NULL` và hành vi của sequence.",
  "Transaction, savepoint, `COMMIT`, `ROLLBACK` và mức cô lập; đây là nền để fixture không rò trạng thái giữa các test.",
  "SQL join, phép tổng hợp và `EXPLAIN (ANALYZE, BUFFERS)`; dữ liệu có phân phối khác nhau có thể tạo query plan khác nhau.",
  "Khác biệt giữa unit test, integration test và end-to-end test; chỉ hai loại sau thường cần PostgreSQL thật."
],

dinh_nghia: "**Seed data** là tập dữ liệu được nạp có chủ đích để đưa một database hoặc một môi trường tới trạng thái nền đã biết: danh mục quốc gia, role mặc định, tài khoản demo hay một bộ dữ liệu hiệu năng. Seed thường sống lâu hơn một test và có thể được chạy lại khi dựng môi trường.\n\n**Fixture** là phần thiết lập có vòng đời gắn với test: nó tạo hoặc cung cấp bản ghi, kết nối, transaction và các phụ thuộc cần cho tình huống kiểm thử, sau đó trả hệ thống về trạng thái cô lập. Fixture không đồng nghĩa với một file JSON; nó là hợp đồng về trạng thái trước và sau test.\n\n**Factory** là hàm sinh đối tượng hoặc hàng theo mặc định hợp lệ và cho phép ghi đè thuộc tính quan trọng. Factory giúp giảm chữ lặp, nhưng không thay thế fixture: một hàm tạo `order` chưa quyết định transaction thuộc về ai hay dữ liệu được dọn lúc nào.",

vi_sao_can: "Test database thất bại theo hai hướng đối nghịch. Quá ít dữ liệu khiến query và constraint chưa bao giờ gặp ca khó; quá nhiều bản ghi dùng chung khiến test không còn biết mình đang dựa vào hàng nào. Khi một test chỉ pass sau test “tạo khách hàng”, suite đã trở thành một chương trình có state ngầm thay vì tập các phép thử độc lập.\n\nTrong hệ thống bán vé của bài trước, logic xác nhận đơn phụ thuộc đồng thời vào sự kiện còn mở, số vé còn lại, `UNIQUE` của email, tổng các `order_items` và trạng thái `payments`. Một fixture chỉ tạo `orders` bằng cách tắt khóa ngoại sẽ chạy nhanh nhưng kiểm một thế giới không tồn tại ở production. Ngược lại, sao chép database production vào laptop làm lộ dữ liệu cá nhân, khó tái lập và thường khiến test chậm đến mức mọi người bỏ chạy.\n\nSeed và fixture tốt biến lỗi thành thông tin: tên kịch bản cho biết invariant nào vỡ, khóa tự nhiên giúp đọc hàng, transaction đảm bảo test kế tiếp không bị nhiễm, còn dataset hiệu năng buộc planner đối diện cardinality và độ lệch thực tế.",

co_che: "### Tách bốn loại dữ liệu trước khi thiết kế\n\nKhông nên gọi mọi lệnh `INSERT` là seed. **Reference data** là dữ liệu miền tương đối ổn định như mã trạng thái. **Bootstrap data** giúp ứng dụng khởi động, chẳng hạn role quản trị đầu tiên. **Scenario data** diễn tả một câu chuyện kiểm thử cụ thể. **Volume data** tạo quy mô và phân phối để thử hiệu năng. Vòng đời và cách kiểm của bốn loại khác nhau.\n\nReference data có thể đi cùng migration nếu code phụ thuộc chặt vào nó, nhưng khi ấy thay đổi phải được version hóa như schema. Scenario data nên nằm gần test để người đọc thấy nguyên nhân. Volume data nên được sinh bằng script có seed ngẫu nhiên cố định hoặc quy tắc tất định, không viết tay hàng vạn dòng.\n\n### Dùng constraint làm bộ sinh phản ví dụ\n\nSchema không chỉ là nơi fixture phải vượt qua; nó là bản đặc tả giúp chọn ca kiểm. Với `UNIQUE (lower(email))`, ta cần hai email khác hoa thường. Với `CHECK (quantity > 0)`, cần ca 0 và số âm. Với khóa ngoại từ `order_items` sang `orders`, cần chứng minh một dòng mồ côi bị từ chối. Với `NOT NULL`, cần phân biệt “không cung cấp” với giá trị rỗng hợp lệ.\n\nĐừng tắt constraint để nạp fixture cho tiện. Nếu production không cho phép trạng thái ấy, test nghiệp vụ trên trạng thái ấy đang trả lời câu hỏi giả. Ngoại lệ là test công cụ sửa chữa dữ liệu hỏng; khi đó việc tạo hỏng phải nằm trong setup được đánh dấu rõ và khôi phục constraint trước assertion.\n\n### Khóa ổn định: tự nhiên, surrogate và tên tượng trưng\n\nHard-code `id = 1` dễ đọc lúc đầu nhưng va chạm với sequence và test chạy song song. Tốt hơn là `INSERT ... RETURNING id` rồi truyền ID qua biến của fixture. Khi cần seed dùng chung có thể chạy lại, một khóa tự nhiên ổn định như `event_code = 'JAZZ-2026-HCM'` giúp tìm hàng mà không phụ thuộc số sequence.\n\nTên tượng trưng trong code test còn rõ hơn: `sold_out_event`, `alice_customer`, `captured_payment`. Test nên tham chiếu đối tượng được fixture trả về, không truy vấn “hàng đầu tiên”. `SELECT ... LIMIT 1` thiếu `ORDER BY` không có thứ tự ổn định và có thể chọn hàng khác sau vacuum hoặc plan thay đổi.\n\n### Seed chạy lại được nhưng không được che drift\n\nIdempotent ở đây nghĩa là chạy seed nhiều lần đưa database tới cùng trạng thái mong muốn. PostgreSQL hỗ trợ `ON CONFLICT`, nhưng lựa chọn `DO NOTHING` có thể giữ lại tên sai từ lần chạy trước. `DO UPDATE` lại có thể ghi đè tùy chỉnh hợp lệ của môi trường. Phải phân loại cột nào do seed sở hữu và cột nào do người dùng sở hữu.\n\nVí dụ seed sở hữu `name` của mã trạng thái thì cập nhật là hợp lý; seed tài khoản demo nhưng mật khẩu đã được đổi bởi người dùng thì ghi đè mỗi lần khởi động là nguy hiểm. Một cách chặt hơn là seed kiểm phiên bản dữ liệu tham chiếu và dừng nếu gặp drift ngoài dự kiến, thay vì âm thầm “sửa” mọi thứ.\n\n```sql\nINSERT INTO events (event_code, title, capacity, status)\nVALUES ('JAZZ-2026-HCM', 'Đêm Jazz Sài Gòn', 100, 'open')\nON CONFLICT (event_code) DO UPDATE\nSET title = EXCLUDED.title,\n    capacity = EXCLUDED.capacity,\n    status = EXCLUDED.status\nWHERE (events.title, events.capacity, events.status)\n   IS DISTINCT FROM (EXCLUDED.title, EXCLUDED.capacity, EXCLUDED.status)\nRETURNING id, event_code;\n```\n\n`IS DISTINCT FROM` xử lý cả `NULL` theo ngữ nghĩa so sánh an toàn. Mệnh đề `WHERE` tránh update và WAL không cần thiết khi giá trị đã giống. Để câu này chạy, `event_code` cần unique index hoặc constraint; cơ chế conflict dựa vào đó để tìm xung đột hiệu quả thay vì quét bảng.\n\n### Sở hữu transaction quyết định độ cô lập\n\nMẫu nhanh cho integration test là mở transaction trước test, nạp fixture, chạy assertion, rồi `ROLLBACK`. PostgreSQL hoàn tác cả dữ liệu do test tạo, nên cleanup không cần đoán danh sách bảng. Mẫu này chỉ đúng khi mọi thao tác ứng dụng dùng cùng connection hoặc ít nhất cùng transaction.\n\nNếu code dưới test mở connection khác và commit thật, rollback ở connection của test không nhìn thấy để hoàn tác. End-to-end test qua HTTP thường gặp đúng vấn đề này. Khi ấy có thể cấp riêng database/schema cho worker, truncate có thứ tự giữa test, hoặc tạo tenant duy nhất rồi xóa theo tenant. Mỗi lựa chọn đánh đổi tốc độ, độ trung thực và khả năng chạy song song.\n\nSavepoint hữu ích khi framework giữ một transaction ngoài cho cả test và code cần kiểm hành vi lỗi. PostgreSQL đưa transaction vào trạng thái aborted sau lỗi SQL; phải `ROLLBACK TO SAVEPOINT` trước khi chạy câu tiếp. Nếu fixture nuốt exception mà không hồi savepoint, assertion sau sẽ chỉ nhận “current transaction is aborted” và che nguyên nhân thật.\n\n### Chạy song song: namespace là tài nguyên\n\nHai worker cùng chèn `alice@example.test` vào constraint unique sẽ va nhau dù từng test chạy riêng đều pass. Không nên sửa bằng cách bỏ unique constraint. Hãy tạo namespace theo worker hoặc test: email `alice+<run_id>@example.test`, `event_code` có hậu tố, hay database riêng.\n\nID ngẫu nhiên giảm va chạm nhưng làm test khó tái lập nếu lỗi không ghi seed. Counter toàn cục lại có race. Giá trị tất định từ `run_id` và tên test thường cân bằng tốt: có thể đọc, tái tạo, đồng thời không đụng worker khác. Cleanup phải chỉ nhắm namespace của test, không dùng `DELETE FROM customers` trong một môi trường dùng chung.\n\n### Factory hợp lệ mặc định, ca sai phải cố ý\n\nFactory nên sinh một đối tượng tối thiểu nhưng hợp lệ theo schema. Mặc định đó giảm thay đổi dây chuyền khi thêm cột bắt buộc. Tuy nhiên factory quá thông minh có thể tự tạo sự kiện, khách, đơn và payment mà test không biết; khi assertion sai, người đọc phải lần theo mạng phụ thuộc ẩn.\n\nMột nguyên tắc hữu ích: factory tạo một aggregate có biên nghiệp vụ rõ, còn test truyền thuộc tính làm nên kịch bản. `make_confirmed_order(customer, event, quantity=2)` có thể hợp lý; `make_everything()` thì không. Với ca vi phạm constraint, viết SQL trực tiếp thường rõ hơn việc thêm nhiều cờ `valid=False`, `without_customer=True` vào factory.\n\n### Dataset tính đúng đắn và dataset hiệu năng không cùng hình dạng\n\nNăm đơn được đặt tên rõ đủ kiểm tổng tiền. Chúng không cho biết index có hữu ích ở năm triệu đơn. Planner chọn `Seq Scan` trên bảng nhỏ là đúng vì đọc vài page rẻ hơn đi qua index. Vì vậy không được dùng plan của fixture nhỏ để tuyên bố production sẽ dùng index.\n\nVolume data cần mô phỏng **cardinality** (số giá trị phân biệt), độ lệch và tương quan. Nếu 95% đơn ở trạng thái `confirmed`, index chỉ có `status` có thể không giúp truy vấn lấy phần lớn bảng. Nếu hầu hết khách có một đơn nhưng một khách doanh nghiệp có hàng trăm nghìn đơn, truy vấn theo `customer_id` có hành vi khác mạnh tùy tham số.\n\nPostgreSQL có `generate_series` để sinh dữ liệu tất định. Sau khi nạp khối lớn phải `ANALYZE`, vì planner cần thống kê. Đừng chạy volume seed trong mọi unit test; tách thành suite hiệu năng hoặc kiểm query plan có ngân sách thời gian phù hợp.\n\n### Fixture cho query plan phải phát biểu kỳ vọng đúng mức\n\nÉp plan phải chứa chuỗi `Index Scan` thường giòn: PostgreSQL có thể chọn `Bitmap Heap Scan` vẫn hiệu quả, hoặc `Seq Scan` đúng khi bảng nhỏ. Kỳ vọng bền hơn gắn với hiện tượng: số hàng trả về, số page đọc, không có sort tràn đĩa, và plan không quét phần dữ liệu ngoài phạm vi một cách phi lý.\n\nMột index cho lịch sử đơn của khách là `(customer_id, created_at DESC)`. Nó khớp điều kiện equality và thứ tự, cho phép dừng sớm với `LIMIT`. Nếu fixture phân bố đều hoàn hảo trong khi production lệch, estimate đẹp giả tạo. Dữ liệu test hiệu năng phải ghi rõ phân phối chứ không chỉ số hàng.\n\n### Dữ liệu nhạy cảm không phải fixture miễn phí\n\nCopy production rồi đổi vài email không bảo đảm ẩn danh. Tên, địa chỉ, timestamp, ghi chú và tổ hợp thuộc tính có thể tái định danh. Hơn nữa bản copy nhanh chóng lỗi thời và không chỉ ra ca nào được test. Dữ liệu tổng hợp nên là mặc định.\n\nNếu bắt buộc dùng mẫu production để nghiên cứu phân phối, cần pipeline ẩn danh được kiểm, quyền truy cập, retention và môi trường tách biệt. Không đưa dump vào Git; dump lớn làm repository phình và lịch sử Git khó xóa triệt để. Fixture dạng code nên chỉ chứa dữ liệu giả nhỏ, không chứa token hay hash lấy từ người dùng thật.\n\n### Cleanup là phần của assertion\n\nTest pass nhưng để lại hàng làm test sau fail vẫn là test hỏng. Với rollback, hãy xác nhận connection không commit ngoài ý muốn. Với tenant cleanup, kiểm số hàng còn lại theo `run_id`. Với database riêng, đảm bảo drop xảy ra cả khi process bị ngắt và có job dọn rác theo tuổi.\n\nThứ tự xóa thủ công phản chiếu khóa ngoại: xóa `payments` và `order_items` trước `orders`, rồi mới tới khách/sự kiện. `TRUNCATE ... CASCADE` nhanh nhưng phạm vi rộng; dùng trong database riêng thì hợp lý, dùng ở môi trường chia sẻ có thể xóa dữ liệu của suite khác. Đây là lúc “nhanh hơn” không đồng nghĩa “an toàn hơn”.\n\n### Khi nào không nên dùng seed hoặc fixture dùng chung\n\nKhông dùng một seed khổng lồ cho mọi test khi mỗi test chỉ cần vài invariant; nó làm setup chậm và tạo phụ thuộc ngầm. Không dùng transaction rollback nếu code cần kiểm commit qua nhiều connection, background worker hoặc message broker. Không dùng snapshot database cố định khi schema thay đổi thường xuyên mà không có bước migration và xác minh tương thích.\n\nNgược lại, không nên mock database cho mọi thứ. Mock không thực thi constraint, collation, transaction hay planner PostgreSQL. Unit test thuần cho phép tính toán có thể mock repository; integration test của SQL và persistence phải chạm một PostgreSQL thật nếu muốn có bằng chứng về hành vi database.",

vi_du: "### Một kịch bản SQL tự hoàn tác\n\nĐoạn dưới giả sử schema hệ thống vé đã tồn tại. Nó tạo đúng một sự kiện, khách, đơn, dòng vé và payment trong transaction, kiểm invariant rồi rollback để có thể chạy lại mà không để rác:\n\n```sql\nBEGIN;\n\nWITH new_event AS (\n  INSERT INTO events (event_code, title, capacity, status)\n  VALUES ('TEST-SOLD-OUT-01', 'Fixture sold out', 2, 'open')\n  RETURNING id\n), new_customer AS (\n  INSERT INTO customers (email, full_name)\n  VALUES ('alice+fixture@example.test', 'Alice Fixture')\n  RETURNING id\n), new_order AS (\n  INSERT INTO orders (customer_id, status, total_cents, created_at)\n  SELECT c.id, 'confirmed', 500000, clock_timestamp()\n  FROM new_customer AS c\n  RETURNING id\n), new_item AS (\n  INSERT INTO order_items (order_id, event_id, quantity, unit_price_cents)\n  SELECT o.id, e.id, 2, 250000\n  FROM new_order AS o CROSS JOIN new_event AS e\n  RETURNING order_id\n)\nINSERT INTO payments (order_id, status, amount_cents)\nSELECT order_id, 'captured', 500000\nFROM new_item;\n\nDO $$\nDECLARE bad_count integer;\nBEGIN\n  SELECT COUNT(*) INTO bad_count\n  FROM orders AS o\n  JOIN order_items AS oi ON oi.order_id = o.id\n  WHERE o.total_cents <> oi.quantity * oi.unit_price_cents\n    AND o.customer_id = (\n      SELECT id FROM customers WHERE email = 'alice+fixture@example.test'\n    );\n  IF bad_count <> 0 THEN\n    RAISE EXCEPTION 'fixture violates order total invariant';\n  END IF;\nEND $$;\n\nROLLBACK;\n```\n\nCTE truyền ID thật qua từng bước, nên không đoán sequence. `clock_timestamp()` làm thời điểm thay đổi thật trong transaction; nếu test cần kết quả thời gian tuyệt đối ổn định, nên truyền timestamp cố định thay vì gọi đồng hồ. `ROLLBACK` giúp script lặp lại, nhưng nếu chạy qua framework thì framework nên sở hữu transaction và trả ID cho assertion.\n\n### Sinh phân phối để xem query plan\n\nMột fixture hiệu năng riêng có thể tạo nhiều đơn cho một khách “nóng” và ít đơn cho khách khác. Sau khi nạp, chạy `ANALYZE orders`, rồi đo:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT id, total_cents, created_at\nFROM orders\nWHERE customer_id = :hot_customer_id\nORDER BY created_at DESC\nLIMIT 20;\n```\n\nBiến `:hot_customer_id` là cú pháp tham số của client, không phải SQL chạy trực tiếp trong `psql`; trong `psql` có thể dùng `\\set hot_customer_id 42` rồi viết `:'hot_customer_id'`. Không ghi trước con số thời gian vì bài không chạy trên máy của người học. Điều phải quan sát là plan có tận dụng index `(customer_id, created_at DESC)` hay không, estimate lệch bao xa, số buffer chạm và việc `LIMIT` có dừng sớm không.\n\nCa biên cho suite gồm database hoàn toàn rỗng; sequence đã nhảy xa vì transaction rollback; hai worker chạy cùng lúc; email chỉ khác chữ hoa; quantity bằng 0; payment lớn hơn tổng đơn; timestamp đúng biên ngày theo timezone; và code ứng dụng mở connection thứ hai rồi commit ngoài transaction fixture.",

so_sanh: "### Chọn công cụ dữ liệu theo câu hỏi của test\n\n| Cách chuẩn bị | Điểm mạnh | Điểm yếu | Dùng khi |\n|---|---|---|---|\n| SQL fixture nhỏ | Hiển thị rõ constraint và quan hệ; chạy trên PostgreSQL thật | Dễ dài nếu factory không hỗ trợ | Integration test cho truy vấn và transaction |\n| Factory trong code | Giá trị mặc định hợp lệ, trả object/ID thuận tiện | Có thể giấu setup và sinh quá nhiều dependency | Nhiều kịch bản cùng biến thể của một aggregate |\n| Seed dùng chung | Dựng demo/reference data nhất quán, chạy lại được | Tạo phụ thuộc ngầm nếu test dựa vào hàng không tuyên bố | Môi trường dev, dữ liệu nền ổn định |\n| Snapshot/template database | Clone nhanh một trạng thái lớn | Khó nhìn ý nghĩa, dễ lệch schema, cần quản vòng đời | Suite cô lập cần baseline lớn và đồng nhất |\n| Dữ liệu tổng hợp bằng generator | Tạo quy mô và phân phối có kiểm soát | Cần ghi seed và mô hình phân phối | Test hiệu năng, query plan, ca lệch |\n| Mock repository | Rất nhanh, không cần database | Không kiểm SQL, constraint, isolation hay planner | Unit test logic thuần ngoài persistence |\n\nKhông có hàng nào thay thế mọi hàng khác. Một pipeline tốt thường dùng factory/SQL cho tính đúng đắn, generator riêng cho hiệu năng, seed cho môi trường dev và mock chỉ ở biên nơi hành vi PostgreSQL không phải đối tượng kiểm thử.",

loi_thuong_gap: [
  "**Test phụ thuộc thứ tự.** Chạy riêng thì fail hoặc đổi thứ tự là fail; nguyên nhân thường là fixture dùng hàng do test trước tạo hoặc cleanup không hoàn tất.",
  "**Hard-code ID từ sequence.** Lỗi xuất hiện sau rollback, chạy song song hoặc seed khác đã tiêu ID; dùng `RETURNING` và truyền tham chiếu thay vì đoán.",
  "**Dùng `ON CONFLICT DO NOTHING` để giấu mọi khác biệt.** Seed báo thành công nhưng hàng cũ sai giá trị; hãy xác định cột seed sở hữu và kiểm drift.",
  "**Tắt constraint để nạp nhanh.** Fixture tạo trạng thái production không thể có, khiến test pass vô nghĩa; chỉ làm có chủ đích cho test sửa dữ liệu hỏng.",
  "**Rollback ở sai connection.** Test vẫn để lại dữ liệu vì ứng dụng mở connection khác và commit; theo dõi connection hoặc cấp database/schema riêng.",
  "**Dùng dữ liệu nhỏ để kết luận về index.** Planner quét tuần tự trên vài page là hợp lý; cần volume data, phân phối đại diện, `ANALYZE` và plan thực.",
  "**Copy production làm fixture.** Nhận biết qua dump chứa email/timestamp thật và không tái tạo được; thay bằng dữ liệu tổng hợp hoặc pipeline ẩn danh được quản trị.",
  "**Cleanup quá rộng.** `TRUNCATE ... CASCADE` hoặc `DELETE` không lọc làm test song song phá nhau; cô lập namespace và chỉ dọn tài nguyên mình sở hữu."
],

tu_kiem_tra: [
  "Phân biệt reference data, bootstrap data, scenario data và volume data bằng bốn ví dụ trong hệ thống bán vé; loại nào nên version hóa cùng migration?",
  "Vì sao `ROLLBACK` fixture không cô lập được end-to-end test nếu request HTTP dùng connection pool khác?",
  "Viết một seed `ON CONFLICT` cho mã trạng thái và giải thích cột nào được update, cột nào phải giữ nguyên để không ghi đè dữ liệu môi trường.",
  "Một test kiểm duplicate email chạy ổn tuần tự nhưng fail ngẫu nhiên khi có bốn worker. Hãy thiết kế namespace vẫn giữ được ý nghĩa kiểm `UNIQUE`.",
  "Planner chọn `Seq Scan` trên bảng 20 hàng dù có index. Vì sao đây chưa phải lỗi, và dataset nào mới tạo được bằng chứng về truy vấn production?",
  "Sau một lỗi SQL, mọi câu lệnh tiếp theo trong transaction đều báo aborted. Savepoint sửa luồng kiểm test này như thế nào?",
  "Hãy phản biện factory tự tạo toàn bộ customer-event-order-payment chỉ từ một lệnh: nó giảm chữ nhưng làm mất thông tin nào trong test?"
],

tai_nguyen: [
  "Tài liệu chính thức PostgreSQL, các phần về `INSERT`, transaction, savepoint, constraint, `ANALYZE` và “Using EXPLAIN”.",
  "“Growing Object-Oriented Software, Guided by Tests” — thiết kế fixture, test isolation và vai trò của test data builder.",
  "“xUnit Test Patterns” — các mẫu setup, teardown, shared fixture và nguyên nhân test không ổn định.",
  "“Database Reliability Engineering” — cách nhìn database test và thay đổi dữ liệu như một phần của vận hành đáng tin cậy."
],

lien_he: [
  "Nối sang mảng Thuật toán và cấu trúc dữ liệu: generator tất định và phân phối lệch giúp tạo đầu vào phản ví dụ, thay vì chỉ kiểm trường hợp trung bình đẹp.",
  "Nối sang mảng Lập trình song song: worker test tranh chấp unique key là race trên namespace dùng chung; cô lập database hoặc định danh theo worker loại bỏ shared state.",
  "Nối sang mảng Bảo mật: dữ liệu production không trở thành an toàn chỉ vì nằm trong staging; data minimization và ẩn danh phải là bước được kiểm chứng.",
  "Nối sang mảng CI/CD: migration phải chạy trước seed, fixture phải tương thích schema mới, còn test song song cần lifecycle cấp và hủy database tự động.",
  "Nối ngược bài Sao lưu và phục hồi: restore drill kiểm invariant trên bản dữ liệu lớn, còn fixture biến cùng invariant thành kịch bản nhỏ có thể chạy ở mỗi commit."
]

};
