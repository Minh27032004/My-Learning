window.LESSONS["05:phuc-vu-model-ai:gioi-han-kich-thuoc-input-va-timeout"] = {

tom_tat: [
  "Giới hạn đầu vào và timeout là **hai ngân sách khác nhau**: ngân sách byte bảo vệ RAM, băng thông và bộ giải mã; ngân sách thời gian bảo vệ worker, GPU và trải nghiệm người gọi. Chỉ đặt một trong hai vẫn để hở một hướng làm cạn tài nguyên.",
  "Không được tin riêng `Content-Length`. Header có thể thiếu, sai hoặc bị cố tình làm giả; server phải vừa từ chối sớm khi header đã vượt ngưỡng, vừa đếm số byte thực đọc trong lúc nhận dữ liệu theo từng khối.",
  "`asyncio.timeout()` giới hạn thời gian chờ của coroutine nhưng không thể cưỡng bức dừng mọi phép suy luận đang chạy trong thread, process hay GPU. Muốn deadline có ý nghĩa vận hành, cần truyền ngân sách xuống từng tầng và cô lập công việc có thể treo.",
  "Endpoint OCR hóa đơn dùng ba rào chắn: tối đa 5 MiB dữ liệu nén, tối đa 20 triệu pixel sau giải mã và deadline tổng 8 giây. Ba ngưỡng chặn ba kiểu khuếch đại tài nguyên khác nhau.",
  "Phản hồi lỗi cần ổn định và không rò rỉ nội bộ: `413` cho đầu vào quá lớn, `408` khi client gửi quá chậm, `504` khi suy luận vượt deadline; log nội bộ giữ `request_id`, giai đoạn và số byte thay vì ghi ảnh hay nội dung nhạy cảm."
],

can_biet_truoc: [
  "HTTP request gồm header và body; biết ý nghĩa của `Content-Length`, status code `413 Payload Too Large`, `408 Request Timeout` và `504 Gateway Timeout`.",
  "Python `async`/`await`, coroutine và event loop; phân biệt công việc chờ I/O với công việc chiếm CPU hoặc GPU.",
  "FastAPI/Starlette `Request.stream()` để đọc body theo từng khối, `HTTPException` để trả lỗi có kiểm soát và middleware/proxy đứng trước ứng dụng.",
  "Khái niệm inference (suy luận bằng model), worker và hàng đợi; timeout của HTTP không đồng nghĩa với việc công việc nền đã bị hủy."
],

dinh_nghia: "**Giới hạn kích thước đầu vào** là contract quy định lượng dữ liệu tối đa mà một request được phép buộc dịch vụ tiếp nhận và xử lý. Nó không chỉ là một con số byte: với ảnh OCR còn phải có giới hạn kích thước sau giải mã, chẳng hạn tổng số pixel, vì một file nén nhỏ vẫn có thể bung thành vùng nhớ rất lớn.\n\n**Timeout** là mốc thời gian sau đó một giai đoạn hoặc toàn request không còn được phép giữ tài nguyên đồng bộ. Trong hệ thống phục vụ model AI nên dùng **deadline** — một thời điểm kết thúc tuyệt đối hoặc ngân sách còn lại — thay vì mỗi tầng tự cấp lại một timeout đầy đủ. Nếu proxy cho 10 giây, ứng dụng lại cho model 10 giây sau khi đã upload 4 giây, lời hứa 10 giây với client đã bị phá vỡ.\n\nVới API OCR hóa đơn của bài này, contract được chọn là:\n\n| Rào chắn | Ngưỡng minh họa | Tài nguyên được bảo vệ |\n|---|---:|---|\n| body nhận qua mạng | 5 MiB | băng thông, RAM đệm, ổ tạm |\n| ảnh sau giải mã | 20.000.000 pixel | RAM, CPU của thư viện ảnh |\n| thời gian đọc body | 3 giây | connection và worker |\n| toàn bộ request | 8 giây | slot suy luận và độ trễ phía người dùng |\n\nCác số trên là **cấu hình của ví dụ**, không phải chuẩn chung. Dịch vụ thật phải chọn từ phân phối dữ liệu hợp lệ, khả năng phần cứng, SLO và thử nghiệm tải; không được chép nguyên 5 MiB hay 8 giây sang một bài toán khác rồi gọi đó là an toàn.",

vi_sao_can: "Một endpoint model biến dữ liệu do người ngoài kiểm soát thành phép tính đắt tiền. Chỉ một request bình thường đã có thể giữ RAM cho body, RAM cho tensor, một thread giải mã, một worker Python và một slot GPU. Nếu không đặt trần, kẻ tấn công không cần khai thác lỗi thực thi mã; họ chỉ cần gửi nhiều body lớn, gửi thật chậm hoặc chọn đầu vào khiến model chạy lâu để tạo **denial of service** (từ chối dịch vụ).\n\nGiới hạn ở schema như “chuỗi tối đa 1.000 ký tự” đến quá muộn nếu framework đã đọc 2 GiB vào bộ nhớ trước khi validation. Tương tự, trả `504` sau 8 giây chưa chắc giải phóng GPU: tác vụ CUDA hoặc hàm đồng bộ trong thread có thể tiếp tục chạy dù coroutine chờ nó đã bị hủy. Khi đó biểu đồ HTTP trông như timeout hoạt động, nhưng năng lực phục vụ vẫn giảm dần vì các công việc “mồ côi” còn chạy phía sau.\n\nẢnh tạo thêm một lớp rủi ro. File PNG 2 MiB có thể mô tả ảnh với hàng chục triệu pixel; sau giải mã sang RGB, mỗi pixel cần nhiều byte, chưa kể các bản sao trung gian và tensor float. Bởi vậy byte trên đường truyền và kích thước dữ liệu đã giải mã phải được kiểm riêng. Đây là nguyên tắc tổng quát: giới hạn phải đặt tại mỗi nơi dữ liệu **nở ra** hoặc công việc được **nhân lên**.\n\nCuối cùng, timeout là một quyết định sản phẩm chứ không chỉ một `try/except`. Quá ngắn làm request hợp lệ thất bại, thúc đẩy client retry và có thể tăng tải đúng lúc hệ thống đang nghẽn. Quá dài giữ kết nối và tài nguyên vô ích. Cần phối hợp deadline, giới hạn đồng thời, retry có kiểm soát và hàng đợi cho tác vụ thật sự dài.",

co_che: "### Lập bản đồ tài nguyên trước khi chọn con số\n\nHãy đi theo vòng đời của ảnh hóa đơn: socket nhận byte → ASGI server chuyển chunk → ứng dụng tích lũy body → thư viện ảnh giải mã → tiền xử lý tạo tensor → model chạy → JSON được mã hóa. Mỗi mũi tên có thể làm tăng tài nguyên. Body 4 MiB không có nghĩa request chỉ dùng 4 MiB RAM; có thể đồng thời tồn tại `bytes`, đối tượng ảnh RGB, mảng NumPy và tensor đã chuẩn hóa.\n\nDo đó, “max upload = 5 MiB” chỉ trả lời một phần. Ta cần ít nhất ba đại lượng: số byte truyền, số phần tử sau giải mã và thời gian giữ tài nguyên hiếm. Với văn bản cho LLM, đại lượng sau giải mã tương ứng có thể là số token; với audio là thời lượng và sample rate; với batch dự đoán là số bản ghi nhân số đặc trưng.\n\nNgưỡng phải dựa trên dữ liệu quan sát được. Thu thập histogram kích thước và độ trễ của request hợp lệ, chọn vùng phục vụ sản phẩm, rồi thử tải với giá trị sát biên. Không ghi payload thô vào log để làm thống kê vì hóa đơn có thể chứa tên, địa chỉ và số tài khoản; chỉ ghi kích thước, loại MIME đã xác minh, số pixel, thời gian từng giai đoạn và mã kết quả.\n\n### Rào chắn sớm ở reverse proxy\n\nReverse proxy có thể từ chối body quá lớn trước khi byte đi sâu vào ứng dụng. Lợi thế là giảm băng thông nội bộ và tránh chiếm worker FastAPI. Tuy nhiên, đây không phải rào chắn duy nhất: cấu hình có thể lệch giữa môi trường, request chunked không có `Content-Length`, và đường gọi nội bộ đôi khi bỏ qua proxy.\n\nRào chắn nên xếp lớp:\n\n```text\nInternet\n  → load balancer: giới hạn kết nối và tốc độ\n  → reverse proxy: giới hạn body, timeout đọc header/body\n  → FastAPI: đếm byte thật, xác minh loại dữ liệu, deadline nghiệp vụ\n  → decoder/model: giới hạn pixel, token, batch và concurrency\n```\n\nCác tầng phải nhất quán nhưng không nhất thiết bằng nhau. Proxy có thể cho thêm một khoảng nhỏ vì multipart chứa boundary và metadata, trong khi ứng dụng giới hạn riêng phần file. Nếu proxy đặt 5 MiB cho toàn multipart còn ứng dụng cho file đúng 5 MiB, request hợp lệ sẽ bị proxy chặn do overhead. Contract công khai phải nói rõ giới hạn áp dụng cho file hay toàn request.\n\n### `Content-Length` chỉ là tín hiệu từ chối sớm\n\nNếu header khai 9 MiB trong khi trần là 5 MiB, có thể trả `413` ngay mà không đọc tiếp. Nhưng chấp nhận request chỉ vì header khai 1 MiB là sai: client kiểm soát header, transfer encoding có thể không cung cấp độ dài, và các thành phần trung gian có thể biến đổi cách truyền.\n\nQuy tắc đúng là “header lớn thì từ chối; header nhỏ không phải bằng chứng an toàn”. Ứng dụng vẫn đếm tổng byte nhận được. Khi tổng vượt trần, nó dừng tích lũy, trả lỗi và không chuyển phần body chưa hoàn chỉnh cho decoder. Việc đọc theo chunk còn tránh thao tác `await request.body()` vốn có thể gom toàn bộ dữ liệu trước khi code của ta được kiểm tra kích thước.\n\nKích thước chunk không phải giới hạn bảo mật. Chunk 64 KiB chỉ quyết định lượng dữ liệu xử lý mỗi vòng; tổng cộng một triệu chunk vẫn khổng lồ. Biến `total` mới là invariant: sau mỗi lần nối, `total ≤ MAX_BODY_BYTES`, nếu không request bị chấm dứt.\n\n### Timeout đọc body chống kiểu gửi nhỏ giọt\n\nMột client có thể gửi đúng 4 MiB nhưng mỗi vài giây mới gửi một byte. Giới hạn kích thước không ngăn connection bị giữ rất lâu. Đây là kiểu tấn công slow upload; cần timeout hoặc minimum data rate ở proxy và deadline đọc body trong ứng dụng.\n\n`asyncio.timeout(READ_TIMEOUT_SECONDS)` bao quanh toàn bộ vòng lặp stream tạo một ngân sách chung. Không nên đặt timeout 3 giây cho từng chunk, vì kẻ gửi chỉ cần đưa một chunk trước mỗi mốc 3 giây để kéo dài vô hạn. Đây cũng là lý do deadline tổng tốt hơn việc “reset đồng hồ” ở mỗi thao tác.\n\nKhi hết thời gian đọc, trả `408` là diễn đạt rằng client không hoàn thành request đúng hạn. Nếu proxy là tầng phát hiện, client có thể nhận mã khác tùy cấu hình; ứng dụng và dashboard cần phân biệt timeout phía client, timeout hàng đợi và timeout suy luận, nếu không một biểu đồ “timeout” chung sẽ che mất nguyên nhân.\n\n### Deadline phải giảm dần qua từng giai đoạn\n\nGiả sử request có ngân sách 8 giây. Upload mất 1,7 giây và giải mã mất 0,4 giây thì model không còn 8 giây; nó còn khoảng 5,9 giây trừ thời gian dành cho serialization. Ta ghi `deadline = loop.time() + 8` ngay khi nhận request, rồi trước mỗi thao tác tính `remaining = deadline - loop.time()`.\n\n```python\ndef con_lai(deadline: float) -> float:\n    remaining = deadline - asyncio.get_running_loop().time()\n    if remaining <= 0:\n        raise TimeoutError(\"request deadline exhausted\")\n    return remaining\n\nasync with asyncio.timeout(con_lai(deadline)):\n    ket_qua = await goi_model(...)\n```\n\nCách này giữ đúng một lời hứa xuyên tầng. Nếu gọi một dịch vụ model qua HTTP, timeout của client nội bộ phải nhỏ hơn `remaining` để FastAPI còn thời gian chuyển lỗi thành phản hồi. Nếu đẩy vào hàng đợi, message nên mang `expires_at`; worker bỏ công việc đã hết hạn thay vì tốn GPU tạo kết quả không ai còn chờ.\n\n### Cancellation không phải nút dừng cưỡng bức\n\nKhi `asyncio.timeout()` hết hạn, task đang chờ nhận cancellation. Coroutine hợp tác sẽ dừng tại điểm `await`. Nhưng hàm OCR đồng bộ chạy trong thread qua `asyncio.to_thread()` không bị Python giết thread an toàn; nó có thể tiếp tục tới khi hàm tự kết thúc. Thư viện native hoặc kernel GPU cũng có quy tắc hủy riêng.\n\nVì thế có ba cấp bảo vệ khác nhau:\n\n| Cách chạy model | Khi request timeout | Biện pháp vận hành |\n|---|---|---|\n| coroutine I/O có hỗ trợ hủy | thường dừng tại `await` | truyền timeout xuống HTTP/database client |\n| hàm đồng bộ trong thread | thread thường tiếp tục | giới hạn concurrency, hàm tự kiểm deadline nếu có thể |\n| process riêng hoặc worker queue | process/job có thể bị cô lập | kill/recycle process theo chính sách, job có `expires_at` |\n\nKhông nên tùy tiện kill thread vì có thể để lock và trạng thái thư viện ở tình trạng hỏng. Với model native có nguy cơ treo, process worker riêng là biên cô lập chắc hơn; đổi lại có chi phí IPC, quản lý model trong bộ nhớ và khởi động lại. Với tác vụ dài hợp lệ, trả `202 Accepted` cùng job ID và xử lý qua hàng đợi thường đúng hơn việc giữ HTTP mở.\n\n### Giới hạn sau giải mã chặn dữ liệu khuếch đại\n\nKiểm MIME từ header chưa đủ vì client tự khai `image/png`. Cần để decoder đọc cấu trúc file, xác minh định dạng được cho phép và lấy chiều rộng, chiều cao trước khi cấp phát các bản sao lớn khi thư viện hỗ trợ. Invariant của ví dụ là `width × height ≤ 20_000_000`.\n\nChỉ kiểm từng chiều cũng có thể sai: 20.000 × 2.000 đều có vẻ dưới một trần chiều rộng/chiều cao tùy ý nhưng tích là 40 triệu pixel. Phép nhân trong Python không tràn số nguyên, song ở ngôn ngữ số nguyên cố định phải kiểm overflow trước khi dùng tích. Ngoài pixel còn cần chặn số frame với GIF/TIFF, metadata bất thường và định dạng không cần thiết; diện tích hợp lệ nhưng hàng nghìn frame vẫn gây khuếch đại.\n\nDecoder là bề mặt tấn công xử lý dữ liệu nhị phân. Cập nhật thư viện, chỉ bật các codec cần dùng và chạy trong môi trường ít quyền là các lớp phòng vệ bổ sung. Giới hạn kích thước giảm tác động nhưng không thay thế việc vá lỗ hổng parser.\n\n### Concurrency là chiều thứ ba của ngân sách\n\nMột request 5 MiB trong 8 giây có thể an toàn, nhưng 1.000 request đồng thời thì không. Giới hạn từng request phải đi cùng giới hạn số tác vụ suy luận đồng thời, hàng đợi hữu hạn và rate limit theo danh tính hoặc tenant. Nếu hàng đợi vô hạn, timeout ở phía HTTP chỉ tạo thêm job mồ côi.\n\nSemaphore trong một process chỉ bảo vệ process đó; chạy bốn worker thì tổng slot gấp bốn. GPU dùng chung cần bộ điều phối ở cấp phù hợp hoặc mỗi worker sở hữu quota rõ ràng. Khi hết năng lực, fail fast bằng `429` hoặc `503` có `Retry-After` hợp lý thường tốt hơn nhận mọi việc rồi timeout. Retry phải có exponential backoff và jitter; retry tức thời biến quá tải nhẹ thành bão request.\n\n### Mã lỗi, log và khả năng điều tra\n\nClient cần lỗi ổn định để sửa request: `413` cho body/pixel vượt giới hạn, `415` cho định dạng không hỗ trợ, `408` cho upload không hoàn tất, `504` cho suy luận không kịp deadline. Không trả stack trace, đường dẫn model hay thông tin GPU. Thông báo có thể công bố ngưỡng byte nhưng tránh phản chiếu tên file hoặc metadata chưa làm sạch.\n\nLog nội bộ nên có `request_id`, tenant đã băm hoặc định danh phù hợp chính sách, `bytes_received`, `width`, `height`, `queue_ms`, `decode_ms`, `inference_ms`, giai đoạn timeout và kết quả. Metric cần đếm số lần bị chặn theo lý do và theo dõi p50/p95/p99. Nếu tỷ lệ `413` tăng sau khi client nâng độ phân giải, đó là vấn đề contract; nếu `504` tăng cùng độ sâu hàng đợi, nghẽn nằm ở năng lực suy luận chứ không phải mạng.\n\nKhông ghi nguyên ảnh, prompt hay token xác thực. Giới hạn tài nguyên mà làm rò dữ liệu qua log là đổi một sự cố sẵn sàng lấy một sự cố riêng tư.\n\n### Khi nào không nên giữ request đồng bộ\n\nKhông có timeout “đủ lớn” nào biến một tác vụ vài phút thành HTTP đồng bộ đáng tin cậy qua nhiều proxy. Nếu OCR tài liệu hàng trăm trang, video hoặc batch lớn là nghiệp vụ hợp lệ, hãy upload trực tiếp vào object storage bằng URL có thời hạn, tạo job nhỏ chứa tham chiếu, xử lý bất đồng bộ và cho client polling/webhook nhận kết quả.\n\nNgược lại, đừng đưa mọi ảnh nhỏ vào queue chỉ vì queue nghe có vẻ mở rộng tốt. Với hóa đơn một trang xử lý dưới SLO, endpoint đồng bộ đơn giản hơn cho client, ít trạng thái và dễ debug. Chuyển sang hàng đợi khi độ dài công việc, độ biến thiên hoặc nhu cầu retry vượt khả năng giữ kết nối; đó là đánh đổi kiến trúc, không phải mặc định.",

vi_du: "### Endpoint FastAPI hoàn chỉnh cho ảnh hóa đơn\n\nVí dụ dưới đây nhận raw body thay vì multipart để tập trung vào cơ chế giới hạn. Client gửi `Content-Type: image/png` hoặc `image/jpeg`. Hàm model giả lập bằng coroutine để code chạy được mà không cần model thật; khi thay bằng model đồng bộ, phải áp dụng lưu ý về thread/process ở phần cơ chế.\n\n```python\nimport asyncio\nimport io\nimport secrets\nfrom typing import Any\n\nfrom fastapi import FastAPI, HTTPException, Request\nfrom PIL import Image, UnidentifiedImageError\n\napp = FastAPI()\n\nMAX_BODY_BYTES = 5 * 1024 * 1024\nMAX_PIXELS = 20_000_000\nREAD_TIMEOUT_SECONDS = 3.0\nREQUEST_TIMEOUT_SECONDS = 8.0\nALLOWED_TYPES = {\"image/png\", \"image/jpeg\"}\n\n\nasync def doc_body_co_gioi_han(request: Request) -> bytes:\n    raw_length = request.headers.get(\"content-length\")\n    if raw_length is not None:\n        try:\n            declared = int(raw_length)\n        except ValueError:\n            raise HTTPException(400, \"Content-Length không hợp lệ\")\n        if declared < 0:\n            raise HTTPException(400, \"Content-Length không hợp lệ\")\n        if declared > MAX_BODY_BYTES:\n            raise HTTPException(413, \"Ảnh vượt giới hạn 5 MiB\")\n\n    chunks: list[bytes] = []\n    total = 0\n    try:\n        async with asyncio.timeout(READ_TIMEOUT_SECONDS):\n            async for chunk in request.stream():\n                total += len(chunk)\n                if total > MAX_BODY_BYTES:\n                    raise HTTPException(413, \"Ảnh vượt giới hạn 5 MiB\")\n                chunks.append(chunk)\n    except TimeoutError:\n        raise HTTPException(408, \"Gửi ảnh quá chậm\")\n\n    if total == 0:\n        raise HTTPException(400, \"Body ảnh rỗng\")\n    return b\"\".join(chunks)\n\n\ndef kiem_tra_anh(data: bytes) -> tuple[int, int]:\n    try:\n        with Image.open(io.BytesIO(data)) as image:\n            width, height = image.size\n            if width <= 0 or height <= 0:\n                raise HTTPException(400, \"Kích thước ảnh không hợp lệ\")\n            if width * height > MAX_PIXELS:\n                raise HTTPException(413, \"Ảnh có quá nhiều pixel\")\n            image.verify()\n            return width, height\n    except UnidentifiedImageError:\n        raise HTTPException(415, \"Body không phải PNG/JPEG hợp lệ\")\n\n\nasync def model_ocr_gia_lap(data: bytes) -> dict[str, Any]:\n    await asyncio.sleep(0.05)\n    return {\"text\": \"TỔNG CỘNG 125000\", \"confidence\": 0.97}\n\n\n@app.post(\"/v1/receipts/ocr\")\nasync def ocr_receipt(request: Request) -> dict[str, Any]:\n    request_id = secrets.token_hex(8)\n    content_type = request.headers.get(\"content-type\", \"\").split(\";\", 1)[0].lower()\n    if content_type not in ALLOWED_TYPES:\n        raise HTTPException(415, \"Chỉ nhận image/png hoặc image/jpeg\")\n\n    loop = asyncio.get_running_loop()\n    deadline = loop.time() + REQUEST_TIMEOUT_SECONDS\n    try:\n        data = await doc_body_co_gioi_han(request)\n        width, height = kiem_tra_anh(data)\n        remaining = deadline - loop.time()\n        if remaining <= 0:\n            raise TimeoutError\n        async with asyncio.timeout(remaining):\n            result = await model_ocr_gia_lap(data)\n    except TimeoutError:\n        raise HTTPException(504, \"OCR không hoàn tất trước deadline\")\n\n    return {\n        \"request_id\": request_id,\n        \"image\": {\"width\": width, \"height\": height},\n        \"result\": result,\n    }\n```\n\nChạy bằng `uvicorn app:app`, rồi tạo ảnh kiểm thử hợp lệ bằng Pillow:\n\n```python\nfrom PIL import Image\n\nImage.new(\"RGB\", (640, 480), \"white\").save(\"receipt.png\")\n```\n\nGọi endpoint:\n\n```bash\ncurl --fail-with-body \\\n  -H \"Content-Type: image/png\" \\\n  --data-binary @receipt.png \\\n  http://127.0.0.1:8000/v1/receipts/ocr\n```\n\n### Kiểm thử những ca mà happy path không chạm tới\n\nCác test sau dùng `TestClient`. Ca header giả xác minh server vẫn đếm byte thực; ca body rỗng và sai định dạng xác minh decoder không nhận dữ liệu tùy ý.\n\n```python\nimport io\nfrom fastapi.testclient import TestClient\nfrom PIL import Image\n\nfrom app import app, MAX_BODY_BYTES\n\nclient = TestClient(app)\n\n\ndef png_bytes(width: int = 32, height: int = 32) -> bytes:\n    output = io.BytesIO()\n    Image.new(\"RGB\", (width, height), \"white\").save(output, format=\"PNG\")\n    return output.getvalue()\n\n\ndef test_anh_hop_le():\n    response = client.post(\n        \"/v1/receipts/ocr\",\n        content=png_bytes(),\n        headers={\"Content-Type\": \"image/png\"},\n    )\n    assert response.status_code == 200\n    assert response.json()[\"image\"] == {\"width\": 32, \"height\": 32}\n\n\ndef test_body_rong():\n    response = client.post(\n        \"/v1/receipts/ocr\", content=b\"\", headers={\"Content-Type\": \"image/png\"}\n    )\n    assert response.status_code == 400\n\n\ndef test_header_khai_nho_nhung_body_that_lon():\n    response = client.post(\n        \"/v1/receipts/ocr\",\n        content=b\"x\" * (MAX_BODY_BYTES + 1),\n        headers={\"Content-Type\": \"image/png\", \"Content-Length\": \"1\"},\n    )\n    assert response.status_code == 413\n\n\ndef test_header_da_qua_lon_bi_tu_choi_som():\n    response = client.post(\n        \"/v1/receipts/ocr\",\n        content=b\"x\",\n        headers={\n            \"Content-Type\": \"image/png\",\n            \"Content-Length\": str(MAX_BODY_BYTES + 1),\n        },\n    )\n    assert response.status_code == 413\n\n\ndef test_noi_dung_khong_phai_anh():\n    response = client.post(\n        \"/v1/receipts/ocr\",\n        content=b\"day khong phai anh\",\n        headers={\"Content-Type\": \"image/png\"},\n    )\n    assert response.status_code == 415\n```\n\n`TestClient` thường tự sửa `Content-Length` theo body, nên test “header khai nhỏ” có thể phụ thuộc transport và không mô phỏng hoàn hảo client độc hại. Test đơn vị vẫn hữu ích cho nhánh logic; để xác minh hành vi wire-level, cần thêm integration test bằng socket/HTTP client cho phép điều khiển framing và chạy qua đúng reverse proxy của production.\n\n### Thử timeout mà không chờ model thật\n\nTa thay hàm model bằng coroutine ngủ lâu hơn ngân sách. Test xác minh contract HTTP, nhưng không được suy diễn rằng mọi backend model đều đã dừng; đó là thuộc tính riêng của cách chạy model.\n\n```python\nimport asyncio\n\nimport app as module_app\n\n\ndef test_model_vuot_deadline(monkeypatch):\n    async def model_cham(data: bytes):\n        await asyncio.sleep(20)\n        return {\"text\": \"không tới đây\"}\n\n    monkeypatch.setattr(module_app, \"model_ocr_gia_lap\", model_cham)\n    monkeypatch.setattr(module_app, \"REQUEST_TIMEOUT_SECONDS\", 0.01)\n\n    response = client.post(\n        \"/v1/receipts/ocr\",\n        content=png_bytes(),\n        headers={\"Content-Type\": \"image/png\"},\n    )\n    assert response.status_code == 504\n```\n\n### Hai chỉnh sửa cần làm trước production\n\nVí dụ tích lũy danh sách chunk rồi `b\"\".join`, nên tại thời điểm nối có thể tồn tại cả chunks và buffer kết quả. Với trần 5 MiB điều này dễ hiểu và thường chấp nhận được; file lớn hơn nên stream vào file tạm có quota hoặc object storage, đồng thời vẫn đếm byte. Không được đổi sang stream mà bỏ trần tổng.\n\nHàm `kiem_tra_anh` là đồng bộ. Với ảnh nhỏ nó minh họa rõ, nhưng decoder nặng có thể chặn event loop. Production nên đưa phần CPU-bound sang thread/process có concurrency hữu hạn; nếu dùng thread, nhớ rằng timeout phía coroutine không giết công việc. Ngoài ra cần middleware ánh xạ lỗi decoder bất ngờ thành phản hồi chung, metric theo giai đoạn, giới hạn request toàn cục và cấu hình tương ứng ở proxy.",

so_sanh: "### Bốn chiến lược thường bị đánh đồng\n\n| Chiến lược | Chặn được gì | Không chặn được gì | Khi phù hợp |\n|---|---|---|---|\n| chỉ kiểm `Content-Length` | từ chối sớm request khai quá lớn | header thiếu/giả, body chunked, dữ liệu nở sau giải mã | chỉ là tối ưu bổ sung, không đứng một mình |\n| đọc toàn bộ rồi kiểm `len(body)` | biết byte thật sau khi đã đọc | đỉnh RAM và băng thông đã bị tiêu thụ | payload rất nhỏ, tầng trước đã có giới hạn chắc chắn |\n| stream và đếm byte | dừng khi tổng vượt trần, kiểm soát RAM tốt hơn | decompression bomb, model chạy lâu | rào chắn chuẩn ở ứng dụng |\n| stream + giới hạn dữ liệu giải mã + deadline | bảo vệ nhiều giai đoạn | quá tải do quá nhiều request đồng thời | endpoint model cần thêm quota/concurrency |\n\n### Timeout đồng bộ và hàng đợi bất đồng bộ\n\n| Tiêu chí | HTTP đồng bộ có deadline | Job qua hàng đợi |\n|---|---|---|\n| trải nghiệm client | một request nhận kết quả ngay | cần job ID, polling hoặc webhook |\n| tác vụ ngắn, ổn định | đơn giản và ít trạng thái | thường phức tạp quá mức |\n| tác vụ dài hoặc độ biến thiên cao | dễ vướng timeout nhiều tầng | chịu retry và điều tiết tải tốt hơn |\n| hủy khi client bỏ đi | khó với thread/GPU | worker có thể kiểm `expires_at` trước khi chạy |\n| chi phí vận hành | thấp hơn | cần broker, trạng thái job, dọn job và idempotency |\n\nKhông nên dùng queue như thuốc chữa cho mọi timeout. Queue chuyển thời gian chờ ra khỏi connection nhưng không làm công việc nhẹ hơn; nếu tốc độ nhận job lớn hơn tốc độ xử lý trong thời gian dài, backlog vẫn tăng. Ngược lại, tăng timeout HTTP không chữa được công việc vốn cần vài phút và không chịu được việc proxy hoặc client ngắt giữa chừng.\n\n### Khi nào KHÔNG nên dùng ngưỡng cứng duy nhất\n\nMột ngưỡng toàn hệ thống không phù hợp khi các tenant có hợp đồng khác nhau hoặc endpoint nhận nhiều loại dữ liệu. Nên có quota theo gói dịch vụ và giới hạn theo loại tác vụ, nhưng mọi nhánh phải có trần tuyệt đối do hạ tầng chịu được. Cho client tự truyền `timeout=600` rồi tin trực tiếp là trao quyền phân bổ tài nguyên cho đầu vào không tin cậy. Client có thể xin deadline ngắn hơn để fail fast; deadline dài hơn phải bị chặn bởi policy server.\n\nNếu dữ liệu lớn là nghiệp vụ cốt lõi, không nên cố nhét nó qua worker API bằng cách nâng `MAX_BODY_BYTES` vô hạn. Direct upload tới object storage giảm việc proxy body qua ứng dụng, song vẫn cần kiểm quota, checksum, loại file, thời hạn URL và quét/xử lý trong môi trường cô lập trước khi model đọc.",

loi_thuong_gap: [
  "**Tin tuyệt đối vào `Content-Length`.** Dấu hiệu là request chunked hoặc header khai nhỏ vẫn làm RAM tăng; sửa bằng cách dùng header để từ chối sớm nhưng luôn đếm byte thật trong stream.",
  "**Gọi `await request.body()` rồi mới kiểm kích thước.** Dấu hiệu là worker bị tăng RSS mạnh trước khi trả `413`; giới hạn phải tác động trong quá trình đọc hoặc ở proxy, không phải sau khi đã cấp phát.",
  "**Chỉ giới hạn byte nén.** Dấu hiệu là file nhỏ vượt ngưỡng RAM/CPU khi decoder chạy; thêm giới hạn pixel, frame, token, thời lượng hoặc số bản ghi tùy loại dữ liệu.",
  "**Reset timeout ở từng tầng hoặc từng chunk.** Dấu hiệu là tổng độ trễ vượt xa SLO dù không thao tác riêng nào timeout; dùng một deadline và truyền ngân sách còn lại xuống dưới.",
  "**Cho rằng trả `504` đã hủy model.** Dấu hiệu là client nhận lỗi nhưng GPU utilization và số thread bận vẫn cao; kiểm semantics hủy của backend, giới hạn concurrency và cô lập bằng process/job khi cần.",
  "**Timeout quá ngắn rồi cho client retry ngay.** Dấu hiệu là timeout tăng cùng lưu lượng retry, tạo vòng khuếch đại lúc nghẽn; dùng backoff, jitter, idempotency và từ chối sớm khi hết năng lực.",
  "**Log payload để điều tra lỗi giới hạn.** Dấu hiệu là ảnh hóa đơn hoặc prompt xuất hiện trong hệ thống log; chỉ ghi metadata tối thiểu, request ID và thời gian từng giai đoạn theo chính sách riêng tư.",
  "**Cấu hình proxy và FastAPI lệch nhau.** Dấu hiệu là cùng payload nhưng môi trường trả mã khác hoặc request hợp lệ bị chặn trước ứng dụng; kiểm thử end-to-end qua đúng ingress và tài liệu hóa giới hạn toàn request so với riêng file."
],

tu_kiem_tra: [
  "Vì sao kiểm `Content-Length <= 5 MiB` không đủ? Hãy nêu ít nhất ba trường hợp request vẫn có thể gây hại và rào chắn tương ứng.",
  "Một ảnh PNG 2 MiB giải mã thành 12.000 × 8.000 pixel. Nó qua giới hạn body nhưng phải bị xử lý thế nào, và tại sao chỉ kiểm chiều rộng riêng lẻ dễ bỏ sót?",
  "Proxy có timeout 10 giây; upload mất 4 giây; ứng dụng sau đó cấp mới 10 giây cho model. Contract nào đã bị phá, và cách biểu diễn deadline nào sửa được lỗi đó?",
  "`asyncio.timeout()` bao quanh `asyncio.to_thread(model.predict, tensor)` hết hạn. HTTP đã trả `504`; điều gì có thể vẫn chạy, và bạn sẽ đo bằng tín hiệu nào để chứng minh?",
  "Thiết kế bộ status code cho bốn lỗi: body vượt trần, MIME không hỗ trợ, client gửi nhỏ giọt và model vượt deadline. Thông tin nào nên trả client, thông tin nào chỉ nên nằm trong log?",
  "Nếu mỗi request riêng lẻ đều nằm dưới 5 MiB và 8 giây, vì sao 1.000 request đồng thời vẫn có thể làm sập dịch vụ? Đề xuất giới hạn ở cấp process, GPU và tenant.",
  "Câu khó: worker lấy job khỏi queue lúc `expires_at` còn 200 ms, trong khi p95 suy luận là 900 ms. Nên chạy, bỏ hay gia hạn? Trình bày invariant và đánh đổi về lãng phí, tính nhất quán, retry.",
  "Khi nào chuyển OCR sang job bất đồng bộ là quyết định đúng, và khi nào nó chỉ thêm broker, trạng thái cùng lỗi idempotency mà không đem lại lợi ích?"
],

tai_nguyen: [
  "Tài liệu chính thức FastAPI — phần Request Files, Request body và Handling Errors; dùng để đối chiếu API của framework đang cài đặt.",
  "Tài liệu chính thức Python — `asyncio` phần Timeouts, Task Cancellation và Running in Threads.",
  "OWASP Application Security Verification Standard — các yêu cầu về validation dữ liệu, upload file, giới hạn tài nguyên và xử lý lỗi.",
  "Sách **Release It!** — Michael T. Nygard, các mẫu stability như timeout, bulkhead và circuit breaker.",
  "Sách **Designing Data-Intensive Applications** — Martin Kleppmann, các chương về hệ thống phân tán, tải, hàng đợi và xử lý lỗi."
],

lien_he: [
  "Nối sang mảng **01 · Mạng máy tính**: `Content-Length`, chunked transfer, connection timeout và reverse proxy quyết định byte đi tới FastAPI như thế nào; hiểu TCP/HTTP giúp phân biệt client ngắt kết nối với model timeout.",
  "Nối sang mảng **01 · Hệ điều hành**: thread không có cơ chế kill an toàn tùy ý, còn process tạo biên cô lập mạnh hơn; đây là nền tảng để chọn `asyncio.to_thread`, process pool hay worker riêng.",
  "Nối sang mảng **04 · Cơ sở dữ liệu**: job bất đồng bộ cần idempotency key, trạng thái có vòng đời và cập nhật transaction để retry không tạo hai kết quả hoặc trừ quota hai lần.",
  "Nối sang mảng **06 · MLOps và vận hành model**: histogram latency, p99, GPU saturation, queue depth và canary giúp chọn lại ngưỡng dựa trên bằng chứng thay vì số đoán; model mới có thể làm deadline cũ không còn phù hợp.",
  "Nối sang mảng **07 · Bảo mật ứng dụng**: upload ảnh là đầu vào nhị phân không tin cậy; defense in depth, rate limit, least privilege và không log dữ liệu nhạy cảm biến giới hạn tài nguyên thành một phần của threat model hoàn chỉnh."
]

};
