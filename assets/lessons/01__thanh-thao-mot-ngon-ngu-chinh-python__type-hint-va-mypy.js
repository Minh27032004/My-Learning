window.LESSONS["01:thanh-thao-mot-ngon-ngu-chinh-python:type-hint-va-mypy"] = {

tom_tat: [
  "**Python KHÔNG kiểm tra type hint lúc chạy.** `def f(x: int)` rồi truyền chuỗi vào thì chương trình chạy bình thường. Muốn kiểm tra thì phải chạy công cụ riêng như `mypy`.",
  "**Giá trị lớn nhất không phải bắt lỗi mà là tài liệu không bao giờ lỗi thời** — chú thích sai thì `mypy` báo, còn docstring sai thì không ai biết.",
  "**`Optional[X]` chính là `X | None`.** Đây là chú thích quan trọng nhất: nó buộc bạn xử lý trường hợp `None`, thứ gây ra phần lớn lỗi `AttributeError` trong Python.",
  "**Thêm dần, không cần làm hết.** Bắt đầu từ chữ ký hàm công khai và ranh giới module; phần bên trong để sau. `mypy` có chế độ cho phép trộn code có và không có chú thích.",
  "**Đừng dùng `Any` để cho qua lỗi.** `Any` tắt mọi kiểm tra và lan sang những chỗ liên quan — dùng nó là mất đúng thứ mình vừa bỏ công thêm vào."
],

can_biet_truoc: [
  "Bài **Hàm, tham số mặc định, `*args`, `**kwargs`** — chú thích kiểu gắn vào chữ ký hàm, và cách chú thích `*args`/`**kwargs` có quy tắc riêng.",
  "Bài **Cú pháp, kiểu dữ liệu** — Python kiểu động nhưng chặt; type hint là cách lấy lại một phần lợi ích của kiểu tĩnh.",
  "Bài **List, dict, set, tuple** — chú thích kiểu cho container cần biết rõ chúng chứa gì.",
  "Bài **Module, package, import** — `if TYPE_CHECKING` để tránh import vòng tròn, đã nhắc ở bài đó."
],

dinh_nghia: "Nói đời thường: type hint giống như **ghi chú trên hộp đựng đồ**: \"hộp này đựng sách\". Cái nhãn không ngăn ai bỏ giày vào — nhưng nó cho người sau biết ý định, và có một người kiểm tra riêng đi soát xem nhãn có khớp với ruột không.\n\nNói chính xác:\n\n- **Type hint** (*chú thích kiểu*) — cú pháp khai báo kiểu mong đợi cho biến, tham số, giá trị trả về. Là **chú thích**, không phải ràng buộc.\n- **Kiểm tra tĩnh** (*static checking*) — phân tích code **mà không chạy nó**. `mypy`, `pyright` là hai công cụ phổ biến.\n- **`Optional[X]`** — bằng `X | None`. Giá trị có thể là `X` hoặc `None`.\n- **`Union[A, B]`** — bằng `A | B`. Một trong hai kiểu.\n- **`Any`** — tắt kiểm tra. Mọi thứ đều hợp lệ.\n- **Kiểu chung** (*generic*) — kiểu có tham số: `list[int]`, `dict[str, float]`.\n- **`Protocol`** — kiểu dựa trên *hình dạng* (có phương thức nào) chứ không dựa trên kế thừa. Đây là cách diễn đạt \"vịt gõ\" (*duck typing*) bằng kiểu.\n\nĐiểm cần nắm ngay: **chú thích kiểu hoàn toàn không ảnh hưởng tới lúc chạy.** Chúng được lưu trong `__annotations__` và bị bỏ qua. Đây vừa là ưu điểm (không tốn hiệu năng, không phá code cũ) vừa là điều bất ngờ với người từ Java hay TypeScript sang.",

vi_sao_can: "Type hint là thứ chia đôi cộng đồng Python một thời, và giờ đã thắng ở các dự án nghiêm túc:\n\n- **Bắt lỗi trước khi chạy.** Truyền `None` vào chỗ mong đợi chuỗi, quên xử lý nhánh trả về `None`, gõ sai tên thuộc tính — `mypy` báo ngay khi bạn còn đang viết, thay vì lúc chương trình chạy vào đúng nhánh hiếm gặp.\n- **Trình soạn thảo trở nên hữu dụng.** Không có chú thích thì gợi ý tự động chỉ đoán mò. Có chú thích thì nó biết `x` là `list[Nguoi]` và gợi ý đúng thuộc tính, đổi tên an toàn được, nhảy tới định nghĩa đúng chỗ.\n- **Tài liệu không bao giờ lỗi thời.** Docstring viết \"nhận một list\" rồi code đổi sang `dict` — không ai biết. Chú thích sai thì công cụ báo.\n- **Bắt buộc khi làm việc nhóm và dự án lớn.** Đọc một hàm 200 dòng của người khác mà không biết tham số là gì thì phải đọc hết mới hiểu.\n- **Định hướng AI/ML.** Các thư viện hiện đại chú thích kiểu đầy đủ, và trình soạn thảo dùng chúng để gợi ý — riêng việc này đã tiết kiệm rất nhiều thời gian tra tài liệu.\n\nTình huống thật rất kinh điển: hàm `tim_nguoi_dung(uid)` trả về `Nguoi` hoặc `None` khi không tìm thấy. Người gọi viết `tim_nguoi_dung(5).ten` và chạy tốt nhiều tháng — vì trong dữ liệu thật uid luôn tồn tại. Rồi một ngày có uid không tồn tại và ứng dụng đổ với `AttributeError: 'NoneType' object has no attribute 'ten'`. Với chú thích `-> Nguoi | None`, `mypy` báo lỗi này **ngay lúc viết**. Phần cơ chế sẽ nói vì sao đây là loại lỗi mà type hint bắt tốt nhất.",

co_che: "### Cú pháp cơ bản\n\n```python\n# Tham số và giá trị trả về\ndef chao(ten: str, so_lan: int = 1) -> str:\n    return f\"Xin chào {ten}! \" * so_lan\n\n# Biến\ntuoi: int = 22\nten: str = \"Minh\"\nds: list[int] = []                    # ⭐ hữu ích khi list rỗng lúc khởi tạo\n\n# Hàm không trả gì\ndef ghi_log(tin: str) -> None:\n    print(tin)\n```\n\nChú thích cho biến chỉ thật sự cần khi **không suy ra được**:\n\n```python\nx = 5                    # mypy tự biết là int — chú thích thừa\nds: list[int] = []       # ⭐ list rỗng thì không suy ra được, cần chú thích\nd: dict[str, list[int]] = {}\n```\n\nNguyên tắc: **chú thích ở chỗ công cụ không tự suy được, đừng chú thích những gì hiển nhiên.** Chú thích thừa làm code rối mà không thêm thông tin.\n\n### Không có tác dụng lúc chạy — chứng minh\n\n```python\ndef cong(a: int, b: int) -> int:\n    return a + b\n\ncong(\"x\", \"y\")           # 'xy' — CHẠY BÌNH THƯỜNG, không lỗi\ncong.__annotations__     # {'a': <class 'int'>, 'b': <class 'int'>, 'return': ...}\n```\n\nĐây là **quyết định thiết kế có chủ đích**, không phải thiếu sót:\n\n```\n① Không tốn hiệu năng lúc chạy\n② Code cũ không có chú thích vẫn chạy nguyên vẹn\n③ Thêm dần được — không phải làm cả dự án một lượt\n④ Thư viện dùng chú thích cho mục đích riêng (pydantic, dataclass, FastAPI)\n```\n\nĐiểm ④ đáng chú ý: một số thư viện **đọc** `__annotations__` rồi tự sinh kiểm tra lúc chạy. `@dataclass` đọc chú thích để sinh `__init__`; các framework web đọc chúng để kiểm tra và chuyển đổi dữ liệu vào. Nhưng đó là **thư viện làm**, không phải Python làm.\n\n### Kiểu container\n\n```python\n# Python 3.9 trở lên — dùng kiểu dựng sẵn\nds: list[int]\nd: dict[str, float]\ns: set[str]\nt: tuple[int, str]                  # ĐÚNG hai phần tử, đúng thứ tự\nt2: tuple[int, ...]                 # ⭐ số lượng tuỳ ý, cùng kiểu\n\n# Lồng nhau\nnhom: dict[str, list[tuple[str, int]]]\n\n# Python 3.8 trở xuống — phải import từ typing\nfrom typing import List, Dict\nds: List[int]\n```\n\nSự khác nhau giữa `tuple[int, str]` và `tuple[int, ...]` phản ánh đúng ý nghĩa dùng của `tuple` ở bài trước: dạng đầu là **bản ghi cấu trúc cố định**, dạng sau là dãy đồng loại.\n\n### `Optional` — chú thích quan trọng nhất\n\n```python\n# Ba cách viết cùng một thứ\nfrom typing import Optional\n\ndef tim(uid: int) -> Optional[str]: ...\ndef tim(uid: int) -> str | None: ...        # ⭐ Python 3.10+, nên dùng\n```\n\nVì sao nó quan trọng nhất — nó buộc bạn xử lý nhánh `None`:\n\n```python\ndef tim_ten(uid: int) -> str | None:\n    return kho.get(uid)\n\nten = tim_ten(5)\nprint(ten.upper())        # ❌ mypy: Item \"None\" of \"str | None\" has no attribute \"upper\"\n\n# ✓ Phải kiểm tra trước\nif ten is not None:\n    print(ten.upper())    # mypy biết ở đây ten chắc chắn là str\n\n# ✓ Hoặc dùng giá trị mặc định\nprint((ten or \"vô danh\").upper())\n```\n\nCơ chế đằng sau gọi là **thu hẹp kiểu** (*type narrowing*): sau `if ten is not None`, `mypy` biết trong nhánh đó `ten` chỉ còn là `str`. Nó hiểu được nhiều dạng kiểm tra:\n\n```python\nif isinstance(x, str): ...          # thu hẹp về str\nif x is None: ... else: ...          # nhánh else không còn None\nif not ds: return                     # sau đó ds chắc chắn không rỗng\nassert x is not None                  # thu hẹp bằng assert\n```\n\nĐây là lý do type hint bắt tốt nhất đúng loại lỗi phổ biến nhất của Python: **quên rằng một giá trị có thể là `None`.**\n\nMột cạm bẫy về giá trị mặc định:\n\n```python\ndef f(ds: list[int] = None):          # ❌ mypy báo lỗi\n    ...\n\ndef f(ds: list[int] | None = None):   # ✓ đúng — và đây là mẫu None ở bài hàm\n    if ds is None:\n        ds = []\n```\n\nChú thích buộc bạn **nói ra sự thật**: tham số này thật sự có thể là `None`.\n\n### `Union`, `Literal`, `Final`\n\n```python\nfrom typing import Literal, Final\n\ndef xu_ly(x: int | str) -> str:              # một trong hai\n    return str(x)\n\n# Literal — chỉ nhận đúng những giá trị này\ndef sap_xep(ds: list[int],\n            chieu: Literal[\"tang\", \"giam\"] = \"tang\") -> list[int]:\n    return sorted(ds, reverse=(chieu == \"giam\"))\n\nsap_xep([3,1], \"tang\")      # ✓\nsap_xep([3,1], \"asc\")       # ❌ mypy bắt được lỗi gõ sai\n\n# Final — hằng số, gán lại là lỗi\nSO_TOI_DA: Final = 100\nSO_TOI_DA = 200             # ❌ mypy: Cannot assign to final name\n```\n\n`Literal` rất được việc: nó biến một tham số chuỗi \"gõ sao cũng được\" thành một tập lựa chọn cố định mà công cụ kiểm tra được và trình soạn thảo gợi ý được.\n\n### Kiểu hàm và `Callable`\n\n```python\nfrom typing import Callable\n\ndef ap_dung(ds: list[int], f: Callable[[int], str]) -> list[str]:\n    #                          └─ nhận int, trả str\n    return [f(x) for x in ds]\n\nap_dung([1, 2], str)                  # ✓\nap_dung([1, 2], lambda x: x * 2)      # ❌ mypy: trả int, mong đợi str\n\nCallable[..., int]                    # tham số tuỳ ý, trả int\n```\n\n### `*args`, `**kwargs`\n\n```python\ndef f(*args: int, **kwargs: str) -> None:\n    # args là tuple[int, ...], kwargs là dict[str, str]\n    ...\n```\n\nChú thích mô tả **kiểu của từng phần tử**, không phải kiểu của cả `tuple` hay `dict`. Đây là chỗ hay nhầm.\n\n### `Protocol` — kiểu theo hình dạng\n\n```python\nfrom typing import Protocol\n\nclass CoDienTich(Protocol):\n    def dien_tich(self) -> float: ...      # ⭐ chỉ khai báo, không cài đặt\n\ndef tong_dien_tich(hinh: list[CoDienTich]) -> float:\n    return sum(h.dien_tich() for h in hinh)\n\nclass HinhTron:                             # ⭐ KHÔNG kế thừa CoDienTich\n    def __init__(self, r: float):\n        self.r = r\n    def dien_tich(self) -> float:\n        return 3.14159 * self.r ** 2\n\ntong_dien_tich([HinhTron(1), HinhTron(2)])  # ✓ hợp lệ vì ĐÚNG HÌNH DẠNG\n```\n\n`Protocol` là cách diễn đạt \"vịt gõ\" bằng kiểu: **không cần kế thừa, chỉ cần có đúng phương thức.** Đây là điểm khác biệt quan trọng với Java, và nó giữ được tính linh hoạt vốn có của Python trong khi vẫn kiểm tra được.\n\nDùng nó khi bạn muốn nhận \"bất cứ thứ gì có phương thức X\" mà không muốn ép người dùng kế thừa class của mình.\n\n### Tham chiếu tiến và import vòng tròn\n\n```python\n# Tên chưa tồn tại lúc định nghĩa ⇒ đặt trong chuỗi\nclass Nut:\n    def __init__(self, gt: int, ke: \"Nut | None\" = None):\n        self.gt = gt\n        self.ke = ke                        # \"Nut\" trong chuỗi vì class chưa xong\n\n# Hoặc bật chế độ hoãn đánh giá cho cả file\nfrom __future__ import annotations\n\nclass Nut:\n    def __init__(self, gt: int, ke: Nut | None = None):    # ✓ không cần chuỗi\n        ...\n```\n\nTránh import vòng tròn — mẫu đã nhắc ở bài module:\n\n```python\nfrom typing import TYPE_CHECKING\n\nif TYPE_CHECKING:                            # ⭐ LUÔN False lúc chạy\n    from don_hang import DonHang             # mypy vẫn đọc được\n\nclass NguoiDung:\n    def don_hang(self) -> list[\"DonHang\"]:   # tên trong chuỗi\n        from don_hang import DonHang         # import thật khi cần\n        return [DonHang()]\n```\n\nĐây là ngoại lệ chính đáng duy nhất cho việc import trong hàm: nó tách được **phụ thuộc kiểu** khỏi **phụ thuộc lúc chạy**.\n\n### `Any` — và vì sao nên tránh\n\n```python\nfrom typing import Any\n\ndef f(x: Any) -> Any:\n    return x.bat_ky_thu_gi()        # mypy KHÔNG kiểm tra gì cả\n```\n\nVấn đề của `Any` là **nó lan ra**:\n\n```python\ndef doc_cau_hinh() -> Any: ...\n\ncf = doc_cau_hinh()          # cf là Any\nhost = cf[\"host\"]            # host cũng là Any\nket_noi(host)                # không ai kiểm tra gì nữa\n```\n\nMột `Any` ở đầu chuỗi làm mất kiểm tra cho toàn bộ phần sau. Ba lựa chọn thay thế:\n\n```python\nobject           # \"kiểu gì cũng được\" nhưng PHẢI kiểm tra trước khi dùng\ndict[str, Any]   # biết là dict, chưa biết giá trị — thoả hiệp hợp lý\nTypedDict        # ⭐ mô tả rõ từng khoá\n```\n\n```python\nfrom typing import TypedDict\n\nclass CauHinh(TypedDict):\n    host: str\n    port: int\n    debug: bool\n\ndef doc_cau_hinh() -> CauHinh: ...\n\ncf = doc_cau_hinh()\ncf[\"host\"].upper()      # ✓ mypy biết là str\ncf[\"hots\"]              # ❌ mypy bắt được lỗi gõ sai khoá\n```\n\n`TypedDict` rất hợp với dữ liệu JSON: nó mô tả hình dạng của một `dict` mà không bắt bạn đổi sang class.\n\n### Chạy `mypy`\n\n```bash\npython -m pip install mypy\npython -m mypy duong_dan.py\npython -m mypy .                     # cả thư mục\n\npython -m mypy --strict .            # nghiêm ngặt — cho dự án mới\npython -m mypy --ignore-missing-imports .   # bỏ qua thư viện không có kiểu\n```\n\nCấu hình trong `pyproject.toml` — nên có để cả nhóm dùng chung:\n\n```toml\n[tool.mypy]\npython_version = \"3.12\"\nwarn_return_any = true\nwarn_unused_ignores = true\ndisallow_untyped_defs = false        # ⭐ cho phép thêm dần\n\n[[tool.mypy.overrides]]\nmodule = [\"thu_vien_khong_co_kieu.*\"]\nignore_missing_imports = true\n```\n\nDòng `disallow_untyped_defs = false` là chìa khoá để **áp dụng dần vào dự án đang có**: hàm chưa chú thích thì bỏ qua, hàm đã chú thích thì kiểm tra đầy đủ.\n\nBỏ qua một dòng cụ thể khi thật sự cần:\n\n```python\nx = ham_la()  # type: ignore[no-any-return]\n```\n\nLuôn ghi rõ mã lỗi trong ngoặc vuông thay vì `# type: ignore` trần — nếu không, nó sẽ che luôn những lỗi khác phát sinh sau này ở dòng đó.\n\n### Chiến lược áp dụng vào dự án có sẵn\n\n```\n① Bật mypy với cấu hình lỏng, sửa hết lỗi hiện có\n② Chú thích các hàm CÔNG KHAI và ranh giới module trước\n   → đây là chỗ cho nhiều giá trị nhất trên mỗi dòng bỏ ra\n③ Chú thích dần phần bên trong khi có dịp sửa code đó\n④ Siết dần cấu hình: bật disallow_untyped_defs cho từng module\n⑤ Đưa mypy vào CI để không lùi lại\n```\n\nĐiểm ② đáng nhấn: chú thích ở **ranh giới** (chỗ dữ liệu vào ra giữa các module, giữa ứng dụng và thế giới bên ngoài) cho phần lớn lợi ích với ít công sức nhất. Phần bên trong một hàm ngắn thì `mypy` tự suy được.\n\n### Giới hạn thật của type hint\n\nCân bằng lại, vì type hint không phải thuốc chữa bách bệnh:\n\n```\n✗ KHÔNG kiểm tra lúc chạy — dữ liệu từ JSON, API, database vẫn phải kiểm tra\n✗ KHÔNG bắt được lỗi logic — kiểu đúng mà thuật toán sai thì vẫn sai\n✗ Tốn công viết và bảo trì\n✗ Kiểu phức tạp có thể khó đọc hơn code nó mô tả\n✗ Nhiều thư viện cũ chưa có chú thích ⇒ mypy không biết gì về chúng\n```\n\nVới dữ liệu từ ngoài, cần kiểm tra **lúc chạy** bằng thư viện xác thực dữ liệu, hoặc bằng chính các phép kiểm tra thủ công ở bài ngoại lệ. Type hint bảo vệ **bên trong** chương trình; nó không thay được việc kiểm tra ở biên.\n\nKhi nào type hint không đáng: script một lần dùng rồi bỏ, notebook thử nghiệm, đoạn code ngắn tự đọc được. Khi nào rất đáng: **thư viện dùng chung, dự án nhiều người, code sẽ tồn tại lâu.**",

vi_du: "### Trước và sau khi chú thích\n\n```python\n# ❌ Không có chú thích — phải đọc thân hàm mới biết nhận gì\ndef xu_ly(du_lieu, nguong=None, sap_xep=True):\n    kq = []\n    for x in du_lieu:\n        if nguong is None or x[\"diem\"] >= nguong:\n            kq.append({\"ten\": x[\"ten\"], \"diem\": x[\"diem\"]})\n    return sorted(kq, key=lambda d: d[\"diem\"]) if sap_xep else kq\n\n# ✓ Có chú thích — chữ ký nói đủ mọi thứ cần biết\nfrom typing import TypedDict\n\nclass BanGhi(TypedDict):\n    ten: str\n    diem: float\n\ndef xu_ly(du_lieu: list[BanGhi],\n          nguong: float | None = None,\n          sap_xep: bool = True) -> list[BanGhi]:\n    kq = [{\"ten\": x[\"ten\"], \"diem\": x[\"diem\"]}\n          for x in du_lieu\n          if nguong is None or x[\"diem\"] >= nguong]\n    return sorted(kq, key=lambda d: d[\"diem\"]) if sap_xep else kq\n```\n\nBản thứ hai trả lời được ngay ba câu hỏi mà bản đầu bắt bạn đọc code: dữ liệu vào có hình dạng gì, `nguong` bỏ qua được không, trả về gì.\n\n### `mypy` bắt lỗi `None` — loại lỗi phổ biến nhất\n\n```python\n# nguoi_dung.py\nfrom dataclasses import dataclass\n\n@dataclass\nclass Nguoi:\n    id: int\n    ten: str\n    email: str | None = None            # ⭐ nói rõ có thể thiếu\n\nKHO: dict[int, Nguoi] = {1: Nguoi(1, \"Minh\", \"m@x.com\"),\n                          2: Nguoi(2, \"An\")}\n\ndef tim(uid: int) -> Nguoi | None:      # ⭐ nói rõ có thể không tìm thấy\n    return KHO.get(uid)\n\ndef gui_mail(uid: int) -> None:\n    n = tim(uid)\n    print(n.email.upper())              # ❌ mypy báo HAI lỗi\n```\n\n```bash\npython -m mypy nguoi_dung.py\n# error: Item \"None\" of \"Nguoi | None\" has no attribute \"email\"\n# error: Item \"None\" of \"str | None\" has no attribute \"upper\"\n```\n\nHai lỗi này là **hai lần ứng dụng có thể đổ trong sản phẩm**, và cả hai bị bắt trước khi chạy một dòng nào.\n\n```python\n# ✓ Bản đúng — mypy im lặng\ndef gui_mail(uid: int) -> None:\n    n = tim(uid)\n    if n is None:\n        print(f\"không có người dùng {uid}\")\n        return\n    if n.email is None:\n        print(f\"{n.ten} chưa có email\")\n        return\n    print(n.email.upper())              # ✓ mypy biết chắc là str\n```\n\nĐây là mẫu \"thoát sớm\" và nó vừa làm `mypy` hài lòng vừa làm code dễ đọc hơn — hai lợi ích cùng chiều.\n\n### Bắt lỗi khi đổi code\n\n```python\n# Phiên bản 1\ndef tinh_diem(bai: list[float]) -> float:\n    return sum(bai) / len(bai)\n\n# Nhiều nơi gọi\ntinh_diem([8.0, 9.0])\n```\n\n```python\n# Phiên bản 2 — đổi kiểu tham số\ndef tinh_diem(bai: dict[str, float]) -> float:\n    return sum(bai.values()) / len(bai)\n\n# mypy chỉ ra NGAY mọi nơi gọi cũ:\n# error: Argument 1 has incompatible type \"list[float]\"; expected \"dict[str, float]\"\n```\n\nKhông có chú thích thì lỗi này chỉ lộ ra khi chạy đúng nhánh gọi hàm đó — có khi là một nhánh hiếm gặp, phát hiện sau nhiều tuần. Đây là giá trị lớn nhất của type hint với **code phải sửa đổi lâu dài**.\n\n### `Literal` chống gõ sai\n\n```python\nfrom typing import Literal\n\nCheDo = Literal[\"doc\", \"ghi\", \"noi\"]\n\ndef mo(duong_dan: str, che_do: CheDo = \"doc\") -> str:\n    anh_xa = {\"doc\": \"r\", \"ghi\": \"w\", \"noi\": \"a\"}\n    return anh_xa[che_do]\n\nmo(\"a.txt\", \"ghi\")     # ✓\nmo(\"a.txt\", \"gi\")      # ❌ mypy: Argument 2 has incompatible type \"Literal['gi']\"\n```\n\nĐặt `CheDo` thành một bí danh kiểu rồi dùng lại nhiều nơi là cách giữ danh sách lựa chọn ở **một chỗ duy nhất** — thêm một chế độ mới thì chỉ sửa một dòng và `mypy` kiểm tra hết những nơi liên quan.\n\n### `Protocol` cho hàm nhận \"bất cứ thứ gì có...\"\n\n```python\nfrom typing import Protocol\n\nclass CoTheGhi(Protocol):\n    def write(self, s: str) -> int: ...\n\ndef xuat_bao_cao(dich: CoTheGhi, dong: list[str]) -> None:\n    for d in dong:\n        dich.write(d + \"\\n\")\n\n# Cả ba đều hợp lệ vì đều có write() — KHÔNG cần kế thừa gì\nimport sys, io\n\nxuat_bao_cao(sys.stdout, [\"a\", \"b\"])\n\nwith open(\"ra.txt\", \"w\", encoding=\"utf-8\") as f:\n    xuat_bao_cao(f, [\"a\", \"b\"])\n\nbuf = io.StringIO()\nxuat_bao_cao(buf, [\"a\", \"b\"])\n```\n\nĐây là chỗ type hint của Python **mềm dẻo hơn** kiểu tĩnh truyền thống: bạn mô tả *cái hàm cần*, không phải *cái class nào được phép*. Người dùng thư viện của bạn không phải kế thừa gì cả.\n\n### `TypedDict` cho dữ liệu JSON\n\n```python\nimport json\nfrom typing import TypedDict\n\nclass CauHinhDB(TypedDict):\n    host: str\n    port: int\n    ten_db: str\n\nclass CauHinh(TypedDict):\n    db: CauHinhDB\n    debug: bool\n\ndef nap(duong_dan: str) -> CauHinh:\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        return json.load(f)          # ⚠️ hứa hẹn kiểu, KHÔNG kiểm tra thật\n\ncf = nap(\"cf.json\")\nprint(cf[\"db\"][\"host\"].upper())     # ✓ mypy biết là str\nprint(cf[\"db\"][\"hots\"])              # ❌ mypy: TypedDict has no key \"hots\"\n```\n\nDòng đánh dấu ⚠️ là giới hạn thật cần nói rõ: `json.load` trả `Any`, và chú thích `-> CauHinh` chỉ là **lời hứa của bạn**. Nếu file JSON thiếu khoá `host`, `mypy` không biết và chương trình vẫn đổ lúc chạy.\n\nMuốn kiểm tra thật thì phải viết ra:\n\n```python\ndef nap(duong_dan: str) -> CauHinh:\n    with open(duong_dan, encoding=\"utf-8\") as f:\n        thô = json.load(f)\n    for khoa in (\"db\", \"debug\"):                     # kiểm tra ở BIÊN\n        if khoa not in thô:\n            raise ValueError(f\"cấu hình thiếu khoá {khoa!r}\")\n    return thô\n```\n\nĐây là ranh giới đáng nhớ: **type hint bảo vệ bên trong chương trình; kiểm tra lúc chạy bảo vệ ở biên.** Hai thứ bổ sung cho nhau, không thay thế nhau.\n\n### Generic — hàm dùng được với nhiều kiểu\n\n```python\n# Python 3.12+ — cú pháp mới, gọn\ndef dau_tien[T](ds: list[T]) -> T | None:\n    return ds[0] if ds else None\n\n# Python 3.11 trở xuống\nfrom typing import TypeVar\nT = TypeVar(\"T\")\n\ndef dau_tien(ds: list[T]) -> T | None:\n    return ds[0] if ds else None\n\n# mypy theo dõi đúng kiểu qua hàm\nx = dau_tien([1, 2, 3])          # x là int | None\ny = dau_tien([\"a\", \"b\"])         # y là str | None\n```\n\nSo với `list[Any] -> Any`: bản generic **giữ được thông tin kiểu**, nên nơi gọi vẫn được kiểm tra đầy đủ. Đây là ví dụ rõ nhất về việc `Any` làm mất gì.\n\n### Áp dụng dần vào dự án có sẵn\n\n```bash\n# ① Bắt đầu lỏng — chỉ kiểm tra hàm ĐÃ có chú thích\ncat >> pyproject.toml << 'EOF'\n[tool.mypy]\npython_version = \"3.12\"\ndisallow_untyped_defs = false\nignore_missing_imports = true\nEOF\n\npython -m mypy .\n\n# ② Xem chỗ nào đang thiếu kiểu nhiều nhất\npython -m mypy --html-report bao_cao .\n\n# ③ Siết dần cho từng module đã chú thích xong\ncat >> pyproject.toml << 'EOF'\n[[tool.mypy.overrides]]\nmodule = [\"goi_cua_toi.xu_ly.*\"]\ndisallow_untyped_defs = true\nEOF\n```\n\nCách này cho phép một dự án lớn tiến từng bước mà không có giai đoạn \"mọi thứ đều báo lỗi, không ai muốn đụng vào\".\n\n### `# type: ignore` đúng cách\n\n```python\n# ❌ ignore trần — che luôn mọi lỗi khác phát sinh sau này ở dòng đó\nket_qua = thu_vien_cu.ham()  # type: ignore\n\n# ✓ ghi rõ mã lỗi\nket_qua = thu_vien_cu.ham()  # type: ignore[no-untyped-call]\n\n# ✓ Tốt hơn nữa: khai báo kiểu cho thư viện thiếu chú thích\n# thu_vien_cu.pyi\ndef ham() -> str: ...\n```\n\nBật `warn_unused_ignores = true` trong cấu hình để `mypy` báo khi một `ignore` đã không còn cần — nó giúp dọn dần những chỗ vá tạm khi thư viện được cập nhật.\n\n### Chú thích cho decorator\n\n```python\nimport functools\nfrom typing import Callable, TypeVar, ParamSpec\n\nP = ParamSpec(\"P\")\nR = TypeVar(\"R\")\n\ndef do_thoi_gian(ham: Callable[P, R]) -> Callable[P, R]:\n    #                            ⭐ giữ NGUYÊN chữ ký hàm gốc\n    @functools.wraps(ham)\n    def bao(*a: P.args, **k: P.kwargs) -> R:\n        import time\n        t = time.perf_counter()\n        try:\n            return ham(*a, **k)\n        finally:\n            print(f\"{ham.__name__}: {time.perf_counter()-t:.4f}s\")\n    return bao\n\n@do_thoi_gian\ndef cong(a: int, b: int) -> int:\n    return a + b\n\ncong(1, 2)          # ✓ mypy biết trả int\ncong(\"x\", \"y\")      # ❌ mypy vẫn bắt được lỗi qua lớp decorator\n```\n\n`ParamSpec` giải quyết đúng vấn đề của bài decorator: không có nó, chú thích `Callable[..., R]` làm **mất** thông tin về tham số, và mọi lời gọi hàm đã trang trí đều không được kiểm tra nữa.\n\n### Kiểm chứng chú thích không ảnh hưởng lúc chạy\n\n```python\ndef f(x: int, y: \"KieuKhongTonTai\") -> \"CungKhongTonTai\":\n    return x\n\nprint(f(5, 10))              # 5 — CHẠY BÌNH THƯỜNG\nprint(f.__annotations__)     # chú thích chỉ là dữ liệu\n\n# Nhưng mypy thì báo:\n# error: Name \"KieuKhongTonTai\" is not defined\n```\n\nThí nghiệm này chốt lại ý cốt lõi của bài: **chú thích là dữ liệu, người kiểm tra là công cụ riêng.** Không chạy `mypy` thì type hint chỉ còn giá trị làm tài liệu và giúp trình soạn thảo — vẫn có ích, nhưng mất phần lớn lợi ích.",

so_sanh: "### Kiểu động, kiểu tĩnh, và type hint\n\n| | Python thuần | Python + `mypy` | Java, C# |\n|---|---|---|---|\n| Kiểm tra lúc biên dịch | ✗ | **✓ bằng công cụ riêng** | ✓ |\n| Kiểm tra lúc chạy | ✗ | ✗ | ✓ |\n| Bắt buộc chú thích | ✗ | ✗ **thêm dần được** | ✓ |\n| Ảnh hưởng hiệu năng | — | không | có (nhưng bù lại tối ưu được) |\n| Linh hoạt | **cao nhất** | cao | thấp hơn |\n\nCột giữa là điểm cân bằng mà Python chọn: **lợi ích của kiểu tĩnh cho ai muốn, mà không ép ai cả.** Đánh đổi là phải nhớ chạy công cụ — không chạy thì chú thích chỉ là ghi chú.\n\n### `Optional[X]`, `X | None`, `Any`\n\n| | `X | None` | `Any` | `object` |\n|---|---|---|---|\n| Kiểm tra | **✓ đầy đủ** | ✗ tắt hết | ✓ nhưng phải thu hẹp |\n| Buộc xử lý `None` | **✓** | ✗ | — |\n| Lan sang chỗ khác | ✗ | **✓ lan rộng** | ✗ |\n| Dùng khi | giá trị có thể thiếu | **hạn chế tối đa** | thật sự nhận mọi kiểu |\n\nHàng \"lan sang chỗ khác\" là lý do chính để tránh `Any`: một `Any` ở đầu chuỗi xử lý làm mất kiểm tra cho toàn bộ phần sau, và bạn mất đúng thứ vừa bỏ công thêm vào.\n\n### Chú thích ở đâu cho đáng công\n\n| Vị trí | Giá trị | Ưu tiên |\n|---|---|---|\n| Chữ ký hàm công khai | **cao nhất** | ① |\n| Ranh giới giữa các module | cao | ② |\n| Thuộc tính của class, dataclass | cao | ② |\n| Biến container khởi tạo rỗng | trung bình | ③ |\n| Biến cục bộ hiển nhiên | **thấp — thừa** | ✗ |\n\nHàng cuối đáng nhớ: `x: int = 5` không thêm thông tin gì mà làm code dài hơn. `mypy` tự suy được.\n\n### `TypedDict`, `dataclass`, `NamedTuple`\n\n| | `TypedDict` | `dataclass` | `NamedTuple` |\n|---|---|---|---|\n| Thực chất là | **`dict`** | class | `tuple` |\n| Truy cập | `d[\"khoa\"]` | `o.thuoc_tinh` | `o.thuoc_tinh` |\n| Mutable | ✓ | ✓ (mặc định) | ✗ |\n| Hashable | ✗ | ✗ (mặc định) | **✓** |\n| Có phương thức | ✗ | **✓** | hạn chế |\n| Kiểm tra lúc chạy | ✗ | ✗ | ✗ |\n| Hợp với | **dữ liệu JSON** | mô hình nghiệp vụ | bản ghi bất biến |\n\n`TypedDict` mạnh ở chỗ nó **không đổi cấu trúc dữ liệu** — vẫn là `dict` thật, `json.dump` được thẳng. Nó chỉ thêm thông tin kiểu.\n\n### `Protocol` và kế thừa class trừu tượng\n\n| | `Protocol` | Kế thừa ABC |\n|---|---|---|\n| Cần kế thừa | **✗** | ✓ |\n| Kiểm tra | theo **hình dạng** | theo **quan hệ kế thừa** |\n| Dùng được với class có sẵn | **✓** | ✗ phải sửa class |\n| Ép cài đặt đủ phương thức lúc chạy | ✗ | ✓ |\n| Hợp với | nhận \"bất cứ thứ gì có X\" | phân cấp có chủ đích |\n\n`Protocol` giữ được tinh thần \"vịt gõ\" của Python: bạn nhận được cả những class viết trước khi thư viện của bạn tồn tại.\n\n### Khi nào type hint đáng và không đáng\n\n| Loại code | Đáng? |\n|---|---|\n| Thư viện dùng chung | **✓ rất đáng** |\n| Dự án nhiều người | **✓** |\n| Code sống lâu, sửa nhiều | **✓** |\n| Ranh giới với dữ liệu ngoài | ✓ (kèm kiểm tra lúc chạy) |\n| Script một lần | ✗ |\n| Notebook thử nghiệm | ✗ |\n| Đoạn 10 dòng tự đọc được | ✗ |\n\nCâu hỏi phân định: **code này có ai đọc lại sau ba tháng không?** Có → chú thích. Không → bỏ qua và không áy náy.\n\n### Type hint bảo vệ được gì\n\n| Loại lỗi | Type hint bắt được? |\n|---|---|\n| Quên xử lý `None` | **✓ tốt nhất** |\n| Truyền sai kiểu tham số | **✓** |\n| Gõ sai tên thuộc tính, tên khoá | ✓ |\n| Đổi chữ ký mà quên sửa nơi gọi | **✓** |\n| Dữ liệu JSON/API sai hình dạng | **✗ cần kiểm tra lúc chạy** |\n| Lỗi logic, thuật toán sai | ✗ |\n| Lỗi hiệu năng | ✗ |\n\nBa hàng cuối là giới hạn thật: **type hint kiểm tra hình dạng, không kiểm tra ý nghĩa.** Nó không thay được kiểm thử, và không thay được việc kiểm tra dữ liệu ở biên.",

loi_thuong_gap: [
  "**Tưởng Python kiểm tra type hint lúc chạy.** Không hề. Phải chạy `mypy` hoặc công cụ tương đương.",
  "**Thêm chú thích rồi không bao giờ chạy `mypy`.** Mất phần lớn lợi ích; chỉ còn tác dụng tài liệu.",
  "**Dùng `Any` để dập tắt lỗi `mypy`.** Nó lan ra và làm mất kiểm tra cho cả chuỗi xử lý phía sau.",
  "**Viết `def f(ds: list[int] = None)`.** Phải là `list[int] | None = None` — chú thích buộc bạn nói ra sự thật.",
  "**Quên rằng hàm có thể trả `None`.** Chú thích `-> X | None` là chú thích có giá trị nhất, đừng bỏ.",
  "**Chú thích những gì hiển nhiên** (`x: int = 5`). Thừa, làm code dài mà không thêm thông tin.",
  "**Dùng `# type: ignore` trần.** Che luôn mọi lỗi khác phát sinh sau này ở dòng đó. Ghi rõ mã lỗi.",
  "**Chú thích `Callable[..., R]` cho decorator.** Mất thông tin tham số. Dùng `ParamSpec`.",
  "**Tin rằng `-> CauHinh` đảm bảo dữ liệu JSON đúng hình dạng.** Đó chỉ là lời hứa; phải kiểm tra ở biên.",
  "**Bật `--strict` ngay từ đầu trên dự án có sẵn.** Hàng nghìn lỗi cùng lúc và không ai muốn đụng vào. Áp dụng dần.",
  "**Nhầm `tuple[int, str]` với `tuple[int, ...]`.** Cái đầu là bản ghi hai phần tử; cái sau là dãy đồng loại.",
  "**Chú thích `*args: tuple[int, ...]`.** Sai — viết `*args: int`, chú thích mô tả kiểu của từng phần tử.",
  "**Import class chỉ để chú thích, gây import vòng tròn.** Dùng `if TYPE_CHECKING` và tên trong chuỗi.",
  "**Coi type hint là thay thế cho kiểm thử.** Nó kiểm tra hình dạng, không kiểm tra ý nghĩa."
],

tu_kiem_tra: [
  "Python có kiểm tra type hint lúc chạy không? Chứng minh bằng một ví dụ.",
  "Nêu bốn lý do thiết kế của việc chú thích không có tác dụng lúc chạy.",
  "Vì sao `X | None` là chú thích có giá trị nhất? \"Thu hẹp kiểu\" là gì?",
  "Vì sao `def f(ds: list[int] = None)` bị `mypy` báo lỗi, và cách viết đúng?",
  "`Any` khác `object` ở đâu? Vì sao `Any` \"lan ra\"?",
  "`tuple[int, str]` và `tuple[int, ...]` khác nhau thế nào? Liên hệ với ý nghĩa dùng của `tuple`.",
  "`Literal` giải quyết vấn đề gì mà `str` không giải quyết được?",
  "`Protocol` khác kế thừa class trừu tượng ở đâu? Vì sao nó hợp với tinh thần Python?",
  "`TypedDict` phù hợp với loại dữ liệu nào, và giới hạn của nó là gì?",
  "`if TYPE_CHECKING` giải quyết vấn đề gì? Vì sao nó không tạo import vòng tròn thật?",
  "Nêu chiến lược năm bước áp dụng `mypy` vào dự án có sẵn.",
  "Vì sao nên ghi mã lỗi trong `# type: ignore[...]` thay vì để trần?",
  "`ParamSpec` giải quyết vấn đề gì với decorator?",
  "Nêu ba loại lỗi type hint bắt được và ba loại nó không bắt được.",
  "Khi nào type hint không đáng công viết?"
],

tai_nguyen: [
  "Tài liệu Python chính thức, module `typing` — tham khảo đầy đủ mọi kiểu chú thích, kèm ghi chú phiên bản nào có gì.",
  "Tài liệu `mypy` — phần *Getting started* và *Existing code* là hai chương nên đọc đầu tiên khi áp dụng vào dự án có sẵn.",
  "Tài liệu Python, mục *Typing* trong thư viện chuẩn — phần về `Protocol`, `TypedDict`, `ParamSpec`.",
  "Sách **Fluent Python** — Luciano Ramalho. Có hẳn phần về type hint, gồm cả khi nào không nên dùng.",
  "Sách **Robust Python** — Patrick Viafore. Cuốn chuyên sâu nhất về việc dùng type hint để làm code Python bền vững."
],

lien_he: [
  "**Cú pháp, kiểu dữ liệu, toán tử**: Python kiểu động nhưng chặt; type hint lấy lại một phần lợi ích của kiểu tĩnh mà không mất tính động.",
  "**Hàm, tham số mặc định**: mẫu `None` làm giá trị mặc định chính là `X | None` khi chú thích.",
  "**List, dict, set, tuple**: `tuple[int, str]` so với `tuple[int, ...]` phản ánh đúng hai cách dùng `tuple` ở bài đó.",
  "**Module, package, import**: `if TYPE_CHECKING` là cách tránh import vòng tròn đã nhắc ở bài đó.",
  "**Decorator**: `ParamSpec` giữ được chữ ký hàm gốc qua lớp bọc — không có nó thì chú thích mất tác dụng.",
  "**Xử lý ngoại lệ**: type hint bảo vệ bên trong, kiểm tra lúc chạy bảo vệ ở biên — hai thứ bổ sung nhau.",
  "**Đọc ghi file, JSON**: `TypedDict` mô tả hình dạng dữ liệu JSON, nhưng vẫn phải kiểm tra thật khi nạp.",
  "**PEP 8 và format tự động** ngay sau: `mypy` và công cụ định dạng cùng nằm trong bộ công cụ chất lượng code chạy trong CI.",
  "**Lập trình hướng đối tượng** ở module 6 mảng 01: `Protocol` là cách diễn đạt \"vịt gõ\" bằng kiểu, và nó liên quan trực tiếp tới đa hình.",
  "**Backend & Web** ở mảng 05: các framework hiện đại dùng chú thích kiểu để tự sinh kiểm tra dữ liệu và tài liệu API."
]

};
