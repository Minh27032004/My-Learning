window.LESSONS["04:thiet-ke-csdl:khi-nao-co-y-pha-chuan-hoa-de-tang-toc"] = {

tom_tat: [
  "Phá chuẩn hoá là lưu có chủ đích một kết quả có thể suy ra từ dữ liệu chuẩn để giảm chi phí đọc, đổi lại phải trả chi phí đồng bộ, lưu trữ và xử lý sai lệch.",
  "Chỉ nên phá chuẩn hoá sau khi xác định truy vấn nóng bằng số đo, đọc `EXPLAIN (ANALYZE, BUFFERS)` và chứng minh index hoặc viết lại query chưa đủ.",
  "Ví dụ bán vé xét ba dạng dữ liệu dư khác nhau: tổng tiền booking, số ghế còn lại của show và snapshot tên sự kiện trên hóa đơn; mỗi dạng cần chiến lược nhất quán riêng.",
  "Cập nhật đồng bộ trong cùng transaction cho kết quả tức thời nhưng tăng contention; cập nhật bất đồng bộ tăng throughput nhưng tạo độ trễ và đòi hỏi khả năng rebuild.",
  "Một thiết kế phá chuẩn hoá hoàn chỉnh phải nêu nguồn chuẩn, quy tắc cập nhật, mức sai lệch cho phép, cách phát hiện drift và thủ tục tái tạo."
],

can_biet_truoc: [
  "1NF, 2NF, 3NF, phụ thuộc hàm và anomaly của bài trước.",
  "Transaction, commit, isolation và race condition ở mức cơ bản.",
  "B-tree index, index ghép và khả năng đọc query plan bằng `EXPLAIN`.",
  "Các phép tổng hợp SQL như `count`, `sum`, `group by` và join."
],

dinh_nghia: "**Phá chuẩn hoá (denormalization)** là quyết định lưu thêm dữ liệu có thể tính hoặc join từ nguồn chuẩn, nhằm tối ưu một đường đọc cụ thể. Bản sao có thể là cột tổng hợp, snapshot thuộc tính, bảng đọc, materialized view hoặc counter được duy trì riêng.\n\nNó khác dữ liệu lặp vô tình ở chỗ quyết định phải có hợp đồng: nguồn nào là authoritative (nguồn chuẩn), khi nào bản sao được cập nhật, cho phép trễ bao lâu, phát hiện sai lệch ra sao và có thể rebuild bằng câu lệnh nào. Không có hợp đồng này, “tối ưu” chỉ là anomaly của bảng chưa chuẩn hoá quay lại dưới tên mới.",

vi_sao_can: "Schema 3NF phù hợp ghi giao dịch, nhưng một số đường đọc phải tổng hợp hàng triệu dòng hoặc join qua nhiều quan hệ với tần suất cao. Trang bán vé có thể hỏi số ghế còn lại mỗi giây; dashboard nhà tổ chức cộng doanh thu của hàng nghìn booking; API mobile cần danh sách booking với tiêu đề sự kiện và tổng tiền trong một round trip. Index giúp tìm đúng hàng nhưng không loại được phép cộng trên tất cả hàng khớp.\n\nNếu workload có số lần đọc lớn hơn rất nhiều số lần ghi, lưu sẵn kết quả có thể chuyển chi phí từ mỗi lần đọc sang lúc ghi. Tuy nhiên cùng ghế bị mua, hủy, hoàn hoặc hết hạn giữ; counter sai một đơn vị có thể khiến bán quá chỗ hoặc báo hết vé giả. Chi phí thật của phá chuẩn hoá không nằm ở cột thêm, mà ở mọi đường làm dữ liệu nguồn thay đổi.\n\nVì thế yêu cầu “tăng tốc” chưa đủ. Phải chỉ ra truy vấn, mục tiêu độ trễ, tần suất, kích thước dữ liệu, plan hiện tại và mức nhất quán sản phẩm chấp nhận. Nếu bảng chỉ vài nghìn hàng hoặc truy vấn chạy hiếm, một index đúng thường rẻ và an toàn hơn cả hệ thống đồng bộ bản sao.",

co_che: "### Bắt đầu bằng ngân sách độ trễ và truy vấn nóng\n\nMột tối ưu có nghĩa khi gắn với SLO cụ thể, chẳng hạn endpoint danh sách show phải đáp ứng phần cơ sở dữ liệu trong giới hạn đã đo ở percentile mục tiêu. Không nên bịa con số chung cho mọi hệ thống. Hãy lấy trace sản xuất hoặc replay dữ liệu đại diện, xếp truy vấn theo tổng thời gian tiêu thụ, rồi chọn điểm nghẽn có ảnh hưởng thật.\n\nGhi lại bốn đại lượng: số lần gọi, số hàng đọc, thời gian thực tế và số buffer chạm. Truy vấn 300 ms chạy mỗi đêm có thể ít đáng ưu tiên hơn truy vấn 15 ms chạy hàng chục nghìn lần. Average che tail latency; cache ấm che lần đọc đĩa; môi trường dữ liệu nhỏ che sai lệch cardinality. Quyết định phá chuẩn hoá phải dựa trên workload gần thực tế.\n\n### Đọc plan trước khi thêm bản sao\n\nGiả sử tính tổng booking:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT b.booking_id, sum(bi.unit_price) AS total_amount\nFROM booking AS b\nJOIN booking_item AS bi USING (booking_id)\nWHERE b.booking_id = 1001\nGROUP BY b.booking_id;\n```\n\nNếu `booking_item` thiếu index bắt đầu bằng `booking_id`, planner có thể quét nhiều dòng không liên quan. Primary key `(booking_id, show_id, seat_id)` của bài trước đã đáp ứng hướng này; nếu schema dùng khóa khác, thêm index có thể giải quyết mà không lưu tổng. Nếu plan chỉ đọc vài item, cột tổng là phức tạp thừa.\n\nNgược lại, báo cáo tổng doanh thu theo organizer qua nhiều năm vẫn phải đọc lượng lớn item dù từng join có index. Index không làm `sum` biến mất. Khi kết quả được đọc liên tục và có thể cập nhật gia tăng, bảng tổng hợp theo ngày hoặc organizer trở thành ứng viên hợp lý.\n\n### Cây quyết định trước khi phá chuẩn hoá\n\nThử theo thứ tự tăng dần chi phí vận hành: sửa điều kiện và tránh N+1 query; chọn đúng cột thay vì `SELECT *`; thêm hoặc điều chỉnh index; cập nhật statistics; partition khi có pruning tự nhiên; cache kết quả ngắn hạn; materialized view; cuối cùng mới lưu bản sao trong bảng giao dịch nếu cần nhất quán tức thời.\n\nThứ tự không phải luật cứng, nhưng buộc ta chứng minh phương án đơn giản hơn không đủ. Ví dụ thêm index cho mỗi query cũng có giá ghi và bộ nhớ; năm index chồng lấp có thể tệ hơn một read model chuyên biệt. Điều quan trọng là so tổng chi phí, không thần thánh hóa chuẩn hoá hay phá chuẩn hoá.\n\n### Dạng một: tổng tiền lưu trên booking\n\n`booking.total_amount` có thể suy từ `sum(booking_item.unit_price)`. Lưu sẵn giúp danh sách booking không aggregate từng item:\n\n```sql\nALTER TABLE booking\n  ADD COLUMN total_amount numeric(14,2) NOT NULL DEFAULT 0,\n  ADD CONSTRAINT booking_total_nonnegative CHECK (total_amount >= 0);\n```\n\nNguồn chuẩn phải được tuyên bố. Nếu item là nguồn chuẩn, mọi insert, update giá và delete item phải điều chỉnh total. Nếu booking đã confirmed không cho sửa item, có thể tính tổng một lần lúc chuyển trạng thái và đóng băng; invariant đơn giản hơn nhiều. Thiết kế trạng thái thường hiệu quả hơn trigger cố hỗ trợ mọi thay đổi tùy ý.\n\nMột cột tổng giúp query danh sách, nhưng không thay thế item dùng cho hóa đơn chi tiết, hoàn từng vé và audit. Xóa item để “tránh trùng” sẽ mất nguồn rebuild. Phá chuẩn hoá đúng luôn giữ nguồn có độ chi tiết cần thiết.\n\n### Cập nhật tổng trong cùng transaction\n\nLuồng ứng dụng có thể chèn item và tăng tổng nguyên tử:\n\n```sql\nBEGIN;\n\nINSERT INTO booking_item (booking_id, show_id, seat_id, unit_price)\nVALUES (1001, 501, 12, 500000);\n\nUPDATE booking\nSET total_amount = total_amount + 500000\nWHERE booking_id = 1001;\n\nCOMMIT;\n```\n\nRow lock trên booking tuần tự hóa các cập nhật cùng booking, nên biểu thức `total_amount = total_amount + ...` không mất update như cách đọc giá trị ra ứng dụng rồi ghi số tuyệt đối. Nhưng lỗi ứng dụng có thể chạy câu đầu mà quên câu hai nếu không cùng transaction; một code path hoàn tiền mới cũng có thể bỏ sót.\n\nTrigger gom logic về database và bao phủ nhiều client, nhưng làm write path khó nhìn, có thể gây deadlock do thứ tự khóa, và migration bulk phải tính đến hiệu ứng phụ. Stored procedure hoặc API duy nhất cho mutation rõ hơn nếu tổ chức kiểm soát được mọi writer. Không có cơ chế nào miễn phí.\n\n### Kiểm tra drift và rebuild tổng\n\nMột job đối soát phải tìm chênh lệch:\n\n```sql\nSELECT b.booking_id,\n       b.total_amount AS stored_total,\n       coalesce(sum(bi.unit_price), 0) AS computed_total\nFROM booking AS b\nLEFT JOIN booking_item AS bi USING (booking_id)\nGROUP BY b.booking_id, b.total_amount\nHAVING b.total_amount <> coalesce(sum(bi.unit_price), 0);\n```\n\nRebuild có thể dùng CTE aggregate rồi update. Phải quyết định chạy trong transaction nào, khóa ra sao và xử lý ghi đang diễn ra thế nào; rebuild trên snapshot rồi ghi đè có thể mất increment mới. Một cách an toàn là khóa phạm vi cần sửa, hoặc xây bảng mới từ snapshot nhất quán rồi chuyển đổi có kiểm soát. “Có thể tính lại” chưa đủ nếu thủ tục tính lại chưa từng được thử.\n\nCa booking không item minh họa lý do dùng `coalesce`: `sum` của tập rỗng là NULL, trong khi total nghiệp vụ có thể là 0. Ca giá âm cần constraint hoặc mô hình refund riêng; cộng số âm mà không phân biệt loại giao dịch làm audit khó hiểu.\n\n### Dạng hai: counter ghế còn lại của show\n\n`show.available_seat_count` hấp dẫn vì trang danh sách chỉ đọc một số. Nguồn chuẩn có thể là tập `show_seat` trừ các hold/sale còn hiệu lực. Khác tổng booking, counter này chịu contention cao: mọi lượt mua của cùng show cập nhật một hàng show.\n\n```sql\nALTER TABLE show\n  ADD COLUMN available_seat_count integer NOT NULL DEFAULT 0,\n  ADD CONSTRAINT show_available_nonnegative\n    CHECK (available_seat_count >= 0);\n\nUPDATE show\nSET available_seat_count = available_seat_count - 1\nWHERE show_id = 501\n  AND available_seat_count > 0\nRETURNING available_seat_count;\n```\n\nNếu không trả hàng, counter đã bằng 0; ứng dụng không được tiếp tục. Điều kiện và decrement trong một `UPDATE` tránh check-then-act trên counter. Nhưng counter không xác định ghế cụ thể còn trống. Unique constraint trên allocation vẫn là tuyến đúng để ngăn bán A12 hai lần. Counter là tối ưu hiển thị, không được trở thành bảo đảm duy nhất của inventory.\n\n### Hot row và sharded counter\n\nMột show cực nóng khiến mọi transaction tranh row lock trên `show`. Counter chính xác tức thời biến hàng đó thành điểm tuần tự. Có thể chia counter thành nhiều shard, mỗi shard nhận một phần increment/decrement rồi đọc bằng `sum`. Cách này giảm contention ghi nhưng lại tăng chi phí đọc và phức tạp phân bổ.\n\nNếu sản phẩm chỉ cần nhãn “còn vé” gần đúng, cập nhật bất đồng bộ hoặc cache vài giây có thể tốt hơn. Nếu quyết định nhận đơn cuối cùng phụ thuộc số chính xác, phải kiểm nguồn inventory trong transaction. Tách đường hiển thị gần đúng khỏi đường commit chính xác là thiết kế thực dụng.\n\n### Dạng ba: snapshot tên trên hóa đơn\n\nKhông phải mọi dữ liệu lặp nhằm tăng tốc. `invoice.event_title` có thể được lưu để bảo toàn lịch sử: event đổi tên sau khi bán nhưng hóa đơn cũ không đổi. Đây là snapshot theo thời điểm, và nguồn nghĩa của nó sau phát hành chính là hóa đơn, không còn là bản cache phải đồng bộ với event.\n\nĐiểm khác biệt rất quan trọng. Với cache title để hiển thị, thay event title phải lan sang bản sao. Với snapshot pháp lý, lan cập nhật lại là lỗi. Cùng một cặp cột giống nhau có chính sách trái ngược tùy ý nghĩa. Tên cột, comment schema và tài liệu mutation phải làm rõ.\n\n### Materialized view cho báo cáo đọc nhiều\n\nPostgreSQL materialized view lưu kết quả truy vấn và làm mới theo lệnh:\n\n```sql\nCREATE MATERIALIZED VIEW organizer_daily_sales AS\nSELECT e.organizer_id,\n       (b.created_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date AS sale_date,\n       count(DISTINCT b.booking_id) AS booking_count,\n       sum(bi.unit_price) AS gross_amount\nFROM booking AS b\nJOIN booking_item AS bi USING (booking_id)\nJOIN show AS s USING (show_id)\nJOIN event AS e USING (event_id)\nWHERE b.status = 'confirmed'\nGROUP BY e.organizer_id,\n         (b.created_at AT TIME ZONE 'Asia/Ho_Chi_Minh')::date;\n\nCREATE UNIQUE INDEX organizer_daily_sales_pk\n  ON organizer_daily_sales (organizer_id, sale_date);\n```\n\nView này làm báo cáo đơn giản và đọc ít hàng. Đổi lại, dữ liệu cũ giữa hai lần refresh. `REFRESH MATERIALIZED VIEW CONCURRENTLY` cần unique index phù hợp và tránh chặn đọc theo cách refresh thường, nhưng vẫn tiêu thụ tài nguyên để tính lại; không nên ghi lịch refresh giả định mà chưa đo thời lượng.\n\nMaterialized view tích hợp tốt khi độ trễ phút hoặc giờ chấp nhận được và phép tính lại không quá lớn. Với cập nhật liên tục trên tập dữ liệu khổng lồ, bảng aggregate cập nhật gia tăng hoặc hệ thống streaming có thể phù hợp hơn, nhưng vận hành phức tạp hơn đáng kể.\n\n### Read model và cập nhật bất đồng bộ\n\nMột read model có thể chứa `booking_id`, title, show time, venue name, item count và total, tối ưu đúng payload API. Transaction ghi nguồn chuẩn phát sự kiện qua outbox; worker đọc sự kiện rồi upsert read model. Người dùng có thể vừa mua xong nhưng danh sách chưa hiện vài giây, nên giao diện cần đọc kết quả command hoặc hiển thị trạng thái pending.\n\nOutbox giải quyết khoảng trống “database commit nhưng gửi message thất bại” bằng cách ghi sự kiện vào cùng transaction với dữ liệu nguồn. Worker phải idempotent (xử lý lặp không làm sai) vì message có thể được giao lại. Cần lưu event ID hoặc phiên bản aggregate để bỏ bản cũ đến sau bản mới.\n\nĐây là lựa chọn mạnh khi tỷ lệ đọc/ghi lớn và payload ổn định, nhưng không nên dùng cho ứng dụng nhỏ chỉ vì kiến trúc nghe hiện đại. Bạn phải vận hành queue, retry, dead-letter, lag monitoring và rebuild consumer.\n\n### Cache khác bản sao trong database ở điểm nào\n\nCache có TTL và chấp nhận mất; database read model thường được coi là dữ liệu phục vụ có thể rebuild nhưng cần bền hơn. Cache-aside đọc nguồn khi miss, ghi cache, rồi invalidate hoặc để hết hạn. Invalidation vẫn khó: event title đổi phải xóa mọi key danh sách chứa nó, không chỉ key event.\n\nCache phù hợp kết quả đọc lặp, dung sai trễ rõ và không tham gia constraint. Không dùng Redis counter làm nguồn duy nhất bán ghế nếu mất key có thể bán quá số lượng, trừ khi toàn bộ giao thức persistence được thiết kế và chứng minh tương ứng. Cơ sở dữ liệu với unique constraint vẫn nên quyết định allocation cuối.\n\n### Index covering có thể tránh nhu cầu phá chuẩn hoá\n\nNếu endpoint chỉ cần `booking_id`, `status`, `total_amount` mà total đã tính khi confirmed, index `INCLUDE` có thể giảm heap fetch:\n\n```sql\nCREATE INDEX booking_customer_recent_idx\nON booking (customer_id, created_at DESC)\nINCLUDE (status, total_amount);\n```\n\nIndex-only scan chỉ xảy ra khi visibility map cho phép và planner thấy có lợi. Cột include làm index lớn, tăng ghi và cache pressure. Đây là tối ưu vật lý, không phải miễn phí. Dù vậy nó thường ít rủi ro nhất quán hơn sao chép title, venue và hàng loạt trường sang booking.\n\n### Mọi đường ghi phải nằm trong ma trận mutation\n\nVới `available_seat_count`, liệt kê: mở bán seat, mua, hủy, hold hết hạn, chuyển show, import dữ liệu, sửa tay vận hành, replay sự kiện và restore backup. Mỗi dòng phải nêu nguồn thay đổi, phép cập nhật counter, khả năng chạy lặp và test.\n\nThiếu một đường là drift chắc chắn, chỉ chưa biết lúc nào. Đặc biệt bulk migration và script quản trị thường bỏ qua service code. Nếu dùng trigger, chúng vẫn chạy khi backfill trừ khi bị tắt; nếu tắt trigger, phải rebuild sau đó. Quy trình vận hành là một phần của thiết kế dữ liệu.\n\n### Quan sát độ đúng, không chỉ quan sát độ nhanh\n\nDashboard cần metric về lag của consumer, số mismatch đối soát, tuổi materialized view, lỗi unique và thời gian chờ row lock. Tối ưu chỉ theo latency có thể che dữ liệu sai. Cảnh báo nên dựa vào ngưỡng nghiệp vụ: counter âm phải không thể xảy ra nhờ check; drift khác 0 có thể cần cảnh báo ngay hoặc theo tỷ lệ tùy dữ liệu.\n\nĐối soát toàn bảng mỗi phút có thể tự tạo tải lớn. Dùng sample, partition theo ngày, checksum theo bucket hoặc incremental audit để cân bằng. Nhưng phải định kỳ chạy kiểm tra đầy đủ trong cửa sổ phù hợp, đặc biệt trước quyết toán.\n\n### Khi nào tuyệt đối chưa nên phá chuẩn hoá\n\nKhông nên làm khi chưa có measurement, schema và workload còn thay đổi nhanh, đội ngũ chưa có ownership cho job đồng bộ, hoặc dữ liệu tham gia quyết định tài chính mà chưa xác định tolerance. Cũng không nên lưu một cột chỉ để tránh một join khóa chính trên vài hàng.\n\nNếu query chậm vì trả quá nhiều dữ liệu cho client, denormalization không sửa được; cần pagination hoặc đổi API. Nếu chậm vì lock, thêm counter chung có thể làm tệ hơn. Nếu ước lượng planner sai do statistics cũ, refresh statistics và sửa skew trước. Đúng bệnh quan trọng hơn kỹ thuật thời thượng.",

vi_du: "### Thí nghiệm đúng: so plan trước và sau trên chính dữ liệu của bạn\n\nTạo truy vấn chuẩn và truy vấn dùng cột tổng, nhưng không ghi con số khi chưa chạy môi trường mục tiêu:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS, WAL)\nSELECT b.booking_id, coalesce(sum(bi.unit_price), 0) AS total\nFROM booking AS b\nLEFT JOIN booking_item AS bi USING (booking_id)\nWHERE b.customer_id = 7\nGROUP BY b.booking_id;\n\nEXPLAIN (ANALYZE, BUFFERS, WAL)\nSELECT booking_id, total_amount\nFROM booking\nWHERE customer_id = 7;\n```\n\nChạy nhiều lần tách cache lạnh/ấm, giữ cùng snapshot dữ liệu và kiểm kết quả hai câu bằng nhau. Đọc `actual rows` so với `rows` ước lượng, loại scan, buffer hit/read và thời gian aggregate. `WAL` hữu ích hơn khi đo write path; với SELECT có thể không phát sinh WAL.\n\nSau đó đo chi phí ghi:\n\n```sql\nBEGIN;\nEXPLAIN (ANALYZE, BUFFERS, WAL)\nUPDATE booking\nSET total_amount = total_amount + 500000\nWHERE booking_id = 1001;\nROLLBACK;\n```\n\nRollback giữ dữ liệu thử nghiệm nhưng câu lệnh vẫn lấy lock và tạo công việc nội bộ; không chạy tùy tiện trên production. Con số phụ thuộc phần cứng, cache và concurrency nên bài không công bố benchmark giả.\n\n### Rebuild `total_amount` có thể kiểm chứng\n\n```sql\nWITH totals AS (\n  SELECT b.booking_id, coalesce(sum(bi.unit_price), 0) AS amount\n  FROM booking AS b\n  LEFT JOIN booking_item AS bi USING (booking_id)\n  GROUP BY b.booking_id\n)\nUPDATE booking AS b\nSET total_amount = t.amount\nFROM totals AS t\nWHERE b.booking_id = t.booking_id\n  AND b.total_amount IS DISTINCT FROM t.amount;\n```\n\n`IS DISTINCT FROM` xử lý NULL an toàn, dù constraint hiện không cho NULL. Điều kiện chỉ update hàng lệch, giảm WAL. Trước khi chạy thật phải xác định cách ngăn mutation đồng thời; trong maintenance window có thể khóa bảng, còn hệ thống luôn hoạt động cần chiến lược version hoặc đối soát lặp đến khi hội tụ.\n\n### Ca biên của counter\n\nThử decrement khi bằng 0, hoàn hai lần và sự kiện bị hủy. `CHECK >= 0` ngăn âm nhưng không ngăn hoàn hai lần làm counter vượt sức chứa. Cần idempotency key cho mutation và constraint `available_seat_count <= capacity` nếu capacity đáng tin, hoặc tốt hơn đối chiếu từ allocation. Mỗi bảo vệ chỉ chặn một lớp sai; không nên diễn giải check đơn giản thành toàn vẹn hoàn chỉnh.",

so_sanh: "### Chọn cơ chế theo độ tươi và chi phí ghi\n\n| Cơ chế | Độ tươi | Chi phí/độ phức tạp ghi | Khả năng rebuild | Hợp với |\n|---|---|---|---|---|\n| aggregate trực tiếp khi đọc | tức thời | thấp ở write, cao ở read | không cần | ít dữ liệu hoặc ít lượt đọc |\n| cột tổng cập nhật cùng transaction | tức thời sau commit | lock và thêm write | từ bảng chi tiết | total booking, invariant hẹp |\n| materialized view | trễ theo lịch refresh | refresh theo lô | tạo lại từ query | dashboard chấp nhận trễ |\n| read model qua outbox | trễ theo consumer lag | queue, retry, idempotency | replay/backfill | tỷ lệ đọc cao, payload ổn định |\n| cache TTL | có thể cũ hoặc mất | invalidation | nạp lại từ nguồn | dữ liệu hiển thị không quyết định giao dịch |\n| snapshot lịch sử | bất biến theo sự kiện | ghi một lần | từ chứng từ/sự kiện nếu còn | hóa đơn, giá chốt |\n\n### Index hay dữ liệu dư\n\n| Câu hỏi | Ưu tiên index | Cân nhắc dữ liệu dư |\n|---|---|---|\n| Có đang quét hàng không liên quan? | có, index giảm phạm vi scan | chưa cần |\n| Vẫn phải aggregate rất nhiều hàng khớp? | index không loại phép tính | tổng hợp trước có thể thắng |\n| Kết quả phải đúng ngay sau commit? | index giữ một nguồn chuẩn | cập nhật đồng bộ, chấp nhận contention |\n| Chấp nhận trễ vài phút? | vẫn có thể đủ | materialized view/read model phù hợp |\n| Có quy trình rebuild và owner? | không bắt buộc bản sao | là điều kiện gần như bắt buộc |\n\nMột hệ thống thường phối hợp: schema 3NF làm nguồn chuẩn, index cho truy vấn giao dịch, materialized view cho báo cáo và snapshot cho hóa đơn. Gọi tất cả là “phá chuẩn hoá” mà không phân biệt nghĩa sẽ dẫn đến chính sách cập nhật sai.",

loi_thuong_gap: [
  "**Phá chuẩn hoá trước khi đo.** Không có query plan, tần suất và mục tiêu độ trễ; hãy sửa query/index rồi benchmark trên dữ liệu đại diện trước.",
  "**Không chỉ định nguồn chuẩn.** Khi total khác tổng item, đội vận hành không biết số nào thắng; tài liệu phải nêu authoritative source và rebuild.",
  "**Cập nhật counter bằng đọc–sửa–ghi ở ứng dụng.** Hai transaction mất increment; dùng phép `SET x = x + delta` có điều kiện trong transaction.",
  "**Dùng counter làm bảo đảm bán ghế duy nhất.** Counter đúng tổng nhưng vẫn có thể bán A12 hai lần; giữ unique constraint trên allocation cụ thể.",
  "**Đồng bộ snapshot lịch sử với dữ liệu hiện tại.** Hóa đơn cũ đổi tên theo event; phân biệt snapshot bất biến với cache cần invalidation.",
  "**Bỏ sót đường hủy, hoàn hoặc timeout.** Drift xuất hiện sau các luồng hiếm; lập ma trận tất cả mutation và test idempotency.",
  "**Có job rebuild nhưng chưa xét ghi đồng thời.** Job ghi đè increment mới; dùng snapshot/lock/version và kiểm thử quy trình phục hồi.",
  "**Tối ưu read nhưng không đo contention write.** Hot row ở show làm transaction chờ lock; quan sát lock wait và cân nhắc độ chính xác gần đúng."
],

tu_kiem_tra: [
  "Một index trên `booking_item(booking_id)` giải quyết phần nào của truy vấn tổng tiền và phần nào nó không thể giải quyết?",
  "Nêu năm thành phần bắt buộc của hợp đồng cho một trường dữ liệu dư.",
  "Vì sao `SET total = total + delta` an toàn hơn đọc total ra ứng dụng rồi ghi lại, và nó vẫn chưa bảo vệ khỏi lỗi nào?",
  "Phân biệt cache title sự kiện với snapshot title trên hóa đơn bằng chính sách cập nhật.",
  "Khi nào materialized view phù hợp hơn read model cập nhật qua outbox?",
  "Câu khó: counter ghế chính xác tạo hot row. Hãy đề xuất hai thiết kế giảm contention và nêu cái giá về độ tươi hoặc chi phí đọc.",
  "Một job đối soát phát hiện drift nhưng hệ thống vẫn ghi liên tục. Bạn sẽ rebuild thế nào để không ghi đè thay đổi mới?",
  "Tại sao `CHECK available_seat_count >= 0` không ngăn hoàn tiền hai lần làm counter vượt sức chứa?"
],

tai_nguyen: [
  "Sách *Designing Data-Intensive Applications*.",
  "Sách *Database Internals*.",
  "Tài liệu PostgreSQL, phần Using EXPLAIN, Indexes và Materialized Views.",
  "Sách *SQL Performance Explained*."
],

lien_he: [
  "Mảng 01 về concurrency giải thích lost update, row lock và contention khi nhiều transaction cùng sửa counter.",
  "Mảng 02 về cấu trúc dữ liệu nối sang B-tree, cache và chi phí duy trì thêm index trên write path.",
  "Mảng 06 về kiến trúc backend sẽ dùng outbox, idempotency và read model để đồng bộ dữ liệu dư bất đồng bộ.",
  "Mảng 07 về data engineering mở rộng materialized aggregate thành pipeline, warehouse và kiểm tra chất lượng dữ liệu theo lô."
]

};
