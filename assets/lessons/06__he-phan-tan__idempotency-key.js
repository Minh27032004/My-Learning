window.LESSONS["06:he-phan-tan:idempotency-key"] = {

tom_tat: [
  "Idempotency key gắn nhiều lần giao cùng một ý định nghiệp vụ vào một kết quả đã lưu, đặc biệt khi client timeout và không biết server đã commit hay chưa.",
  "Server phải lưu key cùng phạm vi, fingerprint của payload, trạng thái xử lý và response; chỉ “nếu thấy key thì bỏ qua” vừa thiếu kết quả vừa dễ che request khác nội dung.",
  "Tính nguyên tử giữa đăng ký key và tác động nghiệp vụ là lõi. Check-then-act trong hai câu lệnh rời vẫn có thể thu tiền hoặc giữ ghế hai lần dưới concurrency.",
  "TTL, retry, crash recovery, tác động ngoài database và quan sát vận hành đều là một phần của hợp đồng; key không tạo exactly-once delivery trên mạng."
],

can_biet_truoc: [
  "HTTP POST, timeout và retry với exponential backoff.",
  "Transaction database, unique constraint và trạng thái commit.",
  "At-least-once delivery của queue và khả năng giao message lặp.",
  "Invariant của nền tảng vé: một ý định thanh toán chỉ được tạo một charge nghiệp vụ."
],

dinh_nghia: "**Idempotency key** là định danh do client tạo cho một **ý định nghiệp vụ**. Khi server nhận lại cùng key trong cùng phạm vi và cùng payload, nó không thực hiện tác động lần nữa mà trả lại kết quả tương thích với lần xử lý đầu. Ví dụ `checkout:customer-17:7f...` đại diện cho một lần bấm thanh toán đơn vé, không đại diện vĩnh viễn cho khách hàng.\n\nMột phép toán idempotent thỏa trực giác `f(f(x)) = f(x)`: áp dụng thêm không đổi trạng thái sau lần đầu. POST thu tiền vốn không idempotent; lớp key biến nhiều lần vận chuyển request thành một lần thực thi logic đã định danh.\n\nĐây là **exactly-once effect** trong một ranh giới lưu trữ được thiết kế, không phải exactly-once network delivery. Gói tin vẫn có thể tới nhiều lần, response vẫn có thể mất, và tác động ở dịch vụ ngoài cần cơ chế riêng hoặc cùng key được bên ngoài tôn trọng.",

vi_sao_can: "Khách bấm thanh toán. Dịch vụ vé gọi cổng thanh toán, ghi đơn và trả response; đúng lúc đó kết nối di động mất. Client chỉ biết timeout. Nếu không retry, khách có thể đã bị trừ tiền nhưng không thấy vé. Nếu retry như một POST mới, hệ thống có thể tạo charge thứ hai. Không có lựa chọn phía client nào suy ra được sự thật từ im lặng.\n\nRetry là hành vi bình thường của mobile app, proxy, SDK, queue và operator chạy lại job. Ở 80 checkout/giây, chỉ 0,5% response không tới cũng tạo 24 trường hợp không xác định mỗi phút. Đây là con số kịch bản để capacity planning, không phải benchmark. Một quy trình hỗ trợ thủ công cho từng trường hợp sẽ không mở rộng.\n\nIdempotency key đưa “lần thử này có cùng ý định với lần trước không?” vào protocol. Server có thể trả cùng `order_id`, client có thể tra trạng thái, và log có một trục tương quan. Nhưng nếu thiết kế bảng key sai, cơ chế tạo cảm giác an toàn giả; do đó bài này tập trung vào nguyên tử, fingerprint và vòng đời.",

co_che: "### Key định danh ý định, không định danh endpoint chung chung\n\nClient sinh UUID ngẫu nhiên cho mỗi lần người dùng xác nhận checkout. Khi retry vì timeout, nó giữ nguyên key. Khi người dùng chủ động tạo đơn mới, kể cả payload giống, nó sinh key mới. Dùng `customer_id` làm key sẽ khóa khách sau giao dịch đầu; dùng timestamp giây có thể va chạm; dùng hash payload không phân biệt hai lần mua hợp lệ giống hệt.\n\nKey cần đủ entropy để không đoán/va chạm thực tế và nên gắn namespace. Server không cần tin key mang nghĩa; nó vẫn xác thực user và quyền với tài nguyên. Key không phải credential.\n\n### Phạm vi uniqueness phải được viết rõ\n\n`abc` của khách Minh không nhất thiết xung đột `abc` của Lan. Khóa database có thể là `(tenant_id, endpoint, idempotency_key)`. Nếu cùng key được dùng cho `/holds` và `/payments`, namespace endpoint ngăn response loại này bị trả cho loại kia.\n\nPhạm vi quá rộng gây false conflict và bảng lớn; quá hẹp cho phép tác động lặp qua hai route tương đương. Nếu API v1 và v2 cùng tạo charge, chúng nên chia một operation namespace hoặc ánh xạ vào cùng command ID.\n\n### Fingerprint chặn tái sử dụng key với nội dung khác\n\nLần đầu key K mang `{order_id: 42, amount: 900000}`. Lần hai K mang amount 990000. Server không được âm thầm trả response cũ hay chạy payload mới. Nó trả `409 Conflict` hoặc lỗi xác định “key reused with different request”.\n\nFingerprint nên tạo từ biểu diễn canonical của các trường ảnh hưởng nghiệp vụ, không từ raw JSON: thứ tự key, khoảng trắng và header vô nghĩa không nên làm khác hash. Ngược lại, bỏ currency hoặc merchant khỏi fingerprint là lỗi bảo mật/nghiệp vụ. Không lưu dữ liệu bí mật thô nếu hash hoặc trường chọn lọc đủ phục vụ so sánh.\n\n### Bản ghi idempotency là một state machine\n\nMột schema thực dụng có `scope`, `key`, `request_hash`, `status`, `resource_id`, `response_code`, `response_body`, `created_at`, `expires_at`. Trạng thái tối thiểu:\n\n```text\nABSENT -> IN_PROGRESS -> SUCCEEDED\n                     -> FAILED_FINAL\n                     -> RECOVERABLE / cần đối soát\n```\n\nKhông phải lỗi nào cũng được cache. Lỗi validation trước tác động có thể trả lại ổn định; lỗi 500 tạm thời trước commit có thể cho phép owner retry; lỗi không biết cổng thanh toán đã charge chưa phải vào trạng thái đối soát, không được chạy lại mù.\n\n### Unique constraint thắng check-then-act\n\nHai request K tới đồng thời. Nếu cả hai chạy `SELECT` không thấy rồi cùng charge, việc `INSERT` sau đó có unique constraint chỉ phát hiện quá muộn. Đăng ký ownership phải là thao tác nguyên tử trước tác động: `INSERT ... ON CONFLICT`, hoặc transaction khóa hàng.\n\nNgười thắng xử lý; người thua đọc trạng thái. Nếu trạng thái `IN_PROGRESS`, nó có thể chờ ngắn, trả `202 Accepted` kèm URL tra cứu, hoặc `409/425` theo hợp đồng. Không nên để cả hai worker tiếp tục.\n\n### Gộp key và thay đổi database trong một transaction\n\nNếu tạo hold và lưu idempotency record cùng database, transaction có thể: chèn key, kiểm ghế, tạo hold, cập nhật record `SUCCEEDED`, rồi commit. Crash trước commit để lại không gì; retry làm lại. Crash sau commit để lại cả hold lẫn response đã lưu; retry trả lại.\n\nĐây là điểm mạnh nhất vì loại bỏ khe “tác động xong nhưng chưa ghi key”. Response body có thể lớn; thường lưu resource ID và dữ liệu đủ để tái dựng response ổn định, nhưng phải cân nhắc resource sau đó đã thay đổi. Nếu hợp đồng nói replay trả đúng response ban đầu, đọc resource hiện tại có thể sai.\n\n### Response replay phải có ngữ nghĩa rõ\n\nLần đầu trả `201 Created`, order 731. Lần retry có thể trả lại 201 cùng body, hoặc 200 biểu thị đã tồn tại; cả hai được nếu SDK và tài liệu thống nhất. Trả một order khác là sai. Header như `Idempotent-Replayed: true` giúp debug nhưng không nên buộc client dựa vào nó để đúng.\n\nNếu response đầu chứa URL ký ngắn hạn đã hết hạn, lưu nguyên body gây trải nghiệm hỏng. Tách phần quyết định ổn định (`order_id`, amount, status tại commit) khỏi artifact tạm thời; phát URL tải mới mà không tái thực hiện charge.\n\n### TTL là cửa sổ bảo đảm, không chỉ dọn đĩa\n\nXóa key sau 24 giờ nghĩa retry sau 24 giờ có thể chạy lại. Client offline hai ngày rồi tự replay queue sẽ phá kỳ vọng nếu không biết cửa sổ. TTL phải dài hơn retry horizon của client, proxy, queue và quy trình vận hành, cộng biên clock.\n\nGiữ vĩnh viễn làm bảng phình và tạo rủi ro dữ liệu cá nhân. Có thể giữ bản ghi nghiệp vụ/order lâu dài, còn payload/response chi tiết ngắn hơn. Với thanh toán, provider transaction ID và ledger thường là lớp dedupe dài hạn hơn cache key API.\n\n### In-progress bị bỏ rơi sau crash\n\nWorker chèn `IN_PROGRESS` rồi chết. Nếu mọi retry mãi nhận “đang xử lý”, key bị kẹt. Bản ghi cần owner/lease, heartbeat hoặc thời điểm `locked_until`. Sau expiry, worker mới chỉ takeover nếu có thể xác định tác động chưa xảy ra hoặc tiếp tục từ checkpoint an toàn.\n\nKhông nên đơn giản đổi `IN_PROGRESS` cũ thành absent rồi gọi charge lại. Trước hết truy vấn provider bằng merchant reference chính là key. Nếu provider thấy charge, hoàn tất record; nếu chắc chắn không thấy và lookup có tính nhất quán phù hợp, mới retry.\n\n### Tác động ngoài transaction tạo dual-write\n\nDatabase không thể commit nguyên tử cùng API cổng thanh toán thông thường. Các thứ tự đều có khe lỗi: charge trước rồi crash trước ghi DB; ghi DB trước rồi charge thất bại. Dùng cùng idempotency key làm merchant reference ở provider thu hẹp khe: provider cũng dedupe charge.\n\nSau đó lưu trạng thái payment attempt và chạy reconciliation. Một lựa chọn khác là transaction outbox: commit ý định thanh toán cùng order, worker gửi ra ngoài at-least-once với key, rồi cập nhật kết quả. Outbox ngăn mất ý định nhưng không tự ngăn provider lặp nếu provider không hỗ trợ dedupe.\n\n### Inbox deduplication cho message consumer\n\nQueue có thể giao event `PaymentCaptured` nhiều lần. Consumer lưu `(consumer_name, message_id)` với thay đổi nghiệp vụ trong cùng transaction. Nếu message ID đã có, nó bỏ tác động và acknowledge. Đây là cùng pattern nhưng phạm vi là consumer, không phải HTTP client.\n\nNếu đánh dấu processed rồi mới cập nhật order, crash làm mất tác động. Nếu cập nhật order rồi mới đánh dấu ở transaction khác, crash làm lặp. Cùng database transaction là phần quyết định, không phải tên bảng inbox.\n\n### Natural idempotency và absolute assignment\n\n`SET profile.name = 'Minh'` tự nhiên idempotent hơn `INCREMENT balance BY 10`. Gửi trạng thái tuyệt đối với version đôi khi đơn giản hơn lưu key cho mọi request. HTTP PUT thường được định nghĩa idempotent theo intended effect, còn POST không mặc nhiên không-idempotent; ngữ nghĩa endpoint mới quyết định.\n\nXóa một tài nguyên hai lần có thể lần đầu 204, lần sau 404 nhưng trạng thái cuối giống nhau. Idempotent không nhất thiết response từng lần giống hệt. Idempotency-key API thanh toán thường chọn replay response để client giải ambiguity, là lời hứa mạnh hơn chỉ trạng thái cuối.\n\n### Optimistic concurrency không thay idempotency\n\nVersion check `UPDATE ... WHERE version=5` ngăn hai actor cùng sửa từ version 5, nhưng một request timeout retry với version mới có thể vẫn tạo operation khác nếu client đọc lại. Idempotency gộp cùng ý định; optimistic concurrency ngăn lost update giữa các ý định cạnh tranh. Nền tảng vé cần cả unique seat invariant, version và key ở các ranh giới khác nhau.\n\n### Security: key không được mở đường rò response\n\nAttacker đoán key của người khác không được nhận body order. Lookup luôn nằm sau authentication và scope tenant/user. So sánh fingerprint không thay authorization. Log key có thể dùng correlation nhưng tránh ghi payload/card data.\n\nMột client độc hại có thể gửi hàng triệu key duy nhất để làm đầy bảng. Rate limiting, quota theo tenant, giới hạn độ dài key và TTL bảo vệ tài nguyên. Key dài không kiểm soát cũng gây index bloat.\n\n### Hot key và concurrent storm\n\nMobile SDK lỗi có thể gửi 1.000 retry cùng K. Tất cả đập vào một hàng khóa, làm connection pool nghẽn. Single-flight trong tiến trình, cache trạng thái terminal, backoff và trả 202 giảm áp lực. Tuy nhiên cache không được là nguồn duy nhất vì instance restart sẽ quên.\n\nĐặt một mutex phân tán cho K rồi bỏ database uniqueness vẫn nguy hiểm: lease hết khi worker còn chạy tạo hai owner. Unique constraint và state transition có điều kiện là nền an toàn; khóa chỉ tối ưu contention.\n\n### Multi-region và nơi sở hữu key\n\nNếu cùng key tới Singapore và Frankfurt trong partition, hai database cục bộ có thể cùng nhận ownership. Với charge, registry idempotency phải nằm trong một consistency domain mạnh, hoặc request được route theo key tới một home region có fencing. Consistent hashing ở bài sau có thể chọn home, nhưng rebalancing cần migration an toàn.\n\nChấp nhận eventual merge cho bảng idempotency là vô nghĩa nếu tác động ngoài đã lặp. Đây là ví dụ rõ về invariant khiến ta chọn consistency thay vì availability cho đường thanh toán.\n\n### API tra trạng thái giải response bị mất\n\n`POST /payments` với K có thể trả 202 và `GET /operations/K`. Client timeout thì hỏi trạng thái trước khi quyết định. Endpoint trả `processing`, `succeeded` với resource ID, `failed_final`, hoặc `needs_review`. Không dùng 404 ngay sau timeout để kết luận absent nếu registry có replication lag.\n\nStatus API cũng cần authorization và retention khớp TTL. Nó làm uncertainty hiện hữu thay vì ép client suy đoán từ mã mạng.\n\n### Khi nào không nên thêm idempotency key\n\nGET thuần đọc và PUT gán trạng thái tuyệt đối có thể đã idempotent. Thêm bảng key cho từng telemetry event chi phí cao hơn giá trị nếu đếm gần đúng chấp nhận lặp. Với operation cục bộ không retry và không có tác động, unique business key có thể đủ.\n\nNgược lại, nói “frontend đã disable nút” không phải lý do bỏ key: double click chỉ là một nguồn lặp; timeout, proxy và job replay vẫn tồn tại. Chọn dựa trên hậu quả lặp và ambiguity, không dựa trên UI.\n\n### Quan sát, đối soát và cảnh báo\n\nMetric nên có tỷ lệ request replay, conflict fingerprint, thời gian ở `IN_PROGRESS`, takeover, số operation `needs_review`, dung lượng bảng và tuổi record. Replay tăng đột biến có thể báo mạng xấu hoặc SDK bug. Conflict fingerprint có thể báo client tái sử dụng key sai.\n\nTrace phải nối các attempt bằng key nhưng vẫn có request ID riêng cho từng lần vận chuyển. Dashboard chỉ đếm HTTP request sẽ tưởng traffic/charge cao; dashboard nghiệp vụ phải đếm operation terminal duy nhất.\n\n### Kịch bản fault injection bắt buộc\n\nChạy hai request cùng K đồng thời; kill process sau provider charge nhưng trước DB update; làm mất response sau commit; giao message hai lần; để `IN_PROGRESS` hết lease; retry sau TTL; gửi cùng key khác amount; làm replica registry chậm; và thử payload JSON khác thứ tự trường.\n\nTiêu chí: tối đa một charge theo ý định, mọi retry cùng payload tìm được cùng order, payload khác bị từ chối, trạng thái kẹt được phục hồi có kiểm chứng, và không user nào đọc response của user khác.",

vi_du: "### Cài đặt SQLite tối giản nhưng nguyên tử\n\nĐoạn Python sau chạy với thư viện chuẩn. Transaction `BEGIN IMMEDIATE` và primary key khiến hai request cùng key không cùng tạo order:\n\n```python\nimport hashlib\nimport json\nimport sqlite3\n\ndb = sqlite3.connect(\":memory:\")\ndb.execute(\"CREATE TABLE idem (scope TEXT, key TEXT, hash TEXT, status TEXT, order_id INTEGER, PRIMARY KEY(scope,key))\")\ndb.execute(\"CREATE TABLE orders (id INTEGER PRIMARY KEY, customer TEXT, seat TEXT UNIQUE)\")\n\ndef fingerprint(payload):\n    canonical = json.dumps(payload, sort_keys=True, separators=(\",\", \":\"))\n    return hashlib.sha256(canonical.encode()).hexdigest()\n\ndef create_order(scope, key, payload):\n    h = fingerprint(payload)\n    db.execute(\"BEGIN IMMEDIATE\")\n    row = db.execute(\n        \"SELECT hash,status,order_id FROM idem WHERE scope=? AND key=?\",\n        (scope, key),\n    ).fetchone()\n    if row:\n        db.commit()\n        if row[0] != h:\n            return 409, {\"error\": \"KEY_REUSED_WITH_DIFFERENT_PAYLOAD\"}\n        return 200, {\"order_id\": row[2], \"replayed\": True}\n\n    db.execute(\"INSERT INTO idem VALUES (?,?,?,'IN_PROGRESS',NULL)\", (scope, key, h))\n    cur = db.execute(\"INSERT INTO orders(customer,seat) VALUES (?,?)\", (payload[\"customer\"], payload[\"seat\"]))\n    db.execute(\"UPDATE idem SET status='SUCCEEDED',order_id=? WHERE scope=? AND key=?\", (cur.lastrowid, scope, key))\n    db.commit()\n    return 201, {\"order_id\": cur.lastrowid, \"replayed\": False}\n\np = {\"customer\": \"Minh\", \"seat\": \"A12\"}\nfirst = create_order(\"checkout\", \"k-7\", p)\nagain = create_order(\"checkout\", \"k-7\", p)\nconflict = create_order(\"checkout\", \"k-7\", {\"customer\": \"Lan\", \"seat\": \"A13\"})\nassert first[0] == 201\nassert again[1][\"order_id\"] == first[1][\"order_id\"]\nassert conflict[0] == 409\nassert db.execute(\"SELECT COUNT(*) FROM orders\").fetchone()[0] == 1\n```\n\nSQLite in-memory tuần tự hóa connection này nên chưa mô phỏng hai process; unique primary key vẫn biểu diễn invariant cần có. Production phải xử lý exception, rollback, isolation và pool connection.\n\n### Khe lỗi khi gọi dịch vụ ngoài\n\n```python\ndef unsafe_charge(provider, db, key, amount):\n    charge = provider.charge(amount)   # thành công\n    # process có thể chết tại đây\n    db.save(key, charge.id)            # chưa chắc chạy\n```\n\nSửa không phải đảo hai dòng; đảo lại tạo khe database có “đã charge” khi provider chưa nhận. Hướng đúng là `provider.charge(amount, idempotency_key=key)` nếu provider hỗ trợ, lưu attempt, và có worker đối soát. Ca biên provider timeout cần query theo merchant reference, không lập tức gọi lại bằng key mới.",

so_sanh: "### Các cơ chế chống lặp giải những lớp khác nhau\n\n| Cơ chế | Ngăn điều gì | Không tự giải được |\n|---|---|---|\n| Idempotency key | Nhiều attempt của cùng ý định | Hai ý định khác cùng tranh ghế |\n| Unique business constraint | Hai hàng vi phạm invariant, như cùng seat | Response timeout và replay kết quả |\n| Optimistic concurrency | Lost update giữa các phiên bản | Tác động ngoài DB sau timeout |\n| Queue dedup/inbox | Message ID được consumer xử lý lặp | Provider ngoài không dedupe |\n| Distributed lock | Giảm concurrent execution khi lease còn hiệu lực | Crash, lease expiry, replay lâu sau đó |\n| Transaction outbox | Không mất event sau DB commit | Exactly-once effect tại consumer |\n\n### Idempotency với “exactly once”\n\nExactly-once delivery thường là cách nói gây hiểu nhầm: sender không thể biết response mất hay request mất. Ta xây at-least-once delivery cộng deduplication, transaction và state machine để đạt một tác động nghiệp vụ quan sát được một lần trong phạm vi. Phải nêu phạm vi: một database, một consumer hay cả cổng thanh toán; càng rộng, giao thức và đối soát càng nhiều.",

loi_thuong_gap: [
  "**Check rồi mới insert key.** Hai request đồng thời đều thấy absent và cùng thực thi; dùng unique constraint/conditional insert trước tác động.",
  "**Không so fingerprint.** Client dùng lại key với amount khác mà nhận response cũ; canonicalize và hash trường nghiệp vụ.",
  "**Đánh dấu processed trước tác động ở transaction khác.** Crash làm message không bao giờ được áp dụng; gộp inbox và thay đổi cùng transaction.",
  "**Xóa key sớm hơn retry horizon.** Request offline quay lại thành charge mới; công bố và phối hợp TTL.",
  "**Cho `IN_PROGRESS` sống vĩnh viễn hoặc takeover mù.** Một phía làm kẹt, phía kia làm lặp; dùng lease cùng lookup/đối soát.",
  "**Coi key là authentication.** Đoán được key có thể lộ order nếu lookup không scope theo user; luôn kiểm quyền trước replay.",
  "**Tin distributed lock thay cho dữ liệu bền.** Lease hết khi worker pause vẫn tạo hai owner; khóa chỉ tối ưu, constraint/state transition mới bảo vệ.",
  "**Sinh key mới mỗi lần retry.** Metric thấy nhiều request “duy nhất” và dedupe vô dụng; SDK phải giữ key cho trọn một ý định."
],

tu_kiem_tra: [
  "Vì sao unique constraint được insert sau khi charge không ngăn double charge dưới concurrency?",
  "Những trường nào phải nằm trong fingerprint của payment và trường nào nên loại khỏi canonical representation?",
  "Worker chết với trạng thái `IN_PROGRESS` sau provider timeout; quy trình takeover an toàn gồm các bước nào?",
  "TTL 24 giờ xung đột thế nào với mobile queue có thể replay sau ba ngày?",
  "Giải thích vì sao transaction outbox bảo đảm không mất ý định nhưng chưa bảo đảm provider chỉ charge một lần.",
  "Thiết kế scope key cho hệ multi-tenant có API v1 và v2 cùng tạo payment.",
  "Khi nào PUT đã đủ idempotent và khi nào vẫn cần key để replay đúng response?",
  "Trong multi-region partition, consistent hashing theo key giúp gì và chưa giải quyết điều gì khi home region đổi?"
],

tai_nguyen: [
  "Designing Data-Intensive Applications — Martin Kleppmann.",
  "Release It! — Michael T. Nygard.",
  "Enterprise Integration Patterns — Gregor Hohpe và Bobby Woolf.",
  "Site Reliability Engineering — Google."
],

lien_he: [
  "Nối sang mảng Cơ sở dữ liệu: unique constraint, transaction isolation và outbox là nền để biến key thành invariant bền.",
  "Nối sang mảng Mạng máy tính: timeout tạo kết quả không xác định vì client không phân biệt request mất với response mất.",
  "Nối sang mảng Bảo mật: authentication, tenant scope, quota và chống replay độc hại bao quanh cơ chế retry hợp lệ.",
  "Nối sang mảng Kiểm thử: concurrency test và fault injection tại ranh giới commit tìm được lỗi mà unit test gọi API một lần không thấy.",
  "Nối sang mảng Quan sát hệ thống: correlation theo operation key, trạng thái pending và reconciliation phân biệt số attempt kỹ thuật với số giao dịch nghiệp vụ."
]

};
