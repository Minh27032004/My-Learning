window.LESSONS["01:mang-may-tinh:cookie-session-va-trang-thai-tren-web"] = {

tom_tat: [
  "**Cookie là cơ chế, session là mô hình.** Cookie chỉ là một cặp header `Set-Cookie` / `Cookie`; session là cách dùng cookie để server nhớ được bạn.",
  "**Hai hướng lưu trạng thái**: giữ dữ liệu **trên server** (session ID) hoặc giữ **trong chính token** (JWT). Đánh đổi cốt lõi là *thu hồi được ngay* so với *không cần tra cứu*.",
  "**Ba cờ cookie là bắt buộc, không phải tuỳ chọn**: `HttpOnly` chặn JavaScript đọc, `Secure` bắt buộc HTTPS, `SameSite` chống CSRF.",
  "**Cookie tự động gửi kèm mọi request tới đúng domain** — tiện lợi này chính là gốc rễ của CSRF, và là lý do `SameSite` ra đời.",
  "**Đừng lưu token xác thực vào `localStorage`.** Nó đọc được bằng JavaScript, nên một lỗ hổng XSS là mất toàn bộ tài khoản người dùng."
],

can_biet_truoc: [
  "Bài **HTTP: method, header, body, status code** — cookie là header, và tính không trạng thái của HTTP là vấn đề mà bài này giải quyết.",
  "Bài **HTTPS, TLS handshake** ngay trước — cờ `Secure` chỉ có nghĩa khi có HTTPS.",
  "Bài **IP, port, DNS** — phạm vi cookie theo domain và subdomain dựa trên cấu trúc tên miền.",
  "Bài **Hệ thống file, đường dẫn, quyền truy cập** ở module 2 — không bắt buộc, nhưng khái niệm phạm vi theo đường dẫn tương tự thuộc tính `Path` của cookie."
],

dinh_nghia: "Nói đời thường: HTTP giống như **một nhân viên tiếp tân bị mất trí nhớ sau mỗi câu**. Cách khắc phục: đưa cho khách một **số thẻ**, và yêu cầu khách chìa thẻ ra mỗi lần nói. Tiếp tân tra số thẻ trong sổ để biết khách là ai. Cookie là số thẻ; sổ tra là session.\n\nNói chính xác:\n\n- **Cookie** — một mẩu dữ liệu nhỏ mà server yêu cầu trình duyệt lưu, qua header `Set-Cookie`. Trình duyệt **tự động gửi lại** trong header `Cookie` cho mọi request tới domain đó.\n- **Session** — mô hình trong đó server lưu dữ liệu về người dùng ở phía mình, và cookie chỉ chứa một **định danh** (*session ID*) để tra cứu.\n- **Token** — chuỗi tự chứa thông tin. **JWT** (*JSON Web Token*) là dạng phổ biến: dữ liệu nằm ngay trong token, được **ký** để không sửa được.\n- **CSRF** (*Cross-Site Request Forgery*) — tấn công lợi dụng việc cookie tự động gửi kèm: một trang khác khiến trình duyệt của bạn gửi request tới site bạn đang đăng nhập.\n- **XSS** (*Cross-Site Scripting*) — tấn công chèn JavaScript vào trang. Nếu thành công, mọi thứ JavaScript đọc được đều bị lấy.\n\nCần phân biệt ngay ba khái niệm hay bị gộp: **xác thực** (*authentication* — bạn là ai), **phân quyền** (*authorization* — bạn được làm gì), và **quản lý phiên** (*session management* — làm sao nhớ bạn giữa các request). Bài này nói về cái thứ ba, nhưng nó là nền để làm hai cái đầu.",

vi_sao_can: "Đây là chỗ mà một lỗi nhỏ trở thành lỗ hổng bảo mật thật, không phải chỗ để \"làm cho chạy được rồi tính sau\":\n\n- **Mọi ứng dụng có đăng nhập đều cần cái này.** Và cài đặt sai thì hoặc người dùng bị đăng xuất liên tục, hoặc tài khoản bị chiếm.\n- **Thiếu một cờ cookie là một lỗ hổng.** Quên `HttpOnly` biến một lỗ XSS thành mất tài khoản. Quên `SameSite` mở đường cho CSRF. Quên `Secure` là gửi token qua HTTP.\n- **Quyết định kiến trúc thật.** Session trên server hay JWT là lựa chọn ảnh hưởng tới khả năng mở rộng, tới việc đăng xuất có tức thì hay không, và tới độ phức tạp vận hành. Rất khó đổi về sau.\n- **Nguồn của những lỗi khó hiểu nhất.** \"Đăng nhập được ở máy tính, không được ở điện thoại\", \"cứ F5 là mất đăng nhập\", \"đăng xuất rồi mà vẫn vào được\" — mỗi triệu chứng ứng với một nguyên nhân cụ thể trong bài này.\n- **Nền cho phần Auth ở mảng Backend.** OAuth, refresh token, single sign-on đều dựng trên các khái niệm ở đây.\n\nTình huống thật đáng nhớ: bạn xây API, lưu JWT vào `localStorage` vì mọi hướng dẫn trên mạng đều làm vậy. Vài tháng sau có một lỗ XSS trong phần hiển thị bình luận. Kẻ tấn công chèn một dòng JavaScript đọc `localStorage` và gửi token của **mọi người dùng đang mở trang** về server của họ. Đổi mật khẩu không cứu được, vì JWT đã ký thì vẫn hợp lệ tới khi hết hạn. Nếu token nằm trong cookie `HttpOnly`, cùng lỗ XSS đó **không** lấy được token — thiệt hại nhỏ hơn nhiều bậc. Phần cơ chế giải thích chính xác vì sao.",

co_che: "### Cookie hoạt động ra sao\n\nToàn bộ cơ chế chỉ là hai header:\n\n```http\n① Server yêu cầu lưu\nHTTP/1.1 200 OK\nSet-Cookie: session_id=a3f9c1b2; HttpOnly; Secure; SameSite=Lax; Max-Age=3600; Path=/\n\n② Trình duyệt TỰ ĐỘNG gửi lại ở MỌI request tới domain đó\nGET /trang-ca-nhan HTTP/1.1\nHost: example.com\nCookie: session_id=a3f9c1b2\n```\n\nChữ **tự động** là chìa khoá của cả bài. Lập trình viên không phải viết dòng nào để gắn cookie vào request — trình duyệt lo. Đây vừa là tiện lợi lớn nhất, vừa là **lỗ hổng gốc**: nếu một trang web khác khiến trình duyệt của bạn gửi request tới `example.com`, cookie **vẫn được gửi kèm**. Đó chính là CSRF, và nó tồn tại được chỉ vì tính tự động này.\n\n### Các thuộc tính của cookie\n\n| Thuộc tính | Việc của nó | Nếu thiếu |\n|---|---|---|\n| `HttpOnly` | JavaScript **không** đọc được | XSS lấy được token |\n| `Secure` | chỉ gửi qua HTTPS | token đi qua HTTP, nghe được |\n| `SameSite` | hạn chế gửi khi request từ site khác | mở đường CSRF |\n| `Max-Age` / `Expires` | thời hạn sống | thành cookie phiên, mất khi đóng trình duyệt |\n| `Domain` | domain nào nhận được | mặc định: chỉ đúng domain đó |\n| `Path` | đường dẫn nào nhận được | mặc định `/` |\n\n**`HttpOnly`** là cờ có giá trị bảo mật cao nhất và cũng dễ đặt nhất. Nó không ngăn được XSS, nhưng nó **giới hạn thiệt hại** khi XSS xảy ra: kẻ tấn công có thể khiến trình duyệt người dùng gửi request, nhưng **không lấy được token mang đi nơi khác**. Khác biệt giữa \"làm hại trong lúc người dùng còn mở tab\" và \"chiếm tài khoản vô thời hạn\" là rất lớn.\n\n**`SameSite`** có ba giá trị và cần hiểu rõ vì mặc định của trình duyệt đã đổi:\n\n```\nStrict  KHÔNG gửi khi request đến từ site khác, kể cả bấm link\n        → an toàn nhất, nhưng bấm link từ email vào là thấy chưa đăng nhập\n\nLax     Gửi khi ĐIỀU HƯỚNG cấp cao bằng GET (bấm link),\n        KHÔNG gửi với POST hay request nền từ site khác\n        → mặc định hiện nay của các trình duyệt, cân bằng tốt\n\nNone    Luôn gửi. BẮT BUỘC phải kèm Secure.\n        → chỉ dùng khi thật sự cần cookie xuyên site\n```\n\n`Lax` là mặc định tốt cho gần như mọi ứng dụng: nó chặn được dạng CSRF nguy hiểm nhất (`POST` từ site khác) mà không phá trải nghiệm bấm link từ email.\n\n**`Domain`** có một chi tiết ngược trực giác:\n\n```\nSet-Cookie: a=1                        → CHỈ example.com\nSet-Cookie: a=1; Domain=example.com    → example.com VÀ MỌI subdomain\n```\n\nGhi `Domain` tường minh **mở rộng** phạm vi thay vì thu hẹp. Hệ quả bảo mật: nếu bạn có `blog.example.com` do một nền tảng bên thứ ba chạy, cookie đặt với `Domain=example.com` sẽ **được gửi tới nền tảng đó**. Đặt cookie ở phạm vi hẹp nhất mà vẫn dùng được là nguyên tắc đúng.\n\n### Session trên server\n\n```\n① Đăng nhập\nClient: POST /login {email, password}\nServer: kiểm tra mật khẩu ✓\n        sinh session_id ngẫu nhiên: a3f9c1b2...\n        LƯU vào kho:  a3f9c1b2 → {user_id: 42, vai_tro: \"admin\", tao_luc: ...}\n        Set-Cookie: session_id=a3f9c1b2; HttpOnly; Secure; SameSite=Lax\n\n② Mỗi request sau\nClient: GET /don-hang    Cookie: session_id=a3f9c1b2\nServer: TRA KHO theo a3f9c1b2 → biết user_id 42\n        xử lý request\n\n③ Đăng xuất\nServer: XOÁ a3f9c1b2 khỏi kho\n        → session_id đó lập tức vô giá trị\n```\n\nHai tính chất quan trọng của mô hình này:\n\n- **Session ID phải ngẫu nhiên mật mã và đủ dài.** Dùng `random()` thông thường hay dùng số tăng dần là lỗ hổng trực tiếp — đoán được ID là chiếm được phiên. Đây là chỗ phải dùng thư viện sinh số ngẫu nhiên an toàn, không tự nghĩ cách.\n- **Đăng xuất có hiệu lực tức thì**, vì dữ liệu nằm ở server và server xoá được. Đây là ưu điểm lớn nhất của mô hình này.\n\nKho lưu session ở đâu:\n\n| Kho | Ưu | Nhược |\n|---|---|---|\n| Bộ nhớ tiến trình | nhanh nhất, đơn giản | **mất khi restart**, không chia sẻ giữa nhiều server |\n| Redis | nhanh, chia sẻ được, có TTL sẵn | thêm một thành phần phải vận hành |\n| Database | bền, không thêm thành phần | chậm hơn, thêm tải cho DB |\n\nChi tiết vận hành đáng nhớ: lưu session trong bộ nhớ tiến trình thì **mỗi lần deploy là mọi người bị đăng xuất**. Đây là lý do gần như mọi hệ thống thật dùng Redis — nó tồn tại độc lập với vòng đời của tiến trình ứng dụng, và có TTL sẵn nên session tự hết hạn mà không cần cron dọn dẹp.\n\n### JWT — trạng thái nằm trong token\n\nJWT gồm ba phần nối bằng dấu chấm:\n\n```\neyJhbGciOiJIUzI1NiJ9  .  eyJzdWIiOiI0MiIsImV4cCI6MTc...  .  SflKxwRJSMeKKF2QT4f\n└─── header ────────┘     └──── payload ──────────────┘     └─── chữ ký ─────┘\n     thuật toán ký              dữ liệu                        xác minh toàn vẹn\n```\n\nĐiều **tối quan trọng** và bị hiểu sai thường xuyên:\n\n```\nHeader và payload chỉ được MÃ HOÁ BASE64URL — đây KHÔNG phải mã hoá.\nBất kỳ ai cũng giải ra đọc được nội dung, không cần khoá gì cả.\n\nChữ ký đảm bảo KHÔNG SỬA ĐƯỢC, chứ KHÔNG đảm bảo KHÔNG ĐỌC ĐƯỢC.\n```\n\nVì vậy **đừng bao giờ đặt dữ liệu bí mật vào payload của JWT** — không mật khẩu, không số thẻ, không thông tin nội bộ. Payload chỉ nên chứa định danh và những thông tin vốn đã không bí mật.\n\nCách server xác minh:\n\n```\n① Tách header, payload, chữ ký\n② Tự tính lại chữ ký từ header + payload + KHOÁ BÍ MẬT của server\n③ So với chữ ký trong token — khác nhau ⇒ token bị sửa ⇒ từ chối\n④ Kiểm tra exp (thời điểm hết hạn) — quá hạn ⇒ từ chối\n```\n\nChú ý bước ②: server **không cần tra cứu gì cả**, chỉ cần khoá bí mật của chính nó. Đây vừa là ưu điểm lớn nhất (không tra kho, mở rộng dễ) vừa là nhược điểm lớn nhất của JWT.\n\n### Nhược điểm cốt lõi của JWT: không thu hồi được\n\n```\nSession:  đăng xuất → xoá khỏi kho → session_id vô giá trị NGAY\nJWT:      đăng xuất → xoá token ở client → nhưng token CŨ VẪN HỢP LỆ\n          tới khi hết hạn, vì server không lưu gì để mà xoá\n```\n\nHệ quả rất cụ thể: token bị đánh cắp thì **không có cách nào vô hiệu hoá nó** ngoài việc đổi khoá bí mật của server — mà làm vậy là đăng xuất **toàn bộ** người dùng. Tương tự, khoá tài khoản một người dùng vi phạm cũng không có hiệu lực ngay: họ vẫn dùng được tới khi token hết hạn.\n\nBa cách khắc phục, và mỗi cách đều có cái giá:\n\n```\n① Hạn ngắn (5–15 phút) + refresh token\n   → cửa sổ nguy hiểm ngắn, nhưng phải cài thêm luồng refresh\n\n② Danh sách thu hồi (blacklist) trong Redis\n   → thu hồi được ngay, NHƯNG phải tra kho mỗi request\n     ⇒ mất đúng ưu điểm chính của JWT\n\n③ Kiểm tra một mốc thời gian trong DB (ví dụ token_valid_from của user)\n   → cho phép \"đăng xuất mọi thiết bị\", nhưng vẫn phải tra DB\n```\n\nĐiểm ② đáng suy nghĩ: rất nhiều hệ thống chọn JWT vì \"không cần tra kho\", rồi thêm blacklist vì cần thu hồi, và cuối cùng **tra kho mỗi request** — tức là có đủ nhược điểm của cả hai mô hình. Nếu đã cần thu hồi tức thì thì session trên server đơn giản hơn và đúng hơn.\n\n### Lưu token ở đâu — quyết định bảo mật quan trọng nhất\n\n| | Cookie `HttpOnly` | `localStorage` |\n|---|---|---|\n| JavaScript đọc được | **✗** | **✓** |\n| XSS lấy được token | ✗ | **✓ mất tài khoản** |\n| Tự gửi kèm request | ✓ | ✗ phải tự thêm header |\n| Rủi ro CSRF | có — cần `SameSite` | không |\n| Dùng cho API khác domain | phức tạp hơn | dễ hơn |\n\nCách đọc bảng này cho đúng: cả hai đều có rủi ro, nhưng **rủi ro không cùng mức độ nghiêm trọng**.\n\n```\nXSS + localStorage  → token bị mang đi, dùng ở đâu cũng được, tới khi hết hạn\nXSS + cookie HttpOnly → kẻ tấn công gửi được request TRONG lúc người dùng mở tab,\n                        nhưng KHÔNG mang token đi được\n\nCSRF → đã có giải pháp chuẩn, hiệu quả: SameSite + CSRF token\nXSS lấy token → KHÔNG có giải pháp bù nào sau khi token đã ra ngoài\n```\n\nKết luận thực dụng: **cookie `HttpOnly` + `SameSite=Lax` + `Secure` là mặc định đúng.** Lý do là CSRF có cách phòng tốt và đã chuẩn hoá, còn mất token vì XSS thì không có cách nào bù lại.\n\n### CSRF hoạt động thế nào và cách chặn\n\n```html\n<!-- Trang độc hại, người dùng đang đăng nhập bank.com ở tab khác -->\n<form action=\"https://bank.com/chuyen-tien\" method=\"POST\" id=\"f\">\n  <input name=\"den\" value=\"ke-tan-cong\">\n  <input name=\"so_tien\" value=\"10000000\">\n</form>\n<script>document.getElementById('f').submit()</script>\n```\n\nTrình duyệt gửi `POST` tới `bank.com` **kèm cookie phiên**, vì cookie tự động gửi theo domain. Server thấy cookie hợp lệ và thực hiện lệnh chuyển tiền. Kẻ tấn công **không cần đọc** cookie — chỉ cần trình duyệt gửi nó đi.\n\nBa lớp phòng, nên dùng kết hợp:\n\n```\n① SameSite=Lax hoặc Strict     ← chặn được ngay ví dụ trên, hiệu quả nhất\n② CSRF token                    ← server sinh token, đặt vào form,\n                                  kiểm tra khi nhận. Site khác không đọc được nó.\n③ Dùng đúng method              ← thao tác thay đổi dữ liệu KHÔNG dùng GET\n```\n\nLớp ③ nối trực tiếp với bài HTTP: nếu `GET /chuyen-tien?so_tien=...` hoạt động thì kẻ tấn công chỉ cần một thẻ `<img src=\"...\">` là xong — không cần cả form. Đây là lý do cụ thể vì sao \"dùng đúng method\" là vấn đề bảo mật, không chỉ là vấn đề thẩm mỹ.\n\n### Cookie phiên và cookie bền\n\n```http\nSet-Cookie: a=1                      ← cookie PHIÊN: mất khi đóng trình duyệt\nSet-Cookie: a=1; Max-Age=2592000     ← cookie BỀN: sống 30 ngày\n```\n\nĐây là cơ chế đằng sau ô \"Ghi nhớ đăng nhập\": không tích thì dùng cookie phiên, tích thì dùng cookie bền. Một lưu ý thực tế: nhiều trình duyệt hiện nay **phục hồi cả cookie phiên** khi mở lại vì tính năng \"khôi phục tab\", nên cookie phiên không còn đảm bảo chắc chắn là mất khi đóng.\n\n### Ba kho lưu ở phía trình duyệt\n\n| | Cookie | `localStorage` | `sessionStorage` |\n|---|---|---|---|\n| Dung lượng | ~4 KB | ~5–10 MB | ~5–10 MB |\n| Tự gửi lên server | **✓** | ✗ | ✗ |\n| JS đọc được | ✗ nếu `HttpOnly` | ✓ | ✓ |\n| Phạm vi | domain + path | origin | **một tab** |\n| Thời hạn | đặt được | vĩnh viễn tới khi xoá | đóng tab là mất |\n\nDòng \"tự gửi lên server\" giải thích vì sao cookie vẫn là nơi đúng cho session dù dung lượng nhỏ: đó là **cơ chế duy nhất mà trình duyệt tự động gắn vào request**. `localStorage` không có tính chất đó, nên phải viết code thêm header — và code đó chạy bằng JavaScript, tức là XSS đọc được.\n\nGiới hạn 4 KB cũng là lý do khác để không nhét nhiều dữ liệu vào cookie: cookie được gửi kèm **mọi** request, kể cả request lấy ảnh và CSS. Một cookie 3 KB nhân với 50 request cho một trang là 150 KB gửi lên vô ích mỗi lần tải trang.",

vi_du: "### Xem cookie thật trên đường truyền\n\n```bash\n# Xem server đặt cookie gì\ncurl -I https://example.com | grep -i set-cookie\n# set-cookie: session=a3f9c1b2; Path=/; HttpOnly; Secure; SameSite=Lax\n\n# Lưu cookie vào file rồi dùng lại — mô phỏng trình duyệt\ncurl -c cookies.txt -X POST -d 'email=a@b.c&password=x' https://example.com/login\ncurl -b cookies.txt https://example.com/trang-ca-nhan\n#     ↑ -c ghi cookie ra file, -b đọc cookie từ file\n\ncat cookies.txt\n# example.com  FALSE  /  TRUE  1735689600  session  a3f9c1b2\n#                        ↑ cột Secure          ↑ hết hạn\n```\n\nCặp `-c` / `-b` là cách kiểm thử luồng đăng nhập bằng dòng lệnh, không cần trình duyệt — rất hữu ích khi viết script kiểm thử hoặc khi gỡ lỗi trên server không có giao diện.\n\n### Kiểm tra cờ bảo mật của cookie\n\n```bash\n# Bộ kiểm nhanh: ba cờ bắt buộc có đủ chưa\ncurl -sI https://example.com | grep -i set-cookie | while read -r line; do\n  echo \"$line\" | grep -qi 'HttpOnly' || echo \"⚠️  THIẾU HttpOnly\"\n  echo \"$line\" | grep -qi 'Secure'   || echo \"⚠️  THIẾU Secure\"\n  echo \"$line\" | grep -qi 'SameSite' || echo \"⚠️  THIẾU SameSite\"\ndone\n```\n\nĐây là loại kiểm tra nên đưa vào CI cho ứng dụng thật: nó bắt được lỗi cấu hình mà không ai để ý cho tới khi bị khai thác.\n\n### Session trên server, cài đặt đúng\n\n```python\n# Flask — session lưu trong Redis, cookie chỉ chứa ID\nimport secrets, json, redis\nfrom flask import Flask, request, make_response\n\napp = Flask(__name__)\nkho = redis.Redis()\nHAN = 3600      # 1 giờ\n\n@app.post(\"/login\")\ndef login():\n    u = kiem_tra_mat_khau(request.json[\"email\"], request.json[\"password\"])\n    if not u:\n        return {\"error\": \"sai thông tin\"}, 401      # 401: chưa xác thực được\n\n    # secrets, KHÔNG dùng random — phải là ngẫu nhiên mật mã\n    sid = secrets.token_urlsafe(32)\n    kho.setex(f\"sess:{sid}\", HAN, json.dumps({\"user_id\": u.id, \"vai_tro\": u.vai_tro}))\n\n    resp = make_response({\"ok\": True})\n    resp.set_cookie(\"session\", sid,\n                    httponly=True,      # JS không đọc được\n                    secure=True,        # chỉ qua HTTPS\n                    samesite=\"Lax\",     # chống CSRF\n                    max_age=HAN,\n                    path=\"/\")\n    return resp\n\n@app.post(\"/logout\")\ndef logout():\n    sid = request.cookies.get(\"session\")\n    if sid:\n        kho.delete(f\"sess:{sid}\")       # ⭐ vô hiệu hoá NGAY phía server\n    resp = make_response({\"ok\": True})\n    resp.delete_cookie(\"session\")\n    return resp\n```\n\nBa chi tiết đáng giải thích:\n\n```\nsecrets.token_urlsafe(32)  ngẫu nhiên mật mã. Dùng random.random() là lỗ hổng:\n                           bộ sinh thường có thể suy ra được trạng thái.\nsetex                      đặt kèm TTL ⇒ session tự hết hạn, không cần cron dọn.\nkho.delete khi logout      đây là điều JWT KHÔNG làm được.\n```\n\nCũng chú ý `resp.delete_cookie` **một mình là không đủ** — nó chỉ yêu cầu trình duyệt bỏ cookie. Nếu không xoá ở server, ai đó đã copy được giá trị cookie vẫn dùng tiếp được. Bước xoá ở server mới là bước thật sự đăng xuất.\n\n### JWT với refresh token\n\n```python\nimport jwt, datetime, secrets\n\nBI_MAT = os.environ[\"JWT_SECRET\"]       # KHÔNG hard-code trong code\n\ndef tao_access_token(user_id):\n    return jwt.encode({\n        \"sub\": str(user_id),\n        \"exp\": datetime.datetime.utcnow() + datetime.timedelta(minutes=15),\n        \"iat\": datetime.datetime.utcnow(),\n    }, BI_MAT, algorithm=\"HS256\")\n\n@app.post(\"/login\")\ndef login():\n    u = kiem_tra_mat_khau(...)\n    access  = tao_access_token(u.id)              # hạn NGẮN, 15 phút\n    refresh = secrets.token_urlsafe(32)           # hạn DÀI, lưu ở server\n    kho.setex(f\"refresh:{refresh}\", 30*86400, str(u.id))\n\n    resp = make_response({\"access_token\": access})\n    # refresh token vào cookie HttpOnly — JS không cần và không nên đọc nó\n    resp.set_cookie(\"refresh\", refresh, httponly=True, secure=True,\n                    samesite=\"Lax\", max_age=30*86400, path=\"/auth\")\n    return resp\n\n@app.post(\"/auth/refresh\")\ndef refresh():\n    rt = request.cookies.get(\"refresh\")\n    uid = kho.get(f\"refresh:{rt}\") if rt else None\n    if not uid:\n        return {\"error\": \"refresh token không hợp lệ\"}, 401\n\n    kho.delete(f\"refresh:{rt}\")                   # ⭐ dùng một lần rồi bỏ\n    moi = secrets.token_urlsafe(32)\n    kho.setex(f\"refresh:{moi}\", 30*86400, uid)\n    resp = make_response({\"access_token\": tao_access_token(int(uid))})\n    resp.set_cookie(\"refresh\", moi, httponly=True, secure=True,\n                    samesite=\"Lax\", max_age=30*86400, path=\"/auth\")\n    return resp\n```\n\nÝ tưởng của mô hình này là **chia đôi trách nhiệm**:\n\n```\naccess token  hạn ngắn, tự chứa, KHÔNG tra kho ⇒ nhanh, mở rộng dễ\nrefresh token hạn dài, lưu ở server, tra kho ⇒ THU HỒI ĐƯỢC\n```\n\nCửa sổ nguy hiểm khi access token bị đánh cắp chỉ còn 15 phút, mà vẫn giữ được khả năng thu hồi thật qua refresh token. Chi tiết `kho.delete` rồi cấp refresh token mới (*rotation*) là cách phát hiện token bị đánh cắp: nếu một refresh token cũ được dùng lại, gần như chắc chắn có hai bên đang dùng cùng tài khoản.\n\n`path=\"/auth\"` cũng có chủ đích: refresh token **chỉ** được gửi tới các endpoint xác thực, không kèm theo mọi request khác. Giảm phạm vi là giảm rủi ro.\n\n### CSRF token\n\n```python\n@app.get(\"/form-chuyen-tien\")\ndef form():\n    csrf = secrets.token_urlsafe(32)\n    session_data[\"csrf\"] = csrf                   # lưu vào session phía server\n    return render(f'<input type=\"hidden\" name=\"csrf\" value=\"{csrf}\">')\n\n@app.post(\"/chuyen-tien\")\ndef chuyen():\n    if request.form.get(\"csrf\") != session_data.get(\"csrf\"):\n        return {\"error\": \"CSRF không hợp lệ\"}, 403     # 403: biết bạn là ai, không cho\n    ...\n```\n\nVì sao cách này chặn được CSRF: site độc hại **không đọc được** nội dung trang của bạn (chính sách same-origin ngăn điều đó), nên không biết giá trị CSRF token để đặt vào form giả. Cookie thì họ khiến trình duyệt gửi được, nhưng CSRF token thì không.\n\n### Chẩn đoán các lỗi phiên thường gặp\n\n| Triệu chứng | Nguyên nhân | Kiểm tra |\n|---|---|---|\n| Đăng nhập rồi F5 là mất | cookie không được đặt | `curl -I` xem có `Set-Cookie` |\n| Chạy local được, deploy thì không | `Secure` mà local dùng HTTP | dùng HTTPS ở local, hoặc bỏ `Secure` khi dev |\n| Mọi lần deploy là đăng xuất hết | session lưu trong bộ nhớ tiến trình | chuyển sang Redis |\n| Đăng xuất rồi vẫn vào được | JWT không thu hồi được | hạ hạn token, hoặc dùng session |\n| Bấm link từ email thì chưa đăng nhập | `SameSite=Strict` | đổi sang `Lax` |\n| Đăng nhập được ở máy tính, không được ở điện thoại | thường là lỗi chứng chỉ | xem bài HTTPS, kiểm tra chuỗi trung gian |\n| API khác domain không nhận cookie | thiếu `SameSite=None; Secure` và CORS | xem lại cấu hình CORS |\n\n```bash\n# Xem toàn bộ cookie và cờ của chúng\ncurl -sI https://example.com | grep -i '^set-cookie'\n\n# Thử với cookie giả để kiểm tra server có xác minh không\ncurl -H 'Cookie: session=gia-mao-hoan-toan' https://example.com/trang-ca-nhan -o /dev/null -w '%{http_code}\\n'\n# 401 ✓ đúng — server từ chối\n# 200 ✗ NGHIÊM TRỌNG — server không xác minh session\n```\n\nPhép thử cuối rất đáng chạy một lần trên mọi ứng dụng: nó phát hiện được lỗi \"chỉ kiểm tra cookie có tồn tại chứ không kiểm tra giá trị\" — một lỗi nghe khó tin nhưng có thật, thường sinh ra từ việc viết middleware vội.",

so_sanh: "### Session trên server và JWT\n\n| | Session (server) | JWT |\n|---|---|---|\n| Dữ liệu ở đâu | **kho phía server** | **trong token** |\n| Server phải tra kho | ✓ mỗi request | ✗ |\n| **Thu hồi tức thì** | **✓** | **✗** |\n| Mở rộng nhiều server | cần kho chung (Redis) | không cần gì |\n| Kích thước gửi mỗi request | nhỏ (chỉ ID) | lớn hơn (cả payload) |\n| Đổi quyền có hiệu lực ngay | ✓ | ✗ tới khi token hết hạn |\n| Phù hợp | web app truyền thống | API nhiều service, không chung kho |\n\nHai hàng in đậm là hai hàng quyết định. Cách chọn thực dụng:\n\n```\nCần đăng xuất tức thì, cần khoá tài khoản ngay  → SESSION\nNhiều service không chia sẻ kho, chấp nhận chậm vài phút → JWT\nĐã phải thêm blacklist cho JWT                  → dùng SESSION cho đơn giản\n```\n\nDòng cuối là lời phản biện đáng nhớ với sự phổ biến của JWT: rất nhiều hệ thống chọn JWT vì \"hiện đại, không cần tra kho\", rồi thêm blacklist vì cần thu hồi. Lúc đó nó **tra kho mỗi request giống session, nhưng phức tạp hơn** — có đủ nhược điểm của cả hai mô hình. JWT xứng đáng khi bạn thật sự có nhiều service độc lập và thật sự chấp nhận được độ trễ thu hồi.\n\n### Cookie `HttpOnly` và `localStorage`\n\n| | Cookie `HttpOnly` | `localStorage` |\n|---|---|---|\n| XSS lấy được token | **✗** | **✓** |\n| Rủi ro CSRF | có, chặn được bằng `SameSite` | không |\n| Tự gửi kèm request | ✓ | ✗ |\n| Dùng cho API khác domain | phức tạp | dễ |\n| Mức độ thiệt hại khi bị tấn công | giới hạn trong lúc mở tab | **mất tài khoản** |\n\nHàng cuối là hàng quyết định, và cách suy luận đúng là **so mức độ thiệt hại chứ không so số lượng rủi ro**: CSRF có giải pháp chuẩn và hiệu quả (`SameSite` + CSRF token), còn token đã ra khỏi máy người dùng thì không có gì bù lại được. Vì vậy cookie `HttpOnly` thắng, dù nó có thêm một loại rủi ro.\n\n### Ba giá trị `SameSite`\n\n| | `Strict` | `Lax` | `None` |\n|---|---|---|---|\n| Bấm link từ site khác | **✗ không gửi** | ✓ gửi | ✓ |\n| `POST` từ site khác | ✗ | **✗** | ✓ |\n| Ảnh/iframe từ site khác | ✗ | ✗ | ✓ |\n| Chống CSRF | mạnh nhất | tốt | **không** |\n| Bắt buộc `Secure` | không | không | **✓** |\n| Dùng khi | trang quản trị, ngân hàng | **mặc định tốt** | thật sự cần xuyên site |\n\n`Lax` là mặc định đúng vì nó chặn đúng thứ nguy hiểm (`POST` và request nền từ site khác) mà không phá việc bấm link từ email hay từ kết quả tìm kiếm. `Strict` dành cho những chỗ mà bất tiện đó là đánh đổi chấp nhận được.\n\n### Cookie, `localStorage`, `sessionStorage`\n\n| | Cookie | `localStorage` | `sessionStorage` |\n|---|---|---|---|\n| Dung lượng | ~4 KB | ~5–10 MB | ~5–10 MB |\n| Gửi lên server | **✓ tự động** | ✗ | ✗ |\n| Sống qua đóng trình duyệt | tuỳ `Max-Age` | ✓ | ✗ |\n| Phạm vi | domain + path | origin | **một tab** |\n| Dùng cho | **session, xác thực** | tuỳ chọn giao diện, nháp | trạng thái tạm một tab |\n\nQuy tắc phân công rõ ràng: **cookie cho những gì server cần biết; hai kho kia cho những gì chỉ trình duyệt cần biết.** Ví dụ đúng cho `localStorage` là chế độ tối, ngôn ngữ đã chọn, bản nháp chưa gửi — không có cái nào là bí mật.\n\n### XSS và CSRF\n\n| | XSS | CSRF |\n|---|---|---|\n| Kẻ tấn công làm gì | **chạy JS trong trang của bạn** | khiến trình duyệt gửi request |\n| Đọc được dữ liệu | **✓ mọi thứ JS đọc được** | ✗ không đọc được phản hồi |\n| Chặn bằng | escape output, CSP | `SameSite`, CSRF token |\n| Cờ cookie giúp được | `HttpOnly` giới hạn thiệt hại | `SameSite` chặn |\n| Mức nghiêm trọng | **cao hơn** | cao |\n\nDòng \"đọc được dữ liệu\" là điểm phân biệt cốt lõi: CSRF làm được **hành động** nhưng không **đọc** được kết quả (chính sách same-origin ngăn), nên nó không lấy được dữ liệu. XSS thì làm được cả hai. Đây là lý do XSS được coi là nghiêm trọng hơn, và là lý do `HttpOnly` — vốn chỉ giới hạn thiệt hại của XSS — lại có giá trị cao như vậy.",

loi_thuong_gap: [
  "**Lưu token xác thực vào `localStorage`.** Một lỗ XSS là mất tài khoản mọi người dùng đang mở trang. Dùng cookie `HttpOnly`.",
  "**Quên `HttpOnly`.** Cờ dễ đặt nhất mà có giá trị bảo mật cao nhất. Không có nó, JavaScript đọc được cookie phiên.",
  "**Quên `SameSite`.** Mở đường cho CSRF. `Lax` là mặc định đúng cho gần như mọi ứng dụng.",
  "**Quên `Secure` trên môi trường thật.** Cookie phiên đi qua HTTP là ai nghe được cũng chiếm được phiên.",
  "**Dùng `random()` để sinh session ID.** Phải dùng bộ sinh ngẫu nhiên mật mã (`secrets`, `crypto`). Đoán được ID là chiếm được phiên.",
  "**Đặt dữ liệu bí mật vào payload JWT.** Payload chỉ base64, ai cũng đọc được. Ký ≠ mã hoá.",
  "**Tưởng xoá token ở client là đã đăng xuất với JWT.** Token cũ vẫn hợp lệ tới khi hết hạn. Server không có gì để xoá.",
  "**Chọn JWT rồi thêm blacklist.** Lúc đó tra kho mỗi request giống session mà phức tạp hơn. Nếu cần thu hồi ngay thì dùng session.",
  "**Lưu session trong bộ nhớ tiến trình ở môi trường thật.** Mỗi lần deploy là mọi người bị đăng xuất, và không chạy được nhiều server.",
  "**Chỉ `delete_cookie` khi đăng xuất mà không xoá session ở server.** Ai đã copy được giá trị cookie vẫn dùng tiếp được.",
  "**Ghi `Domain=example.com` khi không cần.** Nó **mở rộng** phạm vi ra mọi subdomain, gửi cookie tới cả những subdomain do bên thứ ba chạy.",
  "**Nhét nhiều dữ liệu vào cookie.** Cookie gửi kèm mọi request, kể cả ảnh và CSS. Cookie 3 KB × 50 request là 150 KB vô ích mỗi lần tải trang.",
  "**Middleware chỉ kiểm tra cookie có tồn tại chứ không xác minh giá trị.** Nghe khó tin nhưng có thật; thử bằng một cookie giả là phát hiện ngay."
],

tu_kiem_tra: [
  "Cookie và session khác nhau thế nào? Cái nào là cơ chế, cái nào là mô hình?",
  "Vì sao việc cookie tự động gửi kèm request vừa là tiện lợi vừa là lỗ hổng?",
  "Ba cờ cookie bắt buộc là gì, mỗi cờ chặn loại tấn công nào?",
  "`Domain=example.com` làm phạm vi cookie rộng ra hay hẹp lại? Hệ quả bảo mật?",
  "JWT có được mã hoá không? Điều đó có nghĩa gì với những gì bạn được phép đặt vào payload?",
  "Vì sao đăng xuất với JWT không có hiệu lực tức thì, và ba cách khắc phục cùng cái giá của mỗi cách?",
  "So sánh mức độ thiệt hại của XSS khi token nằm trong `localStorage` với khi nằm trong cookie `HttpOnly`.",
  "Mô tả một cuộc tấn công CSRF, và giải thích vì sao `SameSite=Lax` chặn được nó.",
  "Vì sao CSRF token hiệu quả — site độc hại thiếu thứ gì mà không giả mạo được?",
  "\"Mỗi lần deploy là mọi người bị đăng xuất\" — nguyên nhân và cách sửa?",
  "Refresh token rotation phát hiện được điều gì mà refresh token thường không phát hiện?"
],

tai_nguyen: [
  "Tài liệu **MDN Web Docs**, mục *HTTP cookies* và *SameSite cookies* — mô tả chính xác hành vi từng thuộc tính, gồm cả các thay đổi mặc định của trình duyệt.",
  "Tài liệu **OWASP Cheat Sheet Series**, các mục *Session Management*, *Cross-Site Request Forgery Prevention*, *JSON Web Token* — ngắn, thực dụng, viết bởi người làm bảo mật.",
  "Sách **The Web Application Hacker's Handbook** — Dafydd Stuttard & Marcus Pinto. Chương về quản lý phiên phân tích rất nhiều cách cài đặt sai có thật.",
  "Tài liệu của framework bạn dùng, mục về session và CSRF — mỗi framework có mặc định khác nhau, và đọc phần này tránh được phần lớn lỗi.",
  "Sách **Real-World Cryptography** — David Wong. Phần về ký số giúp hiểu vì sao chữ ký JWT đảm bảo toàn vẹn mà không đảm bảo bí mật."
],

lien_he: [
  "**HTTP: method, header, status code**: cookie là header, tính không trạng thái là vấn đề bài này giải quyết, và \"không dùng GET cho thao tác thay đổi dữ liệu\" là một lớp chống CSRF.",
  "**HTTPS, TLS handshake** ngay trước: cờ `Secure` chỉ có nghĩa khi có HTTPS; không có HTTPS thì mọi cơ chế phiên đều vô nghĩa.",
  "**WebSocket vs polling** ngay sau: WebSocket cũng gửi cookie khi bắt tay, nên xác thực phiên áp dụng được — nhưng `SameSite` hành xử khác.",
  "**Proxy, reverse proxy, CDN** ở cuối module: CDN không được cache phản hồi có `Set-Cookie`, và đây là lỗi cấu hình nghiêm trọng nếu làm sai.",
  "**Database** ở mảng 04: kho session là ứng dụng thực tế của TTL và của kho khoá–giá trị.",
  "**Backend & Web** ở mảng 05: phần Auth mở rộng bài này thành OAuth, single sign-on, phân quyền theo vai trò.",
  "**System Design** ở mảng 06: session dùng chung là lý do cần Redis khi mở rộng ngang, và là ví dụ kinh điển của \"trạng thái ở đâu\" trong thiết kế hệ thống.",
  "**Quyền file, chmod, chown** ở module 3: khoá bí mật ký JWT là file phải `chmod 600`, cùng nguyên tắc với khoá riêng SSH."
]

};
