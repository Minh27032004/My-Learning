window.LESSONS["07:llm-engineering:kiem-soat-chi-phi-va-token"] = {

tom_tat: [
  "Chi phí LLM không phải một con số bí ẩn ở cuối tháng: nó là tổng của số lượt gọi, token đầu vào, token kết quả, token được cache, tool call và chính sách phục vụ. Muốn kiểm soát được, phải đo từng thành phần theo request thay vì chỉ nhìn tổng hóa đơn.",
  "Đếm token trước khi gửi chỉ là dự báo; trường `usage` do API trả về mới là số liệu để đối soát. Hai con số có thể lệch vì tokenizer, reasoning token, nội dung tool và cách nhà cung cấp đóng gói request.",
  "Tối ưu đúng thứ tự là loại lời gọi không cần thiết, thu gọn ngữ cảnh theo giá trị, giới hạn kết quả, tận dụng đoạn đầu ổn định cho prompt caching, rồi mới hạ model. Đổi sang model rẻ trước khi có eval dễ mua chi phí thấp bằng chất lượng kém.",
  "Một budget hữu ích có ba tầng: trần cho từng request, hạn mức theo người dùng hoặc tính năng, và cảnh báo/điểm ngắt toàn hệ thống. Chỉ đặt billing alert không ngăn được một vòng lặp lỗi tiêu tiền trong vài phút.",
  "Ví dụ xuyên suốt là Trợ lý Mây của cửa hàng trực tuyến: hỏi đáp chính sách, tóm tắt hội thoại và đề nghị đổi địa chỉ. Mỗi luồng có giá trị, rủi ro và ngân sách token khác nhau nên không thể dùng một cấu hình chung."
],

can_biet_truoc: [
  "Khái niệm token và context window (cửa sổ ngữ cảnh), đặc biệt sự khác nhau giữa token đầu vào, token kết quả và giới hạn ngữ cảnh.",
  "Prompt gồm system/developer instruction, lịch sử hội thoại, dữ liệu truy xuất và câu hỏi người dùng; mọi phần được gửi cho model đều có thể chiếm token.",
  "Biết đọc JSON, viết hàm và chạy script Python; ví dụ dùng thư viện chuẩn, không cần API key hay SDK.",
  "Khái niệm percentile như p50 và p95, cùng khác biệt giữa giá trị trung bình và phần đuôi phân phối.",
  "Eval set (bộ tình huống đánh giá) và release gate từ bài đánh giá LLM, vì giảm chi phí phải được kiểm tra cùng độ đúng và an toàn."
],

dinh_nghia: "**Kiểm soát chi phí và token** là quá trình biến mức tiêu thụ LLM thành một đại lượng có thể dự báo, giới hạn, quan sát và tối ưu mà vẫn giữ các yêu cầu chất lượng. Đơn vị vận hành cơ bản không phải “một cuộc chat” mà là **một request có nhãn**: tính năng nào gọi, model nào xử lý, bao nhiêu token thuộc từng loại, có tool nào được dùng, mất bao lâu và kết quả có đạt tiêu chuẩn hay không.\n\nVới bảng giá tính theo token, chi phí văn bản của một request có thể mô tả bằng công thức Unicode:\n\n`C = I × Pᵢ + K × Pₖ + O × Pₒ + T`\n\nTrong đó `I` là token đầu vào không được cache, `K` là token đầu vào được cache, `O` là token kết quả tính phí, `Pᵢ`, `Pₖ`, `Pₒ` là đơn giá tương ứng cho **một token**, còn `T` là các khoản khác như tool call. Nếu bảng giá công bố theo một triệu token, phải chia đơn giá cho 1.000.000 trước khi nhân. Công thức đơn giản nhưng chỉ hữu ích khi các biến được lấy từ `usage` thực tế và gắn với nghiệp vụ.",

vi_sao_can: "Một prototype vài chục request mỗi ngày có thể sống với việc “xem dashboard cuối tuần”. Production thì không. Trợ lý Mây có 100.000 phiên hỏi đáp trong ngày; chỉ cần lịch sử hội thoại bị gửi lặp thêm 2.000 token mỗi lượt là 200 triệu token đầu vào không tạo thêm giá trị. Ngược lại, cắt lịch sử một cách cơ học có thể làm mất địa chỉ cũ mà người dùng đang yêu cầu sửa, khiến hệ thống thực hiện sai hành động. Vì vậy bài toán là **chi phí trên một kết quả đúng**, không phải chi phí trên một request.\n\nKiểm soát còn cần vì phân phối không đều. Trung bình 900 token có thể che giấu một nhóm p99 dài 40.000 token do người dùng dán tài liệu hoặc do agent gọi tool theo vòng lặp. Một retry không có idempotency có thể vừa trả tiền nhiều lần, vừa đổi địa chỉ nhiều lần. Một prompt mới dài hơn 30% có thể qua review vì câu trả lời đẹp hơn nhưng âm thầm làm unit economics (kinh tế trên một đơn vị giao dịch) không còn khả thi.\n\nCuối cùng, billing alert là tín hiệu trễ và ở cấp tài khoản. Kỹ sư cần biết “luồng tóm tắt tăng bao nhiêu đồng trên mỗi hội thoại sau bản phát hành nào”, còn product owner cần biết “mỗi đơn hàng được cứu tốn bao nhiêu”. Không có telemetry theo request, cả hai chỉ đoán.",

co_che: "### 1. Tách hóa đơn thành các đồng hồ đo được\n\nHãy hình dung request như một giỏ hàng. System prompt, lịch sử, kết quả tìm kiếm và schema tool đều đi vào giỏ đầu vào; câu trả lời và đôi khi reasoning token đi vào giỏ kết quả; tool bên ngoài có thể có giá riêng. “Tổng token” không đủ để chẩn đoán vì hai request cùng 5.000 token nhưng một cái có 4.900 token đầu vào rẻ, cái kia có 4.900 token kết quả đắt hơn theo bảng giá đang áp dụng.\n\nMỗi log nên giữ ít nhất: `request_id`, thời gian, `feature`, `tenant`, model thực tế, trạng thái, số lần thử, latency, `input_tokens`, `cached_input_tokens`, `output_tokens`, tool call, phiên bản prompt và kết quả eval/guardrail. Không ghi nguyên prompt vào log chi phí nếu nó chứa dữ liệu cá nhân; mã băm phiên bản và các số đếm thường đã đủ. Tiền tệ nên lưu bằng số nguyên ở đơn vị nhỏ nhất hoặc `Decimal`, tránh cộng hàng triệu số dấu phẩy động rồi lệch khi đối soát.\n\nPhải đọc định nghĩa usage của đúng API. Ở một số model suy luận, trần kết quả bao gồm cả token suy luận không hiện ra cho người dùng. Vì thế câu trả lời ngắn không chứng minh request rẻ, và trần quá thấp có thể làm response `incomplete`. Dữ liệu streaming cũng cần được chốt khi nhận sự kiện hoàn tất; đếm từng đoạn text stream không thay thế usage chính thức.\n\n### 2. Dự báo trước, đối soát sau\n\nTrước request, tokenizer giúp kiểm tra nguy cơ vượt context window và ước lượng budget. Tuy nhiên đừng dùng quy tắc “một từ bằng bao nhiêu token” để ra hóa đơn: tiếng Việt, dấu câu, JSON, mã nguồn và model khác nhau tạo cách tách khác nhau. Dự báo nên trả về khoảng hoặc thêm headroom (phần dự phòng), không giả vờ chính xác tuyệt đối.\n\nSau response, ghi `usage` do dịch vụ trả về. Cuối ngày, gom theo `feature × model × prompt_version`; so tổng nội bộ với dashboard/hóa đơn. Chênh lệch buộc ta kiểm tra request thất bại có bị tính phí không, retry có bị ghi thiếu, cached token có bị cộng hai lần, hoặc giá vừa thay đổi. Đơn giá phải là dữ liệu có ngày hiệu lực, không chôn trong code. Khi nhà cung cấp đổi giá, ta tái tính đúng theo thời điểm request thay vì sửa lịch sử.\n\nMột dự báo có ý nghĩa dùng phân phối lưu lượng: `chi phí ngày = Σ (số request của nhóm j × chi phí trung bình nhóm j)`. Ký hiệu `Σ` là tổng qua các nhóm như hỏi đáp, tóm tắt và đổi địa chỉ. Cần thêm p50, p95, p99 token/request vì trung bình không báo động được ca dài bất thường.\n\n### 3. Đặt ngân sách theo giá trị và mức rủi ro\n\nTrợ lý Mây không cấp cùng 4.000 token kết quả cho mọi việc. Hỏi chính sách thường cần vài đoạn; tóm tắt hội thoại dài cần nhiều đầu vào nhưng kết quả ngắn; đổi địa chỉ cần JSON rất ngắn song phải ưu tiên độ đúng và authorization. Budget nên là hợp đồng của từng route:\n\n| Luồng | Điều cần bảo vệ | Trần đầu vào | Trần kết quả | Khi vượt |\n|---|---|---:|---:|---|\n| hỏi chính sách | câu trả lời có nguồn | theo số đoạn truy xuất | vừa đủ 3–5 đoạn | thu hẹp truy xuất, hỏi làm rõ |\n| tóm tắt hội thoại | giữ quyết định và việc còn mở | chia đoạn có chồng lấn | bản tóm tắt ngắn | tóm tắt phân cấp |\n| đổi địa chỉ | đúng ý định và quyền | chỉ dữ kiện cần thiết | JSON nhỏ | từ chối tự động, chuyển người |\n\nCác con số thật phải lấy từ eval và telemetry của hệ thống, nên bảng cố ý không bịa một trần dùng chung. Trần request ngăn ca đơn lẻ phình to. Quota theo user/tenant ngăn lạm dụng kéo dài. Hạn mức ngày cho feature ngăn bug phát hành lan rộng. Circuit breaker (bộ ngắt mạch) dừng hoặc chuyển sang chế độ giảm cấp khi tốc độ đốt ngân sách vượt ngưỡng, chứ không đợi tiền chạm mức cuối tháng.\n\n### 4. Loại lời gọi trước khi làm lời gọi rẻ hơn\n\nRequest rẻ nhất là request không gửi. Câu hỏi “đơn hàng của tôi ở đâu” có thể đọc trạng thái xác định từ API rồi render bằng template; không cần LLM diễn giải mỗi lần. Câu chào xã giao có thể dùng câu trả lời cố định. Hai sự kiện giống nhau do client retry cần idempotency key để không sinh hai completion.\n\nSemantic cache (cache theo ý nghĩa) có thể tái dùng câu trả lời cho câu hỏi gần giống, nhưng chỉ an toàn với dữ liệu ít thay đổi và không phụ thuộc quyền riêng. Không cache câu trả lời chứa trạng thái đơn hàng theo khóa chỉ gồm câu hỏi; hai khách cùng hỏi một câu có thể nhận dữ liệu của nhau. Khóa cache phải chứa tenant, quyền, phiên bản nguồn và miền thời gian phù hợp. Với đổi địa chỉ, tốt hơn là không cache quyết định tác vụ.\n\nDeduplication (khử trùng lặp) ở hàng đợi cũng quan trọng. Nếu một webhook được giao ba lần, hệ thống phải nhận ra cùng event ID. Đây thường là tối ưu lớn hơn việc tranh luận model nào rẻ hơn 10%, đồng thời giảm cả lỗi nghiệp vụ.\n\n### 5. Giảm đầu vào theo giá trị thông tin\n\nCắt từ cuối một mảng messages là dễ nhưng nguy hiểm: dữ kiện quan trọng có thể nằm ở giữa. Quản lý ngữ cảnh tốt phân loại nội dung. System rule và ràng buộc an toàn ở đoạn đầu ổn định; trạng thái có cấu trúc giữ riêng; hội thoại cũ được tóm tắt; chỉ các lượt gần nhất được giữ nguyên; RAG chỉ đưa các đoạn liên quan.\n\nRAG không mặc nhiên tiết kiệm. Lấy 20 đoạn rồi nhét hết vào prompt có thể đắt hơn tài liệu gốc. Retriever phải đo recall cùng số token được đưa vào. Có thể lấy rộng bằng embedding, rerank rồi chỉ gửi top-k đã lọc; nếu bằng chứng yếu, hỏi lại thay vì tăng k vô hạn. Schema tool cũng là input: hàng chục tool với mô tả dài được gửi ở mọi lượt sẽ tạo “thuế token” cố định. Chỉ expose tool hợp lệ cho trạng thái hiện tại.\n\nTóm tắt hội thoại là phép nén có mất mát. Bản tóm tắt cần schema như `facts`, `decisions`, `open_questions`, `user_constraints` để giảm nguy cơ bỏ mất chi tiết. Định kỳ so nó với lịch sử bằng eval; đừng tóm tắt nối tiếp mãi vì lỗi nhỏ tích lũy qua nhiều thế hệ.\n\n### 6. Thiết kế đoạn đầu để prompt caching có cơ hội trúng\n\nPrompt caching thường khớp đoạn đầu chung. Vì vậy đặt instruction, ví dụ và tool schema ổn định trước; dữ liệu riêng của request đặt sau. Nếu chèn timestamp, request ID hoặc tên người dùng ở dòng đầu, mỗi request tạo đoạn đầu khác nhau và tỷ lệ cache hit giảm. “Cache hit” phải đo bằng cached token trong usage, không suy từ việc prompt trông giống nhau.\n\nĐánh đổi là cache không miễn phí trong mọi dịch vụ, thời gian lưu hữu hạn và hành vi cụ thể phụ thuộc nhà cung cấp/model. Việc kéo dài prompt chỉ để đủ điều kiện cache có thể làm tổng chi phí tăng. Ngoài ra, cache prompt không phải cache câu trả lời: model vẫn sinh kết quả và vẫn có thể trả lời khác. Hãy so hai cohort có cùng chất lượng, tính tổng tiền gồm cả cache write nếu có, rồi mới kết luận.\n\n### 7. Khống chế kết quả bằng giao diện, không chỉ bằng lời nhắc\n\n“Trả lời ngắn gọn” là mong muốn; `max_output_tokens` là giới hạn cứng. Structured output giúp bỏ lời dẫn và ép trường cần thiết, đặc biệt route đổi địa chỉ chỉ cần `intent`, `new_address`, `needs_confirmation`. Nhưng trần quá sát có thể cắt JSON giữa chừng hoặc không đủ chỗ cho reasoning token. Client phải kiểm tra trạng thái incomplete, không parse mù rồi retry vô hạn.\n\nMột chiến lược tốt là đặt trần theo percentile của các câu đúng trong eval, thêm dự phòng nhỏ, sau đó theo dõi tỷ lệ chạm trần. Nếu rất nhiều response dừng đúng trần, đó là tín hiệu cấu hình hoặc prompt chưa phù hợp. Nếu hiếm response dài nhưng có giá trị, route riêng chúng thay vì nâng trần cho toàn bộ lưu lượng.\n\nStreaming giảm thời gian người dùng cảm nhận nhưng không tự giảm token. Nút hủy có thể tiết kiệm phần chưa sinh nếu dịch vụ thực sự dừng xử lý; cần xác minh qua usage. Đừng tuyên bố tiết kiệm chỉ vì giao diện ngừng hiển thị.\n\n### 8. Chọn model bằng đường biên chất lượng–chi phí\n\nModel routing (định tuyến model) nên dựa trên độ khó và hậu quả sai. Một classifier nhẹ có thể nhận diện FAQ rõ ràng, nhưng classifier cũng có false negative. Route đổi địa chỉ không nên bị đẩy sang model yếu chỉ vì câu ngắn; đây là tác vụ cần xác nhận, tool authorization và độ chính xác cao. Ngược lại, dùng model mạnh nhất để chuẩn hóa mọi lời chào là lãng phí.\n\nLập bảng cho từng ứng viên: task success, lỗi an toàn, latency p95 và chi phí trên task hoàn tất. Loại model không đạt gate bắt buộc trước, rồi chọn điểm rẻ nhất trong nhóm đạt. Chỉ số `cost/request` có thể đánh lừa: model rẻ nhưng làm người dùng hỏi lại hai lần sẽ tăng `requests/resolution`. Chỉ số tốt hơn là `cost/successful_resolution`, kèm tỷ lệ chuyển người và recontact.\n\nBatch processing phù hợp tác vụ không cần phản hồi ngay như tóm tắt kho lưu trữ, nếu nhà cung cấp có chính sách giá/độ trễ phù hợp. Nó không hợp đổi địa chỉ tương tác. Tự ghép nhiều người dùng vào một prompt để “batch” có thể làm rò dữ liệu và khiến một đầu vào lỗi phá cả nhóm; đừng nhầm batching chính thức với thủ thuật ghép prompt.\n\n### 9. Retry, timeout và tool loop là bộ nhân chi phí\n\nRetry chỉ dành cho lỗi tạm thời và phải có exponential backoff cộng jitter. Lỗi validation do prompt quá dài sẽ không tự hết; retry nguyên request chỉ trả tiền hoặc thất bại thêm. Mỗi logical operation cần `operation_id` để log biết ba attempt vẫn là một việc, nhờ đó dashboard hiển thị amplification factor: tổng attempt chia số operation.\n\nAgent phải có `max_tool_calls`, deadline và ngân sách token còn lại. Nếu model liên tục gọi `search_orders` với cùng tham số, orchestrator phát hiện vòng lặp bằng chữ ký tool call và dừng. Tool ghi dữ liệu cần idempotency key; nếu response timeout sau khi tool đã đổi địa chỉ, retry không được đổi lần nữa.\n\nFallback cũng có giá. Gọi model nhỏ rồi luôn gọi model lớn để “kiểm tra” là hai lần tiền; chỉ đáng khi bộ phân luồng đủ chính xác và tỷ lệ escalation thấp. Hedged request gửi song song hai nhà cung cấp giảm tail latency nhưng có thể gần nhân đôi chi phí, chỉ nên dùng cho tác vụ mà độ trễ có giá trị kinh doanh rõ ràng và có khả năng hủy request thua.\n\n### 10. Telemetry biến tối ưu thành thí nghiệm\n\nDashboard cần cả lưu lượng, tiền và chất lượng trên cùng trục phiên bản. Ít nhất theo dõi token/request p50–p99, cached ratio, output/input ratio, retry amplification, cost/success, tỷ lệ incomplete, latency và task success. Gắn `prompt_version` giúp thấy một thay đổi câu chữ làm input tăng; gắn `release` giúp rollback đúng bản.\n\nAnomaly alert nên dựa trên tốc độ và baseline theo giờ/ngày, không chỉ số tiền tuyệt đối. Lưu lượng tối thứ sáu khác sáng thứ hai; một ngưỡng cứng tạo nhiều báo động giả. Tuy nhiên kill switch phải đơn giản và được diễn tập: tắt feature thử nghiệm, hạ quota, chuyển FAQ sang template, nhưng giữ route an toàn thiết yếu.\n\nTối ưu theo vòng lặp: nêu giả thuyết, chạy eval offline, canary một phần lưu lượng, đo cost/success và lỗi theo slice, rồi mở rộng hoặc rollback. Không gộp thay model, đổi prompt và đổi retriever vào một lần vì sẽ không biết yếu tố nào tạo kết quả.\n\n### 11. Khi nào không nên tối ưu thêm\n\nKhông cắt token nếu phần bị cắt là bằng chứng cần cho factuality, instruction an toàn hay trạng thái authorization. Không hạ model khi eval cho thấy nhóm ngôn ngữ hoặc intent hiếm tụt dưới gate. Không dùng semantic cache cho dữ liệu cá nhân biến động nhanh. Không ép batch cho tương tác cần phản hồi tức thời. Không đặt trần kết quả thấp đến mức client phải retry, vì tổng chi phí có thể cao hơn cấu hình ban đầu.\n\nQuy tắc dừng hợp lý là tối ưu cho đến khi chi phí biên của kỹ thuật, rủi ro và chất lượng lớn hơn tiền tiết kiệm kỳ vọng. Một tuần kỹ sư để tiết kiệm một khoản rất nhỏ mỗi tháng không phải chiến thắng. Ngược lại, sửa vòng retry vô hạn vừa giảm tiền vừa tăng độ tin cậy nên thường đáng làm trước.",

vi_du: "### Bộ tính ngân sách chạy offline cho Trợ lý Mây\n\nScript sau không gọi API và không bịa bảng giá. Người vận hành truyền đơn giá hiện hành vào `Price`; hàm nhận usage đã chuẩn hóa từ response, dùng `Decimal` và từ chối dữ liệu vô lý. Chạy bằng `python cost_guard.py`.\n\n```python\nfrom dataclasses import dataclass\nfrom decimal import Decimal, ROUND_HALF_UP\n\nMILLION = Decimal(\"1000000\")\n\n@dataclass(frozen=True)\nclass Price:\n    input_per_million: Decimal\n    cached_input_per_million: Decimal\n    output_per_million: Decimal\n\n@dataclass(frozen=True)\nclass Usage:\n    input_tokens: int\n    cached_input_tokens: int\n    output_tokens: int\n\ndef request_cost(usage: Usage, price: Price) -> Decimal:\n    values = (usage.input_tokens, usage.cached_input_tokens, usage.output_tokens)\n    if any(type(x) is not int or x < 0 for x in values):\n        raise ValueError(\"token phải là số nguyên không âm\")\n    if usage.cached_input_tokens > usage.input_tokens:\n        raise ValueError(\"cached token không thể lớn hơn tổng input token\")\n\n    uncached = usage.input_tokens - usage.cached_input_tokens\n    total = (\n        Decimal(uncached) * price.input_per_million\n        + Decimal(usage.cached_input_tokens) * price.cached_input_per_million\n        + Decimal(usage.output_tokens) * price.output_per_million\n    ) / MILLION\n    return total.quantize(Decimal(\"0.000001\"), rounding=ROUND_HALF_UP)\n\ndef allow_request(estimated_input: int, output_cap: int,\n                  context_limit: int, remaining_tokens: int) -> bool:\n    if min(estimated_input, output_cap, context_limit, remaining_tokens) < 0:\n        return False\n    fits_context = estimated_input + output_cap <= context_limit\n    fits_budget = estimated_input + output_cap <= remaining_tokens\n    return fits_context and fits_budget\n\nif __name__ == \"__main__\":\n    # Giá minh họa do người chạy chủ động cấu hình, không phải giá của model cụ thể.\n    price = Price(Decimal(\"2.00\"), Decimal(\"0.50\"), Decimal(\"8.00\"))\n    samples = {\n        \"faq\": Usage(2400, 1800, 180),\n        \"summary\": Usage(12000, 0, 420),\n        \"empty\": Usage(0, 0, 0),\n    }\n    for name, usage in samples.items():\n        print(name, request_cost(usage, price))\n\n    assert request_cost(samples[\"empty\"], price) == Decimal(\"0.000000\")\n    assert allow_request(3000, 500, 4096, 10000)\n    assert not allow_request(3900, 500, 4096, 10000)\n    try:\n        request_cost(Usage(10, 11, 0), price)\n    except ValueError:\n        print(\"invalid_usage rejected\")\n```\n\nVới đúng dữ liệu minh họa trong code, kết quả là:\n\n```text\nfaq 0.003540\nsummary 0.027360\nempty 0.000000\ninvalid_usage rejected\n```\n\nTa có thể kiểm tay dòng `faq`: 600 token không cache × 2/1.000.000, cộng 1.800 token cache × 0,5/1.000.000, cộng 180 token kết quả × 8/1.000.000, bằng 0,003540. Kết quả chạy và phép tính độc lập khớp nhau; đây là bằng chứng tốt hơn việc chép một con số vào tài liệu. Trong production, adapter của từng provider chuyển response gốc thành `Usage`, còn hàm tính tiền giữ độc lập và được kiểm thử bằng invoice fixture.\n\n### Áp dụng guard trước và sau request\n\nTrước khi gọi, `allow_request` dùng ước lượng bảo thủ để chặn request chắc chắn vượt cửa sổ hoặc quota. Sau khi gọi, hệ thống không lấy ước lượng để trừ quota mà lấy usage thật. Nếu response thiếu usage do mất kết nối, ghi trạng thái `usage_unknown` và đối soát sau; tuyệt đối không tự coi là 0. Ca đầu vào rỗng vẫn có thể có system prompt ở API thật, nên `Usage(0,0,0)` chỉ kiểm hàm toán học chứ không khẳng định request rỗng miễn phí.",

so_sanh: "### So sánh các đòn bẩy theo tác dụng phụ\n\n| Cách làm | Giảm thành phần nào | Điểm mạnh | Rủi ro / lúc không nên dùng |\n|---|---|---|---|\n| bỏ request hoặc khử trùng lặp | toàn bộ request | tiết kiệm lớn, thường tăng độ tin cậy | không bỏ khi LLM cung cấp phán đoán cần thiết |\n| thu gọn ngữ cảnh/RAG | input token | giảm tiền và latency | mất bằng chứng, làm câu trả lời bịa hoặc quên ràng buộc |\n| prompt caching | giá của đoạn đầu trùng | giữ nguyên thông tin cho model | cache miss do đoạn đầu biến động; chính sách giá/lưu giữ phụ thuộc dịch vụ |\n| giới hạn kết quả/structured output | output token | kết quả gọn, dễ parse | cắt giữa chừng, thiếu reasoning budget, retry tăng |\n| model routing | đơn giá và năng lực | trả model mạnh cho ca khó | classifier sai; nhóm hiếm chịu chất lượng thấp |\n| batch chính thức | giá hoặc throughput tùy dịch vụ | tốt cho việc nền không gấp | tăng độ trễ, không hợp tác vụ tương tác |\n| semantic cache | cả input và output | phản hồi rất nhanh với FAQ ổn định | stale data, rò dữ liệu nếu khóa cache thiếu tenant/quyền |\n\nBilling alert, request guard và eval gate giải ba vấn đề khác nhau. Alert báo “đã tiêu bất thường”; guard ngăn một request/quota vượt trần; eval gate ngăn bản tối ưu rẻ nhưng sai được phát hành. Một hệ thống trưởng thành cần cả ba. Nếu chỉ có alert, phản ứng đến sau thiệt hại; chỉ có guard, mọi request dưới trần vẫn có thể cộng thành khoản lớn; chỉ có eval, vòng lặp production vẫn có thể nhân lưu lượng ngoài dự kiến.",

loi_thuong_gap: [
  "Chỉ nhìn `total_tokens` và bỏ qua cached/output/reasoning/tool usage. Dấu hiệu: hai ngày có tổng token gần nhau nhưng tiền lệch mạnh mà dashboard nội bộ không giải thích được.",
  "Ước lượng token rồi dùng nó làm số liệu hóa đơn. Dấu hiệu: tổng nội bộ luôn lệch dashboard và độ lệch tăng với JSON, tiếng Việt hoặc model suy luận.",
  "Hạ model cho mọi route mà không chạy eval theo slice. Dấu hiệu: cost/request giảm nhưng recontact, escalation hoặc lỗi intent hiếm tăng.",
  "Cắt lịch sử bằng số message cố định. Dấu hiệu: trợ lý hỏi lại dữ kiện người dùng đã cung cấp hoặc mất ràng buộc quan trọng nằm ở lượt cũ.",
  "Retry mọi lỗi với cùng payload. Dấu hiệu: `attempts/operation` tăng, lỗi 4xx lặp nguyên văn và một sự kiện nghiệp vụ có nhiều request ID.",
  "Đặt dữ liệu biến động ở đầu prompt rồi kỳ vọng cache trúng. Dấu hiệu: prompt dài và tương tự nhưng `cached_input_tokens / input_tokens` gần bằng 0.",
  "Đặt trần kết quả quá thấp rồi parse như response hoàn chỉnh. Dấu hiệu: JSON hỏng, trạng thái incomplete hoặc request kế tiếp retry cùng câu hỏi.",
  "Ghi prompt và dữ liệu khách hàng nguyên văn vào log chi phí. Dấu hiệu: dashboard tài chính truy cập được địa chỉ, nội dung chat hoặc thông tin cá nhân không cần thiết."
],

tu_kiem_tra: [
  "Vì sao `cost/request` giảm chưa đủ chứng minh tối ưu thành công? Hãy đề xuất mẫu số phản ánh kết quả nghiệp vụ cho ba luồng của Trợ lý Mây.",
  "Một request có 10.000 input token, trong đó 7.000 cached token. Hãy viết công thức tính tiền mà không đếm cached token hai lần.",
  "Tại sao đặt UUID ở dòng đầu prompt có thể làm giảm lợi ích prompt caching dù phần còn lại giống hệt nhau?",
  "Thiết kế ba tầng budget cho route đổi địa chỉ và nêu hành vi giảm cấp an toàn ở mỗi tầng; vì sao không nên tự động chuyển sang model yếu nhất?",
  "Nếu p50 token/request không đổi nhưng p99 tăng gấp mười sau release, bạn sẽ kiểm tra những dimension và log nào trước?",
  "Một model rẻ có task success 92%, model đắt có 97%. Cần thêm những dữ liệu gì để quyết định, thay vì chỉ chia giá cho tỷ lệ thành công?",
  "Thay ba đơn giá minh họa trong script, tự tính dòng `faq` bằng tay rồi chạy chương trình để đối chiếu; những loại lỗi làm tròn hoặc đếm trùng nào có thể khiến hai kết quả lệch nhau?",
  "Khi nào tóm tắt hội thoại làm tổng chi phí tăng thay vì giảm, xét cả chi phí tạo tóm tắt, số lượt tái sử dụng và nguy cơ phải hỏi lại?"
],

tai_nguyen: [
  "Designing Machine Learning Systems — Chip Huyen.",
  "Building Machine Learning Powered Applications — Emmanuel Ameisen.",
  "Machine Learning Design Patterns — Valliappa Lakshmanan, Sara Robinson, Michael Munn.",
  "Official OpenAI API Reference — mục Responses, usage và max output tokens; tra bản hiện hành thay vì ghi nhớ bảng giá."
],

lien_he: [
  "Nối sang **FinOps / Cloud Cost Management** qua unit economics, tagging, budget nhiều tầng, anomaly detection và đối soát hóa đơn theo ngày hiệu lực.",
  "Nối sang **Distributed Systems** qua idempotency, exponential backoff, circuit breaker, timeout và việc retry biến một lỗi thành bộ nhân chi phí.",
  "Nối sang **Data Engineering / Observability** qua event schema, percentile, aggregation theo dimension, bảo vệ dữ liệu cá nhân và dashboard cost–quality.",
  "Nối sang **Software Testing** qua fixture hóa đơn, ca biên số token, eval gate và canary để chứng minh tối ưu không làm hỏng hành vi.",
  "Nối sang **Product Analytics** qua cost per successful resolution, recontact, escalation và quyết định xem một tính năng có unit economics bền vững hay không."
]

};
