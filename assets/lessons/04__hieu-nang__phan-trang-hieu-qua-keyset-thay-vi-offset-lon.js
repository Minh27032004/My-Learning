window.LESSONS["04:hieu-nang:phan-trang-hieu-qua-keyset-thay-vi-offset-lon"] = {

tom_tat: [
  "`OFFSET k` buộc PostgreSQL tạo rồi bỏ qua k hàng trước khi trả trang; index đúng thứ tự giúp tránh sort nhưng không làm việc bỏ qua trở thành miễn phí.",
  "Keyset pagination dùng khóa của hàng cuối trang trước làm ranh giới: với thứ tự `created_at DESC, id DESC`, trang sau có điều kiện tuple `(created_at, id) < ($cursor_time, $cursor_id)`.",
  "Khóa sắp xếp phải tạo thứ tự toàn phần, ổn định và tương thích index; `id` thường là khóa phá hòa khi nhiều hàng có cùng timestamp.",
  "Keyset giữ chi phí gần với kích thước trang và ít trùng/bỏ hàng hơn OFFSET khi có insert mới, nhưng không hỗ trợ nhảy đến trang 500 theo cách tự nhiên.",
  "Con trỏ API nên mã hóa cả khóa vị trí lẫn ngữ cảnh lọc, có kiểm tra tính toàn vẹn và không được được hiểu là ảnh chụp dữ liệu bất biến."
],

can_biet_truoc: [
  "`ORDER BY`, `LIMIT`, `OFFSET` và quy tắc so sánh tuple của PostgreSQL.",
  "Composite B-tree index, hướng `ASC`/`DESC`, leftmost prefix và index condition.",
  "Cách đọc `Limit`, `Sort`, `Index Scan`, `actual rows`, `loops` và `BUFFERS` trong query plan.",
  "Khái niệm HTTP API không trạng thái và mã hóa Base64 ở mức biểu diễn, không phải mã hóa bảo mật."
],

dinh_nghia: "**Keyset pagination** là cách phân trang lấy trang kế tiếp bằng một điều kiện phạm vi bắt đầu sau khóa sắp xếp của hàng cuối đã thấy, thay vì đếm và bỏ qua một số hàng. Nó còn được gọi là seek pagination vì database “tìm đến” ranh giới trong index.\n\nVới danh sách đơn sắp theo `created_at DESC, id DESC`, con trỏ logic là cặp `(created_at, id)`. Trang đầu không có ranh giới; trang sau thêm `WHERE (created_at, id) < ($1, $2)`, giữ nguyên `ORDER BY`, rồi `LIMIT`. Dấu `<` đúng vì ta đi từ giá trị lớn xuống nhỏ. `id` bảo đảm hai đơn trùng timestamp vẫn có vị trí khác nhau.",

vi_sao_can: "Trang đầu bằng `OFFSET 0 LIMIT 50` thường nhanh. Sự cố chỉ lộ khi người dùng hoặc crawler tới sâu: `OFFSET 100000` yêu cầu executor đi qua 100.000 hàng hợp lệ rồi vứt chúng trước khi trả 50 hàng. Nếu query dùng index, plan vẫn có thể trông “đẹp” với `Index Scan`, nhưng `actual rows` ở scan xấp xỉ 100.050 và buffer tăng theo độ sâu. Nếu không có index phục vụ thứ tự, database còn phải lọc/sort tập lớn.\n\nDữ liệu thay đổi tạo lỗi trải nghiệm khác. Người dùng lấy trang 1, một đơn mới được chèn lên đầu, rồi lấy trang 2 bằng `OFFSET 50`: hàng từng ở vị trí 50 bị đẩy xuống 51 và có thể xuất hiện lần nữa. Nếu hàng bị xóa, một hàng chưa xem có thể dạt vào phần đã bỏ qua và bị mất. OFFSET định vị bằng **số ghế trong danh sách đang chuyển động**; keyset định vị bằng **giá trị của hàng đã thấy**.\n\nTrang quản trị đơn hàng là trường hợp phù hợp: người dùng chủ yếu cuộn tiến/lùi theo thời gian, không cần nhảy chính xác đến trang 2.347, và endpoint đã có index `(status, created_at DESC, id DESC)`. Cùng index phục vụ điều kiện `status='PAID'`, thứ tự và phép seek. Tuy nhiên keyset không tự tạo snapshot: update khóa sắp xếp hoặc xóa/chèn phía sau con trỏ vẫn ảnh hưởng trang sau. Phải hiểu đảm bảo thật thay vì gọi nó “nhất quán tuyệt đối”.",

co_che: "### OFFSET làm việc gì bên dưới cú pháp ngắn\n\n```sql\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status = 'PAID'\nORDER BY created_at DESC, id DESC\nOFFSET 100000 LIMIT 50;\n```\n\nExecutor không thể nhảy đến “hàng thứ 100001” trong B-tree theo ordinal vì index lưu khóa và vị trí tuple, không duy trì số lượng hàng nhìn thấy cho mỗi nhánh theo MVCC. Nó đọc luồng đã sắp, kiểm tra visibility, bỏ 100.000 hàng rồi chuyển 50 hàng tiếp. Xóa và phiên bản tuple cũ khiến khái niệm vị trí còn phụ thuộc snapshot.\n\nVì vậy độ phức tạp công việc gần O(k + p), với k là offset và p là page size. Index loại O(n log n) sort, nhưng không loại k hàng bị tiêu thụ. Plan cần đọc cả hàng ở node con của `Limit`: node `Limit` trả 50 không có nghĩa index scan chỉ tạo 50.\n\n### Seek biến số đếm thành ranh giới khóa\n\nTrang đầu:\n\n```sql\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status = 'PAID'\nORDER BY created_at DESC, id DESC\nLIMIT 50;\n```\n\nGiả sử hàng cuối có `created_at='2025-05-10T08:30:00Z'`, `id=7312`. Trang sau:\n\n```sql\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status = 'PAID'\n  AND (created_at, id) < ($1::timestamptz, $2::bigint)\nORDER BY created_at DESC, id DESC\nLIMIT 50;\n```\n\nB-tree định vị cặp ngay dưới ranh giới rồi quét tiếp khoảng 50 entry. Công việc gần O(log n + p): log n để tìm điểm và p để trả trang. Đây là mô hình, không phải lời hứa thời gian cố định; visibility, filter không nằm trong index, heap fetch và contention vẫn tạo chi phí.\n\n### Thứ tự toàn phần cần khóa phá hòa\n\nChỉ `ORDER BY created_at DESC` không đủ. PostgreSQL được phép trả các hàng có cùng timestamp theo bất kỳ thứ tự nào; plan khác hoặc vacuum có thể đổi thứ tự. Nếu cursor chỉ chứa timestamp và dùng `<`, mọi hàng cùng timestamp nhưng chưa lọt trang trước sẽ bị bỏ. Dùng `<=` lại gây lặp.\n\nThêm `id DESC` tạo thứ tự toàn phần vì primary key duy nhất. Cursor phải chứa **mọi biểu thức ORDER BY cần để xác định vị trí**. Nếu nghiệp vụ sắp `priority DESC, created_at ASC, id ASC`, cursor cần cả ba và điều kiện phải phản ánh hướng hỗn hợp; tuple comparison đơn giản chỉ tiện khi các thành phần cùng hướng.\n\n### So sánh tuple là phép so từ trái sang phải\n\n`(created_at, id) < (t, i)` tương đương:\n\n```sql\ncreated_at < t\nOR (created_at = t AND id < i)\n```\n\nPostgreSQL so cột đầu; chỉ khi bằng mới xét cột sau. Biểu thức tuple ngắn hơn, ít lỗi ngoặc và khớp composite index. Với `DESC`, hàng “sau” có khóa nhỏ hơn nên dùng `<`; với `ASC`, dùng `>`. Sai dấu thường tạo lại trang cũ hoặc đi ngược đầu danh sách.\n\nGiá trị `NULL` phá tính trực quan vì so sánh với `NULL` cho unknown. Tốt nhất khóa pagination là `NOT NULL`. Nếu nghiệp vụ phải sort nullable với `NULLS LAST`, hãy chuẩn hóa thành khóa không null có quy tắc rõ, hoặc viết điều kiện phân nhánh tương ứng và tạo expression index; không nhét `NULL` vào cursor rồi mong tuple comparison tự hiểu.\n\n### Index phải cùng hình dạng bộ lọc và thứ tự\n\n```sql\nCREATE INDEX orders_status_created_id_idx\nON orders (status, created_at DESC, id DESC)\nINCLUDE (customer_id);\n```\n\nEquality trên `status` cố định đoạn đầu index; bên trong đoạn `PAID`, entry đã theo `created_at, id` giảm dần. Planner có thể dùng điều kiện seek làm `Index Cond` và dừng sớm sau limit. Nếu endpoint chỉ bao giờ lấy `PAID`, partial index `ON orders(created_at DESC, id DESC) WHERE status='PAID'` nhỏ hơn, nhưng không phục vụ trạng thái khác và prepared predicate phải được planner chứng minh phù hợp.\n\nNếu thêm filter `customer_id=$3` mà index không hỗ trợ, database có thể đi theo thời gian rồi loại nhiều hàng trước khi đủ 50. Khi đó chi phí phụ thuộc độ hiếm customer. Không có một index phục vụ tối ưu mọi tổ hợp filter; API cần giới hạn query shape hoặc thiết kế vài index theo endpoint quan trọng, tránh sinh index cho mọi hoán vị.\n\n### Lấy `page_size + 1` để biết còn trang\n\nThay vì `COUNT(*)` toàn bộ để tạo `has_next_page`, lấy 51 hàng cho page size 50. Nếu có hàng thứ 51, trả 50 và tạo cursor từ hàng thứ 50; nếu không, đã hết. `COUNT(*)` có thể đắt và dưới dữ liệu thay đổi cũng không bảo đảm số trang tương lai.\n\nKhông tạo next cursor từ hàng thứ 51 vì hàng ấy chưa được gửi; dùng nó làm ranh giới với dấu `<` sẽ bỏ mất chính hàng đó. Cursor phải mô tả phần tử cuối client đã nhận. Với trang rỗng, không có next cursor. Với page size 0, API nên từ chối hoặc định nghĩa rõ, thay vì phát cursor vô nghĩa.\n\n### Cursor là hợp đồng, không phải chuỗi timestamp\n\nMột payload có thể là JSON:\n\n```json\n{\"v\":1,\"created_at\":\"2025-05-10T08:30:00.000Z\",\"id\":\"7312\",\"status\":\"PAID\"}\n```\n\nSau đó mã hóa base64url để dùng trong URL. Base64 chỉ làm opaque (khó đọc trực tiếp), ai cũng sửa được. Nếu việc sửa cursor có thể vượt tenant/filter hoặc gây tải bằng khóa cực đoan, ký payload bằng HMAC và kiểm tra chữ ký. Luôn parse kiểu chặt, giới hạn độ dài, kiểm tra version và trả lỗi 400 cho cursor hỏng.\n\nĐưa `status` hoặc fingerprint của filter/sort vào cursor giúp ngăn client lấy cursor của danh sách `PAID` dùng cho `CANCELLED`. Không nhất thiết nhét mọi dữ liệu nhạy cảm; server có thể ký một cấu trúc tối thiểu. `id` bigint nên biểu diễn bằng chuỗi trong JSON để tránh mất chính xác ở JavaScript vượt 2⁵³−1.\n\n### Dữ liệu chèn mới giữa hai trang\n\nSau trang 1, một order mới có khóa lớn hơn cursor được chèn ở đầu. Trang 2 với điều kiện `< cursor` không thấy order mới và cũng không làm hàng cũ bị lặp; đây thường là hành vi mong muốn cho infinite scroll. Refresh từ đầu sẽ thấy dữ liệu mới.\n\nNếu order mới được backdate với khóa nhỏ hơn cursor, nó có thể xuất hiện ở trang sau dù được tạo sau lần đọc đầu. Keyset không cố định snapshot qua nhiều HTTP request. Muốn export nhất quán, cần transaction snapshot giữ trên server, mốc upper bound, hoặc read replica/snapshot workflow; mỗi lựa chọn có chi phí và vòng đời riêng.\n\n### Update khóa sắp xếp và chuyển trạng thái\n\nNếu `created_at` bất biến, cursor ổn định hơn. Nếu sort theo `updated_at`, một hàng có thể nhảy lên đầu sau khi client đã đi qua, nên bị bỏ trong phiên cuộn; hoặc nhảy xuống dưới và xuất hiện lại. Keyset giảm lỗi do thay đổi trước ranh giới nhưng không giải quyết hàng tự đổi khóa.\n\nThiết kế feed thường dùng khóa bất biến như `(created_at, id)` hoặc sequence. Nếu cần bảng xếp hạng theo điểm luôn thay đổi, chấp nhận eventual navigation, dùng snapshot/version, hoặc materialize ranking theo kỳ. Phải nói rõ semantics cho client, không thể vừa live tuyệt đối vừa duyệt mỗi hàng đúng một lần mà không giữ trạng thái.\n\n### Trang trước và hướng đảo\n\nĐể lấy các hàng ngay trước cursor trong danh sách DESC, điều kiện đổi thành `>` và query tạm thời sắp `ASC` để lấy những hàng gần ranh giới nhất:\n\n```sql\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status = 'PAID'\n  AND (created_at, id) > ($1, $2)\nORDER BY created_at ASC, id ASC\nLIMIT 50;\n```\n\nỨng dụng đảo mảng kết quả trước khi trả để giao diện vẫn DESC. Nếu cứ `ORDER BY DESC` với `>`, query lấy 50 hàng xa nhất ở đầu danh sách, không phải trang liền trước. Cursor hai chiều thường có `startCursor` và `endCursor`; tên `before`/`after` nên gắn với thứ tự logic, không gắn cứng dấu toán học.\n\n### Filter thay đổi làm cursor mất nghĩa\n\nCursor từ `status=PAID, customer=7` không nên dùng sau khi người dùng bỏ customer filter. Ranh giới khóa vẫn hợp lệ cú pháp nhưng vị trí thuộc tập khác, khiến client bỏ một đoạn khó giải thích. API có thể từ chối vì fingerprint filter không khớp, hoặc UI xóa cursor và tải lại trang đầu mỗi khi filter/sort đổi.\n\nSearch text càng khó: relevance score có thể thay đổi theo index/search engine và nhiều hàng đồng điểm. Cần score cộng khóa duy nhất, phiên bản thuật toán hoặc point-in-time token do search engine cung cấp. Keyset là pattern chung nhưng cursor phải phản ánh thứ tự thực, không chỉ copy `id`.\n\n### Không có nhảy trang ngẫu nhiên miễn phí\n\nKeyset giỏi “trang kế tiếp”, không biết trực tiếp “trang 500”. Nếu sản phẩm bắt buộc số trang và nhảy bất kỳ, OFFSET có thể là lựa chọn đúng cho tập nhỏ hoặc độ sâu bị giới hạn. Phương án lai: OFFSET cho vài trang đầu, keyset cho “xem thêm”; lưu checkpoint cursor định kỳ; hoặc điều hướng theo khoảng thời gian thay vì số trang.\n\nHiển thị “1–50 trong 2.431.992” còn cần count, có thể dùng số ước lượng, cache hoặc job tổng hợp tùy mức chính xác. Đừng chạy exact `COUNT(*)` nặng trên mọi request chỉ để trang trí. UX có thể dùng `hasNextPage` mà không cần tổng.\n\n### Đọc plan để xác minh seek thật\n\nPlan tốt thường có `Limit` trên `Index Scan`/`Index Only Scan`, với cả status và ranh giới tuple ở `Index Cond`, không có sort lớn. So trang đầu, OFFSET sâu và keyset bằng cùng dữ liệu, cache condition và tùy chọn EXPLAIN. Quan sát số hàng scan, block và temp, không ghi một số ms duy nhất thành chân lý.\n\nNếu điều kiện tuple nằm ở `Filter` thay vì `Index Cond`, kiểm tra kiểu dữ liệu, cast, collation, biểu thức và thứ tự index. Bọc cột bằng hàm như `date(created_at)` có thể ngăn dùng index thường; expression index hoặc viết khoảng timestamp nguyên bản sẽ hợp hơn.\n\n### Khi keyset không đáng dùng\n\nDanh sách vài trăm hàng, trang chỉ truy cập nông, trang báo cáo tĩnh cần nhảy số, hoặc query đã materialize nhỏ có thể dùng OFFSET cho đơn giản. Keyset làm API phức tạp hơn: encode/sign cursor, xử lý chiều, thay đổi filter và test ca biên. Không nên trả cursor mà bên trong vẫn dùng OFFSET; đó chỉ che chi phí.\n\nNgược lại, đừng trì hoãn keyset cho feed vô hạn hoặc bảng hàng triệu bản ghi chỉ vì OFFSET dễ viết. Dấu hiệu quyết định là plan của trang sâu, semantics khi dữ liệu thay đổi và hành vi người dùng, không phải khẩu hiệu “cursor luôn nhanh”.",

vi_du: "### SQL hoàn chỉnh cho trang đơn hàng\n\nSchema và dữ liệu dùng lại bài EXPLAIN. Query lấy dư một hàng:\n\n```sql\n-- Trang đầu\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status = 'PAID'\nORDER BY created_at DESC, id DESC\nLIMIT 51;\n\n-- Trang kế, thay hai literal bằng hàng cuối thực sự đã trả\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status = 'PAID'\n  AND (created_at, id) <\n      (timestamptz '2025-05-10 08:30:00+00', 7312::bigint)\nORDER BY created_at DESC, id DESC\nLIMIT 51;\n```\n\nIndex:\n\n```sql\nCREATE INDEX IF NOT EXISTS orders_status_created_id_idx\nON orders (status, created_at DESC, id DESC)\nINCLUDE (customer_id);\nANALYZE orders;\n```\n\n### Đối chứng OFFSET và keyset bằng plan thật\n\nKhông điền sẵn thời gian vì máy chạy quyết định kết quả. Trước hết lấy một ranh giới có thật ở vị trí sâu chỉ để thí nghiệm:\n\n```sql\nSELECT created_at, id\nFROM orders\nWHERE status = 'PAID'\nORDER BY created_at DESC, id DESC\nOFFSET 50000 LIMIT 1;\n```\n\nSau đó dùng cặp nhận được cho query keyset và chạy:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS, TIMING OFF)\nSELECT id, customer_id, created_at\nFROM orders WHERE status='PAID'\nORDER BY created_at DESC, id DESC\nOFFSET 50000 LIMIT 50;\n\nEXPLAIN (ANALYZE, BUFFERS, TIMING OFF)\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status='PAID'\n  AND (created_at, id) < ($$2025-05-10 08:30:00+00$$::timestamptz, 7312)\nORDER BY created_at DESC, id DESC\nLIMIT 50;\n```\n\nPhải thay literal minh họa bằng cặp vừa truy vấn. So `actual rows` ở scan và `BUFFERS`; nếu dataset không có đủ 50.001 đơn `PAID`, giảm offset. Query tìm cursor bằng OFFSET chỉ là dụng cụ benchmark; API thật nhận cursor từ trang trước.\n\n### Mã hóa cursor an toàn về kiểu\n\n```js\nfunction encodeCursor(row) {\n  const payload = JSON.stringify({\n    v: 1,\n    created_at: new Date(row.created_at).toISOString(),\n    id: String(row.id),\n    status: 'PAID'\n  });\n  return Buffer.from(payload, 'utf8').toString('base64url');\n}\n\nfunction decodeCursor(raw) {\n  if (typeof raw !== 'string' || raw.length > 512) throw new Error('cursor không hợp lệ');\n  let value;\n  try { value = JSON.parse(Buffer.from(raw, 'base64url').toString('utf8')); }\n  catch { throw new Error('cursor không hợp lệ'); }\n  if (value.v !== 1 || value.status !== 'PAID' ||\n      !/^\\d+$/.test(value.id) || !Number.isFinite(Date.parse(value.created_at))) {\n    throw new Error('cursor không hợp lệ');\n  }\n  return value;\n}\n```\n\nĐây mới là validation, chưa có chống sửa. Production có yêu cầu integrity nên HMAC phần bytes payload với secret phía server và so chữ ký constant-time, hoặc dùng thư viện token đã được review. Không đưa secret vào cursor.\n\nCa biên phải test: bảng rỗng trả `items=[]` và `next=null`; đúng 50 hàng không cần next; 51 hàng trả 50 và có next; mọi hàng trùng timestamp vẫn không lặp nhờ id; cursor id vượt 2⁵³ vẫn nguyên vẹn vì giữ chuỗi; cursor sai version/filter bị 400; hàng cuối bị xóa sau trang trước vẫn dùng được vì cursor là giá trị, không cần hàng còn tồn tại.",

so_sanh: "### OFFSET, keyset và snapshot\n\n| Tiêu chí | OFFSET/LIMIT | Keyset/seek | Snapshot hoặc materialized result |\n|---|---|---|---|\n| Chi phí trang sâu | Tăng theo số hàng bỏ qua | Gần page size khi index khớp | Tùy chi phí tạo snapshot; đọc thường ổn định |\n| Insert ở đầu giữa hai request | Có thể gây lặp/trượt hàng | Không xê dịch ranh giới cũ | Không ảnh hưởng snapshot |\n| Update khóa sort | Có thể xê vị trí | Vẫn có thể lặp/bỏ nếu hàng nhảy qua cursor | Không ảnh hưởng bản chụp đã cố định |\n| Nhảy đến trang bất kỳ | Tự nhiên | Không tự nhiên, cần checkpoint | Có thể nếu lưu ordinal |\n| Tổng số trang chính xác | Dễ ghép `COUNT`, nhưng count có giá | Không cần; thường trả `hasNextPage` | Có thể biết khi materialize |\n| Độ phức tạp API | Thấp | Cursor, validation, chiều và filter | Quản lý vòng đời, storage, quyền truy cập |\n| Hợp với | Tập nhỏ, báo cáo tĩnh, trang nông | Feed, timeline, bảng lớn cuộn tuần tự | Export/audit cần ảnh chụp nhất quán |\n\nKeyset không thay thế mọi phân trang. Nó đánh đổi khả năng định vị theo số trang để lấy hiệu năng theo độ sâu và semantics ổn định hơn cho danh sách đang tăng.",

loi_thuong_gap: [
  "Chỉ đưa `created_at` vào cursor dù nhiều hàng trùng; nhận biết bằng mất hoặc lặp hàng tại ranh giới. Thêm khóa duy nhất vào ORDER BY, cursor và index.",
  "Dùng sai dấu cho thứ tự DESC; dấu hiệu trang sau lặp trang đầu hoặc đi về phía mới hơn. DESC thường seek bằng `<`.",
  "Index thiếu cột filter đứng trước hoặc thiếu thứ tự; plan hiện Sort/Filter và scan nhiều hàng. Thiết kế theo query shape thực.",
  "Tạo next cursor từ hàng lấy dư thứ 51; dấu hiệu mỗi trang bỏ một hàng. Cursor phải lấy từ hàng thứ 50 đã trả.",
  "Tin rằng Base64 chống sửa; nhận biết client đổi filter/id trong payload vẫn được chấp nhận. Validate và ký khi integrity quan trọng.",
  "Dùng cursor cũ sau khi thay filter/sort; kết quả bắt đầu giữa một tập khác. Gắn fingerprint ngữ cảnh hoặc reset pagination.",
  "Ép bigint cursor thành JavaScript `Number`; ID lớn bị làm tròn và điều kiện seek sai. Truyền dưới dạng chuỗi tham số bigint.",
  "Tuyên bố keyset là snapshot nhất quán; backdated insert hay update khóa vẫn xuất hiện/lặp. Dùng snapshot riêng nếu nghiệp vụ yêu cầu."
],

tu_kiem_tra: [
  "Vì sao composite index có thể bỏ sort cho OFFSET nhưng vẫn phải đi qua k hàng?",
  "Với `ORDER BY created_at ASC, id ASC`, điều kiện trang sau dùng dấu nào và vì sao?",
  "Tại sao `created_at` không đủ làm cursor nếu độ phân giải timestamp khiến nhiều hàng bằng nhau?",
  "Lấy 51 hàng cho page size 50: cursor kế tiếp lấy từ vị trí nào, và trường hợp nào không trả cursor?",
  "Vì sao xóa hàng từng tạo cursor không làm cursor vô hiệu, còn đổi filter lại có thể làm nó mất nghĩa?",
  "Khó: thiết kế điều kiện trang sau cho `ORDER BY priority DESC, created_at ASC, id ASC`; giải thích vì sao tuple `<` đơn giản không còn đủ.",
  "Khó: sản phẩm vừa cần feed live vừa yêu cầu export mỗi hàng đúng một lần. Bạn tách hai workflow và semantics lưu trữ thế nào?",
  "Plan keyset vẫn đọc 20.000 hàng để trả 50 cho một customer hiếm. Bạn suy luận gì về thứ tự cột index và tập endpoint cần hỗ trợ?"
],

tai_nguyen: [
  "PostgreSQL Documentation — Indexes and ORDER BY, Row and Array Comparisons.",
  "PostgreSQL Documentation — LIMIT and OFFSET, Index-Only Scans.",
  "SQL Performance Explained — Markus Winand, phần pagination và partial results.",
  "Designing Data-Intensive Applications — Martin Kleppmann, phần ordering và snapshots."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: phép tìm ranh giới trong B-tree giải thích O(log n + p), còn index không lưu ordinal nhìn thấy theo MVCC.",
  "Nối sang mảng Thiết kế API: opaque cursor, validation, versioning và filter fingerprint là một phần của hợp đồng phân trang.",
  "Nối sang mảng Bảo mật: HMAC bảo vệ integrity của cursor, trong khi Base64 chỉ là cách biểu diễn và không che bí mật.",
  "Nối sang mảng Hệ phân tán: snapshot, eventual consistency và read model quyết định lời hứa “không lặp, không bỏ” qua nhiều request."
]

};
