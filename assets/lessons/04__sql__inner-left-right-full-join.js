window.LESSONS["04:sql:inner-left-right-full-join"] = {

tom_tat: [
  "JOIN ghép các dòng theo một điều kiện; loại JOIN quyết định giữ hay loại dòng không tìm được đối tác.",
  "`INNER JOIN` chỉ giữ cặp khớp, `LEFT JOIN` bảo toàn bên trái, `RIGHT JOIN` bảo toàn bên phải, còn `FULL JOIN` giữ cả hai phía.",
  "Điều kiện đặt trong `ON` tham gia tạo cặp; điều kiện trong `WHERE` lọc kết quả sau JOIN, nên có thể vô tình biến outer join thành inner join.",
  "Quan hệ một-nhiều làm số dòng tăng theo số cặp khớp; đó là đúng về quan hệ nhưng dễ làm sai phép tổng hợp nếu quên grain.",
  "Hiệu năng JOIN phụ thuộc cardinality, statistics, index trên khóa nối và thuật toán `Nested Loop`, `Hash Join` hoặc `Merge Join` mà planner chọn."
],

can_biet_truoc: [
  "Khóa chính, khóa ngoại, tính duy nhất và quan hệ một-nhiều trong schema quan hệ.",
  "`SELECT`, `WHERE`, logic `NULL` và cách đọc query plan PostgreSQL ở mức scan/index.",
  "Grain của `orders` và `order_items`, cùng bẫy nhân dòng đã gặp trong bài `GROUP BY`.",
  "Khái niệm tập hợp giao, hiệu và phép tích Descartes ở mức trực giác."
],

dinh_nghia: "`JOIN` tạo dòng kết quả bằng cách xét các cặp dòng từ hai nguồn và đánh giá điều kiện `ON`. **Inner join** chỉ xuất các cặp có điều kiện là `TRUE`. **Outer join** còn sinh dòng bổ sung cho phía được bảo toàn khi không có cặp: các cột phía thiếu được điền `NULL`.\n\n```sql\nSELECT o.order_id, c.full_name\nFROM orders AS o\nLEFT JOIN customers AS c\n  ON c.customer_id = o.customer_id;\n```\n\nĐơn mua tại quầy vẫn xuất hiện vì `orders` đứng bên được bảo toàn; `full_name` là `NULL`. `RIGHT JOIN` là hình ảnh đối xứng của `LEFT JOIN`. `FULL JOIN` bảo toàn cả hai, hữu ích khi đối soát hai tập nhưng ít phù hợp với chuỗi JOIN nghiệp vụ thông thường.",

vi_sao_can: "Chuẩn hóa dữ liệu cố ý tách khách, đơn và dòng vé để mỗi sự thật có một nơi cập nhật. Đổi lại, câu hỏi nghiệp vụ “An đã mua vé sự kiện nào?” phải ghép nhiều bảng. JOIN chính là thao tác tái tạo ngữ cảnh mà không sao chép tên khách vào từng đơn hay tên sự kiện vào từng item.\n\nChọn loại JOIN là chọn chính sách đối với dữ liệu thiếu. Báo cáo doanh thu theo khách có thể bỏ đơn vô danh bằng inner join; màn hình vận hành không được làm mất đơn mua tại quầy nên cần left join. Danh sách mọi khách kể cả chưa mua lại đặt `customers` bên trái. Hai câu nhìn gần giống nhưng khác đối tượng cần bảo toàn.\n\nSai JOIN rất nguy hiểm vì thường không báo lỗi. Quên một phần khóa nối tạo nhiều cặp; lọc cột bên phải trong `WHERE` làm mất dòng unmatched; dùng `COUNT(*)` trên left join khiến khách chưa mua trông như có một đơn. Vì vậy bài này theo dõi số dòng và nguồn của `NULL`, không chỉ học bốn từ khóa.",

co_che: "### Hình dung bằng tập cặp, không bằng hình Venn\n\nVới mỗi dòng trái, executor tìm mọi dòng phải làm `ON` thành `TRUE`. Một dòng trái có ba item sẽ tạo ba dòng kết quả. Hai dòng phải cùng khóa tạo hai cặp. Đây là bag semantics: SQL giữ bản sao, không tự khử trùng như tập toán học.\n\n| Loại | Cặp khớp | Trái không khớp | Phải không khớp |\n|---|---:|---:|---:|\n| INNER | giữ | bỏ | bỏ |\n| LEFT | giữ | giữ với NULL phải | bỏ |\n| RIGHT | giữ | bỏ | giữ với NULL trái |\n| FULL | giữ | giữ | giữ |\n\nHình Venn dễ gợi nhầm rằng JOIN chỉ thao tác trên tập khóa; thực tế một khóa có thể lặp và tạo nhiều dòng. Đếm cặp là cách tư duy chính xác hơn.\n\n### INNER JOIN: yêu cầu tồn tại hai phía\n\n```sql\nSELECT o.order_id, c.full_name\nFROM orders o\nJOIN customers c ON c.customer_id = o.customer_id;\n```\n\n`JOIN` không ghi loại mặc định là `INNER JOIN`. Đơn có `customer_id NULL` không khớp vì `NULL = c.customer_id` là `UNKNOWN`, không phải `TRUE`. Khóa ngoại ngăn ID không tồn tại nhưng vẫn cho phép NULL nếu cột nullable. Nếu cột `NOT NULL` và foreign key hợp lệ, inner join orders→customers sẽ không làm mất đơn; planner thậm chí có thể loại bỏ JOIN trong vài trường hợp khi cột bên phải không được dùng.\n\n### LEFT JOIN: chọn phía theo câu hỏi\n\n“Mọi khách và các đơn của họ”:\n\n```sql\nSELECT c.customer_id, c.full_name, o.order_id\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.customer_id;\n```\n\nKhách chưa mua sinh đúng một dòng có `o.order_id NULL`. “Mọi đơn và khách nếu biết” phải đảo nguồn: `orders LEFT JOIN customers`. LEFT không tốt hơn INNER; nó đúng khi phía trái là quần thể báo cáo. Đặt bảng theo câu hỏi giúp đọc dễ hơn việc dùng RIGHT để giữ một bảng nằm bên phải.\n\n### ON khác WHERE trong outer join\n\nHai câu sau không tương đương:\n\n```sql\n-- Giữ mọi khách; chỉ ghép đơn paid\nFROM customers c\nLEFT JOIN orders o\n  ON o.customer_id = c.customer_id AND o.status = 'paid'\n\n-- Ghép mọi đơn, rồi loại dòng không phải paid, gồm cả NULL bổ sung\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.customer_id\nWHERE o.status = 'paid'\n```\n\nỞ câu hai, khách chưa mua có `o.status NULL`; điều kiện `= 'paid'` là `UNKNOWN`, nên bị WHERE loại. Kết quả hành xử như inner join đối với điều kiện này. Nếu mục tiêu là mọi khách, filter phía tùy chọn thuộc `ON`. Nếu thật sự muốn chỉ khách đã có đơn paid, inner join diễn đạt thẳng hơn.\n\n### RIGHT JOIN: hợp lệ nhưng thường có thể đảo\n\n```sql\nSELECT c.full_name, o.order_id\nFROM orders o\nRIGHT JOIN customers c ON c.customer_id = o.customer_id;\n```\n\nCâu này tương đương `customers c LEFT JOIN orders o`. RIGHT hữu ích khi chỉnh một chuỗi query và muốn bảo toàn nguồn bên phải mà không đảo nhiều đoạn, hoặc khi đọc phép biến đổi đối xứng. Trong code ứng dụng, chuỗi left join thường dễ theo dõi “bảng gốc” hơn. Không cấm RIGHT; chỉ đòi lý do về khả năng đọc.\n\n### FULL JOIN: đối soát hai hệ thống\n\nFULL JOIN phát huy khi hai tập đều có dòng mồ côi. Giả sử staging chứa tổng thanh toán từ cổng:\n\n```sql\nCREATE TEMP TABLE gateway_payments(\n  gateway_ref text PRIMARY KEY,\n  order_id bigint,\n  amount numeric(12,2)\n);\n\nSELECT o.order_id, g.gateway_ref, o.total_amount, g.amount\nFROM orders o\nFULL JOIN gateway_payments g ON g.order_id = o.order_id\nWHERE o.order_id IS NULL\n   OR g.order_id IS NULL\n   OR o.total_amount IS DISTINCT FROM g.amount;\n```\n\nDòng chỉ ở orders chỉ thiếu gateway; dòng chỉ ở gateway có thể là callback tới trước import hoặc tham chiếu lỗi; dòng cả hai nhưng lệch tiền cần điều tra. `IS DISTINCT FROM` so sánh an toàn với NULL. Trong mô hình đã có foreign key chặt, FULL JOIN giữa bảng cha-con thường ít ý nghĩa hơn truy vấn đối soát nguồn độc lập.\n\n### NULL thật và NULL do outer join\n\nSau left join, `c.email NULL` có thể nghĩa khách tồn tại nhưng chưa có email, hoặc toàn bộ dòng khách không tồn tại. Kiểm cột `NOT NULL`/khóa chính của phía phải, chẳng hạn `c.customer_id IS NULL`, để nhận biết unmatched. Kiểm một cột nullable sẽ trộn hai trạng thái.\n\n```sql\nCASE WHEN c.customer_id IS NULL THEN 'anonymous'\n     WHEN c.email IS NULL THEN 'known_without_email'\n     ELSE 'known_with_email' END\n```\n\nBa trạng thái này có hành vi chăm sóc khách khác nhau; ép hết thành chuỗi trống làm mất thông tin.\n\n### Đếm sau LEFT JOIN\n\n```sql\nSELECT c.customer_id,\n       COUNT(*) AS result_rows,\n       COUNT(o.order_id) AS actual_orders\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.customer_id\nGROUP BY c.customer_id;\n```\n\nKhách chưa mua vẫn có `COUNT(*) = 1` vì outer join sinh một dòng bổ sung. `COUNT(o.order_id) = 0` vì khóa bên phải là NULL. Đây là ca kiểm kinh điển để phát hiện báo cáo số đơn bị cộng một.\n\n### Cardinality và phép nhân nhiều nhánh\n\nNếu một đơn có 2 items và 3 payment attempts, nối cả hai bảng con trực tiếp tạo 2 × 3 = 6 dòng cho đơn ấy. Mỗi quan hệ riêng đều đúng; tổ hợp làm hai nhánh nhân nhau. Giải pháp là tổng hợp items về một dòng mỗi order và payment attempts về một dòng mỗi order trước khi nối, hoặc chạy truy vấn riêng khi hai danh sách không cần nằm cùng một bảng phẳng.\n\n`DISTINCT` ở cuối chỉ che triệu chứng và có thể xóa dòng hợp lệ giống nhau. Luôn ghi grain mong muốn cạnh thiết kế query: “một dòng mỗi order” là invariant có thể kiểm bằng `GROUP BY order_id HAVING COUNT(*) > 1`.\n\n### Ba thuật toán JOIN vật lý\n\n`Nested Loop` lấy từng dòng nguồn ngoài rồi tìm phía trong; rất tốt khi ngoài nhỏ và phía trong có index. `Hash Join` băm nguồn nhỏ hơn theo khóa rồi dò nguồn kia; phù hợp equi-join lớn nhưng dùng bộ nhớ và không hỗ trợ mọi toán tử. `Merge Join` đi đồng thời trên hai nguồn đã sắp; hữu ích khi input đã có thứ tự hoặc phạm vi lớn.\n\n| Thuật toán | Tình huống thuận lợi | Dấu hiệu bất lợi |\n|---|---|---|\n| Nested Loop | ít dòng ngoài, index lookup rẻ | lặp scan lớn hàng nghìn lần |\n| Hash Join | equi-join, đủ `work_mem` | nhiều batch ghi đĩa |\n| Merge Join | hai phía đã sắp/index | sort đầu vào đắt |\n\nSQL không chỉ định thuật toán. Planner ước lượng cardinality rồi chọn. Statistics sai, tương quan cột mạnh hoặc tham số có phân bố lệch có thể dẫn đến plan kém.\n\n### Index cho khóa nối\n\nPrimary key đã tạo index ở phía được tham chiếu. PostgreSQL không tự tạo index cho cột foreign key, nên `orders(customer_id)` đáng cân nhắc nếu thường đi từ khách sang đơn:\n\n```sql\nCREATE INDEX orders_customer_id_idx ON orders(customer_id);\nCREATE INDEX order_items_order_id_idx ON order_items(order_id);\n```\n\nIndex cũng giúp xóa/cập nhật parent kiểm tra con nhanh hơn. Nhưng bảng con nhỏ hoặc luôn quét phần lớn dữ liệu có thể không hưởng lợi. Xác minh bằng plan; đừng đồng nhất “có foreign key” với “có index ở cả hai phía”.\n\n### Khi nào không nên dùng một JOIN khổng lồ\n\nMàn hình cần object lồng nhiều collection có thể bị Cartesian explosion khi ép thành một bảng phẳng. Hai hoặc ba query có tham số theo batch đôi lúc truyền ít dữ liệu hơn và dễ ánh xạ hơn. Ngược lại, query từng order tạo N+1 round trips là tệ. Lựa chọn hợp lý thường là một query cho cha và một query `WHERE order_id = ANY($1)` cho con, không phải một query mỗi cha cũng không phải JOIN mọi nhánh bằng mọi giá.",

vi_du: "### Ma trận kiểm chứng bốn loại JOIN\n\nHai bảng tạm cô lập semantics khỏi foreign key:\n\n```sql\nCREATE TEMP TABLE left_keys(id integer PRIMARY KEY, label text);\nCREATE TEMP TABLE right_keys(id integer PRIMARY KEY, note text);\nINSERT INTO left_keys VALUES (1,'chỉ trái'),(2,'cả hai'),(4,'cả hai');\nINSERT INTO right_keys VALUES (2,'cả hai'),(3,'chỉ phải'),(4,'cả hai');\n\nSELECT 'inner' AS kind, l.id AS l_id, r.id AS r_id\nFROM left_keys l INNER JOIN right_keys r ON r.id=l.id\nUNION ALL\nSELECT 'left', l.id, r.id\nFROM left_keys l LEFT JOIN right_keys r ON r.id=l.id\nUNION ALL\nSELECT 'right', l.id, r.id\nFROM left_keys l RIGHT JOIN right_keys r ON r.id=l.id\nUNION ALL\nSELECT 'full', l.id, r.id\nFROM left_keys l FULL JOIN right_keys r ON r.id=l.id\nORDER BY kind, l_id NULLS LAST, r_id NULLS LAST;\n```\n\nInner có khóa 2,4; left thêm `(1,NULL)`; right thêm `(NULL,3)`; full có cả hai dòng mồ côi. `UNION ALL` chỉ dùng để đặt bốn kết quả vào một output kiểm tra, không thay thế JOIN.\n\n### Truy vấn nghiệp vụ giữ đơn vô danh\n\n```sql\nSELECT o.order_id,\n       COALESCE(c.full_name, '[mua tại quầy]') AS buyer,\n       e.event_name,\n       i.quantity,\n       i.unit_price\nFROM orders o\nLEFT JOIN customers c ON c.customer_id = o.customer_id\nJOIN order_items i ON i.order_id = o.order_id\nJOIN events e ON e.event_id = i.event_id\nWHERE o.status = 'paid'\nORDER BY o.order_id, i.order_item_id;\n```\n\nCustomers là tùy chọn nên LEFT; paid order phải có item theo kỳ vọng nghiệp vụ nên dùng INNER, dù schema hiện chưa ép “ít nhất một item”. Nếu cần hiển thị cả đơn lỗi không item để vận hành sửa, cả `order_items` cũng phải LEFT. Loại JOIN phải theo mục tiêu màn hình, không chỉ theo foreign key.\n\n### Chứng minh vị trí filter\n\n```sql\n-- A: mọi khách, paid_orders có thể bằng 0\nSELECT c.customer_id, COUNT(o.order_id) AS paid_orders\nFROM customers c\nLEFT JOIN orders o\n  ON o.customer_id=c.customer_id AND o.status='paid'\nGROUP BY c.customer_id ORDER BY c.customer_id;\n\n-- B: chỉ khách có paid order\nSELECT c.customer_id, COUNT(o.order_id) AS paid_orders\nFROM customers c\nLEFT JOIN orders o ON o.customer_id=c.customer_id\nWHERE o.status='paid'\nGROUP BY c.customer_id ORDER BY c.customer_id;\n```\n\nSo sánh số customer_id trong hai kết quả. Query A là báo cáo retention có mẫu số mọi khách; query B là danh sách người mua. Không có query nào tuyệt đối đúng nếu chưa nói câu hỏi.\n\n### Kiểm tra số dòng trước khi tin số tiền\n\n```sql\nSELECT o.order_id, COUNT(*) AS joined_rows\nFROM orders o\nJOIN order_items i ON i.order_id=o.order_id\nGROUP BY o.order_id\nHAVING COUNT(*) > 1;\n```\n\nKết quả không chứng minh lỗi: một đơn nhiều item là hợp lệ. Nó cảnh báo rằng mọi cột cấp order bị lặp trong quan hệ kết quả. Trước `SUM(o.total_amount)`, người viết phải đổi grain hoặc tổng hợp ở cấp order.\n\n### Query plan có thể tự khám phá\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT o.order_id, i.quantity\nFROM orders o\nJOIN order_items i ON i.order_id=o.order_id\nWHERE o.order_id = 1;\n```\n\nTrên dữ liệu nhỏ có thể thấy sequential scan. Khi bảng items lớn và index tồn tại, nested loop với index scan là ứng viên tự nhiên vì phía orders chỉ một dòng. Bỏ điều kiện order_id và đọc phần lớn hai bảng có thể khiến hash join rẻ hơn. Đây là thay đổi plan hợp lý theo cardinality, không phải tính thất thường.",

so_sanh: "### Chọn loại JOIN theo quần thể phải giữ\n\n| Câu hỏi | Loại phù hợp | Phía bảo toàn |\n|---|---|---|\n| Chỉ đơn có khách nhận diện | INNER | không phía nào |\n| Mọi đơn, khách nếu có | orders LEFT customers | orders |\n| Mọi khách, đơn nếu có | customers LEFT orders | customers |\n| Đối soát orders và gateway | FULL | cả hai |\n| RIGHT JOIN tương đương | đảo thành LEFT | tùy cách đọc |\n\n### JOIN hay EXISTS\n\n| Mục tiêu | JOIN | `EXISTS` |\n|---|---|---|\n| Cần cột phía con | phù hợp | không trả cột con |\n| Chỉ hỏi có tồn tại | có thể nhân dòng | diễn đạt semi-join, mỗi cha tối đa một lần |\n| Chống trùng cha | thường cần DISTINCT sai chỗ | tự nhiên không nhân cha |\n\nNếu chỉ tìm khách từng có đơn paid, `WHERE EXISTS (...)` thường nói đúng ý hơn JOIN rồi DISTINCT. Bài subquery lượt sau sẽ phát triển lựa chọn này.",

loi_thuong_gap: [
  "Đặt filter phía tùy chọn trong WHERE sau LEFT JOIN; dòng unmatched biến mất. Chuyển filter vào ON nếu cần bảo toàn trái.",
  "Dùng `COUNT(*)` để đếm con sau LEFT JOIN; cha không con nhận 1. Đếm khóa NOT NULL của bảng con.",
  "Quên một cột của khóa ghép; số dòng tăng bất thường. Kiểm cardinality theo khóa mong muốn trước khi tổng hợp.",
  "Dùng DISTINCT để che JOIN nhân dòng; sort/hash tốn kém và có thể xóa bản ghi hợp lệ. Sửa điều kiện hoặc grain.",
  "Kiểm cột nullable để nhận biết unmatched; NULL thật bị nhầm với dòng bổ sung. Kiểm primary key phía phải.",
  "Cho rằng foreign key tự có index ở bảng con; plan lặp sequential scan khi tìm con. Tạo index nếu workload cần.",
  "Ép mọi quan hệ vào một query nhiều nhánh; items × payments bùng số dòng. Pre-aggregate từng nhánh hoặc tải theo batch.",
  "Dùng LEFT JOIN theo thói quen dù nghiệp vụ đòi tồn tại; dữ liệu lỗi lọt qua dưới dạng NULL. Chọn loại dựa trên invariant và mục tiêu."
],

tu_kiem_tra: [
  "Vì sao LEFT JOIN có thể tạo nhiều hơn số dòng bên trái, dù tên gọi gợi ý “giữ trái”?",
  "Viết hai query: mọi khách kèm số đơn paid, và chỉ khách có ít nhất một đơn paid. Khác biệt nằm ở đâu?",
  "Tại sao kiểm `c.email IS NULL` không đủ để kết luận đơn không có khách khớp?",
  "Một đơn có 2 items và 3 attempts. JOIN cả hai nhánh tạo bao nhiêu dòng, và sửa thế nào nếu cần một dòng mỗi đơn?",
  "Khi nào planner thiên về Nested Loop thay vì Hash Join? Index nào liên quan trong schema này?",
  "FULL JOIN phù hợp với đối soát gateway nhưng thường dư thừa giữa order_items và orders có foreign key vì sao?",
  "Nếu chỉ cần biết khách có đơn paid hay không, vì sao EXISTS thường tốt về semantics hơn JOIN + DISTINCT?"
],

tai_nguyen: [
  "PostgreSQL Documentation — Joined Tables, Table Expressions và EXPLAIN.",
  "“SQL Queries for Mere Mortals” của John L. Viescas và Michael J. Hernandez.",
  "“SQL Antipatterns” của Bill Karwin — lỗi JOIN và mô hình quan hệ.",
  "“The Art of PostgreSQL” của Dimitri Fontaine — JOIN và đọc plan trong PostgreSQL."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: hash join dùng bảng băm, merge join dựa trên hai dãy đã sắp xếp, nested loop giống vòng lặp lồng có tìm kiếm index.",
  "Nối sang mảng Phân tích độ phức tạp: cardinality quyết định số cặp trung gian; nhiều nhánh một-nhiều có thể nhân kích thước thay vì chỉ cộng.",
  "Nối sang mảng Thiết kế API: tránh N+1 bằng batch query, nhưng cũng tránh Cartesian explosion khi tải nhiều collection lồng.",
  "Nối sang mảng Kiểm thử: fixture phải có dòng matched, chỉ-trái, chỉ-phải, NULL thật và khóa lặp để kiểm đầy đủ semantics JOIN."
]

};
