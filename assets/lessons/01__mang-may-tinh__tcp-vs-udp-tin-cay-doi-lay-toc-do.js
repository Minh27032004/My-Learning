window.LESSONS["01:mang-may-tinh:tcp-vs-udp-tin-cay-doi-lay-toc-do"] = {

tom_tat: [
  "**TCP nhận thêm bốn trách nhiệm mà UDP từ chối**: bắt tay tạo kết nối, truyền lại gói mất, xếp lại đúng thứ tự, và điều tiết tốc độ. Mỗi trách nhiệm đổi bằng độ trễ.",
  "**UDP không phải \"TCP kém hơn\"** — nó là lựa chọn đúng khi *dữ liệu cũ đến muộn còn tệ hơn không đến*: gọi thoại, game, video trực tiếp.",
  "**Bắt tay ba bước tốn một vòng đi-về trước khi gửi được byte đầu tiên.** Với HTTPS còn cộng thêm vòng của TLS — đây là lý do latency quan trọng hơn bandwidth với web.",
  "**Head-of-line blocking**: TCP giữ đúng thứ tự nên một gói mất làm nghẽn mọi gói sau nó. Đây chính là vấn đề mà HTTP/3 chuyển sang QUIC trên UDP để giải quyết.",
  "**TIME_WAIT không phải lỗi.** Nó là trạng thái cố ý giữ 30–120 giây sau khi đóng, để gói đi lạc không làm hỏng kết nối mới trùng bộ bốn."
],

can_biet_truoc: [
  "Bài **IP, port, DNS** ngay trước — cần hiểu bộ bốn (IP nguồn, cổng nguồn, IP đích, cổng đích) định danh một kết nối.",
  "Bài **Mô hình TCP/IP 4 tầng** — TCP và UDP đều nằm ở tầng Giao vận, phía trên IP.",
  "Bài **Tiến trình và luồng** ở module 2 — hiểu buffer và việc tiến trình bị chặn khi chờ dữ liệu.",
  "Bài **Tín hiệu, tiến trình nền, exit code** ở module 2 — không bắt buộc, nhưng giúp hiểu vì sao đóng socket đột ngột khác đóng tử tế."
],

dinh_nghia: "Nói đời thường: **TCP là gửi thư bảo đảm, UDP là thả tờ rơi qua cửa sổ.** Thư bảo đảm có xác nhận đã nhận, mất thì gửi lại, tới theo đúng thứ tự — nhưng chậm và tốn thủ tục. Tờ rơi bay ra ngay lập tức, có thể không ai nhận, có thể tới lộn thứ tự, và bạn không bao giờ biết.\n\nNói chính xác:\n\n- **TCP** (*Transmission Control Protocol*) — giao thức tầng Giao vận **có kết nối** và **tin cậy**: đảm bảo dữ liệu tới đủ, đúng thứ tự, không trùng lặp; tự điều tiết tốc độ theo tình trạng mạng.\n- **UDP** (*User Datagram Protocol*) — giao thức tầng Giao vận **không kết nối** và **không đảm bảo gì**: gửi là gửi, không xác nhận, không truyền lại, không sắp thứ tự.\n- **Segment** — đơn vị dữ liệu của TCP. **Datagram** — đơn vị dữ liệu của UDP.\n- **Bắt tay ba bước** (*three-way handshake*) — chuỗi SYN → SYN-ACK → ACK để hai bên thống nhất bắt đầu một kết nối TCP.\n- **Cửa sổ** (*window*) — số byte bên gửi được phép gửi trước khi phải chờ xác nhận. Cơ chế trung tâm của cả điều khiển luồng và điều khiển tắc nghẽn.\n\nCách hiểu đúng nhất về quan hệ giữa hai giao thức: **UDP là những gì còn lại của tầng Giao vận sau khi bỏ hết mọi đảm bảo.** Header UDP chỉ 8 byte, gồm cổng nguồn, cổng đích, độ dài, checksum — vừa đủ để giao đúng tiến trình, không hơn.",

vi_sao_can: "Đây không phải câu hỏi phỏng vấn suông — nó quyết định kiến trúc và giải thích rất nhiều hiện tượng bạn sẽ gặp:\n\n- **Chọn đúng giao thức cho từng việc.** Xây hệ thống chat, gọi video, thu thập metrics, truyền file — mỗi thứ có câu trả lời khác nhau, và chọn sai thì hoặc chậm vô lý, hoặc mất dữ liệu quan trọng.\n- **Hiểu vì sao API chậm.** Mỗi kết nối mới tốn một vòng bắt tay. Không dùng keep-alive, gọi 100 request là 100 lần bắt tay. Đây là nguyên nhân chậm mà đọc code ứng dụng không bao giờ thấy được.\n- **Đọc hiểu lỗi và log.** `Connection reset by peer`, `TIME_WAIT` đầy bảng, timeout khác refused — mỗi thứ tương ứng một trạng thái cụ thể trong máy trạng thái TCP.\n- **Hiểu HTTP/3 và QUIC.** Ngành công nghiệp đang chuyển HTTP sang chạy trên UDP. Không hiểu TCP thì không hiểu vì sao phải làm vậy.\n- **Gỡ lỗi hiệu năng thật.** Băng thông lớn mà truyền vẫn chậm là hiện tượng thường gặp, và nguyên nhân nằm ở cửa sổ TCP kết hợp với độ trễ, không phải ở đường truyền.\n\nTình huống thật đáng nhớ: bạn thuê đường truyền 1 Gbps, tải file từ server ở Mỹ về, chỉ đạt 5 MB/s. Nhà mạng nói đường truyền không có vấn đề, và họ nói đúng. Nguyên nhân là **kích thước cửa sổ TCP chia cho thời gian đi-về** — một công thức mà phần cơ chế sẽ tính cụ thể. Đây là loại vấn đề mà không hiểu TCP thì mãi không tìm ra.",

co_che: "### Bốn trách nhiệm TCP nhận, UDP từ chối\n\nTất cả sự phức tạp của TCP sinh ra từ bốn cam kết:\n\n```\n① Kết nối       biết chắc bên kia đang sẵn sàng, trước khi gửi dữ liệu\n② Tin cậy       gói mất thì phát hiện và gửi lại\n③ Thứ tự        giao cho ứng dụng đúng trình tự đã gửi\n④ Điều tiết     tự giảm tốc khi mạng nghẽn hoặc bên nhận không kịp\n```\n\nUDP không làm bất kỳ điều nào trong bốn điều trên. Và đó **không phải khuyết điểm** — với một cuộc gọi thoại, gói âm thanh của 200 ms trước đến muộn thì đã vô dụng, phát ra chỉ làm méo tiếng. Thà bỏ nó và phát tiếp gói mới.\n\nNguyên tắc chọn giữa hai giao thức có thể gói lại thành một câu:\n\n```\nDữ liệu cũ đến muộn có còn giá trị không?\n  CÓ  → TCP  (file, trang web, database, giao dịch)\n  KHÔNG → UDP (thoại, video trực tiếp, game, metrics)\n```\n\n### Bắt tay ba bước\n\n```\nClient                                    Server\n  │                                          │\n  │──── SYN, seq=x ────────────────────────► │  \"tôi muốn nối, tôi đếm từ x\"\n  │                                          │\n  │ ◄──── SYN-ACK, seq=y, ack=x+1 ───────────│  \"đồng ý, tôi đếm từ y,\n  │                                          │   và tôi đã nhận tới x\"\n  │──── ACK, ack=y+1 ──────────────────────► │  \"tôi cũng nhận được của bạn\"\n  │                                          │\n  │═════ kết nối thành lập, gửi dữ liệu ════ │\n```\n\nVì sao phải **ba** bước chứ không phải hai? Vì mỗi bên cần xác nhận rằng *bên kia đã nhận được số thứ tự khởi đầu của mình*. Hai bước chỉ chứng minh được một chiều. Ba bước là số tối thiểu để cả hai chiều đều được xác nhận — không phải quy ước ngẫu nhiên.\n\nSố thứ tự khởi đầu (`x`, `y`) được chọn **ngẫu nhiên**, không phải bắt đầu từ 0. Lý do là bảo mật: nếu đoán được số thứ tự, kẻ tấn công có thể chèn dữ liệu giả vào kết nối đang chạy.\n\nCái giá phải trả nằm ở độ trễ:\n\n```\nĐộ trễ một chiều 50 ms ⇒ RTT (đi-về) 100 ms\n\nTCP thuần:  1 RTT bắt tay        = 100 ms trước byte đầu tiên\nTCP + TLS:  1 RTT + 1–2 RTT TLS  = 200–300 ms trước byte đầu tiên\n```\n\nĐây là lý do câu \"tối ưu web thì giảm số request\" quan trọng hơn \"nén nhỏ file\" trong nhiều trường hợp: **thời gian bị chi phối bởi số vòng đi-về, không phải số byte**. Cũng vì vậy mà HTTP keep-alive (dùng lại một kết nối cho nhiều request) là tối ưu có giá trị nhất mà gần như không mất gì.\n\n### Đảm bảo tin cậy: số thứ tự và ACK\n\n```\nBên gửi                                  Bên nhận\nseq=1000, 500 byte ────────────────────► nhận\n                    ◄──────────────────── ack=1500  \"đã nhận tới 1500\"\nseq=1500, 500 byte ──────── ✗ MẤT ────►\nseq=2000, 500 byte ────────────────────► nhận, nhưng THIẾU 1500–2000\n                    ◄──────────────────── ack=1500  \"vẫn đang chờ 1500\"\n                    ◄──────────────────── ack=1500  (ACK trùng)\nseq=1500 GỬI LẠI ──────────────────────► nhận đủ\n                    ◄──────────────────── ack=2500  \"giờ đã nhận tới 2500\"\n```\n\nHai cơ chế phát hiện mất gói, và biết cả hai giúp đọc `tcpdump` dễ hơn nhiều:\n\n| Cơ chế | Cách hoạt động | Tốc độ phản ứng |\n|---|---|---|\n| **ACK trùng** | Nhận 3 ACK giống nhau ⇒ suy ra gói giữa bị mất | Nhanh, không cần chờ |\n| **Hết hạn chờ** | Không có ACK nào trong khoảng RTO | Chậm, tối thiểu vài trăm ms |\n\nKhi phải chờ hết hạn, TCP còn **nhân đôi thời gian chờ** cho lần sau (*exponential backoff*): 200 ms → 400 ms → 800 ms... Đây là lý do một kết nối mạng tệ không chỉ chậm mà **chậm theo cấp số nhân** — hiện tượng \"lúc đầu còn tải được, sau đó gần như đứng hẳn\".\n\n### Head-of-line blocking — nhược điểm cốt lõi của TCP\n\nTCP cam kết giao **đúng thứ tự**. Hệ quả là:\n\n```\nGói:  1 ✓   2 ✓   3 ✗MẤT   4 ✓   5 ✓   6 ✓\n\nỨng dụng nhận được:  1, 2, rồi ĐỢI\n                     (4, 5, 6 đã nằm trong buffer của hệ điều hành\n                      nhưng KHÔNG được giao lên, vì thiếu gói 3)\n```\n\nDữ liệu đã tới đích, đã nằm trong bộ nhớ, mà ứng dụng không được đọc — chỉ vì một gói phía trước bị mất. Đây gọi là **head-of-line blocking**.\n\nVấn đề trở nên trầm trọng với HTTP/2, vốn ghép nhiều luồng dữ liệu lên **một** kết nối TCP: một gói mất làm nghẽn *tất cả* các luồng, kể cả những luồng chẳng liên quan gì tới gói đó.\n\nĐó chính là lý do **HTTP/3 chuyển sang QUIC chạy trên UDP**. QUIC tự làm lại tin cậy và thứ tự, nhưng **theo từng luồng riêng biệt** — gói mất của luồng A không chặn luồng B. Đây là ví dụ đẹp về việc chọn UDP không phải để \"bỏ tin cậy\", mà để **tự cài đặt tin cậy theo cách phù hợp hơn**.\n\n### Điều khiển luồng và điều khiển tắc nghẽn\n\nHai thứ tên giống nhau nhưng giải hai bài toán khác nhau — đây là chỗ rất hay bị nhầm:\n\n| | Điều khiển luồng | Điều khiển tắc nghẽn |\n|---|---|---|\n| Bảo vệ ai | **bên nhận** khỏi bị tràn buffer | **mạng ở giữa** khỏi bị nghẽn |\n| Ai quyết định | bên nhận, báo qua trường window | bên gửi tự suy đoán |\n| Biết bằng cách nào | được thông báo tường minh | **đoán** qua việc gói bị mất |\n\nBên nhận báo `window = 65535` nghĩa là \"buffer tôi còn chỗ cho 65535 byte, đừng gửi nhiều hơn\". Nếu ứng dụng đọc chậm, buffer đầy, window tụt về 0 và bên gửi **phải dừng**. Hiện tượng thực tế: tải file chậm mà mạng rảnh, CPU rảnh — vì ứng dụng nhận đang ghi đĩa chậm.\n\nĐiều khiển tắc nghẽn thì tinh vi hơn, vì mạng không nói gì cả:\n\n```\nKhởi động chậm:  1 → 2 → 4 → 8 → 16 gói ...   (tăng gấp đôi mỗi RTT)\nCó gói mất  ⇒  \"mạng nghẽn rồi\"  ⇒  giảm mạnh cửa sổ\nSau đó      ⇒  tăng dần từng chút một, thăm dò lại\n```\n\nGiả định nền tảng: **mất gói nghĩa là mạng nghẽn**. Giả định này đúng với mạng có dây, nhưng **sai với Wi-Fi và mạng di động**, nơi gói mất phần lớn do nhiễu sóng chứ không do nghẽn. TCP phản ứng bằng cách giảm tốc — hoàn toàn không cần thiết. Đây là lý do có nhiều thuật toán điều khiển tắc nghẽn khác nhau, và các thuật toán hiện đại cố gắng đo độ trễ thay vì chỉ nhìn gói mất.\n\n### Công thức giải thích \"đường truyền lớn mà vẫn chậm\"\n\nĐây là phần thực dụng nhất của bài. Thông lượng tối đa của **một** kết nối TCP bị chặn bởi:\n\n```\nthông lượng ≈ kích thước cửa sổ / RTT\n```\n\nÁp vào tình huống ở phần *vì sao cần*:\n\n```\nCửa sổ 64 KB, RTT 100 ms (Việt Nam ↔ Mỹ)\n\n64 KB / 0,1 s = 640 KB/s ≈ 5 MB/s\n```\n\nĐúng bằng con số quan sát được — và **đường truyền 1 Gbps hoàn toàn không liên quan**. Nút cổ chai là cửa sổ chia cho độ trễ.\n\nBa cách khắc phục, theo thứ tự nên thử:\n\n```\n① Tăng cửa sổ         bật window scaling, tăng buffer của hệ điều hành\n② Nhiều kết nối       tải song song 8 luồng → nhân 8 thông lượng\n③ Giảm RTT            đặt server gần người dùng hơn — CDN làm đúng việc này\n```\n\nCách ② giải thích vì sao các trình tải xuống chia file thành nhiều phần tải song song: mỗi kết nối có cửa sổ riêng, nên n kết nối cho n lần thông lượng. Đây không phải mánh, mà là hệ quả trực tiếp của công thức trên.\n\n### Máy trạng thái và `TIME_WAIT`\n\nĐóng kết nối cần bốn bước, vì TCP là song công — mỗi chiều đóng riêng:\n\n```\nA ──── FIN ────► B      \"tôi hết dữ liệu để gửi\"\nA ◄─── ACK ───── B\nA ◄─── FIN ───── B      \"tôi cũng hết\"\nA ──── ACK ────► B\nA: bước vào TIME_WAIT (30–120 giây) rồi mới đóng hẳn\n```\n\nBên chủ động đóng phải nằm trong `TIME_WAIT` một khoảng. Rất nhiều người coi đây là lỗi cần \"sửa\", nhưng nó có hai lý do chính đáng:\n\n1. **Gói đi lạc từ kết nối cũ có thể tới muộn.** Nếu mở ngay một kết nối mới trùng bộ bốn, gói lạc đó sẽ bị hiểu là dữ liệu của kết nối mới — hỏng dữ liệu, và hỏng theo cách rất khó chẩn đoán.\n2. **ACK cuối có thể mất.** Bên kia sẽ gửi lại FIN, và cần có ai đó còn sống để trả lời.\n\nHệ quả thực tế: server đóng nhiều kết nối ngắn sẽ có hàng nghìn socket `TIME_WAIT`.\n\n```bash\nss -tan | awk '{print $1}' | sort | uniq -c | sort -rn\n#  8432 TIME-WAIT\n#   120 ESTAB\n```\n\nCách xử lý **đúng** là giảm số kết nối phải đóng (dùng keep-alive, connection pool), không phải đi tắt `TIME_WAIT`. Các mẹo \"giảm thời gian TIME_WAIT\" trên mạng có làm được, nhưng chúng đánh đổi bằng đúng rủi ro hỏng dữ liệu mà cơ chế này sinh ra để ngăn.\n\n### Header — nhìn thấy sự chênh lệch\n\n```\nHeader UDP: 8 byte\n┌──────────┬──────────┬─────────┬──────────┐\n│cổng nguồn│ cổng đích│ độ dài  │ checksum │\n└──────────┴──────────┴─────────┴──────────┘\n\nHeader TCP: 20 byte tối thiểu\n┌──────────┬──────────┐\n│cổng nguồn│ cổng đích│\n├──────────┴──────────┤\n│  số thứ tự          │  ← để sắp thứ tự và phát hiện mất\n├─────────────────────┤\n│  số xác nhận        │  ← để báo đã nhận tới đâu\n├────┬────┬───────────┤\n│ len│flag│  window   │  ← flag: SYN/ACK/FIN/RST · window: điều khiển luồng\n├────┴────┼───────────┤\n│checksum │urgent ptr │\n└─────────┴───────────┘\n```\n\nToàn bộ 12 byte chênh lệch là **cái giá của bốn cam kết**. Với gói thoại 20 byte dữ liệu, header TCP còn to hơn dữ liệu — thêm một lý do nữa để thoại dùng UDP.\n\nBốn cờ nên biết vì chúng xuất hiện liên tục trong `tcpdump`:\n\n```\nSYN  xin mở kết nối       [S]\nACK  xác nhận đã nhận     [.]\nFIN  đóng tử tế           [F]\nRST  đóng đột ngột        [R]   ← \"connection reset by peer\" là cái này\n```\n\n`RST` đáng chú ý: nó xuất hiện khi một bên nhận dữ liệu cho kết nối mà nó không biết gì — ví dụ tiến trình đã chết, hoặc kết nối đã bị firewall trung gian xoá khỏi bảng theo dõi. Thấy `RST` sau một khoảng im lặng dài thường là dấu hiệu của **timeout ở NAT hoặc firewall giữa đường**, không phải lỗi ứng dụng.",

vi_du: "### Xem bắt tay ba bước bằng mắt\n\n```bash\nsudo tcpdump -i any -n 'host example.com and tcp' -c 10\n\n# 10:00:00.100 IP 192.168.1.10.51234 > 93.184.x.x.443: Flags [S], seq 1000\n# 10:00:00.150 IP 93.184.x.x.443 > 192.168.1.10.51234: Flags [S.], seq 5000, ack 1001\n# 10:00:00.151 IP 192.168.1.10.51234 > 93.184.x.x.443: Flags [.], ack 5001\n#                                                             ↑ [S]=SYN [S.]=SYN-ACK [.]=ACK\n```\n\nĐọc được ba dòng này là hiểu bắt tay. Chú ý mốc thời gian: từ 00.100 tới 00.151 là **51 ms trôi qua mà chưa gửi được byte dữ liệu nào** — đúng bằng một RTT. Đây là con số cụ thể cho câu \"mỗi kết nối mới tốn một vòng đi-về\".\n\n### Đo RTT và tính giới hạn thông lượng\n\n```bash\nping -c 5 example.com\n# rtt min/avg/max = 95.1/98.3/102.7 ms\n\n# Giới hạn thông lượng của MỘT kết nối TCP:\n#   cửa sổ 64 KB / 0,098 s ≈ 653 KB/s ≈ 5,2 MB/s\n\n# Xem cửa sổ thật của hệ điều hành\ncat /proc/sys/net/ipv4/tcp_rmem\n# 4096  131072  6291456\n#  min   default   max      ← max 6 MB nếu window scaling hoạt động\n```\n\nKiểm chứng bằng cách tải song song, dùng chính công thức ở phần cơ chế:\n\n```bash\n# một kết nối\ncurl -o /dev/null -w \"%{speed_download}\\n\" https://example.com/file.bin\n\n# tám kết nối song song — nếu nhanh gần 8 lần thì nút cổ chai là cửa sổ,\n# không phải băng thông đường truyền\nfor i in $(seq 8); do\n  curl -s -o /dev/null -r $((i*10000000))-$(((i+1)*10000000-1)) \\\n       https://example.com/file.bin &\ndone; wait\n```\n\nPhép thử này rất giá trị khi tranh luận với nhà cung cấp hạ tầng: nếu tám luồng nhanh gấp tám, đường truyền không có lỗi gì.\n\n### Đo cái giá của việc không dùng keep-alive\n\n```bash\n# ❌ 10 kết nối riêng — 10 lần bắt tay TCP + 10 lần bắt tay TLS\nfor i in $(seq 10); do\n  curl -s -o /dev/null https://example.com/api\ndone\n\n# ✓ 1 kết nối dùng lại cho 10 request\ncurl -s -o /dev/null https://example.com/api/1 \\\n     -o /dev/null https://example.com/api/2 \\\n     ... # curl tự dùng lại kết nối khi cùng host\n\n# Xem chi tiết từng chặng thời gian\ncurl -w \"@-\" -o /dev/null -s https://example.com <<'EOF'\n     dns:  %{time_namelookup}s\n     tcp:  %{time_connect}s\n     tls:  %{time_appconnect}s\n   byte1:  %{time_starttransfer}s\n   tổng:   %{time_total}s\nEOF\n```\n\nOutput của lệnh cuối là công cụ chẩn đoán chậm tốt nhất mà không cần cài gì:\n\n```\nChặng nào chiếm nhiều thời gian ⇒ sửa ở đó\n  dns cao        → đổi resolver, hoặc bật cache DNS\n  tcp cao        → server ở xa, hoặc mạng tệ\n  tls cao        → xem lại chuỗi chứng chỉ, bật session resumption\n  byte1 − tls    → server xử lý chậm ⇒ lỗi ở ỨNG DỤNG, không phải mạng\n```\n\nPhân biệt được \"chậm vì mạng\" và \"chậm vì server\" chỉ bằng một lệnh — thay vì đoán.\n\n### So sánh trực tiếp TCP và UDP\n\n```bash\n# Cửa sổ 1: dựng server\nnc -l 9000          # TCP\nnc -u -l 9000       # UDP\n\n# Cửa sổ 2: gửi\necho \"xin chao\" | nc localhost 9000\necho \"xin chao\" | nc -u localhost 9000\n```\n\nPhép thử cho thấy khác biệt cốt lõi:\n\n```bash\n# TCP: KHÔNG có server ⇒ báo lỗi ngay\nnc localhost 9999\n# Connection refused          ← TCP biết ngay vì cần bắt tay\n\n# UDP: KHÔNG có server ⇒ IM LẶNG, gửi vẫn \"thành công\"\necho test | nc -u localhost 9999\n# (không lỗi gì)              ← UDP không hề biết bên kia có tồn tại\n```\n\nĐây là điều quan trọng nhất cần nhớ khi lập trình UDP: **`send()` trả về thành công không có nghĩa là dữ liệu đã tới**. Nó chỉ có nghĩa là hệ điều hành đã nhận gói để gửi đi. Mọi cơ chế xác nhận, nếu cần, phải do ứng dụng tự làm.\n\n### Xem trạng thái kết nối\n\n```bash\nss -tan\n# State       Local Address:Port    Peer Address:Port\n# LISTEN      0.0.0.0:443          0.0.0.0:*\n# ESTAB       10.0.1.5:443         1.2.3.4:51234\n# TIME-WAIT   10.0.1.5:443         5.6.7.8:44012\n# CLOSE-WAIT  10.0.1.5:8000        9.9.9.9:33001\n\n# Đếm theo trạng thái — bức tranh sức khoẻ của server\nss -tan | awk 'NR>1 {print $1}' | sort | uniq -c | sort -rn\n```\n\nBảng chẩn đoán theo trạng thái, dùng được ngay:\n\n| Trạng thái nhiều bất thường | Nghĩa là | Sửa thế nào |\n|---|---|---|\n| `TIME-WAIT` | Nhiều kết nối ngắn, server chủ động đóng | Bật keep-alive, dùng connection pool |\n| `CLOSE-WAIT` | **Bên kia đã đóng, ứng dụng bạn chưa gọi `close()`** | Lỗi rò rỉ socket trong code |\n| `SYN-SENT` đọng | Bắt tay không hoàn tất | Firewall chặn, hoặc server quá tải |\n| `SYN-RECV` nhiều | Có thể đang bị SYN flood | Bật SYN cookies |\n\nDòng `CLOSE-WAIT` là dòng đáng chú ý nhất vì nó **luôn là lỗi trong code của bạn**, không phải lỗi mạng: bên kia đã gửi FIN, hệ điều hành đang chờ ứng dụng gọi `close()`, mà ứng dụng thì không gọi. Socket rò rỉ dần cho tới khi hết file descriptor.\n\n### Khi nào chọn cái nào — bảng quyết định\n\n```\nTruyền file, API, database, chat        → TCP\n   dữ liệu sai/thiếu là không chấp nhận được\n\nGọi thoại, video trực tiếp, game        → UDP\n   dữ liệu cũ đến muộn thì vô dụng\n\nDNS                                     → UDP trước, TCP khi phản hồi lớn\n   một câu hỏi một câu trả lời, mất thì hỏi lại — rẻ hơn bắt tay\n\nMetrics, log số lượng lớn               → UDP\n   mất vài điểm dữ liệu không ảnh hưởng bức tranh tổng thể\n\nHTTP/3                                  → UDP (QUIC)\n   tự làm tin cậy theo từng luồng để tránh head-of-line blocking\n```\n\nDNS là ví dụ đáng suy ngẫm: nó chọn UDP **mặc dù cần độ chính xác**, vì một cặp hỏi–đáp nhỏ mà phải bắt tay ba bước thì chi phí gấp nhiều lần lợi ích. Mất gói thì hỏi lại — đơn giản hơn và nhanh hơn. Nhưng khi phản hồi quá lớn (ví dụ có nhiều bản ghi), DNS **tự chuyển sang TCP**. Đây là ví dụ hay về việc chọn giao thức theo tình huống thay vì theo nguyên tắc cứng.",

so_sanh: "### TCP và UDP\n\n| | TCP | UDP |\n|---|---|---|\n| Kết nối | có, bắt tay 3 bước | **không** |\n| Header | 20 byte trở lên | **8 byte** |\n| Đảm bảo tới đích | ✓ truyền lại | ✗ |\n| Đảm bảo thứ tự | ✓ | ✗ |\n| Điều khiển tắc nghẽn | ✓ tự giảm tốc | ✗ gửi hết tốc lực |\n| Độ trễ trước byte đầu | **1 RTT** | 0 |\n| Head-of-line blocking | **có** | không |\n| Gửi tới nhiều máy | không | ✓ multicast/broadcast |\n| Biết bên kia có tồn tại | ✓ ngay lập tức | **✗ không bao giờ** |\n\nHàng cuối là hàng có hệ quả lập trình lớn nhất: với UDP, `send()` thành công **không** chứng minh điều gì về bên nhận.\n\nHàng \"điều khiển tắc nghẽn\" có một mặt tối ít được nhắc: UDP gửi hết tốc lực nghĩa là một ứng dụng UDP viết cẩu thả **có thể làm nghẽn mạng và đè bẹp các kết nối TCP xung quanh** — vì TCP tự nhường còn UDP thì không. Đây là lý do các giao thức UDP nghiêm túc (QUIC, WebRTC) đều **tự cài đặt điều khiển tắc nghẽn**. Chọn UDP là nhận lấy trách nhiệm, không phải bỏ trách nhiệm.\n\n### Điều khiển luồng và điều khiển tắc nghẽn\n\n| | Điều khiển luồng | Điều khiển tắc nghẽn |\n|---|---|---|\n| Vấn đề giải quyết | bên nhận xử lý không kịp | mạng ở giữa không tải nổi |\n| Nguồn thông tin | trường window trong header | **suy đoán** từ gói mất, độ trễ |\n| Ai điều khiển | bên nhận | bên gửi |\n| Chính xác không | tường minh, chính xác | phỏng đoán, có thể sai |\n\nDòng cuối giải thích một vấn đề thực tế: trên Wi-Fi và 4G, gói mất phần lớn do nhiễu chứ không do nghẽn, nên TCP **giảm tốc oan**. Đây là bài toán mở, và là lý do các thuật toán điều khiển tắc nghẽn mới cố đo độ trễ thay vì chỉ đếm gói mất.\n\n### HTTP/1.1, HTTP/2, HTTP/3\n\n| | HTTP/1.1 | HTTP/2 | HTTP/3 |\n|---|---|---|---|\n| Chạy trên | TCP | TCP | **UDP (QUIC)** |\n| Số kết nối | nhiều (6/host) | **một** | một |\n| Ghép nhiều luồng | ✗ | ✓ | ✓ |\n| Head-of-line ở tầng TCP | có | **có, tệ hơn** | **không** |\n| Bắt tay + TLS | 2–3 RTT | 2–3 RTT | **1 RTT, có thể 0** |\n\nCột HTTP/2 có một nghịch lý đáng học: ghép mọi luồng lên một kết nối TCP giải quyết được head-of-line ở tầng HTTP, nhưng **làm nó tệ hơn ở tầng TCP** — trước đây một gói mất chỉ ảnh hưởng 1 trong 6 kết nối, giờ ảnh hưởng tất cả. Đây là ví dụ điển hình cho việc tối ưu một tầng có thể tạo vấn đề ở tầng khác, và là động lực trực tiếp cho HTTP/3.\n\n### Ba loại \"đóng kết nối\"\n\n| | FIN (đóng tử tế) | RST (đóng đột ngột) | Timeout |\n|---|---|---|---|\n| Ai gây ra | ứng dụng gọi `close()` | hệ điều hành, hoặc firewall | không ai — chỉ im lặng |\n| Dữ liệu đang gửi | được gửi hết trước khi đóng | **bị vứt** | không xác định |\n| Bên kia thấy gì | EOF, đọc trả về 0 | `Connection reset by peer` | treo tới khi hết hạn |\n| Có TIME_WAIT | có | không | không |\n\nBảng này giúp đọc log chính xác hơn: `Connection reset by peer` **không** phải \"mạng lỗi\" chung chung, mà là *có ai đó chủ động gửi RST*. Nguyên nhân thường gặp nhất là tiến trình bên kia chết, hoặc thiết bị NAT/firewall giữa đường đã xoá kết nối khỏi bảng theo dõi vì im lặng quá lâu — trường hợp sau khắc phục bằng TCP keep-alive.",

loi_thuong_gap: [
  "**Coi UDP là \"TCP phiên bản kém\".** UDP là lựa chọn đúng khi dữ liệu cũ đến muộn còn tệ hơn không đến. Chọn theo tính chất dữ liệu, không theo \"cái nào tốt hơn\".",
  "**Tưởng `send()` thành công trên UDP nghĩa là đã tới đích.** Nó chỉ nghĩa là hệ điều hành đã nhận gói để gửi. Muốn biết chắc thì ứng dụng phải tự làm xác nhận.",
  "**Không dùng keep-alive / connection pool.** Mỗi request một kết nối là mỗi request một RTT bắt tay, cộng thêm bắt tay TLS.",
  "**Thấy nhiều `TIME_WAIT` rồi đi tắt nó.** Nó là cơ chế bảo vệ. Cách đúng là giảm số kết nối phải đóng.",
  "**Bỏ qua `CLOSE_WAIT`.** Trạng thái này gần như luôn là lỗi code: bên kia đã đóng mà ứng dụng chưa gọi `close()`. Socket rò rỉ tới khi hết file descriptor.",
  "**Kết luận \"đường truyền chậm\" khi băng thông lớn mà tải chậm.** Tính `cửa sổ / RTT` trước. Thử tải song song để xác nhận.",
  "**Nhầm điều khiển luồng với điều khiển tắc nghẽn.** Một cái bảo vệ bên nhận, một cái bảo vệ mạng. Chẩn đoán khác nhau hoàn toàn.",
  "**Viết ứng dụng UDP mà không tự điều tiết tốc độ.** Nó sẽ đè bẹp các kết nối TCP xung quanh, vì TCP tự nhường còn UDP thì không.",
  "**Hiểu `Connection reset by peer` là \"mạng lỗi\".** Đó là RST — có bên nào đó chủ động gửi. Thường là tiến trình chết, hoặc NAT/firewall đã xoá kết nối vì im lặng lâu.",
  "**Tưởng head-of-line blocking là vấn đề của HTTP.** Nó nằm ở TCP. Đó là lý do HTTP/3 phải rời TCP chứ không sửa được ở tầng HTTP.",
  "**Dùng TCP cho dữ liệu thời gian thực.** Truyền lại gói âm thanh của 300 ms trước chỉ làm méo tiếng và tăng độ trễ."
],

tu_kiem_tra: [
  "Nêu bốn trách nhiệm TCP nhận mà UDP từ chối, và cái giá của mỗi trách nhiệm.",
  "Vì sao bắt tay cần đúng ba bước, không phải hai?",
  "Head-of-line blocking là gì? Vì sao HTTP/2 làm nó tệ hơn, và HTTP/3 giải quyết bằng cách nào?",
  "Đường truyền 1 Gbps, RTT 100 ms, cửa sổ 64 KB — thông lượng tối đa của một kết nối TCP là bao nhiêu? Ba cách khắc phục?",
  "Phân biệt điều khiển luồng và điều khiển tắc nghẽn: mỗi cái bảo vệ ai, và lấy thông tin từ đâu?",
  "`TIME_WAIT` tồn tại để làm gì? Vì sao tắt nó là ý tồi?",
  "Server có hàng nghìn socket `CLOSE_WAIT` — lỗi ở đâu, mạng hay code?",
  "Vì sao DNS chọn UDP mặc dù cần chính xác? Khi nào nó chuyển sang TCP?",
  "Trên UDP, `send()` trả về thành công chứng minh được điều gì về bên nhận?",
  "Vì sao TCP giảm tốc \"oan\" trên Wi-Fi và mạng di động?"
],

tai_nguyen: [
  "Sách **TCP/IP Illustrated, Volume 1** — W. Richard Stevens. Các chương về TCP là tài liệu tham khảo chuẩn mực nhất, mổ tới từng byte header.",
  "Sách **Computer Networking: A Top-Down Approach** — Kurose & Ross, chương về tầng Giao vận. Phần điều khiển tắc nghẽn giải thích rất rõ bằng đồ thị.",
  "Sách **High Performance Browser Networking** — Ilya Grigorik. Đọc miễn phí trực tuyến. Chương về TCP và về latency là phần nên đọc đầu tiên nếu quan tâm hiệu năng web.",
  "Tài liệu Linux: `man tcp`, `man udp`, `man ss`, `man tcpdump`. `man tcp` liệt kê mọi tham số nhân hệ thống liên quan tới cửa sổ và keep-alive.",
  "Phần mềm **Wireshark** — chức năng đồ thị thời gian và *Statistics → TCP Stream Graphs* cho thấy cửa sổ và gói truyền lại rất trực quan."
],

lien_he: [
  "**IP, port, DNS** ngay trước: bộ bốn định danh kết nối là nền tảng để hiểu vì sao `TIME_WAIT` cần tồn tại.",
  "**HTTP: method, header, status code** ngay sau: HTTP chạy trên TCP, và keep-alive là header trực tiếp điều khiển việc dùng lại kết nối.",
  "**HTTPS, TLS handshake**: bắt tay TLS xảy ra **sau** bắt tay TCP, nên cộng thêm 1–2 RTT nữa.",
  "**WebSocket vs polling**: WebSocket giữ một kết nối TCP sống, chính là để tránh trả giá bắt tay lặp lại.",
  "**Mô hình TCP/IP 4 tầng**: MTU và MSS quyết định TCP chia dữ liệu thành bao nhiêu segment.",
  "**Concurrency vs parallelism** ở module 2: server xử lý hàng nghìn kết nối đồng thời là bài toán I/O-bound kinh điển, và đó là lý do async I/O ra đời.",
  "**Backend & Web** ở mảng 05: connection pool cho database là ứng dụng trực tiếp của việc tránh bắt tay lặp lại.",
  "**System Design** ở mảng 06: chọn giao thức cho giao tiếp giữa các service, và vì sao gRPC chọn HTTP/2 — đều bắt nguồn từ bài này."
]

};
