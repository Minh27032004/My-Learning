window.LESSONS["04:hieu-nang:doc-explain-query-plan"] = {

tom_tat: [
  "`EXPLAIN` cho biết PostgreSQL dự định thực thi SQL bằng cây operator; `EXPLAIN ANALYZE` thực sự chạy câu lệnh rồi gắn thời gian và số hàng quan sát được vào từng nút.",
  "Đọc plan từ nút sâu nhất đi lên: nút con tạo ra hàng, nút cha nhận hàng ấy để lọc, nối, sắp xếp hoặc giới hạn; dòng trên cùng là kết quả cuối.",
  "Ba chênh lệch đáng điều tra là `rows` ước lượng so với `actual rows`, số vòng `loops`, và block trong `BUFFERS`; chỉ nhìn `cost` hoặc loại scan là chưa đủ.",
  "`Seq Scan` không mặc nhiên xấu và `Index Scan` không mặc nhiên tốt: planner chọn theo tỷ lệ hàng cần lấy, tính tương quan vật lý, chi phí I/O và khả năng đọc tuần tự.",
  "Ví dụ xuyên suốt là trang quản trị đơn hàng: lọc trạng thái, lấy đơn mới nhất, nối khách hàng và tính tổng từ `order_items`."
],

can_biet_truoc: [
  "SQL cơ bản: `SELECT`, `WHERE`, `JOIN`, `GROUP BY`, `ORDER BY` và `LIMIT`.",
  "Index B-tree và composite index, đặc biệt quy tắc cột đứng trước và khả năng cung cấp thứ tự.",
  "Khái niệm page/block dữ liệu, cache và sự khác nhau giữa đọc ngẫu nhiên với đọc tuần tự.",
  "Thống kê phân bố dữ liệu và selectivity (tỷ lệ hàng thỏa điều kiện) ở mức nhập môn."
],

dinh_nghia: "`EXPLAIN` là lệnh yêu cầu PostgreSQL trình bày **execution plan** (kế hoạch thực thi): cây các thao tác vật lý mà planner chọn để tạo kết quả SQL, kèm ước lượng chi phí và số hàng. `EXPLAIN (ANALYZE, BUFFERS)` đi xa hơn: nó chạy câu lệnh thật, đo thời gian, số hàng, số vòng lặp và lượng block đã chạm.\n\nPlan không phải bản dịch SQL theo từng dòng. SQL mô tả kết quả cần có; planner có thể đổi thứ tự join, chọn quét bảng hay index, dùng hash hay vòng lặp lồng nhau, và đẩy điều kiện xuống gần dữ liệu. Vì vậy đọc plan là đọc **dòng chảy hàng qua một cây operator**, không phải tìm xem mỗi từ khóa SQL nằm ở đâu.",

vi_sao_can: "Một truy vấn chậm hiếm khi tự nói nguyên nhân. Cùng biểu hiện “API mất hai giây” có thể đến từ đọc quá nhiều block, ước lượng sai khiến chọn join tệ, sort tràn ra đĩa, cùng một nút bị chạy hàng nghìn lần, khóa chờ, hoặc đơn giản là trả quá nhiều dữ liệu qua mạng. Đo thời gian toàn câu chỉ xác nhận bệnh; plan giúp khoanh vùng bộ phận tiêu tốn công việc.\n\nTrong trang đơn hàng xuyên suốt, câu SQL trông vô hại: lấy 50 đơn `PAID`, nối tên khách và cộng các dòng hàng. Nếu index chỉ có `status`, PostgreSQL có thể tìm được mọi đơn đã thanh toán nhưng vẫn phải sort hàng trăm nghìn bản ghi trước `LIMIT 50`. Nếu thống kê đánh giá sai số đơn `PAID`, planner có thể đặt một bảng lớn ở phía trong `Nested Loop`. Không nhìn plan, ta dễ thêm một index bất kỳ rồi tuyên bố tối ưu; nhìn plan, ta biết index phải phục vụ đồng thời điều kiện và thứ tự, chẳng hạn `(status, created_at DESC, id DESC)`.\n\n`EXPLAIN` cũng là hàng rào an toàn trước thay đổi. Nó cho phép kiểm tra giả thuyết “index mới loại bỏ sort”, “aggregate chỉ xử lý order_items của 50 đơn”, hoặc “query rewrite giảm số hàng trung gian”. Tuy nhiên nó không thay thế đo ở môi trường đại diện: dữ liệu ít, cache ấm và thống kê khác production có thể tạo plan khác hẳn.",

co_che: "### Plan là cây, không phải danh sách\n\nMỗi dòng thụt vào là một nút con cung cấp hàng cho nút cha. Hãy bắt đầu ở nút sâu nhất, đi sang các nút anh em, rồi lần ngược lên. Ví dụ rút gọn:\n\n```text\nLimit\n  -> Sort\n       Sort Key: o.created_at DESC, o.id DESC\n       -> Hash Join\n            Hash Cond: (o.customer_id = c.id)\n            -> Seq Scan on orders o\n                 Filter: (status = 'PAID')\n            -> Hash\n                 -> Seq Scan on customers c\n```\n\nHai `Seq Scan` tạo luồng hàng. `Hash` xây bảng băm từ khách hàng; `Hash Join` dò `customer_id`; `Sort` sắp kết quả; `Limit` chỉ nhận 50 hàng đầu. `LIMIT` nằm trên cùng không có nghĩa PostgreSQL luôn làm ít việc: nếu phía dưới cần sort toàn bộ, nó phải xem đủ ứng viên mới biết 50 hàng mới nhất.\n\n### `cost` là đơn vị mô hình, không phải mili giây\n\nMột nút thường có dạng `cost=0.43..812.91 rows=50 width=40`. Số đầu là startup cost: công việc trước khi trả được hàng đầu. Số sau là total cost nếu nút bị đọc hết. `rows` là số hàng planner dự đoán nút xuất ra; `width` là số byte trung bình mỗi hàng. Cost ghép từ tham số CPU và I/O tương đối, nên `812.91` không có nghĩa 812,91 ms.\n\nStartup cost quan trọng với `LIMIT`: index đã có đúng thứ tự có thể trả hàng đầu gần như ngay lập tức, trong khi sort phải thu thập đầu vào. Total cost quan trọng khi consumer đọc hết, như aggregate toàn bảng. So cost hữu ích giữa các phương án trên cùng cấu hình; dùng nó làm đồng hồ hoặc so giữa hai máy là sai đơn vị.\n\n### `actual time`, `rows` và `loops`\n\nKhi thêm `ANALYZE`, PostgreSQL báo dạng `actual time=0.021..0.118 rows=50 loops=1`. Hai thời điểm là lúc nút phát hàng đầu và hàng cuối trong **mỗi vòng**. `rows` cũng là trung bình mỗi vòng. Muốn hiểu tổng công việc, phải xét `rows × loops` và thời gian trong bối cảnh vòng lặp.\n\nMột `Index Scan` mất 0,05 ms nhưng `loops=20,000` có thể là tâm điểm của plan. Đây là hình thái thường gặp khi `Nested Loop` lấy 20.000 hàng phía ngoài rồi dò phía trong 20.000 lần. Người mới hay chọn dòng có `actual time` lớn nhất mà quên nhân loops, hoặc cộng thời gian cha và con dù thời gian nút cha đã bao gồm phần lớn thời gian con.\n\n### Ước lượng sai làm lựa chọn sai\n\nPlanner không chạy thử mọi plan; nó dựa vào statistics do `ANALYZE` thu thập. Đặt cạnh `rows=12` và `actual rows=48,000`: sai bốn nghìn lần là tín hiệu mạnh hơn việc thấy `Seq Scan`. Sai lệch có thể do statistics cũ, cột lệch phân bố, điều kiện trên biểu thức, tham số prepared statement, hoặc tương quan giữa nhiều cột mà statistics độc lập không biểu diễn được.\n\nVí dụ `status='PAID' AND created_at >= CURRENT_DATE - 1` có thể tương quan: đơn mới chưa kịp thanh toán, nên nhân hai tỷ lệ độc lập cho kết quả sai. Việc đúng trước tiên là `ANALYZE orders`, tăng statistics target có chọn lọc, hoặc tạo extended statistics phù hợp; thêm index không chữa được nhận thức sai của planner nếu nó vẫn không chọn index.\n\n### Scan nói cách tìm hàng\n\n`Seq Scan` đọc các page của bảng theo thứ tự vật lý và kiểm tra điều kiện. Nó hợp lý khi cần phần lớn bảng, bảng nhỏ, hoặc đọc tuần tự rẻ hơn hàng loạt lần nhảy từ index sang heap. `Index Scan` đi B-tree để tìm tuple rồi thường quay về heap lấy cột; nó mạnh khi điều kiện chọn ít hàng hoặc index cung cấp đúng thứ tự. `Index Only Scan` có thể lấy cột từ index, nhưng vẫn cần visibility map để tránh kiểm tra heap; tên gọi không bảo đảm heap fetch bằng 0.\n\n`Bitmap Index Scan` gom vị trí tuple thành bitmap, sau đó `Bitmap Heap Scan` đọc heap theo page. Nó nằm giữa hai cực: nhiều hàng hơn mức phù hợp cho từng lần dò ngẫu nhiên nhưng chưa đủ để quét toàn bảng. Dòng `Rows Removed by Filter` cho biết hàng đã được đọc rồi mới loại; còn `Index Cond` thu hẹp ngay trong cấu trúc index. Chuyển điều kiện từ filter thành index condition thường giảm công việc thật.\n\n### Join là quyết định về số lần và bộ nhớ\n\n`Nested Loop` lấy mỗi hàng bên ngoài rồi tìm hàng khớp bên trong. Với 50 đơn và index `order_items(order_id)`, đây có thể là lựa chọn tuyệt vời: 50 phép dò nhỏ. Với 100.000 đơn mà phía trong không có index, nó thành thảm họa. `Hash Join` xây hash table từ một phía rồi quét phía kia; phù hợp equality join và tập dữ liệu đáng kể, đổi lại cần bộ nhớ, có thể chia batch nếu không đủ. `Merge Join` cần hai luồng đã sắp theo khóa, hữu ích khi thứ tự có sẵn hoặc cần xử lý phạm vi lớn.\n\nKhông có join “nhanh nhất” độc lập với cardinality. Điều cần hỏi là: phía nào có bao nhiêu hàng thực, nút trong chạy bao nhiêu loops, có index hỗ trợ phép dò không, và hash/sort có tràn không. Một plan đổi từ hash sang nested loop sau khi thêm `LIMIT` có thể là cải thiện hợp lý vì chỉ cần vài chục hàng đầu.\n\n### Sort, aggregate và giới hạn bộ nhớ\n\nDòng `Sort Method` phân biệt sort trong RAM với external merge trên đĩa. `work_mem` là ngân sách cho từng operation, không phải toàn query; tăng toàn cục quá cao khi có nhiều connection có thể làm máy cạn RAM. Trước khi tăng, hãy xem có loại sort bằng index đúng thứ tự hay giảm đầu vào sớm được không.\n\n`HashAggregate` giữ nhóm trong bảng băm; `GroupAggregate` thường tiêu thụ đầu vào đã sắp. Với bài toán tổng tiền, aggregate toàn bộ `order_items` rồi mới `LIMIT 50` là dấu hiệu thứ tự logic chưa tốt. Một hướng là chọn 50 order trước trong CTE/subquery rồi mới join dòng hàng; khi đó lượng nhóm phụ thuộc kích thước trang, không phụ thuộc lịch sử toàn cửa hàng.\n\n### `BUFFERS` nối plan với I/O\n\n`BUFFERS` báo block `shared hit`, `read`, `dirtied`, `written`, cùng vùng temp nếu sort/hash tràn. `hit` nghĩa block đã ở shared buffers, không đồng nghĩa miễn phí: CPU vẫn phải duyệt và cache của hệ điều hành còn là tầng khác. `read` cho biết PostgreSQL phải yêu cầu đọc block vào buffer; một lần chạy đầu lạnh và lần sau ấm có thể chênh rõ.\n\nNếu hai query có thời gian gần nhau trên laptop nhưng một query chạm 500 block, query kia chạm 500.000 block, query thứ hai có rủi ro lớn hơn khi tải đồng thời hoặc cache bị cạnh tranh. So buffer thường ổn định hơn so đúng một con số millisecond. `temp read/written` là bằng chứng operation đã dùng file tạm, không nên suy đoán chỉ từ thời gian.\n\n### `LIMIT` và top-N heapsort\n\nKhi không có index cung cấp thứ tự, PostgreSQL có thể dùng top-N heapsort: giữ một heap nhỏ cho 50 ứng viên tốt nhất thay vì sort đầy đủ mọi hàng trong RAM. Điều này tiết kiệm bộ nhớ, nhưng vẫn phải đọc và so sánh toàn bộ hàng thỏa điều kiện. Index `(status, created_at DESC, id DESC)` cho phép định vị vùng `PAID` đã đúng thứ tự và dừng sau 50 hàng; lợi ích không chỉ là bỏ sort mà còn là **early stop**.\n\nNếu nhiều hàng có cùng `created_at`, thêm `id` làm khóa phá hòa vừa tạo thứ tự xác định vừa chuẩn bị cho keyset pagination ở bài sau. Nếu chỉ index `(status, created_at)`, PostgreSQL vẫn có thể cần incremental sort hoặc xử lý thêm để bảo đảm `id DESC`.\n\n### Đo an toàn và trung thực\n\n`EXPLAIN ANALYZE` thực thi câu lệnh. Với `UPDATE`, `DELETE` hoặc hàm có side effect, nó thay đổi dữ liệu trừ khi bọc trong transaction rồi `ROLLBACK`. Ngay cả `SELECT` cũng có thể tốn tải đáng kể. Trên production, bắt đầu bằng `EXPLAIN` không `ANALYZE`, lấy query đã thu thập từ observability, và thử trên môi trường đại diện khi có thể.\n\nCác tùy chọn hữu ích là `EXPLAIN (ANALYZE, BUFFERS, WAL, SETTINGS, FORMAT JSON)`. JSON phù hợp cho công cụ phân tích; dạng text dễ học và trao đổi. Tắt `TIMING` có thể giảm overhead khi query có cực nhiều nút gọi ngắn: `EXPLAIN (ANALYZE, BUFFERS, TIMING OFF)`. Luôn ghi rõ đã dùng cache ấm hay lạnh và không bịa số đo từ một plan chưa chạy.\n\n### Khi nào không nên “tối ưu theo plan”\n\nPlan của một literal, một tenant nhỏ hoặc một snapshot dữ liệu không đại diện có thể dẫn tới index chuyên trị sai tải. Đừng ép planner bằng cách tắt `enable_seqscan` rồi coi đó là sửa xong; tùy chọn ấy chủ yếu giúp chẩn đoán phương án thay thế. Đừng săn zero `Seq Scan` nếu endpoint trả 70% bảng. Và đừng tối ưu query chỉ chiếm 0,1% thời gian hệ thống trong khi N+1 tạo hàng nghìn round trip; plan là kính hiển vi, còn ưu tiên phải đến từ profiling toàn tuyến.",

vi_du: "### Dựng dữ liệu PostgreSQL có thể chạy lại\n\nĐoạn sau tạo dữ liệu xác định, không tuyên bố thời gian cố định vì phần cứng và cache khác nhau. Hãy chạy bằng `psql` trên database thử nghiệm:\n\n```sql\nDROP TABLE IF EXISTS order_items, orders, customers CASCADE;\nCREATE TABLE customers (\n  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  name text NOT NULL\n);\nCREATE TABLE orders (\n  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  customer_id bigint NOT NULL REFERENCES customers(id),\n  status text NOT NULL CHECK (status IN ('PENDING','PAID','CANCELLED')),\n  created_at timestamptz NOT NULL\n);\nCREATE TABLE order_items (\n  id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  order_id bigint NOT NULL REFERENCES orders(id),\n  quantity integer NOT NULL CHECK (quantity > 0),\n  unit_price numeric(12,2) NOT NULL CHECK (unit_price >= 0)\n);\n\nINSERT INTO customers(name)\nSELECT 'Khách ' || g FROM generate_series(1, 10000) AS g;\n\nINSERT INTO orders(customer_id, status, created_at)\nSELECT 1 + (g % 10000),\n       (ARRAY['PENDING','PAID','PAID','CANCELLED'])[1 + (g % 4)],\n       timestamptz '2025-01-01 00:00:00+00' + g * interval '1 minute'\nFROM generate_series(1, 200000) AS g;\n\nINSERT INTO order_items(order_id, quantity, unit_price)\nSELECT o.id, 1 + (x % 3), (10 + (o.id % 500))::numeric\nFROM orders o CROSS JOIN generate_series(1, 3) AS x;\nANALYZE;\n```\n\n### Plan trước và sau index\n\nTrước tiên chỉ chọn 50 đơn, để tách vấn đề tìm trang khỏi aggregate:\n\n```sql\nEXPLAIN (ANALYZE, BUFFERS, TIMING OFF)\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status = 'PAID'\nORDER BY created_at DESC, id DESC\nLIMIT 50;\n\nCREATE INDEX orders_paid_feed_idx\nON orders (status, created_at DESC, id DESC)\nINCLUDE (customer_id);\n\nEXPLAIN (ANALYZE, BUFFERS, TIMING OFF)\nSELECT id, customer_id, created_at\nFROM orders\nWHERE status = 'PAID'\nORDER BY created_at DESC, id DESC\nLIMIT 50;\n```\n\nĐừng chép một con số kỳ vọng. Hãy xác minh ba thay đổi trên output thật: `Sort` có biến mất không, scan có `Index Cond: (status = 'PAID')` không, và số block chạm giảm thế nào. `Heap Fetches` có thể chưa bằng 0 ngay sau insert vì visibility map; `VACUUM (ANALYZE) orders` trên database thử nghiệm giúp quan sát điều kiện của index-only scan.\n\n### Chọn trang trước, tính tiền sau\n\n```sql\nCREATE INDEX order_items_order_id_idx ON order_items(order_id);\n\nEXPLAIN (ANALYZE, BUFFERS, TIMING OFF)\nWITH page AS MATERIALIZED (\n  SELECT id, customer_id, created_at\n  FROM orders\n  WHERE status = 'PAID'\n  ORDER BY created_at DESC, id DESC\n  LIMIT 50\n)\nSELECT p.id, p.created_at, c.name,\n       sum(oi.quantity * oi.unit_price) AS total\nFROM page p\nJOIN customers c ON c.id = p.customer_id\nJOIN order_items oi ON oi.order_id = p.id\nGROUP BY p.id, p.created_at, c.name\nORDER BY p.created_at DESC, p.id DESC;\n```\n\nPlan hợp lý thường chỉ dò `order_items` cho 50 order. Kiểm tra `loops`, `actual rows` và `BUFFERS` thay vì buộc phải thấy đúng một loại join: phiên bản PostgreSQL, thống kê và dữ liệu có thể chọn operator khác mà vẫn tốt.\n\nCa biên cần thử: không có đơn `PAID` thì index trả rỗng nhanh; chỉ một đơn thì aggregate vẫn đúng; nhiều đơn trùng `created_at` vẫn có thứ tự nhờ `id`; order không có item sẽ biến mất với `JOIN`, nên nếu nghiệp vụ cần giữ nó phải dùng `LEFT JOIN` và `COALESCE(sum(...), 0)`.",

so_sanh: "### Chọn công cụ theo câu hỏi\n\n| Công cụ | Trả lời tốt nhất | Điểm mù | Khi nên dùng |\n|---|---|---|---|\n| `EXPLAIN` | Planner dự định chọn cây nào và vì sao cost thấp | Không có thời gian, hàng thật hay buffer thật | Kiểm tra an toàn trước câu lệnh ghi hoặc production nhạy cảm |\n| `EXPLAIN ANALYZE` | Hàng và thời gian thực ở từng nút | Có overhead và thực sự chạy lệnh | Tái hiện query chậm trên dữ liệu đại diện |\n| `BUFFERS` | Bao nhiêu block đến từ cache, đọc hoặc temp | Không mô tả round trip ứng dụng | Phân biệt CPU với áp lực I/O, so lượng công việc |\n| `pg_stat_statements` | Query nào tốn tổng thời gian trên toàn hệ thống | Không tự cho plan của từng lần | Chọn đúng query đáng tối ưu trước |\n| Log truy vấn chậm/APM | Độ trễ đầu-cuối và quan hệ với request | Thường thiếu chi tiết operator | Tìm ảnh hưởng đến người dùng và N+1 |\n\nSo với “thêm index rồi đo endpoint”, plan giải thích cơ chế và dự báo đánh đổi ghi/không gian. Nhưng plan không nhìn thấy serialization, network, connection pool hay code gọi SQL nhiều lần; đó là lý do bài N+1 kế tiếp phải đo số câu lệnh ở tầng ứng dụng.",

loi_thuong_gap: [
  "Thấy `Seq Scan` là kết luận xấu; nhận biết bằng việc bỏ qua tỷ lệ hàng trả về và bảng nhỏ. Hãy so block, rows và selectivity trước.",
  "Đọc plan từ trên xuống như thứ tự chạy; dấu hiệu là giải thích `Limit` chạy trước `Sort`. Hãy lần từ nút lá lên cha.",
  "Nhầm `cost` với millisecond; nhận biết khi đem `cost=1000` so trực tiếp SLA một giây. Cost chỉ là đơn vị tương đối của mô hình.",
  "Bỏ qua `loops`; dấu hiệu là nút trong nhìn cực nhanh nhưng tổng query chậm. Xét hàng và thời gian cùng số vòng.",
  "Chạy `EXPLAIN ANALYZE DELETE/UPDATE` trên dữ liệu thật vì tưởng chỉ mô phỏng; hãy dùng `EXPLAIN` hoặc transaction có `ROLLBACK` đã kiểm soát.",
  "Tăng `work_mem` toàn cục khi thấy sort ra đĩa; dấu hiệu là RAM tăng theo concurrency. Ưu tiên giảm đầu vào hoặc đặt cấu hình cục bộ có đo đạc.",
  "Thêm index nhưng không `ANALYZE`, rồi kết luận planner ngu; nhận biết bởi estimated rows lệch xa actual rows và statistics cũ.",
  "Ép một loại scan bằng planner toggle rồi để đó như bản sửa; toggle chỉ là thí nghiệm phản chứng, không phải thiết kế bền vững."
],

tu_kiem_tra: [
  "Vì sao `LIMIT 50` không bảo đảm chỉ 50 hàng được đọc khi bên dưới có `Sort`?",
  "Trong `actual time=0.01..0.05 rows=2 loops=10000`, những con số nào cho thấy công việc tích lũy lớn?",
  "Khi lấy 70% bảng, vì sao `Seq Scan` có thể rẻ hơn `Index Scan` dù index tìm khóa theo log₂ n?",
  "`Rows Removed by Filter` khác `Index Cond` ở điểm nào về thời điểm hàng bị loại?",
  "Vì sao `shared hit` không có nghĩa operation miễn phí, và vì sao vẫn đáng so với `shared read`?",
  "Khó: estimated rows ở từng scan khá đúng nhưng sau hai điều kiện tương quan lại sai 1000 lần; bạn kiểm tra loại statistics nào trước khi thêm index?",
  "Khó: plan mới có total cost cao hơn nhưng trả hàng đầu sớm hơn nhiều. Với endpoint `LIMIT 20`, vì sao nó vẫn có thể thắng?"
],

tai_nguyen: [
  "PostgreSQL Documentation — Using EXPLAIN và The Query Planning Process.",
  "PostgreSQL Documentation — Planner Statistics và Resource Consumption.",
  "SQL Performance Explained — Markus Winand.",
  "The Art of PostgreSQL — Dimitri Fontaine."
],

lien_he: [
  "Nối sang mảng Cấu trúc dữ liệu: B-tree, hash table và heap giải thích vì sao scan, Hash Join và top-N sort có hình dạng chi phí khác nhau.",
  "Nối sang mảng Hệ điều hành: page cache, đọc đĩa và ngân sách RAM làm rõ các trường `BUFFERS`, temp file và `work_mem`.",
  "Nối sang mảng Backend/API: trace một request phải ghép thời gian ứng dụng với plan, đặc biệt khi một endpoint phát nhiều query nhỏ.",
  "Nối sang mảng Kiểm thử: dataset đại diện, warm-up và lặp phép đo giúp tránh kết luận từ một lần chạy nhiễu."
]

};
