window.LESSONS["05:framework-xay-api-that:django-va-flask-biet-khac-biet"] = {

tom_tat: [
  "Django là web framework tích hợp nhiều quyết định sẵn; Flask là microframework cung cấp lõi HTTP và để đội dự án tự ghép các thành phần còn lại.",
  "Khác biệt quan trọng không nằm ở số dòng của endpoint đầu tiên mà ở tổng số quyết định phải duy trì khi hệ thống có authentication, migration, admin, test và vận hành.",
  "Cùng nghiệp vụ đặt bàn có thể viết đúng bằng Django, Flask hoặc FastAPI; lựa chọn phụ thuộc hình dạng sản phẩm, kỹ năng đội ngũ và mức tự do kiến trúc cần thiết.",
  "Django mặc định hướng tới ứng dụng đầy đủ và có ORM đồng bộ; Flask trung lập hơn nhưng extension không tự tạo thành một kiến trúc nhất quán.",
  "Bảo mật đến từ default hợp lý cộng với cấu hình đúng: CSRF, host, cookie, proxy header và lỗi production đều cần hiểu, không thể chỉ tin tên framework."
],

can_biet_truoc: [
  "HTTP method, status code, header, cookie và JSON body.",
  "Mô hình MVC/MTV, ORM, migration và transaction.",
  "FastAPI routing, Pydantic validation và dependency injection.",
  "WSGI, ASGI ở mức biết chúng là giao diện giữa server và ứng dụng Python."
],

dinh_nghia: "**Django** là framework web Python theo hướng “batteries included”: routing, ORM, migration, template, form, authentication, session, admin và nhiều lớp bảo vệ được thiết kế để phối hợp với nhau. Django gọi cách tổ chức của nó là Model–Template–View; `View` nhận request và điều phối, `Model` quản lý dữ liệu, còn `Template` tạo giao diện.\n\n**Flask** là microframework WSGI: lõi tập trung vào ánh xạ URL, request/response, context và template, còn ORM, migration, validation hay authentication được chọn riêng hoặc tự viết. “Micro” mô tả phạm vi lõi, không giới hạn quy mô ứng dụng.\n\nHai framework đều có thể phục vụ API “Bàn Trống”. Câu hỏi đúng không phải cái nào mạnh tuyệt đối, mà là đội dự án muốn nhận một bộ quy ước tích hợp hay muốn sở hữu các quyết định ghép nối.",

vi_sao_can: "Một demo đặt bàn chỉ cần route `POST /bookings`, nhưng sản phẩm thật nhanh chóng cần tài khoản nhân viên, màn hình quản trị, schema database, migration, phân quyền, chống giả mạo request, email và kiểm thử. Nếu đánh giá framework bằng “Hello World”, ta tối ưu vài phút đầu rồi bỏ qua nhiều năm bảo trì.\n\nDjango xuất hiện để giải quyết phần lặp của website dữ liệu: model khai một lần có thể dùng cho ORM, form và admin; middleware thống nhất session và bảo mật; migration đi cùng model. Flask giải quyết nhu cầu khác: giữ lõi nhỏ để ứng dụng chọn SQLAlchemy, schema library, kiểu authentication và cấu trúc package phù hợp.\n\nBiết khác biệt còn giúp đọc hệ thống có sẵn. Người quen FastAPI dễ hiểu nhầm rằng Django luôn cũ và đồng bộ, hoặc Flask chỉ dành cho đồ chơi. Cả hai kết luận đều bỏ qua bối cảnh. Django có ASGI và async ở những phần hỗ trợ; Flask có thể chạy dịch vụ lớn nếu kỷ luật kiến trúc tốt. Đánh đổi thật nằm ở coherence (độ nhất quán), quyền kiểm soát, hệ sinh thái và chi phí quyết định.",

co_che: "### Cùng request nhưng đi qua hai đường ống\n\nVới Django, URLconf chọn view; middleware chạy quanh view; view dùng model/ORM; response quay ngược qua middleware. Các thành phần biết quy ước chung như settings, request user và transaction. Với Flask, WSGI server gọi app, router tìm view function, `before_request`/`after_request` bao quanh xử lý; extension thường lưu tài nguyên theo application context.\n\nSự khác nhau này ảnh hưởng debug. Trong Django, tìm authentication thường bắt đầu ở middleware và backend chuẩn. Trong Flask, phải biết dự án đã chọn extension hoặc decorator nào. Tự do của Flask giảm ràng buộc ban đầu nhưng tăng lượng tri thức cục bộ mà thành viên mới phải học.\n\n### Django model tạo ra một trục dữ liệu chung\n\nModel `Booking` có thể dùng cho truy vấn, migration và admin. Điều này giảm những ánh xạ rời rạc, đặc biệt hữu ích với back-office. Nhưng đừng trả model trực tiếp ra API: schema lưu trữ và hợp đồng công khai có tốc độ thay đổi khác nhau.\n\n```python\n# bookings/models.py\nfrom django.db import models\n\nclass Booking(models.Model):\n    guest_name = models.CharField(max_length=120)\n    guest_email = models.EmailField()\n    party_size = models.PositiveSmallIntegerField()\n    starts_at = models.DateTimeField()\n    status = models.CharField(max_length=20, default=\"pending\")\n    created_at = models.DateTimeField(auto_now_add=True)\n\n    class Meta:\n        constraints = [\n            models.CheckConstraint(\n                condition=models.Q(party_size__gte=1),\n                name=\"booking_party_size_positive\",\n            )\n        ]\n```\n\nConstraint database quan trọng vì validation ở HTTP không bảo vệ dữ liệu được nhập từ job hoặc admin. Migration biến thay đổi model thành lịch sử có thứ tự. Đánh đổi là domain bị gắn với Django ORM; nếu nghiệp vụ cần nhiều storage hoặc kiến trúc thuần domain, lớp model này có thể quá nhiều trách nhiệm.\n\n### Django view: rõ transaction trước khi nói async\n\nĐặt bàn phải tránh hai request chiếm cùng bàn. `transaction.atomic()` và khóa hàng liên quan đáng quan tâm hơn việc đổi `def` thành `async def`. I/O đồng thời không sửa race condition.\n\n```python\n# bookings/views.py\nimport json\nfrom django.db import transaction\nfrom django.http import JsonResponse\nfrom django.views.decorators.http import require_POST\nfrom .models import Booking\n\n@require_POST\ndef create_booking(request):\n    try:\n        data = json.loads(request.body)\n        party_size = int(data[\"party_size\"])\n        if not 1 <= party_size <= 20:\n            raise ValueError(\"party_size ngoài phạm vi\")\n    except (json.JSONDecodeError, KeyError, TypeError, ValueError):\n        return JsonResponse({\"error\": \"invalid_booking\"}, status=422)\n\n    with transaction.atomic():\n        booking = Booking.objects.create(\n            guest_name=str(data[\"guest_name\"]).strip(),\n            guest_email=str(data[\"guest_email\"]).lower(),\n            party_size=party_size,\n            starts_at=data[\"starts_at\"],\n        )\n    return JsonResponse({\"id\": booking.pk, \"status\": booking.status}, status=201)\n```\n\nĐoạn tối giản này chưa validation ngày giờ tốt và chưa chống trùng; chính sự bất tiện đó cho thấy API chuyên JSON thường dùng Django REST Framework hoặc lớp schema khác. Django core mạnh về web đầy đủ, không tự động biến request JSON thành object có kiểu như FastAPI/Pydantic.\n\n### Flask application factory giữ khởi tạo có kiểm soát\n\nKhông nên tạo app, đọc secret và mở kết nối ngay khi module được import. Application factory nhận config, đăng ký extension và blueprint; test tạo nhiều app độc lập.\n\n```python\n# booking_app/__init__.py\nfrom flask import Flask\nfrom flask_sqlalchemy import SQLAlchemy\n\ndb = SQLAlchemy()\n\ndef create_app(config=None):\n    app = Flask(__name__)\n    app.config.from_mapping(\n        MAX_CONTENT_LENGTH=64 * 1024,\n        JSON_SORT_KEYS=False,\n    )\n    if config:\n        app.config.from_mapping(config)\n    db.init_app(app)\n\n    from .bookings import bp\n    app.register_blueprint(bp, url_prefix=\"/v1/bookings\")\n    return app\n```\n\nFactory làm dependency graph hiện ra và tránh global app cứng. Tuy nhiên `db` vẫn là extension proxy dựa trên context. Gọi nó ngoài request/app context sẽ lỗi; đó là tín hiệu cần hiểu vòng đời thay vì tùy tiện `push()` context ở mọi nơi.\n\n### Flask route cần tự đặt ranh giới validation\n\nFlask phân tích JSON nhưng không áp schema miền. Ta phải chọn Marshmallow, Pydantic hoặc tự kiểm tra. Ví dụ dưới dùng Pydantic để giữ hợp đồng gần bài FastAPI:\n\n```python\nfrom flask import Blueprint, jsonify, request\nfrom pydantic import BaseModel, EmailStr, Field, ValidationError\n\nbp = Blueprint(\"bookings\", __name__)\n\nclass BookingIn(BaseModel):\n    guest_name: str = Field(min_length=1, max_length=120)\n    guest_email: EmailStr\n    party_size: int = Field(ge=1, le=20)\n    starts_at: str\n\n@bp.post(\"\")\ndef create_booking():\n    try:\n        command = BookingIn.model_validate(request.get_json())\n    except (ValidationError, TypeError) as exc:\n        details = exc.errors() if isinstance(exc, ValidationError) else []\n        return jsonify(error=\"invalid_booking\", details=details), 422\n\n    booking = save_booking(command)  # service tự quản transaction\n    return jsonify(id=booking.id, status=booking.status), 201\n```\n\n`save_booking` không nên commit nửa chừng ở nhiều helper. Unit of work phải rõ để ngoại lệ rollback toàn bộ thay đổi. Microframework không ép cấu trúc, nên code review và convention của đội phải làm việc đó.\n\n### Authentication, admin và sản phẩm nội bộ\n\nDjango nổi bật khi Bàn Trống cần nhân viên tìm đặt bàn, sửa trạng thái và phân quyền. Admin sinh giao diện từ model, nhưng không phải giao diện khách hàng và không nên mở công khai thiếu kiểm soát. Django authentication có user, group, permission và session phối hợp sẵn.\n\nFlask có extension cho các mảnh này, song đội dự án quyết định extension nào là nguồn chân lý và cách chúng tương tác. Với một API nhỏ dùng identity provider bên ngoài, sự tối giản ấy có lợi. Với hệ thống quản trị nhiều màn hình, tự ghép có thể tốn hơn phần code nghiệp vụ.\n\n### CSRF, CORS và cookie không phải một vấn đề\n\nDjango bật CSRF middleware trong cấu hình dự án chuẩn cho request thay đổi trạng thái dựa trên cookie. Tắt `csrf_exempt` để “API chạy được” có thể mở lỗ hổng nếu browser tự gửi session cookie. Token Bearer trong header có mô hình khác, nhưng vẫn cần chính sách CORS chính xác.\n\nFlask không tự biết ứng dụng dùng form/session thế nào; bảo vệ CSRF thường đến từ extension. CORS chỉ điều khiển browser cho JavaScript đọc/gửi request chéo origin, không ngăn curl hay server khác gọi API. Không dùng `*` với credential. Cookie production cần `Secure`, `HttpOnly`, `SameSite` phù hợp và khóa ký đủ mạnh.\n\n### Host header, proxy và địa chỉ người dùng\n\nỨng dụng sau reverse proxy nhận scheme/IP gốc qua forwarded header. Tin mọi header do client gửi sẽ cho phép giả IP, tạo URL reset password sai host hoặc né rate limit. Django có `ALLOWED_HOSTS` và các setting proxy; Flask/Werkzeug có middleware tương ứng, nhưng phải cấu hình đúng số proxy tin cậy.\n\nNguyên tắc là proxy biên xóa header từ Internet rồi tự ghi giá trị chuẩn; app chỉ tin proxy đã biết. Không sao chép một cấu hình proxy từ môi trường khác vì số lớp load balancer có thể khác.\n\n### WSGI, ASGI và giới hạn concurrency\n\nFlask truyền thống theo WSGI: mỗi worker xử lý request theo mô hình đồng bộ; tăng worker/thread để có concurrency. Django hỗ trợ cả WSGI và ASGI, nhưng một chuỗi middleware hoặc ORM call đồng bộ vẫn có thể buộc chuyển ngữ cảnh. `async` chỉ giúp khi request chờ nhiều I/O và toàn bộ đường đi có API bất đồng bộ phù hợp.\n\nFastAPI sinh ra quanh ASGI và type hint, nên tự nhiên hơn cho API I/O-bound và streaming. Điều đó không làm Django chậm cho mọi workload. Database query thiếu index hoặc N+1 thường lớn hơn overhead framework. Phải đo trên workload đại diện, không lấy benchmark Hello World làm quyết định sản phẩm.\n\n### Lỗi production phải có hình dạng ổn định\n\nDebug mode của cả hai framework không được mở ra Internet vì trang lỗi có thể lộ environment, đường dẫn và dữ liệu request; một số debugger còn có khả năng thực thi code. Client chỉ nên nhận mã lỗi ổn định cùng request ID, còn stack trace vào log nội bộ.\n\nDjango có handler và middleware; Flask có `errorhandler`. Hãy phân biệt lỗi validation 422, xung đột đặt bàn 409, không xác thực 401, không đủ quyền 403 và lỗi phụ thuộc 503. Trả 500 cho tất cả làm client retry sai và che mất lỗi nghiệp vụ.\n\n### Ca biên của đặt bàn\n\nBody rỗng, JSON sai, email Unicode, giờ không có timezone, thời điểm đã qua, số khách bằng 0 hoặc quá lớn đều phải được quyết định. Hai request đồng thời cho suất cuối cần constraint/lock ở database; kiểm tra “còn bàn” rồi insert ở hai câu lệnh không nguyên tử sẽ overbook.\n\nRequest bị client ngắt không đảm bảo transaction phía server tự dừng. Email xác nhận lỗi sau commit không nên rollback booking; dùng outbox hoặc queue. Nếu retry POST vì timeout, idempotency key ngăn tạo hai booking. Những điều này độc lập framework, nhưng framework ảnh hưởng nơi ta đặt cơ chế.\n\n### Khi nào không chọn mỗi bên\n\nKhông nên chọn Django chỉ vì admin đẹp nếu domain chủ yếu là streaming, websocket hoặc API nhỏ không dùng ORM/template/auth của nó; bạn sẽ mang nhiều convention không sinh lợi. Không nên chọn Flask khi đội muốn một chuẩn thống nhất nhưng chưa có năng lực thiết kế và duy trì bộ extension, security baseline, migration và observability.\n\nĐừng rewrite FastAPI đang ổn chỉ để đồng nhất sở thích. Chi phí chuyển framework gồm migration hành vi lỗi, auth, telemetry và runbook, không chỉ chuyển decorator. Framework tốt là framework làm giảm rủi ro quan trọng của sản phẩm và đội có thể vận hành thành thạo.",

vi_du: "### Kiểm thử cùng một hợp đồng bên ngoài\n\nDù implementation khác, client phải thấy cùng semantics. Contract test có thể được chạy với base URL của Django, Flask hoặc FastAPI:\n\n```python\nimport httpx\n\ndef assert_booking_contract(base_url: str):\n    payload = {\n        \"guest_name\": \"An\",\n        \"guest_email\": \"an@example.com\",\n        \"party_size\": 4,\n        \"starts_at\": \"2030-06-15T19:00:00+07:00\",\n    }\n    response = httpx.post(f\"{base_url}/v1/bookings\", json=payload, timeout=2.0)\n    assert response.status_code == 201\n    body = response.json()\n    assert isinstance(body[\"id\"], int)\n    assert body[\"status\"] == \"pending\"\n\n    bad = httpx.post(\n        f\"{base_url}/v1/bookings\",\n        json={**payload, \"party_size\": 0},\n        timeout=2.0,\n    )\n    assert bad.status_code == 422\n    assert bad.json()[\"error\"] == \"invalid_booking\"\n```\n\nTest này không khẳng định transaction chống overbook; cần integration test gửi đồng thời và kiểm tra constraint. Nó cũng không đo hiệu năng. Giá trị của contract test là giữ giao diện công khai ổn định khi đổi implementation.\n\n### Ma trận quyết định ngắn\n\nHãy chấm nhu cầu thật thay vì đếm tính năng quảng cáo. Nếu Bàn Trống cần website nội bộ, staff permission, CRUD và dữ liệu quan hệ, Django thường giảm thời gian tích hợp. Nếu đây là service mỏng, đội đã chuẩn hóa SQLAlchemy/Pydantic và muốn kiểm soát package, Flask hợp lý. Nếu API type-first, tài liệu OpenAPI và async I/O là trung tâm, FastAPI thường có lợi thế.",

so_sanh: "### Ba lựa chọn cho cùng miền nghiệp vụ\n\n| Tiêu chí | Django | Flask | FastAPI |\n|---|---|---|---|\n| Phạm vi lõi | full-stack tích hợp | HTTP core tối giản | API framework type-first |\n| Dữ liệu | ORM và migration đi kèm | tự chọn | tự chọn, thường SQLAlchemy |\n| Validation JSON | cần form/DRF/schema khác | tự chọn thư viện | Pydantic tích hợp |\n| Admin/auth/session | mạnh và đồng bộ | extension hoặc tự xây | thường ghép ngoài |\n| Giao diện server | WSGI và ASGI | chủ yếu WSGI | ASGI |\n| Giá của tự do | nhiều convention | nhiều quyết định cục bộ | phụ thuộc type model và hệ ASGI |\n| Hợp với Bàn Trống khi | có back-office phong phú | service nhỏ, stack đã chuẩn hóa | API là sản phẩm chính |\n\nSo sánh không nên biến thành “nặng” và “nhẹ”. Django có thể nhẹ về chi phí tổ chức vì nhiều quyết định đã tương thích. Flask có thể nặng về nhận thức khi mỗi repository ghép extension khác nhau. FastAPI giảm công tạo schema API nhưng không cung cấp admin hay hệ permission hoàn chỉnh. Tổng chi phí nằm ở cả vòng đời.",

loi_thuong_gap: [
  "Chọn bằng benchmark Hello World: nhận biết khi tài liệu quyết định chỉ có request/giây mà không nói database, auth hay vận hành; sửa bằng workload và tiêu chí sản phẩm.",
  "Dùng `csrf_exempt` để chữa 403: nhận biết qua endpoint session-cookie không còn CSRF check; sửa luồng token/template thay vì tắt toàn cục.",
  "Flask app toàn cục mở connection lúc import: test khó cô lập và worker fork mang trạng thái sai; nhận biết bằng side effect khi chỉ import module, sửa bằng application factory.",
  "Trả Django model hoặc SQLAlchemy object trực tiếp: vô tình lộ trường nội bộ và gắn API vào schema lưu trữ; nhận biết khi migration làm response đổi.",
  "Tin forwarded header từ mọi client: IP log/rate limit bị giả; nhận biết bằng request trực tiếp tự đặt `X-Forwarded-For`, sửa trust boundary proxy.",
  "Bật debug trên production: trang lỗi lộ config hoặc debugger; nhận biết qua response HTML chi tiết, sửa cấu hình fail-safe và error handler.",
  "Kiểm tra còn bàn ngoài transaction: tải đồng thời tạo booking vượt sức chứa; nhận biết bằng integration test song song, sửa constraint/lock nguyên tử."
],

tu_kiem_tra: [
  "Vì sao “Flask ít file hơn” không đủ kết luận tổng hệ thống đơn giản hơn Django?",
  "Trong request dùng session cookie, CSRF và CORS bảo vệ hai ranh giới khác nhau như thế nào?",
  "Hãy chỉ ra chỗ cần transaction để hai request không chiếm suất cuối và vì sao `async` không sửa được race condition đó.",
  "Khi nào Django admin là đòn bẩy sản phẩm, và khi nào mở rộng nó thành giao diện khách hàng trở thành nợ kỹ thuật?",
  "Application factory của Flask giúp test và worker lifecycle ra sao?",
  "Thiết kế contract test để thay implementation mà client không nhận thấy; phần nào vẫn cần integration test database?",
  "Một Django view async gọi ORM đồng bộ có thể gây vấn đề gì, và bạn cần đo điều gì trước khi rewrite?"
],

tai_nguyen: [
  "Django Documentation — Models, Security và Deployment checklist.",
  "Flask Documentation — Application Factories, Blueprints và Security Considerations.",
  "Two Scoops of Django.",
  "Architecture Patterns with Python."
],

lien_he: [
  "Nối sang mảng Database: ORM không thay transaction, constraint và index; bài toán chống overbook được giải ở ranh giới dữ liệu.",
  "Nối sang mảng Bảo mật: CSRF, cookie, host header và proxy trust là các lớp khác nhau trong threat model web.",
  "Nối sang mảng Kiến trúc phần mềm: framework tích hợp và microframework biểu hiện đánh đổi giữa convention, coupling và quyền tự chọn.",
  "Nối sang mảng Kiểm thử: contract test giữ hành vi HTTP, còn integration test chứng minh transaction dưới concurrency."
]

};
