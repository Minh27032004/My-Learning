window.LESSONS["06:cac-khoi-xay-dung:api-gateway-service-discovery"] = {

tom_tat: [
  "API gateway là cửa vào thống nhất cho client: nó kết thúc TLS, xác thực, giới hạn lưu lượng, định tuyến và ghép một phần dữ liệu; service discovery là cơ chế tìm các replica đang phục vụ ở địa chỉ nào.",
  "Hai thành phần giải hai bài toán khác nhau. Gateway quyết định request nên đi đâu và được phép đi hay không; discovery duy trì ánh xạ tên service sang tập endpoint thay đổi liên tục.",
  "Trong nền tảng Lớp Mở, một request tạo phiên tải video đi qua gateway, được kiểm token rồi chuyển tới một replica `upload-service`; chính dữ liệu video 2–20 GB không nên chảy xuyên gateway.",
  "Client-side discovery giảm một hop nhưng làm mọi client phức tạp; server-side discovery tập trung chính sách nhưng thêm hop và một điểm nghẽn cần nhân bản.",
  "Gateway hữu ích khi chính sách biên đủ nhiều để bù chi phí vận hành. Monolith nhỏ hoặc hệ thống chỉ có vài client nội bộ thường chưa cần lớp này."
],

can_biet_truoc: [
  "HTTP request/response, status code, header, TLS và khái niệm reverse proxy.",
  "Load balancer L4/L7, health check chủ động và bị động, timeout kết nối.",
  "Microservice, replica, container và lý do địa chỉ IP của tiến trình có thể thay đổi sau khi triển khai lại.",
  "Token xác thực, rate limiting và nguyên tắc retry chỉ an toàn với thao tác idempotent."
],

dinh_nghia: "**API gateway** là reverse proxy ở ranh giới hệ thống, cung cấp một API ổn định cho client rồi áp dụng các chính sách dùng chung và định tuyến request đến service phía trong. Nó không đồng nghĩa với load balancer: load balancer chủ yếu phân phối lưu lượng giữa các endpoint tương đương, còn gateway hiểu route nghiệp vụ, danh tính người gọi, quota và đôi khi biến đổi giao thức hoặc ghép phản hồi.\n\n**Service discovery** (khám phá dịch vụ) là quá trình đăng ký, theo dõi và tra cứu ánh xạ từ tên logic như `upload-service` đến các endpoint hiện còn hợp lệ như `10.24.7.18:8080`. Registry là nơi giữ ánh xạ; discovery là toàn bộ vòng đời gồm đăng ký, health check, tra cứu, cache và loại endpoint chết.\n\nHai khái niệm thường đứng cạnh nhau vì gateway phải tìm được replica trước khi chuyển tiếp, nhưng có thể tồn tại độc lập: một gateway có thể định tuyến tới địa chỉ tĩnh, còn các worker nội bộ vẫn dùng discovery mà không đi qua gateway.",

vi_sao_can: "Ở Lớp Mở, ứng dụng web, iOS và Android cần gọi `auth`, `course`, `upload`, `billing`. Nếu client biết từng hostname nội bộ, mỗi lần tách service hoặc đổi đường dẫn phải phát hành ba ứng dụng; phiên bản mobile cũ có thể sống nhiều tháng. Một cửa vào `api.lopmo.example` giữ hợp đồng phía ngoài ổn định và giấu cấu trúc triển khai phía trong.\n\nĐịa chỉ phía trong lại không ổn định. Giả sử `upload-service` có 12 replica giờ cao điểm. Autoscaler thêm 8 replica khi giảng viên đồng loạt tải video, sau đó thu về 4 replica ban đêm. Pod bị thay có IP mới; danh sách ghi tay trong cấu hình sẽ lỗi ngay sau lần tự chữa đầu tiên. Registry biến “service nào?” thành danh sách endpoint sống tại thời điểm tra cứu.\n\nGateway còn gom những việc nếu để từng service tự làm sẽ dễ lệch: xác minh chữ ký token, gắn request ID, chặn payload metadata quá lớn, quota theo trường học và chuẩn hóa log. Tuy nhiên tập trung không có nghĩa là nhét mọi nghiệp vụ vào cửa ngõ. Nếu gateway tính học phí hoặc quản lý trạng thái upload, nó trở thành monolith phân tán: mọi thay đổi nghiệp vụ đều phải triển khai lớp có blast radius lớn nhất.\n\nQuy mô 8.000 request/s không tự động bắt buộc có gateway. Lý do đúng là số loại client, số chính sách dùng chung và tốc độ thay đổi topology. Một hệ thống 20.000 request/s nhưng chỉ một service vẫn có thể dùng load balancer đơn giản; hệ thống 500 request/s với hàng chục API đối tác lại có thể hưởng lợi rõ rệt từ gateway.",

co_che: "### Một request đi hết đường nào\n\nGiảng viên bấm “Tải video”, ứng dụng gọi `POST /v1/courses/c42/uploads`. DNS đưa client tới load balancer công khai, rồi một replica gateway nhận request. Gateway kết thúc TLS, tạo hoặc giữ `trace-id`, kiểm token, kiểm quota và khớp route `/v1/courses/*/uploads` với `upload-service`. Nó hỏi lớp discovery để lấy tập endpoint khỏe, chọn một replica, đặt deadline cho lời gọi phía sau và chuyển tiếp.\n\n```text\nclient → public load balancer → gateway\n                              ├─ kiểm token + quota\n                              ├─ tra upload-service → {u1, u2, u3}\n                              └─ gọi u2 → trả upload_id + signed URL\n\nclient ───────── PUT từng part trực tiếp ─────────→ blob storage\n```\n\nChi tiết cuối rất quan trọng: gateway chỉ xử lý request metadata vài kilobyte. Nếu video 10 GB đi xuyên gateway, mỗi byte vào rồi lại ra làm gấp đôi băng thông mạng ở cửa ngõ, chiếm connection lâu và mở rộng blast radius. Gateway cấp quyền ngắn hạn để client tải thẳng sang blob storage; bài kế tiếp tiếp tục luồng này.\n\n### Control plane và data plane\n\nService discovery dễ hiểu hơn khi tách hai mặt phẳng. **Control plane** thu thập trạng thái: endpoint đăng ký, heartbeat, health check, rollout và thay đổi cấu hình. **Data plane** xử lý request thật: gateway lấy một endpoint đã biết và gửi lưu lượng đến đó.\n\nNếu registry chập chờn trong 5 giây, data plane không nhất thiết dừng. Gateway có thể dùng snapshot cuối cùng và health check cục bộ. Ngược lại, nếu thiết kế buộc mọi request đồng bộ hỏi registry, registry trở thành dependency trên đường nóng: 8.000 request/s biến thành ít nhất 8.000 lookup/s trước cả khi service nhận việc. Cách thường dùng là watch một luồng thay đổi hoặc cache danh sách trong vài giây, rồi cân bằng trên bộ nhớ.\n\nĐánh đổi của cache là độ cũ. TTL 30 giây giảm tải registry nhưng có thể tiếp tục chọn pod vừa chết. TTL 1 giây phản ứng nhanh hơn nhưng tăng lookup và tạo dao động khi hàng trăm gateway cùng làm mới. Watch đẩy thay đổi hiệu quả hơn polling, song cần xử lý mất kết nối, số phiên bản và resync để không bỏ sót sự kiện.\n\n### Đăng ký: service tự báo hay nền tảng quan sát\n\nVới self-registration, mỗi instance khởi động rồi gọi registry: “tôi là `upload-service`, ở IP này, cổng này”. Cách đó dùng được ngoài orchestrator, nhưng ứng dụng phải biết credential registry, gia hạn lease và hủy đăng ký đúng lúc. Tiến trình bị kill cứng không kịp hủy nên registry vẫn cần lease hết hạn.\n\nVới third-party registration, orchestrator hoặc agent quan sát lifecycle rồi cập nhật registry. Ứng dụng ít code hạ tầng hơn, và trạng thái triển khai là nguồn sự thật. Kubernetes Service/EndpointSlice là ví dụ về hướng này. Mặt trái là coupling với nền tảng: chạy cùng binary trên máy ảo trần có thể cần adapter khác.\n\nMột bản ghi endpoint nên có ít nhất service name, địa chỉ, cổng, zone, phiên bản, protocol và trạng thái readiness. Gắn quá nhiều metadata nghiệp vụ vào registry làm truy vấn phức tạp và khó nhất quán; registry nên trả lời “gọi được ở đâu”, không thay database cấu hình sản phẩm.\n\n### Readiness khác liveness\n\nLiveness hỏi tiến trình còn hoạt động hay đã kẹt đến mức cần khởi động lại. Readiness hỏi instance có nên nhận request mới ngay lúc này không. Khi `upload-service` đang nạp cache quyền truy cập hoặc chưa kết nối được metadata database, nó có thể sống nhưng chưa sẵn sàng.\n\nNếu dùng cùng một probe cho cả hai, database chậm có thể khiến hàng loạt pod restart đồng thời, làm tình hình nặng thêm. Nếu readiness quá lạc quan, gateway gửi request tới instance chưa khởi tạo xong và sinh 5xx trong rollout. Health check cũng cần ngưỡng: một lần timeout không nên loại ngay endpoint vì packet loss thoáng qua; ba lần thất bại liên tiếp phản ứng chậm hơn nhưng tránh flapping.\n\nHealth check không chứng minh toàn bộ nghiệp vụ khỏe. Endpoint `/ready` trả 200 trong khi thread pool đã đầy vẫn là false positive. Gateway nên bổ sung passive health: quan sát timeout, connection reset và tỷ lệ 5xx thực, tạm eject endpoint bất thường rồi thăm dò lại.\n\n### Client-side và server-side discovery\n\nClient-side discovery nghĩa là caller tự lấy danh sách endpoint và chọn đích. Một service nội bộ gọi service khác không cần hop qua proxy trung tâm, giữ latency thấp và có thể chọn cùng zone. Nhưng thư viện discovery, cân bằng tải, retry, circuit breaker phải được triển khai nhất quán cho Java, Go, Node.js; nâng chính sách cần nâng nhiều ứng dụng.\n\nServer-side discovery đặt quyết định ở load balancer, gateway hoặc sidecar. Client chỉ gọi tên ổn định; proxy theo dõi endpoint. Điều này làm client đơn giản và quản trị tập trung, đổi lại thêm proxy, tài nguyên và một hop mạng. Service mesh thường dùng sidecar hoặc proxy cấp nút để tránh một proxy trung tâm, nhưng tăng độ phức tạp quan sát và vận hành.\n\nKhông có lựa chọn tuyệt đối. Lớp Mở dùng server-side discovery tại gateway vì mobile không được nhìn topology nội bộ; giữa các service có thể dùng DNS/Service của orchestrator. Một đội chỉ có Go và yêu cầu latency cực thấp có thể chấp nhận client-side library chung.\n\n### Route, phiên bản và triển khai canary\n\nGateway khớp method, host, path và đôi khi header. Route cụ thể phải ưu tiên hơn route rộng: `/v1/courses/search` không được bị hiểu thành `{course_id} = search`. Sau khi chọn service, gateway có thể chia 5% request có header nội bộ sang `upload-service:v2` để canary.\n\nCanary theo tỷ lệ thuần có một ca biên: cùng người dùng có thể lúc vào v1, lúc vào v2, gây rối nếu hai phiên bản tạo trạng thái không tương thích. Hash ổn định trên tenant hoặc user giữ một nhóm ở cùng phiên bản. Dù vậy rollback chỉ an toàn nếu schema và message tương thích ngược; gateway không thể cứu một migration phá dữ liệu.\n\nĐừng dùng route gateway làm hệ thống feature flag nghiệp vụ. Quy tắc “trường A được upload 4K nếu đã mua gói” cần dữ liệu miền và audit, nên nằm ở service hoặc hệ thống flag chuyên dụng. Gateway phù hợp với phân luồng thô dựa trên thuộc tính đã xác minh.\n\n### Xác thực ở biên, ủy quyền ở miền\n\nGateway có thể xác minh token hợp lệ, issuer, audience, thời hạn rồi truyền identity đã chuẩn hóa. Việc này tránh mọi service lặp parsing và giảm request rác. Nhưng quyết định “giảng viên này có quyền sửa khóa c42 không?” thuộc `course-service`, vì chỉ miền nghiệp vụ biết quan hệ sở hữu hiện tại.\n\nChỉ tin header identity nếu gateway xóa header cùng tên do client gửi và mạng nội bộ xác thực gateway. Nếu service công khai vô tình nhận `X-User-Id: admin` trực tiếp, việc xác thực tập trung biến thành lỗ hổng. Giữa gateway và service có thể dùng mTLS hoặc token nội bộ có audience hẹp.\n\n### Timeout, retry và ngân sách thời gian\n\nClient đặt deadline 3 giây không có nghĩa gateway được dùng đủ 3 giây cho mỗi lần thử. Trừ thời gian TLS, queue và trả phản hồi, gateway có thể chỉ dành 2,4 giây cho upstream. Nếu lần đầu timeout sau 2 giây rồi retry thêm 2 giây, lời hứa ngoài đã vỡ.\n\nRetry tại nhiều tầng tạo khuếch đại. Client thử 3 lần, gateway thử 3 lần, service lại thử database 3 lần: một thao tác có thể gây 27 lần chạm database. Chỉ một tầng nên sở hữu retry cho một cạnh gọi; dùng backoff có jitter và giới hạn tổng deadline. `POST` tạo upload cần idempotency key, nếu không lần đầu đã thành công nhưng phản hồi mất có thể tạo hai phiên.\n\nGateway cũng không nên retry khi đã bắt đầu stream response, vì không thể thu hồi byte client đã nhận. Với upload lớn, đây là thêm một lý do để data path tránh gateway.\n\n### Rate limiting và backpressure\n\nGiới hạn 100 request/phút theo IP dễ chặn cả trường học dùng chung NAT và lại không chặn bot đổi IP. Lớp Mở định danh quota theo tenant sau xác thực, kèm một ngưỡng IP trước xác thực để bảo vệ parser. Token bucket cho phép burst ngắn nhưng giữ tốc độ trung bình; quota phân tán cần quyết định mức nhất quán.\n\nCounter cục bộ trên 20 gateway cho throughput cao nhưng quota 100 có thể thành gần 2.000 trong trường hợp xấu. Counter tập trung chính xác hơn nhưng thêm network call trên đường nóng. Có thể cấp từng “lô token” cho gateway: giảm số lần gọi kho trung tâm, đổi lại cho phép vượt nhẹ khi gateway chết còn token chưa dùng.\n\nKhi upstream quá tải, queue vô hạn chỉ biến lỗi nhanh thành timeout chậm và ăn hết bộ nhớ. Gateway nên giới hạn số request chờ, trả 429 cho quota hoặc 503 cho quá tải, kèm `Retry-After` khi có thể. Load shedding giữ phần request còn lại có cơ hội hoàn thành.\n\n### Gateway failure và cách thu hẹp blast radius\n\nVì mọi client đi qua gateway, lỗi cấu hình route có thể tác động rộng hơn lỗi một service. Do đó gateway phải stateless khi có thể, chạy nhiều replica qua nhiều zone, giới hạn cấu hình bằng schema và canary chính bản cấu hình. Dashboard cần tách latency gateway tự tiêu tốn khỏi latency upstream.\n\nRegistry cũng cần quorum và snapshot, nhưng đừng nhầm “registry highly available” với “request chắc chắn thành công”. Network partition có thể làm hai phía thấy danh sách khác nhau. Data plane nên ưu tiên tiếp tục với endpoint đã biết, loại cục bộ endpoint thực sự lỗi, và chấp nhận topology cũ trong khoảng hữu hạn.\n\nKhi nào không nên có gateway? Monolith có một public API, hai người vận hành và lưu lượng nhỏ thường chỉ cần reverse proxy/load balancer. Thêm gateway lúc đó tạo thêm cấu hình, chứng thư, log và điểm lỗi mà không giải quyết biến động topology hay chính sách trùng lặp nào.",

vi_du: "### Bộ mô phỏng discovery có lease và chọn cùng zone\n\nĐoạn Python sau chạy độc lập. Nó không giả làm registry production; mục tiêu là làm lộ ba trạng thái: endpoint còn lease, endpoint sẵn sàng, và lựa chọn ưu tiên cùng zone. Đồng hồ được truyền vào để test không phải chờ thật.\n\n```python\nfrom dataclasses import dataclass\n\n@dataclass\nclass Endpoint:\n    address: str\n    zone: str\n    lease_until: float\n    ready: bool = True\n\nclass Registry:\n    def __init__(self):\n        self.services = {}\n\n    def register(self, service, endpoint):\n        self.services.setdefault(service, {})[endpoint.address] = endpoint\n\n    def discover(self, service, now, caller_zone):\n        living = [e for e in self.services.get(service, {}).values()\n                  if e.ready and e.lease_until > now]\n        local = [e for e in living if e.zone == caller_zone]\n        return sorted(local or living, key=lambda e: e.address)\n\nr = Registry()\nr.register(\"upload\", Endpoint(\"10.0.1.7:8080\", \"a\", 130))\nr.register(\"upload\", Endpoint(\"10.0.2.4:8080\", \"b\", 200))\nr.register(\"upload\", Endpoint(\"10.0.1.9:8080\", \"a\", 200, ready=False))\n\nassert [e.address for e in r.discover(\"upload\", 100, \"a\")] == [\"10.0.1.7:8080\"]\nassert [e.address for e in r.discover(\"upload\", 150, \"a\")] == [\"10.0.2.4:8080\"]\nassert r.discover(\"missing\", 150, \"a\") == []\nprint(\"discovery xử lý đúng endpoint hết lease, chưa ready và service rỗng\")\n```\n\nỞ thời điểm 100, endpoint zone a được ưu tiên. Tới 150, lease của nó đã hết; endpoint zone b là lựa chọn còn lại. Endpoint `10.0.1.9` không được nhận lưu lượng dù lease còn hạn vì readiness false. Trong hệ thật còn cần watch, version, cân bằng tải và passive health.\n\n### Handler gateway giữ data path ra ngoài\n\nMã giả gần với JavaScript dưới đây cho thấy ranh giới đúng: gateway tạo phiên và trả URL tải thẳng, không nhận body video.\n\n```javascript\nasync function createUpload(req, deps) {\n  const identity = await deps.verifyToken(req.headers.authorization);\n  const courseId = req.params.courseId;\n  const key = req.headers[\"idempotency-key\"];\n  if (!key) return { status: 400, body: { error: \"missing idempotency key\" } };\n\n  const endpoint = deps.discovery.pick(\"upload-service\", req.zone);\n  if (!endpoint) return { status: 503, body: { error: \"upload unavailable\" } };\n\n  return deps.http.post(endpoint + \"/sessions\", {\n    deadlineMs: Math.min(req.remainingMs - 100, 2000),\n    headers: { \"x-user-id\": identity.userId, \"idempotency-key\": key },\n    json: { courseId }\n  });\n}\n```\n\nCa biên `remainingMs ≤ 100` cần bị từ chối sớm thay vì truyền deadline âm. Production code cũng phải xóa header identity từ client trước khi tự đặt lại. Với request tạo phiên, retry chỉ được bật khi idempotency key được service lưu cùng kết quả.\n\n### Tính nhanh sức chứa mà không biến nó thành benchmark\n\nNếu một gateway process đã được load test trong chính môi trường triển khai và chịu 1.200 request/s tại mức CPU mục tiêu, lưu lượng đỉnh 8.000 request/s cần tối thiểu `ceil(8000 / 1200) = 7` process về throughput. Chạy 7 là không có chỗ cho một process chết hay lệch tải. Dành 40% headroom cho burst và rollout cho kết quả `ceil(8000 × 1,4 / 1200) = 10`.\n\nĐây là phép lập kế hoạch, không phải số phổ quát của gateway. TLS, logging, kích thước token và plugin có thể đổi năng lực nhiều lần; phải đo binary, cấu hình và payload thật. Đồng thời kiểm tra bandwidth: 8.000 phản hồi metadata 20 KB/s tương đương khoảng 160 MB/s đầu ra trước overhead, có thể chạm giới hạn mạng trước CPU.",

so_sanh: "### Chọn kiến trúc theo chi phí đang có\n\n| Phương án | Điểm mạnh | Giá phải trả | Hợp với Lớp Mở khi nào |\n|---|---|---|---|\n| Client gọi thẳng service | Ít hop, ít hạ tầng | Lộ topology, client gánh auth và discovery | Chỉ cho vài caller nội bộ được kiểm soát |\n| API gateway + server-side discovery | Client đơn giản, chính sách biên thống nhất | Thêm latency, blast radius và đội vận hành | Public API có web/mobile/đối tác |\n| Client-side discovery | Caller chọn endpoint trực tiếp, dễ ưu tiên zone | SDK phức tạp và lệch hành vi giữa ngôn ngữ | Nội bộ đồng nhất stack, rất nhạy latency |\n| Service mesh | Chính sách east-west ngoài code ứng dụng | Proxy, telemetry và debug phức tạp | Nhiều service, đội platform đủ năng lực |\n\n### Gateway không thay thế mọi lớp khác\n\n| Nhu cầu | Thành phần chịu trách nhiệm chính | Vì sao không đẩy hết vào gateway |\n|---|---|---|\n| Chia lưu lượng giữa replica tương đương | Load balancer | Không cần hiểu nghiệp vụ |\n| Tìm endpoint đang sống | Service discovery | Topology thay đổi thuộc control plane |\n| Quyền sửa khóa học | `course-service` | Cần trạng thái miền mới nhất |\n| Chống request công khai quá mức | Gateway | Có danh tính và quota ở biên |\n| Truyền file 20 GB | Client ↔ blob storage | Tránh nhân đôi bandwidth và timeout dài |\n\nMột “gateway thông minh, service ngu” nghe hấp dẫn nhưng khiến cửa ngõ sở hữu nghiệp vụ của mọi đội. Hướng bền hơn là gateway đủ thông minh về giao thức và bảo vệ biên, còn service làm chủ quy tắc miền.",

loi_thuong_gap: [
  "**Tra registry trên từng request.** Nhận biết qua QPS registry xấp xỉ QPS API và latency tăng khi registry chậm; sửa bằng watch/cache có version và đường lui dùng snapshot cuối.",
  "**Coi liveness là readiness.** Dấu hiệu là rollout làm pod restart dây chuyền khi dependency chậm; tách “cần restart” khỏi “tạm ngừng nhận request”.",
  "**Retry ở mọi tầng.** Một sự cố database nhỏ tạo bão request, QPS phía sau tăng khi tỷ lệ thành công giảm; quy định một chủ sở hữu retry, tổng deadline và idempotency.",
  "**Đưa video lớn qua gateway.** Bandwidth vào và ra cùng tăng, connection sống hàng phút, autoscale không cứu được egress; chỉ để gateway cấp signed URL và metadata.",
  "**Tin header danh tính do client gửi.** Có thể nhận biết bằng test gọi thẳng service với `X-User-Id`; gateway phải xóa header giả và service chỉ tin kênh đã xác thực.",
  "**Nhét ủy quyền nghiệp vụ vào route.** Cấu hình gateway phải truy vấn database miền hoặc deploy mỗi khi gói sản phẩm đổi; giữ quyết định sở hữu tài nguyên trong service.",
  "**Dùng TTL dài mà không passive health.** Sau khi pod chết vẫn thấy timeout đúng bằng TTL; eject endpoint theo lỗi thực và đồng bộ lại với registry."
],

tu_kiem_tra: [
  "Vẽ đường đi của request tạo phiên upload và chỉ rõ chỗ nào thuộc control plane, chỗ nào thuộc data plane.",
  "Vì sao registry mất kết nối 5 giây không nhất thiết làm API dừng? Snapshot cũ gây rủi ro nào và giới hạn rủi ro đó ra sao?",
  "Client timeout 3 giây; gateway có hai lần thử upstream. Hãy phân bổ deadline và giải thích vì sao hai lần 2 giây là sai.",
  "Nếu có 20 gateway và quota tenant là 100 request/phút, counter cục bộ có thể vượt bao nhiêu? Nêu một thiết kế trung gian giữa local và centralized.",
  "Tại sao xác thực token hợp với gateway nhưng quyền sửa khóa học không nên nằm ở đó?",
  "Một endpoint `/ready` luôn trả 200 nhưng thread pool đầy. Passive health quan sát tín hiệu nào để bù điểm mù này?",
  "Hệ thống monolith có 500 request/s và một mobile client. Hãy bảo vệ quyết định không dùng API gateway riêng, đồng thời nêu dấu hiệu khiến quyết định cần xem lại."
],

tai_nguyen: [
  "“Designing Data-Intensive Applications” — Martin Kleppmann, các phần về hệ phân tán, lỗi từng phần và service discovery.",
  "“Site Reliability Engineering” — Betsy Beyer và cộng sự, các chương về load balancing, handling overload và cascading failures.",
  "“Building Microservices” — Sam Newman, các phần về giao tiếp service, gateway và phân rã hệ thống.",
  "“Release It!” — Michael T. Nygard, các stability pattern như timeout, circuit breaker và bulkhead."
],

lien_he: [
  "Nối sang mảng Mạng máy tính: DNS, TLS, HTTP và reverse proxy là nền vật lý/giao thức mà gateway và discovery dựa vào; hiểu cache DNS giúp giải thích độ cũ endpoint.",
  "Nối sang mảng Bảo mật: gateway là điểm thực thi authentication, rate limiting và mTLS, nhưng authorization cấp tài nguyên vẫn cần nguyên tắc least privilege trong service.",
  "Nối sang mảng Cơ sở dữ liệu: registry ưu tiên availability và dữ liệu có thể hơi cũ, trong khi idempotency key cần transaction bền vững để không tạo hai phiên upload.",
  "Nối sang mảng Quan sát hệ thống: trace-id phải xuyên gateway và upstream để tách latency hàng đợi, network, discovery và xử lý nghiệp vụ."
]

};
