window.LESSONS["05:framework-xay-api-that:fastapi-routing-dependency-injection-async"] = {

tom_tat: [
  "Một route FastAPI không chỉ ghép URL với một hàm: framework đối chiếu method và path, giải tham số, chạy cây dependency, gọi endpoint rồi tuần tự hóa response.",
  "Dependency injection của FastAPI phù hợp với tài nguyên theo request như người dùng hiện tại, database session và policy; nó không thay thế việc tách business logic thành module độc lập.",
  "`async def` chỉ giúp một worker phục vụ request khác trong lúc chờ I/O bất đồng bộ. Đặt lời gọi blocking bên trong sẽ khóa event loop và làm toàn bộ worker đứng lại.",
  "API đơn hàng xuyên suốt dùng `APIRouter`, dependency xác thực Bearer token, service giả lập và các status code có chủ đích; code có thể chạy bằng Uvicorn.",
  "Bảo mật bắt đầu trước endpoint: không tin header do client tự khai, giới hạn quyền theo tài nguyên, không đưa secret vào log và không để route tĩnh bị route động nuốt mất."
],

can_biet_truoc: [
  "HTTP method, path, header, body và ý nghĩa các status code 200, 201, 401, 403, 404.",
  "Python type hint, hàm thường, coroutine, `await` và context manager ở mức nhập môn.",
  "JSON object/array và sự khác nhau giữa dữ liệu đầu vào với đối tượng Python.",
  "Khái niệm I/O-bound so với CPU-bound; chưa cần biết chi tiết ASGI."
],

dinh_nghia: "**FastAPI** là web framework Python dựa trên chuẩn ASGI. **Routing** là phép ánh xạ một cặp HTTP method–path tới endpoint cụ thể. **Dependency injection** là cơ chế để FastAPI tự tạo và truyền các giá trị mà endpoint khai báo qua `Depends`, đồng thời quản lý thứ tự và vòng đời của chúng. **Async** trong ngữ cảnh này là cooperative concurrency: coroutine chủ động nhường event loop tại `await` để worker xử lý công việc khác trong lúc chờ I/O.\n\nBa khái niệm liên quan nhưng không đồng nhất. Route quyết định code nào nhận request; dependency chuẩn bị ngữ cảnh dùng chung; `async` quyết định cách worker chờ. Một endpoint có thể là `def` nhưng vẫn dùng dependency, hoặc là `async def` mà chẳng tạo thêm concurrency nếu bên trong chỉ tính CPU.",

vi_sao_can: "Một API demo thường đặt mọi thứ trong một hàm: đọc token, kiểm tra dữ liệu, gọi database và trả dictionary. Cách đó chạy ở happy path nhưng nhanh chóng gây ba vấn đề. Thứ nhất, authorization bị sao chép giữa route nên một endpoint mới dễ quên kiểm tra quyền. Thứ hai, test phải dựng cả framework dù chỉ muốn kiểm tra quy tắc “không bán quá tồn kho”. Thứ ba, một lời gọi thư viện blocking lọt vào `async def` có thể biến lợi thế concurrency thành nghẽn cổ chai khó thấy.\n\nAPI đơn hàng còn có ranh giới bảo mật rõ: khách A không được đọc đơn của khách B; token hết hạn phải bị từ chối trước business logic; `/orders/summary` không được bị hiểu là `order_id=\"summary\"`; lỗi nội bộ không được lộ stack trace. Routing và dependency giúp diễn đạt các ranh giới đó ở một chỗ. Async giúp dùng thời gian chờ database hoặc dịch vụ thanh toán hiệu quả hơn, nhưng chỉ khi toàn bộ đường I/O thực sự bất đồng bộ.\n\nĐiều cần tránh là chọn FastAPI chỉ vì “nhanh”. Framework giảm boilerplate và sinh OpenAPI tốt, nhưng không tự giải quyết transaction, idempotency, rate limit hay thiết kế domain. Những trách nhiệm ấy vẫn phải được mô hình hóa rõ.",

co_che: "### Một request đi qua ứng dụng như thế nào\n\nGiả sử client gửi `POST /orders` kèm JSON và header `Authorization`. ASGI server nhận byte từ socket, tạo scope chứa method, path và header rồi gọi ứng dụng FastAPI. Router duyệt bảng route để tìm cả method lẫn mẫu path phù hợp. Sau đó FastAPI đọc chữ ký endpoint, dựng đồ thị dependency, lấy dữ liệu từ path/query/header/body, rồi mới gọi hàm endpoint. Giá trị trả về tiếp tục đi qua response model, JSON encoder và middleware trước khi thành byte trên mạng.\n\nĐiểm quan trọng là endpoint không phải cửa đầu tiên. Nếu body sai schema, dependency xác thực thất bại hoặc route không tồn tại, endpoint chưa từng chạy. Vì thế log “đã vào hàm create_order” không đủ để điều tra mọi response 4xx.\n\n### Routing: method cũng là một phần của danh tính\n\n`GET /orders/42` và `DELETE /orders/42` dùng cùng path nhưng là hai operation khác. Route động như `/orders/{order_id}` còn có thể xung đột về ý nghĩa với `/orders/summary`. Khai route tĩnh trước route động là cách phòng vệ dễ hiểu; tốt hơn nữa là dùng kiểu path rõ như UUID hoặc số nguyên để `summary` không hợp lệ ngay ở bước parse.\n\nPrefix và tag thuộc `APIRouter` giúp chia ứng dụng theo capability. Chúng không phải lớp business. File `orders_router.py` nên biết HTTP, còn `OrderService` nên biết quy tắc đơn hàng nhưng không biết `HTTPException`.\n\n### Ghép router mà không tạo global hỗn loạn\n\nỨng dụng lớn thường có một composition root: nơi khởi tạo `FastAPI`, gắn middleware và `include_router`. Router con khai prefix `/orders`; endpoint chỉ khai phần còn lại. Cách này tránh lặp chuỗi URL và cho phép test router độc lập. Tuy nhiên, đừng tạo một router cho mỗi hàm: quá nhiều mảnh nhỏ làm người đọc khó thấy contract tổng thể.\n\n```python\nfrom fastapi import APIRouter, FastAPI\n\norders = APIRouter(prefix=\"/orders\", tags=[\"orders\"])\n\n@orders.get(\"/{order_id}\")\nasync def get_order(order_id: int):\n    return {\"id\": order_id, \"status\": \"pending\"}\n\napp = FastAPI(title=\"Cua hang nho\")\napp.include_router(orders)\n```\n\n### Dependency là một đồ thị, không phải danh sách callback\n\nMột dependency có thể phụ thuộc dependency khác. `require_customer` cần `read_token`; `get_order_service` có thể cần database session. FastAPI giải các nút theo quan hệ phụ thuộc và cache kết quả trong phạm vi một request theo mặc định. Nếu hai tham số cùng cần `current_user`, việc giải token không nhất thiết chạy hai lần.\n\nCaching theo request hữu ích cho session và identity, nhưng có đánh đổi: dependency có side effect không nên dựa vào số lần gọi ngầm. Nếu thực sự cần chạy lại, có thể tắt cache tại điểm dùng, song đó thường là tín hiệu thiết kế cần xem lại.\n\n### `yield` và vòng đời tài nguyên\n\nDependency dùng `yield` có hai nửa. Phần trước `yield` cấp tài nguyên; giá trị được truyền xuống endpoint; phần sau thu hồi tài nguyên kể cả khi endpoint ném exception. Đây là nơi phù hợp để mở/đóng database session. Nó giống context manager theo phạm vi request.\n\nKhông nên đặt transaction commit tự động cho mọi request một cách mù quáng. Một `GET` không cần commit; một use case ghi nhiều bảng phải quyết định ranh giới transaction ở service. Cleanup đóng session là trách nhiệm hạ tầng, còn commit hay rollback là quyết định nghiệp vụ có chủ đích.\n\n### Authentication không đồng nghĩa authorization\n\nDependency đọc Bearer token trả lời “ai đang gọi”. Authorization trả lời “người đó có quyền làm việc này không”. Chỉ kiểm token hợp lệ rồi cho phép `GET /orders/{id}` là lỗi IDOR: kẻ tấn công đổi ID để xem đơn người khác. Endpoint hoặc policy dependency phải đối chiếu `order.customer_id` với subject trong token.\n\nHeader như `X-User-Id` chỉ đáng tin nếu một reverse proxy được kiểm soát đã xóa header từ Internet và tự gắn lại. Nếu ứng dụng public đọc thẳng nó, client có thể tự nhận bất kỳ danh tính nào. Bài ví dụ dùng token giả để minh họa luồng, không phải cơ chế production.\n\n### Event loop và ý nghĩa thật của `await`\n\nEvent loop chạy một coroutine cho tới khi coroutine gặp `await` trên thao tác chưa hoàn tất. Lúc đó nó giữ trạng thái coroutine, chuyển sang request khác, rồi quay lại khi I/O sẵn sàng. Nó không tạo CPU thứ hai. Nếu code gọi `time.sleep(2)` hoặc thư viện HTTP đồng bộ, coroutine không nhường quyền và các request khác trên cùng event loop phải chờ.\n\n`await asyncio.sleep(2)` chỉ là mô phỏng I/O. Trong hệ thật, driver database và HTTP client cũng phải có API async. Một chuỗi chỉ async ở lớp ngoài nhưng blocking ở đáy là “async giả”.\n\n### Chọn `def` hay `async def`\n\nNếu thư viện được gọi là bất đồng bộ, dùng `async def` và `await`. Nếu công việc dùng thư viện blocking, endpoint `def` cho phép FastAPI chạy nó trong thread pool, tránh khóa event loop; cách này vẫn tiêu tốn thread và không biến thư viện thành scalable vô hạn. Với CPU-bound như nén ảnh lớn, thread pool còn chịu giới hạn CPU/GIL; nên chuyển sang worker process hoặc job queue.\n\n| Công việc trong endpoint | Cách phù hợp | Lý do |\n|---|---|---|\n| Chờ async database/HTTP | `async def` + `await` | nhường event loop khi socket chưa sẵn sàng |\n| Gọi SDK chỉ có blocking API | `def` hoặc đưa sang thread có kiểm soát | không chặn event loop chính |\n| Tính toán CPU kéo dài | hàng đợi và process worker | request worker không nên giữ CPU lâu |\n| Logic nhỏ, không I/O | `def` hay `async def` đều được | chênh lệch không đáng để làm code khó hiểu |\n\n### Hủy request, timeout và backpressure\n\nClient rời đi không có nghĩa mọi side effect đã tự hoàn tác. Coroutine có thể bị hủy tại một điểm `await`, nhưng database đã commit hoặc lời gọi thanh toán đã tới hệ ngoài. Do đó thao tác tạo đơn cần idempotency key và transaction, chứ không trông chờ cancellation.\n\nTimeout phải tồn tại ở client lẫn lời gọi ra ngoài. Không timeout khiến coroutine treo và giữ connection pool. Khi tải vượt khả năng, hệ thống cần giới hạn connection, queue hoặc rate limit để thất bại sớm; nhận vô hạn request rồi chờ vô hạn chỉ dời sự cố sang bộ nhớ.\n\n### OpenAPI là hệ quả của contract đã khai báo\n\nFastAPI dùng type hint, tham số và model để sinh schema. Đây là lợi ích lớn: tài liệu và validation cùng xuất phát từ code. Nhưng OpenAPI chỉ đúng nếu response model đúng và route không trả dữ liệu tùy tiện. Đừng xem trang docs là bằng chứng authorization đúng; nó mô tả hình dạng request, không chứng minh policy.\n\n### Khi nào không nên chọn FastAPI\n\nKhông nên đổi framework chỉ để chạy theo benchmark khi đội ngũ đã vận hành tốt Django hoặc Flask và bottleneck nằm ở database. FastAPI cũng không tự nhiên phù hợp cho batch CPU nặng, ứng dụng cần admin/CMS hoàn chỉnh ngay, hay dự án mà toàn bộ SDK phụ thuộc đều blocking. Chi phí migration, quan sát production và đào tạo thường lớn hơn vài microsecond dispatch route.",

vi_du: "### API đơn hàng tối thiểu có thể chạy\n\nLưu đoạn sau thành `main.py`, cài `fastapi` và `uvicorn`, rồi chạy `uvicorn main:app --reload`. Kho dữ liệu trong RAM chỉ phục vụ học routing và dependency; nhiều process sẽ không chia sẻ dictionary này.\n\n```python\nimport asyncio\nfrom dataclasses import dataclass\nfrom typing import Annotated\n\nfrom fastapi import Depends, FastAPI, Header, HTTPException, status\n\napp = FastAPI()\nORDERS = {1: {\"id\": 1, \"customer_id\": \"an\", \"status\": \"pending\"}}\n\n@dataclass(frozen=True)\nclass Principal:\n    customer_id: str\n\nasync def current_customer(\n    authorization: Annotated[str | None, Header()] = None,\n) -> Principal:\n    if authorization != \"Bearer demo-an\":\n        raise HTTPException(\n            status_code=status.HTTP_401_UNAUTHORIZED,\n            detail=\"token không hợp lệ\",\n            headers={\"WWW-Authenticate\": \"Bearer\"},\n        )\n    return Principal(customer_id=\"an\")\n\nCurrentCustomer = Annotated[Principal, Depends(current_customer)]\n\n@app.get(\"/orders/summary\")\nasync def summary(user: CurrentCustomer):\n    own = [o for o in ORDERS.values() if o[\"customer_id\"] == user.customer_id]\n    return {\"customer_id\": user.customer_id, \"total\": len(own)}\n\n@app.get(\"/orders/{order_id}\")\nasync def get_order(order_id: int, user: CurrentCustomer):\n    await asyncio.sleep(0.01)  # mô phỏng driver database bất đồng bộ\n    order = ORDERS.get(order_id)\n    if order is None:\n        raise HTTPException(status_code=404, detail=\"không tìm thấy đơn\")\n    if order[\"customer_id\"] != user.customer_id:\n        # 404 tránh tiết lộ rằng tài nguyên của người khác tồn tại\n        raise HTTPException(status_code=404, detail=\"không tìm thấy đơn\")\n    return order\n```\n\nKiểm tra bằng hai lệnh:\n\n```bash\ncurl -i http://127.0.0.1:8000/orders/summary -H \"Authorization: Bearer demo-an\"\ncurl -i http://127.0.0.1:8000/orders/1 -H \"Authorization: Bearer sai\"\n```\n\nRequest đầu nhận 200 và tổng bằng 1. Request sau nhận 401 kèm `WWW-Authenticate`; endpoint không chạy. Hãy thêm đơn của `customer_id=\"binh\"`, vẫn dùng token của An và gọi ID đó: response nên là 404. Đây là test authorization theo ownership, khác với test token.\n\n### Test dependency mà không giả token thật\n\nFastAPI cho phép override dependency khi test. Ta thay đúng nút identity, không tắt toàn bộ security middleware:\n\n```python\nfrom fastapi.testclient import TestClient\n\ndef fake_customer():\n    return Principal(customer_id=\"an\")\n\napp.dependency_overrides[current_customer] = fake_customer\nclient = TestClient(app)\n\ndef test_an_doc_duoc_don_cua_minh():\n    response = client.get(\"/orders/1\")\n    assert response.status_code == 200\n    assert response.json()[\"customer_id\"] == \"an\"\n```\n\nSau test suite, cần xóa `app.dependency_overrides` để trạng thái không rò sang test khác. Với business rule phức tạp, test trực tiếp service vẫn nhanh và rõ hơn test qua HTTP.",

so_sanh: "### Dependency injection của FastAPI khác container DI truyền thống\n\n| Tiêu chí | `Depends` của FastAPI | Tự gọi helper | DI container tổng quát |\n|---|---|---|---|\n| Phạm vi mạnh nhất | dữ liệu/tài nguyên theo request | logic rất nhỏ, thuần | object graph toàn ứng dụng |\n| Tích hợp HTTP | trực tiếp với header, cookie, lỗi | phải tự truyền | phụ thuộc thư viện |\n| Dễ override trong test | có `dependency_overrides` | monkeypatch hoặc truyền tay | thường có binding riêng |\n| Rủi ro | business logic mắc vào framework | lặp kiểm tra, tham số dài | cấu hình “ma thuật”, khó lần dấu |\n\n`Depends` rất hợp cho adapter HTTP, nhưng truyền constructor tường minh thường tốt hơn trong domain. Nếu `OrderService` nhận repository qua constructor, ta có thể dùng service trong CLI, worker và test mà không cần request FastAPI.\n\n### Đồng bộ, bất đồng bộ và nhiều process\n\n| Mô hình | Tốt khi | Không chữa được |\n|---|---|---|\n| endpoint đồng bộ trong thread pool | SDK blocking, tải vừa phải | quá nhiều thread, CPU nặng |\n| một event loop với async I/O | nhiều kết nối chủ yếu chờ | lời gọi blocking, tính toán dài |\n| nhiều worker process | dùng nhiều lõi, cô lập lỗi | state trong RAM không nhất quán |\n\nAsync là lựa chọn về cách chờ, không phải huy chương kiến trúc. API ít tải với code đồng bộ rõ ràng thường đáng tin hơn hệ async trộn driver sai. Chỉ đổi khi profile cho thấy thời gian chờ và concurrency thực sự là vấn đề.",

loi_thuong_gap: [
  "Dùng `time.sleep`, `requests` hoặc driver database đồng bộ bên trong `async def`; nhận biết qua việc một request chậm kéo latency của nhiều endpoint không liên quan tăng cùng lúc.",
  "Chỉ xác thực token mà không kiểm ownership; thử đổi `order_id` bằng cùng token vẫn đọc được dữ liệu khách khác là bằng chứng lỗi IDOR.",
  "Tin `X-User-Id` do Internet gửi thẳng; kiểm tra bằng cách tự đặt header và thấy server nhận danh tính giả.",
  "Đặt route `/{order_id}` quá rộng trước `/summary`; gọi đường tĩnh nhận lỗi parse ID hoặc chạy nhầm endpoint.",
  "Giữ dictionary/cache mutable trong process và tưởng mọi worker cùng thấy; chạy hai worker rồi request luân phiên cho kết quả khác nhau.",
  "Đưa toàn bộ business rule vào dependency; dấu hiệu là test quy tắc đơn hàng phải tạo Request hoặc biết `Depends`.",
  "Không đặt timeout cho lời gọi ra ngoài; connection pool tăng dần, request ở trạng thái chờ và hệ thống hết tài nguyên khi upstream treo."
],

tu_kiem_tra: [
  "Vì sao endpoint chưa chắc chạy dù router đã tìm đúng method và path? Hãy kể ít nhất hai bước có thể chặn trước nó.",
  "Một `async def` gọi thư viện đồng bộ mất ba giây ảnh hưởng event loop ra sao, và sửa thế nào nếu thư viện chưa có bản async?",
  "Authentication thành công có đủ để trả đơn hàng theo ID không? Viết điều kiện authorization còn thiếu.",
  "Dependency dùng `yield` nên chịu trách nhiệm cleanup nào, và vì sao tự động commit mọi request có thể sai?",
  "Nếu chạy bốn worker Uvicorn, biến `ORDERS` trong ví dụ có những bốn bản. Điều đó phá hành vi nào?",
  "Câu khó: client hủy request ngay sau khi cổng thanh toán đã nhận lệnh nhưng trước khi API trả response. Async cancellation có ngăn thanh toán trùng khi client retry không? Cơ chế nào mới xử lý được?"
],

tai_nguyen: [
  "FastAPI Documentation — Tutorial, Dependencies và Async.",
  "Starlette Documentation — Routing, Middleware và ASGI.",
  "Python Documentation — `asyncio` và coroutines.",
  "Architecture Patterns with Python — Harry Percival và Bob Gregory."
],

lien_he: [
  "Nối sang mảng Bảo mật Web: dependency xác thực chỉ là điểm đầu; authorization theo ownership, rate limit và quản lý secret hoàn thiện biên bảo vệ.",
  "Nối sang mảng Database: session theo request, transaction và connection pool quyết định tính đúng đắn của route tạo đơn.",
  "Nối sang mảng Testing: dependency override tạo seam cho integration test, còn domain service nên được unit test không qua HTTP.",
  "Nối sang mảng Hệ điều hành: event loop, thread pool và process giải thích vì sao blocking call ảnh hưởng throughput khác nhau."
]

};
