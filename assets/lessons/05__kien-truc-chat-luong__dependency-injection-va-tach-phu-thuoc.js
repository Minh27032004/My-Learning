window.LESSONS["05:kien-truc-chat-luong:dependency-injection-va-tach-phu-thuoc"] = {

tom_tat: [
  "Dependency injection (tiêm phụ thuộc) nghĩa là đối tượng nhận cộng tác viên từ bên ngoài thay vì tự tạo chúng; nhờ đó policy không bị khóa vào database, clock hay dịch vụ email cụ thể.",
  "Dependency inversion là nguyên tắc về chiều phụ thuộc, còn FastAPI `Depends` là một cơ chế giải tham số ở biên HTTP; hai khái niệm liên quan nhưng không đồng nhất.",
  "Composition root là nơi duy nhất lắp object graph, quản lý vòng đời và chọn implementation theo cấu hình đã kiểm tra.",
  "DI tốt làm failure có thể tiêm vào test, nhưng DI quá mức tạo interface vô nghĩa, đồ thị khó đọc và lỗi chỉ xuất hiện lúc runtime.",
  "Đối tượng dùng chung phải được xem xét về thread safety, connection lifetime, secret và tenant isolation trước khi cache."
],

can_biet_truoc: [
  "Constructor, class, Protocol và duck typing trong Python.",
  "FastAPI `Depends`, request scope và hàm generator có `yield`.",
  "Khác biệt giữa cấu hình ứng dụng, secret và trạng thái nghiệp vụ.",
  "Unit test, fake, stub và mock ở mức nhập môn."
],

dinh_nghia: "**Dependency injection (DI, tiêm phụ thuộc)** là việc truyền một đối tượng cần dùng vào nơi tiêu thụ qua constructor, tham số hàm hoặc thuộc tính, thay vì để nơi tiêu thụ tự khởi tạo hay tìm nó trong biến toàn cục. **Dependency inversion** nói thêm rằng policy cấp cao phụ thuộc vào abstraction do nó cần, còn chi tiết hạ tầng hiện thực abstraction ấy. DI là kỹ thuật nối các phần; inversion là hình dạng kiến trúc mong muốn. Một container có thể tự động nối object nhưng không tự đảm bảo thiết kế đúng.",

vi_sao_can: "Trong API workshop, `BookingService` cần kho booking, đồng hồ và nơi phát sự kiện. Nếu constructor của service gọi thẳng `PostgresRepository(os.getenv(...))`, `datetime.now()` và SMTP client, mọi test sẽ phụ thuộc mạng, thời gian thật và secret. Thay PostgreSQL bằng API đối tác buộc sửa policy. Khó hơn nữa, đường lỗi “mail server timeout sau khi đã giữ vé” gần như không tái hiện ổn định.\n\nTiêm ba phụ thuộc khiến lựa chọn được dời về điểm khởi động. Test đưa fixed clock và fake publisher; production đưa adapter thật. Sự tách này đáng giá vì **quyết định đặt vé thay đổi vì nghiệp vụ**, còn driver database thay đổi vì vận hành. Nhưng nếu tạo `StringFormatterInterface` cho một phép nối chuỗi thuần, ta chỉ đổi code trực tiếp thành nghi lễ. Ranh giới nên bám vào I/O, nguồn bất định, side effect hoặc policy có nhiều chiến lược.",

co_che: "### Từ `new` ẩn sang constructor rõ ràng\n\nHãy đọc signature như danh sách điều kiện để service hoạt động. `BookingService(repo, clock, events)` nói ngay nó cần lưu dữ liệu, biết thời điểm và công bố sự kiện. Nếu service tự tạo cả ba, dependency vẫn tồn tại nhưng bị giấu. Constructor injection ưu tiên vì object không thể tồn tại ở trạng thái thiếu cộng tác viên và type checker nhìn thấy quan hệ.\n\nParameter injection phù hợp cho giá trị thay theo lần gọi, như actor hoặc request id. Property injection dễ tạo object “nửa sống”: quên gán thuộc tính chỉ nổ ở nhánh hiếm. Service locator còn tệ hơn vì hàm trông không có dependency nhưng âm thầm truy cập registry toàn cục.\n\n### Protocol thuộc về phía tiêu thụ\n\nService chỉ cần ba thao tác nhỏ, không cần biết toàn bộ SDK. Một `BookingRepository` Protocol nên được đặt gần use case và mô tả chính xác semantic: operation có idempotent không, khi thiếu trả gì, lỗi nào có ý nghĩa. Adapter PostgreSQL tuân theo contract đó. Đây là inversion: chi tiết hướng vào nhu cầu của policy.\n\nKhông nhất thiết tạo abstract base class. Python structural typing cho phép fake có đúng method được chấp nhận. Protocol giúp kiểm tĩnh và tài liệu hóa; runtime vẫn cần test contract. Nếu interface phình theo mọi khả năng của database, chiều abstraction đã bị đảo ngược: policy đang nhận ngôn ngữ hạ tầng.\n\n### Composition root lắp toàn bộ graph\n\nComposition root là đoạn gần `app = FastAPI()` nơi đọc settings, tạo engine, repository, publisher và service. Nó được phép biết concrete class. Phần còn lại không rải `if ENV == ...`. Khi cấu hình thiếu, ứng dụng nên fail fast lúc khởi động thay vì chờ request đầu tiên.\n\nObject graph cần quan sát được. Một factory nhỏ, khai báo tường minh thường dễ debug hơn container phản chiếu kỳ diệu. Container hữu ích khi graph lớn và lifecycle phức tạp, nhưng phải có validation khi startup và cách in dependency chain khi resolve thất bại.\n\n### FastAPI `Depends` giải dependency ở biên\n\nFastAPI nhìn signature, gọi dependency con, cache kết quả trong một request theo mặc định rồi truyền vào endpoint. Generator dependency với `yield` đặt phần trước yield như acquire và phần sau như cleanup. Đây là nơi đẹp để mở database session và đảm bảo đóng nó dù endpoint lỗi.\n\nKhông nên rải `Depends` vào domain service. Khi đó service chỉ chạy được dưới bộ giải của FastAPI và unit test phải giả framework. Endpoint hoặc provider chuyển object framework thành constructor injection thuần. `Depends` là adapter, không phải kiến trúc toàn hệ thống.\n\n### Vòng đời: singleton, application và request\n\nSettings bất biến, HTTP connection pool và engine thường sống suốt process. Database session/transaction thường sống theo request hoặc use case. Actor chắc chắn thuộc request. Ghép sai lifetime gây hai nhóm lỗi: tạo client mỗi request làm cạn socket; hoặc dùng chung session không thread-safe khiến dữ liệu request này lẫn request khác.\n\nNhiều worker là nhiều process, vì vậy “singleton” Python chỉ đơn nhất trong một process. In-memory repository hay rate limiter không còn toàn cục khi deploy bốn worker. Muốn trạng thái chia sẻ phải dùng kho ngoài hoặc thiết kế partition rõ.\n\n### Cleanup không được dựa vào happy path\n\nProvider session phải rollback khi exception và luôn close. Context manager hoặc dependency `yield` biểu đạt cấu trúc này tốt hơn gọi close ở cuối endpoint, vì dòng cuối không chạy khi lỗi. Cleanup cũng có thể thất bại; log lỗi đóng kết nối nhưng đừng che exception gốc.\n\nVới streaming response, thời điểm cleanup có thể khác response thường. Nếu stream cần session sau khi endpoint trả, đóng sớm sẽ nổ giữa luồng. Giải pháp thường là đọc dữ liệu cần thiết trước, hoặc quản lý lifetime của stream riêng thay vì giữ transaction dài.\n\n### Clock và ID cũng là dependency\n\n`datetime.now()` và `uuid4()` tưởng vô hại nhưng làm test phụ thuộc thời điểm. Inject `Clock.now()` cho quy tắc hạn đăng ký giúp test đúng khoảnh khắc trước, bằng và sau deadline. ID generator cố định làm snapshot dễ đoán. Không cần inject mọi hàm thuần; inject nguồn bất định khi giá trị ảnh hưởng quyết định.\n\nThời gian production phải timezone-aware và so trong một chuẩn nhất quán. Fake clock không sửa lỗi logic timezone; nó chỉ cho phép tạo ca biên lặp lại. Contract nên nói rõ `now()` trả UTC-aware datetime.\n\n### Fake, stub và mock không giống nhau\n\nFake repository có implementation đơn giản và state thật trong RAM. Stub trả câu trả lời được lập trình, phù hợp ép nhánh timeout. Mock kiểm tương tác, hữu ích khi “phải phát đúng một event” là contract. Lạm dụng mock từng lời gọi nội bộ làm test vỡ sau refactor dù hành vi không đổi.\n\nTest service nên khẳng định kết quả và state quan sát được. Contract test chạy cùng bộ ca trên fake và adapter production để phát hiện fake quá dễ dãi, chẳng hạn fake cho email phân biệt hoa thường trong khi unique index không cho.\n\n### Failure injection là lợi ích lớn nhất\n\nMột adapter `FailingEventPublisher` có thể ném timeout đúng lần gọi thứ nhất. Nhờ vậy ta chứng minh booking đã commit thì outbox còn sự kiện, hoặc toàn transaction rollback. Nếu dependency bị tạo sâu trong method, test phải monkeypatch tên import mong manh.\n\nChaos ở unit test không thay load test. Race condition cần nhiều transaction trên database thật. DI chỉ mở cánh cửa đưa failure có kiểm soát vào; độ trung thực phụ thuộc fake và cấp test.\n\n### Bảo mật của object graph\n\nKhông inject secret dạng chuỗi vào mọi service. Provider tạo client đã cấu hình, giữ secret ở mép hạ tầng và tránh `repr` làm lộ. Settings lấy từ nguồn tin cậy, validate allowlist cho host; đừng để query parameter chọn database URL hoặc adapter class.\n\nMulti-tenant cần đặc biệt cẩn thận. Cache repository theo tenant bằng dictionary không giới hạn vừa rò bộ nhớ vừa có nguy cơ dùng nhầm credential. Tenant id phải đến từ identity đã xác minh, được gắn vào request scope và adapter bắt buộc filter tenant ở mọi query.\n\n### Circular dependency báo hiệu thiết kế có vấn đề\n\nNếu BookingService cần NotificationService, rồi NotificationService lại cần BookingService, container có thể dùng lazy proxy để chạy nhưng mô hình vẫn rối. Tách event hoặc một policy nhỏ thường phá vòng tự nhiên. Vòng dependency khiến thứ tự khởi tạo, transaction và test khó hiểu.\n\nĐừng chữa vòng bằng import trong method như giải pháp cuối cùng; cách đó chỉ giấu chu kỳ type/module. Vẽ graph và hỏi trách nhiệm nào đang bị chia sai.\n\n### Khi DI không đáng dùng\n\nValue object, parser thuần, phép tính giá không I/O thường gọi trực tiếp dễ đọc hơn. Adapter chỉ có một dòng cũng chưa cần Protocol nếu không tạo ranh giới có giá trị. Manual DI phù hợp đa số ứng dụng vừa; framework container chỉ đáng dùng khi số lượng provider và scope đã thật sự khó quản lý.\n\nĐánh đổi cốt lõi là tính tường minh với độ linh hoạt. Mọi interface đều là một contract phải đặt tên, test và duy trì. Chỉ trả chi phí đó ở nơi biến động hoặc failure cần cô lập.",

vi_du: "### Ứng dụng có clock và repository được tiêm\n\n```python\nfrom dataclasses import dataclass\nfrom datetime import datetime, timezone\nfrom typing import Protocol\nfrom fastapi import Depends, FastAPI, Header\nfrom pydantic import BaseModel, EmailStr\n\nclass Clock(Protocol):\n    def now(self) -> datetime: ...\n\nclass BookingRepo(Protocol):\n    def add(self, workshop_id: str, email: str, created_at: datetime) -> str: ...\n\nclass SystemClock:\n    def now(self) -> datetime:\n        return datetime.now(timezone.utc)\n\nclass MemoryRepo:\n    def __init__(self) -> None:\n        self.rows: list[tuple[str, str, datetime]] = []\n    def add(self, workshop_id: str, email: str, created_at: datetime) -> str:\n        booking_id = f\"b-{len(self.rows) + 1}\"\n        self.rows.append((workshop_id, email, created_at))\n        return booking_id\n\n@dataclass\nclass BookingService:\n    repo: BookingRepo\n    clock: Clock\n    def reserve(self, workshop_id: str, email: str) -> str:\n        return self.repo.add(workshop_id, email.casefold(), self.clock.now())\n\napp = FastAPI()\nrepo = MemoryRepo()                 # composition root\nclock = SystemClock()\n\ndef get_service() -> BookingService:\n    return BookingService(repo, clock)\n\nclass Body(BaseModel):\n    email: EmailStr\n\n@app.post(\"/workshops/{workshop_id}/bookings\")\ndef reserve(workshop_id: str, body: Body,\n            service: BookingService = Depends(get_service)) -> dict[str, str]:\n    return {\"id\": service.reserve(workshop_id, str(body.email))}\n```\n\nChạy bằng `uvicorn main:app`. Ví dụ dùng singleton `MemoryRepo`, chỉ đúng cho demo một process; production phải inject request-scoped session vào repository.\n\n### Test deadline bằng fixed clock\n\n```python\nclass FixedClock:\n    def __init__(self, value: datetime) -> None:\n        self.value = value\n    def now(self) -> datetime:\n        return self.value\n\nfixed = FixedClock(datetime(2030, 1, 1, tzinfo=timezone.utc))\nfake = MemoryRepo()\nservice = BookingService(fake, fixed)\nassert service.reserve(\"ws-101\", \"Lan@Example.com\") == \"b-1\"\nassert fake.rows[0][1] == \"lan@example.com\"\nassert fake.rows[0][2] == fixed.value\n```\n\nĐây là kiểm tra deterministic: không sleep, không vá global clock. Ca khó tiếp theo là làm repository ném timeout và xác nhận service không tự retry operation ghi nếu chưa có idempotency key.\n\n### Session dependency có cleanup\n\n```python\ndef get_session():\n    session = SessionFactory()\n    try:\n        yield session\n        session.commit()\n    except Exception:\n        session.rollback()\n        raise\n    finally:\n        session.close()\n```\n\nMẫu này chỉ hợp nếu một request tương ứng một transaction. Streaming, nhiều use case trong request hoặc commit trước khi gọi dịch vụ ngoài cần ranh giới khác; không sao chép như công thức vạn năng.",

so_sanh: "### Các cách cung cấp dependency\n\n| Cách | Quan hệ có hiện trên signature? | Test | Rủi ro |\n|---|---:|---|---|\n| Tự `new` trong service | không | phải monkeypatch hoặc gọi thật | policy khóa vào chi tiết |\n| Biến global | không | state rò giữa test | race, thứ tự test |\n| Service locator | một phần | thay registry | dependency ẩn, lỗi runtime |\n| Constructor injection | có | truyền fake trực tiếp | constructor dài nếu class ôm quá nhiều việc |\n| FastAPI `Depends` ở endpoint | có tại biên | override provider | kéo framework vào lõi nếu lạm dụng |\n\nConstructor injection là mặc định tốt cho lõi. `Depends` giải lifecycle ở adapter HTTP. Global chỉ chấp nhận cho hằng bất biến hoặc object hạ tầng được lắp có kiểm soát tại composition root, không cho mutable business state.",

loi_thuong_gap: [
  "Gọi DI là dependency inversion; nhận biết khi đã truyền object nhưng service vẫn type theo concrete SDK.",
  "Tạo database client mỗi request; metric cho thấy số connection và handshake tăng theo lưu lượng.",
  "Dùng chung một session mutable; lỗi ngẫu nhiên xuất hiện khi request đồng thời hoặc tenant bị lẫn.",
  "Đưa `Depends` vào domain class; unit test phải chạy dependency solver của FastAPI.",
  "Mock mọi method nội bộ; đổi cách triển khai không đổi hành vi vẫn làm hàng loạt test hỏng.",
  "Cho client chọn implementation bằng chuỗi import; đây là đường thực thi code hoặc SSRF nguy hiểm.",
  "Provider `yield` không rollback/close; pool cạn dần và request sau timeout."
],

tu_kiem_tra: [
  "Vì sao truyền `PostgresRepository` qua constructor chưa chắc đã dependency inversion?",
  "Engine, session, actor và settings nên có lifetime nào trong FastAPI?",
  "Một fake repository phải chia sẻ bộ contract test nào với adapter thật?",
  "Tại sao inject clock tốt hơn vá `datetime.now` ở tên import?",
  "Nếu provider cleanup lỗi trong lúc service cũng lỗi, bạn giữ và log exception nào?",
  "Hãy phá circular dependency giữa booking và notification mà không dùng lazy service locator."
],

tai_nguyen: [
  "Dependency Injection Principles, Practices, and Patterns — Mark Seemann và Steven van Deursen",
  "Architecture Patterns with Python — Harry Percival và Bob Gregory",
  "Clean Architecture — Robert C. Martin",
  "FastAPI Official Documentation"
],

lien_he: [
  "Nối sang mảng Lập trình hướng đối tượng qua Dependency Inversion Principle, Protocol và composition.",
  "Nối sang mảng Kiểm thử qua fake, stub, mock, contract test và failure injection có thể lặp lại.",
  "Nối sang mảng DevOps qua secret management, startup validation, connection pool và vòng đời process.",
  "Nối sang mảng Bảo mật qua tenant context, credential scope và việc không cho đầu vào chọn adapter."
]

};
